// Test: enum-ordinal.from-function
enum Priority
  low
  medium
  high
end 'Priority'

typealias OrdinalValue = int(0 to 100)

function getOrdinal(p Priority) returns OrdinalValue
  return p.ordinal
end 'getOrdinal'

function main() returns ExitCode
  var p = Priority.high
  if getOrdinal(p) == 2 'check'
    return 1
  end 'check'
  return 0
end 'main'
---
ExitCode: 1
---
module {
  func @enum-ordinal.getOrdinal(p: i64) -> u8 {
  entry:
    x86.prologue stack_size=16
    x86.mov [rbp-8], rcx
    x86.mov rax, [rbp-8]
    x86.xor rdx, rdx
    x86.xor rbx, rbx
    x86.cmp rax, rbx
    x86.sete rbx
    x86.movzx rbx, rbxb
    x86.xor rsi, rsi
    x86.test rbx, rbx
    x86.mov rdi, rdx
    x86.cmovne rdi, rsi
    x86.mov rdx, 1
    x86.cmp rax, rdx
    x86.sete rdx
    x86.movzx rdx, rdxb
    x86.mov rbx, 1
    x86.test rdx, rdx
    x86.mov rsi, rdi
    x86.cmovne rsi, rbx
    x86.mov rdx, 2
    x86.cmp rax, rdx
    x86.sete rax
    x86.movzx rax, raxb
    x86.mov rdx, 2
    x86.test rax, rax
    x86.mov rbx, rsi
    x86.cmovne rbx, rdx
    x86.mov [rbp-16], rbx
    x86.xor rax, rax
    x86.cmp rbx, rax
    x86.setl rax
    x86.movzx rax, raxb
    x86.mov rdx, 100
    x86.cmp rbx, rdx
    x86.setg rdx
    x86.movzx rdx, rdxb
    x86.or rax, rdx
    x86.test rax, rax
    x86.je enum-ordinal.getOrdinal.__range_ok_0
  __range_panic_0:
    x86.lea_symdata rax, [__panic_msg_16]
    x86.mov rcx, rax
    x86.call maxon_panic
  __range_ok_0:
    x86.mov rax, [rbp-16]
    x86.epilogue
    x86.ret
  }
  func @enum-ordinal.main() -> u32 {
  entry:
    x86.prologue stack_size=16
    x86.mov rax, 2
    x86.mov [rbp-8], rax
    x86.mov rcx, [rbp-8]
    x86.call enum-ordinal.getOrdinal
    x86.mov rdx, 2
    x86.cmp rax, rdx
    x86.jne enum-ordinal.main.check_0.after
  check_0:
    x86.mov rax, 1
    x86.epilogue
    x86.ret
  check_0.after:
    x86.xor rax, rax
    x86.epilogue
    x86.ret
  }
}
---
