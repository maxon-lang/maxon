// Test: default-throws-no-match
union Color
  red
  green
  blue
end 'Color'

union MatchError
  unmatched
end 'MatchError'

function checkColor(c Color) returns ExitCode throws MatchError
  match c 'check'
    Color.red then return 1
    Color.green then return 2
    default throws MatchError.unmatched
  end 'check'
end 'checkColor'

function main() returns ExitCode
  var c = Color.blue
  var result = try checkColor(c) otherwise 99
  return result
end 'main'
---
ExitCode: 99
---
module {
  func @union-match-only.checkColor(c: i64) -> u32 {
  entry:
    x86.prologue stack_size=16
    x86.mov [rbp-8], rcx
    x86.jmp union-match-only.checkColor.check_0.cmp0
  check_0.cmp0:
    x86.mov rax, [rbp-8]
    x86.xor rcx, rcx
    x86.cmp rax, rcx
    x86.jne union-match-only.checkColor.check_0.cmp1
  check_0.case0:
    x86.mov rax, 1
    x86.xor rdx, rdx
    x86.epilogue
    x86.ret
  check_0.cmp1:
    x86.mov rax, [rbp-8]
    x86.mov rcx, 1
    x86.cmp rax, rcx
    x86.jne union-match-only.checkColor.check_0.case2
  check_0.case1:
    x86.mov rax, 2
    x86.xor rdx, rdx
    x86.epilogue
    x86.ret
  check_0.case2:
    x86.xor rax, rax
    x86.mov rcx, 1
    x86.add rax, rcx
    x86.mov rdx, rax
    x86.xor rax, rax
    x86.epilogue
    x86.ret
  check_0.merge:
  }
  func @union-match-only.main() -> u32 {
  entry:
    x86.prologue stack_size=32
    x86.mov rax, 2
    x86.mov [rbp-8], rax
    x86.mov rcx, [rbp-8]
    x86.call union-match-only.checkColor
    x86.mov ecx, rax
    x86.mov rax, 99
    x86.mov [rbp-16], rax
    x86.mov [rbp-24], rcx
    x86.xor rax, rax
    x86.cmp rdx, rax
    x86.je union-match-only.main.otherwise_default_continue_3
  otherwise_default_error_2:
    x86.mov rax, [rbp-16]
    x86.mov [rbp-24], rax
    x86.jmp union-match-only.main.otherwise_default_continue_3
  otherwise_default_continue_3:
    x86.mov rax, [rbp-24]
    x86.xor rcx, rcx
    x86.cmp rax, rcx
    x86.setl rdx
    x86.movzx rdx, rdxb
    x86.mov rbx, 4294967295
    x86.cmp rax, rbx
    x86.setg rsi
    x86.movzx rsi, rsib
    x86.or rdx, rsi
    x86.test rdx, rdx
    x86.je union-match-only.main.__range_ok_4
  __range_panic_4:
    x86.lea_symdata rax, [__panic_msg_43]
    x86.mov rcx, rax
    x86.call maxon_panic
  __range_ok_4:
    x86.epilogue
    x86.ret
  }
}
---
