// Test: panic.basic
function main() returns ExitCode
		panic("something went wrong")
		return 0
end 'main'
---
ExitCode: 1
Stderr: ```
panic at panic.basic.test:3: something went wrong
Stack trace:
  in main
  in mrt_start
```
---
module {
  func @main() -> u32 {
  entry:
    x86.prologue stack_size=16
    x86.lea rax, [rip+__panic_msg_0]
    x86.mov rcx, rax
    x86.call mrt_panic
    x86.mov [rbp+-8], rax
    x86.mov rax, 0
    x86.epilogue
    x86.ret
  }
}
---
