// Test: float-return-from-function
typealias Float = float(f64.min to f64.max)

function computePi() returns Float
	return 3.14
end 'computePi'

function main() returns ExitCode
	var x = computePi()
	var result = trunc(x)
	return result
end 'main'
---
ExitCode: 3
---
module {
  func @float-return-from-function.computePi() -> f64 {
  entry:
    x86.prologue stack_size=16
    x86.mov rax, 4614253070214989086
    x86.mov [rbp+-8], rax
    x86.movsd xmm0, [rbp+-8]
    x86.epilogue
    x86.ret
  }
  func @main() -> u32 {
  entry:
    x86.prologue stack_size=16
    x86.call float-return-from-function.computePi
    x86.cvttsd2si rax, xmm0
    x86.test rax, rax
    x86.setl rcx
    x86.mov rdx, 4294967295
    x86.cmp rax, rdx
    x86.setg rbx
    x86.or rcx, rbx
    x86.je __range_ok_1
  __range_panic_1:
    x86.lea rax, [rip+__panic_msg_1]
    x86.mov rcx, rax
    x86.call mrt_panic
  __range_ok_1:
    x86.epilogue
    x86.ret
  }
}
---
