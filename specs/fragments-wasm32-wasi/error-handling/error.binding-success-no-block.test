// Test: error.binding-success-no-block

typealias Integer = int(i64.min to i64.max)

// Test that error binding block is skipped on success
enum MyError implements Error
	failed
end 'MyError'

var counter = 0 as Integer

function mayFail(shouldFail bool) returns Integer throws MyError
	counter = counter + 1
	if shouldFail 'check'
		throw MyError.failed
	end 'check'
	return 100
end 'mayFail'

function main() returns ExitCode
	var result = 0
	try mayFail(false) otherwise (e) 'handler'
		match e 'kind'
			failed then result = 99
		end 'kind'
	end 'handler'
	return result
end 'main'
---
ExitCode: 0
---
// CompiledIR
module {
  func @mayFail(local0: i1) -> i64 {
  entry:
    %11 = mir.param local0 : i1
    %1 = mir.global_addr @__data_counter
    %2 = mir.load %1, 0 width: qword
    %3 = mir.mov_imm 1 : i64
    %4 = mir.add.i64 %2, %3
    %5 = mir.global_addr @__data_counter
    mir.store %4, %5, 0 width: qword
    mir.cond_br %11 [then: check_0(), else: check_0.after()]
  check_0:
    %7 = mir.mov_imm 0 : i64
    %8 = mir.mov_imm 1 : i64
    mir.error_return %7, %8
  check_0.after:
    %9 = mir.mov_imm 100 : i64
    %10 = mir.mov_imm 0 : i64
    mir.error_return %9, %10
  }
  func @main() -> u8 {
  entry:
    %0 = mir.mov_imm 0 : i64
    %2, %3 = mir.try_call @mayFail(%0)
    %5 = mir.cmp ne, %3, %0
    mir.cond_br %5 [then: try_0.otherwise(), else: try_0.merge(%0, %2)]
  try_0.otherwise:
    %7 = mir.mov_imm 1 : i64
    %8 = mir.sub.i64 %3, %7
    %11 = mir.cmp eq, %8, %0
    mir.cond_br %11 [then: kind_0.case0(), else: kind_0.merge(%0)]
  kind_0.merge(%16: i64):
    mir.br try_0.merge(%16, %0)
  kind_0.case0:
    %13 = mir.mov_imm 99 : i64
    mir.br kind_0.merge(%13)
  try_0.merge(%17: i64, %18: i64):
    %19 = mir.mov_imm 125 : i64
    %20 = mir.cmp ugt, %17, %19
    mir.cond_br %20 [then: __range_panic_0(), else: __range_ok_0()]
  __range_panic_0:
    %21 = mir.global_addr @__panic_msg_9fda0e963610437a
    %22 = mir.call @mrt_panic(%21)
  __range_ok_0:
    mir.ret %17
  }
}
---
