// Test: type-multiple-methods

typealias Integer = int(i64.min to i64.max)

type Counter
	var count Integer

	function increment()
		count = count + 1
	end 'increment'

	function decrement()
		count = count - 1
	end 'decrement'

	function reset()
		count = 0
	end 'reset'

	function get() returns Integer
		return count
	end 'get'

	static function create(count Integer) returns Self
		return Self{count: count}
	end 'create'
end 'Counter'

function main() returns ExitCode
	var c = Counter.create(count: 10)
	c.increment()
	c.increment()
	c.decrement()
	return c.get()
end 'main'
---
ExitCode: 11
---
// CompiledIR
module {
  func @Counter.increment(local0: i64) {
  entry:
    %7 = mir.param local0 : i64
    %1 = mir.load %7, 0
    %3 = mir.mov_imm 1 : i64
    %4 = mir.add.i64 %1, %3
    mir.store %4, %7, 0
    mir.ret %6
  }
  func @Counter.decrement(local0: i64) {
  entry:
    %7 = mir.param local0 : i64
    %1 = mir.load %7, 0
    %3 = mir.mov_imm 1 : i64
    %4 = mir.sub.i64 %1, %3
    mir.store %4, %7, 0
    mir.ret %6
  }
  func @Counter.get(local0: i64) -> i64 {
  entry:
    %3 = mir.param local0 : i64
    %1 = mir.load %3, 0
    mir.ret %1
  }
  func @Counter.create(local0: i64) -> i64 {
  entry:
    %4 = mir.param local0 : i64
    %1 = mir.mov_imm 8 : i64
    %2 = mir.call @mrt_alloc(%1)
    mir.store %4, %2, 0
    mir.ret %2
  }
  func @type-methods.main() -> u8 {
  entry:
    %0 = mir.mov_imm 10 : i64
    %1 = mir.call @Counter.create(%0)
    %3 = mir.call @Counter.increment(%1)
    %5 = mir.call @Counter.increment(%1)
    %7 = mir.call @Counter.decrement(%1)
    %9 = mir.call @Counter.get(%1)
    %10 = mir.mov_imm 125 : i64
    %11 = mir.cmp ugt, %9, %10
    mir.cond_br %11 [then: __range_panic_3(), else: __range_ok_3()]
  __range_panic_3:
    %12 = mir.global_addr @__panic_msg_3
    %13 = mir.call @mrt_panic(%12)
  __range_ok_3:
    mir.ret %9
  }
}
---
