// Test: return-function-call

typealias Integer = int(i64.min to i64.max)

function getValue() returns Integer
	return 42
end 'getValue'

function main() returns ExitCode
	return getValue()
end 'main'
---
ExitCode: 42
---
// CompiledIR
module {
  func @return-function-call.getValue() -> i64 {
  entry:
    %0 = mir.mov_imm 42 : i64
    mir.ret %0
  }
  func @main() -> u8 {
  entry:
    %0 = mir.call @return-function-call.getValue()
    %1 = mir.mov_imm 125 : i64
    %2 = mir.cmp ugt, %0, %1
    mir.cond_br %2 [then: __range_panic_0(), else: __range_ok_0()]
  __range_panic_0:
    %3 = mir.global_addr @__panic_msg_0
    %4 = mir.call @mrt_panic(%3)
  __range_ok_0:
    mir.ret %0
  }
}
---
