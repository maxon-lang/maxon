// Test: try-otherwise-value-flow.fallback-path-returns-otherwise-value
enum E
	bad
end 'E'

function alwaysThrows(x ExitCode) returns ExitCode throws E
	if x >= 0 'always'
		throw E.bad
	end 'always'
	return x
end 'alwaysThrows'

function main() returns ExitCode
	let v = try alwaysThrows(5) otherwise 99
	return v
end 'main'
---
ExitCode: 99
---
// CompiledIR
module {
  func @alwaysThrows(local0: u8) -> u8 {
  entry:
    %8 = mir.param local0 : u8
    %2 = mir.mov_imm 0 : i64
    %3 = mir.cmp ge, %8, %2
    mir.cond_br %3 [then: always_0(), else: always_0.after()]
  always_0:
    %4 = mir.mov_imm 1 : i64
    %5 = mir.mov_imm 0 : i64
    mir.error_return %5, %4
  always_0.after:
    %7 = mir.mov_imm 0 : i64
    mir.error_return %8, %7
  }
  func @main() -> u8 {
  entry:
    %0 = mir.mov_imm 5 : i64
    %1, %2 = mir.try_call @alwaysThrows(%0)
    %3 = mir.mov_imm 0 : i64
    %4 = mir.cmp ne, %2, %3
    mir.cond_br %4 [then: try_0.otherwise(), else: try_0.merge(%1)]
  try_0.otherwise:
    %6 = mir.mov_imm 99 : i64
    mir.br try_0.merge(%6)
  try_0.merge:
    %10 = mir.mov_imm 125 : i64
    %11 = mir.cmp ugt, %9, %10
    mir.cond_br %11 [then: __range_panic_0(), else: __range_ok_0()]
  __range_panic_0:
    %12 = mir.global_addr @__panic_msg_1
    %13 = mir.call @mrt_panic(%12)
  __range_ok_0:
    mir.ret %9
  }
}
---
