// Test: try-otherwise-value-flow.identity-call-success
enum E
	bad
end 'E'

function ident(x ExitCode) returns ExitCode throws E
	return x
end 'ident'

function main() returns ExitCode
	let v = try ident(7) otherwise 99
	return v
end 'main'
---
ExitCode: 7
---
// CompiledIR
module {
  func @ident(local0: u8) -> u8 {
  entry:
    %3 = mir.param local0 : u8
    %2 = mir.mov_imm 0 : i64
    mir.error_return %3, %2
  }
  func @main() -> u8 {
  entry:
    %0 = mir.mov_imm 7 : i64
    %1, %2 = mir.try_call @ident(%0)
    %3 = mir.mov_imm 0 : i64
    %4 = mir.cmp ne, %2, %3
    mir.cond_br %4 [then: try_0.otherwise(), else: try_0.merge(%1)]
  try_0.otherwise:
    %6 = mir.mov_imm 99 : i64
    mir.br try_0.merge(%6)
  try_0.merge(%9: i64):
    %10 = mir.mov_imm 125 : i64
    %11 = mir.cmp ugt, %9, %10
    mir.cond_br %11 [then: __range_panic_0(), else: __range_ok_0()]
  __range_panic_0:
    %12 = mir.global_addr @__panic_msg_f844260ba83a933f
    %13 = mir.call @mrt_panic(%12)
  __range_ok_0:
    mir.ret %9
  }
}
---
