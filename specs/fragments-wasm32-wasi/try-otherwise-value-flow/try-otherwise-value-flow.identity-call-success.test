// Test: try-otherwise-value-flow.identity-call-success
enum E
	bad
end 'E'

function ident(x ExitCode) returns ExitCode throws E
	return x
end 'ident'

function main() returns ExitCode
	let v = try ident(7) otherwise 99
	return v
end 'main'
---
ExitCode: 7
---
// CompiledIR
module {
  func @ident(local0: u8) -> u8 {
  entry:
    %3 = mir.param local0 : u8
    %2 = mir.mov_imm 0 : i64
    mir.error_return %3, %2
  }
  func @main() -> u8 {
  entry:
    %2 = mir.mov_imm 7 : i64
    %3, %4 = mir.try_call @ident(%2)
    %5 = mir.mov_imm 0 : i64
    %6 = mir.cmp ne, %4, %5
    mir.cond_br %6 [then: try_1.otherwise(), else: try_1.merge(%3)]
  try_1.otherwise:
    %8 = mir.mov_imm 99 : i64
    mir.br try_1.merge(%8)
  try_1.merge:
    %12 = mir.mov_imm 125 : i64
    %13 = mir.cmp ugt, %11, %12
    mir.cond_br %13 [then: __range_panic_2(), else: __range_ok_2()]
  __range_panic_2:
    %14 = mir.global_addr @__panic_msg_2
    %15 = mir.call @mrt_panic(%14)
  __range_ok_2:
    mir.ret %11
  }
}
---
