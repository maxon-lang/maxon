// Test: self-with-params

typealias Integer = int(i64.min to i64.max)

type Accumulator
	var total Integer

	function add(value Integer)
		self.total = self.total + value
	end 'add'

	function getTotal() returns Integer
		return self.total
	end 'getTotal'

	static function create(total Integer) returns Self
		return Self{total: total}
	end 'create'
end 'Accumulator'

function main() returns ExitCode
	let acc = Accumulator.create(total: 0)
	acc.add(10)
	acc.add(20)
	acc.add(12)
	return acc.getTotal()
end 'main'
---
ExitCode: 42
---
// CompiledIR
module {
  func @Accumulator.add(local0: i64, local1: i64) {
  entry:
    %8 = mir.param local0 : i64
    %9 = mir.param local1 : i64
    %3 = mir.load %8, 0
    %5 = mir.add.i64 %3, %9
    mir.store %5, %8, 0
    %7 = mir.mov_imm 0 : i64
    mir.ret %7
  }
  func @Accumulator.getTotal(local0: i64) -> i64 {
  entry:
    %3 = mir.param local0 : i64
    %2 = mir.load %3, 0
    mir.ret %2
  }
  func @Accumulator.create(local0: i64) -> i64 {
  entry:
    %4 = mir.param local0 : i64
    %1 = mir.mov_imm 8 : i64
    %2 = mir.call @mrt_alloc(%1)
    mir.store %4, %2, 0
    mir.ret %2
  }
  func @self-keyword.main() -> u8 {
  entry:
    %0 = mir.mov_imm 0 : i64
    %1 = mir.call @Accumulator.create(%0)
    %3 = mir.mov_imm 10 : i64
    %4 = mir.call @Accumulator.add(%1, %3)
    %6 = mir.mov_imm 20 : i64
    %7 = mir.call @Accumulator.add(%1, %6)
    %9 = mir.mov_imm 12 : i64
    %10 = mir.call @Accumulator.add(%1, %9)
    %12 = mir.call @Accumulator.getTotal(%1)
    %13 = mir.mov_imm 125 : i64
    %14 = mir.cmp ugt, %12, %13
    mir.cond_br %14 [then: __range_panic_2(), else: __range_ok_2()]
  __range_panic_2:
    %15 = mir.global_addr @__panic_msg_2
    %16 = mir.call @mrt_panic(%15)
  __range_ok_2:
    mir.ret %12
  }
}
---
