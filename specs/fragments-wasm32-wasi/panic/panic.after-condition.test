// Test: panic.after-condition
typealias Integer = int(i64.min to i64.max)

function check(n Integer) returns Integer
		if n < 0 'negative'
				panic("negative value")
		end 'negative'
		return n
end 'check'

function main() returns ExitCode
		return check(Integer{-1})
end 'main'
---
ExitCode: 1
Stderr: ```
panic at panic.after-condition.test:6: negative value
Stack trace:
  in panic.check
  in main
  in mrt_start
```
---
// CompiledIR
module {
  func @panic.check(local0: i64) -> i64 {
  entry:
    %7 = mir.param local0 : i64
    %2 = mir.mov_imm 0 : i64
    %3 = mir.cmp lt, %7, %2
    mir.cond_br %3 [then: negative_1(), else: negative_1.after()]
  negative_1:
    %4 = mir.global_addr @__panic_msg_0
    %5 = mir.call @mrt_panic(%4)
    mir.br negative_1.after()
  negative_1.after:
    mir.ret %7
  }
  func @main() -> u8 {
  entry:
    %0 = mir.mov_imm 1 : i64
    %1 = mir.neg.i64 %0
    %2 = mir.call @panic.check(%1)
    %3 = mir.mov_imm 125 : i64
    %4 = mir.cmp ugt, %2, %3
    mir.cond_br %4 [then: __range_panic_0(), else: __range_ok_0()]
  __range_panic_0:
    %5 = mir.global_addr @__panic_msg_1
    %6 = mir.call @mrt_panic(%5)
  __range_ok_0:
    mir.ret %2
  }
}
---
