// Test: exported-struct-var-cross-file
// --- file: state.maxon
typealias SmallInt = int(0 to u8.max)

export type Counter
		export var value SmallInt

		export static function create(value SmallInt) returns Self
			return Self{value: value}
		end 'create'
end 'Counter'

export var shared = Counter.create(value: 0)

// --- file: main.maxon
function main() returns ExitCode
		shared.value = 42
		return shared.value
end 'main'
---
ExitCode: 42
---
// CompiledIR
module {
  func @main() -> u8 {
  entry:
    %0 = mir.mov_imm 42 : i64
    %1 = mir.global_addr @__data_shared
    %2 = mir.load %1, 0 width: qword
    mir.store %0, %2, 0 width: byte
    %3 = mir.global_addr @__data_shared
    %4 = mir.load %3, 0 width: qword
    %5 = mir.load %4, 0 width: byte
    %6 = mir.mov_imm 255 : i64
    %7 = mir.cmp ugt, %5, %6
    mir.cond_br %7 [then: __range_panic_0(), else: __range_ok_0()]
  __range_panic_0:
    %8 = mir.global_addr @__panic_msg_0
    %9 = mir.call @mrt_panic(%8)
  __range_ok_0:
    mir.ret %5
  }
}
---
