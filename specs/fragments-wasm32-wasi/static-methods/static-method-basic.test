// Test: static-method-basic

typealias Integer = int(i64.min to i64.max)

type Box
	export var value as Integer

	static function make(v Integer) returns Box
		return Box{value: v}
	end 'make'
end 'Box'

function main() returns ExitCode
	let b = Box.make(42)
	return b.value
end 'main'
---
ExitCode: 42
---
// CompiledIR
module {
  func @main() -> u8 {
  entry:
    %1 = mir.mov_imm 42 : i64
    %5 = mir.mov_imm 8 : i64
    %6 = mir.mov_imm 0 : i64
    %7 = mir.mov_imm 0 : i64
    %8 = mir.call @stdlib.__mm_alloc(%5, %6, %7)
    mir.store %1, %8, 0 width: qword
    %4 = mir.load %8, 0 width: qword
    %9 = mir.call @mm_drop(%8)
    %10 = mir.mov_imm 125 : i64
    %11 = mir.cmp ugt, %4, %10
    mir.cond_br %11 [then: __range_panic_0(), else: __range_ok_0()]
  __range_panic_0:
    %12 = mir.global_addr @__panic_msg_4165135daf8ef0f0
    %13 = mir.call @mrt_panic(%12)
  __range_ok_0:
    mir.ret %4
  }
}
---
