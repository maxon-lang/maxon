// Test: range-check-panic.in-range
typealias SmallInt = int(0 to 10)

function check(x SmallInt) returns SmallInt
  return x
end 'check'

function main() returns ExitCode
  return check(5)
end 'main'
---
ExitCode: 5
---
// CompiledIR
module {
  func @range-check-panic.check(local0: u8) -> u8 {
  entry:
    %1 = mir.param local0 : u8
    %2 = mir.mov_imm 10 : i64
    %3 = mir.cmp ugt, %1, %2
    mir.cond_br %3 [then: __range_panic_0(), else: __range_ok_0()]
  __range_panic_0:
    %4 = mir.global_addr @__panic_msg_0
    %5 = mir.call @mrt_panic(%4)
  __range_ok_0:
    mir.ret %1
  }
  func @main() -> u8 {
  entry:
    %0 = mir.mov_imm 5 : i64
    %1 = mir.call @range-check-panic.check(%0)
    %6 = mir.mov_imm 125 : i64
    %7 = mir.cmp ugt, %1, %6
    mir.cond_br %7 [then: __range_panic_1(), else: __range_ok_1()]
  __range_panic_1:
    %8 = mir.global_addr @__panic_msg_1
    %9 = mir.call @mrt_panic(%8)
  __range_ok_1:
    mir.ret %1
  }
}
---
