// Test: tuple-as-param

typealias Integer = int(i64.min to i64.max)

function sum(t (Integer, Integer)) returns Integer
	return t.0 + t.1
end 'sum'

function main() returns ExitCode
	let t = (10, 32)
	return sum(t)
end 'main'
---
ExitCode: 42
---
// CompiledIR
module {
  func @main() -> u8 {
  entry:
    %0 = mir.mov_imm 10 : i64
    %1 = mir.mov_imm 32 : i64
    %2 = mir.mov_imm 16 : i64
    %3 = mir.call @mrt_alloc(%2)
    mir.store %0, %3, 0 width: qword
    mir.store %1, %3, 8 width: qword
    %6 = mir.load %3, 0 width: qword
    %7 = mir.load %3, 8 width: qword
    %8 = mir.add.i64 %6, %7
    %9 = mir.mov_imm 125 : i64
    %10 = mir.cmp ugt, %8, %9
    mir.cond_br %10 [then: __range_panic_0(), else: __range_ok_0()]
  __range_panic_0:
    %11 = mir.global_addr @__panic_msg_bba43685af9bb6ba
    %12 = mir.call @mrt_panic(%11)
  __range_ok_0:
    mir.ret %8
  }
}
---
