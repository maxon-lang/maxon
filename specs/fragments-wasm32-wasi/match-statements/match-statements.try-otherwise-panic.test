// Test: match-statements.try-otherwise-panic
enum Err implements Error
	bad
end 'Err'

function ok() throws Err
	return
end 'ok'

function dispatch(kind Tally)
	match kind 'k'
		1 then try ok() otherwise panic("unreachable")
		default panic("unreachable")
	end 'k'
end 'dispatch'

typealias Tally = int(0 to 100)

function main() returns ExitCode
	dispatch(1)
	return 0
end 'main'
---
ExitCode: 0
---
// CompiledIR
module {
  func @dispatch(local0: u8) {
  entry:
    %17 = mir.param local0 : u8
    %2 = mir.mov_imm 1 : i64
    %3 = mir.cmp eq, %17, %2
    mir.cond_br %3 [then: k_0.merge(), else: k_0.case1()]
  k_0.merge:
    mir.ret %4
  k_0.case1:
    %14 = mir.global_addr @__panic_msg_b8addeb5faa064f5
    %15 = mir.call @mrt_panic(%14)
    mir.ret %16
  }
  func @main() -> u8 {
  entry:
    %0 = mir.mov_imm 1 : i64
    %1 = mir.call @dispatch(%0)
    %2 = mir.mov_imm 0 : i64
    mir.ret %2
  }
}
---
