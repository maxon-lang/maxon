// Test: interface-method-via-extended-interface

typealias Integer = int(i64.min to i64.max)

interface Base
	function ping(payload Integer) returns Integer
end 'Base'

interface Extended extends Base
	function other() returns Integer
end 'Extended'

type Impl implements Extended
	let n Integer

	function ping(payload Integer) returns Integer
		return n
	end 'ping'

	function other() returns Integer
		return n + 1
	end 'other'

	static function create(n Integer) returns Self
		return Self{n: n}
	end 'create'
end 'Impl'

function callPing(b Base) returns Integer
	return b.ping(payload: 7)
end 'callPing'

function main() returns ExitCode
	let i = Impl.create(n: 5)
	return callPing(i)
end 'main'
---
ExitCode: 5
---
// CompiledIR
module {
  func @Impl.ping(local0: i64, local1: i64) -> i64 {
  entry:
    %4 = mir.param local0 : i64
    %2 = mir.load %4, 0
    mir.ret %2
  }
  func @Impl.other(local0: i64) -> i64 {
  entry:
    %5 = mir.param local0 : i64
    %1 = mir.load %5, 0
    %3 = mir.mov_imm 1 : i64
    %4 = mir.add.i64 %1, %3
    mir.ret %4
  }
  func @callPing(local0: i64, local1: i64) -> i64 {
  entry:
    %6 = mir.param local0 : i64
    mir.store_slot slot_0, %6
    %7 = mir.param local1 : i64
    mir.store_slot slot_1, %7
    %2 = mir.load_slot slot_0
    %3 = mir.load_slot slot_1
    %4 = mir.mov_imm 7 : i64
    %5 = mir.witness_call %3, [0](%2, %4)
    mir.ret %5
  }
  func @main() -> u8 {
  entry:
    %0 = mir.mov_imm 5 : i64
    %5 = mir.mov_imm 8 : i64
    %6 = mir.call @mrt_alloc(%5)
    mir.store %0, %6, 0
    %3 = mir.global_addr @__witness_Impl_Base
    %4 = mir.call @callPing(%6, %3)
    %7 = mir.mov_imm 125 : i64
    %8 = mir.cmp ugt, %4, %7
    mir.cond_br %8 [then: __range_panic_5(), else: __range_ok_5()]
  __range_panic_5:
    %9 = mir.global_addr @__panic_msg_5
    %10 = mir.call @mrt_panic(%9)
  __range_ok_5:
    mir.ret %4
  }
}
---
