// Test: docs-example-1

typealias Radius = float(f64.min to f64.max)

function circleArea(radius Radius) returns Radius
	return 3.14159 * radius * radius
end 'circleArea'

function main() returns ExitCode
	let area = circleArea(5.0)
	return trunc(area)  // Returns 78
end 'main'
---
ExitCode: 78
---
// CompiledIR
module {
  func @float-type.main() -> u8 {
  entry:
    %0 = mir.mov_imm 4617315517961601024 : f64
    %4 = mir.mov_imm 4614256650576692846 : f64
    %5 = mir.mul.f64 %4, %0
    %6 = mir.mul.f64 %5, %0
    %3 = mir.fptosi.i64 %6
    %7 = mir.mov_imm 125 : i64
    %8 = mir.cmp ugt, %3, %7
    mir.cond_br %8 [then: __range_panic_4(), else: __range_ok_4()]
  __range_panic_4:
    %9 = mir.global_addr @__panic_msg_4
    %10 = mir.call @mrt_panic(%9)
  __range_ok_4:
    mir.ret %3
  }
}
---
