// Test: dispatch-multiple-methods

typealias Integer = int(i64.min to i64.max)

interface Shape
	function area() returns Integer
	function perimeter() returns Integer
end 'Shape'

type Rect implements Shape
	let w Integer
	let h Integer

	function area() returns Integer
		return w * h
	end 'area'

	function perimeter() returns Integer
		return 2 * (w + h)
	end 'perimeter'

	static function create(w Integer, h Integer) returns Self
		return Self{w: w, h: h}
	end 'create'
end 'Rect'

function measure(s Shape) returns Integer
	return s.area() + s.perimeter()
end 'measure'

function main() returns ExitCode
	let r = Rect.create(w: 3, h: 4)
	return measure(r)
end 'main'
---
ExitCode: 26
---
// CompiledIR
module {
  func @Rect.area(local0: i64) -> i64 {
  entry:
    %6 = mir.param local0 : i64
    %1 = mir.load %6, 0 width: qword
    %3 = mir.load %6, 8 width: qword
    %5 = mir.mul.i64 %1, %3
    mir.ret %5
  }
  func @Rect.perimeter(local0: i64) -> i64 {
  entry:
    %8 = mir.param local0 : i64
    %1 = mir.mov_imm 2 : i64
    %2 = mir.load %8, 0 width: qword
    %4 = mir.load %8, 8 width: qword
    %6 = mir.add.i64 %2, %4
    %7 = mir.mul.i64 %1, %6
    mir.ret %7
  }
  func @measure(local0: i64, local1: i64) -> i64 {
  entry:
    %9 = mir.param local0 : i64
    mir.store_slot slot_0, %9
    %10 = mir.param local1 : i64
    mir.store_slot slot_1, %10
    %2 = mir.load_slot slot_0
    %3 = mir.load_slot slot_1
    %4 = mir.witness_call %3, [0](%2)
    %5 = mir.load_slot slot_0
    %6 = mir.load_slot slot_1
    %7 = mir.witness_call %6, [1](%5)
    %8 = mir.add.i64 %4, %7
    mir.ret %8
  }
  func @main() -> u8 {
  entry:
    %0 = mir.mov_imm 3 : i64
    %1 = mir.mov_imm 4 : i64
    %6 = mir.mov_imm 16 : i64
    %7 = mir.call @mrt_alloc(%6)
    mir.store %0, %7, 0 width: qword
    mir.store %1, %7, 8 width: qword
    %4 = mir.global_addr @__witness_Rect_Shape
    %5 = mir.call @measure(%7, %4)
    %8 = mir.mov_imm 255 : i64
    %9 = mir.cmp ugt, %5, %8
    mir.cond_br %9 [then: __range_panic_0(), else: __range_ok_0()]
  __range_panic_0:
    %10 = mir.global_addr @__panic_msg_0
    %11 = mir.call @mrt_panic(%10)
  __range_ok_0:
    mir.ret %5
  }
}
---
