// Test: rc-alias-incref
typealias Integer = int(i64.min to i64.max)

type Box
	export var value as Integer

	static function create(value Integer) returns Self
		return Self{value: value}
	end 'create'
end 'Box'

function main() returns ExitCode
	@heap let a = Box.create(42)
	let b = a
	return b.value
end 'main'
---
ExitCode: 42
---
// CompiledIR
module {
  func @main() -> u8 {
  entry:
    %2 = mir.mov_imm 42 : i64
    %7 = mir.mov_imm 8 : i64
    %8 = mir.mov_imm 0 : i64
    %9 = mir.mov_imm 1 : i64
    %10 = mir.call @stdlib.__mm_alloc_needzero(%7, %8, %9)
    mir.store %2, %10, 0 width: qword
    %6 = mir.load %10, 0 width: qword
    %11 = mir.call @mm_drop(%10)
    %12 = mir.mov_imm 255 : i64
    %13 = mir.cmp ugt, %6, %12
    mir.cond_br %13 [then: __range_panic_0(), else: __range_ok_0()]
  __range_panic_0:
    %14 = mir.global_addr @__panic_msg_66bb751b7fa79bf6
    %15 = mir.call @mrt_panic(%14)
  __range_ok_0:
    mir.ret %6
  }
}
---
