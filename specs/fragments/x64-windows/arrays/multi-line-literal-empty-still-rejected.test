// Test: multi-line-literal-empty-still-rejected
function main() returns ExitCode
	let a = [
	]
	return a.count()
end 'main'
---
CompilerError:
error E2015: <fragment>:3:10: Unsupported: an empty array literal `[]` — its element type cannot be inferred; use `Array with T` + `.create()` for an empty typed array
