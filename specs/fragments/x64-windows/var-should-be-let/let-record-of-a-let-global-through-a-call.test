// Test: let-record-of-a-let-global-through-a-call

typealias Integer = int(i64.min to i64.max)

type Point
	export var x as Integer

	static function create(x Integer) returns Self
		return Self{x: x}
	end 'create'
end 'Point'

let origin = Point.create(1)

function relay(p Point) returns Point
	return p
end 'relay'

function main() returns ExitCode
	var b = relay(origin)
	b.x = 99
	return origin.x
end 'main'
---
CompilerError:
error E3078: <fragment>:20:6: cannot assign the result of 'relay', which may share storage with immutable variable 'origin', to mutable binding 'b'; use 'let' instead of 'var', or use clone()
