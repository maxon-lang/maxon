// Test: float-to-int-field-write-rejected
type Reading
	export var value as Integer = 0

	export static function make() returns Self
		return Self{}
	end 'make'
end 'Reading'

function main() returns ExitCode
	var r = Reading.make()
	r.value = 3.7
	return r.value
end 'main'
typealias Integer = int(i64.min to i64.max)
---
CompilerError:
error E3009: <fragment>:12:4: cannot implicitly convert 'float' to 'int': the conversion is lossy and must be explicit — use trunc(x) to truncate toward zero (or round/floor/ceil)
