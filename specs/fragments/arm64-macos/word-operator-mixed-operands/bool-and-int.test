// Test: bool-and-int
function main() returns ExitCode
	let flag = true
	let r = flag and 4
	if r 'r'
		return 1
	end 'r'
	return 0
end 'main'
---
CompilerError:
error E3005: <fragment>:4:15: operator 'and' requires both operands to be the same type (both bool or both int)
