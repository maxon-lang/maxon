// Test: sin.with-int-promotion
function main() returns ExitCode
  var x = 0  // int
  var result = Math.sin(x)  // x promoted to 0.0
  if result == 0.0 'check'
    return 0
  end 'check'
  return 1
end 'main'
---
ExitCode: 0
---
module {
  func @sin.main() -> u32 {
  entry:
    x86.xor rax, rax
    x86.cvtsi2sd xmm0, rax
    x86.call stdlib.Math.sin
    x86.movsd xmm1, [rip+__float_0]
    x86.ucomisd xmm0, xmm1
    x86.jne sin.main.check_0.after
    x86.jp sin.main.check_0.after
  check_0:
    x86.xor rax, rax
    x86.ret
  check_0.after:
    x86.mov rax, 1
    x86.ret
  }
}
---
