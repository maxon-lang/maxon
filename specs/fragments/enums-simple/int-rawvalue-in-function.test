// Test: int-rawvalue-in-function
enum HttpStatus
  ok = 200
  notFound = 404
end 'HttpStatus'

function getCode(s HttpStatus) returns int
  return s.rawValue
end 'getCode'

function main() returns int
  var status = HttpStatus.notFound
  var code = getCode(status)
  if code == 404 'check'
    return 1
  end 'check'
  return 0
end 'main'
---
ExitCode: 1
---
module {
  func @enums-simple.getCode(s: i64) -> i64 {
  entry:
    x86.mov eax, ecx
    x86.ret
  }
  func @enums-simple.main() -> i64 {
  entry:
    x86.mov eax, 404
    x86.mov rcx, rax
    x86.call enums-simple.getCode
    x86.mov ecx, 404
    x86.cmp eax, ecx
    x86.jne enums-simple.main.check_0.after
  check_0:
    x86.mov eax, 1
    x86.ret
  check_0.after:
    x86.xor eax, eax
    x86.ret
  }
}
---
