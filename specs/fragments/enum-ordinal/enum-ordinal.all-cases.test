// Test: enum-ordinal.all-cases
enum Direction
  north
  south
  east
  west
end 'Direction'

function main() returns ExitCode
  var n = Direction.north
  var s = Direction.south
  var e = Direction.east
  var w = Direction.west
  if n.ordinal == 0 and s.ordinal == 1 and e.ordinal == 2 and w.ordinal == 3 'check'
    return 1
  end 'check'
  return 0
end 'main'
---
ExitCode: 1
---
module {
  func @enum-ordinal.main() -> u32 {
  entry:
    x86.prologue stack_size=32
    x86.xor rax, rax
    x86.mov [rbp-8], rax
    x86.mov rcx, 1
    x86.mov [rbp-16], rcx
    x86.mov rdx, 2
    x86.mov [rbp-24], rdx
    x86.mov rbx, 3
    x86.mov [rbp-32], rbx
    x86.mov rsi, [rbp-8]
    x86.xor rdi, rdi
    x86.xor r8, r8
    x86.cmp rsi, r8
    x86.sete r9
    x86.movzx r9, r9b
    x86.xor rax, rax
    x86.test r9, r9
    x86.mov rcx, rdi
    x86.cmovne rcx, rax
    x86.mov rax, 1
    x86.cmp rsi, rax
    x86.sete rax
    x86.movzx rax, raxb
    x86.mov rdx, 1
    x86.test rax, rax
    x86.mov rbx, rcx
    x86.cmovne rbx, rdx
    x86.mov rax, 2
    x86.cmp rsi, rax
    x86.sete rax
    x86.movzx rax, raxb
    x86.mov rcx, 2
    x86.test rax, rax
    x86.mov rdx, rbx
    x86.cmovne rdx, rcx
    x86.mov rax, 3
    x86.cmp rsi, rax
    x86.sete rax
    x86.movzx rax, raxb
    x86.mov rcx, 3
    x86.test rax, rax
    x86.mov rbx, rdx
    x86.cmovne rbx, rcx
    x86.xor rax, rax
    x86.cmp rbx, rax
    x86.sete rax
    x86.movzx rax, raxb
    x86.mov rcx, [rbp-16]
    x86.xor rdx, rdx
    x86.xor rbx, rbx
    x86.cmp rcx, rbx
    x86.sete rbx
    x86.movzx rbx, rbxb
    x86.xor rsi, rsi
    x86.test rbx, rbx
    x86.mov rdi, rdx
    x86.cmovne rdi, rsi
    x86.mov rdx, 1
    x86.cmp rcx, rdx
    x86.sete rdx
    x86.movzx rdx, rdxb
    x86.mov rbx, 1
    x86.test rdx, rdx
    x86.mov rsi, rdi
    x86.cmovne rsi, rbx
    x86.mov rdx, 2
    x86.cmp rcx, rdx
    x86.sete rdx
    x86.movzx rdx, rdxb
    x86.mov rbx, 2
    x86.test rdx, rdx
    x86.mov rdi, rsi
    x86.cmovne rdi, rbx
    x86.mov rdx, 3
    x86.cmp rcx, rdx
    x86.sete rcx
    x86.movzx rcx, rcxb
    x86.mov rdx, 3
    x86.test rcx, rcx
    x86.mov rbx, rdi
    x86.cmovne rbx, rdx
    x86.mov rcx, 1
    x86.cmp rbx, rcx
    x86.sete rcx
    x86.movzx rcx, rcxb
    x86.and rax, rcx
    x86.mov rcx, [rbp-24]
    x86.xor rdx, rdx
    x86.xor rbx, rbx
    x86.cmp rcx, rbx
    x86.sete rbx
    x86.movzx rbx, rbxb
    x86.xor rsi, rsi
    x86.test rbx, rbx
    x86.mov rdi, rdx
    x86.cmovne rdi, rsi
    x86.mov rdx, 1
    x86.cmp rcx, rdx
    x86.sete rdx
    x86.movzx rdx, rdxb
    x86.mov rbx, 1
    x86.test rdx, rdx
    x86.mov rsi, rdi
    x86.cmovne rsi, rbx
    x86.mov rdx, 2
    x86.cmp rcx, rdx
    x86.sete rdx
    x86.movzx rdx, rdxb
    x86.mov rbx, 2
    x86.test rdx, rdx
    x86.mov rdi, rsi
    x86.cmovne rdi, rbx
    x86.mov rdx, 3
    x86.cmp rcx, rdx
    x86.sete rcx
    x86.movzx rcx, rcxb
    x86.mov rdx, 3
    x86.test rcx, rcx
    x86.mov rbx, rdi
    x86.cmovne rbx, rdx
    x86.mov rcx, 2
    x86.cmp rbx, rcx
    x86.sete rcx
    x86.movzx rcx, rcxb
    x86.and rax, rcx
    x86.mov rcx, [rbp-32]
    x86.xor rdx, rdx
    x86.xor rbx, rbx
    x86.cmp rcx, rbx
    x86.sete rbx
    x86.movzx rbx, rbxb
    x86.xor rsi, rsi
    x86.test rbx, rbx
    x86.mov rdi, rdx
    x86.cmovne rdi, rsi
    x86.mov rdx, 1
    x86.cmp rcx, rdx
    x86.sete rdx
    x86.movzx rdx, rdxb
    x86.mov rbx, 1
    x86.test rdx, rdx
    x86.mov rsi, rdi
    x86.cmovne rsi, rbx
    x86.mov rdx, 2
    x86.cmp rcx, rdx
    x86.sete rdx
    x86.movzx rdx, rdxb
    x86.mov rbx, 2
    x86.test rdx, rdx
    x86.mov rdi, rsi
    x86.cmovne rdi, rbx
    x86.mov rdx, 3
    x86.cmp rcx, rdx
    x86.sete rcx
    x86.movzx rcx, rcxb
    x86.mov rdx, 3
    x86.test rcx, rcx
    x86.mov rbx, rdi
    x86.cmovne rbx, rdx
    x86.mov rcx, 3
    x86.cmp rbx, rcx
    x86.sete rcx
    x86.movzx rcx, rcxb
    x86.and rax, rcx
    x86.test rax, rax
    x86.je enum-ordinal.main.check_0.after
  check_0:
    x86.mov rax, 1
    x86.epilogue
    x86.ret
  check_0.after:
    x86.xor rax, rax
    x86.epilogue
    x86.ret
  }
}
---
