// Test: break.labeled-continue-triple-nested
function main() returns int
    var count = 0
    var a = 0
    while a < 2 'outer'
        a = a + 1
        var b = 0
        while b < 3 'middle'
            b = b + 1
            var c = 0
            while c < 4 'inner'
                c = c + 1
                count = count + 1
                if c == 2 'check'
                    continue 'middle'
                end 'check'
            end 'inner'
        end 'middle'
    end 'outer'
    return count
end 'main'
---
ExitCode: 12
---
func.func @main() -> i64 {
  ^entry:
    x86.prologue stack_size=56 saved_gprs=[RBX,R12,R13,R14,R15]
    x86.mov rax, 0
    x86.lea rcx, qword ptr [rbp-40]
    x86.mov qword ptr [rcx], rax
    x86.mov r8, 2
    x86.mov r9, 3
    x86.mov r10, 1
    x86.mov r11, rax
    x86.jmp while.cond
  ^while.cond(%33: i64):
    x86.cmp r11, r8
    x86.setl rdx
    x86.movzx rdx, rdx
    x86.test rdx, rdx
    x86.jne while.body
    x86.jmp while.exit
  ^while.body:
    x86.mov rdx, 1
    x86.mov rbx, r11
    x86.add rbx, rdx
    x86.mov rdx, 0
    x86.mov r12, rdx
    x86.jmp while.cond_1
  ^while.exit:
    x86.mov rdx, qword ptr [rcx]
    x86.mov rax, rdx
    x86.epilogue saved_gprs=[RBX,R12,R13,R14,R15]
    x86.ret
  ^while.cond_1(%34: i64):
    x86.cmp r12, r9
    x86.setl rdx
    x86.movzx rdx, rdx
    x86.test rdx, rdx
    x86.jne while.body_1
    x86.jmp while.cond.args.from.while.cond_1
    while.cond.args.from.while.cond_1:
    x86.mov r11, rbx
    x86.jmp while.cond
  ^while.body_1:
    x86.mov rdx, 1
    x86.mov r13, r12
    x86.add r13, rdx
    x86.mov rdx, 0
    x86.lea r14, qword ptr [rbp-48]
    x86.mov qword ptr [r14], rdx
    x86.jmp while.cond_2
  ^while.cond_2:
    x86.mov rdx, qword ptr [r14]
    x86.mov rax, 4
    x86.cmp rdx, rax
    x86.setl rax
    x86.movzx rax, rax
    x86.test rax, rax
    x86.jne while.body_2
    x86.jmp while.cond_1.args.from.while.cond_2
    while.cond_1.args.from.while.cond_2:
    x86.mov r12, r13
    x86.jmp while.cond_1
  ^while.body_2:
    x86.mov rax, qword ptr [r14]
    x86.mov rdx, rax
    x86.add rdx, r10
    x86.mov qword ptr [r14], rdx
    x86.mov rdx, qword ptr [rcx]
    x86.mov rax, 1
    x86.mov r15, rdx
    x86.add r15, rax
    x86.mov qword ptr [rcx], r15
    x86.mov rax, qword ptr [r14]
    x86.mov rdx, 2
    x86.cmp rax, rdx
    x86.sete rax
    x86.movzx rax, rax
    x86.test rax, rax
    x86.jne while.cond_1.args.from.while.body_2
    x86.jmp while.cond_2
    while.cond_1.args.from.while.body_2:
    x86.mov r12, r13
    x86.jmp while.cond_1
}
---
