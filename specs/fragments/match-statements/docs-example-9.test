// Test: docs-example-9
typealias Score = int(i64.min to i64.max)

function classify(age Score) returns Score
  match age 'category'
    min upto 0 then return 0       // invalid (negative)
    0 upto 18 then return 1     // minor
    18 to max then return 2       // adult
  end 'category'
end 'classify'

function main() returns ExitCode
  return classify(25)
end 'main'

---
ExitCode: 2
---
module {
  func @match-statements.classify(age: i64) -> i64 {
  entry:
    x86.prologue stack_size=16
    x86.mov [rbp-8], rcx
    x86.jmp match-statements.classify.category_0.cmp0
  category_0.cmp0:
    x86.mov rax, [rbp-8]
    x86.xor rcx, rcx
    x86.cmp rax, rcx
    x86.jge match-statements.classify.category_0.cmp1
  category_0.case0:
    x86.xor rax, rax
    x86.epilogue
    x86.ret
  category_0.cmp1:
    x86.mov rax, [rbp-8]
    x86.xor rcx, rcx
    x86.cmp rax, rcx
    x86.setge rdx
    x86.movzx rdx, rdxb
    x86.mov rbx, [rbp-8]
    x86.mov rsi, 18
    x86.cmp rbx, rsi
    x86.setl rdi
    x86.movzx rdi, rdib
    x86.and rdx, rdi
    x86.test rdx, rdx
    x86.je match-statements.classify.category_0.cmp2
  category_0.case1:
    x86.mov rax, 1
    x86.epilogue
    x86.ret
  category_0.cmp2:
    x86.mov rax, [rbp-8]
    x86.mov rcx, 18
    x86.cmp rax, rcx
    x86.jl match-statements.classify.category_0.merge
  category_0.case2:
    x86.mov rax, 2
    x86.epilogue
    x86.ret
  category_0.merge:
  }
  func @match-statements.main() -> u32 {
  entry:
    x86.prologue stack_size=16
    x86.mov rcx, 25
    x86.call match-statements.classify
    x86.mov [rbp-8], rax
    x86.xor rcx, rcx
    x86.cmp rax, rcx
    x86.setl rcx
    x86.movzx rcx, rcxb
    x86.mov rdx, 4294967295
    x86.cmp rax, rdx
    x86.setg rax
    x86.movzx rax, raxb
    x86.or rcx, rax
    x86.test rcx, rcx
    x86.je match-statements.main.__range_ok_0
  __range_panic_0:
    x86.lea_symdata rax, [__panic_msg_24]
    x86.mov rcx, rax
    x86.call maxon_panic
  __range_ok_0:
    x86.mov rax, [rbp-8]
    x86.epilogue
    x86.ret
  }
}
---
