// Test: first-class-function.pass-as-argument

typealias Integer = int(i64.min to i64.max)

function apply(f (Integer) returns Integer, x Integer) returns Integer
  return f(x)
end 'apply'

function triple(n Integer) returns Integer
  return n * 3
end 'triple'

function main() returns ExitCode
  return apply(triple, x: 10)
end 'main'
---
ExitCode: 30
---
module {
  func @first-class-functions.apply(f: i64, __env_f: i64, x: i64) -> i64 {
  entry:
    x86.prologue stack_size=16
    x86.mov [rbp-8], ecx
    x86.mov r10, ecx
    x86.mov rcx, r8
    x86.call r10
    x86.epilogue
    x86.ret
  }
  func @first-class-functions.triple(n: i64) -> i64 {
  entry:
    x86.mov eax, 3
    x86.imul ecx, eax
    x86.mov eax, ecx
    x86.ret
  }
  func @first-class-functions.main() -> u32 {
  entry:
    x86.prologue stack_size=16
    x86.lea_func eax, [first-class-functions.triple]
    x86.mov ecx, 10
    x86.xor edx, edx
    x86.mov r8, rcx
    x86.mov rcx, rax
    x86.call first-class-functions.apply
    x86.mov [rbp-8], eax
    x86.xor ebx, ebx
    x86.cmp eax, ebx
    x86.setl esi
    x86.movzx esi, esib
    x86.mov rdi, 4294967295
    x86.cmp rax, rdi
    x86.setg r8
    x86.movzx r8, r8b
    x86.or esi, r8
    x86.test esi, esi
    x86.je first-class-functions.main.__range_ok_0
  __range_panic_0:
    x86.lea_symdata rax, [__panic_msg_17]
    x86.mov rcx, rax
    x86.call maxon_panic
  __range_ok_0:
    x86.mov eax, [rbp-8]
    x86.epilogue
    x86.ret
  }
}
---
