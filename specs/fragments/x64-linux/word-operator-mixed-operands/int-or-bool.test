// Test: int-or-bool
function main() returns ExitCode
	let flag = false
	let r = 4 or flag
	if r 'r'
		return 1
	end 'r'
	return 0
end 'main'
---
CompilerError:
error E3005: <fragment>:4:12: operator 'or' requires both operands to be the same type (both bool or both int)
