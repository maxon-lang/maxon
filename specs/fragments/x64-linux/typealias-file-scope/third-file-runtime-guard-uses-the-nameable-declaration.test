// Test: third-file-runtime-guard-uses-the-nameable-declaration
// --- file: alias.maxon
export typealias Codepoint = int(0 to 1114111)

// --- file: lib.maxon
export typealias Integer = int(i64.min to i64.max)

export function lift(n Integer) returns Codepoint
	return n as Codepoint
end 'lift'

export function widen(c Codepoint) returns Codepoint
	return c
end 'widen'

// --- file: main.maxon
typealias Codepoint = int(0 to 100)
typealias Integer = int(i64.min to i64.max)

function opaque(n Integer) returns Integer
	return n
end 'opaque'

function narrow(c Codepoint) returns Integer
	return c
end 'narrow'

function main() returns ExitCode
	let big = opaque(70000)
	return (((widen(lift(big)) / 1000) as Integer) - narrow(28)) as ExitCode
end 'main'
---
ExitCode: 42
---
func @main {
  entry:
    x64.pushReg rbp
    x64.movRegReg rbp, rsp
    x64.prologue 32
  __rc_ok#10:
    x64.movRegImm32 rcx, 70000
  __il_cont#5:
    x64.movRegImm rax, 442721857769029239
    x64.mulHighReg rcx
    x64.subRegReg rcx, rcx, rdx
    x64.shrRegImm8 rcx, rcx, 1
    x64.leaRegRegReg rax, rcx, rdx
    x64.shrRegImm8 rax, rax, 9
  __il_cont#4:
    x64.leaRegRegImm32 r8, rax, -28
    x64.cmpRegImm32 r8, 0
    x64.jcc less, __rc_panic
  __rc_chk:
    x64.cmpRegImm32 r8, 255
    x64.jcc greater, __rc_panic
  __rc_ok#1:
    x64.epilogue 32
    x64.popReg rbp
    x64.ret
  __rc_panic:
    x64.leaRegRdata rcx, [rip + __str_blob_3]  ; "panic at main.maxon:14: Range check failed: value outside typealias 'ExitCode'\x0a"
    x64.callDirect mrt_panic
    x64.movRegImm32 r8, 0
    x64.epilogue 32
    x64.popReg rbp
    x64.ret
}
