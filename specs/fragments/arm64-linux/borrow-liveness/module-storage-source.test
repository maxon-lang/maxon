// Test: module-storage-source
var g = ["hello world this is a long string for heap allocation"]

function main() returns ExitCode
	let s = try g.get(0) otherwise ""
	g.clear()
	print("[{s}]\n")
	return 0
end 'main'
---
CompilerError:
error E3070: <fragment>:6:4: cannot mutate 'g' via 'clear' while it is borrowed by 's' (borrowed at line 5)
