// Test: error.or-chain.overlap
typealias Integer = int(i64.min to i64.max)

union Op
	add
	sub
	mul
	exec(code Integer)
end 'Op'

function main() returns ExitCode
	let op = Op.add
	match op 'check'
		add or
			sub then return 1
		sub or
			mul then return 2
		exec(code) then return code
	end 'check'
end 'main'
---
CompilerError:
error E2027: <fragment>:16:3: overlapping pattern in match: 'sub' is already covered
