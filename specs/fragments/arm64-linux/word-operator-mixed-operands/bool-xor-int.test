// Test: bool-xor-int
function main() returns ExitCode
	let flag = true
	let r = flag xor 4
	if r 'r'
		return 1
	end 'r'
	return 0
end 'main'
---
CompilerError:
error E3005: <fragment>:4:15: operator 'xor' requires both operands to be the same type (both bool or both int)
