// Test: int-var-to-float-param

typealias Integer = int(i64.min to i64.max)
typealias Float = float(f64.min to f64.max)

function takeFloat(x Float) returns Integer
	return trunc(x)
end 'takeFloat'

function main() returns ExitCode
	let i = 42
	return takeFloat(i)
end 'main'
---
ExitCode: 42
---
// CompiledIR
module {
  func @implicit-type-conversion.takeFloat(x: f64) -> i64 {
  entry:
    x64.cvttsd2si rax, xmm0
    x64.ret
  }
  func @implicit-type-conversion.main() -> u32 {
  entry:
    x64.prologue stack_size=16
    x64.mov rax, 42
    x64.cvtsi2sd xmm0, rax
    x64.call implicit-type-conversion.takeFloat
    x64.xor ecx, ecx
    x64.mov edx, 4294967295
    x64.cmp rax, rdx
    x64.jg implicit-type-conversion.main.__range_panic_0
    x64.cmp rax, rcx
    x64.jl implicit-type-conversion.main.__range_panic_0
    x64.jmp implicit-type-conversion.main.__range_ok_0
  __range_panic_0:
    x64.lea_symdata rax, [__panic_msg_0]
    x64.mov rcx, rax
    x64.call mrt_panic
  __range_ok_0:
    x64.epilogue
    x64.ret
  }
}
---
