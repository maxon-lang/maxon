// Test: floor.with-ceil
function main() returns ExitCode
	let x = 3.7
	let a = trunc(floor(x))
	let b = trunc(ceil(x))
	return a + b
end 'main'
---
ExitCode: 7
---
// CompiledIR
module {
  func @floor.main() -> u32 {
  entry:
    x64.prologue stack_size=16
    x64.movsd xmm0, [rip+__float_3.7]
    x64.roundsd xmm1, xmm0, 1
    x64.cvttsd2si rax, xmm1
    x64.roundsd xmm2, xmm0, 2
    x64.cvttsd2si rcx, xmm2
    x64.add rax, rcx
    x64.xor edx, edx
    x64.mov ebx, 4294967295
    x64.cmp rax, rbx
    x64.jg floor.main.__range_panic_0
    x64.cmp rax, rdx
    x64.jl floor.main.__range_panic_0
    x64.jmp floor.main.__range_ok_0
  __range_panic_0:
    x64.lea_symdata rax, [__panic_msg_2]
    x64.mov rcx, rax
    x64.call mrt_panic
  __range_ok_0:
    x64.epilogue
    x64.ret
  }
}
---
