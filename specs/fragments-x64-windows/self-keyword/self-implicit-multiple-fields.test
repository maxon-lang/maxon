// Test: self-implicit-multiple-fields

typealias Integer = int(i64.min to i64.max)

type Rectangle
	var width Integer
	var height Integer

	function area() returns Integer
		return width * height
	end 'area'

	static function create(width Integer, height Integer) returns Self
		return Self{width: width, height: height}
	end 'create'
end 'Rectangle'

function main() returns ExitCode
	let r = Rectangle.create(width: 6, height: 7)
	return r.area()
end 'main'
---
ExitCode: 42
---
// CompiledIR
module {
  func @Rectangle.area(rcx: i64) -> i64 {
  entry:
    x64.mov r9, [rcx+8] (8b)
    x64.mov r8, [rcx+0] (8b)
    x64.imul r8, r9
    x64.ret
  }
  func @Rectangle.create(rcx: i64, rdx: i64) -> i64 {
  entry:
    x64.prologue stack_size=16
    x64.mov r12, rcx
    x64.mov r13, rdx
    x64.mov ecx, 16
    x64.call mrt_alloc
    x64.mov [r8+0], r12 (8b)
    x64.mov [r8+8], r13 (8b)
    x64.epilogue
    x64.ret
  }
  func @self-keyword.main() -> u8 {
  entry:
    x64.prologue stack_size=16
    x64.mov edx, 7
    x64.mov ecx, 6
    x64.call Rectangle.create
    x64.mov rcx, r8
    x64.call Rectangle.area
    x64.cmp r8, 125
    x64.jbe __range_ok_5
  __range_panic_5:
    x64.lea rcx, [rip+__panic_msg_5]
    x64.call mrt_panic
  __range_ok_5:
    x64.epilogue
    x64.ret
  }
}
---
