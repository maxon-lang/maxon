// Test: div-with-call

typealias Integer = int(i64.min to i64.max)

function helper(x Integer) returns Integer
	return x * 2
end 'helper'

function divCall(a Integer, b Integer) returns Integer
	let temp = trunc(a / b)
	let result = helper(temp)
	return result + temp
end 'divCall'

function main() returns ExitCode
	return divCall(10, b: 2)
end 'main'
---
ExitCode: 15
---
// CompiledIR
module {
  func @arithmetic.helper(x: i64) -> i64 {
  entry:
    x64.mov rax, 2
    x64.imul rcx, rax
    x64.mov rax, rcx
    x64.ret
  }
  func @arithmetic.divCall(a: i64, b: i64) -> i64 {
  entry:
    x64.prologue stack_size=16
    x64.mov rbx, rdx
    x64.mov [rbp-8], rdx
    x64.mov rax, rcx
    x64.cqo
    x64.idiv rbx
    x64.mov [rbp-16], rax
    x64.mov rcx, [rbp-16]
    x64.call arithmetic.helper
    x64.mov rcx, [rbp-16]
    x64.add rax, rcx
    x64.epilogue
    x64.ret
  }
  func @arithmetic.main() -> u32 {
  entry:
    x64.prologue stack_size=16
    x64.mov rcx, 10
    x64.mov rdx, 2
    x64.call arithmetic.divCall
    x64.xor ecx, ecx
    x64.mov edx, 4294967295
    x64.cmp rax, rdx
    x64.jg arithmetic.main.__range_panic_0
    x64.cmp rax, rcx
    x64.jl arithmetic.main.__range_panic_0
    x64.jmp arithmetic.main.__range_ok_0
  __range_panic_0:
    x64.lea_symdata rax, [__panic_msg_9]
    x64.mov rcx, rax
    x64.call mrt_panic
  __range_ok_0:
    x64.epilogue
    x64.ret
  }
}
---
