// Test: div-live-values

typealias Integer = int(i64.min to i64.max)

function divLive(a Integer, b Integer, x Integer) returns Integer
	let preserved = x + 1
	let result = a / b
	return trunc(result + preserved)
end 'divLive'

function main() returns ExitCode
	return divLive(10, b: 2, x: 5)
end 'main'
---
ExitCode: 11
---
// CompiledIR
module {
  func @div-live-values.divLive(rcx: i64, rdx: i64, rax: i64) -> i64 {
  entry:
    x64.prologue stack_size=32
    x64.mov rbx, rcx
    x64.mov rsi, rdx
    x64.mov rdi, rax
    x64.mov rax, rbx
    x64.cqo
    x64.idiv rsi
    x64.mov rbx, rdi
    x64.add rbx, 1
    x64.mov rsi, rax
    x64.add rsi, rbx
    x64.mov r8, rsi
    x64.epilogue
    x64.ret
  }
  func @main() -> u8 {
  entry:
    x64.prologue stack_size=16
    x64.mov eax, 5
    x64.mov edx, 2
    x64.mov ecx, 10
    x64.call div-live-values.divLive
    x64.cmp r8, 125
    x64.jbe __range_ok_0
  __range_panic_0:
    x64.lea rcx, [rip+__panic_msg_0]
    x64.call mrt_panic
  __range_ok_0:
    x64.epilogue
    x64.ret
  }
}
---
