// Test: mod-live-values

typealias Integer = int(i64.min to i64.max)

function modLive(a Integer, b Integer, x Integer) returns Integer
	let preserved = x + 1
	let result = a mod b
	return result + preserved
end 'modLive'

function main() returns ExitCode
	return modLive(10, b: 3, x: 5)
end 'main'
---
ExitCode: 7
---
module {
  func @mod-live-values.modLive(rcx: i64, rdx: i64, rax: i64) -> i64 {
  entry:
    x64.prologue stack_size=32
    x64.mov rbx, rcx
    x64.mov rsi, rdx
    x64.mov rdi, rax
    x64.mov rax, rbx
    x64.cqo
    x64.idiv rsi
    x64.mov rax, rdi
    x64.add rax, 1
    x64.mov rbx, rdx
    x64.add rbx, rax
    x64.mov r8, rbx
    x64.epilogue
    x64.ret
  }
  func @main() -> u8 {
  entry:
    x64.prologue stack_size=16
    x64.mov eax, 5
    x64.mov edx, 3
    x64.mov ecx, 10
    x64.call mod-live-values.modLive
    x64.cmp r8, 125
    x64.jbe __range_ok_0
  __range_panic_0:
    x64.lea rcx, [rip+__panic_msg_0]
    x64.call mrt_panic
  __range_ok_0:
    x64.epilogue
    x64.ret
  }
}
---
