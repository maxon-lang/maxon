// Test: with-function-call

typealias Integer = int(i64.min to i64.max)

function main() returns ExitCode
	let x = 3
	return 5 + add(x, b: 4)
end 'main'

function add(a Integer, b Integer) returns Integer
	return a + b
end 'add'
---
ExitCode: 12
---
// CompiledIR
module {
  func @main() -> u8 {
  entry:
    x64.prologue stack_size=32
    x64.mov edx, 4
    x64.mov ecx, 3
    x64.call with-function-call.add
    x64.mov ebx, 5
    x64.add rbx, r8
    x64.cmp rbx, 125
    x64.jbe __range_ok_0
  __range_panic_0:
    x64.lea rcx, [rip+__panic_msg_0]
    x64.call mrt_panic
  __range_ok_0:
    x64.mov r8, rbx
    x64.epilogue
    x64.ret
  }
  func @with-function-call.add(rcx: i64, rdx: i64) -> i64 {
  entry:
    x64.prologue stack_size=16
    x64.mov rbx, rcx
    x64.mov rcx, rbx
    x64.add rcx, rdx
    x64.mov r8, rcx
    x64.epilogue
    x64.ret
  }
}
---
