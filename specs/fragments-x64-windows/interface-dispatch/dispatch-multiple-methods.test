// Test: dispatch-multiple-methods

typealias Integer = int(i64.min to i64.max)

interface Shape
	function area() returns Integer
	function perimeter() returns Integer
end 'Shape'

type Rect implements Shape
	let w Integer
	let h Integer

	function area() returns Integer
		return w * h
	end 'area'

	function perimeter() returns Integer
		return 2 * (w + h)
	end 'perimeter'

	static function create(w Integer, h Integer) returns Self
		return Self{w: w, h: h}
	end 'create'
end 'Rect'

function measure(s Shape) returns Integer
	return s.area() + s.perimeter()
end 'measure'

function main() returns ExitCode
	let r = Rect.create(3, h: 4)
	return measure(r)
end 'main'
---
ExitCode: 26
---
// CompiledIR
module {
  func @Rect.area(rcx: i64) -> i64 {
  entry:
    x64.mov r9, [rcx+8] (8b)
    x64.mov r8, [rcx+0] (8b)
    x64.imul r8, r9
    x64.ret
  }
  func @Rect.perimeter(rcx: i64) -> i64 {
  entry:
    x64.mov r8, [rcx+8] (8b)
    x64.mov r9, [rcx+0] (8b)
    x64.mov esi, 2
    x64.add r9, r8
    x64.mov r8, r9
    x64.imul r8, rsi
    x64.ret
  }
  func @measure(rcx: i64, rdx: i64) -> i64 {
  entry:
    x64.prologue stack_size=32
    x64.mov [rbp-8], rcx
    x64.mov [rbp-16], rdx
    x64.mov r8, [rbp-16]
    x64.mov rcx, [rbp-8]
    x64.mov r10, [r8+24] (8b)
    x64.call r10
    x64.mov r12, r8
    x64.mov r8, [rbp-16]
    x64.mov rcx, [rbp-8]
    x64.mov r10, [r8+32] (8b)
    x64.call r10
    x64.mov r9, r12
    x64.add r9, r8
    x64.mov r8, r9
    x64.epilogue
    x64.ret
  }
  func @main() -> u32 {
  entry:
    x64.prologue stack_size=16
    x64.mov ecx, 16
    x64.call mrt_alloc
    x64.mov r9d, 3
    x64.mov [r8+0], r9 (8b)
    x64.mov r9d, 4
    x64.mov [r8+8], r9 (8b)
    x64.lea rdx, [rip+__witness_Rect_Shape]
    x64.mov rcx, r8
    x64.call measure
    x64.mov r9d, 4294967295
    x64.cmp r8, r9
    x64.jbe __range_ok_0
  __range_panic_0:
    x64.lea r12, [rip+__panic_msg_0]
    x64.mov rcx, r12
    x64.call mrt_panic
  __range_ok_0:
    x64.epilogue
    x64.ret
  }
}
---
