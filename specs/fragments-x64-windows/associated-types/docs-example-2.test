// Test: docs-example-2
typealias Score = int(i64.min to i64.max)

interface Summable uses Element
	function sum() returns Element
end 'Summable'

type ScorePair implements Summable with Score
	let a as Score
	let b as Score

	function sum() returns Score
		return a + b
	end 'sum'

	static function create(a Score, b Score) returns Self
		return Self{a: a, b: b}
	end 'create'
end 'ScorePair'

function main() returns ExitCode
	let p = ScorePair.create(10, b: 32)
	return p.sum()
end 'main'
---
ExitCode: 42
---
// CompiledIR
module {
  func @main() -> u32 {
  entry:
    x64.prologue stack_size=16
    x64.mov eax, 1
    x64.xor edx, edx
    x64.mov ecx, 16
    x64.call stdlib.__mm_alloc_needzero
    x64.mov r9d, 10
    x64.mov [r8+0], r9 (8b)
    x64.mov r9d, 32
    x64.mov [r8+8], r9 (8b)
    x64.mov rbx, [r8+8] (8b)
    x64.mov r12, [r8+0] (8b)
    x64.mov rcx, r8
    x64.call mm_drop
    x64.mov r9d, 4294967295
    x64.mov r8, r12
    x64.add r8, rbx
    x64.cmp r8, r9
    x64.jbe __range_ok_0
  __range_panic_0:
    x64.lea rcx, [rip+__panic_msg_e27b031ff626f117]
    x64.call mrt_panic
  __range_ok_0:
    x64.epilogue
    x64.ret
  }
}
---
