// Test: type-as-parameter-name-crossfile
// --- file: shared.maxon
export typealias StdType = int(i64.min to i64.max)

// --- file: helper.maxon
export function identity(type StdType) returns StdType
	return type
end 'identity'

// --- file: main.maxon
function main() returns ExitCode
	return identity(42)
end 'main'
---
ExitCode: 42
---
// CompiledIR
module {
  func @helper.identity(type: i64) -> i64 {
  entry:
    x64.mov rax, rcx
    x64.ret
  }
  func @main() -> u32 {
  entry:
    x64.prologue stack_size=16
    x64.mov rcx, 42
    x64.call helper.identity
    x64.xor ecx, ecx
    x64.mov edx, 4294967295
    x64.cmp rax, rdx
    x64.jg main.__range_panic_0
    x64.cmp rax, rcx
    x64.jl main.__range_panic_0
    x64.jmp main.__range_ok_0
  __range_panic_0:
    x64.lea_symdata rax, [__panic_msg_0]
    x64.mov rcx, rax
    x64.call mrt_panic
  __range_ok_0:
    x64.epilogue
    x64.ret
  }
}
---
