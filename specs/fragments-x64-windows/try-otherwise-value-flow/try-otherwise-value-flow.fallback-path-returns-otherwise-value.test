// Test: try-otherwise-value-flow.fallback-path-returns-otherwise-value
enum E
	bad
end 'E'

function alwaysThrows(x ExitCode) returns ExitCode throws E
	if x >= 0 'always'
		throw E.bad
	end 'always'
	return x
end 'alwaysThrows'

function main() returns ExitCode
	let v = try alwaysThrows(5) otherwise 99
	return v
end 'main'
---
ExitCode: 99
---
// CompiledIR
module {
  func @try-otherwise-value-flow.alwaysThrows(x: i64) -> u32 {
  entry:
    x64.prologue stack_size=16
    x64.mov [rbp-8], rcx
    x64.xor eax, eax
    x64.cmp rcx, rax
    x64.jl try-otherwise-value-flow.alwaysThrows.always_0.after
  always_0:
    x64.xor eax, eax
    x64.mov rcx, 1
    x64.add rax, rcx
    x64.mov rdx, rax
    x64.xor eax, eax
    x64.epilogue
    x64.ret
  always_0.after:
    x64.mov rax, [rbp-8]
    x64.xor ecx, ecx
    x64.mov edx, 4294967295
    x64.cmp rax, rdx
    x64.jg try-otherwise-value-flow.alwaysThrows.__range_panic_0
    x64.cmp rax, rcx
    x64.jl try-otherwise-value-flow.alwaysThrows.__range_panic_0
    x64.jmp try-otherwise-value-flow.alwaysThrows.__range_ok_0
  __range_panic_0:
    x64.lea_symdata rax, [__panic_msg_0]
    x64.mov rcx, rax
    x64.call mrt_panic
  __range_ok_0:
    x64.xor edx, edx
    x64.epilogue
    x64.ret
  }
  func @try-otherwise-value-flow.main() -> u32 {
  entry:
    x64.prologue stack_size=16
    x64.mov rcx, 5
    x64.call try-otherwise-value-flow.alwaysThrows
    x64.mov ecx, rax
    x64.mov rax, 99
    x64.mov [rbp-8], rax
    x64.mov [rbp-16], rcx
    x64.xor ecx, ecx
    x64.cmp rdx, rcx
    x64.je try-otherwise-value-flow.main.otherwise_default_continue_0
  otherwise_default_error_0:
    x64.mov rax, [rbp-8]
    x64.mov [rbp-16], rax
    x64.jmp try-otherwise-value-flow.main.otherwise_default_continue_0
  otherwise_default_continue_0:
    x64.mov rax, [rbp-16]
    x64.xor ecx, ecx
    x64.mov edx, 4294967295
    x64.cmp rax, rdx
    x64.jg try-otherwise-value-flow.main.__range_panic_0
    x64.cmp rax, rcx
    x64.jl try-otherwise-value-flow.main.__range_panic_0
    x64.jmp try-otherwise-value-flow.main.__range_ok_0
  __range_panic_0:
    x64.lea_symdata rax, [__panic_msg_1]
    x64.mov rcx, rax
    x64.call mrt_panic
  __range_ok_0:
    x64.epilogue
    x64.ret
  }
}
---
