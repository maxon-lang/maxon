// Test: float-type-disambiguation
typealias Integer = int(i64.min to i64.max)
typealias Decimal = float(f64.min to f64.max)

function measure(value Integer) returns Integer
	return value
end 'measure'

function measure(value Decimal) returns Integer
	return trunc(value)
end 'measure'

function main() returns ExitCode
	return measure(42.0)
end 'main'
---
ExitCode: 42
---
// CompiledIR
module {
  func @function-overloads.measure$value_f64(value: f64) -> i64 {
  entry:
    x64.cvttsd2si rax, xmm0
    x64.ret
  }
  func @function-overloads.main() -> u32 {
  entry:
    x64.prologue stack_size=16
    x64.movsd xmm0, [rip+__float_42]
    x64.call function-overloads.measure$value_f64
    x64.xor ecx, ecx
    x64.mov edx, 4294967295
    x64.cmp rax, rdx
    x64.jg function-overloads.main.__range_panic_0
    x64.cmp rax, rcx
    x64.jl function-overloads.main.__range_panic_0
    x64.jmp function-overloads.main.__range_ok_0
  __range_panic_0:
    x64.lea_symdata rax, [__panic_msg_0]
    x64.mov rcx, rax
    x64.call mrt_panic
  __range_ok_0:
    x64.epilogue
    x64.ret
  }
}
---
