// Test: panic.after-condition
typealias Integer = int(i64.min to i64.max)

function check(n Integer) returns Integer
		if n < 0 'negative'
				panic("negative value")
		end 'negative'
		return n
end 'check'

function main() returns ExitCode
		return check(Integer{-1})
end 'main'
---
ExitCode: 1
Stderr: ```
panic at panic.after-condition.test:6: negative value
Stack trace:
  in panic.check
  in main
  in mrt_start
```
---
module {
  func @panic.check(ptr: i64) -> i64 {
  entry:
    x64.prologue stack_size=16
    x64.test rcx, rcx
    x64.jge negative_1.after
  negative_1:
    x64.lea rax, [rip+__panic_msg_3]
    x64.mov rcx, rax
    x64.call mrt_panic
  negative_1.after:
    x64.mov rax, -9223372036854775807
    x64.cmp rcx, rax
    x64.jge __range_ok_0
  __range_panic_0:
    x64.lea rax, [rip+__panic_msg_0]
    x64.mov rcx, rax
    x64.call mrt_panic
  __range_ok_0:
    x64.mov rax, rcx
    x64.epilogue
    x64.ret
  }
  func @main() -> u8 {
  entry:
    x64.prologue stack_size=16
    x64.mov rax, 1
    x64.mov rcx, rax
    x64.neg rcx
    x64.mov [rbp+-8], rcx
    x64.call panic.check
    x64.cmp rax, 125
    x64.jbe __range_ok_1
  __range_panic_1:
    x64.lea rax, [rip+__panic_msg_1]
    x64.mov rcx, rax
    x64.call mrt_panic
  __range_ok_1:
    x64.epilogue
    x64.ret
  }
}
---
