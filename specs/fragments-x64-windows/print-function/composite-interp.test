// Test: composite-interp
function main() returns ExitCode
	let x = 42
	print("value is {x} and doubled is {x + x}\n")
	return 0
end 'main'
---
ExitCode: 0
Stdout: ```
value is 42 and doubled is 84
```
---
// CompiledIR
module {
  func @main() -> u8 {
  entry:
    x64.prologue stack_size=32
    x64.mov edx, 9
    x64.lea rcx, [rip+__str_0]
    x64.call mrt_write_stdout
    x64.mov ecx, 42
    x64.call mrt_printInt
    x64.mov edx, 16
    x64.lea rcx, [rip+__str_1]
    x64.call mrt_write_stdout
    x64.mov ecx, 84
    x64.call mrt_printInt
    x64.mov edx, 1
    x64.lea rcx, [rip+__str_2]
    x64.call mrt_write_stdout
    x64.xor r8d, r8d
    x64.epilogue
    x64.ret
  }
}
---
