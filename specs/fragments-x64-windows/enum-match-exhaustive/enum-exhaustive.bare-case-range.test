// Test: enum-exhaustive.bare-case-range
enum Priority
		low
		medium
		high
		critical
end 'Priority'

function main() returns ExitCode
	let p = Priority.high
	match p 'check'
		low to medium then return 1
		high to critical then return 2
	end 'check'
end 'main'
---
ExitCode: 2
---
// CompiledIR
module {
  func @main() -> u32 {
  entry:
    x64.prologue stack_size=16
    x64.mov rax, 2
    x64.mov [rbp-8], rax
    x64.mov rcx, [rbp-8]
    x64.mov rdx, 1
    x64.cmp rcx, rdx
    x64.jbe main.check_0.case0
  check_0.dispatch0:
    x64.mov rax, 2
    x64.mov rcx, [rbp-8]
    x64.sub rcx, rax
    x64.mov rdx, 1
    x64.cmp rcx, rdx
    x64.jbe main.check_0.case1
  check_0.dispatch1:
    x64.jmp main.check_0.merge
  check_0.case0:
    x64.mov rax, 1
    x64.epilogue
    x64.ret
  check_0.case1:
    x64.mov rax, 2
    x64.epilogue
    x64.ret
  check_0.merge:
  }
}
---
