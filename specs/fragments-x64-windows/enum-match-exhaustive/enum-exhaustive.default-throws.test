// Test: enum-exhaustive.default-throws
enum Color
		red
		green
		blue
end 'Color'

enum AppError
		unmatched
end 'AppError'

typealias ColorCode = int(0 to 10)

function checkColor(c Color) returns ColorCode throws AppError
	match c 'check'
		red then return 1
		default throws AppError.unmatched
	end 'check'
end 'checkColor'

function main() returns ExitCode
	let result = try checkColor(Color.red) otherwise 'err'
		return 99
	end 'err'
	return result
end 'main'
---
ExitCode: 1
---
// CompiledIR
module {
  func @checkColor(rcx: u8) -> u8 {
  entry:
    x64.test rcx, rcx
    x64.jne check_0.case1
  check_0.case0:
    x64.xor edx, edx
    x64.mov r8d, 1
    x64.ret
  check_0.case1:
    x64.mov edx, 1
    x64.xor r8d, r8d
    x64.ret
  }
  func @main() -> u32 {
  entry:
    x64.prologue stack_size=16
    x64.xor ecx, ecx
    x64.call checkColor
    x64.mov r9, r8
    x64.test rdx, rdx
    x64.je try_0.merge
  try_0.otherwise:
    x64.mov r8d, 99
    x64.epilogue
    x64.ret
  try_0.merge:
    x64.mov r8d, 4294967295
    x64.cmp r9, r8
    x64.jbe __range_ok_0
  __range_panic_0:
    x64.lea r12, [rip+__panic_msg_af1f86f955945541]
    x64.mov rcx, r12
    x64.call mrt_panic
  __range_ok_0:
    x64.mov r8, r9
    x64.epilogue
    x64.ret
  }
}
---
