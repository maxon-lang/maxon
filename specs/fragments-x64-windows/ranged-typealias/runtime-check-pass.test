// Test: runtime-check-pass
typealias Integer = int(i64.min to i64.max)
typealias Age = int(0 to 150)

function makeAge(n Integer) returns Integer
	let a = n as Age
	return a
end 'makeAge'

function main() returns ExitCode
	return makeAge(50)
end 'main'
---
ExitCode: 50
---
// CompiledIR
module {
  func @ranged-typealias.makeAge(n: i64) -> i64 {
  entry:
    x64.prologue stack_size=16
    x64.xor eax, eax
    x64.mov rdx, 150
    x64.cmp rcx, rdx
    x64.jg ranged-typealias.makeAge.__range_panic_0
    x64.cmp rcx, rax
    x64.jl ranged-typealias.makeAge.__range_panic_0
    x64.jmp ranged-typealias.makeAge.__range_ok_0
  __range_panic_0:
    x64.lea_symdata rax, [__panic_msg_5]
    x64.mov rcx, rax
    x64.call mrt_panic
  __range_ok_0:
    x64.mov rax, rcx
    x64.epilogue
    x64.ret
  }
  func @ranged-typealias.main() -> u32 {
  entry:
    x64.prologue stack_size=16
    x64.mov rcx, 50
    x64.call ranged-typealias.makeAge
    x64.xor ecx, ecx
    x64.mov edx, 4294967295
    x64.cmp rax, rdx
    x64.jg ranged-typealias.main.__range_panic_0
    x64.cmp rax, rcx
    x64.jl ranged-typealias.main.__range_panic_0
    x64.jmp ranged-typealias.main.__range_ok_0
  __range_panic_0:
    x64.lea_symdata rax, [__panic_msg_6]
    x64.mov rcx, rax
    x64.call mrt_panic
  __range_ok_0:
    x64.epilogue
    x64.ret
  }
}
---
