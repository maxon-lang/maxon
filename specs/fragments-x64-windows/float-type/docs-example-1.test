// Test: docs-example-1

typealias Radius = float(f64.min to f64.max)

function circleArea(radius Radius) returns Radius
	return 3.14159 * radius * radius
end 'circleArea'

function main() returns ExitCode
	var area = circleArea(5.0)
	return trunc(area)  // Returns 78
end 'main'
---
ExitCode: 78
---
module {
  func @docs-example-1.circleArea(ptr: i64) -> f64 {
  entry:
    x64.movsd xmm1, [rip+__float_4614256650576692846]
    x64.mulsd xmm1, xmm0
    x64.mulsd xmm1, xmm0
    x64.movsd xmm0, xmm1
    x64.ret
  }
  func @main() -> u8 {
  entry:
    x64.prologue stack_size=16
    x64.movsd xmm1, [rip+__float_4617315517961601024]
    x64.movsd [rbp+-8], xmm0
    x64.movsd xmm0, xmm1
    x64.call docs-example-1.circleArea
    x64.cvttsd2si rax, xmm0
    x64.cmp rax, 125
    x64.jbe __range_ok_0
  __range_panic_0:
    x64.lea rax, [rip+__panic_msg_0]
    x64.mov rcx, rax
    x64.call mrt_panic
  __range_ok_0:
    x64.epilogue
    x64.ret
  }
}
---
