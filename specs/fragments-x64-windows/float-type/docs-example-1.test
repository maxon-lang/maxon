// Test: docs-example-1

typealias Radius = float(f64.min to f64.max)

function circleArea(radius Radius) returns Radius
	return 3.14159 * radius * radius
end 'circleArea'

function main() returns ExitCode
	let area = circleArea(5.0)
	return trunc(area)  // Returns 78
end 'main'

---
ExitCode: 78
---
// CompiledIR
module {
  func @float-type.circleArea(radius: f64) -> f64 {
  entry:
    x64.movsd xmm1, [rip+__float_3.14159]
    x64.movsd xmm2, xmm1
    x64.mulsd xmm2, xmm0
    x64.movsd xmm1, xmm2
    x64.mulsd xmm1, xmm0
    x64.movsd xmm0, xmm1
    x64.ret
  }
  func @float-type.main() -> u32 {
  entry:
    x64.prologue stack_size=16
    x64.movsd xmm0, [rip+__float_5]
    x64.call float-type.circleArea
    x64.cvttsd2si rax, xmm0
    x64.xor ecx, ecx
    x64.mov edx, 4294967295
    x64.cmp rax, rdx
    x64.jg float-type.main.__range_panic_0
    x64.cmp rax, rcx
    x64.jl float-type.main.__range_panic_0
    x64.jmp float-type.main.__range_ok_0
  __range_panic_0:
    x64.lea_symdata rax, [__panic_msg_0]
    x64.mov rcx, rax
    x64.call mrt_panic
  __range_ok_0:
    x64.epilogue
    x64.ret
  }
}
---
