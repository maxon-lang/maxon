// Test: otherwise-panic.not-hit
typealias Integer = int(i64.min to i64.max)

enum PanicNotHitError implements Error
	bad
end 'PanicNotHitError'

function maybeFail(b bool) returns Integer throws PanicNotHitError
	if b 'c'
		throw PanicNotHitError.bad
	end 'c'
	return 7
end 'maybeFail'

function main() returns ExitCode
	var total = 0
	try 'work'
		let a = maybeFail(false)
		let b = maybeFail(false)
		total = a + b
	end 'work'
	otherwise panic("should not happen")
	return total
end 'main'
---
ExitCode: 14
---
// CompiledIR
module {
  func @try-block-otherwise-divergent.maybeFail(b: i1) -> i64 {
  entry:
    x64.test rcx, rcx
    x64.je try-block-otherwise-divergent.maybeFail.c_0.after
  c_0:
    x64.xor eax, eax
    x64.mov rcx, 1
    x64.add rax, rcx
    x64.mov rdx, rax
    x64.xor eax, eax
    x64.ret
  c_0.after:
    x64.mov rax, 7
    x64.xor edx, edx
    x64.ret
  }
  func @try-block-otherwise-divergent.main() -> u32 {
  entry:
    x64.prologue stack_size=32
    x64.xor eax, eax
    x64.mov [rbp-8], rax
    x64.xor ecx, ecx
    x64.call try-block-otherwise-divergent.maybeFail
    x64.mov [rbp-16], rax
    x64.xor ecx, ecx
    x64.cmp rdx, rcx
    x64.jne try-block-otherwise-divergent.main.try_block_err_2
  try_block_continue_5:
    x64.mov rax, [rbp-16]
    x64.mov [rbp-24], rax
    x64.xor ecx, ecx
    x64.call try-block-otherwise-divergent.maybeFail
    x64.mov [rbp-32], rax
    x64.xor ecx, ecx
    x64.cmp rdx, rcx
    x64.jne try-block-otherwise-divergent.main.try_block_throw_7
  try_block_continue_8:
    x64.mov rax, [rbp-32]
    x64.mov rcx, [rbp-24]
    x64.add rcx, rax
    x64.mov [rbp-8], rcx
    x64.jmp try-block-otherwise-divergent.main.try_block_after_3
  try_block_throw_7:
    x64.jmp try-block-otherwise-divergent.main.try_block_err_2
  try_block_err_2:
    x64.lea_symdata rax, [__panic_msg_0]
    x64.mov rcx, rax
    x64.call mrt_panic
  try_block_after_3:
    x64.mov rax, [rbp-8]
    x64.xor ecx, ecx
    x64.mov edx, 4294967295
    x64.cmp rax, rdx
    x64.jg try-block-otherwise-divergent.main.__range_panic_9
    x64.cmp rax, rcx
    x64.jl try-block-otherwise-divergent.main.__range_panic_9
    x64.jmp try-block-otherwise-divergent.main.__range_ok_9
  __range_panic_9:
    x64.lea_symdata rax, [__panic_msg_1]
    x64.mov rcx, rax
    x64.call mrt_panic
  __range_ok_9:
    x64.epilogue
    x64.ret
  }
}
---
