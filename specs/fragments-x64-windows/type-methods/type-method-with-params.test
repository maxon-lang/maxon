// Test: type-method-with-params

typealias Integer = int(i64.min to i64.max)

type Adder
	var total Integer

	function add(value Integer)
		total = total + value
	end 'add'

	function getTotal() returns Integer
		return total
	end 'getTotal'

	static function create(total Integer) returns Self
		return Self{total: total}
	end 'create'
end 'Adder'

function main() returns ExitCode
	var a = Adder.create(total: 0)
	a.add(10)
	a.add(32)
	return a.getTotal()
end 'main'
---
ExitCode: 42
---
// CompiledIR
module {
  func @Adder.add(rcx: i64, rdx: i64) {
  entry:
    x64.mov r8, rcx
    x64.mov r9, [r8+0] (8b)
    x64.add r9, rdx
    x64.mov [r8+0], r9 (8b)
    x64.xor r8d, r8d
    x64.ret
  }
  func @Adder.getTotal(rcx: i64) -> i64 {
  entry:
    x64.mov r8, [rcx+0] (8b)
    x64.ret
  }
  func @Adder.create(rcx: i64) -> i64 {
  entry:
    x64.prologue stack_size=32
    x64.mov r12, rcx
    x64.mov ecx, 8
    x64.call mrt_alloc
    x64.mov [r8+0], r12 (8b)
    x64.epilogue
    x64.ret
  }
  func @main() -> u8 {
  entry:
    x64.prologue stack_size=32
    x64.xor ecx, ecx
    x64.call Adder.create
    x64.mov r12, r8
    x64.mov edx, 10
    x64.mov rcx, r12
    x64.call Adder.add
    x64.mov edx, 32
    x64.mov rcx, r12
    x64.call Adder.add
    x64.mov rcx, r12
    x64.call Adder.getTotal
    x64.cmp r8, 125
    x64.jbe __range_ok_0
  __range_panic_0:
    x64.lea rcx, [rip+__panic_msg_0]
    x64.call mrt_panic
  __range_ok_0:
    x64.epilogue
    x64.ret
  }
}
---
