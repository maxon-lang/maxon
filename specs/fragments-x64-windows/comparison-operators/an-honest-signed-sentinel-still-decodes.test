// Test: an-honest-signed-sentinel-still-decodes
typealias Slot = int(-1 to 4095)

function findSlot(key Slot) returns Slot
	if key == 7 'found'
		return 3 as Slot
	end 'found'
	return 0 - 1
end 'findSlot'

function main() returns ExitCode
	let slot = findSlot(9 as Slot)

	if slot >= 0 'wronglyFound'
		return 1
	end 'wronglyFound'
	return 42
end 'main'
---
ExitCode: 42
---
// CompiledIR
module {
  func @findSlot(key: i64) -> i16 {
  entry:
    x64.prologue stack_size=16
    x64.mov [rbp-8], rcx
    x64.mov rax, -1
    x64.mov rdx, 4095
    x64.cmp rcx, rdx
    x64.jg findSlot.__range_panic_0
    x64.cmp rcx, rax
    x64.jl findSlot.__range_panic_0
    x64.jmp findSlot.__range_ok_0
  __range_panic_0:
    x64.lea_symdata rax, [__panic_msg_0]
    x64.mov rcx, rax
    x64.call mrt_panic
  __range_ok_0:
    x64.mov rbx, 7
    x64.mov rsi, [rbp-8]
    x64.cmp rsi, rbx
    x64.jne findSlot.found_0.after
  found_0:
    x64.mov rax, 3
    x64.epilogue
    x64.ret
  found_0.after:
    x64.xor eax, eax
    x64.mov rcx, 1
    x64.sub rax, rcx
    x64.mov rdx, -1
    x64.mov rbx, 4095
    x64.cmp rax, rbx
    x64.jg findSlot.__range_panic_1
    x64.cmp rax, rdx
    x64.jl findSlot.__range_panic_1
    x64.jmp findSlot.__range_ok_1
  __range_panic_1:
    x64.lea_symdata rax, [__panic_msg_1]
    x64.mov rcx, rax
    x64.call mrt_panic
  __range_ok_1:
    x64.epilogue
    x64.ret
  }
  func @main() -> u32 {
  entry:
    x64.prologue stack_size=16
    x64.mov rcx, 9
    x64.call findSlot
    x64.xor ecx, ecx
    x64.cmp rax, rcx
    x64.jl main.wronglyFound_0.after
  wronglyFound_0:
    x64.mov rax, 1
    x64.epilogue
    x64.ret
  wronglyFound_0.after:
    x64.mov rax, 42
    x64.epilogue
    x64.ret
  }
}
---
