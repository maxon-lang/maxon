// Test: match-statements.nested-in-function

typealias Integer = int(i64.min to i64.max)

function categorize(n Integer) returns Integer
	match n 'cat'
		1 or 2 or 3 then return 1
		4 or 5 or 6 then return 2
		default then return 0
	end 'cat'
end 'categorize'

function main() returns ExitCode
	return categorize(5)
end 'main'
---
ExitCode: 2
---
// CompiledIR
module {
  func @categorize(n: i64) -> i64 {
  entry:
    x64.prologue stack_size=16
    x64.mov [rbp-8], rcx
    x64.mov rax, [rbp-8]
    x64.mov rcx, 1
    x64.mov rdx, rax
    x64.sub rdx, rcx
    x64.mov rbx, 2
    x64.cmp rdx, rbx
    x64.jbe categorize.cat_0.case0
  cat_0.dispatch0:
    x64.mov rax, 4
    x64.mov rcx, [rbp-8]
    x64.sub rcx, rax
    x64.mov rdx, 2
    x64.cmp rcx, rdx
    x64.jbe categorize.cat_0.case1
  cat_0.dispatch1:
    x64.jmp categorize.cat_0.case2
  cat_0.case0:
    x64.mov rax, 1
    x64.epilogue
    x64.ret
  cat_0.case1:
    x64.mov rax, 2
    x64.epilogue
    x64.ret
  cat_0.case2:
    x64.xor eax, eax
    x64.epilogue
    x64.ret
  cat_0.merge:
  }
  func @main() -> u32 {
  entry:
    x64.prologue stack_size=16
    x64.mov rcx, 5
    x64.call categorize
    x64.xor ecx, ecx
    x64.mov edx, 4294967295
    x64.cmp rax, rdx
    x64.jg main.__range_panic_0
    x64.cmp rax, rcx
    x64.jl main.__range_panic_0
    x64.jmp main.__range_ok_0
  __range_panic_0:
    x64.lea_symdata rax, [__panic_msg_0]
    x64.mov rcx, rax
    x64.call mrt_panic
  __range_ok_0:
    x64.epilogue
    x64.ret
  }
}
---
