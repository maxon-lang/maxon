// Test: while-loops.basic
function main() returns ExitCode
var x = 5
var i = 3
while i > 0 'loop'
x = x + 2
i = i - 1
if i == 0 'check'
break
end 'check'
end 'loop'
return x
end 'main'
---
ExitCode: 11
---
// CompiledIR
module {
  func @while-loops.main() -> u32 {
  entry:
    x64.prologue stack_size=16
    x64.mov rax, 5
    x64.mov [rbp-8], rax
    x64.mov rcx, 3
    x64.mov [rbp-16], rcx
    x64.jmp while-loops.main.loop_0.header
  loop_0.header:
    x64.xor eax, eax
    x64.mov rcx, [rbp-16]
    x64.cmp rcx, rax
    x64.jle while-loops.main.loop_0.exit
  loop_0:
    x64.mov rax, 2
    x64.mov rcx, [rbp-8]
    x64.add rcx, rax
    x64.mov [rbp-8], rcx
    x64.mov rdx, 1
    x64.mov rbx, [rbp-16]
    x64.sub rbx, rdx
    x64.mov [rbp-16], rbx
    x64.xor esi, esi
    x64.cmp rbx, rsi
    x64.jne while-loops.main.check_1.after
  check_1:
    x64.jmp while-loops.main.loop_0.exit
  check_1.after:
    x64.jmp while-loops.main.loop_0.header
  loop_0.exit:
    x64.mov rax, [rbp-8]
    x64.xor ecx, ecx
    x64.mov edx, 4294967295
    x64.cmp rax, rdx
    x64.jg while-loops.main.__range_panic_2
    x64.cmp rax, rcx
    x64.jl while-loops.main.__range_panic_2
    x64.jmp while-loops.main.__range_ok_2
  __range_panic_2:
    x64.lea_symdata rax, [__panic_msg_0]
    x64.mov rcx, rax
    x64.call mrt_panic
  __range_ok_2:
    x64.epilogue
    x64.ret
  }
}
---
