// Test: nested-control
function main() returns ExitCode
	var result = 0
	var i = 0
	while i < 3 'outer'
		var j = 0
		while j < 3 'inner'
			if (i + j) mod 2 == 0 'even'
				result = result + 1
			end 'even'
			j = j + 1
		end 'inner'
		i = i + 1
	end 'outer'
	return result
end 'main'
---
ExitCode: 5
---
module {
  func @main() -> u32 {
  entry:
    x64.prologue stack_size=32
    x64.xor rax, rax
    x64.mov [rbp-8], rax
    x64.xor rcx, rcx
    x64.mov [rbp-16], rcx
    x64.jmp main.outer_0.header
  outer_0.header:
    x64.mov rax, 3
    x64.mov rcx, [rbp-16]
    x64.cmp rcx, rax
    x64.jge main.outer_0.exit
  outer_0:
    x64.xor rax, rax
    x64.mov [rbp-24], rax
    x64.jmp main.inner_1.header
  inner_1.header:
    x64.mov rax, 3
    x64.mov rcx, [rbp-24]
    x64.cmp rcx, rax
    x64.jge main.inner_1.exit
  inner_1:
    x64.mov rax, [rbp-16]
    x64.mov rcx, [rbp-24]
    x64.add rax, rcx
    x64.mov rdx, 2
    x64.mov rcx, rdx
    x64.mov [rbp-32], rax
    x64.cqo
    x64.idiv rcx
    x64.xor rbx, rbx
    x64.cmp rdx, rbx
    x64.jne main.even_2.merge
  even_2:
    x64.mov rax, 1
    x64.mov rcx, [rbp-8]
    x64.add rcx, rax
    x64.mov [rbp-8], rcx
    x64.jmp main.even_2.merge
  even_2.merge:
    x64.mov rax, 1
    x64.mov rcx, [rbp-24]
    x64.add rcx, rax
    x64.mov [rbp-24], rcx
    x64.jmp main.inner_1.header
  inner_1.exit:
    x64.mov rax, 1
    x64.mov rcx, [rbp-16]
    x64.add rcx, rax
    x64.mov [rbp-16], rcx
    x64.jmp main.outer_0.header
  outer_0.exit:
    x64.mov rax, [rbp-8]
    x64.xor rcx, rcx
    x64.mov edx, 4294967295
    x64.cmp rax, rdx
    x64.jg main.__range_panic_3
    x64.cmp rax, rcx
    x64.jl main.__range_panic_3
    x64.jmp main.__range_ok_3
  __range_panic_3:
    x64.lea_symdata rax, [__panic_msg_0]
    x64.mov rcx, rax
    x64.call mrt_panic
  __range_ok_3:
    x64.epilogue
    x64.ret
  }
}
---
