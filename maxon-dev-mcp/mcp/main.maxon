// Entry point for the Maxon MCP server. Reads newline-delimited JSON-RPC 2.0
// requests on stdin, dispatches them to tool handlers, and writes responses
// to stdout. Runs until stdin EOF.

function main() returns ExitCode
	runServer()
	return 0
end 'main'
