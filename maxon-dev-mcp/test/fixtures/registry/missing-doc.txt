# CONFORMANCE FIXTURE -- an entry with no `doc` line.
# The gate REJECTS it (a code with no doc is a code that cannot be looked up).
# The MCP's old parser answered with `doc: ""` -- an empty explanation presented as an
# explanation. The tool must REFUSE.

E3010 SemanticUnneededCast
	csharp SemanticUnneededCast
	shv2 unneededCast
	doc A cast is redundant: the value already has the destination type.

E5001 CodeEmitterRegisterPressure
	shv2 registerPressure
