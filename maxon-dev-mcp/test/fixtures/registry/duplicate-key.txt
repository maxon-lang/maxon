# CONFORMANCE FIXTURE -- one entry declares `csharp` TWICE.
# The gate REJECTS it. The MCP's old parser pushed BOTH claims and answered with a
# duplicated compiler key.
# The tool must REFUSE: a registry the gate will not accept is one no tool may answer from.

E3010 SemanticUnneededCast
	csharp SemanticUnneededCast
	csharp SemanticUnneededCastAgain
	shv2 unneededCast
	doc A cast is redundant: the value already has the destination type.

E5001 CodeEmitterRegisterPressure
	shv2 registerPressure
	doc Out of registers, and no spill placement is forced.
