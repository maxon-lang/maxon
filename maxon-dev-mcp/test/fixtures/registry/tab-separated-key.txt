# CONFORMANCE FIXTURE -- a TAB between key and value.
# The gate ACCEPTS it: key and value are separated by a run of spaces OR TABS.
# It used to FAIL `dotnet build` here, while the MCP's parser was perfectly happy with it
# -- and docs/error-codes.txt's own header said "Keys are tab-indented".

E3010 SemanticUnneededCast
	csharp	SemanticUnneededCast
	shv2	unneededCast
	doc	A cast is redundant: the value already has the destination type.

E5001 CodeEmitterRegisterPressure
	shv2 registerPressure
	doc Out of registers, and no spill placement is forced.
