// Test nested scopes and variable shadowing
function main() int
	let x = 10

	if x > 5 'block1'
		let y = 20
		let z = x + y
		print(z)
	end 'block1'

	if x < 15 'block2'
		let y = 30
		let z = x + y
		print(z)
	end 'block2'

	return 0
end 'main'
