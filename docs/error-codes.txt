# The Maxon error-code registry -- the SINGLE SOURCE OF TRUTH for the 4-digit
# error-code space, shared by all three compilers.
#
# THIS FILE IS AUTHORED. Four files are GENERATED FROM IT, and none may be edited:
#
#   maxon-sharp/Compiler/ErrorCode.g.cs                (the C# bootstrap's enum)
#   maxon-selfhosted/Compiler/ErrorCodeRegistry.maxon  (v1's enum, deprecated)
#   maxon-shv2/Compiler/ErrorCodeRegistry.maxon        (shv2's enum)
#   docs/error-codes.json                              (the artifact TOOLS read)
#
# Regenerate them with:  maxon error-codes generate
# Verify them with:      maxon error-codes check   <- run by every build that USES one
#
# A duplicate number FAILS THE BUILD, naming both claimants. That is the whole
# point: this file exists because the number space used to be written down three
# times, and two agents took E3099 on the same day, each having correctly
# grepped a different copy.
#
# THIS GRAMMAR HAS EXACTLY ONE PARSER: maxon-sharp/Compiler/ErrorCodeRegistry.cs.
# It briefly had two -- the MCP's `lookup_error_code` hand-parsed it as well -- and
# they disagreed on five inputs, one of which reported `emittedBy: {}` for a code the
# bootstrap declares. A format described in two places is the same disease as a number
# space written in three. Tools now read docs/error-codes.json, which THIS parser emits
# and stamps with a hash of these bytes; a tool that finds the hash stale REFUSES to
# answer rather than answering from a copy nobody checked.
#
# FORMAT  (a KEY LINE is any line starting with whitespace; an ENTRY runs from its
#          header to the next header, and a blank line does NOT end it)
#
#   E<nnnn> <CanonicalName>        an entry; the canonical name IS the meaning
#       csharp     <MemberName>    this compiler emits the code, spelled thus
#       selfhosted <memberName>
#       shv2       <memberName>
#       reserved   <why>           nobody emits it -- but the number is TAKEN
#       doc        <prose>         repeatable; the text `lookup_error_code` returns
#
#   Key and value are separated by a run of SPACES OR TABS, and so are the number and
#   the canonical name. Blank lines and `#` comments are ignored anywhere, including
#   between an entry's key lines. An unknown key is an ERROR, never a skipped line.
#
# RULES THE CHECKER ENFORCES
#   - a number appears at most once
#   - a canonical name appears at most once
#   - one compiler never spells two codes with the same member name, and never
#     declares the same key twice on one entry
#   - an entry is either `reserved` (no compiler claims it) or claimed by >= 1
#   - EVERY CLAIM IS LIVE: a claimed member must be mentioned by that compiler's own
#     source. A claim naming a member nobody emits is a lie the registry tells about
#     itself, and 22 rows were telling it. (The converse is structural: a code that is
#     emitted cannot be absent here, because the enum is generated -- `ErrorCode.Foo`
#     does not compile unless this file put Foo in it.)
#   - every entry has at least one `doc` line
#   - the four generated files match, byte for byte
#
# The STAGE is DERIVED from the leading digit -- it is never written down:
#   1xxx lexer   2xxx parser   3xxx semantic   4xxx IR   5xxx code emitter
#   6xxx PE writer                                       9xxx internal
#
# TO ADD A CODE: take the next free number in the right band, add an entry here, run
# `maxon error-codes generate`, and WRITE THE CODE THAT EMITS IT -- the liveness rule
# fails the build if you stop after generating. Do not hand-edit a generated file; the
# check will catch you, which is the idea.
#
# NEVER REFERENCE A CODE BY ITS NUMBER OUTSIDE THIS FILE. Use the generated member
# (`ErrorCode.SemanticUnneededCast`, and `.Format()` if you need the "E3010" spelling).
# A literal "E3010" in a source file is a fourth copy of the number space, and renumbering
# the code leaves it pointing at nothing while every gate stays green.

E1001 LexerUnexpectedCharacter
	selfhosted lexerUnexpectedCharacter
	doc The lexer met a character that cannot begin any token.

E1002 LexerUnterminatedString
	csharp LexerUnterminatedString
	selfhosted lexerUnterminatedString
	shv2 lexerUnterminatedString
	doc A string literal reached end-of-line or end-of-file with no closing quote.

E1003 LexerUnterminatedChar
	reserved retired 2026-07-14 -- the member was DECLARED but no site ever emitted it, which `error-codes check` now refuses. The number stays TAKEN: a retired meaning must not be silently reused.
	doc A character literal reached end-of-line or end-of-file with no closing quote.

E1004 LexerInvalidEscape
	csharp LexerInvalidEscape
	selfhosted lexerInvalidEscape
	shv2 lexerInvalidEscape
	doc A backslash escape in a string or character literal names no known escape.

E1005 LexerInvalidNumber
	reserved retired 2026-07-14 -- the member was DECLARED but no site ever emitted it, which `error-codes check` now refuses. The number stays TAKEN: a retired meaning must not be silently reused.
	doc A numeric literal is malformed (a stray radix prefix, a digit outside the radix, or a misplaced separator).

E1006 LexerUnescapedBrace
	csharp LexerUnescapedBrace
	selfhosted lexerUnescapedBrace
	shv2 lexerUnescapedBrace
	doc A literal brace appears unescaped in a string literal. A '{' opens interpolation, so write '\{' to embed a literal brace.

E1007 LexerUnterminatedBlockComment
	csharp LexerUnterminatedBlockComment
	selfhosted lexerUnterminatedBlockComment
	shv2 lexerUnterminatedBlockComment
	doc A block comment reached end-of-file with no closing delimiter.

E1008 LexerUnexpectedEof
	shv2 lexerUnexpectedEof
	doc The lexer reached end-of-file in the middle of a token.

E1009 LexerUnknownDirective
	shv2 lexerUnknownDirective
	doc A '#' compiler directive names a directive the lexer does not know.

E2001 ParserUnexpectedToken
	csharp ParserUnexpectedToken
	selfhosted parserUnexpectedToken
	shv2 parserUnexpectedToken
	doc The parser met a token that cannot appear at this point in the grammar.

E2002 ParserExpectedIdentifier
	reserved retired 2026-07-14 -- the member was DECLARED but no site ever emitted it, which `error-codes check` now refuses. The number stays TAKEN: a retired meaning must not be silently reused.
	doc An identifier was required here (a declaration name, a parameter name, a field name).

E2003 ParserExpectedType
	csharp ParserExpectedType
	selfhosted parserExpectedType
	shv2 parserExpectedType
	doc A type was required here and the token stream had something else.
	doc shv2 emits it for the two shapes where a name IS present but is not a
	doc usable type: a bare sized type as a typealias RHS (`typealias I = i64`),
	doc and an `as` cast target that names a typealias the reading file cannot
	doc see. A cast target no declaration binds ANYWHERE is E3011 instead, which
	doc is sharper than the reference's blanket E2003 -- see
	doc specs-shv2/cast-target-type-resolution.md.

E2004 ParserExpectedExpression
	csharp ParserExpectedExpression
	selfhosted parserExpectedExpression
	shv2 parserExpressionError
	doc An expression is not well formed, or is well formed and not usable where only a constant is. Broader than its C#/v1 name (ParserExpectedExpression) suggests: all three compilers report five distinct message families under it -- a primary was required and the token stream had something else; a name in an expression has no binding in scope; a binary arithmetic/shift operator got a non-numeric operand; a unary '-' got a non-numeric operand; and a top-level binding's initializer does not fold to a compile-time constant.

E2005 ParserExpectedStatement
	reserved retired 2026-07-14 -- the member was DECLARED but no site ever emitted it, which `error-codes check` now refuses. The number stays TAKEN: a retired meaning must not be silently reused.
	doc A statement was required here and the token stream had something else.

E2006 ParserExpectedEnd
	reserved retired 2026-07-14 -- the member was DECLARED but no site ever emitted it, which `error-codes check` now refuses. The number stays TAKEN: a retired meaning must not be silently reused.
	doc A block was left unterminated: its 'end' is missing.

E2007 ParserUnexpectedEof
	csharp ParserUnexpectedEof
	selfhosted parserUnexpectedEof
	shv2 parserUnexpectedEof
	doc The parser reached end-of-file with a construct still open.

E2008 ParserMismatchedEndLabel
	csharp ParserMismatchedEndLabel
	doc A block's 'end' label does not match the label its header opened.

E2009 ParserInvalidAssignment
	reserved retired 2026-07-14 -- the member was DECLARED but no site ever emitted it, which `error-codes check` now refuses. The number stays TAKEN: a retired meaning must not be silently reused.
	doc The left side of an assignment is not an assignable place.

E2010 ParserExpectedToken
	csharp ParserExpectedToken
	selfhosted parserExpectedToken
	shv2 parserExpectedToken
	doc A specific token was required here. Reports as: Expected 'X' but got 'Y'.

E2011 ParserLiteralOverflow
	csharp ParserLiteralOverflow
	selfhosted parserLiteralOverflow
	shv2 parserLiteralOverflow
	doc An integer literal does not fit the range of its type.

E2012 ParserCircularDependency
	csharp ParserCircularDependency
	selfhosted parserCircularDependency
	shv2 parserCircularDependency
	doc A declaration cycle: something is defined in terms of itself. Broader than 'import cycle', which is only one of the shapes all three compilers report under it -- the other is a cycle among top-level constants ('let A = B + 1' / 'let B = A + 1'), where no initializer can be evaluated because each waits on the other.

E2013 ParserImmutableVariable
	csharp ParserImmutableVariable
	selfhosted parserImmutableVariable
	shv2 assignToImmutable
	doc An assignment targets a binding declared 'let'. Declare it 'var', or do not reassign it.

E2014 ParserDuplicateParameter
	shv2 duplicateParameter
	doc One parameter list declares the same name twice. The second parameter would be dead and uncallable.

E2015 ParserUnsupportedFeature
	shv2 parserUnsupportedFeature
	doc The source uses a language construct this compiler does not implement yet.

E2016 ParserInvalidCharacterLiteral
	shv2 parserInvalidCharacterLiteral
	doc A character literal is malformed (empty, or holding more than one character).

E2025 ParserMatchFallthroughWithReturn
	csharp ParserMatchFallthroughWithReturn
	selfhosted parserMatchFallthroughWithReturn
	shv2 matchFallthroughWithReturn
	doc A match arm falls through to the next arm while the enclosing function still owes a return.

E2026 ParserMatchNotExhaustive
	csharp ParserMatchNotExhaustive
	selfhosted parserMatchNotExhaustive
	shv2 matchNotExhaustive
	doc A match does not cover every case of its scrutinee and has no default arm.

E2027 ParserMatchDuplicatePattern
	csharp ParserMatchDuplicatePattern
	selfhosted parserMatchDuplicatePattern
	shv2 matchDuplicatePattern
	doc Two match arms test the same pattern; the second can never run.

E2028 ParserMatchTypeMismatch
	csharp ParserMatchTypeMismatch
	selfhosted parserMatchTypeMismatch
	shv2 matchTypeMismatch
	doc A match's arms, or a ternary's two branches, produce different types (or a ternary condition is not bool).
	doc a match arm or postfix-ternary's two arms produce different types,
	doc or a ternary condition is not bool. Mirrors the C# bootstrap's
	doc ParserMatchTypeMismatch -- thrown at parse time so it wins over the
	doc body-finish unused-variable check (E3012).

E2029 ParserMatchDefaultNotLast
	csharp ParserMatchDefaultNotLast
	selfhosted parserMatchDefaultNotLast
	shv2 matchDefaultNotLast
	doc A match's 'default' arm is not the last arm.

E2042 ParserMatchMissingBlockId
	csharp ParserMatchMissingBlockId
	selfhosted parserMatchMissingBlockId
	shv2 matchMissingBlockId
	doc A match arm opens a block but gives it no block label.

E2043 ParserMatchMismatchedBlockId
	csharp ParserMatchMismatchedBlockId
	selfhosted parserMatchMismatchedBlockId
	shv2 matchMismatchedBlockId
	doc A match arm's block label does not match the label its arm opened.

E2044 ParserMatchDefaultWithEnum
	reserved retired 2026-07-14 -- the member was DECLARED but no site ever emitted it, which `error-codes check` now refuses. The number stays TAKEN: a retired meaning must not be silently reused.
	doc A match over an enum has a 'default' arm. Enums are matched exhaustively so that adding a case forces every site to handle it.

E2045 ParserNonConstantInitializer
	csharp ParserNonConstantInitializer
	selfhosted parserNonConstantInitializer
	shv2 parserNonConstantInitializer
	doc A top-level constant's initializer is not a compile-time constant.

E2046 ParserMatchDefaultEnumMustThrow
	csharp ParserMatchDefaultEnumMustThrow
	selfhosted parserMatchDefaultEnumMustThrow
	shv2 matchDefaultEnumMustThrow
	doc A match over an enum may only carry a 'default' arm if that arm throws.

E2047 ParserControlOutsideLoop
	shv2 controlOutsideLoop
	doc 'break' or 'continue' appears outside any loop, or targets a label that names no enclosing loop.

E2048 ParserRedundantLoopLabel
	csharp ParserRedundantLoopLabel
	selfhosted parserRedundantLoopLabel
	shv2 labelTargetsOwnLoop
	doc 'break'/'continue' carries the label of the innermost enclosing loop. The label is redundant.
	doc `break 'lab'` / `continue 'lab'` where `lab` names the
	doc innermost enclosing loop (with no intervening match for `break`). The
	doc label is redundant -- unlabeled `break`/`continue` already targets
	doc that loop. Emitted from `resolveBreakTarget` / `resolveContinueTarget`.

E2049 ParserMatchBlockStatement
	csharp ParserMatchBlockStatement
	selfhosted parserMatchBlockStatementNotAllowed
	shv2 matchBlockStatement
	doc A match arm's body is a block-opening statement. Each arm body must be a single statement.
	doc Phase 9 Stage 0: a match arm body cannot be a block-opening
	doc statement (`if` / `while` / `for` / `match` / block-form `try`).
	doc Each arm body is a single statement; block-form constructs would
	doc allocate persistent slots that leak on every error path through
	doc the match.

E2050 ParserOtherwiseBlockMissingBinding
	csharp ParserOtherwiseBlockMissingBinding
	doc An 'otherwise' block form declares no error binding.

E2051 ParserReservedIdentifier
	csharp ParserReservedIdentifier
	selfhosted parserReservedIdentifier
	shv2 reservedIdentifier
	doc A declaration uses a name reserved by the compiler.

E2052 ParserFirstArgCannotBeNamed
	csharp ParserFirstArgCannotBeNamed
	selfhosted parserFirstArgCannotBeNamed
	shv2 callFirstArgNamed
	doc A call labels its first argument. The first argument is positional; only the second and later arguments take 'name:' labels.
	doc a call site labeled its first argument (`f(name: value, ...)`).
	doc The first argument is positional-only; only the second and subsequent
	doc arguments may be labeled.

E2053 ParserCallArgMissingLabel
	shv2 callArgMissingLabel
	doc A call leaves a second-or-later argument unlabeled. Every argument after the first must carry its 'name:' label.

E2054 ParserShiftCountNegative
	csharp ParserShiftCountNegative
	shv2 shiftCountNegative
	doc A shift ('shl'/'shr') whose count the compiler FOLDED and found NEGATIVE --
	doc 'a shl -1', 'a shl -(1)', 'let SHIFT = -1' ... 'a shl SHIFT'. A negative count is
	doc not a shift the other way: the hardware MASKS it, so 'a shl -1' silently computed
	doc 'a shl 63' -- the MAXIMUM LEFT shift, a wrong answer with the opposite sign.
	doc Maxon follows Go: the count must be non-negative; a constant one is this compile
	doc error, and one that only appears at RUN TIME panics.
	doc WARNING -- a count of 64 OR MORE IS NOT THIS ERROR. It is a legal, well-defined
	doc shift that moves every bit out ('x shl 64' == 0), because Go puts no upper limit
	doc on a shift count. This code briefly rejected it, which OVER-rejects a correct
	doc program. The compiler SATURATES such a count instead of masking it.
	doc A PARSE-time check, positioned at the count as WRITTEN. It asks the CONSTANT
	doc FOLDER, not the token shape: 'let SHIFT = -1' is the same -1 as the literal, and a
	doc check that only looked for a bare literal token missed its own motivating example.

E2055 GenericBaseNotGeneric
	shv2 genericBaseNotGeneric
	doc A 'Base with Args' instantiation names a 'Base' that declares no type parameters --
	doc a non-generic type, or a name that is not a generic 'type' at all. The oracle reports
	doc "Type 'X' has no associated types" for exactly this (P1.6-A).

E2056 GenericArityMismatch
	shv2 genericArityMismatch
	doc A generic instantiation supplies the wrong number of type arguments -- 'Pair with Integer'
	doc where 'type Pair uses A, B' expects two (P1.6-A).

E2057 GenericManagedArg
	reserved retired 2026-07-21 -- P1.6-B2 made managed type arguments legal: a managed generic instance now OWNS its type argument and drops it exactly once through its synthesized `__destruct_<instance>` cascade, so there is nothing left to reject. The number stays TAKEN: a retired meaning must not be silently reused.
	doc A generic type argument of a managed type -- a String, or a struct with a managed field --
	doc was rejected in the trivial slice (P1.6-A): the shared trivial destructor dropped nothing, so
	doc a managed argument would leak. P1.6-B2 lifted the restriction (owning managed containers).

E2058 ParserTestOutsideTestFile
	csharp ParserTestOutsideTestFile
	doc A `test` declaration appears in a file whose name does not end in `.test.maxon`. Tests are
	doc confined to those files by ONE rule checked in ONE place, so which declarations a build
	doc carries is answerable from the file list alone -- no flag, no attribute, nothing to read the
	doc body for. The message names the fix: rename the file, or move the declaration.

E2059 ParserTestEmptyName
	csharp ParserTestEmptyName
	doc A `test` declaration's name is the empty character literal (`test ''`). The name is the
	doc test's whole identity -- it is what the reporter prints and what `end ''` must match -- and
	doc an empty one names nothing. Refused at the declaration rather than at the report, where an
	doc unnamed failure would be the least useful place to discover it.

E2060 ParserCallerLocationOutsideDefault
	csharp ParserCallerLocationOutsideDefault
	doc '__line__' or '__file__' appeared somewhere other than a function parameter's default
	doc value -- in an ordinary expression, or as a struct FIELD default, which is a different
	doc thing despite the shared '= <expr>' spelling.
	doc These two are a CALLING CONVENTION, not a reflection facility: each expands at the CALL
	doc SITE, and a parameter default is the only position that has a call site to expand
	doc against. A field default expands at a struct literal and an ordinary expression expands
	doc nowhere, so neither has an answer to give.
	doc Raised at TWO grammar positions, from one rule: the expression position catches it when
	doc the token is evaluated, and the field-default position catches it when the tokens are
	doc CAPTURED. The capture-side check is not merely a better error location -- without it a
	doc field default nobody triggers is never expanded, and the misuse would go undiagnosed.

E2061 GenericBarePrimitiveArg
	shv2 genericBarePrimitiveArg
	doc A 'Base with Args' instantiation supplies a BARE numeric primitive keyword -- 'Box with int',
	doc 'Array with float' -- where the ranged-typealias rule requires a declared domain
	doc ('typealias Integer = int(i64.min to i64.max)', then 'Box with Integer'). It is the same rule
	doc every other type position already enforces, reaching the one position that never did.
	doc 'bool' is DELIBERATELY ADMITTED and is not this error: it is already a constrained type with
	doc nothing to range. So are 'String', a user 'type'/'enum', a type PARAMETER, a nested instance
	doc and a ranged alias -- this names ONLY the two bare numeric keywords.
	doc Distinct from E3011 (unknownType), which means the name resolves to NOTHING; here it resolves
	doc to a real type that is forbidden in this position.
	doc Distinct from E2062: a bare 'float' is BOTH a bare primitive and a float, and E2062 claims it,
	doc because THIS code's advice ("declare a ranged typealias") is true for 'int' and a trap for
	doc 'float'. So this code only ever names 'int'.

E2062 GenericFloatTypeArgUnsupported
	shv2 genericFloatTypeArg
	doc A generic type argument resolves to 'float' -- bare ('Box with float') or through a ranged
	doc float typealias ('typealias Real = float(...)' then 'Box with Real'). shv2 DICTIONARY-PASSES
	doc rather than monomorphizing, so a type parameter is one opaque 8-byte GENERAL-PURPOSE slot; a
	doc float value is born in a floating-point register and has no way to travel through it. Reaching
	doc the backend, it panicked on the x64 emitter's cross-register-file assertion.
	doc A DELIBERATE DIVERGENCE FROM BOTH REFERENCES, which accept it because they MONOMORPHIZE (the
	doc instantiation gets a genuine f64 slot). It is a compiler limitation, not a language rule, and
	doc the message says so rather than naming a workaround -- there is none at this milestone.
	doc It is asked BEFORE E2061 so a bare 'float' gets this message and not the ranged-typealias
	doc advice, which would direct the reader into the very panic this refusal exists to close.

E2063 DirectiveUnbalanced
	shv2 directiveUnbalanced
	doc A '#if' / '#else' / '#endif' region is structurally unbalanced: a '#if' still open at end of
	doc file, or an '#endif' / '#else' with no '#if' to close.
	doc A DELIBERATE DIVERGENCE FROM BOTH REFERENCES, which accept all three silently -- v1's
	doc 'accept()' calls are optional and its 'skipConditionalBranch' simply returns at EOF, and the
	doc bootstrap compiles an unterminated '#if' and a stray '#endif' clean (measured).
	doc Accepting them is a wrong-answer generator, not a leniency: an unterminated '#if' silently
	doc SWALLOWS the rest of the file, so every declaration after it vanishes and the only symptom is
	doc an unrelated 'undefined' further on. shv2 refuses, naming the position of the directive that
	doc has no partner.
	doc Verified safe before it was added: every '#if' region in specs/, specs-shv2/ and stdlib/ (100
	doc directive lines) is balanced, so nothing in the corpus relied on the references' tolerance.

E2064 DirectiveUnknownPredicate
	shv2 directiveUnknownPredicate
	doc A '#if' condition names a conditional-compilation predicate that does not exist -- anything
	doc other than 'os', 'arch', 'testing', 'rcSanitize' or 'leakReport'.
	doc Distinct from E1009 (lexerUnknownDirective), which names an unknown DIRECTIVE ('#wibble', a
	doc bad token); this names a known directive whose CONDITION calls an unknown function.
	doc An unknown ARGUMENT to a KNOWN predicate ('os(Plan9)') is deliberately NOT this error -- it
	doc evaluates to FALSE, matching both references and letting a condition name a platform this
	doc compiler does not target without becoming a build failure.
	doc Names ONLY the unknown-name fault. A condition the grammar cannot READ at all -- a missing
	doc ')', an operator with no right-hand side, text trailing the condition -- is E2065; it used to
	doc share this code, and 'your predicate name is unknown' is the wrong advice for a missing paren.

E2065 DirectiveMalformedCondition
	shv2 directiveMalformedCondition
	doc A '#if' condition the directive grammar has no production for. Two shapes reach it: the
	doc condition itself does not parse (a missing ')', an 'and' with no right-hand side, a bare
	doc 'true' where a predicate call belongs, a condition that runs off the end of the file), or the
	doc condition parses but TEXT FOLLOWS IT on the same line.
	doc Split out of E2064, which names a different fault: there the condition reads fine and the
	doc function it calls does not exist, and the fix is to correct a NAME. Here there is nothing to
	doc look up. One code answering both told a reader with an unbalanced paren that their predicate
	doc name was unknown.
	doc The trailing-text shape is a REFUSAL where the bootstrap is silent (measured: it discards the
	doc remainder of a '#if' line, so '#if os(Windows) n = 5' assigns nothing). Accepting it is a
	doc wrong-answer generator: the tokens after the condition are emitted into the program as code,
	doc so '#if os(Windows) os(Linux)' is a hard error on Windows and silence on Linux, where the
	doc region is dead and the junk is skipped along with it. A condition ends at its own newline.

E2066 ConformanceWithArgSurplus
	shv2 conformanceWithArgSurplus
	doc A conformance's PARENTHESIZED 'with (...)' list binds more arguments than the interface
	doc declares 'uses' names -- 'type Holder implements One with (Integer, Float)' against
	doc 'interface One uses A', which bound A := Integer, silently DROPPED Float and compiled clean
	doc (measured, exit 42): a typo nothing reported.
	doc DELIBERATELY ASYMMETRIC, and the asymmetry IS the rule. The same surplus written WITHOUT
	doc parentheses is still ignored, because the two spellings are not the same claim: parentheses
	doc make the list explicit, so its LENGTH is something the author asserted and can be wrong
	doc about, while unparenthesized the length is decided by the interface's 'uses' arity and a
	doc trailing comma legitimately belongs to the outer 'implements' list. Both references consume
	doc exactly 'arity' items in the unparenthesized form, and 'surplus-conformance-argument-is-
	doc ignored' pins it.
	doc NOT E2056 GenericArityMismatch, which is a different construct with a different rule. That
	doc one is a generic INSTANTIATION ('Pair with Integer' as a type reference) and its arity is
	doc SYMMETRIC -- too few is the same error as too many. Here too FEW is a different diagnostic
	doc entirely (E3016, naming the first UNBOUND associated type, raised whole-program by
	doc ConformanceCheck), and too many is refused only in one of the two spellings. One code over
	doc two rules that agree on neither symmetry nor spelling would document neither.
	doc It is NOT raised when the interface name resolves to nothing: a name no file declares has no
	doc arity to be surplus of, and the program's real error is E3015 'implements unknown interface'.

E2067 ParserBuiltinArgNamed
	shv2 builtinArgNamed
	doc A call to a bare compiler BUILTIN labels one of its arguments -- 'min(3.0, b: 5.0)'.
	doc A builtin's arguments are ALL POSITIONAL, at every index, because a 'name:' label names a
	doc PARAMETER OF A DECLARATION and a builtin has no declaration to have parameters. Nothing
	doc downstream could check such a label either: a builtin emits no call op, so it never reaches
	doc the slotting that validates a label against a callee's parameter names.
	doc NOT E2052/E2053, and that is the whole reason this code exists rather than reusing one of
	doc them. Both of those state the ORDINARY call rule -- 'only the second and later arguments
	doc take name: labels' -- which is FALSE for a builtin at every position. Reusing E2052 here
	doc would answer a labelled second argument with a sentence saying second arguments may be
	doc labelled.
	doc It fires only for callees whose arguments this compiler decides are positional -- the MATH
	doc intrinsics, and that set is stated in exactly ONE place, 'Parser.calleeTakesPositionalArgsOnly'.
	doc Every member IS a machine instruction and will never acquire a declaration to take labels
	doc from. It covers the arity-1 members as well as the arity-2 ones, because the same wrong noun
	doc would otherwise survive one door over: 'abs(x: 1.0)' would keep answering E2052, whose
	doc sentence invites the nonsense fix 'abs(1.0, x: ...)'.
	doc It is NOT 'every builtin'. TWELVE bare builtins stay on the ordinary E2052/E2053 rule:
	doc 'print', 'runProcess', 'spawnReadLine', the seven 'subp*' streaming-subprocess builtins, and
	doc the two UAX #29 classifiers 'graphemeBreakProperty' / 'isExtendedPictographic'. Every one is
	doc a STAND-IN for a stdlib declaration that will have real, labellable parameters --
	doc 'subpWriteLine(h, line: s)' already LABELS its second argument on purpose, written the way
	doc the eventual method call will be written, and the two classifiers are named in
	doc 'Parser.maxon' as what must be retired first once the grapheme module can be loaded.
	doc ⚠ THE BOOTSTRAP DOES NOT AGREE, AND CANNOT. Measured: it answers all three of
	doc 'min(3.0, b: 5.0)', 'min(a: 3.0, 5.0)' and 'abs(x: 1.0)' with E2004 "Undefined variable",
	doc because its builtin path parses arguments with a bare expression parser and reads the label
	doc as a variable reference. That names the wrong thing entirely -- the defect is the label, not
	doc a missing binding -- which is the clearest argument for shv2 registering its own code here.

E2068 ParserMalformedNumericLiteral
	csharp ParserMalformedNumericLiteral
	shv2 parserMalformedNumericLiteral
	doc A numeric literal's text is not a number -- two decimal points ('1.5.3'), or any byte the
	doc number grammar does not admit.
	doc IT REPLACES A PANIC, and the panic's premise is why this code exists. The parser used to
	doc assert a malformed literal was UNREACHABLE from source, on the ground that Lexer.scanNumber
	doc only ever hands over digits, '_', '.', 'e'/'E' and an exponent sign -- so a rejection could
	doc only mean the lexer and the number reader had diverged, a compiler defect rather than a
	doc program's. That premise was FALSE: the lexer's trailing-byte rule appended a SECOND '.' to a
	doc token that already carried a fraction, so '1.5.3' reached the reader as '1.5.' and CRASHED
	doc the compiler. A parser must be able to answer 'that literal is not well-formed' with a
	doc position, and a layer that cannot say no is a layer whose caller's leniency has nowhere to
	doc surface.

E3001 SemanticNoMain
	csharp SemanticNoMain
	selfhosted semanticNoMain
	shv2 noMainFunction
	doc The program declares no 'main' function.

E3002 SemanticMainWrongReturnType
	csharp SemanticMainWrongReturnType
	selfhosted semanticMainWrongReturnType
	shv2 mainWrongReturnType
	doc 'main' declares a return type other than ExitCode.

E3003 SemanticUndefinedVariable
	csharp SemanticUndefinedVariable
	selfhosted semanticUndefinedVariable
	doc A name used in an expression has no binding in scope.

E3004 SemanticUndefinedFunction
	csharp SemanticUndefinedFunction
	shv2 callUnknownFunction
	doc A call names a function that does not exist -- a typo, or a callee not visible from here.

E3005 SemanticTypeMismatch
	csharp SemanticTypeMismatch
	selfhosted semanticTypeMismatch
	shv2 typeMismatch
	doc A value's type does not match the type required at this position.

E3006 SemanticDuplicateDefinition
	csharp SemanticDuplicateDefinition
	selfhosted semanticDuplicateDefinition
	shv2 duplicateDefinition
	doc A name is declared twice where exactly one binding is legal: two functions with the same signature, a top-level `let`/`var` declared twice in a file, a TYPE name claimed by two declarations, or a local shadowing a 'self' field.
	doc a duplicate definition or a local-vs-self-field shadow. Two
	doc shapes share the code: (a) two function definitions with the same
	doc name and signature -- fires from `reportDuplicateFunction` in the
	doc parser for same-file duplicates and from the project-level main
	doc registration for cross-file `main` collisions; (b) a local
	doc declaration inside an instance method whose name collides with a
	doc `self.field` (matches the C# bootstrap's broader interpretation of
	doc "duplicate definition" -- the local would silently shadow the field);
	doc (c) two top-level `let`/`var` declarations of one name in a file --
	doc fires from `isRedeclaredBinding` at the real parse in shv2 and from a
	doc per-file declared-name set in the bootstrap's CollectAndEvaluateTopLevelDecls;
	doc a `let` and a `var` share one storage key, so kind is irrelevant. The
	doc top-level twin of (a); (d) a TYPE name claimed by two declarations --
	doc `type` / `enum` / `union` / `interface` / `typealias` all file one name
	doc in one whole-program namespace, and a fixed resolution cascade used to
	doc pick the winner silently. Fires from `commitTypeNameDecls` in shv2 at the
	doc merge. Kind-independent exactly as (c) is: what is refused is the SECOND
	doc declaration of the name, whichever keyword introduced either of them. The
	doc one exception is two `typealias` declarations, which are E3061 -- and
	doc which are legal outright when they are the same alias FORM in different
	doc files, a non-exported typealias being file-local; (e) a name claimed
	doc twice in the COMPILED type namespace -- a generic instantiation has no
	doc source name, so the compiler joins its base to its arguments (`Box with
	doc String` -> `Box_String`) and derives every per-type symbol from that
	doc string, which a declared `type Box_String` derives its own from too.
	doc Fires from `checkTypeSymbolNamespace` in shv2, whole-program, after the
	doc instantiations are interned. Two INSTANTIATIONS can claim one compiled
	doc name as well, `_` being a legal name character and the join therefore
	doc not injective. A `typealias` is not a claimant here: it mints no symbol
	doc of its own.

E3007 SemanticAmbiguousFunctionCall
	csharp SemanticAmbiguousFunctionCall
	selfhosted semanticAmbiguousOverload
	shv2 semanticAmbiguousOverload
	doc A call cannot pick a unique overload: two or more candidates match it indistinguishably.
	doc call site cannot pick a unique overload because multiple
	doc variants have indistinguishable signatures at this call. Emitted when
	doc two overloads share the same parameter types and the caller doesn't
	doc disambiguate with named arguments. Matches the C# bootstrap wording.

E3008 SemanticSymbolNotExported
	csharp SemanticSymbolNotExported
	selfhosted semanticSymbolNotExported
	shv2 symbolNotExported
	doc A symbol is referenced from another file but is not declared 'export'.

E3009 SemanticUnsafeCast
	csharp SemanticUnsafeCast
	selfhosted semanticInvalidCast
	shv2 semanticUnsafeCast
	doc A conversion that cannot be proven safe: an explicit cast whose source range does
	doc not fit the destination (`256 as Byte`), an explicit cast that is lossy or
	doc meaningless (`5.0 as int`, `true as int`), or the SAME lossy conversion reached
	doc IMPLICITLY, where a value meets a declared type it cannot reach without loss
	doc (`takeInt(3.7)`, `return 3.7` from a `returns int`). One code, because it is one
	doc fact: the answer to every one of them is an explicit `trunc`/`round`/`floor`/`ceil`.
	doc The implicit half was a WRONG ANSWER until P1.0d.4 -- specs/type-casting.md
	doc rejected `5.0 as int` while specs/implicit-type-conversion.md silently truncated
	doc the identical conversion at a call argument.

E3010 SemanticUnneededCast
	csharp SemanticUnneededCast
	selfhosted semanticUnneededCast
	shv2 semanticUnneededCast
	doc A cast is redundant: the destination type already admits every value the source type can hold.

E3011 SemanticUnknownType
	csharp SemanticUnknownType
	selfhosted semanticUnknownType
	shv2 unknownType
	doc A named type resolves to no declared type -- a typo, or a type this compiler slice does not have.

E3012 SemanticUnusedVariable
	csharp SemanticUnusedVariable
	selfhosted semanticUnusedVariable
	shv2 semanticUnusedVariable
	doc A local binding is declared and never read.

E3013 SemanticMissingReturn
	csharp SemanticMissingReturn
	selfhosted semanticMissingReturn
	shv2 missingReturn
	doc A value-returning function can reach its end without returning, or a 'return' in it carries no value.

E3014 SemanticUnexportedFieldAccess
	csharp SemanticUnexportedFieldAccess
	selfhosted semanticUnexportedFieldAccess
	shv2 unexportedFieldAccess
	doc A field not declared 'export' is read or written from outside its own type.
	doc The gate is the TYPE, not the FILE, and it covers WRITES as well as reads --
	doc both measured against the bootstrap: `type B` touching `type A`'s unexported
	doc field is E3014 in the same file, and `v.private = 42` reports it exactly as
	doc `return v.private` does. This line said "read from another file" until P1.1a
	doc wave 3 and was wrong on both counts. Neither compiler's CHECK ever agreed
	doc with it -- v1 gates on `structName == enclosingType`
	doc (TypeResolution.maxon:8204) -- so it was the prose that drifted, and it
	doc drifted in one place and was read in three: this entry is the only copy, and
	doc correcting it corrects every generated registry at once.
	doc Initialization is exempt: every field, exported or not, may be named in a
	doc struct literal, because the type itself determines what fields exist and
	doc visibility only governs access AFTER construction (specs/export-var-fields.md).
	doc (was internally `semanticTypeResolutionCycle` while the
	doc unexported-field-access check was unimplemented). Bootstrap parity
	doc reclaims 3014 for the field-visibility diagnostic; the typealias-cycle
	doc diagnostic moves to 3091 (a code unused on both sides).

E3015 SemanticUnknownInterface
	selfhosted semanticUnknownInterface
	shv2       semanticUnknownInterface
	doc An 'implements' clause names an interface that does not exist.

E3016 SemanticPartialInterfaceImpl
	csharp SemanticPartialInterfaceImpl
	selfhosted semanticPartialInterfaceImpl
	shv2       semanticPartialInterfaceImpl
	doc A type claims to implement an interface but does not define all of its members.

E3017 SemanticWhereConstraintViolation
	csharp SemanticWhereConstraintViolation
	selfhosted semanticConstraintViolation
	shv2       semanticWhereConstraintViolation
	doc A generic argument violates the 'where' constraint declared on the type parameter.

E3018 SemanticUnknownField
	csharp SemanticUnknownField
	shv2       semanticUnknownField
	doc A field access names a field the type does not have.

E3019 SemanticImmutableRefToMutatingParam
	csharp SemanticImmutableRefToMutatingParam
	selfhosted semanticImmutableRefToMutatingParam
	shv2 semanticImmutableRefToMutatingParam
	doc An immutable ('let') binding is passed to a parameter the callee mutates, or a mutating method is called on it.
	doc a mutating method (push/pop/set/append/...) or a parameter-mutating
	doc function is called on an immutable (`let`-bound) receiver/argument. The
	doc receiver's data would be modified through a binding the user declared
	doc immutable. Emitted by the Maxon-tier borrow/mutation check.

E3030 SemanticEnumDuplicateCase
	csharp SemanticEnumDuplicateCase
	selfhosted semanticEnumDuplicateCase
	shv2 enumDuplicateCase
	doc An enum declares the same case name twice.

E3031 SemanticEnumDuplicateRawValue
	csharp SemanticEnumDuplicateRawValue
	selfhosted semanticEnumDuplicateRawValue
	shv2 enumDuplicateRawValue
	doc Two enum cases declare the same raw value.

E3032 SemanticEnumRawValueTypeMismatch
	csharp SemanticEnumRawValueTypeMismatch
	selfhosted semanticEnumRawValueTypeMismatch
	shv2 enumRawValueTypeMismatch
	doc An enum case's raw value has a type other than the enum's declared raw-value type.

E3034 SemanticEnumUnknownCase
	csharp SemanticEnumUnknownCase
	selfhosted semanticEnumUnknownCase
	shv2 enumUnknownCase
	doc A match arm names a case the scrutinee's enum does not have.

E3035 SemanticEnumWrongBindingCount
	csharp SemanticEnumWrongBindingCount
	selfhosted semanticMatchPayloadArityMismatch
	doc A match arm's payload-binding pattern supplies the wrong number of bindings for the case's payload.
	doc a match-arm payload-binding pattern (`case(x, y)`) supplies
	doc the wrong number of bindings for the case's declared payload. Emitted
	doc by `parseCaseBindingPattern` when an arity check can be performed at
	doc parse time, or deferred to TypeResolution when the case's owning
	doc union isn't yet known.

E3036 SemanticWrongArgCount
	csharp SemanticWrongArgCount
	selfhosted semanticWrongArgCount
	shv2 callArgCountMismatch
	doc A call passes a different number of arguments than the callee declares parameters.

E3037 SemanticCallUnknownArgLabel
	shv2 callUnknownArgLabel
	doc A call's 'name:' label names no parameter of the callee.

E3038 SemanticCallDuplicateArg
	shv2 callDuplicateArg
	doc Two arguments of one call target the same parameter.

E3054 SemanticMainCannotThrow
	csharp SemanticMainCannotThrow
	selfhosted semanticMainCannotThrow
	shv2 mainCannotThrow
	doc 'main' is declared 'throws'. There is no caller left to handle the error.

E3055 SemanticTryRequiresThrowingFunction
	csharp SemanticTryRequiresThrowingFunction
	selfhosted semanticTryRequiresThrowingFunction
	shv2 tryRequiresThrowingFunction
	doc 'try' is applied to a call that cannot throw.

E3057 SemanticThrowingFunctionRequiresTry
	csharp SemanticThrowingFunctionRequiresTry
	selfhosted semanticThrowingFunctionRequiresTry
	shv2 throwingFunctionRequiresTry
	doc A throwing call is made without 'try'.

E3058 SemanticOtherwiseRequiresTry
	csharp SemanticOtherwiseRequiresTry
	selfhosted semanticOtherwiseRequiresTry
	shv2 otherwiseRequiresTry
	doc 'otherwise' is written without a 'try'.

E3059 SemanticErrorTypeMismatch
	csharp SemanticErrorTypeMismatch
	selfhosted semanticOtherwiseTypeMismatch
	shv2 errorTypeMismatch
	doc An 'otherwise' handler's value has a type other than the 'try' expression's type.

E3060 SemanticAmbiguousTypeReference
	reserved retired 2026-07-14 -- the member was DECLARED but no site ever emitted it, which `error-codes check` now refuses. The number stays TAKEN: a retired meaning must not be silently reused.
	doc A bare type name has more than one reachable definition; qualify it.

E3061 SemanticDuplicateTypeAlias
	csharp SemanticDuplicateTypeAlias
	selfhosted semanticDuplicateTypeAlias
	shv2 semanticDuplicateTypeAlias
	doc Two typealiases in scope declare the same name.
	doc Two declarations of one name are legal only when both are `typealias` of
	doc the SAME FORM (both ranged, both function, both generic-instance) in
	doc DIFFERENT files -- a non-exported typealias is file-local
	doc (specs/duplicate-typealias.md). Every other pair collides: same file
	doc whatever the forms, and different files whenever the forms differ, since
	doc only the ranged registry is scoped per declaring file. shv2 raises this
	doc from `commitTypeNameDecls` when BOTH declarations are typealiases; a pair
	doc involving a `type`/`enum`/`union`/`interface` is E3006 instead.

E3062 SemanticUnusedTypeAlias
	csharp SemanticUnusedTypeAlias
	selfhosted semanticUnusedTypeAlias
	shv2 semanticUnusedTypeAlias
	doc A typealias is declared and never used.
	doc "Used" means the NAME appears in a type position in its OWN declaring
	doc file. An exported alias is exempt (one file cannot see another's uses),
	doc and being implicitly inferable from a bare `[...]` literal is not a use.

E3063 SemanticAmbiguousTypeAlias
	csharp SemanticAmbiguousTypeAlias
	selfhosted semanticAmbiguousTypeAlias
	shv2 semanticAmbiguousTypeAlias
	doc a bare-name type reference has more than one reachable
	doc typealias definition under directory-as-module rules. The user must
	doc write the directory-qualified form (`dir.Name`) to disambiguate.
	doc Mirrors E3095 (functions) but operates on the typealias registry.

E3064 SemanticDiscardedPureResult
	csharp SemanticDiscardedPureResult
	selfhosted semanticDiscardedPureResult
	doc a pure-function call's result was discarded. Pure callees must
	doc have their result used -- neither bare `foo()` nor `_ = foo()` is
	doc permitted. Fires from `checkDiscardedResults`.

E3065 SemanticDiscardedImpureResult
	csharp SemanticDiscardedImpureResult
	selfhosted semanticDiscardedImpureResult
	doc an impure-function call's result was discarded without the
	doc explicit `_ =` opt-in. The impure-call result still has to flow into
	doc either a binding (`let r = foo()`) or an explicit discard
	doc (`_ = foo()`). Fires from `checkDiscardedResults`.

E3066 SemanticEnumNotComparable
	csharp SemanticEnumNotComparable
	selfhosted semanticEnumNotComparable
	shv2 semanticEnumNotComparable
	doc A 'union' value is compared with '=='/'!='. Unions carry no synthesized equality; use 'match'.
	doc a `union` value was compared with `==` / `!=`. Unions carry no
	doc synthesized equality -- a new case can't be added without every match site
	doc being forced to handle it, so the only way to inspect a union is `match`.
	doc Enums (which auto-implement Equatable) are unaffected. Stdlib source is
	doc exempt (trusted). Emitted from `maybeRewriteCmpToEquals` in TypeResolution;
	doc mirrors the C# bootstrap's `SemanticEnumNotComparable`.

E3067 SemanticSelfAssignment
	csharp SemanticSelfAssignment
	selfhosted semanticSelfAssignment
	doc a textual self-assignment (`x = x`, `p.x = p.x`) or a
	doc discard-assignment whose RHS isn't a function call (`_ = 42`). The
	doc former has no effect; the latter is the historic shape for opting out
	doc of a "discarded result" warning, but a non-call RHS makes the discard
	doc nonsensical. Emitted from `parseVarAssignment`,
	doc `parseLocalFieldAssignment`, and `parseDiscardAssignment`.

E3068 SemanticRefIdentityOnPrimitive
	csharp SemanticRefIdentityOnPrimitive
	selfhosted semanticRefIdentityOnPrimitive
	doc The 'is'/'is not' reference-identity operator is applied to a primitive, which has no object identity.
	doc the `is` / `is not` reference-identity operator was applied to a
	doc primitive value (`int`, `float`, `bool`, `byte`, ...). Identity only has
	doc meaning for reference types (structs are heap pointers); primitives are
	doc values with no object identity. Emitted from the `is`-operand type check
	doc in TypeResolution. Mirrors the C# bootstrap's
	doc `SemanticRefIdentityOnPrimitive`.

E3069 SemanticEqRequiresEquatable
	csharp SemanticEqRequiresEquatable
	doc '=='/'!=' is applied to a type that does not implement Equatable.

E3070 SemanticBorrowConflict
	csharp SemanticBorrowConflict
	selfhosted semanticBorrowConflict
	shv2 semanticBorrowConflict
	doc borrow conflict -- a collection was mutated (e.g. `arr.push(x)`)
	doc while a reference into its backing buffer is still live (e.g.
	doc `let s = try arr.get(0) ...`). A growth could reallocate the buffer
	doc and dangle the outstanding reference. Emitted by the Maxon-tier
	doc `runMaxonBorrowCheck` pass. Mirrors the C# bootstrap's
	doc `SemanticBorrowConflict` (same code, same message format).

E3071 SemanticUnreachableCode
	csharp SemanticUnreachableCode
	selfhosted semanticUnreachableCode
	doc a statement appears in the same straight-line block body
	doc immediately after a `return`, `throw`, or `panic(...)`. The trailing
	doc statement is unreachable -- control never falls through a
	doc function-terminating statement -- so it is dead code. Emitted by the
	doc parser's statement-list loop when a terminator is not the last
	doc statement of its block. Control-flow that creates separate blocks
	doc (a `return` inside an `if`, with code after the `if`) does NOT trigger
	doc this: that following code is reachable on the else path. Mirrors the C#
	doc bootstrap's `SemanticUnreachableCode` (same code, same message format).

E3072 SemanticBuiltinTypeConstruction
	csharp SemanticBuiltinTypeConstruction
	selfhosted semanticBuiltinTypeConstruction
	shv2 semanticBuiltinTypeConstruction
	doc a struct-literal names a compiler builtin managed type
	doc (`__ManagedFile`, `__ManagedMemory`, `__ManagedDirectory`, ...). These
	doc carry an opaque runtime handle and must be obtained through their static
	doc factory methods (e.g. `__ManagedFile.openRead`), never built directly.
	doc Emitted by the parser at the struct-literal site. Mirrors the C#
	doc bootstrap's `SemanticBuiltinTypeConstruction`.

E3073 AsyncNonYielding
	csharp AsyncNonYielding
	selfhosted asyncNonYielding
	doc an `async f()` call targets a function that never yields (contains
	doc no await / I/O stub call, transitively). `async` is only for I/O-concurrent
	doc work -- a non-yielding callee would run to completion synchronously on the
	doc spawning thread, so the spawn is pointless. Emitted by `checkAsyncYielding`.
	doc Mirrors the C# bootstrap's `AsyncNonYielding`.

E3074 SemanticSubprocessUnsupportedTarget
	selfhosted semanticSubprocessUnsupportedTarget
	doc A user program targeting wasm32-wasi reaches the 'Subprocess' API (transitively
	doc calling a maxon_subprocess_* builtin). WASI has no process-spawn primitive, so
	doc subprocess cannot exist on that target -- rather than silently throwing
	doc 'spawnFailed' at runtime, the call is rejected at compile time. Guard the call
	doc with `#if not os(Wasi)` to exclude it from the wasm build.
	doc Self-hosted only: the C# bootstrap has no such check, so it declares no member.
	doc The number is held by THIS ROW, not by a comment -- which is how it used to be
	doc held, and why an agent grepping `= 307` saw it free. (Originally
	doc SemanticDiscardedEnumeratedIndex, retired with the withIterator redesign.)

E3075 SemanticMatchQualifiedCaseName
	csharp SemanticMatchQualifiedCaseName
	selfhosted semanticMatchQualifiedCaseName
	shv2 matchQualifiedCaseName
	doc A match arm names a case in qualified form (`Color.red`). The scrutinee's enum
	doc is already known at the arm, so the bare case name suffices.

E3076 SemanticConstructorRestriction
	csharp SemanticConstructorRestriction
	selfhosted semanticConstructorRestriction
	shv2       semanticConstructorRestriction
	doc a struct literal (`Type{...}`) is written outside the type's own
	doc methods. Construction is restricted to the type's own methods / static
	doc factories so invariants can be enforced at the single construction point;
	doc external callers must go through a `static function create(...)`. Exempt:
	doc `Self{...}` and `Type{...}` inside `Type`'s own body, plus construction
	doc through a typealias visible at the site (inner alias of the current
	doc type/extension, or a top-level alias). Emitted by `parseStructLiteral`.
	doc Mirrors the C# bootstrap's `SemanticConstructorRestriction`.

E3077 SemanticVarShouldBeLet
	csharp SemanticVarShouldBeLet
	selfhosted semanticVarShouldBeLet
	doc a local declared with `var` is never reassigned, so it should
	doc be a `let`. Detected after type resolution by
	doc `checkVarShouldBeLetForEntry` (a `var` whose name never appears in the
	doc per-function reassigned-var set). E3012 (unused variable) takes
	doc precedence -- an unused `var` reports as unused, not as should-be-let.
	doc Mirrors the C# bootstrap's `SemanticVarShouldBeLet`.

E3078 SemanticVarFromImmutable
	csharp SemanticVarFromImmutable
	selfhosted semanticVarFromImmutable
	doc `var b = a` (or `var b = a.field`) where the source `a` is an
	doc immutable (`let`) binding of a reference type (struct / enum /
	doc function). Copying an immutable reference-type binding into a mutable
	doc one would alias the same heap value through a writable name; the user
	doc must either keep `b` immutable (`let`) or take an independent copy via
	doc `clone()`. Value-typed sources (int/float/bool/byte) are independent
	doc copies and are allowed. Detected after type resolution by
	doc `checkVarFromImmutableForEntry`. Mirrors the C# bootstrap's
	doc `SemanticVarFromImmutable`.

E3079 SemanticEnumCannotHaveAssociatedValues
	csharp SemanticEnumCannotHaveAssociatedValues
	doc An 'enum' declares a case with associated values. Cases with payloads make it a 'union'.

E3080 SemanticUnionCannotHaveRawValues
	reserved retired 2026-07-14 -- the member was DECLARED but no site ever emitted it, which `error-codes check` now refuses. The number stays TAKEN: a retired meaning must not be silently reused.
	doc A 'union' declares raw values. Raw values belong to 'enum'.

E3081 SemanticMatchDiscardedBindings
	csharp SemanticMatchDiscardedBindings
	selfhosted semanticMatchDiscardedBindings
	shv2 semanticMatchDiscardedBindings
	doc A match arm binds a case's payload values and then never reads them. Drop the bindings, or use them.

E3082 SemanticEmptyBlock
	csharp SemanticEmptyBlock
	selfhosted semanticEmptyBlock
	shv2 semanticEmptyBlock
	doc a labeled control-flow block (`if`/`else`/`while`/`for`) has no
	doc statements between its header and matching `end`. The C# bootstrap
	doc rejects this so dead control-flow can't sneak past a refactor; emitted
	doc from `parseBodyOrEmpty` after `parseScopedStatements` reports no
	doc statements were consumed.

E3083 SemanticTryBlockNoThrows
	csharp SemanticTryBlockNoThrows
	selfhosted semanticTryBlockNoThrows
	doc a block-form `try 'label' ... end` body contains no throwing
	doc calls (no bare throwing call/methodCall, no routed `throw`). The
	doc construct exists to funnel multiple throwing calls into one handler;
	doc a body that can never fail has no error to route, so the `otherwise`
	doc handler is dead. Mirrors the C# bootstrap's `SemanticTryBlockNoThrows`.

E3084 SemanticTryBlockBindingNotMatched
	csharp SemanticTryBlockBindingNotMatched
	selfhosted semanticTryBlockBindingNotMatched
	doc the `otherwise (e) 'label' ... end` handler of a block-form
	doc try must contain a `match` on the error binding `e`. Without it, the
	doc synthesized (possibly union) error value is never inspected and the
	doc handler cannot dispatch on which error occurred. Mirrors the C#
	doc bootstrap's `SemanticTryBlockBindingNotMatched`.

E3085 SemanticUnionMatchPatternAmbiguous
	csharp SemanticUnionMatchPatternAmbiguous
	selfhosted semanticUnionMatchPatternAmbiguous
	doc a bare case-name pattern in a match on a synthesized error
	doc union is shared by two or more union members, so it is ambiguous --
	doc the user must qualify it as `EnumName.case`. Mirrors the C#
	doc bootstrap's `SemanticUnionMatchPatternAmbiguous`.

E3086 SemanticFieldNotInitialized
	csharp SemanticFieldNotInitialized
	selfhosted semanticFieldNotInitialized
	shv2       semanticFieldNotInitialized
	doc a struct literal omits a field that has no default value, is not a
	doc compiler-synthesized managed builtin, and (for a return-site `Self{...}`
	doc in a static factory) was not proven definitely assigned via `self.field =
	doc expr` on every control-flow path to the literal. Emitted from the parser's
	doc struct-literal field-init checks and the post-body definite-assignment
	doc dataflow pass.

E3087 SemanticRedundantContainsGet
	csharp SemanticRedundantContainsGet
	selfhosted semanticRedundantContainsGet
	doc an `if x.contains(k)` whose then-block immediately performs
	doc `try x.get(k) otherwise ...` on the same receiver/key pair does the
	doc lookup twice. The compiler asks the user to rewrite as `if let v =
	doc try x.get(k) ... 'lbl'` (or `if var`) so a single lookup serves both
	doc the membership test and the value bind.

E3088 SemanticSymbolNotInModuleScope
	csharp SemanticSymbolNotInModuleScope
	selfhosted semanticSymbolNotInModuleScope
	shv2 symbolNotInModuleScope
	doc A 'module'-visible symbol is accessed from outside its declaring directory subtree.
	doc Symbol declared with the `module` keyword (visible to the declaring directory
	doc subtree only) but accessed from outside that subtree.

E3089 SemanticTypeResolutionLeak
	selfhosted semanticTypeResolutionLeak
	doc A type-resolution artifact escaped into a later pass. An internal invariant.

E3090 SemanticLibraryDuplicateName
	selfhosted semanticLibraryDuplicateName
	doc Two '--library' directories share a basename, so their dir-derived namespaces would alias.
	doc two external library directories (from `--library=DIR` /
	doc `build.maxon library("...")`) resolve to the same basename, so their
	doc dir-derived namespaces would alias and symbols can't be disambiguated.

E3091 SemanticTypeResolutionCycle
	selfhosted semanticTypeResolutionCycle
	shv2 semanticTypeResolutionCycle
	doc A typealias chain refers back to itself.

E3092 SemanticUnusedExportedSymbol
	selfhosted semanticUnusedExportedSymbol
	doc An 'export' symbol is never referenced outside its declaring file. Drop the modifier, or make it 'module'.
	doc an `export` (global) decl is never referenced from any file
	doc outside its declaring file. The decl could be file-private (drop the
	doc modifier) or `module`-visible. Applies to functions, types, enums,
	doc typealiases, top-level constants, top-level vars, and exported struct
	doc fields. Emitted by `checkUnusedExports` after all files have been
	doc parsed and type-resolved.

E3093 SemanticExportableAsModule
	selfhosted semanticExportableAsModule
	doc An 'export' symbol is referenced only inside its declaring directory subtree. It could be 'module'.
	doc an `export` (global) decl is referenced only from files inside
	doc its declaring directory subtree. The decl could be downgraded to
	doc `module`-visible. Emitted by `checkUnusedExports`.

E3094 SemanticUnusedModuleSymbol
	selfhosted semanticUnusedModuleSymbol
	doc A 'module' symbol is never referenced outside its declaring file. It could be file-private.
	doc a `module` decl is never referenced from any file other than
	doc the one it was declared in. The decl could be file-private (drop the
	doc `module` modifier). Emitted by `checkUnusedExports`.

E3095 SemanticAmbiguousCrossFileCall
	selfhosted semanticAmbiguousCrossFileCall
	shv2 semanticAmbiguousCrossFileCall
	doc A bare function name has more than one reachable definition; qualify it as 'dir.name()'.
	doc a bare-name function call has more than one reachable
	doc definition under directory-as-module rules. The user must write the
	doc directory-qualified form (`dir.name()`) to disambiguate. Emitted by
	doc the cross-file fallback in `qualifyCalleeForContextWithType` when
	doc `methodNameIndex` returns multiple distinct visible candidates.

E3096 SemanticLibraryNotFound
	selfhosted semanticLibraryNotFound
	doc A '--library' directory does not exist on disk.
	doc an external library directory named via `--library=DIR` /
	doc `build.maxon library("...")` does not exist on disk.

E3097 SemanticEnumAccessorComparison
	csharp SemanticEnumAccessorComparison
	selfhosted semanticEnumAccessorComparison
	doc `==`/`!=` against an enum/union value's `.name`, `.ordinal`, or
	doc `.rawValue` accessor. Such a comparison is checking which case a value
	doc is, which must be done on the value itself (`value == Type.case` for a
	doc payload-free case, or `match value` for a union variant -- so adding a
	doc case forces every site to handle it instead of silently slipping
	doc through). Comparing the derived string/int accessor bypasses that.
	doc 3089-3096 are taken on the self-hosted side (semanticTypeResolutionLeak
	doc onward), so this shared diagnostic takes the next code free on both
	doc sides. Mirrors the C# bootstrap's `SemanticEnumAccessorComparison`.

E3098 SemanticPromiseErrorTypeMismatch
	csharp SemanticPromiseErrorTypeMismatch
	doc A promise is stored in a Promise type that does not name the error its thunk throws.
	doc `Promise` is parameterised by BOTH what its thunk returns and what its thunk throws:
	doc `Promise with T` is a NON-throwing promise, `Promise with (T, E)` one that throws E.
	doc Storing a promise in the wrong one is refused here -- including, in particular, storing
	doc a THROWING promise in a `Promise with T`, which is what used to erase the error type.
	doc That erasure was the root of a family of bugs, all of which this refusal (plus the
	doc two-parameter form that makes it avoidable) turns into impossibilities:
	doc - `otherwise (e)` had no type to give `e`, and silently handed back the raw i64
	doc promise handle typed `int`;
	doc - an associated-value error's payload had no static type to mm_decref, so it LEAKED
	doc (only a runtime `errorIsHeapPtr` bit in the box approximated the answer);
	doc - propagation (a bare `try await p` inside a `throws` function) had no error type to
	doc check the enclosing function's `throws` against, so a thunk throwing A could be
	doc awaited inside a function throwing B and A's ordinals reinterpreted as B's tags.
	doc The fix a diagnostic can name is the point: it says which two-parameter type to write.

E3099 SemanticCapturingClosureEscapes
	csharp SemanticCapturingClosureEscapes
	shv2 capturingClosureEscapes
	doc A closure that CAPTURES, ESCAPING the frame its captures point into.
	doc THE RULE: a closure that captures may not escape its defining frame.
	doc A closure captures BY REFERENCE: LowerClosureCreate allocates an environment and
	doc fills it with the ADDRESSES of the enclosing frame's stack slots, so that reads
	doc through a capture see later mutations of the captured variable. The environment is
	doc therefore only meaningful while that frame is alive. Let the closure outlive the
	doc frame and every captured read dereferences a dead frame -- the classic upward-funarg
	doc problem. It compiles clean and dies at runtime, so it is refused where the mistake
	doc is still legible rather than left to fault inside emitted code the author never wrote.
	doc The routes refused are every store the parser can see WITHOUT interprocedural
	doc analysis:
	doc - RETURNING one out of the frame that built it (`makeAdder` -- the common idiom, and
	doc the reason this exists). Returning a closure an OUTER frame built is fine and is
	doc allowed: that environment points into a frame that is still alive.
	doc - storing one in a struct FIELD, a GLOBAL/static, a CONTAINER (array/map literal)
	doc element, a union's associated-value PAYLOAD, or through a PAYLOAD BINDING (which
	doc looks like a plain local but is an alias INTO the enum's heap box, so assigning
	doc through it writes back). Each is one 8-byte slot holding the code pointer alone,
	doc and each is heap memory outliving every frame, so the store drops the environment;
	doc the call then passes env=0 and the first captured read dereferences null.
	doc DELIBERATELY NOT REFUSED -- the interprocedural route: a capturing closure passed as a
	doc CALL ARGUMENT to a callee that then stores it (`Handler.create(function(n) gives n +
	doc bump)`), and symmetrically a capturing closure arriving as a call's RETURN value. At
	doc that store the value is a *parameter*, and whether it carries an environment is a fact
	doc about the CALLER -- deciding it needs a per-parameter escape summary propagated over
	doc the call graph, i.e. escape analysis proper. That is scoped OUT of the bootstrap and
	doc stays a runtime nil-deref. Passing a capturing closure DOWN to a callee that only
	doc CALLS it is perfectly safe and must keep working.
	doc A NON-capturing closure is unaffected and must keep working -- it lowers to a plain
	doc MaxonFunctionRefOp, has no environment to lose, and passes every check above BY
	doc CONSTRUCTION rather than by an exception carved for it. It is what a table of handlers
	doc or passes keyed by a struct field is built from.
	doc What would MAKE the refused routes work is escape analysis plus by-value (or boxed)
	doc capture, so a closure's environment outlives the frame that built it. That is a real
	doc language mechanism and it is DELIBERATELY DEFERRED here: adopting it would change the
	doc by-reference capture semantics the closure specs currently pin. shv2 schedules it at
	doc P1.5, where it co-lands with `async` -- a green-thread capture IS an escape.

E3100 SemanticPromiseAlreadyAwaited
	csharp SemanticPromiseAlreadyAwaited
	shv2 promiseAlreadyAwaited
	doc A promise is awaited a SECOND time. `await` is LINEAR: a promise is awaited exactly once.
	doc 3100 rather than 3099: E3099 is SemanticCapturingClosureEscapes, which landed on main while
	doc this was in review. Both of us picked "the next code free" and both of us were right, because
	doc the registry is written down TWICE -- here and in maxon-selfhosted/Compiler/ErrorCode.maxon --
	doc and neither copy sees the other's pending codes. Same disease as the promise's two bits, one
	doc level up. Keep the two files in step until there is only one of them.
	doc The thunk owns its result and HANDS IT OVER at the await -- that is the ownership model the
	doc language already has everywhere else. A second await would take a second +1 on a payload the
	doc thunk only ever owned once, and the two releases underflow the refcount and free it twice
	doc ("mm_decref: refcount underflow (already zero)"). It is not an error-handling bug and does
	doc not need a throwing thunk: a plain `async` returning a String double-frees identically.
	doc So the double-free is made UNREPRESENTABLE rather than fixed. The check is flow-sensitive --
	doc two awaits of the same promise in mutually exclusive branches are each the only await on
	doc their own path, and are allowed; what is refused is a second await REACHABLE from a first.

E3101 SemanticThrowingFunctionAsValue
	csharp SemanticThrowingFunctionAsValue
	shv2 throwingFunctionAsValue
	doc A `throws` function is referenced as a VALUE (`let f = risky`). A function type cannot
	doc express `throws` -- the grammar is `function(T) returns U`, with no throws clause -- so the
	doc binding silently drops it, and there is no indirect try-call to carry it: `StdIndirectCallOp`
	doc has no ErrorFlag, unlike `StdTryCallOp`.
	doc Before this check the call was not merely unchecked, it was WRONG. The callee took the error
	doc return (ordinal in RDX, `xor rax, rax` in RAX); the indirect caller read RAX and ignored RDX,
	doc so the throw vanished and the caller received 0 -- the dummy -- as a normal result. `try` was
	doc bypassed entirely by round-tripping the function through a value.
	doc Refused rather than supported, on evidence: no spec, no stdlib file, and none of
	doc maxon-selfhosted's 191,487 lines ever wanted a throwing function value. Adding `throws` to
	doc the function-type grammar plus an indirect try-call ABI is a FEATURE, and would be built for
	doc nobody. If a real need appears, that is the shape -- and this code is what says so.

E3102 SemanticUseAfterMove
	shv2 useAfterMove
	doc A binding is READ after its owned heap value was MOVED OUT of it. Under shv2's static
	doc single-owner model an owned value (an owned String, a struct box) has exactly one owner; a
	doc `let u = t` / `s = t` whose RHS is a bare reference to an owned binding TRANSFERS ownership to
	doc the destination, and the source is left moved-from. A later read of the source is refused --
	doc it would read a value another binding now owns and will drop, and dropping it twice is a
	doc double-free. A WRITE to a moved-from `var` is legal (it revives the binding). shv2-only: the
	doc reference compilers refcount, so aliasing is legal there and there is no move to violate.

E3103 SemanticDivisionByZero
	csharp SemanticDivisionByZero
	shv2 semanticDivisionByZero
	doc A `/` or `mod` has a divisor the compiler holds as the constant 0 (`a / 0`, `a mod 0`, a
	doc divisor that folded to 0, or the float `a / 0.0` and `a / -0.0` -- both of which give an
	doc infinity). Unlike a possibly-zero divisor -- which is a throwing operation handled with
	doc `try` (E3057) -- a provably-zero one is neither recoverable nor safe: it is a bug, rejected
	doc at compile time.

E3104 SemanticTargetUnsupportedConstruct
	shv2 targetUnsupportedConstruct
	doc A construct whose RUNTIME SUBSTRATE exists on one target only is reached by a program compiled
	doc for another. shv2's green-thread scheduler and its clock/subprocess/IOCP imports are
	doc x64-windows only at this rung, so `sleep`, `runProcess`, the streaming-subprocess builtins and
	doc the `__Builtins` clock intrinsics all lower to runtime entries no arm64/wasm/linux backend can
	doc emit. The call is refused at ITS SOURCE SPAN, naming the runtime entry that has no lowering.
	doc Distinct from E3074, which is the self-hosted compiler's subprocess-specific wasm rejection:
	doc this one is about the target's substrate in general, not about one API.
	doc Before it, every such program PANICKED three tiers down in StdToWasm/StdToArm64 with no file
	doc and no line -- a compiler crash where a diagnostic was owed.

E3105 SemanticTypeAliasUnderlyingConflict
	shv2 semanticTypeAliasUnderlyingConflict
	doc Two FILES declare the same ranged typealias name over DIFFERENT underlying primitives -- one
	doc `int`, the other `float`. Two files declaring one name over two RANGES is legal and stays so
	doc (a non-exported typealias is file-local: `specs/duplicate-typealias.md`), because the range is
	doc resolved and enforced per declaring file. The underlying PRIMITIVE is not: the readers that
	doc have no file to ask from -- type resolution of a struct field, union payload classification
	doc reached from the emitted runtime, generic type-argument and conformance-signature
	doc canonicalization -- all read it through one bare-name door, so a name whose declarations
	doc disagree about int-vs-float has no answer that door can give. It is refused at the SECOND
	doc file's declaration rather than answered arbitrarily.
	doc Before it, the parser resolved such a name file-scoped while those readers resolved it
	doc last-wins, and the disagreement reached the backends: the x64 emitter panicked on an xmm
	doc value in a gpr slot, wasm emitted a module its own validator rejected, and a struct field
	doc typed by the alias silently compiled to the wrong width.

E3106 SemanticArrayResizeManagedElement
	csharp SemanticArrayResizeManagedElement
	shv2 semanticArrayResizeManagedElement
	doc `Array.resize` is called on an array whose ELEMENT TYPE IS MANAGED -- a struct, a `String`,
	doc a nested container, a boxed union. Such an element lives in the buffer as a refcounted
	doc POINTER, so the zeroed slots `resize` exposes are NULL: an absence, not a value. Maxon has
	doc no default constructor, so there is nothing correct to put there, and the array it hands
	doc back is one whose `count()` does not agree with its `get()` -- `count()` says N while
	doc `get(0)` throws. Refused at the call site, naming the element type.
	doc The refusal covers the WHOLE call and not only its growing half, because which half a
	doc `resize(n)` is cannot be known until it runs: `n` is compared against a length, and a
	doc compiler holds neither. The two halves therefore split by NAME. Growing supplies the element
	doc the type cannot invent -- `push(value)` appends one, `growFilled(newLength, value:)` grows to
	doc a length in one call -- and shrinking needs no element at all, so `truncate(newLength)` stays
	doc available for every element type. (Both of those are bootstrap-only: shv2 synthesizes `Array`
	doc rather than compiling stdlib/Array.maxon, and its roster has neither, so its message names
	doc only `push`.)
	doc It is refused only where the element type is KNOWN. An element that is still an unbound
	doc type parameter is not: one generic body serves every instantiation, and the deliberately
	doc sparse slot tables in `stdlib/Map.maxon` and `stdlib/Set.maxon` are built exactly that way
	doc -- they track occupancy in a parallel `states` column and never read a slot they did not
	doc write. The layer where an unwritten slot is a defined state is `__ManagedMemory.setLength`,
	doc and it stays available.

E3109 SemanticBufferSetManagedElement
	shv2 semanticBufferSetManagedElement
	doc The BUFFER surface's `set` (`arr.managed.set(index, value:)`) is called on an array whose
	doc ELEMENT TYPE IS MANAGED -- a struct, a `String`, a nested container, a boxed union. That
	doc setter is bounded by CAPACITY rather than by length (a 2026-07-30 user ruling), so it can
	doc write into `[length, capacity)` -- slots no length has published yet. For a TRIVIAL element
	doc that is the whole point: it is the stage-then-`setLength`-to-publish idiom the buffer surface
	doc exists to support. For a MANAGED element it opens a hole, because that region carries no
	doc OWNERSHIP: `__arr_decref` destroys only `[0, length)`, `push`/`insert`/`append` store AT
	doc `length` without destroying the occupant, and a grow or a copy-on-write detach copies only
	doc the live bytes and abandons the rest. A staged element is therefore owned by NOBODY until a
	doc `setLength` publishes it, and is leaked outright if one never comes.
	doc Refused rather than fixed, and the choice is deliberate. Making ownership follow the staged
	doc slot would put a destructor gate on every managed `push` -- a hot path -- and would make the
	doc `[length, capacity)`-reads-ZERO invariant conditional, which five separate array operations
	doc currently rely on. Refusing costs none of that, and costs no capability: `[0, length)` is
	doc exactly what the ARRAY surface's own `set(index, value:)` covers, which is length-bounded,
	doc owns what it stores, and is unaffected. So the message names it.
	doc Refused at the call site, naming the element type, and only where that type is KNOWN. It
	doc needs no carve-out for an unbound type parameter: `.managed` is unreachable through a shared
	doc generic body at all (a field access through it is refused one step earlier), so every site
	doc this rule can see has a concrete element.

E3110 SemanticBufferByteAccessManagedElement
	shv2 semanticBufferByteAccessManagedElement
	doc The BUFFER surface's RAW BYTE access -- `arr.managed.setByte(offset, value:)` or
	doc `arr.managed.byteAt(offset)` -- is used on an array whose ELEMENT TYPE IS MANAGED: a struct,
	doc a `String`, a nested container, a boxed union.
	doc A managed element does not live in the buffer as DATA. It lives there as a POINTER to a heap
	doc allocation, so the bytes those two members address are the bytes of an ADDRESS. Reading one
	doc hands a fragment of a heap address back to the program as though it were a value -- a silent
	doc wrong answer, and an information disclosure. Writing one corrupts the pointer in place, and
	doc the element it named is then unreachable and unreleasable.
	doc This is NOT the reason E3109 refuses `managed.set`, and the two must not be collapsed. That
	doc one is about OWNERSHIP of a staged element and applies only past the published length, so it
	doc has a harmless half and an exact replacement. This one is about the element's REPRESENTATION,
	doc so it holds at EVERY offset, published or staged, and there is no bound that would rescue it
	doc and no other spelling that does the same thing. Byte access to a managed element is not a
	doc thing a correct program wants.
	doc `byteAt` is refused alongside `setByte` rather than after it. It returns a value, so it
	doc corrupts nothing and trips no leak gate -- it is precisely the half that stays green forever
	doc if only the writer is fixed.
	doc TRIVIAL elements are untouched, which is the whole point of the rule being about the element
	doc type: `__ManagedMemory.create` can only ever yield a trivial element, so the byte-level
	doc staging the buffer surface exists for -- string building, a NUL terminator written at the
	doc length boundary -- is unaffected.

E3107 SemanticDuplicateTestName
	csharp SemanticDuplicateTestName
	doc Two `test` declarations in one file carry names that SANITIZE to the same symbol. A test has
	doc two names -- the prose one the reporter prints, and the mangled `__test_<sanitized>` one that
	doc reaches the symbol table -- and the sanitizer maps every character outside [A-Za-z0-9_] to
	doc `_`, so `adds two` and `adds-two` collide even though they read differently.
	doc The message names BOTH prose names and the symbol they share, because the collision is
	doc invisible in either name alone.
	doc Refusing it is independently worth doing: two tests with the same DISPLAY name are
	doc unreportable -- a reader of the report cannot tell which one failed -- so this rejects the
	doc identical-prose case for its own reason, not merely as a symbol-table accident.

E3108 SemanticTestingStdlibUnavailable
	csharp SemanticTestingStdlibUnavailable
	doc A `test` declaration needs something from `stdlib/Testing.maxon` that this compile cannot
	doc see. Two symbols there are reachable WITHOUT the author naming them: `TestFailure`, which
	doc every `test` implicitly throws, and `__TestReport`, which the handler the compiler
	doc synthesizes for an uncaught foreign error reports through.
	doc Because neither name is written by the program, neither can be a misspelling -- so the
	doc message names the missing symbol AND the file that has to supply it, rather than reporting
	doc an unknown type or an unknown function at a line whose source mentions neither.
	doc ONE code for both, because it is one condition seen from two sides: a compile whose Testing
	doc module is absent or truncated. Two codes would let a reader fix half the cause and meet the
	doc other half on the next build.

E3111 SemanticConflictingInterfaceConformance
	shv2 semanticConflictingInterfaceConformance
	doc One conforming type reaches ONE interface's requirement by two routes that select DIFFERENT
	doc members for it. A conforming type has exactly one witness table per interface and exactly one
	doc address per method slot, so an interface has to be conformed to exactly ONE way; two routes
	doc that substitute a requirement differently want two addresses in one slot.
	doc Both routes are ordinary source. `implements Conv with Whole, Conv with Real` names one
	doc interface twice and binds its associated type differently each time; and
	doc `implements Child with Whole, Parent with Real`, where `Child extends Parent` re-declares the
	doc same `uses` name, reaches `Parent` twice while naming no interface twice at all -- which is
	doc why the condition is detected where the two selections MEET rather than by a rule about the
	doc shape of the `implements` clause.
	doc It takes two overloads of the member's name to be reachable. With one member per name the
	doc second route simply fails to match and the type is refused as a partial implementation
	doc (E3016) instead; the overload set is what lets both routes succeed and disagree.
	doc The message names both selected members' SIGNATURES rather than their registration names --
	doc `convert#Real` is a key this compiler mints, not something an author wrote.

E3112 SemanticShadowedStdlibNameIsAlsoAMember
	shv2 stdlibShadowedNameIsAlsoAMember
	doc A user program declares a type name that a whitelisted stdlib module ALSO declares, and that
	doc same spelling is used by stdlib source as a MEMBER name -- a field, a method, or an argument
	doc label. The shadowing rule moves the stdlib TYPE declaration into the reserved `__` space so both
	doc declarations can coexist, and that rename is applied uniformly to the stdlib file's identifier
	doc tokens, which is what makes it reach every spelling of the type at once. A member is the one
	doc spelling that is also reachable from USER code, whose tokens are never rewritten, so the two
	doc sides would disagree about one name and the member would silently stop resolving. Refused
	doc rather than risked; rename the type in the user program.

E3113 SemanticThrowsTypeNotAnErrorEnum
	csharp SemanticThrowsTypeNotAnErrorEnum
	shv2 throwsTypeNotAnErrorEnum
	doc A function's `throws` clause names something that is not a declared enum or union.
	doc The error-flag ABI carries a caught error as `ordinal + ErrorFlagOrdinalBias` for a payload-free
	doc enum and as a heap BOX POINTER for a payload-carrying union, and which of the two it is comes
	doc off the DECLARED clause at the catch site while the throw site derives it from the value it
	doc actually throws. A clause naming anything with no declared cases -- an INTERFACE (`throws Error`),
	doc a struct, or a name that resolves to nothing at all -- leaves those two derivations free to
	doc disagree: `throws Error` throwing a payload-carrying union was decoded as an ordinal and the box
	doc was never released (exit 101, a leak, in BOTH compilers before this check existed).
	doc It is a rule about a FUNCTION's own clause, not about an interface REQUIREMENT's: a requirement
	doc whose `throws` names an interface is the abstract error channel `Error` exists for, is dispatched
	doc through the witness ABI, and is guarded separately by E3016.

E3114 SemanticAmbiguousWitnessDispatch
	shv2 semanticAmbiguousWitnessDispatch
	doc A method call on a constrained type parameter names something that MORE THAN ONE of the
	doc requirements its `where` constraints supply could be. An interface and its `extends` parents
	doc may each declare the name, and two constraints on one parameter may each declare it too --
	doc and those are DISTINCT requirements occupying DISTINCT witness-table slots, so "they are both
	doc called `label` and both take one argument" does not make them interchangeable: two same-named,
	doc same-arity requirements may differ in RESULT type, and are then met by two different members
	doc and two different addresses.
	doc DISTINCT means declared by two different interfaces. One requirement reached through two
	doc constraints -- `where T is Left and Right` where both `extends Root` -- is NOT this code: the
	doc accepted member is filed under (conformer, DECLARING interface, method name), so both routes
	doc stamp their slot from one filing and bind one address. Counting those two routes as two
	doc claimants was a false reject whose message named a requirement as its own rival.
	doc A witness dispatch compiles ONE slot offset into the calling function, so there is nothing to
	doc choose by and nothing a later pass could repair. Taking the first candidate is what the search
	doc did before this code existed, and it is a silent wrong function pointer rather than a
	doc diagnostic -- which is why the ambiguity is refused rather than resolved by declaration order.
	doc The message names BOTH claimants by their DECLARING interface, since that is the interface the
	doc author has to change; the cure is to rename one requirement or to drop one constraint.

E3115 SemanticNoWitnessRequirementForArgCount
	shv2 semanticNoWitnessRequirementForArgCount
	doc A method call on a constrained type parameter names a method that the parameter's `where`
	doc constraints supply SEVERAL requirements for, and the call's argument count matches none of
	doc them.
	doc It is E3036's case (an arity mismatch) at a call site where E3036 cannot be written: that code
	doc names ONE callee and ONE expected count, and here there are several, none of which is the one
	doc the author meant. Blaming an arbitrary candidate would print a true sentence about the wrong
	doc requirement -- the exact failure `interface Derived extends Base` produced before the
	doc requirement list became transitive, when `Derived.label` was reported to expect one argument
	doc on a call to an inherited zero-argument `label()`.
	doc With exactly ONE requirement of the name there is nothing to select between and the arity is
	doc reported by E3036 as it always was, so this code is reached only where a choice genuinely
	doc existed.

E3116 SemanticStaticRequirementNotDispatchable
	shv2 semanticStaticRequirementNotDispatchable
	doc A method call on a VALUE of a constrained type parameter names a requirement its `where`
	doc constraints declare `static`. A `static` member has no receiver, so there is nothing for the
	doc call to dispatch on; a `static` member is called on a concrete type, which is exactly what a
	doc `where` constraint does not name.
	doc The resolver matched a requirement by NAME alone, so it bound the static's witness slot and
	doc prepended a receiver the callee has no parameter for. That is an ABI disagreement inside a
	doc slot, not a near-miss: x64 compiled clean and answered correctly only because the spurious
	doc receiver landed in an argument register a zero-parameter callee never reads, while wasm's
	doc `call_indirect` -- which checks the declared functype against the target's own -- trapped
	doc `indirect call type mismatch` at runtime.
	doc Neither neighbouring message could carry it. E3036 states an argument COUNT, which is what the
	doc same call shape gets on a CONCRETE receiver and is about the wrong thing; E2015's "no `where`
	doc constraint declares a method" would be FALSE, since the constraint declares it and only its
	doc receiver kind is wrong.
	doc The OPERATOR form of the same hole takes E3005 instead, and deliberately: there the question is
	doc "is this requirement the `Equatable`/`Comparable` protocol?", a static one is not, and the
	doc author's cure is the sentence E3005 already prints for a throwing or wrong-arity look-alike.

E4001 IrUnsupportedExpression
	csharp IrUnsupportedExpression
	doc The IR builder met an expression form it cannot lower.

E4002 IrUnsupportedStatement
	reserved retired 2026-07-14 -- the member was DECLARED but no site ever emitted it, which `error-codes check` now refuses. The number stays TAKEN: a retired meaning must not be silently reused.
	doc The IR builder met a statement form it cannot lower.

E4003 IrUndefinedType
	reserved retired 2026-07-14 -- the member was DECLARED but no site ever emitted it, which `error-codes check` now refuses. The number stays TAKEN: a retired meaning must not be silently reused.
	doc A type reached the IR builder unresolved.

E4004 IrUndefinedVariable
	reserved retired 2026-07-14 -- the member was DECLARED but no site ever emitted it, which `error-codes check` now refuses. The number stays TAKEN: a retired meaning must not be silently reused.
	doc A variable reached the IR builder unresolved.

E4005 IrUndefinedFunction
	reserved retired 2026-07-14 -- the member was DECLARED but no site ever emitted it, which `error-codes check` now refuses. The number stays TAKEN: a retired meaning must not be silently reused.
	doc A function reached the IR builder unresolved.

E4006 IrInvalidFieldAccess
	csharp IrInvalidFieldAccess
	selfhosted irInvalidFieldAccess
	shv2 invalidFieldAccess
	doc A field access could not be lowered: the base is not a struct, or the field has no slot.

E4007 IrInvalidMethodCall
	csharp IrInvalidMethodCall
	doc A method call could not be lowered.

E4008 IrUnsupportedInstruction
	reserved retired 2026-07-14 -- the member was DECLARED but no site ever emitted it, which `error-codes check` now refuses. The number stays TAKEN: a retired meaning must not be silently reused.
	doc A lowering pass met an IR instruction it does not handle.

E4014 IrTypeCycle
	csharp IrTypeCycle
	selfhosted irTypeCycle
	doc a type definition contains a reference cycle -- it transitively
	doc contains itself through struct fields, union associated values, or
	doc container element types. Maxon's ownership model requires acyclic type
	doc graphs for deterministic destruction, so recursive type references are
	doc rejected. Emitted by the type-cycle check after type resolution. Mirrors
	doc the C# bootstrap's `IrTypeCycle` (same code, same message format).

E4015 IrDeclarationTakesCompilerOwnedName
	shv2 declarationTakesCompilerOwnedName
	doc a declaration in the source took a function name that belongs to the compiler.
	doc Reachable only from `stdlib/Builtins.maxon`, the one file whose declarations may
	doc carry the reserved `__` prefix (E2051's D6 exemption): the name is declarable
	doc THERE, so what E2051 refuses is not what this refuses. ONE rule, and the compiler
	doc can enforce it at the two places it USES such a name, since the full roster of
	doc names it owns is not enumerable (the dynamic families are built from the program's
	doc own type names).
	doc (1) The name is also a function the compiler EMITS into this program, and a program
	doc cannot hold two functions of one name. Detected where the collision first becomes
	doc visible -- a name index over the module's functions -- and told apart from an
	doc installer that ran twice by PROVENANCE, since a synthesized function carries no
	doc `sourceFilePath` and a parsed one always does.
	doc (2) The name is one the reachability prune ROOTS unconditionally, so the
	doc declaration is resurrected into a program that installs no runtime under that name
	doc -- with no body, because the pre-elimination passes skipped the one they were told
	doc was unreachable. Refused whether or not the compiler emits its own copy HERE, so
	doc the rule is about the name and not about which runtime floor a given program
	doc happens to carry.

E4016 IrBuiltinErrorEnumMismatch
	shv2 builtinErrorEnumMismatch
	doc the error enum a builtin runtime family throws is DECLARED in `stdlib/Builtins.maxon`
	doc (`__ManagedMemoryError`, `__ManagedFileError`, `__ManagedDirectoryError`), and this checkout's
	doc declaration disagrees with the ordinals the compiler's runtime transcribes as literals -- either
	doc the enum is not declared at all (the module was not loaded), or a case it declares sits at a
	doc different position than the runtime throws it at. The case ORDER is the wire format: a throwing
	doc runtime returns `ordinal + bias` as its error flag and a handler arm is selected from it, so a
	doc disagreement routes every handler arm to the wrong case SILENTLY. Reported rather than panicked
	doc because the declaration is now a property of a FILE on disk: `stdlib/Builtins.maxon` may be
	doc edited, while the two families the compiler still SYNTHESIZES (`__ArrayError`,
	doc `__DivisionByZeroError`) keep the panic, since only the compiler's own source can make those
	doc disagree.

E4099 IrUnresolvedCall
	selfhosted irUnresolvedCall
	doc internal compiler-invariant violation -- a `call` / `methodCall`
	doc / `tryCall` / `tryMethodCall` op survived past TypeResolution without an
	doc entry in `project.resolvedCallees` and without an "intentionally dynamic"
	doc dispatch shape (interface receiver / type-parameter receiver). Indicates
	doc a resolver-or-propagation gap; surfacing it here catches future
	doc regressions before lowering trips its own panic.

E5001 CodeEmitterRegisterPressure
	shv2 registerPressure
	doc Register pressure: a value set live across a call, an idiv, or a loop genuinely needs more registers than the target has, after the cold-spill splitter has moved every idle value out and rematerialized every constant. Only the programmer can resolve a hot overflow, so the compiler refuses rather than guessing.

E5002 CodeEmitterUnsupportedInstruction
	reserved retired 2026-07-14 -- the member was DECLARED but no site ever emitted it, which `error-codes check` now refuses. The number stays TAKEN: a retired meaning must not be silently reused.
	doc The code emitter met an IR instruction it cannot encode for this target.

E5003 CodeEmitterCoverageNeedsDebugInfo
	csharp CodeEmitterCoverageNeedsDebugInfo
	doc `--coverage` was combined with `--no-debug-info`. The instrumented binary's counters are
	doc anonymous numbers: what each one counts lives in the `.mxdbg` sidecar's coverage-point
	doc table, which `--no-debug-info` suppresses. Emitting them anyway would produce a `.mxcov`
	doc data file nothing could ever interpret, so the combination is refused rather than silently
	doc dropping either half.

E5004 CodeEmitterUnsupportedTarget
	csharp CodeEmitterUnsupportedTarget
	doc The requested target is one this compiler cannot write an executable for. maxon-sharp ships a
	doc PE writer and a Mach-O writer and no others, so the pairs it can emit are exactly x64-windows
	doc and arm64-macos.
	doc Refused rather than approximated, because the approximation was silent and wrong: the object
	doc writer used to be chosen by ARCHITECTURE alone, so `--target=x64-linux` reached the PE writer
	doc and exited 0 having produced a WINDOWS executable whose stdlib had been parsed with
	doc `#if os(Linux)` taken -- POSIX semantics inside a PE. `arm64-linux` and `arm64-windows` reached
	doc the Mach-O writer the same way, the latter writing a Mach-O named `.exe`.
	doc The DEFAULT target is checked too, not only an explicit `--target=`: the default is the host,
	doc which on a Linux box is x64-linux, so a plain `maxon build` there produced a PE as well.
	doc Source-level Linux awareness is unaffected and deliberately kept -- `#if os(Linux)` remains a
	doc legal predicate the parser evaluates. What is withdrawn is the claim to EMIT for it.

E6001 PeWriteError
	reserved retired 2026-07-14 -- the member was DECLARED but no site ever emitted it, which `error-codes check` now refuses. The number stays TAKEN: a retired meaning must not be silently reused.
	doc The PE writer could not produce the output image.

E6002 BinaryOutputNotReplaceable
	csharp BinaryOutputNotReplaceable
	shv2 binaryOutputNotReplaceable
	doc The previous build output at this path could not be removed before the build began, so this
	doc build cannot replace it -- it is locked (on Windows a RUNNING executable cannot be deleted)
	doc or read-only. Refused up front rather than at the write, because the write is logged and
	doc swallowed: carrying on would print "Wrote N bytes" and exit 0 over an untouched STALE binary,
	doc which is the exact confident-wrong-answer this deletion exists to prevent.
	doc The message names the LIKELY cause as well as the mechanical one: in practice the holder is a
	doc spec-test or a build of this same tree that has not exited yet, and the compiler cannot
	doc identify it (that would need RestartManager/NtQuerySystemInformation, imported nowhere here).
	doc It also states that the previous binary SURVIVED -- this is the one branch where a failed build
	doc leaves an output behind, so a `;`-chained build-then-test runs the stale one. The wait-or-kill
	doc deadline it quotes is `WedgeWatchdogMs` (maxon-shv2/Testing/SpecWorkerPool.maxon), read from
	doc the constant so the message and the watchdog cannot disagree.

E6003 BinaryOutputNotWritten
	shv2 binaryOutputNotWritten
	doc The backend reported no failure, yet nothing exists at the output path after the write. The
	doc build removed that path before compiling, so its ABSENCE afterwards means the writer did not
	doc produce it -- a swallowed IO failure. A build that cannot say where its output is must not
	doc report success.

E9001 InternalError
	csharp InternalError
	doc An internal compiler invariant was violated. This is a compiler bug.
