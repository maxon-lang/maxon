# Spec-port log

The dated trend of the `/spec-port` loop — one row per tick, read downwards. It records **what the loop
did**, the way `docs/optimization-log.md` records what the compiler's cost did. It is a TREND, not a
backlog: it never holds work. Work the loop uncovers but that is not this spec's — a defect in another
mechanism, a rung nobody has reached — goes to `maxon-shv2/PLAN.md`. **A case in the spec being ported is
never such work; that is implemented, not filed.**

⛔ **NOTHING IS LOAD-BEARING HERE, AND THAT IS A CHANGE.** This log used to retire a spec from the loop:
a `DEFERRED` row meant the selector skipped it forever. **Deferring is gone** (user ruling 2026-08-02,
`.claude/skills/spec-port/SKILL.md` §4 — you implement until every case passes, however large), the
selector no longer reads this file at all, and `PORTED` is the only outcome a row can carry. A spec
leaves the loop by being ported and by nothing else.

The order the loop walks is `maxon-selfhosted/Testing/SpecTestRunner.maxon`'s `whitelist` — ~265 names
ordered easiest-first by the v1 loop that earned the ordering.

`cases` is *cases passing / `<!-- test:` markers in the file*, and **the two are always equal** — a tick
does not end until they are. Rows dated before 2026-08-02 may show a shortfall; that is the old process,
and the difference is cases it disabled rather than implemented. Those are debt, not precedent.

| spec | date | outcome | cases | note |
|------|------|---------|-------|------|
| print-function | 2026-08-01 | PORTED | 4/4 | No compiler change: string interpolation already handled every case (`{x}`, `{10 + 5}`, repeated calls, two holes + literal text in one string). Suite 3258 → 3262. Goldens read and balanced; the ```stdout gate falsified by sabotage before the tick was closed. |
| panic | 2026-08-01 | PORTED | 4/4 | A user `panic("…")` dropped its message and exited 134 — while shv2's OWN range-check panic already printed a located message + stack trace and exited 1. One runtime, one door unwired. Now routed through the same `mrt_panic` (`StdOp.osRangePanic` → `osPanic`, two producers now), message baked at parse time; `RuntimeAbort.userPanic = 134` deleted as a dead claim. 338 goldens moved — the panic block went 4 instructions → 2 wherever stdlib panics appear. Suite 3262 → 3266. All 4 ported cases are literals and all 4 pass — nothing diverged. Interpolated holes still render `{}`; that is pinned by `panic-interpolation` (a separate unported spec), taken as tick 3. |
| function-declaration | 2026-08-01 | PORTED | 6/6 | A missing `returns` keyword (`function foo() int`) was not caught at the signature: `parseOptionalReturnType` reads "no `returns`" as VOID, so the header ended at `)` and the `int` fell into the body, dying as `E2015 Unsupported: int statement` instead of the spec's `E2001 unexpected token: 'int'`. New `requireSignatureLineEnd` at the real-parse site (not the shared helper, which runs before `throws`, and not the tolerant sweep, which would swallow it). Existing `matchExpressionStrayToken` generalised to `strayToken` — same E2001, same sentence, two raisers now — and the token-display fact deduplicated into `Lexer.tokenDisplayText` (3 open-coded readers → 1). Suite 3266 → 3272, exe −4,608 B against a measured control. Nothing shelved. Review found 5 code-quality issues, all fixed before the commit; the same-shaped hole in `parseInterfaceMethod` is left to the spec that covers it (§3b). |
| method-calls | 2026-08-01 | PORTED | 13/14 | A bare implicit-self field alias used as a METHOD RECEIVER (`inner.get()` inside `Outer`, where `var inner as Inner`) was refused E2015. ⭐ **The refusal was too broad and its stated premise was measurably FALSE**: it claimed "no struct-typed field can be constructed yet", but `Outer.create(Inner.create(42))` builds and runs, and the EXPLICIT `self.inner.get()` spelling of the identical call already answered 42 through the same lowering — only the bare spelling was refused. The premise holds solely for the SELF-REFERENTIAL shape it was written against (`type Node { var next as Node }`), which is inconstructible. Predicate narrowed from "is a struct-typed self-field alias" to **constructibility = the struct-typed-field graph is acyclic** (new `ProgramSignatures.structTypeIsConstructible`, a `visiting`/`settled` DFS, so mutual recursion `A`↔`B` is caught by the same rule); `byteArrayContains` moved to `Project.maxon` and exported so its two callers share one scan. `requireStructBase` deliberately NOT narrowed — its box is `binding.boundValue`, which a self-field alias leaves 0, and 0 IS the receiver. **`specs-shv2/self-field-struct-typed.md` is byte-unchanged and its 4 cases stay green**, refused at the identical positions. 4 error expectations retracted to shv2’s own registered diagnostics (E2053 ×2, E3036 wording, E3004) — each ratified by `docs/error-codes.txt` and by already-ported specs, not decided here; see the spec’s own note block. 1 shelved: `error-no-such-method-on-stdlib-generic` needs **Map (Phase 2, PLAN.md P1.0c)**. Review found 7 issues, all resolved before the commit — the largest being that the "can this raw slot name an aggregate, and what name?" two-step was written **five** tag-guard ways and four name-resolution ways inside this one type; it is now two helpers, call-for-call equivalent (**no committed fragment moved**). Suite 3272 → 3285, exe +10,240 B. |
| type-casting | 2026-08-02 | PORTED | 30/30 | Two cast rules, and the first was a LIVE SILENT WRONG ANSWER. **E3009**: `castHasNoLegalConversion`'s final clause deemed any two different SCALAR classes convertible — its own header stated the premise, *"different concrete classes convert ONLY when both are scalars (int↔float, bool↔int)"*, and it is false. Measured before the fix: `5.9 as Integer` printed **5** (silent truncation, the very conversion `implicit-type-conversion.md` already refused at every implicit door) and `true as Integer` printed **true** (the cast a complete no-op, the value keeping its bool type through to `print`). Only `int → float` legitimately crosses. **E3010** `unneeded cast` was `notEmittedBy: [shv2]` — absent, not broken: newly claimed in `docs/error-codes.txt` (regenerated, all four generated files) and implemented via a sparse `ValueDeclaredAliasMap` recording the ranged typealias a value's DECLARED type names, plus a non-throwing recoverable report — which is the crux, since `multiple-in-one-function` pins three diagnostics from one compile and `multiple-across-functions` two. ⭐ **The discriminator is that BOTH ends must be a named alias**: `let x = 42; x as Float` is legal while `identity(42) as JsonFloat` is E3010, differing only in that `identity` DECLARES `returns Integer`. v1 does this as a late IR pass over `MaxonOp.convert`; shv2 has no such op (a no-op cast emits nothing), so it moved to the parser — a departure with a reason. ⛔⛔ **The full suite then went RED at 6, and every one was a shv2-AUTHORED spec whose test PROGRAM had encoded the bug** — `x as int` as scaffolding, one of them (`register-pressure`) documenting the dependency outright ("`x as int` is a `cvttsd2si`"). The filtered run could not see it; only the unfiltered suite could. **Settled by measurement, not by judgement**: the bootstrap oracle refuses those exact shapes with the identical code AND identical wording. Repaired to `trunc(x)` under a user ruling, and both spellings lower through the same `MaxonUnaryOp.trunc` — so **the emitted IR of all six is byte-identical** and only the embedded source line moved. 11 non-host goldens (4 arm64-macos, 1 arm64-linux, 6 x64-linux) are stale pending the cross-target sweep. Spec byte-identical to `/specs` — **zero retractions, nothing shelved**. Review found 5 issues, all confirmed against the source by me and all fixed before the commit; the largest was that the `float → int` rule had just been written a SECOND time, 127 lines from the existing copy that serves the implicit doors — the exact split this tick was fixing — now one `isLossyFloatToInt` beside its already-factored mirror `widensIntToFloat`. Suite 3288 → 3318, exe +11,776 B against a measured control. |
| lexer-edge-cases | 2026-08-02 | PORTED | 26/26 | 14 cases were one fault wearing two hats: a PERMANENT refusal filed under E2015 `ParserUnsupportedFeature` ("a construct this compiler does not implement yet"). `dispatchTopLevel`'s fall-through also printed `tok.kind.rawValue` — the token's CATEGORY — where the reader needs its source text, which is why a stray `!` read as "top-level identifier". Now E2001 via the existing `tokenDisplayText` (punctuators carry the shared `EmptySourceText`, so reading `.value` directly would have rendered them empty). The 14th, `string-with-newline`, is the same fault in `evalConstCharLiteral`'s `multiByte` arm → E2004. Measured, and it is why this was a fix and not a shelve: `let x = 'e'` compiles at module scope and `let x = 'é'` does not, but the BOOTSTRAP refuses `'é'` there too under E2004 — so neither compiler is awaiting a feature, and E2015 misdescribed a permanent rejection. shv2 now agrees with the oracle byte-for-byte on `'é'`, which no spec covers. 1 retraction: `empty-squote-string` pinned to E2016 `ParserInvalidCharacterLiteral`, registered to shv2 ALONE, where the bootstrap folds the same program into its generic E2004. Nothing shelved. Suite 3379/0 after this tick. |
| missing-return-error | 2026-08-02 | PORTED | 4/4 | No compiler change: all 4 cases passed on the first run, spec byte-identical to `/specs` — zero retractions, nothing shelved. E3013 `missing return statement` was already wired at both doors the spec pins: a `returns` function with an EMPTY body (`no-return`), and one whose only `return` sits inside an `if` with no else path (`missing-else-return`), both located at the function's `returns` keyword column and both in the bootstrap's spelling verbatim. The case worth naming is `match-binding-return-plus-panic-arm` — a function body that is a single `match` with a payload-binding arm that returns, a sibling `panic(...)` arm, and NO `default` arm: every path returns or diverges, so the merge block is unreachable and must be treated as terminated or E3013 fires on a correct program. shv2 already treats it so, and the case is a run-test (exit 3, the bound payload) rather than a compilerError, so the green also says the binding survived to the return instead of the merge block merely being skipped. Suite 3383/0 after this tick. |
| unknown-keyword-error | 2026-08-02 | PORTED | 3/3 | All 3 cases were one fault, and it is the SAME fault `806bc0260` fixed one scope up the day before: a permanent syntactic refusal filed under E2015 `ParserUnsupportedFeature` ("a construct this compiler does not implement yet"), rendering `tok.kind.rawValue` — the token's CATEGORY — where the spec, the bootstrap and v1 all quote the token's SOURCE TEXT. A bare `foo` on a line of its own reported `Unsupported: identifier statement`; no future feature makes it legal, and shv2 already owns the code that says so. ⭐ **The fix had to be a NARROWING, not the whole-arm conversion `dispatchTopLevel` got**: four already-ported specs pin that arm's current sentence as live `maxoncstderr` expectations (`first-class-functions` `h.op`, `managed-memory-methods` `arr.managed`, `self-type-member-expression` `Self.make(1)`/`Gate.make(1)`, `static-method-statement` `Point.create()`) — and every one of them is a MEMBER ACCESS, a well-formed expression refused for a reason that exists only in statement position (a value-returning static discards a box nothing would free; a field read is a borrow with no one here to borrow for). That refusal really does await the ownership rung, so E2015 is honest for it; nothing else reaching the arm opens any construct at all. The discriminator is one token of lookahead — next token is `.` — matching the neighbouring `callStmt` arm's style, and it is deliberately kind-agnostic on the RECEIVER (`"abc".foo()` is a member access too), which makes the split conservative by construction: every token followed by `.` keeps the exact sentence it had before, and only tokens that are not move. No new error code, no registry change — `ParseError.strayToken` already rendered E2001 `unexpected token: '{got}'` verbatim. Spec byte-identical to `/specs`: zero retractions, nothing shelved. Review found 4 comment defects (a WHY sentence that said "permanent" and "permanent only until" of the same refusal; the `tokenDisplayText`-vs-`.value` rationale written down a FOURTH time against its own definition's "it is ONE definition" warning; a false premise about what the `else` arm enforces; a code/comment divergence on the lookahead guard) — all fixed before the commit, and it correctly refused to factor the two arms' shared `current()`-and-throw shape, which share three tokens of shape and no logic. Suite 3386/0 after this tick. |
| expected-expression-error | 2026-08-02 | PORTED | 2/2 | 1 of 2 red: `let x =` with nothing after the `=` reported `Expected expression but got 'newline'` where the spec says `'(empty)'`. ⭐ **`(empty)` is a documented cross-compiler WIRE VALUE, not a phrasing choice** — v1's `formatExpressionTokenValue` says so in its own comment (*"Mirrors `FormatTokenValueForError` in the C# bootstrap so messages like `Expected expression but got '(empty)'` line up across compilers"*), and both references keep it as a **SECOND renderer reserved for the E2004 expression path**, distinct from their general token renderer. shv2 had no such renderer at all: its five `expectedExpression` raisers each inlined `tok.kind.rawValue`, and `TokenKind.newline`'s rawValue is the NAME `"newline"`, not a spelling — so a missing operand was named after the layout token that closed the line. New `expressionOperandDisplayText` beside `tokenDisplayText`, plus the named wire constant `AbsentOperandText`. **The discriminator had to depart from both references**: they key on the token's VALUE being empty, which shv2 cannot, because in shv2 every punctuator and operator carries `EmptySourceText` too — that rule would have rendered the sibling case's `got '*'` and the three `got '-'` pins in `unary-negation`/`unary-operators` as `(empty)`. shv2 keys on the KIND, the thing that actually decides whether `rawValue` is a spelling (`TokenKind.star` is `"*"`) or a name for a position in the stream. All FIVE raisers were routed, not just the one on the failing path: they are one union variant whose `got` rendering was hand-inlined five times, and fixing one would have made the same variant render two ways depending on which raiser fired. EOF deliberately untouched — the bootstrap raises its own `ParserUnexpectedEof` BEFORE that renderer is reached, so `(empty)` was never EOF's answer there and there is no convention to mirror; what shv2 should report there is `lexer-parser-robustness`'s question (unported, PLAN.md:2423). Spec byte-identical to `/specs`: zero retractions, nothing shelved. Diagnostics only — no lowering, no codegen, no golden movement outside this spec. Review found 2 comment defects, both fixed: `tokenDisplayText`'s doc claimed "the kind's own spelling IS the text", which the new function had just proved false for the two layout kinds and which read as an invitation to re-unify the renderers and silently restore `got 'newline'`; and the reasoned-about-then-unrecorded boundary around the rest of the "rawValue is a NAME" family. I corrected the reviewer on one point: it wanted the `intLiteral` → `'integer literal'` reading (where both references print `'5'`) filed as a divergence, but NO spec on either side pins it, which makes it an unspecified corner rather than a divergence — §3b, no note and no rung. Suite 3388/0 after this tick. |
| duplicate-block-identifiers | 2026-08-02 | PORTED | 4/4 | No compiler change: all 4 cases passed on the first run, spec byte-identical to `/specs` — zero retractions, nothing shelved. ⚠ Worth knowing what this spec does and does not claim, because its NAME promises a check that its own text disclaims: the "Invalid Code — Duplicate at Same Level" example sits in a ```text block under the note *"Currently duplicate block identifiers at the same scope level are not validated. This is a known limitation."* — so it is documentation, not a case, and no compiler is asked to reject it. All 4 active cases are POSITIVE, and what they actually pin is block-identifier SCOPING: two sibling blocks with distinct ids, an inner `'check'` shadowing an outer `'check'` (which must be legal), a `while` nested inside an `if`, and — the one with teeth — an `if 'check' … else 'else_check'` whose else-arm carries its own id and holds a further nested `'nested'` block. shv2 already resolves all four, and each is a run-test rather than a compilerError, so the greens say the blocks ran and produced 2, not merely that the ids parsed. Suite 3417/0 after this tick. |
| method-call-on-parameter | 2026-08-02 | PORTED | 4/4 | No compiler change: all 4 cases passed on the first run, spec byte-identical to `/specs` — zero retractions, nothing shelved. What it pins is that a method's RECEIVER is resolved from the expression written, not assumed to be `self`, in the one shape where confusing the two is invisible: `other` is a parameter of the SAME type as `self`, so dispatching to the wrong receiver still typechecks, still compiles and still returns a number — it just returns the wrong one. Each case is a run-test with distinct field values chosen so the two readings disagree (`f1.callBarOn(f2)` must answer 42, `f2`'s value, not 10), which is what makes the greens evidence rather than coincidence. `method-with-args-on-same-type-parameter` adds the twin question for ARGUMENTS — `other.addTo(n)` must forward the caller's `n` while binding `self` to `other` (100 + 7 vs 50 + 7 ⇒ 57 pins both halves at once) — and `sibling-method-call-still-works` is the control from the other side: a bare `double()` with no receiver written must still find `self`. Related to `method-calls` (tick 5), which found the bare implicit-self FIELD alias refused as a receiver; the parameter and sibling doors here were already open. Suite 3421/0 after this tick. |
| stdlib-basic | 2026-08-02 | PORTED | 1/1 | `Math.exp` was `E3004 call to undefined function` — and the fix is ONE LINE, because `Math.exp` is pure Maxon (a Taylor series, no libm and no intrinsic) and shv2 simply was not loading the module: `StdlibLoader.whitelistedStdlibModules` is an explicit whitelist whose own header says *"WIDENING IS ONE LINE"*. Measured before listing, which is the check that file prescribes: `maxon-shv2 build stdlib/Math.maxon` exits with `E3001: No 'main' function found` AND NOTHING ELSE. Every clause of the file's collision rule was CHECKED rather than assumed (and re-checked at review against the sources): the module declares ZERO free functions — all ten are statics of `Math` — which makes the whole E3006/bare-builtin-shadow clause vacuous; it is instead the first listed module to CONSUME a bare builtin (`Math.pow` calls `floor(exponent)`, answered by `MaxonUnaryOp.floor`), which is the sound direction; `Math` and `Real` collide with no other listed module and `Math` is no builtin. ⭐ **Second listed module to carry LITERALS (14 float constants) and ZERO committed goldens moved** — the first independent confirmation that the 317-golden debt `Builtins.maxon` came due on was actually PAID by `StdlibFacts.unreachable` + `requireUnreachableStdlibStayedDead`. ⚠⚠ **BUT THE TICK FOUND A SILENT WRONG ANSWER, PRE-EXISTING AND NOT CAUSED BY THIS LINE, AND IT IS SHIPPING**: `Math.exp(-1.0)` returns 2.718 under shv2 where the bootstrap returns 0.368. Reproduced minimally with an oracle control — 21 lines, no Math and no stdlib: a `bool` from a float comparison, held across a loop, reads back FALSE (bootstrap 11, shv2 22). The golden shows the mechanism outright rather than by inference: the second `if` emits NO COMPARE and consumes EFLAGS from the loop header's INTEGER `cmpRegReg rcx, rax`, where `rcx == rax` at exit ⇒ ZF=1 ⇒ `above` false ⇒ the reciprocal is skipped. Filed on PLAN.md as its own rung (backend flags liveness, needs arm64 too, and wants a spec — ~3,420 corpus cases catch none of it). ⚠ **The golden this tick commits PINS that miscompiled lowering** and must be re-minted by that rung, not read as a specification. The spec's own case is `Math.exp(0.0)`, which takes the early return and cannot see any of it. Spec byte-identical to `/specs`: zero retractions, nothing shelved. Review confirmed all three load-bearing claims at the sources, upgraded the mechanism from inferred to proven, and caught the entry re-transcribing a prose roster of other modules' type names — the exact drift this file's own rule forbids, already disagreeing with the copy 20 lines above it. Suite 3422/0 after this tick. |
| stdlib-autodiscovery | 2026-08-02 | PORTED | 5/5 | No compiler change. 4 of 5 cases passed on the first run — `Math.pow` and `sqrt` autodiscover, link and answer correctly through both the qualified and bare spellings, which is what the previous tick's `Math.maxon` whitelist entry bought. ONE retraction, and it is §1 row (c) — an already-ported spec pinning the other spelling — reinforced by the registry: `wrong-arg-count` expected `E3036 … missing argument for parameter 'exponent'` and shv2 emits **the same code** with an arity sentence, `'Math.pow' expects 2 argument(s) but 1 were provided`. `docs/error-codes.txt` names E3036 `SemanticWrongArgCount` and documents it as *"a call passes a different number of arguments than the callee declares parameters"* — an ARITY rule, which is what shv2 states; the bootstrap folds a named-parameter diagnostic into the same code and shv2 has no second sentence for it. **Eight live cases across seven ported specs pin shv2's spelling**, so making the original text pass would have meant changing those eight — the HALT shape, not a retraction. ⭐ Following the previous tick's lesson I probed the paths this spec does NOT reach before committing goldens over `Math`: `Math.pow(2.0, -2.0)`, `Math.pow(9.0, 0.5)` and `sqrt(2.0)` all agree with the bootstrap oracle (both exit 7), so these goldens are not minted over the `Math.exp` miscompile filed yesterday. Review verified all eight pins LIVE by their owning markers, confirmed the registry text verbatim, and **caught two false claims in my own note**: I wrote that the bootstrap anchors "at the absent argument's column" (it does not — 3:20 is the member name `pow`, 3:15 the start of `Math.pow`, and the argument would sit near 26) and that "every one of them anchors on the callee" (the eight actually vary — bare name, whole call expression, method name — so the pins settle the SENTENCE and the column is whatever each emit site's `callRange` reports). Both corrected in the note. It also found, and I confirmed at both sites, that E3036 has TWO hand-written sentences in shv2 while the helper meant to prevent exactly that says so in its own header — filed on PLAN.md, since both spellings are pinned green and no spec will ever surface it. Suite 3427/0 after this tick. |
| pow | 2026-08-02 | PORTED | 5/5 | No compiler change. 5 of 6 passed on the first run — including `int-promotion` (`Math.pow(3, exponent: 2)`, ints promoted at the call) and `basic-with-stdlib-ir`, whose `<!-- IncludeStdlibIr -->` directive shv2's runner already honours. 1 SHELVED, and it is a gap in the caller's ABI rather than anything to do with `pow`: `fractional-exponent` asserts through `print("{result}\n")`, and float interpolation is **P1.2 wave B-float, blocked on shv2 having no f64 function-argument ABI** — the `{result}` hole has to call `mrt_f64_to_string`. Refused at the hole by `Parser.maxon:25104`, whose own sentence sequences it (*"float arrives next"*), and already shelved on identical grounds by `string-interpolation.md` (`float-interpolation`, `float-literal`) and `float-type.md` (`float-print-negative-and-repeat`). It is the ONLY case in the file asserting via a ```stdout block; every other computes a float through the same `Math.pow` call and reads it back through `trunc`, so `pow` keeps full coverage without it. ⭐ Continuing the discipline from the `Math.exp` finding, I probed the three special cases the spec's own Notes CLAIM but no case tests — `pow(0.0, y)`, `pow(1.0, y)` and a negative exponent — against the bootstrap oracle: both compilers exit 7, so these goldens are not minted over a wrong answer. ⚠ I botched the shelve on the first attempt in exactly the way §2 warns about — left a `test:` marker under the `disabled-test:`, which would have kept the case running while the file read as shelved — and caught it on the marker check before running anything. Review confirmed the replacement took, verified `Parser.maxon:25104` is really that refusal and that all three cross-referenced shelves are themselves `disabled-test:` rather than live. Suite 3432/0 after this tick. |
| abs | 2026-08-02 | PORTED | 6/6 | 6/6, spec byte-identical to `/specs`, **nothing shelved** — and it took FOUR mechanisms, not one gap. ⭐ **`abs`**: the 5th arity-1 float intrinsic beside `floor`/`ceil`/`round`/`sqrt`, across both IR tiers and all three targets. ⚠ **x64 departs from BOTH references and had to**: they lower it `andpd dest,[rip+mask]`, which shv2 cannot do for two independent measured reasons — `andpd`'s memory operand is m128 and faults unless 16-byte aligned while shv2's `.rdata` states *"No alignment and no padding"*, and the register form needs a second XMM the isel cannot mint (every `TargetVReg.virtual` names a Std `ValueId`, so a backend-minted vreg is classed `gpr`). shv2 clears the sign bit in place — `psllq dest,1; psrlq dest,1` — as ONE `TargetOp` whose encoder owns the pair, because two ops would define one vreg twice in an SSA tier; exact for ±0/±inf/NaN since it is purely bits. ⭐ **Spec harness**: `<!-- Args: … -->` now parsed and forwarded as argv from index 1, proven with an argv-echo probe AND a negative control (deleting only the directive line turned the case red). ⭐ **`CommandLine.args()`**: `StringError` synthesized exactly as `ArrayError` is (both are declared in stdlib modules the loader cannot list); new `String.indexAfter` over the existing `__strix_advance`; new `Runtime/CommandLineRuntime.maxon` with a CRT-rules splitter behind `GetCommandLineA` + one appended kernel32 IAT slot; E3104 compile-time refusals for arm64-macos/x64-linux/wasm. The bootstrap's `GetCommandLineW` + shell32 path was rejected deliberately: shv2's Windows lane is ANSI end to end, so a single UTF-8 producer would corrupt any argv fed to `__ManagedFile.openRead`. ⚠⚠ **The full suite caught a regression five filtered runs could not**: whitelisting `CommandLine.maxon` made a STDLIB module's internal types depend on the USER's `Byte` — `emitOwnedManagedMemoryResult` minted `Array with Byte` — so a program declaring a wide `Byte` failed to compile a module it never calls. **It was the SECOND sighting**: BATCH2 cured the identical sentence in `stdlib/URL.maxon` and named the cure `__StringByte`, a name too specific to generalize, so the next synthesized byte buffer walked into it again. Renamed `__ManagedByte` for the CLASS, and `StdlibLoader`'s collision rule gained the sixth clause it was missing. ⚠ **Review then found the same defect shape once more, in this tick's own work**: `StringError`'s ordinals were literals in `StringIndexRuntime` and a seeded case ORDER in `Project`, with nothing tying them — five other builtin-error families share `BuiltinErrorOrdinalCheck` and this new sixth had not joined. Fixed and SABOTAGE-VERIFIED: swapping the two case lines now aborts, where before the fix that swap left the suite green. It also caught `NulTerminatorByte = 1` sitting in scope with `NulByte = 0` (a "cleanup" swapping them overflows the scratch by one on the terminating store) and a `requireWin64FileIo` panic that had outlived its first family into a wrong-noun diagnostic. One committed case rewritten under an explicit user ruling: `error.throws-a-stdlib-error-shv2-does-not-declare` pinned shv2's ONE measured divergence from the oracle on `throws StringError`; synthesis removes the divergence, so it became `throws-a-stdlib-error-shv2-synthesizes` and now pins the agreement (both compilers exit 2). Three roster-text expectations moved because `indexAfter` genuinely exists and the absent-clause was corrected (`indexBefore` needs the backward segmenter, `charAt` needs `makeCharFromBytes` — they had been wrongly lumped together, the third distinct wrongness in that one sentence). 6 non-host fragments (arm64-macos, x64-linux) carry the old roster text pending their sweep. Suite 3462/0 after this tick. |
| unused-parameters | 2026-08-02 | PORTED | 6/6 | 6/6, one retraction. shv2 emitted E3012 for NOTHING, so this needed the check end to end: a per-function candidate column, a `referenced` bit written at the three read doors, and the per-METHOD interface waiver (an implementer is forced to declare every parameter the contract names). ⭐ Scoped to PARAMETERS on measurement, not taste — all 6 cases here are parameters, and locals are `unused-variables.md`'s subject at whitelist 170, 144 places down; the first brief built locals too and reddened 6 cases that had no legal repair (a borrow of an opaque generic element cannot be bound (E3012), discarded (E3064) or used, since a type parameter has no operations). ⚠ Two defects the tick REACHED, both fixed: `_` was refused as a duplicate parameter name (E2014) though the oracle accepts it, and a defaulted argument's slot was keyed by the parameter's NAME — lossy the instant two parameters are both `_`, the project's signature bug one door over (E3038; now an explicit `argDefaultSlots` column, source order and the documented `argImmutableNames` contract untouched). The retraction: `void-function-unused` wanted the bootstrap's semantic E3005 + E3012; shv2 refuses at the PARSER under its own registered E2053, so the parse unwinds and the second diagnostic structurally cannot follow — program left byte-identical, divergence recorded in the case. 21 committed cases across 11 specs had been declaring dead parameters and were repaired: 19 `_` renames, 5 expectations flipped (two user rulings — no exemption, match the bootstrap). Re-enabled 1 debt case; added 2 (a runtime stand-in for a flipped case, and the waiver's positive half — SABOTAGE-VERIFIED, and the sabotage showed the waiver was load-bearing for 7 further cases none of which was named for it). ⚠ `stack-arguments/a-stack-argument-store-does-not-clobber-an-argument-register` is flipped and its x64 ABI property is now pinned by NOTHING; recorded in the file, deliberately WITHOUT a stand-in, because every legal shape makes the other parameters live and that shape already passed without catching the bug. ⚠ arm64 goldens for `register-pressure/hot-loop-overflow-arm64` are stale by one line — an arm64-ONLY case, so no host here will ever refresh them. Suite 3483/0 after this tick. |
| min | 2026-08-02 | PORTED | 16/16 | 16/16 (14 ported + 2 shv2-authored, additive). `min` is shv2's FIRST bare arity-2 builtin — every prior one (`trunc`/`floor`/`ceil`/`round`/`sqrt`/`abs`) is arity-1, so all 14 cases failed identically on E2053 *before* `min` was ever resolved: the label rule had never had to exempt a builtin. Oracle-measured — one program containing both `min(3.0, 5.0)` and a user `twoArg(1.0, 2.0)` drew E3005 on the USER call only. Built `MaxonBinOp.min`/`StdBinOpcode.min` across both IR tiers and all three targets, sharing the arity-1 operand path rather than forking it. ⭐ **THE NaN/−0.0 DECISION IS THE FINDING: there was no reference semantics to copy.** The bootstrap emits raw `minsd` on x64 and raw `fmin` on arm64, and those disagree on NaN and on ±0 — so BOTH references ship a `min` whose answer depends on the target, and no spec case sees it. Adopted `min(a,b) = a when a < b else b` (one sentence; `a < b` is false on unordered AND for `+0.0 < -0.0`), and made all three targets bit-identical to it: x64 `minsd`, arm64 `fcmp`+`fcsel mi` (deliberately NOT `fmin`), wasm `f64.lt`+`select` (deliberately NOT `f64.min`). Verified three ways — bootstrap and shv2 agree on x64, wasm matches under wasmtime, arm64 decoded from the emitted Mach-O. ⚠ New E2067 `ParserBuiltinArgNamed`: without it `min(3.0, b: 5.0)` was SILENTLY ACCEPTED with the label dropped. Widened to all seven math intrinsics — `abs(x: 1.0)` had been E2052, whose sentence promises later arguments take labels, which is false for a callee with no declaration; leaving it would have been the defect class surviving inside its own fix. No live E2052 pin is on a builtin (all four are user functions/methods), and the bootstrap has no reading of these programs that mentions labels at all — it reports `E2004 Undefined variable 'b'`, because its builtin tables bypass `ParseCallArgs`. Review caught the roster written down TWICE 33,000 lines apart, with this diff's own comment scheduling the break for the `max` tick; the emit arms now route THROUGH the roster, and the consolidation shrank the exe 4,608 bytes. ⚠ Floats compile and RUN under wasm32-wasi, contradicting CLAUDE.md's Targets note — reported, not edited. Suite 3500/0 after this tick. |
| max | 2026-08-02 | PORTED | 14/14 | 14/14, byte-identical to `/specs`, no compiler-design work — this is `min`'s mirror (`c0802e309`, one commit earlier), and the tick's whole question was whether the family's SECOND member could be added by sharing rather than copying. It was. ⭐ `TargetOp.minsdRegReg` became `minMaxSdRegReg(dest, lhs, rhs, kind FloatMinMaxKind)` — one variant with a discriminating field instead of a parallel `maxsdRegReg` twin, which would have duplicated an arm in the operand model, the printer, the register allocator, the live-range splitter, two liveness tables, `constDefInfo`, the prologue pass and both foreign-ISA panics. **Zero new arms**; the review independently confirmed all SIX destructuring sites handle `kind` coherently (two ignore it correctly — `kind` names no register) and that the single op-construction site cannot mint a wrong kind. New `FloatMinMaxKind` beside `FloatRoundMode`, plus `floatMinMaxKindFor` and `floatMinMaxSelectPred` in `StdLoweringShared`, so the min-vs-max decision is stated ONCE and read by all three isels. arm64 needed no new `TargetOp` (`arm64Fcsel` already carried `cond`) and wasm no new emitter (`f64Gt` already existed). ⭐ **The select condition is DERIVED, not chosen per target**: `floatMinMaxSelectPred(kind)` feeds each backend's existing float-compare mapping, so `min`/`max` cannot drift from source-level `<`/`>` on a NaN. Semantics mirror `min` exactly — `max(a,b) = a when a > b else b`, false on unordered AND for `-0.0 > +0.0` — with the NaN reasoning widened in place to cover the pair rather than copied. Measured three ways: shv2 and the bootstrap both exit 5 on x64, wasm identical under wasmtime (native `f64.max` would have differed, so the run positively distinguishes `f64.gt`+`select`), and arm64 decoded from the Mach-O — `fcsel gt` (cond=12) against `min`'s `mi` (cond=4). ⚠ **The near miss was `hi`**, which reads TRUE on unordered: it compiles, passes every ordered case, and is wrong only on NaN — and `Arm64CondCode`'s comments CLAIMED `hi` was "float >", one fact written twice with the wrong copy naming exactly the trap. Corrected. Zero churn in any pre-existing golden. Also removed a second copy of the math-intrinsic roster from the E2067 registry doc, which now points at `calleeTakesPositionalArgsOnly`. Suite 3514/0 after this tick. |
| sin | 2026-08-02 | PORTED | 3/3 | 3/3, one retraction — and the spec was a 3-case trigger for a subsystem. `Math.sin` and int→float promotion already worked; the gap was **float interpolation**, refused outright by E2015. ⭐ **THE FORMAT IS A USER RULING, BECAUSE THERE WAS NO CORRECT REFERENCE TO COPY.** The bootstrap's formatter rounds the fraction to 6 decimals and **never carries into the integer part** — `1.9999999` prints `1.999999`, `3.9999996` prints `3.999999`, and above 2^63 it emits `-9223372036854775808.775808`. `/specs` ENCODES that defect: `sin(1.5708)` is `0.9999999999932534` and is written `0.999999`; `/specs/log.md` and five expectations in `/specs/log10.md` do the same for values that are exactly 1, 2, 3 and 4. Ruled: shv2 prints the **SHORTEST ROUND-TRIP** representation (Python/JS/Rust/Go/Swift/Java/.NET), which makes the whole class unreachable rather than fixing one instance. One retraction — `sin.basic`'s `stdout` — documented in the case; round-trip is structural (a candidate is returned only after re-parsing to the identical bits). ⭐ **The float subsystem is ONE algorithm in `stdlib/Builtins.maxon`** serving all FOUR readers/writers — the compiler's literal reader, its diagnostic renderer, the runtime `"{x}"`, and `float.fromString`, which converged onto it (new `bitcastI64ToF64` across three backends + `__Builtins.bitsToFloat`; the naive reader deleted). ⛔ **The convergence is what proved the collapse was load-bearing rather than tidy**: forcing the exact reader into a real program for the first time exposed that it never accepted a leading `-`, that **shv2's own allocator could not compile it** (E5001, hidden because DFE had always dropped it), and **two silent wrong answers** (`"e5"` → `+0.0`, `"1.2.3"` → `1.23`). ⚠ **Three shv2 BACKEND defects, all reproducible with no float in sight, all fixed with sabotage-verified pins**: compare/branch fusion proved per-BLOCK but recorded per-VALUE (a lossy key — a second branch emitted a bare `jcc` on stale flags, no `cmp`, no `setcc`), which **closes the filed `Math.exp` miscompile** (`exp(-2.0)` returned the un-inverted `7.389…`); a reuse-def transient counted only against the full pool on a comment's false premise; and `emitScopeDrops` writing to an implicit destination (double free). ⚠ **`1.5e` compiled as `1.5`, and `1.5.3` CRASHED the compiler** having silently dropped the `3` — fixed in BOTH layers (lexer lookahead + `malformed` becomes E2068 instead of a panic), each sabotage-verified alone. Same `FormatException` hole fixed in the bootstrap, so both compilers now agree. 8 disabled tests unblocked and enabled; 2 re-shelved with measured reasons. 38 `specs/fragments` regenerated (bootstrap type-ids shift +2 for two new stdlib types). Suite 3566/0 after this tick. |
| cos | 2026-08-02 | PORTED | 3/3 | 3/3, one retraction, **no compiler change** — the cheapest possible tick, and that is the point: `sin` built float interpolation and `cos` simply rides it. Two cases passed on the copy; the third failed only on the `stdout` FORMAT. ⭐ **This is the first recurrence of the shortest-round-trip ruling, and it is a MECHANICAL consequence, not a fresh judgement** — every `/specs` file that prints a float renders it in the bootstrap's fixed-6-decimal form, so every one needs the same retraction. `log`, `log2`, `log10`, `tan`, `atan2` and `pass-by-reference` are still owed it; `log.md` and five expectations in `log10.md` additionally encode the carry-loss defect itself. The prose deliberately does NOT restate the argument — it points at `specs-shv2/sin.md`, which carries it once. ⚠ **Verified rather than assumed, because a shortest digit string is only correct for the exact double it came from**: `Math.cos` is bit-identical under both compilers for all four inputs, and `cos(1.0)` prints `0.5403023058681397` — NOT the more familiar `0.5403023058681398`, which is a DIFFERENT double one ulp up (`…764` vs `…763`). Review then found the corroboration that makes the retraction self-checking without running anything: each `/specs` value is exactly the 6-decimal rendering of the double replacing it, so the two blocks describe the same four numbers and only the formatter differs. That sentence is now in the file. Suite 3569/0 after this tick. |
| tan | 2026-08-02 | PORTED | 4/4 | 4/4, one retraction, **no compiler change** — the second consecutive mechanical recurrence of the shortest-round-trip ruling (after `cos`), and the pattern is now settled: 3 of 4 cases passed on the byte-identical copy, and the only failure was the `stdout` FORMAT. Still owed the same retraction: `log`, `log2`, `log10`, `atan2`, `pass-by-reference`. The prose points at `specs-shv2/sin.md` for the argument rather than restating it. ⚠ **Verified per file, and the per-file check is not ceremony**: `Math.tan` is bit-identical under both compilers here (`0`, `4603095874924660554`, `4609692760021066663`, `4607182435342692924`), and each old value is exactly the fixed-6-decimal rendering of the double replacing it (`0.5463024898437905` → `0.546302`, `1.5574077246549025` → `1.557408`, `1.0000036732118494` → `1.000004`), so the two blocks describe the same four doubles. ⭐ **Review caught a real error in the retraction's PROSE that the green suite could not**: those bit patterns were labelled the INPUTS when they are the RESULTS. The numbers were right and the noun was wrong — the worse failure mode, because a reader checking the claim as written finds it false, and `4607182418800017408` (the true bits of input `1.0`) is itself listed in `cos.md`, so a wrong reading confirms easily. `cos.md` words it correctly; the slip was new to this file. It also independently confirmed minimality (each digit string is the shortest round-trip; dropping any significant digit breaks it) and noted that MSVC's libm returns one ULP either side for two of these — harmless, since both ULPs render to the same 6-decimal value and the case is run-asserted green. Suite 3573/0 after this tick. |
| atan2 | 2026-08-02 | PORTED | 8/8 | 8/8 (7 ported + 1 added), and the tick found a WRONG ANSWER, not a format gap. ⭐ **`Math.atan` was inaccurate by ~1e-2 near \|z\| = 1** — its 24-term Taylor series IS the Leibniz series at z = 1, converging like 1/n — so `atan2(1,1)` printed `0.7953941713587581` where π/4 is `0.7853981633974483`. Both compilers shared it (shared stdlib). ⚠ **It survived because the only two cases exercising an off-axis path PRINTED WITHOUT ASSERTING**: every other case lands on an axis, where `atan2` returns a stored literal or hands `atan` a zero the series never sees. The one code path with real arithmetic was the one path nothing checked. Fixed by range reduction (oddness + reciprocal + half-angle) so nothing reaches the series above `tan(π/8)`; worst error over 68 points now ~2 ULP. ⭐ **The termination proof is subtler than the code said**: `(z−1)/(z+1)` is an INVOLUTION whose fixed point is √2−1, so a threshold below it maps the fold's output straight back and recurses forever — demonstrated at 0.3 (stack overflow). The constant is the correctly-rounded √2−1, 0.14 ULP under the true value, with **no double in the gap**. ⚠ **Review then found a live numerical defect nobody was hunting: TWO SPELLINGS OF ln 2** in one file — `0.693147180559945` (2.7 ULP low, multiplied by the exponent up to ~1000 inside `log`) beside the correct `0.6931471805599453`. One fact, two sites, already drifted. Collapsed to one constant; `log`'s worst error 7 ULP → ≤2, which moved `pow/fractional-exponent` from 4 ULP under 2 to 1 — **making that spec's own prose ("lands one ulp under 2") true for the first time**. Consolidated π/2π/π-half/π-quarter/ln2/ln10/tan(π/8) to one definition each (π had 3 copies), extracted `lnBySeries` (the atanh series was duplicated between `log`/`log2`) and `reduceToPiRange` (duplicated between `sin`/`cos`), named all five series term limits. ⚠ **IEEE signed zeros: 4 of 8 `atan2` zero combinations were wrong**, plus `atan`/`sin`/`tan` returning `+0.0` for `-0.0`. All fixed and pinned (sabotage flips 3 of 6 lines). An agent first called this unfixable — "no way to observe a zero's sign" — after grepping the COMPILER for `signbit`; the working idiom was in `stdlib/Builtins.maxon`, and `__Builtins.floatToBits` had been added three ticks earlier. **Absence of a name you guessed is not absence of a mechanism.** ⚠ **A BOOTSTRAP defect found on the way: `-0.0` LITERALS were silently miscompiled** — the float constant pool was keyed by value, and `double.Equals(+0.0, -0.0)` is true, so both shared one rdata slot and the first spelling in the file won. Order-dependent, in the compiler this loop uses as its ORACLE. Re-keyed on the bit pattern. Its two exposed goldens had the miscompile baked in and passed because hash/compare equality is insensitive to it — a golden encoding the bug it should have caught. Regenerated; they are now the fix's only pin. C# suite 3237/0. Suite 3581/0 after this tick. |
| log | 2026-08-02 | PORTED | 6/6 | ⭐ **THREE SPECS IN ONE TICK, by user ruling, because they share an implementation and needed ONE semantics decision rather than three guesses a tick apart.** 15 ported cases (`log` 4, `log2` 4, `log10` 7) all passed on format retractions alone — every failure was the shortest-round-trip rendering, no wrong answers. ⚠ **`log10.md` is the format ruling's worst victim: four of its values are the bootstrap's CARRY-LOSS defect itself, not re-renderings** — `log10(100.0)` is `1.9999999999999996`, which a *correct* six-decimal printer renders `2.0`, so `1.999999` was never a faithful rendering of that double under any formatter. ⭐ **THE RULING: IEEE 754 for non-positive input.** All three returned the sentinel `0.0`, and the harm was concrete — `log(1.0)` returns `0.0` too, so a caller could not tell a valid answer from a rejected argument. Now `log(±0.0)` is `-inf` and `log(x<0)` is `nan`. ⚠ **The three specs DISAGREED with each other**: `log.md` said 0.0, `log2.md` said 0 "in current implementation" (an honest hedge), and `log10.md` documented NaN and negative infinity — **prose that was FALSE when written and became TRUE with this change**, so that file needed no retraction at all. `log10` also grew **no guard**: it is `log` over a finite non-zero constant and IEEE division carries both specials through, so its answers are inherited rather than restated. ⭐ **`Math.pow` went IEEE in the same tick (second user ruling), and it was a WRONG ANSWER not a sentinel**: `pow(-2.0, 3.0)` returned `0.0` where the answer is `-8.0` — a negative base with an integer exponent is an ordinary computation. Now the full special-case table, verified against C99 F.9.4.4 by review. ⚠⚠ **THE TICK'S OWN RULING CREATED TWO HANGS AND REVIEW CAUGHT BOTH**: making infinities reachable meant `Math.log(+inf)` never terminated (`normalize_down` halves, and `inf/2.0` is `inf`) — one line of Maxon, `0.0 - Math.log10(0.0)`, reaches it; and `Math.exp` had the same shape reachable from two FINITE arguments via `pow(10.0, 1e308)`. Both guarded, both sabotage-verified as **hangs, not wrong answers**. ⚠ A third guard — `exp(-inf)` — was load-bearing and **pinned by nothing**, so deleting it hung while the suite stayed green; now pinned. Also fixed: the bootstrap printed `--9223372036854775808.775808` for `-inf` and a plausible-looking negative for NaN (both targets, hand-written assembly, one copy each) — a garbling oracle is worse than a broken one. Three reviews, every finding a claim that read as true: a fixed point described as a tuning knob, an arm64 comment describing x64's symptom, my own prose asserting a wrong answer where the failure is a hang, and a justification for two duplicated tables citing an obstacle that did not exist. Suite 3615/0 after this tick. |
| exp | 2026-08-02 | PORTED | 5/5 | 5/5, **byte-identical to `/specs` — zero retractions, no compiler change**, the first spec in eight ticks that needed nothing. `exp.md` asserts exit codes through `trunc()` and carries no `stdout` block, so the shortest-round-trip ruling does not touch it. ⭐ **THE VALUE OF THIS TICK IS WHAT THE PORT PROVED, NOT WHAT IT ADDED: it CLOSED an open `PLAN.md` rung.** The filed float-compare/bool miscompile — a `bool` from a float comparison, held across a loop, reading back false — was fixed three ticks ago by the `sin` port (`c5e5ac179`, compare/branch fusion proved per BLOCK but recorded per VALUE ⇒ a lossy key). Both `PLAN.md` and `StdlibLoader.maxon`'s Math-whitelist note still declared it LIVE, the latter saying the entry "SHIPS A MEASURED WRONG ANSWER". Verified closed by running the rung's own probe: **11 on both compilers** where shv2 gave 22, and `Math.exp(-2.0)` now agrees across compilers. ⚠ **A second rung was also stale** — E3012 was marked "DOES NOT EXIST IN shv2 AT ALL"; it exists for parameters since `ff9c825fa`. Marked HALF closed, with the four previously-shelved cases given individual dispositions rather than a blanket one. ⚠⚠ **REVIEW CAUGHT ME COMMITTING THE EXACT SIN THE ENTRY WARNS ABOUT.** The original rung said `sin`/`cos`/`log`/`log2`/`atan` "want checking with it"; my closure upgraded that guess to "carried the same shape and were never separately wrong" — **without doing the check** — two sentences after praising the entry for catching that failure mode. Checked on review: `is_negative` occurs at three lines in `Math.maxon`, **all inside `exp`**. `atan` folds by recursion, `log`/`log2` hold no boolean, `sin`/`cos` never compare against `0.0`. They were never wrong because they never had the shape. Review also corrected two more of my claims: the primary pin was built **by design** in the fix's own commit (`float-compare-branch/one-compare-two-branches-across-a-loop`, integer-only and stdlib-independent) rather than arriving by the whitelist — `exp.md`'s `negative` is a secondary end-to-end confirmation that would stop pinning silently if `Math.exp` were rewritten; and a shelf reason I inherited blamed `for … in` loops, which **compile fine today** (the real blocker is `List with T`, E2015). Also fixed two adjacent stale comments: `CmpPredMap`'s header still said "Keyed by ValueId" directly above a `Map with (BlockRef, …)` — one fact written twice, the two copies contradicting, in the very code whose lossy key was the defect — and an `interface-conformance` shelf reason saying E3012 "is not implemented in shv2". Suite 3620/0 after this tick. |
| lexer-parser-robustness | 2026-08-02 | PORTED | 70/70 | 70 adversarial lex/parse cases. Needed `\xNN` in REGULAR string and character literals — shv2 had it for byte strings only (`b"\xc0"`), so the machinery existed and had simply never been wired to the other two doors; `\u`'s path was the template and the two now share one marker-keyed `DigitEscape`. Matched the ORACLE on the semantics `specs/hex-escape.md` leaves open: in a regular literal `\xNN` is a CODEPOINT, UTF-8 encoded (`"\xC0"` ⇒ `C3 80`), identical to `\u00NN` — only in `b"…"` is it a raw byte. 31 captured stderr lines regenerated, which **this spec's own Documentation ratifies in advance** ("if the compiler is changed to emit a different (still-correct) diagnostic, the captured text will need to be regenerated"): 22 EOF-truncation cases where shv2 names the token it actually saw instead of the bootstrap's "unexpected end of input", and 4 where **`/specs` shows a BACKSLASH AS `/`** because the C# harness blanket-replaces `\`→`/` in stderr (`maxon-sharp/Testing/TestRunner.cs:1635`) — `specs/hex-escape.md:147` carries the same E1004 message with a real backslash, which is the corroboration that made it a capture artifact rather than a claim. ⭐ **Three renderings were left ALONE because ported specs already pin them, and changing one would have been a §HALT, not a fix**: `'(empty)'` (`expected-expression-error.md:60`), `'\{'` (`self-type-member-expression.md:791`), and E2015 `'int' has no method named …` (`character-type.md:472`) where `/specs` says E4006 — shv2 claims E4006 too, but only at IR lowering, which this program never reaches. One `stdout` block added: `interpolation-nesting-deep` pins exit 0 and no stdout, and **shv2's harness reads an unmentioned stream as an assertion of silence** (`checkStreamSilent`) where the reference harness does not check it at all — the bootstrap prints `inner`, so the block records what both compilers agree on rather than what shv2 happens to do. Suite 3690/0 after this tick. |
| if-try | 2026-08-02 | PORTED | 24/24 | 24/24, **byte-identical to `/specs` — zero retractions**, and a §7 tick: the spec is a whole language feature shv2 did not have, and closing it took **three mechanisms**. (1) **`if try` / `if let\|var x = try …` / `else (e)` / `else if let`** — the catching form. It reuses `parseTry`'s three steps but deliberately NOT `desugarTry`, because that routine's flag-split, error block and merge *are* what an `if` already is; ⭐ the departure from v1 is that the catching `try` is recognized **lexically at the one position the language grants it**, not by v1's `inIfCondition` parser mode — a mode that can fire anywhere is answerable for everywhere it can reach, and `if a and try f()` would silently have acquired a meaning no spec grants. (2) **`Character.asciiValue()`**, which had been left unbuilt *because* the `if try` form did not exist — its own refusal message said so. (3) **`Map`**, needed only by the two E3087 lint cases. ⚠ **The filed blocker was 1 of 6**: tuples-of-type-parameters, expression field defaults, `KeyArray{}` on a builtin, `__ManagedMemory with Key` fields, and the transitive-consume fixpoint — and **the shape that DOES compile is worse than the ones that don't**, a generic holding an `Array with T` over managed `T` compiling clean, printing right, then ACCESS-VIOLATING at teardown where the oracle exits 0. That measurement killed the whitelist route: `Map` is a compiler-owned builtin generic, the two-parameter twin of `Set`, and `SetRuntime` went 839→~200 lines as both now shape-parameterize one shared `HashTableRuntime`. The **E3087** lint runs in the PARSER, where the receiver tokens are still in hand, rather than rebuilding the access path from SSA as both references must. ⭐ **Three cases in OTHER files were owed and are now enabled** (`string-type/find-last-not-found` plus `character-type`'s two `ascii-value-*`), and **two shv2-native specs had their EXAMPLES falsified rather than their mechanisms**: `builtin-member-rosters` pins a roster that is *derived* from the dispatch, and `generic-types` spelled its undeclared generic base `Map` on the stated ground that it was "a real stdlib generic that shv2 has not built" — a premise this tick made false. Both guards verified still firing against a genuinely undeclared base before re-spelling. Review found 11 items; 10 applied, and **finding 9 was REFUTED BY MEASUREMENT** — the proposed panic on a "closed" tag set was reachable from user code and destroyed a correct E2062 on its way. Suite 3721/0 after this tick. |
| closure-self | 2026-08-02 | PORTED | 4/4 | 4/4, **byte-identical to `/specs` — zero retractions**. ⭐ **THE REFUSAL NAMED A WHOLE UNBUILT RUNG AND THE RUNG WAS BUILT.** shv2 refused with *"a closure that captures `self` (P1.5-A2b: capturing closures + env block)"*, which reads as though env blocks did not exist — **measured before planning, they do**: a closure capturing an ordinary local compiles, runs and returns the right value. The real gap was one door wide: the capture machinery keyed on `tok.value`, and the receiver is bound under `SelfParamName` (`__self`), a name **no token spells**, so it was structurally invisible. Re-keyed on `baseBindingName` — the routine whose own header already claimed to be "the ONLY place the `self` token and the receiver parameter's name are connected" — and `self` became an ordinary capture at all three doors. By-value capture (shv2's documented departure from both references' by-reference model) is observably correct for `self` because copying the heap POINTER still observes current instance state: probed with a mutation between capture and call, shv2 and the bootstrap both exit 41. ⚠ **One diagnostic re-coded on my ruling**: `self` outside an instance method was **E2015 `Unsupported`**, and "unsupported" means *not built yet* for something **permanently illegal** — the wrong category, not just the wrong sentence. Now **E2001** with `/specs`' wording; no ported spec pinned the old text (grepped: zero live pins) and no registry change was needed. ⭐⭐ **An shv2-AUTHORED case had encoded the refusal as the answer**: `closure-capture.bare-self-field-refused` pinned this exact E2015 on a program whose own prose says *"this program returned 25 … where the answer is 4"*. It is not a `/specs` case — one of 8 shv2-only additions to that ported file — and it was scaffolding that named its own removal rung. shv2 now returns 4, so it is **converted, not deleted** (renamed `…-captured`, asserting exit 4): it still fails if the env slot ever hands the receiver's box back, and its `closure-self` sibling cannot replace it because that spec must spell fields through a ranged alias while this one uses the bare `int` whose lowering used to panic. Review found 2, both real and both fixed: a fourth un-shared copy of `baseNamesAValueInScope` missing its `Self` guard, and — **a regression this diff introduced** — a blanket `otherwise` in `implicitSelfReceiver` that swallowed the nested-capture refusal and reported `'T.helper' expects 1 argument(s) but 0 were provided`, the receiver counted as the missing argument. Confirmed with a repro, not accepted on the report. Suite 3734/0 after this tick. |
| list-custom-types | 2026-08-03 | PORTED | 5/5 | 5/5, **byte-identical to `/specs` — zero retractions**, and one mechanism: `List with <T>` as a **fourth compiler-owned generic container** beside `Array`/`Set`/`Map`. New `Runtime/ListRuntime.maxon` — a 32-byte chain header (`head`/`tail`/`count`/`element_drop`) over 24-byte nodes, 13 `StdOp` graphs, behind a `usesList` bit — plus the `for … in` cursor form and `List from […]`. ⚠ **THE FILED BLOCKER WAS AGAIN 1 OF 6** (E3086 on `create()`'s `Self{}`; then the absent `__ManagedList`/`__ManagedListNode` substrate; then blocker 3 below; then no iteration protocol at all; then no `<UserType> from […]` door; then transitive-consume). Two of those were independently fatal to the stdlib route. ⛔⛔ **BLOCKER 3 IS THE SAME DEFECT THE `Map` TICK MEASURED, AND IT IS NOT `Array`-SPECIFIC**: a generic type holding **any** compiler-owned container over **its own type parameter** prints the right answer and then **SEGFAULTS at teardown** when the element is managed — `Holder with Int` exits 0, `Holder with String` exits 139 — because in a shared generic body the element's managed-ness is erased and no element destructor is stamped. `List`'s only field is exactly that shape (`chain as __ManagedList with Element`), so the `Map` precedent's deciding measurement **did** reach here; I had briefed that it might not. Now measured twice, in two ticks, from two directions. ⇒ `List` is synthesized, `stdlib/List.maxon` stays unwhitelisted. Departures from v1, each with a reason: **nodes are NOT refcounted** (shv2 exposes no node type, the cursor is compiler-owned and the iteration lock forbids mutating the source, so there is never a second owner); compile-time managed-ness rather than v1's runtime `elem_managed` bit (the third user of the stamped-destructor mechanism, not a fourth mechanism); a loop-header node cursor rather than a heap iterator object. Oracle-differential across trivial/String/struct-with-String elements, drain-and-refill, empty iteration, all three out-of-bounds throws and a 200-iteration leak loop: **byte-identical stdout and exit 0 on both compilers**. Review found 5; all applied, and finding 1's fix was **sabotage-verified** — adding a name to the throwing predicate now dies at install naming it, where the old guard was silent, and the reverted binary is byte-identical. ⚠ Worth knowing: **`Map` has no spec in `specs-shv2` at all**, so its create parse, column gate and refusal sentence are pinned by nothing; `specs/map.md` is whitelist 125 and will cover it at its own tick. Suite 3750/0 after this tick. |
| vector | 2026-08-03 | PORTED | 26/26 | 26/26, **byte-identical to `/specs` — zero retractions**. `Vector with <N> <T>` is the **fifth compiler-owned container** and the first whose type identity carries a **VALUE**: `Vector with 3 Int` and `Vector with 4 Int` are different types. ⚠ **The filed blocker was 1 of 8 — the third tick running I have to write that sentence.** ⭐ The one that decided the route was not the one I predicted: **`stdlib/Vector.maxon`'s only field is UNSPELLABLE in shv2** — it is `__ManagedMemory with Element`, and shv2's `__ManagedMemory` is an arity-ZERO alias for `Array with Byte`, so the module cannot be whitelisted at all (E2055). That settled it before the generic-container teardown segfault was even reached — which does also reach `Vector`'s shape, for a third independent confirmation. Also invisible until earlier ones cleared: **float container elements did not work in three separate layers** (E2062 at the type argument, a `containerSlotValueType` PANIC on the read, and an `emitRegRegMove` xmm→gpr PANIC in the backend), float and bool array literals were E2015, and `__arr_mem_create` is throwing so the obvious one-call `create()` made every case E3057. **Departure from v1 on how the size is carried**: v1 prepends `MaxonType.constInt(size)`; shv2 puts it in a second column on the instance registry, part of the intern KEY, because shv2 reads types through a dense `(ValueTypeTag, TypeNameId)` column and a `constInt` arm would owe four routers an answer about "the type that is the number 3" — a number is not a type, it is a second coordinate. A Vector **IS an `Array` record** (same 48 bytes, same `__arr_*`, same decref); `Vector from […]` emits **no runtime op at all** — the literal already built the record, so it is a `retypeValue`. ⛔⛔ **REVIEW CAUGHT A SEGFAULT THIS TICK INTRODUCED, and I measured it before acting**: the element gate decided "may this live inline" from tag predicates, and **`tagIsIntegral(named)` is `true`** while `named` also covers boxed unions, generic-instance aliases and struct names — so `Vector with 2 IntArray` compiled, answered `count() == 2` while `get(0)` took the empty-slot branch, and **`for … in` over it exited 139**. `emitVectorCreateOp` reaches `__arr_resize` without passing `requireArrayResizeHasZeroElement`, whose own header had predicted this in these words: *"that read becomes a null deref in a function whose own text still looks correct."* Cured by asking the ONE door `containerElementIsManaged` — and the invariant is now written at BOTH doors, since neither may be relaxed alone. ⚠ The first attempt at the refusal **committed finding 1's own error while explaining it** (`'IntArray' (int)` — `typeTagName(named)` is the word `int`). Six further findings applied, including a float-element predicate spelled twice on the two sides its own comment says must never disagree. ⭐ Container-arm duplication is finally trending DOWN: a sixth container would touch **9 sites, against `List`'s 11**. One derived enumeration updated in the shv2-native `character-ownership.md` — admitting float and bool made its literal-element list true only if it changed; the case's point (a `Character` is refused) is untouched. Suite 3832/0 after this tick. |
| primitive-stringable | 2026-08-03 | PORTED | 5/5 | 5/5, **byte-identical to `/specs` — zero retractions**, and the smallest tick in a while because **the conversion already existed and the spec said so**. Its Notes mandate *"uses the same conversion as string interpolation"*, and interpolation already printed exactly the expected strings — measured before any work: `42\|3.14\|true\|7`, with `3.14` and not `3.140000`. So the gap was purely the **dispatch roster**: `dispatchBuiltinConformanceMethod` resolved a primitive receiver's method name only through `builtinConformerMethod`, which walks `Hashable`/`Equatable`/`Comparable`, and `Stringable` is not in that walk. ⭐ **No second formatter was written, deliberately** — `emitValueToString` builds the three interpolation part arrays and calls the existing `materializeInterpExpr` + `emitStringInterp`, i.e. `"{x}"` with zero literal fragments. That is also how **both** references do it (`extension int implements Stringable` whose body is literally `return "{self}"`), so the property is reproduced rather than re-derived; the departure is that shv2 has no `extension` mechanism, so the one-expression body is emitted at the dispatch site — same conversion, one frame fewer. ⚠ This mattered: a previous tick found **two** float formatters where converging them exposed three bugs in code that had never run. `clone` was NOT built — the refusal message named it, but no case pins it. **`Stringable` is deliberately NOT added to `isIntrinsicBuiltinConformance`**, which is the witness-table answer: every conformance it admits owes a real `<type>.<method>` symbol for a slot reloc, and a lowering has none, so admitting it would make `where T is Stringable` typecheck against a slot nothing can fill. ⚠ Worth recording that **the two references disagree there anyway** — the bootstrap honours `where T is Stringable` for `T = int`, v1 does not — so "match the reference" was not well-defined for the generic half, and nothing pins it. One derived expectation moved and it was mine to mint: `lexer-parser-robustness.md:540`'s roster sentence, which I ported two ticks ago. It now names only what IS supplied (`hash`, `equals`, `compare`, `toString`); the hand-written "which are not built" tail is dropped rather than corrected, because **that tail is the half that rotted** and this file's own precedent is to derive the roster and stop asserting absence. Suite 3837/0 after this tick. |
| challenge-nested-structs | 2026-08-03 | PORTED | 4/4 | 4/4, **byte-identical to `/specs` — zero retractions, and NO COMPILER CHANGE**: green on the very first run. Nested struct fields, a nested struct returned from a function, three levels of nesting, and a struct with several nested fields all already work in shv2. ⭐ **The value of a tick like this is what it PROVES, not what it adds** — struct layout, by-value field copies through nesting, and returning an aggregate whose fields are themselves aggregates are now pinned by a `/specs` file rather than only by shv2's own tests, and they were pinned at zero cost. The §2 counts were run anyway and are the point of running them: 4 markers, 4 ran, 0 disabled, no name spelled both ways — a spec that passes on the first run is exactly the one where a silently-dropped case would go unnoticed. No review was run because there is no compiler diff to review; the §6 bounds questions (byte-identical to `/specs`, nothing outside this spec touched, no expectation weakened) are answered above and all four minted goldens are committed. Suite 3841/0 after this tick. |
| challenge-struct-lifetime | 2026-08-03 | PORTED | 3/3 | 3/3, **byte-identical to `/specs` — zero retractions, no compiler change**, green on the first run: a struct returned with computed fields, a struct passed to a function and returned, and several struct returns in one program. Second consecutive `challenge-*` file to need nothing — that family is `/specs`' stress cases for struct value semantics, and shv2's static-ownership-from-commit-1 design apparently pays for them all at once. **The §2 counts were run precisely because it passed immediately** (3 markers, 3 ran, 0 disabled, no name spelled both ways) — a first-run green is the one tick where a silently-dropped case would never be noticed. No reviewer pass (a code-quality review over an empty compiler diff has no surface) and no build-cost row (that log measures what a compiler change costs, and there was none); the §6 bounds questions are mine and are answered — byte-identical, nothing outside the spec touched, no expectation weakened, all three minted goldens committed. Suite 3844/0 after this tick. |
| bool-bit-packing | 2026-08-03 | PORTED | 20/20 | 20/20, **byte-identical to `/specs` — zero retractions, no compiler change**, green on the first run. ⛔⛔ **AND THAT IS THE FINDING: shv2 DOES NOT BIT-PACK, and this spec passes anyway.** Measured, not inferred — `SignatureIndex.maxon:902` says *"`boolean` is ONE byte (`i1`, whose `storageBytes` is 1)"*, and the only mention of packing in all of shv2 is `LayoutDescriptor.maxon:21`'s reserved-for-later flag `bit1=bitPacked — B2/P1.7`. So an `Array with bool` costs **8× the memory this file's Documentation describes**, and all 20 cases pass regardless, because every one of them asserts observable behaviour — get/set/count/iterate/slice/remove/COW results — and **none observes the representation**. The Documentation states the mechanism in detail (`elementSize = -1`, bit `i * width`, widths dividing 8, COW at bit granularity) and the cases pin none of it. ⭐ **The ladder already owns the rung, and it NAMES THIS SPEC AS ITS ACCEPTANCE CORPUS**: `PLAN.md:2549` — *"⬜ SUB-BYTE BIT-PACKING (`bool` + narrow ranged ints) — the boundary P1.7-finish Slice 2 deliberately did NOT cross … That needs a negative `element_size` encoding and has its own unported corpus: `specs/bool-bit-packing.md` (20 cases) and `specs/ranged-int-bit-packing.md` (11), neither in `specs-shv2`."* ⚠ **That expectation is now measurably wrong: the corpus is ported, it is GREEN, and it did not need the rung.** Whoever takes P1.7-finish/B2 cannot use these 20 cases as their gate — they discriminate nothing about packing, and a rung that lands with them green has proved only that it did not break the API. It needs a case that observes the representation. Not filed on `PLAN.md` here: `72fd69067` ruled the ladder is not `/spec-port`'s to edit, and the row exists already — what is new is that its stated acceptance corpus does not test its subject. Reported in the commit for whoever runs `/rung`. Suite 3864/0 after this tick. |
| ranged-int-bit-packing | 2026-08-03 | PORTED | 11/11 | 11/11, **byte-identical to `/specs` — zero retractions**, and it is the rung `PLAN.md:2549` filed as ⬜: **SUB-BYTE BIT-PACKING**. `bool` and any `int(0 to N≤15)` now store 1/2/4 bits per element, with `element_size@24` carrying the packed stride as a **negative bit width**. ⭐ **This spec is the discriminating gate its sibling was not.** One tick ago `bool-bit-packing` went 20/20 green while shv2 did not pack at all, because none of its cases observes the representation; **every** case here reads `arr.managed.elementSize()` and three pin the exact packed `.rdata` bytes (`[0,1,2,3]` at 2 bits ⇒ the single byte `228`), which is what proves LSB-first order and `ceil(count*bits/8)` sizing. The PLAN row names both files as the rung's corpus and expected both RED; only this one ever could have been. ⚠ **The filed blocker was 1 of 8** (fourth tick running), and blocker 2 was **unavoidable, not incidental**: exposing `elementSize()` falsifies a committed case whose stated purpose was *"THE CONTROL FOR THE OTHER HALF OF THE SENTENCE"* — the roster's hand-written *"are not built"* clause. That clause was quoted verbatim by **21 expectations and 66 fragments**, and building one of its entries rotted all of it at once; it is now derived-supplied-only, which is the third instance of that pattern in six ticks and by far the largest. **43 expectations moved and every one was shv2-authored** — verified independently: 5 of the 7 files are shv2-native, and the 35 failing cases in the two `/specs`-ported files exist in `/specs` **zero** times. 42 re-minted; 1 converted rather than deleted (`error.buffer-has-no-element-size` → `buffer-reports-its-element-size`, keeping the door). Other blockers, none visible until the one before it cleared: `arrayElementSize` returned an **unsigned** `ByteOffset` so it could not express a packed stamp; the whole array runtime strided in bytes at ~20 sites; the O(1) zero-copy COW slice **cannot** represent a packed view (no byte address for bit 1, no bit-offset field) and degrades to an O(n) bit copy for packed elements only; and shv2 had **no constant-literal `.rdata` path at all** — `ModuleInit` had declined one as "the optimizer's call", but the spec pins the bytes, so for this door the language decides. ⭐ **A regression was shipped and caught by review**: alias-aware literal inference read `ValueDeclaredAliasMap` from a second site, making `[v as N, 3 as N]` a **false rejection of a legal program** with a false message; the fix — a fourth `applyCast` re-tag predicate — deleted more than it added and restored that map to the single reader its own safety argument assumes. Review then found seven more, the sharpest a byte-only `count · element_size` in `ManagedFileRuntime` that was safe **only** because a predicate two files away excludes packed strides, with nothing at the site saying so; `emitElementByteLen`'s claim widened from "in this file" to "ANYWHERE", which made it checkable, and it was checked. One new refusal landed pinned rather than bare: a duplicated `true` arm is now E2027 instead of silent dead code, and its message names `'true'` where it first named the folded `'1'`. Suite 3884/0 after this tick. |
| array-return-element-from-loop | 2026-08-04 | PORTED | 1/1 | 1/1, **byte-identical to `/specs` — zero retractions**, and **no compiler change**. The spec pins a refcount trap in for-in lowering: the body has TWO names on one managed allocation — the hidden `__forin_result_N` holding `iter.current()` and the user's loop variable — so `return e` must skip BOTH in scope cleanup, or the caller frees memory already freed. shv2 does not have that defect: its ownership is static from commit 1 and the transfer out of the body was already correct, so the tick is COVERAGE rather than repair — the case is now pinned against every future change to for-in lowering, which it was not before. One golden minted (host lane) and committed per §5. ⚠ §6's code-quality review is deliberately ABSENT and that is the honest report, not an omission: the working diff is the spec copy plus its minted golden, **zero lines of compiler source**, so a quality pass had nothing to read. Suite 4088/0 after this tick. |
| repro-array-union-field-reassign | 2026-08-04 | PORTED | 4/4 | 4/4, **byte-identical to `/specs` — zero retractions, no compiler change**, green on the first run. Four `maxoncstderr` cases pinning E3006 clause (b) — a local shadowing a `self` field is a compile error — across all four declaration doors: match-pattern binding, function parameter, `let`, and for-in loop variable. shv2 already refuses every one with the same code, message and positions; `docs/error-codes.txt` documents that clause as shv2's own (`duplicateDefinition`), so **the rule was built and pinned by nothing in `specs-shv2` until now**. The defect it was written from is not a style rule: `IsSelfField` keying on NAME ALONE silently rerouted a local's reads and writes to `self.field` through the heap pointer, so a `MaxonType` local clobbered a `MaxonTypeArray` field and the next `.count()` dereferenced the union's tag bytes as a pointer. ⚠ The filename says `repro-array-union-field-reassign` while the frontmatter `feature:` says `match-binding-vs-self-field-name-collision` — inherited from `/specs` and deliberately left alone, since the copy is the claim. §2's counts were run and are the point of running them on a first-run green: 4 markers, 4 ran, 0 disabled, no name spelled both ways. ⭐ **First tick under the FAST PATH** (user ruling, this date): a spec that passes immediately lands immediately — no full suite, no reviewer, no finish script, because a diff of one spec file plus its goldens has no compiler source to review and cannot regress another spec. |
| interface-extensions | 2026-08-04 | PORTED | 11/11 | needed FOUR mechanisms, not one: `extension <Interface>` declarations with per-conformer monomorphization; `Array.map`/`Set.map`; `[k: v]` Map literals with Map iteration and `Map.map`; and a container-element type hint for untyped closure parameters. shv2 monomorphizes an extension by RE-PARSING the body tokens once per conformer rather than cloning IR (v1) or MLIR (bootstrap) — the substitution-dependent closure miscompile both references need `SpecializeClosureName` to cure is unrepresentable here. `map` joins the compiler-owned container surface rather than routing through `stdlib`'s `extension Iterable`, which shv2 admits neither module for. Also re-enabled the six `collection.md` map cases that `Array.map` unblocked (that file is byte-identical to /specs again), and re-pinned 17 derived roster expectations across two files. Review found a real blocker — an untyped multi-param `map` transform miscompiled to a plausible wrong answer — fixed inference-keyed, matching the oracle. Suite 4129/0 after this tick. |
| conditional-extensions | 2026-08-04 | PORTED | 6/6 | SIX mechanisms: `where` clauses on `extension` declarations; `extension <Type>` targeting a type rather than an interface; the E4006 "conditional extension withheld" diagnostic naming which constraint failed and which type failed it; the USER CURSOR PROTOCOL — a fifth `for … in` form over any type declaring `current()` + `advance() throws IterationError`; `IterationError` as a compiler-owned enum; and `Array.contains` as a two-overload conditional extension (membership + subsequence) with real float-ABI work. An interface extension's `where` is a compile-time FILTER; a type extension's becomes WITNESS CONSTRAINTS checked at the call site, because shv2 shares one body per generic type. The cursor protocol is STRUCTURAL, not a conformance — measured against the oracle, which accepts a bare type with the two methods and no `implements` clause, so requiring one would have been a false reject. Also enabled 3 cases in two other specs that the coercion fix unblocked (`self-field-method-chain.md` is byte-identical to /specs again), re-pinned 16 derived roster expectations, and corrected a FALSE claim that had propagated into the porting process itself. Suite 4162/0 after this tick. |
| primitive-cloneable | 2026-08-04 | PORTED | 4/4 | `clone()` on `int`/`float`/`bool` built as a LOWERING beside `toString`'s, not as a conformance: the reference's whole body is `return self` (`stdlib/PrimitiveExtensions.maxon`), so there is no symbol to install and `Cloneable` stays out of `isIntrinsicBuiltinConformance` for that predicate's own stated reason. `return receiver` was WRONG for the byte case — a result sharing the receiver's ValueId shares its declared ranged alias, and `x.clone() as Integer` was `E3010 unneeded cast`; the clone result denotes no alias (measured against the oracle), so an alias-bearing receiver mints one `v - 0` (`- 0`, not `+ 0`: `-0.0 + 0.0` is `+0.0`) and every other receiver emits nothing. Retracted one shv2-authored `maxoncstderr` in `lexer-parser-robustness.md`: its roster is DERIVED and grew a `clone`. Suite 4166/0 after this tick. |
| type-checking | 2026-08-04 | PORTED | 12/12 | A USER RULING, and the tick is the ruling rather than the spec: `/specs`'s diagnostic wording wins, and **a type is named by the `typealias` the AUTHOR WROTE** — `IntArray` not the `Array_Int` mint, `Integer` not the erased `int` — **namespace-qualified when contested, or by declaring FILE where the namespace is empty** (two root files share it, and `'x.Bytes' vs 'x.Bytes'` is the contentless refusal the ruling forbids). The mint stays the IDENTITY and the `.rdata` label, printed only where no source line names the instance. ⚠ ONE SHAPE, SEVEN DOORS: three passes each found the next by tripping over a red case, the fourth swept the rest and left the inventory. Also: a container mutator's element argument moved off the ASSIGNMENT sentence, which had been calling the method `push` a "variable"; `print` lost its bespoke sentence; a typealias generic-argument cycle became E2012 with a rendered path. Review found a LIVE compiler panic (an inner generic alias rebuilt through `create` took the defaults for seven fields it already carried) and two doors still printing the tag word. Codegen NEUTRAL — measured against a control build of `origin/main`: the same 42 fragments drift on both. Suite 4187/0 after this tick. |
| ownership | 2026-08-04 | PORTED | 38/38 | A USER RULING on the ownership model, and five mechanisms behind it. **`let b = a` from an IMMUTABLE `a` is a second immutable reference, not a move** — both stay valid, ONE drop at the owner's scope exit, no refcount traffic; a mutable source still moves; **`var b = a` from an immutable reference-typed `a` is now E3078** (registered for csharp+selfhosted, never claimed by shv2 — the refusal simply did not exist). shv2 had generalised *"assignment moves"* into *"binding moves"* and never built the refusal that makes aliasing safe: **two bugs that cancelled**, since the wrong move poisoned the source and made the missing refusal unreachable. Also built: `@heap` (shape-validated — every shv2 struct is already a heap box), **E4014** recursive-type cycles with a rendered path, `__ManagedList`/`__ManagedListNode` exposed (the chain already existed under `List`), top-level `var` container globals, and a nested `function` typealias in a `type` body. 13 `disabled-test:` markers re-enabled in `ownership-edge-cases.md` — cases owed by this tick's mechanisms. ⛔ Review caught a blocker no case could reach: the destructuring hidden temp was blamed BY NAME (`'__destructure_3_5'`); chasing it found `var a = (q).inner` and `var a = t.0` compiling through an immutable binding where the oracle refuses both. Suite 4268/0 after this tick. |
| map | 2026-08-05 | PORTED | 34/34 | `Map.insert`/`Map.remove` were a SURFACE gap, not a runtime one — `HashTableRuntime` already had the duplicate-key branch and the tombstone-reusing probe, so `buildHtInsert` gained an `HtDuplicatePolicy` (which decides one branch AND the shape of all four exits, because a `throws` stamp changes every exit) and `buildSetRemove` MOVED in as the shared `buildHtRemove`. ⚠ `SetRuntime`'s header had argued remove could not be shared because a map's "would have to hand the VALUE back"; `/specs/map.md` declares `remove(key) returns bool` — the set's signature exactly, so the premise was never read off the language and the second copy it justified is gone. The map's VALUE column had been held to the KEY's gate (a 3-name whitelist that exists because a key is hashed through witnesses); it is now the one-word rule `List` already used. Also: unparenthesized `Map with K, V`, `for (k, v) in m` destructuring (which unblocked 4 shelved cases in `tuples.md`/`block-scoping.md`), and two pre-existing wrong answers fixed — an enum silently stored into an int-valued map, and `Map with (Integer, Integer)` refusing its own literal. ⛔ Review caught the tick's own new hole: a `bool` value column admitted at the gate and refused at every write with "this `Map`'s value is a bool — got a 'bool' value". Suite 4319/0 after this tick. |
| map-struct-bytearray | 2026-08-05 | PORTED | 3/3 | Three cases, and the door they needed was FIVE doors: a top-level `var` whose initializer is a USER TYPE's static factory call (`var db = Database.create(EntryMap.create(), …)`). The constant evaluator read `Database.` as a ranged-type bound, so the whole shape died as `E2010 Expected 'min' or 'max'` — a diagnostic describing a different program. Also needed: `__module_init` building more than `__arr_create`, a field chain rooted at a global, E3087 not firing on storage every function can reach, and `calleeVisibleFrom` not comparing `__module_init`'s empty path. ⭐ The `Array`-only limit on global containers is gone — Set/Map/List/Vector all work. ⛔ The review then found the tick had opened three holes, and I reproduced two before acting: a user generic at file scope PANICKED the compiler (was a clean refusal before), a borrowed factory argument LEAKED (exit 101), and an `export type`'s file-private `static` was callable from another file's global. All three closed — the visibility one by adding the per-function tier column the sweep had never carried, not by leaving the check switched off. Suite 4334/0 after this tick. |
| map-try-otherwise-block | 2026-08-05 | PORTED | 4/4 | Block-form `try 'l' … end 'l' otherwise (e) 'h' … end 'h'` BUILT — the body's calls are BARE and any throw routes to one shared handler. Routing is at EMIT time from the two doors that append a `MaxonOp.call`, because shv2's parser mints SSA and block args as it goes: v1's post-body rescan cannot recover what each carried binding held at a call it has walked past, and the bootstrap's slot machine has no slot to read. ⭐ E3057 needed NO change — it is keyed on the op KIND, so a routed call is exempt because it became a `tryCall`, and every call the router declines stays a plain `call` and is still reported. That makes a missed routing door a LOUD E3057 rather than a dropped flag. ⛔ Review then found two blockers the 4 green cases structurally cannot reach, and I reproduced both: a managed-returning routed call SEGFAULTED (the landing pad decreffed a register the error edge never wrote — the exact hazard `desugarTry` documents and guards, reproduced without the guard), and a closure in the body PANICKED the compiler (the reset half of save-and-restore was copied, the restore half was not, in the very line citing `loops` as its precedent). A third finding — move state not reconciled at the merge — measured exit 101 against the oracle's 0 in TWO shapes, and was pre-existing in `desugarTry` too; both forms fixed. Suite 4350/0 after this tick. |
| array-hashable | 2026-08-05 | PORTED | 11/11 | `Array` becomes the SIXTH intrinsic builtin conformance (beside int/float/bool/String/Character) — but unlike those five it is a FAMILY, and the design turns on the conformance being byte-level over the backing buffer: both references define it that way and `/specs/array-hashable.md` says so in words ("the raw bytes of the array's managed memory using djb2"). ⇒ `where Element is Hashable and Equatable` is purely an AVAILABILITY clause — it decides whether the method exists, never what it computes — so there is no witness threading, no per-instance impl, and every conforming `Array with E` collapses to ONE conformer name `Array` served by one pair of `.text` symbols. The collapse is CONDITIONAL (a non-conforming element keeps its mangled name, which nothing claims), which is how a conditional conformance survives a name-keyed model. ⛔ Three of the eleven cases COMPILED AND ANSWERED WRONG before this: `[1,2,3] == [1,2,3]` fell through to a raw compare of two record ADDRESSES and was false. ⛔ Review then found the availability predicate written TWICE, in two files, deciding DIFFERENT consequences (one gates the method door, one gates the conformer-name collapse that stamps the witness tables) — agreeing today, one edit from a silent wrong answer either way; now one walk returning the `ExtensionConstraintCheck` that already existed. Suite 4377/0 after this tick. |
| module-level-struct-var | 2026-08-09 | PORTED | 3/3 | 2 of 3 passed on the first run; the third needed nested factory calls in a top-level initializer (`var state = Outer.create(Inner.create(0))`), which was a deliberate E2015 refusal. The folded-value union could not carry a call arm — `call → args → argument → call` names itself and Maxon forbids a recursive type (E4014) — so the call tree is now FLATTENED INTO AN ARENA and an argument keeps only an integer index. Mirrored entry-for-entry at the fold tier and the index tier so one id names the same call on both sides; the emitter walks it depth-first left-to-right, exactly as a body's call site does. Only the TYPE had to be flattened — the recursion that remains is the walk's. Suite 4868/0 after this tick. |
| regalloc | 2026-08-10 | PORTED | 6/6 | Fast path (§7b): green on the first filtered run, byte-identical to `/specs`, zero compiler source changed — the six register-allocator gating programs (constant rematerialization, the X64 reg-mem dialect, the spilled-source fold, eviction fixup, the pressure-aware scheduler, and call-arg parallel-copy sequentialization) all already produce their pinned exit codes under shv2. ⚠ The start script flagged a COUNT MISMATCH — 6 `<!-- test:` markers against 8 ran — and it was a FILTER COLLISION, not a dropped or surplus case: shv2's `--filter` is a substring of the `<spec>/<test>` label, so `regalloc/` also matches the already-ported `generic-hash-table-regalloc/`. Per-test detail (`spec_test_outcome`) splits it 6 + 2, and this spec's own count is exactly 6/6. **A filter that is a substring of another spec's name inflates the §2 total; read the per-test list before believing a surplus.** No suite figure — the fast path measures none, and a number nobody measured looks exactly like one that was. |
| enum-allcasenames | 2026-08-10 | PORTED | 8/8 | TWO mechanisms, and the second one was owed to a spec that had already landed. **`.allCaseNames`** did not exist at any door: `parseDottedPrimary`'s enum arm routes `Color.<member>` straight to `parseEnumLiteral`, whose only question is *"which case is this?"*, so every non-case member became E3034. The names were already sitting in `EnumLayout.caseNames`; nothing was wired to the door. It now emits exactly what the source literal `["red","green","blue"]` emits, op for op, and the `Array with String` instance is interned on the WHOLE-FILE probe walk rather than the declaration loop — which skips method bodies, so `Color.allCaseNames` inside a `type` method would otherwise have found no instance. **String and character raw-value backing** was a parser refusal in both spellings (an `=` raw value, and a literal standing in the case-name position). ⚠ Its stated reason had ROTTED — it blamed *"String (P1.2)"*, landed long ago — and measurement found nothing missing at all: a character raw value IS a number (its codepoint) so it tags exactly as an int does, and a string raw value only needed somewhere to live that is not the i64 tag. That is the 4th parallel column `caseRawValues`, which exists because a string-backed case's tag is only a declaration index, so E3031 CANNOT be a tag compare there — two string cases sharing a raw value would have been a silent over-acceptance introduced by this tick. ⭐ **FIVE `disabled-test:` cases in the already-ported `enum-full.md` were blocked on exactly this mechanism and are now live** (`implicit-string-backed`, `implicit-char-backed`, `explicit-char-backed`, `error.raw-value-type-mismatch`, `error.mixed-backing-types`), taking that file 37 → 42 and five cases closer to `/specs`; the diff there is 5 marker flips and nothing else. ⛔ Review found six, all confirmed against the source before acting, and one was a LIVE hole this tick had opened: the reserved-name check read the RAW token text while every other reader used the DECODED name, so an escape-encoded reserved case name declared a `__`-prefixed case and compiled — it now reports E2051. Also folded: the rule *"a string backing's tag is not its raw value"* had been written twice, in two files, each as an unguarded `if` falling through to the tag path, against the diff's own promise that struct and function backings arrive later — now one exhaustive `EnumLayout.tagCarriesRawValue()`. And a comment asserting *"both reference compilers decide it in this order"* about `allCaseNames`-vs-case-name was half FALSE: v1 does, the bootstrap asks `GetCase` first (`2-Parser.cs:19051` against the `allCaseNames` arm at `:19099`). The ordering stands — shv2 agrees with v1 and no spec covers the collision — but the claim is now accurate rather than settled-sounding. Suite 5136/0 after this tick. |
| enum-ordinal | 2026-08-10 | PORTED | 10/10 | 7 of 10 passed on the first run; the 3 that did not were `.ordinal` on an enum whose cases declare explicit raw values (int-, float- and char-backed — the last only REACHABLE since char backing landed the day before). Not a missing feature but a missing COMPUTATION: shv2 represents an enum value AS its tag, so where tag == declaration index `.ordinal` was already `<value> + 0` and cost nothing, and where they differ nothing computed the map, so the door REFUSED rather than answering 500 for ordinal 2 — the right call, and what was missing was the mapping. Now a branchless `Σᵢ i × (value == tagᵢ)`, three ops per case, case 0's term elided as identically zero. ⭐ **Two departures from v1, both deliberate.** It expands in the PARSER, not behind a new `MaxonOp` at the lowering tier: the parser is the tier holding `EnumLayout` and the three ops it needs already exist, so a new union variant would ripple through every exhaustive match to buy nothing. And it compares the tag as **i64 for every backing** where v1 uses `cmpF64Eq` for floats — which is how shv2's `match` dispatches, so the accessor and the dispatch cannot disagree about which case a value is, and it is strictly MORE correct: `pos = 0.0` and `neg = -0.0` are two distinct declarable cases that IEEE equality collapses to one, making v1's formula answer ordinal 1 for both. ⚠ **The refusal's stated reason was UNEARNED** — it said the mapping "arrives with the rung that lands the `.rawValue`/`.name` accessors", and neither was touched or needed: those want a tag→PAYLOAD map, this wants a tag→INDEX map. The sentence was inherited, not earned, and did not survive into the replacement. ⛔ **The tick then found a SILENT WRONG ANSWER on its own accessor and fixed it rather than filing it** (CLAUDE.md: pre-existing does not decide disposition). `.ordinal` on a BOXED union added zero to a box pointer: `Container.value(7).ordinal` printed 7077912 against the oracle's 1 — and the payload-FREE `empty` was wrong too (7078078 against 0), because `isBoxed` decides per UNION, not per case, so a bare-tag case of a boxed union is a tag IN A BOX and its value is a pointer as well. No committed golden anywhere in the tree read `.ordinal` on a boxed union, which is exactly how it survived. ⛔ Review then found five, all confirmed against the source first. The substantive one: *"where an enum value's tag lives"* had become TWO decisions — `parseMatch` and `parseEnumOrdinal` already shared the OFFSET (`emitUnionTagLoad`) and each kept its own copy of the RULE, which is the half that drifts — now one `emitEnumTagOf`. And `retypeValue`'s header carried a safety invariant this change falsified ("the value's Std width is unchanged", true of all 14 existing callers, false of an i1→i64 retype). ⭐ Measured rather than argued: the raw-mint alternative REACHES on all three backends today, and was still refused — it means emitting a deliberately false width and relying on every backend to keep disregarding it, which is precisely the shape X5's wasm defect came from (`v * 2` answering 3,705,032,704 for 8,000,000,000, because that field DID select the width). The header states the truth instead. Suite 5147/0 after this tick. |
| match-range-single-value | 2026-08-10 | PORTED | 9/9 | 3 of 9 passed; the other 6 were all *"expected a compile error but compilation succeeded"* — this tick ADDS A REFUSAL, whose blast radius is the whole corpus. A match range covering exactly one value (`5 to 5`, `red upto green`, `'a' to 'a'`) is now E2027. ⭐ **No registry work at all**, which is unusual for a new diagnostic: E2027 is the shared code for *a degenerate match pattern* (`ParserMatchDuplicatePattern`), shv2 already claimed it as `matchDuplicatePattern`, and both references emit this spec's exact message under it. The rule is ONE predicate over a decoded bound pair — the references grew three and four emit sites respectively, which is how they happened to grow and not a design to copy — with each caller projecting into its own ordered domain (values, enum DECLARATION INDICES, char CODEPOINTS, float order keys) and a single `hasAdjacentValues` flag carrying the entire float exclusion, since `upto` can only be single-valued where the domain has an immediate successor. ⛔ **Review then found the new rule was INCONSISTENT WITH ITSELF, and I reproduced it against the oracle**: `1.0 to 1.0` refused, `-1.0 to -1.0` accepted, where the bootstrap refuses both. A negated float lowers to `0.0 - x` and floats are never constant-folded, so the guard requiring both bounds to be known constants silently returned for every negative float. Fixed at the root — the bound is now decoded from its TOKENS rather than from the emitted value, so the fold table's integer-only behaviour cannot reach the rule at all. ⚠ Two things fell out. The guard's stated reason named an IMPOSSIBLE program (*"an int endpoint widened against a float scrutinee"* — `comparableOperands` ends `tagIsFloat(left) == tagIsFloat(right)`, so that is E2028 one call earlier); it was deleted rather than re-commented, and any other token shape now panics naming the invariant. And the tick's headline departure from both references — comparing `floatOrderKey`s so `-0.0 to 0.0` reads as one value — had been **INERT ON THE REACHABLE SET**, because `-0.0` is a negated literal and never arrived. Closing the hole is what made it load-bearing: `-0.0 to 0.0` now refuses, and those two bounds are `i64.min` and `0`, so only the order key makes them one value. Also folded: a second token-span renderer whose original's header warns that a copy *"quietly names the offending token something the source never wrote"* — now one `tokenSourceSpelling` in the Lexer covering char, string AND byte-string, which also fixed a pre-existing two-column under-advance that `tokenTextFrom`'s own ⚠ had confessed; and the sibling *"is this range empty"* predicate, still forked across two sites while this tick's placement argument rested on the two rules being disjoint. Suite 5156/0 after this tick. |
| enum-struct-field-match | 2026-08-10 | PORTED | 3/3 | Fast path (§7b): green on the first filtered run, byte-identical, zero compiler source changed. A payload-bearing (BOXED) union living in a struct field, matched three ways — through a `let` copy of the field, DIRECTLY off the field expression (`match d.shape`), and with its payload extracted (`number(n) then return n`). All already work. ⭐ Worth noting against the two preceding ticks, which both turned on boxed unions: `enum-ordinal` found `.ordinal` reading a boxed union's box POINTER as a bare tag, and `assignment.md`'s shelved case is blocked on `.ordinal` through an enum-typed FIELD — yet `match` over that same field-held boxed union was correct all along. The two doors were never the same door, and the field-base restriction is `enumOrdinalFieldAt`'s alone. No suite figure — the fast path measures none, and a number nobody measured looks exactly like one that was. |
| mutable-enums | 2026-08-10 | PORTED | 8/8 | built the write-back half of mutable match bindings, which had never existed — `declarePayloadBindings` hard-coded `mutable: false` and there was no store path into the box at all. Scrutinee mutability now comes from OWNERSHIP (`liveOwnerIndexOf`, the scan the move machinery already reads) rather than v1's token probe. ⚠ PLAN.md had filed this spec's 8 cases as residual #74, "a DECISION (a deliberate divergence)" — twice; that category does not exist, and the entry is corrected. Review found a REAL leak the filter could not reach: `scrutMutable` (membership, survives a phi) and `scrutOwned` (provenance, phi-blind) are two sources for one fact, so a `var` union matched inside the loop that reassigns it took the retain path with the slot still occupied and stored over it — exit 101, now demoted to E2013 at the branch that chooses the acquisition. Suite 5167/0 after this tick. |
| union-cases | 2026-08-10 | PORTED | 8/8 | a §7 tick — three mechanisms, not one. The whole enum reflection surface was missing (`.name`, `.rawValue`, `.allCases`, `.fromRawValue`; only `.ordinal` existed), `U.unionCases` did not exist at all, and the payload path's SCALAR arm never asked for type IDENTITY, so any enum was accepted into any enum-typed payload slot — a wrong answer, not a missing message. ⭐ Scoping by grepping OTHER specs' disabled debt BEFORE briefing found three shelved cases this mechanism owed: `union-scalar-backing` 2→4 (both marked "later rung" / "P1.2 .rawValue / .name accessors") and `enum-full` 42→43, whose shelved case was a WORDING STANDOFF a previous tick declined to reconcile. Review found six things; the one correctness lead was REFUTED by the ported spec itself — `runtime-accessors` pins `sh.rawValue` on a payload-bearing union as 0/1/2 and the oracle agrees, so the proposed refusal would have turned a committed case red. Suite 5179/0 after this tick. |
| enum-function-backing | 2026-08-10 | PORTED | 6/6 | the filed gap was one refusal; three more sat behind it. A type-qualified accessor never worked for ANY backing (`Mime.json.rawValue` was E2010 against a correct oracle). The previous tick's `EnumCaseConstant` column absorbed the function backing whole — one more per-case machine word, taking `FirstCaseIndex` because a function address is never provably zero — and deferred binding needed no pass at all, since the declaration records the NAME and the whole-program sweep already runs first. ⭐ Then the marker sweep: all four `disabled-test:` in `first-class-functions.md` lifted, 2 stale (blockers had landed) and 2 genuine, both built ⇒ that file is at ZERO disabled, 121→125 active. Review found a container literal accepting mismatched function shapes — reproduced as TWO wrong answers (an argument nobody passed; a float return read out of a GPR) — and a cross-interner id read that a GREEN CASE COULD NOT DISTINGUISH FROM CORRECTNESS: sabotage showed the line executes and the two interners merely agreed at id 3. Suite 5191/0 after this tick. |
| enum-struct-backing | 2026-08-11 | PORTED | 20/20 | ⚠ the spec DIVERGES BY ADDITION ONLY — 0 removed lines, 8 shv2-authored `maxoncstderr` cases added (20/20), because the tick's five new refusals had ZERO committed coverage, F1's entire fix among them. ⭐ THE BIG MEASUREMENT DELETED A DESIGN: there is no compile-time factory evaluation — the oracle answers 3 for `Meta.create(1)` even with NO SUCH STATIC DECLARED, so an argument means its position or its label. Review found a backing field's constant was never checked against the field's DECLARED type: a `Real` field PANICKED the x64 emitter, a `String` field access-violated, a `bool` field was right BY LUCK. Then the fix reproduced its own defect one type over — a boxed union's BOX POINTER classified as a tag (0xC0000005) — and asserted an ordering premise the same diff disproves 90 lines up. Marker sweep: `function-overloads` 3 of 4 stale, now 26/26 and zero disabled. Suite 5219/0 after this tick. |
| enum-nested-struct-backing | 2026-08-11 | PORTED | 17/17 | ⚠ BOTH specs diverge BY ADDITION ONLY (0 removed lines): the ported spec runs 17/17 (4 canonical + 13 shv2-authored), and one shv2-authored case was DELETED from `enum-struct-backing.md` (19/19) because THIS TICK SUPERSEDED IT — I added it one commit ago to pin a refusal the very next whitelist entry removes. ⭐ The blocker was NOT the one the previous tick predicted: nested LITERALS were refused by name, but all 4 cases use the nested FACTORY form and died in the const-argument reader at the inner `(` (E2010) before reaching that refusal. Declaration is FLAT with a group id (a recursive type is E4014); the READ is leaf-path keyed, from v1. ⛔ ALSO FIXES A BOOTSTRAP MISCOMPILE-CLASS BUG (`maxon-sharp`): arg-pinning captured `opIndex` before inserting, so the 2nd pin into one block landed on the op defining the next argument's value — E9001 on a legal program. Bootstrap battery re-run twice, 3352/0, 0 goldens written, shv2 tree hash byte-identical both times. Suite 5243/0 after this tick. |
| union-struct-backing | 2026-08-11 | PORTED | 6/6 | ⚠ FIVE specs diverge BY ADDITION ONLY (0 removed lines in each). Two independent gaps: a union case's payload list was TERMINAL, so `=` was never reached; and the type-qualified member door admitted only the three accessors AND sat below the boxed-union early return. ⛔ THE REAL FIND WAS IN A FILE THE DIFF DOES NOT TOUCH: `unionCases` hard-codes the companion's tag as the DECLARATION INDEX, which was a correct restatement of the union's tag only while boxed unions could not declare raw values — this tick removes that constraint, and its own new case commits the declaration that breaks it. MEASURED: `op.rawValue` 5, companion 0, round-trip THROWS — the companion's stated purpose in /specs. Nothing composed the two halves, so neither the filters nor the full suite gated it. The C# bootstrap has the IDENTICAL defect, so the oracle could not adjudicate; v1's comment was the only reference that settled it. Also fixes a SECOND bootstrap crash (E9001, null key) whose repro was WIDER than the bug. Suite 5254/0 after this tick. |
| struct-construction-restriction | 2026-08-11 | PORTED | 2/2 | a 2-case wording tick where everything expensive was in the SENTENCES AROUND the change. shv2's E3076 diverged from `/specs` in two respects — a sentence of its own invention naming a concrete `'Box.create(...)'`, and an anchor at the TYPE NAME where the oracle anchors INSIDE THE BRACE. Not a judgement call: `/specs` holds exactly ONE E3076 sentence tree-wide, the bootstrap emits it verbatim, and the only pins carrying shv2's spelling were 4 in a shv2-authored file. ⭐ The implementer read the anchor off the ORACLE'S EMITTER rather than off my three samples, making "the token after `{`" a RULE; v1 was read and deliberately NOT followed (it anchors at the type name, contradicting `/specs`' own pinned column). ⛔ Review then found the fix had falsified FOUR other comments — one justifying a live arm-order decision — and, best of all, that a paragraph claiming a "deliberate departure from the oracle" describes a departure THAT DOES NOT EXIST, while inviting the edit that would create one. Suite 5256/0 after this tick. |
| enum-narrow-storage | 2026-08-11 | PORTED | 6/6 | a §7 rung with NO v1 REFERENCE — v1 answers `i64` for every enum and excludes them from packing CITING THIS VERY SPEC, so the width derivation was designed from scratch (min/max of `caseTags`, one ladder). ⭐ The scoping pass paid for itself: the `.rdata` pin is a PREFIX COMPARE FROM BYTE 0, so `got 0x70` was the panic-message prefix in all four failures and "the blob must be FIRST" is a design constraint, not a symptom. ⛔ Review found a THIRD sign-extension door and I MEASURED it: `Signal.minus` (-1) reached a user comparator as **255** through `Array.sort`, and the sort ORDER was wrong too — both existing doors cover values coming OUT of a call; a shared corpus body passing a slot INTO user code as an ARGUMENT has nothing coming out. Fixed at the callee's ENTRY, which is complete rather than an enumeration. `sizeof` deliberately NOT narrowed (the spec's own sentence names three places and `sizeof` is not one); struct fields deliberately NOT narrowed, because shv2 has no narrow field for ANY type and it needs backend widths two of whose three lanes this host cannot execute. Suite 5269/0 after this tick. |
| match-exhaustive-default-panic | 2026-08-11 | PORTED | 10/10 | ⚡ §7b FAST PATH — passed 10/10 on the first run, zero compiler source changed, so no reviewer and no full suite (a new spec file cannot change another spec's outcome). NO SUITE FIGURE: none was measured on this path, and carrying the previous tick's number forward would look exactly like one that was. The three §2 counts were run precisely BECAUSE it passed immediately — 10 markers == 10 ran, zero `disabled-test:`, no name spelled both ways, and `## Tests` verified as the LAST `## ` heading so no case is silently dropped. Covers `default panic` on statements and expressions, enum scrutinees, enum-range patterns, and three refusals (missing default on each form, plain `default` on an enum). Already solid because shv2's own source is a heavy user: `.claude/CLAUDE.md`'s Code Quality rule mandates `default throws` / `default panic` and forbids a bare `default`, so the compiler dogfoods this feature everywhere. |
| panic-stack-trace | 2026-08-11 | PORTED | 3/3 | ⚡ §7b FAST PATH — 3/3 on the first run, zero compiler source changed, so no reviewer and no full suite. NO SUITE FIGURE: none was measured on this path. The three §2 counts run precisely BECAUSE it passed immediately — 3 markers == 3 ran, zero `disabled-test:`, no name spelled both ways, byte-identical to `/specs`, and `## Notes` verified to sit BEFORE `## Tests` so the test region runs to EOF with nothing dropped. Substantive despite being free: the cases pin actual `stderr`, so a range-check panic's message, the RBP-chain walk, the function names resolved from the embedded symbol table and `mrt_start` at the base all had to agree with the reference byte-for-byte across a 3-deep call chain. ⚠ Host lane only — these carry no `targets:` marker, and `.claude/CLAUDE.md` records `mrt_panic` as a Windows-only hand-assembled chunk (8 cases elsewhere are marker-gated for exactly that reason), so the arm64 and wasm lanes get their verdict when their sweep next runs, not here. |
| self-assignment | 2026-08-11 | PORTED | 5/5 | a NEW REFUSAL, where the corpus sweep IS the tick. E3067 was unclaimed by shv2 and unemitted; two rules under one code (textual `x = x` / `p.x = p.x`, and `_ = <literal>`). Enables 2 owed cases (`discarded-results`, `unused-variables`) whose stated blocker was exactly this. ⛔⛔ THE SWEEP THAT SAID "ZERO HITS" HAD NEVER RUN: `grep -P` is refused by this box's locale, and `2>/dev/null` swallowed the refusal, so EMPTY OUTPUT READ AS ZERO HITS — the stale-binary trap in a new costume. Re-run with the locale fixed and stderr visible, it found `specs-shv2/var-reassign-drops.md:293`, an ENABLED `s = s` test with prose titled "Self-Assign Is a No-Op" — and the only regression pin on the drop-then-keep guard. Rewritten (not deleted) to reach that guard by VALUE IDENTITY rather than spelling, so it survives the day the rule goes semantic. ⚠ The discard half is bounded to a bare LITERAL, not the doc's "any other expression": `_ = <identifier>` is live in the STDLIB plus five committed pins, so the literal prose would have reddened every spec-test run. Suite 5289/0 after this tick. |
| duplicate-functions | 2026-08-11 | PORTED | 3/3 | a 3-case wording tick whose rename was ONE `return` — the branch split already existed. ⭐ The framing was the work: canonical `/specs` distinguishes THREE situations under E3006 (a plain duplicate function, a name collision AFTER MANGLING, a duplicate top-level variable) and shv2 had collapsed two, so a blanket rename would have moved 11 pins that should not move. ⛔ Review then found the plain sentence is selected by ENUMERATING two known mangled kinds rather than testing mintedness, and the mint makes more: MEASURED, a redeclared overload member reported `Duplicate function 'pick#bool'` — a symbol appearing nowhere in the source, where canonical puts only written names. Cured by a STRUCTURAL predicate (`#` is outside the identifier alphabet, so `base == name` cannot be true of a minted name), and pinned. ⚠ Two of the review's own claims died to measurement: the bootstrap does NOT refuse that program at all (shv2 is stricter, so there was nothing to "match"), and its second population is caught earlier by a dedicated guard with a better message. Suite 5295/0 after this tick. |
| var-should-be-let | 2026-08-11 | PORTED | 12/12 | the widest NEW REFUSAL of this loop — E3077, a `var` never reassigned must be `let`. The corpus sweep WAS the tick: 3655 `var` declarations across specs-shv2 programs, compiled ONLY by shv2 (stdlib and shv2's own source were already safe because the BOOTSTRAP enforces E3077 and builds both). 41 programs corrected in 15 files, 0 canonical. ⛔⛔ Review found the rule's six marking doors were an ENUMERATION and there was a SEVENTH — and I measured the consequence: `var o` / `var i = o.inner` / `i.value = 99` had NO LEGAL SPELLING. Following the compiler's own advice each time gave E3077 → E3078 → E2013, and `clone()`, the escape E3078 ADVERTISES, is itself broken (E2015; pre-existing, and the oracle compiles it). The predicate's own header said the invariant in capitals — *"TWO RULES REST ON IT, AND THEY MUST NOT DRIFT"* — and E3077's evidence was recorded on one side only. ⚠ The implementer also caught itself reporting a FALSE GREEN: a stale `.maxon-tree.lock` from a timed-out run made every later `spec-test` refuse to start, and "0 failing" read as success. Suite 5308/0 after this tick. |
| hex-escape | 2026-08-11 | PORTED | 8/8 | ONE RULE, TWO VALIDATORS — every literal form reached the shared parameterized E1004 producer except the byte-string decoder, which ran its own validator throwing the E2015 catch-all with a sentence that never quoted what the author wrote. shv2 already CLAIMED E1004 and already emitted the exact sentence on the sibling route (string interpolation), so the fix was to call the producer and DELETE the duplicate. ⛔ Review found a comment promising the column is "blamed where it is written" — FALSE: it is the opening delimiter advanced by the escape's offset in the BODY, so it falls short by the delimiter's width, and this diff added the widest one (`b"`, 2). Every canonical pin is an offset-0 escape where all three compilers coincide, so NO GOLDEN CAN CATCH IT — a future agent measuring `b"AB\xG0"` would read 12-vs-10 as a regression and revert correct behaviour. Now pinned by a non-first-escape case. ⚠ The reviewer's suggested union refactor did NOT hold (Maxon match arms are single-statement); the same cure by another route found the width arithmetic at FOUR sites, not two. Suite 5318/0 after this tick. |
| reserved-self | 2026-08-11 | PORTED | 6/6 | two unrelated problems. (A) shv2's E2010 template QUOTED EVERY expectation, where canonical quotes only a token SPELLING. ⚠ I framed that rule wrong TWICE in one tick — "bare category ⇒ unquote" (refuted: canonical quotes `'interpolation end'`) and then "multi-word phrase ⇒ quote" (refuted: canonical writes `expected type name` BARE). The rule is the reference's code — `KeywordMap`+`OperatorMap` entries are quoted because they are strings an author must TYPE — and the discriminator is PROVENANCE, not text. Exactly one string changes rendering: `identifier`. (B) `requireUnreservedName` enforced only the `__` reservation, so a parameter named `self` walked past and was misdiagnosed downstream TWICE — `E3012 unused variable`, or `E2001 'self' can only be used inside instance methods` if used. ⭐ The door's own header said EIGHT sites against 15 calls, and the review found TWO BINDERS THAT NEVER ASKED IT: `try f() otherwise (__x)` and `else (__e)` COMPILED AND RAN (exit 42) where the oracle refuses. `self` was never reachable there, so the ported spec was already complete — the stale census was the only thing that showed the `__` hole. Suite 5324/0 after this tick. |
| internal-types | 2026-08-11 | PORTED | 2/2 | ⚡ §7b FAST PATH — 2/2 on the first run, zero compiler source changed, so no reviewer and no full suite (a new spec file cannot change another spec's outcome). NO SUITE FIGURE: none was measured on this path, and carrying the previous tick's number forward would look exactly like one that was. The three §2 counts run precisely BECAUSE it passed immediately — 2 markers == 2 ran, zero `disabled-test:`, no name spelled both ways, byte-identical to `/specs`, and `## Tests` verified as the LAST `## ` heading so the test region runs to EOF with nothing dropped. ⚠ Worth a reader knowing what this spec does and does not pin: its `## Documentation` is a full surface listing for the `__`-prefixed builtins (`__ManagedMemory`'s 14 instance + 2 static methods, `__ManagedList`, `__ManagedListNode`, `__ManagedListError`), but its two cases exercise NONE of them by name — they call `Array.create/push/count` and an array literal, which reach `__ManagedMemory` only INDIRECTLY (`stdlib/Array.maxon:105`, `typealias ElementMemory = __ManagedMemory with Element`). That is canonical's own design and the copy is byte-identical, so nothing was edited; but the builtin method surface itself is documented here and pinned elsewhere, and a later agent should not read this file's green as coverage of it. |
| codegen-internals | 2026-08-11 | PORTED | 32/32 | a §7 rung wearing a spec's clothes: 19/32 on the first run, and the other 13 took four mechanisms, a memory-safety fix and a USER RULING. ⛔ **The widened constant-blob path made a PRE-EXISTING wild write reachable** — `buildSetFrom`/`buildListFrom` null moved-out slots straight through `buffer@0` and never load `capacity@16`, so the `.rdata` read-only sentinel could not save them (shv2 already stamps v1's `capacity = -2`; the COW protocol was intact and these two bypassed it). 13 `set/` cases went 0xC0000005 the moment an int-element literal stopped being a heap record. Cured where the two conditions coincide: *immortal ⇒ trivial ⇒ guarded out* — the store that could reach read-only memory is exactly the store that was doing nothing. ⭐ **v1, not the bootstrap, settled the `.rdata` layout**: the oracle passes these pins only because it never materializes `panic()` strings into `.rdata` at all, while v1 gets it structurally — `PassPipeline.maxon:85` runs the constant-array blob pass BEFORE the two passes that mint `__panic_msg_*`, so program literals precede compiler-composed payloads by construction. Two measured dead ends recorded so nobody walks them twice: blob/record segregation breaks `rdata-section` (its image is a descriptor followed by a panic blob), and the label-anchored read `SpecTestRunner.maxon` had prescribed in capitals since BATCH29 CANNOT express a two-blob pin. USER RULING: a `utf8` line is LOCATED, not prefixed — it names one interned blob, where scalar lines are fields of one payload — so shv2's static `.rdata` String record (a real improvement over v1 heap-allocating 48 bytes per literal) was not spent on a golden; mixed blocks and `utf8 ""` are refused, and a new shv2-native case fails under the old rule. ⚠ **The full suite caught two regressions thirteen filtered runs did not**: a case that stopped COMPILING (E5001, ten registers short) and a FALSE REFUSAL misreported as a fail-open until measured — both one cause, cured by `retypeValue` (a rename, no second live range) because minting is the wrong instrument for a representational no-op. ⛔ Review then found a fail-open the 5359-case suite structurally could not see: `ByteArray from [Color.red, …]` compiled and answered 1 while `Color.green as Byte` is refused E3009 — and the bootstrap answers 0, two compilers disagreeing on a program neither refuses. One review call died to measurement (the oracle ACCEPTS `[10 as Byte, 20]`, so the old refusal was shv2 being over-strict; gating it back would have made shv2 wrong). 2026 goldens regenerated: `__str_blob_N` is a running counter, so any ordering change renumbers thousands — v1 content-addresses them and pays nothing, which is the real fix and not this tick. Suite 5359/0 after this tick. |
| same-name-methods | 2026-08-11 | PORTED | 5/5 | a static and an instance method of one name were ONE registration name, because both halves of the overload key skip the receiver slot on a premise that is FALSE for exactly this pair — `overloadTypeSuffix` said "the receiver is the same type for every overload of one method, so it distinguishes nothing", and a static beside an instance has no receiver at all. So `static getValue()` and `getValue()` both rendered `typeSuffix=""`/`labelSuffix=""` and collided (E3006); `returns-self` was the same cause one pass earlier, the sweep's return type for `Counter.reset` going last-wins to void and the call reading E2004. The two cases that already passed are the two whose arities differ. Fixed by giving a CONTESTED static its own base (`Box.getValue#__static`, minted on contest only, unforgeable because `requireUnreservedName` refuses `__` in both suffix sources); the instance keeps the source spelling, and the contest is a fact the whole-program sweep publishes that both the declaration door and the call door read back — `contestedExtensionMethods`' shape — rather than the references' rename-the-incumbent, which a per-file parser cannot do. Review found five, all five confirmed, one by compiling the program it named: the new E3006 arm was gated on the CONTEST rather than on the PROPERTY, so a duplicate INSTANCE pair was told to give its statics distinct parameters, quoting a name the author had written. Its two sentences are now pinned by a new shv2-only spec, `static-instance-name-duplicates` (6 cases), because canonical structurally cannot cover a mangled-name sentence and this tick had left them measured but ungated. Suite 5377/0 after this tick. |
| enum-match-range | 2026-08-11 | PORTED | 10/10 | 9/10 on the first run; the tenth differed by ONE WORD and no compiler behaviour changed. Canonical pins `match on enum 'Op'` for a scrutinee the same program declares as `union Op`; shv2 derives the noun from `EnumLayout.kindWord()` and says `union`. Retracted the word, because the spelling is ratified TWICE: `enum-match-exhaustive/error.union-not-exhaustive` is an shv2-authored, live, green case pinning `match on union 'Shape'` and recording the bootstrap's hardcoded `"enum"` as a documented oracle divergence (`PLAN.md` finding #34, CLOSED `8718c8d7c`), and canonical ITSELF writes the noun for a union scrutinee in `/specs/try-block.md` (`match on error union is not exhaustive`). Verified there is no contradicting pin anywhere in either tree: the disputed line is the ONLY place in the whole corpus where an `enum '<Name>'` expectation names a `union`-declared type. The bootstrap's source settles it — it literally interpolates the constant `{"enum"}` at that one site while branching on `IsUnion` at its E3034 sites four lines away. ⛔ The verification also turned up a comment in `BuiltinConformanceRuntime.maxon` quoting a "verified" E2026 transcript for a type nothing declares (`'Op'` for `union StdOp`) — RE-MEASURED by appending a variant and building, and corrected. ⚠ Its noun is `enum` and that is CORRECT, which is the trap: the sentence is the BOOTSTRAP's, because shv2's own source is built by `maxon build maxon-shv2`. "Fixing" it to `union` would have made a true comment false. Suite 5387/0 after this tick. |
| enum-allcases | 2026-08-11 | PORTED | 8/8 | 7/8 on the first run. `<Union>.allCases` came out as the generic `E3034 unknown union case: 'allCases'` where the language requires a refusal that names the rule, `E4006 allCases is not available on unions`. ⛔ The cause was a comment that REFUTED ITSELF at the site: `parseEnumLiteral` claimed `allCases` and `fromRawValue` inside one `if not layout.isUnion 'enumOnlyStatics'` gate, and the header reported the oracle giving `allCases` a dedicated E4006 and `fromRawValue` a fall-through — then concluded "both are therefore claimed only for an enum; on a union they fall through". It measured the asymmetry correctly and then applied one member's disposition to both. Verified against the oracle's source: `2-Parser.cs:19091` throws the dedicated E4006, `:19113` falls through, and v1 agrees to the character. Fixed by claiming `allCases` unconditionally and throwing a new `ParseError.allCasesOnUnion` on a union, leaving `fromRawValue`'s verdict byte-identical; both live `fromRawValue` pins (`enum-full`, `enum-struct-backing`) held unmoved. No registry change — E4006's `shv2 invalidFieldAccess` claim was already live. ⚠ One deliberate widening: a union declaring a case literally named `allCases` now gets E4006 instead of resolving the case — member-name-first, matching v1 on both members, and a classified sweep found 267 `allCases` hits across 98 files with ZERO such declared cases. ⛔ Review then found the same defect class twice more, both PRE-EXISTING: two comments in `Project.maxon` claimed a payload-free "enum OR union" already exposes this surface and credited `/specs/union-cases.md` for it — the spec says "enums", the widening was the comment's own, and it is FALSE both ways (E4006 for `.allCases`, E3034 for `.fromRawValue`), while being the stated justification for `appendUnionCasesCompanion`'s gate. Suite 5395/0 after this tick. |
| collection-contains | 2026-08-11 | PORTED | 16/16 | ⚡ §7b FAST PATH — 16/16 on the first run, zero compiler source changed, so no reviewer and no full suite (a new spec file cannot change another spec's outcome). NO SUITE FIGURE: none was measured on this path, and carrying the previous tick's number forward would look exactly like one that was. The three §2 counts run precisely BECAUSE it passed immediately — 16 markers == 16 ran, zero `disabled-test:`, no name spelled both ways, byte-identical to `/specs`, `status: stable` (not `draft`), and `## Tests` (line 40) verified as the LAST `## ` heading so the region runs to EOF with nothing dropped. All 16 cases minted a golden, so every one of them compiled. ⚠ What the green does and does NOT cover: both `contains` variants are exercised — single ELEMENT (`Array` of int, `Array` of String, `String`.contains(Character), `Set`) and contiguous SUBSEQUENCE (`Array` in `Array`, substring in `String`), plus the empty, first-element and last-element edges and both at-start/at-end subsequence boundaries. But the file has **no `error.*` case at all**, so nothing here pins the REFUSAL side: the documentation states the element variant "Requires the element type to be `Equatable`", and what shv2 says for a non-`Equatable` element, or for a non-contiguous match being asked of the subsequence variant, is unpinned by this spec. That is canonical's own design and the copy is byte-identical, so nothing was edited — but a later agent should not read this file's green as coverage of the `Equatable` constraint. |
| array-contains | 2026-08-11 | PORTED | 1/1 | ⚡ §7b FAST PATH — 1/1 on the first run, zero compiler source changed, so no reviewer and no full suite. NO SUITE FIGURE: none was measured on this path, and carrying the previous tick's number forward would look exactly like one that was. §2's three counts run precisely BECAUSE it passed immediately — 1 marker == 1 ran, zero `disabled-test:`, no duplicate name, byte-identical to `/specs`, `## Tests` (line 14) verified as the LAST `## ` heading, and the one case minted its golden so it genuinely compiled. ⭐ **A ONE-CASE SPEC IS WHERE A GREEN MOST EASILY PROVES NOTHING, so this one was READ, and it holds up.** `ByteArrayArray.contains(b"hello")` is asked with a `b"hello"` that is a DIFFERENT ALLOCATION from the one pushed, and `stdout` pins both `found` and `not found` — so a pointer-identity implementation prints `not found` twice and fails. The case is self-sabotaging by construction, which is the property a single-case file needs. ⚠ It is NOT redundant with `collection-contains`, ported one tick earlier: that file's element types were int, String and Character, whose equality is their own; here the element is itself a managed `Array with Byte`, so `.contains` must dispatch through **Array's conditional `Equatable` conformance over a ranged-int element** — the nested-generic path, and the one the spec's prose says the case exists for ("element comparison requires calling `.equals()` rather than comparing pointers"). |
| refcount-shared-borrow-root-phi-args | 2026-08-12 | PORTED | 1/1 | ⚡ §7b FAST PATH — 1/1 on the first run, zero compiler source changed, so no reviewer and no full suite. NO SUITE FIGURE: none was measured on this path, and carrying the previous tick's number forward would look exactly like one that was. §2's three counts run precisely BECAUSE it passed immediately — 1 marker == 1 ran, zero `disabled-test:`, no duplicate name, byte-identical to `/specs`, `status: experimental` (not `draft`), `## Tests` (line 45) verified as the LAST `## ` heading so the region runs to EOF, and the case minted its golden so it genuinely compiled. ⭐ **A ONE-CASE MEMORY-SAFETY SPEC IS WHERE A GREEN MOST EASILY PROVES NOTHING, so the emitted IR was READ — and what it shows is that shv2 SATISFIES this spec WITHOUT ENTERING THE HAZARD IT DESCRIBES.** The spec's subject is a `break` edge carrying several phi-args that are all *interior borrows of one root*, where the root's single deferred `+1` must be released ONCE and AFTER the field increfs; releasing it per-block-arg frees a map-owned element out from under the map. shv2's lowering of `failedParam = c.paramName` (assignment into an outer `var String`) is a **COPY** — `__mm_alloc` + `__str_copy` into a fresh allocation, then `__str_decref` of the var's previous value — three times, one per field. The borrowed element `c` (`__managed_get`, elem size 8, so a pointer to a heap `Clause`) takes **NO incref and NO decref anywhere in the loop**; the only container release is `__managed_decref` of the borrowed `clauses` array at the merge. ⇒ The "several phi-args on one edge" shape IS present (three values reach `forexit`: `slot4` plus two live registers), but they are OWNED COPIES, not interior borrows of a shared root, so the per-block-arg over-release this spec was written to catch is unreachable on this lowering. ⚠ That is NOT a divergence and nothing was edited — the spec asserts only `exitcode 27`, shv2 returns 27, no leak, and copy-on-assignment-to-a-`var` is the ruled ownership semantics ("MUTABILITY decides move-vs-alias"). But a later agent must NOT read this green as coverage of borrow-root release ordering: it is coverage of the copy path, and the day shv2 borrows here instead, this case starts testing its title. |
| refcount-global-getter-forward | 2026-08-12 | PORTED | 1/1 | ⚡ §7b FAST PATH — 1/1 on the first run, zero compiler source changed, so no reviewer and no full suite. NO SUITE FIGURE: none was measured on this path. §2's three counts run precisely BECAUSE it passed immediately — 1 marker == 1 ran, zero `disabled-test:`, no duplicate name, byte-identical to `/specs`, `## Tests` (line 48) is the LAST `## ` heading, and the case minted its golden so it genuinely compiled. ⛔⛔ **THIS FILE'S OWN FRONTMATTER MAKES A CLAIM ABOUT shv2 THAT IS NOW MEASURED FALSE.** Its `status-reason` (written 2026-08-06, BATCH29/A3a) ends *"shv2 cannot take it either: E2015 for a top-level instantiation of a user generic"* — and `typealias ItemBox = Box with Item` at module scope compiles here today, so the case runs and passes. Nothing was edited: `/specs/**` is never written to and the copy must stay byte-identical, so the correction lives here. ⚠ `status: selfhosted` does NOT suspend a spec in this suite — `SpecParser` skips only the literal `draft` (its own comment says so in capitals), which is why the file ran at all. ⭐ **The green is REAL but it is NOT coverage of this spec's stated subject.** The spec's Documentation describes a v1 mechanism — `funcReturnsGlobalBorrow`, a strict subset of the borrow set, suppressing the returned-borrow retain on a forwarded module-global — and **shv2 HAS NO SUCH CLASSIFICATION** (`grep funcReturnsGlobalBorrow` hits four v1 files and zero shv2 files). The emitted IR shows why it passes anyway: shv2 returns an OWNED `+1`, not a borrow. `loadBase` emits `__mm_retain` on `__data_cachedBase` in BOTH arms before returning; `buildFor`'s native arm forwards that owned reference with no second retain and no release; its two merged arms retain `cachedMerged` and release the forwarded `base`; `augment` calls `__destruct_Box_Item` on the result after reading `size()`. Every `+1` is balanced by exactly one release, so the leak the spec guards against cannot arise — not because the retain was suppressed, but because there is no borrow to forward. ⇒ a later agent must not read this file's green as evidence shv2 classifies global-borrow-returning getters; it does not, and does not need to. |
| refcount-param-field-borrow-forward | 2026-08-12 | PORTED | 1/1 | no compiler change. The one retraction is the spec's `i64` type spellings → `int`: `i64` is NOT a Maxon type name, and `/specs/ranged-typealias.md` says so in the language's own words ("Bare sized type 'i64' is not allowed. Use explicit range syntax") — a rule the ALREADY-PORTED `specs-shv2/ranged-typealias.md` pins. Both runnable compilers refuse it in every position (measured: bootstrap E2003, shv2 E3011). v1 passed the file only through a hole in its OWN refusal: it rejects `typealias X = i64` but lets an unknown named type in a field/param/return position fall through to `StdType.i64` storage (`Project.maxon:3888`). With the legal spelling the subject passes on the first run and does not leak — shv2's borrow classification already suppresses the forwarded retain. Suite 5454/0 after this tick. |
| refcount-infallible-borrow-throw-edge | 2026-08-12 | PORTED | 1/1 | ⚡ §7b FAST PATH — 1/1 on the first run, zero compiler source changed, so no reviewer and no full suite. NO SUITE FIGURE: none was measured on this path, and carrying the previous tick's number forward would look exactly like one that was. §2's three counts run precisely BECAUSE it passed immediately — 1 marker == 1 ran, zero `disabled-test:`, no duplicate name, byte-identical to `/specs`, `status: experimental` (not `draft`), `## Tests` (line 47) verified as the LAST `## ` heading so the region runs to EOF, and the case minted its golden so it genuinely compiled. ⭐ **A ONE-CASE MEMORY-SAFETY SPEC IS WHERE A GREEN MOST EASILY PROVES NOTHING, so the emitted IR was READ — and shv2 is immune for EXACTLY THE REASON THE SPEC ITSELF GIVES FOR THE C# ORACLE BEING IMMUNE.** The spec's hazard is v1's: a direct-call result is tagged `callReturnRc1` (assumes a transferred `+1`), which is a LIE for a borrow-returning `current()`, so the release side decrefs it on a sibling throw edge and strips the collection's own reference. shv2's `ArrayIterator.current` ends `__managed_cursor_current` → **`__retain_type_param`** — it hands the caller its own `+1`, so there is no borrow and no lie. The throw-edge decref the spec calls deadly **is present and is correct**: `Reader.consume`'s `tryerr#6` calls `__destruct_Item` on `cur` before rethrowing, balancing the retain `current()` performed, while `tryok#5` forwards the same owned value out. The spec's own text names this immunity ("uniform borrow: every element read hands the reader its own `+1`") and attributes it to the oracle; it is true of shv2 too. ⚠ The hazard SHAPE is fully present and the throw edge genuinely fires — exit 6 is `arr.count()` 1 + the `otherwise` fallback's tag 5, which is only reachable if `advance()` threw — and the self-recursive never-taken arm did keep `consume` a REAL call (`x64.callDirect Reader.consume` in the emitted `rec` block), so the inliner did not collapse the multi-path borrow-forward the spec warns it would hide. ⇒ a later agent must NOT read this green as coverage of a borrow-returning-call release classification: shv2 has no such classification and does not need one, and the day `current()` returns an interior borrow instead of a retained element, this case starts testing its title. Third file in a row from this family (`refcount-shared-borrow-root-phi-args`, `refcount-global-getter-forward`) where a v1 refcount mechanism has no shv2 analogue and the spec passes on the ownership shape shv2 actually has. |
| typealias-managed-field-assignment | 2026-08-12 | PORTED | 3/3 | ⚡ §7b FAST PATH — 3/3 on the first run, zero compiler source changed, so no reviewer and no full suite. NO SUITE FIGURE: none was measured on this path. §2's three counts run precisely BECAUSE it passed immediately — 3 markers == 3 ran, zero `disabled-test:`, no duplicate name, byte-identical to `/specs`, `status: stable` (not `draft`), `## Tests` (line 30) is the LAST `## ` heading, and all three cases minted a golden so each genuinely compiled. ⭐ **UNLIKE THE LAST THREE REFCOUNT TICKS, THIS GREEN IS SUBSTANTIVE AND THE IR CONFIRMS IT.** The file is self-verifying by construction: every case loops 3000× under the leak gate, so a single missed leg compounds into exit 101, and `alias-typed-field-cloned-both-live` additionally pins `done 6000` (3000 × (1+1)) so a missed incref would double-free and a spurious one would leak. The emitted `cloneBlk` shows all three legs present and shows the store **ALIASES rather than copies** — `load rbx,[src+0]` ; `load rcx,[block+0]` ; `__managed_decref` ; `__mm_incref(rbx)` ; `store [block+0],rbx`, with no `__managed_clone` and no fresh alloc — so the alias name IS resolved before ownership classification, which is the property the Documentation says a raw-alias-name classifier would break. Leg 3 is `__destruct_Blk` on both structs in main's loop. ⛔⛔ **BUT READING THAT IR FOUND A LIVE USE-AFTER-FREE THE SPEC DOES NOT REACH: shv2 emits decref-old BEFORE incref-new, which is the REVERSE of the order this spec's own Documentation numbers them.** With the field the sole owner, the decref drops the refcount to 0 and frees, and the incref then touches freed memory. The direct spelling `self.args = self.args` is refused by **E3067** ("self-assignment has no effect"), so the refusal was MASKING the defect on the sibling INDIRECT route — a struct in shv2 is a pointer to a shared heap box, so `assign(a, src: a)` over `function assign(dst Blk, src Blk)` makes `dst` and `src` the same box. Measured: **shv2 exits `0xC0000005` (access violation)**; the **bootstrap oracle is ALSO wrong but differently** — it prints `n=1 v=0` where `v` should be 7, then `mm_decref: refcount underflow (already zero)`, exit 1. ⇒ neither compiler is a reference for this shape and the oracle cannot be copied; the fix is the standard protocol order (incref-new, then decref-old), which makes self-assignment a net no-op. Fixed in its own commit immediately after this port — see the following log row and `maxon-shv2/build-cost-log.md`. |
| managed-field-store-order | 2026-08-12 | PORTED | 3/3 | ⚠ **NOT A PORT — a shv2-AUTHORED pin**, and the row is here because the `typealias-managed-field-assignment` row above forward-references it. Landed the compiler fix that spec's IR exposed: `Parser.emitFieldWrite` emitted release-then-acquire, so a store whose two operands alias one record freed it and then touched freed memory (measured `0xC0000005`; the bootstrap is ALSO wrong, printing the element as `0` and reporting `mm_decref: refcount underflow`, so it is not the oracle here). Fix is ONE statement moved — `moveInPayload` hoisted above `emitDecref` — giving acquire → release → store, the order the three sibling durable-storage writes already use. The two doors PBR-1 named (`emitCheckedSelfFieldStore`, `parseFieldAssignment`) already converge on this single emission, so it is a consolidated-site fix, not an N-site one. Suite **5502/0**, no leak, and after re-minting, **0 of 5502 goldens differ**. Golden drift attribution was exact: 8 fragments drifted and every one is a managed field store; re-minting five whole spec filters touched no other golden. ⭐ **THE REVIEW CAUGHT THAT THE PIN PINNED NOTHING AGAINST THE OPPOSITE ERROR.** The three new cases carried only ```` ```stdout ````, and `SpecTestRunner.checkRunExitCode`'s `unpinned` arm is `TestOutcome.pass` — so a build that acquires without ever releasing leaks, exits 101, prints exactly what a correct run prints, and the cases report PASS. The suite-level `memoryLeak` flag does not save it either: it reads the RUNNER process's exit code (`SpecOutcomeTool.maxon:75`), not any test program's. Fixed by pinning `exitcode 0` on all three, which the goldens confirm took effect (`ExitCode: unpinned` → `ExitCode: 0`). ⚠ **The same hole is open in the CANONICAL spec above** — `/specs/typealias-managed-field-assignment.md` claims "any skipped incref/decref fails the test with a leak exit" while pinning no exit code, so a LEAK there passes; a missed incref is still caught (it double-frees and crashes). `/specs` is read-only and the copy must stay byte-identical, so it is recorded here and nowhere else. ⛔ **NO `build-cost-log.md` ROW**, deliberately: that log is written by `spec-port-finish.sh`, which measures every column, and this fix landed by hand — a hand-written row is a number nobody measured and looks exactly like one that was. Two reviewer findings were filed to PLAN.md instead of fixed here, both four-door changes with suite-wide golden churn: the durable-slot rebind protocol is prose at four doors and code at none, and the stated reason for release-BEFORE-store has expired (all four doors now load the displaced value into an SSA temp first, so `acquire → store → release` is strictly safer at identical cost). ⚠ `emitOpaqueFieldReassign` still releases first and is LEFT that way on purpose — safe only because `valueIsOwnedHeap` makes its acquire emit no IR, with a scheduled widening that would reproduce the defect; that reason is now written at its own decref. |
| refcount-consumed-arg-copy-success-edge | 2026-08-12 | PORTED | 1/1 | ⚡ §7b FAST PATH — 1/1 on the first run, zero compiler source changed, so no reviewer and no full suite. NO SUITE FIGURE: none was measured on this path. §2's three counts run precisely BECAUSE it passed immediately — 1 marker == 1 ran, zero `disabled-test:`, no duplicate name, byte-identical to `/specs`, `status: stable` (not `draft`), `## Tests` (line 50) is the LAST `## ` heading, and the case minted its golden so it genuinely compiled. ⭐ **THIS ONE PINS ```` ```exitcode 18 ````, NOT stdout — so unlike the previous tick's file its leak gate is genuinely ARMED**: a leak exits 101, 101 ≠ 18, the case FAILS. (Contrast `typealias-managed-field-assignment` two rows up, whose stdout-only cases pass while leaking — see the note there.) The hit path is exercised 6 times: round 0 misses and inserts, rounds 1–2 hit, sum = 3 × (1+2+3) = 18. ⭐ **The release the spec demands IS emitted and IS necessary** — `memoized`'s `hit` block is `mov rcx, r13` / `__str_decref`, releasing the `+1` `keyFor` produced, on the edge where the key dies without reaching `insert`. So the case is load-bearing, not vacuous. ⚠ **But it does NOT cover the mechanism the Documentation describes, because shv2 never reaches it: NO COPY IS MINTED.** The same `r13` is handed to `Map.get` and to `Map.insert` with no incref between, and the IR shows why that is correct — **shv2's `Map.get` BORROWS the key** (it hashes it and compares through the `Equatable` witness, and there is no `__drop_type_param` on the key anywhere in `Map.get`), while **`Map.insert` CONSUMES it** (`__drop_type_param` on both key and value on the duplicate-key path; `__managed_set` into the keys array otherwise). The spec's hazard needs `get` to consume by the type-param convention, forcing a copy so the caller keeps its original — and then a success-edge guard that suppressed that original's release. With `get` borrowing there is no copy, no retained original, and no suppression question. ⇒ what this case actually pins is that a locally-built `String` dying on an `if let` SUCCESS edge is released there — worth having, and not what the title says. **Fourth consecutive file in this family** (after `refcount-shared-borrow-root-phi-args`, `refcount-global-getter-forward`, `refcount-infallible-borrow-throw-edge`) where a v1 refcount mechanism has no shv2 analogue and the spec passes on the ownership shape shv2 actually has; the day `Map.get` consumes its key, this case starts testing its title. |
| refcount-reassigned-slot-borrow | 2026-08-12 | PORTED | 6/6 | 4/6 on the first run, and **both failures were in the STDLIB, not in the spec's program** — `E2015: overloading 'Array.sort'` raised at `stdlib/Array.maxon:652`. Three defects behind it, all from PBR-2, all fixed here. **Suite 5503 → 5516/0**, no leak, **0 of 5516 goldens differ**. ⭐ **A1 — a by-reference forwarding EDGE is a bare name.** A user free function `swap` that reassigns a parameter made `Array.swap` (`stdlib/helpers/sort/smallSort.maxon:31`, a METHOD inside `export extension Array`) inherit by-ref-ness, which travelled transitively to the overloaded `Array.sort`. ⚠ **My diagnosis was half wrong and the implementer corrected it**: keying the DECLARATION by a qualified name fixes nothing, because an edge only ever carries the token before the `(` — the conflation lives in the EDGE. Fixed by partitioning calls on whether a receiver was written (`ForwardCall`), plus a `self.`/`Self.` shape keyed by the ENCLOSING TYPE, which is static and needs no resolver. ⚠ The symmetric rule is UNSOUND and was measured before building: a bare call DOES reach a method through implicit `self`, so only the receiver-qualified direction narrows. ⛔⛔ **A2 — THE REFUSAL WAS MASKING A WRONG ANSWER, the third time this session.** With it lifted, `byref-param-reassign-then-read-borrow` returned **1061109567 = `0x3F3F3F3F`**, the freed-slab fill, where the spec pins 5. `rebindableSlotReads` (W41) already solves that class, but **its header enumerated TWO producers when a PBR-2 cell is a third that was never wired in** — the stale comment IS why the bug existed ([[comment-premise-the-code-doesnt-test]] as root cause, not as a side note). One line at `emitCellLoad`. ⭐ **B — a narrow-ranged by-reference scalar parameter guarded the WRONG OPERAND**: the emitted check compared the CELL POINTER against the range (`cmpRegImm32 rcx, 0` / `1000000`), so a valid 99 panicked *"value outside typealias 'Int'"* while the bootstrap oracle returned 99 correctly. A full-range alias hid it because `rangeIsFull` emits no guard at all, so the split was never between two types. Fixed with a `GuardedOperand` union in `InsertRangeChecks`. ⚠ **A2 and B were invisible for the same structural reason — a refusal and an elision each hid a wrong answer downstream.** ⭐ **The review then found the fix claimed a closure it did not have**, and I confirmed it by measurement: a user METHOD named `swap` still tripped the identical E2015. **The `self.` narrowing is sound but does NOT reach it**, and the attribution was PROVED BY SABOTAGE rather than inferred — the carrier is `Array.swap`'s OWN body, `try managed.swap(i, j: j)`, a dotted call on a FIELD receiver whose type a token sweep cannot read; routing that one call through locals makes the program compile, then reverted (`git status stdlib/` clean). ⇒ the still-open shape is FILED to PLAN.md and documented at the line a reader lands on, naming the carrier and forbidding the stdlib "fix"; the two closed shapes are pinned. Review also caught the shape→family rule written TWICE in two vocabularies — agreeing only by coincidence, and diverging them would make `calleeTakesAnyParamByRef` answer FALSE for a callee the graph marked by-reference, so a caller passes a value where the callee dereferences an address. Now one function (`ByRefNodeSpace.targetsOf`) both sides spend; the second name set is deleted. Pinned by a new shv2-only spec `byref-param-widening-and-guard.md`, **7 cases, every one pinning ```` ```exitcode ````** (the trap from `managed-field-store-order` two rows up), including a sabotage case proving the range guard still fires and a float arm. ⛔ **NO `build-cost-log.md` ROW**: that log is written by `spec-port-finish.sh`, which measures every column, and this landed by hand. |
| witness-managed-return | 2026-08-12 | PORTED | 2/2 | the HALT resolved by ruling: a durable store CO-OWNS (increfs) rather than moves, so an owned value may reach two sinks. The spec's subject — witness dispatch — was never at fault; the direct-call twin failed identically. One shared sink door, both double-move guards narrowed to the opaque case, 5 stale "shv2 is move-only" comments rewritten. Binding-to-binding moves deliberately unchanged. 11 shv2-authored expectations flipped and 2 shelved cases re-enabled in struct-enum-array-grow (its stated reason — "a consume-analysis divergence from the bootstrap" — was one of the things §3b says cannot exist), making that file byte-identical to /specs again. Opaque `T` doors still move: a shared body may not carry the descriptor holding retainFunc, so `reuse-moved-out-opaque-element` did NOT flip — the one place the concrete spelling is legal and the generic spelling is E3102, and a rung. No last-use-move optimization: the IR has no op-retraction door, so every durable store now costs an incref + scope decref. 746 goldens re-minted on the host lane. Suite 5561/0 after this tick. |
| free-function-from-method | 2026-08-12 | PORTED | 1/1 | ⚡ §7b FAST PATH — 1/1 on the first run, zero compiler source changed, so no reviewer and no full suite. NO SUITE FIGURE: none was measured on this path. §2's three counts run precisely BECAUSE it passed immediately — 1 marker == 1 ran, zero `disabled-test:`, no duplicate name, byte-identical to `/specs`, `status: stable` (not `draft`), and `## Tests` (line 14) is the LAST `## ` heading, so nothing was silently dropped. ⭐ **The leak gate is ARMED**: the case pins ```` ```exitcode 42 ````, not stdout — a leak exits 101, 101 ≠ 42, the case FAILS. (Contrast the stdout-only hole recorded two rows above, where a case passes *while* leaking because nothing checks the exit code.) ⭐ **And it tests its SUBJECT rather than passing beside it**: `doubled()` calls the module-level `double(self.value)` UNQUALIFIED from inside a type body, which is precisely the "must not assume every unqualified call inside a type body refers to a method of that type" rule the Documentation states, and 21 × 2 = 42 ASSERTS the answer rather than merely compiling. Landed on the tick immediately after the durable-store co-own ruling and untouched by it — the program owns no heap, so there is no durable store to co-own. |
| rethrow-stashed-self-field | 2026-08-12 | PORTED | 1/1 | ⚡ §7b FAST PATH — 1/1 on the first run, zero compiler source changed. NO SUITE FIGURE: none measured on this path. §2's counts run BECAUSE it passed immediately — 1 marker == 1 ran, zero `disabled-test:`, no duplicate name, byte-identical to `/specs`, `status: stable`, `## Tests` is the LAST `## ` heading. Leak gate ARMED (pins ```` ```exitcode 0 ````; a leak exits 101 ≠ 0). ⭐⭐ **BUT I READ THE IR BEFORE CREDITING THE GREEN, AND THE MECHANISM THE SPEC DESCRIBES DOES NOT EXIST HERE.** The Documentation's subject is a reload-after-call: *"the compiler caches the loaded heap pointer in a local at function entry… that local must be reloaded before any later use, otherwise the local dangles"*. **shv2 never stashes it.** `Holder.outer` is `callDirect Holder.inner` in `entry`, then in the `tryerr` block `loadRegBaseDisp rbx, [rbx + 0]` — the field is loaded **at the point of use, AFTER the mutating call**, so there is no entry-cached local to go stale and nothing for a reload pass to fix. ⇒ what this case actually pins is that shv2 does NOT hoist that load, which is a live REGRESSION PIN rather than a vacuous pass: hoisting it would read the stale `first` variant and return 1, failing the case. It also genuinely verifies the ownership transfer — `__mm_incref` on the field value before it becomes the thrown result, matched by `__mm_decref` on `r10` in `main`'s handler, with `__destruct_Holder` still releasing the field's own reference: balanced, no double free, no leak. ⚠ **FIFTH consecutive file in this family** (after `refcount-shared-borrow-root-phi-args`, `refcount-global-getter-forward`, `refcount-infallible-borrow-throw-edge`, `refcount-consumed-arg-copy-success-edge`) where a v1 refcount mechanism has no shv2 analogue and the spec passes on the ownership shape shv2 actually has. The day shv2 gains an entry-load cache for self fields, this case starts testing its title. |
| try-block | 2026-08-12 | PORTED | 33/33 | a spec that was really a RUNG — 28 of 33 red, and every refusal was a gap the block-form `try` build (`fa548fac5`) deliberately left, each naming its own missing mechanism. Four closed: the STANDALONE `otherwise` spelling (shv2 took only the attached `end 'work' otherwise (e) 'bad'`, and BOTH are canonical — `/specs/map-try-otherwise-block.md:93` uses the attached one and the ported copy pins it — so this is a widening); a routed bare `throw` via a value form of the error-flag encode lowered through the SAME `encodeScalarErrorFlag` as the terminator, forked on the identical gate the refusal used so the set that routes is exactly the set that was refused; TWO error types through one handler by fusing `(member, case)` onto one dense integer line; and five diagnostics. ⭐ **The departure is the thesis**: both references carry a discriminant beside the flag and each wrote a SECOND match engine, while fusing to one ordinal lets shv2 reuse `parseMatchCommon`'s interval-plan dispatch, carried-var phis and move reconciliation rather than duplicate them. ⛔⛔ **The review found a SILENT WRONG ANSWER and the header claiming safety is why it would have stayed invisible**: the union lookup was keyed on the BARE NAME of the `(e)` binding, so any shadowing binding hijacked the dispatch — measured 11 where the bootstrap oracle says 22, compiling clean; with non-colliding case names it failed loud (E3034), which is how the silent form was found. `ErrorUnionInfo`'s own header argued this could not happen ("no shared name space for a join to collide in" — true handler-vs-handler, FALSE for the handler's own scope): a lossy key as an identity wearing a comment that says it is safe. Fixed by keying on the bound `flag ValueId` so `lookupValue` decides; ⭐ E3084 deliberately stays NAME-keyed (syntactic "did the author write `match e`?") because the identity test there refused a program the oracle answers 22 for — both halves pinned in a new `try-block-shadowed-binding.md` (6/6) whose red-gate control reads 11 before / 22 after. Also fixed: `enumTagSpan` duplicated a walk one file over MINUS its zero-case guard (a panic), the routed edge's arg list built twice verbatim against a POSITIONAL block-arg list, and unguarded overflow reachable because `tagOfIndex` returns the IEEE-754 BIT PATTERN for a float-backed enum (~1.38e19). ⭐ **0 of 5612 goldens differ** — and that zero is load-bearing: giving `encodeScalarErrorFlag` a destination swapped the Std mint order inside `lowerThrowError`, which touches EVERY scalar throw, and moved x64 nowhere. Suite 5612/0 after this tick. |
| try-block-otherwise-divergent | 2026-08-13 | PORTED | 6/6 | the sequel to `try-block` — the two TERMINAL handler forms beside the block one: `otherwise [(binding)] panic(…)` and `otherwise [(binding)] throws E.case`, a single expression with no `end` of its own and an OPTIONAL binding. `finishTryBlock` needed NO change (the new path returns `TryHandler.terminated`, which its existing arm already carries) and the binding goes through the existing `declareCaughtError`. ⛔⛔ **THE REVIEW FOUND THIS TICK'S OWN HEADLINE CLAIM DESCRIBING DEAD CODE**: the first draft reported E3084's exemption as STRUCTURAL via a new `CaughtErrorForm.divergentHandler` arm in `tryBlockBindingToMatch` — but that routine has exactly ONE call site, inside `parseOtherwiseBlock`, which the divergent path never reaches, so the arm was unreachable and the ⚠ comment above it described a call that cannot happen. The exemption is real but lives elsewhere (`parseDivergentOtherwise` never calls `parseOtherwiseBlock`, so the two fields are never armed) and is now documented there; the third case is DELETED, its only live consumer having given it and `blockHandler` identical results. ⛔ **And the fork was COPIED, not shared** — `parseDivergentOtherwiseForm` duplicated the `throws`/`panic` dispatch already in `parseMatchCommon`'s `default` arm, while `divergentHandlerOpensAt` ("the two keywords, in one place") guarded only ADMISSION, so the pair was spelled in THREE places with nothing coupling the third. Now one `parseDivergingTerminalForm` admitted by that predicate everywhere; a third verbatim copy of the caught-error BINDER grammar was extracted too (shared with `parseCatchingElseBody`). ⭐ **A latent wrong value, MEASURED against the oracle and then refused**: the union cleanup runs before the form parses — safe for a block handler because E3084 forces the binding through a `match` whose arms re-enrol the box, but a divergent handler is the first form to bind a synthesized-union `(e)` with E3084 exempt, so on a boxed member's edge the binding is the numeric value of an already-freed pointer. The bootstrap prints `failed: 1` (the raw ordinal+bias flag), so there was nothing meaningful to match and shv2's would be strictly worse — unstable where the oracle's is deterministic. Refused with E2015 naming the gap and pointing at the two forms that work, which rejects nothing the spec requires and makes the cleanup-before-parse ordering SOUND rather than accidentally unobserved. ⭐ The extent scan needs no share of the keyword roster and that is a PROPERTY: `otherwiseOpensBlock` is a POSITIVE test for the block label, so every terminal form is excluded by construction — independently confirmed by review, and these two are exactly the pair whose disagreement is `assertScanAligned`'s panic on a correct program. 0 of 5618 goldens differ, including after the refactor reached `parseMatchCommon` and `parseCatchingElseBody`. Suite 5621/0 after this tick. |
| lazy-static | 2026-08-13 | PORTED | 12/12 | ⛔⛔ **THE FIRST PASS REACHED 12/12 WITHOUT THE FEATURE, AND THE SPEC COULD NOT TELL.** Four gaps closed, every case green — while shv2 still initialized statics EAGERLY in `__module_init`, against the spec's defining sentence *"evaluated the first time the static field is accessed"*. ⚠ All twelve canonical cases READ the static they declare, so eager and lazy are indistinguishable to every one (`initialized-once` prints `1 1 1 1`, which eager satisfies too — it also runs exactly once). The discriminator is a static NEVER accessed with a side-effecting initializer: **shv2 `1`, oracle `0`**. Now the RED-GATE CONTROL of a new 10-case shv2-only spec. ⚠ **A structural argument looked like a reason to keep eager and was not**: shv2 must know every global's SLOT TYPE whole-program before any file parses, so the references' re-parse-from-a-token-region strategy cannot type the slot — but typing and EVALUATING are independent; the slot is still typed eagerly from head tokens, only evaluation moved. The constraint binds the ADMITTED SET, not the timing. ⭐ **Out of line where both references test inline at every load** — one branch per STATIC, not per ACCESS; `/specs/lazy-static.md:353-361` exists because the inline shape looped endlessly on a second load in one function, and that failure mode is now unreachable by construction. **Guard = in-band sentinel** (the flag and the value cannot disagree because they are the same word), checked against EVERY admitted form: scalars never take the path, all five record producers are heap allocations, an empty container is a real record, wasm cannot place a valid address at 0, and the width worry is moot since `valueTagToStdType` collapses the tags to i64. **Recursion was reachable all along** — the "may not name another global" rule bounds the initializer EXPRESSION, never a callee's body — now a named panic where the bootstrap merely dies in `mm_incref called with NULL pointer`. ⭐⭐ **The managed-ownership finding is a DELETED PREMISE**: `buildGlobalCleanup` asserted "`__module_init` fills EVERY entry" and dismissed both references' null guards as artefacts — laziness makes that false, and an unaccessed managed static reaching cleanup at zero would crash on exit for a correct program. The scope boundary is pinned from BOTH sides and both halves are canonical (`specs/dead-top-level-var-elim.md:31` requires the eager half), so making all managed globals lazy would have broken the second. ⛔ **Review then found the GREEN-THREAD STACK GUARD being withheld from user code** — the exemption matches any `__`-prefixed pathless function, which held only because the two existing exempt families run OUTSIDE `main` where no green thread exists; split into a provenance question and a stack-behaviour question off a positive claim bit, pinned across a real stack-grow. Plus: cleanup guarded ONE of two sentinels; a PROVENANCE field (`ManagedGlobal.scope`) was being read as a BEHAVIOUR flag by three sites that agreed only by coincidence; and `$` in a synthesized name had ALREADY lost its one meaning before this tick (`assignDataLabels` was the second), so the join moved to `#`. ⚠ A write-only static runs its initializer — a SEMANTIC CHOICE the oracle cannot arbitrate, because the bootstrap ICEs on that program (`E9001: Unresolved global: Box.slot.__initialized`); recorded as a choice, and the bootstrap defect left alone as different work. Suite 5655/0 after this tick. |
| tuple-assign | 2026-08-13 | PORTED | 10/10 | shv2 could not parse the form at all (all ten `E2001: unexpected token: '('`). `(x, let y) = f()` is the DECLARING destructure with the per-position question widened — rebind / declare / discard — desugaring exactly as `let (x, y) = t` already did, RHS parsed ONCE into a hidden temp then one field-load per non-discard target. ⭐ **No ambiguity to resolve, and MEASURED not assumed**: shv2 admits NO statement opening with `(` today, so the predicate can only turn an E2001 into a program. ⚠ **The second caller was the load-bearing one** — `assignedBindingsIn` scans for `identifier =`, and in `(a, b) = step(a)` neither name is followed by `=`, so a loop-carried var would have minted no header phi and reset every trip: a wrong answer, not a diagnostic. ⭐⭐ **THE ORACLE CONTRADICTS ITSELF, so "does shv2 match it" has no single answer.** Wiring E3064 needed an effect analysis; a review called it a false rejection, I measured TWO spellings against the bootstrap, found agreement and called the finding refuted — **an unsound generalisation from too few shapes**. The third spelling diverges and is the one the corpus writes: bare `n = n + 1` in a method COMPILES on the bootstrap and was E3064 on shv2, while `self.n = …` and a write through a parameter are E3064 on BOTH. One write, two spellings, two verdicts ⇒ rows 2–3 are not evidence that refusing is right, they are evidence the bootstrap has a hole, and a check sampling only those rows reads as total agreement. shv2 now accepts all three deliberately — the safe direction for a REFUSING diagnostic. ⭐ **The cure is this rule's own column**: the first attempt borrowed `storageWrittenParamMask`, built for E3070's array-element question and deliberately narrowed to Array fields — a fact recorded for consumer A reused as the safety floor for consumer B, where A's narrowing is B's hole. Replaced by a bit set at `emitFieldWrite` (ONE site, not two) beside the parameter masks; the distinguishing line is `emitFieldWrite` vs `emitFieldStore`, since the raw store also fills a FRESH box and would have marked every value-building function, making the summary vacuous rather than conservative. **Two independent reviewers found the same duplication** without seeing each other — the loop-phi scan re-deriving the target walk under a comment claiming it did not. Also: the grammar accepted `let (x, y) = t` as a tuple ASSIGNMENT (fixed in the grammar, not at a call site); three readers of "does this statement write a name" and only one had been taught, so a by-ref parameter could not be a tuple-assign target where plain assignment compiled; twelve shelved markers swept in `discarded-results.md` — six enabled, six re-shelved with measured reasons, one revealing that shv2 counts a `throws` clause as an effect where canonical calls the function pure. Two new shv2-only specs pin what the canonical file structurally cannot see — two of its cases are BYTE-IDENTICAL programs, so the `var`-qualified target was exercised by nothing. 0 of 5680 goldens differ; scale allocation counts byte-identical; the one number that moved (+25,888 bytes at rung 0) is the new bool widening every `IrFunction` record and was reported rather than left to be found. Suite 5680/0 after this tick. |
| short-type | 2026-08-13 | PORTED | 8/8 | ⚡ NO COMPILER CHANGE — 7/8 on the first run, and the eighth failed on the SPEC HARNESS refusing the pin's notation, not on anything shv2 emitted (`the .rdata pin is malformed: '10 20 30' is not a valid u16`). ⭐ **The compiler was already right**: the program returns 60, measured directly. `/specs/short-type.md` pins ```` ```RequiredRdata u16 10 20 30 ```` — a type tag then SPACE-SEPARATED values — while shv2 takes everything after the first space as ONE value. ⚠ **ONE line in all of `/specs` uses that spelling**; every other multi-value pin writes `<type>[] v, v, v` (`i8[] -1, 0, 1`, `i16[] -200, 0, 200`, `u8[] 0, 1, 2`, …) and canonical itself writes `u16[] 100, 1000, 10000` elsewhere — 19 of 20. ⛔ **And v1 did not support it as a NOTATION either — it special-cased it for this file by name**: `SpecTestRunner.maxon`'s `if typeTag == "u16"` arm carries the comment *"Space-separated values to match the bootstrap (short-type.md)"*, while every other scalar tag there parses exactly one value and `u16[]` sits beside it as the array form. So it is a compatibility accommodation for a single line, applied to `u16` alone. ⇒ **USER RULING: correct the pin rather than import the accommodation.** ONE retraction, `u16 10 20 30` → `u16[] 10, 20, 30` — notation, not meaning: same three values, same width, same bytes, and the pin is now actually CHECKED (7 compared / 0 differ) where before it could not be read at all. The alternative was a second spelling for one type and no other — the asymmetric wart the harness's own type-roster comment already warns about. §2's counts run precisely because most of it passed immediately: 8 markers == 8 ran, zero `disabled-test:`, no duplicate name, `## Tests` the last `## ` heading, `status: active` not `draft`. NO SUITE FIGURE: none measured on this path. |
| panic-interpolation | 2026-08-13 | PORTED | 6/6 | the one red case was `panic("hex: {x:x}")` and **the failure was never about panic** — `print("hex: {x:x}\n")` fails identically at the identical position, so `{expr:spec}` FORMAT-SPEC INTERPOLATION was unimplemented in the PARSER, everywhere. ⭐ **The renderers already existed and were unreferenced** (`__int_toStringFormatted`, `__float_toStringPadded`, `__float_toStringFixed` complete in `stdlib/Builtins.maxon` with nothing calling them) — a wiring job, not a port. Lexed rather than parser-reassembled, and that departure from v1 is FORCED: an shv2 token whose kind implies its text carries `EmptySourceText`, so v1's concatenation of token values would lose the `.` in `.2`. One representation parsed once and carried as constants (`grep formatSpec` over the compiler was EMPTY before), one consumer, three call-emitting arms collapsed into one before a third could reproduce the bootstrap's own duplication. **Debt swept: 17 shelved cases → 3.** ⚠ **The float blocker was asserted on FIVE ALREADY-PASSING cases** — a blocker note on a green test is how a whole area stays believed-blocked. ⛔ **TWO CANONICAL CASES THE PORT HAD DROPPED, restored byte-identical** — they existed in `/specs` and NOWHERE in `specs-shv2/`, and they are precisely the two guarding this design's risks (a 200-byte field into a 72-byte scratch; the enum arm silently DROPPING the specifier). ⚠ **TWELVE more are still missing**, including an entire `union-*` family of ten, so union interpolation is untested there — left for its own tick. ⛔⛔ **THE REVIEW FOUND A SOURCE-DELETING BUG 5731 GREEN CASES COULD NOT SEE**: `scanInterpFormatSpec` had no newline bound — the ONLY byte-consuming scanner in the lexer that did not stop at `Ascii.lf` while every sibling does so deliberately — so it ran to the next `}` anywhere in the file. `print("a{x:")` on one line and `print("b{y}")` on the next COMPILED CLEAN and printed `a1`, the second statement gone, where the bootstrap refuses with E1006. Found by noticing the odd one out among siblings, not by any test, because no case has that typo. A second consequence recorded at the site: only `newline()` moves `line`, so a body that eats newlines mispositions every later diagnostic in the file. Fixed to the bootstrap's exact code, message and position, attribution checked first (the no-colon form was always refused correctly), and pinned with its measured pre-fix behaviour as a red-gate control. ⭐ **A duplication became a BUILD FAILURE rather than a comment**: `MaxFormatFieldWidth` now declares AT `FormatFieldWidth`, so narrowing the type without the constant fails on that line instead of turning an E2011 into a compiler panic; a third spelling of the same bound was deleted. The compiler/stdlib pair genuinely CANNOT be joined (both directions refused by the language, `E2010` and `E3003`; and they are separately compiled artifacts, so a shared alias would bake this build's bound into a call resolved against the user's stdlib) — the comment now names which direction breaks silently. The review also confirmed the `:`-at-depth-1 rule sound (all eleven colon consumers sit inside a bracket family) and the `f000000f` shape unable to recur, while catching one claim the code did not have — fixed in the CODE, not the comment, and eight width/precision/base shapes now match the oracle byte-for-byte. 0 of 5732 goldens differ. Suite 5740/0 after this tick. |
| match-jump-table | 2026-08-13 | PORTED | 8/8 | ⚡ §7b FAST PATH — 8/8 on the first run, zero compiler source changed, so no reviewer and no full suite. NO SUITE FIGURE: none measured on this path. §2's counts run precisely BECAUSE it passed immediately — 8 markers == 8 ran, zero `disabled-test:`, no duplicate name, byte-identical to `/specs`, `status: experimental` (not `draft`, so it runs), `## Tests` (line 19) the LAST `## ` heading. ⚠ **BUT ITS CASES CANNOT SEE ITS SUBJECT, so I measured it rather than crediting the green.** The file is named for a CODEGEN property — *"Enum match statements with 4 or more cases are optimized to use a jump table instead of a linear comparison chain… O(1) dispatch instead of O(n)"* — and it pins **7 `stdout` blocks and 1 `exitcode`, with no `RequiredIR` and no `.rdata`**. Every case would pass unchanged on a linear if-chain, so 8/8 is not evidence the optimization exists. Its sibling `match-dispatch.md` (ported, 22 cases) pins only exitcodes too. ⭐ **MEASURED: shv2 really does emit it** — the minted fragment for `four-cases-statement` carries `x64.jumpTable rax, default=matchdead [[0]=matcharm#1, [1]=matcharm#2, [2]=matcharm#3, [3]=matcharm#4] @__jumptable_8` and **zero `cmp` instructions**, so it is a genuine table rather than a chain. ⇒ **What actually pins the subject here is the committed GOLDEN, not the spec's assertions** — landing these 8 fragments is what makes a regression to a comparison chain show up, as golden drift, on the next run. Worth knowing which artifact carries the guarantee: for a codegen spec whose cases assert only behaviour, the fragment is the gate and the `stdout` is not. ⚠ Also inherited from canonical, not introduced: 7 of 8 cases pin `stdout` ALONE, and an unpinned exitcode means their leak gate is never armed. |
| match-expr-arm-divergent | 2026-08-13 | PORTED | 7/7 | 0/7 → 7/7, every case failing `E2010: Expected 'gives' but got 'panic'` — a match EXPRESSION arm may DIVERGE instead of giving a value, the divergent form replacing `gives` outright. ⭐ **The mechanism already existed and this is its FOURTH CALLER**: `try-block-otherwise-divergent` (`4d5bcf24b5`, the same session) built `divergentHandlerOpensAt` as the one keyword roster and `parseDivergingTerminalForm` as the one reader of the fork, so `parseArmBody`'s expression branch simply asks the roster before consuming `gives` — no new fork, no copy of the `default` arm's body, and the helper needed no extension. **+51/−10 in one file, and nothing downstream was missing**: the merge, the exhaustiveness check and the reader all already did the right thing; it was one unwired door. **Exhaustiveness needed nothing at all** — `collectEnumPatternIntervals` stamps the coverage vector FROM THE PATTERN, before the body is parsed, so a diverging arm had already covered its case. `reachesMerge` is COMPUTED rather than asserted false on purpose: a broken invariant then panics with a clear message instead of feeding the result phi a garbage operand. **Three censuses corrected and one RETIRED** — `TryBlockHandlerShape`'s enumeration became a POINTER to the roster rather than gaining a fourth entry, because a hand-updated list is the thing that goes stale and a reference cannot. ⛔ **A LEGAL PROGRAM PANICKED THE COMPILER**: a match expression whose every arm diverges hit `finalizeMatchMerge`'s panic and printed twenty frames, where the bootstrap compiles and runs it. Reachable before this tick via `default panic(…)`, so pre-existing — which changes nothing about whose job it is. Now a positioned E2015, reusing an existing code with the reuse argued (its registry doc is "a construct this compiler does not implement YET" and it is already listed shv2-only, `notEmittedBy: csharp, selfhosted`); E2001 and E3xxx were rejected because either would file a PENDING FEATURE as a permanent illegality. Pinned in a new shv2-only spec as a BOUNDED GAP not a language rule, with both doors covered and — deliberately — a GREEN CONTROL that the statement form the diagnostic recommends actually compiles, so the message cannot advise a rewrite nobody checked. ⚠ Finding, not fixed: **a compiler panic strands the tree lock for 60 s** (confirmed twice — two bootstrap invocations refused with `this checkout is BUSY`); the release point exists and is correctly shaped, but `panic()` aborts through the EMITTED RUNTIME's routine shared with every user program, so the clean cure is an OS-held lock the kernel drops on process death. 0 of 5759 goldens differ. Suite 5761/0 after this tick. |
| match-enum-or-pattern | 2026-08-13 | PORTED | 9/9 | 8/9 → 9/9. The red case was shv2's OWN refusal deferring to "a later rung" that had arrived. **Nothing downstream needed building** — the alternatives already branch to ONE arm body whose head already loads bindings from the shared box; the path was not wired to the door. ⛔⛔ **LIFTING THE REFUSAL EXPOSED THREE DEFECTS**, measured: a managed payload where a payload-less alternative also matches (`0xC0000005`), a CROSS-TYPE slot which was **SILENT — `speak=7078117`, a String pointer returned as an integer** — and two alternatives both binding one slot on a sole-owned scrutinee (`0xC0000005`). ⚠ **The BOOTSTRAP miscompiles the silent one too** (`speak=140699269836832`); both references allow all three with NO guard, so shv2 takes their mechanism and DEPARTS on the guard, precedent `rejectFallthroughIntoBindingArm`. ⛔⛔ **AND THE REVIEW THEN FOUND THAT GUARD HAD TWO HOLES OF ITS OWN — both shapes the deleted refusal used to block, so both NEWLY REACHABLE.** WRONG AXIS: it checked SLOT identity while `install` is keyed by NAME and overwrites, so `walk(dir) or run(_, dir)` compiled clean and answered `walk=0` where 7 was stored. WRONG PREDICATE: zero-inhabitability asked `payloadClassIsManaged`, documented as answering *"does this class own a heap drop?"* — so a `function`-typed payload classified scalar and zero became a NULL CODE POINTER, on a union shape **already in the corpus** (`first-class-functions.md:1712`): `idle or run(op) gives op(3)` printed `run=6` then `0xC0000005`. ⭐ Fixed as ONE rule read from its two ends — `declarePayloadBindings` walks a list indexed by SLOT and installs into a scope keyed by NAME, so `requireArmBindingsAreDistinct` checks both in one walk over the arm's finished list, which also closes a PRE-EXISTING `walk(x, x)` hole with no second copy (red-gated against the BOOTSTRAP, since this binary already carried the fix and could not red-gate itself). ⭐ And `zeroInhabitsPayloadType` FAILS CLOSED — an admit-list, not an exhaustive match, precisely so a future tag lands as a false reject rather than a null dereference; it caught a case nobody listed, an ENUM payload whose tags need not include 0. ⭐⭐ **"(zero here)" is a guarantee but NOT the one first claimed**: the load-bearing mechanism is `emitEnumBox`'s explicit zero-fill (*"THE ZERO-FILL IS MANDATORY even though the slab returns zeroed memory"*), not `__mm_alloc`'s zeroing — the draft cited the WEAKER guarantee, the one carrying the "a future free-list must re-zero" caveat, and had already propagated that into the error registry. Corrected in four artifacts, with a BACK-REFERENCE at the zero-fill loop so an author editing it learns a diagnostic now rests on it. Also: `intervalMark` was derived twice and is now passed in; `rejectFallthroughIntoBindingArm`'s premise was falsified by this change and now states why the refusal stays unconditional. 0 of 5770 goldens differ, suite re-run AFTER the two new refusals rather than only after the feature. Suite 5770/0 after this tick. |
| enum-hashable | 2026-08-13 | PORTED | 10/10 | a payload-free `enum` now conforms intrinsically to `Hashable`/`Equatable` with `int`'s impls (an enum value IS its i64 tag), so nothing is synthesized per enum. ONE retraction, ratified by `map.md` + `array-conditional-conformance-withheld.md`: shv2 reports both unsatisfied constraints where the bootstrap ABORTS at the first — measured, it drops a second independent bad instantiation entirely. The grant opened a WRONG ANSWER + leak no case could reach (an enum declaring its own `hash` was stamped into the witness slot unvalidated: `get=0` and exit 101 against the oracle's 20) — found by the pre-commit review, closed by E3130 asking the same `signatureMatches` a declared conformance is judged by. Widening the key roster also exposed a wrong answer at the `Set from [...]` literal door (`count=3, contains=false` vs `2/true`, an enum array packing narrower than `__set_from`'s 8-byte stride); that admission was reverted and its refusal now asks the canonical stride predicate. Suite 5794/0 after this tick. |
| generic-module-merge-pattern | 2026-08-13 | PORTED | 2/2 | ⚡ §7b FAST PATH — 2/2 on the first run, zero compiler source changed, so no reviewer and no full suite. NO SUITE FIGURE: none measured on this path. §2's counts run precisely BECAUSE it passed immediately — 2 markers == 2 ran, zero `disabled-test:`, no duplicate name, byte-identical to `/specs`, `status: experimental` (not `draft`), and `## Tests` (line 14) is the LAST `## ` heading, so nothing is below the active-test region. ⭐ **Checked against `match-jump-table`'s lesson — CAN THESE CASES SEE THEIR SUBJECT? Here, yes, and for a structural reason worth recording: this spec's subject is BEHAVIOUR, not codegen.** It claims to exercise "the full IrModule pattern from the self-hosted compiler" — a generic type with an INNER typealias (`typealias OpArray = Array with Op` inside `type GenModule uses Op`), an interface with an associated type (`interface Mergeable uses Func`), an `export extension Mergeable` supplying a default `merge` that iterates through seven interface requirements, and List iteration over a struct field. Every one of those is load-bearing for the printed numbers: `merge` must actually call `appendOps` + clone/push each function for `post: ops=3 funcs=2`, and `opRefSum=1` requires the `IntList` field to have retained both pushed indices (0+1). Case 2 pins that TWO specializations of one generic (`GenContainer with Integer`, `GenContainer with String`) coexist with distinct inner `OpArray` instantiations — `ints=3 sum=60` / `strings=2 count=2`. ⇒ Both cases pin `exitcode` AND `stdout`, so unlike the 7 stdout-only cases inherited in `match-jump-table` **the leak gate IS armed here** (an exit 101 would fail the pinned `0`). No mechanism was owed: P1.6/P1.7 generics, associated types and extension defaults all already carry this. |
| monomorphization-recursive-type | 2026-08-13 | PORTED | 2/2 | 1/2 → 2/2. The port itself was +37 lines in one file; the review then added a second false refusal and a duplication hoist, so the landed diff is `Parser.maxon` +75/-3 and `TypeRules.maxon` +27. The mechanism was complete and one DOOR was unwired: `typealias IntListArray = Array with IntList` + `.create()`/`.push()` already compiled and printed the right answer on this spec's own program; what a bare `[…]` could not do was NAME the element type. A `List with Integer` local carries `ValueTypeTag.genericInstance`, which `typeTagName` renders as `"struct"` while `tagMayNameAggregate` (`structRef or named`) refuses it — so the refusal listed "a struct" among the permitted elements while refusing an element it had just called `'struct'`. ⭐ The clean leak gate is EVIDENCE, not an assumption: sabotaging `arrayElementDestroyValue` to stamp the trivial destructor made the identical loop-scoped program exit 101, so the gate genuinely sees these boxes on this route (sabotage fully reverted). ⚠ The BOOTSTRAP is wrong on the neighbouring program — it accepts `[[1,2],[3,4]]` for `count()` then nil-faults in `__Array_i64.get` reading an element back; shv2 answers correctly. ⛔ LEFT OPEN, and it is a user call: the refusal's roster is FALSE — it omits a function value (unlisted since before this tick) and now a container. Rendering it from the arms is the cure, but the composed sentence is pinned byte-for-byte in two `maxoncstderr` blocks of the shv2-authored `character-ownership.md`, which this loop may not edit. ⭐ The review then found the SAME self-refuting shape one tag over and MEASUREMENT SETTLED IT BOTH WAYS: my first three probes compiled, because a DIRECT call's result is not `exitCode`-tagged — the tag is only acquired through `mintValueOfType` from a declared type. Through a real entrance (`typealias IntThunk = function() returns ExitCode`; `let arr = [t(), t()]`) it was a FALSE REFUSAL the bootstrap compiles, now fixed. ⇒ grep where a tag is ASSIGNED, not where it is tested. That fix would have been the THIRD copy of `integer or exitCode`, so it was hoisted to `tagIsPlainInteger` and both existing sites rewired. Suite 5798/0 after this tick. |
| leak-checker | 2026-08-13 | PORTED | 8/8 | ⚡ §7b FAST PATH — 8/8 on the first run, zero compiler source changed, so no reviewer and no full suite. NO SUITE FIGURE: none measured on this path. §2's counts run precisely BECAUSE it passed immediately — 8 markers == 8 ran, zero `disabled-test:`, no duplicate name, byte-identical to `/specs`, `status: experimental` (not `draft`), `## Tests` (line 28) the LAST `## ` heading. ⭐ **THE GREEN IS REAL AND THE GATE IS ARMED — checked, not assumed.** All **8 of 8 cases pin `exitcode`** (0, 0, 0, 0, 42, 42, 0, 0), so exit 101 fails them; this file does NOT carry the stdout-only hole that leaves the leak gate disarmed (contrast `match-jump-table`, 7 of 8 stdout-only). And the INSTRUMENT is independently proven live in this tree today: the `enum-hashable` tick measured exit **101** on a leaked String reaching a witness slot, and the `monomorphization-recursive-type` tick reproduced 101 deliberately by sabotaging `arrayElementDestroyValue` to stamp the trivial destructor. So a regression in any of these eight would actually be caught rather than passing quietly. ⚠ **BUT THE FILE DOES NOT TEST ITS OWN SUBJECT'S OUTPUT.** All eight are NO-LEAK regression pins (string scope, array scope, string return, exit-code preservation, `if let X = try map.get(K)` on a union-valued map, conditionally-used enum args, closure-returned managed results) — each a real MM bug someone paid for, using the leak checker as an INSTRUMENT. Nothing asserts the diagnostic the spec documents: `Leak detected: N allocation(s) not freed` appears exactly once in the file, in the Documentation prose (line 23), and in **zero** assertions. There is no positive case, so the message's wording and count are unpinned on every lane. ⚠ Also unpinned: the doc claims *"It does not change the program's exit code"*, which the corpus tests only in the NO-leak direction (`no-leak.exit-code-preserved` returns 42) — and CLAUDE.md's "exit code 101 means a memory leak was detected" is the leaking direction, which no case reaches. Inherited from canonical, not introduced; a spec may not be edited here, so recorded rather than fixed. |
| async-await | 2026-08-13 | PORTED | 47/47 | 2/47 → 47/47. A spec that was really a RUNG, taken as one per §7. Scoping HALTED first and the halt was right: five shv2-authored pins contradicted canonical (retracted separately, `c3af1d7757`, user ruling), and `trace-yield` was reported as needing multi-M — **I relayed that and it was FALSE**: v1's runner drops worker-lifecycle lines before comparing (`normalizeAsyncTraceStderr`), so no worker pool was needed. ⭐ **The headline blocker accounted for a MINORITY of the failures**: `requireAsyncResult` read the return tag PRE-RESOLUTION, so `returns Integer` over a ranged alias arrived as bare `named` — indistinguishable from a `String` — and was refused for a channel it fits exactly. That one wiring gap alone took 2 → 24. Built: managed-async ownership (a per-callee `__gt_release_<callee>(gt, resultConsumed)`, because a release is TYPE-DIRECTED — `decrefCalleeFor` routes to seven callees, so the approved bitmask design was REFUTED mid-build and replaced); `Promise with T` storage, handle stored UNBOXED (boxing makes `Array with IntPromise` managed and trips the deep-clone gate five cases iterate through, and cloning a promise is the double free E3100 exists to prevent); `await` as a statement; E3005/E3057 per canonical; file IO that genuinely parks; E3073 whose seeds are the RUNTIME PARK POINTS not v1's hand-written 30-name roster, so `File.exists` is DERIVED and nothing can rot; `p.cancel()`; and the AsyncTrace facility. ⛔ **THREE latent wrong answers found on the way**, none reachable before this work: an await result minted with the FILLER type id (id 0 is the first name the file interns, so a ranged-alias result typed itself as `FilePath`); `emitAwaitOp` never enrolling the result as an owned temp; and a timer store with no capacity guard writing past 256 entries. ⭐ **Runtime.yield() was added to BOTH compilers** (user ruling — stdlib will call it, and stdlib is shared, so the bootstrap must understand it or the oracle stops compiling). ⛔⛔ **AND THE BOOTSTRAP'S HALF WAS INERT FOR WORKER THREADS** — `__gt_enqueue` writes `P->runnext`, the FRONT, and `__gt_dequeue` reads runnext first, so the yielder re-selected itself: measured `spinnerSawFlag=0` against shv2's `1`. **The canonical spec's own "discriminating case" did not discriminate** — traced with the yield assumed completely inert it still produced the pinned value, because the interleaving came from spawn order plus runnext displacement. Fixed with a back-of-queue path (Go's `globrunqput` line) and the case repaired, then SABOTAGE-VERIFIED to fail against the real defect rather than merely against total inertness. Fourteen sabotage controls across the tick. Suite 5849/0 (shv2) + 3361/0 (bootstrap); 151 host goldens re-minted; the other three lanes owe theirs. |
| reference-identity | 2026-08-13 | PORTED | 11/11 | 1/12 → 11/11, canonical 12 minus ONE case retracted under a direct user ruling. Two gaps, neither a bug in an existing mechanism: `is`/`is not` did not exist as an expression operator (`TokenKind.is` was already lexed for `where T is Hashable`, but `infixPrecedence` said `NotAnInfixOperator`, so the climber stopped and the LEFT OPERAND'S OWN TYPE reached the condition — hence ten identical `'if' requires a bool condition, got 'struct'`); and `.clone()` on a user struct resolved to a callee no file declares, where the E2015 about `'unknown'` was MASKING the real `E3004 call to undefined function 'Point.clone'`. shv2 already had the whole cloner (`synthesizeStructCloner`), reachable only as an array element-cloner stamp with no door from source — so ~25 lines against both references' ~200-line synthesis passes. ⭐ Literal interning already worked and `is` only made it observable (`registerRdataConstant` dedupes by bytes); `is` compares the FUSED RECORD not `buffer@0`, because two records legitimately share one buffer and a buffer compare would call a substring and its source one object. ⛔⛔ **THE REVIEW CAUGHT A COMPILER PANIC AND I REPRODUCED IT**: `infixPrecedence` has TWO consumers and only `parseBinary` got the new arm — `let X = 1 is 2` panicked `evalConstBinary` → `foldConstBinOp` → `infixMaxonOp`, whose message names the broken invariant verbatim (*"infixPrecedence must gate this call"*). Reachable from any top-level initializer; the same diff had added a header claiming `is` was only reachable after an operand "which a declaration header has none of". ⛔⛔ **AND THE CLONE DOOR SILENTLY ALIASED A GENERIC'S TYPE PARAMETER** — `Box with String`.clone() passed the gate (because `typeIsManaged` is false for a `typeParameter` slot), stamped the result as the BARE BASE, and blitted the `T` word un-retained: a second owner of a refcount-1 record, at **exit 0, no leak, no panic**, because the gate and the cloner AGREED ON THE SAME WRONG ANSWER. It surfaced only because the `is` this tick added compares `valueNameId`s. ⛔ THE RETRACTION: canonical pins `mutation-through-alias` expecting a write through `var b = a` to be visible through `a`; the 2026-08-04 ownership ruling makes that a MOVE precisely because "a second name could watch the value change". User ruled the ownership model wins (2026-08-13). ⚠ This leaves canonical `/specs` asserting an aliasing rule the language as ruled does not have — upstream of this loop, and it will not reconcile itself. Suite 5871/0. |
| stack-promotion | 2026-08-14 | PORTED | 6/6 | ⛔⛔ **PASSED 6/6 ON THE FIRST RUN AGAINST AN OPTIMIZATION THAT DID NOT EXIST**, and the goldens proved it: `stack-local-primitive-struct`, whose own prose says *"Must produce NO mm_alloc for Point"*, emitted `movRegImm32 rcx, 16` + `callDirect __mm_alloc` — identical to the cases that are supposed to heap-allocate. Every case pins only an `exitcode`; the `MmTrace: true` its Documentation claims to use appears in NO case, and shv2's `SpecParser` does not know that marker anyway. User ruled: build it. ⭐ **NEITHER REFERENCE HAD THE ANSWER** — v1 lists this spec DEFERRED, and the bootstrap's `StackPromotionAnalysisPass` is **DEAD CODE**: it keys on a struct literal bound in the same function, which E3076 forbids outside the type's own methods, so it can never fire for a user struct (measured: the bootstrap emits `mm_alloc` for all six). ⇒ The factory is not a style choice but a LANGUAGE-MANDATED indirection, so seeing through it IS the feature: the pass recognizes a record constructor (one block, one `__mm_alloc(constSize, 0)`, each field stored once from its own params/constants, `ret box`) and rebuilds it at the CALL SITE, reusing the call's own result id so no downstream op is rewritten. One hop — `heap-when-returned` still allocates because `makePoint` forwards rather than allocates. Admit-list FAILS CLOSED, with `collectStdOpUses` (the dialect's one oracle) answering "does anything else read this", not a second enumeration. ⭐ **A HAZARD NOTHING NAMED**: `__gt_morestack` grows a green thread's stack by COPYING it and freeing the old pages, rebasing only rsp and the rbp chain — and `leaRegSlot` is the first op in shv2 to put a frame address in a REGISTER, which nothing enumerates to fix. Gated off under `usesGt`; not theoretical — `deep(400)` SEGFAULTS without it. ⛔ **THE SPEC COULD NOT FAIL IF THE PASS REGRESSED**, so `specs-shv2/stack-promotion-escape.md` was authored (shv2-authored, 9 cases, all exit-code-pinned), **each validated by REMOVING the rule it guards**: 9 of 9 fail under sabotage (0xC0000005 ×4, exit 101 ×3, a segfault, and one SILENT WRONG ANSWER — 19 where 18 is right, a callee's `__mm_retain` writing a refcount into the neighbouring record's payload). ⚠ **TWO of those cases were only ACCIDENTALLY correct on the first cut** — the module-level `var` needed a spilling call between store and read, and the green-thread case needed `deep(400)`; both now document why. **One route was DROPPED rather than shipped unfalsifiable** (closure capture: shv2 already refuses a capturing closure that escapes, so the refusal is belt-and-braces over another subsystem's rule). ⭐ **A LATENT MISCOMPILE fixed on the way, unrelated and reachable today**: `stdOpDefinedValue` reported only a two-result op's PRIMARY def, so `nextFreeValueId` could hand back an id a `tryCall` already defines as its error flag — `InsertRangeChecks` mints from exactly that number, so its first guard value could collide with a live value, no diagnostic anywhere. Review fixed the dangerous duplication (candidate set from one ops walk, escape verdict from another — a disagreement promotes an escaping record with no compile error at either site), an ELEVENTH hand-rolled name→index map with a THIRD tie-break policy, and two stale claims. Scale: linear, ×1.57→×1.95 on a doubling ladder against the corpus's own ×1.97; 2.15% of allocations, ~1% of CPU. Suite 5886/0, 5886 goldens compared 0 differ; 328 host goldens re-minted. |
| optimizer-refcount | 2026-08-17 | PORTED | 9/9 | needed one predicate: `reassignBindingValue`'s `oldValue == value` self-rebind guard elided an OWNERSHIP HAND-BACK whole. A move re-homes a ValueId rather than minting one, so `var b = a` gives `b` exactly `a`'s id and `a = b` satisfied the identity test — leaving `a` poisoned and `b` live, a statement with no effect. Gated it on `not binding.movedFrom`. USER RULING this tick: a hand-back is a move like any other, so the assignment TARGET survives and the SOURCE is poisoned; `var-reassign-drops.md`'s `self-assign` case had encoded the elision as its expectation (it read the source) and was corrected with its prose and golden. Its golden drift was exactly the one changed source line — zero emitted-code movement. Also dropped the two `RequiredIR:<target>` blocks, per the `vector.md` precedent. Suite 5969/0 after this tick. |
| array-cursor | 2026-08-18 | PORTED | 15/15 | byte-identical; the gap was that an interface EXTENSION reached only the conformers whose `implements` clause NAMED it, never those reaching it through `extends`. `stdlib/Interfaces.maxon` declares `advanceBy` on `Iterator` and `retreatBy` on `BidirectionalIterator extends Iterator`, so `ArrayIterator implements BidirectionalIterator` had the second and not the first — two methods written ten lines apart in one file, one working. `ProgramSignatures.conformersOf` now asks `typeDeclaresInterface`, which already owned the transitive walk. Review found the fix had duplicated that walk's list-level half; unified onto ONE home, which also retired a per-name allocation on the pre-existing path. Suite 5984/0 after this tick. |
| list | 2026-08-18 | PORTED | 32/32 | byte-identical; needed SIX `List` methods the builtin refused as "later slices" — last/isEmpty/insert/remove/removeLast/clear (clone is called by no case and stays refused). Three of the 19 red cases were `maxoncstderr` pins on E3070 that exposed a SECOND defect: `dispatchListMethod` passed `receiverStorage: NoParamPositions` under a premise copied from the String/Set/Map dispatchers — "a list write cannot free an ARRAY element" — which is false for a surface that hands out borrows, so no list method ever set a bit the cross-function borrow check reads. `removeFirst` never had that half either; a borrowed `first()` across a callee's `removeFirst` compiled clean and ran to 0xC0000005. Review then caught the new `insert` LEAKING its moved-in element on the out-of-bounds path (exit 101), invisible to all four `insert` cases because they use `int` elements; fixed by parameterising the existing `emitDestroyRejectedElement` rather than copying it, and pinned by a new shv2-authored spec. Suite 5984 -> 6019. Suite 6019/0 after this tick. |
