# Optimization log

What the compiler's cost has actually done, change by change. **This is `scale-test`'s deliverable.**

**Read the tables downwards.** Each row is one recorded change, dated. A column *is* its history, so
the progression is the thing you see rather than something you have to reconstruct.

`scale-test` renders **no verdict**. There is nothing to pass, no golden to accept, and no light to
turn green — it is an instrument, and this document is what it is an instrument *for*. The last row
of each table below is the previous datapoint: every run reads it and reports the delta from it, so
you still see exactly what moved, without anything failing.

⚠ **When a row's headline was measured on a HAND-BUILT ladder rather than the corpus, the ladder must
be committed, not described.** `scale-test`'s corpus is a list of shapes someone thought to generate,
so most optimizer passes end up building one to answer a question the corpus cannot express — and
every one that threw it away left a number nobody could re-run. The Wave 2 rung reported a base
column that came out **~60× different** when someone rebuilt the ladder: it had measured a different
program, and there was nothing to check that against. **The generators live in
`maxon-shv2/Testing/ladders/` — add yours there and name it in your row.**

## How rows get here

Rows are **appended by the tool, not by hand**. Running

```
maxon-shv2 scale-test --note="<what changed, and why the numbers moved>"
```

records this run as a new dated row in both tables. **A run without `--note` records nothing** —
scale-test is run dozens of times a day against dirty trees and abandoned experiments, and a log of
all of them would be a log nobody reads.

**`--note` is an instruction, not a request: if you pass one, the row is written.** Even if the
numbers are identical to the row above it. The run will tell you plainly that nothing moved, and then
record it anyway, because you are the one who knows whether it is a datapoint and the instrument is
not.

The `--note` is not a formality. The instrument can see exactly *what* moved and can never see *why*,
and six months from now a number with no reason attached is worth almost nothing. So the reason is
demanded at the one moment it is still known: from you, now. See
[`maxon-shv2/Testing/ScaleHistory.maxon`](../maxon-shv2/Testing/ScaleHistory.maxon).

## Reading the numbers

The rungs are generated programs, each **double** the last, so rung 5 is 32× rung 0. Rung 0 shows
constant-factor wins; only rung 5 shows whether a change bent the curve.

**And because the ladder doubles, the RATIO between two rungs IS the growth.** Divide a rung by the
one before it: **2× is linear, 4× is quadratic**. That is the whole method — there is nothing fitted,
no exponent, no residual, and no threshold for anyone to argue about. `scale-test` prints that ratio
beside every phase; these tables carry the raw counts it is taken from.

| what | how much to trust it |
| --- | --- |
| **allocations, bytes** (per rung) | **Exact and bit-for-bit reproducible.** The same source through the same compiler makes exactly the same allocations, on an idle machine or a loaded one. A number here that moved, moved for a *reason*. |
| **the rung-over-rung ratio** | **Exact**, for the same reason: it is a division of two numbers that cannot move. |
| **CPU ticks** (per rung) | **MEASURED: median 1.7%, worst 6.3%, across three idle runs — for phases above 50M ticks, which are the ones a ratio is read from.** Small phases are much noisier (`phase:runtimeAugment` at ~100K ticks swung 28.6%); do not read a growth ratio off one. **A movement inside the band is NOT a datapoint.** Against the question the ladder asks — ×2 linear vs ×4 quadratic — 6% has a 100% margin. Rows are the MINIMUM of `--repeat` compiles. |
| **the CPU rung-over-rung RATIO** | **The durable part, and it survives a busy box.** Absolute ticks do NOT: a fully CPU-loaded machine inflated every rung by **+49% to +52%** (cache and SMT contention — this counter ignores preemption, not contention). But it inflated them *uniformly*, so the growth reading came through nearly intact: **loaded ×1.90/×1.98 against idle ×1.86/×2.00.** Compare ratios across dates and boxes; compare absolutes only within one machine's own quiet runs. |
| **wall time** | **Not measured, and never will be here.** It is machine-dependent in the way that matters: it counts every *other* process too, so a dated column of it would compare a loaded box in July against an idle one in August. Measured: one run read `phase:parse` at ×5.03 then ×1.78 across a doubling ladder — preemption, not a curve. For the milliseconds of one compile, use the compiler's own `--log=compiler:debug`, which now prints a `cpu%` column beside the wall `%`; a phase where the two disagree spent its wall time not running. |

These are the compiler's own allocation counts and byte volumes while compiling that rung — **not**
peak resident memory, which nothing currently measures.

> ### ⚠ THREE READING RULES. The first two are the 2026-07-14 Go-growth / raw-counter change; the third is about the CORPUS:
>
> **1. ×2.1 — not ×2.00 — is the byte column's LINEAR reading.** `Array` grows by Go's `nextslicecap`
> (double below 256 elements, then ease toward 1.25×), and that ratio is a *function of the buffer's
> size*, so the bytes a buffer allocates over its life are a *rising* multiple of its final size (2.0 at
> the threshold, up to 5.0 as the ratio approaches 1.25). The ladder doubles the program each rung, so
> that multiple climbs each rung, and **a phase whose bytes are perfectly linear reads above ×2.00 while
> it climbs.** It saturates at ×5 — a transient bend, not a superlinearity. So a byte reading around
> ×2.1 is this policy; well past it is still a quadratic. See `grownCapacity` in `stdlib/Array.maxon`.
>
> **2. The `allocs` column counts the RAW allocator layer, and older rows do not.** Every row on or below
> the "three changes rebased onto…" row counts raw+tracked allocations; every row above counts only
> tracked (structs, handles, Strings), so array/string *buffers* were invisible to it. The two are not
> comparable — the one-time step up (~1.06M at rung 5) is the instrument gaining sight of allocations
> that were always there, not the compiler doing more.
>
> **3. THE CORPUS IS VERSIONED TOO, AND A ROW THAT CHANGED IT IS AN EPOCH BOUNDARY, NOT A DATAPOINT.**
> Every number here is *this compiler compiling that corpus*, and `ScaleCorpus.maxon` is a source file
> like any other. When it gains a knob or retunes one the generated programs get bigger, so **every
> column moves for a reason that has nothing to do with the compiler**: the delta on that row is
> meaningless, and nothing above it is comparable with anything below it. The `change` column is the
> **only** mark there is — the tool cannot know a knob moved — so such rows say so themselves, in
> words like "CORPUS CHANGED", "CORPUS INSTRUMENT CHANGE", "CORPUS BLIND SPOTS CLOSED", "CORPUS
> RETUNE". ⚠ **The most recent boundary is the easiest to miss**: `2026-07-25` *"CORPUS RETUNE, not a
> compiler change: FloatsLivePerSpillLoop 4 -> 12"* opens with prose instead of the usual banner, yet
> it is a full boundary — rung 5 goes 16,020,150 → 16,223,032 across two rows whose compiler is
> byte-identical.

Frees are measured and reported but not tabulated here: they track allocations almost exactly, so a
third table would be a near-duplicate paid for in width. They are in `--result-json` for anyone who
wants them.

Dates are UTC. The columns assume a six-rung ladder; changing the ladder's length changes what the
numbers mean, so the tool refuses to append a row of a different width rather than make the tables
ragged.

## Allocations

| date | change | rung 0 | rung 1 | rung 2 | rung 3 | rung 4 | rung 5 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 2026-07-13 | *(baseline)* the scaling suite is introduced | 366,957 | 773,790 | 1,742,398 | 4,296,568 | 11,872,878 | 36,897,948 |
| 2026-07-13 | a name is a slice of the source, not a heap String | 316,018 | 644,760 | 1,369,342 | 3,087,700 | 7,601,346 | 20,936,928 |
| 2026-07-13 | a byte string literal IS a ByteArray, and may be a global | 315,808 | 644,350 | 1,368,532 | 3,086,090 | 7,598,136 | 20,930,518 |
| 2026-07-13 | for-in over an Array is an index counter, decided at lowering | 281,775 | 575,590 | 1,226,720 | 2,783,774 | 6,917,212 | 19,261,978 |
| 2026-07-14 | Rebase onto the rewritten main: the ladder now sees the for-in index-counter lowering and the compiler-traces-itself work at the same time. Allocs and frees are exactly the for-in commit's; bytes are its numbers plus the constant +8/rung that origin already accepted, and the two compose with no interaction. | 281,775 | 575,590 | 1,226,720 | 2,783,774 | 6,917,212 | 19,261,978 |
| 2026-07-14 | try_call on an associated-value enum no longer allocates a placeholder EnumDummy: null already means absent, and scope cleanup was already null-guarded, so the dummy was allocated, increffed, decreffed and freed on every successful call without ever being read | 224,996 | 456,886 | 966,324 | 2,168,570 | 5,306,696 | 14,518,054 |
| 2026-07-14 | P1.0d block scoping: the parser now pushes and pops a real Scope per if/while body — pushScope/popScope existed, were correct, and were never called, so a let inside an if leaked to the function frame. Two Scope containers per function  | 225,935 | 458,593 | 969,351 | 2,173,373 | 5,311,595 | 14,509,321 |
| 2026-07-14 | scale-test stopped measuring its own residue: the runner wrote each rung's executable and metrics.tsv INTO the rung source directory, which loadProject enumerates before filtering to .maxon, so every run after the first compiled two extra directory entries per rung (+26 allocs, +945 bytes). Outputs now live in the corpus root and stale files are pruned, so a cold and a warm .scale-tmp now agree exactly. The COMPILER is unchanged; every earlier row in these tables was measured warm and is offset by that constant. | 225,909 | 458,567 | 969,325 | 2,173,347 | 5,311,569 | 14,509,295 |
| 2026-07-14 | elimTrivialBlockArgs re-swept the WHOLE FUNCTION to a fixpoint, and the sweep count was the length of the fold chain, so the pass was QUADRATIC: collectReadyArgs deferred any phi whose fold target was also folding that sweep, which peels exactly one link per O(function) sweep. A run of N sequential loops holding one idle var builds a chain of ~2N (loop L's header phi for the var is fed by loop L-1's EXIT phi for it), and the pass measured x3.95 allocs / x4.85 bytes / x4.23 time per DOUBLING -- folding a LINEAR 2N phis with quadratic work, which is the tell. It is now a WORKLIST: when a phi folds, only the phis it FEEDS can have changed, and a reverse (incoming -> arg) CSR names them in O(1); chains resolve through a path-compressed union-find instead of one sweep per link. On that shape at N of 128: 798,403 allocations fall to 6,090 (131x) and 107.5ms to 1.8ms (60x), growth x3.95 to x1.98 (linear), folding the identical 256 phis. (This note was TRUNCATED when written: CommandLine.optionValue cut every option value at its SECOND "=", so "N=128" ended the sentence. Repaired, and the stdlib bug fixed.) | 228,020 | 462,434 | 976,672 | 2,187,610 | 5,339,644 | 14,564,974 |
| 2026-07-14 | P1.0d.2: short-circuit and/or, word-form bitwise, char literals — plus the trivial-phi pass rewritten from an O(chain-length x IR) fixpoint (each whole-function sweep peeled exactly ONE link of the fold chain) to a worklist + path-compressed union-find whose trigger slots MIGRATE as folds re-point reads. On 128 sequential loops: 798,403 -> 6,090 allocations and x3.95 -> x1.98 growth, folding the identical 256 phis. elimTrivialBlockArgs is now x1.96 (linear). The rung itself adds a shrinking constant (+1.0% allocs at rung 0 falling to +0.4% at rung 5); short-circuit mints one phi and takes no mutable-var snapshot, so it does NOT multiply the parser's known O(V.B). | 228,016 | 462,430 | 976,668 | 2,187,606 | 5,339,640 | 14,564,970 |
| 2026-07-14 | P1.0d.3: bool/int type discipline — ONE agreement rule (TypeRules.typesAgree) now gates arithmetic, shifts, unary minus, comparisons, if/while conditions, REASSIGNMENT, return values and call arguments, where before only and/or/xor had one. So `4 + flag` compiled to 5, `4 * flag` to 4, `flag shl 4` to 16, `takeInt(flag)` put a bool in an int parameter, and `if 4` branched on nonzero — wrong ANSWERS, not crashes. Memory moved and is attributed EXACTLY, at all six rungs: bytes = 16 x functionCount + 56. The 16/function is IrFunction gaining ONE pointer field (`valueTags`, the parser's per-value type column, PUBLISHED rather than dropped so SemanticCheck can check a call argument against a callee that may be in another file), which rounds the object up one 16-byte slab size class. The flat +56 bytes / +2 allocs is the entry stub's empty ValueTypeTagArray (header + buffer) in createFromStd, once per program. The per-value TAG DATA COSTS NOTHING: the parser already allocated and filled exactly that array per function and threw it away at each function's end; it is now handed to IrFunction.create by reference. Allocations are FLAT (+2) across all six rungs — the type oracle does not scale with the program. | 228,018 | 462,432 | 976,670 | 2,187,608 | 5,339,642 | 14,564,972 |
| 2026-07-14 | The splitter's driver is INCREMENTAL: it re-derives only the dirty region a split touched, where it used to rebuild liveness and re-sweep the whole function's pressure after EVERY split (O(function x splits)). Loop i's accumulators are live only inside loop i, so a spill in one loop cannot change the pressure in any other -- the old driver re-read all 64 loops after every one of ~320 splits anyway. regalloc:liveness grows x2.04 per doubling where it grew x3.69; regalloc allocations x2.00; the whole compile x2.02 allocs and x2.07 bytes. LINEAR. Enabled by block-relative op sequence numbers, per-block editable live sets and an editable use index; the byte-exact specs-shv2 goldens do not move, so every decision is identical. THE BYTE COLUMN CAUGHT WHAT THE ALLOC COLUMN COULD NOT: the first cut grew value-indexed columns with Array.resize, which reserves EXACTLY the length asked, so every split reallocated and copied all eleven of them in full -- 608 MB in splitting alone, growing x6.75 per doubling, at a low and innocent-looking allocation COUNT. They double now: 865 MB -> 272 MB total | 165,462 | 324,326 | 642,918 | 1,283,572 | 2,578,086 | 5,218,960 |
| 2026-07-14 | Flags metadata unified: StdOp.const now declares clobbersFlags:FALSE (it lowers only to movRegImm32/movRegImm -- a bare `mov`, which writes no EFLAGS), and TargetOpMeta.setsFlags is DELETED (written 40x, read 0x, and it CONTRADICTED the Std tier -- a v1 port artifact: v1 fuses compare/branch at the MIR->X64 boundary and really does read setsFlags off a target op, but shv2 moved that scan up to the Std tier and kept the field anyway). The false clobbersFlags broke compare/branch fusion on `if a > 0 and b > 0`, because a short-circuit's seed literal sits between the compare and its branch: the backward scan stopped at the `mov` and refused to fuse. Fixed, the fusion crosses it and each short-circuit site drops FOUR instructions -- setcc, a redundant cmp, AND the phi copy (the seed is now minted straight into the phi's register), plus one fewer live register. MEMORY MOVED BY EXACTLY ZERO, ON EVERY RUNG, AND THAT IS THE POINT WORTH RECORDING: TargetOpMeta is per-VARIANT static metadata, never heap, so dropping a field from it costs nothing -- but the deeper reason is that ScaleCorpus CONTAINS NO `and`/`or` AT ALL, so a codegen change to the commonest conditional shape in the language is entirely INVISIBLE to this instrument. A second instance of the corpus-blindness recorded as OPEN.md #5 (whose known case is O(V.B) SSA hidden by longFunction mutating ONE accumulator). The corpus must grow short-circuit conditions. | 165,462 | 324,326 | 642,918 | 1,283,572 | 2,578,086 | 5,218,960 |
| 2026-07-14 | CORPUS CHANGED -- ⚠ THE DELTA COLUMN ON THIS ROW IS MEANINGLESS. The +26% is a BIGGER CORPUS, not a slower compiler; this row is the NEW BASELINE and nothing above it is comparable. ScaleCorpus was BLIND in the one dimension that mattered. The parser builds SSA on the fly at a cost of O(vars-in-scope) per branch -- each `if` SNAPSHOTS the whole mutable-var set, each `while` MINTS A PHI PER VAR IN SCOPE -- so the true cost is O(V x B). But `longFunction` and `deepBlocks` each declared ONE accumulator and mutated it in every branch: V=1, O(V x B) collapsed to O(B), and the knobs NAMED for growing branches could not see the cost of growing branches. Per user directive, FIX THE CORPUS'S REALISM, NOT THE PROBE: both knobs now carry SIX mutable locals (a fixed, realistic count -- real functions carry a handful) and each branch mutates a ROTATING SUBSET, never all of them, because a branch that writes every local makes "cost O(vars in scope)" and "cost O(vars assigned)" the same number and the corpus cannot tell apart the two things that decide whether a write-trail is worth building. deepBlocks' two arms now write DIFFERENT locals, so the merge does real work instead of the trivial one-phi case. Also: every guard is now a SHORT-CIRCUIT `and`, because the corpus contained `and`/`or` EXACTLY NOWHERE -- found the hard way when the `const` flags fix moved zero memory. WHAT IT NOW SEES, and it is the defect OPEN.md #5 predicted: pruneDeadBlockArgs ALLOCATIONS grow x1.98 x1.99 x1.995 x1.997 x1.999 -- dead linear -- while its BYTES grow x2.00 x2.02 x2.04 x2.09 x2.17. Allocation COUNT linear while BYTES bend means the allocations are getting BIGGER: the dense columns sized by `blockArgIdBound`, which is superlinear because a ValueId is minted for every phi the parser mints, INCLUDING the ones later pruned. phase:parse bends the same way (x1.97 -> x2.06). THE BYTE COLUMN CAUGHT WHAT THE ALLOC COLUMN COULD NOT, again. The write-trail fix lands next, against this baseline. specs-shv2 239/0. | 209,028 | 410,568 | 814,512 | 1,625,870 | 3,261,792 | 6,585,482 |
| 2026-07-14 | Loop phis: a loop now carries a phi for the vars its extent ASSIGNS, not every var in scope. `parseWhileStatement` minted a header phi for EVERY mutable var in scope, unconditionally, before the body was parsed -- so it could not know which vars the body touches. A function's `var`s accumulate, so the Nth loop minted ~N phis and burned a ValueId on each, INCLUDING every one pruneDeadBlockArgs later deleted. blockArgIdBound (and every dense column sized by it) is O(ValueIds), so phis nobody kept were paid for in BYTES by every pass in between. THAT is why the ALLOC column stayed dead linear while the BYTE column bent: the allocations were not more numerous, they were BIGGER. Fixed by reading the loop's assigned names off the TOKENS up front (namesAssignedIn / loopBodyEndIndex). ⚠ THE DESIGN TRAP, RECORDED BECAUSE I PROPOSED IT AND IT IS UNSOUND: minting the phi LAZILY on a var's first mention CANNOT WORK -- parseIfStatement snapshots the mutable-var set into local ValueId arrays on the Maxon CALL STACK, and a phi minted lazily after such a snapshot retroactively changes the value that was live where it was taken, with nothing able to reach in and patch it (`while c / if d / x = 2` then returns 1, not 2). EAGER minting is what makes those snapshots true, and once minting is eager the criterion is ASSIGNMENTS, not mentions -- a var the loop never assigns keeps its pre-loop value, whose definition DOMINATES the header, so a read binds it correctly with no phi at all. GOLDENS DID NOT MOVE, and that is the proof the whole cost was intermediate: pruneDeadBlockArgs and elimTrivialBlockArgs were already deleting precisely this surplus, so the emitted code was always identical -- the ValueIds were minted, carried through four passes inflating every dense column, and then thrown away. Eight sequential pressured loops minted 332 header phis of which 260 were surplus; they now mint exactly 72. specs-shv2 242/0, no E5001 (fewer dead phis means LOWER maxlive -- this is the anti-E5001 direction). | 208,898 | 410,250 | 813,602 | 1,622,912 | 3,251,282 | 6,546,044 |
| 2026-07-14 | CROSS-FILE SIGNATURE INDEX -- and it is a NET REDUCTION, because a whole-program query placed UNDER a per-file one turned two latent QUADRATICS into visible ones. (Re-measured after rebasing onto the loop-phi change; the two fixes are in the QUERY SPINE and that one is in the parser's SSA construction, so they compose -- the rung-5 alloc delta is -91,209 on both bases, bit for bit.) WHAT WAS ADDED: `queryProgramSignatures`, one token sweep of every file folded into one table, so a call's result type is exact across files (it was `unresolved`, which AGREES WITH EVERYTHING, so `isReady()` from another file added to 41 and returned 42). Its own phase, LINEAR: allocs x1.91, bytes x1.93. WHAT IT EXPOSED, and neither was mine: (1) `clearDepsFor` rebuilt a FLAT EDGE ARRAY THAT NOTHING EVER READ -- it scanned every dependency edge in the project and rendered each edge's dependent to a fresh String just to compare it -- so one clear per file over a graph with a few edges per file allocated O(files^2) Strings. The flat array and the reverse index were the same fact written down twice, and only the index was read. Deleted the array; `clearDepsFor` is now O(1). (2) `compositeSourceHash` re-folded EVERY file's hash on EVERY call, and every `queryParseOps` must now validate the signature index before it can validate itself, so it went from 2 calls to files+2 -- O(files^2). Memoized, with `fileChanged` as its single invalidator. HOW THEY WERE FOUND: the first run's DELTA COLUMN ITSELF grew x1.66 x2.07 x2.56 x3.04 x3.43 on a ladder that DOUBLES. A linear change cannot make a delta grow x3.4, so the thing it perturbed was not linear. NET at rung 5: allocs -91,209, bytes -5,091,010. Ratios x1.96..x1.99 allocs, x1.97..x1.99 bytes -- linear. Rung 0 is +675 allocs: the new phase's fixed cost on the smallest program, which the quadratics are too small to offset there. THE STANDING RULE THIS LEAVES: a whole-program query placed under a per-file one is asked ONCE PER FILE, so anything O(files) inside it is O(files^2) -- multiply by the file count before adding work to one. ALSO FOUND, a BOOTSTRAP bug (OPEN.md #14): a ternary `a if c else b` whose arms mix a BORROWED field read with a freshly-OWNED value leaks on every build (exit 101). specs-shv2 242/0 -> 262/0 (+20: 7 cross-file-signatures, 8 void-call-result, 5 ported export-keyword -- 4 of them the suite's first MULTI-FILE cases). | 209,573 | 410,841 | 813,449 | 1,618,967 | 3,230,537 | 6,454,835 |
| 2026-07-14 | SHIFTS: Go's rule -- the count is NOT masked, shr is ARITHMETIC, only a NEGATIVE count is an error. WHAT MOVED, and it is the ONLY thing that moved: +0.56% bytes / +0.05% allocs, LINEAR (bytes delta ratios x1.97 x1.98 x1.99 x2.00 x2.00 on a DOUBLING ladder; rung5/rung0 = 31x on a 32x ladder). WHY: the parser grew a real CONSTANT FOLDER, and it is TWO DENSE PER-VALUE COLUMNS beside valueTags -- valueConsts (ParsedInt, 8 bytes) + valueConstKnown (bool, 1 byte) = 9 bytes per SSA value, pushed in lockstep by mintValue and re-created per FUNCTION (so the cost is bounded by the largest function, not the module). It exists because the shift rule is the one rule in the language that must ask WHAT IS this operand, not WHAT TYPE is it: a negative count is E2054 and a count of 64-or-more names a different shift than the hardware performs. WHAT DID NOT MOVE, and this is the point: the GUARD. A shift by a count the compiler cannot see now emits 6-7 ops (an and/neg/or/sar mask, then a select or a count-clamp), and the corpus has NO dynamic shifts -- so the guard contributes exactly zero here, and a CONSTANT count in 0..63 (every x shr 8, every x shl 3) is folded and pays nothing at all. The curve therefore stayed clean: the delta is 9 bytes x values and nothing else, which is why it doubles when the ladder doubles. specs 2950/0 -> 2957/0 (+7), specs-shv2 268/0 -> 274/0 (+6). | 209,735 | 411,095 | 813,883 | 1,619,761 | 3,232,051 | 6,457,789 |
| 2026-07-14 | SHIFTS FIX (fold vs codegen + shv2 signedness): the parser's whole-program declaration index (SignatureIndex/ProgramSignatures) now also carries every RANGED TYPEALIAS, so a shift can ask its left operand's declared range for signedness across files -- u64.max shr 60 is now logical (15) in shv2 as in the bootstrap. WHAT MOVED, and it is the ONLY thing that moved: a small LINEAR allocation delta (+38 allocs/+976 bytes at rung0, DOUBLING per rung: +46/+62/+94/+158/+286 allocs -- x2 on a x2 ladder, so linear). WHY: one RangedTypeAliasMap upsert + hash-mix per typealias DECLARATION, plus a per-file RangedTypeAliasArray in the token sweep. The corpus opens most files with a typealias, so the count scales with program size and doubles when the ladder doubles -- which is exactly the shape seen. Every phase stayed x1.99-x2.00. The x64 emitter gained a third right shift (shrLogical -> SHR alongside shr -> SAR); zero corpus effect (no unsigned-ranged shifts in the corpus). specs 2957/0 -> 2964/0 (+7), specs-shv2 274/0 -> 279/0 (+5). | 209,773 | 411,141 | 813,945 | 1,619,855 | 3,232,209 | 6,458,075 |
| 2026-07-15 | CORPUS INSTRUMENT CHANGE (not a compiler change): the corpus gained TWO doubling axes and shift coverage, and now DECLARES its own coverage and blind spots every run (printCorpusManifest). So the large delta vs the row above is the CORPUS growing, NOT a compiler regression -- rows before and after this one are NOT comparable because they measure different programs. NEW: (1) LocalsPerFunction is now a DOUBLING knob (base 128, ~4096 locals at rung5) carried by longFunction and deepBlocks -- it measures the parser per-branch O(vars-in-scope) cost; longFunction/deepBlocks were reshaped E5001-safe (a bounded 6-var working set the branches MUTATE, plus V scope-fillers each folded on declaration so they stay IN SCOPE for the parser snapshot but live for ~1 instruction, so the machine never spills -- the old sum-all-V shape tripped E5001 at V>13 and could never have carried a doubling V). (2) a nested-loop DEPTH knob (base 2, depth 64 at rung5), all loops sharing ONE accumulator so peak pressure is 1 at any depth. (3) fixed shift COVERAGE: const and dynamic counts over signed and ranged int(0 to u64.max) operands -- the shape that once hid const=0 vs dyn=-4294967296. WHAT THE V DIMENSION NOW SHOWS: phase:parse ALLOCATIONS x1.99 LINEAR -- the deleted ParseExpression O(V) HashSet snapshot regression guard is GREEN, the fix confirmed; phase:parse BYTES x3.91 QUADRATIC -- the if-half mutable-var-set copies, O(V) per if times O(ifs) ifs, invisible until now because V was a fixed 6. That is a KNOWN, deliberately-unfixed cost (a surplus copy minting no ValueId; OPEN.md 5), surfaced not manufactured; flattening it is a separate rung. All regalloc rows x1.99-x2.03 -- the E5001-safe shape holds under V=4096. The nested-loop knob reads ~LINEAR in memory: its O(depth x tokens) loop-phi token pre-scan is TIME only and INVISIBLE to this memory-only instrument BY DESIGN -- the manifest names it as the worked example of that blind spot. I did NOT touch the compiler or the instrument to flatten any curve. specs-shv2 279/0. | 235,839 | 456,997 | 899,381 | 1,784,411 | 3,554,989 | 7,097,271 |
| 2026-07-15 | THREE CHANGES rebased onto origin's 2026-07-15 corpus-instrument-change baseline (Go-growth array policy, raw-layer alloc counting, StringBuilder + exact String.append). Measured on origin's NEW bigger corpus (doubling V dimension + nested loops + shift coverage), so these are not comparable to my earlier rows on the smaller corpus. ⚠ ALLOC COLUMN: change (2) makes `allocs` count the RAW allocator layer (array/string buffers) for the first time, so THIS ROW AND BELOW count raw+tracked and every row ABOVE is tracked-only — NOT comparable. The +46,702..+1,505,664 alloc delta is dominated by that (~1.2M of the +1.5M at rung 5 is raw allocations that were always happening, now visible); frees stay exact (Dtotal-Dlive per layer). BYTES moved both directions and I have NOT fully decomposed it: +1.7%/+3.2%/+1.0% at rungs 0-2, then -12.5%/-6.5%/-8.2% at rungs 3-5 (net -308 MB at rung 5). The rise at small rungs is the Go-easing churn on the many arrays under the 256-element threshold (which still double); the FALL at large rungs is consistent with the growFilled column fixes (ReachCache went Theta(rows^2 x blocks) -> Theta(rows x blocks); ReuseCopyPlan likewise) becoming measurable for the FIRST time on origin's much larger corpus, plus the RegNumColumn narrowing — the old corpus could not exercise those paths (they read 0 delta then). I am flagging this as attributed-by-suspect, not proven per-byte. WHAT IS PROVEN: no phase ratio shows a NEW quadratic from my changes. pruneDeadBlockArgs x1.99 allocs, regalloc:splitting x2.01 allocs / x2.11 bytes, regalloc:liveness x1.99 — all linear (x2.1 bytes is the growth-policy amplification bend, not a quadratic). The one superlinear phase, phase:parse BYTES x3.90, is ORIGIN's own deliberately-unfixed if-snapshot quadratic (its 2026-07-15 row documents it), unchanged by me. StringBuilder does not move the ladder (string building is not in a measured compile). Also fixed a contradiction origin shipped: TWO ScaleCorpus comments still called the pressured-loops knob KNOWN SUPERLINEAR in the splitter, stale since 49b160ce2 made the driver incremental (regalloc:splitting/liveness both ~x2.0). specs-shv2 279/279, csharp 2974/2974, no leaks. | 282,541 | 546,990 | 1,077,180 | 2,140,375 | 4,285,884 | 8,602,935 |
| 2026-07-15 | Deleted the query spine's WRITE-ONLY dependency graph (depIndex / recordDependency / clearDepsFor / activeQueryStack / QueryKey / queryKeyString). Nothing ever read a bucket: shv2 validates a memo by a content-hash compare whose inputs are named in parseMemoKey, so a dependency is DERIVED from the input, never recorded as it is read. It was kept for the day a query depended on another file's DERIVED state — that day came on 2026-07-14 (queryParseOps -> queryProgramSignatures) and was answered by mixing the index's hash into the parse memo key (232f6c80a); nothing consulted the graph, which that same commit extended with an edge for a consumer that did not exist. ALLOCATIONS FALL AT EVERY RUNG and the SAVING ITSELF IS LINEAR: -464 / -596 / -874 / -1,404 / -2,474 / -4,600, a delta ratio climbing 1.28 -> 1.47 -> 1.61 -> 1.76 -> 1.86 toward x2.00 on a ladder that doubles — exactly the shape of per-query bookkeeping billed once per query, which is what it was. Bytes fall the same way (-16,016 .. -167,130). Removed per edge: one queryKeyString String render plus a Map get/upsert; per clearDepsFor, a Map contains/remove. NO phase changed shape — this is a pure deletion of hot-path work, so the counts can only go down, and every rung did. The one superlinear phase, phase:parse BYTES, is origin's own documented if-snapshot quadratic and is untouched. specs-shv2 279/279, csharp 2974/2974, mcp 30/30. | 282,077 | 546,394 | 1,076,306 | 2,138,971 | 4,283,410 | 8,598,335 |
| 2026-07-15 | Parser if-snapshot BYTE QUADRATIC fix, re-measured after rebasing onto origin (which since my last row absorbed my earlier work, the closures feature, and the query-spine write-only-graph deletion — the row directly above this one). The fix itself is unchanged from when it first landed; only the baseline moved, so this row states its effect against origin's current tip. parseIfStatement snapshotted the boundValue of EVERY mutable var in scope (V) three times per if and merged over all V — Theta(V x branches) BYTES, which the corpus's doubling V-dimension exposed as phase:parse bytes x3.90 (quadratic) while allocs stayed x1.99. An if now carries only the vars its TOKEN EXTENT ASSIGNS (mirror of the loop-phi fix 11b9bbcba): namesAssignedIn over the whole then+else+else-if extent found by blockStatementEndIndex, filtered against mutableVars IN DECLARATION ORDER (load-bearing — the differing vars are always a subset of the assigned set, so the minted phi sequence is exactly the old whole-list merge's, which is why goldens do not move). Guarded by assertScanAligned: a too-short extent PANICS rather than silently dropping a phi. RESULT vs the query-spine row above: phase:parse bytes x3.90 -> ~x2.03 (linear); rung-5 total bytes fall from ~3.4 GB to ~490 MB (about -86%), the if-snapshot quadratic was essentially the entire memory profile at scale. Allocs fall too (the surplus snapshot arrays are gone). specs-shv2 279/279 BYTE-IDENTICAL (no SSA decision moved — pruneDeadBlockArgs already deleted exactly the surplus phis this now never mints), csharp 2983/2983, no leaks. Also in these commits: the scanner that finds the if extent is unified with the loop's (one blockStatementEndIndex, not a near-copy), and the corpus comment that still called this quadratic deliberately-unfixed now points at the fix. | 283,783 | 548,184 | 1,076,664 | 2,133,265 | 4,246,376 | 8,473,045 |
| 2026-07-15 | P1.0d.3 - the emitted panic runtime + frame pointers on EVERY function. A divide by zero was a raw 0xC0000094 hardware trap escaping with empty stderr; it is now exit 1 plus "panic: integer divide by zero" plus a symbolized backtrace, caught by a VEH thunk. The movement is LINEAR IN FUNCTION COUNT and fully attributed: allocsDelta = 786 + 6.68 x functions (rung 5 predicts 5986 vs 5994 measured, error <= 3.7 against real function counts). ATTRIBUTION, corrected by the optimizer against the implementer's first reading: 74 percent of the delta is phase:prologueEpilogue - the frame pointers, one push rbp + mov rbp,rsp per function and one pop rbp per return site - at a DEAD-FLAT 5.64-5.66 allocs per function across a 21.7x span. It is NOT __symtable, which costs ~0 allocations: it appends into geometrically-growing ByteArrays, so its alloc COUNT is O(log n) and its cost is bytes only. The constant is 677 in phase:encode and it is EXACTLY FLAT - 677/678/677/677/677/677 across a 32x program spanning 12 to 136 files - which MEASURES rather than assumes that the seven runtime chunks are built once per compile, not once per file (the whole-program-query-under-a-per-file-one trap; under that bug rung 5 would read 677 x 136 = 92072). The byte-delta non-monotonicity at rungs 0-2 (119861 at rung 2 < 132730 at rung 1) is phase:link ALONE - the difference of two capacity-class-rounded PE image buffers, i.e. a difference of quantized linear quantities, not smooth; excluding link the byte delta is perfectly monotonic (1.40, 1.52, 1.71, 1.81, 1.92) and link's ABSOLUTE bytes are x2.01. NOTHING BENDS. At rung 5 the whole rung costs +0.07 percent of allocations and +0.15 percent of bytes. NOT CACHED, deliberately: the seven runtime chunks are rebuilt from identical bytes each compile, but concatX64FunctionChunks consumes and rebases each chunk's fixups, so a cached chunk needs a per-compile deep clone costing about what building it costs - the cache would save approximately nothing and add a shared-mutable hazard under 12 workers. BLIND SPOT, recorded so the next reader knows: the generated corpus contains no divide or mod at all, so this instrument structurally CANNOT see the divide path. That is sound only because the design emits NO divisor test (the CPU traps for free), so a divide costs exactly what it did before. | 284,809 | 549,374 | 1,078,172 | 2,135,414 | 4,249,806 | 8,479,039 |
| 2026-07-15 | P1.0d.5a - top-level `let`: a module-scope constant is a compile-time NUMBER that inlines as an ordinary literal at each use. No IR op, no .data, no relocation, no backend change - and zero pre-existing goldens moved, which is the proof: `return ANSWER` compiles to exactly what `return 42` compiles to. Suite 281 to 295. The delta is EXACT and LINEAR IN FILES: phase:signatures allocs = 2 x files + 1, hitting 25/33/49/81/145/273 against 12/16/24/40/72/136 files on all six rungs, and bytes = 48 x files + 672, also exact on all six. phase:parse is EXACTLY UNCHANGED. Rung 5: +0.0035 percent allocations, +0.0015 percent bytes. The named O(files squared) hazard - a whole-program query asked once per file - did NOT fire. ⚠ READ THIS BEFORE TRUSTING THE NUMBER ABOVE: IT MEASURES THE FEATURE INERT. The generated corpus contains NO top-level `let` AT ALL (verified by emitting it: zero column-0 `let`, 56 body-local ones), so the constant arena is EMPTY on every rung and the DFS, the lookup and mixConstant never run. `2 x files + 1` is the signature of an empty arena: it proves the per-file SWEEP is linear and says NOTHING about the evaluator. This is the THIRD blind spot of this kind - the corpus also contained no `and`/`or` at all (which is why the const-flags fix, four instructions off every short-circuit site in the language, read as zero movement on every rung), and it generates no `/` or `mod` (so it cannot see the divide path the panic runtime was built for). The pattern is now explicit: THE CORPUS IS SYSTEMATICALLY BLIND TO THE FEATURE JUST LANDED, because it was grown to expose the last bug rather than the next one. So the evaluator was measured OFF-INSTRUMENT instead, on private ladders: 400 to 12,800 constants gives x2.00 allocations and x2.00 bytes per doubling, flat across a 32x span (the memo holds); and 8 to 128 files x 50 constants each with cross-file `export let` - the named hazard in its live shape - gives x2.00, where the once-per-file bug would read x4.00. Refuted by measurement, not by argument. | 284,857 | 549,430 | 1,078,244 | 2,135,518 | 4,249,974 | 8,479,335 |
| 2026-07-15 | P1.0d.5b - top-level `var`: a module-scope global with a REAL .data slot. New Std MEMORY band (globalAddr + loadIndirect + storeIndirect, v1's shape), DataSectionEntry in GlobalDataTable, a .data section in PeWriter, the dataSectionRipRelDisp32 arm. Initializers are constant-only, so they are const-evaluated into .data BYTES and there is NO __module_init. Suite 295 to 317. The delta is EXACTLY LINEAR IN FILES on all six rungs, zero residual, in BOTH columns: allocs = 66 x files - 273, bytes = 14932 x files - 109760. At rung 5 that is +0.10 percent allocations and +0.39 percent bytes. The named O(files squared) hazard did NOT fire - declaredGlobals() is called ONCE from compileToCodeResult, structurally outside the per-file query (and measured x1.98 across 3200 files). ⚠⚠ READ THIS BEFORE TRUSTING THE FIT: IT MEASURES THE FEATURE INERT, FOR THE FOURTH TIME. The generated corpus contains NO top-level `var` at all (verified by emitting it: 0 across 300 files), so layOut gets an empty array. The prior three: no `and`/`or` (which made the const-flags fix - four instructions off every short-circuit site in the language - read as zero movement), no `/` or `mod` (hiding the divide path the panic runtime exists for), and no top-level `let`. THE CORPUS IS GROWN TO EXPOSE THE LAST BUG, SO IT IS BLIND TO THE NEXT ONE. ⭐ AND THIS RUNG PROVES A SECOND, DEEPER BLIND SPOT THAT IS BY DESIGN: the optimizer found a REAL quadratic here - sortBySizeDescendingStable was Theta(globals squared), because storageBytes is only {1,2,4,8} so an all-int program never stopped the insertion scan early. Fixed to four appends-in-order, O(n), with a count invariant so a future 16-byte type panics rather than silently dropping a slot. Measured off-instrument at 8000 globals: 619ms to 166ms whole-compile; the unattributed bucket (layOut bills outside every PhaseProbe) went 30.2/120.3/460.3ms at n=2000/4000/8000 - x3.98, x3.83 - to 2.0/4.1/9.0ms. ⚠ ALLOCATIONS AND BYTES WERE EXACTLY x2.00 BOTH BEFORE AND AFTER: this was a TIME-ONLY superlinearity, and scale-test collects memory only, deliberately, because time is not trendable across machines. So a time-only quadratic is STRUCTURALLY INVISIBLE HERE and always will be. That is not a defect in the instrument - it is the price of the property that makes it trustworthy - but it is a limit to state out loud rather than rediscover. Its own comment had defended the cost as "a program's top-level var count is a handful": the same appeal to an empirical corpus property that v1's aliasing bug rests on three files away. Not fixed, and flagged honestly: layOut's remaining cost is dataIndex.upsert (String-keyed Map), which dominates what is left but swings 742 to 4206 ns per insert NON-MONOTONICALLY, and n=128,000 is cheaper per insert than n=16,000 - no algorithm is sublinear in n for n inserts, so it is allocator/heap-state dominated, not a complexity class. It is stdlib, pre-existing, and shared with every String-keyed Map in the compiler. Rewriting it on a curve nobody can explain, inside a rung commit, would be optimizing the gauge. | 285,376 | 550,213 | 1,079,555 | 2,137,885 | 4,254,453 | 8,488,038 |
| 2026-07-15 | Envelope-collapse / byte-fusion / static-literal series (the 10 commits 4c20796b1..739bf7a0f), measured against origin's prior tip. All rungs drop sharply — rung-5 allocs 8.47M->6.56M (-1.91M, ~-23%), bytes 490MB->482MB per-rung with the reduction concentrated in per-object overhead — because a managed value stopped being TWO heap objects (wrapper record + separate __ManagedMemory) and became ONE: a String/Character/Array now IS its own __ManagedMemory (buffer@0..parent@32 inline), an owned small buffer is byte-fused INLINE in the record's own allocation (parent_ptr=MmParentInline, first grow detaches), and a never-mutated string/byte/char/array literal proved by LiteralCoverageAnalysisPass is emitted ONCE as a shared immortal .data record (refcount=2^62, incref/decref no-op) at zero per-evaluation allocation. Growth stays LINEAR: every phase x1.98-x2.11 per doubling rung, no new quadratic; the allocation drop is a constant-factor-per-object win, uniform across rungs. specs-shv2 279/279, csharp 3002/3002, no leaks. This review's own edits are allocation-neutral (EmitStaticRecordUserPtr dedup is codegen-neutral: 0 fragment churn; mm_decref immortal-guard reorder changes no alloc/free counts), so these numbers are the committed series' effect, not the review's. | 219,424 | 424,205 | 833,521 | 1,651,826 | 3,288,417 | 6,562,118 |
| 2026-07-15 | Envelope series RE-MEASURED on the post-rebase tip (the 12 envelope/review commits replayed onto origin's cde9b8946, which added top-level let/var globals, floats groundwork, the emitted panic runtime and per-function frame pointers). Confirms the envelope-collapse win holds against origin's CURRENT tip and corpus: rung-5 allocs vs origin's P1.0d.5b row (8,488,038) fall to 6,572,769 (-1.92M, ~-23%), the same drop the pre-rebase row reported against the if-snapshot baseline — the two baselines are the same corpus and origin's P1.0d work was ~memory-neutral (8.47M->8.49M), so the row is comparable, not a corpus artifact. Delta vs the pre-rebase envelope row directly above is small and FULLY attributed to origin's work now present, NOT the envelope code (which is byte-for-byte the same): +10,651 allocs / +2.6MB at rung 5 (+0.16% / +0.54%), of which phase:prologueEpilogue alone is 2,848->7,266 allocs — the per-function frame pointers from origin's P1.0d.3 panic-runtime commit, absent from the pre-rebase measurement. Growth stays LINEAR everywhere (phase:regalloc x2.05, isel x2.01, parse x1.99; no phase bends). specs-shv2 317/317, csharp 3002/3002, no leaks, 0 fragment churn on the combined tree. | 220,519 | 425,612 | 835,542 | 1,655,080 | 3,294,136 | 6,572,769 |
| 2026-07-15 | P1.0d.4 x64 SSE BACKEND (half a rung — the parser/allocator land beside it). The SSE band's encoders + the float lowering: addsd/subsd/mulsd/divsd, ucomisd, movsd (reg/mem/rip), cvtsi2sd, cvttsd2si, the .rdata f64 constant pool, and the float-equality two-block split. Suite 317/19 UNCHANGED and the FRAGMENT DIFF IS EMPTY — every existing byte of codegen is identical, which is the proof that regNumber's low-nibble mask (rax..r15 and xmm0..xmm15 share an encoding space) and the class-dispatched movRegReg/storeSlotReg/loadRegSlot/xchgRegReg are behaviour-preserving for the GPR paths. DELTA IS LINEAR AND FITS EXACTLY: allocs = 552 x 2^rung + 370 across all six rungs, zero residual, and it is 100 PERCENT IN phase:isel — every other phase, regalloc included, is bit-identical to the P1.0d.5b baseline. At rung 5 that is +0.20 percent allocations. It is the per-function containers ISel now builds (the fused-compare float column, the ordered-block map, the split candidate list); the +370 constant is per-compile. ⭐ THE FIRST CUT WAS TWICE THAT (+1,528 at rung 0, allocs = 1528/2440/4240...) AND THE INSTRUMENT IS WHAT CAUGHT IT: the fused-compare second column was a `Map with (ValueId, StdType)` storing a full operand type per compare — ~4.7 allocations each — when BOTH readers immediately project it through stdTypeIsFloat and nobody asks f32-vs-f64. Replaced with a dense bool column (StdValueUseSet) holding the question actually asked, "is this fused compare a float one": an all-integer function now inserts NOTHING and the backing array is never allocated, so the cost went from proportional-to-compares to zero for programs with no floats. A column nobody reads is not free. ⚠⚠ AND FOR THE FIFTH CONSECUTIVE TIME, THE CORPUS MEASURES THE FEATURE INERT — this time the manifest says so in its own words: "NOT GENERATED ... float arithmetic". Verified by emitting it: ZERO float literals across rung 0's 12 files. So NOT ONE of the ~700 lines of SSE lowering and encoding this rung adds is executed by any number above; the delta is purely what ISel now builds for every function whether or not it ever sees a float. The prior four (no and/or, no div/mod, no top-level var, no top-level let) are recorded in the row above; the pattern is now the rule and not the exception — THE CORPUS IS GROWN TO EXPOSE THE LAST BUG, SO IT IS BLIND TO THE NEXT ONE. What DID verify the emitted bytes is off-instrument: all 18 SSE encodings were diffed against llvm-mc and match byte-for-byte, including the F2-before-REX ordering (F2 41 0F 58 DB for addsd xmm3/xmm11), REX.R vs REX.B on extended XMMs, the no-REX case, and cvtsi2sd xmm11, r9 = F2 4D 0F 2A D9 which proves one low-nibble mask encodes a GPR and an XMM correctly in the same instruction. | 286,298 | 551,687 | 1,082,133 | 2,142,671 | 4,263,655 | 8,506,072 |
| 2026-07-15 | P1.0d.4 FLOATS — THE FIRST ROW IN THIS LOG THAT MEASURES A FLOAT AT ALL, so it is a NEW BASELINE and NOT a delta: the CORPUS changed, not only the compiler. ScaleCorpus generated ZERO float instructions until today — the row above says so in its own words ("NOT GENERATED ... float arithmetic"; "ZERO float literals across rung 0's 12 files") — and it was the FIFTH consecutive rung the ladder was blind to what landed. Two new knobs, DOUBLING like every other: floatOps (float + - * /, compares in BOTH forms — feeding a jcc, and materialized as a bool in a GPR — branch-merged float PHIs, int->float promotion in, trunc out) and floatSpill (N loops holding 4 floats live ACROSS a call: every XMM is caller-saved in the Wave-1 pool, so the allowed set is EXACTLY empty and every one is a FORCED spill, the most expensive thing a float does on this target). The int knobs' generated text is byte-identical, so the columns above stay readable downwards. Floats are now 22.9 percent of allocations at rung 5. ⭐ THE +2,379,450 AT RUNG 5 DECOMPOSES EXACTLY, AND NONE OF IT IS A REGRESSION: -108,488 is this rung's own COMMITTED work (measured against a pristine HEAD build — the row above predates HEAD by 15 commits, so its delta was never this rung's), and +2,487,938 is the float dimension the ladder now compiles for the first time. -108,488 + 2,487,938 = +2,379,450, and the identity holds at ALL SIX rungs. ⭐ THE FLOAT RUNG IS LINEAR — a MEASURED null result, not an assumption. The ladder spans 32x, so a linear phase grows exactly 32x across it and a quadratic 1024x. EVERY float column lands at or below 32.01x: total 30.40, regalloc 30.80, parse 30.13, lex 31.05, isel 30.74, coloring 31.51, rewrite 31.42, liveness 29.51, lowerMaxonToStd 31.43, ssaDestruction 31.94, splitting 32.01. The highest IS the forced-spill path (regalloc:splitting at 32.01x); its rung-over-rung ratios read x2.03 at the top ONLY because it starts with a small deficit that the span test dissolves. --per-type agrees at type AND scope granularity — nothing above x2.00 (realloc x1.92, OpIndexArray.create x1.98, IrBlockArgArray.create x1.99). NOTHING WAS OPTIMIZED BECAUSE NOTHING WAS SUPERLINEAR. ⭐⭐ WHAT THE BLINDNESS WAS HIDING: A FLOAT PHI PANICKED THE x64 EMITTER. `var f = 0.0` mutated in a loop or a branch — the commonest float idiom there is — died with "a register-to-register move from xmm0 to rax crosses register files". Parser.mintPhi hardcoded EVERY phi's argType to i64, which was harmless for exactly as long as every value lived in a GPR; P1.0d.4 gave that field its FIRST CONSUMER (regClassForStdType(arg.argType) picks a phi's REGISTER FILE), so a float phi claimed i64, was coloured into a GPR, and SSA destruction emitted `mov rax, xmm0`. The comment above mintPhi PREDICTED THIS RUNG BY NAME — "the scaffold the float (XMM) register class will need at P1.0d.4, and that is when it acquires a consumer and a reason to be right" — and the rung gave it the consumer without the reason. The suite could not see it either: float-compare-branch.md puts a phi on a float compare's EDGES, but the value it carries is an int, so a float-TYPED phi was covered by nothing. Fixed in LowerMaxonToStd.retypeBlockArgs, which types every phi from stdTypeOf — the same ONE oracle every operand's type comes from — rather than at the mint site, where the collapse is not yet total (a parse-time tag can still be `unresolved`, which is E3004's business and not a panic's). THE FIX COSTS EXACTLY ZERO: allocations and bytes BIT-IDENTICAL to pristine HEAD on the float-free corpus at all six rungs, which is what the single reader predicts (i1/u32/i64 all map to gpr; only a float phi changes file). Fragment diff is additions-only across every pre-existing golden; +3 specs (float-phi-loop-carried / -through-branch / -both-arms); suite 355/355 at --workers=1 and =12. TWO LEADS CLOSED WITHOUT ACTION, verified rather than assumed: (1) `.allCases` in a hot path — NONE remains; every surviving site is per-compile setup or reporting, and RegAllocStats.create (the one that looks per-function) is reached from project.timings.regAlloc, once per compile. (2) ISel's 552 x 2^rung residual is REAL — StdToX64Conversion builds four per-function containers (cmpPreds, fusedFloatCmps, orderedBlockIds, valuesReadFromRegisters) for every function whether or not it ever sees a float — but it is 0.21 percent of rung 5 and LINEAR, and making it lazy would fork a conditional construction path for a constant factor. Growth curves are the mandate; that is not one. | 365,426 | 704,974 | 1,383,780 | 2,740,933 | 5,455,447 | 10,885,522 |
| 2026-07-15 | P1.0r OPTIMIZATION PASS - the heap, the refcount runtime and the minimal struct. NOTHING WAS OPTIMIZED BECAUSE NOTHING IS SUPERLINEAR - a measured null result, and every flagged suspect was refuted by measurement rather than by argument. ⚠⚠ READ THIS BEFORE TRUSTING THE DELTA COLUMN: THE -2,494,194 IS NOT THIS RUNG'S AND IS NOT A COMPILER CHANGE AT ALL - IT IS A STALE BASELINE IN THIS LOG. The row above (P1.0d.4 FLOATS) records rung5 allocs = 10,885,522; that number DOES NOT REPRODUCE FROM ITS OWN COMMIT. Measured, not inferred: ed86a5eb8 - the very commit that wrote the row - checked out into a clean worktree, its C# bootstrap rebuilt from source (sha d07c23c3, distinct from today's aad7c678 but the SAME 8,581,499 bytes, which is exactly how a size check fools you), and shv2 rebuilt by it, measures rung5 = 8,369,027, i.e. -22.9% against what the row claims. The branch point 583882b52 measures 8,369,027 too - BIT-IDENTICAL TO ed86a5eb8 AT ALL SIX RUNGS (280,461/541,515/1,063,389/2,106,766/4,193,804/8,369,027), which independently MEASURES the seven inherited commits as exactly memory-neutral, including the bootstrap's range-arm fix 645a690cc (a real codegen change over a construct shv2's source is full of, and it moves nothing here). So the row above was recorded from a tree that is not its own commit - the alloc column is ~30% high while its BYTE column is only 1.9% high, which is the envelope-collapse signature of rows 142/143 (a per-object-overhead win: allocs -23%, bytes ~flat), i.e. the row's numbers predate work its own tree already contained. THIS ROW IS A NEW, REPRODUCIBLE BASELINE: three independent trees on one machine agree. Three hypotheses were killed by measurement before this one survived - (1) "the worktree lacks the float corpus" (it HAS ed86a5eb8; and the corpus emits 278 float literals at rung0, 8,710 at rung5), (2) "the stdlib changed" (git diff ed86a5eb8..HEAD -- stdlib/ is EMPTY), (3) "the bootstrap's range-arm fix did it" (rebuilt at ed86a5eb8 with its OWN bootstrap: identical numbers). ATTRIBUTION IS A MEASUREMENT, NOT A STORY THAT FITS, and each of those three fit. ⭐ P1.0r'S OWN COST IS EXACT AND CLOSED-FORM, WITH ZERO RESIDUAL ON ALL SIX RUNGS: allocs = 5 x files + blocks + functions + 26. Against files=14/18/26/42/74/138, blocks=653/1303/2603/5203/10403/20803, functions=38/62/110/206/398/782 it predicts 787/1401/2765/5493/10949/21861 and measures 787/1401/2765/5493/10949/21861 - residual EXACTLY 0 at every rung. It DOUBLES per rung (x1.88, 1.94, 1.97, 1.98, 1.99; 28.3x over the ladder's 32x span) so it is LINEAR, and at rung 5 it is +0.27% of allocations. The four terms, each named at its line: (a) 3 x files in phase:signatures - the declaration sweep's per-file struct columns; (b) 2 x files in phase:parse - TWO Parser.create()s per file (the sweep's scanner and the real parse), each building an ownedBindings VarInfoArray, and the SWEEP's copy is structurally dead (a sweep parses no body and declares no binding); (c) functions - the per-function `self.ownedBindings = VarInfoArray.create()` reset (Parser.maxon:1919); (d) blocks - and this is 93% of the whole rung (20,803 of 22,301 at rung 5): Parser.openBlock() now returns a HEAP-ALLOCATED BlockMark box where the baseline returned a bare integer (`return self.mutableVars.count()` -> `return BlockMark.create(mutableCount, ownedCount:)`), so every if/else/while body allocates a two-field box to carry the second mark this rung needs. LEFT ALONE DELIBERATELY, and named here so the next reader starts at the right line: it is LINEAR and it is a constant factor, and both alternatives are worse than the 0.26% they would buy - bit-packing two BindingMarks into one int is a magic value across a range that spans u64, and a parallel mark stack trades the structural guarantee that the mutable-list and owned-list heights are opened and closed TOGETHER (which is what the mark exists to make unloseable - a drained-late owned list is a DOUBLE FREE) for an amortized array push. Growth curves are the mandate; that is not one, and this is the same call the P1.0d.4 row made on ISel's 552 x 2^rung residual. ⭐ SUSPECT 1 REFUTED BY MEASUREMENT - the flagged O(files^2) hazard (the whole-program signature query sits UNDER a per-file one, and this rung taught its token sweep to mint Point.create). Measured OFF-INSTRUMENT in the hazard's LIVE shape, because the corpus cannot see it: a ladder of 8/16/32/64/128 files, each DECLARING a struct and READING a struct declared in ANOTHER file (verified to compile and return the right answer: a cross-file T0.create + field reads gives 10). phase:signatures reads 978/1859/3596/7045/13918 allocs = x1.90, x1.93, x1.96, x1.98 - 14.2x over a 16x file span. Under the once-per-file bug it reads x4.00 and 256x. The memo holds; the hazard did not fire. phase:parse on the same ladder is x1.99. SUSPECT 2 (moduleUsesHeap walks every op of every function to answer one bool): it is O(ops), called ONCE (Compiler.maxon:158), and it ALLOCATES NOTHING - which the flat 26 residual above proves rather than assumes, because 26 is the same at rung 0 and at rung 5 while a per-block or per-op iterator would scale with the ladder. SUSPECT 4 (the ownedBindings drain): closeBlock pops to a recorded height (amortized O(1) per binding) and emitScopeDrops is O(live struct bindings) per return, which is the SIZE OF THE EMITTED OUTPUT - every return MUST decref every live box - not an algorithm with a curve. ⭐ SUSPECT 3 LEFT ALONE, AND THE REASON IS MEASURED RATHER THAN ASSERTED. StructLayout.indexOfField (Project.maxon:224) is a LINEAR SCAN, and the two references disagree about it: v1 hashes (fieldByName as FieldNameIndexMap, Project.maxon:3973, built per StructType.create) and the C# bootstrap scans (st.Fields.FirstOrDefault(f => f.Name == fieldName), 2-Parser.cs:19216). shv2 matches the bootstrap. It IS O(F) per access and it IS visible under torture - a controlled experiment holding the program shape fixed at 200 accesses and varying ONLY the field index (a scan is the one thing that can tell first from last) measures reading the LAST field of a 512-field struct at +2,658,300 ns over reading the FIRST, with the gap growing in F. But it is NOT a growth curve in program size, and the measurement that settles it is the field-count distribution of the two real codebases: v1 is 3.5x larger than shv2 (369 declared types vs 107) yet its MEAN field count is identical (5.4 vs 5.3) and its MEDIAN is LOWER (3 vs 4). Programs grow by adding TYPES, not by widening them, so the cost is O(accesses x 5.3) - linear with a bounded constant - and a per-layout name->index map would buy a heap Map per declared type plus a name hash per lookup to save ~2.6 byte-compares. That is a structural argument backed by two independent 100k+ line corpora, NOT the "a program's X count is a handful" appeal that row 141 correctly convicted layOut of - there globals genuinely grow with program size, here fields per type measurably do not. ⚠⚠ AND FOR THE SIXTH CONSECUTIVE RUNG THE CORPUS IS BLIND TO WHAT LANDED, and this time the manifest says so in its own words: "NOT GENERATED ... structs". Verified by emitting it: ZERO `type` declarations and ZERO Self{} across every rung. So NOT ONE LINE of osAllocPages, __slab_alloc, __mm_alloc/incref/decref/free, the leak check, or the struct front-end is executed by ANY number in this row - phase:runtimeAugment is 54/54/55/56/57/58, i.e. the runtime is never installed because moduleUsesHeap is correctly false on every rung. What this row measures is EXACTLY what a struct-free program pays for the rung EXISTING, which is the 5 x files + blocks + functions + 26 above - and that is worth having (it is the proof that `return 42` still compiles to what it always did, which is the rung's own stated promise) but it says NOTHING about the heap. The prior five: no and/or, no div/mod, no top-level var, no top-level let, no floats. Everything about structs in this pass was therefore measured off-instrument, on the private ladders above. GATES: build exit 0 zero warnings; specs-shv2 357/0 at --workers=1 AND --workers=12, byte-identical; zero M in specs-shv2/fragments/; no exit 101. NO COMPILER CODE WAS CHANGED BY THIS PASS - the numbers in this row are the rung's, not the optimizer's. | 281,248 | 542,996 | 1,066,258 | 2,112,411 | 4,205,001 | 8,391,328 |
| 2026-07-16 | CORPUS BLIND SPOTS CLOSED — FIVE NEW KNOBS. ⚠⚠ THE CORPUS CHANGED: EVERY COLUMN ABOVE THIS LINE IS INCOMPARABLE WITH EVERY COLUMN BELOW IT. This row measures a DIFFERENT PROGRAM, not a different compiler. NO COMPILER CODE WAS CHANGED — the only edit is maxon-shv2/Testing/ScaleCorpus.maxon. Do not read the delta column as a regression: rung5 allocs 8,391,328 -> 9,932,791 (+18.4%) is the five new knobs' extra program, and the ladder stayed LINEAR through it (total allocs x1.90 x1.95 x1.97 x1.99 x1.99; every phase x1.94-x2.00). Precedent: P1.0d.4 moved the columns the same way when it added the float knobs. ⭐ WHAT WAS BLIND, MEASURED BY EMITTING THE CORPUS AND COUNTING (before -> after, at rung1): top-level `let` 0 -> 64; top-level `var` 0 -> 64; `type` decls 0 -> 20; Self{} 0 -> 20; .create() 0 -> 80; integer `/` 0 -> 64; `mod` 0 -> 64. Every one now DOUBLES per rung (rung0->rung5: lets 32->1024, vars 32->1024, types 12->260, creates 40->1280, div/mod 32->1024 each). The corpus had been FULL of `/` and every one was a FLOAT `/` — a different Std op (binOp(div), no fixed registers) — so counting the OPERATOR said "covered" and only counting its TYPE found the hole. Four landed rungs were unmeasured: P1.0d.3 (the whole idiv RAX/RDX path), P1.0d.5a top-level let (suite 281->295), P1.0d.5b top-level var (295->317), P1.0r structs+heap (355->357). ⭐ AND THE MANIFEST LIED BY OMISSION, which is the half of this rung that matters. It printed "let / var" under CONSTRUCTS GENERATED meaning the BODY-LOCAL kind, and named the top-level kind under NEITHER heading — so two rungs and 36 spec tests read as covered by a reader doing exactly what the manifest asks. It is now held to a stated rule: every construct in the subset appears under EXACTLY ONE heading, and a PARTLY-generated construct (structs — reads yes, writes no) says which part under GENERATED rather than being filed whole under either. Structs' two interior blind spots are named where a reader looking up structs will find them: field WRITES (P1.1a wave 2) and structs across a CALL (P1.4). ⭐⭐ A REAL SUPERLINEARITY, FOUND AND THEN DESIGNED OUT OF THE KNOB — NOT HIDDEN. The first draft put each `create` at FUNCTION-BODY scope and the ladder went superlinear: regalloc:splitting read x3.12 x3.45 x3.69 x3.83 x3.91 (converging on x4.00 = QUADRATIC) and was 17,110,308 of 26,404,072 allocs = 65% of the whole compile, while every other phase read x2.00. CAUSE (measured, not argued — an isolating ladder per knob refuted the obvious suspects first): intDivide, globalVars and globalConsts all read splitting x1.00-x1.01, i.e. FLAT; a control ladder of 64->1024 sequential CALLS with a value live across each also read FLAT (320 -> 326 allocs), killing "it is the call brackets". Only the two struct knobs bent. The mechanism is ownership: a struct binding is OWNED, so it does NOT die at its last read — it stays on the parser's ownedBindings list until the `return`, where emitScopeDrops (Parser.maxon:2524) decrefs every one. N creates at function scope therefore put N pointers LIVE SIMULTANEOUSLY at the return against a 14-GPR pool; they are spillable so it is never E5001, and the splitter instead does ~N splits, each re-sweeping the O(N) ops of the one enormous block (SplitLiveRanges' driver is incremental in DIRTY BLOCKS, and there is only one). FIX: each create now sits in its own `if` block, so the binding's scope — and its live range — is bounded. Measured on the private ladder, 32/64/128/256/512 creates: function scope x3.49 x3.70 x3.84 x3.91; block scope x1.82 x1.90 x1.95 x1.97. On the full ladder regalloc:splitting went x3.91 -> x2.01. THE KNOB WAS WRONG, NOT THE COMPILER: this is trap 7 (bounded liveness) and it is the same call the file already makes for locals — fillerLocalsDecl keeps V IN SCOPE BUT NOT LIVE precisely because a doubling count of LIVE values trips E5001 at V>13. The splitter's O(splits x ops-in-dirty-block) is REAL and reproducible, but it is a curve in live-values-per-block, not in PROGRAM SIZE, and real code bounds it — the same reasoning the row above used to leave StructLayout.indexOfField alone. It is left alone and named here so the next reader starts at the right line. ⚠ AND THE TRIGGER TO RE-MEASURE IS NOT P1.4 OR P1.7 — this row named those two on plausibility alone, the independent optimizer MEASURED them wrong, and the correction is recorded here rather than the claim quietly dropped, because a wrong trigger in a comment is the "compiler nobody wrote" class freshly minted: it would have sent the next reader to re-measure the two rungs that CANNOT move this number while leaving the one that can unwatched. A struct crossing a call (P1.4) is bounded by PARAMETER COUNT — and that is not a corpus convention but a compiler cap, `Parser.maxon:50` ("M5 caps a function at six parameters") — and an array (P1.7) is ONE owned value (`for x in arr` holds one element live), so neither makes K grow with PROGRAM SIZE, which is the only growth that could bend this curve. THE REAL TRIGGER IS AN INLINER: inlining N callees each holding an owned local into ONE block is precisely what makes K scale with program size, and shv2 has none today — its whole default pipeline is resolveTypes / semanticCheck / lowerMaxonToStd / pruneDeadBlockArgs / elimTrivialBlockArgs / foldConstOperands (PassPipeline.maxon:135). Any desugaring that emits many owned temporaries into one block does the same. The ceiling is bounded but its constant is bad, so it is debt and not a non-issue. ⚠ THE BLOCK SCOPING DOES NOT LEAN ON THE closeBlock LEAK, which is the first thing a reader will suspect. closeBlock (Parser.maxon:2343) drains the binding and deliberately emits NO drop, so a struct in a block leaks (exit 101 — verified, and a stated P1.4 gap this instrument never observes because it COMPILES these programs and never RUNS them). That leak is not what bounds the range: when P1.4 emits the drop at the block's END, the range is bounded by the block either way. Correct before the fix and after it. ⚠ phase:runtimeAugment IS STILL FLAT (58/59/60/61/62/63) AND THAT IS CORRECT — DO NOT READ IT AS THE STRUCT KNOB FAILING TO REACH THE HEAP. It is fixed-cost BY CONSTRUCTION: the phase times augmentWithRuntime only, which is prependEntryStub — ONE hand-built entry stub. The heap runtime proper (installMmRuntime, five FIXED functions: slabAlloc/mmAlloc/mmDecref/mmFree/mmLeakCheck) is called at Compiler.maxon:180 OUTSIDE every PhaseProbe, so it is billed to NO PHASE AT ALL — a real attribution gap (phases do not sum to total), constant-cost so it distorts no trend, reported and not fixed because it is compiler code and outside this rung. There is also no __destruct_* cascade yet to make it per-TYPE (LowerMaxonToStd.maxon:9 names it as future work). PROOF THE HEAP IS REACHED, since this phase cannot give it: a struct in a block exits 101, which is unreachable unless __mm_leak_check was linked in; and runtimeAugment's own allocs moved 54->58 at every rung (the stub taking its checkLeaks branch) while its bytes now grow 3,016->26,696 (prependEntryStub PREPENDS to a function array that doubles, an O(N) copy; the +1 alloc/rung is that array's growth realloc). The struct knobs' real cost lands in lowerMaxonToStd/isel/regalloc/encode. ⭐ THE TWO STRUCT KNOBS ARE DELIBERATELY TWO: structTypes doubles TYPE declarations (12->260, a per-TYPE cost) while structAllocs doubles CREATE SITES (32->1024) over a FIXED set of 4 types — so P1.1a wave 2 moves one and a layout-descriptor change moves the other. The combined `type` column reads x1.67..x1.97 rather than x2.00 and that is arithmetic, not a bend: it is 8<<rung + 4, a doubling knob plus a deliberate constant; n_stypes alone is exactly 8/16/32/64/128/256. Each rung also gained 5 files (19 at rung0, 143 at rung5), worth a measured +13 allocs/+459 bytes each. GATES: build exit 0 zero warnings; specs-shv2 357/0, memoryLeak false, --workers=1 and --workers=12 BYTE-IDENTICAL; git status specs-shv2/fragments/ EMPTY (no golden moved — a corpus change must not touch codegen, and did not); scale-test exit 0 with assertCorpusCompiled passing (code growth x1.93 x1.96 x1.98 x1.99 x1.99 against a x1.50 floor); no run exited 101. | 342,532 | 652,277 | 1,271,312 | 2,508,637 | 4,983,127 | 9,932,791 |
| 2026-07-16 | MAKE THE COPIES CHECK EACH OTHER — +2 allocs / +296 bytes, FLAT at every rung, and it is a CHECK'S OWN COST, not a regression. THE CORPUS DID NOT CHANGE: the emitted text is byte-identical to the row above (342 files, recursive diff empty, re-proven after every edit including the review's). The entire delta lands in phase:regalloc — 113,973 -> 113,975 at rung 0 and 3,284,334 -> 3,284,336 at rung 5 — which is `assertRegisterFileMatchesEnum()` reading `X64Register.allCases` to count it, once per module at RegisterAllocator.maxon:428, beside `assertCallClobberConsistent`. Flat across a 32x ladder is the signature of a per-module constant; it does not scale and it never will. ⭐ WHAT IT BUYS: `RegisterFileSize` (32) is now held to `X64Register.allCases.count()`, and `FirstXmmRegisterNumber` (16) to the register the enum actually puts at that number. Both were literals RESPELLED from the enum with only a comment ("the two must move together") and a spec test holding them — and `regClassOf` is THE place the GPR/XMM boundary is decided, so a drifted boundary colors an f64 into a GPR. SABOTAGED AND SEEN RED before being believed: FirstXmmRegisterNumber=15 -> exit 1, "the register the enum puts at that number is not xmm0"; =32 -> E3034 at COMPILE time, so that drift now builds nothing at all. ⭐⭐ AND THE BETTER OUTCOME, WHERE IT WAS AVAILABLE: `MaxUnguardedShiftCount` was a literal 63 beside a literal 64 — ONE hardware fact (the operand width) written twice — and is now DERIVED, `(ShiftCountBits - 1)`, verified const-folded (it casts to int(63 to 63) and runs). No check exists for that pair because there is nothing left to compare: drift is UNREPRESENTABLE, not asserted. A derivation beats a check exactly as a check beats a comment. ⚠ It is NOT the only copy and the code says so: TargetDialect.ShiftCount's int(0 to 63) is a RANGE BOUND (E2010, unreadable by any assert) and moves BY HAND. DELETED, same rung, same rule that deleted __mm_incref at P1.0r: classifyGlobalLabel + GlobalLabelClass + isRuntimeMutableGlobalLabel had ZERO callers and held a second copy of "__data_" beside DataLabelPrefix — "they cannot disagree only because one is dead". OPEN.md predicted it would go live when __slab_ globals arrived; P1.0r shipped __slab_cursor/__slab_end and nothing wired it, so the prediction was already falsified. Also fixed: four comments claiming mechanisms nobody wrote (TargetLiveness:172 cited an assert that never touched what it claimed; RegisterAllocator:146 called RegNum int(0 to 16) and REASONED FROM that bound when it is int(0 to 32); two "u16 mask" comments stale since P1.0d.4 doubled the file). The review closed one more the implementer had left: the chain knob's top link was coupled to its loop bound by ADJACENCY not agreement, and the stated reason for leaving it ("byte-identity forbids closing it") was false — byte-identity is re-run, not lost, and the closure emits identical text. Gates: build exit 0 zero warnings; specs-shv2 357/0 memoryLeak false; workers=1 vs 12 byte-identical; fragments EMPTY; no 101. | 342,534 | 652,279 | 1,271,314 | 2,508,639 | 4,983,129 | 9,932,793 |
| 2026-07-16 | P1.1a wave 2 (struct field writes + mutability + field defaults) costs +156 allocs at rung 0 and +3,380 at rung 5 — LINEAR, ~0.03-0.05% of total, and it is a per-TYPE cost. ⚠ READ THE DELTA COLUMN AGAINST THIS, NOT AGAINST THE TOOL'S: the tool reports +480/+34,952 because the row above does not reproduce at the commit it was recorded on. MEASURED BY A/B, not inferred: same corpus, same bin/maxon.exe, two shv2 binaries differing only by this commit (parent rung0 342,858 -> 343,014; rung5 9,964,365 -> 9,967,745). The row above logs rung0 342,534 / rung5 9,932,793, so its baseline sits 324/31,572 BELOW the actual parent — ~10x this change at rung 5, which would have swallowed it whole. THE CORPUS IS NOT THE CAUSE AND THAT WAS TESTED, NOT ARGUED: the review commit 9ba3af660 edited ScaleCorpus.maxon, so the obvious suspect was that its "THE CORPUS DID NOT CHANGE" claim was false — it is TRUE (built the generator at bbc1ea32a and at 9ba3af660, emitted both ladders, recursive diff EMPTY across all 6 rungs). The corpus being identical is what localizes the drift to the shv2 BINARY, and the only input to that which is not in git is bin/maxon.exe — the documented stale-bootstrap trap (reference_stale_bin_maxon_exe.md: "a baseline you built YOURSELF can measure a tree that does not exist"). An A/B is immune to it by construction; an absolute row is not. ⭐ WHERE THE +156 LANDS, EXACTLY: phase:parse +84 and phase:signatures +72 at rung 0 (+1,820 / +1,560 at rung 5), and EVERY OTHER PHASE +0 — not "small", zero. Those are precisely the two phases that build a StructLayout (the real parse and the whole-program token sweep, one layout each per declared type), which is the attribution rather than a story that fits it. Per type: ~6 allocs at rung 0 falling to ~4.7 at rung 5 as array growth amortizes = the 2 new columns (BoolArray fieldMutable + FieldDefaultArray fieldDefaults) plus each one's first-push backing store. Rung-over-rung x1.67 x1.80 x1.89 x1.94 x1.97 -> 2.00 = linear in the `type` declaration knob, which doubles per rung; a per-field cost cannot bend a ladder whose field count is linear in it. FieldDefault.none costs NOTHING and the numbers say so: 24 fields at rung 0 would have added +24/phase if a payload-free union arm were boxed, and the count is ~4 arrays/type with no field term — ARCHITECTURE's "Maxon boxes each payload-carrying union case", confirmed by measurement. ⚠ THE CORPUS STILL DOES NOT EXERCISE THIS RUNG'S FEATURE AND ITS OWN MANIFEST SAYS SO: field WRITES are named as a struct blind spot ("P1.1a wave 2 and NOT IMPLEMENTED", ScaleCorpus manifest), and no generated field carries a DEFAULT, so what moved is only the layout that now has room for both. Closing those knobs is the instrument's rung, not this one — the movement is reported, the instrument is untouched. Gates: dotnet build exit 0; build shv2 exit 0 zero warnings; specs-shv2 370/0 (was 358/11 against the ported specs before the fix), memoryLeak false; workers=1 vs 12 BYTE-IDENTICAL; git status specs-shv2/fragments/ ADDITIONS ONLY (no pre-existing golden moved, so no existing program's codegen changed); no run exited 101. | 343,014 | 653,871 | 1,275,130 | 2,516,903 | 5,000,289 | 9,967,745 |
| 2026-07-16 | P1.1a WAVE 2 — OPTIMIZATION PASS: NOTHING WAS SUPERLINEAR, SO NOTHING WAS CHANGED. Every number on this row is IDENTICAL to the row above because the compiler is byte-identical to it — this row is a VERIFICATION, not a change, and a null result recorded on purpose (P1.0r's optimizer wrote the same kind of row). ⭐ THE THREE CANDIDATES, EACH MEASURED RATHER THAN ARGUED. (1) `StructLayout.indexOfField` (Project.maxon:276) is a LINEAR SCAN of the name column and the rung made it HOTTER — it is now asked by every field READ, every field WRITE and every `Self{}` label, so a literal filling all F fields costs O(F^2). It is DEBT, not a bug, and the constant is MEASURED, not assumed: across all 166 `type` declarations in maxon-shv2/ + stdlib/ (the P2 self-host workload) F is max 22 (`UseIndex`, TargetLiveness.maxon) and mean 4.14, so one lookup is at most 22 byte compares and the sum over the program is LINEAR IN PROGRAM SIZE. A per-layout name-to-index map would buy a heap object per declared type to serve a 4-element scan: a PESSIMIZATION on every real program, which is why `FuncSignature.indexOfParam` is a linear scan too and says so. ⚠ THE RE-MEASURE TRIGGER IS A MACHINE-GENERATED STRUCT — a layout whose field count grows with PROGRAM size rather than with one declaration's own source. I deliberately name NO RUNG: F is bounded by what a human types in one declaration, a closure env (P1.5) is bounded by its own closure body, and monomorphization does not change a field count — OPEN.md #24's first draft is the cautionary tale, it named two rungs that could not move the number and the review deleted them. (2) `recordStruct`'s hash now mixes 2 more facts per field via `mixField`, and it is asked ONCE PER FILE over that file's OWN structs (`queryProgramSignatures` sweeps each source path once, Queries.maxon:119-133; `foldFile` folds only that file's `structs` array), so it is O(total declared fields) = LINEAR. It is specifically NOT the standing O(files^2) shape — a whole-program query placed UNDER a per-file one — which is what that rule exists to catch. (3) `fieldAssignsAt` (Parser.maxon:5621) is a 4-token O(1) lookahead asked ONCE PER STATEMENT by `parseStatement`, and it is NOT reached from `namesAssignedIn`: that per-token scan over a loop's extent reads only `assignsAt` (Parser.maxon:3250), which is CORRECT rather than an omission — a field store assigns no BINDING, so there is no loop-carried value and no phi to mint. ⭐⭐ THE RUNG'S OWN COST, RE-MEASURED INDEPENDENTLY AND CONFIRMED EXACTLY: +156 allocs at rung 0 rising to +3,380 at rung 5, landing in phase:parse (+84 to +1,820) and phase:signatures (+72 to +1,560) with ALL EIGHTEEN other phases AND ALL SEVEN regalloc sub-phases at EXACTLY +0 — not "small", zero. Those are precisely the two phases that build a StructLayout. Delta ratios x1.67 x1.80 x1.89 x1.94 x1.97 converging on 2.00 on a DOUBLING ladder = LINEAR. ⚠⚠ THE BASELINE TWO ROWS UP IS WRONG, AND IT IS NOW PROVEN RATHER THAN SUSPECTED — NOT THE CORPUS, NOT A MYSTERY. The check-sweep row logs rung0 342,534 / rung5 9,932,793. Built from source in a clean worktree with its own bootstrap, its own commit (f419febb6) measures 342,858 / 9,964,365 — 324 / 31,572 HIGHER. WHAT PROVES IT: 2f94c0a21 (post-bootstrap, pre-check-sweep) measures 342,856 and f419febb6 measures 342,858, EXACTLY the +2 the check-sweep claims for ITSELF. So THE CHECK-SWEEP'S DELTA WAS RIGHT AND ITS BASELINE WAS STALE. The 324 is the 14-commit window between the corpus rung's measurement (74ea57d1f, 17:53, which legitimately measured 342,532 against the then-current bootstrap) and the check-sweep's (bbc1ea32a, 20:02): FIVE BOOTSTRAP COMMITS LANDED AT 19:32 — including `4a33acb36 String's iterator holds a String, not a record, and for-in got 1.5x faster`, which changes how the bootstrap lowers for-in and therefore how many allocations the shv2 IT BUILDS makes — plus coupled stdlib and shv2 String changes. bin/maxon.exe is GITIGNORED and nothing rebuilds it, so a tree that took those commits kept the OLD binary and the shv2 built from it kept the old allocation behaviour: the documented stale-bootstrap trap (OPEN.md #22, reference_stale_bin_maxon_exe.md — "a baseline you built YOURSELF can measure a tree that does not exist"), caught in the act with the smoking gun dated. ⇒ ROWS "corpus: five knobs" AND "make the copies check each other" ARE PRE-19:32-BOOTSTRAP NUMBERS: their absolutes sit ~324 (rung 0) to ~31,572 (rung 5) BELOW what their own commits actually produce. They are comparable to EACH OTHER and NOT to this row or the one above it. I am NOT rewriting them — a measurement I did not take is not mine to edit, and replacing a wrong number with a re-derived one would hide that the gap existed — but the trend now SAYS so. ⭐ AND THE CORPUS IS EXONERATED BY MEASUREMENT, NOT BY ARGUMENT: the 2f94c0a21 -> f419febb6 comparison above SPANS the review commit 9ba3af660's 182-line ScaleCorpus.maxon edit and lands on exactly +2 — the check-sweep's compiler change ALONE — so that generator edit emits byte-identical text and moved EXACTLY ZERO. ⚠ THE CORPUS CANNOT SEE THIS RUNG AND IT ALSO CANNOT SEE F: `structTypeDecl` (ScaleCorpus.maxon:1226) emits exactly TWO fields per generated type while the ladder doubles the NUMBER OF TYPES, so the per-struct field-count dimension that `indexOfField`'s O(F^2) lives in is STRUCTURALLY INVISIBLE to this instrument — which is why F's bound had to be measured against real source instead. Same shape as OPEN.md #24's K-vs-blocks separation: two variables, and only measurement tells them apart. Closing that knob is the instrument's rung, not this one; I did not touch the instrument. GATES (this pass changed no compiler code): build csharp exit 0 and shv2 exit 0, zero warnings; specs-shv2 370/0 exit 0, memoryLeak false; specs-shv2/fragments/ zero modified and zero untracked; no run exited 101. | 343,014 | 653,871 | 1,275,130 | 2,516,903 | 5,000,289 | 9,967,745 |
| 2026-07-16 | P1.1a WAVE 3 — instance methods + self, field visibility (E3014), E3011 at field declarations. +148 allocs at rung 0 rising to +3,496 at rung 5, and it is LINEAR: delta ratios x1.73 x1.84 x1.92 x1.96 x1.98 converging on 2.00 on a DOUBLING ladder. Nothing regalloc, nothing backend. ⭐ THE SPLIT IS MEASURED, NOT ARGUED — the two contributions were separated by BUILDING THE COMPILER WITH checkStructFieldTypes DISABLED and re-running the ladder: (1) StructLayout.fieldExported, the new visibility column = +135 rung0 / +3,235 rung5 (ratios x1.74 x1.85 x1.92 x1.96 x1.98). One extra BoolArray per StructLayout, and a layout is built TWICE per declared type — once by the token sweep (recordScannedType) and once by the real parse (parseTypeDeclaration) — so it is two arrays per type, and the corpus's structTypes knob grows the NUMBER of types per rung. Linear in declared types, and it is the storage E3014 reads. (2) checkStructFieldTypes, the new registry walk in resolveTypes = +13 rung0 / +261 rung5 (ratios x1.62 x1.76 x1.86 x1.93 x1.96) — one MapIterator per COMPILE plus one ArrayIterator per declared type. 261 allocations at rung 5 is 0.0026% of the run; the per-layout iterator could be an index loop, but resolveTypes' own neighbouring loop over param types is an iterator and consistency is worth more than 0.0026% — recorded so the optimizer can disagree with a NUMBER rather than a feeling. ⚠⚠ THE INSTANCE-METHOD MECHANISM ITSELF COSTS ZERO HERE, AND THE CORPUS STRUCTURALLY CANNOT SEE IT: ScaleCorpus's structTypeDecl emits types with FIELDS and no METHODS, so not one generated program declares an instance method, calls one, or names a bare self field. Every allocation above is the FIELD-DECLARATION path. The receiver parameter, the self-field aliases, the method-call receiver prepend and the E3014 check are all INVISIBLE to this ladder, so their cost is bounded by construction rather than by measurement: a receiver is one more entry in the three parallel call columns (pushCallArg) and one more parameter binding, both O(1) per call site; declareSelfFieldAliases is O(fields of the enclosing type) once per method, which is the same F that P1.1a wave 2's row measured at max 22 / mean 4.14 across all 166 type declarations in maxon-shv2/ + stdlib/. Closing that knob is the instrument's rung, not this one; I did not touch the instrument. GATES: build csharp exit 0 zero warnings; build shv2 exit 0 zero warnings; specs-shv2 392/0 exit 0 (371 baseline + 21 ported, every one RED before the change); workers=1 vs workers=12 stdout BYTE-IDENTICAL; specs-shv2/fragments/ additions only, ZERO modified; C# suite 3028 passed exit 0; no run exited 101. ⭐ CODEGEN NEUTRALITY PROVEN BY EXPERIMENT RATHER THAN ASSUMED: specs/ shows 3 modified + 2 untracked fragments after a C# suite run, and a clean worktree at UNMODIFIED main (71962e9f7) with its own freshly built bootstrap regenerates BYTE-IDENTICAL content for every one of them — so that churn is PRE-EXISTING drift in main's committed fragments (stale since 2078becf5) and not this rung's. This branch's only maxon-sharp change is a comment-only regeneration of ErrorCode.g.cs from the one corrected doc line. | 343,162 | 654,127 | 1,275,602 | 2,517,807 | 5,002,057 | 9,971,241 |
| 2026-07-16 | P1.1a WAVE 3 — OPTIMIZATION PASS: NOTHING WAS SUPERLINEAR, SO NOTHING WAS CHANGED. Every number on this row is IDENTICAL to the row above because the compiler is byte-identical to it — a VERIFICATION, not a change, and a null result recorded on purpose (the wave-2 and P1.0r optimizers wrote the same kind of row). ⭐⭐ THE RUNG'S ENTIRE ROW RE-VERIFIED BY INDEPENDENT A/B, AND IT REPRODUCES TO THE SINGLE ALLOCATION. This is worth the run because this very log documents TWO rows whose baselines were stale (OPEN.md #22: bin/maxon.exe is gitignored and nothing rebuilds it), so an inherited delta is worth less than a re-measured one. Method: ONE bootstrap built from source (md5 9668dc186dc3b284bea3b5e894904593) COPIED into a detached worktree at unmodified main (71962e9f7) and used to build BOTH shv2 binaries, so the only variable is the rung's commits — immune to the stale-bootstrap trap by construction rather than by hope. Result, branch minus main: +148 +256 +472 +904 +1,768 +3,496, ratios x1.73 x1.84 x1.92 x1.96 x1.98 -> 2.00 on a DOUBLING ladder = LINEAR. The rung's SPLIT is confirmed exactly too: StructLayout.fieldExported (parse +111->+2,715 and signatures +24->+520) = +135 -> +3,235, and checkStructFieldTypes (resolveTypes) = +13 -> +261. phase:regalloc, phase:lex, phase:isel, phase:merge and phase:lowerMaxonToStd are EXACTLY +0 — not "small", zero. Main's own absolutes independently reproduce the wave-2 row (343,014 / 9,967,745), so the trend now has two rebuilt-from-source anchor points and the numbers above are comparable to the two rows beneath them. ⭐ WHY checkStructFieldTypes IS LINEAR IS STRUCTURAL, NOT JUST MEASURED, and that is the load-bearing half: it walks the WHOLE project's struct registry while `resolveTypes(project, module)` takes a MODULE, which is exactly the silhouette of an O(files x structs) quadratic. It is not one. PassPipeline.run dispatches each pass EXACTLY ONCE (PassPipeline.maxon:211) over the single merged maxonModule, and Compiler.maxon:167 is the ONLY PassPipeline.create in the tree — so the walk runs once per COMPILE and costs O(total declared fields), each field one interner array-index plus two hash lookups (resolveNamedType). That distinction is not academic: Compiler.maxon:130's own comment records that `queryProgramSignatures` is a whole-program query sitting UNDER a per-file one and has grown and had removed TWO quadratics of precisely that shape. checkStructFieldTypes sits in the pipeline, not under the per-file query, which is what makes it linear. Measured ratios x1.62 x1.76 x1.86 x1.93 x1.96 — approaching 2.00 FROM BELOW, the signature of a*n+b (the one MapIterator per compile is the b), never above it. ⭐ THE ONE NUMBER ABOVE 2.00, AND IT IS NOT A BEND: regalloc:rewrite BYTES reads x2.204 on the last rung while its ALLOCS read x1.997 — same allocation count, bigger allocations (bytes/alloc 29.9 29.9 30.0 30.1 30.3 33.4, one step). It is capacity rounding in geometric array growth, and the proof is that the bytes ratios BOUNCE BOTH WAYS: regalloc:blockOrder BYTES goes x2.479 at rung 2 and back DOWN to x1.988 at rung 3. A superlinear curve does not bounce back down. It is also IDENTICAL on both sides of the A/B, so it is not this rung's in any case. ALLOCATIONS are the exact, bit-reproducible column and they converge on 2.00 from below in every phase and every regalloc sub-phase. ⚠ WHAT I DELIBERATELY DID NOT CHANGE, EACH WITH A NUMBER RATHER THAN A FEELING. (1) The 0.0026% iterator: the rung's row invited the optimizer to "disagree with a NUMBER" over checkStructFieldTypes' per-layout ArrayIterator. The number is 261 allocations out of 9,971,241 at rung 5. Declined — constant factors are not the mandate, and the neighbouring loop over param types is an iterator too, so an index loop here would buy 0.0026% and cost the consistency. (2) `declareSelfFieldAliases` (Parser.maxon:5029) asks `fieldTypeOf(layout.name, ...)` PER FIELD, which re-hashes the struct name once per field when the layout is already in hand — F redundant map lookups per method entry where 1 would do. Left alone: F is max 22 / mean 4.14 across all 166 type declarations in maxon-shv2/ + stdlib/, so it is a constant factor, AND the call is load-bearing rather than a lookup — `fieldTypeOf` is the door that RE-INTERNS a `named` type into the calling file's own interner (reading the column raw would bind aliases to the SWEEP's type-name ids, which this file's interner cannot resolve). A layout-taking overload would save ~4 byte-compares and add a SECOND adoption door beside the one `emitFieldLoad` already uses. (3) `StructLayout.indexOfField`: OPEN.md #28 — READ, not re-derived. Its O(F^2) lives in a dimension this instrument holds at F=2 by construction; the debt judgment there is measured and sound and I add nothing to it. (4) Method-call resolution is O(1) per call site and has no scan: `methodCallsAt` is a 4-token lookahead plus one scope hash lookup, `requireStructBinding` is an interner array-index plus one `structOf` hash, and `qualifiedMethodName` allocates one name per call site = linear in call sites. ⚠ AND THE HONEST LIMIT, RESTATED BECAUSE IT IS THE THING A FUTURE READER WILL WANT: the instance-method mechanism ITSELF is measured at ZERO here and CANNOT be otherwise — ScaleCorpus emits types with fields and no METHODS, so no generated program declares one, calls one, or names a bare self field. Every allocation in the +148 is the FIELD-DECLARATION path. The receiver, the aliases and the E3014 check are bounded by CONSTRUCTION (reading the code), not by this ladder. Closing that knob is the instrument's rung; I did not touch the instrument. GATES (this pass changed no compiler code): build csharp exit 0 zero warnings; build shv2 exit 0 zero warnings; specs-shv2 395/0 exit 0, memoryLeak false; workers=1 exit 0 vs workers=12 exit 0, stdout BYTE-IDENTICAL; specs-shv2/fragments/ zero modified and zero untracked; no run exited 101. | 343,162 | 654,127 | 1,275,602 | 2,517,807 | 5,002,057 | 9,971,241 |
| 2026-07-16 | P1.1a WAVE 3 — INDEPENDENT REVIEW: +1 allocation per field-access SITE, bought deliberately to make a silent wrong answer unrepresentable. THE FIND: a self-field alias has no SSA value (createSelfField leaves boundValue 0 because a read is a loadIndirect) and ValueId 0 IS THE RECEIVER. parseVariableReference guarded that and its comment states it exactly; parseFieldAccess, parseMethodCall and parseFieldAssignment each fetched the binding, proved its layout, then read binding.boundValue THEMSELVES. One fact, written once, needed in four places. It is reachable: parseTypeReference mints a structRef DIRECTLY for the enclosing type's own name (namesEnclosingType arm), so `var next as Node` inside `type Node` is a structRef, requireStructBinding admitted it, and boundValue was 0. MEASURED on the unguarded build via dump_ir: `return next.a` -> a single `loadRegBaseDisp.word64 r8, [rcx + 0]` = `return self.a`, exit 0 no diagnostic; `return next.readA()` -> a bare `callDirect Node.readA` with NO argument setup at all (the receiver was already in rcx) = `self.readA()`, exit 0 no diagnostic; `next.a = 99` -> E2013 "cannot assign to immutable variable: 'next'", false of an export var and a rejection only by accident of the alias being built mutable:false. THE FIX IS NOT A FOURTH GUARD: requireStructBinding -> requireStructBase now returns the BOX WITH the layout (new `type StructBase`), so it is the ONE reader of a base binding's boundValue and a caller cannot hold a layout it proved beside a box it assumed. That record is the entire cost of this row. WHY THIS SHAPE OF NUMBER IS EXPECTED: StructBase is a Maxon `type` = a heap value, 2 fields = 16 bytes, allocated once per FIELD-ACCESS SITE AT PARSE TIME (not at runtime of the compiled program). Deltas vs the optimizer's row: +80 +160 +320 +640 +1,280 +2,560 allocations, ratios x2.00 x2.00 x2.00 x2.00 x2.00 on a DOUBLING ladder = LINEAR, exactly; bytes +1,280 +2,560 +5,120 +10,240 +20,480 +40,960 = 16.00 bytes/alloc at EVERY rung, flat, which is the record's own size and says the count is one-per-site with no hidden second allocation. It is 0.026% of rung 5's 9,973,801. Every phase other than parse is +0. ACCEPTED rather than avoided, and the alternative was real: adding the isSelfField guard INSIDE the old layout-returning requireStructBinding costs ZERO allocations and fixes the same three bugs — but leaves boundValue readable at three sites, safe only because an earlier throw happened to fire, which is "safe because of an ordering elsewhere", the exact property the project's cure ladder ranks LAST (DERIVE > CHECK > COMMENT; make drift unrepresentable if you can). 0.026%, linear, compile-time-only, in exchange for a class of wrong answer becoming unwritable. Recorded so the next reader sees the trade rather than a mystery. ⚠ THE INSTRUMENT STILL CANNOT SEE THE SUBJECT, unchanged from the optimizer's row and restated because it bounds this row too: ScaleCorpus emits types with fields and NO METHODS, so no generated program declares a method, calls one, or names a bare self field — the +80 is entirely p.x / p.x = v field-access sites in free functions. The three bugs this row paid for are ones the ladder cannot reach; they were found by reading and pinned by specs-shv2/self-field-struct-typed.md (4 cases), whose reds were verified by two separate sabotages. GATES: build csharp exit 0 zero warnings; build shv2 exit 0; specs-shv2 399/0 exit 0 memoryLeak false (395 + 4 new); workers=1 vs workers=12 stdout BYTE-IDENTICAL both exit 0; specs-shv2/fragments/ additions only, ZERO modified — the refactor changed no emitted code; no run exited 101. | 343,242 | 654,287 | 1,275,922 | 2,518,447 | 5,003,337 | 9,973,801 |
| 2026-07-16 | P1.1b WAVE A — OPTIMIZER: scalar `match` (statement + expression; value/or/range patterns; block-arg `gives`/carried-var join). NOTHING SUPERLINEAR FOUND; NO COMPILER CODE CHANGED. MEASURED: all six rungs allocsDelta 0 / bytesDelta 0 vs the Wave 3 row — byte-for-byte identical corpus memory; parse phase scales 62,975→120,639→235,754→465,636→925,096→1,843,629 = x2.00/rung, LINEAR. WHY 0 DELTA — THE INSTRUMENT CANNOT SEE THE SUBJECT: ScaleCorpus has no match knob and no emitter appends `match`/`then`/`gives`, so no generated program enters the match parser; the match path measures ZERO here, same class as instance methods (OPEN #30) and the Wave 3 caveat. Mechanism verified by READING (Parser.maxon parseMatchCommon ~3691): lowering is a CHAIN of one condBranch per arm — O(arms) — with every per-arm helper O(1) (mintPhi/recordBranchEdge/emitCondBranch push, constOf/tagOf are dense-column reads); or-fold + range two-compares are linear in pattern tokens; finalizeMatchMerge's carried×arms nesting is inherent SSA phi-join cost (same shape if/while pay, and equal to the size of the exit-val snapshots already built). THE ONE NON-INHERENT SUPERLINEAR SHAPE, LEFT AS PRECEDENTED BOUNDED DEBT: checkAndRecordDuplicate (E2027, Parser.maxon ~4187) linear-scans `seenValues` per known-const single-value pattern → O(P^2) in ONE match's arm count. Whole-program cost is O(sum P_match^2) = LINEAR for bounded hand-written arm counts; quadratic only in a single-giant-match, the identical degenerate case the project already ACCEPTS for parameter-duplicate detection (byteArrayContains, Parser.maxon:558-560: "parameter lists are tiny... so no membership set is worth its heap object", citing FuncSignature.indexOfParam). NOT converted to a Set: a `Set with ParsedInt` would allocate a heap object PER MATCH — a NEW parse-phase allocation this very instrument tracks — to save a bounded scan, contradicting both that precedent and "don't micro-optimize". The codebase's convention is consistent: hash Set for unbounded token scans (assignedBindingsIn's AssignedNameSet), linear scan for small hand-written lists (params, match arms). GATES: specs-shv2 458/0 exit 0 memoryLeak false; match filter 60/0 exit 0; scale-test exit 0 (corpus grew x1.93..x1.99, floor x1.50); fragments/ clean, working tree clean — no codegen touched. | 343,242 | 654,287 | 1,275,922 | 2,518,447 | 5,003,337 | 9,973,801 |
| 2026-07-16 | P1.1b WAVE B — payload-free enum/union + Color.red + enum match + OPEN #21 ordinal ranges + union. NOTHING SUPERLINEAR: every phase x1.9..x2.2/rung, LINEAR, unchanged in shape from Wave A. SMALL POSITIVE DELTA vs Wave A (allocsDelta 136→212→364→668→1276→2492, bytesDelta 6304→...→118400), growing x1.9/rung = LINEAR, <0.05% of total. WHAT MOVED: the new EnumLayout PARSE-STAGING REGISTRY — one EnumLayoutArray per file on FileParseArtifact (create), one per file in the signature-index sweep (foldDeclaredSignaturesInto), one EnumLayoutMap per compile on Project — plus the extra kindAt lookahead keywordIsMatchCaseName adds to opensBlockAt/blockDepthAfter/blockStatementEndIndex (pure, no alloc). The corpus generates NO enums, so the enum PARSE/LOWER paths measure ZERO here (same instrument-blindness as match at Wave A); the delta is purely the empty per-file registry arrays a new parse-staging family costs — the identical per-file cost structs/aliases/bindings each already pay (StructLayoutArray, RangedTypeAliasArray, TopLevelDeclArray on the same artifact). NOT lazily allocated: structs do not lazy-allocate their per-file array either, and deviating would be an inconsistency to save a bounded constant. Mechanism verified by READING: enum lowering is a CHAIN of one condBranch per arm (O(arms), reusing Wave A's engine); the #21 range emits an OR (O(covered)) or a two-compare tag range with an O(cases) coverage scan — all linear in the enum's case count, which is a hand-written bound. GATES: specs-shv2 506/0 exit 0 memoryLeak false; enum filter 43/0; worker-count invariance byte-identical (j1==j12); fragments additions only, 0 modified; ErrorCode.g.cs + selfhosted registry unchanged. | 343,378 | 654,499 | 1,276,286 | 2,519,115 | 5,004,613 | 9,976,293 |
| 2026-07-16 | P1.2 WAVE A — string LITERALS + print + ==. NOTHING SUPERLINEAR: every phase x1.99..x2.20/rung, LINEAR, unchanged in shape from P1.1b (corpus code grows x1.93..x1.99). SMALL POSITIVE LINEAR DELTA vs P1.1b Wave B (allocsDelta 70->102->166->294->550->1062, bytesDelta 3344->...->50960, x1.9/rung = LINEAR, <0.02% of total). WHAT MOVED, and it is the ONLY thing that can: the corpus generates NO string literal, NO print, NO string == (same instrument-blindness the manifest documents for each un-generated construct), so the rung's feature paths — rdata record/blob registration, buffer fixup, __str_eq/__print_string install, stringLiteral/rdataAddr/osWriteStdout lowering — measure EXACTLY ZERO here. The delta is `scanRuntimeUsage` REPLACING `moduleUsesHeap`: the old scan EARLY-RETURNED on the first __mm_ call (the corpus allocates structs early), the new one must answer THREE runtime-usage questions (heap / print / streq) and so walks the whole module, paying the per-function/per-block loop cost for the functions the early return used to skip — O(program), linear, one scan per compile. This is the deliberate one-walk-not-three design the contract asked for; the price is <0.02% and linear. Mechanism verified by READING: __str_eq is a length check + a byte loop (O(bytes), a phi-carried index reusing the parser's on-the-fly-SSA loop shape); __print_string is straight-line (two loads + one osWriteStdout); both are installed ONLY when referenced, so a print-free/==-free program carries neither (verified: a print+== binary has __print_string/__str_eq and ZERO __mm_ symbols). GATES: specs-shv2 523/0 exit 0 memoryLeak false; string filter 15/0; worker-count invariance byte-identical (w1==w12); fragments additions only, 0 modified; error-code registry unchanged (no new codes). | 343,448 | 654,601 | 1,276,452 | 2,519,409 | 5,005,163 | 9,977,355 |
| 2026-07-16 | P1.2 WAVE A optimize — killed the ONLY superlinear the rung added: GlobalDataTable.registerRdataConstant computed each fresh .rdata constant's offset via rdataSizeBytes(), an O(entries) re-sum of every prior payload, so registering N distinct constants was O(N^2). A String literal registers TWO constants (blob + record) and literals-per-program are UNBOUNDED, so this is a real O(N^2) reachable at self-host scale (thousands of literals) — exactly the debt the field's own comment flagged 'if .rdata ever holds thousands of entries (P1.2 strings)'. Fix: replaced the re-sum with a running-total field rdataSizeSoFar advanced beside each push; O(N^2)->O(N), offsets bit-identical by construction, guarded by rdataImage's existing drift assert. INSTRUMENT-BLIND: the re-sum ALLOCATES NOTHING, so it is a compile-TIME cost this memory-only instrument cannot see, AND the corpus emits no strings — bounded by READING, not measured. The other three candidates are LINEAR by inspection: scanRuntimeUsage O(program) one walk; patchStringBufferFixups O(literals) one pass; __str_eq is runtime O(len), correct by nature. WHAT MOVED here: allocsDelta 0 every rung (count identical); bytesDelta +552,+808,+1320,+2344,+4392,+8488 = exactly 8*(1+functionCount) per rung. Attribution: the new 8-byte field rides on EVERY GlobalDataTable instance — the one project.globalData (phase:load, +8 constant) and each per-function FunctionCodeChunk.rdataConstants EMPTY placeholder that emitFunctionChunk allocates (phase:encode, +8*functions). Linear, +0.0026%->+0.0011% (shrinking fraction) — the constant-factor price of the tidy O(n) fix. GATES: build csharp/shv2 exit 0 zero warnings; specs-shv2 523/0 exit 0 memoryLeak false; worker-count invariance w1==w12 byte-identical; fragments additions-only 0 modified; error-code registry unchanged. | 343,448 | 654,601 | 1,276,452 | 2,519,409 | 5,005,163 | 9,977,355 |
| 2026-07-16 | P1.2 wave B — primitive interpolation (int/bool/String/expr) + statement-scoped temp-drop + sparse owned-heap provenance. The corpus emits NO interpolation, so it exercises the drop machinery's per-statement overhead, not the interp path itself. After guarding parseStatement's drain on pendingTempDrops.count()>0 (drainPendingTemps reallocates the list, so an unconditional per-statement call was one alloc on every line of the compiler), the residual delta vs P1.2 wave A is the two per-function ValueIdArray.create() calls in the parser's reset (pendingTempDrops + ownedHeapValues), O(functions), linear. All phase exponents ~x2.0 (linear); no superlinear introduced. | 343,802 | 655,203 | 1,277,550 | 2,521,499 | 5,009,237 | 9,985,397 |
| 2026-07-16 | value-class column grow-on-write (setValueClass growFilled) so it covers a fused compare's flags-only boolean when that is a function's highest ValueId; growFilled fires only for that shape and is amortized-linear. | 343,802 | 655,203 | 1,277,550 | 2,521,499 | 5,009,237 | 9,985,397 |
| 2026-07-16 | owned-heap provenance moved from a sparse list (ownedHeapValues, scanned once per binding by valueIsOwnedHeap — O(N^2) for a function with N bound interpolations) to a per-value bool column (valueOwnsHeap, seeded false in mintValue/recordValueType like valueConstKnown, set true in trackOwnedTemp, read O(1)). The delta here (+2.5KB@r0 .. +112KB@r5, doubling per rung => linear) is that one bool per minted value; it buys removal of the per-binding linear scan. Byte-identical codegen (564/0, fragments clean); parse stays x2.06. | 343,995 | 655,490 | 1,278,016 | 2,522,305 | 5,010,707 | 9,988,175 |
| 2026-07-16 | Block-scope ownership drops: parseBlockBody now emits __mm_decref for block-local owned bindings on fall-through, and break/continue drop down to the enclosing loop's owned height (LoopContext.ownedFloor), and emitScopeDrops gained a floor param. The corpus binds structs inside if/while bodies that PREVIOUSLY LEAKED (no drop was emitted; scale-test compiles but never runs, so it never flagged them). Emitting the now-correct drops adds compile-time memory that is LINEAR in the count of such bindings: allocs delta doubles per rung (+1,340@r0 .. +42,278@r5), phase:parse stays x2.06, regalloc x2.06. The one large byte move — regalloc:criticalEdges r5 +2.16MB — is +1 ALLOCATION (191,171->191,172), a single growable buffer crossing a power-of-2 doubling threshold on the slightly larger op stream, NOT superlinear growth. codeBytes rose too (the emitted programs now contain the decref calls). 577/0, memoryLeak false, fragments additions-only. | 345,335 | 658,159 | 1,283,318 | 2,532,899 | 5,031,857 | 10,030,453 |
| 2026-07-17 | P1.2 Wave C: static single-owner MOVES + use-after-move (OPEN #41). VarInfo gains a `movedFrom` bool; `let u = t` / `s = t` from a bare owned binding now MOVES (source poisoned, its drop skipped, a later read = E3102 use-after-move). Memory moved DOWN and it is EXPLAINED: `parseVariableReference` no longer resolves a local via `Scope.lookupValue`'s throw-on-miss — it checks `scope.contains` first — so a top-level binding reference (every global/constant read) no longer allocates AND catches a `ScopeLookupError.notFound(String.from(name))`. That is ~2 allocs saved per global read, LINEAR in the corpus's global reads: allocsDelta -192@r0 .. -6144@r5 (doubles per rung), bytesDelta -1536@r0 .. -63040@r5. No exponent moved (parse x2.06, regalloc x2.06; regalloc:criticalEdges x2.31 is the known power-of-2 buffer threshold, not superlinear). `poisonMoveSource` is an O(ownedBindings) scan per owned bind/assign that allocates nothing, so it does not appear here. 595/0, memoryLeak false, worker-invariant byte-identical, fragments additions-only. | 345,143 | 657,775 | 1,282,550 | 2,531,363 | 5,028,785 | 10,024,309 |
| 2026-07-17 | optimize P1.2 Wave C moves: gate the move-source poison on a bare-local RHS. `poisonMoveSource` was an O(ownedBindings) linear scan run on EVERY owned bind (parseBinding) and reassignment (reassignBindingValue) — including plain consumes (`let u = build()`) that scanned the whole list and found nothing → O(N^2) compile TIME for a function accumulating N owned bindings with ~N bind/assign ops. It now runs only when the RHS was a bare reference to a LOCAL (`let u = t` / `s = t`), the sole shape that can move an owned value out of an existing binding; a new O(1) parse-level gate `rhsWasBareLocalReference` (single-identifier-token + scope.contains + not isSelfField) decides, so every consume — the common case — skips the scan and the bind path is linear. BYTE-IDENTICAL: the scan is a proven no-op for any non-bare-local RHS, so gating it out changes no behaviour (595/0, memoryLeak false, fragments additions-only; the 4 move probes unchanged, incl. the loop-carried-var-as-move-source case which stays exit 0 "v1" — the reason the poison keeps its membership scan rather than switching to a valueIsOwned gate). Memory UNCHANGED at every rung (allocsDelta 0, bytesDelta 0): the scan and the gate allocate nothing, so this TIME fix is invisible to the memory instrument by design — recorded so the trend explains why an ownership-path commit moved no bytes. No exponent moved (parse x2.06, regalloc x2.06). Residual: an actual bare-local MOVE still scans (rare), and the assign path retains its pre-existing bindingIsOwned scan; a fully-O(1) poison would need either a second copy of owned-set membership (forbidden drift) or a valueIsOwned gate (alters loop-phi drop bookkeeping), so the gate is the correct scoping. | 345,143 | 657,775 | 1,282,550 | 2,531,363 | 5,028,785 | 10,024,309 |
| 2026-07-17 | OPEN #42 fix: use-after-move (E3102) now enforced at field access / field store / method receiver, not only bare reads. Consolidated the `movedFrom` check into ONE shared guard `Parser.requireBindingLive(binding, nameTok)` called from `parseVariableReference` (migrated) and `requireStructBase` (the single funnel for `p.x`, `p.x = …`, `p.m()`). Memory UNCHANGED at every rung (allocsDelta 0, bytesDelta 0): the guard is a pure front-end bool test on the base binding plus an early throw on the reject path — it allocates nothing, and the scale corpus generates no moved-then-used programs, so the guard never fires here. Recorded so the trend explains why a front-end-diagnostic commit moved no bytes. No exponent moved (parse x2.06, regalloc x2.06). | 345,143 | 657,775 | 1,282,550 | 2,531,363 | 5,028,785 | 10,024,309 |
| 2026-07-17 | P1.2 Wave D (String.append): +16 bytes FLAT at every rung, allocsDelta 0, no exponent moved (parse x2.06, regalloc x2.06 unchanged). The ONLY heap change the scale corpus sees is RuntimeUsage growing by two bool fields (usesStrAppend/usesStrDecref) — one struct per compile, so a constant +16 bytes independent of program size. The corpus generates no String.append and no var-string-literal, so __str_append/__str_decref and the eager-promotion path are never exercised here; their cost is measured off-instrument by the append + leak-gate specs (rc-string-concat-loop-no-leak: alloc/free balanced, exit 5, no leak). The drop-routing change (owned-String drops now call __str_decref, not __mm_decref) is a call-target swap that allocates nothing. | 345,143 | 657,775 | 1,282,550 | 2,531,363 | 5,028,785 | 10,024,309 |
| 2026-07-17 | P1.3 slice 1 (scalar-payload unions): allocsDelta -1 / bytesDelta -2 FLAT at every rung vs P1.2 Wave D, no exponent moved — every phase ratio ~x2.0 (parse x2.06, regalloc x2.06, signatures x2.0), all linear. The corpus is BOTH match-blind and union-blind (OPEN #30), so it exercises NONE of the new boxed-union paths (construct/emitEnumBox, the tag-load dispatch, payload extraction, the trivial-destructor drop) nor union `==` rejection; the flat -1/-2 is a one-time constant delta on the shared parse path, not a scaling change. The feature's superlinear surface was checked by reading, not by the corpus: every new scan (emitEnumBox validate/store/zero-fill, parseCasePayloadBindings, maxPayloadArity/isBoxed/boxSizeBytes, remapEnumLayoutPayloadTypes over cases x fields) is bounded by per-ENUM sizes (cases, arity, fields-per-case) — constants vs program size, so total work is O(program), linear. The implementer's two committed regression fixes are present and correct: (1) valueIsUnion's `named`-tag pre-check before enumMatchFor, which otherwise boxed two throwaway DeclaredEnum.undeclared per compare (had charged +1257/rung, doubling); (2) parseDottedPrimary's non-allocating containsEnum guard before enumOf, which otherwise boxed a DeclaredEnum per Point.create(...). OPTIMIZATION PASS: the one flagged residual — an empty PayloadBindingArray allocated per match arm — is REAL and was measured off-corpus with --metrics (1 alloc + ~40B per scalar arm; 2 per single-case enum arm from arm-loop + emitEnumPatternAtom): a 1000-arm scalar match dropped 998 allocs/39,904 bytes, a 200-case enum match 398 allocs/15,904 bytes, both frees in lockstep (leak-free). LEFT IN PLACE: the obvious fix (a shared empty-array sentinel on the Parser) REGRESSES the corpus-VISIBLE scaling path — Parser.create runs per top-level constant (SignatureIndex.walkConstInitializer, once per const-decl), so a sentinel allocated in the constructor adds one allocation per declaration; measured as an allocsDelta climbing +101/+173/+317/+605/+1181/+2333 across the rungs (~linear in program size, concentrated in phase:signatures), and reverted. A module-level singleton is unavailable (top-level const initializers may not call functions). The residual matches the existing per-arm matchEdgeArgs idiom in the same loop and costs nothing on the declaration-heavy path the instrument measures; removing it trades a real scaling cost for an unmeasurable off-corpus constant factor, so per the mandate it is left. Suite 615/0 exit 0 leak-free, fragments clean. | 345,142 | 657,774 | 1,282,549 | 2,531,362 | 5,028,784 | 10,024,308 |
| 2026-07-17 | P1.3 slice 2 (MANAGED payloads — String/struct in unions): allocsDelta FLAT +2 at every rung, bytesDelta +4,672 doubling to +133,632 (~linear). BOTH explained. (1) The +2 flat allocs are the per-compile empty `RuntimeUsage.neededUnionDestructors` ByteArrayArray (header + buffer) — one constant allocation regardless of program size; `noteUnionDestructorUsage`/`installUnionDestructors` iterate `project.enumTypes` once per compile and the corpus is union-blind, so their loops allocate nothing. (2) The doubling bytesDelta with FLAT allocs is the signature of a per-instance struct that grew: `VarInfo` gained one `partiallyMoved` bool (P1.3 slice 2 — a binding-match that moved a managed payload out is E3102-on-read but still dropped, distinct from `movedFrom` which skips the drop). VarInfos scale with bindings (~program size), so +8 aligned bytes each doubles per rung while the COUNT is unchanged — a necessary, minimal, linear cost of the feature. Every phase ratio ~x2.0 (parse x2.06, regalloc x2.06, liveness x2.06, all linear); nothing bent. ONE regression found + fixed BEFORE this measurement: `decrefCalleeFor` (the new drop router) called `enumMatchFor` on EVERY dropped owned value, boxing a throwaway `DeclaredEnum.undeclared` per struct/String drop — the exact hot-path allocation `valueIsUnion` documents; it had charged allocsDelta +42 doubling to +1282 (linear). Fixed with the same `tag == named` pre-check `valueIsUnion` uses (a union box is the only `named`-tagged drop; structs are `structRef`, interp buffers `integer`), collapsing that delta to 0. The feature's superlinear surface was checked by reading: the classifier (classifyUnionPayload) is 3 Map lookups per payload FIELD, bounded by per-union declaration size (cases x fields = constants vs program), and the destructor synthesis is per-union (once), so all work is O(program). Suite 631/0 exit 0 leak-free (630 + 1 loop probe not in suite); worker-invariance byte-identical; fragments additions-only. | 345,144 | 657,776 | 1,282,551 | 2,531,364 | 5,028,786 | 10,024,310 |
| 2026-07-17 | P1.3 slice 2 OPTIMIZE — killed an O(managed-unions x drop-sites) quadratic on the union-destructor install path; the corpus cannot see it, so it was found by READING and confirmed by a targeted probe. THE QUADRATIC: `RuntimeUsage.neededUnionDestructors` collected one entry PER `__destruct_<U>` DROP SITE (a `ByteArrayArray`, un-deduplicated), and BOTH consumers — `noteUnionDestructorUsage` and `installUnionDestructors` — walked EVERY union (`project.enumTypes`) and asked "is this union's destructor referenced?" via `bytesArrayContains`, a LINEAR SCAN of that per-drop-site list. That is O(managed-unions x drop-sites): both dimensions grow linearly with program size, so a program with many distinct managed unions is quadratic — the exact "linear lookup inside a linear walk" the mandate hunts, and the identical shape `IrModule.dropFunctionsByName` already fixed by interning its drop list into a Set. FIX: `neededUnionDestructors` is now a `Set with ByteArray` (new `UnionDestructorSet` typealias); `recordCallUsage` INSERTS (auto-dedup, <=U distinct entries not D duplicated), both consumers query `.contains` O(1), and the dead `bytesArrayContains` is deleted. Net O(unions x drops) -> O(unions + drops), linear. WHAT MOVED, AND IT IS A CONSTANT NOT A CURVE: allocsDelta +3 / bytesDelta +120 FLAT at every rung (rung0 == rung5), frees +3 in lockstep (leak-free). It is entirely the empty `Set` container replacing the empty `ByteArrayArray` in `RuntimeUsage.create()` — one per compile, does NOT scale, in no phase's per-rung count (every phase exponent unchanged: parse x2.06, regalloc x2.06, liveness x2.06). A flat constant traded for a growth-curve fix, the direction the mandate endorses ("constants are not the mandate; growth curves are"). INSTRUMENT-BLIND BY CONSTRUCTION: the corpus generates no enums AND no unions, so `project.enumTypes` is EMPTY and both consumers' loops iterate zero times here — the quadratic is unreachable by this ladder, which is why the win is invisible and the only measurable effect is the container swap. Confirmed correct off-corpus by (1) the full suite: 631/0 exit 0 leak-free, every union-managed-payload drop/leak-free test (the destructor-synthesis path) green; (2) a targeted probe — two DISTINCT managed unions (Msg/String, Note/String), Msg dropped at TWO sites — compiled and ran exit 7 with NO leak and NO missing-`__destruct_*` link error, proving both destructors synthesized, the dedup correct (one `__destruct_Msg` for two drops), and membership gating right. RESIDUAL LEFT, WITH A NUMBER NOT A FEELING: `decrefCalleeFor` re-runs `unionHasManagedField(layout)` (which allocates a `PayloadClass` box per classified field) at EVERY union-box drop site — a per-reference recompute of the classifier. It is realistically LINEAR (total = sum over drops of that union's declaration size = O(drops x bounded-decl-size)), superlinear only for a single pathological giant-union dropped many times; a per-compile managed-union memo would remove the recompute but is a constant-factor win on a linear curve (the mandate says don't chase it) on a correctness-critical path the instrument cannot measure, so it is left. GATES: build shv2 exit 0 zero warnings; specs-shv2 631/0 exit 0 memoryLeak false; git status specs-shv2/fragments/ EMPTY (0 modified, byte-identical codegen — a pure membership optimization emits the same destructors); probe exit 7 no leak. | 345,147 | 657,779 | 1,282,554 | 2,531,367 | 5,028,789 | 10,024,313 |
| 2026-07-18 | P1.3 fix (match owned-value flow, fa685a7a1) — INDEPENDENT OPTIMIZE READING, no code change. The corrective fix (temp-scrutinee enrollment + owned-`gives` transfer to the result phi) moves NOTHING on the ladder: every rung's allocs/bytes match the previous logged run EXACTLY (allocsDelta/bytesDelta 0 at all 6 rungs), all phases x2.0 (parse x1.99, regalloc x1.99, liveness x1.99 by rung4->5). Expected, and confirmed by READING not measurement: the corpus generates no unions and no `match` with an owned scrutinee/gives, so every new path is instrument-invisible. LINEARITY ARGUMENT (whole-program): all new scans walk per-STATEMENT or per-MATCH collections, never a program-sized one. (1) `pendingTempsContain(scrut)` and the scrutinee-enrollment `removeFromPendingTemps` run ONCE per match over `pendingTempDrops`, which is drained per statement (Parser.maxon:3330) -> its length is the statement-local owned-temp count, a source constant. (2) `resolveGivesOwnership` (two loops), the `finalGives` build, and the `finalizeMatchMerge` edge loop are each O(reachingArms), once per match; `valueIsOwned`/`tagOf` are O(1) column `.get`s. A match's arm count is a HAND-WRITTEN constant (the #32 E2027 O(P^2) precedent: bounded by a source constant is linear whole-program), so these sum to O(total arms) = linear. (3) `transferGiveToPhi` per gives-arm does `removeFromPendingTemps` O(pending) + `poisonMoveSource` O(ownedBindings); `ownedBindings` is bounded by the live owned bindings in the scope stack (a per-function constant, drained per block), so the whole-program cost is O(total gives-arms x bounded-bindings) = linear, the SAME class as the pre-existing move-poison sites. RESIDUAL LEFT (constant, not curve, with a reason not a feeling): `transferGiveToPhi` calls `poisonMoveSource` UNCONDITIONALLY for every owned give, unlike the pre-existing move sites which gate that O(ownedBindings) scan behind the syntactic `rhsWasBareLocalReference` check (the note at Parser.maxon:4179-4223 explains that gate exists precisely to skip the scan-and-find-nothing). For the common owned-TEMP give (an interpolation/struct box, which lives in `pendingTempDrops`, disjoint from `ownedBindings`) the poison scan is guaranteed to find nothing. This does NOT change the linear whole-program class (only the constant factor), and cheaply gating it would trade one bounded scan (`pendingTempsContain`) for another (`poisonMoveSource`) with no asymptotic gain and extra code on a correctness-critical path the instrument cannot see, so per the mandate ("constants are not the mandate; growth curves are") it is LEFT. GATES: build shv2 exit 0 zero warnings; specs-shv2 641/0 exit 0 memoryLeak false; scale-test exit 0, allocsDelta/bytesDelta 0 every rung. | 345,147 | 657,779 | 1,282,554 | 2,531,367 | 5,028,789 | 10,024,313 |
| 2026-07-18 | P1.4a Wave 1 — cross-call struct/union ownership (BORROW params + ADOPT returns, OPEN #44 closed). allocsDelta +238/+398/+718/+1358/+2638/+5198, bytesDelta +7384..+162136 (rung0..5); ratios 1.67 to 1.97 CONVERGING to x2.0 = LINEAR, tracking the corpus struct-count growth (structAllocs doubles per rung, same asymptote as emitted-code x1.93 to x1.99). NO superlinearity. WHAT moved: struct/union values crossing a CALL now ride the SAME owned-temp machinery String/union temps already use, extended to structs: (1) emitStructLiteral enrolls the box via trackOwnedTemp (a pendingTempDrops push) so an unbound Self{} / struct-literal-as-arg drops at statement end; (2) mintOwnedCallResult ADOPTS a struct/boxed-union call result via trackOwnedTemp so let p=makePair() frees it once; (3) parseReturnStatement's move-out now fires for owned struct/union returns (removeFromOwnedBindings + removeFromPendingTemps, each a filter-rebuild) so return t / return Self{} / return U.case(x) leave the callee's drop set. Each is an O(1) push or an O(bounded) rebuild PER struct/return; pendingTempDrops is per-STATEMENT and ownedBindings per-BLOCK (both bounded by source constants), so whole-program LINEAR — the SAME class as the pre-existing String/union owned-temp sites. resolveNamedStruct (per param/return/call, named to structRef) is allocation-free (nameOf returns a reference, containsStruct is a Map.contains) except when it constructs a structRef for a genuine named struct type, which the corpus never does (its structs are Self-returning factories, already structRef). structFieldIsManaged is answered inside SignatureIndex, contains-guarded and allocation-free (corpus-invisible: its struct fields are bare int, tag not named). GATES: build exit 0 zero warnings; specs-shv2 647/0 (641 baseline + 6 enabled) exit 0 memoryLeak FALSE; workers=1 equals workers=12 byte-identical; fragments additions-only (plus intended error.union-return to union-return rename); scale-test exit 0. | 345,385 | 658,177 | 1,283,272 | 2,532,725 | 5,031,427 | 10,029,511 |
| 2026-07-18 | P1.4a Wave 1 OPTIMIZATION PASS (independent) — NO CODE CHANGE. Verdict: no superlinear algorithm introduced; every new path is O(1) hashed-map/column or a loop bounded by a struct's hand-written field count (#28/#32 precedent), summing to whole-program LINEAR. Confirmed BOTH ways. (a) Static: resolveNamedStruct/All/Return (3 parse sites) is O(1) per struct reference — containsStruct is a Map.contains (hashed on the name bytes, a constant), typeNames.nameOf is an array index; the *All fold is bounded by a function's param count. structFieldIsManaged is O(1) per field (all hashed structTypes/enumTypes .contains/.get + a typeNames array index inside the index's own interner, no adoptType, no DeclaredStruct/DeclaredEnum box); rejectManagedStructFields is O(fieldCount) per construct site, fieldCount a per-struct declaration constant. valueIsBoxedUnion is O(1) — the exact mirror of valueIsUnion with the SAME named-tag hot-path allocation guard, called once per return and once per call result (short-circuited past when the tag is structRef). valueIsOwnedHeap is one BoolArray.get, strictly CHEAPER than the deleted valueIsOwned (which added a tagIsStructRef OR). (b) Measured, allocsDelta 0 on every rung — this tree reproduces the implementer's logged run bit-for-bit, so I measured exactly the committed state: total allocs x1.906/1.950/1.974/1.987/1.993, phase:parse (where resolveNamedStruct + emitStructLiteral trackOwnedTemp + mintOwnedCallResult adoption + the return move-out live) x1.913/1.952/1.974/1.986/1.993, phase:signatures (structFieldIsManaged reads) x1.815/1.896/1.943/1.969/1.984 — every phase converges to x2.0 MONOTONICALLY FROM BELOW (the fixed-per-rung-overhead-amortizing signature); nothing overshoots, so no phase bends superlinear. TWO residuals LEFT deliberately, both constant-factor and neither superlinear: (1) rejectManagedStructFields calls structFieldIsManaged(name, i) per field, which re-fetches the SAME layout by name (structTypes.get) once per field instead of once per struct — a CPU redundancy bounded by the struct's declared field count (#28/#32), that allocates nothing and is invisible to this memory instrument; classification must stay inside SignatureIndex because it reads that index's private typeNames/enumTypes interner. (2) resolveNamedStructAll allocates one throwaway MaxonTypeArray per function even when no param is a struct — a linear per-function constant already inside the logged +238..+5198 delta. Per the mandate (constant factors are not the target; a tidy O(n) beats a clever one), both are left as-is. Gates re-run on this tree: build exit 0 zero warnings; specs-shv2 647/0 exit 0 memoryLeak FALSE; git status clean, fragments unmoved. | 345,385 | 658,177 | 1,283,272 | 2,532,725 | 5,031,427 | 10,029,511 |
| 2026-07-18 | P1.4a wave 2 (managed struct fields + destructor cascade + param-consume analysis + chained field access) | 346,245 | 659,622 | 1,285,878 | 2,537,644 | 5,040,963 | 10,048,272 |
| 2026-07-18 | P1.4a wave 2 OPTIMIZATION PASS (independent) — NO CODE CHANGE. Verdict: nothing superlinear over a compiler-sized collection; deltas 0 at every rung vs the implementer's own wave-2 row, confirming the same binary. The CONSUME ANALYSIS is genuinely corpus-exercised, not inert: every generated struct's static create(x,y) is a receiver-less function whose body is Self{x:x,y:y}, so scanConsumeBits records it with bits[true,true], recordParamConsume folds it into the parse-memo hash, and every create call site runs applyCallerConsume's positional+labelled slotting — and all of it stays linear: phase:signatures x1.99 (the added body walk + colon lookups, each colon O(this func's own param count), total O(total tokens)), phase:merge x2.00 (foldFile threads paramConsumes by name, no interner remap), phase:parse x2.06 (applyCallerConsume/transferConsumedArgs slot O(call arity) per consuming call; resolveFieldChain recurses O(chain depth); bindParameters O(params) with O(1) paramIsConsumed+valueIsManagedHeap). Every per-reference cost is bounded by ONE declaration's or call's own size — the #28/#32 precedent — so it is O(program), linear. The DESTRUCTOR CASCADE CLOSURE is NOT corpus-exercised: corpus structs are scalar-only (x int, y int), so noteDestructorUsage/closeDestructorNeeds see an empty needed-set and phase:runtimeAugment reads x1.57 flat AND allocation-free — the instrument structurally cannot see this path's scaling. Confirmed linear by STATIC reasoning instead: noteDestructorUsage is called ONCE per compile (Compiler.maxon:168), never per drop-site/reference; closeDestructorNeeds is a MONOTONE fixpoint whose pass count is bounded by struct NESTING DEPTH (a source constant — a flat set of managed structs converges in 1 pass, only struct-in-struct nesting adds passes), each pass O(types x fieldCount), so O(depth x types x fieldCount) = O(program) once. The theoretical worst case (one maximally-nested chain, depth ~= types → O(types^2)) needs a degenerate program, is allocation-light (invisible here), and is bounded by source structure exactly like #28/#32 — LEFT in place; a worklist closure would be a micro-optimization on an unmeasurable path with behavior risk, which the mandate forbids. regalloc:criticalEdges x2.31 is pre-existing regalloc (untouched by this wave, delta 0). Gates: shv2 suite 657/0 exit 0 memoryLeak FALSE; specs-shv2/fragments clean; build 0 warnings. | 346,245 | 659,622 | 1,285,878 | 2,537,644 | 5,040,963 | 10,048,272 |
| 2026-07-18 | P1.4a-wave-2 CORRECTIVE: path-sensitive conditional-move DROPS. Verdict: LINEAR, nothing superlinear on the realistic corpus. The fix reconciles VarInfo.movedFrom at every if/if-else/match join (captureMoveMark + restoreMoveMark + reconcileMovesAtMerge, mirroring snapshotBindings/restoreBindings for the drop-gating bit) and rejects an outer-binding move that escapes a loop. Measured constant-factor increase vs the wave-2 baseline (row 201): total bytes +133,312/+266,240/+532,096/+1,063,808/+2,127,232/+4,254,080 across rungs 0-5, each DOUBLING with the doubling corpus, so it is a LINEAR per-branch cost (parse allocs x1.99, parse bytes x2.06 — identical curve to the baseline's x2.06). The cost is the MoveMark capture: captureMoveMark walks ownedBindings (O(owned-in-scope)) and allocates BoolArray+ValueIdArray, called ~2-3x per if branch and once per reaching match arm. On the corpus the branch-heavy functions (longFunction/deepBlocks) carry only SCALAR accumulators, so ownedBindings is ~0 there and each capture is O(1)/empty — the instrument sees the empty-array per-branch overhead, which is O(branches)=O(program), linear. The THEORETICAL worst case is a single function with N owned Strings AND N branches, where per-branch O(owned) sums to O(N^2); it needs a degenerate function, is bounded by source structure exactly like the #28/#32/destructor-cascade precedents, and the corpus (scalar-only struct fields, scalar branch accumulators) structurally cannot exercise it — LEFT in place per the mandate (a tidy O(n) beats a clever delta-tracker; an empty-mark cache is a constant-factor micro-opt with added parser state). Gates: build exit 0 zero warnings; shv2 suite 670/0 exit 0 memoryLeak FALSE; workers=1==workers=12 byte-identical; fragments additions-only. | 350,561 | 668,242 | 1,303,106 | 2,572,088 | 5,109,839 | 10,186,012 |
| 2026-07-18 | P1.4a wave 2 CORRECTIVE OPTIMIZATION PASS (independent, over 2bee3d9b8 + 411c80338) — NO CODE CHANGE. Verdict: nothing superlinear over a compiler-sized collection; allocsDelta/bytesDelta 0 at every rung vs the implementer's own corrective row, so this tree reproduces that binary bit-for-bit and I measured exactly the committed state. The new path is path-sensitive drop reconciliation: captureMoveMark/restoreMoveMark snapshot+rewind the movedFrom bit of every owned binding around a branch, and reconcileMovesAtMerge settles it at each if/if-else/match join. All of it rides ownedBindings, which is a SCOPE-DRAINED STACK — openBlock records its height (Parser.maxon:3745), closeBlock pops back to that mark (3766) — so it is bounded by the CURRENT FUNCTION'S nesting depth, never a compiler-sized accumulator, and is fully drained when the function's outermost block closes. Per-op costs: captureMoveMark/restoreMoveMark O(owned-in-scope) per branch (~2-3x per if, once per fresh match arm); reconcileMovesAtMerge O(owned x edges) per merge (edges=2 for if, =reaching-arm-count for match — both SOURCE CONSTANTS); thenMovesLiveBinding O(owned) once per if-no-else; bindingIndexEscapesLoop O(1) (top-of-loop-stack ownedFloor compare). This is the declared TWIN of the pre-existing snapshotBindings/restoreBindings machinery (same O(carried x branches) shape over assignedBindings), so it inherits that design's accepted linearity: every per-branch/per-merge cost is bounded by one function's own structure, summing to O(program) = LINEAR. The THEORETICAL worst case is one degenerate function with N owned bindings AND N nested branches, where per-branch O(owned) sums to O(N^2) — bounded by source structure exactly like the #28/#32 precedents, and the corpus (scalar branch accumulators in longFunction/deepBlocks, so ownedBindings is ~0 there; scalar-only struct fields) structurally cannot exercise it. The empty-array-per-branch allocation (captureMoveMark allocates BoolArray+ValueIdArray even when ownedBindings is empty, like Slice-1's throwaway array) is a per-branch CONSTANT FACTOR = O(branches) = O(program), linear, and is already inside the corrective's logged +133,312..+4,254,080-byte delta; LEFT per the mandate — removing it needs a shared empty-mark singleton, i.e. a fast-path fork the mandate forbids, and constant factors are not the target (a tidy O(n) beats a clever cache with added parser state). Readings: phase:parse allocs x1.92/1.96/1.98/1.99/1.99 CONVERGING to x2.0 from below (the fixed-overhead-amortizing linear signature), phase:parse bytes STABLE x2.06 across every rung (a stable ratio is linear; a superlinear bends UPWARD rung to rung — it does not). regalloc:criticalEdges x2.31 is pre-existing regalloc (untouched by this rung, delta 0 — the known power-of-2 buffer threshold). Gates re-run on this tree: build exit 0 zero warnings; specs-shv2 670/0 exit 0 memoryLeak FALSE; no code change so codegen is byte-identical by construction and specs-shv2/fragments is unmoved; git status clean. | 350,561 | 668,242 | 1,303,106 | 2,572,088 | 5,109,839 | 10,186,012 |
| 2026-07-18 | P1.4b WAVE 1 (scalar error handling) OPTIMIZATION PASS (independent, over bbbd9d74b) -- NOTHING SUPERLINEAR, NO CODE CHANGE. The delta THIS ROW PRINTS (+6,570..+201,439 allocs / +174,704..+5,124,336 bytes) is a STALE-BASELINE ARTIFACT, not the rung's cost: the prior logged row's absolutes (350,561 / 10,186,012) are LOWER than what the rung's own PARENT (287ba1cdb, P1.4a wave 2 CLOSED) actually produces when rebuilt from source (357,096 / 10,387,292) -- the OPEN #22 / 2026-07-16 stale-bin trap, where a logged baseline measured a compiler that no longer exists. So I A/B'd it the immune way: ONE bootstrap (bin/maxon.exe) built BOTH shv2 binaries -- parent 287ba1cdb (registry hash fb8d0957a4a3bbd3) and branch bbbd9d74b (hash c773be82c0fbcdea) -- the only variable the rung's own diff. TRUE cost, branch minus parent: ALLOCS +35 +39 +47 +63 +95 +159 (rung 0..5); BYTES +53,928 +104,888 +206,808 +410,648 +818,328 +1,633,688. Both LINEAR (each doubles across the doubling ladder). The rung's real allocation footprint is +159 at rung 5 = 0.0015% of total, NOT the 1.94% this row's stale delta implies -- the implementer's "~1.8%/x1.97" self-report inherited the stale baseline; the A/B corrects it. SPLIT, exactly: alloc delta = phase:load +5 (const) + phase:signatures +19..+143 (= 15 fixed + 4*2^rung, the throws-clause plumbing threaded into the declaration SWEEP/fold -- one ThrowsClauseEntryArray per file plus the fold's iterator over it, empty here because no fn throws) + phase:semanticCheck +5 (const: buildThrowsMap builds one program-wide map, checkMainCannotThrow one scan) + phase:lowerMaxonToStd +1 (const). BYTES delta is dominated by phase:parse +51,832..+1,609,768 at ZERO extra allocations -- the SAME IrFunction objects made WIDER by the new inline throwsTypeName ThrowsClause field, a per-function fact that is inherent and unavoidable (parse alloc COUNT delta is exactly 0). FREES track allocs to the unit at every rung (+19..+143 signatures etc.) -- no leak, specs-shv2 memoryLeak FALSE. WHY nothing is superlinear, structurally not just measured: throwsOf/throwsMap.contains/funcSignatures.get are O(1) hashed Map lookups (funcThrows records THROWING fns only, so a non-throwing callee costs no entry and contains beats a boxing get-otherwise-none); buildThrowsMap + checkMainCannotThrow are each ONE O(functions) pass built once, and checkCalls does an O(1) contains per call, so semanticCheck is O(ops) not O(calls x functions); the parser try/otherwise desugar rides the PRE-EXISTING if/while binding machinery (snapshotBindings/restoreBindings/mergeAtContinuation/emitScopeDrops -- unchanged by this rung) at the same per-construct cost, and parsePostfixCast/evalConstPostfix are token-bounded while-loops; SsaDestruction colorAndDropSelfMoves stays a SINGLE linear pass -- the tryCall two-result capture is found by an O(1) prevWasCall + one-op lookahead with a skipNext guard, never a nested scan; lowerTryCall / X64+Arm64 lowerTryCall slot args O(args) exactly like lowerCall; FoldConstOperands only widened a match range (no loop). The scale-test corpus contains ZERO try/throws/otherwise/throw/panic (rung 0 = 56 fns, 0 error keywords), so the entire measured cost is the ALWAYS-ON plumbing -- confirming the design goal (only throwing fns cost hot allocations) holds, and the residual is the wider IrFunction, inherent. Every phase alloc RATIO on the branch is <= x2.00. No fast-path fork, no inlined helper: nothing to change. RECORDING this run re-anchors the logged baseline to the branch's TRUE absolutes (357,131 / 10,387,451, which the A/B confirms bit-for-bit) so the next rung's printed delta is honest again. Gates on this tree: build exit 0 zero warnings; specs-shv2 718/0 exit 0, no 101; git status clean, specs-shv2/fragments unmoved (no code change, codegen byte-identical by construction). | 357,131 | 681,101 | 1,328,537 | 2,622,663 | 5,210,702 | 10,387,451 |
| 2026-07-18 | wasm32-wasi scalar backend rung (StdToWasm/WasmBinary/WasmComponent + BackendDispatch/CodeResult/Main/SpecRunner) + its independent OPTIMIZATION pass. Re-baselines the log onto current main; the previous row's baseline predates several main merges. ATTRIBUTION of the +17,055..+456,650 alloc delta the instrument reports vs the previous row: it is NOT this rung. Measured by isolating HEAD vs this rung's base 964b6e149 (rebuilt shv2 only, C# bootstrap untouched by the rung): the rung's ENTIRE host-path memory effect is +1 allocation / +48 bytes per COMPILE — identical at every rung (rung0 367,616->367,617; rung5 10,642,662->10,642,663; +48 bytes everywhere), landing wholly in phase:link. That is the single empty `wasmCoreModule: ByteArray.create()` field this rung added to CodeResult.create (one CodeResult per compile) — a per-compile CONSTANT, not linear and not superlinear in program size (unchanged from 64KB rung0 to 2MB rung5). LEFT as-is: it is O(1)/compile and removing it would need a union/optional field (a fork the mandate forbids for a constant). The remaining ~17k..456k of the instrument's delta is prior-main (throwing-division 8c79208ec, arm64 fragment/Wave-2 sync fce8fb829) — all below 964b6e149, already in this rung's base, so this row merely folds them into the trend. OPTIMIZATION applied this pass (WasmBinary.internFuncType): the func-type interner deduped by a linear scan over every previously-interned type — a findFirst-over-an-array run ONCE PER DEFINED FUNCTION, i.e. O(N functions x D distinct signatures), up to O(N^2) on a large scalar program (the wasm backend is a shipping path: `--target=wasm32-wasi` accepts arbitrarily large scalar programs, not just test snippets). Replaced with a bytes-keyed dedup Map (FuncTypeDedupMap), O(1) per intern, mirroring GlobalDataTable.valueDedup which fixed the identical O(N^2) for .rdata constants. The map returns the first-interned index for equal bytes exactly as the scan did, so the emitted type section is BYTE-IDENTICAL: specs-shv2 --target=wasm32-wasi 90/0 exit 0 (no leak). This optimization is INVISIBLE to scale-test by construction — scale-test compiles the corpus for the HOST, never --target=wasm32-wasi, so it never reaches the wasm encoder; the host numbers in this row are bit-identical to the un-optimized rung's host numbers, which is the correct evidence that the change is host-neutral. NOT superlinear-in-reachable-input and LEFT: StdToWasm.sequenceParallelCopy's parallel-move has an accidental extra O(n) factor (findLeafMove->isAnySource is O(n) called O(n) times = O(n^3) vs the standard O(n^2)), but n is a single edge's phi-move count and the standard algorithm is O(n^2) regardless; fixing the extra factor needs source-count bookkeeping threaded through the swap-rewrite of correctness-critical phi sequencing, poor risk/reward. buildValtypeTable/resolveDependents/widthOfValue is memoized (the `resolved` set) so each dependent is walked once = O(values), linear; the loop+br_table dispatcher and WasmBinary LEB128/section assembly are all linear. Gates on this tree: build exit 0 zero warnings; specs-shv2 host (arm64-macos) 668/0 exit 0 no leak; specs-shv2 wasm32-wasi 90/0 exit 0 no leak; git status shows only WasmBinary.maxon (+ this log row); specs-shv2/fragments clean (host codegen byte-identical). | 367,617 | 698,900 | 1,362,853 | 2,689,515 | 5,339,666 | 10,642,663 |
| 2026-07-18 | P1.4b Wave 2b (managed RETURN — tryCall-result reconcile + OPEN #49 gate removal). Corpus has no error handling, so 0-delta expected; optimizer confirmed nothing superlinear (O(1)/statement-local per tryCall). | 357,132 | 681,102 | 1,328,538 | 2,622,664 | 5,210,703 | 10,387,452 |
| 2026-07-18 | PE .rsrc manifest (asInvoker, ported from v1) — a FIXED +22 allocs / +2,422 bytes at every rung (a constant one-per-compile .rsrc section built once), NOT superlinear. Opts every emitted exe out of Windows UAC installer-detection; supersedes the test-only exe rename. | 357,154 | 681,124 | 1,328,560 | 2,622,686 | 5,210,725 | 10,387,474 |
| 2026-07-18 | OPEN #54 Slice A (aggregate type-identity check) OPTIMIZATION PASS (independent) — NO CODE CHANGE. Immune A/B (one bootstrap, both shv2 binaries: parent 524981047 vs HEAD 78a937a0d): allocs 0 AND bytes 0 at every rung. The name-check reads the dense scalar `valueTags`/`valueNameIds` columns via `aggregateNameOf(tag, nameId)` (array-index + `Map.contains`, no scan, no `MaxonType` materialized) and returns the shared `EmptySourceText` for the scalar case, so a scalar-returning function allocates nothing; `String.from(...)` is confined to the throw branch. `requireManagedValueMatches` now shares that pair and drops its prior `self.typeOf()` union allocation (corpus-invisible). Each helper runs once per return/assign/otherwise — O(program), linear. | 357,154 | 681,124 | 1,328,560 | 2,622,686 | 5,210,725 | 10,387,474 |
| 2026-07-18 | OPEN #54 Slice B (aggregate type-identity at the CONSUMING call-arg site) OPTIMIZATION PASS (independent) — NO CODE CHANGE. Unlike Slice A (which read existing columns, 0 delta) this rung PUBLISHES a new dense per-value column `valueNameIds` on IrFunction (mirror of `valueTags`), so it has a REAL, LINEAR cost. Immune A/B (one bootstrap bin/maxon.exe, both shv2 binaries: parent f3d9c7f65 vs HEAD aa2ac2509): allocsDelta +612/+1,142/+2,202/+4,318/+8,548/+17,002 (delta ratios 1.87 1.93 1.96 1.98 1.99 -> x2.0, LINEAR); bytesDelta +22,856/+41,224/+89,384/+180,704/+416,280/+893,840 (geometric mean x2.09 over the 32x ladder — LINEAR, nowhere near x4). PER-PHASE ATTRIBUTION (parent vs HEAD, exact): the ENTIRE allocs delta lands in phase:merge (14,106 -> 14,712 at rung 0, x1.99) and it is `remapArtifact` rebuilding the published column on the MULTI-FILE (non-identity) remap path — files 2..N of each rung, which the corpus reaches because its file-count knob doubles per rung — one TypeNameIdArray.create() per function plus one transient MaxonType union per value via `remapValueNameId` (maxonTypeOfTag -> remapMaxonType -> typeNameIdOf, the deliberate filler-safe round-trip that avoids a second copy of which tags bear a name; left as-is, a hardcoded tag predicate would duplicate that classification). BYTES split two linear ways: phase:parse +440 -> +8,376 is the new `valueNameIds` pointer field widening each IrFunction record by 8 bytes x functions (440/8 = 55 funcs at rung 0) — the retained-column FIELD cost; phase:merge +22,128 -> +885,176 (dominant) is the rebuilt column plus those transient unions. UNCHANGED: phase:parse ALLOCS byte-identical (68,466 both) — parent already filled `self.valueNameIds` per function, so publishing it by-reference extends the column LIFETIME whole-compile, not the cumulative alloc COUNT; phase:semanticCheck byte-identical (566 allocs / 16,236 bytes both) — the new checkArgTypes struct-identity branch reads `valueNameIds` only for a STRUCT parameter and the corpus has none (structs never cross a call boundary — ScaleCorpus manifest). NOTHING SUPERLINEAR: checkArgTypes' branch is O(1) per call-arg (array-index get + typeNames.nameOf + bounded namedAggregatesConflict compare) and remapValueNameId is O(1) per value (match + remap.apply array-index) — no scan, no nested walk, nothing indexed linearly. specs-shv2 743/0, fragments byte-identical, no leak. | 357,766 | 682,266 | 1,330,762 | 2,627,004 | 5,219,273 | 10,404,476 |
| 2026-07-19 | OPEN #56 fix (destructor-needs closure reads the PROJECT struct layout, not a signatures re-fetch) OPTIMIZATION PASS (independent) — NO CODE CHANGE by the optimizer. The fix swaps `addNestedStructDestructors` per-struct `requireDeclaredStruct(signatures, layout.name)` re-fetch for the PROJECT `layout` that `closeDestructorNeeds` already holds while iterating `project.structTypes`, REMOVING one O(1) `signatures.structOf` = `structTypes.get` name-hash + map lookup AND one `DeclaredStruct` union construction per struct-with-a-needed-destructor per fixpoint pass. Immune A/B (one bootstrap bin/maxon.exe, both shv2 binaries: parent 6d2694e93 vs HEAD f42e2c00c, both built by the freshly-rebuilt bootstrap): allocs 0 AND bytes 0 at EVERY rung (357,766 / 682,266 / 1,330,762 / 2,627,004 / 5,219,273 / 10,404,476 allocs and 21,663,305 / 43,779,214 / 90,502,352 / 184,264,834 / 377,453,081 / 774,333,995 bytes, identical). WHY 0: the changed function is UNREACHABLE by the corpus — ScaleCorpus structs are scalar-only (x int, y int) with no managed field, so noteDestructorUsage seeds an EMPTY neededDestructors set, the closeDestructorNeeds guard `neededDestructors.contains(destructorSymbol(layout.name))` is false for every struct, and addNestedStructDestructors never runs (same structural blind spot as OPEN #54 Slice A/B). NOTHING SUPERLINEAR (static, since the instrument cannot see the path): the fixpoint shape is UNCHANGED — closeDestructorNeeds still iterates project.structTypes / enumTypes once per pass, pass count bounded by struct NESTING DEPTH (a source constant; a flat set of managed structs converges in 1 pass), each pass O(types x fieldCount) = O(program) once. The edit strictly REMOVES a constant-factor per-struct O(1) lookup + union alloc from the exercised (nested-managed-struct) path; no scan is added. Emitted code shrinks 264 bytes (4,529,438 -> 4,529,174) from dropping the requireDeclaredStruct call. specs-shv2 744/0, fragments additions-only (no M), no leak (exit 0). | 357,766 | 682,266 | 1,330,762 | 2,627,004 | 5,219,273 | 10,404,476 |
| 2026-07-19 | OPEN #52 fix (union-payload interner re-intern doors) OPTIMIZATION PASS (independent) — NO CODE CHANGE by the optimizer. The fix adds TWO re-intern doors so a signatures-layout payload id is resolved against the interner that MINTED it, not the parser's own: Door A exports `adoptType` (re-intern a payload's `named`/`structRef` id BY NAME into a caller interner) and routes 3 parser union-payload sites through it (requirePayloadLowerable + declarePayloadBindings classify, emitUnionPayloadLoad value-type mint); Door B adds `ProgramSignatures.unionBoxDropCallee(layout)` = `unionNameDropCallee(self, self.typeNames, layout)` and routes `decrefCalleeFor`'s union branch through it, resolving the drop-callee over the SIGNATURES interner instead of the artifact interner. Immune A/B (one bootstrap bin/maxon.exe, both shv2 binaries: parent e06879d98 vs HEAD 6ae9b56cd, both built by the freshly-rebuilt bootstrap): allocs 0 AND bytes 0 at EVERY rung, every per-phase alloc/byte row bit-for-bit identical (357,766 / 682,266 / 1,330,762 / 2,627,004 / 5,219,273 / 10,404,476 allocs and 21,663,305 / 43,779,214 / 90,502,352 / 184,264,834 / 377,453,081 / 774,333,995 bytes). WHY 0: the scale corpus is scalar-only and union-payload-blind (ScaleCorpus manifest — no `match`, no payload-bearing union constructed or dropped), so NONE of the changed sites runs; the delta is 0 by construction, not by luck. NOTHING SUPERLINEAR (static, since the instrument cannot see the path): Door A `adoptType` is a `match` + one `nameOf` (array index in the signatures interner) + one `intern` (bytes-hash into the artifact interner) = O(1) per union-payload USE, added at 3 sites each already visited once per payload use (declarePayloadBindings loops per match-arm binding, a source constant), so per-use cost stays O(1) and whole-program stays O(union-payload uses) = linear. Door B is the SAME loop the parent ran inline: `unionBoxDropCallee` -> `unionNameDropCallee` -> `unionHasManagedField` iterates a union's cases x payloads (bounded by that union's own declared arity, a source constant) once per union-value DROP; the parent already ran `unionHasManagedField` at that exact site, so no scan over a program-sized collection was added and the drop-path complexity is unchanged (O(bounded-decl-size) per drop, O(program) whole-program). No fast-path fork, no inlined helper — nothing to change. Emitted code is byte-identical for existing programs (the doors change resolution, not codegen). specs-shv2 748/0 exit 0 memoryLeak FALSE; git status specs-shv2/fragments/ no M (additions-only; one pre-existing untracked stray ignored); build exit 0 zero warnings. | 357,766 | 682,266 | 1,330,762 | 2,627,004 | 5,219,273 | 10,404,476 |
| 2026-07-19 | OPEN #45 / #54 Slice C (value-position `match … gives` type-agreement check) OPTIMIZATION PASS (independent) — NO CODE CHANGE by the optimizer. The rung adds ONE flat per-reaching-arm loop in `finalizeMatchMerge` (Parser.maxon:6749, `checkGiveTypes`) that compares each reaching arm's `gives` value type to the FIRST arm's — scalar-class via `typesAgree`, aggregate-ness via `aggregateNameOf` emptiness, both-aggregate via `namedAggregatesConflict` — throwing E3005 (`giveTypeMismatch`) on disagreement so an ill-typed result phi is refused before it is built (closing a String-read-off-int / int-derefed-as-box segfault and a two-different-struct wild-free). Immune A/B (one bootstrap bin/maxon.exe, both shv2 binaries: parent 2430ef2dc vs HEAD 261b21fc1, both built by the freshly-rebuilt bootstrap): allocs 0 AND bytes 0 at EVERY rung, and the per-phase ALLOCATIONS and BYTES tables are bit-for-bit IDENTICAL parent vs HEAD (357,766 / 682,266 / 1,330,762 / 2,627,004 / 5,219,273 / 10,404,476 allocs and 21,663,305 / 43,779,214 / 90,502,352 / 184,264,834 / 377,453,081 / 774,333,995 bytes). WHY 0: the scale corpus contains NO `match` at all (ScaleCorpus manifest, Testing/ScaleCorpus.maxon:82 — "no `for`, no `match`"), so `finalizeMatchMerge` and the new `checkGiveTypes` loop are never reached; the delta is 0 by construction, not by luck — the same structural blind spot as OPEN #54 Slice A/B, #56 and #52. NOTHING SUPERLINEAR (static, since the instrument cannot see the path): the new loop is a SINGLE flat pass over `reachingArms` (i from 1 to count), placed at the SAME level as — and sequential with, NOT nested inside — the pre-existing `checkGiveClass` O(arms) loop, so it contributes O(arms), never O(arms squared). Per arm the work is O(1): `tagOf` / `valueNameId` are dense `valueTags` / `valueNameIds` column .get's; `aggregateNameOf` is one `typeNames.nameOf` array-index plus `containsStruct` / `containsEnum` (a `Map.contains`, no scan, no allocation) and returns the shared `EmptySourceText` for every scalar so no `MaxonType` is materialized; `typesAgree` is a `typeClassOf` tag compare; `namedAggregatesConflict` is a bounded byte-equal of two interned aggregate NAMES (an identifier length — a source constant, not program-sized). `giveTypeName` + `String.from` are confined to the throw branch, so a valid (agreeing) match allocates NOTHING new. A match's arm count is a hand-written source constant (the #28 / #32 / destructor-cascade precedent — bounded by a source constant is linear whole-program), so the loop sums to O(total match arms) = O(program), linear. The DOMINANT term of `finalizeMatchMerge` is UNCHANGED: the pre-existing O(carried x arms) phi-diff loops (`eachVar` Parser.maxon:6830, `recordEdges` 6869) still dominate; the new check adds a strictly-smaller O(arms) term and does not worsen that pre-existing complexity. No fast-path fork, no inlined helper — nothing to change. Emitted code is byte-identical for valid programs (the check only throws on ill-typed matches; fragments additions-only). GATES on this tree: build exit 0 zero warnings, error-codes registry OK; specs-shv2 754/0 exit 0 memoryLeak FALSE; git status specs-shv2/fragments/ no M (the byte-identical HEAD binary reproduces the spec-tested build). | 357,766 | 682,266 | 1,330,762 | 2,627,004 | 5,219,273 | 10,404,476 |
| 2026-07-19 | F2a float RETURN (OPEN #50) OPTIMIZATION PASS (independent) — NO CODE CHANGE by the optimizer. Movement is a FLAT +2 allocs / +40 bytes at EVERY rung (rung0 == rung5), a once-per-compile CONSTANT, not superlinear. Immune A/B (one bootstrap bin/maxon.exe, both shv2 binaries: base F1 1e765a3a0 vs F2a working tree): base reproduces the last logged row EXACTLY (delta 0 at all 6 rungs), confirming F1 is scale-neutral and the +2/+40 is THIS slice, not a stale baseline (F1 landed after the last row). PER-PHASE A/B splits the delta exactly: phase:parse +2 allocs / +32 bytes, phase:isel +8 bytes / 0 allocs, every other phase bit-identical. (1) phase:parse +2/+32 is NOT the backend calleeResultClasses map as first suspected — it is main() TWO return statements. The corpus emits `function main() returns ExitCode` once per compile (ScaleCorpus.mainSource, a FIXED block not scaled) with `return 0` and `return 1`; ExitCode is a ranged int typealias so its return tag is `named`, and F2a new floatResolvedTag (parseReturnStatement) calls namedIsFloatAlias -> aliasOf(ExitCode), which constructs a boxed DeclaredAlias union (16B) per return. main is emitted ONCE, so exactly 2 boxes = +2 allocs / +32 bytes, a per-compile constant independent of program size (struct/int returns are structRef/integer, never `named`, so they never fire it; ExitCode underlying is int so tagIsFloat is false and no MaxonType.float box is made — one DeclaredAlias box per firing). (2) phase:isel +8/0 is the StdToX64Lowering struct widened by the ONE new calleeResultClasses pointer field (one lowering struct per compile). The calleeResultClasses MAP is built once in create() exactly as it was built once in buildValueClassColumns before F2a — 0 allocation delta (isel allocs bit-identical base vs F2a), the build merely MOVED phase-internally, count unchanged. NOTHING SUPERLINEAR: namedIsFloatAlias is O(1) per named-return (internedName array-index + aliasOf hashed Map.get + one O(1) DeclaredAlias box), bounded by return-statement count = O(program) linear, the SAME accepted boxed-DeclaredAlias idiom valueIsUnsigned/fieldStorageType already use; floatResolvedTag/resolveFloatAliasType are O(1)-gated (return early when tag != named, so the parser helpers never fire on the corpus except main 2 ExitCode returns); backend buildCalleeResultClasses is O(functions) built ONCE per compile (count unchanged from the pre-F2a build inside buildValueClassColumns), and calleeResultClass/callResultRegister/emitReturnValueMove are O(1) per call/return — the Map is precisely the O(1) index that AVOIDS a Theta(calls x functions) scan. No fast-path fork, no inlined helper. LEFT as-is per the mandate: the per-named-return DeclaredAlias box is a LINEAR constant-factor matching the existing aliasOf idiom, and removing it would need a non-boxing float-alias query in the front-end resolution logic this pass must not touch (byte-identical fragments are pinned). specs-shv2 fragments clean (no code change -> codegen byte-identical by construction). | 357,768 | 682,268 | 1,330,764 | 2,627,006 | 5,219,275 | 10,404,478 |
| 2026-07-19 | F2b float ARGUMENTS (OPEN #57) OPTIMIZATION PASS (independent) — DECISION: kept the two dense maps, NO CODE CHANGE by the optimizer; nothing superlinear. The F2b slice adds a GENUINELY NEW O(functions) structure, the backend calleeParamClasses Map (callee name to per-parameter float bitmask), built ONCE per compile in each backend create via buildCalleeParamClasses — the argument-side twin of F2a calleeResultClasses. Delta vs the F2a row: allocs +43 +43 +51 +59 +67 +75 (rung0..5), bytes +8,632 +8,632 +16,984 +33,528 +66,456 +132,152 (rung5 = 0.017% of 774MB). LINEAR, not superlinear: the BYTES double per rung (x1.97..x1.99, the corpus itself doubles) because the map has one entry per function and its four backing arrays (keys pointers, u64 mask values, states, hashes) are O(functions); the ALLOCS grow only additively +8/rung because the map is DOUBLING-backed, so building it is O(log functions) reallocs, not O(functions). per-type (rungs 0-2) CONFIRMS: calleeParamClasses and buildCalleeParamClasses are a TAIL contributor (not in the top-20 type or scope table), every listed type is x1.88..x1.99, and the realloc bucket carrying the map backing-array growth is x1.88 (below 2.0). The function-NAME bytes are NOT duplicated: Map.insertAtSlot retains a refcounted reference to the same StdModule-owned ByteArray that calleeResultClasses already keys on (keys.set is a retain, not a byte copy), so what the second map costs is a second hash-table SLOT machinery over the same key set, not a second copy of the names. AUDIT of every new helper: computeParamSlotIndex counts preceding same-file params in a loop bounded by paramIndex under MaxRegisterParams (6, parser-enforced) = O(1) per param and O(functions) total; floatParamMaskOf is O(params at most 6) once per function; emitCallArgMoves is O(args at most 6) per call; calleeParamClass is an O(1) hashed get plus a bit shift; parser resolveFloatAliasType is O(1) and allocates ZERO on the corpus (params are int or struct, so it early-returns on tag not equal to named). None is a per-op or per-value walk that scales. CONSOLIDATION CALL — kept dense, did NOT merge into one map, did NOT take the sparse option. (1) It is LINEAR and 0.017% of total; the mandate is to hunt superlinear, not micro-optimize a constant factor. (2) The two maps are NOT one-fact-twice: they store INDEPENDENT facts (return register class vs per-parameter float mask), both rebuilt every compile from stdModule.functions, so they cannot silently drift — the shared key set is derived, not a hazard. (3) To stay allocation-flat a consolidated value MUST be a packed scalar: a struct value heap-boxes per entry in shv2 (structs are heap values), which would turn the flat +8/rung alloc delta into +O(functions)/rung, strictly WORSE — the measured flatness proves the current scalar value is stored inline. Bit-packing the result-class bit with the param mask reintroduces a bit-layout that two readers must agree on, and forces the map type through about 11 sites in the F2a return path (buildValueClassColumns then buildFunctionValueClasses then recordBlockOpClasses then recordOpResultClass then calleeResultClass, plus both backends create, callResultRegister and threading) which the F2b brief fenced off and which is byte-pinned — invasive for a ~0.008% (one of two hash tables) saving. Rejected the sparse-map option: a missing entry degrading to an all-GPR default turns a genuinely-wrong float argument from a specific panic at calleeParamClass into a later, vaguer emitter failure. GATES: shv2 suite 763/0 exit 0 (no leak); specs-shv2 fragments additions-only — one new f32-function-param-return test, zero modified — so all existing codegen is byte-identical. | 357,811 | 682,311 | 1,330,815 | 2,627,065 | 5,219,342 | 10,404,553 |
| 2026-07-19 | F3b throwing-float RETURN throw edge (OPEN #60) OPTIMIZATION PASS (independent) — DECISION: KEEP the StdReturnType field, NO CODE CHANGE by the optimizer; nothing superlinear. Movement is a FLAT +1 alloc / +1 free / +24 bytes at EVERY rung (rung0 == rung5), a once-per-compile CONSTANT, not superlinear. A/B build isolates it exactly: base F3a 40a159bea vs F3b working tree, both rebuilt on one bootstrap; the base reproduces THIS logged F2b row EXACTLY (delta 0 at all 6 rungs), which also confirms F3a (int->float ARG widening, siToFp) is scale-neutral on the corpus — the corpus has no float arguments so siToFp never fires — so F3a legitimately added no row and this +1/+24 is F3b ALONE, not a stale baseline. Per-phase A/B attributes the WHOLE delta to phase:lowerMaxonToStd (+1 alloc / +1 free / +24 bytes at every rung; every other phase bit-identical): it is the ONE StdReturnType.void union box minted by create() in the Self{...} initializer (create runs once per compile in lowerMaxonToStd), freed once when first overwritten / at teardown (hence +1 free, a matched alloc, no leak). It is NOT the per-function assignment self.currentReturnType = srcFunc.returnType — that is a refcount RETAIN of the existing IrFunction.returnType box, no new alloc; had it allocated per function the delta would DOUBLE per rung, but it is flat. It is NOT the emitZeroConstOfReturnType float branch — the corpus has no throwing-float function so that branch is never taken, and the non-float branch delegates to the unchanged emitZeroConst, identical op emission (fragments prove byte-identical codegen: additions-only, zero modified goldens). DECISION KEEP vs bool: (1) the movement is once-per-compile, 24 bytes = 0.0000031% of the rung-5 774MB; the mandate hunts superlinear growth, not once-per-compile constants, and this does not scale at all. (2) The field stores the RAW return type and classes float-ness at the point of use via the SAME stdReturnTypeIsFloat predicate the backend emitReturnValueMove applies to the SAME IrFunction.returnType — one fact, one predicate, two readers that cannot drift; a bool currentReturnIsFloat would cache the DERIVED answer instead of the fact, and if register-file classification ever needs more than a bool (a future SIMD/vector return class distinct from scalar float) only emitZeroConstOfReturnType changes under KEEP whereas the bool field type and every assignment site would have to change. (3) It mirrors its sibling field currentThrows as ThrowsClause, which stores the raw clause not a derived bool — a bool would make currentReturnType the odd field out. (4) A bool is a code change that would require the full gate battery and risk the byte-pinned codegen to save a constant the mandate says not to chase. SUPERLINEAR AUDIT: emitZeroConstOfReturnType is O(1) per throw edge (one union match + one mint-and-append const, same op count as the emitZeroConst it replaced) firing at most once per throw/propagate/error-return terminator = O(program) linear; currentReturnType is a scalar field set once per function = O(functions) linear; no data structure indexed by program size, no nested scan, no repeated rebuild, no dense-index walk. Nothing superlinear. GATES: no code change (KEEP) so codegen byte-identical by construction; shv2 suite 767/0 exit 0 (no leak); specs-shv2 fragments additions-only (one new error.throwing-float-return.test, zero modified existing goldens). | 357,812 | 682,312 | 1,330,816 | 2,627,066 | 5,219,343 | 10,404,554 |
| 2026-07-20 | arm64-linux RUNG 1 (static ELF writer + arm64Syscall op + OsImportSlot hoist) — DECISION: KEEP, NO CODE CHANGE; nothing superlinear, and the rung's TRUE cost is +1 alloc / +48 bytes FLAT AT EVERY RUNG (rung0 delta == rung5 delta), a once-per-compile constant. ⚠ READ THE RAW DELTA COLUMN ABOVE WITH CARE — it is measured against the last logged row (F3b, 2026-07-19) and origin/main advanced TWICE since without logging one (OPEN #52 767->771, OPEN #62 771->774), so the +10,497(rung0)..+255,347(rung5) it shows is ACCUMULATED UPSTREAM WORK, NOT this rung. A/B isolates it exactly: a second worktree at the pre-rung parent 01b901de5, built on the SAME bootstrap binary, reproduces that delta to within 1 alloc / 48 bytes at all six rungs (base rung0 368,309/23,409,379 vs rung 368,310/23,409,427; base rung5 10,659,901/801,125,168 vs rung 10,659,902/801,125,216). So the whole visible movement is the unlogged upstream rows and the rung itself is +1/+48. ATTRIBUTION IS SINGULAR: the ONLY module-scope heap literal the rung adds is ElfWriter.maxon:54 'let ElfIdent = b"\x7fELF..."', a 16-byte ByteArray minted ONCE at module init (16-byte payload, 48 bytes with allocator header/rounding) — grep over every file the rung adds or edits returns exactly this one candidate, which is why the delta is flat rather than per-function or per-rung. KEEP vs inlining the magic bytes at the call site: the project rule is no magic values, and e_ident IS a named structural constant; trading a named constant for a bare hex literal to save one once-per-compile allocation is chasing a number the mandate explicitly says not to chase. SUPERLINEAR AUDIT: writeElf is a single linear pass over code/rdata/dataPrelude with no nested scan and no structure indexed by program size (the byte images are appended once each); patchArm64SectionRelocations is O(relocations) and was HOISTED, not duplicated, out of MachOWriter so both writers share one walk; the arm64Syscall op is O(1) per OS-primitive lowering (at most three sites in the entry stub + isel) and adds one union arm, not a pass; the debug-symbol section pass is O(symbols). Nothing indexes by program size, nothing rebuilds, nothing rescans. GATES: shv2 suite 772/0 exit 0 (not 101, no leak); specs-shv2 fragment tree EMPTY (zero modified goldens — arm64-macos and x64-windows codegen byte-identical, which is also what proves the three X64 range-arm edits are inert); C# suite 3044/3044 exit 0 with specs/ and specs-shv2/ both clean, required because the rung also fixes a pre-existing bootstrap Mach-O GOT chained-fixup page-boundary bug that blocked the suite entirely. | 368,310 | 700,127 | 1,365,156 | 2,693,958 | 5,348,379 | 10,659,902 |
| 2026-07-20 | P1.4b Wave 2c (boxed-union struct FIELDS: construct/read/reassign/scope-drop + the throw-of-field MOVE-OUT transfer). DELTA vs F3b: -520 alloc EVENTS / +62,208 bytes at rung5, LINEAR (doubles per rung, ~x1.9), NET FEWER ALLOCATIONS. A/B (revert Parser+SignatureIndex to HEAD, SAME copied bootstrap, rebuild) reproduces the F3b baseline EXACTLY (delta 0 at all 6 rungs) => the delta is REAL, not a stale bin. ALLOC REDUCTION = the DELETED per-construction rejectUnsupportedManagedStructField scan, which classified every field of every constructed struct via classifyUnionPayload (one PayloadClass box per field), now unnecessary because a boxed-union field is a MANAGED PayloadClass that moveInPayload/emitFieldWrite handle end to end; corpus rung0 = 12 structs x 2 int fields = exactly -24 (matches). BYTES INCREASE = a realloc-SIZING shift, benign + linear, NOT a per-element cost: --per-type shows the dominant allocator is realloc (array-backing growth, ~87MB / 338K allocs at the traced rungs); dropping the transient per-construction PayloadClass allocs shifts WHEN a growable buffer crosses its doubling threshold, so it reallocs to a slightly larger capacity. The new throw-of-field recording is INERT on the corpus (only int struct fields, no unions, so emitFieldLoad named-field guard never fires) and is 4 SCALAR parser fields anyway (zero allocation, no map). SUPERLINEAR AUDIT: classifyUnionPayload O(1) per field; throw move-out O(1) per throw edge (one null store + one owned-bit set); lastFieldRead = 4 scalar fields, no structure indexed by program size, no scan, no per-op growth. Nothing superlinear. GATES: build 0/0; shv2 suite 788/0 exit 0 (no leak, incl. throw/reassign/deep-cascade probes); worker-invariance byte-identical (workers 1 vs 12, same md5); fragments additions-only + ONE justified message-text M (borrowed-throw reject message extended to name the boxed-union FIELD move-out). | 357,788 | 682,272 | 1,330,744 | 2,626,930 | 5,219,079 | 10,404,034 |
| 2026-07-20 | P1.5-A1 non-capturing function values (813/0). NEW: Maxon functionRef/indirectCall, Std funcAddr/callIndirect, x64 leaRegFunc/callReg, arm64 isel producers; function(...) returns T type parsing; bare-function-name to functionRef via the whole-program signature sweep (forward refs resolve); E3005/E3101 discipline; a per-file FUNCTION-typealias registry. DELTA (A1-only, vs the pre-A1 compiler ee224152c): +2,494 alloc EVENTS / +118,456 bytes at rung5, LINEAR (the A1-only alloc delta itself DOUBLES per rung: x1.55 x1.71 x1.83 x1.91 x1.95, converging to x2.0). NOTE the delta COLUMN on this row reads +4,586 / +210,528 instead, because the previous LOG ROW is Wave 2c -- TWO non-A1 commits behind ee224152c -- so the column additionally counts a221da742 (x86 GT double-schedule fix) + 3e2eecc9c (fragment regen), which add +2,092 allocs / +92,072 bytes at rung5 and which the ee224152c A/B excludes; +2,092 + +2,494 = the +4,586 shown. A/B: pre-A1 baseline is ee224152c (TEST-ONLY, compiler bit-identical to HEAD~2), full rebuild, SAME copied bootstrap; the corpus is byte-identical between the two (same source AND code bytes at all 6 rungs), so the delta is REAL, not a stale bin. SUPERLINEAR AUDIT (the mandate) -- NOTHING superlinear. The specific risk (OPEN.md standing rule: a whole-program query placed UNDER a per-file one is asked ONCE PER FILE, so anything O(files) inside is O(files^2) -- the shape of the clearDepsFor/compositeSourceHash quadratics): queryProgramSignatures is MEMOIZED (computed once, ONE pass over files, then every per-file queryParseOps is a cache hit), and A1's read side is all O(1) Map lookups -- isFunctionAlias/functionAliasOf/returnTypeOf/throwsOf -- so NO O(files) body was added under the per-file parse; the fold (foldFile) walks each file's function-alias array exactly once. The standing rule's OWN diagnostic is passed: a quadratic-under-per-file makes the DELTA grow ~x3.4 on a doubling ladder; A1's delta doubles (x1.55..x1.95), it does not. indexOfField added to parseMethodCall is a bounded per-struct scan (the same measured pattern as indexOfParam), O(program size), not superlinear. ATTRIBUTION of the delta (all LINEAR): +10 FLAT across every rung = the one-time whole-program functionTypeAliases Map (ProgramSignatures + Project, created once). +143 in phase:parse = EXACTLY one per file (143 files at rung5) = the real-parse FileParseArtifact.functionTypeAliases staging array. +2,334 in phase:signatures (the declaration sweep) = the sweep-side function-alias staging: the local functionAliases array is 1/file (MEASURED: a reuse-the-artifact-array probe dropped signatures by exactly 143), the rest is the enlarged per-file sweep artifact + a linear realloc/threshold side-effect -- --per-type confirms NO function-alias TYPE or SCOPE is a significant allocator (they sit in the 288-type / 479-scope TAIL; the dominant allocator is realloc / raw-buffer array growth), so the sweep delta is threshold movement, not significant new logical objects. The read-side guard isFunctionAlias -- called on EVERY named type reference in both sweep and parse -- is PROVEN zero-allocation: neutralizing it left signatures bit-identical at 198,637 (Map.contains hashes the ByteArray key in place). CORPUS BLINDNESS (reported, NOT chased, instrument UNTOUCHED): the corpus emits NO function values (same class as instance-methods #30 / structs #4d), so the whole function-value LOWERING path -- functionRef/indirectCall to funcAddr/callIndirect to leaRegFunc/callReg + the arm64 twin -- measures ZERO. That is a corpus knob nobody added, not an optimization. NO code change was made: nothing was superlinear, so this row records the A1 feature's cost, not a fix. GATES: build 0 warnings; shv2 suite 813/0 exit 0 (no leak); specs-shv2/fragments byte-identical (empty git status) -- proves byte-identical codegen. | 358,034 | 682,658 | 1,331,410 | 2,628,156 | 5,221,425 | 10,408,620 |
| 2026-07-21 | x64-linux backend (raw-syscall codegen + static ELF, WSL cross-runner) + unlock 67 x64-generic tests on x64-linux (885/0 = x64-windows 887 minus the 2 SIGFPE divide/mod-by-zero tests, deferred). SCALE-NEUTRAL BY CONSTRUCTION on this host: scale-test compiles for the HOST (x64-windows), which takes the WINDOWS OS-call lowering (VirtualAlloc/WriteFile/ExitProcess via IAT); the new x64-linux path (mmap/write/exit_group syscalls, ELF x64 writer, X64LinuxRuntime) is NOT exercised here. The one new IR op x64Syscall adds a constant arm to every TargetOp match (O(1)); the fan-out edits are O(1)/op; the ELF reloc patcher patchX64SectionRelocations is O(relocs) linear (the arm64/PE twin). SUPERLINEAR AUDIT: all phase exponents ~x2.0 on the doubling ladder (regalloc:criticalEdges x2.31 is the pre-existing liveness two-call-site sum, unchanged by this rung). The delta-vs-last-row (rung5 +57,078 allocs / +9.87M bytes, itself DOUBLING per rung = LINEAR) spans the intervening upstream rungs P1.5-A2 + #71 + OPEN-14, NOT this rung, whose host-path allocation contribution is structurally nil. NO code changed for scale. GATES: build 0 warnings; x64-windows 887/0, x64-linux 885/0 (worker-invariant, goldens byte-identical w1==w12); fragments/x64-windows untouched. | 360,588 | 686,992 | 1,339,286 | 2,643,080 | 5,250,415 | 10,465,698 |
| 2026-07-21 | P1.5-B1a single-M cooperative green-thread scheduler (async/await): async f(args) spawns a green thread and yields a Promise handle, await p drives the cooperative scheduler to completion. Suite 887->899/0 host, 874/0 wasm unaffected (async is x64-gated). B1a-ONLY cost isolated by A/B vs the pre-B1a compiler 48832f3f6 (full rebuild, SAME copied bootstrap, corpus byte-identical at all 6 rungs): +6,160 allocs / +267,428 bytes at rung5, strictly LINEAR -- the B1a-only alloc delta DOUBLES per rung (x1.57 1.71 1.81 1.89 1.93, converging to 2.0) and so do the bytes (x1.77 1.96 1.96 2.03 2.02). READ THE DELTA COLUMN ON THIS ROW CAREFULLY: it instead shows +63,237 allocs / +10,140,417 bytes because the previous LOG ROW is A1 (2026-07-20) and SEVEN compiler commits sit between A1 and B1a -- A2b-1 + A2b-2 (capturing + uniform (args,env) ABI closures), OPEN #71 inline conditional, OPEN #14 borrowed-aggregate merge-arm reject -- which the pre-B1a baseline already carries (measured +57,077 allocs at rung5); +57,077 + +6,160 = the +63,237 shown, so the B1a FEATURE is +6,160, not +63,237. ATTRIBUTION: 100% of the +6,160 is ONE new per-value column, valuePromiseMarks (Parser.maxon:1842) -- a dense BoolArray in the SAME lockstep as valueOwnsHeap: pushed false in mintValue/recordValueType (Parser.maxon:8483/8521), set by markValuePromise on the single value emitAsyncCall mints, read O(1) by valueIsPromise. It splits across EXACTLY two phases (every other phase byte-identical HEAD vs pre-B1a): +3,969 in phase:parse = the per-value pushes during the real parse; +2,191 in phase:signatures = the BoolArray.create() in the Parser CONSTRUCTOR (Parser.maxon:2003), because queryProgramSignatures builds a Parser per file (Queries.maxon:130) and a throwaway Parser per const-initializer (SignatureIndex.maxon:1385) -- the SAME field, charged to whichever phase constructs the Parser (the identical parse/signatures split the A1 row documented for its own new column). The sparse-Set alternative was MEASURED WORSE (+7,364 allocs / +292K bytes at rung5: a per-function empty hash table costs more than the dense bit across many small functions), so the dense column stands (code note Parser.maxon:1837). SUPERLINEAR AUDIT (the mandate): NOTHING superlinear. The GT scheduler runtime (GtRuntime.maxon + X64GtRuntime.maxon: installGtRuntime, the .data globals, the two hand-assembled chunks) is emitted ONCE per module like MmRuntime and gated on RuntimeUsage.usesGt; the scale corpus emits NO async so usesGt=false, and the whole runtime + the asyncCall/await LOWERING (lowerAsyncCall/emitAsyncSpawn/lowerAwait) + validateAsyncCall measure ZERO -- corpus blindness (same class as A1 function-values / #30 instance-methods), reported not chased, instrument UNTOUCHED. The three per-input touch points are all bounded: requireAsyncScalarParams loops a callee own paramCount (per async-call op), emitAsyncSpawn arg-spill loop is bounded by MaxAsyncArgs (6), and moduleHasFunction(GtSpawnName) is ONE O(functions) scan per compile in buildX64Backend (mirrors the leak gate __mm_leak_check module probe), not inside any per-function or per-op loop. PER-TYPE CORROBORATION (--per-type traces rungs 0-2 with a separate mm-trace binary): the ONLY by-SCOPE movers HEAD vs pre-B1a are BoolArray.create (29,670 -> 30,557 = +887, the valuePromiseMarks constructor) and realloc (341,223 -> 342,169 = +946, the per-value push doubling of that same BoolArray), summing to +1,833 = EXACTLY the timed A/B rungs-0-2 delta (350 + 548 + 935); no new type or scope appears (the async LOWERING scopes are corpus-blind), and every row grows x1.88-1.99, nothing x4. NO code change was made -- nothing was superlinear -- so this row records the B1a feature cost, not a fix. GATES: build 0 warnings; specs-shv2 899/0 host exit 0 (no leak); specs-shv2 874/0 wasm exit 0; specs-shv2/fragments byte-identical (empty git status = byte-identical codegen). | 360,937 | 687,539 | 1,340,220 | 2,644,770 | 5,253,602 | 10,471,857 |
| 2026-07-21 | P1.5-B2a linear await (E3100): a COMPILE-TIME reachability pass (SemanticCheck.maxon checkLinearAwait) refusing a second await of the same promise reachable from a first, keyed on the promise SSA ValueId -- NO new per-value column, NO codegen, NO runtime change. OWN COST ZERO on the async-free ladder, proven by A/B (pass call ON vs OFF, both rebuilt): the per-phase ALLOCATIONS and BYTES tables are BIT-FOR-BIT IDENTICAL at all 6 rungs on vs off; phase:semanticCheck is 571 (rung0) to 14,707 (rung5) at x1.98 (LINEAR) both ways. The whole reachability machinery sits behind functionHasAwait, a bounded NON-ALLOCATING tag scan that returns false on every corpus function -- the scale corpus emits NO async (its own blind-spot doc lists closures/async/generics as NOT GENERATED), so the pass never runs and allocates nothing (same corpus-blindness class as A1 function-values and B1a async lowering; reported not chased, instrument UNTOUCHED). STALE-BASELINE CAVEAT -- READ THE DELTA ON THIS ROW AS SPANNING AN UNLOGGED GAP, NOT THIS RUNG: the +521 (rung0) to +9,201 (rung5) alloc delta and +24,807 to +443,440 byte delta shown against the previous row is NOT B2a and is NOT any corpus-visible change since the B1a row. MEASURED: the B1a-row commit 727a783b1, the B2a base f0d87269b, and B2a all produce byte-identical corpus numbers (361,458 rung0 to 10,481,058 rung5) -- the corpus has not moved at all across b8a595169 (B1a dedup: export-only plus an async-gated guard), d29dee4a8 (B1a stack-leak fix: GtRuntime-only plus async specs) and f0d87269b (docs). The delta is entirely the B1a row STALE STORED baseline: its numbers (360,937 rung0) do not reproduce on its own commit tree (727a783b1 measures 361,458), because the B1a feature was measured before the parallel-repo work its base later folded in -- OPEN #54 Slice B2, #66 x86 GT double-schedule fix, P1.9 ranged typealiases, x64-linux backend -- none of which took a log row of its own. COMPLEXITY NOTE (not a corpus issue): reachableReawait is O(await-sites x blocks) worst-case per await-bearing function (a BFS per await site, each block visited once); zero on the async-free corpus, and await-bearing functions are rare and small -- noted, not optimized (no measurable cost to optimize). GATES: build 0 warnings; scale-test exit 0; host 941/0, wasm 908/0, fragments additions-only. | 361,458 | 688,340 | 1,341,582 | 2,647,251 | 5,258,323 | 10,481,058 |
| 2026-07-21 | P1.5-B2b error-carrying try await (E3057/E3059 soundness). NEW: MaxonOp.tryAwait lowered to tryCall(__gt_try_await); __gt_try_await refolded WITH __gt_await into buildGtAwaitDriver(name, throwing), emitted ONCE per module that spawns a GT (gated on usesGt, like MmRuntime); GT struct grows 0x50 to 0x60 (threw field); trampoline captures R10 into gt.threw. Front end: asyncCallCalleeOf (promise to defining-asyncCall callee), checkPropagateType plus rejectPropagateAgainstEnclosing (E3059 propagate-type check, which also closes the gap for ordinary try), plain-await-of-throwing reject (E3057), the tryAwait to tryCall lowering; the linearity pass checkLinearAwait EXTENDED to treat tryAwait as an await site. OWN COST ZERO on the async-free ladder: the instrument delta-vs-last-row is 0 at ALL 6 rungs (allocs 361,458 rung0 to 10,481,058 rung5; bytes 21,992,353 to 785,322,804), BYTE-IDENTICAL to the B2a row -- every per-phase allocs and bytes column unchanged, phase:parse x1.99, phase:signatures x1.98, phase:semanticCheck x1.98, all LINEAR. STALE-BASELINE STATUS -- unlike the prior three rows this delta is a TRUE zero, NOT a stale-baseline artifact: verified the B2a stored baseline reproduces EXACTLY on this tree (361,458 to 10,481,058 == this run, delta 0 every rung), and only two DOC commits (B2a COMPLETE, OPEN #89) sit between the B2a log row and B2b, so there is no corpus-visible upstream work to caveat this time. CORPUS BLINDNESS (reported, NOT chased, instrument UNTOUCHED) -- STRONGER than usesGt=false: ScaleCorpus emits NO try, throws, otherwise, async or await AT ALL (verified in the generator; the documented subset excludes them), so beyond the GT/async path being skipped (usesGt=false, functionHasAwait=false) the NEW per-try-site checkPropagateType is ALSO never reached -- the corpus cannot see B2b front-end additions at all (same blind-spot class the corpus doc lists for async/generics). SUPERLINEAR AUDIT (the mandate) -- NOTHING superlinear. checkPropagateType is O(1) NON-ALLOCATING per bare-propagate try-site: one funcThrows.get Map lookup (hashes the ByteArray key in place, no alloc) plus one throwsTypeName field match, no loop, and reached ONLY on the propagate form (try f() with no otherwise), rarer still. asyncCallCalleeOf is an O(function-ops) linear scan per await site (locates the promise defining asyncCall), invoked TWICE per await site (once for the awaited result tag, once for the throwing-check or callee-name) = a constant 2x factor, still O(function-ops); await sites are rare and small (same profile as the B2a reachableReawait scan) and this path is under separate correctness review -- noted, not touched. checkLinearAwait keeps its unchanged O(await-sites x blocks) per await-bearing-function shape, now counting tryAwait sites too. NO code change was made -- nothing was superlinear -- so this row records the B2b feature (zero) cost, not a fix. GATES: build 0 warnings; host 956/0 exit 0 (no leak); scale-test exit 0; specs-shv2/fragments byte-identical (empty git status = byte-identical codegen). | 361,458 | 688,340 | 1,341,582 | 2,647,251 | 5,258,323 | 10,481,058 |
| 2026-07-21 | P1.6-A generic types (front-end, no descriptor), rebased onto B2b main. NO superlinear defect (GenericInstanceRegistry intern is O(1) composite-key, not v1's O(n2), independently verified). Own cost LINEAR: new per-file staging (Parser arrays + genericAliases + StructLayout.typeParamNames) + registry once/build; signatures-phase delta increments track 352x2^n. CORPUS-BLIND: the ladder emits zero 'with', so intern() is never called and the generic-instance lowering path measures zero — reported, not chased, instrument untouched. | 362,037 | 689,299 | 1,343,301 | 2,650,490 | 5,264,602 | 10,493,417 |
| 2026-07-21 | P1.5 mid-body yield + netpoller, proven via sleep(ms) (957 to 963) -- INDEPENDENT OPTIMIZATION PASS, NO CODE CHANGE. Verdict: NOTHING superlinear; this row records the feature cost, not a fix. The delta-vs-last-row is a FLAT +16 allocs / +1,160 bytes at ALL 6 rungs (allocs 361,458 to 10,481,058 becomes 361,474 to 10,481,074) -- a once-per-compile O(1) CONSTANT, NOT per-rung growth (a per-rung term would DOUBLE the delta each rung, +16 +32 +64 ...; it stays +16 flat). MECHANISM: the 3 new always-present IatSlot imports QueryPerformanceCounter / QueryPerformanceFrequency / Sleep, appended to the IatSlot enum in PeWriter, grow every x64-windows PE import table from 5 to 8 kernel32 slots UNCONDITIONALLY -- the import table is NOT gated on usesGt -- about 5.3 allocs / 387 bytes per import (name string + ILT/IAT thunks). CORPUS BLINDNESS (reported, NOT chased, instrument UNTOUCHED): the whole yield/timer/sleep/clock runtime -- __gt_drive_until, __gt_sleep, __gt_timer_add/check/earliest, __gt_now_ns in GtRuntime.maxon plus the unsorted timer array -- is gated on usesGt, and ScaleCorpus lists async/closures/generics as a documented structural blind spot and emits NO sleep, async or await, so usesGt is FALSE and that entire path measures ZERO. Only the unconditional import-table growth is visible. SUPERLINEAR AUDIT (the mandate): every phase is x1.72 to x2.01 allocs and at or below x2.31 bytes (parse x1.99, regalloc x1.99, lex x1.99; the byte tops criticalEdges x2.31, blockOrder x2.12, elimTrivialBlockArgs and sugarGate x2.11 are the documented Go-nextslicecap linear band, not a bend), nothing near x4. COMPLEXITY DEBT FILED (record, do NOT optimize -- under separate correctness review): the timer store is an UNSORTED O(n) array, so __gt_timer_earliest (peek-min) and __gt_timer_check are O(n) per call where n is the count of CONCURRENTLY-sleeping GTs -- a RUNTIME cost invisible to compile-time scale-test (the corpus never sleeps), correct for the current scale (at most 2 concurrent sleeps); the 16-byte entry layout (deadline at offset 0, gt at offset 8) is min-heap-compatible so a sift-down can be dropped in later if a harness sleeps many GTs at once. STALE-BASELINE CAVEAT: the delta is taken against the stored B2b log row (commit 285d806a6, the DIRECT parent); this commit touches ONLY maxon-shv2 sources, NO maxon-sharp/.cs, so the C# bootstrap is byte-identical and the same bin/maxon.exe compiles both, and the delta being FLAT at all 6 rungs is itself proof it is a once-per-compile constant rather than a stale-baseline artifact hiding per-rung work. But parallel-repo (arm64-macOS / x64-linux) rows may land on main between B2b and this row after merge, so the row DIRECTLY ABOVE this one may shift; the +16 is a TRUE single-commit delta on THIS branch. GATES: build 0 warnings; host 963/0 exit 0 (no leak); wasm 911/0 (sleep x64-gated); byte-identical across --workers 1 and 12; specs-shv2/fragments clean (empty git status proves byte-identical codegen); scale-test exit 0. | 361,474 | 688,356 | 1,341,598 | 2,647,267 | 5,258,339 | 10,481,074 |
| 2026-07-21 | P1.5 async subprocess spawn + yielding await (runProcess) -- INDEPENDENT OPTIMIZATION PASS, NO CODE CHANGE; this row records the feature cost, not a fix. VERDICT: NOTHING superlinear. TRUE single-rung delta, measured BRANCH-vs-CLEAN-PARENT (main 5e3cfac98, both freshly built from the SAME bin/maxon.exe, NOT the stale log row): FLAT +28 allocs / +2,040 bytes at ALL 6 rungs (rung0 362,437->362,465 ... rung5 10,505,721->10,505,749; bytes rung0 22,043,145->22,045,185 ... rung5 786,630,284->786,632,324) -- a once-per-compile O(1) CONSTANT, NOT per-rung growth (a per-rung term would DOUBLE each rung, +28 +56 +112...; it stays +28 flat). MECHANISM: 5 new always-present IatSlot imports CreateProcessA/GetExitCodeProcess/WaitForSingleObject/WaitForMultipleObjects/CloseHandle appended to the IatSlot enum in PeWriter; kernel32ImportNames() iterates IatSlot.allCases, so every x64-windows PE import table grows from 8 to 13 kernel32 slots UNCONDITIONALLY (NOT gated on usesGt) -- about 5.6 allocs / 408 bytes per import (name String + ILT/IAT thunks). SAME class as the prior mid-body-yield row (+16/+1,160 for 3 imports). CORPUS BLINDNESS (reported, NOT chased, instrument UNTOUCHED): the whole runProcess/subprocess runtime -- buildGtProcessRun, buildGtProcAdd/Count/Check, the process store, netpoll in buildGtDriveUntil in GtRuntime.maxon -- is gated on usesGt, and ScaleCorpus emits NO async/await/runProcess, so usesGt is FALSE and that entire path measures ZERO. Only the unconditional import-table growth is visible. STALE-BASELINE CAUTION: the delta this run PRINTS against the last log row (+991 -> +24,675, DOUBLING) is NOT this rung -- it is the intervening main commits between the stored log row (285d806a6) and this branch's parent (main 5e3cfac98), notably b323ecc47 (E3006 reject-duplicate top-level let/var, a real per-program pass); measuring branch-vs-parent isolates THIS rung to the flat +28/+2,040. SUPERLINEAR AUDIT (the mandate): every phase x1.72-x2.01 allocs (parse x1.99, regalloc x1.99, lex x1.99); byte tops criticalEdges x2.31, blockOrder x2.12, elimTrivialBlockArgs/sugarGate x2.11 are the documented Go-nextslicecap linear band, nothing near x4. RUNTIME ALGORITHMS (invisible to compile-time scale-test, corpus never spawns; reasoned analytically): (1) buildGtProcCheck scans ALL parked children per netpoll wake and there is one wake per child exit -> O(n^2) in CONCURRENTLY-parked children, but n <= ProcStoreCapacity=64, a compile-time constant, so it is a CAPPED constant, NOT superlinear in program size -- the SAME shape as the existing buildGtTimerCheck; NOT fixed (do not fix a capped constant). (2) buildGtProcessRun's cmdline byte-copy loop is O(command-line length) -- linear. (3) OPEN.md STANDING RULE holds: no whole-program query nested under a per-file one -- emitRunProcess is O(1), scanRuntimeUsage stays a single linear op-walk, isGtRuntimeCallee is a constant-prefix compare. GATES: build 0 warnings; new async-subprocess specs 6/0; full async band 47/0; workers 1 vs 12 BYTE-IDENTICAL (async band); specs-shv2/fragments clean (empty git status = byte-identical codegen); scale-test exit 0. | 362,465 | 690,111 | 1,344,881 | 2,653,606 | 5,270,790 | 10,505,749 |
| 2026-07-21 | P1.6-B1 sizeof(T) via layout descriptor + dictionary-passing (feat b10db06df + fix 94329dd0e), OPTIMIZATION PASS. CORPUS-BLIND ROW: ScaleCorpus is generics-free (no uses/with/sizeof), so EVERY new B1 path is unexercised here and a flat/byte-identical row proves nothing about them — complexity was established by READING + a targeted micro-benchmark, not this instrument. COMPLEXITY BY READING (per suspect, N=methods-per-type, C=self-call edges, I=distinct generic instances): (1) computeTypeDescriptorNeeds pre-scan = one peek-only walk of the type body, each token visited once (outer walk jumps each method body that the inner scan covers) = O(type-body tokens), LINEAR. (2) LayoutDescriptorRegistry = a Map keyed by GenericInstanceId; contains/labelFor/record all O(1) — NOT v1's linear scan. (3) mangleGenericInstance/mangleTypeArg = recursion over the instance's structural tree O(tree), called ONCE per distinct instance inside ensureLayoutDescriptor (gated by registry.contains), never inside a comparator/sort — no v1 O(N^2*depth) label sort. (4) sizeofOperandIsTypeParamAt/isSelfMethodCallAt = O(1), once per token in the linear method scan. (5) ensureLayoutDescriptor + registerRdataConstantLabeled = one blob per distinct gid, deduped by the registry AND idempotent-by-label in the rdata table = O(I). Always-run lowering/parser additions (buildLayoutNeedingFuncs O(functions*params) once; finalizeCallArgsWithLayout one O(1) map lookup per call; requireSelfForDescriptorForwarding/retypeGenericAliasConstructorResult short-circuit O(1) in non-generic context) are all LINEAR in program size and allocate NOTHING per-function on a generics-free program — which is why this row moves only with unrelated work. DEFECT FOUND + FIXED (this pass): solveDescriptorNeedFixpoint was a Gauss-Seidel iterate-to-stable = O(N^2 + N*C), QUADRATIC in ONE generic type's method count on a deep self-call chain (the machine-generated wide-type trigger the mandate names). MEASURED on a generated Sizer with N chained self-calling methods (mN reads sizeof(T); m(i) calls m(i+1), so need propagates one hop/round against declaration order -> O(N) rounds): parse phase 5.8/16.4/79.8/288 ms at N=100/200/400/800 = ~4x per doubling (parse was 73% of compile at N=800). REWRITTEN to reverse-reachability BFS over a counting-sort (CSR) reversed self-call graph = O(N+C): parse 3.1/6.0/12.3/30.3 ms = ~2x per doubling (linear); N=800 parse 288->30 ms (9.5x). Same least-fixpoint -> byte-identical codegen (suite 981/0, fragments clean, --workers 1==12 byte-identical, sizeof self-forward/transitive-two-hop pass with exit 1/16). CORPUS-INVISIBLE: the fixpoint runs only for generic types (none in the corpus), so this row is byte-identical pre- and post-fix. REBASED onto async-subprocess main (cfca516fb); the numbers in THIS row are the INTEGRATED tree (async-subprocess + B1), re-measured by the coordinator post-rebase. Delta vs the async-subprocess row directly above = FLAT +12 allocs at every rung (B1's once-per-compile setup: buildLayoutNeedingFuncs + two empty registries in create), plus a per-program BYTE term converging x2.0 (+1,336 rung0 -> +19,192 rung5 = LINEAR in program size, NOT a bend). Scale-test's own delta-vs-previous read a FLAT +28 allocs / +2,040 bytes at all 6 rungs -- the async-subprocess once-per-compile import constant folded in by the rebase. Nothing per-rung superlinear. SUPERLINEAR AUDIT: every phase x1.72-x2.01 allocs, <=x2.31 bytes, nothing near x4. GATES: build 0 warnings; suite 981/0 exit 0 no leak; --workers 1==12 byte-identical; fragments clean; scale-test exit 0. | 362,477 | 690,123 | 1,344,893 | 2,653,618 | 5,270,802 | 10,505,761 |
| 2026-07-22 | P1.5-B1a′ slice 1 — GT→system-stack syscall shim (x64-windows), commit 06d479ee0, OPTIMIZATION PASS. ALL DELTAS 0 at every rung (allocs+bytes byte-identical to the P1.6-B1 row above). EXPECTED and correct: the ScaleCorpus is NON-GT (never spawns a green thread), so `moduleUsesGt = moduleHasFunction(GtSpawnName)` is false for every corpus program and every `iatCall` takes the bare `encodeIatCall` path via `emitGtGatedIatCall`'s else-branch — the shim emits ZERO bytes and allocates NOTHING on this corpus. The 0-delta independently corroborates the coordinator's 0 non-async fragment churn: the `usesGt` gate keeps non-GT codegen byte-identical. SUPERLINEAR AUDIT (the point of this pass — every addition is O(1) per unit, so total encode stays LINEAR in program size): (1) BackendDispatch — the GT gate `moduleHasFunction` (an O(F) linear scan of module.functions) is HOISTED ABOVE the `for func` encode loop, computed ONCE and threaded into every emitFunctionChunk as a bool; it also DEDUPS the pre-existing `'gtChunks'` scan, so the encode phase now makes exactly ONE O(F) scan for the GT question (was implicitly one already), NOT the O(F²) it would be if computed per-function. O(1) per function. (2) The shim `emitGtShimmedIatCall` is O(1) per iatCall: the stack-arg copy `for k in 0 upto stackArgWords` is bounded by CreateProcessStackArgWords=6 (WriteFile=1, all others 0), and `resolveChunkRel32Fixups` runs over a SHIM-LOCAL fixup list of exactly 2 fixups / 2 labels (never the enclosing function's), so no module-wide scan. gtShimStackArgWords is two equality checks. Two small per-shim allocations (ChunkRel32FixupArray+ChunkLabelOffsetMap) are O(1) per iatCall = linear total, and fire only in GT modules whose iatCall count is linear in program size — deliberate local-fixup scoping, not a bend. (3) Context-switch TIB save/load (emitSaveTibIntoGt/emitLoadTibFromGt, 4 encoder calls each) are built ONCE inside buildGtContextSwitchChunk — O(1) for the whole program. (4) GtRuntime adds a fixed number of Std ops to __gt_init/__gt_spawn (built once) and one DataSectionEntry — all O(1). New X64Backend encoders (encodeMovGsAbsReg/encodeMovRegGsAbs) are constant byte pushes. CONCLUSION: no superlinearity introduced; NO CHANGE NEEDED. Raw doubling ladder confirms whole-compiler linearity (allocs x1.90→1.99 per doubling, bytes x2). Gates: build 0 warnings; scale-test exit 0. | 362,477 | 690,123 | 1,344,893 | 2,653,618 | 5,270,802 | 10,505,761 |
| 2026-07-21 | P1.6-B2 (managed generic type args: owned + dropped-once via synthesized per-instance __destruct_<mangled> cascade) OPTIMIZATION PASS (independent). DECISION: NO CODE CHANGE. CORPUS-BLIND ROW: ScaleCorpus is generics-free (no uses/with), so the ENTIRE B2 generic-instance path -- destructor synthesis (synthesizeGenericInstanceDestructor/installGenericInstanceDestructors), the call-site type-param FEED consume, the typeIsManaged/typeArgIsOwned classifiers, and the needs-closure -- is UNEXERCISED here; complexity below is by READING, not this instrument. ATTRIBUTION of the non-flat +87/+155/+291/+563/+1107/+2195 alloc delta vs the B1 row (ratios 1.78->1.88->1.93->1.97->1.98, converging to x2.0 = LINEAR; ~17 B/box, 37200/2195=16.95): it is the per-CALL ConstructorInstance union HEAP-BOX. B2 threads a ConstructorInstance (instance(gid) or none) from parseCall / dispatchMethodOnBase / parseQualifiedCall(constructorInstanceOf) down through parseCallNamed->emitCall->applyCallerConsume so the caller-side consume can move a managed type-arg into an owning box. A Maxon union value is a heap object EVERY case (the bootstrap that compiles shv2 boxes it -- same fact SignatureIndex's DeclaredFunctionAlias guard names), so every parsed call mints one box; the generics-free corpus makes every one `.none`. LINEAR (one box per parsed call), NOT a masked superlinear -- structurally one alloc/call; the sub-2.0 early ratios are the FIXED mainSource block diluting a per-call term. WHY NOT REDUCED: (a) it is a LINEAR per-call CONSTANT FACTOR -- 2195/10,507,956 = 0.02% of rung-5 allocs -- which the mandate deprioritizes vs growth curves; (b) it MIRRORS the pre-existing, accepted ReceiverArg (none or value(id)) per-call union box already threaded through the same parseCallNamed -- the project's established parser representation for an optional per-call descriptor, so keeping the union is the consistent tidy-O(n) the mandate prefers over a clever-O(n); (c) the obvious cheap removal (one shared module-level `ConstructorInstance.none`, the sharedEmptyDescriptorNeeds pattern) is UNSAFE and was PROVEN so this pass: it panicked with a use-after-free (nil/invalid access addr 0xff..f9, top frame parseQualifiedCall) because the bootstrap MOVES/consumes the returned+passed union (constructorInstanceOf RETURNS it, a move) rather than borrowing it, so the shared global's refcount hits 0 and it is freed under the next call; sharedEmpty* Maps/Arrays are safe ONLY because they are STORED (increfed), which this borrow-only argument path never does. A safe removal would replace the union with a sentinel-encoded GenericInstanceId across ~6 signatures -- scope creep for 0.02%. BIG-O PER SUSPECT (by reading; every term 0 on the corpus): (1) scanConsumeBits feed recording -- the `feeds` array is GATED by genericScope; outside a generic type initialTypeParamFeeds returns the SHARED empty array (ZERO per-function alloc) and recordParamConsume's feeds-hash loop does not run; O(body tokens), LINEAR, allocation-free on every non-generic function -- NOT the delta. (2) applyCallerConsume/transferConsumedArgs -- O(args)/call (unchanged from P1.4a); the new argIsConsumedAt->instanceFeedConsumes is O(1) (constructorInstance=none short-circuits on the corpus); a fed managed arg costs O(argFields), a bounded per-type constant. LINEAR/call. (3) typeIsManaged O(1) (tag test + Map.contains; a genericInstance tag returns true WITHOUT recursing); typeArgIsOwned/genericInstanceHasManagedField O(nested-type-size)/query, un-memoized but bounded by a small source constant and LINEAR in query count -- NOT a per-file*per-struct O(n^2/n^3): the queries are per-instance and the distinct-instance count is tiny. (4) closeDestructorNeeds/addNestedInstanceDestructors (flagged highest-risk) -- a while-changed FIXPOINT re-scanning ALL structs+unions+instances per iteration, but this is the SAME shape as the PRE-EXISTING struct/union closure that B2 merely EXTENDED with an instance loop, NOT a new quadratic. Iterations are bounded by max destructor NESTING DEPTH D (one level propagates per pass) => O(D*(S+U+I)) with D a SOURCE-bounded small constant that does NOT grow with program size (a bigger program has more types, not deeper nesting). Worst case O(T^2) ONLY for a pathological depth-T linear nesting chain (Box with (Box with (... T deep ...))), which is unrealistic and parser-recursion bounded. Distinct from B1's solveDescriptorNeedFixpoint (over the method-call graph, which DOES scale with program size -- hence its O(N+C) rewrite): a depth-bounded closure is effectively linear, so FILED bounded, NOT fixed (a fix would rewrite pre-existing struct/union code). mangleGenericInstance is re-called per instance per iteration (allocates a mangled String) -- a constant-factor inefficiency, zero on the corpus. (5) GenericArgNode nested interning (resolveGenericArgs/resolveArgNode/internNestedNode) -- a single pre-order walk of the flat node list, O(nodes), O(1) intern per node, depth-first, no re-intern/re-sort (no v1 O(N^2*depth) label sort); once per generic typealias; zero on the corpus. LINEAR. GATES: shv2 build exit 0 + 0 warnings + error-codes OK; suite 995/0 exit 0, no leak/101; scale-test exit 0. No compiler code changed this pass, so codegen is byte-identical to committed B2 (02786836f) and the fragments + --workers 1==12 gates are inherited unchanged. | 362,564 | 690,278 | 1,345,184 | 2,654,181 | 5,271,909 | 10,507,956 |
| 2026-07-22 | P1.5-B1a′ slice 2 — relocating morestack + 2 KB GT stacks (x64-windows). Suite 989→993/0 (+4 async-stack-growth), morestack disasm-verified line-by-line vs v1. DELTA +5 allocs / +216 bytes CONSTANT at every rung (was 0 at slice 1) — FLAT, not growing, so O(1) per compile, NOT superlinear. EXPLAINED: the ScaleCorpus is NON-GT, so the new GT machinery emits NOTHING on it — the prologue guard is gated `usesGt && !isRuntimeFunction && frameBytes>0` (false for every corpus function, and computeGuardFrameBytes is itself gated behind `mightGuard` so a non-GT function does not even pay the callee-saved scan), the `__gt_morestack` chunk is appended only when `moduleUsesGt`, and `osFreePages` is emitted only inside `__gt_drive_until`. The ONLY thing that touches a non-GT program's output is the new `IatSlot.VirtualFree` kernel32 import: like every IatSlot it is UNCONDITIONALLY present in every PE's import table (kernel32ImportNames iterates IatSlot.allCases), so its name string + IAT/ILT thunks + Hint/Name entry are built ONCE per compile in the link/writeExe phase = the fixed +216 bytes / +5 allocs. Constant across all 6 rungs confirms it is per-compile, not per-program-size. SUPERLINEAR AUDIT of the GT additions (fire only on GT modules, whose iatCall/function counts are linear in program size, so still linear total): morestack chunk is built ONCE per GT program (buildGtMorestackChunk, O(1)); the guard is O(1) per guarded function (a fixed instruction sequence + a guard-local 1-label fixup map, never a module-wide scan); computeGuardFrameBytes is O(ops) per guarded function = the same linear scan the prologue pass already does, and runs only for GT modules; osFreePages lowering is a fixed 4 TargetOps. CONCLUSION: no superlinearity; the flat +216 is the cost of one new unconditional kernel32 import. Gates: build 0 warnings, suite 993/0 workers-invariant, scale-test exit 0. | 362,482 | 690,128 | 1,344,898 | 2,653,623 | 5,270,807 | 10,505,766 |
| 2026-07-22 | P1.6-C (per-instance ranged typealiases in generics) OPTIMIZATION PASS (independent). DECISION: CODE CHANGE (front-end only, no codegen change). CORPUS-BLINDNESS: ScaleCorpus is generics-free (no uses/with/nested-typealias), so the per-instance-alias paths (nested-typealias sweep recording, qualified Instance.Member resolution, per-instance E3005 arg check) are UNEXERCISED here; the Big-O below is by READING, cross-checked against the --per-type instrument. BIG-O PER SUSPECT (all LINEAR/O(1); confirmed by the --per-type growth column, every P1.6-C type x<=1.80 < 2.0): (1) nested-typealias sweep (recordScannedType/recordScannedSignature) -- an O(1) atNestedTypeAlias() peek per type-member and an O(1) enclosingInnerAliases.count() gate per method; innerAliasParamsIn (the only per-param walk) is GATED off unless enclosingInnerAliases is non-empty, so ZERO on generics-free; NO whole-program lookup nested in the per-file sweep (no O(files^2)). (2) innerAliases/methodInnerAliasParams registries -- O(1) Map lookups (isInnerAlias/innerAliasOf/innerAliasMember/methodInnerAliasParamsOf), no scans. (3) containsQualifier / qualified resolution -- an allocation-free O(len) byte scan per type-ref/coercion; the dotted-path split + queryProgramSignatures fetch is reached only for a name carrying a '.', never the plain-alias hot path. (4) valueInstanceAlias map -- shared-empty COW, O(1) populate/lookup, reset+save/restore at closure boundary. (5) erasure query (resolveNamedType) -- plain-alias checked FIRST (O(1) map), qualified path only for dotted names, queryProgramSignatures memoized (cache hit). (6) per-instance E3005 arg check -- O(1) map lookup per call, O(args) only when the callee has an inner-alias param. NO superlinear term. ATTRIBUTION of the committed-P1.6-C generics-free delta (isolated by rebuilding at 75bdb05fc pre-P1.6-C vs c38ad6306): committed P1.6-C added +2505/+4237/+7701/+14629/+28485/+56197 allocs (converging x2.0 = LINEAR, but a LARGE constant), REFUTING the implementer's 'allocates nothing on the generics-free path' claim. Dominant source = Parser.create allocating 5 NEW collections per construction (3 sharedEmpty* COW anchors + 2 working fields enclosingInnerAliases/valueInstanceAlias freshly created then immediately overwritten) -- a DIVERGENCE from this very file's documented sharedEmptyDescriptorNeeds/sharedEmptyTypeParamFeeds pattern (module-level globals, ONE alloc per compile, parsers INCREF). Secondary = checkPerInstanceArgs minting an InnerAliasParamLookup union HEAP-BOX on EVERY slotted call (bootstrap boxes every union case). FIX (this pass): (a) moved the 3 anchors to module-level globals and pointed the 2 working fields at them (incref, no per-parser alloc) -- realizing the implementer's stated intent; safe because the shared collections are STORED/increfed and never mutated (COW before any write), the exact invariant sharedEmptyDescriptorNeeds relies on. (b) gated checkPerInstanceArgs on a new allocation-free ProgramSignatures.hasInnerAliasParams() bool BEFORE the union-minting lookup, mirroring the parser's enclosingInnerAliases.count() gate and aggregateNameFor's containsQualifier gate. RESULT: generics-free delta vs pre-P1.6-C dropped from +2505..+56197 to +74/+82/+98/+130/+194/+322 (removed -2431..-55875, ~99.4%). Residual = the 2 per-file sweep out-arrays InnerAliasEntryArray/MethodInnerAliasParamEntryArray (x1.21, consistent with the 9 sibling per-file working arrays in foldDeclaredSignaturesInto -- kept) + one-time module globals + fixed-mainSource splitQualifiedName/QualifiedNameParts unions (x1.00). GATES: shv2 build exit 0 + 0 warnings + error-codes OK; suite 1005/0 exit 0, no leak/101; per-instance-typealias 7/7 (E3005 wrong-instance + cast-launder errors still fire); fragments byte-identical (git status specs-shv2/fragments empty -- front-end change, no golden moved); --workers 1==12 identical outcomes. The row's numbers are the POST-FIX build. | 362,650 | 690,380 | 1,345,318 | 2,654,379 | 5,272,235 | 10,508,538 |
| 2026-07-22 | P1.7 slice 1 (Array scalar/trivial-element core) OPTIMIZATION PASS (independent). DECISION: NO CODE CHANGE — no superlinearity found; the slice is allocation-neutral on this corpus. TRUE P1.7 MARGINAL (measured by building AND running BOTH binaries directly: HEAD 181be5d5c vs its parent HEAD~1 33ee161a1): +0 allocs at EVERY rung, +8 bytes FLAT (constant, not per-rung — 704-696 = 1024-1016 = ... = 8). That +8 is one grown RuntimeUsage record: the new `usesArray bool` field rounds the single per-compile record up 8 bytes; same allocation COUNT, +8 bytes, O(1) per compile. That is the ENTIRE measurable footprint of the slice on the array-free/generics-free/field-chain-free ScaleCorpus. STALE-BASELINE WARNING for THIS row's delta columns (+29/+45/+77/+141/+269/+525 allocs): they are vs the LAST LOGGED ROW (the P1.6-C optimization pass, ~commit 5095643cc), NOT vs P1.7's parent. The PARENT binary (HEAD~1, containing NONE of P1.7) reproduces the IDENTICAL +29/+45/+77/+141/+269/+525 delta against that same log row — so that entire term is PRE-P1.7, contributed by the two P1.6-C follow-up commits (e65a164c9 'per-instance alias decays to plain int at all coercion sites', 6ce5eb0dc 'P1.6-C CLOSED') that landed AFTER the row was recorded. It fits 13 + 16*2^n exactly (successive differences 16/32/64/128/256 double), i.e. a fixed 13-alloc constant + a 16-per-doubling-unit linear term = LINEAR, and not this pass's subject. The implementer's report attributed this stale-baseline delta to P1.7; the real P1.7 delta is +0 allocs / +8 bytes-flat. SUPERLINEARITY HUNT (corpus is array-free, so the new paths are verified by READING, cross-checked against the per-phase alloc diff HEAD-vs-parent = 0 in every phase). All new algorithms O(1) or linear: (1) structHasArrayField = O(fields), called ONCE per referenced struct in noteDestructorUsage's single per-compile pass = O(total fields) linear, NOT per-access; int fields hit fieldTypeIsArray's `tag != named` early-out with zero alloc (int is TokenKind.int -> MaxonType.integer, never `named`), so it is the allocation-free twin of structHasStringField on scalar fields. (2) fieldTypeOf / typeIsManaged / managedFieldDropCallee generic-alias retag = O(1) map lookups, gated behind maxonTypeTag==named. (3) resolveFieldChainFrom = fieldTypeOf computed ONCE and threaded into structLayoutOfField (the implementer's transient double-fieldTypeOf is gone; confirmed no redundant recompute remains); O(1) per hop, O(chain) per chain = linear. (4) isArrayInstance / arrayElementType / arrayElementIsManaged / isArrayBaseName = O(1); every parser call site (postfix loop, binding+value method dispatch, qualified static call, field-chain end) is gated behind a tag/alias check that short-circuits off the array path. (5) installArrayRuntime = O(1)-gated on usesArray, builds a FIXED ~18 helpers ONCE, independent of program size. (6) isArrayRuntimeCallee / isCompilerInternalCallee = O(prefix) bytesStartWith per call = O(1); internArrayIntegerInstance = idempotent O(1) intern, once per file that contains a `[`. (7) dispatchArrayMethod = a fixed 16-way name compare = O(1) constant per array method call. NO nested scan over ops/values/blocks/symbols, NO findFirst-in-a-loop, NO per-element fixpoint/liveness/layout rebuild. Whole-compiler per-phase and per-rung alloc ratios all <= 2.0 (rung4->5 x1.99), tracking the corpus's own x1.93..1.99 code growth — no phase superlinear. GATES: shv2 build exit 0 + 0 warnings + error-codes OK; suite 1079/0 exit 0, no leak/101; fragments byte-identical (git status specs-shv2/fragments empty). No code change was committed by this pass; the absolute numbers in this row are the committed 181be5d5c build. | 362,679 | 690,425 | 1,345,395 | 2,654,520 | 5,272,504 | 10,509,063 |
| 2026-07-22 | P1.5 value-merge residuals #31/#80/#81 (match/ternary value-merge finish) OPTIMIZATION PASS (independent). DECISION: NO CODE CHANGE — the rung is already LINEAR with a tiny constant and carries no superlinear term; the one common-path allocation is a per-file working array consistent with its siblings, and the whole-program drain is O(Sigma arms), all O(1) map lookups. ATTRIBUTION (isolated by diffing pre-P1.5 main@33ee161a1 vs worktree@ccb4d0211, both freshly built on the same idle box, so the delta is exact and reproducible): P1.5 adds +104/+176 allocs at rung0/rung1 (TRUE delta). The row-over-row deltas the log shows (+133/+221/+397/+749/+1453/+2861) are vs the P1.6-C-OPT baseline row; ~+29 of the rung0 gap is the intervening P1.6-C decay/review fixes (e65a164c9, 1430ad6a0), NOT P1.5. LINEAR: row-delta ratios 1.66->1.80->1.89->1.94->1.97 -> 2.0; the true-delta split is phase:signatures +83/+151 (x1.82) and phase:parse +19/+23 (x1.21), every allocation ~48 B (empty-array-sized). SOURCE: the sole common-path allocation P1.5 adds is the 11th per-file array PendingFnMergeCheckArray in FileParseArtifact.create — allocated once in the signature-sweep throwaway artifact (queryProgramSignatures builds one Parser/FileParseArtifact per file and discards all 11 working arrays, exactly as it already discards the 10 pre-existing siblings) and once in the real-parse artifact — plus 2 one-time arrays (Project.create, resetMergeTargets). CORPUS-BLINDNESS: ScaleCorpus has ZERO ternary/match value-merges and ZERO function typealiases, so the whole-program drain, the PendingFnMergeCheck records, and FunctionTypeAlias.display are ALL UNEXERCISED here; their Big-O is by READING. TASK-2 (COW vs fresh): pendingFnMergeChecks stays FRESH-per-file, matching its 10 FileParseArtifact.create siblings (funcSignatures/funcReturnTypes/rangeChecks/typeAliases/functionTypeAliases/...), NONE of which are COW'd. It is NOT a Parser.create sharedEmpty* COW anchor: those anchors are populated only for rare generic files and were moved to module globals last pass; this array is likewise populated only for rare merge files, but COW'ing it alone while its siblings stay fresh is exactly the inconsistency the file's pattern forbids ('do NOT COW something its siblings don't'). DRAIN O-ANALYSIS (checkValueMergeSignatures, Queries.maxon): per recorded merge, per arm — armFnTypeResolvable = funcSignatures.contains OR functionTypeAliases.contains (both Map with ByteArray keys, O(1)); armFnTypeDisplay = Map.get O(1) + formatFnTypeDisplay O(params). Total O(Sigma arms x params) = O(program), NOT O(merges x functions); commitPendingFnMergeChecks is O(Sigma checks) with no file^2 loop; no whole-program scan nested in the drain. buildFunctionDisplay: the 'fn(...) returns ...' display is built ONCE per function typealias on the REAL parse (readTypeAliasDeclaration buildFunctionDisplay:true at Parser.maxon:3450) only; the token sweep (recordScannedTypeAlias, :3069) and readNestedRangedAlias (:3688) pass false — no double-build. NOTED, not fixed (constant factor on a corpus-unexercised path, not the growth mandate; flagged to reviewer): readFunctionTypeAlias (Parser.maxon:3837) allocates paramTypes MaxonTypeArray unconditionally even for buildDisplay:false, though the :3835 comment claims the false path 'pays for neither the array nor the string' — still O(function-typealias count) = linear. GATES: no code change, so the committed build's gates stand (suite 1014/0 no leak; --workers 1==12 byte-identical; specs-shv2/fragments byte-identical — front-end only); worktree git status clean; scale-test exit 0. REBASED onto P1.7 main (ca9dc2b61): coordinator re-measured on the integrated tree — allocs UNCHANGED (P1.7 true delta +0 allocs), bytes +8/rung (P1.7 usesArray growing the once-per-compile RuntimeUsage record). | 362,783 | 690,601 | 1,345,715 | 2,655,128 | 5,273,688 | 10,511,399 |
| 2026-07-22 | P1.7 slice 2 (b"…" byte-string literals) optimization pass: INERT on corpus (no byte-strings) + linear-by-reading audit — no superlinearity. decodeByteStringLiteral single-pass O(literal len); registerByteArrayLiteralBlob reuses registerRdataConstant's amortized-O(1) rdataSizeSoFar cursor + byte-keyed dedup (NOT per-literal re-sum); internArrayByteInstance idempotent hash intern (once/file); __arr_decref drop-guard is a constant-time capacity<0 branch, not a scan. | 362,679 | 690,425 | 1,345,395 | 2,654,520 | 5,272,504 | 10,509,063 |
| 2026-07-22 | P1.5 #78 (indirect calls carry callee float result/param types, x64/arm64/wasm) OPTIMIZATION PASS - NOTHING SUPERLINEAR, NO CODE CHANGE, BIT-FOR-BIT INERT ON CORPUS. The +104..+2336 allocs / +4984..+112120 bytes this row prints is a STALE-BASELINE ARTIFACT (the OPEN #22 trap, same as the 2026-07-18 P1.4b row), NOT the rung's cost. Proven the immune A/B way: rebuilt the rung's OWN PARENT b2ca9c1d3 from source with the same bootstrap and ran scale-test on it -- parent and branch (HEAD) produce IDENTICAL numbers at EVERY rung and EVERY phase (rung0 362,783/22,058,513; rung5 10,511,399/786,871,828; lowerMaxonToStd/isel/regalloc all identical to the byte), and the PARENT already prints this same +104..+2336 delta vs the last logged row. So the rung's true footprint is EXACTLY 0 allocs / 0 bytes; the printed delta is pre-existing main drift between the last logged capture and b2ca9c1d3, present with none of these two commits. WHY inert: ScaleCorpus emits no function values / indirect calls, so appendCallIndirect and every backend callIndirect arm never fire; the sole per-every-program change -- the StdOp union gaining SCALAR resultType+argFloatMask on the callIndirect variant -- did NOT grow sizeof(StdOp) (callIndirect is not the union's widest variant), so module.ops is byte-identical. SUPERLINEARITY AUDIT (read-only, all LINEAR): the wasm functype intern is amortized-O(1) -- internFuncType (WasmBinary.maxon:917) dedups on the ENCODED-BYTES key via funcTypeDedup, a stdlib open-addressed hash Map (get/upsert O(1), key-hash O(args)); it is NOT the old O(distinct-signatures) findFirst scan. buildIndirectCallTypeIdxMap is ONE O(module-ops) walk; per callIndirect the key build (encodeFuncType/indirectCallParamValTypes) is O(args) and emitCallIndirect (StdToWasm.maxon:1436) is O(args) key + O(1) get -- total wasm cost O(module + sum-args), NO per-call scan over interned types. x64/arm64 emitIndirectCallArgMoves are single O(args) loops with two independent GPR/FP counters (argIsFloat is a bit test), no nested scan; indirectResultRegister/arm64IndirectResultRegister are O(1); front-end appendCallIndirect mask build is O(args). No code changed -> codegen trivially neutral (git status + specs-shv2/fragments/ clean). | 362,783 | 690,601 | 1,345,715 | 2,655,128 | 5,273,688 | 10,511,399 |
| 2026-07-22 | P1.7 slice 3a (CONCRETE managed-element arrays) optimization pass. Audit of the 5 slice surfaces: (2) element-destroy stamp arrayElementDestroyValue->managedFieldDropCallee is O(1) for String (early return) and O(fields) for a struct/union element via structHasManagedField — the PRE-EXISTING shared drop-callee resolution, field-count bounded in practice, no new class; (3) emitElementDestroyRange is emitted ONCE per module (installArrayRuntime, __arr_decref/__arr_clear/__arr_resize share it), the runtime walk is correctly O(length); (4) moveManagedValueInto / transferConsumedArg promotion is O(1) per element/arg and the literal parse moves each element in one-at-a-time so an N-element literal is O(N) not O(N^2); (5) the functionRef bare-__-address path (lower/scan/thunk-collect) is O(1) per funcref (a __ prefix check). FIXED (1): internArrayLiteralAggregateInstances over-interned Array-with-<every declared struct/union> once per [<identifier>...]-literal FILE, and structTypes/enumTypes ACCUMULATE across files, so it was O(files x types) throwaway allocations (each arrayInstanceForNamedElement builds a MaxonTypeArray+MaxonType.named+instance key only to hit the interner and discard). Added arrayLiteralElementsInterned (Set with ByteArray): a name already over-interned is skipped, so interning is O(types); a count early-out short-circuits the whole pass once every declared type is interned. giid-PRESERVING: the skipped calls were idempotent interner hits that assigned no gid, so the first-intern order (and every emitted gid) is unchanged — proven by the spec suite staying 1129/0 with ZERO fragment movement (git clean but for the one source file). INERT on the array-free scale corpus: the aggregate loop never runs (no [<ident>...] literals), phase:signatures byte-identical and x2.00. Movement vs slice-2 baseline is a FLAT +8 allocs/+336 bytes at EVERY rung (rung0==rung5) — the one empty dedup Set constructed per compile in ProgramSignatures.create(), a once-per-compile constant, NOT superlinear; the doubling remainder (+104..+2336) is slice-3a's own feature overhead vs slice-2, linear (x2/rung), not this pass. The O(files x types)->O(types) win only manifests on multi-file programs that heavily use [struct,...] aggregate literals (a brand-new feature; self-compile and the corpus have none). | 362,791 | 690,609 | 1,345,723 | 2,655,136 | 5,273,696 | 10,511,407 |
| 2026-07-22 | P1.7 slice 3a REVIEW fixes (element leak on set/clear/resize-shrink + E3102 cross-arg double-consume). Explains the movement vs the slice-3a optimization-pass row above. (A) The E3102 double-consume guard adds ONE ValueIdArray (`consumedOwned`) per CONSUMING call in transferConsumedArgs plus an O(consumed-args) scan — a LINEAR constant factor: allocsDelta DOUBLES per rung (+40,+80,+160,+320,+640,+1280 rung0..5), i.e. proportional to program size, all phase ratios stay x2.00 (phase:parse x1.91..x1.99). It is the same shape as the pre-existing per-struct-literal `movedOwningValues` allocation and is +1280/10.5M = 0.012% of rung-5 allocs. Not a new complexity class. (B) The array-runtime leak fix (managed elements destroyed at set/clear/resize-shrink via the shared emitDestroyRangeIfManaged gate, folding __arr_decref's inline gate in too) is INERT on this array-free corpus: its extra blocks live in __-prefixed runtime helpers emitted ONCE per module (installArrayRuntime), not per user program, so it adds nothing to the ladder. Suite 1131/0 no-101; --workers=1 == --workers=12. | 362,831 | 690,689 | 1,345,883 | 2,655,456 | 5,274,336 | 10,512,687 |
| 2026-07-22 | P1.7 COW slice B (O(1) zero-copy array slices; `__mm_incref` returns, the view being its first caller). +78 allocs / +3,566 bytes / +24 `.text` bytes, **FLAT at EVERY rung 0-5** — a per-MODULE constant, not a growth class; all phase ratios stay x2.00. ATTRIBUTION PROVEN BY A/B, NOT ASSERTED: a temporary patch restoring `emitRefcountCheckToLastOwner`'s old THREADED entry->lower shape (header+count computed once in `entry`, reused in `lower`), leaving all 616 other changed lines untouched, returned every rung to the previous numbers EXACTLY (0/0/0) and codeBytes to -24. So the entire movement is the 5 Std ops `emitAdjustRefcount` adds to `__mm_decref`'s `lower` block (header base recomputed twice + refcount reloaded) at the ONE reachable call site per module; per-phase it decomposes +6 un-bucketed / +17 isel / +55 regalloc / +1 sugarGate / **-1 prologueEpilogue** — the THREADED form allocates MORE there, because keeping header+rc live across the edge raises register pressure in `__mm_decref`, so threading is not free either. BOUGHT DELIBERATELY: it puts `owners - 1` and the refcount RMW in ONE function each, now that `ArrayRuntime.__arr_cow_detach`'s arm 2 reads that convention ACROSS a file boundary (a respelled `rc != 0` would not error — it would silently mutate a shared buffer), and makes the future green-thread `LOCK` prefix land on one site instead of two. Cost: 0.021% of rung-0 allocs, 0.00074% of rung-5. The 454-line ArrayRuntime COW change is otherwise INERT here (array-free corpus, `installArrayRuntime` never runs). NOTHING SUPERLINEAR: `emitArrayView` contains no loop, and nested views FLATTEN — a view's `parent@32` is always the ORIGINAL base, never another record, so there is no chain to walk. Measured, not read: 2,000,000 slices at constant total count with nesting depth 1/10/100/1000 → 121/97/107/98 ms (flat; a parent-chain walk would make depth 1000 ~1000x slower), and slice LENGTH 1k->100k at 1,000,000 slices → 48.9/49.4/50.9 ms, ~50 ns/slice, flat across 100x. Both are 100x-1000x discriminators, far outside the +-30% code-layout noise floor. Suite 1197/0 no-101; --workers=1 == --workers=12. | 362,909 | 690,767 | 1,345,961 | 2,655,534 | 5,274,414 | 10,512,765 |
| 2026-07-22 | P1.0d.4 residual slices A+C MERGED (float literals + math intrinsics). SLICE A — exact decimal->IEEE-754 float literals. parseFloatBits applied the decimal exponent by repeated *10 in an i64, which WRAPPED: 1.0e20 gave a wrong value, 1.0e64 silently became 0.0 (10^k contains 2^k, so k>=64 is exactly 0 mod 2^64), and 1.0e63 HUNG THE COMPILER FOREVER (10^63 == 2^63 == i64.min, negative, so the shiftUp normalization loop doubled a negative without bound). NOT A PORT: v1's NumberParsing.maxon:13-226 is BYTE-IDENTICAL to shv2's and carries the identical bug (shv2 inherited it by copying); maxon-sharp sidesteps it via .NET double.Parse. Replaced with bounded-width base-2^32 limb magnitudes; significand accumulated in 9-digit strides to a 768-significant-digit cap plus a STICKY flag feeding round-to-nearest-even; MaxBigLimbs=144 enforced by a panicking requireLimbBudget; subnormals and underflow-to-zero added (NEITHER EXISTED); overflow now decided by the CONVERTED VALUE, not a decimal estimate. COST +1,106/+2,194/+4,370/+8,722/+17,426/+34,834 rung0..5 — exact doubling per rung, LINEAR in program size, attributed exactly: rung 0 holds 278 float literals (--emit-corpus) and 1106/278 = 3.98, i.e. an exact conversion needs two heap magnitudes where the wrong i64 pair needed none, at 2 allocations per Array (Self{} envelope + first push's ensureCapacity). 0.30% of rung-0 allocations, 0.33% of rung-5's; all phase ratios unchanged x2.00-x2.11; NOT a new complexity class. The 768+sticky correctness fix itself is +2 allocs/+80 bytes FLAT at every rung. NO small-value fast path (it would be a second implementation of one fact) and none is needed: allocations are FLAT from 1 to 34 significant digits because a magnitude's width tracks its VALUE (empty array == zero), and 91% of the 1,402 float literals in shv2+stdlib carry 2-3 digits — ~1.9ms of a ~13s self-build (0.015%). Independent optimizer: NO superlinearity, NO code change. It verified the implementer's bigShiftLeft fix (a whole-limb insert(0,..) loop, Theta(shift x count)) by REVERTING it — real on allocations (-3 allocs/-696 bytes per literal) but A WASH on wall time, because Array.insert delegates to a memmove builtin (stdlib/Array.maxon:282) while the replacement is a bounds-checked element loop; kept because it is asymptotically right and allocates less, but it did NOT buy the time it appears to. REJECTED WITH MEASUREMENTS: hoisting pow10 out of bigMulPow10's stride loop (-0.9%..-3.1% wall, ZERO allocation change, and identical code read 79,368 then 70,215 us — a 13% load swing, so the gain is BELOW THE NOISE FLOOR of the only instrument that can see it); factoring 10^k = 2^k*5^k to fold 2^k into the binary exponent (would cut operand width 1.43x and the dominant divide loop ~30%, but it is a constant factor that rewrites the exponent bookkeeping the brand-new subnormal/underflow paths depend on — bit-risk against freshly-settled correctness); Knuth algorithm D for the 53-step mantissa loop; fusing bigCompare+bigSubtract. Worst admitted input is 768 digits at e-300 (~350-400 us/literal), NOT 1000 digits at e-324 (201 us) — the latter falls below the least subnormal, significandBits clamps to 0, and the divide loop runs ZERO iterations. INDEPENDENT REVIEW FOUND A BLOCKER THE SUITE WAS GREEN OVER: sciExp was clamped at a FIXED 100000, but it competes with decExp in magnitudeExp = (sigDigits-1)+decExp+sciExp, and decExp moves ONE PER DIGIT — so a long enough digit run OUT-PULLS the clamp and FLIPS THE VERDICT IN BOTH DIRECTIONS. 0.<200001 zeros>1e999999999 (true magnitude 1e999799997, astronomically past f64.max) was ACCEPTED AND COMPILED TO 0.0 with no diagnostic; 1<200000 zeros>.0e-999999999 got a SPURIOUS E2011. Both verdicts confirmed against the bootstrap oracle. The ceiling is now bytes.count()+DecimalWindowSpan, swept over 96 cases spanning the cancellation region, 0 disagreements, with two regression tests PROVEN to have teeth (reintroducing the fixed clamp fails exactly those two). Review also DEDUPED the carry flush, written THREE times (bigAddSmall, bigMulSmall, and a third spelling in bigShiftLeft that pushed UNMASKED and deferred the budget check) with nothing making them agree — folded into bigPushCarry so requireLimbBudget's claim that every growing site ends there is STRUCTURAL, not conventional. And it CORRECTED THE 768 BOUND'S JUSTIFICATION: the comment claimed midpoints terminate within 752 digits, but 752 is the width of 5^1075 ALONE, ignoring the odd significand which runs to 2^54-1 and adds up to 16 more — the true widest rounding boundary (2^54-1)*2^-1075 needs EXACTLY 768, so the budget has ZERO slack while its stated reason would have justified shaving 16 digits off it (the same wrong number appeared in the spec prose: one fact written down twice, wrong both times). Two further stale bounds that contradicted each other (341 vs the real 1091) corrected; the extremum literal reaches exactly 114 limbs against the budget of 144, measured by narrowing MaxBigLimbs and binary-searching, so requireLimbBudget CANNOT fire on admitted input. MEASURED DEBT FILED WITH ITS TRIGGER: bigMulPow10 and the digit accumulation are Theta(strides x limbs) — quadratic in exponent and digit count — LINEAR IN PRACTICE only because the real corpus tops out at 21 significant digits and the only extreme-exponent literals are a handful of type bounds. The trigger that would bend it is A WIDER FLOAT TYPE: binary128 pushes the decimal exponent 308->4932, the digit budget 768->~11,500 and MaxBigLimbs 144->~1,700, growing both quadratic terms ~15x per dimension (~225x). SLICE C — floor/ceil/round/sqrt on x64+arm64+wasm: BIT-FOR-BIT INERT on this corpus, independently confirmed (main's own binary reproduces the last logged row with delta 0, AND the branch's full scale-test stdout is BYTE-IDENTICAL to main's across all six rungs, every phase, every regalloc bucket). x64 roundsd 66 0F 3A 0B /r imm8 (floor 01, ceil 02, round 00 — the BOOTSTRAP's plain immediates, deliberately NOT v1's 0x08/0x09/0x0A, whose extra bit is the Precision Mask: it changes no value and shv2 reads no MXCSR flag) via a NEW encodeRoundsdRegReg, a three-byte opcode plus a trailing imm8 that encodeSseRegReg structurally cannot express; sqrtsd F2 0F 51 /r on the existing path; arm64 frintm/frintp/frintn/fsqrt; wasm f64.floor/ceil/nearest/sqrt. round is ties-to-EVEN on all three targets, both references agree, specs/round.md's round.halfway pins round(2.5)==2, and it was verified against the running oracle. NOTE: shv2's x64 baseline moves from SSE2 to SSE4.1 HERE AND NOWHERE ELSE. The four new callee.equals in parseCallNamed allocate NOTHING (Array.equals short-circuits on length, stdlib/Array.maxon:581); numericBuiltinOperand/emitMathIntrinsic build a String only inside their two throw branches. REJECTED: replacing the 8-way builtin chain with a hash/table lookup — Array.hash walks EVERY callee byte unconditionally where the chain mostly fails on length alone, and a miss returning a payload-free union case would RE-INTRODUCE the very allocation the implementer had just removed. That removal is itself the slice's best find: a first-draft union MathIntrinsic{notAnIntrinsic, op(..)} resolver moved memory +85/+153/+289/+561/+1105/+2193 (doubling per rung = ONE ALLOCATION PER PARSED CALL); the implementer established the true baseline by running main's own binary, A/B'd it, and discovered that A PAYLOAD-FREE UNION CASE STILL HEAP-ALLOCATES — replaced with direct dispatch, back to parity. INDEPENDENT REVIEW FOUND A FORKED DECISION: the StdUnaryOpcode->FloatRoundMode mapping was written TWICE, in StdToX64Conversion and StdToArm64Conversion, with nothing making the copies agree — and the arm64 site's comment ASSERTED the property the code lacked ("decided once, encoded twice"). A shared enum makes both copies spell the same member NAMES; it does not make the MAPPING shared. Change ceil to towardNegative in one isel and NEITHER SITE READS AS WRONG: x64 silently computes floor where the program said ceil — a WRONG ANSWER, not a build break — and asymmetrically so, because the x64 copy is pinned by committed goldens while arm64's can be regenerated on NO host available here, so the weakly-gated copy is the one free to drift. Un-forked into floatRoundModeFor in StdLoweringShared beside accessWidthFor/unaryResultClass; each isel's three arms collapse to one floor-to-round range and sqrt PANICS rather than getting an arm; fragments bit-for-bit identical, proving the lowering unchanged. Review also verified the roundsd encoder at a byte level THE SUITE STRUCTURALLY CANNOT REACH (committed tests only ever touch xmm0-xmm2, so REX on roundsd is untested): forcing 11 simultaneously-live floats produced 66 45 0F 3A 0B C0 01 (the 66 prefix BEFORE REX, with REX.R and REX.B both set) and F2 45 0F 51 CF = sqrtsd xmm9,xmm15, the program returning 91 against an independent computation. The band-append invariant was RE-DERIVED INDEPENDENTLY rather than trusted: TargetOp has 107 members, insertions at ordinals 52-53 and 100-101, 19 straddling range arms enumerated and each read in context and confirmed correct for an xmm->xmm plain def; the three shared Target files the implementer had requested needed ZERO edits and none was manufactured. TWO ORACLE DEFECTS FOUND, both maxon-sharp, both where shv2 is RIGHT: the bootstrap answers sqrt(true) with an internal E9001 plus a C# stack trace where a positioned diagnostic belongs (shv2 gives a clean E3005), and it CRASHES with E9001 "all FP registers occupied" under a 20-live-float pressure probe that shv2 compiles correctly (255, matching wasm). MEASURED DEBT FILED, QUANTIFIED AND ATTRIBUTED, NOT FIXED: the payload-free-union allocation generalizes to existing code and is WORSE than first reported — parseCallNamed constructs TWO such boxes per parsed call, ReceiverArg.none AND ConstructorInstance.none on the same line (Parser.maxon:15507; ReceiverArg.none also at :12786) — costing 174/310/582/1,126/2,214/4,390 allocations and 2,784..70,240 bytes across rungs 0-5, LINEAR (increments 68/136/272/544/1088 double exactly), 0.042% of rung-5 allocations. Static call counts (87/155/291/563/1107/2195) cross-validate to within 2 per rung against the implementer's independent dynamic A/B. ATTRIBUTION IS BOTH CODEGENS, which is what decides where it belongs: the measured allocations are the BOOTSTRAP's (MaxonToStandardConversion.cs:533-541 calls EmitAlloc(8+maxPayload*8) UNCONDITIONALLY; the zero-arg case only empties argsList), and shv2's OWN lowering does the identical thing (verified from emitted IR: two 16-byte __mm_alloc for the same union shape) — SO THE COST SURVIVES SELF-HOSTING and is not a bootstrap-only artifact. ALSO MEASURED, TREE-WIDE AND PRE-EXISTING: the bootstrap forms a jump table only for >=4 DENSE INDIVIDUALLY-NAMED arms (JumpTableFormationPass.cs:12,55,59 — Threshold=4, ordinals dense from 0), so ANY `to` range arm demotes a whole match to a linear compare chain (probe: 9 named arms -> a 9-case x64 jump_table; the same enum with two range arms -> a linear cmp chain). This slice demoted StdToWasm.unaryResultValType (5 dense arms -> range arms); UPHELD by the reviewer, and NOT because the cost is small (<=4 compares in a compile-time-only wasm-lowering helper) but because StdLoweringShared.unaryResultClass is that table's TWIN, answers the same question for the native targets, and ALREADY read neg-to-logicalNot on main — leaving one collapsed and the other not would be one fact written down twice in two shapes. The big TargetOp matches (targetOpOperands/colorTargetOp/opPhysicalRegs) already contained range arms and were NEVER jump tables: pre-existing, unchanged in kind. maxonUnaryOpToStdOpcode and both native lowerUnaryOp went 5->9 dense arms and KEEP their jump tables. THE GENERAL TENSION IS TREE-WIDE AND UNRESOLVED, recorded here as a finding and not acted on: CLAUDE.md's "consolidate redundant match arms" rule systematically demotes jump tables to compare chains in this compiler. TRAP REPAIRED BEFORE THIS RUN: the previous row's allocations and bytes notes had DIVERGED (one hand-edited to read "Same row as the allocations table"), and scale-test correctly REFUSED to measure anything until they matched — the log is the artifact, so the LOG was repaired rather than the instrument weakened. GATES on the merged tree: csharp+shv2 builds exit 0 with ZERO warnings; specs-shv2 1249/0 exit 0 no-101 (1175 baseline -> 1210 slice A -> 1234 after rebase over upstream P1.7 COW slice B -> 1249); --workers=1 and --workers=12 BYTE-IDENTICAL; specs-shv2/fragments additions only, zero M. | 364,015 | 692,961 | 1,350,331 | 2,664,256 | 5,291,840 | 10,547,599 |
| 2026-07-23 | #108 — __mm_free POISONS the freed payload, ALWAYS ON. Bump alloc never reuses a slot, so freed memory keeps its bytes and a use-after-free reads what it wrote before the free — INVISIBLE to every test. __mm_free now overwrites [ptr, ptr+size) with 0x3F the instant a box dies, turning the class loud (read-after-free returns 63 or faults on a non-canonical poisoned pointer). Unconditional, not a flag (user directive). MOVEMENT +352 allocs / +15,442 bytes FLAT at every rung = a per-MODULE constant, NOT the emitted program's poison cost (scale-test structurally cannot see that — shv2 is bootstrap-compiled). It is the COMPILER compiling the now-larger __mm_free (4 blocks vs 1, once per program via installMmRuntime): more ops through isel/regalloc/encode for that one runtime function. Flat across rungs = per-module, not program-proportional, not superlinear. codeBytes +64 = the poison loop's machine code, once per program. GOLDEN-NEUTRAL: __mm_free is __-prefixed (excluded from fragments), so ZERO committed fragment moved. PROVEN TEETH (a gate is not evidence until seen RED): moving __str_append's release ahead of its blit (the #43 bug) made `var s=abc; s.append(s); s.append(s)` print abcabc?????? under poison, restored -> abcabcabcabc; reviewer re-proved RED with a different __str_decref sabotage (0xC0000005 fault). Suite 1308/0 under poison — no committed test masks a UAF the suite reaches. REVIEW single-sourced the byte-fill loop (a near-copy of ArrayRuntime.emitZeroRange) -> MmRuntime.emitFillRange(dest, byteCount, fillValue), homed in the base layer ArrayRuntime already depends on; 4 array zeroing sites now share it, emitZeroRange deleted (net -14 lines). Pairs with the zeroing contract: must land BEFORE a recycling free list (a reused slot must then be re-zeroed on alloc, poison-then-zero, or [length,capacity)-reads-zero breaks). General cure for the bump-masked-UAF class; #40 and the P1.7 borrow-on-get residual are now poison-detectable the day they become suite-reachable. | 364,367 | 693,313 | 1,350,683 | 2,664,608 | 5,292,192 | 10,547,951 |
| 2026-07-23 | match-dispatch optimization pass (independent optimizer over commit 8743e80fb — I did not write it). Two O(n²)-in-arm-count defects in the match LOWERING path, removed. ⚠ SCALE-TEST IS BLIND TO BOTH: the corpus has no `match`, so every rung reads delta 0 — the numbers here MOVED NOTHING and cannot; both fixes were measured with a hand-built adversarial match (256..8192 distinct integer arms), the compiler's own --metrics parse-phase nanos, min over 6-8 runs on an idle box. (1) normalizeDispatchIntervals (Parser.maxon) was a subtract-and-insert that, per raw interval, scanned the whole claimed set AND inserted into the middle of a sorted array — Θ(n²) in EVERY input order (ascending pays two from-zero scans + cheap append; descending/random pay the O(n) mid-array shift). Isolated via range-form arms (which skip the dup check): ORIGINAL parse 21.06/62.17/195.90/700.93 ms at n=1024/2048/4096/8192, ratios 2.95→3.15→3.58 per doubling (quadratic); NEW 12.10/24.45/48.69/99.89 ms, ratios ~2.0 (linear) — ~600 ms of O(n²) gone at 8192, and now ORDER-INDEPENDENT (asc/desc/rand all ~equal). Replaced with an offline sweep: hand-written stable merge sort of the interval indices by lo, then a left-to-right sweep with a min-heap KEYED BY ARM PRIORITY so the earliest-arm owner is the heap minimum; a segment closes only when the owner changes (an earlier arm activating at its lo, or the owner ending at its hi). Reproduces the old first-wins partition SEGMENT-FOR-SEGMENT — specs-shv2 stays 1266/0 with fragments byte-identical (M on Parser.maxon ONLY), proving identical codegen. (2) recordDuplicateInt (pre-existing, the O(n) lookup inside the O(n) arm walk — the classic) linear-scanned a ParsedIntArray of seen values per point arm → Θ(n²). Isolated as point-form minus range-form parse: 1.46/7.59/31.4/126.7 ms at n=1024..8192, ratios ~4x (quadratic). Replaced the list with a hash Set (SeenPatternValues = Set with ParsedInt); membership is O(1), throw-on-first-duplicate-in-arm-order preserved (E2027 still fires, verified). Combined point-form parse 8192: was climbing to 226.57 ms (normalize-fixed only) → 96.65 ms both-fixed, ratios flat 2.00/2.01/2.04 = linear. Gates: shv2 build exit 0 zero warnings; specs-shv2 1266/0 exit 0 no-101; fragments/ clean (byte-identical codegen); scale-test delta 0 every rung (blind corpus, above). NO fast/slow fork, NO instrument edit. | 364,367 | 693,313 | 1,350,683 | 2,664,608 | 5,292,192 | 10,547,951 |
| 2026-07-23 | #65 implicit-self method resolution (bare `foo()` inside a method resolves to a sibling method). allocsDelta 0 on EVERY rung — the lazy per-type `siblingReceivers` map never builds on the scale corpus (its generated programs make no intra-type calls), and a bare call routes through the EXISTING `parseCallNamed`/`ReceiverArg` path (byte-identical IR, zero fragment M). The only movement is a small positive bytesDelta = 3 new per-`Parser` fields (`siblingReceivers`, `siblingReceiverScanType`, `enclosingTypeBodyStart`, ~24 bytes) times the parser count per rung — a per-FILE constant factor, NOT a new allocation and NOT superlinear (it doubles with file count exactly as the corpus doubles). The memoized-lazy design is the minimal footprint: eager would add a Map per type; no-memo risks quadratic re-scans. Review found + fixed two duplication defects (single-sourced self-capture refusal via requireNotInSelfCapture, and the static-method token test via functionDeclIsStatic) — neither touches allocation. Suite 1347/0, --workers=1==12 byte-identical, no 101. | 364,367 | 693,313 | 1,350,683 | 2,664,608 | 5,292,192 | 10,547,951 |
| 2026-07-23 | #40 conditional-ownership rung CLOSED (retain-promotion, approach A per user ruling) — the reachable UAF where a borrowed managed-aggregate PARAM re-bound to a var and conditionally reassigned to owned in a nested block dropped the fresh box at the inner block while the outer var was read past it. Fix: at `var q = <borrowed struct/union/generic-instance>` emit a NEW `__mm_retain(p)` (the incref twin of __mm_incref but RETURNS the ptr, so q gets a FRESH owned ValueId co-owning p's box) so q is owned-from-declaration, the buggy borrowedToOwned push-at-wrong-depth branch never fires, and the refcount balances on EVERY path (then decrefs the retained box on reassign + drops the new box at exit; else drops the retained box at exit, caller keeps ownership). Mirrors promoteMutableString with an incref instead of a copy (struct = reference semantics, so a copy would break aliasing — verified q.x=100 shows through to p on both compilers). A BOUNDED refcount exception like COW #105 — expands the no-runtime-refcount thesis to borrowed-aggregate-param vars (user sanctioned). allocsDelta 0 on EVERY rung + bytesDelta +8 constant (one usesRetain bool on the per-compile RuntimeUsage record): the scale corpus emits NO borrowed-aggregate-param vars so __mm_retain never fires and IR is byte-identical there; the retain/decref pair is O(1) per such var (one at declaration, one at scope exit) — linear, no superlinearity. Review found + fixed a rung-INTRODUCED blocker the green suite hid: making var q=p OWNED opened the reassignment String-only promoteBorrowed branch to aggregates, so q=p2 (reassign to ANOTHER borrowed param) blitted struct fields as a String record → garbage 5111944 / 0xC0000005 fault where the oracle gives 8; fixed by consolidating the promotion dispatch into promoteBorrowedToOwned used by BOTH declaration and reassignment. Full refcount-balance matrix verified under poison-on-free #108 (then/else/never-reassigned/break/continue/return-adopt/loop-carried/nested/match-arm/managed-String-field): no 101, no 0x3F3F3F3F, no fault. Fresh-ValueId proven: let r=p reborrow does NOT poison q; let r=q moves q not p. Suite 1360→1362→1366/0 (rebased over upstream), --workers=1==12 byte-identical, wasm 1143/144 (pre-existing), fragments additions-only zero M on existing/String goldens. Closes P1.2 (String+ownership) — the reachable residual is gone; String constructs were always correct. ⚠ STALE-BASELINE CORRECTION (coordinator, post-merge A/B): the PRINTED row delta above (+196/+292/+484/+868/+1636/+3172 allocs; +8384..+135360 bytes) is NOT #40 — it spans the UPSTREAM perf commit ffc521235 (jump-table distinct-successor dedup, O(span x distinct)->O(span)) that landed between the #65 log row and this one. PROVEN: #40's PARENT 134600b69 (upstream perf, NO #40) has BIT-IDENTICAL absolute allocs (rung0 364563 == merged 364563; rung1 693605==693605; rung2 1351167==1351167). So #40's ISOLATED footprint is +0 allocs / +8 bytes (the single usesRetain bool on the per-compile RuntimeUsage record), exactly as claimed; the entire printed delta is the perf commit's, an OPEN-#22 stale-baseline artifact. | 364,563 | 693,605 | 1,351,167 | 2,665,476 | 5,293,828 | 10,551,123 |
| 2026-07-23 | P1.7 wasm array support (commit 43d5c767e) — throwing-accessor signatures + void indirect calls. Two wasm-only correctness fixes, both invisible on native: (1) the 7 throwing array accessors (get/set/first/last/pop/remove/slice) now stamp `throws ArrayError`, so the wasm signature's error-flag result matches the dual-register call ABI, plus an install-time DRIFT GUARD (verifyThrowingAccessorsMatchPredicate) tying that stamp to isThrowingArrayRuntimeCallee in BOTH directions; (2) StdOp.callIndirect.resultType widened StdType to StdReturnType (void or value), so the per-element destructor call interns a wasm functype with ZERO results instead of a spurious i64 result that trapped call_indirect (exit 134). ALLOC BIT-NEUTRAL, A/B PROVEN: I rebuilt the base 238c304ed shv2 and re-ran scale-test — absolute allocs 374,958 / 711,349 / 1,385,503 / 2,732,489 / 5,423,241 / 10,807,360 and every byte are BIT-IDENTICAL to this rung at every rung and every phase and regalloc bucket. This rung's own footprint is 0 allocs / 0 bytes. WHY neutral: the drift guard is O(module.functions) and allocation-free on the happy path (a value-union match + a 6-byte prefix compare + 7 Array.equals; the interpolated panic String builds ONLY on drift); the throws stamp only sets an existing IrFunction field (the `ArrayError` String is a single module-scope `let`); StdReturnType is a VALUE union whose value arm wraps the StdType enum with no managed heap field, so callIndirect keeps its ONE managed field (`args`) and adds no per-op allocation; and on the native scale-test path the void-vs-value distinction is only whether an unused result register is captured — no allocation change, and fragments byte-identical (native codegen unchanged, verified). The PRINTED row delta (+10,395 / +17,744 / +34,336 / +67,013 / +129,413 / +256,237 allocs; +1,730,786 .. +26,692,036 bytes, roughly doubling per rung = LINEAR, a pre-existing per-program constant factor) is ENTIRELY a stale-baseline artifact predating the base: the base 238c304ed run — which does NOT contain this rung — prints the IDENTICAL delta against the last logged #40 row, so the movement belongs to commits between the #40 log row and the base, not to P1.7. This row RE-BASELINES the log to the current absolute numbers so the next rung measures from a correct baseline (OPEN-#22). Rung-over-rung alloc ratios 1.90 -> 1.95 -> 1.97 -> 1.98 -> 1.99 approach 2.0 = clean linear, no superlinearity. Suites: specs-shv2 native host 1229/0, wasm32-wasi 1291/0, both exit 0 no-101; fragments clean. | 374,958 | 711,349 | 1,385,503 | 2,732,489 | 5,423,241 | 10,807,360 |
| 2026-07-23 | P1.7 3b-ii/3b-iii (funcAbs64InRdata data->text reloc + opaque-T managed-element ownership), rebased onto main. OPTIMIZATION PASS (no compiler change): all 5 superlinearity candidates confirmed LINEAR by reading -- bakeFuncAbs64Relocs builds its debug-symbol index ONCE then O(1) Map.get per reloc (O(symbols+relocs)); rootManagedOpaqueArrayElementDrops is O(instances x fields)=O(program); scanConsumeBits/markOpaqueArrayMutatorFeeds is O(body tokens) once per method (bounded per-mutator-call arg re-walk, constant factor); managedOpaqueArrayElementOf is O(fields). ISOLATED HEAD-vs-main (this rung alone) = a NET REDUCTION on the generic-free corpus: -309/-645/-1317/-2661/-5349/-10725 allocs across rungs, ~x2/rung (LINEAR), essentially all in phase:encode. WHY: the rung removed the ONLY payload-carrying case, funcAbs64InRdata(String,String,ByteOffset), from union RdataRelocKind (it moved onto the new PendingRdataReloc struct on GlobalDataTable). RdataRelocKind therefore collapsed from a BOXED union to an integer-backed enum, so every RdataRelocation.kind -- one per code->rdata relocation (float islands, String-literal records, .data global leas) -- stores an inline tag instead of heap-boxing the union. One fewer alloc per relocation; relocations are linear in program size. COST: exactly +1 alloc/+48 bytes per COMPILE (flat every rung, phase:load) = the new empty PendingRdataRelocArray in GlobalDataTable.create(), a per-compilation constant, not superlinear; all other new machinery is generic/managed-opaque gated and never runs on this corpus (the isolated measurement shows the only non-encode movement is that flat +1). The recorded delta-vs-#65 (-113..-7553 allocs) is this rung's -309..-10725 SUMMED with the native-jump-table rungs (3df3d78d5+ffc521235, on main since #65 but never given a scale row) which ADD an rdata blob + per-slot relocs per dense match (+196..+3172 allocs, measured main-vs-#65). Suite 1352/0, no leak. | 364,254 | 692,960 | 1,349,850 | 2,662,815 | 5,288,479 | 10,540,398 |
| 2026-07-23 | P1.7 3b-iv (transitive array-feed fixpoint for opaque-T managed-element forwards). Fixes a reachable double-free: a method that FORWARDS its type-parameter element argument to a consuming sibling (add -> store -> self.items.push) now marks the forwarder's parameter a FEED via a least-fixpoint over the self-call forward graph (recordTransitiveArrayFeeds, mirroring solveDescriptorNeedFixpoint but per-parameter with forward edges + a linear reverse-reachability BFS), so the outermost concrete call site promotes-and-consumes the managed argument exactly as the directly-pushing method's site does. ZERO delta every rung, byte-for-byte: the whole pass is gated on enclosingTypeParams (a generic type), and the scale corpus is generic-free, so it costs one isEmpty() check per type declaration and allocates nothing. Suite 1355/0 (1352 + forward-to-consuming-sibling/multi-hop-forward/trivial-element-forward), no leak, -j1==-j12. | 364,254 | 692,960 | 1,349,850 | 2,662,815 | 5,288,479 | 10,540,398 |
| 2026-07-23 | P1.7 Finding A (bare opaque-T MANAGED field reassignment now WORKS, was E2015). Two parts: (1) decoupled __drop_type_param's install from usesArray onto its own usesDropTypeParam bit (installDropTypeParamRuntime) so an array-free generic struct's field-reassign drop links; (2) front-end: emitFieldWrite drops the old opaque value via the descriptor-gated __drop_type_param (shared body) or the retyped concrete decref (concrete instance) and transfers the new, gated on a managed instantiation; markOpaqueFieldReassignFeed makes the reassigned param an owned feed; a reassignsOpaqueField descriptor-need signal; SignatureIndex's managedOpaqueArrayElementOf broadened to stamp destroyFunc@40 for a bare managed opaque field too. Constant +16 bytes/rung, allocsDelta 0, ratios unchanged (linear x2.0x): the whole feature is generic-gated and the scale corpus is generic-free, so the only movement is the one extra bool field on the single per-compile RuntimeUsage struct. Suite 1393/0 (1391 + reassign-in-loop + struct-element, 2 flipped from E2015-reject), no leak, -j1==-j12, wasm 1303/0. | 364,254 | 692,960 | 1,349,850 | 2,662,815 | 5,288,479 | 10,540,398 |
| 2026-07-23 | P1.7 slice 3b-vi-b: managed-element array slice/clone/append via eager per-element deep-clone (__str_clone + __clone_<T> cascade + _managed array copies). The emitted runtime is managed-array-gated (usesManagedArrayCopy/neededCloners), so the generic/managed-free scale corpus installs NO cloner and adds NO runtime. But this FIRST cut still allocated one clonerSymbol ByteArray per struct+enum in the membership checks of noteClonerUsage + installStruct/UnionCloners — paid on EVERY compile including the managed-free corpus — a LINEAR allocsDelta +126/+206/+366/+686/+1326/+2606 (~1.9x/rung) concentrated in the install step. Fixed in the very next row by an empty-set early-out; the net rung cost is the constant one-Set field on RuntimeUsage. | 364,380 | 693,166 | 1,350,216 | 2,663,501 | 5,289,805 | 10,543,004 |
| 2026-07-23 | P1.7 slice 3b-vi-b optimize: early-out noteClonerUsage/installStructCloners/installUnionCloners when neededCloners is empty. The prior row's linear allocsDelta (+126..+2606, ~1.9x/rung) was one clonerSymbol ByteArray allocated per struct+enum in the membership checks — paid on EVERY compile including the managed-free scale corpus. With the empty-set early-out that cost is now managed-copy-gated only; the corpus references no cloner, so the delta should return to ~0. | 364,258 | 692,964 | 1,349,854 | 2,662,819 | 5,288,483 | 10,540,402 |
| 2026-07-23 | P1.7 slice 3b-vi-b-β: OPAQUE-T managed slice/clone/append via descriptor copyFunc@32 + funcAbs64InRdata reloc + __arr_{slice,clone,append}_opaque wrappers. Expected allocation-neutral: the scale corpus is non-generic and managed-free, so usesOpaqueArrayCopy stays false (no wrappers/stamps/cloner-roots) and the new descriptor-need copy signal fires only inside generic bodies. MEASURED: allocsΔ0 on every rung; bytesΔ a constant +8 (flat across all six rungs, not scaling) — the once-per-compile RuntimeUsage box gained one 8-byte slot for the new usesOpaqueArrayCopy bool field. | 364,258 | 692,964 | 1,349,854 | 2,662,819 | 5,288,483 | 10,540,402 |
| 2026-07-23 | P1.5-B2 #88 optimization pass (un-awaited Promise drop-reclaim + cancel). allocsDelta 0 / bytesDelta 0 EVERY rung, all phase ratios linear (parse x2.0, isel x2.00, regalloc x2.06) — byte-identical scalar-corpus memory traffic vs the last logged run (P1.7 Finding A; note the gap also spans the unlogged P1.5-B1c — both are async-runtime/gated, so the zero delta covers both). No compiler-side change made: the rung was already optimal. WHY zero: (1) the 4 front-end hot-path additions (parseBinding/reassignBinding/decrefCalleeFor/requireBindingLive) are single O(1) valuePromiseMarks BoolArray.get column reads — seeded in mintValue/recordValueType lockstep like every other per-value column, panic-interp lazy so zero alloc on the success path; the async-free corpus exercises them on every binding/reassign/read and they cost nothing measurable (constant factor, no bent curve). (2) __gt_promise_drop + emitGtRunQueueRemove/emitGtTimerStoreRemove/emitGtProcStoreRemove + the shared emitGtStructReclaim (a de-dup of the await-tail reclaim) + the __gt_live_count leak gate are runtime functions emitted ONCE per program (O(1) compiler cost) and never run in the async-free corpus. The emitted runtime scans are O(pending/parked GTs) but linear-in-practice: simultaneously-live un-awaited promises are bounded by the fixed count of source-level promise bindings (a loop reuses ONE binding, so it cannot accumulate — spawn-drop-loop-bounded emits a tight per-iter __gt_spawn;__gt_promise_drop where the dropped GT is the sole ready queue head → O(1) head-removal). A runtime O(N^2) would require O(N) literal un-awaited-and-parked spawn sites = O(N) source; not triggerable from a bounded loop (measured-debt shape, like SplitLiveRanges K^2). Gates: suite 1403/0, exit 0, no leak / no GT-abort(75), -j1==-j12 byte-identical (async 63/63 both), fragments clean. | 364,254 | 692,960 | 1,349,850 | 2,662,815 | 5,288,479 | 10,540,398 |
| 2026-07-23 | P1.7a slice 1 (interfaces front-end): interface decls + declared-conformance checking (E3015/E3016) + `"{userStruct}"` direct-toString interp. Allocation-neutral for the conformance-free scale corpus: checkConformance short-circuits on zero conformances, and the artifact's new `interfaces`/`conformances` arrays point at SHARED EMPTY singletons (copy-on-write on first record) so an interface-free file allocates nothing extra. A first cut eagerly allocated those two empty arrays per FileParseArtifact — a linear allocsDelta +216/+360/+648/+1224/+2376/+4680 (~2x/rung, scaling with file count) — fixed here by the shared-empty pattern (same discipline as sharedEmptyInnerAliases). Residual per-compile cost is a small constant: createProject + resetMergeTargets each allocate the project-level interfaces map + conformances array once. The bytesΔ still scales (+2,144/+3,296/+5,600/+10,208/+19,424/+37,856, ~2x/rung asymptotic) but allocsΔ is FLAT +12 — that combination is the per-file FileParseArtifact RECORD gaining two pointer fields (interfaces + conformances, 16 bytes): the record is ONE allocation, just 16 bytes larger per file, so the byte total scales with file count while the alloc count does not. It is the minimal representation of the per-file contributions (the pure-parser invariant forbids writing them anywhere but the artifact), the per-file analogue of the prior row's per-compile "+8 bytes for a bool field". | 364,270 | 692,976 | 1,349,866 | 2,662,831 | 5,288,495 | 10,540,414 |
| 2026-07-23 | P1.5 #93 (runProcess is a THROWING builtin, Design A scalar SubprocessError) optimization pass — NO compiler change: the rung is footprint-neutral and introduces no superlinearity or duplication. ATTRIBUTION CORRECTED. The instrument shows allocsDelta +4 / bytesDelta +192 FLAT on every rung vs the prior row (P1.5-B2 #88 opt pass, 22,121,353). The implementer attributed that to the unlogged eb3c834b0 (#88 review). That is WRONG: an A/B/C over three source states, ALL rebuilt with one identical current bootstrap, shows they are byte-for-byte identical (rung0 22,121,545 … rung5 788,061,772, every phase, every regalloc sub-bucket): (A) HEAD 8f16a08db (#93), (B) base c90c973a2 (post-eb3c834b0, pre-#93), (C) 55b3a24f9 (the very commit that WROTE the 22,121,353 row, BEFORE eb3c834b0). So #93 isolated = 0 AND eb3c834b0 isolated = 0. Since 55b3a24f9 rebuilds to 22,121,545 (+192 vs its own committed 22,121,353), the +4/+192 is a BUILD-PROVENANCE offset, not an shv2-source regression: stdlib + maxon-sharp are byte-identical across 55b3a24f9..HEAD, so the branch was rebased after row 300 was recorded and row 300's shv2 binary was emitted by a different (pre-rebase) bootstrap that lowered shv2's per-compile setup with 4 fewer boxed temporaries. Flat-per-compile (not scaling), so it is a constant, never a bent curve. This row RE-BASELINES the P1.5 line at 22,121,545; future rows compare here and the provenance offset is absorbed. WHY #93 is zero on this async-free corpus: (1) the throwing __gt_process_run / __gt_proc_add rewrites and the new SubprocessError constants live in usesGt-gated GT builders that never install without async; (2) isThrowingRuntimeCallee is a clean O(1) NON-allocating OR-compose of isThrowingArrayRuntimeCallee + isThrowingGtRuntimeCallee (ByteArray.equals = length-check then byte loop bounded by the ~16-byte callee name; bytesStartWith is a prefix compare) — NOT a copy; validatePlainCall's E3057 gate now runs isThrowingRuntimeCallee on every plain call, one extra byte-equals per non-throwing callee = a constant factor, linear in program size, allocation-free, no bent curve; the per-family message recompute of isThrowingGtRuntimeCallee is error-path only (once per diagnostic). No new per-value column, no nested scan, no repeated rebuild. Gates: build zero-warnings, suite 1429/0 exit 0 no leak / no GT-abort(75), fragments clean (only the committed async-subprocess try-rewrite fragments moved, which are HEAD). | 364,258 | 692,964 | 1,349,854 | 2,662,819 | 5,288,483 | 10,540,402 |
| 2026-07-23 | P1.5 #64 borrowed-container boxed-union field throw made a RETAIN (retainThrownField: one __mm_incref in emitThrowError's borrowedField branch, replacing the E2015 reject). #64 ISOLATED FOOTPRINT = 0, A/B-PROVEN: parent 1087e8501 with Parser.maxon stashed, rebuilt on the same bootstrap, is BYTE-IDENTICAL to this build every rung (rung0 364,270 allocs / 22,123,689 bytes) — the retain fires only on a borrowed-container boxed-union field throw, which the arithmetic corpus never generates, so the parser walks an identical path with and without the change. The growing +2,144..+37,856 bytes vs the prior (#93) row is NOT #64: it is drift from the #93 review commit 7f3721b00, which landed AFTER the #93 perf row 64eba9bdb was recorded and was never re-measured, plus build provenance. This row re-baselines the P1.5 line to absorb that drift; future rows compare here. | 364,270 | 692,976 | 1,349,866 | 2,662,831 | 5,288,495 | 10,540,414 |
| 2026-07-24 | P1.7a slice 2a (witness-table dispatch for constrained type params) OPTIMIZATION PASS — NO compiler change; no superlinearity found, all witness paths LINEAR. CORPUS-BLIND ROW: scale-test's corpus is witness/generic-free so NONE of the new paths fire here — the +6/+bytesΔ below is only the passive record-widening + once-per-compile setup, and the witness complexity was established on a CUSTOM witness corpus (below), not this instrument. DEFAULT-CORPUS FOOTPRINT vs base 65d64dd97 (row 306): allocsΔ FLAT +6 on EVERY rung (per-compile constant, not scaling) = the once-per-compile setup allocations (MaxonToStdLowering.emittedWitnessTables Set + interfaces map ref; ProgramSignatures.genericAliasSites Array; buildDeclaredConformanceMap/checkWhereConstraints short-circuit allocation-free on zero instantiation sites). bytesΔ SCALES x2.0/rung (+64,848..+1,977,920, ~= sourceBytes/rung) = records that scale in COUNT got WIDER with no new per-element alloc: StructLayout gained the whereConstraints pointer (points at sharedEmptyWhereConstraints for every unconstrained struct — no alloc), GenericAliasEntry gained nameLine/nameColumn/filePath. FLAT allocs + scaling bytes = the slice-1 (row 304) pattern: existing records grew, none added. LINEAR, not a bend. WITNESS CORPUS (off-instrument, --metrics per-phase memory, bit-reproducible): N distinct (Type,Digest) conformances = N instantiation sites = N witness tables = N dispatch sites, doubled N=100/200/400/800, work spread over N bounded functions so the KNOWN per-function live-range-splitter quadratic stays out. EVERY phase x1.97-2.02 (LINEAR): lowerMaxonToStd (ensureWitnessTable blob build + finalizeCallArgs dictionary-passing) 14396->28527->56759->113198 x1.98/1.99/1.99; semanticCheck (checkWhereConstraints E3017) 2512->5012->10012->20012 x2.00; parse (witnessDispatch + materializeEscapingBorrowTemp temp-arg push) x1.98-2.00; AND regalloc:splitting/liveness x1.97-2.01 even though EVERY function carries a witnessCall — so the new op adds no superlinear scan. WHY linear: emittedWitnessTables dedup + structTypes/interfaces/declared lookups are open-addressing hash O(1); ensureWitnessTable is one flat O(methods) loop per unique (Type,Iface). materializeEscapingBorrowTemp's per-position pendingTempsContain/removeFromPendingTemps scan is O(K^2) per constructor call but K is STRUCTURALLY CAPPED: the backend refuses >6 params and multi-constrained multi-type-param generics are unsupported (E2015 'top-level ,'), so a single-constraint generic has ONE feed position = O(1)/call — a bounded constant, NOTE not defect (same shape as the pre-existing isRepeatedOwningMove O(K^2) in the same loop). A GIANT-single-main witness corpus does show regalloc:splitting x3.8-3.9 (~quad) — that is the KNOWN/budgeted splitter (recompute-liveness-per-split, ARCHITECTURE.md:1336), not the witness op (per-function it is x2.0). Gates: suite 1458/0 exit 0 no leak. | 364,276 | 692,982 | 1,349,872 | 2,662,837 | 5,288,501 | 10,540,420 |
| 2026-07-24 | P1.5 dogfood slice 1a (IOCP overlapped-read substrate: spawnReadLine probe + io runtime). ATTRIBUTION CORRECTED — the prior note predicted corpus-neutral, which is WRONG. Slice-1a ISOLATED footprint vs parent 65d64dd97 (the committed P1.5 #64 baseline row): allocsDelta +77 FLAT every rung; bytesDelta GROWS ~linearly (rung0 +68,288, rung5 +1,959,536, ~2x per rung = strictly LINEAR, the curve does NOT bend). CAUSE of the growing bytes: the flat StdOp union got ONE FIELD WIDER. The bootstrap heap-allocates every associated-value union instance at 8 + maxPayloadSlots*8 bytes (MaxonToStandardConversion GetMaxFlatPayloadSlots counts FIELDS per case — each associated value is one 8-byte slot, scalar or pointer); the new osReadOverlapped(resultId,handle,buf,cap,bytesOutPtr,ovl) is the SOLE 6-field case (5 genuine ReadFile args + the BOOL result), one past the prior max of 5 (binOp/cmp/callIndirect), so maxPayload 5->6 widened EVERY StdOp heap instance 48->56 bytes = +8 B per op, paid by ALL programs whether or not they do io. PROVEN: every rung0 phase delta is an exact multiple of 8 — lowerMaxonToStd +52,216 = 8x6,527, foldConstOperands +10,704 = 8x1,338, total +68,288 = 8x8,536. The FLAT +77 allocs (and the ~+4,728 writeExe bytes) are a SEPARATE fixed per-compile cost: kernel32ImportNames iterates IatSlot.allCases, so the 15 new IOCP kernel32 imports are written into EVERY PE unconditionally — a constant, not a scaling term. DECISION: ACCEPTED as inherent LINEAR debt (the flat-StdOp thesis tax: pay max-case width for O(1) dispatch), NOT narrowed. ReadFile is genuinely a 5-argument call, and trimming osReadOverlapped to 5 operands needs a contortion — pack the NON-ADJACENT bytesOut (a per-read slab alloc) and ovl (embedded in the GT struct at 0x60) behind a hard-coded backend layout offset, or hard-code the buffer cap, or fork the op — none clean or correctness-neutral, and the anti-dup reviewer would collapse a fork. +8 B/StdOp is a constant factor, linear in program size, no bent curve (mandate: constant factors are not the target; growth curves are). RE-MEASURE TRIGGER: the first future StdOp case with >=7 associated values pushes maxPayload 6->7 and re-widens every StdOp instance by a further 8 B; until then every <=6-field op rides free. IO PATH superlinearity hunt: NONE — the io runtime graph is usesIocp-gated (never emitted for the arithmetic corpus) and is a fixed-size emission; the runtime proc/handle store scans are cap-64 bounded. Gates: no compiler change (accept-only), so scale is bit-identical to the measured HEAD every rung; suite 1455/0 exit 0 no leak, fragments clean. | 364,347 | 693,053 | 1,349,943 | 2,662,908 | 5,288,572 | 10,540,491 |
| 2026-07-24 | Retain-escaping-borrow rung (generalize #40/#64 co-ownership to escaping generic-field trivial-struct borrows), RE-MEASURED on the merged tree after rebasing onto P1.5 IOCP slice 1a. MY rung's own footprint is 0 — the retain (`__mm_incref` at a generic constructor feed) and the container's trivial-struct type-param field decref (`genericInstanceFieldIsManaged` now `typeIsManaged` → a `__destruct_<Instance>` cascade) fire ONLY on generic instantiations, and the scale corpus is entirely non-generic, so none of the new paths execute here (corpus-blind, same as slices 1/2a). The +82 allocs / +4,952 bytes FLAT every rung vs the pre-rebase measurement is ENTIRELY IOCP slice 1a's constant, NOT this rung: the whole delta lands in phase:writeExe (+82 allocs / +4,944 bytes flat) = the 15 unconditional kernel32 IOCP imports written into every PE, already attributed in the IOCP row above; the residual ~8 B is StdOp-width provenance. This row re-baselines the retain line onto the current bootstrap (same discipline as the P1.5 #93/#64 provenance re-baselines); future rows compare here. CODEGEN change verified on the witness/generic suites (9 goldens: `__mm_incref` before the constructor feed + the container box drop `__mm_decref`→`__destruct_<Instance>`; the temp no longer needs its scope-local materialization register). The reassign of a co-owned trivial field is REJECTED (E2015) — no leak path shipped. Gates: native 1469/0, wasm 1344/0, -j1==-j12 byte-identical, exit 0, no leak. | 364,358 | 693,064 | 1,349,954 | 2,662,919 | 5,288,583 | 10,540,502 |
| 2026-07-24 | P1.7a Slice 2b-i (integer Hashable+Equatable primitive conformance + HashValue, x64). Own footprint 0: the synthesized int.hash/int.equals impls and their witness tables fire ONLY on a `where T is Hashable/Equatable` instantiation bound to a primitive int; the scale corpus is entirely non-generic/conformance-free, so none of the new paths execute (corpus-blind, same as slices 1/2a/retain-escaping). installPrimitiveConformanceRuntime short-circuits allocation-free when globalData.pendingRdataRelocs is empty (the whole scale corpus), so the two qualifiedMethodName impl-name allocations never mint here — allocsΔ0/bytesΔ0 vs the retain-escaping baseline. Piece 4's emitCompare interception adds only two O(1) isEnclosingTypeParam tag-checks per ==/!=, allocation-free. Gates: native 1485/0, wasm 1354/0, -j1==-j12 byte-identical, exit 0, no leak. | 364,358 | 693,064 | 1,349,954 | 2,662,919 | 5,288,583 | 10,540,502 |
| 2026-07-24 | P1.5 dogfood slice 1b (StreamingSubprocess handle-table API: 2 StdOps + 7 builtins + runtime) INCLUDING the io_owns_pipe optimization (d3a31df8a). ATTRIBUTION CORRECTED. The vs-row-310 delta the instrument prints (allocsD +11 FLAT; bytesD +1,520/+2,224/+3,632/+6,448/+12,080/+23,344 = 816 flat + 704*2^n, a strictly-LINEAR term the low-rung x1.46 ratio only dilutes) is NOT slice 1b -- it is BUILD PROVENANCE. Proven by A/B on ONE (current) bootstrap: the slice-1a base source (02f465509, containing NONE of slice 1b) rebuilt here reproduces that delta IDENTICALLY vs row 310 (allocsD +11, bytesD +1,512..+23,336). Row 310's shv2 binary was emitted by an earlier pre-rebase bootstrap; the +11-alloc / 704*2^n-byte term is pre-slice-1b, the same trap as rows 307/308. TRUE ISOLATED slice-1b(+fix+opt) footprint, HEAD vs base rebuilt on the SAME bootstrap: allocsD 0 EVERY rung; bytesD +8 FLAT EVERY rung (22,193,489->22,193,497 ... 790,082,500->790,082,508) = the once-per-compile RuntimeUsage record gaining the usesStreaming bool slot, a per-compile CONSTANT, not superlinear. WHY no per-op growth: the 2 new StdOps are osPipeCreateOutbound(4 fields) + osWriteFile(5 fields), both <=6, so maxPayload stays 6 (slice 1a's osReadOverlapped) -- NO StdOp union widening; slice 1a's >=7-field re-measure trigger is NOT tripped. The 7 bare-name builtins + streaming/IOCP runtime are usesStreaming/usesIocp-gated and corpus-blind (never emitted for the arithmetic ScaleCorpus). OPTIMIZATION is corpus-blind too: the io_owns_pipe marker now rides io_handle@0x80's spare low bit (kernel HANDLEs are 4-aligned) instead of a dedicated GtOffIoOwnsPipe@0xB0 field, reverting GtStructSize 0xE8->0xE0 and the 17-async-golden +8 offset churn (goldens now byte-identical to slice 1a); cross-thread-safe because the completion thread never touches io_handle. That +8/GT was a RUNTIME cost (every async GT), invisible to this compile-time instrument -- removing it does not move these numbers, and HEAD-with-opt still shows only +8-flat vs base. RUNTIME superlinearity hunt (corpus cannot reach the streaming code -- read it): NONE. Handle table O(1) indexed (slot = table + handle*128); spawn free-slot scan is cap-SubpMaxHandles=64 = O(1). Line read re-scans ONLY new bytes (scan resume index re-enters at pre-read bufLen), buffer grows by DOUBLING (geometric O(total)), found-compaction bounded <= one SubpReadChunkBytes per line (remaining <= chunk always, accumulation stops at first newline) = O(1)/line; write buffer doubles. All linear/bounded. This row RE-BASELINES the P1.5 line at the current-bootstrap numbers (22,193,497...); future rows compare here and the provenance offset is absorbed. Gates: suite 1470/0 exit 0 no leak/101; --workers=1 vs 12 PE+IR byte-identical; 1a spawn-read-line 4/4; drop-reader-then-reread PASS; fragments clean. | 364,358 | 693,064 | 1,349,954 | 2,662,919 | 5,288,583 | 10,540,502 |
| 2026-07-24 | P1.7b Slice A (Set hash-table builtin — core, x64, int scalar keys). Own runtime footprint 0 on this corpus: the `__set_*` install is `usesSet`-gated and the scale corpus is entirely Set-free, so no set runtime is emitted and no set/witness path executes (corpus-blind, like slices 1/2a/2b-i/retain-escaping). allocsΔ0 all rungs. The bytesΔ+8 all rungs is ONE constant: `RuntimeUsage` gained a `usesSet` bool field, enlarging the single per-compile `RuntimeUsage` record (`scanRuntimeUsage` mints exactly one) by one 8-byte field slot — rung-independent, zero extra allocations, the exact same per-field cost `usesArray`/`usesStrClone`/etc. each carry. `fourI64Params` promoted from ArrayRuntime to MmRuntime (behavior-identical relocation, joins its 1/2/3-param siblings); ArrayRuntime's `__arr_slice_managed` signature unchanged. Gates: native 1498/0, wasm 1356/0, -j1==-j12 byte-identical, exit 0, no leak. RE-BASELINED onto the merged tree (rebased over P1.5 dogfood slice 1b + debugger P3c): merged-tree native 1507/0, wasm 1356/0, -j1==-j12 byte-identical, fragments byte-identical; the further +8 bytes/rung vs the pre-rebase numbers is P1.5-1b's `usesStreaming` RuntimeUsage field (a sibling per-compile constant — see the P1.5 row above), allocsΔ0 unchanged. | 364,358 | 693,064 | 1,349,954 | 2,662,919 | 5,288,583 | 10,540,502 |
| 2026-07-24 | Dead-function elimination rung (reachability prune at the target-neutral backend entry) + its optimization pass. ATTRIBUTION VERIFIED BY FULL A/B ON ONE BOOTSTRAP, and there is no provenance offset this time: the base (b6a6fd4c5, containing none of the rung) rebuilt on this bootstrap reproduces the last logged row EXACTLY, delta 0 on all six rungs, so every number below is the rung. PER-PHASE A/B: exactly TWO columns moved. Every other phase -- lex, signatures, parse, merge, resolveTypes, semanticCheck, lowerMaxonToStd, insertRangeChecks, pruneDeadBlockArgs, elimTrivialBlockArgs, foldConstOperands, sugarGate, isel, regalloc, prologueEpilogue, runtimeAugment, encode, link, writeExe, and all 7 regalloc sub-phases -- is 0 allocs / 0 bytes on EVERY rung. (1) The new phase:deadFunctions column is the whole scaling delta. (2) A FLAT instrumentation term of +2 allocs / +112 bytes per compile, identical at every rung, in three pieces, each pinned: +0/+48 billed to phase:load is the CompilePhase enum going 21->22 arms, widening six phase-count-sized 8-byte-element arrays (CompileTimings.phaseNanos, PhaseMemoryColumns allocs/frees/bytes, and two CompilePhase.allCases materializations) -- CAUSALLY PROVEN, not inferred, by building a throwaway 23rd enum arm with no pass and no probe behind it, which costs exactly +0 allocs/+48 bytes, all in phase:load, on all six rungs; +1/+32 billed INSIDE phase:deadFunctions is the phase's own PhaseProbe record (4 fields x 8B: nanos, allocTotal, allocLive, bytes), allocated after its own opening counter snapshot so it falls inside the window it opens; +1/+32 unattributed is the PhaseDelta that recordPhase builds after the closing counter read. So the pass BODY alone is +318/+136,968 ... +3,935/+6,640,884, isolated by stubbing the body and re-measuring. LINEAR, shown three ways. (a) The body's allocation FIRST DIFFERENCES double: 127, 253, 478, 930, 1829, ratios 1.992/1.889/1.946/1.967 -- the exact signature of c + k*2^n on a doubling ladder, i.e. linear plus a per-compile constant. (b) The body/sugarGate cost ratio DECREASES monotonically, 1.195->1.135->1.099->1.066->1.041->1.024 for allocs and 1.084->1.027 for bytes. sugarGate is the same single IrModule.liveOpIndices walk over every live op, so DFE's marginal cost per unit of program converges on a pass already known linear; a superlinear term would make that ratio GROW, not shrink. This is a stronger statement than the byte ratios alone (x2.03/2.48/2.14/2.14/2.11 vs sugarGate's x2.11/2.50/2.15/2.15/2.11), which are both just the documented Array grownCapacity ~x2.1 linear reading. (c) By construction: indexFunctionsByName is F hash inserts; seedRoots is one O(1) lookup per pendingRdataReloc; markLiveFunction marks BEFORE pushing so no function can be pushed twice and the worklist pops at most F times; markCalleesOf walks liveOpIndices(f) once per LIVE function, so O(total live ops); every callee name resolves through ONE stdlib Map.get (open addressing, cached hashes) instead of a scan; getFunc/funcCount are array indexing. Total O(F + ops + relocs + total callee-name bytes) with no nested scan, no findFirst in a loop, and no unbounded re-push. CORPUS CAVEAT, load-bearing: this is the worst case for cost and the best case for benefit. --log=ir:debug reports dfe: dropped 0 unreachable function(s) of 60 at rung 0 -- the arithmetic corpus contains NO dead code, so every allocation above is pure walk and NONE of it is offset by back-end work the prune removed. The instrument structurally cannot show the win. BENEFIT, measured separately on a heap program the corpus cannot express (Array create/push/get plus print, which pulls in the all-or-nothing installArrayRuntime closure): 15 of 31 functions dropped, and the whole compile falls 41,540->24,356 allocations (-41.4%) and 1,912,683->1,141,518 bytes (-40.3%); regalloc 24,646->11,040 allocs (-55%), isel 5,670->3,363 (-41%), encode 2,825->1,781 (-37%), prologueEpilogue 540->258 (-52%); emitted code 7,354->4,103 bytes (-44%). DFE's own cost on that program is 131 allocations, so it spends 131 to save about 17,300 -- roughly 130x. THE ONE OPTIMIZATION MADE: sweepUnreachedFunctions materialized String.from(func.name) EAGERLY beside its lazy logTrace, spending one String per function DROPPED -- a cost that rises with exactly how well the prune works -- to render a line that is off on every build but a --log=ir:trace one. Moved inside the closure: -15 allocs / -720 bytes on the heap program (-10.3% of the pass's own footprint, one String per drop at 48B), exactly 0 on this corpus because nothing here is dropped, trace output unchanged (15 lines under ir:trace), emitted code byte-identical. NOT DONE, with reasons. No compactOps/compactBlocks after the sweep: it would add an O(ops) walk plus an ops-sized forwarding array to the one column this instrument CAN see, to buy a reduction in peak resident memory, which nothing here measures at all -- and nothing downstream of DFE walks the Std module's flat arrays (verified: every Std consumer goes through liveOpIndices or func.blockRefs), so the orphans are inert, not a correctness risk. No CHUNK-level pruning (a sleep-only program still carries the __gt_trampoline chunk as dead bytes): the hand-assembled chunks are a FIXED set, O(1) in program size, so they cannot bend any curve, and pruning them needs a Target-tier chunk reachability graph -- a SECOND decider beside RuntimeUsage, which is the precise defect this rung removed. MEASURED DEBT; the trigger that would make it worth paying is chunk count becoming a function of program size, which it has never been. Gates: x64 suite 1502/0 exit 0 no leak/101; wasm32-wasi suite 1360/0; --workers=1 vs --workers=12 PE byte-identical on both a 23-file corpus rung and the heap program; specs-shv2/fragments clean. | 364,678 | 693,511 | 1,350,654 | 2,664,097 | 5,290,691 | 10,544,439 |
| 2026-07-24 | DFE rung — FINAL post-review state (supersedes the row above, which measured the rung before its independent review). The independent review found and fixed TWO reachable defects, and this row is the cost of the correctness fix. (1) BLOCKER: DFE renumbers module.functions, but ValueOriginTable/ParamOriginTable key every row by a function's POSITION in that array, so after the first pruned function every later function resolved its values against a DIFFERENT function's rows — loud half a RegisterPressureDiagnostic COMPILER-BUG panic, silent half worse: E5001 pointing at the WRONG SOURCE LINE. Fixed with ValueOriginTable.remapFunctionIndices + the ParamOriginTable half. (2) HIGH: the moduleHasFunction->RuntimeUsage collapse had been applied to x64 and arm64 but NOT wasm, whose appendLeakGate still asked the module and SILENTLY skipped the leak gate (measured by deleting the __mm_leak_check root: x64 and arm64 panic at link, wasm exits 0 with a 4,332-byte module vs 4,421). Now all three backends emit the gate from usage.usesHeap, so pruning that root is a link failure on EVERY target — a structural guard, which is why no leak-firing spec is needed. THE DELTA vs the previous row is entirely the remap: allocsDelta +6..+11 and bytesDelta +1,032..+24,712, which is the newIndexOfOld array (one F-sized index array per compile, F = function count) — LINEAR in program size, ~0.003% of total compile bytes, and it carries a dropped==0 early-out so the arithmetic corpus (which drops nothing) pays only the array and never a renumbering walk. Everything in the previous row's analysis still holds unchanged: the pass body is linear (allocation first differences double; the body/sugarGate ratio decreases monotonically), only phase:deadFunctions and a flat +2/+112 instrumentation term moved, and the corpus is the worst case for cost and best for benefit (dropped 0 of 60 here, while a heap program drops 15 of 31 for -41% allocations and -44% emitted code, spending 131 allocations to save ~17,300). Gates on this final state: x64 suite 1502/0 exit 0 no leak/101; wasm32-wasi 1360/0; --workers=1 == --workers=12; specs-shv2/fragments clean. | 364,689 | 693,523 | 1,350,667 | 2,664,111 | 5,290,706 | 10,544,455 |
| 2026-07-24 | P1.7b Slice B — `Set from [ints]` construction (`__set_from` runtime fn + parser construct + sweep pre-intern of `Set with integer`). OWN FOOTPRINT 0, A/B-VERIFIED ON ONE BOOTSTRAP: every new path is `usesSet`-gated (the `__set_from` install rides the existing `installSetRuntime` early-return) or `Set from`-triggered (the parser construct, element-inference, and sweep pre-intern), and the scale corpus is entirely Set-free, so none of them execute here (corpus-blind, like slices 1/2a/2b-i/Slice A). PROVEN by A/B: the base (my four compiler-source edits stashed, rebuilt on the SAME copied bootstrap) produces byte-identical rung totals — allocsΔ0/bytesΔ0 EVERY rung vs this row (rung0 22,331,617 both; rung1 45,011,574 both). The +8 bytes/rung the instrument prints vs the last logged row (the DFE-FINAL row above, 22,331,609…) is NOT this rung: it is BUILD PROVENANCE — the base equally shows it (the copied `bin/maxon.exe` was emitted by a bootstrap one step newer than the DFE row's), a per-compile constant absorbed by re-baselining this line onto the current bootstrap, the same discipline rows 317/320 apply. Future rows compare here. The extra per-token sweep check (`Set`-identifier + `from` lookahead) and the extra `isSetWitnessCallee` `.equals` clause are allocation-free comparisons. Gates: native 1517/0, wasm 1360/0, -j1==-j12 byte-identical, exit 0, no leak. | 364,689 | 693,523 | 1,350,667 | 2,664,111 | 5,290,706 | 10,544,455 |
| 2026-07-24 | `__Builtins.*` recognition (stdlib-whitelist chain step 2) — the three Clock intrinsics + the reserved-callee panic fix. DELTA IS A FLAT PER-COMPILE CONSTANT: +5 allocs / +232 bytes at EVERY rung of a 32x ladder, 0.001% of rung 0 and 0.00005% of rung 5. FULLY ATTRIBUTED BY MEASUREMENT, not inference: it is the ONE new kernel32 import name (`GetSystemTimeAsFileTime`, for the wall clock's `osReadWallClock`). Proof — a temporary SECOND import (`GetTickCount64`) was added, rebuilt and re-measured, and the delta became exactly +10 allocs / +448 bytes at every rung, i.e. +5 allocations per import exactly, the extra 216 bytes being the shorter name's buffers. Those five are the five `String.toByteArray()`/`.name` calls the PE import-table build makes PER IMPORT: `kernel32ImportNames`' `slot.name`, `sumHintSizes`, `importHintNameEntrySize` x2 (the IAT and ILT passes), and `appendImportHintNameEntry`. Nothing else in the rung costs anything measurable: the `__Builtins.` recognizer is one `bytesStartWith` prefix compare per parsed call and allocates NOTHING (proven by the flatness — rung 5 parses 32x the calls of rung 0 and shows the same +5); the parser's reserved-callee rejection is one more prefix compare on the same path; `RuntimeUsage.usesWallClock` is an inline bool on a single per-compile struct; and `installClockRuntime` early-returns for every program that does not read the calendar (the whole corpus), so the clock runtime function and its `.data` slot cost the arithmetic ladder zero. Gates: x64 1525/0 exit 0 no leak; wasm32-wasi 1366/0 exit 0; --workers=1 == --workers=12 byte-identical; fragments additions-only. | 364,689 | 693,523 | 1,350,667 | 2,664,111 | 5,290,706 | 10,544,455 |
| 2026-07-24 | P1.7b String Hashable+Equatable conformance: String.hash (djb2) + String.equals (shared __str_eq graph) synthesized post-pipeline, witness-reloc-gated; file/predicate renamed Primitive->Builtin. Corpus is conformance-free so installBuiltinConformanceRuntime short-circuits (pendingRdataRelocs==0) and no per-compile field added ⇒ own footprint allocsΔ0/bytesΔ0. RE-BASELINED onto the merged tree (rebased over the `__Builtins.*` recognition work): the +5 allocs / +224 bytes per rung vs the pre-rebase numbers is that upstream work's per-compile footprint (see the `__Builtins` row directly above), NOT this rung — whose own footprint stays 0. Merged-tree gates: native 1534/0, wasm 1366/0, -j1==-j12 byte-identical, fragments clean. | 364,689 | 693,523 | 1,350,667 | 2,664,111 | 5,290,706 | 10,544,455 |
| 2026-07-24 | P1.7b Set-with-String (managed-key drop): Set record 40->48B (elemDrop@40), __set_create gains an elemDrop param, 3 drop sites + __set_from move-out. Scale corpus is Set-free, so per-phase memory expected FLAT vs prior row. | 364,689 | 693,523 | 1,350,667 | 2,664,111 | 5,290,706 | 10,544,455 |
| 2026-07-24 | stdlib-whitelist loader (chain step 3) — Clock prepended to every compile via `Compiler/StdlibWhitelist.maxon`; located by exe-dir walk, registered like a user source, DFE prunes it when unused. DELTA IS A FLAT PER-COMPILE CONSTANT: ~+2.7K allocs / ~+0.5MB bytes at EVERY rung of a 32x ladder (was linear before two fixes). ATTRIBUTION: (1) the constant is Clock's fixed lex+parse+lower (7 fns, pruned by DFE before the backend); (2) the runtime-floor skip's reachability check short-circuits (allocation-free `moduleReferencesWhitelisted` opRefs scan) for every program that names no whitelisted callee — the whole non-Clock corpus — so its O(functions) name->index map + reached array are never built; (3) DFE's `sweepUnreachedFunctions` now skips `valueOrigins.remapFunctionIndices` on TRAILING drops (no survivor's index moved), which is exactly the shape an unused whitelisted module makes since it merges LAST — without it every non-Clock compile paid the +487K..+20.5M program-sized column rebuild the `dropped==0` fast path already documents. Gates: x64 1533/0 exit 0 no leak; wasm32-wasi 1367/0 exit 0; --workers=1==12 byte-identical; fragments additions-only (zero M). | 367,377 | 696,224 | 1,353,374 | 2,666,848 | 5,293,473 | 10,547,258 |
| 2026-07-24 | stdlib-whitelist loader — OPTIMIZER pass (in-place valueOrigins compaction). REMOVES the whitelist's dominant linear byte term: bytesDelta vs the row above is -486,672 (rung0) .. -20,503,416 (rung5), ~2.5% of TOTAL compile bytes at the top rung, on EVERY runtime-installing compile (not only whitelisted ones). ROOT CAUSE + CORRECTION of the row above: its claim (3) — that DFE's trailing-drop skip spares the corpus — is FALSE for any program that installs an emitted runtime. The scale corpus allocates (structs), so the mm heap runtime is appended AFTER the merged module (Compiler.maxon:266) while DFE runs later still (BackendDispatch:92); Clock therefore drops from the MIDDLE, a survivor (a runtime fn) shifts, keptNumberingShifted=true, and `valueOrigins.remapFunctionIndices` RAN on every rung — its O(rows) rebuild pushing survivor rows into 9 fresh columns (3 ValueOriginTable + 6 ParamOriginTable), 2-5x the survivor count in bytes under the geometric growth policy. That rebuild WAS the linear bytesDelta (measured: phase:deadFunctions was 78%->98% of the whole whitelist byte delta; A/B vs empty whitelist = +487,737..+20,503,457, x2.0/rung). THE FIX: compact the columns IN PLACE (set survivor rows over the leftmost free slot, then resize-down), mirroring IrModule.assignCompactionSlots. write<=read throughout, so no unread slot is clobbered; the result is byte-identical to the rebuild (same survivor rows, same remapped funcIndex) so E5001 span resolution and every golden are unchanged — this is a memory optimization only, NOT semantic, and cannot reintroduce the step-1 renumber bug. After the fix phase:deadFunctions returns to the empty-whitelist baseline (delta +41 bytes flat, the 7 extra Clock slots in newIndexOfOld). RESIDUAL whitelist cost (A/B vs empty whitelist, post-fix): +135K (rung0) .. +360K (rung5) bytes = flat Clock lex+parse+lower (~107K, verified identical rung0 vs rung5) + the O(program) `moduleReferencesWhitelisted` short-circuit scan (isolated by a bypass A/B at +16K..+246K, x2.0/rung — LINEAR, run once per compile, the minimal cost of asking "does this program reference a whitelisted callee"; not nested, no superlinearity). Allocs delta is a near-flat per-compile constant ~+2.6K (Clock's fixed parse). BENEFIT: the whitelist reuses existing stdlib source instead of reimplementing it — a per-compile constant for programs that USE a whitelisted module, and byte-identical goldens (ZERO) for the whole corpus that does not. Gates: x64 1533/0 exit 0 no leak; wasm32-wasi 1367/0 exit 0; --workers=1==12; specs-shv2/fragments zero M; verify-warm-rebuild PASS. | 367,287 | 696,125 | 1,353,260 | 2,666,719 | 5,293,329 | 10,547,093 |
| 2026-07-24 | stdlib-whitelist loader — REVIEW fix (`moduleReferencesWhitelisted` now SKIPS the whitelisted functions' OWN bodies before scanning). bytesDelta vs the row above is NEGATIVE: -16,048 (rung0) .. -246,156 (rung5), x2.0/rung; allocsDelta -74 (rung0) .. -134 (rung5). CORRECTS the row above's residual attribution. That row credited a "+16K..+246K x2.0/rung `moduleReferencesWhitelisted` short-circuit scan" as the whitelist's last growing term and called it linear-but-minimal. It was neither the scan nor minimal: the scan is ALLOCATION-FREE (a direct `block.opRefs` walk that allocates nothing). The growing term was the O(functions) reachability MAP — the name->index map + `reached` bool array the runtime-floor skip's full walk builds — which built on EVERY compile because the short-circuit NEVER FIRED: the whitelisted set is internally connected (`Clock.elapsedMs` calls `Clock.nowMs`), so scanning the whitelisted bodies THEMSELVES always found an intra-whitelist edge and returned true. The prior "bypass A/B" that isolated +16K..+246K bypassed the WHOLE function, so it measured (free scan + always-built map) and mislabeled the sum "the scan". THE FIX skips `whitelisted.contains(func.name)` bodies before scanning, so the only edge that counts is the first crossing INTO the whitelist from a non-whitelisted (user/runtime) caller — the exact question — which is false for the whole non-Clock corpus, so the map is now never built. That the drop (-16K..-246K) EQUALS the prior bypass A/B in magnitude proves the scan itself costs ~0 bytes and the entire term was the map. NEW residual whitelist cost for the non-Clock corpus (vs empty whitelist): a FLAT per-compile constant — Clock's fixed lex+parse+lower (~107K-119K, DFE-pruned before the backend) — with NO growing term; the allocation-free O(program) scan is the only O(program) work left and allocates nothing. Byte-neutral for goldens (only changes WHICH programs skip the map build, never emitted output). Gates: x64 1533/0 exit 0 no leak; wasm32-wasi 1367/0 exit 0; --workers=1==12 byte-identical; specs-shv2/fragments zero M. RE-BASELINED onto the merged tree (rebased over P1.7b String-Hashable+Equatable + Set-with-String, which added 19 x64 tests): merged-tree x64 1552/0, wasm32-wasi 1367/0, exit 0 no leak, --workers=1==12; the scale numbers here are unchanged by the rebase (same deterministic corpus). | 367,213 | 696,051 | 1,353,171 | 2,666,615 | 5,293,210 | 10,546,959 |
| 2026-07-25 | P1.7a slice 2b-ii (Comparable + Ordering) OPTIMIZATION PASS — **NO COMPILER CHANGE. Nothing in the rung is superlinear, and the one superlinearity I did find is invisible to this instrument by construction.** Every number on this row is bit-identical to a re-run of the rung commit d2c8bd79f (verified: the rebuilt binary is byte-identical in code size, 6,562,522, and `git status` is clean), so this row is a VERIFICATION, not a delta. (1) THE IMPLEMENTER'S READING REPRODUCED, AND THE LOAD-BEARING HALF PROVEN: +68 allocs / +2,376 bytes vs row 336, bit-identical at rung 0 AND rung 5. What makes that a CONSTANT rather than a leak shaped like one is WHICH axes the ladder doubles underneath it: the corpus grows from 19 files / 12 `type` declarations at rung 0 to 143 files / 260 declarations at rung 5, and the delta does not move by one allocation. That refutes two suspects outright — `createEnumRegistry` is O(1) PER COMPILE and not O(files) (a per-file seed would have grown 7.5x), and `requireTypeNameNotCompilerOwned` -> `isCompilerOwnedTypeName` (three `ByteArray.equals`, run at EVERY `type`/`enum`/typealias declaration) allocates exactly ZERO (a 21.7x growth in declarations moved nothing). The +68 is the two enum registries' first `Map` upsert initializing their 16-slot arrays, as claimed. (2) THE CORPUS IS BLIND TO THIS RUNG, so the +68 measures only the seed. Its manifest declares `generics` NOT GENERATED, and the emitted corpus contains ZERO interfaces, ZERO enums and ZERO generic instantiations — so NONE of the witness/ordering paths run: not `builtinOrderingEnum` per operator, not `orderingTagOf`, not `findWitnessDispatch`, not the witness-reloc-gated `int.compare` install. I therefore measured them on a PURPOSE-BUILT doubling ladder (a `Pair uses T where T is Comparable` with N methods x 4 ordering operators, 64 -> 2,048 operators, against an identical-shape CONTROL on a plain concrete int). BOTH ARE LINEAR: allocs x1.809 x1.893 x1.942 x1.970 x1.984 (converging on x2.00 from below exactly as the real corpus does, the fixed per-compile constant amortizing away), bytes x2.013 at the top rung. The ordering path's OWN marginal cost — ladder minus control — is also linear (x1.844 x1.910 x1.951 x1.974 x1.986), settling at 99.7 allocations per ordering operator. No superlinear term exists in the rung. (3) `builtinOrderingEnum()` COSTS EXACTLY 10 ALLOCATIONS / 376 BYTES PER CALL, and `emitOrderingVerdict` calls it once per ordering operator. Measured directly by an A/B probe compiler that made ONE EXTRA call per operator: +5,120 allocs over 512 operators and +20,480 over 2,048 — 10.000 per call both times, +376 bytes both times (the record, three column arrays, three empty payload arrays, three first-push buffers). LEFT ALONE DELIBERATELY, and the reason is the interesting part: it is 10% of the path's 99.7 allocs/operator, it is a CONSTANT FACTOR not a curve, it is ZERO on the real corpus (shv2's own source contains no ordering operator on a constrained type parameter), and the obvious fix is a TRAP — an eagerly-built per-`Parser` cached layout would pay 10 allocations per FILE whether or not the file compares anything, converting today's FLAT +68 into a GROWING term (+1,430 at rung 5's 143 files). A lazy memo would avoid that and buy a constant factor on a path nothing takes; not worth an option field and a liveness question. `orderingTagOf` -> `indexOfCase` is a 3-element scan and allocation-free; noise, measured and left. (4) THE FIND, and it is TIME-ONLY so this instrument STRUCTURALLY CANNOT SEE IT (the corpus manifest warns of exactly this class in its own words: "a cost that re-walks tokens or chases pointers but allocates little will read FLAT in these columns even when it is quadratic in time"). `Parser.requireInterfaceForParse` (Parser.maxon:16750) resolves a `where`-constraint interface by LINEAR SCAN over `self.artifact.interfaces` after missing the builtin table — so witness dispatch is O(dispatch sites x interfaces declared in the same file). Isolated by an A/B in which the ONLY variable is the hit's INDEX: the same program, same N interfaces, same N dispatch sites, with `Digest` declared LAST vs FIRST. Allocations are IDENTICAL (1,417,523 both — that is why it is invisible here), while parse time diverges — best-of-3 parse ratios x2.00 x2.52 x2.52 (last) against x1.90 x2.05 x2.19 (first), and at N=1024 a +24.15 ms parse delta out of a +27.05 ms whole-compile delta, everything else inside noise. FILED AS MEASURED DEBT, NOT FIXED, because the real corpus bounds it: the maximum interfaces declared in ANY one file across maxon-shv2, stdlib, maxon-selfhosted and specs-shv2 is 17 (and `requireInterfaceForParse` scans only the CURRENT file's artifact), so K <= 17 and the term is linear-in-practice today — the `SplitLiveRanges` K^2 case, max K = 8. RE-MEASURE TRIGGER: a machine-generated source file, or any rung that lands many protocol interfaces in ONE artifact; the cure is an index beside `artifact.interfaces` with a SINGLE writer, which would also remove `builtinInterface`'s construct-on-every-lookup (it builds a fresh `IrInterface` per call — the same constructor-as-lookup shape as `builtinOrderingEnum`). (5) Also verified by reading, since the corpus generates no enums to exercise them: the permanently-seeded `Ordering` entry multiplies inside NO walker. `installUnionDestructors`/`installUnionCloners` select unions with a managed payload (payload-free `Ordering` hits `continue`), `noteDestructorUsage`/`noteClonerUsage` gate on `neededDestructors`/`neededCloners` membership, and `closeDestructorNeeds`'s fixpoint gains exactly one entry per pass — a flat constant, already inside the +68. Gates: x64 1567/0 exit 0 no leak; wasm32-wasi 1375/0 exit 0; `--workers=1` stdout BYTE-IDENTICAL to `--workers=12` (both 1567/0); `specs-shv2/fragments` ZERO modified; whole worktree clean. | 367,281 | 696,119 | 1,353,239 | 2,666,683 | 5,293,278 | 10,547,027 |
| 2026-07-25 | P1.5 #73 (derived capturing-closure escape invariant) — OPTIMIZER pass. TWO results, one about the rung and one about this instrument's baseline. (1) THE RUNG + THIS OPTIMIZATION HAVE A FOOTPRINT OF EXACTLY ZERO ON THIS CORPUS, A/B-VERIFIED ON ONE BOOTSTRAP AT ONE PATH: the byte-pristine base 13a3fcefe (Parser.maxon + SemanticCheck.maxon checked out at the base commit, rebuilt in THIS worktree) produces 367,213/22,451,270 ... 10,546,959/796,862,949 — identical allocation-for-allocation and byte-for-byte to HEAD-with-this-optimization on ALL SIX rungs. Expected and structural: the new pass is gated by `boolArrayAny(func.valueIsCapturingClosure)`, an allocation-free bit scan, and the arithmetic corpus contains NO closures and no function-typed parameters, so `checkCapturingClosureUses` never scans a body and `buildEscapeSummary` short-circuits. phase:semanticCheck stays exactly linear here (571/1,027/1,939/3,763/7,411/14,707 allocs, x1.99 per rung). THE CORPUS THEREFORE CANNOT SEE THIS RUNG AT ALL — same structural blindness as the DFE rung's "dropped 0 of 60"; the real finding was measured OFF-corpus and is below. (2) THE QUADRATIC THIS PASS FIXED, measured on a closure-dense program the corpus cannot express (one straight-line function holding C capturing closures, each passed down to a callee that only calls it — all ACCEPTED, so the whole body is scanned): the rung as merged scanned the body ONCE PER CLOSURE, and since a function's op count grows WITH its closure count that is a quadratic in the function's own size. phase:semanticCheck BEFORE: 1.1 / 4.3 / 16.4 / 66.7 / 271.8 ms for C = 40/80/160/320/640 — x3.9, x3.8, x4.1, x4.1 per doubling, i.e. ~4x = QUADRATIC (271.8/1.1 = 247x over a 16x input; 16^2 = 256). AFTER: 0.2 / 0.3 / 0.7 / 1.7 / 2.8 ms — ~x2 per doubling = LINEAR, confirmed to a 6th rung at C=1280 (7.2 ms, x2.1). 97x faster at C=640. ATTRIBUTION IS DIRECT, not inferred: commenting out the single `checkCapturingClosureUses` call took the BEFORE ladder to 0.1/0.3/0.5/1.4/2.3 ms, so the pass WAS 99.2% of semanticCheck's time at C=640. THE FIX: `EscapeScanSubject` stops naming the KIND of value and starts naming WHICH values one pass tracks — `everyCapturingClosure` sweeps the whole marked column in a SINGLE body walk (the mark is an O(1) array read, so testing "is this operand tracked" costs the same as comparing one id), making the cost O(ops) whatever the closure density. The ONE band-append `match` that is the rung's whole guarantee is untouched and still the only enumeration of escape sinks. (3) ALLOCATIONS WENT DOWN TOO, and the first version of this fix made them go UP — recorded because it is the trap: a Maxon UNION value is a HEAP OBJECT, so the obvious inversion (have the per-op band match hand back WHICH value escaped, as a `ValueEscapeSite`) costs ONE ALLOCATION PER OP SCANNED — measured at +11 allocations per closure (12,825 vs 5,774 at C=640), turning a time quadratic into a linear allocation regression. A `type` is no better; `ConstValue`'s own comment says so ("a `type` (so, one heap object per operand folded)"). The shipped version keeps every per-op/per-operand leaf answering in a BOOL and constructs the union at most ONCE per scan, recovering WHICH closure with a separate `oneCapturingClosure` query over the ONE op that stopped the sweep — O(closures), on the refusal path only, never on the accepting one. Result vs the rung as merged: phase:semanticCheck allocations 374/734/1,454/2,894/5,774 -> 337/657/1,297/2,577/5,137, i.e. -637 at C=640 = exactly ONE allocation removed per capturing closure (the per-closure union the per-closure scan used to build). (4) ⚠ THE -126 FLAT DELTA THIS RUN PRINTS AGAINST THE ROW ABOVE IS NOT COMPILER WORK — IT IS THE LENGTH OF THE REPO-ROOT PATH, and that is MEASURED, not inferred. The same commit 13a3fcefe, the same bootstrap sources, the same corpus, built and run in a SECOND worktree differing ONLY in its directory name, reports a different flat constant: `...\worktrees\p15-escape-invariant` gives 22,451,270 (delta -126) and `...\worktrees\p15x` gives 22,450,598 (delta -798) — 672 bytes apart at EVERY rung of a 32x ladder, with allocsDelta 0 in both, over a path 16 characters shorter (42 bytes per character). So a `bytesDelta` that is FLAT across the ladder and moves no allocation COUNT is, to first order, a statement about where the tree lives, not about what the compiler did. This subsumes part of what rows 326/330/332 called "build provenance": a row recorded from a worktree carries its worktree's path length, and the next reader in a differently-named directory sees the difference as a phantom constant. READING RULE, so nobody chases it again: judge a change by allocsDelta and by whether a byte delta GROWS with the rung; a flat byte delta with allocsDelta 0 should first be checked against the path the previous row was recorded from. THIS row is recorded from `.claude/worktrees/p15-escape-invariant`. Gates on the final state: x64 1561/0 exit 0 no leak (no 101); wasm32-wasi 1374/0 exit 0; --workers=1 1561/0 and PE byte-identical to --workers=12 (md5 c16ed43d…); specs-shv2/fragments COMPLETELY CLEAN (zero M) after x64, wasm and workers=1 runs — codegen byte-neutral, as a pure-perf refactor must be. | 367,213 | 696,051 | 1,353,171 | 2,666,615 | 5,293,210 | 10,546,959 |
| 2026-07-25 | P1.5 #73 REBASED ONTO UPSTREAM f44275314 (P1.7a 2b-ii Comparable+Ordering) — the +68 allocs / +3,720 bytes flat at EVERY rung is UPSTREAM's, not this rung's, and the attribution is clean because this row and the one above it were recorded from the SAME worktree, so the path-length constant documented in the row above is held CONTROLLED. Reasoning, by that row's own reading rule: allocsDelta is NOT 0 (+68), so this IS compiler work rather than a path artifact; and it is FLAT across a 32x ladder rather than growing, so it is a fixed setup cost, not an algorithmic term — consistent with P1.7a 2b-ii registering additional builtin conformances (Comparable/Ordering) once per compile. This rung contributes ZERO: measured 0 allocs / 0 bytes on all six rungs against a byte-pristine base, twice, and structurally blind here because `checkCapturingClosureUses` is gated by an allocation-free `boolArrayAny` bit scan and the arithmetic corpus contains no closures and no function-typed parameters. phase:semanticCheck remains exactly linear (571/1,027/1,939/3,763/7,411/14,707 allocs, x1.99 per rung). The rung's real measurement is OFF-corpus and is in the row above (a closure-density quadratic, 271.8 ms -> 2.8 ms at C=640). Gates on the rebased tree: x64 1581/0 exit 0; wasm32-wasi 1387/0 exit 0; --workers=1 == --workers=12 byte-identical stdout, both 1581/0; specs-shv2/fragments COMPLETELY CLEAN (zero M) after x64 and wasm runs, so the derived escape rule stays codegen-neutral against upstream's tree as well as against its own base. | 367,281 | 696,119 | 1,353,239 | 2,666,683 | 5,293,278 | 10,547,027 |
| 2026-07-25 | P1.5 whitelist-widening #2 — three fixes, ZERO whitelist growth. The stdlib/Sleep.maxon entry was TRIED and then HELD OFF by coordinator ruling (see below), so `whitelistedStdlibRelativePaths()` is byte-identical to base and only the three defects the attempt exposed are here: `__Builtins.<void member>(...)` in STATEMENT position, the `__Builtins.sleep` arm routed through the bare builtin's own `emitSleep`, and `__gt_sleep` joining the E3104 target gate (`isClockSubstrateCallee` -> `calleeNeedsWin32Substrate`, moved beside `targetHasWin32Substrate`) so a `sleep` compiled for wasm/arm64 is refused at the call span instead of panicking in the backend. MEASUREMENT: EXACTLY ZERO — 0 allocs and 0 bytes at all six rungs, bit-for-bit identical to base. ATTRIBUTION IS CONTROLLED, not inferred: this same worktree was rebuilt from the PRISTINE 2f2049e50 compiler sources and measured, giving 367,281 / 696,119 / 1,353,239 / 2,666,683 / 5,293,278 / 10,547,027 allocs and 22,454,948 / 45,142,745 / 93,231,484 / 189,656,980 / 388,482,350 / 796,866,627 bytes — the SAME six pairs this build produces. The -42 bytes/rung against the previous logged row is therefore this worktree's PATH-LENGTH CONSTANT and not compiler work: it is present in a build containing none of this rung's code, which is the check that the A/B method is sound. STRUCTURALLY ZERO, as expected: `builtinsIntrinsicCallsAt` short-circuits on `kindAt(index+1) != dot` before allocating anything and the arithmetic corpus contains no `ident.ident(` statement; `isBuiltinsIntrinsicCallee` is the `bytesStartWith` the code already called, given a name; `emitSleep`'s new `name` parameter is a borrowed ByteArray read only on throw paths; `calleeNeedsWin32Substrate` adds one `equals` inside a predicate only `__`-prefixed callees reach, of which the corpus has none. ⚠ FOR CONTRAST, and as the price list for the next widening: the WITHDRAWN entry measured +677/+701/+678/+701/+677/+677 allocs and +30,550/+37,174/+31,062/+43,318/+30,550/+30,550 bytes — FLAT across the 32x ladder, one more stdlib file load/lex/signature/parse/merge'd on EVERY compile. That is the whitelist's per-module design cost, and it is what a whitelisted module must earn back in delivered capability; Sleep could not, because the parser's bare-name `sleep` builtin claims every call site before any registry is consulted, so no ordinary call could reach the module. Gates: build exit 0 zero warnings; x64 1586/0 exit 0 (base 1581 + 5 new builtins-sleep cases); wasm32-wasi 1389/0 exit 0 (base 1387 + 2 E3104 rejection cases); --workers=1 == --workers=12 byte-identical stdout on BOTH targets; specs-shv2/fragments ADDITIONS ONLY, zero M after x64 and wasm runs. | 367,281 | 696,119 | 1,353,239 | 2,666,683 | 5,293,278 | 10,547,027 |
| 2026-07-25 | P1.5 whitelist-widening #2 — OPTIMIZER pass. NO COMPILER CHANGE: nothing in the rung is superlinear, and the one constant-factor cost found was PRICED and deliberately NOT shipped. (1) THE ROW ABOVE IS CONFIRMED INDEPENDENTLY, not accepted: this worktree was rebuilt from the pristine 2f2049e50 compiler sources IN THE SAME PATH (holding the path-length constant fixed) and produced the same six pairs — 367,281 / 696,119 / 1,353,239 / 2,666,683 / 5,293,278 / 10,547,027 allocs — so the -42 bytes/rung really is this worktree path and not compiler work. (2) THE LADDER BLIND SPOT WAS CLOSED WITH AN ADVERSARIAL CORPUS. The arithmetic ladder generates no ident.ident( statement at all, so it structurally could not see the new statement-door predicate builtinsIntrinsicCallsAt do anything. Six statement-shape corpora at 2000 AND 4000 statements each were compiled with BOTH binaries and diffed per phase: bare-call f(), method-call b.m(), field-assign b.n=k, reassign x=x+k, bare sleep(0) (driving the new name parameter of emitSleep 2000/4000 times), and Clock.nowMs() (driving isBuiltinsIntrinsicCallee in parseCallNamed AND calleeNeedsWin32Substrate in semanticCheck 2000/4000 times). ALL TWELVE PROGRAMS: 0 allocations and 0 bytes delta in EVERY phase. The predicate or-chain short-circuits, so a bare-call statement pays exactly two kindAt reads and no allocation — measured, not asserted. (3) THE ONE REAL COST, PRICED: builtinsIntrinsicCallsAt answers a question about its FIRST token by building the whole qualified name with qualifiedMethodName — 4 allocations and 201 bytes per __Builtins.<member>(...) STATEMENT, measured by building a variant whose last line is a token compare and diffing a 2000- and 4000-statement corpus (exactly 8,000 and 16,000 allocations). NOT SHIPPED, because the occurrence count today is ZERO: whitelisted stdlib/Clock.maxon uses all three intrinsics in VALUE position (return __Builtins.currentTimeMs()), which never reaches the statement door, and even a whitelist grown to Sleep+Print+PrintError+Subprocess is about 9 such statements = 36 allocations FLAT per compile, against the +677 allocations per module the whitelist itself costs. A constant factor with no occurrences is a change that only looks like an optimization. (4) A READING TO EXPLAIN, not a regression: every phase ALLOCATION column is x2.01 or below, while the BYTES columns read x2.05 to x2.31 (regalloc:criticalEdges highest). Bytes-per-allocation rises about 3 percent per rung because the corpus DOUBLES locals-in-scope per function every rung (the V dimension, named in its own manifest), so the per-function arrays are genuinely bigger each rung: a linear allocation COUNT with a growing allocation SIZE, not a bent curve. Gates re-run on the final tree: build exit 0 with zero warning lines; x64 1586/0 exit 0 no leak; wasm32-wasi 1389/0 exit 0; specs-shv2/fragments 5 A and 0 M with git status empty after both suites; the rebuilt binary is byte-identical to the one measured. | 367,281 | 696,119 | 1,353,239 | 2,666,683 | 5,293,278 | 10,547,027 |
| 2026-07-25 | CORPUS CHANGED -- ⚠ THE DELTA COLUMN ON THIS ROW IS MEANINGLESS, and nothing above it is comparable: this row is the NEW BASELINE. The ladder had been measuring the compiler roughly as it stood at P1.1a -- in 109,697 lines of rung-5 source it generated ZERO of `match`, `enum`, `String`, `Array with`, `type ... uses`, `interface`, `implements`, `print(`, ` or `, ` xor `, and struct field WRITE. Ten new knobs add them, each doubling per rung and each paired against the knob a future change must be free to move independently of it: enum TYPES vs match SITES (rotating over all three dispatch strategies), String op sites vs managed-struct TYPES (per synthesized __destruct_<T>), trivial-element vs MANAGED-element Array groups, generic BASES (one shared body) vs generic INSTANTIATIONS (one layout descriptor), interface CONFORMANCES vs concrete DISPATCH SITES -- plus a fixed-size coverage file (logical/bitwise or/xor/not, a narrow-range `as` cast over an opaque value, print, a loop that breaks, a struct-typed field written through the loaded box). Corpus rung0 19->30 files, rung5 143->185 files and 109,697->139,381 lines. THE OPTIMIZATION IS SEPARATE FROM THE CORPUS JUMP AND IS A/B-MEASURED ON THIS SAME NEW CORPUS, 8 rungs, one binary rebuild per change. The new heap knobs exposed a REAL SUPERLINEARITY in regalloc:splitting: bytes x192.1 over a x128 ladder (N^1.084) with the per-rung ratio CLIMBING monotonically 1.77 -> 2.61 while allocations stayed linear at x2.03 -- linear allocs plus superlinear bytes is one O(N)-sized allocation made O(N) times. It was TWO scratch buffers, both in SplitLiveRanges.maxon, both sized by the FUNCTION and allocated per SPLIT. (1) materializePeak took a FRESH wordsPerRow live-before row on every peak, and refreshPeak materializes a peak after EVERY split, so it cost O(splits x values) bytes per function. It is now SplitScratch.peakLiveBefore, grown with growFilled (GEOMETRIC: refill/resize go through reserve, which takes the ask at face value, so they would reallocate and copy the whole row at every tick of wordsPerRow and keep the same shape at a smaller constant) and returned to all-zero in place; usesAtPeakOp's sibling row got the same treatment. Its saving QUADRUPLES per rung -- 1,568 / 11,024 / 46,944 / 189,896 / 759,776 / 3,034,208 / 12,125,928 / 48,474,016 bytes -- which is exactly what a splits x values term looks like on a doubling ladder, and it took splitting to x166.7 (N^1.054). (2) markBlocksReachableFromPeak allocated its BFS worklist FRESH per un-memoized (peak block, def block) pair, and that walk pushes a block per IN-EDGE: an O(edges) allocation per walk. It is now ReachCache.worklist, cleared per walk, exactly as LivenessMarkScratch already owns the worklist of the other CFG walk in this pass. Its saving also quadruples -- 832 / 17,040 / 84,272 / 358,000 / 1,462,512 / 5,899,760 / 23,674,864 / 104,249,328 bytes. RESULT: regalloc:splitting bytes 1,896,611->364,331,348 becomes 1,894,211->211,608,004, i.e. x111.7 over a x128 ladder, N^1.084 -> N^0.972, and the per-rung ratio stops climbing and sits FLAT at the byte column's linear reading (2.02 2.05 2.03 2.04 across the top four rungs). That is -41.9% of the phase at rung 7 (-152,723,344 bytes) and -3.2% of the whole compile; allocations were already linear and stay linear (x2.03 -> x2.02). NOTHING ELSE MOVED: at all 8 rungs every other phase and every other regalloc sub-phase is bit-identical (liveness, coloring, rewrite, criticalEdges, blockOrder, ssaDestruction, and all 22 non-regalloc phases), so the entire delta is those two buffers. CODEGEN-NEUTRAL: x64 1581/0 and wasm32-wasi 1387/0, both exit 0, with specs-shv2/fragments COMPLETELY CLEAN (zero M) after BOTH runs -- 1581 programs' Target IR byte-identical. And because a stale liveness row is a MISCOMPILE rather than a crash, the pass's own VerifyIncrementalSplit (which compares every incremental peak row word-for-word against a full rebuild at every split) was switched on: 1581/0 under it, then SABOTAGED by removing the zeroing to prove the check fires -- 11 of 15 register-spill tests then fail, 8 reporting "the peak's live-before row differs from a full sweep's at word 0" and 3 on the per-class live tally, which catches it even with verify OFF. ALSO NEW ON THIS ROW, AND IT IS THE CORPUS WAKING A PASS RATHER THAN A REGRESSION: phase:insertRangeChecks went from INERT to ACTIVE. The old corpus emitted no cast at all and only FULL-range aliases (int(0 to u64.max), int(i64.min to i64.max)), which rangeIsFull refuses to guard, so the pass did nothing; the coverage file now declares typealias ScaleCoverNarrow = int(0 to 4095) and casts to it. Its bytes read x2.41 at rung 5, and that is a REALLOCATION LUMP, NOT A CURVE: over 8 rungs the ratio humps at 2.41 and then FALLS BACK (1.62 1.69 1.70 2.08 2.41 2.26 2.07), where a quadratic's ratio rises monotonically toward 4 and cannot fall, while allocations converge to x1.95 from BELOW (1.28 1.43 1.59 1.74 1.85 1.91 1.95) -- a fixed per-compile part plus a linear one. This is reading rule 1 above: Go-style geometric growth makes a linear phase's BYTE column read above 2.00 while a buffer is still climbing. The guard-SITE count is FIXED at one per rung, so the growing part is not the guard emitter but the per-function scan over module.functions (InsertRangeChecks.maxon:83-86), whose project.rangeChecks.contains(func.name) probe now hashes against a non-empty map. The phase is 0.066% of rung-7 bytes. MEASURED DEBT, TIME-ONLY AND STRUCTURALLY INVISIBLE TO THIS INSTRUMENT: RangeCheckEmitter locates each guard site's block with getBlockByIdIn (IrModule.maxon:361), a linear scan of func.blockRefs, so the pass is O(sites x blocks) in TIME and allocates NOTHING for it -- no rung of this ladder can show it. ⚠ NUMBERS RE-BASELINED ONTO THE MERGED TREE, rebased TWICE over upstream (13 commits: whitelist-widening #2, debugger P4d-1/P4d-GT-STACK, and an x64 refusal of an unencodable imm32). The deltas vs the pre-rebase measurement — rung0 -211 allocs / -2,946 B, rung5 -17,850 allocs / -8,934,514 B — are UPSTREAM's plus a path-length constant (this tree's path is 14 chars shorter than the rung worktree's, worth -546 B at rung 5, which is why the two rebased runs differed by exactly that and their ALLOCS were identical). They are NOT this rung's: regalloc:splitting is BIT-IDENTICAL at 1,894,211 -> 51,024,570 with the per-rung ratio flat at 2.04 across BOTH rebases, and that is the whole of what the optimization claims. | 604,317 | 1,096,057 | 2,079,233 | 4,044,250 | 7,975,388 | 15,840,463 |
| 2026-07-25 | Stdlib diagnostic attribution + the REAL stdlib loader. FLAT +1,030 allocations / +52,338 bytes per compile, IDENTICAL at rung 0 and rung 5 (a 32x span) — zero growth term. ATTRIBUTED BY PHASE, not guessed: the only phase that moves is phase:load, +1,029 at BOTH ends, which is the loader now ENUMERATING stdlib/ (49 files, Directory.list + a FilePath per entry + a byte-lexicographic sort of the survivors) instead of opening one hardcoded path. That is the price of making the loader real so the whitelist can be deleted rather than rewritten; it is a function of the stdlib directory, never of the user program. The remaining +1 is the StdlibFacts carrier. TWO GROWING TERMS WERE FOUND AND REMOVED BEFORE THIS ROW, both mine and both the same mistake — asking FilePath.isInside a question the loader already knew the answer to. isInside allocates ~4 Strings per call (on Windows canonicalForCompare clones and lower-cases BOTH sides, plus an interpolated needle). (1) Classifying every module FUNCTION as stdlib-or-user by path: +3,633 allocs at rung 0 rising to +48,273 at rung 5, doubling with the corpus. (2) Classifying every SOURCE FILE by path: +60 rising to +1,920, still doubling, because the generated corpus has many files. Fixed by deriving the stdlib function-name set ONCE from the parse artifacts of the files the loader itself recorded (O(stdlib functions), a memo hit) and answering the per-function question with a ByteArray hash against that set. LESSON, worth the row on its own: a path predicate that reads like a pure comparison is four allocations in this stdlib, so it belongs at load time, once per file, never in a per-function or per-op loop. Also measured OFF-CORPUS, because the arithmetic ladder contains no stdlib call at all and is structurally blind to the crossing path: 200 vs 400 Clock.nowMs() crossing sites cost an IDENTICAL delta, so the substrate fixpoint is per-compile and has no per-call-site term; 200 vs 400 non-crossing calls on wasm (the target where the new map lookup actually executes) likewise cost an identical delta, so the per-call-op gate allocates nothing. Gates on the final tree: build exit 0 zero warnings; x64 1586/0; wasm32-wasi 1394/0; --workers=1 byte-identical to --workers=12 on both targets; specs-shv2/fragments zero M and zero A; verify-warm-rebuild PASS on a Clock program. | 368,311 | 697,149 | 1,354,269 | 2,667,713 | 5,294,308 | 10,548,057 |
| 2026-07-25 | Optimizer pass on the stdlib-loader rung: the SORT IS GONE, and the whitelist filter stopped being quadratic. -180 allocations / -9,810 bytes at EVERY rung, identical across the 32x span — a per-compile constant, as it must be: nothing here is a function of the user program. ATTRIBUTED EXACTLY, by mm-trace scope diff, not guessed: String.clone -46, cow_copy -46, String.sliceBytes -92. All of it is FilePath.canonicalForCompare, called 46 fewer times, because the whitelist's membership test was a LINEAR SCAN calling FilePath.equals (which canonicalizes BOTH sides — on Windows a clone + a lower-case each) and is now a Set-with-FilePath hash lookup (which canonicalizes ONE). (1) THE SORT WAS A MEASURED QUADRATIC, and deleting it is a user ruling, not a perf tweak: "the compiler must not depend on file order to work correctly, so it shouldn't sort — any nondeterminism just needs to be handled". A sort does not remove an order dependence, it makes one reliably HIDDEN. It was also genuinely superlinear, and the price is measured rather than argued: with the whitelist filter deleted (the end state) against synthetic stdlibs of 12/24/48/96/192 modules, the sort alone cost +122 / +436 / +1,638 / +6,344 / +24,970 allocations, i.e. x3.57 x3.76 x3.87 x3.94 per DOUBLING — converging on x4.00, textbook O(n^2). Insertion sort, and each comparison was 4 allocations because pathSortsBefore did toString().toByteArray() on both sides. At this checkout's real 49 stdlib files that is ~1,640 allocations and ~67 KB per compile the end state will now never pay. TODAY it shows as zero in the table above, because with one whitelisted module the sort did no comparisons at all — which is exactly why it had to be priced at the size it was heading for, not the size it is. (2) The filter's remaining O(files x listed) term is now O(files): the list is DESIGNED to grow one entry per widening rung until it names all 49, and a 48-entry scan would have cost ~9,400 allocations a compile against the hash lookup's ~98. (3) THE PREVIOUS ROW'S +1,030 IS CONFIRMED INDEPENDENTLY, measured base 80553e65a vs rung HEAD in ONE worktree so the path-length constant is controlled: +1,030 allocations / +52,368 bytes total, of which +1,024 / +52,148 in phase:load and +6 unattributed. The attribution to the directory enumeration was right. That row's mention of "a byte-lexicographic sort of the survivors" is superseded here. Net rung cost vs base is now +850 / +42,552. (4) THE WHITELIST-DELETED WORLD IS LINEAR, which is the measurement that mattered, since the point of the rung is the world where all of stdlib loads. Built a filter-deleted compiler against synthetic stdlibs of 12->192 modules (8 functions each, chained into ONE 1,536-deep call chain so every stdlib function is reachable AND substrate-marked — maximum stress for the attribution fixpoint). Every phase converges to x2.00 per doubling: total x1.87 x1.93 x1.96 x1.98; parse x2.00; semanticCheck x2.00; regalloc x1.99; phase:load x1.47 -> x1.86 (fixed cost amortizing away). No scope in the mm-trace table exceeds x2.00 either. The fixpoint really did run: cross-compiling the 192-module chain to wasm answers E3104 at prog/main.maxon:2:10 naming 'Syn191.s191_7' and the entry '__gt_now_ns' it reaches 1,536 hops later — transitive attribution at the user's own span, at scale. (5) ORDER-INDEPENDENCE IS NOW MEASURED, NOT ASSUMED. With the stdlib walk reversed at 48 modules the program runs identically and the emitted IR is a PURE PERMUTATION (same multiset of lines). With the USER directory walk reversed, all 1586 x64 tests still pass and wasm stays 1394/0; 14 cross-file fragments move, 13 of them pure block permutations and 1 swapping which of two same-named file-private globals gets the $1 suffix. So the compiler is semantically order-independent; what follows load order is the EMISSION order and that suffix. Left as-is and reported: canonicalizing that belongs at the emission point, not at the loader's input. LANDMINE, measured and NOT fixed here (outside this rung's files): FilePath is the key type of all three query-spine memos (SourceFileMap / TokenCacheMap / ParseCacheMap), and FilePath.hash/equals canonicalize, so every memo probe clones and lower-cases a whole path — ~23 String.clone per source file, measured LINEAR at 2/9/33 sources (161/322/874 clones, ~23 per file at both steps). Linear, so it is debt and not a defect; re-measure if a pass starts probing those maps per FUNCTION or per OP rather than per file. | 368,131 | 696,969 | 1,354,089 | 2,667,533 | 5,294,128 | 10,547,877 |
| 2026-07-25 | ⚠ STALE-BASELINE ROW — THE HUGE DELTA ABOVE THIS ROW IS THE CORPUS CHANGING, NOT COMPILER WORK. This rung (real stdlib loader + stdlib-call diagnostic attribution) was REBASED onto upstream, which had landed a scale-corpus catch-up (`ScaleCorpus.maxon` changed, 10 commits). The previous two rows were recorded against the OLD corpus; this one is the FIRST against the new one, so the printed delta compares two different inputs and means nothing about the compiler. PROOF IT IS THE CORPUS, not an argument: rung-0 `sourceBytes` went 64,830 -> 97,427 (+50%), and the delta GROWS with the rung (+237,036 / +399,938 / +725,994 allocations at rungs 0/1/2) — a corpus change scales with the ladder, whereas a per-compile constant is FLAT across it, which is precisely how every real per-compile cost in this log has read. THE RUNG'S OWN COST IS UNCHANGED AND STILL VALID: +850 allocations / +42,552 bytes, FLAT at every rung, measured pre-rebase as a controlled A/B (base 80553e65a vs rung HEAD, both built in ONE worktree so the path-length constant is held fixed) and attributed to `phase:load`'s directory enumeration — the price of a REAL loader that walks `stdlib/` instead of a hardcoded list. That A/B remains valid because both of its arms used the same corpus; only the delta-vs-previous-row is invalidated. ⭐ READING RULE, so nobody chases this twice (it has happened before in this log — an earlier rung attributed +196…+3172 to itself when it was an upstream perf commit showing through a stale baseline): AFTER A REBASE THAT CROSSES A `ScaleCorpus.maxon` CHANGE, the delta against the previous row is meaningless by construction; re-establish the baseline with a same-corpus A/B and say so, and judge growth by whether a delta is FLAT across the ladder or scales with it. Gates on the rebased tree: build exit 0 zero warnings; x64 1586/0 exit 0; wasm32-wasi 1394/0 exit 0; specs-shv2/fragments zero M and zero A vs the new base; attribution re-verified at the user's span for the direct and indirect cases. | 605,167 | 1,096,907 | 2,080,083 | 4,045,100 | 7,976,238 | 15,841,313 |
| 2026-07-25 | REVIEW commit on the stdlib-loader rung (`stdlib-diagnostic-attribution`). +26 / +27 / +27 / +28 / +28 / +29 allocations and +2,774 … +6,358 bytes — a per-compile CONSTANT plus one geometric array, not a growth change; every phase still x1.89-2.16 per doubling. THE TWO HALVES WERE MEASURED SEPARATELY, in this worktree against this corpus: the shared enumerator alone is +9 / +10 / +10 / +11 / +11 / +12 allocations and +1,480 … +5,064 bytes, and the path guard adds +17 allocations and +1,294 bytes FLAT at every one of the six rungs. WHAT MOVED AND WHY: (1) the two recursive `.maxon` directory walks became ONE (`Compiler.collectMaxonSources`, used by the user root and by `stdlib/` alike) — the user walk now COLLECTS its paths into one `MaxonSourcePathArray` before loading them instead of loading as it descends, which is one array of n FilePath slots under geometric growth: O(log n) allocations, O(n) bytes, and it is the whole of the bytes delta (+1,480 at rung 0 doubling to +5,064 at rung 5). The stdlib side now enumerates all 49 files and filters after, instead of filtering inside the walk: +48 array slots, flat. Load ORDER is byte-identical (same depth-first sequence), which the zero-fragment-movement gate confirms. (2) a per-compile constant: one `Directory.currentPath()` + one `FilePath.isInside` asking whether the user's root lies under `stdlib/` — the guard for a REACHABLE wrong answer this review found by probing: `maxon-shv2 build stdlib/Clock.maxon` (relative) answered FIVE `E3006 duplicate definition of function 'Clock.nowMs'` against Clock's own lines, because `FilePath.equals` compares path TEXT and the relative spelling and the loader's absolute one are two identities to the query database, so the file was parsed twice and collided with itself. It now answers `E3001 No 'main' function found`. ⚠ THE ALTERNATIVE WAS MEASURED AND REJECTED, which is why the guard is shaped this way: resolving the project root so every registered path is absolute also fixes it, and it is the deeper repair — but it lengthens every path the three FilePath-keyed query memos clone (~23 String.clone per source file, the debt this rung already filed) and measured +824 / +959 / +1,229 / +1,769 / +2,849 / +5,009 allocations, doubling exactly with the corpus. That is 2.6x the per-file `isInside` cost this same rung's optimizer measured and rejected one commit earlier, so taking it here would have quietly undone that decision. The root-level guard buys the same corrected answer for a constant. ONE SPELLING OF A PATH IS STILL NOT ONE IDENTITY IN THE QUERY DATABASE — reported to the coordinator as its own rung, not fixed here. Gates: build exit 0; x64 1586/0 exit 0; wasm32-wasi 1394/0 exit 0; specs-shv2/fragments zero M zero A; arm64-macos attribution verified by hand (this host cannot run that suite). | 605,193 | 1,096,934 | 2,080,110 | 4,045,128 | 7,976,266 | 15,841,342 |
| 2026-07-25 | Retire the bare-name `sleep` builtin — `stdlib/Sleep.maxon` becomes the real `sleep`. THE ENTRY COSTS +683 ALLOCATIONS / +31,308 BYTES PER COMPILE, IDENTICAL AT RUNG 0 AND RUNG 5 (a 32x span): zero growth term, one more 7-line stdlib file walked through the front end. MEASURED AS A CONTROLLED A/B IN THIS ONE WORKTREE, not read off the delta column, because the previous row was recorded in a differently-named worktree: arm B is this exact tree with ONLY the `listWhitelistedModule(… "Sleep.maxon")` line removed, rebuilt and measured. That arm reads +0 ALLOCATIONS at every rung against the previous row and +1,312 bytes flat — so retiring the builtin itself (one deleted `equals` in `parseCallNamed`, one dropped `name` parameter on `emitSleep`) is EXACTLY FREE, and the +1,312 is this worktree's PATH-LENGTH CONSTANT. The delta column above therefore reads +683 / +32,620, of which +31,308 is the entry and +1,312 is the path. ATTRIBUTED PER PHASE, and the attribution is IDENTICAL at rung 0 and rung 5 — which is the proof it is a per-compile constant rather than a small growth term: load +34, lex +80, signatures +114, parse +257, merge +24, resolveTypes +4, lowerMaxonToStd +14, foldConstOperands +22, sugarGate +2, deadFunctions +1, isel +6. Nothing in regalloc, encode, link or writeExe moves at all. BYTE-NEUTRALITY IS MEASURED, NOT ASSUMED: `codeBytes` is EQUAL between the two arms at all six rungs (62,457 / 115,686 / 222,294 / 435,554 / 862,198 / 1,716,126), so a program that never sleeps emits the identical image — DFE prunes `@sleep` before isel. The two phases that still move after the prune are exactly the ones that must: `lowerMaxonToStd` runs BEFORE dead-function elimination and lowers the extra Maxon function, and `deadFunctions`+`isel` pay a few allocations because `IrModule.ops` is append-only, so the swept function's ops remain as orphans in an array a later column is sized from. THE 15 COMMITTED GOLDENS THAT MOVED ARE THE OTHER HALF OF THE SAME FACT and are entirely "a call frame appeared": 15 call sites change `x64.callDirect __gt_sleep` to `x64.callDirect sleep` across 14 files, each file gains one identical 8-line `func @sleep` block (prologue, `callDirect __gt_sleep`, epilogue — the millisecond argument needs no `mov`, since param 0 and call-arg 0 are both rcx), and the 15th is `async-sleep.float-arg-rejected`'s pinned error text moving from the builtin's bespoke `E3005 'sleep' requires a integer` to the ordinary `E3009 argument 'milliseconds': cannot implicitly convert 'float' to 'int' … use trunc(x)`, which is BYTE-IDENTICAL to what the C# bootstrap answers for the same program. Nothing else in the diff of all 15 files. Gates on the final tree: build exit 0 with zero warning lines; x64 1587/0 exit 0 (base 1586 + 1 new case); wasm32-wasi 1395/0 exit 0 (base 1394 + 1 new case); `--workers=1` byte-identical to `--workers=12` on BOTH targets; `specs-shv2/fragments` 15 M (each justified above) and 1 A; `grep -rli whitelist maxon-shv2/ --include=*.maxon` returns StdlibLoader.maxon and nothing else; verify-warm-rebuild PASS on a sleeping program; E3104 attribution verified by hand on all four non-x64-windows targets, at the user's own span and naming 'sleep'. | 605,876 | 1,097,633 | 2,080,793 | 4,045,811 | 7,976,949 | 15,842,025 |
| 2026-07-25 | OPTIMIZER PASS on the sleep-retirement rung — NO CODE CHANGE SHIPPED, because nothing superlinear was found and the numbers are already right; what follows is measurement, and the delta column above is 0 at every rung because this is the same tree the row below measured. THE TWO COST CLAIMS ARE CONFIRMED, by A/B/C arms built and measured in ONE worktree against ONE binary path so the path-length constant cancels instead of being subtracted. (1) RETIRING THE BUILTIN IS EXACTLY FREE, and more exactly than claimed: arm C (base compiler 31724ec31, Clock-only whitelist) vs arm B (this rung's compiler, Clock-only whitelist) reads +0 allocations, +0 BYTES and identical codeBytes at ALL SIX rungs. The row below attributed +1,312 bytes to a "path-length constant"; measured same-tree that term is 0 — it was the cross-worktree comparison, not the change. CORRECTED HERE. (2) THE WHITELIST ENTRY COSTS +683 allocations / +31,308 bytes — CONFIRMED at rungs 0, 2, 3, 4 and 5. ⚠ RUNG 1 READS +699 / +35,724. The row below measured only rungs 0 and 5 and could not see it. It is NOT a growth term: rung 5 is a 32x program and still reads exactly +683. It is a container capacity-doubling boundary that the extra 683 allocations happen to push rung 1 across, worth +16 allocations once, and it does not recur. (3) THE PER-ENTRY TERM IS 16 ALLOCATIONS / 925 BYTES, isolated by listing the SAME module 49 times — the Set dedups, so the LOADED set is byte-identical and ONLY the list construction varies: +752 allocations for 47 extra entries, 16.0 each to three digits, flat at all six rungs. So the filter is LINEAR in its own length with no quadratic term left in it, and the +683 decomposes as 16 of entry plus 667 of Sleep.maxon's own lex+parse+signature+merge+lower. A second entry is therefore near-free beyond the module it names, exactly as designed. (4) ⭐ THE FORWARDING FRAME — the systematic price of this whole retirement plan, since every stdlib leaf that forwards to a `__Builtins.*` intrinsic now costs a real call frame. MEASURED at 86 BYTES OF x64 CODE, ONCE PER RETIRED BUILTIN AND NOT PER CALL SITE: the same 86 whether the program is 233 bytes (6,056 -> 6,142) or 1.4 MB (897,253 -> 897,339). Of those 86, only ~34 are the 7-op frame the goldens show; the other ~52 are the GT STACK GUARD `emitFunctionChunk` emits at the byte level for every non-runtime function of a GT module, which is invisible to the Target-IR goldens. A NON-GT forwarder (the shape `print`/`trunc`/the four math intrinsics will take) costs only the ~34: measured 32 and 37 bytes for two successive forwarders in a trunc program. So retiring the sixteen remaining stand-ins buys a ONE-OFF ~0.5-1.4 KB of code, not a per-call-site cost. Compile-time price on a realistic sleeping program (rung-4 corpus + one `sleep(0)`): base 8,011,309 allocations -> 8,012,648, i.e. +1,339, of which +683 is the whitelist entry every compile pays anyway and ~656 is the extra function crossing the back end. (5) ⭐⭐ SHV2 HAS NO INLINER, so the frame is PERMANENT — confirmed at PLAN.md:493 ("there is no LICM/CSE/inliner") and ARCHITECTURE.md:572, where DCE/CSE/LICM and the inliner are called "scheduled, not present"; the pipeline is resolveTypes -> semanticCheck -> lowerMaxonToStd -> pruneDeadBlockArgs -> elimTrivialBlockArgs -> foldConstOperands and nothing in it hoists or inlines. WHAT AN INLINER WOULD BE WORTH, MEASURED rather than argued: a 200,000,000-iteration loop whose body is one `trunc` runs 282 ms called directly and 536 ms through a one-line forwarder, three runs each on an idle box, steady state — +254 ms / 200M = ~1.27 ns, ~4-5 cycles per call, and a 1.90x slowdown on that loop. IRRELEVANT FOR `sleep` (a scheduler park dwarfs it) and irrelevant for `print`/`runProcess` (a syscall does), but it IS the whole cost for `trunc` and the four math intrinsics, which are single instructions behind the frame. ⇒ Retire the syscall-shaped stand-ins freely; the four math ones and `trunc` are the ones that want either an inliner or — far cheaper, and named here so the choice is deliberate — a TAIL-CALL for a body that is one forwarding call, which turns the whole 7-op frame into a single `jmp`. NEITHER WAS BUILT; this is a price, not a change. (6) ⭐ THE WHITELIST-DELETED WORLD, EXTRAPOLATED FROM MEASUREMENT, NOT FROM ARITHMETIC. Loading stdlib is LINEAR IN THE MODULE COUNT — measured, not assumed: 20 extra probe modules cost 17,505 allocations and 40 cost 34,435, a ratio of x1.967 per DOUBLING of the file count, with per-module cost FALLING slightly (875 -> 861) as the fixed terms amortize. Flat across all six rungs in both arms, so it is a per-compile CONSTANT and not a growth term in the user program either, and codeBytes was equal to the base arm at every rung in every probe arm — DFE prunes all of it. The right unit is the FUNCTION, not the byte: per-function cost measured 489 (Clock, 5 functions), 667 (Sleep, 1) and 696 (probes, 4,800), while per-byte rates disagree by 7x because real stdlib is comment-dense at 925 bytes/function against the probes' 163. Real `stdlib/` is 49 files, 642,703 bytes, 695 function declarations ⇒ THE FULLY-LOADED STDLIB WILL COST ROUGHLY 350,000-500,000 ALLOCATIONS AND ~25 MB ON EVERY COMPILE, as a FLAT FLOOR. That is about 60-80% of an entire rung-0 compile (605,876 allocations / 35.5 MB) and about 2-3% of a rung-5 one. ⚠ IT WILL NEVER APPEAR AS A BEND IN ANY CURVE — it is a floor, not a slope, which is exactly why it is written down now: the instrument this log is fed by cannot flag it. Deleting the filter also RETURNS the 49x16 = ~784 allocations of list construction, which is 0.2% of what it admits. NOT A DEFECT AND NOT DEBT: nothing here is superlinear, every term above was measured linear, and the floor is a design consequence of parsing all of stdlib on every compile that only a lazy/cached stdlib artifact would remove — a rung of its own, not an optimization pass. GATES on this exact tree: build exit 0 zero warnings; x64 spec-test 1587/0 exit 0; wasm32-wasi spec-test 1396/0 exit 0 (the row below says 1395 — CORRECTED, measured twice); no exit 101; `git status` empty after both suites, so codegen is byte-identical. | 605,876 | 1,097,633 | 2,080,793 | 4,045,811 | 7,976,949 | 15,842,025 |
| 2026-07-25 | P1.7a DIRECT PRIMITIVE DISPATCH — OPTIMIZER PASS. THE RUNG'S PER-CALL-SITE COST IS GONE; WHAT IS LEFT IS A FLAT PER-COMPILE CONSTANT. Numbers on this row are the OPTIMIZED compiler; the rung as merged read +62/+94/+158/+286/+542/+1,054 allocs and the base read 0 (this same tree, same path, one binary rebuild per point — the base rebuild reproduced the CORPUS CHANGED row's allocations EXACTLY at all six rungs, with a flat +546 bytes that is pure path length, which is what makes the byte column below attributable). (1) THE TWO-TERM FIT IS CONFIRMED AND THEN REFUTED AS "STRUCTURAL". The rung's numbers fit `30 + 32*2^n` allocs and `1410 + 512*2^n` bytes EXACTLY (differences 32/64/128/256/512 and 512/1024/2048/4096/8192; 512/32 = 16 bytes per allocation). But the doubling half was NOT the feature's own dispatch gate in any load-bearing sense: it was ONE 16-BYTE HEAP BOX PER METHOD-CALL SITE, and the sites paying it were STRUCT method calls in a corpus that contains ZERO direct dispatches. Localized: the whole doubling term sits in phase:parse (+32/+64/+128/+256/+512/+1,024 allocs, +512..+16,384 bytes) and in NO other phase at any rung. Attributed by a knob experiment rather than by arithmetic: 64 extra `p.bump(acc)` sites cost +64 parse allocs / +1,024 parse bytes, while 64 extra array-method (`arr.count()`), String-method (`s.byteLength()`), struct-FIELD-read (`p.x`), ranged-alias-argument and plain-call sites each cost EXACTLY 0 — so the term moves only with the sites that fall past the String/Array/Set arms into the new classifier, and nothing else. CAUSE: `BuiltinConformerLookup` was a union carrying the conformer's NAME (`conformer(typeName ByteArray)`), and a union with an associated value is heap-boxed, so classifying a receiver allocated a box even to answer "no conformer at all". CURE: the classifier answers with a PAYLOAD-FREE enum (`BuiltinConformer`, a scalar), and the name is fetched by `builtinConformerName` only once a dispatch is certain to be emitted — an EXHAUSTIVE match, so a conformer added to the tag map fails to compile until it is given a name, which is a stronger check than the union shape had. `Self`-argument agreement compares CONFORMERS instead of names for the same reason. RESULT: every phase's delta is now 0 at every rung, and the rung's whole footprint is the flat term below. (2) THE INSTALL IS O(ops + 5*F) ONCE PER COMPILE AND ALLOCATION-FLAT — AUDITED AND MEASURED. `referencedBuiltinImpls` walks pendingRdataRelocs once and the flat `module.ops` array once (<=5 length-failing byte compares per call/tryCall/funcAddr op); `undefinedImplNames` walks `module.functions` once per candidate, 5 candidates, fixed. Not O(ops x impls), not per function. The proof it has no growing term is that the residual +30 allocs / +1,410 bytes is IDENTICAL at rung 0 and rung 5 across a 32x ladder — so `module.getFunc(i)` and `module.ops.get(i)` allocate nothing, and the walk's cost is time only. It IS newly paid by EVERY compile, including the conformance-free ones (it replaced an allocation-free `pendingRdataRelocs.count() == 0` early-out), and it is kept deliberately: it is what makes REFERENCED <=> INSTALLED one predicate instead of two, and 30 allocations is 0.005% of the smallest rung and 0.0002% of the largest. (3) `isCompilerInternalCallee`'s new second clause IS allocation-free as intended: phase:semanticCheck and phase:lowerMaxonToStd — which ask it of EVERY callee in the program, via validateCall and lowerCall — read +0 allocs / +0 bytes at every rung, before and after this pass. (4) MEASURED DEBT, STRUCTURALLY INVISIBLE TO THIS INSTRUMENT: the direct-dispatch path itself never runs on this ladder (the corpus generates no `i.hash()`/`i.equals()`/`i.compare()`), so measured separately on generated programs of 64/128/256/512 dispatch sites. It is LINEAR — parse allocations grow x1.971 x1.981 x1.988 per doubling — but carries a per-site premium of ~12 allocations for `.hash()` and ~20 for `.equals()` over an ordinary call at the same site (29.41 vs 17.41, and 104.54 vs 84.59, allocations per site at N=256, against structurally matched controls). CAUSE: `builtinConformerMethod` resolves the method by asking `requireBuiltinInterface` for each candidate protocol, and that RE-SYNTHESIZES a whole `IrInterface` (its method array, both param arrays, the sig) on every call — `hash` builds 1 (Hashable hits first), `equals` builds 2 (Hashable is built and missed, then Equatable), `compare` 3, which is exactly the ~8-allocation step between hash and equals. Pre-existing pattern, not introduced here: the comparison-operator lowering calls `requireBuiltinInterface` per site too. NOT FIXED because it is measured linear and a fix means memoizing the synthesized protocols, whose blast radius is every `builtinInterface` caller. RE-MEASURE TRIGGER: a corpus knob that generates direct dispatches (which would make it visible here), or a fourth builtin-conformable protocol (every added protocol adds one interface build to every MISS). GATES: x64 1594/0 and wasm32-wasi 1398/0, both exit 0 (no 101), with specs-shv2/fragments COMPLETELY CLEAN (zero M) after BOTH runs — 1594 programs' Target IR byte-identical, which is the proof this pass is behaviour-neutral. The three diagnostics the refactor re-routes were probed directly and all name the conformer correctly: E3005 "'equals' requires a int, but its argument is String", E2015 "'int' has no method named 'toString'", E3036 "'compare' takes exactly 1 argument, but 0 were given". | 604,347 | 1,096,087 | 2,079,263 | 4,044,280 | 7,975,418 | 15,840,493 |
| 2026-07-25 | DUPLICATE TYPEALIAS IS A DIAGNOSTIC + FILE-SCOPED ALIAS RESOLUTION. `project.typeAliases` and `ProgramSignatures.typeAliases` were ONE whole-program map keyed by the bare name, so the LAST file merged won and its range silently replaced every other file's: `600 as Limit` compiled clean in a file whose own `Limit` is `int(0 to 500)` because another file declared a wider one, and a user's `typealias Milliseconds = int(0 to 100)` was overridden by stdlib/Sleep.maxon's. Both are now correct (E3005, matching the bootstrap oracle byte for byte), and a SAME-FILE duplicate is E3061 at the redeclaration's name token. COST, ATTRIBUTED AND LINEAR: +1,911/+2,044/+2,228/+2,645/+3,445/+5,062 allocs, which fits `flat + 5.9 x (typealias DECLARATIONS)` — the corpus declares 12 + 17*2^n aliases (29/46/80/148/284/556, an exact fit), and the rung-over-rung delta divided by the alias-count delta is 5.94/5.88/6.13 at the three largest rungs. The 5.9 is `fileScopedDeclKey` building one key per declaration (declFilePath.toString + String.from(name) + the interpolation + toByteArray ~= 4 objects, plus the second map entry), charged twice per compile because both the pre-parse signature sweep and the merge record every declaration. It is bounded by DECLARATIONS, never by USES. THE LOOKUP PATH ALLOCATES NOTHING, and that is measured rather than asserted: `RangedAliasRegistry.lookup` builds a key only when `multiplyDeclared` holds the name, and NO alias name in the generated corpus is declared in two files, so that set stays empty at every rung and the scoped probe never runs. Without that gate the key would be built once per named type reference — the parser's hot path — and the term would track USES instead of declarations. Share of total: 0.32% at rung 0, 0.032% at rung 5. Bytes +92,878..+224,483, only 2.4x across a 32x ladder, so dominated by the flat term. ONE AVAILABLE FOLLOW-UP, NOT TAKEN HERE: `fileScopedDeclKey` allocates ~4 objects where 2 would do (append bytes rather than interpolate Strings); it is the SHARED builder that top-level let/var resolution also uses (`declFor`), so making it cheaper improves both and belongs in its own measured pass rather than riding a diagnostics rung. | 606,258 | 1,098,131 | 2,081,491 | 4,046,925 | 7,978,863 | 15,845,555 |
| 2026-07-25 | OPTIMIZER PASS on the file-scoped-typealias rung. THE LOOKUP PATH'S COST WAS REAL AND THIS CORPUS IS STRUCTURALLY BLIND TO IT; it is now GONE, and the rung is allocation-NEGATIVE against its own base from rung 2 up. Deltas against the row below are the change itself (same worktree, same corpus): -324/-454/-682/-1,154/-2,066/-3,906 allocations, -14.6K to -142.2K bytes. (1) THE HEADLINE. The row below prices the lookup path at zero because `RangedAliasRegistry.lookup` built its composite key only when `multiplyDeclared` held the name, and NO alias name in the generated corpus is declared in two files — verified here by grepping `--emit-corpus`: all 169 ranged declarations at rung 5 have distinct names, so that set is empty at every rung and the branch never runs. THE GATE IS EXACTLY WHAT MAKES THE CORPUS BLIND. `stdlib/` is not so kind: SEVEN files declare `Byte` (Character, Console, File, helpers/string/views, Json, String, Subprocess), three declare `ByteCount`, nine names in all are multiply-declared, and `Byte` is one of the most-used aliases there is — so the cost arrives in full the day the whitelist is deleted. MEASURED OFF-CORPUS on a program where a TWO-FILE alias is USED N times with the use count scaled and everything else held identical (same file count, same declaration count, same source; the control's second file declares a DIFFERENT name, so the only difference is whether the name is contested, and both arms emit the same code): the rung cost 24 ALLOCATIONS AND 2,224 BYTES PER USE — +2,446 / +4,846 / +9,646 / +19,246 allocations at N = 100/200/400/800, exactly `46 + 24N`. Linear in uses, never superlinear, and invisible here. It is now FLAT: +33 allocations / +2,782 bytes at ALL FOUR of those N, identical to the byte — the per-use term is GONE, not reduced. CAUSE: `fileScopedDeclKey` builds a `path SEP name` composite per probe (String.from + interpolation + toByteArray, about 4 objects, plus O(path) copying twice). CURE: a two-level map, name -> (declaring-file path -> alias), populated ONLY for names more than one FILE declares, with the invariant "every declaration of the name or none". The probe that decides a name is contested IS the probe holding the answer, so there is no key to build and no cost gate to keep honest, and an uncontested name costs one short-name hash and falls through to the bare answer — what the whole-program map cost before file scoping existed. The inner map is keyed by the path STRING deliberately: `FilePath.hash` case-folds on Windows (canonicalForCompare clones and lower-cases), so `Map with (FilePath, V)` would allocate per probe, which is the cost being removed. (2) THE `5.9 x DECLARATIONS` FIT IS CORRECTED — right shape, wrong denominator, real constant 3.4x bigger. `12 + 17*2^n` (29/46/80/148/284/556) counts ALL typealiases; the generic-instantiation ones (`typealias X = Base with Arg`, 15 at rung 0 rising to 387 at rung 5) go to `genericAliases` and never reach this registry. It sees `9 + 5*2^n` RANGED declarations — 14/19/29/49/89/169 — plus 5 from the whitelisted `stdlib/Clock.maxon`. Controlled A/B in ONE worktree (base dfe427944 vs rung HEAD 45935eaaf, both binaries built here so the path-length constant cancels): +352/+468/+668/+1,084/+1,884/+3,500 allocations, slope 20.2 PER DECLARATION with a zero intercept, not 5.9. Now about 2.0 per declaration (+20/+25/+35/+55/+95/+175 in phase:signatures and the same again in phase:merge, where each was +96 to +740), because the FIRST declaration of a name — in a program where nothing is redeclared, every one of them — now costs one probe and one insert and builds nothing at all. (3) "BOUNDED BY DECLARATIONS, NEVER BY USES" WAS NOT TRUE, and this instrument's own phase table showed it: phase:resolveTypes gained `36 + 36 x 2^n` allocations (+72/+108/+180/+324/+612/+1,188), a term doubling with the PROGRAM. CAUSE, confirmed by removing it rather than by argument: `resolveNamedType` asks the registry once per named TYPE REFERENCE and the answer came back as `DeclaredAlias`, a union with an associated value, which is HEAP-BOXED — the identical defect the P1.7a pass fixed one rung earlier for `BuiltinConformerLookup`. CURE, a narrowing rather than a workaround: all FOUR bare-name readers (`resolveNamedType`, `canonicalTypeName`, `classifyUnionPayload`, `canonicalizeRangedAlias`) want `underlying`, the int/float keyword, and none of them may read the RANGE — a reader with no file would be reading a stranger's bounds. Each said so in a comment; the door now hands back a `MaxonType` so none of them CAN. phase:resolveTypes is back to +0 at every rung and phase:parse goes NEGATIVE (`-12 - 24 x 2^n`, -780 at rung 5) because `classifyUnionPayload` was already paying that box BEFORE this rung. Net rung cost vs base is now `42 - 14 x 2^n` allocations (+28 at rung 0, -406 at rung 5) and `1,904 + 16 x 2^n` bytes — essentially flat across a 32x ladder where it was +19,582 rising to +181,082. (4) MEASURED AND DELIBERATELY NOT CHANGED. `declFor` (top-level let/var resolution) builds a `fileScopedDeclKey` on EVERY probe, unconditionally, before it knows the name is even file-private — the same 4 allocations, pre-existing. Left alone because `declIndex` is ONE flat map shared with `topLevelStorageKey`'s writes, so giving it this treatment changes that map's shape and its writer, not alias resolution; re-measure if a pass starts asking it per OP rather than per identifier. `fileScopedDeclKey` is no longer exported — the alias registry was its only cross-file caller. Also corrected: the bare probe is NOT off the hot path as the row below implies; `resolveNamedType` is its hottest caller, at once per named type reference. GATES on this exact tree: build exit 0 with zero warning lines; x64 spec-test 1602/0 exit 0; wasm32-wasi spec-test 1411/0 exit 0; no exit 101; specs-shv2/fragments ZERO M after BOTH suites, so 1,602 programs' Target IR is byte-identical and this pass is behaviour-neutral; codeBytes equal to the BASE compiler's at rungs 0/3/5 (62,457 / 435,554 / 1,716,126). | 605,934 | 1,097,677 | 2,080,809 | 4,045,771 | 7,976,797 | 15,841,649 |
| 2026-07-25 | REVIEW PASS on the file-scoped-typealias rung — E3105 added, and it costs NOTHING: every rung matches the optimizer's row above EXACTLY (delta 0 allocations, 0 bytes, at all six), and codeBytes is unchanged (62,457 / 115,686 / 222,294 / 435,554 / 862,198 / 1,716,126). That is the expected reading and it is worth having: the new check is one `maxonTypeTag` comparison on a name some file has ALREADY declared — never on a first declaration, which is every declaration in nearly every program and in every rung of this corpus, where all 169 ranged names at rung 5 are distinct. So the corpus is structurally blind to the check for the same reason it was blind to the lookup cost, and a delta of exactly 0 is what a correctly-gated check looks like here rather than evidence it does nothing. WHAT IT FIXES IS A WRONG ANSWER, NOT A COST. The rung scoped the PARSER's alias lookup by declaring file while `resolveNamedType` / `canonicalTypeName` / `classifyUnionPayload` / `canonicalizeRangedAlias` kept resolving the bare name last-wins. Two deciders, and nothing made them agree: on base dfe427944 both were bare and agreed, so a user program declaring `Measure` as `int` in one file and `float` in another COMPILED; on the rung the parser said `int` inside the int file while type resolution said `float`, and the disagreement reached the backends. MEASURED, not argued: x64 died with `panic at X64Backend.maxon:1367: xmm0 is in the xmm register file where the gpr file is required` (an ICE, no diagnostic); wasm emitted a component its own validator rejected with `type mismatch: expected i32, found f64`; and a struct field typed by the alias compiled silently to the wrong width where base main had rejected the same program with a clean E3009. The cure makes the bare door's contract STRUCTURAL rather than a bet about today's corpus — `record` refuses a cross-file pair whose underlying primitives differ, so every declaration of a name shares one primitive in any program that compiles and the bare answer IS the answer a scoped lookup would give. Two files over two RANGES stays legal and is untouched (seven stdlib files declare `Byte`; both ported `duplicate-typealias` tests and the new same-underlying guard pass). GATES on this exact tree after the fix: build exit 0 zero warnings, 138 codes; x64 spec-test 1604/0 exit 0; wasm32-wasi spec-test 1413/0 exit 0 (1602/1411 plus the two new tests); no exit 101; specs-shv2/fragments ZERO M after BOTH suites. | 605,934 | 1,097,677 | 2,080,809 | 4,045,771 | 7,976,797 | 15,841,649 |
| 2026-07-25 | P1.7a 2b-iii (float + bool conformance) — OPTIMIZER PASS. (1) THE IMPLEMENTER'S MEASUREMENT IS CONFIRMED EXACTLY, and its caveat about the log was right: the `previous` row this instrument compares against is the DIRECT-DISPATCH OPTIMIZER row (27de41e16), TWO commits before this rung's base (dfe427944), so the printed delta of +1,571..+1,574 belongs to the two commits in between and not to this rung. Rebuilt the base binary in THIS worktree at THIS path (path length is a per-compile constant — the previous optimizer priced 546 bytes of it) and A/B'd: +12 allocations / +592 bytes, IDENTICAL AT ALL SIX RUNGS across the 32x ladder. EVERY per-phase delta is 0 at every rung, which is not a rounding artefact: `installBuiltinConformanceRuntime` is called from Compiler.maxon:328, in the UNPHASED tail after the pipeline, so its cost lands in the total and in no phase bucket. (2) ATTRIBUTED BY A KNOB, NOT BY ARITHMETIC. Replacing exactly the three new `qualifiedMethodName` joins with an already-built ByteArray — keeping the 8-entry candidate array, the three extra `referenced.contains` tests, the three new builders and everything else the rung added — returns the numbers to base EXACTLY: +0 allocations / +0 bytes at both rungs measured. So the whole +12/+592 is three name joins at 4 allocations and ~197 bytes each, and three things the rung also added are measured FREE: the candidate list widening 5 -> 8 (walked per pending rdata reloc and per module op) costs ZERO allocations; `isBuiltinConformanceImplName` widening from one qualifier test to three (asked of EVERY callee by validateCall and lowerCall) costs ZERO; and `MmRuntime.emitCmp`'s new defaulted `operandType StdType = StdType.i64` costs ZERO at all 79 call sites that take the default (84 emitCmp sites, 5 explicit). That last one was cross-checked against the BOOTSTRAP's lowering, which is what compiles these sites: a defaulted enum argument is one constant into the argument register — the defaulted call emits `xor edx, edx` where the explicit call emits `mov rdx, 1` — no box, and the enum's struct RAW VALUE is never materialized. (3) ONE CHANGE SHIPPED, worth +2 allocations per INSTALLED impl. `buildBuiltinEquals`/`buildOrderingCompare`/`buildFloatCompare` each built the parameter-type array TWICE — once for `createFromStd`'s declared signature, once for `emitTypedParams` — where the base built it once and used an allocation-free `emitParams(count: 2)`. `emitTypedParams` now reads `func.paramTypes` (which `createFromStd` stores BY REFERENCE), so the `param` ops emitted ARE the types declared: one fact instead of two derivations free to disagree the day an operand type maps to a param shape conditionally. MEASURED off-corpus, because the scale corpus installs no impl at all: a program that installs `int.equals` or `int.compare` read +14 against base and now reads +12, the same flat constant every other program pays; `int.hash`, whose builder was untouched, read +12 throughout. On this ladder the change is a no-op, which is why the row's numbers are the rung's unchanged +12/+592. (4) RUNG 1'S FILED DEBT RE-MEASURED OFF-CORPUS — IT DID NOT GROW, AND THE INT HALF IS BYTE-IDENTICAL. `builtinConformerMethod` still re-synthesizes a whole `IrInterface` per candidate protocol per dispatch site. Generated ladders of 64/128/256/512 dispatch sites against structurally matched controls (a struct instance-method call `box.compareTo(b)` — receiver plus one positional argument, the same parseMethodCall shape, falling past the conformer classifier into the struct arm), marginal phase:parse allocations per site taken as the slope between N=256 and N=512: int.hash +7.00, int.equals +16.00, int.compare +23.00, float.equals +7.00, float.compare +14.00, bool.compare +3.00. LINEAR in every arm (phase:parse x1.90-x1.96 per doubling). The premium tracks the number of interface BUILDS exactly as the debt predicts, and the RAGGED conformance rows make the NEW conformers CHEAPER than int rather than dearer: a protocol `isIntrinsicBuiltinConformance` withholds is `continue`d before `requireBuiltinInterface` is reached, so float.compare builds 2 (Equatable built and missed, Comparable hit) where int.compare builds 3, and bool.compare builds 1. A/B against the base binary on the same ladder: phase:parse is IDENTICAL TO THE BYTE at every N for every int variant (+0), so the two `typeName.equals` arms added to `isIntrinsicBuiltinConformance` cost nothing on the dispatch path. Re-measure trigger unchanged: a corpus knob that generates direct dispatches, or a fourth builtin-conformable protocol. (5) A REAL QUADRATIC FOUND — `regalloc:splitting` GOES QUADRATIC WHENEVER A FLOAT IS LIVE ACROSS CALLS. It is PRE-EXISTING and NOT this rung's, and it is NOT the known liveness-recompute debt. Same generated program, only the declared type of two locals changed: with FLOAT locals `regalloc:splitting` allocations run 1,561,997 -> 6,628,499 -> 28,064,380 at N=128/256/512 (x4.24, x4.23) while lex/parse/merge/isel of that same compile all sit at x1.9; with INT locals the same bucket is 1,762 -> 2,660 -> 4,454 (x1.51, x1.67). At N=512 that is 6,300x. `regalloc:liveness` beside it reads x1.86/x1.92 — LINEAR — so the whole-function-rebuild-per-split debt is not what this is. Emitted CODE grows x1.84/x1.91, also linear, so the SPLIT COUNT is linear and it is the PER-SPLIT repair that is proportional to the whole function: the victim is a value live across the entire body, so its dirty region IS the body, and Theta(N) splits each dirtying Theta(N) is the curve. The trigger is structural and already named in the source: shv2's x64 pool has NO callee-saved XMM (X64Backend.maxon:653 — Win64 preserves all 128 bits of xmm6-15 and `movsd` cannot save that, "a Wave 2 problem rather than a free win"), so a float live across a call is forbidden EVERY XMM (SplitLiveRanges' PressurePeak.witnessClass documents exactly this empty-witness case) and every call forces a store/reload bracket. PROVEN PRE-EXISTING by A/B: the base binary reproduces the identical curve — the float control's total is 28,598,570 on base against 28,598,582 here at N=512, the same flat +12 — and that control program contains NO builtin-conformance dispatch of any kind. NOT FIXED HERE, for a gate reason rather than an effort one: the structural cure is making xmm6-15 callee-saved, which CHANGES CODEGEN for every float program, and this pass's non-negotiable gate is byte-identical Target IR (fragments zero M). That makes it a rung, not an optimizer edit, and it sits in files this pass does not own. Handed to the coordinator as a DEFECT TO RUN DOWN, deliberately not filed as measured-linear debt, because it is measured QUADRATIC. This instrument structurally cannot see it: `regalloc:splitting` on the corpus is x1.71/1.85/1.93/1.98/2.00, linear, because the corpus generates no float held live across a run of calls — the same blind spot that hides the rung-1 dispatch debt, in a different phase. GATES: x64 1606/0 at --workers=12 and again at --workers=1, wasm32-wasi 1415/0, all exit 0 (no 101), with specs-shv2/fragments COMPLETELY CLEAN (zero M) after BOTH suites — 1606 programs' Target IR byte-identical, which is the proof the shipped change is behaviour-neutral. The DERIVED reject list was probed on all three conformers and each is right where the deleted fixed string would have lied: float is told it supplies `equals`, `compare` (no hash), bool `compare` alone, int `hash`, `equals`, `compare`. | 605,918 | 1,097,675 | 2,080,835 | 4,045,853 | 7,976,991 | 15,842,067 |
| 2026-07-25 | UNDECLARED-CAST rung — `5 as CompletelyMadeUpNameXyz` compiled clean and returned 5, and so did `5 as Point` and `5 as Scor`. `parseTypeReference` answers a few type spellings syntactically and hands back a bare `MaxonType.named` for everything else — a declared `type` named from outside its own body, a declared enum/union, a ranged typealias, a qualified inner alias, and a name nothing declares at all — and `maxonTypeTag` reads all five as ONE tag, so `applyCast`'s legality gate was asking about a NAME and its answer for a name denoting nothing matched its answer for a ranged int alias: convert nothing, flow the value on. The cure is NOT a second cascade in the parser: `TypeResolution`'s one list of sources became `denotedNamedType`, raising a payload-free `UnknownTypeName` instead of reporting, so each of its THREE readers picks the anchor — `resolveNamedType` reports E3011 unpositioned as before, `Parser.castTargetTag` reports it at the `as`, and `Parser.requireConstCastTargetKind` covers the SECOND cast site (a top-level `let X = 5 as T`, folded by the const evaluator, which never reaches the expression path). Falling out of the same lookup: `5 as Point` is now E3009 "Cannot cast from int to struct", the message `Self` and the type's own name already gave, and the one `applyCast`'s header claimed for `n as SomeStructAlias` while being false for the spelling users write. THE NUMBERS: allocations delta EXACTLY 0 at all six rungs, and `codeBytes` unchanged at all six (62,457 / 115,686 / 222,294 / 435,554 / 862,198 / 1,716,126) — codegen is byte-neutral, which a diagnostic-only rung owes. Bytes moved +408, FLAT: identical at rung 0 and rung 5 across a 32x input span, with zero additional allocations, so nothing algorithmic changed and there is no growing term. ATTRIBUTION IS AN INFERENCE CONSISTENT WITH THAT FLATNESS, NOT A MEASURED FACT: `resolveTypes` now fetches `queryProgramSignatures` ONCE for the whole pass, where it was previously fetched inside `resolveNamedType` on the rare dotted-name arm only — this corpus writes no qualified names, so the fetch count went 0 -> 1 per compile, which is exactly the shape of a flat constant. On a program that uses qualified aliases it is strictly FEWER fetches, not more. A SECOND CHANGE THE CORPUS IS STRUCTURALLY BLIND TO: the cascade now reads the whole-program signature index rather than the `Project` registries, which is the only way the parser can ask it at all (the pure-parser invariant). Both hold the same declarations through the same doors — `upsertDeclaredEnum` is the single write of BOTH enum registries and `createEnumRegistry` seeds both — and the parser has ALREADY been deciding struct-ness from the index for every `named` parameter and return type (`resolveNamedStruct`), so this makes resolution agree with the parser rather than merely with itself. MEASURED, not argued, on the corpus this instrument compiles: no rung's allocation count moved. SPEC NOTE: `ranged-typealias.md`'s ported `cast-to-stdlib-internal-typealias` was green because its `ElementCount` lives in `stdlib/Array.maxon`, which the loader whitelist does not list — the name resolved to nothing, the cast evaporated, and the exit code came out right anyway. It is now disabled naming that blocker, the file staying a byte-identical port of `/specs`, and the property is re-tested for the RIGHT reason in the new `cast-target-type-resolution.md` against `Milliseconds` (`stdlib/Sleep.maxon`, non-exported and whitelisted) paired with `5 as Millisecond`. That pair is the whole discriminator and it has to be: MEASURED — with a runtime operand `x as Milliseconds` and `x as NoSuchName` emit BYTE-IDENTICAL Target IR, because all six whitelisted stdlib aliases are `int(0 to u64.max)`, a range that admits every 64-bit value and emits no guard. GATES on this exact tree: build exit 0 zero warnings, 138 codes; x64 spec-test 1610/0 exit 0; wasm32-wasi spec-test 1419/0 exit 0; `--workers=1` byte-identical to `--workers=12`; no exit 101; specs-shv2/fragments zero M after both suites. | 605,934 | 1,097,677 | 2,080,809 | 4,045,771 | 7,976,797 | 15,841,649 |
| 2026-07-25 | UNDECLARED-CAST rung — `5 as CompletelyMadeUpNameXyz` compiled clean and returned 5, and so did `5 as Point` and `5 as Scor`. `parseTypeReference` answers a few type spellings syntactically and hands back a bare `MaxonType.named` for everything else — a declared `type` named from outside its own body, a declared enum/union, a ranged typealias, a qualified inner alias, and a name nothing declares at all — and `maxonTypeTag` reads all five as ONE tag, so `applyCast`'s legality gate was asking about a NAME and its answer for a name denoting nothing matched its answer for a ranged int alias: convert nothing, flow the value on. The cure is NOT a second cascade in the parser: `TypeResolution`'s one list of sources became `denotedNamedType`, raising a payload-free `UnknownTypeName` instead of reporting, so each of its THREE readers picks the anchor — `resolveNamedType` reports E3011 unpositioned as before, `Parser.castTargetTag` reports it at the `as`, and `Parser.requireConstCastTargetKind` covers the SECOND cast site (a top-level `let X = 5 as T`, folded by the const evaluator, which never reaches the expression path). Falling out of the same lookup: `5 as Point` is now E3009 "Cannot cast from int to struct", the message `Self` and the type's own name already gave, and the one `applyCast`'s header claimed for `n as SomeStructAlias` while being false for the spelling users write. THE NUMBERS, RE-MEASURED BY THE OPTIMIZER AGAINST A BASE BUILT IN THIS SAME WORKTREE: the rung is EXACTLY memory-neutral — 0 allocations AND 0 bytes delta at all six rungs, and `codeBytes` unchanged at all six (62,457 / 115,686 / 222,294 / 435,554 / 862,198 / 1,716,126), so codegen is byte-neutral, which a diagnostic-only rung owes. ⚠ THE `+408 BYTES FLAT` THIS ROW ORIGINALLY RECORDED IS NOT A COMPILER COST, AND THE INFERENCE IT CARRIED (the hoisted `queryProgramSignatures` fetch) IS WITHDRAWN — IT IS THE LENGTH OF THE CHECKOUT PATH. Measured, not argued: base a2cc7c823 rebuilt and run at THIS worktree's path reproduces this row's bytes exactly at all six rungs, so base-minus-head is 0 in every column; the SAME base sources built and run in a second worktree whose path is 14 characters shorter read 2,856 bytes LOWER at every rung, FLAT, with allocations and `codeBytes` identical TO THE UNIT. That is 204 bytes per path character, and 408 / 204 = exactly 2 characters — the previous row was measured in a worktree named 2 characters shorter, and subtracting 408 from every rung of this row lands on it to the byte (35,550,817 / 67,277,183 / 134,705,957 / 271,420,664 / 553,012,768 / 1,129,518,324). ⭐ THE DURABLE FACT FOR EVERY FUTURE ROW: the corpus root `.scale-tmp` is relative to the WORKING DIRECTORY, so the BYTES column carries the checkout path and is NOT comparable across worktrees, while the ALLOCATIONS column is (it did not move by one at any path length). Compare bytes only within one path, and read a small FLAT bytes delta across a rebased or re-worktree'd rung as the path until proven otherwise. WHAT THE HOT PATH ACTUALLY COSTS — measured on purpose-built probes, because the ladder cannot separate these: a PLAIN named type reference costs 0 allocations. 20,000 of them in signature position allocate 16,029 in `resolveTypes`, which is 4 per FUNCTION (the `MaxonTypeArray` and the `FuncSignature` re-sync) and 0 per REFERENCE, identical under base and head — so the payload-free `UnknownTypeName` is MEASURED rather than asserted: making `denotedNamedType` throwing added no per-call box. The QUALIFIED-alias win is real and small: 8,000 `StrWrapper.Idx` references resolve 1.0 ms faster (29.8 -> 28.8 ms, median of 7 on an idle box), which is -125 ns per qualified reference and -3.4% of the phase, at 0 allocations and 0 bytes — the per-reference `queryProgramSignatures` hit it removes is O(1) (`compositeSourceHash` carries its own dirty flag), so it never had bytes to give back. ⚠ MEASURED DEBT, PRE-EXISTING AND NOT THIS RUNG'S: the qualified arm ITSELF costs 18 allocations and 700 bytes PER REFERENCE (36,038 allocs / 1,400,968 bytes at 1,000 functions, doubling exactly to 144,038 / 5,600,968 at 4,000) — `splitQualifiedName` and `joinQualifiedName` rebuild the `Base.Member` key per reference and `DeclaredAlias` boxes on both the miss and the hit. It is MEASURED LINEAR (x2.00 per doubling at three sizes), so it is debt and not a defect; the trigger that would make it bend is generic-heavy code once P1.7 Array lands. A SECOND CHANGE THE CORPUS IS STRUCTURALLY BLIND TO: the cascade now reads the whole-program signature index rather than the `Project` registries, which is the only way the parser can ask it at all (the pure-parser invariant). That REDUCES the decider count rather than adding one — the parser was ALREADY deciding struct-ness from the index for every `named` parameter and return type (`resolveNamedStruct`), so resolution now agrees with the parser instead of merely with itself, and `upsertDeclaredEnum` is the single write of BOTH enum registries with `createEnumRegistry` seeding both. Probed directly rather than trusted: a struct, an enum and a ranged typealias declared in one file and named from a second file's signatures all resolve (exit 7), so the index is not missing what the registries hold. SPEC NOTE: `ranged-typealias.md`'s ported `cast-to-stdlib-internal-typealias` was green because its `ElementCount` lives in `stdlib/Array.maxon`, which the loader whitelist does not list — the name resolved to nothing, the cast evaporated, and the exit code came out right anyway. It is now disabled naming that blocker, the file staying a byte-identical port of `/specs`, and the property is re-tested for the RIGHT reason in the new `cast-target-type-resolution.md` against `Milliseconds` (`stdlib/Sleep.maxon`, non-exported and whitelisted) paired with `5 as Millisecond`. That pair is the whole discriminator and it has to be: MEASURED — with a runtime operand `x as Milliseconds` and `x as NoSuchName` emit BYTE-IDENTICAL Target IR, because all six whitelisted stdlib aliases are `int(0 to u64.max)`, a range that admits every 64-bit value and emits no guard. GATES on this exact tree: build exit 0 zero warnings, 138 codes; x64 spec-test 1610/0 exit 0; wasm32-wasi spec-test 1419/0 exit 0; `--workers=1` byte-identical to `--workers=12`; no exit 101; specs-shv2/fragments zero M after both suites. OPTIMIZER PASS: NO COMPILER CHANGE SHIPPED — nothing in the rung is superlinear (`resolveTypes` allocations are 607/927/1,567/2,847/5,407/10,527, an exact x2 per doubling, and `Map.contains` is an open-addressing probe with a cached hash, so the cascade is O(name length) per reference and not O(declarations)), and the one constant-factor term worth naming is filed above as measured debt rather than chased. Gates re-run by the optimizer on this same tree: build exit 0, x64 1610/0 exit 0, wasm32-wasi 1419/0 exit 0, `specs-shv2/fragments` and the whole working tree clean after both suites. | 605,934 | 1,097,677 | 2,080,809 | 4,045,771 | 7,976,797 | 15,841,649 |
| 2026-07-25 | TYPE-NAME COLLISION rung — `type Box` plus `typealias Box` compiled CLEAN with the alias discarded (exit 7), and the same pair the other way round said only `E3009: Cannot cast from int to struct`. Five keywords file a type name in a whole-program map keyed by the BARE name and nothing checked for a second claimant, so a FIXED cascade (`denotedNamedType`: struct, then enum, then alias) picked the winner instead of the author. Six pairs measured silent or misdiagnosed, including cross-file, where it is not merely order-dependent but unconditional: a `type Box` in ANY file beat a `typealias Box` in the reader's OWN file, because only the ranged-alias registry is file-scoped. THE RULE: a type name is declared exactly once, with ONE exception — two `typealias` of the SAME FORM in DIFFERENT files. The carve-out is per alias FORM and not per keyword because `stdlib/` needs both halves at once: SEVEN files declare `Byte = int(0 to u8.max)` (ranged) and FIVE declare `ByteArray = Array with Byte` (generic-instance), so a ranged-only rule makes all of `stdlib/` unloadable; and it must NOT span two forms, measured — a ranged `Handler` in a.maxon had its own `5 as Handler` rejected with "Cannot cast from int to function" against b.maxon's function alias. ONE DOOR: `TypeNameRegistry` is the sole decider of "is this name already declared", fed by one ORDERED per-file array filled at `dispatchTopLevel` (the only site that sees all five keywords, which is what makes the order the file's SOURCE order across kinds and lets the blame land on the LATER declaration); `RangedAliasRegistry.record` loses its `redeclaredInSameFile` arm and keeps only E3105, its own fact. THE NUMBERS, A/B'd IN THIS ONE WORKTREE at one checkout path (base 85281a39a rebuilt here, then head): `codeBytes` IDENTICAL at all six rungs (62,457 / 115,686 / 222,294 / 435,554 / 862,198 / 1,716,126) — codegen byte-neutral, which a diagnostic-only rung owes. Allocations +373 / +561 / +924 / +1,637 / +3,050 / +5,863 and bytes +21,472 / +37,432 / +69,192 / +132,552 / +259,112 / +510,536. That is LINEAR and it is linear in the right variable: solving the two-term fit against the emitted corpus (79 type declarations over 30 files at rung 0; 1,598 over 185 at rung 5) gives 3.2 allocations per TYPE DECLARATION and 4.0 per FILE, so the term grows with DECLARATIONS and never with the program's op count — +5,863 is 0.037% of rung 5's 15,847,524. The three-per-declaration are the `TypeNameDecl` box, the ordered array's amortized growth and the registry map's entry; the per-file four are the artifact's own `TypeNameDeclArray`. ALSO FIXED, and it is why the headline case still failed once the check existed: a `ParseError` discards the whole artifact, so a file that died on the collision's OWN CONSEQUENCE never reported the collision — `Parser.abortedParseArtifact` carries exactly the type-name declarations across an abort (each a FINISHED record, so there is no half-built state to salvage), and both diagnostics now appear with the E3009 first because the parser runs before the merge. NOT THIS RUNG's, PRE-EXISTING, REPRODUCED ON THE BASE BINARY IN THIS WORKTREE: `verify-warm-rebuild maxon-shv2/track0` panics in `ProgramSignatures.baseLayoutOf` — "generic instance 1's base struct 'Promise' is not declared" — identically at 85281a39a and at head. GATES: shv2 build exit 0 zero warnings; x64 1631/0; wasm32-wasi 1440/0; --workers=1 byte-identical to --workers=12; no exit 101; fragments additions-only (zero M) and `specs/` clean; csharp build exit 0 and its suite 3103/0. | 606,319 | 1,098,250 | 2,081,745 | 4,047,420 | 7,979,859 | 15,847,524 |
| 2026-07-25 | TYPE-NAME COLLISION rung — OPTIMIZER PASS. (1) THE RUNG'S OWN NUMBERS ARE CONFIRMED, RE-MEASURED A/B IN THIS ONE WORKTREE AT ONE CHECKOUT PATH: base 85281a39a checked out over this tree, both compilers rebuilt, measured, then head restored and rebuilt. Head-minus-base is +373 / +561 / +924 / +1,637 / +3,050 / +5,863 allocations and +21,472 / +37,432 / +69,192 / +132,552 / +259,112 / +510,536 bytes — EXACTLY the figures the implementer reported, to the unit, at all six rungs — and `codeBytes` is identical base-to-head at all six (62,457 / 115,686 / 222,294 / 435,554 / 862,198 / 1,716,126), so codegen is byte-neutral. The two-term fit is confirmed against the EMITTED corpus, counted rather than taken on trust: 79 type declarations over 30 files at rung 0, 1,598 over 185 at rung 5, giving 3.21 allocations per type DECLARATION and 3.99 per FILE, a model that predicts all six deltas within 2%. The decisive reading is that the new term's SHARE of the program SHRINKS monotonically as the ladder doubles — 6.15e-4, 5.11e-4, 4.44e-4, 4.04e-4, 3.82e-4, 3.70e-4 — which is what "linear in declarations, never in op count" looks like from the outside. (2) ONE CHANGE SHIPPED, AND IT IS DEAD WORK REMOVED, NOT A CONSTANT FACTOR SHAVED: `FileParseArtifact.create` built the new `TypeNameDeclArray` EAGERLY, but only `recordTypeName` (reached from `dispatchTopLevel`) ever pushes one, and `dispatchTopLevel` runs on the REAL parse alone — so TWO of the three `Parser` construction sites could never fill it. The tolerant declaration sweep (`foldDeclaredSignaturesInto`, one Parser per file) walks tokens itself, and `walkConstInitializer` (one Parser per top-level constant) evaluates an expression; both were billed one allocation apiece for a structure with no writer. `typeNameDecls` now points at a shared empty singleton and copies on write at the first push, exactly as `interfaces`/`conformances` have since P1.7a — the pay-for-use discipline this same file already documents, applied to the field the rung added. MEASURED, and the attribution is exact to the unit: `phase:signatures` head-minus-base is now 0 AT ALL SIX RUNGS (it was +96 / +165 / +303 / +579 / +1,131 / +2,235, which is precisely files + top-level constants: 30+64 at rung 0, 185+2,048 at rung 5), and `phase:parse` drops by exactly 17 at rung 0 and exactly 141 at rung 5 — the counts of corpus files that declare NO type at all (30 files, 13 with declarations; 185 files, 44 with declarations). Total saving −113 / −186 / −332 / −624 / −1,208 / −2,376 allocations and −4,520 / −7,440 / −13,280 / −24,960 / −48,320 / −95,040 bytes, at 40 bytes per empty array. That is 41% of the whole rung's memory cost, and it was being spent on nothing. The rung now costs +260 / +375 / +592 / +1,013 / +1,842 / +3,487, whose share of the program halves down the ladder (4.29e-4 to 2.20e-4). `codeBytes` unchanged at all six rungs by the optimization. (3) THE DECLARATION-PATH PROBE IS O(1) AND ALLOCATION-FREE, and the second half is MEASURED rather than argued. `TypeNameRegistry.record` keys `Map with (ByteArray, TypeNameDecl)` on the NAME TOKEN'S OWN ByteArray — it builds no key, so the previous rung's filed debt (`splitQualifiedName`/`joinQualifiedName` rebuilding a `Base.Member` key per reference at 18 allocations each) is NOT repeated one level over. `Map` is open-addressing with a cached-hash guard and `Array.hash` walks the bytes in place, so the probe is O(name length) and never O(declarations). The one thing that looked suspicious was that the common path — the FIRST declaration of every name, which in a clean program is all of them — reaches its `otherwise` arm through a `get` that THROWS. An experiment replaced it with a non-throwing `contains` probe, rebuilt, and re-measured: `phase:merge` came out IDENTICAL TO THE UNIT at all six rungs (+119 / +176 / +282 / +486 / +886 / +1,678 either way), so `MapError` rides the scalar error channel and allocates nothing. The experiment was REVERTED — it bought zero and cost a `panic` arm. (4) WHAT REMAINS IS IRREDUCIBLE AND IS NOT DEBT: ~1 allocation per type declaration in `phase:parse` (the ordered per-file record, which is the fact no per-kind array can hold) and ~1 in `phase:merge` (1,598 declarations + 44 iterators + 36 map-growth reallocs = 1,678 at rung 5, exactly). Both are proportional to type DECLARATIONS, which is the feature's actual input; nothing here is superlinear and nothing is triggerable into bending. `Parser.abortedParseArtifact` is free on the happy path by construction — it is called only from `queryParseOps`' `otherwise (parseErr)` arm — and the per-file cost that DID exist on the happy path was the eager array, which is what (2) removed. GATES on this exact tree: shv2 build exit 0 zero warnings, 138 codes; x64 spec-test 1631/0 exit 0 no leak; `--workers=1` 1631/0 exit 0 (serial determinism); wasm32-wasi spec-test 1440/0 exit 0; csharp spec-test 3103/0 exit 0; `specs-shv2/fragments` and `specs/` ZERO modified after all three suites, working tree carrying only the one source edit. | 606,206 | 1,098,064 | 2,081,413 | 4,046,796 | 7,978,651 | 15,845,148 |
| 2026-07-25 | P1.7 Workstream O — the TWO DUAL CASCADE QUADRATICS COLLAPSED. The +7,159 in this row is BOUGHT, not lost, and is attributed to the unit below. (1) WHAT SHIPPED: two pairs that each wrote ONE fact down TWICE and so carried ONE quadratic twice. PAIR 1, the per-instantiation gates (`genericTypeFieldHas{Managed,CoOwnedTrivial,Uncopyable}Instantiation`), already shared one scan, but that scan walked the WHOLE instance registry `.equals`-ing every base name — O(call sites x TOTAL instances); `GenericInstanceRegistry` now carries a by-base-id index written by the same `intern` that assigns the ids (the second payload=>id direction beside the existing composite-key one), and `TypeNameInterner.idOf` resolves the base NAME to that id WITHOUT interning, because interning on a miss appends and shifts every later id into the emitted mangling. PAIR 2, `closeDestructorNeeds` and its clone dual `noteClonerUsage`, were each a `while changed` RE-SCAN of every declared type — D passes over T types for a cascade chain of depth D, re-minting every type's `__destruct_`/`__clone_` symbol on every pass; both now drive ONE shared `CascadeWorklist` (`Compiler/Runtime/CascadeNeeds.maxon`) that registers the node space once and expands each referenced node EXACTLY once, O(types + fields). The growth flag is gone: the queue is the growth signal. (2) NEITHER QUADRATIC IS VISIBLE HERE AND BOTH WERE MEASURED ELSEWHERE, on purpose-built ladders at the trigger each PLAN.md filing names, via `--metrics` min-of-5 on an idle box. PAIR 1 (N generic types, each one opaque-field-reassign site), `phase:parse` ratio per doubling 2.05/2.16/2.57/2.57 -> 1.96/2.00/2.18/1.94; at N=1024, 323.7ms -> 175.8ms. Its per-phase ALLOCATIONS are BIT-IDENTICAL before and after at every rung (748,425 both ways) — which is the correctness evidence, not just a perf note: the gate visits exactly the same instances, it has merely stopped walking the rest. PAIR 2 (D-deep managed chain) in the EXACT unattributed-allocation column, per doubling: cloner 2.75/3.25/3.66/3.82 -> 1.63/1.77/1.87/1.93, destructor 2.94/3.33/3.69/3.83 -> 1.78/1.88/1.93/1.96. At D=512 that is 3,957,231 -> 174,870 allocations (cloner) and 1,909,550 -> 85,541 (destructor): 22.6x and 22.3x, both curves now linear. (3) THIS ROW'S DELTA IS A/B'd IN ONE WORKTREE AT ONE CHECKOUT PATH: base 15321679e checked out over this tree, rebuilt and measured — allocations came back IDENTICAL to the previous row at all six rungs and bytes -204, exactly the one-character path difference, which is what makes the comparison sound — then head restored and rebuilt. Head-minus-base = +446/+681/+1,128/+2,005/+3,732/+7,159 allocations, and it is LINEAR: per-doubling 1.53/1.66/1.78/1.86/1.92, rising toward 2.00 from below, with the term's share of the program SHRINKING (7.35e-4 down to 4.52e-4). ATTRIBUTION IS EXACTLY THREE-WAY: `phase:load` +5 FLAT at every rung (the one new `byBase` map object per `ProgramSignatures.create`); `phase:signatures` +21/+30/+55/+96/+169/+306 (by-base buckets filled as the sweep interns instances); `unattributed` +420/+646/+1,068/+1,904/+3,558/+6,848 — the destructor closure's node REGISTRATION at ~2 allocations per registered node, one boxed `CascadeNode` and one collision bucket. (4) THE COLLISION BUCKET IS NOT DEFENSIVE PROGRAMMING — it was PROBED and the collision is REACHABLE: a `type Box_String` declared alongside a `Box with String` whose mangling is also `Box_String` compiles and runs today, so a single-valued index would have silently stopped expanding one of two claimants. Expanding every claimant is precisely what the old all-types re-scan did, so the bucket keeps behaviour identical. (5) ONE FURTHER CHANGE KEPT, ONE DECLINED. Kept: seeding now drives from the NEEDED SET rather than probing that set once per declared type — O(referenced) instead of O(types), which also deleted a whole parallel column and put the seed and discover questions in one place. Declined: parallel scalar columns to dodge the union box, and prefix-stripping to dodge registration for structs/unions, would buy ~0.02% of rung-5 allocations in exchange for a second lookup path and the very wrong-table hazard the layout-carrying node exists to prevent (the OPEN #56 mismatch). The mandate says a tidy O(n) beats a clever O(n), so it was not done. GATES on this tree: shv2 build exit 0 zero warnings, 138 codes; x64 spec-test 1634/0 exit 0 no leak at BOTH --workers=12 and --workers=1; wasm32-wasi 1443/0 exit 0; `specs-shv2/fragments` ZERO modified after all three suites, with one ADDED golden for the new `clone-managed-three-level-cascade` spec (the committed clone spec nested ONE level and so never exercised the transitive step either closure exists for). | 606,652 | 1,098,745 | 2,082,541 | 4,048,801 | 7,982,383 | 15,852,307 |
| 2026-07-25 | OPTIMIZER PASS on the forced-split-cascade rung. (1) THE RUNG HEADLINE IS INDEPENDENTLY CONFIRMED, re-measured A/B in ONE worktree at ONE checkout path: base 15321679e rebuilt here, `regalloc:splitting` bytes 19,434,340 / 74,569,077 / 296,624,503 at N=128/256/512 (x3.84, x3.98 = N^1.97) against 424,761 / 837,185 / 2,318,502 after (N^1.22 over that span, N^1.16 out to N=1024) — 128.0x fewer bytes at N=512, 161x less time, emitted code -33%/-37%/-38%, splitter allocations x1.97/1.98/1.99, and the int control executable byte-identical at every N. (2) THE REMAINING SUPERLINEARITY IS FIXED, NOT FILED. `regalloc:splitting` was still Theta(insertions x block) in TIME while its allocations were linear, so THIS INSTRUMENT IS STRUCTURALLY BLIND TO IT: 22.6 / 58.0 / 171.5 / 549.7 ms at N=128..1024 fits 16.0n + 6.59n^2 to within 1%, so 77% of splitting and 68% of the whole compile at N=1024 was the quadratic term. All THREE Theta(block) costs per inserted op — the SEARCH for the anchor op position (`positionInBlock`), `Array.insert` suffix shift, and the walk of the block use thread that restates sequence numbers (`shiftBlockSeqs`) — fall to ONE change of shape, because fixing any one alone buys no asymptotic gain: the splices are now LOGGED and materialized in one backward pass per block (`IrBlock.spliceOpRefs`), which puts every position in the block PRE-SPLIT coordinates, which in turn lets a reload anchor position be READ OUT of the use record that asked for it (`UseIndex.usePosAt` — a sequence number IS block x stride + position) instead of searched for. After: 16.2 / 32.8 / 69.6 / 138.6 ms, N^1.03, fitted quadratic coefficient ~0; 3.97x faster at N=1024 and the whole compile 2.95x. (3) THE ALLOCATION DELTA. The RUNG cost is confirmed at +3.3..+3.5% and strictly FLAT — +19,850 / +37,225 / +71,978 / +141,483 / +280,494 / +558,515, delta ratios 1.875 / 1.934 / 1.966 / 1.982 / 1.991 converging on 2.00, no growth term — being one `slotResident` column plus three `SplitScratch` buffers per function against two `OperandList.create()` removed from the per-split path. THIS PASS ADDS ZERO: allocations AND frees identical to the rung at all six rungs, and codeBytes identical. Only bytes move, +0.006% (+4,442 .. +67,682, sub-2.0 ratios), and it is one deliberate cost: `spliceOpRefs` grows `opRefs` with `growFilled` (geometric) rather than `resize` (exact-fit), because exact-fit is precisely the Theta(extensions x length) trap `Array.growFilled` documents for a buffer extended repeatedly — a block spliced into by several splits in turn. The first cut used `resize` and was measured at +192 allocations for it; that is the whole reason the byte line moved. CORRECTNESS, beyond the green suite and beyond the goldens, because neither is sufficient for a change to WHERE ops land: the emitted executable is BYTE-IDENTICAL before vs after for all six scale-corpus rungs (rung 5 = 139,381 lines, 1,915,990 bytes, with `regalloc:splitting` making 1.08M allocations inside it) and for every ladder program including N=1024; and the one new identity this rests on — that a use record sequence number gives the same position the old search returned — was ASSERTED against that search in a throwaway build and run over the whole 1636-test suite, the ladder and the corpus, with zero mismatches. x64 1636/0 at --workers=1 and =12 with byte-identical output, wasm32-wasi 1445/0, `specs-shv2/fragments/` clean, `verify-warm-rebuild` PASS, `VerifyIncrementalSplit=true` green on the suite, no run exited 101. | 626,056 | 1,135,289 | 2,153,391 | 4,188,279 | 8,259,145 | 16,403,663 |
| 2026-07-25 | INDEPENDENT REVIEW of the forced-split-cascade rung. Allocations and bytes both fall slightly and with NO growth term: -205/-290/-460/-800/-1,480/-2,840 allocs and -14,376..-119,776 bytes across rungs 0-5, delta ratios 1.41/1.59/1.74/1.85/1.92 converging on 2.00 — a flat per-function saving, not a change of curve. It is one thing: `markRunStarts` no longer derives a use's position by SEARCHING the block's op list for its op index. That search made it re-collect the whole block (`collectBlockOps` into a per-function scratch array) for every use-block of every split, and — because the scan for the FIRST use in a block starts at position 0 while `havePrevious` is false — it evaluated `opStrandsClass` over the entire prefix ahead of that use and THREW THE ANSWER AWAY: Theta(block) per split, Theta(splits x block) per function, ~M x 2M discarded calls on a straight-line function with M distinct f64 locals read after one call. It allocated almost nothing, which is why this instrument was nearly blind to it; it was found by reading. Both endpoints now come out of the use records (`usePositionIn`), which also removes the SECOND derivation of a use's position — the rung had just introduced `bodyPositionOfUse` to read that same fact out of the index, and two derivations of one position cannot disagree loudly. The `SplitScratch.splitOps` buffer goes with it. Behaviour is identical: zero golden churn, x64 1636/0, wasm32-wasi 1445/0, no run exited 101. Also added, at O(1) and always on: `usePositionIn` compares the op at the position against the op the record names, which is the only cross-check between the two halves of the splice-shift rule (`IrBlock.spliceOpRefs` PERFORMS the shift, `SplitEdits.splicesAtOrBefore` PREDICTS it, in different files, nothing deriving one from the other); and `SplitEdits.commitBlock` now proves the op it is about to remove IS the one the removal meant. Both were seen RED — inverting `splicesAtOrBefore`'s comparison fails 4 register-spill tests with the new message instead of miscompiling silently. | 625,851 | 1,134,999 | 2,152,931 | 4,187,479 | 8,257,665 | 16,400,823 |
| 2026-07-25 | MANGLE-COLLISION — a generic instantiation's COMPILED name and a declared type name are one namespace (E3006). WHAT SHIPPED: `ParseStaging.checkTypeSymbolNamespace`, one whole-program walk of the interned generic instances called from `compileToCodeResult` beside `checkWhereConstraints` (both halves of what it compares are complete there: `queryProgramSignatures` has interned every instantiation, `queryAllModule` has merged every declaration). Each instance's `mangleGenericInstance` string is probed against `TypeNameRegistry` (new `declarationOf`, the ask-without-filing door — `record` cannot serve a prober because it files as it asks) and against a claim map of the manglings seen so far. THE DEFECT WAS THREE-SHAPED AND ALL THREE WERE MEASURED ON THE BASE COMPILER, each from a build that exited 0 with NO diagnostic: (a) LEAK — `type Box_String` with two String fields plus a `Box with String` — `installStructDestructors` and `installGenericInstanceDestructors` both emit `__destruct_Box_String`, the later install wins, the struct's second String is never released, program exits 101; (b) SILENT SUCCESS — same pair with MATCHING layouts (`Box_String` holding one `Holder`) — the surviving cascade releases a Holder box through `__str_decref`, which lands on a real refcount header, and the program returns 4 with no leak, so the type confusion is invisible until a field moves; (c) SIGSEGV with NO declaration involved — `_` is a legal name character so the base_arg join is NOT injective: `Pair with (Box_Int, Str)` and `Pair with (Box, Int_Str)` both compile to `Pair_Box_Int_Str`, and each pair's fields are then released through the other's per-field callees. The bootstrap has (a) too and is worse — `mm_decref: refcount underflow` plus a nil-pointer panic — so this is a genuine language rule, not an shv2 artifact. THE COST, A/B against the previous row at the same base commit e306c562a (ALLOCATIONS only; bytes are not comparable across checkout paths and this tree's path is longer): +117/+203/+367/+687/+1319/+2575 allocations, per-doubling 1.73/1.81/1.87/1.92/1.95 — rising toward 2.00 from BELOW, i.e. LINEAR, and 0.016% of rung 5's 15,854,882. Attribution is exact and entirely expected: one `Map` per compile that instantiates any generic, plus ~2 allocations per interned instance (the mangling `String` and the `ByteArray` key both probes share). A generics-free program returns before allocating anything at all. DECLINED, deliberately: a filter that skips instances whose mangling cannot be ambiguous (no `_` in the base name or in any argument's mangling) would cut most of the walk on the real corpus but needs most of the mangling work to decide, and the mandate is that a tidy O(n) beats a clever O(n). NOTED FOR A LATER OPTIMIZER PASS, not done here: `mangleGenericInstance` returns a `String` that FIVE of its seven call sites immediately `.toByteArray()` (four in MmRuntime, one in SignatureIndex) — a pre-existing round-trip this rung adds a sixth site to; changing the return type would pay back across all of them at once. GATES on this tree: bootstrap build exit 0 zero warnings (ErrorCode.g.cs regenerated, comment-only) and C# suite 3103/0 with `git status specs/ specs-shv2/` byte-identical across it (codegen neutrality); shv2 build exit 0 zero warnings, 138 codes; x64 spec-test 1641/0 exit 0 no leak at BOTH --workers=12 and --workers=1, stdout byte-identical; wasm32-wasi 1450/0 exit 0; `specs-shv2/fragments` ZERO modified, seven ADDED goldens for the seven new `type-name-collision` cases. | 606,769 | 1,098,948 | 2,082,908 | 4,049,488 | 7,983,702 | 15,854,882 |
| 2026-07-25 | MANGLE-COLLISION, OPTIMIZATION PASS — the E3006 compiled-name check is LINEAR (verified past rung 5), and `mangleGenericInstance` is now DERIVED ONCE, which removes a pre-existing QUADRATIC. (1) THE CHECK IS LINEAR, and this is now measured rather than extrapolated. A/B in ONE worktree at this commit (check present vs. its walk suppressed, everything else identical), extended to EIGHT rungs: the check costs +117/+203/+367/+687/+1319/+2575/+5079/+10079 allocations, per-doubling 1.735/1.808/1.872/1.920/1.952/1.972/1.984. The implementer measured to rung 5 and read "rising toward 2.00 from below"; carried two rungs further it SETTLES at 1.984 and never crosses 2.00. The decisive statement is not the ratio but the SHARE: 0.0193/0.0185/0.0176/0.0170/0.0165/0.0162/0.0161/0.0160 percent of each rung's total — monotonically DECREASING across a 128x span. A superlinear term's share grows; this one shrinks. (For contrast, the cascade rung's gates read x2.05 at N=128 and x2.57 by N=512 — they crossed 2.00 and kept climbing.) Complexity confirmed by reading: one pass over the interned instances, two O(1) map probes each (`TypeNameRegistry.declarationOf`, then the claim map), no nested scan. It does NOT use `GenericInstancesByBaseMap` and must not — a compiled-name collision CROSSES bases (`Box_A with B` and `Box with A_B` share a mangling and no base) and the declared half is not in that index at all. `instantiationAnchor`'s linear site scan runs only after a collision is already found, i.e. never on a program that compiles. (2) THE ROUND-TRIP, TAKEN — and it was hiding a quadratic, not a constant factor. `mangleGenericInstance` was recomputed at every ask by EIGHT call sites, and the recomputation RECURSES: `mangleTypeArg`'s nested arm re-enters it, so an instance nested d deep re-derived all d enclosing names on every one of those asks. MEASURED on a hand-written chain of N inline-nested instantiations (`Wrap with (Wrap with (... Small))`, ~5 KB of source at N=1600, no scale corpus involved): allocations inside `mangleGenericInstance` were 9,897 / 39,797 / 159,597 at N = 50/100/200 — x4.02 and x4.01 per doubling, a textbook quadratic — and 87% of every allocation the compile made. Whole-compile cost at N=1600 was 31,134,966 allocations / 55,790,817,306 bytes / 5,922 ms, of which 97% was UNATTRIBUTED in the per-phase table because the passes that ask sit between phase brackets. THE FIX: `ProgramSignatures.compiledInstanceNames`, a gid-keyed memo (`CompiledInstanceNameMap`); each name is built once from its argument's already-built name, and the accessor now returns the `ByteArray` six of the eight callers wanted (the `.toByteArray()` at all six is gone; the two that want a `String` say `String.from`). AFTER, same probe: N=1600 is 407,079 allocations / 93,009,340 bytes / 915 ms — 76x fewer allocations, 600x fewer bytes, 6.5x faster — and whole-compile allocations at N=100 vs 200 go 28,954 -> 51,077, x1.76, LINEAR. `mangleGenericInstance` no longer appears in the ranked scope table at all. (3) THE ROW ABOVE: the memo pays back on the REALISTIC ladder too, and more than the check ever cost. -196/-380/-756/-1516/-3044/-6108 allocations here, and -12,244/-24,524 at rungs 6/7; the savings ratio per doubling is 1.94/1.99/2.005/2.008/2.007/2.005/2.003, i.e. proportional to the instance count, as a de-duplicated per-instance derivation must be. Netted against the check's own cost the rung is now allocation-NEGATIVE versus its base commit e306c562a: -79/-177/-389/-829/-1725/-3533/-7165/-14445. The safety check is free and then some. CODEGEN NEUTRAL, which is the gate that matters for a pure-perf refactor: the memo hands back the same bytes, and `specs-shv2/fragments` is byte-identical (zero modified) across the x64 suite, the wasm suite AND the C# suite. STILL OPEN, and reported to the coordinator rather than filed here: a SECOND, time-only superlinearity on the same pathological input survives (N=1600 still 915 ms, ~x3.4 per doubling) while its ALLOCATIONS are linear — so `scale-test` is structurally blind to it, exactly as this corpus's own header warns. It is NOT this rung's: suppressing the E3006 check changes it by ~8% (846 ms vs 915 ms at N=1600, within noise). It lives in the region between `queryAllModule` and the pipeline, which is 80.9% of that compile's wall time and is billed to NO phase — an attribution gap worth closing before the term is hunted. | 606,573 | 1,098,568 | 2,082,152 | 4,047,972 | 7,980,658 | 15,848,774 |
| 2026-07-25 | review(E3006): builtin-instance early-outs in genericInstanceHasStringField + addNestedInstanceDestructors — turns two reachable compiler panics into the documented answer; expected allocation-neutral (the guards fire only where the old code would have crashed) | 606,573 | 1,098,568 | 2,082,152 | 4,047,972 | 7,980,658 | 15,848,774 |
| 2026-07-25 | Wave 2 (xmm6-15 callee-saved) MEASURED ALONE — the scale corpus is UNCHANGED for this row (FloatsLivePerSpillLoop still 4); the next row is the corpus retune. A float live across a call no longer force-spills a store/reload bracket, so the splitter mints fewer values and does less repair. | 613,518 | 1,110,982 | 2,105,421 | 4,092,725 | 8,067,926 | 16,020,150 |
| 2026-07-25 | CORPUS RETUNE, not a compiler change: FloatsLivePerSpillLoop 4 -> 12. Wave 2 made the floatSpill knob INERT — its four cross-call floats now fit the ten callee-saved XMMs, so the knob that exists to measure the float FORCE-SPILL path had stopped forcing one, and would have reported a flat Delta0 for a path it no longer reaches (the instrument-cannot-express-the-feature trap). Twelve exceeds ten by the same slack the GPR knob keeps over its five. Everything grows because the generated programs are bigger; the compiler is byte-identical to the row above. | 619,564 | 1,123,277 | 2,130,294 | 4,142,840 | 8,168,790 | 16,223,032 |
| 2026-07-25 | OPTIMIZER PASS on the Wave-2 callee-saved-XMM rung. NO COMPILER PIPELINE CODE CHANGED — every number below is IDENTICAL to the row above, and that Delta0 IS the self-check on this pass's two edits: `ScaleCorpus.maxon` (the knob derivation, item 6) and prose in this file. (1) THE HEADLINE REPRODUCES, BUT THE RUNG'S TABLE DOES NOT, because the rung recorded no ladder — its 404,409/788,153/2,218,086 -> 43,717/86,829/165,645 cannot be re-run by anyone. So one was built and is recorded here: ONE straight-line function, N units, each = define one f64 local, call an opaque int fn, read the local (N floats each live across ONE call), plus an int-typed CONTROL of the same shape. `regalloc:splitting` BYTES, base 65ed46138 -> head, at N=128/256/512/1024: 24,466,558 / 96,360,042 / 382,486,175 / 1,524,199,159 (x3.94, x3.97, x3.98 — a clean N^1.99 quadratic) -> 77,359 / 142,119 / 314,175 / 669,695 (N^1.04). Splitter ALLOCATIONS go 1,275,640 / 5,073,917 / 20,239,875 / 80,849,418 (x3.98/3.99/3.99) -> 427 / 429 / 432 / 435, i.e. CONSTANT: one float across one call now takes a callee-saved XMM and no split happens at all. At N=1024 that is 126.3 s of splitting -> 13 ms, and 1.52 GB -> 0.67 MB. Emitted code -35 percent, and all six executables (float and int, N=128..512) return the SAME exit code under both compilers. THE INT CONTROL IS BIT-IDENTICAL base vs head at every N (426/428/430 splitter allocations, same bytes, same codeBytes) — the correctness evidence that Wave 2 provably did not touch integer allocation. (2) NO NEW SUPERLINEARITY; all four suspects checked and each is constant or linear. `assertCalleeSavedListsMatchMasks` runs ONCE PER MODULE over 15 registers (its only caller `assertCallClobberConsistent` is called once from `allocateRegisters`, which BackendDispatch calls once per module). `TargetLiveness.growValueSpace`'s new `for v in valueCount upto newValueCount` writes only the ids THAT call minted, and the one `LivenessResult` the splitter maintains has a monotonically rising `valueCount` (a full rebuild happens only under `VerifyIncrementalSplit`), so the loop sums to the total minted ids over a whole function — amortized O(1) per minted value, NOT the splits x values shape the previous rung removed. `colouredPhysicalRegs` is ONE scan per function feeding BOTH `usedRegsFrom` calls (5- and 10-element constant lists) — the same single scan `usedCalleeSavedRegs` already was. The GT chunk's save/restore loops are 10 iterations at chunk-BUILD time, once per module. (3) THE 75 PERCENT OF THE WAVE-2 ROW'S OWN DELTA THAT THE ROW DOES NOT NAME. A/B'd base vs head on an IDENTICAL corpus (both knob=4, two worktrees at equal-length paths). Head-minus-base allocations: `regalloc:coloring` -10,369 / -19,554 / -37,905 / -74,620 / -148,037 / -294,884, and `regalloc:splitting` -2,518 / -5,250 / -10,837 / -22,238 / -45,525 / -93,023. The row credits the splitter, which is only 24 percent of it. The other 75 percent is that `callerSavedMask()` STOPPED ALLOCATING: Wave 1 built it as `classRegisterMask(RegClass.xmm, file: x64RegisterFile())`, and `x64RegisterFile()` constructs a heap `RegisterFile` — on the COLORER'S PER-VALUE PATH (`preferredVolatilityMask`, up to 3x per `chooseRegister`). Spelling xmm0-5 out register-by-register deleted it. PROVEN, not inferred: a probe build of the BASE compiler changing ONLY that expression to the literal 0xFFFF0000 (a bit-identical mask) reproduces -10,358 / -19,531 / -37,858 / -74,525 / -147,846 / -294,501 in `regalloc:coloring` — 99.9 percent of the saving — with codeBytes IDENTICAL at all six rungs. Both curves are flat: delta ratios 1.886/1.938/1.968/1.984/1.992 converging on 2.00 from below, no growth term. It is the same waste `RegBits.maxon` already documents for arm64's encoder; the two remaining `x64RegisterFile()` calls are both once-per-module asserts. (4) THE ONE COST THIS RUNG ADDS, MEASURED AND DELIBERATELY NOT TAKEN: `phase:prologueEpilogue` +195 / +280 / +450 / +790 / +1,470 / +2,830 allocations and +7,448 .. +112,848 bytes, ratios converging on 2.00 — one extra `X64RegisterArray` per function, because `usedRegsFrom` is now called twice where `usedCalleeSavedRegs` allocated one list. It could be removed by keeping `coloured and calleeSavedMask()` as a mask and iterating the constant order filtered by it, but that respells "walk the order, test the mask" at four sites to buy 0.017 percent of rung-5 allocations on a curve that is already linear. A tidy O(n) beats a clever O(n) — declined on purpose, recorded so nobody re-derives it. (5) A QUADRATIC FOUND, MEASURED, AND SHOWN NOT TO BE THIS RUNG'S. On a SINGLE-BASIC-BLOCK function with a constant density of forced spills, `regalloc:splitting` is quadratic in program size: 8 ints live across each of N calls gives 124,175 / 489,272 / 1,943,676 / 7,749,362 allocations at N=16/32/64/128 (x3.94/3.97/3.99). It is FILE-AGNOSTIC and PRE-EXISTING — base and head are BIT-IDENTICAL on that ladder, every column at every N — and the float twin (12 floats per unit) is quadratic on BOTH, head merely 6.4x cheaper. The cause is the incremental repair's DIRTY UNIT being a BLOCK: in a one-block function every split re-commits and re-sweeps the whole function. It is MEASURED LINEAR IN PRACTICE, which is why it is reported rather than fixed here: the corpus's own shape — N LOOPS each holding its floats live across a call — reads x2.02 per doubling out to 256 loops (21,374 / 43,207 / 87,418 / 176,924 / 358,112 splitting allocations), and this six-rung ladder reads x2.04 in bytes with allocation ratios converging on 2.00. RE-MEASURE TRIGGER: a function whose ONE basic block grows with program size while forcing spills at that density — machine-generated straight-line numeric code, or an INLINER that flattens loops away (shv2 has none). (6) THE RETUNED KNOB IS LIVE, AND ITS STATED RATIONALE WAS WRONG. Swept `FloatsLivePerSpillLoop` on a 64-loop ladder of the corpus's exact shape (`regalloc:splitting` allocations): 4 -> 784, 6 -> 784, 8 -> 2,064, 9 -> 2,714, 10 -> 31,364, 11 -> 57,644, 12 -> 87,418, 13 -> 120,886, where 784 is the per-function floor. So the OLD value of 4 was genuinely INERT on head (784, the floor) while forcing 93,808 on base — the rung's inertness claim is CONFIRMED by measurement, not taken on trust — and 12 sits comfortably past the knee. But the knee is at TEN, not eleven, because `total` is an eleventh cross-call float; and the claim "twelve is ten plus the same slack the GPR knob keeps over its five" is FALSE — `PressuredValuesPerLoop` is 8 over 5, a slack of THREE. Corrected in `ScaleCorpus.maxon` with the sweep written down. AND THE TEN IS NO LONGER WRITTEN DOWN AT ALL (review finding F6): `floatsLivePerSpillLoop()` now returns `regMaskPopcount(calleeSavedMask() and classRegisterMask(RegClass.xmm, file: x64RegisterFile()))` plus `FloatSpillHeadroom`, so the half's width comes from the allocator's own mask and the next move of that line RETUNES the knob instead of blinding it — which is exactly how it went blind this time. (It is a function, not a `let`, only because a global initializer may not call one: E2045.) VERIFIED BOTH WAYS: the emitted corpus is BYTE-IDENTICAL to the literal-12 corpus at all six rungs and scale-test reports Delta0, so the derivation yields exactly 12; and with the class term flipped to `RegClass.gpr` in a throwaway build the corpus regenerates at SEVEN floats per loop (the five callee-saved GPRs plus the same headroom) — the derivation seen RED, so it reads the mask rather than merely coinciding with it. (7) BOTH WAVE-2 ROWS VERIFIED REPRODUCIBLE bit-for-bit, in separate worktrees at equal-length paths: the corpus-retune row comes back Delta0 on all six rungs, and the knob=4 row reproduces 613,518 / 1,110,982 / 2,105,421 / 4,092,725 / 8,067,926 / 16,020,150 exactly. Wave 2's own saving over the review row is -12,333 / -24,017 / -47,510 / -94,754 / -189,739 / -380,673, ratios 1.948..2.006 — flat, no change of curve. The corpus-retune row is a full EPOCH BOUNDARY marked only in prose, unlike every earlier corpus row in this log, so READING RULE 3 has been added to this file's header where a reader actually looks. GATES on this tree: shv2 build exit 0, 138 codes; x64 spec-test 1641/0 exit 0 no leak at --workers=1 AND --workers=12; wasm32-wasi 1449/0 exit 0; `specs-shv2/fragments/` ZERO modified after all four suite runs, with x64-linux and arm64-macos untouched. ALSO SEEN AGAIN: an orphaned `maxon-shv2 build --target=wasm32-wasi` child outlived the GREEN wasm suite and held the compiler binary open, failing the next build with E9001 — the already-filed harness-robustness defect, reproduced from the wasm runner. | 619,564 | 1,123,277 | 2,130,294 | 4,142,840 | 8,168,790 | 16,223,032 |
| 2026-07-25 | RE-BASELINE AFTER REBASING WAVE 2 ONTO 2518ab962 — NO WAVE-2 CODE CHANGED, AND THE DELTA BELOW IS UPSTREAM'S, NOT THIS RUNG'S. The three Wave 2 rows above (the rung, the corpus retune, the optimizer pass) were all measured against base 65ed46138. While the rung was in review, main took NINE commits: four for the E3006 generic compiled-name collision (including the `mangleGenericInstance` memo, which its own row records as allocation-NEGATIVE) and five for the debugger's P5 `maxon mcp`. Rebasing put those four rows ABOVE the Wave 2 rows, so the delta from the row directly above ours silently spanned upstream's work as well — three honestly-measured rows making a false comparison, which is exactly the trap this log's epoch convention exists for. This row is the corrected reading of the MERGED tree: rung 5 is 16,219,501 allocations / 1,150,742,609 bytes against the optimizer row's 16,223,032 / 1,150,807,911, i.e. -3,531 allocations and -65,302 bytes, and ALL OF IT IS UPSTREAM — the Wave 2 commits are byte-identical across the rebase (the conflict was confined to this file and to PLAN.md, both of which only append) and the E3006 memo is the row that predicted a saving of that shape. ⇒ READ THE WAVE-2 SAVING OFF THE THREE ROWS ABOVE, WHICH ARE INTERNALLY CONSISTENT AT ONE BASE; read THIS row only as the merged tree's new starting point. GATES on the rebased tree, all re-run by the coordinator rather than inherited: shv2 build exit 0 zero warnings; x64 spec-test 1651/0 at BOTH --workers=12 and --workers=1 with ZERO golden churn after each; wasm32-wasi 1457/0; total working-tree churn 0; no run exited 101. The count moved 1643 -> 1651 purely by upstream's eight new type-name-collision cases. | 619,487 | 1,123,102 | 2,129,907 | 4,142,013 | 8,167,067 | 16,219,501 |
| 2026-07-25 | P1.7 generic type-argument mismatch is REJECTED (E3005) — a generic instance gains its nominal identity (its compiled name) and a bare `T` parameter is substituted through the call's instance. A/B IN THIS WORKTREE against the same tree with the 5 compiler files stashed: +28/+56/+112/+224/+448/+896 allocations and +512 B ×2^n — exactly ×2 per doubling, i.e. LINEAR, and 0.0045% of the compile. Split: `phase:signatures` up (deriveCompiledInstanceNames pre-derives every swept instance's compiled name so the PARSE only ever reads one — a per-file fan-out must not write that memo) against the unphased E3006 sweep coming down by the same derivations, plus `phase:semanticCheck` +12→+384, the new per-generic-call CallGenericInstance. ⚠ The PREVIOUS row does not reproduce on an unchanged tree: the stashed base measured +19,645 allocations at rung 0 against it, so that delta is NOT this rung's and the prior row appears to have been recorded against a differently-built binary. | 626,246 | 1,135,559 | 2,153,782 | 4,188,879 | 8,260,120 | 16,405,345 |
| 2026-07-25 | P1.7 alias-named type arguments + E3091 cyclic generic aliases. SAME-LINEAGE A/B (one bootstrap, md5 4dfdbf34, both sides): +24/+37/+62/+111/+208/+401 allocations, ~x1.86 per doubling — sub-linear against a x2 corpus, 0.0038% of the compile. TWO movements, opposite signs, both attributed: `phase:semanticCheck` -384 at rung 5 is the CallGenericInstance union box REMOVED (a Maxon union value is a heap object, minted once per call into a shared generic body; callInstanceSubject returns a plain ValueId instead) — exactly the cost the optimizer isolated. Against it, `phase:signatures` +8 and the UNPHASED cycle walk (checkGenericAliasCycles, in compileToCodeResult outside any probe) account for the gross, plus the genericAliasDecls order column. ⚠ Supersedes the 2026-07-25 row above, which straddled a bootstrap refresh and is VOID for comparison — main's bin/ was stale (built 2026-07-24 with 26 newer maxon-sharp/*.cs), so that row and this one were measured on different lineages. | 626,270 | 1,135,596 | 2,153,844 | 4,188,990 | 8,260,328 | 16,405,746 |
| 2026-07-25 | P1.7 alias/E3091 OPTIMIZATION PASS — a CUBIC removed from the E3091 cycle check, and the rung re-baselined. (1) THE ROW ABOVE REPRODUCES AND IS NOT VOID IN THIS LINEAGE. dc0e1127c rebuilt with the same bootstrap (bin/maxon.exe md5 c5c966e9) reads delta 0 on allocations AND bytes at all six rungs, and ba91248db rebuilt in a throwaway worktree from that SAME bootstrap binary reads 626,246 / 1,135,559 / 2,153,782 / 4,188,879 / 8,260,120 / 16,405,345 — so the row above's "+24/+37/+62/+111/+208/+401" is confirmed at BOTH ends. ⚠ MEASUREMENT TRAP, FOUND THE HARD WAY AND WORTH KNOWING: `phase:load` BYTES track the compiler EXECUTABLE'S OWN FILENAME LENGTH. A probe copy named `probe-A.exe` (11 chars) read 3 bytes less at EVERY rung than `maxon-shv2.exe` (14); renaming that byte-identical binary to a 14-char name made the row reproduce bit-for-bit. Give A/B probe binaries equal-length names, or every rung's byte column shifts by the difference — and the shift is perfectly reproducible, so it looks exactly like a code change. (2) THE `CallGenericInstance` UNION BOX DELETION IS CONFIRMED AT THE COMMIT BOUNDARY, not inferred: `phase:semanticCheck` allocations ba91248db → dc0e1127c are 1882→1870 / 3516→3492 / 6784→6736 / 13320→13224 / 26392→26200 / 52536→52152, i.e. −12/−24/−48/−96/−192/−384, exactly x2 per doubling. One heap box per call into a shared generic body, gone — the symmetric undo of the "+12→+384" the row above booked for adding it. (3) THE CUBIC. `firstCyclicAliasDecl` asked "does this alias reach itself?" once PER ALIAS, and each ask re-walked the instances that alias's arguments reach with a LINEAR SCAN of a `visited` array at every pop — O(aliases x instances x visited). MEASURED, A/B in this worktree at dc0e1127c against the same tree with only this check suppressed, on a chain (`N0 = Box with S0`, `Ni = Box with N(i-1)`) that COMPILES CLEAN: the check ALONE cost 31 / 268 / 1,751 / 12,298 ms at N = 400 / 800 / 1,600 / 3,200 — x8.6, x6.5, x7.0 per doubling, i.e. CUBIC — while everything else in the compile stayed flat at 139…197 ms over that same 8x span. THE FIX: one Tarjan SCC over the whole instance-argument graph (`nodesOnACycle` in CsrGraph.maxon, over an adjacency built by the shared `buildCsr` counting sort), after which the blame is read off the cycle set in declaration-record order. AFTER, same ladder: 149 / 197 / 307 ms at N = 1,600 / 3,200 / 6,400 against a check-suppressed 143 / 194 / 295 ms — the check costs 6 / 3 / 12 ms and no longer grows out of the noise floor. Semantics verified DIFFERENTIALLY rather than assumed: 11 hand-built cycle shapes (diamond, 3-cycle, feeder-declared-first, two independent cycles in both declaration orders, a cycle entered through a `Pair` and through an inline nested instantiation, two clean DAGs) give BYTE-IDENTICAL diagnostics and exit codes under the old detector and the new one. (4) THE LADDER'S OWN MOVEMENT, EVERY UNIT ACCOUNTED: −5 / −29 / −77 / −173 / −365 / −749 allocations. The corpus declares 15 / 27 / 51 / 99 / 195 / 387 generic aliases; the old walk allocated exactly 2 arrays per alias (its `pending` and its `visited`) and the new pass allocates a FLAT 25 whatever the alias count is — so the saving is exactly 2 x aliases − 25 at every rung (30−25, 54−25, 102−25, 198−25, 390−25, 774−25), which is why it converges on x2.05 per doubling rather than being a constant. It lands in NO phase row, because `checkGenericAliasCycles` runs in `compileToCodeResult` outside every phase probe; the per-phase tables are otherwise identical. (Of that flat 25, ONE allocation and 56 bytes are the `SccWalk` record that carries the walk's six columns — deliberately paid: it replaced a seven-argument thread through four helpers, and a flat +1 per compile is not a growth curve.) (5) THE READ-TIME ALIAS RESOLUTION IS LINEAR — measured, not assumed. On a ladder doubling BOTH the alias count and the generic call sites (K aliases + K one-call functions, so the register allocator is not the subject), `phase:semanticCheck` allocations are 5,413 / 10,813 / 21,613 / 43,213 at K = 200 / 400 / 800 / 1,600 — x2.00 per doubling across an 8x span. `namedTypeArgIdentity` is one `Map.get` on `genericAliases` plus a read of the `compiledInstanceNames` memo that `deriveCompiledInstanceNames` already warmed: both O(1), no per-comparison walk of the alias table. ⚠ The FIRST version of that ladder put all 2K calls in ONE function and read x4.4–x5.5 per doubling — of which 98.6% was `regalloc:splitting`, the KNOWN budgeted debt (ARCHITECTURE.md:1336-1345) and not the alias read at all; `semanticCheck` was 0.9 → 1.8 ms on that very ladder. A ladder that hammers the splitter measures the splitter. CODEGEN NEUTRAL, the gate that matters for a pure-perf change: x64 1664/0, wasm32-wasi 1473/0, C# 3103/0, `--workers=1` byte-identical to `--workers=12`, and `git status specs/ specs-shv2/` EMPTY after all three suites. No exit 101. | 626,265 | 1,135,567 | 2,153,767 | 4,188,817 | 8,259,963 | 16,404,997 |
| 2026-07-25 | P1.7 alias/E3091 RE-BASELINE AFTER REBASING ONTO d2deebd4d — and the delta below is THREE causes, separated by same-lineage A/B rather than guessed. Net vs the row above: allocations −6,570 / −12,118 / −23,244 / −45,659 / −90,702 / −181,213, bytes +82,415 … +11,362,004. (1) THIS RUNG'S LAST TWO COMMITS (the ONE-canonical-identity fix and the review's cubic + false-rejection fix) contribute +161 / +283 / +519 allocations at rungs 0/1/2 — MEASURED, both sides built from the SAME bootstrap on the SAME rebased upstream, so nothing else moved between them. Ratios x1.76 then x1.83, converging on x2.00: LINEAR, and it is the second memo — `canonicalInstanceName` sits beside `mangleGenericInstance` (which still names every emitted symbol and the E3006 namespace) and holds one entry per instance. A second table costs one entry per instance; that is the price of routing four extractors through ONE door instead of three of them disagreeing. (2) UPSTREAM's Wave 2 (xmm6-15 callee-saved) takes allocations DOWN: a float live across a call no longer force-spills a store/reload bracket, so the splitter mints fewer values and does less repair. (3) UPSTREAM's CORPUS RETUNE (FloatsLivePerSpillLoop 4 -> 12) makes the generated programs BIGGER, which is why bytes rise while allocations fall — the two upstream causes push opposite ways and (2)+(3) together dwarf (1) by two orders of magnitude. ⚠ THE ROW ABOVE IS NOT COMPARABLE TO THIS ONE FOR ANYTHING BUT THIS ROLL-UP: it was measured before Wave 2 and before the retune, on a smaller corpus and a different register allocator. Attribute across this boundary only via a same-lineage A/B, never by subtracting the two rows. GATES on the rebased tree: x64 1674/0, wasm32-wasi 1480/0, C# 3103/0, `--workers=1` byte-identical to `--workers=12`, fragments zero `M` after all three suites (upstream's regalloc change did NOT move this rung's goldens), no exit 101. | 619,695 | 1,123,449 | 2,130,523 | 4,143,158 | 8,169,261 | 16,223,784 |
| 2026-07-26 | P1.7a TOP-LEVEL MANAGED `let` — a module-scope `let` may hold a String or a b"…" literal, materialized at every READ exactly as its literal already is (String: the ONE immortal .rdata record, deduped by bytes; byte-string: an owned 48-byte record over the ONE immortal .rdata blob). Allocations +64/+128/+256/+512/+1,024/+2,048 — EXACTLY doubling, so LINEAR, and rung 0's 64 is EXACTLY the 64 top-level let/var declarations the generated corpus contains (counted in .scale-tmp/rung0). It is ONE ConstInitializerValue box per top-level binding folded: the const evaluator's entry point now answers with a typed union (a NUMBER or a managed literal's BYTES) instead of a bare ConstValue, which is what keeps every fold below it scalar and untouched. Kept, on the coldest path in the compiler (once per top-level declaration in a program), because the alternative is a sentinel. TWO OTHER UNIONS WERE MEASURED AND REMOVED, by same-lineage A/B on this branch: a ManagedLiteralProbe answering 'which literal kind?' cost a second box per top-level binding (+248 -> +184 at rung 0) and an ArrayReceiverMutability answering 'may this receiver be written?' cost one box per ARRAY-METHOD CALL SITE (+184 -> +64), the latter being the same reading, and the same fix, that BuiltinConformer's header records one rung earlier — Maxon boxes a union case INCLUDING a payload-free one. Bytes +8,424 at rung 0 is those 64 boxes plus VarInfo gaining isParameter (8 bytes on every binding), which is what tells 'may this NAME be rebound?' (a parameter: no) from 'may the container it denotes be written?' (a parameter: yes). GATES: x64 1680/0, wasm32-wasi 1486/0, --workers=1 byte-identical to --workers=12, verify-warm-rebuild PASS, fragments 6 additions and ONE M whose PROGRAM changed (pop-via-local-binding's `let arr = self.items` is E3019 in the reference compiler and is now `var`), no exit 101. | 619,759 | 1,123,577 | 2,130,779 | 4,143,670 | 8,170,285 | 16,225,832 |
| 2026-07-26 | P1.7a OPTIMIZER — the managed-constant receiver probe is filtered by NAME, not by a whole-program BOOL. +8 allocations and +320 bytes at EVERY rung, FLAT rather than doubling, and that shape is the whole point: it is ONE `Set with ByteArray` (`ProgramSignatures.managedConstantNames`) created once per compile, replacing the `anyManagedConstant` bool. THIS CORPUS DECLARES NO MANAGED CONSTANT, so the instrument sees only the cost and structurally CANNOT see the benefit — the benefit was measured on a purpose-built ladder instead. What the bool did: it spared a program with no managed constant, but went permanently true at the FIRST one, after which every STATIC dotted call in the whole program (`Widget.create(…)`, `String.from(…)` — any base that is neither a local nor a capture) paid the full file-scoped probe just to be told "not a constant", and that probe BUILDS A STRING KEY (`fileScopedDeclKey`, ~4 allocations) plus union boxes. MEASURED on a 64→1,024 static-dotted-call ladder, one managed constant present vs absent: +390/+710/+1,350/+2,630/+5,190 parse allocations — linear in CALL SITES, a cost cliff that one `let G = "hi"` anywhere switches on for the entire program (and that the stdlib whitelist would switch on for every compile). With the name set the same delta is a FLAT +40 at all five rungs: O(static dotted call sites) → O(1). The set is a FILTER, never the answer — a hit still resolves through `constantValueOf`, so file-private scoping and `let`-vs-`var` keep their single home; VERIFIED on two files each declaring a *different* file-private `let SHARED` (filed only under the composite key), which still print their own values, IR byte-identical to before. ALSO CONFIRMED, not changed: the byte payload is NOT copied per read at runtime — two reads of one constant emit ONE shared `__str_rec_1` (`x64.leaRegRdata`), so v1's rematerialize-per-read curve is genuinely avoided; the E3019 receiver-mutability check allocates NOTHING (a `let`-receiver ladder and a `var`-receiver ladder are allocation-identical at every rung); `VarInfo.isParameter` is a scalar field costing zero extra allocations; and the implementer's +64/+128/+256/+512/+1,024/+2,048 was reproduced at BOTH ends by same-lineage A/B. GATES: x64 1680/0, wasm32-wasi 1486/0, --workers=1 byte-identical to --workers=12, verify-warm-rebuild PASS, fragments CLEAN (zero modified), no exit 101. | 619,767 | 1,123,585 | 2,130,787 | 4,143,678 | 8,170,293 | 16,225,840 |
| 2026-07-26 | P1.7 slice 2a TOP-LEVEL MANAGED `var` — a String or b"…" module-scope `var` is an 8-byte POINTER slot in writable .data, filled before `main` by a synthesized `__module_init` and released after it by `__maxon_global_cleanup` (both MAXON-tier functions appended to the merged module, so `lowerMaxonToStd` builds the literal's record and `scanRuntimeUsage` installs the floor they need). ⚠ THE DELTA COLUMN IS NOT THIS CHANGE: the previous row was recorded two rungs ago (the P1.7a optimizer, 1680/0) and the stdlib-whitelist rungs merged since were never logged, so the reported +1/-768 spans all three. MEASURED DIRECTLY by building base 8c1337f55 and this branch and running the ladder on both: rung0 619,767 -> 619,768 allocs and 36,016,862 -> 36,016,910 bytes; rung5 16,225,840 -> 16,225,841 and 1,151,209,107 -> 1,151,209,155. So THIS rung is +1 allocation and +48 bytes at EVERY rung, FLAT rather than doubling — one empty `ManagedGlobalArray` per compile (`signatures.managedGlobals()`), because THIS CORPUS DECLARES NO MANAGED GLOBAL and the instrument structurally cannot see anything else. Flat is the whole claim: nothing was added per op, per function or per file. The per-global cost was measured on a purpose-built 64->1,024 ladder of managed String globals each appended and read (allocations 60,060 / 100,967 / 182,633 / 345,756 / 671,776 = x1.68 x1.81 x1.89 x1.94, approaching x2.00 as the fixed floor amortizes — LINEAR in globals), and the 1,024-global executable runs and exits 0, so 1,024 clones in the init balance 1,024 decrefs in the cleanup. Two runtime branches BOTH reference compilers carry are absent by construction rather than by choice: the init store needs no load-and-decref of the old value (the slot is zero and shv2 does not share the assignment path, so no `func.Name == "__module_init"` match), and neither the cleanup nor a user re-assignment needs a null guard (`__module_init` runs unconditionally and fills every entry of the same list). GATES: x64 1692/0, wasm32-wasi 1497/0, --workers=1 byte-identical to --workers=12, verify-warm-rebuild PASS on managed-global programs, fragments 3 ADDITIONS and zero M, no exit 101. | 619,768 | 1,123,586 | 2,130,788 | 4,143,679 | 8,170,294 | 16,225,841 |
| 2026-07-26 | P1.7 slice 2a OPTIMIZER PASS. THE STANDARD CORPUS MOVED BY EXACTLY ZERO (delta 0 allocations, 0 bytes at every rung) — this pass changed only the managed-top-level RECEIVER path, and this corpus declares no managed binding, so nothing here reaches it. VERIFICATION FIRST, all by same-lineage A/B (one bootstrap; probe binaries given EQUAL-LENGTH names, which is what makes the numbers comparable — the implementer's row read +52/-4 against mine purely because `maxon-shv2.exe` is 4 chars longer than a probe name, and `phase:load` bytes track the executable's own filename): (1) the implementer's "+1 allocation / +48 bytes FLAT at every rung" is CONFIRMED EXACTLY, base 8c1337f55 vs branch 3bbf47902, rung0 619,767->619,768 / 36,016,858->36,016,906 through rung5 16,225,840->16,225,841 / 1,151,209,103->1,151,209,151 — one empty `ManagedGlobalArray` per compile from `signatures.managedGlobals()`, and a control ladder of 64..4,096 SCALAR globals reads the same +1/+48 at every size, so nothing was added per global, per op, per function or per file. (2) The per-global ladder is LINEAR and was pushed 16x further than claimed, to 16,384 managed String globals: total allocations 36,393 -> 5,276,154 with rung ratios 1.57 1.72 1.84 1.91 1.96 1.98 1.99 1.99, approaching 2.00 FROM BELOW and never exceeding it, and — the claim that actually settles it — a FLAT MARGINAL of 321.0 allocations per global at 512->1024, 321.3 at 1024->2048 and 321.0 at 8192->16384. A superlinearity makes the marginal GROW; the "floor amortizing" story predicts exactly this constant, so the x1.68 tail was the floor and not a curve. Byte-string globals read the same shape (marginal 418/global, flat). (3) "The dense per-value type columns, paid ONCE" is TRUE: `SynthesizedFunction.open` runs exactly twice per compile and `mint` is a push; `regalloc:splitting` is FLAT across the whole ladder (1,203 -> 1,242 for 64 -> 16,384 globals) because a global's ops die immediately, where a control ladder of N LOCAL Strings in one function reproduces the known budgeted `SplitLiveRanges` quadratic at ratios 3.88 3.94 3.97 3.99 — the managed-global path structurally avoids it. (4) NO files x globals term: 1,024 globals spread over 1/8/64/512 files costs a flat ~336 allocations per FILE with no growth in the globals term, so `managedGlobals()` is genuinely asked once and not from under the per-file query. (5) `TopLevelGlobalLookup`'s payload widening from a `ValueTypeTag` to a whole `MaxonType` costs ZERO: 4,096 global reads, parse-phase allocations 472,662 on both base and branch, because the `MaxonType` box merely moved from `emitGlobalLoad` to `resolvedGlobalOf`. WHAT CHANGED, and why the numbers above did not: the union-boxing audit found `parseMethodCall` testing `managedTopLevelReceiverAt` as a predicate and then RE-RESOLVING the identical name to build the receiver — visible in the code as an impossible-state `none` panic justified by "the two read the same resolution and cannot disagree". Measured as exactly 12.000 allocations per managed-`var` receiver site and 6.000 per managed-`let` site across a 1,024..16,384-site ladder. The gate now hands its resolution to the caller instead of being re-derived, and the panic arm is gone: managed-`var` receiver sites -12.000 allocations each, and managed-`let` sites — a P1.7a path this rung did not otherwise touch — now cost 3.000 FEWER than at base 8c1337f55, having briefly cost 3.000 MORE (the extra `ManagedReceiverBinding` box the slice added on that path). THE INTERESTING PART IS THE FIRST ATTEMPT, WHICH THIS INSTRUMENT CAUGHT: making the gate itself return the union was tidier and cost +441/+873/+1,737/+3,465 allocations on rungs 2-5 OF THIS CORPUS, doubling with the program, because a Maxon union value is a heap object INCLUDING the payload-free `none` — so a union-returning gate allocates once per DOTTED CALL rather than once per managed-receiver site. Split into an allocation-free bool gate (`managedTopLevelBaseAt`: the name filter plus `staticCallClaimsBase`) in front of the one boxing resolution, and the corpus returned to zero. The narrow ANSWER was not the cheap thing; the narrow QUESTION was. GATES: build exit 0 zero warnings, x64 1692/0, wasm32-wasi 1497/0, --workers=1 byte-identical to --workers=12, fragments zero M (so codegen is byte-identical), verify-warm-rebuild PASS on six managed-global programs incl. 1,024 globals and 4,096 receiver sites, no exit 101. | 619,768 | 1,123,586 | 2,130,788 | 4,143,679 | 8,170,294 | 16,225,841 |
| 2026-07-26 | P1.7 slice 2b — top-level ARRAY LITERAL globals (`let` and `var`). ALLOCATIONS MOVED BY EXACTLY ZERO at every rung (619,768 / 1,123,586 / 2,130,788 / 4,143,679 / 8,170,294 / 16,225,841), and getting there took finding TWO regressions this instrument caught, both of the same shape the last row warned about — a Maxon union value is a HEAP OBJECT, so a union placed on a per-SITE path allocates once per site forever. (1) The dedup that folded `arrayElementIsManaged` + `arrayElementDropCallee` into one union-returning `arrayElementDestroy` door cost EXACTLY +1 allocation per ARRAY-CREATION SITE — measured by same-lineage A/B on equal-length binaries at 32 sites -> +32 and 64 sites -> +64, and the scale corpus read +32/+64/+128/+256/+512/+1,024, which is its array-create knob doubling. The union now serves only the top-level-array-global path, which builds it ONCE PER MANAGED GLOBAL; the parser's per-site emitter composes the same two accessors base did, so the DECISION is still single-homed in `arrayElementIsManaged` and what is written twice is a three-line composition. (2) Splitting `declFor` into a shared `resolveDecl` + a filter re-BOXED the resolved answer, costing +1 allocation per name resolution that is not a local (+284 at rung 0 doubling to +9,212). Restored to the base two-probe shape, and the kind-free probe it existed for was deleted: the assignment rejection turned out never to need one, because the caller that meets a stored-but-immutable binding already holds the resolution that proves the name is declared. A THIRD attempt — sharing ONE module-level `TopLevelDeclLookup.notFound` to make both miss paths allocation-free — is recorded as a DEAD END: it built and then failed 128 spec tests with nil-pointer panics, so a payload-free union box is NOT safe to share the way `sharedEmptyInstanceAliasMap` is. BYTES moved and the movement is structural, attributed by the same A/B: +24 bytes per TOP-LEVEL DECLARATION (`TopLevelDecl` gained `hasStorage` — the one criterion deciding which of the two use-doors a binding goes through, now that an array `let` is immutable AND stored) and +8 bytes per TOP-LEVEL GLOBAL READ (`TopLevelGlobalLookup.found` gained `mutable`, which is what makes `Live.push(1)` an E3019 and `Live = [...]` an E2013 where `Buffer` accepts both), plus 48 bytes flat per compile. Both are linear constant factors on a 36 MB rung-0 compile (0.009%), and neither changes a growth rate. WHAT THE RUNG ADDED: an array initializer folds to its elements in the SAME scalar constant evaluator (`[BASE, BASE + 2]` works), `__module_init` builds the record with `__arr_create` + one `__arr_push` per element and CLONES a String element so the array may own it, and v1's `ConstantArrayLiteralRdata` element-buffer-into-rdata pass was deliberately NOT ported (it is an optimization, and it would fork a second element-emission path before the first is proven). GATES: build exit 0 zero warnings, x64 1710/0, wasm32-wasi 1515/0, --workers=1 byte-identical to --workers=12, fragments 18 additions and ZERO M (so no existing program's codegen moved), verify-warm-rebuild PASS on an array-globals program (determinism + cache + all four invalidation probes), no exit 101 on a leak battery incl. String elements, a 300-iteration push loop, a reassign and an untouched global. | 619,768 | 1,123,586 | 2,130,788 | 4,143,679 | 8,170,294 | 16,225,841 |
| 2026-07-26 | P1.7 slice 2b OPTIMIZATION PASS — ONE QUADRATIC FOUND AND FIXED, AND THIS INSTRUMENT IS STRUCTURALLY BLIND TO IT. ALLOCATIONS AND BYTES MOVED BY EXACTLY ZERO at all six rungs against the rung row (619,768 / 1,123,586 / 2,130,788 / 4,143,679 / 8,170,294 / 16,225,841 and 36,020,134 … 1,151,286,283), and codeBytes is identical too (63,349 / 117,262 / 225,250 / 441,270 / 873,434 / 1,738,402) — so this row is a VERIFICATION of the rung plus one change the corpus cannot see. (1) THE FIND: `Parser.immutableGlobalReads` — the slice's new "was this value read out of an IMMUTABLE global's slot?" mark, which is what refuses `var b = A` on a `let` array (E2015) — was a `ValueIdArray` LINEAR-SCANNED at the promotion gate. That is O(1) only if one of the two populations is bounded and NEITHER IS: the marks are one function's immutable-global reads, the scans are its borrowed managed-aggregate promotions, so a single function carrying K of each pays Theta(K^2). MEASURED by same-lineage A/B on a ladder of K interleaved `g(A)` reads and `b = s` promotions against the IDENTICAL program with `A` declared `var` (which marks nothing, so the two differ in exactly this structure), min-of-5, idle box: the scan's own cost was 3.52 / 15.55 / 65.22 / 259.55 ms at K = 2,000 / 4,000 / 8,000 / 16,000 — per-doubling x4.41, x4.19, x3.98, a textbook quadratic — and 39% of `phase:parse` at the top rung, with the `let` variant's parse bending x2.11/x2.29/x2.47 while the `var` control stayed flat at x1.96/x2.00/x1.99. THE FIX is a `Set with ValueId` (`ImmutableGlobalReadSet`) probed with `contains`, the same COW-shared-empty discipline `sharedEmptyInstanceAliasMap` uses, and the now-single-caller `valueIsImmutableGlobalRead` is inlined at its one gate. AFTER, same ladder: the `let` variant is LINEAR at x1.99/x1.98/x2.02, the isolated delta collapses from 259.55 ms to 5.79 ms at K = 16,000 and stops growing, and parse at K = 16,000 goes 669.85 -> 410.87 ms (1.63x). It cost +45..+55 allocations at K = 2,000..16,000 (the hash table's own buckets against the list's geometric growth) and ZERO on every program that does not mark. (2) WHY THIS ROW READS ZERO, WHICH IS THE POINT AND NOT AN OVERSIGHT: the old scan ALLOCATED ALMOST NOTHING (+21 allocations at K = 16,000 — the list's growth), so a memory instrument could never have seen it; it was found by reading and confirmed with `--metrics`. And the corpus declares no array `let`, so `recordGlobalReadValue` never marks, the copy-on-write never fires, `Set.contains` on the zero-capacity shared empty returns before it hashes anything, and the shared empty itself is a module-level `let` built before the measurement window opens. Pay-for-use, working. (3) THE OTHER TARGETS ARE LINEAR AND ARE NOW MEASURED, NOT ASSUMED. `initialRecordOf`'s array arm (`__arr_create` + one `__arr_push` per element) is O(elements): a ladder of a single top-level literal at N = 256..4,096 reads total x1.30/x1.51/x1.68/x1.85 with `phase:signatures` and `phase:lowerMaxonToStd` allocations DOUBLING exactly per rung (+513/+1,026/+2,050/+4,098 and +514/+1,028/+2,053/+4,101). Its one bend — `regalloc:liveness` at x2.22 — is NOT the new arm: an in-body `var A = [ … ]` CONTROL of the same N bends identically (x2.19) and is in fact slightly dearer overall (146.60 vs 137.33 ms at N = 4,096), so that is the shared one-huge-block shape meeting the budgeted per-split liveness recompute. `hasStorage` asked mid-evaluation (`evalConstNamed` -> `constantDeclFor`) is O(1) — two `declIndex` hash probes then one array index, no arena re-walk — confirmed on a ladder of N array `let` globals each read once at N = 64..1,024: total time x1.94, allocations x1.95, `phase:signatures` x1.99, `phase:parse` x1.98. (4) BOTH ACCEPTED CONSTANTS VERIFIED BY A/B PROBE AND BOTH STAND. `ArrayElementDestroy` on the parser's per-SITE emitter costs EXACTLY +1 allocation and +16 bytes per array-creation site (32 sites: 82,850 -> 82,882; 64 sites: 226,749 -> 226,813) — reproduced to the unit, and the decision stays single-homed in `arrayElementIsManaged`, which both sites read. A kind-free shared `resolveDecl` + filter costs EXACTLY +1 allocation per non-local name resolution (+256/+512/+1,024/+2,048/+4,096 on the globals ladder, doubling perfectly) because `TopLevelDeclLookup` is a union and a union value is a heap object; the two-probe shape stays. RESIDUAL FOR THE REVIEWER, not fixed here because it is a duplication question and costs nothing either way: `ParseError.assignToImmutable(String.from(nameTok.value), line: …, column: …)` is now constructed at THREE sites in Parser.maxon (8045 new, 8103 and 8406 pre-existing) and is on the throw path only, so single-sourcing it needs no probe and no allocation — it is the *resolutions* that legitimately cannot share, not the diagnostic. (5) v1's `ConstantArrayLiteralRdata` DECLINED, with the number that would reverse it: the present path costs ~63 allocations and ~30 us per constant element (regression over N = 256..4,096), which is real but LINEAR, and it is the language's array-literal cost rather than the top level's — the in-body control pays the same. Moving the element buffer to `.rdata` would fork a second element-emission path for a shape no source in this tree has yet, and its `capacity = -2` encoding needs a `__arr_push` detach the runtime does not have, which is a rung and not an optimizer pass. RE-MEASURE TRIGGER: a machine-generated all-integer top-level table large enough that `__module_init`'s 2 ops per element dominate its compile. GATES on this tree: shv2 build exit 0 zero warnings (139 codes); x64 spec-test 1710/0 exit 0 no leak at BOTH `--workers=12` and `--workers=1` with BYTE-IDENTICAL stdout; wasm32-wasi 1515/0 exit 0; `specs-shv2/fragments` ZERO modified and the whole worktree clean but for the one source file, so codegen is byte-identical; `verify-warm-rebuild` PASS on an array-globals program; leak battery (String elements, 300-iteration push loop, reassign, untouched global) exit 0; and the eight behaviours the rung pinned all reproduce — 2, 9, 42, `hi hi! hi`, E2015 on the writable alias, E2004 in both declaration orders. | 619,768 | 1,123,586 | 2,130,788 | 4,143,679 | 8,170,294 | 16,225,841 |
| 2026-07-26 | P1.7 slice 2b REVIEW — a MERGE PHI now inherits the immutable-global-read mark, and it costs ZERO. ALLOCATIONS AND BYTES MOVED BY EXACTLY ZERO at all six rungs against the optimizer's row (619,768 / 1,123,586 / 2,130,788 / 4,143,679 / 8,170,294 / 16,225,841 and 36,020,134 … 1,151,286,283). THE DEFECT: the slice's E2015 refusal of `var b = <a `let` array>` keys on a mark that rides the VALUE — deliberately, so it needs no second encoding of the name-resolution order — but a value MERGE mints a NEW value, so the mark stopped at every join and the refusal was bypassed by writing the same thing one keyword longer. MEASURED at 3 where 2 is correct through FOUR shapes that all compiled clean: `var pick = A if c else M`; the degenerate `A if c else A` (both arms the same immutable global); a reassignment of an already-owned `var` from a ternary; and a `match … gives A` arm — with E2013, E3019 and the direct E2015 all intact and nothing to report any of them. THE FIX is one call at the one site that mints a result phi (`finalizeMatchMerge`), so the ternary and the match-expression are closed by construction rather than by two edits: `propagateImmutableGlobalReadToPhi` marks the phi if ANY reaching give is marked. The mark's WRITE is now single-homed in `markImmutableGlobalRead` — that half is the dangerous one, because a second insert site that skipped the copy-on-write identity test would insert into the MODULE-LEVEL `sharedEmptyImmutableGlobalReads` and every function in every file across the per-file parse fan-out would then see a mark naming an unrelated value in its own SSA space. WHY THIS ROW READS ZERO, which is the point: the new loop runs once per value-merge arm and its only work is `Set.contains` on the zero-capacity shared empty, which returns before it hashes anything, and the corpus declares no array `let` so nothing is ever marked — the same pay-for-use shape the optimizer's row established, extended to a second reader. NOT over-refused, verified by A/B: a merge of two MUTABLE globals still binds and still SHARES (new spec `top-level-array-merge-of-mutable-globals-shares`, P grows to 3 and Q stays 2), a `let` binding of a merge that includes an immutable global is still legal, owned array-literal merges still work, and String `var` globals still merge and COPY on promotion. STILL OPEN and reported to the coordinator, not fixed here: the guard is INTRA-FUNCTION and stops at a call — `g(A)` whose callee does `var b = xs` still grows a `let` array (measured: 3), because whether a parameter may hold an immutable global's record is a property of every call site, not of the callee, so it needs the whole-program call-graph fixpoint the transitive-consume case waits on. The reference compiler does not refuse the call form either (it shares and then leaks, exit 101), so shv2 is no looser, only not yet stricter; the bound is now stated in `specs-shv2/static-variables.md` and at the refusal site rather than left to be discovered. ALSO in this pass: `ParseError.assignToImmutable` single-sourced into `assignToImmutableError(nameTok)` (the optimizer's handed-over residual — three sites, throw path only, no probe and no allocation), and `builtArrayRecord` now takes `valueType`/`range` like its two sibling arms instead of the whole `ManagedGlobal`. GATES: shv2 build exit 0 zero warnings (139 codes); x64 spec-test 1713/0 exit 0 no leak at BOTH `--workers=12` and `--workers=1` with BYTE-IDENTICAL stdout; wasm32-wasi 1518/0 exit 0; `specs-shv2/fragments` ZERO modified (three ADDED for the new tests); leak battery (String elements read/pushed cross-function/300-iteration loop/reassigned/untouched/emptied via clear and pop/element overwritten by set) all exit 0; and the rung's pinned behaviours all reproduce — 2, 42, 9, byte-string 99. | 619,768 | 1,123,586 | 2,130,788 | 4,143,679 | 8,170,294 | 16,225,841 |
| 2026-07-26 | P1.7 slice 3 — DEAD-GLOBAL ELIMINATION. A top-level binding nothing reads or writes no longer reaches the binary: its `.data` slot, the `__module_init` ops that build and store its record, and the `__maxon_global_cleanup` drop that releases it are all produced from ONE filtered arena, so the three go together by construction rather than by three edits in sequence. MEASURED acceptance: `var UNUSED = [1,2,3]` in a program that never mentions it went 3,676 -> 1,512 emitted code bytes, which is EXACTLY the floor for the same program with no global at all; a dead `var`/`let` String and a dead byte-string likewise land on 1,512; and the guard held — a write-only `SINK.push(7)` global stayed at 3,680, unmoved. THE MOVEMENT IN THIS ROW IS THE PASS'S WORKING SET AND NOTHING ELSE, established by A/B against a base binary built from f0308a29d and RUN FROM THIS SAME WORKTREE PATH (path length shifts the byte totals by a constant +1,428, so a cross-path comparison would have mis-attributed it): (1) +8 bytes in `phase:load`, CONSTANT at all six rungs — the new `ProgramSignatures.storedGlobalCount` field on the `SignatureMemo.empty()` placeholder, one 8-byte field crossing an allocator size class, once per compile; (2) +22/+28/+34/+40/+46/+52 allocations and +3,224/+6,416/+12,680/+25,088/+49,784/+99,056 bytes in the UNATTRIBUTED bucket (`liveGlobalLabels` runs in `compileToCodeResult` between phase probes) — the `LiveGlobalLabelSet` itself, one entry per LIVE top-level global. The corpus DOUBLES its top-level `var` knob per rung (32 -> 1,024 globals), and the bytes double with it at x2.00: this is LINEAR in globals declared, which is the pass's irreducible working set, and it is 0.0003% of allocations and 0.0086% of bytes at rung 5. The +6 allocations per DOUBLING are the Set's log2(K) rehash steps, not per-op work — and the direct evidence for that is that EVERY PER-PHASE ALLOCATION COUNT IS BIT-IDENTICAL base vs head at all six rungs, so the O(ops) label walk allocates nothing per op. The walk is also SKIPPED ENTIRELY when the program declares no stored global (`storedGlobalCount == 0`), which is most of the spec corpus — a second unconditional full-module walk would have been a per-compile TIME cost allocating nothing, i.e. exactly the shape this instrument is blind to. NOT PRE-SIZED, with a reason: `stdlib/Set.maxon` has no `reserve`, the exact bound is known (`storedGlobalCount`), and adding one is a stdlib change outside this rung's file list — reported to the coordinator rather than taken. CODEGEN ON THE LADDER IS UNMOVED: `codeBytes` is bit-identical at all six rungs (63,349 / 117,262 / 225,250 / 441,270 / 873,434 / 1,738,402), because every corpus global is both read and written and so every one of them is live. GATES: shv2 build exit 0 zero warnings; x64 spec-test 1720/0 exit 0 no leak at `--workers=12` AND `--workers=1` with BYTE-IDENTICAL stdout; wasm32-wasi 1521/0 exit 0; `verify-warm-rebuild` PASS on determinism + cache + invalidation; ONE fragment modified (`static-variables/top-level-array-grow-reassign-untouched`, whose program deliberately declares a never-touched array global — its slot and init run are now correctly absent, prose updated) and the rest ADDED. | 619,790 | 1,123,614 | 2,130,822 | 4,143,719 | 8,170,340 | 16,225,893 |
| 2026-07-26 | P1.7 slice 3 OPTIMIZER — NO CODE CHANGE, and that is the finding. Every number in this row is bit-identical to the implementer's (allocsDelta 0 and bytesDelta 0 at all six rungs) because nothing was changed; this row records the VERIFICATION and the two axes the ladder structurally cannot reach. THE IMPLEMENTER'S ROW REPRODUCES EXACTLY, re-derived independently against a base binary built from f0308a29d in its OWN worktree and run from an EQUAL-LENGTH path (.../worktrees/base-globals vs .../worktrees/dead-globals, both 54 chars): +22/+28/+34/+40/+46/+52 allocations and +3,224/+6,416/+12,680/+25,088/+49,784/+99,056 bytes, +8 bytes in phase:load CONSTANT at all six rungs, and EVERY per-phase allocation count bit-identical base vs head at all six rungs. The path-length phantom is real and was controlled rather than assumed: the base binary reads bytesDelta -408 CONSTANT at all six rungs against this log's last row (produced from the dead-globals path) while head reads 0 — a perfectly reproducible constant that looks exactly like a code change and is not one. THE LADDER CANNOT SEPARATE OPS FROM GLOBALS, so both axes were measured on purpose-built corpora with --metrics. (1) MANY OPS, ONE GLOBAL — 64 to 2,048 chained functions, a 32x op span, with one live global so the walk actually RUNS: unattributed allocations FLAT at 707 across the whole span (689 with the walk short-circuited). The O(ops) label walk allocates EXACTLY NOTHING PER OP, measured with it running, which is a stronger statement than the ladder can make because the ladder never varies ops against a fixed global count. Its time is 0.04 -> 0.67 ms across 32x — linear, and 0.08% of the 825 ms compile. (2) MANY GLOBALS, FEW OPS — 32 to 16,384 DEAD globals: unattributed allocations FLAT at 689, time 0.35 -> 2.55 ms, linear; the dead path costs nothing beyond the two arena walks. (3) THE DIAGONAL, ops AND globals together, 4,096 -> 16,384 LIVE globals with one reader per 8: unattributed x2.05, x2.05 and total x1.98, x1.99 — THERE IS NO O(ops x globals) TERM. LiveGlobalLabelSet's mechanism is nailed rather than inferred: +6 allocations per DOUBLING is 6 per Set.grow (three arrays, two allocations each) over log2(K) grows, while BYTES are linear at ~96/global because the grow at step j allocates a table of size 2^j — log allocations and linear bytes are ONE mechanism, and it is the whole of the slice's added memory; nothing else in it allocates. contributesStorage is O(1) and re-derives no liveness (2 x 16,384 calls at 689 allocations flat). UNION BOXING, the tenth check: the slice introduces no new union; MaxonOp is matched BY VALUE once per op and costs zero allocations across the 32x op span, and TopLevelConstant likewise — no shared payload-free box was attempted. SET.RESERVE PRICED AND DECLINED: it would turn 6*log2(K) allocations into 6 and roughly halve the set's bytes (a doubling chain sums to 2x the final table, one exact allocation is 1x), which at rung 5 is ~50 KB of 1.15 GB = 0.004%, and on the real spec corpus is ZERO — with 0 stored globals the walk never runs and with 1 the set never grows past its initial 16. It changes a CONSTANT, not a curve, in a type every compiled program and the compiler itself share. NEW MEASURED DEBT, reported to the coordinator and NOT fixed: stdlib/Array.hash is djb2 masked to 32 bits and Set/Map index with the LOW bits, so identifiers sharing a prefix and differing only in a numeric suffix cluster hard under linear probing — 77.5 probes per insert at K=16,384 against 0.53 for well-distributed keys, a 146x constant. IT IS INVISIBLE TO THIS INSTRUMENT AND VISIBLE ONLY IN TIME: same binary, same K, IDENTICAL allocation and byte counts, unattributed time x3.66 per doubling with G<n> names against x2.05 with identifier-like names, phase:signatures x2.39 against x2.00 and phase:parse x2.25 against x2.00 on the same swap. Measured linear-in-practice today — this corpus's own gv<n> knob tops out at 1,024 globals where it costs ~7,800 extra probes in total — and it is erratic (hash/capacity resonance), not superlinear. The trigger that would make it bend is machine-generated code minting >=8k prefix+numeric identifiers in one namespace. Not fixed because the fix is in stdlib/Array.hash, shared by every Set and Map in every program, and a hash change reorders every Set/Map iteration — a codegen risk out of all proportion to a cost nothing real pays. GATES on the unchanged binary: build exit 0 zero warnings; x64 spec-test 1720/0 exit 0 no leak at --workers=12 AND --workers=1 with BYTE-IDENTICAL stdout; wasm32-wasi 1521/0 exit 0; git status EMPTY after two full x64 suites and a wasm suite, so specs-shv2/fragments is clean and codegen is byte-identical; dead array global and dead String global both 1,512 emitted bytes, exactly the floor of a program with no global, and a write-only global stays unshrunk. | 619,790 | 1,123,614 | 2,130,822 | 4,143,719 | 8,170,340 | 16,225,893 |
| 2026-07-26 | STACK ARGUMENTS (>6 parameters). ALLOCATION-NEUTRAL AND FLAT: allocsDelta 0 at all six rungs, and bytes +612 CONSTANT across the whole 32x span — measured against a base binary built from ca5954754 in its own worktree, not inferred from this log's previous row. Per-phase attribution of the 612: +408 phase:load, +24 phase:parse, +180 outside any named phase, every one of them the SAME number at rung 0 and rung 5, so it is a fixed per-PROCESS cost and not proportional to ops, functions, calls, parameters or files (the most likely source is the compiler's own longer diagnostic strings: the two refusals this rung replaces were ~430 chars and the two it adds are ~800, at the same allocation COUNT, so each lands in a larger slab size class). THE PATH-LENGTH PHANTOM THE PREVIOUS ROW WARNS ABOUT WAS TESTED AND REFUTED HERE rather than assumed: running the identical binary through a junction with a 12-char worktree name (dead-globalz) instead of the real 10-char one (stack-args) reproduced -408 against this log EXACTLY, so the offset against the log row is not path length; the honest comparison is base-vs-head in the same tree, which is what the +612 above is. TIME, which this instrument cannot see: rung 5 compiled in 10602/10834 ms on base and 10654/10509 ms on head — inside noise, head faster on the second pair. That matters because the rung REPLACED the caller's running gpr/fp counters with per-argument calls to the shared placement rule (abiFileSlotIndex / abiStackSlotsBefore), which is O(args) per argument instead of O(1) — quadratic in the ARGUMENT COUNT, bounded by MaxAbiArgs=64 (so <=4096 bit tests at the worst signature the language accepts, and ~9 at the corpus's typical 3), and it is bought back on the callee side, where the per-parameter computeParamSlotIndex walk of the parameter TYPES is gone: each function now folds ONE ArgFloatMask and every param op reads a scalar. Net: no allocation moved, no curve bent, and the only number that changed is a constant. | 619,790 | 1,123,614 | 2,130,822 | 4,143,719 | 8,170,340 | 16,225,893 |
| 2026-07-26 | STACK ARGUMENTS OPTIMIZER — NO CODE CHANGE, and the reason is measured. Every number here is bit-identical to the implementer's row above. ⭐ THE ROW ABOVE'S "+612 bytes CONSTANT" IS NOT REAL; READ IT WITH THIS CORRECTION. A base binary built from ca5954754 by the SAME bootstrap, in a worktree named to the SAME LENGTH (.../worktrees/stack-base vs .../worktrees/stack-args, both 85-char exe paths), produces BIT-IDENTICAL allocations AND bytes at all six rungs — 36,022,542 / 68,168,287 / 136,587,408 / 275,106,314 / 560,702,945 / 1,151,384,523 on both sides. The rung costs nothing HERE because its new paths are DEAD on this corpus: the generated code's maximum arity is TWO, and zero functions exceed the old six-parameter cap, so THE LADDER STRUCTURALLY CANNOT EXERCISE THIS RUNG. Control that the A/B is real: the binaries differ (7,983,616 vs 8,026,112 bytes), and base REFUSES a 7-parameter program with E2015 while head compiles it and returns 28. ⭐⭐ THE PATH-LENGTH DISPUTE IS SETTLED IN FAVOUR OF THE PREVIOUS ROW. Executable path length is CAUSAL for the byte column at EXACTLY 200 BYTES PER CHARACTER, with allocation counts totally invariant. Measured two independent ways. (1) One 7-line file, one binary, four junction paths of name length 1/10/30/60: total +200/char, decomposing into phase:load 132/char + unattributed 60/char + phase:parse 8/char, which sum to exactly 200 — every coefficient an exact integer over three independent spacings, and NO OTHER PHASE MOVES AT ALL (isel, regalloc, encode, lex, merge, signatures are completely path-insensitive). Source path is 29/char (unattributed 21 + parse 4 + load 4); output path 5/char (unattributed 3 + writeExe 2). (2) On THIS ladder: the same base binary through two junctions in the same parent, names differing by exactly TWO characters, reads 36,037,142 vs 36,037,542 — a difference of EXACTLY 400 bytes, constant at every rung, allocations identical at 619,790 both. So `dead-globals`(12) vs `stack-base`(10) predicts −400 where the instrument reads −408: 98% of that −408 was path length, and the residual −8 is a real code difference from the review commit between the logged row and ca5954754. ⚠ THE REFUTATION IN THE ROW ABOVE INVERTS THE TEST. A path artifact is a FIXED NUMBER OF COPIES OF THE EXECUTABLE'S OWN PATH (~200 per process), so it is IDENTICAL AT RUNG 0 AND RUNG 5 and moves no allocation — which is precisely the signature that row cites as proof its +612 was "a fixed per-PROCESS cost" and "not proportional to ops, functions, calls, parameters or files". That signature does not discriminate between the two hypotheses: it is what BOTH look like. The discriminator is the COEFFICIENT. ⇒ STANDING RULE FOR EVERY FUTURE A/B IN THIS PROJECT: the ALLOCATION column is immune to path length and is the trustworthy instrument; for the BYTE column either equalize the WHOLE executable path character-for-character or subtract 200 × (Δchars). An "equal-length worktree NAME" is not the control, because the coefficient applies to the whole path, not the name. SUPERLINEARITY AUDIT (the mandate). The rung's shared placement rule — abiFileSlotIndex / abiArgIsOnStack / abiStackSlotsBefore in Targets/Shared/StdLoweringShared.maxon — IS Θ(N²) per call site in the ARGUMENT COUNT: each of the three walks the preceding arguments, and emitArgMovesByFloatMask calls them once per argument. NOT FIXED, because the bound is real and measured: N ≤ MaxAbiArgs = 64, enforced by a parser diagnostic, so the program-wide total is 64 × (total arguments) = LINEAR IN PROGRAM SIZE. Two purpose-built ladders, separated as the mandate asks. (A) TOTAL ARGUMENTS HELD CONSTANT at 1,024 while arity goes 8→16→32→64: phase:isel is FLAT at 1.55 / 1.48 / 1.55 / 1.79 ms — the entire quadratic term is +0.24 ms at the worst arity the language accepts, inside a 3,273 ms compile — while regalloc over the same ladder runs 9 → 865 → 2,526 → 3,251 ms, which is the BUDGETED SplitLiveRanges debt (ARCHITECTURE.md:1336) and is what a naive many-args ladder actually measures. (B) ARITY FIXED AT THE 64 CEILING while call sites go 8→16→32→64: isel ×1.85 ×1.89 ×1.96, converging on ×2.00 — LINEAR in call sites. Denominator: on the real rung-5 corpus isel is 3.07% of the compile (310.9 of 10,129 ms), behind regalloc at 38.6% and parse at 37.3%. UNION BOXING, THE ELEVENTH CHECK — CLEAN, and measured on the per-argument path rather than reasoned about. Allocations per ARGUMENT across the register→stack boundary, at 64 fixed call sites: FLAT on x64 at +5.05/arg through arity 5..10 (x64 crosses into stack slots at 7), and on arm64 they DROP from +5.05 to +4.03 at arity 9, where the new arm64StoreOutgoingArg replaces a movRegReg — the new op carries a plain integer slot instead of a second TargetVReg, so the stack path allocates one FEWER per argument than the register path it parallels. The new recordOffset field on arm64Prologue/arm64Epilogue is per-function and free: base vs head on an arity-6 arm64 cross-compile are identical in every metrics row. REPORTED, NOT FIXED (a constant factor, and unmeasurable): calleeArgFloatMask rebuilds bit-by-bit, with N ByteArray-keyed Map lookups, the very mask that IS the stored map value — one lookup would do, and it runs twice per direct call op. It allocates nothing and sits far below timing noise (~0.2% of a compile at the corpus's typical arity), so changing it here would be a change that only LOOKS like an optimization; it goes to the reviewer as redundancy instead. GATES on the unchanged binary: x64 spec-test 1736/0, exit 0, no leak; git status EMPTY after the suite, so specs-shv2/fragments is clean and codegen is byte-identical. | 619,790 | 1,123,614 | 2,130,822 | 4,143,719 | 8,170,340 | 16,225,893 |
| 2026-07-26 | PARAMETER-MUTATION ANALYSIS (E3019 call-site half). ⭐ ALL OF IT IS phase:parse, AND IT IS ONE ALLOCATION PER SOURCE-WRITTEN CALL SITE. A/B against a base binary built from b408023ff IN THIS SAME WORKTREE (so the executable path is character-identical and the previous row's 200-bytes-per-char rule does not apply to this comparison): rung0 parse 113,418 -> 113,912 (+494) and semanticCheck 1,878 -> 1,974 (+96); rung3 parse 790,037 -> 793,345 (+3,308) and semanticCheck 13,288 -> 13,414 (+126). The corpus has 559 / 3,709 call-ish sites at those rungs, so the parse term is +0.88 and +0.89 allocations per call site — the new `MaxonOp.call.argImmutableNames` column, one `ByteArrayArray` built per source call by `pushCallArg` alongside the three columns already there. It is LINEAR by construction and the ladder agrees: totals +590 / +1,000 / +1,811 / +3,434 / +6,667 / +13,116, ratios x1.69 x1.81 x1.90 x1.94 x1.97 converging on x2.00. The column is needed because the fact is SYNTACTIC: `let a = p` carries the parameter `p`'s own ValueId while being a `let`, so a per-VALUE mark would refuse `f(p)` in any function that also wrote `let a = p` (measured: the oracle refuses `f(a)` and accepts `f(p)`). `settledImmutableArgNames` substitutes a shared empty array when no argument names an immutable binding, so the transient dies instead of living as long as the module — that saves RETAINED bytes, not the allocation. ⭐⭐ THE WHOLE-PROGRAM FIXPOINT COSTS A NEAR-CONSTANT ~100 ALLOCATIONS, AND THE FIRST DRAFT DID NOT. It was written with a `Map with (ByteArray, OpIndexArray)` reverse-edge index — exactly the "Array with (a per-node array)" shape `CsrGraph.maxon`'s header forbids in as many words ("a new adjacency belongs here rather than hand-rolled at its call site"). MEASURED on a purpose-built ladder that doubles functions, call sites AND depth together — a maximal chain f0->f1->...->f(N-1) where the last pushes, so EVERY function ends up in the summary and every gain re-enqueues its caller, the worst shape a mutation fixpoint has: phase:semanticCheck allocations base / map-index / CSR were 3,215 / 4,199 / 3,357 at N=400 and 51,215 / 64,319 / 51,453 at N=6,400. The map index cost +984 -> +13,104 (LINEAR in the call graph, one array per callee); the CSR costs +142 -> +238 (six dense columns' geometric growth, i.e. logarithmic). 55x less at N=6,400, and the drain loop now does ZERO hashing because a callee is a function INDEX in the CSR, not a name. The published summary went the same way: a dense MutatedParamMaskArray by function index plus the ONE name->index map the edge walk already needs, instead of a second name-keyed map of masks built by N upserts. TIME on that same ladder, base vs CSR head: 1.3/2.7/5.7/12.3/25.8 ms vs 2.7/6.2/12.4/27.3/59.8 ms — head/base is a CONSTANT 2.08 2.30 2.18 2.22 2.32, i.e. the SAME growth exponent as the base pass (both ~x2.1 per doubling), so the fixpoint is linear in the call graph and what remains is a constant factor: the edge walk visits every op of every function, a second module walk that a whole-program summary cannot avoid (it must be COMPLETE before the first call site is checked). On the real corpus semanticCheck is 53,128 of 16,239,569 allocations at rung 5 (0.33%), so that constant is invisible there. NOT FIXED, REPORTED: the per-call `argImmutableNames` array could be eliminated entirely by accumulating into a per-parser FLAT stack with a mark per call — the `Scope` frame idiom this compiler already uses — copying a slice out only for the calls that actually carry an immutable-binding argument. It is worth ~+13,000 allocations at rung 5 (0.08% of the compile) against a nesting-correctness requirement in `pushCallArg`, the one writer of the call's parallel columns; measured and left for the optimizer rather than taken here. BYTES: +126,448 / +234,448 / +424,152 / +834,856 / +1,666,739 / +3,326,755, ratios x1.85 x1.81 x1.97 x2.00 x2.00 — linear, same term. Against the PREVIOUS logged row (worktree `stack-args`, 10 chars, vs `param-mutation`, 14) the standing 200-bytes-per-character rule adds a constant +800 to every rung of that comparison, which is 0.7% of the smallest delta and none of the shape. GATES: x64 spec-test 1769/0 exit 0 no leak; wasm32-wasi 1569/0 exit 0; --workers=1 and --workers=12 byte-identical; specs-shv2/fragments additions only, zero M on any pre-existing golden. | 620,380 | 1,124,614 | 2,132,633 | 4,147,153 | 8,177,007 | 16,239,009 |
| 2026-07-26 | OPTIMIZATION PASS over PARAMETER-MUTATION. ⭐ MEMORY IS BIT-IDENTICAL ON ALL SIX RUNGS (allocs delta 0, bytes delta 0) — the one change is a pure-TIME fix, which is the class scale-test is structurally blind to and which was found by READING plus a ladder. ⭐⭐ WHAT WAS FOUND: `Project.argSlotPosition` resolved a labelled argument by `FuncSignature.indexOfParam`, a SCAN of the whole parameter-name list. Maxon spells calls `f(a, b: b, c: c)` — labels in declaration order — so that scan cost 1+2+...+P comparisons, i.e. O(P^2) PER CALL, and this rung made the program ask for that mapping THREE times per call (slotCallArgs, addMutationEdges, checkImmutableArgToMutatingParam) where base asked once. Testing `paramNames[argIndex]` first collapses the common case to O(P) per call; the reordered case falls through to the same scan after one extra comparison. It is EXACTLY equivalent, not an approximation: a repeated parameter name is E2014 at the declaration (measured), so a hit at `argIndex` means `indexOfParam` has no earlier match to find. MEASURED on the new, COMMITTED `Testing/ladders/genmutchain.sh 1 256 <P>` (one chain, depth 256, every argument labelled in declaration order), phase:semanticCheck: P=1..64 was 1.207/1.532/2.353/4.172/8.659/22.887/69.820 ms and is now 1.195/1.493/2.099/3.357/5.802/10.869/20.524 ms. Growth per DOUBLING of P falls x3.05 -> x1.89 while the program itself doubles at x2.00, so the quadratic TERM is gone rather than merely smaller — and 20.5 ms lands BELOW the pre-rung base's 39.6 ms, which had the same scan and paid it once instead of twice. ⚠ ON THE REAL CORPUS IT IS WORTH ~1.5%: rung3/4/5 semanticCheck 16.90/34.51/69.74 ms -> 16.55/33.95/68.72 ms, because the corpus's functions have FEW parameters. The ladder is pathological on purpose and the corpus is what a real program looks like; both are reported per `Testing/ladders/README.md`. It is kept because it removes a growth term for six lines in ONE shared function with no new allocation, and because the trend is toward wider signatures — THIS RUNG widened `validateCall` and `checkSlottedCall` to 12 parameters each. ⭐ THE FIXPOINT ITSELF WAS LADDERED ON THREE SEPARATE AXES and is LINEAR. Axis A, DEPTH AT CONSTANT PROGRAM SIZE (chains x depth = 2048, so a 1024x change in depth at ~constant source bytes) — the decisive experiment the implementer's ladder could not run because it doubled functions, call sites and depth together: semanticCheck allocations 17,602 -> 16,589 (FLAT, drifting DOWN as fewer chain heads mean fewer main call sites) and time 10.2 -> 13.4 ms NON-MONOTONICALLY (20.3 at depth 512, 17.4 at 1024, 13.4 at 2048), i.e. noise, not a depth term. Axis B, SIZE at constant depth: 16.8x source -> 15.5x allocations, 19.2x time, per-doubling 2.08 2.11 2.03 2.16. Both linear. The worklist's `queued` flag is what earns this — a caller is re-enqueued only on a real MASK CHANGE and duplicate enqueues COALESCE, so the theoretical `MaxAbiArgs`=64 re-entry factor needs bits to arrive in separate rounds and the seed-first drain order prevents that. MEASURED DEBT (linear today, bounded by construction): the fixpoint is O(V + E x min(K,64)) worst case where K is a callee's parameter count; K cannot exceed `MaxAbiArgs`=64 (`requireAbiParamCount`), so this is a bounded constant that cannot bend with program size, and axis D shows the coalescing collapses it in practice. REPRODUCED BOTH ENDS of the previous row's headline against a base binary rebuilt from b408023ff at the SAME canonical path: axis C semanticCheck allocations base 3,215 / head 3,357 at N=400 and base 51,215 / head 51,453 at N=6,400 — the logged +142 and +238 CSR overhead confirmed exactly, and head/base time a constant 2.13 2.85 2.45 2.12 1.99 rather than a growing one. NOT TAKEN, WITH REASONS: (1) the previous row's own hand-off — replacing the per-call `argImmutableNames` transient with a flat stack + per-call mark — is worth ~13,000 allocations at rung 5 (0.08%) but requires a mark/truncate discipline in `pushCallArg` that must survive the parser's `throws ParseError` unwind; a leaked stack frame would mis-blame every subsequent call, which is a correctness hazard priced at 0.08% and declined. (2) Gating `checkImmutableArgToMutatingParam` on the O(1) `argImmutableNames.isEmpty()` before the `maskOf` hash was measured and NOT shipped: semanticCheck is 68.7 ms of a 10,143 ms rung-5 compile (0.68%), so a constant factor inside it is unobservable and shipping it would only look like an optimization. TARGETS CLEARED BY READING: `noteReceiverWrite` returns on `not mutatesReceiver` before any work and `immutableReceiverNameOf` returns a shared empty or a zero-copy token slice, so the parser seed costs nothing per non-writing method call; no union was introduced or widened beyond `MaxonOp`'s three call cases gaining one pointer column, and every downstream reader discards it with `_` rather than copying it. GATES: x64 spec-test 1769/0 exit 0 no leak; wasm32-wasi 1569/0 exit 0; --workers=1 and --workers=12 byte-identical (md5 d99c4330ea8dd5724ee5820e0cd4b92d); specs-shv2/fragments zero M after BOTH suites; labelled-call probes 7-params-in-order=28, 7-params-fully-reversed=42, unknown label E3037, duplicate label E3038, transitive var=9, transitive let=E3019. | 620,380 | 1,124,614 | 2,132,633 | 4,147,153 | 8,177,007 | 16,239,009 |
| 2026-07-26 | THE CPU-TIME COLUMN LANDS, and this row is its first datapoint: there is no previous CPU row to difference against, so the ## CPU table starts here. scale-test measured MEMORY ONLY and declared that blind spot on every run; a cost that re-walks tokens or chases pointers but allocates little read FLAT however quadratic it was. Mechanism: one bootstrap intrinsic __Builtins.threadCpuTicks() (QueryThreadCycleTime on Windows, clock_gettime(CLOCK_THREAD_CPUTIME_ID) on POSIX) bracketed by PhaseProbe INNERMOST, displacing the wall clock, because the tightest bracket belongs to the number people act on. WALL time is still never collected and that argument is unchanged: it counts every OTHER process, and one run once read phase:parse at x5.03 then x1.78 across a DOUBLING ladder, which is preemption and not a curve. CPU time cannot see preemption at all (verified: across a 300 ms sleep it advanced 837,520 ticks while wall advanced 301,000,000 ns). NOISE FLOOR, MEASURED not assumed, over three idle runs plus one against a machine loaded to every core: phases above 50M ticks read median 1.7% / worst 6.3%; small phases (~100K ticks) swing up to 31% and no ratio should be read off one; a FULLY LOADED box inflates every rung by +49%..+52% because this counter ignores PREEMPTION but not CONTENTION - yet it inflates them UNIFORMLY, so the rung-over-rung RATIO survived nearly intact (loaded x1.90/x1.98 against idle x1.86/x2.00). Trend ratios; treat absolutes as a fact about one machine on one day. --repeat=<n> (default 3) compiles each rung n times and keeps the MINIMUM CPU per phase, since noise can only ADD; the repeats also CROSS-CHECK that allocs/frees/bytes are IDENTICAL every time, a compiler-determinism gate the one-shot runner structurally could not make, and a difference is a BROKEN RUN rather than a number to average away. WHY THIS ROW MOVED: the previous row is the parameter-mutation optimization pass, logged at essentially origin's tip (its gates read 1769/0 against this row's 1774/0, a handful of tests apart), so these deltas are THIS change rather than a sum over an unlogged gap. (An earlier draft of this note claimed it spanned 23 unlogged upstream commits; that was wrong - upstream logged its row.) The shape is the load-bearing part: the instrument now costs a FLAT +200 allocations at every rung (it was +546/+716/+1056/+1736, GROWING, until regAllocAccumulated stopped routing through costOf). That mattered because returning a struct BY VALUE ALLOCATES - measured at exactly one allocation per return and INDEPENDENT of field count, 4/5/6 fields all costing 10,000 over 10,000 returns - and that function runs once per FUNCTION, so the instrument was moving the very column the log calls bit-for-bit reproducible. A constant offset cannot bend a curve; a growing one can. ALSO FOUND, unfixed: Array.resize is BROKEN for STRUCT element types, setting the length without giving the slots storage, so count() answers N while get(0) reports OOB - ScaleTestRunner.parseRungMetrics has carried that call for its whole life and survives only because it writes every slot before reading one. Gates: C# 3103/0, shv2 1774/0, no fragment churn, no exit 101. | 620,580 | 1,124,814 | 2,132,833 | 4,147,353 | 8,177,207 | 16,239,209 |
| 2026-07-26 | TWO TIME-ONLY SUPERLINEARITIES FOUND BY THE NEW CPU COLUMN AND FIXED, on its first ladder. Both allocate nothing, so every row above this one is blind to them: this is the column earning its place. (1) THE ENCODER MAP WAS QUADRATIC IN BLOCKS PER FUNCTION. X64Backend.emitFunctionChunk keyed a per-function block-offset Map by block id; Map is open-addressed with linear probing and int.hash() is the IDENTITY (stdlib/PrimitiveExtensions.maxon), so a key lands at id and (capacity-1). A function block ids are TWO CONTIGUOUS RUNS - its original blocks, then the blocks regalloc:criticalEdges appended - and layout order INTERLEAVES them (measured on a real 3,073-block function: 0 1 2049 2 3 2050 4 5 2051 ...). The high run WRAPS onto the low run slots, the two become one solid cluster, and probes per insert measured EXACTLY capacity/4 + 2/3 - collapsing to 1.00 the instant capacity first exceeds the id range. Capacity is proportional to blocks, so probes per insert is proportional to blocks: O(B^2) per function, 171.44 probes/insert against 1.00 for the same keys sorted. The key IS the index, so the Map is now two pre-sized arrays: O(B). Corpus phase:encode CPU ratios x1.61 x1.93 x2.22 x3.69 x2.58 -> x1.68 x1.90 x1.99 x2.03 x2.04, compounding 65.8 -> 26.3 over five doublings where 32 is linear - superlinear to SUB-linear. Rung 5 encode 1,744,427,240 -> 503,060,720 ticks; encode falls from 4.62% to 1.65% of the whole compile. MEMORY IS NEGATIVE, which is why the columns below FALL: -23,848 allocations and -10,666,765 bytes at rung 5 (-32% of encode, -33% at every rung), and the whole-compile deltas equal those two numbers TO THE DIGIT, so nothing outside encode moved. Codegen byte-identical, proven not inferred: 6 programs on x64-windows plus 4 programs on arm64-macos, arm64-linux and x64-linux, all hashed identical before and after. Ladder committed: Testing/ladders/genemit.sh, three independent knobs; ladders A (function count) and B (ops per function) were ALREADY FLAT at x2.00, and that is what located the term. (2) THE BLOCK-EXTENT SCAN NOW STEPS OVER A NESTED CONSTRUCT. Parser.blockStatementEndIndex walked every token in a construct extent to find its closing end, once per if/while/match, so a nested construct was re-walked by every enclosing one: O(depth x tokens), allocation-free. A lazily-built per-file extent column answers where the block opened here closes in one array read. Ladder committed: Testing/ladders/gennest.sh, which holds depth x statements FIXED so program size barely moves while nesting doubles - the only way to tell a depth term from a size term. Measured at constant size, 1024 statements in every program: parse CPU 23.6M / 28.2M / 37.4M / 56.3M / 98.2M at depth 4..64 (x1.20 x1.33 x1.50 x1.74, climbing toward x2.00) with allocations only x1.49 over a 16x depth change. After: the d4->d64 slope falls +68.8M -> +34.6M ticks, HALF the depth term. It costs +2 allocations per FILE and +8 bytes per TOKEN for the column (+0.55% of parse bytes at rung 5) - a linear, explainable step, and the reason the byte column does not fall as far as the allocation column. It also gave the block-CLOSER classification ONE home; it had been written out independently at eight sites. ⚠ TWO CORRECTIONS TO THE RECORD, both measured. FIRST: phase:parse superlinearity ON THIS CORPUS IS NOT NESTING DEPTH. Halving the depth term left the corpus parse ratios unchanged (x1.91 x2.14 x2.39 x2.75 x3.03 against a baseline x1.94 x2.16 x2.43 x2.74 x2.99). The depth term is real and gennest.sh proves it at constant size, but this corpus barely exercises it; the corpus parse curve is Theta(mutable bindings x constructs) - d_blocks.maxon is ONE function with 518 mutable vars in scope, and both callers ask a membership question once per binding per construct: 414,400 tests at rung 2 and 26.5 MILLION at rung 5. That is FILED, not fixed: the cure is carried sets computed bottom-up over the construct tree, which changes phi ORDERING that the goldens pin, so it is a rung and not an edit. SECOND: the first attempt at the parse fix was a per-file name->assignment-sites index with binary search. It was implemented exactly, gated GREEN, and its ladder went flat - and then measured x3.45 on the real corpus at rung 5, because it replaced a small per-construct set (one hash and one probe per test) with a structure whose per-test cost multiplied that same Theta(V x C). It was BACKED OUT. A ladder-flat, corpus-worse change is a regression, and only running both caught it. ⚠ THE MAP TRAP IS A CLASS, NOT AN INSTANCE: identity hash plus linear probing is quadratic for ANY interleaved union of contiguous int runs. SemanticCheck.buildBlockByIdMap is the identical pattern and is cold only because Maxon-tier ids have no split run - one inliner away from hot. A hash mixer in stdlib/Map.maxon would kill the whole class in three lines and was deliberately NOT done here: it changes every Map iteration order, so any pass emitting in Map order could move bytes, risking the C# 3103 suite and every golden. Gates: C# 3103/0, shv2 1774/0 at --workers=1 and =12 byte-identical, zero fragment churn, zero build warnings, no exit 101. | 618,844 | 1,122,274 | 2,128,822 | 4,140,535 | 8,164,910 | 16,216,089 |
| 2026-07-26 | Array.resize refused for a managed element type (E3106) + Array.get distinguishes emptySlot from indexOutOfBounds. shv2's gate is a parse-time check on the resize arm; the bootstrap's is a pre-monomorphization SemanticCheckPass walk over MaxonCallOp. Neither runs on the scale corpus's own hot path, so this run is a NEUTRALITY check, not a target. | 618,844 | 1,122,274 | 2,128,822 | 4,140,535 | 8,164,910 | 16,216,089 |
| 2026-07-26 | THE P1.5-FINISH OPTIMIZATION PASS, and the headline is NOT this rung's code — it is a QUADRATIC IN `Map` ITSELF that the rung's own instrument row had already flagged as a curve nobody had explained. phase:encode goes x3.02 -> x1.98 (rung 5: 231,100,750 -> 81,443,584 CPU ticks, -65%) and phase:link x1.79 -> x2.00, with the ALLOCATION AND BYTE COLUMNS BIT-IDENTICAL AT EVERY RUNG (checked by diff, not by eye) - because the fix changes only which SLOT a key probes, never what is allocated. WHAT IT WAS: `Map`/`Set` are power-of-two open-addressed tables with LINEAR probing, and `int.hash()` is the IDENTITY (`self and 0xFFFFFFFF`), so the slot is the key's low bits and structure in the keys became structure in the table. The victim was the per-function block-offset table in BOTH backends (`Arm64BlockOffsetMap` / `ChunkLabelOffsetMap`), keyed by BlockId. Those ids are PERFECTLY DENSE - a run 0..N-1, verified, gap histogram is a single entry (1, 6144) - but they arrive in LAYOUT order, which interleaves the parser's blocks with the ones minted later (critical-edge splits): 0, 1, 4097, 2, 3, 4098, ... At every capacity below the largest id the two runs alias onto the same slots, interleaved insertion grows ONE primary cluster, and each doubling rehashes and rebuilds it. Isolated in a bootstrap-compiled microbenchmark with the SAME key set, count and density and only the ORDER changed: 24,576 ids cost 346.6 ms in arrival order against 2.3 ms in increasing order (x3.8 x3.9 x3.7 x4.0 per doubling - a quadratic, 150x at that size). FIX: `spreadHash` in stdlib/Interfaces.maxon (MurmurHash3 fmix32) applied at the SIX places Map and Set turn a hash into a slot index - one door, so the two containers cannot come to disagree. It deliberately does NOT change `hash()` itself and is NOT what the `hashes` arrays cache; those still hold the key's true hash for the skip-the-equals guard. HOW IT WAS FOUND, which is the part worth copying: the memory columns read x1.96 for encode the whole time and could never have shown this. The CPU column showed the curve; a hand ladder (maxon-shv2/Testing/ladders/genblocks.sh, committed) separated blocks-per-FUNCTION from functions-per-module - encode read x3.21 x3.37 x3.69 on `blocks` against x2.0 flat on BOTH controls (`funcs`, `straight`) - and a bisect by disabling one map operation at a time pinned it to the upsert rather than the patching or the lookups. THIS RUNG'S OWN COST, A/B'd on this host base-vs-head, is a FLAT +1 allocation / +16 bytes at every rung in phase:parse and nothing else anywhere: the retagged-cast mint (`emitRetaggedCastValue`) firing at the single `as ScaleCoverNarrow` site in the fixed-size h_cover.maxon. CORRECTING THE RECORD: that flatness is the CORPUS, not the cost. Measured on probes of 1/10/100 cast sites, `castChangesTheShiftFill`'s extra `signatures.aliasOf` costs +1 allocation PER `as <declared-alias>` SITE (+11 at 10 sites, +101 at 100), and a MINTING cast costs ~+8.9 parse and ~+28 total allocations per site. It reads flat here only because the whole six-rung corpus contains exactly ONE named-alias cast and it sits in a file that does not double. CLOSURES ARE INVISIBLE TO THIS LADDER ENTIRELY - ScaleCorpus lists them under NOT GENERATED - so the capture half of the rung was measured on a second committed ladder (genclosure.sh): the path is LINEAR in program size (x1.98 x1.99 x1.99 parse allocations over 128..1024 closures), `fieldStorageType` is O(1) per capture READ SITE, and `captureSlotFor`'s O(reads x captures) scan is linear-in-practice - the language caps a function at 64 argument slots (E2015), so the scan is bounded at ~2,048 comparisons per closure and K=32 -> K=64 reads x1.41. Re-measure it if a closure can ever capture hundreds of distinct LOCAL bindings. STILL OPEN, unchanged by this row: phase:parse remains x3.26, the loop-phi token pre-scan (Parser.loopBodyEndIndex + namesAssignedIn, O(depth x tokens)) that commit 6ee12ff37 attributed - it is the corpus's own documented prediction and is NOT this pass's. Gates: shv2 1593/0 arm64-macos and 1588/0 wasm32-wasi, --workers=1 stdout byte-identical to --workers=12 AND to the pre-change baseline (sha 0d4993c64e), specs-shv2/fragments EMPTY after a full run (byte-identical codegen, which is what proves a hash change moved no emitted IR), C# bootstrap 3102/0 with its arm64 fragment churn proven pre-existing (byte-identical diff with and without the change), no exit 101. | 614,774 | 1,115,895 | 2,117,094 | 4,116,889 | 8,121,392 | 16,123,845 |
| 2026-07-26 | REBASE RE-MEASUREMENT of the P1.5-finish optimization pass. The row above it was measured against a base that PREDATES upstream a78d17c35, and its numbers are therefore stale — this row is what is true with both fixes present. Post-rebase the curves hold: phase:encode x1.99, phase:link x1.98, every phase ~x2.00. The allocation deltas against that row are NEGATIVE at every rung (-1,472 -> -18,361 allocations, -163,098 -> -4,188,382 bytes) and that is upstream's work, not a regression: a78d17c35 replaced the x64 per-function block-offset Map with two pre-sized arrays, so those allocations no longer happen at all. THE TWO FIXES ARE COMPLEMENTARY AND NEITHER IS REDUNDANT, which is the point worth recording. Upstream fixed the INSTANCE it could see from a Windows host (X64Backend.emitFunctionChunk; 'the key IS the index', so the Map became arrays) and its own row states in as many words that it DELIBERATELY DECLINED the class fix: 'A hash mixer in stdlib/Map.maxon would kill the whole class in three lines and was deliberately NOT done here: it changes every Map iteration order, so any pass emitting in Map order could move bytes, risking the C# 3103 suite and every golden.' This branch took that class fix (spreadHash, MurmurHash3 fmix32, at the six slot derivations in Map/Set) and MEASURED the risk it was declined on, rather than reasoning about it. The risk is zero, empirically: specs-shv2/fragments EMPTY after a full run on two targets, the C# bootstrap's fragment diff BYTE-IDENTICAL with and without the change (sha aef478e2b6, A/B'd against an unmodified checkout), wasm 0 failures, verify-warm-rebuild PASS, and an independent sweep of all 16 Map/Set iteration sites in the compiler found exactly one user-visible consequence — the ORDER of diagnostics for a program with several undeclared field types, which was already map order and not source order, and which TypeResolution.maxon:133 documents as such. ⚠ AND ON THIS HOST THE MIXER IS THE ONE DOING THE WORK: scale-test's compileRung spawns build with no --target, so on arm64-macos it is Arm64BlockOffsetMap that carries the block-offset table, and upstream's array fix is x64-only. An x64 host would have measured the reverse. Neither fix subsumes the other; the class fix additionally covers SemanticCheck.buildBlockByIdMap, which upstream's own row flags as 'the identical pattern and cold only because Maxon-tier ids have no split run - one inliner away from hot'. Gates on the rebased tree: shv2 1591/0 arm64-macos, 1586/0 wasm32-wasi, --workers=1 byte-identical to --workers=12, specs-shv2/fragments clean after a full run, C# bootstrap 3104/0, no exit 101. | 613,302 | 1,113,764 | 2,113,782 | 4,111,350 | 8,111,534 | 16,105,484 |
| 2026-07-27 | OPTIMIZATION PASS over the P1.7 instance-name-space rung. NO COMPILER CODE WAS CHANGED, and that is the result. WHAT MOVED AND WHY: -15 -27 -51 -99 -195 -387 allocations and -240 -432 -816 -1584 -3120 -6192 bytes, i.e. EXACTLY -1 allocation and -16 bytes PER INTERNED GENERIC INSTANCE. This corpus's instance count is 3 + 12x2^rung (3 fixed Array instances plus a doubling generics knob) and the deltas match it digit for digit; ratios x1.80 x1.89 x1.94 x1.97 x1.98 converge on x2.00, which is what 'linear in instance count' means. The allocation that stopped happening is the CompiledNameClaimant boxed union in ParseStaging.maxon, which checkTypeSymbolNamespace minted once per instance even for its payload-free 'unclaimed' case; the rung deleted the union and the arm chain with it, leaving a bare claimed.get(compiled). A/B'd base 42ea9a054 vs head IN THIS ONE WORKTREE on this Windows host, the two binaries named to equal length so phase:load's filename-length term cancels. Independently reproduced here, NOT carried over from the implementer's own numbers. THE DELTA COLUMN OF THIS ROW IS MEANINGLESS AND SHOULD NOT BE READ: the row above it was measured on an arm64-macos host. THE HUNT'S PRIMARY TARGET was reservedIfDeclared, a LOOP over a name space run once per generic instance inside the compiler's name mangler. It performs AT MOST TWO PROBES on any program that compiles, and a probe is two Map.contains (containsStruct / containsEnum, SignatureIndex.maxon:3509 and :4585 - hash lookups, so the derivation is NOT O(instances x declared types)). A THIRD probe requires a declaration whose own name starts with __, which is E2051, so it exists only on programs already refused. Measured on a new committed ladder, maxon-shv2/Testing/ladders/geninstances.sh, at 64..2048 instances: phase:signatures allocations are IDENTICAL base and head at every rung (3089 5396 9943 18970 36964 72882, x1.97) - the added probe allocates NOTHING on the uncontested path - and signatures CPU is +0.7%..+1.3%, flat with N and inside the noise band. THE CONTEST PATH costs +4 allocations and +198 bytes per contested instance, measured as contested minus a BYTE-IDENTICAL control (type Zed_A<i> in place of type Box_A<i>, so the extra declarations' own sweep cost cancels): 256 512 1024 2048 4096 8192 allocations across the 32x span, i.e. constant per instance, x1.98 both columns. DELIBERATELY NOT OPTIMIZED - it is a constant factor on a path bounded by the declared-type count, and constant factors are not the mandate; for the record it is one String interpolation and a direct ByteArray reserve+append would make it 1 allocation. THE ADVERSARY - the shape built to make the loop iterate k times, k declarations each two characters longer than the last - reads -1 0 +4 +14 +64 ms against its byte-identical control over 40 KB to 8.5 MB of source, about 7e-6 ms per source byte and FLAT. It is LINEAR EVEN THERE, and structurally so: probe j costs a declaration 2j characters long, so k probes require Theta(k^2) of SOURCE to buy Theta(k^2) of work. CORRECTION TO THE RECORD: this corpus DOES generate generics and interfaces (its own manifest lists both), so the standing instruction that it contains none is stale - and it is precisely what makes the per-instance delta above readable here at all. UNCHANGED AND NOT THIS RUNG'S: phase:parse reads x3.26 on base and head alike, the already-filed Theta(mutable bindings x constructs) debt. Gates on this tree: shv2 1785/0, specs-shv2/fragments clean, no exit 101. | 618,838 | 1,122,401 | 2,129,215 | 4,141,460 | 8,166,899 | 16,220,206 |
| 2026-07-27 | Map probe trap #123, the last two BlockId-keyed Maps: SemanticCheck.buildBlockByIdMap and StdToWasm.BlockDispatchMap are now ONE dense BlockKeyedTable (IrModule.blockIndexById), which BlockOffsetTable is also an instantiation of. THE LADDER SEES ONLY THE THIRD: its corpus has no async and always builds for the host, so the two converted sites read a true Delta 0 here — measured elsewhere (semanticCheck -1,536 allocs / -92% bytes on Testing/ladders/genawait.sh 24 400; wasm encode -6,864 allocs on the rung3 corpus). What DID move here is bytes:encode, -2,328 .. -42,008, exactly 131 + 160*2^k CodeOffset slots per rung: forBlockCount now reserve()s the value column (exact ask) where it used to growFilled() it (which floors at Array.MinimumCapacity = 4), so every chunk of under 4 blocks stops rounding its offset column up to 4. Allocation counts are unchanged in every phase; the CPU column moved only inside its noise band. | 613,302 | 1,113,764 | 2,113,782 | 4,111,350 | 8,111,534 | 16,105,484 |
| 2026-07-26 | Map probe trap #123, the last two BlockId-keyed Maps: SemanticCheck.buildBlockByIdMap and StdToWasm.BlockDispatchMap are now ONE dense BlockKeyedTable (IrModule.blockIndexById), which BlockOffsetTable is also an instantiation of. THE LADDER SEES ONLY THE THIRD: its corpus has no async and always builds for the host, so the two converted sites read a true Delta 0 here — measured elsewhere (semanticCheck -1,536 allocs / -92% bytes on Testing/ladders/genawait.sh 24 400; wasm encode -6,864 allocs on the rung3 corpus). What DID move here is bytes:encode, -2,328 .. -42,008, exactly 131 + 160*2^k CodeOffset slots per rung: forBlockCount now reserve()s the value column (exact ask) where it used to growFilled() it (which floors at Array.MinimumCapacity = 4), so every chunk of under 4 blocks stops rounding its offset column up to 4. Allocation counts are unchanged in every phase; the CPU column moved only inside its noise band. | 613,302 | 1,113,764 | 2,113,782 | 4,111,350 | 8,111,534 | 16,105,484 |
| 2026-07-27 | OPTIMIZATION PASS over the witness-label injectivity rung (f64e4d0a5). NO COMPILER CODE WAS CHANGED, and that is the result. A/B base 858d8dbef vs head f64e4d0a5, both REBUILT AND RE-RUN IN THIS WORKTREE: allocations and bytes are bit-identical at all six rungs (618,838 / 36,000,693 through 16,220,206 / 1,150,878,945), which confirms the implementer's claim to the digit. BUT THAT ZERO IS A CORPUS BLIND SPOT, NOT EVIDENCE: ScaleCorpus.maxon:554 states this ladder generates no 'where' clause anywhere, so it mints NO witness table and never calls witnessTableLabel or ensureWitnessTable at all. The rung was therefore measured on a purpose-built witness ladder instead (scratch, not committed). Shape A holds ONE (conformer, interface) pair and doubles the DISPATCH SITES 256..4096 - the label is minted per SITE, BEFORE the memo check at LowerMaxonToStd.maxon:912, so this is the per-call cost - and shape B doubles the PAIRS 16..256, i.e. the tables actually emitted. Head minus base is EXACTLY 0 allocations and 0 bytes at every rung of both, in phase:lowerMaxonToStd and in total. The separator costs nothing because underscore and dot are both one byte, so the label String is the same length and the same allocation; and the witness path is LINEAR in both dimensions, phase:lowerMaxonToStd allocations x1.97 x1.98 x1.99 x2.00 over sites and x1.93 x1.96 x1.98 x1.99 over pairs. THE ONE GENUINELY NEW RUN-TIME PATH is requireIdenticalPayload, a ByteArray.equals() on the already-registered branch of registerRdataConstantLabeled, whose cost would be O(payload) PER HIT. ITS HIT COUNT WAS MEASURED, NOT ARGUED: the body was temporarily replaced by an unconditional panic and the compiler rebuilt, so ANY re-registration aborts. The full x64-windows suite still passed 1793/0, as did 256 distinct witness tables in one program, 4096 witness reference sites in one program, and the whole 5-rung scale ladder. The branch is taken ZERO times - both callers memoize before they ask (ensureWitnessTable on the label, ensureLayoutDescriptor on the gid), exactly as its header claims. Instrumentation reverted; the committed binary is byte-identical to the one every A/B above ran with. THE +612 BYTES IN THIS ROW'S DELTA COLUMN IS NOT THIS RUNG'S AND SHOULD NOT BE READ. It appears identically on the BASE binary, and the decisive test is that checking out cdd6fc8bf - the very commit that wrote the previous row - and rebuilding it HERE reproduces this row's numbers exactly while still reading +612 against its own recorded row. So the previous row is not comparable across environments, and no number was 'fixed'. The byte column carries a path-length term, measured twice on an unchanged binary: +49 bytes for +49 characters of the compiler's own filename on a trivial program, and +34,597 bytes for +30 characters of the SOURCE filename on a large one, with the allocation count unmoved both times. The other documented candidate is the bootstrap refresh between the two rows (bin/maxon.exe here postdates af8c2e931, which changed maxon-sharp), which moves shv2's memory profile but not its output. THE ALLOCATION COLUMN IS IMMUNE TO BOTH and reads 0 at every rung, which is why it is the one to trust. NOT THIS RUNG'S, CONFIRMED PRE-EXISTING: regalloc:splitting is quadratic in PER-FUNCTION pressure (x3.64 x3.80 allocations, up to x4.13 CPU, on shape B whose main holds 256 constructions), the budgeted SplitLiveRanges term of ARCHITECTURE.md:1336-1345; with per-function pressure bounded it reads x1.85 x1.92 even at 4096 witness sites, so it tracks pressure and not witness dispatch. Gates on this tree: shv2 build exit 0, x64-windows 1793/0, wasm32-wasi 1597/0, specs-shv2/fragments clean after the run, no exit 101. | 618,838 | 1,122,401 | 2,129,215 | 4,141,460 | 8,166,899 | 16,220,206 |
| 2026-07-27 | RE-BASELINE of the witness-label injectivity rung's row onto the MERGED tree (rebased over upstream #123's BlockId dense-table work df5b5ffd8/220c5efed plus the arm64 type-name-collision goldens 27549b251). The row directly above was measured on the pre-rebase branch based at 858d8dbef, and upstream changed a data structure the compiler indexes by — so those ABSOLUTE numbers are not the merged tree's, and the next rung must compare against THIS row rather than that one. THE RUNG'S OWN FOOTPRINT IS STILL 0, re-verified after the rebase rather than re-asserted: the separator is one byte either way, and this ladder cannot reach the code at all (ScaleCorpus.maxon:554 generates no 'where' clause, so no witness table is minted and witnessTableLabel/ensureWitnessTable are never called), which is exactly why the rung was measured on a purpose-built witness ladder instead — +0 allocations and +0 bytes at every rung across 256..4096 dispatch sites and 16..256 (conformer, interface) pairs, linear on both axes. Any delta this row shows against the previous one therefore belongs to upstream's dense-table change and not to the witness join. Merged-tree gates: build exit 0 zero warnings, x64-windows 1793/0, wasm32-wasi 1597/0, specs-shv2/fragments EMPTY after both suite runs (byte-identical codegen), no run exited 101. | 618,838 | 1,122,401 | 2,129,215 | 4,141,460 | 8,166,899 | 16,220,206 |
| 2026-07-27 | Splitter: the confined half DEFERRED until it can win + applyForbidden SCOPED to the split's own ids + PeakRank stopped being a heap object minted per winning op and twice per tree combine + ScannedValues.all hoisted out of buildUseIndex's and computeLivenessOn's per-block loops + the fresh-id walk made a word-range walk. WHY THE CPU COLUMN READS A TRUE ZERO FOR THE HEADLINE FIX, and it is corpus blindness of the KNOWN kind: the ladder's pressured functions are LOOPS, which enter confined mode at their first analysis, so the deferral -- the whole 8.8x -- CANNOT engage here and correctly does not move. It is measured on the shape the corpus cannot express (Testing/ladders/genwidelive.sh 400 sum, one basic block, 396 splits: regalloc:splitting 11.49s -> 1.56s; x64-large-frame-arg7 53.9s -> ~6.1s; suite 56.3s -> ~18s). A Delta0 from a ladder that cannot express the feature measures the instrument's blind spot, not the cost. WHAT DID MOVE IS THE EXACT COLUMN, and it is attributable to the digit: whole-compile allocations fall from the ScannedValues hoist (one heap object per block of every function, in TWO per-block loops), and regalloc:splitting allocations fall a further 3.2%/4.8%/6.4%/7.8% across rungs 0-3 from the PeakRank de-allocation, every removed allocation dividing out at exactly 72 bytes -- the per-op mint dominates a single-block function, the O(log blocks) combine term dominates this many-block corpus and its share GROWS with rung, which is what the two halves being complementary predicts. CAVEAT ON THE CPU COLUMN THIS RUN ONLY: 12 competing maxon processes were live on the box (suite wall read 46.6s then 18.1s for identical work), so treat every CPU ratio here as unreliable and read the exact columns; that is the reason the memory columns exist. Residual, NOT closed: the pass is still superlinear on one basic block (per-split cost x1.50-1.71 per doubling, was x2.05) -- four O(block)-per-split walks remain and need a positional structure with order maintenance, filed as its own rung. Decisions UNCHANGED throughout: 243 goldens byte-identical on every target that ran, valuesSplit 96/196/396. | 615,240 | 1,114,446 | 2,111,682 | 4,103,277 | 8,084,440 | 16,043,183 |
| 2026-07-27 | Splitter re-space: SCOPED to the smallest subtree that can absorb the splice, and the recorded slots it moves are now actually REPAIRED. Two findings, one cause. (1) THE RUNG'S `PressureIndex.relayout` WAS NOT OFF THE PATH — "zero re-spaces on the whole scale corpus" is not what this corpus does. Measured with the pre-change binary: corpus rung0 fires ONE function-wide re-space of 1024 slots, rung1 fires TWO of 2048 each. Count doubles per rung AND size doubles per rung, so its total work went x4.0 per doubling — a quadratic sitting on the shipping corpus, invisible because a re-space allocates almost nothing and the ladder that would show it (genwidelive, one block, wide gaps) never fires one. (2) IT ALSO CRASHED. `SplitEdits.insertedSlots` records each spliced op's slot as it is seated; a re-space mid-batch MOVES those ops, and `appendInsertedSlots` asserted in a comment that the re-space "re-derived" the slots already returned — nothing did. Reachable at the SHIPPING SlotGap=4 on a plain straight-line function (alternating pressure humps: an add reading two already-spilled values sitting directly after a call whose own result is spilled puts THREE ops in one gap, and midpoint subdivision fits two): `PressureIndex.opAtSlot: slot 166 holds no op` out of `reindexSplitValues`, at every rung of a 12/24/48 ladder. THE FIX: `makeRoom` climbs from the exhausted gap to the first ancestor whose ops still fit it two slots apart and re-spaces ONLY that subtree (`respaceRange`, Theta(subtree)); the function-wide `relayout` survives as the fallback for a block that has outgrown its run, which needs the block to roughly DOUBLE its op count, so it is O(log growth) per block rather than per collision. `PressureRelayoutScratch` became a WINDOW with an old->new map, and `applyMoves` is the repair the comment used to merely claim. MEASURED on the corpus: rungs 0/1/2 now do 4/10/22 local re-spaces of 8-16 slots each = 48/128/288 slot-moves against the old 1024/4096/(16384) — LINEAR, with the per-event scope CONSTANT at 8-16 slots across all three rungs and ZERO function-wide re-spaces at any of them. WHAT MOVED IN THE EXACT COLUMNS, attributed: regalloc:splitting allocations +180/+270/+452/+816/+1544/+3000 across rungs 0-5, converging on x2.0 — exactly ONE per function that runs the splitter, the `moved` column the slot repair needs. Bytes FALL -30,277/-66,573/-139,133/-284,253/-574,493/-1,154,973, which is the avoided function-wide capture (~41,984 bytes of columns for a 1024-slot capture) less the ~10,080 bytes of per-function `moved` headers and ~2,000 of small local captures: -29,904 predicted against -30,277 measured at rung 0. UNCHANGED, deliberately: valuesSplit 96/196/396, 243 goldens byte-identical, `genwidelive` dead-control exit 7 at every rung. The one-block residual is NOT what this touched and did not move — min-of-3 CPU on genwidelive sum reads x2.395/x2.390 before and x2.199/x2.458 after, i.e. ~N^1.25 both ways, which is the Hall matching plus fillLiveBeforeOp, still measured-linear-per-split debt. Gates: build 0 warnings, suite 1793/0, and 1793/0 again under VerifyIncrementalSplit=true at SlotGap=4 AND at SlotGap=2 (which forces the function-wide fallback, covering the path the corpus no longer reaches). | 574,286 | 1,034,863 | 1,954,841 | 3,791,918 | 7,464,045 | 14,804,714 |
| 2026-07-27 | P1.7 finish Slice 1 (genericInstanceHasBaseLayout replacing a 4x-duplicated isArrayInstance-or-isSetInstance stop condition): THE COMPILER'S COST DID NOT MOVE AT ALL, and the whole of this row's apparent movement is an ARTIFACT OF THE CHECKOUT PATH, measured rather than assumed. (1) THE CODE COSTS ZERO. Against the parent (d28583442) the allocations, frees and bytes are bit-identical at EVERY rung and in EVERY per-phase bucket except phase:load and phase:parse; the whole-compile allocation count is identical to the digit. It also holds far outside the corpus: on Testing/ladders/geninstances.sh at 8,000 and 32,000 interned instances (the corpus's rung-5 count is ~387, so 32,000 is ~80x it) both compilers allocate 786,144 / 54,649,587 bytes and 3,114,445 / 225,247,505 bytes respectively -- identical to the digit both times. That ladder is the right one because rootManagedOpaqueArrayElementDrops opens the changed gate over EVERY interned instance unconditionally, so N=32000 exercises the new predicate 32,003 times per compile. (2) THE +3,876 BYTES IS THE WORKTREE'S PATH, NOT A COST, AND HERE IS THE PROOF: the PARENT's binary, copied into this worktree and run from this worktree's path, reproduces THIS row's numbers to the digit (rung 0: 615,240 allocs / 35,870,885 bytes; rung 1: 1,114,446 / 67,809,887). Same code, different path, this row's numbers. The delta lands in phase:load (-14 allocs, +3,115 bytes), phase:parse (+232 bytes) and the un-bucketed driver (+1,740), it is CONSTANT at all six rungs, and it is rung-independent because it tracks the length of the paths the driver holds, not the program. This worktree's root is 29 characters longer than the main checkout's. SO: a run recorded from the main checkout will read about -5,087 bytes / +14 allocs against this row, and that is the path unwinding, NOT a regression -- subtract it. (3) THE PER-CALL QUESTION, asked because the old form was two integer-ish name compares and the new one adds a string-keyed Map probe: the new form is NOT more expensive, and it is arguably cheaper. isArrayInstance(id) or isSetInstance(id) derived the base name TWICE (two bounds-checked array gets and a managed ByteArray refcount pair, each); the new predicate derives it ONCE and adds one Map.contains over a 3-to-5 byte interned name. No name is CONSTRUCTED on either path -- TypeNameInterner's names are zero-copy slices of the source -- which is why the exact columns above can read a true zero. The CPU column cannot resolve the difference and the reason is documented rather than waved at: a NULL CONTROL running the IDENTICAL binary on both sides of the same interleaved min-of-9 protocol produced -0.70% (N=8000) and +3.83% (N=32000) at the whole-compile level, the same magnitude and sign pattern as the real A/B (+0.79% and +3.65%), and a blocked-rather-than-interleaved repeat of the same A/B flipped a sign (phase:signatures +2.17% -> -0.11%). This box's floor for this experiment is about +/-4-5%, so any per-call claim here would be a statement about the apparatus. (4) NO SUPERLINEAR STRUCTURE, checked not assumed: the gate is O(1) and is opened ONCE per walk (not per field), the walk count is O(interned instances) exactly as before, and typeIsManaged returns true immediately for a nested genericInstance tag rather than recursing, so a deep Box-with-Box-with-String nest cannot re-walk. (5) MEASURED DEBT, filed not fixed: genericInstanceHasManagedField and genericInstanceHasStringField re-derive baseLayoutOf INSIDE their per-field loop -- genericInstanceFieldCount and substituteInstanceFieldType each call it -- so a base-name derive plus a name-keyed Map probe is paid once PER FIELD, where their sibling managedOpaqueArrayElementOf hoists `let layout = baseLayoutOf(id)` above the loop and does not. It is LINEAR (O(instances x fields), no recursion), it runs only for instances whose destructor is referenced, and generic bases carry few fields, so it is a constant factor and not a curve; with the noise floor measured at +/-4-5% above it could not be shown to move anything, and the mandate is growth curves rather than constant factors, so it is recorded here rather than shipped as a change that only looks like an optimization. The trigger that would make it worth taking: a machine-generated wide generic base (tens of fields) instantiated many times. Suite 1831/0, specs-shv2/fragments byte-identical, build 0 warnings. | 615,240 | 1,114,446 | 2,111,682 | 4,103,277 | 8,084,440 | 16,043,183 |
| 2026-07-27 | SPLITTER POSITIONAL STRUCTURE — the whole rung, and THIS ROW'S DELTA IS +192 ALLOCATIONS AND +7,621 BYTES AT EVERY RUNG, CONSTANT — AND IT IS NOT THE COMPILER. It is ENTIRELY `phase:load` (+192 allocs / +8,233 bytes, flat at every rung, rung 0 through rung 5 alike), i.e. the already-documented PATH-LENGTH / environment term: the row above was measured in this rung's WORKTREE and this one on the merged main checkout. EVERY COMPILER PHASE IS BIT-IDENTICAL between the two runs — `regalloc:splitting` reads 32,554 allocations at rung 0 and 624,377 at rung 5 on BOTH. ⚠ The coordinator predicted this row would be delta-0 and it was not; the prediction was wrong and the instrument was right, which is precisely why the columns are read rather than the expectation. ⚠ AND ITS DELTA WAS MEASURED AGAINST THE "Splitter re-space" ROW, which is NOT necessarily the row directly above this one — upstream rows have been interleaved by a rebase since, so read this row's delta against THAT row by name, never by position. It exists to attribute the jump in the "Splitter re-space" row, which was written by the rung's optimizer pass after increments 1 and 2 were already in the binary and therefore attributed only the re-space fix. Read the two rows together. WHAT THE RUNG DID, measured on Testing/ladders/genwidelive.sh (Theta(N) values live in ONE basic block, so Theta(N) splits over a Theta(N) block): regalloc:splitting allocations 129,966 / 509,828 / 2,019,538 -> 6,683 / 13,095 / 25,905 at N=100/200/400, i.e. -98.7% at N=400 and per-doubling x3.92/x3.96 -> x1.96/x1.98 — QUADRATIC TO LINEAR in the exact column. CPU 6,243M -> 1,670M ticks (-72.7%), per-doubling x3.21/x3.57 -> x2.31/x2.48, i.e. N^1.83 -> N^1.25. ON THIS MANY-FUNCTION CORPUS regalloc:splitting is LINEAR IN BOTH COLUMNS (allocations x1.59 x1.74 x1.85 x1.92 x1.96 converging on x2.0; CPU x1.91 x2.04 x1.98 x2.08 x2.12) — the residual N^1.25 is specific to ONE function carrying Theta(N) simultaneously-live values and this corpus cannot express it. THE +8.9% BYTES between the previous rung's row and the row above is THIS RUNG'S, and it is the PressureIndex's columns — the price of gap-labelled slots, measured linear, deliberate. Against it: whole-compile allocations fell ~7% at every rung. THREE MECHANISMS: (1) static per-op facts memoized by union VARIANT ordinal — 112 cases, constant size, NO staleness class, so no invalidation hook exists to get wrong; (2) the block's peak is a per-op MAX-SUFFIX-SUM over gap-labelled slots, NOT the per-interval range-add PLAN.md prescribed (the interval form needs endpoints in pass-through blocks, re-derived from live sets that are mid-edit; the per-op form separates per value, so a split's repair is O(uses of one value)); (3) the other three per-split walks were never range problems at all — they walked a whole block to service an O(1) value set, and are now edit-proportional. MEASURED: the tree ALONE was not enough (x2.81/x3.14, barely moved); both halves give x2.25/x2.45. THE LARGER HALF OF THE ALLOCATION WIN WAS UNION BOXING, pre-existing and invisible to reading: a PAYLOAD-FREE case of a payload-carrying union still heap-allocates (mm_alloc 16 bytes + mm_incref), and `ValueSpillSlot.none` was built TWICE PER OP per dirty block per split just to say "this op touches no slot" — 94.7% of the phase's allocations; `ValueDefSite` was the same shape per value crossing the peak. Both are now enums. Attribution exact: bytes-removed / allocations-removed = 16.0 at all three ladder rungs, to the digit. RESIDUAL, so the rung is ◑ not ✅: ~31% of the pass is still superlinear on one block — confinedPeakAt/hallVerdictAt 49.5% (exact Hall bipartite matching, measured LINEAR x2.03/x2.07; a matching is not a suffix sum, so it is a different problem), fillLiveBeforeOp 15.8% (Theta(pressure) data no count-tree can carry), SplitEdits.resequence/commit 8.6%, chooseVictim 7.0%. Re-measure trigger: a single block's PRESSURE growing — an inliner, or machine-generated wide types. DECISIONS UNCHANGED THROUGHOUT: valuesSplit 96/196/396, 243 goldens byte-identical, dead-control exit 7. Gates: suite 1794/0 (+1 = the re-space regression spec, seen RED at 5d99436bb with `opAtSlot: slot 166 holds no op`), VerifyIncrementalSplit ON at SlotGap=4 AND 2, x64-windows 1794/0, x64-linux 1688/0, wasm32-wasi 1598/0, arm64-macos 1603/0, arm64-linux SKIP (wedged OrbStack guest, filed). | 574,478 | 1,035,055 | 1,955,033 | 3,792,110 | 7,464,237 | 14,804,906 |
| 2026-07-27 | P1.7 finish Slice 2 (arrayElementSize narrowing a ranged-int array element to the smallest of 1/2/4/8 bytes that holds its declared range). NO CODE CHANGE FROM THE OPTIMIZER, and the numbers say why. ///// A/B PROTOCOL: the parent (d51ddd665) was built in a worktree whose absolute root is EXACTLY THE SAME LENGTH as this one (51 chars: .claude/worktrees/p17-basel vs .claude/worktrees/p17-esize) from a byte-identical bootstrap (sha256 5457eab2256c9da7802bbefb085719e041b3b2f91b2cf971be7862b8adf1c9c2). So the phase:load path-length artifact the PREVIOUS row had to argue away is EXACTLY ZERO here rather than subtracted, and every remaining byte is the code. The last committed row was NOT a usable baseline (it reads -40,942 allocs / +3,423,006 bytes against this run, all of it commits and a bootstrap between the two worktrees), which is why the A/B was taken against a freshly built parent instead. ///// THE DELTA DECOMPOSES COMPLETELY -- four terms, and they sum to the digit at all six rungs. head-minus-parent whole-compile allocations: +12, -12, -92, -236, -556, -1,180 (rungs 0..5); bytes +3,936, +7,712, +6,432, +12,384, +7,264, +13,728. (1) phase:parse -40/-80/-160/-320/-640/-1,280 allocations and -640..-20,480 bytes -- A PERFECT DOUBLING, and it is a SAVING: exactly -1 allocation and -16 bytes per Array-with-named-element size query, 40 such sites at rung 0 doubling to 1,280 at rung 5. CAUSE: arrayElementSize no longer routes the trivial case through genericInstanceSoleArgLogicalSize then typeLogicalByteSize, whose `named` arm called structOf(name), and structOf returns DeclaredStruct -- A UNION WITH AN ASSOCIATED-VALUE ARM, therefore HEAP-BOXED, 16 bytes, allocated on EVERY call including the `undeclared` answer a ranged alias always gets. The new trivialElementStorageBytes answers out of Map.get's own `throws` channel and boxes nothing. This is the same allocation class RangedAliasRegistry.underlyingInAnyFile already documents having removed for the same reason. Independently confirmed off-corpus: on a throwaway ladder of 128/256/512/1024/2048 array-create sites the head-minus-parent phase:parse allocation delta was -128/-256/-512/-1,024/-2,048 -- exactly -1.00 per site at every point. (2) phase:load +10 allocations / +432 bytes, CONSTANT at every rung, and (4) an un-bucketed +10 / +432, also constant at every rung. These are the SAME constant twice: constructing one extra empty Map field. Two RangedAliasRegistry instances exist (Project's and ProgramSignatures'); each gained `storageBytes`; Project's is constructed inside loadProject so it bills to phase:load, ProgramSignatures' is constructed outside any phase bracket. It is construction and not first-insert, because loadProject only reads files into the query DB -- no alias is recorded before phase:signatures. (3) phase:signatures AND phase:merge, each +16/+24/+24/+32/+32/+40 allocations and +1,856/+4,064/+4,064/+8,320/+8,320/+16,672 bytes -- the two storageBytes maps' backing arrays. THAT BYTE SERIES IS A POWER-OF-TWO STEP FUNCTION (Map capacity 16/32/64/128), not a curve: it steps once per ~2 rungs while the program doubles every rung, because the index is sized by the number of DISTINCT RANGED-ALIAS NAMES and not by program size. Net: the change is allocation-NEGATIVE from rung 1 upward and increasingly so, because the per-site saving grows with the program while every cost term is constant or sub-linear. Bytes stay net-positive at +13,728 on rung 5, which is +0.0011% of 1.24 GB. ///// THE PER-CALL QUESTION, ASKED BECAUSE THE DOC COMMENT SAYS "FOLD OVER EVERY DECLARATION": the fold is at WRITE time. RangedAliasRegistry.record calls widenStorageBytesFor once per declaration (one probe plus at most one upsert, O(1) amortised), and storageBytesInEveryFile is ONE Map.get -- there is no walk on the read path, which is already the index shape a fix would have introduced. MEASURED, not inferred, on a purpose-built ladder of F files each declaring the SAME alias `Small` plus S array-create sites over `Array with Small` (the corpus cannot express this: it generates ~387 interned instances at rung 5 but never has two files declare one alias name). The 960 extra array-create sites between S=64 and S=1024 cost 125,932 allocations at F=8 AND 125,932 allocations at F=1024 -- BIT-IDENTICAL across a 128x change in the number of files declaring the name, which is the exact column proving the per-call cost is independent of F. CPU on the same pair read 131,507,845 vs 138,189,331 ticks (+5.1%, at this box's measured +/-4-5% floor for this experiment, and the exact column says the work done is identical). ///// THE WRITE SIDE IS LINEAR TOO, and the contested-name path was isolated with a CONTROL: arm "same" (F files declaring one name) against arm "distinct" (F files declaring F different names, identical file/line/token count, so multiplyDeclared is never touched). Same arm, F=64..1024: phase:merge allocations 3,433 / 5,135 / 8,499 / 15,193 / 28,543 (x1.50 x1.65 x1.79 x1.88) and CPU 3,995,106 / 7,004,530 / 12,564,668 / 24,087,463 / 47,782,345 ticks (x1.75 x1.79 x1.92 x1.98); phase:signatures allocations 8,125 / 15,501 / 30,237 / 59,693 / 118,589 (x1.91 x1.95 x1.97 x1.99). Distinct arm: merge allocations 4,134 / 5,925 / 9,442 / 16,417 / 30,304 and CPU 3,335,636 / 5,364,798 / 9,251,049 / 17,138,828 / 37,317,020. EVERY ONE OF THOSE IS x2 PER DOUBLING -- no quadratic. The contested path costs ~28% more CPU in phase:merge at F=1024 than the control (the declFilePath.toString() and the byFile probe), which is a CONSTANT FACTOR on a phase that is ~1.4% of the compile, measured linear, and nowhere near triggered today: the widest real case is stdlib's seven files privately declaring `Byte`. The trigger that would make it worth revisiting is a program where hundreds of files declare one alias name. ///// CORPUS BLIND SPOT, stated rather than filed: the ladder above exercises the new LOOKUP 40x2^n times per compile, which is what the -1/site is measured off, but it NEVER EXERCISES A NARROWED STRIDE. Every array element the generator emits is `ScaleElement`/`ScaleGenElement` = int(i64.min to i64.max) (negative low, so rangedAliasStorageBytes returns the machine word by its first arm), `String`, or a struct -- all still 8. A future row must not read this corpus as evidence about narrowing itself; the throwaway ladder's `Small = int(0 to u8.max)` does emit `movRegImm32 rcx, 1` into __arr_create and was checked in the IR. ///// Build 0 warnings; suites x64 1837/0, x64-linux 1731/0, wasm32-wasi 1641/0, no leaks; specs-shv2/fragments byte-identical (git status empty after all three). | 574,298 | 1,034,851 | 1,954,749 | 3,791,682 | 7,463,489 | 14,803,534 |
| 2026-07-27 | P1.7 finish Slice 3 (three front-end divergences: newline tolerance inside [...], generic type-ARGUMENT validation via denotedNamedType, and an int fast path that no longer decides the element type from the first TOKEN) PLUS the optimizer's own change: the parser's two whole-program generic checks no longer run during the tolerant declaration sweep. ///// A/B PROTOCOL: the parent (679e023a9) was rebuilt in THIS worktree from the same bootstrap (sha256 5457eab2256c9da7802bbefb085719e041b3b2f91b2cf971be7862b8adf1c9c2) and reproduced the last committed row EXACTLY -- delta 0 allocations and 0 bytes at all six rungs -- so the committed row is a valid baseline here and every number below is this branch's. The worktree root is 51 chars, the same length as the p17-esize root that wrote the previous row, so the phase:load path-length artifact is zero rather than subtracted. ///// THREE STATES, whole-compile allocations rungs 0..5. Parent: 574,298 / 1,034,851 / 1,954,749 / 3,791,682 / 7,463,489 / 14,803,534. Slice as implemented (049b954b2): +106 / +194 / +370 / +722 / +1,426 / +2,834. After this commit: -46 / -92 / -182 / -360 / -714 / -1,420 against the PARENT -- the slice is now allocation-NEGATIVE, a swing of -4,254 at rung 5. Bytes after this commit: -2,356 / -4,808 / -9,718 / -19,542 / -39,190 / -78,514. ///// THE IMPLEMENTER'S MEASUREMENT, VERIFIED AND DECOMPOSED. Their +106..+2,834 reproduces to the digit and splits into exactly two phases with zero residue at every rung: phase:signatures +101/+185/+353/+689/+1,361/+2,705 and phase:parse +5/+9/+17/+33/+65/+129. Both series are EXACT geometrics -- signatures = 17 + 84*2^n (consecutive differences 84/168/336/672/1344, perfect doublings) and parse = 1 + 4*2^n (differences 4/8/16/32/64). LINEAR, confirmed off-corpus 4.5x beyond the ladder: a throwaway ladder of N=64..2048 `typealias I_i = GBox with E_i` declarations, each with its own ranged-int argument, read head-minus-parent phase:signatures +385/+769/+1,537/+3,074/+6,146/+12,290 -- 6.016/6.008/6.004/6.004/6.002/6.001 allocations per generic type ARGUMENT, ratios 1.997/1.999/2.000/1.999/2.000 -- with phase:parse delta exactly 0. ///// THE 20x SIGNATURES-VS-PARSE SPLIT IS TWO DIFFERENT COSTS, not one cost with a puzzle. (a) In the SWEEP denotedNamedType FAILS for every file-local argument -- a file's own declarations fold only after its whole token walk -- so each one built a positioned E3011: String.from(name) plus an interpolated message plus a Diagnostic record, ~6 allocations, into an artifact that is then discarded. (b) In the REAL PARSE the same call SUCCEEDS and the entire cost is MaxonType.structRef(id) -- a union arm with an associated value, therefore HEAP-BOXED -- returned for an argument naming a declared STRUCT and dropped on the floor by `_ =`. Isolated on a second throwaway ladder of N struct-typed arguments: +1 allocation and +16 bytes per struct-named argument, EXACTLY, at N=64/128/256/512/1024. The corpus emits instances/2 of that shape (the managed-holder arm), which is 4*2^n -- the parse series, to the digit. ///// THE CHANGE: ProgramSignatures.allFilesFolded, set once in queryProgramSignatures the moment the last file is folded; checkGenericInstance and checkGenericArgType return immediately when it is false. Both are PURE DIAGNOSTIC -- neither returns a value nor mutates anything but artifact.diagnostics -- and the sweeper's artifact is never merged (queryProgramSignatures builds a throwaway Parser; mergeArtifact runs only on the real parse's), so the sweep's verdict could never be reported by any program. The real parse re-reads every one of these declarations with the index complete (both a top-level `typealias X = Base with Arg` and a nested one inside a type body have a sweep reader and a real-parse reader that funnel through the same readTypeAliasDeclaration / readNestedTypeAlias), so every diagnostic a program earns is still recorded exactly once at the same position. The flag lives on the INDEX and not on the Parser because only the index knows which side of the fold each of its three readers -- sweeper, real parse, constant evaluator -- is on; threaded through Parser.create it would be one fact written at three call sites. ///// WHAT IT SAVES, DECOMPOSED TO THE DIGIT, this commit vs parent at all six rungs. phase:signatures -51/-101/-199/-393/-779/-1,549 allocations and -2,452/-4,968/-10,006/-20,086/-40,246/-80,594 bytes, and that is TWO terms: the whole of the slice's +2,705, plus 1,549 at rung 5 of PRE-EXISTING dead work from checkGenericInstance, which has been building the same discarded diagnostics since P1.6-A -- ~4 allocations per generic-instance declaration (its structOf DeclaredStruct box plus the E2055 'has no associated types' message the sweep records for a base whose own file has not folded). phase:parse +5/+9/+17/+33/+65/+129 allocations and +80/+144/+272/+528/+1,040/+2,064 bytes, IDENTICAL to the slice by construction: the gate is false only in the sweep, so the real parse does bit-identical work. phase:load 0 allocations / +8 bytes constant at every rung, plus an un-bucketed 0 / +8 -- the same 8 bytes twice, ProgramSignatures growing by one bool field; two are constructed, the empty placeholder in SignatureMemo.empty() (billed to phase:load) and the real one in queryProgramSignatures (built before the first phase probe, so un-bucketed). They sum exactly: rung 5 allocations -1,549 + 129 = -1,420, bytes -80,594 + 2,064 + 8 + 8 = -78,514. Off-corpus the gate is -10.00 allocations per generic alias declaration versus the slice and -4.00 versus the parent, FLAT at N=64..2048 (-10.094/-10.055/-10.031/-10.020/-10.012/-10.007). ///// CPU: NO CLAIM MADE. phase:signatures read 1,039M (parent) against 1,045M (gated) ticks at rung 5, +0.5%; whole-compile 32.19G against 32.09G, -0.3%. Both are inside this box's measured +/-4-5% band, so the CPU column can neither confirm nor deny a saving of this size and is not asked to. The exact allocation columns are the evidence. ///// RESIDUAL KEPT, MEASURED AND ATTRIBUTED: the +1 allocation / +16 bytes per generic type argument that names a declared STRUCT stays (128 of them at rung 5 = +0.0009% of the compile). It is denotedNamedType materialising a boxed MaxonType.structRef for a caller that only needs 'is it declared?'. Killing it needs a second predicate over the same six-arm cascade, and checkGenericArgType asks denotedNamedType precisely so that a type ARGUMENT and an `as`-cast TARGET can never come to disagree about what a name means. A 16-byte box is not worth a second door onto that question. ///// NO SUPERLINEARITY, and the slice's other three pieces were each read for one: initializerEndIndex now counts bracket depth instead of stopping at the first newline, still ONE forward scan per top-level binding, and a runaway (an unclosed `[`) leaves the cursor at EOF so it can happen at most once per file; arrayElementIsSingleToken/indexPastNewlines scan the newline run after ONE element and each element's run is disjoint, so the sum over a literal is bounded by its newlines; finishArrayLiteral's skipNewlines advance the cursor. The slice adds no pass, no IR op, and no collection the compiler indexes by. ///// CORPUS: the generic knobs DO double per rung (GenericInstancesBase 8 and GenericBasesBase 4 are rung-0 values knobAt doubles), giving ~448 generic type arguments and ~384 instance declarations at rung 5, which is why this cost was visible at all; the two throwaway ladders reach 2,048 arguments and stay flat at 6.00 and 1.00 allocations per site. ///// Build 0 warnings; suites x64 1851/0, x64-linux 1745/0, wasm32-wasi 1654/0, exit 0 (no leaks); verify-warm-rebuild PASS on a generic-alias program (determinism + cache + invalidation -- re-checked because the new flag lives on the memoized index); specs-shv2/fragments byte-identical (git status empty after all three suite runs). | 574,252 | 1,034,759 | 1,954,567 | 3,791,322 | 7,462,775 | 14,802,114 |
| 2026-07-27 | STALE-ARTIFACT HYGIENE (the previous build's output is now DELETED BEFORE the pipeline instead of being truncated by the writer at the end, so a compile that fails leaves nothing runnable behind). NO CODE CHANGE FROM THE OPTIMIZER, and the measurements say why. ///// A/B PROTOCOL: the parent (5bafc9bc0) was rebuilt in THIS worktree from the SAME bootstrap (sha256 8abfd02bd6158878b81d87580c9d542b9433fe6513e9bc0f12090887fe7838b4) and reproduced the last committed row EXACTLY -- delta 0 allocations and 0 bytes at all six rungs -- so the committed row is a valid baseline here and every number below is this branch's. The worktree root is 51 chars, the same length as the p17-finish root that wrote the previous row, so the phase:load path-length artifact is zero rather than subtracted. The two shv2 binaries (parent sha256 6092db6e, head 1ba204b3) therefore differ only in shv2 source. ///// THE DELTA: +14 allocations and +530 bytes, FLAT AT ALL SIX RUNGS across a 32x ladder. O(1) PER COMPILE -- not per function, per block or per op -- which is the whole claim this rung had to support. AND IT IS ENTIRELY UN-BUCKETED: all 22 CompilePhases and all 7 regalloc sub-phases are IDENTICAL TO THE DIGIT between parent and head, in allocations AND in bytes, at every rung. That is not just the attribution, it is the proof the pipeline was not touched -- the new work sits between loadProject and the first pass, outside every PhaseProbe. ///// ATTRIBUTED TO THE DIGIT WITH ZERO RESIDUE, and re-runnable: maxon-shv2/Testing/ladders/genfsprobe.sh 20000 out.maxon, whose discard_plus_require case reads 14 allocations / 528 bytes -- which is +14/+530 at the ladder's one-character-longer path. THE SPLIT: one FilePath.changeExtension is 10 allocations / 368 bytes; the four filesystem calls (File.delete twice and File.exists once in discardPreviousOutput, File.exists once in requireOutputWritten) are 4 allocations / 160 bytes, exactly ONE allocation of exactly 40 BYTES EACH. THAT 40 BYTES IS NOT THE PATH: it is the green-thread runtime's SyncRequest. __io_submit_sync mm_raw_allocs one, enqueues it, SetEvents a SYNC WORKER THREAD and parks the caller until the worker completes it, so every __ManagedFile call is a cross-thread round trip. Build the probe with the BOOTSTRAP (shv2 cannot compile stdlib/File.maxon yet); that is not a weaker measurement, because the shv2 binary is itself bootstrap-emitted from the same stdlib and the same runtime, so it is the same machine code. ///// STAR -- A TRAP THIS FOUND, worth more than the row itself: FilePath.changeExtension charges by what the String is a PRODUCT OF, not by what it says. resolveOutputPath builds outputPath by INTERPOLATION; on Windows FilePath.create then normalizes forward slashes to backslashes through String.replace. An output argument already spelt with backslashes skips the replace, so the stored String is the interpolation's own buffer and changeExtension costs 10 allocations / 368 bytes; one spelt with forward slashes goes through replace and costs 8 / 343. TWO FilePaths THAT COMPARE EQUAL COST DIFFERENT AMOUNTS -- build them from literals instead and the difference vanishes, which is how you can tell it is the buffer and not the bytes. scale-test spells its rung paths with FilePath.join (backslashes), so this ladder carries the 10-allocation form; driving the IDENTICAL rung-0 compile by hand with forward-slash arguments reads +12 / +504 instead. VERIFIED BOTH WAYS: backslash argv reproduced scale-test's rung-0 totals to the digit (parent 574,252 / 39,291,535, head 574,266 / 39,292,065) while forward-slash argv gave 574,263 and 574,275. Anyone comparing a hand-run figure against a ladder figure must match the spelling first. ///// THE TIME COST, MEASURED SEPARATELY BECAUSE NO MEMORY COLUMN CAN PRICE A SYSCALL (a File.delete allocates one 40-byte record whatever it costs). Per compile on this box, median of 3 x 20,000 iterations: a failing File.delete 52.8 us (twice), File.exists 48.2 us absent and 62.9 us present, changeExtension 1.4 us -- about 205 us in total, against a real delete of a present file at 267 us and a 64-byte File.writeBinary at 449 us. That is roughly 1 percent of a 24.6 ms trivial compile and 0.001 percent of the 14 s that builds shv2 itself. AT SUITE SCALE: 1,851 x64 spec tests times 205 us is 0.38 s of CPU, spread over 12 workers, about 32 ms of the suite's 18.4 s wall -- 0.17 percent. END-TO-END A/B, three interleaved passes of 150 compiles: success path parent 24.79/24.42/24.55 ms against head 25.08/24.85/24.75 ms, plus 0.31 ms per compile (the output EXISTS in that loop, so the delete is a real one); failure path parent 22.55/22.38/22.11 against head 22.26/22.16/22.49, delta minus 0.04 ms, inside its own noise. MEASURABLE IN A MICROBENCHMARK, FREE AT SUITE SCALE. ///// THE ONE CANDIDATE OPTIMIZATION, REJECTED ON ITS NUMBERS: guarding the delete with a File.exists. Measured, exists-then-delete on an absent path is 48.2 us against a bare failing delete's 52.8 us -- 4.6 us, which is 8.5 ms of CPU across the entire x64 suite -- and on a present path it costs a whole extra 62.9 us exists ON TOP of the delete it still has to do. It would also put a SECOND existence question in front of the one discardPreviousOutput already asks, whose entire job is to tell 'was not there' from 'would not go'. Not taken. ///// NO SUPERLINEARITY, AND STRUCTURALLY NONE TO FIND: the rung adds no pass, no IR op and no collection the compiler indexes by. Every added call runs once per compile(); all seven changeExtension sites in shv2 are once per compile or once per spec test, none inside a loop over functions, blocks or ops. The wasm arm gains the most -- writeWasm now stages a core module and an embed file and publishes the finished component with one File.rename, so plus two changeExtension, one rename and one delete -- and that is still O(1) sitting between two wasm-tools subprocess spawns. ///// CPU COLUMN: NO CLAIM MADE, and it could not make one -- four syscalls is about 10^5 ticks against a 32-giga-tick rung-5 compile. The un-bucketed CPU delta read -7.5M, -3.0M, -3.5M, -5.2M, -58.6M and +7.9M across the rungs, both signs, entirely inside this box's band. phase:parse's bend (parent x1.93 x2.18 x2.46 x2.87 x3.29, head x2.02 x2.25 x2.42 x2.88 x3.22, 38.7 percent of the rung-5 compile) is UNCHANGED between the two binaries and is PLAN.md Workstream O item 122, already filed with its cure and its re-measure trigger. Re-confirmed here, not rediscovered. ///// CORPUS BLIND SPOT THAT MATTERS FOR THIS ROW: every rung of this ladder SUCCEEDS, so it never exercises the path the rung was built to fix. The +14/+530 is the cost on the SUCCESS path; a FAILING compile pays 13 allocations, about 490 bytes and about 155 us of it (requireOutputWritten never runs), and no column here will ever show that. ///// Build 0 warnings; suites x64 1851/0 in 18.4 s, x64-linux 1745/0 in 58.4 s, wasm32-wasi 1654/0 in 26.2 s, C# bootstrap 3105/0 in 74.4 s, all exit 0 and no leaks; specs-shv2/fragments and specs/fragments byte-identical (git status empty after all four runs). | 574,266 | 1,034,773 | 1,954,581 | 3,791,336 | 7,462,789 | 14,802,128 |
| 2026-07-28 | P1.7a slice 2b-v: THROWING WITNESS DISPATCH (StdOp.witnessTryCall, MaxonOp.witnessTryDispatch, an isThrowing flag on witnessDispatch, a ThrowsClause on InterfaceMethodSig, a TryTarget union replacing a bare callee ByteArray). NO CODE CHANGE FROM THE OPTIMIZER; the measurements below say why, and they CORRECT the implementer's attribution. ///// A/B PROTOCOL: the parent (33fef8c52) was rebuilt in a SIBLING worktree whose root path is 34 characters, the SAME LENGTH as this one, from the IDENTICAL bootstrap binary (md5 93ce2ab682b40d4d2b34031cd07f347a, copied not rebuilt), so the phase:load path-length artifact is ZERO rather than subtracted and the two shv2 binaries differ only in shv2 source. phase:load reads delta 0 allocations and 0 bytes at all six rungs, which is that protocol checking itself. ///// THE DELTA, head minus parent: rung0 +148 allocs / +106,968 bytes; rung1 +242 / +198,272; rung2 +430 / +380,976; rung3 +806 / +746,288; rung4 +1,558 / +1,477,008; rung5 +3,062 / +2,938,352. Delta ratios x1.64 x1.78 x1.87 x1.93 x1.97 across a DOUBLING ladder, converging on x2.00 => LINEAR. +0.021 percent allocations and +0.236 percent bytes at rung 5. The implementer's figures reproduced to the digit. ///// ATTRIBUTION 1 IS WRONG, AND A KNOB SAYS SO RATHER THAN ARITHMETIC. The implementer read the added allocations as '+40 bytes per INTERFACE METHOD, paid twice', because phase:signatures reads EXACTLY +40.00 bytes per added allocation at all six rungs. Both halves are wrong, and the story could not have been right: scale-test's corpus generates NO interfaces at all, so a per-interface-method cost is a zero this ladder is structurally incapable of measuring. THREE KNOBS, each varying ONE thing with the rest fixed, all run head-vs-parent through --log=compiler:debug's per-phase memory table: (a) INTERFACE-METHOD COUNT K = 0 / 20 / 60 in one file -> allocations FLAT at +3 signatures / +3 parse, and signatures BYTES flat at +120. InterfaceMethodSig's new ThrowsClause field allocates NOTHING; it costs +8 bytes INLINE per interface method, visible only in phase:parse (+120 / +280 / +600, slope exactly 8.0). It allocates nothing because the PARENT already called parseOptionalThrowsClause() and threw the result away -- head merely keeps it. (b) FUNCTION COUNT N = 0 / 200 / 400 / 800 in one file -> allocations FLAT at +3 / +3 again. Not per function either. (c) FILE COUNT F = 2 / 6 / 21 -> allocations = F + 2, IN EACH PASS, at every size (+4, +8, +23). SO THE ADDED ALLOCATIONS ARE PER FILE PER PARSE PASS -- one for the signatures pass and one for the parse pass -- which is a thing this corpus DOES generate (its file count doubles with the rung: 9 + 23 x 2^k, reproducing +96 / +165 / +303 / +579 / +1,131 / +2,235 in phase:signatures exactly). ///// AND THE 40 BYTES IS TWO UNRELATED COSTS THAT HAPPEN TO SUM TO 40. Proven by adding two MORE ThrowsClause fields to Parser and rebuilding: allocations went up EXACTLY x3 at every file count (3->9, 4->12, 8->24, 23->69), while the byte delta fit a + 24c for field-count c with residue zero in all four cases -- a ThrowsClause.none box is 24 BYTES, and the constant a was exactly 16 x (number of Parsers) in all four, which is the OTHER two new Parser fields (lastWitnessDispatchIndex, lastWitnessDispatchName) as 16 bytes of INLINE struct growth. 24 + 16 = 40. An attribution that only FITS the numbers is not an attribution. ///// ATTRIBUTION 2 IS CORRECT, AND NOW PROVEN BY KNOB RATHER THAN INFERRED: +8 BYTES ON EVERY StdOp BOX, WITH ZERO EXTRA ALLOCATIONS, because witnessTryCall's SEVEN fields make it the widest variant in the flat union (previous maximum six: witnessCall, osReadOverlapped) and the bootstrap sizes a union's payload box by its WIDEST CASE. THE KNOB: adding ONE dummy 8th ValueId field to witnessTryCall and rebuilding EXACTLY DOUBLED the byte delta at three independent program sizes -- 280 -> 560, 9,880 -> 19,760, 38,680 -> 77,360 -- with the allocation delta unchanged at zero. Slope 48.0 bytes per function with one field and 96.0 with two, i.e. 6 StdOp boxes per trivial function x 8 bytes per field. At rung 5 the tax lands ENTIRELY in the three phases that allocate StdOps -- lowerMaxonToStd +2,297,176 bytes, foldConstOperands +417,752, insertRangeChecks +72, all with delta 0 allocations -- and is EXACTLY ZERO in every Maxon-dialect phase (parse, merge, semanticCheck) and in isel, regalloc, encode, link and writeExe. 2.71 MB of a 1.2447 GB compile: 0.218 percent of bytes, 0 allocations, strictly O(StdOp count). ///// AND MaxonOp DID NOT GROW, WHICH IS WHY THE MAXON-DIALECT PHASES READ ZERO: witnessDispatch went 5 -> 6 fields and witnessTryDispatch is 6, but MaxonOp's widest variant is tryCall at SEVEN and always was, so the Maxon box is untouched. The rung got that for free and the per-phase numbers confirm it independently of reading the union. ///// THE 7-FIELD WIDTH IS PAID, DELIBERATELY, AND HERE IS THE RULE RATHER THAN A DEFECT. It is not narrowable without giving up the thing the op is for: all seven fields are witnessCall's six VERBATIM plus the one thing tryCall adds to call (a second def, errorFlagId), which is precisely what lets StdToX64Conversion share the witnessCall lowering rather than re-derive it. Packing methodIndex with argFloatMask would be a magic bitfield; a bool discriminator on witnessCall instead of a distinct variant is the explicitly ruled-out v1 isVoid mistake that shv2's union-typed StdReturnType exists to avoid. STANDING RULE: ANY NEW WIDEST StdOp VARIANT TAXES EVERY StdOp ALLOCATION BY 8 BYTES PER FIELD. The measured price of the 7th is 0.218 percent of a self-scale compile's bytes and zero allocations; an 8th would cost the same again. ///// NO SUPERLINEARITY IN WHAT THE RUNG ADDED, MEASURED ON CORPORA BUILT FOR IT -- because a zero from THIS ladder would have meant nothing (it emits no interfaces, no generics and no witness dispatch; its own manifest says so). Three hand-built ladders, each doubling: (1) WITNESS-DISPATCH ladder, 64 -> 1024 `try self.item.digest()` sites through a `where T is Digest` witness, one throwing interface method: every phase x2.00 in ALLOCATIONS (total x1.85 x1.92 x1.96 x1.98) and every phase x2.00 in CPU TICKS, minimum of 3 runs (total x1.73 x1.88 x1.96 x2.01). (2) CONFORMANCE ladder, 128 -> 1024 conforming types against a 2-method throwing interface, so validateThrowsConformance -- the check this slice added, and it runs in BOTH directions -- is exercised on 256 -> 2,048 pairs: every phase x2.00, total x1.95 x1.97 x1.99. (3) WIDE-INTERFACE ladder, M interface methods x M dispatch sites, 64 -> 1024: allocations x2.00 throughout. The try desugar's rewrite is index-addressed, not scanned (rewriteLastCallToTryCall reads opRefs[count-1] and commitWitnessTryDispatchRewrite does ops.set(opIdx), both O(1)), which is why (1) stays flat. ///// THE CONSTANT FACTORS WERE LEFT ALONE, ON PURPOSE. The code is already alive to the ThrowsClause.none box and avoids it in both hot paths -- Parser.maxon:3793 gates the pre-scan's clause on the `throws` KEYWORD so a non-throwing function allocates neither a clause nor an entry, and SemanticCheck.maxon:1105 uses throwsMap.contains rather than `get otherwise ThrowsClause.none` for exactly this reason ('would box a none for every one of the program's calls that is not to a throwing callee'). The one box this rung adds is 1 per Parser = 2 per file per compile, about 1,490 of 14.8M allocations at rung 5 (0.010 percent), and removing it would mean replacing a `var` field with a new managed global. Not taken: that is a constant factor, and the mandate is growth curves. ///// ONE TERM FOUND, MEASURED, AND FILED RATHER THAN FIXED -- PRE-EXISTING (slice 2a), UNCHANGED BY THIS RUNG. interfaceMethodIndex (IrInterface.maxon:302) and requireInterfaceForParse (Parser.maxon:18542) are LINEAR SCANS run once per witness-dispatch site, over the interface's methods and over the file's interfaces respectively -- a linear lookup inside a walk over dispatch sites, so the term is O(dispatch sites x interface methods). ISOLATED WITH A SIZE-MATCHED KNOB, which is the only way to see it: same M methods, same M dispatch sites, only the TARGETED method's position moved from FIRST to LAST (programs 190,327 vs 191,351 bytes, a 0.5 percent difference that is just longer digit strings). phase:parse costs +13.7 percent at M=D=256, +39.3 percent at 512, +36.2 percent at 1024, minimum of 5 runs each. WHAT IS NOT CLAIMED: a confirmed quadratic EXPONENT. The isolated delta grew 12.7M -> 70.1M -> 133.4M ticks where a clean D x M term predicts x4.00 per doubling, and x5.52 then x1.90 is not that -- the cpu-percent column quantizes at 0.1 percent and the first-position baseline moves too, so this measurement can establish that the term EXISTS and is per-dispatch-site, and cannot establish its exponent. At an absurd 1024-method interface it is worth about 5 percent of the compile. RE-MEASURE TRIGGER: an interface wider than a few dozen methods (machine-generated conformance), or one generic body with many dispatch sites against one wide interface. CURE: index IrInterface.methods by name instead of scanning. ///// CPU COLUMN: NO CLAIM ON THE A/B. The un-bucketed per-rung CPU delta read both signs and sat inside this box's band; four syscall-free 40-byte boxes per file cannot be priced against a 34-giga-tick rung-5 compile. phase:parse's known bend is PLAN.md Workstream O item 122, unchanged between the two binaries and re-confirmed, not rediscovered. ///// GATES: build exit 0 with zero warnings; specs-shv2 x64 1861 passed / 0 failed, exit 0, no leak (no exit 101); specs-shv2/fragments byte-identical -- `git status --short specs-shv2/fragments/` empty after the suite run, and the whole working tree clean, which is what proves this pass emitted byte-identical codegen. | 574,428 | 1,035,029 | 1,955,025 | 3,792,156 | 7,464,361 | 14,805,204 |
| 2026-07-28 | P1.7a slice 2b-iv-B: WASM WITNESS LOWERING (bakeFuncTableIndexRelocs baking a funcref TABLE INDEX into the same .rdata slot x64 bakes a .text VA into, witnessCall/witnessTryCall interned through internIndirectCallType, emitWitnessCall/emitWitnessTryCall, the funcref table enabled on a non-empty pendingRdataRelocs, and the compile-time refusal requireIndirectlyReachableParamsAreMachineWords). NO CODE CHANGE FROM THE OPTIMIZER; the measurements below say why. ///// A/B PROTOCOL: the parent (a2543d15c) was rebuilt in THIS worktree, same path, same bootstrap (bin/maxon.exe sha256 d00fc7df6ebae372cd7f83878913f1ea4d1aee08890fc1bc0373068c3ab72543), and both scale-tests were run from the same 33-character root, so the path artifact is zero rather than subtracted. head sha256 0f7b9cda8d2c86c48e71edc653a24d40400fb396e9e3fb006d79e8b50ea54647, parent 9acbc177db2e1e73e2e34530d7272ee5c7ffffcf3dd22c848b55730e9f549548. RESULT: allocations AND bytes bit-identical at all six rungs, 574,428 / 39,395,954 through 14,805,204 / 1,244,702,896. The implementer's delta-0 claim is confirmed to the digit. ///// THE -412 IS NOW ATTRIBUTED, AND IT IS THE CHECKOUT PATH LENGTH AT 202 BYTES PER CHARACTER. Measured, not inferred: two directory junctions onto this same worktree at 34 and 35 characters, identical binary, identical corpus. The delta versus the committed row reads -412 at 33 characters, -210 at 34, -8 at 35 -- exactly +202 per character, twice over. Located per character: phase:load +134, phase:parse +8, un-bucketed +60, FLAT at every rung (rung 0 and rung 1 each move by exactly those amounts, so it is a fixed set of path buffers crossing size classes, not a per-corpus-file cost). The committed 2b-v row was therefore recorded from a root two characters longer than this one, plus an 8-byte residue. ///// THE IMPLEMENTER'S SUB-CLAIM IS REFUTED AND ITS CONCLUSION UPHELD. It measured phase:load at 1 byte per character (67232 -> 67234 for a 2-character-longer name) and concluded that path length could not produce -412. The 1-byte figure is real and I reproduced it exactly -- 67,502 -> 67,500 for a 2-character-shorter binary NAME on a single-file compile, where the only path in phase:load is argv[0]. It simply does not generalize to the scale corpus, whose phase:load holds many more path buffers and moves 134 bytes per character of ROOT. Right answer, wrong reason: an artifact measured in one bucket does not price the same artifact in another. ///// THE DELTA-0 ABOVE MEASURES UNREACHED, NOT FREE, AND THE IMPLEMENTER SAID SO ITSELF. This corpus generates no interface, no witness dispatch and no managed-opaque-T descriptor, so globalData.pendingRdataRelocs is EMPTY at every rung and BOTH new walks execute ZERO iterations. A ladder was built for them (temp/genwitness.sh): N conformer types against a 2-method interface, every one dispatched through a single `where T is Digest` generic body, N = 32 -> 1024 doubling, so the reloc list doubles 64 -> 2,048. Confirmed per-conformer rather than shared, by the new refusal itself: fed a bool parameter it names `__witness_P0.Digest'+32`, i.e. one witness blob per conformer with two method slots. ///// WASM IS THE ONLY TARGET THAT RUNS THE NEW CODE, and a host-only scale-test cannot see any of it. buildWasmBackend bills the entire backend to phase:encode, so bakeFuncTableIndexRelocs, requireIndirectlyReachableParamsAreMachineWords, the two newly-interned op kinds and emitWitnessCall all land in that one bucket. Compiled by hand with --target=wasm32-wasi --metrics, minimum of 3 per rung. phase:encode ALLOCATIONS 9,280 -> 17,607 -> 34,221 -> 67,414 -> 133,763 -> 266,417 (x1.90 x1.94 x1.97 x1.98 x1.99); phase:encode BYTES 550,083 -> 1,084,886 -> 2,140,163 -> 4,262,508 -> 8,565,812 -> 17,260,267 (x1.97 x1.97 x1.99 x2.01 x2.02); phase:encode CPU 9,619,410 -> 18,823,224 -> 38,886,862 -> 71,012,108 -> 146,346,498 -> 298,904,609 ticks (x1.96 x2.07 x1.83 x2.06 x2.04, the 1.83 inside the band). LINEAR IN RELOCS in all three columns across a 32x span. Neither walk is accidentally quadratic through a map: calleeFuncIdx.get and calleeParamTypes.get are ByteArray-keyed Map lookups built once from module.functions, GlobalDataTable.rdataOffsetOf is a Map lookup too, and the guard's inner loop is over ONE callee's parameter list. ///// AND IT IS CORRECT AT SCALE, not merely fast: the N=1024 rung -- 2,048 baked funcref table indices -- compiles and RUNS under wasmtime with exit 0. ///// X64 A/B ON THE SAME SATURATED LADDER: EXACTLY 0 ALLOCATIONS AND 0 BYTES IN EVERY PER-PHASE BUCKET AND IN THE TOTAL, at N=32 and N=1024 alike. That is where the shared front-end changes (Parser.interfaceReturnMaxonType claiming ExitCode, builtinProtocolResultStdType) get priced, because the parent cannot be A/B'd on wasm at all -- it refuses every program holding a funcAbs64 relocation, and replacing that blanket refusal is the whole slice. ///// A SELF-CORRECTION WORTH MORE THAN THE RESULT: my first x64 A/B read +7 allocations / +330 bytes, flat at all six rungs, and I was one step from filing it as a per-interface cost. It was the same path artifact one level down -- the two binaries sat at ./maxon-shv2/.maxon/maxon-shv2.exe and ./temp/parent-shv2.exe, 16 characters apart in argv[0]. Re-run with both at 20-character paths the delta is exactly zero, in every bucket. A FLAT non-zero delta between two binaries is a path check before it is a finding. ///// CPU COLUMN: NO CLAIM ON THE A/B. Single sample; the dominant buckets read +0.4 percent phase:regalloc, +0.1 percent regalloc:splitting, +0.2 percent total -- inside the band -- while the sub-1-percent phases swing +-28 percent, which is what an un-repeated CPU sample looks like. ///// NOTHING WAS CHANGED, AND TWO CANDIDATES WERE DECLINED ON PURPOSE. (1) The two walks over pendingRdataRelocs could be fused into one, halving the map lookups. DECLINED: the guard is a compile-time REFUSAL and must finish before any slot is patched, so fusing would panic with the .rdata image half-baked -- and it is a constant factor on a walk worth low single digits of one phase. (2) emitIndirectCallOp rebuilds the signature key (a WasmValTypeArray plus an encoded ByteArray) per indirect-call op just to look up the interned type index: 2 allocations per witness call, about 4,096 of phase:encode's 266,417 at N=1024, 1.5 percent. DECLINED: pre-existing (emitCallIndirect did exactly this before this slice, which merely routes two more op kinds through it), O(1) per op, and rebuilding through the same two helpers internIndirectCallType built it with is the stated invariant that keeps the call-site immediate from disagreeing with the interned type. Trading a correctness-by-construction invariant for 1.5 percent of one phase's allocations is not the mandate. ///// TWO KNOWN TERMS RE-CONFIRMED, NOT REDISCOVERED. This ladder bends in phase:parse (x2.56 x3.05 x2.80 on wasm, where no register allocator runs) and in regalloc:splitting (x3.08 x3.20 x3.20 on x64). Those are Workstream O item 122 -- interfaceMethodIndex and requireInterfaceForParse as linear scans per witness-dispatch site, filed by the 2b-v pass -- and the budgeted splitter re-liveness (ARCHITECTURE.md:1336-1345). Both read allocation-identical between the two binaries, so neither moved, and neither is this slice's to fix. ///// GATES: build exit 0 with zero warnings; specs-shv2 x64-windows 1865 passed / 0 failed, exit 0, no leak; specs-shv2 wasm32-wasi 1754 passed / 0 failed, exit 0, no leak; `git status --short specs-shv2/fragments/` empty after BOTH suite runs and 0 files changed versus main, and the whole working tree clean -- which is what proves this pass emitted byte-identical codegen on both targets. | 574,428 | 1,035,029 | 1,955,025 | 3,792,156 | 7,464,361 | 14,805,204 |
| 2026-07-28 | P1.8 SLICE A -- `for` loops (numeric ranges + Array iteration). NO CODE CHANGE FROM THE OPTIMIZER: nothing measured superlinear, and the two constant-factor candidates were both unmeasurable (see REJECTED below). The deliverable is a COMMITTED LADDER, because this corpus cannot see the rung at all. ///// A/B PROTOCOL: the parent (016751f8b) was rebuilt in THIS worktree from this worktree's bootstrap and both binaries were measured at the SAME canonical path (maxon-shv2/.maxon/maxon-shv2.exe, swapped between runs), so the two differ only in shv2 source. ⚠ THE COMMITTED ROW ABOVE IS NOT A VALID BASELINE HERE: it was written in a 51-character worktree root and this one is 28, and the in-tree parent reproduces it at +14 allocations / -4,109 bytes FLAT at all six rungs -- a path artifact, not a change. So the delta column of THIS row carries that artifact too (+156 = +142 real, +14 path). Every attribution below is head-minus-in-tree-parent, which is artifact-free. ///// THE DELTA, head vs in-tree parent: +142 ALLOCATIONS, FLAT AT ALL SIX RUNGS across a 32x ladder -- O(1) PER COMPILE, not per function, per block, per op or per binding access. Bytes are 6,288 + 1,008 * 2^rung: +7,296 +8,304 +10,320 +14,352 +22,416 +38,544, whose successive differences are 1,008 2,016 4,032 8,064 16,128 -- EXACTLY DOUBLING, i.e. perfectly linear in corpus size. ATTRIBUTED WITH NO RESIDUE: the +142 is +108 `unattributed` (building the new `__arr_get_unchecked` Std graph, which sits outside every CompilePhase probe) plus +28 `phase:isel` (selecting it), once per compile; the 1,008 * 2^rung bytes is 8 bytes per record on `LoopPhiVar.entryValue` and `LoopContext.continueTargetId`, paid by the corpus's `while` loops -- the corpus has no `for`, so this whole byte term is the OLD construct carrying the new field. The implementer's own claim (`6288 + 1008 * 2^n`) reproduces to the digit. ///// ⛔ THE CORPUS IS STRUCTURALLY BLIND TO THIS RUNG, and a Delta-0 from it would be the instrument's blind spot and not the cost. `ScaleCorpus` generates ZERO `for` loops -- it could not, they did not parse until this commit -- so the lowering, the element read and `Parser.iterationLockedBindings` are invisible in allocations, bytes AND CPU alike. The cure is precedent, not invention (P1.7a 2b-ii built its own generics ladder for the same reason): this rung commits `maxon-shv2/Testing/ladders/genfor.sh <loops> <depth> <accesses> <array or range or noloop> <out>`, whose knobs are independent -- program size is loops x accesses, so DEPTH MOVES ALONE. ///// THE READING THAT MATTERS IS THE NO-LOOP CONTROL, because the lock is read at FOUR doors on ordinary binding-access paths (`immutableReceiverNameOf`, `parseFieldChainCall`, `selfFieldIsWritable`, `requireMutableBinding`) -- a per-access tax on every program for a feature it does not use is the exact shape of the defect the P1.7a 2b-i pass found. Measured `genfor.sh N 1 2 noloop` at N = 64/128/256/512/1024, head against the in-tree parent: `phase:parse`, `phase:lex`, `phase:signatures`, `phase:merge`, `phase:lowerMaxonToStd` and `phase:regalloc` are IDENTICAL TO THE DIGIT at all five rungs (parse: 14,816 / 27,842 / 53,863 / 105,877 / 209,874 both ways). The entire delta is the same +142 flat, in the same two buckets. NOTHING PER ACCESS. That is what the `count() == 0` guard in front of the walk buys, and it is now measured on a program built to defeat it rather than on a corpus that never enters it. ///// NOTHING SUPERLINEAR, WITH THE LADDERS THAT ESTABLISH IT. loops 64 -> 1,024 (`genfor.sh N 1 2 array`): `phase:parse` allocations x2.00 x2.00 x2.00 x2.00, CPU x2.04 x2.02 x1.99 x1.99, and every other phase x2.00. `range` mode (literal bounds, so the lock stack stays EMPTY): x2.00 in every phase in both columns. accesses 1 -> 8 at a fixed loop count: linear per marginal statement. DEPTH, constant size (`genfor.sh D D 512/D array` -- one function, exactly 512 accesses at every rung, 1,043 -> 1,057 lines): `phase:parse` allocations 6,541 / 6,674 / 6,940 / 7,469, an excess over depth 1 of 133 / 399 / 928 = 133 x (1, 3, 7), EXACTLY proportional to (D-1). That is `namesAssignedIn`'s documented Theta(tokens x depth) scan, LINEAR IN DEPTH, and the lock walk adds nothing measurable on top of it. `phase:regalloc` reads x7.21 from depth 1 to 2 then x1.68 x1.95 -- a MODE CHANGE (the splitter engages at all once two loops nest), not a curve, and the identical shape appears on a nested-`while` ladder built against the PARENT binary (x5.40 x4.43), so it is not this rung's. ///// STAR -- THE DEPTH KNOB HAS A CEILING THE COMPILER ITSELF ENFORCES, which is why `bindingIsIterationLocked`'s linear scan cannot go quadratic on anything that compiles. Every `for` mints an index phi live across every loop nested inside it, so depth D holds D values at once and E5001 refuses: MEASURED, depth 10 compiles and depth 11 is "needs 1 more register(s) than are available". `gennest.sh` evades exactly this by giving every `while` level the SAME guard variable; a `for` cannot, because the counter is minted by the lowering and not by the program. So the lock stack is bounded by ~10 in any program that builds, and an O(depth) scan behind an empty-stack guard is O(1) in practice. ///// PRE-EXISTING BEND RE-CONFIRMED, NOT REDISCOVERED: `phase:parse`'s corpus growth is x3.18 on the parent and x3.19 on head at rung 5 -- UNCHANGED -- and it is PLAN.md Workstream O item 122 (the Theta(bindings in scope x constructs) walk `mintLoopHeaderPhis` and `assignedBindingsIn` share). `for` now joins `while`, `if` and `match` in triggering it and makes it NO WORSE: a hand ladder of N/2 mutable bindings then N/2 loops in one function reads `phase:parse` CPU x2.00 x2.17 x2.29 x2.67 x3.03 in the `for` spelling against x2.12 x2.12 x2.40 x2.61 x3.09 in the `while` spelling ON THE PARENT BINARY, with allocations flat at x1.99 in both -- the same curve, and exactly the shape a memory-only instrument cannot see. Not fixed here: it is not this rung's, the obvious inversion is already measured a NET LOSS in `namesAssignedIn`'s header (`phase:parse` x3.5 on the corpus), and the real cure named there is a bottom-up carried-set redesign. ///// ONE UNATTRIBUTED CPU READING, REPORTED RATHER THAN LAUNDERED: `phase:signatures` runs ~+7% on the corpus (and ~+10% on the no-loop ladder) head against parent, FLAT at every rung, with allocations IDENTICAL TO THE DIGIT and the growth ratio unchanged at x2.01 both ways. It is a CONSTANT FACTOR, not a curve. The only new per-token work in that phase is one enum comparison in `opensBlockAt` (`or kind == TokenKind.for`), and that cannot be it by two orders of magnitude: the rung-5 delta is 56.9M ticks over roughly 5 x 10^5 tokens, about 114 ticks per token, against ~1 for a compare. Most likely code layout between two different binaries -- `phase:lex`, whose source is IDENTICAL, scatters +-5% in BOTH directions across the same pair. Worth 0.23% of the compile; recorded so the next reader does not rediscover it as new. ///// REJECTED, ON THEIR NUMBERS: (1) hoisting the `count() == 0` test in front of `iterationLockNameAt`'s `scope.contains` + `scope.getInfo`, so a field-chain array method call in a loop-free program skips a hash lookup. It allocates nothing either way, so the exact column cannot price it, and the ladder built to maximise it (one `b.items.push` per statement, 1,024 of them) reads `phase:parse` CPU inside the layout band. A change I cannot measure is not an optimization. (2) The `count() == 0` guard the implementer says it PREDICTED would matter and MEASURED identical -- kept, and now justified by a measurement it did not have: on the `noloop` control the guard is what makes the four doors cost the digit-identical zero above. ///// REPORTED TO THE COORDINATOR, OUT OF THIS PASS'S SCOPE (a correctness question, not a cost): `for i in n upto 3 'l' ... n = n + 1 ... end` is E2013 in shv2 and COMPILES AND RUNS in the bootstrap oracle (returns 3). `lockIterationSource` is called BEFORE the range/array fork, so a RANGE whose start bound is a NAME locks that name for the whole body -- although the rung's own header states both bounds are evaluated once in the preheader, and the END bound is correctly writable (`for i in 0 upto n` with `n = n + 1` compiles). An asymmetry between the two bounds of one range, and a divergence from the oracle on a legal program. | 574,422 | 1,034,929 | 1,954,737 | 3,791,492 | 7,462,945 | 14,802,284 |
| 2026-07-28 | P1.8 SLICE A -- INDEPENDENT REVIEW. Two reachable defects fixed (a 5th mutation door segfaulting on all four spellings of a self-field write during `for … in` over that field; a missing range-bound type guard that PANICKED the x64 emitter on a float range and silently accepted a bool one), three duplications single-sourced, 15 pinning tests. Suite 1891/0 -> 1906/0. ///// COST OF THE REVIEW ITSELF: +0 ALLOCATIONS at every rung, and BYTES -64 -112 -208 -400 -784 against the optimizer's row -- successive differences -48 -96 -192 -384, exactly doubling, i.e. linear in corpus size. That is `LoopContext.headerId` deleted: 8 bytes off every loop context, paid back by the corpus's `while` loops. The field was WRITE-ONLY (declared, constructed, written at both loop parsers, read NOWHERE) and its value coincided with `continueTargetId` for `while` and not for `for` -- the trap with the check removed. The two new guards are behind the existing `count() == 0` test and allocate nothing. ///// ***THE PREVIOUS ROW'S ONE UNATTRIBUTED READING IS NOW ATTRIBUTED, AND ITS STATED HYPOTHESIS IS REFUTED BY ITS OWN CONTROL.*** That row recorded `phase:signatures` ~+7% head-vs-parent, flat, allocations identical, and offered "most likely code layout between two different binaries", citing `phase:lex` (IDENTICAL SOURCE) as scattering +-5% in BOTH directions. Re-measured here against a THIRD, independently built binary (review-head vs `main` rebuilt in this worktree, both run from the same canonical path, 5 rungs, repeat=5): `phase:signatures` is +8.6% +10.4% +8.1% +8.4% +8.6% -- CONSISTENTLY POSITIVE at every rung. The control does NOT scatter: `phase:lex`, whose source is identical in all three binaries, reads -2.4% -2.7% -3.1% -3.1% -1.8% -- consistently NEGATIVE and three to five times smaller; `phase:merge`, also untouched, is -2.2%..+0.7%. So the binary-identity component on this pair is about -2 to -3% with a STABLE SIGN, and signatures sits ~11 points away from it in the OPPOSITE direction. It is real work, not layout. ***>>> CORRECTION 2026-07-28, AND THIS SENTENCE IS THE ONE THAT WAS WRONG: THE +8.6% DOES NOT SURVIVE INTERLEAVING AND IS A MEASUREMENT-PROVENANCE ARTIFACT.*** User hypothesis ("some of the measurements were done on a different machine"), tested on an IDLE box with the two binaries swapped at ONE canonical path and the runs INTERLEAVED P/H/P/H (4 pairs, rungs=3, repeat=3, both phases read from the SAME run's table): `phase:signatures` reads +1.03% +0.55% +1.23% (overall mean +0.94%) and CHANGES SIGN across pairs (-3.43% +0.68% +3.94% +3.74%), while the control `phase:lex` REPRODUCES at -2.24% -1.51% -1.31% (mean -1.69%). The entire claim above rested on signatures being "CONSISTENTLY POSITIVE at every rung"; interleaved, that consistency is gone and the effect cannot be distinguished from zero. WARNING -- WHY THE `phase:lex` CONTROL DID NOT CATCH IT, which is the transferable lesson: lex excludes a UNIFORM factor, but a drift confined to a TIME WINDOW need not move every phase equally -- lex reproduced and signatures did not, i.e. the two phases have DIFFERENT sensitivity to the drift, which is exactly what disqualifies one as a control for the other. A stable SIGN across rungs is NOT evidence of a real effect when all head runs sit in one time window and all parent runs in another; only INTERLEAVING separates code from clock. UNAFFECTED AND STILL TRUE: the BYTE finding (allocation count identical to the digit, bytes +768/+1,320/+2,424/+4,632/+9,048 -- the records that already existed got bigger). Bytes are exact and reproducible; it was only the CPU story built on top of them that failed. ///// WHAT IT IS: `phase:signatures` allocation COUNT is identical to the digit at every rung (14,061 / 23,816 / 43,209 / 81,854 / 158,940 both binaries) while its BYTES move +768 +1,320 +2,424 +4,632 +9,048 -- successive differences 552 1,104 2,208 4,416, exactly doubling, over a constant +216. Same count, more bytes: THE RECORDS THAT ALREADY EXISTED GOT BIGGER, and nothing new is allocated. That is the 8-byte fields the rung added (`Parser.iterationLockedBindings` -- a `Parser` is built per top-level constant by `SignatureIndex.walkConstInitializer` -- plus `LoopPhiVar.entryValue` and `LoopContext.continueTargetId`). ///// ⚠ THE BYTE GROWTH IS THE MARKER, NOT THE COST: +9,048 bytes on 7,316,360 is +0.12%, which cannot buy +8.6% of CPU. What the bytes prove is WHICH objects changed; the CPU is spread across the several constant-factor additions on that phase's paths (a wider `Parser` record and its constructor, `opensBlockAt`'s extra arm, `parseBlockBody`'s new parameter and empty-block test, `edgeArgsFor`'s selector, `fieldChainMethodCallAt` now going through `fieldChainEndAfter`). Isolating which one needs PER-FUNCTION CPU attribution, which shv2 does not have -- that is the instrument gap this reading names, and it is why "unattributed" was the honest word before and "real, unresolved between five candidates" is the honest word now. Worth ~0.4% of the compile (signatures is ~4.8% of it). NOT chased: every candidate is the price of the feature, and no single one is measurable apart from the others. ///// REJECTIONS RE-CHECKED AND UPHELD: hoisting `count() == 0` in front of `iterationLockNameAt`'s two hash lookups stands rejected -- confirmed it has exactly ONE caller (`parseFieldChainCall`, an array-typed field-chain receiver with a trailing call), so it is a narrow path and not a per-statement tax, the maximising ladder read inside the band, and the guard it would duplicate already exists one call deeper in `bindingIsIterationLocked`. Shipping it would be the unmeasurable change the previous pass correctly declined. | 574,422 | 1,034,929 | 1,954,737 | 3,791,492 | 7,462,945 | 14,802,284 |
| 2026-07-28 | P1.8 SLICE A -- INDEPENDENT REVIEW. Two reachable defects fixed (a 5th mutation door segfaulting on all four spellings of a self-field write during `for … in` over that field; a missing range-bound type guard that PANICKED the x64 emitter on a float range and silently accepted a bool one), three duplications single-sourced, 15 pinning tests. Suite 1891/0 -> 1906/0. ///// COST OF THE REVIEW ITSELF: +0 ALLOCATIONS at every rung, and BYTES -64 -112 -208 -400 -784 against the optimizer's row -- successive differences -48 -96 -192 -384, exactly doubling, i.e. linear in corpus size. That is `LoopContext.headerId` deleted: 8 bytes off every loop context, paid back by the corpus's `while` loops. The field was WRITE-ONLY (declared, constructed, written at both loop parsers, read NOWHERE) and its value coincided with `continueTargetId` for `while` and not for `for` -- the trap with the check removed. The two new guards are behind the existing `count() == 0` test and allocate nothing. ///// ***THE PREVIOUS ROW'S ONE UNATTRIBUTED READING IS NOW ATTRIBUTED, AND ITS STATED HYPOTHESIS IS REFUTED BY ITS OWN CONTROL.*** That row recorded `phase:signatures` ~+7% head-vs-parent, flat, allocations identical, and offered "most likely code layout between two different binaries", citing `phase:lex` (IDENTICAL SOURCE) as scattering +-5% in BOTH directions. Re-measured here against a THIRD, independently built binary (review-head vs `main` rebuilt in this worktree, both run from the same canonical path, 5 rungs, repeat=5): `phase:signatures` is +8.6% +10.4% +8.1% +8.4% +8.6% -- CONSISTENTLY POSITIVE at every rung. The control does NOT scatter: `phase:lex`, whose source is identical in all three binaries, reads -2.4% -2.7% -3.1% -3.1% -1.8% -- consistently NEGATIVE and three to five times smaller; `phase:merge`, also untouched, is -2.2%..+0.7%. So the binary-identity component on this pair is about -2 to -3% with a STABLE SIGN, and signatures sits ~11 points away from it in the OPPOSITE direction. It is real work, not layout. ///// WHAT IT IS: `phase:signatures` allocation COUNT is identical to the digit at every rung (14,061 / 23,816 / 43,209 / 81,854 / 158,940 both binaries) while its BYTES move +768 +1,320 +2,424 +4,632 +9,048 -- successive differences 552 1,104 2,208 4,416, exactly doubling, over a constant +216. Same count, more bytes: THE RECORDS THAT ALREADY EXISTED GOT BIGGER, and nothing new is allocated. That is the 8-byte fields the rung added (`Parser.iterationLockedBindings` -- a `Parser` is built per top-level constant by `SignatureIndex.walkConstInitializer` -- plus `LoopPhiVar.entryValue` and `LoopContext.continueTargetId`). ///// ⚠ THE BYTE GROWTH IS THE MARKER, NOT THE COST: +9,048 bytes on 7,316,360 is +0.12%, which cannot buy +8.6% of CPU. What the bytes prove is WHICH objects changed; the CPU is spread across the several constant-factor additions on that phase's paths (a wider `Parser` record and its constructor, `opensBlockAt`'s extra arm, `parseBlockBody`'s new parameter and empty-block test, `edgeArgsFor`'s selector, `fieldChainMethodCallAt` now going through `fieldChainEndAfter`). Isolating which one needs PER-FUNCTION CPU attribution, which shv2 does not have -- that is the instrument gap this reading names, and it is why "unattributed" was the honest word before and "real, unresolved between five candidates" is the honest word now. Worth ~0.4% of the compile (signatures is ~4.8% of it). NOT chased: every candidate is the price of the feature, and no single one is measurable apart from the others. ///// REJECTIONS RE-CHECKED AND UPHELD: hoisting `count() == 0` in front of `iterationLockNameAt`'s two hash lookups stands rejected -- confirmed it has exactly ONE caller (`parseFieldChainCall`, an array-typed field-chain receiver with a trailing call), so it is a narrow path and not a per-statement tax, the maximising ladder read inside the band, and the guard it would duplicate already exists one call deeper in `bindingIsIterationLocked`. Shipping it would be the unmeasurable change the previous pass correctly declined. | 574,422 | 1,034,929 | 1,954,737 | 3,791,492 | 7,462,945 | 14,802,284 |
| 2026-07-28 | P1.7a SLICE 2b-vi (multi-argument witness dispatch) -- OPTIMIZATION PASS. NO CODE CHANGE, and the measurements are why. ///// ***READ THE +148..+3062 DELTA ABOVE AS `main`'s ADVANCE SINCE THE 2026-07-28 ROW, NOT AS THIS RUNG'S.*** That was the implementer's claim and it is CONFIRMED INDEPENDENTLY, not taken: `main` @153d04620 was rebuilt from the same bootstrap in a control worktree whose path is the SAME LENGTH (33 chars, `maxon-p17a-ctrl` vs `maxon-p17a-2bvi`) so the phase:load path-length artifact is zero rather than subtracted, and its scale-test reproduces 574,570 / 1,035,171 / 1,955,167 / 3,792,298 / 7,464,503 / 14,805,346 -- IDENTICAL TO THE DIGIT to the head run, and carrying the identical +148..+3062 delta. A full text diff of both runs' per-phase ALLOCATION and BYTE tables is EMPTY: no phase, at any rung, differs by one allocation or one byte. This rung's own delta against its parent is ZERO everywhere. ///// AND THAT ZERO MEASURES **UNREACHED, NOT FREE** -- PROVEN FROM THE CORPUS, NOT ASSUMED. `--emit-corpus` over all 465 generated files: 276 `interface`, 276 `implements`, 270 `uses` -- and **ZERO `where`, at every rung**. The corpus's own comment says it: "4 unconstrained generic bases". A witness dispatch needs `where T is <Interface>`, so `Parser.parseWitnessMethodOnValue` -- every line this rung touched -- runs EXACTLY ZERO TIMES in the whole ladder. This is the trap slices 2b-ii, 2b-v and 2b-iv-B each hit; the answer again is a ladder built by hand. ///// NEW INSTRUMENT: `maxon-shv2/Testing/ladders/genwitnessargs.sh`, the ARGUMENT-COUNT axis `genwitness.sh` holds at one -- knobs conformers x methods x args x label ORDER (`decl` or `reverse`). The two orders are THE SAME PROGRAM TO THE BYTE (fixed-width labels, the literal always `1`), so subtracting them is the label SCAN and nothing else. ///// PER-CALL-SITE COST OF THE RUNG, exact and differential (same source, both binaries, `--log=compiler:debug`): **+14 allocations and +465 bytes per witness dispatch, IN `phase:parse` ONLY**, and dead flat across a 16x span -- methods 4/8/16/32/64 gave +56/+112/+224/+448/+896 allocations and +1,860/+3,720/+7,440/+14,880/+29,760 bytes, i.e. 14.00 and 465.0 per site every time. Every other phase reads delta-0 to the digit. LINEAR IN CALL SITES; nothing superlinear. ///// ATTRIBUTED, 13 of 14, BY PROBE RATHER THAN BY ARITHMETIC: a throwaway `ValueIdArray.create()`+push added per call site cost EXACTLY 2 allocations (measured +4 at 2 sites, +68 at 34, at both arity 1 and 63) => an array is 2. Six arrays are net-new (`userArgs`, `argLabels`, `argRanges`, `argImmutableNames`, plus `slotCallArgs`' `slotIds`+`filled`; `withWitnessReceiver`'s array REPLACES the old hand-rolled `args`, so it is net 0) = 12. A throwaway `ArgSlotResult.ordered(...)` cost EXACTLY 1 (+2 at 2 sites, +34 at 34, arity-independent) => the union box `slotCallArgs` returns = 1. Total 13; the residual 1 is the assumption that `BoolArray`/`SourceRangeArray` cost what `ValueIdArray` costs, which is the only thing here not separately measured. ///// ARITY IS LINEAR, NOT QUADRATIC. Isolating one call site exactly (one interface, one conformer, one method, N identical dispatch lines, so dN is a call site and nothing else): per-site allocations 36.7 / 41.6 / 52.6 / 79.9 / 126.9 / 214.2 / 375.7 at args 1/2/4/8/16/32/63 -- a clean **~31 + 5.5xA**, residuals +-7% (array growth stepping). A P^2 term would put args=63 near 830, not 376. ///// `WitnessDispatchTarget.paramNames` IS A SHARED REFERENCE, NOT A PER-SITE COPY -- settled by experiment, since shv2 has no per-scope allocation attribution outside the scale corpus and the corpus cannot express a witness dispatch (`build --mm-trace` does not exist). A SECOND, redundant `ByteArrayArray` field added to `WitnessDispatchTarget` and stored from the same source moved the per-site cost by **exactly 0 allocations** at both arity 1 and 63. A `let` field store shares the buffer. ///// `argSlotPosition`'s O(P) FAST PATH IS INTACT -- the check the refactor most endangered, since routing through `indexOfParamIn` unconditionally would restore an O(P^2)-per-call term no correctness test would notice. Re-ran the ladder its own header cites, `genmutchain.sh 1 256 <P>`, on BOTH binaries: `phase:semanticCheck` allocations and bytes IDENTICAL TO THE DIGIT at P=8/16/32/64 (6,333 / 10,975 / 19,742 / 36,749), CPU ticks head/parent 0.97 / 0.96 / 0.97 / 1.01 -- inside the noise band -- and growth x1.73 x1.85 x1.88 per doubling of P (head) against x1.75 x1.83 x1.80 (parent). Linear both. The x3.05 quadratic that header records as removed is still removed. ///// A BONUS THE RUNG DID NOT CLAIM: `Parser.transferConsumedArgs` used to re-spell the mapping inline (`i if label.isEmpty() else info.indexOfParam(label)`) and so had NO fast path; narrowing `argSlotPosition` to take a name LIST is what let it call the shared door, so the parser's consume transfer GAINED the O(P) fast path it never had. ///// THE ONE QUADRATIC THAT EXISTS IS PRE-EXISTING, SHARED, AND BOUNDED -- filed, not fixed. Fully REVERSE-ordered labels miss the fast path on every argument and fall back to `indexOfParamIn`'s scan: ~P^2/2 comparisons per call. Measured on a DIRECT-call ladder both binaries accept (64 functions, first arg positional, rest reverse-labelled), exact per-phase ticks from `--metrics`: at P=63 head 33,495,572 vs parent 33,850,842 -- head marginally FASTER, well inside noise, allocations identical -- so the rung neither introduced it nor moved it, and per-doubling it runs x2.04 x2.63 x3.05 (decl: x1.58 x1.64 x1.78). The witness path inherits precisely the same cost and no more: at P=63 the reverse-minus-decl scan is **420,906 ticks per witness call site vs the direct call's 411,140 -- 2.4% apart**, which is the cleanest available statement that 2b-vi routed witness dispatch onto the existing shared door rather than growing a second one. AND IT CANNOT GROW: **P is capped at 63 by the language itself** -- args=64 is refused `E2015: a function with 65 argument slots -- more than the 64 a call can carry` (the 64th is the receiver; the cap is the float mask's width). A quadratic in a variable the type system bounds at 64 is a CONSTANT, ~2,048 comparisons per call site, linear in program size. Not chased: the only cure is a per-signature name-to-index map, which `indexOfParamIn`'s header already rejects with its reason (a heap object on every signature, in every program, to remove a bounded term on a shape nobody writes), and it would be a real memory regression bought to win a pathological input. ///// GATES: build 0 warnings, suite 1940/0 exit 0, and `git status --short specs-shv2/fragments/` EMPTY after a full suite run -- byte-identical codegen. | 574,570 | 1,035,171 | 1,955,167 | 3,792,298 | 7,464,503 | 14,805,346 |
| 2026-07-28 | E3070 BORROW LIVENESS — a collection may not be mutated while an element borrowed out of it is live. shv2's `arr.get(i)` hands back the element POINTER and takes no reference for it, so any write that shortens or replaces the array frees a record the caller still holds. Measured 0xC0000005 before this on `clear`, `push`, a rebind `arr = other`, `grow(arr)`, a field chain `b.items.clear()`, a bare self-field alias, a method clearing its own field (`b.wipe()`, which printed 4557430888798830399 out of freed memory where the oracle prints 44), and FOUR spellings over top-level MODULE storage. Suite 1922 -> 2003/0. ///// COST, PATH-CLEAN A/B against parent 2a0dcd8d9, RUNGS 0-3: allocations +118 +199 +358 +669, bytes +32,960 +55,624 +105,096 +202,496. Delta ratios x1.69 x1.80 x1.87 converging on x2.00 against the corpus's own x1.80 x1.88 x1.93 => LINEAR, a constant factor per unit of source, +0.018% allocations and +0.071% bytes at rung 2. Bytes are two 8-byte fields making records that already existed bigger (`IrFunction.borrowFacts`, and the per-binding subject id) plus the borrow records themselves; `phase:merge` moves bytes with +0 allocations, which is that signature exactly. ///// ***READ THIS BEFORE YOU TRUST ANY DELTA THIS INSTRUMENT PRINTS: THE PATH-LENGTH ARTIFACT BIT AGAIN, AND IT PRODUCED THREE DIFFERENT ANSWERS FOR ONE CHANGE.*** Path strings are allocated, so allocation counts depend on the LENGTH OF THE DIRECTORY THE COMPILER RUNS FROM. (a) The delta compares against the row below, which was written from a 28-char worktree: on an UNCHANGED main, from the 22-char main checkout, that reads +340/+434/+622 -- the artifact alone, no code. (b) The coordinator's own head-vs-main A/B, taken from two DIFFERENT paths (22 vs 28 chars), read -93/-12/+145 -- the artifact with the sign REVERSED, which made a real cost look like a saving. (c) Both binaries run from ONE canonical path read +118/+199/+358. Only (c) is a measurement. ⇒ **AN A/B IS ONLY VALID IF BOTH BINARIES ARE EXECUTED FROM THE SAME DIRECTORY**, swapped in place; and a committed row is a baseline ONLY for runs from the path that wrote it. This row was written from the main checkout for exactly that reason. It is the same lesson the P1.8 Slice A row recorded as "+142 real plus a +14 path-length artifact" -- recorded there, and still not enough to stop it happening twice more. ///// TWO QUADRATICS FOUND AND FIXED IN-RUNG, neither filed: (1) `BorrowCheck.reportFirstLiveBorrow` restarted at the head of the subject's bucket for every write => O(sites x borrows-of-subject); a borrow dead at one site is dead at every later one (its last use is fixed once parsing ends, and sites arrive in token order), so a per-subject CURSOR steps over it once and for all -- 94.3M -> 0.10M ticks at K=1024, and the delta stops growing. (2) the pending-claim list never shrank, because `s = try arr.get(0) otherwise ""` is an ASSIGNMENT and no binding completes the borrow, so `retargetPendingBorrow`/`attachPendingBorrows` re-walked the whole list every time; now cut to a per-statement WATERMARK -- a watermark and not a reset, because `otherwise return` re-enters the statement parser mid-initializer and a reset there is a measured 0xC0000005. Both amortization arguments are ORDER arguments and both are now checked where the order is established, with a panic on violation: silently skipping a live borrow is a use-after-free nobody would be told about. Sabotage confirmed both panics fire. ///// ⛔ **THE CORPUS WAS NOT BLIND HERE -- IT WAS NARROW, WHICH READS AS COVERAGE AND IS WORSE.** `ScaleCorpus`'s `v_marray.maxon` does hold one `let mgN = try msN.get(0) otherwise ""` per managed-Array group, so `BorrowCheck` runs and returns a healthy non-zero at every rung. But every subject carries EXACTLY ONE borrow and every accessor is bound to a name, so both quadratics above have degree 1 there and CANNOT appear. A non-zero reading from a corpus that cannot express the shape is not evidence. `maxon-shv2/Testing/ladders/genborrow.sh` is committed (knobs: borrows per subject, mutation sites, distinct subjects), and it is what read the x3.8-x4.1 per doubling that the fixes deleted. ///// MEASURED DEBT, NOT FIXED: `Parser.borrowSubjectIdOf` is a linear scan of the distinct-subject list, entered once per mint and once per recorded write => O(K^2) in K distinct borrowed-and-written storages in ONE body (x3.99 x2.23 x2.66 x3.08). Fitted aK+bK^2 over the top two rungs: a=15,697, b=36.2 ticks, so the quadratic term does not equal the linear one below K~434 distinct subjects in a single function body. The corpus reaches 256 (~2.4M ticks at rung 5, ~0.02% of the compile); hand-written code reaches single digits. TRIGGER TO RE-MEASURE: machine-generated code declaring hundreds of distinct arrays in one body. Both closures would tax every program (8 bytes on every VarInfo, or a per-function Map), which is why it is filed rather than shipped. ///// A NO-BORROW CONTROL says the recording is genuinely demand-driven: a function that borrows nothing allocates ZERO extra at every K and the shared-empty `BorrowFacts` is never copy-on-written; parse CPU on a program that is nothing but array method calls is +5.2..+7.0%, spread across several constant-factor additions with no single one measurable apart from the others -- the same per-function CPU attribution gap the row below names. | 574,880 | 1,035,562 | 1,955,717 | 3,793,159 | 7,465,981 | 14,808,049 |
| 2026-07-28 | int->float PROMOTION DOORS. SEVEN doors fixed, all sharing one shape: "ask checkDeclaredType, accept ok, forget to convert". `return` PANICKED (X64Backend.maxon:715); struct-literal store, field assign, self-field assign, field default, global init and the LOCAL `var` REBIND all SILENTLY STORED THE RAW INT BIT PATTERN (~1.5e-323, coordinator-reproduced: `p.raw() == 3.0` FALSE, exit 7). ///// allocsDelta 0 AT ALL SIX RUNGS -- the conversion reuses the existing MaxonUnaryOp.promoteToFloat -> StdArithUnaryOpcode.siToFp, which already lowers on all three backends (x64 cvtsi2sd, wasm emitSiToFp, arm64 scvtf), so no new op and no target gate. ///// bytesDelta IS REAL AND FULLY ATTRIBUTED: +960/+1,632/+2,976/+5,664/+11,040/+21,792 net of the flat +206 CHECKOUT-PATH-LENGTH artifact (this worktree's path is one char longer than the logged control's; a flat non-zero delta is a PATH CHECK before it is a finding). Over the corpus's struct-field counts 60/102/186/354/690/1,362 that is **EXACTLY 16.00 BYTES PER STRUCT FIELD AT EVERY RUNG, TO THE DIGIT**. ///// CAUSE: FieldDefault.literal gained a TAG field so the widening decision reads the literal's own tag instead of resting on a SEPARATE FALSE REJECTION elsewhere (a correctness argument propped up by a bug -- removed on coordinator ruling). A Maxon union box is sized to its WIDEST ARM, so one new field costs 8 B per box; the box COUNT is unchanged, which is exactly why allocations did not move. TWO layouts are built per compile (phase:signatures' sweep and phase:parse's real parse, one StructLayout each) -- the deltas split identically to the byte between those two phases -- so it is 8.00 B/field PER LAYOUT and **16.00 B/field PER COMPILE**. ///// NOTE THE COMMIT MESSAGE `df7b91dfb` STATES 8.00 AND IS HALVED: it quoted the per-layout figure as the per-compile one. The mechanism it describes is correct; only the headline quotient is wrong. Corrected here and in PLAN.md, which are the durable records. 0.0018% of compile bytes at rung 5. Strictly linear in fields. ///// The widening ARM is unreached by the scale corpus (no int meets a float slot in it), measured instead on a hand ladder at +4.1 allocations per widened site, flat, x1.89 x1.94 x1.97 across 64->512 sites -- one extra IR op per conversion, linear. | 574,570 | 1,035,171 | 1,955,167 | 3,792,298 | 7,464,503 | 14,805,346 |
| 2026-07-28 | P1.8 SLICE C — the SEVEN byte/ASCII String methods (`startsWith` `endsWith` `contains` `toLower` `toUpper` `replace` `split`), as eight Std-tier runtime graphs plus parser dispatcher arms. Suite 2003 -> 2036/0 on its own base, 2056/0 after rebasing onto upstream. ///// ⭐⭐ **THE HEADLINE IS NOT THIS SLICE'S COST. THE LADDER BUILT TO SEE THIS SLICE FOUND A QUADRATIC IN `__str_append` THAT HAD BEEN THERE SINCE P1.2.** Exact growth (`requiredLen`) meant the buffer was full the instant it was published, so the next append reallocated and re-copied every byte written so far: `var s = ""` plus a loop of appends was O(n^2) bytes and one allocation per append. MEASURED with `Testing/ladders/genstring.sh <n> data-appendloop`: x3.94 x4.07 x3.82 x3.88 per doubling, and **5.0 SECONDS to build a 288 KB string**. Now `2 * requiredLen` -- branch-free, because `requiredLen = selfLen + otherLen >= selfLen`, so doubling it is unconditionally big enough without a `max(...)` and without a compare. After: x1.82 x2.21 x1.77 x1.94, top rung **0.98 ms — 5,118x**. Coordinator-verified end to end: 294,912 bytes built in 15 ms of total process time. Safe because `capacity@16` has exactly three readers and none treats it as the allocation's size, and Strings expose no `capacity()` to user code. The comment above that line had called geometric growth "a later optimization with no consumer here" -- the consumer was the commonest string idiom in the language. ///// ⛔ **THE CORPUS IS BLIND TO ALL SEVEN METHODS, AND THE PROOF IS AN INVENTORY, NOT AN ASSUMPTION.** `--emit-corpus` dumped, every method call across all six rungs enumerated: `create push count get append scaleBy probe byteLength slice reserve clone firstVal`. Not one of the seven. ⚠ **But it is NOT blind to `String`** -- 756 `==` sites plus `append`/`byteLength`/interpolation -- which is exactly why this slice's shared `__str_eq` change WAS visible while the seven methods themselves read Δ0. That is E3070's "narrow, not blind" trap running in REVERSE: there, a neighbour of the shape read non-zero and looked like coverage; here, the same neighbour reading non-zero could have been mistaken for coverage of the methods. **Check what the corpus CONTAINS before believing what a Δ0 means, in either direction.** `maxon-shv2/Testing/ladders/genstring.sh` is committed (knobs: call sites, segments, matches, haystack, needle-scaling). ///// COST, PATH-CLEAN A/B (both binaries swapped into the SAME slot, per the row below): **Slice C itself is +11 allocations / +295 bytes, FLAT at all six rungs** -- +15/+664 for the 15 new module-level `b"..."` globals (7 method names, 8 runtime symbols), +7/+167 `phase:isel`, -11/-472 `phase:regalloc` from the `emitByteRangeCompareLoop` critical-edge deletion. The `__str_append` growth fix costs a further flat +103 allocations / +3,569 bytes per compile. The seven methods measured LINEAR on every shape the new ladder can express: `sites-*` x1.90-x1.99 out to 2,048 call sites; `data-split` x2.04 over 16x segments (the search advances and `__arr_push` is amortized); `data-replace` x1.96 over 16x matches (the two-pass count really does size ONE allocation); `toUpper` x2.00. A String-free control A/B'd +0 allocations. ///// ⚠⚠ **THE RECORDED -181 / -8,132 THIS ROW REPLACES WAS THE PATH-LENGTH ARTIFACT, AND THE COORDINATOR READ IT WRONG IN A NEW WAY.** I argued the delta could not be the artifact because it was FLAT while the artifact "grows with rung count". It does not -- the artifact is per-compile path strings, so it is flat too. Measured: parent `d9d0f980a` from a 26-char worktree reads **-192 at every rung to the digit** against its own committed row; -192 + 11 = -181, and -8,427 + 295 = -8,132. The arithmetic closes exactly. ⇒ **"It is flat, therefore it is not the artifact" IS NOT A VALID ARGUMENT.** The only valid one remains the row below's: run both binaries from the SAME directory, swapped in place. ///// MEASURED DEBT, FILED NOT FIXED: `__str_find` is a naive search carrying a genuine O(haystack x needle) term -- `data-findquad` reads x3.92 x4.15 x3.95 x4.14. It needs BOTH a needle whose length scales with the haystack AND data self-similar enough to defeat the first-byte reject; a fixed needle is a constant factor and `data-find` reads x2.04. Measured LINEAR on every realistic shape including `split` and `replace`, its two biggest consumers. **TRIGGER TO RE-MEASURE: a consumer that searches with a needle whose length grows with its input.** The fix is two-way/KMP. ⚠ `stdlib/String.maxon`'s `findIn` -- which BOTH reference compilers run -- has the same shape, so this is not a shv2 divergence. ///// A SABOTAGE THAT DID NOT FIRE, reported rather than left implied: disabling the `Array with String` pre-intern for `.split` still passes 2035/0, because parse-time interning is idempotent and lands on the same giid. The pre-intern is DEFENSIVE, not load-bearing. Kept for consistency with `hasStringArrayLiteral`/`hasSetFromLiteral`; flagged so it is not mistaken for a checked claim. | 574,994 | 1,035,676 | 1,955,831 | 3,793,273 | 7,466,095 | 14,808,163 |
| 2026-07-28 | A FUNCTION VALUE'S DECLARED TYPE IS AUTHORITATIVE. `FunctionTypeAlias` stored only its RETURN type and threw the declared PARAMETER types away, and three silent wrong answers lived in that one gap: (1) an int literal at a `float` parameter was NOT widened at an INDIRECT call, through all three callee kinds -- an alias-typed parameter, a bare functionRef and a lifted closure (coordinator-reproduced exit 0 where 4/6/6 is right); (2) a mismatched function-to-function CAST was accepted untouched (`intish as FloatFn` read an integer back out of the float return register), as was the same disagreement through a function-typed PARAMETER and a `return` -- only the `function` TAG was ever compared, so two different function types read as one; (3) an indirect call's ARITY and argument TYPES were unchecked (`f(3)` on a two-parameter type read `b` out of whatever the register held; a String at an int parameter crashed). ///// COST: +7 ALLOCATIONS FLAT at every rung and `phase:parse` allocation-IDENTICAL to the parent, A/B'd against a parent build made and run from THIS SAME worktree path. Bytes +1,352/+1,944/+3,128, fully attributed and entirely "records that already existed got bigger", nothing new allocated per program unit: `phase:load` +264 FLAT (two new `Project` containers), `phase:lowerMaxonToStd` +24 FLAT (three registry fields on the one lowering object), and `phase:signatures`+`phase:parse` = 8 B per FileParseArtifact for its new door array, at IDENTICAL allocation counts -- an artifact is built per file AND per top-level constant, which is exactly the +768/+1,320/+2,424 doubling seen. ///// ⚠⚠ THE FIRST SHAPE OF THIS COST 240x MORE AND THE OBVIOUS ATTRIBUTION WAS WRONG TWICE. Measured +4,497/+8,666/+17,007/+33,676 allocations, x1.93..x1.99 -- linear, but 0.9% of the whole compile. Half was `SemanticCheck.checkArgTypes` rendering a parameter LABEL (`String.from` + interpolation) for EVERY argument of EVERY call before deciding whether to report anything -- now built only inside the reporting arms (`argParamLabel`), taking `phase:semanticCheck` to delta 0. The other half was a `String` place NOUN ("variable"/"global"/"field") threaded from `Parser.assignableValue` into the door recorder: ONE ALLOCATION PER ASSIGNMENT, per struct-literal field and per global store in the program, +1,852 at rung 0. ⚠ It read as the SAME +1,852 TO THE DIGIT whether the door's guard sat inside the recorder, at the caller, or with the declared type passed as a `MaxonType` instead of a `(tag, nameId)` pair -- three plausible causes, all refuted, and only deleting the whole call site moved it. What fixed it was making the noun a SCALAR (`FunctionTypeDoor` gained `variable`/`global`/`field` arms, rendered to words only where a diagnostic is built). LESSON: on a parser path that runs per statement, what a guarded call COSTS is its ARGUMENTS, not its branch -- and the only way to find which argument is to delete the site and add the pieces back. ///// The widening and the new checks are UNREACHED by the scale corpus -- `--emit-corpus` confirms ZERO function typealiases at every rung and `phase:merge` reads delta 0, i.e. not one door was ever recorded -- so the deltas above measure the STRUCTURAL cost only and say nothing about the checks themselves. ///// GATES: build 0 warnings, suite 2023 -> 2036/0 exit 0, wasm 1925/0 exit 0, fragments ADDITIONS ONLY (13 x64-windows + 13 x64-linux, no M), verify-warm-rebuild PASS. | 574,695 | 1,035,377 | 1,955,532 | 3,792,974 | 7,465,796 | 14,807,864 |
| 2026-07-28 | A FUNCTION VALUE'S DECLARED TYPE IS AUTHORITATIVE. `FunctionTypeAlias` stored only its RETURN type and threw the declared PARAMETER types away, and three silent wrong answers lived in that one gap: (1) an int literal at a `float` parameter was NOT widened at an INDIRECT call, through all three callee kinds -- an alias-typed parameter, a bare functionRef and a lifted closure (coordinator-reproduced exit 0 where 4/6/6 is right); (2) a mismatched function-to-function CAST was accepted untouched (`intish as FloatFn` read an integer back out of the float return register), as was the same disagreement through a function-typed PARAMETER and a `return` -- only the `function` TAG was ever compared, so two different function types read as one; (3) an indirect call's ARITY and argument TYPES were unchecked (`f(3)` on a two-parameter type read `b` out of whatever the register held; a String at an int parameter crashed). ///// COST: +7 ALLOCATIONS FLAT at every rung and `phase:parse` allocation-IDENTICAL to the parent, A/B'd against a parent build made and run from THIS SAME worktree path. Bytes +1,352/+1,944/+3,128, fully attributed and entirely "records that already existed got bigger", nothing new allocated per program unit: `phase:load` +264 FLAT (two new `Project` containers), `phase:lowerMaxonToStd` +24 FLAT (three registry fields on the one lowering object), and `phase:signatures`+`phase:parse` = 8 B per FileParseArtifact for its new door array, at IDENTICAL allocation counts -- an artifact is built per file AND per top-level constant, which is exactly the +768/+1,320/+2,424 doubling seen. ///// ⚠⚠ THE FIRST SHAPE OF THIS COST 240x MORE AND THE OBVIOUS ATTRIBUTION WAS WRONG TWICE. Measured +4,497/+8,666/+17,007/+33,676 allocations, x1.93..x1.99 -- linear, but 0.9% of the whole compile. Half was `SemanticCheck.checkArgTypes` rendering a parameter LABEL (`String.from` + interpolation) for EVERY argument of EVERY call before deciding whether to report anything -- now built only inside the reporting arms (`argParamLabel`), taking `phase:semanticCheck` to delta 0. The other half was a `String` place NOUN ("variable"/"global"/"field") threaded from `Parser.assignableValue` into the door recorder: ONE ALLOCATION PER ASSIGNMENT, per struct-literal field and per global store in the program, +1,852 at rung 0. ⚠ It read as the SAME +1,852 TO THE DIGIT whether the door's guard sat inside the recorder, at the caller, or with the declared type passed as a `MaxonType` instead of a `(tag, nameId)` pair -- three plausible causes, all refuted, and only deleting the whole call site moved it. What fixed it was making the noun a SCALAR (`FunctionTypeDoor` gained `variable`/`global`/`field` arms, rendered to words only where a diagnostic is built). LESSON (⚠ CORRECTED BY THE OPTIMIZATION-PASS ROW BELOW, which rebuilt all three variants on top of the shipped head and measured them): it is NOT "its arguments". An argument of the guarded call that the callee merely READS is free -- measured, delta ZERO. What a guarded call costs is OWNERSHIP TRANSFER: a MANAGED argument the callee CONSUMES forces the caller to hand over an owned copy, and that copy is emitted UNCONDITIONALLY ahead of the guard, which is why the three relocations of the guard all read the same +1,852. ///// The widening and the new checks are UNREACHED by the scale corpus -- `--emit-corpus` confirms ZERO function typealiases at every rung and `phase:merge` reads delta 0, i.e. not one door was ever recorded -- so the deltas above measure the STRUCTURAL cost only and say nothing about the checks themselves. ///// GATES: build 0 warnings, suite 2023 -> 2036/0 exit 0, wasm 1925/0 exit 0, fragments ADDITIONS ONLY (13 x64-windows + 13 x64-linux, no M), verify-warm-rebuild PASS. | 574,695 | 1,035,377 | 1,955,532 | 3,792,974 | 7,465,796 | 14,807,864 |
| 2026-07-28 | A FUNCTION VALUE'S DECLARED TYPE IS AUTHORITATIVE -- OPTIMIZATION PASS. NO CODE CHANGE, and the measurements are why. ///// THE +7 FLAT IS CONFIRMED INDEPENDENTLY, not taken. `main` @9fa71cc79 was rebuilt IN THIS SAME WORKTREE (detached HEAD, so the checkout path is identical to the head build's CHARACTER FOR CHARACTER and the phase:load path-length artifact is structurally zero rather than subtracted) and its scale-test reads 574,688 / 1,035,370 / 1,955,525 / 3,792,967 / 7,465,789 / 14,807,857 against the head run's 574,695 / 1,035,377 / 1,955,532 / 3,792,974 / 7,465,796 / 14,807,864 -- +7 AT EVERY RUNG, and the head run reproduces the logged row with delta 0. ⭐ AND THE IMPLEMENTER'S EXTRAPOLATION WAS RIGHT FOR THE RIGHT REASON, which needed the control to establish: the +125..+2,518 against the LOGGED row is +7 (this rung) plus the parent's own +118/+199/+358/+669/+1,286/+2,511 advance since the 2b-vi baseline, and that second term is now MEASURED as the control's own allocsDelta rather than fitted from four points. A doubling offset across a doubling ladder is the shape a real per-program cost has too; only an equal-path control tells them apart. ///// ⚠⚠ THE DELTA IS STRUCTURAL ONLY AND THE LADDER IS BLIND, RE-CONFIRMED FROM THE CORPUS: `--emit-corpus` over all 465 generated files finds ZERO `typealias = function(...)` and ZERO function references at every rung, so `resolveFunctionAliasShapes`, `declaredFunctionShapeOf`, `checkIndirectCall`, `checkFunctionTypeDoors` and `indirectCalleeParamTypes` run EXACTLY ZERO TIMES in the whole ladder. ///// NEW INSTRUMENT: `maxon-shv2/Testing/ladders/genfnval.sh <aliases> <sites> <arity> <indirect / direct>` -- the axis the corpus cannot express, with `direct` as the same-shape control (same alias declarations, same callee bodies, same CALL COUNT, by NAME, so no function value, no indirect call and no door exists in it). ⚠ Its two modes are NOT byte-identical and cannot be: a direct call's 2nd+ arguments MUST be labelled (E2053) and an indirect call's CANNOT be, so read it as an upper bound on the machinery and read RATIOS within a mode. ///// ⭐ RESULT: NOTHING ON THIS RUNG'S PATHS IS SUPERLINEAR, and it is measured rather than argued. Doubling aliases (and with them call sites and doors) 64 -> 512, HEAD MINUS PARENT on the identical source, allocations: total +2,006 / +3,934 / +7,784 / +15,474 = x1.96 x1.98 x1.99. Per phase, all x1.96..x2.00: resolveTypes +609/+1,193/+2,353/+4,665 (9.1 allocations per function typealias, and IDENTICAL in `direct` mode -- it is the DECLARATION cost, which is exactly what the control was built to prove); semanticCheck +768/+1,536/+3,072/+6,144 (x2.00 to the digit, 12 per alias over 3 indirect calls + 2 doors); lowerMaxonToStd +192/+384/+768/+1,536 (x2.00, ONE union box per indirect call in `indirectCalleeParamTypes`); parse +330/+651/+1,292/+2,573. Subtracting the control leaves the CALL+DOOR machinery alone at +1,035/+2,061/+4,111/+8,209 = x1.99 x1.99 x2.00. CPU ticks (min of 3) read x1.93..x2.06 on every phase of both modes. `declaredFunctionShapeOf` is TWO HASH-MAP GETS, not a scan, so the `sites x aliases` quadratic the hunt was for would have read x4.00 on this ladder and reads x2.00. ///// ⚠⚠ THE RECORDED LESSON OF THE ROW ABOVE IS CORRECTED HERE -- it was directionally right and MIS-STATED, and the mis-statement is the actionable part. Reconstructed the blamed shape on top of the shipped head and measured three variants at rung 0: (V1) the `String` noun restored as an argument of `assignableValue` and consumed by `requireAssignable` -- i.e. the PARENT'S OWN SHAPE, which is the point, since the parent already paid for that literal at every one of those call sites -- reads 574,695, delta ZERO; (V3) the noun ALSO passed as an argument of the GUARDED recorder call but only READ there -- 574,695, delta ZERO; (V2) the same argument, CONSUMED (stored on the `PendingFunctionTypeDoor` record) -- 576,547, +1,852 in phase:parse, THE LOGGED NUMBER TO THE DIGIT. So it is not "its arguments": V3 puts the argument on the guarded call for free. ⭐ THE RULE IS OWNERSHIP TRANSFER: a MANAGED argument that the guarded callee CONSUMES forces the caller to hand over an owned copy, and that copy is emitted UNCONDITIONALLY, ahead of the guard -- so a branch taken at essentially no call site still pays once per call for every managed argument it would have consumed. That also explains the three "refuted" causes without any further hypothesis: moving the guard inside the recorder, moving it to the caller, and changing the declared type's representation all leave the CONSUMED String argument exactly where it was, which is why all three read +1,852 to the digit. The corpus records ZERO doors, so not one of those 1,852 copies was ever used. ///// GATES: build 0 warnings; suite 2036/0 exit 0; wasm 1925/0 exit 0; fragments byte-identical after both suites; verify-warm-rebuild PASS (run on a genfnval program, so the doors list is exercised across a warm re-merge -- it is reset in `resetMergeTargets`, so the E3070-shaped "pending list that never shrank" is NOT present here). ///// ⛔ ONE PRE-EXISTING DEFECT FOUND AND REPORTED, NOT WORKED AROUND: an indirect call of arity >= 14 panics the register allocator (`RegisterAllocator.maxon:1173 chooseRegister: no free register`). REPRODUCES IDENTICALLY ON `main` @9fa71cc79 -- arity 13 compiles, 14 panics, on both binaries -- so it is not this rung's; the same arity 16 through a DIRECT call compiles clean. It caps genfnval's arity knob at 13. | 574,695 | 1,035,377 | 1,955,532 | 3,792,974 | 7,465,796 | 14,807,864 |
| 2026-07-28 | A FUNCTION VALUE'S DECLARED TYPE IS AUTHORITATIVE -- INDEPENDENT REVIEW. ONE BLOCKER FIXED, plus the duplication it exposed. ///// THE BLOCKER WAS A FALSE REJECT, and the shape the rung's own comment said it had no case for: `resolvedTypesMatch` compared a NESTED function type (a function type's function-typed PARAMETER) by NAME id while comparing everything else RESOLVED, so `typealias Outer = function(f InnerA) returns Integer` REFUSED a function declared `(f InnerB)` even when InnerA and InnerB resolve identically. The bootstrap ORACLE compiles that program and runs it (exit 42), the parent shv2 @9fa71cc79 compiles and runs it (42), and head refused it -- with a diagnostic that read `expected 'fn(function) returns int', got 'fn(function) returns int'`, i.e. a type failing to match itself, because `maxonTypeName` rendered any function type as the bare word `function`. FIXED both halves: the nested type now DESCENDS (`nestedFunctionTypesAgree`), which is the same rule one level down and is required for soundness in the other direction too -- if the nested types genuinely differ, the callee calls its own parameter through the OTHER signature and the wrong answer lands one level in; and `maxonTypeName` now renders a `function` tag by its alias name, so the mismatch reads `expected 'fn(InnerA) returns int', got 'fn(InnerB) returns int'`. ///// THE DESCENT NEEDS A CYCLE GUARD AND HAS ONE, because shv2 ACCEPTS a self-referential function alias (`typealias SelfFn = function(f SelfFn)` compiles on head AND on the parent; the bootstrap refuses it as E2012) and two aliases may name each other. Guard is coinductive on the (declared, got) NAME PAIR: a pair already under comparison AGREES, which is the only answer a recursive type has and cannot false-reject. Verified terminating and correct on four hand-built cycles -- self-referential, mutually recursive A/B accepted, structurally-equal recursive A vs B accepted, recursive A vs B with a differing return REFUSED. ///// COST: +1 ALLOCATION FLAT AND +16 BYTES FLAT at every one of the six rungs, against the row above. It is ONE object for the whole compile -- `sharedEmptyFunctionTypePairs`, the empty pending list every top-level comparison starts from, never mutated (`pendingWithPair` builds a fresh array) -- the same pay-for-use discipline `sharedEmptyBorrowFacts` carries. NOTHING is per-program-unit: the `declaredId == gotId` short-circuit means an ordinary nested parameter (same alias both sides) allocates nothing and touches no registry, and a descent only happens when the two names actually differ. Corpus is still blind to this whole axis (zero function typealiases in all 465 generated files), so the flat +1 is the structural cost and nothing else. ///// DUPLICATION REMOVED, which is what made the fix cheap rather than expensive: `declaredFunctionShapeOf` took its three registries as three loose parameters and the id->name interner was a FOURTH fact carried separately, so SemanticCheck and LowerMaxonToStd each held a private copy of `typeNames.nameOf(id) or panic` and a private copy of `declaredFunctionShapeOf(...) or panic`. That is 3 copies of each once the descent needed both. Collapsed to ONE `FunctionTypeRegistry` carrier plus ONE `functionTypeNameOf` and ONE `requireFunctionShapeOf`, both exported from TypeResolution; the lowering now holds one field where it held four. Also deleted `hasDeclaredFunctionShape`, which had NO callers. ///// SPECS: 2 authored cases pinning the rule in both directions (`nested-function-type-agrees-by-shape` accepted, `error.nested-function-type-mismatch` refused with the readable message). ///// ⛔ SECOND PRE-EXISTING DEFECT FOUND AND REPORTED, NOT WORKED AROUND: on wasm, passing a function value into a parameter whose function TYPE has a function-typed parameter emits a module that fails validation (`type mismatch: expected i64 but nothing on stack`). Narrowed to the nested function TYPE alone -- no indirect call anywhere, the SAME alias on both sides -- and it reproduces byte-for-byte on `main` @9fa71cc79, so it is neither this rung's nor newly reachable. It is why the accepted case above lists x64 targets only. ///// GATES: build 0 warnings; suite 2036 -> 2038/0 exit 0; wasm 1925 -> 1926/0 exit 0; fragments additions-only (2 new x64-windows, no M); verify-warm-rebuild PASS on a genfnval program AND on the nested-alias case. | 574,696 | 1,035,378 | 1,955,533 | 3,792,975 | 7,465,797 | 14,807,865 |
| 2026-07-28 | P1.8 SLICE B — `Character`, UAX#29 GRAPHEME SEGMENTATION, `String.count()`, `for c in s`. A real `ValueTypeTag.character` (user ruling), a new `GraphemeRuntime` family, and the per-trip owned-Character frame. Suite 2056 -> 2114/0 on its own base, **2129/0** after rebasing onto upstream. x64-windows / x64-linux / wasm32-wasi all PASS; arm64 SKIP. ///// COST, PATH-CLEAN A/B (both binaries swapped into the SAME slot, interleaved): **+0 ALLOCATIONS and +64 BYTES, FLAT at every rung** — eight new `bool` fields on ONE `RuntimeUsage` record per compile. Confirmed two ways: the no-Character control read +0 allocs / +64 bytes / no measurable CPU (4.419 s vs 4.426 s, head faster in 2 of 4 interleaved trials), and the arithmetic closes against the committed row — slice C's artifact is -8,427, this tree reads -8,363, and **-8,427 + 64 = -8,363 exactly**. Compile growth x1.85-x1.99 in allocations AND CPU out to 1,024 call sites across all five ladder modes; run growth x2 per doubling on every mode. **Nothing superlinear.** ///// ⛔ **THE CORPUS IS BLIND TO EVERY PART OF THIS RUNG, BY INVENTORY.** `--emit-corpus` at rung 5: `.count()` appears **1,024 times and every one is an Array/Set receiver** — not one String; `.bytes()` **0**; **`for … in` in any form: 0**; `'…'` tokens **53,766, of which 0 are in a value position** (every one is a block label). ⚠ **But it is NOT blind to `String`** (756 `==` sites), which is the trap in reverse: a neighbour of the shape reads non-zero and can be mistaken for coverage. **Check what the corpus CONTAINS before believing what a Δ0 means, in either direction.** `maxon-shv2/Testing/ladders/genstring-grapheme.sh` is committed (knobs: call sites, segments, cluster width, ASCII/2-byte/ZWJ/flag seeds, countloop). ⚠⚠ **AND NOTE `for … in` IS STILL ZERO — P1.8 Slice A committed `genfor.sh` for exactly that hole and the MAIN corpus was never taught the construct.** Four consecutive rungs have now each built a private ladder because the shared instrument cannot express what they added; that is an instrument problem, not four coincidences. ///// ⭐⭐ **THE FINDING IS NOT A CURVE, IT IS THE ALLOCATOR: `for c in s` MINTS ONE Character PER TRIP AND THE SLAB NEVER GIVES IT BACK.** `MmRuntime` is a bump allocator with no free list, so memory is linear in **ITERATIONS, not live data**: 65,536 clusters -> 11.6 MB, 262,144 -> 97.1 MB, **1,048,576 -> 398.7 MB**, at ~82 bytes per minted Character. **Two controls make it unambiguous**: `data-count-ascii` runs the SAME `__gr_end` scan minting nothing and is **FLAT at 3.7 MB across a 4x data range**, and the C# bootstrap runs the same source in the same time (155.7 vs 156.4 ms) at **7.3 MB**. It is NOT a leak — every allocation is freed and no run exits 101 — the slab simply never reuses a slot. **FILED, not fixed**: the cure is a recycling `__mm_free`, which `MmRuntime.maxon:738` already anticipates in its own comment, and that is a runtime rung. **TRIGGER TO RE-MEASURE: any construct that allocates once per loop trip — `for c in s` is the FIRST one the language has, which is precisely why this debt only became visible now.** ///// Also measured: `for c in s` costs 17x what `count()` costs per cluster and ALL of it is `__char_at` (one alloc+blit+free per trip, ~54 ns, CONSTANT in the segmentation work — 54.1 ASCII / 56.2 two-byte / 83.6 for a 25-byte cluster), so the GB state machine hides no per-trip cost. shv2's `count()` is **2.07x FASTER than the bootstrap's** on the identical O(n) algorithm (16.8 ms vs 34.8 ms over 1,048,576 clusters). `data-countloop` is x3.79-x4.05 — quadratic BY DESIGN, in user code, exactly as `countGraphemes` is O(n) per call in both compilers. ///// ⚠⚠ **READ THE PRINTED DELTA ON THIS ROW AS A SPAN, NOT AS THIS RUNG'S COST.** The instrument compares against the row BELOW, which was written by the upstream function-value rung from a different tree; between the two rows THREE things landed — that rung, the BOOTSTRAP ORACLE fixes, and this slice. So the printed +5,836 / +9,043 / +15,459 / +28,289 / +53,951 / +105,273 is the sum of all three plus a path-length component, **not** Slice B. Slice B's own cost is the path-clean interleaved A/B above: **+0 allocations, +64 bytes, flat**. ⚠ The span IS linear — successive ratios x1.55 x1.71 x1.83 x1.91 x1.95, converging on x2.00, ~+0.7% of allocations — so there is no superlinearity in it; but its ATTRIBUTION between the three landings is **OWED, not claimed**. The likeliest single contributor is that the oracle rung changed `maxon-sharp` CODEGEN, and shv2's binary is PRODUCED BY the bootstrap, so a bootstrap change moves shv2's own allocation behaviour — but that is a hypothesis, and this project's rule is that a story which fits is not an attribution. **Whoever next needs this number should measure it path-clean against `cdcab0969`.** ///// REJECTED WITH MEASUREMENTS: eliding the `__char_at` mint for `for _ in s` (sound, worth ~14x, but it fixes only the discard and would make `character-ownership.md`'s `discardTrips` case — written to pin that a discarded cluster is still dropped — vacuous); and an immortal 256-entry `.rdata` Character table for one-byte ASCII clusters (would remove ~94% of ASCII iteration cost, but `__str_decref` would have to learn to tolerate an rdata record, which is a Character-REPRESENTATION decision, not an optimizer's). | 580,532 | 1,044,421 | 1,970,992 | 3,821,264 | 7,519,748 | 14,913,138 |
| 2026-07-28 | PARSE SUPERLINEARITY FOUND AND FIXED — `Parser.assignedBindingsIn` was Θ(vars in scope × merging constructs), and it ALLOCATED NOTHING. `phase:parse` was the only superlinear phase on the ladder: CPU x2.02 x2.21 x2.52 x2.90 x3.33 down the six rungs while its allocations held at x1.85-x1.99, and its share of compile CPU climbed 15.3% -> 36.7% while regalloc's fell — on that trajectory it overtakes the register allocator at the next rung. ///// THE BEND, NAMED: every `if` `match` `while` `for` asked for the mutable bindings its token extent assigns, and the answer was computed by walking the WHOLE `mutableVars` list and probing each binding's name against a per-construct `Set`. One hash of a `ByteArray` name per binding in scope per construct. Instrumented on the corpus: **39,400,068 probes at rung 5 across 18,571 constructs, x3.99 per doubling — and the carried set they were selecting was never larger than TWO (maxA = 2, sumA = 27,180).** The scale corpus doubles both axes at once (`LocalsPerFunctionBase` 128<<r and `LongIfsBase`/`DeepBlocksBase`), so the product quadrupled per rung while source bytes doubled. ///// THE LADDER THAT SEPARATED THE AXES: `maxon-shv2/Testing/ladders/genscope.sh <constructs> <locals> (if ifelse while match straight)` (COMMITTED, indexed in that directory's README). C alone x1.72 x1.86 x1.89; V alone x1.46 x1.57 x1.83; BOTH x2.21 x2.45 x2.83; and the `straight` CONTROL — same V, same statement count, no merging construct — x1.80 x1.90 x1.89. Linear in each knob, quadratic in the two together, flat without the construct: that is an O(V x C) term and it belongs to the construct, not to program size. Confirmed before the fix was designed by capping the walk at 8 entries, which took the bend to x1.88 x1.94 x1.94 with allocations UNCHANGED to the digit. ///// THE FIX: invert the lookup. The parser now carries an index of `mutableVars` — `mutableVarInnermost` (name -> innermost position) plus `mutableVarShadowed` (per position, the position it shadows), pushed/popped in exact lockstep with the stack under the same `openBlock` mark. `assignedBindingsIn` now scans the extent's tokens, asks the index per ASSIGNED NAME, and collects positions into an ASCENDING list — ascending position IS `mutableVars` declaration order, which is the order the phis must be minted in, so no sort and no reordering. `namesAssignedIn` and its per-construct `Set` are gone; `mintLoopHeaderPhis` takes the same carried list, so the two orderings that had to agree are now ONE. The shadow chain is what keeps this EXACT: it yields every entry a name hides, which is precisely what the old walk found. ///// RESULT, path-clean A/B (parent `3f2e3203f` and the fix built in the SAME worktree at the SAME path, min-of-3, machine idle): `phase:parse` CPU x1.89 x1.92 x2.00 x2.06 x1.98 — LINEAR. Rung 5 parse CPU **13,160,151,600 -> 3,354,949,049 (-74.5%, 3.92x)**; whole-compile CPU **35.87G -> 24.78G ticks (-30.9%)**; parse's share of compile CPU now reads 14.5% -> 13.5% ACROSS the ladder instead of climbing to 36.7%. Same-binary CONTROL (baseline run twice): memory bit-identical at every rung, CPU spread -5.5%..+0.5% — so the noise band is ~6% and a 3.9x is not in it. ///// MEMORY, and it is fully attributed — EXACTLY TWO PHASES MOVED. `phase:parse` -4,060 .. -143,487 allocations and -277,848 .. -9,493,072 bytes: that is the per-construct `AssignedNameSet` (and its internal columns) no longer being built, ~7.7 allocations per construct. `phase:signatures` **+0 allocations** and +1,536 .. +35,760 bytes: the `Parser` RECORD grew 16 bytes (two new pointer fields), and 1,536/16 = 96 = the 30 files plus 66 top-level constants that phase builds a `Parser` for at rung 0. The two structures themselves cost nothing there because they start at shared empties and are copy-on-written by the first `var` push (`sharedEmptyIterationLocks`'s pattern) — an unconditional `create()` pair measured +13,410 allocations / +590,040 bytes in `phase:signatures` at rung 5, a phase this change has no business touching, and that is why the COW is there. Totals close exactly: -9,493,072 + 35,760 = -9,457,312. ///// ⚠ WHAT IS STILL THERE: the token scan itself still walks token-by-token, so a construct nested D deep still has its tokens re-read by all D enclosing scans — the `LoopNestBase` knob's cost, isolated by `gennest.sh`, UNTOUCHED by this change. The old note in `blockStatementEndIndex` that inverting that scan costs `phase:parse` x3.5 was measured when its per-test constant was multiplied by the Θ(V) walk this row removes; that measurement no longer stands either way and the question is open again. ///// ⚠ AND ONE MEASURED BOUND ON THE NEW CODE: the ordered insert is O(A^2) in shifts where the old walk was O(V) in hash probes, so a construct assigning EVERY binding in scope is its worst case — a shape `genscope.sh` deliberately does not generate. Measured by hand at A = V = 250/500/1000/2000 (one wide `if`) and at 8 wide `if`s with V = 125..1000: both stay LINEAR (x1.95-x2.03) and cost +1%..+4%, because a 4-byte shift is ~30x cheaper than the `ByteArray` hash probe it replaced. RE-MEASURE TRIGGER: a rung that makes wide carried sets ordinary (a macro expander, machine-generated bodies). ///// ⚠ READ THE PRINTED DELTA AS A SPAN, NOT AS THIS CHANGE'S COST. The instrument compares against the row below, which was recorded in a DIFFERENT tree, so it prints -4,335 .. -146,056 allocations where this change's own, path-clean A/B is -4,060 .. -143,487. The ~275 .. ~2,569 difference is that tree, not this change, and the attribution above is the A/B rather than the printed delta. ///// GATES: shv2 x64 **2142/0**, wasm32-wasi **2029/0**, `verify-warm-rebuild` PASS, no 101, and `specs-shv2/fragments/` EMPTY after both full runs — byte-identical codegen, which is the correctness proof this change needed most, since the carried set's ORDER decides phi order. | 576,197 | 1,035,546 | 1,952,996 | 3,784,995 | 7,446,894 | 14,767,082 |
| 2026-07-28 | P1.8 SLICE B2 — `StringIndex` + `startIndex`/`endIndex`/`findFirst`/`findLast`/`slice`, closing Slice B's sanctioned residual. Suite 2142 -> 2178/0; x64-linux PASS, wasm32-wasi PASS, arm64 SKIP (remote, opt-in). Build 0 warnings; specs-shv2/fragments EMPTY after a full unfiltered run, which is what proves the codegen byte-identical. ///// NO OPTIMIZER TRIGGER FIRED, and each of the three conditions was checked rather than waved at: the rung adds no pass and no IR op (it adds Std-graph runtime entry points and one synthesized struct layout); the ALLOCATION ladder is x1.80 x1.89 x1.94 x1.97 x1.98, converging on x2.00 with nothing anywhere near the ~x2.4 threshold; and the one Workstream-O re-measure trigger it touches is discussed below. ///// READ THIS ROW'S DELTA COLUMN AS A PATH ARTIFACT, NOT AS A SAVING. It prints -275 -349 -497 -793 -1,385 -2,569 allocations and -8,693 -9,877 -12,245 -16,981 -26,453 -45,397 bytes — i.e. NEGATIVE at every rung, growing with rung. A rung that only ADDS a feature does not make the compiler allocate less; this is the checkout-path term this log has now recorded three times (see the 2026-07-27 P1.7-Slice-1 row, which proved it by running the PARENT's binary from the worktree path and reproducing the child's numbers to the digit). This tree's root `maxon-p18b2` differs in length from the main checkout's, and the previous row was written from a different tree again. ⚠ **THIS RUNG'S OWN COST IS THEREFORE NOT ESTABLISHED BY THIS ROW, AND IS OWED, NOT CLAIMED** — a path-clean interleaved A/B against `3f2e3203f`, both binaries swapped into ONE canonical path, is what would settle it. Recording that it is unmeasured rather than reading the printed delta as a result. ///// THE CORPUS IS BLIND TO EVERY CONSTRUCT THIS RUNG ADDS, BY INVENTORY, so a Δ0 from it would measure the instrument and not the cost. Slice B's row established the inventory at rung 5 with `--emit-corpus`: `.count()` appears 1,024 times and EVERY receiver is an Array or Set — not one String — and `.bytes()` is 0. B2 adds only String-receiver methods (`startIndex`/`endIndex`/`findFirst`/`findLast`/`slice`), so the corpus cannot reach a single line of the new parser dispatch or any of the new runtime graphs. That is the fifth consecutive rung whose feature the shared corpus cannot express; four of them have now each committed a private ladder instead. It is an instrument problem, not five coincidences. ///// phase:parse CPU reads x2.04 x2.21 x2.36 x2.67 x3.08 while its ALLOCATIONS stay dead linear at x1.85 x1.92 x1.96 x1.98 x1.99. That divergence is exactly the allocate-nothing shape the CPU column was added to catch — and it is NOT this rung's and NOT new: it is the already-filed Theta(mutable bindings x constructs) debt (`d_blocks.maxon` is ONE function with 518 mutable vars in scope, and both callers ask a membership question once per binding per construct — 26.5 MILLION tests at rung 5). The 2026-07-26 encoder row filed it, and the 2026-07-27 P1.7 row re-measured it at x3.26 on BASE AND HEAD ALIKE. This row's x3.08 sits inside that, on a corpus that cannot reach B2's code at all. ⚠ SUPERSEDED WITHIN HOURS, AND THE ROW ABOVE IS THE CURE: upstream `88ca97b33` identified `Parser.assignedBindingsIn` as the Θ(vars in scope × constructs) term and FIXED it while this rung sat in its worktree. So this row is a measurement of the PRE-FIX compiler — correct when taken, stale by the time it merged. Kept rather than silently re-measured, because a trend table is read downwards and this is the last datapoint before the cure. ///// ⭐ A SECOND CONSTRUCT NOW HITS SLICE B's NAMED SLAB TRIGGER, and this is the one genuinely new observation. Slice B filed the `MmRuntime` bump-allocator debt (no free list ⇒ memory linear in ITERATIONS, not live data; 1,048,576 minted Characters -> 398.7 MB) and wrote its re-measure trigger as "any construct that allocates once per loop trip — `for c in s` is the FIRST one the language has". **`slice` in a loop is the SECOND**: every trip mints a fresh owned String, because slice COPIES rather than viewing. Probed at 200 trips: correct, exit 0, no 101 — it is not a leak, the slab simply never reuses the slot, exactly as filed. The debt and its cure (a recycling `__mm_free`, which `MmRuntime.maxon:738` already anticipates in its own comment) are UNCHANGED; what changes is that the trigger has fired a second time from an independent direction, which is evidence the runtime rung is worth sequencing rather than a new finding. ///// ⭐⭐ THE REVIEW FOUND A REACHABLE COMPILER CRASH THE GREEN SUITE WAS STRUCTURALLY BLIND TO, and the blindness is the transferable part: `registerStringIndexType`'s only caller sat inside `foldDeclaredSignaturesInto`, a DECLARATION walk whose arms hand a whole `type` to a `recordScanned*` routine and resume past its `end` — so tokens inside method bodies never reach it. A `StringIndex` producer inside a `type` method left the compiler-owned `StructLayout` unregistered and died two ways (field read -> `stringIndexLayoutOrPanic`; scope-exit drop -> `managedNameDropCallee`). All 2,177 tests spelled the feature in a free-standing function — THE ONLY SHAPE THAT LOOP CAN SEE — and two comments asserted the opposite, including the loop's own "walks EVERY token in the file, function bodies included". Cured by DERIVE (its own complete token walk) and pinned RED against the parent. Sabotage: end-exclusive->inclusive 9 red; byte->grapheme->byte position 2 red; not-found throw->sentinel 2 red; `StringIndex` box drop removed 19 red, ALL exit 101. | 580,257 | 1,044,072 | 1,970,495 | 3,820,471 | 7,518,363 | 14,910,569 |
| 2026-07-29 | P1.7a FINAL RESIDUAL — `float` conforms to `Hashable`, via a Std-tier BIT REINTERPRETATION (`StdUnaryOpcode.bitcastF64ToI64`; x64 `movq r64,xmm`, arm64 `FMOV Xd,Dn`, wasm `i64.reinterpret_f64`). Suite 2178 -> 2185/0, exit 0, no 101. x64-linux PASS, wasm32-wasi PASS, arm64-macos/arm64-linux SKIP (remote, opt-in, UNVERIFIED — not proven good). Build 0 warnings; specs-shv2/fragments 14 ADDITIONS and ZERO `M`, which is what proves the codegen byte-identical for all 2178 pre-existing programs. ///// ⚠⚠ READ THE PRINTED DELTA COLUMN AS `main`'s ADVANCE, NOT AS THIS RUNG — AND THIS TIME IT WAS REFUTED BY MEASUREMENT RATHER THAN FILED. The instrument prints -4,052 -8,518 -17,491 -35,468 -71,461 -143,479 allocations against the stored baseline: NEGATIVE and GROWING with rung, ~1% at rung 5. A rung that only ADDS a feature cannot make the compiler allocate proportionally less. The stored baseline is a PREVIOUS LOG ROW, not this rung's parent, and `main` advanced between them (0c6ca2923, 88ca97b33, 73884cbbf, 71cd8479b). Settled the way slice 2b-vi settled its own +148..+3062: the PARENT (71cd8479b) was built in a control worktree at an EXACTLY EQUAL-LENGTH path (`maxon-ctrl` vs `maxon-p17a`, both 28 chars) so the `phase:load` 202 B/char term CANCELS rather than being argued away. ///// ⭐ THIS RUNG'S OWN FOOTPRINT IS +8 ALLOCATIONS / +510 BYTES PER COMPILE, DEAD FLAT AT ALL SIX RUNGS (a 32x span), and attributed to NO phase — `installBuiltinConformanceRuntime` is called from `Compiler.maxon:452`, outside every `CompilePhase` bracket. Attributed BY PROBE, not by arithmetic: a temporary tenth never-referenced candidate added to the install roster measured +7 allocs / +312 bytes, equally flat, so the +8/+510 is one candidate (`float.hash`) plus the array growth its arrival crossed. Probe reverted. ///// THE OPTIMIZER AGENT TRIGGER FIRED (the rung adds an IR op) and the ladder shows no bend: allocations x1.797 x1.886 x1.938 x1.967 x1.983, converging on x2.00, nothing near the ~x2.4 threshold. NO COMPILER CODE CHANGED. ///// THE CORPUS IS BLIND TO THIS RUNG BY CONSTRUCTION, so its Delta0 would have measured the instrument: `ScaleCorpus` emits ZERO float `.hash()` calls, and the install gate is REFERENCED-but-undefined <=> INSTALLED, so `buildFloatHash` is never built and the new TargetOp never emitted anywhere on the ladder. That is the sixth-plus consecutive rung with this problem. Private ladder built: `maxon-shv2/Testing/ladders/genfloathash.sh` (4 modes), whose PROOF OF REACH is the strongest available kind — the PARENT compiler REFUSES its `hash` file outright (E2015: 'float' has no method named 'hash'). Per-call-site cost measured FLAT: hash 595.4 / 594.1 / 593.8 allocations per site against control 595.7 / 594.6 / 594.1 — a `f.hash()` site costs what a float comparison costs. ///// A NEW WIDEST `TargetOp` VARIANT WOULD HAVE TAXED EVERY `TargetOp` ALLOCATION (slice 2b-v's standing rule, 8 B per field): it did not — the widest variant is unchanged at 4 fields (`x64JumpTable`, `arm64Msub`) and both new ops carry 2. Verified by measurement, not by reading: the controls stay flat at +8/+510 on programs holding thousands of TargetOps, where a widened union would climb with program size. All new arms in TargetLiveness / SsaDestruction / SplitLiveRanges / RegisterAllocator are O(1). ///// ⭐⭐ MEASURED DEBT -> WORKSTREAM O, AND THE EXISTING TRIGGER WAS ON THE WRONG AXIS. Admitting `float` to `Hashable` costs +10 ALLOCATIONS / +344 BYTES PER FLOAT `.equals()`/`.compare()` CALL SITE — exact at all four sizes (10 x sites + 8), linear, controls flat. Nothing in `float.equals`'s own path changed; its POSITION IN A SEARCH ORDER did. `builtinConformerMethod` (IrInterface.maxon:691) walks Hashable -> Equatable -> Comparable and `continue`s past non-conformers; float's row now STOPS at Hashable, so a whole `IrInterface` is synthesized, searched for `equals`, missed, and discarded before Equatable is ever reached. FILED, NOT FIXED — linear in sites is a constant factor, and the review traced the cure to confirm it is not a ride-along: `builtinInterface` returns `InterfaceLookup.found(IrInterface)` BY VALUE and `IrInterface` owns three arrays, so memoizing it either clones per call (re-paying most of the 10) or returns a BORROW of a compiler-lifetime cache, which changes the return type and every consumer and introduces exactly the long-lived-borrow shape E3070's liveness pass now polices. The cheap alternative — asking "does this protocol declare this method?" without building the interface — is forbidden by the design's own thesis, since it re-introduces the second roster `builtinConformerMethod` exists to eliminate. ⚠ THE OLD RE-MEASURE TRIGGER READ "a fourth builtin-conformable protocol". IT DID NOT FIRE — `builtinConformableProtocolNames` still returns exactly three — YET THE DEBT GOT WORSE ANYWAY, because the cost is (protocols x conformance-ROW WIDTH) and this rung widened a ROW. REPLACEMENT TRIGGER: any rung that adds a (type, protocol) PAIR to `isIntrinsicBuiltinConformance`, or a fourth protocol. A re-measure trigger that cannot fire for the thing that actually worsens the cost is this project's signature bug wearing an instrument's clothes. ///// ⭐ THE PLAN'S OWN PREMISES WERE MEASURED FALSE BEFORE THE WAVE, TWICE. PLAN.md listed TWO residuals for P1.7a; `2b-iv-C` (arm64 witness lowering) had already landed six commits earlier in 227fb1051 (coordinator-confirmed by disassembling a cross-compiled witness binary: `ldr x16, [x1, #0x18]` + `blr x16`, slot 24 = first method). And `Box with float` under a `where` constraint is a clean E2062 refusal, not the compile-time panic the row recorded — which is why `float.hash` is DIRECT-dispatch only and this rung made no crash reachable. ///// ⭐⭐ A REVIEW SABOTAGE RESULT WORTH KEEPING, ABOUT TEST DESIGN RATHER THAN THIS RUNG: injecting the exact ModR/M swap `encodeMovqGprXmm`'s comment warns of turned RED only the THREE cases that PIN A NUMBER (`chained`, `negative`, `nan`). The three that assert RELATIONS (`nonzero`, `zero-normalization`, `equal-values-equal-hashes`) all stayed GREEN, because both sides of the relation were wrong identically. A relational assertion cannot catch a bug that moves both operands. | 576,205 | 1,035,554 | 1,953,004 | 3,785,003 | 7,446,902 | 14,767,090 |
| 2026-07-29 | P1.8 SLICE D OPTIMIZATION PASS — `CharacterSet` + UCD `General_Category` + the three trims. ///// ⚠⚠ READ THE DELTA COLUMN AS A PATH ARTIFACT AGAIN — AND THEN IGNORE IT, BECAUSE THIS ROW REPLACES IT WITH A REAL MEASUREMENT. The printed delta is -4,046 … -143,473 allocations, negative and growing with rung, against a previous row written from the `maxon-p18b2` tree (11 chars) while this one is `maxon-p18d` (10). That is the checkout-path term this log has now recorded FOUR times, and the B2 row explicitly recorded its own rung cost as "owed, not claimed". ⭐ IT IS NO LONGER OWED. Paid here by a path-clean A/B: the parent `71cd8479b` checked out to `C:\Users\Eric\dev\maxon-base` — EXACTLY as long as `C:\Users\Eric\dev\maxon-p18d`, so the path term cancels — and built by the BYTE-IDENTICAL bootstrap (md5 9375432ee32201bf81263d4d9212c3e8 in both trees). THE WHOLE OF SLICE D COSTS THE SHARED CORPUS +14 ALLOCATIONS, FLAT AT ALL SIX RUNGS (a 32x span), and bytes of +1,352 +1,864 +2,888 +4,936 +9,032 +17,224 — which DOUBLE exactly, i.e. 16 bytes per FUNCTION plus ~840. Growth ratios are IDENTICAL on both binaries (x1.80 x1.89 x1.94 x1.97 x1.98), so the rung changes no curve anywhere. The +16/function is `phase:parse` with ZERO extra allocations — an existing per-function allocation crossing one 16-byte slab size class, the P1.0d.3 signature; which field was not isolated, and at 0.006% of the compile it was not chased further. ///// THE CORPUS IS BLIND TO EVERY CONSTRUCT THIS RUNG ADDS, MEASURED NOT ASSUMED: `--emit-corpus` at rung 5 finds `trim` 0, `CharacterSet` 0, `CharSet` 0, `Set with` 0, `Character` 0, `\uXXXX` 0. SIXTH consecutive rung with that property. New private ladder committed: `Testing/ladders/gentrim.sh`, four families (sites/data/edge/loop). ///// ⭐⭐ THE FIX: `trimStart` WALKED THE WHOLE STRING TO ANSWER A QUESTION IT HAD ALREADY SETTLED AT THE FIRST KEPT CLUSTER. `__str_trim` scans forward only (a deliberate divergence — the reference's backward `findGraphemeStart` has no GB12/GB13 rule and splits a flag the forward segmenter joins), and it ran that scan to the END on all three trims. But with `fromEnd` clear the result's high bound is the byte LENGTH and `keptEnd` is never read, so every cluster visited after the first NON-matching one was pure waste: a `__gr_end`, a `__char_at` MINT (an `__mm_alloc` + blit the bump slab never gives back), a `__cs_contains` and a `__str_decref`, all to compute a discarded bound. MEASURED on one call — `"abcdefgh"x65,536`.trimStart(), which has NOTHING to cut — 56.2 ms and 45.0 MB of peak RSS to answer "byte 0". FIXED by exiting the walk at the first kept cluster when `fromEnd` is clear (`buildStrTrim`'s new `firstKeptStop` arm, guarded by `keptStart == sentinel` so it is reached AT MOST ONCE per trim and no other path pays for it). AFTER: 0.46 ms and 4.8 MB — 121x and 9.3x — and the 0.46 ms that remains is the RESULT COPY (`emitSliceSegment` blits 512 KB), which is the floor for an owned result. On the ladder, `data-trimstart-clean` at 524,288 bytes went 262.2 ms -> 4.6 ms and 208.0 MB -> 6.7 MB. ⚠ `trim` and `trimEnd` are UNCHANGED to within noise at every rung (262.4 vs 268.7 ms; 261.8 vs 261.1 ms; RSS identical to the KB): they genuinely need the far end and they paid nothing for the exit. ⚠ THIS ROW'S SIX NUMBERS ARE Δ0 FOR THAT FIX, BIT-FOR-BIT, at all six rungs in allocations AND bytes — verified by building the pre-fix HEAD and re-running. That Δ0 is the instrument's blind spot, not the cost: `buildStrTrim` runs only under `usage.usesStrTrim` and the corpus never sets it. ///// `__str_trim` IS O(n), NOT O(n²) — the question the brief asked, settled by measurement rather than by reading the graph. `data-*` reads x2.00 per doubling out to 524,288 bytes in EVERY seed, in time and in peak RSS alike; `edge-*` (matched pad doubling against a fixed body) likewise. Per CLUSTER at the top rung: ASCII 102 ns, 2-byte BMP 150 ns, supplementary 190 ns — so `__gr_end`'s general scan costs ~48 ns over its ASCII fast path and `__ucd_cat`'s 806-ENTRY BINARY SEARCH costs ~40 ns over a direct BMP byte load. Real but bounded constants; nothing bends. ///// ⭐ THE SLAB TRIGGER FIRES A THIRD AND FOURTH TIME, and one of them is now PRICED. Workstream O's debt (bump allocator, no free list ⇒ memory linear in ITERATIONS not live data) named its trigger as "any construct that allocates once per loop trip"; `for c in s` was the first, `slice` the second. Slice D adds TWO more: a trim mints an owned result String per call, and A PREDEFINED `CharacterSet` IS REBUILT FROM SCRATCH ON EVERY CALL (the reference caches one in a `static let`; shv2 has no module-level `static let` of a heap value). PRICED by the `loop-trim` vs `loop-trim-shared` A/B — the same program but for whether the set is hoisted out of the loop: ~1,068 ns and ~1,155 BYTES OF NEVER-RECLAIMED SLAB PER CALL, which is HALF of what a whole `trim()` of a 9-byte string costs. It is a CONSTANT per call (the set has a fixed 7 members), not a growing one; what makes it matter is that the slab never gives it back, so in a loop it is linear in trips (x2.00 per doubling in RSS, 4,096 -> 65,536 trips). NOT fixed here: caching needs a language feature (module-level static heap values), and the slab's cure is a recycling `__mm_free` that `MmRuntime.maxon:738` already anticipates. Both remain the coordinator's to sequence. ///// ALSO MEASURED, NOT FIXED, AND REPORTED FOR A RULING: a no-argument `s.trim()` costs the COMPILER ~3,266 allocations per SITE against ~500 for `s.trim(cs)`, because the parser inlines the whole 7-member default preset (a set create, 7 minted Character literals, 7 inserts, a box) at every site. At 1,024 sites that is 4.24M allocations against the control's 0.90M. It is strictly LINEAR in sites, so it is a constant factor and not the mandate — recorded rather than chased, and a runtime `__cs_default()` entry point would collapse it if the coordinator wants it. | 576,211 | 1,035,560 | 1,953,010 | 3,785,009 | 7,446,908 | 14,767,096 |
| 2026-07-29 | P1.8 SLICE E OPTIMIZATION PASS — the String view/method leftovers, and the stdlib WHITELIST's first real scaling question. NOTHING WAS MADE FASTER, because nothing found was superlinear; what this row buys is the ATTRIBUTION of a delta column that would otherwise have read as an unexplained bend. ///// ⭐⭐ THE TRIGGER WAS THE PARSE DELTA GROWING +1,678 -> +2,422 ACROSS THE LADDER (total +4,429 -> +5,660), on a rung whose structural change is that `stdlib/helpers/string/utf16.maxon` is now compiled into EVERY invocation. The question that raises is the one `StdlibLoader.maxon`'s own header makes urgent — the list "IS DESIGNED TO GROW ... until it names every file there", 49 files against 3 listed — so if per-compile cost were O(corpus x listed modules), today's 0.005% becomes a genuine superlinearity in the loader's intended end state. THE ANSWER IS NO, AND IT IS EXACT RATHER THAN APPROXIMATE. `maxon-shv2/Testing/ladders/genwhitelist.sh` (committed) varies the MODULE COUNT with the corpus HELD — the axis the shared instrument has no knob for, since its module count is fixed at whatever the whitelist says that day, and a product term and a sum term are indistinguishable when one factor never varies. Extra `phase:parse` allocations vs MODULES=0, at 128/256/512-function corpora: +1,914 / +7,656 / +30,632 / +122,512 for 1/4/16/64 modules — IDENTICAL TO THE ALLOCATION at every corpus size, exactly linear in the module count, exactly invariant to the corpus. The same knob dropped into `scale-test`'s OWN emitted rung 0 (30 files, 4,890 lines) and rung 5 (185 files, 140,917 lines) reads the SAME four numbers at both ends of a 32x span. Repeated with modules carrying array literals and generic instances — the documented `O(files x types)` hazard `arrayLiteralElementsInterned` exists for — parse reads +8,936 (M=16) / +35,728 (M=64) at rung 0 and the same at rung 5, while `signatures` and `merge` come out very slightly SMALLER on the bigger corpus (-55/-126, -26/-80) because the corpus has already interned what the module wants. That is the only cross term that exists and it has the WRONG SIGN to be a hazard. ⇒ 49 listed modules will cost the SUM of 49 modules' own parses, at any program size; per unit of user code an unused listed module costs ZERO. ///// ⭐ SO WHAT WAS THE +744? NOT THE WHITELIST — measured with a THIRD binary, `nowl`, identical to head but for the one whitelist line, built by the same bootstrap and driven interleaved from an equal-length path. `head - nowl` (the whitelist entry ALONE) is +1,654 `phase:parse` allocations at EVERY ONE of the six rungs, +659 lex, +212 signatures, ~+400 merge, +4,453 total at rung 0 and +4,444 at rung 5 — FLAT, and independently reproducing the implementer's own isolated figure. `nowl - base` (all the REST of slice E) is +24 / +48 / +96 / +192 / +384 / +768 across the six rungs — an exact x2.00 doubling, strictly linear in program size — plus a FLAT -70 saving from the `insertRangeChecks` unreachable-stdlib skip. It is +3 ALLOCATIONS PER TRIVIAL ARRAY-ELEMENT READ: `emitArrayElementAccessor` now asks `arrayElementValueType`, which `adoptType`-interns the element's declared name, where it used to hardcode `ValueTypeTag.integer`. Verified in isolation at 8/16/32/64 `.get()` sites reading +24/+48/+96/+192, and confirmed by deleting `u_arrays.maxon` from the corpus, which takes the delta to EXACTLY ZERO in parse AND merge. It is the price of the type-identity fix that makes a `Byte` read back out of an `Array with Byte` still a `Byte` — a slope, not a curve. ///// ⛔ THE CORPUS IS BLIND TO EVERY PART OF THIS RUNG — SEVENTH CONSECUTIVE RUNG. `--emit-corpus` at rung 5: `toByteArray` 0, `replaceFirst` 0, `codepoints` 0, `utf16` 0, `String.from` 0, `.bytes()` 0, `.replace(` 0, `.isEmpty()` 0 in any receiver. ⚠ AND THE TRAP IN REVERSE: `.clone()` appears 256 times and EVERY ONE IS AN `Array` RECEIVER (`ac0`…`ac255`, the array-ops knob) — not one String. A non-zero on the shared ladder for a `clone` still cannot be ABOUT this rung. `maxon-shv2/Testing/ladders/genstringviews.sh` is committed as the instrument (three families: `sites-*` compile, `data-*` run-on-length, `loop-*` run-on-trips; nine site modes incl. a control and the whitelisted `utf16*` free functions; ascii/wide/supplementary seeds). ///// WHAT IT READ. COMPILE, 64 -> 1,024 sites: every one of the nine modes linear in allocations and CPU, all ratios approaching x2.00 from below. `sites-utf16fns` is the CHEAPEST of the nine (488,192 allocations at 1,024 sites vs the control's 921,668) — a whitelisted stdlib call costs less than the control's `append`. RUN, `data-*` 32,768 -> 524,288 bytes: ⭐⭐ x2.00 PER DOUBLING IN EVERY MODE AND EVERY SEED, so ALL THREE MATERIALIZED VIEWS ARE O(n) — the push-only loops (`__str_to_bytes`/`__str_to_codepoints`/`__str_to_utf16` call `__arr_push` once per byte or codepoint into an array created with NO reserve) do NOT inherit a reallocation quadratic, which was a real open question and is now measured rather than read off `__arr_grown_cap`'s geometric policy. Per byte at the top rung: `clone` 1.66 ns, `replaceFirst` 3.2 ns, `replace` 4.6 ns, `toByteArray` 16.4 ns, `codepoints` 83.6 ns, `utf16` 93.2 ns. `replaceFirst` beats `replace` 1.43x at every rung on an absent needle, confirming the slice's claim that a known result size removes one of `replace`'s two passes. ⭐ Per CODEPOINT, `utf16` reads 93.2 / 96.5 / 191.1 ns for ascii / wide / SUPPLEMENTARY — the supplementary seed costs almost exactly DOUBLE, which IS the surrogate-pair arm pushing two units where BMP pushes one, and no seed but `supp` reaches it. ///// ⚠ WORKSTREAM O's SLAB DEBT FIRES AGAIN, RE-MEASURED, NOT RE-FILED. `loop-*` at 2^17 -> 2^20 trips reads x2.00 per doubling in peak RSS for every construct: memory linear in ITERATIONS. Never-reclaimed slab per trip at 1,048,576 trips on a 10-codepoint subject: `clone()` 72 B (the cheapest measured — HALF the control, which mints two String records to a clone's one), control 141 B, `toByteArray()` 161 B, `codepoints()` 376 B, `utf16()` 378 B. ⇒ `codepoints()`/`utf16()` cost ~2.6x the control and ~5x `clone()` per call, because an 8-byte-element array created with no reserve walks 4 -> 8 -> 16 capacities and the bump allocator keeps every intermediate buffer. The runtime KNOWS the bound at entry (`__str_to_bytes` already loads `len`), so one `__arr_reserve` would collapse it — a CONSTANT-FACTOR change on an already-linear curve, and it would move committed goldens, so it is REPORTED for a decision and deliberately not shipped by the optimizer that found it. ///// GATES: build exit 0 / 0 warnings, and the rebuilt binary is md5-IDENTICAL to the one the coordinator gated, so the tree is exactly as committed. Full unfiltered suite 2293 passed / 0 failed, exit 0 (no 101). `specs-shv2/fragments/` EMPTY after that run — byte-identical codegen. This pass changed no compiler source, so every per-rung number above is unmoved by it; the two ladders are the only files added. | 580,640 | 1,040,020 | 1,957,558 | 3,789,709 | 7,451,968 | 14,772,756 |
| 2026-07-29 | BREAK-IN-MATCH RUNG — a `match` STATEMENT is now a `break` target. LINEAR, and the delta is a SLOPE not a curve. Allocation deltas vs base: +365 / +546 / +910 / +1,636 / +3,090 / +5,996 across the six rungs; delta ratios 1.50 -> 1.67 -> 1.80 -> 1.89 -> 1.94, approaching x2.00 FROM BELOW while the corpus's own emitted code grows x1.85 -> x1.99. Sub-x2 throughout, so per unit of user code the cost is flat. ///// ATTRIBUTION, exact rather than inferred, because the change is small enough to enumerate: (1) ONE `MatchContextArray` per function parse and per closure parse, mirroring the `LoopContextArray` already created at those same three sites (`parseFunction`, and the save/reset/restore pair in `parseClosureExpression`); (2) ONE `MatchContext` + ONE `MatchArmExitArray` per match STATEMENT — gated on `not isExpression`, so a match EXPRESSION costs zero; (3) nothing per statement, per op, per value, or per token. That is why the delta tracks the corpus's match-statement count and nothing else. ///// NO NEW SUPERLINEARITY, and the one lookup that could have been one is not: `resolveControlTarget` walks the match stack at most ONCE per `break`/`continue` parsed, bounded by MATCH NESTING DEPTH — the identical O(depth) bound `self.loops` has had since M4b, over a stack that E2049 keeps shallow (a match arm body is a single statement and may not open a block, so matches nest only through the scrutinee). The labelled search scans matches-then-loops, two bounded walks, not a product. ///// NO STEP-6b OPTIMIZER TRIGGER FIRED and this row records why, so the absence is a reading rather than an omission: the rung adds no PASS, no IR OP, and no collection the compiler INDEXES BY (a parser stack pushed and popped in lockstep with syntax is not an index), and no allocation ratio came near the 2.4 threshold. The hunt carries to the phase-boundary sweep. ///// CORRECTNESS EVIDENCE STRONGER THAN THE SPEED READING, which is the part worth keeping: `specs-shv2/fragments/` is EMPTY under `git status` after a full unfiltered run, and against `main` the rung moves ZERO pre-existing goldens on x64-windows AND on x64-linux — 19 additions per target plus exactly ONE modification, `match-owned-value-flow/temp-scrutinee-break-arm`, whose diff is the SOURCE LINE ALONE (`break` -> `break 'loop'`) with byte-identical IR on BOTH targets. A parser change that re-targets a control-flow keyword and moves no existing byte of codegen is the strongest statement available that the new target is reached only by the programs that ask for it. | 581,005 | 1,040,566 | 1,958,468 | 3,791,345 | 7,455,058 | 14,778,752 |
| 2026-07-29 | P1.9 DECLARATION-SITE DIAGNOSTICS — E3062 unused-typealias (per-file use tracking, drained whole-program), E3005 bound-PROVENANCE validation at the typealias declaration, E2003 bare-sized shorthand, alias VISIBILITY + the stdlib carve-out, and E3005 on an out-of-range `otherwise` value. Measured on the MERGED tree, so this delta is against the BREAK-IN-MATCH row directly above. ///// LINEAR, AND THE SHAPE IS EXPLAINED RATHER THAN ASSERTED. Allocation delta +4,320 / +4,993 / +6,343 / +9,011 / +14,315 / +24,891 across the six rungs; ratios 1.16 -> 1.27 -> 1.42 -> 1.59 -> 1.74, rising monotonically toward x2.00 FROM BELOW. That is the signature of a FLAT term plus a linear one, not of a curve: fitting `a + b*n` to the two ENDPOINTS alone gives **a = 3,656 flat, b = ~664 per corpus unit**, and that two-parameter fit then predicts all four INTERIOR rungs to within 0.5% (4,983 / 6,310 / 8,965 / 14,274 predicted vs 4,993 / 6,343 / 9,011 / 14,315 measured). The flat term is the fixed per-compile cost — the seeded stdlib alias, the registry structures, the all-declarations `nameable` index — paid once whatever the program size; the ~664/unit is the per-FILE used-alias set plus the carried bound provenance, which is what a per-file check must cost. At rung 5 the whole rung is +24,891 on 14,803,643 = **+0.17%**. ///// ⚠ AN EARLIER READING OF THIS SAME RUNG SAID +1,990 -> +11,897 (fit: 1,670 flat + 320/unit), AND IT IS NOT A CONTRADICTION — IT IS A DIFFERENT BINARY. That was step 6a, run on the implementer's commit ALONE; the figures here include the independent review's two fixes (`RangedAliasRegistry.nameable`, an all-declarations index probed on the error path, plus the parenthesized-conformance reader and a visibility bit folded into the signature content hash) and the coordinator's path-resolution fix. The cost roughly DOUBLED and the SHAPE did not move at all — same approach-from-below, same sub-x2 ratios, same sub-1% fit. Recording both is the point: a per-rung ladder read taken before the review is a reading of code that is not what ships. ///// NO STEP-6b OPTIMIZER TRIGGER FIRED. The threshold is an ALLOCATION ratio past ~2.4 that you cannot explain; the highest here is 1.74 and it is accounted for to 0.5% by two parameters. `phase:resolveTypes`, where E3062 and the visibility check both live, reads 643/963/1,603/2,883/5,443/10,563 — ratios 1.50/1.66/1.80/1.89/1.94, the same shape. The hunt carries to the phase-boundary sweep. ///// ⚠⚠ THE CORPUS CANNOT EXPRESS THIS RUNG'S SUBJECT, SO THE DELTA ABOVE IS NOT ABOUT IT. `scale-test`'s generated programs declare ranged typealiases and USE them, so across every rung the E3062 arm that REPORTS never fires once, no bound-provenance mismatch is ever constructed, no bare-sized RHS is ever written, no alias is ever hidden cross-file, and no `otherwise` literal is ever out of range. What the ladder measures is the cost of ASKING on a corpus where every answer comes back "fine" — the per-file set built and walked, the provenance carried and compared. That bound is worth having and it is real, but a future rung must not read this row as evidence about the DIAGNOSTICS. Eighth consecutive rung whose subject the shared corpus cannot reach. | 585,325 | 1,045,559 | 1,964,811 | 3,800,356 | 7,469,373 | 14,803,643 |
| 2026-07-29 | OPTIMIZER PASS over the ranged-alias bound-enforcement rung. THE HEADLINE IS A CORPUS-BLINDNESS FINDING, NOT A SPEEDUP, and it reframes the rung's own row above. THE SHARED LADDER CANNOT SEE THIS RUNG'S GUARDS AT ALL. ScaleCorpus DOES declare narrow ranged typealiases -- ScaleCoverNarrow int(0 to 4095), ScaleWide int(0 to u64.max), and 4->128 ScaleXElem<N> int(0 to 10<N>) that DOUBLE with the rung -- so a reader checking the manifest for "does the corpus emit ranged aliases" gets YES and stops. But not one of them reaches a position that emits a guard: ScaleXElem<N> is only ever a generic TYPE ARGUMENT (ScaleXBox with ScaleXElem<N>), whose field and parameter are declared with the opaque type parameter T, so the alias is erased before any door sees it -- 128 narrow aliases, zero doors; and every alias that DOES reach a door (ScaleXHoldValue, ScaleMeasure<N>, ScaleElement, ScaleNamedValue, ScaleRowValue, ScaleSigned, ScaleDispMeasure, ScaleGenElement) is int(i64.min to i64.max), FULL RANGE, which needsRuntimeGuard discards at rangeIsFull. MEASURED, not inferred: the compiled rung0 binary and the compiled rung5 binary each contain EXACTLY ONE range-check panic blob, the same one, and an instrumented build confirms exactly ONE function in the whole program ever emits a runtime guard -- scaleCoverCast, at every rung. So the rung's +1,340/+2,119/+3,665/+6,751/+12,885/+25,150 measured the RECORDING of sites that are then discarded as vacuous; the EMISSION path has never been on this ladder. That is the subtler form of the blindness the manifest warns about: the construct is present, the KNOB THAT MAKES IT COST ANYTHING is not. ///// SO A PURPOSE-BUILT LADDER WAS ADDED -- Testing/ladders/genrangesites.sh, two modes, onefunc (N guards in ONE function, the product axis) and spread (the same N one-per-function, the control). It found and this pass FIXED a real O(sites x blocks) term: RangeCheckEmitter.blockEndGuardSite located each guard site's block with IrModule.getBlockByIdIn, a LINEAR SCAN of func.blockRefs, once per site -- and worse than the plain product, because every guard the pass emits APPENDS TWO BLOCKS (__rc_ok, __rc_panic) to the very set the next site's scan walks. It now resolves through buildBlockIdIndex/blockById, the index pruneDeadBlockArgs introduced for exactly this shape one pass over ("would be O(edges x blocks), the shape the allocator's data-representation contract exists to forbid") and elimTrivialBlockArgs already shares -- a third user of one index, not a fourth mechanism. THE TERM WAS ISOLATED BY HOLDING ONE AXIS AND MOVING THE OTHER, genwhitelist.sh-style: with guards held at S and blocks swept B=512..4096, the CPU slope in B was 4,045 ticks/block at S=1 and 5,564 at S=8 -- a slope that MOVED WITH S is the product term, and it separates as roughly 3,828 per block per function plus 217 per (site x block). After the fix the two slopes are 3,905 and 4,020, ratio 1.375 -> 1.029: the S-dependence is GONE. Phase CPU at S=8 B=4096: 23,244,143 -> 16,867,165 ticks, -27.4%. ⚠ THE ALLOCATION COLUMN IS BLIND TO ALL OF IT -- it read FLAT in B before and after, because a scan allocates nothing. This is the third column earning its place. ///// THE +2 ALLOCATIONS ON THIS ROW, FLAT AT EVERY RUNG, ARE THE WHOLE COST: the shared empty index plus the one real index scaleCoverCast builds. It was NOT flat in the first cut. Storing the index on RangeCheckEmitter with an unconditional ValueIdArray.create() cost +45/+66/+108/+192/+360/+696 across the rungs, because an emitter is built for EVERY function carrying any range-check side data -- which includes every function that merely passes a CONSTANT ARGUMENT or constructs a generic instance -- so the overwhelming majority paid for an array they never read. Fixed the way Parser already solves the identical problem, a shared never-mutated empty (sharedEmptyInstanceAliasNames), and the index is built lazily on the first RUNTIME guard, which a compileTimeOnly call-argument site never reaches. The residual growing BYTES (+392/+560/+896/+1,568/+2,912/+5,600) are allocation-free and fully attributed: they are the 8-byte field added to RangeCheckEmitter times the emitter count -- 392/8 = 49 emitters at rung0, 5,600/8 = 700 at rung5, matching the +45 and +696 allocation counts that were just removed. ///// HANDED BACK, NOT FIXED, BECAUSE IT IS NOT THIS PASS'S SUBSYSTEM: ~16 runtime range-check guards in ONE function PANIC the compiler in the register allocator -- "register-pressure E5001 ... value is in the blocking set but resolves to NO source span ... This is a COMPILER BUG" from RegisterPressureDiagnostic, raised out of splitLiveRanges. Bisected to n=16 guards, and the CONTROL is clean and separates cleanly: the identical program with the alias widened to int(i64.min to i64.max) -- same statements, same field stores, no guards -- compiles fine, and n=12 compiles fine. It is PRE-EXISTING rather than this rung's: the old `as`-cast door reaches it at the same n=16. But this rung makes it far easier to reach, because a field store is far more common than an explicit cast. It also CAPS the onefunc ladder at n<=12, which is why the product term above had to be measured on the block axis instead. ///// CORRECTNESS: full unfiltered spec-test 2348/0, exit 0, no leak, and specs-shv2/fragments COMPLETELY CLEAN after the run -- byte-identical Target IR for 2,348 programs, which is the strongest available statement that an optimization changed no behaviour. | 582,347 | 1,042,687 | 1,962,135 | 3,798,098 | 7,467,945 | 14,803,904 |
| 2026-07-29 | RANGED-ALIAS BOUNDS ENFORCED AT EVERY POSITION — six doors, not two. LINEAR; the delta is a SLOPE. Allocations vs the break-in-match row: +1,340 / +2,119 / +3,665 / +6,751 / +12,885 / +25,150, delta ratios 1.58 -> 1.73 -> 1.84 -> 1.91 -> 1.95, approaching x2.00 FROM BELOW against a corpus growing x1.85 -> x1.99. ///// ⭐⭐ THE MOST IMPORTANT THING THIS ROW RECORDS IS THAT THE LADDER CANNOT SEE THIS RUNG AT ALL — EIGHTH CONSECUTIVE RUNG, and the mechanism is a NEW ONE worth naming. The manifest check PASSES: `ScaleCorpus.maxon` really does declare narrow ranged aliases (`ScaleCoverNarrow = int(0 to 4095)`, and 4->128 `ScaleXElem<N>` that DOUBLE with the rung). But `ScaleXElem<N>` is only ever a GENERIC TYPE ARGUMENT (`ScaleXBox with ScaleXElem<N>`), so the field and parameter it flows through are declared with the opaque type parameter `T` and the alias is erased before any door sees it: 128 narrow aliases, ZERO doors. Every alias that DOES reach a door is `int(i64.min to i64.max)` — full range, discarded at `rangeIsFull`. MEASURED, not inferred: the compiled rung-0 and rung-5 binaries each contain EXACTLY ONE range-check panic blob, the same one, and an instrumented build confirms exactly ONE function ever emits a runtime guard, at every rung. ⇒ **A NARROW ALIAS IN THE CORPUS IS NOT THE SAME FACT AS A NARROW ALIAS AT A DOOR.** A future manifest check for this feature must count DOORS, not aliases. `Testing/ladders/genrangesites.sh` is committed as the instrument that does. ///// WHAT THE PURPOSE-BUILT LADDER FOUND, which the shared one structurally could not: `blockEndGuardSite` located each site's block with `getBlockByIdIn`, a LINEAR SCAN per site — and worse than a plain product, because every guard emitted APPENDS TWO BLOCKS to the set the next site's scan walks. O(sites x blocks) -> O(blocks + sites) via the block-id index `pruneDeadBlockArgs` already owns (third user of one index, not a fourth mechanism). Isolated by holding guards and sweeping blocks: slope 4,045 -> 3,905 ticks/block at S=1 and 5,564 -> 4,020 at S=8, so the PRODUCT term went 1.375 -> 1.029. Phase CPU at S=8/B=4096: 23,244,143 -> 16,867,165 ticks, -27.4%. ⚠ **THE ALLOCATION COLUMN READ FLAT IN B BEFORE AND AFTER — a scan allocates nothing, so this was findable ONLY in CPU.** That is the column O4 exists for, earning itself again. ///// THEN THE FIX THAT MATTERED MORE THAN THE SPEED. A guard SPLITS its block, so resolving every site to the parser-recorded block id chained guards BACKWARDS: guard k+1's cascade landed before guard k's branch. Two consequences, both silent: guards ran in REVERSE SOURCE ORDER (the compiler reported the SECOND of two bad casts), and `recordFusableCompare` lost fusion for N-1 compares, which then materialized through `setcc` and sat in registers until the 14-GPR file overflowed at N=16 and the RULE 3 backstop panicked. `where-clauses.two-constraints` shows it was a genuine MISCOMPILE, not just ordering: two cascades compared rbx against 0 and r12 against 1000, mixing two fields' values across two checks. Fixed by making the index a CHAIN END rather than a block identity; the values stop existing as registers at all, so no rematerialization was needed. Threshold gone: both doors compile and run at N=512. At n=12 `regalloc:splitting` went 149,767,046 -> 73,836,068 CPU (-50.7%) and 7,196 -> 4,393 allocations (-39%). ///// AND THEN GUARD POSITION, on a user ruling. A guard fired at END OF BLOCK, so the whole rest of the block ran first: an out-of-range value was PRINTED and read back out of a `Percent` slot before its panic, and `100 / d` panicked "integer divide by zero" where the oracle panics "Range check failed 'NonZero'" — the language's own check pre-empted. Fixed with a two-tier position (`maxonOpPos` stamped by the parser, `stdOpPos` translated by a single `RangeSiteWalk` in `lowerMaxonToStd`), composed with the chain via `guardChainBase` so N guards at N positions fire in source order. +11 allocations FLAT for the whole compile plus +1 per EMITTED guard; 70 goldens moved, every one the same shape (the cascade moving up, the instructions between site and old block end moving past it), emitted code SIZE identical at every rung. ///// ⚠ MEASURED DEBT, RE-MEASURABLE AND NOT FIXED: `regalloc:splitting` is QUADRATIC IN GUARDS-PER-FUNCTION, now observable to n=512 (x2.09, 2.37, 3.06, 3.53, 3.77, 3.89 per doubling). The `spread` control — same guard count, one per function — is LINEAR (->x1.96), and a guard-free N-blocks-in-one-function control is linear too, so it is per-function CONCENTRATION, not guards and not blocks alone. NOT created by this rung (block and value counts unchanged; the fix strictly reduced it), only newly REACHABLE, and it looks like Workstream O's existing `regalloc:splitting` debt arrived at by a new axis. Re-measure trigger: `genrangesites.sh onefunc` at n>=128. | 583,441 | 1,044,723 | 1,966,055 | 3,805,786 | 7,483,169 | 14,834,200 |
| 2026-07-29 | RE-READ ON THE MERGED TREE — the rows above this one were measured on a binary that did not contain P1.9's declaration-site half, which landed upstream mid-rung. Same correction upstream's own last row made, for the same reason, and it is worth stating as a rule: A LADDER ROW IS ABOUT A BINARY, NOT ABOUT A CHANGE, so a rebase onto an advanced main invalidates it even though nothing in the rung moved. ///// THE MERGED NUMBERS: allocations 587,761 / 1,049,716 / 1,972,398 / 3,814,797 / 7,497,484 / 14,859,091, deltas +4,320 / +4,993 / +6,343 / +9,011 / +14,315 / +24,891 vs the row directly above. (⚠ CORRECTED AFTER THE RUN: the note as submitted quoted deltas from an earlier measurement against a DIFFERENT BASELINE, which is the same class of error this row exists to fix. The numbers here are the ones the instrument printed.) Delta ratios 1.16 -> 1.27 -> 1.42 -> 1.59 -> 1.74 — approaching x2.00 from below, LINEAR, and TIGHTER than the pre-merge read (1.58 -> 1.95) because P1.9's declaration-site checks add per-declaration work that scales with the corpus rather than per-site work that does not. `phase:insertRangeChecks` reads 2,013 / 3,390 / 6,128 / 11,586 / 22,488 / 44,270 — ratios 1.68 -> 1.81 -> 1.89 -> 1.94 -> 1.97, linear, 0.30% of the compile at rung 5 (up from 0.094% pre-merge, which is the guard-POSITION work: a two-tier position per site plus the `RangeSiteWalk` translation). ///// NOTHING SUPERLINEAR APPEARED IN THE COMBINATION, which was the open question a merge of two range-checking rungs raises: P1.9 checks a typealias at its DECLARATION (per-file, drained whole-program) and this rung checks values at their POSITIONS (per-site), and the two do not multiply — declarations and sites are independent axes and the ladder grows both. `regalloc:splitting` is unmoved at x2.19/x2.14/... per doubling, the same profile as before the merge. ///// ⚠ THE MEASURED DEBT FROM THE ROW ABOVE STANDS UNCHANGED and is NOT re-measured here, because the shared ladder still cannot express it: `regalloc:splitting` is quadratic in guards-per-FUNCTION (x3.89 per doubling at n=512 on `genrangesites.sh onefunc`), while this corpus emits one guard in the whole program. Re-measure trigger is unchanged: `genrangesites.sh onefunc` at n>=128, not this table. | 587,761 | 1,049,716 | 1,972,398 | 3,814,797 | 7,497,484 | 14,859,091 |
| 2026-07-29 | G1 (worker-exe split) — HARNESS-ONLY, AND THE LADDER IS UNMOVED BY IT. The rung splits `Main.runSpecTest`'s single `Process.executablePath()` into workerExe (what a pool worker IS) and compilerExe (the compiler under test, new `--compiler=<path>`), touching only Main.maxon + Testing/SpecWorkerPool.maxon — no pass, no IR op, no collection the compiler indexes by, so no step-6b optimizer trigger fired. ///// ⚠ THE +192 ALLOCS AND NEGATIVE BYTES DELTAS AGAINST THE ROW ABOVE ARE NOT THIS RUNG'S, AND THAT IS A MEASUREMENT RATHER THAN A READING. A/B at the SAME CHECKOUT PATH (the one thing that silently reverses this measurement's sign), rebuilding the pre-change sources from 518a19274 into the same location: PRE reads allocs 587,953 / 1,049,908 / 1,972,590 and POST reads 587,953 / 1,049,908 / 1,972,590 — identical to the digit, with allocsDelta +192 and bytesDelta -12,107 / -14,627 / -19,667 on BOTH. (The A/B climbed 3 rungs, not 6; the delta is a CONSTANT +192 across all six rungs of the post run, i.e. flat in input size, so it is a fixed per-compile cost and the three shared rungs settle it.) The deltas are therefore inherited from commits that landed on main between the previous logged row's binary and 10e518b8e; G1 contributes exactly ZERO. Worth writing down because the obvious story — "a new module-level `--compiler=` string constant plus a new function, hence a few more startup allocations" — is plausible, is flat-in-rung-size exactly as the +192 is, and is WRONG. ///// The A/B is the only honest form for this question: a Δ0 claim about MY change cannot be read off a delta against SOMEBODY ELSE'S baseline, because a log row is about a BINARY and not about a change — the rule the row above this one states — and two independent code deltas separate that logged binary from this one. | 587,953 | 1,049,908 | 1,972,590 | 3,814,989 | 7,497,676 | 14,859,283 |
| 2026-07-29 | FIRST-CLASS TUPLES (heap-allocated) + an O(files x types) FIX THAT PREDATES THEM. ///// THE RUNG ITSELF IS +12 ALLOCATIONS FLAT AT EVERY RUNG — two `ProgramSignatures` maps constructed once per compile, and nothing else. A tuple is a SYNTHESIZED STRUCT (`__Tuple2.int.int`) typed through the existing `MaxonType.structRef`, so no new type-system arm, and the per-Parser layout map uses the codebase's shared-empty + copy-on-write idiom so a tuple-free file allocates ZERO. ///// ⛔⛔ NINTH CONSECUTIVE RUNG THE SHARED LADDER CANNOT SEE, and this time it is not subtle — the feature did not EXIST until this commit, so the corpus could not contain it. MEASURED rather than assumed: `--emit-corpus` wrote 465 files / 279,453 lines across rungs 0-5, and greps for tuple destructuring, `.N` element access, tuple literals, tuple return types and tuple typealiases return ZERO hits each; `Testing/ScaleCorpus.maxon` contains the string "tuple" zero times. So the +12 measures two empty maps and not one line of `internTupleType`/`registerTupleLayout`/`canonicalTupleName`/`parseTupleLiteral`. `Testing/ladders/gentuples.sh` is committed as the instrument that does reach them — seven modes in four CONTROL PAIRS, which is what lets a product term separate. ///// WHAT THE PURPOSE-BUILT LADDER FOUND, and it was NOT tuples: `internArrayLiteralAggregateInstances` ran ONCE PER FILE holding an `[<identifier>...]` literal, walking every struct/union registered SO FAR over tables still growing — O(files x types). `phase:signatures` allocations at 64->1024 files: 18,356 / 39,630 / 94,408 / 253,058 / 766,908 = x2.16 x2.38 x2.68 x3.03, AFTER 15,490 / 29,750 / 58,218 / 115,102 / 228,818 = x1.92 x1.96 x1.98 x1.99. CPU 785,944,503 -> 187,830,368 ticks at n=1024. ⭐ THE STRUCT-ONLY CONTROL (no tuples anywhere) BENDS IDENTICALLY, which is how it was attributed as PRE-EXISTING: tuples merely added a SECOND producer of registered types and thereby a second way to reach it. The fix is a HOIST — `queryProgramSignatures` folds every file before any reader runs, so one final walk interns the same types; it also closes a latent hole where a type declared after the last literal-bearing file was never interned at all. The deleted dedup Set is why the rung's own footprint fell +20 -> +12. ⚠ The hoist CHANGES GENERIC-INSTANCE ID ORDER (hash-bucket order of a rehashing map -> one final walk); verified safe by the strongest available check — all 2,372 pre-existing fragment goldens byte-identical after a full run. ///// ⚠ MEASURED DEBT, RE-MEASURABLE, NOT FIXED, and NEITHER IS TUPLE-SPECIFIC. (1) `StructLayout.indexOfField` (`Project.maxon:1422`) is O(fields) by NAME STRING, so n accesses over n fields is a product: `arity` parse CPU x1.61/2.34/2.95/3.53 at n=32->512 against `access`'s x1.19/1.34/1.52/2.11 with the field count pinned at 2 — and a DECLARED STRUCT with 512 fields read 512 times reads x1.31/1.76/1.98/2.73 on the same ladder, so it is the struct layout's shape, not tuples'. Allocation-flat, CPU-only, structurally invisible to the memory columns. The realistic instance is the compiler's OWN `Parser` record (~50 fields), where every `self.x` pays an averaged 25-compare scan as a CONSTANT FACTOR, not a curve. Re-measure trigger: `gentuples.sh arity` at n>=128. (2) A nested tuple's mangled name is O(total leaves), so a depth-d chain is O(d^2) bytes of name: `nest` parse CPU x1.37/1.98/2.77/3.55 at depth 8->128. Inherent to a MANGLED-NAME IDENTITY; fixing it needs structural ids rather than names. Linear in practice at realistic depth (<=4). Re-measure trigger: `gentuples.sh nest` at n>=32. ///// CORRECTNESS: full unfiltered suite 2401/0 exit 0 (no 101), x64-linux PASS, wasm32-wasi PASS, arm64 SKIP; 29 fragment ADDITIONS and ZERO modifications, and `specs-shv2/fragments/` clean after a full re-run — byte-identical codegen for every pre-existing program, which is the strongest statement available that a feature this size is purely additive. | 587,773 | 1,049,728 | 1,972,410 | 3,814,809 | 7,497,496 | 14,859,103 |
| 2026-07-29 | D5 `#if` CONDITIONAL COMPILATION + A MEMO-PROBE FIX THAT MOSTLY PREDATES IT. NET IS NEGATIVE: rung 5 goes 14,864,427 -> 14,858,715, i.e. -388 BELOW the pre-rung baseline, because the fix returns more than the rung costs. ///// THE RUNG ALONE measured +968/+1,108/+1,388/+1,956/+3,084/+5,324 allocations (`828 + 140*2^n`, successive increments x2.00 x2.03 x1.99 x1.99 — dead linear), and `phase:directives` 33/38/48/68/108/188 which is EXACTLY files+3 (corpus has 30/35/45/65/105/185 files). ///// ⛔⛔ TENTH CONSECUTIVE RUNG THE SHARED LADDER CANNOT SEE, and MEASURED not assumed: `--emit-corpus` wrote 465 .maxon files across rungs 0-5 and a grep for hashIf, hashElse and hashEndif spellings returns ZERO matches. So `phase:directives` on this ladder is the per-file memo BOOKKEEPING and NOT ONE TOKEN WAS EVER FILTERED while it was taken. `Testing/ladders/gendirectives.sh` is committed as the instrument that does reach it — nine modes in four CONTROL PAIRS. ///// WHAT THE PURPOSE-BUILT LADDER FOUND: NO SUPERLINEARITY, on any of five axes, and here are the numbers rather than a claim. (a) REGION COUNT: `regions` x1.99 allocs / x2.01 cpu at n=64->2048 against the `plain` control, whose surviving program is IDENTICAL and whose directives phase is FLAT at 5 allocations. (b) DEAD-BRANCH SIZE: `dead` x1.99 / x1.97 — skipping is proportional to what is skipped and no worse, and cheap: 139,616 bytes against `regions`' 2,207,504 for the same input tokens. (c) NESTING DEPTH — THE PRODUCT AXIS, since a liveness test that WALKED the frame stack would be depth x tokens: `nest` 4,130 allocs / 6,753,314 ticks against the depth-1 `flat` control's 4,119 / 5,903,606, both x1.99 — INDISTINGUISHABLE, so liveness is a top-of-stack read and the frame stack is O(1) per directive. (d) CONDITION LENGTH: `cond` 14 allocations FLAT from n=64 to 2048 (`equalsIgnoringAsciiCase` compares source slices in place and allocates nothing), CPU x1.88 — the DELIBERATELY non-short-circuiting evaluator costs exactly its atoms, which is what that design was supposed to cost. (e) CONDITION DEPTH: `parens` 14 allocs flat, CPU x1.66; it stack-overflows between depth 1024 and 2048, AND THE CONTROL FAILS IDENTICALLY — an ordinary expression with 2048 nested parens overflows in `Parser.current` the same way — so that ceiling is the compiler's uniform recursive-descent property, not a hole this rung opened. ///// ALSO MEASURED: the two token caches SHARE one array and there is no hidden copy. A directive-free file's directives phase allocates 40 bytes (n=2048 `plain`: 200 bytes for the whole compile) against 2,207,504 bytes when the filter actually rebuilds — four orders of magnitude, which no copy could hide behind. And a `#if` region costs ~2 allocations (a `ConditionCursor` + a `DirectiveFrame`) whatever it contains. ///// THE FIX — THE MEMO PATTERN PROBED ITS `Map with (FilePath, _)` TWICE FOR ONE FACT, and that is not a free lookup on Windows: `FilePath.hash` CLONES AND LOWER-CASES the path (`canonicalForCompare`) and `FilePath.equals` clones twice more, which is why `Project.maxon`'s `RangedAliasByDeclaringFile` is keyed by a plain String instead. Every query did `xCacheValid(path)` (which probed the map to compare a hash, then threw the memo away) and then probed the SAME map again to read the value — with a panic guarding the disagreement that second probe made expressible. `sourceFiles` was probed once to VALIDATE and again to STAMP. `queryParseOps` computed `currentParseMemoKey` twice. Now: one `sourceEntryOf` per query serves both the validity hash and the stamp, `if let memo = cache.get(path)` serves both the hash and the value, and the parse key is computed once and stamped with that same value. `querySourceFile`, `tokensCacheValid`, `activeTokensCacheValid`, `parseCacheValid` and two impossible-state panics are deleted. RESULT: -(162 + 30*files) allocations, EXACT at every one of the six rungs (-1,062 / -1,212 / -1,512 / -2,112 / -3,312 / -5,712) — three successful FilePath map probes removed per file, so A SUCCESSFUL FILEPATH-KEYED PROBE COSTS 10 ALLOCATIONS. Bytes -43,487 at rung 5 = ~78 per probe, one cloned path string. CPU -0.09% at rung 5: INSIDE the noise band, not a datapoint. Most of this predates D5 — the `queryTokens` and `queryParseOps` halves are the pre-existing spine. ///// ⚠ MEASURED DEBT, NOT FIXED: `tokensHoldDirective` (`ConditionalCompilation.maxon:228`) RE-SCANS EVERY TOKEN OF EVERY FILE to answer a question the LEXER already knows for free, and it is exactly the cost class the memory columns cannot see — `phase:directives` allocations are FLAT per file while its CPU reads 3,390,864 / 6,436,030 / 12,617,014 / 25,826,151 / 50,942,407 / 100,523,097 = x1.90 x1.96 x2.05 x1.97 x1.97, dead linear and 0.41% OF TOTAL COMPILE CPU at rung 5 on a corpus containing no directive at all. ATTRIBUTED, not guessed: that CPU tracks SOURCE BYTES (x29.7 across the ladder) and not FILE COUNT (x6.2), so it is the token walk and not the per-file probe overhead. Removing it means threading a flag out of `tokenize` through `TokenMemo`, i.e. putting directive knowledge in the lexer's signature, for four tenths of one percent — a constant factor, so it is filed rather than chased. Re-measure trigger: `gendirectives.sh plain` (its directives phase is 100% this scan), or `phase:directives` CPU passing ~1% of a rung. ///// CORRECTNESS: full unfiltered suite 2424/0 exit 0 (no 101); `verify-warm-rebuild` PASS on a 36-file project WITH directives (determinism + cache + invalidation, including the new activeToken hit/miss assertions); and `specs-shv2/fragments/` COMPLETELY CLEAN after the full run — zero modified, zero untracked, so codegen is byte-identical for all 8,436 goldens. | 587,679 | 1,049,624 | 1,972,286 | 3,814,653 | 7,497,268 | 14,858,715 |
| 2026-07-29 | D1 (instance methods on `union`/`enum`) — ALLOCATIONS UNCHANGED, BYTES +1,040/+1,632/+2,816/... and the instrument attributes 100% of it. Allocs delta is 0 at EVERY rung, so the rung adds no allocation site. The bytes rise is one added field: `Parser.enclosingTypeIsEnum as bool` (the KIND half of the fact `enclosingType` names). ///// ATTRIBUTION IS ARITHMETIC HERE, NOT A STORY. The whole delta lands in exactly the two phases that construct Parser records: rung 0 = `phase:signatures` +776 and `phase:parse` +264 = 1,040, the reported delta to the byte; rung 2 = +2,432 and +384 = 2,816, likewise exact. Nothing else moved. A wider record charged per file is linear in file count, which is what the ratios say: bytesDelta grows x1.57 then x1.73, tracking the corpus's own x1.85/x1.92 code growth rather than outpacing it. Share at rung 2: 2,816 of 149,378,947 bytes = 0.0019%. `phase:signatures`/`phase:parse` also each gained +2 FREES, flat in rung size — `enterTypeScope` repoints the three per-declaration scopes at their shared empties, dropping two references the old `openTypeParamScope` path held. ///// NO STEP-6b TRIGGER. No new pass, no new IR op, and total bytes per rung ratio is x1.88 then x1.99 — linear, nothing near 2.4. ///// ⚠⚠ AND THE COLUMN THAT WOULD MATTER MOST IS BLIND HERE, WHICH IS THE HONEST CAVEAT: the generated corpus declares NO enum/union methods at all, so the sweep's new method-signature scan and the new `dispatchMethodOnEnumReceiver` arm are never entered by any rung. Their cost is UNMEASURED, not zero — a Delta-0 from a ladder that cannot express the feature measures the instrument's blind spot, not the cost, and that is the corpus blind spot rather than the allocation-vs-CPU one. What IS measured is that a program with no enum methods pays only the wider record. Re-measure trigger: a corpus knob that emits enum/union methods, or the 5d harness re-extraction, whose 13 files include five union `displayReason`/`isPass` methods. | 587,953 | 1,049,908 | 1,972,590 | 3,814,989 | 7,497,676 | 14,859,283 |
| 2026-07-30 | D2 (the postfix/member walk) — +146/+306/+626/+1,266/+2,546/+5,106 allocations, and the instrument attributes 100% of it to ONE phase and ONE object. ///// MEASURED AS AN A/B IN THIS WORKTREE, both binaries rebuilt: base origin/main (c9b5b6239) vs head. The whole-module delta equals `phase:parse`'s delta TO THE UNIT at every rung — parse allocs 116,048->116,194 (+146), 211,130->211,436 (+306), 400,939->401,565 (+626), 779,626->780,892 (+1,266), 1,536,332->1,538,878 (+2,546), 3,048,581->3,053,687 (+5,106) — and every other phase reads exactly 0. Bytes likewise: parse +345,888 at rung 5 over 5,106 objects = 67.7 bytes each, which is a header plus seven 8-byte fields. ///// IT IS ONE `MethodReceiver` PER MEMBER-ACCESS SITE, at parse time. The rung collapses two dispatch chains (eight arms keyed on a `VarInfo`, four keyed on a value) into one switch, and the receiver record is what both doors hand it. The deltas are 146 x2.10 x2.05 x2.02 x2.01 x2.01 — converging on exactly x2 against a ladder that doubles, i.e. strictly LINEAR in the number of `.member` sites, forever. Share at rung 5: 5,106 of 14,863,821 = 0.034%. ///// THE COST MODEL IS THE ONE THIS PATH ALREADY HAD, not a new one: `requireStructBase` allocates a `StructBase` per struct base for the identical reason ("ONE ANSWER, BECAUSE IT IS ONE QUESTION"), and a member access that previously reached it already paid a record. What is new is that a String/Array/Set/conformer receiver — which never reached `requireStructBase` — now pays one too. IT IS AVOIDABLE AND I AM NAMING IT: flattening `MethodReceiver` into seven parameters on `dispatchMethodOnReceiver` removes 100% of this delta and costs the switch nothing, since the record is the calling convention and not the thesis. Left as a record deliberately (it is the shape `StructBase` already established here); handed to the optimizer with an exact target. ///// NO STEP-6b TRIGGER: no new pass, no new IR op, no new whole-program query. Total allocations per rung x1.79 x1.88 x1.93 x1.97 x1.98, bytes x1.88 x1.99 x2.01 x2.03 x2.05 — unchanged from base. ///// CPU is inside the noise band and is not read as a datapoint. ///// CORPUS BLIND SPOT, stated because the feature is exactly the kind the ladder cannot express: ScaleCorpus emits no string/array/call-result RECEIVER chains and no enum methods, so the newly-reachable arms (`"abc".byteLength()`, `Inner.make(9).get()`, `o.inner.get()`, `pick().score()`) are never entered by any rung. Their per-call cost is UNMEASURED, not zero. What IS measured is that the shared switch charges every EXISTING member access one record. | 587,825 | 1,049,930 | 1,972,912 | 3,815,919 | 7,499,814 | 14,863,821 |
| 2026-07-30 | D1d + D1c (keyword-as-a-NAME is not block structure; E3081 for an all-discard binding list) — NEUTRAL, and the interesting number is one this rung INTRODUCED AND THEN REMOVED. ///// ⚠ THE DELTAS AGAINST THE ROW ABOVE ARE NOT THIS RUNG'S AND CANNOT BE: that row is D1's, measured before D5 and P1.10 landed, so -82/-92/-112 allocs and -1,048/-1,101/-1,213 bytes mix THREE code deltas (D5's token-tier directives — note the new `phase:directives` at 33/38/48 allocs — P1.10's tuples plus its O(files x types) fix, and this rung). A log row is about a BINARY, not about a change. The attributable measurement is a SAME-PATH A/B against a parent-commit binary built in this checkout: allocations and bytes BYTE-IDENTICAL at all six rungs. ///// ⭐⭐ AND THAT A/B IS WHY THE RUNG IS NEUTRAL RATHER THAN WORSE. The first cut of the E3081 work spelled its comma separator once by collecting into an array — `joinCommaSeparated(array)` — which put a `StringArray.create()` in `checkEnumExhaustive`, a routine that runs on EVERY enum match and not only on a rejected one. Measured: **+20 allocations at rung 0 DOUBLING to +640 at rung 5, x2.00 every rung**. Replaced by `appendCommaSeparated(list, item)` — the separator is still written down once, and nothing is allocated on the success path. Independently confirmed here off the instrument: `phase:semanticCheck` reads 1,996 / 3,628 / 6,883, IDENTICAL to the D1 row above, so no per-match allocation survives. ⭐ **A DEDUP THAT IS CORRECT CAN STILL BE UNSCALABLE — "spell it once" is not free if the once is on a hot path.** ///// ⚠ A MEASUREMENT ARTEFACT WORTH KNOWING: the first re-run of that A/B read a flat -1 BYTE at every rung. Cause was the two A/B binaries' FILENAMES differing by one character — the exe path is a String the compile allocates — and equal-length names took it to 0. Cousin of the checkout-path-length effect already recorded in this log; it reaches the byte column through argv, not through the tree. ///// NO STEP-6b TRIGGER: no new pass, no new IR op, and per-rung totals are x1.88 then x1.99. | 587,871 | 1,049,816 | 1,972,478 | 3,814,845 | 7,497,460 | 14,858,907 |
| 2026-07-30 | D4 — a member-access refusal may not assert a type the compiler INVENTED (E3011 for an undeclared type name, at all three member-access doors). NEUTRAL, and neutral for a structural reason rather than a lucky one. ///// THE NEW WORK IS ON AN ALREADY-THROWING PATH AND CANNOT COST THE SUCCESS PATH ANYTHING. `undeclaredBaseTypeNameOf` asks `TypeResolution.denotedNamedType` — a name lookup plus a registry cascade — but it is only ever reached INSIDE a branch that has already decided to refuse the program. Per REFUSED member access, never per field read. The generated corpus contains no refused member access at all, so the expected delta is exactly zero and that is what the instrument reads: `phase:resolveTypes` and `phase:parse` are unmoved, and a same-path A/B against a parent-commit binary built in one checkout was BYTE-IDENTICAL in allocations and bytes at all six rungs. The two success-path hoists (binding `capturedTag` and `typeNameIdOf` once instead of twice, in `capturedStructBase` and `requireStructBase`) are reads of an already-materialized value, so they are allocation-neutral too. ///// WARNING, AND IT IS THE SECOND ROW RUNNING TO CARRY IT: the deltas printed against the previous row are NOT attributable to any code change. Measured from a worktree whose path is 3 characters longer than the main checkout's, the ladder read -46/+114/+434/+1,074/+2,354/+4,914 allocs — a delta that DOUBLES every rung because the corpus passes a path per file and the file count doubles. It is the checkout-path-length artefact the previous row already records, and the row above that one records its argv cousin (two A/B binaries whose FILENAMES differed by one character read a flat -1 byte at every rung). This row is measured from the main checkout after the merge, so its own deltas are against a differently-pathed predecessor and should be read the same way. THE ATTRIBUTABLE MEASUREMENT IS ALWAYS THE SAME-PATH A/B, never the cross-row delta. ///// ⭐ AND THE ARTEFACT REACHES THE ALLOCATION COLUMN, NOT ONLY THE BYTE COLUMN THIS LOG HAD RECORDED IT FOR — measured here by accident, worth knowing on purpose: the SAME commit, built and measured in two checkouts differing in nothing but path length, reads a CONSTANT **+14 allocations in `phase:load` at every one of the six rungs** (587,825→587,839 … 14,863,821→14,863,835) together with ~4.5 KB FEWER bytes in the shorter path. CONSTANT rather than doubling, because `load` opens a fixed set of files per compile while the per-file path cost doubles with the corpus — so the two artefacts have DIFFERENT SHAPES and a flat offset in a memory column can be the CHECKOUT rather than the code. Allocations are still bit-for-bit reproducible; they are reproducible FOR A GIVEN PATH. ///// NO STEP-6b TRIGGER: no new pass, no new IR op, no new collection the compiler indexes by. Per-rung totals x1.79 x1.88 x1.93 x1.97 x1.98 — at or below x2.00 every rung, nothing near the 2.4 that would spend an optimizer agent. | 587,839 | 1,049,944 | 1,972,926 | 3,815,933 | 7,499,828 | 14,863,835 |
| 2026-07-30 | D1e — pin the UNION rvalue receiver (3 spec cases; postfix-member-walk.md). NEUTRAL, and this row is the strongest neutrality claim the log can carry: THE COMPILER SOURCE DIFF IS EMPTY. `git diff` over the branch is one spec file plus 6 new goldens; `maxon-shv2/Compiler/Parser.maxon` was verified BYTE-IDENTICAL to main after the sabotage used to prove the new cases load-bearing was reverted. No pass, no IR op, no collection the compiler indexes by — and no code at all. ///// ⭐ SO THIS ROW IS A NEGATIVE CONTROL FOR THE CHECKOUT-PATH-LENGTH ARTEFACT THE PREVIOUS TWO ROWS DOCUMENT, and it is a better one than either, because those had real code changes to argue about and this one has none. Measured from the worktree C:\\Users\\Eric\\dev\\maxon-D1e, the ladder reads a CONSTANT -14 allocations at every one of the six rungs (587,825 / 1,049,930 / 1,972,912 / 3,815,919 / 7,499,814 / 14,863,821) with bytes +6,263...+32,303 DOUBLING per rung. Two different shapes in one reading, exactly as the D4 row predicts: the FLAT -14 is `phase:load`'s fixed per-compile file set, the DOUBLING byte delta is the per-file path cost riding a corpus that doubles. Since no compiler code changed, BOTH are the checkout and NEITHER is a cost — which is the cleanest confirmation available that a flat offset in an allocation column can be the path rather than the code. ///// THE CROSS-ROW DELTA REMAINS UNATTRIBUTABLE; the attributable measurement is always the same-path A/B, and here it is trivially byte-identical because the sources are. ///// NO STEP-6b TRIGGER: per-rung totals x1.79 x1.88 x1.93 x1.97 x1.98, at or below x2.00 every rung and nothing near the 2.4 that would spend an optimizer agent. | 587,825 | 1,049,930 | 1,972,912 | 3,815,919 | 7,499,814 | 14,863,821 |
| 2026-07-30 | R4.1 — the file-IO runtime's OPEN slice: `__ManagedFile` + `__ManagedMemory` + `__ManagedFileError` + the `osFileOpenRead` StdOp + `__mf_open_read`. Host-only by the 2026-07-30 ruling; every other target refuses at the call site with the EXISTING E3104. ///// ALLOCATIONS: +222 / +267 / +352 / +517 / +842 / +1,487 across the six rungs. LINEAR AND FULLY ATTRIBUTED — it fits ~181 flat + ~41 per doubling-unit to within 2% at every rung (rung 3 predicted 507 vs 517 actual, rung 4 predicted 834 vs 842). The flat term is `createEnumRegistry` seeding a SECOND builtin enum (`__ManagedFileError`) per registry construction; the per-file term is that seeding riding a file count that doubles. `phase:signatures` carries it (+70 at rung 0, +395 at rung 5) exactly as that attribution predicts. +1,487 is +0.01% of rung 5's 14.87M. ///// NO SUPERLINEARITY. Per-phase ratios are at or about x2.00 across the board; `regalloc:splitting` x2.14 and `liveness` x2.08 are the pre-existing, already-documented bends and are untouched by this rung — nothing here enters regalloc, because the new op is a system call with one def and one use. ///// ⚠ THE CPU COLUMN IS NOT USED TO CLAIM ANYTHING HERE, and that is deliberate rather than an omission: the previous row was measured from a DIFFERENT checkout path on a DIFFERENT binary, and this log's own standing warning is that the attributable measurement is always the same-path A/B and never the cross-row delta. A cross-row CPU reading on `phase:signatures` looked like a double-digit regression; it is not attributable and is not claimed. ///// ⭐ STEP-6b RAN (this rung adds an IR op, which is the trigger) AND FOUND ONE REAL CONSTANT-FACTOR COST, WHICH IS FIXED RATHER THAN FILED: the declaration sweep took a THIRD complete walk of every file's token array, because each compiler-owned layout probe (`StringIndex`, `CharacterSet`, and now `__ManagedFile`) had its own `fileNamesAnyToken` pass. Each probe genuinely needs a COMPLETE pass — the reason is a measured crash, not caution, since `recordScannedType` swallows method bodies — but completeness constrains what a walk VISITS, not how many walks there are. Fused into one `fileNamesProducerMask` that asks all three per token and exits once all three are satisfied: worst case one full scan where it was three, best case unchanged. Suite 2564/0 and fragments unchanged across the fusion, so it is behaviour-identical. | 588,047 | 1,050,197 | 1,973,264 | 3,816,436 | 7,500,656 | 14,865,308 |
| 2026-07-30 | D1b — RETAIN a managed payload bound out of a BORROWED union (+ refuse `and fallthrough` INTO a payload-binding arm). NEUTRAL, and this row carries the ATTRIBUTABLE measurement rather than the cross-row one. ///// ⭐ SAME-PATH A/B, BYTE-IDENTICAL AT ALL SIX RUNGS — measured, not asserted: `git checkout main -- Compiler/Parser.maxon`, rebuild, ladder; restore, rebuild, ladder. Same worktree, same path, same host, only the compiler source differing. Both read 588,251 / 1,057,073 / 1,993,397 / 3,863,167 / 7,607,234 / 15,087,230. Not one allocation moved. ///// ⚠⚠ THE DELTA COLUMN OPPOSITE IS A CROSS-PLATFORM ARTEFACT AND IS NOT A COST — READ NOTHING INTO IT. ⚠ NOTE ADDED AT REBASE: the row directly above this one is R4.1's, but this row's delta column was computed against D1e's row (now two above), because R4.1 landed while D1b was in flight. The delta is a property of the two rows COMPARED AT RUN TIME, not of the rows' final adjacency. That D1e row was measured on WINDOWS from C:\Users\Eric\dev\maxon-D1e; this row is arm64-macOS — a different OS, a different arch and a different checkout path, so the +426 to +223,409 allocs and -2.2M to -75.9M bytes are three artefacts at once. The proof it is not the rung is free and sits in this very run: THE PRE-RUNG COMPILER PRINTS THE IDENTICAL DELTA COLUMN, because the delta is a property of the two ROWS, not of the two BINARIES. That is the D1e row's own checkout-path finding reproduced across a platform boundary, one step stronger. ///// ⚠ AND THE LADDER STRUCTURALLY CANNOT SEE THIS RUNG: the corpus emits no `match`, so `declarePayloadBindings` — the one function the rung changed — is never reached at any rung. A Δ0 here is THE INSTRUMENT'S BLIND SPOT (the CORPUS blindness, which the CPU column cannot cure either), not evidence the mechanism is free. Its real cost is one `__mm_incref` per managed payload bound out of a borrowed union, bounded and unconditional by construction, verified in the emitted arm64 rather than argued: the payload load, then `bl __mm_incref` as the very next op, then exactly one `bl __str_decref` at the arm exit. ///// NO STEP-6b TRIGGER: per-rung totals x1.79 x1.88 x1.93 x1.96 x1.98, at or below x2.00 every rung and nothing near the 2.4 that would spend an optimizer agent — and a byte-identical A/B leaves one nothing to hunt. | 588,251 | 1,057,073 | 1,993,397 | 3,863,167 | 7,607,234 | 15,087,230 |
| 2026-07-30 | W1 — a function TYPEALIAS dropped `ExitCode`'s WIDTH, so an ExitCode-returning function VALUE trapped on wasm (`indirect call type mismatch`). One rule, three doors, now one. NEUTRAL. ///// ⭐ SAME-PATH A/B, BYTE-IDENTICAL AT ALL SIX RUNGS — measured by the coordinator, not inherited: revert BOTH changed compiler files to main, rebuild, ladder; restore, rebuild, ladder. Same worktree, same path, same host. Both read 588,336 / 1,057,178 / 1,993,542 / 3,863,392 / 7,607,619 / 15,087,935. Not one allocation moved, which is the expected shape — the fix replaces a tag with a wider tag on one already-taken branch and adds no allocation anywhere. ///// ⚠⚠ AND THE DELTA COLUMN OPPOSITE IS AGAIN NOT A COST, BUT ITS STORY IS DIFFERENT FROM D1b's AND WORTH READING. It shows +85 / +105 / +145 / +225 / +385 / +705 allocs, which GROWS with the rung (≈4 allocs and 54 bytes per added corpus file) — so the flat-offset checkout-path story the D1e and D1b rows document would NOT have covered it, and it must not be waved away with that argument. The pre-rung binary at this same path prints the IDENTICAL totals AND the identical delta column, so the growth is a property of the two log ROWS (a cross-checkout, cross-host per-file path cost riding a corpus that doubles), not of the two BINARIES. Third consecutive row where the attributable measurement is the same-path A/B and the cross-row delta is an artefact. ///// ⚠ CORPUS REACHABILITY, so a Δ0 is not misread as "free": the ladder emits NO function typealias, so the fixed door is never taken at any rung. It DOES emit 4 interfaces per rung, all returning a user ranged alias — those now route through the new shared `builtinTypeNameTag` and reach the identical `named` answer with identical allocations, which is what the byte-identical A/B also demonstrates. ///// NO STEP-6b TRIGGER: per-rung allocation totals x1.79 x1.88 x1.93 x1.96 x1.98, at or below x2.00 every rung and nothing near the 2.4 that would spend an optimizer agent; CPU x1.85 x1.92 x1.97 x2.01 x2.02, linear. A byte-identical A/B leaves an optimizer nothing to hunt. | 588,336 | 1,057,178 | 1,993,542 | 3,863,392 | 7,607,619 | 15,087,935 |
| 2026-07-30 | R4.2 — the rest of the file IO: `read`/`write`/`size`/`exists`/`delete`/`rename`/`stat`/`statField`/`statFree` + `openWrite`/`openWriteExecutable`, the 7 new StdOps with their 27 classification sites each, the RAII destructor, and the three `__ManagedMemory` members. Host-only, E3104 on every other target, per the R4.1 ruling. ///// ALLOCATIONS vs R4.1: +892 / +915 / +891 / +891 / +891 / +891 — FLAT, and the flatness IS the finding: the delta does not move by ONE allocation while the file count goes x8 across rungs 2-5, which refutes a per-file term outright. It is a per-COMPILE constant. `createEnumRegistry` seeds 16 more `__ManagedFileError` cases plus a whole 8-case `__ManagedMemoryError`, and it runs once per `EnumLayoutMap` CONSTRUCTION — three per compile (Queries.maxon:190, QueryDatabase.maxon:80, Project.maxon:2330) — never once per file. +891 is +0.006% of rung 5's 14.87M. ///// STEP-6b FOUND AND FIXED THE RUNG'S ENTIRE *GROWING* ALLOCATION TERM, AND IT WAS NOT WHERE THE PRE-OPTIMIZER READING PUT IT. Before the fix the delta read +2,201 / +3,544 / +6,160 / +11,440 / +22,000 / +43,120 — a term that DOUBLES per rung — and it had been attributed to the enum seeding riding a doubling file count. It was not that: the seeding is not per-file (above), and `phase:signatures` allocations are IDENTICAL before and after the fix at every rung. The growing term was `Parser.dispatchArrayMethod`, into whose TOP R4.2 hoisted `signatures.internArrayByteInstance()` so it ran for EVERY array method call in the program instead of only inside the `.managed` field arm — and each call built a fresh one-element `MaxonTypeArray` to key the instance registry with, i.e. A HEAP ALLOCATION PER ARRAY METHOD CALL. Memoized on `ProgramSignatures` (`arrayByteInstance` + a `computed` flag beside it, `TokenMemo`'s shape, because 0 is a real `GenericInstanceId`): the answer cannot change, since `GenericInstanceRegistry` only ever grows and interning is idempotent, and the FIRST ask is still the WRITE, so the registry contents and every id derived from them are exactly what they were. Removed -1,309 / -2,629 / -5,269 / -10,549 / -21,109 / -42,229 allocations — x32.3 across a x32 ladder — and ALL of it lands in `phase:parse` to the digit (rung 5: 3,096,945 -> 3,054,716 = -42,229, which is the whole-compile delta exactly). The rung's cost is now flat where it grew. ///// ONE MORE, CPU-ONLY AND SAID TO BE SO: `Parser.isManagedFileType` asked "is this a `__ManagedFile`?" by HASHING the name and probing the interner — INSERTING on a miss — on every method dispatch in the program, where its two siblings `isStringIndexType`/`isCharacterSetType` read the name behind the id (a dense-column index plus a byte compare) and mutate nothing. R4.2 widened its tag gate from `structRef` alone to `structRef` OR `named`, putting that probe on the enum and ranged-alias receivers too, one arm ABOVE the enum test whose own comment explains that it gates on the tag first precisely so the interner is not touched for a receiver that is not its business. It now uses the siblings' form. ITS ALLOCATION DELTA MEASURED EXACTLY ZERO at all six rungs — the probe was always a HIT in this corpus, so the exact column cannot see it and NO speedup is claimed; what is claimed is that a predicate no longer mutates the interner as a side effect. ///// NO SUPERLINEARITY. Per-phase ratios are at or about x2.00; `regalloc:splitting` and `liveness` are the pre-existing documented bends and are untouched — nothing this rung adds enters regalloc in this corpus, which contains no file IO at all. Read the manifest before reading anything into that: a zero from a ladder that cannot express the feature is not evidence about the feature. ///// NO CPU CLAIM ACROSS ROWS, deliberately: this tree's checkout path differs from the previous row's, and this log's standing rule is that the attributable measurement is the same-path A/B and never the cross-row delta. ///// createEnumRegistry RULING — NOT made demand-driven, and NOT filed as debt either. It is a flat per-compile 891 allocations (0.006%), measured above; the same suggestion was already REFUSED with a measurement at P1.7a slice 2b-ii, where an eager per-`Parser` layout would have turned a flat +68 into +1,430 at 143 files; and R4.1's reason stands — it is the one door reaching BOTH enum registries, and a handler resolves its `match e` through `Project.enumTypes`, so demand-driving it re-opens the two-registries-disagree wrong-answer class to buy nothing measurable. ///// Suite 2574/0, exit 0, no leak, and `specs-shv2/fragments/` empty-diff across BOTH changes, so codegen is byte-identical. | 588,939 | 1,051,112 | 1,974,155 | 3,817,327 | 7,501,547 | 14,866,199 |
<!-- scale-history:allocations -->

## Bytes

| date | change | rung 0 | rung 1 | rung 2 | rung 3 | rung 4 | rung 5 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 2026-07-13 | *(baseline)* the scaling suite is introduced | 15,018,355 | 33,313,844 | 81,458,950 | 226,863,241 | 739,103,906 | 2,859,036,397 |
| 2026-07-13 | a name is a slice of the source, not a heap String | 13,125,747 | 27,565,923 | 61,724,631 | 154,176,421 | 458,959,060 | 1,735,970,667 |
| 2026-07-13 | a byte string literal IS a ByteArray, and may be a global | 13,120,707 | 27,556,083 | 61,705,191 | 154,137,781 | 458,882,020 | 1,735,816,827 |
| 2026-07-13 | for-in over an Array is an index counter, decided at lowering | 12,113,819 | 25,521,299 | 57,502,887 | 145,153,461 | 438,545,764 | 1,685,625,083 |
| 2026-07-14 | Rebase onto the rewritten main: the ladder now sees the for-in index-counter lowering and the compiler-traces-itself work at the same time. Allocs and frees are exactly the for-in commit's; bytes are its numbers plus the constant +8/rung that origin already accepted, and the two compose with no interaction. | 12,113,827 | 25,521,307 | 57,502,895 | 145,153,469 | 438,545,772 | 1,685,625,091 |
| 2026-07-14 | try_call on an associated-value enum no longer allocates a placeholder EnumDummy: null already means absent, and scope cleanup was already null-guarded, so the dummy was allocated, increffed, decreffed and freed on every successful call without ever being read | 10,092,819 | 21,316,475 | 48,359,471 | 123,847,293 | 383,771,820 | 1,527,346,499 |
| 2026-07-14 | P1.0d block scoping: the parser now pushes and pops a real Scope per if/while body — pushScope/popScope existed, were correct, and were never called, so a let inside an if leaked to the function frame. Two Scope containers per function  | 10,036,452 | 21,194,700 | 48,086,144 | 123,187,918 | 382,008,573 | 1,522,048,404 |
| 2026-07-14 | scale-test stopped measuring its own residue: the runner wrote each rung's executable and metrics.tsv INTO the rung source directory, which loadProject enumerates before filtering to .maxon, so every run after the first compiled two extra directory entries per rung (+26 allocs, +945 bytes). Outputs now live in the corpus root and stale files are pruned, so a cold and a warm .scale-tmp now agree exactly. The COMPILER is unchanged; every earlier row in these tables was measured warm and is offset by that constant. | 10,035,504 | 21,193,752 | 48,085,196 | 123,186,970 | 382,007,625 | 1,522,047,456 |
| 2026-07-14 | elimTrivialBlockArgs re-swept the WHOLE FUNCTION to a fixpoint, and the sweep count was the length of the fold chain, so the pass was QUADRATIC: collectReadyArgs deferred any phi whose fold target was also folding that sweep, which peels exactly one link per O(function) sweep. A run of N sequential loops holding one idle var builds a chain of ~2N (loop L's header phi for the var is fed by loop L-1's EXIT phi for it), and the pass measured x3.95 allocs / x4.85 bytes / x4.23 time per DOUBLING -- folding a LINEAR 2N phis with quadratic work, which is the tell. It is now a WORKLIST: when a phi folds, only the phis it FEEDS can have changed, and a reverse (incoming -> arg) CSR names them in O(1); chains resolve through a path-compressed union-find instead of one sweep per link. On that shape at N of 128: 798,403 allocations fall to 6,090 (131x) and 107.5ms to 1.8ms (60x), growth x3.95 to x1.98 (linear), folding the identical 256 phis. (This note was TRUNCATED when written: CommandLine.optionValue cut every option value at its SECOND "=", so "N=128" ended the sentence. Repaired, and the stdlib bug fixed.) | 10,303,792 | 21,724,590 | 49,157,229 | 125,332,785 | 386,473,068 | 1,531,710,779 |
| 2026-07-14 | P1.0d.2: short-circuit and/or, word-form bitwise, char literals — plus the trivial-phi pass rewritten from an O(chain-length x IR) fixpoint (each whole-function sweep peeled exactly ONE link of the fold chain) to a worklist + path-compressed union-find whose trigger slots MIGRATE as folds re-point reads. On 128 sequential loops: 798,403 -> 6,090 allocations and x3.95 -> x1.98 growth, folding the identical 256 phis. elimTrivialBlockArgs is now x1.96 (linear). The rung itself adds a shrinking constant (+1.0% allocs at rung 0 falling to +0.4% at rung 5); short-circuit mints one phi and takes no mutable-var snapshot, so it does NOT multiply the parser's known O(V.B). | 10,302,131 | 21,718,747 | 49,144,218 | 125,302,038 | 386,393,240 | 1,531,478,357 |
| 2026-07-14 | P1.0d.3: bool/int type discipline — ONE agreement rule (TypeRules.typesAgree) now gates arithmetic, shifts, unary minus, comparisons, if/while conditions, REASSIGNMENT, return values and call arguments, where before only and/or/xor had one. So `4 + flag` compiled to 5, `4 * flag` to 4, `flag shl 4` to 16, `takeInt(flag)` put a bool in an int parameter, and `if 4` branched on nonzero — wrong ANSWERS, not crashes. Memory moved and is attributed EXACTLY, at all six rungs: bytes = 16 x functionCount + 56. The 16/function is IrFunction gaining ONE pointer field (`valueTags`, the parser's per-value type column, PUBLISHED rather than dropped so SemanticCheck can check a call argument against a callee that may be in another file), which rounds the object up one 16-byte slab size class. The flat +56 bytes / +2 allocs is the entry stub's empty ValueTypeTagArray (header + buffer) in createFromStd, once per program. The per-value TAG DATA COSTS NOTHING: the parser already allocated and filled exactly that array per function and threw it away at each function's end; it is now handed to IrFunction.create by reference. Allocations are FLAT (+2) across all six rungs — the type oracle does not scale with the program. | 10,302,667 | 21,719,667 | 49,145,906 | 125,305,262 | 386,399,536 | 1,531,490,797 |
| 2026-07-14 | The splitter's driver is INCREMENTAL: it re-derives only the dirty region a split touched, where it used to rebuild liveness and re-sweep the whole function's pressure after EVERY split (O(function x splits)). Loop i's accumulators are live only inside loop i, so a spill in one loop cannot change the pressure in any other -- the old driver re-read all 64 loops after every one of ~320 splits anyway. regalloc:liveness grows x2.04 per doubling where it grew x3.69; regalloc allocations x2.00; the whole compile x2.02 allocs and x2.07 bytes. LINEAR. Enabled by block-relative op sequence numbers, per-block editable live sets and an editable use index; the byte-exact specs-shv2 goldens do not move, so every decision is identical. THE BYTE COLUMN CAUGHT WHAT THE ALLOC COLUMN COULD NOT: the first cut grew value-indexed columns with Array.resize, which reserves EXACTLY the length asked, so every split reallocated and copied all eleven of them in full -- 608 MB in splitting alone, growing x6.75 per doubling, at a low and innocent-looking allocation COUNT. They double now: 865 MB -> 272 MB total | 8,152,479 | 16,115,172 | 32,157,752 | 64,640,069 | 131,399,777 | 272,094,490 |
| 2026-07-14 | Flags metadata unified: StdOp.const now declares clobbersFlags:FALSE (it lowers only to movRegImm32/movRegImm -- a bare `mov`, which writes no EFLAGS), and TargetOpMeta.setsFlags is DELETED (written 40x, read 0x, and it CONTRADICTED the Std tier -- a v1 port artifact: v1 fuses compare/branch at the MIR->X64 boundary and really does read setsFlags off a target op, but shv2 moved that scan up to the Std tier and kept the field anyway). The false clobbersFlags broke compare/branch fusion on `if a > 0 and b > 0`, because a short-circuit's seed literal sits between the compare and its branch: the backward scan stopped at the `mov` and refused to fuse. Fixed, the fusion crosses it and each short-circuit site drops FOUR instructions -- setcc, a redundant cmp, AND the phi copy (the seed is now minted straight into the phi's register), plus one fewer live register. MEMORY MOVED BY EXACTLY ZERO, ON EVERY RUNG, AND THAT IS THE POINT WORTH RECORDING: TargetOpMeta is per-VARIANT static metadata, never heap, so dropping a field from it costs nothing -- but the deeper reason is that ScaleCorpus CONTAINS NO `and`/`or` AT ALL, so a codegen change to the commonest conditional shape in the language is entirely INVISIBLE to this instrument. A second instance of the corpus-blindness recorded as OPEN.md #5 (whose known case is O(V.B) SSA hidden by longFunction mutating ONE accumulator). The corpus must grow short-circuit conditions. | 8,152,479 | 16,115,172 | 32,157,752 | 64,640,069 | 131,399,777 | 272,094,490 |
| 2026-07-14 | CORPUS CHANGED -- ⚠ THE DELTA COLUMN ON THIS ROW IS MEANINGLESS. The +26% is a BIGGER CORPUS, not a slower compiler; this row is the NEW BASELINE and nothing above it is comparable. ScaleCorpus was BLIND in the one dimension that mattered. The parser builds SSA on the fly at a cost of O(vars-in-scope) per branch -- each `if` SNAPSHOTS the whole mutable-var set, each `while` MINTS A PHI PER VAR IN SCOPE -- so the true cost is O(V x B). But `longFunction` and `deepBlocks` each declared ONE accumulator and mutated it in every branch: V=1, O(V x B) collapsed to O(B), and the knobs NAMED for growing branches could not see the cost of growing branches. Per user directive, FIX THE CORPUS'S REALISM, NOT THE PROBE: both knobs now carry SIX mutable locals (a fixed, realistic count -- real functions carry a handful) and each branch mutates a ROTATING SUBSET, never all of them, because a branch that writes every local makes "cost O(vars in scope)" and "cost O(vars assigned)" the same number and the corpus cannot tell apart the two things that decide whether a write-trail is worth building. deepBlocks' two arms now write DIFFERENT locals, so the merge does real work instead of the trivial one-phi case. Also: every guard is now a SHORT-CIRCUIT `and`, because the corpus contained `and`/`or` EXACTLY NOWHERE -- found the hard way when the `const` flags fix moved zero memory. WHAT IT NOW SEES, and it is the defect OPEN.md #5 predicted: pruneDeadBlockArgs ALLOCATIONS grow x1.98 x1.99 x1.995 x1.997 x1.999 -- dead linear -- while its BYTES grow x2.00 x2.02 x2.04 x2.09 x2.17. Allocation COUNT linear while BYTES bend means the allocations are getting BIGGER: the dense columns sized by `blockArgIdBound`, which is superlinear because a ValueId is minted for every phi the parser mints, INCLUDING the ones later pruned. phase:parse bends the same way (x1.97 -> x2.06). THE BYTE COLUMN CAUGHT WHAT THE ALLOC COLUMN COULD NOT, again. The write-trail fix lands next, against this baseline. specs-shv2 239/0. | 10,854,449 | 21,484,258 | 42,861,050 | 86,011,855 | 174,108,507 | 357,477,140 |
| 2026-07-14 | Loop phis: a loop now carries a phi for the vars its extent ASSIGNS, not every var in scope. `parseWhileStatement` minted a header phi for EVERY mutable var in scope, unconditionally, before the body was parsed -- so it could not know which vars the body touches. A function's `var`s accumulate, so the Nth loop minted ~N phis and burned a ValueId on each, INCLUDING every one pruneDeadBlockArgs later deleted. blockArgIdBound (and every dense column sized by it) is O(ValueIds), so phis nobody kept were paid for in BYTES by every pass in between. THAT is why the ALLOC column stayed dead linear while the BYTE column bent: the allocations were not more numerous, they were BIGGER. Fixed by reading the loop's assigned names off the TOKENS up front (namesAssignedIn / loopBodyEndIndex). ⚠ THE DESIGN TRAP, RECORDED BECAUSE I PROPOSED IT AND IT IS UNSOUND: minting the phi LAZILY on a var's first mention CANNOT WORK -- parseIfStatement snapshots the mutable-var set into local ValueId arrays on the Maxon CALL STACK, and a phi minted lazily after such a snapshot retroactively changes the value that was live where it was taken, with nothing able to reach in and patch it (`while c / if d / x = 2` then returns 1, not 2). EAGER minting is what makes those snapshots true, and once minting is eager the criterion is ASSIGNMENTS, not mentions -- a var the loop never assigns keeps its pre-loop value, whose definition DOMINATES the header, so a read binds it correctly with no phi at all. GOLDENS DID NOT MOVE, and that is the proof the whole cost was intermediate: pruneDeadBlockArgs and elimTrivialBlockArgs were already deleting precisely this surplus, so the emitted code was always identical -- the ValueIds were minted, carried through four passes inflating every dense column, and then thrown away. Eight sequential pressured loops minted 332 header phis of which 260 were surplus; they now mint exactly 72. specs-shv2 242/0, no E5001 (fewer dead phis means LOWER maxlive -- this is the anti-E5001 direction). | 10,839,733 | 21,433,935 | 42,654,355 | 85,264,817 | 171,032,115 | 344,648,680 |
| 2026-07-14 | CROSS-FILE SIGNATURE INDEX -- and it is a NET REDUCTION, because a whole-program query placed UNDER a per-file one turned two latent QUADRATICS into visible ones. (Re-measured after rebasing onto the loop-phi change; the two fixes are in the QUERY SPINE and that one is in the parser's SSA construction, so they compose -- the rung-5 alloc delta is -91,209 on both bases, bit for bit.) WHAT WAS ADDED: `queryProgramSignatures`, one token sweep of every file folded into one table, so a call's result type is exact across files (it was `unresolved`, which AGREES WITH EVERYTHING, so `isReady()` from another file added to 41 and returned 42). Its own phase, LINEAR: allocs x1.91, bytes x1.93. WHAT IT EXPOSED, and neither was mine: (1) `clearDepsFor` rebuilt a FLAT EDGE ARRAY THAT NOTHING EVER READ -- it scanned every dependency edge in the project and rendered each edge's dependent to a fresh String just to compare it -- so one clear per file over a graph with a few edges per file allocated O(files^2) Strings. The flat array and the reverse index were the same fact written down twice, and only the index was read. Deleted the array; `clearDepsFor` is now O(1). (2) `compositeSourceHash` re-folded EVERY file's hash on EVERY call, and every `queryParseOps` must now validate the signature index before it can validate itself, so it went from 2 calls to files+2 -- O(files^2). Memoized, with `fileChanged` as its single invalidator. HOW THEY WERE FOUND: the first run's DELTA COLUMN ITSELF grew x1.66 x2.07 x2.56 x3.04 x3.43 on a ladder that DOUBLES. A linear change cannot make a delta grow x3.4, so the thing it perturbed was not linear. NET at rung 5: allocs -91,209, bytes -5,091,010. Ratios x1.96..x1.99 allocs, x1.97..x1.99 bytes -- linear. Rung 0 is +675 allocs: the new phase's fixed cost on the smallest program, which the quadratics are too small to offset there. THE STANDING RULE THIS LEAVES: a whole-program query placed under a per-file one is asked ONCE PER FILE, so anything O(files) inside it is O(files^2) -- multiply by the file count before adding work to one. ALSO FOUND, a BOOTSTRAP bug (OPEN.md #14): a ternary `a if c else b` whose arms mix a BORROWED field read with a freshly-OWNED value leaks on every build (exit 101). specs-shv2 242/0 -> 262/0 (+20: 7 cross-file-signatures, 8 void-call-result, 5 ported export-keyword -- 4 of them the suite's first MULTI-FILE cases). | 10,846,649 | 21,425,767 | 42,590,101 | 84,966,699 | 169,782,229 | 339,557,670 |
| 2026-07-14 | SHIFTS: Go's rule -- the count is NOT masked, shr is ARITHMETIC, only a NEGATIVE count is an error. WHAT MOVED, and it is the ONLY thing that moved: +0.56% bytes / +0.05% allocs, LINEAR (bytes delta ratios x1.97 x1.98 x1.99 x2.00 x2.00 on a DOUBLING ladder; rung5/rung0 = 31x on a 32x ladder). WHY: the parser grew a real CONSTANT FOLDER, and it is TWO DENSE PER-VALUE COLUMNS beside valueTags -- valueConsts (ParsedInt, 8 bytes) + valueConstKnown (bool, 1 byte) = 9 bytes per SSA value, pushed in lockstep by mintValue and re-created per FUNCTION (so the cost is bounded by the largest function, not the module). It exists because the shift rule is the one rule in the language that must ask WHAT IS this operand, not WHAT TYPE is it: a negative count is E2054 and a count of 64-or-more names a different shift than the hardware performs. WHAT DID NOT MOVE, and this is the point: the GUARD. A shift by a count the compiler cannot see now emits 6-7 ops (an and/neg/or/sar mask, then a select or a count-clamp), and the corpus has NO dynamic shifts -- so the guard contributes exactly zero here, and a CONSTANT count in 0..63 (every x shr 8, every x shl 3) is folded and pays nothing at all. The curve therefore stayed clean: the delta is 9 bytes x values and nothing else, which is why it doubles when the ladder doubles. specs 2950/0 -> 2957/0 (+7), specs-shv2 268/0 -> 274/0 (+6). | 10,908,246 | 21,546,940 | 42,830,362 | 85,445,136 | 170,737,018 | 341,465,163 |
| 2026-07-14 | SHIFTS FIX (fold vs codegen + shv2 signedness): the parser's whole-program declaration index (SignatureIndex/ProgramSignatures) now also carries every RANGED TYPEALIAS, so a shift can ask its left operand's declared range for signedness across files -- u64.max shr 60 is now logical (15) in shv2 as in the bootstrap. WHAT MOVED, and it is the ONLY thing that moved: a small LINEAR allocation delta (+38 allocs/+976 bytes at rung0, DOUBLING per rung: +46/+62/+94/+158/+286 allocs -- x2 on a x2 ladder, so linear). WHY: one RangedTypeAliasMap upsert + hash-mix per typealias DECLARATION, plus a per-file RangedTypeAliasArray in the token sweep. The corpus opens most files with a typealias, so the count scales with program size and doubles when the ladder doubles -- which is exactly the shape seen. Every phase stayed x1.99-x2.00. The x64 emitter gained a third right shift (shrLogical -> SHR alongside shr -> SAR); zero corpus effect (no unsigned-ranged shifts in the corpus). specs 2957/0 -> 2964/0 (+7), specs-shv2 274/0 -> 279/0 (+5). | 10,909,222 | 21,548,108 | 42,831,914 | 85,447,456 | 170,740,874 | 341,472,091 |
| 2026-07-15 | CORPUS INSTRUMENT CHANGE (not a compiler change): the corpus gained TWO doubling axes and shift coverage, and now DECLARES its own coverage and blind spots every run (printCorpusManifest). So the large delta vs the row above is the CORPUS growing, NOT a compiler regression -- rows before and after this one are NOT comparable because they measure different programs. NEW: (1) LocalsPerFunction is now a DOUBLING knob (base 128, ~4096 locals at rung5) carried by longFunction and deepBlocks -- it measures the parser per-branch O(vars-in-scope) cost; longFunction/deepBlocks were reshaped E5001-safe (a bounded 6-var working set the branches MUTATE, plus V scope-fillers each folded on declaration so they stay IN SCOPE for the parser snapshot but live for ~1 instruction, so the machine never spills -- the old sum-all-V shape tripped E5001 at V>13 and could never have carried a doubling V). (2) a nested-loop DEPTH knob (base 2, depth 64 at rung5), all loops sharing ONE accumulator so peak pressure is 1 at any depth. (3) fixed shift COVERAGE: const and dynamic counts over signed and ranged int(0 to u64.max) operands -- the shape that once hid const=0 vs dyn=-4294967296. WHAT THE V DIMENSION NOW SHOWS: phase:parse ALLOCATIONS x1.99 LINEAR -- the deleted ParseExpression O(V) HashSet snapshot regression guard is GREEN, the fix confirmed; phase:parse BYTES x3.91 QUADRATIC -- the if-half mutable-var-set copies, O(V) per if times O(ifs) ifs, invisible until now because V was a fixed 6. That is a KNOWN, deliberately-unfixed cost (a surplus copy minting no ValueId; OPEN.md 5), surfaced not manufactured; flattening it is a separate rung. All regalloc rows x1.99-x2.03 -- the E5001-safe shape holds under V=4096. The nested-loop knob reads ~LINEAR in memory: its O(depth x tokens) loop-phi token pre-scan is TIME only and INVISIBLE to this memory-only instrument BY DESIGN -- the manifest names it as the worked example of that blind spot. I did NOT touch the compiler or the instrument to flatten any curve. specs-shv2 279/0. | 16,266,840 | 38,571,110 | 102,618,751 | 307,007,559 | 1,033,076,297 | 3,743,676,074 |
| 2026-07-15 | THREE CHANGES rebased onto origin's 2026-07-15 corpus-instrument-change baseline (Go-growth array policy, raw-layer alloc counting, StringBuilder + exact String.append). Measured on origin's NEW bigger corpus (doubling V dimension + nested loops + shift coverage), so these are not comparable to my earlier rows on the smaller corpus. ⚠ ALLOC COLUMN: change (2) makes `allocs` count the RAW allocator layer (array/string buffers) for the first time, so THIS ROW AND BELOW count raw+tracked and every row ABOVE is tracked-only — NOT comparable. The +46,702..+1,505,664 alloc delta is dominated by that (~1.2M of the +1.5M at rung 5 is raw allocations that were always happening, now visible); frees stay exact (Dtotal-Dlive per layer). BYTES moved both directions and I have NOT fully decomposed it: +1.7%/+3.2%/+1.0% at rungs 0-2, then -12.5%/-6.5%/-8.2% at rungs 3-5 (net -308 MB at rung 5). The rise at small rungs is the Go-easing churn on the many arrays under the 256-element threshold (which still double); the FALL at large rungs is consistent with the growFilled column fixes (ReachCache went Theta(rows^2 x blocks) -> Theta(rows x blocks); ReuseCopyPlan likewise) becoming measurable for the FIRST time on origin's much larger corpus, plus the RegNumColumn narrowing — the old corpus could not exercise those paths (they read 0 delta then). I am flagging this as attributed-by-suspect, not proven per-byte. WHAT IS PROVEN: no phase ratio shows a NEW quadratic from my changes. pruneDeadBlockArgs x1.99 allocs, regalloc:splitting x2.01 allocs / x2.11 bytes, regalloc:liveness x1.99 — all linear (x2.1 bytes is the growth-policy amplification bend, not a quadratic). The one superlinear phase, phase:parse BYTES x3.90, is ORIGIN's own deliberately-unfixed if-snapshot quadratic (its 2026-07-15 row documents it), unchanged by me. StringBuilder does not move the ladder (string building is not in a measured compile). Also fixed a contradiction origin shipped: TWO ScaleCorpus comments still called the pressured-loops knob KNOWN SUPERLINEAR in the splitter, stale since 49b160ce2 made the driver incremental (regalloc:splitting/liveness both ~x2.0). specs-shv2 279/279, csharp 2974/2974, no leaks. | 16,550,522 | 39,833,112 | 103,693,025 | 272,874,382 | 969,967,318 | 3,435,675,423 |
| 2026-07-15 | Deleted the query spine's WRITE-ONLY dependency graph (depIndex / recordDependency / clearDepsFor / activeQueryStack / QueryKey / queryKeyString). Nothing ever read a bucket: shv2 validates a memo by a content-hash compare whose inputs are named in parseMemoKey, so a dependency is DERIVED from the input, never recorded as it is read. It was kept for the day a query depended on another file's DERIVED state — that day came on 2026-07-14 (queryParseOps -> queryProgramSignatures) and was answered by mixing the index's hash into the parse memo key (232f6c80a); nothing consulted the graph, which that same commit extended with an edge for a consumer that did not exist. ALLOCATIONS FALL AT EVERY RUNG and the SAVING ITSELF IS LINEAR: -464 / -596 / -874 / -1,404 / -2,474 / -4,600, a delta ratio climbing 1.28 -> 1.47 -> 1.61 -> 1.76 -> 1.86 toward x2.00 on a ladder that doubles — exactly the shape of per-query bookkeeping billed once per query, which is what it was. Bytes fall the same way (-16,016 .. -167,130). Removed per edge: one queryKeyString String render plus a Map get/upsert; per clearDepsFor, a Map contains/remove. NO phase changed shape — this is a pure deletion of hot-path work, so the counts can only go down, and every rung did. The one superlinear phase, phase:parse BYTES, is origin's own documented if-snapshot quadratic and is untouched. specs-shv2 279/279, csharp 2974/2974, mcp 30/30. | 16,534,506 | 39,813,560 | 103,661,315 | 272,826,912 | 969,879,944 | 3,435,508,293 |
| 2026-07-15 | Parser if-snapshot BYTE QUADRATIC fix, re-measured after rebasing onto origin (which since my last row absorbed my earlier work, the closures feature, and the query-spine write-only-graph deletion — the row directly above this one). The fix itself is unchanged from when it first landed; only the baseline moved, so this row states its effect against origin's current tip. parseIfStatement snapshotted the boundValue of EVERY mutable var in scope (V) three times per if and merged over all V — Theta(V x branches) BYTES, which the corpus's doubling V-dimension exposed as phase:parse bytes x3.90 (quadratic) while allocs stayed x1.99. An if now carries only the vars its TOKEN EXTENT ASSIGNS (mirror of the loop-phi fix 11b9bbcba): namesAssignedIn over the whole then+else+else-if extent found by blockStatementEndIndex, filtered against mutableVars IN DECLARATION ORDER (load-bearing — the differing vars are always a subset of the assigned set, so the minted phi sequence is exactly the old whole-list merge's, which is why goldens do not move). Guarded by assertScanAligned: a too-short extent PANICS rather than silently dropping a phi. RESULT vs the query-spine row above: phase:parse bytes x3.90 -> ~x2.03 (linear); rung-5 total bytes fall from ~3.4 GB to ~490 MB (about -86%), the if-snapshot quadratic was essentially the entire memory profile at scale. Allocs fall too (the surplus snapshot arrays are gone). specs-shv2 279/279 BYTE-IDENTICAL (no SSA decision moved — pruneDeadBlockArgs already deleted exactly the surplus phis this now never mints), csharp 2983/2983, no leaks. Also in these commits: the scanner that finds the if extent is unified with the loop's (one blockStatementEndIndex, not a near-copy), and the corpus comment that still called this quadratic deliberately-unfixed now points at the fix. | 13,568,706 | 27,327,792 | 57,390,011 | 117,205,336 | 239,922,624 | 490,383,485 |
| 2026-07-15 | P1.0d.3 - the emitted panic runtime + frame pointers on EVERY function. A divide by zero was a raw 0xC0000094 hardware trap escaping with empty stderr; it is now exit 1 plus "panic: integer divide by zero" plus a symbolized backtrace, caught by a VEH thunk. The movement is LINEAR IN FUNCTION COUNT and fully attributed: allocsDelta = 786 + 6.68 x functions (rung 5 predicts 5986 vs 5994 measured, error <= 3.7 against real function counts). ATTRIBUTION, corrected by the optimizer against the implementer's first reading: 74 percent of the delta is phase:prologueEpilogue - the frame pointers, one push rbp + mov rbp,rsp per function and one pop rbp per return site - at a DEAD-FLAT 5.64-5.66 allocs per function across a 21.7x span. It is NOT __symtable, which costs ~0 allocations: it appends into geometrically-growing ByteArrays, so its alloc COUNT is O(log n) and its cost is bytes only. The constant is 677 in phase:encode and it is EXACTLY FLAT - 677/678/677/677/677/677 across a 32x program spanning 12 to 136 files - which MEASURES rather than assumes that the seven runtime chunks are built once per compile, not once per file (the whole-program-query-under-a-per-file-one trap; under that bug rung 5 would read 677 x 136 = 92072). The byte-delta non-monotonicity at rungs 0-2 (119861 at rung 2 < 132730 at rung 1) is phase:link ALONE - the difference of two capacity-class-rounded PE image buffers, i.e. a difference of quantized linear quantities, not smooth; excluding link the byte delta is perfectly monotonic (1.40, 1.52, 1.71, 1.81, 1.92) and link's ABSOLUTE bytes are x2.01. NOTHING BENDS. At rung 5 the whole rung costs +0.07 percent of allocations and +0.15 percent of bytes. NOT CACHED, deliberately: the seven runtime chunks are rebuilt from identical bytes each compile, but concatX64FunctionChunks consumes and rebases each chunk's fixups, so a cached chunk needs a per-compile deep clone costing about what building it costs - the cache would save approximately nothing and add a shared-mutable hazard under 12 workers. BLIND SPOT, recorded so the next reader knows: the generated corpus contains no divide or mod at all, so this instrument structurally CANNOT see the divide path. That is sound only because the design emits NO divisor test (the CPU traps for free), so a divide costs exactly what it did before. | 13,650,183 | 27,460,522 | 57,509,872 | 117,409,116 | 240,294,856 | 491,107,617 |
| 2026-07-15 | P1.0d.5a - top-level `let`: a module-scope constant is a compile-time NUMBER that inlines as an ordinary literal at each use. No IR op, no .data, no relocation, no backend change - and zero pre-existing goldens moved, which is the proof: `return ANSWER` compiles to exactly what `return 42` compiles to. Suite 281 to 295. The delta is EXACT and LINEAR IN FILES: phase:signatures allocs = 2 x files + 1, hitting 25/33/49/81/145/273 against 12/16/24/40/72/136 files on all six rungs, and bytes = 48 x files + 672, also exact on all six. phase:parse is EXACTLY UNCHANGED. Rung 5: +0.0035 percent allocations, +0.0015 percent bytes. The named O(files squared) hazard - a whole-program query asked once per file - did NOT fire. ⚠ READ THIS BEFORE TRUSTING THE NUMBER ABOVE: IT MEASURES THE FEATURE INERT. The generated corpus contains NO top-level `let` AT ALL (verified by emitting it: zero column-0 `let`, 56 body-local ones), so the constant arena is EMPTY on every rung and the DFS, the lookup and mixConstant never run. `2 x files + 1` is the signature of an empty arena: it proves the per-file SWEEP is linear and says NOTHING about the evaluator. This is the THIRD blind spot of this kind - the corpus also contained no `and`/`or` at all (which is why the const-flags fix, four instructions off every short-circuit site in the language, read as zero movement on every rung), and it generates no `/` or `mod` (so it cannot see the divide path the panic runtime was built for). The pattern is now explicit: THE CORPUS IS SYSTEMATICALLY BLIND TO THE FEATURE JUST LANDED, because it was grown to expose the last bug rather than the next one. So the evaluator was measured OFF-INSTRUMENT instead, on private ladders: 400 to 12,800 constants gives x2.00 allocations and x2.00 bytes per doubling, flat across a 32x span (the memo holds); and 8 to 128 files x 50 constants each with cross-file `export let` - the named hazard in its live shape - gives x2.00, where the once-per-file bug would read x4.00. Refuted by measurement, not by argument. | 13,651,431 | 27,461,962 | 57,511,696 | 117,411,708 | 240,298,984 | 491,114,817 |
| 2026-07-15 | P1.0d.5b - top-level `var`: a module-scope global with a REAL .data slot. New Std MEMORY band (globalAddr + loadIndirect + storeIndirect, v1's shape), DataSectionEntry in GlobalDataTable, a .data section in PeWriter, the dataSectionRipRelDisp32 arm. Initializers are constant-only, so they are const-evaluated into .data BYTES and there is NO __module_init. Suite 295 to 317. The delta is EXACTLY LINEAR IN FILES on all six rungs, zero residual, in BOTH columns: allocs = 66 x files - 273, bytes = 14932 x files - 109760. At rung 5 that is +0.10 percent allocations and +0.39 percent bytes. The named O(files squared) hazard did NOT fire - declaredGlobals() is called ONCE from compileToCodeResult, structurally outside the per-file query (and measured x1.98 across 3200 files). ⚠⚠ READ THIS BEFORE TRUSTING THE FIT: IT MEASURES THE FEATURE INERT, FOR THE FOURTH TIME. The generated corpus contains NO top-level `var` at all (verified by emitting it: 0 across 300 files), so layOut gets an empty array. The prior three: no `and`/`or` (which made the const-flags fix - four instructions off every short-circuit site in the language - read as zero movement), no `/` or `mod` (hiding the divide path the panic runtime exists for), and no top-level `let`. THE CORPUS IS GROWN TO EXPOSE THE LAST BUG, SO IT IS BLIND TO THE NEXT ONE. ⭐ AND THIS RUNG PROVES A SECOND, DEEPER BLIND SPOT THAT IS BY DESIGN: the optimizer found a REAL quadratic here - sortBySizeDescendingStable was Theta(globals squared), because storageBytes is only {1,2,4,8} so an all-int program never stopped the insertion scan early. Fixed to four appends-in-order, O(n), with a count invariant so a future 16-byte type panics rather than silently dropping a slot. Measured off-instrument at 8000 globals: 619ms to 166ms whole-compile; the unattributed bucket (layOut bills outside every PhaseProbe) went 30.2/120.3/460.3ms at n=2000/4000/8000 - x3.98, x3.83 - to 2.0/4.1/9.0ms. ⚠ ALLOCATIONS AND BYTES WERE EXACTLY x2.00 BOTH BEFORE AND AFTER: this was a TIME-ONLY superlinearity, and scale-test collects memory only, deliberately, because time is not trendable across machines. So a time-only quadratic is STRUCTURALLY INVISIBLE HERE and always will be. That is not a defect in the instrument - it is the price of the property that makes it trustworthy - but it is a limit to state out loud rather than rediscover. Its own comment had defended the cost as "a program's top-level var count is a handful": the same appeal to an empirical corpus property that v1's aliasing bug rests on three files away. Not fixed, and flagged honestly: layOut's remaining cost is dataIndex.upsert (String-keyed Map), which dominates what is left but swings 742 to 4206 ns per insert NON-MONOTONICALLY, and n=128,000 is cheaper per insert than n=16,000 - no algorithm is sublinear in n for n inserts, so it is allocator/heap-state dominated, not a complexity class. It is stdlib, pre-existing, and shared with every String-keyed Map in the compiler. Rewriting it on a curve nobody can explain, inside a rung commit, would be optimizing the gauge. | 13,720,855 | 27,591,114 | 57,760,304 | 117,899,228 | 241,264,328 | 493,035,809 |
| 2026-07-15 | Envelope-collapse / byte-fusion / static-literal series (the 10 commits 4c20796b1..739bf7a0f), measured against origin's prior tip. All rungs drop sharply — rung-5 allocs 8.47M->6.56M (-1.91M, ~-23%), bytes 490MB->482MB per-rung with the reduction concentrated in per-object overhead — because a managed value stopped being TWO heap objects (wrapper record + separate __ManagedMemory) and became ONE: a String/Character/Array now IS its own __ManagedMemory (buffer@0..parent@32 inline), an owned small buffer is byte-fused INLINE in the record's own allocation (parent_ptr=MmParentInline, first grow detaches), and a never-mutated string/byte/char/array literal proved by LiteralCoverageAnalysisPass is emitted ONCE as a shared immortal .data record (refcount=2^62, incref/decref no-op) at zero per-evaluation allocation. Growth stays LINEAR: every phase x1.98-x2.11 per doubling rung, no new quadratic; the allocation drop is a constant-factor-per-object win, uniform across rungs. specs-shv2 279/279, csharp 3002/3002, no leaks. This review's own edits are allocation-neutral (EmitStaticRecordUserPtr dedup is codegen-neutral: 0 fragment churn; mm_decref immortal-guard reorder changes no alloc/free counts), so these numbers are the committed series' effect, not the review's. | 13,295,926 | 26,785,418 | 56,308,973 | 115,043,725 | 235,590,505 | 481,709,174 |
| 2026-07-15 | Envelope series RE-MEASURED on the post-rebase tip (the 12 envelope/review commits replayed onto origin's cde9b8946, which added top-level let/var globals, floats groundwork, the emitted panic runtime and per-function frame pointers). Confirms the envelope-collapse win holds against origin's CURRENT tip and corpus: rung-5 allocs vs origin's P1.0d.5b row (8,488,038) fall to 6,572,769 (-1.92M, ~-23%), the same drop the pre-rebase row reported against the if-snapshot baseline — the two baselines are the same corpus and origin's P1.0d work was ~memory-neutral (8.47M->8.49M), so the row is comparable, not a corpus artifact. Delta vs the pre-rebase envelope row directly above is small and FULLY attributed to origin's work now present, NOT the envelope code (which is byte-for-byte the same): +10,651 allocs / +2.6MB at rung 5 (+0.16% / +0.54%), of which phase:prologueEpilogue alone is 2,848->7,266 allocs — the per-function frame pointers from origin's P1.0d.3 panic-runtime commit, absent from the pre-rebase measurement. Growth stays LINEAR everywhere (phase:regalloc x2.05, isel x2.01, parse x1.99; no phase bends). specs-shv2 317/317, csharp 3002/3002, no leaks, 0 fragment churn on the combined tree. | 13,444,571 | 27,044,244 | 56,672,786 | 115,727,169 | 236,913,825 | 484,327,242 |
| 2026-07-15 | P1.0d.4 x64 SSE BACKEND (half a rung — the parser/allocator land beside it). The SSE band's encoders + the float lowering: addsd/subsd/mulsd/divsd, ucomisd, movsd (reg/mem/rip), cvtsi2sd, cvttsd2si, the .rdata f64 constant pool, and the float-equality two-block split. Suite 317/19 UNCHANGED and the FRAGMENT DIFF IS EMPTY — every existing byte of codegen is identical, which is the proof that regNumber's low-nibble mask (rax..r15 and xmm0..xmm15 share an encoding space) and the class-dispatched movRegReg/storeSlotReg/loadRegSlot/xchgRegReg are behaviour-preserving for the GPR paths. DELTA IS LINEAR AND FITS EXACTLY: allocs = 552 x 2^rung + 370 across all six rungs, zero residual, and it is 100 PERCENT IN phase:isel — every other phase, regalloc included, is bit-identical to the P1.0d.5b baseline. At rung 5 that is +0.20 percent allocations. It is the per-function containers ISel now builds (the fused-compare float column, the ordered-block map, the split candidate list); the +370 constant is per-compile. ⭐ THE FIRST CUT WAS TWICE THAT (+1,528 at rung 0, allocs = 1528/2440/4240...) AND THE INSTRUMENT IS WHAT CAUGHT IT: the fused-compare second column was a `Map with (ValueId, StdType)` storing a full operand type per compare — ~4.7 allocations each — when BOTH readers immediately project it through stdTypeIsFloat and nobody asks f32-vs-f64. Replaced with a dense bool column (StdValueUseSet) holding the question actually asked, "is this fused compare a float one": an all-integer function now inserts NOTHING and the backing array is never allocated, so the cost went from proportional-to-compares to zero for programs with no floats. A column nobody reads is not free. ⚠⚠ AND FOR THE FIFTH CONSECUTIVE TIME, THE CORPUS MEASURES THE FEATURE INERT — this time the manifest says so in its own words: "NOT GENERATED ... float arithmetic". Verified by emitting it: ZERO float literals across rung 0's 12 files. So NOT ONE of the ~700 lines of SSE lowering and encoding this rung adds is executed by any number above; the delta is purely what ISel now builds for every function whether or not it ever sees a float. The prior four (no and/or, no div/mod, no top-level var, no top-level let) are recorded in the row above; the pattern is now the rule and not the exception — THE CORPUS IS GROWN TO EXPOSE THE LAST BUG, SO IT IS BLIND TO THE NEXT ONE. What DID verify the emitted bytes is off-instrument: all 18 SSE encodings were diffed against llvm-mc and match byte-for-byte, including the F2-before-REX ordering (F2 41 0F 58 DB for addsd xmm3/xmm11), REX.R vs REX.B on extended XMMs, the no-REX case, and cvtsi2sd xmm11, r9 = F2 4D 0F 2A D9 which proves one low-nibble mask encodes a GPR and an XMM correctly in the same instruction. | 13,792,711 | 27,722,010 | 58,009,280 | 118,384,364 | 242,221,784 | 494,937,905 |
| 2026-07-15 | P1.0d.4 FLOATS — THE FIRST ROW IN THIS LOG THAT MEASURES A FLOAT AT ALL, so it is a NEW BASELINE and NOT a delta: the CORPUS changed, not only the compiler. ScaleCorpus generated ZERO float instructions until today — the row above says so in its own words ("NOT GENERATED ... float arithmetic"; "ZERO float literals across rung 0's 12 files") — and it was the FIFTH consecutive rung the ladder was blind to what landed. Two new knobs, DOUBLING like every other: floatOps (float + - * /, compares in BOTH forms — feeding a jcc, and materialized as a bool in a GPR — branch-merged float PHIs, int->float promotion in, trunc out) and floatSpill (N loops holding 4 floats live ACROSS a call: every XMM is caller-saved in the Wave-1 pool, so the allowed set is EXACTLY empty and every one is a FORCED spill, the most expensive thing a float does on this target). The int knobs' generated text is byte-identical, so the columns above stay readable downwards. Floats are now 22.9 percent of allocations at rung 5. ⭐ THE +2,379,450 AT RUNG 5 DECOMPOSES EXACTLY, AND NONE OF IT IS A REGRESSION: -108,488 is this rung's own COMMITTED work (measured against a pristine HEAD build — the row above predates HEAD by 15 commits, so its delta was never this rung's), and +2,487,938 is the float dimension the ladder now compiles for the first time. -108,488 + 2,487,938 = +2,379,450, and the identity holds at ALL SIX rungs. ⭐ THE FLOAT RUNG IS LINEAR — a MEASURED null result, not an assumption. The ladder spans 32x, so a linear phase grows exactly 32x across it and a quadratic 1024x. EVERY float column lands at or below 32.01x: total 30.40, regalloc 30.80, parse 30.13, lex 31.05, isel 30.74, coloring 31.51, rewrite 31.42, liveness 29.51, lowerMaxonToStd 31.43, ssaDestruction 31.94, splitting 32.01. The highest IS the forced-spill path (regalloc:splitting at 32.01x); its rung-over-rung ratios read x2.03 at the top ONLY because it starts with a small deficit that the span test dissolves. --per-type agrees at type AND scope granularity — nothing above x2.00 (realloc x1.92, OpIndexArray.create x1.98, IrBlockArgArray.create x1.99). NOTHING WAS OPTIMIZED BECAUSE NOTHING WAS SUPERLINEAR. ⭐⭐ WHAT THE BLINDNESS WAS HIDING: A FLOAT PHI PANICKED THE x64 EMITTER. `var f = 0.0` mutated in a loop or a branch — the commonest float idiom there is — died with "a register-to-register move from xmm0 to rax crosses register files". Parser.mintPhi hardcoded EVERY phi's argType to i64, which was harmless for exactly as long as every value lived in a GPR; P1.0d.4 gave that field its FIRST CONSUMER (regClassForStdType(arg.argType) picks a phi's REGISTER FILE), so a float phi claimed i64, was coloured into a GPR, and SSA destruction emitted `mov rax, xmm0`. The comment above mintPhi PREDICTED THIS RUNG BY NAME — "the scaffold the float (XMM) register class will need at P1.0d.4, and that is when it acquires a consumer and a reason to be right" — and the rung gave it the consumer without the reason. The suite could not see it either: float-compare-branch.md puts a phi on a float compare's EDGES, but the value it carries is an int, so a float-TYPED phi was covered by nothing. Fixed in LowerMaxonToStd.retypeBlockArgs, which types every phi from stdTypeOf — the same ONE oracle every operand's type comes from — rather than at the mint site, where the collapse is not yet total (a parse-time tag can still be `unresolved`, which is E3004's business and not a panic's). THE FIX COSTS EXACTLY ZERO: allocations and bytes BIT-IDENTICAL to pristine HEAD on the float-free corpus at all six rungs, which is what the single reader predicts (i1/u32/i64 all map to gpr; only a float phi changes file). Fragment diff is additions-only across every pre-existing golden; +3 specs (float-phi-loop-carried / -through-branch / -both-arms); suite 355/355 at --workers=1 and =12. TWO LEADS CLOSED WITHOUT ACTION, verified rather than assumed: (1) `.allCases` in a hot path — NONE remains; every surviving site is per-compile setup or reporting, and RegAllocStats.create (the one that looks per-function) is reached from project.timings.regAlloc, once per compile. (2) ISel's 552 x 2^rung residual is REAL — StdToX64Conversion builds four per-function containers (cmpPreds, fusedFloatCmps, orderedBlockIds, valuesReadFromRegisters) for every function whether or not it ever sees a float — but it is 0.21 percent of rung 5 and LINEAR, and making it lazy would fork a conditional construction path for a constant factor. Growth curves are the mandate; that is not one. | 18,011,632 | 36,281,760 | 75,528,599 | 153,360,015 | 316,037,694 | 646,133,471 |
| 2026-07-15 | P1.0r OPTIMIZATION PASS - the heap, the refcount runtime and the minimal struct. NOTHING WAS OPTIMIZED BECAUSE NOTHING IS SUPERLINEAR - a measured null result, and every flagged suspect was refuted by measurement rather than by argument. ⚠⚠ READ THIS BEFORE TRUSTING THE DELTA COLUMN: THE -2,494,194 IS NOT THIS RUNG'S AND IS NOT A COMPILER CHANGE AT ALL - IT IS A STALE BASELINE IN THIS LOG. The row above (P1.0d.4 FLOATS) records rung5 allocs = 10,885,522; that number DOES NOT REPRODUCE FROM ITS OWN COMMIT. Measured, not inferred: ed86a5eb8 - the very commit that wrote the row - checked out into a clean worktree, its C# bootstrap rebuilt from source (sha d07c23c3, distinct from today's aad7c678 but the SAME 8,581,499 bytes, which is exactly how a size check fools you), and shv2 rebuilt by it, measures rung5 = 8,369,027, i.e. -22.9% against what the row claims. The branch point 583882b52 measures 8,369,027 too - BIT-IDENTICAL TO ed86a5eb8 AT ALL SIX RUNGS (280,461/541,515/1,063,389/2,106,766/4,193,804/8,369,027), which independently MEASURES the seven inherited commits as exactly memory-neutral, including the bootstrap's range-arm fix 645a690cc (a real codegen change over a construct shv2's source is full of, and it moves nothing here). So the row above was recorded from a tree that is not its own commit - the alloc column is ~30% high while its BYTE column is only 1.9% high, which is the envelope-collapse signature of rows 142/143 (a per-object-overhead win: allocs -23%, bytes ~flat), i.e. the row's numbers predate work its own tree already contained. THIS ROW IS A NEW, REPRODUCIBLE BASELINE: three independent trees on one machine agree. Three hypotheses were killed by measurement before this one survived - (1) "the worktree lacks the float corpus" (it HAS ed86a5eb8; and the corpus emits 278 float literals at rung0, 8,710 at rung5), (2) "the stdlib changed" (git diff ed86a5eb8..HEAD -- stdlib/ is EMPTY), (3) "the bootstrap's range-arm fix did it" (rebuilt at ed86a5eb8 with its OWN bootstrap: identical numbers). ATTRIBUTION IS A MEASUREMENT, NOT A STORY THAT FITS, and each of those three fit. ⭐ P1.0r'S OWN COST IS EXACT AND CLOSED-FORM, WITH ZERO RESIDUAL ON ALL SIX RUNGS: allocs = 5 x files + blocks + functions + 26. Against files=14/18/26/42/74/138, blocks=653/1303/2603/5203/10403/20803, functions=38/62/110/206/398/782 it predicts 787/1401/2765/5493/10949/21861 and measures 787/1401/2765/5493/10949/21861 - residual EXACTLY 0 at every rung. It DOUBLES per rung (x1.88, 1.94, 1.97, 1.98, 1.99; 28.3x over the ladder's 32x span) so it is LINEAR, and at rung 5 it is +0.27% of allocations. The four terms, each named at its line: (a) 3 x files in phase:signatures - the declaration sweep's per-file struct columns; (b) 2 x files in phase:parse - TWO Parser.create()s per file (the sweep's scanner and the real parse), each building an ownedBindings VarInfoArray, and the SWEEP's copy is structurally dead (a sweep parses no body and declares no binding); (c) functions - the per-function `self.ownedBindings = VarInfoArray.create()` reset (Parser.maxon:1919); (d) blocks - and this is 93% of the whole rung (20,803 of 22,301 at rung 5): Parser.openBlock() now returns a HEAP-ALLOCATED BlockMark box where the baseline returned a bare integer (`return self.mutableVars.count()` -> `return BlockMark.create(mutableCount, ownedCount:)`), so every if/else/while body allocates a two-field box to carry the second mark this rung needs. LEFT ALONE DELIBERATELY, and named here so the next reader starts at the right line: it is LINEAR and it is a constant factor, and both alternatives are worse than the 0.26% they would buy - bit-packing two BindingMarks into one int is a magic value across a range that spans u64, and a parallel mark stack trades the structural guarantee that the mutable-list and owned-list heights are opened and closed TOGETHER (which is what the mark exists to make unloseable - a drained-late owned list is a DOUBLE FREE) for an amortized array push. Growth curves are the mandate; that is not one, and this is the same call the P1.0d.4 row made on ISel's 552 x 2^rung residual. ⭐ SUSPECT 1 REFUTED BY MEASUREMENT - the flagged O(files^2) hazard (the whole-program signature query sits UNDER a per-file one, and this rung taught its token sweep to mint Point.create). Measured OFF-INSTRUMENT in the hazard's LIVE shape, because the corpus cannot see it: a ladder of 8/16/32/64/128 files, each DECLARING a struct and READING a struct declared in ANOTHER file (verified to compile and return the right answer: a cross-file T0.create + field reads gives 10). phase:signatures reads 978/1859/3596/7045/13918 allocs = x1.90, x1.93, x1.96, x1.98 - 14.2x over a 16x file span. Under the once-per-file bug it reads x4.00 and 256x. The memo holds; the hazard did not fire. phase:parse on the same ladder is x1.99. SUSPECT 2 (moduleUsesHeap walks every op of every function to answer one bool): it is O(ops), called ONCE (Compiler.maxon:158), and it ALLOCATES NOTHING - which the flat 26 residual above proves rather than assumes, because 26 is the same at rung 0 and at rung 5 while a per-block or per-op iterator would scale with the ladder. SUSPECT 4 (the ownedBindings drain): closeBlock pops to a recorded height (amortized O(1) per binding) and emitScopeDrops is O(live struct bindings) per return, which is the SIZE OF THE EMITTED OUTPUT - every return MUST decref every live box - not an algorithm with a curve. ⭐ SUSPECT 3 LEFT ALONE, AND THE REASON IS MEASURED RATHER THAN ASSERTED. StructLayout.indexOfField (Project.maxon:224) is a LINEAR SCAN, and the two references disagree about it: v1 hashes (fieldByName as FieldNameIndexMap, Project.maxon:3973, built per StructType.create) and the C# bootstrap scans (st.Fields.FirstOrDefault(f => f.Name == fieldName), 2-Parser.cs:19216). shv2 matches the bootstrap. It IS O(F) per access and it IS visible under torture - a controlled experiment holding the program shape fixed at 200 accesses and varying ONLY the field index (a scan is the one thing that can tell first from last) measures reading the LAST field of a 512-field struct at +2,658,300 ns over reading the FIRST, with the gap growing in F. But it is NOT a growth curve in program size, and the measurement that settles it is the field-count distribution of the two real codebases: v1 is 3.5x larger than shv2 (369 declared types vs 107) yet its MEAN field count is identical (5.4 vs 5.3) and its MEDIAN is LOWER (3 vs 4). Programs grow by adding TYPES, not by widening them, so the cost is O(accesses x 5.3) - linear with a bounded constant - and a per-layout name->index map would buy a heap Map per declared type plus a name hash per lookup to save ~2.6 byte-compares. That is a structural argument backed by two independent 100k+ line corpora, NOT the "a program's X count is a handful" appeal that row 141 correctly convicted layOut of - there globals genuinely grow with program size, here fields per type measurably do not. ⚠⚠ AND FOR THE SIXTH CONSECUTIVE RUNG THE CORPUS IS BLIND TO WHAT LANDED, and this time the manifest says so in its own words: "NOT GENERATED ... structs". Verified by emitting it: ZERO `type` declarations and ZERO Self{} across every rung. So NOT ONE LINE of osAllocPages, __slab_alloc, __mm_alloc/incref/decref/free, the leak check, or the struct front-end is executed by ANY number in this row - phase:runtimeAugment is 54/54/55/56/57/58, i.e. the runtime is never installed because moduleUsesHeap is correctly false on every rung. What this row measures is EXACTLY what a struct-free program pays for the rung EXISTING, which is the 5 x files + blocks + functions + 26 above - and that is worth having (it is the proof that `return 42` still compiles to what it always did, which is the rung's own stated promise) but it says NOTHING about the heap. The prior five: no and/or, no div/mod, no top-level var, no top-level let, no floats. Everything about structs in this pass was therefore measured off-instrument, on the private ladders above. GATES: build exit 0 zero warnings; specs-shv2 357/0 at --workers=1 AND --workers=12, byte-identical; zero M in specs-shv2/fragments/; no exit 101. NO COMPILER CODE WAS CHANGED BY THIS PASS - the numbers in this row are the rung's, not the optimizer's. | 17,629,150 | 35,521,060 | 74,011,395 | 150,326,838 | 309,962,985 | 633,968,890 |
| 2026-07-16 | CORPUS BLIND SPOTS CLOSED — FIVE NEW KNOBS. ⚠⚠ THE CORPUS CHANGED: EVERY COLUMN ABOVE THIS LINE IS INCOMPARABLE WITH EVERY COLUMN BELOW IT. This row measures a DIFFERENT PROGRAM, not a different compiler. NO COMPILER CODE WAS CHANGED — the only edit is maxon-shv2/Testing/ScaleCorpus.maxon. Do not read the delta column as a regression: rung5 allocs 8,391,328 -> 9,932,791 (+18.4%) is the five new knobs' extra program, and the ladder stayed LINEAR through it (total allocs x1.90 x1.95 x1.97 x1.99 x1.99; every phase x1.94-x2.00). Precedent: P1.0d.4 moved the columns the same way when it added the float knobs. ⭐ WHAT WAS BLIND, MEASURED BY EMITTING THE CORPUS AND COUNTING (before -> after, at rung1): top-level `let` 0 -> 64; top-level `var` 0 -> 64; `type` decls 0 -> 20; Self{} 0 -> 20; .create() 0 -> 80; integer `/` 0 -> 64; `mod` 0 -> 64. Every one now DOUBLES per rung (rung0->rung5: lets 32->1024, vars 32->1024, types 12->260, creates 40->1280, div/mod 32->1024 each). The corpus had been FULL of `/` and every one was a FLOAT `/` — a different Std op (binOp(div), no fixed registers) — so counting the OPERATOR said "covered" and only counting its TYPE found the hole. Four landed rungs were unmeasured: P1.0d.3 (the whole idiv RAX/RDX path), P1.0d.5a top-level let (suite 281->295), P1.0d.5b top-level var (295->317), P1.0r structs+heap (355->357). ⭐ AND THE MANIFEST LIED BY OMISSION, which is the half of this rung that matters. It printed "let / var" under CONSTRUCTS GENERATED meaning the BODY-LOCAL kind, and named the top-level kind under NEITHER heading — so two rungs and 36 spec tests read as covered by a reader doing exactly what the manifest asks. It is now held to a stated rule: every construct in the subset appears under EXACTLY ONE heading, and a PARTLY-generated construct (structs — reads yes, writes no) says which part under GENERATED rather than being filed whole under either. Structs' two interior blind spots are named where a reader looking up structs will find them: field WRITES (P1.1a wave 2) and structs across a CALL (P1.4). ⭐⭐ A REAL SUPERLINEARITY, FOUND AND THEN DESIGNED OUT OF THE KNOB — NOT HIDDEN. The first draft put each `create` at FUNCTION-BODY scope and the ladder went superlinear: regalloc:splitting read x3.12 x3.45 x3.69 x3.83 x3.91 (converging on x4.00 = QUADRATIC) and was 17,110,308 of 26,404,072 allocs = 65% of the whole compile, while every other phase read x2.00. CAUSE (measured, not argued — an isolating ladder per knob refuted the obvious suspects first): intDivide, globalVars and globalConsts all read splitting x1.00-x1.01, i.e. FLAT; a control ladder of 64->1024 sequential CALLS with a value live across each also read FLAT (320 -> 326 allocs), killing "it is the call brackets". Only the two struct knobs bent. The mechanism is ownership: a struct binding is OWNED, so it does NOT die at its last read — it stays on the parser's ownedBindings list until the `return`, where emitScopeDrops (Parser.maxon:2524) decrefs every one. N creates at function scope therefore put N pointers LIVE SIMULTANEOUSLY at the return against a 14-GPR pool; they are spillable so it is never E5001, and the splitter instead does ~N splits, each re-sweeping the O(N) ops of the one enormous block (SplitLiveRanges' driver is incremental in DIRTY BLOCKS, and there is only one). FIX: each create now sits in its own `if` block, so the binding's scope — and its live range — is bounded. Measured on the private ladder, 32/64/128/256/512 creates: function scope x3.49 x3.70 x3.84 x3.91; block scope x1.82 x1.90 x1.95 x1.97. On the full ladder regalloc:splitting went x3.91 -> x2.01. THE KNOB WAS WRONG, NOT THE COMPILER: this is trap 7 (bounded liveness) and it is the same call the file already makes for locals — fillerLocalsDecl keeps V IN SCOPE BUT NOT LIVE precisely because a doubling count of LIVE values trips E5001 at V>13. The splitter's O(splits x ops-in-dirty-block) is REAL and reproducible, but it is a curve in live-values-per-block, not in PROGRAM SIZE, and real code bounds it — the same reasoning the row above used to leave StructLayout.indexOfField alone. It is left alone and named here so the next reader starts at the right line. ⚠ AND THE TRIGGER TO RE-MEASURE IS NOT P1.4 OR P1.7 — this row named those two on plausibility alone, the independent optimizer MEASURED them wrong, and the correction is recorded here rather than the claim quietly dropped, because a wrong trigger in a comment is the "compiler nobody wrote" class freshly minted: it would have sent the next reader to re-measure the two rungs that CANNOT move this number while leaving the one that can unwatched. A struct crossing a call (P1.4) is bounded by PARAMETER COUNT — and that is not a corpus convention but a compiler cap, `Parser.maxon:50` ("M5 caps a function at six parameters") — and an array (P1.7) is ONE owned value (`for x in arr` holds one element live), so neither makes K grow with PROGRAM SIZE, which is the only growth that could bend this curve. THE REAL TRIGGER IS AN INLINER: inlining N callees each holding an owned local into ONE block is precisely what makes K scale with program size, and shv2 has none today — its whole default pipeline is resolveTypes / semanticCheck / lowerMaxonToStd / pruneDeadBlockArgs / elimTrivialBlockArgs / foldConstOperands (PassPipeline.maxon:135). Any desugaring that emits many owned temporaries into one block does the same. The ceiling is bounded but its constant is bad, so it is debt and not a non-issue. ⚠ THE BLOCK SCOPING DOES NOT LEAN ON THE closeBlock LEAK, which is the first thing a reader will suspect. closeBlock (Parser.maxon:2343) drains the binding and deliberately emits NO drop, so a struct in a block leaks (exit 101 — verified, and a stated P1.4 gap this instrument never observes because it COMPILES these programs and never RUNS them). That leak is not what bounds the range: when P1.4 emits the drop at the block's END, the range is bounded by the block either way. Correct before the fix and after it. ⚠ phase:runtimeAugment IS STILL FLAT (58/59/60/61/62/63) AND THAT IS CORRECT — DO NOT READ IT AS THE STRUCT KNOB FAILING TO REACH THE HEAP. It is fixed-cost BY CONSTRUCTION: the phase times augmentWithRuntime only, which is prependEntryStub — ONE hand-built entry stub. The heap runtime proper (installMmRuntime, five FIXED functions: slabAlloc/mmAlloc/mmDecref/mmFree/mmLeakCheck) is called at Compiler.maxon:180 OUTSIDE every PhaseProbe, so it is billed to NO PHASE AT ALL — a real attribution gap (phases do not sum to total), constant-cost so it distorts no trend, reported and not fixed because it is compiler code and outside this rung. There is also no __destruct_* cascade yet to make it per-TYPE (LowerMaxonToStd.maxon:9 names it as future work). PROOF THE HEAP IS REACHED, since this phase cannot give it: a struct in a block exits 101, which is unreachable unless __mm_leak_check was linked in; and runtimeAugment's own allocs moved 54->58 at every rung (the stub taking its checkLeaks branch) while its bytes now grow 3,016->26,696 (prependEntryStub PREPENDS to a function array that doubles, an O(N) copy; the +1 alloc/rung is that array's growth realloc). The struct knobs' real cost lands in lowerMaxonToStd/isel/regalloc/encode. ⭐ THE TWO STRUCT KNOBS ARE DELIBERATELY TWO: structTypes doubles TYPE declarations (12->260, a per-TYPE cost) while structAllocs doubles CREATE SITES (32->1024) over a FIXED set of 4 types — so P1.1a wave 2 moves one and a layout-descriptor change moves the other. The combined `type` column reads x1.67..x1.97 rather than x2.00 and that is arithmetic, not a bend: it is 8<<rung + 4, a doubling knob plus a deliberate constant; n_stypes alone is exactly 8/16/32/64/128/256. Each rung also gained 5 files (19 at rung0, 143 at rung5), worth a measured +13 allocs/+459 bytes each. GATES: build exit 0 zero warnings; specs-shv2 357/0, memoryLeak false, --workers=1 and --workers=12 BYTE-IDENTICAL; git status specs-shv2/fragments/ EMPTY (no golden moved — a corpus change must not touch codegen, and did not); scale-test exit 0 with assertCorpusCompiled passing (code growth x1.93 x1.96 x1.98 x1.99 x1.99 against a x1.50 floor); no run exited 101. | 21,189,728 | 42,171,114 | 88,608,443 | 180,300,604 | 369,835,413 | 758,867,942 |
| 2026-07-16 | MAKE THE COPIES CHECK EACH OTHER — +2 allocs / +296 bytes, FLAT at every rung, and it is a CHECK'S OWN COST, not a regression. THE CORPUS DID NOT CHANGE: the emitted text is byte-identical to the row above (342 files, recursive diff empty, re-proven after every edit including the review's). The entire delta lands in phase:regalloc — 113,973 -> 113,975 at rung 0 and 3,284,334 -> 3,284,336 at rung 5 — which is `assertRegisterFileMatchesEnum()` reading `X64Register.allCases` to count it, once per module at RegisterAllocator.maxon:428, beside `assertCallClobberConsistent`. Flat across a 32x ladder is the signature of a per-module constant; it does not scale and it never will. ⭐ WHAT IT BUYS: `RegisterFileSize` (32) is now held to `X64Register.allCases.count()`, and `FirstXmmRegisterNumber` (16) to the register the enum actually puts at that number. Both were literals RESPELLED from the enum with only a comment ("the two must move together") and a spec test holding them — and `regClassOf` is THE place the GPR/XMM boundary is decided, so a drifted boundary colors an f64 into a GPR. SABOTAGED AND SEEN RED before being believed: FirstXmmRegisterNumber=15 -> exit 1, "the register the enum puts at that number is not xmm0"; =32 -> E3034 at COMPILE time, so that drift now builds nothing at all. ⭐⭐ AND THE BETTER OUTCOME, WHERE IT WAS AVAILABLE: `MaxUnguardedShiftCount` was a literal 63 beside a literal 64 — ONE hardware fact (the operand width) written twice — and is now DERIVED, `(ShiftCountBits - 1)`, verified const-folded (it casts to int(63 to 63) and runs). No check exists for that pair because there is nothing left to compare: drift is UNREPRESENTABLE, not asserted. A derivation beats a check exactly as a check beats a comment. ⚠ It is NOT the only copy and the code says so: TargetDialect.ShiftCount's int(0 to 63) is a RANGE BOUND (E2010, unreadable by any assert) and moves BY HAND. DELETED, same rung, same rule that deleted __mm_incref at P1.0r: classifyGlobalLabel + GlobalLabelClass + isRuntimeMutableGlobalLabel had ZERO callers and held a second copy of "__data_" beside DataLabelPrefix — "they cannot disagree only because one is dead". OPEN.md predicted it would go live when __slab_ globals arrived; P1.0r shipped __slab_cursor/__slab_end and nothing wired it, so the prediction was already falsified. Also fixed: four comments claiming mechanisms nobody wrote (TargetLiveness:172 cited an assert that never touched what it claimed; RegisterAllocator:146 called RegNum int(0 to 16) and REASONED FROM that bound when it is int(0 to 32); two "u16 mask" comments stale since P1.0d.4 doubled the file). The review closed one more the implementer had left: the chain knob's top link was coupled to its loop bound by ADJACENCY not agreement, and the stated reason for leaving it ("byte-identity forbids closing it") was false — byte-identity is re-run, not lost, and the closure emits identical text. Gates: build exit 0 zero warnings; specs-shv2 357/0 memoryLeak false; workers=1 vs 12 byte-identical; fragments EMPTY; no 101. | 21,190,024 | 42,171,410 | 88,608,739 | 180,300,900 | 369,835,709 | 758,868,238 |
| 2026-07-16 | P1.1a wave 2 (struct field writes + mutability + field defaults) costs +156 allocs at rung 0 and +3,380 at rung 5 — LINEAR, ~0.03-0.05% of total, and it is a per-TYPE cost. ⚠ READ THE DELTA COLUMN AGAINST THIS, NOT AGAINST THE TOOL'S: the tool reports +480/+34,952 because the row above does not reproduce at the commit it was recorded on. MEASURED BY A/B, not inferred: same corpus, same bin/maxon.exe, two shv2 binaries differing only by this commit (parent rung0 342,858 -> 343,014; rung5 9,964,365 -> 9,967,745). The row above logs rung0 342,534 / rung5 9,932,793, so its baseline sits 324/31,572 BELOW the actual parent — ~10x this change at rung 5, which would have swallowed it whole. THE CORPUS IS NOT THE CAUSE AND THAT WAS TESTED, NOT ARGUED: the review commit 9ba3af660 edited ScaleCorpus.maxon, so the obvious suspect was that its "THE CORPUS DID NOT CHANGE" claim was false — it is TRUE (built the generator at bbc1ea32a and at 9ba3af660, emitted both ladders, recursive diff EMPTY across all 6 rungs). The corpus being identical is what localizes the drift to the shv2 BINARY, and the only input to that which is not in git is bin/maxon.exe — the documented stale-bootstrap trap (reference_stale_bin_maxon_exe.md: "a baseline you built YOURSELF can measure a tree that does not exist"). An A/B is immune to it by construction; an absolute row is not. ⭐ WHERE THE +156 LANDS, EXACTLY: phase:parse +84 and phase:signatures +72 at rung 0 (+1,820 / +1,560 at rung 5), and EVERY OTHER PHASE +0 — not "small", zero. Those are precisely the two phases that build a StructLayout (the real parse and the whole-program token sweep, one layout each per declared type), which is the attribution rather than a story that fits it. Per type: ~6 allocs at rung 0 falling to ~4.7 at rung 5 as array growth amortizes = the 2 new columns (BoolArray fieldMutable + FieldDefaultArray fieldDefaults) plus each one's first-push backing store. Rung-over-rung x1.67 x1.80 x1.89 x1.94 x1.97 -> 2.00 = linear in the `type` declaration knob, which doubles per rung; a per-field cost cannot bend a ladder whose field count is linear in it. FieldDefault.none costs NOTHING and the numbers say so: 24 fields at rung 0 would have added +24/phase if a payload-free union arm were boxed, and the count is ~4 arrays/type with no field term — ARCHITECTURE's "Maxon boxes each payload-carrying union case", confirmed by measurement. ⚠ THE CORPUS STILL DOES NOT EXERCISE THIS RUNG'S FEATURE AND ITS OWN MANIFEST SAYS SO: field WRITES are named as a struct blind spot ("P1.1a wave 2 and NOT IMPLEMENTED", ScaleCorpus manifest), and no generated field carries a DEFAULT, so what moved is only the layout that now has room for both. Closing those knobs is the instrument's rung, not this one — the movement is reported, the instrument is untouched. Gates: dotnet build exit 0; build shv2 exit 0 zero warnings; specs-shv2 370/0 (was 358/11 against the ported specs before the fix), memoryLeak false; workers=1 vs 12 BYTE-IDENTICAL; git status specs-shv2/fragments/ ADDITIONS ONLY (no pre-existing golden moved, so no existing program's codegen changed); no run exited 101. | 21,209,138 | 42,234,624 | 88,759,991 | 180,628,120 | 370,514,865 | 760,250,510 |
| 2026-07-16 | P1.1a WAVE 2 — OPTIMIZATION PASS: NOTHING WAS SUPERLINEAR, SO NOTHING WAS CHANGED. Every number on this row is IDENTICAL to the row above because the compiler is byte-identical to it — this row is a VERIFICATION, not a change, and a null result recorded on purpose (P1.0r's optimizer wrote the same kind of row). ⭐ THE THREE CANDIDATES, EACH MEASURED RATHER THAN ARGUED. (1) `StructLayout.indexOfField` (Project.maxon:276) is a LINEAR SCAN of the name column and the rung made it HOTTER — it is now asked by every field READ, every field WRITE and every `Self{}` label, so a literal filling all F fields costs O(F^2). It is DEBT, not a bug, and the constant is MEASURED, not assumed: across all 166 `type` declarations in maxon-shv2/ + stdlib/ (the P2 self-host workload) F is max 22 (`UseIndex`, TargetLiveness.maxon) and mean 4.14, so one lookup is at most 22 byte compares and the sum over the program is LINEAR IN PROGRAM SIZE. A per-layout name-to-index map would buy a heap object per declared type to serve a 4-element scan: a PESSIMIZATION on every real program, which is why `FuncSignature.indexOfParam` is a linear scan too and says so. ⚠ THE RE-MEASURE TRIGGER IS A MACHINE-GENERATED STRUCT — a layout whose field count grows with PROGRAM size rather than with one declaration's own source. I deliberately name NO RUNG: F is bounded by what a human types in one declaration, a closure env (P1.5) is bounded by its own closure body, and monomorphization does not change a field count — OPEN.md #24's first draft is the cautionary tale, it named two rungs that could not move the number and the review deleted them. (2) `recordStruct`'s hash now mixes 2 more facts per field via `mixField`, and it is asked ONCE PER FILE over that file's OWN structs (`queryProgramSignatures` sweeps each source path once, Queries.maxon:119-133; `foldFile` folds only that file's `structs` array), so it is O(total declared fields) = LINEAR. It is specifically NOT the standing O(files^2) shape — a whole-program query placed UNDER a per-file one — which is what that rule exists to catch. (3) `fieldAssignsAt` (Parser.maxon:5621) is a 4-token O(1) lookahead asked ONCE PER STATEMENT by `parseStatement`, and it is NOT reached from `namesAssignedIn`: that per-token scan over a loop's extent reads only `assignsAt` (Parser.maxon:3250), which is CORRECT rather than an omission — a field store assigns no BINDING, so there is no loop-carried value and no phi to mint. ⭐⭐ THE RUNG'S OWN COST, RE-MEASURED INDEPENDENTLY AND CONFIRMED EXACTLY: +156 allocs at rung 0 rising to +3,380 at rung 5, landing in phase:parse (+84 to +1,820) and phase:signatures (+72 to +1,560) with ALL EIGHTEEN other phases AND ALL SEVEN regalloc sub-phases at EXACTLY +0 — not "small", zero. Those are precisely the two phases that build a StructLayout. Delta ratios x1.67 x1.80 x1.89 x1.94 x1.97 converging on 2.00 on a DOUBLING ladder = LINEAR. ⚠⚠ THE BASELINE TWO ROWS UP IS WRONG, AND IT IS NOW PROVEN RATHER THAN SUSPECTED — NOT THE CORPUS, NOT A MYSTERY. The check-sweep row logs rung0 342,534 / rung5 9,932,793. Built from source in a clean worktree with its own bootstrap, its own commit (f419febb6) measures 342,858 / 9,964,365 — 324 / 31,572 HIGHER. WHAT PROVES IT: 2f94c0a21 (post-bootstrap, pre-check-sweep) measures 342,856 and f419febb6 measures 342,858, EXACTLY the +2 the check-sweep claims for ITSELF. So THE CHECK-SWEEP'S DELTA WAS RIGHT AND ITS BASELINE WAS STALE. The 324 is the 14-commit window between the corpus rung's measurement (74ea57d1f, 17:53, which legitimately measured 342,532 against the then-current bootstrap) and the check-sweep's (bbc1ea32a, 20:02): FIVE BOOTSTRAP COMMITS LANDED AT 19:32 — including `4a33acb36 String's iterator holds a String, not a record, and for-in got 1.5x faster`, which changes how the bootstrap lowers for-in and therefore how many allocations the shv2 IT BUILDS makes — plus coupled stdlib and shv2 String changes. bin/maxon.exe is GITIGNORED and nothing rebuilds it, so a tree that took those commits kept the OLD binary and the shv2 built from it kept the old allocation behaviour: the documented stale-bootstrap trap (OPEN.md #22, reference_stale_bin_maxon_exe.md — "a baseline you built YOURSELF can measure a tree that does not exist"), caught in the act with the smoking gun dated. ⇒ ROWS "corpus: five knobs" AND "make the copies check each other" ARE PRE-19:32-BOOTSTRAP NUMBERS: their absolutes sit ~324 (rung 0) to ~31,572 (rung 5) BELOW what their own commits actually produce. They are comparable to EACH OTHER and NOT to this row or the one above it. I am NOT rewriting them — a measurement I did not take is not mine to edit, and replacing a wrong number with a re-derived one would hide that the gap existed — but the trend now SAYS so. ⭐ AND THE CORPUS IS EXONERATED BY MEASUREMENT, NOT BY ARGUMENT: the 2f94c0a21 -> f419febb6 comparison above SPANS the review commit 9ba3af660's 182-line ScaleCorpus.maxon edit and lands on exactly +2 — the check-sweep's compiler change ALONE — so that generator edit emits byte-identical text and moved EXACTLY ZERO. ⚠ THE CORPUS CANNOT SEE THIS RUNG AND IT ALSO CANNOT SEE F: `structTypeDecl` (ScaleCorpus.maxon:1226) emits exactly TWO fields per generated type while the ladder doubles the NUMBER OF TYPES, so the per-struct field-count dimension that `indexOfField`'s O(F^2) lives in is STRUCTURALLY INVISIBLE to this instrument — which is why F's bound had to be measured against real source instead. Same shape as OPEN.md #24's K-vs-blocks separation: two variables, and only measurement tells them apart. Closing that knob is the instrument's rung, not this one; I did not touch the instrument. GATES (this pass changed no compiler code): build csharp exit 0 and shv2 exit 0, zero warnings; specs-shv2 370/0 exit 0, memoryLeak false; specs-shv2/fragments/ zero modified and zero untracked; no run exited 101. | 21,209,138 | 42,234,624 | 88,759,991 | 180,628,120 | 370,514,865 | 760,250,510 |
| 2026-07-16 | P1.1a WAVE 3 — instance methods + self, field visibility (E3014), E3011 at field declarations. +148 allocs at rung 0 rising to +3,496 at rung 5, and it is LINEAR: delta ratios x1.73 x1.84 x1.92 x1.96 x1.98 converging on 2.00 on a DOUBLING ladder. Nothing regalloc, nothing backend. ⭐ THE SPLIT IS MEASURED, NOT ARGUED — the two contributions were separated by BUILDING THE COMPILER WITH checkStructFieldTypes DISABLED and re-running the ladder: (1) StructLayout.fieldExported, the new visibility column = +135 rung0 / +3,235 rung5 (ratios x1.74 x1.85 x1.92 x1.96 x1.98). One extra BoolArray per StructLayout, and a layout is built TWICE per declared type — once by the token sweep (recordScannedType) and once by the real parse (parseTypeDeclaration) — so it is two arrays per type, and the corpus's structTypes knob grows the NUMBER of types per rung. Linear in declared types, and it is the storage E3014 reads. (2) checkStructFieldTypes, the new registry walk in resolveTypes = +13 rung0 / +261 rung5 (ratios x1.62 x1.76 x1.86 x1.93 x1.96) — one MapIterator per COMPILE plus one ArrayIterator per declared type. 261 allocations at rung 5 is 0.0026% of the run; the per-layout iterator could be an index loop, but resolveTypes' own neighbouring loop over param types is an iterator and consistency is worth more than 0.0026% — recorded so the optimizer can disagree with a NUMBER rather than a feeling. ⚠⚠ THE INSTANCE-METHOD MECHANISM ITSELF COSTS ZERO HERE, AND THE CORPUS STRUCTURALLY CANNOT SEE IT: ScaleCorpus's structTypeDecl emits types with FIELDS and no METHODS, so not one generated program declares an instance method, calls one, or names a bare self field. Every allocation above is the FIELD-DECLARATION path. The receiver parameter, the self-field aliases, the method-call receiver prepend and the E3014 check are all INVISIBLE to this ladder, so their cost is bounded by construction rather than by measurement: a receiver is one more entry in the three parallel call columns (pushCallArg) and one more parameter binding, both O(1) per call site; declareSelfFieldAliases is O(fields of the enclosing type) once per method, which is the same F that P1.1a wave 2's row measured at max 22 / mean 4.14 across all 166 type declarations in maxon-shv2/ + stdlib/. Closing that knob is the instrument's rung, not this one; I did not touch the instrument. GATES: build csharp exit 0 zero warnings; build shv2 exit 0 zero warnings; specs-shv2 392/0 exit 0 (371 baseline + 21 ported, every one RED before the change); workers=1 vs workers=12 stdout BYTE-IDENTICAL; specs-shv2/fragments/ additions only, ZERO modified; C# suite 3028 passed exit 0; no run exited 101. ⭐ CODEGEN NEUTRALITY PROVEN BY EXPERIMENT RATHER THAN ASSUMED: specs/ shows 3 modified + 2 untracked fragments after a C# suite run, and a clean worktree at UNMODIFIED main (71962e9f7) with its own freshly built bootstrap regenerates BYTE-IDENTICAL content for every one of them — so that churn is PRE-EXISTING drift in main's committed fragments (stale since 2078becf5) and not this rung's. This branch's only maxon-sharp change is a comment-only regeneration of ErrorCode.g.cs from the one corrected doc line. | 21,211,938 | 42,239,424 | 88,768,791 | 180,644,920 | 370,547,665 | 760,315,310 |
| 2026-07-16 | P1.1a WAVE 3 — OPTIMIZATION PASS: NOTHING WAS SUPERLINEAR, SO NOTHING WAS CHANGED. Every number on this row is IDENTICAL to the row above because the compiler is byte-identical to it — a VERIFICATION, not a change, and a null result recorded on purpose (the wave-2 and P1.0r optimizers wrote the same kind of row). ⭐⭐ THE RUNG'S ENTIRE ROW RE-VERIFIED BY INDEPENDENT A/B, AND IT REPRODUCES TO THE SINGLE ALLOCATION. This is worth the run because this very log documents TWO rows whose baselines were stale (OPEN.md #22: bin/maxon.exe is gitignored and nothing rebuilds it), so an inherited delta is worth less than a re-measured one. Method: ONE bootstrap built from source (md5 9668dc186dc3b284bea3b5e894904593) COPIED into a detached worktree at unmodified main (71962e9f7) and used to build BOTH shv2 binaries, so the only variable is the rung's commits — immune to the stale-bootstrap trap by construction rather than by hope. Result, branch minus main: +148 +256 +472 +904 +1,768 +3,496, ratios x1.73 x1.84 x1.92 x1.96 x1.98 -> 2.00 on a DOUBLING ladder = LINEAR. The rung's SPLIT is confirmed exactly too: StructLayout.fieldExported (parse +111->+2,715 and signatures +24->+520) = +135 -> +3,235, and checkStructFieldTypes (resolveTypes) = +13 -> +261. phase:regalloc, phase:lex, phase:isel, phase:merge and phase:lowerMaxonToStd are EXACTLY +0 — not "small", zero. Main's own absolutes independently reproduce the wave-2 row (343,014 / 9,967,745), so the trend now has two rebuilt-from-source anchor points and the numbers above are comparable to the two rows beneath them. ⭐ WHY checkStructFieldTypes IS LINEAR IS STRUCTURAL, NOT JUST MEASURED, and that is the load-bearing half: it walks the WHOLE project's struct registry while `resolveTypes(project, module)` takes a MODULE, which is exactly the silhouette of an O(files x structs) quadratic. It is not one. PassPipeline.run dispatches each pass EXACTLY ONCE (PassPipeline.maxon:211) over the single merged maxonModule, and Compiler.maxon:167 is the ONLY PassPipeline.create in the tree — so the walk runs once per COMPILE and costs O(total declared fields), each field one interner array-index plus two hash lookups (resolveNamedType). That distinction is not academic: Compiler.maxon:130's own comment records that `queryProgramSignatures` is a whole-program query sitting UNDER a per-file one and has grown and had removed TWO quadratics of precisely that shape. checkStructFieldTypes sits in the pipeline, not under the per-file query, which is what makes it linear. Measured ratios x1.62 x1.76 x1.86 x1.93 x1.96 — approaching 2.00 FROM BELOW, the signature of a*n+b (the one MapIterator per compile is the b), never above it. ⭐ THE ONE NUMBER ABOVE 2.00, AND IT IS NOT A BEND: regalloc:rewrite BYTES reads x2.204 on the last rung while its ALLOCS read x1.997 — same allocation count, bigger allocations (bytes/alloc 29.9 29.9 30.0 30.1 30.3 33.4, one step). It is capacity rounding in geometric array growth, and the proof is that the bytes ratios BOUNCE BOTH WAYS: regalloc:blockOrder BYTES goes x2.479 at rung 2 and back DOWN to x1.988 at rung 3. A superlinear curve does not bounce back down. It is also IDENTICAL on both sides of the A/B, so it is not this rung's in any case. ALLOCATIONS are the exact, bit-reproducible column and they converge on 2.00 from below in every phase and every regalloc sub-phase. ⚠ WHAT I DELIBERATELY DID NOT CHANGE, EACH WITH A NUMBER RATHER THAN A FEELING. (1) The 0.0026% iterator: the rung's row invited the optimizer to "disagree with a NUMBER" over checkStructFieldTypes' per-layout ArrayIterator. The number is 261 allocations out of 9,971,241 at rung 5. Declined — constant factors are not the mandate, and the neighbouring loop over param types is an iterator too, so an index loop here would buy 0.0026% and cost the consistency. (2) `declareSelfFieldAliases` (Parser.maxon:5029) asks `fieldTypeOf(layout.name, ...)` PER FIELD, which re-hashes the struct name once per field when the layout is already in hand — F redundant map lookups per method entry where 1 would do. Left alone: F is max 22 / mean 4.14 across all 166 type declarations in maxon-shv2/ + stdlib/, so it is a constant factor, AND the call is load-bearing rather than a lookup — `fieldTypeOf` is the door that RE-INTERNS a `named` type into the calling file's own interner (reading the column raw would bind aliases to the SWEEP's type-name ids, which this file's interner cannot resolve). A layout-taking overload would save ~4 byte-compares and add a SECOND adoption door beside the one `emitFieldLoad` already uses. (3) `StructLayout.indexOfField`: OPEN.md #28 — READ, not re-derived. Its O(F^2) lives in a dimension this instrument holds at F=2 by construction; the debt judgment there is measured and sound and I add nothing to it. (4) Method-call resolution is O(1) per call site and has no scan: `methodCallsAt` is a 4-token lookahead plus one scope hash lookup, `requireStructBinding` is an interner array-index plus one `structOf` hash, and `qualifiedMethodName` allocates one name per call site = linear in call sites. ⚠ AND THE HONEST LIMIT, RESTATED BECAUSE IT IS THE THING A FUTURE READER WILL WANT: the instance-method mechanism ITSELF is measured at ZERO here and CANNOT be otherwise — ScaleCorpus emits types with fields and no METHODS, so no generated program declares one, calls one, or names a bare self field. Every allocation in the +148 is the FIELD-DECLARATION path. The receiver, the aliases and the E3014 check are bounded by CONSTRUCTION (reading the code), not by this ladder. Closing that knob is the instrument's rung; I did not touch the instrument. GATES (this pass changed no compiler code): build csharp exit 0 zero warnings; build shv2 exit 0 zero warnings; specs-shv2 395/0 exit 0, memoryLeak false; workers=1 exit 0 vs workers=12 exit 0, stdout BYTE-IDENTICAL; specs-shv2/fragments/ zero modified and zero untracked; no run exited 101. | 21,211,938 | 42,239,424 | 88,768,791 | 180,644,920 | 370,547,665 | 760,315,310 |
| 2026-07-16 | P1.1a WAVE 3 — INDEPENDENT REVIEW: +1 allocation per field-access SITE, bought deliberately to make a silent wrong answer unrepresentable. THE FIND: a self-field alias has no SSA value (createSelfField leaves boundValue 0 because a read is a loadIndirect) and ValueId 0 IS THE RECEIVER. parseVariableReference guarded that and its comment states it exactly; parseFieldAccess, parseMethodCall and parseFieldAssignment each fetched the binding, proved its layout, then read binding.boundValue THEMSELVES. One fact, written once, needed in four places. It is reachable: parseTypeReference mints a structRef DIRECTLY for the enclosing type's own name (namesEnclosingType arm), so `var next as Node` inside `type Node` is a structRef, requireStructBinding admitted it, and boundValue was 0. MEASURED on the unguarded build via dump_ir: `return next.a` -> a single `loadRegBaseDisp.word64 r8, [rcx + 0]` = `return self.a`, exit 0 no diagnostic; `return next.readA()` -> a bare `callDirect Node.readA` with NO argument setup at all (the receiver was already in rcx) = `self.readA()`, exit 0 no diagnostic; `next.a = 99` -> E2013 "cannot assign to immutable variable: 'next'", false of an export var and a rejection only by accident of the alias being built mutable:false. THE FIX IS NOT A FOURTH GUARD: requireStructBinding -> requireStructBase now returns the BOX WITH the layout (new `type StructBase`), so it is the ONE reader of a base binding's boundValue and a caller cannot hold a layout it proved beside a box it assumed. That record is the entire cost of this row. WHY THIS SHAPE OF NUMBER IS EXPECTED: StructBase is a Maxon `type` = a heap value, 2 fields = 16 bytes, allocated once per FIELD-ACCESS SITE AT PARSE TIME (not at runtime of the compiled program). Deltas vs the optimizer's row: +80 +160 +320 +640 +1,280 +2,560 allocations, ratios x2.00 x2.00 x2.00 x2.00 x2.00 on a DOUBLING ladder = LINEAR, exactly; bytes +1,280 +2,560 +5,120 +10,240 +20,480 +40,960 = 16.00 bytes/alloc at EVERY rung, flat, which is the record's own size and says the count is one-per-site with no hidden second allocation. It is 0.026% of rung 5's 9,973,801. Every phase other than parse is +0. ACCEPTED rather than avoided, and the alternative was real: adding the isSelfField guard INSIDE the old layout-returning requireStructBinding costs ZERO allocations and fixes the same three bugs — but leaves boundValue readable at three sites, safe only because an earlier throw happened to fire, which is "safe because of an ordering elsewhere", the exact property the project's cure ladder ranks LAST (DERIVE > CHECK > COMMENT; make drift unrepresentable if you can). 0.026%, linear, compile-time-only, in exchange for a class of wrong answer becoming unwritable. Recorded so the next reader sees the trade rather than a mystery. ⚠ THE INSTRUMENT STILL CANNOT SEE THE SUBJECT, unchanged from the optimizer's row and restated because it bounds this row too: ScaleCorpus emits types with fields and NO METHODS, so no generated program declares a method, calls one, or names a bare self field — the +80 is entirely p.x / p.x = v field-access sites in free functions. The three bugs this row paid for are ones the ladder cannot reach; they were found by reading and pinned by specs-shv2/self-field-struct-typed.md (4 cases), whose reds were verified by two separate sabotages. GATES: build csharp exit 0 zero warnings; build shv2 exit 0; specs-shv2 399/0 exit 0 memoryLeak false (395 + 4 new); workers=1 vs workers=12 stdout BYTE-IDENTICAL both exit 0; specs-shv2/fragments/ additions only, ZERO modified — the refactor changed no emitted code; no run exited 101. | 21,213,218 | 42,241,984 | 88,773,911 | 180,655,160 | 370,568,145 | 760,356,270 |
| 2026-07-16 | P1.1b WAVE A — OPTIMIZER: scalar `match` (statement + expression; value/or/range patterns; block-arg `gives`/carried-var join). NOTHING SUPERLINEAR FOUND; NO COMPILER CODE CHANGED. MEASURED: all six rungs allocsDelta 0 / bytesDelta 0 vs the Wave 3 row — byte-for-byte identical corpus memory; parse phase scales 62,975→120,639→235,754→465,636→925,096→1,843,629 = x2.00/rung, LINEAR. WHY 0 DELTA — THE INSTRUMENT CANNOT SEE THE SUBJECT: ScaleCorpus has no match knob and no emitter appends `match`/`then`/`gives`, so no generated program enters the match parser; the match path measures ZERO here, same class as instance methods (OPEN #30) and the Wave 3 caveat. Mechanism verified by READING (Parser.maxon parseMatchCommon ~3691): lowering is a CHAIN of one condBranch per arm — O(arms) — with every per-arm helper O(1) (mintPhi/recordBranchEdge/emitCondBranch push, constOf/tagOf are dense-column reads); or-fold + range two-compares are linear in pattern tokens; finalizeMatchMerge's carried×arms nesting is inherent SSA phi-join cost (same shape if/while pay, and equal to the size of the exit-val snapshots already built). THE ONE NON-INHERENT SUPERLINEAR SHAPE, LEFT AS PRECEDENTED BOUNDED DEBT: checkAndRecordDuplicate (E2027, Parser.maxon ~4187) linear-scans `seenValues` per known-const single-value pattern → O(P^2) in ONE match's arm count. Whole-program cost is O(sum P_match^2) = LINEAR for bounded hand-written arm counts; quadratic only in a single-giant-match, the identical degenerate case the project already ACCEPTS for parameter-duplicate detection (byteArrayContains, Parser.maxon:558-560: "parameter lists are tiny... so no membership set is worth its heap object", citing FuncSignature.indexOfParam). NOT converted to a Set: a `Set with ParsedInt` would allocate a heap object PER MATCH — a NEW parse-phase allocation this very instrument tracks — to save a bounded scan, contradicting both that precedent and "don't micro-optimize". The codebase's convention is consistent: hash Set for unbounded token scans (assignedBindingsIn's AssignedNameSet), linear scan for small hand-written lists (params, match arms). GATES: specs-shv2 458/0 exit 0 memoryLeak false; match filter 60/0 exit 0; scale-test exit 0 (corpus grew x1.93..x1.99, floor x1.50); fragments/ clean, working tree clean — no codegen touched. | 21,213,218 | 42,241,984 | 88,773,911 | 180,655,160 | 370,568,145 | 760,356,270 |
| 2026-07-16 | P1.1b WAVE B — payload-free enum/union + Color.red + enum match + OPEN #21 ordinal ranges + union. NOTHING SUPERLINEAR: every phase x1.9..x2.2/rung, LINEAR, unchanged in shape from Wave A. SMALL POSITIVE DELTA vs Wave A (allocsDelta 136→212→364→668→1276→2492, bytesDelta 6304→...→118400), growing x1.9/rung = LINEAR, <0.05% of total. WHAT MOVED: the new EnumLayout PARSE-STAGING REGISTRY — one EnumLayoutArray per file on FileParseArtifact (create), one per file in the signature-index sweep (foldDeclaredSignaturesInto), one EnumLayoutMap per compile on Project — plus the extra kindAt lookahead keywordIsMatchCaseName adds to opensBlockAt/blockDepthAfter/blockStatementEndIndex (pure, no alloc). The corpus generates NO enums, so the enum PARSE/LOWER paths measure ZERO here (same instrument-blindness as match at Wave A); the delta is purely the empty per-file registry arrays a new parse-staging family costs — the identical per-file cost structs/aliases/bindings each already pay (StructLayoutArray, RangedTypeAliasArray, TopLevelDeclArray on the same artifact). NOT lazily allocated: structs do not lazy-allocate their per-file array either, and deviating would be an inconsistency to save a bounded constant. Mechanism verified by READING: enum lowering is a CHAIN of one condBranch per arm (O(arms), reusing Wave A's engine); the #21 range emits an OR (O(covered)) or a two-compare tag range with an O(cases) coverage scan — all linear in the enum's case count, which is a hand-written bound. GATES: specs-shv2 506/0 exit 0 memoryLeak false; enum filter 43/0; worker-count invariance byte-identical (j1==j12); fragments additions only, 0 modified; ErrorCode.g.cs + selfhosted registry unchanged. | 21,219,522 | 42,251,904 | 88,791,063 | 180,686,776 | 370,628,689 | 760,474,670 |
| 2026-07-16 | P1.2 WAVE A — string LITERALS + print + ==. NOTHING SUPERLINEAR: every phase x1.99..x2.20/rung, LINEAR, unchanged in shape from P1.1b (corpus code grows x1.93..x1.99). SMALL POSITIVE LINEAR DELTA vs P1.1b Wave B (allocsDelta 70->102->166->294->550->1062, bytesDelta 3344->...->50960, x1.9/rung = LINEAR, <0.02% of total). WHAT MOVED, and it is the ONLY thing that can: the corpus generates NO string literal, NO print, NO string == (same instrument-blindness the manifest documents for each un-generated construct), so the rung's feature paths — rdata record/blob registration, buffer fixup, __str_eq/__print_string install, stringLiteral/rdataAddr/osWriteStdout lowering — measure EXACTLY ZERO here. The delta is `scanRuntimeUsage` REPLACING `moduleUsesHeap`: the old scan EARLY-RETURNED on the first __mm_ call (the corpus allocates structs early), the new one must answer THREE runtime-usage questions (heap / print / streq) and so walks the whole module, paying the per-function/per-block loop cost for the functions the early return used to skip — O(program), linear, one scan per compile. This is the deliberate one-walk-not-three design the contract asked for; the price is <0.02% and linear. Mechanism verified by READING: __str_eq is a length check + a byte loop (O(bytes), a phi-carried index reusing the parser's on-the-fly-SSA loop shape); __print_string is straight-line (two loads + one osWriteStdout); both are installed ONLY when referenced, so a print-free/==-free program carries neither (verified: a print+== binary has __print_string/__str_eq and ZERO __mm_ symbols). GATES: specs-shv2 523/0 exit 0 memoryLeak false; string filter 15/0; worker-count invariance byte-identical (w1==w12); fragments additions only, 0 modified; error-code registry unchanged (no new codes). | 21,222,866 | 42,256,784 | 88,799,015 | 180,700,872 | 370,655,073 | 760,525,630 |
| 2026-07-16 | P1.2 WAVE A optimize — killed the ONLY superlinear the rung added: GlobalDataTable.registerRdataConstant computed each fresh .rdata constant's offset via rdataSizeBytes(), an O(entries) re-sum of every prior payload, so registering N distinct constants was O(N^2). A String literal registers TWO constants (blob + record) and literals-per-program are UNBOUNDED, so this is a real O(N^2) reachable at self-host scale (thousands of literals) — exactly the debt the field's own comment flagged 'if .rdata ever holds thousands of entries (P1.2 strings)'. Fix: replaced the re-sum with a running-total field rdataSizeSoFar advanced beside each push; O(N^2)->O(N), offsets bit-identical by construction, guarded by rdataImage's existing drift assert. INSTRUMENT-BLIND: the re-sum ALLOCATES NOTHING, so it is a compile-TIME cost this memory-only instrument cannot see, AND the corpus emits no strings — bounded by READING, not measured. The other three candidates are LINEAR by inspection: scanRuntimeUsage O(program) one walk; patchStringBufferFixups O(literals) one pass; __str_eq is runtime O(len), correct by nature. WHAT MOVED here: allocsDelta 0 every rung (count identical); bytesDelta +552,+808,+1320,+2344,+4392,+8488 = exactly 8*(1+functionCount) per rung. Attribution: the new 8-byte field rides on EVERY GlobalDataTable instance — the one project.globalData (phase:load, +8 constant) and each per-function FunctionCodeChunk.rdataConstants EMPTY placeholder that emitFunctionChunk allocates (phase:encode, +8*functions). Linear, +0.0026%->+0.0011% (shrinking fraction) — the constant-factor price of the tidy O(n) fix. GATES: build csharp/shv2 exit 0 zero warnings; specs-shv2 523/0 exit 0 memoryLeak false; worker-count invariance w1==w12 byte-identical; fragments additions-only 0 modified; error-code registry unchanged. | 21,223,418 | 42,257,592 | 88,800,335 | 180,703,216 | 370,659,465 | 760,534,118 |
| 2026-07-16 | P1.2 wave B — primitive interpolation (int/bool/String/expr) + statement-scoped temp-drop + sparse owned-heap provenance. The corpus emits NO interpolation, so it exercises the drop machinery's per-statement overhead, not the interp path itself. After guarding parseStatement's drain on pendingTempDrops.count()>0 (drainPendingTemps reallocates the list, so an unconditional per-statement call was one alloc on every line of the compiler), the residual delta vs P1.2 wave A is the two per-function ValueIdArray.create() calls in the parser's reset (pendingTempDrops + ownedHeapValues), O(functions), linear. All phase exponents ~x2.0 (linear); no superlinear introduced. | 21,239,234 | 42,284,480 | 88,849,367 | 180,796,536 | 370,841,361 | 760,893,166 |
| 2026-07-16 | value-class column grow-on-write (setValueClass growFilled) so it covers a fused compare's flags-only boolean when that is a function's highest ValueId; growFilled fires only for that shape and is amortized-linear. | 21,239,234 | 42,284,480 | 88,849,367 | 180,796,536 | 370,841,361 | 760,893,166 |
| 2026-07-16 | owned-heap provenance moved from a sparse list (ownedHeapValues, scanned once per binding by valueIsOwnedHeap — O(N^2) for a function with N bound interpolations) to a per-value bool column (valueOwnsHeap, seeded false in mintValue/recordValueType like valueConstKnown, set true in trackOwnedTemp, read O(1)). The delta here (+2.5KB@r0 .. +112KB@r5, doubling per rung => linear) is that one bool per minted value; it buys removal of the per-binding linear scan. Byte-identical codegen (564/0, fragments clean); parse stays x2.06. | 21,241,720 | 42,289,574 | 88,861,285 | 180,821,517 | 370,894,478 | 761,004,908 |
| 2026-07-16 | Block-scope ownership drops: parseBlockBody now emits __mm_decref for block-local owned bindings on fall-through, and break/continue drop down to the enclosing loop's owned height (LoopContext.ownedFloor), and emitScopeDrops gained a floor param. The corpus binds structs inside if/while bodies that PREVIOUSLY LEAKED (no drop was emitted; scale-test compiles but never runs, so it never flagged them). Emitting the now-correct drops adds compile-time memory that is LINEAR in the count of such bindings: allocs delta doubles per rung (+1,340@r0 .. +42,278@r5), phase:parse stays x2.06, regalloc x2.06. The one large byte move — regalloc:criticalEdges r5 +2.16MB — is +1 ALLOCATION (191,171->191,172), a single growable buffer crossing a power-of-2 doubling threshold on the slightly larger op stream, NOT superlinear growth. codeBytes rose too (the emitted programs now contain the decref calls). 577/0, memoryLeak false, fragments additions-only. | 21,294,578 | 43,074,931 | 89,115,781 | 181,518,847 | 371,935,430 | 763,266,624 |
| 2026-07-17 | P1.2 Wave C: static single-owner MOVES + use-after-move (OPEN #41). VarInfo gains a `movedFrom` bool; `let u = t` / `s = t` from a bare owned binding now MOVES (source poisoned, its drop skipped, a later read = E3102 use-after-move). Memory moved DOWN and it is EXPLAINED: `parseVariableReference` no longer resolves a local via `Scope.lookupValue`'s throw-on-miss — it checks `scope.contains` first — so a top-level binding reference (every global/constant read) no longer allocates AND catches a `ScopeLookupError.notFound(String.from(name))`. That is ~2 allocs saved per global read, LINEAR in the corpus's global reads: allocsDelta -192@r0 .. -6144@r5 (doubles per rung), bytesDelta -1536@r0 .. -63040@r5. No exponent moved (parse x2.06, regalloc x2.06; regalloc:criticalEdges x2.31 is the known power-of-2 buffer threshold, not superlinear). `poisonMoveSource` is an O(ownedBindings) scan per owned bind/assign that allocates nothing, so it does not appear here. 595/0, memoryLeak false, worker-invariant byte-identical, fragments additions-only. | 21,293,042 | 43,071,411 | 89,108,293 | 181,503,423 | 371,904,134 | 763,203,584 |
| 2026-07-17 | optimize P1.2 Wave C moves: gate the move-source poison on a bare-local RHS. `poisonMoveSource` was an O(ownedBindings) linear scan run on EVERY owned bind (parseBinding) and reassignment (reassignBindingValue) — including plain consumes (`let u = build()`) that scanned the whole list and found nothing → O(N^2) compile TIME for a function accumulating N owned bindings with ~N bind/assign ops. It now runs only when the RHS was a bare reference to a LOCAL (`let u = t` / `s = t`), the sole shape that can move an owned value out of an existing binding; a new O(1) parse-level gate `rhsWasBareLocalReference` (single-identifier-token + scope.contains + not isSelfField) decides, so every consume — the common case — skips the scan and the bind path is linear. BYTE-IDENTICAL: the scan is a proven no-op for any non-bare-local RHS, so gating it out changes no behaviour (595/0, memoryLeak false, fragments additions-only; the 4 move probes unchanged, incl. the loop-carried-var-as-move-source case which stays exit 0 "v1" — the reason the poison keeps its membership scan rather than switching to a valueIsOwned gate). Memory UNCHANGED at every rung (allocsDelta 0, bytesDelta 0): the scan and the gate allocate nothing, so this TIME fix is invisible to the memory instrument by design — recorded so the trend explains why an ownership-path commit moved no bytes. No exponent moved (parse x2.06, regalloc x2.06). Residual: an actual bare-local MOVE still scans (rare), and the assign path retains its pre-existing bindingIsOwned scan; a fully-O(1) poison would need either a second copy of owned-set membership (forbidden drift) or a valueIsOwned gate (alters loop-phi drop bookkeeping), so the gate is the correct scoping. | 21,293,042 | 43,071,411 | 89,108,293 | 181,503,423 | 371,904,134 | 763,203,584 |
| 2026-07-17 | OPEN #42 fix: use-after-move (E3102) now enforced at field access / field store / method receiver, not only bare reads. Consolidated the `movedFrom` check into ONE shared guard `Parser.requireBindingLive(binding, nameTok)` called from `parseVariableReference` (migrated) and `requireStructBase` (the single funnel for `p.x`, `p.x = …`, `p.m()`). Memory UNCHANGED at every rung (allocsDelta 0, bytesDelta 0): the guard is a pure front-end bool test on the base binding plus an early throw on the reject path — it allocates nothing, and the scale corpus generates no moved-then-used programs, so the guard never fires here. Recorded so the trend explains why a front-end-diagnostic commit moved no bytes. No exponent moved (parse x2.06, regalloc x2.06). | 21,293,042 | 43,071,411 | 89,108,293 | 181,503,423 | 371,904,134 | 763,203,584 |
| 2026-07-17 | P1.2 Wave D (String.append): +16 bytes FLAT at every rung, allocsDelta 0, no exponent moved (parse x2.06, regalloc x2.06 unchanged). The ONLY heap change the scale corpus sees is RuntimeUsage growing by two bool fields (usesStrAppend/usesStrDecref) — one struct per compile, so a constant +16 bytes independent of program size. The corpus generates no String.append and no var-string-literal, so __str_append/__str_decref and the eager-promotion path are never exercised here; their cost is measured off-instrument by the append + leak-gate specs (rc-string-concat-loop-no-leak: alloc/free balanced, exit 5, no leak). The drop-routing change (owned-String drops now call __str_decref, not __mm_decref) is a call-target swap that allocates nothing. | 21,293,058 | 43,071,427 | 89,108,309 | 181,503,439 | 371,904,150 | 763,203,600 |
| 2026-07-17 | P1.3 slice 1 (scalar-payload unions): allocsDelta -1 / bytesDelta -2 FLAT at every rung vs P1.2 Wave D, no exponent moved — every phase ratio ~x2.0 (parse x2.06, regalloc x2.06, signatures x2.0), all linear. The corpus is BOTH match-blind and union-blind (OPEN #30), so it exercises NONE of the new boxed-union paths (construct/emitEnumBox, the tag-load dispatch, payload extraction, the trivial-destructor drop) nor union `==` rejection; the flat -1/-2 is a one-time constant delta on the shared parse path, not a scaling change. The feature's superlinear surface was checked by reading, not by the corpus: every new scan (emitEnumBox validate/store/zero-fill, parseCasePayloadBindings, maxPayloadArity/isBoxed/boxSizeBytes, remapEnumLayoutPayloadTypes over cases x fields) is bounded by per-ENUM sizes (cases, arity, fields-per-case) — constants vs program size, so total work is O(program), linear. The implementer's two committed regression fixes are present and correct: (1) valueIsUnion's `named`-tag pre-check before enumMatchFor, which otherwise boxed two throwaway DeclaredEnum.undeclared per compare (had charged +1257/rung, doubling); (2) parseDottedPrimary's non-allocating containsEnum guard before enumOf, which otherwise boxed a DeclaredEnum per Point.create(...). OPTIMIZATION PASS: the one flagged residual — an empty PayloadBindingArray allocated per match arm — is REAL and was measured off-corpus with --metrics (1 alloc + ~40B per scalar arm; 2 per single-case enum arm from arm-loop + emitEnumPatternAtom): a 1000-arm scalar match dropped 998 allocs/39,904 bytes, a 200-case enum match 398 allocs/15,904 bytes, both frees in lockstep (leak-free). LEFT IN PLACE: the obvious fix (a shared empty-array sentinel on the Parser) REGRESSES the corpus-VISIBLE scaling path — Parser.create runs per top-level constant (SignatureIndex.walkConstInitializer, once per const-decl), so a sentinel allocated in the constructor adds one allocation per declaration; measured as an allocsDelta climbing +101/+173/+317/+605/+1181/+2333 across the rungs (~linear in program size, concentrated in phase:signatures), and reverted. A module-level singleton is unavailable (top-level const initializers may not call functions). The residual matches the existing per-arm matchEdgeArgs idiom in the same loop and costs nothing on the declaration-heavy path the instrument measures; removing it trades a real scaling cost for an unmeasurable off-corpus constant factor, so per the mandate it is left. Suite 615/0 exit 0 leak-free, fragments clean. | 21,293,056 | 43,071,425 | 89,108,307 | 181,503,437 | 371,904,148 | 763,203,598 |
| 2026-07-17 | P1.3 slice 2 (MANAGED payloads — String/struct in unions): allocsDelta FLAT +2 at every rung, bytesDelta +4,672 doubling to +133,632 (~linear). BOTH explained. (1) The +2 flat allocs are the per-compile empty `RuntimeUsage.neededUnionDestructors` ByteArrayArray (header + buffer) — one constant allocation regardless of program size; `noteUnionDestructorUsage`/`installUnionDestructors` iterate `project.enumTypes` once per compile and the corpus is union-blind, so their loops allocate nothing. (2) The doubling bytesDelta with FLAT allocs is the signature of a per-instance struct that grew: `VarInfo` gained one `partiallyMoved` bool (P1.3 slice 2 — a binding-match that moved a managed payload out is E3102-on-read but still dropped, distinct from `movedFrom` which skips the drop). VarInfos scale with bindings (~program size), so +8 aligned bytes each doubles per rung while the COUNT is unchanged — a necessary, minimal, linear cost of the feature. Every phase ratio ~x2.0 (parse x2.06, regalloc x2.06, liveness x2.06, all linear); nothing bent. ONE regression found + fixed BEFORE this measurement: `decrefCalleeFor` (the new drop router) called `enumMatchFor` on EVERY dropped owned value, boxing a throwaway `DeclaredEnum.undeclared` per struct/String drop — the exact hot-path allocation `valueIsUnion` documents; it had charged allocsDelta +42 doubling to +1282 (linear). Fixed with the same `tag == named` pre-check `valueIsUnion` uses (a union box is the only `named`-tagged drop; structs are `structRef`, interp buffers `integer`), collapsing that delta to 0. The feature's superlinear surface was checked by reading: the classifier (classifyUnionPayload) is 3 Map lookups per payload FIELD, bounded by per-union declaration size (cases x fields = constants vs program), and the destructor synthesis is per-union (once), so all work is O(program). Suite 631/0 exit 0 leak-free (630 + 1 loop probe not in suite); worker-invariance byte-identical; fragments additions-only. | 21,297,728 | 43,080,257 | 89,125,459 | 181,537,229 | 371,971,220 | 763,337,230 |
| 2026-07-17 | P1.3 slice 2 OPTIMIZE — killed an O(managed-unions x drop-sites) quadratic on the union-destructor install path; the corpus cannot see it, so it was found by READING and confirmed by a targeted probe. THE QUADRATIC: `RuntimeUsage.neededUnionDestructors` collected one entry PER `__destruct_<U>` DROP SITE (a `ByteArrayArray`, un-deduplicated), and BOTH consumers — `noteUnionDestructorUsage` and `installUnionDestructors` — walked EVERY union (`project.enumTypes`) and asked "is this union's destructor referenced?" via `bytesArrayContains`, a LINEAR SCAN of that per-drop-site list. That is O(managed-unions x drop-sites): both dimensions grow linearly with program size, so a program with many distinct managed unions is quadratic — the exact "linear lookup inside a linear walk" the mandate hunts, and the identical shape `IrModule.dropFunctionsByName` already fixed by interning its drop list into a Set. FIX: `neededUnionDestructors` is now a `Set with ByteArray` (new `UnionDestructorSet` typealias); `recordCallUsage` INSERTS (auto-dedup, <=U distinct entries not D duplicated), both consumers query `.contains` O(1), and the dead `bytesArrayContains` is deleted. Net O(unions x drops) -> O(unions + drops), linear. WHAT MOVED, AND IT IS A CONSTANT NOT A CURVE: allocsDelta +3 / bytesDelta +120 FLAT at every rung (rung0 == rung5), frees +3 in lockstep (leak-free). It is entirely the empty `Set` container replacing the empty `ByteArrayArray` in `RuntimeUsage.create()` — one per compile, does NOT scale, in no phase's per-rung count (every phase exponent unchanged: parse x2.06, regalloc x2.06, liveness x2.06). A flat constant traded for a growth-curve fix, the direction the mandate endorses ("constants are not the mandate; growth curves are"). INSTRUMENT-BLIND BY CONSTRUCTION: the corpus generates no enums AND no unions, so `project.enumTypes` is EMPTY and both consumers' loops iterate zero times here — the quadratic is unreachable by this ladder, which is why the win is invisible and the only measurable effect is the container swap. Confirmed correct off-corpus by (1) the full suite: 631/0 exit 0 leak-free, every union-managed-payload drop/leak-free test (the destructor-synthesis path) green; (2) a targeted probe — two DISTINCT managed unions (Msg/String, Note/String), Msg dropped at TWO sites — compiled and ran exit 7 with NO leak and NO missing-`__destruct_*` link error, proving both destructors synthesized, the dedup correct (one `__destruct_Msg` for two drops), and membership gating right. RESIDUAL LEFT, WITH A NUMBER NOT A FEELING: `decrefCalleeFor` re-runs `unionHasManagedField(layout)` (which allocates a `PayloadClass` box per classified field) at EVERY union-box drop site — a per-reference recompute of the classifier. It is realistically LINEAR (total = sum over drops of that union's declaration size = O(drops x bounded-decl-size)), superlinear only for a single pathological giant-union dropped many times; a per-compile managed-union memo would remove the recompute but is a constant-factor win on a linear curve (the mandate says don't chase it) on a correctness-critical path the instrument cannot measure, so it is left. GATES: build shv2 exit 0 zero warnings; specs-shv2 631/0 exit 0 memoryLeak false; git status specs-shv2/fragments/ EMPTY (0 modified, byte-identical codegen — a pure membership optimization emits the same destructors); probe exit 7 no leak. | 21,297,848 | 43,080,377 | 89,125,579 | 181,537,349 | 371,971,340 | 763,337,350 |
| 2026-07-18 | P1.3 fix (match owned-value flow, fa685a7a1) — INDEPENDENT OPTIMIZE READING, no code change. The corrective fix (temp-scrutinee enrollment + owned-`gives` transfer to the result phi) moves NOTHING on the ladder: every rung's allocs/bytes match the previous logged run EXACTLY (allocsDelta/bytesDelta 0 at all 6 rungs), all phases x2.0 (parse x1.99, regalloc x1.99, liveness x1.99 by rung4->5). Expected, and confirmed by READING not measurement: the corpus generates no unions and no `match` with an owned scrutinee/gives, so every new path is instrument-invisible. LINEARITY ARGUMENT (whole-program): all new scans walk per-STATEMENT or per-MATCH collections, never a program-sized one. (1) `pendingTempsContain(scrut)` and the scrutinee-enrollment `removeFromPendingTemps` run ONCE per match over `pendingTempDrops`, which is drained per statement (Parser.maxon:3330) -> its length is the statement-local owned-temp count, a source constant. (2) `resolveGivesOwnership` (two loops), the `finalGives` build, and the `finalizeMatchMerge` edge loop are each O(reachingArms), once per match; `valueIsOwned`/`tagOf` are O(1) column `.get`s. A match's arm count is a HAND-WRITTEN constant (the #32 E2027 O(P^2) precedent: bounded by a source constant is linear whole-program), so these sum to O(total arms) = linear. (3) `transferGiveToPhi` per gives-arm does `removeFromPendingTemps` O(pending) + `poisonMoveSource` O(ownedBindings); `ownedBindings` is bounded by the live owned bindings in the scope stack (a per-function constant, drained per block), so the whole-program cost is O(total gives-arms x bounded-bindings) = linear, the SAME class as the pre-existing move-poison sites. RESIDUAL LEFT (constant, not curve, with a reason not a feeling): `transferGiveToPhi` calls `poisonMoveSource` UNCONDITIONALLY for every owned give, unlike the pre-existing move sites which gate that O(ownedBindings) scan behind the syntactic `rhsWasBareLocalReference` check (the note at Parser.maxon:4179-4223 explains that gate exists precisely to skip the scan-and-find-nothing). For the common owned-TEMP give (an interpolation/struct box, which lives in `pendingTempDrops`, disjoint from `ownedBindings`) the poison scan is guaranteed to find nothing. This does NOT change the linear whole-program class (only the constant factor), and cheaply gating it would trade one bounded scan (`pendingTempsContain`) for another (`poisonMoveSource`) with no asymptotic gain and extra code on a correctness-critical path the instrument cannot see, so per the mandate ("constants are not the mandate; growth curves are") it is LEFT. GATES: build shv2 exit 0 zero warnings; specs-shv2 641/0 exit 0 memoryLeak false; scale-test exit 0, allocsDelta/bytesDelta 0 every rung. | 21,297,848 | 43,080,377 | 89,125,579 | 181,537,349 | 371,971,340 | 763,337,350 |
| 2026-07-18 | P1.4a Wave 1 — cross-call struct/union ownership (BORROW params + ADOPT returns, OPEN #44 closed). allocsDelta +238/+398/+718/+1358/+2638/+5198, bytesDelta +7384..+162136 (rung0..5); ratios 1.67 to 1.97 CONVERGING to x2.0 = LINEAR, tracking the corpus struct-count growth (structAllocs doubles per rung, same asymptote as emitted-code x1.93 to x1.99). NO superlinearity. WHAT moved: struct/union values crossing a CALL now ride the SAME owned-temp machinery String/union temps already use, extended to structs: (1) emitStructLiteral enrolls the box via trackOwnedTemp (a pendingTempDrops push) so an unbound Self{} / struct-literal-as-arg drops at statement end; (2) mintOwnedCallResult ADOPTS a struct/boxed-union call result via trackOwnedTemp so let p=makePair() frees it once; (3) parseReturnStatement's move-out now fires for owned struct/union returns (removeFromOwnedBindings + removeFromPendingTemps, each a filter-rebuild) so return t / return Self{} / return U.case(x) leave the callee's drop set. Each is an O(1) push or an O(bounded) rebuild PER struct/return; pendingTempDrops is per-STATEMENT and ownedBindings per-BLOCK (both bounded by source constants), so whole-program LINEAR — the SAME class as the pre-existing String/union owned-temp sites. resolveNamedStruct (per param/return/call, named to structRef) is allocation-free (nameOf returns a reference, containsStruct is a Map.contains) except when it constructs a structRef for a genuine named struct type, which the corpus never does (its structs are Self-returning factories, already structRef). structFieldIsManaged is answered inside SignatureIndex, contains-guarded and allocation-free (corpus-invisible: its struct fields are bare int, tag not named). GATES: build exit 0 zero warnings; specs-shv2 647/0 (641 baseline + 6 enabled) exit 0 memoryLeak FALSE; workers=1 equals workers=12 byte-identical; fragments additions-only (plus intended error.union-return to union-return rename); scale-test exit 0. | 21,305,232 | 43,092,753 | 89,147,939 | 181,579,677 | 372,053,604 | 763,499,486 |
| 2026-07-18 | P1.4a Wave 1 OPTIMIZATION PASS (independent) — NO CODE CHANGE. Verdict: no superlinear algorithm introduced; every new path is O(1) hashed-map/column or a loop bounded by a struct's hand-written field count (#28/#32 precedent), summing to whole-program LINEAR. Confirmed BOTH ways. (a) Static: resolveNamedStruct/All/Return (3 parse sites) is O(1) per struct reference — containsStruct is a Map.contains (hashed on the name bytes, a constant), typeNames.nameOf is an array index; the *All fold is bounded by a function's param count. structFieldIsManaged is O(1) per field (all hashed structTypes/enumTypes .contains/.get + a typeNames array index inside the index's own interner, no adoptType, no DeclaredStruct/DeclaredEnum box); rejectManagedStructFields is O(fieldCount) per construct site, fieldCount a per-struct declaration constant. valueIsBoxedUnion is O(1) — the exact mirror of valueIsUnion with the SAME named-tag hot-path allocation guard, called once per return and once per call result (short-circuited past when the tag is structRef). valueIsOwnedHeap is one BoolArray.get, strictly CHEAPER than the deleted valueIsOwned (which added a tagIsStructRef OR). (b) Measured, allocsDelta 0 on every rung — this tree reproduces the implementer's logged run bit-for-bit, so I measured exactly the committed state: total allocs x1.906/1.950/1.974/1.987/1.993, phase:parse (where resolveNamedStruct + emitStructLiteral trackOwnedTemp + mintOwnedCallResult adoption + the return move-out live) x1.913/1.952/1.974/1.986/1.993, phase:signatures (structFieldIsManaged reads) x1.815/1.896/1.943/1.969/1.984 — every phase converges to x2.0 MONOTONICALLY FROM BELOW (the fixed-per-rung-overhead-amortizing signature); nothing overshoots, so no phase bends superlinear. TWO residuals LEFT deliberately, both constant-factor and neither superlinear: (1) rejectManagedStructFields calls structFieldIsManaged(name, i) per field, which re-fetches the SAME layout by name (structTypes.get) once per field instead of once per struct — a CPU redundancy bounded by the struct's declared field count (#28/#32), that allocates nothing and is invisible to this memory instrument; classification must stay inside SignatureIndex because it reads that index's private typeNames/enumTypes interner. (2) resolveNamedStructAll allocates one throwaway MaxonTypeArray per function even when no param is a struct — a linear per-function constant already inside the logged +238..+5198 delta. Per the mandate (constant factors are not the target; a tidy O(n) beats a clever one), both are left as-is. Gates re-run on this tree: build exit 0 zero warnings; specs-shv2 647/0 exit 0 memoryLeak FALSE; git status clean, fragments unmoved. | 21,305,232 | 43,092,753 | 89,147,939 | 181,579,677 | 372,053,604 | 763,499,486 |
| 2026-07-18 | P1.4a wave 2 (managed struct fields + destructor cascade + param-consume analysis + chained field access) | 21,329,963 | 43,134,864 | 89,224,658 | 181,725,452 | 372,337,387 | 764,059,269 |
| 2026-07-18 | P1.4a wave 2 OPTIMIZATION PASS (independent) — NO CODE CHANGE. Verdict: nothing superlinear over a compiler-sized collection; deltas 0 at every rung vs the implementer's own wave-2 row, confirming the same binary. The CONSUME ANALYSIS is genuinely corpus-exercised, not inert: every generated struct's static create(x,y) is a receiver-less function whose body is Self{x:x,y:y}, so scanConsumeBits records it with bits[true,true], recordParamConsume folds it into the parse-memo hash, and every create call site runs applyCallerConsume's positional+labelled slotting — and all of it stays linear: phase:signatures x1.99 (the added body walk + colon lookups, each colon O(this func's own param count), total O(total tokens)), phase:merge x2.00 (foldFile threads paramConsumes by name, no interner remap), phase:parse x2.06 (applyCallerConsume/transferConsumedArgs slot O(call arity) per consuming call; resolveFieldChain recurses O(chain depth); bindParameters O(params) with O(1) paramIsConsumed+valueIsManagedHeap). Every per-reference cost is bounded by ONE declaration's or call's own size — the #28/#32 precedent — so it is O(program), linear. The DESTRUCTOR CASCADE CLOSURE is NOT corpus-exercised: corpus structs are scalar-only (x int, y int), so noteDestructorUsage/closeDestructorNeeds see an empty needed-set and phase:runtimeAugment reads x1.57 flat AND allocation-free — the instrument structurally cannot see this path's scaling. Confirmed linear by STATIC reasoning instead: noteDestructorUsage is called ONCE per compile (Compiler.maxon:168), never per drop-site/reference; closeDestructorNeeds is a MONOTONE fixpoint whose pass count is bounded by struct NESTING DEPTH (a source constant — a flat set of managed structs converges in 1 pass, only struct-in-struct nesting adds passes), each pass O(types x fieldCount), so O(depth x types x fieldCount) = O(program) once. The theoretical worst case (one maximally-nested chain, depth ~= types → O(types^2)) needs a degenerate program, is allocation-light (invisible here), and is bounded by source structure exactly like #28/#32 — LEFT in place; a worklist closure would be a micro-optimization on an unmeasurable path with behavior risk, which the mandate forbids. regalloc:criticalEdges x2.31 is pre-existing regalloc (untouched by this wave, delta 0). Gates: shv2 suite 657/0 exit 0 memoryLeak FALSE; specs-shv2/fragments clean; build 0 warnings. | 21,329,963 | 43,134,864 | 89,224,658 | 181,725,452 | 372,337,387 | 764,059,269 |
| 2026-07-18 | P1.4a-wave-2 CORRECTIVE: path-sensitive conditional-move DROPS. Verdict: LINEAR, nothing superlinear on the realistic corpus. The fix reconciles VarInfo.movedFrom at every if/if-else/match join (captureMoveMark + restoreMoveMark + reconcileMovesAtMerge, mirroring snapshotBindings/restoreBindings for the drop-gating bit) and rejects an outer-binding move that escapes a loop. Measured constant-factor increase vs the wave-2 baseline (row 201): total bytes +133,312/+266,240/+532,096/+1,063,808/+2,127,232/+4,254,080 across rungs 0-5, each DOUBLING with the doubling corpus, so it is a LINEAR per-branch cost (parse allocs x1.99, parse bytes x2.06 — identical curve to the baseline's x2.06). The cost is the MoveMark capture: captureMoveMark walks ownedBindings (O(owned-in-scope)) and allocates BoolArray+ValueIdArray, called ~2-3x per if branch and once per reaching match arm. On the corpus the branch-heavy functions (longFunction/deepBlocks) carry only SCALAR accumulators, so ownedBindings is ~0 there and each capture is O(1)/empty — the instrument sees the empty-array per-branch overhead, which is O(branches)=O(program), linear. The THEORETICAL worst case is a single function with N owned Strings AND N branches, where per-branch O(owned) sums to O(N^2); it needs a degenerate function, is bounded by source structure exactly like the #28/#32/destructor-cascade precedents, and the corpus (scalar-only struct fields, scalar branch accumulators) structurally cannot exercise it — LEFT in place per the mandate (a tidy O(n) beats a clever delta-tracker; an empty-mark cache is a constant-factor micro-opt with added parser state). Gates: build exit 0 zero warnings; shv2 suite 670/0 exit 0 memoryLeak FALSE; workers=1==workers=12 byte-identical; fragments additions-only. | 21,463,275 | 43,401,104 | 89,756,754 | 182,789,260 | 374,464,619 | 768,313,349 |
| 2026-07-18 | P1.4a wave 2 CORRECTIVE OPTIMIZATION PASS (independent, over 2bee3d9b8 + 411c80338) — NO CODE CHANGE. Verdict: nothing superlinear over a compiler-sized collection; allocsDelta/bytesDelta 0 at every rung vs the implementer's own corrective row, so this tree reproduces that binary bit-for-bit and I measured exactly the committed state. The new path is path-sensitive drop reconciliation: captureMoveMark/restoreMoveMark snapshot+rewind the movedFrom bit of every owned binding around a branch, and reconcileMovesAtMerge settles it at each if/if-else/match join. All of it rides ownedBindings, which is a SCOPE-DRAINED STACK — openBlock records its height (Parser.maxon:3745), closeBlock pops back to that mark (3766) — so it is bounded by the CURRENT FUNCTION'S nesting depth, never a compiler-sized accumulator, and is fully drained when the function's outermost block closes. Per-op costs: captureMoveMark/restoreMoveMark O(owned-in-scope) per branch (~2-3x per if, once per fresh match arm); reconcileMovesAtMerge O(owned x edges) per merge (edges=2 for if, =reaching-arm-count for match — both SOURCE CONSTANTS); thenMovesLiveBinding O(owned) once per if-no-else; bindingIndexEscapesLoop O(1) (top-of-loop-stack ownedFloor compare). This is the declared TWIN of the pre-existing snapshotBindings/restoreBindings machinery (same O(carried x branches) shape over assignedBindings), so it inherits that design's accepted linearity: every per-branch/per-merge cost is bounded by one function's own structure, summing to O(program) = LINEAR. The THEORETICAL worst case is one degenerate function with N owned bindings AND N nested branches, where per-branch O(owned) sums to O(N^2) — bounded by source structure exactly like the #28/#32 precedents, and the corpus (scalar branch accumulators in longFunction/deepBlocks, so ownedBindings is ~0 there; scalar-only struct fields) structurally cannot exercise it. The empty-array-per-branch allocation (captureMoveMark allocates BoolArray+ValueIdArray even when ownedBindings is empty, like Slice-1's throwaway array) is a per-branch CONSTANT FACTOR = O(branches) = O(program), linear, and is already inside the corrective's logged +133,312..+4,254,080-byte delta; LEFT per the mandate — removing it needs a shared empty-mark singleton, i.e. a fast-path fork the mandate forbids, and constant factors are not the target (a tidy O(n) beats a clever cache with added parser state). Readings: phase:parse allocs x1.92/1.96/1.98/1.99/1.99 CONVERGING to x2.0 from below (the fixed-overhead-amortizing linear signature), phase:parse bytes STABLE x2.06 across every rung (a stable ratio is linear; a superlinear bends UPWARD rung to rung — it does not). regalloc:criticalEdges x2.31 is pre-existing regalloc (untouched by this rung, delta 0 — the known power-of-2 buffer threshold). Gates re-run on this tree: build exit 0 zero warnings; specs-shv2 670/0 exit 0 memoryLeak FALSE; no code change so codegen is byte-identical by construction and specs-shv2/fragments is unmoved; git status clean. | 21,463,275 | 43,401,104 | 89,756,754 | 182,789,260 | 374,464,619 | 768,313,349 |
| 2026-07-18 | P1.4b WAVE 1 (scalar error handling) OPTIMIZATION PASS (independent, over bbbd9d74b) -- NOTHING SUPERLINEAR, NO CODE CHANGE. The delta THIS ROW PRINTS (+6,570..+201,439 allocs / +174,704..+5,124,336 bytes) is a STALE-BASELINE ARTIFACT, not the rung's cost: the prior logged row's absolutes (350,561 / 10,186,012) are LOWER than what the rung's own PARENT (287ba1cdb, P1.4a wave 2 CLOSED) actually produces when rebuilt from source (357,096 / 10,387,292) -- the OPEN #22 / 2026-07-16 stale-bin trap, where a logged baseline measured a compiler that no longer exists. So I A/B'd it the immune way: ONE bootstrap (bin/maxon.exe) built BOTH shv2 binaries -- parent 287ba1cdb (registry hash fb8d0957a4a3bbd3) and branch bbbd9d74b (hash c773be82c0fbcdea) -- the only variable the rung's own diff. TRUE cost, branch minus parent: ALLOCS +35 +39 +47 +63 +95 +159 (rung 0..5); BYTES +53,928 +104,888 +206,808 +410,648 +818,328 +1,633,688. Both LINEAR (each doubles across the doubling ladder). The rung's real allocation footprint is +159 at rung 5 = 0.0015% of total, NOT the 1.94% this row's stale delta implies -- the implementer's "~1.8%/x1.97" self-report inherited the stale baseline; the A/B corrects it. SPLIT, exactly: alloc delta = phase:load +5 (const) + phase:signatures +19..+143 (= 15 fixed + 4*2^rung, the throws-clause plumbing threaded into the declaration SWEEP/fold -- one ThrowsClauseEntryArray per file plus the fold's iterator over it, empty here because no fn throws) + phase:semanticCheck +5 (const: buildThrowsMap builds one program-wide map, checkMainCannotThrow one scan) + phase:lowerMaxonToStd +1 (const). BYTES delta is dominated by phase:parse +51,832..+1,609,768 at ZERO extra allocations -- the SAME IrFunction objects made WIDER by the new inline throwsTypeName ThrowsClause field, a per-function fact that is inherent and unavoidable (parse alloc COUNT delta is exactly 0). FREES track allocs to the unit at every rung (+19..+143 signatures etc.) -- no leak, specs-shv2 memoryLeak FALSE. WHY nothing is superlinear, structurally not just measured: throwsOf/throwsMap.contains/funcSignatures.get are O(1) hashed Map lookups (funcThrows records THROWING fns only, so a non-throwing callee costs no entry and contains beats a boxing get-otherwise-none); buildThrowsMap + checkMainCannotThrow are each ONE O(functions) pass built once, and checkCalls does an O(1) contains per call, so semanticCheck is O(ops) not O(calls x functions); the parser try/otherwise desugar rides the PRE-EXISTING if/while binding machinery (snapshotBindings/restoreBindings/mergeAtContinuation/emitScopeDrops -- unchanged by this rung) at the same per-construct cost, and parsePostfixCast/evalConstPostfix are token-bounded while-loops; SsaDestruction colorAndDropSelfMoves stays a SINGLE linear pass -- the tryCall two-result capture is found by an O(1) prevWasCall + one-op lookahead with a skipNext guard, never a nested scan; lowerTryCall / X64+Arm64 lowerTryCall slot args O(args) exactly like lowerCall; FoldConstOperands only widened a match range (no loop). The scale-test corpus contains ZERO try/throws/otherwise/throw/panic (rung 0 = 56 fns, 0 error keywords), so the entire measured cost is the ALWAYS-ON plumbing -- confirming the design goal (only throwing fns cost hot allocations) holds, and the residual is the wider IrFunction, inherent. Every phase alloc RATIO on the branch is <= x2.00. No fast-path fork, no inlined helper: nothing to change. RECORDING this run re-anchors the logged baseline to the branch's TRUE absolutes (357,131 / 10,387,451, which the A/B confirms bit-for-bit) so the next rung's printed delta is honest again. Gates on this tree: build exit 0 zero warnings; specs-shv2 718/0 exit 0, no 101; git status clean, specs-shv2/fragments unmoved (no code change, codegen byte-identical by construction). | 21,637,979 | 43,735,520 | 90,410,498 | 184,081,660 | 377,034,331 | 773,437,685 |
| 2026-07-18 | P1.4b WAVE 2a (managed error VALUES — boxed-union throw/catch/drop) OPTIMIZATION PASS (independent, over 48238c2aa) -- NOTHING SUPERLINEAR, NO CODE CHANGE, and the true cost on this corpus is EXACTLY ZERO. Confirmed the immune way, an A/B against the PARENT rather than the logged baseline (the OPEN #22 stale-bin trap this log has now been bitten by twice): ONE bootstrap (bin/maxon.exe) built BOTH shv2 binaries from source -- the parent's three compiler files (Parser.maxon + LowerMaxonToStd.maxon + MaxonDialect.maxon at e1902ee10, Wave 1 CLOSED) and the branch's (48238c2aa) -- the only variable the rung's own diff. The two binaries are GENUINELY DIFFERENT (parent code 4,325,306 bytes, branch 4,329,960) yet produce BIT-FOR-BIT identical scale-test memory at all six rungs: allocs 357,131 / 681,101 / 1,328,537 / 2,622,663 / 5,210,702 / 10,387,451; bytes 21,637,979 .. 773,437,685. So the always-on plumbing Wave 2a adds costs 0 allocations / 0 bytes -- and the tool's own delta column agrees (0 at every rung vs row 207, which the branch reproduces exactly). WHY it is free here: the scale corpus has ZERO error-handling keywords (rung 0 = 0 throw/try/otherwise/catch), so NOT ONE throwError op, otherwise-handler or box-drop is ever built; the dialect field swap (throwError's dead `errorType MaxonType` -> live `errorIsBoxed bool`) only touches an op the corpus never emits, and the sole hot-path touch is `moveOutForExit`, called on EVERY `return` -- but it is a byte-identical factoring of the inline move-out that already lived in parseReturnStatement (`let saved = ownedBindings; if valueIsOwnedHeap: removeFromOwnedBindings + removeFromPendingTemps; return saved`), so it allocates nothing new and changes no count (measured: parse allocs/bytes identical to parent at every rung). WHY nothing is superlinear, structurally not just measured: (1) `throwsTypeIsBoxedUnion` -- the ONE fact the error-flag ABI turns on, read at 3 sites (throw encode + both catch decodes) -- is O(1): a tag compare, then `typeNames.nameOf` (an interner array-index) and `signatures.enumOf(name)` (a hashed Map.get; the layout it returns carries plain names/ints, no per-union scan). (2) `retypeValue` is two O(1) indexed column `.set`s; `markValueOwnsHeap` one O(1) `.set`; `enrollCaughtBox` = retypeValue + mark + one amortized `ownedBindings.push`, all O(1) per catch site, NO loop over the function's bindings or temps. (3) the uniform box-drop across every otherwise-form rides the PRE-EXISTING per-site machinery unchanged -- `dropArmScopedOwned` -> `emitScopeDrops(floor)` walks only the owned bindings ABOVE the handler's floor (O(live owned in this handler), the size of the emitted drops, not a program-sized scan) exactly ONCE per catch, and `decrefCalleeFor` keeps its `named`-tag hot-path guard so a non-union drop never boxes a throwaway DeclaredEnum. Every per-site cost is bounded by ONE throw/catch's own structure, summing to O(program) = LINEAR -- the #28/#32 precedent, the same class as P1.4a's owned-temp sites. And the change REDUCES duplication rather than forking a scan: moveOutForExit factors the return/throw move-out into one helper, parseUnboundOtherwiseForm splits the discard-forms out so the caller brackets them with one enrol/drop, and enrollCaughtBox is ONE enrolment shape for every catching form -- no fast-path fork, no helper inlined at N sites. Every phase alloc RATIO is <= x2.00 (criticalEdges x1.98, splitting x2.01, coloring x1.99, liveness x1.98; the BYTES-column ratios slightly above 2.0 -- criticalEdges x2.31, elimTrivialBlockArgs x2.11 -- are the pre-existing power-of-2 buffer-threshold artifacts, delta 0 vs parent, untouched by this rung). Nothing to change. Gates on this tree (final Wave-2a binary): build exit 0 zero warnings; specs-shv2 725/0 exit 0 at --workers=12, no 101; git status clean, specs-shv2/fragments/ EMPTY (no code change -> codegen byte-identical by construction); scale-test exit 0. | 21,637,979 | 43,735,520 | 90,410,498 | 184,081,660 | 377,034,331 | 773,437,685 |
| 2026-07-18 | wasm32-wasi scalar backend rung (StdToWasm/WasmBinary/WasmComponent + BackendDispatch/CodeResult/Main/SpecRunner) + its independent OPTIMIZATION pass. Re-baselines the log onto current main; the previous row's baseline predates several main merges. ATTRIBUTION of the +17,055..+456,650 alloc delta the instrument reports vs the previous row: it is NOT this rung. Measured by isolating HEAD vs this rung's base 964b6e149 (rebuilt shv2 only, C# bootstrap untouched by the rung): the rung's ENTIRE host-path memory effect is +1 allocation / +48 bytes per COMPILE — identical at every rung (rung0 367,616->367,617; rung5 10,642,662->10,642,663; +48 bytes everywhere), landing wholly in phase:link. That is the single empty `wasmCoreModule: ByteArray.create()` field this rung added to CodeResult.create (one CodeResult per compile) — a per-compile CONSTANT, not linear and not superlinear in program size (unchanged from 64KB rung0 to 2MB rung5). LEFT as-is: it is O(1)/compile and removing it would need a union/optional field (a fork the mandate forbids for a constant). The remaining ~17k..456k of the instrument's delta is prior-main (throwing-division 8c79208ec, arm64 fragment/Wave-2 sync fce8fb829) — all below 964b6e149, already in this rung's base, so this row merely folds them into the trend. OPTIMIZATION applied this pass (WasmBinary.internFuncType): the func-type interner deduped by a linear scan over every previously-interned type — a findFirst-over-an-array run ONCE PER DEFINED FUNCTION, i.e. O(N functions x D distinct signatures), up to O(N^2) on a large scalar program (the wasm backend is a shipping path: `--target=wasm32-wasi` accepts arbitrarily large scalar programs, not just test snippets). Replaced with a bytes-keyed dedup Map (FuncTypeDedupMap), O(1) per intern, mirroring GlobalDataTable.valueDedup which fixed the identical O(N^2) for .rdata constants. The map returns the first-interned index for equal bytes exactly as the scan did, so the emitted type section is BYTE-IDENTICAL: specs-shv2 --target=wasm32-wasi 90/0 exit 0 (no leak). This optimization is INVISIBLE to scale-test by construction — scale-test compiles the corpus for the HOST, never --target=wasm32-wasi, so it never reaches the wasm encoder; the host numbers in this row are bit-identical to the un-optimized rung's host numbers, which is the correct evidence that the change is host-neutral. NOT superlinear-in-reachable-input and LEFT: StdToWasm.sequenceParallelCopy's parallel-move has an accidental extra O(n) factor (findLeafMove->isAnySource is O(n) called O(n) times = O(n^3) vs the standard O(n^2)), but n is a single edge's phi-move count and the standard algorithm is O(n^2) regardless; fixing the extra factor needs source-count bookkeeping threaded through the swap-rewrite of correctness-critical phi sequencing, poor risk/reward. buildValtypeTable/resolveDependents/widthOfValue is memoized (the `resolved` set) so each dependent is walked once = O(values), linear; the loop+br_table dispatcher and WasmBinary LEB128/section assembly are all linear. Gates on this tree: build exit 0 zero warnings; specs-shv2 host (arm64-macos) 668/0 exit 0 no leak; specs-shv2 wasm32-wasi 90/0 exit 0 no leak; git status shows only WasmBinary.maxon (+ this log row); specs-shv2/fragments clean (host codegen byte-identical). | 23,323,947 | 45,731,334 | 94,352,830 | 191,199,467 | 390,202,400 | 798,465,472 |
| 2026-07-18 | P1.4b Wave 2b (managed RETURN — tryCall-result reconcile + OPEN #49 gate removal). Corpus has no error handling, so 0-delta expected; optimizer confirmed nothing superlinear (O(1)/statement-local per tryCall). | 21,638,027 | 43,735,568 | 90,410,546 | 184,081,708 | 377,034,379 | 773,437,733 |
| 2026-07-18 | PE .rsrc manifest (asInvoker, ported from v1) — a FIXED +22 allocs / +2,422 bytes at every rung (a constant one-per-compile .rsrc section built once), NOT superlinear. Opts every emitted exe out of Windows UAC installer-detection; supersedes the test-only exe rename. | 21,640,449 | 43,737,990 | 90,412,968 | 184,084,130 | 377,036,801 | 773,440,155 |
| 2026-07-18 | OPEN #54 Slice A (aggregate type-identity check) OPTIMIZATION PASS (independent) — NO CODE CHANGE. Immune A/B (one bootstrap, both shv2 binaries: parent 524981047 vs HEAD 78a937a0d): allocs 0 AND bytes 0 at every rung. The name-check reads the dense scalar `valueTags`/`valueNameIds` columns via `aggregateNameOf(tag, nameId)` (array-index + `Map.contains`, no scan, no `MaxonType` materialized) and returns the shared `EmptySourceText` for the scalar case, so a scalar-returning function allocates nothing; `String.from(...)` is confined to the throw branch. `requireManagedValueMatches` now shares that pair and drops its prior `self.typeOf()` union allocation (corpus-invisible). Each helper runs once per return/assign/otherwise — O(program), linear. | 21,640,449 | 43,737,990 | 90,412,968 | 184,084,130 | 377,036,801 | 773,440,155 |
| 2026-07-18 | OPEN #54 Slice B (aggregate type-identity at the CONSUMING call-arg site) OPTIMIZATION PASS (independent) — NO CODE CHANGE. Unlike Slice A (which read existing columns, 0 delta) this rung PUBLISHES a new dense per-value column `valueNameIds` on IrFunction (mirror of `valueTags`), so it has a REAL, LINEAR cost. Immune A/B (one bootstrap bin/maxon.exe, both shv2 binaries: parent f3d9c7f65 vs HEAD aa2ac2509): allocsDelta +612/+1,142/+2,202/+4,318/+8,548/+17,002 (delta ratios 1.87 1.93 1.96 1.98 1.99 -> x2.0, LINEAR); bytesDelta +22,856/+41,224/+89,384/+180,704/+416,280/+893,840 (geometric mean x2.09 over the 32x ladder — LINEAR, nowhere near x4). PER-PHASE ATTRIBUTION (parent vs HEAD, exact): the ENTIRE allocs delta lands in phase:merge (14,106 -> 14,712 at rung 0, x1.99) and it is `remapArtifact` rebuilding the published column on the MULTI-FILE (non-identity) remap path — files 2..N of each rung, which the corpus reaches because its file-count knob doubles per rung — one TypeNameIdArray.create() per function plus one transient MaxonType union per value via `remapValueNameId` (maxonTypeOfTag -> remapMaxonType -> typeNameIdOf, the deliberate filler-safe round-trip that avoids a second copy of which tags bear a name; left as-is, a hardcoded tag predicate would duplicate that classification). BYTES split two linear ways: phase:parse +440 -> +8,376 is the new `valueNameIds` pointer field widening each IrFunction record by 8 bytes x functions (440/8 = 55 funcs at rung 0) — the retained-column FIELD cost; phase:merge +22,128 -> +885,176 (dominant) is the rebuilt column plus those transient unions. UNCHANGED: phase:parse ALLOCS byte-identical (68,466 both) — parent already filled `self.valueNameIds` per function, so publishing it by-reference extends the column LIFETIME whole-compile, not the cumulative alloc COUNT; phase:semanticCheck byte-identical (566 allocs / 16,236 bytes both) — the new checkArgTypes struct-identity branch reads `valueNameIds` only for a STRUCT parameter and the corpus has none (structs never cross a call boundary — ScaleCorpus manifest). NOTHING SUPERLINEAR: checkArgTypes' branch is O(1) per call-arg (array-index get + typeNames.nameOf + bounded namedAggregatesConflict compare) and remapValueNameId is O(1) per value (match + remap.apply array-index) — no scan, no nested walk, nothing indexed linearly. specs-shv2 743/0, fragments byte-identical, no leak. | 21,663,305 | 43,779,214 | 90,502,352 | 184,264,834 | 377,453,081 | 774,333,995 |
| 2026-07-19 | OPEN #56 fix (destructor-needs closure reads the PROJECT struct layout, not a signatures re-fetch) OPTIMIZATION PASS (independent) — NO CODE CHANGE by the optimizer. The fix swaps `addNestedStructDestructors` per-struct `requireDeclaredStruct(signatures, layout.name)` re-fetch for the PROJECT `layout` that `closeDestructorNeeds` already holds while iterating `project.structTypes`, REMOVING one O(1) `signatures.structOf` = `structTypes.get` name-hash + map lookup AND one `DeclaredStruct` union construction per struct-with-a-needed-destructor per fixpoint pass. Immune A/B (one bootstrap bin/maxon.exe, both shv2 binaries: parent 6d2694e93 vs HEAD f42e2c00c, both built by the freshly-rebuilt bootstrap): allocs 0 AND bytes 0 at EVERY rung (357,766 / 682,266 / 1,330,762 / 2,627,004 / 5,219,273 / 10,404,476 allocs and 21,663,305 / 43,779,214 / 90,502,352 / 184,264,834 / 377,453,081 / 774,333,995 bytes, identical). WHY 0: the changed function is UNREACHABLE by the corpus — ScaleCorpus structs are scalar-only (x int, y int) with no managed field, so noteDestructorUsage seeds an EMPTY neededDestructors set, the closeDestructorNeeds guard `neededDestructors.contains(destructorSymbol(layout.name))` is false for every struct, and addNestedStructDestructors never runs (same structural blind spot as OPEN #54 Slice A/B). NOTHING SUPERLINEAR (static, since the instrument cannot see the path): the fixpoint shape is UNCHANGED — closeDestructorNeeds still iterates project.structTypes / enumTypes once per pass, pass count bounded by struct NESTING DEPTH (a source constant; a flat set of managed structs converges in 1 pass), each pass O(types x fieldCount) = O(program) once. The edit strictly REMOVES a constant-factor per-struct O(1) lookup + union alloc from the exercised (nested-managed-struct) path; no scan is added. Emitted code shrinks 264 bytes (4,529,438 -> 4,529,174) from dropping the requireDeclaredStruct call. specs-shv2 744/0, fragments additions-only (no M), no leak (exit 0). | 21,663,305 | 43,779,214 | 90,502,352 | 184,264,834 | 377,453,081 | 774,333,995 |
| 2026-07-19 | OPEN #52 fix (union-payload interner re-intern doors) OPTIMIZATION PASS (independent) — NO CODE CHANGE by the optimizer. The fix adds TWO re-intern doors so a signatures-layout payload id is resolved against the interner that MINTED it, not the parser's own: Door A exports `adoptType` (re-intern a payload's `named`/`structRef` id BY NAME into a caller interner) and routes 3 parser union-payload sites through it (requirePayloadLowerable + declarePayloadBindings classify, emitUnionPayloadLoad value-type mint); Door B adds `ProgramSignatures.unionBoxDropCallee(layout)` = `unionNameDropCallee(self, self.typeNames, layout)` and routes `decrefCalleeFor`'s union branch through it, resolving the drop-callee over the SIGNATURES interner instead of the artifact interner. Immune A/B (one bootstrap bin/maxon.exe, both shv2 binaries: parent e06879d98 vs HEAD 6ae9b56cd, both built by the freshly-rebuilt bootstrap): allocs 0 AND bytes 0 at EVERY rung, every per-phase alloc/byte row bit-for-bit identical (357,766 / 682,266 / 1,330,762 / 2,627,004 / 5,219,273 / 10,404,476 allocs and 21,663,305 / 43,779,214 / 90,502,352 / 184,264,834 / 377,453,081 / 774,333,995 bytes). WHY 0: the scale corpus is scalar-only and union-payload-blind (ScaleCorpus manifest — no `match`, no payload-bearing union constructed or dropped), so NONE of the changed sites runs; the delta is 0 by construction, not by luck. NOTHING SUPERLINEAR (static, since the instrument cannot see the path): Door A `adoptType` is a `match` + one `nameOf` (array index in the signatures interner) + one `intern` (bytes-hash into the artifact interner) = O(1) per union-payload USE, added at 3 sites each already visited once per payload use (declarePayloadBindings loops per match-arm binding, a source constant), so per-use cost stays O(1) and whole-program stays O(union-payload uses) = linear. Door B is the SAME loop the parent ran inline: `unionBoxDropCallee` -> `unionNameDropCallee` -> `unionHasManagedField` iterates a union's cases x payloads (bounded by that union's own declared arity, a source constant) once per union-value DROP; the parent already ran `unionHasManagedField` at that exact site, so no scan over a program-sized collection was added and the drop-path complexity is unchanged (O(bounded-decl-size) per drop, O(program) whole-program). No fast-path fork, no inlined helper — nothing to change. Emitted code is byte-identical for existing programs (the doors change resolution, not codegen). specs-shv2 748/0 exit 0 memoryLeak FALSE; git status specs-shv2/fragments/ no M (additions-only; one pre-existing untracked stray ignored); build exit 0 zero warnings. | 21,663,305 | 43,779,214 | 90,502,352 | 184,264,834 | 377,453,081 | 774,333,995 |
| 2026-07-19 | OPEN #45 / #54 Slice C (value-position `match … gives` type-agreement check) OPTIMIZATION PASS (independent) — NO CODE CHANGE by the optimizer. The rung adds ONE flat per-reaching-arm loop in `finalizeMatchMerge` (Parser.maxon:6749, `checkGiveTypes`) that compares each reaching arm's `gives` value type to the FIRST arm's — scalar-class via `typesAgree`, aggregate-ness via `aggregateNameOf` emptiness, both-aggregate via `namedAggregatesConflict` — throwing E3005 (`giveTypeMismatch`) on disagreement so an ill-typed result phi is refused before it is built (closing a String-read-off-int / int-derefed-as-box segfault and a two-different-struct wild-free). Immune A/B (one bootstrap bin/maxon.exe, both shv2 binaries: parent 2430ef2dc vs HEAD 261b21fc1, both built by the freshly-rebuilt bootstrap): allocs 0 AND bytes 0 at EVERY rung, and the per-phase ALLOCATIONS and BYTES tables are bit-for-bit IDENTICAL parent vs HEAD (357,766 / 682,266 / 1,330,762 / 2,627,004 / 5,219,273 / 10,404,476 allocs and 21,663,305 / 43,779,214 / 90,502,352 / 184,264,834 / 377,453,081 / 774,333,995 bytes). WHY 0: the scale corpus contains NO `match` at all (ScaleCorpus manifest, Testing/ScaleCorpus.maxon:82 — "no `for`, no `match`"), so `finalizeMatchMerge` and the new `checkGiveTypes` loop are never reached; the delta is 0 by construction, not by luck — the same structural blind spot as OPEN #54 Slice A/B, #56 and #52. NOTHING SUPERLINEAR (static, since the instrument cannot see the path): the new loop is a SINGLE flat pass over `reachingArms` (i from 1 to count), placed at the SAME level as — and sequential with, NOT nested inside — the pre-existing `checkGiveClass` O(arms) loop, so it contributes O(arms), never O(arms squared). Per arm the work is O(1): `tagOf` / `valueNameId` are dense `valueTags` / `valueNameIds` column .get's; `aggregateNameOf` is one `typeNames.nameOf` array-index plus `containsStruct` / `containsEnum` (a `Map.contains`, no scan, no allocation) and returns the shared `EmptySourceText` for every scalar so no `MaxonType` is materialized; `typesAgree` is a `typeClassOf` tag compare; `namedAggregatesConflict` is a bounded byte-equal of two interned aggregate NAMES (an identifier length — a source constant, not program-sized). `giveTypeName` + `String.from` are confined to the throw branch, so a valid (agreeing) match allocates NOTHING new. A match's arm count is a hand-written source constant (the #28 / #32 / destructor-cascade precedent — bounded by a source constant is linear whole-program), so the loop sums to O(total match arms) = O(program), linear. The DOMINANT term of `finalizeMatchMerge` is UNCHANGED: the pre-existing O(carried x arms) phi-diff loops (`eachVar` Parser.maxon:6830, `recordEdges` 6869) still dominate; the new check adds a strictly-smaller O(arms) term and does not worsen that pre-existing complexity. No fast-path fork, no inlined helper — nothing to change. Emitted code is byte-identical for valid programs (the check only throws on ill-typed matches; fragments additions-only). GATES on this tree: build exit 0 zero warnings, error-codes registry OK; specs-shv2 754/0 exit 0 memoryLeak FALSE; git status specs-shv2/fragments/ no M (the byte-identical HEAD binary reproduces the spec-tested build). | 21,663,305 | 43,779,214 | 90,502,352 | 184,264,834 | 377,453,081 | 774,333,995 |
| 2026-07-19 | F2a float RETURN (OPEN #50) OPTIMIZATION PASS (independent) — NO CODE CHANGE by the optimizer. Movement is a FLAT +2 allocs / +40 bytes at EVERY rung (rung0 == rung5), a once-per-compile CONSTANT, not superlinear. Immune A/B (one bootstrap bin/maxon.exe, both shv2 binaries: base F1 1e765a3a0 vs F2a working tree): base reproduces the last logged row EXACTLY (delta 0 at all 6 rungs), confirming F1 is scale-neutral and the +2/+40 is THIS slice, not a stale baseline (F1 landed after the last row). PER-PHASE A/B splits the delta exactly: phase:parse +2 allocs / +32 bytes, phase:isel +8 bytes / 0 allocs, every other phase bit-identical. (1) phase:parse +2/+32 is NOT the backend calleeResultClasses map as first suspected — it is main() TWO return statements. The corpus emits `function main() returns ExitCode` once per compile (ScaleCorpus.mainSource, a FIXED block not scaled) with `return 0` and `return 1`; ExitCode is a ranged int typealias so its return tag is `named`, and F2a new floatResolvedTag (parseReturnStatement) calls namedIsFloatAlias -> aliasOf(ExitCode), which constructs a boxed DeclaredAlias union (16B) per return. main is emitted ONCE, so exactly 2 boxes = +2 allocs / +32 bytes, a per-compile constant independent of program size (struct/int returns are structRef/integer, never `named`, so they never fire it; ExitCode underlying is int so tagIsFloat is false and no MaxonType.float box is made — one DeclaredAlias box per firing). (2) phase:isel +8/0 is the StdToX64Lowering struct widened by the ONE new calleeResultClasses pointer field (one lowering struct per compile). The calleeResultClasses MAP is built once in create() exactly as it was built once in buildValueClassColumns before F2a — 0 allocation delta (isel allocs bit-identical base vs F2a), the build merely MOVED phase-internally, count unchanged. NOTHING SUPERLINEAR: namedIsFloatAlias is O(1) per named-return (internedName array-index + aliasOf hashed Map.get + one O(1) DeclaredAlias box), bounded by return-statement count = O(program) linear, the SAME accepted boxed-DeclaredAlias idiom valueIsUnsigned/fieldStorageType already use; floatResolvedTag/resolveFloatAliasType are O(1)-gated (return early when tag != named, so the parser helpers never fire on the corpus except main 2 ExitCode returns); backend buildCalleeResultClasses is O(functions) built ONCE per compile (count unchanged from the pre-F2a build inside buildValueClassColumns), and calleeResultClass/callResultRegister/emitReturnValueMove are O(1) per call/return — the Map is precisely the O(1) index that AVOIDS a Theta(calls x functions) scan. No fast-path fork, no inlined helper. LEFT as-is per the mandate: the per-named-return DeclaredAlias box is a LINEAR constant-factor matching the existing aliasOf idiom, and removing it would need a non-boxing float-alias query in the front-end resolution logic this pass must not touch (byte-identical fragments are pinned). specs-shv2 fragments clean (no code change -> codegen byte-identical by construction). | 21,663,345 | 43,779,254 | 90,502,392 | 184,264,874 | 377,453,121 | 774,334,035 |
| 2026-07-19 | F2b float ARGUMENTS (OPEN #57) OPTIMIZATION PASS (independent) — DECISION: kept the two dense maps, NO CODE CHANGE by the optimizer; nothing superlinear. The F2b slice adds a GENUINELY NEW O(functions) structure, the backend calleeParamClasses Map (callee name to per-parameter float bitmask), built ONCE per compile in each backend create via buildCalleeParamClasses — the argument-side twin of F2a calleeResultClasses. Delta vs the F2a row: allocs +43 +43 +51 +59 +67 +75 (rung0..5), bytes +8,632 +8,632 +16,984 +33,528 +66,456 +132,152 (rung5 = 0.017% of 774MB). LINEAR, not superlinear: the BYTES double per rung (x1.97..x1.99, the corpus itself doubles) because the map has one entry per function and its four backing arrays (keys pointers, u64 mask values, states, hashes) are O(functions); the ALLOCS grow only additively +8/rung because the map is DOUBLING-backed, so building it is O(log functions) reallocs, not O(functions). per-type (rungs 0-2) CONFIRMS: calleeParamClasses and buildCalleeParamClasses are a TAIL contributor (not in the top-20 type or scope table), every listed type is x1.88..x1.99, and the realloc bucket carrying the map backing-array growth is x1.88 (below 2.0). The function-NAME bytes are NOT duplicated: Map.insertAtSlot retains a refcounted reference to the same StdModule-owned ByteArray that calleeResultClasses already keys on (keys.set is a retain, not a byte copy), so what the second map costs is a second hash-table SLOT machinery over the same key set, not a second copy of the names. AUDIT of every new helper: computeParamSlotIndex counts preceding same-file params in a loop bounded by paramIndex under MaxRegisterParams (6, parser-enforced) = O(1) per param and O(functions) total; floatParamMaskOf is O(params at most 6) once per function; emitCallArgMoves is O(args at most 6) per call; calleeParamClass is an O(1) hashed get plus a bit shift; parser resolveFloatAliasType is O(1) and allocates ZERO on the corpus (params are int or struct, so it early-returns on tag not equal to named). None is a per-op or per-value walk that scales. CONSOLIDATION CALL — kept dense, did NOT merge into one map, did NOT take the sparse option. (1) It is LINEAR and 0.017% of total; the mandate is to hunt superlinear, not micro-optimize a constant factor. (2) The two maps are NOT one-fact-twice: they store INDEPENDENT facts (return register class vs per-parameter float mask), both rebuilt every compile from stdModule.functions, so they cannot silently drift — the shared key set is derived, not a hazard. (3) To stay allocation-flat a consolidated value MUST be a packed scalar: a struct value heap-boxes per entry in shv2 (structs are heap values), which would turn the flat +8/rung alloc delta into +O(functions)/rung, strictly WORSE — the measured flatness proves the current scalar value is stored inline. Bit-packing the result-class bit with the param mask reintroduces a bit-layout that two readers must agree on, and forces the map type through about 11 sites in the F2a return path (buildValueClassColumns then buildFunctionValueClasses then recordBlockOpClasses then recordOpResultClass then calleeResultClass, plus both backends create, callResultRegister and threading) which the F2b brief fenced off and which is byte-pinned — invasive for a ~0.008% (one of two hash tables) saving. Rejected the sparse-map option: a missing entry degrading to an all-GPR default turns a genuinely-wrong float argument from a specific panic at calleeParamClass into a later, vaguer emitter failure. GATES: shv2 suite 763/0 exit 0 (no leak); specs-shv2 fragments additions-only — one new f32-function-param-return test, zero modified — so all existing codegen is byte-identical. | 21,671,977 | 43,787,886 | 90,519,376 | 184,298,402 | 377,519,577 | 774,466,187 |
| 2026-07-19 | F3b throwing-float RETURN throw edge (OPEN #60) OPTIMIZATION PASS (independent) — DECISION: KEEP the StdReturnType field, NO CODE CHANGE by the optimizer; nothing superlinear. Movement is a FLAT +1 alloc / +1 free / +24 bytes at EVERY rung (rung0 == rung5), a once-per-compile CONSTANT, not superlinear. A/B build isolates it exactly: base F3a 40a159bea vs F3b working tree, both rebuilt on one bootstrap; the base reproduces THIS logged F2b row EXACTLY (delta 0 at all 6 rungs), which also confirms F3a (int->float ARG widening, siToFp) is scale-neutral on the corpus — the corpus has no float arguments so siToFp never fires — so F3a legitimately added no row and this +1/+24 is F3b ALONE, not a stale baseline. Per-phase A/B attributes the WHOLE delta to phase:lowerMaxonToStd (+1 alloc / +1 free / +24 bytes at every rung; every other phase bit-identical): it is the ONE StdReturnType.void union box minted by create() in the Self{...} initializer (create runs once per compile in lowerMaxonToStd), freed once when first overwritten / at teardown (hence +1 free, a matched alloc, no leak). It is NOT the per-function assignment self.currentReturnType = srcFunc.returnType — that is a refcount RETAIN of the existing IrFunction.returnType box, no new alloc; had it allocated per function the delta would DOUBLE per rung, but it is flat. It is NOT the emitZeroConstOfReturnType float branch — the corpus has no throwing-float function so that branch is never taken, and the non-float branch delegates to the unchanged emitZeroConst, identical op emission (fragments prove byte-identical codegen: additions-only, zero modified goldens). DECISION KEEP vs bool: (1) the movement is once-per-compile, 24 bytes = 0.0000031% of the rung-5 774MB; the mandate hunts superlinear growth, not once-per-compile constants, and this does not scale at all. (2) The field stores the RAW return type and classes float-ness at the point of use via the SAME stdReturnTypeIsFloat predicate the backend emitReturnValueMove applies to the SAME IrFunction.returnType — one fact, one predicate, two readers that cannot drift; a bool currentReturnIsFloat would cache the DERIVED answer instead of the fact, and if register-file classification ever needs more than a bool (a future SIMD/vector return class distinct from scalar float) only emitZeroConstOfReturnType changes under KEEP whereas the bool field type and every assignment site would have to change. (3) It mirrors its sibling field currentThrows as ThrowsClause, which stores the raw clause not a derived bool — a bool would make currentReturnType the odd field out. (4) A bool is a code change that would require the full gate battery and risk the byte-pinned codegen to save a constant the mandate says not to chase. SUPERLINEAR AUDIT: emitZeroConstOfReturnType is O(1) per throw edge (one union match + one mint-and-append const, same op count as the emitZeroConst it replaced) firing at most once per throw/propagate/error-return terminator = O(program) linear; currentReturnType is a scalar field set once per function = O(functions) linear; no data structure indexed by program size, no nested scan, no repeated rebuild, no dense-index walk. Nothing superlinear. GATES: no code change (KEEP) so codegen byte-identical by construction; shv2 suite 767/0 exit 0 (no leak); specs-shv2 fragments additions-only (one new error.throwing-float-return.test, zero modified existing goldens). | 21,672,001 | 43,787,910 | 90,519,400 | 184,298,426 | 377,519,601 | 774,466,211 |
| 2026-07-20 | arm64-linux RUNG 1 (static ELF writer + arm64Syscall op + OsImportSlot hoist) — DECISION: KEEP, NO CODE CHANGE; nothing superlinear, and the rung's TRUE cost is +1 alloc / +48 bytes FLAT AT EVERY RUNG (rung0 delta == rung5 delta), a once-per-compile constant. ⚠ READ THE RAW DELTA COLUMN ABOVE WITH CARE — it is measured against the last logged row (F3b, 2026-07-19) and origin/main advanced TWICE since without logging one (OPEN #52 767->771, OPEN #62 771->774), so the +10,497(rung0)..+255,347(rung5) it shows is ACCUMULATED UPSTREAM WORK, NOT this rung. A/B isolates it exactly: a second worktree at the pre-rung parent 01b901de5, built on the SAME bootstrap binary, reproduces that delta to within 1 alloc / 48 bytes at all six rungs (base rung0 368,309/23,409,379 vs rung 368,310/23,409,427; base rung5 10,659,901/801,125,168 vs rung 10,659,902/801,125,216). So the whole visible movement is the unlogged upstream rows and the rung itself is +1/+48. ATTRIBUTION IS SINGULAR: the ONLY module-scope heap literal the rung adds is ElfWriter.maxon:54 'let ElfIdent = b"\x7fELF..."', a 16-byte ByteArray minted ONCE at module init (16-byte payload, 48 bytes with allocator header/rounding) — grep over every file the rung adds or edits returns exactly this one candidate, which is why the delta is flat rather than per-function or per-rung. KEEP vs inlining the magic bytes at the call site: the project rule is no magic values, and e_ident IS a named structural constant; trading a named constant for a bare hex literal to save one once-per-compile allocation is chasing a number the mandate explicitly says not to chase. SUPERLINEAR AUDIT: writeElf is a single linear pass over code/rdata/dataPrelude with no nested scan and no structure indexed by program size (the byte images are appended once each); patchArm64SectionRelocations is O(relocations) and was HOISTED, not duplicated, out of MachOWriter so both writers share one walk; the arm64Syscall op is O(1) per OS-primitive lowering (at most three sites in the entry stub + isel) and adds one union arm, not a pass; the debug-symbol section pass is O(symbols). Nothing indexes by program size, nothing rebuilds, nothing rescans. GATES: shv2 suite 772/0 exit 0 (not 101, no leak); specs-shv2 fragment tree EMPTY (zero modified goldens — arm64-macos and x64-windows codegen byte-identical, which is also what proves the three X64 range-arm edits are inert); C# suite 3044/3044 exit 0 with specs/ and specs-shv2/ both clean, required because the rung also fixes a pre-existing bootstrap Mach-O GOT chained-fixup page-boundary bug that blocked the suite entirely. | 23,409,427 | 45,886,142 | 94,666,070 | 191,824,411 | 391,503,528 | 801,125,216 |
| 2026-07-20 | P1.4b Wave 2c (boxed-union struct FIELDS: construct/read/reassign/scope-drop + the throw-of-field MOVE-OUT transfer). DELTA vs F3b: -520 alloc EVENTS / +62,208 bytes at rung5, LINEAR (doubles per rung, ~x1.9), NET FEWER ALLOCATIONS. A/B (revert Parser+SignatureIndex to HEAD, SAME copied bootstrap, rebuild) reproduces the F3b baseline EXACTLY (delta 0 at all 6 rungs) => the delta is REAL, not a stale bin. ALLOC REDUCTION = the DELETED per-construction rejectUnsupportedManagedStructField scan, which classified every field of every constructed struct via classifyUnionPayload (one PayloadClass box per field), now unnecessary because a boxed-union field is a MANAGED PayloadClass that moveInPayload/emitFieldWrite handle end to end; corpus rung0 = 12 structs x 2 int fields = exactly -24 (matches). BYTES INCREASE = a realloc-SIZING shift, benign + linear, NOT a per-element cost: --per-type shows the dominant allocator is realloc (array-backing growth, ~87MB / 338K allocs at the traced rungs); dropping the transient per-construction PayloadClass allocs shifts WHEN a growable buffer crosses its doubling threshold, so it reallocs to a slightly larger capacity. The new throw-of-field recording is INERT on the corpus (only int struct fields, no unions, so emitFieldLoad named-field guard never fires) and is 4 SCALAR parser fields anyway (zero allocation, no map). SUPERLINEAR AUDIT: classifyUnionPayload O(1) per field; throw move-out O(1) per throw edge (one null store + one owned-bit set); lastFieldRead = 4 scalar fields, no structure indexed by program size, no scan, no per-op growth. Nothing superlinear. GATES: build 0/0; shv2 suite 788/0 exit 0 (no leak, incl. throw/reassign/deep-cascade probes); worker-invariance byte-identical (workers 1 vs 12, same md5); fragments additions-only + ONE justified message-text M (borrowed-throw reject message extended to name the boxed-union FIELD move-out). | 21,674,689 | 43,792,518 | 90,527,848 | 184,314,554 | 377,551,089 | 774,528,419 |
| 2026-07-20 | P1.5-A1 non-capturing function values (813/0). NEW: Maxon functionRef/indirectCall, Std funcAddr/callIndirect, x64 leaRegFunc/callReg, arm64 isel producers; function(...) returns T type parsing; bare-function-name to functionRef via the whole-program signature sweep (forward refs resolve); E3005/E3101 discipline; a per-file FUNCTION-typealias registry. DELTA (A1-only, vs the pre-A1 compiler ee224152c): +2,494 alloc EVENTS / +118,456 bytes at rung5, LINEAR (the A1-only alloc delta itself DOUBLES per rung: x1.55 x1.71 x1.83 x1.91 x1.95, converging to x2.0). NOTE the delta COLUMN on this row reads +4,586 / +210,528 instead, because the previous LOG ROW is Wave 2c -- TWO non-A1 commits behind ee224152c -- so the column additionally counts a221da742 (x86 GT double-schedule fix) + 3e2eecc9c (fragment regen), which add +2,092 allocs / +92,072 bytes at rung5 and which the ee224152c A/B excludes; +2,092 + +2,494 = the +4,586 shown. A/B: pre-A1 baseline is ee224152c (TEST-ONLY, compiler bit-identical to HEAD~2), full rebuild, SAME copied bootstrap; the corpus is byte-identical between the two (same source AND code bytes at all 6 rungs), so the delta is REAL, not a stale bin. SUPERLINEAR AUDIT (the mandate) -- NOTHING superlinear. The specific risk (OPEN.md standing rule: a whole-program query placed UNDER a per-file one is asked ONCE PER FILE, so anything O(files) inside is O(files^2) -- the shape of the clearDepsFor/compositeSourceHash quadratics): queryProgramSignatures is MEMOIZED (computed once, ONE pass over files, then every per-file queryParseOps is a cache hit), and A1's read side is all O(1) Map lookups -- isFunctionAlias/functionAliasOf/returnTypeOf/throwsOf -- so NO O(files) body was added under the per-file parse; the fold (foldFile) walks each file's function-alias array exactly once. The standing rule's OWN diagnostic is passed: a quadratic-under-per-file makes the DELTA grow ~x3.4 on a doubling ladder; A1's delta doubles (x1.55..x1.95), it does not. indexOfField added to parseMethodCall is a bounded per-struct scan (the same measured pattern as indexOfParam), O(program size), not superlinear. ATTRIBUTION of the delta (all LINEAR): +10 FLAT across every rung = the one-time whole-program functionTypeAliases Map (ProgramSignatures + Project, created once). +143 in phase:parse = EXACTLY one per file (143 files at rung5) = the real-parse FileParseArtifact.functionTypeAliases staging array. +2,334 in phase:signatures (the declaration sweep) = the sweep-side function-alias staging: the local functionAliases array is 1/file (MEASURED: a reuse-the-artifact-array probe dropped signatures by exactly 143), the rest is the enlarged per-file sweep artifact + a linear realloc/threshold side-effect -- --per-type confirms NO function-alias TYPE or SCOPE is a significant allocator (they sit in the 288-type / 479-scope TAIL; the dominant allocator is realloc / raw-buffer array growth), so the sweep delta is threshold movement, not significant new logical objects. The read-side guard isFunctionAlias -- called on EVERY named type reference in both sweep and parse -- is PROVEN zero-allocation: neutralizing it left signatures bit-identical at 198,637 (Map.contains hashes the ByteArray key in place). CORPUS BLINDNESS (reported, NOT chased, instrument UNTOUCHED): the corpus emits NO function values (same class as instance-methods #30 / structs #4d), so the whole function-value LOWERING path -- functionRef/indirectCall to funcAddr/callIndirect to leaRegFunc/callReg + the arm64 twin -- measures ZERO. That is a corpus knob nobody added, not an optimization. NO code change was made: nothing was superlinear, so this row records the A1 feature's cost, not a fix. GATES: build 0 warnings; shv2 suite 813/0 exit 0 (no leak); specs-shv2/fragments byte-identical (empty git status) -- proves byte-identical codegen. | 21,685,825 | 43,810,086 | 90,558,280 | 184,370,714 | 377,658,705 | 774,738,947 |
| 2026-07-21 | x64-linux backend (raw-syscall codegen + static ELF, WSL cross-runner) + unlock 67 x64-generic tests on x64-linux (885/0 = x64-windows 887 minus the 2 SIGFPE divide/mod-by-zero tests, deferred). SCALE-NEUTRAL BY CONSTRUCTION on this host: scale-test compiles for the HOST (x64-windows), which takes the WINDOWS OS-call lowering (VirtualAlloc/WriteFile/ExitProcess via IAT); the new x64-linux path (mmap/write/exit_group syscalls, ELF x64 writer, X64LinuxRuntime) is NOT exercised here. The one new IR op x64Syscall adds a constant arm to every TargetOp match (O(1)); the fan-out edits are O(1)/op; the ELF reloc patcher patchX64SectionRelocations is O(relocs) linear (the arm64/PE twin). SUPERLINEAR AUDIT: all phase exponents ~x2.0 on the doubling ladder (regalloc:criticalEdges x2.31 is the pre-existing liveness two-call-site sum, unchanged by this rung). The delta-vs-last-row (rung5 +57,078 allocs / +9.87M bytes, itself DOUBLING per rung = LINEAR) spans the intervening upstream rungs P1.5-A2 + #71 + OPEN-14, NOT this rung, whose host-path allocation contribution is structurally nil. NO code changed for scale. GATES: build 0 warnings; x64-windows 887/0, x64-linux 885/0 (worker-invariant, goldens byte-identical w1==w12); fragments/x64-windows untouched. | 21,958,004 | 44,328,541 | 91,673,951 | 186,643,008 | 382,430,905 | 784,611,984 |
| 2026-07-21 | P1.5-B1a single-M cooperative green-thread scheduler (async/await): async f(args) spawns a green thread and yields a Promise handle, await p drives the cooperative scheduler to completion. Suite 887->899/0 host, 874/0 wasm unaffected (async is x64-gated). B1a-ONLY cost isolated by A/B vs the pre-B1a compiler 48832f3f6 (full rebuild, SAME copied bootstrap, corpus byte-identical at all 6 rungs): +6,160 allocs / +267,428 bytes at rung5, strictly LINEAR -- the B1a-only alloc delta DOUBLES per rung (x1.57 1.71 1.81 1.89 1.93, converging to 2.0) and so do the bytes (x1.77 1.96 1.96 2.03 2.02). READ THE DELTA COLUMN ON THIS ROW CAREFULLY: it instead shows +63,237 allocs / +10,140,417 bytes because the previous LOG ROW is A1 (2026-07-20) and SEVEN compiler commits sit between A1 and B1a -- A2b-1 + A2b-2 (capturing + uniform (args,env) ABI closures), OPEN #71 inline conditional, OPEN #14 borrowed-aggregate merge-arm reject -- which the pre-B1a baseline already carries (measured +57,077 allocs at rung5); +57,077 + +6,160 = the +63,237 shown, so the B1a FEATURE is +6,160, not +63,237. ATTRIBUTION: 100% of the +6,160 is ONE new per-value column, valuePromiseMarks (Parser.maxon:1842) -- a dense BoolArray in the SAME lockstep as valueOwnsHeap: pushed false in mintValue/recordValueType (Parser.maxon:8483/8521), set by markValuePromise on the single value emitAsyncCall mints, read O(1) by valueIsPromise. It splits across EXACTLY two phases (every other phase byte-identical HEAD vs pre-B1a): +3,969 in phase:parse = the per-value pushes during the real parse; +2,191 in phase:signatures = the BoolArray.create() in the Parser CONSTRUCTOR (Parser.maxon:2003), because queryProgramSignatures builds a Parser per file (Queries.maxon:130) and a throwaway Parser per const-initializer (SignatureIndex.maxon:1385) -- the SAME field, charged to whichever phase constructs the Parser (the identical parse/signatures split the A1 row documented for its own new column). The sparse-Set alternative was MEASURED WORSE (+7,364 allocs / +292K bytes at rung5: a per-function empty hash table costs more than the dense bit across many small functions), so the dense column stands (code note Parser.maxon:1837). SUPERLINEAR AUDIT (the mandate): NOTHING superlinear. The GT scheduler runtime (GtRuntime.maxon + X64GtRuntime.maxon: installGtRuntime, the .data globals, the two hand-assembled chunks) is emitted ONCE per module like MmRuntime and gated on RuntimeUsage.usesGt; the scale corpus emits NO async so usesGt=false, and the whole runtime + the asyncCall/await LOWERING (lowerAsyncCall/emitAsyncSpawn/lowerAwait) + validateAsyncCall measure ZERO -- corpus blindness (same class as A1 function-values / #30 instance-methods), reported not chased, instrument UNTOUCHED. The three per-input touch points are all bounded: requireAsyncScalarParams loops a callee own paramCount (per async-call op), emitAsyncSpawn arg-spill loop is bounded by MaxAsyncArgs (6), and moduleHasFunction(GtSpawnName) is ONE O(functions) scan per compile in buildX64Backend (mirrors the leak gate __mm_leak_check module probe), not inside any per-function or per-op loop. PER-TYPE CORROBORATION (--per-type traces rungs 0-2 with a separate mm-trace binary): the ONLY by-SCOPE movers HEAD vs pre-B1a are BoolArray.create (29,670 -> 30,557 = +887, the valuePromiseMarks constructor) and realloc (341,223 -> 342,169 = +946, the per-value push doubling of that same BoolArray), summing to +1,833 = EXACTLY the timed A/B rungs-0-2 delta (350 + 548 + 935); no new type or scope appears (the async LOWERING scopes are corpus-blind), and every row grows x1.88-1.99, nothing x4. NO code change was made -- nothing was superlinear -- so this row records the B1a feature cost, not a fix. GATES: build 0 warnings; specs-shv2 899/0 host exit 0 (no leak); specs-shv2 874/0 wasm exit 0; specs-shv2/fragments byte-identical (empty git status = byte-identical codegen). | 21,967,546 | 44,345,427 | 91,707,133 | 186,708,197 | 382,562,976 | 784,879,364 |
| 2026-07-21 | P1.5-B2a linear await (E3100): a COMPILE-TIME reachability pass (SemanticCheck.maxon checkLinearAwait) refusing a second await of the same promise reachable from a first, keyed on the promise SSA ValueId -- NO new per-value column, NO codegen, NO runtime change. OWN COST ZERO on the async-free ladder, proven by A/B (pass call ON vs OFF, both rebuilt): the per-phase ALLOCATIONS and BYTES tables are BIT-FOR-BIT IDENTICAL at all 6 rungs on vs off; phase:semanticCheck is 571 (rung0) to 14,707 (rung5) at x1.98 (LINEAR) both ways. The whole reachability machinery sits behind functionHasAwait, a bounded NON-ALLOCATING tag scan that returns false on every corpus function -- the scale corpus emits NO async (its own blind-spot doc lists closures/async/generics as NOT GENERATED), so the pass never runs and allocates nothing (same corpus-blindness class as A1 function-values and B1a async lowering; reported not chased, instrument UNTOUCHED). STALE-BASELINE CAVEAT -- READ THE DELTA ON THIS ROW AS SPANNING AN UNLOGGED GAP, NOT THIS RUNG: the +521 (rung0) to +9,201 (rung5) alloc delta and +24,807 to +443,440 byte delta shown against the previous row is NOT B2a and is NOT any corpus-visible change since the B1a row. MEASURED: the B1a-row commit 727a783b1, the B2a base f0d87269b, and B2a all produce byte-identical corpus numbers (361,458 rung0 to 10,481,058 rung5) -- the corpus has not moved at all across b8a595169 (B1a dedup: export-only plus an async-gated guard), d29dee4a8 (B1a stack-leak fix: GtRuntime-only plus async specs) and f0d87269b (docs). The delta is entirely the B1a row STALE STORED baseline: its numbers (360,937 rung0) do not reproduce on its own commit tree (727a783b1 measures 361,458), because the B1a feature was measured before the parallel-repo work its base later folded in -- OPEN #54 Slice B2, #66 x86 GT double-schedule fix, P1.9 ranged typealiases, x64-linux backend -- none of which took a log row of its own. COMPLEXITY NOTE (not a corpus issue): reachableReawait is O(await-sites x blocks) worst-case per await-bearing function (a BFS per await site, each block visited once); zero on the async-free corpus, and await-bearing functions are rare and small -- noted, not optimized (no measurable cost to optimize). GATES: build 0 warnings; scale-test exit 0; host 941/0, wasm 908/0, fragments additions-only. | 21,992,353 | 44,383,738 | 91,779,117 | 186,827,541 | 382,790,343 | 785,322,804 |
| 2026-07-21 | P1.5-B2b error-carrying try await (E3057/E3059 soundness). NEW: MaxonOp.tryAwait lowered to tryCall(__gt_try_await); __gt_try_await refolded WITH __gt_await into buildGtAwaitDriver(name, throwing), emitted ONCE per module that spawns a GT (gated on usesGt, like MmRuntime); GT struct grows 0x50 to 0x60 (threw field); trampoline captures R10 into gt.threw. Front end: asyncCallCalleeOf (promise to defining-asyncCall callee), checkPropagateType plus rejectPropagateAgainstEnclosing (E3059 propagate-type check, which also closes the gap for ordinary try), plain-await-of-throwing reject (E3057), the tryAwait to tryCall lowering; the linearity pass checkLinearAwait EXTENDED to treat tryAwait as an await site. OWN COST ZERO on the async-free ladder: the instrument delta-vs-last-row is 0 at ALL 6 rungs (allocs 361,458 rung0 to 10,481,058 rung5; bytes 21,992,353 to 785,322,804), BYTE-IDENTICAL to the B2a row -- every per-phase allocs and bytes column unchanged, phase:parse x1.99, phase:signatures x1.98, phase:semanticCheck x1.98, all LINEAR. STALE-BASELINE STATUS -- unlike the prior three rows this delta is a TRUE zero, NOT a stale-baseline artifact: verified the B2a stored baseline reproduces EXACTLY on this tree (361,458 to 10,481,058 == this run, delta 0 every rung), and only two DOC commits (B2a COMPLETE, OPEN #89) sit between the B2a log row and B2b, so there is no corpus-visible upstream work to caveat this time. CORPUS BLINDNESS (reported, NOT chased, instrument UNTOUCHED) -- STRONGER than usesGt=false: ScaleCorpus emits NO try, throws, otherwise, async or await AT ALL (verified in the generator; the documented subset excludes them), so beyond the GT/async path being skipped (usesGt=false, functionHasAwait=false) the NEW per-try-site checkPropagateType is ALSO never reached -- the corpus cannot see B2b front-end additions at all (same blind-spot class the corpus doc lists for async/generics). SUPERLINEAR AUDIT (the mandate) -- NOTHING superlinear. checkPropagateType is O(1) NON-ALLOCATING per bare-propagate try-site: one funcThrows.get Map lookup (hashes the ByteArray key in place, no alloc) plus one throwsTypeName field match, no loop, and reached ONLY on the propagate form (try f() with no otherwise), rarer still. asyncCallCalleeOf is an O(function-ops) linear scan per await site (locates the promise defining asyncCall), invoked TWICE per await site (once for the awaited result tag, once for the throwing-check or callee-name) = a constant 2x factor, still O(function-ops); await sites are rare and small (same profile as the B2a reachableReawait scan) and this path is under separate correctness review -- noted, not touched. checkLinearAwait keeps its unchanged O(await-sites x blocks) per await-bearing-function shape, now counting tryAwait sites too. NO code change was made -- nothing was superlinear -- so this row records the B2b feature (zero) cost, not a fix. GATES: build 0 warnings; host 956/0 exit 0 (no leak); scale-test exit 0; specs-shv2/fragments byte-identical (empty git status = byte-identical codegen). | 21,992,353 | 44,383,738 | 91,779,117 | 186,827,541 | 382,790,343 | 785,322,804 |
| 2026-07-21 | P1.6-A generic types (front-end, no descriptor), rebased onto B2b main. NO superlinear defect (GenericInstanceRegistry intern is O(1) composite-key, not v1's O(n2), independently verified). Own cost LINEAR: new per-file staging (Parser arrays + genericAliases + StructLayout.typeParamNames) + registry once/build; signatures-phase delta increments track 352x2^n. CORPUS-BLIND: the ladder emits zero 'with', so intern() is never called and the generic-instance lowering path measures zero — reported, not chased, instrument untouched. | 22,019,817 | 44,429,410 | 91,861,205 | 186,982,461 | 383,090,927 | 785,914,716 |
| 2026-07-21 | P1.5 mid-body yield + netpoller, proven via sleep(ms) (957 to 963) -- INDEPENDENT OPTIMIZATION PASS, NO CODE CHANGE. Verdict: NOTHING superlinear; this row records the feature cost, not a fix. The delta-vs-last-row is a FLAT +16 allocs / +1,160 bytes at ALL 6 rungs (allocs 361,458 to 10,481,058 becomes 361,474 to 10,481,074) -- a once-per-compile O(1) CONSTANT, NOT per-rung growth (a per-rung term would DOUBLE the delta each rung, +16 +32 +64 ...; it stays +16 flat). MECHANISM: the 3 new always-present IatSlot imports QueryPerformanceCounter / QueryPerformanceFrequency / Sleep, appended to the IatSlot enum in PeWriter, grow every x64-windows PE import table from 5 to 8 kernel32 slots UNCONDITIONALLY -- the import table is NOT gated on usesGt -- about 5.3 allocs / 387 bytes per import (name string + ILT/IAT thunks). CORPUS BLINDNESS (reported, NOT chased, instrument UNTOUCHED): the whole yield/timer/sleep/clock runtime -- __gt_drive_until, __gt_sleep, __gt_timer_add/check/earliest, __gt_now_ns in GtRuntime.maxon plus the unsorted timer array -- is gated on usesGt, and ScaleCorpus lists async/closures/generics as a documented structural blind spot and emits NO sleep, async or await, so usesGt is FALSE and that entire path measures ZERO. Only the unconditional import-table growth is visible. SUPERLINEAR AUDIT (the mandate): every phase is x1.72 to x2.01 allocs and at or below x2.31 bytes (parse x1.99, regalloc x1.99, lex x1.99; the byte tops criticalEdges x2.31, blockOrder x2.12, elimTrivialBlockArgs and sugarGate x2.11 are the documented Go-nextslicecap linear band, not a bend), nothing near x4. COMPLEXITY DEBT FILED (record, do NOT optimize -- under separate correctness review): the timer store is an UNSORTED O(n) array, so __gt_timer_earliest (peek-min) and __gt_timer_check are O(n) per call where n is the count of CONCURRENTLY-sleeping GTs -- a RUNTIME cost invisible to compile-time scale-test (the corpus never sleeps), correct for the current scale (at most 2 concurrent sleeps); the 16-byte entry layout (deadline at offset 0, gt at offset 8) is min-heap-compatible so a sift-down can be dropped in later if a harness sleeps many GTs at once. STALE-BASELINE CAVEAT: the delta is taken against the stored B2b log row (commit 285d806a6, the DIRECT parent); this commit touches ONLY maxon-shv2 sources, NO maxon-sharp/.cs, so the C# bootstrap is byte-identical and the same bin/maxon.exe compiles both, and the delta being FLAT at all 6 rungs is itself proof it is a once-per-compile constant rather than a stale-baseline artifact hiding per-rung work. But parallel-repo (arm64-macOS / x64-linux) rows may land on main between B2b and this row after merge, so the row DIRECTLY ABOVE this one may shift; the +16 is a TRUE single-commit delta on THIS branch. GATES: build 0 warnings; host 963/0 exit 0 (no leak); wasm 911/0 (sleep x64-gated); byte-identical across --workers 1 and 12; specs-shv2/fragments clean (empty git status proves byte-identical codegen); scale-test exit 0. | 21,993,513 | 44,384,898 | 91,780,277 | 186,828,701 | 382,791,503 | 785,323,964 |
| 2026-07-21 | P1.5 async subprocess spawn + yielding await (runProcess) -- INDEPENDENT OPTIMIZATION PASS, NO CODE CHANGE; this row records the feature cost, not a fix. VERDICT: NOTHING superlinear. TRUE single-rung delta, measured BRANCH-vs-CLEAN-PARENT (main 5e3cfac98, both freshly built from the SAME bin/maxon.exe, NOT the stale log row): FLAT +28 allocs / +2,040 bytes at ALL 6 rungs (rung0 362,437->362,465 ... rung5 10,505,721->10,505,749; bytes rung0 22,043,145->22,045,185 ... rung5 786,630,284->786,632,324) -- a once-per-compile O(1) CONSTANT, NOT per-rung growth (a per-rung term would DOUBLE each rung, +28 +56 +112...; it stays +28 flat). MECHANISM: 5 new always-present IatSlot imports CreateProcessA/GetExitCodeProcess/WaitForSingleObject/WaitForMultipleObjects/CloseHandle appended to the IatSlot enum in PeWriter; kernel32ImportNames() iterates IatSlot.allCases, so every x64-windows PE import table grows from 8 to 13 kernel32 slots UNCONDITIONALLY (NOT gated on usesGt) -- about 5.6 allocs / 408 bytes per import (name String + ILT/IAT thunks). SAME class as the prior mid-body-yield row (+16/+1,160 for 3 imports). CORPUS BLINDNESS (reported, NOT chased, instrument UNTOUCHED): the whole runProcess/subprocess runtime -- buildGtProcessRun, buildGtProcAdd/Count/Check, the process store, netpoll in buildGtDriveUntil in GtRuntime.maxon -- is gated on usesGt, and ScaleCorpus emits NO async/await/runProcess, so usesGt is FALSE and that entire path measures ZERO. Only the unconditional import-table growth is visible. STALE-BASELINE CAUTION: the delta this run PRINTS against the last log row (+991 -> +24,675, DOUBLING) is NOT this rung -- it is the intervening main commits between the stored log row (285d806a6) and this branch's parent (main 5e3cfac98), notably b323ecc47 (E3006 reject-duplicate top-level let/var, a real per-program pass); measuring branch-vs-parent isolates THIS rung to the flat +28/+2,040. SUPERLINEAR AUDIT (the mandate): every phase x1.72-x2.01 allocs (parse x1.99, regalloc x1.99, lex x1.99); byte tops criticalEdges x2.31, blockOrder x2.12, elimTrivialBlockArgs/sugarGate x2.11 are the documented Go-nextslicecap linear band, nothing near x4. RUNTIME ALGORITHMS (invisible to compile-time scale-test, corpus never spawns; reasoned analytically): (1) buildGtProcCheck scans ALL parked children per netpoll wake and there is one wake per child exit -> O(n^2) in CONCURRENTLY-parked children, but n <= ProcStoreCapacity=64, a compile-time constant, so it is a CAPPED constant, NOT superlinear in program size -- the SAME shape as the existing buildGtTimerCheck; NOT fixed (do not fix a capped constant). (2) buildGtProcessRun's cmdline byte-copy loop is O(command-line length) -- linear. (3) OPEN.md STANDING RULE holds: no whole-program query nested under a per-file one -- emitRunProcess is O(1), scanRuntimeUsage stays a single linear op-walk, isGtRuntimeCallee is a constant-prefix compare. GATES: build 0 warnings; new async-subprocess specs 6/0; full async band 47/0; workers 1 vs 12 BYTE-IDENTICAL (async band); specs-shv2/fragments clean (empty git status = byte-identical codegen); scale-test exit 0. | 22,045,185 | 44,476,986 | 91,953,309 | 187,163,909 | 383,451,063 | 786,632,324 |
| 2026-07-21 | P1.6-B1 sizeof(T) via layout descriptor + dictionary-passing (feat b10db06df + fix 94329dd0e), OPTIMIZATION PASS. CORPUS-BLIND ROW: ScaleCorpus is generics-free (no uses/with/sizeof), so EVERY new B1 path is unexercised here and a flat/byte-identical row proves nothing about them — complexity was established by READING + a targeted micro-benchmark, not this instrument. COMPLEXITY BY READING (per suspect, N=methods-per-type, C=self-call edges, I=distinct generic instances): (1) computeTypeDescriptorNeeds pre-scan = one peek-only walk of the type body, each token visited once (outer walk jumps each method body that the inner scan covers) = O(type-body tokens), LINEAR. (2) LayoutDescriptorRegistry = a Map keyed by GenericInstanceId; contains/labelFor/record all O(1) — NOT v1's linear scan. (3) mangleGenericInstance/mangleTypeArg = recursion over the instance's structural tree O(tree), called ONCE per distinct instance inside ensureLayoutDescriptor (gated by registry.contains), never inside a comparator/sort — no v1 O(N^2*depth) label sort. (4) sizeofOperandIsTypeParamAt/isSelfMethodCallAt = O(1), once per token in the linear method scan. (5) ensureLayoutDescriptor + registerRdataConstantLabeled = one blob per distinct gid, deduped by the registry AND idempotent-by-label in the rdata table = O(I). Always-run lowering/parser additions (buildLayoutNeedingFuncs O(functions*params) once; finalizeCallArgsWithLayout one O(1) map lookup per call; requireSelfForDescriptorForwarding/retypeGenericAliasConstructorResult short-circuit O(1) in non-generic context) are all LINEAR in program size and allocate NOTHING per-function on a generics-free program — which is why this row moves only with unrelated work. DEFECT FOUND + FIXED (this pass): solveDescriptorNeedFixpoint was a Gauss-Seidel iterate-to-stable = O(N^2 + N*C), QUADRATIC in ONE generic type's method count on a deep self-call chain (the machine-generated wide-type trigger the mandate names). MEASURED on a generated Sizer with N chained self-calling methods (mN reads sizeof(T); m(i) calls m(i+1), so need propagates one hop/round against declaration order -> O(N) rounds): parse phase 5.8/16.4/79.8/288 ms at N=100/200/400/800 = ~4x per doubling (parse was 73% of compile at N=800). REWRITTEN to reverse-reachability BFS over a counting-sort (CSR) reversed self-call graph = O(N+C): parse 3.1/6.0/12.3/30.3 ms = ~2x per doubling (linear); N=800 parse 288->30 ms (9.5x). Same least-fixpoint -> byte-identical codegen (suite 981/0, fragments clean, --workers 1==12 byte-identical, sizeof self-forward/transitive-two-hop pass with exit 1/16). CORPUS-INVISIBLE: the fixpoint runs only for generic types (none in the corpus), so this row is byte-identical pre- and post-fix. REBASED onto async-subprocess main (cfca516fb); the numbers in THIS row are the INTEGRATED tree (async-subprocess + B1), re-measured by the coordinator post-rebase. Delta vs the async-subprocess row directly above = FLAT +12 allocs at every rung (B1's once-per-compile setup: buildLayoutNeedingFuncs + two empty registries in create), plus a per-program BYTE term converging x2.0 (+1,336 rung0 -> +19,192 rung5 = LINEAR in program size, NOT a bend). Scale-test's own delta-vs-previous read a FLAT +28 allocs / +2,040 bytes at all 6 rungs -- the async-subprocess once-per-compile import constant folded in by the rebase. Nothing per-rung superlinear. SUPERLINEAR AUDIT: every phase x1.72-x2.01 allocs, <=x2.31 bytes, nothing near x4. GATES: build 0 warnings; suite 981/0 exit 0 no leak; --workers 1==12 byte-identical; fragments clean; scale-test exit 0. | 22,046,521 | 44,478,898 | 91,956,373 | 187,169,277 | 383,461,039 | 786,651,516 |
| 2026-07-22 | P1.5-B1a′ slice 1 — GT→system-stack syscall shim (x64-windows), commit 06d479ee0, OPTIMIZATION PASS. ALL DELTAS 0 at every rung (allocs+bytes byte-identical to the P1.6-B1 row above). EXPECTED and correct: the ScaleCorpus is NON-GT (never spawns a green thread), so `moduleUsesGt = moduleHasFunction(GtSpawnName)` is false for every corpus program and every `iatCall` takes the bare `encodeIatCall` path via `emitGtGatedIatCall`'s else-branch — the shim emits ZERO bytes and allocates NOTHING on this corpus. The 0-delta independently corroborates the coordinator's 0 non-async fragment churn: the `usesGt` gate keeps non-GT codegen byte-identical. SUPERLINEAR AUDIT (the point of this pass — every addition is O(1) per unit, so total encode stays LINEAR in program size): (1) BackendDispatch — the GT gate `moduleHasFunction` (an O(F) linear scan of module.functions) is HOISTED ABOVE the `for func` encode loop, computed ONCE and threaded into every emitFunctionChunk as a bool; it also DEDUPS the pre-existing `'gtChunks'` scan, so the encode phase now makes exactly ONE O(F) scan for the GT question (was implicitly one already), NOT the O(F²) it would be if computed per-function. O(1) per function. (2) The shim `emitGtShimmedIatCall` is O(1) per iatCall: the stack-arg copy `for k in 0 upto stackArgWords` is bounded by CreateProcessStackArgWords=6 (WriteFile=1, all others 0), and `resolveChunkRel32Fixups` runs over a SHIM-LOCAL fixup list of exactly 2 fixups / 2 labels (never the enclosing function's), so no module-wide scan. gtShimStackArgWords is two equality checks. Two small per-shim allocations (ChunkRel32FixupArray+ChunkLabelOffsetMap) are O(1) per iatCall = linear total, and fire only in GT modules whose iatCall count is linear in program size — deliberate local-fixup scoping, not a bend. (3) Context-switch TIB save/load (emitSaveTibIntoGt/emitLoadTibFromGt, 4 encoder calls each) are built ONCE inside buildGtContextSwitchChunk — O(1) for the whole program. (4) GtRuntime adds a fixed number of Std ops to __gt_init/__gt_spawn (built once) and one DataSectionEntry — all O(1). New X64Backend encoders (encodeMovGsAbsReg/encodeMovRegGsAbs) are constant byte pushes. CONCLUSION: no superlinearity introduced; NO CHANGE NEEDED. Raw doubling ladder confirms whole-compiler linearity (allocs x1.90→1.99 per doubling, bytes x2). Gates: build 0 warnings; scale-test exit 0. | 22,046,521 | 44,478,898 | 91,956,373 | 187,169,277 | 383,461,039 | 786,651,516 |
| 2026-07-21 | P1.6-B2 (managed generic type args: owned + dropped-once via synthesized per-instance __destruct_<mangled> cascade) OPTIMIZATION PASS (independent). DECISION: NO CODE CHANGE. CORPUS-BLIND ROW: ScaleCorpus is generics-free (no uses/with), so the ENTIRE B2 generic-instance path -- destructor synthesis (synthesizeGenericInstanceDestructor/installGenericInstanceDestructors), the call-site type-param FEED consume, the typeIsManaged/typeArgIsOwned classifiers, and the needs-closure -- is UNEXERCISED here; complexity below is by READING, not this instrument. ATTRIBUTION of the non-flat +87/+155/+291/+563/+1107/+2195 alloc delta vs the B1 row (ratios 1.78->1.88->1.93->1.97->1.98, converging to x2.0 = LINEAR; ~17 B/box, 37200/2195=16.95): it is the per-CALL ConstructorInstance union HEAP-BOX. B2 threads a ConstructorInstance (instance(gid) or none) from parseCall / dispatchMethodOnBase / parseQualifiedCall(constructorInstanceOf) down through parseCallNamed->emitCall->applyCallerConsume so the caller-side consume can move a managed type-arg into an owning box. A Maxon union value is a heap object EVERY case (the bootstrap that compiles shv2 boxes it -- same fact SignatureIndex's DeclaredFunctionAlias guard names), so every parsed call mints one box; the generics-free corpus makes every one `.none`. LINEAR (one box per parsed call), NOT a masked superlinear -- structurally one alloc/call; the sub-2.0 early ratios are the FIXED mainSource block diluting a per-call term. WHY NOT REDUCED: (a) it is a LINEAR per-call CONSTANT FACTOR -- 2195/10,507,956 = 0.02% of rung-5 allocs -- which the mandate deprioritizes vs growth curves; (b) it MIRRORS the pre-existing, accepted ReceiverArg (none or value(id)) per-call union box already threaded through the same parseCallNamed -- the project's established parser representation for an optional per-call descriptor, so keeping the union is the consistent tidy-O(n) the mandate prefers over a clever-O(n); (c) the obvious cheap removal (one shared module-level `ConstructorInstance.none`, the sharedEmptyDescriptorNeeds pattern) is UNSAFE and was PROVEN so this pass: it panicked with a use-after-free (nil/invalid access addr 0xff..f9, top frame parseQualifiedCall) because the bootstrap MOVES/consumes the returned+passed union (constructorInstanceOf RETURNS it, a move) rather than borrowing it, so the shared global's refcount hits 0 and it is freed under the next call; sharedEmpty* Maps/Arrays are safe ONLY because they are STORED (increfed), which this borrow-only argument path never does. A safe removal would replace the union with a sentinel-encoded GenericInstanceId across ~6 signatures -- scope creep for 0.02%. BIG-O PER SUSPECT (by reading; every term 0 on the corpus): (1) scanConsumeBits feed recording -- the `feeds` array is GATED by genericScope; outside a generic type initialTypeParamFeeds returns the SHARED empty array (ZERO per-function alloc) and recordParamConsume's feeds-hash loop does not run; O(body tokens), LINEAR, allocation-free on every non-generic function -- NOT the delta. (2) applyCallerConsume/transferConsumedArgs -- O(args)/call (unchanged from P1.4a); the new argIsConsumedAt->instanceFeedConsumes is O(1) (constructorInstance=none short-circuits on the corpus); a fed managed arg costs O(argFields), a bounded per-type constant. LINEAR/call. (3) typeIsManaged O(1) (tag test + Map.contains; a genericInstance tag returns true WITHOUT recursing); typeArgIsOwned/genericInstanceHasManagedField O(nested-type-size)/query, un-memoized but bounded by a small source constant and LINEAR in query count -- NOT a per-file*per-struct O(n^2/n^3): the queries are per-instance and the distinct-instance count is tiny. (4) closeDestructorNeeds/addNestedInstanceDestructors (flagged highest-risk) -- a while-changed FIXPOINT re-scanning ALL structs+unions+instances per iteration, but this is the SAME shape as the PRE-EXISTING struct/union closure that B2 merely EXTENDED with an instance loop, NOT a new quadratic. Iterations are bounded by max destructor NESTING DEPTH D (one level propagates per pass) => O(D*(S+U+I)) with D a SOURCE-bounded small constant that does NOT grow with program size (a bigger program has more types, not deeper nesting). Worst case O(T^2) ONLY for a pathological depth-T linear nesting chain (Box with (Box with (... T deep ...))), which is unrealistic and parser-recursion bounded. Distinct from B1's solveDescriptorNeedFixpoint (over the method-call graph, which DOES scale with program size -- hence its O(N+C) rewrite): a depth-bounded closure is effectively linear, so FILED bounded, NOT fixed (a fix would rewrite pre-existing struct/union code). mangleGenericInstance is re-called per instance per iteration (allocates a mangled String) -- a constant-factor inefficiency, zero on the corpus. (5) GenericArgNode nested interning (resolveGenericArgs/resolveArgNode/internNestedNode) -- a single pre-order walk of the flat node list, O(nodes), O(1) intern per node, depth-first, no re-intern/re-sort (no v1 O(N^2*depth) label sort); once per generic typealias; zero on the corpus. LINEAR. GATES: shv2 build exit 0 + 0 warnings + error-codes OK; suite 995/0 exit 0, no leak/101; scale-test exit 0. No compiler code changed this pass, so codegen is byte-identical to committed B2 (02786836f) and the fragments + --workers 1==12 gates are inherited unchanged. | 22,048,009 | 44,481,538 | 91,961,317 | 187,178,829 | 383,479,807 | 786,688,716 |
| 2026-07-22 | P1.5-B1a′ slice 2 — relocating morestack + 2 KB GT stacks (x64-windows). Suite 989→993/0 (+4 async-stack-growth), morestack disasm-verified line-by-line vs v1. DELTA +5 allocs / +216 bytes CONSTANT at every rung (was 0 at slice 1) — FLAT, not growing, so O(1) per compile, NOT superlinear. EXPLAINED: the ScaleCorpus is NON-GT, so the new GT machinery emits NOTHING on it — the prologue guard is gated `usesGt && !isRuntimeFunction && frameBytes>0` (false for every corpus function, and computeGuardFrameBytes is itself gated behind `mightGuard` so a non-GT function does not even pay the callee-saved scan), the `__gt_morestack` chunk is appended only when `moduleUsesGt`, and `osFreePages` is emitted only inside `__gt_drive_until`. The ONLY thing that touches a non-GT program's output is the new `IatSlot.VirtualFree` kernel32 import: like every IatSlot it is UNCONDITIONALLY present in every PE's import table (kernel32ImportNames iterates IatSlot.allCases), so its name string + IAT/ILT thunks + Hint/Name entry are built ONCE per compile in the link/writeExe phase = the fixed +216 bytes / +5 allocs. Constant across all 6 rungs confirms it is per-compile, not per-program-size. SUPERLINEAR AUDIT of the GT additions (fire only on GT modules, whose iatCall/function counts are linear in program size, so still linear total): morestack chunk is built ONCE per GT program (buildGtMorestackChunk, O(1)); the guard is O(1) per guarded function (a fixed instruction sequence + a guard-local 1-label fixup map, never a module-wide scan); computeGuardFrameBytes is O(ops) per guarded function = the same linear scan the prologue pass already does, and runs only for GT modules; osFreePages lowering is a fixed 4 TargetOps. CONCLUSION: no superlinearity; the flat +216 is the cost of one new unconditional kernel32 import. Gates: build 0 warnings, suite 993/0 workers-invariant, scale-test exit 0. | 22,046,737 | 44,479,114 | 91,956,589 | 187,169,493 | 383,461,255 | 786,651,732 |
| 2026-07-22 | P1.6-C (per-instance ranged typealiases in generics) OPTIMIZATION PASS (independent). DECISION: CODE CHANGE (front-end only, no codegen change). CORPUS-BLINDNESS: ScaleCorpus is generics-free (no uses/with/nested-typealias), so the per-instance-alias paths (nested-typealias sweep recording, qualified Instance.Member resolution, per-instance E3005 arg check) are UNEXERCISED here; the Big-O below is by READING, cross-checked against the --per-type instrument. BIG-O PER SUSPECT (all LINEAR/O(1); confirmed by the --per-type growth column, every P1.6-C type x<=1.80 < 2.0): (1) nested-typealias sweep (recordScannedType/recordScannedSignature) -- an O(1) atNestedTypeAlias() peek per type-member and an O(1) enclosingInnerAliases.count() gate per method; innerAliasParamsIn (the only per-param walk) is GATED off unless enclosingInnerAliases is non-empty, so ZERO on generics-free; NO whole-program lookup nested in the per-file sweep (no O(files^2)). (2) innerAliases/methodInnerAliasParams registries -- O(1) Map lookups (isInnerAlias/innerAliasOf/innerAliasMember/methodInnerAliasParamsOf), no scans. (3) containsQualifier / qualified resolution -- an allocation-free O(len) byte scan per type-ref/coercion; the dotted-path split + queryProgramSignatures fetch is reached only for a name carrying a '.', never the plain-alias hot path. (4) valueInstanceAlias map -- shared-empty COW, O(1) populate/lookup, reset+save/restore at closure boundary. (5) erasure query (resolveNamedType) -- plain-alias checked FIRST (O(1) map), qualified path only for dotted names, queryProgramSignatures memoized (cache hit). (6) per-instance E3005 arg check -- O(1) map lookup per call, O(args) only when the callee has an inner-alias param. NO superlinear term. ATTRIBUTION of the committed-P1.6-C generics-free delta (isolated by rebuilding at 75bdb05fc pre-P1.6-C vs c38ad6306): committed P1.6-C added +2505/+4237/+7701/+14629/+28485/+56197 allocs (converging x2.0 = LINEAR, but a LARGE constant), REFUTING the implementer's 'allocates nothing on the generics-free path' claim. Dominant source = Parser.create allocating 5 NEW collections per construction (3 sharedEmpty* COW anchors + 2 working fields enclosingInnerAliases/valueInstanceAlias freshly created then immediately overwritten) -- a DIVERGENCE from this very file's documented sharedEmptyDescriptorNeeds/sharedEmptyTypeParamFeeds pattern (module-level globals, ONE alloc per compile, parsers INCREF). Secondary = checkPerInstanceArgs minting an InnerAliasParamLookup union HEAP-BOX on EVERY slotted call (bootstrap boxes every union case). FIX (this pass): (a) moved the 3 anchors to module-level globals and pointed the 2 working fields at them (incref, no per-parser alloc) -- realizing the implementer's stated intent; safe because the shared collections are STORED/increfed and never mutated (COW before any write), the exact invariant sharedEmptyDescriptorNeeds relies on. (b) gated checkPerInstanceArgs on a new allocation-free ProgramSignatures.hasInnerAliasParams() bool BEFORE the union-minting lookup, mirroring the parser's enclosingInnerAliases.count() gate and aggregateNameFor's containsQualifier gate. RESULT: generics-free delta vs pre-P1.6-C dropped from +2505..+56197 to +74/+82/+98/+130/+194/+322 (removed -2431..-55875, ~99.4%). Residual = the 2 per-file sweep out-arrays InnerAliasEntryArray/MethodInnerAliasParamEntryArray (x1.21, consistent with the 9 sibling per-file working arrays in foldDeclaredSignaturesInto -- kept) + one-time module globals + fixed-mainSource splitQualifiedName/QualifiedNameParts unions (x1.00). GATES: shv2 build exit 0 + 0 warnings + error-codes OK; suite 1005/0 exit 0, no leak/101; per-instance-typealias 7/7 (E3005 wrong-instance + cast-launder errors still fire); fragments byte-identical (git status specs-shv2/fragments empty -- front-end change, no golden moved); --workers 1==12 identical outcomes. The row's numbers are the POST-FIX build. | 22,052,825 | 44,488,146 | 91,971,509 | 187,196,189 | 383,511,503 | 786,749,084 |
| 2026-07-22 | P1.7 slice 1 (Array scalar/trivial-element core) OPTIMIZATION PASS (independent). DECISION: NO CODE CHANGE — no superlinearity found; the slice is allocation-neutral on this corpus. TRUE P1.7 MARGINAL (measured by building AND running BOTH binaries directly: HEAD 181be5d5c vs its parent HEAD~1 33ee161a1): +0 allocs at EVERY rung, +8 bytes FLAT (constant, not per-rung — 704-696 = 1024-1016 = ... = 8). That +8 is one grown RuntimeUsage record: the new `usesArray bool` field rounds the single per-compile record up 8 bytes; same allocation COUNT, +8 bytes, O(1) per compile. That is the ENTIRE measurable footprint of the slice on the array-free/generics-free/field-chain-free ScaleCorpus. STALE-BASELINE WARNING for THIS row's delta columns (+29/+45/+77/+141/+269/+525 allocs): they are vs the LAST LOGGED ROW (the P1.6-C optimization pass, ~commit 5095643cc), NOT vs P1.7's parent. The PARENT binary (HEAD~1, containing NONE of P1.7) reproduces the IDENTICAL +29/+45/+77/+141/+269/+525 delta against that same log row — so that entire term is PRE-P1.7, contributed by the two P1.6-C follow-up commits (e65a164c9 'per-instance alias decays to plain int at all coercion sites', 6ce5eb0dc 'P1.6-C CLOSED') that landed AFTER the row was recorded. It fits 13 + 16*2^n exactly (successive differences 16/32/64/128/256 double), i.e. a fixed 13-alloc constant + a 16-per-doubling-unit linear term = LINEAR, and not this pass's subject. The implementer's report attributed this stale-baseline delta to P1.7; the real P1.7 delta is +0 allocs / +8 bytes-flat. SUPERLINEARITY HUNT (corpus is array-free, so the new paths are verified by READING, cross-checked against the per-phase alloc diff HEAD-vs-parent = 0 in every phase). All new algorithms O(1) or linear: (1) structHasArrayField = O(fields), called ONCE per referenced struct in noteDestructorUsage's single per-compile pass = O(total fields) linear, NOT per-access; int fields hit fieldTypeIsArray's `tag != named` early-out with zero alloc (int is TokenKind.int -> MaxonType.integer, never `named`), so it is the allocation-free twin of structHasStringField on scalar fields. (2) fieldTypeOf / typeIsManaged / managedFieldDropCallee generic-alias retag = O(1) map lookups, gated behind maxonTypeTag==named. (3) resolveFieldChainFrom = fieldTypeOf computed ONCE and threaded into structLayoutOfField (the implementer's transient double-fieldTypeOf is gone; confirmed no redundant recompute remains); O(1) per hop, O(chain) per chain = linear. (4) isArrayInstance / arrayElementType / arrayElementIsManaged / isArrayBaseName = O(1); every parser call site (postfix loop, binding+value method dispatch, qualified static call, field-chain end) is gated behind a tag/alias check that short-circuits off the array path. (5) installArrayRuntime = O(1)-gated on usesArray, builds a FIXED ~18 helpers ONCE, independent of program size. (6) isArrayRuntimeCallee / isCompilerInternalCallee = O(prefix) bytesStartWith per call = O(1); internArrayIntegerInstance = idempotent O(1) intern, once per file that contains a `[`. (7) dispatchArrayMethod = a fixed 16-way name compare = O(1) constant per array method call. NO nested scan over ops/values/blocks/symbols, NO findFirst-in-a-loop, NO per-element fixpoint/liveness/layout rebuild. Whole-compiler per-phase and per-rung alloc ratios all <= 2.0 (rung4->5 x1.99), tracking the corpus's own x1.93..1.99 code growth — no phase superlinear. GATES: shv2 build exit 0 + 0 warnings + error-codes OK; suite 1079/0 exit 0, no leak/101; fragments byte-identical (git status specs-shv2/fragments empty). No code change was committed by this pass; the absolute numbers in this row are the committed 181be5d5c build. | 22,053,529 | 44,489,170 | 91,973,173 | 187,199,133 | 383,517,007 | 786,759,708 |
| 2026-07-22 | P1.5 value-merge residuals #31/#80/#81 (match/ternary value-merge finish) OPTIMIZATION PASS (independent). DECISION: NO CODE CHANGE — the rung is already LINEAR with a tiny constant and carries no superlinear term; the one common-path allocation is a per-file working array consistent with its siblings, and the whole-program drain is O(Sigma arms), all O(1) map lookups. ATTRIBUTION (isolated by diffing pre-P1.5 main@33ee161a1 vs worktree@ccb4d0211, both freshly built on the same idle box, so the delta is exact and reproducible): P1.5 adds +104/+176 allocs at rung0/rung1 (TRUE delta). The row-over-row deltas the log shows (+133/+221/+397/+749/+1453/+2861) are vs the P1.6-C-OPT baseline row; ~+29 of the rung0 gap is the intervening P1.6-C decay/review fixes (e65a164c9, 1430ad6a0), NOT P1.5. LINEAR: row-delta ratios 1.66->1.80->1.89->1.94->1.97 -> 2.0; the true-delta split is phase:signatures +83/+151 (x1.82) and phase:parse +19/+23 (x1.21), every allocation ~48 B (empty-array-sized). SOURCE: the sole common-path allocation P1.5 adds is the 11th per-file array PendingFnMergeCheckArray in FileParseArtifact.create — allocated once in the signature-sweep throwaway artifact (queryProgramSignatures builds one Parser/FileParseArtifact per file and discards all 11 working arrays, exactly as it already discards the 10 pre-existing siblings) and once in the real-parse artifact — plus 2 one-time arrays (Project.create, resetMergeTargets). CORPUS-BLINDNESS: ScaleCorpus has ZERO ternary/match value-merges and ZERO function typealiases, so the whole-program drain, the PendingFnMergeCheck records, and FunctionTypeAlias.display are ALL UNEXERCISED here; their Big-O is by READING. TASK-2 (COW vs fresh): pendingFnMergeChecks stays FRESH-per-file, matching its 10 FileParseArtifact.create siblings (funcSignatures/funcReturnTypes/rangeChecks/typeAliases/functionTypeAliases/...), NONE of which are COW'd. It is NOT a Parser.create sharedEmpty* COW anchor: those anchors are populated only for rare generic files and were moved to module globals last pass; this array is likewise populated only for rare merge files, but COW'ing it alone while its siblings stay fresh is exactly the inconsistency the file's pattern forbids ('do NOT COW something its siblings don't'). DRAIN O-ANALYSIS (checkValueMergeSignatures, Queries.maxon): per recorded merge, per arm — armFnTypeResolvable = funcSignatures.contains OR functionTypeAliases.contains (both Map with ByteArray keys, O(1)); armFnTypeDisplay = Map.get O(1) + formatFnTypeDisplay O(params). Total O(Sigma arms x params) = O(program), NOT O(merges x functions); commitPendingFnMergeChecks is O(Sigma checks) with no file^2 loop; no whole-program scan nested in the drain. buildFunctionDisplay: the 'fn(...) returns ...' display is built ONCE per function typealias on the REAL parse (readTypeAliasDeclaration buildFunctionDisplay:true at Parser.maxon:3450) only; the token sweep (recordScannedTypeAlias, :3069) and readNestedRangedAlias (:3688) pass false — no double-build. NOTED, not fixed (constant factor on a corpus-unexercised path, not the growth mandate; flagged to reviewer): readFunctionTypeAlias (Parser.maxon:3837) allocates paramTypes MaxonTypeArray unconditionally even for buildDisplay:false, though the :3835 comment claims the false path 'pays for neither the array nor the string' — still O(function-typealias count) = linear. GATES: no code change, so the committed build's gates stand (suite 1014/0 no leak; --workers 1==12 byte-identical; specs-shv2/fragments byte-identical — front-end only); worktree git status clean; scale-test exit 0. REBASED onto P1.7 main (ca9dc2b61): coordinator re-measured on the integrated tree — allocs UNCHANGED (P1.7 true delta +0 allocs), bytes +8/rung (P1.7 usesArray growing the once-per-compile RuntimeUsage record). | 22,058,513 | 44,497,610 | 91,988,525 | 187,228,309 | 383,573,831 | 786,871,828 |
| 2026-07-22 | P1.7 slice 2 (b"…" byte-string literals) optimization pass: INERT on corpus (no byte-strings) + linear-by-reading audit — no superlinearity. decodeByteStringLiteral single-pass O(literal len); registerByteArrayLiteralBlob reuses registerRdataConstant's amortized-O(1) rdataSizeSoFar cursor + byte-keyed dedup (NOT per-literal re-sum); internArrayByteInstance idempotent hash intern (once/file); __arr_decref drop-guard is a constant-time capacity<0 branch, not a scan. | 22,053,529 | 44,489,170 | 91,973,173 | 187,199,133 | 383,517,007 | 786,759,708 |
| 2026-07-22 | P1.5 #78 (indirect calls carry callee float result/param types, x64/arm64/wasm) OPTIMIZATION PASS - NOTHING SUPERLINEAR, NO CODE CHANGE, BIT-FOR-BIT INERT ON CORPUS. The +104..+2336 allocs / +4984..+112120 bytes this row prints is a STALE-BASELINE ARTIFACT (the OPEN #22 trap, same as the 2026-07-18 P1.4b row), NOT the rung's cost. Proven the immune A/B way: rebuilt the rung's OWN PARENT b2ca9c1d3 from source with the same bootstrap and ran scale-test on it -- parent and branch (HEAD) produce IDENTICAL numbers at EVERY rung and EVERY phase (rung0 362,783/22,058,513; rung5 10,511,399/786,871,828; lowerMaxonToStd/isel/regalloc all identical to the byte), and the PARENT already prints this same +104..+2336 delta vs the last logged row. So the rung's true footprint is EXACTLY 0 allocs / 0 bytes; the printed delta is pre-existing main drift between the last logged capture and b2ca9c1d3, present with none of these two commits. WHY inert: ScaleCorpus emits no function values / indirect calls, so appendCallIndirect and every backend callIndirect arm never fire; the sole per-every-program change -- the StdOp union gaining SCALAR resultType+argFloatMask on the callIndirect variant -- did NOT grow sizeof(StdOp) (callIndirect is not the union's widest variant), so module.ops is byte-identical. SUPERLINEARITY AUDIT (read-only, all LINEAR): the wasm functype intern is amortized-O(1) -- internFuncType (WasmBinary.maxon:917) dedups on the ENCODED-BYTES key via funcTypeDedup, a stdlib open-addressed hash Map (get/upsert O(1), key-hash O(args)); it is NOT the old O(distinct-signatures) findFirst scan. buildIndirectCallTypeIdxMap is ONE O(module-ops) walk; per callIndirect the key build (encodeFuncType/indirectCallParamValTypes) is O(args) and emitCallIndirect (StdToWasm.maxon:1436) is O(args) key + O(1) get -- total wasm cost O(module + sum-args), NO per-call scan over interned types. x64/arm64 emitIndirectCallArgMoves are single O(args) loops with two independent GPR/FP counters (argIsFloat is a bit test), no nested scan; indirectResultRegister/arm64IndirectResultRegister are O(1); front-end appendCallIndirect mask build is O(args). No code changed -> codegen trivially neutral (git status + specs-shv2/fragments/ clean). | 22,058,513 | 44,497,610 | 91,988,525 | 187,228,309 | 383,573,831 | 786,871,828 |
| 2026-07-22 | P1.7 slice 3a (CONCRETE managed-element arrays) optimization pass. Audit of the 5 slice surfaces: (2) element-destroy stamp arrayElementDestroyValue->managedFieldDropCallee is O(1) for String (early return) and O(fields) for a struct/union element via structHasManagedField — the PRE-EXISTING shared drop-callee resolution, field-count bounded in practice, no new class; (3) emitElementDestroyRange is emitted ONCE per module (installArrayRuntime, __arr_decref/__arr_clear/__arr_resize share it), the runtime walk is correctly O(length); (4) moveManagedValueInto / transferConsumedArg promotion is O(1) per element/arg and the literal parse moves each element in one-at-a-time so an N-element literal is O(N) not O(N^2); (5) the functionRef bare-__-address path (lower/scan/thunk-collect) is O(1) per funcref (a __ prefix check). FIXED (1): internArrayLiteralAggregateInstances over-interned Array-with-<every declared struct/union> once per [<identifier>...]-literal FILE, and structTypes/enumTypes ACCUMULATE across files, so it was O(files x types) throwaway allocations (each arrayInstanceForNamedElement builds a MaxonTypeArray+MaxonType.named+instance key only to hit the interner and discard). Added arrayLiteralElementsInterned (Set with ByteArray): a name already over-interned is skipped, so interning is O(types); a count early-out short-circuits the whole pass once every declared type is interned. giid-PRESERVING: the skipped calls were idempotent interner hits that assigned no gid, so the first-intern order (and every emitted gid) is unchanged — proven by the spec suite staying 1129/0 with ZERO fragment movement (git clean but for the one source file). INERT on the array-free scale corpus: the aggregate loop never runs (no [<ident>...] literals), phase:signatures byte-identical and x2.00. Movement vs slice-2 baseline is a FLAT +8 allocs/+336 bytes at EVERY rung (rung0==rung5) — the one empty dedup Set constructed per compile in ProgramSignatures.create(), a once-per-compile constant, NOT superlinear; the doubling remainder (+104..+2336) is slice-3a's own feature overhead vs slice-2, linear (x2/rung), not this pass. The O(files x types)->O(types) win only manifests on multi-file programs that heavily use [struct,...] aggregate literals (a brand-new feature; self-compile and the corpus have none). | 22,058,849 | 44,497,946 | 91,988,861 | 187,228,645 | 383,574,167 | 786,872,164 |
| 2026-07-22 | P1.7 slice 3a REVIEW fixes (element leak on set/clear/resize-shrink + E3102 cross-arg double-consume). Explains the movement vs the slice-3a optimization-pass row above. (A) The E3102 double-consume guard adds ONE ValueIdArray (`consumedOwned`) per CONSUMING call in transferConsumedArgs plus an O(consumed-args) scan — a LINEAR constant factor: allocsDelta DOUBLES per rung (+40,+80,+160,+320,+640,+1280 rung0..5), i.e. proportional to program size, all phase ratios stay x2.00 (phase:parse x1.91..x1.99). It is the same shape as the pre-existing per-struct-literal `movedOwningValues` allocation and is +1280/10.5M = 0.012% of rung-5 allocs. Not a new complexity class. (B) The array-runtime leak fix (managed elements destroyed at set/clear/resize-shrink via the shared emitDestroyRangeIfManaged gate, folding __arr_decref's inline gate in too) is INERT on this array-free corpus: its extra blocks live in __-prefixed runtime helpers emitted ONCE per module (installArrayRuntime), not per user program, so it adds nothing to the ladder. Suite 1131/0 no-101; --workers=1 == --workers=12. | 22,060,449 | 44,501,146 | 91,995,261 | 187,241,445 | 383,599,767 | 786,923,364 |
| 2026-07-22 | P1.7 COW slice B (O(1) zero-copy array slices; `__mm_incref` returns, the view being its first caller). +78 allocs / +3,566 bytes / +24 `.text` bytes, **FLAT at EVERY rung 0-5** — a per-MODULE constant, not a growth class; all phase ratios stay x2.00. ATTRIBUTION PROVEN BY A/B, NOT ASSERTED: a temporary patch restoring `emitRefcountCheckToLastOwner`'s old THREADED entry->lower shape (header+count computed once in `entry`, reused in `lower`), leaving all 616 other changed lines untouched, returned every rung to the previous numbers EXACTLY (0/0/0) and codeBytes to -24. So the entire movement is the 5 Std ops `emitAdjustRefcount` adds to `__mm_decref`'s `lower` block (header base recomputed twice + refcount reloaded) at the ONE reachable call site per module; per-phase it decomposes +6 un-bucketed / +17 isel / +55 regalloc / +1 sugarGate / **-1 prologueEpilogue** — the THREADED form allocates MORE there, because keeping header+rc live across the edge raises register pressure in `__mm_decref`, so threading is not free either. BOUGHT DELIBERATELY: it puts `owners - 1` and the refcount RMW in ONE function each, now that `ArrayRuntime.__arr_cow_detach`'s arm 2 reads that convention ACROSS a file boundary (a respelled `rc != 0` would not error — it would silently mutate a shared buffer), and makes the future green-thread `LOCK` prefix land on one site instead of two. Cost: 0.021% of rung-0 allocs, 0.00074% of rung-5. The 454-line ArrayRuntime COW change is otherwise INERT here (array-free corpus, `installArrayRuntime` never runs). NOTHING SUPERLINEAR: `emitArrayView` contains no loop, and nested views FLATTEN — a view's `parent@32` is always the ORIGINAL base, never another record, so there is no chain to walk. Measured, not read: 2,000,000 slices at constant total count with nesting depth 1/10/100/1000 → 121/97/107/98 ms (flat; a parent-chain walk would make depth 1000 ~1000x slower), and slice LENGTH 1k->100k at 1,000,000 slices → 48.9/49.4/50.9 ms, ~50 ns/slice, flat across 100x. Both are 100x-1000x discriminators, far outside the +-30% code-layout noise floor. Suite 1197/0 no-101; --workers=1 == --workers=12. | 22,064,015 | 44,504,712 | 91,998,818 | 187,245,011 | 383,603,333 | 786,926,921 |
| 2026-07-22 | P1.0d.4 residual slices A+C MERGED (float literals + math intrinsics). SLICE A — exact decimal->IEEE-754 float literals. parseFloatBits applied the decimal exponent by repeated *10 in an i64, which WRAPPED: 1.0e20 gave a wrong value, 1.0e64 silently became 0.0 (10^k contains 2^k, so k>=64 is exactly 0 mod 2^64), and 1.0e63 HUNG THE COMPILER FOREVER (10^63 == 2^63 == i64.min, negative, so the shiftUp normalization loop doubled a negative without bound). NOT A PORT: v1's NumberParsing.maxon:13-226 is BYTE-IDENTICAL to shv2's and carries the identical bug (shv2 inherited it by copying); maxon-sharp sidesteps it via .NET double.Parse. Replaced with bounded-width base-2^32 limb magnitudes; significand accumulated in 9-digit strides to a 768-significant-digit cap plus a STICKY flag feeding round-to-nearest-even; MaxBigLimbs=144 enforced by a panicking requireLimbBudget; subnormals and underflow-to-zero added (NEITHER EXISTED); overflow now decided by the CONVERTED VALUE, not a decimal estimate. COST +1,106/+2,194/+4,370/+8,722/+17,426/+34,834 rung0..5 — exact doubling per rung, LINEAR in program size, attributed exactly: rung 0 holds 278 float literals (--emit-corpus) and 1106/278 = 3.98, i.e. an exact conversion needs two heap magnitudes where the wrong i64 pair needed none, at 2 allocations per Array (Self{} envelope + first push's ensureCapacity). 0.30% of rung-0 allocations, 0.33% of rung-5's; all phase ratios unchanged x2.00-x2.11; NOT a new complexity class. The 768+sticky correctness fix itself is +2 allocs/+80 bytes FLAT at every rung. NO small-value fast path (it would be a second implementation of one fact) and none is needed: allocations are FLAT from 1 to 34 significant digits because a magnitude's width tracks its VALUE (empty array == zero), and 91% of the 1,402 float literals in shv2+stdlib carry 2-3 digits — ~1.9ms of a ~13s self-build (0.015%). Independent optimizer: NO superlinearity, NO code change. It verified the implementer's bigShiftLeft fix (a whole-limb insert(0,..) loop, Theta(shift x count)) by REVERTING it — real on allocations (-3 allocs/-696 bytes per literal) but A WASH on wall time, because Array.insert delegates to a memmove builtin (stdlib/Array.maxon:282) while the replacement is a bounds-checked element loop; kept because it is asymptotically right and allocates less, but it did NOT buy the time it appears to. REJECTED WITH MEASUREMENTS: hoisting pow10 out of bigMulPow10's stride loop (-0.9%..-3.1% wall, ZERO allocation change, and identical code read 79,368 then 70,215 us — a 13% load swing, so the gain is BELOW THE NOISE FLOOR of the only instrument that can see it); factoring 10^k = 2^k*5^k to fold 2^k into the binary exponent (would cut operand width 1.43x and the dominant divide loop ~30%, but it is a constant factor that rewrites the exponent bookkeeping the brand-new subnormal/underflow paths depend on — bit-risk against freshly-settled correctness); Knuth algorithm D for the 53-step mantissa loop; fusing bigCompare+bigSubtract. Worst admitted input is 768 digits at e-300 (~350-400 us/literal), NOT 1000 digits at e-324 (201 us) — the latter falls below the least subnormal, significandBits clamps to 0, and the divide loop runs ZERO iterations. INDEPENDENT REVIEW FOUND A BLOCKER THE SUITE WAS GREEN OVER: sciExp was clamped at a FIXED 100000, but it competes with decExp in magnitudeExp = (sigDigits-1)+decExp+sciExp, and decExp moves ONE PER DIGIT — so a long enough digit run OUT-PULLS the clamp and FLIPS THE VERDICT IN BOTH DIRECTIONS. 0.<200001 zeros>1e999999999 (true magnitude 1e999799997, astronomically past f64.max) was ACCEPTED AND COMPILED TO 0.0 with no diagnostic; 1<200000 zeros>.0e-999999999 got a SPURIOUS E2011. Both verdicts confirmed against the bootstrap oracle. The ceiling is now bytes.count()+DecimalWindowSpan, swept over 96 cases spanning the cancellation region, 0 disagreements, with two regression tests PROVEN to have teeth (reintroducing the fixed clamp fails exactly those two). Review also DEDUPED the carry flush, written THREE times (bigAddSmall, bigMulSmall, and a third spelling in bigShiftLeft that pushed UNMASKED and deferred the budget check) with nothing making them agree — folded into bigPushCarry so requireLimbBudget's claim that every growing site ends there is STRUCTURAL, not conventional. And it CORRECTED THE 768 BOUND'S JUSTIFICATION: the comment claimed midpoints terminate within 752 digits, but 752 is the width of 5^1075 ALONE, ignoring the odd significand which runs to 2^54-1 and adds up to 16 more — the true widest rounding boundary (2^54-1)*2^-1075 needs EXACTLY 768, so the budget has ZERO slack while its stated reason would have justified shaving 16 digits off it (the same wrong number appeared in the spec prose: one fact written down twice, wrong both times). Two further stale bounds that contradicted each other (341 vs the real 1091) corrected; the extremum literal reaches exactly 114 limbs against the budget of 144, measured by narrowing MaxBigLimbs and binary-searching, so requireLimbBudget CANNOT fire on admitted input. MEASURED DEBT FILED WITH ITS TRIGGER: bigMulPow10 and the digit accumulation are Theta(strides x limbs) — quadratic in exponent and digit count — LINEAR IN PRACTICE only because the real corpus tops out at 21 significant digits and the only extreme-exponent literals are a handful of type bounds. The trigger that would bend it is A WIDER FLOAT TYPE: binary128 pushes the decimal exponent 308->4932, the digit budget 768->~11,500 and MaxBigLimbs 144->~1,700, growing both quadratic terms ~15x per dimension (~225x). SLICE C — floor/ceil/round/sqrt on x64+arm64+wasm: BIT-FOR-BIT INERT on this corpus, independently confirmed (main's own binary reproduces the last logged row with delta 0, AND the branch's full scale-test stdout is BYTE-IDENTICAL to main's across all six rungs, every phase, every regalloc bucket). x64 roundsd 66 0F 3A 0B /r imm8 (floor 01, ceil 02, round 00 — the BOOTSTRAP's plain immediates, deliberately NOT v1's 0x08/0x09/0x0A, whose extra bit is the Precision Mask: it changes no value and shv2 reads no MXCSR flag) via a NEW encodeRoundsdRegReg, a three-byte opcode plus a trailing imm8 that encodeSseRegReg structurally cannot express; sqrtsd F2 0F 51 /r on the existing path; arm64 frintm/frintp/frintn/fsqrt; wasm f64.floor/ceil/nearest/sqrt. round is ties-to-EVEN on all three targets, both references agree, specs/round.md's round.halfway pins round(2.5)==2, and it was verified against the running oracle. NOTE: shv2's x64 baseline moves from SSE2 to SSE4.1 HERE AND NOWHERE ELSE. The four new callee.equals in parseCallNamed allocate NOTHING (Array.equals short-circuits on length, stdlib/Array.maxon:581); numericBuiltinOperand/emitMathIntrinsic build a String only inside their two throw branches. REJECTED: replacing the 8-way builtin chain with a hash/table lookup — Array.hash walks EVERY callee byte unconditionally where the chain mostly fails on length alone, and a miss returning a payload-free union case would RE-INTRODUCE the very allocation the implementer had just removed. That removal is itself the slice's best find: a first-draft union MathIntrinsic{notAnIntrinsic, op(..)} resolver moved memory +85/+153/+289/+561/+1105/+2193 (doubling per rung = ONE ALLOCATION PER PARSED CALL); the implementer established the true baseline by running main's own binary, A/B'd it, and discovered that A PAYLOAD-FREE UNION CASE STILL HEAP-ALLOCATES — replaced with direct dispatch, back to parity. INDEPENDENT REVIEW FOUND A FORKED DECISION: the StdUnaryOpcode->FloatRoundMode mapping was written TWICE, in StdToX64Conversion and StdToArm64Conversion, with nothing making the copies agree — and the arm64 site's comment ASSERTED the property the code lacked ("decided once, encoded twice"). A shared enum makes both copies spell the same member NAMES; it does not make the MAPPING shared. Change ceil to towardNegative in one isel and NEITHER SITE READS AS WRONG: x64 silently computes floor where the program said ceil — a WRONG ANSWER, not a build break — and asymmetrically so, because the x64 copy is pinned by committed goldens while arm64's can be regenerated on NO host available here, so the weakly-gated copy is the one free to drift. Un-forked into floatRoundModeFor in StdLoweringShared beside accessWidthFor/unaryResultClass; each isel's three arms collapse to one floor-to-round range and sqrt PANICS rather than getting an arm; fragments bit-for-bit identical, proving the lowering unchanged. Review also verified the roundsd encoder at a byte level THE SUITE STRUCTURALLY CANNOT REACH (committed tests only ever touch xmm0-xmm2, so REX on roundsd is untested): forcing 11 simultaneously-live floats produced 66 45 0F 3A 0B C0 01 (the 66 prefix BEFORE REX, with REX.R and REX.B both set) and F2 45 0F 51 CF = sqrtsd xmm9,xmm15, the program returning 91 against an independent computation. The band-append invariant was RE-DERIVED INDEPENDENTLY rather than trusted: TargetOp has 107 members, insertions at ordinals 52-53 and 100-101, 19 straddling range arms enumerated and each read in context and confirmed correct for an xmm->xmm plain def; the three shared Target files the implementer had requested needed ZERO edits and none was manufactured. TWO ORACLE DEFECTS FOUND, both maxon-sharp, both where shv2 is RIGHT: the bootstrap answers sqrt(true) with an internal E9001 plus a C# stack trace where a positioned diagnostic belongs (shv2 gives a clean E3005), and it CRASHES with E9001 "all FP registers occupied" under a 20-live-float pressure probe that shv2 compiles correctly (255, matching wasm). MEASURED DEBT FILED, QUANTIFIED AND ATTRIBUTED, NOT FIXED: the payload-free-union allocation generalizes to existing code and is WORSE than first reported — parseCallNamed constructs TWO such boxes per parsed call, ReceiverArg.none AND ConstructorInstance.none on the same line (Parser.maxon:15507; ReceiverArg.none also at :12786) — costing 174/310/582/1,126/2,214/4,390 allocations and 2,784..70,240 bytes across rungs 0-5, LINEAR (increments 68/136/272/544/1088 double exactly), 0.042% of rung-5 allocations. Static call counts (87/155/291/563/1107/2195) cross-validate to within 2 per rung against the implementer's independent dynamic A/B. ATTRIBUTION IS BOTH CODEGENS, which is what decides where it belongs: the measured allocations are the BOOTSTRAP's (MaxonToStandardConversion.cs:533-541 calls EmitAlloc(8+maxPayload*8) UNCONDITIONALLY; the zero-arg case only empties argsList), and shv2's OWN lowering does the identical thing (verified from emitted IR: two 16-byte __mm_alloc for the same union shape) — SO THE COST SURVIVES SELF-HOSTING and is not a bootstrap-only artifact. ALSO MEASURED, TREE-WIDE AND PRE-EXISTING: the bootstrap forms a jump table only for >=4 DENSE INDIVIDUALLY-NAMED arms (JumpTableFormationPass.cs:12,55,59 — Threshold=4, ordinals dense from 0), so ANY `to` range arm demotes a whole match to a linear compare chain (probe: 9 named arms -> a 9-case x64 jump_table; the same enum with two range arms -> a linear cmp chain). This slice demoted StdToWasm.unaryResultValType (5 dense arms -> range arms); UPHELD by the reviewer, and NOT because the cost is small (<=4 compares in a compile-time-only wasm-lowering helper) but because StdLoweringShared.unaryResultClass is that table's TWIN, answers the same question for the native targets, and ALREADY read neg-to-logicalNot on main — leaving one collapsed and the other not would be one fact written down twice in two shapes. The big TargetOp matches (targetOpOperands/colorTargetOp/opPhysicalRegs) already contained range arms and were NEVER jump tables: pre-existing, unchanged in kind. maxonUnaryOpToStdOpcode and both native lowerUnaryOp went 5->9 dense arms and KEEP their jump tables. THE GENERAL TENSION IS TREE-WIDE AND UNRESOLVED, recorded here as a finding and not acted on: CLAUDE.md's "consolidate redundant match arms" rule systematically demotes jump tables to compare chains in this compiler. TRAP REPAIRED BEFORE THIS RUN: the previous row's allocations and bytes notes had DIVERGED (one hand-edited to read "Same row as the allocations table"), and scale-test correctly REFUSED to measure anything until they matched — the log is the artifact, so the LOG was repaired rather than the instrument weakened. GATES on the merged tree: csharp+shv2 builds exit 0 with ZERO warnings; specs-shv2 1249/0 exit 0 no-101 (1175 baseline -> 1210 slice A -> 1234 after rebase over upstream P1.7 COW slice B -> 1249); --workers=1 and --workers=12 BYTE-IDENTICAL; specs-shv2/fragments additions only, zero M. | 22,103,839 | 44,583,704 | 92,156,146 | 187,559,011 | 384,230,677 | 788,180,953 |
| 2026-07-23 | #108 — __mm_free POISONS the freed payload, ALWAYS ON. Bump alloc never reuses a slot, so freed memory keeps its bytes and a use-after-free reads what it wrote before the free — INVISIBLE to every test. __mm_free now overwrites [ptr, ptr+size) with 0x3F the instant a box dies, turning the class loud (read-after-free returns 63 or faults on a non-canonical poisoned pointer). Unconditional, not a flag (user directive). MOVEMENT +352 allocs / +15,442 bytes FLAT at every rung = a per-MODULE constant, NOT the emitted program's poison cost (scale-test structurally cannot see that — shv2 is bootstrap-compiled). It is the COMPILER compiling the now-larger __mm_free (4 blocks vs 1, once per program via installMmRuntime): more ops through isel/regalloc/encode for that one runtime function. Flat across rungs = per-module, not program-proportional, not superlinear. codeBytes +64 = the poison loop's machine code, once per program. GOLDEN-NEUTRAL: __mm_free is __-prefixed (excluded from fragments), so ZERO committed fragment moved. PROVEN TEETH (a gate is not evidence until seen RED): moving __str_append's release ahead of its blit (the #43 bug) made `var s=abc; s.append(s); s.append(s)` print abcabc?????? under poison, restored -> abcabcabcabc; reviewer re-proved RED with a different __str_decref sabotage (0xC0000005 fault). Suite 1308/0 under poison — no committed test masks a UAF the suite reaches. REVIEW single-sourced the byte-fill loop (a near-copy of ArrayRuntime.emitZeroRange) -> MmRuntime.emitFillRange(dest, byteCount, fillValue), homed in the base layer ArrayRuntime already depends on; 4 array zeroing sites now share it, emitZeroRange deleted (net -14 lines). Pairs with the zeroing contract: must land BEFORE a recycling free list (a reused slot must then be re-zeroed on alloc, poison-then-zero, or [length,capacity)-reads-zero breaks). General cure for the bump-masked-UAF class; #40 and the P1.7 borrow-on-get residual are now poison-detectable the day they become suite-reachable. | 22,119,281 | 44,599,146 | 92,171,597 | 187,574,453 | 384,246,119 | 788,196,404 |
| 2026-07-23 | match-dispatch optimization pass (independent optimizer over commit 8743e80fb — I did not write it). Two O(n²)-in-arm-count defects in the match LOWERING path, removed. ⚠ SCALE-TEST IS BLIND TO BOTH: the corpus has no `match`, so every rung reads delta 0 — the numbers here MOVED NOTHING and cannot; both fixes were measured with a hand-built adversarial match (256..8192 distinct integer arms), the compiler's own --metrics parse-phase nanos, min over 6-8 runs on an idle box. (1) normalizeDispatchIntervals (Parser.maxon) was a subtract-and-insert that, per raw interval, scanned the whole claimed set AND inserted into the middle of a sorted array — Θ(n²) in EVERY input order (ascending pays two from-zero scans + cheap append; descending/random pay the O(n) mid-array shift). Isolated via range-form arms (which skip the dup check): ORIGINAL parse 21.06/62.17/195.90/700.93 ms at n=1024/2048/4096/8192, ratios 2.95→3.15→3.58 per doubling (quadratic); NEW 12.10/24.45/48.69/99.89 ms, ratios ~2.0 (linear) — ~600 ms of O(n²) gone at 8192, and now ORDER-INDEPENDENT (asc/desc/rand all ~equal). Replaced with an offline sweep: hand-written stable merge sort of the interval indices by lo, then a left-to-right sweep with a min-heap KEYED BY ARM PRIORITY so the earliest-arm owner is the heap minimum; a segment closes only when the owner changes (an earlier arm activating at its lo, or the owner ending at its hi). Reproduces the old first-wins partition SEGMENT-FOR-SEGMENT — specs-shv2 stays 1266/0 with fragments byte-identical (M on Parser.maxon ONLY), proving identical codegen. (2) recordDuplicateInt (pre-existing, the O(n) lookup inside the O(n) arm walk — the classic) linear-scanned a ParsedIntArray of seen values per point arm → Θ(n²). Isolated as point-form minus range-form parse: 1.46/7.59/31.4/126.7 ms at n=1024..8192, ratios ~4x (quadratic). Replaced the list with a hash Set (SeenPatternValues = Set with ParsedInt); membership is O(1), throw-on-first-duplicate-in-arm-order preserved (E2027 still fires, verified). Combined point-form parse 8192: was climbing to 226.57 ms (normalize-fixed only) → 96.65 ms both-fixed, ratios flat 2.00/2.01/2.04 = linear. Gates: shv2 build exit 0 zero warnings; specs-shv2 1266/0 exit 0 no-101; fragments/ clean (byte-identical codegen); scale-test delta 0 every rung (blind corpus, above). NO fast/slow fork, NO instrument edit. | 22,119,281 | 44,599,146 | 92,171,597 | 187,574,453 | 384,246,119 | 788,196,404 |
| 2026-07-23 | #65 implicit-self method resolution (bare `foo()` inside a method resolves to a sibling method). allocsDelta 0 on EVERY rung — the lazy per-type `siblingReceivers` map never builds on the scale corpus (its generated programs make no intra-type calls), and a bare call routes through the EXISTING `parseCallNamed`/`ReceiverArg` path (byte-identical IR, zero fragment M). The only movement is a small positive bytesDelta = 3 new per-`Parser` fields (`siblingReceivers`, `siblingReceiverScanType`, `enclosingTypeBodyStart`, ~24 bytes) times the parser count per rung — a per-FILE constant factor, NOT a new allocation and NOT superlinear (it doubles with file count exactly as the corpus doubles). The memoized-lazy design is the minimal footprint: eager would add a Map per type; no-memo risks quadratic re-scans. Review found + fixed two duplication defects (single-sourced self-capture refusal via requireNotInSelfCapture, and the static-method token test via functionDeclIsStatic) — neither touches allocation. Suite 1347/0, --workers=1==12 byte-identical, no 101. | 22,121,729 | 44,603,322 | 92,179,229 | 187,588,997 | 384,274,487 | 788,252,420 |
| 2026-07-23 | #40 conditional-ownership rung CLOSED (retain-promotion, approach A per user ruling) — the reachable UAF where a borrowed managed-aggregate PARAM re-bound to a var and conditionally reassigned to owned in a nested block dropped the fresh box at the inner block while the outer var was read past it. Fix: at `var q = <borrowed struct/union/generic-instance>` emit a NEW `__mm_retain(p)` (the incref twin of __mm_incref but RETURNS the ptr, so q gets a FRESH owned ValueId co-owning p's box) so q is owned-from-declaration, the buggy borrowedToOwned push-at-wrong-depth branch never fires, and the refcount balances on EVERY path (then decrefs the retained box on reassign + drops the new box at exit; else drops the retained box at exit, caller keeps ownership). Mirrors promoteMutableString with an incref instead of a copy (struct = reference semantics, so a copy would break aliasing — verified q.x=100 shows through to p on both compilers). A BOUNDED refcount exception like COW #105 — expands the no-runtime-refcount thesis to borrowed-aggregate-param vars (user sanctioned). allocsDelta 0 on EVERY rung + bytesDelta +8 constant (one usesRetain bool on the per-compile RuntimeUsage record): the scale corpus emits NO borrowed-aggregate-param vars so __mm_retain never fires and IR is byte-identical there; the retain/decref pair is O(1) per such var (one at declaration, one at scope exit) — linear, no superlinearity. Review found + fixed a rung-INTRODUCED blocker the green suite hid: making var q=p OWNED opened the reassignment String-only promoteBorrowed branch to aggregates, so q=p2 (reassign to ANOTHER borrowed param) blitted struct fields as a String record → garbage 5111944 / 0xC0000005 fault where the oracle gives 8; fixed by consolidating the promotion dispatch into promoteBorrowedToOwned used by BOTH declaration and reassignment. Full refcount-balance matrix verified under poison-on-free #108 (then/else/never-reassigned/break/continue/return-adopt/loop-carried/nested/match-arm/managed-String-field): no 101, no 0x3F3F3F3F, no fault. Fresh-ValueId proven: let r=p reborrow does NOT poison q; let r=q moves q not p. Suite 1360→1362→1366/0 (rebased over upstream), --workers=1==12 byte-identical, wasm 1143/144 (pre-existing), fragments additions-only zero M on existing/String goldens. Closes P1.2 (String+ownership) — the reachable residual is gone; String constructs were always correct. ⚠ STALE-BASELINE CORRECTION (coordinator, post-merge A/B): the PRINTED row delta above (+196/+292/+484/+868/+1636/+3172 allocs; +8384..+135360 bytes) is NOT #40 — it spans the UPSTREAM perf commit ffc521235 (jump-table distinct-successor dedup, O(span x distinct)->O(span)) that landed between the #65 log row and this one. PROVEN: #40's PARENT 134600b69 (upstream perf, NO #40) has BIT-IDENTICAL absolute allocs (rung0 364563 == merged 364563; rung1 693605==693605; rung2 1351167==1351167). So #40's ISOLATED footprint is +0 allocs / +8 bytes (the single usesRetain bool on the per-compile RuntimeUsage record), exactly as claimed; the entire printed delta is the perf commit's, an OPEN-#22 stale-baseline artifact. | 22,130,113 | 44,615,802 | 92,199,901 | 187,626,053 | 384,344,311 | 788,387,780 |
| 2026-07-23 | P1.7 wasm array support (commit 43d5c767e) — throwing-accessor signatures + void indirect calls. Two wasm-only correctness fixes, both invisible on native: (1) the 7 throwing array accessors (get/set/first/last/pop/remove/slice) now stamp `throws ArrayError`, so the wasm signature's error-flag result matches the dual-register call ABI, plus an install-time DRIFT GUARD (verifyThrowingAccessorsMatchPredicate) tying that stamp to isThrowingArrayRuntimeCallee in BOTH directions; (2) StdOp.callIndirect.resultType widened StdType to StdReturnType (void or value), so the per-element destructor call interns a wasm functype with ZERO results instead of a spurious i64 result that trapped call_indirect (exit 134). ALLOC BIT-NEUTRAL, A/B PROVEN: I rebuilt the base 238c304ed shv2 and re-ran scale-test — absolute allocs 374,958 / 711,349 / 1,385,503 / 2,732,489 / 5,423,241 / 10,807,360 and every byte are BIT-IDENTICAL to this rung at every rung and every phase and regalloc bucket. This rung's own footprint is 0 allocs / 0 bytes. WHY neutral: the drift guard is O(module.functions) and allocation-free on the happy path (a value-union match + a 6-byte prefix compare + 7 Array.equals; the interpolated panic String builds ONLY on drift); the throws stamp only sets an existing IrFunction field (the `ArrayError` String is a single module-scope `let`); StdReturnType is a VALUE union whose value arm wraps the StdType enum with no managed heap field, so callIndirect keeps its ONE managed field (`args`) and adds no per-op allocation; and on the native scale-test path the void-vs-value distinction is only whether an unused result register is captured — no allocation change, and fragments byte-identical (native codegen unchanged, verified). The PRINTED row delta (+10,395 / +17,744 / +34,336 / +67,013 / +129,413 / +256,237 allocs; +1,730,786 .. +26,692,036 bytes, roughly doubling per rung = LINEAR, a pre-existing per-program constant factor) is ENTIRELY a stale-baseline artifact predating the base: the base 238c304ed run — which does NOT contain this rung — prints the IDENTICAL delta against the last logged #40 row, so the movement belongs to commits between the #40 log row and the base, not to P1.7. This row RE-BASELINES the log to the current absolute numbers so the next rung measures from a correct baseline (OPEN-#22). Rung-over-rung alloc ratios 1.90 -> 1.95 -> 1.97 -> 1.98 -> 1.99 approach 2.0 = clean linear, no superlinearity. Suites: specs-shv2 native host 1229/0, wasm32-wasi 1291/0, both exit 0 no-101; fragments clean. | 23,860,899 | 46,708,674 | 96,337,706 | 195,154,349 | 398,340,798 | 815,079,816 |
| 2026-07-23 | P1.7 3b-ii/3b-iii (funcAbs64InRdata data->text reloc + opaque-T managed-element ownership), rebased onto main. OPTIMIZATION PASS (no compiler change): all 5 superlinearity candidates confirmed LINEAR by reading -- bakeFuncAbs64Relocs builds its debug-symbol index ONCE then O(1) Map.get per reloc (O(symbols+relocs)); rootManagedOpaqueArrayElementDrops is O(instances x fields)=O(program); scanConsumeBits/markOpaqueArrayMutatorFeeds is O(body tokens) once per method (bounded per-mutator-call arg re-walk, constant factor); managedOpaqueArrayElementOf is O(fields). ISOLATED HEAD-vs-main (this rung alone) = a NET REDUCTION on the generic-free corpus: -309/-645/-1317/-2661/-5349/-10725 allocs across rungs, ~x2/rung (LINEAR), essentially all in phase:encode. WHY: the rung removed the ONLY payload-carrying case, funcAbs64InRdata(String,String,ByteOffset), from union RdataRelocKind (it moved onto the new PendingRdataReloc struct on GlobalDataTable). RdataRelocKind therefore collapsed from a BOXED union to an integer-backed enum, so every RdataRelocation.kind -- one per code->rdata relocation (float islands, String-literal records, .data global leas) -- stores an inline tag instead of heap-boxing the union. One fewer alloc per relocation; relocations are linear in program size. COST: exactly +1 alloc/+48 bytes per COMPILE (flat every rung, phase:load) = the new empty PendingRdataRelocArray in GlobalDataTable.create(), a per-compilation constant, not superlinear; all other new machinery is generic/managed-opaque gated and never runs on this corpus (the isolated measurement shows the only non-encode movement is that flat +1). The recorded delta-vs-#65 (-113..-7553 allocs) is this rung's -309..-10725 SUMMED with the native-jump-table rungs (3df3d78d5+ffc521235, on main since #65 but never given a scale row) which ADD an rdata blob + per-slot relocs per dense match (+196..+3172 allocs, measured main-vs-#65). Suite 1352/0, no leak. | 22,121,337 | 44,596,786 | 92,160,405 | 187,545,597 | 384,181,935 | 788,061,564 |
| 2026-07-23 | P1.7 3b-iv (transitive array-feed fixpoint for opaque-T managed-element forwards). Fixes a reachable double-free: a method that FORWARDS its type-parameter element argument to a consuming sibling (add -> store -> self.items.push) now marks the forwarder's parameter a FEED via a least-fixpoint over the self-call forward graph (recordTransitiveArrayFeeds, mirroring solveDescriptorNeedFixpoint but per-parameter with forward edges + a linear reverse-reachability BFS), so the outermost concrete call site promotes-and-consumes the managed argument exactly as the directly-pushing method's site does. ZERO delta every rung, byte-for-byte: the whole pass is gated on enclosingTypeParams (a generic type), and the scale corpus is generic-free, so it costs one isEmpty() check per type declaration and allocates nothing. Suite 1355/0 (1352 + forward-to-consuming-sibling/multi-hop-forward/trivial-element-forward), no leak, -j1==-j12. | 22,121,337 | 44,596,786 | 92,160,405 | 187,545,597 | 384,181,935 | 788,061,564 |
| 2026-07-23 | P1.7 Finding A (bare opaque-T MANAGED field reassignment now WORKS, was E2015). Two parts: (1) decoupled __drop_type_param's install from usesArray onto its own usesDropTypeParam bit (installDropTypeParamRuntime) so an array-free generic struct's field-reassign drop links; (2) front-end: emitFieldWrite drops the old opaque value via the descriptor-gated __drop_type_param (shared body) or the retyped concrete decref (concrete instance) and transfers the new, gated on a managed instantiation; markOpaqueFieldReassignFeed makes the reassigned param an owned feed; a reassignsOpaqueField descriptor-need signal; SignatureIndex's managedOpaqueArrayElementOf broadened to stamp destroyFunc@40 for a bare managed opaque field too. Constant +16 bytes/rung, allocsDelta 0, ratios unchanged (linear x2.0x): the whole feature is generic-gated and the scale corpus is generic-free, so the only movement is the one extra bool field on the single per-compile RuntimeUsage struct. Suite 1393/0 (1391 + reassign-in-loop + struct-element, 2 flipped from E2015-reject), no leak, -j1==-j12, wasm 1303/0. | 22,121,353 | 44,596,802 | 92,160,421 | 187,545,613 | 384,181,951 | 788,061,580 |
| 2026-07-23 | P1.7 slice 3b-vi-b: managed-element array slice/clone/append via eager per-element deep-clone (__str_clone + __clone_<T> cascade + _managed array copies). The emitted runtime is managed-array-gated (usesManagedArrayCopy/neededCloners), so the generic/managed-free scale corpus installs NO cloner and adds NO runtime. But this FIRST cut still allocated one clonerSymbol ByteArray per struct+enum in the membership checks of noteClonerUsage + installStruct/UnionCloners — paid on EVERY compile including the managed-free corpus — a LINEAR allocsDelta +126/+206/+366/+686/+1326/+2606 (~1.9x/rung) concentrated in the install step. Fixed in the very next row by an empty-set early-out; the net rung cost is the constant one-Set field on RuntimeUsage. | 22,126,881 | 44,605,846 | 92,176,505 | 187,575,777 | 384,240,331 | 788,176,536 |
| 2026-07-23 | P1.7 slice 3b-vi-b optimize: early-out noteClonerUsage/installStructCloners/installUnionCloners when neededCloners is empty. The prior row's linear allocsDelta (+126..+2606, ~1.9x/rung) was one clonerSymbol ByteArray allocated per struct+enum in the membership checks — paid on EVERY compile including the managed-free scale corpus. With the empty-set early-out that cost is now managed-copy-gated only; the corpus references no cloner, so the delta should return to ~0. | 22,121,537 | 44,596,986 | 92,160,605 | 187,545,797 | 384,182,135 | 788,061,764 |
| 2026-07-23 | P1.7 slice 3b-vi-b-β: OPAQUE-T managed slice/clone/append via descriptor copyFunc@32 + funcAbs64InRdata reloc + __arr_{slice,clone,append}_opaque wrappers. Expected allocation-neutral: the scale corpus is non-generic and managed-free, so usesOpaqueArrayCopy stays false (no wrappers/stamps/cloner-roots) and the new descriptor-need copy signal fires only inside generic bodies. MEASURED: allocsΔ0 on every rung; bytesΔ a constant +8 (flat across all six rungs, not scaling) — the once-per-compile RuntimeUsage box gained one 8-byte slot for the new usesOpaqueArrayCopy bool field. | 22,121,545 | 44,596,994 | 92,160,613 | 187,545,805 | 384,182,143 | 788,061,772 |
| 2026-07-23 | P1.5-B2 #88 optimization pass (un-awaited Promise drop-reclaim + cancel). allocsDelta 0 / bytesDelta 0 EVERY rung, all phase ratios linear (parse x2.0, isel x2.00, regalloc x2.06) — byte-identical scalar-corpus memory traffic vs the last logged run (P1.7 Finding A; note the gap also spans the unlogged P1.5-B1c — both are async-runtime/gated, so the zero delta covers both). No compiler-side change made: the rung was already optimal. WHY zero: (1) the 4 front-end hot-path additions (parseBinding/reassignBinding/decrefCalleeFor/requireBindingLive) are single O(1) valuePromiseMarks BoolArray.get column reads — seeded in mintValue/recordValueType lockstep like every other per-value column, panic-interp lazy so zero alloc on the success path; the async-free corpus exercises them on every binding/reassign/read and they cost nothing measurable (constant factor, no bent curve). (2) __gt_promise_drop + emitGtRunQueueRemove/emitGtTimerStoreRemove/emitGtProcStoreRemove + the shared emitGtStructReclaim (a de-dup of the await-tail reclaim) + the __gt_live_count leak gate are runtime functions emitted ONCE per program (O(1) compiler cost) and never run in the async-free corpus. The emitted runtime scans are O(pending/parked GTs) but linear-in-practice: simultaneously-live un-awaited promises are bounded by the fixed count of source-level promise bindings (a loop reuses ONE binding, so it cannot accumulate — spawn-drop-loop-bounded emits a tight per-iter __gt_spawn;__gt_promise_drop where the dropped GT is the sole ready queue head → O(1) head-removal). A runtime O(N^2) would require O(N) literal un-awaited-and-parked spawn sites = O(N) source; not triggerable from a bounded loop (measured-debt shape, like SplitLiveRanges K^2). Gates: suite 1403/0, exit 0, no leak / no GT-abort(75), -j1==-j12 byte-identical (async 63/63 both), fragments clean. | 22,121,353 | 44,596,802 | 92,160,421 | 187,545,613 | 384,181,951 | 788,061,580 |
| 2026-07-23 | P1.7a slice 1 (interfaces front-end): interface decls + declared-conformance checking (E3015/E3016) + `"{userStruct}"` direct-toString interp. Allocation-neutral for the conformance-free scale corpus: checkConformance short-circuits on zero conformances, and the artifact's new `interfaces`/`conformances` arrays point at SHARED EMPTY singletons (copy-on-write on first record) so an interface-free file allocates nothing extra. A first cut eagerly allocated those two empty arrays per FileParseArtifact — a linear allocsDelta +216/+360/+648/+1224/+2376/+4680 (~2x/rung, scaling with file count) — fixed here by the shared-empty pattern (same discipline as sharedEmptyInnerAliases). Residual per-compile cost is a small constant: createProject + resetMergeTargets each allocate the project-level interfaces map + conformances array once. The bytesΔ still scales (+2,144/+3,296/+5,600/+10,208/+19,424/+37,856, ~2x/rung asymptotic) but allocsΔ is FLAT +12 — that combination is the per-file FileParseArtifact RECORD gaining two pointer fields (interfaces + conformances, 16 bytes): the record is ONE allocation, just 16 bytes larger per file, so the byte total scales with file count while the alloc count does not. It is the minimal representation of the per-file contributions (the pure-parser invariant forbids writing them anywhere but the artifact), the per-file analogue of the prior row's per-compile "+8 bytes for a bool field". | 22,123,689 | 44,600,290 | 92,166,213 | 187,556,013 | 384,201,567 | 788,099,628 |
| 2026-07-23 | P1.5 #93 (runProcess is a THROWING builtin, Design A scalar SubprocessError) optimization pass — NO compiler change: the rung is footprint-neutral and introduces no superlinearity or duplication. ATTRIBUTION CORRECTED. The instrument shows allocsDelta +4 / bytesDelta +192 FLAT on every rung vs the prior row (P1.5-B2 #88 opt pass, 22,121,353). The implementer attributed that to the unlogged eb3c834b0 (#88 review). That is WRONG: an A/B/C over three source states, ALL rebuilt with one identical current bootstrap, shows they are byte-for-byte identical (rung0 22,121,545 … rung5 788,061,772, every phase, every regalloc sub-bucket): (A) HEAD 8f16a08db (#93), (B) base c90c973a2 (post-eb3c834b0, pre-#93), (C) 55b3a24f9 (the very commit that WROTE the 22,121,353 row, BEFORE eb3c834b0). So #93 isolated = 0 AND eb3c834b0 isolated = 0. Since 55b3a24f9 rebuilds to 22,121,545 (+192 vs its own committed 22,121,353), the +4/+192 is a BUILD-PROVENANCE offset, not an shv2-source regression: stdlib + maxon-sharp are byte-identical across 55b3a24f9..HEAD, so the branch was rebased after row 300 was recorded and row 300's shv2 binary was emitted by a different (pre-rebase) bootstrap that lowered shv2's per-compile setup with 4 fewer boxed temporaries. Flat-per-compile (not scaling), so it is a constant, never a bent curve. This row RE-BASELINES the P1.5 line at 22,121,545; future rows compare here and the provenance offset is absorbed. WHY #93 is zero on this async-free corpus: (1) the throwing __gt_process_run / __gt_proc_add rewrites and the new SubprocessError constants live in usesGt-gated GT builders that never install without async; (2) isThrowingRuntimeCallee is a clean O(1) NON-allocating OR-compose of isThrowingArrayRuntimeCallee + isThrowingGtRuntimeCallee (ByteArray.equals = length-check then byte loop bounded by the ~16-byte callee name; bytesStartWith is a prefix compare) — NOT a copy; validatePlainCall's E3057 gate now runs isThrowingRuntimeCallee on every plain call, one extra byte-equals per non-throwing callee = a constant factor, linear in program size, allocation-free, no bent curve; the per-family message recompute of isThrowingGtRuntimeCallee is error-path only (once per diagnostic). No new per-value column, no nested scan, no repeated rebuild. Gates: build zero-warnings, suite 1429/0 exit 0 no leak / no GT-abort(75), fragments clean (only the committed async-subprocess try-rewrite fragments moved, which are HEAD). | 22,121,545 | 44,596,994 | 92,160,613 | 187,545,805 | 384,182,143 | 788,061,772 |
| 2026-07-23 | P1.5 #64 borrowed-container boxed-union field throw made a RETAIN (retainThrownField: one __mm_incref in emitThrowError's borrowedField branch, replacing the E2015 reject). #64 ISOLATED FOOTPRINT = 0, A/B-PROVEN: parent 1087e8501 with Parser.maxon stashed, rebuilt on the same bootstrap, is BYTE-IDENTICAL to this build every rung (rung0 364,270 allocs / 22,123,689 bytes) — the retain fires only on a borrowed-container boxed-union field throw, which the arithmetic corpus never generates, so the parser walks an identical path with and without the change. The growing +2,144..+37,856 bytes vs the prior (#93) row is NOT #64: it is drift from the #93 review commit 7f3721b00, which landed AFTER the #93 perf row 64eba9bdb was recorded and was never re-measured, plus build provenance. This row re-baselines the P1.5 line to absorb that drift; future rows compare here. | 22,123,689 | 44,600,290 | 92,166,213 | 187,556,013 | 384,201,567 | 788,099,628 |
| 2026-07-24 | P1.7a slice 2a (witness-table dispatch for constrained type params) OPTIMIZATION PASS — NO compiler change; no superlinearity found, all witness paths LINEAR. CORPUS-BLIND ROW: scale-test's corpus is witness/generic-free so NONE of the new paths fire here — the +6/+bytesΔ below is only the passive record-widening + once-per-compile setup, and the witness complexity was established on a CUSTOM witness corpus (below), not this instrument. DEFAULT-CORPUS FOOTPRINT vs base 65d64dd97 (row 306): allocsΔ FLAT +6 on EVERY rung (per-compile constant, not scaling) = the once-per-compile setup allocations (MaxonToStdLowering.emittedWitnessTables Set + interfaces map ref; ProgramSignatures.genericAliasSites Array; buildDeclaredConformanceMap/checkWhereConstraints short-circuit allocation-free on zero instantiation sites). bytesΔ SCALES x2.0/rung (+64,848..+1,977,920, ~= sourceBytes/rung) = records that scale in COUNT got WIDER with no new per-element alloc: StructLayout gained the whereConstraints pointer (points at sharedEmptyWhereConstraints for every unconstrained struct — no alloc), GenericAliasEntry gained nameLine/nameColumn/filePath. FLAT allocs + scaling bytes = the slice-1 (row 304) pattern: existing records grew, none added. LINEAR, not a bend. WITNESS CORPUS (off-instrument, --metrics per-phase memory, bit-reproducible): N distinct (Type,Digest) conformances = N instantiation sites = N witness tables = N dispatch sites, doubled N=100/200/400/800, work spread over N bounded functions so the KNOWN per-function live-range-splitter quadratic stays out. EVERY phase x1.97-2.02 (LINEAR): lowerMaxonToStd (ensureWitnessTable blob build + finalizeCallArgs dictionary-passing) 14396->28527->56759->113198 x1.98/1.99/1.99; semanticCheck (checkWhereConstraints E3017) 2512->5012->10012->20012 x2.00; parse (witnessDispatch + materializeEscapingBorrowTemp temp-arg push) x1.98-2.00; AND regalloc:splitting/liveness x1.97-2.01 even though EVERY function carries a witnessCall — so the new op adds no superlinear scan. WHY linear: emittedWitnessTables dedup + structTypes/interfaces/declared lookups are open-addressing hash O(1); ensureWitnessTable is one flat O(methods) loop per unique (Type,Iface). materializeEscapingBorrowTemp's per-position pendingTempsContain/removeFromPendingTemps scan is O(K^2) per constructor call but K is STRUCTURALLY CAPPED: the backend refuses >6 params and multi-constrained multi-type-param generics are unsupported (E2015 'top-level ,'), so a single-constraint generic has ONE feed position = O(1)/call — a bounded constant, NOTE not defect (same shape as the pre-existing isRepeatedOwningMove O(K^2) in the same loop). A GIANT-single-main witness corpus does show regalloc:splitting x3.8-3.9 (~quad) — that is the KNOWN/budgeted splitter (recompute-liveness-per-split, ARCHITECTURE.md:1336), not the witness op (per-function it is x2.0). Gates: suite 1458/0 exit 0 no leak. | 22,188,537 | 44,726,850 | 92,416,197 | 188,052,845 | 385,192,095 | 790,077,548 |
| 2026-07-24 | P1.5 dogfood slice 1a (IOCP overlapped-read substrate: spawnReadLine probe + io runtime). ATTRIBUTION CORRECTED — the prior note predicted corpus-neutral, which is WRONG. Slice-1a ISOLATED footprint vs parent 65d64dd97 (the committed P1.5 #64 baseline row): allocsDelta +77 FLAT every rung; bytesDelta GROWS ~linearly (rung0 +68,288, rung5 +1,959,536, ~2x per rung = strictly LINEAR, the curve does NOT bend). CAUSE of the growing bytes: the flat StdOp union got ONE FIELD WIDER. The bootstrap heap-allocates every associated-value union instance at 8 + maxPayloadSlots*8 bytes (MaxonToStandardConversion GetMaxFlatPayloadSlots counts FIELDS per case — each associated value is one 8-byte slot, scalar or pointer); the new osReadOverlapped(resultId,handle,buf,cap,bytesOutPtr,ovl) is the SOLE 6-field case (5 genuine ReadFile args + the BOOL result), one past the prior max of 5 (binOp/cmp/callIndirect), so maxPayload 5->6 widened EVERY StdOp heap instance 48->56 bytes = +8 B per op, paid by ALL programs whether or not they do io. PROVEN: every rung0 phase delta is an exact multiple of 8 — lowerMaxonToStd +52,216 = 8x6,527, foldConstOperands +10,704 = 8x1,338, total +68,288 = 8x8,536. The FLAT +77 allocs (and the ~+4,728 writeExe bytes) are a SEPARATE fixed per-compile cost: kernel32ImportNames iterates IatSlot.allCases, so the 15 new IOCP kernel32 imports are written into EVERY PE unconditionally — a constant, not a scaling term. DECISION: ACCEPTED as inherent LINEAR debt (the flat-StdOp thesis tax: pay max-case width for O(1) dispatch), NOT narrowed. ReadFile is genuinely a 5-argument call, and trimming osReadOverlapped to 5 operands needs a contortion — pack the NON-ADJACENT bytesOut (a per-read slab alloc) and ovl (embedded in the GT struct at 0x60) behind a hard-coded backend layout offset, or hard-code the buffer cap, or fork the op — none clean or correctness-neutral, and the anti-dup reviewer would collapse a fork. +8 B/StdOp is a constant factor, linear in program size, no bent curve (mandate: constant factors are not the target; growth curves are). RE-MEASURE TRIGGER: the first future StdOp case with >=7 associated values pushes maxPayload 6->7 and re-widens every StdOp instance by a further 8 B; until then every <=6-field op rides free. IO PATH superlinearity hunt: NONE — the io runtime graph is usesIocp-gated (never emitted for the arithmetic corpus) and is a fixed-size emission; the runtime proc/handle store scans are cap-64 bounded. Gates: no compiler change (accept-only), so scale is bit-identical to the measured HEAD every rung; suite 1455/0 exit 0 no leak, fragments clean. | 22,191,977 | 44,729,586 | 92,417,525 | 188,051,357 | 385,184,975 | 790,059,164 |
| 2026-07-24 | Retain-escaping-borrow rung (generalize #40/#64 co-ownership to escaping generic-field trivial-struct borrows), RE-MEASURED on the merged tree after rebasing onto P1.5 IOCP slice 1a. MY rung's own footprint is 0 — the retain (`__mm_incref` at a generic constructor feed) and the container's trivial-struct type-param field decref (`genericInstanceFieldIsManaged` now `typeIsManaged` → a `__destruct_<Instance>` cascade) fire ONLY on generic instantiations, and the scale corpus is entirely non-generic, so none of the new paths execute here (corpus-blind, same as slices 1/2a). The +82 allocs / +4,952 bytes FLAT every rung vs the pre-rebase measurement is ENTIRELY IOCP slice 1a's constant, NOT this rung: the whole delta lands in phase:writeExe (+82 allocs / +4,944 bytes flat) = the 15 unconditional kernel32 IOCP imports written into every PE, already attributed in the IOCP row above; the residual ~8 B is StdOp-width provenance. This row re-baselines the retain line onto the current bootstrap (same discipline as the P1.5 #93/#64 provenance re-baselines); future rows compare here. CODEGEN change verified on the witness/generic suites (9 goldens: `__mm_incref` before the constructor feed + the container box drop `__mm_decref`→`__destruct_<Instance>`; the temp no longer needs its scope-local materialization register). The reassign of a co-owned trivial field is REJECTED (E2015) — no leak path shipped. Gates: native 1469/0, wasm 1344/0, -j1==-j12 byte-identical, exit 0, no leak. | 22,193,489 | 44,731,802 | 92,421,149 | 188,057,797 | 385,197,047 | 790,082,500 |
| 2026-07-24 | P1.7a Slice 2b-i (integer Hashable+Equatable primitive conformance + HashValue, x64). Own footprint 0: the synthesized int.hash/int.equals impls and their witness tables fire ONLY on a `where T is Hashable/Equatable` instantiation bound to a primitive int; the scale corpus is entirely non-generic/conformance-free, so none of the new paths execute (corpus-blind, same as slices 1/2a/retain-escaping). installPrimitiveConformanceRuntime short-circuits allocation-free when globalData.pendingRdataRelocs is empty (the whole scale corpus), so the two qualifiedMethodName impl-name allocations never mint here — allocsΔ0/bytesΔ0 vs the retain-escaping baseline. Piece 4's emitCompare interception adds only two O(1) isEnclosingTypeParam tag-checks per ==/!=, allocation-free. Gates: native 1485/0, wasm 1354/0, -j1==-j12 byte-identical, exit 0, no leak. | 22,193,489 | 44,731,802 | 92,421,149 | 188,057,797 | 385,197,047 | 790,082,500 |
| 2026-07-24 | P1.5 dogfood slice 1b (StreamingSubprocess handle-table API: 2 StdOps + 7 builtins + runtime) INCLUDING the io_owns_pipe optimization (d3a31df8a). ATTRIBUTION CORRECTED. The vs-row-310 delta the instrument prints (allocsD +11 FLAT; bytesD +1,520/+2,224/+3,632/+6,448/+12,080/+23,344 = 816 flat + 704*2^n, a strictly-LINEAR term the low-rung x1.46 ratio only dilutes) is NOT slice 1b -- it is BUILD PROVENANCE. Proven by A/B on ONE (current) bootstrap: the slice-1a base source (02f465509, containing NONE of slice 1b) rebuilt here reproduces that delta IDENTICALLY vs row 310 (allocsD +11, bytesD +1,512..+23,336). Row 310's shv2 binary was emitted by an earlier pre-rebase bootstrap; the +11-alloc / 704*2^n-byte term is pre-slice-1b, the same trap as rows 307/308. TRUE ISOLATED slice-1b(+fix+opt) footprint, HEAD vs base rebuilt on the SAME bootstrap: allocsD 0 EVERY rung; bytesD +8 FLAT EVERY rung (22,193,489->22,193,497 ... 790,082,500->790,082,508) = the once-per-compile RuntimeUsage record gaining the usesStreaming bool slot, a per-compile CONSTANT, not superlinear. WHY no per-op growth: the 2 new StdOps are osPipeCreateOutbound(4 fields) + osWriteFile(5 fields), both <=6, so maxPayload stays 6 (slice 1a's osReadOverlapped) -- NO StdOp union widening; slice 1a's >=7-field re-measure trigger is NOT tripped. The 7 bare-name builtins + streaming/IOCP runtime are usesStreaming/usesIocp-gated and corpus-blind (never emitted for the arithmetic ScaleCorpus). OPTIMIZATION is corpus-blind too: the io_owns_pipe marker now rides io_handle@0x80's spare low bit (kernel HANDLEs are 4-aligned) instead of a dedicated GtOffIoOwnsPipe@0xB0 field, reverting GtStructSize 0xE8->0xE0 and the 17-async-golden +8 offset churn (goldens now byte-identical to slice 1a); cross-thread-safe because the completion thread never touches io_handle. That +8/GT was a RUNTIME cost (every async GT), invisible to this compile-time instrument -- removing it does not move these numbers, and HEAD-with-opt still shows only +8-flat vs base. RUNTIME superlinearity hunt (corpus cannot reach the streaming code -- read it): NONE. Handle table O(1) indexed (slot = table + handle*128); spawn free-slot scan is cap-SubpMaxHandles=64 = O(1). Line read re-scans ONLY new bytes (scan resume index re-enters at pre-read bufLen), buffer grows by DOUBLING (geometric O(total)), found-compaction bounded <= one SubpReadChunkBytes per line (remaining <= chunk always, accumulation stops at first newline) = O(1)/line; write buffer doubles. All linear/bounded. This row RE-BASELINES the P1.5 line at the current-bootstrap numbers (22,193,497...); future rows compare here and the provenance offset is absorbed. Gates: suite 1470/0 exit 0 no leak/101; --workers=1 vs 12 PE+IR byte-identical; 1a spawn-read-line 4/4; drop-reader-then-reread PASS; fragments clean. | 22,193,497 | 44,731,810 | 92,421,157 | 188,057,805 | 385,197,055 | 790,082,508 |
| 2026-07-24 | P1.7b Slice A (Set hash-table builtin — core, x64, int scalar keys). Own runtime footprint 0 on this corpus: the `__set_*` install is `usesSet`-gated and the scale corpus is entirely Set-free, so no set runtime is emitted and no set/witness path executes (corpus-blind, like slices 1/2a/2b-i/retain-escaping). allocsΔ0 all rungs. The bytesΔ+8 all rungs is ONE constant: `RuntimeUsage` gained a `usesSet` bool field, enlarging the single per-compile `RuntimeUsage` record (`scanRuntimeUsage` mints exactly one) by one 8-byte field slot — rung-independent, zero extra allocations, the exact same per-field cost `usesArray`/`usesStrClone`/etc. each carry. `fourI64Params` promoted from ArrayRuntime to MmRuntime (behavior-identical relocation, joins its 1/2/3-param siblings); ArrayRuntime's `__arr_slice_managed` signature unchanged. Gates: native 1498/0, wasm 1356/0, -j1==-j12 byte-identical, exit 0, no leak. RE-BASELINED onto the merged tree (rebased over P1.5 dogfood slice 1b + debugger P3c): merged-tree native 1507/0, wasm 1356/0, -j1==-j12 byte-identical, fragments byte-identical; the further +8 bytes/rung vs the pre-rebase numbers is P1.5-1b's `usesStreaming` RuntimeUsage field (a sibling per-compile constant — see the P1.5 row above), allocsΔ0 unchanged. | 22,193,505 | 44,731,818 | 92,421,165 | 188,057,813 | 385,197,063 | 790,082,516 |
| 2026-07-24 | Dead-function elimination rung (reachability prune at the target-neutral backend entry) + its optimization pass. ATTRIBUTION VERIFIED BY FULL A/B ON ONE BOOTSTRAP, and there is no provenance offset this time: the base (b6a6fd4c5, containing none of the rung) rebuilt on this bootstrap reproduces the last logged row EXACTLY, delta 0 on all six rungs, so every number below is the rung. PER-PHASE A/B: exactly TWO columns moved. Every other phase -- lex, signatures, parse, merge, resolveTypes, semanticCheck, lowerMaxonToStd, insertRangeChecks, pruneDeadBlockArgs, elimTrivialBlockArgs, foldConstOperands, sugarGate, isel, regalloc, prologueEpilogue, runtimeAugment, encode, link, writeExe, and all 7 regalloc sub-phases -- is 0 allocs / 0 bytes on EVERY rung. (1) The new phase:deadFunctions column is the whole scaling delta. (2) A FLAT instrumentation term of +2 allocs / +112 bytes per compile, identical at every rung, in three pieces, each pinned: +0/+48 billed to phase:load is the CompilePhase enum going 21->22 arms, widening six phase-count-sized 8-byte-element arrays (CompileTimings.phaseNanos, PhaseMemoryColumns allocs/frees/bytes, and two CompilePhase.allCases materializations) -- CAUSALLY PROVEN, not inferred, by building a throwaway 23rd enum arm with no pass and no probe behind it, which costs exactly +0 allocs/+48 bytes, all in phase:load, on all six rungs; +1/+32 billed INSIDE phase:deadFunctions is the phase's own PhaseProbe record (4 fields x 8B: nanos, allocTotal, allocLive, bytes), allocated after its own opening counter snapshot so it falls inside the window it opens; +1/+32 unattributed is the PhaseDelta that recordPhase builds after the closing counter read. So the pass BODY alone is +318/+136,968 ... +3,935/+6,640,884, isolated by stubbing the body and re-measuring. LINEAR, shown three ways. (a) The body's allocation FIRST DIFFERENCES double: 127, 253, 478, 930, 1829, ratios 1.992/1.889/1.946/1.967 -- the exact signature of c + k*2^n on a doubling ladder, i.e. linear plus a per-compile constant. (b) The body/sugarGate cost ratio DECREASES monotonically, 1.195->1.135->1.099->1.066->1.041->1.024 for allocs and 1.084->1.027 for bytes. sugarGate is the same single IrModule.liveOpIndices walk over every live op, so DFE's marginal cost per unit of program converges on a pass already known linear; a superlinear term would make that ratio GROW, not shrink. This is a stronger statement than the byte ratios alone (x2.03/2.48/2.14/2.14/2.11 vs sugarGate's x2.11/2.50/2.15/2.15/2.11), which are both just the documented Array grownCapacity ~x2.1 linear reading. (c) By construction: indexFunctionsByName is F hash inserts; seedRoots is one O(1) lookup per pendingRdataReloc; markLiveFunction marks BEFORE pushing so no function can be pushed twice and the worklist pops at most F times; markCalleesOf walks liveOpIndices(f) once per LIVE function, so O(total live ops); every callee name resolves through ONE stdlib Map.get (open addressing, cached hashes) instead of a scan; getFunc/funcCount are array indexing. Total O(F + ops + relocs + total callee-name bytes) with no nested scan, no findFirst in a loop, and no unbounded re-push. CORPUS CAVEAT, load-bearing: this is the worst case for cost and the best case for benefit. --log=ir:debug reports dfe: dropped 0 unreachable function(s) of 60 at rung 0 -- the arithmetic corpus contains NO dead code, so every allocation above is pure walk and NONE of it is offset by back-end work the prune removed. The instrument structurally cannot show the win. BENEFIT, measured separately on a heap program the corpus cannot express (Array create/push/get plus print, which pulls in the all-or-nothing installArrayRuntime closure): 15 of 31 functions dropped, and the whole compile falls 41,540->24,356 allocations (-41.4%) and 1,912,683->1,141,518 bytes (-40.3%); regalloc 24,646->11,040 allocs (-55%), isel 5,670->3,363 (-41%), encode 2,825->1,781 (-37%), prologueEpilogue 540->258 (-52%); emitted code 7,354->4,103 bytes (-44%). DFE's own cost on that program is 131 allocations, so it spends 131 to save about 17,300 -- roughly 130x. THE ONE OPTIMIZATION MADE: sweepUnreachedFunctions materialized String.from(func.name) EAGERLY beside its lazy logTrace, spending one String per function DROPPED -- a cost that rises with exactly how well the prune works -- to render a line that is off on every build but a --log=ir:trace one. Moved inside the closure: -15 allocs / -720 bytes on the heap program (-10.3% of the pass's own footprint, one String per drop at 48B), exactly 0 on this corpus because nothing here is dropped, trace output unchanged (15 lines under ir:trace), emitted code byte-identical. NOT DONE, with reasons. No compactOps/compactBlocks after the sweep: it would add an O(ops) walk plus an ops-sized forwarding array to the one column this instrument CAN see, to buy a reduction in peak resident memory, which nothing here measures at all -- and nothing downstream of DFE walks the Std module's flat arrays (verified: every Std consumer goes through liveOpIndices or func.blockRefs), so the orphans are inert, not a correctness risk. No CHUNK-level pruning (a sleep-only program still carries the __gt_trampoline chunk as dead bytes): the hand-assembled chunks are a FIXED set, O(1) in program size, so they cannot bend any curve, and pruning them needs a Target-tier chunk reachability graph -- a SECOND decider beside RuntimeUsage, which is the precise defect this rung removed. MEASURED DEBT; the trigger that would make it worth paying is chunk count becoming a function of program size, which it has never been. Gates: x64 suite 1502/0 exit 0 no leak/101; wasm32-wasi suite 1360/0; --workers=1 vs --workers=12 PE byte-identical on both a 23-file corpus rung and the heap program; specs-shv2/fragments clean. | 22,330,577 | 45,009,510 | 93,108,969 | 189,530,369 | 388,349,083 | 796,723,504 |
| 2026-07-24 | DFE rung — FINAL post-review state (supersedes the row above, which measured the rung before its independent review). The independent review found and fixed TWO reachable defects, and this row is the cost of the correctness fix. (1) BLOCKER: DFE renumbers module.functions, but ValueOriginTable/ParamOriginTable key every row by a function's POSITION in that array, so after the first pruned function every later function resolved its values against a DIFFERENT function's rows — loud half a RegisterPressureDiagnostic COMPILER-BUG panic, silent half worse: E5001 pointing at the WRONG SOURCE LINE. Fixed with ValueOriginTable.remapFunctionIndices + the ParamOriginTable half. (2) HIGH: the moduleHasFunction->RuntimeUsage collapse had been applied to x64 and arm64 but NOT wasm, whose appendLeakGate still asked the module and SILENTLY skipped the leak gate (measured by deleting the __mm_leak_check root: x64 and arm64 panic at link, wasm exits 0 with a 4,332-byte module vs 4,421). Now all three backends emit the gate from usage.usesHeap, so pruning that root is a link failure on EVERY target — a structural guard, which is why no leak-firing spec is needed. THE DELTA vs the previous row is entirely the remap: allocsDelta +6..+11 and bytesDelta +1,032..+24,712, which is the newIndexOfOld array (one F-sized index array per compile, F = function count) — LINEAR in program size, ~0.003% of total compile bytes, and it carries a dropped==0 early-out so the arithmetic corpus (which drops nothing) pays only the array and never a renumbering walk. Everything in the previous row's analysis still holds unchanged: the pass body is linear (allocation first differences double; the body/sugarGate ratio decreases monotonically), only phase:deadFunctions and a flat +2/+112 instrumentation term moved, and the corpus is the worst case for cost and best for benefit (dropped 0 of 60 here, while a heap program drops 15 of 31 for -41% allocations and -44% emitted code, spending 131 allocations to save ~17,300). Gates on this final state: x64 suite 1502/0 exit 0 no leak/101; wasm32-wasi 1360/0; --workers=1 == --workers=12; specs-shv2/fragments clean. | 22,331,609 | 45,011,566 | 93,113,073 | 189,538,569 | 388,363,939 | 796,748,216 |
| 2026-07-24 | P1.7b Slice B — `Set from [ints]` construction (`__set_from` runtime fn + parser construct + sweep pre-intern of `Set with integer`). OWN FOOTPRINT 0, A/B-VERIFIED ON ONE BOOTSTRAP: every new path is `usesSet`-gated (the `__set_from` install rides the existing `installSetRuntime` early-return) or `Set from`-triggered (the parser construct, element-inference, and sweep pre-intern), and the scale corpus is entirely Set-free, so none of them execute here (corpus-blind, like slices 1/2a/2b-i/Slice A). PROVEN by A/B: the base (my four compiler-source edits stashed, rebuilt on the SAME copied bootstrap) produces byte-identical rung totals — allocsΔ0/bytesΔ0 EVERY rung vs this row (rung0 22,331,617 both; rung1 45,011,574 both). The +8 bytes/rung the instrument prints vs the last logged row (the DFE-FINAL row above, 22,331,609…) is NOT this rung: it is BUILD PROVENANCE — the base equally shows it (the copied `bin/maxon.exe` was emitted by a bootstrap one step newer than the DFE row's), a per-compile constant absorbed by re-baselining this line onto the current bootstrap, the same discipline rows 317/320 apply. Future rows compare here. The extra per-token sweep check (`Set`-identifier + `from` lookahead) and the extra `isSetWitnessCallee` `.equals` clause are allocation-free comparisons. Gates: native 1517/0, wasm 1360/0, -j1==-j12 byte-identical, exit 0, no leak. | 22,331,841 | 45,011,798 | 93,113,305 | 189,538,801 | 388,364,171 | 796,748,448 |
| 2026-07-24 | `__Builtins.*` recognition (stdlib-whitelist chain step 2) — the three Clock intrinsics + the reserved-callee panic fix. DELTA IS A FLAT PER-COMPILE CONSTANT: +5 allocs / +232 bytes at EVERY rung of a 32x ladder, 0.001% of rung 0 and 0.00005% of rung 5. FULLY ATTRIBUTED BY MEASUREMENT, not inference: it is the ONE new kernel32 import name (`GetSystemTimeAsFileTime`, for the wall clock's `osReadWallClock`). Proof — a temporary SECOND import (`GetTickCount64`) was added, rebuilt and re-measured, and the delta became exactly +10 allocs / +448 bytes at every rung, i.e. +5 allocations per import exactly, the extra 216 bytes being the shorter name's buffers. Those five are the five `String.toByteArray()`/`.name` calls the PE import-table build makes PER IMPORT: `kernel32ImportNames`' `slot.name`, `sumHintSizes`, `importHintNameEntrySize` x2 (the IAT and ILT passes), and `appendImportHintNameEntry`. Nothing else in the rung costs anything measurable: the `__Builtins.` recognizer is one `bytesStartWith` prefix compare per parsed call and allocates NOTHING (proven by the flatness — rung 5 parses 32x the calls of rung 0 and shows the same +5); the parser's reserved-callee rejection is one more prefix compare on the same path; `RuntimeUsage.usesWallClock` is an inline bool on a single per-compile struct; and `installClockRuntime` early-returns for every program that does not read the calendar (the whole corpus), so the clock runtime function and its `.data` slot cost the arithmetic ladder zero. Gates: x64 1525/0 exit 0 no leak; wasm32-wasi 1366/0 exit 0; --workers=1 == --workers=12 byte-identical; fragments additions-only. | 22,331,841 | 45,011,798 | 93,113,305 | 189,538,801 | 388,364,171 | 796,748,448 |
| 2026-07-24 | P1.7b String Hashable+Equatable conformance: String.hash (djb2) + String.equals (shared __str_eq graph) synthesized post-pipeline, witness-reloc-gated; file/predicate renamed Primitive->Builtin. Corpus is conformance-free so installBuiltinConformanceRuntime short-circuits (pendingRdataRelocs==0) and no per-compile field added ⇒ own footprint allocsΔ0/bytesΔ0. RE-BASELINED onto the merged tree (rebased over the `__Builtins.*` recognition work): the +5 allocs / +224 bytes per rung vs the pre-rebase numbers is that upstream work's per-compile footprint (see the `__Builtins` row directly above), NOT this rung — whose own footprint stays 0. Merged-tree gates: native 1534/0, wasm 1366/0, -j1==-j12 byte-identical, fragments clean. | 22,331,841 | 45,011,798 | 93,113,305 | 189,538,801 | 388,364,171 | 796,748,448 |
| 2026-07-24 | P1.7b Set-with-String (managed-key drop): Set record 40->48B (elemDrop@40), __set_create gains an elemDrop param, 3 drop sites + __set_from move-out. Scale corpus is Set-free, so per-phase memory expected FLAT vs prior row. | 22,331,841 | 45,011,798 | 93,113,305 | 189,538,801 | 388,364,171 | 796,748,448 |
| 2026-07-24 | stdlib-whitelist loader (chain step 3) — Clock prepended to every compile via `Compiler/StdlibWhitelist.maxon`; located by exe-dir walk, registered like a user source, DFE prunes it when unused. DELTA IS A FLAT PER-COMPILE CONSTANT: ~+2.7K allocs / ~+0.5MB bytes at EVERY rung of a 32x ladder (was linear before two fixes). ATTRIBUTION: (1) the constant is Clock's fixed lex+parse+lower (7 fns, pruned by DFE before the backend); (2) the runtime-floor skip's reachability check short-circuits (allocation-free `moduleReferencesWhitelisted` opRefs scan) for every program that names no whitelisted callee — the whole non-Clock corpus — so its O(functions) name->index map + reached array are never built; (3) DFE's `sweepUnreachedFunctions` now skips `valueOrigins.remapFunctionIndices` on TRAILING drops (no survivor's index moved), which is exactly the shape an unused whitelisted module makes since it merges LAST — without it every non-Clock compile paid the +487K..+20.5M program-sized column rebuild the `dropped==0` fast path already documents. Gates: x64 1533/0 exit 0 no leak; wasm32-wasi 1367/0 exit 0; --workers=1==12 byte-identical; fragments additions-only (zero M). | 22,954,116 | 46,284,445 | 95,708,664 | 194,796,152 | 398,866,434 | 817,612,647 |
| 2026-07-24 | stdlib-whitelist loader — OPTIMIZER pass (in-place valueOrigins compaction). REMOVES the whitelist's dominant linear byte term: bytesDelta vs the row above is -486,672 (rung0) .. -20,503,416 (rung5), ~2.5% of TOTAL compile bytes at the top rung, on EVERY runtime-installing compile (not only whitelisted ones). ROOT CAUSE + CORRECTION of the row above: its claim (3) — that DFE's trailing-drop skip spares the corpus — is FALSE for any program that installs an emitted runtime. The scale corpus allocates (structs), so the mm heap runtime is appended AFTER the merged module (Compiler.maxon:266) while DFE runs later still (BackendDispatch:92); Clock therefore drops from the MIDDLE, a survivor (a runtime fn) shifts, keptNumberingShifted=true, and `valueOrigins.remapFunctionIndices` RAN on every rung — its O(rows) rebuild pushing survivor rows into 9 fresh columns (3 ValueOriginTable + 6 ParamOriginTable), 2-5x the survivor count in bytes under the geometric growth policy. That rebuild WAS the linear bytesDelta (measured: phase:deadFunctions was 78%->98% of the whole whitelist byte delta; A/B vs empty whitelist = +487,737..+20,503,457, x2.0/rung). THE FIX: compact the columns IN PLACE (set survivor rows over the leftmost free slot, then resize-down), mirroring IrModule.assignCompactionSlots. write<=read throughout, so no unread slot is clobbered; the result is byte-identical to the rebuild (same survivor rows, same remapped funcIndex) so E5001 span resolution and every golden are unchanged — this is a memory optimization only, NOT semantic, and cannot reintroduce the step-1 renumber bug. After the fix phase:deadFunctions returns to the empty-whitelist baseline (delta +41 bytes flat, the 7 extra Clock slots in newIndexOfOld). RESIDUAL whitelist cost (A/B vs empty whitelist, post-fix): +135K (rung0) .. +360K (rung5) bytes = flat Clock lex+parse+lower (~107K, verified identical rung0 vs rung5) + the O(program) `moduleReferencesWhitelisted` short-circuit scan (isolated by a bypass A/B at +16K..+246K, x2.0/rung — LINEAR, run once per compile, the minimal cost of asking "does this program reference a whitelisted callee"; not nested, no superlinearity). Allocs delta is a near-flat per-compile constant ~+2.6K (Clock's fixed parse). BENEFIT: the whitelist reuses existing stdlib source instead of reimplementing it — a per-compile constant for programs that USE a whitelisted module, and byte-identical goldens (ZERO) for the whole corpus that does not. Gates: x64 1533/0 exit 0 no leak; wasm32-wasi 1367/0 exit 0; --workers=1==12; specs-shv2/fragments zero M; verify-warm-rebuild PASS. | 22,467,444 | 45,155,245 | 93,259,632 | 189,716,144 | 388,603,266 | 797,109,231 |
| 2026-07-24 | stdlib-whitelist loader — REVIEW fix (`moduleReferencesWhitelisted` now SKIPS the whitelisted functions' OWN bodies before scanning). bytesDelta vs the row above is NEGATIVE: -16,048 (rung0) .. -246,156 (rung5), x2.0/rung; allocsDelta -74 (rung0) .. -134 (rung5). CORRECTS the row above's residual attribution. That row credited a "+16K..+246K x2.0/rung `moduleReferencesWhitelisted` short-circuit scan" as the whitelist's last growing term and called it linear-but-minimal. It was neither the scan nor minimal: the scan is ALLOCATION-FREE (a direct `block.opRefs` walk that allocates nothing). The growing term was the O(functions) reachability MAP — the name->index map + `reached` bool array the runtime-floor skip's full walk builds — which built on EVERY compile because the short-circuit NEVER FIRED: the whitelisted set is internally connected (`Clock.elapsedMs` calls `Clock.nowMs`), so scanning the whitelisted bodies THEMSELVES always found an intra-whitelist edge and returned true. The prior "bypass A/B" that isolated +16K..+246K bypassed the WHOLE function, so it measured (free scan + always-built map) and mislabeled the sum "the scan". THE FIX skips `whitelisted.contains(func.name)` bodies before scanning, so the only edge that counts is the first crossing INTO the whitelist from a non-whitelisted (user/runtime) caller — the exact question — which is false for the whole non-Clock corpus, so the map is now never built. That the drop (-16K..-246K) EQUALS the prior bypass A/B in magnitude proves the scan itself costs ~0 bytes and the entire term was the map. NEW residual whitelist cost for the non-Clock corpus (vs empty whitelist): a FLAT per-compile constant — Clock's fixed lex+parse+lower (~107K-119K, DFE-pruned before the backend) — with NO growing term; the allocation-free O(program) scan is the only O(program) work left and allocates nothing. Byte-neutral for goldens (only changes WHICH programs skip the map build, never emitted output). Gates: x64 1533/0 exit 0 no leak; wasm32-wasi 1367/0 exit 0; --workers=1==12 byte-identical; specs-shv2/fragments zero M. RE-BASELINED onto the merged tree (rebased over P1.7b String-Hashable+Equatable + Set-with-String, which added 19 x64 tests): merged-tree x64 1552/0, wasm32-wasi 1367/0, exit 0 no leak, --workers=1==12; the scale numbers here are unchanged by the rebase (same deterministic corpus). | 22,451,396 | 45,139,193 | 93,227,932 | 189,653,428 | 388,478,798 | 796,863,075 |
| 2026-07-25 | P1.7a slice 2b-ii (Comparable + Ordering) OPTIMIZATION PASS — **NO COMPILER CHANGE. Nothing in the rung is superlinear, and the one superlinearity I did find is invisible to this instrument by construction.** Every number on this row is bit-identical to a re-run of the rung commit d2c8bd79f (verified: the rebuilt binary is byte-identical in code size, 6,562,522, and `git status` is clean), so this row is a VERIFICATION, not a delta. (1) THE IMPLEMENTER'S READING REPRODUCED, AND THE LOAD-BEARING HALF PROVEN: +68 allocs / +2,376 bytes vs row 336, bit-identical at rung 0 AND rung 5. What makes that a CONSTANT rather than a leak shaped like one is WHICH axes the ladder doubles underneath it: the corpus grows from 19 files / 12 `type` declarations at rung 0 to 143 files / 260 declarations at rung 5, and the delta does not move by one allocation. That refutes two suspects outright — `createEnumRegistry` is O(1) PER COMPILE and not O(files) (a per-file seed would have grown 7.5x), and `requireTypeNameNotCompilerOwned` -> `isCompilerOwnedTypeName` (three `ByteArray.equals`, run at EVERY `type`/`enum`/typealias declaration) allocates exactly ZERO (a 21.7x growth in declarations moved nothing). The +68 is the two enum registries' first `Map` upsert initializing their 16-slot arrays, as claimed. (2) THE CORPUS IS BLIND TO THIS RUNG, so the +68 measures only the seed. Its manifest declares `generics` NOT GENERATED, and the emitted corpus contains ZERO interfaces, ZERO enums and ZERO generic instantiations — so NONE of the witness/ordering paths run: not `builtinOrderingEnum` per operator, not `orderingTagOf`, not `findWitnessDispatch`, not the witness-reloc-gated `int.compare` install. I therefore measured them on a PURPOSE-BUILT doubling ladder (a `Pair uses T where T is Comparable` with N methods x 4 ordering operators, 64 -> 2,048 operators, against an identical-shape CONTROL on a plain concrete int). BOTH ARE LINEAR: allocs x1.809 x1.893 x1.942 x1.970 x1.984 (converging on x2.00 from below exactly as the real corpus does, the fixed per-compile constant amortizing away), bytes x2.013 at the top rung. The ordering path's OWN marginal cost — ladder minus control — is also linear (x1.844 x1.910 x1.951 x1.974 x1.986), settling at 99.7 allocations per ordering operator. No superlinear term exists in the rung. (3) `builtinOrderingEnum()` COSTS EXACTLY 10 ALLOCATIONS / 376 BYTES PER CALL, and `emitOrderingVerdict` calls it once per ordering operator. Measured directly by an A/B probe compiler that made ONE EXTRA call per operator: +5,120 allocs over 512 operators and +20,480 over 2,048 — 10.000 per call both times, +376 bytes both times (the record, three column arrays, three empty payload arrays, three first-push buffers). LEFT ALONE DELIBERATELY, and the reason is the interesting part: it is 10% of the path's 99.7 allocs/operator, it is a CONSTANT FACTOR not a curve, it is ZERO on the real corpus (shv2's own source contains no ordering operator on a constrained type parameter), and the obvious fix is a TRAP — an eagerly-built per-`Parser` cached layout would pay 10 allocations per FILE whether or not the file compares anything, converting today's FLAT +68 into a GROWING term (+1,430 at rung 5's 143 files). A lazy memo would avoid that and buy a constant factor on a path nothing takes; not worth an option field and a liveness question. `orderingTagOf` -> `indexOfCase` is a 3-element scan and allocation-free; noise, measured and left. (4) THE FIND, and it is TIME-ONLY so this instrument STRUCTURALLY CANNOT SEE IT (the corpus manifest warns of exactly this class in its own words: "a cost that re-walks tokens or chases pointers but allocates little will read FLAT in these columns even when it is quadratic in time"). `Parser.requireInterfaceForParse` (Parser.maxon:16750) resolves a `where`-constraint interface by LINEAR SCAN over `self.artifact.interfaces` after missing the builtin table — so witness dispatch is O(dispatch sites x interfaces declared in the same file). Isolated by an A/B in which the ONLY variable is the hit's INDEX: the same program, same N interfaces, same N dispatch sites, with `Digest` declared LAST vs FIRST. Allocations are IDENTICAL (1,417,523 both — that is why it is invisible here), while parse time diverges — best-of-3 parse ratios x2.00 x2.52 x2.52 (last) against x1.90 x2.05 x2.19 (first), and at N=1024 a +24.15 ms parse delta out of a +27.05 ms whole-compile delta, everything else inside noise. FILED AS MEASURED DEBT, NOT FIXED, because the real corpus bounds it: the maximum interfaces declared in ANY one file across maxon-shv2, stdlib, maxon-selfhosted and specs-shv2 is 17 (and `requireInterfaceForParse` scans only the CURRENT file's artifact), so K <= 17 and the term is linear-in-practice today — the `SplitLiveRanges` K^2 case, max K = 8. RE-MEASURE TRIGGER: a machine-generated source file, or any rung that lands many protocol interfaces in ONE artifact; the cure is an index beside `artifact.interfaces` with a SINGLE writer, which would also remove `builtinInterface`'s construct-on-every-lookup (it builds a fresh `IrInterface` per call — the same constructor-as-lookup shape as `builtinOrderingEnum`). (5) Also verified by reading, since the corpus generates no enums to exercise them: the permanently-seeded `Ordering` entry multiplies inside NO walker. `installUnionDestructors`/`installUnionCloners` select unions with a managed payload (payload-free `Ordering` hits `continue`), `noteDestructorUsage`/`noteClonerUsage` gate on `neededDestructors`/`neededCloners` membership, and `closeDestructorNeeds`'s fixpoint gains exactly one entry per pass — a flat constant, already inside the +68. Gates: x64 1567/0 exit 0 no leak; wasm32-wasi 1375/0 exit 0; `--workers=1` stdout BYTE-IDENTICAL to `--workers=12` (both 1567/0); `specs-shv2/fragments` ZERO modified; whole worktree clean. | 22,453,772 | 45,141,569 | 93,230,308 | 189,655,804 | 388,481,174 | 796,865,451 |
| 2026-07-25 | P1.5 #73 (derived capturing-closure escape invariant) — OPTIMIZER pass. TWO results, one about the rung and one about this instrument's baseline. (1) THE RUNG + THIS OPTIMIZATION HAVE A FOOTPRINT OF EXACTLY ZERO ON THIS CORPUS, A/B-VERIFIED ON ONE BOOTSTRAP AT ONE PATH: the byte-pristine base 13a3fcefe (Parser.maxon + SemanticCheck.maxon checked out at the base commit, rebuilt in THIS worktree) produces 367,213/22,451,270 ... 10,546,959/796,862,949 — identical allocation-for-allocation and byte-for-byte to HEAD-with-this-optimization on ALL SIX rungs. Expected and structural: the new pass is gated by `boolArrayAny(func.valueIsCapturingClosure)`, an allocation-free bit scan, and the arithmetic corpus contains NO closures and no function-typed parameters, so `checkCapturingClosureUses` never scans a body and `buildEscapeSummary` short-circuits. phase:semanticCheck stays exactly linear here (571/1,027/1,939/3,763/7,411/14,707 allocs, x1.99 per rung). THE CORPUS THEREFORE CANNOT SEE THIS RUNG AT ALL — same structural blindness as the DFE rung's "dropped 0 of 60"; the real finding was measured OFF-corpus and is below. (2) THE QUADRATIC THIS PASS FIXED, measured on a closure-dense program the corpus cannot express (one straight-line function holding C capturing closures, each passed down to a callee that only calls it — all ACCEPTED, so the whole body is scanned): the rung as merged scanned the body ONCE PER CLOSURE, and since a function's op count grows WITH its closure count that is a quadratic in the function's own size. phase:semanticCheck BEFORE: 1.1 / 4.3 / 16.4 / 66.7 / 271.8 ms for C = 40/80/160/320/640 — x3.9, x3.8, x4.1, x4.1 per doubling, i.e. ~4x = QUADRATIC (271.8/1.1 = 247x over a 16x input; 16^2 = 256). AFTER: 0.2 / 0.3 / 0.7 / 1.7 / 2.8 ms — ~x2 per doubling = LINEAR, confirmed to a 6th rung at C=1280 (7.2 ms, x2.1). 97x faster at C=640. ATTRIBUTION IS DIRECT, not inferred: commenting out the single `checkCapturingClosureUses` call took the BEFORE ladder to 0.1/0.3/0.5/1.4/2.3 ms, so the pass WAS 99.2% of semanticCheck's time at C=640. THE FIX: `EscapeScanSubject` stops naming the KIND of value and starts naming WHICH values one pass tracks — `everyCapturingClosure` sweeps the whole marked column in a SINGLE body walk (the mark is an O(1) array read, so testing "is this operand tracked" costs the same as comparing one id), making the cost O(ops) whatever the closure density. The ONE band-append `match` that is the rung's whole guarantee is untouched and still the only enumeration of escape sinks. (3) ALLOCATIONS WENT DOWN TOO, and the first version of this fix made them go UP — recorded because it is the trap: a Maxon UNION value is a HEAP OBJECT, so the obvious inversion (have the per-op band match hand back WHICH value escaped, as a `ValueEscapeSite`) costs ONE ALLOCATION PER OP SCANNED — measured at +11 allocations per closure (12,825 vs 5,774 at C=640), turning a time quadratic into a linear allocation regression. A `type` is no better; `ConstValue`'s own comment says so ("a `type` (so, one heap object per operand folded)"). The shipped version keeps every per-op/per-operand leaf answering in a BOOL and constructs the union at most ONCE per scan, recovering WHICH closure with a separate `oneCapturingClosure` query over the ONE op that stopped the sweep — O(closures), on the refusal path only, never on the accepting one. Result vs the rung as merged: phase:semanticCheck allocations 374/734/1,454/2,894/5,774 -> 337/657/1,297/2,577/5,137, i.e. -637 at C=640 = exactly ONE allocation removed per capturing closure (the per-closure union the per-closure scan used to build). (4) ⚠ THE -126 FLAT DELTA THIS RUN PRINTS AGAINST THE ROW ABOVE IS NOT COMPILER WORK — IT IS THE LENGTH OF THE REPO-ROOT PATH, and that is MEASURED, not inferred. The same commit 13a3fcefe, the same bootstrap sources, the same corpus, built and run in a SECOND worktree differing ONLY in its directory name, reports a different flat constant: `...\worktrees\p15-escape-invariant` gives 22,451,270 (delta -126) and `...\worktrees\p15x` gives 22,450,598 (delta -798) — 672 bytes apart at EVERY rung of a 32x ladder, with allocsDelta 0 in both, over a path 16 characters shorter (42 bytes per character). So a `bytesDelta` that is FLAT across the ladder and moves no allocation COUNT is, to first order, a statement about where the tree lives, not about what the compiler did. This subsumes part of what rows 326/330/332 called "build provenance": a row recorded from a worktree carries its worktree's path length, and the next reader in a differently-named directory sees the difference as a phantom constant. READING RULE, so nobody chases it again: judge a change by allocsDelta and by whether a byte delta GROWS with the rung; a flat byte delta with allocsDelta 0 should first be checked against the path the previous row was recorded from. THIS row is recorded from `.claude/worktrees/p15-escape-invariant`. Gates on the final state: x64 1561/0 exit 0 no leak (no 101); wasm32-wasi 1374/0 exit 0; --workers=1 1561/0 and PE byte-identical to --workers=12 (md5 c16ed43d…); specs-shv2/fragments COMPLETELY CLEAN (zero M) after x64, wasm and workers=1 runs — codegen byte-neutral, as a pure-perf refactor must be. | 22,451,270 | 45,139,067 | 93,227,806 | 189,653,302 | 388,478,672 | 796,862,949 |
| 2026-07-25 | P1.5 #73 REBASED ONTO UPSTREAM f44275314 (P1.7a 2b-ii Comparable+Ordering) — the +68 allocs / +3,720 bytes flat at EVERY rung is UPSTREAM's, not this rung's, and the attribution is clean because this row and the one above it were recorded from the SAME worktree, so the path-length constant documented in the row above is held CONTROLLED. Reasoning, by that row's own reading rule: allocsDelta is NOT 0 (+68), so this IS compiler work rather than a path artifact; and it is FLAT across a 32x ladder rather than growing, so it is a fixed setup cost, not an algorithmic term — consistent with P1.7a 2b-ii registering additional builtin conformances (Comparable/Ordering) once per compile. This rung contributes ZERO: measured 0 allocs / 0 bytes on all six rungs against a byte-pristine base, twice, and structurally blind here because `checkCapturingClosureUses` is gated by an allocation-free `boolArrayAny` bit scan and the arithmetic corpus contains no closures and no function-typed parameters. phase:semanticCheck remains exactly linear (571/1,027/1,939/3,763/7,411/14,707 allocs, x1.99 per rung). The rung's real measurement is OFF-corpus and is in the row above (a closure-density quadratic, 271.8 ms -> 2.8 ms at C=640). Gates on the rebased tree: x64 1581/0 exit 0; wasm32-wasi 1387/0 exit 0; --workers=1 == --workers=12 byte-identical stdout, both 1581/0; specs-shv2/fragments COMPLETELY CLEAN (zero M) after x64 and wasm runs, so the derived escape rule stays codegen-neutral against upstream's tree as well as against its own base. | 22,454,990 | 45,142,787 | 93,231,526 | 189,657,022 | 388,482,392 | 796,866,669 |
| 2026-07-25 | P1.5 whitelist-widening #2 — three fixes, ZERO whitelist growth. The stdlib/Sleep.maxon entry was TRIED and then HELD OFF by coordinator ruling (see below), so `whitelistedStdlibRelativePaths()` is byte-identical to base and only the three defects the attempt exposed are here: `__Builtins.<void member>(...)` in STATEMENT position, the `__Builtins.sleep` arm routed through the bare builtin's own `emitSleep`, and `__gt_sleep` joining the E3104 target gate (`isClockSubstrateCallee` -> `calleeNeedsWin32Substrate`, moved beside `targetHasWin32Substrate`) so a `sleep` compiled for wasm/arm64 is refused at the call span instead of panicking in the backend. MEASUREMENT: EXACTLY ZERO — 0 allocs and 0 bytes at all six rungs, bit-for-bit identical to base. ATTRIBUTION IS CONTROLLED, not inferred: this same worktree was rebuilt from the PRISTINE 2f2049e50 compiler sources and measured, giving 367,281 / 696,119 / 1,353,239 / 2,666,683 / 5,293,278 / 10,547,027 allocs and 22,454,948 / 45,142,745 / 93,231,484 / 189,656,980 / 388,482,350 / 796,866,627 bytes — the SAME six pairs this build produces. The -42 bytes/rung against the previous logged row is therefore this worktree's PATH-LENGTH CONSTANT and not compiler work: it is present in a build containing none of this rung's code, which is the check that the A/B method is sound. STRUCTURALLY ZERO, as expected: `builtinsIntrinsicCallsAt` short-circuits on `kindAt(index+1) != dot` before allocating anything and the arithmetic corpus contains no `ident.ident(` statement; `isBuiltinsIntrinsicCallee` is the `bytesStartWith` the code already called, given a name; `emitSleep`'s new `name` parameter is a borrowed ByteArray read only on throw paths; `calleeNeedsWin32Substrate` adds one `equals` inside a predicate only `__`-prefixed callees reach, of which the corpus has none. ⚠ FOR CONTRAST, and as the price list for the next widening: the WITHDRAWN entry measured +677/+701/+678/+701/+677/+677 allocs and +30,550/+37,174/+31,062/+43,318/+30,550/+30,550 bytes — FLAT across the 32x ladder, one more stdlib file load/lex/signature/parse/merge'd on EVERY compile. That is the whitelist's per-module design cost, and it is what a whitelisted module must earn back in delivered capability; Sleep could not, because the parser's bare-name `sleep` builtin claims every call site before any registry is consulted, so no ordinary call could reach the module. Gates: build exit 0 zero warnings; x64 1586/0 exit 0 (base 1581 + 5 new builtins-sleep cases); wasm32-wasi 1389/0 exit 0 (base 1387 + 2 E3104 rejection cases); --workers=1 == --workers=12 byte-identical stdout on BOTH targets; specs-shv2/fragments ADDITIONS ONLY, zero M after x64 and wasm runs. | 22,454,948 | 45,142,745 | 93,231,484 | 189,656,980 | 388,482,350 | 796,866,627 |
| 2026-07-25 | P1.5 whitelist-widening #2 — OPTIMIZER pass. NO COMPILER CHANGE: nothing in the rung is superlinear, and the one constant-factor cost found was PRICED and deliberately NOT shipped. (1) THE ROW ABOVE IS CONFIRMED INDEPENDENTLY, not accepted: this worktree was rebuilt from the pristine 2f2049e50 compiler sources IN THE SAME PATH (holding the path-length constant fixed) and produced the same six pairs — 367,281 / 696,119 / 1,353,239 / 2,666,683 / 5,293,278 / 10,547,027 allocs — so the -42 bytes/rung really is this worktree path and not compiler work. (2) THE LADDER BLIND SPOT WAS CLOSED WITH AN ADVERSARIAL CORPUS. The arithmetic ladder generates no ident.ident( statement at all, so it structurally could not see the new statement-door predicate builtinsIntrinsicCallsAt do anything. Six statement-shape corpora at 2000 AND 4000 statements each were compiled with BOTH binaries and diffed per phase: bare-call f(), method-call b.m(), field-assign b.n=k, reassign x=x+k, bare sleep(0) (driving the new name parameter of emitSleep 2000/4000 times), and Clock.nowMs() (driving isBuiltinsIntrinsicCallee in parseCallNamed AND calleeNeedsWin32Substrate in semanticCheck 2000/4000 times). ALL TWELVE PROGRAMS: 0 allocations and 0 bytes delta in EVERY phase. The predicate or-chain short-circuits, so a bare-call statement pays exactly two kindAt reads and no allocation — measured, not asserted. (3) THE ONE REAL COST, PRICED: builtinsIntrinsicCallsAt answers a question about its FIRST token by building the whole qualified name with qualifiedMethodName — 4 allocations and 201 bytes per __Builtins.<member>(...) STATEMENT, measured by building a variant whose last line is a token compare and diffing a 2000- and 4000-statement corpus (exactly 8,000 and 16,000 allocations). NOT SHIPPED, because the occurrence count today is ZERO: whitelisted stdlib/Clock.maxon uses all three intrinsics in VALUE position (return __Builtins.currentTimeMs()), which never reaches the statement door, and even a whitelist grown to Sleep+Print+PrintError+Subprocess is about 9 such statements = 36 allocations FLAT per compile, against the +677 allocations per module the whitelist itself costs. A constant factor with no occurrences is a change that only looks like an optimization. (4) A READING TO EXPLAIN, not a regression: every phase ALLOCATION column is x2.01 or below, while the BYTES columns read x2.05 to x2.31 (regalloc:criticalEdges highest). Bytes-per-allocation rises about 3 percent per rung because the corpus DOUBLES locals-in-scope per function every rung (the V dimension, named in its own manifest), so the per-function arrays are genuinely bigger each rung: a linear allocation COUNT with a growing allocation SIZE, not a bent curve. Gates re-run on the final tree: build exit 0 with zero warning lines; x64 1586/0 exit 0 no leak; wasm32-wasi 1389/0 exit 0; specs-shv2/fragments 5 A and 0 M with git status empty after both suites; the rebuilt binary is byte-identical to the one measured. | 22,454,948 | 45,142,745 | 93,231,484 | 189,656,980 | 388,482,350 | 796,866,627 |
| 2026-07-25 | CORPUS CHANGED -- ⚠ THE DELTA COLUMN ON THIS ROW IS MEANINGLESS, and nothing above it is comparable: this row is the NEW BASELINE. The ladder had been measuring the compiler roughly as it stood at P1.1a -- in 109,697 lines of rung-5 source it generated ZERO of `match`, `enum`, `String`, `Array with`, `type ... uses`, `interface`, `implements`, `print(`, ` or `, ` xor `, and struct field WRITE. Ten new knobs add them, each doubling per rung and each paired against the knob a future change must be free to move independently of it: enum TYPES vs match SITES (rotating over all three dispatch strategies), String op sites vs managed-struct TYPES (per synthesized __destruct_<T>), trivial-element vs MANAGED-element Array groups, generic BASES (one shared body) vs generic INSTANTIATIONS (one layout descriptor), interface CONFORMANCES vs concrete DISPATCH SITES -- plus a fixed-size coverage file (logical/bitwise or/xor/not, a narrow-range `as` cast over an opaque value, print, a loop that breaks, a struct-typed field written through the loaded box). Corpus rung0 19->30 files, rung5 143->185 files and 109,697->139,381 lines. THE OPTIMIZATION IS SEPARATE FROM THE CORPUS JUMP AND IS A/B-MEASURED ON THIS SAME NEW CORPUS, 8 rungs, one binary rebuild per change. The new heap knobs exposed a REAL SUPERLINEARITY in regalloc:splitting: bytes x192.1 over a x128 ladder (N^1.084) with the per-rung ratio CLIMBING monotonically 1.77 -> 2.61 while allocations stayed linear at x2.03 -- linear allocs plus superlinear bytes is one O(N)-sized allocation made O(N) times. It was TWO scratch buffers, both in SplitLiveRanges.maxon, both sized by the FUNCTION and allocated per SPLIT. (1) materializePeak took a FRESH wordsPerRow live-before row on every peak, and refreshPeak materializes a peak after EVERY split, so it cost O(splits x values) bytes per function. It is now SplitScratch.peakLiveBefore, grown with growFilled (GEOMETRIC: refill/resize go through reserve, which takes the ask at face value, so they would reallocate and copy the whole row at every tick of wordsPerRow and keep the same shape at a smaller constant) and returned to all-zero in place; usesAtPeakOp's sibling row got the same treatment. Its saving QUADRUPLES per rung -- 1,568 / 11,024 / 46,944 / 189,896 / 759,776 / 3,034,208 / 12,125,928 / 48,474,016 bytes -- which is exactly what a splits x values term looks like on a doubling ladder, and it took splitting to x166.7 (N^1.054). (2) markBlocksReachableFromPeak allocated its BFS worklist FRESH per un-memoized (peak block, def block) pair, and that walk pushes a block per IN-EDGE: an O(edges) allocation per walk. It is now ReachCache.worklist, cleared per walk, exactly as LivenessMarkScratch already owns the worklist of the other CFG walk in this pass. Its saving also quadruples -- 832 / 17,040 / 84,272 / 358,000 / 1,462,512 / 5,899,760 / 23,674,864 / 104,249,328 bytes. RESULT: regalloc:splitting bytes 1,896,611->364,331,348 becomes 1,894,211->211,608,004, i.e. x111.7 over a x128 ladder, N^1.084 -> N^0.972, and the per-rung ratio stops climbing and sits FLAT at the byte column's linear reading (2.02 2.05 2.03 2.04 across the top four rungs). That is -41.9% of the phase at rung 7 (-152,723,344 bytes) and -3.2% of the whole compile; allocations were already linear and stay linear (x2.03 -> x2.02). NOTHING ELSE MOVED: at all 8 rungs every other phase and every other regalloc sub-phase is bit-identical (liveness, coloring, rewrite, criticalEdges, blockOrder, ssaDestruction, and all 22 non-regalloc phases), so the entire delta is those two buffers. CODEGEN-NEUTRAL: x64 1581/0 and wasm32-wasi 1387/0, both exit 0, with specs-shv2/fragments COMPLETELY CLEAN (zero M) after BOTH runs -- 1581 programs' Target IR byte-identical. And because a stale liveness row is a MISCOMPILE rather than a crash, the pass's own VerifyIncrementalSplit (which compares every incremental peak row word-for-word against a full rebuild at every split) was switched on: 1581/0 under it, then SABOTAGED by removing the zeroing to prove the check fires -- 11 of 15 register-spill tests then fail, 8 reporting "the peak's live-before row differs from a full sweep's at word 0" and 3 on the per-class live tally, which catches it even with verify OFF. ALSO NEW ON THIS ROW, AND IT IS THE CORPUS WAKING A PASS RATHER THAN A REGRESSION: phase:insertRangeChecks went from INERT to ACTIVE. The old corpus emitted no cast at all and only FULL-range aliases (int(0 to u64.max), int(i64.min to i64.max)), which rangeIsFull refuses to guard, so the pass did nothing; the coverage file now declares typealias ScaleCoverNarrow = int(0 to 4095) and casts to it. Its bytes read x2.41 at rung 5, and that is a REALLOCATION LUMP, NOT A CURVE: over 8 rungs the ratio humps at 2.41 and then FALLS BACK (1.62 1.69 1.70 2.08 2.41 2.26 2.07), where a quadratic's ratio rises monotonically toward 4 and cannot fall, while allocations converge to x1.95 from BELOW (1.28 1.43 1.59 1.74 1.85 1.91 1.95) -- a fixed per-compile part plus a linear one. This is reading rule 1 above: Go-style geometric growth makes a linear phase's BYTE column read above 2.00 while a buffer is still climbing. The guard-SITE count is FIXED at one per rung, so the growing part is not the guard emitter but the per-function scan over module.functions (InsertRangeChecks.maxon:83-86), whose project.rangeChecks.contains(func.name) probe now hashes against a non-empty map. The phase is 0.066% of rung-7 bytes. MEASURED DEBT, TIME-ONLY AND STRUCTURALLY INVISIBLE TO THIS INSTRUMENT: RangeCheckEmitter locates each guard site's block with getBlockByIdIn (IrModule.maxon:361), a linear scan of func.blockRefs, so the pass is O(sites x blocks) in TIME and allocates NOTHING for it -- no rung of this ladder can show it. ⚠ NUMBERS RE-BASELINED ONTO THE MERGED TREE, rebased TWICE over upstream (13 commits: whitelist-widening #2, debugger P4d-1/P4d-GT-STACK, and an x64 refusal of an unencodable imm32). The deltas vs the pre-rebase measurement — rung0 -211 allocs / -2,946 B, rung5 -17,850 allocs / -8,934,514 B — are UPSTREAM's plus a path-length constant (this tree's path is 14 chars shorter than the rung worktree's, worth -546 B at rung 5, which is why the two rebased runs differed by exactly that and their ALLOCS were identical). They are NOT this rung's: regalloc:splitting is BIT-IDENTICAL at 1,894,211 -> 51,024,570 with the per-rung ratio flat at 2.04 across BOTH rebases, and that is the whole of what the optimization claims. | 35,470,621 | 67,192,043 | 134,625,201 | 271,338,819 | 552,930,795 | 1,129,434,047 |
| 2026-07-25 | Stdlib diagnostic attribution + the REAL stdlib loader. FLAT +1,030 allocations / +52,338 bytes per compile, IDENTICAL at rung 0 and rung 5 (a 32x span) — zero growth term. ATTRIBUTED BY PHASE, not guessed: the only phase that moves is phase:load, +1,029 at BOTH ends, which is the loader now ENUMERATING stdlib/ (49 files, Directory.list + a FilePath per entry + a byte-lexicographic sort of the survivors) instead of opening one hardcoded path. That is the price of making the loader real so the whitelist can be deleted rather than rewritten; it is a function of the stdlib directory, never of the user program. The remaining +1 is the StdlibFacts carrier. TWO GROWING TERMS WERE FOUND AND REMOVED BEFORE THIS ROW, both mine and both the same mistake — asking FilePath.isInside a question the loader already knew the answer to. isInside allocates ~4 Strings per call (on Windows canonicalForCompare clones and lower-cases BOTH sides, plus an interpolated needle). (1) Classifying every module FUNCTION as stdlib-or-user by path: +3,633 allocs at rung 0 rising to +48,273 at rung 5, doubling with the corpus. (2) Classifying every SOURCE FILE by path: +60 rising to +1,920, still doubling, because the generated corpus has many files. Fixed by deriving the stdlib function-name set ONCE from the parse artifacts of the files the loader itself recorded (O(stdlib functions), a memo hit) and answering the per-function question with a ByteArray hash against that set. LESSON, worth the row on its own: a path predicate that reads like a pure comparison is four allocations in this stdlib, so it belongs at load time, once per file, never in a per-function or per-op loop. Also measured OFF-CORPUS, because the arithmetic ladder contains no stdlib call at all and is structurally blind to the crossing path: 200 vs 400 Clock.nowMs() crossing sites cost an IDENTICAL delta, so the substrate fixpoint is per-compile and has no per-call-site term; 200 vs 400 non-crossing calls on wasm (the target where the new map lookup actually executes) likewise cost an identical delta, so the per-call-op gate allocates nothing. Gates on the final tree: build exit 0 zero warnings; x64 1586/0; wasm32-wasi 1394/0; --workers=1 byte-identical to --workers=12 on both targets; specs-shv2/fragments zero M and zero A; verify-warm-rebuild PASS on a Clock program. | 22,507,286 | 45,195,083 | 93,283,822 | 189,709,318 | 388,534,688 | 796,918,965 |
| 2026-07-25 | Optimizer pass on the stdlib-loader rung: the SORT IS GONE, and the whitelist filter stopped being quadratic. -180 allocations / -9,810 bytes at EVERY rung, identical across the 32x span — a per-compile constant, as it must be: nothing here is a function of the user program. ATTRIBUTED EXACTLY, by mm-trace scope diff, not guessed: String.clone -46, cow_copy -46, String.sliceBytes -92. All of it is FilePath.canonicalForCompare, called 46 fewer times, because the whitelist's membership test was a LINEAR SCAN calling FilePath.equals (which canonicalizes BOTH sides — on Windows a clone + a lower-case each) and is now a Set-with-FilePath hash lookup (which canonicalizes ONE). (1) THE SORT WAS A MEASURED QUADRATIC, and deleting it is a user ruling, not a perf tweak: "the compiler must not depend on file order to work correctly, so it shouldn't sort — any nondeterminism just needs to be handled". A sort does not remove an order dependence, it makes one reliably HIDDEN. It was also genuinely superlinear, and the price is measured rather than argued: with the whitelist filter deleted (the end state) against synthetic stdlibs of 12/24/48/96/192 modules, the sort alone cost +122 / +436 / +1,638 / +6,344 / +24,970 allocations, i.e. x3.57 x3.76 x3.87 x3.94 per DOUBLING — converging on x4.00, textbook O(n^2). Insertion sort, and each comparison was 4 allocations because pathSortsBefore did toString().toByteArray() on both sides. At this checkout's real 49 stdlib files that is ~1,640 allocations and ~67 KB per compile the end state will now never pay. TODAY it shows as zero in the table above, because with one whitelisted module the sort did no comparisons at all — which is exactly why it had to be priced at the size it was heading for, not the size it is. (2) The filter's remaining O(files x listed) term is now O(files): the list is DESIGNED to grow one entry per widening rung until it names all 49, and a 48-entry scan would have cost ~9,400 allocations a compile against the hash lookup's ~98. (3) THE PREVIOUS ROW'S +1,030 IS CONFIRMED INDEPENDENTLY, measured base 80553e65a vs rung HEAD in ONE worktree so the path-length constant is controlled: +1,030 allocations / +52,368 bytes total, of which +1,024 / +52,148 in phase:load and +6 unattributed. The attribution to the directory enumeration was right. That row's mention of "a byte-lexicographic sort of the survivors" is superseded here. Net rung cost vs base is now +850 / +42,552. (4) THE WHITELIST-DELETED WORLD IS LINEAR, which is the measurement that mattered, since the point of the rung is the world where all of stdlib loads. Built a filter-deleted compiler against synthetic stdlibs of 12->192 modules (8 functions each, chained into ONE 1,536-deep call chain so every stdlib function is reachable AND substrate-marked — maximum stress for the attribution fixpoint). Every phase converges to x2.00 per doubling: total x1.87 x1.93 x1.96 x1.98; parse x2.00; semanticCheck x2.00; regalloc x1.99; phase:load x1.47 -> x1.86 (fixed cost amortizing away). No scope in the mm-trace table exceeds x2.00 either. The fixpoint really did run: cross-compiling the 192-module chain to wasm answers E3104 at prog/main.maxon:2:10 naming 'Syn191.s191_7' and the entry '__gt_now_ns' it reaches 1,536 hops later — transitive attribution at the user's own span, at scale. (5) ORDER-INDEPENDENCE IS NOW MEASURED, NOT ASSUMED. With the stdlib walk reversed at 48 modules the program runs identically and the emitted IR is a PURE PERMUTATION (same multiset of lines). With the USER directory walk reversed, all 1586 x64 tests still pass and wasm stays 1394/0; 14 cross-file fragments move, 13 of them pure block permutations and 1 swapping which of two same-named file-private globals gets the $1 suffix. So the compiler is semantically order-independent; what follows load order is the EMISSION order and that suffix. Left as-is and reported: canonicalizing that belongs at the emission point, not at the loader's input. LANDMINE, measured and NOT fixed here (outside this rung's files): FilePath is the key type of all three query-spine memos (SourceFileMap / TokenCacheMap / ParseCacheMap), and FilePath.hash/equals canonicalize, so every memo probe clones and lower-cases a whole path — ~23 String.clone per source file, measured LINEAR at 2/9/33 sources (161/322/874 clones, ~23 per file at both steps). Linear, so it is debt and not a defect; re-measure if a pass starts probing those maps per FUNCTION or per OP rather than per file. | 22,497,476 | 45,185,273 | 93,274,012 | 189,699,508 | 388,524,878 | 796,909,155 |
| 2026-07-25 | ⚠ STALE-BASELINE ROW — THE HUGE DELTA ABOVE THIS ROW IS THE CORPUS CHANGING, NOT COMPILER WORK. This rung (real stdlib loader + stdlib-call diagnostic attribution) was REBASED onto upstream, which had landed a scale-corpus catch-up (`ScaleCorpus.maxon` changed, 10 commits). The previous two rows were recorded against the OLD corpus; this one is the FIRST against the new one, so the printed delta compares two different inputs and means nothing about the compiler. PROOF IT IS THE CORPUS, not an argument: rung-0 `sourceBytes` went 64,830 -> 97,427 (+50%), and the delta GROWS with the rung (+237,036 / +399,938 / +725,994 allocations at rungs 0/1/2) — a corpus change scales with the ladder, whereas a per-compile constant is FLAT across it, which is precisely how every real per-compile cost in this log has read. THE RUNG'S OWN COST IS UNCHANGED AND STILL VALID: +850 allocations / +42,552 bytes, FLAT at every rung, measured pre-rebase as a controlled A/B (base 80553e65a vs rung HEAD, both built in ONE worktree so the path-length constant is held fixed) and attributed to `phase:load`'s directory enumeration — the price of a REAL loader that walks `stdlib/` instead of a hardcoded list. That A/B remains valid because both of its arms used the same corpus; only the delta-vs-previous-row is invalidated. ⭐ READING RULE, so nobody chases this twice (it has happened before in this log — an earlier rung attributed +196…+3172 to itself when it was an upstream perf commit showing through a stale baseline): AFTER A REBASE THAT CROSSES A `ScaleCorpus.maxon` CHANGE, the delta against the previous row is meaningless by construction; re-establish the baseline with a same-corpus A/B and say so, and judge growth by whether a delta is FLAT across the ladder or scales with it. Gates on the rebased tree: build exit 0 zero warnings; x64 1586/0 exit 0; wasm32-wasi 1394/0 exit 0; specs-shv2/fragments zero M and zero A vs the new base; attribution re-verified at the user's span for the direct and indirect cases. | 35,514,745 | 67,236,167 | 134,669,325 | 271,382,943 | 552,974,919 | 1,129,478,171 |
| 2026-07-25 | REVIEW commit on the stdlib-loader rung (`stdlib-diagnostic-attribution`). +26 / +27 / +27 / +28 / +28 / +29 allocations and +2,774 … +6,358 bytes — a per-compile CONSTANT plus one geometric array, not a growth change; every phase still x1.89-2.16 per doubling. THE TWO HALVES WERE MEASURED SEPARATELY, in this worktree against this corpus: the shared enumerator alone is +9 / +10 / +10 / +11 / +11 / +12 allocations and +1,480 … +5,064 bytes, and the path guard adds +17 allocations and +1,294 bytes FLAT at every one of the six rungs. WHAT MOVED AND WHY: (1) the two recursive `.maxon` directory walks became ONE (`Compiler.collectMaxonSources`, used by the user root and by `stdlib/` alike) — the user walk now COLLECTS its paths into one `MaxonSourcePathArray` before loading them instead of loading as it descends, which is one array of n FilePath slots under geometric growth: O(log n) allocations, O(n) bytes, and it is the whole of the bytes delta (+1,480 at rung 0 doubling to +5,064 at rung 5). The stdlib side now enumerates all 49 files and filters after, instead of filtering inside the walk: +48 array slots, flat. Load ORDER is byte-identical (same depth-first sequence), which the zero-fragment-movement gate confirms. (2) a per-compile constant: one `Directory.currentPath()` + one `FilePath.isInside` asking whether the user's root lies under `stdlib/` — the guard for a REACHABLE wrong answer this review found by probing: `maxon-shv2 build stdlib/Clock.maxon` (relative) answered FIVE `E3006 duplicate definition of function 'Clock.nowMs'` against Clock's own lines, because `FilePath.equals` compares path TEXT and the relative spelling and the loader's absolute one are two identities to the query database, so the file was parsed twice and collided with itself. It now answers `E3001 No 'main' function found`. ⚠ THE ALTERNATIVE WAS MEASURED AND REJECTED, which is why the guard is shaped this way: resolving the project root so every registered path is absolute also fixes it, and it is the deeper repair — but it lengthens every path the three FilePath-keyed query memos clone (~23 String.clone per source file, the debt this rung already filed) and measured +824 / +959 / +1,229 / +1,769 / +2,849 / +5,009 allocations, doubling exactly with the corpus. That is 2.6x the per-file `isInside` cost this same rung's optimizer measured and rejected one commit earlier, so taking it here would have quietly undone that decision. The root-level guard buys the same corrected answer for a constant. ONE SPELLING OF A PATH IS STILL NOT ONE IDENTITY IN THE QUERY DATABASE — reported to the coordinator as its own rung, not fixed here. Gates: build exit 0; x64 1586/0 exit 0; wasm32-wasi 1394/0 exit 0; specs-shv2/fragments zero M zero A; arm64-macos attribution verified by hand (this host cannot run that suite). | 35,517,519 | 67,239,453 | 134,672,611 | 271,387,253 | 552,979,229 | 1,129,484,529 |
| 2026-07-25 | Retire the bare-name `sleep` builtin — `stdlib/Sleep.maxon` becomes the real `sleep`. THE ENTRY COSTS +683 ALLOCATIONS / +31,308 BYTES PER COMPILE, IDENTICAL AT RUNG 0 AND RUNG 5 (a 32x span): zero growth term, one more 7-line stdlib file walked through the front end. MEASURED AS A CONTROLLED A/B IN THIS ONE WORKTREE, not read off the delta column, because the previous row was recorded in a differently-named worktree: arm B is this exact tree with ONLY the `listWhitelistedModule(… "Sleep.maxon")` line removed, rebuilt and measured. That arm reads +0 ALLOCATIONS at every rung against the previous row and +1,312 bytes flat — so retiring the builtin itself (one deleted `equals` in `parseCallNamed`, one dropped `name` parameter on `emitSleep`) is EXACTLY FREE, and the +1,312 is this worktree's PATH-LENGTH CONSTANT. The delta column above therefore reads +683 / +32,620, of which +31,308 is the entry and +1,312 is the path. ATTRIBUTED PER PHASE, and the attribution is IDENTICAL at rung 0 and rung 5 — which is the proof it is a per-compile constant rather than a small growth term: load +34, lex +80, signatures +114, parse +257, merge +24, resolveTypes +4, lowerMaxonToStd +14, foldConstOperands +22, sugarGate +2, deadFunctions +1, isel +6. Nothing in regalloc, encode, link or writeExe moves at all. BYTE-NEUTRALITY IS MEASURED, NOT ASSUMED: `codeBytes` is EQUAL between the two arms at all six rungs (62,457 / 115,686 / 222,294 / 435,554 / 862,198 / 1,716,126), so a program that never sleeps emits the identical image — DFE prunes `@sleep` before isel. The two phases that still move after the prune are exactly the ones that must: `lowerMaxonToStd` runs BEFORE dead-function elimination and lowers the extra Maxon function, and `deadFunctions`+`isel` pay a few allocations because `IrModule.ops` is append-only, so the swept function's ops remain as orphans in an array a later column is sized from. THE 15 COMMITTED GOLDENS THAT MOVED ARE THE OTHER HALF OF THE SAME FACT and are entirely "a call frame appeared": 15 call sites change `x64.callDirect __gt_sleep` to `x64.callDirect sleep` across 14 files, each file gains one identical 8-line `func @sleep` block (prologue, `callDirect __gt_sleep`, epilogue — the millisecond argument needs no `mov`, since param 0 and call-arg 0 are both rcx), and the 15th is `async-sleep.float-arg-rejected`'s pinned error text moving from the builtin's bespoke `E3005 'sleep' requires a integer` to the ordinary `E3009 argument 'milliseconds': cannot implicitly convert 'float' to 'int' … use trunc(x)`, which is BYTE-IDENTICAL to what the C# bootstrap answers for the same program. Nothing else in the diff of all 15 files. Gates on the final tree: build exit 0 with zero warning lines; x64 1587/0 exit 0 (base 1586 + 1 new case); wasm32-wasi 1395/0 exit 0 (base 1394 + 1 new case); `--workers=1` byte-identical to `--workers=12` on BOTH targets; `specs-shv2/fragments` 15 M (each justified above) and 1 A; `grep -rli whitelist maxon-shv2/ --include=*.maxon` returns StdlibLoader.maxon and nothing else; verify-warm-rebuild PASS on a sleeping program; E3104 attribution verified by hand on all four non-x64-windows targets, at the user's own span and naming 'sleep'. | 35,550,139 | 67,276,489 | 134,705,231 | 271,419,874 | 553,011,850 | 1,129,517,150 |
| 2026-07-25 | OPTIMIZER PASS on the sleep-retirement rung — NO CODE CHANGE SHIPPED, because nothing superlinear was found and the numbers are already right; what follows is measurement, and the delta column above is 0 at every rung because this is the same tree the row below measured. THE TWO COST CLAIMS ARE CONFIRMED, by A/B/C arms built and measured in ONE worktree against ONE binary path so the path-length constant cancels instead of being subtracted. (1) RETIRING THE BUILTIN IS EXACTLY FREE, and more exactly than claimed: arm C (base compiler 31724ec31, Clock-only whitelist) vs arm B (this rung's compiler, Clock-only whitelist) reads +0 allocations, +0 BYTES and identical codeBytes at ALL SIX rungs. The row below attributed +1,312 bytes to a "path-length constant"; measured same-tree that term is 0 — it was the cross-worktree comparison, not the change. CORRECTED HERE. (2) THE WHITELIST ENTRY COSTS +683 allocations / +31,308 bytes — CONFIRMED at rungs 0, 2, 3, 4 and 5. ⚠ RUNG 1 READS +699 / +35,724. The row below measured only rungs 0 and 5 and could not see it. It is NOT a growth term: rung 5 is a 32x program and still reads exactly +683. It is a container capacity-doubling boundary that the extra 683 allocations happen to push rung 1 across, worth +16 allocations once, and it does not recur. (3) THE PER-ENTRY TERM IS 16 ALLOCATIONS / 925 BYTES, isolated by listing the SAME module 49 times — the Set dedups, so the LOADED set is byte-identical and ONLY the list construction varies: +752 allocations for 47 extra entries, 16.0 each to three digits, flat at all six rungs. So the filter is LINEAR in its own length with no quadratic term left in it, and the +683 decomposes as 16 of entry plus 667 of Sleep.maxon's own lex+parse+signature+merge+lower. A second entry is therefore near-free beyond the module it names, exactly as designed. (4) ⭐ THE FORWARDING FRAME — the systematic price of this whole retirement plan, since every stdlib leaf that forwards to a `__Builtins.*` intrinsic now costs a real call frame. MEASURED at 86 BYTES OF x64 CODE, ONCE PER RETIRED BUILTIN AND NOT PER CALL SITE: the same 86 whether the program is 233 bytes (6,056 -> 6,142) or 1.4 MB (897,253 -> 897,339). Of those 86, only ~34 are the 7-op frame the goldens show; the other ~52 are the GT STACK GUARD `emitFunctionChunk` emits at the byte level for every non-runtime function of a GT module, which is invisible to the Target-IR goldens. A NON-GT forwarder (the shape `print`/`trunc`/the four math intrinsics will take) costs only the ~34: measured 32 and 37 bytes for two successive forwarders in a trunc program. So retiring the sixteen remaining stand-ins buys a ONE-OFF ~0.5-1.4 KB of code, not a per-call-site cost. Compile-time price on a realistic sleeping program (rung-4 corpus + one `sleep(0)`): base 8,011,309 allocations -> 8,012,648, i.e. +1,339, of which +683 is the whitelist entry every compile pays anyway and ~656 is the extra function crossing the back end. (5) ⭐⭐ SHV2 HAS NO INLINER, so the frame is PERMANENT — confirmed at PLAN.md:493 ("there is no LICM/CSE/inliner") and ARCHITECTURE.md:572, where DCE/CSE/LICM and the inliner are called "scheduled, not present"; the pipeline is resolveTypes -> semanticCheck -> lowerMaxonToStd -> pruneDeadBlockArgs -> elimTrivialBlockArgs -> foldConstOperands and nothing in it hoists or inlines. WHAT AN INLINER WOULD BE WORTH, MEASURED rather than argued: a 200,000,000-iteration loop whose body is one `trunc` runs 282 ms called directly and 536 ms through a one-line forwarder, three runs each on an idle box, steady state — +254 ms / 200M = ~1.27 ns, ~4-5 cycles per call, and a 1.90x slowdown on that loop. IRRELEVANT FOR `sleep` (a scheduler park dwarfs it) and irrelevant for `print`/`runProcess` (a syscall does), but it IS the whole cost for `trunc` and the four math intrinsics, which are single instructions behind the frame. ⇒ Retire the syscall-shaped stand-ins freely; the four math ones and `trunc` are the ones that want either an inliner or — far cheaper, and named here so the choice is deliberate — a TAIL-CALL for a body that is one forwarding call, which turns the whole 7-op frame into a single `jmp`. NEITHER WAS BUILT; this is a price, not a change. (6) ⭐ THE WHITELIST-DELETED WORLD, EXTRAPOLATED FROM MEASUREMENT, NOT FROM ARITHMETIC. Loading stdlib is LINEAR IN THE MODULE COUNT — measured, not assumed: 20 extra probe modules cost 17,505 allocations and 40 cost 34,435, a ratio of x1.967 per DOUBLING of the file count, with per-module cost FALLING slightly (875 -> 861) as the fixed terms amortize. Flat across all six rungs in both arms, so it is a per-compile CONSTANT and not a growth term in the user program either, and codeBytes was equal to the base arm at every rung in every probe arm — DFE prunes all of it. The right unit is the FUNCTION, not the byte: per-function cost measured 489 (Clock, 5 functions), 667 (Sleep, 1) and 696 (probes, 4,800), while per-byte rates disagree by 7x because real stdlib is comment-dense at 925 bytes/function against the probes' 163. Real `stdlib/` is 49 files, 642,703 bytes, 695 function declarations ⇒ THE FULLY-LOADED STDLIB WILL COST ROUGHLY 350,000-500,000 ALLOCATIONS AND ~25 MB ON EVERY COMPILE, as a FLAT FLOOR. That is about 60-80% of an entire rung-0 compile (605,876 allocations / 35.5 MB) and about 2-3% of a rung-5 one. ⚠ IT WILL NEVER APPEAR AS A BEND IN ANY CURVE — it is a floor, not a slope, which is exactly why it is written down now: the instrument this log is fed by cannot flag it. Deleting the filter also RETURNS the 49x16 = ~784 allocations of list construction, which is 0.2% of what it admits. NOT A DEFECT AND NOT DEBT: nothing here is superlinear, every term above was measured linear, and the floor is a design consequence of parsing all of stdlib on every compile that only a lazy/cached stdlib artifact would remove — a rung of its own, not an optimization pass. GATES on this exact tree: build exit 0 zero warnings; x64 spec-test 1587/0 exit 0; wasm32-wasi spec-test 1396/0 exit 0 (the row below says 1395 — CORRECTED, measured twice); no exit 101; `git status` empty after both suites, so codegen is byte-identical. | 35,550,139 | 67,276,489 | 134,705,231 | 271,419,874 | 553,011,850 | 1,129,517,150 |
| 2026-07-25 | P1.7a DIRECT PRIMITIVE DISPATCH — OPTIMIZER PASS. THE RUNG'S PER-CALL-SITE COST IS GONE; WHAT IS LEFT IS A FLAT PER-COMPILE CONSTANT. Numbers on this row are the OPTIMIZED compiler; the rung as merged read +62/+94/+158/+286/+542/+1,054 allocs and the base read 0 (this same tree, same path, one binary rebuild per point — the base rebuild reproduced the CORPUS CHANGED row's allocations EXACTLY at all six rungs, with a flat +546 bytes that is pure path length, which is what makes the byte column below attributable). (1) THE TWO-TERM FIT IS CONFIRMED AND THEN REFUTED AS "STRUCTURAL". The rung's numbers fit `30 + 32*2^n` allocs and `1410 + 512*2^n` bytes EXACTLY (differences 32/64/128/256/512 and 512/1024/2048/4096/8192; 512/32 = 16 bytes per allocation). But the doubling half was NOT the feature's own dispatch gate in any load-bearing sense: it was ONE 16-BYTE HEAP BOX PER METHOD-CALL SITE, and the sites paying it were STRUCT method calls in a corpus that contains ZERO direct dispatches. Localized: the whole doubling term sits in phase:parse (+32/+64/+128/+256/+512/+1,024 allocs, +512..+16,384 bytes) and in NO other phase at any rung. Attributed by a knob experiment rather than by arithmetic: 64 extra `p.bump(acc)` sites cost +64 parse allocs / +1,024 parse bytes, while 64 extra array-method (`arr.count()`), String-method (`s.byteLength()`), struct-FIELD-read (`p.x`), ranged-alias-argument and plain-call sites each cost EXACTLY 0 — so the term moves only with the sites that fall past the String/Array/Set arms into the new classifier, and nothing else. CAUSE: `BuiltinConformerLookup` was a union carrying the conformer's NAME (`conformer(typeName ByteArray)`), and a union with an associated value is heap-boxed, so classifying a receiver allocated a box even to answer "no conformer at all". CURE: the classifier answers with a PAYLOAD-FREE enum (`BuiltinConformer`, a scalar), and the name is fetched by `builtinConformerName` only once a dispatch is certain to be emitted — an EXHAUSTIVE match, so a conformer added to the tag map fails to compile until it is given a name, which is a stronger check than the union shape had. `Self`-argument agreement compares CONFORMERS instead of names for the same reason. RESULT: every phase's delta is now 0 at every rung, and the rung's whole footprint is the flat term below. (2) THE INSTALL IS O(ops + 5*F) ONCE PER COMPILE AND ALLOCATION-FLAT — AUDITED AND MEASURED. `referencedBuiltinImpls` walks pendingRdataRelocs once and the flat `module.ops` array once (<=5 length-failing byte compares per call/tryCall/funcAddr op); `undefinedImplNames` walks `module.functions` once per candidate, 5 candidates, fixed. Not O(ops x impls), not per function. The proof it has no growing term is that the residual +30 allocs / +1,410 bytes is IDENTICAL at rung 0 and rung 5 across a 32x ladder — so `module.getFunc(i)` and `module.ops.get(i)` allocate nothing, and the walk's cost is time only. It IS newly paid by EVERY compile, including the conformance-free ones (it replaced an allocation-free `pendingRdataRelocs.count() == 0` early-out), and it is kept deliberately: it is what makes REFERENCED <=> INSTALLED one predicate instead of two, and 30 allocations is 0.005% of the smallest rung and 0.0002% of the largest. (3) `isCompilerInternalCallee`'s new second clause IS allocation-free as intended: phase:semanticCheck and phase:lowerMaxonToStd — which ask it of EVERY callee in the program, via validateCall and lowerCall — read +0 allocs / +0 bytes at every rung, before and after this pass. (4) MEASURED DEBT, STRUCTURALLY INVISIBLE TO THIS INSTRUMENT: the direct-dispatch path itself never runs on this ladder (the corpus generates no `i.hash()`/`i.equals()`/`i.compare()`), so measured separately on generated programs of 64/128/256/512 dispatch sites. It is LINEAR — parse allocations grow x1.971 x1.981 x1.988 per doubling — but carries a per-site premium of ~12 allocations for `.hash()` and ~20 for `.equals()` over an ordinary call at the same site (29.41 vs 17.41, and 104.54 vs 84.59, allocations per site at N=256, against structurally matched controls). CAUSE: `builtinConformerMethod` resolves the method by asking `requireBuiltinInterface` for each candidate protocol, and that RE-SYNTHESIZES a whole `IrInterface` (its method array, both param arrays, the sig) on every call — `hash` builds 1 (Hashable hits first), `equals` builds 2 (Hashable is built and missed, then Equatable), `compare` 3, which is exactly the ~8-allocation step between hash and equals. Pre-existing pattern, not introduced here: the comparison-operator lowering calls `requireBuiltinInterface` per site too. NOT FIXED because it is measured linear and a fix means memoizing the synthesized protocols, whose blast radius is every `builtinInterface` caller. RE-MEASURE TRIGGER: a corpus knob that generates direct dispatches (which would make it visible here), or a fourth builtin-conformable protocol (every added protocol adds one interface build to every MISS). GATES: x64 1594/0 and wasm32-wasi 1398/0, both exit 0 (no 101), with specs-shv2/fragments COMPLETELY CLEAN (zero M) after BOTH runs — 1594 programs' Target IR byte-identical, which is the proof this pass is behaviour-neutral. The three diagnostics the refactor re-routes were probed directly and all name the conformer correctly: E3005 "'equals' requires a int, but its argument is String", E2015 "'int' has no method named 'toString'", E3036 "'compare' takes exactly 1 argument, but 0 were given". | 35,472,577 | 67,193,999 | 134,627,157 | 271,340,775 | 552,932,751 | 1,129,436,003 |
| 2026-07-25 | DUPLICATE TYPEALIAS IS A DIAGNOSTIC + FILE-SCOPED ALIAS RESOLUTION. `project.typeAliases` and `ProgramSignatures.typeAliases` were ONE whole-program map keyed by the bare name, so the LAST file merged won and its range silently replaced every other file's: `600 as Limit` compiled clean in a file whose own `Limit` is `int(0 to 500)` because another file declared a wider one, and a user's `typealias Milliseconds = int(0 to 100)` was overridden by stdlib/Sleep.maxon's. Both are now correct (E3005, matching the bootstrap oracle byte for byte), and a SAME-FILE duplicate is E3061 at the redeclaration's name token. COST, ATTRIBUTED AND LINEAR: +1,911/+2,044/+2,228/+2,645/+3,445/+5,062 allocs, which fits `flat + 5.9 x (typealias DECLARATIONS)` — the corpus declares 12 + 17*2^n aliases (29/46/80/148/284/556, an exact fit), and the rung-over-rung delta divided by the alias-count delta is 5.94/5.88/6.13 at the three largest rungs. The 5.9 is `fileScopedDeclKey` building one key per declaration (declFilePath.toString + String.from(name) + the interpolation + toByteArray ~= 4 objects, plus the second map entry), charged twice per compile because both the pre-parse signature sweep and the merge record every declaration. It is bounded by DECLARATIONS, never by USES. THE LOOKUP PATH ALLOCATES NOTHING, and that is measured rather than asserted: `RangedAliasRegistry.lookup` builds a key only when `multiplyDeclared` holds the name, and NO alias name in the generated corpus is declared in two files, so that set stays empty at every rung and the scoped probe never runs. Without that gate the key would be built once per named type reference — the parser's hot path — and the term would track USES instead of declarations. Share of total: 0.32% at rung 0, 0.032% at rung 5. Bytes +92,878..+224,483, only 2.4x across a 32x ladder, so dominated by the flat term. ONE AVAILABLE FOLLOW-UP, NOT TAKEN HERE: `fileScopedDeclKey` allocates ~4 objects where 2 would do (append bytes rather than interpolate Strings); it is the SHARED builder that top-level let/var resolution also uses (`declFor`), so making it cheaper improves both and belongs in its own measured pass rather than riding a diagnostics rung. | 35,565,455 | 67,299,385 | 134,734,459 | 271,470,278 | 553,087,634 | 1,129,660,486 |
| 2026-07-25 | OPTIMIZER PASS on the file-scoped-typealias rung. THE LOOKUP PATH'S COST WAS REAL AND THIS CORPUS IS STRUCTURALLY BLIND TO IT; it is now GONE, and the rung is allocation-NEGATIVE against its own base from rung 2 up. Deltas against the row below are the change itself (same worktree, same corpus): -324/-454/-682/-1,154/-2,066/-3,906 allocations, -14.6K to -142.2K bytes. (1) THE HEADLINE. The row below prices the lookup path at zero because `RangedAliasRegistry.lookup` built its composite key only when `multiplyDeclared` held the name, and NO alias name in the generated corpus is declared in two files — verified here by grepping `--emit-corpus`: all 169 ranged declarations at rung 5 have distinct names, so that set is empty at every rung and the branch never runs. THE GATE IS EXACTLY WHAT MAKES THE CORPUS BLIND. `stdlib/` is not so kind: SEVEN files declare `Byte` (Character, Console, File, helpers/string/views, Json, String, Subprocess), three declare `ByteCount`, nine names in all are multiply-declared, and `Byte` is one of the most-used aliases there is — so the cost arrives in full the day the whitelist is deleted. MEASURED OFF-CORPUS on a program where a TWO-FILE alias is USED N times with the use count scaled and everything else held identical (same file count, same declaration count, same source; the control's second file declares a DIFFERENT name, so the only difference is whether the name is contested, and both arms emit the same code): the rung cost 24 ALLOCATIONS AND 2,224 BYTES PER USE — +2,446 / +4,846 / +9,646 / +19,246 allocations at N = 100/200/400/800, exactly `46 + 24N`. Linear in uses, never superlinear, and invisible here. It is now FLAT: +33 allocations / +2,782 bytes at ALL FOUR of those N, identical to the byte — the per-use term is GONE, not reduced. CAUSE: `fileScopedDeclKey` builds a `path SEP name` composite per probe (String.from + interpolation + toByteArray, about 4 objects, plus O(path) copying twice). CURE: a two-level map, name -> (declaring-file path -> alias), populated ONLY for names more than one FILE declares, with the invariant "every declaration of the name or none". The probe that decides a name is contested IS the probe holding the answer, so there is no key to build and no cost gate to keep honest, and an uncontested name costs one short-name hash and falls through to the bare answer — what the whole-program map cost before file scoping existed. The inner map is keyed by the path STRING deliberately: `FilePath.hash` case-folds on Windows (canonicalForCompare clones and lower-cases), so `Map with (FilePath, V)` would allocate per probe, which is the cost being removed. (2) THE `5.9 x DECLARATIONS` FIT IS CORRECTED — right shape, wrong denominator, real constant 3.4x bigger. `12 + 17*2^n` (29/46/80/148/284/556) counts ALL typealiases; the generic-instantiation ones (`typealias X = Base with Arg`, 15 at rung 0 rising to 387 at rung 5) go to `genericAliases` and never reach this registry. It sees `9 + 5*2^n` RANGED declarations — 14/19/29/49/89/169 — plus 5 from the whitelisted `stdlib/Clock.maxon`. Controlled A/B in ONE worktree (base dfe427944 vs rung HEAD 45935eaaf, both binaries built here so the path-length constant cancels): +352/+468/+668/+1,084/+1,884/+3,500 allocations, slope 20.2 PER DECLARATION with a zero intercept, not 5.9. Now about 2.0 per declaration (+20/+25/+35/+55/+95/+175 in phase:signatures and the same again in phase:merge, where each was +96 to +740), because the FIRST declaration of a name — in a program where nothing is redeclared, every one of them — now costs one probe and one insert and builds nothing at all. (3) "BOUNDED BY DECLARATIONS, NEVER BY USES" WAS NOT TRUE, and this instrument's own phase table showed it: phase:resolveTypes gained `36 + 36 x 2^n` allocations (+72/+108/+180/+324/+612/+1,188), a term doubling with the PROGRAM. CAUSE, confirmed by removing it rather than by argument: `resolveNamedType` asks the registry once per named TYPE REFERENCE and the answer came back as `DeclaredAlias`, a union with an associated value, which is HEAP-BOXED — the identical defect the P1.7a pass fixed one rung earlier for `BuiltinConformerLookup`. CURE, a narrowing rather than a workaround: all FOUR bare-name readers (`resolveNamedType`, `canonicalTypeName`, `classifyUnionPayload`, `canonicalizeRangedAlias`) want `underlying`, the int/float keyword, and none of them may read the RANGE — a reader with no file would be reading a stranger's bounds. Each said so in a comment; the door now hands back a `MaxonType` so none of them CAN. phase:resolveTypes is back to +0 at every rung and phase:parse goes NEGATIVE (`-12 - 24 x 2^n`, -780 at rung 5) because `classifyUnionPayload` was already paying that box BEFORE this rung. Net rung cost vs base is now `42 - 14 x 2^n` allocations (+28 at rung 0, -406 at rung 5) and `1,904 + 16 x 2^n` bytes — essentially flat across a 32x ladder where it was +19,582 rising to +181,082. (4) MEASURED AND DELIBERATELY NOT CHANGED. `declFor` (top-level let/var resolution) builds a `fileScopedDeclKey` on EVERY probe, unconditionally, before it knows the name is even file-private — the same 4 allocations, pre-existing. Left alone because `declIndex` is ONE flat map shared with `topLevelStorageKey`'s writes, so giving it this treatment changes that map's shape and its writer, not alias resolution; re-measure if a pass starts asking it per OP rather than per identifier. `fileScopedDeclKey` is no longer exported — the alias registry was its only cross-file caller. Also corrected: the bare probe is NOT off the hot path as the row below implies; `resolveNamedType` is its hottest caller, at once per named type reference. GATES on this exact tree: build exit 0 with zero warning lines; x64 spec-test 1602/0 exit 0; wasm32-wasi spec-test 1411/0 exit 0; no exit 101; specs-shv2/fragments ZERO M after BOTH suites, so 1,602 programs' Target IR is byte-identical and this pass is behaviour-neutral; codeBytes equal to the BASE compiler's at rungs 0/3/5 (62,457 / 435,554 / 1,716,126). | 35,550,817 | 67,277,183 | 134,705,957 | 271,420,664 | 553,012,768 | 1,129,518,324 |
| 2026-07-25 | REVIEW PASS on the file-scoped-typealias rung — E3105 added, and it costs NOTHING: every rung matches the optimizer's row above EXACTLY (delta 0 allocations, 0 bytes, at all six), and codeBytes is unchanged (62,457 / 115,686 / 222,294 / 435,554 / 862,198 / 1,716,126). That is the expected reading and it is worth having: the new check is one `maxonTypeTag` comparison on a name some file has ALREADY declared — never on a first declaration, which is every declaration in nearly every program and in every rung of this corpus, where all 169 ranged names at rung 5 are distinct. So the corpus is structurally blind to the check for the same reason it was blind to the lookup cost, and a delta of exactly 0 is what a correctly-gated check looks like here rather than evidence it does nothing. WHAT IT FIXES IS A WRONG ANSWER, NOT A COST. The rung scoped the PARSER's alias lookup by declaring file while `resolveNamedType` / `canonicalTypeName` / `classifyUnionPayload` / `canonicalizeRangedAlias` kept resolving the bare name last-wins. Two deciders, and nothing made them agree: on base dfe427944 both were bare and agreed, so a user program declaring `Measure` as `int` in one file and `float` in another COMPILED; on the rung the parser said `int` inside the int file while type resolution said `float`, and the disagreement reached the backends. MEASURED, not argued: x64 died with `panic at X64Backend.maxon:1367: xmm0 is in the xmm register file where the gpr file is required` (an ICE, no diagnostic); wasm emitted a component its own validator rejected with `type mismatch: expected i32, found f64`; and a struct field typed by the alias compiled silently to the wrong width where base main had rejected the same program with a clean E3009. The cure makes the bare door's contract STRUCTURAL rather than a bet about today's corpus — `record` refuses a cross-file pair whose underlying primitives differ, so every declaration of a name shares one primitive in any program that compiles and the bare answer IS the answer a scoped lookup would give. Two files over two RANGES stays legal and is untouched (seven stdlib files declare `Byte`; both ported `duplicate-typealias` tests and the new same-underlying guard pass). GATES on this exact tree after the fix: build exit 0 zero warnings, 138 codes; x64 spec-test 1604/0 exit 0; wasm32-wasi spec-test 1413/0 exit 0 (1602/1411 plus the two new tests); no exit 101; specs-shv2/fragments ZERO M after BOTH suites. | 35,550,817 | 67,277,183 | 134,705,957 | 271,420,664 | 553,012,768 | 1,129,518,324 |
| 2026-07-25 | P1.7a 2b-iii (float + bool conformance) — OPTIMIZER PASS. (1) THE IMPLEMENTER'S MEASUREMENT IS CONFIRMED EXACTLY, and its caveat about the log was right: the `previous` row this instrument compares against is the DIRECT-DISPATCH OPTIMIZER row (27de41e16), TWO commits before this rung's base (dfe427944), so the printed delta of +1,571..+1,574 belongs to the two commits in between and not to this rung. Rebuilt the base binary in THIS worktree at THIS path (path length is a per-compile constant — the previous optimizer priced 546 bytes of it) and A/B'd: +12 allocations / +592 bytes, IDENTICAL AT ALL SIX RUNGS across the 32x ladder. EVERY per-phase delta is 0 at every rung, which is not a rounding artefact: `installBuiltinConformanceRuntime` is called from Compiler.maxon:328, in the UNPHASED tail after the pipeline, so its cost lands in the total and in no phase bucket. (2) ATTRIBUTED BY A KNOB, NOT BY ARITHMETIC. Replacing exactly the three new `qualifiedMethodName` joins with an already-built ByteArray — keeping the 8-entry candidate array, the three extra `referenced.contains` tests, the three new builders and everything else the rung added — returns the numbers to base EXACTLY: +0 allocations / +0 bytes at both rungs measured. So the whole +12/+592 is three name joins at 4 allocations and ~197 bytes each, and three things the rung also added are measured FREE: the candidate list widening 5 -> 8 (walked per pending rdata reloc and per module op) costs ZERO allocations; `isBuiltinConformanceImplName` widening from one qualifier test to three (asked of EVERY callee by validateCall and lowerCall) costs ZERO; and `MmRuntime.emitCmp`'s new defaulted `operandType StdType = StdType.i64` costs ZERO at all 79 call sites that take the default (84 emitCmp sites, 5 explicit). That last one was cross-checked against the BOOTSTRAP's lowering, which is what compiles these sites: a defaulted enum argument is one constant into the argument register — the defaulted call emits `xor edx, edx` where the explicit call emits `mov rdx, 1` — no box, and the enum's struct RAW VALUE is never materialized. (3) ONE CHANGE SHIPPED, worth +2 allocations per INSTALLED impl. `buildBuiltinEquals`/`buildOrderingCompare`/`buildFloatCompare` each built the parameter-type array TWICE — once for `createFromStd`'s declared signature, once for `emitTypedParams` — where the base built it once and used an allocation-free `emitParams(count: 2)`. `emitTypedParams` now reads `func.paramTypes` (which `createFromStd` stores BY REFERENCE), so the `param` ops emitted ARE the types declared: one fact instead of two derivations free to disagree the day an operand type maps to a param shape conditionally. MEASURED off-corpus, because the scale corpus installs no impl at all: a program that installs `int.equals` or `int.compare` read +14 against base and now reads +12, the same flat constant every other program pays; `int.hash`, whose builder was untouched, read +12 throughout. On this ladder the change is a no-op, which is why the row's numbers are the rung's unchanged +12/+592. (4) RUNG 1'S FILED DEBT RE-MEASURED OFF-CORPUS — IT DID NOT GROW, AND THE INT HALF IS BYTE-IDENTICAL. `builtinConformerMethod` still re-synthesizes a whole `IrInterface` per candidate protocol per dispatch site. Generated ladders of 64/128/256/512 dispatch sites against structurally matched controls (a struct instance-method call `box.compareTo(b)` — receiver plus one positional argument, the same parseMethodCall shape, falling past the conformer classifier into the struct arm), marginal phase:parse allocations per site taken as the slope between N=256 and N=512: int.hash +7.00, int.equals +16.00, int.compare +23.00, float.equals +7.00, float.compare +14.00, bool.compare +3.00. LINEAR in every arm (phase:parse x1.90-x1.96 per doubling). The premium tracks the number of interface BUILDS exactly as the debt predicts, and the RAGGED conformance rows make the NEW conformers CHEAPER than int rather than dearer: a protocol `isIntrinsicBuiltinConformance` withholds is `continue`d before `requireBuiltinInterface` is reached, so float.compare builds 2 (Equatable built and missed, Comparable hit) where int.compare builds 3, and bool.compare builds 1. A/B against the base binary on the same ladder: phase:parse is IDENTICAL TO THE BYTE at every N for every int variant (+0), so the two `typeName.equals` arms added to `isIntrinsicBuiltinConformance` cost nothing on the dispatch path. Re-measure trigger unchanged: a corpus knob that generates direct dispatches, or a fourth builtin-conformable protocol. (5) A REAL QUADRATIC FOUND — `regalloc:splitting` GOES QUADRATIC WHENEVER A FLOAT IS LIVE ACROSS CALLS. It is PRE-EXISTING and NOT this rung's, and it is NOT the known liveness-recompute debt. Same generated program, only the declared type of two locals changed: with FLOAT locals `regalloc:splitting` allocations run 1,561,997 -> 6,628,499 -> 28,064,380 at N=128/256/512 (x4.24, x4.23) while lex/parse/merge/isel of that same compile all sit at x1.9; with INT locals the same bucket is 1,762 -> 2,660 -> 4,454 (x1.51, x1.67). At N=512 that is 6,300x. `regalloc:liveness` beside it reads x1.86/x1.92 — LINEAR — so the whole-function-rebuild-per-split debt is not what this is. Emitted CODE grows x1.84/x1.91, also linear, so the SPLIT COUNT is linear and it is the PER-SPLIT repair that is proportional to the whole function: the victim is a value live across the entire body, so its dirty region IS the body, and Theta(N) splits each dirtying Theta(N) is the curve. The trigger is structural and already named in the source: shv2's x64 pool has NO callee-saved XMM (X64Backend.maxon:653 — Win64 preserves all 128 bits of xmm6-15 and `movsd` cannot save that, "a Wave 2 problem rather than a free win"), so a float live across a call is forbidden EVERY XMM (SplitLiveRanges' PressurePeak.witnessClass documents exactly this empty-witness case) and every call forces a store/reload bracket. PROVEN PRE-EXISTING by A/B: the base binary reproduces the identical curve — the float control's total is 28,598,570 on base against 28,598,582 here at N=512, the same flat +12 — and that control program contains NO builtin-conformance dispatch of any kind. NOT FIXED HERE, for a gate reason rather than an effort one: the structural cure is making xmm6-15 callee-saved, which CHANGES CODEGEN for every float program, and this pass's non-negotiable gate is byte-identical Target IR (fragments zero M). That makes it a rung, not an optimizer edit, and it sits in files this pass does not own. Handed to the coordinator as a DEFECT TO RUN DOWN, deliberately not filed as measured-linear debt, because it is measured QUADRATIC. This instrument structurally cannot see it: `regalloc:splitting` on the corpus is x1.71/1.85/1.93/1.98/2.00, linear, because the corpus generates no float held live across a run of calls — the same blind spot that hides the rung-1 dispatch debt, in a different phase. GATES: x64 1606/0 at --workers=12 and again at --workers=1, wasm32-wasi 1415/0, all exit 0 (no 101), with specs-shv2/fragments COMPLETELY CLEAN (zero M) after BOTH suites — 1606 programs' Target IR byte-identical, which is the proof the shipped change is behaviour-neutral. The DERIVED reject list was probed on all three conformers and each is right where the deleted fixed string would have lied: float is told it supplies `equals`, `compare` (no hash), bool `compare` alone, int `hash`, `equals`, `compare`. | 35,545,001 | 67,271,351 | 134,700,093 | 271,414,736 | 553,006,712 | 1,129,512,012 |
| 2026-07-25 | UNDECLARED-CAST rung — `5 as CompletelyMadeUpNameXyz` compiled clean and returned 5, and so did `5 as Point` and `5 as Scor`. `parseTypeReference` answers a few type spellings syntactically and hands back a bare `MaxonType.named` for everything else — a declared `type` named from outside its own body, a declared enum/union, a ranged typealias, a qualified inner alias, and a name nothing declares at all — and `maxonTypeTag` reads all five as ONE tag, so `applyCast`'s legality gate was asking about a NAME and its answer for a name denoting nothing matched its answer for a ranged int alias: convert nothing, flow the value on. The cure is NOT a second cascade in the parser: `TypeResolution`'s one list of sources became `denotedNamedType`, raising a payload-free `UnknownTypeName` instead of reporting, so each of its THREE readers picks the anchor — `resolveNamedType` reports E3011 unpositioned as before, `Parser.castTargetTag` reports it at the `as`, and `Parser.requireConstCastTargetKind` covers the SECOND cast site (a top-level `let X = 5 as T`, folded by the const evaluator, which never reaches the expression path). Falling out of the same lookup: `5 as Point` is now E3009 "Cannot cast from int to struct", the message `Self` and the type's own name already gave, and the one `applyCast`'s header claimed for `n as SomeStructAlias` while being false for the spelling users write. THE NUMBERS: allocations delta EXACTLY 0 at all six rungs, and `codeBytes` unchanged at all six (62,457 / 115,686 / 222,294 / 435,554 / 862,198 / 1,716,126) — codegen is byte-neutral, which a diagnostic-only rung owes. Bytes moved +408, FLAT: identical at rung 0 and rung 5 across a 32x input span, with zero additional allocations, so nothing algorithmic changed and there is no growing term. ATTRIBUTION IS AN INFERENCE CONSISTENT WITH THAT FLATNESS, NOT A MEASURED FACT: `resolveTypes` now fetches `queryProgramSignatures` ONCE for the whole pass, where it was previously fetched inside `resolveNamedType` on the rare dotted-name arm only — this corpus writes no qualified names, so the fetch count went 0 -> 1 per compile, which is exactly the shape of a flat constant. On a program that uses qualified aliases it is strictly FEWER fetches, not more. A SECOND CHANGE THE CORPUS IS STRUCTURALLY BLIND TO: the cascade now reads the whole-program signature index rather than the `Project` registries, which is the only way the parser can ask it at all (the pure-parser invariant). Both hold the same declarations through the same doors — `upsertDeclaredEnum` is the single write of BOTH enum registries and `createEnumRegistry` seeds both — and the parser has ALREADY been deciding struct-ness from the index for every `named` parameter and return type (`resolveNamedStruct`), so this makes resolution agree with the parser rather than merely with itself. MEASURED, not argued, on the corpus this instrument compiles: no rung's allocation count moved. SPEC NOTE: `ranged-typealias.md`'s ported `cast-to-stdlib-internal-typealias` was green because its `ElementCount` lives in `stdlib/Array.maxon`, which the loader whitelist does not list — the name resolved to nothing, the cast evaporated, and the exit code came out right anyway. It is now disabled naming that blocker, the file staying a byte-identical port of `/specs`, and the property is re-tested for the RIGHT reason in the new `cast-target-type-resolution.md` against `Milliseconds` (`stdlib/Sleep.maxon`, non-exported and whitelisted) paired with `5 as Millisecond`. That pair is the whole discriminator and it has to be: MEASURED — with a runtime operand `x as Milliseconds` and `x as NoSuchName` emit BYTE-IDENTICAL Target IR, because all six whitelisted stdlib aliases are `int(0 to u64.max)`, a range that admits every 64-bit value and emits no guard. GATES on this exact tree: build exit 0 zero warnings, 138 codes; x64 spec-test 1610/0 exit 0; wasm32-wasi spec-test 1419/0 exit 0; `--workers=1` byte-identical to `--workers=12`; no exit 101; specs-shv2/fragments zero M after both suites. | 35,551,225 | 67,277,591 | 134,706,365 | 271,421,072 | 553,013,176 | 1,129,518,732 |
| 2026-07-25 | UNDECLARED-CAST rung — `5 as CompletelyMadeUpNameXyz` compiled clean and returned 5, and so did `5 as Point` and `5 as Scor`. `parseTypeReference` answers a few type spellings syntactically and hands back a bare `MaxonType.named` for everything else — a declared `type` named from outside its own body, a declared enum/union, a ranged typealias, a qualified inner alias, and a name nothing declares at all — and `maxonTypeTag` reads all five as ONE tag, so `applyCast`'s legality gate was asking about a NAME and its answer for a name denoting nothing matched its answer for a ranged int alias: convert nothing, flow the value on. The cure is NOT a second cascade in the parser: `TypeResolution`'s one list of sources became `denotedNamedType`, raising a payload-free `UnknownTypeName` instead of reporting, so each of its THREE readers picks the anchor — `resolveNamedType` reports E3011 unpositioned as before, `Parser.castTargetTag` reports it at the `as`, and `Parser.requireConstCastTargetKind` covers the SECOND cast site (a top-level `let X = 5 as T`, folded by the const evaluator, which never reaches the expression path). Falling out of the same lookup: `5 as Point` is now E3009 "Cannot cast from int to struct", the message `Self` and the type's own name already gave, and the one `applyCast`'s header claimed for `n as SomeStructAlias` while being false for the spelling users write. THE NUMBERS, RE-MEASURED BY THE OPTIMIZER AGAINST A BASE BUILT IN THIS SAME WORKTREE: the rung is EXACTLY memory-neutral — 0 allocations AND 0 bytes delta at all six rungs, and `codeBytes` unchanged at all six (62,457 / 115,686 / 222,294 / 435,554 / 862,198 / 1,716,126), so codegen is byte-neutral, which a diagnostic-only rung owes. ⚠ THE `+408 BYTES FLAT` THIS ROW ORIGINALLY RECORDED IS NOT A COMPILER COST, AND THE INFERENCE IT CARRIED (the hoisted `queryProgramSignatures` fetch) IS WITHDRAWN — IT IS THE LENGTH OF THE CHECKOUT PATH. Measured, not argued: base a2cc7c823 rebuilt and run at THIS worktree's path reproduces this row's bytes exactly at all six rungs, so base-minus-head is 0 in every column; the SAME base sources built and run in a second worktree whose path is 14 characters shorter read 2,856 bytes LOWER at every rung, FLAT, with allocations and `codeBytes` identical TO THE UNIT. That is 204 bytes per path character, and 408 / 204 = exactly 2 characters — the previous row was measured in a worktree named 2 characters shorter, and subtracting 408 from every rung of this row lands on it to the byte (35,550,817 / 67,277,183 / 134,705,957 / 271,420,664 / 553,012,768 / 1,129,518,324). ⭐ THE DURABLE FACT FOR EVERY FUTURE ROW: the corpus root `.scale-tmp` is relative to the WORKING DIRECTORY, so the BYTES column carries the checkout path and is NOT comparable across worktrees, while the ALLOCATIONS column is (it did not move by one at any path length). Compare bytes only within one path, and read a small FLAT bytes delta across a rebased or re-worktree'd rung as the path until proven otherwise. WHAT THE HOT PATH ACTUALLY COSTS — measured on purpose-built probes, because the ladder cannot separate these: a PLAIN named type reference costs 0 allocations. 20,000 of them in signature position allocate 16,029 in `resolveTypes`, which is 4 per FUNCTION (the `MaxonTypeArray` and the `FuncSignature` re-sync) and 0 per REFERENCE, identical under base and head — so the payload-free `UnknownTypeName` is MEASURED rather than asserted: making `denotedNamedType` throwing added no per-call box. The QUALIFIED-alias win is real and small: 8,000 `StrWrapper.Idx` references resolve 1.0 ms faster (29.8 -> 28.8 ms, median of 7 on an idle box), which is -125 ns per qualified reference and -3.4% of the phase, at 0 allocations and 0 bytes — the per-reference `queryProgramSignatures` hit it removes is O(1) (`compositeSourceHash` carries its own dirty flag), so it never had bytes to give back. ⚠ MEASURED DEBT, PRE-EXISTING AND NOT THIS RUNG'S: the qualified arm ITSELF costs 18 allocations and 700 bytes PER REFERENCE (36,038 allocs / 1,400,968 bytes at 1,000 functions, doubling exactly to 144,038 / 5,600,968 at 4,000) — `splitQualifiedName` and `joinQualifiedName` rebuild the `Base.Member` key per reference and `DeclaredAlias` boxes on both the miss and the hit. It is MEASURED LINEAR (x2.00 per doubling at three sizes), so it is debt and not a defect; the trigger that would make it bend is generic-heavy code once P1.7 Array lands. A SECOND CHANGE THE CORPUS IS STRUCTURALLY BLIND TO: the cascade now reads the whole-program signature index rather than the `Project` registries, which is the only way the parser can ask it at all (the pure-parser invariant). That REDUCES the decider count rather than adding one — the parser was ALREADY deciding struct-ness from the index for every `named` parameter and return type (`resolveNamedStruct`), so resolution now agrees with the parser instead of merely with itself, and `upsertDeclaredEnum` is the single write of BOTH enum registries with `createEnumRegistry` seeding both. Probed directly rather than trusted: a struct, an enum and a ranged typealias declared in one file and named from a second file's signatures all resolve (exit 7), so the index is not missing what the registries hold. SPEC NOTE: `ranged-typealias.md`'s ported `cast-to-stdlib-internal-typealias` was green because its `ElementCount` lives in `stdlib/Array.maxon`, which the loader whitelist does not list — the name resolved to nothing, the cast evaporated, and the exit code came out right anyway. It is now disabled naming that blocker, the file staying a byte-identical port of `/specs`, and the property is re-tested for the RIGHT reason in the new `cast-target-type-resolution.md` against `Milliseconds` (`stdlib/Sleep.maxon`, non-exported and whitelisted) paired with `5 as Millisecond`. That pair is the whole discriminator and it has to be: MEASURED — with a runtime operand `x as Milliseconds` and `x as NoSuchName` emit BYTE-IDENTICAL Target IR, because all six whitelisted stdlib aliases are `int(0 to u64.max)`, a range that admits every 64-bit value and emits no guard. GATES on this exact tree: build exit 0 zero warnings, 138 codes; x64 spec-test 1610/0 exit 0; wasm32-wasi spec-test 1419/0 exit 0; `--workers=1` byte-identical to `--workers=12`; no exit 101; specs-shv2/fragments zero M after both suites. OPTIMIZER PASS: NO COMPILER CHANGE SHIPPED — nothing in the rung is superlinear (`resolveTypes` allocations are 607/927/1,567/2,847/5,407/10,527, an exact x2 per doubling, and `Map.contains` is an open-addressing probe with a cached hash, so the cascade is O(name length) per reference and not O(declarations)), and the one constant-factor term worth naming is filed above as measured debt rather than chased. Gates re-run by the optimizer on this same tree: build exit 0, x64 1610/0 exit 0, wasm32-wasi 1419/0 exit 0, `specs-shv2/fragments` and the whole working tree clean after both suites. | 35,551,225 | 67,277,591 | 134,706,365 | 271,421,072 | 553,013,176 | 1,129,518,732 |
| 2026-07-25 | TYPE-NAME COLLISION rung — `type Box` plus `typealias Box` compiled CLEAN with the alias discarded (exit 7), and the same pair the other way round said only `E3009: Cannot cast from int to struct`. Five keywords file a type name in a whole-program map keyed by the BARE name and nothing checked for a second claimant, so a FIXED cascade (`denotedNamedType`: struct, then enum, then alias) picked the winner instead of the author. Six pairs measured silent or misdiagnosed, including cross-file, where it is not merely order-dependent but unconditional: a `type Box` in ANY file beat a `typealias Box` in the reader's OWN file, because only the ranged-alias registry is file-scoped. THE RULE: a type name is declared exactly once, with ONE exception — two `typealias` of the SAME FORM in DIFFERENT files. The carve-out is per alias FORM and not per keyword because `stdlib/` needs both halves at once: SEVEN files declare `Byte = int(0 to u8.max)` (ranged) and FIVE declare `ByteArray = Array with Byte` (generic-instance), so a ranged-only rule makes all of `stdlib/` unloadable; and it must NOT span two forms, measured — a ranged `Handler` in a.maxon had its own `5 as Handler` rejected with "Cannot cast from int to function" against b.maxon's function alias. ONE DOOR: `TypeNameRegistry` is the sole decider of "is this name already declared", fed by one ORDERED per-file array filled at `dispatchTopLevel` (the only site that sees all five keywords, which is what makes the order the file's SOURCE order across kinds and lets the blame land on the LATER declaration); `RangedAliasRegistry.record` loses its `redeclaredInSameFile` arm and keeps only E3105, its own fact. THE NUMBERS, A/B'd IN THIS ONE WORKTREE at one checkout path (base 85281a39a rebuilt here, then head): `codeBytes` IDENTICAL at all six rungs (62,457 / 115,686 / 222,294 / 435,554 / 862,198 / 1,716,126) — codegen byte-neutral, which a diagnostic-only rung owes. Allocations +373 / +561 / +924 / +1,637 / +3,050 / +5,863 and bytes +21,472 / +37,432 / +69,192 / +132,552 / +259,112 / +510,536. That is LINEAR and it is linear in the right variable: solving the two-term fit against the emitted corpus (79 type declarations over 30 files at rung 0; 1,598 over 185 at rung 5) gives 3.2 allocations per TYPE DECLARATION and 4.0 per FILE, so the term grows with DECLARATIONS and never with the program's op count — +5,863 is 0.037% of rung 5's 15,847,524. The three-per-declaration are the `TypeNameDecl` box, the ordered array's amortized growth and the registry map's entry; the per-file four are the artifact's own `TypeNameDeclArray`. ALSO FIXED, and it is why the headline case still failed once the check existed: a `ParseError` discards the whole artifact, so a file that died on the collision's OWN CONSEQUENCE never reported the collision — `Parser.abortedParseArtifact` carries exactly the type-name declarations across an abort (each a FINISHED record, so there is no half-built state to salvage), and both diagnostics now appear with the E3009 first because the parser runs before the merge. NOT THIS RUNG's, PRE-EXISTING, REPRODUCED ON THE BASE BINARY IN THIS WORKTREE: `verify-warm-rebuild maxon-shv2/track0` panics in `ProgramSignatures.baseLayoutOf` — "generic instance 1's base struct 'Promise' is not declared" — identically at 85281a39a and at head. GATES: shv2 build exit 0 zero warnings; x64 1631/0; wasm32-wasi 1440/0; --workers=1 byte-identical to --workers=12; no exit 101; fragments additions-only (zero M) and `specs/` clean; csharp build exit 0 and its suite 3103/0. | 35,574,309 | 67,316,635 | 134,777,169 | 271,555,236 | 553,273,900 | 1,130,030,880 |
| 2026-07-25 | TYPE-NAME COLLISION rung — OPTIMIZER PASS. (1) THE RUNG'S OWN NUMBERS ARE CONFIRMED, RE-MEASURED A/B IN THIS ONE WORKTREE AT ONE CHECKOUT PATH: base 85281a39a checked out over this tree, both compilers rebuilt, measured, then head restored and rebuilt. Head-minus-base is +373 / +561 / +924 / +1,637 / +3,050 / +5,863 allocations and +21,472 / +37,432 / +69,192 / +132,552 / +259,112 / +510,536 bytes — EXACTLY the figures the implementer reported, to the unit, at all six rungs — and `codeBytes` is identical base-to-head at all six (62,457 / 115,686 / 222,294 / 435,554 / 862,198 / 1,716,126), so codegen is byte-neutral. The two-term fit is confirmed against the EMITTED corpus, counted rather than taken on trust: 79 type declarations over 30 files at rung 0, 1,598 over 185 at rung 5, giving 3.21 allocations per type DECLARATION and 3.99 per FILE, a model that predicts all six deltas within 2%. The decisive reading is that the new term's SHARE of the program SHRINKS monotonically as the ladder doubles — 6.15e-4, 5.11e-4, 4.44e-4, 4.04e-4, 3.82e-4, 3.70e-4 — which is what "linear in declarations, never in op count" looks like from the outside. (2) ONE CHANGE SHIPPED, AND IT IS DEAD WORK REMOVED, NOT A CONSTANT FACTOR SHAVED: `FileParseArtifact.create` built the new `TypeNameDeclArray` EAGERLY, but only `recordTypeName` (reached from `dispatchTopLevel`) ever pushes one, and `dispatchTopLevel` runs on the REAL parse alone — so TWO of the three `Parser` construction sites could never fill it. The tolerant declaration sweep (`foldDeclaredSignaturesInto`, one Parser per file) walks tokens itself, and `walkConstInitializer` (one Parser per top-level constant) evaluates an expression; both were billed one allocation apiece for a structure with no writer. `typeNameDecls` now points at a shared empty singleton and copies on write at the first push, exactly as `interfaces`/`conformances` have since P1.7a — the pay-for-use discipline this same file already documents, applied to the field the rung added. MEASURED, and the attribution is exact to the unit: `phase:signatures` head-minus-base is now 0 AT ALL SIX RUNGS (it was +96 / +165 / +303 / +579 / +1,131 / +2,235, which is precisely files + top-level constants: 30+64 at rung 0, 185+2,048 at rung 5), and `phase:parse` drops by exactly 17 at rung 0 and exactly 141 at rung 5 — the counts of corpus files that declare NO type at all (30 files, 13 with declarations; 185 files, 44 with declarations). Total saving −113 / −186 / −332 / −624 / −1,208 / −2,376 allocations and −4,520 / −7,440 / −13,280 / −24,960 / −48,320 / −95,040 bytes, at 40 bytes per empty array. That is 41% of the whole rung's memory cost, and it was being spent on nothing. The rung now costs +260 / +375 / +592 / +1,013 / +1,842 / +3,487, whose share of the program halves down the ladder (4.29e-4 to 2.20e-4). `codeBytes` unchanged at all six rungs by the optimization. (3) THE DECLARATION-PATH PROBE IS O(1) AND ALLOCATION-FREE, and the second half is MEASURED rather than argued. `TypeNameRegistry.record` keys `Map with (ByteArray, TypeNameDecl)` on the NAME TOKEN'S OWN ByteArray — it builds no key, so the previous rung's filed debt (`splitQualifiedName`/`joinQualifiedName` rebuilding a `Base.Member` key per reference at 18 allocations each) is NOT repeated one level over. `Map` is open-addressing with a cached-hash guard and `Array.hash` walks the bytes in place, so the probe is O(name length) and never O(declarations). The one thing that looked suspicious was that the common path — the FIRST declaration of every name, which in a clean program is all of them — reaches its `otherwise` arm through a `get` that THROWS. An experiment replaced it with a non-throwing `contains` probe, rebuilt, and re-measured: `phase:merge` came out IDENTICAL TO THE UNIT at all six rungs (+119 / +176 / +282 / +486 / +886 / +1,678 either way), so `MapError` rides the scalar error channel and allocates nothing. The experiment was REVERTED — it bought zero and cost a `panic` arm. (4) WHAT REMAINS IS IRREDUCIBLE AND IS NOT DEBT: ~1 allocation per type declaration in `phase:parse` (the ordered per-file record, which is the fact no per-kind array can hold) and ~1 in `phase:merge` (1,598 declarations + 44 iterators + 36 map-growth reallocs = 1,678 at rung 5, exactly). Both are proportional to type DECLARATIONS, which is the feature's actual input; nothing here is superlinear and nothing is triggerable into bending. `Parser.abortedParseArtifact` is free on the happy path by construction — it is called only from `queryParseOps`' `otherwise (parseErr)` arm — and the per-file cost that DID exist on the happy path was the eager array, which is what (2) removed. GATES on this exact tree: shv2 build exit 0 zero warnings, 138 codes; x64 spec-test 1631/0 exit 0 no leak; `--workers=1` 1631/0 exit 0 (serial determinism); wasm32-wasi spec-test 1440/0 exit 0; csharp spec-test 3103/0 exit 0; `specs-shv2/fragments` and `specs/` ZERO modified after all three suites, working tree carrying only the one source edit. | 35,569,789 | 67,309,195 | 134,763,889 | 271,530,276 | 553,225,580 | 1,129,935,840 |
| 2026-07-25 | P1.7 Workstream O — the TWO DUAL CASCADE QUADRATICS COLLAPSED. The +7,159 in this row is BOUGHT, not lost, and is attributed to the unit below. (1) WHAT SHIPPED: two pairs that each wrote ONE fact down TWICE and so carried ONE quadratic twice. PAIR 1, the per-instantiation gates (`genericTypeFieldHas{Managed,CoOwnedTrivial,Uncopyable}Instantiation`), already shared one scan, but that scan walked the WHOLE instance registry `.equals`-ing every base name — O(call sites x TOTAL instances); `GenericInstanceRegistry` now carries a by-base-id index written by the same `intern` that assigns the ids (the second payload=>id direction beside the existing composite-key one), and `TypeNameInterner.idOf` resolves the base NAME to that id WITHOUT interning, because interning on a miss appends and shifts every later id into the emitted mangling. PAIR 2, `closeDestructorNeeds` and its clone dual `noteClonerUsage`, were each a `while changed` RE-SCAN of every declared type — D passes over T types for a cascade chain of depth D, re-minting every type's `__destruct_`/`__clone_` symbol on every pass; both now drive ONE shared `CascadeWorklist` (`Compiler/Runtime/CascadeNeeds.maxon`) that registers the node space once and expands each referenced node EXACTLY once, O(types + fields). The growth flag is gone: the queue is the growth signal. (2) NEITHER QUADRATIC IS VISIBLE HERE AND BOTH WERE MEASURED ELSEWHERE, on purpose-built ladders at the trigger each PLAN.md filing names, via `--metrics` min-of-5 on an idle box. PAIR 1 (N generic types, each one opaque-field-reassign site), `phase:parse` ratio per doubling 2.05/2.16/2.57/2.57 -> 1.96/2.00/2.18/1.94; at N=1024, 323.7ms -> 175.8ms. Its per-phase ALLOCATIONS are BIT-IDENTICAL before and after at every rung (748,425 both ways) — which is the correctness evidence, not just a perf note: the gate visits exactly the same instances, it has merely stopped walking the rest. PAIR 2 (D-deep managed chain) in the EXACT unattributed-allocation column, per doubling: cloner 2.75/3.25/3.66/3.82 -> 1.63/1.77/1.87/1.93, destructor 2.94/3.33/3.69/3.83 -> 1.78/1.88/1.93/1.96. At D=512 that is 3,957,231 -> 174,870 allocations (cloner) and 1,909,550 -> 85,541 (destructor): 22.6x and 22.3x, both curves now linear. (3) THIS ROW'S DELTA IS A/B'd IN ONE WORKTREE AT ONE CHECKOUT PATH: base 15321679e checked out over this tree, rebuilt and measured — allocations came back IDENTICAL to the previous row at all six rungs and bytes -204, exactly the one-character path difference, which is what makes the comparison sound — then head restored and rebuilt. Head-minus-base = +446/+681/+1,128/+2,005/+3,732/+7,159 allocations, and it is LINEAR: per-doubling 1.53/1.66/1.78/1.86/1.92, rising toward 2.00 from below, with the term's share of the program SHRINKING (7.35e-4 down to 4.52e-4). ATTRIBUTION IS EXACTLY THREE-WAY: `phase:load` +5 FLAT at every rung (the one new `byBase` map object per `ProgramSignatures.create`); `phase:signatures` +21/+30/+55/+96/+169/+306 (by-base buckets filled as the sweep interns instances); `unattributed` +420/+646/+1,068/+1,904/+3,558/+6,848 — the destructor closure's node REGISTRATION at ~2 allocations per registered node, one boxed `CascadeNode` and one collision bucket. (4) THE COLLISION BUCKET IS NOT DEFENSIVE PROGRAMMING — it was PROBED and the collision is REACHABLE: a `type Box_String` declared alongside a `Box with String` whose mangling is also `Box_String` compiles and runs today, so a single-valued index would have silently stopped expanding one of two claimants. Expanding every claimant is precisely what the old all-types re-scan did, so the bucket keeps behaviour identical. (5) ONE FURTHER CHANGE KEPT, ONE DECLINED. Kept: seeding now drives from the NEEDED SET rather than probing that set once per declared type — O(referenced) instead of O(types), which also deleted a whole parallel column and put the seed and discover questions in one place. Declined: parallel scalar columns to dodge the union box, and prefix-stripping to dodge registration for structs/unions, would buy ~0.02% of rung-5 allocations in exchange for a second lookup path and the very wrong-table hazard the layout-carrying node exists to prevent (the OPEN #56 mismatch). The mandate says a tidy O(n) beats a clever O(n), so it was not done. GATES on this tree: shv2 build exit 0 zero warnings, 138 codes; x64 spec-test 1634/0 exit 0 no leak at BOTH --workers=12 and --workers=1; wasm32-wasi 1443/0 exit 0; `specs-shv2/fragments` ZERO modified after all three suites, with one ADDED golden for the new `clone-managed-three-level-cascade` spec (the committed clone spec nested ONE level and so never exercised the transitive step either closure exists for). | 35,595,881 | 67,356,695 | 134,838,525 | 271,671,248 | 553,501,344 | 1,130,499,721 |
| 2026-07-25 | OPTIMIZER PASS on the forced-split-cascade rung. (1) THE RUNG HEADLINE IS INDEPENDENTLY CONFIRMED, re-measured A/B in ONE worktree at ONE checkout path: base 15321679e rebuilt here, `regalloc:splitting` bytes 19,434,340 / 74,569,077 / 296,624,503 at N=128/256/512 (x3.84, x3.98 = N^1.97) against 424,761 / 837,185 / 2,318,502 after (N^1.22 over that span, N^1.16 out to N=1024) — 128.0x fewer bytes at N=512, 161x less time, emitted code -33%/-37%/-38%, splitter allocations x1.97/1.98/1.99, and the int control executable byte-identical at every N. (2) THE REMAINING SUPERLINEARITY IS FIXED, NOT FILED. `regalloc:splitting` was still Theta(insertions x block) in TIME while its allocations were linear, so THIS INSTRUMENT IS STRUCTURALLY BLIND TO IT: 22.6 / 58.0 / 171.5 / 549.7 ms at N=128..1024 fits 16.0n + 6.59n^2 to within 1%, so 77% of splitting and 68% of the whole compile at N=1024 was the quadratic term. All THREE Theta(block) costs per inserted op — the SEARCH for the anchor op position (`positionInBlock`), `Array.insert` suffix shift, and the walk of the block use thread that restates sequence numbers (`shiftBlockSeqs`) — fall to ONE change of shape, because fixing any one alone buys no asymptotic gain: the splices are now LOGGED and materialized in one backward pass per block (`IrBlock.spliceOpRefs`), which puts every position in the block PRE-SPLIT coordinates, which in turn lets a reload anchor position be READ OUT of the use record that asked for it (`UseIndex.usePosAt` — a sequence number IS block x stride + position) instead of searched for. After: 16.2 / 32.8 / 69.6 / 138.6 ms, N^1.03, fitted quadratic coefficient ~0; 3.97x faster at N=1024 and the whole compile 2.95x. (3) THE ALLOCATION DELTA. The RUNG cost is confirmed at +3.3..+3.5% and strictly FLAT — +19,850 / +37,225 / +71,978 / +141,483 / +280,494 / +558,515, delta ratios 1.875 / 1.934 / 1.966 / 1.982 / 1.991 converging on 2.00, no growth term — being one `slotResident` column plus three `SplitScratch` buffers per function against two `OperandList.create()` removed from the per-split path. THIS PASS ADDS ZERO: allocations AND frees identical to the rung at all six rungs, and codeBytes identical. Only bytes move, +0.006% (+4,442 .. +67,682, sub-2.0 ratios), and it is one deliberate cost: `spliceOpRefs` grows `opRefs` with `growFilled` (geometric) rather than `resize` (exact-fit), because exact-fit is precisely the Theta(extensions x length) trap `Array.growFilled` documents for a buffer extended repeatedly — a block spliced into by several splits in turn. The first cut used `resize` and was measured at +192 allocations for it; that is the whole reason the byte line moved. CORRECTNESS, beyond the green suite and beyond the goldens, because neither is sufficient for a change to WHERE ops land: the emitted executable is BYTE-IDENTICAL before vs after for all six scale-corpus rungs (rung 5 = 139,381 lines, 1,915,990 bytes, with `regalloc:splitting` making 1.08M allocations inside it) and for every ladder program including N=1024; and the one new identity this rests on — that a use record sequence number gives the same position the old search returned — was ASSERTED against that search in a throwaway build and run over the whole 1636-test suite, the ladder and the corpus, with zero mismatches. x64 1636/0 at --workers=1 and =12 with byte-identical output, wasm32-wasi 1445/0, `specs-shv2/fragments/` clean, `verify-warm-rebuild` PASS, `VerifyIncrementalSplit=true` green on the suite, no run exited 101. | 35,909,823 | 67,936,881 | 135,966,880 | 273,883,829 | 557,880,274 | 1,139,192,798 |
| 2026-07-25 | INDEPENDENT REVIEW of the forced-split-cascade rung. Allocations and bytes both fall slightly and with NO growth term: -205/-290/-460/-800/-1,480/-2,840 allocs and -14,376..-119,776 bytes across rungs 0-5, delta ratios 1.41/1.59/1.74/1.85/1.92 converging on 2.00 — a flat per-function saving, not a change of curve. It is one thing: `markRunStarts` no longer derives a use's position by SEARCHING the block's op list for its op index. That search made it re-collect the whole block (`collectBlockOps` into a per-function scratch array) for every use-block of every split, and — because the scan for the FIRST use in a block starts at position 0 while `havePrevious` is false — it evaluated `opStrandsClass` over the entire prefix ahead of that use and THREW THE ANSWER AWAY: Theta(block) per split, Theta(splits x block) per function, ~M x 2M discarded calls on a straight-line function with M distinct f64 locals read after one call. It allocated almost nothing, which is why this instrument was nearly blind to it; it was found by reading. Both endpoints now come out of the use records (`usePositionIn`), which also removes the SECOND derivation of a use's position — the rung had just introduced `bodyPositionOfUse` to read that same fact out of the index, and two derivations of one position cannot disagree loudly. The `SplitScratch.splitOps` buffer goes with it. Behaviour is identical: zero golden churn, x64 1636/0, wasm32-wasi 1445/0, no run exited 101. Also added, at O(1) and always on: `usePositionIn` compares the op at the position against the op the record names, which is the only cross-check between the two halves of the splice-shift rule (`IrBlock.spliceOpRefs` PERFORMS the shift, `SplitEdits.splicesAtOrBefore` PREDICTS it, in different files, nothing deriving one from the other); and `SplitEdits.commitBlock` now proves the op it is about to remove IS the one the removal meant. Both were seen RED — inverting `splicesAtOrBefore`'s comparison fails 4 register-spill tests with the new message instead of miscompiling silently. | 35,895,447 | 67,919,105 | 135,942,304 | 273,845,653 | 557,814,898 | 1,139,073,022 |
| 2026-07-25 | MANGLE-COLLISION — a generic instantiation's COMPILED name and a declared type name are one namespace (E3006). WHAT SHIPPED: `ParseStaging.checkTypeSymbolNamespace`, one whole-program walk of the interned generic instances called from `compileToCodeResult` beside `checkWhereConstraints` (both halves of what it compares are complete there: `queryProgramSignatures` has interned every instantiation, `queryAllModule` has merged every declaration). Each instance's `mangleGenericInstance` string is probed against `TypeNameRegistry` (new `declarationOf`, the ask-without-filing door — `record` cannot serve a prober because it files as it asks) and against a claim map of the manglings seen so far. THE DEFECT WAS THREE-SHAPED AND ALL THREE WERE MEASURED ON THE BASE COMPILER, each from a build that exited 0 with NO diagnostic: (a) LEAK — `type Box_String` with two String fields plus a `Box with String` — `installStructDestructors` and `installGenericInstanceDestructors` both emit `__destruct_Box_String`, the later install wins, the struct's second String is never released, program exits 101; (b) SILENT SUCCESS — same pair with MATCHING layouts (`Box_String` holding one `Holder`) — the surviving cascade releases a Holder box through `__str_decref`, which lands on a real refcount header, and the program returns 4 with no leak, so the type confusion is invisible until a field moves; (c) SIGSEGV with NO declaration involved — `_` is a legal name character so the base_arg join is NOT injective: `Pair with (Box_Int, Str)` and `Pair with (Box, Int_Str)` both compile to `Pair_Box_Int_Str`, and each pair's fields are then released through the other's per-field callees. The bootstrap has (a) too and is worse — `mm_decref: refcount underflow` plus a nil-pointer panic — so this is a genuine language rule, not an shv2 artifact. THE COST, A/B against the previous row at the same base commit e306c562a (ALLOCATIONS only; bytes are not comparable across checkout paths and this tree's path is longer): +117/+203/+367/+687/+1319/+2575 allocations, per-doubling 1.73/1.81/1.87/1.92/1.95 — rising toward 2.00 from BELOW, i.e. LINEAR, and 0.016% of rung 5's 15,854,882. Attribution is exact and entirely expected: one `Map` per compile that instantiates any generic, plus ~2 allocations per interned instance (the mangling `String` and the `ByteArray` key both probes share). A generics-free program returns before allocating anything at all. DECLINED, deliberately: a filter that skips instances whose mangling cannot be ambiguous (no `_` in the base name or in any argument's mangling) would cut most of the walk on the real corpus but needs most of the mangling work to decide, and the mandate is that a tidy O(n) beats a clever O(n). NOTED FOR A LATER OPTIMIZER PASS, not done here: `mangleGenericInstance` returns a `String` that FIVE of its seven call sites immediately `.toByteArray()` (four in MmRuntime, one in SignatureIndex) — a pre-existing round-trip this rung adds a sixth site to; changing the return type would pay back across all of them at once. GATES on this tree: bootstrap build exit 0 zero warnings (ErrorCode.g.cs regenerated, comment-only) and C# suite 3103/0 with `git status specs/ specs-shv2/` byte-identical across it (codegen neutrality); shv2 build exit 0 zero warnings, 138 codes; x64 spec-test 1641/0 exit 0 no leak at BOTH --workers=12 and --workers=1, stdout byte-identical; wasm32-wasi 1450/0 exit 0; `specs-shv2/fragments` ZERO modified, seven ADDED goldens for the seven new `type-name-collision` cases. | 35,601,186 | 67,367,394 | 134,859,869 | 271,713,728 | 553,585,992 | 1,130,668,731 |
| 2026-07-25 | MANGLE-COLLISION, OPTIMIZATION PASS — the E3006 compiled-name check is LINEAR (verified past rung 5), and `mangleGenericInstance` is now DERIVED ONCE, which removes a pre-existing QUADRATIC. (1) THE CHECK IS LINEAR, and this is now measured rather than extrapolated. A/B in ONE worktree at this commit (check present vs. its walk suppressed, everything else identical), extended to EIGHT rungs: the check costs +117/+203/+367/+687/+1319/+2575/+5079/+10079 allocations, per-doubling 1.735/1.808/1.872/1.920/1.952/1.972/1.984. The implementer measured to rung 5 and read "rising toward 2.00 from below"; carried two rungs further it SETTLES at 1.984 and never crosses 2.00. The decisive statement is not the ratio but the SHARE: 0.0193/0.0185/0.0176/0.0170/0.0165/0.0162/0.0161/0.0160 percent of each rung's total — monotonically DECREASING across a 128x span. A superlinear term's share grows; this one shrinks. (For contrast, the cascade rung's gates read x2.05 at N=128 and x2.57 by N=512 — they crossed 2.00 and kept climbing.) Complexity confirmed by reading: one pass over the interned instances, two O(1) map probes each (`TypeNameRegistry.declarationOf`, then the claim map), no nested scan. It does NOT use `GenericInstancesByBaseMap` and must not — a compiled-name collision CROSSES bases (`Box_A with B` and `Box with A_B` share a mangling and no base) and the declared half is not in that index at all. `instantiationAnchor`'s linear site scan runs only after a collision is already found, i.e. never on a program that compiles. (2) THE ROUND-TRIP, TAKEN — and it was hiding a quadratic, not a constant factor. `mangleGenericInstance` was recomputed at every ask by EIGHT call sites, and the recomputation RECURSES: `mangleTypeArg`'s nested arm re-enters it, so an instance nested d deep re-derived all d enclosing names on every one of those asks. MEASURED on a hand-written chain of N inline-nested instantiations (`Wrap with (Wrap with (... Small))`, ~5 KB of source at N=1600, no scale corpus involved): allocations inside `mangleGenericInstance` were 9,897 / 39,797 / 159,597 at N = 50/100/200 — x4.02 and x4.01 per doubling, a textbook quadratic — and 87% of every allocation the compile made. Whole-compile cost at N=1600 was 31,134,966 allocations / 55,790,817,306 bytes / 5,922 ms, of which 97% was UNATTRIBUTED in the per-phase table because the passes that ask sit between phase brackets. THE FIX: `ProgramSignatures.compiledInstanceNames`, a gid-keyed memo (`CompiledInstanceNameMap`); each name is built once from its argument's already-built name, and the accessor now returns the `ByteArray` six of the eight callers wanted (the `.toByteArray()` at all six is gone; the two that want a `String` say `String.from`). AFTER, same probe: N=1600 is 407,079 allocations / 93,009,340 bytes / 915 ms — 76x fewer allocations, 600x fewer bytes, 6.5x faster — and whole-compile allocations at N=100 vs 200 go 28,954 -> 51,077, x1.76, LINEAR. `mangleGenericInstance` no longer appears in the ranked scope table at all. (3) THE ROW ABOVE: the memo pays back on the REALISTIC ladder too, and more than the check ever cost. -196/-380/-756/-1516/-3044/-6108 allocations here, and -12,244/-24,524 at rungs 6/7; the savings ratio per doubling is 1.94/1.99/2.005/2.008/2.007/2.005/2.003, i.e. proportional to the instance count, as a de-duplicated per-instance derivation must be. Netted against the check's own cost the rung is now allocation-NEGATIVE versus its base commit e306c562a: -79/-177/-389/-829/-1725/-3533/-7165/-14445. The safety check is free and then some. CODEGEN NEUTRAL, which is the gate that matters for a pure-perf refactor: the memo hands back the same bytes, and `specs-shv2/fragments` is byte-identical (zero modified) across the x64 suite, the wasm suite AND the C# suite. STILL OPEN, and reported to the coordinator rather than filed here: a SECOND, time-only superlinearity on the same pathological input survives (N=1600 still 915 ms, ~x3.4 per doubling) while its ALLOCATIONS are linear — so `scale-test` is structurally blind to it, exactly as this corpus's own header warns. It is NOT this rung's: suppressing the E3006 check changes it by ~8% (846 ms vs 915 ms at N=1600, within noise). It lives in the region between `queryAllModule` and the pipeline, which is 80.9% of that compile's wall time and is billed to NO phase — an attribution gap worth closing before the term is hunted. | 35,592,785 | 67,351,881 | 134,829,931 | 271,654,774 | 553,468,622 | 1,130,433,751 |
| 2026-07-25 | review(E3006): builtin-instance early-outs in genericInstanceHasStringField + addNestedInstanceDestructors — turns two reachable compiler panics into the documented answer; expected allocation-neutral (the guards fire only where the old code would have crashed) | 35,592,785 | 67,351,881 | 134,829,931 | 271,654,774 | 553,468,622 | 1,130,433,751 |
| 2026-07-25 | Wave 2 (xmm6-15 callee-saved) MEASURED ALONE — the scale corpus is UNCHANGED for this row (FloatsLivePerSpillLoop still 4); the next row is the corpus retune. A float live across a call no longer force-spills a store/reload bracket, so the splitter mints fewer values and does less repair. | 35,659,444 | 67,441,176 | 134,969,809 | 271,929,691 | 553,856,201 | 1,131,039,281 |
| 2026-07-25 | CORPUS RETUNE, not a compiler change: FloatsLivePerSpillLoop 4 -> 12. Wave 2 made the floatSpill knob INERT — its four cross-call floats now fit the ten callee-saved XMMs, so the knob that exists to measure the float FORCE-SPILL path had stopped forcing one, and would have reported a flat Delta0 for a path it no longer reaches (the instrument-cannot-express-the-feature trap). Twelve exceeds ten by the same slack the GPR knob keeps over its five. Everything grows because the generated programs are bigger; the compiler is byte-identical to the row above. | 36,001,032 | 68,128,343 | 136,511,208 | 274,958,182 | 560,411,697 | 1,150,807,911 |
| 2026-07-25 | OPTIMIZER PASS on the Wave-2 callee-saved-XMM rung. NO COMPILER PIPELINE CODE CHANGED — every number below is IDENTICAL to the row above, and that Delta0 IS the self-check on this pass's two edits: `ScaleCorpus.maxon` (the knob derivation, item 6) and prose in this file. (1) THE HEADLINE REPRODUCES, BUT THE RUNG'S TABLE DOES NOT, because the rung recorded no ladder — its 404,409/788,153/2,218,086 -> 43,717/86,829/165,645 cannot be re-run by anyone. So one was built and is recorded here: ONE straight-line function, N units, each = define one f64 local, call an opaque int fn, read the local (N floats each live across ONE call), plus an int-typed CONTROL of the same shape. `regalloc:splitting` BYTES, base 65ed46138 -> head, at N=128/256/512/1024: 24,466,558 / 96,360,042 / 382,486,175 / 1,524,199,159 (x3.94, x3.97, x3.98 — a clean N^1.99 quadratic) -> 77,359 / 142,119 / 314,175 / 669,695 (N^1.04). Splitter ALLOCATIONS go 1,275,640 / 5,073,917 / 20,239,875 / 80,849,418 (x3.98/3.99/3.99) -> 427 / 429 / 432 / 435, i.e. CONSTANT: one float across one call now takes a callee-saved XMM and no split happens at all. At N=1024 that is 126.3 s of splitting -> 13 ms, and 1.52 GB -> 0.67 MB. Emitted code -35 percent, and all six executables (float and int, N=128..512) return the SAME exit code under both compilers. THE INT CONTROL IS BIT-IDENTICAL base vs head at every N (426/428/430 splitter allocations, same bytes, same codeBytes) — the correctness evidence that Wave 2 provably did not touch integer allocation. (2) NO NEW SUPERLINEARITY; all four suspects checked and each is constant or linear. `assertCalleeSavedListsMatchMasks` runs ONCE PER MODULE over 15 registers (its only caller `assertCallClobberConsistent` is called once from `allocateRegisters`, which BackendDispatch calls once per module). `TargetLiveness.growValueSpace`'s new `for v in valueCount upto newValueCount` writes only the ids THAT call minted, and the one `LivenessResult` the splitter maintains has a monotonically rising `valueCount` (a full rebuild happens only under `VerifyIncrementalSplit`), so the loop sums to the total minted ids over a whole function — amortized O(1) per minted value, NOT the splits x values shape the previous rung removed. `colouredPhysicalRegs` is ONE scan per function feeding BOTH `usedRegsFrom` calls (5- and 10-element constant lists) — the same single scan `usedCalleeSavedRegs` already was. The GT chunk's save/restore loops are 10 iterations at chunk-BUILD time, once per module. (3) THE 75 PERCENT OF THE WAVE-2 ROW'S OWN DELTA THAT THE ROW DOES NOT NAME. A/B'd base vs head on an IDENTICAL corpus (both knob=4, two worktrees at equal-length paths). Head-minus-base allocations: `regalloc:coloring` -10,369 / -19,554 / -37,905 / -74,620 / -148,037 / -294,884, and `regalloc:splitting` -2,518 / -5,250 / -10,837 / -22,238 / -45,525 / -93,023. The row credits the splitter, which is only 24 percent of it. The other 75 percent is that `callerSavedMask()` STOPPED ALLOCATING: Wave 1 built it as `classRegisterMask(RegClass.xmm, file: x64RegisterFile())`, and `x64RegisterFile()` constructs a heap `RegisterFile` — on the COLORER'S PER-VALUE PATH (`preferredVolatilityMask`, up to 3x per `chooseRegister`). Spelling xmm0-5 out register-by-register deleted it. PROVEN, not inferred: a probe build of the BASE compiler changing ONLY that expression to the literal 0xFFFF0000 (a bit-identical mask) reproduces -10,358 / -19,531 / -37,858 / -74,525 / -147,846 / -294,501 in `regalloc:coloring` — 99.9 percent of the saving — with codeBytes IDENTICAL at all six rungs. Both curves are flat: delta ratios 1.886/1.938/1.968/1.984/1.992 converging on 2.00 from below, no growth term. It is the same waste `RegBits.maxon` already documents for arm64's encoder; the two remaining `x64RegisterFile()` calls are both once-per-module asserts. (4) THE ONE COST THIS RUNG ADDS, MEASURED AND DELIBERATELY NOT TAKEN: `phase:prologueEpilogue` +195 / +280 / +450 / +790 / +1,470 / +2,830 allocations and +7,448 .. +112,848 bytes, ratios converging on 2.00 — one extra `X64RegisterArray` per function, because `usedRegsFrom` is now called twice where `usedCalleeSavedRegs` allocated one list. It could be removed by keeping `coloured and calleeSavedMask()` as a mask and iterating the constant order filtered by it, but that respells "walk the order, test the mask" at four sites to buy 0.017 percent of rung-5 allocations on a curve that is already linear. A tidy O(n) beats a clever O(n) — declined on purpose, recorded so nobody re-derives it. (5) A QUADRATIC FOUND, MEASURED, AND SHOWN NOT TO BE THIS RUNG'S. On a SINGLE-BASIC-BLOCK function with a constant density of forced spills, `regalloc:splitting` is quadratic in program size: 8 ints live across each of N calls gives 124,175 / 489,272 / 1,943,676 / 7,749,362 allocations at N=16/32/64/128 (x3.94/3.97/3.99). It is FILE-AGNOSTIC and PRE-EXISTING — base and head are BIT-IDENTICAL on that ladder, every column at every N — and the float twin (12 floats per unit) is quadratic on BOTH, head merely 6.4x cheaper. The cause is the incremental repair's DIRTY UNIT being a BLOCK: in a one-block function every split re-commits and re-sweeps the whole function. It is MEASURED LINEAR IN PRACTICE, which is why it is reported rather than fixed here: the corpus's own shape — N LOOPS each holding its floats live across a call — reads x2.02 per doubling out to 256 loops (21,374 / 43,207 / 87,418 / 176,924 / 358,112 splitting allocations), and this six-rung ladder reads x2.04 in bytes with allocation ratios converging on 2.00. RE-MEASURE TRIGGER: a function whose ONE basic block grows with program size while forcing spills at that density — machine-generated straight-line numeric code, or an INLINER that flattens loops away (shv2 has none). (6) THE RETUNED KNOB IS LIVE, AND ITS STATED RATIONALE WAS WRONG. Swept `FloatsLivePerSpillLoop` on a 64-loop ladder of the corpus's exact shape (`regalloc:splitting` allocations): 4 -> 784, 6 -> 784, 8 -> 2,064, 9 -> 2,714, 10 -> 31,364, 11 -> 57,644, 12 -> 87,418, 13 -> 120,886, where 784 is the per-function floor. So the OLD value of 4 was genuinely INERT on head (784, the floor) while forcing 93,808 on base — the rung's inertness claim is CONFIRMED by measurement, not taken on trust — and 12 sits comfortably past the knee. But the knee is at TEN, not eleven, because `total` is an eleventh cross-call float; and the claim "twelve is ten plus the same slack the GPR knob keeps over its five" is FALSE — `PressuredValuesPerLoop` is 8 over 5, a slack of THREE. Corrected in `ScaleCorpus.maxon` with the sweep written down. AND THE TEN IS NO LONGER WRITTEN DOWN AT ALL (review finding F6): `floatsLivePerSpillLoop()` now returns `regMaskPopcount(calleeSavedMask() and classRegisterMask(RegClass.xmm, file: x64RegisterFile()))` plus `FloatSpillHeadroom`, so the half's width comes from the allocator's own mask and the next move of that line RETUNES the knob instead of blinding it — which is exactly how it went blind this time. (It is a function, not a `let`, only because a global initializer may not call one: E2045.) VERIFIED BOTH WAYS: the emitted corpus is BYTE-IDENTICAL to the literal-12 corpus at all six rungs and scale-test reports Delta0, so the derivation yields exactly 12; and with the class term flipped to `RegClass.gpr` in a throwaway build the corpus regenerates at SEVEN floats per loop (the five callee-saved GPRs plus the same headroom) — the derivation seen RED, so it reads the mask rather than merely coinciding with it. (7) BOTH WAVE-2 ROWS VERIFIED REPRODUCIBLE bit-for-bit, in separate worktrees at equal-length paths: the corpus-retune row comes back Delta0 on all six rungs, and the knob=4 row reproduces 613,518 / 1,110,982 / 2,105,421 / 4,092,725 / 8,067,926 / 16,020,150 exactly. Wave 2's own saving over the review row is -12,333 / -24,017 / -47,510 / -94,754 / -189,739 / -380,673, ratios 1.948..2.006 — flat, no change of curve. The corpus-retune row is a full EPOCH BOUNDARY marked only in prose, unlike every earlier corpus row in this log, so READING RULE 3 has been added to this file's header where a reader actually looks. GATES on this tree: shv2 build exit 0, 138 codes; x64 spec-test 1641/0 exit 0 no leak at --workers=1 AND --workers=12; wasm32-wasi 1449/0 exit 0; `specs-shv2/fragments/` ZERO modified after all four suite runs, with x64-linux and arm64-macos untouched. ALSO SEEN AGAIN: an orphaned `maxon-shv2 build --target=wasm32-wasi` child outlived the GREEN wasm suite and held the compiler binary open, failing the next build with E9001 — the already-filed harness-robustness defect, reproduced from the wasm runner. | 36,001,032 | 68,128,343 | 136,511,208 | 274,958,182 | 560,411,697 | 1,150,807,911 |
| 2026-07-25 | RE-BASELINE AFTER REBASING WAVE 2 ONTO 2518ab962 — NO WAVE-2 CODE CHANGED, AND THE DELTA BELOW IS UPSTREAM'S, NOT THIS RUNG'S. The three Wave 2 rows above (the rung, the corpus retune, the optimizer pass) were all measured against base 65ed46138. While the rung was in review, main took NINE commits: four for the E3006 generic compiled-name collision (including the `mangleGenericInstance` memo, which its own row records as allocation-NEGATIVE) and five for the debugger's P5 `maxon mcp`. Rebasing put those four rows ABOVE the Wave 2 rows, so the delta from the row directly above ours silently spanned upstream's work as well — three honestly-measured rows making a false comparison, which is exactly the trap this log's epoch convention exists for. This row is the corrected reading of the MERGED tree: rung 5 is 16,219,501 allocations / 1,150,742,609 bytes against the optimizer row's 16,223,032 / 1,150,807,911, i.e. -3,531 allocations and -65,302 bytes, and ALL OF IT IS UPSTREAM — the Wave 2 commits are byte-identical across the rebase (the conflict was confined to this file and to PLAN.md, both of which only append) and the E3006 memo is the row that predicted a saving of that shape. ⇒ READ THE WAVE-2 SAVING OFF THE THREE ROWS ABOVE, WHICH ARE INTERNALLY CONSISTENT AT ONE BASE; read THIS row only as the merged tree's new starting point. GATES on the rebased tree, all re-run by the coordinator rather than inherited: shv2 build exit 0 zero warnings; x64 spec-test 1651/0 at BOTH --workers=12 and --workers=1 with ZERO golden churn after each; wasm32-wasi 1457/0; total working-tree churn 0; no run exited 101. The count moved 1643 -> 1651 purely by upstream's eight new type-name-collision cases. | 35,998,604 | 68,124,197 | 136,503,282 | 274,942,376 | 560,379,643 | 1,150,742,609 |
| 2026-07-25 | P1.7 generic type-argument mismatch is REJECTED (E3005) — a generic instance gains its nominal identity (its compiled name) and a bare `T` parameter is substituted through the call's instance. A/B IN THIS WORKTREE against the same tree with the 5 compiler files stashed: +28/+56/+112/+224/+448/+896 allocations and +512 B ×2^n — exactly ×2 per doubling, i.e. LINEAR, and 0.0045% of the compile. Split: `phase:signatures` up (deriveCompiledInstanceNames pre-derives every swept instance's compiled name so the PARSE only ever reads one — a per-file fan-out must not write that memo) against the unphased E3006 sweep coming down by the same derivations, plus `phase:semanticCheck` +12→+384, the new per-generic-call CallGenericInstance. ⚠ The PREVIOUS row does not reproduce on an unchanged tree: the stashed base measured +19,645 allocations at rung 0 against it, so that delta is NOT this rung's and the prior row appears to have been recorded against a differently-built binary. | 35,925,075 | 67,968,935 | 136,016,514 | 273,980,367 | 558,072,252 | 1,139,593,437 |
| 2026-07-25 | P1.7 alias-named type arguments + E3091 cyclic generic aliases. SAME-LINEAGE A/B (one bootstrap, md5 4dfdbf34, both sides): +24/+37/+62/+111/+208/+401 allocations, ~x1.86 per doubling — sub-linear against a x2 corpus, 0.0038% of the compile. TWO movements, opposite signs, both attributed: `phase:semanticCheck` -384 at rung 5 is the CallGenericInstance union box REMOVED (a Maxon union value is a heap object, minted once per call into a shared generic body; callInstanceSubject returns a plain ValueId instead) — exactly the cost the optimizer isolated. Against it, `phase:signatures` +8 and the UNPHASED cycle walk (checkGenericAliasCycles, in compileToCodeResult outside any probe) account for the gross, plus the genericAliasDecls order column. ⚠ Supersedes the 2026-07-25 row above, which straddled a bootstrap refresh and is VOID for comparison — main's bin/ was stale (built 2026-07-24 with 26 newer maxon-sharp/*.cs), so that row and this one were measured on different lineages. | 35,926,419 | 67,971,303 | 136,020,930 | 273,988,879 | 558,088,956 | 1,139,626,525 |
| 2026-07-25 | P1.7 alias/E3091 OPTIMIZATION PASS — a CUBIC removed from the E3091 cycle check, and the rung re-baselined. (1) THE ROW ABOVE REPRODUCES AND IS NOT VOID IN THIS LINEAGE. dc0e1127c rebuilt with the same bootstrap (bin/maxon.exe md5 c5c966e9) reads delta 0 on allocations AND bytes at all six rungs, and ba91248db rebuilt in a throwaway worktree from that SAME bootstrap binary reads 626,246 / 1,135,559 / 2,153,782 / 4,188,879 / 8,260,120 / 16,405,345 — so the row above's "+24/+37/+62/+111/+208/+401" is confirmed at BOTH ends. ⚠ MEASUREMENT TRAP, FOUND THE HARD WAY AND WORTH KNOWING: `phase:load` BYTES track the compiler EXECUTABLE'S OWN FILENAME LENGTH. A probe copy named `probe-A.exe` (11 chars) read 3 bytes less at EVERY rung than `maxon-shv2.exe` (14); renaming that byte-identical binary to a 14-char name made the row reproduce bit-for-bit. Give A/B probe binaries equal-length names, or every rung's byte column shifts by the difference — and the shift is perfectly reproducible, so it looks exactly like a code change. (2) THE `CallGenericInstance` UNION BOX DELETION IS CONFIRMED AT THE COMMIT BOUNDARY, not inferred: `phase:semanticCheck` allocations ba91248db → dc0e1127c are 1882→1870 / 3516→3492 / 6784→6736 / 13320→13224 / 26392→26200 / 52536→52152, i.e. −12/−24/−48/−96/−192/−384, exactly x2 per doubling. One heap box per call into a shared generic body, gone — the symmetric undo of the "+12→+384" the row above booked for adding it. (3) THE CUBIC. `firstCyclicAliasDecl` asked "does this alias reach itself?" once PER ALIAS, and each ask re-walked the instances that alias's arguments reach with a LINEAR SCAN of a `visited` array at every pop — O(aliases x instances x visited). MEASURED, A/B in this worktree at dc0e1127c against the same tree with only this check suppressed, on a chain (`N0 = Box with S0`, `Ni = Box with N(i-1)`) that COMPILES CLEAN: the check ALONE cost 31 / 268 / 1,751 / 12,298 ms at N = 400 / 800 / 1,600 / 3,200 — x8.6, x6.5, x7.0 per doubling, i.e. CUBIC — while everything else in the compile stayed flat at 139…197 ms over that same 8x span. THE FIX: one Tarjan SCC over the whole instance-argument graph (`nodesOnACycle` in CsrGraph.maxon, over an adjacency built by the shared `buildCsr` counting sort), after which the blame is read off the cycle set in declaration-record order. AFTER, same ladder: 149 / 197 / 307 ms at N = 1,600 / 3,200 / 6,400 against a check-suppressed 143 / 194 / 295 ms — the check costs 6 / 3 / 12 ms and no longer grows out of the noise floor. Semantics verified DIFFERENTIALLY rather than assumed: 11 hand-built cycle shapes (diamond, 3-cycle, feeder-declared-first, two independent cycles in both declaration orders, a cycle entered through a `Pair` and through an inline nested instantiation, two clean DAGs) give BYTE-IDENTICAL diagnostics and exit codes under the old detector and the new one. (4) THE LADDER'S OWN MOVEMENT, EVERY UNIT ACCOUNTED: −5 / −29 / −77 / −173 / −365 / −749 allocations. The corpus declares 15 / 27 / 51 / 99 / 195 / 387 generic aliases; the old walk allocated exactly 2 arrays per alias (its `pending` and its `visited`) and the new pass allocates a FLAT 25 whatever the alias count is — so the saving is exactly 2 x aliases − 25 at every rung (30−25, 54−25, 102−25, 198−25, 390−25, 774−25), which is why it converges on x2.05 per doubling rather than being a constant. It lands in NO phase row, because `checkGenericAliasCycles` runs in `compileToCodeResult` outside every phase probe; the per-phase tables are otherwise identical. (Of that flat 25, ONE allocation and 56 bytes are the `SccWalk` record that carries the walk's six columns — deliberately paid: it replaced a seven-argument thread through four helpers, and a flat +1 per compile is not a growth curve.) (5) THE READ-TIME ALIAS RESOLUTION IS LINEAR — measured, not assumed. On a ladder doubling BOTH the alias count and the generic call sites (K aliases + K one-call functions, so the register allocator is not the subject), `phase:semanticCheck` allocations are 5,413 / 10,813 / 21,613 / 43,213 at K = 200 / 400 / 800 / 1,600 — x2.00 per doubling across an 8x span. `namedTypeArgIdentity` is one `Map.get` on `genericAliases` plus a read of the `compiledInstanceNames` memo that `deriveCompiledInstanceNames` already warmed: both O(1), no per-comparison walk of the alias table. ⚠ The FIRST version of that ladder put all 2K calls in ONE function and read x4.4–x5.5 per doubling — of which 98.6% was `regalloc:splitting`, the KNOWN budgeted debt (ARCHITECTURE.md:1336-1345) and not the alias read at all; `semanticCheck` was 0.9 → 1.8 ms on that very ladder. A ladder that hammers the splitter measures the splitter. CODEGEN NEUTRAL, the gate that matters for a pure-perf change: x64 1664/0, wasm32-wasi 1473/0, C# 3103/0, `--workers=1` byte-identical to `--workers=12`, and `git status specs/ specs-shv2/` EMPTY after all three suites. No exit 101. | 35,926,519 | 67,970,927 | 136,019,600 | 273,985,641 | 558,081,902 | 1,139,611,839 |
| 2026-07-25 | P1.7 alias/E3091 RE-BASELINE AFTER REBASING ONTO d2deebd4d — and the delta below is THREE causes, separated by same-lineage A/B rather than guessed. Net vs the row above: allocations −6,570 / −12,118 / −23,244 / −45,659 / −90,702 / −181,213, bytes +82,415 … +11,362,004. (1) THIS RUNG'S LAST TWO COMMITS (the ONE-canonical-identity fix and the review's cubic + false-rejection fix) contribute +161 / +283 / +519 allocations at rungs 0/1/2 — MEASURED, both sides built from the SAME bootstrap on the SAME rebased upstream, so nothing else moved between them. Ratios x1.76 then x1.83, converging on x2.00: LINEAR, and it is the second memo — `canonicalInstanceName` sits beside `mangleGenericInstance` (which still names every emitted symbol and the E3006 namespace) and holds one entry per instance. A second table costs one entry per instance; that is the price of routing four extractors through ONE door instead of three of them disagreeing. (2) UPSTREAM's Wave 2 (xmm6-15 callee-saved) takes allocations DOWN: a float live across a call no longer force-spills a store/reload bracket, so the splitter mints fewer values and does less repair. (3) UPSTREAM's CORPUS RETUNE (FloatsLivePerSpillLoop 4 -> 12) makes the generated programs BIGGER, which is why bytes rise while allocations fall — the two upstream causes push opposite ways and (2)+(3) together dwarf (1) by two orders of magnitude. ⚠ THE ROW ABOVE IS NOT COMPARABLE TO THIS ONE FOR ANYTHING BUT THIS ROLL-UP: it was measured before Wave 2 and before the retune, on a smaller corpus and a different register allocator. Attribute across this boundary only via a same-lineage A/B, never by subtracting the two rows. GATES on the rebased tree: x64 1674/0, wasm32-wasi 1480/0, C# 3103/0, `--workers=1` byte-identical to `--workers=12`, fragments zero `M` after all three suites (upstream's regalloc change did NOT move this rung's goldens), no exit 101. | 36,008,934 | 68,141,767 | 136,535,192 | 275,002,818 | 560,497,017 | 1,150,973,843 |
| 2026-07-26 | P1.7a TOP-LEVEL MANAGED `let` — a module-scope `let` may hold a String or a b"…" literal, materialized at every READ exactly as its literal already is (String: the ONE immortal .rdata record, deduped by bytes; byte-string: an owned 48-byte record over the ONE immortal .rdata blob). Allocations +64/+128/+256/+512/+1,024/+2,048 — EXACTLY doubling, so LINEAR, and rung 0's 64 is EXACTLY the 64 top-level let/var declarations the generated corpus contains (counted in .scale-tmp/rung0). It is ONE ConstInitializerValue box per top-level binding folded: the const evaluator's entry point now answers with a typed union (a NUMBER or a managed literal's BYTES) instead of a bare ConstValue, which is what keeps every fold below it scalar and untouched. Kept, on the coldest path in the compiler (once per top-level declaration in a program), because the alternative is a sentinel. TWO OTHER UNIONS WERE MEASURED AND REMOVED, by same-lineage A/B on this branch: a ManagedLiteralProbe answering 'which literal kind?' cost a second box per top-level binding (+248 -> +184 at rung 0) and an ArrayReceiverMutability answering 'may this receiver be written?' cost one box per ARRAY-METHOD CALL SITE (+184 -> +64), the latter being the same reading, and the same fix, that BuiltinConformer's header records one rung earlier — Maxon boxes a union case INCLUDING a payload-free one. Bytes +8,424 at rung 0 is those 64 boxes plus VarInfo gaining isParameter (8 bytes on every binding), which is what tells 'may this NAME be rebound?' (a parameter: no) from 'may the container it denotes be written?' (a parameter: yes). GATES: x64 1680/0, wasm32-wasi 1486/0, --workers=1 byte-identical to --workers=12, verify-warm-rebuild PASS, fragments 6 additions and ONE M whose PROGRAM changed (pop-via-local-binding's `let arr = self.items` is E3019 in the reference compiler and is now `var`), no exit 101. | 36,017,358 | 68,157,527 | 136,565,616 | 275,062,578 | 560,615,441 | 1,151,209,603 |
| 2026-07-26 | P1.7a OPTIMIZER — the managed-constant receiver probe is filtered by NAME, not by a whole-program BOOL. +8 allocations and +320 bytes at EVERY rung, FLAT rather than doubling, and that shape is the whole point: it is ONE `Set with ByteArray` (`ProgramSignatures.managedConstantNames`) created once per compile, replacing the `anyManagedConstant` bool. THIS CORPUS DECLARES NO MANAGED CONSTANT, so the instrument sees only the cost and structurally CANNOT see the benefit — the benefit was measured on a purpose-built ladder instead. What the bool did: it spared a program with no managed constant, but went permanently true at the FIRST one, after which every STATIC dotted call in the whole program (`Widget.create(…)`, `String.from(…)` — any base that is neither a local nor a capture) paid the full file-scoped probe just to be told "not a constant", and that probe BUILDS A STRING KEY (`fileScopedDeclKey`, ~4 allocations) plus union boxes. MEASURED on a 64→1,024 static-dotted-call ladder, one managed constant present vs absent: +390/+710/+1,350/+2,630/+5,190 parse allocations — linear in CALL SITES, a cost cliff that one `let G = "hi"` anywhere switches on for the entire program (and that the stdlib whitelist would switch on for every compile). With the name set the same delta is a FLAT +40 at all five rungs: O(static dotted call sites) → O(1). The set is a FILTER, never the answer — a hit still resolves through `constantValueOf`, so file-private scoping and `let`-vs-`var` keep their single home; VERIFIED on two files each declaring a *different* file-private `let SHARED` (filed only under the composite key), which still print their own values, IR byte-identical to before. ALSO CONFIRMED, not changed: the byte payload is NOT copied per read at runtime — two reads of one constant emit ONE shared `__str_rec_1` (`x64.leaRegRdata`), so v1's rematerialize-per-read curve is genuinely avoided; the E3019 receiver-mutability check allocates NOTHING (a `let`-receiver ladder and a `var`-receiver ladder are allocation-identical at every rung); `VarInfo.isParameter` is a scalar field costing zero extra allocations; and the implementer's +64/+128/+256/+512/+1,024/+2,048 was reproduced at BOTH ends by same-lineage A/B. GATES: x64 1680/0, wasm32-wasi 1486/0, --workers=1 byte-identical to --workers=12, verify-warm-rebuild PASS, fragments CLEAN (zero modified), no exit 101. | 36,017,678 | 68,157,847 | 136,565,936 | 275,062,898 | 560,615,761 | 1,151,209,923 |
| 2026-07-26 | P1.7 slice 2a TOP-LEVEL MANAGED `var` — a String or b"…" module-scope `var` is an 8-byte POINTER slot in writable .data, filled before `main` by a synthesized `__module_init` and released after it by `__maxon_global_cleanup` (both MAXON-tier functions appended to the merged module, so `lowerMaxonToStd` builds the literal's record and `scanRuntimeUsage` installs the floor they need). ⚠ THE DELTA COLUMN IS NOT THIS CHANGE: the previous row was recorded two rungs ago (the P1.7a optimizer, 1680/0) and the stdlib-whitelist rungs merged since were never logged, so the reported +1/-768 spans all three. MEASURED DIRECTLY by building base 8c1337f55 and this branch and running the ladder on both: rung0 619,767 -> 619,768 allocs and 36,016,862 -> 36,016,910 bytes; rung5 16,225,840 -> 16,225,841 and 1,151,209,107 -> 1,151,209,155. So THIS rung is +1 allocation and +48 bytes at EVERY rung, FLAT rather than doubling — one empty `ManagedGlobalArray` per compile (`signatures.managedGlobals()`), because THIS CORPUS DECLARES NO MANAGED GLOBAL and the instrument structurally cannot see anything else. Flat is the whole claim: nothing was added per op, per function or per file. The per-global cost was measured on a purpose-built 64->1,024 ladder of managed String globals each appended and read (allocations 60,060 / 100,967 / 182,633 / 345,756 / 671,776 = x1.68 x1.81 x1.89 x1.94, approaching x2.00 as the fixed floor amortizes — LINEAR in globals), and the 1,024-global executable runs and exits 0, so 1,024 clones in the init balance 1,024 decrefs in the cleanup. Two runtime branches BOTH reference compilers carry are absent by construction rather than by choice: the init store needs no load-and-decref of the old value (the slot is zero and shv2 does not share the assignment path, so no `func.Name == "__module_init"` match), and neither the cleanup nor a user re-assignment needs a null guard (`__module_init` runs unconditionally and fills every entry of the same list). GATES: x64 1692/0, wasm32-wasi 1497/0, --workers=1 byte-identical to --workers=12, verify-warm-rebuild PASS on managed-global programs, fragments 3 ADDITIONS and zero M, no exit 101. | 36,016,910 | 68,157,079 | 136,565,168 | 275,062,130 | 560,614,993 | 1,151,209,155 |
| 2026-07-26 | P1.7 slice 2a OPTIMIZER PASS. THE STANDARD CORPUS MOVED BY EXACTLY ZERO (delta 0 allocations, 0 bytes at every rung) — this pass changed only the managed-top-level RECEIVER path, and this corpus declares no managed binding, so nothing here reaches it. VERIFICATION FIRST, all by same-lineage A/B (one bootstrap; probe binaries given EQUAL-LENGTH names, which is what makes the numbers comparable — the implementer's row read +52/-4 against mine purely because `maxon-shv2.exe` is 4 chars longer than a probe name, and `phase:load` bytes track the executable's own filename): (1) the implementer's "+1 allocation / +48 bytes FLAT at every rung" is CONFIRMED EXACTLY, base 8c1337f55 vs branch 3bbf47902, rung0 619,767->619,768 / 36,016,858->36,016,906 through rung5 16,225,840->16,225,841 / 1,151,209,103->1,151,209,151 — one empty `ManagedGlobalArray` per compile from `signatures.managedGlobals()`, and a control ladder of 64..4,096 SCALAR globals reads the same +1/+48 at every size, so nothing was added per global, per op, per function or per file. (2) The per-global ladder is LINEAR and was pushed 16x further than claimed, to 16,384 managed String globals: total allocations 36,393 -> 5,276,154 with rung ratios 1.57 1.72 1.84 1.91 1.96 1.98 1.99 1.99, approaching 2.00 FROM BELOW and never exceeding it, and — the claim that actually settles it — a FLAT MARGINAL of 321.0 allocations per global at 512->1024, 321.3 at 1024->2048 and 321.0 at 8192->16384. A superlinearity makes the marginal GROW; the "floor amortizing" story predicts exactly this constant, so the x1.68 tail was the floor and not a curve. Byte-string globals read the same shape (marginal 418/global, flat). (3) "The dense per-value type columns, paid ONCE" is TRUE: `SynthesizedFunction.open` runs exactly twice per compile and `mint` is a push; `regalloc:splitting` is FLAT across the whole ladder (1,203 -> 1,242 for 64 -> 16,384 globals) because a global's ops die immediately, where a control ladder of N LOCAL Strings in one function reproduces the known budgeted `SplitLiveRanges` quadratic at ratios 3.88 3.94 3.97 3.99 — the managed-global path structurally avoids it. (4) NO files x globals term: 1,024 globals spread over 1/8/64/512 files costs a flat ~336 allocations per FILE with no growth in the globals term, so `managedGlobals()` is genuinely asked once and not from under the per-file query. (5) `TopLevelGlobalLookup`'s payload widening from a `ValueTypeTag` to a whole `MaxonType` costs ZERO: 4,096 global reads, parse-phase allocations 472,662 on both base and branch, because the `MaxonType` box merely moved from `emitGlobalLoad` to `resolvedGlobalOf`. WHAT CHANGED, and why the numbers above did not: the union-boxing audit found `parseMethodCall` testing `managedTopLevelReceiverAt` as a predicate and then RE-RESOLVING the identical name to build the receiver — visible in the code as an impossible-state `none` panic justified by "the two read the same resolution and cannot disagree". Measured as exactly 12.000 allocations per managed-`var` receiver site and 6.000 per managed-`let` site across a 1,024..16,384-site ladder. The gate now hands its resolution to the caller instead of being re-derived, and the panic arm is gone: managed-`var` receiver sites -12.000 allocations each, and managed-`let` sites — a P1.7a path this rung did not otherwise touch — now cost 3.000 FEWER than at base 8c1337f55, having briefly cost 3.000 MORE (the extra `ManagedReceiverBinding` box the slice added on that path). THE INTERESTING PART IS THE FIRST ATTEMPT, WHICH THIS INSTRUMENT CAUGHT: making the gate itself return the union was tidier and cost +441/+873/+1,737/+3,465 allocations on rungs 2-5 OF THIS CORPUS, doubling with the program, because a Maxon union value is a heap object INCLUDING the payload-free `none` — so a union-returning gate allocates once per DOTTED CALL rather than once per managed-receiver site. Split into an allocation-free bool gate (`managedTopLevelBaseAt`: the name filter plus `staticCallClaimsBase`) in front of the one boxing resolution, and the corpus returned to zero. The narrow ANSWER was not the cheap thing; the narrow QUESTION was. GATES: build exit 0 zero warnings, x64 1692/0, wasm32-wasi 1497/0, --workers=1 byte-identical to --workers=12, fragments zero M (so codegen is byte-identical), verify-warm-rebuild PASS on six managed-global programs incl. 1,024 globals and 4,096 receiver sites, no exit 101. | 36,016,910 | 68,157,079 | 136,565,168 | 275,062,130 | 560,614,993 | 1,151,209,155 |
| 2026-07-26 | P1.7 slice 2b — top-level ARRAY LITERAL globals (`let` and `var`). ALLOCATIONS MOVED BY EXACTLY ZERO at every rung (619,768 / 1,123,586 / 2,130,788 / 4,143,679 / 8,170,294 / 16,225,841), and getting there took finding TWO regressions this instrument caught, both of the same shape the last row warned about — a Maxon union value is a HEAP OBJECT, so a union placed on a per-SITE path allocates once per site forever. (1) The dedup that folded `arrayElementIsManaged` + `arrayElementDropCallee` into one union-returning `arrayElementDestroy` door cost EXACTLY +1 allocation per ARRAY-CREATION SITE — measured by same-lineage A/B on equal-length binaries at 32 sites -> +32 and 64 sites -> +64, and the scale corpus read +32/+64/+128/+256/+512/+1,024, which is its array-create knob doubling. The union now serves only the top-level-array-global path, which builds it ONCE PER MANAGED GLOBAL; the parser's per-site emitter composes the same two accessors base did, so the DECISION is still single-homed in `arrayElementIsManaged` and what is written twice is a three-line composition. (2) Splitting `declFor` into a shared `resolveDecl` + a filter re-BOXED the resolved answer, costing +1 allocation per name resolution that is not a local (+284 at rung 0 doubling to +9,212). Restored to the base two-probe shape, and the kind-free probe it existed for was deleted: the assignment rejection turned out never to need one, because the caller that meets a stored-but-immutable binding already holds the resolution that proves the name is declared. A THIRD attempt — sharing ONE module-level `TopLevelDeclLookup.notFound` to make both miss paths allocation-free — is recorded as a DEAD END: it built and then failed 128 spec tests with nil-pointer panics, so a payload-free union box is NOT safe to share the way `sharedEmptyInstanceAliasMap` is. BYTES moved and the movement is structural, attributed by the same A/B: +24 bytes per TOP-LEVEL DECLARATION (`TopLevelDecl` gained `hasStorage` — the one criterion deciding which of the two use-doors a binding goes through, now that an array `let` is immutable AND stored) and +8 bytes per TOP-LEVEL GLOBAL READ (`TopLevelGlobalLookup.found` gained `mutable`, which is what makes `Live.push(1)` an E3019 and `Live = [...]` an E2013 where `Buffer` accepts both), plus 48 bytes flat per compile. Both are linear constant factors on a 36 MB rung-0 compile (0.009%), and neither changes a growth rate. WHAT THE RUNG ADDED: an array initializer folds to its elements in the SAME scalar constant evaluator (`[BASE, BASE + 2]` works), `__module_init` builds the record with `__arr_create` + one `__arr_push` per element and CLONES a String element so the array may own it, and v1's `ConstantArrayLiteralRdata` element-buffer-into-rdata pass was deliberately NOT ported (it is an optimization, and it would fork a second element-emission path before the first is proven). GATES: build exit 0 zero warnings, x64 1710/0, wasm32-wasi 1515/0, --workers=1 byte-identical to --workers=12, fragments 18 additions and ZERO M (so no existing program's codegen moved), verify-warm-rebuild PASS on an array-globals program (determinism + cache + all four invalidation probes), no exit 101 on a leak battery incl. String elements, a 300-iteration push loop, a reassign and an untouched global. | 36,020,134 | 68,162,687 | 136,575,544 | 275,082,042 | 560,653,977 | 1,151,286,283 |
| 2026-07-26 | P1.7 slice 2b OPTIMIZATION PASS — ONE QUADRATIC FOUND AND FIXED, AND THIS INSTRUMENT IS STRUCTURALLY BLIND TO IT. ALLOCATIONS AND BYTES MOVED BY EXACTLY ZERO at all six rungs against the rung row (619,768 / 1,123,586 / 2,130,788 / 4,143,679 / 8,170,294 / 16,225,841 and 36,020,134 … 1,151,286,283), and codeBytes is identical too (63,349 / 117,262 / 225,250 / 441,270 / 873,434 / 1,738,402) — so this row is a VERIFICATION of the rung plus one change the corpus cannot see. (1) THE FIND: `Parser.immutableGlobalReads` — the slice's new "was this value read out of an IMMUTABLE global's slot?" mark, which is what refuses `var b = A` on a `let` array (E2015) — was a `ValueIdArray` LINEAR-SCANNED at the promotion gate. That is O(1) only if one of the two populations is bounded and NEITHER IS: the marks are one function's immutable-global reads, the scans are its borrowed managed-aggregate promotions, so a single function carrying K of each pays Theta(K^2). MEASURED by same-lineage A/B on a ladder of K interleaved `g(A)` reads and `b = s` promotions against the IDENTICAL program with `A` declared `var` (which marks nothing, so the two differ in exactly this structure), min-of-5, idle box: the scan's own cost was 3.52 / 15.55 / 65.22 / 259.55 ms at K = 2,000 / 4,000 / 8,000 / 16,000 — per-doubling x4.41, x4.19, x3.98, a textbook quadratic — and 39% of `phase:parse` at the top rung, with the `let` variant's parse bending x2.11/x2.29/x2.47 while the `var` control stayed flat at x1.96/x2.00/x1.99. THE FIX is a `Set with ValueId` (`ImmutableGlobalReadSet`) probed with `contains`, the same COW-shared-empty discipline `sharedEmptyInstanceAliasMap` uses, and the now-single-caller `valueIsImmutableGlobalRead` is inlined at its one gate. AFTER, same ladder: the `let` variant is LINEAR at x1.99/x1.98/x2.02, the isolated delta collapses from 259.55 ms to 5.79 ms at K = 16,000 and stops growing, and parse at K = 16,000 goes 669.85 -> 410.87 ms (1.63x). It cost +45..+55 allocations at K = 2,000..16,000 (the hash table's own buckets against the list's geometric growth) and ZERO on every program that does not mark. (2) WHY THIS ROW READS ZERO, WHICH IS THE POINT AND NOT AN OVERSIGHT: the old scan ALLOCATED ALMOST NOTHING (+21 allocations at K = 16,000 — the list's growth), so a memory instrument could never have seen it; it was found by reading and confirmed with `--metrics`. And the corpus declares no array `let`, so `recordGlobalReadValue` never marks, the copy-on-write never fires, `Set.contains` on the zero-capacity shared empty returns before it hashes anything, and the shared empty itself is a module-level `let` built before the measurement window opens. Pay-for-use, working. (3) THE OTHER TARGETS ARE LINEAR AND ARE NOW MEASURED, NOT ASSUMED. `initialRecordOf`'s array arm (`__arr_create` + one `__arr_push` per element) is O(elements): a ladder of a single top-level literal at N = 256..4,096 reads total x1.30/x1.51/x1.68/x1.85 with `phase:signatures` and `phase:lowerMaxonToStd` allocations DOUBLING exactly per rung (+513/+1,026/+2,050/+4,098 and +514/+1,028/+2,053/+4,101). Its one bend — `regalloc:liveness` at x2.22 — is NOT the new arm: an in-body `var A = [ … ]` CONTROL of the same N bends identically (x2.19) and is in fact slightly dearer overall (146.60 vs 137.33 ms at N = 4,096), so that is the shared one-huge-block shape meeting the budgeted per-split liveness recompute. `hasStorage` asked mid-evaluation (`evalConstNamed` -> `constantDeclFor`) is O(1) — two `declIndex` hash probes then one array index, no arena re-walk — confirmed on a ladder of N array `let` globals each read once at N = 64..1,024: total time x1.94, allocations x1.95, `phase:signatures` x1.99, `phase:parse` x1.98. (4) BOTH ACCEPTED CONSTANTS VERIFIED BY A/B PROBE AND BOTH STAND. `ArrayElementDestroy` on the parser's per-SITE emitter costs EXACTLY +1 allocation and +16 bytes per array-creation site (32 sites: 82,850 -> 82,882; 64 sites: 226,749 -> 226,813) — reproduced to the unit, and the decision stays single-homed in `arrayElementIsManaged`, which both sites read. A kind-free shared `resolveDecl` + filter costs EXACTLY +1 allocation per non-local name resolution (+256/+512/+1,024/+2,048/+4,096 on the globals ladder, doubling perfectly) because `TopLevelDeclLookup` is a union and a union value is a heap object; the two-probe shape stays. RESIDUAL FOR THE REVIEWER, not fixed here because it is a duplication question and costs nothing either way: `ParseError.assignToImmutable(String.from(nameTok.value), line: …, column: …)` is now constructed at THREE sites in Parser.maxon (8045 new, 8103 and 8406 pre-existing) and is on the throw path only, so single-sourcing it needs no probe and no allocation — it is the *resolutions* that legitimately cannot share, not the diagnostic. (5) v1's `ConstantArrayLiteralRdata` DECLINED, with the number that would reverse it: the present path costs ~63 allocations and ~30 us per constant element (regression over N = 256..4,096), which is real but LINEAR, and it is the language's array-literal cost rather than the top level's — the in-body control pays the same. Moving the element buffer to `.rdata` would fork a second element-emission path for a shape no source in this tree has yet, and its `capacity = -2` encoding needs a `__arr_push` detach the runtime does not have, which is a rung and not an optimizer pass. RE-MEASURE TRIGGER: a machine-generated all-integer top-level table large enough that `__module_init`'s 2 ops per element dominate its compile. GATES on this tree: shv2 build exit 0 zero warnings (139 codes); x64 spec-test 1710/0 exit 0 no leak at BOTH `--workers=12` and `--workers=1` with BYTE-IDENTICAL stdout; wasm32-wasi 1515/0 exit 0; `specs-shv2/fragments` ZERO modified and the whole worktree clean but for the one source file, so codegen is byte-identical; `verify-warm-rebuild` PASS on an array-globals program; leak battery (String elements, 300-iteration push loop, reassign, untouched global) exit 0; and the eight behaviours the rung pinned all reproduce — 2, 9, 42, `hi hi! hi`, E2015 on the writable alias, E2004 in both declaration orders. | 36,020,134 | 68,162,687 | 136,575,544 | 275,082,042 | 560,653,977 | 1,151,286,283 |
| 2026-07-26 | P1.7 slice 2b REVIEW — a MERGE PHI now inherits the immutable-global-read mark, and it costs ZERO. ALLOCATIONS AND BYTES MOVED BY EXACTLY ZERO at all six rungs against the optimizer's row (619,768 / 1,123,586 / 2,130,788 / 4,143,679 / 8,170,294 / 16,225,841 and 36,020,134 … 1,151,286,283). THE DEFECT: the slice's E2015 refusal of `var b = <a `let` array>` keys on a mark that rides the VALUE — deliberately, so it needs no second encoding of the name-resolution order — but a value MERGE mints a NEW value, so the mark stopped at every join and the refusal was bypassed by writing the same thing one keyword longer. MEASURED at 3 where 2 is correct through FOUR shapes that all compiled clean: `var pick = A if c else M`; the degenerate `A if c else A` (both arms the same immutable global); a reassignment of an already-owned `var` from a ternary; and a `match … gives A` arm — with E2013, E3019 and the direct E2015 all intact and nothing to report any of them. THE FIX is one call at the one site that mints a result phi (`finalizeMatchMerge`), so the ternary and the match-expression are closed by construction rather than by two edits: `propagateImmutableGlobalReadToPhi` marks the phi if ANY reaching give is marked. The mark's WRITE is now single-homed in `markImmutableGlobalRead` — that half is the dangerous one, because a second insert site that skipped the copy-on-write identity test would insert into the MODULE-LEVEL `sharedEmptyImmutableGlobalReads` and every function in every file across the per-file parse fan-out would then see a mark naming an unrelated value in its own SSA space. WHY THIS ROW READS ZERO, which is the point: the new loop runs once per value-merge arm and its only work is `Set.contains` on the zero-capacity shared empty, which returns before it hashes anything, and the corpus declares no array `let` so nothing is ever marked — the same pay-for-use shape the optimizer's row established, extended to a second reader. NOT over-refused, verified by A/B: a merge of two MUTABLE globals still binds and still SHARES (new spec `top-level-array-merge-of-mutable-globals-shares`, P grows to 3 and Q stays 2), a `let` binding of a merge that includes an immutable global is still legal, owned array-literal merges still work, and String `var` globals still merge and COPY on promotion. STILL OPEN and reported to the coordinator, not fixed here: the guard is INTRA-FUNCTION and stops at a call — `g(A)` whose callee does `var b = xs` still grows a `let` array (measured: 3), because whether a parameter may hold an immutable global's record is a property of every call site, not of the callee, so it needs the whole-program call-graph fixpoint the transitive-consume case waits on. The reference compiler does not refuse the call form either (it shares and then leaks, exit 101), so shv2 is no looser, only not yet stricter; the bound is now stated in `specs-shv2/static-variables.md` and at the refusal site rather than left to be discovered. ALSO in this pass: `ParseError.assignToImmutable` single-sourced into `assignToImmutableError(nameTok)` (the optimizer's handed-over residual — three sites, throw path only, no probe and no allocation), and `builtArrayRecord` now takes `valueType`/`range` like its two sibling arms instead of the whole `ManagedGlobal`. GATES: shv2 build exit 0 zero warnings (139 codes); x64 spec-test 1713/0 exit 0 no leak at BOTH `--workers=12` and `--workers=1` with BYTE-IDENTICAL stdout; wasm32-wasi 1518/0 exit 0; `specs-shv2/fragments` ZERO modified (three ADDED for the new tests); leak battery (String elements read/pushed cross-function/300-iteration loop/reassigned/untouched/emptied via clear and pop/element overwritten by set) all exit 0; and the rung's pinned behaviours all reproduce — 2, 42, 9, byte-string 99. | 36,020,134 | 68,162,687 | 136,575,544 | 275,082,042 | 560,653,977 | 1,151,286,283 |
| 2026-07-26 | P1.7 slice 3 — DEAD-GLOBAL ELIMINATION. A top-level binding nothing reads or writes no longer reaches the binary: its `.data` slot, the `__module_init` ops that build and store its record, and the `__maxon_global_cleanup` drop that releases it are all produced from ONE filtered arena, so the three go together by construction rather than by three edits in sequence. MEASURED acceptance: `var UNUSED = [1,2,3]` in a program that never mentions it went 3,676 -> 1,512 emitted code bytes, which is EXACTLY the floor for the same program with no global at all; a dead `var`/`let` String and a dead byte-string likewise land on 1,512; and the guard held — a write-only `SINK.push(7)` global stayed at 3,680, unmoved. THE MOVEMENT IN THIS ROW IS THE PASS'S WORKING SET AND NOTHING ELSE, established by A/B against a base binary built from f0308a29d and RUN FROM THIS SAME WORKTREE PATH (path length shifts the byte totals by a constant +1,428, so a cross-path comparison would have mis-attributed it): (1) +8 bytes in `phase:load`, CONSTANT at all six rungs — the new `ProgramSignatures.storedGlobalCount` field on the `SignatureMemo.empty()` placeholder, one 8-byte field crossing an allocator size class, once per compile; (2) +22/+28/+34/+40/+46/+52 allocations and +3,224/+6,416/+12,680/+25,088/+49,784/+99,056 bytes in the UNATTRIBUTED bucket (`liveGlobalLabels` runs in `compileToCodeResult` between phase probes) — the `LiveGlobalLabelSet` itself, one entry per LIVE top-level global. The corpus DOUBLES its top-level `var` knob per rung (32 -> 1,024 globals), and the bytes double with it at x2.00: this is LINEAR in globals declared, which is the pass's irreducible working set, and it is 0.0003% of allocations and 0.0086% of bytes at rung 5. The +6 allocations per DOUBLING are the Set's log2(K) rehash steps, not per-op work — and the direct evidence for that is that EVERY PER-PHASE ALLOCATION COUNT IS BIT-IDENTICAL base vs head at all six rungs, so the O(ops) label walk allocates nothing per op. The walk is also SKIPPED ENTIRELY when the program declares no stored global (`storedGlobalCount == 0`), which is most of the spec corpus — a second unconditional full-module walk would have been a per-compile TIME cost allocating nothing, i.e. exactly the shape this instrument is blind to. NOT PRE-SIZED, with a reason: `stdlib/Set.maxon` has no `reserve`, the exact bound is known (`storedGlobalCount`), and adding one is a stdlib change outside this rung's file list — reported to the coordinator rather than taken. CODEGEN ON THE LADDER IS UNMOVED: `codeBytes` is bit-identical at all six rungs (63,349 / 117,262 / 225,250 / 441,270 / 873,434 / 1,738,402), because every corpus global is both read and written and so every one of them is live. GATES: shv2 build exit 0 zero warnings; x64 spec-test 1720/0 exit 0 no leak at `--workers=12` AND `--workers=1` with BYTE-IDENTICAL stdout; wasm32-wasi 1521/0 exit 0; `verify-warm-rebuild` PASS on determinism + cache + invalidation; ONE fragment modified (`static-variables/top-level-array-grow-reassign-untouched`, whose program deliberately declares a never-touched array global — its slot and init run are now correctly absent, prose updated) and the rest ADDED. | 36,022,950 | 68,168,695 | 136,587,816 | 275,106,722 | 560,703,353 | 1,151,384,931 |
| 2026-07-26 | P1.7 slice 3 OPTIMIZER — NO CODE CHANGE, and that is the finding. Every number in this row is bit-identical to the implementer's (allocsDelta 0 and bytesDelta 0 at all six rungs) because nothing was changed; this row records the VERIFICATION and the two axes the ladder structurally cannot reach. THE IMPLEMENTER'S ROW REPRODUCES EXACTLY, re-derived independently against a base binary built from f0308a29d in its OWN worktree and run from an EQUAL-LENGTH path (.../worktrees/base-globals vs .../worktrees/dead-globals, both 54 chars): +22/+28/+34/+40/+46/+52 allocations and +3,224/+6,416/+12,680/+25,088/+49,784/+99,056 bytes, +8 bytes in phase:load CONSTANT at all six rungs, and EVERY per-phase allocation count bit-identical base vs head at all six rungs. The path-length phantom is real and was controlled rather than assumed: the base binary reads bytesDelta -408 CONSTANT at all six rungs against this log's last row (produced from the dead-globals path) while head reads 0 — a perfectly reproducible constant that looks exactly like a code change and is not one. THE LADDER CANNOT SEPARATE OPS FROM GLOBALS, so both axes were measured on purpose-built corpora with --metrics. (1) MANY OPS, ONE GLOBAL — 64 to 2,048 chained functions, a 32x op span, with one live global so the walk actually RUNS: unattributed allocations FLAT at 707 across the whole span (689 with the walk short-circuited). The O(ops) label walk allocates EXACTLY NOTHING PER OP, measured with it running, which is a stronger statement than the ladder can make because the ladder never varies ops against a fixed global count. Its time is 0.04 -> 0.67 ms across 32x — linear, and 0.08% of the 825 ms compile. (2) MANY GLOBALS, FEW OPS — 32 to 16,384 DEAD globals: unattributed allocations FLAT at 689, time 0.35 -> 2.55 ms, linear; the dead path costs nothing beyond the two arena walks. (3) THE DIAGONAL, ops AND globals together, 4,096 -> 16,384 LIVE globals with one reader per 8: unattributed x2.05, x2.05 and total x1.98, x1.99 — THERE IS NO O(ops x globals) TERM. LiveGlobalLabelSet's mechanism is nailed rather than inferred: +6 allocations per DOUBLING is 6 per Set.grow (three arrays, two allocations each) over log2(K) grows, while BYTES are linear at ~96/global because the grow at step j allocates a table of size 2^j — log allocations and linear bytes are ONE mechanism, and it is the whole of the slice's added memory; nothing else in it allocates. contributesStorage is O(1) and re-derives no liveness (2 x 16,384 calls at 689 allocations flat). UNION BOXING, the tenth check: the slice introduces no new union; MaxonOp is matched BY VALUE once per op and costs zero allocations across the 32x op span, and TopLevelConstant likewise — no shared payload-free box was attempted. SET.RESERVE PRICED AND DECLINED: it would turn 6*log2(K) allocations into 6 and roughly halve the set's bytes (a doubling chain sums to 2x the final table, one exact allocation is 1x), which at rung 5 is ~50 KB of 1.15 GB = 0.004%, and on the real spec corpus is ZERO — with 0 stored globals the walk never runs and with 1 the set never grows past its initial 16. It changes a CONSTANT, not a curve, in a type every compiled program and the compiler itself share. NEW MEASURED DEBT, reported to the coordinator and NOT fixed: stdlib/Array.hash is djb2 masked to 32 bits and Set/Map index with the LOW bits, so identifiers sharing a prefix and differing only in a numeric suffix cluster hard under linear probing — 77.5 probes per insert at K=16,384 against 0.53 for well-distributed keys, a 146x constant. IT IS INVISIBLE TO THIS INSTRUMENT AND VISIBLE ONLY IN TIME: same binary, same K, IDENTICAL allocation and byte counts, unattributed time x3.66 per doubling with G<n> names against x2.05 with identifier-like names, phase:signatures x2.39 against x2.00 and phase:parse x2.25 against x2.00 on the same swap. Measured linear-in-practice today — this corpus's own gv<n> knob tops out at 1,024 globals where it costs ~7,800 extra probes in total — and it is erratic (hash/capacity resonance), not superlinear. The trigger that would make it bend is machine-generated code minting >=8k prefix+numeric identifiers in one namespace. Not fixed because the fix is in stdlib/Array.hash, shared by every Set and Map in every program, and a hash change reorders every Set/Map iteration — a codegen risk out of all proportion to a cost nothing real pays. GATES on the unchanged binary: build exit 0 zero warnings; x64 spec-test 1720/0 exit 0 no leak at --workers=12 AND --workers=1 with BYTE-IDENTICAL stdout; wasm32-wasi 1521/0 exit 0; git status EMPTY after two full x64 suites and a wasm suite, so specs-shv2/fragments is clean and codegen is byte-identical; dead array global and dead String global both 1,512 emitted bytes, exactly the floor of a program with no global, and a write-only global stays unshrunk. | 36,022,950 | 68,168,695 | 136,587,816 | 275,106,722 | 560,703,353 | 1,151,384,931 |
| 2026-07-26 | STACK ARGUMENTS (>6 parameters). ALLOCATION-NEUTRAL AND FLAT: allocsDelta 0 at all six rungs, and bytes +612 CONSTANT across the whole 32x span — measured against a base binary built from ca5954754 in its own worktree, not inferred from this log's previous row. Per-phase attribution of the 612: +408 phase:load, +24 phase:parse, +180 outside any named phase, every one of them the SAME number at rung 0 and rung 5, so it is a fixed per-PROCESS cost and not proportional to ops, functions, calls, parameters or files (the most likely source is the compiler's own longer diagnostic strings: the two refusals this rung replaces were ~430 chars and the two it adds are ~800, at the same allocation COUNT, so each lands in a larger slab size class). THE PATH-LENGTH PHANTOM THE PREVIOUS ROW WARNS ABOUT WAS TESTED AND REFUTED HERE rather than assumed: running the identical binary through a junction with a 12-char worktree name (dead-globalz) instead of the real 10-char one (stack-args) reproduced -408 against this log EXACTLY, so the offset against the log row is not path length; the honest comparison is base-vs-head in the same tree, which is what the +612 above is. TIME, which this instrument cannot see: rung 5 compiled in 10602/10834 ms on base and 10654/10509 ms on head — inside noise, head faster on the second pair. That matters because the rung REPLACED the caller's running gpr/fp counters with per-argument calls to the shared placement rule (abiFileSlotIndex / abiStackSlotsBefore), which is O(args) per argument instead of O(1) — quadratic in the ARGUMENT COUNT, bounded by MaxAbiArgs=64 (so <=4096 bit tests at the worst signature the language accepts, and ~9 at the corpus's typical 3), and it is bought back on the callee side, where the per-parameter computeParamSlotIndex walk of the parameter TYPES is gone: each function now folds ONE ArgFloatMask and every param op reads a scalar. Net: no allocation moved, no curve bent, and the only number that changed is a constant. | 36,022,542 | 68,168,287 | 136,587,408 | 275,106,314 | 560,702,945 | 1,151,384,523 |
| 2026-07-26 | STACK ARGUMENTS OPTIMIZER — NO CODE CHANGE, and the reason is measured. Every number here is bit-identical to the implementer's row above. ⭐ THE ROW ABOVE'S "+612 bytes CONSTANT" IS NOT REAL; READ IT WITH THIS CORRECTION. A base binary built from ca5954754 by the SAME bootstrap, in a worktree named to the SAME LENGTH (.../worktrees/stack-base vs .../worktrees/stack-args, both 85-char exe paths), produces BIT-IDENTICAL allocations AND bytes at all six rungs — 36,022,542 / 68,168,287 / 136,587,408 / 275,106,314 / 560,702,945 / 1,151,384,523 on both sides. The rung costs nothing HERE because its new paths are DEAD on this corpus: the generated code's maximum arity is TWO, and zero functions exceed the old six-parameter cap, so THE LADDER STRUCTURALLY CANNOT EXERCISE THIS RUNG. Control that the A/B is real: the binaries differ (7,983,616 vs 8,026,112 bytes), and base REFUSES a 7-parameter program with E2015 while head compiles it and returns 28. ⭐⭐ THE PATH-LENGTH DISPUTE IS SETTLED IN FAVOUR OF THE PREVIOUS ROW. Executable path length is CAUSAL for the byte column at EXACTLY 200 BYTES PER CHARACTER, with allocation counts totally invariant. Measured two independent ways. (1) One 7-line file, one binary, four junction paths of name length 1/10/30/60: total +200/char, decomposing into phase:load 132/char + unattributed 60/char + phase:parse 8/char, which sum to exactly 200 — every coefficient an exact integer over three independent spacings, and NO OTHER PHASE MOVES AT ALL (isel, regalloc, encode, lex, merge, signatures are completely path-insensitive). Source path is 29/char (unattributed 21 + parse 4 + load 4); output path 5/char (unattributed 3 + writeExe 2). (2) On THIS ladder: the same base binary through two junctions in the same parent, names differing by exactly TWO characters, reads 36,037,142 vs 36,037,542 — a difference of EXACTLY 400 bytes, constant at every rung, allocations identical at 619,790 both. So `dead-globals`(12) vs `stack-base`(10) predicts −400 where the instrument reads −408: 98% of that −408 was path length, and the residual −8 is a real code difference from the review commit between the logged row and ca5954754. ⚠ THE REFUTATION IN THE ROW ABOVE INVERTS THE TEST. A path artifact is a FIXED NUMBER OF COPIES OF THE EXECUTABLE'S OWN PATH (~200 per process), so it is IDENTICAL AT RUNG 0 AND RUNG 5 and moves no allocation — which is precisely the signature that row cites as proof its +612 was "a fixed per-PROCESS cost" and "not proportional to ops, functions, calls, parameters or files". That signature does not discriminate between the two hypotheses: it is what BOTH look like. The discriminator is the COEFFICIENT. ⇒ STANDING RULE FOR EVERY FUTURE A/B IN THIS PROJECT: the ALLOCATION column is immune to path length and is the trustworthy instrument; for the BYTE column either equalize the WHOLE executable path character-for-character or subtract 200 × (Δchars). An "equal-length worktree NAME" is not the control, because the coefficient applies to the whole path, not the name. SUPERLINEARITY AUDIT (the mandate). The rung's shared placement rule — abiFileSlotIndex / abiArgIsOnStack / abiStackSlotsBefore in Targets/Shared/StdLoweringShared.maxon — IS Θ(N²) per call site in the ARGUMENT COUNT: each of the three walks the preceding arguments, and emitArgMovesByFloatMask calls them once per argument. NOT FIXED, because the bound is real and measured: N ≤ MaxAbiArgs = 64, enforced by a parser diagnostic, so the program-wide total is 64 × (total arguments) = LINEAR IN PROGRAM SIZE. Two purpose-built ladders, separated as the mandate asks. (A) TOTAL ARGUMENTS HELD CONSTANT at 1,024 while arity goes 8→16→32→64: phase:isel is FLAT at 1.55 / 1.48 / 1.55 / 1.79 ms — the entire quadratic term is +0.24 ms at the worst arity the language accepts, inside a 3,273 ms compile — while regalloc over the same ladder runs 9 → 865 → 2,526 → 3,251 ms, which is the BUDGETED SplitLiveRanges debt (ARCHITECTURE.md:1336) and is what a naive many-args ladder actually measures. (B) ARITY FIXED AT THE 64 CEILING while call sites go 8→16→32→64: isel ×1.85 ×1.89 ×1.96, converging on ×2.00 — LINEAR in call sites. Denominator: on the real rung-5 corpus isel is 3.07% of the compile (310.9 of 10,129 ms), behind regalloc at 38.6% and parse at 37.3%. UNION BOXING, THE ELEVENTH CHECK — CLEAN, and measured on the per-argument path rather than reasoned about. Allocations per ARGUMENT across the register→stack boundary, at 64 fixed call sites: FLAT on x64 at +5.05/arg through arity 5..10 (x64 crosses into stack slots at 7), and on arm64 they DROP from +5.05 to +4.03 at arity 9, where the new arm64StoreOutgoingArg replaces a movRegReg — the new op carries a plain integer slot instead of a second TargetVReg, so the stack path allocates one FEWER per argument than the register path it parallels. The new recordOffset field on arm64Prologue/arm64Epilogue is per-function and free: base vs head on an arity-6 arm64 cross-compile are identical in every metrics row. REPORTED, NOT FIXED (a constant factor, and unmeasurable): calleeArgFloatMask rebuilds bit-by-bit, with N ByteArray-keyed Map lookups, the very mask that IS the stored map value — one lookup would do, and it runs twice per direct call op. It allocates nothing and sits far below timing noise (~0.2% of a compile at the corpus's typical arity), so changing it here would be a change that only LOOKS like an optimization; it goes to the reviewer as redundancy instead. GATES on the unchanged binary: x64 spec-test 1736/0, exit 0, no leak; git status EMPTY after the suite, so specs-shv2/fragments is clean and codegen is byte-identical. | 36,022,542 | 68,168,287 | 136,587,408 | 275,106,314 | 560,702,945 | 1,151,384,523 |
| 2026-07-26 | PARAMETER-MUTATION ANALYSIS (E3019 call-site half). ⭐ ALL OF IT IS phase:parse, AND IT IS ONE ALLOCATION PER SOURCE-WRITTEN CALL SITE. A/B against a base binary built from b408023ff IN THIS SAME WORKTREE (so the executable path is character-identical and the previous row's 200-bytes-per-char rule does not apply to this comparison): rung0 parse 113,418 -> 113,912 (+494) and semanticCheck 1,878 -> 1,974 (+96); rung3 parse 790,037 -> 793,345 (+3,308) and semanticCheck 13,288 -> 13,414 (+126). The corpus has 559 / 3,709 call-ish sites at those rungs, so the parse term is +0.88 and +0.89 allocations per call site — the new `MaxonOp.call.argImmutableNames` column, one `ByteArrayArray` built per source call by `pushCallArg` alongside the three columns already there. It is LINEAR by construction and the ladder agrees: totals +590 / +1,000 / +1,811 / +3,434 / +6,667 / +13,116, ratios x1.69 x1.81 x1.90 x1.94 x1.97 converging on x2.00. The column is needed because the fact is SYNTACTIC: `let a = p` carries the parameter `p`'s own ValueId while being a `let`, so a per-VALUE mark would refuse `f(p)` in any function that also wrote `let a = p` (measured: the oracle refuses `f(a)` and accepts `f(p)`). `settledImmutableArgNames` substitutes a shared empty array when no argument names an immutable binding, so the transient dies instead of living as long as the module — that saves RETAINED bytes, not the allocation. ⭐⭐ THE WHOLE-PROGRAM FIXPOINT COSTS A NEAR-CONSTANT ~100 ALLOCATIONS, AND THE FIRST DRAFT DID NOT. It was written with a `Map with (ByteArray, OpIndexArray)` reverse-edge index — exactly the "Array with (a per-node array)" shape `CsrGraph.maxon`'s header forbids in as many words ("a new adjacency belongs here rather than hand-rolled at its call site"). MEASURED on a purpose-built ladder that doubles functions, call sites AND depth together — a maximal chain f0->f1->...->f(N-1) where the last pushes, so EVERY function ends up in the summary and every gain re-enqueues its caller, the worst shape a mutation fixpoint has: phase:semanticCheck allocations base / map-index / CSR were 3,215 / 4,199 / 3,357 at N=400 and 51,215 / 64,319 / 51,453 at N=6,400. The map index cost +984 -> +13,104 (LINEAR in the call graph, one array per callee); the CSR costs +142 -> +238 (six dense columns' geometric growth, i.e. logarithmic). 55x less at N=6,400, and the drain loop now does ZERO hashing because a callee is a function INDEX in the CSR, not a name. The published summary went the same way: a dense MutatedParamMaskArray by function index plus the ONE name->index map the edge walk already needs, instead of a second name-keyed map of masks built by N upserts. TIME on that same ladder, base vs CSR head: 1.3/2.7/5.7/12.3/25.8 ms vs 2.7/6.2/12.4/27.3/59.8 ms — head/base is a CONSTANT 2.08 2.30 2.18 2.22 2.32, i.e. the SAME growth exponent as the base pass (both ~x2.1 per doubling), so the fixpoint is linear in the call graph and what remains is a constant factor: the edge walk visits every op of every function, a second module walk that a whole-program summary cannot avoid (it must be COMPLETE before the first call site is checked). On the real corpus semanticCheck is 53,128 of 16,239,569 allocations at rung 5 (0.33%), so that constant is invisible there. NOT FIXED, REPORTED: the per-call `argImmutableNames` array could be eliminated entirely by accumulating into a per-parser FLAT stack with a mark per call — the `Scope` frame idiom this compiler already uses — copying a slice out only for the calls that actually carry an immutable-binding argument. It is worth ~+13,000 allocations at rung 5 (0.08% of the compile) against a nesting-correctness requirement in `pushCallArg`, the one writer of the call's parallel columns; measured and left for the optimizer rather than taken here. BYTES: +126,448 / +234,448 / +424,152 / +834,856 / +1,666,739 / +3,326,755, ratios x1.85 x1.81 x1.97 x2.00 x2.00 — linear, same term. Against the PREVIOUS logged row (worktree `stack-args`, 10 chars, vs `param-mutation`, 14) the standing 200-bytes-per-character rule adds a constant +800 to every rung of that comparison, which is 0.7% of the smallest delta and none of the shape. GATES: x64 spec-test 1769/0 exit 0 no leak; wasm32-wasi 1569/0 exit 0; --workers=1 and --workers=12 byte-identical; specs-shv2/fragments additions only, zero M on any pre-existing golden. | 36,148,990 | 68,402,735 | 137,011,560 | 275,941,170 | 562,369,684 | 1,154,711,278 |
| 2026-07-26 | OPTIMIZATION PASS over PARAMETER-MUTATION. ⭐ MEMORY IS BIT-IDENTICAL ON ALL SIX RUNGS (allocs delta 0, bytes delta 0) — the one change is a pure-TIME fix, which is the class scale-test is structurally blind to and which was found by READING plus a ladder. ⭐⭐ WHAT WAS FOUND: `Project.argSlotPosition` resolved a labelled argument by `FuncSignature.indexOfParam`, a SCAN of the whole parameter-name list. Maxon spells calls `f(a, b: b, c: c)` — labels in declaration order — so that scan cost 1+2+...+P comparisons, i.e. O(P^2) PER CALL, and this rung made the program ask for that mapping THREE times per call (slotCallArgs, addMutationEdges, checkImmutableArgToMutatingParam) where base asked once. Testing `paramNames[argIndex]` first collapses the common case to O(P) per call; the reordered case falls through to the same scan after one extra comparison. It is EXACTLY equivalent, not an approximation: a repeated parameter name is E2014 at the declaration (measured), so a hit at `argIndex` means `indexOfParam` has no earlier match to find. MEASURED on the new, COMMITTED `Testing/ladders/genmutchain.sh 1 256 <P>` (one chain, depth 256, every argument labelled in declaration order), phase:semanticCheck: P=1..64 was 1.207/1.532/2.353/4.172/8.659/22.887/69.820 ms and is now 1.195/1.493/2.099/3.357/5.802/10.869/20.524 ms. Growth per DOUBLING of P falls x3.05 -> x1.89 while the program itself doubles at x2.00, so the quadratic TERM is gone rather than merely smaller — and 20.5 ms lands BELOW the pre-rung base's 39.6 ms, which had the same scan and paid it once instead of twice. ⚠ ON THE REAL CORPUS IT IS WORTH ~1.5%: rung3/4/5 semanticCheck 16.90/34.51/69.74 ms -> 16.55/33.95/68.72 ms, because the corpus's functions have FEW parameters. The ladder is pathological on purpose and the corpus is what a real program looks like; both are reported per `Testing/ladders/README.md`. It is kept because it removes a growth term for six lines in ONE shared function with no new allocation, and because the trend is toward wider signatures — THIS RUNG widened `validateCall` and `checkSlottedCall` to 12 parameters each. ⭐ THE FIXPOINT ITSELF WAS LADDERED ON THREE SEPARATE AXES and is LINEAR. Axis A, DEPTH AT CONSTANT PROGRAM SIZE (chains x depth = 2048, so a 1024x change in depth at ~constant source bytes) — the decisive experiment the implementer's ladder could not run because it doubled functions, call sites and depth together: semanticCheck allocations 17,602 -> 16,589 (FLAT, drifting DOWN as fewer chain heads mean fewer main call sites) and time 10.2 -> 13.4 ms NON-MONOTONICALLY (20.3 at depth 512, 17.4 at 1024, 13.4 at 2048), i.e. noise, not a depth term. Axis B, SIZE at constant depth: 16.8x source -> 15.5x allocations, 19.2x time, per-doubling 2.08 2.11 2.03 2.16. Both linear. The worklist's `queued` flag is what earns this — a caller is re-enqueued only on a real MASK CHANGE and duplicate enqueues COALESCE, so the theoretical `MaxAbiArgs`=64 re-entry factor needs bits to arrive in separate rounds and the seed-first drain order prevents that. MEASURED DEBT (linear today, bounded by construction): the fixpoint is O(V + E x min(K,64)) worst case where K is a callee's parameter count; K cannot exceed `MaxAbiArgs`=64 (`requireAbiParamCount`), so this is a bounded constant that cannot bend with program size, and axis D shows the coalescing collapses it in practice. REPRODUCED BOTH ENDS of the previous row's headline against a base binary rebuilt from b408023ff at the SAME canonical path: axis C semanticCheck allocations base 3,215 / head 3,357 at N=400 and base 51,215 / head 51,453 at N=6,400 — the logged +142 and +238 CSR overhead confirmed exactly, and head/base time a constant 2.13 2.85 2.45 2.12 1.99 rather than a growing one. NOT TAKEN, WITH REASONS: (1) the previous row's own hand-off — replacing the per-call `argImmutableNames` transient with a flat stack + per-call mark — is worth ~13,000 allocations at rung 5 (0.08%) but requires a mark/truncate discipline in `pushCallArg` that must survive the parser's `throws ParseError` unwind; a leaked stack frame would mis-blame every subsequent call, which is a correctness hazard priced at 0.08% and declined. (2) Gating `checkImmutableArgToMutatingParam` on the O(1) `argImmutableNames.isEmpty()` before the `maskOf` hash was measured and NOT shipped: semanticCheck is 68.7 ms of a 10,143 ms rung-5 compile (0.68%), so a constant factor inside it is unobservable and shipping it would only look like an optimization. TARGETS CLEARED BY READING: `noteReceiverWrite` returns on `not mutatesReceiver` before any work and `immutableReceiverNameOf` returns a shared empty or a zero-copy token slice, so the parser seed costs nothing per non-writing method call; no union was introduced or widened beyond `MaxonOp`'s three call cases gaining one pointer column, and every downstream reader discards it with `_` rather than copying it. GATES: x64 spec-test 1769/0 exit 0 no leak; wasm32-wasi 1569/0 exit 0; --workers=1 and --workers=12 byte-identical (md5 d99c4330ea8dd5724ee5820e0cd4b92d); specs-shv2/fragments zero M after BOTH suites; labelled-call probes 7-params-in-order=28, 7-params-fully-reversed=42, unknown label E3037, duplicate label E3038, transitive var=9, transitive let=E3019. | 36,148,990 | 68,402,735 | 137,011,560 | 275,941,170 | 562,369,684 | 1,154,711,278 |
| 2026-07-26 | THE CPU-TIME COLUMN LANDS, and this row is its first datapoint: there is no previous CPU row to difference against, so the ## CPU table starts here. scale-test measured MEMORY ONLY and declared that blind spot on every run; a cost that re-walks tokens or chases pointers but allocates little read FLAT however quadratic it was. Mechanism: one bootstrap intrinsic __Builtins.threadCpuTicks() (QueryThreadCycleTime on Windows, clock_gettime(CLOCK_THREAD_CPUTIME_ID) on POSIX) bracketed by PhaseProbe INNERMOST, displacing the wall clock, because the tightest bracket belongs to the number people act on. WALL time is still never collected and that argument is unchanged: it counts every OTHER process, and one run once read phase:parse at x5.03 then x1.78 across a DOUBLING ladder, which is preemption and not a curve. CPU time cannot see preemption at all (verified: across a 300 ms sleep it advanced 837,520 ticks while wall advanced 301,000,000 ns). NOISE FLOOR, MEASURED not assumed, over three idle runs plus one against a machine loaded to every core: phases above 50M ticks read median 1.7% / worst 6.3%; small phases (~100K ticks) swing up to 31% and no ratio should be read off one; a FULLY LOADED box inflates every rung by +49%..+52% because this counter ignores PREEMPTION but not CONTENTION - yet it inflates them UNIFORMLY, so the rung-over-rung RATIO survived nearly intact (loaded x1.90/x1.98 against idle x1.86/x2.00). Trend ratios; treat absolutes as a fact about one machine on one day. --repeat=<n> (default 3) compiles each rung n times and keeps the MINIMUM CPU per phase, since noise can only ADD; the repeats also CROSS-CHECK that allocs/frees/bytes are IDENTICAL every time, a compiler-determinism gate the one-shot runner structurally could not make, and a difference is a BROKEN RUN rather than a number to average away. WHY THIS ROW MOVED: the previous row is the parameter-mutation optimization pass, logged at essentially origin's tip (its gates read 1769/0 against this row's 1774/0, a handful of tests apart), so these deltas are THIS change rather than a sum over an unlogged gap. (An earlier draft of this note claimed it spanned 23 unlogged upstream commits; that was wrong - upstream logged its row.) The shape is the load-bearing part: the instrument now costs a FLAT +200 allocations at every rung (it was +546/+716/+1056/+1736, GROWING, until regAllocAccumulated stopped routing through costOf). That mattered because returning a struct BY VALUE ALLOCATES - measured at exactly one allocation per return and INDEPENDENT of field count, 4/5/6 fields all costing 10,000 over 10,000 returns - and that function runs once per FUNCTION, so the instrument was moving the very column the log calls bit-for-bit reproducible. A constant offset cannot bend a curve; a growing one can. ALSO FOUND, unfixed: Array.resize is BROKEN for STRUCT element types, setting the length without giving the slots storage, so count() answers N while get(0) reports OOB - ScaleTestRunner.parseRungMetrics has carried that call for its whole life and survives only because it writes every slot before reading one. Gates: C# 3103/0, shv2 1774/0, no fragment churn, no exit 101. | 36,176,875 | 68,441,820 | 137,073,045 | 276,047,455 | 562,565,569 | 1,155,086,363 |
| 2026-07-26 | TWO TIME-ONLY SUPERLINEARITIES FOUND BY THE NEW CPU COLUMN AND FIXED, on its first ladder. Both allocate nothing, so every row above this one is blind to them: this is the column earning its place. (1) THE ENCODER MAP WAS QUADRATIC IN BLOCKS PER FUNCTION. X64Backend.emitFunctionChunk keyed a per-function block-offset Map by block id; Map is open-addressed with linear probing and int.hash() is the IDENTITY (stdlib/PrimitiveExtensions.maxon), so a key lands at id and (capacity-1). A function block ids are TWO CONTIGUOUS RUNS - its original blocks, then the blocks regalloc:criticalEdges appended - and layout order INTERLEAVES them (measured on a real 3,073-block function: 0 1 2049 2 3 2050 4 5 2051 ...). The high run WRAPS onto the low run slots, the two become one solid cluster, and probes per insert measured EXACTLY capacity/4 + 2/3 - collapsing to 1.00 the instant capacity first exceeds the id range. Capacity is proportional to blocks, so probes per insert is proportional to blocks: O(B^2) per function, 171.44 probes/insert against 1.00 for the same keys sorted. The key IS the index, so the Map is now two pre-sized arrays: O(B). Corpus phase:encode CPU ratios x1.61 x1.93 x2.22 x3.69 x2.58 -> x1.68 x1.90 x1.99 x2.03 x2.04, compounding 65.8 -> 26.3 over five doublings where 32 is linear - superlinear to SUB-linear. Rung 5 encode 1,744,427,240 -> 503,060,720 ticks; encode falls from 4.62% to 1.65% of the whole compile. MEMORY IS NEGATIVE, which is why the columns below FALL: -23,848 allocations and -10,666,765 bytes at rung 5 (-32% of encode, -33% at every rung), and the whole-compile deltas equal those two numbers TO THE DIGIT, so nothing outside encode moved. Codegen byte-identical, proven not inferred: 6 programs on x64-windows plus 4 programs on arm64-macos, arm64-linux and x64-linux, all hashed identical before and after. Ladder committed: Testing/ladders/genemit.sh, three independent knobs; ladders A (function count) and B (ops per function) were ALREADY FLAT at x2.00, and that is what located the term. (2) THE BLOCK-EXTENT SCAN NOW STEPS OVER A NESTED CONSTRUCT. Parser.blockStatementEndIndex walked every token in a construct extent to find its closing end, once per if/while/match, so a nested construct was re-walked by every enclosing one: O(depth x tokens), allocation-free. A lazily-built per-file extent column answers where the block opened here closes in one array read. Ladder committed: Testing/ladders/gennest.sh, which holds depth x statements FIXED so program size barely moves while nesting doubles - the only way to tell a depth term from a size term. Measured at constant size, 1024 statements in every program: parse CPU 23.6M / 28.2M / 37.4M / 56.3M / 98.2M at depth 4..64 (x1.20 x1.33 x1.50 x1.74, climbing toward x2.00) with allocations only x1.49 over a 16x depth change. After: the d4->d64 slope falls +68.8M -> +34.6M ticks, HALF the depth term. It costs +2 allocations per FILE and +8 bytes per TOKEN for the column (+0.55% of parse bytes at rung 5) - a linear, explainable step, and the reason the byte column does not fall as far as the allocation column. It also gave the block-CLOSER classification ONE home; it had been written out independently at eight sites. ⚠ TWO CORRECTIONS TO THE RECORD, both measured. FIRST: phase:parse superlinearity ON THIS CORPUS IS NOT NESTING DEPTH. Halving the depth term left the corpus parse ratios unchanged (x1.91 x2.14 x2.39 x2.75 x3.03 against a baseline x1.94 x2.16 x2.43 x2.74 x2.99). The depth term is real and gennest.sh proves it at constant size, but this corpus barely exercises it; the corpus parse curve is Theta(mutable bindings x constructs) - d_blocks.maxon is ONE function with 518 mutable vars in scope, and both callers ask a membership question once per binding per construct: 414,400 tests at rung 2 and 26.5 MILLION at rung 5. That is FILED, not fixed: the cure is carried sets computed bottom-up over the construct tree, which changes phi ORDERING that the goldens pin, so it is a rung and not an edit. SECOND: the first attempt at the parse fix was a per-file name->assignment-sites index with binary search. It was implemented exactly, gated GREEN, and its ladder went flat - and then measured x3.45 on the real corpus at rung 5, because it replaced a small per-construct set (one hash and one probe per test) with a structure whose per-test cost multiplied that same Theta(V x C). It was BACKED OUT. A ladder-flat, corpus-worse change is a regression, and only running both caught it. ⚠ THE MAP TRAP IS A CLASS, NOT AN INSTANCE: identity hash plus linear probing is quadratic for ANY interleaved union of contiguous int runs. SemanticCheck.buildBlockByIdMap is the identical pattern and is cold only because Maxon-tier ids have no split run - one inliner away from hot. A hash mixer in stdlib/Map.maxon would kill the whole class in three lines and was deliberately NOT done here: it changes every Map iteration order, so any pass emitting in Map order could move bytes, risking the C# 3103 suite and every golden. Gates: C# 3103/0, shv2 1774/0 at --workers=1 and =12 byte-identical, zero fragment churn, zero build warnings, no exit 101. | 36,004,522 | 68,134,924 | 136,500,045 | 274,944,764 | 560,406,416 | 1,150,816,806 |
| 2026-07-26 | Array.resize refused for a managed element type (E3106) + Array.get distinguishes emptySlot from indexOutOfBounds. shv2's gate is a parse-time check on the resize arm; the bootstrap's is a pre-monomorphization SemanticCheckPass walk over MaxonCallOp. Neither runs on the scale corpus's own hot path, so this run is a NEUTRALITY check, not a target. | 36,004,522 | 68,134,924 | 136,500,045 | 274,944,764 | 560,406,416 | 1,150,816,806 |
| 2026-07-26 | THE P1.5-FINISH OPTIMIZATION PASS, and the headline is NOT this rung's code — it is a QUADRATIC IN `Map` ITSELF that the rung's own instrument row had already flagged as a curve nobody had explained. phase:encode goes x3.02 -> x1.98 (rung 5: 231,100,750 -> 81,443,584 CPU ticks, -65%) and phase:link x1.79 -> x2.00, with the ALLOCATION AND BYTE COLUMNS BIT-IDENTICAL AT EVERY RUNG (checked by diff, not by eye) - because the fix changes only which SLOT a key probes, never what is allocated. WHAT IT WAS: `Map`/`Set` are power-of-two open-addressed tables with LINEAR probing, and `int.hash()` is the IDENTITY (`self and 0xFFFFFFFF`), so the slot is the key's low bits and structure in the keys became structure in the table. The victim was the per-function block-offset table in BOTH backends (`Arm64BlockOffsetMap` / `ChunkLabelOffsetMap`), keyed by BlockId. Those ids are PERFECTLY DENSE - a run 0..N-1, verified, gap histogram is a single entry (1, 6144) - but they arrive in LAYOUT order, which interleaves the parser's blocks with the ones minted later (critical-edge splits): 0, 1, 4097, 2, 3, 4098, ... At every capacity below the largest id the two runs alias onto the same slots, interleaved insertion grows ONE primary cluster, and each doubling rehashes and rebuilds it. Isolated in a bootstrap-compiled microbenchmark with the SAME key set, count and density and only the ORDER changed: 24,576 ids cost 346.6 ms in arrival order against 2.3 ms in increasing order (x3.8 x3.9 x3.7 x4.0 per doubling - a quadratic, 150x at that size). FIX: `spreadHash` in stdlib/Interfaces.maxon (MurmurHash3 fmix32) applied at the SIX places Map and Set turn a hash into a slot index - one door, so the two containers cannot come to disagree. It deliberately does NOT change `hash()` itself and is NOT what the `hashes` arrays cache; those still hold the key's true hash for the skip-the-equals guard. HOW IT WAS FOUND, which is the part worth copying: the memory columns read x1.96 for encode the whole time and could never have shown this. The CPU column showed the curve; a hand ladder (maxon-shv2/Testing/ladders/genblocks.sh, committed) separated blocks-per-FUNCTION from functions-per-module - encode read x3.21 x3.37 x3.69 on `blocks` against x2.0 flat on BOTH controls (`funcs`, `straight`) - and a bisect by disabling one map operation at a time pinned it to the upsert rather than the patching or the lookups. THIS RUNG'S OWN COST, A/B'd on this host base-vs-head, is a FLAT +1 allocation / +16 bytes at every rung in phase:parse and nothing else anywhere: the retagged-cast mint (`emitRetaggedCastValue`) firing at the single `as ScaleCoverNarrow` site in the fixed-size h_cover.maxon. CORRECTING THE RECORD: that flatness is the CORPUS, not the cost. Measured on probes of 1/10/100 cast sites, `castChangesTheShiftFill`'s extra `signatures.aliasOf` costs +1 allocation PER `as <declared-alias>` SITE (+11 at 10 sites, +101 at 100), and a MINTING cast costs ~+8.9 parse and ~+28 total allocations per site. It reads flat here only because the whole six-rung corpus contains exactly ONE named-alias cast and it sits in a file that does not double. CLOSURES ARE INVISIBLE TO THIS LADDER ENTIRELY - ScaleCorpus lists them under NOT GENERATED - so the capture half of the rung was measured on a second committed ladder (genclosure.sh): the path is LINEAR in program size (x1.98 x1.99 x1.99 parse allocations over 128..1024 closures), `fieldStorageType` is O(1) per capture READ SITE, and `captureSlotFor`'s O(reads x captures) scan is linear-in-practice - the language caps a function at 64 argument slots (E2015), so the scan is bounded at ~2,048 comparisons per closure and K=32 -> K=64 reads x1.41. Re-measure it if a closure can ever capture hundreds of distinct LOCAL bindings. STILL OPEN, unchanged by this row: phase:parse remains x3.26, the loop-phi token pre-scan (Parser.loopBodyEndIndex + namesAssignedIn, O(depth x tokens)) that commit 6ee12ff37 attributed - it is the corpus's own documented prediction and is NOT this pass's. Gates: shv2 1593/0 arm64-macos and 1588/0 wasm32-wasi, --workers=1 stdout byte-identical to --workers=12 AND to the pre-change baseline (sha 0d4993c64e), specs-shv2/fragments EMPTY after a full run (byte-identical codegen, which is what proves a hash change moved no emitted IR), C# bootstrap 3102/0 with its arm64 fragment churn proven pre-existing (byte-identical diff with and without the change), no exit 101. | 37,634,221 | 70,455,204 | 140,310,505 | 284,999,232 | 579,041,311 | 1,176,695,817 |
| 2026-07-26 | REBASE RE-MEASUREMENT of the P1.5-finish optimization pass. The row above it was measured against a base that PREDATES upstream a78d17c35, and its numbers are therefore stale — this row is what is true with both fixes present. Post-rebase the curves hold: phase:encode x1.99, phase:link x1.98, every phase ~x2.00. The allocation deltas against that row are NEGATIVE at every rung (-1,472 -> -18,361 allocations, -163,098 -> -4,188,382 bytes) and that is upstream's work, not a regression: a78d17c35 replaced the x64 per-function block-offset Map with two pre-sized arrays, so those allocations no longer happen at all. THE TWO FIXES ARE COMPLEMENTARY AND NEITHER IS REDUNDANT, which is the point worth recording. Upstream fixed the INSTANCE it could see from a Windows host (X64Backend.emitFunctionChunk; 'the key IS the index', so the Map became arrays) and its own row states in as many words that it DELIBERATELY DECLINED the class fix: 'A hash mixer in stdlib/Map.maxon would kill the whole class in three lines and was deliberately NOT done here: it changes every Map iteration order, so any pass emitting in Map order could move bytes, risking the C# 3103 suite and every golden.' This branch took that class fix (spreadHash, MurmurHash3 fmix32, at the six slot derivations in Map/Set) and MEASURED the risk it was declined on, rather than reasoning about it. The risk is zero, empirically: specs-shv2/fragments EMPTY after a full run on two targets, the C# bootstrap's fragment diff BYTE-IDENTICAL with and without the change (sha aef478e2b6, A/B'd against an unmodified checkout), wasm 0 failures, verify-warm-rebuild PASS, and an independent sweep of all 16 Map/Set iteration sites in the compiler found exactly one user-visible consequence — the ORDER of diagnostics for a program with several undeclared field types, which was already map order and not source order, and which TypeResolution.maxon:133 documents as such. ⚠ AND ON THIS HOST THE MIXER IS THE ONE DOING THE WORK: scale-test's compileRung spawns build with no --target, so on arm64-macos it is Arm64BlockOffsetMap that carries the block-offset table, and upstream's array fix is x64-only. An x64 host would have measured the reverse. Neither fix subsumes the other; the class fix additionally covers SemanticCheck.buildBlockByIdMap, which upstream's own row flags as 'the identical pattern and cold only because Maxon-tier ids have no split run - one inliner away from hot'. Gates on the rebased tree: shv2 1591/0 arm64-macos, 1586/0 wasm32-wasi, --workers=1 byte-identical to --workers=12, specs-shv2/fragments clean after a full run, C# bootstrap 3104/0, no exit 101. | 37,471,123 | 70,159,883 | 139,753,720 | 283,922,036 | 576,926,213 | 1,172,507,435 |
| 2026-07-27 | OPTIMIZATION PASS over the P1.7 instance-name-space rung. NO COMPILER CODE WAS CHANGED, and that is the result. WHAT MOVED AND WHY: -15 -27 -51 -99 -195 -387 allocations and -240 -432 -816 -1584 -3120 -6192 bytes, i.e. EXACTLY -1 allocation and -16 bytes PER INTERNED GENERIC INSTANCE. This corpus's instance count is 3 + 12x2^rung (3 fixed Array instances plus a doubling generics knob) and the deltas match it digit for digit; ratios x1.80 x1.89 x1.94 x1.97 x1.98 converge on x2.00, which is what 'linear in instance count' means. The allocation that stopped happening is the CompiledNameClaimant boxed union in ParseStaging.maxon, which checkTypeSymbolNamespace minted once per instance even for its payload-free 'unclaimed' case; the rung deleted the union and the arm chain with it, leaving a bare claimed.get(compiled). A/B'd base 42ea9a054 vs head IN THIS ONE WORKTREE on this Windows host, the two binaries named to equal length so phase:load's filename-length term cancels. Independently reproduced here, NOT carried over from the implementer's own numbers. THE DELTA COLUMN OF THIS ROW IS MEANINGLESS AND SHOULD NOT BE READ: the row above it was measured on an arm64-macos host. THE HUNT'S PRIMARY TARGET was reservedIfDeclared, a LOOP over a name space run once per generic instance inside the compiler's name mangler. It performs AT MOST TWO PROBES on any program that compiles, and a probe is two Map.contains (containsStruct / containsEnum, SignatureIndex.maxon:3509 and :4585 - hash lookups, so the derivation is NOT O(instances x declared types)). A THIRD probe requires a declaration whose own name starts with __, which is E2051, so it exists only on programs already refused. Measured on a new committed ladder, maxon-shv2/Testing/ladders/geninstances.sh, at 64..2048 instances: phase:signatures allocations are IDENTICAL base and head at every rung (3089 5396 9943 18970 36964 72882, x1.97) - the added probe allocates NOTHING on the uncontested path - and signatures CPU is +0.7%..+1.3%, flat with N and inside the noise band. THE CONTEST PATH costs +4 allocations and +198 bytes per contested instance, measured as contested minus a BYTE-IDENTICAL control (type Zed_A<i> in place of type Box_A<i>, so the extra declarations' own sweep cost cancels): 256 512 1024 2048 4096 8192 allocations across the 32x span, i.e. constant per instance, x1.98 both columns. DELIBERATELY NOT OPTIMIZED - it is a constant factor on a path bounded by the declared-type count, and constant factors are not the mandate; for the record it is one String interpolation and a direct ByteArray reserve+append would make it 1 allocation. THE ADVERSARY - the shape built to make the loop iterate k times, k declarations each two characters longer than the last - reads -1 0 +4 +14 +64 ms against its byte-identical control over 40 KB to 8.5 MB of source, about 7e-6 ms per source byte and FLAT. It is LINEAR EVEN THERE, and structurally so: probe j costs a declaration 2j characters long, so k probes require Theta(k^2) of SOURCE to buy Theta(k^2) of work. CORRECTION TO THE RECORD: this corpus DOES generate generics and interfaces (its own manifest lists both), so the standing instruction that it contains none is stale - and it is precisely what makes the per-instance delta above readable here at all. UNCHANGED AND NOT THIS RUNG'S: phase:parse reads x3.26 on base and head alike, the already-filed Theta(mutable bindings x constructs) debt. Gates on this tree: shv2 1785/0, specs-shv2/fragments clean, no exit 101. | 36,000,081 | 68,132,611 | 136,501,988 | 274,955,219 | 560,433,895 | 1,150,878,333 |
| 2026-07-27 | Map probe trap #123, the last two BlockId-keyed Maps: SemanticCheck.buildBlockByIdMap and StdToWasm.BlockDispatchMap are now ONE dense BlockKeyedTable (IrModule.blockIndexById), which BlockOffsetTable is also an instantiation of. THE LADDER SEES ONLY THE THIRD: its corpus has no async and always builds for the host, so the two converted sites read a true Delta 0 here — measured elsewhere (semanticCheck -1,536 allocs / -92% bytes on Testing/ladders/genawait.sh 24 400; wasm encode -6,864 allocs on the rung3 corpus). What DID move here is bytes:encode, -2,328 .. -42,008, exactly 131 + 160*2^k CodeOffset slots per rung: forBlockCount now reserve()s the value column (exact ask) where it used to growFilled() it (which floors at Array.MinimumCapacity = 4), so every chunk of under 4 blocks stops rounding its offset column up to 4. Allocation counts are unchanged in every phase; the CPU column moved only inside its noise band. | 37,468,555 | 70,156,035 | 139,747,312 | 283,910,508 | 576,904,445 | 1,172,465,187 |
| 2026-07-26 | Map probe trap #123, the last two BlockId-keyed Maps: SemanticCheck.buildBlockByIdMap and StdToWasm.BlockDispatchMap are now ONE dense BlockKeyedTable (IrModule.blockIndexById), which BlockOffsetTable is also an instantiation of. THE LADDER SEES ONLY THE THIRD: its corpus has no async and always builds for the host, so the two converted sites read a true Delta 0 here — measured elsewhere (semanticCheck -1,536 allocs / -92% bytes on Testing/ladders/genawait.sh 24 400; wasm encode -6,864 allocs on the rung3 corpus). What DID move here is bytes:encode, -2,328 .. -42,008, exactly 131 + 160*2^k CodeOffset slots per rung: forBlockCount now reserve()s the value column (exact ask) where it used to growFilled() it (which floors at Array.MinimumCapacity = 4), so every chunk of under 4 blocks stops rounding its offset column up to 4. Allocation counts are unchanged in every phase; the CPU column moved only inside its noise band. | 37,468,555 | 70,156,035 | 139,747,312 | 283,910,508 | 576,904,445 | 1,172,465,187 |
| 2026-07-27 | OPTIMIZATION PASS over the witness-label injectivity rung (f64e4d0a5). NO COMPILER CODE WAS CHANGED, and that is the result. A/B base 858d8dbef vs head f64e4d0a5, both REBUILT AND RE-RUN IN THIS WORKTREE: allocations and bytes are bit-identical at all six rungs (618,838 / 36,000,693 through 16,220,206 / 1,150,878,945), which confirms the implementer's claim to the digit. BUT THAT ZERO IS A CORPUS BLIND SPOT, NOT EVIDENCE: ScaleCorpus.maxon:554 states this ladder generates no 'where' clause anywhere, so it mints NO witness table and never calls witnessTableLabel or ensureWitnessTable at all. The rung was therefore measured on a purpose-built witness ladder instead (scratch, not committed). Shape A holds ONE (conformer, interface) pair and doubles the DISPATCH SITES 256..4096 - the label is minted per SITE, BEFORE the memo check at LowerMaxonToStd.maxon:912, so this is the per-call cost - and shape B doubles the PAIRS 16..256, i.e. the tables actually emitted. Head minus base is EXACTLY 0 allocations and 0 bytes at every rung of both, in phase:lowerMaxonToStd and in total. The separator costs nothing because underscore and dot are both one byte, so the label String is the same length and the same allocation; and the witness path is LINEAR in both dimensions, phase:lowerMaxonToStd allocations x1.97 x1.98 x1.99 x2.00 over sites and x1.93 x1.96 x1.98 x1.99 over pairs. THE ONE GENUINELY NEW RUN-TIME PATH is requireIdenticalPayload, a ByteArray.equals() on the already-registered branch of registerRdataConstantLabeled, whose cost would be O(payload) PER HIT. ITS HIT COUNT WAS MEASURED, NOT ARGUED: the body was temporarily replaced by an unconditional panic and the compiler rebuilt, so ANY re-registration aborts. The full x64-windows suite still passed 1793/0, as did 256 distinct witness tables in one program, 4096 witness reference sites in one program, and the whole 5-rung scale ladder. The branch is taken ZERO times - both callers memoize before they ask (ensureWitnessTable on the label, ensureLayoutDescriptor on the gid), exactly as its header claims. Instrumentation reverted; the committed binary is byte-identical to the one every A/B above ran with. THE +612 BYTES IN THIS ROW'S DELTA COLUMN IS NOT THIS RUNG'S AND SHOULD NOT BE READ. It appears identically on the BASE binary, and the decisive test is that checking out cdd6fc8bf - the very commit that wrote the previous row - and rebuilding it HERE reproduces this row's numbers exactly while still reading +612 against its own recorded row. So the previous row is not comparable across environments, and no number was 'fixed'. The byte column carries a path-length term, measured twice on an unchanged binary: +49 bytes for +49 characters of the compiler's own filename on a trivial program, and +34,597 bytes for +30 characters of the SOURCE filename on a large one, with the allocation count unmoved both times. The other documented candidate is the bootstrap refresh between the two rows (bin/maxon.exe here postdates af8c2e931, which changed maxon-sharp), which moves shv2's memory profile but not its output. THE ALLOCATION COLUMN IS IMMUNE TO BOTH and reads 0 at every rung, which is why it is the one to trust. NOT THIS RUNG'S, CONFIRMED PRE-EXISTING: regalloc:splitting is quadratic in PER-FUNCTION pressure (x3.64 x3.80 allocations, up to x4.13 CPU, on shape B whose main holds 256 constructions), the budgeted SplitLiveRanges term of ARCHITECTURE.md:1336-1345; with per-function pressure bounded it reads x1.85 x1.92 even at 4096 witness sites, so it tracks pressure and not witness dispatch. Gates on this tree: shv2 build exit 0, x64-windows 1793/0, wasm32-wasi 1597/0, specs-shv2/fragments clean after the run, no exit 101. | 36,000,693 | 68,133,223 | 136,502,600 | 274,955,831 | 560,434,507 | 1,150,878,945 |
| 2026-07-27 | RE-BASELINE of the witness-label injectivity rung's row onto the MERGED tree (rebased over upstream #123's BlockId dense-table work df5b5ffd8/220c5efed plus the arm64 type-name-collision goldens 27549b251). The row directly above was measured on the pre-rebase branch based at 858d8dbef, and upstream changed a data structure the compiler indexes by — so those ABSOLUTE numbers are not the merged tree's, and the next rung must compare against THIS row rather than that one. THE RUNG'S OWN FOOTPRINT IS STILL 0, re-verified after the rebase rather than re-asserted: the separator is one byte either way, and this ladder cannot reach the code at all (ScaleCorpus.maxon:554 generates no 'where' clause, so no witness table is minted and witnessTableLabel/ensureWitnessTable are never called), which is exactly why the rung was measured on a purpose-built witness ladder instead — +0 allocations and +0 bytes at every rung across 256..4096 dispatch sites and 16..256 (conformer, interface) pairs, linear on both axes. Any delta this row shows against the previous one therefore belongs to upstream's dense-table change and not to the witness join. Merged-tree gates: build exit 0 zero warnings, x64-windows 1793/0, wasm32-wasi 1597/0, specs-shv2/fragments EMPTY after both suite runs (byte-identical codegen), no run exited 101. | 35,998,341 | 68,129,591 | 136,496,408 | 274,944,519 | 560,412,955 | 1,150,836,913 |
| 2026-07-27 | Splitter: the confined half DEFERRED until it can win + applyForbidden SCOPED to the split's own ids + PeakRank stopped being a heap object minted per winning op and twice per tree combine + ScannedValues.all hoisted out of buildUseIndex's and computeLivenessOn's per-block loops + the fresh-id walk made a word-range walk. WHY THE CPU COLUMN READS A TRUE ZERO FOR THE HEADLINE FIX, and it is corpus blindness of the KNOWN kind: the ladder's pressured functions are LOOPS, which enter confined mode at their first analysis, so the deferral -- the whole 8.8x -- CANNOT engage here and correctly does not move. It is measured on the shape the corpus cannot express (Testing/ladders/genwidelive.sh 400 sum, one basic block, 396 splits: regalloc:splitting 11.49s -> 1.56s; x64-large-frame-arg7 53.9s -> ~6.1s; suite 56.3s -> ~18s). A Delta0 from a ladder that cannot express the feature measures the instrument's blind spot, not the cost. WHAT DID MOVE IS THE EXACT COLUMN, and it is attributable to the digit: whole-compile allocations fall from the ScannedValues hoist (one heap object per block of every function, in TWO per-block loops), and regalloc:splitting allocations fall a further 3.2%/4.8%/6.4%/7.8% across rungs 0-3 from the PeakRank de-allocation, every removed allocation dividing out at exactly 72 bytes -- the per-op mint dominates a single-block function, the O(log blocks) combine term dominates this many-block corpus and its share GROWS with rung, which is what the two halves being complementary predicts. CAVEAT ON THE CPU COLUMN THIS RUN ONLY: 12 competing maxon processes were live on the box (suite wall read 46.6s then 18.1s for identical work), so treat every CPU ratio here as unreliable and read the exact columns; that is the reason the memory columns exist. Residual, NOT closed: the pass is still superlinear on one basic block (per-split cost x1.50-1.71 per doubling, was x2.05) -- four O(block)-per-split walks remain and need a positional structure with order maintenance, filed as its own rung. Decisions UNCHANGED throughout: 243 goldens byte-identical on every target that ran, valuesSplit 96/196/396. | 35,867,009 | 67,806,011 | 135,726,540 | 273,174,171 | 556,427,711 | 1,141,988,677 |
| 2026-07-27 | Splitter re-space: SCOPED to the smallest subtree that can absorb the splice, and the recorded slots it moves are now actually REPAIRED. Two findings, one cause. (1) THE RUNG'S `PressureIndex.relayout` WAS NOT OFF THE PATH — "zero re-spaces on the whole scale corpus" is not what this corpus does. Measured with the pre-change binary: corpus rung0 fires ONE function-wide re-space of 1024 slots, rung1 fires TWO of 2048 each. Count doubles per rung AND size doubles per rung, so its total work went x4.0 per doubling — a quadratic sitting on the shipping corpus, invisible because a re-space allocates almost nothing and the ladder that would show it (genwidelive, one block, wide gaps) never fires one. (2) IT ALSO CRASHED. `SplitEdits.insertedSlots` records each spliced op's slot as it is seated; a re-space mid-batch MOVES those ops, and `appendInsertedSlots` asserted in a comment that the re-space "re-derived" the slots already returned — nothing did. Reachable at the SHIPPING SlotGap=4 on a plain straight-line function (alternating pressure humps: an add reading two already-spilled values sitting directly after a call whose own result is spilled puts THREE ops in one gap, and midpoint subdivision fits two): `PressureIndex.opAtSlot: slot 166 holds no op` out of `reindexSplitValues`, at every rung of a 12/24/48 ladder. THE FIX: `makeRoom` climbs from the exhausted gap to the first ancestor whose ops still fit it two slots apart and re-spaces ONLY that subtree (`respaceRange`, Theta(subtree)); the function-wide `relayout` survives as the fallback for a block that has outgrown its run, which needs the block to roughly DOUBLE its op count, so it is O(log growth) per block rather than per collision. `PressureRelayoutScratch` became a WINDOW with an old->new map, and `applyMoves` is the repair the comment used to merely claim. MEASURED on the corpus: rungs 0/1/2 now do 4/10/22 local re-spaces of 8-16 slots each = 48/128/288 slot-moves against the old 1024/4096/(16384) — LINEAR, with the per-event scope CONSTANT at 8-16 slots across all three rungs and ZERO function-wide re-spaces at any of them. WHAT MOVED IN THE EXACT COLUMNS, attributed: regalloc:splitting allocations +180/+270/+452/+816/+1544/+3000 across rungs 0-5, converging on x2.0 — exactly ONE per function that runs the splitter, the `moved` column the slot repair needs. Bytes FALL -30,277/-66,573/-139,133/-284,253/-574,493/-1,154,973, which is the avoided function-wide capture (~41,984 bytes of columns for a 1024-slot capture) less the ~10,080 bytes of per-function `moved` headers and ~2,000 of small local captures: -29,904 predicted against -30,277 measured at rung 0. UNCHANGED, deliberately: valuesSplit 96/196/396, 243 goldens byte-identical, `genwidelive` dead-control exit 7 at every rung. The one-block residual is NOT what this touched and did not move — min-of-3 CPU on genwidelive sum reads x2.395/x2.390 before and x2.199/x2.458 after, i.e. ~N^1.25 both ways, which is the Hall matching plus fillLiveBeforeOp, still measured-linear-per-split debt. Gates: build 0 warnings, suite 1793/0, and 1793/0 again under VerifyIncrementalSplit=true at SlotGap=4 AND at SlotGap=2 (which forces the function-wide fallback, covering the path the corpus no longer reaches). | 39,286,079 | 74,335,433 | 148,476,730 | 298,365,881 | 606,502,477 | 1,241,828,003 |
| 2026-07-27 | P1.7 finish Slice 1 (genericInstanceHasBaseLayout replacing a 4x-duplicated isArrayInstance-or-isSetInstance stop condition): THE COMPILER'S COST DID NOT MOVE AT ALL, and the whole of this row's apparent movement is an ARTIFACT OF THE CHECKOUT PATH, measured rather than assumed. (1) THE CODE COSTS ZERO. Against the parent (d28583442) the allocations, frees and bytes are bit-identical at EVERY rung and in EVERY per-phase bucket except phase:load and phase:parse; the whole-compile allocation count is identical to the digit. It also holds far outside the corpus: on Testing/ladders/geninstances.sh at 8,000 and 32,000 interned instances (the corpus's rung-5 count is ~387, so 32,000 is ~80x it) both compilers allocate 786,144 / 54,649,587 bytes and 3,114,445 / 225,247,505 bytes respectively -- identical to the digit both times. That ladder is the right one because rootManagedOpaqueArrayElementDrops opens the changed gate over EVERY interned instance unconditionally, so N=32000 exercises the new predicate 32,003 times per compile. (2) THE +3,876 BYTES IS THE WORKTREE'S PATH, NOT A COST, AND HERE IS THE PROOF: the PARENT's binary, copied into this worktree and run from this worktree's path, reproduces THIS row's numbers to the digit (rung 0: 615,240 allocs / 35,870,885 bytes; rung 1: 1,114,446 / 67,809,887). Same code, different path, this row's numbers. The delta lands in phase:load (-14 allocs, +3,115 bytes), phase:parse (+232 bytes) and the un-bucketed driver (+1,740), it is CONSTANT at all six rungs, and it is rung-independent because it tracks the length of the paths the driver holds, not the program. This worktree's root is 29 characters longer than the main checkout's. SO: a run recorded from the main checkout will read about -5,087 bytes / +14 allocs against this row, and that is the path unwinding, NOT a regression -- subtract it. (3) THE PER-CALL QUESTION, asked because the old form was two integer-ish name compares and the new one adds a string-keyed Map probe: the new form is NOT more expensive, and it is arguably cheaper. isArrayInstance(id) or isSetInstance(id) derived the base name TWICE (two bounds-checked array gets and a managed ByteArray refcount pair, each); the new predicate derives it ONCE and adds one Map.contains over a 3-to-5 byte interned name. No name is CONSTRUCTED on either path -- TypeNameInterner's names are zero-copy slices of the source -- which is why the exact columns above can read a true zero. The CPU column cannot resolve the difference and the reason is documented rather than waved at: a NULL CONTROL running the IDENTICAL binary on both sides of the same interleaved min-of-9 protocol produced -0.70% (N=8000) and +3.83% (N=32000) at the whole-compile level, the same magnitude and sign pattern as the real A/B (+0.79% and +3.65%), and a blocked-rather-than-interleaved repeat of the same A/B flipped a sign (phase:signatures +2.17% -> -0.11%). This box's floor for this experiment is about +/-4-5%, so any per-call claim here would be a statement about the apparatus. (4) NO SUPERLINEAR STRUCTURE, checked not assumed: the gate is O(1) and is opened ONCE per walk (not per field), the walk count is O(interned instances) exactly as before, and typeIsManaged returns true immediately for a nested genericInstance tag rather than recursing, so a deep Box-with-Box-with-String nest cannot re-walk. (5) MEASURED DEBT, filed not fixed: genericInstanceHasManagedField and genericInstanceHasStringField re-derive baseLayoutOf INSIDE their per-field loop -- genericInstanceFieldCount and substituteInstanceFieldType each call it -- so a base-name derive plus a name-keyed Map probe is paid once PER FIELD, where their sibling managedOpaqueArrayElementOf hoists `let layout = baseLayoutOf(id)` above the loop and does not. It is LINEAR (O(instances x fields), no recursion), it runs only for instances whose destructor is referenced, and generic bases carry few fields, so it is a constant factor and not a curve; with the noise floor measured at +/-4-5% above it could not be shown to move anything, and the mandate is growth curves rather than constant factors, so it is recorded here rather than shipped as a change that only looks like an optimization. The trigger that would make it worth taking: a machine-generated wide generic base (tens of fields) instantiated many times. Suite 1831/0, specs-shv2/fragments byte-identical, build 0 warnings. | 35,870,885 | 67,809,887 | 135,730,416 | 273,178,047 | 556,431,587 | 1,141,992,553 |
| 2026-07-27 | SPLITTER POSITIONAL STRUCTURE — the whole rung, and THIS ROW'S DELTA IS +192 ALLOCATIONS AND +7,621 BYTES AT EVERY RUNG, CONSTANT — AND IT IS NOT THE COMPILER. It is ENTIRELY `phase:load` (+192 allocs / +8,233 bytes, flat at every rung, rung 0 through rung 5 alike), i.e. the already-documented PATH-LENGTH / environment term: the row above was measured in this rung's WORKTREE and this one on the merged main checkout. EVERY COMPILER PHASE IS BIT-IDENTICAL between the two runs — `regalloc:splitting` reads 32,554 allocations at rung 0 and 624,377 at rung 5 on BOTH. ⚠ The coordinator predicted this row would be delta-0 and it was not; the prediction was wrong and the instrument was right, which is precisely why the columns are read rather than the expectation. ⚠ AND ITS DELTA WAS MEASURED AGAINST THE "Splitter re-space" ROW, which is NOT necessarily the row directly above this one — upstream rows have been interleaved by a rebase since, so read this row's delta against THAT row by name, never by position. It exists to attribute the jump in the "Splitter re-space" row, which was written by the rung's optimizer pass after increments 1 and 2 were already in the binary and therefore attributed only the re-space fix. Read the two rows together. WHAT THE RUNG DID, measured on Testing/ladders/genwidelive.sh (Theta(N) values live in ONE basic block, so Theta(N) splits over a Theta(N) block): regalloc:splitting allocations 129,966 / 509,828 / 2,019,538 -> 6,683 / 13,095 / 25,905 at N=100/200/400, i.e. -98.7% at N=400 and per-doubling x3.92/x3.96 -> x1.96/x1.98 — QUADRATIC TO LINEAR in the exact column. CPU 6,243M -> 1,670M ticks (-72.7%), per-doubling x3.21/x3.57 -> x2.31/x2.48, i.e. N^1.83 -> N^1.25. ON THIS MANY-FUNCTION CORPUS regalloc:splitting is LINEAR IN BOTH COLUMNS (allocations x1.59 x1.74 x1.85 x1.92 x1.96 converging on x2.0; CPU x1.91 x2.04 x1.98 x2.08 x2.12) — the residual N^1.25 is specific to ONE function carrying Theta(N) simultaneously-live values and this corpus cannot express it. THE +8.9% BYTES between the previous rung's row and the row above is THIS RUNG'S, and it is the PressureIndex's columns — the price of gap-labelled slots, measured linear, deliberate. Against it: whole-compile allocations fell ~7% at every rung. THREE MECHANISMS: (1) static per-op facts memoized by union VARIANT ordinal — 112 cases, constant size, NO staleness class, so no invalidation hook exists to get wrong; (2) the block's peak is a per-op MAX-SUFFIX-SUM over gap-labelled slots, NOT the per-interval range-add PLAN.md prescribed (the interval form needs endpoints in pass-through blocks, re-derived from live sets that are mid-edit; the per-op form separates per value, so a split's repair is O(uses of one value)); (3) the other three per-split walks were never range problems at all — they walked a whole block to service an O(1) value set, and are now edit-proportional. MEASURED: the tree ALONE was not enough (x2.81/x3.14, barely moved); both halves give x2.25/x2.45. THE LARGER HALF OF THE ALLOCATION WIN WAS UNION BOXING, pre-existing and invisible to reading: a PAYLOAD-FREE case of a payload-carrying union still heap-allocates (mm_alloc 16 bytes + mm_incref), and `ValueSpillSlot.none` was built TWICE PER OP per dirty block per split just to say "this op touches no slot" — 94.7% of the phase's allocations; `ValueDefSite` was the same shape per value crossing the peak. Both are now enums. Attribution exact: bytes-removed / allocations-removed = 16.0 at all three ladder rungs, to the digit. RESIDUAL, so the rung is ◑ not ✅: ~31% of the pass is still superlinear on one block — confinedPeakAt/hallVerdictAt 49.5% (exact Hall bipartite matching, measured LINEAR x2.03/x2.07; a matching is not a suffix sum, so it is a different problem), fillLiveBeforeOp 15.8% (Theta(pressure) data no count-tree can carry), SplitEdits.resequence/commit 8.6%, chooseVictim 7.0%. Re-measure trigger: a single block's PRESSURE growing — an inliner, or machine-generated wide types. DECISIONS UNCHANGED THROUGHOUT: valuesSplit 96/196/396, 243 goldens byte-identical, dead-control exit 7. Gates: suite 1794/0 (+1 = the re-space regression spec, seen RED at 5d99436bb with `opAtSlot: slot 166 holds no op`), VerifyIncrementalSplit ON at SlotGap=4 AND 2, x64-windows 1794/0, x64-linux 1688/0, wasm32-wasi 1598/0, arm64-macos 1603/0, arm64-linux SKIP (wedged OrbStack guest, filed). | 39,293,700 | 74,343,054 | 148,484,351 | 298,373,502 | 606,510,098 | 1,241,835,624 |
| 2026-07-27 | P1.7 finish Slice 2 (arrayElementSize narrowing a ranged-int array element to the smallest of 1/2/4/8 bytes that holds its declared range). NO CODE CHANGE FROM THE OPTIMIZER, and the numbers say why. ///// A/B PROTOCOL: the parent (d51ddd665) was built in a worktree whose absolute root is EXACTLY THE SAME LENGTH as this one (51 chars: .claude/worktrees/p17-basel vs .claude/worktrees/p17-esize) from a byte-identical bootstrap (sha256 5457eab2256c9da7802bbefb085719e041b3b2f91b2cf971be7862b8adf1c9c2). So the phase:load path-length artifact the PREVIOUS row had to argue away is EXACTLY ZERO here rather than subtracted, and every remaining byte is the code. The last committed row was NOT a usable baseline (it reads -40,942 allocs / +3,423,006 bytes against this run, all of it commits and a bootstrap between the two worktrees), which is why the A/B was taken against a freshly built parent instead. ///// THE DELTA DECOMPOSES COMPLETELY -- four terms, and they sum to the digit at all six rungs. head-minus-parent whole-compile allocations: +12, -12, -92, -236, -556, -1,180 (rungs 0..5); bytes +3,936, +7,712, +6,432, +12,384, +7,264, +13,728. (1) phase:parse -40/-80/-160/-320/-640/-1,280 allocations and -640..-20,480 bytes -- A PERFECT DOUBLING, and it is a SAVING: exactly -1 allocation and -16 bytes per Array-with-named-element size query, 40 such sites at rung 0 doubling to 1,280 at rung 5. CAUSE: arrayElementSize no longer routes the trivial case through genericInstanceSoleArgLogicalSize then typeLogicalByteSize, whose `named` arm called structOf(name), and structOf returns DeclaredStruct -- A UNION WITH AN ASSOCIATED-VALUE ARM, therefore HEAP-BOXED, 16 bytes, allocated on EVERY call including the `undeclared` answer a ranged alias always gets. The new trivialElementStorageBytes answers out of Map.get's own `throws` channel and boxes nothing. This is the same allocation class RangedAliasRegistry.underlyingInAnyFile already documents having removed for the same reason. Independently confirmed off-corpus: on a throwaway ladder of 128/256/512/1024/2048 array-create sites the head-minus-parent phase:parse allocation delta was -128/-256/-512/-1,024/-2,048 -- exactly -1.00 per site at every point. (2) phase:load +10 allocations / +432 bytes, CONSTANT at every rung, and (4) an un-bucketed +10 / +432, also constant at every rung. These are the SAME constant twice: constructing one extra empty Map field. Two RangedAliasRegistry instances exist (Project's and ProgramSignatures'); each gained `storageBytes`; Project's is constructed inside loadProject so it bills to phase:load, ProgramSignatures' is constructed outside any phase bracket. It is construction and not first-insert, because loadProject only reads files into the query DB -- no alias is recorded before phase:signatures. (3) phase:signatures AND phase:merge, each +16/+24/+24/+32/+32/+40 allocations and +1,856/+4,064/+4,064/+8,320/+8,320/+16,672 bytes -- the two storageBytes maps' backing arrays. THAT BYTE SERIES IS A POWER-OF-TWO STEP FUNCTION (Map capacity 16/32/64/128), not a curve: it steps once per ~2 rungs while the program doubles every rung, because the index is sized by the number of DISTINCT RANGED-ALIAS NAMES and not by program size. Net: the change is allocation-NEGATIVE from rung 1 upward and increasingly so, because the per-site saving grows with the program while every cost term is constant or sub-linear. Bytes stay net-positive at +13,728 on rung 5, which is +0.0011% of 1.24 GB. ///// THE PER-CALL QUESTION, ASKED BECAUSE THE DOC COMMENT SAYS "FOLD OVER EVERY DECLARATION": the fold is at WRITE time. RangedAliasRegistry.record calls widenStorageBytesFor once per declaration (one probe plus at most one upsert, O(1) amortised), and storageBytesInEveryFile is ONE Map.get -- there is no walk on the read path, which is already the index shape a fix would have introduced. MEASURED, not inferred, on a purpose-built ladder of F files each declaring the SAME alias `Small` plus S array-create sites over `Array with Small` (the corpus cannot express this: it generates ~387 interned instances at rung 5 but never has two files declare one alias name). The 960 extra array-create sites between S=64 and S=1024 cost 125,932 allocations at F=8 AND 125,932 allocations at F=1024 -- BIT-IDENTICAL across a 128x change in the number of files declaring the name, which is the exact column proving the per-call cost is independent of F. CPU on the same pair read 131,507,845 vs 138,189,331 ticks (+5.1%, at this box's measured +/-4-5% floor for this experiment, and the exact column says the work done is identical). ///// THE WRITE SIDE IS LINEAR TOO, and the contested-name path was isolated with a CONTROL: arm "same" (F files declaring one name) against arm "distinct" (F files declaring F different names, identical file/line/token count, so multiplyDeclared is never touched). Same arm, F=64..1024: phase:merge allocations 3,433 / 5,135 / 8,499 / 15,193 / 28,543 (x1.50 x1.65 x1.79 x1.88) and CPU 3,995,106 / 7,004,530 / 12,564,668 / 24,087,463 / 47,782,345 ticks (x1.75 x1.79 x1.92 x1.98); phase:signatures allocations 8,125 / 15,501 / 30,237 / 59,693 / 118,589 (x1.91 x1.95 x1.97 x1.99). Distinct arm: merge allocations 4,134 / 5,925 / 9,442 / 16,417 / 30,304 and CPU 3,335,636 / 5,364,798 / 9,251,049 / 17,138,828 / 37,317,020. EVERY ONE OF THOSE IS x2 PER DOUBLING -- no quadratic. The contested path costs ~28% more CPU in phase:merge at F=1024 than the control (the declFilePath.toString() and the byFile probe), which is a CONSTANT FACTOR on a phase that is ~1.4% of the compile, measured linear, and nowhere near triggered today: the widest real case is stdlib's seven files privately declaring `Byte`. The trigger that would make it worth revisiting is a program where hundreds of files declare one alias name. ///// CORPUS BLIND SPOT, stated rather than filed: the ladder above exercises the new LOOKUP 40x2^n times per compile, which is what the -1/site is measured off, but it NEVER EXERCISES A NARROWED STRIDE. Every array element the generator emits is `ScaleElement`/`ScaleGenElement` = int(i64.min to i64.max) (negative low, so rangedAliasStorageBytes returns the machine word by its first arm), `String`, or a struct -- all still 8. A future row must not read this corpus as evidence about narrowing itself; the throwaway ladder's `Small = int(0 to u8.max)` does emit `movRegImm32 rcx, 1` into __arr_create and was checked in the IR. ///// Build 0 warnings; suites x64 1837/0, x64-linux 1731/0, wasm32-wasi 1641/0, no leaks; specs-shv2/fragments byte-identical (git status empty after all three). | 39,293,891 | 74,347,021 | 148,487,038 | 298,382,141 | 606,513,617 | 1,241,845,607 |
| 2026-07-27 | P1.7 finish Slice 3 (three front-end divergences: newline tolerance inside [...], generic type-ARGUMENT validation via denotedNamedType, and an int fast path that no longer decides the element type from the first TOKEN) PLUS the optimizer's own change: the parser's two whole-program generic checks no longer run during the tolerant declaration sweep. ///// A/B PROTOCOL: the parent (679e023a9) was rebuilt in THIS worktree from the same bootstrap (sha256 5457eab2256c9da7802bbefb085719e041b3b2f91b2cf971be7862b8adf1c9c2) and reproduced the last committed row EXACTLY -- delta 0 allocations and 0 bytes at all six rungs -- so the committed row is a valid baseline here and every number below is this branch's. The worktree root is 51 chars, the same length as the p17-esize root that wrote the previous row, so the phase:load path-length artifact is zero rather than subtracted. ///// THREE STATES, whole-compile allocations rungs 0..5. Parent: 574,298 / 1,034,851 / 1,954,749 / 3,791,682 / 7,463,489 / 14,803,534. Slice as implemented (049b954b2): +106 / +194 / +370 / +722 / +1,426 / +2,834. After this commit: -46 / -92 / -182 / -360 / -714 / -1,420 against the PARENT -- the slice is now allocation-NEGATIVE, a swing of -4,254 at rung 5. Bytes after this commit: -2,356 / -4,808 / -9,718 / -19,542 / -39,190 / -78,514. ///// THE IMPLEMENTER'S MEASUREMENT, VERIFIED AND DECOMPOSED. Their +106..+2,834 reproduces to the digit and splits into exactly two phases with zero residue at every rung: phase:signatures +101/+185/+353/+689/+1,361/+2,705 and phase:parse +5/+9/+17/+33/+65/+129. Both series are EXACT geometrics -- signatures = 17 + 84*2^n (consecutive differences 84/168/336/672/1344, perfect doublings) and parse = 1 + 4*2^n (differences 4/8/16/32/64). LINEAR, confirmed off-corpus 4.5x beyond the ladder: a throwaway ladder of N=64..2048 `typealias I_i = GBox with E_i` declarations, each with its own ranged-int argument, read head-minus-parent phase:signatures +385/+769/+1,537/+3,074/+6,146/+12,290 -- 6.016/6.008/6.004/6.004/6.002/6.001 allocations per generic type ARGUMENT, ratios 1.997/1.999/2.000/1.999/2.000 -- with phase:parse delta exactly 0. ///// THE 20x SIGNATURES-VS-PARSE SPLIT IS TWO DIFFERENT COSTS, not one cost with a puzzle. (a) In the SWEEP denotedNamedType FAILS for every file-local argument -- a file's own declarations fold only after its whole token walk -- so each one built a positioned E3011: String.from(name) plus an interpolated message plus a Diagnostic record, ~6 allocations, into an artifact that is then discarded. (b) In the REAL PARSE the same call SUCCEEDS and the entire cost is MaxonType.structRef(id) -- a union arm with an associated value, therefore HEAP-BOXED -- returned for an argument naming a declared STRUCT and dropped on the floor by `_ =`. Isolated on a second throwaway ladder of N struct-typed arguments: +1 allocation and +16 bytes per struct-named argument, EXACTLY, at N=64/128/256/512/1024. The corpus emits instances/2 of that shape (the managed-holder arm), which is 4*2^n -- the parse series, to the digit. ///// THE CHANGE: ProgramSignatures.allFilesFolded, set once in queryProgramSignatures the moment the last file is folded; checkGenericInstance and checkGenericArgType return immediately when it is false. Both are PURE DIAGNOSTIC -- neither returns a value nor mutates anything but artifact.diagnostics -- and the sweeper's artifact is never merged (queryProgramSignatures builds a throwaway Parser; mergeArtifact runs only on the real parse's), so the sweep's verdict could never be reported by any program. The real parse re-reads every one of these declarations with the index complete (both a top-level `typealias X = Base with Arg` and a nested one inside a type body have a sweep reader and a real-parse reader that funnel through the same readTypeAliasDeclaration / readNestedTypeAlias), so every diagnostic a program earns is still recorded exactly once at the same position. The flag lives on the INDEX and not on the Parser because only the index knows which side of the fold each of its three readers -- sweeper, real parse, constant evaluator -- is on; threaded through Parser.create it would be one fact written at three call sites. ///// WHAT IT SAVES, DECOMPOSED TO THE DIGIT, this commit vs parent at all six rungs. phase:signatures -51/-101/-199/-393/-779/-1,549 allocations and -2,452/-4,968/-10,006/-20,086/-40,246/-80,594 bytes, and that is TWO terms: the whole of the slice's +2,705, plus 1,549 at rung 5 of PRE-EXISTING dead work from checkGenericInstance, which has been building the same discarded diagnostics since P1.6-A -- ~4 allocations per generic-instance declaration (its structOf DeclaredStruct box plus the E2055 'has no associated types' message the sweep records for a base whose own file has not folded). phase:parse +5/+9/+17/+33/+65/+129 allocations and +80/+144/+272/+528/+1,040/+2,064 bytes, IDENTICAL to the slice by construction: the gate is false only in the sweep, so the real parse does bit-identical work. phase:load 0 allocations / +8 bytes constant at every rung, plus an un-bucketed 0 / +8 -- the same 8 bytes twice, ProgramSignatures growing by one bool field; two are constructed, the empty placeholder in SignatureMemo.empty() (billed to phase:load) and the real one in queryProgramSignatures (built before the first phase probe, so un-bucketed). They sum exactly: rung 5 allocations -1,549 + 129 = -1,420, bytes -80,594 + 2,064 + 8 + 8 = -78,514. Off-corpus the gate is -10.00 allocations per generic alias declaration versus the slice and -4.00 versus the parent, FLAT at N=64..2048 (-10.094/-10.055/-10.031/-10.020/-10.012/-10.007). ///// CPU: NO CLAIM MADE. phase:signatures read 1,039M (parent) against 1,045M (gated) ticks at rung 5, +0.5%; whole-compile 32.19G against 32.09G, -0.3%. Both are inside this box's measured +/-4-5% band, so the CPU column can neither confirm nor deny a saving of this size and is not asked to. The exact allocation columns are the evidence. ///// RESIDUAL KEPT, MEASURED AND ATTRIBUTED: the +1 allocation / +16 bytes per generic type argument that names a declared STRUCT stays (128 of them at rung 5 = +0.0009% of the compile). It is denotedNamedType materialising a boxed MaxonType.structRef for a caller that only needs 'is it declared?'. Killing it needs a second predicate over the same six-arm cascade, and checkGenericArgType asks denotedNamedType precisely so that a type ARGUMENT and an `as`-cast TARGET can never come to disagree about what a name means. A 16-byte box is not worth a second door onto that question. ///// NO SUPERLINEARITY, and the slice's other three pieces were each read for one: initializerEndIndex now counts bracket depth instead of stopping at the first newline, still ONE forward scan per top-level binding, and a runaway (an unclosed `[`) leaves the cursor at EOF so it can happen at most once per file; arrayElementIsSingleToken/indexPastNewlines scan the newline run after ONE element and each element's run is disjoint, so the sum over a literal is bounded by its newlines; finishArrayLiteral's skipNewlines advance the cursor. The slice adds no pass, no IR op, and no collection the compiler indexes by. ///// CORPUS: the generic knobs DO double per rung (GenericInstancesBase 8 and GenericBasesBase 4 are rung-0 values knobAt doubles), giving ~448 generic type arguments and ~384 instance declarations at rung 5, which is why this cost was visible at all; the two throwaway ladders reach 2,048 arguments and stay flat at 6.00 and 1.00 allocations per site. ///// Build 0 warnings; suites x64 1851/0, x64-linux 1745/0, wasm32-wasi 1654/0, exit 0 (no leaks); verify-warm-rebuild PASS on a generic-alias program (determinism + cache + invalidation -- re-checked because the new flag lives on the memoized index); specs-shv2/fragments byte-identical (git status empty after all three suite runs). | 39,291,535 | 74,342,213 | 148,477,320 | 298,362,599 | 606,474,427 | 1,241,767,093 |
| 2026-07-27 | STALE-ARTIFACT HYGIENE (the previous build's output is now DELETED BEFORE the pipeline instead of being truncated by the writer at the end, so a compile that fails leaves nothing runnable behind). NO CODE CHANGE FROM THE OPTIMIZER, and the measurements say why. ///// A/B PROTOCOL: the parent (5bafc9bc0) was rebuilt in THIS worktree from the SAME bootstrap (sha256 8abfd02bd6158878b81d87580c9d542b9433fe6513e9bc0f12090887fe7838b4) and reproduced the last committed row EXACTLY -- delta 0 allocations and 0 bytes at all six rungs -- so the committed row is a valid baseline here and every number below is this branch's. The worktree root is 51 chars, the same length as the p17-finish root that wrote the previous row, so the phase:load path-length artifact is zero rather than subtracted. The two shv2 binaries (parent sha256 6092db6e, head 1ba204b3) therefore differ only in shv2 source. ///// THE DELTA: +14 allocations and +530 bytes, FLAT AT ALL SIX RUNGS across a 32x ladder. O(1) PER COMPILE -- not per function, per block or per op -- which is the whole claim this rung had to support. AND IT IS ENTIRELY UN-BUCKETED: all 22 CompilePhases and all 7 regalloc sub-phases are IDENTICAL TO THE DIGIT between parent and head, in allocations AND in bytes, at every rung. That is not just the attribution, it is the proof the pipeline was not touched -- the new work sits between loadProject and the first pass, outside every PhaseProbe. ///// ATTRIBUTED TO THE DIGIT WITH ZERO RESIDUE, and re-runnable: maxon-shv2/Testing/ladders/genfsprobe.sh 20000 out.maxon, whose discard_plus_require case reads 14 allocations / 528 bytes -- which is +14/+530 at the ladder's one-character-longer path. THE SPLIT: one FilePath.changeExtension is 10 allocations / 368 bytes; the four filesystem calls (File.delete twice and File.exists once in discardPreviousOutput, File.exists once in requireOutputWritten) are 4 allocations / 160 bytes, exactly ONE allocation of exactly 40 BYTES EACH. THAT 40 BYTES IS NOT THE PATH: it is the green-thread runtime's SyncRequest. __io_submit_sync mm_raw_allocs one, enqueues it, SetEvents a SYNC WORKER THREAD and parks the caller until the worker completes it, so every __ManagedFile call is a cross-thread round trip. Build the probe with the BOOTSTRAP (shv2 cannot compile stdlib/File.maxon yet); that is not a weaker measurement, because the shv2 binary is itself bootstrap-emitted from the same stdlib and the same runtime, so it is the same machine code. ///// STAR -- A TRAP THIS FOUND, worth more than the row itself: FilePath.changeExtension charges by what the String is a PRODUCT OF, not by what it says. resolveOutputPath builds outputPath by INTERPOLATION; on Windows FilePath.create then normalizes forward slashes to backslashes through String.replace. An output argument already spelt with backslashes skips the replace, so the stored String is the interpolation's own buffer and changeExtension costs 10 allocations / 368 bytes; one spelt with forward slashes goes through replace and costs 8 / 343. TWO FilePaths THAT COMPARE EQUAL COST DIFFERENT AMOUNTS -- build them from literals instead and the difference vanishes, which is how you can tell it is the buffer and not the bytes. scale-test spells its rung paths with FilePath.join (backslashes), so this ladder carries the 10-allocation form; driving the IDENTICAL rung-0 compile by hand with forward-slash arguments reads +12 / +504 instead. VERIFIED BOTH WAYS: backslash argv reproduced scale-test's rung-0 totals to the digit (parent 574,252 / 39,291,535, head 574,266 / 39,292,065) while forward-slash argv gave 574,263 and 574,275. Anyone comparing a hand-run figure against a ladder figure must match the spelling first. ///// THE TIME COST, MEASURED SEPARATELY BECAUSE NO MEMORY COLUMN CAN PRICE A SYSCALL (a File.delete allocates one 40-byte record whatever it costs). Per compile on this box, median of 3 x 20,000 iterations: a failing File.delete 52.8 us (twice), File.exists 48.2 us absent and 62.9 us present, changeExtension 1.4 us -- about 205 us in total, against a real delete of a present file at 267 us and a 64-byte File.writeBinary at 449 us. That is roughly 1 percent of a 24.6 ms trivial compile and 0.001 percent of the 14 s that builds shv2 itself. AT SUITE SCALE: 1,851 x64 spec tests times 205 us is 0.38 s of CPU, spread over 12 workers, about 32 ms of the suite's 18.4 s wall -- 0.17 percent. END-TO-END A/B, three interleaved passes of 150 compiles: success path parent 24.79/24.42/24.55 ms against head 25.08/24.85/24.75 ms, plus 0.31 ms per compile (the output EXISTS in that loop, so the delete is a real one); failure path parent 22.55/22.38/22.11 against head 22.26/22.16/22.49, delta minus 0.04 ms, inside its own noise. MEASURABLE IN A MICROBENCHMARK, FREE AT SUITE SCALE. ///// THE ONE CANDIDATE OPTIMIZATION, REJECTED ON ITS NUMBERS: guarding the delete with a File.exists. Measured, exists-then-delete on an absent path is 48.2 us against a bare failing delete's 52.8 us -- 4.6 us, which is 8.5 ms of CPU across the entire x64 suite -- and on a present path it costs a whole extra 62.9 us exists ON TOP of the delete it still has to do. It would also put a SECOND existence question in front of the one discardPreviousOutput already asks, whose entire job is to tell 'was not there' from 'would not go'. Not taken. ///// NO SUPERLINEARITY, AND STRUCTURALLY NONE TO FIND: the rung adds no pass, no IR op and no collection the compiler indexes by. Every added call runs once per compile(); all seven changeExtension sites in shv2 are once per compile or once per spec test, none inside a loop over functions, blocks or ops. The wasm arm gains the most -- writeWasm now stages a core module and an embed file and publishes the finished component with one File.rename, so plus two changeExtension, one rename and one delete -- and that is still O(1) sitting between two wasm-tools subprocess spawns. ///// CPU COLUMN: NO CLAIM MADE, and it could not make one -- four syscalls is about 10^5 ticks against a 32-giga-tick rung-5 compile. The un-bucketed CPU delta read -7.5M, -3.0M, -3.5M, -5.2M, -58.6M and +7.9M across the rungs, both signs, entirely inside this box's band. phase:parse's bend (parent x1.93 x2.18 x2.46 x2.87 x3.29, head x2.02 x2.25 x2.42 x2.88 x3.22, 38.7 percent of the rung-5 compile) is UNCHANGED between the two binaries and is PLAN.md Workstream O item 122, already filed with its cure and its re-measure trigger. Re-confirmed here, not rediscovered. ///// CORPUS BLIND SPOT THAT MATTERS FOR THIS ROW: every rung of this ladder SUCCEEDS, so it never exercises the path the rung was built to fix. The +14/+530 is the cost on the SUCCESS path; a FAILING compile pays 13 allocations, about 490 bytes and about 155 us of it (requireOutputWritten never runs), and no column here will ever show that. ///// Build 0 warnings; suites x64 1851/0 in 18.4 s, x64-linux 1745/0 in 58.4 s, wasm32-wasi 1654/0 in 26.2 s, C# bootstrap 3105/0 in 74.4 s, all exit 0 and no leaks; specs-shv2/fragments and specs/fragments byte-identical (git status empty after all four runs). | 39,292,065 | 74,342,743 | 148,477,850 | 298,363,129 | 606,474,957 | 1,241,767,623 |
| 2026-07-28 | P1.7a slice 2b-v: THROWING WITNESS DISPATCH (StdOp.witnessTryCall, MaxonOp.witnessTryDispatch, an isThrowing flag on witnessDispatch, a ThrowsClause on InterfaceMethodSig, a TryTarget union replacing a bare callee ByteArray). NO CODE CHANGE FROM THE OPTIMIZER; the measurements below say why, and they CORRECT the implementer's attribution. ///// A/B PROTOCOL: the parent (33fef8c52) was rebuilt in a SIBLING worktree whose root path is 34 characters, the SAME LENGTH as this one, from the IDENTICAL bootstrap binary (md5 93ce2ab682b40d4d2b34031cd07f347a, copied not rebuilt), so the phase:load path-length artifact is ZERO rather than subtracted and the two shv2 binaries differ only in shv2 source. phase:load reads delta 0 allocations and 0 bytes at all six rungs, which is that protocol checking itself. ///// THE DELTA, head minus parent: rung0 +148 allocs / +106,968 bytes; rung1 +242 / +198,272; rung2 +430 / +380,976; rung3 +806 / +746,288; rung4 +1,558 / +1,477,008; rung5 +3,062 / +2,938,352. Delta ratios x1.64 x1.78 x1.87 x1.93 x1.97 across a DOUBLING ladder, converging on x2.00 => LINEAR. +0.021 percent allocations and +0.236 percent bytes at rung 5. The implementer's figures reproduced to the digit. ///// ATTRIBUTION 1 IS WRONG, AND A KNOB SAYS SO RATHER THAN ARITHMETIC. The implementer read the added allocations as '+40 bytes per INTERFACE METHOD, paid twice', because phase:signatures reads EXACTLY +40.00 bytes per added allocation at all six rungs. Both halves are wrong, and the story could not have been right: scale-test's corpus generates NO interfaces at all, so a per-interface-method cost is a zero this ladder is structurally incapable of measuring. THREE KNOBS, each varying ONE thing with the rest fixed, all run head-vs-parent through --log=compiler:debug's per-phase memory table: (a) INTERFACE-METHOD COUNT K = 0 / 20 / 60 in one file -> allocations FLAT at +3 signatures / +3 parse, and signatures BYTES flat at +120. InterfaceMethodSig's new ThrowsClause field allocates NOTHING; it costs +8 bytes INLINE per interface method, visible only in phase:parse (+120 / +280 / +600, slope exactly 8.0). It allocates nothing because the PARENT already called parseOptionalThrowsClause() and threw the result away -- head merely keeps it. (b) FUNCTION COUNT N = 0 / 200 / 400 / 800 in one file -> allocations FLAT at +3 / +3 again. Not per function either. (c) FILE COUNT F = 2 / 6 / 21 -> allocations = F + 2, IN EACH PASS, at every size (+4, +8, +23). SO THE ADDED ALLOCATIONS ARE PER FILE PER PARSE PASS -- one for the signatures pass and one for the parse pass -- which is a thing this corpus DOES generate (its file count doubles with the rung: 9 + 23 x 2^k, reproducing +96 / +165 / +303 / +579 / +1,131 / +2,235 in phase:signatures exactly). ///// AND THE 40 BYTES IS TWO UNRELATED COSTS THAT HAPPEN TO SUM TO 40. Proven by adding two MORE ThrowsClause fields to Parser and rebuilding: allocations went up EXACTLY x3 at every file count (3->9, 4->12, 8->24, 23->69), while the byte delta fit a + 24c for field-count c with residue zero in all four cases -- a ThrowsClause.none box is 24 BYTES, and the constant a was exactly 16 x (number of Parsers) in all four, which is the OTHER two new Parser fields (lastWitnessDispatchIndex, lastWitnessDispatchName) as 16 bytes of INLINE struct growth. 24 + 16 = 40. An attribution that only FITS the numbers is not an attribution. ///// ATTRIBUTION 2 IS CORRECT, AND NOW PROVEN BY KNOB RATHER THAN INFERRED: +8 BYTES ON EVERY StdOp BOX, WITH ZERO EXTRA ALLOCATIONS, because witnessTryCall's SEVEN fields make it the widest variant in the flat union (previous maximum six: witnessCall, osReadOverlapped) and the bootstrap sizes a union's payload box by its WIDEST CASE. THE KNOB: adding ONE dummy 8th ValueId field to witnessTryCall and rebuilding EXACTLY DOUBLED the byte delta at three independent program sizes -- 280 -> 560, 9,880 -> 19,760, 38,680 -> 77,360 -- with the allocation delta unchanged at zero. Slope 48.0 bytes per function with one field and 96.0 with two, i.e. 6 StdOp boxes per trivial function x 8 bytes per field. At rung 5 the tax lands ENTIRELY in the three phases that allocate StdOps -- lowerMaxonToStd +2,297,176 bytes, foldConstOperands +417,752, insertRangeChecks +72, all with delta 0 allocations -- and is EXACTLY ZERO in every Maxon-dialect phase (parse, merge, semanticCheck) and in isel, regalloc, encode, link and writeExe. 2.71 MB of a 1.2447 GB compile: 0.218 percent of bytes, 0 allocations, strictly O(StdOp count). ///// AND MaxonOp DID NOT GROW, WHICH IS WHY THE MAXON-DIALECT PHASES READ ZERO: witnessDispatch went 5 -> 6 fields and witnessTryDispatch is 6, but MaxonOp's widest variant is tryCall at SEVEN and always was, so the Maxon box is untouched. The rung got that for free and the per-phase numbers confirm it independently of reading the union. ///// THE 7-FIELD WIDTH IS PAID, DELIBERATELY, AND HERE IS THE RULE RATHER THAN A DEFECT. It is not narrowable without giving up the thing the op is for: all seven fields are witnessCall's six VERBATIM plus the one thing tryCall adds to call (a second def, errorFlagId), which is precisely what lets StdToX64Conversion share the witnessCall lowering rather than re-derive it. Packing methodIndex with argFloatMask would be a magic bitfield; a bool discriminator on witnessCall instead of a distinct variant is the explicitly ruled-out v1 isVoid mistake that shv2's union-typed StdReturnType exists to avoid. STANDING RULE: ANY NEW WIDEST StdOp VARIANT TAXES EVERY StdOp ALLOCATION BY 8 BYTES PER FIELD. The measured price of the 7th is 0.218 percent of a self-scale compile's bytes and zero allocations; an 8th would cost the same again. ///// NO SUPERLINEARITY IN WHAT THE RUNG ADDED, MEASURED ON CORPORA BUILT FOR IT -- because a zero from THIS ladder would have meant nothing (it emits no interfaces, no generics and no witness dispatch; its own manifest says so). Three hand-built ladders, each doubling: (1) WITNESS-DISPATCH ladder, 64 -> 1024 `try self.item.digest()` sites through a `where T is Digest` witness, one throwing interface method: every phase x2.00 in ALLOCATIONS (total x1.85 x1.92 x1.96 x1.98) and every phase x2.00 in CPU TICKS, minimum of 3 runs (total x1.73 x1.88 x1.96 x2.01). (2) CONFORMANCE ladder, 128 -> 1024 conforming types against a 2-method throwing interface, so validateThrowsConformance -- the check this slice added, and it runs in BOTH directions -- is exercised on 256 -> 2,048 pairs: every phase x2.00, total x1.95 x1.97 x1.99. (3) WIDE-INTERFACE ladder, M interface methods x M dispatch sites, 64 -> 1024: allocations x2.00 throughout. The try desugar's rewrite is index-addressed, not scanned (rewriteLastCallToTryCall reads opRefs[count-1] and commitWitnessTryDispatchRewrite does ops.set(opIdx), both O(1)), which is why (1) stays flat. ///// THE CONSTANT FACTORS WERE LEFT ALONE, ON PURPOSE. The code is already alive to the ThrowsClause.none box and avoids it in both hot paths -- Parser.maxon:3793 gates the pre-scan's clause on the `throws` KEYWORD so a non-throwing function allocates neither a clause nor an entry, and SemanticCheck.maxon:1105 uses throwsMap.contains rather than `get otherwise ThrowsClause.none` for exactly this reason ('would box a none for every one of the program's calls that is not to a throwing callee'). The one box this rung adds is 1 per Parser = 2 per file per compile, about 1,490 of 14.8M allocations at rung 5 (0.010 percent), and removing it would mean replacing a `var` field with a new managed global. Not taken: that is a constant factor, and the mandate is growth curves. ///// ONE TERM FOUND, MEASURED, AND FILED RATHER THAN FIXED -- PRE-EXISTING (slice 2a), UNCHANGED BY THIS RUNG. interfaceMethodIndex (IrInterface.maxon:302) and requireInterfaceForParse (Parser.maxon:18542) are LINEAR SCANS run once per witness-dispatch site, over the interface's methods and over the file's interfaces respectively -- a linear lookup inside a walk over dispatch sites, so the term is O(dispatch sites x interface methods). ISOLATED WITH A SIZE-MATCHED KNOB, which is the only way to see it: same M methods, same M dispatch sites, only the TARGETED method's position moved from FIRST to LAST (programs 190,327 vs 191,351 bytes, a 0.5 percent difference that is just longer digit strings). phase:parse costs +13.7 percent at M=D=256, +39.3 percent at 512, +36.2 percent at 1024, minimum of 5 runs each. WHAT IS NOT CLAIMED: a confirmed quadratic EXPONENT. The isolated delta grew 12.7M -> 70.1M -> 133.4M ticks where a clean D x M term predicts x4.00 per doubling, and x5.52 then x1.90 is not that -- the cpu-percent column quantizes at 0.1 percent and the first-position baseline moves too, so this measurement can establish that the term EXISTS and is per-dispatch-site, and cannot establish its exponent. At an absurd 1024-method interface it is worth about 5 percent of the compile. RE-MEASURE TRIGGER: an interface wider than a few dozen methods (machine-generated conformance), or one generic body with many dispatch sites against one wide interface. CURE: index IrInterface.methods by name instead of scanning. ///// CPU COLUMN: NO CLAIM ON THE A/B. The un-bucketed per-rung CPU delta read both signs and sat inside this box's band; four syscall-free 40-byte boxes per file cannot be priced against a 34-giga-tick rung-5 compile. phase:parse's known bend is PLAN.md Workstream O item 122, unchanged between the two binaries and re-confirmed, not rediscovered. ///// GATES: build exit 0 with zero warnings; specs-shv2 x64 1861 passed / 0 failed, exit 0, no leak (no exit 101); specs-shv2/fragments byte-identical -- `git status --short specs-shv2/fragments/` empty after the suite run, and the whole working tree clean, which is what proves this pass emitted byte-identical codegen. | 39,396,366 | 74,538,348 | 148,856,159 | 299,106,750 | 607,949,298 | 1,244,703,308 |
| 2026-07-28 | P1.7a slice 2b-iv-B: WASM WITNESS LOWERING (bakeFuncTableIndexRelocs baking a funcref TABLE INDEX into the same .rdata slot x64 bakes a .text VA into, witnessCall/witnessTryCall interned through internIndirectCallType, emitWitnessCall/emitWitnessTryCall, the funcref table enabled on a non-empty pendingRdataRelocs, and the compile-time refusal requireIndirectlyReachableParamsAreMachineWords). NO CODE CHANGE FROM THE OPTIMIZER; the measurements below say why. ///// A/B PROTOCOL: the parent (a2543d15c) was rebuilt in THIS worktree, same path, same bootstrap (bin/maxon.exe sha256 d00fc7df6ebae372cd7f83878913f1ea4d1aee08890fc1bc0373068c3ab72543), and both scale-tests were run from the same 33-character root, so the path artifact is zero rather than subtracted. head sha256 0f7b9cda8d2c86c48e71edc653a24d40400fb396e9e3fb006d79e8b50ea54647, parent 9acbc177db2e1e73e2e34530d7272ee5c7ffffcf3dd22c848b55730e9f549548. RESULT: allocations AND bytes bit-identical at all six rungs, 574,428 / 39,395,954 through 14,805,204 / 1,244,702,896. The implementer's delta-0 claim is confirmed to the digit. ///// THE -412 IS NOW ATTRIBUTED, AND IT IS THE CHECKOUT PATH LENGTH AT 202 BYTES PER CHARACTER. Measured, not inferred: two directory junctions onto this same worktree at 34 and 35 characters, identical binary, identical corpus. The delta versus the committed row reads -412 at 33 characters, -210 at 34, -8 at 35 -- exactly +202 per character, twice over. Located per character: phase:load +134, phase:parse +8, un-bucketed +60, FLAT at every rung (rung 0 and rung 1 each move by exactly those amounts, so it is a fixed set of path buffers crossing size classes, not a per-corpus-file cost). The committed 2b-v row was therefore recorded from a root two characters longer than this one, plus an 8-byte residue. ///// THE IMPLEMENTER'S SUB-CLAIM IS REFUTED AND ITS CONCLUSION UPHELD. It measured phase:load at 1 byte per character (67232 -> 67234 for a 2-character-longer name) and concluded that path length could not produce -412. The 1-byte figure is real and I reproduced it exactly -- 67,502 -> 67,500 for a 2-character-shorter binary NAME on a single-file compile, where the only path in phase:load is argv[0]. It simply does not generalize to the scale corpus, whose phase:load holds many more path buffers and moves 134 bytes per character of ROOT. Right answer, wrong reason: an artifact measured in one bucket does not price the same artifact in another. ///// THE DELTA-0 ABOVE MEASURES UNREACHED, NOT FREE, AND THE IMPLEMENTER SAID SO ITSELF. This corpus generates no interface, no witness dispatch and no managed-opaque-T descriptor, so globalData.pendingRdataRelocs is EMPTY at every rung and BOTH new walks execute ZERO iterations. A ladder was built for them (temp/genwitness.sh): N conformer types against a 2-method interface, every one dispatched through a single `where T is Digest` generic body, N = 32 -> 1024 doubling, so the reloc list doubles 64 -> 2,048. Confirmed per-conformer rather than shared, by the new refusal itself: fed a bool parameter it names `__witness_P0.Digest'+32`, i.e. one witness blob per conformer with two method slots. ///// WASM IS THE ONLY TARGET THAT RUNS THE NEW CODE, and a host-only scale-test cannot see any of it. buildWasmBackend bills the entire backend to phase:encode, so bakeFuncTableIndexRelocs, requireIndirectlyReachableParamsAreMachineWords, the two newly-interned op kinds and emitWitnessCall all land in that one bucket. Compiled by hand with --target=wasm32-wasi --metrics, minimum of 3 per rung. phase:encode ALLOCATIONS 9,280 -> 17,607 -> 34,221 -> 67,414 -> 133,763 -> 266,417 (x1.90 x1.94 x1.97 x1.98 x1.99); phase:encode BYTES 550,083 -> 1,084,886 -> 2,140,163 -> 4,262,508 -> 8,565,812 -> 17,260,267 (x1.97 x1.97 x1.99 x2.01 x2.02); phase:encode CPU 9,619,410 -> 18,823,224 -> 38,886,862 -> 71,012,108 -> 146,346,498 -> 298,904,609 ticks (x1.96 x2.07 x1.83 x2.06 x2.04, the 1.83 inside the band). LINEAR IN RELOCS in all three columns across a 32x span. Neither walk is accidentally quadratic through a map: calleeFuncIdx.get and calleeParamTypes.get are ByteArray-keyed Map lookups built once from module.functions, GlobalDataTable.rdataOffsetOf is a Map lookup too, and the guard's inner loop is over ONE callee's parameter list. ///// AND IT IS CORRECT AT SCALE, not merely fast: the N=1024 rung -- 2,048 baked funcref table indices -- compiles and RUNS under wasmtime with exit 0. ///// X64 A/B ON THE SAME SATURATED LADDER: EXACTLY 0 ALLOCATIONS AND 0 BYTES IN EVERY PER-PHASE BUCKET AND IN THE TOTAL, at N=32 and N=1024 alike. That is where the shared front-end changes (Parser.interfaceReturnMaxonType claiming ExitCode, builtinProtocolResultStdType) get priced, because the parent cannot be A/B'd on wasm at all -- it refuses every program holding a funcAbs64 relocation, and replacing that blanket refusal is the whole slice. ///// A SELF-CORRECTION WORTH MORE THAN THE RESULT: my first x64 A/B read +7 allocations / +330 bytes, flat at all six rungs, and I was one step from filing it as a per-interface cost. It was the same path artifact one level down -- the two binaries sat at ./maxon-shv2/.maxon/maxon-shv2.exe and ./temp/parent-shv2.exe, 16 characters apart in argv[0]. Re-run with both at 20-character paths the delta is exactly zero, in every bucket. A FLAT non-zero delta between two binaries is a path check before it is a finding. ///// CPU COLUMN: NO CLAIM ON THE A/B. Single sample; the dominant buckets read +0.4 percent phase:regalloc, +0.1 percent regalloc:splitting, +0.2 percent total -- inside the band -- while the sub-1-percent phases swing +-28 percent, which is what an un-repeated CPU sample looks like. ///// NOTHING WAS CHANGED, AND TWO CANDIDATES WERE DECLINED ON PURPOSE. (1) The two walks over pendingRdataRelocs could be fused into one, halving the map lookups. DECLINED: the guard is a compile-time REFUSAL and must finish before any slot is patched, so fusing would panic with the .rdata image half-baked -- and it is a constant factor on a walk worth low single digits of one phase. (2) emitIndirectCallOp rebuilds the signature key (a WasmValTypeArray plus an encoded ByteArray) per indirect-call op just to look up the interned type index: 2 allocations per witness call, about 4,096 of phase:encode's 266,417 at N=1024, 1.5 percent. DECLINED: pre-existing (emitCallIndirect did exactly this before this slice, which merely routes two more op kinds through it), O(1) per op, and rebuilding through the same two helpers internIndirectCallType built it with is the stated invariant that keeps the call-site immediate from disagreeing with the interned type. Trading a correctness-by-construction invariant for 1.5 percent of one phase's allocations is not the mandate. ///// TWO KNOWN TERMS RE-CONFIRMED, NOT REDISCOVERED. This ladder bends in phase:parse (x2.56 x3.05 x2.80 on wasm, where no register allocator runs) and in regalloc:splitting (x3.08 x3.20 x3.20 on x64). Those are Workstream O item 122 -- interfaceMethodIndex and requireInterfaceForParse as linear scans per witness-dispatch site, filed by the 2b-v pass -- and the budgeted splitter re-liveness (ARCHITECTURE.md:1336-1345). Both read allocation-identical between the two binaries, so neither moved, and neither is this slice's to fix. ///// GATES: build exit 0 with zero warnings; specs-shv2 x64-windows 1865 passed / 0 failed, exit 0, no leak; specs-shv2 wasm32-wasi 1754 passed / 0 failed, exit 0, no leak; `git status --short specs-shv2/fragments/` empty after BOTH suite runs and 0 files changed versus main, and the whole working tree clean -- which is what proves this pass emitted byte-identical codegen on both targets. | 39,395,954 | 74,537,936 | 148,855,747 | 299,106,338 | 607,948,886 | 1,244,702,896 |
| 2026-07-28 | P1.8 SLICE A -- `for` loops (numeric ranges + Array iteration). NO CODE CHANGE FROM THE OPTIMIZER: nothing measured superlinear, and the two constant-factor candidates were both unmeasurable (see REJECTED below). The deliverable is a COMMITTED LADDER, because this corpus cannot see the rung at all. ///// A/B PROTOCOL: the parent (016751f8b) was rebuilt in THIS worktree from this worktree's bootstrap and both binaries were measured at the SAME canonical path (maxon-shv2/.maxon/maxon-shv2.exe, swapped between runs), so the two differ only in shv2 source. ⚠ THE COMMITTED ROW ABOVE IS NOT A VALID BASELINE HERE: it was written in a 51-character worktree root and this one is 28, and the in-tree parent reproduces it at +14 allocations / -4,109 bytes FLAT at all six rungs -- a path artifact, not a change. So the delta column of THIS row carries that artifact too (+156 = +142 real, +14 path). Every attribution below is head-minus-in-tree-parent, which is artifact-free. ///// THE DELTA, head vs in-tree parent: +142 ALLOCATIONS, FLAT AT ALL SIX RUNGS across a 32x ladder -- O(1) PER COMPILE, not per function, per block, per op or per binding access. Bytes are 6,288 + 1,008 * 2^rung: +7,296 +8,304 +10,320 +14,352 +22,416 +38,544, whose successive differences are 1,008 2,016 4,032 8,064 16,128 -- EXACTLY DOUBLING, i.e. perfectly linear in corpus size. ATTRIBUTED WITH NO RESIDUE: the +142 is +108 `unattributed` (building the new `__arr_get_unchecked` Std graph, which sits outside every CompilePhase probe) plus +28 `phase:isel` (selecting it), once per compile; the 1,008 * 2^rung bytes is 8 bytes per record on `LoopPhiVar.entryValue` and `LoopContext.continueTargetId`, paid by the corpus's `while` loops -- the corpus has no `for`, so this whole byte term is the OLD construct carrying the new field. The implementer's own claim (`6288 + 1008 * 2^n`) reproduces to the digit. ///// ⛔ THE CORPUS IS STRUCTURALLY BLIND TO THIS RUNG, and a Delta-0 from it would be the instrument's blind spot and not the cost. `ScaleCorpus` generates ZERO `for` loops -- it could not, they did not parse until this commit -- so the lowering, the element read and `Parser.iterationLockedBindings` are invisible in allocations, bytes AND CPU alike. The cure is precedent, not invention (P1.7a 2b-ii built its own generics ladder for the same reason): this rung commits `maxon-shv2/Testing/ladders/genfor.sh <loops> <depth> <accesses> <array or range or noloop> <out>`, whose knobs are independent -- program size is loops x accesses, so DEPTH MOVES ALONE. ///// THE READING THAT MATTERS IS THE NO-LOOP CONTROL, because the lock is read at FOUR doors on ordinary binding-access paths (`immutableReceiverNameOf`, `parseFieldChainCall`, `selfFieldIsWritable`, `requireMutableBinding`) -- a per-access tax on every program for a feature it does not use is the exact shape of the defect the P1.7a 2b-i pass found. Measured `genfor.sh N 1 2 noloop` at N = 64/128/256/512/1024, head against the in-tree parent: `phase:parse`, `phase:lex`, `phase:signatures`, `phase:merge`, `phase:lowerMaxonToStd` and `phase:regalloc` are IDENTICAL TO THE DIGIT at all five rungs (parse: 14,816 / 27,842 / 53,863 / 105,877 / 209,874 both ways). The entire delta is the same +142 flat, in the same two buckets. NOTHING PER ACCESS. That is what the `count() == 0` guard in front of the walk buys, and it is now measured on a program built to defeat it rather than on a corpus that never enters it. ///// NOTHING SUPERLINEAR, WITH THE LADDERS THAT ESTABLISH IT. loops 64 -> 1,024 (`genfor.sh N 1 2 array`): `phase:parse` allocations x2.00 x2.00 x2.00 x2.00, CPU x2.04 x2.02 x1.99 x1.99, and every other phase x2.00. `range` mode (literal bounds, so the lock stack stays EMPTY): x2.00 in every phase in both columns. accesses 1 -> 8 at a fixed loop count: linear per marginal statement. DEPTH, constant size (`genfor.sh D D 512/D array` -- one function, exactly 512 accesses at every rung, 1,043 -> 1,057 lines): `phase:parse` allocations 6,541 / 6,674 / 6,940 / 7,469, an excess over depth 1 of 133 / 399 / 928 = 133 x (1, 3, 7), EXACTLY proportional to (D-1). That is `namesAssignedIn`'s documented Theta(tokens x depth) scan, LINEAR IN DEPTH, and the lock walk adds nothing measurable on top of it. `phase:regalloc` reads x7.21 from depth 1 to 2 then x1.68 x1.95 -- a MODE CHANGE (the splitter engages at all once two loops nest), not a curve, and the identical shape appears on a nested-`while` ladder built against the PARENT binary (x5.40 x4.43), so it is not this rung's. ///// STAR -- THE DEPTH KNOB HAS A CEILING THE COMPILER ITSELF ENFORCES, which is why `bindingIsIterationLocked`'s linear scan cannot go quadratic on anything that compiles. Every `for` mints an index phi live across every loop nested inside it, so depth D holds D values at once and E5001 refuses: MEASURED, depth 10 compiles and depth 11 is "needs 1 more register(s) than are available". `gennest.sh` evades exactly this by giving every `while` level the SAME guard variable; a `for` cannot, because the counter is minted by the lowering and not by the program. So the lock stack is bounded by ~10 in any program that builds, and an O(depth) scan behind an empty-stack guard is O(1) in practice. ///// PRE-EXISTING BEND RE-CONFIRMED, NOT REDISCOVERED: `phase:parse`'s corpus growth is x3.18 on the parent and x3.19 on head at rung 5 -- UNCHANGED -- and it is PLAN.md Workstream O item 122 (the Theta(bindings in scope x constructs) walk `mintLoopHeaderPhis` and `assignedBindingsIn` share). `for` now joins `while`, `if` and `match` in triggering it and makes it NO WORSE: a hand ladder of N/2 mutable bindings then N/2 loops in one function reads `phase:parse` CPU x2.00 x2.17 x2.29 x2.67 x3.03 in the `for` spelling against x2.12 x2.12 x2.40 x2.61 x3.09 in the `while` spelling ON THE PARENT BINARY, with allocations flat at x1.99 in both -- the same curve, and exactly the shape a memory-only instrument cannot see. Not fixed here: it is not this rung's, the obvious inversion is already measured a NET LOSS in `namesAssignedIn`'s header (`phase:parse` x3.5 on the corpus), and the real cure named there is a bottom-up carried-set redesign. ///// ONE UNATTRIBUTED CPU READING, REPORTED RATHER THAN LAUNDERED: `phase:signatures` runs ~+7% on the corpus (and ~+10% on the no-loop ladder) head against parent, FLAT at every rung, with allocations IDENTICAL TO THE DIGIT and the growth ratio unchanged at x2.01 both ways. It is a CONSTANT FACTOR, not a curve. The only new per-token work in that phase is one enum comparison in `opensBlockAt` (`or kind == TokenKind.for`), and that cannot be it by two orders of magnitude: the rung-5 delta is 56.9M ticks over roughly 5 x 10^5 tokens, about 114 ticks per token, against ~1 for a compare. Most likely code layout between two different binaries -- `phase:lex`, whose source is IDENTICAL, scatters +-5% in BOTH directions across the same pair. Worth 0.23% of the compile; recorded so the next reader does not rediscover it as new. ///// REJECTED, ON THEIR NUMBERS: (1) hoisting the `count() == 0` test in front of `iterationLockNameAt`'s `scope.contains` + `scope.getInfo`, so a field-chain array method call in a loop-free program skips a hash lookup. It allocates nothing either way, so the exact column cannot price it, and the ladder built to maximise it (one `b.items.push` per statement, 1,024 of them) reads `phase:parse` CPU inside the layout band. A change I cannot measure is not an optimization. (2) The `count() == 0` guard the implementer says it PREDICTED would matter and MEASURED identical -- kept, and now justified by a measurement it did not have: on the `noloop` control the guard is what makes the four doors cost the digit-identical zero above. ///// REPORTED TO THE COORDINATOR, OUT OF THIS PASS'S SCOPE (a correctness question, not a cost): `for i in n upto 3 'l' ... n = n + 1 ... end` is E2013 in shv2 and COMPILES AND RUNS in the bootstrap oracle (returns 3). `lockIterationSource` is called BEFORE the range/array fork, so a RANGE whose start bound is a NAME locks that name for the whole body -- although the rung's own header states both bounds are evaluated once in the preheader, and the END bound is correctly writable (`for i in 0 upto n` with `n = n + 1` compiles). An asymmetry between the two bounds of one range, and a divergence from the oracle on a legal program. | 39,295,252 | 74,346,938 | 148,484,061 | 298,373,372 | 606,493,264 | 1,241,802,058 |
| 2026-07-28 | P1.8 SLICE A -- INDEPENDENT REVIEW. Two reachable defects fixed (a 5th mutation door segfaulting on all four spellings of a self-field write during `for … in` over that field; a missing range-bound type guard that PANICKED the x64 emitter on a float range and silently accepted a bool one), three duplications single-sourced, 15 pinning tests. Suite 1891/0 -> 1906/0. ///// COST OF THE REVIEW ITSELF: +0 ALLOCATIONS at every rung, and BYTES -64 -112 -208 -400 -784 against the optimizer's row -- successive differences -48 -96 -192 -384, exactly doubling, i.e. linear in corpus size. That is `LoopContext.headerId` deleted: 8 bytes off every loop context, paid back by the corpus's `while` loops. The field was WRITE-ONLY (declared, constructed, written at both loop parsers, read NOWHERE) and its value coincided with `continueTargetId` for `while` and not for `for` -- the trap with the check removed. The two new guards are behind the existing `count() == 0` test and allocate nothing. ///// ***THE PREVIOUS ROW'S ONE UNATTRIBUTED READING IS NOW ATTRIBUTED, AND ITS STATED HYPOTHESIS IS REFUTED BY ITS OWN CONTROL.*** That row recorded `phase:signatures` ~+7% head-vs-parent, flat, allocations identical, and offered "most likely code layout between two different binaries", citing `phase:lex` (IDENTICAL SOURCE) as scattering +-5% in BOTH directions. Re-measured here against a THIRD, independently built binary (review-head vs `main` rebuilt in this worktree, both run from the same canonical path, 5 rungs, repeat=5): `phase:signatures` is +8.6% +10.4% +8.1% +8.4% +8.6% -- CONSISTENTLY POSITIVE at every rung. The control does NOT scatter: `phase:lex`, whose source is identical in all three binaries, reads -2.4% -2.7% -3.1% -3.1% -1.8% -- consistently NEGATIVE and three to five times smaller; `phase:merge`, also untouched, is -2.2%..+0.7%. So the binary-identity component on this pair is about -2 to -3% with a STABLE SIGN, and signatures sits ~11 points away from it in the OPPOSITE direction. It is real work, not layout. ***>>> CORRECTION 2026-07-28, AND THIS SENTENCE IS THE ONE THAT WAS WRONG: THE +8.6% DOES NOT SURVIVE INTERLEAVING AND IS A MEASUREMENT-PROVENANCE ARTIFACT.*** User hypothesis ("some of the measurements were done on a different machine"), tested on an IDLE box with the two binaries swapped at ONE canonical path and the runs INTERLEAVED P/H/P/H (4 pairs, rungs=3, repeat=3, both phases read from the SAME run's table): `phase:signatures` reads +1.03% +0.55% +1.23% (overall mean +0.94%) and CHANGES SIGN across pairs (-3.43% +0.68% +3.94% +3.74%), while the control `phase:lex` REPRODUCES at -2.24% -1.51% -1.31% (mean -1.69%). The entire claim above rested on signatures being "CONSISTENTLY POSITIVE at every rung"; interleaved, that consistency is gone and the effect cannot be distinguished from zero. WARNING -- WHY THE `phase:lex` CONTROL DID NOT CATCH IT, which is the transferable lesson: lex excludes a UNIFORM factor, but a drift confined to a TIME WINDOW need not move every phase equally -- lex reproduced and signatures did not, i.e. the two phases have DIFFERENT sensitivity to the drift, which is exactly what disqualifies one as a control for the other. A stable SIGN across rungs is NOT evidence of a real effect when all head runs sit in one time window and all parent runs in another; only INTERLEAVING separates code from clock. UNAFFECTED AND STILL TRUE: the BYTE finding (allocation count identical to the digit, bytes +768/+1,320/+2,424/+4,632/+9,048 -- the records that already existed got bigger). Bytes are exact and reproducible; it was only the CPU story built on top of them that failed. ///// WHAT IT IS: `phase:signatures` allocation COUNT is identical to the digit at every rung (14,061 / 23,816 / 43,209 / 81,854 / 158,940 both binaries) while its BYTES move +768 +1,320 +2,424 +4,632 +9,048 -- successive differences 552 1,104 2,208 4,416, exactly doubling, over a constant +216. Same count, more bytes: THE RECORDS THAT ALREADY EXISTED GOT BIGGER, and nothing new is allocated. That is the 8-byte fields the rung added (`Parser.iterationLockedBindings` -- a `Parser` is built per top-level constant by `SignatureIndex.walkConstInitializer` -- plus `LoopPhiVar.entryValue` and `LoopContext.continueTargetId`). ///// ⚠ THE BYTE GROWTH IS THE MARKER, NOT THE COST: +9,048 bytes on 7,316,360 is +0.12%, which cannot buy +8.6% of CPU. What the bytes prove is WHICH objects changed; the CPU is spread across the several constant-factor additions on that phase's paths (a wider `Parser` record and its constructor, `opensBlockAt`'s extra arm, `parseBlockBody`'s new parameter and empty-block test, `edgeArgsFor`'s selector, `fieldChainMethodCallAt` now going through `fieldChainEndAfter`). Isolating which one needs PER-FUNCTION CPU attribution, which shv2 does not have -- that is the instrument gap this reading names, and it is why "unattributed" was the honest word before and "real, unresolved between five candidates" is the honest word now. Worth ~0.4% of the compile (signatures is ~4.8% of it). NOT chased: every candidate is the price of the feature, and no single one is measurable apart from the others. ///// REJECTIONS RE-CHECKED AND UPHELD: hoisting `count() == 0` in front of `iterationLockNameAt`'s two hash lookups stands rejected -- confirmed it has exactly ONE caller (`parseFieldChainCall`, an array-typed field-chain receiver with a trailing call), so it is a narrow path and not a per-statement tax, the maximising ladder read inside the band, and the guard it would duplicate already exists one call deeper in `bindingIsIterationLocked`. Shipping it would be the unmeasurable change the previous pass correctly declined. | 39,295,188 | 74,346,826 | 148,483,853 | 298,372,972 | 606,492,480 | 1,241,800,506 |
| 2026-07-28 | P1.8 SLICE A -- INDEPENDENT REVIEW. Two reachable defects fixed (a 5th mutation door segfaulting on all four spellings of a self-field write during `for … in` over that field; a missing range-bound type guard that PANICKED the x64 emitter on a float range and silently accepted a bool one), three duplications single-sourced, 15 pinning tests. Suite 1891/0 -> 1906/0. ///// COST OF THE REVIEW ITSELF: +0 ALLOCATIONS at every rung, and BYTES -64 -112 -208 -400 -784 against the optimizer's row -- successive differences -48 -96 -192 -384, exactly doubling, i.e. linear in corpus size. That is `LoopContext.headerId` deleted: 8 bytes off every loop context, paid back by the corpus's `while` loops. The field was WRITE-ONLY (declared, constructed, written at both loop parsers, read NOWHERE) and its value coincided with `continueTargetId` for `while` and not for `for` -- the trap with the check removed. The two new guards are behind the existing `count() == 0` test and allocate nothing. ///// ***THE PREVIOUS ROW'S ONE UNATTRIBUTED READING IS NOW ATTRIBUTED, AND ITS STATED HYPOTHESIS IS REFUTED BY ITS OWN CONTROL.*** That row recorded `phase:signatures` ~+7% head-vs-parent, flat, allocations identical, and offered "most likely code layout between two different binaries", citing `phase:lex` (IDENTICAL SOURCE) as scattering +-5% in BOTH directions. Re-measured here against a THIRD, independently built binary (review-head vs `main` rebuilt in this worktree, both run from the same canonical path, 5 rungs, repeat=5): `phase:signatures` is +8.6% +10.4% +8.1% +8.4% +8.6% -- CONSISTENTLY POSITIVE at every rung. The control does NOT scatter: `phase:lex`, whose source is identical in all three binaries, reads -2.4% -2.7% -3.1% -3.1% -1.8% -- consistently NEGATIVE and three to five times smaller; `phase:merge`, also untouched, is -2.2%..+0.7%. So the binary-identity component on this pair is about -2 to -3% with a STABLE SIGN, and signatures sits ~11 points away from it in the OPPOSITE direction. It is real work, not layout. ///// WHAT IT IS: `phase:signatures` allocation COUNT is identical to the digit at every rung (14,061 / 23,816 / 43,209 / 81,854 / 158,940 both binaries) while its BYTES move +768 +1,320 +2,424 +4,632 +9,048 -- successive differences 552 1,104 2,208 4,416, exactly doubling, over a constant +216. Same count, more bytes: THE RECORDS THAT ALREADY EXISTED GOT BIGGER, and nothing new is allocated. That is the 8-byte fields the rung added (`Parser.iterationLockedBindings` -- a `Parser` is built per top-level constant by `SignatureIndex.walkConstInitializer` -- plus `LoopPhiVar.entryValue` and `LoopContext.continueTargetId`). ///// ⚠ THE BYTE GROWTH IS THE MARKER, NOT THE COST: +9,048 bytes on 7,316,360 is +0.12%, which cannot buy +8.6% of CPU. What the bytes prove is WHICH objects changed; the CPU is spread across the several constant-factor additions on that phase's paths (a wider `Parser` record and its constructor, `opensBlockAt`'s extra arm, `parseBlockBody`'s new parameter and empty-block test, `edgeArgsFor`'s selector, `fieldChainMethodCallAt` now going through `fieldChainEndAfter`). Isolating which one needs PER-FUNCTION CPU attribution, which shv2 does not have -- that is the instrument gap this reading names, and it is why "unattributed" was the honest word before and "real, unresolved between five candidates" is the honest word now. Worth ~0.4% of the compile (signatures is ~4.8% of it). NOT chased: every candidate is the price of the feature, and no single one is measurable apart from the others. ///// REJECTIONS RE-CHECKED AND UPHELD: hoisting `count() == 0` in front of `iterationLockNameAt`'s two hash lookups stands rejected -- confirmed it has exactly ONE caller (`parseFieldChainCall`, an array-typed field-chain receiver with a trailing call), so it is a narrow path and not a per-statement tax, the maximising ladder read inside the band, and the guard it would duplicate already exists one call deeper in `bindingIsIterationLocked`. Shipping it would be the unmeasurable change the previous pass correctly declined. | 39,295,188 | 74,346,826 | 148,483,853 | 298,372,972 | 606,492,480 | 1,241,800,506 |
| 2026-07-28 | P1.7a SLICE 2b-vi (multi-argument witness dispatch) -- OPTIMIZATION PASS. NO CODE CHANGE, and the measurements are why. ///// ***READ THE +148..+3062 DELTA ABOVE AS `main`'s ADVANCE SINCE THE 2026-07-28 ROW, NOT AS THIS RUNG'S.*** That was the implementer's claim and it is CONFIRMED INDEPENDENTLY, not taken: `main` @153d04620 was rebuilt from the same bootstrap in a control worktree whose path is the SAME LENGTH (33 chars, `maxon-p17a-ctrl` vs `maxon-p17a-2bvi`) so the phase:load path-length artifact is zero rather than subtracted, and its scale-test reproduces 574,570 / 1,035,171 / 1,955,167 / 3,792,298 / 7,464,503 / 14,805,346 -- IDENTICAL TO THE DIGIT to the head run, and carrying the identical +148..+3062 delta. A full text diff of both runs' per-phase ALLOCATION and BYTE tables is EMPTY: no phase, at any rung, differs by one allocation or one byte. This rung's own delta against its parent is ZERO everywhere. ///// AND THAT ZERO MEASURES **UNREACHED, NOT FREE** -- PROVEN FROM THE CORPUS, NOT ASSUMED. `--emit-corpus` over all 465 generated files: 276 `interface`, 276 `implements`, 270 `uses` -- and **ZERO `where`, at every rung**. The corpus's own comment says it: "4 unconstrained generic bases". A witness dispatch needs `where T is <Interface>`, so `Parser.parseWitnessMethodOnValue` -- every line this rung touched -- runs EXACTLY ZERO TIMES in the whole ladder. This is the trap slices 2b-ii, 2b-v and 2b-iv-B each hit; the answer again is a ladder built by hand. ///// NEW INSTRUMENT: `maxon-shv2/Testing/ladders/genwitnessargs.sh`, the ARGUMENT-COUNT axis `genwitness.sh` holds at one -- knobs conformers x methods x args x label ORDER (`decl` or `reverse`). The two orders are THE SAME PROGRAM TO THE BYTE (fixed-width labels, the literal always `1`), so subtracting them is the label SCAN and nothing else. ///// PER-CALL-SITE COST OF THE RUNG, exact and differential (same source, both binaries, `--log=compiler:debug`): **+14 allocations and +465 bytes per witness dispatch, IN `phase:parse` ONLY**, and dead flat across a 16x span -- methods 4/8/16/32/64 gave +56/+112/+224/+448/+896 allocations and +1,860/+3,720/+7,440/+14,880/+29,760 bytes, i.e. 14.00 and 465.0 per site every time. Every other phase reads delta-0 to the digit. LINEAR IN CALL SITES; nothing superlinear. ///// ATTRIBUTED, 13 of 14, BY PROBE RATHER THAN BY ARITHMETIC: a throwaway `ValueIdArray.create()`+push added per call site cost EXACTLY 2 allocations (measured +4 at 2 sites, +68 at 34, at both arity 1 and 63) => an array is 2. Six arrays are net-new (`userArgs`, `argLabels`, `argRanges`, `argImmutableNames`, plus `slotCallArgs`' `slotIds`+`filled`; `withWitnessReceiver`'s array REPLACES the old hand-rolled `args`, so it is net 0) = 12. A throwaway `ArgSlotResult.ordered(...)` cost EXACTLY 1 (+2 at 2 sites, +34 at 34, arity-independent) => the union box `slotCallArgs` returns = 1. Total 13; the residual 1 is the assumption that `BoolArray`/`SourceRangeArray` cost what `ValueIdArray` costs, which is the only thing here not separately measured. ///// ARITY IS LINEAR, NOT QUADRATIC. Isolating one call site exactly (one interface, one conformer, one method, N identical dispatch lines, so dN is a call site and nothing else): per-site allocations 36.7 / 41.6 / 52.6 / 79.9 / 126.9 / 214.2 / 375.7 at args 1/2/4/8/16/32/63 -- a clean **~31 + 5.5xA**, residuals +-7% (array growth stepping). A P^2 term would put args=63 near 830, not 376. ///// `WitnessDispatchTarget.paramNames` IS A SHARED REFERENCE, NOT A PER-SITE COPY -- settled by experiment, since shv2 has no per-scope allocation attribution outside the scale corpus and the corpus cannot express a witness dispatch (`build --mm-trace` does not exist). A SECOND, redundant `ByteArrayArray` field added to `WitnessDispatchTarget` and stored from the same source moved the per-site cost by **exactly 0 allocations** at both arity 1 and 63. A `let` field store shares the buffer. ///// `argSlotPosition`'s O(P) FAST PATH IS INTACT -- the check the refactor most endangered, since routing through `indexOfParamIn` unconditionally would restore an O(P^2)-per-call term no correctness test would notice. Re-ran the ladder its own header cites, `genmutchain.sh 1 256 <P>`, on BOTH binaries: `phase:semanticCheck` allocations and bytes IDENTICAL TO THE DIGIT at P=8/16/32/64 (6,333 / 10,975 / 19,742 / 36,749), CPU ticks head/parent 0.97 / 0.96 / 0.97 / 1.01 -- inside the noise band -- and growth x1.73 x1.85 x1.88 per doubling of P (head) against x1.75 x1.83 x1.80 (parent). Linear both. The x3.05 quadratic that header records as removed is still removed. ///// A BONUS THE RUNG DID NOT CLAIM: `Parser.transferConsumedArgs` used to re-spell the mapping inline (`i if label.isEmpty() else info.indexOfParam(label)`) and so had NO fast path; narrowing `argSlotPosition` to take a name LIST is what let it call the shared door, so the parser's consume transfer GAINED the O(P) fast path it never had. ///// THE ONE QUADRATIC THAT EXISTS IS PRE-EXISTING, SHARED, AND BOUNDED -- filed, not fixed. Fully REVERSE-ordered labels miss the fast path on every argument and fall back to `indexOfParamIn`'s scan: ~P^2/2 comparisons per call. Measured on a DIRECT-call ladder both binaries accept (64 functions, first arg positional, rest reverse-labelled), exact per-phase ticks from `--metrics`: at P=63 head 33,495,572 vs parent 33,850,842 -- head marginally FASTER, well inside noise, allocations identical -- so the rung neither introduced it nor moved it, and per-doubling it runs x2.04 x2.63 x3.05 (decl: x1.58 x1.64 x1.78). The witness path inherits precisely the same cost and no more: at P=63 the reverse-minus-decl scan is **420,906 ticks per witness call site vs the direct call's 411,140 -- 2.4% apart**, which is the cleanest available statement that 2b-vi routed witness dispatch onto the existing shared door rather than growing a second one. AND IT CANNOT GROW: **P is capped at 63 by the language itself** -- args=64 is refused `E2015: a function with 65 argument slots -- more than the 64 a call can carry` (the 64th is the receiver; the cap is the float mask's width). A quadratic in a variable the type system bounds at 64 is a CONSTANT, ~2,048 comparisons per call site, linear in program size. Not chased: the only cure is a per-signature name-to-index map, which `indexOfParamIn`'s header already rejects with its reason (a heap object on every signature, in every program, to remove a bounded term on a shape nobody writes), and it would be a real memory regression bought to win a pathological input. ///// GATES: build 0 warnings, suite 1940/0 exit 0, and `git status --short specs-shv2/fragments/` EMPTY after a full suite run -- byte-identical codegen. | 39,403,354 | 74,546,296 | 148,866,027 | 299,120,458 | 607,970,686 | 1,244,740,056 |
| 2026-07-28 | E3070 BORROW LIVENESS — a collection may not be mutated while an element borrowed out of it is live. shv2's `arr.get(i)` hands back the element POINTER and takes no reference for it, so any write that shortens or replaces the array frees a record the caller still holds. Measured 0xC0000005 before this on `clear`, `push`, a rebind `arr = other`, `grow(arr)`, a field chain `b.items.clear()`, a bare self-field alias, a method clearing its own field (`b.wipe()`, which printed 4557430888798830399 out of freed memory where the oracle prints 44), and FOUR spellings over top-level MODULE storage. Suite 1922 -> 2003/0. ///// COST, PATH-CLEAN A/B against parent 2a0dcd8d9, RUNGS 0-3: allocations +118 +199 +358 +669, bytes +32,960 +55,624 +105,096 +202,496. Delta ratios x1.69 x1.80 x1.87 converging on x2.00 against the corpus's own x1.80 x1.88 x1.93 => LINEAR, a constant factor per unit of source, +0.018% allocations and +0.071% bytes at rung 2. Bytes are two 8-byte fields making records that already existed bigger (`IrFunction.borrowFacts`, and the per-binding subject id) plus the borrow records themselves; `phase:merge` moves bytes with +0 allocations, which is that signature exactly. ///// ***READ THIS BEFORE YOU TRUST ANY DELTA THIS INSTRUMENT PRINTS: THE PATH-LENGTH ARTIFACT BIT AGAIN, AND IT PRODUCED THREE DIFFERENT ANSWERS FOR ONE CHANGE.*** Path strings are allocated, so allocation counts depend on the LENGTH OF THE DIRECTORY THE COMPILER RUNS FROM. (a) The delta compares against the row below, which was written from a 28-char worktree: on an UNCHANGED main, from the 22-char main checkout, that reads +340/+434/+622 -- the artifact alone, no code. (b) The coordinator's own head-vs-main A/B, taken from two DIFFERENT paths (22 vs 28 chars), read -93/-12/+145 -- the artifact with the sign REVERSED, which made a real cost look like a saving. (c) Both binaries run from ONE canonical path read +118/+199/+358. Only (c) is a measurement. ⇒ **AN A/B IS ONLY VALID IF BOTH BINARIES ARE EXECUTED FROM THE SAME DIRECTORY**, swapped in place; and a committed row is a baseline ONLY for runs from the path that wrote it. This row was written from the main checkout for exactly that reason. It is the same lesson the P1.8 Slice A row recorded as "+142 real plus a +14 path-length artifact" -- recorded there, and still not enough to stop it happening twice more. ///// TWO QUADRATICS FOUND AND FIXED IN-RUNG, neither filed: (1) `BorrowCheck.reportFirstLiveBorrow` restarted at the head of the subject's bucket for every write => O(sites x borrows-of-subject); a borrow dead at one site is dead at every later one (its last use is fixed once parsing ends, and sites arrive in token order), so a per-subject CURSOR steps over it once and for all -- 94.3M -> 0.10M ticks at K=1024, and the delta stops growing. (2) the pending-claim list never shrank, because `s = try arr.get(0) otherwise ""` is an ASSIGNMENT and no binding completes the borrow, so `retargetPendingBorrow`/`attachPendingBorrows` re-walked the whole list every time; now cut to a per-statement WATERMARK -- a watermark and not a reset, because `otherwise return` re-enters the statement parser mid-initializer and a reset there is a measured 0xC0000005. Both amortization arguments are ORDER arguments and both are now checked where the order is established, with a panic on violation: silently skipping a live borrow is a use-after-free nobody would be told about. Sabotage confirmed both panics fire. ///// ⛔ **THE CORPUS WAS NOT BLIND HERE -- IT WAS NARROW, WHICH READS AS COVERAGE AND IS WORSE.** `ScaleCorpus`'s `v_marray.maxon` does hold one `let mgN = try msN.get(0) otherwise ""` per managed-Array group, so `BorrowCheck` runs and returns a healthy non-zero at every rung. But every subject carries EXACTLY ONE borrow and every accessor is bound to a name, so both quadratics above have degree 1 there and CANNOT appear. A non-zero reading from a corpus that cannot express the shape is not evidence. `maxon-shv2/Testing/ladders/genborrow.sh` is committed (knobs: borrows per subject, mutation sites, distinct subjects), and it is what read the x3.8-x4.1 per doubling that the fixes deleted. ///// MEASURED DEBT, NOT FIXED: `Parser.borrowSubjectIdOf` is a linear scan of the distinct-subject list, entered once per mint and once per recorded write => O(K^2) in K distinct borrowed-and-written storages in ONE body (x3.99 x2.23 x2.66 x3.08). Fitted aK+bK^2 over the top two rungs: a=15,697, b=36.2 ticks, so the quadratic term does not equal the linear one below K~434 distinct subjects in a single function body. The corpus reaches 256 (~2.4M ticks at rung 5, ~0.02% of the compile); hand-written code reaches single digits. TRIGGER TO RE-MEASURE: machine-generated code declaring hundreds of distinct arrays in one body. Both closures would tax every program (8 bytes on every VarInfo, or a per-function Map), which is why it is filed rather than shipped. ///// A NO-BORROW CONTROL says the recording is genuinely demand-driven: a function that borrows nothing allocates ZERO extra at every K and the shared-empty `BorrowFacts` is never copy-on-written; parse CPU on a program that is nothing but array method calls is +5.2..+7.0%, spread across several constant-factor additions with no single one measurable apart from the others -- the same per-function CPU attribution gap the row below names. | 39,443,711 | 74,609,317 | 148,978,520 | 299,330,351 | 608,385,926 | 1,245,560,952 |
| 2026-07-28 | int->float PROMOTION DOORS. SEVEN doors fixed, all sharing one shape: "ask checkDeclaredType, accept ok, forget to convert". `return` PANICKED (X64Backend.maxon:715); struct-literal store, field assign, self-field assign, field default, global init and the LOCAL `var` REBIND all SILENTLY STORED THE RAW INT BIT PATTERN (~1.5e-323, coordinator-reproduced: `p.raw() == 3.0` FALSE, exit 7). ///// allocsDelta 0 AT ALL SIX RUNGS -- the conversion reuses the existing MaxonUnaryOp.promoteToFloat -> StdArithUnaryOpcode.siToFp, which already lowers on all three backends (x64 cvtsi2sd, wasm emitSiToFp, arm64 scvtf), so no new op and no target gate. ///// bytesDelta IS REAL AND FULLY ATTRIBUTED: +960/+1,632/+2,976/+5,664/+11,040/+21,792 net of the flat +206 CHECKOUT-PATH-LENGTH artifact (this worktree's path is one char longer than the logged control's; a flat non-zero delta is a PATH CHECK before it is a finding). Over the corpus's struct-field counts 60/102/186/354/690/1,362 that is **EXACTLY 16.00 BYTES PER STRUCT FIELD AT EVERY RUNG, TO THE DIGIT**. ///// CAUSE: FieldDefault.literal gained a TAG field so the widening decision reads the literal's own tag instead of resting on a SEPARATE FALSE REJECTION elsewhere (a correctness argument propped up by a bug -- removed on coordinator ruling). A Maxon union box is sized to its WIDEST ARM, so one new field costs 8 B per box; the box COUNT is unchanged, which is exactly why allocations did not move. TWO layouts are built per compile (phase:signatures' sweep and phase:parse's real parse, one StructLayout each) -- the deltas split identically to the byte between those two phases -- so it is 8.00 B/field PER LAYOUT and **16.00 B/field PER COMPILE**. ///// NOTE THE COMMIT MESSAGE `df7b91dfb` STATES 8.00 AND IS HALVED: it quoted the per-layout figure as the per-compile one. The mechanism it describes is correct; only the headline quotient is wrong. Corrected here and in PLAN.md, which are the durable records. 0.0018% of compile bytes at rung 5. Strictly linear in fields. ///// The widening ARM is unreached by the scale corpus (no int meets a float slot in it), measured instead on a hand ladder at +4.1 allocations per widened site, flat, x1.89 x1.94 x1.97 across 64->512 sites -- one extra IR op per conversion, linear. | 39,404,520 | 74,548,134 | 148,869,209 | 299,126,328 | 607,981,932 | 1,244,762,054 |
| 2026-07-28 | P1.8 SLICE C — the SEVEN byte/ASCII String methods (`startsWith` `endsWith` `contains` `toLower` `toUpper` `replace` `split`), as eight Std-tier runtime graphs plus parser dispatcher arms. Suite 2003 -> 2036/0 on its own base, 2056/0 after rebasing onto upstream. ///// ⭐⭐ **THE HEADLINE IS NOT THIS SLICE'S COST. THE LADDER BUILT TO SEE THIS SLICE FOUND A QUADRATIC IN `__str_append` THAT HAD BEEN THERE SINCE P1.2.** Exact growth (`requiredLen`) meant the buffer was full the instant it was published, so the next append reallocated and re-copied every byte written so far: `var s = ""` plus a loop of appends was O(n^2) bytes and one allocation per append. MEASURED with `Testing/ladders/genstring.sh <n> data-appendloop`: x3.94 x4.07 x3.82 x3.88 per doubling, and **5.0 SECONDS to build a 288 KB string**. Now `2 * requiredLen` -- branch-free, because `requiredLen = selfLen + otherLen >= selfLen`, so doubling it is unconditionally big enough without a `max(...)` and without a compare. After: x1.82 x2.21 x1.77 x1.94, top rung **0.98 ms — 5,118x**. Coordinator-verified end to end: 294,912 bytes built in 15 ms of total process time. Safe because `capacity@16` has exactly three readers and none treats it as the allocation's size, and Strings expose no `capacity()` to user code. The comment above that line had called geometric growth "a later optimization with no consumer here" -- the consumer was the commonest string idiom in the language. ///// ⛔ **THE CORPUS IS BLIND TO ALL SEVEN METHODS, AND THE PROOF IS AN INVENTORY, NOT AN ASSUMPTION.** `--emit-corpus` dumped, every method call across all six rungs enumerated: `create push count get append scaleBy probe byteLength slice reserve clone firstVal`. Not one of the seven. ⚠ **But it is NOT blind to `String`** -- 756 `==` sites plus `append`/`byteLength`/interpolation -- which is exactly why this slice's shared `__str_eq` change WAS visible while the seven methods themselves read Δ0. That is E3070's "narrow, not blind" trap running in REVERSE: there, a neighbour of the shape read non-zero and looked like coverage; here, the same neighbour reading non-zero could have been mistaken for coverage of the methods. **Check what the corpus CONTAINS before believing what a Δ0 means, in either direction.** `maxon-shv2/Testing/ladders/genstring.sh` is committed (knobs: call sites, segments, matches, haystack, needle-scaling). ///// COST, PATH-CLEAN A/B (both binaries swapped into the SAME slot, per the row below): **Slice C itself is +11 allocations / +295 bytes, FLAT at all six rungs** -- +15/+664 for the 15 new module-level `b"..."` globals (7 method names, 8 runtime symbols), +7/+167 `phase:isel`, -11/-472 `phase:regalloc` from the `emitByteRangeCompareLoop` critical-edge deletion. The `__str_append` growth fix costs a further flat +103 allocations / +3,569 bytes per compile. The seven methods measured LINEAR on every shape the new ladder can express: `sites-*` x1.90-x1.99 out to 2,048 call sites; `data-split` x2.04 over 16x segments (the search advances and `__arr_push` is amortized); `data-replace` x1.96 over 16x matches (the two-pass count really does size ONE allocation); `toUpper` x2.00. A String-free control A/B'd +0 allocations. ///// ⚠⚠ **THE RECORDED -181 / -8,132 THIS ROW REPLACES WAS THE PATH-LENGTH ARTIFACT, AND THE COORDINATOR READ IT WRONG IN A NEW WAY.** I argued the delta could not be the artifact because it was FLAT while the artifact "grows with rung count". It does not -- the artifact is per-compile path strings, so it is flat too. Measured: parent `d9d0f980a` from a 26-char worktree reads **-192 at every rung to the digit** against its own committed row; -192 + 11 = -181, and -8,427 + 295 = -8,132. The arithmetic closes exactly. ⇒ **"It is flat, therefore it is not the artifact" IS NOT A VALID ARGUMENT.** The only valid one remains the row below's: run both binaries from the SAME directory, swapped in place. ///// MEASURED DEBT, FILED NOT FIXED: `__str_find` is a naive search carrying a genuine O(haystack x needle) term -- `data-findquad` reads x3.92 x4.15 x3.95 x4.14. It needs BOTH a needle whose length scales with the haystack AND data self-similar enough to defeat the first-byte reject; a fixed needle is a constant factor and `data-find` reads x2.04. Measured LINEAR on every realistic shape including `split` and `replace`, its two biggest consumers. **TRIGGER TO RE-MEASURE: a consumer that searches with a needle whose length grows with its input.** The fix is two-way/KMP. ⚠ `stdlib/String.maxon`'s `findIn` -- which BOTH reference compilers run -- has the same shape, so this is not a shv2 divergence. ///// A SABOTAGE THAT DID NOT FIRE, reported rather than left implied: disabling the `Array with String` pre-intern for `.split` still passes 2035/0, because parse-time interning is idempotent and lands on the same giid. The pre-intern is DEFENSIVE, not load-bearing. Kept for consistency with `hasStringArrayLiteral`/`hasSetFromLiteral`; flagged so it is not mistaken for a checked claim. | 39,448,526 | 74,614,813 | 148,985,360 | 299,339,879 | 608,400,830 | 1,245,586,608 |
| 2026-07-28 | A FUNCTION VALUE'S DECLARED TYPE IS AUTHORITATIVE. `FunctionTypeAlias` stored only its RETURN type and threw the declared PARAMETER types away, and three silent wrong answers lived in that one gap: (1) an int literal at a `float` parameter was NOT widened at an INDIRECT call, through all three callee kinds -- an alias-typed parameter, a bare functionRef and a lifted closure (coordinator-reproduced exit 0 where 4/6/6 is right); (2) a mismatched function-to-function CAST was accepted untouched (`intish as FloatFn` read an integer back out of the float return register), as was the same disagreement through a function-typed PARAMETER and a `return` -- only the `function` TAG was ever compared, so two different function types read as one; (3) an indirect call's ARITY and argument TYPES were unchecked (`f(3)` on a two-parameter type read `b` out of whatever the register held; a String at an int parameter crashed). ///// COST: +7 ALLOCATIONS FLAT at every rung and `phase:parse` allocation-IDENTICAL to the parent, A/B'd against a parent build made and run from THIS SAME worktree path. Bytes +1,352/+1,944/+3,128, fully attributed and entirely "records that already existed got bigger", nothing new allocated per program unit: `phase:load` +264 FLAT (two new `Project` containers), `phase:lowerMaxonToStd` +24 FLAT (three registry fields on the one lowering object), and `phase:signatures`+`phase:parse` = 8 B per FileParseArtifact for its new door array, at IDENTICAL allocation counts -- an artifact is built per file AND per top-level constant, which is exactly the +768/+1,320/+2,424 doubling seen. ///// ⚠⚠ THE FIRST SHAPE OF THIS COST 240x MORE AND THE OBVIOUS ATTRIBUTION WAS WRONG TWICE. Measured +4,497/+8,666/+17,007/+33,676 allocations, x1.93..x1.99 -- linear, but 0.9% of the whole compile. Half was `SemanticCheck.checkArgTypes` rendering a parameter LABEL (`String.from` + interpolation) for EVERY argument of EVERY call before deciding whether to report anything -- now built only inside the reporting arms (`argParamLabel`), taking `phase:semanticCheck` to delta 0. The other half was a `String` place NOUN ("variable"/"global"/"field") threaded from `Parser.assignableValue` into the door recorder: ONE ALLOCATION PER ASSIGNMENT, per struct-literal field and per global store in the program, +1,852 at rung 0. ⚠ It read as the SAME +1,852 TO THE DIGIT whether the door's guard sat inside the recorder, at the caller, or with the declared type passed as a `MaxonType` instead of a `(tag, nameId)` pair -- three plausible causes, all refuted, and only deleting the whole call site moved it. What fixed it was making the noun a SCALAR (`FunctionTypeDoor` gained `variable`/`global`/`field` arms, rendered to words only where a diagnostic is built). LESSON: on a parser path that runs per statement, what a guarded call COSTS is its ARGUMENTS, not its branch -- and the only way to find which argument is to delete the site and add the pieces back. ///// The widening and the new checks are UNREACHED by the scale corpus -- `--emit-corpus` confirms ZERO function typealiases at every rung and `phase:merge` reads delta 0, i.e. not one door was ever recorded -- so the deltas above measure the STRUCTURAL cost only and say nothing about the checks themselves. ///// GATES: build 0 warnings, suite 2023 -> 2036/0 exit 0, wasm 1925/0 exit 0, fragments ADDITIONS ONLY (13 x64-windows + 13 x64-linux, no M), verify-warm-rebuild PASS. | 39,439,038 | 74,605,908 | 148,977,639 | 299,334,526 | 608,400,213 | 1,245,595,463 |
| 2026-07-28 | A FUNCTION VALUE'S DECLARED TYPE IS AUTHORITATIVE. `FunctionTypeAlias` stored only its RETURN type and threw the declared PARAMETER types away, and three silent wrong answers lived in that one gap: (1) an int literal at a `float` parameter was NOT widened at an INDIRECT call, through all three callee kinds -- an alias-typed parameter, a bare functionRef and a lifted closure (coordinator-reproduced exit 0 where 4/6/6 is right); (2) a mismatched function-to-function CAST was accepted untouched (`intish as FloatFn` read an integer back out of the float return register), as was the same disagreement through a function-typed PARAMETER and a `return` -- only the `function` TAG was ever compared, so two different function types read as one; (3) an indirect call's ARITY and argument TYPES were unchecked (`f(3)` on a two-parameter type read `b` out of whatever the register held; a String at an int parameter crashed). ///// COST: +7 ALLOCATIONS FLAT at every rung and `phase:parse` allocation-IDENTICAL to the parent, A/B'd against a parent build made and run from THIS SAME worktree path. Bytes +1,352/+1,944/+3,128, fully attributed and entirely "records that already existed got bigger", nothing new allocated per program unit: `phase:load` +264 FLAT (two new `Project` containers), `phase:lowerMaxonToStd` +24 FLAT (three registry fields on the one lowering object), and `phase:signatures`+`phase:parse` = 8 B per FileParseArtifact for its new door array, at IDENTICAL allocation counts -- an artifact is built per file AND per top-level constant, which is exactly the +768/+1,320/+2,424 doubling seen. ///// ⚠⚠ THE FIRST SHAPE OF THIS COST 240x MORE AND THE OBVIOUS ATTRIBUTION WAS WRONG TWICE. Measured +4,497/+8,666/+17,007/+33,676 allocations, x1.93..x1.99 -- linear, but 0.9% of the whole compile. Half was `SemanticCheck.checkArgTypes` rendering a parameter LABEL (`String.from` + interpolation) for EVERY argument of EVERY call before deciding whether to report anything -- now built only inside the reporting arms (`argParamLabel`), taking `phase:semanticCheck` to delta 0. The other half was a `String` place NOUN ("variable"/"global"/"field") threaded from `Parser.assignableValue` into the door recorder: ONE ALLOCATION PER ASSIGNMENT, per struct-literal field and per global store in the program, +1,852 at rung 0. ⚠ It read as the SAME +1,852 TO THE DIGIT whether the door's guard sat inside the recorder, at the caller, or with the declared type passed as a `MaxonType` instead of a `(tag, nameId)` pair -- three plausible causes, all refuted, and only deleting the whole call site moved it. What fixed it was making the noun a SCALAR (`FunctionTypeDoor` gained `variable`/`global`/`field` arms, rendered to words only where a diagnostic is built). LESSON (⚠ CORRECTED BY THE OPTIMIZATION-PASS ROW BELOW, which rebuilt all three variants on top of the shipped head and measured them): it is NOT "its arguments". An argument of the guarded call that the callee merely READS is free -- measured, delta ZERO. What a guarded call costs is OWNERSHIP TRANSFER: a MANAGED argument the callee CONSUMES forces the caller to hand over an owned copy, and that copy is emitted UNCONDITIONALLY ahead of the guard, which is why the three relocations of the guard all read the same +1,852. ///// The widening and the new checks are UNREACHED by the scale corpus -- `--emit-corpus` confirms ZERO function typealiases at every rung and `phase:merge` reads delta 0, i.e. not one door was ever recorded -- so the deltas above measure the STRUCTURAL cost only and say nothing about the checks themselves. ///// GATES: build 0 warnings, suite 2023 -> 2036/0 exit 0, wasm 1925/0 exit 0, fragments ADDITIONS ONLY (13 x64-windows + 13 x64-linux, no M), verify-warm-rebuild PASS. | 39,439,038 | 74,605,908 | 148,977,639 | 299,334,526 | 608,400,213 | 1,245,595,463 |
| 2026-07-28 | A FUNCTION VALUE'S DECLARED TYPE IS AUTHORITATIVE -- OPTIMIZATION PASS. NO CODE CHANGE, and the measurements are why. ///// THE +7 FLAT IS CONFIRMED INDEPENDENTLY, not taken. `main` @9fa71cc79 was rebuilt IN THIS SAME WORKTREE (detached HEAD, so the checkout path is identical to the head build's CHARACTER FOR CHARACTER and the phase:load path-length artifact is structurally zero rather than subtracted) and its scale-test reads 574,688 / 1,035,370 / 1,955,525 / 3,792,967 / 7,465,789 / 14,807,857 against the head run's 574,695 / 1,035,377 / 1,955,532 / 3,792,974 / 7,465,796 / 14,807,864 -- +7 AT EVERY RUNG, and the head run reproduces the logged row with delta 0. ⭐ AND THE IMPLEMENTER'S EXTRAPOLATION WAS RIGHT FOR THE RIGHT REASON, which needed the control to establish: the +125..+2,518 against the LOGGED row is +7 (this rung) plus the parent's own +118/+199/+358/+669/+1,286/+2,511 advance since the 2b-vi baseline, and that second term is now MEASURED as the control's own allocsDelta rather than fitted from four points. A doubling offset across a doubling ladder is the shape a real per-program cost has too; only an equal-path control tells them apart. ///// ⚠⚠ THE DELTA IS STRUCTURAL ONLY AND THE LADDER IS BLIND, RE-CONFIRMED FROM THE CORPUS: `--emit-corpus` over all 465 generated files finds ZERO `typealias = function(...)` and ZERO function references at every rung, so `resolveFunctionAliasShapes`, `declaredFunctionShapeOf`, `checkIndirectCall`, `checkFunctionTypeDoors` and `indirectCalleeParamTypes` run EXACTLY ZERO TIMES in the whole ladder. ///// NEW INSTRUMENT: `maxon-shv2/Testing/ladders/genfnval.sh <aliases> <sites> <arity> <indirect / direct>` -- the axis the corpus cannot express, with `direct` as the same-shape control (same alias declarations, same callee bodies, same CALL COUNT, by NAME, so no function value, no indirect call and no door exists in it). ⚠ Its two modes are NOT byte-identical and cannot be: a direct call's 2nd+ arguments MUST be labelled (E2053) and an indirect call's CANNOT be, so read it as an upper bound on the machinery and read RATIOS within a mode. ///// ⭐ RESULT: NOTHING ON THIS RUNG'S PATHS IS SUPERLINEAR, and it is measured rather than argued. Doubling aliases (and with them call sites and doors) 64 -> 512, HEAD MINUS PARENT on the identical source, allocations: total +2,006 / +3,934 / +7,784 / +15,474 = x1.96 x1.98 x1.99. Per phase, all x1.96..x2.00: resolveTypes +609/+1,193/+2,353/+4,665 (9.1 allocations per function typealias, and IDENTICAL in `direct` mode -- it is the DECLARATION cost, which is exactly what the control was built to prove); semanticCheck +768/+1,536/+3,072/+6,144 (x2.00 to the digit, 12 per alias over 3 indirect calls + 2 doors); lowerMaxonToStd +192/+384/+768/+1,536 (x2.00, ONE union box per indirect call in `indirectCalleeParamTypes`); parse +330/+651/+1,292/+2,573. Subtracting the control leaves the CALL+DOOR machinery alone at +1,035/+2,061/+4,111/+8,209 = x1.99 x1.99 x2.00. CPU ticks (min of 3) read x1.93..x2.06 on every phase of both modes. `declaredFunctionShapeOf` is TWO HASH-MAP GETS, not a scan, so the `sites x aliases` quadratic the hunt was for would have read x4.00 on this ladder and reads x2.00. ///// ⚠⚠ THE RECORDED LESSON OF THE ROW ABOVE IS CORRECTED HERE -- it was directionally right and MIS-STATED, and the mis-statement is the actionable part. Reconstructed the blamed shape on top of the shipped head and measured three variants at rung 0: (V1) the `String` noun restored as an argument of `assignableValue` and consumed by `requireAssignable` -- i.e. the PARENT'S OWN SHAPE, which is the point, since the parent already paid for that literal at every one of those call sites -- reads 574,695, delta ZERO; (V3) the noun ALSO passed as an argument of the GUARDED recorder call but only READ there -- 574,695, delta ZERO; (V2) the same argument, CONSUMED (stored on the `PendingFunctionTypeDoor` record) -- 576,547, +1,852 in phase:parse, THE LOGGED NUMBER TO THE DIGIT. So it is not "its arguments": V3 puts the argument on the guarded call for free. ⭐ THE RULE IS OWNERSHIP TRANSFER: a MANAGED argument that the guarded callee CONSUMES forces the caller to hand over an owned copy, and that copy is emitted UNCONDITIONALLY, ahead of the guard -- so a branch taken at essentially no call site still pays once per call for every managed argument it would have consumed. That also explains the three "refuted" causes without any further hypothesis: moving the guard inside the recorder, moving it to the caller, and changing the declared type's representation all leave the CONSUMED String argument exactly where it was, which is why all three read +1,852 to the digit. The corpus records ZERO doors, so not one of those 1,852 copies was ever used. ///// GATES: build 0 warnings; suite 2036/0 exit 0; wasm 1925/0 exit 0; fragments byte-identical after both suites; verify-warm-rebuild PASS (run on a genfnval program, so the doors list is exercised across a warm re-merge -- it is reset in `resetMergeTargets`, so the E3070-shaped "pending list that never shrank" is NOT present here). ///// ⛔ ONE PRE-EXISTING DEFECT FOUND AND REPORTED, NOT WORKED AROUND: an indirect call of arity >= 14 panics the register allocator (`RegisterAllocator.maxon:1173 chooseRegister: no free register`). REPRODUCES IDENTICALLY ON `main` @9fa71cc79 -- arity 13 compiles, 14 panics, on both binaries -- so it is not this rung's; the same arity 16 through a DIRECT call compiles clean. It caps genfnval's arity knob at 13. | 39,439,038 | 74,605,908 | 148,977,639 | 299,334,526 | 608,400,213 | 1,245,595,463 |
| 2026-07-28 | A FUNCTION VALUE'S DECLARED TYPE IS AUTHORITATIVE -- INDEPENDENT REVIEW. ONE BLOCKER FIXED, plus the duplication it exposed. ///// THE BLOCKER WAS A FALSE REJECT, and the shape the rung's own comment said it had no case for: `resolvedTypesMatch` compared a NESTED function type (a function type's function-typed PARAMETER) by NAME id while comparing everything else RESOLVED, so `typealias Outer = function(f InnerA) returns Integer` REFUSED a function declared `(f InnerB)` even when InnerA and InnerB resolve identically. The bootstrap ORACLE compiles that program and runs it (exit 42), the parent shv2 @9fa71cc79 compiles and runs it (42), and head refused it -- with a diagnostic that read `expected 'fn(function) returns int', got 'fn(function) returns int'`, i.e. a type failing to match itself, because `maxonTypeName` rendered any function type as the bare word `function`. FIXED both halves: the nested type now DESCENDS (`nestedFunctionTypesAgree`), which is the same rule one level down and is required for soundness in the other direction too -- if the nested types genuinely differ, the callee calls its own parameter through the OTHER signature and the wrong answer lands one level in; and `maxonTypeName` now renders a `function` tag by its alias name, so the mismatch reads `expected 'fn(InnerA) returns int', got 'fn(InnerB) returns int'`. ///// THE DESCENT NEEDS A CYCLE GUARD AND HAS ONE, because shv2 ACCEPTS a self-referential function alias (`typealias SelfFn = function(f SelfFn)` compiles on head AND on the parent; the bootstrap refuses it as E2012) and two aliases may name each other. Guard is coinductive on the (declared, got) NAME PAIR: a pair already under comparison AGREES, which is the only answer a recursive type has and cannot false-reject. Verified terminating and correct on four hand-built cycles -- self-referential, mutually recursive A/B accepted, structurally-equal recursive A vs B accepted, recursive A vs B with a differing return REFUSED. ///// COST: +1 ALLOCATION FLAT AND +16 BYTES FLAT at every one of the six rungs, against the row above. It is ONE object for the whole compile -- `sharedEmptyFunctionTypePairs`, the empty pending list every top-level comparison starts from, never mutated (`pendingWithPair` builds a fresh array) -- the same pay-for-use discipline `sharedEmptyBorrowFacts` carries. NOTHING is per-program-unit: the `declaredId == gotId` short-circuit means an ordinary nested parameter (same alias both sides) allocates nothing and touches no registry, and a descent only happens when the two names actually differ. Corpus is still blind to this whole axis (zero function typealiases in all 465 generated files), so the flat +1 is the structural cost and nothing else. ///// DUPLICATION REMOVED, which is what made the fix cheap rather than expensive: `declaredFunctionShapeOf` took its three registries as three loose parameters and the id->name interner was a FOURTH fact carried separately, so SemanticCheck and LowerMaxonToStd each held a private copy of `typeNames.nameOf(id) or panic` and a private copy of `declaredFunctionShapeOf(...) or panic`. That is 3 copies of each once the descent needed both. Collapsed to ONE `FunctionTypeRegistry` carrier plus ONE `functionTypeNameOf` and ONE `requireFunctionShapeOf`, both exported from TypeResolution; the lowering now holds one field where it held four. Also deleted `hasDeclaredFunctionShape`, which had NO callers. ///// SPECS: 2 authored cases pinning the rule in both directions (`nested-function-type-agrees-by-shape` accepted, `error.nested-function-type-mismatch` refused with the readable message). ///// ⛔ SECOND PRE-EXISTING DEFECT FOUND AND REPORTED, NOT WORKED AROUND: on wasm, passing a function value into a parameter whose function TYPE has a function-typed parameter emits a module that fails validation (`type mismatch: expected i64 but nothing on stack`). Narrowed to the nested function TYPE alone -- no indirect call anywhere, the SAME alias on both sides -- and it reproduces byte-for-byte on `main` @9fa71cc79, so it is neither this rung's nor newly reachable. It is why the accepted case above lists x64 targets only. ///// GATES: build 0 warnings; suite 2036 -> 2038/0 exit 0; wasm 1925 -> 1926/0 exit 0; fragments additions-only (2 new x64-windows, no M); verify-warm-rebuild PASS on a genfnval program AND on the nested-alias case. | 39,439,054 | 74,605,924 | 148,977,655 | 299,334,542 | 608,400,229 | 1,245,595,479 |
| 2026-07-28 | P1.8 SLICE B — `Character`, UAX#29 GRAPHEME SEGMENTATION, `String.count()`, `for c in s`. A real `ValueTypeTag.character` (user ruling), a new `GraphemeRuntime` family, and the per-trip owned-Character frame. Suite 2056 -> 2114/0 on its own base, **2129/0** after rebasing onto upstream. x64-windows / x64-linux / wasm32-wasi all PASS; arm64 SKIP. ///// COST, PATH-CLEAN A/B (both binaries swapped into the SAME slot, interleaved): **+0 ALLOCATIONS and +64 BYTES, FLAT at every rung** — eight new `bool` fields on ONE `RuntimeUsage` record per compile. Confirmed two ways: the no-Character control read +0 allocs / +64 bytes / no measurable CPU (4.419 s vs 4.426 s, head faster in 2 of 4 interleaved trials), and the arithmetic closes against the committed row — slice C's artifact is -8,427, this tree reads -8,363, and **-8,427 + 64 = -8,363 exactly**. Compile growth x1.85-x1.99 in allocations AND CPU out to 1,024 call sites across all five ladder modes; run growth x2 per doubling on every mode. **Nothing superlinear.** ///// ⛔ **THE CORPUS IS BLIND TO EVERY PART OF THIS RUNG, BY INVENTORY.** `--emit-corpus` at rung 5: `.count()` appears **1,024 times and every one is an Array/Set receiver** — not one String; `.bytes()` **0**; **`for … in` in any form: 0**; `'…'` tokens **53,766, of which 0 are in a value position** (every one is a block label). ⚠ **But it is NOT blind to `String`** (756 `==` sites), which is the trap in reverse: a neighbour of the shape reads non-zero and can be mistaken for coverage. **Check what the corpus CONTAINS before believing what a Δ0 means, in either direction.** `maxon-shv2/Testing/ladders/genstring-grapheme.sh` is committed (knobs: call sites, segments, cluster width, ASCII/2-byte/ZWJ/flag seeds, countloop). ⚠⚠ **AND NOTE `for … in` IS STILL ZERO — P1.8 Slice A committed `genfor.sh` for exactly that hole and the MAIN corpus was never taught the construct.** Four consecutive rungs have now each built a private ladder because the shared instrument cannot express what they added; that is an instrument problem, not four coincidences. ///// ⭐⭐ **THE FINDING IS NOT A CURVE, IT IS THE ALLOCATOR: `for c in s` MINTS ONE Character PER TRIP AND THE SLAB NEVER GIVES IT BACK.** `MmRuntime` is a bump allocator with no free list, so memory is linear in **ITERATIONS, not live data**: 65,536 clusters -> 11.6 MB, 262,144 -> 97.1 MB, **1,048,576 -> 398.7 MB**, at ~82 bytes per minted Character. **Two controls make it unambiguous**: `data-count-ascii` runs the SAME `__gr_end` scan minting nothing and is **FLAT at 3.7 MB across a 4x data range**, and the C# bootstrap runs the same source in the same time (155.7 vs 156.4 ms) at **7.3 MB**. It is NOT a leak — every allocation is freed and no run exits 101 — the slab simply never reuses a slot. **FILED, not fixed**: the cure is a recycling `__mm_free`, which `MmRuntime.maxon:738` already anticipates in its own comment, and that is a runtime rung. **TRIGGER TO RE-MEASURE: any construct that allocates once per loop trip — `for c in s` is the FIRST one the language has, which is precisely why this debt only became visible now.** ///// Also measured: `for c in s` costs 17x what `count()` costs per cluster and ALL of it is `__char_at` (one alloc+blit+free per trip, ~54 ns, CONSTANT in the segmentation work — 54.1 ASCII / 56.2 two-byte / 83.6 for a 25-byte cluster), so the GB state machine hides no per-trip cost. shv2's `count()` is **2.07x FASTER than the bootstrap's** on the identical O(n) algorithm (16.8 ms vs 34.8 ms over 1,048,576 clusters). `data-countloop` is x3.79-x4.05 — quadratic BY DESIGN, in user code, exactly as `countGraphemes` is O(n) per call in both compilers. ///// ⚠⚠ **READ THE PRINTED DELTA ON THIS ROW AS A SPAN, NOT AS THIS RUNG'S COST.** The instrument compares against the row BELOW, which was written by the upstream function-value rung from a different tree; between the two rows THREE things landed — that rung, the BOOTSTRAP ORACLE fixes, and this slice. So the printed +5,836 / +9,043 / +15,459 / +28,289 / +53,951 / +105,273 is the sum of all three plus a path-length component, **not** Slice B. Slice B's own cost is the path-clean interleaved A/B above: **+0 allocations, +64 bytes, flat**. ⚠ The span IS linear — successive ratios x1.55 x1.71 x1.83 x1.91 x1.95, converging on x2.00, ~+0.7% of allocations — so there is no superlinearity in it; but its ATTRIBUTION between the three landings is **OWED, not claimed**. The likeliest single contributor is that the oracle rung changed `maxon-sharp` CODEGEN, and shv2's binary is PRODUCED BY the bootstrap, so a bootstrap change moves shv2's own allocation behaviour — but that is a hypothesis, and this project's rule is that a story which fits is not an attribution. **Whoever next needs this number should measure it path-clean against `cdcab0969`.** ///// REJECTED WITH MEASUREMENTS: eliding the `__char_at` mint for `for _ in s` (sound, worth ~14x, but it fixes only the discard and would make `character-ownership.md`'s `discardTrips` case — written to pin that a discarded cluster is still dropped — vacuous); and an immortal 256-entry `.rdata` Character table for one-byte ASCII clusters (would remove ~94% of ASCII iteration cost, but `__str_decref` would have to learn to tolerate an rdata record, which is a Character-REPRESENTATION decision, not an optimizer's). | 39,665,596 | 74,954,045 | 149,569,434 | 300,415,238 | 610,459,369 | 1,249,625,709 |
| 2026-07-28 | PARSE SUPERLINEARITY FOUND AND FIXED — `Parser.assignedBindingsIn` was Θ(vars in scope × merging constructs), and it ALLOCATED NOTHING. `phase:parse` was the only superlinear phase on the ladder: CPU x2.02 x2.21 x2.52 x2.90 x3.33 down the six rungs while its allocations held at x1.85-x1.99, and its share of compile CPU climbed 15.3% -> 36.7% while regalloc's fell — on that trajectory it overtakes the register allocator at the next rung. ///// THE BEND, NAMED: every `if` `match` `while` `for` asked for the mutable bindings its token extent assigns, and the answer was computed by walking the WHOLE `mutableVars` list and probing each binding's name against a per-construct `Set`. One hash of a `ByteArray` name per binding in scope per construct. Instrumented on the corpus: **39,400,068 probes at rung 5 across 18,571 constructs, x3.99 per doubling — and the carried set they were selecting was never larger than TWO (maxA = 2, sumA = 27,180).** The scale corpus doubles both axes at once (`LocalsPerFunctionBase` 128<<r and `LongIfsBase`/`DeepBlocksBase`), so the product quadrupled per rung while source bytes doubled. ///// THE LADDER THAT SEPARATED THE AXES: `maxon-shv2/Testing/ladders/genscope.sh <constructs> <locals> (if ifelse while match straight)` (COMMITTED, indexed in that directory's README). C alone x1.72 x1.86 x1.89; V alone x1.46 x1.57 x1.83; BOTH x2.21 x2.45 x2.83; and the `straight` CONTROL — same V, same statement count, no merging construct — x1.80 x1.90 x1.89. Linear in each knob, quadratic in the two together, flat without the construct: that is an O(V x C) term and it belongs to the construct, not to program size. Confirmed before the fix was designed by capping the walk at 8 entries, which took the bend to x1.88 x1.94 x1.94 with allocations UNCHANGED to the digit. ///// THE FIX: invert the lookup. The parser now carries an index of `mutableVars` — `mutableVarInnermost` (name -> innermost position) plus `mutableVarShadowed` (per position, the position it shadows), pushed/popped in exact lockstep with the stack under the same `openBlock` mark. `assignedBindingsIn` now scans the extent's tokens, asks the index per ASSIGNED NAME, and collects positions into an ASCENDING list — ascending position IS `mutableVars` declaration order, which is the order the phis must be minted in, so no sort and no reordering. `namesAssignedIn` and its per-construct `Set` are gone; `mintLoopHeaderPhis` takes the same carried list, so the two orderings that had to agree are now ONE. The shadow chain is what keeps this EXACT: it yields every entry a name hides, which is precisely what the old walk found. ///// RESULT, path-clean A/B (parent `3f2e3203f` and the fix built in the SAME worktree at the SAME path, min-of-3, machine idle): `phase:parse` CPU x1.89 x1.92 x2.00 x2.06 x1.98 — LINEAR. Rung 5 parse CPU **13,160,151,600 -> 3,354,949,049 (-74.5%, 3.92x)**; whole-compile CPU **35.87G -> 24.78G ticks (-30.9%)**; parse's share of compile CPU now reads 14.5% -> 13.5% ACROSS the ladder instead of climbing to 36.7%. Same-binary CONTROL (baseline run twice): memory bit-identical at every rung, CPU spread -5.5%..+0.5% — so the noise band is ~6% and a 3.9x is not in it. ///// MEMORY, and it is fully attributed — EXACTLY TWO PHASES MOVED. `phase:parse` -4,060 .. -143,487 allocations and -277,848 .. -9,493,072 bytes: that is the per-construct `AssignedNameSet` (and its internal columns) no longer being built, ~7.7 allocations per construct. `phase:signatures` **+0 allocations** and +1,536 .. +35,760 bytes: the `Parser` RECORD grew 16 bytes (two new pointer fields), and 1,536/16 = 96 = the 30 files plus 66 top-level constants that phase builds a `Parser` for at rung 0. The two structures themselves cost nothing there because they start at shared empties and are copy-on-written by the first `var` push (`sharedEmptyIterationLocks`'s pattern) — an unconditional `create()` pair measured +13,410 allocations / +590,040 bytes in `phase:signatures` at rung 5, a phase this change has no business touching, and that is why the COW is there. Totals close exactly: -9,493,072 + 35,760 = -9,457,312. ///// ⚠ WHAT IS STILL THERE: the token scan itself still walks token-by-token, so a construct nested D deep still has its tokens re-read by all D enclosing scans — the `LoopNestBase` knob's cost, isolated by `gennest.sh`, UNTOUCHED by this change. The old note in `blockStatementEndIndex` that inverting that scan costs `phase:parse` x3.5 was measured when its per-test constant was multiplied by the Θ(V) walk this row removes; that measurement no longer stands either way and the question is open again. ///// ⚠ AND ONE MEASURED BOUND ON THE NEW CODE: the ordered insert is O(A^2) in shifts where the old walk was O(V) in hash probes, so a construct assigning EVERY binding in scope is its worst case — a shape `genscope.sh` deliberately does not generate. Measured by hand at A = V = 250/500/1000/2000 (one wide `if`) and at 8 wide `if`s with V = 125..1000: both stay LINEAR (x1.95-x2.03) and cost +1%..+4%, because a 4-byte shift is ~30x cheaper than the `ByteArray` hash probe it replaced. RE-MEASURE TRIGGER: a rung that makes wide carried sets ordinary (a macro expander, machine-generated bodies). ///// ⚠ READ THE PRINTED DELTA AS A SPAN, NOT AS THIS CHANGE'S COST. The instrument compares against the row below, which was recorded in a DIFFERENT tree, so it prints -4,335 .. -146,056 allocations where this change's own, path-clean A/B is -4,060 .. -143,487. The ~275 .. ~2,569 difference is that tree, not this change, and the attribution above is the A/B rather than the printed delta. ///// GATES: shv2 x64 **2142/0**, wasm32-wasi **2029/0**, `verify-warm-rebuild` PASS, no 101, and `specs-shv2/fragments/` EMPTY after both full runs — byte-identical codegen, which is the correctness proof this change needed most, since the carried set's ORDER decides phi order. | 39,382,555 | 74,374,748 | 148,394,961 | 298,046,805 | 605,714,888 | 1,240,124,964 |
| 2026-07-28 | P1.8 SLICE B2 — `StringIndex` + `startIndex`/`endIndex`/`findFirst`/`findLast`/`slice`, closing Slice B's sanctioned residual. Suite 2142 -> 2178/0; x64-linux PASS, wasm32-wasi PASS, arm64 SKIP (remote, opt-in). Build 0 warnings; specs-shv2/fragments EMPTY after a full unfiltered run, which is what proves the codegen byte-identical. ///// NO OPTIMIZER TRIGGER FIRED, and each of the three conditions was checked rather than waved at: the rung adds no pass and no IR op (it adds Std-graph runtime entry points and one synthesized struct layout); the ALLOCATION ladder is x1.80 x1.89 x1.94 x1.97 x1.98, converging on x2.00 with nothing anywhere near the ~x2.4 threshold; and the one Workstream-O re-measure trigger it touches is discussed below. ///// READ THIS ROW'S DELTA COLUMN AS A PATH ARTIFACT, NOT AS A SAVING. It prints -275 -349 -497 -793 -1,385 -2,569 allocations and -8,693 -9,877 -12,245 -16,981 -26,453 -45,397 bytes — i.e. NEGATIVE at every rung, growing with rung. A rung that only ADDS a feature does not make the compiler allocate less; this is the checkout-path term this log has now recorded three times (see the 2026-07-27 P1.7-Slice-1 row, which proved it by running the PARENT's binary from the worktree path and reproducing the child's numbers to the digit). This tree's root `maxon-p18b2` differs in length from the main checkout's, and the previous row was written from a different tree again. ⚠ **THIS RUNG'S OWN COST IS THEREFORE NOT ESTABLISHED BY THIS ROW, AND IS OWED, NOT CLAIMED** — a path-clean interleaved A/B against `3f2e3203f`, both binaries swapped into ONE canonical path, is what would settle it. Recording that it is unmeasured rather than reading the printed delta as a result. ///// THE CORPUS IS BLIND TO EVERY CONSTRUCT THIS RUNG ADDS, BY INVENTORY, so a Δ0 from it would measure the instrument and not the cost. Slice B's row established the inventory at rung 5 with `--emit-corpus`: `.count()` appears 1,024 times and EVERY receiver is an Array or Set — not one String — and `.bytes()` is 0. B2 adds only String-receiver methods (`startIndex`/`endIndex`/`findFirst`/`findLast`/`slice`), so the corpus cannot reach a single line of the new parser dispatch or any of the new runtime graphs. That is the fifth consecutive rung whose feature the shared corpus cannot express; four of them have now each committed a private ladder instead. It is an instrument problem, not five coincidences. ///// phase:parse CPU reads x2.04 x2.21 x2.36 x2.67 x3.08 while its ALLOCATIONS stay dead linear at x1.85 x1.92 x1.96 x1.98 x1.99. That divergence is exactly the allocate-nothing shape the CPU column was added to catch — and it is NOT this rung's and NOT new: it is the already-filed Theta(mutable bindings x constructs) debt (`d_blocks.maxon` is ONE function with 518 mutable vars in scope, and both callers ask a membership question once per binding per construct — 26.5 MILLION tests at rung 5). The 2026-07-26 encoder row filed it, and the 2026-07-27 P1.7 row re-measured it at x3.26 on BASE AND HEAD ALIKE. This row's x3.08 sits inside that, on a corpus that cannot reach B2's code at all. ⚠ SUPERSEDED WITHIN HOURS, AND THE ROW ABOVE IS THE CURE: upstream `88ca97b33` identified `Parser.assignedBindingsIn` as the Θ(vars in scope × constructs) term and FIXED it while this rung sat in its worktree. So this row is a measurement of the PRE-FIX compiler — correct when taken, stale by the time it merged. Kept rather than silently re-measured, because a trend table is read downwards and this is the last datapoint before the cure. ///// ⭐ A SECOND CONSTRUCT NOW HITS SLICE B's NAMED SLAB TRIGGER, and this is the one genuinely new observation. Slice B filed the `MmRuntime` bump-allocator debt (no free list ⇒ memory linear in ITERATIONS, not live data; 1,048,576 minted Characters -> 398.7 MB) and wrote its re-measure trigger as "any construct that allocates once per loop trip — `for c in s` is the FIRST one the language has". **`slice` in a loop is the SECOND**: every trip mints a fresh owned String, because slice COPIES rather than viewing. Probed at 200 trips: correct, exit 0, no 101 — it is not a leak, the slab simply never reuses the slot, exactly as filed. The debt and its cure (a recycling `__mm_free`, which `MmRuntime.maxon:738` already anticipates in its own comment) are UNCHANGED; what changes is that the trigger has fired a second time from an independent direction, which is evidence the runtime rung is worth sequencing rather than a new finding. ///// ⭐⭐ THE REVIEW FOUND A REACHABLE COMPILER CRASH THE GREEN SUITE WAS STRUCTURALLY BLIND TO, and the blindness is the transferable part: `registerStringIndexType`'s only caller sat inside `foldDeclaredSignaturesInto`, a DECLARATION walk whose arms hand a whole `type` to a `recordScanned*` routine and resume past its `end` — so tokens inside method bodies never reach it. A `StringIndex` producer inside a `type` method left the compiler-owned `StructLayout` unregistered and died two ways (field read -> `stringIndexLayoutOrPanic`; scope-exit drop -> `managedNameDropCallee`). All 2,177 tests spelled the feature in a free-standing function — THE ONLY SHAPE THAT LOOP CAN SEE — and two comments asserted the opposite, including the loop's own "walks EVERY token in the file, function bodies included". Cured by DERIVE (its own complete token walk) and pinned RED against the parent. Sabotage: end-exclusive->inclusive 9 red; byte->grapheme->byte position 2 red; not-found throw->sentinel 2 red; `StringIndex` box drop removed 19 red, ALL exit 101. | 39,656,903 | 74,944,168 | 149,557,189 | 300,398,257 | 610,432,916 | 1,249,580,312 |
| 2026-07-29 | P1.7a FINAL RESIDUAL — `float` conforms to `Hashable`, via a Std-tier BIT REINTERPRETATION (`StdUnaryOpcode.bitcastF64ToI64`; x64 `movq r64,xmm`, arm64 `FMOV Xd,Dn`, wasm `i64.reinterpret_f64`). Suite 2178 -> 2185/0, exit 0, no 101. x64-linux PASS, wasm32-wasi PASS, arm64-macos/arm64-linux SKIP (remote, opt-in, UNVERIFIED — not proven good). Build 0 warnings; specs-shv2/fragments 14 ADDITIONS and ZERO `M`, which is what proves the codegen byte-identical for all 2178 pre-existing programs. ///// ⚠⚠ READ THE PRINTED DELTA COLUMN AS `main`'s ADVANCE, NOT AS THIS RUNG — AND THIS TIME IT WAS REFUTED BY MEASUREMENT RATHER THAN FILED. The instrument prints -4,052 -8,518 -17,491 -35,468 -71,461 -143,479 allocations against the stored baseline: NEGATIVE and GROWING with rung, ~1% at rung 5. A rung that only ADDS a feature cannot make the compiler allocate proportionally less. The stored baseline is a PREVIOUS LOG ROW, not this rung's parent, and `main` advanced between them (0c6ca2923, 88ca97b33, 73884cbbf, 71cd8479b). Settled the way slice 2b-vi settled its own +148..+3062: the PARENT (71cd8479b) was built in a control worktree at an EXACTLY EQUAL-LENGTH path (`maxon-ctrl` vs `maxon-p17a`, both 28 chars) so the `phase:load` 202 B/char term CANCELS rather than being argued away. ///// ⭐ THIS RUNG'S OWN FOOTPRINT IS +8 ALLOCATIONS / +510 BYTES PER COMPILE, DEAD FLAT AT ALL SIX RUNGS (a 32x span), and attributed to NO phase — `installBuiltinConformanceRuntime` is called from `Compiler.maxon:452`, outside every `CompilePhase` bracket. Attributed BY PROBE, not by arithmetic: a temporary tenth never-referenced candidate added to the install roster measured +7 allocs / +312 bytes, equally flat, so the +8/+510 is one candidate (`float.hash`) plus the array growth its arrival crossed. Probe reverted. ///// THE OPTIMIZER AGENT TRIGGER FIRED (the rung adds an IR op) and the ladder shows no bend: allocations x1.797 x1.886 x1.938 x1.967 x1.983, converging on x2.00, nothing near the ~x2.4 threshold. NO COMPILER CODE CHANGED. ///// THE CORPUS IS BLIND TO THIS RUNG BY CONSTRUCTION, so its Delta0 would have measured the instrument: `ScaleCorpus` emits ZERO float `.hash()` calls, and the install gate is REFERENCED-but-undefined <=> INSTALLED, so `buildFloatHash` is never built and the new TargetOp never emitted anywhere on the ladder. That is the sixth-plus consecutive rung with this problem. Private ladder built: `maxon-shv2/Testing/ladders/genfloathash.sh` (4 modes), whose PROOF OF REACH is the strongest available kind — the PARENT compiler REFUSES its `hash` file outright (E2015: 'float' has no method named 'hash'). Per-call-site cost measured FLAT: hash 595.4 / 594.1 / 593.8 allocations per site against control 595.7 / 594.6 / 594.1 — a `f.hash()` site costs what a float comparison costs. ///// A NEW WIDEST `TargetOp` VARIANT WOULD HAVE TAXED EVERY `TargetOp` ALLOCATION (slice 2b-v's standing rule, 8 B per field): it did not — the widest variant is unchanged at 4 fields (`x64JumpTable`, `arm64Msub`) and both new ops carry 2. Verified by measurement, not by reading: the controls stay flat at +8/+510 on programs holding thousands of TargetOps, where a widened union would climb with program size. All new arms in TargetLiveness / SsaDestruction / SplitLiveRanges / RegisterAllocator are O(1). ///// ⭐⭐ MEASURED DEBT -> WORKSTREAM O, AND THE EXISTING TRIGGER WAS ON THE WRONG AXIS. Admitting `float` to `Hashable` costs +10 ALLOCATIONS / +344 BYTES PER FLOAT `.equals()`/`.compare()` CALL SITE — exact at all four sizes (10 x sites + 8), linear, controls flat. Nothing in `float.equals`'s own path changed; its POSITION IN A SEARCH ORDER did. `builtinConformerMethod` (IrInterface.maxon:691) walks Hashable -> Equatable -> Comparable and `continue`s past non-conformers; float's row now STOPS at Hashable, so a whole `IrInterface` is synthesized, searched for `equals`, missed, and discarded before Equatable is ever reached. FILED, NOT FIXED — linear in sites is a constant factor, and the review traced the cure to confirm it is not a ride-along: `builtinInterface` returns `InterfaceLookup.found(IrInterface)` BY VALUE and `IrInterface` owns three arrays, so memoizing it either clones per call (re-paying most of the 10) or returns a BORROW of a compiler-lifetime cache, which changes the return type and every consumer and introduces exactly the long-lived-borrow shape E3070's liveness pass now polices. The cheap alternative — asking "does this protocol declare this method?" without building the interface — is forbidden by the design's own thesis, since it re-introduces the second roster `builtinConformerMethod` exists to eliminate. ⚠ THE OLD RE-MEASURE TRIGGER READ "a fourth builtin-conformable protocol". IT DID NOT FIRE — `builtinConformableProtocolNames` still returns exactly three — YET THE DEBT GOT WORSE ANYWAY, because the cost is (protocols x conformance-ROW WIDTH) and this rung widened a ROW. REPLACEMENT TRIGGER: any rung that adds a (type, protocol) PAIR to `isIntrinsicBuiltinConformance`, or a fourth protocol. A re-measure trigger that cannot fire for the thing that actually worsens the cost is this project's signature bug wearing an instrument's clothes. ///// ⭐ THE PLAN'S OWN PREMISES WERE MEASURED FALSE BEFORE THE WAVE, TWICE. PLAN.md listed TWO residuals for P1.7a; `2b-iv-C` (arm64 witness lowering) had already landed six commits earlier in 227fb1051 (coordinator-confirmed by disassembling a cross-compiled witness binary: `ldr x16, [x1, #0x18]` + `blr x16`, slot 24 = first method). And `Box with float` under a `where` constraint is a clean E2062 refusal, not the compile-time panic the row recorded — which is why `float.hash` is DIRECT-dispatch only and this rung made no crash reachable. ///// ⭐⭐ A REVIEW SABOTAGE RESULT WORTH KEEPING, ABOUT TEST DESIGN RATHER THAN THIS RUNG: injecting the exact ModR/M swap `encodeMovqGprXmm`'s comment warns of turned RED only the THREE cases that PIN A NUMBER (`chained`, `negative`, `nan`). The three that assert RELATIONS (`nonzero`, `zero-normalization`, `equal-values-equal-hashes`) all stayed GREEN, because both sides of the relation were wrong identically. A relational assertion cannot catch a bug that moves both operands. | 39,380,763 | 74,372,956 | 148,393,169 | 298,045,013 | 605,713,096 | 1,240,123,172 |
| 2026-07-29 | P1.8 SLICE D OPTIMIZATION PASS — `CharacterSet` + UCD `General_Category` + the three trims. ///// ⚠⚠ READ THE DELTA COLUMN AS A PATH ARTIFACT AGAIN — AND THEN IGNORE IT, BECAUSE THIS ROW REPLACES IT WITH A REAL MEASUREMENT. The printed delta is -4,046 … -143,473 allocations, negative and growing with rung, against a previous row written from the `maxon-p18b2` tree (11 chars) while this one is `maxon-p18d` (10). That is the checkout-path term this log has now recorded FOUR times, and the B2 row explicitly recorded its own rung cost as "owed, not claimed". ⭐ IT IS NO LONGER OWED. Paid here by a path-clean A/B: the parent `71cd8479b` checked out to `C:\Users\Eric\dev\maxon-base` — EXACTLY as long as `C:\Users\Eric\dev\maxon-p18d`, so the path term cancels — and built by the BYTE-IDENTICAL bootstrap (md5 9375432ee32201bf81263d4d9212c3e8 in both trees). THE WHOLE OF SLICE D COSTS THE SHARED CORPUS +14 ALLOCATIONS, FLAT AT ALL SIX RUNGS (a 32x span), and bytes of +1,352 +1,864 +2,888 +4,936 +9,032 +17,224 — which DOUBLE exactly, i.e. 16 bytes per FUNCTION plus ~840. Growth ratios are IDENTICAL on both binaries (x1.80 x1.89 x1.94 x1.97 x1.98), so the rung changes no curve anywhere. The +16/function is `phase:parse` with ZERO extra allocations — an existing per-function allocation crossing one 16-byte slab size class, the P1.0d.3 signature; which field was not isolated, and at 0.006% of the compile it was not chased further. ///// THE CORPUS IS BLIND TO EVERY CONSTRUCT THIS RUNG ADDS, MEASURED NOT ASSUMED: `--emit-corpus` at rung 5 finds `trim` 0, `CharacterSet` 0, `CharSet` 0, `Set with` 0, `Character` 0, `\uXXXX` 0. SIXTH consecutive rung with that property. New private ladder committed: `Testing/ladders/gentrim.sh`, four families (sites/data/edge/loop). ///// ⭐⭐ THE FIX: `trimStart` WALKED THE WHOLE STRING TO ANSWER A QUESTION IT HAD ALREADY SETTLED AT THE FIRST KEPT CLUSTER. `__str_trim` scans forward only (a deliberate divergence — the reference's backward `findGraphemeStart` has no GB12/GB13 rule and splits a flag the forward segmenter joins), and it ran that scan to the END on all three trims. But with `fromEnd` clear the result's high bound is the byte LENGTH and `keptEnd` is never read, so every cluster visited after the first NON-matching one was pure waste: a `__gr_end`, a `__char_at` MINT (an `__mm_alloc` + blit the bump slab never gives back), a `__cs_contains` and a `__str_decref`, all to compute a discarded bound. MEASURED on one call — `"abcdefgh"x65,536`.trimStart(), which has NOTHING to cut — 56.2 ms and 45.0 MB of peak RSS to answer "byte 0". FIXED by exiting the walk at the first kept cluster when `fromEnd` is clear (`buildStrTrim`'s new `firstKeptStop` arm, guarded by `keptStart == sentinel` so it is reached AT MOST ONCE per trim and no other path pays for it). AFTER: 0.46 ms and 4.8 MB — 121x and 9.3x — and the 0.46 ms that remains is the RESULT COPY (`emitSliceSegment` blits 512 KB), which is the floor for an owned result. On the ladder, `data-trimstart-clean` at 524,288 bytes went 262.2 ms -> 4.6 ms and 208.0 MB -> 6.7 MB. ⚠ `trim` and `trimEnd` are UNCHANGED to within noise at every rung (262.4 vs 268.7 ms; 261.8 vs 261.1 ms; RSS identical to the KB): they genuinely need the far end and they paid nothing for the exit. ⚠ THIS ROW'S SIX NUMBERS ARE Δ0 FOR THAT FIX, BIT-FOR-BIT, at all six rungs in allocations AND bytes — verified by building the pre-fix HEAD and re-running. That Δ0 is the instrument's blind spot, not the cost: `buildStrTrim` runs only under `usage.usesStrTrim` and the corpus never sets it. ///// `__str_trim` IS O(n), NOT O(n²) — the question the brief asked, settled by measurement rather than by reading the graph. `data-*` reads x2.00 per doubling out to 524,288 bytes in EVERY seed, in time and in peak RSS alike; `edge-*` (matched pad doubling against a fixed body) likewise. Per CLUSTER at the top rung: ASCII 102 ns, 2-byte BMP 150 ns, supplementary 190 ns — so `__gr_end`'s general scan costs ~48 ns over its ASCII fast path and `__ucd_cat`'s 806-ENTRY BINARY SEARCH costs ~40 ns over a direct BMP byte load. Real but bounded constants; nothing bends. ///// ⭐ THE SLAB TRIGGER FIRES A THIRD AND FOURTH TIME, and one of them is now PRICED. Workstream O's debt (bump allocator, no free list ⇒ memory linear in ITERATIONS not live data) named its trigger as "any construct that allocates once per loop trip"; `for c in s` was the first, `slice` the second. Slice D adds TWO more: a trim mints an owned result String per call, and A PREDEFINED `CharacterSet` IS REBUILT FROM SCRATCH ON EVERY CALL (the reference caches one in a `static let`; shv2 has no module-level `static let` of a heap value). PRICED by the `loop-trim` vs `loop-trim-shared` A/B — the same program but for whether the set is hoisted out of the loop: ~1,068 ns and ~1,155 BYTES OF NEVER-RECLAIMED SLAB PER CALL, which is HALF of what a whole `trim()` of a 9-byte string costs. It is a CONSTANT per call (the set has a fixed 7 members), not a growing one; what makes it matter is that the slab never gives it back, so in a loop it is linear in trips (x2.00 per doubling in RSS, 4,096 -> 65,536 trips). NOT fixed here: caching needs a language feature (module-level static heap values), and the slab's cure is a recycling `__mm_free` that `MmRuntime.maxon:738` already anticipates. Both remain the coordinator's to sequence. ///// ALSO MEASURED, NOT FIXED, AND REPORTED FOR A RULING: a no-argument `s.trim()` costs the COMPILER ~3,266 allocations per SITE against ~500 for `s.trim(cs)`, because the parser inlines the whole 7-member default preset (a set create, 7 minted Character literals, 7 inserts, a box) at every site. At 1,024 sites that is 4.24M allocations against the control's 0.90M. It is strictly LINEAR in sites, so it is a constant factor and not the mandate — recorded rather than chased, and a runtime `__cs_default()` entry point would collapse it if the coordinator wants it. | 39,381,605 | 74,374,310 | 148,395,547 | 298,049,439 | 605,721,618 | 1,240,139,886 |
| 2026-07-29 | P1.8 SLICE E OPTIMIZATION PASS — the String view/method leftovers, and the stdlib WHITELIST's first real scaling question. NOTHING WAS MADE FASTER, because nothing found was superlinear; what this row buys is the ATTRIBUTION of a delta column that would otherwise have read as an unexplained bend. ///// ⭐⭐ THE TRIGGER WAS THE PARSE DELTA GROWING +1,678 -> +2,422 ACROSS THE LADDER (total +4,429 -> +5,660), on a rung whose structural change is that `stdlib/helpers/string/utf16.maxon` is now compiled into EVERY invocation. The question that raises is the one `StdlibLoader.maxon`'s own header makes urgent — the list "IS DESIGNED TO GROW ... until it names every file there", 49 files against 3 listed — so if per-compile cost were O(corpus x listed modules), today's 0.005% becomes a genuine superlinearity in the loader's intended end state. THE ANSWER IS NO, AND IT IS EXACT RATHER THAN APPROXIMATE. `maxon-shv2/Testing/ladders/genwhitelist.sh` (committed) varies the MODULE COUNT with the corpus HELD — the axis the shared instrument has no knob for, since its module count is fixed at whatever the whitelist says that day, and a product term and a sum term are indistinguishable when one factor never varies. Extra `phase:parse` allocations vs MODULES=0, at 128/256/512-function corpora: +1,914 / +7,656 / +30,632 / +122,512 for 1/4/16/64 modules — IDENTICAL TO THE ALLOCATION at every corpus size, exactly linear in the module count, exactly invariant to the corpus. The same knob dropped into `scale-test`'s OWN emitted rung 0 (30 files, 4,890 lines) and rung 5 (185 files, 140,917 lines) reads the SAME four numbers at both ends of a 32x span. Repeated with modules carrying array literals and generic instances — the documented `O(files x types)` hazard `arrayLiteralElementsInterned` exists for — parse reads +8,936 (M=16) / +35,728 (M=64) at rung 0 and the same at rung 5, while `signatures` and `merge` come out very slightly SMALLER on the bigger corpus (-55/-126, -26/-80) because the corpus has already interned what the module wants. That is the only cross term that exists and it has the WRONG SIGN to be a hazard. ⇒ 49 listed modules will cost the SUM of 49 modules' own parses, at any program size; per unit of user code an unused listed module costs ZERO. ///// ⭐ SO WHAT WAS THE +744? NOT THE WHITELIST — measured with a THIRD binary, `nowl`, identical to head but for the one whitelist line, built by the same bootstrap and driven interleaved from an equal-length path. `head - nowl` (the whitelist entry ALONE) is +1,654 `phase:parse` allocations at EVERY ONE of the six rungs, +659 lex, +212 signatures, ~+400 merge, +4,453 total at rung 0 and +4,444 at rung 5 — FLAT, and independently reproducing the implementer's own isolated figure. `nowl - base` (all the REST of slice E) is +24 / +48 / +96 / +192 / +384 / +768 across the six rungs — an exact x2.00 doubling, strictly linear in program size — plus a FLAT -70 saving from the `insertRangeChecks` unreachable-stdlib skip. It is +3 ALLOCATIONS PER TRIVIAL ARRAY-ELEMENT READ: `emitArrayElementAccessor` now asks `arrayElementValueType`, which `adoptType`-interns the element's declared name, where it used to hardcode `ValueTypeTag.integer`. Verified in isolation at 8/16/32/64 `.get()` sites reading +24/+48/+96/+192, and confirmed by deleting `u_arrays.maxon` from the corpus, which takes the delta to EXACTLY ZERO in parse AND merge. It is the price of the type-identity fix that makes a `Byte` read back out of an `Array with Byte` still a `Byte` — a slope, not a curve. ///// ⛔ THE CORPUS IS BLIND TO EVERY PART OF THIS RUNG — SEVENTH CONSECUTIVE RUNG. `--emit-corpus` at rung 5: `toByteArray` 0, `replaceFirst` 0, `codepoints` 0, `utf16` 0, `String.from` 0, `.bytes()` 0, `.replace(` 0, `.isEmpty()` 0 in any receiver. ⚠ AND THE TRAP IN REVERSE: `.clone()` appears 256 times and EVERY ONE IS AN `Array` RECEIVER (`ac0`…`ac255`, the array-ops knob) — not one String. A non-zero on the shared ladder for a `clone` still cannot be ABOUT this rung. `maxon-shv2/Testing/ladders/genstringviews.sh` is committed as the instrument (three families: `sites-*` compile, `data-*` run-on-length, `loop-*` run-on-trips; nine site modes incl. a control and the whitelisted `utf16*` free functions; ascii/wide/supplementary seeds). ///// WHAT IT READ. COMPILE, 64 -> 1,024 sites: every one of the nine modes linear in allocations and CPU, all ratios approaching x2.00 from below. `sites-utf16fns` is the CHEAPEST of the nine (488,192 allocations at 1,024 sites vs the control's 921,668) — a whitelisted stdlib call costs less than the control's `append`. RUN, `data-*` 32,768 -> 524,288 bytes: ⭐⭐ x2.00 PER DOUBLING IN EVERY MODE AND EVERY SEED, so ALL THREE MATERIALIZED VIEWS ARE O(n) — the push-only loops (`__str_to_bytes`/`__str_to_codepoints`/`__str_to_utf16` call `__arr_push` once per byte or codepoint into an array created with NO reserve) do NOT inherit a reallocation quadratic, which was a real open question and is now measured rather than read off `__arr_grown_cap`'s geometric policy. Per byte at the top rung: `clone` 1.66 ns, `replaceFirst` 3.2 ns, `replace` 4.6 ns, `toByteArray` 16.4 ns, `codepoints` 83.6 ns, `utf16` 93.2 ns. `replaceFirst` beats `replace` 1.43x at every rung on an absent needle, confirming the slice's claim that a known result size removes one of `replace`'s two passes. ⭐ Per CODEPOINT, `utf16` reads 93.2 / 96.5 / 191.1 ns for ascii / wide / SUPPLEMENTARY — the supplementary seed costs almost exactly DOUBLE, which IS the surrogate-pair arm pushing two units where BMP pushes one, and no seed but `supp` reaches it. ///// ⚠ WORKSTREAM O's SLAB DEBT FIRES AGAIN, RE-MEASURED, NOT RE-FILED. `loop-*` at 2^17 -> 2^20 trips reads x2.00 per doubling in peak RSS for every construct: memory linear in ITERATIONS. Never-reclaimed slab per trip at 1,048,576 trips on a 10-codepoint subject: `clone()` 72 B (the cheapest measured — HALF the control, which mints two String records to a clone's one), control 141 B, `toByteArray()` 161 B, `codepoints()` 376 B, `utf16()` 378 B. ⇒ `codepoints()`/`utf16()` cost ~2.6x the control and ~5x `clone()` per call, because an 8-byte-element array created with no reserve walks 4 -> 8 -> 16 capacities and the bump allocator keeps every intermediate buffer. The runtime KNOWS the bound at entry (`__str_to_bytes` already loads `len`), so one `__arr_reserve` would collapse it — a CONSTANT-FACTOR change on an already-linear curve, and it would move committed goldens, so it is REPORTED for a decision and deliberately not shipped by the optimizer that found it. ///// GATES: build exit 0 / 0 warnings, and the rebuilt binary is md5-IDENTICAL to the one the coordinator gated, so the tree is exactly as committed. Full unfiltered suite 2293 passed / 0 failed, exit 0 (no 101). `specs-shv2/fragments/` EMPTY after that run — byte-identical codegen. This pass changed no compiler source, so every per-rung number above is unmoved by it; the two ladders are the only files added. | 39,582,207 | 74,572,896 | 148,599,797 | 298,252,249 | 606,094,700 | 1,240,359,592 |
| 2026-07-29 | BREAK-IN-MATCH RUNG — a `match` STATEMENT is now a `break` target. LINEAR, and the delta is a SLOPE not a curve. Allocation deltas vs base: +365 / +546 / +910 / +1,636 / +3,090 / +5,996 across the six rungs; delta ratios 1.50 -> 1.67 -> 1.80 -> 1.89 -> 1.94, approaching x2.00 FROM BELOW while the corpus's own emitted code grows x1.85 -> x1.99. Sub-x2 throughout, so per unit of user code the cost is flat. ///// ATTRIBUTION, exact rather than inferred, because the change is small enough to enumerate: (1) ONE `MatchContextArray` per function parse and per closure parse, mirroring the `LoopContextArray` already created at those same three sites (`parseFunction`, and the save/reset/restore pair in `parseClosureExpression`); (2) ONE `MatchContext` + ONE `MatchArmExitArray` per match STATEMENT — gated on `not isExpression`, so a match EXPRESSION costs zero; (3) nothing per statement, per op, per value, or per token. That is why the delta tracks the corpus's match-statement count and nothing else. ///// NO NEW SUPERLINEARITY, and the one lookup that could have been one is not: `resolveControlTarget` walks the match stack at most ONCE per `break`/`continue` parsed, bounded by MATCH NESTING DEPTH — the identical O(depth) bound `self.loops` has had since M4b, over a stack that E2049 keeps shallow (a match arm body is a single statement and may not open a block, so matches nest only through the scrutinee). The labelled search scans matches-then-loops, two bounded walks, not a product. ///// NO STEP-6b OPTIMIZER TRIGGER FIRED and this row records why, so the absence is a reading rather than an omission: the rung adds no PASS, no IR OP, and no collection the compiler INDEXES BY (a parser stack pushed and popped in lockstep with syntax is not an index), and no allocation ratio came near the 2.4 threshold. The hunt carries to the phase-boundary sweep. ///// CORRECTNESS EVIDENCE STRONGER THAN THE SPEED READING, which is the part worth keeping: `specs-shv2/fragments/` is EMPTY under `git status` after a full unfiltered run, and against `main` the rung moves ZERO pre-existing goldens on x64-windows AND on x64-linux — 19 additions per target plus exactly ONE modification, `match-owned-value-flow/temp-scrutinee-break-arm`, whose diff is the SOURCE LINE ALONE (`break` -> `break 'loop'`) with byte-identical IR on BOTH targets. A parser change that re-targets a control-flow keyword and moves no existing byte of codegen is the strongest statement available that the new target is reached only by the programs that ask for it. | 39,601,877 | 74,600,326 | 148,642,819 | 298,326,383 | 606,231,130 | 1,240,620,542 |
| 2026-07-29 | P1.9 DECLARATION-SITE DIAGNOSTICS — E3062 unused-typealias (per-file use tracking, drained whole-program), E3005 bound-PROVENANCE validation at the typealias declaration, E2003 bare-sized shorthand, alias VISIBILITY + the stdlib carve-out, and E3005 on an out-of-range `otherwise` value. Measured on the MERGED tree, so this delta is against the BREAK-IN-MATCH row directly above. ///// LINEAR, AND THE SHAPE IS EXPLAINED RATHER THAN ASSERTED. Allocation delta +4,320 / +4,993 / +6,343 / +9,011 / +14,315 / +24,891 across the six rungs; ratios 1.16 -> 1.27 -> 1.42 -> 1.59 -> 1.74, rising monotonically toward x2.00 FROM BELOW. That is the signature of a FLAT term plus a linear one, not of a curve: fitting `a + b*n` to the two ENDPOINTS alone gives **a = 3,656 flat, b = ~664 per corpus unit**, and that two-parameter fit then predicts all four INTERIOR rungs to within 0.5% (4,983 / 6,310 / 8,965 / 14,274 predicted vs 4,993 / 6,343 / 9,011 / 14,315 measured). The flat term is the fixed per-compile cost — the seeded stdlib alias, the registry structures, the all-declarations `nameable` index — paid once whatever the program size; the ~664/unit is the per-FILE used-alias set plus the carried bound provenance, which is what a per-file check must cost. At rung 5 the whole rung is +24,891 on 14,803,643 = **+0.17%**. ///// ⚠ AN EARLIER READING OF THIS SAME RUNG SAID +1,990 -> +11,897 (fit: 1,670 flat + 320/unit), AND IT IS NOT A CONTRADICTION — IT IS A DIFFERENT BINARY. That was step 6a, run on the implementer's commit ALONE; the figures here include the independent review's two fixes (`RangedAliasRegistry.nameable`, an all-declarations index probed on the error path, plus the parenthesized-conformance reader and a visibility bit folded into the signature content hash) and the coordinator's path-resolution fix. The cost roughly DOUBLED and the SHAPE did not move at all — same approach-from-below, same sub-x2 ratios, same sub-1% fit. Recording both is the point: a per-rung ladder read taken before the review is a reading of code that is not what ships. ///// NO STEP-6b OPTIMIZER TRIGGER FIRED. The threshold is an ALLOCATION ratio past ~2.4 that you cannot explain; the highest here is 1.74 and it is accounted for to 0.5% by two parameters. `phase:resolveTypes`, where E3062 and the visibility check both live, reads 643/963/1,603/2,883/5,443/10,563 — ratios 1.50/1.66/1.80/1.89/1.94, the same shape. The hunt carries to the phase-boundary sweep. ///// ⚠⚠ THE CORPUS CANNOT EXPRESS THIS RUNG'S SUBJECT, SO THE DELTA ABOVE IS NOT ABOUT IT. `scale-test`'s generated programs declare ranged typealiases and USE them, so across every rung the E3062 arm that REPORTS never fires once, no bound-provenance mismatch is ever constructed, no bare-sized RHS is ever written, no alias is ever hidden cross-file, and no `otherwise` literal is ever out of range. What the ladder measures is the cost of ASKING on a corpus where every answer comes back "fine" — the per-file set built and walked, the provenance carried and compared. That bound is worth having and it is real, but a future rung must not read this row as evidence about the DIAGNOSTICS. Eighth consecutive rung whose subject the shared corpus cannot reach. | 39,824,776 | 74,862,085 | 148,984,782 | 298,828,194 | 607,052,097 | 1,242,079,541 |
| 2026-07-29 | OPTIMIZER PASS over the ranged-alias bound-enforcement rung. THE HEADLINE IS A CORPUS-BLINDNESS FINDING, NOT A SPEEDUP, and it reframes the rung's own row above. THE SHARED LADDER CANNOT SEE THIS RUNG'S GUARDS AT ALL. ScaleCorpus DOES declare narrow ranged typealiases -- ScaleCoverNarrow int(0 to 4095), ScaleWide int(0 to u64.max), and 4->128 ScaleXElem<N> int(0 to 10<N>) that DOUBLE with the rung -- so a reader checking the manifest for "does the corpus emit ranged aliases" gets YES and stops. But not one of them reaches a position that emits a guard: ScaleXElem<N> is only ever a generic TYPE ARGUMENT (ScaleXBox with ScaleXElem<N>), whose field and parameter are declared with the opaque type parameter T, so the alias is erased before any door sees it -- 128 narrow aliases, zero doors; and every alias that DOES reach a door (ScaleXHoldValue, ScaleMeasure<N>, ScaleElement, ScaleNamedValue, ScaleRowValue, ScaleSigned, ScaleDispMeasure, ScaleGenElement) is int(i64.min to i64.max), FULL RANGE, which needsRuntimeGuard discards at rangeIsFull. MEASURED, not inferred: the compiled rung0 binary and the compiled rung5 binary each contain EXACTLY ONE range-check panic blob, the same one, and an instrumented build confirms exactly ONE function in the whole program ever emits a runtime guard -- scaleCoverCast, at every rung. So the rung's +1,340/+2,119/+3,665/+6,751/+12,885/+25,150 measured the RECORDING of sites that are then discarded as vacuous; the EMISSION path has never been on this ladder. That is the subtler form of the blindness the manifest warns about: the construct is present, the KNOB THAT MAKES IT COST ANYTHING is not. ///// SO A PURPOSE-BUILT LADDER WAS ADDED -- Testing/ladders/genrangesites.sh, two modes, onefunc (N guards in ONE function, the product axis) and spread (the same N one-per-function, the control). It found and this pass FIXED a real O(sites x blocks) term: RangeCheckEmitter.blockEndGuardSite located each guard site's block with IrModule.getBlockByIdIn, a LINEAR SCAN of func.blockRefs, once per site -- and worse than the plain product, because every guard the pass emits APPENDS TWO BLOCKS (__rc_ok, __rc_panic) to the very set the next site's scan walks. It now resolves through buildBlockIdIndex/blockById, the index pruneDeadBlockArgs introduced for exactly this shape one pass over ("would be O(edges x blocks), the shape the allocator's data-representation contract exists to forbid") and elimTrivialBlockArgs already shares -- a third user of one index, not a fourth mechanism. THE TERM WAS ISOLATED BY HOLDING ONE AXIS AND MOVING THE OTHER, genwhitelist.sh-style: with guards held at S and blocks swept B=512..4096, the CPU slope in B was 4,045 ticks/block at S=1 and 5,564 at S=8 -- a slope that MOVED WITH S is the product term, and it separates as roughly 3,828 per block per function plus 217 per (site x block). After the fix the two slopes are 3,905 and 4,020, ratio 1.375 -> 1.029: the S-dependence is GONE. Phase CPU at S=8 B=4096: 23,244,143 -> 16,867,165 ticks, -27.4%. ⚠ THE ALLOCATION COLUMN IS BLIND TO ALL OF IT -- it read FLAT in B before and after, because a scan allocates nothing. This is the third column earning its place. ///// THE +2 ALLOCATIONS ON THIS ROW, FLAT AT EVERY RUNG, ARE THE WHOLE COST: the shared empty index plus the one real index scaleCoverCast builds. It was NOT flat in the first cut. Storing the index on RangeCheckEmitter with an unconditional ValueIdArray.create() cost +45/+66/+108/+192/+360/+696 across the rungs, because an emitter is built for EVERY function carrying any range-check side data -- which includes every function that merely passes a CONSTANT ARGUMENT or constructs a generic instance -- so the overwhelming majority paid for an array they never read. Fixed the way Parser already solves the identical problem, a shared never-mutated empty (sharedEmptyInstanceAliasNames), and the index is built lazily on the first RUNTIME guard, which a compileTimeOnly call-argument site never reaches. The residual growing BYTES (+392/+560/+896/+1,568/+2,912/+5,600) are allocation-free and fully attributed: they are the 8-byte field added to RangeCheckEmitter times the emitter count -- 392/8 = 49 emitters at rung0, 5,600/8 = 700 at rung5, matching the +45 and +696 allocation counts that were just removed. ///// HANDED BACK, NOT FIXED, BECAUSE IT IS NOT THIS PASS'S SUBSYSTEM: ~16 runtime range-check guards in ONE function PANIC the compiler in the register allocator -- "register-pressure E5001 ... value is in the blocking set but resolves to NO source span ... This is a COMPILER BUG" from RegisterPressureDiagnostic, raised out of splitLiveRanges. Bisected to n=16 guards, and the CONTROL is clean and separates cleanly: the identical program with the alias widened to int(i64.min to i64.max) -- same statements, same field stores, no guards -- compiles fine, and n=12 compiles fine. It is PRE-EXISTING rather than this rung's: the old `as`-cast door reaches it at the same n=16. But this rung makes it far easier to reach, because a field store is far more common than an explicit cast. It also CAPS the onefunc ladder at n<=12, which is why the product term above had to be measured on the block axis instead. ///// CORRECTNESS: full unfiltered spec-test 2348/0, exit 0, no leak, and specs-shv2/fragments COMPLETELY CLEAN after the run -- byte-identical Target IR for 2,348 programs, which is the strongest available statement that an optimization changed no behaviour. | 39,684,455 | 74,757,300 | 148,962,620 | 298,990,651 | 607,620,438 | 1,243,588,674 |
| 2026-07-29 | RANGED-ALIAS BOUNDS ENFORCED AT EVERY POSITION — six doors, not two. LINEAR; the delta is a SLOPE. Allocations vs the break-in-match row: +1,340 / +2,119 / +3,665 / +6,751 / +12,885 / +25,150, delta ratios 1.58 -> 1.73 -> 1.84 -> 1.91 -> 1.95, approaching x2.00 FROM BELOW against a corpus growing x1.85 -> x1.99. ///// ⭐⭐ THE MOST IMPORTANT THING THIS ROW RECORDS IS THAT THE LADDER CANNOT SEE THIS RUNG AT ALL — EIGHTH CONSECUTIVE RUNG, and the mechanism is a NEW ONE worth naming. The manifest check PASSES: `ScaleCorpus.maxon` really does declare narrow ranged aliases (`ScaleCoverNarrow = int(0 to 4095)`, and 4->128 `ScaleXElem<N>` that DOUBLE with the rung). But `ScaleXElem<N>` is only ever a GENERIC TYPE ARGUMENT (`ScaleXBox with ScaleXElem<N>`), so the field and parameter it flows through are declared with the opaque type parameter `T` and the alias is erased before any door sees it: 128 narrow aliases, ZERO doors. Every alias that DOES reach a door is `int(i64.min to i64.max)` — full range, discarded at `rangeIsFull`. MEASURED, not inferred: the compiled rung-0 and rung-5 binaries each contain EXACTLY ONE range-check panic blob, the same one, and an instrumented build confirms exactly ONE function ever emits a runtime guard, at every rung. ⇒ **A NARROW ALIAS IN THE CORPUS IS NOT THE SAME FACT AS A NARROW ALIAS AT A DOOR.** A future manifest check for this feature must count DOORS, not aliases. `Testing/ladders/genrangesites.sh` is committed as the instrument that does. ///// WHAT THE PURPOSE-BUILT LADDER FOUND, which the shared one structurally could not: `blockEndGuardSite` located each site's block with `getBlockByIdIn`, a LINEAR SCAN per site — and worse than a plain product, because every guard emitted APPENDS TWO BLOCKS to the set the next site's scan walks. O(sites x blocks) -> O(blocks + sites) via the block-id index `pruneDeadBlockArgs` already owns (third user of one index, not a fourth mechanism). Isolated by holding guards and sweeping blocks: slope 4,045 -> 3,905 ticks/block at S=1 and 5,564 -> 4,020 at S=8, so the PRODUCT term went 1.375 -> 1.029. Phase CPU at S=8/B=4096: 23,244,143 -> 16,867,165 ticks, -27.4%. ⚠ **THE ALLOCATION COLUMN READ FLAT IN B BEFORE AND AFTER — a scan allocates nothing, so this was findable ONLY in CPU.** That is the column O4 exists for, earning itself again. ///// THEN THE FIX THAT MATTERED MORE THAN THE SPEED. A guard SPLITS its block, so resolving every site to the parser-recorded block id chained guards BACKWARDS: guard k+1's cascade landed before guard k's branch. Two consequences, both silent: guards ran in REVERSE SOURCE ORDER (the compiler reported the SECOND of two bad casts), and `recordFusableCompare` lost fusion for N-1 compares, which then materialized through `setcc` and sat in registers until the 14-GPR file overflowed at N=16 and the RULE 3 backstop panicked. `where-clauses.two-constraints` shows it was a genuine MISCOMPILE, not just ordering: two cascades compared rbx against 0 and r12 against 1000, mixing two fields' values across two checks. Fixed by making the index a CHAIN END rather than a block identity; the values stop existing as registers at all, so no rematerialization was needed. Threshold gone: both doors compile and run at N=512. At n=12 `regalloc:splitting` went 149,767,046 -> 73,836,068 CPU (-50.7%) and 7,196 -> 4,393 allocations (-39%). ///// AND THEN GUARD POSITION, on a user ruling. A guard fired at END OF BLOCK, so the whole rest of the block ran first: an out-of-range value was PRINTED and read back out of a `Percent` slot before its panic, and `100 / d` panicked "integer divide by zero" where the oracle panics "Range check failed 'NonZero'" — the language's own check pre-empted. Fixed with a two-tier position (`maxonOpPos` stamped by the parser, `stdOpPos` translated by a single `RangeSiteWalk` in `lowerMaxonToStd`), composed with the chain via `guardChainBase` so N guards at N positions fire in source order. +11 allocations FLAT for the whole compile plus +1 per EMITTED guard; 70 goldens moved, every one the same shape (the cascade moving up, the instructions between site and old block end moving past it), emitted code SIZE identical at every rung. ///// ⚠ MEASURED DEBT, RE-MEASURABLE AND NOT FIXED: `regalloc:splitting` is QUADRATIC IN GUARDS-PER-FUNCTION, now observable to n=512 (x2.09, 2.37, 3.06, 3.53, 3.77, 3.89 per doubling). The `spread` control — same guard count, one per function — is LINEAR (->x1.96), and a guard-free N-blocks-in-one-function control is linear too, so it is per-function CONCENTRATION, not guards and not blocks alone. NOT created by this rung (block and value counts unchanged; the fix strictly reduced it), only newly REACHABLE, and it looks like Workstream O's existing `regalloc:splitting` debt arrived at by a new axis. Re-measure trigger: `genrangesites.sh onefunc` at n>=128. | 39,704,879 | 74,794,988 | 149,034,836 | 299,131,923 | 607,899,822 | 1,244,144,282 |
| 2026-07-29 | RE-READ ON THE MERGED TREE — the rows above this one were measured on a binary that did not contain P1.9's declaration-site half, which landed upstream mid-rung. Same correction upstream's own last row made, for the same reason, and it is worth stating as a rule: A LADDER ROW IS ABOUT A BINARY, NOT ABOUT A CHANGE, so a rebase onto an advanced main invalidates it even though nothing in the rung moved. ///// THE MERGED NUMBERS: allocations 587,761 / 1,049,716 / 1,972,398 / 3,814,797 / 7,497,484 / 14,859,091, deltas +4,320 / +4,993 / +6,343 / +9,011 / +14,315 / +24,891 vs the row directly above. (⚠ CORRECTED AFTER THE RUN: the note as submitted quoted deltas from an earlier measurement against a DIFFERENT BASELINE, which is the same class of error this row exists to fix. The numbers here are the ones the instrument printed.) Delta ratios 1.16 -> 1.27 -> 1.42 -> 1.59 -> 1.74 — approaching x2.00 from below, LINEAR, and TIGHTER than the pre-merge read (1.58 -> 1.95) because P1.9's declaration-site checks add per-declaration work that scales with the corpus rather than per-site work that does not. `phase:insertRangeChecks` reads 2,013 / 3,390 / 6,128 / 11,586 / 22,488 / 44,270 — ratios 1.68 -> 1.81 -> 1.89 -> 1.94 -> 1.97, linear, 0.30% of the compile at rung 5 (up from 0.094% pre-merge, which is the guard-POSITION work: a two-tier position per site plus the `RangeSiteWalk` translation). ///// NOTHING SUPERLINEAR APPEARED IN THE COMBINATION, which was the open question a merge of two range-checking rungs raises: P1.9 checks a typealias at its DECLARATION (per-file, drained whole-program) and this rung checks values at their POSITIONS (per-site), and the two do not multiply — declarations and sites are independent axes and the ladder grows both. `regalloc:splitting` is unmoved at x2.19/x2.14/... per doubling, the same profile as before the merge. ///// ⚠ THE MEASURED DEBT FROM THE ROW ABOVE STANDS UNCHANGED and is NOT re-measured here, because the shared ladder still cannot express it: `regalloc:splitting` is quadratic in guards-per-FUNCTION (x3.89 per doubling at n=512 on `genrangesites.sh onefunc`), while this corpus emits one guard in the whole program. Re-measure trigger is unchanged: `genrangesites.sh onefunc` at n>=128, not this table. | 39,941,737 | 75,072,386 | 149,395,798 | 299,659,453 | 608,759,948 | 1,245,669,320 |
| 2026-07-29 | G1 (worker-exe split) — HARNESS-ONLY, AND THE LADDER IS UNMOVED BY IT. The rung splits `Main.runSpecTest`'s single `Process.executablePath()` into workerExe (what a pool worker IS) and compilerExe (the compiler under test, new `--compiler=<path>`), touching only Main.maxon + Testing/SpecWorkerPool.maxon — no pass, no IR op, no collection the compiler indexes by, so no step-6b optimizer trigger fired. ///// ⚠ THE +192 ALLOCS AND NEGATIVE BYTES DELTAS AGAINST THE ROW ABOVE ARE NOT THIS RUNG'S, AND THAT IS A MEASUREMENT RATHER THAN A READING. A/B at the SAME CHECKOUT PATH (the one thing that silently reverses this measurement's sign), rebuilding the pre-change sources from 518a19274 into the same location: PRE reads allocs 587,953 / 1,049,908 / 1,972,590 and POST reads 587,953 / 1,049,908 / 1,972,590 — identical to the digit, with allocsDelta +192 and bytesDelta -12,107 / -14,627 / -19,667 on BOTH. (The A/B climbed 3 rungs, not 6; the delta is a CONSTANT +192 across all six rungs of the post run, i.e. flat in input size, so it is a fixed per-compile cost and the three shared rungs settle it.) The deltas are therefore inherited from commits that landed on main between the previous logged row's binary and 10e518b8e; G1 contributes exactly ZERO. Worth writing down because the obvious story — "a new module-level `--compiler=` string constant plus a new function, hence a few more startup allocations" — is plausible, is flat-in-rung-size exactly as the +192 is, and is WRONG. ///// The A/B is the only honest form for this question: a Δ0 claim about MY change cannot be read off a delta against SOMEBODY ELSE'S baseline, because a log row is about a BINARY and not about a change — the rule the row above this one states — and two independent code deltas separate that logged binary from this one. | 39,929,630 | 75,057,759 | 149,376,131 | 299,629,706 | 608,710,041 | 1,245,579,093 |
| 2026-07-29 | FIRST-CLASS TUPLES (heap-allocated) + an O(files x types) FIX THAT PREDATES THEM. ///// THE RUNG ITSELF IS +12 ALLOCATIONS FLAT AT EVERY RUNG — two `ProgramSignatures` maps constructed once per compile, and nothing else. A tuple is a SYNTHESIZED STRUCT (`__Tuple2.int.int`) typed through the existing `MaxonType.structRef`, so no new type-system arm, and the per-Parser layout map uses the codebase's shared-empty + copy-on-write idiom so a tuple-free file allocates ZERO. ///// ⛔⛔ NINTH CONSECUTIVE RUNG THE SHARED LADDER CANNOT SEE, and this time it is not subtle — the feature did not EXIST until this commit, so the corpus could not contain it. MEASURED rather than assumed: `--emit-corpus` wrote 465 files / 279,453 lines across rungs 0-5, and greps for tuple destructuring, `.N` element access, tuple literals, tuple return types and tuple typealiases return ZERO hits each; `Testing/ScaleCorpus.maxon` contains the string "tuple" zero times. So the +12 measures two empty maps and not one line of `internTupleType`/`registerTupleLayout`/`canonicalTupleName`/`parseTupleLiteral`. `Testing/ladders/gentuples.sh` is committed as the instrument that does reach them — seven modes in four CONTROL PAIRS, which is what lets a product term separate. ///// WHAT THE PURPOSE-BUILT LADDER FOUND, and it was NOT tuples: `internArrayLiteralAggregateInstances` ran ONCE PER FILE holding an `[<identifier>...]` literal, walking every struct/union registered SO FAR over tables still growing — O(files x types). `phase:signatures` allocations at 64->1024 files: 18,356 / 39,630 / 94,408 / 253,058 / 766,908 = x2.16 x2.38 x2.68 x3.03, AFTER 15,490 / 29,750 / 58,218 / 115,102 / 228,818 = x1.92 x1.96 x1.98 x1.99. CPU 785,944,503 -> 187,830,368 ticks at n=1024. ⭐ THE STRUCT-ONLY CONTROL (no tuples anywhere) BENDS IDENTICALLY, which is how it was attributed as PRE-EXISTING: tuples merely added a SECOND producer of registered types and thereby a second way to reach it. The fix is a HOIST — `queryProgramSignatures` folds every file before any reader runs, so one final walk interns the same types; it also closes a latent hole where a type declared after the last literal-bearing file was never interned at all. The deleted dedup Set is why the rung's own footprint fell +20 -> +12. ⚠ The hoist CHANGES GENERIC-INSTANCE ID ORDER (hash-bucket order of a rehashing map -> one final walk); verified safe by the strongest available check — all 2,372 pre-existing fragment goldens byte-identical after a full run. ///// ⚠ MEASURED DEBT, RE-MEASURABLE, NOT FIXED, and NEITHER IS TUPLE-SPECIFIC. (1) `StructLayout.indexOfField` (`Project.maxon:1422`) is O(fields) by NAME STRING, so n accesses over n fields is a product: `arity` parse CPU x1.61/2.34/2.95/3.53 at n=32->512 against `access`'s x1.19/1.34/1.52/2.11 with the field count pinned at 2 — and a DECLARED STRUCT with 512 fields read 512 times reads x1.31/1.76/1.98/2.73 on the same ladder, so it is the struct layout's shape, not tuples'. Allocation-flat, CPU-only, structurally invisible to the memory columns. The realistic instance is the compiler's OWN `Parser` record (~50 fields), where every `self.x` pays an averaged 25-compare scan as a CONSTANT FACTOR, not a curve. Re-measure trigger: `gentuples.sh arity` at n>=128. (2) A nested tuple's mangled name is O(total leaves), so a depth-d chain is O(d^2) bytes of name: `nest` parse CPU x1.37/1.98/2.77/3.55 at depth 8->128. Inherent to a MANGLED-NAME IDENTITY; fixing it needs structural ids rather than names. Linear in practice at realistic depth (<=4). Re-measure trigger: `gentuples.sh nest` at n>=32. ///// CORRECTNESS: full unfiltered suite 2401/0 exit 0 (no 101), x64-linux PASS, wasm32-wasi PASS, arm64 SKIP; 29 fragment ADDITIONS and ZERO modifications, and `specs-shv2/fragments/` clean after a full re-run — byte-identical codegen for every pre-existing program, which is the strongest statement available that a feature this size is purely additive. | 39,934,336 | 75,064,527 | 149,387,023 | 299,648,846 | 608,745,677 | 1,245,647,721 |
| 2026-07-29 | D5 `#if` CONDITIONAL COMPILATION + A MEMO-PROBE FIX THAT MOSTLY PREDATES IT. NET IS NEGATIVE: rung 5 goes 14,864,427 -> 14,858,715, i.e. -388 BELOW the pre-rung baseline, because the fix returns more than the rung costs. ///// THE RUNG ALONE measured +968/+1,108/+1,388/+1,956/+3,084/+5,324 allocations (`828 + 140*2^n`, successive increments x2.00 x2.03 x1.99 x1.99 — dead linear), and `phase:directives` 33/38/48/68/108/188 which is EXACTLY files+3 (corpus has 30/35/45/65/105/185 files). ///// ⛔⛔ TENTH CONSECUTIVE RUNG THE SHARED LADDER CANNOT SEE, and MEASURED not assumed: `--emit-corpus` wrote 465 .maxon files across rungs 0-5 and a grep for hashIf, hashElse and hashEndif spellings returns ZERO matches. So `phase:directives` on this ladder is the per-file memo BOOKKEEPING and NOT ONE TOKEN WAS EVER FILTERED while it was taken. `Testing/ladders/gendirectives.sh` is committed as the instrument that does reach it — nine modes in four CONTROL PAIRS. ///// WHAT THE PURPOSE-BUILT LADDER FOUND: NO SUPERLINEARITY, on any of five axes, and here are the numbers rather than a claim. (a) REGION COUNT: `regions` x1.99 allocs / x2.01 cpu at n=64->2048 against the `plain` control, whose surviving program is IDENTICAL and whose directives phase is FLAT at 5 allocations. (b) DEAD-BRANCH SIZE: `dead` x1.99 / x1.97 — skipping is proportional to what is skipped and no worse, and cheap: 139,616 bytes against `regions`' 2,207,504 for the same input tokens. (c) NESTING DEPTH — THE PRODUCT AXIS, since a liveness test that WALKED the frame stack would be depth x tokens: `nest` 4,130 allocs / 6,753,314 ticks against the depth-1 `flat` control's 4,119 / 5,903,606, both x1.99 — INDISTINGUISHABLE, so liveness is a top-of-stack read and the frame stack is O(1) per directive. (d) CONDITION LENGTH: `cond` 14 allocations FLAT from n=64 to 2048 (`equalsIgnoringAsciiCase` compares source slices in place and allocates nothing), CPU x1.88 — the DELIBERATELY non-short-circuiting evaluator costs exactly its atoms, which is what that design was supposed to cost. (e) CONDITION DEPTH: `parens` 14 allocs flat, CPU x1.66; it stack-overflows between depth 1024 and 2048, AND THE CONTROL FAILS IDENTICALLY — an ordinary expression with 2048 nested parens overflows in `Parser.current` the same way — so that ceiling is the compiler's uniform recursive-descent property, not a hole this rung opened. ///// ALSO MEASURED: the two token caches SHARE one array and there is no hidden copy. A directive-free file's directives phase allocates 40 bytes (n=2048 `plain`: 200 bytes for the whole compile) against 2,207,504 bytes when the filter actually rebuilds — four orders of magnitude, which no copy could hide behind. And a `#if` region costs ~2 allocations (a `ConditionCursor` + a `DirectiveFrame`) whatever it contains. ///// THE FIX — THE MEMO PATTERN PROBED ITS `Map with (FilePath, _)` TWICE FOR ONE FACT, and that is not a free lookup on Windows: `FilePath.hash` CLONES AND LOWER-CASES the path (`canonicalForCompare`) and `FilePath.equals` clones twice more, which is why `Project.maxon`'s `RangedAliasByDeclaringFile` is keyed by a plain String instead. Every query did `xCacheValid(path)` (which probed the map to compare a hash, then threw the memo away) and then probed the SAME map again to read the value — with a panic guarding the disagreement that second probe made expressible. `sourceFiles` was probed once to VALIDATE and again to STAMP. `queryParseOps` computed `currentParseMemoKey` twice. Now: one `sourceEntryOf` per query serves both the validity hash and the stamp, `if let memo = cache.get(path)` serves both the hash and the value, and the parse key is computed once and stamped with that same value. `querySourceFile`, `tokensCacheValid`, `activeTokensCacheValid`, `parseCacheValid` and two impossible-state panics are deleted. RESULT: -(162 + 30*files) allocations, EXACT at every one of the six rungs (-1,062 / -1,212 / -1,512 / -2,112 / -3,312 / -5,712) — three successful FilePath map probes removed per file, so A SUCCESSFUL FILEPATH-KEYED PROBE COSTS 10 ALLOCATIONS. Bytes -43,487 at rung 5 = ~78 per probe, one cloned path string. CPU -0.09% at rung 5: INSIDE the noise band, not a datapoint. Most of this predates D5 — the `queryTokens` and `queryParseOps` halves are the pre-existing spine. ///// ⚠ MEASURED DEBT, NOT FIXED: `tokensHoldDirective` (`ConditionalCompilation.maxon:228`) RE-SCANS EVERY TOKEN OF EVERY FILE to answer a question the LEXER already knows for free, and it is exactly the cost class the memory columns cannot see — `phase:directives` allocations are FLAT per file while its CPU reads 3,390,864 / 6,436,030 / 12,617,014 / 25,826,151 / 50,942,407 / 100,523,097 = x1.90 x1.96 x2.05 x1.97 x1.97, dead linear and 0.41% OF TOTAL COMPILE CPU at rung 5 on a corpus containing no directive at all. ATTRIBUTED, not guessed: that CPU tracks SOURCE BYTES (x29.7 across the ladder) and not FILE COUNT (x6.2), so it is the token walk and not the per-file probe overhead. Removing it means threading a flag out of `tokenize` through `TokenMemo`, i.e. putting directive knowledge in the lexer's signature, for four tenths of one percent — a constant factor, so it is filed rather than chased. Re-measure trigger: `gendirectives.sh plain` (its directives phase is 100% this scan), or `phase:directives` CPU passing ~1% of a rung. ///// CORRECTNESS: full unfiltered suite 2424/0 exit 0 (no 101); `verify-warm-rebuild` PASS on a 36-file project WITH directives (determinism + cache + invalidation, including the new activeToken hit/miss assertions); and `specs-shv2/fragments/` COMPLETELY CLEAN after the full run — zero modified, zero untracked, so codegen is byte-identical for all 8,436 goldens. | 39,924,444 | 75,053,150 | 149,372,670 | 299,632,793 | 608,726,058 | 1,245,604,234 |
| 2026-07-29 | D1 (instance methods on `union`/`enum`) — ALLOCATIONS UNCHANGED, BYTES +1,040/+1,632/+2,816/... and the instrument attributes 100% of it. Allocs delta is 0 at EVERY rung, so the rung adds no allocation site. The bytes rise is one added field: `Parser.enclosingTypeIsEnum as bool` (the KIND half of the fact `enclosingType` names). ///// ATTRIBUTION IS ARITHMETIC HERE, NOT A STORY. The whole delta lands in exactly the two phases that construct Parser records: rung 0 = `phase:signatures` +776 and `phase:parse` +264 = 1,040, the reported delta to the byte; rung 2 = +2,432 and +384 = 2,816, likewise exact. Nothing else moved. A wider record charged per file is linear in file count, which is what the ratios say: bytesDelta grows x1.57 then x1.73, tracking the corpus's own x1.85/x1.92 code growth rather than outpacing it. Share at rung 2: 2,816 of 149,378,947 bytes = 0.0019%. `phase:signatures`/`phase:parse` also each gained +2 FREES, flat in rung size — `enterTypeScope` repoints the three per-declaration scopes at their shared empties, dropping two references the old `openTypeParamScope` path held. ///// NO STEP-6b TRIGGER. No new pass, no new IR op, and total bytes per rung ratio is x1.88 then x1.99 — linear, nothing near 2.4. ///// ⚠⚠ AND THE COLUMN THAT WOULD MATTER MOST IS BLIND HERE, WHICH IS THE HONEST CAVEAT: the generated corpus declares NO enum/union methods at all, so the sweep's new method-signature scan and the new `dispatchMethodOnEnumReceiver` arm are never entered by any rung. Their cost is UNMEASURED, not zero — a Delta-0 from a ladder that cannot express the feature measures the instrument's blind spot, not the cost, and that is the corpus blind spot rather than the allocation-vs-CPU one. What IS measured is that a program with no enum methods pays only the wider record. Re-measure trigger: a corpus knob that emits enum/union methods, or the 5d harness re-extraction, whose 13 files include five union `displayReason`/`isPass` methods. | 39,930,670 | 75,059,391 | 149,378,947 | 299,634,890 | 608,719,961 | 1,245,598,485 |
| 2026-07-30 | D2 (the postfix/member walk) — +146/+306/+626/+1,266/+2,546/+5,106 allocations, and the instrument attributes 100% of it to ONE phase and ONE object. ///// MEASURED AS AN A/B IN THIS WORKTREE, both binaries rebuilt: base origin/main (c9b5b6239) vs head. The whole-module delta equals `phase:parse`'s delta TO THE UNIT at every rung — parse allocs 116,048->116,194 (+146), 211,130->211,436 (+306), 400,939->401,565 (+626), 779,626->780,892 (+1,266), 1,536,332->1,538,878 (+2,546), 3,048,581->3,053,687 (+5,106) — and every other phase reads exactly 0. Bytes likewise: parse +345,888 at rung 5 over 5,106 objects = 67.7 bytes each, which is a header plus seven 8-byte fields. ///// IT IS ONE `MethodReceiver` PER MEMBER-ACCESS SITE, at parse time. The rung collapses two dispatch chains (eight arms keyed on a `VarInfo`, four keyed on a value) into one switch, and the receiver record is what both doors hand it. The deltas are 146 x2.10 x2.05 x2.02 x2.01 x2.01 — converging on exactly x2 against a ladder that doubles, i.e. strictly LINEAR in the number of `.member` sites, forever. Share at rung 5: 5,106 of 14,863,821 = 0.034%. ///// THE COST MODEL IS THE ONE THIS PATH ALREADY HAD, not a new one: `requireStructBase` allocates a `StructBase` per struct base for the identical reason ("ONE ANSWER, BECAUSE IT IS ONE QUESTION"), and a member access that previously reached it already paid a record. What is new is that a String/Array/Set/conformer receiver — which never reached `requireStructBase` — now pays one too. IT IS AVOIDABLE AND I AM NAMING IT: flattening `MethodReceiver` into seven parameters on `dispatchMethodOnReceiver` removes 100% of this delta and costs the switch nothing, since the record is the calling convention and not the thesis. Left as a record deliberately (it is the shape `StructBase` already established here); handed to the optimizer with an exact target. ///// NO STEP-6b TRIGGER: no new pass, no new IR op, no new whole-program query. Total allocations per rung x1.79 x1.88 x1.93 x1.97 x1.98, bytes x1.88 x1.99 x2.01 x2.03 x2.05 — unchanged from base. ///// CPU is inside the noise band and is not read as a datapoint. ///// CORPUS BLIND SPOT, stated because the feature is exactly the kind the ladder cannot express: ScaleCorpus emits no string/array/call-result RECEIVER chains and no enum methods, so the newly-reachable arms (`"abc".byteLength()`, `Inner.make(9).get()`, `o.inner.get()`, `pick().score()`) are never entered by any rung. Their per-call cost is UNMEASURED, not zero. What IS measured is that the shared switch charges every EXISTING member access one record. | 39,936,076 | 75,076,190 | 149,418,526 | 299,724,281 | 608,908,810 | 1,245,969,514 |
| 2026-07-30 | D1d + D1c (keyword-as-a-NAME is not block structure; E3081 for an all-discard binding list) — NEUTRAL, and the interesting number is one this rung INTRODUCED AND THEN REMOVED. ///// ⚠ THE DELTAS AGAINST THE ROW ABOVE ARE NOT THIS RUNG'S AND CANNOT BE: that row is D1's, measured before D5 and P1.10 landed, so -82/-92/-112 allocs and -1,048/-1,101/-1,213 bytes mix THREE code deltas (D5's token-tier directives — note the new `phase:directives` at 33/38/48 allocs — P1.10's tuples plus its O(files x types) fix, and this rung). A log row is about a BINARY, not about a change. The attributable measurement is a SAME-PATH A/B against a parent-commit binary built in this checkout: allocations and bytes BYTE-IDENTICAL at all six rungs. ///// ⭐⭐ AND THAT A/B IS WHY THE RUNG IS NEUTRAL RATHER THAN WORSE. The first cut of the E3081 work spelled its comma separator once by collecting into an array — `joinCommaSeparated(array)` — which put a `StringArray.create()` in `checkEnumExhaustive`, a routine that runs on EVERY enum match and not only on a rejected one. Measured: **+20 allocations at rung 0 DOUBLING to +640 at rung 5, x2.00 every rung**. Replaced by `appendCommaSeparated(list, item)` — the separator is still written down once, and nothing is allocated on the success path. Independently confirmed here off the instrument: `phase:semanticCheck` reads 1,996 / 3,628 / 6,883, IDENTICAL to the D1 row above, so no per-match allocation survives. ⭐ **A DEDUP THAT IS CORRECT CAN STILL BE UNSCALABLE — "spell it once" is not free if the once is on a hot path.** ///// ⚠ A MEASUREMENT ARTEFACT WORTH KNOWING: the first re-run of that A/B read a flat -1 BYTE at every rung. Cause was the two A/B binaries' FILENAMES differing by one character — the exe path is a String the compile allocates — and equal-length names took it to 0. Cousin of the checkout-path-length effect already recorded in this log; it reaches the byte column through argv, not through the tree. ///// NO STEP-6b TRIGGER: no new pass, no new IR op, and per-rung totals are x1.88 then x1.99. | 39,929,622 | 75,058,290 | 149,377,734 | 299,637,705 | 608,730,666 | 1,245,608,234 |
| 2026-07-30 | D4 — a member-access refusal may not assert a type the compiler INVENTED (E3011 for an undeclared type name, at all three member-access doors). NEUTRAL, and neutral for a structural reason rather than a lucky one. ///// THE NEW WORK IS ON AN ALREADY-THROWING PATH AND CANNOT COST THE SUCCESS PATH ANYTHING. `undeclaredBaseTypeNameOf` asks `TypeResolution.denotedNamedType` — a name lookup plus a registry cascade — but it is only ever reached INSIDE a branch that has already decided to refuse the program. Per REFUSED member access, never per field read. The generated corpus contains no refused member access at all, so the expected delta is exactly zero and that is what the instrument reads: `phase:resolveTypes` and `phase:parse` are unmoved, and a same-path A/B against a parent-commit binary built in one checkout was BYTE-IDENTICAL in allocations and bytes at all six rungs. The two success-path hoists (binding `capturedTag` and `typeNameIdOf` once instead of twice, in `capturedStructBase` and `requireStructBase`) are reads of an already-materialized value, so they are allocation-neutral too. ///// WARNING, AND IT IS THE SECOND ROW RUNNING TO CARRY IT: the deltas printed against the previous row are NOT attributable to any code change. Measured from a worktree whose path is 3 characters longer than the main checkout's, the ladder read -46/+114/+434/+1,074/+2,354/+4,914 allocs — a delta that DOUBLES every rung because the corpus passes a path per file and the file count doubles. It is the checkout-path-length artefact the previous row already records, and the row above that one records its argv cousin (two A/B binaries whose FILENAMES differed by one character read a flat -1 byte at every rung). This row is measured from the main checkout after the merge, so its own deltas are against a differently-pathed predecessor and should be read the same way. THE ATTRIBUTABLE MEASUREMENT IS ALWAYS THE SAME-PATH A/B, never the cross-row delta. ///// ⭐ AND THE ARTEFACT REACHES THE ALLOCATION COLUMN, NOT ONLY THE BYTE COLUMN THIS LOG HAD RECORDED IT FOR — measured here by accident, worth knowing on purpose: the SAME commit, built and measured in two checkouts differing in nothing but path length, reads a CONSTANT **+14 allocations in `phase:load` at every one of the six rungs** (587,825→587,839 … 14,863,821→14,863,835) together with ~4.5 KB FEWER bytes in the shorter path. CONSTANT rather than doubling, because `load` opens a fixed set of files per compile while the per-file path cost doubles with the corpus — so the two artefacts have DIFFERENT SHAPES and a flat offset in a memory column can be the CHECKOUT rather than the code. Allocations are still bit-for-bit reproducible; they are reproducible FOR A GIVEN PATH. ///// NO STEP-6b TRIGGER: no new pass, no new IR op, no new collection the compiler indexes by. Per-rung totals x1.79 x1.88 x1.93 x1.97 x1.98 — at or below x2.00 every rung, nothing near the 2.4 that would spend an optimizer agent. | 39,931,586 | 75,071,070 | 149,412,146 | 299,715,381 | 608,894,870 | 1,245,945,494 |
| 2026-07-30 | D1e — pin the UNION rvalue receiver (3 spec cases; postfix-member-walk.md). NEUTRAL, and this row is the strongest neutrality claim the log can carry: THE COMPILER SOURCE DIFF IS EMPTY. `git diff` over the branch is one spec file plus 6 new goldens; `maxon-shv2/Compiler/Parser.maxon` was verified BYTE-IDENTICAL to main after the sabotage used to prove the new cases load-bearing was reverted. No pass, no IR op, no collection the compiler indexes by — and no code at all. ///// ⭐ SO THIS ROW IS A NEGATIVE CONTROL FOR THE CHECKOUT-PATH-LENGTH ARTEFACT THE PREVIOUS TWO ROWS DOCUMENT, and it is a better one than either, because those had real code changes to argue about and this one has none. Measured from the worktree C:\\Users\\Eric\\dev\\maxon-D1e, the ladder reads a CONSTANT -14 allocations at every one of the six rungs (587,825 / 1,049,930 / 1,972,912 / 3,815,919 / 7,499,814 / 14,863,821) with bytes +6,263...+32,303 DOUBLING per rung. Two different shapes in one reading, exactly as the D4 row predicts: the FLAT -14 is `phase:load`'s fixed per-compile file set, the DOUBLING byte delta is the per-file path cost riding a corpus that doubles. Since no compiler code changed, BOTH are the checkout and NEITHER is a cost — which is the cleanest confirmation available that a flat offset in an allocation column can be the path rather than the code. ///// THE CROSS-ROW DELTA REMAINS UNATTRIBUTABLE; the attributable measurement is always the same-path A/B, and here it is trivially byte-identical because the sources are. ///// NO STEP-6b TRIGGER: per-rung totals x1.79 x1.88 x1.93 x1.97 x1.98, at or below x2.00 every rung and nothing near the 2.4 that would spend an optimizer agent. | 39,937,849 | 75,078,173 | 149,420,929 | 299,727,524 | 608,913,733 | 1,245,977,797 |
| 2026-07-30 | R4.1 — the file-IO runtime's OPEN slice: `__ManagedFile` + `__ManagedMemory` + `__ManagedFileError` + the `osFileOpenRead` StdOp + `__mf_open_read`. Host-only by the 2026-07-30 ruling; every other target refuses at the call site with the EXISTING E3104. ///// ALLOCATIONS: +222 / +267 / +352 / +517 / +842 / +1,487 across the six rungs. LINEAR AND FULLY ATTRIBUTED — it fits ~181 flat + ~41 per doubling-unit to within 2% at every rung (rung 3 predicted 507 vs 517 actual, rung 4 predicted 834 vs 842). The flat term is `createEnumRegistry` seeding a SECOND builtin enum (`__ManagedFileError`) per registry construction; the per-file term is that seeding riding a file count that doubles. `phase:signatures` carries it (+70 at rung 0, +395 at rung 5) exactly as that attribution predicts. +1,487 is +0.01% of rung 5's 14.87M. ///// NO SUPERLINEARITY. Per-phase ratios are at or about x2.00 across the board; `regalloc:splitting` x2.14 and `liveness` x2.08 are the pre-existing, already-documented bends and are untouched by this rung — nothing here enters regalloc, because the new op is a system call with one def and one use. ///// ⚠ THE CPU COLUMN IS NOT USED TO CLAIM ANYTHING HERE, and that is deliberate rather than an omission: the previous row was measured from a DIFFERENT checkout path on a DIFFERENT binary, and this log's own standing warning is that the attributable measurement is always the same-path A/B and never the cross-row delta. A cross-row CPU reading on `phase:signatures` looked like a double-digit regression; it is not attributable and is not claimed. ///// ⭐ STEP-6b RAN (this rung adds an IR op, which is the trigger) AND FOUND ONE REAL CONSTANT-FACTOR COST, WHICH IS FIXED RATHER THAN FILED: the declaration sweep took a THIRD complete walk of every file's token array, because each compiler-owned layout probe (`StringIndex`, `CharacterSet`, and now `__ManagedFile`) had its own `fileNamesAnyToken` pass. Each probe genuinely needs a COMPLETE pass — the reason is a measured crash, not caution, since `recordScannedType` swallows method bodies — but completeness constrains what a walk VISITS, not how many walks there are. Fused into one `fileNamesProducerMask` that asks all three per token and exits once all three are satisfied: worst case one full scan where it was three, best case unchanged. Suite 2564/0 and fragments unchanged across the fusion, so it is behaviour-identical. | 39,947,861 | 75,091,225 | 149,440,061 | 299,758,816 | 608,969,345 | 1,246,078,977 |
| 2026-07-30 | D1b — RETAIN a managed payload bound out of a BORROWED union (+ refuse `and fallthrough` INTO a payload-binding arm). NEUTRAL, and this row carries the ATTRIBUTABLE measurement rather than the cross-row one. ///// ⭐ SAME-PATH A/B, BYTE-IDENTICAL AT ALL SIX RUNGS — measured, not asserted: `git checkout main -- Compiler/Parser.maxon`, rebuild, ladder; restore, rebuild, ladder. Same worktree, same path, same host, only the compiler source differing. Both read 588,251 / 1,057,073 / 1,993,397 / 3,863,167 / 7,607,234 / 15,087,230. Not one allocation moved. ///// ⚠⚠ THE DELTA COLUMN OPPOSITE IS A CROSS-PLATFORM ARTEFACT AND IS NOT A COST — READ NOTHING INTO IT. ⚠ NOTE ADDED AT REBASE: the row directly above this one is R4.1's, but this row's delta column was computed against D1e's row (now two above), because R4.1 landed while D1b was in flight. The delta is a property of the two rows COMPARED AT RUN TIME, not of the rows' final adjacency. That D1e row was measured on WINDOWS from C:\Users\Eric\dev\maxon-D1e; this row is arm64-macOS — a different OS, a different arch and a different checkout path, so the +426 to +223,409 allocs and -2.2M to -75.9M bytes are three artefacts at once. The proof it is not the rung is free and sits in this very run: THE PRE-RUNG COMPILER PRINTS THE IDENTICAL DELTA COLUMN, because the delta is a property of the two ROWS, not of the two BINARIES. That is the D1e row's own checkout-path finding reproduced across a platform boundary, one step stronger. ///// ⚠ AND THE LADDER STRUCTURALLY CANNOT SEE THIS RUNG: the corpus emits no `match`, so `declarePayloadBindings` — the one function the rung changed — is never reached at any rung. A Δ0 here is THE INSTRUMENT'S BLIND SPOT (the CORPUS blindness, which the CPU column cannot cure either), not evidence the mechanism is free. Its real cost is one `__mm_incref` per managed payload bound out of a borrowed union, bounded and unconditional by construction, verified in the emitted arm64 rather than argued: the payload load, then `bl __mm_incref` as the very next op, then exactly one `bl __str_decref` at the arm exit. ///// NO STEP-6b TRIGGER: per-rung totals x1.79 x1.88 x1.93 x1.96 x1.98, at or below x2.00 every rung and nothing near the 2.4 that would spend an optimizer agent — and a byte-identical A/B leaves one nothing to hunt. | 37,723,357 | 70,470,039 | 139,898,150 | 283,725,286 | 576,088,592 | 1,170,055,023 |
| 2026-07-30 | W1 — a function TYPEALIAS dropped `ExitCode`'s WIDTH, so an ExitCode-returning function VALUE trapped on wasm (`indirect call type mismatch`). One rule, three doors, now one. NEUTRAL. ///// ⭐ SAME-PATH A/B, BYTE-IDENTICAL AT ALL SIX RUNGS — measured by the coordinator, not inherited: revert BOTH changed compiler files to main, rebuild, ladder; restore, rebuild, ladder. Same worktree, same path, same host. Both read 588,336 / 1,057,178 / 1,993,542 / 3,863,392 / 7,607,619 / 15,087,935. Not one allocation moved, which is the expected shape — the fix replaces a tag with a wider tag on one already-taken branch and adds no allocation anywhere. ///// ⚠⚠ AND THE DELTA COLUMN OPPOSITE IS AGAIN NOT A COST, BUT ITS STORY IS DIFFERENT FROM D1b's AND WORTH READING. It shows +85 / +105 / +145 / +225 / +385 / +705 allocs, which GROWS with the rung (≈4 allocs and 54 bytes per added corpus file) — so the flat-offset checkout-path story the D1e and D1b rows document would NOT have covered it, and it must not be waved away with that argument. The pre-rung binary at this same path prints the IDENTICAL totals AND the identical delta column, so the growth is a property of the two log ROWS (a cross-checkout, cross-host per-file path cost riding a corpus that doubles), not of the two BINARIES. Third consecutive row where the attributable measurement is the same-path A/B and the cross-row delta is an artefact. ///// ⚠ CORPUS REACHABILITY, so a Δ0 is not misread as "free": the ladder emits NO function typealias, so the fixed door is never taken at any rung. It DOES emit 4 interfaces per rung, all returning a user ranged alias — those now route through the new shared `builtinTypeNameTag` and reach the identical `named` answer with identical allocations, which is what the byte-identical A/B also demonstrates. ///// NO STEP-6b TRIGGER: per-rung allocation totals x1.79 x1.88 x1.93 x1.96 x1.98, at or below x2.00 every rung and nothing near the 2.4 that would spend an optimizer agent; CPU x1.85 x1.92 x1.97 x2.01 x2.02, linear. A byte-identical A/B leaves an optimizer nothing to hunt. | 37,725,819 | 70,472,771 | 139,901,422 | 283,729,638 | 576,095,104 | 1,170,065,855 |
| 2026-07-30 | R4.2 — the rest of the file IO: `read`/`write`/`size`/`exists`/`delete`/`rename`/`stat`/`statField`/`statFree` + `openWrite`/`openWriteExecutable`, the 7 new StdOps with their 27 classification sites each, the RAII destructor, and the three `__ManagedMemory` members. Host-only, E3104 on every other target, per the R4.1 ruling. ///// ALLOCATIONS vs R4.1: +892 / +915 / +891 / +891 / +891 / +891 — FLAT, and the flatness IS the finding: the delta does not move by ONE allocation while the file count goes x8 across rungs 2-5, which refutes a per-file term outright. It is a per-COMPILE constant. `createEnumRegistry` seeds 16 more `__ManagedFileError` cases plus a whole 8-case `__ManagedMemoryError`, and it runs once per `EnumLayoutMap` CONSTRUCTION — three per compile (Queries.maxon:190, QueryDatabase.maxon:80, Project.maxon:2330) — never once per file. +891 is +0.006% of rung 5's 14.87M. ///// STEP-6b FOUND AND FIXED THE RUNG'S ENTIRE *GROWING* ALLOCATION TERM, AND IT WAS NOT WHERE THE PRE-OPTIMIZER READING PUT IT. Before the fix the delta read +2,201 / +3,544 / +6,160 / +11,440 / +22,000 / +43,120 — a term that DOUBLES per rung — and it had been attributed to the enum seeding riding a doubling file count. It was not that: the seeding is not per-file (above), and `phase:signatures` allocations are IDENTICAL before and after the fix at every rung. The growing term was `Parser.dispatchArrayMethod`, into whose TOP R4.2 hoisted `signatures.internArrayByteInstance()` so it ran for EVERY array method call in the program instead of only inside the `.managed` field arm — and each call built a fresh one-element `MaxonTypeArray` to key the instance registry with, i.e. A HEAP ALLOCATION PER ARRAY METHOD CALL. Memoized on `ProgramSignatures` (`arrayByteInstance` + a `computed` flag beside it, `TokenMemo`'s shape, because 0 is a real `GenericInstanceId`): the answer cannot change, since `GenericInstanceRegistry` only ever grows and interning is idempotent, and the FIRST ask is still the WRITE, so the registry contents and every id derived from them are exactly what they were. Removed -1,309 / -2,629 / -5,269 / -10,549 / -21,109 / -42,229 allocations — x32.3 across a x32 ladder — and ALL of it lands in `phase:parse` to the digit (rung 5: 3,096,945 -> 3,054,716 = -42,229, which is the whole-compile delta exactly). The rung's cost is now flat where it grew. ///// ONE MORE, CPU-ONLY AND SAID TO BE SO: `Parser.isManagedFileType` asked "is this a `__ManagedFile`?" by HASHING the name and probing the interner — INSERTING on a miss — on every method dispatch in the program, where its two siblings `isStringIndexType`/`isCharacterSetType` read the name behind the id (a dense-column index plus a byte compare) and mutate nothing. R4.2 widened its tag gate from `structRef` alone to `structRef` OR `named`, putting that probe on the enum and ranged-alias receivers too, one arm ABOVE the enum test whose own comment explains that it gates on the tag first precisely so the interner is not touched for a receiver that is not its business. It now uses the siblings' form. ITS ALLOCATION DELTA MEASURED EXACTLY ZERO at all six rungs — the probe was always a HIT in this corpus, so the exact column cannot see it and NO speedup is claimed; what is claimed is that a predicate no longer mutates the interner as a side effect. ///// NO SUPERLINEARITY. Per-phase ratios are at or about x2.00; `regalloc:splitting` and `liveness` are the pre-existing documented bends and are untouched — nothing this rung adds enters regalloc in this corpus, which contains no file IO at all. Read the manifest before reading anything into that: a zero from a ladder that cannot express the feature is not evidence about the feature. ///// NO CPU CLAIM ACROSS ROWS, deliberately: this tree's checkout path differs from the previous row's, and this log's standing rule is that the attributable measurement is the same-path A/B and never the cross-row delta. ///// createEnumRegistry RULING — NOT made demand-driven, and NOT filed as debt either. It is a flat per-compile 891 allocations (0.006%), measured above; the same suggestion was already REFUSED with a measurement at P1.7a slice 2b-ii, where an eager per-`Parser` layout would have turned a flat +68 into +1,430 at 143 files; and R4.1's reason stands — it is the one door reaching BOTH enum registries, and a handler resolves its `match e` through `Project.enumTypes`, so demand-driving it re-opens the two-registries-disagree wrong-answer class to buy nothing measurable. ///// Suite 2574/0, exit 0, no leak, and `specs-shv2/fragments/` empty-diff across BOTH changes, so codegen is byte-identical. | 40,103,005 | 75,144,283 | 149,482,823 | 299,802,419 | 609,014,628 | 1,246,127,623 |
<!-- scale-history:bytes -->

## CPU

**CPU ticks consumed by the compiling thread, per rung.** Added 2026-07-25. This is the only column
here that can see a cost which **allocates nothing** — the class of defect the two tables above are
structurally blind to, and the class that every time-only quadratic in `maxon-shv2/PLAN.md` belongs
to (`requireInterfaceForParse` was measured with allocations *identical to the digit*, 1,417,523 both
ways, against a +24.15 ms parse delta).

**It is not wall time, and the distinction is the whole reason it can be here.** Wall time counts
every other process on the box; this counts only the thread doing the compiling, so preemption is
invisible to it. A run whose parse phase reads ×5.03 then ×1.78 on a doubling ladder — which happened
— was measuring the machine.

> ### ⚠ HOW TO READ THIS TABLE, AND IT IS NOT LIKE THE OTHER TWO
>
> **Allocations and bytes are exact: any movement is real. This column has a noise band, and a
> movement inside it is NOT a datapoint.**
>
> **The band, as measured rather than assumed** (three idle runs of `--rungs=3 --repeat=3`, plus one
> against a machine loaded to every core):
>
> | | reading |
> | --- | --- |
> | phases > 50M ticks, idle | **median 1.7%, worst 6.3%** |
> | small phases (~100K ticks), idle | up to **31%** — never read a ratio off one |
> | per-rung total, idle | 6.7% / 2.5% / 0.3% (larger rungs are quieter) |
> | per-rung total, **fully loaded box** | **+49% … +52%** |
> | rung-over-rung RATIO, loaded vs idle | **×1.90/×1.98 vs ×1.86/×2.00** — essentially unchanged |
>
> ⚠ **So "it is immune to a busy machine" is FALSE, and the true statement is better.** This counter
> ignores *preemption*, not *contention*: other cores fighting for cache and SMT siblings make the
> same work take half again as many ticks. What survives is the **ratio**, because the inflation is
> uniform — which is exactly the number the doubling ladder is read for. Trend ratios; treat absolutes
> as a fact about one machine on one day.
>
> Against a claimed few-percent constant-factor win this column is worth nothing — use the allocation
> columns, which are exact. **Memory was bit-identical across all four runs, including the loaded
> one**, which is both the reason those columns are trusted and a live check that the compiler is
> deterministic (`--repeat` asserts it on every run).
>
> **The unit is platform-defined** — TSC ticks on Windows, nanoseconds on POSIX — and there is no
> honest conversion between them (`QueryPerformanceFrequency` is the performance counter's rate, not
> the TSC's). **So compare RATIOS between rungs, which are unit-free, and absolutes only against rows
> measured on the same platform.**

| date | change | rung 0 | rung 1 | rung 2 | rung 3 | rung 4 | rung 5 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 2026-07-26 | THE CPU-TIME COLUMN LANDS, and this row is its first datapoint: there is no previous CPU row to difference against, so the ## CPU table starts here. scale-test measured MEMORY ONLY and declared that blind spot on every run; a cost that re-walks tokens or chases pointers but allocates little read FLAT however quadratic it was. Mechanism: one bootstrap intrinsic __Builtins.threadCpuTicks() (QueryThreadCycleTime on Windows, clock_gettime(CLOCK_THREAD_CPUTIME_ID) on POSIX) bracketed by PhaseProbe INNERMOST, displacing the wall clock, because the tightest bracket belongs to the number people act on. WALL time is still never collected and that argument is unchanged: it counts every OTHER process, and one run once read phase:parse at x5.03 then x1.78 across a DOUBLING ladder, which is preemption and not a curve. CPU time cannot see preemption at all (verified: across a 300 ms sleep it advanced 837,520 ticks while wall advanced 301,000,000 ns). NOISE FLOOR, MEASURED not assumed, over three idle runs plus one against a machine loaded to every core: phases above 50M ticks read median 1.7% / worst 6.3%; small phases (~100K ticks) swing up to 31% and no ratio should be read off one; a FULLY LOADED box inflates every rung by +49%..+52% because this counter ignores PREEMPTION but not CONTENTION - yet it inflates them UNIFORMLY, so the rung-over-rung RATIO survived nearly intact (loaded x1.90/x1.98 against idle x1.86/x2.00). Trend ratios; treat absolutes as a fact about one machine on one day. --repeat=<n> (default 3) compiles each rung n times and keeps the MINIMUM CPU per phase, since noise can only ADD; the repeats also CROSS-CHECK that allocs/frees/bytes are IDENTICAL every time, a compiler-determinism gate the one-shot runner structurally could not make, and a difference is a BROKEN RUN rather than a number to average away. WHY THIS ROW MOVED: the previous row is the parameter-mutation optimization pass, logged at essentially origin's tip (its gates read 1769/0 against this row's 1774/0, a handful of tests apart), so these deltas are THIS change rather than a sum over an unlogged gap. (An earlier draft of this note claimed it spanned 23 unlogged upstream commits; that was wrong - upstream logged its row.) The shape is the load-bearing part: the instrument now costs a FLAT +200 allocations at every rung (it was +546/+716/+1056/+1736, GROWING, until regAllocAccumulated stopped routing through costOf). That mattered because returning a struct BY VALUE ALLOCATES - measured at exactly one allocation per return and INDEPENDENT of field count, 4/5/6 fields all costing 10,000 over 10,000 returns - and that function runs once per FUNCTION, so the instrument was moving the very column the log calls bit-for-bit reproducible. A constant offset cannot bend a curve; a growing one can. ALSO FOUND, unfixed: Array.resize is BROKEN for STRUCT element types, setting the length without giving the slots storage, so count() answers N while get(0) reports OOB - ScaleTestRunner.parseRungMetrics has carried that call for its whole life and survives only because it writes every slot before reading one. Gates: C# 3103/0, shv2 1774/0, no fragment churn, no exit 101. | 840,398,880 | 1,424,135,120 | 2,796,876,760 | 5,766,189,540 | 12,774,339,540 | 30,073,496,780 |
| 2026-07-26 | TWO TIME-ONLY SUPERLINEARITIES FOUND BY THE NEW CPU COLUMN AND FIXED, on its first ladder. Both allocate nothing, so every row above this one is blind to them: this is the column earning its place. (1) THE ENCODER MAP WAS QUADRATIC IN BLOCKS PER FUNCTION. X64Backend.emitFunctionChunk keyed a per-function block-offset Map by block id; Map is open-addressed with linear probing and int.hash() is the IDENTITY (stdlib/PrimitiveExtensions.maxon), so a key lands at id and (capacity-1). A function block ids are TWO CONTIGUOUS RUNS - its original blocks, then the blocks regalloc:criticalEdges appended - and layout order INTERLEAVES them (measured on a real 3,073-block function: 0 1 2049 2 3 2050 4 5 2051 ...). The high run WRAPS onto the low run slots, the two become one solid cluster, and probes per insert measured EXACTLY capacity/4 + 2/3 - collapsing to 1.00 the instant capacity first exceeds the id range. Capacity is proportional to blocks, so probes per insert is proportional to blocks: O(B^2) per function, 171.44 probes/insert against 1.00 for the same keys sorted. The key IS the index, so the Map is now two pre-sized arrays: O(B). Corpus phase:encode CPU ratios x1.61 x1.93 x2.22 x3.69 x2.58 -> x1.68 x1.90 x1.99 x2.03 x2.04, compounding 65.8 -> 26.3 over five doublings where 32 is linear - superlinear to SUB-linear. Rung 5 encode 1,744,427,240 -> 503,060,720 ticks; encode falls from 4.62% to 1.65% of the whole compile. MEMORY IS NEGATIVE, which is why the columns below FALL: -23,848 allocations and -10,666,765 bytes at rung 5 (-32% of encode, -33% at every rung), and the whole-compile deltas equal those two numbers TO THE DIGIT, so nothing outside encode moved. Codegen byte-identical, proven not inferred: 6 programs on x64-windows plus 4 programs on arm64-macos, arm64-linux and x64-linux, all hashed identical before and after. Ladder committed: Testing/ladders/genemit.sh, three independent knobs; ladders A (function count) and B (ops per function) were ALREADY FLAT at x2.00, and that is what located the term. (2) THE BLOCK-EXTENT SCAN NOW STEPS OVER A NESTED CONSTRUCT. Parser.blockStatementEndIndex walked every token in a construct extent to find its closing end, once per if/while/match, so a nested construct was re-walked by every enclosing one: O(depth x tokens), allocation-free. A lazily-built per-file extent column answers where the block opened here closes in one array read. Ladder committed: Testing/ladders/gennest.sh, which holds depth x statements FIXED so program size barely moves while nesting doubles - the only way to tell a depth term from a size term. Measured at constant size, 1024 statements in every program: parse CPU 23.6M / 28.2M / 37.4M / 56.3M / 98.2M at depth 4..64 (x1.20 x1.33 x1.50 x1.74, climbing toward x2.00) with allocations only x1.49 over a 16x depth change. After: the d4->d64 slope falls +68.8M -> +34.6M ticks, HALF the depth term. It costs +2 allocations per FILE and +8 bytes per TOKEN for the column (+0.55% of parse bytes at rung 5) - a linear, explainable step, and the reason the byte column does not fall as far as the allocation column. It also gave the block-CLOSER classification ONE home; it had been written out independently at eight sites. ⚠ TWO CORRECTIONS TO THE RECORD, both measured. FIRST: phase:parse superlinearity ON THIS CORPUS IS NOT NESTING DEPTH. Halving the depth term left the corpus parse ratios unchanged (x1.91 x2.14 x2.39 x2.75 x3.03 against a baseline x1.94 x2.16 x2.43 x2.74 x2.99). The depth term is real and gennest.sh proves it at constant size, but this corpus barely exercises it; the corpus parse curve is Theta(mutable bindings x constructs) - d_blocks.maxon is ONE function with 518 mutable vars in scope, and both callers ask a membership question once per binding per construct: 414,400 tests at rung 2 and 26.5 MILLION at rung 5. That is FILED, not fixed: the cure is carried sets computed bottom-up over the construct tree, which changes phi ORDERING that the goldens pin, so it is a rung and not an edit. SECOND: the first attempt at the parse fix was a per-file name->assignment-sites index with binary search. It was implemented exactly, gated GREEN, and its ladder went flat - and then measured x3.45 on the real corpus at rung 5, because it replaced a small per-construct set (one hash and one probe per test) with a structure whose per-test cost multiplied that same Theta(V x C). It was BACKED OUT. A ladder-flat, corpus-worse change is a regression, and only running both caught it. ⚠ THE MAP TRAP IS A CLASS, NOT AN INSTANCE: identity hash plus linear probing is quadratic for ANY interleaved union of contiguous int runs. SemanticCheck.buildBlockByIdMap is the identical pattern and is cold only because Maxon-tier ids have no split run - one inliner away from hot. A hash mixer in stdlib/Map.maxon would kill the whole class in three lines and was deliberately NOT done here: it changes every Map iteration order, so any pass emitting in Map order could move bytes, risking the C# 3103 suite and every golden. Gates: C# 3103/0, shv2 1774/0 at --workers=1 and =12 byte-identical, zero fragment churn, zero build warnings, no exit 101. | 849,344,840 | 1,424,322,080 | 2,833,046,680 | 5,869,691,660 | 13,235,005,180 | 34,240,615,920 |
| 2026-07-26 | Array.resize refused for a managed element type (E3106) + Array.get distinguishes emptySlot from indexOutOfBounds. shv2's gate is a parse-time check on the resize arm; the bootstrap's is a pre-monomorphization SemanticCheckPass walk over MaxonCallOp. Neither runs on the scale corpus's own hot path, so this run is a NEUTRALITY check, not a target. | 783,849,560 | 1,474,059,140 | 2,812,391,020 | 5,813,260,520 | 12,560,580,800 | 31,414,745,160 |
| 2026-07-26 | THE P1.5-FINISH OPTIMIZATION PASS, and the headline is NOT this rung's code — it is a QUADRATIC IN `Map` ITSELF that the rung's own instrument row had already flagged as a curve nobody had explained. phase:encode goes x3.02 -> x1.98 (rung 5: 231,100,750 -> 81,443,584 CPU ticks, -65%) and phase:link x1.79 -> x2.00, with the ALLOCATION AND BYTE COLUMNS BIT-IDENTICAL AT EVERY RUNG (checked by diff, not by eye) - because the fix changes only which SLOT a key probes, never what is allocated. WHAT IT WAS: `Map`/`Set` are power-of-two open-addressed tables with LINEAR probing, and `int.hash()` is the IDENTITY (`self and 0xFFFFFFFF`), so the slot is the key's low bits and structure in the keys became structure in the table. The victim was the per-function block-offset table in BOTH backends (`Arm64BlockOffsetMap` / `ChunkLabelOffsetMap`), keyed by BlockId. Those ids are PERFECTLY DENSE - a run 0..N-1, verified, gap histogram is a single entry (1, 6144) - but they arrive in LAYOUT order, which interleaves the parser's blocks with the ones minted later (critical-edge splits): 0, 1, 4097, 2, 3, 4098, ... At every capacity below the largest id the two runs alias onto the same slots, interleaved insertion grows ONE primary cluster, and each doubling rehashes and rebuilds it. Isolated in a bootstrap-compiled microbenchmark with the SAME key set, count and density and only the ORDER changed: 24,576 ids cost 346.6 ms in arrival order against 2.3 ms in increasing order (x3.8 x3.9 x3.7 x4.0 per doubling - a quadratic, 150x at that size). FIX: `spreadHash` in stdlib/Interfaces.maxon (MurmurHash3 fmix32) applied at the SIX places Map and Set turn a hash into a slot index - one door, so the two containers cannot come to disagree. It deliberately does NOT change `hash()` itself and is NOT what the `hashes` arrays cache; those still hold the key's true hash for the skip-the-equals guard. HOW IT WAS FOUND, which is the part worth copying: the memory columns read x1.96 for encode the whole time and could never have shown this. The CPU column showed the curve; a hand ladder (maxon-shv2/Testing/ladders/genblocks.sh, committed) separated blocks-per-FUNCTION from functions-per-module - encode read x3.21 x3.37 x3.69 on `blocks` against x2.0 flat on BOTH controls (`funcs`, `straight`) - and a bisect by disabling one map operation at a time pinned it to the upsert rather than the patching or the lookups. THIS RUNG'S OWN COST, A/B'd on this host base-vs-head, is a FLAT +1 allocation / +16 bytes at every rung in phase:parse and nothing else anywhere: the retagged-cast mint (`emitRetaggedCastValue`) firing at the single `as ScaleCoverNarrow` site in the fixed-size h_cover.maxon. CORRECTING THE RECORD: that flatness is the CORPUS, not the cost. Measured on probes of 1/10/100 cast sites, `castChangesTheShiftFill`'s extra `signatures.aliasOf` costs +1 allocation PER `as <declared-alias>` SITE (+11 at 10 sites, +101 at 100), and a MINTING cast costs ~+8.9 parse and ~+28 total allocations per site. It reads flat here only because the whole six-rung corpus contains exactly ONE named-alias cast and it sits in a file that does not double. CLOSURES ARE INVISIBLE TO THIS LADDER ENTIRELY - ScaleCorpus lists them under NOT GENERATED - so the capture half of the rung was measured on a second committed ladder (genclosure.sh): the path is LINEAR in program size (x1.98 x1.99 x1.99 parse allocations over 128..1024 closures), `fieldStorageType` is O(1) per capture READ SITE, and `captureSlotFor`'s O(reads x captures) scan is linear-in-practice - the language caps a function at 64 argument slots (E2015), so the scan is bounded at ~2,048 comparisons per closure and K=32 -> K=64 reads x1.41. Re-measure it if a closure can ever capture hundreds of distinct LOCAL bindings. STILL OPEN, unchanged by this row: phase:parse remains x3.26, the loop-phi token pre-scan (Parser.loopBodyEndIndex + namesAssignedIn, O(depth x tokens)) that commit 6ee12ff37 attributed - it is the corpus's own documented prediction and is NOT this pass's. Gates: shv2 1593/0 arm64-macos and 1588/0 wasm32-wasi, --workers=1 stdout byte-identical to --workers=12 AND to the pre-change baseline (sha 0d4993c64e), specs-shv2/fragments EMPTY after a full run (byte-identical codegen, which is what proves a hash change moved no emitted IR), C# bootstrap 3102/0 with its arm64 fragment churn proven pre-existing (byte-identical diff with and without the change), no exit 101. | 116,846,834 | 216,579,917 | 428,098,334 | 884,103,708 | 1,924,480,250 | 4,506,964,750 |
| 2026-07-26 | REBASE RE-MEASUREMENT of the P1.5-finish optimization pass. The row above it was measured against a base that PREDATES upstream a78d17c35, and its numbers are therefore stale — this row is what is true with both fixes present. Post-rebase the curves hold: phase:encode x1.99, phase:link x1.98, every phase ~x2.00. The allocation deltas against that row are NEGATIVE at every rung (-1,472 -> -18,361 allocations, -163,098 -> -4,188,382 bytes) and that is upstream's work, not a regression: a78d17c35 replaced the x64 per-function block-offset Map with two pre-sized arrays, so those allocations no longer happen at all. THE TWO FIXES ARE COMPLEMENTARY AND NEITHER IS REDUNDANT, which is the point worth recording. Upstream fixed the INSTANCE it could see from a Windows host (X64Backend.emitFunctionChunk; 'the key IS the index', so the Map became arrays) and its own row states in as many words that it DELIBERATELY DECLINED the class fix: 'A hash mixer in stdlib/Map.maxon would kill the whole class in three lines and was deliberately NOT done here: it changes every Map iteration order, so any pass emitting in Map order could move bytes, risking the C# 3103 suite and every golden.' This branch took that class fix (spreadHash, MurmurHash3 fmix32, at the six slot derivations in Map/Set) and MEASURED the risk it was declined on, rather than reasoning about it. The risk is zero, empirically: specs-shv2/fragments EMPTY after a full run on two targets, the C# bootstrap's fragment diff BYTE-IDENTICAL with and without the change (sha aef478e2b6, A/B'd against an unmodified checkout), wasm 0 failures, verify-warm-rebuild PASS, and an independent sweep of all 16 Map/Set iteration sites in the compiler found exactly one user-visible consequence — the ORDER of diagnostics for a program with several undeclared field types, which was already map order and not source order, and which TypeResolution.maxon:133 documents as such. ⚠ AND ON THIS HOST THE MIXER IS THE ONE DOING THE WORK: scale-test's compileRung spawns build with no --target, so on arm64-macos it is Arm64BlockOffsetMap that carries the block-offset table, and upstream's array fix is x64-only. An x64 host would have measured the reverse. Neither fix subsumes the other; the class fix additionally covers SemanticCheck.buildBlockByIdMap, which upstream's own row flags as 'the identical pattern and cold only because Maxon-tier ids have no split run - one inliner away from hot'. Gates on the rebased tree: shv2 1591/0 arm64-macos, 1586/0 wasm32-wasi, --workers=1 byte-identical to --workers=12, specs-shv2/fragments clean after a full run, C# bootstrap 3104/0, no exit 101. | 116,305,416 | 217,397,792 | 428,937,125 | 885,603,250 | 1,927,560,167 | 4,498,716,167 |
| 2026-07-27 | OPTIMIZATION PASS over the P1.7 instance-name-space rung. NO COMPILER CODE WAS CHANGED, and that is the result. WHAT MOVED AND WHY: -15 -27 -51 -99 -195 -387 allocations and -240 -432 -816 -1584 -3120 -6192 bytes, i.e. EXACTLY -1 allocation and -16 bytes PER INTERNED GENERIC INSTANCE. This corpus's instance count is 3 + 12x2^rung (3 fixed Array instances plus a doubling generics knob) and the deltas match it digit for digit; ratios x1.80 x1.89 x1.94 x1.97 x1.98 converge on x2.00, which is what 'linear in instance count' means. The allocation that stopped happening is the CompiledNameClaimant boxed union in ParseStaging.maxon, which checkTypeSymbolNamespace minted once per instance even for its payload-free 'unclaimed' case; the rung deleted the union and the arm chain with it, leaving a bare claimed.get(compiled). A/B'd base 42ea9a054 vs head IN THIS ONE WORKTREE on this Windows host, the two binaries named to equal length so phase:load's filename-length term cancels. Independently reproduced here, NOT carried over from the implementer's own numbers. THE DELTA COLUMN OF THIS ROW IS MEANINGLESS AND SHOULD NOT BE READ: the row above it was measured on an arm64-macos host. THE HUNT'S PRIMARY TARGET was reservedIfDeclared, a LOOP over a name space run once per generic instance inside the compiler's name mangler. It performs AT MOST TWO PROBES on any program that compiles, and a probe is two Map.contains (containsStruct / containsEnum, SignatureIndex.maxon:3509 and :4585 - hash lookups, so the derivation is NOT O(instances x declared types)). A THIRD probe requires a declaration whose own name starts with __, which is E2051, so it exists only on programs already refused. Measured on a new committed ladder, maxon-shv2/Testing/ladders/geninstances.sh, at 64..2048 instances: phase:signatures allocations are IDENTICAL base and head at every rung (3089 5396 9943 18970 36964 72882, x1.97) - the added probe allocates NOTHING on the uncontested path - and signatures CPU is +0.7%..+1.3%, flat with N and inside the noise band. THE CONTEST PATH costs +4 allocations and +198 bytes per contested instance, measured as contested minus a BYTE-IDENTICAL control (type Zed_A<i> in place of type Box_A<i>, so the extra declarations' own sweep cost cancels): 256 512 1024 2048 4096 8192 allocations across the 32x span, i.e. constant per instance, x1.98 both columns. DELIBERATELY NOT OPTIMIZED - it is a constant factor on a path bounded by the declared-type count, and constant factors are not the mandate; for the record it is one String interpolation and a direct ByteArray reserve+append would make it 1 allocation. THE ADVERSARY - the shape built to make the loop iterate k times, k declarations each two characters longer than the last - reads -1 0 +4 +14 +64 ms against its byte-identical control over 40 KB to 8.5 MB of source, about 7e-6 ms per source byte and FLAT. It is LINEAR EVEN THERE, and structurally so: probe j costs a declaration 2j characters long, so k probes require Theta(k^2) of SOURCE to buy Theta(k^2) of work. CORRECTION TO THE RECORD: this corpus DOES generate generics and interfaces (its own manifest lists both), so the standing instruction that it contains none is stale - and it is precisely what makes the per-instance delta above readable here at all. UNCHANGED AND NOT THIS RUNG'S: phase:parse reads x3.26 on base and head alike, the already-filed Theta(mutable bindings x constructs) debt. Gates on this tree: shv2 1785/0, specs-shv2/fragments clean, no exit 101. | 761,655,886 | 1,456,475,444 | 2,844,410,343 | 5,926,154,151 | 12,983,077,682 | 31,122,306,768 |
| 2026-07-27 | Map probe trap #123, the last two BlockId-keyed Maps: SemanticCheck.buildBlockByIdMap and StdToWasm.BlockDispatchMap are now ONE dense BlockKeyedTable (IrModule.blockIndexById), which BlockOffsetTable is also an instantiation of. THE LADDER SEES ONLY THE THIRD: its corpus has no async and always builds for the host, so the two converted sites read a true Delta 0 here — measured elsewhere (semanticCheck -1,536 allocs / -92% bytes on Testing/ladders/genawait.sh 24 400; wasm encode -6,864 allocs on the rung3 corpus). What DID move here is bytes:encode, -2,328 .. -42,008, exactly 131 + 160*2^k CodeOffset slots per rung: forBlockCount now reserve()s the value column (exact ask) where it used to growFilled() it (which floors at Array.MinimumCapacity = 4), so every chunk of under 4 blocks stops rounding its offset column up to 4. Allocation counts are unchanged in every phase; the CPU column moved only inside its noise band. | 116,674,042 | 217,475,542 | 430,563,792 | 888,140,458 | 1,936,296,625 | 4,527,877,708 |
| 2026-07-26 | Map probe trap #123, the last two BlockId-keyed Maps: SemanticCheck.buildBlockByIdMap and StdToWasm.BlockDispatchMap are now ONE dense BlockKeyedTable (IrModule.blockIndexById), which BlockOffsetTable is also an instantiation of. THE LADDER SEES ONLY THE THIRD: its corpus has no async and always builds for the host, so the two converted sites read a true Delta 0 here — measured elsewhere (semanticCheck -1,536 allocs / -92% bytes on Testing/ladders/genawait.sh 24 400; wasm encode -6,864 allocs on the rung3 corpus). What DID move here is bytes:encode, -2,328 .. -42,008, exactly 131 + 160*2^k CodeOffset slots per rung: forBlockCount now reserve()s the value column (exact ask) where it used to growFilled() it (which floors at Array.MinimumCapacity = 4), so every chunk of under 4 blocks stops rounding its offset column up to 4. Allocation counts are unchanged in every phase; the CPU column moved only inside its noise band. | 116,674,042 | 217,475,542 | 430,563,792 | 888,140,458 | 1,936,296,625 | 4,527,877,708 |
| 2026-07-27 | OPTIMIZATION PASS over the witness-label injectivity rung (f64e4d0a5). NO COMPILER CODE WAS CHANGED, and that is the result. A/B base 858d8dbef vs head f64e4d0a5, both REBUILT AND RE-RUN IN THIS WORKTREE: allocations and bytes are bit-identical at all six rungs (618,838 / 36,000,693 through 16,220,206 / 1,150,878,945), which confirms the implementer's claim to the digit. BUT THAT ZERO IS A CORPUS BLIND SPOT, NOT EVIDENCE: ScaleCorpus.maxon:554 states this ladder generates no 'where' clause anywhere, so it mints NO witness table and never calls witnessTableLabel or ensureWitnessTable at all. The rung was therefore measured on a purpose-built witness ladder instead (scratch, not committed). Shape A holds ONE (conformer, interface) pair and doubles the DISPATCH SITES 256..4096 - the label is minted per SITE, BEFORE the memo check at LowerMaxonToStd.maxon:912, so this is the per-call cost - and shape B doubles the PAIRS 16..256, i.e. the tables actually emitted. Head minus base is EXACTLY 0 allocations and 0 bytes at every rung of both, in phase:lowerMaxonToStd and in total. The separator costs nothing because underscore and dot are both one byte, so the label String is the same length and the same allocation; and the witness path is LINEAR in both dimensions, phase:lowerMaxonToStd allocations x1.97 x1.98 x1.99 x2.00 over sites and x1.93 x1.96 x1.98 x1.99 over pairs. THE ONE GENUINELY NEW RUN-TIME PATH is requireIdenticalPayload, a ByteArray.equals() on the already-registered branch of registerRdataConstantLabeled, whose cost would be O(payload) PER HIT. ITS HIT COUNT WAS MEASURED, NOT ARGUED: the body was temporarily replaced by an unconditional panic and the compiler rebuilt, so ANY re-registration aborts. The full x64-windows suite still passed 1793/0, as did 256 distinct witness tables in one program, 4096 witness reference sites in one program, and the whole 5-rung scale ladder. The branch is taken ZERO times - both callers memoize before they ask (ensureWitnessTable on the label, ensureLayoutDescriptor on the gid), exactly as its header claims. Instrumentation reverted; the committed binary is byte-identical to the one every A/B above ran with. THE +612 BYTES IN THIS ROW'S DELTA COLUMN IS NOT THIS RUNG'S AND SHOULD NOT BE READ. It appears identically on the BASE binary, and the decisive test is that checking out cdd6fc8bf - the very commit that wrote the previous row - and rebuilding it HERE reproduces this row's numbers exactly while still reading +612 against its own recorded row. So the previous row is not comparable across environments, and no number was 'fixed'. The byte column carries a path-length term, measured twice on an unchanged binary: +49 bytes for +49 characters of the compiler's own filename on a trivial program, and +34,597 bytes for +30 characters of the SOURCE filename on a large one, with the allocation count unmoved both times. The other documented candidate is the bootstrap refresh between the two rows (bin/maxon.exe here postdates af8c2e931, which changed maxon-sharp), which moves shv2's memory profile but not its output. THE ALLOCATION COLUMN IS IMMUNE TO BOTH and reads 0 at every rung, which is why it is the one to trust. NOT THIS RUNG'S, CONFIRMED PRE-EXISTING: regalloc:splitting is quadratic in PER-FUNCTION pressure (x3.64 x3.80 allocations, up to x4.13 CPU, on shape B whose main holds 256 constructions), the budgeted SplitLiveRanges term of ARCHITECTURE.md:1336-1345; with per-function pressure bounded it reads x1.85 x1.92 even at 4096 witness sites, so it tracks pressure and not witness dispatch. Gates on this tree: shv2 build exit 0, x64-windows 1793/0, wasm32-wasi 1597/0, specs-shv2/fragments clean after the run, no exit 101. | 761,772,732 | 1,476,124,108 | 2,887,874,212 | 5,915,531,810 | 13,067,859,621 | 31,285,496,807 |
| 2026-07-27 | RE-BASELINE of the witness-label injectivity rung's row onto the MERGED tree (rebased over upstream #123's BlockId dense-table work df5b5ffd8/220c5efed plus the arm64 type-name-collision goldens 27549b251). The row directly above was measured on the pre-rebase branch based at 858d8dbef, and upstream changed a data structure the compiler indexes by — so those ABSOLUTE numbers are not the merged tree's, and the next rung must compare against THIS row rather than that one. THE RUNG'S OWN FOOTPRINT IS STILL 0, re-verified after the rebase rather than re-asserted: the separator is one byte either way, and this ladder cannot reach the code at all (ScaleCorpus.maxon:554 generates no 'where' clause, so no witness table is minted and witnessTableLabel/ensureWitnessTable are never called), which is exactly why the rung was measured on a purpose-built witness ladder instead — +0 allocations and +0 bytes at every rung across 256..4096 dispatch sites and 16..256 (conformer, interface) pairs, linear on both axes. Any delta this row shows against the previous one therefore belongs to upstream's dense-table change and not to the witness join. Merged-tree gates: build exit 0 zero warnings, x64-windows 1793/0, wasm32-wasi 1597/0, specs-shv2/fragments EMPTY after both suite runs (byte-identical codegen), no run exited 101. | 776,795,503 | 1,455,739,043 | 2,862,572,480 | 5,962,385,305 | 13,195,040,157 | 31,586,449,075 |
| 2026-07-27 | Splitter: the confined half DEFERRED until it can win + applyForbidden SCOPED to the split's own ids + PeakRank stopped being a heap object minted per winning op and twice per tree combine + ScannedValues.all hoisted out of buildUseIndex's and computeLivenessOn's per-block loops + the fresh-id walk made a word-range walk. WHY THE CPU COLUMN READS A TRUE ZERO FOR THE HEADLINE FIX, and it is corpus blindness of the KNOWN kind: the ladder's pressured functions are LOOPS, which enter confined mode at their first analysis, so the deferral -- the whole 8.8x -- CANNOT engage here and correctly does not move. It is measured on the shape the corpus cannot express (Testing/ladders/genwidelive.sh 400 sum, one basic block, 396 splits: regalloc:splitting 11.49s -> 1.56s; x64-large-frame-arg7 53.9s -> ~6.1s; suite 56.3s -> ~18s). A Delta0 from a ladder that cannot express the feature measures the instrument's blind spot, not the cost. WHAT DID MOVE IS THE EXACT COLUMN, and it is attributable to the digit: whole-compile allocations fall from the ScannedValues hoist (one heap object per block of every function, in TWO per-block loops), and regalloc:splitting allocations fall a further 3.2%/4.8%/6.4%/7.8% across rungs 0-3 from the PeakRank de-allocation, every removed allocation dividing out at exactly 72 bytes -- the per-op mint dominates a single-block function, the O(log blocks) combine term dominates this many-block corpus and its share GROWS with rung, which is what the two halves being complementary predicts. CAVEAT ON THE CPU COLUMN THIS RUN ONLY: 12 competing maxon processes were live on the box (suite wall read 46.6s then 18.1s for identical work), so treat every CPU ratio here as unreliable and read the exact columns; that is the reason the memory columns exist. Residual, NOT closed: the pass is still superlinear on one basic block (per-split cost x1.50-1.71 per doubling, was x2.05) -- four O(block)-per-split walks remain and need a positional structure with order maintenance, filed as its own rung. Decisions UNCHANGED throughout: 243 goldens byte-identical on every target that ran, valuesSplit 96/196/396. | 754,180,680 | 1,411,699,240 | 2,784,877,500 | 5,662,042,560 | 12,327,608,880 | 28,425,521,140 |
| 2026-07-27 | Splitter re-space: SCOPED to the smallest subtree that can absorb the splice, and the recorded slots it moves are now actually REPAIRED. Two findings, one cause. (1) THE RUNG'S `PressureIndex.relayout` WAS NOT OFF THE PATH — "zero re-spaces on the whole scale corpus" is not what this corpus does. Measured with the pre-change binary: corpus rung0 fires ONE function-wide re-space of 1024 slots, rung1 fires TWO of 2048 each. Count doubles per rung AND size doubles per rung, so its total work went x4.0 per doubling — a quadratic sitting on the shipping corpus, invisible because a re-space allocates almost nothing and the ladder that would show it (genwidelive, one block, wide gaps) never fires one. (2) IT ALSO CRASHED. `SplitEdits.insertedSlots` records each spliced op's slot as it is seated; a re-space mid-batch MOVES those ops, and `appendInsertedSlots` asserted in a comment that the re-space "re-derived" the slots already returned — nothing did. Reachable at the SHIPPING SlotGap=4 on a plain straight-line function (alternating pressure humps: an add reading two already-spilled values sitting directly after a call whose own result is spilled puts THREE ops in one gap, and midpoint subdivision fits two): `PressureIndex.opAtSlot: slot 166 holds no op` out of `reindexSplitValues`, at every rung of a 12/24/48 ladder. THE FIX: `makeRoom` climbs from the exhausted gap to the first ancestor whose ops still fit it two slots apart and re-spaces ONLY that subtree (`respaceRange`, Theta(subtree)); the function-wide `relayout` survives as the fallback for a block that has outgrown its run, which needs the block to roughly DOUBLE its op count, so it is O(log growth) per block rather than per collision. `PressureRelayoutScratch` became a WINDOW with an old->new map, and `applyMoves` is the repair the comment used to merely claim. MEASURED on the corpus: rungs 0/1/2 now do 4/10/22 local re-spaces of 8-16 slots each = 48/128/288 slot-moves against the old 1024/4096/(16384) — LINEAR, with the per-event scope CONSTANT at 8-16 slots across all three rungs and ZERO function-wide re-spaces at any of them. WHAT MOVED IN THE EXACT COLUMNS, attributed: regalloc:splitting allocations +180/+270/+452/+816/+1544/+3000 across rungs 0-5, converging on x2.0 — exactly ONE per function that runs the splitter, the `moved` column the slot repair needs. Bytes FALL -30,277/-66,573/-139,133/-284,253/-574,493/-1,154,973, which is the avoided function-wide capture (~41,984 bytes of columns for a 1024-slot capture) less the ~10,080 bytes of per-function `moved` headers and ~2,000 of small local captures: -29,904 predicted against -30,277 measured at rung 0. UNCHANGED, deliberately: valuesSplit 96/196/396, 243 goldens byte-identical, `genwidelive` dead-control exit 7 at every rung. The one-block residual is NOT what this touched and did not move — min-of-3 CPU on genwidelive sum reads x2.395/x2.390 before and x2.199/x2.458 after, i.e. ~N^1.25 both ways, which is the Hall matching plus fillLiveBeforeOp, still measured-linear-per-split debt. Gates: build 0 warnings, suite 1793/0, and 1793/0 again under VerifyIncrementalSplit=true at SlotGap=4 AND at SlotGap=2 (which forces the function-wide fallback, covering the path the corpus no longer reaches). | 748,412,280 | 1,444,552,900 | 2,838,576,820 | 5,906,790,300 | 12,782,905,880 | 29,772,721,840 |
| 2026-07-27 | P1.7 finish Slice 1 (genericInstanceHasBaseLayout replacing a 4x-duplicated isArrayInstance-or-isSetInstance stop condition): THE COMPILER'S COST DID NOT MOVE AT ALL, and the whole of this row's apparent movement is an ARTIFACT OF THE CHECKOUT PATH, measured rather than assumed. (1) THE CODE COSTS ZERO. Against the parent (d28583442) the allocations, frees and bytes are bit-identical at EVERY rung and in EVERY per-phase bucket except phase:load and phase:parse; the whole-compile allocation count is identical to the digit. It also holds far outside the corpus: on Testing/ladders/geninstances.sh at 8,000 and 32,000 interned instances (the corpus's rung-5 count is ~387, so 32,000 is ~80x it) both compilers allocate 786,144 / 54,649,587 bytes and 3,114,445 / 225,247,505 bytes respectively -- identical to the digit both times. That ladder is the right one because rootManagedOpaqueArrayElementDrops opens the changed gate over EVERY interned instance unconditionally, so N=32000 exercises the new predicate 32,003 times per compile. (2) THE +3,876 BYTES IS THE WORKTREE'S PATH, NOT A COST, AND HERE IS THE PROOF: the PARENT's binary, copied into this worktree and run from this worktree's path, reproduces THIS row's numbers to the digit (rung 0: 615,240 allocs / 35,870,885 bytes; rung 1: 1,114,446 / 67,809,887). Same code, different path, this row's numbers. The delta lands in phase:load (-14 allocs, +3,115 bytes), phase:parse (+232 bytes) and the un-bucketed driver (+1,740), it is CONSTANT at all six rungs, and it is rung-independent because it tracks the length of the paths the driver holds, not the program. This worktree's root is 29 characters longer than the main checkout's. SO: a run recorded from the main checkout will read about -5,087 bytes / +14 allocs against this row, and that is the path unwinding, NOT a regression -- subtract it. (3) THE PER-CALL QUESTION, asked because the old form was two integer-ish name compares and the new one adds a string-keyed Map probe: the new form is NOT more expensive, and it is arguably cheaper. isArrayInstance(id) or isSetInstance(id) derived the base name TWICE (two bounds-checked array gets and a managed ByteArray refcount pair, each); the new predicate derives it ONCE and adds one Map.contains over a 3-to-5 byte interned name. No name is CONSTRUCTED on either path -- TypeNameInterner's names are zero-copy slices of the source -- which is why the exact columns above can read a true zero. The CPU column cannot resolve the difference and the reason is documented rather than waved at: a NULL CONTROL running the IDENTICAL binary on both sides of the same interleaved min-of-9 protocol produced -0.70% (N=8000) and +3.83% (N=32000) at the whole-compile level, the same magnitude and sign pattern as the real A/B (+0.79% and +3.65%), and a blocked-rather-than-interleaved repeat of the same A/B flipped a sign (phase:signatures +2.17% -> -0.11%). This box's floor for this experiment is about +/-4-5%, so any per-call claim here would be a statement about the apparatus. (4) NO SUPERLINEAR STRUCTURE, checked not assumed: the gate is O(1) and is opened ONCE per walk (not per field), the walk count is O(interned instances) exactly as before, and typeIsManaged returns true immediately for a nested genericInstance tag rather than recursing, so a deep Box-with-Box-with-String nest cannot re-walk. (5) MEASURED DEBT, filed not fixed: genericInstanceHasManagedField and genericInstanceHasStringField re-derive baseLayoutOf INSIDE their per-field loop -- genericInstanceFieldCount and substituteInstanceFieldType each call it -- so a base-name derive plus a name-keyed Map probe is paid once PER FIELD, where their sibling managedOpaqueArrayElementOf hoists `let layout = baseLayoutOf(id)` above the loop and does not. It is LINEAR (O(instances x fields), no recursion), it runs only for instances whose destructor is referenced, and generic bases carry few fields, so it is a constant factor and not a curve; with the noise floor measured at +/-4-5% above it could not be shown to move anything, and the mandate is growth curves rather than constant factors, so it is recorded here rather than shipped as a change that only looks like an optimization. The trigger that would make it worth taking: a machine-generated wide generic base (tens of fields) instantiated many times. Suite 1831/0, specs-shv2/fragments byte-identical, build 0 warnings. | 776,729,758 | 1,433,047,747 | 2,852,662,660 | 5,886,464,842 | 13,054,297,487 | 31,388,695,659 |
| 2026-07-27 | SPLITTER POSITIONAL STRUCTURE — the whole rung, and THIS ROW'S DELTA IS +192 ALLOCATIONS AND +7,621 BYTES AT EVERY RUNG, CONSTANT — AND IT IS NOT THE COMPILER. It is ENTIRELY `phase:load` (+192 allocs / +8,233 bytes, flat at every rung, rung 0 through rung 5 alike), i.e. the already-documented PATH-LENGTH / environment term: the row above was measured in this rung's WORKTREE and this one on the merged main checkout. EVERY COMPILER PHASE IS BIT-IDENTICAL between the two runs — `regalloc:splitting` reads 32,554 allocations at rung 0 and 624,377 at rung 5 on BOTH. ⚠ The coordinator predicted this row would be delta-0 and it was not; the prediction was wrong and the instrument was right, which is precisely why the columns are read rather than the expectation. ⚠ AND ITS DELTA WAS MEASURED AGAINST THE "Splitter re-space" ROW, which is NOT necessarily the row directly above this one — upstream rows have been interleaved by a rebase since, so read this row's delta against THAT row by name, never by position. It exists to attribute the jump in the "Splitter re-space" row, which was written by the rung's optimizer pass after increments 1 and 2 were already in the binary and therefore attributed only the re-space fix. Read the two rows together. WHAT THE RUNG DID, measured on Testing/ladders/genwidelive.sh (Theta(N) values live in ONE basic block, so Theta(N) splits over a Theta(N) block): regalloc:splitting allocations 129,966 / 509,828 / 2,019,538 -> 6,683 / 13,095 / 25,905 at N=100/200/400, i.e. -98.7% at N=400 and per-doubling x3.92/x3.96 -> x1.96/x1.98 — QUADRATIC TO LINEAR in the exact column. CPU 6,243M -> 1,670M ticks (-72.7%), per-doubling x3.21/x3.57 -> x2.31/x2.48, i.e. N^1.83 -> N^1.25. ON THIS MANY-FUNCTION CORPUS regalloc:splitting is LINEAR IN BOTH COLUMNS (allocations x1.59 x1.74 x1.85 x1.92 x1.96 converging on x2.0; CPU x1.91 x2.04 x1.98 x2.08 x2.12) — the residual N^1.25 is specific to ONE function carrying Theta(N) simultaneously-live values and this corpus cannot express it. THE +8.9% BYTES between the previous rung's row and the row above is THIS RUNG'S, and it is the PressureIndex's columns — the price of gap-labelled slots, measured linear, deliberate. Against it: whole-compile allocations fell ~7% at every rung. THREE MECHANISMS: (1) static per-op facts memoized by union VARIANT ordinal — 112 cases, constant size, NO staleness class, so no invalidation hook exists to get wrong; (2) the block's peak is a per-op MAX-SUFFIX-SUM over gap-labelled slots, NOT the per-interval range-add PLAN.md prescribed (the interval form needs endpoints in pass-through blocks, re-derived from live sets that are mid-edit; the per-op form separates per value, so a split's repair is O(uses of one value)); (3) the other three per-split walks were never range problems at all — they walked a whole block to service an O(1) value set, and are now edit-proportional. MEASURED: the tree ALONE was not enough (x2.81/x3.14, barely moved); both halves give x2.25/x2.45. THE LARGER HALF OF THE ALLOCATION WIN WAS UNION BOXING, pre-existing and invisible to reading: a PAYLOAD-FREE case of a payload-carrying union still heap-allocates (mm_alloc 16 bytes + mm_incref), and `ValueSpillSlot.none` was built TWICE PER OP per dirty block per split just to say "this op touches no slot" — 94.7% of the phase's allocations; `ValueDefSite` was the same shape per value crossing the peak. Both are now enums. Attribution exact: bytes-removed / allocations-removed = 16.0 at all three ladder rungs, to the digit. RESIDUAL, so the rung is ◑ not ✅: ~31% of the pass is still superlinear on one block — confinedPeakAt/hallVerdictAt 49.5% (exact Hall bipartite matching, measured LINEAR x2.03/x2.07; a matching is not a suffix sum, so it is a different problem), fillLiveBeforeOp 15.8% (Theta(pressure) data no count-tree can carry), SplitEdits.resequence/commit 8.6%, chooseVictim 7.0%. Re-measure trigger: a single block's PRESSURE growing — an inliner, or machine-generated wide types. DECISIONS UNCHANGED THROUGHOUT: valuesSplit 96/196/396, 243 goldens byte-identical, dead-control exit 7. Gates: suite 1794/0 (+1 = the re-space regression spec, seen RED at 5d99436bb with `opAtSlot: slot 166 holds no op`), VerifyIncrementalSplit ON at SlotGap=4 AND 2, x64-windows 1794/0, x64-linux 1688/0, wasm32-wasi 1598/0, arm64-macos 1603/0, arm64-linux SKIP (wedged OrbStack guest, filed). | 838,548,660 | 1,616,132,780 | 2,919,872,120 | 6,014,788,960 | 13,086,512,960 | 30,150,875,800 |
| 2026-07-27 | P1.7 finish Slice 2 (arrayElementSize narrowing a ranged-int array element to the smallest of 1/2/4/8 bytes that holds its declared range). NO CODE CHANGE FROM THE OPTIMIZER, and the numbers say why. ///// A/B PROTOCOL: the parent (d51ddd665) was built in a worktree whose absolute root is EXACTLY THE SAME LENGTH as this one (51 chars: .claude/worktrees/p17-basel vs .claude/worktrees/p17-esize) from a byte-identical bootstrap (sha256 5457eab2256c9da7802bbefb085719e041b3b2f91b2cf971be7862b8adf1c9c2). So the phase:load path-length artifact the PREVIOUS row had to argue away is EXACTLY ZERO here rather than subtracted, and every remaining byte is the code. The last committed row was NOT a usable baseline (it reads -40,942 allocs / +3,423,006 bytes against this run, all of it commits and a bootstrap between the two worktrees), which is why the A/B was taken against a freshly built parent instead. ///// THE DELTA DECOMPOSES COMPLETELY -- four terms, and they sum to the digit at all six rungs. head-minus-parent whole-compile allocations: +12, -12, -92, -236, -556, -1,180 (rungs 0..5); bytes +3,936, +7,712, +6,432, +12,384, +7,264, +13,728. (1) phase:parse -40/-80/-160/-320/-640/-1,280 allocations and -640..-20,480 bytes -- A PERFECT DOUBLING, and it is a SAVING: exactly -1 allocation and -16 bytes per Array-with-named-element size query, 40 such sites at rung 0 doubling to 1,280 at rung 5. CAUSE: arrayElementSize no longer routes the trivial case through genericInstanceSoleArgLogicalSize then typeLogicalByteSize, whose `named` arm called structOf(name), and structOf returns DeclaredStruct -- A UNION WITH AN ASSOCIATED-VALUE ARM, therefore HEAP-BOXED, 16 bytes, allocated on EVERY call including the `undeclared` answer a ranged alias always gets. The new trivialElementStorageBytes answers out of Map.get's own `throws` channel and boxes nothing. This is the same allocation class RangedAliasRegistry.underlyingInAnyFile already documents having removed for the same reason. Independently confirmed off-corpus: on a throwaway ladder of 128/256/512/1024/2048 array-create sites the head-minus-parent phase:parse allocation delta was -128/-256/-512/-1,024/-2,048 -- exactly -1.00 per site at every point. (2) phase:load +10 allocations / +432 bytes, CONSTANT at every rung, and (4) an un-bucketed +10 / +432, also constant at every rung. These are the SAME constant twice: constructing one extra empty Map field. Two RangedAliasRegistry instances exist (Project's and ProgramSignatures'); each gained `storageBytes`; Project's is constructed inside loadProject so it bills to phase:load, ProgramSignatures' is constructed outside any phase bracket. It is construction and not first-insert, because loadProject only reads files into the query DB -- no alias is recorded before phase:signatures. (3) phase:signatures AND phase:merge, each +16/+24/+24/+32/+32/+40 allocations and +1,856/+4,064/+4,064/+8,320/+8,320/+16,672 bytes -- the two storageBytes maps' backing arrays. THAT BYTE SERIES IS A POWER-OF-TWO STEP FUNCTION (Map capacity 16/32/64/128), not a curve: it steps once per ~2 rungs while the program doubles every rung, because the index is sized by the number of DISTINCT RANGED-ALIAS NAMES and not by program size. Net: the change is allocation-NEGATIVE from rung 1 upward and increasingly so, because the per-site saving grows with the program while every cost term is constant or sub-linear. Bytes stay net-positive at +13,728 on rung 5, which is +0.0011% of 1.24 GB. ///// THE PER-CALL QUESTION, ASKED BECAUSE THE DOC COMMENT SAYS "FOLD OVER EVERY DECLARATION": the fold is at WRITE time. RangedAliasRegistry.record calls widenStorageBytesFor once per declaration (one probe plus at most one upsert, O(1) amortised), and storageBytesInEveryFile is ONE Map.get -- there is no walk on the read path, which is already the index shape a fix would have introduced. MEASURED, not inferred, on a purpose-built ladder of F files each declaring the SAME alias `Small` plus S array-create sites over `Array with Small` (the corpus cannot express this: it generates ~387 interned instances at rung 5 but never has two files declare one alias name). The 960 extra array-create sites between S=64 and S=1024 cost 125,932 allocations at F=8 AND 125,932 allocations at F=1024 -- BIT-IDENTICAL across a 128x change in the number of files declaring the name, which is the exact column proving the per-call cost is independent of F. CPU on the same pair read 131,507,845 vs 138,189,331 ticks (+5.1%, at this box's measured +/-4-5% floor for this experiment, and the exact column says the work done is identical). ///// THE WRITE SIDE IS LINEAR TOO, and the contested-name path was isolated with a CONTROL: arm "same" (F files declaring one name) against arm "distinct" (F files declaring F different names, identical file/line/token count, so multiplyDeclared is never touched). Same arm, F=64..1024: phase:merge allocations 3,433 / 5,135 / 8,499 / 15,193 / 28,543 (x1.50 x1.65 x1.79 x1.88) and CPU 3,995,106 / 7,004,530 / 12,564,668 / 24,087,463 / 47,782,345 ticks (x1.75 x1.79 x1.92 x1.98); phase:signatures allocations 8,125 / 15,501 / 30,237 / 59,693 / 118,589 (x1.91 x1.95 x1.97 x1.99). Distinct arm: merge allocations 4,134 / 5,925 / 9,442 / 16,417 / 30,304 and CPU 3,335,636 / 5,364,798 / 9,251,049 / 17,138,828 / 37,317,020. EVERY ONE OF THOSE IS x2 PER DOUBLING -- no quadratic. The contested path costs ~28% more CPU in phase:merge at F=1024 than the control (the declFilePath.toString() and the byFile probe), which is a CONSTANT FACTOR on a phase that is ~1.4% of the compile, measured linear, and nowhere near triggered today: the widest real case is stdlib's seven files privately declaring `Byte`. The trigger that would make it worth revisiting is a program where hundreds of files declare one alias name. ///// CORPUS BLIND SPOT, stated rather than filed: the ladder above exercises the new LOOKUP 40x2^n times per compile, which is what the -1/site is measured off, but it NEVER EXERCISES A NARROWED STRIDE. Every array element the generator emits is `ScaleElement`/`ScaleGenElement` = int(i64.min to i64.max) (negative low, so rangedAliasStorageBytes returns the machine word by its first arm), `String`, or a struct -- all still 8. A future row must not read this corpus as evidence about narrowing itself; the throwaway ladder's `Small = int(0 to u8.max)` does emit `movRegImm32 rcx, 1` into __arr_create and was checked in the IR. ///// Build 0 warnings; suites x64 1837/0, x64-linux 1731/0, wasm32-wasi 1641/0, no leaks; specs-shv2/fragments byte-identical (git status empty after all three). | 800,957,053 | 1,501,221,854 | 2,942,598,859 | 6,220,804,199 | 13,505,680,421 | 32,421,002,728 |
| 2026-07-27 | P1.7 finish Slice 3 (three front-end divergences: newline tolerance inside [...], generic type-ARGUMENT validation via denotedNamedType, and an int fast path that no longer decides the element type from the first TOKEN) PLUS the optimizer's own change: the parser's two whole-program generic checks no longer run during the tolerant declaration sweep. ///// A/B PROTOCOL: the parent (679e023a9) was rebuilt in THIS worktree from the same bootstrap (sha256 5457eab2256c9da7802bbefb085719e041b3b2f91b2cf971be7862b8adf1c9c2) and reproduced the last committed row EXACTLY -- delta 0 allocations and 0 bytes at all six rungs -- so the committed row is a valid baseline here and every number below is this branch's. The worktree root is 51 chars, the same length as the p17-esize root that wrote the previous row, so the phase:load path-length artifact is zero rather than subtracted. ///// THREE STATES, whole-compile allocations rungs 0..5. Parent: 574,298 / 1,034,851 / 1,954,749 / 3,791,682 / 7,463,489 / 14,803,534. Slice as implemented (049b954b2): +106 / +194 / +370 / +722 / +1,426 / +2,834. After this commit: -46 / -92 / -182 / -360 / -714 / -1,420 against the PARENT -- the slice is now allocation-NEGATIVE, a swing of -4,254 at rung 5. Bytes after this commit: -2,356 / -4,808 / -9,718 / -19,542 / -39,190 / -78,514. ///// THE IMPLEMENTER'S MEASUREMENT, VERIFIED AND DECOMPOSED. Their +106..+2,834 reproduces to the digit and splits into exactly two phases with zero residue at every rung: phase:signatures +101/+185/+353/+689/+1,361/+2,705 and phase:parse +5/+9/+17/+33/+65/+129. Both series are EXACT geometrics -- signatures = 17 + 84*2^n (consecutive differences 84/168/336/672/1344, perfect doublings) and parse = 1 + 4*2^n (differences 4/8/16/32/64). LINEAR, confirmed off-corpus 4.5x beyond the ladder: a throwaway ladder of N=64..2048 `typealias I_i = GBox with E_i` declarations, each with its own ranged-int argument, read head-minus-parent phase:signatures +385/+769/+1,537/+3,074/+6,146/+12,290 -- 6.016/6.008/6.004/6.004/6.002/6.001 allocations per generic type ARGUMENT, ratios 1.997/1.999/2.000/1.999/2.000 -- with phase:parse delta exactly 0. ///// THE 20x SIGNATURES-VS-PARSE SPLIT IS TWO DIFFERENT COSTS, not one cost with a puzzle. (a) In the SWEEP denotedNamedType FAILS for every file-local argument -- a file's own declarations fold only after its whole token walk -- so each one built a positioned E3011: String.from(name) plus an interpolated message plus a Diagnostic record, ~6 allocations, into an artifact that is then discarded. (b) In the REAL PARSE the same call SUCCEEDS and the entire cost is MaxonType.structRef(id) -- a union arm with an associated value, therefore HEAP-BOXED -- returned for an argument naming a declared STRUCT and dropped on the floor by `_ =`. Isolated on a second throwaway ladder of N struct-typed arguments: +1 allocation and +16 bytes per struct-named argument, EXACTLY, at N=64/128/256/512/1024. The corpus emits instances/2 of that shape (the managed-holder arm), which is 4*2^n -- the parse series, to the digit. ///// THE CHANGE: ProgramSignatures.allFilesFolded, set once in queryProgramSignatures the moment the last file is folded; checkGenericInstance and checkGenericArgType return immediately when it is false. Both are PURE DIAGNOSTIC -- neither returns a value nor mutates anything but artifact.diagnostics -- and the sweeper's artifact is never merged (queryProgramSignatures builds a throwaway Parser; mergeArtifact runs only on the real parse's), so the sweep's verdict could never be reported by any program. The real parse re-reads every one of these declarations with the index complete (both a top-level `typealias X = Base with Arg` and a nested one inside a type body have a sweep reader and a real-parse reader that funnel through the same readTypeAliasDeclaration / readNestedTypeAlias), so every diagnostic a program earns is still recorded exactly once at the same position. The flag lives on the INDEX and not on the Parser because only the index knows which side of the fold each of its three readers -- sweeper, real parse, constant evaluator -- is on; threaded through Parser.create it would be one fact written at three call sites. ///// WHAT IT SAVES, DECOMPOSED TO THE DIGIT, this commit vs parent at all six rungs. phase:signatures -51/-101/-199/-393/-779/-1,549 allocations and -2,452/-4,968/-10,006/-20,086/-40,246/-80,594 bytes, and that is TWO terms: the whole of the slice's +2,705, plus 1,549 at rung 5 of PRE-EXISTING dead work from checkGenericInstance, which has been building the same discarded diagnostics since P1.6-A -- ~4 allocations per generic-instance declaration (its structOf DeclaredStruct box plus the E2055 'has no associated types' message the sweep records for a base whose own file has not folded). phase:parse +5/+9/+17/+33/+65/+129 allocations and +80/+144/+272/+528/+1,040/+2,064 bytes, IDENTICAL to the slice by construction: the gate is false only in the sweep, so the real parse does bit-identical work. phase:load 0 allocations / +8 bytes constant at every rung, plus an un-bucketed 0 / +8 -- the same 8 bytes twice, ProgramSignatures growing by one bool field; two are constructed, the empty placeholder in SignatureMemo.empty() (billed to phase:load) and the real one in queryProgramSignatures (built before the first phase probe, so un-bucketed). They sum exactly: rung 5 allocations -1,549 + 129 = -1,420, bytes -80,594 + 2,064 + 8 + 8 = -78,514. Off-corpus the gate is -10.00 allocations per generic alias declaration versus the slice and -4.00 versus the parent, FLAT at N=64..2048 (-10.094/-10.055/-10.031/-10.020/-10.012/-10.007). ///// CPU: NO CLAIM MADE. phase:signatures read 1,039M (parent) against 1,045M (gated) ticks at rung 5, +0.5%; whole-compile 32.19G against 32.09G, -0.3%. Both are inside this box's measured +/-4-5% band, so the CPU column can neither confirm nor deny a saving of this size and is not asked to. The exact allocation columns are the evidence. ///// RESIDUAL KEPT, MEASURED AND ATTRIBUTED: the +1 allocation / +16 bytes per generic type argument that names a declared STRUCT stays (128 of them at rung 5 = +0.0009% of the compile). It is denotedNamedType materialising a boxed MaxonType.structRef for a caller that only needs 'is it declared?'. Killing it needs a second predicate over the same six-arm cascade, and checkGenericArgType asks denotedNamedType precisely so that a type ARGUMENT and an `as`-cast TARGET can never come to disagree about what a name means. A 16-byte box is not worth a second door onto that question. ///// NO SUPERLINEARITY, and the slice's other three pieces were each read for one: initializerEndIndex now counts bracket depth instead of stopping at the first newline, still ONE forward scan per top-level binding, and a runaway (an unclosed `[`) leaves the cursor at EOF so it can happen at most once per file; arrayElementIsSingleToken/indexPastNewlines scan the newline run after ONE element and each element's run is disjoint, so the sum over a literal is bounded by its newlines; finishArrayLiteral's skipNewlines advance the cursor. The slice adds no pass, no IR op, and no collection the compiler indexes by. ///// CORPUS: the generic knobs DO double per rung (GenericInstancesBase 8 and GenericBasesBase 4 are rung-0 values knobAt doubles), giving ~448 generic type arguments and ~384 instance declarations at rung 5, which is why this cost was visible at all; the two throwaway ladders reach 2,048 arguments and stay flat at 6.00 and 1.00 allocations per site. ///// Build 0 warnings; suites x64 1851/0, x64-linux 1745/0, wasm32-wasi 1654/0, exit 0 (no leaks); verify-warm-rebuild PASS on a generic-alias program (determinism + cache + invalidation -- re-checked because the new flag lives on the memoized index); specs-shv2/fragments byte-identical (git status empty after all three suite runs). | 783,939,902 | 1,481,099,474 | 2,949,361,413 | 6,098,706,080 | 13,219,836,590 | 31,902,797,568 |
| 2026-07-27 | STALE-ARTIFACT HYGIENE (the previous build's output is now DELETED BEFORE the pipeline instead of being truncated by the writer at the end, so a compile that fails leaves nothing runnable behind). NO CODE CHANGE FROM THE OPTIMIZER, and the measurements say why. ///// A/B PROTOCOL: the parent (5bafc9bc0) was rebuilt in THIS worktree from the SAME bootstrap (sha256 8abfd02bd6158878b81d87580c9d542b9433fe6513e9bc0f12090887fe7838b4) and reproduced the last committed row EXACTLY -- delta 0 allocations and 0 bytes at all six rungs -- so the committed row is a valid baseline here and every number below is this branch's. The worktree root is 51 chars, the same length as the p17-finish root that wrote the previous row, so the phase:load path-length artifact is zero rather than subtracted. The two shv2 binaries (parent sha256 6092db6e, head 1ba204b3) therefore differ only in shv2 source. ///// THE DELTA: +14 allocations and +530 bytes, FLAT AT ALL SIX RUNGS across a 32x ladder. O(1) PER COMPILE -- not per function, per block or per op -- which is the whole claim this rung had to support. AND IT IS ENTIRELY UN-BUCKETED: all 22 CompilePhases and all 7 regalloc sub-phases are IDENTICAL TO THE DIGIT between parent and head, in allocations AND in bytes, at every rung. That is not just the attribution, it is the proof the pipeline was not touched -- the new work sits between loadProject and the first pass, outside every PhaseProbe. ///// ATTRIBUTED TO THE DIGIT WITH ZERO RESIDUE, and re-runnable: maxon-shv2/Testing/ladders/genfsprobe.sh 20000 out.maxon, whose discard_plus_require case reads 14 allocations / 528 bytes -- which is +14/+530 at the ladder's one-character-longer path. THE SPLIT: one FilePath.changeExtension is 10 allocations / 368 bytes; the four filesystem calls (File.delete twice and File.exists once in discardPreviousOutput, File.exists once in requireOutputWritten) are 4 allocations / 160 bytes, exactly ONE allocation of exactly 40 BYTES EACH. THAT 40 BYTES IS NOT THE PATH: it is the green-thread runtime's SyncRequest. __io_submit_sync mm_raw_allocs one, enqueues it, SetEvents a SYNC WORKER THREAD and parks the caller until the worker completes it, so every __ManagedFile call is a cross-thread round trip. Build the probe with the BOOTSTRAP (shv2 cannot compile stdlib/File.maxon yet); that is not a weaker measurement, because the shv2 binary is itself bootstrap-emitted from the same stdlib and the same runtime, so it is the same machine code. ///// STAR -- A TRAP THIS FOUND, worth more than the row itself: FilePath.changeExtension charges by what the String is a PRODUCT OF, not by what it says. resolveOutputPath builds outputPath by INTERPOLATION; on Windows FilePath.create then normalizes forward slashes to backslashes through String.replace. An output argument already spelt with backslashes skips the replace, so the stored String is the interpolation's own buffer and changeExtension costs 10 allocations / 368 bytes; one spelt with forward slashes goes through replace and costs 8 / 343. TWO FilePaths THAT COMPARE EQUAL COST DIFFERENT AMOUNTS -- build them from literals instead and the difference vanishes, which is how you can tell it is the buffer and not the bytes. scale-test spells its rung paths with FilePath.join (backslashes), so this ladder carries the 10-allocation form; driving the IDENTICAL rung-0 compile by hand with forward-slash arguments reads +12 / +504 instead. VERIFIED BOTH WAYS: backslash argv reproduced scale-test's rung-0 totals to the digit (parent 574,252 / 39,291,535, head 574,266 / 39,292,065) while forward-slash argv gave 574,263 and 574,275. Anyone comparing a hand-run figure against a ladder figure must match the spelling first. ///// THE TIME COST, MEASURED SEPARATELY BECAUSE NO MEMORY COLUMN CAN PRICE A SYSCALL (a File.delete allocates one 40-byte record whatever it costs). Per compile on this box, median of 3 x 20,000 iterations: a failing File.delete 52.8 us (twice), File.exists 48.2 us absent and 62.9 us present, changeExtension 1.4 us -- about 205 us in total, against a real delete of a present file at 267 us and a 64-byte File.writeBinary at 449 us. That is roughly 1 percent of a 24.6 ms trivial compile and 0.001 percent of the 14 s that builds shv2 itself. AT SUITE SCALE: 1,851 x64 spec tests times 205 us is 0.38 s of CPU, spread over 12 workers, about 32 ms of the suite's 18.4 s wall -- 0.17 percent. END-TO-END A/B, three interleaved passes of 150 compiles: success path parent 24.79/24.42/24.55 ms against head 25.08/24.85/24.75 ms, plus 0.31 ms per compile (the output EXISTS in that loop, so the delete is a real one); failure path parent 22.55/22.38/22.11 against head 22.26/22.16/22.49, delta minus 0.04 ms, inside its own noise. MEASURABLE IN A MICROBENCHMARK, FREE AT SUITE SCALE. ///// THE ONE CANDIDATE OPTIMIZATION, REJECTED ON ITS NUMBERS: guarding the delete with a File.exists. Measured, exists-then-delete on an absent path is 48.2 us against a bare failing delete's 52.8 us -- 4.6 us, which is 8.5 ms of CPU across the entire x64 suite -- and on a present path it costs a whole extra 62.9 us exists ON TOP of the delete it still has to do. It would also put a SECOND existence question in front of the one discardPreviousOutput already asks, whose entire job is to tell 'was not there' from 'would not go'. Not taken. ///// NO SUPERLINEARITY, AND STRUCTURALLY NONE TO FIND: the rung adds no pass, no IR op and no collection the compiler indexes by. Every added call runs once per compile(); all seven changeExtension sites in shv2 are once per compile or once per spec test, none inside a loop over functions, blocks or ops. The wasm arm gains the most -- writeWasm now stages a core module and an embed file and publishes the finished component with one File.rename, so plus two changeExtension, one rename and one delete -- and that is still O(1) sitting between two wasm-tools subprocess spawns. ///// CPU COLUMN: NO CLAIM MADE, and it could not make one -- four syscalls is about 10^5 ticks against a 32-giga-tick rung-5 compile. The un-bucketed CPU delta read -7.5M, -3.0M, -3.5M, -5.2M, -58.6M and +7.9M across the rungs, both signs, entirely inside this box's band. phase:parse's bend (parent x1.93 x2.18 x2.46 x2.87 x3.29, head x2.02 x2.25 x2.42 x2.88 x3.22, 38.7 percent of the rung-5 compile) is UNCHANGED between the two binaries and is PLAN.md Workstream O item 122, already filed with its cure and its re-measure trigger. Re-confirmed here, not rediscovered. ///// CORPUS BLIND SPOT THAT MATTERS FOR THIS ROW: every rung of this ladder SUCCEEDS, so it never exercises the path the rung was built to fix. The +14/+530 is the cost on the SUCCESS path; a FAILING compile pays 13 allocations, about 490 bytes and about 155 us of it (requireOutputWritten never runs), and no column here will ever show that. ///// Build 0 warnings; suites x64 1851/0 in 18.4 s, x64-linux 1745/0 in 58.4 s, wasm32-wasi 1654/0 in 26.2 s, C# bootstrap 3105/0 in 74.4 s, all exit 0 and no leaks; specs-shv2/fragments and specs/fragments byte-identical (git status empty after all four runs). | 787,109,844 | 1,490,136,342 | 2,901,182,650 | 6,022,174,303 | 13,243,309,919 | 31,820,334,095 |
| 2026-07-28 | P1.7a slice 2b-v: THROWING WITNESS DISPATCH (StdOp.witnessTryCall, MaxonOp.witnessTryDispatch, an isThrowing flag on witnessDispatch, a ThrowsClause on InterfaceMethodSig, a TryTarget union replacing a bare callee ByteArray). NO CODE CHANGE FROM THE OPTIMIZER; the measurements below say why, and they CORRECT the implementer's attribution. ///// A/B PROTOCOL: the parent (33fef8c52) was rebuilt in a SIBLING worktree whose root path is 34 characters, the SAME LENGTH as this one, from the IDENTICAL bootstrap binary (md5 93ce2ab682b40d4d2b34031cd07f347a, copied not rebuilt), so the phase:load path-length artifact is ZERO rather than subtracted and the two shv2 binaries differ only in shv2 source. phase:load reads delta 0 allocations and 0 bytes at all six rungs, which is that protocol checking itself. ///// THE DELTA, head minus parent: rung0 +148 allocs / +106,968 bytes; rung1 +242 / +198,272; rung2 +430 / +380,976; rung3 +806 / +746,288; rung4 +1,558 / +1,477,008; rung5 +3,062 / +2,938,352. Delta ratios x1.64 x1.78 x1.87 x1.93 x1.97 across a DOUBLING ladder, converging on x2.00 => LINEAR. +0.021 percent allocations and +0.236 percent bytes at rung 5. The implementer's figures reproduced to the digit. ///// ATTRIBUTION 1 IS WRONG, AND A KNOB SAYS SO RATHER THAN ARITHMETIC. The implementer read the added allocations as '+40 bytes per INTERFACE METHOD, paid twice', because phase:signatures reads EXACTLY +40.00 bytes per added allocation at all six rungs. Both halves are wrong, and the story could not have been right: scale-test's corpus generates NO interfaces at all, so a per-interface-method cost is a zero this ladder is structurally incapable of measuring. THREE KNOBS, each varying ONE thing with the rest fixed, all run head-vs-parent through --log=compiler:debug's per-phase memory table: (a) INTERFACE-METHOD COUNT K = 0 / 20 / 60 in one file -> allocations FLAT at +3 signatures / +3 parse, and signatures BYTES flat at +120. InterfaceMethodSig's new ThrowsClause field allocates NOTHING; it costs +8 bytes INLINE per interface method, visible only in phase:parse (+120 / +280 / +600, slope exactly 8.0). It allocates nothing because the PARENT already called parseOptionalThrowsClause() and threw the result away -- head merely keeps it. (b) FUNCTION COUNT N = 0 / 200 / 400 / 800 in one file -> allocations FLAT at +3 / +3 again. Not per function either. (c) FILE COUNT F = 2 / 6 / 21 -> allocations = F + 2, IN EACH PASS, at every size (+4, +8, +23). SO THE ADDED ALLOCATIONS ARE PER FILE PER PARSE PASS -- one for the signatures pass and one for the parse pass -- which is a thing this corpus DOES generate (its file count doubles with the rung: 9 + 23 x 2^k, reproducing +96 / +165 / +303 / +579 / +1,131 / +2,235 in phase:signatures exactly). ///// AND THE 40 BYTES IS TWO UNRELATED COSTS THAT HAPPEN TO SUM TO 40. Proven by adding two MORE ThrowsClause fields to Parser and rebuilding: allocations went up EXACTLY x3 at every file count (3->9, 4->12, 8->24, 23->69), while the byte delta fit a + 24c for field-count c with residue zero in all four cases -- a ThrowsClause.none box is 24 BYTES, and the constant a was exactly 16 x (number of Parsers) in all four, which is the OTHER two new Parser fields (lastWitnessDispatchIndex, lastWitnessDispatchName) as 16 bytes of INLINE struct growth. 24 + 16 = 40. An attribution that only FITS the numbers is not an attribution. ///// ATTRIBUTION 2 IS CORRECT, AND NOW PROVEN BY KNOB RATHER THAN INFERRED: +8 BYTES ON EVERY StdOp BOX, WITH ZERO EXTRA ALLOCATIONS, because witnessTryCall's SEVEN fields make it the widest variant in the flat union (previous maximum six: witnessCall, osReadOverlapped) and the bootstrap sizes a union's payload box by its WIDEST CASE. THE KNOB: adding ONE dummy 8th ValueId field to witnessTryCall and rebuilding EXACTLY DOUBLED the byte delta at three independent program sizes -- 280 -> 560, 9,880 -> 19,760, 38,680 -> 77,360 -- with the allocation delta unchanged at zero. Slope 48.0 bytes per function with one field and 96.0 with two, i.e. 6 StdOp boxes per trivial function x 8 bytes per field. At rung 5 the tax lands ENTIRELY in the three phases that allocate StdOps -- lowerMaxonToStd +2,297,176 bytes, foldConstOperands +417,752, insertRangeChecks +72, all with delta 0 allocations -- and is EXACTLY ZERO in every Maxon-dialect phase (parse, merge, semanticCheck) and in isel, regalloc, encode, link and writeExe. 2.71 MB of a 1.2447 GB compile: 0.218 percent of bytes, 0 allocations, strictly O(StdOp count). ///// AND MaxonOp DID NOT GROW, WHICH IS WHY THE MAXON-DIALECT PHASES READ ZERO: witnessDispatch went 5 -> 6 fields and witnessTryDispatch is 6, but MaxonOp's widest variant is tryCall at SEVEN and always was, so the Maxon box is untouched. The rung got that for free and the per-phase numbers confirm it independently of reading the union. ///// THE 7-FIELD WIDTH IS PAID, DELIBERATELY, AND HERE IS THE RULE RATHER THAN A DEFECT. It is not narrowable without giving up the thing the op is for: all seven fields are witnessCall's six VERBATIM plus the one thing tryCall adds to call (a second def, errorFlagId), which is precisely what lets StdToX64Conversion share the witnessCall lowering rather than re-derive it. Packing methodIndex with argFloatMask would be a magic bitfield; a bool discriminator on witnessCall instead of a distinct variant is the explicitly ruled-out v1 isVoid mistake that shv2's union-typed StdReturnType exists to avoid. STANDING RULE: ANY NEW WIDEST StdOp VARIANT TAXES EVERY StdOp ALLOCATION BY 8 BYTES PER FIELD. The measured price of the 7th is 0.218 percent of a self-scale compile's bytes and zero allocations; an 8th would cost the same again. ///// NO SUPERLINEARITY IN WHAT THE RUNG ADDED, MEASURED ON CORPORA BUILT FOR IT -- because a zero from THIS ladder would have meant nothing (it emits no interfaces, no generics and no witness dispatch; its own manifest says so). Three hand-built ladders, each doubling: (1) WITNESS-DISPATCH ladder, 64 -> 1024 `try self.item.digest()` sites through a `where T is Digest` witness, one throwing interface method: every phase x2.00 in ALLOCATIONS (total x1.85 x1.92 x1.96 x1.98) and every phase x2.00 in CPU TICKS, minimum of 3 runs (total x1.73 x1.88 x1.96 x2.01). (2) CONFORMANCE ladder, 128 -> 1024 conforming types against a 2-method throwing interface, so validateThrowsConformance -- the check this slice added, and it runs in BOTH directions -- is exercised on 256 -> 2,048 pairs: every phase x2.00, total x1.95 x1.97 x1.99. (3) WIDE-INTERFACE ladder, M interface methods x M dispatch sites, 64 -> 1024: allocations x2.00 throughout. The try desugar's rewrite is index-addressed, not scanned (rewriteLastCallToTryCall reads opRefs[count-1] and commitWitnessTryDispatchRewrite does ops.set(opIdx), both O(1)), which is why (1) stays flat. ///// THE CONSTANT FACTORS WERE LEFT ALONE, ON PURPOSE. The code is already alive to the ThrowsClause.none box and avoids it in both hot paths -- Parser.maxon:3793 gates the pre-scan's clause on the `throws` KEYWORD so a non-throwing function allocates neither a clause nor an entry, and SemanticCheck.maxon:1105 uses throwsMap.contains rather than `get otherwise ThrowsClause.none` for exactly this reason ('would box a none for every one of the program's calls that is not to a throwing callee'). The one box this rung adds is 1 per Parser = 2 per file per compile, about 1,490 of 14.8M allocations at rung 5 (0.010 percent), and removing it would mean replacing a `var` field with a new managed global. Not taken: that is a constant factor, and the mandate is growth curves. ///// ONE TERM FOUND, MEASURED, AND FILED RATHER THAN FIXED -- PRE-EXISTING (slice 2a), UNCHANGED BY THIS RUNG. interfaceMethodIndex (IrInterface.maxon:302) and requireInterfaceForParse (Parser.maxon:18542) are LINEAR SCANS run once per witness-dispatch site, over the interface's methods and over the file's interfaces respectively -- a linear lookup inside a walk over dispatch sites, so the term is O(dispatch sites x interface methods). ISOLATED WITH A SIZE-MATCHED KNOB, which is the only way to see it: same M methods, same M dispatch sites, only the TARGETED method's position moved from FIRST to LAST (programs 190,327 vs 191,351 bytes, a 0.5 percent difference that is just longer digit strings). phase:parse costs +13.7 percent at M=D=256, +39.3 percent at 512, +36.2 percent at 1024, minimum of 5 runs each. WHAT IS NOT CLAIMED: a confirmed quadratic EXPONENT. The isolated delta grew 12.7M -> 70.1M -> 133.4M ticks where a clean D x M term predicts x4.00 per doubling, and x5.52 then x1.90 is not that -- the cpu-percent column quantizes at 0.1 percent and the first-position baseline moves too, so this measurement can establish that the term EXISTS and is per-dispatch-site, and cannot establish its exponent. At an absurd 1024-method interface it is worth about 5 percent of the compile. RE-MEASURE TRIGGER: an interface wider than a few dozen methods (machine-generated conformance), or one generic body with many dispatch sites against one wide interface. CURE: index IrInterface.methods by name instead of scanning. ///// CPU COLUMN: NO CLAIM ON THE A/B. The un-bucketed per-rung CPU delta read both signs and sat inside this box's band; four syscall-free 40-byte boxes per file cannot be priced against a 34-giga-tick rung-5 compile. phase:parse's known bend is PLAN.md Workstream O item 122, unchanged between the two binaries and re-confirmed, not rediscovered. ///// GATES: build exit 0 with zero warnings; specs-shv2 x64 1861 passed / 0 failed, exit 0, no leak (no exit 101); specs-shv2/fragments byte-identical -- `git status --short specs-shv2/fragments/` empty after the suite run, and the whole working tree clean, which is what proves this pass emitted byte-identical codegen. | 895,660,808 | 1,579,109,566 | 3,089,279,871 | 6,756,940,476 | 14,404,379,841 | 36,343,838,486 |
| 2026-07-28 | P1.7a slice 2b-iv-B: WASM WITNESS LOWERING (bakeFuncTableIndexRelocs baking a funcref TABLE INDEX into the same .rdata slot x64 bakes a .text VA into, witnessCall/witnessTryCall interned through internIndirectCallType, emitWitnessCall/emitWitnessTryCall, the funcref table enabled on a non-empty pendingRdataRelocs, and the compile-time refusal requireIndirectlyReachableParamsAreMachineWords). NO CODE CHANGE FROM THE OPTIMIZER; the measurements below say why. ///// A/B PROTOCOL: the parent (a2543d15c) was rebuilt in THIS worktree, same path, same bootstrap (bin/maxon.exe sha256 d00fc7df6ebae372cd7f83878913f1ea4d1aee08890fc1bc0373068c3ab72543), and both scale-tests were run from the same 33-character root, so the path artifact is zero rather than subtracted. head sha256 0f7b9cda8d2c86c48e71edc653a24d40400fb396e9e3fb006d79e8b50ea54647, parent 9acbc177db2e1e73e2e34530d7272ee5c7ffffcf3dd22c848b55730e9f549548. RESULT: allocations AND bytes bit-identical at all six rungs, 574,428 / 39,395,954 through 14,805,204 / 1,244,702,896. The implementer's delta-0 claim is confirmed to the digit. ///// THE -412 IS NOW ATTRIBUTED, AND IT IS THE CHECKOUT PATH LENGTH AT 202 BYTES PER CHARACTER. Measured, not inferred: two directory junctions onto this same worktree at 34 and 35 characters, identical binary, identical corpus. The delta versus the committed row reads -412 at 33 characters, -210 at 34, -8 at 35 -- exactly +202 per character, twice over. Located per character: phase:load +134, phase:parse +8, un-bucketed +60, FLAT at every rung (rung 0 and rung 1 each move by exactly those amounts, so it is a fixed set of path buffers crossing size classes, not a per-corpus-file cost). The committed 2b-v row was therefore recorded from a root two characters longer than this one, plus an 8-byte residue. ///// THE IMPLEMENTER'S SUB-CLAIM IS REFUTED AND ITS CONCLUSION UPHELD. It measured phase:load at 1 byte per character (67232 -> 67234 for a 2-character-longer name) and concluded that path length could not produce -412. The 1-byte figure is real and I reproduced it exactly -- 67,502 -> 67,500 for a 2-character-shorter binary NAME on a single-file compile, where the only path in phase:load is argv[0]. It simply does not generalize to the scale corpus, whose phase:load holds many more path buffers and moves 134 bytes per character of ROOT. Right answer, wrong reason: an artifact measured in one bucket does not price the same artifact in another. ///// THE DELTA-0 ABOVE MEASURES UNREACHED, NOT FREE, AND THE IMPLEMENTER SAID SO ITSELF. This corpus generates no interface, no witness dispatch and no managed-opaque-T descriptor, so globalData.pendingRdataRelocs is EMPTY at every rung and BOTH new walks execute ZERO iterations. A ladder was built for them (temp/genwitness.sh): N conformer types against a 2-method interface, every one dispatched through a single `where T is Digest` generic body, N = 32 -> 1024 doubling, so the reloc list doubles 64 -> 2,048. Confirmed per-conformer rather than shared, by the new refusal itself: fed a bool parameter it names `__witness_P0.Digest'+32`, i.e. one witness blob per conformer with two method slots. ///// WASM IS THE ONLY TARGET THAT RUNS THE NEW CODE, and a host-only scale-test cannot see any of it. buildWasmBackend bills the entire backend to phase:encode, so bakeFuncTableIndexRelocs, requireIndirectlyReachableParamsAreMachineWords, the two newly-interned op kinds and emitWitnessCall all land in that one bucket. Compiled by hand with --target=wasm32-wasi --metrics, minimum of 3 per rung. phase:encode ALLOCATIONS 9,280 -> 17,607 -> 34,221 -> 67,414 -> 133,763 -> 266,417 (x1.90 x1.94 x1.97 x1.98 x1.99); phase:encode BYTES 550,083 -> 1,084,886 -> 2,140,163 -> 4,262,508 -> 8,565,812 -> 17,260,267 (x1.97 x1.97 x1.99 x2.01 x2.02); phase:encode CPU 9,619,410 -> 18,823,224 -> 38,886,862 -> 71,012,108 -> 146,346,498 -> 298,904,609 ticks (x1.96 x2.07 x1.83 x2.06 x2.04, the 1.83 inside the band). LINEAR IN RELOCS in all three columns across a 32x span. Neither walk is accidentally quadratic through a map: calleeFuncIdx.get and calleeParamTypes.get are ByteArray-keyed Map lookups built once from module.functions, GlobalDataTable.rdataOffsetOf is a Map lookup too, and the guard's inner loop is over ONE callee's parameter list. ///// AND IT IS CORRECT AT SCALE, not merely fast: the N=1024 rung -- 2,048 baked funcref table indices -- compiles and RUNS under wasmtime with exit 0. ///// X64 A/B ON THE SAME SATURATED LADDER: EXACTLY 0 ALLOCATIONS AND 0 BYTES IN EVERY PER-PHASE BUCKET AND IN THE TOTAL, at N=32 and N=1024 alike. That is where the shared front-end changes (Parser.interfaceReturnMaxonType claiming ExitCode, builtinProtocolResultStdType) get priced, because the parent cannot be A/B'd on wasm at all -- it refuses every program holding a funcAbs64 relocation, and replacing that blanket refusal is the whole slice. ///// A SELF-CORRECTION WORTH MORE THAN THE RESULT: my first x64 A/B read +7 allocations / +330 bytes, flat at all six rungs, and I was one step from filing it as a per-interface cost. It was the same path artifact one level down -- the two binaries sat at ./maxon-shv2/.maxon/maxon-shv2.exe and ./temp/parent-shv2.exe, 16 characters apart in argv[0]. Re-run with both at 20-character paths the delta is exactly zero, in every bucket. A FLAT non-zero delta between two binaries is a path check before it is a finding. ///// CPU COLUMN: NO CLAIM ON THE A/B. Single sample; the dominant buckets read +0.4 percent phase:regalloc, +0.1 percent regalloc:splitting, +0.2 percent total -- inside the band -- while the sub-1-percent phases swing +-28 percent, which is what an un-repeated CPU sample looks like. ///// NOTHING WAS CHANGED, AND TWO CANDIDATES WERE DECLINED ON PURPOSE. (1) The two walks over pendingRdataRelocs could be fused into one, halving the map lookups. DECLINED: the guard is a compile-time REFUSAL and must finish before any slot is patched, so fusing would panic with the .rdata image half-baked -- and it is a constant factor on a walk worth low single digits of one phase. (2) emitIndirectCallOp rebuilds the signature key (a WasmValTypeArray plus an encoded ByteArray) per indirect-call op just to look up the interned type index: 2 allocations per witness call, about 4,096 of phase:encode's 266,417 at N=1024, 1.5 percent. DECLINED: pre-existing (emitCallIndirect did exactly this before this slice, which merely routes two more op kinds through it), O(1) per op, and rebuilding through the same two helpers internIndirectCallType built it with is the stated invariant that keeps the call-site immediate from disagreeing with the interned type. Trading a correctness-by-construction invariant for 1.5 percent of one phase's allocations is not the mandate. ///// TWO KNOWN TERMS RE-CONFIRMED, NOT REDISCOVERED. This ladder bends in phase:parse (x2.56 x3.05 x2.80 on wasm, where no register allocator runs) and in regalloc:splitting (x3.08 x3.20 x3.20 on x64). Those are Workstream O item 122 -- interfaceMethodIndex and requireInterfaceForParse as linear scans per witness-dispatch site, filed by the 2b-v pass -- and the budgeted splitter re-liveness (ARCHITECTURE.md:1336-1345). Both read allocation-identical between the two binaries, so neither moved, and neither is this slice's to fix. ///// GATES: build exit 0 with zero warnings; specs-shv2 x64-windows 1865 passed / 0 failed, exit 0, no leak; specs-shv2 wasm32-wasi 1754 passed / 0 failed, exit 0, no leak; `git status --short specs-shv2/fragments/` empty after BOTH suite runs and 0 files changed versus main, and the whole working tree clean -- which is what proves this pass emitted byte-identical codegen on both targets. | 798,483,936 | 1,482,563,327 | 2,922,104,918 | 6,143,467,805 | 13,509,637,934 | 32,351,082,813 |
| 2026-07-28 | P1.8 SLICE A -- `for` loops (numeric ranges + Array iteration). NO CODE CHANGE FROM THE OPTIMIZER: nothing measured superlinear, and the two constant-factor candidates were both unmeasurable (see REJECTED below). The deliverable is a COMMITTED LADDER, because this corpus cannot see the rung at all. ///// A/B PROTOCOL: the parent (016751f8b) was rebuilt in THIS worktree from this worktree's bootstrap and both binaries were measured at the SAME canonical path (maxon-shv2/.maxon/maxon-shv2.exe, swapped between runs), so the two differ only in shv2 source. ⚠ THE COMMITTED ROW ABOVE IS NOT A VALID BASELINE HERE: it was written in a 51-character worktree root and this one is 28, and the in-tree parent reproduces it at +14 allocations / -4,109 bytes FLAT at all six rungs -- a path artifact, not a change. So the delta column of THIS row carries that artifact too (+156 = +142 real, +14 path). Every attribution below is head-minus-in-tree-parent, which is artifact-free. ///// THE DELTA, head vs in-tree parent: +142 ALLOCATIONS, FLAT AT ALL SIX RUNGS across a 32x ladder -- O(1) PER COMPILE, not per function, per block, per op or per binding access. Bytes are 6,288 + 1,008 * 2^rung: +7,296 +8,304 +10,320 +14,352 +22,416 +38,544, whose successive differences are 1,008 2,016 4,032 8,064 16,128 -- EXACTLY DOUBLING, i.e. perfectly linear in corpus size. ATTRIBUTED WITH NO RESIDUE: the +142 is +108 `unattributed` (building the new `__arr_get_unchecked` Std graph, which sits outside every CompilePhase probe) plus +28 `phase:isel` (selecting it), once per compile; the 1,008 * 2^rung bytes is 8 bytes per record on `LoopPhiVar.entryValue` and `LoopContext.continueTargetId`, paid by the corpus's `while` loops -- the corpus has no `for`, so this whole byte term is the OLD construct carrying the new field. The implementer's own claim (`6288 + 1008 * 2^n`) reproduces to the digit. ///// ⛔ THE CORPUS IS STRUCTURALLY BLIND TO THIS RUNG, and a Delta-0 from it would be the instrument's blind spot and not the cost. `ScaleCorpus` generates ZERO `for` loops -- it could not, they did not parse until this commit -- so the lowering, the element read and `Parser.iterationLockedBindings` are invisible in allocations, bytes AND CPU alike. The cure is precedent, not invention (P1.7a 2b-ii built its own generics ladder for the same reason): this rung commits `maxon-shv2/Testing/ladders/genfor.sh <loops> <depth> <accesses> <array or range or noloop> <out>`, whose knobs are independent -- program size is loops x accesses, so DEPTH MOVES ALONE. ///// THE READING THAT MATTERS IS THE NO-LOOP CONTROL, because the lock is read at FOUR doors on ordinary binding-access paths (`immutableReceiverNameOf`, `parseFieldChainCall`, `selfFieldIsWritable`, `requireMutableBinding`) -- a per-access tax on every program for a feature it does not use is the exact shape of the defect the P1.7a 2b-i pass found. Measured `genfor.sh N 1 2 noloop` at N = 64/128/256/512/1024, head against the in-tree parent: `phase:parse`, `phase:lex`, `phase:signatures`, `phase:merge`, `phase:lowerMaxonToStd` and `phase:regalloc` are IDENTICAL TO THE DIGIT at all five rungs (parse: 14,816 / 27,842 / 53,863 / 105,877 / 209,874 both ways). The entire delta is the same +142 flat, in the same two buckets. NOTHING PER ACCESS. That is what the `count() == 0` guard in front of the walk buys, and it is now measured on a program built to defeat it rather than on a corpus that never enters it. ///// NOTHING SUPERLINEAR, WITH THE LADDERS THAT ESTABLISH IT. loops 64 -> 1,024 (`genfor.sh N 1 2 array`): `phase:parse` allocations x2.00 x2.00 x2.00 x2.00, CPU x2.04 x2.02 x1.99 x1.99, and every other phase x2.00. `range` mode (literal bounds, so the lock stack stays EMPTY): x2.00 in every phase in both columns. accesses 1 -> 8 at a fixed loop count: linear per marginal statement. DEPTH, constant size (`genfor.sh D D 512/D array` -- one function, exactly 512 accesses at every rung, 1,043 -> 1,057 lines): `phase:parse` allocations 6,541 / 6,674 / 6,940 / 7,469, an excess over depth 1 of 133 / 399 / 928 = 133 x (1, 3, 7), EXACTLY proportional to (D-1). That is `namesAssignedIn`'s documented Theta(tokens x depth) scan, LINEAR IN DEPTH, and the lock walk adds nothing measurable on top of it. `phase:regalloc` reads x7.21 from depth 1 to 2 then x1.68 x1.95 -- a MODE CHANGE (the splitter engages at all once two loops nest), not a curve, and the identical shape appears on a nested-`while` ladder built against the PARENT binary (x5.40 x4.43), so it is not this rung's. ///// STAR -- THE DEPTH KNOB HAS A CEILING THE COMPILER ITSELF ENFORCES, which is why `bindingIsIterationLocked`'s linear scan cannot go quadratic on anything that compiles. Every `for` mints an index phi live across every loop nested inside it, so depth D holds D values at once and E5001 refuses: MEASURED, depth 10 compiles and depth 11 is "needs 1 more register(s) than are available". `gennest.sh` evades exactly this by giving every `while` level the SAME guard variable; a `for` cannot, because the counter is minted by the lowering and not by the program. So the lock stack is bounded by ~10 in any program that builds, and an O(depth) scan behind an empty-stack guard is O(1) in practice. ///// PRE-EXISTING BEND RE-CONFIRMED, NOT REDISCOVERED: `phase:parse`'s corpus growth is x3.18 on the parent and x3.19 on head at rung 5 -- UNCHANGED -- and it is PLAN.md Workstream O item 122 (the Theta(bindings in scope x constructs) walk `mintLoopHeaderPhis` and `assignedBindingsIn` share). `for` now joins `while`, `if` and `match` in triggering it and makes it NO WORSE: a hand ladder of N/2 mutable bindings then N/2 loops in one function reads `phase:parse` CPU x2.00 x2.17 x2.29 x2.67 x3.03 in the `for` spelling against x2.12 x2.12 x2.40 x2.61 x3.09 in the `while` spelling ON THE PARENT BINARY, with allocations flat at x1.99 in both -- the same curve, and exactly the shape a memory-only instrument cannot see. Not fixed here: it is not this rung's, the obvious inversion is already measured a NET LOSS in `namesAssignedIn`'s header (`phase:parse` x3.5 on the corpus), and the real cure named there is a bottom-up carried-set redesign. ///// ONE UNATTRIBUTED CPU READING, REPORTED RATHER THAN LAUNDERED: `phase:signatures` runs ~+7% on the corpus (and ~+10% on the no-loop ladder) head against parent, FLAT at every rung, with allocations IDENTICAL TO THE DIGIT and the growth ratio unchanged at x2.01 both ways. It is a CONSTANT FACTOR, not a curve. The only new per-token work in that phase is one enum comparison in `opensBlockAt` (`or kind == TokenKind.for`), and that cannot be it by two orders of magnitude: the rung-5 delta is 56.9M ticks over roughly 5 x 10^5 tokens, about 114 ticks per token, against ~1 for a compare. Most likely code layout between two different binaries -- `phase:lex`, whose source is IDENTICAL, scatters +-5% in BOTH directions across the same pair. Worth 0.23% of the compile; recorded so the next reader does not rediscover it as new. ///// REJECTED, ON THEIR NUMBERS: (1) hoisting the `count() == 0` test in front of `iterationLockNameAt`'s `scope.contains` + `scope.getInfo`, so a field-chain array method call in a loop-free program skips a hash lookup. It allocates nothing either way, so the exact column cannot price it, and the ladder built to maximise it (one `b.items.push` per statement, 1,024 of them) reads `phase:parse` CPU inside the layout band. A change I cannot measure is not an optimization. (2) The `count() == 0` guard the implementer says it PREDICTED would matter and MEASURED identical -- kept, and now justified by a measurement it did not have: on the `noloop` control the guard is what makes the four doors cost the digit-identical zero above. ///// REPORTED TO THE COORDINATOR, OUT OF THIS PASS'S SCOPE (a correctness question, not a cost): `for i in n upto 3 'l' ... n = n + 1 ... end` is E2013 in shv2 and COMPILES AND RUNS in the bootstrap oracle (returns 3). `lockIterationSource` is called BEFORE the range/array fork, so a RANGE whose start bound is a NAME locks that name for the whole body -- although the rung's own header states both bounds are evaluated once in the preheader, and the END bound is correctly writable (`for i in 0 upto n` with `n = n + 1` compiles). An asymmetry between the two bounds of one range, and a divergence from the oracle on a legal program. | 768,725,560 | 1,471,468,300 | 2,883,343,480 | 5,925,591,560 | 13,270,551,140 | 33,596,131,740 |
| 2026-07-28 | P1.8 SLICE A -- INDEPENDENT REVIEW. Two reachable defects fixed (a 5th mutation door segfaulting on all four spellings of a self-field write during `for … in` over that field; a missing range-bound type guard that PANICKED the x64 emitter on a float range and silently accepted a bool one), three duplications single-sourced, 15 pinning tests. Suite 1891/0 -> 1906/0. ///// COST OF THE REVIEW ITSELF: +0 ALLOCATIONS at every rung, and BYTES -64 -112 -208 -400 -784 against the optimizer's row -- successive differences -48 -96 -192 -384, exactly doubling, i.e. linear in corpus size. That is `LoopContext.headerId` deleted: 8 bytes off every loop context, paid back by the corpus's `while` loops. The field was WRITE-ONLY (declared, constructed, written at both loop parsers, read NOWHERE) and its value coincided with `continueTargetId` for `while` and not for `for` -- the trap with the check removed. The two new guards are behind the existing `count() == 0` test and allocate nothing. ///// ***THE PREVIOUS ROW'S ONE UNATTRIBUTED READING IS NOW ATTRIBUTED, AND ITS STATED HYPOTHESIS IS REFUTED BY ITS OWN CONTROL.*** That row recorded `phase:signatures` ~+7% head-vs-parent, flat, allocations identical, and offered "most likely code layout between two different binaries", citing `phase:lex` (IDENTICAL SOURCE) as scattering +-5% in BOTH directions. Re-measured here against a THIRD, independently built binary (review-head vs `main` rebuilt in this worktree, both run from the same canonical path, 5 rungs, repeat=5): `phase:signatures` is +8.6% +10.4% +8.1% +8.4% +8.6% -- CONSISTENTLY POSITIVE at every rung. The control does NOT scatter: `phase:lex`, whose source is identical in all three binaries, reads -2.4% -2.7% -3.1% -3.1% -1.8% -- consistently NEGATIVE and three to five times smaller; `phase:merge`, also untouched, is -2.2%..+0.7%. So the binary-identity component on this pair is about -2 to -3% with a STABLE SIGN, and signatures sits ~11 points away from it in the OPPOSITE direction. It is real work, not layout. ***>>> CORRECTION 2026-07-28, AND THIS SENTENCE IS THE ONE THAT WAS WRONG: THE +8.6% DOES NOT SURVIVE INTERLEAVING AND IS A MEASUREMENT-PROVENANCE ARTIFACT.*** User hypothesis ("some of the measurements were done on a different machine"), tested on an IDLE box with the two binaries swapped at ONE canonical path and the runs INTERLEAVED P/H/P/H (4 pairs, rungs=3, repeat=3, both phases read from the SAME run's table): `phase:signatures` reads +1.03% +0.55% +1.23% (overall mean +0.94%) and CHANGES SIGN across pairs (-3.43% +0.68% +3.94% +3.74%), while the control `phase:lex` REPRODUCES at -2.24% -1.51% -1.31% (mean -1.69%). The entire claim above rested on signatures being "CONSISTENTLY POSITIVE at every rung"; interleaved, that consistency is gone and the effect cannot be distinguished from zero. WARNING -- WHY THE `phase:lex` CONTROL DID NOT CATCH IT, which is the transferable lesson: lex excludes a UNIFORM factor, but a drift confined to a TIME WINDOW need not move every phase equally -- lex reproduced and signatures did not, i.e. the two phases have DIFFERENT sensitivity to the drift, which is exactly what disqualifies one as a control for the other. A stable SIGN across rungs is NOT evidence of a real effect when all head runs sit in one time window and all parent runs in another; only INTERLEAVING separates code from clock. UNAFFECTED AND STILL TRUE: the BYTE finding (allocation count identical to the digit, bytes +768/+1,320/+2,424/+4,632/+9,048 -- the records that already existed got bigger). Bytes are exact and reproducible; it was only the CPU story built on top of them that failed. ///// WHAT IT IS: `phase:signatures` allocation COUNT is identical to the digit at every rung (14,061 / 23,816 / 43,209 / 81,854 / 158,940 both binaries) while its BYTES move +768 +1,320 +2,424 +4,632 +9,048 -- successive differences 552 1,104 2,208 4,416, exactly doubling, over a constant +216. Same count, more bytes: THE RECORDS THAT ALREADY EXISTED GOT BIGGER, and nothing new is allocated. That is the 8-byte fields the rung added (`Parser.iterationLockedBindings` -- a `Parser` is built per top-level constant by `SignatureIndex.walkConstInitializer` -- plus `LoopPhiVar.entryValue` and `LoopContext.continueTargetId`). ///// ⚠ THE BYTE GROWTH IS THE MARKER, NOT THE COST: +9,048 bytes on 7,316,360 is +0.12%, which cannot buy +8.6% of CPU. What the bytes prove is WHICH objects changed; the CPU is spread across the several constant-factor additions on that phase's paths (a wider `Parser` record and its constructor, `opensBlockAt`'s extra arm, `parseBlockBody`'s new parameter and empty-block test, `edgeArgsFor`'s selector, `fieldChainMethodCallAt` now going through `fieldChainEndAfter`). Isolating which one needs PER-FUNCTION CPU attribution, which shv2 does not have -- that is the instrument gap this reading names, and it is why "unattributed" was the honest word before and "real, unresolved between five candidates" is the honest word now. Worth ~0.4% of the compile (signatures is ~4.8% of it). NOT chased: every candidate is the price of the feature, and no single one is measurable apart from the others. ///// REJECTIONS RE-CHECKED AND UPHELD: hoisting `count() == 0` in front of `iterationLockNameAt`'s two hash lookups stands rejected -- confirmed it has exactly ONE caller (`parseFieldChainCall`, an array-typed field-chain receiver with a trailing call), so it is a narrow path and not a per-statement tax, the maximising ladder read inside the band, and the guard it would duplicate already exists one call deeper in `bindingIsIterationLocked`. Shipping it would be the unmeasurable change the previous pass correctly declined. | 775,505,140 | 1,475,359,880 | 2,852,373,100 | 5,998,970,320 | 12,991,292,180 | 32,279,652,520 |
| 2026-07-28 | **INSTRUMENT CHANGE, NOT A COMPILER CHANGE — `DefaultRepeatCount` 3 → 1, so every CPU row from here down is a SINGLE SAMPLE where every row above is a MINIMUM OF THREE.** Read the step at this date as a change of estimator, never as a regression. WHY: the repeats were buying the routine per-rung read nothing — measured on one unchanged tree, the per-phase ALLOCATION tables at `--repeat=3` and `--repeat=1` are BYTE-IDENTICAL (as `ScaleTestRunner.maxon` already promised: "MEMORY needs exactly ONE repetition"), while the run costs 51 s vs 17 s. Cost is linear in the count. WHAT IT COSTS, both stated rather than discovered later: (1) a single sample can only be ≥ a minimum, since noise only ADDS — measured same-session at rung 5, single-sample reads +9.7% (lex), +10.2% (parse), +8.9% (regalloc) above the min-of-3, and per-phase exponents wobble up to ±0.5 on the SAME unchanged compiler (`pruneDeadBlockArgs` 1.91 vs 2.41, `semanticCheck` 1.85 vs 2.32, `runtimeAugment` 1.95 vs 1.11) — so read a CPU bend as a TRIGGER TO RE-MEASURE at `--repeat=3`, not as a finding; (2) the cross-repeat allocation-EQUALITY check cannot run at 1, and `--repeat=2` is the cheapest way to buy it back (codegen determinism is still gated far harder by the fragment goldens over ~1,900 spec tests, which simply do not look at allocation counts). ⚠ THE NUMBERS IN THIS ROW ARE NOT COMPARABLE TO THE ROW ABOVE IN EITHER DIRECTION: different estimator AND a different day, and this log has already been burned once by reading a cross-time-window CPU delta as code (see the 2026-07-28 correction below, where a "consistent" +8.6% collapsed to +0.94% and changed sign under interleaving). The ALLOCATION and BYTES tables are unaffected by this change and stay exactly comparable. | 843,091,940 | 1,634,004,560 | 3,077,674,720 | 5,951,773,180 | 12,896,628,100 | 30,929,067,920 |
| 2026-07-28 | P1.8 SLICE A -- INDEPENDENT REVIEW. Two reachable defects fixed (a 5th mutation door segfaulting on all four spellings of a self-field write during `for … in` over that field; a missing range-bound type guard that PANICKED the x64 emitter on a float range and silently accepted a bool one), three duplications single-sourced, 15 pinning tests. Suite 1891/0 -> 1906/0. ///// COST OF THE REVIEW ITSELF: +0 ALLOCATIONS at every rung, and BYTES -64 -112 -208 -400 -784 against the optimizer's row -- successive differences -48 -96 -192 -384, exactly doubling, i.e. linear in corpus size. That is `LoopContext.headerId` deleted: 8 bytes off every loop context, paid back by the corpus's `while` loops. The field was WRITE-ONLY (declared, constructed, written at both loop parsers, read NOWHERE) and its value coincided with `continueTargetId` for `while` and not for `for` -- the trap with the check removed. The two new guards are behind the existing `count() == 0` test and allocate nothing. ///// ***THE PREVIOUS ROW'S ONE UNATTRIBUTED READING IS NOW ATTRIBUTED, AND ITS STATED HYPOTHESIS IS REFUTED BY ITS OWN CONTROL.*** That row recorded `phase:signatures` ~+7% head-vs-parent, flat, allocations identical, and offered "most likely code layout between two different binaries", citing `phase:lex` (IDENTICAL SOURCE) as scattering +-5% in BOTH directions. Re-measured here against a THIRD, independently built binary (review-head vs `main` rebuilt in this worktree, both run from the same canonical path, 5 rungs, repeat=5): `phase:signatures` is +8.6% +10.4% +8.1% +8.4% +8.6% -- CONSISTENTLY POSITIVE at every rung. The control does NOT scatter: `phase:lex`, whose source is identical in all three binaries, reads -2.4% -2.7% -3.1% -3.1% -1.8% -- consistently NEGATIVE and three to five times smaller; `phase:merge`, also untouched, is -2.2%..+0.7%. So the binary-identity component on this pair is about -2 to -3% with a STABLE SIGN, and signatures sits ~11 points away from it in the OPPOSITE direction. It is real work, not layout. ///// WHAT IT IS: `phase:signatures` allocation COUNT is identical to the digit at every rung (14,061 / 23,816 / 43,209 / 81,854 / 158,940 both binaries) while its BYTES move +768 +1,320 +2,424 +4,632 +9,048 -- successive differences 552 1,104 2,208 4,416, exactly doubling, over a constant +216. Same count, more bytes: THE RECORDS THAT ALREADY EXISTED GOT BIGGER, and nothing new is allocated. That is the 8-byte fields the rung added (`Parser.iterationLockedBindings` -- a `Parser` is built per top-level constant by `SignatureIndex.walkConstInitializer` -- plus `LoopPhiVar.entryValue` and `LoopContext.continueTargetId`). ///// ⚠ THE BYTE GROWTH IS THE MARKER, NOT THE COST: +9,048 bytes on 7,316,360 is +0.12%, which cannot buy +8.6% of CPU. What the bytes prove is WHICH objects changed; the CPU is spread across the several constant-factor additions on that phase's paths (a wider `Parser` record and its constructor, `opensBlockAt`'s extra arm, `parseBlockBody`'s new parameter and empty-block test, `edgeArgsFor`'s selector, `fieldChainMethodCallAt` now going through `fieldChainEndAfter`). Isolating which one needs PER-FUNCTION CPU attribution, which shv2 does not have -- that is the instrument gap this reading names, and it is why "unattributed" was the honest word before and "real, unresolved between five candidates" is the honest word now. Worth ~0.4% of the compile (signatures is ~4.8% of it). NOT chased: every candidate is the price of the feature, and no single one is measurable apart from the others. ///// REJECTIONS RE-CHECKED AND UPHELD: hoisting `count() == 0` in front of `iterationLockNameAt`'s two hash lookups stands rejected -- confirmed it has exactly ONE caller (`parseFieldChainCall`, an array-typed field-chain receiver with a trailing call), so it is a narrow path and not a per-statement tax, the maximising ladder read inside the band, and the guard it would duplicate already exists one call deeper in `bindingIsIterationLocked`. Shipping it would be the unmeasurable change the previous pass correctly declined. | 775,505,140 | 1,475,359,880 | 2,852,373,100 | 5,998,970,320 | 12,991,292,180 | 32,279,652,520 |
| 2026-07-28 | P1.7a SLICE 2b-vi (multi-argument witness dispatch) -- OPTIMIZATION PASS. NO CODE CHANGE, and the measurements are why. ///// ***READ THE +148..+3062 DELTA ABOVE AS `main`'s ADVANCE SINCE THE 2026-07-28 ROW, NOT AS THIS RUNG'S.*** That was the implementer's claim and it is CONFIRMED INDEPENDENTLY, not taken: `main` @153d04620 was rebuilt from the same bootstrap in a control worktree whose path is the SAME LENGTH (33 chars, `maxon-p17a-ctrl` vs `maxon-p17a-2bvi`) so the phase:load path-length artifact is zero rather than subtracted, and its scale-test reproduces 574,570 / 1,035,171 / 1,955,167 / 3,792,298 / 7,464,503 / 14,805,346 -- IDENTICAL TO THE DIGIT to the head run, and carrying the identical +148..+3062 delta. A full text diff of both runs' per-phase ALLOCATION and BYTE tables is EMPTY: no phase, at any rung, differs by one allocation or one byte. This rung's own delta against its parent is ZERO everywhere. ///// AND THAT ZERO MEASURES **UNREACHED, NOT FREE** -- PROVEN FROM THE CORPUS, NOT ASSUMED. `--emit-corpus` over all 465 generated files: 276 `interface`, 276 `implements`, 270 `uses` -- and **ZERO `where`, at every rung**. The corpus's own comment says it: "4 unconstrained generic bases". A witness dispatch needs `where T is <Interface>`, so `Parser.parseWitnessMethodOnValue` -- every line this rung touched -- runs EXACTLY ZERO TIMES in the whole ladder. This is the trap slices 2b-ii, 2b-v and 2b-iv-B each hit; the answer again is a ladder built by hand. ///// NEW INSTRUMENT: `maxon-shv2/Testing/ladders/genwitnessargs.sh`, the ARGUMENT-COUNT axis `genwitness.sh` holds at one -- knobs conformers x methods x args x label ORDER (`decl` or `reverse`). The two orders are THE SAME PROGRAM TO THE BYTE (fixed-width labels, the literal always `1`), so subtracting them is the label SCAN and nothing else. ///// PER-CALL-SITE COST OF THE RUNG, exact and differential (same source, both binaries, `--log=compiler:debug`): **+14 allocations and +465 bytes per witness dispatch, IN `phase:parse` ONLY**, and dead flat across a 16x span -- methods 4/8/16/32/64 gave +56/+112/+224/+448/+896 allocations and +1,860/+3,720/+7,440/+14,880/+29,760 bytes, i.e. 14.00 and 465.0 per site every time. Every other phase reads delta-0 to the digit. LINEAR IN CALL SITES; nothing superlinear. ///// ATTRIBUTED, 13 of 14, BY PROBE RATHER THAN BY ARITHMETIC: a throwaway `ValueIdArray.create()`+push added per call site cost EXACTLY 2 allocations (measured +4 at 2 sites, +68 at 34, at both arity 1 and 63) => an array is 2. Six arrays are net-new (`userArgs`, `argLabels`, `argRanges`, `argImmutableNames`, plus `slotCallArgs`' `slotIds`+`filled`; `withWitnessReceiver`'s array REPLACES the old hand-rolled `args`, so it is net 0) = 12. A throwaway `ArgSlotResult.ordered(...)` cost EXACTLY 1 (+2 at 2 sites, +34 at 34, arity-independent) => the union box `slotCallArgs` returns = 1. Total 13; the residual 1 is the assumption that `BoolArray`/`SourceRangeArray` cost what `ValueIdArray` costs, which is the only thing here not separately measured. ///// ARITY IS LINEAR, NOT QUADRATIC. Isolating one call site exactly (one interface, one conformer, one method, N identical dispatch lines, so dN is a call site and nothing else): per-site allocations 36.7 / 41.6 / 52.6 / 79.9 / 126.9 / 214.2 / 375.7 at args 1/2/4/8/16/32/63 -- a clean **~31 + 5.5xA**, residuals +-7% (array growth stepping). A P^2 term would put args=63 near 830, not 376. ///// `WitnessDispatchTarget.paramNames` IS A SHARED REFERENCE, NOT A PER-SITE COPY -- settled by experiment, since shv2 has no per-scope allocation attribution outside the scale corpus and the corpus cannot express a witness dispatch (`build --mm-trace` does not exist). A SECOND, redundant `ByteArrayArray` field added to `WitnessDispatchTarget` and stored from the same source moved the per-site cost by **exactly 0 allocations** at both arity 1 and 63. A `let` field store shares the buffer. ///// `argSlotPosition`'s O(P) FAST PATH IS INTACT -- the check the refactor most endangered, since routing through `indexOfParamIn` unconditionally would restore an O(P^2)-per-call term no correctness test would notice. Re-ran the ladder its own header cites, `genmutchain.sh 1 256 <P>`, on BOTH binaries: `phase:semanticCheck` allocations and bytes IDENTICAL TO THE DIGIT at P=8/16/32/64 (6,333 / 10,975 / 19,742 / 36,749), CPU ticks head/parent 0.97 / 0.96 / 0.97 / 1.01 -- inside the noise band -- and growth x1.73 x1.85 x1.88 per doubling of P (head) against x1.75 x1.83 x1.80 (parent). Linear both. The x3.05 quadratic that header records as removed is still removed. ///// A BONUS THE RUNG DID NOT CLAIM: `Parser.transferConsumedArgs` used to re-spell the mapping inline (`i if label.isEmpty() else info.indexOfParam(label)`) and so had NO fast path; narrowing `argSlotPosition` to take a name LIST is what let it call the shared door, so the parser's consume transfer GAINED the O(P) fast path it never had. ///// THE ONE QUADRATIC THAT EXISTS IS PRE-EXISTING, SHARED, AND BOUNDED -- filed, not fixed. Fully REVERSE-ordered labels miss the fast path on every argument and fall back to `indexOfParamIn`'s scan: ~P^2/2 comparisons per call. Measured on a DIRECT-call ladder both binaries accept (64 functions, first arg positional, rest reverse-labelled), exact per-phase ticks from `--metrics`: at P=63 head 33,495,572 vs parent 33,850,842 -- head marginally FASTER, well inside noise, allocations identical -- so the rung neither introduced it nor moved it, and per-doubling it runs x2.04 x2.63 x3.05 (decl: x1.58 x1.64 x1.78). The witness path inherits precisely the same cost and no more: at P=63 the reverse-minus-decl scan is **420,906 ticks per witness call site vs the direct call's 411,140 -- 2.4% apart**, which is the cleanest available statement that 2b-vi routed witness dispatch onto the existing shared door rather than growing a second one. AND IT CANNOT GROW: **P is capped at 63 by the language itself** -- args=64 is refused `E2015: a function with 65 argument slots -- more than the 64 a call can carry` (the 64th is the receiver; the cap is the float mask's width). A quadratic in a variable the type system bounds at 64 is a CONSTANT, ~2,048 comparisons per call site, linear in program size. Not chased: the only cure is a per-signature name-to-index map, which `indexOfParamIn`'s header already rejects with its reason (a heap object on every signature, in every program, to remove a bounded term on a shape nobody writes), and it would be a real memory regression bought to win a pathological input. ///// GATES: build 0 warnings, suite 1940/0 exit 0, and `git status --short specs-shv2/fragments/` EMPTY after a full suite run -- byte-identical codegen. | 783,155,936 | 1,492,486,651 | 2,935,166,247 | 6,138,012,339 | 13,549,771,486 | 32,860,967,851 |
| 2026-07-28 | E3070 BORROW LIVENESS — a collection may not be mutated while an element borrowed out of it is live. shv2's `arr.get(i)` hands back the element POINTER and takes no reference for it, so any write that shortens or replaces the array frees a record the caller still holds. Measured 0xC0000005 before this on `clear`, `push`, a rebind `arr = other`, `grow(arr)`, a field chain `b.items.clear()`, a bare self-field alias, a method clearing its own field (`b.wipe()`, which printed 4557430888798830399 out of freed memory where the oracle prints 44), and FOUR spellings over top-level MODULE storage. Suite 1922 -> 2003/0. ///// COST, PATH-CLEAN A/B against parent 2a0dcd8d9, RUNGS 0-3: allocations +118 +199 +358 +669, bytes +32,960 +55,624 +105,096 +202,496. Delta ratios x1.69 x1.80 x1.87 converging on x2.00 against the corpus's own x1.80 x1.88 x1.93 => LINEAR, a constant factor per unit of source, +0.018% allocations and +0.071% bytes at rung 2. Bytes are two 8-byte fields making records that already existed bigger (`IrFunction.borrowFacts`, and the per-binding subject id) plus the borrow records themselves; `phase:merge` moves bytes with +0 allocations, which is that signature exactly. ///// ***READ THIS BEFORE YOU TRUST ANY DELTA THIS INSTRUMENT PRINTS: THE PATH-LENGTH ARTIFACT BIT AGAIN, AND IT PRODUCED THREE DIFFERENT ANSWERS FOR ONE CHANGE.*** Path strings are allocated, so allocation counts depend on the LENGTH OF THE DIRECTORY THE COMPILER RUNS FROM. (a) The delta compares against the row below, which was written from a 28-char worktree: on an UNCHANGED main, from the 22-char main checkout, that reads +340/+434/+622 -- the artifact alone, no code. (b) The coordinator's own head-vs-main A/B, taken from two DIFFERENT paths (22 vs 28 chars), read -93/-12/+145 -- the artifact with the sign REVERSED, which made a real cost look like a saving. (c) Both binaries run from ONE canonical path read +118/+199/+358. Only (c) is a measurement. ⇒ **AN A/B IS ONLY VALID IF BOTH BINARIES ARE EXECUTED FROM THE SAME DIRECTORY**, swapped in place; and a committed row is a baseline ONLY for runs from the path that wrote it. This row was written from the main checkout for exactly that reason. It is the same lesson the P1.8 Slice A row recorded as "+142 real plus a +14 path-length artifact" -- recorded there, and still not enough to stop it happening twice more. ///// TWO QUADRATICS FOUND AND FIXED IN-RUNG, neither filed: (1) `BorrowCheck.reportFirstLiveBorrow` restarted at the head of the subject's bucket for every write => O(sites x borrows-of-subject); a borrow dead at one site is dead at every later one (its last use is fixed once parsing ends, and sites arrive in token order), so a per-subject CURSOR steps over it once and for all -- 94.3M -> 0.10M ticks at K=1024, and the delta stops growing. (2) the pending-claim list never shrank, because `s = try arr.get(0) otherwise ""` is an ASSIGNMENT and no binding completes the borrow, so `retargetPendingBorrow`/`attachPendingBorrows` re-walked the whole list every time; now cut to a per-statement WATERMARK -- a watermark and not a reset, because `otherwise return` re-enters the statement parser mid-initializer and a reset there is a measured 0xC0000005. Both amortization arguments are ORDER arguments and both are now checked where the order is established, with a panic on violation: silently skipping a live borrow is a use-after-free nobody would be told about. Sabotage confirmed both panics fire. ///// ⛔ **THE CORPUS WAS NOT BLIND HERE -- IT WAS NARROW, WHICH READS AS COVERAGE AND IS WORSE.** `ScaleCorpus`'s `v_marray.maxon` does hold one `let mgN = try msN.get(0) otherwise ""` per managed-Array group, so `BorrowCheck` runs and returns a healthy non-zero at every rung. But every subject carries EXACTLY ONE borrow and every accessor is bound to a name, so both quadratics above have degree 1 there and CANNOT appear. A non-zero reading from a corpus that cannot express the shape is not evidence. `maxon-shv2/Testing/ladders/genborrow.sh` is committed (knobs: borrows per subject, mutation sites, distinct subjects), and it is what read the x3.8-x4.1 per doubling that the fixes deleted. ///// MEASURED DEBT, NOT FIXED: `Parser.borrowSubjectIdOf` is a linear scan of the distinct-subject list, entered once per mint and once per recorded write => O(K^2) in K distinct borrowed-and-written storages in ONE body (x3.99 x2.23 x2.66 x3.08). Fitted aK+bK^2 over the top two rungs: a=15,697, b=36.2 ticks, so the quadratic term does not equal the linear one below K~434 distinct subjects in a single function body. The corpus reaches 256 (~2.4M ticks at rung 5, ~0.02% of the compile); hand-written code reaches single digits. TRIGGER TO RE-MEASURE: machine-generated code declaring hundreds of distinct arrays in one body. Both closures would tax every program (8 bytes on every VarInfo, or a per-function Map), which is why it is filed rather than shipped. ///// A NO-BORROW CONTROL says the recording is genuinely demand-driven: a function that borrows nothing allocates ZERO extra at every K and the shared-empty `BorrowFacts` is never copy-on-written; parse CPU on a program that is nothing but array method calls is +5.2..+7.0%, spread across several constant-factor additions with no single one measurable apart from the others -- the same per-function CPU attribution gap the row below names. | 812,035,680 | 1,520,443,460 | 2,997,445,700 | 5,993,980,160 | 12,873,575,020 | 29,617,442,440 |
| 2026-07-28 | int->float PROMOTION DOORS. SEVEN doors fixed, all sharing one shape: "ask checkDeclaredType, accept ok, forget to convert". `return` PANICKED (X64Backend.maxon:715); struct-literal store, field assign, self-field assign, field default, global init and the LOCAL `var` REBIND all SILENTLY STORED THE RAW INT BIT PATTERN (~1.5e-323, coordinator-reproduced: `p.raw() == 3.0` FALSE, exit 7). ///// allocsDelta 0 AT ALL SIX RUNGS -- the conversion reuses the existing MaxonUnaryOp.promoteToFloat -> StdArithUnaryOpcode.siToFp, which already lowers on all three backends (x64 cvtsi2sd, wasm emitSiToFp, arm64 scvtf), so no new op and no target gate. ///// bytesDelta IS REAL AND FULLY ATTRIBUTED: +960/+1,632/+2,976/+5,664/+11,040/+21,792 net of the flat +206 CHECKOUT-PATH-LENGTH artifact (this worktree's path is one char longer than the logged control's; a flat non-zero delta is a PATH CHECK before it is a finding). Over the corpus's struct-field counts 60/102/186/354/690/1,362 that is **EXACTLY 16.00 BYTES PER STRUCT FIELD AT EVERY RUNG, TO THE DIGIT**. ///// CAUSE: FieldDefault.literal gained a TAG field so the widening decision reads the literal's own tag instead of resting on a SEPARATE FALSE REJECTION elsewhere (a correctness argument propped up by a bug -- removed on coordinator ruling). A Maxon union box is sized to its WIDEST ARM, so one new field costs 8 B per box; the box COUNT is unchanged, which is exactly why allocations did not move. TWO layouts are built per compile (phase:signatures' sweep and phase:parse's real parse, one StructLayout each) -- the deltas split identically to the byte between those two phases -- so it is 8.00 B/field PER LAYOUT and **16.00 B/field PER COMPILE**. ///// NOTE THE COMMIT MESSAGE `df7b91dfb` STATES 8.00 AND IS HALVED: it quoted the per-layout figure as the per-compile one. The mechanism it describes is correct; only the headline quotient is wrong. Corrected here and in PLAN.md, which are the durable records. 0.0018% of compile bytes at rung 5. Strictly linear in fields. ///// The widening ARM is unreached by the scale corpus (no int meets a float slot in it), measured instead on a hand ladder at +4.1 allocations per widened site, flat, x1.89 x1.94 x1.97 across 64->512 sites -- one extra IR op per conversion, linear. | 882,120,690 | 1,918,102,507 | 3,417,089,321 | 6,528,371,917 | 13,752,739,993 | 34,775,168,362 |
| 2026-07-28 | P1.8 SLICE C — the SEVEN byte/ASCII String methods (`startsWith` `endsWith` `contains` `toLower` `toUpper` `replace` `split`), as eight Std-tier runtime graphs plus parser dispatcher arms. Suite 2003 -> 2036/0 on its own base, 2056/0 after rebasing onto upstream. ///// ⭐⭐ **THE HEADLINE IS NOT THIS SLICE'S COST. THE LADDER BUILT TO SEE THIS SLICE FOUND A QUADRATIC IN `__str_append` THAT HAD BEEN THERE SINCE P1.2.** Exact growth (`requiredLen`) meant the buffer was full the instant it was published, so the next append reallocated and re-copied every byte written so far: `var s = ""` plus a loop of appends was O(n^2) bytes and one allocation per append. MEASURED with `Testing/ladders/genstring.sh <n> data-appendloop`: x3.94 x4.07 x3.82 x3.88 per doubling, and **5.0 SECONDS to build a 288 KB string**. Now `2 * requiredLen` -- branch-free, because `requiredLen = selfLen + otherLen >= selfLen`, so doubling it is unconditionally big enough without a `max(...)` and without a compare. After: x1.82 x2.21 x1.77 x1.94, top rung **0.98 ms — 5,118x**. Coordinator-verified end to end: 294,912 bytes built in 15 ms of total process time. Safe because `capacity@16` has exactly three readers and none treats it as the allocation's size, and Strings expose no `capacity()` to user code. The comment above that line had called geometric growth "a later optimization with no consumer here" -- the consumer was the commonest string idiom in the language. ///// ⛔ **THE CORPUS IS BLIND TO ALL SEVEN METHODS, AND THE PROOF IS AN INVENTORY, NOT AN ASSUMPTION.** `--emit-corpus` dumped, every method call across all six rungs enumerated: `create push count get append scaleBy probe byteLength slice reserve clone firstVal`. Not one of the seven. ⚠ **But it is NOT blind to `String`** -- 756 `==` sites plus `append`/`byteLength`/interpolation -- which is exactly why this slice's shared `__str_eq` change WAS visible while the seven methods themselves read Δ0. That is E3070's "narrow, not blind" trap running in REVERSE: there, a neighbour of the shape read non-zero and looked like coverage; here, the same neighbour reading non-zero could have been mistaken for coverage of the methods. **Check what the corpus CONTAINS before believing what a Δ0 means, in either direction.** `maxon-shv2/Testing/ladders/genstring.sh` is committed (knobs: call sites, segments, matches, haystack, needle-scaling). ///// COST, PATH-CLEAN A/B (both binaries swapped into the SAME slot, per the row below): **Slice C itself is +11 allocations / +295 bytes, FLAT at all six rungs** -- +15/+664 for the 15 new module-level `b"..."` globals (7 method names, 8 runtime symbols), +7/+167 `phase:isel`, -11/-472 `phase:regalloc` from the `emitByteRangeCompareLoop` critical-edge deletion. The `__str_append` growth fix costs a further flat +103 allocations / +3,569 bytes per compile. The seven methods measured LINEAR on every shape the new ladder can express: `sites-*` x1.90-x1.99 out to 2,048 call sites; `data-split` x2.04 over 16x segments (the search advances and `__arr_push` is amortized); `data-replace` x1.96 over 16x matches (the two-pass count really does size ONE allocation); `toUpper` x2.00. A String-free control A/B'd +0 allocations. ///// ⚠⚠ **THE RECORDED -181 / -8,132 THIS ROW REPLACES WAS THE PATH-LENGTH ARTIFACT, AND THE COORDINATOR READ IT WRONG IN A NEW WAY.** I argued the delta could not be the artifact because it was FLAT while the artifact "grows with rung count". It does not -- the artifact is per-compile path strings, so it is flat too. Measured: parent `d9d0f980a` from a 26-char worktree reads **-192 at every rung to the digit** against its own committed row; -192 + 11 = -181, and -8,427 + 295 = -8,132. The arithmetic closes exactly. ⇒ **"It is flat, therefore it is not the artifact" IS NOT A VALID ARGUMENT.** The only valid one remains the row below's: run both binaries from the SAME directory, swapped in place. ///// MEASURED DEBT, FILED NOT FIXED: `__str_find` is a naive search carrying a genuine O(haystack x needle) term -- `data-findquad` reads x3.92 x4.15 x3.95 x4.14. It needs BOTH a needle whose length scales with the haystack AND data self-similar enough to defeat the first-byte reject; a fixed needle is a constant factor and `data-find` reads x2.04. Measured LINEAR on every realistic shape including `split` and `replace`, its two biggest consumers. **TRIGGER TO RE-MEASURE: a consumer that searches with a needle whose length grows with its input.** The fix is two-way/KMP. ⚠ `stdlib/String.maxon`'s `findIn` -- which BOTH reference compilers run -- has the same shape, so this is not a shv2 divergence. ///// A SABOTAGE THAT DID NOT FIRE, reported rather than left implied: disabling the `Array with String` pre-intern for `.split` still passes 2035/0, because parse-time interning is idempotent and lands on the same giid. The pre-intern is DEFENSIVE, not load-bearing. Kept for consistency with `hasStringArrayLiteral`/`hasSetFromLiteral`; flagged so it is not mistaken for a checked claim. | 806,818,660 | 1,577,355,680 | 2,977,010,060 | 6,082,979,960 | 13,127,467,080 | 29,696,015,800 |
| 2026-07-28 | A FUNCTION VALUE'S DECLARED TYPE IS AUTHORITATIVE. `FunctionTypeAlias` stored only its RETURN type and threw the declared PARAMETER types away, and three silent wrong answers lived in that one gap: (1) an int literal at a `float` parameter was NOT widened at an INDIRECT call, through all three callee kinds -- an alias-typed parameter, a bare functionRef and a lifted closure (coordinator-reproduced exit 0 where 4/6/6 is right); (2) a mismatched function-to-function CAST was accepted untouched (`intish as FloatFn` read an integer back out of the float return register), as was the same disagreement through a function-typed PARAMETER and a `return` -- only the `function` TAG was ever compared, so two different function types read as one; (3) an indirect call's ARITY and argument TYPES were unchecked (`f(3)` on a two-parameter type read `b` out of whatever the register held; a String at an int parameter crashed). ///// COST: +7 ALLOCATIONS FLAT at every rung and `phase:parse` allocation-IDENTICAL to the parent, A/B'd against a parent build made and run from THIS SAME worktree path. Bytes +1,352/+1,944/+3,128, fully attributed and entirely "records that already existed got bigger", nothing new allocated per program unit: `phase:load` +264 FLAT (two new `Project` containers), `phase:lowerMaxonToStd` +24 FLAT (three registry fields on the one lowering object), and `phase:signatures`+`phase:parse` = 8 B per FileParseArtifact for its new door array, at IDENTICAL allocation counts -- an artifact is built per file AND per top-level constant, which is exactly the +768/+1,320/+2,424 doubling seen. ///// ⚠⚠ THE FIRST SHAPE OF THIS COST 240x MORE AND THE OBVIOUS ATTRIBUTION WAS WRONG TWICE. Measured +4,497/+8,666/+17,007/+33,676 allocations, x1.93..x1.99 -- linear, but 0.9% of the whole compile. Half was `SemanticCheck.checkArgTypes` rendering a parameter LABEL (`String.from` + interpolation) for EVERY argument of EVERY call before deciding whether to report anything -- now built only inside the reporting arms (`argParamLabel`), taking `phase:semanticCheck` to delta 0. The other half was a `String` place NOUN ("variable"/"global"/"field") threaded from `Parser.assignableValue` into the door recorder: ONE ALLOCATION PER ASSIGNMENT, per struct-literal field and per global store in the program, +1,852 at rung 0. ⚠ It read as the SAME +1,852 TO THE DIGIT whether the door's guard sat inside the recorder, at the caller, or with the declared type passed as a `MaxonType` instead of a `(tag, nameId)` pair -- three plausible causes, all refuted, and only deleting the whole call site moved it. What fixed it was making the noun a SCALAR (`FunctionTypeDoor` gained `variable`/`global`/`field` arms, rendered to words only where a diagnostic is built). LESSON: on a parser path that runs per statement, what a guarded call COSTS is its ARGUMENTS, not its branch -- and the only way to find which argument is to delete the site and add the pieces back. ///// The widening and the new checks are UNREACHED by the scale corpus -- `--emit-corpus` confirms ZERO function typealiases at every rung and `phase:merge` reads delta 0, i.e. not one door was ever recorded -- so the deltas above measure the STRUCTURAL cost only and say nothing about the checks themselves. ///// GATES: build 0 warnings, suite 2023 -> 2036/0 exit 0, wasm 1925/0 exit 0, fragments ADDITIONS ONLY (13 x64-windows + 13 x64-linux, no M), verify-warm-rebuild PASS. | 947,031,929 | 1,557,966,662 | 3,266,887,485 | 6,454,756,330 | 14,813,666,257 | 36,765,387,225 |
| 2026-07-28 | A FUNCTION VALUE'S DECLARED TYPE IS AUTHORITATIVE. `FunctionTypeAlias` stored only its RETURN type and threw the declared PARAMETER types away, and three silent wrong answers lived in that one gap: (1) an int literal at a `float` parameter was NOT widened at an INDIRECT call, through all three callee kinds -- an alias-typed parameter, a bare functionRef and a lifted closure (coordinator-reproduced exit 0 where 4/6/6 is right); (2) a mismatched function-to-function CAST was accepted untouched (`intish as FloatFn` read an integer back out of the float return register), as was the same disagreement through a function-typed PARAMETER and a `return` -- only the `function` TAG was ever compared, so two different function types read as one; (3) an indirect call's ARITY and argument TYPES were unchecked (`f(3)` on a two-parameter type read `b` out of whatever the register held; a String at an int parameter crashed). ///// COST: +7 ALLOCATIONS FLAT at every rung and `phase:parse` allocation-IDENTICAL to the parent, A/B'd against a parent build made and run from THIS SAME worktree path. Bytes +1,352/+1,944/+3,128, fully attributed and entirely "records that already existed got bigger", nothing new allocated per program unit: `phase:load` +264 FLAT (two new `Project` containers), `phase:lowerMaxonToStd` +24 FLAT (three registry fields on the one lowering object), and `phase:signatures`+`phase:parse` = 8 B per FileParseArtifact for its new door array, at IDENTICAL allocation counts -- an artifact is built per file AND per top-level constant, which is exactly the +768/+1,320/+2,424 doubling seen. ///// ⚠⚠ THE FIRST SHAPE OF THIS COST 240x MORE AND THE OBVIOUS ATTRIBUTION WAS WRONG TWICE. Measured +4,497/+8,666/+17,007/+33,676 allocations, x1.93..x1.99 -- linear, but 0.9% of the whole compile. Half was `SemanticCheck.checkArgTypes` rendering a parameter LABEL (`String.from` + interpolation) for EVERY argument of EVERY call before deciding whether to report anything -- now built only inside the reporting arms (`argParamLabel`), taking `phase:semanticCheck` to delta 0. The other half was a `String` place NOUN ("variable"/"global"/"field") threaded from `Parser.assignableValue` into the door recorder: ONE ALLOCATION PER ASSIGNMENT, per struct-literal field and per global store in the program, +1,852 at rung 0. ⚠ It read as the SAME +1,852 TO THE DIGIT whether the door's guard sat inside the recorder, at the caller, or with the declared type passed as a `MaxonType` instead of a `(tag, nameId)` pair -- three plausible causes, all refuted, and only deleting the whole call site moved it. What fixed it was making the noun a SCALAR (`FunctionTypeDoor` gained `variable`/`global`/`field` arms, rendered to words only where a diagnostic is built). LESSON (⚠ CORRECTED BY THE OPTIMIZATION-PASS ROW BELOW, which rebuilt all three variants on top of the shipped head and measured them): it is NOT "its arguments". An argument of the guarded call that the callee merely READS is free -- measured, delta ZERO. What a guarded call costs is OWNERSHIP TRANSFER: a MANAGED argument the callee CONSUMES forces the caller to hand over an owned copy, and that copy is emitted UNCONDITIONALLY ahead of the guard, which is why the three relocations of the guard all read the same +1,852. ///// The widening and the new checks are UNREACHED by the scale corpus -- `--emit-corpus` confirms ZERO function typealiases at every rung and `phase:merge` reads delta 0, i.e. not one door was ever recorded -- so the deltas above measure the STRUCTURAL cost only and say nothing about the checks themselves. ///// GATES: build 0 warnings, suite 2023 -> 2036/0 exit 0, wasm 1925/0 exit 0, fragments ADDITIONS ONLY (13 x64-windows + 13 x64-linux, no M), verify-warm-rebuild PASS. | 947,031,929 | 1,557,966,662 | 3,266,887,485 | 6,454,756,330 | 14,813,666,257 | 36,765,387,225 |
| 2026-07-28 | A FUNCTION VALUE'S DECLARED TYPE IS AUTHORITATIVE -- OPTIMIZATION PASS. NO CODE CHANGE, and the measurements are why. ///// THE +7 FLAT IS CONFIRMED INDEPENDENTLY, not taken. `main` @9fa71cc79 was rebuilt IN THIS SAME WORKTREE (detached HEAD, so the checkout path is identical to the head build's CHARACTER FOR CHARACTER and the phase:load path-length artifact is structurally zero rather than subtracted) and its scale-test reads 574,688 / 1,035,370 / 1,955,525 / 3,792,967 / 7,465,789 / 14,807,857 against the head run's 574,695 / 1,035,377 / 1,955,532 / 3,792,974 / 7,465,796 / 14,807,864 -- +7 AT EVERY RUNG, and the head run reproduces the logged row with delta 0. ⭐ AND THE IMPLEMENTER'S EXTRAPOLATION WAS RIGHT FOR THE RIGHT REASON, which needed the control to establish: the +125..+2,518 against the LOGGED row is +7 (this rung) plus the parent's own +118/+199/+358/+669/+1,286/+2,511 advance since the 2b-vi baseline, and that second term is now MEASURED as the control's own allocsDelta rather than fitted from four points. A doubling offset across a doubling ladder is the shape a real per-program cost has too; only an equal-path control tells them apart. ///// ⚠⚠ THE DELTA IS STRUCTURAL ONLY AND THE LADDER IS BLIND, RE-CONFIRMED FROM THE CORPUS: `--emit-corpus` over all 465 generated files finds ZERO `typealias = function(...)` and ZERO function references at every rung, so `resolveFunctionAliasShapes`, `declaredFunctionShapeOf`, `checkIndirectCall`, `checkFunctionTypeDoors` and `indirectCalleeParamTypes` run EXACTLY ZERO TIMES in the whole ladder. ///// NEW INSTRUMENT: `maxon-shv2/Testing/ladders/genfnval.sh <aliases> <sites> <arity> <indirect / direct>` -- the axis the corpus cannot express, with `direct` as the same-shape control (same alias declarations, same callee bodies, same CALL COUNT, by NAME, so no function value, no indirect call and no door exists in it). ⚠ Its two modes are NOT byte-identical and cannot be: a direct call's 2nd+ arguments MUST be labelled (E2053) and an indirect call's CANNOT be, so read it as an upper bound on the machinery and read RATIOS within a mode. ///// ⭐ RESULT: NOTHING ON THIS RUNG'S PATHS IS SUPERLINEAR, and it is measured rather than argued. Doubling aliases (and with them call sites and doors) 64 -> 512, HEAD MINUS PARENT on the identical source, allocations: total +2,006 / +3,934 / +7,784 / +15,474 = x1.96 x1.98 x1.99. Per phase, all x1.96..x2.00: resolveTypes +609/+1,193/+2,353/+4,665 (9.1 allocations per function typealias, and IDENTICAL in `direct` mode -- it is the DECLARATION cost, which is exactly what the control was built to prove); semanticCheck +768/+1,536/+3,072/+6,144 (x2.00 to the digit, 12 per alias over 3 indirect calls + 2 doors); lowerMaxonToStd +192/+384/+768/+1,536 (x2.00, ONE union box per indirect call in `indirectCalleeParamTypes`); parse +330/+651/+1,292/+2,573. Subtracting the control leaves the CALL+DOOR machinery alone at +1,035/+2,061/+4,111/+8,209 = x1.99 x1.99 x2.00. CPU ticks (min of 3) read x1.93..x2.06 on every phase of both modes. `declaredFunctionShapeOf` is TWO HASH-MAP GETS, not a scan, so the `sites x aliases` quadratic the hunt was for would have read x4.00 on this ladder and reads x2.00. ///// ⚠⚠ THE RECORDED LESSON OF THE ROW ABOVE IS CORRECTED HERE -- it was directionally right and MIS-STATED, and the mis-statement is the actionable part. Reconstructed the blamed shape on top of the shipped head and measured three variants at rung 0: (V1) the `String` noun restored as an argument of `assignableValue` and consumed by `requireAssignable` -- i.e. the PARENT'S OWN SHAPE, which is the point, since the parent already paid for that literal at every one of those call sites -- reads 574,695, delta ZERO; (V3) the noun ALSO passed as an argument of the GUARDED recorder call but only READ there -- 574,695, delta ZERO; (V2) the same argument, CONSUMED (stored on the `PendingFunctionTypeDoor` record) -- 576,547, +1,852 in phase:parse, THE LOGGED NUMBER TO THE DIGIT. So it is not "its arguments": V3 puts the argument on the guarded call for free. ⭐ THE RULE IS OWNERSHIP TRANSFER: a MANAGED argument that the guarded callee CONSUMES forces the caller to hand over an owned copy, and that copy is emitted UNCONDITIONALLY, ahead of the guard -- so a branch taken at essentially no call site still pays once per call for every managed argument it would have consumed. That also explains the three "refuted" causes without any further hypothesis: moving the guard inside the recorder, moving it to the caller, and changing the declared type's representation all leave the CONSUMED String argument exactly where it was, which is why all three read +1,852 to the digit. The corpus records ZERO doors, so not one of those 1,852 copies was ever used. ///// GATES: build 0 warnings; suite 2036/0 exit 0; wasm 1925/0 exit 0; fragments byte-identical after both suites; verify-warm-rebuild PASS (run on a genfnval program, so the doors list is exercised across a warm re-merge -- it is reset in `resetMergeTargets`, so the E3070-shaped "pending list that never shrank" is NOT present here). ///// ⛔ ONE PRE-EXISTING DEFECT FOUND AND REPORTED, NOT WORKED AROUND: an indirect call of arity >= 14 panics the register allocator (`RegisterAllocator.maxon:1173 chooseRegister: no free register`). REPRODUCES IDENTICALLY ON `main` @9fa71cc79 -- arity 13 compiles, 14 panics, on both binaries -- so it is not this rung's; the same arity 16 through a DIRECT call compiles clean. It caps genfnval's arity knob at 13. | 787,065,825 | 1,691,490,271 | 3,084,287,235 | 6,427,136,370 | 14,035,450,946 | 34,225,757,232 |
| 2026-07-28 | A FUNCTION VALUE'S DECLARED TYPE IS AUTHORITATIVE -- INDEPENDENT REVIEW. ONE BLOCKER FIXED, plus the duplication it exposed. ///// THE BLOCKER WAS A FALSE REJECT, and the shape the rung's own comment said it had no case for: `resolvedTypesMatch` compared a NESTED function type (a function type's function-typed PARAMETER) by NAME id while comparing everything else RESOLVED, so `typealias Outer = function(f InnerA) returns Integer` REFUSED a function declared `(f InnerB)` even when InnerA and InnerB resolve identically. The bootstrap ORACLE compiles that program and runs it (exit 42), the parent shv2 @9fa71cc79 compiles and runs it (42), and head refused it -- with a diagnostic that read `expected 'fn(function) returns int', got 'fn(function) returns int'`, i.e. a type failing to match itself, because `maxonTypeName` rendered any function type as the bare word `function`. FIXED both halves: the nested type now DESCENDS (`nestedFunctionTypesAgree`), which is the same rule one level down and is required for soundness in the other direction too -- if the nested types genuinely differ, the callee calls its own parameter through the OTHER signature and the wrong answer lands one level in; and `maxonTypeName` now renders a `function` tag by its alias name, so the mismatch reads `expected 'fn(InnerA) returns int', got 'fn(InnerB) returns int'`. ///// THE DESCENT NEEDS A CYCLE GUARD AND HAS ONE, because shv2 ACCEPTS a self-referential function alias (`typealias SelfFn = function(f SelfFn)` compiles on head AND on the parent; the bootstrap refuses it as E2012) and two aliases may name each other. Guard is coinductive on the (declared, got) NAME PAIR: a pair already under comparison AGREES, which is the only answer a recursive type has and cannot false-reject. Verified terminating and correct on four hand-built cycles -- self-referential, mutually recursive A/B accepted, structurally-equal recursive A vs B accepted, recursive A vs B with a differing return REFUSED. ///// COST: +1 ALLOCATION FLAT AND +16 BYTES FLAT at every one of the six rungs, against the row above. It is ONE object for the whole compile -- `sharedEmptyFunctionTypePairs`, the empty pending list every top-level comparison starts from, never mutated (`pendingWithPair` builds a fresh array) -- the same pay-for-use discipline `sharedEmptyBorrowFacts` carries. NOTHING is per-program-unit: the `declaredId == gotId` short-circuit means an ordinary nested parameter (same alias both sides) allocates nothing and touches no registry, and a descent only happens when the two names actually differ. Corpus is still blind to this whole axis (zero function typealiases in all 465 generated files), so the flat +1 is the structural cost and nothing else. ///// DUPLICATION REMOVED, which is what made the fix cheap rather than expensive: `declaredFunctionShapeOf` took its three registries as three loose parameters and the id->name interner was a FOURTH fact carried separately, so SemanticCheck and LowerMaxonToStd each held a private copy of `typeNames.nameOf(id) or panic` and a private copy of `declaredFunctionShapeOf(...) or panic`. That is 3 copies of each once the descent needed both. Collapsed to ONE `FunctionTypeRegistry` carrier plus ONE `functionTypeNameOf` and ONE `requireFunctionShapeOf`, both exported from TypeResolution; the lowering now holds one field where it held four. Also deleted `hasDeclaredFunctionShape`, which had NO callers. ///// SPECS: 2 authored cases pinning the rule in both directions (`nested-function-type-agrees-by-shape` accepted, `error.nested-function-type-mismatch` refused with the readable message). ///// ⛔ SECOND PRE-EXISTING DEFECT FOUND AND REPORTED, NOT WORKED AROUND: on wasm, passing a function value into a parameter whose function TYPE has a function-typed parameter emits a module that fails validation (`type mismatch: expected i64 but nothing on stack`). Narrowed to the nested function TYPE alone -- no indirect call anywhere, the SAME alias on both sides -- and it reproduces byte-for-byte on `main` @9fa71cc79, so it is neither this rung's nor newly reachable. It is why the accepted case above lists x64 targets only. ///// GATES: build 0 warnings; suite 2036 -> 2038/0 exit 0; wasm 1925 -> 1926/0 exit 0; fragments additions-only (2 new x64-windows, no M); verify-warm-rebuild PASS on a genfnval program AND on the nested-alias case. | 791,692,287 | 1,556,262,787 | 2,959,007,591 | 6,229,491,150 | 14,142,156,024 | 34,658,888,330 |
| 2026-07-28 | P1.8 SLICE B — `Character`, UAX#29 GRAPHEME SEGMENTATION, `String.count()`, `for c in s`. A real `ValueTypeTag.character` (user ruling), a new `GraphemeRuntime` family, and the per-trip owned-Character frame. Suite 2056 -> 2114/0 on its own base, **2129/0** after rebasing onto upstream. x64-windows / x64-linux / wasm32-wasi all PASS; arm64 SKIP. ///// COST, PATH-CLEAN A/B (both binaries swapped into the SAME slot, interleaved): **+0 ALLOCATIONS and +64 BYTES, FLAT at every rung** — eight new `bool` fields on ONE `RuntimeUsage` record per compile. Confirmed two ways: the no-Character control read +0 allocs / +64 bytes / no measurable CPU (4.419 s vs 4.426 s, head faster in 2 of 4 interleaved trials), and the arithmetic closes against the committed row — slice C's artifact is -8,427, this tree reads -8,363, and **-8,427 + 64 = -8,363 exactly**. Compile growth x1.85-x1.99 in allocations AND CPU out to 1,024 call sites across all five ladder modes; run growth x2 per doubling on every mode. **Nothing superlinear.** ///// ⛔ **THE CORPUS IS BLIND TO EVERY PART OF THIS RUNG, BY INVENTORY.** `--emit-corpus` at rung 5: `.count()` appears **1,024 times and every one is an Array/Set receiver** — not one String; `.bytes()` **0**; **`for … in` in any form: 0**; `'…'` tokens **53,766, of which 0 are in a value position** (every one is a block label). ⚠ **But it is NOT blind to `String`** (756 `==` sites), which is the trap in reverse: a neighbour of the shape reads non-zero and can be mistaken for coverage. **Check what the corpus CONTAINS before believing what a Δ0 means, in either direction.** `maxon-shv2/Testing/ladders/genstring-grapheme.sh` is committed (knobs: call sites, segments, cluster width, ASCII/2-byte/ZWJ/flag seeds, countloop). ⚠⚠ **AND NOTE `for … in` IS STILL ZERO — P1.8 Slice A committed `genfor.sh` for exactly that hole and the MAIN corpus was never taught the construct.** Four consecutive rungs have now each built a private ladder because the shared instrument cannot express what they added; that is an instrument problem, not four coincidences. ///// ⭐⭐ **THE FINDING IS NOT A CURVE, IT IS THE ALLOCATOR: `for c in s` MINTS ONE Character PER TRIP AND THE SLAB NEVER GIVES IT BACK.** `MmRuntime` is a bump allocator with no free list, so memory is linear in **ITERATIONS, not live data**: 65,536 clusters -> 11.6 MB, 262,144 -> 97.1 MB, **1,048,576 -> 398.7 MB**, at ~82 bytes per minted Character. **Two controls make it unambiguous**: `data-count-ascii` runs the SAME `__gr_end` scan minting nothing and is **FLAT at 3.7 MB across a 4x data range**, and the C# bootstrap runs the same source in the same time (155.7 vs 156.4 ms) at **7.3 MB**. It is NOT a leak — every allocation is freed and no run exits 101 — the slab simply never reuses a slot. **FILED, not fixed**: the cure is a recycling `__mm_free`, which `MmRuntime.maxon:738` already anticipates in its own comment, and that is a runtime rung. **TRIGGER TO RE-MEASURE: any construct that allocates once per loop trip — `for c in s` is the FIRST one the language has, which is precisely why this debt only became visible now.** ///// Also measured: `for c in s` costs 17x what `count()` costs per cluster and ALL of it is `__char_at` (one alloc+blit+free per trip, ~54 ns, CONSTANT in the segmentation work — 54.1 ASCII / 56.2 two-byte / 83.6 for a 25-byte cluster), so the GB state machine hides no per-trip cost. shv2's `count()` is **2.07x FASTER than the bootstrap's** on the identical O(n) algorithm (16.8 ms vs 34.8 ms over 1,048,576 clusters). `data-countloop` is x3.79-x4.05 — quadratic BY DESIGN, in user code, exactly as `countGraphemes` is O(n) per call in both compilers. ///// ⚠⚠ **READ THE PRINTED DELTA ON THIS ROW AS A SPAN, NOT AS THIS RUNG'S COST.** The instrument compares against the row BELOW, which was written by the upstream function-value rung from a different tree; between the two rows THREE things landed — that rung, the BOOTSTRAP ORACLE fixes, and this slice. So the printed +5,836 / +9,043 / +15,459 / +28,289 / +53,951 / +105,273 is the sum of all three plus a path-length component, **not** Slice B. Slice B's own cost is the path-clean interleaved A/B above: **+0 allocations, +64 bytes, flat**. ⚠ The span IS linear — successive ratios x1.55 x1.71 x1.83 x1.91 x1.95, converging on x2.00, ~+0.7% of allocations — so there is no superlinearity in it; but its ATTRIBUTION between the three landings is **OWED, not claimed**. The likeliest single contributor is that the oracle rung changed `maxon-sharp` CODEGEN, and shv2's binary is PRODUCED BY the bootstrap, so a bootstrap change moves shv2's own allocation behaviour — but that is a hypothesis, and this project's rule is that a story which fits is not an attribution. **Whoever next needs this number should measure it path-clean against `cdcab0969`.** ///// REJECTED WITH MEASUREMENTS: eliding the `__char_at` mint for `for _ in s` (sound, worth ~14x, but it fixes only the discard and would make `character-ownership.md`'s `discardTrips` case — written to pin that a discarded cluster is still dropped — vacuous); and an immortal 256-entry `.rdata` Character table for one-byte ASCII clusters (would remove ~94% of ASCII iteration cost, but `__str_decref` would have to learn to tolerate an rdata record, which is a Character-REPRESENTATION decision, not an optimizer's). | 800,829,860 | 1,497,472,840 | 2,949,710,480 | 5,996,580,880 | 12,971,638,960 | 29,997,977,100 |
| 2026-07-28 | PARSE SUPERLINEARITY FOUND AND FIXED — `Parser.assignedBindingsIn` was Θ(vars in scope × merging constructs), and it ALLOCATED NOTHING. `phase:parse` was the only superlinear phase on the ladder: CPU x2.02 x2.21 x2.52 x2.90 x3.33 down the six rungs while its allocations held at x1.85-x1.99, and its share of compile CPU climbed 15.3% -> 36.7% while regalloc's fell — on that trajectory it overtakes the register allocator at the next rung. ///// THE BEND, NAMED: every `if` `match` `while` `for` asked for the mutable bindings its token extent assigns, and the answer was computed by walking the WHOLE `mutableVars` list and probing each binding's name against a per-construct `Set`. One hash of a `ByteArray` name per binding in scope per construct. Instrumented on the corpus: **39,400,068 probes at rung 5 across 18,571 constructs, x3.99 per doubling — and the carried set they were selecting was never larger than TWO (maxA = 2, sumA = 27,180).** The scale corpus doubles both axes at once (`LocalsPerFunctionBase` 128<<r and `LongIfsBase`/`DeepBlocksBase`), so the product quadrupled per rung while source bytes doubled. ///// THE LADDER THAT SEPARATED THE AXES: `maxon-shv2/Testing/ladders/genscope.sh <constructs> <locals> (if ifelse while match straight)` (COMMITTED, indexed in that directory's README). C alone x1.72 x1.86 x1.89; V alone x1.46 x1.57 x1.83; BOTH x2.21 x2.45 x2.83; and the `straight` CONTROL — same V, same statement count, no merging construct — x1.80 x1.90 x1.89. Linear in each knob, quadratic in the two together, flat without the construct: that is an O(V x C) term and it belongs to the construct, not to program size. Confirmed before the fix was designed by capping the walk at 8 entries, which took the bend to x1.88 x1.94 x1.94 with allocations UNCHANGED to the digit. ///// THE FIX: invert the lookup. The parser now carries an index of `mutableVars` — `mutableVarInnermost` (name -> innermost position) plus `mutableVarShadowed` (per position, the position it shadows), pushed/popped in exact lockstep with the stack under the same `openBlock` mark. `assignedBindingsIn` now scans the extent's tokens, asks the index per ASSIGNED NAME, and collects positions into an ASCENDING list — ascending position IS `mutableVars` declaration order, which is the order the phis must be minted in, so no sort and no reordering. `namesAssignedIn` and its per-construct `Set` are gone; `mintLoopHeaderPhis` takes the same carried list, so the two orderings that had to agree are now ONE. The shadow chain is what keeps this EXACT: it yields every entry a name hides, which is precisely what the old walk found. ///// RESULT, path-clean A/B (parent `3f2e3203f` and the fix built in the SAME worktree at the SAME path, min-of-3, machine idle): `phase:parse` CPU x1.89 x1.92 x2.00 x2.06 x1.98 — LINEAR. Rung 5 parse CPU **13,160,151,600 -> 3,354,949,049 (-74.5%, 3.92x)**; whole-compile CPU **35.87G -> 24.78G ticks (-30.9%)**; parse's share of compile CPU now reads 14.5% -> 13.5% ACROSS the ladder instead of climbing to 36.7%. Same-binary CONTROL (baseline run twice): memory bit-identical at every rung, CPU spread -5.5%..+0.5% — so the noise band is ~6% and a 3.9x is not in it. ///// MEMORY, and it is fully attributed — EXACTLY TWO PHASES MOVED. `phase:parse` -4,060 .. -143,487 allocations and -277,848 .. -9,493,072 bytes: that is the per-construct `AssignedNameSet` (and its internal columns) no longer being built, ~7.7 allocations per construct. `phase:signatures` **+0 allocations** and +1,536 .. +35,760 bytes: the `Parser` RECORD grew 16 bytes (two new pointer fields), and 1,536/16 = 96 = the 30 files plus 66 top-level constants that phase builds a `Parser` for at rung 0. The two structures themselves cost nothing there because they start at shared empties and are copy-on-written by the first `var` push (`sharedEmptyIterationLocks`'s pattern) — an unconditional `create()` pair measured +13,410 allocations / +590,040 bytes in `phase:signatures` at rung 5, a phase this change has no business touching, and that is why the COW is there. Totals close exactly: -9,493,072 + 35,760 = -9,457,312. ///// ⚠ WHAT IS STILL THERE: the token scan itself still walks token-by-token, so a construct nested D deep still has its tokens re-read by all D enclosing scans — the `LoopNestBase` knob's cost, isolated by `gennest.sh`, UNTOUCHED by this change. The old note in `blockStatementEndIndex` that inverting that scan costs `phase:parse` x3.5 was measured when its per-test constant was multiplied by the Θ(V) walk this row removes; that measurement no longer stands either way and the question is open again. ///// ⚠ AND ONE MEASURED BOUND ON THE NEW CODE: the ordered insert is O(A^2) in shifts where the old walk was O(V) in hash probes, so a construct assigning EVERY binding in scope is its worst case — a shape `genscope.sh` deliberately does not generate. Measured by hand at A = V = 250/500/1000/2000 (one wide `if`) and at 8 wide `if`s with V = 125..1000: both stay LINEAR (x1.95-x2.03) and cost +1%..+4%, because a 4-byte shift is ~30x cheaper than the `ByteArray` hash probe it replaced. RE-MEASURE TRIGGER: a rung that makes wide carried sets ordinary (a macro expander, machine-generated bodies). ///// ⚠ READ THE PRINTED DELTA AS A SPAN, NOT AS THIS CHANGE'S COST. The instrument compares against the row below, which was recorded in a DIFFERENT tree, so it prints -4,335 .. -146,056 allocations where this change's own, path-clean A/B is -4,060 .. -143,487. The ~275 .. ~2,569 difference is that tree, not this change, and the attribution above is the A/B rather than the printed delta. ///// GATES: shv2 x64 **2142/0**, wasm32-wasi **2029/0**, `verify-warm-rebuild` PASS, no 101, and `specs-shv2/fragments/` EMPTY after both full runs — byte-identical codegen, which is the correctness proof this change needed most, since the carried set's ORDER decides phi order. | 783,302,433 | 1,487,852,184 | 2,894,909,706 | 5,831,082,432 | 11,687,536,766 | 24,441,340,509 |
| 2026-07-28 | P1.8 SLICE B2 — `StringIndex` + `startIndex`/`endIndex`/`findFirst`/`findLast`/`slice`, closing Slice B's sanctioned residual. Suite 2142 -> 2178/0; x64-linux PASS, wasm32-wasi PASS, arm64 SKIP (remote, opt-in). Build 0 warnings; specs-shv2/fragments EMPTY after a full unfiltered run, which is what proves the codegen byte-identical. ///// NO OPTIMIZER TRIGGER FIRED, and each of the three conditions was checked rather than waved at: the rung adds no pass and no IR op (it adds Std-graph runtime entry points and one synthesized struct layout); the ALLOCATION ladder is x1.80 x1.89 x1.94 x1.97 x1.98, converging on x2.00 with nothing anywhere near the ~x2.4 threshold; and the one Workstream-O re-measure trigger it touches is discussed below. ///// READ THIS ROW'S DELTA COLUMN AS A PATH ARTIFACT, NOT AS A SAVING. It prints -275 -349 -497 -793 -1,385 -2,569 allocations and -8,693 -9,877 -12,245 -16,981 -26,453 -45,397 bytes — i.e. NEGATIVE at every rung, growing with rung. A rung that only ADDS a feature does not make the compiler allocate less; this is the checkout-path term this log has now recorded three times (see the 2026-07-27 P1.7-Slice-1 row, which proved it by running the PARENT's binary from the worktree path and reproducing the child's numbers to the digit). This tree's root `maxon-p18b2` differs in length from the main checkout's, and the previous row was written from a different tree again. ⚠ **THIS RUNG'S OWN COST IS THEREFORE NOT ESTABLISHED BY THIS ROW, AND IS OWED, NOT CLAIMED** — a path-clean interleaved A/B against `3f2e3203f`, both binaries swapped into ONE canonical path, is what would settle it. Recording that it is unmeasured rather than reading the printed delta as a result. ///// THE CORPUS IS BLIND TO EVERY CONSTRUCT THIS RUNG ADDS, BY INVENTORY, so a Δ0 from it would measure the instrument and not the cost. Slice B's row established the inventory at rung 5 with `--emit-corpus`: `.count()` appears 1,024 times and EVERY receiver is an Array or Set — not one String — and `.bytes()` is 0. B2 adds only String-receiver methods (`startIndex`/`endIndex`/`findFirst`/`findLast`/`slice`), so the corpus cannot reach a single line of the new parser dispatch or any of the new runtime graphs. That is the fifth consecutive rung whose feature the shared corpus cannot express; four of them have now each committed a private ladder instead. It is an instrument problem, not five coincidences. ///// phase:parse CPU reads x2.04 x2.21 x2.36 x2.67 x3.08 while its ALLOCATIONS stay dead linear at x1.85 x1.92 x1.96 x1.98 x1.99. That divergence is exactly the allocate-nothing shape the CPU column was added to catch — and it is NOT this rung's and NOT new: it is the already-filed Theta(mutable bindings x constructs) debt (`d_blocks.maxon` is ONE function with 518 mutable vars in scope, and both callers ask a membership question once per binding per construct — 26.5 MILLION tests at rung 5). The 2026-07-26 encoder row filed it, and the 2026-07-27 P1.7 row re-measured it at x3.26 on BASE AND HEAD ALIKE. This row's x3.08 sits inside that, on a corpus that cannot reach B2's code at all. ⚠ SUPERSEDED WITHIN HOURS, AND THE ROW ABOVE IS THE CURE: upstream `88ca97b33` identified `Parser.assignedBindingsIn` as the Θ(vars in scope × constructs) term and FIXED it while this rung sat in its worktree. So this row is a measurement of the PRE-FIX compiler — correct when taken, stale by the time it merged. Kept rather than silently re-measured, because a trend table is read downwards and this is the last datapoint before the cure. ///// ⭐ A SECOND CONSTRUCT NOW HITS SLICE B's NAMED SLAB TRIGGER, and this is the one genuinely new observation. Slice B filed the `MmRuntime` bump-allocator debt (no free list ⇒ memory linear in ITERATIONS, not live data; 1,048,576 minted Characters -> 398.7 MB) and wrote its re-measure trigger as "any construct that allocates once per loop trip — `for c in s` is the FIRST one the language has". **`slice` in a loop is the SECOND**: every trip mints a fresh owned String, because slice COPIES rather than viewing. Probed at 200 trips: correct, exit 0, no 101 — it is not a leak, the slab simply never reuses the slot, exactly as filed. The debt and its cure (a recycling `__mm_free`, which `MmRuntime.maxon:738` already anticipates in its own comment) are UNCHANGED; what changes is that the trigger has fired a second time from an independent direction, which is evidence the runtime rung is worth sequencing rather than a new finding. ///// ⭐⭐ THE REVIEW FOUND A REACHABLE COMPILER CRASH THE GREEN SUITE WAS STRUCTURALLY BLIND TO, and the blindness is the transferable part: `registerStringIndexType`'s only caller sat inside `foldDeclaredSignaturesInto`, a DECLARATION walk whose arms hand a whole `type` to a `recordScanned*` routine and resume past its `end` — so tokens inside method bodies never reach it. A `StringIndex` producer inside a `type` method left the compiler-owned `StructLayout` unregistered and died two ways (field read -> `stringIndexLayoutOrPanic`; scope-exit drop -> `managedNameDropCallee`). All 2,177 tests spelled the feature in a free-standing function — THE ONLY SHAPE THAT LOOP CAN SEE — and two comments asserted the opposite, including the loop's own "walks EVERY token in the file, function bodies included". Cured by DERIVE (its own complete token walk) and pinned RED against the parent. Sabotage: end-exclusive->inclusive 9 red; byte->grapheme->byte position 2 red; not-found throw->sentinel 2 red; `StringIndex` box drop removed 19 red, ALL exit 101. | 817,859,180 | 1,544,027,400 | 3,101,444,100 | 6,320,644,880 | 13,705,697,500 | 31,945,167,400 |
| 2026-07-29 | P1.7a FINAL RESIDUAL — `float` conforms to `Hashable`, via a Std-tier BIT REINTERPRETATION (`StdUnaryOpcode.bitcastF64ToI64`; x64 `movq r64,xmm`, arm64 `FMOV Xd,Dn`, wasm `i64.reinterpret_f64`). Suite 2178 -> 2185/0, exit 0, no 101. x64-linux PASS, wasm32-wasi PASS, arm64-macos/arm64-linux SKIP (remote, opt-in, UNVERIFIED — not proven good). Build 0 warnings; specs-shv2/fragments 14 ADDITIONS and ZERO `M`, which is what proves the codegen byte-identical for all 2178 pre-existing programs. ///// ⚠⚠ READ THE PRINTED DELTA COLUMN AS `main`'s ADVANCE, NOT AS THIS RUNG — AND THIS TIME IT WAS REFUTED BY MEASUREMENT RATHER THAN FILED. The instrument prints -4,052 -8,518 -17,491 -35,468 -71,461 -143,479 allocations against the stored baseline: NEGATIVE and GROWING with rung, ~1% at rung 5. A rung that only ADDS a feature cannot make the compiler allocate proportionally less. The stored baseline is a PREVIOUS LOG ROW, not this rung's parent, and `main` advanced between them (0c6ca2923, 88ca97b33, 73884cbbf, 71cd8479b). Settled the way slice 2b-vi settled its own +148..+3062: the PARENT (71cd8479b) was built in a control worktree at an EXACTLY EQUAL-LENGTH path (`maxon-ctrl` vs `maxon-p17a`, both 28 chars) so the `phase:load` 202 B/char term CANCELS rather than being argued away. ///// ⭐ THIS RUNG'S OWN FOOTPRINT IS +8 ALLOCATIONS / +510 BYTES PER COMPILE, DEAD FLAT AT ALL SIX RUNGS (a 32x span), and attributed to NO phase — `installBuiltinConformanceRuntime` is called from `Compiler.maxon:452`, outside every `CompilePhase` bracket. Attributed BY PROBE, not by arithmetic: a temporary tenth never-referenced candidate added to the install roster measured +7 allocs / +312 bytes, equally flat, so the +8/+510 is one candidate (`float.hash`) plus the array growth its arrival crossed. Probe reverted. ///// THE OPTIMIZER AGENT TRIGGER FIRED (the rung adds an IR op) and the ladder shows no bend: allocations x1.797 x1.886 x1.938 x1.967 x1.983, converging on x2.00, nothing near the ~x2.4 threshold. NO COMPILER CODE CHANGED. ///// THE CORPUS IS BLIND TO THIS RUNG BY CONSTRUCTION, so its Delta0 would have measured the instrument: `ScaleCorpus` emits ZERO float `.hash()` calls, and the install gate is REFERENCED-but-undefined <=> INSTALLED, so `buildFloatHash` is never built and the new TargetOp never emitted anywhere on the ladder. That is the sixth-plus consecutive rung with this problem. Private ladder built: `maxon-shv2/Testing/ladders/genfloathash.sh` (4 modes), whose PROOF OF REACH is the strongest available kind — the PARENT compiler REFUSES its `hash` file outright (E2015: 'float' has no method named 'hash'). Per-call-site cost measured FLAT: hash 595.4 / 594.1 / 593.8 allocations per site against control 595.7 / 594.6 / 594.1 — a `f.hash()` site costs what a float comparison costs. ///// A NEW WIDEST `TargetOp` VARIANT WOULD HAVE TAXED EVERY `TargetOp` ALLOCATION (slice 2b-v's standing rule, 8 B per field): it did not — the widest variant is unchanged at 4 fields (`x64JumpTable`, `arm64Msub`) and both new ops carry 2. Verified by measurement, not by reading: the controls stay flat at +8/+510 on programs holding thousands of TargetOps, where a widened union would climb with program size. All new arms in TargetLiveness / SsaDestruction / SplitLiveRanges / RegisterAllocator are O(1). ///// ⭐⭐ MEASURED DEBT -> WORKSTREAM O, AND THE EXISTING TRIGGER WAS ON THE WRONG AXIS. Admitting `float` to `Hashable` costs +10 ALLOCATIONS / +344 BYTES PER FLOAT `.equals()`/`.compare()` CALL SITE — exact at all four sizes (10 x sites + 8), linear, controls flat. Nothing in `float.equals`'s own path changed; its POSITION IN A SEARCH ORDER did. `builtinConformerMethod` (IrInterface.maxon:691) walks Hashable -> Equatable -> Comparable and `continue`s past non-conformers; float's row now STOPS at Hashable, so a whole `IrInterface` is synthesized, searched for `equals`, missed, and discarded before Equatable is ever reached. FILED, NOT FIXED — linear in sites is a constant factor, and the review traced the cure to confirm it is not a ride-along: `builtinInterface` returns `InterfaceLookup.found(IrInterface)` BY VALUE and `IrInterface` owns three arrays, so memoizing it either clones per call (re-paying most of the 10) or returns a BORROW of a compiler-lifetime cache, which changes the return type and every consumer and introduces exactly the long-lived-borrow shape E3070's liveness pass now polices. The cheap alternative — asking "does this protocol declare this method?" without building the interface — is forbidden by the design's own thesis, since it re-introduces the second roster `builtinConformerMethod` exists to eliminate. ⚠ THE OLD RE-MEASURE TRIGGER READ "a fourth builtin-conformable protocol". IT DID NOT FIRE — `builtinConformableProtocolNames` still returns exactly three — YET THE DEBT GOT WORSE ANYWAY, because the cost is (protocols x conformance-ROW WIDTH) and this rung widened a ROW. REPLACEMENT TRIGGER: any rung that adds a (type, protocol) PAIR to `isIntrinsicBuiltinConformance`, or a fourth protocol. A re-measure trigger that cannot fire for the thing that actually worsens the cost is this project's signature bug wearing an instrument's clothes. ///// ⭐ THE PLAN'S OWN PREMISES WERE MEASURED FALSE BEFORE THE WAVE, TWICE. PLAN.md listed TWO residuals for P1.7a; `2b-iv-C` (arm64 witness lowering) had already landed six commits earlier in 227fb1051 (coordinator-confirmed by disassembling a cross-compiled witness binary: `ldr x16, [x1, #0x18]` + `blr x16`, slot 24 = first method). And `Box with float` under a `where` constraint is a clean E2062 refusal, not the compile-time panic the row recorded — which is why `float.hash` is DIRECT-dispatch only and this rung made no crash reachable. ///// ⭐⭐ A REVIEW SABOTAGE RESULT WORTH KEEPING, ABOUT TEST DESIGN RATHER THAN THIS RUNG: injecting the exact ModR/M swap `encodeMovqGprXmm`'s comment warns of turned RED only the THREE cases that PIN A NUMBER (`chained`, `negative`, `nan`). The three that assert RELATIONS (`nonzero`, `zero-normalization`, `equal-values-equal-hashes`) all stayed GREEN, because both sides of the relation were wrong identically. A relational assertion cannot catch a bug that moves both operands. | 827,986,940 | 1,477,371,220 | 2,996,740,420 | 6,086,098,620 | 12,068,623,680 | 24,612,748,580 |
| 2026-07-29 | P1.8 SLICE D OPTIMIZATION PASS — `CharacterSet` + UCD `General_Category` + the three trims. ///// ⚠⚠ READ THE DELTA COLUMN AS A PATH ARTIFACT AGAIN — AND THEN IGNORE IT, BECAUSE THIS ROW REPLACES IT WITH A REAL MEASUREMENT. The printed delta is -4,046 … -143,473 allocations, negative and growing with rung, against a previous row written from the `maxon-p18b2` tree (11 chars) while this one is `maxon-p18d` (10). That is the checkout-path term this log has now recorded FOUR times, and the B2 row explicitly recorded its own rung cost as "owed, not claimed". ⭐ IT IS NO LONGER OWED. Paid here by a path-clean A/B: the parent `71cd8479b` checked out to `C:\Users\Eric\dev\maxon-base` — EXACTLY as long as `C:\Users\Eric\dev\maxon-p18d`, so the path term cancels — and built by the BYTE-IDENTICAL bootstrap (md5 9375432ee32201bf81263d4d9212c3e8 in both trees). THE WHOLE OF SLICE D COSTS THE SHARED CORPUS +14 ALLOCATIONS, FLAT AT ALL SIX RUNGS (a 32x span), and bytes of +1,352 +1,864 +2,888 +4,936 +9,032 +17,224 — which DOUBLE exactly, i.e. 16 bytes per FUNCTION plus ~840. Growth ratios are IDENTICAL on both binaries (x1.80 x1.89 x1.94 x1.97 x1.98), so the rung changes no curve anywhere. The +16/function is `phase:parse` with ZERO extra allocations — an existing per-function allocation crossing one 16-byte slab size class, the P1.0d.3 signature; which field was not isolated, and at 0.006% of the compile it was not chased further. ///// THE CORPUS IS BLIND TO EVERY CONSTRUCT THIS RUNG ADDS, MEASURED NOT ASSUMED: `--emit-corpus` at rung 5 finds `trim` 0, `CharacterSet` 0, `CharSet` 0, `Set with` 0, `Character` 0, `\uXXXX` 0. SIXTH consecutive rung with that property. New private ladder committed: `Testing/ladders/gentrim.sh`, four families (sites/data/edge/loop). ///// ⭐⭐ THE FIX: `trimStart` WALKED THE WHOLE STRING TO ANSWER A QUESTION IT HAD ALREADY SETTLED AT THE FIRST KEPT CLUSTER. `__str_trim` scans forward only (a deliberate divergence — the reference's backward `findGraphemeStart` has no GB12/GB13 rule and splits a flag the forward segmenter joins), and it ran that scan to the END on all three trims. But with `fromEnd` clear the result's high bound is the byte LENGTH and `keptEnd` is never read, so every cluster visited after the first NON-matching one was pure waste: a `__gr_end`, a `__char_at` MINT (an `__mm_alloc` + blit the bump slab never gives back), a `__cs_contains` and a `__str_decref`, all to compute a discarded bound. MEASURED on one call — `"abcdefgh"x65,536`.trimStart(), which has NOTHING to cut — 56.2 ms and 45.0 MB of peak RSS to answer "byte 0". FIXED by exiting the walk at the first kept cluster when `fromEnd` is clear (`buildStrTrim`'s new `firstKeptStop` arm, guarded by `keptStart == sentinel` so it is reached AT MOST ONCE per trim and no other path pays for it). AFTER: 0.46 ms and 4.8 MB — 121x and 9.3x — and the 0.46 ms that remains is the RESULT COPY (`emitSliceSegment` blits 512 KB), which is the floor for an owned result. On the ladder, `data-trimstart-clean` at 524,288 bytes went 262.2 ms -> 4.6 ms and 208.0 MB -> 6.7 MB. ⚠ `trim` and `trimEnd` are UNCHANGED to within noise at every rung (262.4 vs 268.7 ms; 261.8 vs 261.1 ms; RSS identical to the KB): they genuinely need the far end and they paid nothing for the exit. ⚠ THIS ROW'S SIX NUMBERS ARE Δ0 FOR THAT FIX, BIT-FOR-BIT, at all six rungs in allocations AND bytes — verified by building the pre-fix HEAD and re-running. That Δ0 is the instrument's blind spot, not the cost: `buildStrTrim` runs only under `usage.usesStrTrim` and the corpus never sets it. ///// `__str_trim` IS O(n), NOT O(n²) — the question the brief asked, settled by measurement rather than by reading the graph. `data-*` reads x2.00 per doubling out to 524,288 bytes in EVERY seed, in time and in peak RSS alike; `edge-*` (matched pad doubling against a fixed body) likewise. Per CLUSTER at the top rung: ASCII 102 ns, 2-byte BMP 150 ns, supplementary 190 ns — so `__gr_end`'s general scan costs ~48 ns over its ASCII fast path and `__ucd_cat`'s 806-ENTRY BINARY SEARCH costs ~40 ns over a direct BMP byte load. Real but bounded constants; nothing bends. ///// ⭐ THE SLAB TRIGGER FIRES A THIRD AND FOURTH TIME, and one of them is now PRICED. Workstream O's debt (bump allocator, no free list ⇒ memory linear in ITERATIONS not live data) named its trigger as "any construct that allocates once per loop trip"; `for c in s` was the first, `slice` the second. Slice D adds TWO more: a trim mints an owned result String per call, and A PREDEFINED `CharacterSet` IS REBUILT FROM SCRATCH ON EVERY CALL (the reference caches one in a `static let`; shv2 has no module-level `static let` of a heap value). PRICED by the `loop-trim` vs `loop-trim-shared` A/B — the same program but for whether the set is hoisted out of the loop: ~1,068 ns and ~1,155 BYTES OF NEVER-RECLAIMED SLAB PER CALL, which is HALF of what a whole `trim()` of a 9-byte string costs. It is a CONSTANT per call (the set has a fixed 7 members), not a growing one; what makes it matter is that the slab never gives it back, so in a loop it is linear in trips (x2.00 per doubling in RSS, 4,096 -> 65,536 trips). NOT fixed here: caching needs a language feature (module-level static heap values), and the slab's cure is a recycling `__mm_free` that `MmRuntime.maxon:738` already anticipates. Both remain the coordinator's to sequence. ///// ALSO MEASURED, NOT FIXED, AND REPORTED FOR A RULING: a no-argument `s.trim()` costs the COMPILER ~3,266 allocations per SITE against ~500 for `s.trim(cs)`, because the parser inlines the whole 7-member default preset (a set create, 7 minted Character literals, 7 inserts, a box) at every site. At 1,024 sites that is 4.24M allocations against the control's 0.90M. It is strictly LINEAR in sites, so it is a constant factor and not the mandate — recorded rather than chased, and a runtime `__cs_default()` entry point would collapse it if the coordinator wants it. | 832,414,858 | 1,507,171,773 | 2,951,793,706 | 5,886,497,442 | 11,896,649,410 | 24,111,713,049 |
| 2026-07-29 | P1.8 SLICE E OPTIMIZATION PASS — the String view/method leftovers, and the stdlib WHITELIST's first real scaling question. NOTHING WAS MADE FASTER, because nothing found was superlinear; what this row buys is the ATTRIBUTION of a delta column that would otherwise have read as an unexplained bend. ///// ⭐⭐ THE TRIGGER WAS THE PARSE DELTA GROWING +1,678 -> +2,422 ACROSS THE LADDER (total +4,429 -> +5,660), on a rung whose structural change is that `stdlib/helpers/string/utf16.maxon` is now compiled into EVERY invocation. The question that raises is the one `StdlibLoader.maxon`'s own header makes urgent — the list "IS DESIGNED TO GROW ... until it names every file there", 49 files against 3 listed — so if per-compile cost were O(corpus x listed modules), today's 0.005% becomes a genuine superlinearity in the loader's intended end state. THE ANSWER IS NO, AND IT IS EXACT RATHER THAN APPROXIMATE. `maxon-shv2/Testing/ladders/genwhitelist.sh` (committed) varies the MODULE COUNT with the corpus HELD — the axis the shared instrument has no knob for, since its module count is fixed at whatever the whitelist says that day, and a product term and a sum term are indistinguishable when one factor never varies. Extra `phase:parse` allocations vs MODULES=0, at 128/256/512-function corpora: +1,914 / +7,656 / +30,632 / +122,512 for 1/4/16/64 modules — IDENTICAL TO THE ALLOCATION at every corpus size, exactly linear in the module count, exactly invariant to the corpus. The same knob dropped into `scale-test`'s OWN emitted rung 0 (30 files, 4,890 lines) and rung 5 (185 files, 140,917 lines) reads the SAME four numbers at both ends of a 32x span. Repeated with modules carrying array literals and generic instances — the documented `O(files x types)` hazard `arrayLiteralElementsInterned` exists for — parse reads +8,936 (M=16) / +35,728 (M=64) at rung 0 and the same at rung 5, while `signatures` and `merge` come out very slightly SMALLER on the bigger corpus (-55/-126, -26/-80) because the corpus has already interned what the module wants. That is the only cross term that exists and it has the WRONG SIGN to be a hazard. ⇒ 49 listed modules will cost the SUM of 49 modules' own parses, at any program size; per unit of user code an unused listed module costs ZERO. ///// ⭐ SO WHAT WAS THE +744? NOT THE WHITELIST — measured with a THIRD binary, `nowl`, identical to head but for the one whitelist line, built by the same bootstrap and driven interleaved from an equal-length path. `head - nowl` (the whitelist entry ALONE) is +1,654 `phase:parse` allocations at EVERY ONE of the six rungs, +659 lex, +212 signatures, ~+400 merge, +4,453 total at rung 0 and +4,444 at rung 5 — FLAT, and independently reproducing the implementer's own isolated figure. `nowl - base` (all the REST of slice E) is +24 / +48 / +96 / +192 / +384 / +768 across the six rungs — an exact x2.00 doubling, strictly linear in program size — plus a FLAT -70 saving from the `insertRangeChecks` unreachable-stdlib skip. It is +3 ALLOCATIONS PER TRIVIAL ARRAY-ELEMENT READ: `emitArrayElementAccessor` now asks `arrayElementValueType`, which `adoptType`-interns the element's declared name, where it used to hardcode `ValueTypeTag.integer`. Verified in isolation at 8/16/32/64 `.get()` sites reading +24/+48/+96/+192, and confirmed by deleting `u_arrays.maxon` from the corpus, which takes the delta to EXACTLY ZERO in parse AND merge. It is the price of the type-identity fix that makes a `Byte` read back out of an `Array with Byte` still a `Byte` — a slope, not a curve. ///// ⛔ THE CORPUS IS BLIND TO EVERY PART OF THIS RUNG — SEVENTH CONSECUTIVE RUNG. `--emit-corpus` at rung 5: `toByteArray` 0, `replaceFirst` 0, `codepoints` 0, `utf16` 0, `String.from` 0, `.bytes()` 0, `.replace(` 0, `.isEmpty()` 0 in any receiver. ⚠ AND THE TRAP IN REVERSE: `.clone()` appears 256 times and EVERY ONE IS AN `Array` RECEIVER (`ac0`…`ac255`, the array-ops knob) — not one String. A non-zero on the shared ladder for a `clone` still cannot be ABOUT this rung. `maxon-shv2/Testing/ladders/genstringviews.sh` is committed as the instrument (three families: `sites-*` compile, `data-*` run-on-length, `loop-*` run-on-trips; nine site modes incl. a control and the whitelisted `utf16*` free functions; ascii/wide/supplementary seeds). ///// WHAT IT READ. COMPILE, 64 -> 1,024 sites: every one of the nine modes linear in allocations and CPU, all ratios approaching x2.00 from below. `sites-utf16fns` is the CHEAPEST of the nine (488,192 allocations at 1,024 sites vs the control's 921,668) — a whitelisted stdlib call costs less than the control's `append`. RUN, `data-*` 32,768 -> 524,288 bytes: ⭐⭐ x2.00 PER DOUBLING IN EVERY MODE AND EVERY SEED, so ALL THREE MATERIALIZED VIEWS ARE O(n) — the push-only loops (`__str_to_bytes`/`__str_to_codepoints`/`__str_to_utf16` call `__arr_push` once per byte or codepoint into an array created with NO reserve) do NOT inherit a reallocation quadratic, which was a real open question and is now measured rather than read off `__arr_grown_cap`'s geometric policy. Per byte at the top rung: `clone` 1.66 ns, `replaceFirst` 3.2 ns, `replace` 4.6 ns, `toByteArray` 16.4 ns, `codepoints` 83.6 ns, `utf16` 93.2 ns. `replaceFirst` beats `replace` 1.43x at every rung on an absent needle, confirming the slice's claim that a known result size removes one of `replace`'s two passes. ⭐ Per CODEPOINT, `utf16` reads 93.2 / 96.5 / 191.1 ns for ascii / wide / SUPPLEMENTARY — the supplementary seed costs almost exactly DOUBLE, which IS the surrogate-pair arm pushing two units where BMP pushes one, and no seed but `supp` reaches it. ///// ⚠ WORKSTREAM O's SLAB DEBT FIRES AGAIN, RE-MEASURED, NOT RE-FILED. `loop-*` at 2^17 -> 2^20 trips reads x2.00 per doubling in peak RSS for every construct: memory linear in ITERATIONS. Never-reclaimed slab per trip at 1,048,576 trips on a 10-codepoint subject: `clone()` 72 B (the cheapest measured — HALF the control, which mints two String records to a clone's one), control 141 B, `toByteArray()` 161 B, `codepoints()` 376 B, `utf16()` 378 B. ⇒ `codepoints()`/`utf16()` cost ~2.6x the control and ~5x `clone()` per call, because an 8-byte-element array created with no reserve walks 4 -> 8 -> 16 capacities and the bump allocator keeps every intermediate buffer. The runtime KNOWS the bound at entry (`__str_to_bytes` already loads `len`), so one `__arr_reserve` would collapse it — a CONSTANT-FACTOR change on an already-linear curve, and it would move committed goldens, so it is REPORTED for a decision and deliberately not shipped by the optimizer that found it. ///// GATES: build exit 0 / 0 warnings, and the rebuilt binary is md5-IDENTICAL to the one the coordinator gated, so the tree is exactly as committed. Full unfiltered suite 2293 passed / 0 failed, exit 0 (no 101). `specs-shv2/fragments/` EMPTY after that run — byte-identical codegen. This pass changed no compiler source, so every per-rung number above is unmoved by it; the two ladders are the only files added. | 865,507,616 | 1,569,659,637 | 2,988,823,831 | 5,900,724,827 | 11,812,221,009 | 24,236,232,893 |
| 2026-07-29 | BREAK-IN-MATCH RUNG — a `match` STATEMENT is now a `break` target. LINEAR, and the delta is a SLOPE not a curve. Allocation deltas vs base: +365 / +546 / +910 / +1,636 / +3,090 / +5,996 across the six rungs; delta ratios 1.50 -> 1.67 -> 1.80 -> 1.89 -> 1.94, approaching x2.00 FROM BELOW while the corpus's own emitted code grows x1.85 -> x1.99. Sub-x2 throughout, so per unit of user code the cost is flat. ///// ATTRIBUTION, exact rather than inferred, because the change is small enough to enumerate: (1) ONE `MatchContextArray` per function parse and per closure parse, mirroring the `LoopContextArray` already created at those same three sites (`parseFunction`, and the save/reset/restore pair in `parseClosureExpression`); (2) ONE `MatchContext` + ONE `MatchArmExitArray` per match STATEMENT — gated on `not isExpression`, so a match EXPRESSION costs zero; (3) nothing per statement, per op, per value, or per token. That is why the delta tracks the corpus's match-statement count and nothing else. ///// NO NEW SUPERLINEARITY, and the one lookup that could have been one is not: `resolveControlTarget` walks the match stack at most ONCE per `break`/`continue` parsed, bounded by MATCH NESTING DEPTH — the identical O(depth) bound `self.loops` has had since M4b, over a stack that E2049 keeps shallow (a match arm body is a single statement and may not open a block, so matches nest only through the scrutinee). The labelled search scans matches-then-loops, two bounded walks, not a product. ///// NO STEP-6b OPTIMIZER TRIGGER FIRED and this row records why, so the absence is a reading rather than an omission: the rung adds no PASS, no IR OP, and no collection the compiler INDEXES BY (a parser stack pushed and popped in lockstep with syntax is not an index), and no allocation ratio came near the 2.4 threshold. The hunt carries to the phase-boundary sweep. ///// CORRECTNESS EVIDENCE STRONGER THAN THE SPEED READING, which is the part worth keeping: `specs-shv2/fragments/` is EMPTY under `git status` after a full unfiltered run, and against `main` the rung moves ZERO pre-existing goldens on x64-windows AND on x64-linux — 19 additions per target plus exactly ONE modification, `match-owned-value-flow/temp-scrutinee-break-arm`, whose diff is the SOURCE LINE ALONE (`break` -> `break 'loop'`) with byte-identical IR on BOTH targets. A parser change that re-targets a control-flow keyword and moves no existing byte of codegen is the strongest statement available that the new target is reached only by the programs that ask for it. | 828,209,399 | 1,530,619,882 | 2,974,121,755 | 5,844,019,579 | 11,786,134,080 | 24,133,240,898 |
| 2026-07-29 | P1.9 DECLARATION-SITE DIAGNOSTICS — E3062 unused-typealias (per-file use tracking, drained whole-program), E3005 bound-PROVENANCE validation at the typealias declaration, E2003 bare-sized shorthand, alias VISIBILITY + the stdlib carve-out, and E3005 on an out-of-range `otherwise` value. Measured on the MERGED tree, so this delta is against the BREAK-IN-MATCH row directly above. ///// LINEAR, AND THE SHAPE IS EXPLAINED RATHER THAN ASSERTED. Allocation delta +4,320 / +4,993 / +6,343 / +9,011 / +14,315 / +24,891 across the six rungs; ratios 1.16 -> 1.27 -> 1.42 -> 1.59 -> 1.74, rising monotonically toward x2.00 FROM BELOW. That is the signature of a FLAT term plus a linear one, not of a curve: fitting `a + b*n` to the two ENDPOINTS alone gives **a = 3,656 flat, b = ~664 per corpus unit**, and that two-parameter fit then predicts all four INTERIOR rungs to within 0.5% (4,983 / 6,310 / 8,965 / 14,274 predicted vs 4,993 / 6,343 / 9,011 / 14,315 measured). The flat term is the fixed per-compile cost — the seeded stdlib alias, the registry structures, the all-declarations `nameable` index — paid once whatever the program size; the ~664/unit is the per-FILE used-alias set plus the carried bound provenance, which is what a per-file check must cost. At rung 5 the whole rung is +24,891 on 14,803,643 = **+0.17%**. ///// ⚠ AN EARLIER READING OF THIS SAME RUNG SAID +1,990 -> +11,897 (fit: 1,670 flat + 320/unit), AND IT IS NOT A CONTRADICTION — IT IS A DIFFERENT BINARY. That was step 6a, run on the implementer's commit ALONE; the figures here include the independent review's two fixes (`RangedAliasRegistry.nameable`, an all-declarations index probed on the error path, plus the parenthesized-conformance reader and a visibility bit folded into the signature content hash) and the coordinator's path-resolution fix. The cost roughly DOUBLED and the SHAPE did not move at all — same approach-from-below, same sub-x2 ratios, same sub-1% fit. Recording both is the point: a per-rung ladder read taken before the review is a reading of code that is not what ships. ///// NO STEP-6b OPTIMIZER TRIGGER FIRED. The threshold is an ALLOCATION ratio past ~2.4 that you cannot explain; the highest here is 1.74 and it is accounted for to 0.5% by two parameters. `phase:resolveTypes`, where E3062 and the visibility check both live, reads 643/963/1,603/2,883/5,443/10,563 — ratios 1.50/1.66/1.80/1.89/1.94, the same shape. The hunt carries to the phase-boundary sweep. ///// ⚠⚠ THE CORPUS CANNOT EXPRESS THIS RUNG'S SUBJECT, SO THE DELTA ABOVE IS NOT ABOUT IT. `scale-test`'s generated programs declare ranged typealiases and USE them, so across every rung the E3062 arm that REPORTS never fires once, no bound-provenance mismatch is ever constructed, no bare-sized RHS is ever written, no alias is ever hidden cross-file, and no `otherwise` literal is ever out of range. What the ladder measures is the cost of ASKING on a corpus where every answer comes back "fine" — the per-file set built and walked, the provenance carried and compared. That bound is worth having and it is real, but a future rung must not read this row as evidence about the DIAGNOSTICS. Eighth consecutive rung whose subject the shared corpus cannot reach. | 794,797,360 | 1,486,013,940 | 2,960,596,720 | 5,932,764,060 | 11,785,234,120 | 24,021,427,160 |
| 2026-07-29 | OPTIMIZER PASS over the ranged-alias bound-enforcement rung. THE HEADLINE IS A CORPUS-BLINDNESS FINDING, NOT A SPEEDUP, and it reframes the rung's own row above. THE SHARED LADDER CANNOT SEE THIS RUNG'S GUARDS AT ALL. ScaleCorpus DOES declare narrow ranged typealiases -- ScaleCoverNarrow int(0 to 4095), ScaleWide int(0 to u64.max), and 4->128 ScaleXElem<N> int(0 to 10<N>) that DOUBLE with the rung -- so a reader checking the manifest for "does the corpus emit ranged aliases" gets YES and stops. But not one of them reaches a position that emits a guard: ScaleXElem<N> is only ever a generic TYPE ARGUMENT (ScaleXBox with ScaleXElem<N>), whose field and parameter are declared with the opaque type parameter T, so the alias is erased before any door sees it -- 128 narrow aliases, zero doors; and every alias that DOES reach a door (ScaleXHoldValue, ScaleMeasure<N>, ScaleElement, ScaleNamedValue, ScaleRowValue, ScaleSigned, ScaleDispMeasure, ScaleGenElement) is int(i64.min to i64.max), FULL RANGE, which needsRuntimeGuard discards at rangeIsFull. MEASURED, not inferred: the compiled rung0 binary and the compiled rung5 binary each contain EXACTLY ONE range-check panic blob, the same one, and an instrumented build confirms exactly ONE function in the whole program ever emits a runtime guard -- scaleCoverCast, at every rung. So the rung's +1,340/+2,119/+3,665/+6,751/+12,885/+25,150 measured the RECORDING of sites that are then discarded as vacuous; the EMISSION path has never been on this ladder. That is the subtler form of the blindness the manifest warns about: the construct is present, the KNOB THAT MAKES IT COST ANYTHING is not. ///// SO A PURPOSE-BUILT LADDER WAS ADDED -- Testing/ladders/genrangesites.sh, two modes, onefunc (N guards in ONE function, the product axis) and spread (the same N one-per-function, the control). It found and this pass FIXED a real O(sites x blocks) term: RangeCheckEmitter.blockEndGuardSite located each guard site's block with IrModule.getBlockByIdIn, a LINEAR SCAN of func.blockRefs, once per site -- and worse than the plain product, because every guard the pass emits APPENDS TWO BLOCKS (__rc_ok, __rc_panic) to the very set the next site's scan walks. It now resolves through buildBlockIdIndex/blockById, the index pruneDeadBlockArgs introduced for exactly this shape one pass over ("would be O(edges x blocks), the shape the allocator's data-representation contract exists to forbid") and elimTrivialBlockArgs already shares -- a third user of one index, not a fourth mechanism. THE TERM WAS ISOLATED BY HOLDING ONE AXIS AND MOVING THE OTHER, genwhitelist.sh-style: with guards held at S and blocks swept B=512..4096, the CPU slope in B was 4,045 ticks/block at S=1 and 5,564 at S=8 -- a slope that MOVED WITH S is the product term, and it separates as roughly 3,828 per block per function plus 217 per (site x block). After the fix the two slopes are 3,905 and 4,020, ratio 1.375 -> 1.029: the S-dependence is GONE. Phase CPU at S=8 B=4096: 23,244,143 -> 16,867,165 ticks, -27.4%. ⚠ THE ALLOCATION COLUMN IS BLIND TO ALL OF IT -- it read FLAT in B before and after, because a scan allocates nothing. This is the third column earning its place. ///// THE +2 ALLOCATIONS ON THIS ROW, FLAT AT EVERY RUNG, ARE THE WHOLE COST: the shared empty index plus the one real index scaleCoverCast builds. It was NOT flat in the first cut. Storing the index on RangeCheckEmitter with an unconditional ValueIdArray.create() cost +45/+66/+108/+192/+360/+696 across the rungs, because an emitter is built for EVERY function carrying any range-check side data -- which includes every function that merely passes a CONSTANT ARGUMENT or constructs a generic instance -- so the overwhelming majority paid for an array they never read. Fixed the way Parser already solves the identical problem, a shared never-mutated empty (sharedEmptyInstanceAliasNames), and the index is built lazily on the first RUNTIME guard, which a compileTimeOnly call-argument site never reaches. The residual growing BYTES (+392/+560/+896/+1,568/+2,912/+5,600) are allocation-free and fully attributed: they are the 8-byte field added to RangeCheckEmitter times the emitter count -- 392/8 = 49 emitters at rung0, 5,600/8 = 700 at rung5, matching the +45 and +696 allocation counts that were just removed. ///// HANDED BACK, NOT FIXED, BECAUSE IT IS NOT THIS PASS'S SUBSYSTEM: ~16 runtime range-check guards in ONE function PANIC the compiler in the register allocator -- "register-pressure E5001 ... value is in the blocking set but resolves to NO source span ... This is a COMPILER BUG" from RegisterPressureDiagnostic, raised out of splitLiveRanges. Bisected to n=16 guards, and the CONTROL is clean and separates cleanly: the identical program with the alias widened to int(i64.min to i64.max) -- same statements, same field stores, no guards -- compiles fine, and n=12 compiles fine. It is PRE-EXISTING rather than this rung's: the old `as`-cast door reaches it at the same n=16. But this rung makes it far easier to reach, because a field store is far more common than an explicit cast. It also CAPS the onefunc ladder at n<=12, which is why the product term above had to be measured on the block axis instead. ///// CORRECTNESS: full unfiltered spec-test 2348/0, exit 0, no leak, and specs-shv2/fragments COMPLETELY CLEAN after the run -- byte-identical Target IR for 2,348 programs, which is the strongest available statement that an optimization changed no behaviour. | 825,652,901 | 1,533,320,621 | 2,952,487,289 | 5,830,476,219 | 11,744,028,857 | 24,082,952,720 |
| 2026-07-29 | RANGED-ALIAS BOUNDS ENFORCED AT EVERY POSITION — six doors, not two. LINEAR; the delta is a SLOPE. Allocations vs the break-in-match row: +1,340 / +2,119 / +3,665 / +6,751 / +12,885 / +25,150, delta ratios 1.58 -> 1.73 -> 1.84 -> 1.91 -> 1.95, approaching x2.00 FROM BELOW against a corpus growing x1.85 -> x1.99. ///// ⭐⭐ THE MOST IMPORTANT THING THIS ROW RECORDS IS THAT THE LADDER CANNOT SEE THIS RUNG AT ALL — EIGHTH CONSECUTIVE RUNG, and the mechanism is a NEW ONE worth naming. The manifest check PASSES: `ScaleCorpus.maxon` really does declare narrow ranged aliases (`ScaleCoverNarrow = int(0 to 4095)`, and 4->128 `ScaleXElem<N>` that DOUBLE with the rung). But `ScaleXElem<N>` is only ever a GENERIC TYPE ARGUMENT (`ScaleXBox with ScaleXElem<N>`), so the field and parameter it flows through are declared with the opaque type parameter `T` and the alias is erased before any door sees it: 128 narrow aliases, ZERO doors. Every alias that DOES reach a door is `int(i64.min to i64.max)` — full range, discarded at `rangeIsFull`. MEASURED, not inferred: the compiled rung-0 and rung-5 binaries each contain EXACTLY ONE range-check panic blob, the same one, and an instrumented build confirms exactly ONE function ever emits a runtime guard, at every rung. ⇒ **A NARROW ALIAS IN THE CORPUS IS NOT THE SAME FACT AS A NARROW ALIAS AT A DOOR.** A future manifest check for this feature must count DOORS, not aliases. `Testing/ladders/genrangesites.sh` is committed as the instrument that does. ///// WHAT THE PURPOSE-BUILT LADDER FOUND, which the shared one structurally could not: `blockEndGuardSite` located each site's block with `getBlockByIdIn`, a LINEAR SCAN per site — and worse than a plain product, because every guard emitted APPENDS TWO BLOCKS to the set the next site's scan walks. O(sites x blocks) -> O(blocks + sites) via the block-id index `pruneDeadBlockArgs` already owns (third user of one index, not a fourth mechanism). Isolated by holding guards and sweeping blocks: slope 4,045 -> 3,905 ticks/block at S=1 and 5,564 -> 4,020 at S=8, so the PRODUCT term went 1.375 -> 1.029. Phase CPU at S=8/B=4096: 23,244,143 -> 16,867,165 ticks, -27.4%. ⚠ **THE ALLOCATION COLUMN READ FLAT IN B BEFORE AND AFTER — a scan allocates nothing, so this was findable ONLY in CPU.** That is the column O4 exists for, earning itself again. ///// THEN THE FIX THAT MATTERED MORE THAN THE SPEED. A guard SPLITS its block, so resolving every site to the parser-recorded block id chained guards BACKWARDS: guard k+1's cascade landed before guard k's branch. Two consequences, both silent: guards ran in REVERSE SOURCE ORDER (the compiler reported the SECOND of two bad casts), and `recordFusableCompare` lost fusion for N-1 compares, which then materialized through `setcc` and sat in registers until the 14-GPR file overflowed at N=16 and the RULE 3 backstop panicked. `where-clauses.two-constraints` shows it was a genuine MISCOMPILE, not just ordering: two cascades compared rbx against 0 and r12 against 1000, mixing two fields' values across two checks. Fixed by making the index a CHAIN END rather than a block identity; the values stop existing as registers at all, so no rematerialization was needed. Threshold gone: both doors compile and run at N=512. At n=12 `regalloc:splitting` went 149,767,046 -> 73,836,068 CPU (-50.7%) and 7,196 -> 4,393 allocations (-39%). ///// AND THEN GUARD POSITION, on a user ruling. A guard fired at END OF BLOCK, so the whole rest of the block ran first: an out-of-range value was PRINTED and read back out of a `Percent` slot before its panic, and `100 / d` panicked "integer divide by zero" where the oracle panics "Range check failed 'NonZero'" — the language's own check pre-empted. Fixed with a two-tier position (`maxonOpPos` stamped by the parser, `stdOpPos` translated by a single `RangeSiteWalk` in `lowerMaxonToStd`), composed with the chain via `guardChainBase` so N guards at N positions fire in source order. +11 allocations FLAT for the whole compile plus +1 per EMITTED guard; 70 goldens moved, every one the same shape (the cascade moving up, the instructions between site and old block end moving past it), emitted code SIZE identical at every rung. ///// ⚠ MEASURED DEBT, RE-MEASURABLE AND NOT FIXED: `regalloc:splitting` is QUADRATIC IN GUARDS-PER-FUNCTION, now observable to n=512 (x2.09, 2.37, 3.06, 3.53, 3.77, 3.89 per doubling). The `spread` control — same guard count, one per function — is LINEAR (->x1.96), and a guard-free N-blocks-in-one-function control is linear too, so it is per-function CONCENTRATION, not guards and not blocks alone. NOT created by this rung (block and value counts unchanged; the fix strictly reduced it), only newly REACHABLE, and it looks like Workstream O's existing `regalloc:splitting` debt arrived at by a new axis. Re-measure trigger: `genrangesites.sh onefunc` at n>=128. | 829,341,939 | 1,556,402,558 | 3,029,073,967 | 5,919,801,421 | 11,951,823,163 | 24,353,348,855 |
| 2026-07-29 | RE-READ ON THE MERGED TREE — the rows above this one were measured on a binary that did not contain P1.9's declaration-site half, which landed upstream mid-rung. Same correction upstream's own last row made, for the same reason, and it is worth stating as a rule: A LADDER ROW IS ABOUT A BINARY, NOT ABOUT A CHANGE, so a rebase onto an advanced main invalidates it even though nothing in the rung moved. ///// THE MERGED NUMBERS: allocations 587,761 / 1,049,716 / 1,972,398 / 3,814,797 / 7,497,484 / 14,859,091, deltas +4,320 / +4,993 / +6,343 / +9,011 / +14,315 / +24,891 vs the row directly above. (⚠ CORRECTED AFTER THE RUN: the note as submitted quoted deltas from an earlier measurement against a DIFFERENT BASELINE, which is the same class of error this row exists to fix. The numbers here are the ones the instrument printed.) Delta ratios 1.16 -> 1.27 -> 1.42 -> 1.59 -> 1.74 — approaching x2.00 from below, LINEAR, and TIGHTER than the pre-merge read (1.58 -> 1.95) because P1.9's declaration-site checks add per-declaration work that scales with the corpus rather than per-site work that does not. `phase:insertRangeChecks` reads 2,013 / 3,390 / 6,128 / 11,586 / 22,488 / 44,270 — ratios 1.68 -> 1.81 -> 1.89 -> 1.94 -> 1.97, linear, 0.30% of the compile at rung 5 (up from 0.094% pre-merge, which is the guard-POSITION work: a two-tier position per site plus the `RangeSiteWalk` translation). ///// NOTHING SUPERLINEAR APPEARED IN THE COMBINATION, which was the open question a merge of two range-checking rungs raises: P1.9 checks a typealias at its DECLARATION (per-file, drained whole-program) and this rung checks values at their POSITIONS (per-site), and the two do not multiply — declarations and sites are independent axes and the ladder grows both. `regalloc:splitting` is unmoved at x2.19/x2.14/... per doubling, the same profile as before the merge. ///// ⚠ THE MEASURED DEBT FROM THE ROW ABOVE STANDS UNCHANGED and is NOT re-measured here, because the shared ladder still cannot express it: `regalloc:splitting` is quadratic in guards-per-FUNCTION (x3.89 per doubling at n=512 on `genrangesites.sh onefunc`), while this corpus emits one guard in the whole program. Re-measure trigger is unchanged: `genrangesites.sh onefunc` at n>=128, not this table. | 834,247,378 | 1,576,424,451 | 3,064,337,515 | 6,034,364,618 | 11,899,690,751 | 24,354,125,025 |
| 2026-07-29 | G1 (worker-exe split) — HARNESS-ONLY, AND THE LADDER IS UNMOVED BY IT. The rung splits `Main.runSpecTest`'s single `Process.executablePath()` into workerExe (what a pool worker IS) and compilerExe (the compiler under test, new `--compiler=<path>`), touching only Main.maxon + Testing/SpecWorkerPool.maxon — no pass, no IR op, no collection the compiler indexes by, so no step-6b optimizer trigger fired. ///// ⚠ THE +192 ALLOCS AND NEGATIVE BYTES DELTAS AGAINST THE ROW ABOVE ARE NOT THIS RUNG'S, AND THAT IS A MEASUREMENT RATHER THAN A READING. A/B at the SAME CHECKOUT PATH (the one thing that silently reverses this measurement's sign), rebuilding the pre-change sources from 518a19274 into the same location: PRE reads allocs 587,953 / 1,049,908 / 1,972,590 and POST reads 587,953 / 1,049,908 / 1,972,590 — identical to the digit, with allocsDelta +192 and bytesDelta -12,107 / -14,627 / -19,667 on BOTH. (The A/B climbed 3 rungs, not 6; the delta is a CONSTANT +192 across all six rungs of the post run, i.e. flat in input size, so it is a fixed per-compile cost and the three shared rungs settle it.) The deltas are therefore inherited from commits that landed on main between the previous logged row's binary and 10e518b8e; G1 contributes exactly ZERO. Worth writing down because the obvious story — "a new module-level `--compiler=` string constant plus a new function, hence a few more startup allocations" — is plausible, is flat-in-rung-size exactly as the +192 is, and is WRONG. ///// The A/B is the only honest form for this question: a Δ0 claim about MY change cannot be read off a delta against SOMEBODY ELSE'S baseline, because a log row is about a BINARY and not about a change — the rule the row above this one states — and two independent code deltas separate that logged binary from this one. | 898,299,100 | 1,693,196,020 | 3,358,947,300 | 6,017,930,420 | 12,221,485,900 | 27,686,487,260 |
| 2026-07-29 | FIRST-CLASS TUPLES (heap-allocated) + an O(files x types) FIX THAT PREDATES THEM. ///// THE RUNG ITSELF IS +12 ALLOCATIONS FLAT AT EVERY RUNG — two `ProgramSignatures` maps constructed once per compile, and nothing else. A tuple is a SYNTHESIZED STRUCT (`__Tuple2.int.int`) typed through the existing `MaxonType.structRef`, so no new type-system arm, and the per-Parser layout map uses the codebase's shared-empty + copy-on-write idiom so a tuple-free file allocates ZERO. ///// ⛔⛔ NINTH CONSECUTIVE RUNG THE SHARED LADDER CANNOT SEE, and this time it is not subtle — the feature did not EXIST until this commit, so the corpus could not contain it. MEASURED rather than assumed: `--emit-corpus` wrote 465 files / 279,453 lines across rungs 0-5, and greps for tuple destructuring, `.N` element access, tuple literals, tuple return types and tuple typealiases return ZERO hits each; `Testing/ScaleCorpus.maxon` contains the string "tuple" zero times. So the +12 measures two empty maps and not one line of `internTupleType`/`registerTupleLayout`/`canonicalTupleName`/`parseTupleLiteral`. `Testing/ladders/gentuples.sh` is committed as the instrument that does reach them — seven modes in four CONTROL PAIRS, which is what lets a product term separate. ///// WHAT THE PURPOSE-BUILT LADDER FOUND, and it was NOT tuples: `internArrayLiteralAggregateInstances` ran ONCE PER FILE holding an `[<identifier>...]` literal, walking every struct/union registered SO FAR over tables still growing — O(files x types). `phase:signatures` allocations at 64->1024 files: 18,356 / 39,630 / 94,408 / 253,058 / 766,908 = x2.16 x2.38 x2.68 x3.03, AFTER 15,490 / 29,750 / 58,218 / 115,102 / 228,818 = x1.92 x1.96 x1.98 x1.99. CPU 785,944,503 -> 187,830,368 ticks at n=1024. ⭐ THE STRUCT-ONLY CONTROL (no tuples anywhere) BENDS IDENTICALLY, which is how it was attributed as PRE-EXISTING: tuples merely added a SECOND producer of registered types and thereby a second way to reach it. The fix is a HOIST — `queryProgramSignatures` folds every file before any reader runs, so one final walk interns the same types; it also closes a latent hole where a type declared after the last literal-bearing file was never interned at all. The deleted dedup Set is why the rung's own footprint fell +20 -> +12. ⚠ The hoist CHANGES GENERIC-INSTANCE ID ORDER (hash-bucket order of a rehashing map -> one final walk); verified safe by the strongest available check — all 2,372 pre-existing fragment goldens byte-identical after a full run. ///// ⚠ MEASURED DEBT, RE-MEASURABLE, NOT FIXED, and NEITHER IS TUPLE-SPECIFIC. (1) `StructLayout.indexOfField` (`Project.maxon:1422`) is O(fields) by NAME STRING, so n accesses over n fields is a product: `arity` parse CPU x1.61/2.34/2.95/3.53 at n=32->512 against `access`'s x1.19/1.34/1.52/2.11 with the field count pinned at 2 — and a DECLARED STRUCT with 512 fields read 512 times reads x1.31/1.76/1.98/2.73 on the same ladder, so it is the struct layout's shape, not tuples'. Allocation-flat, CPU-only, structurally invisible to the memory columns. The realistic instance is the compiler's OWN `Parser` record (~50 fields), where every `self.x` pays an averaged 25-compare scan as a CONSTANT FACTOR, not a curve. Re-measure trigger: `gentuples.sh arity` at n>=128. (2) A nested tuple's mangled name is O(total leaves), so a depth-d chain is O(d^2) bytes of name: `nest` parse CPU x1.37/1.98/2.77/3.55 at depth 8->128. Inherent to a MANGLED-NAME IDENTITY; fixing it needs structural ids rather than names. Linear in practice at realistic depth (<=4). Re-measure trigger: `gentuples.sh nest` at n>=32. ///// CORRECTNESS: full unfiltered suite 2401/0 exit 0 (no 101), x64-linux PASS, wasm32-wasi PASS, arm64 SKIP; 29 fragment ADDITIONS and ZERO modifications, and `specs-shv2/fragments/` clean after a full re-run — byte-identical codegen for every pre-existing program, which is the strongest statement available that a feature this size is purely additive. | 841,869,906 | 1,585,776,367 | 3,178,543,442 | 5,984,182,238 | 11,959,408,031 | 24,445,305,849 |
| 2026-07-29 | D5 `#if` CONDITIONAL COMPILATION + A MEMO-PROBE FIX THAT MOSTLY PREDATES IT. NET IS NEGATIVE: rung 5 goes 14,864,427 -> 14,858,715, i.e. -388 BELOW the pre-rung baseline, because the fix returns more than the rung costs. ///// THE RUNG ALONE measured +968/+1,108/+1,388/+1,956/+3,084/+5,324 allocations (`828 + 140*2^n`, successive increments x2.00 x2.03 x1.99 x1.99 — dead linear), and `phase:directives` 33/38/48/68/108/188 which is EXACTLY files+3 (corpus has 30/35/45/65/105/185 files). ///// ⛔⛔ TENTH CONSECUTIVE RUNG THE SHARED LADDER CANNOT SEE, and MEASURED not assumed: `--emit-corpus` wrote 465 .maxon files across rungs 0-5 and a grep for hashIf, hashElse and hashEndif spellings returns ZERO matches. So `phase:directives` on this ladder is the per-file memo BOOKKEEPING and NOT ONE TOKEN WAS EVER FILTERED while it was taken. `Testing/ladders/gendirectives.sh` is committed as the instrument that does reach it — nine modes in four CONTROL PAIRS. ///// WHAT THE PURPOSE-BUILT LADDER FOUND: NO SUPERLINEARITY, on any of five axes, and here are the numbers rather than a claim. (a) REGION COUNT: `regions` x1.99 allocs / x2.01 cpu at n=64->2048 against the `plain` control, whose surviving program is IDENTICAL and whose directives phase is FLAT at 5 allocations. (b) DEAD-BRANCH SIZE: `dead` x1.99 / x1.97 — skipping is proportional to what is skipped and no worse, and cheap: 139,616 bytes against `regions`' 2,207,504 for the same input tokens. (c) NESTING DEPTH — THE PRODUCT AXIS, since a liveness test that WALKED the frame stack would be depth x tokens: `nest` 4,130 allocs / 6,753,314 ticks against the depth-1 `flat` control's 4,119 / 5,903,606, both x1.99 — INDISTINGUISHABLE, so liveness is a top-of-stack read and the frame stack is O(1) per directive. (d) CONDITION LENGTH: `cond` 14 allocations FLAT from n=64 to 2048 (`equalsIgnoringAsciiCase` compares source slices in place and allocates nothing), CPU x1.88 — the DELIBERATELY non-short-circuiting evaluator costs exactly its atoms, which is what that design was supposed to cost. (e) CONDITION DEPTH: `parens` 14 allocs flat, CPU x1.66; it stack-overflows between depth 1024 and 2048, AND THE CONTROL FAILS IDENTICALLY — an ordinary expression with 2048 nested parens overflows in `Parser.current` the same way — so that ceiling is the compiler's uniform recursive-descent property, not a hole this rung opened. ///// ALSO MEASURED: the two token caches SHARE one array and there is no hidden copy. A directive-free file's directives phase allocates 40 bytes (n=2048 `plain`: 200 bytes for the whole compile) against 2,207,504 bytes when the filter actually rebuilds — four orders of magnitude, which no copy could hide behind. And a `#if` region costs ~2 allocations (a `ConditionCursor` + a `DirectiveFrame`) whatever it contains. ///// THE FIX — THE MEMO PATTERN PROBED ITS `Map with (FilePath, _)` TWICE FOR ONE FACT, and that is not a free lookup on Windows: `FilePath.hash` CLONES AND LOWER-CASES the path (`canonicalForCompare`) and `FilePath.equals` clones twice more, which is why `Project.maxon`'s `RangedAliasByDeclaringFile` is keyed by a plain String instead. Every query did `xCacheValid(path)` (which probed the map to compare a hash, then threw the memo away) and then probed the SAME map again to read the value — with a panic guarding the disagreement that second probe made expressible. `sourceFiles` was probed once to VALIDATE and again to STAMP. `queryParseOps` computed `currentParseMemoKey` twice. Now: one `sourceEntryOf` per query serves both the validity hash and the stamp, `if let memo = cache.get(path)` serves both the hash and the value, and the parse key is computed once and stamped with that same value. `querySourceFile`, `tokensCacheValid`, `activeTokensCacheValid`, `parseCacheValid` and two impossible-state panics are deleted. RESULT: -(162 + 30*files) allocations, EXACT at every one of the six rungs (-1,062 / -1,212 / -1,512 / -2,112 / -3,312 / -5,712) — three successful FilePath map probes removed per file, so A SUCCESSFUL FILEPATH-KEYED PROBE COSTS 10 ALLOCATIONS. Bytes -43,487 at rung 5 = ~78 per probe, one cloned path string. CPU -0.09% at rung 5: INSIDE the noise band, not a datapoint. Most of this predates D5 — the `queryTokens` and `queryParseOps` halves are the pre-existing spine. ///// ⚠ MEASURED DEBT, NOT FIXED: `tokensHoldDirective` (`ConditionalCompilation.maxon:228`) RE-SCANS EVERY TOKEN OF EVERY FILE to answer a question the LEXER already knows for free, and it is exactly the cost class the memory columns cannot see — `phase:directives` allocations are FLAT per file while its CPU reads 3,390,864 / 6,436,030 / 12,617,014 / 25,826,151 / 50,942,407 / 100,523,097 = x1.90 x1.96 x2.05 x1.97 x1.97, dead linear and 0.41% OF TOTAL COMPILE CPU at rung 5 on a corpus containing no directive at all. ATTRIBUTED, not guessed: that CPU tracks SOURCE BYTES (x29.7 across the ladder) and not FILE COUNT (x6.2), so it is the token walk and not the per-file probe overhead. Removing it means threading a flag out of `tokenize` through `TokenMemo`, i.e. putting directive knowledge in the lexer's signature, for four tenths of one percent — a constant factor, so it is filed rather than chased. Re-measure trigger: `gendirectives.sh plain` (its directives phase is 100% this scan), or `phase:directives` CPU passing ~1% of a rung. ///// CORRECTNESS: full unfiltered suite 2424/0 exit 0 (no 101); `verify-warm-rebuild` PASS on a 36-file project WITH directives (determinism + cache + invalidation, including the new activeToken hit/miss assertions); and `specs-shv2/fragments/` COMPLETELY CLEAN after the full run — zero modified, zero untracked, so codegen is byte-identical for all 8,436 goldens. | 841,462,567 | 1,580,145,313 | 3,067,131,855 | 5,992,261,887 | 12,006,779,799 | 24,584,197,255 |
| 2026-07-29 | D1 (instance methods on `union`/`enum`) — ALLOCATIONS UNCHANGED, BYTES +1,040/+1,632/+2,816/... and the instrument attributes 100% of it. Allocs delta is 0 at EVERY rung, so the rung adds no allocation site. The bytes rise is one added field: `Parser.enclosingTypeIsEnum as bool` (the KIND half of the fact `enclosingType` names). ///// ATTRIBUTION IS ARITHMETIC HERE, NOT A STORY. The whole delta lands in exactly the two phases that construct Parser records: rung 0 = `phase:signatures` +776 and `phase:parse` +264 = 1,040, the reported delta to the byte; rung 2 = +2,432 and +384 = 2,816, likewise exact. Nothing else moved. A wider record charged per file is linear in file count, which is what the ratios say: bytesDelta grows x1.57 then x1.73, tracking the corpus's own x1.85/x1.92 code growth rather than outpacing it. Share at rung 2: 2,816 of 149,378,947 bytes = 0.0019%. `phase:signatures`/`phase:parse` also each gained +2 FREES, flat in rung size — `enterTypeScope` repoints the three per-declaration scopes at their shared empties, dropping two references the old `openTypeParamScope` path held. ///// NO STEP-6b TRIGGER. No new pass, no new IR op, and total bytes per rung ratio is x1.88 then x1.99 — linear, nothing near 2.4. ///// ⚠⚠ AND THE COLUMN THAT WOULD MATTER MOST IS BLIND HERE, WHICH IS THE HONEST CAVEAT: the generated corpus declares NO enum/union methods at all, so the sweep's new method-signature scan and the new `dispatchMethodOnEnumReceiver` arm are never entered by any rung. Their cost is UNMEASURED, not zero — a Delta-0 from a ladder that cannot express the feature measures the instrument's blind spot, not the cost, and that is the corpus blind spot rather than the allocation-vs-CPU one. What IS measured is that a program with no enum methods pays only the wider record. Re-measure trigger: a corpus knob that emits enum/union methods, or the 5d harness re-extraction, whose 13 files include five union `displayReason`/`isPass` methods. | 910,427,560 | 1,606,225,800 | 3,160,253,280 | 5,985,099,180 | 11,902,233,080 | 24,855,412,020 |
| 2026-07-30 | D2 (the postfix/member walk) — +146/+306/+626/+1,266/+2,546/+5,106 allocations, and the instrument attributes 100% of it to ONE phase and ONE object. ///// MEASURED AS AN A/B IN THIS WORKTREE, both binaries rebuilt: base origin/main (c9b5b6239) vs head. The whole-module delta equals `phase:parse`'s delta TO THE UNIT at every rung — parse allocs 116,048->116,194 (+146), 211,130->211,436 (+306), 400,939->401,565 (+626), 779,626->780,892 (+1,266), 1,536,332->1,538,878 (+2,546), 3,048,581->3,053,687 (+5,106) — and every other phase reads exactly 0. Bytes likewise: parse +345,888 at rung 5 over 5,106 objects = 67.7 bytes each, which is a header plus seven 8-byte fields. ///// IT IS ONE `MethodReceiver` PER MEMBER-ACCESS SITE, at parse time. The rung collapses two dispatch chains (eight arms keyed on a `VarInfo`, four keyed on a value) into one switch, and the receiver record is what both doors hand it. The deltas are 146 x2.10 x2.05 x2.02 x2.01 x2.01 — converging on exactly x2 against a ladder that doubles, i.e. strictly LINEAR in the number of `.member` sites, forever. Share at rung 5: 5,106 of 14,863,821 = 0.034%. ///// THE COST MODEL IS THE ONE THIS PATH ALREADY HAD, not a new one: `requireStructBase` allocates a `StructBase` per struct base for the identical reason ("ONE ANSWER, BECAUSE IT IS ONE QUESTION"), and a member access that previously reached it already paid a record. What is new is that a String/Array/Set/conformer receiver — which never reached `requireStructBase` — now pays one too. IT IS AVOIDABLE AND I AM NAMING IT: flattening `MethodReceiver` into seven parameters on `dispatchMethodOnReceiver` removes 100% of this delta and costs the switch nothing, since the record is the calling convention and not the thesis. Left as a record deliberately (it is the shape `StructBase` already established here); handed to the optimizer with an exact target. ///// NO STEP-6b TRIGGER: no new pass, no new IR op, no new whole-program query. Total allocations per rung x1.79 x1.88 x1.93 x1.97 x1.98, bytes x1.88 x1.99 x2.01 x2.03 x2.05 — unchanged from base. ///// CPU is inside the noise band and is not read as a datapoint. ///// CORPUS BLIND SPOT, stated because the feature is exactly the kind the ladder cannot express: ScaleCorpus emits no string/array/call-result RECEIVER chains and no enum methods, so the newly-reachable arms (`"abc".byteLength()`, `Inner.make(9).get()`, `o.inner.get()`, `pick().score()`) are never entered by any rung. Their per-call cost is UNMEASURED, not zero. What IS measured is that the shared switch charges every EXISTING member access one record. | 842,686,366 | 1,551,911,127 | 3,060,450,357 | 5,943,361,290 | 11,980,472,928 | 24,507,415,171 |
| 2026-07-30 | D1d + D1c (keyword-as-a-NAME is not block structure; E3081 for an all-discard binding list) — NEUTRAL, and the interesting number is one this rung INTRODUCED AND THEN REMOVED. ///// ⚠ THE DELTAS AGAINST THE ROW ABOVE ARE NOT THIS RUNG'S AND CANNOT BE: that row is D1's, measured before D5 and P1.10 landed, so -82/-92/-112 allocs and -1,048/-1,101/-1,213 bytes mix THREE code deltas (D5's token-tier directives — note the new `phase:directives` at 33/38/48 allocs — P1.10's tuples plus its O(files x types) fix, and this rung). A log row is about a BINARY, not about a change. The attributable measurement is a SAME-PATH A/B against a parent-commit binary built in this checkout: allocations and bytes BYTE-IDENTICAL at all six rungs. ///// ⭐⭐ AND THAT A/B IS WHY THE RUNG IS NEUTRAL RATHER THAN WORSE. The first cut of the E3081 work spelled its comma separator once by collecting into an array — `joinCommaSeparated(array)` — which put a `StringArray.create()` in `checkEnumExhaustive`, a routine that runs on EVERY enum match and not only on a rejected one. Measured: **+20 allocations at rung 0 DOUBLING to +640 at rung 5, x2.00 every rung**. Replaced by `appendCommaSeparated(list, item)` — the separator is still written down once, and nothing is allocated on the success path. Independently confirmed here off the instrument: `phase:semanticCheck` reads 1,996 / 3,628 / 6,883, IDENTICAL to the D1 row above, so no per-match allocation survives. ⭐ **A DEDUP THAT IS CORRECT CAN STILL BE UNSCALABLE — "spell it once" is not free if the once is on a hot path.** ///// ⚠ A MEASUREMENT ARTEFACT WORTH KNOWING: the first re-run of that A/B read a flat -1 BYTE at every rung. Cause was the two A/B binaries' FILENAMES differing by one character — the exe path is a String the compile allocates — and equal-length names took it to 0. Cousin of the checkout-path-length effect already recorded in this log; it reaches the byte column through argv, not through the tree. ///// NO STEP-6b TRIGGER: no new pass, no new IR op, and per-rung totals are x1.88 then x1.99. | 913,574,340 | 1,613,148,640 | 3,120,666,780 | 6,023,828,020 | 12,004,227,740 | 24,643,635,360 |
| 2026-07-30 | D4 — a member-access refusal may not assert a type the compiler INVENTED (E3011 for an undeclared type name, at all three member-access doors). NEUTRAL, and neutral for a structural reason rather than a lucky one. ///// THE NEW WORK IS ON AN ALREADY-THROWING PATH AND CANNOT COST THE SUCCESS PATH ANYTHING. `undeclaredBaseTypeNameOf` asks `TypeResolution.denotedNamedType` — a name lookup plus a registry cascade — but it is only ever reached INSIDE a branch that has already decided to refuse the program. Per REFUSED member access, never per field read. The generated corpus contains no refused member access at all, so the expected delta is exactly zero and that is what the instrument reads: `phase:resolveTypes` and `phase:parse` are unmoved, and a same-path A/B against a parent-commit binary built in one checkout was BYTE-IDENTICAL in allocations and bytes at all six rungs. The two success-path hoists (binding `capturedTag` and `typeNameIdOf` once instead of twice, in `capturedStructBase` and `requireStructBase`) are reads of an already-materialized value, so they are allocation-neutral too. ///// WARNING, AND IT IS THE SECOND ROW RUNNING TO CARRY IT: the deltas printed against the previous row are NOT attributable to any code change. Measured from a worktree whose path is 3 characters longer than the main checkout's, the ladder read -46/+114/+434/+1,074/+2,354/+4,914 allocs — a delta that DOUBLES every rung because the corpus passes a path per file and the file count doubles. It is the checkout-path-length artefact the previous row already records, and the row above that one records its argv cousin (two A/B binaries whose FILENAMES differed by one character read a flat -1 byte at every rung). This row is measured from the main checkout after the merge, so its own deltas are against a differently-pathed predecessor and should be read the same way. THE ATTRIBUTABLE MEASUREMENT IS ALWAYS THE SAME-PATH A/B, never the cross-row delta. ///// ⭐ AND THE ARTEFACT REACHES THE ALLOCATION COLUMN, NOT ONLY THE BYTE COLUMN THIS LOG HAD RECORDED IT FOR — measured here by accident, worth knowing on purpose: the SAME commit, built and measured in two checkouts differing in nothing but path length, reads a CONSTANT **+14 allocations in `phase:load` at every one of the six rungs** (587,825→587,839 … 14,863,821→14,863,835) together with ~4.5 KB FEWER bytes in the shorter path. CONSTANT rather than doubling, because `load` opens a fixed set of files per compile while the per-file path cost doubles with the corpus — so the two artefacts have DIFFERENT SHAPES and a flat offset in a memory column can be the CHECKOUT rather than the code. Allocations are still bit-for-bit reproducible; they are reproducible FOR A GIVEN PATH. ///// NO STEP-6b TRIGGER: no new pass, no new IR op, no new collection the compiler indexes by. Per-rung totals x1.79 x1.88 x1.93 x1.97 x1.98 — at or below x2.00 every rung, nothing near the 2.4 that would spend an optimizer agent. | 927,426,358 | 1,593,868,056 | 3,119,556,568 | 6,169,414,297 | 12,155,701,376 | 24,850,878,991 |
| 2026-07-30 | D1e — pin the UNION rvalue receiver (3 spec cases; postfix-member-walk.md). NEUTRAL, and this row is the strongest neutrality claim the log can carry: THE COMPILER SOURCE DIFF IS EMPTY. `git diff` over the branch is one spec file plus 6 new goldens; `maxon-shv2/Compiler/Parser.maxon` was verified BYTE-IDENTICAL to main after the sabotage used to prove the new cases load-bearing was reverted. No pass, no IR op, no collection the compiler indexes by — and no code at all. ///// ⭐ SO THIS ROW IS A NEGATIVE CONTROL FOR THE CHECKOUT-PATH-LENGTH ARTEFACT THE PREVIOUS TWO ROWS DOCUMENT, and it is a better one than either, because those had real code changes to argue about and this one has none. Measured from the worktree C:\\Users\\Eric\\dev\\maxon-D1e, the ladder reads a CONSTANT -14 allocations at every one of the six rungs (587,825 / 1,049,930 / 1,972,912 / 3,815,919 / 7,499,814 / 14,863,821) with bytes +6,263...+32,303 DOUBLING per rung. Two different shapes in one reading, exactly as the D4 row predicts: the FLAT -14 is `phase:load`'s fixed per-compile file set, the DOUBLING byte delta is the per-file path cost riding a corpus that doubles. Since no compiler code changed, BOTH are the checkout and NEITHER is a cost — which is the cleanest confirmation available that a flat offset in an allocation column can be the path rather than the code. ///// THE CROSS-ROW DELTA REMAINS UNATTRIBUTABLE; the attributable measurement is always the same-path A/B, and here it is trivially byte-identical because the sources are. ///// NO STEP-6b TRIGGER: per-rung totals x1.79 x1.88 x1.93 x1.97 x1.98, at or below x2.00 every rung and nothing near the 2.4 that would spend an optimizer agent. | 875,893,404 | 1,606,270,254 | 3,050,208,191 | 6,040,796,091 | 12,139,233,512 | 24,732,807,277 |
| 2026-07-30 | R4.1 — the file-IO runtime's OPEN slice: `__ManagedFile` + `__ManagedMemory` + `__ManagedFileError` + the `osFileOpenRead` StdOp + `__mf_open_read`. Host-only by the 2026-07-30 ruling; every other target refuses at the call site with the EXISTING E3104. ///// ALLOCATIONS: +222 / +267 / +352 / +517 / +842 / +1,487 across the six rungs. LINEAR AND FULLY ATTRIBUTED — it fits ~181 flat + ~41 per doubling-unit to within 2% at every rung (rung 3 predicted 507 vs 517 actual, rung 4 predicted 834 vs 842). The flat term is `createEnumRegistry` seeding a SECOND builtin enum (`__ManagedFileError`) per registry construction; the per-file term is that seeding riding a file count that doubles. `phase:signatures` carries it (+70 at rung 0, +395 at rung 5) exactly as that attribution predicts. +1,487 is +0.01% of rung 5's 14.87M. ///// NO SUPERLINEARITY. Per-phase ratios are at or about x2.00 across the board; `regalloc:splitting` x2.14 and `liveness` x2.08 are the pre-existing, already-documented bends and are untouched by this rung — nothing here enters regalloc, because the new op is a system call with one def and one use. ///// ⚠ THE CPU COLUMN IS NOT USED TO CLAIM ANYTHING HERE, and that is deliberate rather than an omission: the previous row was measured from a DIFFERENT checkout path on a DIFFERENT binary, and this log's own standing warning is that the attributable measurement is always the same-path A/B and never the cross-row delta. A cross-row CPU reading on `phase:signatures` looked like a double-digit regression; it is not attributable and is not claimed. ///// ⭐ STEP-6b RAN (this rung adds an IR op, which is the trigger) AND FOUND ONE REAL CONSTANT-FACTOR COST, WHICH IS FIXED RATHER THAN FILED: the declaration sweep took a THIRD complete walk of every file's token array, because each compiler-owned layout probe (`StringIndex`, `CharacterSet`, and now `__ManagedFile`) had its own `fileNamesAnyToken` pass. Each probe genuinely needs a COMPLETE pass — the reason is a measured crash, not caution, since `recordScannedType` swallows method bodies — but completeness constrains what a walk VISITS, not how many walks there are. Fused into one `fileNamesProducerMask` that asks all three per token and exits once all three are satisfied: worst case one full scan where it was three, best case unchanged. Suite 2564/0 and fragments unchanged across the fusion, so it is behaviour-identical. | 862,833,023 | 1,628,801,677 | 3,142,974,388 | 6,134,120,080 | 12,369,645,010 | 25,283,783,570 |
| 2026-07-30 | D1b — RETAIN a managed payload bound out of a BORROWED union (+ refuse `and fallthrough` INTO a payload-binding arm). NEUTRAL, and this row carries the ATTRIBUTABLE measurement rather than the cross-row one. ///// ⭐ SAME-PATH A/B, BYTE-IDENTICAL AT ALL SIX RUNGS — measured, not asserted: `git checkout main -- Compiler/Parser.maxon`, rebuild, ladder; restore, rebuild, ladder. Same worktree, same path, same host, only the compiler source differing. Both read 588,251 / 1,057,073 / 1,993,397 / 3,863,167 / 7,607,234 / 15,087,230. Not one allocation moved. ///// ⚠⚠ THE DELTA COLUMN OPPOSITE IS A CROSS-PLATFORM ARTEFACT AND IS NOT A COST — READ NOTHING INTO IT. ⚠ NOTE ADDED AT REBASE: the row directly above this one is R4.1's, but this row's delta column was computed against D1e's row (now two above), because R4.1 landed while D1b was in flight. The delta is a property of the two rows COMPARED AT RUN TIME, not of the rows' final adjacency. That D1e row was measured on WINDOWS from C:\Users\Eric\dev\maxon-D1e; this row is arm64-macOS — a different OS, a different arch and a different checkout path, so the +426 to +223,409 allocs and -2.2M to -75.9M bytes are three artefacts at once. The proof it is not the rung is free and sits in this very run: THE PRE-RUNG COMPILER PRINTS THE IDENTICAL DELTA COLUMN, because the delta is a property of the two ROWS, not of the two BINARIES. That is the D1e row's own checkout-path finding reproduced across a platform boundary, one step stronger. ///// ⚠ AND THE LADDER STRUCTURALLY CANNOT SEE THIS RUNG: the corpus emits no `match`, so `declarePayloadBindings` — the one function the rung changed — is never reached at any rung. A Δ0 here is THE INSTRUMENT'S BLIND SPOT (the CORPUS blindness, which the CPU column cannot cure either), not evidence the mechanism is free. Its real cost is one `__mm_incref` per managed payload bound out of a borrowed union, bounded and unconditional by construction, verified in the emitted arm64 rather than argued: the payload load, then `bl __mm_incref` as the very next op, then exactly one `bl __str_decref` at the arm exit. ///// NO STEP-6b TRIGGER: per-rung totals x1.79 x1.88 x1.93 x1.96 x1.98, at or below x2.00 every rung and nothing near the 2.4 that would spend an optimizer agent — and a byte-identical A/B leaves one nothing to hunt. | 118,954,333 | 214,686,666 | 412,033,167 | 812,144,000 | 1,628,747,708 | 3,294,400,042 |
| 2026-07-30 | W1 — a function TYPEALIAS dropped `ExitCode`'s WIDTH, so an ExitCode-returning function VALUE trapped on wasm (`indirect call type mismatch`). One rule, three doors, now one. NEUTRAL. ///// ⭐ SAME-PATH A/B, BYTE-IDENTICAL AT ALL SIX RUNGS — measured by the coordinator, not inherited: revert BOTH changed compiler files to main, rebuild, ladder; restore, rebuild, ladder. Same worktree, same path, same host. Both read 588,336 / 1,057,178 / 1,993,542 / 3,863,392 / 7,607,619 / 15,087,935. Not one allocation moved, which is the expected shape — the fix replaces a tag with a wider tag on one already-taken branch and adds no allocation anywhere. ///// ⚠⚠ AND THE DELTA COLUMN OPPOSITE IS AGAIN NOT A COST, BUT ITS STORY IS DIFFERENT FROM D1b's AND WORTH READING. It shows +85 / +105 / +145 / +225 / +385 / +705 allocs, which GROWS with the rung (≈4 allocs and 54 bytes per added corpus file) — so the flat-offset checkout-path story the D1e and D1b rows document would NOT have covered it, and it must not be waved away with that argument. The pre-rung binary at this same path prints the IDENTICAL totals AND the identical delta column, so the growth is a property of the two log ROWS (a cross-checkout, cross-host per-file path cost riding a corpus that doubles), not of the two BINARIES. Third consecutive row where the attributable measurement is the same-path A/B and the cross-row delta is an artefact. ///// ⚠ CORPUS REACHABILITY, so a Δ0 is not misread as "free": the ladder emits NO function typealias, so the fixed door is never taken at any rung. It DOES emit 4 interfaces per rung, all returning a user ranged alias — those now route through the new shared `builtinTypeNameTag` and reach the identical `named` answer with identical allocations, which is what the byte-identical A/B also demonstrates. ///// NO STEP-6b TRIGGER: per-rung allocation totals x1.79 x1.88 x1.93 x1.96 x1.98, at or below x2.00 every rung and nothing near the 2.4 that would spend an optimizer agent; CPU x1.85 x1.92 x1.97 x2.01 x2.02, linear. A byte-identical A/B leaves an optimizer nothing to hunt. | 121,192,291 | 222,848,584 | 428,983,000 | 849,747,750 | 1,707,055,708 | 3,467,202,958 |
| 2026-07-30 | R4.2 — the rest of the file IO: `read`/`write`/`size`/`exists`/`delete`/`rename`/`stat`/`statField`/`statFree` + `openWrite`/`openWriteExecutable`, the 7 new StdOps with their 27 classification sites each, the RAII destructor, and the three `__ManagedMemory` members. Host-only, E3104 on every other target, per the R4.1 ruling. ///// ALLOCATIONS vs R4.1: +892 / +915 / +891 / +891 / +891 / +891 — FLAT, and the flatness IS the finding: the delta does not move by ONE allocation while the file count goes x8 across rungs 2-5, which refutes a per-file term outright. It is a per-COMPILE constant. `createEnumRegistry` seeds 16 more `__ManagedFileError` cases plus a whole 8-case `__ManagedMemoryError`, and it runs once per `EnumLayoutMap` CONSTRUCTION — three per compile (Queries.maxon:190, QueryDatabase.maxon:80, Project.maxon:2330) — never once per file. +891 is +0.006% of rung 5's 14.87M. ///// STEP-6b FOUND AND FIXED THE RUNG'S ENTIRE *GROWING* ALLOCATION TERM, AND IT WAS NOT WHERE THE PRE-OPTIMIZER READING PUT IT. Before the fix the delta read +2,201 / +3,544 / +6,160 / +11,440 / +22,000 / +43,120 — a term that DOUBLES per rung — and it had been attributed to the enum seeding riding a doubling file count. It was not that: the seeding is not per-file (above), and `phase:signatures` allocations are IDENTICAL before and after the fix at every rung. The growing term was `Parser.dispatchArrayMethod`, into whose TOP R4.2 hoisted `signatures.internArrayByteInstance()` so it ran for EVERY array method call in the program instead of only inside the `.managed` field arm — and each call built a fresh one-element `MaxonTypeArray` to key the instance registry with, i.e. A HEAP ALLOCATION PER ARRAY METHOD CALL. Memoized on `ProgramSignatures` (`arrayByteInstance` + a `computed` flag beside it, `TokenMemo`'s shape, because 0 is a real `GenericInstanceId`): the answer cannot change, since `GenericInstanceRegistry` only ever grows and interning is idempotent, and the FIRST ask is still the WRITE, so the registry contents and every id derived from them are exactly what they were. Removed -1,309 / -2,629 / -5,269 / -10,549 / -21,109 / -42,229 allocations — x32.3 across a x32 ladder — and ALL of it lands in `phase:parse` to the digit (rung 5: 3,096,945 -> 3,054,716 = -42,229, which is the whole-compile delta exactly). The rung's cost is now flat where it grew. ///// ONE MORE, CPU-ONLY AND SAID TO BE SO: `Parser.isManagedFileType` asked "is this a `__ManagedFile`?" by HASHING the name and probing the interner — INSERTING on a miss — on every method dispatch in the program, where its two siblings `isStringIndexType`/`isCharacterSetType` read the name behind the id (a dense-column index plus a byte compare) and mutate nothing. R4.2 widened its tag gate from `structRef` alone to `structRef` OR `named`, putting that probe on the enum and ranged-alias receivers too, one arm ABOVE the enum test whose own comment explains that it gates on the tag first precisely so the interner is not touched for a receiver that is not its business. It now uses the siblings' form. ITS ALLOCATION DELTA MEASURED EXACTLY ZERO at all six rungs — the probe was always a HIT in this corpus, so the exact column cannot see it and NO speedup is claimed; what is claimed is that a predicate no longer mutates the interner as a side effect. ///// NO SUPERLINEARITY. Per-phase ratios are at or about x2.00; `regalloc:splitting` and `liveness` are the pre-existing documented bends and are untouched — nothing this rung adds enters regalloc in this corpus, which contains no file IO at all. Read the manifest before reading anything into that: a zero from a ladder that cannot express the feature is not evidence about the feature. ///// NO CPU CLAIM ACROSS ROWS, deliberately: this tree's checkout path differs from the previous row's, and this log's standing rule is that the attributable measurement is the same-path A/B and never the cross-row delta. ///// createEnumRegistry RULING — NOT made demand-driven, and NOT filed as debt either. It is a flat per-compile 891 allocations (0.006%), measured above; the same suggestion was already REFUSED with a measurement at P1.7a slice 2b-ii, where an eager per-`Parser` layout would have turned a flat +68 into +1,430 at 143 files; and R4.1's reason stands — it is the one door reaching BOTH enum registries, and a handler resolves its `match e` through `Project.enumTypes`, so demand-driving it re-opens the two-registries-disagree wrong-answer class to buy nothing measurable. ///// Suite 2574/0, exit 0, no leak, and `specs-shv2/fragments/` empty-diff across BOTH changes, so codegen is byte-identical. | 862,908,033 | 1,583,162,725 | 3,075,092,362 | 6,111,855,776 | 12,246,272,024 | 25,160,090,569 |
<!-- scale-history:cpu -->

Since the suite was introduced, rung 5 has gone **36,897,948 → 14,509,321 allocations** (−61%) and
**2.86 GB → 1.52 GB** (−47%).

## Exponents — CLOSED 2026-07-14. Kept as history; no longer written.

**The tool no longer fits exponents, and no longer records time at all.** The row below is the only
one that was ever machine-written, and it is left exactly as it was measured. Deleting honestly-taken
numbers to tidy up a format change would be the worst of both worlds; they are simply not extended.

Two reasons it went, and they are the same reason twice:

**The ladder DOUBLES, so the ratio between two rungs already IS the growth.** x2.00 is linear, x4.00
is quadratic — read straight off the allocation counts in the tables above, no fit required. An
exponent could tell you nothing those numbers had not already told you, and it dragged in a residual,
which dragged in a NOISY verdict, which got an exemption written for it so it would stop complaining.
That is optimizing the gauge instead of the engine.

**And time can never be trended.** It is machine-dependent, so a dated column of it compares a loaded
box in July against an idle one in August. The `time /` half of every cell below is a fact about the
machine that ran it. (The compiler still times itself — `--log=compiler:debug` and `--metrics=<path>`
report per-phase milliseconds for one compile, which is where a clock is worth reading.)

What replaced it: `scale-test` prints per-phase **allocations and bytes at every rung**, with the
rung-over-rung ratio beside them. `regalloc:liveness` reads **x2.80** and `regalloc:splitting`
**x2.52** where every other phase sits on **x1.98** — the known-superlinear splitter, visible without
a single fitted number.

| date | change | phase:load | phase:lex | phase:parse | phase:merge | phase:resolveTypes | phase:semanticCheck | phase:lowerMaxonToStd | phase:pruneDeadBlockArgs | phase:elimTrivialBlockArgs | phase:foldConstOperands | phase:sugarGate | phase:isel | phase:regalloc | phase:prologueEpilogue | phase:runtimeAugment | phase:encode | phase:link | phase:writeExe | phase:emitIr | regalloc:criticalEdges | regalloc:blockOrder | regalloc:splitting | regalloc:liveness | regalloc:coloring | regalloc:ssaDestruction | regalloc:rewrite | aggregate |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 2026-07-14 | scale-test stopped measuring its own residue: the runner wrote each rung's executable and metrics.tsv INTO the rung source directory, which loadProject enumerates before filtering to .maxon, so every run after the first compiled two extra directory entries per rung (+26 allocs, +945 bytes). Outputs now live in the corpus root and stale files are pruned, so a cold and a warm .scale-tmp now agree exactly. The COMPILER is unchanged; every earlier row in these tables was measured warm and is offset by that constant. | 0.478 / 0.597 | 0.844 / 0.983 | 0.819 / 0.987 | 0.866 / 0.984 | 0.747 / 0.928 | 0.872 / 0.980 | 0.865 / 0.985 | 1.026 / 0.980 | 0.870 / 0.884 | 1.079 / 0.982 | 0.807 / 0.984 | 0.803 / 0.982 | 1.554 / 1.386 | 0.795 / 0.983 | 0.433 / 0.000 | 0.856 / 0.969 | 0.790 / 0.768 | 0.674 / 0.681 | n/a / n/a | 0.752 / 0.971 | 0.817 / 0.951 | 1.782 / 1.493 | 1.598 / 1.617 | 0.826 / 0.984 | 0.896 / 0.988 | 0.922 / 0.986 | 1.342 / 1.182 |

## Notes on the changes

The four earliest rows predate the automated log; their numbers are reconstructed from the diffs in
git, so they are accurate but were not written by the tool. They also predate the exponent table,
which is why it starts empty.

**`667ec9eee` — a name is a slice of the source, not a heap String.** By far the largest win so far.
Token text stopped being a heap `String` and became a zero-copy `ByteArray` slice into the source
bytes already in memory. Note the *shape*: rung 0 fell 14% but rung 5 fell 43%. That did not shave a
constant factor, it bent the curve.

**`4f81c7efe` — a byte string literal IS a ByteArray, and may be a global.** A bootstrap fix, not a
compiler optimization. `b"..."` at module scope resolved to an auto-created `__Array_i8` rather than
the stdlib `ByteArray`, which forced byte constants to be rebuilt per occurrence inside accessor
functions. The measurable win is small; the point was making the lexer's keyword table a byte-literal
map at all.

**`ac9e7db47` — for-in over an Array is an index counter.** `for x in arr` no longer allocates an
iterator object (a heap `ArrayIterator` wrapping a heap cursor). It lowers to an integer counter over
the backing buffer, in the parser, where the loop is built — replacing a post-monomorphization pass
that pattern-matched the CFG back into a loop and bailed on exactly the loops a compiler actually
runs (arrays of ops, blocks, tokens, strings).

**`EnumDummy` — a `try_call` on an associated-value enum stops allocating a placeholder.** The
biggest single win in the table: −24.6% of *all* allocations at rung 5. The bootstrap lowered a
throwing call returning a union into a heap-allocated dummy enum, a `select` between it and the real
result, and a decref of whichever lost — because a `try_call` returns null on the error path and the
lowering believed scope cleanup needed a real rc=1 allocation to decref. It never did: scope-end
cleanup already emits a null-GUARDED decref, and every managed slot is zeroed on function entry, so a
null slot is already the well-defined "nothing to release" case. On the success path the dummy was
allocated, increffed, decreffed and freed **without ever being read**. Null is what absent means, and
storing it is now the whole lowering.

The dummy was also *leaking* on the error path (see below), so this is a correctness fix wearing an
optimization's clothes.

**2026-07-16 — bootstrap constant resolver, not shv2: no table row moved, and none could.** This
change is in the **C# bootstrap's** cross-file top-level constant resolver, which `scale-test` does
not measure — the instrument compiles generated programs with **shv2**, and shv2 was rebuilt
byte-for-byte identically by the patched bootstrap (sha256 `1062b0d…`, forward *and*
`MAXON_SOURCE_ORDER=reverse`). The subject of every row above is unchanged, so there is nothing for
the tool to record. (On this arm64-macOS host the instrument cannot run at all: shv2's M1 backend
allocates for x64-windows only, so `RegisterAllocator.maxon` panics on the host arch — orthogonal to
this change.)

The bootstrap regression it fixes was real and superlinear. The rung `2408411dc` made cross-file
constant resolution order-independent by folding every reference against a **whole-program-visible**
list of constant declarations; `EvaluateConstant` looked a name up in that list with a linear
`FirstOrDefault`, once per reference — **O(references × constants)**, quadratic in a constant-heavy
project. Measured on a generated corpus of N cross-file constants with `--timing` (the bootstrap's
own per-phase instrument, since `scale-test` measures the wrong compiler here), the `preScan` phase
grew **×4.2 per doubling** at N = 2000→4000 (39 → 164 ms) — a clean quadratic. Replacing the scan
with a name→decl dictionary built once per file (first-wins, so a file's own declaration still
shadows a foreign one of the same name) makes each lookup O(1): that same step is now ×2.4 (15 → 36
ms) and the dominant term is gone. The O(files × constants) that remains is the *same* class as the
pre-existing `SeedFromModule` seed this diff never touched — sub-millisecond at the ~600-constant
scale of a real self-build, and quadratic before this rung too.

**2026-07-17 — the perspective-fold's cache was PER-PARSER, so a foreign perspective was rebuilt in
every parser that read it. Bootstrap again; no table row moved.** The follow-up `d7ed60d30` folds each
top-level constant in its DECLARER's perspective, resolving that file's private aliases; a foreign
perspective is built by `ConstantDeclSetFor(filePath)`, which scans the whole-program declaration list
once and memoizes the result. The memo was an **instance field** (`_constantDeclSetCache`), and a fresh
`Parser` is created for every file in every pass — so the SAME declarer's perspective was rebuilt once
per parser that folded one of its constants, across both the PreScan and Parse passes. On a deep
cross-file constant chain folded in a seeding-defeating order (which is exactly what
`MAXON_SOURCE_ORDER=reverse` exercises) the cascade re-derives each perspective at every level of every
parser: total perspective-build work grew **super-quadratically**, above the pre-existing and accepted
O(files × constants) baseline.

Measured with a temporary deterministic counter (perspective builds, and decls scanned across them —
exact, load-independent) on a generated chain of N single-constant files, `MAXON_SOURCE_ORDER=reverse`:
builds grew **×2.5–3 per doubling (~N¹·³)** and total decls scanned **×~4.5 (~N²·¹⁶)** — F, the number
of distinct perspective demands, was *larger than the file count* because the cache did not dedupe
across parsers. The fix threads ONE cache through every parser of a compilation
(`Compiler.CompileSources` owns it; scoped to the call, not static, so a reused module — the LSP's
cached stdlib module — never serves a stale set). After it: builds are **exactly linear (×2.00 per
doubling, one build per file)** and decls scanned is **×~4 = O(N²)**, the baseline class restored. At
N = 3200 that is 71.1M → 10.4M decls scanned (**6.85×** less), and reverse-order `preScan` wall-clock
went from **4.56× the parent at N = 3200 to 1.97×** — a bounded constant factor over the parent's own
O(files × constants), where before the gap *widened* with N.

It was a **latent** superlinearity, not a live regression: on realistic input the whole path is inert —
the stdlib (~600 constants) does **0** perspective builds and a real `maxon-shv2` self-build does **2**
(816 decls scanned), because real cross-file constant references are shallow and mostly fold from a
value already seeded by their declarer. Behaviour is byte-identical: **3041/3041** C# spec tests pass
forward AND `MAXON_SOURCE_ORDER=reverse`, `git status specs/ specs-shv2/` empty (codegen unchanged),
the shv2 self-build is bit-for-bit `1062b0d…` in both orders, and the three perspective/collision
reopen behaviours hold both ways (`cross-file-exported-reads-own-private` → 42,
`cross-file-exported-cast-to-own-private-alias` → 42, `cross-file-private-constant-name-collision` →
**122**, not 42). No run exited 101. (`scale-test` measures shv2 and cannot run on this arm64-macOS
host anyway — the bootstrap's `--timing` plus the temporary build counter are the right instruments
here, as with `0c974245e` above.)

**2026-07-18 — the arm64 encoder stopped building a `RegisterFile` per register operand.**
`scale-test` could not measure this: its corpus is heap/struct/global-heavy and the arm64 M1 backend
panics at `osAllocPages` on rung 0, so the ladder never produces arm64 data on this host. Measured
instead with `--metrics` on a synthetic 200-function scalar module (~36,800 ALU statements, which the
arm64 backend *can* emit), before vs after:

| phase | allocs before | allocs after | bytes before | bytes after |
| --- | ---: | ---: | ---: | ---: |
| encode | 123,130 | 10,913 | 2,636,989 | 841,517 |
| total | 1,527,795 | 1,415,376 | 115,908,333 | 114,109,634 |

**−112,217 allocations in the encode phase (−91%), and the total delta (−112,419) is the same number**
— so essentially every allocation removed was in the encoder. The cause: `arm64RegNumIsGpr` (the class
test every colored operand goes through, via `arm64GprField`/`arm64FpField`) read the GPR/SIMD boundary
as `arm64RegisterFile().firstVector`, **constructing a throwaway `RegisterFile` heap object to read the
compile-time constant 31** — once per register operand of every op, i.e. O(ops) pure-waste heap
objects. The x64 side never had this: `regClassOf` reads the `FirstXmmRegisterNumber` constant directly
and its comment says it does so precisely to avoid building a `RegisterFile` on the encoder's
per-register path. The fix names the arm64 twin (`Arm64FirstVectorRegister = 31`), so `arm64RegisterFile()`
and the encoder share one home for "31" and the hot path allocates nothing (the two non-hot per-function
readers, `arm64CallerSavedMask` and `arm64UsedCalleeSaved`, read the constant too). It is a constant-
factor win, not a bent curve — the count is still linear in ops — but the constant was ~110k objects a
scalar module. Codegen is byte-identical (the module emits the same 154,464 code bytes before and after;
`git status specs-shv2/fragments/` empty, so both the x64-windows and arm64-macos goldens are
untouched); the arm64 suite is **489/0**, exit 0 (no leak). x64 is provably neutral — nothing on the x64
path was touched and `arm64RegisterFile()` still returns the same value.

**2026-07-26 — the last two `BlockId`-keyed `Map`s became the dense table the other one already was
(PLAN #123 closed), and the ladder can see only a sliver of it.** `SemanticCheck.buildBlockByIdMap`
and `StdToWasm.BlockDispatchMap` are gone; both now read one `BlockIndexById` built by
`IrModule.blockIndexById` — the inverse of `func.blockRefs` — which is an instantiation of a new
generic `BlockKeyedTable`, and `BlockOffsetTable` is the other. Three hand-rolled copies of
array-pair-plus-presence-column would otherwise have existed; there is now one.

⚠ **`scale-test` structurally cannot see either converted site**, and the Δ0 it reports for them is the
corpus blind spot, not a result: `ScaleCorpus` generates no `async` (so `functionHasAwait` never opens
the gate) and `compileRung` spawns `build` with no `--target` (so the wasm backend never runs). Both
were measured directly with `--metrics`:

| ladder | phase | allocs before | allocs after | bytes before | bytes after |
| --- | --- | ---: | ---: | ---: | ---: |
| `Testing/ladders/genawait.sh 24 400` (x64) | semanticCheck | 2,248 | **712** | 3,191,376 | **258,744** |
| `scale-test --emit-corpus` rung3, `--target=wasm32-wasi` | encode | 123,468 | **116,604** | 24,043,224 | **21,799,335** |

**−1,536 allocations (−68%) and −92% of the bytes** on the await ladder; **−6,864 allocations** on the
wasm rung, and in BOTH compiles the whole-module total moved by exactly that phase's delta and every
other phase read Δ0 — so the attribution is complete, not inferred. The await ladder's `<ifs>` knob is
what names the mechanism: at a fixed 24 functions, allocations used to climb **1,864 → 2,056 → 2,248**
as blocks per function doubled — **+8 per function per doubling**, which is the `Map`'s rehash chain
(one `grow()` per capacity doubling, four fresh columns each) — and are now **FLAT at 712**, because a
pre-sized dense table is two allocations per function however many blocks it has.

**What the ladder DID move is `bytes:encode`, and only that**: −2,328 / −3,608 / −6,168 / −11,288 /
−21,528 / −42,008 down the rungs, with **allocation counts unchanged in every phase**. Divided by
`sizeof(CodeOffset)` those are 291 / 451 / 771 / 1,411 / 2,691 / 5,251 slots — exactly `131 + 160·2^k`,
a constant stdlib component plus a corpus component that doubles precisely with the rung, which is what
a per-chunk constant looks like. The cause is one line: `forBlockCount` now `reserve`s the value column
(`managed.grow(n)`, the exact ask) where it used to `growFilled` it, and `growFilled` routes through
`grownCapacity` → `atLeastMinimum`, which floors at `Array.MinimumCapacity` = 4. So a chunk of fewer
than four blocks no longer rounds its offset column up to four slots. `phase:encode`'s byte curve is
x2.03 before and after — the constant moved, the exponent did not.

⭐ **And the measurement worth keeping is the one about `spreadHash`, because it runs the other way.**
Replaying the real key sequences of a 2,808-function corpus through `Map`'s exact algorithm, these two
maps' keys arrive **perfectly ascending in 2,808/2,808 and 24/24 instances** — Std-tier and Maxon-tier
`blockRefs` layout order IS id order, because the interleaving run comes from the register allocator's
critical-edge split, which neither site sees. That is the case an identity hash gets *perfect*: **1.000
probes/insert**. Under today's `spreadHash` the same keys cost **3.088** and **3.062**. The class-level
mixer is right — it is what makes the trap unreachable for any key order — but on a dense id run it
*costs* about 3× the probes it insures against, which is why the per-site "tidy O(n)" #123 always
preferred was still worth taking after the mixer landed, and not instead of it. Codegen-neutral:
`specs-shv2/fragments/` empty after full runs on arm64-macos AND arm64-linux; shv2 **1591/0**
arm64-macos, **1586/0** wasm32-wasi, **1589/0** arm64-linux, C# bootstrap **3104/3104**, all exit 0;
`--workers=1` byte-identical to `--workers=12`.

## Bugs this uncovered

Removing the dummy exposed a leak it had been half-hiding, and the leak in turn exposed a hole in the
test suite. Both are fixed; both are worth knowing about.

**A routed try-block call leaked one reference per call.** A bare throwing call inside a
`try 'blk' … end 'blk' otherwise (e)` block has its result hoisted into a `__try_block_result_N` temp
that receives the callee's *transferred* reference — so that temp owns it and must release it.
`VarRegistry.KeysSince` excluded exactly those temps from scope-end cleanup, on the theory that a
downstream `let x = <call>` aliased the same slot without increfing. That was true for a struct
return, which was separately handed a `CallReturn` `__call_tmp_` that turned the alias into a *move*,
and false for an associated-value union, which was handed nothing and so increfed like any other
alias. Both special cases are gone: the try-block form now works the way every single-statement `try`
form already did, and the temp that receives the reference is the temp that releases it.

**80% of the spec suite could not see a memory leak.** `mm_leak_check` overrides the process exit code
with 101 when an allocation is still live at exit. But 2311 of the 2886 tests are compiled together
into one batched binary whose per-test verdicts are parsed from stdout markers carrying each test's
own `main` return value — and `TestRunner` discarded the batched binary's process exit code entirely.
The leak checker ran, printed, and had its verdict thrown away. A leaking batched test still emitted a
full set of passing markers. The runner now reads that exit code; because the leak counter is
process-global and cannot say *which* test leaked, a non-zero batch exit invalidates the batch and
re-runs its tests individually, where each one's own leak check attributes it.

## What the profile says is left

From `scale-test --per-type`, which attributes every allocation to the TYPE allocated *and* the SCOPE
that allocated it. **The scope column is the one that finds things** — a `String` row can never tell
you that 150 of them came from `emitFixedToken`.

| what | share | note |
| --- | ---: | --- |
| `OpIndexList` / `BlockRefList` | — | **DONE, and this row is kept only so the claim is not re-derived.** It said `IrBlock.opRefs` and `IrFunction.blockRefs` were *linked lists of integers* costing a heap node per op reference plus a `ListIterator` per traversal, and called `Array` the biggest single win available. They ARE `Array`s now (`IrBlock.maxon:150`, `IrFunction.maxon:110`); the win was taken. Spotted 2026-07-26 by the encode-quadratic pass, which went looking for this ~17% and found it already gone. |
| `stepBackwardOverOp` et al. | ~7% | A superlinear cluster inside the register allocator (fitted exponent ~1.47), not a constant factor. |

`ArrayIterator` (once the single biggest allocating scope, at 102,845) and `EnumDummy` (once ~20% of
all allocations) no longer appear.
