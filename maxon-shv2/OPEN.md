# Open findings — maxon-shv2

**What this is:** the live backlog of *bugs and gaps*, as distinct from `PLAN.md` (which owns the
**ladder** — what to build next). Everything here was found by **running code shv2 did not write**, and
almost every one is a **wrong answer, not a crash**. Recorded 2026-07-14.

> **The through-line, and the thing to internalise:** nearly every real bug below is **one fact written
> down twice**. A promise's error type distilled into two bits, with the type discarded. A return op
> derived from the token *and* from the declared type. The spec pool's parent and worker each deciding
> which tests a spec selects. The ring's size in three places. An MCP tool described twice — which is
> what taught a *model* the wrong thing about `scale-test`, and cost the most. **When two places must
> agree and nothing makes them, they will drift, and the failure is a wrong answer.**
>
> **⭐ AND ITS OTHER FACE, added 2026-07-14: the duplicate that does not drift, it just COSTS.** The
> query spine's dependency graph was a reverse index **plus** a flat array of every edge — and nothing
> ever read the array. It could not go wrong; it went **quadratic**, because `clearDepsFor` rebuilt it by
> scanning every edge in the project and rendering each one to a `String` to compare it. **The same
> question finds both: is this fact stored twice? If yes, either they disagree (a wrong answer) or one is
> dead weight being maintained at cost.** (The error-code registry, #12, is the drifting kind; this is the
> costing kind. Both are the same question.)
>
> **⭐ AND ITS CURE, learned 2026-07-16 (#23): WHEN TWO PLACES MUST AGREE, MAKE THEM CHECK EACH OTHER.**
> The iatCall bug — `implicitDefs: 0` under a comment reading *"full call-barrier semantics"* — was fixed not
> by an instrument but by **`assertCallClobberConsistent`**, which compares the op metadata against
> `RegisterAllocator.callerSavedMask` on every `allocateRegisters` and panics naming the drift. **Verified by
> trying to put the bug back: it no longer compiles anything.** The bug is **unrepresentable, not fixed**.
> Three green gates had sat on it. ⇒ **A check between the two copies beats every gate, because a gate can be
> pointed at the wrong program and a check cannot.** (`maxon error-codes check` is the same move — see #12,
> which is closed, and which this file went on claiming was open.)
>
> **⚠ A STANDING RULE, from the same rung: a WHOLE-PROGRAM query placed UNDER a per-file one is asked
> ONCE PER FILE.** Anything O(files) inside it is therefore **O(files²)**. Two quadratics hid behind
> exactly that, and were found only because a new query made the scale-test *delta* grow **x3.4 per rung
> on a ladder that doubles** — a linear change cannot do that. Before adding work to such a query,
> multiply by the file count.

---

## 🔴 Wrong answers — a correct program compiles and returns the wrong number

### 1. ✅ FIXED 2026-07-14 (`SignatureIndex.maxon`) — cross-file callee types deferred into an unsound ACCEPT (`isReady() + 41` compiled and returned 42). Fixed UPWARD by a whole-program signature index (`queryProgramSignatures`) so `unresolved` means exactly one thing: a callee NO file declares (→ E3004, unreachable at codegen). Memo re-keyed on `mix(contentHash, ProgramSignatures.hash)` + a third `invalidation` property on `verify-warm-rebuild`. **LESSON: the `.test` multi-file limit was a lie about the FORMAT — 650 `// --- file:` occurrences already existed; shv2's `SpecParser` just didn't read it. Check the corpus before believing a claim about the corpus.**

### 2. ✅ FIXED 2026-07-15 (= P1.0d.3) — `a / 0` escaped as `0xC0000094` with empty stderr; now exit 1 via a VEH `#DE` thunk + a symbolized backtrace (suite 279→281, first runtime). **LESSON: a VEH fault handler, not a per-`idiv` divisor check (the cheap-path scope cut); FRAME POINTERS on every function (forced by P1.5's `__gt_morestack`); a ` ```stderr ` fence for RUNTIME stderr.** ⚠ Still open, inverted: `SpecParser` now last-block-wins on a *second* ` ```maxon ` fence in one region — wants a policy call (panic on duplicate fence?), no spec trips it today.

### 3. Awaiting one promise TWICE double-frees its managed result
`mm_decref: refcount underflow (already zero)`. The payload is handed out as an owned **+1 per await**
though the thunk owns it once. **Not** an error-type bug — a *non-throwing* promise with a managed
result double-frees identically.
⇒ A language decision: make `await` **linear** (a promise is awaited once, the compiler rejects a
second), or incref per await. **LOAD-BEARING FOR P1.5** — decide it *before* shv2 ports `async`,
alongside `Promise with (T, E)` (see PLAN.md). A promise that carries its error type **and** has a
defined await-arity has no representable version of either bug.

### 17. ⚠ BOOTSTRAP BUG — the reference compiler ALIASES two file-private `var`s of the same name into ONE `.data` slot
Three files, each `var counter` file-private with a different value; `getA()+getB()` expected 107, **got 200** — the later file's initializer wins the slot and both readers get it. Compiles clean, no diagnostic, a silent wrong answer. Root cause: the name RESOLVER is file-scoped (a cross-file read of a non-exported decl is a correct E2004) but the `.data` LABEL is global-by-name — *"which variable is this?"* answered in two places that disagree. **⚠ The corpus structurally cannot see it**: `top-level-let.md`'s `file-private-same-name-cross-file` pins the shape but for `let`, which is INLINED (no label to collide); there is no `var` twin.
⇒ **shv2 did NOT port this** (confirmed at P1.0d.5b, #4e): identity is per-FILE by mechanism. Kept as a documented, still-live bootstrap wrong-answer.

---

## ⚠ Gaps the plan never listed — all found by the corpus

### 4. ✅ FIXED (= P1.0d.5) — top-level `var` (globals) AND top-level `let` were missing entirely (both `E2015: Unsupported: top-level …`). `let` = P1.0d.5a (281→295, front-end only, compile-time-evaluated + inlined), `var` = P1.0d.5b (295→317, the Std memory band `globalAddr`/`loadIndirect`/`storeIndirect` + a `.data` section). 118/276 `/specs` files use a top-level decl. See #4e for closure detail. **LESSON: check the corpus for the feature's own spec by NAME (`top-level-let.md`) before believing it has none.**

### 4b. ⚠ Three things P1.0d.5a left open (independent review/optimizer, deliberately NOT fixed)
- ⭐ **The constant DFS's recursion depth is UNBOUNDED — SIGSEGV (139), no diagnostic, DEFEATS `E2012`.** Depth 700 compiles; 800 SIGSEGVs; a cycle of 800 faults before `recordCycle` raises E2012. Depth, not count. INHERITED (shv2 is strictly better — the bootstrap stack-overflows at 700). **Fix = an explicit WORKLIST (its own rung, in BOTH compilers); the cheap dependency-graph pre-scan is a THIRD evaluator — do not.**
- **A DUPLICATE top-level constant is silently accepted** (`let A=1`/`let A=2` → 1; a duplicate function is E3006). Needs a spec case.
- **Assigning to a constant reports the WRONG code** (`A=5` on a top-level `let` → `E2004 Undefined variable`; should be `E2013 ParserImmutableVariable` — it is defined, just immutable).

### 4c. ✅ FIXED (P1.0d.5a) — `verify-warm-rebuild`'s battery could not see whether a constant's VALUE rides the parse-memo key; a sabotage deleting `mixConstant`'s value arm stayed green. Added property 3(c) (`let __warmRebuildProbeConstant = 7 → 8`), red on the sabotage. **LESSON: both prior probes ADD A NAME, and a name-only key moves on that — only an edit that holds the NAME fixed and moves the NUMBER discriminates. When a gate asserts a property, ask what edit would break it, and make it.**

### 4d. ✅ CLOSED 2026-07-16 (`74ea57d1f`, `726a606f1`) — the scale corpus was systematically blind to the feature just landed (3rd instance). Five knobs added (`globalConsts`/`globalVars`/`structTypes`/`structAllocs`/`intDivide`), every one doubling per rung, no compiler code changed. **LESSON: counting an OPERATOR ≠ counting its TYPE (every `/` in the corpus was a FLOAT `/`, a different Std op from integer `/`); an undeclared gap reads as coverage — every construct now appears under EXACTLY ONE manifest heading, a partly-generated one names which part. SCOPE: `scale-test` COMPILES each rung, never RUNS it — the emitted program's runtime is measured by nothing here.**

### 4f. ✅ FIXED (P1.0d.5b) — a real Θ(globals²): `GlobalDataTable.sortBySizeDescendingStable` scanned to the first strictly-smaller entry, but `storageBytes` is only {1,2,4,8}, so all-`int` globals never stopped early. Fixed to four appends-in-order, O(n). Measured off-instrument: 8000 globals 619ms→166ms. **⚠ DURABLE LIMIT: `scale-test` was bit-for-bit identical across the fix — it collects MEMORY only, deliberately (time is machine-dependent). A TIME-only superlinearity is invisible to it and always will be — NOT a defect, NOT a reason to add a time column. For that, measure off-instrument and say so. And: a complexity defended by what programs happen to look like is a bug waiting for a program.**

### 4h. ✅ FIXED 2026-07-15 (`make-copies-check`) — three cures for ONE FACT WRITTEN TWICE, ranked (reach for them in this order): **1. DERIVE IT** (write the fact once — `ShiftCountBits - 1`; an assert is a consolation prize for a fact you could not derive); **2. ASSERT IT** when derivation is refused (`assertRegisterFileMatchesEnum`, +2 allocs/+296 B flat — the SAME call cost +5.6M allocations on the per-op path; position is the whole difference); **3. MAKE IT A REQUIRED PARAMETER** (each corpus knob answers a required `driver RungDriver` — a knob that forgets it COMPILES NOTHING). Closed the 14th (chain) driver too (cure 1: read the fact off the generator, byte-identical). **⚠ Sub-lessons: ask `value == Type.case`, not `.rawValue != n` (E3097; a narrowing cast that launders a line is one nobody can safely tidy); order checks most-primitive-first (a derived check outrunning the primitive one accuses the wrong party); `TargetDialect.ShiftCount = int(0 to 63)` is a FORCED third copy (a ranged typealias's bounds must be literals) and says so.**

### 4g. ✅ FIXED 2026-07-15 (`make-copies-check`) — EIGHT "a comment describing a compiler nobody wrote" instances found + fixed (dead `classifyGlobalLabel` with zero callers DELETED; `PeWriter`/`TargetPrinter`/`TargetLiveness`/`RegisterAllocator` comments citing wrong bounds or nonexistent asserts; a stale "u16 register mask" since P1.0d.4 doubled the file to 32). Each now names the type (`RegMask`) rather than respelling a width. **LESSON: a number in prose is just another copy; a comment true when written and never re-read when the code moved is the drift.** ⚠ Also: `parseTopLevelAssignment`'s `let` arm cited a `disabled-test:` pin — a citation is not a test, check the pin is ENABLED (added `top-level-let-scalar-reassign-error`).

### 4i. 🔴 OPEN BOOTSTRAP BUG — E3097 (`SemanticEnumAccessorComparison`) is fully defeated by any NARROWING `as` cast
A safety check a one-token edit turns off. `let o = c.rawValue as Ordinal; if o == 1` compiles and runs — the exact case-test E3097 forbids, and adding an enum case would NOT force that site to handle it (the silent-slip-through it exists to prevent). A WIDE cast does NOT defeat it (elided) — the tell: what hides the accessor is the range-check node a narrowing cast inserts, so the check is keyed on syntax rather than on the question. **NOT fixed here (maxon-sharp code, needs the full C# suite as its gate) — its own rung. shv2 doesn't emit E3097 yet (`notEmittedBy: [shv2]`) but will need the rule at semantic checking, keyed on the QUESTION not the token shape.**

### 4e. ✅ CLOSED — top-level `let`+`var` both DONE (P1.0d.5a 281→295; P1.0d.5b 295→317). ⭐ **shv2 does NOT inherit #17's aliasing bug: identity is per-FILE BY MECHANISM** (`fileScopedDeclKey(name, readerFilePath)`, label = bare name + `$1` only on collision). Wrote `specs-shv2/global-file-private-same-name.md` (shv2 returns 118 where the bootstrap returns 212). ⚠ **`loadIndirect` MUST stay `isPure: false`** (else a global read hoists out of a loop that writes it) — but `isPure` has ZERO readers, so the spec verifies the ANSWER, not the flag (see #27). The `short-circuit-elision.md` divide-by-zero workaround was retired (real cases moved to `short-circuit-lowering.md`, git R100 renames = the codegen didn't move).
> **⚠ THREE BOOTSTRAP BUGS P1.0d.5a tripped over, each still real (worked around by convention/binding):** (1) a param named **`end`** silently destroys the enclosing type's member table → 68 cascading `E4006` hundreds of lines away (dodged by convention `endIndex`/`countEnd`; `type`/`enum`/`union`/`interface` were fixed 2026-04-11, `end` was not); (2) `throw` of a union payload matched from a CALL RESULT → `mm_incref NULL` (bind scrutinee + payload to locals; same family as #14/#16); (3) `self.declAt(i).outcome = x` → `E2001` (no field assignment through a call result).

### 5. ✅ FIXED — the `while` half of an O(V·B) parser blowup: `parseWhileStatement` minted a header phi for EVERY mutable var in scope, unconditionally, before the body was parsed. Fixed by deciding the carried set UP FRONT from the tokens (`namesAssignedIn`) — a loop carries a phi only for the mutable vars its extent ASSIGNS; goldens didn't move (`pruneDeadBlockArgs`/`elimTrivialBlockArgs` were already deleting the surplus). Scale bytes x2.17→x1.99. **LESSON: EAGER minting is what makes `parseIfStatement`'s value-id snapshots true (a lazy phi retroactively changes a value already captured on the Maxon call stack); the `if` half is left alone (its snapshots mint no ValueIds). The BYTE column caught what the ALLOC column could not — allocations bigger, not more numerous. Corpus was also `and`/`or`-blind — FIX THE CORPUS'S REALISM, NOT THE PROBE.**

### 9. ⚠ STILL OPEN, UNREPRODUCIBLE — the `mm_incref NULL` in `__module_init` does not reproduce
Reported (2026-07-13): a `Testing/`-only union-typed `Array` made the bootstrap crash `__module_init` blaming the Lexer's `b""`-keyed keyword map. A differential against the pre-fix bootstrap shows **the reported combination passes even BEFORE the fix** — so the symptom remains unexplained. A *related* miscompile of the same class WAS fixed (`bf2823bcb`): `DeadFunctionElimination` hand-rolled its operand list (5 op kinds of ~80), so `maxon.enum_construct`'s payload looked dead and was deleted; `MaxonOp.Operands` is now **`abstract`** so a new op kind cannot reintroduce the hole. **⇒ Do not assume it is gone. If it recurs, the differential table in git history is the starting point. LESSON: the repro is the asset — record it, not just the symptom.**

### 18. ✅ DONE 2026-07-15 (P1.0d.4, floats) — suite 317/19→355/0, 38 goldens added, ZERO modified. ONE `X64Register` enum (xmm at rawValue 16-31); class-agnostic OPS, class-dispatched ENCODERS; the class column travels forward from the lowering. **⭐⭐ THE CONTRACT WAS WRONG TEN TIMES, all one shape — I specified TYPE surfaces and not OPERATIONS** (a register enum with no instructions, `trunc` with 0 hits, `classPoolSize` returning the file's width not the pool's). **⭐ `Parser.mintPhi` hardcoded `argType: i64` and its comment NAMED THIS RUNG — the rung gave it the consumer and not the reason; `var f = 0.0` in a loop crashed the emitter, found only by fixing the instrument. READ THE COMMENT THAT NAMES YOUR RUNG; Step 0 is the SPEC PORT LIST.** Deferred at the time (now all resolved): the float ABI (float param/return) = #50/#57.

### 19. ⭐ `/specs` CONTRADICTED ITSELF on lossy narrowing — half FIXED, half OPEN
`type-casting.md` said *"lossy conversions are not allowed"* (`5.0 as int` → E3009, "use trunc/round/…") while `implicit-type-conversion.md` truncated `takeInt(3.7)` → exit 3. ✅ **FIXED for float→int** (spec + shv2 + bootstrap), rule drawn WHOLE (a coercion site is anywhere a value meets a declared type, so `return f` too — which panicked the x64 emitter one site over). The bootstrap disagreed with ITSELF (`CoerceValueToExpectedKind` rejected, `ConvertArgToParamType` truncated) — which is what proves it was a bug, not a design.
**⚠ STILL OPEN — `f64 → f32` coerces implicitly in BOTH compilers.** Also lossy, silent, absent from `type-casting.md`'s E3009 list — the same bug wearing a different pair of types. **⚠ ALSO: three rules answer three questions about one tag pair** (`typesAgree` · `checkDeclaredType` · `comparableOperands`) — **deliberately NOT unified** (merging any two collapses them into each other's bugs). Do not "simplify". **LESSON: a compiler passing a spec proves only that it implements what the spec says, contradiction included — read the EXPECTATION, and when two specs could cover one rule, grep for the other one.**

### 20. ⚠ arm64 / wasm compile-error fragments cannot be regenerated on a Windows host — STRUCTURAL
`spec-test --target=arm64-macos` crashes (`Win32Exception 193`): the runner cross-compiles then tries to EXECUTE. A compile-ERROR test still emits its fragment (never executed), so P1.0d.4 left `specs/fragments-{arm64-macos,wasm32-wasi}/` short by exactly one executing file, and hand-writing it was refused (needs an on-target run). Same class as [[project_arm64_pending_verification]].

### 82. ✅ FIXED 2026-07-21 (`a635f2b8c`, 922→924/0) — x64-linux SIGFPE fault runtime; divide/mod-by-zero un-gated
x64-linux now installs a SIGFPE handler (`X64Runtime`'s linux arm of `appendX64RuntimeChunks` + `prependLinuxEntryStub` calling `mrt_runtime_init`) that prints `panic: integer divide by zero` + a symbolized backtrace and `exit_group(1)` — the Linux twin of the Windows VEH runtime, REUSING its 4 OS-neutral chunks (fault_backtrace/walk_frames/print_frame/write_hex) and differing only in the stderr writer (write syscall), the fault entry (a handler reading the ucontext gregs RIP@168/RBP@120/RSP@160), and `runtime_init` (rt_sigaction). `safety.md`'s divide/mod-by-zero widened to `x64-windows, x64-linux`; workers-invariant, Windows byte-identical (957/0). ⚠ **THE LESSON: x86-64 REQUIRES `SA_RESTORER` + a valid `sa_restorer` trampoline** (`mov eax,15; syscall` = rt_sigreturn); without it the kernel raises a KERNEL SIGSEGV building the signal-return frame BEFORE the handler runs — even for a handler that never returns. Found by **strace** (SIGFPE → `SIGSEGV SI_KERNEL`), no v1 reference (v1's x64-linux was compile-only). The scalar range guard lowers to a bare `exit_group` (not `mrt_panic`), so the Linux runtime omits the software-panic chunk (see #86).

### 83. ✅ FIXED 2026-07-21 (`cd0f6ac2a`) — POSIX mmap constants promoted to a shared home
The 5 POSIX-universal mmap parameters (base/prot/anon-private-linux/fd/offset) now live in `Targets/Shared/PosixMmap.maxon`, consumed by BOTH Linux isels (x64's `X64LinuxRuntime` and arm64's `StdToArm64Conversion`); arm64-macOS keeps its Darwin `MmapAnonPrivateMacos = 0x1002` locally, the one target that needs it. Values unchanged ⇒ codegen byte-identical (957/0 + 922/0, no fragment moved). The drifting "one fact twice" the independent review flagged is now one fact.

## 🔧 Instrument & tooling

### 7. ✅ FIXED for floats at P1.0d.4 (after FIVE rungs, hiding a compiler crash) — the corpus was blind to the feature just landed; `ScaleCorpus` PRINTED its own blind spot yet a float rung measured byte-identical. Grew `floatOps`/`floatSpill` knobs that double like every other. **LESSON: the rung process GATES THE OPTIMIZER on `scale-test`, so extending the corpus is a PRECONDITION of the optimize step; making the instrument SEE is the OPPOSITE of silencing one it has. The corpus is grown to expose the LAST bug, so it is blind to the NEXT — read the manifest's printed blind spots before trusting a zero.**

### 6. ✅ FIXED (twice) — the scale instrument kept silently dying on its own leftovers (metrics/exe written into the measured dir → cold/warm disagreed +26 allocs; `.scale-tmp` only ever written never deleted → the by-SCOPE table worked only on a fresh checkout, and the pass SKIPPED WITH EXIT 0). **RULE (so there is no third time): every directory the instrument generates must be reconstructed from scratch each run, and anything it did not generate this run must be refused or removed — never inherited. Both failures were silent.**

### 8. ✅ FIXED (`b896a70bd`) — the DebugStream torn-read race was closed (`4cfbba70d`), but adding the commit bit was a BREAKING WIRE CHANGE that never bumped `DsVersion` (written by the monitor, read by nobody): a pre-fix producer under today's monitor reported `0 decoded, 283221 dropped` for a run that exited cleanly with code 42. Fixed to `DsVersion = 2` + a two-way handshake (mismatch → loud refusal, exit 3). **LESSON: an instrument that lies is worse than no instrument — and shv2's backend must emit this protocol (Workstream R1).**

### 7. ✅ FIXED (`282d08421`) — `StdOp.const` claimed `clobbersFlags` (justified by an `xor reg,reg` idiom the lowering never emits) and `TargetOpMeta.setsFlags` was written 40× / read 0×. `const` now declares `clobbersFlags: FALSE`, dropping FOUR instructions per short-circuit site; `setsFlags` DELETED (a v1 port artifact — shv2 moved the fusion scan up to the Std tier and kept v1's dead field). **⚠ The fusion rests on an unstated precondition: the allocator inserts Target ops into the window the Std scan proved safe — it survives only because every insertable op is `mov`-class/flag-neutral. Add a flag-writing one and the fusion is a silent miscompile the Std tier cannot see coming.**

### 7b. ✅ FIXED — `a shl -1` silently became `a shl 63` (E2054, both compilers). A shift-count LITERAL outside `0..63` is now a compile error; a runtime shift still masks (pinned, so the rule can't be "fixed" into over-rejection). **⚠ The bug underneath was #12 again: `0..63` written twice (`FoldConstOperands.MaxShiftCount` + `TargetDialect.ShiftCount`) with the front end enforcing neither.** Now ONE declaration (`TypeRules.MinShiftCount`/`MaxShiftCount` + `shiftCountInRange`); `TargetDialect.ShiftCount`'s `int(0 to 63)` is the one forced restatement (a ranged typealias can't be bounded by a named constant). Also caught `EvalConstShift` doing `1 shl 100 → 1 shl 36` through C#'s own `<<`.

### 12. ✅ FIXED — the error-code registry was written down twice (`ErrorCode.cs` + `ErrorCode.maxon`), and two agents collided on E3099 on the same day. `docs/error-codes.txt` is now the single source of truth; `maxon error-codes generate` produces four files; `maxon error-codes check` fails the build on a duplicate/drift/dead-claim wherever a generated file is USED. **⚠ LESSON ABOUT THIS FILE: a backlog whose through-line is ONE FACT WRITTEN TWICE was itself carrying this entry as "open" long after CLAUDE.md documents it fixed — the copy nobody executed went stale. An entry here is not evidence; check it before you act on it.**

### 8. ✅ FIXED (`b443f497e`) — the bootstrap could not CALL a function-typed FIELD (indirect-call lowering existed only in `ParsePrimary`'s bare-identifier branch, so a function-typed *parameter* worked and nothing else did). Fixed by giving the postfix chain a call arm — a function value is callable wherever it lands. Behind it was a bigger bug: #13.

### 13. ✅ FIXED (`99a8927b9`) — a capturing closure that ESCAPES its frame nil-derefs (closures capture by reference → a dead frame). E3099 generalized from the struct-FIELD partial fix to the rule itself: **a closure that CAPTURES may not ESCAPE its defining frame**, refused on every route the parser sees. The sweep of the OPS (not the four listed routes) found TWO silent routes — a union assoc-value PAYLOAD and a payload BINDING (an alias into the enum's heap box). Also fixed a latent FALSE reject (capturing-var map keyed by NAME, never cleared between functions). **⚠ STILL OPEN ON PURPOSE — the INTERPROCEDURAL route** (a capturing closure passed as a call ARG to a callee that stores it, or arriving as a RETURN value): needs a per-parameter escape summary over the call graph — **exactly shv2's P1.5**, where capture-into-heap IS escape and closures co-land with `async`.

### 14. ⚠ BOOTSTRAP BUG — LEAKS on a ternary whose arms mix a BORROWED read with an OWNED value
`let t = entry.returnType if remap.isIdentity else remapMaxonReturnType(...)` → `MM leak: 7 allocation(s) remain`, exit 101, every build. The conditional does not reconcile the two ownerships; written as `if`/`else` it is clean. Fails LOUDLY (leak gate), the only reason it is tolerable. **⚠ shv2 will hit this too — a ternary is the natural way to write "remap only if the ids moved".** (Cost a wrong diagnosis first — the plausible suspect wasn't it. Verify your own diagnosis.)
> ### ✅ FIXED IN SHV2 2026-07-20 — the borrowed-aggregate merge arm now REJECTS cleanly (E2015), no exit 101 reachable.
> A borrowed non-text AGGREGATE arm merged into an OWNED result phi leaked (exit 101), reachable identically via the inline conditional (`#71`) AND `match … gives` (both `record(e.kind if identity else remapKind(e.kind))` and its match twin exited 101). **ROOT was `Parser.maxon` `promoteBorrowedGive`**, which returned a borrowed non-text aggregate give UNPROMOTED when the result phi is owned — its comment's "the type system will reject it" was FALSE for a same-type borrowed-vs-owned aggregate — so the owned phi dropped a value it doesn't own. **FIX: `promoteBorrowedGive` now `throw`s `ParseError.unsupported` (E2015) on a borrowed non-text aggregate give into an owned phi** (a String give still promotes via `promoteToOwnedString`); `throws` propagated through `resolveGivesOwnership` → `finalizeMatchMerge`. The non-text-aggregate boundary is named once (`valueIsNonTextAggregate`) and shared with the identical `return <borrowed aggregate>` refusal (`emitOwnedValueReturn`). Specs: the disabled `ternary-expression` borrowed-arm cases (`borrowed-arm-selected`, `chained-arms`) are enabled as clean E2015 REJECTS; `owned-arm-selected` — an ENABLED runtime test that dodged the leak only by `identity=false`, using the SAME refused construct — was repurposed to a reject too; a `match … gives` twin (`match-owned-value-flow/match-borrowed-aggregate-give-refused`) pins the shared merge. **x64 882→887/0, wasm 869→874/0.** ⭐ The implementer's OWN review caught a REACHABLE PANIC it first introduced (a `panic("unreachable")` reached by an owned-String arm merged with an `unresolved` undeclared-call arm — `unresolved` agrees with everything in `typesAgree`, empty aggregate name — turning a clean E3004 into a crash); fixed by DEFERRING an `unresolved` give (the doomed program is E3004-rejected before codegen), guarded by `*owned-string-arm-undeclared-call` specs. `valueIsManagedHeap` derived from `valueIsNonTextAggregate`; `mintOwnedCallResult`'s inline copy collapsed. ⏭ Golden-free follow-up: `emitOwnedValueReturn`'s return message uses `typeTagName` (prints `int`/`struct`) — upgrade to `aggregateNameOf` (the precise name the new give refusal uses). ⚠ The **bootstrap** still LEAKS this shape (the original #14 finding, a maxon-sharp bug) — untouched here, out of scope. Consuming/copying a borrowed aggregate across the merge stays deferred to cross-call consume (P1.5).

### 80. 🔵 P1.5 #71 FOUND — two REUSED merge diagnostics say "match" for a TERNARY
The ternary shares `finalizeMatchMerge`, so a cross-register-class ternary → E2015 "a match expression whose arms…" (OPEN #31) and a mixed-type ternary → E3005 "match arms give incompatible types". Honest (shv2 reuses the check) but user-facing wording is wrong for `a if c else b`. Making them construct-agnostic touches `match-expr-divergent-class.md` goldens. Small follow-up.

### 81. 🔵 P1.5 #71 FOUND — `finalizeMatchMerge` does NOT compare function SIGNATURES (match + ternary)
A mismatched-signature function value merges silently through a `match … gives` or ternary (`unary if c else binary` compiles + returns 42; the match twin too). `finalizeMatchMerge` agrees on the `function` CLASS but not the signature. Shared with match, not a ternary gap. Its own signature-agreement rung (disabled `function-arm-signature-mismatch` needs it).

### 87. 🔵 P1.5-B1a RESIDUAL — the GT STRUCT (~80 B) bump-leaks per spawn (the stack is now recycled; the struct is not)
The B1a stack free-list (`__gt_stack_free_head`) recycles the catastrophic 1 MiB stack per spawn, but the ~80-byte `GreenThread` STRUCT is `__slab_alloc`'d and NOT recycled. It CANNOT be, at this rung: the struct must stay RESIDENT after completion so a `await p; await p` RE-AWAIT still reads `p.result`/`p.status` (the review's finding, guarded by a spec). And the bump slab has no free list anyway. So a spawn/await loop still leaks ~80 B/spawn (invisible to the `__mm` gate — `__slab`, not `__mm`). **Non-catastrophic** (80 B vs the 1 MiB that was): 15000 spawns ≈ 1.2 MB of resident structs, bounded by a different constant than the stack. Reclaiming it needs PROMISE-LIVENESS (free the struct when the last `await` of a promise retires it) → **B2 (liveness)** or a real recycling allocator → **B1c**. Filed rather than fixed, per the review's sanction.

⭐ **BLOCKER REMOVED by P1.5-B2a (E3100 linear-await), but the recycle itself is DEFERRED to B1c — assessed, not forced.** The "must stay resident for `await p; await p`" reason above is now GONE: `E3100 SemanticPromiseAlreadyAwaited` makes a reachable re-await unrepresentable (a scalar promise can only be awaited where it was spawned — the promise mark is per-function and does not cross frames, so a loaded/parameter/returned handle is `await`-refused E2015 — so at-most-once holds for every awaitable promise). The struct is therefore provably `__gt_await`'d at most once and dead after its single await. **But the RECYCLE is a delicate multi-part change to the hand-assembled scheduler, NOT a one-line mirror of the stack free-list, and was left for B1c per the B2a brief's "only if clean" sanction:** (1) the struct is read at `__gt_await`'s `extract` block (`GtOffResult`) AFTER the stack recycle, so the struct's recycle point is a NEW block after `extract`, distinct from the stack's post-switch point; (2) the struct owns a SEPARATE `__slab_alloc`'d arg buffer (`GtOffArgBuf@0x48`) that leaks unless co-recycled, so recycling the struct alone only partly closes the per-spawn leak; (3) a struct free-list needs its own intrusive link (`GtOffNext@0x38`, free after the GT leaves the run queue) and must preserve nested-await LIFO correctness (`async-scheduler.nested`) and never recycle GT0. A subtle use-after-free here is invisible to the current tests, so it belongs with the B1c runtime work rather than bolted onto the B2a semantic-check rung. **#88 (never-awaited) still leaks the struct AND stack regardless — that path never reaches `__gt_await`.**

### 88. 🔵 P1.5-B1a RESIDUAL — a NEVER-AWAITED GT never completes, so its stack is never recycled (`while true { async f() }` still leaks 1 MiB/spawn)
The stack free-list recycles a stack from the WAITER side, AFTER the GT completes (`__gt_await`'s `run` block, post-switch). A GT that is spawned and **never awaited** never runs, never completes, never reaches that recycle point — so its 1 MiB stack (and its struct) leak. `let p = async f()` with no `await` still commits 1 MiB per spawn; a `while true { async f() }` loop is unbounded. This is DISTINCT from #87: it is the STACK leaking again, but only on the never-awaited path (the awaited path is fully bounded now). Fixing it needs LINEARITY — reject or drain an unconsumed promise (an `E3100`-style "promise must be awaited" gate, or an exit-drain that runs the ready queue at `main` return) → **B2**. Filed as an explicit decision: B1a's acceptance specs await everything, so the awaited path (the common one) is bounded; the never-awaited path is a documented, deferred gap, not a silent one.

### 89. 🔵 P1.5-B2a LIMITATION — `await` of a LOOP-CARRIED / block-arg promise is over-rejected (E2015), because the promise mark and the linearity both key on the `asyncCall` ValueId
A promise REASSIGNED inside a loop (`var p = async f(); while { … await p …; p = async g() }`) or otherwise flowing through a BLOCK ARG has no single `asyncCall` ValueId — so shv2's promise-value tracking (the `valuePromiseMarks` bool, set on the asyncCall RESULT and NOT propagated across block args) does not see it as a promise, and `await p` is rejected **E2015** ("the operand here is not an async spawn's result"). This over-rejects a LEGAL pattern (await-then-rearm at the loop bottom, which the bootstrap ACCEPTS — each thread awaited exactly once). **Pre-existing** (B1a's promise mark never flowed through block args; B2a's ValueId-keyed linearity inherits the same limit — it can only reason about a promise traceable to ONE `asyncCall`). **SOUND** (rejects cleanly, never miscompiles or leaks) and it hits **NO corpus linearity case** — the corpus's accept cases (`await-aliased-loop-element`, `rearm-after-await`) re-spawn a FRESH `let p = async …` each iteration (directly traceable to an asyncCall), which shv2 accepts and runs. Fixing it = flow the promise identity (and awaited-ness) THROUGH block args — coupled to **B2c** (Promise as a first-class flowing/stored value). Filed, not fixed. Probes: `cond_rearm_loop`, `rearm_bottom` → E2015; the fresh-per-iter twins → accepted. ⏭ Minor same-area code-quality note: `functionHasAwait` (SemanticCheck) is a per-function op scan that MIRRORS `buildEscapeSummary`'s gate skeleton — a shared `functionHasOpMatching(pred)` would state the block-op-walk once (a pattern echo across different predicates, not a strict "one fact twice"; a small DRY pass for later).

### 62-CLASS RECURRENCE (#71, FIXED) — `opensBlockAt` over-counted a ternary if/else as a block opener
The inline conditional spells `if`/`else` in EXPRESSION position with NO closing `end`, so the carried-var token pre-scan (`opensBlockAt`/`blockDepthAfter` → `blockStatementEndIndex`) over-counted block depth and PANICKED (`assertScanAligned`) whenever a ternary sat inside a scanned region (an `if`/`while` condition, a `match` scrutinee, or `let x = a if c else b` in a LOOP BODY — idiomatic). Suite was green only because no test nested a ternary under a scanned block. Fixed by the #71 review (`f10a6862d`): `ifBeginsStatement`/`elseFollowsBlockEnd` distinguish a block if/else (statement-start / after a then-`end`+label) from a ternary one, mirroring the bootstrap's `SkipToMatchingEnd`. **⭐ THE RECURRING LESSON (#62, #5, now #71): every new construct that spells a block keyword — or spells one WITHOUT a block — must teach `opensBlockAt`.**

### 15. ✅ FIXED — the bootstrap RESET the register allocator over any block that PANICS (a comment stated the safe-making invariant as an ASSUMPTION, false the moment a panicking block was also an ordinary fall-through reading an SSA value from before the guard → `E9001: value has no register and no stack home`). The invariant is now ENFORCED (diverging only if it also reads no cross-block SSA value). Found because shv2's own `assertSignedReach` has exactly that shape. **LESSON: a comment stating a precondition is a test that was never written.**

### 16. ✅ FIXED 2026-07-19 (in maxon-sharp, together with #47 — SAME BUG) — BOOTSTRAP LEAKED a payload-carrying error built in a helper and thrown by its caller
`throw self.shiftCountNegative(...)` (call RETURNS a payload-carrying error union) → `MM leak`; constructing at the throw site is clean. `throw <call>` double-incref'd the returned (already-owned) error box. **FIXED**: `LowerThrow` now skips the transfer-incref for an already-owned call-result (`!temps.IsCallReturnTransfer`). See #47 for the full write-up.

### 21. ✅ FIXED 2026-07-15 (bootstrap) — an enum range arm over an EXPLICIT-int-backed enum matched NOTHING, silently. `IrEnumCase.Ordinal` was ONE FIELD WITH TWO MEANINGS (the auto-increment tag counter AND a declaration position); a range arm read stored ordinals `0,11,21` (neither position nor raw value) so `low to medium` compiled to `[0,11]`, excluding `medium`'s tag 20 → returned 0, no diagnostic. Survived because the corpus had no explicit-INT enum with a range arm (the one explicit enum was float-backed, a different path). Fixed: a range arm is the OR of the cases it covers, by declaration index (kept as a two-compare tag range only where no uncovered case's tag falls between). **⚠ shv2 must implement ORDINAL order at P1.1b (done — Wave B, parallel-column `EnumLayout`); do not port the raw-value reading.**

### 23. ✅ FIXED at P1.0r (`ac512711a`) — THREE GREEN INSTRUMENTS sat on a 1000× memory bug. `TargetDialect.iatCall` carried `implicitDefs: 0` under a comment reading *"full call-barrier semantics"* (ONE FACT WRITTEN TWICE, DISAGREEING), so nothing was forbidden across an OS call — INERT until P1.0r put an `iatCall` inside a register-allocated function; `VirtualAlloc` clobbered `size` → one 64KiB VirtualAlloc per object, 133.74 MB → 412 KB for 2000 Points. Every gate was green (specs, worker-invariance, scale-test — the last structurally could not see it: its corpus had no struct, and even a runtime alloc counter would have missed it — 2000 `__mm_alloc` calls either way; what blew up was pages from the OS). Found only by an INDEPENDENT reviewer reading the emitted machine code. **⭐ WHAT CLOSED THE CLASS is not an instrument: `assertCallClobberConsistent` compares `iatCall.implicitDefs` against `callerSavedMask` every `allocateRegisters` — the bug is now UNREPRESENTABLE. ⚠ shv2's emitted runtime is still measured by nothing (a runtime instrument is its own rung, needs a reporting channel — a corpus program has no `print`, an exit code is 8-bit — do not smuggle it into a corpus rung).**

### 24. ⚠ Three findings from the corpus rung, none fixed, each in code the rung did not own
- ⭐ **`SplitLiveRanges` is O(blocks × K²)**, K = simultaneously-live *owned* values in one block. A struct binding is owned (sits on `ownedBindings` until the `return`). K measured across all 2,445 functions: max 8, mean 0.16, so Σ_f O(K_f²) is linear in program size ⇒ **DEBT, not a bug.** ⚠ Re-measure trigger = an INLINER (not P1.4/P1.7 — P1.4 is capped at 6 params, an array is one owned value); shv2 has no inliner and no DCE.
- **`installMmRuntime` is billed to NO PHASE** (`Compiler.maxon:180`, outside every `PhaseProbe`) ⇒ phases do not sum to total. Isolated: a constant 316-alloc delta. Constant in time too (share FALLING), so not the "dominant cost in the wrong bucket" class — but `--metrics` emits an `unattributed` row `scale-test` does not surface.
- **`docs/error-codes.txt`'s E1006 doc text is WRONG** ("must be doubled"; the lexer/spec say `\{`, verified both ways). One fact written twice, disagreeing, in the registry that exists to be the single copy.
> ⭐ **PROCESS NOTE: the independent REVIEW caught the independent OPTIMIZER** — the optimizer priced a splitter ceiling at "K=65 ⇒ 1.28s in `Internals.maxon:1274`"; line 1274 is `__slab_free`, nothing to do with live-value counts, and no K=65 bound exists. The reviewer REFUSED to record it (*"replacing a wrong claim with an unverifiable one re-mints the defect"*). That is why the review is independent and runs LAST. *(A fourth bullet — `mainSource` re-deriving 13/15 driver names — was CLOSED by make-copies-check `9ba3af660`; see #4h.)*

### 25. ✅ FIXED 2026-07-16 (P1.1a wave 3) — shv2 silently ACCEPTED a private field read (`v.private` from outside `type Value` compiled and returned 42; bootstrap rejects E3014), and the comment excusing the dropped `export` bit stated a FALSE reason (*"field visibility gates cross-FILE access, and shv2 has no cross-file resolution"* — but it gates access from outside the TYPE, same file; `error.unexported-field-read/-write` are single-file). E3014 now lives in `requireFieldAccessible` (one home, read+write callers, TYPE-scoped). **⚠ The fix's OWN comment then asserted "the gate is the TYPE not the FILE" while nothing checked it (whole corpus reaches the field from `main()`) — a sabotage kept the suite at 392/0; closed for real by `field-visibility-is-type-scoped.md`.** #27's disease, a fourth time.

### 26. ⚠ FUNCTION OVERLOADING IS ON NO RUNG OF THE LADDER, and a marker blames the wrong one
`structs.md`'s `struct-field-default` is marked "P1.1a wave 2 — field defaults" but is blocked by two `create` overloads (arity 0 and 2) → `E3006`. Overloading appears NOWHERE (zero hits in PLAN/ARCHITECTURE/this file/`Compiler/`), yet `PLAN.md:487` leans on it (the stdlib has overload sets). **⇒ Where overloading belongs on the ladder is a PLAN decision (it is function resolution, orthogonal to structs) and needs a number. LESSON: a `disabled-test:` reason is prose — nothing verifies the rung it names is the rung that unblocks it, so the roadmap can be wrong in exactly the way this file is about.**

### 27. 🔴 `StdOpMeta.isPure` HAS ZERO READERS, and SIX places called it a live correctness constraint — the PROSE is fixed; the survey it demands is an OPEN rung
Third instance of #4c's shape (a gate reporting PASS while structurally blind), found by SABOTAGE: `loadIndirect` flipped to `isPure: true` (the exact edit six sites call a silent wrong answer) and the suite read 371/0 — the property holds because NOTHING HOISTS (no LICM/CSE/inliner), not because of the flag. A sweep of all nine `StdOpMeta` fields found only THREE have readers (`category`/`isCall`/`clobbersFlags`); **six are read NOWHERE: `role`, `isPure`, `isUnsupportedInInlineBody`, `isMemory`, `isStore`, `isCmp`.** ✅ The PROSE was corrected at seven sites; `isPure` is KEPT (SOLE declaration, reader SCHEDULED — unlike #7's `setsFlags`, a deleted second home). **⇒ OPEN RUNG: what to do with the five other unread fields — a survey with a decision per field (`isMemory`/`isStore` are scheduler barriers, `isCmp` is compare/branch pairing), orthogonal to structs, needs a number. ⚠ These values are now held by REVIEW ALONE — no spec can fail on them; a wrong one is a silent wrong answer in every program at once the day a hoisting pass lands.**

### 28. ⚠ The corpus generates structs with a HARDCODED field count (2), so the one struct dimension with a superlinear term is invisible — DEBT
`structTypeDecl` emits exactly `x`,`y` on every generated type, so F=2 always. That is where the superlinear term is: `StructLayout.indexOfField` is a LINEAR SCAN and every field read/write/`Self{}` label now asks it, so a literal filling F fields costs O(F²); the ladder doubles the NUMBER of types (a ×4-per-type constant, never bends). Measured: across 166 `type` decls F max 22 mean 4.14; programs grow by adding TYPES not widening them ⇒ **DEBT.** Re-measure trigger = a machine-GENERATED wide type (the corpus is the one thing that could produce one — needs a field-count knob; no rung named per #24). **LESSON: closing a corpus gap by generating a construct does NOT close it by DIMENSION — #4d made structs appear, at one width/shape/field-count.**

### 22. ⚠ `bin/maxon.exe` IS GITIGNORED AND NOTHING REBUILDS IT — a baseline can measure a tree that does not exist
`maxon build maxon-shv2` drives `./bin/maxon.exe`, gitignored, whatever some earlier session last built. A 276 KB swing looked exactly like the change under test (an edit to the bootstrap's match lowering); two measurements killed it (`dump_ir` showed the intended 2-instruction range compare unchanged; stash+rebuild → byte-identical). **⇒ LESSONS: (1) a binary nothing rebuilds is a THIRD copy of the source (same disease as the MCP server binary and the error-code enums) — `build csharp` FIRST, before any baseline you intend to trust. (2) ⭐ Attribution is not a story that fits — it is a measurement. The jump-table explanation was coherent, plausible, matched the numbers in both binaries, and was still wrong.**

## 📋 Environment / process notes that cost real time

- 🟡 **IN A WORKTREE, PASS `repoRoot` TO EVERY `maxon-dev` MCP TOOL — otherwise it drives the MAIN REPO.**
  One stdio server is shared by every agent; its cwd is the MCP host's, not yours, so the default root is
  the checkout holding the server's own binary. All nine tools take `repoRoot` (absolute path of your
  worktree). Results ECHO the root actually used (read it back); a non-checkout root is REFUSED, never
  swapped. `updateRequired`/`run_scale_test note:`/`fmt` **edit** the tree they are pointed at. See the box
  in `.claude/CLAUDE.md`. *(Caught 2026-07-14 with five agents running against it — the project's own
  signature bug at the TOOLING level; documented `13855215b`.)*
- ⚠ **THE MCP STALENESS GUARD HAS A HOLE THE SHAPE OF THE RUNNING PROCESS.** It compares the binary's mtime
  against its sources, not the running process — so *rebuild-without-restart* sails through: a stale server
  keeps serving stale tool schemas (measured: `build` returned success echoing no `repoRoot` while the
  on-disk binary contained it). ⇒ If a tool's schema/result disagrees with its source, suspect the PROCESS
  first: compare the server's `StartTime` against the binary's mtime (`Get-Process maxon-dev-mcp`), restart.
- **A FAILED BUILD LEAVES THE OLD BINARY** — `spec-test` then runs the previous compiler and reports green.
  Check the build's exit code before believing a test result. (Three false greens one session.)
- **A copied `bin/maxon.exe` is FROZEN** — `bin/` is gitignored, a worktree starts without a compiler and one
  gets copied in, so a stale copy silently reverts every bootstrap change on the branch (once looked like a
  25% allocation regression that did not exist). If the branch touches `maxon-sharp/`, `dotnet build` in the worktree.
- **A FILTERED C# `spec-test` always dirties fragments** (process-static id counter) — only a FULL run's
  `git status specs/` means anything.
- **`./bin/maxon.exe fmt` with arguments reformats the ENTIRE TREE in place** (two agents destroyed unrelated files).
- **`pkill -f maxon-dev-mcp` does not match Windows processes** — the exe stays locked and the build silently
  keeps failing. Use PowerShell `Stop-Process`. (`buildall.sh` has the same hole.)
- ⚠ **The C# network specs (`async-tcp`, `tcp-client`) TIME OUT (~12s) on this machine** — 2893/2 not 2895/0.
  Environmental (a same-day 2895/0 commit fails identically), a fresh box should restore it.
- ⚠ **shv2 `spec-test` reads `specs-shv2/` RELATIVE TO CWD — run it FROM the worktree**, or the worktree
  binary silently tests the WRONG repo's specs (a new spec then shows "no tests selected" + an unchanged
  suite count = a CWD bug masquerading as discovery; burned many turns in #59). See [[reference-spec-test-cwd-specdir]].

### 29. ⚠ Two receiver-ownership facts from P1.1a wave 3 (mostly historical — the P1.4 they deferred to has since landed)
**(a) FIXED** — `next.a` compiled to `self.a` (a self-field alias has `boundValue == 0` and ValueId 0 IS the receiver; three sites re-read `boundValue` where `parseVariableReference` guarded it — one fact, four homes). Fixed by DERIVE: `requireStructBase` returns the box WITH the layout (`StructBase`), one reader. Pinned `self-field-struct-typed.md`. **(b)** — `next = other` lowers to an un-increfed struct-pointer store; the borrowed-receiver conclusion survives only because bare `self`-as-value is E2015 and a struct-typed field is uninstantiable, so no live struct pointer reaches a field. Was "left for P1.4 consume analysis" — P1.4a Wave 2 has since delivered consume-into-field (verify this specific store is now covered). #27's shape (a property holding for a reason other than the one written).

### 30. ⚠ `scale-test` is BLIND to the instance-method mechanism (and to `match`) — a corpus knob nobody added
The wave-3 optimizer measured ZERO allocation for the instance-method path (receiver, self-field aliases, method-call lowering) because the corpus emits no methods; every allocation in its delta is the field-DECLARATION path. #4d's shape (closed for structs/heap/globals/idiv by adding knobs); **methods are the knob nobody added, and P1.1b Wave A found the corpus is ALSO `match`-blind** (no knob emits `match`/`then`/`gives`). ⇒ Fold a method knob + a `match` knob in together, once, deliberately (not urgent — the method path is O(1)/O(fields) by inspection). The optimizer correctly did NOT touch the instrument to paper over it.

### 31. ⚠ A cross-register-class `match` EXPRESSION is refused (E2015); promoting it is a FOLLOW-UP SLICE
An integer give and a float give feeding one result phi panicked the backend (`X64Backend:603 crosses register files`) until Wave A guarded it — now refused with a positioned E2015 (`finalizeMatchMerge`, pinned `match-expr-divergent-class.md`). A deliberate DIVERGENCE from the oracle (which promotes int arms to float). shv2 HAS `promoteToFloat` + the lattice; it is blocked on there being nothing to hold the result (a promoted match yields a float VALUE and `float` is now nameable, but the promotion has no consumer yet — the `mintPhi` trap). **⇒ A FOLLOW-UP SLICE now that the float type-system rung has landed** (F1–F2b, #50/#57): replace the E2015 refusal with unify-via-`arithResultTag` + promote each non-float arm's give **in its own exit block before its branch to merge** (the fiddly part). Same-class arms (int+bool) already match the oracle exactly.

### 32. ⚠ The E2027 duplicate-pattern check is O(P²) in one match's arm count — DEBT, kept deliberately
`checkAndRecordDuplicate` linear-scans a `seenValues` list per const single-value pattern. P is a single match's HAND-WRITTEN arm count (never bulk-generated), so whole-program cost is linear for bounded arms, quadratic only in a degenerate single-giant-match. Matches the project's own `byteArrayContains` parameter-duplicate precedent; a `Set` would allocate a heap object per match to save a bounded scan. Same class as `indexOfField`'s O(F²) (#28). Revisit only if a real program writes a match with hundreds of literal arms (none does).

### 33. ⚠ A NEGATIVE float-backed enum raw value rejects with a MISLEADING E2010 where the bootstrap accepts
`enum T { a = -1.0  b = 0.0  c = 1.0 }` → shv2 E2010 "Expected integer literal but got float literal" (positive float tags parse fine — the `= -x.y` minus→int parse path fails `consume(intLiteral)`); bootstrap accepts and runs. A clean reject, no crash/wrong-answer, no corpus case. The message misrepresents the limitation (positive floats ARE accepted). **⭐ The implementer's deferral reason was ALSO wrong (review corrected it): negatives would be handled CORRECTLY if they parsed (set-membership over signed i64).** ⇒ Follow-up: support them or emit a message naming the real gap; needs its own spec + oracle cross-check.

### 34. ⚠ Union `E2026`/`E2046` wording: shv2 says "union" consistently; the bootstrap hardcodes "enum"; nothing pins either
After the E3034 fix threaded the noun through `EnumLayout.kindWord()`, shv2 says "union" across E3034/E2026/E2046 for a union scrutinee; the bootstrap hardcodes `{"enum"}` in E2026/E2046 (an apparent oversight — its E3034 IS `IsUnion`-conditional). No corpus golden pins union E2026/E2046 wording. ⇒ Coordinator's call: author union spec tests pinning shv2's "union", or reproduce the bootstrap's "enum". An unpinned wording detail on a diagnostic no case reaches.

### 35. ✅ FIXED 2026-07-16 (`abb6bde17`/`f3550590e`, 523→533) — a USER `__`-prefixed function silently MISCOMPILED (Wave A widened `isCompilerInternalCallee` from `__mm_`-only to any `__`, but shv2 never emitted E2051, so `__add(7)` with a missing arg ran with an uninit arg → exit 65543, and `__foo()` panicked the backend). E2051 now emitted at 9 declaration sites via one `requireUnreservedName`, so the `__` predicate is provably SOUND. **LESSON (found by the INDEPENDENT review, self-review missed it — a 5th time this rung): a widened namespace predicate needs its namespace ENFORCED, or it routes user code past validation.** Residual: `b"__"` is 3 cross-referenced copies (a small shared-constant cleanup, spans TargetPrinter).

### 36. ✅ FIXED 2026-07-16 (`837b948d2`) — a valid program PANICKED the backend when a fused compare was the function's highest ValueId (the value-class column was sized by `scanFunctionValueCount` = highest ValueId used as an OPERAND, but `recordOpResultClass` writes for every value an op DEFINES, and a fused loop-test `cmp` defines a boolean consumed only as EFLAGS → column one short). Fixed at source: `setValueClass` grows the column (`growFilled`) to cover every defined id; the pre-size stays as the allocator lower bound. The signature bug (ONE FACT WRITTEN TWICE) at the backend; Wave B only DODGED it by luck for `__int_to_string`.

### 37. ✅ FIXED 2026-07-16 (`bbcf46e32`+`deac21315`) — returning an OWNED interpolation String across a call LEAKED (exit 101; the callee correctly move-outs, the caller couldn't recover ownership — `valueOwnsHeap` is per-function). Found by the INDEPENDENT review by PROBING (no enabled test returned an owned String). User ruled "leaks are not ok" so the full static-ownership convention was implemented, not deferred: `mintOwnedCallResult`→`trackOwnedTemp`, callee moves the return out, borrowed literal/param returns heap-PROMOTED (`promoteToOwnedString`). See [[project-p12-string-ownership]].

### 38. ✅ FIXED 2026-07-16 (`3ec65cbe5`+`deac21315`) — an owned binding in an `if`/`while` body that control FALLS THROUGH was never dropped (leak; `closeBlock` drained without emitting `__mm_decref`). Owned bindings now drop on every edge that leaves a block ALIVE, exactly once (fall-through, `break`/`continue` down to `LoopContext.ownedFloor`; `emitScopeDrops` gained a `floor` param). **The review found + fixed a leak the first cut missed** (a returned loop-local stripped from `ownedBindings` by the return move-out → sibling iterations' fall-through dropped nothing — the "one fact, two readers" bug).

### 39. ✅ FIXED 2026-07-16 (`4a2ae34f1`+`66754ea88`) — reassigning a `var` owned binding did NOT drop the old box (masked leak; net count cancelled + bump allocator hid the UAF). `reassignBindingValue` now drops the overwritten value (keyed on `ownedBindings` MEMBERSHIP, since a loop-carried `var` phi has no provenance bit); owned→borrowed PROMOTES the RHS. Found by the INDEPENDENT review by probing. Deeper aliasing/depth cases split to #40/#41.

### 40. 🔴 (its own DEPTH-MODEL rung) — borrowed→owned reassignment INSIDE a nested block is a use-after-free, MASKED (exit 0)
`var s = ""; if c s = build(1) end; print(s)` → exit 0 today, but the emitted x64 decrefs the `build(1)` box at block `end` and `print` reads the freed pointer. Cause: `reassignBindingValue`'s borrowed→owned `ownedBindings.push` lands ABOVE the block's `BlockMark.ownedCount`, so the fall-through drop frees a FUNCTION-scope binding at block end. **Structural: the height-stack model can't mark a binding owned SHALLOWER than the current block** (a hybrid splits ownership across two places = the signature bug; rejecting the common `var s=""; if c s=build() end; use s` is bad UX). ⚠ **STAYS MASKED under the bump allocator (never recycles) → becomes a WRONG ANSWER the moment `__mm_free` gets a free list. TIME-SENSITIVE — do before the free list.** Fix = a height-stack → per-scope-frame rework. Investigated 2026-07-17, user-deferred (chose Wave D). Own rung, near the recycling allocator.

### 41. ✅ FIXED 2026-07-16 (Wave C, `6c06e698c`..`75bd75fac`) — `s = t` between two owned bindings was a DOUBLE-FREE (both end in `ownedBindings` pointing at the same box; scope exit decrefs twice). Under static single-owner it is a MOVE: source poisoned (`VarInfo.movedFrom`), drop-skipped, a later read = E3102 use-after-move (shv2's FIRST ownership program-rejection); reassigning revives. The #39 drop-on-reassign fix SURFACED this pre-existing double-free (the base parser hid it by ALSO leaking `build(1)` — a false clean). See [[project-p12-wave-c-moves]].

### 42. ✅ FIXED 2026-07-16 (`51f543fdd`, 597→602) — use-after-move was enforced ONLY at bare reads, not field-access / method-receiver / STORE (`let q=p; p.x=99; return q.x` returned 99 — a write through moved-from `p` mutating `q`'s aliased box, an observable WRONG ANSWER). One shared `requireBindingLive` guard at the TWO funnels (`parseVariableReference` + `requireStructBase`); a field store is a USE not a revive, only a full reassignment revives. The INDEPENDENT review enumerated every use-site + SABOTAGED the guard. Same "one predicate, one enforcing site" disease.

### 43. 🔴 (P1.2 Wave D follow-up) — `s.append(s)` (SELF-append) is a use-after-free, MASKED (bump allocator)
`__str_append` captures `otherBuf` at entry, then on a GROW runs `freeOld = __mm_free(selfBuf)` BEFORE `blit` reads `otherBuf` — when `self==other`, `otherBuf==selfBuf`, so the blit's SOURCE is the buffer just freed. STAYS MASKED (freed bytes still intact under bump alloc, output even correct `"ss"`); a WRONG ANSWER the moment `__mm_free` recycles (class of #40/#42). The in-place path (enough capacity, own root) is safe. **Fix (own rung): delay the old-buffer free until AFTER the blit + a DESIGN RULING (accept `s.append(s)` yielding `"ss"`, or reject?) + a spec either way. Do before the recycling free list, alongside #40.** [[project-p12-wave-b-closed]]

### 44. ✅ FIXED 2026-07-18 (P1.4a Wave 1, `354995f3f`) — a boxed union crossed a call boundary in NEITHER direction (PARAM E2015, RETURN an exit-101 LEAK, interim-rejected E2015). RETURN now ADOPTS a boxed-union call-result (`mintOwnedCallResult` via a `named`+`isBoxed` LAYOUT lookup, `valueIsBoxedUnion`); a boxed union as a PARAM borrows correctly. The cross-call borrow-vs-consume ruling P1.1a/P1.2 both deferred here. [[project-p12-wave-c-moves]]

### ✅ FIXED 2026-07-18 (`fa685a7a1`, 632→641) — a TEMPORARY union scrutinee leaked (exit 101) and an owned `gives` value double-freed (0xC0000005). Both REACHABLE, both PRE-EXISTING (Slice 1 / P1.2, on main), both found by the Slice-2 review by probing (suite green over both). FIX: `parseMatchCommon` enrolls an owned-temp scrutinee as a match-scoped owned binding dropped once on all four exits; an owned give TRANSFERS to the phi (`transferGiveToPhi`, phi marked owned). **The reason "leaks are not ok" was codified into the rung skill the same day.**

### 45. ✅ FIXED 2026-07-19 (= #54 Slice C, `29844a2ff`..`7bda27e44`, 748→754) — a match-expression `gives` MIX of different types (register-class-compatible) had NO diagnostic and SEGFAULTED (`finalizeMatchMerge` checked register-class, not type identity; `String`+`int` gives → the phi took `String`, `print` deref'd the int 5 → exit 139). A `checkGiveTypes` agreement loop composes three cases against the first arm (both-scalar `typesAgree`; one-aggregate DISAGREEMENT via `aggregateNameOf`; both-aggregate `namedAggregatesConflict`) → E3005. Reuses the shared TypeRules helpers. **⭐ Coordinator verification caught a LIVE SEGFAULT the first cut missed** — `ValueTypeTag.named` is OVERLOADED (a boxed-union VALUE also carries `named`), closed via the aggregate-ness arm. See #58 (bootstrap also crashes) + #59 (the cross-cutting `named`-overload fix). ⚠ The independent reviewer HUNG; the coordinator did the full review.

### 58. 🔴 BOOTSTRAP BUG (found by #54 Slice C oracle check) — the C# bootstrap internal-errors on a mixed-type `match … gives`
`maxon build` on `let out = match c { 0 gives "hello"; default gives 5 }` → `E9001: Value cannot be null. (Parameter 'key')` — a C# null-ref, not a clean type diagnostic (its `finalizeMatchMerge`-analog has its own match-give-type bug). So BOTH compilers were broken (shv2 accepted+segfaulted, now E3005 via #54C; the bootstrap internal-errors). Its own C# subsystem, not reachable from an shv2 rung; specs-shv2 unaffected. File for when the bootstrap's match handling is next touched; correct behavior mirrors shv2's E3005.

### 59. ✅ FIXED 2026-07-19 (754→756) — the `ValueTypeTag.named` OVERLOAD left an aggregate-vs-scalar hole at the coercion sites (`return <boxed-union>` from a `returns-Integer` fn COMPILED and LEAKED 101 — the twin of #54 Slice C). Extended the SHARED `namedAggregatesConflict` (`TypeRules.maxon:243`) to fire on aggregate-vs-scalar (exactly one side named), REORDERED after the tag check + un-gated at return/reassign/otherwise (the reorder is the key to ZERO golden churn — a genuine class mismatch is caught by the tag check first). field/payload inherit it. Full suite 756/0, zero drift. ⏭ **The CALL-ARG site is DEFERRED to #54 Slice B2** (its union param name is erased before SemanticCheck). ⚠ Implemented by the coordinator directly (agents surfacing permission dialogs); a long dead-end was the spec-test runner resolving `specDir` relative to CWD ([[project-open54-ab-closed]]).

### 46. ⚠ BOOTSTRAP BUG (tripped by the P1.3 match-flow fix) — the whole-program mutating-parameter fixpoint does NOT converge once a call graph grows, adversarially demanding `let` then `var` on a binding already `let`
Routing the merge edge through `recordBranchEdge` made the bootstrap reject the edge loop's `arm` (already `let`) with **E3077** ("never reassigned; use 'let'") — a "use let" against a `let` is the signature of a non-converging fixpoint. Worked around in shv2 by a documented 2-line direct `branchEdges.push` (byte-identical IR). Residual hazard: if `recordBranchEdge` gains logic this site won't inherit it (flagged in-place). The real bug is the bootstrap's analysis; a note for when it is next touched.

### 47. ✅ FIXED 2026-07-19 (in maxon-sharp — user ruling: fix the bootstrap so it is a solid reference for shv2) — the compiler leaked on rejecting a non-literal FIELD DEFAULT (exit 101, not 1) === SAME BUG AS #16
`type T { export var label as String = "x" }` correctly rejects E2015 but the shv2 compiler leaked 4 allocations → exit 101. ⭐ **ROOT CAUSE (my earlier "swallowed sweep message" guess was WRONG — verify your own diagnosis): a `throw <CALL RESULT>` double-increfs the thrown error.** shv2's `readFieldDefaultLiteral` does `throw self.rejectNonLiteralFieldDefault()` — a helper RETURNS the ParseError, the caller THROWS it — which is EXACTLY OPEN #16. Reproduced minimally in a bootstrap-compiled program: `throw buildErr()` leaks, `throw PErr.x(...)` (constructed at the throw site) is clean. mm-trace: a call result arrives owned (rc=1, the callee's return-incref), `LowerThrow` increfs AGAIN (rc=2), the receiver decrefs once → rc=1 leaks. **FIX (maxon-sharp `MaxonToStandardConversion.Calls.cs` LowerThrow):** gate the transfer-incref on `!temps.IsCallReturnTransfer(name)` — skip it for an already-owned call-result (an `Orphan|CallReturn` temp), keep it for a FRESH construct (rc=0) or a BORROWED `throw self.field`; mirrors `LowerReturn`. Uses an EXISTING ownership signal, no new mechanism. **Gates: dotnet build 0/0; C# suite 3045/0; shv2 suite 782/0; `specs-shv2/` EMPTY (shv2's emitted codegen unchanged — only its runtime leak fixed); the #47 field-default reject now exit 1, leak GONE; zero existing `specs/` goldens moved (no committed spec used `throw <call-result>` — which is why #47 stayed open) + one regression test `throw-transfers-ownership/throw-call-result-transfers-single-reference`.** Delegated the C# fix to an implementer in an isolated worktree; coordinator-verified all gates. ⇒ **This also FIXES OPEN #16.**
> ⚠ **A SEPARATE PRE-EXISTING bootstrap bug the implementer found while probing (NOT this fix, NOT worsened by it) — filed as #63:** `throw <local heap-error var>` / `throw e` re-throwing a caught HEAP error PANICS (`mm_incref NULL`) because the throw sites emit `MaxonScopeEndOp` WITHOUT `keepVars` (unlike return), so `scope_end` decrefs+NULLs the thrown local before the throw's incref loads it.

### 48. ⚠ (P1.4a Wave 2 — harness robustness) — the spec runner's `.spec-tmp` staging can build against a STALE leftover `.maxon` from a racing worker
`SpecTestRunner.purgeSourceFiles` rebuilds a per-test directory but does not fully reset `.spec-tmp` per run, so a stale `.maxon` from a racing concurrent worker can silently join a later build and hang/miscompile an unrelated program (this session: a leftover made `comparison-operands` hang). Same class as #6. A harness-robustness fix (fully purge or namespace `.spec-tmp` per run/worker), gated by the full spec harness — its own follow-up, not compiler code.

### 49. ✅ RESOLVED 2026-07-18 (P1.4b Wave 2b, `d16a45f0f`) — a throwing fn with a MANAGED return type accept-and-CRASHED (segfault; a `tryCall` result register is NULL on the error edge) → Wave 1 turned it into a clean E2015 gate, Wave 2b made it WORK: the gate + `returnTypeOwnsHeapBox`/`payloadClassOwnsHeapBox` + the reject spec removed, the tryCall result re-scoped to the ok edge (NULL on the error edge). Enabled the `[wave2-managed]` cases.

### 50. ✅ FIXED 2026-07-19 (F2a, `f8c516e33`, 761→762) — float-returning FUNCTIONS panicked the x64 backend (`StdToX64Conversion:1783` "float returns not supported until M3", a general gap, NOT errors-specific). END-TO-END, three blockers: (1) FRONT-END — a float-alias return type mis-read as int (via ONE shared `namedIsFloatAlias` + typed doors `floatResolvedTag`/`resolveFloatAliasType`; a 6th stale "every alias is int" comment fixed); (2) BACKEND — XMM0/d0 return codegen (`buildCalleeResultClasses` threaded so capture-file and coloring-file are one fact); (3) CALL RESULT — a float-alias call result stayed `named` so `trunc` wouldn't fire. The float rung is "do BOTH" (user ruling): F1 nameable (`fda8f73bb`) → F2a return → F2b args (#57). See #60 (throwing-throw-path gap deferred here).

### 51. ⚠ (pre-existing, surfaced by P1.4b Wave 1 review) — `as` casts + ranged-int narrowing are NOT enforced ANYWHERE
Wave 1 added `as` support (the error specs need `0 as Integer`), but `parsePostfixCast`/`evalConstPostfix` return the value UNCHANGED — no retag, no range check. Adds NO new hole (bare `return 300` and `return x` also truncate to exit 44 — shv2 doesn't enforce ranged narrowing on returns/assignments at all), and the inert cast can't produce a wrong-typed value. A pre-existing type-checking gap (ranged narrowing is P1.9-adjacent), not a regression — file, don't block. Also: the sibling `maxonOpIdxToString` segfault remains a documented accept-and-crash deferral (same "latent crash → clean reject" sweep applies).

### 52. ✅ FIXED 2026-07-19 — a union-payload INTERNER MISMATCH (same class as #56)
The parser resolved a `signatures` union-layout's payload id against `self.artifact.typeNames` (the wrong interner) — construct/match-bind/scope-drop paths. Triggerable **single-file by a ranged alias** (a `typealias Integer = int(...)` shifts project ids, so BoxA's id is misread → wrong E3005 or a silent misclassification), not only cross-file. ✅ **SINGLE-FILE slice FIXED 2026-07-19** (`6ae9b56cd`, 745→748): Door A routes the 3 value-producing sites + the load-value mint through `adoptType(..., into: self.artifact.typeNames)`; Door B (`unionBoxDropCallee`) resolves the payload drop-callee against the layout's OWN interner (a memory-SAFETY fix — the mis-resolved drop returned `MmDecrefName` not `destructorSymbol` → leak/wild-free). ✅ **BACKWARD-ordering slice VERIFIED CLOSED 2026-07-19** (spec-only rung, NO compiler change, 767→771): the "declaration-order union-layout PRESCAN" this slice asked for **already exists** — `queryProgramSignatures` → `foldDeclaredSignaturesInto` → `foldFile` folds EVERY file's union LAYOUTS and `throws` clauses into the shared index BEFORE any body parses (Parser.maxon:1962). The interner half is closed because **every `classifyUnionPayload` call site adopts the payload id into the interner it resolves against** — the union-payload sites via `adoptType(..., into: self.artifact.typeNames)` (Door A), the struct-field sites via `fieldTypeOf(..., into: self.artifact.typeNames)` — so the `:582` panic is **structurally unreachable** from the parser. Probed ALL supported backward-ordering payload shapes (scalar / ranged-alias / String / struct via throw-catch AND construct-match; adversarial decoy-types shifting the earlier file's interner) → all correct, no leak; the nested-union payload is cleanly rejected (E2015, a separate later rung). Enabled `error.cross-file-throws-caught-later-file` + authored 3 backward managed-payload coverage tests that were previously UNPINNED (String/struct drop + interner-shift). Did NOT bisect the exact commit that flipped red→green (likely the #54 A/B/C + #56/#59 interner work + the always-present `foldFile` seeding); the STRUCTURAL closure above is the durable fact, not a story that fits ([[reference-stale-bin-maxon-exe]] — attribution is a measurement). ⏭ **Still separate: #54 Slice B2 (UNION call-arg type-identity)** — that needs a pre-erasure param-name carrier, NOT the prescan, so the prescan's existence does not close it. See [[project-open56-interner-mismatch]] (the recurring interner-mismatch family — an id resolved against the WRONG interner; cure = re-intern before resolving, or keep resolution inside the interner's owner).

### 53. ✅ FIXED 2026-07-19 (775→782) — shv2 does not parse LABELED union-construction arguments
`LexErr.pair(7, column: 13)` → `E2004 "Undefined variable 'column'"` — union construction accepted only POSITIONAL args. **⭐ THE SCOPE WAS BIGGER than "add labels": the ORACLE applies the CALL-arg rule to union construction** (first arg positional, second+ MUST be named), verified by running the bootstrap: `pair(7, column: 13)`→ok, `pair(7, 13)`→REJECT "second must be named", `pair(line: 7, …)`→REJECT "first arg named". So shv2 was wrong BOTH ways — it accepted multi-arg positional (which the oracle rejects) AND rejected the labeled form. **FIX:** `parseEnumBoxConstruct` (Parser.maxon) rewritten + 3 helpers (`parsePayloadArgs`/`payloadSlotFor`/`requireAllPayloadsProvided`) to build LAYOUT-parallel slot columns, mirroring `parseStructLiteralFields` × `consumeArgLabel`: arg 0 positional→slot 0, later args named→slot by payload FIELD name (`PayloadField.name`), `slotBareLocal` per SLOT (the ownership trap — a reordering named managed arg must poison the right source). Reuses shv2's struct-field diagnostics (E2052 first-named, E2053 second-positional, E3018 unknown+duplicate [pre-existing shared code], E3086 missing). Hardened a payload-free-with-parens crash → clean E2015. **The "shv2 tests what shv2 does" trap in action:** two ENABLED shv2-authored `union-managed-payload.md` tests used the oracle-rejected positional form (`both("a","b")`) — RELABELED to `both("a", b: "…")` (codegen-neutral: IR byte-identical, only the source line moved). ENABLED corpus test `throw-transfers-ownership/propagate-throw-otherwise-no-binding-decrefs` (used the labeled form, was RED). Implementer + INDEPENDENT REVIEW (merge-as-is; 20-probe table incl. out-of-order labeled managed → E3102 on the right binding, leak-free; duplication = justified parallelism not one-fact-twice). ⏭ **RESIDUAL (small follow-up, review-flagged, non-blocking):** the missing-arg reuse of E3086 says "…and it has no default value" — misleading for a union payload (which can NEVER have a default); a union-appropriate message wants a dedicated ParseError variant. ⏭ **STILL DISABLED (a DIFFERENT mechanism, NOT this rung):** `propagate-throw-through-local-struct` — blocked by constructing a `Self{}` struct literal with a boxed-union FIELD (E2015, a later P1.4b wave), verified; the labeled union arg inside it parsed fine.

### 54. ✅ FULLY CLOSED 2026-07-20 (Slice B2 = `8b2bec781`, 788→792) — shv2 now name-checks aggregate TYPE IDENTITY everywhere. Was: a mismatched struct/union dropped with the WRONG destructor = wild free / SEGFAULT (139), a type-soundness hole
Every struct shares the `structRef` tag and every boxed union `named`, and the checks are TAG-level only, so `return BoxB` from a `returns BoxA` fn (BoxA has a String field, BoxB scalar-only) COMPILES and `__destruct_BoxA` frees BoxB's scalar as a String pointer → exit 139, reachable with NO error handling. **⭐ USER (2026-07-18): v1 already fixed this — PORT v1's type-identity check, don't re-derive.** ✅ **Slice A** (`16c729b07`, 736→741): return / reassign / `otherwise`-value — ported v1's exact-interned-name check into ONE shared `aggregateNameOf`/`namedAggregatesConflict` pair, applied additively; **the ONE-FACT-WRITTEN-TWICE signature bug ABSENT (first clean P1.4-era review).** ✅ **Slice B** (`aa2ac2509`, 741→743): consuming CALL-ARG for STRUCTS — published `valueNameIds` on `IrFunction` + a name branch in `SemanticCheck.checkArgTypes`; `namedAggregatesConflict` moved to shared `TypeRules.maxon`; 2nd clean review. ✅ **Slice C** = value-position `match … gives` merge = **#45** (fixed). ✅ **Slice B2 CLOSED** (`8b2bec781`, 788→792) — UNION call-arg. A union PARAM is erased to `integer` at `TypeResolution.resolveNamedType:192` (dropping its `named` identity — the OPPOSITE of a struct, which keeps `structRef(id)`), so it survives to SemanticCheck as a bare integer and the tag check waves a mismatched `named` arg through. FIX = a **pre-erasure per-param NAME carrier**: `FuncSignature.paramAggregateNames as ByteArrayArray` (`Project.maxon`), filled at parse in `Parser.paramAggregateNamesOf`/`recordFuncSignature` where the param is still `named`, threaded STRAIGHT THROUGH `TypeResolution.syncResolvedSignature` (the load-bearing carry-through — the resolution that erases the type must not drop the name), read in `SemanticCheck.checkArgTypes` via a generalized `aggregateNameFor` (the `structRefName`→both-tags twin) + the shared `namedAggregatesConflict` → E3005. **It travels by NAME (ByteArray, no ids) — structurally immune to the #52 interner landmine** (the arg side resolves against the merged `project.typeNames`, arg name-ids remapped project-global at `mergeArtifact`). ⭐ The `funcParamConsumes` pattern was the TEMPLATE but NOT reusable as-is (it records only CONSUMING functions; the borrowed-arg reject needs ALL functions) — a per-`FuncSignature` field instead. ⭐⭐ CLEAN independent review (merge as-is): cross-file #52 probed 5 ways (id-shift, declared-after-use) all oracle-matched; NO over-rejection (float/ranged/cross-alias/enum/labeled-out-of-order/method-receiver all correct); the `aggregateNameFor`/`aggregateNameOf` twins are a FORCED tier-boundary copy (the two interners never co-exist) with the comparison single-homed. **`maxonOpIdxToString` (#51) DROPPED from scope — it is NOT in shv2** (a v1 printer, already guarded there). [[project-open54-slice-b2-closed]] [[project-open54-ab-closed]] [[project-open56-interner-mismatch]]

**⇒ THIS COMPLETES ALL OF P1.4** (moves+borrows P1.4a + errors P1.4b Waves 1/2a/2b/2c + the aggregate-identity quartet A/B/B2/C). Next major = P1.5 (closures + async + escape analysis), which also re-enables OPEN #64 (borrowed-container field throw) and #65 (implicit-self).

### 55. ✅ FIXED 2026-07-19 (774→775) — `try <voidThrowingCall> otherwise <scalar>` in STATEMENT position PANICS in lowering
`try doIt(9) otherwise 0` where `doIt` returns void (throwing) → `LowerMaxonToStd:104: valueTagToStdType: a 'void' tag names no value` (a void try-result merged with a value fallback mints a void phi block-arg lowering can't type). Confirmed PRE-EXISTING on `main`. A void `try` whose `otherwise` supplies a value is ill-formed (the success path has no value to bind). **FIX:** a `tagOf(result) == void` guard in `parseUnboundOtherwiseForm`'s value-form branch throws **E3059** (`errorTypeMismatch`) — the statement-position twin of `parseTry`'s existing value-position void reject (both E3059; E3059's doc IS "an otherwise handler's value has a type other than the try expression's type"). PROBED all `otherwise` forms on a void try first: ONLY the value form panicked — `ignore`, single-statement `return`/`throw`, `otherwise 'block' … end`, and `otherwise (e) 'block' … end` all compile correctly and are untouched. Red-before-green: authored `error.void-try-otherwise-value` (maxoncstderr). Scale-neutral (delta 0; the guard is O(1) and never fires on the corpus). Same "latent accept-and-crash → clean reject" family as #50/#51/#62.

### 56. ✅ FIXED 2026-07-19 (`f42e2c00c`..`c3dec820f`, 743→744) — a VALID nested struct whose field type has a `static create` with a RANGED-INT-ALIAS param PANICKED the destructor walk (`SignatureIndex:822`). Root cause was NOT ranged-alias classification (the implementer's first hypothesis, WRONG) — it was an INTERNER MISMATCH: `addNestedStructDestructors` re-fetched the struct layout from `signatures` (ids minted against `signatures.typeNames`) but resolved that id against `project.typeNames`; declaring the alias shifted project ids → the same id named `BoxA` in one interner and `Integer` in the other. FIX: take the PROJECT `StructLayout` the caller already holds; review single-homed the `(project layout, project.typeNames)` pairing into `managedStructFieldDropCallee`. **LESSON: verify your own diagnosis — which pass raises this and what does it read** ([[feedback-verify-your-own-diagnosis]]). Same family as #52. [[project-open56-interner-mismatch]]

### 57. ✅ FIXED 2026-07-19 (F2b, `c428191ac`, 762→763) — a `float` passed as a NON-FIRST argument PANICKED x64 codegen (`X64Backend:603 rdx→xmm0 crosses register files`; float args assigned an integer arg register). A float param/arg now travels in XMM (x64) / SIMD d-reg (arm64) by a SEPARATE int/FP counter (v1's convention, not positional): `argFpRegisters` + `lowerParam` captures from the XMM slot; an extracted `emitCallArgMoves` routes each arg by the callee's param class; `buildCalleeParamClasses` (a u64 float bitmask). **⭐⭐ REVIEW CAUGHT A REAL ARM64 COMPILER CRASH (the signature bug again): the SHARED liveness clobber census decoded float-arg regs through the x64-only `operandRegister`, so an arm64 `d1` (RegNum 32) panicked on ANY 2+-float-arg call — the local x64 suite was green over it; a `--target=arm64-macos` CROSS-COMPILE probe caught it.** Fixed target-neutrally. **⭐ LESSON: a cross-compile probe is a cheap EARLY catch for an arm64-affecting change.** ✅ **int→float ARG widening DONE (F3a, `cc698eaf9`, 763→766)** — `LowerMaxonToStd.lowerCall`/`lowerTryCall` insert a `siToFp` for an int arg into a float param (the parser can't: callee param types are a post-merge whole-program fact); the shared `widenIntArgsToFloatParams` reuses `promoteToFloat`, guarded to int→float only, with an allocation-free fast path (scale delta 0). ⏭ Remaining float follow-up: **#60**. The float feature ("do BOTH": F1 → F2a → F2b + F3a arg widening) is COMPLETE bar #60.

### 60. ✅ FIXED 2026-07-19 (F3b, `0b113576a`, 766→767) — a THROWING function returning float compile-PANICKED on the THROW edge (`lowerThrowError`/`lowerPropagateError` emitted an i64 GPR zero as the throw-edge PRIMARY default, routed to XMM0/d0 for a float return → `X64Backend:603` cross-file `rax→xmm0`). FIX (one tier, `LowerMaxonToStd`, target-neutral): a per-function `currentReturnType StdReturnType` + `emitZeroConstOfReturnType` mints an XMM f64 zero for a float return (else the unchanged i64), at the 3 throw-edge primary sites. **⭐ ANTI-DRIFT (review: DERIVE-grade): keyed off the SAME `stdReturnTypeIsFloat`/`IrFunction.returnType` the backend's `emitReturnValueMove` reads — one value on one object, cannot disagree.** Non-float throwers (void/int/String/struct) keep the i64 default → byte-identical goldens. Authored red-first `error.throwing-float-return`. **⇒ THE FLOAT STORY IS COMPLETE: nameable (F1) → return (F2a #50) → args (F2b #57) → int→float widening (F3a) → throwing-return (F3b #60).**

### 62. ✅ FIXED 2026-07-19 (774/0; found by OPEN #52's verification probe, ORTHOGONAL to #52) — a block-form `try … otherwise … end` STATEMENT inside a `while` loop PANICS the parser
**FIX:** taught `opensBlockAt` (Parser.maxon) that a block-form `otherwise` opens a block, via a new `otherwiseOpensBlock` predicate — `otherwise 'label'` (a bare charLiteral) OR the exact SHAPE `otherwise ( identifier ) 'label'` (binding form). The non-block forms (`ignore`, a fallback value, single-statement return/break/continue/throw/panic) open nothing. The shape check (not merely a leading `(`) is load-bearing: `otherwise` IS a legal enum case name (`tokenNamesEnumCase`), so a keyword-spelled match arm `otherwise(x) then` also leads with `otherwise (` — the `( id ) 'label'` shape tells the two apart (a payload case is `( bindings ) then/gives`). ONE fix closes BOTH consumers of `opensBlockAt`: the while/if carried-var extent scan (`blockStatementEndIndex`, the panic) AND the declaration sweep (`blockDepthAfter`) — the latter was a LATENT miscount (a block-form `try` in a function body prematurely decremented depth, so a following local `let`/`var` was recorded as a spurious top-level binding; VERIFIED pre-fix but UNOBSERVABLE — recording is tolerant and the real parse resolves locals correctly, so no wrong answer manifested; no spec pins it since there is no observable difference). Red-before-green: 3 authored specs (`error.try-binding-block-in-while`, `error.try-label-block-in-while`, `error.try-block-in-if`) — all panicked pre-fix, green post. Scale-neutral (delta 0 all rungs; corpus has no block-form try). ⭐ Same machinery / lesson as **#5**. Original report below.

<details><summary>original report</summary>
`while … 'loop' … try risky(i) otherwise (e) 'h' … end 'h' … end 'loop'` → `panic at Parser.maxon:6210: parseWhileStatement: the token scan predicted the closing 'end' at token N but the parser closed the last body at token M — 'opensBlockAt' does not know every statement that opens a block, so the carried-variable set it computed may be missing a carried var`. **Reproduces SINGLE-FILE, with an enum error type, NO cross-file ordering** — so it is NOT a #52 interner bug; #52's probe merely surfaced it. Root cause is the SAME machinery as **#5**: `parseWhileStatement` decides its carried-variable set up front from a token pre-scan whose block-depth counter (`opensBlockAt` / `blockDepthAfter`) does not recognize a **block-form `try`/`otherwise`** as opening a block, so the loop-body extent is mis-predicted and a deliberate self-guard PANICS (rather than silently miscompiling — it fails LOUDLY, which is why it is a panic and not a wrong answer). Controls all compile fine: `while` + plain `match`, `while` + `if`-block, `while` + **single-statement** `try … otherwise 0`. ⇒ Fix = teach the while pre-scan's block-opener detection about the block-form `try` (and its `otherwise` block), the same shape as the P1.0d scalar-core `opensBlockAt` gap. Its own small parser rung; red-before-green spec = the minimal repro above.
</details>

### 63. 🔴 BOOTSTRAP BUG (found by #47's implementer while probing throw ownership; PRE-EXISTING on `main`, NOT introduced by #47) — `throw <local heap-error var>` / `throw e` re-throwing a caught HEAP error PANICS (`mm_incref NULL`)
`let e = PErr.unsupported("…"); throw e` (a payload-carrying error in a LOCAL) and `otherwise (e) 'h' throw e end` (re-throwing a caught HEAP error) both PANIC with `mm_incref called with NULL pointer`. **Root cause (NOT #47's throw-of-call-result incref — a DIFFERENT throw-ownership gap):** `ParseThrow` (`maxon-sharp/Compiler/2-Parser.cs:8534`) and its two siblings (`:8720`, `:14096`) emit `new MaxonScopeEndOp(GetScopeEndVars())` **WITHOUT the `keepVars` argument** — whereas the RETURN sites (`:8434`, `:22724`) compute `keepVars` to protect the transferred variable. So for `throw e`, `scope_end` DECREFS `e` (rc→0, freed) AND NULLS its slot (`store 0, e`) BEFORE the throw's incref loads it → NULL. Proof: `return e` and `throw e` produce structurally identical maxon-IR (`scope_end [e,…]` then terminator); return works because `scope_end` sees `e` in `KeepVars` (emits `mm_trace_transfer`, no decref/null), throw does not. The existing `error.rethrow-caught-binding` spec passes only because it uses a SIMPLE enum (ordinal path), never the heap branch. **⇒ A complete fix is a throw-ownership `keepVars` addition at the 3 throw sites AND teaching `LowerThrow` to skip the incref for a KEPT owned local (mirroring `LowerReturn`) — else `keepVars` alone turns the panic into a leak.** Its own maxon-sharp rung (adjacent to #47/#16's throw work). Repros: `…/scratchpad/impl47/ctrl_local.maxon`, `ctrl_rethrow.maxon`. **shv2 hits this too** — `throw <local error>` / `throw e` re-throw of a heap error are natural patterns.

### 64. 🔵 P1.5 (deferred by P1.4b Wave 2c review — a SOUND case conservatively rejected, NOT a bug) — re-enable `throw <boxed-union field of a BORROWED container>` once cross-call move tracking exists
Wave 2c's review found that `throw self.field` of a boxed-union field through a BORROWED receiver, where the caller CATCHES and then RE-READS the moved-out field, was a reachable SILENT SEGFAULT (139): `moveOutThrownField` nulls the field slot through the borrow (the CALLER's box), which is sound when the caller tears the container down (propagation) but a use-after-move on re-read. Fixed 2026-07-20 (`807180533`) by **gating the field move-out on an OWNED-LOCAL container** (`valueIsOwnedHeap(lastFieldReadBox)`) — a borrowed `self`-field now gets a clean E2015 (`error.boxed-union-field-throw-borrowed-self`), consistent with the twin `moveOutManagedPayload` which already refuses a borrowed scrutinee. **This is CONSERVATIVE: the propagate / catch-without-reread callers are SOUND but rejected too**, because nothing at the throw site can tell a re-reading caller from a tearing-down one — that needs cross-call move tracking (escape analysis, P1.5). ⇒ At P1.5, once a field moved out of a borrowed container can be tracked into the caller (poisoning the re-read into an E3102), the throw-site gate can widen from "owned-local" to "owned-local OR (borrowed AND no surviving re-read of the field)". Same family as the P1.4a loop-exit-reconcile and the `rc-enum-struct-payload-freed` borrowed-param deferral. Re-enables `throw-transfers-ownership/propagate-throw-through-local-struct` (with #65).

### 65. 🔵 P1.5-adjacent (surfaced by P1.4b Wave 2c) — shv2 has NO implicit-self method resolution: a bare `bump()` inside an instance method is a FREE call → E3004
Inside an instance method, a bare call `bump()` (meaning `self.bump()`) does NOT resolve — shv2 treats it as a free function and raises `E3004: call to undefined function 'bump'`. (Bare self-FIELD reads/writes DO work — `pendingError`, `hasPending = …`; only bare method CALLS don't.) The language needs `<EnclosingType>.<name>` method-name resolution with static-vs-instance receiver handling — oracle-verified against the bootstrap: a bare instance `helper()` prepends `self`, a bare static `mk()` gets NO receiver. A distinct front-end feature/rung, NOT boxed-union fields and NOT #64. One of the TWO blockers (with #64) keeping `throw-transfers-ownership/propagate-throw-through-local-struct` disabled.

### 66. ✅ FIXED 2026-07-20 (on a WINDOWS host — the first that could execute the IOCP path) — the x86 GT double-schedule. The bug was REAL but NOT where this entry first pointed: `__io_check_completions` is DEAD code; the live hole was the overlapped-submit ARM-WINDOW in the `__io_complete_gt` path.
The original filing named `__io_check_completions` by transposing the arm64 `__io_poll_kqueue` fix (`8163f1056`). **On x86 that transposition does not hold.** `__io_check_completions` drains a done queue that **NOTHING FEEDS** — `__io_enqueue_completion` has ZERO callers (it and `__io_dequeue_completion` are leftover dead code from before the migration to the direct `__io_complete_gt`, whose own comment says it was added to *"bypass the intermediate done queue, eliminating the race where any worker could dequeue the completion"*). x86 reaps overlapped completions on a **dedicated IOCP drain thread** (`__io_completion_loop` → `__io_complete_gt`), where arm64 polls the kqueue INLINE. So the function this entry named is inert, and my first read wrongly concluded *"dead code, no live bug"* — [[feedback-verify-your-own-diagnosis]].

**The REAL hole (found by an adversarial review that tried to REFUTE the "no bug" read):** the two backends implement the `ioYielded` protocol with **OPPOSITE polarity**, and nothing made them agree (this entry's own through-line). On **arm64** a RUNNING GT has `ioYielded==0`, set to 1 only when `__gt_context_switch` fully parks it — so its skip-if-0 gate covers the whole armed period. On **x86** a running GT carries a **STALE `ioYielded==1`** (set by its resumer, never cleared on resume), and the overlapped-submit paths cleared it to 0 only in the async-yield branch — **AFTER `ReadFile`/`WSARecv` armed the IOCP op**. A completion reaped in that arm→clear window saw the stale 1, `__io_complete_gt`'s spin-gate passed, and it enqueued a GT **still executing on some M's stack** → a second M resumed it onto its pre-`__gt_morestack` (relocated, munmapped) stack → **exit 139**. The park-side timing asymmetry this entry warned to *"verify FIRST"* IS the root cause.

**REPRODUCED + FIXED — controlled experiment (this Windows host, 16 CPUs, `maxon-shv2 spec-test --workers=16`):**
- unwidened, no fix: **24/24 green** (latent — the arm window is sub-microsecond).
- artificially WIDENED the arm→clear window (~7 ms spin on the pipe read path), no fix: **15/20 CRASHED (exit 139)**.
- widened + fix: **20/20 green, byte-identical**.
- final (no widening) + fix: **30/30 green**; `-j1`==`-j16` byte-identical; C# suite **3045/0**; `git status specs/ specs-shv2/` EMPTY.

**FIX** (`X86CodeEmitter.Runtime.cs`, all THREE overlapped submit paths — pipe / file / socket, "one fact written three times"): clear `ioYielded=0` **BEFORE arming** the op (the order `__io_submit_sync` already used), and restore `ioYielded=1` on the sync-completion paths (their IOCP packet is suppressed by `FILE_SKIP_COMPLETION_PORT_ON_SUCCESS`, so the GT never parks and no completer targets them). Now `ioYielded==0` for the entire armed period, so `__io_complete_gt`'s spin-gate blocks until the GT actually parks and its resumer republishes `ioYielded=1`. **NO hang risk** — the gate still SPINS (never skips), so no wakeup is lost; that is why the x86 fix is a "clear earlier", NOT the arm64 "decline-to-consume" (which an IOCP completion cannot afford — it is consumed on dequeue). arm64 is UNAFFECTED (its running-GT `ioYielded==0` invariant already covers the arm window).

⏭ **FOLLOW-UP (deliberately NOT done here — orthogonal, kept the commit focused on the race):** remove the dead done-queue machinery — `__io_enqueue_completion` (0 callers, x86 AND arm64), `__io_dequeue_completion`, `__io_done_head`/`_tail`/`_cs`, and `__io_check_completions` + its 8 inert call sites. ⚠ `__io_done_event` is LIVE (park loops wait on it) — KEEP it. A check that the two backends' wakeup paths agree on the `ioYielded` protocol would make this whole class unrepresentable (cf. `assertCallClobberConsistent`, #23) — the file's own through-line: the two backends must agree, and nothing makes them.

### 67. ✅ CODEGEN LANDED 2026-07-20 (`801505d41`) — arm64 function-value ADRP+ADD relocation channel wired. ⏭ Un-restriction of the cases to arm64 remains (needs an arm64 RUNNER).
**The codegen is DONE.** `arm64EmitLeaFuncAddr` now materializes a function's `.text` address (ADRP+ADD) instead of panicking: a new `Arm64FuncAdrpReloc {codeOffset, funcName, textOffset}` + `Arm64FuncAdrpRelocArray` on `FunctionCodeChunk`/`CodeResult` (`Targets/Shared/CodeResult.maxon`), filed by the encoder (`Arm64Backend.maxon`), resolved post-layout by `arm64ResolveFuncAdrpRelocs` (so a forward-referenced function resolves), and patched against `textVmAddr+textOffset` by `patchArm64FuncAdrpRelocs` — called by BOTH `MachOWriter` and `ElfWriter`. Ported v1's `Arm64FuncAdrpReloc` record + patch math, mirroring shv2's own working `arm64LeaRdata`/`arm64LeaGlobal` channel; the review factored the shared `ADRP+(ADD/LDR)@+4` body into `patchArm64AdrpPair` (also de-duped the import-call path). `FunctionCodeChunk.createWith`/`CodeResult.create` signatures kept UNCHANGED (new fields init empty, assigned post-construction) so x64 callers are untouched.
**Verified on the Windows host (max possible — NO arm64 runner here):** function-value AND closure samples compile clean on `--target=arm64-macos` AND `--target=arm64-linux` (no panic); objects validate (`llvm-objdump`/`readelf`); encoding disassembled correct incl. a forward-reference probe (ADRP page + ADD offset resolve to `.text`); x64 **821/0** + wasm **808/0** UNAFFECTED, worker-invariant, fragments byte-identical, scale linear.
⏭ **REMAINING — un-restrict the A1 function-value + A2a closure cases to arm64.** NOT done here because this host cannot RUN arm64, so an un-restriction can't be verified green; several cases also use struct/union/String, whose arm64 support is runtime-floor-dependent (arm64-macos gates M2+ features; arm64-linux runs most). ⇒ Un-restrict on an arm64 RUNNER (parallel repo / Mac), case-by-case per each case's feature set, run-verifying each. The scalar + compile-error cases should un-restrict cleanly; the struct/union/String ones follow the existing arm64 per-target gating.

### 68. 🔵 P1.5-A1 FOLLOW-UP (independent review) — statement-position indirect calls are unsupported ⇒ a `function()` VOID type is uncallable
`parseStatement`'s `callStmt` branch (`Parser.maxon:3984`) sends a bare `f(21)` STATEMENT straight to the direct-call path with NO function-typed-local diversion — the mirror of the diversion that DOES exist in `parsePrimary` (`Parser.maxon:8931`) for value position. So `f(21)` as a discarded statement, and calling a `function()` void-typed value, both give a MISLEADING `E3004: call to undefined function 'f'`. The spec-documented `typealias Callback = function()` (`/specs` first-class-functions, `status: stable`) cannot be CALLED, and the void-handling arms (`readFunctionTypeAlias` void, `mintIndirectCallResult` void) are unreachable at statement position. **Clean error, not a crash**, and OUT of A1's enabled slice (all 21 A1 cases are value-position; `field-void-statement` is separately disabled for Array/P1.7). ⇒ Own slice: wire `parseStatement` like `parsePrimary` + author statement-position + void-call specs. Natural fit for A2 or a small front-end follow-up.

### 69. 🔵 P1.5-A1 FOLLOW-UP (independent review) — two E3005 routing sites are unpinned (a #27-class gap, working-by-probe)
The function-into-non-function E3005 CONDITION is single-homed (`checkDeclaredType`→`DeclaredTypeMatch.functionIntoNonFunction`, `TypeRules.maxon:222`) and pinned by the global/local/return corpus cases, but the ROUTING at the **call-arg** site (`SemanticCheck.maxon:218`) and the **field-store** site (`Parser.maxon:9435`) has ZERO direct tests — a sabotage removing the `checkDeclaredType` call at either stays green (both probe-verified WORKING by the reviewer: `E3005 … as argument 'x'` / `E3005 … to field 'slot'`). ⇒ Author `function-value-as-arg-errors` + `function-value-into-field-errors` (oracle-checked against the bootstrap, which also rejects E3005-class; compile-error cases are target-neutral), → 815/0. Same #27 disease (a gate pinned by nothing), a lesser instance (shared condition IS pinned + match-exhaustiveness guards a missing arm).

### 70. 🔴 BOOTSTRAP BUG (found by P1.5-A1's implementer) — a managed `Array` payload bound as a variant's LAST field in a LARGE match corrupts the scrutinee's decref
Binding a managed `Array` payload that is the TAIL field of a variant, inside a large `match` arm, triggers a C# bootstrap refcount miscompile — a `mm_decref` on a sentinel pointer that crashed EVERY `try`/`throw` program's lowering (44 regressions when `indirectCall` was added to the big `lowerOp` match). Isolated by bisection. **Worked around in shv2 A1** by routing `indirectCall` to a dedicated `lowerIndirectCall` (`LowerMaxonToStd.maxon:565`) whose match binds only `(result, callee, args)` — no managed-Array tail field. The self-hosted compiler won't need the workaround; the bootstrap's `match`-arm ownership analysis is the real bug. File for when the bootstrap's match handling is next touched.

### 71. 🔵 P1.5-A2a FOUND — the inline conditional expression `a if c else b` is NOT PARSED (E2010); blocks all TERNARY closure cases
shv2's parser does not accept the value-position ternary `<expr> if <cond> else <expr>` — `let h = f if c else dbl` gives **E2010** (the bootstrap parses it; confirmed by direct test). This is a PRE-EXISTING front-end gap unrelated to closures, surfaced by A2a: `first-class-function.non-capturing-closure-through-ternary` stays disabled for this reason (the other 8 A2a cases enabled). ⚠ It is ALSO a prerequisite for EVERY A2b ternary case (`capturing-closure-in-ternary-arm-errors`, `-in-ternary-to-global-errors`, `-in-ternary-used-in-frame-errors`, `non-capturing-closure-through-ternary`) — A2b's escape rule refuses a capturing closure as a ternary ARM, which cannot be tested until the ternary parses. ⇒ **Its own small front-end rung (inline conditional expression), a prerequisite for A2b's ternary coverage.** Author red-first specs; oracle-verify against the bootstrap.

### 72. 🔵 P1.5-A2a FOUND (bootstrap, by the A2a review) — a helper storing into ~11 ALIASED `self` array-fields makes the shv2 SELF-COMPILE `mm_decref`-crash (double-free via `__destruct_FuncSignature`)
Not a bootstrap BUILD error — a SELF-COMPILE-time double-free: a method that reassigns ~11 aliased `self` array-fields (the per-body column-reset pattern) makes the shv2 compiler the bootstrap emits crash in `__destruct_FuncSignature`. It FORCED A2a to keep the per-body column reset INLINE at 3 sites (`self.valueTags`… reset) rather than factor it. **The review's recommended clean fix: a swappable `BodyColumns` sub-object** (one self-field reassign, `self.bodyColumns.valueTags`) — but that touches hundreds of `self.valueTags`→`self.bodyColumns.valueTags` sites, so its own rung. Same family as the bootstrap match-arm bug (#70): the C# ownership analysis mishandles multi-aliased self-field stores.

### 73. 🔵 P1.5-A2b-1 (independent review) — capturing-closure escape checking is PER-SITE (enumerated), not a DERIVED invariant; a future value-sink silently reopens the hole
A2b-1's E3099 escape rejection checks each SINK individually. The review found **FOUR reachable sinks the implementer missed** (all fixed to clean compile errors in `96daddca6`, each spec-pinned): **F1** struct construction `T{op: closure}` (was a compiler PANIC → E3099), **F2** call-arg `apply(f,x)` (silent SEGFAULT 139, env not threaded to a generic callee — this is A2b-2's pass-down, rejected E2015 until then), **F3** match-expression `gives` arm (SEGFAULT, phi carries fn-ptr not env → E3099), **F4** `try c() otherwise <closure>` (SEGFAULT 139 on the error edge → E3099). F3/F4 are the reachable twins of the *disabled* ternary tests. Enumeration is now COMPLETE for today's grammar — but a future value-sink (a new literal/merge) reopens it silently. ⇒ **Replace the per-site checks with ONE DERIVED INVARIANT**: "a capturing-closure VALUE may appear ONLY as the direct callee of an indirect call OR a frame-local binding; every other position is refused," checked at the value-flow funnels. The DERIVE cure ([[feedback_one_fact_cure_ladder]]) for the enumerated-per-site disease. Its own hardening rung (touches the whole value-flow surface). NOT a blocker — per-site checks are complete+correct today.

### 74. 🔵 P1.5-A2b-1 — payload-binding IMMUTABILITY divergence: shv2 refuses a capturing-closure payload-binding store with E2013 (stronger), not the bootstrap's E3099
`capturing-closure-in-payload-binding-errors` disabled: shv2 declares union payload bindings IMMUTABLE frame-local copies (`Parser.maxon:7941`, `mutable: false`), so `run(op) then op = <closure>` is E2013 — the bootstrap's E3099 route (a payload binding is an ALIAS INTO the heap box, a write-back) doesn't exist in shv2 (a safer guarantee, no write-back unsafety). A divergence to CARRY, not a bug; author an shv2 spec pinning E2013 if it should be pinned. Its own rung only if shv2 ever makes payload bindings mutable aliases.

### 75. 🔵 P1.5-A2b-1 — `capturing-closure-name-not-leaked` needs borrowed-struct-copy-return (`var hh=h; return hh` → E2015, no struct clone)
Disabled: the case does `var hh = h; hh.op = op; return hh` — a borrowed-struct COPY-then-return shv2 refuses E2015. The case's actual PROPERTY (the escape mark keys on the VALUE, so an unrelated param `op` is NOT falsely flagged) IS independently verified: `hh.op = op` compiles PAST the field-store escape check (a false flag would throw E3099 there) and reaches E2015 at the return. Enable when struct-copy-return lands.

### 76. 🔵 P1.5-A2b-1 — `capturing-closure-bound-outside-loop-called-inside` restricted wasm-only (x64 interpolation register pressure E5001, closure-INDEPENDENT)
The exact body (`print("i={i} -> {r}\n")` + `acc=acc+r` in a loop) needs 16 live values > the 14-reg x64 pool → **E5001** ("refuse the search, not the spill"). Proven closure-INDEPENDENT: the same body with `let r = i + 5` (no closure) E5001s too (closure adds ~4 regs, plain ~2 — both hit the wall). Restricted to `wasm32-wasi` (no register cap) where it passes + covers the env-drop-timing probe; x64 timing is covered by `-rebound-in-loop`/`-called-from-nested-block`. Not a closure bug — the E5001 thesis firing on a high-pressure interpolation program; "fixing" it means spilling against that thesis (not intended).

### 13-UPDATE (P1.5-A2b-2) — ✅ THE INTERPROCEDURAL CLOSURE-ESCAPE half of #13 IS CLOSED for shv2 (the per-parameter escape summary)
#13 said the interprocedural route (a capturing closure passed as a call ARG to a callee that STORES/RETURNS it) "needs a per-parameter escape summary over the call graph — exactly shv2's P1.5." **A2b-2 built it** (`SemanticCheck.buildEscapeSummary`, `f6b8ec98e`): a whole-program pre-pass where a function-typed param ESCAPES iff its ValueId is used **anywhere except as an `indirectCall` callee** (store/global/payload store, call/try/indirect ARG, `ret`, or a **phi branch-edge input**) — a WHITELIST (sound-by-construction, the #73 DERIVE cure applied), transitive-pass conservatively = escaping. Caller check: a CAPTURING closure at an escaping param → E3099; a plain function value passes (no env). ⭐ The independent review found the removal of A2b-1's blanket guard had reopened this as a reachable SEGFAULT (139); the summary re-closes it. Coordinator adversarially probed 7 routes (store/return/copy-then-return/copy-then-store/conditional-return/payload-via-callee/loop-phi) — all E3099; accepts (plain-fn, call-pass-down) green. ⚠ The bootstrap does NOT reject these (it's #13, deferred there) — shv2 is now STRICTER (like #58); the red-first specs pin shv2's E3099.

### 77. 🔵 P1.5-A2b-2 FOUND — string-backed error-enum raw values (`case = "text"`) are unsupported (E2015)
`enum ApplyError implements Error { negative = "n must not be negative" }` → E2015 "string backings arrive with later rungs". Blocks `capturing-closure-passed-to-try-call` (disabled with this reason) — the try-call ENV THREADING it exists to test is implemented + verified green with an INT-backed error enum (exit 114). Orthogonal to closures/env. ⇒ Its own rung (string-backed enum raw values); may unblock other corpus.

### 78. 🔵 P1.5-A2b-2 FOUND — a wasm function VALUE returning a non-i64-width type (ExitCode/u32) traps `call_indirect` ("indirect call type mismatch")
A1's wasm `call_indirect` functype is arg-count-derived and hardcodes `(i64ⁿ)→i64` (`StdToWasm.maxon:414`) because the `callIndirect`/`indirectCall` op carries **no result type** (A1's "no signature to slot against"). So a function value RETURNING `ExitCode`/u32 (not i64) traps on wasm's strict functype check. Proven closure-INDEPENDENT (a plain function value with an ExitCode return traps; Integer-return shapes run clean). `block-local-method-receiver` restricted to x64-windows for this. ⇒ Thread the callIndirect's RESULT WIDTH to the wasm tier (the op needs a result-type field, or derive it). Its own follow-up.

### 79. 🔵 P1.5-A2b-2 FOUND (exotic, no corpus) — a closure that itself takes a function-typed PARAM doesn't get a companion env for it
`parseClosureExpression` doesn't run `bindClosureEnvParams`, so a closure like `function(g UO) gives g(5)` would call `g` env-less (a capturing closure passed to it would lose its env). No corpus case; noted for completeness. Also: the escape summary's transitive rule (pass-onward = escaping without consulting the target) may over-reject an exotic pass-through-that-only-calls chain — sound, no corpus coverage.

### 84. ✅ FIXED (p1.9-fixups) — `int(N>0 to u64.max)` signed-cascade MIS-REJECTED a bit-63-set in-range value
The P1.9 range guard uses SIGNED compares (shv2's `StdCmpPred` has no `ugt`/`ult`). That gave the right verdict for every enabled range and every range with a `0`/negative low bound, but NOT for a range with a **positive** low bound AND an **unsigned-max** upper: `int(N>0 to u64.max)`. Its upper check is elided (the upper rides as `-1`), leaving only a signed lower check `value < N`, which wrongly REJECTED a bit-63-set value that is unsigned-in-range. **Fixed WITHIN signed compares** — no `ugt`/`ult` added: out-of-range for this shape is `value >= 0 AND value < N` (a non-negative value below the low bound; a negative value is a huge unsigned IN range). `TypeRules.rangeIsUnsignedMaxUpper(low, high)` is the one place the shape is recognized; `boundChecks` emits the two-check cascade `value < 0 → IN RANGE, value < N → panic` (`RangeBoundCheck.onTrue`/`RangeCheckEdge` route a check's TRUE edge to `ok` instead of `panic`), which the backend fuses to `cmp value,0; jl ok; cmp value,N; jl panic; jmp ok`; `literalInRange` reads `value < 0 OR value >= low`. ✅ **ORACLE AGREEMENT RESTORED (p1.9-followups-2):** the bootstrap's `EmitRuntimeRangeCheck` (2-Parser.cs) is now ALSO unsigned-correct, the SAME way — it special-cases `int(N>0 to u64.max)` as out-of-range `value >= 0 AND value < N` (the `And(Ge(v,0), Lt(v,N))` idiom shared with range-pattern matching), so `1 shl 63 as int(5 to u64.max)` reaches a normal exit in BOTH compilers and `3` still rejects. The bootstrap's own **LITERAL** check was already unsigned-correct, so its runtime and literal checks now agree with each other AND with shv2's. Pinned in both suites: `specs-shv2/ranged-typealias.md` — `unsigned-max-upper-runtime-pass` (bit-63 runtime cast, panic→exit 7), `unsigned-max-upper-literal-in-range` (`-1` in range, E3005→exit 7), `error.unsigned-max-upper-literal-out-of-range` (`3` still rejected) — and the bootstrap twin `specs/ranged-typealias.md` `unsigned-max-upper-runtime-pass`. Existing `unsigned-domain-negative-sentinel-cast` (`(-1) as int(0 to u64.max)`) is the coupled guard for the `literalInRange` half. The E3005 range **display** also agrees now: the bootstrap's `FormatRange` already rendered a `u64.max` bound as its unsigned decimal (via `(ulong)`), and shv2's `formatRangeDisplay` was brought into line — a `u64.max` upper shows `18446744073709551615`, not the raw signed `-1` (`TypeRules.UnsignedFullUpperDecimal`); `error.unsigned-max-upper-literal-out-of-range` pins the new spelling.

### 85. ✅ FIXED (p1.9-fixups) — a TOP-LEVEL `let` cast into a ranged alias skipped the compile-time E3005
`let BAD = 300 as Byte` at MODULE scope compiled with no E3005 (exit 44), whereas the same cast in a function body was caught. P1.9 commits cast sites per-FUNCTION; a top-level `let` const-initializer isn't a function, so its cast site was dropped. Top-level lets are const-only (E2045 blocks calls), so ONLY the out-of-range-literal E3005 case was affected (no runtime-guard path). **Fixed in the const-eval path**: `Parser.applyConstCast` now looks the target alias up (`signatures.aliasOf`) and runs the literal check (`checkConstCastRange`/`reportConstCastIfOutOfRange`), throwing `ParseError.constantCastOutOfRange` — re-raised at the `let` by `raiseBindingFailure` and reported as E3005 by `Queries.reportParseError`, anchored at the `as` token. The message is the SHARED `TypeRules.literalOutOfRangeMessage` (extracted from `InsertRangeChecks` so the body-cast and const-cast E3005s cannot phrase one rule two ways). It depends on the #84 `literalInRange` fix: without it, the newly-run check would reject the stdlib's own valid `256 as ElementCount`/`1024 as ByteLimit` (`int(0 to u64.max)`) — the two fixes land together. Verified against the bootstrap oracle (`Value 300 is outside the range of 'SmallByte' (int(0 to 10))`, byte-identical). Pinned by `specs-shv2/ranged-typealias.md`: `error.top-level-cast-out-of-range` and `top-level-cast-in-range`.

### 86. 🟡 P1.9 FOLLOW-UP — arm64 goldens still need regeneration (✅ x64-linux HALF DONE 2026-07-21)
✅ **x64-linux half DONE** (`e7174c551`): the whole `fragments/x64-linux` tree was regenerated on this WSL2-capable host (contra the note below — this machine HAS a distro; the parallel repo's does not), 924/0, so P1.9's range-check cases + the ternary/return-guard cases are now pinned there. **arm64 half REMAINS** — `arm64-macos`/`arm64-linux` need a Mac/OrbStack.

P1.9 landed x64-windows (917/0) + wasm (902/0) green, per user ruling (2026-07-20/21) to merge on the runnable targets and defer the rest. The range-check guard is target-independent Std IR, so it changes arm64-macOS/arm64-linux **and** x64-linux codegen too, but **this Windows host cannot run those suites** — arm64 needs a Mac/OrbStack; x64-linux needs a WSL2 distro (`wsl -e /bin/true` fails here, no distro installed). The x64-linux **code** is correct and verified by IR (`osRangePanic` → bare `exit_group(1)`, no `mrt_panic` crash — `cb7107e8c`); only the `.test` goldens are missing/stale on those three target trees. ⇒ On a capable host, a one-shot per target: `maxon-shv2 spec-test --target=<t> --update-required` for `arm64-linux`, `arm64-macos`, `x64-linux`, REVIEW the diff (every change should be a pure guard-block addition + the OS-call substitution the drift-detector expects), and commit. Until then, those three suites will show P1.9's ~30 new cases + the 3 regenerated ternary cases + the 5 return-guard cases as failing-for-missing-golden.
