// assertion-failure.test.maxon — A red assertion: the `✗` marker, the FAIL block, and the `expected:`/`received:` detail.
//
// One spawning `test` per file — `fixtures.test.maxon`'s header owns why.

test 'assertion-failure prints the expected/received block and exits 1'
	try checkFixture("assertion-failure")
end 'assertion-failure prints the expected/received block and exits 1'
