// all-pass.test.maxon — The whole GREEN report: two tests, one file, both passing, and the summary under them.
//
// One spawning `test` per file — `fixtures.test.maxon`'s header owns why.

test 'all-pass reports two green tests and exits 0'
	try checkFixture("all-pass")
end 'all-pass reports two green tests and exits 0'
