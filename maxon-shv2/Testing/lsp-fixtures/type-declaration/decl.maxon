// Fixture for `lsp-selftest`'s type-declaration-position pin.
// ⚠ THE LINE AND COLUMN NUMBERS BELOW ARE THE ASSERTION. Do not reformat this
// file, and do not insert or remove lines, without updating the expectations in
// `LspDeclarationSelfTest`. `LspFixtureBox` is declared on line 7 and
// `LspFixtureShade` on line 11, each with its name starting at column 6.

type LspFixtureBox
	export let width as LspFixtureSize
end 'LspFixtureBox'

enum LspFixtureShade
	light
	dark
end 'LspFixtureShade'

typealias LspFixtureSize = int(0 to 100)

function main() returns ExitCode
	let box = LspFixtureBox{width: 3}
	let shade = LspFixtureShade.light
	print("{box.width}{shade.name}\n")
	return 0
end 'main'
