# This directory holds a FIXTURE that `lsp-selftest` loads as its own project. Without this marker
# the compiler's own build walks `maxon-shv2/Testing/` and compiles `decl.maxon` INTO the compiler,
# where its `main` collides with the real one (E3006). The same reason `track0/` carries one.
