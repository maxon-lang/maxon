# maxon-shv2 — The Two-Phase Plan

> **THE SPINE (user-set, 2026-07-13). Two goals, in order:**
>
> ### 🚩 **PHASE 1 — shv2 runs its own spec tests**, the way `maxon-selfhosted` does:
> ### **shv2 compiles its own spec harness**, and the shv2-compiled harness runs
> ### `specs-shv2/` — **IN PARALLEL, on a green-thread worker pool.**
> ### *(The mini-bootstrap. Formerly "Gate H".)*
>
> ### 🚩 **PHASE 2 — shv2 fully self-hosts.** shv2 compiles shv2; 3-stage bootstrap
> ### fixpoint (stage-2 == stage-3, byte-identical).
>
> Everything below is subordinate to those two. Phase 1's boundary is **measured**, not
> guessed (§"Phase 1 — the measured boundary"); Phase 2 is *"Phase 1, plus exactly what
> shv2's own source adds."*
>
> **Why this split earns its keep:** the harness is a **real, already-trusted, nontrivial
> Maxon program — ~30× smaller than the compiler**. A miscompile surfaces at 1/30th the
> debugging cost, on code we already know is correct. It is the nearest point at which
> "does shv2 compile real Maxon?" gets an answer, and it comes with a **free differential
> oracle** (below). Self-host is the back half of the project no matter how it is sequenced;
> Phase 1 is the checkpoint that keeps that from being a six-month integration hole.

---

## 📊 Status at a glance — every step, one table

*Snapshot 2026-07-22. Current suite head: `specs-shv2` **1162/0** (host). This chart is an INDEX,
regenerated by hand — each step's authoritative detail (and the exact residual numbers) lives in the
ladder tables further down. When a status here and the detail below disagree, the detail wins; fix this.*

**Status:** ✅ **complete** · ◑ **residuals** (core landed, open items — the rung is NOT done) · ⬜ **pending** · ⬆ **promoted/moved**

### Phase 1 — the ladder

| # | Mechanism | Status | Suite | Open residuals / notes |
|---|---|:--:|:--:|---|
| **P1.0o** | compiler traces itself (Workstream O1 — `__DebugStream`) | ✅ | — | — |
| **P1.0a** | parallel worker pool for the harness | ✅ | — | — |
| **P1.0b** | Workstream S — `disabled-test:` marker + on-demand porting | ✅ | — | marker shipped; bulk port intentionally NOT done (ongoing ratchet, user directive) |
| **P1.0c** | measure the stdlib cone | ✅ | — | settled `Set`∈Phase 1, `Map`∈Phase 2 |
| **P1.0d** | complete the SCALAR CORE | ✅ | 126→355 | umbrella; slices below |
| P1.0d.2 | `not`/`and`/`or` · bitwise · chars | ✅ | — | — |
| P1.0d.3 | `a / 0` ⇒ clean panic | ✅ | 279→281 | — |
| P1.0d.4 | floats (f64) + F1/F2/F3 number-type story | ✅ | →1313 | **COMPLETE 2026-07-22.** A (exact float literals) · C (`floor`/`ceil`/`round`/`sqrt`) · D1 (`maxon-sharp` inverse bitcast `bitsToFloat`) · B (#19 float RANGE CHECKS) · A′ (`i64-min-literal` + literal-text overflow diagnostics + `-N` fold) · **D2 (float const-fold via D1's bitcast, NOT software IEEE-754)**. Every listed residual (#19, `floor`/`ceil`/`round`, float const-fold) CLOSED. ⚠⚠ **BUT THE int→float PROMOTION HALF OF THE NUMBER-TYPE STORY WAS NEVER FINISHED, AND IT HELD A SILENT WRONG ANSWER UNTIL 2026-07-28** — see the row below. | ✅ **int→float PROMOTION DOORS — DONE, MERGED** (impl `f4d727474`+`df7b91dfb` · review `316d9b0b7`; x64-windows **1951→1971/0**, wasm **1860/0**, builds 0 warnings, no 101, fragments additions-only, `verify-warm-rebuild` PASS; arm64 SKIP — remote, UNVERIFIED). **ONE SHAPE, SEVEN DOORS:** *"ask `checkDeclaredType`, accept `ok`, forget to convert."* `checkDeclaredType` is a pure VERDICT function — it answers `ok` for int-into-float (a widening, not a narrowing) and cannot convert — so every caller that trusted the verdict and stored the value shipped the raw int. **`return <int>` from a `returns float` PANICKED** (`X64Backend.maxon:715`, `rax→xmm0`); **the struct-literal store, field assignment, self-field assignment, field DEFAULT, global init and the LOCAL `var` REBIND all SILENTLY STORED THE INT BIT PATTERN** — coordinator-reproduced: `Self{v: 3}` then `p.raw() == 3.0` is **FALSE**, the field reading ≈**1.5e-323** and `trunc` of it **0**. ⭐ **THE SEVENTH DOOR WAS FOUND BY THE IMPLEMENTER AND MISSED BY A FULL SURVEY** — the survey ruled locals out ("no type annotation exists"), but `binding.varType` holds the INFERRED type, so `var x = 0.0; x = 3` is a slot after all: it silently rebound `x` to int, and **inside a loop it fed an i64 and an f64 into one header phi**, the identical panic. **Assume the enumeration is short; the base rate says so.** **THE FIX IS ONE RULE STATED ONCE** — `TypeRules.widensIntToFloat`, the obligation beside the verdict that carries it — with exactly TWO conversions, one per representation (`Parser.coerceToDeclared`→the existing `promoteToFloat` for SSA values; `TypeRules.intToFloatBits` for parse-time constants) and **NO third primitive**. `requireFieldType` now RETURNS the coerced `ValueId` so three of the doors are covered *by construction* rather than by three edits that must agree. ⚠ **NO ORACLE EXISTS: the bootstrap CRASHES** (`E9001 … float value %0 has no FP register and no stack home`) on `Self{v: 3}`, so consistency with shv2's OWN working doors (call arguments, witness dispatch, ternary/`match` merges, math intrinsics) IS the specification — **user ruling: COERCE.** ⛔ **Comparisons deliberately still REJECT** (`emitCompare`'s own comment: promoting there "is exactly the bug P1.0d.4 shipped and then fixed"), and float→int NARROWING still E3009 at all seven doors — review probed both with float VARIABLES, not just literals, because the corpus only tests literals. **ALSO LANDED:** the ranged-float-alias FIELD path (`ProgramSignatures.fieldTypeOf`'s `maybeAlias` block had `isFunctionAlias` and `isGenericAlias` arms and **no float sibling**, so such a field resolved to `int` ⇒ false E3009), and `FieldDefault.literal` gained a TAG — ⭐⭐ because the widening decision had come to **REST ON A SEPARATE FALSE REJECTION** (a correctness argument propped up by a bug; coordinator ordered it removed, since the next person to fix that false reject would have silently broken the widening with nothing failing). ⭐ **THE SABOTAGE EARNED ITS KEEP TWICE:** it proved the tag read load-bearing (reds exactly `float-alias-struct-field-float-default`, while the zero-default sibling correctly stays green — **zero is the single fixed point of the int→f64 bit conversion**, so neither case alone would have caught it), and it exposed an OUTER GUARD THAT COULD NOT FAIL, deleted — *"a condition that cannot fail is worse than no condition, because the next reader trusts it."* ⭐⭐ **THE INDEPENDENT REVIEW FOUND THE SAME PATTERN ONE LEVEL DOWN** (`316d9b0b7`): `widensIntToFloat` is `tagIsIntegral(value) and tagIsFloat(declared)`, and **`tagIsIntegral` answers `true` for EVERY `named` alias — float ones included** — so the rule would hand a float-alias value straight to a `cvtsi2sd` over an f64 bit pattern. The header named that verdict as the safeguard; the real safeguard was an unstated parser invariant (every producer lifts via `resolveFloatAliasType`/`fieldTypeOf`). `coerceToDeclared` now **panics** on an unlifted value. Not reachable today — but a comment doing a check's job, for the third time in one session. It also proved the `allFilesFolded` gate move **strictly STRENGTHENS**: previously a bad field default threw *during the sweep* and **broke out of the member loop, leaving the layout SHORT**. **Review found NO eighth door** (30+ probes; `Array with float` is E2062, float union payloads E2015, annotated locals/globals are not syntax, compound assignment is not syntax) and left ONE four-arm copy deliberately — `requireScalarPayloadArg`, whose `DeclaredTypeMatch` is matched exhaustively so a new arm reds both sites at COMPILE time, and whose float path **panics** rather than coercing, so the rung that enables float union payloads gets a loud stop. **DELIVERABLE — the corpus gap IS the story:** `specs/implicit-type-conversion.md` states the rule for arguments, `return` **AND** assignment (*"the rule is not special to arguments"*), but **every widening test in it goes through call ARGUMENTS**, and its one return-direction test is the NARROWING case. **The doc covered three sites and the corpus exercised one — which is exactly why seven doors shipped uncaught**, alongside the fact that `specs-shv2/` contained **ZERO structs with a float field**. 20 authored cases close both gaps, each asserting a computed VALUE (the bug compiled fine and computed the wrong number). ⭐ Coordinator UNGATED the 5 new `float-type.md` cases: they had been given a 3-target gate in a file whose 8 pre-existing tests are ungated and DO run on arm64 — silent coverage narrowing is the rot pattern that produced 317 stale goldens, and a MISSING golden never fails a lane, so **arm64 goldens are simply owed at the next Mac sync.** **SCALE:** `allocsDelta` **0 at all six rungs** (the conversion reuses `MaxonUnaryOp.promoteToFloat`→`StdArithUnaryOpcode.siToFp`, already lowered by all three backends — x64 `cvtsi2sd`, wasm `emitSiToFp`, arm64 `scvtf` — so no new op and no target gate). `bytesDelta` is real and attributed **to the digit: EXACTLY 16.00 BYTES PER STRUCT FIELD at every rung** (a Maxon union box is sized to its widest arm, `literal` gained one field ⇒ 8 B/box; box COUNT unchanged, which is why allocations did not move; and TWO layouts are built per compile — the sweep's and the real parse's — the deltas splitting identically to the byte between `phase:signatures` and `phase:parse`). ⚠ **The commit message `df7b91dfb` states 8.00 and is HALVED** — it quoted the per-LAYOUT figure as the per-COMPILE one; the mechanism it describes is right, only the headline quotient is wrong, corrected here and in `docs/optimization-log.md`, which are the durable records. 0.0018% of compile bytes at rung 5. **⇒ TWO RESIDUALS FILED:** · ✅ **`try f() otherwise <int>` — WITHDRAWN 2026-07-28: THE FILING WAS WRONG, AND MEASURING THE ORACLE KILLED THE RUNG BEFORE IT COST ONE.** It had been filed as *"the ONE merge site that REJECTS where its two siblings promote (ternary → 35, `match` → 42) — consistency says it should coerce."* The premise is false. **The bootstrap REJECTS BOTH DIRECTIONS TOO**, coordinator-measured: a float result with `otherwise 3` ⇒ **E3005**, and an int result with `otherwise 3.0` ⇒ **E3059 with shv2's message character-for-character**. ⇒ **shv2 already MATCHES the oracle at this door.** The asymmetry is real but it is **the LANGUAGE's own** — the oracle promotes at a match/ternary merge (which `finalizeMatchMerge`'s own comment cites as its reason for promoting: *"The reference oracle promotes the INTEGER arms to float"*) and refuses at `otherwise` — and shv2 faithfully reproduces both. Making `otherwise` coerce would therefore be a **deliberate divergence from the oracle**, i.e. a language-design ruling, not a defect fix; **it is NOT scheduled, and the next rung to touch this must not "fix" it into a divergence by mistake.** ⚠ **ONE REAL FINDING SURVIVES → the bootstrap-oracle bug list:** the oracle's float-result message is **BOGUS** — `otherwise value 3 is outside the range of 'Real' (float(-1.7976931348623157E+308 to 1.7976931348623157E+308))`, and **3 is plainly inside that range**. The rejection may be intended; the reason given for it is not. shv2's own E3059 (*"otherwise type 'int' does not match expected type 'float'"*) states the actual rule correctly, so this is a divergence **in shv2's favour** and no change is owed here. · ⬜ **FUNCTION-VALUE WRONG RETURN TYPE — A SILENT WRONG ANSWER LIVE ON `main`:** `intish(x float) returns int` passed where `typealias FloatFn = function(x float) returns float` is declared computes **14 instead of 42**, no closures involved. This is the RETURN-type variant of the already-filed function-value float-widening rung (P1.7a's list); the two are one mechanism — `TypeClass.function`'s tag-level assignability — and should land together. |
| P1.0d.5a | top-level `let` | ✅ | 281→295 | — |
| P1.0d.5b | top-level `var` (globals) | ✅ | 295→317 | — |
| **P1.0r** | R1-core — allocator + refcounting runtime | ✅ | 355→357 | — |
| **P1.1a** | structs (waves 1–3) | ✅ | 357→399 | — |
| **P1.1b** | enums + `match` | ✅ | →1316 | **COMPLETE 2026-07-22.** #33 (neg float-backed enum) CLOSED · #34 (union wording — shv2 already says "union", now pinned; bootstrap says "enum" = maxon-sharp finding) CLOSED · #58 CLOSED. Bootstrap-side divergences (#34 wording, `-0.0`-fold) filed, don't hold shv2's phase |
| **P1.2** | `String` + ownership + drops (waves A–D) | ✅ | →611 | ✅ #43 (self-append UAF) CLOSED with #105. **✅ #40 CLOSED 2026-07-23** (main `7057bf8ad`+`b9f13dcac`) — the reachable UAF (P1.2 depth-model × P1.4a borrowed-aggregate PARAMS) is fixed by RETAIN-PROMOTION (user ruling: approach A). No open residuals |
| **P1.3** | owned payloads in enums/unions (slices 1–2) | ✅ | →641 | owned-local done; #46 (bootstrap fixpoint bypass) residual; cross-call param/return = P1.4 |
| **P1.4a** | cross-call ownership (waves 1–2) | ✅ | →670 | #48 (`.spec-tmp` race) + loop-exit-reconcile follow-up |
| **P1.4b** | errors — `throws`/`try`/`otherwise` | ✅ | 670→792 | **COMPLETE 2026-07-20** (`8b2bec781`). 4 waves — scalar core (`9b9455698`) · managed values (`8ca5225f2`) · managed return (`d16a45f0f`) · boxed-union fields (`807180533`) — + OPEN #54 Slice B2 (union call-arg type-identity, last piece). The once-disabled test `propagate-throw-through-local-struct` waited on P1.5 #64 (✅ CLOSED 2026-07-23 by retain — now enabled) |
| **P1.5** | closures + `async` + escape → `shared` | ✅ | A1 →813; **finish →1591** | **COMPLETE 2026-07-26** — the last three front-end residuals closed together, and the arm64 half is now RUN rather than asserted. **✅ #67 CLOSED**: `arm64EmitLeaFuncAddr` was never a panic stub (it files an `Arm64FuncAdrpReloc` and emits ADRP+ADD) — `StdToArm64Conversion.maxon` merely *said* it was, and that ONE FALSE COMMENT is why 105 per-target opt-in `<!-- targets: -->` markers survived across the closure families, `stack-arguments`, `ranged-typealias` and `ternary-expression`, in flat defiance of `selectedByTarget`'s own rule (*"NEVER a per-target opt-in"*). Markers 339→226, arm64-macos RUN count **1488→1593**, and 104 of the new goldens now pin `arm64.leaFuncAddr` — the encoder the comment called a stub is pinned by committed output. ⚠ 8 cases went back behind a marker WITH a measured reason (they pin a panic MESSAGE; `mrt_panic` is a Windows-only hand-assembled chunk, proved by the SAME 8 failing identically on arm64 AND wasm). **✅ #75 CLOSED**: its recorded `E2015` struct-copy-return blocker was STALE — that shape compiles and runs today; the real blocker was a lowering PANIC on capturing a ranged-alias binding (`388b24bc2`), and with that fixed the test re-enables unweakened. **✅ #79 RESOLVED, not deferred**: measured, not a defect — a plain function into a closure with a fn-typed param WORKS (42); only the capturing case is refused, and refused CLEANLY with an accurate E2015. An accepted feature gap with a guard. **#74 remains a DECISION** (deliberate divergence, pinned by nothing — pinning it is its own rung) and does not hold the phase open. Historic detail follows: residuals #75/#79 (front-end; ✅ **#73 escape rule DERIVED — CLOSED 2026-07-24 `b6cd4a8b3`**, and it was NOT cosmetic: an `if`-continuation phi and a loop-header phi are merges the enumeration never listed, so `var f = <capturing closure>` reassigned in an `if`/`while` compiled clean and SEGFAULTED (0xC0000005, coordinator-reproduced on `13a3fcefe`); #74 is a DECISION (a deliberate divergence) not a residual, though it is pinned by NOTHING — `specs/mutable-enums.md`'s 8 cases are un-ported and shv2 diverges on all of them, so pinning it is its own rung; ✅ #65 implicit-self CLOSED 2026-07-23 `55cafda11`; ✅ **#64 borrowed-container boxed-union field throw RETAINS CLOSED 2026-07-23 `eea72ec74`** — `retainThrownField` increfs the box instead of the E2015 reject, applying the #40 retain-promotion thesis to the throw error channel; enables `propagate-throw-through-local-struct`) (runtime ✅ ALL CLOSED: #87+#92 B1c `b9bc8c98b` · #88 drop-reclaim+cancel B2 `eb3c834b0` · #89/B2c await-block-arg-promise · ✅ **#93 runProcess-throws CLOSED 2026-07-23 Design A `7f3721b00`** — scalar `RunProcessError`, rides the B2b async throws ABI, NOT the plan's message-carrying Design B); **P1.5's async/runtime slice is COMPLETE** — only front-end #73/#74/#75/#79 (cross-rung-blocked or deliberate divergences) + #67 arm64 remain, so P1.5 stays ◑. ✅ **FUNCTION-VALUE DECLARED TYPING — DONE, MERGED** (impl `f8400bf93` · optimize `3951bd99c` · review `229931c60`; x64-windows **2023→2038/0**, x64-linux PASS, wasm **1926/0**, builds 0 warnings, no 101, fragments additions-only, `verify-warm-rebuild` PASS, scale **+1 allocation flat**; arm64 SKIP — remote, UNVERIFIED). **THE RED — coordinator-reproduced on `main`:** (A) `f(2)` — an int literal at a float parameter through an **INDIRECT** call — was **not widened**: compiled, ran, **wrong answer**, while the control `f(2.0)` was correct; (B) `intish as FloatFn` — a function returning `Integer` cast to `function(x Real) returns Real` — was **ACCEPTED**, and the integer 14 read back as an f64 bit pattern (≈**6.9e-323**). ⭐ **ROOT CAUSE WAS ONE FACT, AND THE CODE HAD ALREADY WRITTEN IT DOWN AS "a later rung":** `FunctionTypeAlias` stored only the RETURN type; the declared **parameter types were parsed and DISCARDED**, surviving only as a flat `display` string (`Project.maxon:858-859`: *"arg-type/arity checking against the PARAM types is a later rung, so they are parsed-and-discarded, not stored"*). They are now stored as **interner-free `(tag, name)` pairs** — deliberately NOT `MaxonType`, because a function alias travels by NAME through both fold paths and is remapped by neither, so interner ids would leak across two registries — resolved once into `FunctionShape`/`FunctionShapeMap`, and consulted through ONE rule (`declaredFunctionShapeOf` + `functionShapesAgree`). `appendCallIndirect` widens through the **EXISTING** `widenIntArgsToFloatParams`, no second widening. ⚠ Its header had claimed placement *"matches the callee thunk's declared parameter types BY CONSTRUCTION"* — true only when every argument is already a value of its own declared type, **FALSE for an untyped literal**; a comment doing a check's job, now corrected. ⛔ **The `__fnref_` thunk could NOT carry the fix** — `synthesizeFnRefThunk` derives its signature from the real FUNCTION, and there is exactly ONE thunk per function shared by every use site. ⭐⭐ **THE IMPLEMENTER MADE THE CALL THAT MATTERED: it compares RESOLVED shapes, not the nominal `display` string.** A lifted closure's return type is **INFERRED**, so `function(x Ratio) gives x * 3.0` has return `float` where the `ScaleFn` it is passed to spells `Ratio` — and **both reference compilers accept that program**. Nominal comparison would have false-rejected across the corpus. (The merge check stays nominal, matching the oracle; a door asks a different question.) **IT ALSO ADDED indirect-call ARITY + ARGUMENT-TYPE checking** beyond the brief, and was right to: the documented sentence names arg-type/arity checking, `floatWidenedArg`'s stated precondition (*"a non-float argument at a float parameter is ALWAYS an integer — SemanticCheck has already rejected every other type"*) is **FALSE** for an indirect call without it, and **the bootstrap already checks it** — so this closes a divergence rather than opening one; review confirmed shv2's new diagnostics match its own direct-call ones character-for-character. ⭐ **A DEFECT IT TRIPPED OVER AND FIXED:** `syncResolvedSignature` copied `func.maxonParamTypes` wholesale, but a **lifted closure carries one MORE** (the promoted `__env`) than its `funcSignatures` entry records names for ⇒ **2 types against 1 name**, the exact lockstep mismatch `checkArgTypes` panics about — held by every other function and silently broken by this one, invisible until something asked a signature how many parameters its *types* describe. Cured at `sourceParamTypesOf` (the NAME column is the authority); review verified the prefix-alignment claim holds structurally for every closure shape (`Parser.maxon:22689` — append, never insert, exactly one). ⭐⭐ **THE INDEPENDENT REVIEW FOUND A FALSE REJECT AND IT WAS A BLOCKER** (`229931c60`): `resolvedTypesMatch` compared a **NESTED** function type (a function type's function-typed parameter) **by NAME ID** while comparing every other type resolved — so two structurally identical aliases under different names were refused. **The bootstrap accepts the program (exit 42) and so does parent shv2 (42); head refused it** — coordinator-reproduced both at 42 after the fix. And the message was `expected 'fn(function) returns int', got 'fn(function) returns int'` — **a type failing to match itself**, because `maxonTypeName` rendered any function tag as the bare word `function`. Both halves fixed, with a **coinductive cycle guard** on the (declared, got) name pair, because shv2 accepts `typealias SelfFn = function(f SelfFn)` and two aliases may name each other. **Fixing it exposed duplication the rung had not caused:** the id→name and name→shape reads **each already existed TWICE** (private copies in `SemanticCheck` and `LowerMaxonToStd`), because the lookup took three registries as loose parameters plus the interner as a fourth carried fact — collapsed to one `FunctionTypeRegistry` carrier; the lowering now holds one field where it held four. Also deleted `hasDeclaredFunctionShape`, which had **no callers**. ⭐ **THE RUNG FIXES TWO MORE WRONG ANSWERS THAN ITS OWN SPECS CLAIM** (review-measured): an int literal at a float parameter through a **struct-field callee** (parent exits 0, head 6) and through a **capturing closure** — where **the parent SEGFAULTS**. **OPTIMIZER: no code change, and the measurements are why.** The scale corpus emits **zero function typealiases and zero function references at every rung**, so every new path runs exactly zero times there — a Δ0 measures the blind spot. It built `Testing/ladders/genfnval.sh` (aliases × sites × arity × indirect|direct) and measured **×1.96–×2.00 on every phase**; a per-site registry scan would read **×4.00**. `declaredFunctionShapeOf` is two hash gets (O(1)); `resolveFunctionAliasShapes` runs once per compile; the door list **does shrink** (`resetMergeTargets`) — the E3070-shaped *"pending list that never shrank"* is **not** present here. It confirmed +7 allocations flat against a control built **in this same worktree at a detached HEAD**, so the path artifact is structurally zero rather than subtracted. ⭐⭐ **AND IT REFUTED THE LESSON THE IMPLEMENTER HAD WRITTEN INTO THE TREND LOG.** The implementer's first shape cost **240×** the final one and it concluded *"on a per-statement parser path, what a guarded call costs is its ARGUMENTS, not its branch."* Measured three variants: passing the managed `String` noun to the guarded call and only **READING** it costs **0**; passing one the callee **CONSUMES** costs **+1,852**, to the digit. ⇒ **The rule is OWNERSHIP TRANSFER: a managed argument the guarded callee consumes forces the caller to hand over an owned copy, and that copy is emitted UNCONDITIONALLY, ahead of the guard.** That also explains why all three of the implementer's "refuted" hypotheses read identical — each left the consumed `String` exactly where it was. ⚠ And the logical hole the optimizer caught: the parent **already** passed that noun at every one of those sites, so *"one allocation per assignment"* could never have explained a delta **against the parent**. Corrected in all three tables of `docs/optimization-log.md`; the cure was making the noun a scalar enum. **PORTS NOTHING AND SAYS WHY** — survey-verified clean "none": no case in either corpus passes a bare int literal at a float parameter through an indirect call (the three float-param indirect tests at `first-class-functions.md:2277-2334` all pass already-float-typed values), and **no case anywhere asserts that a mismatched-signature function-to-function cast is rejected**; the one function-cast test that exists is a cross-KIND rejection caught by a different rule. **⇒ FOUR RESIDUALS FILED, all pre-existing, all reproduced on `9fa71cc79`:** · ⬜ **An INDIRECT call of arity ≥ 14 PANICS the register allocator** (`RegisterAllocator.maxon:1173`, *"no free register for value 14"*) — arity 13 compiles, and a **DIRECT** call at arity 16 compiles clean, so it is specific to the indirect path. Optimizer-measured on both binaries; this rung did **not** lower the threshold (review confirmed). · ⬜ **wasm MISCOMPILES a nested function type** — passing a function value into a parameter whose function type has a function-typed parameter emits a module failing validation (*"expected i64 but nothing on stack"*), narrowed to the nested TYPE alone with no indirect call and the same alias on both sides. **This is why the new accepted case is x64-gated** — a stated reason, not silent narrowing. · ⬜ **shv2 accepts a CIRCULAR function typealias** (`typealias SelfFn = function(f SelfFn)`) where the bootstrap refuses it (**E2012**) — not a wrong answer today (the review's cycle guard handles it), but a divergence. · ⬜ **The `ExitCode` return-type RESOLUTION divergence stands** (`closure-capture.md:305`) and the new check correctly does **not** catch it — both sides resolve to `exitCode` and genuinely agree; the fault is upstream in `Parser.functionAliasReturnType` rebuilding it as `named`/i64 against a target declared u32. **RATIFIED DIVERGENCE (coordinator):** shv2 accepts a function typealias as an `as`-cast target where the bootstrap refuses one outright (`E2003`); shv2 follows **v1** (any registered typealias name, kind legality decided later), which is what `castTargetTag`/`denotedNamedType` are built for — the same precedent as P1.7a slice 1's E3015. **This rung NARROWS that divergence**: what shv2 accepts is now signature-checked, where before it was accepted and miscompiled. |
| **P1.6** | generics + layout descriptors (A/B1/B2/C) | ✅ | →2262 | #94 (generic-instance union payload match-bind) → Future rungs. ⚠ **REOPENED ✅→◑ 2026-07-28 by the BARE-TYPE-ARGUMENT rung**, which found P1.6-A admits things that reach a COMPILER PANIC. Bare/float type arguments are now refused (E2061/E2062, below) — but **`X64Backend.maxon:715` is still reachable with a LEGAL type argument** (`Box with Integer` … `create(1.5)`, a float VALUE at a `T`-typed formal), so the mechanism is not done. ✅✅ **CLOSED AGAIN 2026-07-28 — and TWO OF THE THREE RESIDUALS WERE ALREADY FIXED WHEN THE RUNG OPENED** (main `fd5bee0ea`; x64-windows 2241→**2262/0**, x64-linux PASS, wasm PASS, arm64 SKIP — remote, UNVERIFIED). Only residual (1) reproduced. **(2) and (3) were closed by the declared-type-rule cluster that landed for P1.8** (`5587ed047` · `3c841c5eb` · `bbff47a86` · `9b985f5bb`) and this row never caught up: `Self{v: 3}` into a bare `float` field returns **3**, `return 3` from a `returns float` function returns **3**, and a ranged float alias works at a field literal, an int-literal field, a method param, a field assignment and a return — all measured, all green, against a row still describing them as a silent wrong answer and a panic. ⚠ **That is the SECOND time in two rungs a P1.6/P1.7a residual was measured false before its wave** (P1.7a's `2b-iv-C` had shipped six commits early). **REPRODUCE A RESIDUAL BEFORE PLANNING IT** — the plan is a claim about the past and nothing re-checks it |
| **P1.7** | `Array` (1/2/3a/**3b**/4 + COW A/B + **finish 1/2/3**) | ✅ | →1851 | ✅ **MANAGED opaque-`T` arrays — move-IN + element read/move-OUT + FIELD-reassign + slice/clone/append COMPLETE** (`Container with String`): move-in `e8b45b613` (reloc `destroyFunc@40` + ownership: call-site consume + TRANSITIVE-feed forwarding + MOVE-TRACKING double-move→E3102 / conditional→runtime drop); **✅ 3b-vi-a `18b64f22c`** = get/first/last BORROW + pop/remove OWNED move-out (drop-in-body via `__drop_type_param`; **returning** an owned opaque element out of the method is cleanly E2015-rejected — a review-found leak). x64-only. ✅ **3b-vii `42a8c7e69`** — the reachable `0xC0000005` on bare opaque-`T` field reassignment (`self.saved = x`) is CLOSED: a MANAGED opaque field reassignment is now a clean **E2015** (was a fault), trivial preserved byte-identical. Making it WORK is the follow-up (impl saved a front-end at `scratchpad/full-working-impl.diff`): **(B) ✅ FIXED `65a7090b6`** — the pre-existing NON-generic reachable LEAK (reassigning a managed-owning-STRUCT field `h.n = Named.create(...)` dropped the old value via trivial `__mm_decref` not `__destruct_<T>`, because `fieldTypeOf` tags a declared struct `named`): `decrefCalleeFor`'s `named` arm now routes a declared struct through the SAME `managedFieldDropCallee` the `structRef` arm uses (native 1391/0, no double-free). This UNBLOCKS (A). **(A) ✅ FIXED `60b4ba9bc`+`96c914adc`** (native 1393/0, wasm 1303/0, `-j1`==`-j12`, no leak, scale allocsΔ0) — a MANAGED opaque-`T` FIELD reassignment (`self.saved = next` shared body AND `b.saved = "gamma"` concrete instance) now WORKS: drop the old opaque value via the descriptor-gated `__drop_type_param`, transfer the new. TWO parts: (1) decoupled `__drop_type_param`'s install from `usesArray` onto its OWN `usesDropTypeParam` `RuntimeUsage` bit (`StringRuntime.recordCallUsage` sets it + a standalone `installDropTypeParamRuntime` gated on it) so an array-FREE generic struct's field-reassign drop links; (2) front-end `emitFieldWrite`→`emitOpaqueFieldReassign` (shared-body `structRef` box) / retyped concrete decref (concrete `genericInstance` box, via Finding B's struct path) + a `reassignsOpaqueField` descriptor-need signal + `markOpaqueFieldReassignFeed` enrolling the reassigned param OWNED; `SignatureIndex.managedOpaqueArrayElementOf` broadened to stamp `destroyFunc@40` for a bare managed opaque field too. Removed 3b-vii's E2015 reject; move-tracking gives double-move→E3102, self-alias `self.a=self.b`→clean E2015. Independent review single-sourced the `self.<field> =` reassignment-head recognizer (was written TWICE, drift→fault) into `fieldReassignHeadAt` (`96c914adc`) — the signature bug, caught again. **NAMING DEBT (accepted, follow-up):** `managedOpaqueArrayElementOf`/`genericInstanceHasManagedOpaqueArrayField` now span bare fields but keep their `...Array...` names (rename touches LowerMaxonToStd + MmRuntime — low-risk). **3b-vi-b-α ✅ CONCRETE managed slice/clone/append WORK** (`3487ec063`→rebased, native 1406/0 wasm 1313/0, `-j1`==`-j12`, no leak, fragments 0-modified): eager per-element DEEP-CLONE (shv2 is move-only — independent single-owner copies, NOT COW/incref; COW stays trivial-only, sidestepping the managed-view double-free `__arr_decref` would cause). A `__clone_<T>` family = the DUAL of `__destruct_<T>`: `__str_clone` (fused-record via `promoteToOwnedString`'s path), `synthesizeStruct/UnionCloner` per concrete type (recursive through nested managed struct/array/union fields, null+tag-guarded), `__arr_{slice,clone,append}_managed` looping `callIndirect(funcAddr(__clone_<E>))` per element into a FRESH OWNER buffer. Cross-target (funcAddr, NO reloc → unrestricted, wasm-verified). Front-end `requireArrayElementCopyable`/`arrayElementCloneValue`; DCE-root + install `noteClonerUsage`/`installStruct/UnionCloners` + `RuntimeUsage.{usesStrClone,usesManagedArrayCopy,neededCloners}`. Unlocks the 5 `*-source-freed` specs + 5 extras (incl. `merge-modules` struct→String+nested-Array recursion). ⭐ Optimizer (built a managed corpus — scale-test's is managed-free): cloner synth/install/classifier LINEAR; leak-governing clone-set==drop-set is single-sourced. ⭐ Review CLEAN (no double-free/leak/mis-clone; probed union-tag-guard, literal-rdata clone, append-preserves-src, wasm deep-clone). **✅ CLOSED 2026-07-25 — BOTH needs-closures were REAL quadratics too, and the "fix (if ever)" was worth doing now.**
This entry said `noteClonerUsage` was "LINEAR on realistic input… bends only on machine-generated 256-deep chains".
**Measured on a D-deep managed chain, both halves bend well before that:** cloner **×2.75 → ×3.82** and destructor
**×2.94 → ×3.83** per doubling at D=64→512; at D=512 that is **3,957,231 → 174,870** allocations (cloner) and
**1,909,550 → 85,541** (destructor). PLAN.md's own claim that `closeDestructorNeeds` carried the *identical*
quadratic is CONFIRMED independently. FIXED with the ONE shared worklist this entry prescribed
(`Compiler/Runtime/CascadeNeeds.maxon`); after: ×1.93 / ×1.96, and the review re-measured 3.30× fewer allocations at
D=256 with **byte-identical output**. ⚠ **The worklist is demand-driven BY CONTRACT, not for speed** — expanding a
node is what turns on that type's `RuntimeUsage` bits, so an eager sweep would declare runtime for types nothing
drops or clones. Review tested that sharply rather than by absence: a program that DOES drop and clone, plus three
managed types nothing constructs, is **byte-identical (same sha, same size)** to the same program with those
declarations deleted. **LEGIBILITY FOLLOW-UP (not a bug):** three clone/destroy DUALS (`noteClonerUsage`/`closeDestructorNeeds`, `managedFieldCloneStrategy`/`managedFieldDropCallee`, the synthesize twins) are parallel dispatch/scaffolding — accepted-as-dual (drift fails LOUD: link error / red test, never silent), single-source when a legibility rung touches MmRuntime/SignatureIndex. **3b-vi-b-β ✅ OPAQUE-`T` managed slice/clone/append WORK** (`ff72e0ea7`+`9b1424e26`+`6368efcb0`→rebased, native 1414/0 wasm 1315/0, `-j1`==`-j12`, no leak, fragments 0-modified) — the slot-for-slot DUAL of the opaque DROP path: `LAYOUT_OFFSET_COPY_FUNC=32` stamped via the SAME `funcAbs64InRdata` reloc as `destroyFunc@40` (`managedOpaqueArrayElementCloneCallee`); `__arr_{slice,clone,append}_opaque(arr,…,descriptor)` load `copyFunc@32` + branch ONCE (0→trivial COW/block-copy, ≠0→α's `__arr_*_managed`); DCE-root `rootManagedOpaqueArrayElementClones`; `RuntimeUsage.usesOpaqueArrayCopy`. x64-only (reloc; wasm/arm64 loudly refuse via existing `requireNoFuncAbs64Relocs`). `__clone_type_param` NOT built (would be dead — the array loop `callIndirect`s copyFunc directly). Optimizer: allocation-neutral (+8 bytes constant), over-stamp/over-root bounded-linear. ⭐ **Review found+FIXED a REACHABLE COMPILER PANIC** (`9b1424e26`) — the signature bug again: the stamp/root gate read the element's TOP-LEVEL clone strategy while the copy reject read the FULL-GRAPH `typeSupportsDeepClone`, and since stamp/root synthesize `__clone_<T>` UNCONDITIONALLY (even in a copy-FREE program), a DROP-ONLY `Container with Item` (Item owns a non-deep-cloneable `Array with (Array with String)`) rooted an unbuildable `__clone_Item` → panic; FIXED by single-sourcing both envelopes on the full-graph gate + an `opaque-drop-only-uncopyable-struct-element` regression spec. **⇒ P1.7 Array's MANAGED work is COMPLETE** (concrete + opaque; move-in/out, field-reassign, slice/clone/append). **✅ CLOSED 2026-07-25 — THE PER-INSTANTIATION GATES WERE A REAL QUADRATIC, NOT "linear-in-practice".** This entry
used to say the two gates were "linear-in-practice (0 corpus hits…)". **Measured at the trigger it named, that was
WRONG:** isolated by corpus−control on a ladder of N generic bases each with one opaque-field-reassign site, the gate
cost **×2.36 / ×2.85 / ×3.57 / ×3.59 per doubling, converging on ×4** (2.11 → 173.45 ms at N=64→1024). ⚠ It read as
zero because it is **TIME-only and allocation-free, so `scale-test` STRUCTURALLY CANNOT SEE IT** — "0 corpus hits"
was evidence of the instrument's blind spot, not of the cost. FIXED with the shared by-base-id index this entry
prescribed (`GenericInstancesByBaseMap`, written by the same `intern` that assigns the ids); after: ×1.94.
⭐ **The correctness evidence is stronger than the speed-up:** `phase:parse` allocations are IDENTICAL head-vs-base
at every N — the gate visits the same instances and merely stopped walking the rest — and the independent review
A/B'd both compilers over **32 probes to byte-identical executables AND byte-identical Target IR, zero diffs**. **LEGIBILITY FOLLOW-UP:** clone/destroy-dual single-sourcing (the twin gates + the stamp/root/callee duals — all accepted-as-dual, fail-loud). **PRE-EXISTING generic limitations surfaced (out of rung → a future generics slice):** a cross-instance `Self`-typed param fails E3005 (`absorb(other Self)` called `dest.absorb(src)`); `Self.create()` static call fails to parse in a method body. Borrow-on-get UAF resolved (`936b22ab3`). ⭐⭐ **TWO NEW FINDINGS from the 2026-07-25 cascade rung, BOTH pre-existing and BOTH filed as their own rungs (see the two ⬜ entries directly below this row's section).** ✅ **RESERVED-SPACE DISJOINTNESS — CLOSED 2026-07-26 (main `22195fba7`; x64 1778→**1789/0**, wasm 1597/0, `-j1`==`-j12` byte-identical, fragments **0-modified**, no 101).** The compiled name of a generic instance was claimed in the **USER's** type namespace, so a legal `type Array_Foo` was refused **E3006** naming an instantiation nowhere in the source — an `[Foo…]` literal interns `Array with Foo` silently, and the element type need not even match. **USER RULING: adopt the oracle's disjointness** — the bootstrap accepts all of these because it names instances `__Array_Foo`, inside the `__` space **E2051 already bars user declarations from**; shv2 shipped the guard and left the names outside it. A contested name is now re-minted behind `__`, **on contest only** (0 goldens moved — the property that made it payable on a host that cannot regenerate arm64/x64-linux), at the **sole producer** `mangleGenericInstance` and NOT at the check the plan named — `Parser.decrefCalleeFor` bakes `__destruct_<mangled>` into a drop call at PARSE time, proven by IR dump. **The E3006 PAIR check STAYS**: two instantiations that compile to one name have no declaration to move aside, and **neither reference compiler diagnoses that** (v1 first-write-wins silently), so shv2 is ahead and stays. Also fixed: an `interface` named like a mangling was wrongly E3006'd (an interface mints no per-type symbol). ⭐ Review corrected the safety argument itself — reserved-ness is not what makes minting safe (the re-probe buys that for any prefix); `__` uniquely buys disjointness from every OTHER INSTANCE's name, the half nothing probes, since a compiled name always starts with its base — an invariant now written down and pinned. ⭐ Coverage was prose that **disagreed with itself** on the claimant roster; both halves are now cases, FALSIFIED by deleting each arm (dropping `containsEnum` makes a `union` contest `panic: two functions are named '__destruct_Box_String'`). ⚠ Corrected pre-merge: the `Array_T`-owning-a-String case does **not** reach `baseLayoutOf`'s panic — that cure (`5356807c8`) predates the branch. Optimizer: **at most 2 probes on anything that compiles**, both `Map.contains`; allocations **DOWN exactly 1 per interned instance**. ⇒ **RESIDUAL (sanctioned, now COST-ONLY):** interning is still indiscriminate — every declared struct/enum on any `[identifier`; under disjointness it contests nothing, and narrowing it is a token-level soundness problem of its own. **⛔ The review also found a REACHABLE SILENT MISCOMPILE in the WITNESS namespace — the identical non-injective `_` join, pre-existing, outside this rung's footprint ⇒ its own rung, and the NEXT one (see "Future rungs").**
✅✅ **P1.7 IS COMPLETE — CLOSED 2026-07-27 by the three "finish P1.7" slices (main `d51ddd665` · `679e023a9` · `24224f71c`; x64 1793→**1851/0**, x64-linux **1745/0**, wasm **1654/0**, builds 0 warnings, no 101).** The rung was held open by a residual list that had **outlived its own causes**, and the first job was to find out which entries were still true.
**⭐ SLICE 1 — THE MARKERS THAT OUTLIVED THEIR CAUSE (`d51ddd665`).** A `disabled-test:` marker naming a rung is a claim about the past that nothing re-checks. Sweeping every case blaming bare `P1.7` (⚠ `P1\.7(?![ab0-9])` — `P1.7a`/`P1.7b` are other rungs) and flipping all 102 in a worktree: **35 passed immediately**, 67 were re-attributed to their *measured* blocker (P1.8 `for-in` 21 · the `__ManagedMemory` `.managed` door 12 · E3106 resize-of-managed 6 · nested-union payload 4 · move-only sharing 3 · E3070 3 · module-level 3). **Zero bare-`P1.7` attributions remain in the corpus.** The sweep also exposed **TWO REACHABLE COMPILER PANICS** — `arr.<field>` on the synthesized `Array` (`Parser.maxon:19613`) and `Map with (K,V)` reaching `baseLayoutOf` — which were **ONE fact**: a generic base lacks a `StructLayout` for exactly two reasons (SYNTHESIZED `Array`/`Set`; UNDECLARED, E2055 already recorded), and **four field walks each knew only the first**. Cured by `ProgramSignatures.genericInstanceHasBaseLayout`, which *is* `baseLayoutOf`'s precondition. ⭐⭐ **The review then found the de-duplication INCOMPLETE, and the miss was a SILENT MISCOMPILE live on `main`**: `structLayoutOfBinding` asked `structOf` ("is a type of this NAME declared?") not the instance question, so with a user `type Array uses T` present, `a.value` on an `Array with Integer` **returned the element buffer pointer** (coordinator-reproduced: prints `7077984`) and writing it faulted `0xC0000005` at teardown. ⭐ Its **sabotage proved the gate load-bearing in BOTH directions** — forced `true` → 343 failures, forced `false` → **75 failures, every one exit 101**: a skipped field walk is a LEAK, worse than the crash it prevents. ⚠ **And it caught 3 of the 35 flips passing WITHOUT touching the mechanism they name** (`param-mutating-method-is-impure` passes only because shv2 has **no purity classification at all** — `_ = pureCall()` compiles where the oracle raises E3064); re-disabled/annotated. **A case that passes is not automatically a case you may enable.**
**⭐ SLICE 2 — `element_size` UNIFICATION, the last correctness divergence P1.7 owned (`679e023a9`).** RED, oracle-verified: `Bytes.create()`+`push` then `.append(b"CD")` gave **oracle 4 / shv2 exit 72**, both directions. Root cause was **ONE FACT WRITTEN TWICE** — `arrayElementSize` returned **8** unconditionally for any ranged `named` alias while `lowerByteStringLiteral` hardcoded **1** — and `requireArrayAppendArg` **structurally could not see it**, because it re-derives *both* sides through the same wrong function. Fixed to the ladder **both references already draw** (`ComputeOptimalIntType`, `MlirType.cs:472`; `computeOptimalIntType`, v1 `Parser.maxon:21227`): narrow to the smallest of **1/2/4/8** holding the declared range. ⭐ **A compile-time STAMP fix, no runtime work** — every `__arr_*` helper already reads `element_size@24` at run time and `emitLoadElem`/`emitStoreElem` are byte loops. **Three decisions the plan did not anticipate, each escalated not taken silently:** (1) **UNSIGNED ladder only** — a negative lower bound keeps 8, because the shared byte loop **zero-extends** and the record carries no signedness (naive narrowing made `int(-5 to 5)` read `-5` back as **251**); signed narrow storage is its own rung; (2) it is a **fold over every declaration of the name**, not a probe — an instance interns on the NAME, so a probe is last-merge-wins and truncates (`Utf16UnitCount` declared `int(1 to 2)` and `int(0 to u64.max)` in two files); (3) **not** in `typeLogicalByteSize`, which desynced `sizeof`. ⭐⭐ **The review found the slice had WIDENED an existing gap**: `arrayElementSize` is documented "THE SOLE PRODUCER of `element_size@24`" and is not — with `typealias Byte = int(0 to 1000)`, a `b"…"` record strides 1 while `.create()` strides 2, **one static type, two behaviours, no diagnostic** (`lit.get(2)`=44 vs `made.get(2)`=300), and `.append` is the *only* op that compares strides. Cleanly REJECTED (E2015) at **two** throw sites — the top-level-global path was measured slipping the first gate. ⭐ **`array-clone-element-size.md` was ASPIRATIONAL and is now a real gate**: its assertions could not fail (read and write always agree on whatever stride was stamped), so it now uses the `b"…"` literal as an **independent producer** — RED (exit 72) on the parent. Optimizer: **allocation-NEGATIVE from rung 1**, A/B'd against a fresh parent build with a byte-identical bootstrap in a worktree of *exactly equal path length* so the `phase:load` artifact is zero rather than argued away.
**⭐ SLICE 3 — THREE FRONT-END ORACLE DIVERGENCES (`24224f71c`).** (1) a **multi-line array literal** was `E2004 Expected expression but got 'newline'` where the oracle returns 20 — newlines are now layout at the three boundaries `parseStructLiteralFields` already allows, and a **twin one layer down** was found: `initializerEndIndex` ended a *top-level* initializer at the first newline, so `var xs = [10,\n20]` handed the const evaluator a truncated range (now bracket-depth counted, with the `]` decrement guarded because `BracketDepth` is unsigned and would wrap). (2) **generic type ARGUMENTS were never validated** — `Array with UnknownType` **compiled AND RAN** (returned 9) where the oracle rejects; now **E3011** via `denotedNamedType`, the same cascade the `as`-cast sites ask, so an argument and a cast target cannot disagree. ⚠ **The brief said "E2003"; that is the ORACLE's numbering** — in shv2's registry E2003 is `ParserExpectedType` and is *not emitted*, so using it would give one number two meanings. (3) a **ONE-element literal** `[10 as Index]` (`int(0 to u64.max)`) was refused as *"mixed element types"* — a **doubly-false message** — because the int fast path chose the instance from the first **TOKEN** and then checked the parsed **element** against it; the "mixed" verdict is now unreachable from a one-element literal **by construction**. ⭐ Optimizer made the slice allocation-NEGATIVE (**+2,834 → −1,420** at rung 5) by gating both generic checks on a new `allFilesFolded` — during the tolerant sweep they built positioned diagnostics the discarded artifact threw away; it also found `checkGenericInstance` had been doing this **since P1.6-A** (1,549 allocs/rung 5). ⭐ Review **sabotaged that gate** (forced `false` → 4 failures, so neither check is dead), proved the managed-element move-in decision **identical multi-line and one-line** (E3102 fires at both; no 101), and single-sourced `skipNewlines`/`indexPastNewlines` — **two copies of "which tokens are layout"**, whose drift would not be a compile error. **Zero fragment movement across a 20-call-site parser change.** |
| **P1.7a** | interfaces + witness tables (⬆ from P2.1) | ✅ | →2185 | **Slice 1 ✅ FRONT-END: interface decls + declared conformance + `"{userStruct}"` interp** (`08df12007`+`f3ee5308b`→rebased, native 1449/0 wasm 1336/0, `-j1`==`-j12`, no leak, fragments 0-modified). NO witnesses/existentials — concrete dispatch is a DIRECT `<Type>.toString()` call (exactly the monomorphizing oracle; witnesses only needed for UNKNOWN concrete = constrained type-params = Slice 2). Net-new `IR/Maxon/IrInterface.maxon` + `ConformanceCheck.maxon`: parse `interface … end` + `type X implements I`; Map-indexed ConformanceRegistry; conformance CHECK (**E3016** missing/wrong-sig incl. `extends`-inherited — matches the oracle byte-for-byte; **E3015** unknown-interface — a DELIBERATE divergence toward v1, the bootstrap silently accepts); builtin interfaces synthesized on lookup from `stdlib/Interfaces.maxon`; `"{userStruct}"`→direct `toString` (+1-owned classify, leak-free). Interface-typed params/fields/union-payloads (EXISTENTIALS, plan-settled OUT of Phase 1) cleanly REJECTED (E3011). ⭐⭐ **Review found+FIXED 2 rung-owned defects by PROBING** (`f3ee5308b`): (1) a COMPILER PANIC — a conforming type with a duplicate-named method (`toString()`+`toString(format)`, no overloading) crashed (the signature bug: param TYPES read from `module.functions`, NAMES from `funcSignatures`, disagree on a duplicate) → gate `checkConformance` on `projectHasErrors` (the disagreement only arises from an already-recorded E3006); (2) a FALSE-REJECT — an interface/impl reaching a shared type through two distinct ranged typealiases wrongly E3016'd, contradicting shv2's own range-blind identity (`takesA(makeB())` compiles) → `canonicalTypeName` collapses aliases via the SAME resolution `resolveNamedType` uses (no second path), message unchanged, `int`/`float` still rejects. + 2 regression specs. Optimizer: LINEAR (conformance-check ×2.0/doubling on a built corpus; the shared-empty COW fix keeps the conformance-free scale corpus flat +12). Ports `interfaces.md` + `interface-conformance.md` + the 3 staged Stringable-interp cases (+20/+2). **Slice 2a ✅ WITNESS MACHINERY (x64 core)** (`1fad057df`+`e6089e73a`+`ec9114930`→rebased, native 1458/0 wasm 1340/0, `-j1`==`-j12`, no leak): a constrained type param dispatches via a runtime witness — witness rdata blob (`24+8N`, decl-order slots, `funcAbs64` relocs, DCE-rooted); the FUSED `witnessCall` StdOp (load fn-ptr @(24+8·idx) + indirectCall in ONE → x64 `callMem [reg+off]`, fn-ptr NEVER virtualized); a `witnessDispatch` MaxonOp; dictionary-passing (witness param trailing after `__layout`; concrete site materializes `rdataAddr(__witness_<T>.<I>)`, generic→generic forwards the caller's slot); **E3017** at a non-conforming instantiation. x64-only (reloc; arm64/wasm loudly refuse). `where T is Digest`…`self.item.digest()` → 97. USER interface + USER struct only (primitives + stdlib-alias returns = Slice 2b). ⭐ **User RULING (keep the BORROW model): a temporary struct type-arg is MATERIALIZED as a scope-local** (`e6089e73a` — gave the temp a lifetime, `create(p);p.getX()` unchanged; fixed 2 latently-dangling sizeof goldens). ⭐⭐ **Review: witness mechanism VERIFIED CORRECT** (slot-index `24+8·idx`=`callMem [rbx+32]` for method#1, two-constraint witness-param NOT-swapped, two concrete instances each own their table, DCE-root, no double-free) + FIXED a signature-disease dup (`ec9114930` — `lowerCallIndirect`/`lowerWitnessCall` held a verbatim indirect-result-capture copy → shared `captureIndirectResult`). **Optimizer LINEAR** (witness corpus ×2.0/doubling; `materializeEscapingBorrowTemp` O(K²)/call K≤6-capped → Workstream-O NOTE). **⭐⭐ RESIDUAL → E3070 (new rung below):** a CROSS-SCOPE borrow of a struct type-arg into a generic field is a REACHABLE UAF that Slice 2a newly makes OBSERVABLE (witness dispatch is the FIRST reader of a stored borrowed generic element; before this a `T`-method / field-chain read was E2015) — `var b=PointBox.create(p0); if…then {let pInner=Point.create(3,y:4); b=PointBox.create(pInner)} end; b.itemDigest()` reads freed `pInner` (CONFIRMED range-panic on 0x3F). Same-scope + temp-materialized are SOUND; the cross-scope (inner-source / named-inner / loop-carried) is genuine borrow-liveness (root: `typeArgIsOwned`'s borrow-not-consume, `SignatureIndex.maxon`). **USER RULED (2026-07-23): fix it by RETAIN — ✅ CLOSED by the retain-escaping rung below** (co-own the escaping trivial struct via `__mm_retain`, generalizing #40/#64; the cross-scope program WORKS → 97, matching the oracle; SUPERSEDES the temp-materialization). NOT a reject-based NLL pass — the true `E3070` mutate-while-borrowed check stays a separate future rung. **ABI-slot signature-disease NOTE:** the `[user][env][layout][witness]` param order's numeric OFFSET is written twice (the COUNT is single-sourced via `witnessParamCountForCallee`; the offset in the parser seeding + `bindImplicitParams`) — correct today, watch on any order change. **◑ Slice 2b SUB-SLICED. ✅ Slice 2b-i — INTEGER `Hashable`+`Equatable` + `HashValue` (x64) DONE, MERGED `fc6e2201c`** (merged-tree native **1487/0**, wasm **1356/0**, `-j1`==`-j12` byte-identical, scale allocsΔ0/bytesΔ0 bit-identical across the debugger-P3b bootstrap rebase, exit 0, no leak): `int` conforms to `Hashable`/`Equatable` INTRINSICALLY — no user `implements`. `int.hash`/`int.equals` are synthesized as Std-tier runtime helpers (net-new `Runtime/PrimitiveConformanceRuntime.maxon`, the `installStringRuntime` pattern) installed post-pipeline, keyed on a witness-table funcAbs64 reloc naming them (table-built ⟺ impl-installed; short-circuits allocation-free otherwise, hence scale Δ0). `int.hash()`=`self and 0xFFFFFFFF` (low-32 mask; `HashValue`=`int(0 to u32.max)` resolved inline in `resolveNamedType` like `ExitCode`, killing E3011); `int.equals`=`==`. A ranged int alias (`Integer`) conforms via the SINGLE-SOURCED `ProgramSignatures.conformerTypeArgName` (`Integer`→`int`, shared by the E3017 check + witness materialization) + `isIntrinsicPrimitiveConformance`/`typeConformsTo` (intrinsic OR user registry — ONE predicate). ⭐ **Piece 4: `==`/`!=` on an ENCLOSING Equatable-constrained type-param → Equatable witness dispatch** (`emitCompare`→`emitTypeParamEquality`, mirroring `.equals()`), which ALSO FIXED a reachable wrong-answer (a constrained-T STRUCT `==` was a pointer-compare of two distinct heap allocs → exit 1); an UNCONSTRAINED type-param `==` is **E3005** (`eq-requires-equatable`); a concrete `int`/scalar `==` is unchanged (scalar cmp); a `typeParameter`-tagged CONCRETE generic-method result in a non-generic caller (`let v=box.get(); v==42`) stays a scalar compare (the `isEnclosingTypeParam` gate — a review-caught regression fixed pre-merge). ⭐⭐ **The independent review found a REACHABLE SEGFAULT the Piece-4 construct reaches** → FIXED before merge (`fc6e2201c`): `self.item == 42` / `self.item.equals(42)` with a STRUCT `T` marshalled the concrete literal into the callee's `Self`-typed `other` pointer → `0xC0000005` (the method-call form was a pre-existing slice-2a twin, closed uniformly). A SHARED `requireGenuineSelfArgs` at the ONE witness-dispatch arg-validation point (both the operator AND method-call paths) rejects a `Self`-typed formal supplied by a non-type-param actual with a clean **E3005** — semantically correct (a literal is not provably `Self` in a `where T is Equatable` body); verified not to false-reject any genuine-`T` form (2 RED→GREEN error specs). Optimizer: no superlinearity, already optimal. Review: duplication single-sourced (`typeConformsTo`/`conformerTypeArgName`/`findWitnessDispatch` each ONE). Un-disabled `interfaces.md` basic-interface/multiple-interfaces (HashValue); ports `equatable.md` (7 user cases + primitive-via-constraint) + `primitive-conformance.md` (witness-form hash/equals/`==`, x64-gated). ✅ **Slice 2b-ii — `Comparable` + `Ordering` (int, witness form, x64) DONE, MERGED `79696a8ab`** (merged-tree native **1572/0**, wasm **1380/0**, `-j1`==`-j12` byte-identical, fragments **20 added / 0 modified**, cold builds 0 warnings, exit 0, no leak; scale **+68 allocs / +2,376 B FLAT and bit-identical at every one of the 6 rungs**). ⚠ **THE PREMISE THIS ROW USED TO STATE WAS MEASURED FALSE**: enum-case value exprs and enum-case `match` were NOT "both unimplemented" — a *user* enum does both today (`Direction.north` + `match d … north gives 7` runs). **The sole blocker was that `Ordering` never entered the enum registries**, which three independent readers consult: `containsEnum`/`enumOf` (qualified case value → was E2010 at `Parser.maxon:5596`, the `Type.min`/`max` path), `enumMatchFor` via `parseMatchCommon` (enum `match` → was E2015, scrutinee fell to a SCALAR match), and `resolveNamedType`'s enum arm (the type itself → was E3011). ⇒ **ONE seeded registration cured all three with ZERO per-reader by-name arms** — `Project.createEnumRegistry()` is THE one constructor of an `EnumLayoutMap`, used at both registries' single creation points (⚠ **there are TWO maps**, `Project.enumTypes` the authority and `ProgramSignatures.enumTypes` the parser's index — the plan's "one registry" framing was the coordinator's error, corrected by the implementer without losing the thesis). `Ordering` is transcribed VERBATIM from `stdlib/Interfaces.maxon:34-38` through the SAME `EnumLayout`/`addCase` path a user enum takes; **tags are DERIVED** (`addBuiltinOrderingCase` uses `layout.caseCount()` as the next tag; `orderingTagOf` reads them back off the layout and panics on an unknown case) — a **deliberate divergence from the bootstrap**, which spells 0/1/2 as C# literals at four consumer sites with nothing tying them to the declaration (`2-Parser.cs:22293-22302`, a latent coupling the survey flagged). `int × Comparable` added to `isIntrinsicBuiltinConformance` (per-conformer now — `String` must NOT get it, `stdlib/String.maxon:34` omits `Comparable` and both references agree); `buildIntCompare` joins `buildIntHash`/`buildIntEquals` under the identical witness-reloc gate. Witness dispatch of `compare` needed **NO new ABI** (slot 0 ⇒ `callMem [reg+24]`, plain GPR result — an enum resolves to `integer`, so it is register-identical to `int.hash`'s `HashValue`); verified, not rebuilt. **Ordering OPERATORS on a constrained `T`** route to the `Comparable` witness + an ordinal check (`emitTypeParamEquality` generalized to the shared `emitTypeParamComparison`; `<`⇒`lessThan`, `>`⇒`greaterThan`, `<=`⇒not-`greaterThan`, `>=`⇒not-`lessThan`, the bootstrap's correct 3-valued mapping); an UNCONSTRAINED `T` ordering is a clean **E3005** via the generalized `operatorRequiresConformance` (ONE variant serving both families, Equatable's message byte-identical). **This piece was IN-rung deliberately**: `TypeRules.maxon:162` lets a `typeParameter` agree with anything, so `a < b` fell to a raw `MaxonOp.compare` — a POINTER compare for a struct `T` — unreachable only because nothing could conform to `Comparable`, so this rung made it reachable and therefore owned it. **DELIBERATE DIVERGENCE (coordinator-ratified):** a declaration minting a NOMINAL identity under a compiler-owned type name (`Ordering`/`HashValue`/`ExitCode`) is refused **E2015** (`Parser.requireTypeNameNotCompilerOwned` + the ONE predicate `TypeResolution.isCompilerOwnedTypeName`); the bootstrap permits it only because its `Ordering` comes from a NAMESPACED stdlib module, which shv2 structurally lacks — the alternatives were a silent half-merge (a fault) or silently ignoring the declaration. **A RANGED `typealias` over an owned name stays LEGAL** (it erases to the same primitive, so it cannot disagree) — the first version over-rejected and the corpus caught it, 27 cases of `typealias ExitCode = int(0 to 125)` in `generic-type-nested-array-typealias.md`; coordinator swept BOTH corpora and confirmed **every** occurrence is a ranged alias, so the reject is corpus-unobservable. `interface <ownedName>` is admitted (an interface name mints no type identity — probed, runs correctly). **⭐⭐ THIS RUNG FIXED A PRE-EXISTING REACHABLE SEGFAULT ON `main` THAT NEEDED NO `Comparable`**: a user `type HashValue` + `Box uses T where T is Hashable` @ `Integer` made intrinsic `int.hash`'s INTEGER result typed `structRef` (the parser's `resolveNamedStruct` knows nothing of the names `resolveNamedType` claims first), adopted as an owned struct box and dropped as one → **`0xC0000005`, coordinator-reproduced on the pre-rung base**. **⭐⭐ AND THE INDEPENDENT REVIEW FOUND TWO MORE, both FIXED pre-merge (`79696a8ab`):** (1) the **CROSS-PARAMETER `Self` arg** — see the narrowed 2b-i note below — a `0xC0000005` **live on `main`** via `Equatable` alone, plus a silent wrong answer (exit 11, int compared against a heap pointer); (2) a **non-protocol interface HIJACKING the operator** — `findWitnessDispatch` matches by method NAME (correctly, it is shared with `.method()`), so a user `interface Weird { compare(other Self) returns Integer }` made `a < b` mean *"less than whenever the author said EQUAL"* (exit 1), and the `equals` twin made a comparison evaluate to **7**; cured by FETCHING the protocol (`requireBuiltinInterface` supplies both the method name searched and the result type the verdict reads) instead of describing it in three parallel spellings, which also deleted two ternaries at the call site. Review also PROVED `Project.maxon:788`'s comment FALSE — a user `enum Ordering` CAN reach `ProgramSignatures.enumTypes` through the tolerant declaration sweep (`foldFile`→`recordEnum`), which runs BEFORE the real parse refuses it (proved by making the write panic) — cured by making the seed non-displaceable at the ONE write door `upsertDeclaredEnum`, removing two copies of the upsert; **a comment doing a check's job, caught again**. Duplication single-sourced: `SetRuntime.maxon:127-133`'s hand-copied `Hashable`/`Equatable` spellings DELETED (`HashableInterfaceName` exported — the rung had exported `Equatable`, half-invalidating the comment's stated reason while leaving the copies). **Optimizer: NO code change — nothing superlinear** (the +68/+2,376 is a flat per-compile constant; files 19→143 and `type` decls 12→260 across the ladder while the delta did not move by one allocation, which refutes both a per-file and a per-declaration term; it built its OWN 64→2,048-operator `where T is Comparable` ladder because **the scale corpus generates no generics/interfaces/enums at all**, and measured that path LINEAR at ×1.984 with `builtinOrderingEnum()` at exactly 10 allocs/call, O(1) per operator; it also REFUSED the coordinator's registry-fetch suggestion with a measurement — an eager per-`Parser` layout would convert the flat +68 into +1,430 at 143 files). Ports NOTHING from `/specs` and says so: all 13 `specs/primitive-comparable.md` cases need **direct primitive method dispatch** on a concrete value (a separate mechanism, shared with `i.hash()`), `array-sort.md`'s 39 need `Array.sort` + comparator closures, `conditional-extensions.md` needs `extension` (P2.4), the 3 `match-range.character` cases need `Character`. Deliverable is authored `specs-shv2/comparable.md` — **15 cases, 0 disabled**, 7 gated `<!-- targets: x64-windows, x64-linux -->` — the route `primitive-conformance.md`/`string-conformance.md` took, plus 5 review-added regression cases (2 landing in `primitive-conformance.md`, the Equatable twins). ✅ **DIRECT PRIMITIVE METHOD DISPATCH — DONE, MERGED `69cac9796`** (impl `55b15b084`+`4a70f62df` · optimize `27de41e16` · review `69cac9796`; merged-tree native **1596/0**, wasm **1405/0**, `-j1`==`-j12` byte-identical, fragments additions-only, builds 0 warnings, no 101; **rebased THREE times** over a very active upstream, only ever conflicting in `docs/optimization-log.md` where both sides append a row). **`i.compare(j)` / `i.hash()` / `i.equals(j)` on a CONCRETE `int`** (and ranged aliases) now dispatch **directly** to the bodies 2b-i/2b-ii already synthesized — previously reachable *only* through a witness table. ⭐ **DELIVERABLE IS REAL CORPUS, NOT AUTHORED CASES** — the first P1.7a slice for which that is true: ports `specs/primitive-comparable.md` and `specs/primitive-hashable.md` byte-identical and enables **8 canonical cases** (int compare ×3, `byte.compare.less` via the ranged alias, `int.hash`, `int.equals` ×2, plus `specs-shv2/type-methods.md:245` `method-call-on-call-result` flipped after 
 waiting on "NO rung of the ladder"). **ONE BODY, TWO DISPATCH PATHS** (v1's architecture: its witness slot points at the same `int.compare` symbol) — no second body synthesized. ⚠⚠ **THE PLAN WAS WRONG AND THE IMPLEMENTER STOPPED TO SAY SO** (the coordinator ruled, then approved a one-line widening): the brief asserted "the gap is entirely at install time", but **two gates fire EARLIER** — `SemanticCheck.validateCall` and `LowerMaxonToStd.lowerCall` both ask `MmRuntime.isCompilerInternalCallee`, which tests for a `__` prefix, and `BuiltinConformanceRuntime.maxon:9-10` says in its own words that these impls are *"NOT `__`-prefixed, so they carry a real `.text` symbol the witness-table reloc can name."* **So `compiler-owned ⟺ __-prefixed` was ALREADY FALSE for those five symbols** — nothing had ever called one BY NAME to expose it. Widened for the **`int.` qualifier only**, which is sound with no caveats because **`int` is a KEYWORD**: no user symbol can wear that qualifier (probed: `int.compare(1,2)`→E2004, `type int`→E2010). `String.*` deliberately stays OUT — a user `type String` *can* own `String.hash` (see the reachable-segfault entry below). ⚠ **That predicate has ELEVEN real call sites and the coordinator's grep found only seven** — the implementer audited all 11 independently and found the four missed (`LowerMaxonToStd:1087/1423/1755`, `Targets/Shared/StdLoweringShared:248`). Verdicts: #10 `validateCall` is load-bearing but its arity/type checks are **RE-HOMED in the parser, not lost** (probed: `a.compare()`→**E3036** counted in USER args so the receiver slot is never named; `i.equals("x")`→**E3005**); #5 `requireThrowingTryTarget` is an **improvement** (`try i.compare(j)`→clean E3055); the rest unreachable. **#11 `StdLoweringShared:248` genuinely DEGRADES** — a callee with no Std function falls back to GPR instead of panicking — **and the review TESTED it by breaking `REFERENCED ⇒ INSTALLED`**: the failure stays **loud** one phase later (`resolveCallFixups: call to unknown function 'int.hash'` on x64, `callResultValType` on wasm), never a wrong answer, and the GPR class is correct anyway (all three impls are i64-in/i64-out). Install gate generalized *table-built ⟺ impl-installed* → **NAMED-but-not-DEFINED ⟺ installed**, ONE predicate. ⭐ **THE IMPLEMENTER FOUND A SILENT WRONG ANSWER IN ITS OWN CHANGE, BY PROBING, WHILE 1586 TESTS WERE GREEN**: installing the synthesized djb2 body OVER a user's `String.hash` returned **5381 — djb2's SEED** — instead of 7; cured by installing only symbols the module does not already define, which is why the invariant says *not DEFINED* rather than merely *referenced*. ⭐ **v1's char-literal trap does NOT reproduce, and that is a measurement**: v1's silent wrong answer needed a materialized char POINTER to mismatch against a raw codepoint; shv2 lowers a char literal to a plain `integer` on BOTH sides, so `'A'.compare('B')` routes through the new arm and exits **65** (`lessThan`). ⭐⭐ **OPTIMIZER: `scale-test` REFUTED the implementer's own attribution.** The cost fit `30 + 32·2ⁿ` allocations exactly — and the implementer read the doubling half as "the feature's own per-call-site gate, structurally right". **The optimizer tested that against a KNOB instead of accepting the arithmetic**: the whole doubling term sat in `phase:parse`, and **64 extra struct-method call sites → +64 allocations while 64 extra array-method / String-method / field-read / ranged-alias / plain-call sites → exactly 0 each.** Cause: `BuiltinConformerLookup` was a **payload-carrying union**, which HEAP-BOXES — so classifying a receiver allocated 16 bytes **even to answer "no conformer at all"**, taxing every struct method call in every program for a feature those programs never use, in a corpus containing zero direct dispatches. Cured with a payload-free enum + an **exhaustive** `builtinConformerName` match (a conformer added to the tag map now fails to COMPILE until it is named — stronger than what the union shape carried), net −1 function ⇒ **every phase Δ0 at every rung**; the rung's whole footprint is a flat **+30 allocs / +1,410 B per compile**, identical at rung 0 and rung 5. Install audited O(ops + 5·F) **once** per compile; `isCompilerInternalCallee` measured allocation-free (+0/+0 in the two phases that ask it of every callee). ⭐⭐ **REVIEW: THE CLASSIFIER DID NOT ADMIT ITS OWN RESULT TYPE.** `HashValue` — what `hash()` RETURNS — was not a conformer, so `i.hash().hash()` was **E2015**, while **two headers in the rung's own new code asserted it chained** (*"…makes `Integer`, `Byte`, `HashValue` and a bare `int` one conformer rather than four"*). One fact written twice: `Parser.rangedAliasTag` reduces a `named` type through the alias REGISTRY, but `HashValue` is the one ranged int alias the COMPILER declares (shv2 reads no stdlib ⇒ in no registry), while `TypeResolution.resolveNamedType` has erased that exact name to `integer` since slice 2a. Cured by stating the name set ONCE (`IrInterface.isSynthesizedIntAliasName`), both readers spelling it, pinned by a new `int.hash.chained` spec; zero fragment churn ⇒ behaviour-neutral for all 1594 pre-existing programs. Review also **verified the separator check FIRES by SABOTAGE** (flipping the byte turned all 8 cases RED with E3004) and **REFUSED a fold** the coordinator floated, with the right reason: a shared name-extractor has no free spelling here — a payload union boxes (the exact cost the optimizer had just deleted), an out-array pushes a managed ByteArray per hit, a closure captures — and `markCalleeOf` walks every live op of every function, so folding would REINTRODUCE a per-op allocation; the copies are bound by a compile-time exhaustiveness check it verified (`E2026`) rather than asserted. **DECISIONS:** `String`-direct dispatch stays out (different dispatcher, own slice); an **`ExitCode` receiver does not dispatch** — it carries its OWN value tag rather than `named`, so the alias reduction never reaches it and `a.compare(b)` falls to a clean **E2015** while `Integer`/`Byte`/bare `int` all work — a recorded asymmetry, not a defect. **MEASURED DEBT (Workstream O):** `builtinConformerMethod` calls `requireBuiltinInterface` per candidate protocol, which **re-synthesizes a whole `IrInterface` every call** (~12 extra allocs for `.hash()`, ~20 for `.equals()`; `hash` builds 1, `equals` 2, `compare` 3 — that IS the step) — **linear** (×1.971→×1.988 over 64→512 sites) and **structurally invisible to `scale-test`, which generates no direct dispatches**; it is the same root cause as this row's existing "memoize `builtinInterface`" note. **RE-MEASURE TRIGGER:** a corpus knob that generates direct dispatches, or a fourth builtin-conformable protocol. ✅ **2b-iii — FLOAT + BOOL CONFORMANCE — DONE, MERGED `9d13bc7cf`** (impl `037642701` · optimize `df725538a` · review `9d13bc7cf`; merged-tree native **1614/0**, wasm **1423/0**, `-j1`==`-j12` byte-identical, fragments 10 additions / zero `M`, builds 0 warnings, no 101; rebased over an extremely active upstream, every conflict confined to `docs/optimization-log.md`). **`float × {Comparable, Equatable}`** and **`bool × {Comparable}`** conform intrinsically ⇒ `f.compare(g)` / `f.equals(g)` / `b.compare(c)` dispatch directly. ⭐ **Flips 10 CANONICAL cases**, so with rung 1 the two ported files now stand at **18 of 20 green** (`specs/primitive-comparable.md` 13/13, `specs/primitive-hashable.md` 5/7). `float.compare` is the TOTAL order both references pin — NaN at the bottom, two NaNs `equalTo`, tested by the canonical `x != x`; `-0.0` vs `+0.0` reaches `equalTo` **by fallthrough, with no signed-zero branch emitted** (verified in the IR). `float.equals` is plain IEEE, so `NaN.equals(NaN)` is **false** — a deliberate, spec-pinned asymmetry with `compare`, now commented rather than left to be "fixed" by a later reader. ⭐ **THE SHARED-BUILDER INSIGHT (implementer, better than the brief asked for): `bool.compare` IS `int.compare`'s graph.** Both references write bool's body as an explicit 0/1 mapping only because the SOURCE language has no bool→int cast — but a Std `i1` already *is* 0 or 1 in its machine word. So three builders became two, parameterized by operand type (`buildBuiltinEquals`, `buildOrderingCompare`), emission order preserved ⇒ `int.*` goldens byte-identical. ⭐⭐ **THE REVIEW PROBED THAT PREMISE ON 29 ROUTES × 2 TARGETS RATHER THAN REASONING ABOUT IT** — comparison results, `not`, `and`/`or`/`xor` merges, struct fields, globals, ternaries, if-merge phis, loop-carried block args, float `<`/`!=`/`==`/`>=` incl. **the parity leg of `nan != nan`**, `equals` results consumed as bool, bool==bool, union payloads, `__fnref_` thunks, closures — each made opaque through a non-inlined call so const-folding could not launder it, with a detector where **only an exact 0/1 answers `equalTo`**. It holds, and two structural facts say why: `encodeSetccReg` (`X64Backend.maxon:565`) emits `setcc dest8` **followed by `movzx dest64, dest8` indivisibly**, and an `Array` of bool does not exist (E2015), so a slot is not a route. arm64 cross-compiles clean (`cset lt`/`gt`; `fcmp d0,d0`+`cset ne` is correct because unordered clears Z, and the ordered legs use `MI`/`GT` not `LT`). ⭐⭐ **REVIEW FOUND THE DUPLICATION THAT FAILS AS A WRONG ANSWER:** `buildOrderingCompare` and `buildFloatCompare`'s tail were **two transcriptions of the same five statements**, held together only by a comment reading *"puts a NaN preamble in front of this same cascade"* — a test nobody wrote. An edit reaching one and missing the other makes **`int` and `float` order differently in ONE program**, with nothing failing to compile and no golden moving. Single-sourced as `emitOrderedCascade`, entered at `entry` by the bare form and at `ordered` by the float form (the verdict blocks stay the caller's, because block-creation order IS emission order — which is how both goldens stay byte-identical). ⭐ **AND IT KILLED THE ROSTER PATTERN, not just the stale copy:** the installed-impl list existed at **FOUR** sites (`Compiler.maxon:317` stale for the SECOND rung running · `MmRuntime.maxon:146` · `Parser.maxon:1020`, a *count* that said "eighteen" when the answer is **nineteen** · `BuiltinConformanceRuntime.maxon:5`). All four enumerations DELETED and pointed at the one owner, `isIntrinsicBuiltinConformance` — *"a pointer cannot go stale; a roster at a reader always will."* Also made minting and registering an impl name inseparable (`candidateImplName`): binding one without registering it meant the reference walk never saw it, its `if` never fired, and **the impl was never built** — dying at link time naming a symbol whose builder sat ten lines below. **OPTIMIZER:** rung cost is **+12 allocs / +592 B per compile, FLAT at all six rungs**, attributed **by knob** (aliasing away the three new `qualifiedMethodName` joins returns to base *exactly*, so the widened candidate list, the three-qualifier predicate and `emitCmp`'s defaulted `operandType` across 79 defaulting sites are each measured FREE). It also found the builders constructing the param-type array **twice** and made `emitTypedParams` read `func.paramTypes` by reference so the ops emitted ARE the types declared (+14 → +12); the review then **audited that aliasing** — every writer traced, and a grep for any in-place mutation of `paramTypes` returns nothing. **Rung 1's filed debt re-measured and did NOT grow** — and the ragged conformance rows make the new conformers CHEAPER than `int` per dispatch (`float.equals` +7/site vs `int.equals` +16), because a protocol `isIntrinsicBuiltinConformance` withholds is skipped before `requireBuiltinInterface` is ever reached. **DECISIONS:** no `float × Hashable` (see the bitcast rung below) and `bool` gets Comparable ONLY — both references omit Hashable/Equatable for bool (*"bool arrays use bit-packing"*) and the corpus has no such case, positive or negative. **REMAINING SLICES (future rungs) — 2b-iv SUB-SLICED B/C by the 2b-v coordinator, because the two backends need genuinely different mechanisms and only one is verifiable on the Windows host:** **2b-iv-B wasm witness lowering** — ⭐ **the biggest unlock left in P1.7a: 85 currently x64-gated spec blocks** (63 witness-family + 22 managed-opaque-`T` descriptor), because BOTH families are refused by the SAME gate (`requireNoFuncAbs64Relocs`, `StdToWasm.maxon:189`) — coordinator-probed on both. ✅ **The route is already mapped and is SMALL**: shv2's wasm backend ALREADY has `funcAddr`→table-index, `call_indirect`, a funcref table + elem section covering every function, and a `buildIndirectCallTypeIdxMap` pre-pass that explicitly `break`s on `witnessCall` (`StdToWasm.maxon:491`). v1 does exactly this and its witness blob layout is TARGET-AGNOSTIC — only the patched VALUE differs: native writers bake an absolute code VA, wasm bakes the FUNCREF-TABLE INDEX into the same 8-byte slot (v1 `patchOneFuncAbs64InRdataWasm`). Insertion point is structural: `calleeFuncIdx` is already in hand at the top of `buildWasmCoreModule` and `applyStringBufferFixups` already patches the `.rdata` IMAGE in place for the identical reason. ⚠ Traps: a ZERO slot picks `table[0]` and traps *"indirect call type mismatch"*; and `closure-capture.md:303` records that an `ExitCode`-returning fn value ALREADY traps because the functype is arg-count-derived to `(i64ⁿ)→i64`, so RESULT WIDTH must be carried. · **2b-iv-C arm64 witness lowering** — the harder half and rightly separate: arm64 has NO rdata→text mechanism at all (`ADRP`+`ADD` materializes a function address into a REGISTER from code, and cannot patch a slot inside an `.rdata` blob); macOS needs a `DYLD_CHAINED_PTR_64` bind variant beside the existing rdata→rdata String rebases, and arm64-linux/windows have no analog. · **2b-v float `Hashable`** (was numbered 2b-iii, which collided with the landed float+bool slice — renumbered here; needs a `floatToBits` reinterpret + −0 normalize) · ✅ **Slice 2b-vi — MULTI-ARGUMENT WITNESS DISPATCH — DONE** (impl `d8f44a28b` · optimize `bdea7bf1e` · review `5587ed047`; x64-windows **1922→1942/0**, wasm **1831/0**, builds 0 warnings, no 101, fragments additions-only + ONE intentional `M`, scale allocs/bytes **Δ0 at every rung**). **THE RED:** every interface method with ≥2 parameters was UNCALLABLE through a witness — `self.item.add(1, b: 2)` → **`E2004: Undefined variable 'b'`** — because `parseWitnessMethodOnValue` parsed its argument list with a hand-rolled loop that read no LABELS, while the language requires them from argument 2 onward. ⭐⭐ **BUT THE FILED DESCRIPTION WAS WRONG THAT THIS WAS "a clean COMPILE ERROR not a wrong answer", AND THE COORDINATOR MEASURED IT BEFORE PLANNING:** the same loop did **no ARITY check either**, so an under-supplied dispatch **compiled clean and read a garbage register** — `self.item.add(1)` against two formals exited **1073754132**, `self.item.add()` against one formal exited **1073750019**, and with a ranged return type the garbage instead fired a **spurious range-check panic on a correct program**. Arity was therefore pulled INTO the rung rather than left on the separately-filed type-check rung: it falls out of label slotting for free, and shipping labels without it would have left a silent garbage read reachable through the very construct the slice enables. **THE THESIS — no new rule and NO NEW ERROR CODE.** shv2 already had ONE argument-binding door (`consumeArgLabel` for E2052/E2053; `slotCallArgs`/`argSlotPosition` → `ArgSlotResult` for E3036/E3037/E3038); the witness site was a **THIRD, unwritten spelling** of "parse an argument list" that silently skipped all five. `argSlotPosition`/`slotCallArgs` were narrowed from taking a `FuncSignature` to taking a `paramNames ByteArrayArray` (exact, since `paramCount()` IS `paramNames.count()` and `indexOfParam` is a pure scan of it), `WitnessDispatchTarget` gained the `paramNames` the `InterfaceMethodSig` at its one construction site was already dropping, and the site now calls the shared `parseCallArgs` → `slotCallArgs`. ⭐ **That collapsed THREE copies of the label→position mapping into one** — including a pre-existing hand-rolled copy in `Parser.transferConsumedArgs` which had silently lost the measured O(P) fast path, so the consume transfer **GAINED** a fast path it never had. ⭐ **THE RULE IS NOT "identical to a concrete call", AND THE CORPUS SAID SO BEFORE THE WAVE:** `specs/interface-dispatch.md:212-219` states that an interface **or type-parameter** method call **MAY name its FIRST argument** (E2052 is re-applied only for concrete-struct/enum/builtin receivers), cross-referenced by `specs/parameter-labels.md:200-204`, and v1 agrees in code (`allowFirstNamed: true` at its method-call site). ⇒ **E2052 EXEMPT, E2053 ENFORCED.** The coordinator's first design had it uniform and would have broken a canonical tested rule; the survey caught it. **LABELS BIND AGAINST THE INTERFACE**, forced twice over: under dictionary-passing there is no concrete callee to ask (the same reasoning 2b-v ratified for `throws`), and `ConformanceCheck.signatureMatches` compares arity and TYPES only — parameter names are read solely to format E3016 — so several conformers may spell their parameters differently and the interface's names are the ONLY authoritative label vocabulary. ⚠ **BOTH REFERENCES HAVE THIS SAME HOLE, so neither is a specification here**: the bootstrap's constrained-type-param path STRIPS any label at any position and appends in source order (so `add(b: 2, a: 1)` binds backwards, uncaught), and v1's `buildDispatchArgIds` discards labels identically. shv2 reorders correctly and is deliberately better. ⚠ **The bootstrap CANNOT ORACLE THIS SHAPE AT ALL** — `where T is <iface>` gives `E4006: Primitive type 'int' has no method named 'add'` even for the single-argument control (coordinator-verified), so the design rests on the corpus + v1 + inference, as the witness-label rung's did. **REQUIRED COMPANION EDIT — the rung's own proof:** `specs-shv2/where-clauses.md`'s live `float-formal-among-int-formals` called `self.item.combine(1, 3, 5)`, all positional, legal **only because of this bug**; now `combine(1, k: 3, b: 5)`. It is the ONLY multi-arg witness call in either corpus (two independent surveys + a coordinator grep), and its fragment diff is a **single embedded source line** — Target IR including the `cvtsi2sd` position byte-identical. ⭐⭐ **THE INDEPENDENT REVIEW FOUND A FOURTH HOLE IN THE SAME CONJUNCTION, LIVE ON `main`, AND FIXED IT** (`5587ed047`): `requireProtocolResult` proved "is this the Equatable/Comparable protocol?" by result type, by `throws`, and (as of this rung) by arity — **but never by the formal's TYPE**. So `interface WeirdEq { function equals(other Integer) returns bool }` hijacked `==` and handed the receiver's `T` POINTER to a formal read as an int: **two equal `Point(5)` values compared UNEQUAL, exit 0, no diagnostic — coordinator-reproduced on `main` @`153d04620`** (and the `float` shape `cvtsi2sd`s the pointer, the `String` shape reads an unmanaged struct pointer through a managed header). `requireGenuineSelfArgs` is structurally blind to it because this interface declares no `Self` formal. Cured by `requireProtocolShape`, which takes the whole fetched `InterfaceMethodSig` and compares formal lists through one `interfaceFormalsMatch` — **subsuming the rung's own arity check, so it is ONE question rather than four checks standing beside each other.** ⚠ **Note the pattern this closes: `findWitnessDispatch` matches by method NAME, and 2b-ii, 2b-v, this rung and this review EACH added one more discriminator** (result type + protocol fetch, `throws`, arity, formal types). It is now one comparison of the whole signature, which is what it should have been. **OPTIMIZER: no code change, and the measurements are why.** The ladder reads Δ0 — and **that zero measures UNREACHED, not free, PROVEN FROM THE CORPUS**: `--emit-corpus` over all 465 generated files holds 276 `interface`, 276 `implements`, 270 `uses` and **ZERO `where`**, so every line this slice touched runs exactly zero times in the ladder (the trap 2b-ii, 2b-v and 2b-iv-B each hit). New instrument `maxon-shv2/Testing/ladders/genwitnessargs.sh` adds the ARGUMENT-COUNT axis `genwitness.sh` holds at one. Cost is **+14 allocations / +465 bytes per witness dispatch, in `phase:parse` only, dead flat across a 16× span**, attributed **13 of 14 by probe rather than arithmetic** (a throwaway array measured exactly 2 allocations, a throwaway `ArgSlotResult.ordered(...)` exactly 1); arity is **~31 + 5.5·A**, linear — a P² term would put A=63 near 830, not 376. `WitnessDispatchTarget.paramNames` proved a SHARED REFERENCE by experiment (a second redundant field moved the cost by exactly 0). `argSlotPosition`'s O(P) fast path — the thing the refactor most endangered — re-verified on the `genmutchain.sh` ladder its own header cites: allocations identical to the digit, growth ×1.73→×1.88, the ×3.05 quadratic still gone. ⚠ **It also refuted a delta rather than filing it**: the `+148…+3062` the instrument showed was **`main`'s advance since the previous log row, not this rung**, confirmed by rebuilding `main` in a control worktree at an **EQUAL-LENGTH path** (the 202 B/char `phase:load` trap) and diffing both runs' per-phase tables to empty. **PORTS ONE REAL CORPUS FILE AND AUTHORS THE REST, AND SAYS WHY:** no case anywhere in ~2,700 reaches a ≥2-parameter interface method through a constrained type parameter, so **0 disabled cases flipped and 0 witness cases were portable**; the deliverable is authored `where-clauses.md` cases (labelled, out-of-order, first-arg-labelled, ranged-return regression, + E2053/E3036×2/E3037/E3038 and two `requireProtocolShape` regressions) plus a genuine port of **`specs/parameter-labels.md`** — the canonical statement of the rule this slice extends, measured at **4 passed / 6 failed** before the wave, landing 8 enabled. ⚠ Its 2 remaining `disabled-test:` cases are **parameter defaults** (a real later milestone), and **4 `maxoncstderr` blocks carry shv2's own dedicated codes** (E2053/E3037, and E2052's fuller message) where the bootstrap enforces the same rules through generic fallbacks (E3005/E3003) — a **pre-existing ratified divergence**, since `docs/error-codes.txt` defines E2053/E3037 for exactly these rules and claims them for shv2 alone. Those 8 cases exercise the CONCRETE path and passed before the rung too: a ratchet, not this slice's unlock. **⇒ TWO ITEMS FILED, both below: the E3019 witness blindness (its own rung) and the reverse-label scan (Workstream O).**  · ⬜ **NEW: E3019 IS NEVER *ASKED* AT A WITNESS DISPATCH — a `let` is mutated with no diagnostic.** `parseWitnessMethodOnValue` fills `argImmutableNames` and discards it, so the immutable-argument-to-mutating-parameter check never runs for a witness call — **and the blindness propagates OUTWARD**, so the concrete caller's *transitive* check is wrong too. Coordinator-reproduced on `main` @`153d04620` **and identically on the 2b-vi branch, with a ONE-argument call**, so it is strictly pre-existing and untouched by the slice: a `let a = IntArray.create()` passed through `Box.run(dest)` into a witness-dispatched `grow(dest)` is **pushed to, exits 9, no diagnostic**. ⭐ **RULED an OVER-ACCEPTANCE, not a miscompile** (coordinator, confirmed by the 2b-vi reviewer, which grew the array 40 elements through several reallocations and found the caller's binding still correct — count and elements intact): the array is live and owned and the push is well-defined, which is materially unlike the garbage-read 2b-vi fixed, and is why it could be deferred at all. **It needs its own rung because it needs a DESIGN RULING:** the mutation summary is keyed by CALLEE and dictionary-passing has no single callee, so answering it means either declaring mutability on the interface method or taking the union over every registered conformer. ⚠ **The one alternative the process otherwise demands — reject the construct cleanly — is NOT available**: 2b-iv-B deliberately un-gated 22 managed-opaque-`T` blocks, and a blanket refusal of managed arguments through witnesses would undo shipped function. **RED IS ALREADY COMMITTED**: `specs-shv2/where-clauses.md` `witness-mutation-of-let-argument-refused`, `disabled-test:` naming this rung, with the projected E3019 position flagged as projected. · **MEASURED DEBT → Workstream O (constant, not chased):** fully REVERSE-ordered argument labels miss `argSlotPosition`'s fast path on every argument and fall back to `indexOfParamIn`'s scan, ~P²/2 comparisons per call. **Pre-existing and SHARED with the concrete path** — measured on a direct-call ladder both binaries accept, head 33,495,572 vs parent 33,850,842 ticks at P=63, head marginally faster and well inside noise, allocations identical — and the witness path inherits it at **420,906 ticks/site against the direct call's 411,140, 2.4% apart**, which is the cleanest available evidence that 2b-vi routed onto the shared door rather than growing a second one. **IT CANNOT GROW: P is capped at 63 by the language** (`E2015` refuses a call at 65 argument slots — the cap is the float mask's width), so a quadratic in a variable the type system bounds at 64 is a **constant**, ~2,048 comparisons/site, linear in program size. **RE-MEASURE TRIGGER: any change to that argument-slot cap.** The only cure is the per-signature name→index map `indexOfParamIn`'s own header already rejects with its reason — a heap object on every signature in every program, to remove a bounded term on a shape nobody writes. · ✅ **ALL SLICES OF P1.7a ARE NOW DONE — THE RUNG IS COMPLETE (2026-07-29).** ⚠⚠ **THIS SENTENCE USED TO SAY “only 2b-iv-C (arm64) and float `Hashable` remain”, AND HALF OF IT WAS ALREADY FALSE WHEN IT WAS WRITTEN**: **2b-iv-C had landed six commits earlier in `227fb1051`** (arm64 witness dispatch — `ldr x16,[witness,#slot]` + `blr x16`, the ELF bake widened from a CPU test to a FIXED-BASE-FORMAT test, and Mach-O slots threaded as `DYLD_CHAINED_PTR_64_OFFSET` rebases into the ONE chain a String `buffer@0` already rode; measured arm64-macos **2034/0** natively with 115 previously-gated tests un-gated). The coordinator confirmed it independently before planning, by disassembling a cross-compiled witness binary (`ldr x16, [x1, #0x18]` + `blr x16`, slot 24 = the first method) rather than trusting this row — **the plan's own ladder had gone stale against the tree, which is this project's signature bug appearing in the document that tracks it.** ✅ **FLOAT `Hashable` — DONE, MERGED** (impl `72ecaf9dd` · sweep `072e143e4` · optimize `62c7df756` · review `4881d7179`+`fef04ec9a`; suite **2178→2185/0**, exit 0 no 101, x64-linux PASS, wasm32-wasi PASS, **arm64 SKIP — remote, UNVERIFIED**, fragments **14 additions / ZERO `M`**, builds 0 warnings). Net-new **`StdUnaryOpcode.bitcastF64ToI64`**, the dialect's first cross-register-class op that is NOT a numeric convert — appended PAST `sqrt` so no existing range arm silently re-slices (the band-append trap slice 2b-v paid 13-ranges-into-26-arms to control). Its own `TargetOp` per native target (x64 `movqGprXmm` = `66 REX.W 0F 7E /r`; arm64 `arm64FmovGprFp` = `FMOV Xd,Dn` = `0x9E660000`); wasm needs none (`i64.reinterpret_f64` = `0xBD`). **Both `emitRegRegMove` cross-class panics are untouched and un-widened** — a MOVE still never crosses a file; the remedy is a third named op, exactly as those panics say. Deliverable is **REAL CORPUS**: the two `disabled-test:` cases in `specs-shv2/primitive-hashable.md` flipped byte-identical to `specs/`, taking that file to **7/7** and the two primitive-conformance files to **20/20**, plus 5 authored regressions. ⭐ **THE IMPLEMENTER REFUSED THE BRIEF'S ALGORITHM, WITH A MEASUREMENT.** The brief said to TAKE v1's `stdlib/PrimitiveExtensions.maxon:31-37` body exactly, including `if bits == i64.min return 0`. **That branch is DEAD FOR EVERY INPUT** — `0x8000000000000000 and 0xFFFFFFFF` is already `0`, the same `0` the branch returns — so `float.hash` is **three instructions** and the `-0.0` property is held instead by `assertSignBitAboveHashMask`, **seen RED** (`HashLow32Mask = -1` panics on the first program that installs the impl). The review oracle-diffed 24 float values — both zeros literal AND computed, subnormals, ±infinity, three NaN construction routes, adjacent-mantissa neighbours — byte-for-byte against the bootstrap, and confirmed `{(+0.0,−0.0)}` really is the only `equals`-equal bit-differing pair (NaN is equal to nothing under plain-IEEE `float.equals`, which is why NaN is deliberately NOT normalized — commented, not left to be “fixed”). ⚠ The assert's blind spot is stated out loud: it checks the mask's WIDTH, not the fold's OPERATION, so a future xor-fold or rotate would pass it vacuously. ⭐⭐ **THE REVIEW ANSWERED A COORDINATOR SUSPICION BY REFUTING ITS PREMISE.** `SsaDestruction.isPhysicalSelfMove` carried a conscious arm warning that `fmov x3, d3` “shares a `RegNum` only because the numbering is one space”. **Backwards**: `TargetDialect.maxon:45-77` and `:206-270` put GPRs and FP registers in DISJOINT ranges on BOTH targets (`xmm0 = 16`, `d0 = 31`), so shv2's single number space is precisely what ABOLISHES that hazard — it would be real only under v1's two-enum design. Comment rewritten. And the x64/arm64 asymmetry the coordinator flagged is **STRUCTURAL, not a judgment**: `arm64FmovGprFp` and `bitcastF64ToI64` are each the LAST member of their enum so exhaustiveness FORCED explicit arms, while `movqGprXmm` sits mid-union where **17 range arms span it — all 17 verified correct FOR THE RIGHT REASON**, and the one site where a wrong answer would be a real miscompile (`opPhysicalRegs`, callee-saved tracking) is enumerated per-op and got a conscious arm. No latent bug; no arm owed. ⭐ **DUPLICATION: THE SIXTH STALE COUNT, AND IT CONTRADICTED ITSELF INSIDE ONE FILE** — `BuiltinConformanceRuntime.maxon:51` said the low-32 mask is “shared by **both hashers**” while its own `returnLow32Masked` header 347 lines below already said “all three (`int`, `float`, `String`)”. The roster now lives at the fold and the constant carries no count. Also swept: that file's header roster (which omitted `float.hash` one line above the sentence “a count is a copy that goes stale on the next rung”); a fourth family across `SignatureIndex.maxon:929` / `Parser.maxon:19238`/`:23129` and a **user-visible message** at `:23157` all saying a float union payload needs “the bitcast **ops**” plural when one of the two now ships; and `SseMandatoryPrefix.packedDouble`'s 66-prefix roster, stale TWICE (P1.0d.4-C's `roundsd`, then this rung's `movq`). ⭐⭐ **A SABOTAGE RESULT ABOUT TEST DESIGN, WORTH MORE THAN THE RUNG**: injecting the exact ModR/M swap `encodeMovqGprXmm`'s comment warns of turned RED only the **three cases that PIN A NUMBER** (`chained`/`negative`/`nan`); `nonzero`, `zero-normalization` and `equal-values-equal-hashes` all stayed **GREEN**, because they assert RELATIONS and both sides were wrong identically. **A relational assertion cannot catch a bug that moves both operands.** ⚠ `Box with float` under a `where` constraint is a clean **E2062** refusal (NOT the compile-time panic this row used to claim), so `float.hash` is **DIRECT-dispatch only** and the rung made no crash reachable; `error.float-type-argument-still-refused` pins that guard. **OPTIMIZER: no compiler change, and the measurements are why** — rung footprint **+8 allocs / +510 B per compile, FLAT across a 32× span**, A/B'd against the parent built at an **EXACTLY EQUAL-LENGTH path** so the `phase:load` 202 B/char term cancels; the instrument's own −4,052…−143,479 delta column was **REFUTED as `main`'s advance, not filed**. Private ladder `Testing/ladders/genfloathash.sh` built because the scale corpus emits ZERO float `.hash()` calls — its proof of reach is that the PARENT compiler REFUSES its `hash` file outright. · **MEASURED DEBT → Workstream O, AND THE OLD TRIGGER WAS ON THE WRONG AXIS:** admitting `float` to `Hashable` costs **+10 allocs / +344 B per float `.equals()`/`.compare()` site** (exact at four sizes, `10×sites+8`, linear, controls flat) — nothing in `float.equals`'s path changed, its POSITION IN A SEARCH ORDER did, so `builtinConformerMethod` now synthesizes and discards a whole `IrInterface` before reaching Equatable. Filed, not fixed: the cure needs `builtinInterface` to return a BORROW of a compiler-lifetime cache (it returns an `IrInterface` owning three arrays BY VALUE), which changes every consumer and introduces the long-lived-borrow shape E3070 polices — a rung, not a ride-along; and the cheap alternative is forbidden by the design's own thesis, since it re-introduces the second roster `builtinConformerMethod` exists to eliminate. ⚠ **The existing trigger read “a fourth builtin-conformable protocol” and DID NOT FIRE — yet the debt got worse anyway**, because the cost is (protocols × conformance-ROW WIDTH) and this rung widened a ROW. **REPLACEMENT TRIGGER: any rung that adds a (type, protocol) PAIR to `isIntrinsicBuiltinConformance`, or a fourth protocol.** · ⚠ **OWED TO THE HAND-SYNC:** the 7 new cases have x64-windows and x64-linux goldens and **NONE on arm64-\***, and a MISSING golden never fails — a future arm64 run would silently CREATE them rather than go red. `primitive-hashable.md` deliberately carries no `targets:` marker: the cases are target-neutral and SHOULD run everywhere. · **STILL OPEN, AS THEIR OWN NUMBERED RUNGS — NOT P1.7a RESIDUALS:** the E3019 witness blindness (needs a design ruling); GENERIC-BODY SUBSTITUTION TYPE-CHECKING (the surviving TYPE half of witness argument checking); FUNCTION-VALUE FLOAT WIDENING (`typealias Scaler = function(float) returns float` — a silent wrong answer live on `main`, no witness involved); and **the SCALE-CORPUS GAP, whose stated justification is now VOID** — `ScaleCorpus.maxon:2824-2828` still says witness dispatch, `Set` and opaque-`T` elements are absent because “on arm64/wasm a witness call is a `panic()`”, which `227fb1051` and 2b-iv-B falsified; the gap may still be right, but it now has NO REASON, so it is re-opened as a rung rather than left standing on a dead argument. · ⬜ **NEW: FUNCTION-VALUE FLOAT WIDENING — A SILENT WRONG ANSWER LIVE ON `main` TODAY.** The exact twin of the witness-dispatch bug 2b-iv-B fixed, but through a DIFFERENT DOOR, so that fix does not generalise: `typealias Scaler = function(float) returns float; let f = twice as Scaler; f(2)` returns the WRONG ANSWER on x64 (exit 1 where 4.0 is meant) and traps `indirect call type mismatch` on wasm. **Reproduced with `main`'s own compiler at `a2543d15c`, with NO witness involved.** Root cause is the function-TYPE alias's declared param types (`Parser.functionAliasReturnType` / the `FunctionTypeAlias` path), not the interface path. ⚠ `appendCallIndirect`'s own header (`LowerMaxonToStd.maxon:1292-1300`) claims placement "matches the callee thunk's declared parameter types **by construction**" — **that claim is FALSE for exactly this shape**, and a comment doing a check's job is this project's signature bug. Pre-existing P1.5-A1/#78. **Its own rung.** · ◑ **A WITNESS DISPATCH DOES NOT TYPE-CHECK ITS ARGUMENTS against the interface's formals** — ✅ **the ARITY half is CLOSED by 2b-vi** (`self.item.scale()` with ZERO actuals for a one-formal method is now **E3036**; it used to compile clean and read a garbage register). **The TYPE half remains**: `self.item.scale("x")` (a `String` where the formal is `float`) still compiles CLEAN on x64 and wasm. That half is the natural home for the `GENERIC-BODY SUBSTITUTION TYPE-CHECKING` rung already filed above, because it needs substitution machinery arity does not. Found by 2b-iv-B's review; halved by 2b-vi. ⚠ **Note what 2b-vi's own review then proved about the surviving half: it is not merely a missing diagnostic.** A near-miss protocol whose formal TYPE differs (`interface WeirdEq { function equals(other Integer) returns bool }`) hijacked `==` and handed the receiver's `T` POINTER to a formal read as an int — **two equal values compared UNEQUAL, exit 0, no diagnostic, coordinator-reproduced on `main` @`153d04620`**. That specific door is now shut by `requireProtocolShape` (below), but it is the clearest evidence available that "the compiler does not check argument types under substitution" cashes out as wrong answers, not just as over-acceptance. · ⬜ **NEW: `Box with float` under ANY `where T is Equatable/Comparable` constraint PANICS at compile time on BOTH targets** (x64 `X64Backend.maxon:715` cross-register-file move; wasm `coerceOnStack` f64→i64). Reproduced identically with `main`'s compiler — pre-existing **P1.6-A** "trivial type argument" gap, target-symmetric, and a LOUD refusal rather than a wrong answer. Noted because `comparable.md` calls float conformance "a separate future mechanism" and this is what that costs today. **GENERIC-BODY SUBSTITUTION TYPE-CHECKING is a SEPARATE future rung** (surfaced by the 2b-i review): the GENERAL form of 2b-i's Self-arg reject — reject `A == B` / `witness.method(arg)` whenever an operand/argument type mismatches under substitution (cross-type-param `A==B`, nested generics, ALL argument positions, return-type mismatches). `TypeRules.maxon:159` "a type parameter agrees with anything (no substitution)" is the documented deferral this closes. 2b-i closed only the **SAME-parameter half** of the `Self`-typed-witness-arg crash class — ⚠ **2b-ii's independent review found the CROSS-PARAMETER half still live, and reachable on `main` through `Equatable` ALONE**: `requireGenuineSelfArgs` required the actual to be a value of *some* enclosing type parameter, not the SAME one, so `type Mix uses A, B where A is Equatable, B is Equatable` @ `(Point, Integer)` with `self.a == self.b` had `Point.equals` dereference the integer `9` as its `other Point` ⇒ **`0xC0000005`, coordinator-reproduced on the pre-2b-ii base with no `Comparable` anywhere**; the `(Integer, Point)` order instead gave a SILENT WRONG ANSWER (exit 11, an int compared against a heap pointer). ⭐ Note this was **not** ill-typed input by any rule the compiler states — `self.a == self.b` in a two-parameter generic is ordinary code — so it belonged to the crash class, not to the substitution-type-check rung. CLOSED in 2b-ii at the ONE check point (`isReceiverTypeParamValue`: the actual's parameter POSITION must equal the receiver's), serving BOTH operator families and both the operator and `.method()` forms, with the three pre-existing goldens byte-identical. The remaining argument positions, nested generics and return-type mismatches stay reachable-only-from-ill-typed-input until this rung. **DIRECT primitive method dispatch (`i.hash()` on a concrete `int` VALUE, E2015 today) is a SEPARATE future mechanism** (v1's statically-known-primitive→direct-call path); 2b-i is witness-form only. P1.7b `Set` needs only 2b-i for int keys (String keys ride a sibling String-conformance slice).** **DISTINCT FUTURE FEATURES surfaced (out of rung):** method OVERLOADING (blocks `stringable-format-spec`/FormattedStringable — non-mangled `Type.method` collision → E3006); stdlib type-ALIAS resolution — `HashValue` ✅ resolved in 2b-i, `Ordering`→E3011 still blocks Comparable *use* (2b-ii); E3012 unused-var checking; inline ranged field type `var n as int(0 to 100)` (pre-existing E2010, needs a typealias). **FOLLOW-UP NOTES (optimizer, constant-factor linear):** memoize `builtinInterface` (re-synth per lookup); cache `collectInterfaceMethods` per-interface (rebuilt per conforming type). ✅ **Slice 2b-v — THROWING WITNESS DISPATCH (`witnessTryCall`, x64) — DONE** (impl `0f18068ec`+`09d11426a` · optimize `8247d0f6c` · review `a393a8d1c`; x64-windows **1865/0**, x64-linux **PASS**, wasm **PASS**, builds 0 warnings, no 101, fragments **14 added / 0 modified**, scale allocsΔ**0** at every rung). ⚠ **arm64-macos / arm64-linux SKIPPED — the Mac was asleep at gate time, so they are UNVERIFIED, not proven good.** Mitigated but not replaced by cross-compilation: an ordinary program still builds for both arm64 targets, and the two witness ops each hit their OWN loud refusal (`StdToArm64Conversion.maxon:497` `witnessCall` / `:498` `witnessTryCall`). **THE RED:** shv2 **never enforced the `try` requirement at a witness dispatch** — `SemanticCheck`'s throws map is keyed by callee NAME and a witness method has none — so a bare throwing dispatch compiled and **silently dropped the error flag** (coordinator-reproduced: exit **0** where the answer is 55; the direct-call control gave 55, and both the oracle and shv2's own direct path answer **E3057**). ONE root cause, TWO symptoms: a missing diagnostic and a wrong answer. ⭐ **THE PLAN WAS WRONG AT THE CODE AND THE IMPLEMENTER STOPPED TO SAY SO** — the brief asserted the repro should exit 55; it cannot, because the repro itself omits `try` and is **E3057** by the language's own rule. It measured that against the oracle before writing a line and covered BOTH outcomes. **THE OP:** `StdOp.witnessTryCall` appended at the VERY END of the union, governed by one identity — **`witnessTryCall : witnessCall :: tryCall : call`**. ⚠ **The band-append invariant refused every `witnessCall to witnessTryCall` range arm** (the P1.5 IOCP band sits physically BETWEEN them), forcing **13 ranges into 26 conscious arms** — the invariant finding the call sites instead of a reviewer. **LEFT BEHIND DELIBERATELY:** v1's `isVoid` bool threaded through ~14 files — shv2's `resultType` is a `StdReturnType` union and `funcResultValTypes` is the ONE door both interning and emission use, so **shv2 pre-pays that bug structurally**. **DELIBERATE DIVERGENCE FROM BOTH REFERENCES (coordinator-ratified):** conformance now checks the throws relation in **three** directions, where both oracles check only one — impl must throw where the interface does (**E3016**, the oracles' wording verbatim); impl may **not** throw where the interface does not (**E3016**; both references ACCEPT this and coordinator-reproduced the hazard on `main`: exit **0** with the throw swallowed, since the interface promises no-throw so the error has nowhere to go); and the two named error types must be **EQUAL**. The references are right for themselves — they MONOMORPHIZE, so their `try` reads the callee's own clause; under dictionary-passing the `try` has no callee to ask and types its binding off the **INTERFACE**. ⭐⭐ **THE INDEPENDENT REVIEW FOUND TWO REACHABLE DEFECTS, BOTH FIXED PRE-MERGE (`a393a8d1c`):** (1) a **SEGFAULT this slice itself made reachable** — with the third direction missing, an interface declaring a BOXED union against an impl throwing a SCALAR enum had the `try` dereference the ordinal as a box pointer (**exit 139**), and the milder half caught `OtherError.one` as **`DigestError.beta`** (**exit 72, no diagnostic**); the slice's own header claimed the propagate path caught this and it provably cannot, because that check compares the *interface's* clause against the enclosing function's and in exactly this program those agree. (2) ⭐ **A LIVE WRONG ANSWER ON `main` THAT IS NOT ABOUT WITNESSES AT ALL** — a bare `try` propagating out of a **NON-THROWING** function silently drops the error: `return try inner(3)` in a non-throwing `outer` **compiled and exited 0**, while the oracle refuses it (**E2001**) and **`specs-shv2/error-handling.md:132` already stated the rule** — prose and compiler had disagreed. The slice's own comment had filed and declined it. Fixed at the ONE place both spellings pass through, because a witness-only version of a rule that is not about witnesses would be this project's signature bug; blast radius MEASURED at zero. ⭐ **The review also caught its own regression with the instrument**: its first refactor boxed a verdict union per conforming method (+16→+264 allocs across the ladder, landing OUTSIDE every per-phase bucket because `checkConformance` runs before the pipeline) — reworked to Δ**0**. **OPTIMIZER: no code change, and the measurements are why.** It **REFUTED** the implementer's own attribution by KNOB (the +40 B was NOT per-interface-method — flat in interface methods and in function count, linear in FILES; the “40” was a 24-byte `ThrowsClause.none` box plus 16 B of inline struct growth summing by coincidence) and **PROVED** the second by EXPERIMENT (a dummy 8th field exactly DOUBLED the byte delta at three program sizes with allocations still zero). ⭐ **STANDING RULE worth keeping: any new WIDEST `StdOp` variant taxes EVERY `StdOp` allocation by 8 bytes per field** — the 7th cost **0.218% of bytes, 0 allocations**, strictly O(StdOp count); paid deliberately, since narrowing means a magic bitfield or the ruled-out `isVoid` bool. No superlinearity on three purpose-built ladders (×2.00 in allocations AND CPU) — built by hand because **the scale corpus generates no interfaces at all**, so its Δ0 would have meant nothing. **PORTS NOTHING FROM `/specs`, AND SAYS WHY:** the only two corpus files touching this mechanism (`interface-dispatch-throws-match.md`, `witness-managed-return-throws.md`) BOTH reach the witness through an interface-typed param/return — an **EXISTENTIAL**, E3011, plan-settled out of Phase 1 — and no corpus file contains both `where T is` and `throws`. Deliverable is authored `specs-shv2/witness-throws.md`: **14 cases, 0 disabled** (runtime cases x64-gated, compile-error cases ungated — a diagnostic is target-independent). **NEW SLICE FILED → 2b-vi (below).** ✅ **Slice 2b-iv-B — WASM WITNESS LOWERING — DONE** (impl `b92da57cc` · optimize `49e1cfe22` · review `c4c4c5a12` · goldens `7a1c5a591`; x64-windows **1867/0**, x64-linux **PASS**, wasm **1662→1756/0**, builds 0 warnings, no 101, fragments additions-only). ⚠ **arm64-macos / arm64-linux UNVERIFIED — the remote lane was ABANDONED, not run.** The gate printed `FAIL`, but **zero arm64 tests executed**, which is the *unreachable ⇒ SKIP* case wearing a FAIL label. **ROOT CAUSE, and it is an INFRASTRUCTURE HAZARD worth knowing: `scripts/remote-mac.sh` has NO LOCK, and two sessions drove the SAME Mac at the same time** — a parallel run (`a976124ba`, started 21:40) and this one (`c4c4c5a12`, 21:47) both checked out, built and tested in the one remote tree `~/Dev/maxon`, which was left sitting at a THIRD sha belonging to neither. This run yielded (it started second) and was killed by hand; the other was left untouched. **⇒ The remote lane is not safe to run concurrently; add a lock or serialise by hand.** Mitigated by the review's byte-compare: the shared-code changes move **ZERO BYTES on all four register targets** (arm64-macos, arm64-linux, x64-windows, x64-linux), because `regClassForStdType(i1)` and `(i64)` are both GPR — so the arm64 goldens CORRECTLY did not move. ⭐⭐ **THE DELIVERABLE IS 92 REAL CORPUS BLOCKS UN-GATED** — 63 witness-family + 22 managed-opaque-`T` descriptor + 6 `witness-throws` + 1 `arrays` — because **ONE gate refused BOTH families** (`requireNoFuncAbs64Relocs`). Of the 92 x64-only blocks **only 6 remain**, and they are genuinely x64-ISA (`register-pressure` 4 — the 14-GPR pool; `safety` 2 — `idiv` `#DE`, where AArch64 `SDIV` returns 0 and cannot trap). **THE THESIS (v1's, confirmed): the witness blob layout is TARGET-AGNOSTIC and only the VALUE in the slot differs** — x64 bakes an absolute `.text` VA because its reader DEREFERENCES it (`call qword [witness+24+8i]`); wasm has no code addresses, so its reader is a `call_indirect` and the slot holds the callee's **FUNCREF-TABLE INDEX**. shv2's wasm backend already had every piece (`funcAddr` already IS a table index, `call_indirect`, a table+elem section covering every function, and a type-interning pre-pass that merely `break`ed on `witnessCall`), so this is FIVE WIRINGS and no new machinery. ⚠ **A ZERO SLOT IS NOT INERT ON WASM** — 0 is a VALID table index naming the first import, so a missed reloc would dispatch to `exit-with-code`; review PROVED it cannot happen by construction (`calleeFuncIdx = WasmImportCount + i ≥ 1`, and an unresolvable name panics rather than being skipped). ⭐ **THE `call_indirect` TYPE CONTRACT IS CHECKED BY wasmtime AT RUN TIME, NOT BY THE COMPILER**, and it cost three findings. **(a) `bool` RESULT — a THREE-WAY divergence blocking 18 cases**: the interface says `Equatable.equals` returns `bool` (i1⇒wasm i32) while `createTwoOperandImpl`, `buildStrEq` AND `SetRuntime`'s hand-built witness calls each independently declared i64 — **one fact written down three times**, invisible on register targets (both GPR) and a hard trap on wasm; there is no `ConformanceCheck` for SYNTHESIZED impls, so nothing held them to their interface. Single-sourced as `IrInterface.builtinProtocolResultStdType`, which **panics** on an unrecognised fourth protocol rather than defaulting to a machine word. **(b) `ExitCode` RESULT** — `Parser.interfaceReturnMaxonType` fell through to `named`/i64 while the impl resolved it to u32: right on x64, trapped on wasm; fixed. **(c) the PARAMETER half CANNOT be fixed here** (it needs per-argument WIDTH at the wasm tier, which no op carries) ⇒ a **compile-time refusal** `requireIndirectlyReachableParamsAreMachineWords`, deliberately NOT a new op field (slice 2b-v measured that a new widest `StdOp` variant taxes EVERY `StdOp` allocation 8 B/field). ⭐ It **CANNOT refuse a program that used to compile** — every program it rejects holds a `funcAbs64` reloc, and wasm refused ALL of those wholesale until this slice; a strict NARROWING of that blanket refusal, review-tested, and no spec in the 92 hits it. ⭐⭐ **THE INDEPENDENT REVIEW FOUND A REACHABLE SILENT WRONG ANSWER, LIVE SINCE SLICE 2a, AND IT IS THE HALF THE NEW GUARD STRUCTURALLY CANNOT SEE** (`c4c4c5a12`): a witness dispatch never WIDENED an integral actual supplied for a **`float`** interface formal. A DIRECT call gets this at the Maxon→Std boundary off the callee's resolved signature (`LowerMaxonToStd.widenIntArgsToFloatParams`); a witness dispatch has NO callee to look up, and `argFloatMaskOf` reads the mask off the ALREADY-TYPED argument values rather than off the interface. `self.item.scale(2)` against `scale(k float)` emitted `movRegImm32 rdx, 2` into a body doing `mulsd xmm0, xmm1` — **multiplying whatever was left in xmm0. Coordinator-reproduced on `main`: exit 0 where the answer is 6**, no diagnostic, green suite over it; on wasm it trapped `indirect call type mismatch`. An f64 parameter is LEGAL, so the narrow-param guard is blind to it — **the guard under-refuses, and it reached a wrong answer.** Fixed at the ONE door both dispatch forms pass through (idempotent `promoteToFloat`, fast path when no formal is `float` ⇒ allocation-neutral, whole conformance corpus byte-identical); 2 regression specs pin `cvtsi2sdRegReg` immediately ahead of `callMem [rbx+24]`, incl. the formal→actual MAPPING (`combine(a int, k float, b int)`) that a single-float-formal test cannot see. **Review also single-sourced 2 duplications**: the `.rdata` slot ADDRESS was spelled TWICE (`CodeResult.maxon:155` / `StdToWasm.maxon:356`) when the twins' whole thesis is that only the VALUE is target-specific — drift there corrupts a blob on ONE target with **no diagnostic on either**, a slot being 8 opaque bytes to every reader (now `rdataFuncSlotOffset`); and `Parser.interfaceReturnMaxonType` re-spelled the four conformer names as fresh literals while `IrInterface` exports them and `BoolConformerName`'s own header says a second spelling *"is the same fact and so must not become a second constant"*. **OPTIMIZER: no code change, and the measurement is why** — `ScaleCorpus` emits NO interface, so `pendingRdataRelocs` is EMPTY and both new walks run **zero iterations**: a Δ0 there measures UNREACHED, not free. It BUILT the ladder (`maxon-shv2/Testing/ladders/genwitness.sh`, `relocs = conformers × methods`) and measured **`allocations = 17,677 + 53.18 × relocs`, residuals 0.04%/0.01%** — a straight line, confirmed on a second axis at ×1.90→×1.99, with the N=1024 rung (**2,048 baked funcref indices**) running under wasmtime exit 0. ⭐ **It also ATTRIBUTED a −412 B delta the implementer had written off as environmental: it is the CHECKOUT ROOT PATH LENGTH at exactly 202 B/char** (measured with two directory junctions onto one worktree: −412 at 33 chars, −210 at 34, −8 at 35), refuting the implementer's supporting reasoning (the 1 B/char figure is real, but only for a single-file compile where argv[0] is the only path in `phase:load`). ⚠⚠ **STANDING MEASUREMENT TRAP, hit by BOTH the optimizer and the reviewer in ONE rung: A FLAT NON-ZERO DELTA BETWEEN TWO BINARIES IS A PATH CHECK BEFORE IT IS A FINDING.** The optimizer's first x64 A/B read +7 allocs/+330 B flat at six rungs and it was one step from filing a per-interface cost — the two binaries sat 16 characters apart in argv[0]; at equal-length paths the delta is exactly 0. The reviewer's first cross-target compare read `arm64-macos DIFFERS` — it was the output FILENAME embedded in the MachO. Same family as the `phase:load`-tracks-filename-length trap. |
| **retain-escaping** | **Retain escaping borrows (generalize #40/#64)** — a struct type-arg escaping into a longer-lived generic field is RETAINED/co-owned, not rejected | ✅ | →1465 | **COMPLETE 2026-07-23 (user ruling: RETAIN, NOT a reject-based NLL pass).** The RED (confirmed range-panic on freed `pInner`): `var b=PointBox.create(p0); if…then {let pInner=Point.create(3,y:4); b=PointBox.create(pInner)} end; b.itemDigest()` → now **97, leak-free**. FIX generalizes OPEN #40's `__mm_retain` co-ownership thesis to the generic-field escape: a TRIVIAL struct/instance type-arg (the `typeArgIsOwned`-false, `typeIsManaged`-true class) is RETAINED at the STATIC constructor feed (`handleEscapingBorrowFeed` → `emitIncref`), so the box holds a real second reference; the box's destructor decrefs it (`genericInstanceFieldIsManaged` now routes a type-param field through `typeIsManaged`, so `Box with Point` gains a `__destruct_<Instance>` co-owned-field decref), and the caller's own drop decrefs the caller's reference — freed EXACTLY once on every path (temp / named-inner / outer / loop-carried), matching the value oracle. The consume boundary (`typeArgIsOwned`) is UNCHANGED, so `create(p); p.getX()` keeps `p` usable. **Supersedes** the P1.7a temporary-materialization (`materializeEscapingBorrowTemp` now serves ONLY the instance-method array-mutator feed, whose element the callee owns). **Scope discipline:** fires ONLY for a trivial struct/instance at a STATIC constructor feed (`ParamConsumeInfo.isInstanceMethod` gates out array/reassign feeds); managed type-args (consumed) + scalars (no heap) UNCHANGED. A SHARED-body reassignment of a co-owned trivial field (`self.saved = next`) cannot drop the old co-owned value without a trivial-struct single-value drop (a later slice) → cleanly REJECTED (E2015, `genericTypeFieldHasCoOwnedTrivialInstantiation`), never leaked. Ports `retain-escaping-borrow.md` (7 cases: cross-scope-temp/named-inner/loop-carried → 97; cross-scope-leak-free; source-stays-usable; trivial-instance-arg-co-owned; the reject). 9 codegen goldens moved (justified: `__mm_incref` + `__destruct_<Instance>`). Gates: native 1465/0, wasm 1344/0 (generic structs absent from wasm's scalar core), -j1==-j12 byte-identical, scale allocsΔ0/bytesΔ0 (generic-gated, corpus is generics-free), exit 0, no leak. x64-windows goldens only; x64-linux/arm64 cross-target goldens OWED (a capable host resyncs, per #86). **MERGED onto `main` `c79e36c12`** (rebased over P1.5 IOCP slice 1a; merged-tree re-verified native **1469/0** + wasm 1344/0 + -j1==-j12 byte-identical + fragments byte-identical; the scale row was re-baselined onto the merged tree — the +82 allocs/+4,952 B vs the pre-rebase numbers is IOCP's unconditional-PE-import constant in `writeExe`, NOT this rung, whose footprint stays 0). ⭐ **Independent review single-sourced the 3-way `genericTypeFieldHas{Managed,CoOwnedTrivial,Uncopyable}Instantiation` scan** (the rung ADDED the third copy) into ONE `anyInstanceTypeArgHasKind` parameterized by a `TypeArgInstantiationKind` enum — code-dedup done; the O(sites×instances) by-base-name-index debt (P1.7 row's Workstream-O note) is unchanged. Review also PROVED the refcount balance by probing under #108 poison (double co-own rc1→3, `Box with (Array with Integer)`, reassign, loop-carried — freed once on every path) and upheld both implementer deviations. ⚠ **The TRUE `E3070` NLL borrow-liveness pass is a SEPARATE, still-open future rung this did NOT build** — a value can't be MUTATED while a borrow into it is live (`arr.get(0); arr.push(x)` reallocating the buffer under a live element borrow), the 3 `disabled-test: E3070` cases in `array-realloc-dangling-ref.md`. This rung fixed the ESCAPE (a borrow outliving its referent, via retain) but not the MUTATE-while-borrowed reject; the `E3070` code stays reserved for that pass. |
| **P1.7b** | `Set` + `Hashable`/`Equatable` (⬆ from P2.3) | ✅ | →1548 | **✅ SLICE A ✅ + SLICE B ✅ — SLICE A = the hash-table CORE (`.create()`, INT scalar keys, x64)** (`375bd078a`, native **1498/0**, wasm 1356/0, `-j1`==`-j12` byte-identical, scale allocsΔ0/bytesΔ+8 [the `RuntimeUsage.usesSet` bool field, one per compile], exit 0, no leak). `Set` is a HAND-SYNTHESIZED compiler BUILTIN exactly like `Array`/`String` (**USER RULING 2026-07-24: synthesize-as-builtin, NOT compile `stdlib/Set.maxon`**): net-new `Runtime/SetRuntime.maxon` of `StdOp` graphs (`__set_{create,insert,contains,remove,count,grow,decref}`), a compiler-known `Set` base in `SignatureIndex` (`isSetInstance`; box = `SetRecordBytes` 40; drop → `__set_decref` via `genericInstanceBoxDropCallee`; the `managedOpaqueArrayElementOf` guard extended so a `Set` gid never hits `baseLayoutOf`), `dispatchSetMethod` in the parser, `usesSet`-gated install (golden-neutral for set-free programs). Open-addressing table ported from v1 `stdlib/Set.maxon`: 3 parallel buffers (elements/states/hashes) + count/capacity, power-of-2 cap + linear probe, `SetSlotState` tombstones, lazy-init 16, grow (double + rehash reusing cached hashes) at 75% load. ⭐ **THE CRUX — the key's `hash()`/`equals()` dispatch through the key type's Hashable/Equatable WITNESS tables (dictionary-passing, reusing 2b-i)**: `LowerMaxonToStd.appendSetWitnessArgs` materializes `rdataAddr(__witness_int.Hashable/Equatable)` at the concrete site + passes them as the two hidden trailing args; the `__set_{insert,contains,remove}` bodies invoke them with the fused `witnessCall` StdOp (`callMem [reg+24]`). x64-only (funcAbs64 reloc), like all witness work. ⚠ **Register pressure**: the buffer pointers / capacity / mask are RELOADED from the record inside the probe loops (not hoisted), keeping the cross-loop live set (`{set, elem, eqWitness, hash}` + loop indices) under the 14-GPR pool — a hoisted version tripped E5001 in the span-less runtime fn. `fourI64Params` promoted ArrayRuntime→MmRuntime (joins its 1/2/3-param family). Ports `specs/set.md: empty-set` verbatim + 10 authored `.create()`-based cases (insert / dup-noop / contains T-F / remove bool+count / remove-then-contains / remove-reinsert / grow-past-16 contains-all / negative keys); x64-gated like the witness specs. **SLICE B ✅ — `Set from [1,2,3]` CONSTRUCTION SYNTAX (int keys, x64)** (native **1517/0**, wasm 1360/0 [set is x64-gated ⇒ 0 wasm set tests before/after], `-j1`==`-j12` byte-identical, scale allocsΔ0/bytesΔ0 — A/B-verified own footprint 0, the corpus is Set-free and the stashed-source base rebuilt on the same bootstrap is byte-identical; the +8 B vs the DFE log row is bootstrap provenance the base equally shows — exit 0, no leak). shv2 had NO `from`-construction (`Array from […]`→E2004); this adds the ONE form the canonical `set.md` needs. Three parts, reusing the EXISTING array-literal machinery: (1) **parser** — `parsePrimary` intercepts a bare `Set` identifier + `TokenKind.from` → `parseSetFromConstruction`, which parses the `[…]` through the EXISTING `parseArrayLiteral` (an owned `Array with <elem>` temp) and reads its inferred element type; `setInstanceForArrayLiteral` maps `Array with integer`→`Set with integer` (int-only gate — a String/managed literal is a clean E2015) and emits `__set_from`; (2) **runtime** — a new hand-built `Runtime/SetRuntime.maxon:__set_from(arr, hashWitness, eqWitness)` StdOp graph that BORROWS the array (reads `buffer@0`/`length@8`) and loops `__set_insert`ing each scalar int (which grows + dedups — a 15-elem literal grows exactly `grow.preserves-elements`), returns the owned set; (3) **witness threading** — `__set_from` joins `isSetWitnessCallee`, and `LowerMaxonToStd.appendSetWitnessArgs` (via new `setWitnessKeyGid`) reads the key type off the RESULT set's gid — its `args[0]` is the source ARRAY, not the set — → `__witness_int.Hashable/Equatable` through 2b-i's `conformerTypeArgName`. ⭐ **Ownership**: the array temp drops (`__arr_decref`) at statement end AFTER `__set_from` borrows it, and the set drops (`__set_decref`) at scope exit via `genericInstanceBoxDropCallee` — leak-free, verified under #108 poison (100× grow-during-from loop + dedup + negatives + from-then-insert/remove all exit-0). Sweep pre-interns `Set with integer` on a `Set from` token (parser-purity: the parser READS the pre-interned instance, never first-writes the shared registry — `internSetIntegerInstance`, the twin of `internArrayIntegerInstance`). Ports the **13 canonical `set.md` `from`-based int cases VERBATIM** (basic.creation/contains-T/contains-F, insert.new/dup/then-contains, remove.existing/nonexistent/then-contains, grow.preserves-elements [grow-during-from], empty.single-element, remove-reinsert, negative-values), RECONCILED with Slice A: the 9 name-colliding authored `.create()` analogues are REPLACED by their canonical `from` twins (9 goldens moved — a different program, justified), the redundant authored `contains.absent` is dropped, and Slice-A's `empty-set` (`.create()`) + the loop-insert grow test (renamed `grow.insert-loop` — a DISTINCT path: grow driven by user `.insert()` calls + a phantom-absence check) are kept. **✅ SLICE C — STRING `Hashable`+`Equatable` CONFORMANCE (x64, the String-keys prerequisite)** (merged-tree native **1522/0**, wasm **1360/0** [String-conformance cases are x64-gated ⇒ 0 wasm], `-j1`==`-j12` byte-identical, scale allocsΔ0/bytesΔ0 every rung [corpus is conformance-free ⇒ `installBuiltinConformanceRuntime` short-circuits at `pendingRdataRelocs==0`; no per-compile field added], exit 0, no leak). `String` conforms to `Hashable`/`Equatable` INTRINSICALLY (no user `implements`) — the managed-concrete twin of 2b-i's `int`. `String.hash` = **djb2** (`h=5381`; `h=h*33+b` per byte reading the record's `buffer@0`/`length@8`; masked `and 0xFFFFFFFF` ONCE at the end — i64 mul/add wrap mod 2^64 and `(x mod 2^64) mod 2^32 == x mod 2^32`, so the end-only mask is the correct low-32 djb2 for ANY length) is a NEW post-pipeline Std graph (`""`→5381, `"abc"`→193485963, `"hi"`→5863446); `String.equals` SHARES `__str_eq`'s byte-compare graph via a NAME-parameterized `buildStrEq` (ONE graph, two `.text` symbols — the operator path `__str_eq` + the witness path `String.equals`, the `buildIntEquals` pattern, no dup/no thin-wrapper). Installed by the UNIFIED `installBuiltinConformanceRuntime` (renamed from `installPrimitiveConformanceRuntime`; file `Runtime/BuiltinConformanceRuntime.maxon`, renamed from `PrimitiveConformanceRuntime.maxon`), each of the 4 impls witness-reloc-gated exactly like 2b-i (table-built ⟺ impl-installed). `String` registered in `isIntrinsicBuiltinConformance` (renamed from `isIntrinsicPrimitiveConformance` — the SHARED predicate/concept renamed "Primitive"→"Builtin" since `String` is MANAGED not primitive; per-type constants kept specific: `IntConformerName`/`StringConformerName`); `conformerTypeArgName`→`primitiveTypeTagName(string)`=`"String"` + `typeConformsTo`'s intrinsic-OR-registry flow through UNCHANGED. ⭐ **THE CRUX — String is the FIRST witness receiver with a NON-TRIVIAL DROP — VERIFIED**: `self.item` and the `equals` `other` arg are BORROWED (the impls read through the pointer, free nothing; dictionary-passing reads `T` opaquely ⇒ no move/drop at the dispatch site; the container drops `item` ONCE via its layout descriptor `destroyFunc@40`). A `Box with String` hashed twice + compared then dropped, three heap-String equals-boxes, and a 100× construct/hash/drop loop are all leak-free + double-free-free under #108 poison (exit 0, no 101, correct djb2). Ports `specs-shv2/string-conformance.md` (5 x64-gated cases: djb2 hash values, equals content, Hashable+Equatable Set-key shape, `==` operator, loop leak-probe). ⚠ **BOOTSTRAP-ORACLE BUG FOUND (→ coordinator, a maxon-sharp bug OUTSIDE this rung): the C# bootstrap MISCOMPILES a managed `T` through a generic witness** — `Box with String where T is Hashable` dispatched through the witness returns identity/address hashes, not djb2 — so the reference djb2 values came from the DIRECT algorithm (`stdlib/helpers/string/hash.maxon`), NOT the bootstrap's generic path. **✅ SLICE D — SET-WITH-STRING (managed-KEY drop, x64) — COMPLETES THE STRING-KEYED SET** (native **1548/0**, wasm **1366/0** [Set/String-key cases are x64-gated ⇒ 0 wasm set tests before/after], `-j1`==`-j12` BYTE-IDENTICAL [same SHA256], scale allocsΔ0/bytesΔ0 EVERY rung [the corpus is Set-free; `SetRecordBytes=48` is a compile-time constant, no per-compile field added], exit 0, no leak). A `Set with String` now OWNS its heap-String keys. String `Hashable`/`Equatable` conformance (Slice C) already threads `key.hash()`/`key.equals()` through `appendSetWitnessArgs` UNCHANGED; this rung adds only per-KEY OWNERSHIP, mirroring `Array with String` EXACTLY — store the element drop fn in the record, read it back at each slot-vacating op. ⭐ **THE MECHANISM:** (1) the Set record grows **40→48 B** — `SetElemDropOffset=40` (the twin of `Array`'s `ArrElementDestroyOffset=40`); (2) `__set_create` gains an **`elemDrop` param** stamped into `elemDrop@40` at birth (`buildArrCreate`'s `elementDestroy` twin); (3) the front end stamps `funcAddr(__str_decref)` at the `.create()` site (`setElementDestroyValue`, the twin of `arrayElementDestroyValue`; int key → 0; a DIRECT code→register `leaRegFunc`, no reloc); (4) **THREE drop sites**, each reading `elemDrop@40` (gated `!=0`, `callIndirect(drop, key)` single-sourced through `ArrayRuntime.emitElementDestructorCall` — the ONE home of that ABI, which the set's stored key destructor shares with the array's element destructor by construction): insert's `dupFound` DROPS the new key the set did not keep; remove's `found` DROPS the stored key (its arg stays the caller's BORROW for the compare); decref's teardown WALKS `[0, capacity)` dropping every `states[i]==Occupied` key (gated on the STATE buffer, not a null pointer — a Deleted slot's key was already dropped by remove); (5) `__set_from` reads the SOURCE ARRAY's `element_destroy@40` for its internal `__set_create` (the array and set share a key type, so no new param/threading) and NULLs each moved-out array slot (mirror `__arr_pop`) so the array's own `__arr_decref` skips it — no double-free; (6) `__set_grow` stays DROP-FREE (it relocates key POINTERS, drops nothing). Parser: `insert` MOVES a managed key in (`moveKeyIntoSet`, the twin of `moveElementIntoArray`, reusing the SHARED `moveManagedValueInto`), `contains`/`remove` BORROW. Both E2015 gates lifted to admit String (`requireSetKeyType`, `setInstanceForArrayLiteral`→`setInstanceForString`); the sweep over-interns `Set with String` on a `Set from` token (parser-purity preserved — READ not first-write). ⭐ **THE CRUX (managed hash tables leak/double-free at the dedup/remove/rehash edges) — VERIFIED under #108 poison, each drop site frees EXACTLY once:** oracle `Set with String` insert 3/dup→3/remove→2 (exit 2); `Set from ["a","b","a","c"]`→3 (dedup + move-out, no double-free with the array's decref); grow past 16 with 20 distinct heap keys (relocation drops nothing); a 300× interleaved create+grow+dup+remove+drop + from-dedup stress loop with interpolated (heap) keys → exit 0, no 101, no 0x3F. Ports **14 authored `set-string.md` x64-gated cases** (create/insert/count, dup-no-leak, contains T/F, remove-drops + remove-nonexistent + remove-then-contains, grow-with-managed-elements, from distinct/dedup/contains, and 3 heap-key leak-probe loops for the dup/remove/decref sites). The 2 int-Set `.create()` goldens moved (`M`, JUSTIFIED — the ONLY diff is `movRegImm32 rcx, 0`, the `elemDrop=0` arg, no logic drift); 14 new String-Set goldens are additions. NO `LowerMaxonToStd` change (the elemDrop is a plain runtime-call arg, not a witness). The `__set_from` SET-slot-stride watch item from Slice B is now EXERCISED with managed elements and reads correctly (String keys are 8-byte pointers = `SetSlotBytes`). **FUTURE SLICES (future rungs, NOT P1.7b — kept named): `Character` conformance (needs the `Character` heap type to exist first); direct concrete `s.hash()` on a String value (v1's statically-known-type→direct-call, E2015 today); float/bool conformance; `Set with ByteArray` per-gid witness thunks → P2.2; iteration / `for-in` over a Set → needs P1.8; `Map` → P2.3. NOTES (out of rung, → coordinator): (1) E5001 on a span-less runtime fn PANICS the register-pressure DIAGNOSTIC (`RegisterPressureDiagnostic.maxon` classifies param 0 as "not a parameter" for a `returnLine:0` fn) — a pre-existing latent SHARED-allocator bug BOTH Set slices AVOID by staying under pressure (Slice B's `__set_from` loop keeps `{set, hashWitness, eqWitness, count, buffer, i}` = 6 GPRs live, well under the 14-GPR pool), neither fixes; a candidate compiler-robustness future rung. (2) ✅ **HELPER-RENAME DEBT — CLOSED BY D11 (2026-07-31), and this row was STALE THREE WAYS, which is why it is rewritten rather than ticked.** It named `requireArrayVoidStatement` (renamed by D11 to `requireVoidMethodIsStatement`), `parseArrayNoArgs` (**gone** — it is `consumeEmptyArgList`), and called the refusal **E3059** when it is **E2004** (E3059 is the `try` door). ⚠ And the row read the debt as *legibility*, which is what let it sit: the Array-prefixed name was the visible half of a real defect — the helper tested `resultUsed` and NOTHING about the method name, so its only scope was where it sat, and an unknown name shared that scope. See the **D11** row.** |
| **P1.8** | `String` methods · `for-in` | ✅ | →2178 | **SLICED A/B/C/D — the rung is FIVE mechanisms and the survey said so: counted loops, Array iteration, String grapheme iteration (needs a `Character` type that does not exist), ~30 String methods, and Workstream R2 (UCD tables + `CharacterSet`). The dependency chain fixes the order: `trim()` → `CharacterSet` → `Set with Character` → `Character` → UAX#29.** ✅ **SLICE A — `for` LOOPS (numeric ranges + Array iteration) COMPLETE 2026-07-28** (x64-windows **1851→1906/0**, x64-linux **1800/0**, wasm32-wasi **1709/0**, build 0 warnings, fragments additions-only, no 101; ⚠ **arm64-macos/arm64-linux SKIPPED — UNVERIFIED**, cancelled by the user and owed to a hand sync). ONE five-block lowering (`preheader → forhdr → body → forstep → forexit`) serves both source forms; the **STEP block is the one `while` does not have**, and `LoopContext` gained the `continueTargetId` its own comment had predicted (`while` sets it to its header ⇒ ONE rule, no construct to branch on). Range bounds are evaluated ONCE in the preheader; `to` inclusive / `upto` exclusive / **descending is EMPTY** (oracle-measured: `10 to 1` → 0 trips, `3 to 3` → 1). The Array form is the **bootstrap's index counter, NOT v1's heap cursor** — `__arr_count` re-read LIVE in the header IS the bounds check, which is what licenses the new non-throwing `__arr_get_unchecked` (`__arr_get` is declared throwing ⇒ a bare call is E3057 by design, and its NULL-slot arm would silently TRUNCATE the iteration). **v1's cursor was left on the floor with its two documented bugs** (a dangling buffer snapshot; a double-retain leak that leaked *every* `for x in <managed collection>`). ⭐ **THE OWNERSHIP RULING (coordinator, user-ratified): the element is a BORROW**, through the existing `emitArrayElementAccessor(owned: false)` door — the references DISAGREE here (v1 borrows via `markBorrowSlot`, the bootstrap RETAINS via an incref in `MaxonManagedMemGetOp`) and the corpus does not settle it (`rc-for-in-elem-decrefed` pins only `60`, which both models satisfy). Borrowing keeps ONE rule for the one door that reads an array element, and costs zero refcount traffic per iteration. ⚠ **Borrow's safety obligation is paid by a LOCK, not by a comment**: the iterated storage is immutable for the body's duration, reusing **E3019** (the oracle's own code for it — no new diagnostic). **Ports `specs/ranges.md` BYTE-IDENTICAL** (14 counted-loop cases enabled; 8 disabled naming `Character` or a first-class `Range` value) and flips 21 of the corpus's 38 `P1.8 for-in` markers + 3 `empty-block` siblings; **12 stale markers re-marked** (String/view iteration → B/C, `for (k,v) in m` → D). ⭐ **A case that PASSES is not a case you may enable**: `ranges.character-range` passes and stays disabled — it only COUNTS iterations, so 26 codepoints count like 26 characters, while its twin `character-range-print` (which looks at the value) does not. **THREE REACHABLE DEFECTS FOUND AND FIXED IN-RUNG, none deferred:** (1) the range form locked its START bound's variable but not its END — `for i in n upto 3` with `n = n + 1` was E2013 where the oracle returns **3**, one range with two rules; the lock now lives in the array arm ALONE, because a range holds no borrow to protect. (2) ⭐⭐ **A FIFTH MUTATION DOOR, SEGFAULTING ON ALL FOUR SPELLINGS** — `emitCheckedSelfFieldStore` asked `layout.fieldIsMutable` where its sibling asked `iterationLock + layout.fieldIsMutable`: ONE question about one field, TWO derivations, and only one grew the lock clause (`0xC0000005`). The root cause ran deeper than a missing check — the lock's key came from the **SOURCE's** spelling and every door's from the **WRITE's**, and `baseBindingName` answers `SelfParamName` for `self.items` but the field's alias for `items`, so **2 of 8 cells were silently accepted even where a door DID ask**. Cured by `iterationSubjectNameAt`, the ONE derivation both sides read, pinned by an 8-cell matrix. (3) an unguarded range DOMAIN: `for x in 1.0 to 3.0` panicked in `X64Backend`, and `for b in false to true` ran two trips **incrementing a bool** — now a clean E2015 that says why. ⭐ **SABOTAGE FOUND THE REAL HOLE:** forcing the lock to "not locked" turned **ONE** test red out of 1891 — the mechanism standing between the borrow model and a UAF was covered by a single case. Now **11**. (E3082 forced false → 5 red; array bound `<`→`<=` → 13 red, four segfaults.) **OPTIMIZER: nothing superlinear, and it built the ladder to prove it** — ⛔ `ScaleCorpus` generates ZERO `for` loops (it could not; they did not parse), so a Δ0 from it would be **the instrument's blind spot, not the cost**; `maxon-shv2/Testing/ladders/genfor.sh` is committed, with knobs whose DEPTH moves independently of size. Loop count ×2.00 allocations / ×2.00 CPU per doubling; **the no-loop CONTROL is the reading that mattered** (six phases identical TO THE DIGIT at all five rungs ⇒ the four lock doors impose NO per-access tax on programs that never loop). Footprint **+142 allocations FLAT** at all six rungs (+108 building `__arr_get_unchecked`'s Std graph, +28 isel) — the implementer's "+156" was +142 real plus a **+14 path-length artifact**, and the committed log row was NOT a valid baseline in a 28-char worktree. `bindingIsIterationLocked`'s O(depth) scan cannot go quadratic on anything that COMPILES: every `for` mints an index phi live across every nested loop, so **E5001 caps depth near 10** (measured: 10 compiles, 11 does not). ⚠⚠ **THE `phase:signatures` +8.6% CPU READING WAS A MEASUREMENT ARTIFACT, AND IT TOOK A THIRD MEASUREMENT TO SEE IT — USER HYPOTHESIS, 2026-07-28: *"some of the measurements were done on a different machine"*.** The optimizer recorded it unattributed; the review then declared it *"real work, not layout"* because it was **CONSISTENTLY POSITIVE at every rung** while its control `phase:lex` (identical source) ran −2.4…−3.1%. **Re-tested on an IDLE box with the binaries swapped at ONE canonical path and the runs INTERLEAVED P/H/P/H** (4 pairs, both phases read from the SAME run's table): `phase:signatures` collapses to **+0.94% mean and CHANGES SIGN across pairs** (−3.43 +0.68 +3.94 +3.74), while `phase:lex` **reproduces** (−1.69% mean). ⇒ the consistency came from all head runs sitting in ONE TIME WINDOW and all parent runs in another, not from the code. ⭐ **THE TRANSFERABLE LESSON: a stable SIGN across rungs is not evidence of a real effect unless the runs were INTERLEAVED** — and `phase:lex` could not catch this because it excludes only a UNIFORM factor, whereas a time-window drift moves different phases by different amounts (lex reproduced, signatures did not, which is precisely what disqualifies one as a control for the other). ✅ **The BYTE finding is UNAFFECTED and still true** (allocation count identical to the digit, bytes +768…+9,048 — the records that already existed got bigger); only the CPU story built on top of it failed. The claimed "per-function CPU attribution" instrument gap is **not** established by this reading. ⚠ **DEBT THIS RUNG LEAVES NAMED:** the write-door list is a **hand-maintained COMMENT** and has now been wrong TWICE, each time found by a segfault — the compiler cannot state its own set of write doors, so the 8-cell matrix is a CHECK standing in for a DERIVE (the cure ladder's middle rung, not its top). ⬜ **ALL SIX SLICES COMPLETE — A · B · B2 · C · D · E — P1.8 IS CLOSED 2026-07-28** (`716cc8bd4`; suite **1851 → 2318/0** across the phase, x64-windows / x64-linux / wasm32-wasi all PASS, ⚠ **arm64 SKIP — remote, UNVERIFIED, and the phase changed codegen so the arm64 goldens are OWED a mint at the next periodic sync**). ⭐ **ZERO `disabled-test:` markers still blame P1.8**: the eleven that did were re-attributed to their MEASURED blocker when the rung closed (5 first-class `Range`/iterator + 2 `Map` destructuring → the TUPLES rung; 2 character ranges → its own rung; 2 E3005 instance-name rendering → its own), which is the discipline the P1.7 sweep established. Remaining P1.8 mentions in the corpus are CONTEXTUAL history (*"NOT a `findLast` gap: Slice B2 shipped `findLast`"*), not blockers.** ⚠⚠ **SLICE D'S SCOPE AS WRITTEN HERE WAS WRONG IN BOTH DIRECTIONS, AND THE CORPUS IS WHAT SAID SO** (coordinator survey, 2026-07-28). This row had D as *"R2 + `CharacterSet` + `trim()`, and `Map` + `(k,v)` destructuring"*. Measured against the 56 live corpus cases still blaming P1.8: **`Map` is 2 of them**, and **45 are String views/methods no slice claimed at all**. Two things therefore moved OFF D, each for a reason that is a slicing criterion and not a preference: (1) **`Map` + `(k,v)` → its own rung, WITH TUPLES** — `Map.current()` returns `Entry = (Key, Value)`, a genuine tuple, and shv2 has **ZERO** tuple support; tuples are first-class with their own ABI (`specs/tuples.md` 18 cases, `specs/map.md` 34, +7 companions) ⇒ a hard dependency on a distinct feature needing its own contract. (2) **the 45 String-surface cases → SLICE E** — they share `parseStringMethodCall` and `StringRuntime.maxon` with `trim`, so they could not be a concurrent agent (exclusive-file-list collision). ⇒ **D ran as ONE chain with ONE implementer** (R2 → `Set with Character` → `CharacterSet` → the three trims), which is what a hard internal dependency plus a shared file leaves you: a 2-agent wave would have collided on `LowerMaxonToStd.maxon` and stalled on R2's contract. **E** = the String view/method leftovers: `.utf16()` + the nine `utf16*` free functions (13 cases) · byte/codepoint view ITERATION + `.toByteArray()` + `String.from(ByteArray)` (16) · `.isEmpty()`/`.clone()`/`.replaceFirst()`/implicit-append (10) · String-source cursor iteration (6). |
| **P1.9** | ranged typealiases | ✅ | →2372 | **CLOSED 2026-07-29.** *(This row read `→2352` until it was re-read on the MERGED tree, 2026-07-29: `c077feeaa`'s 29 new programs landed after the number was written. ⚠ The re-read's FIRST attempt reported `2319 passed, 53 failed` — every one a golden-fragment mismatch, every one gone at 2372/0 after `build csharp` then `build shv2`. The on-disk binary is gitignored and nothing rebuilds it; a suite count is a claim about a BINARY, not about a tree.)* #86 measured FALSE in both halves — x64-linux PASSES (the row cited a 2026-07-25 `1225 passed, 288 failed`), and an unverified remote arm64 lane is a reported SKIP, never a residual. The residual that was real was recorded in no row: the DECLARATION-site half (E3062 · E3005-at-declaration · E2003 bare-sized · alias visibility + stdlib carve-out · E3005 on an `otherwise` value). See the detail row. ⚠ arm64 goldens owed to the periodic sync — this rung moved codegen |
| 🚩 | **PHASE 1 GATE** — differential oracle | ⬜ | — | `maxon.exe`- vs `maxon-shv2.exe`-built harness identical + `-j1==-jN` + clean mm-trace. ⚠⚠ **EVERY RUNG ABOVE IS ✅ AND THIS ROW IS NOT — they are different claims, and this is the one that means "shv2 runs its own tests".** MEASURED 2026-07-29 (below): the extracted runner builds clean with `maxon.exe` and gives shv2 **13 files / 13 `E2015 Unsupported`** — a LOWER bound (one error per file). Dominant blocker is NOT a ladder rung but the **stdlib cone** (§"THE DOGFOOD'S REAL BLOCKER"): **3 of 48** files whitelisted, so `FilePath` / `File` / `Directory` / `Subprocess` / `Executable` / `StringArray` are all undefined to shv2 |

### Phase 2 — self-host

| # | Mechanism | Status | Notes |
|---|---|:--:|---|
| ~~P2.1~~ | interfaces + witness tables | ⬆ | promoted to **P1.7a** |
| **P2.2** | conditional conformance | ⬜ | per-gid witness tables + thunks — the last big generics piece |
| **P2.3** | `Map` (`Set` ⬆ to P1.7b) | ⬜ | multi-param generics; genuinely unreached in Phase 1 |
| **P2.4** | `extension` | ⬜ | — |
| **P2.5** | closure dogfood (`LazyMessage`) | ⬜ | acceptance test for P1.5 |
| **P2.6** | per-function fan-out + parallel frontend | ⬜ | 1-core-vs-N-core byte identity gate |
| 🚩 | **PHASE 2 GATE** — self-host | ⬜ | 3-stage fixpoint: stage-2 == stage-3, byte-identical |

### Workstreams — cross-cutting, land in slices with their rung

| Slice | What | Status | Lands at |
|---|---|:--:|---|
| O1 | `__DebugStream` builtin + monitor decode | ✅ | before P1.0a (= P1.0o) |
| O2 | `gt`/`p_id`/`unit_id` demux in the monitor | ✅ | with P1.0a |
| O3 | shv2's own backend emits the builtin (survives self-host) | ⬜ | obligation on the R1 port |
| O4 | per-phase **CPU-TIME** column — the instrument that can see a cost which ALLOCATES NOTHING | ✅ | 2026-07-25; spans `maxon-sharp` (the intrinsic) + shv2 (`PhaseProbe`, `--metrics`, `scale-test`) |
| R1 | allocator + refcount runtime (core) | ✅ | P1.0r |
| R2 | UCD table access for real `String` methods | ✅ | P1.8 Slice D, 2026-07-28 (`bd3b0aee7`). ⭐ **ITS TRUE SIZE WAS ONE PROPERTY, ONE FUNCTION, ONE CALL SITE** — `General_Category`, via `unicodeGeneralCategory`, reached only from `CharacterSet.contains`'s `categoryMask != 0` branch. Grapheme segmentation and `toLower`/`toUpper` are hand-coded range checks touching **zero** UCD bytes, so R2 was never as broad as "real String methods" implied. shv2 route = **v1's** (the blob is an ordinary `.rdata` constant), NOT the bootstrap's dedicated `ucddata` section: shv2 emits for four object formats and a dedicated section is four section-table edits for no semantic gain. Measured right — `rdataAddr` already works on PE and on the wasm component with no section work |
| R3 | GT scheduler + async substrate | ✅ | single-M SUBSTRATE complete 2026-07-23 — every listed residual closed (#87/#92 reclamation B1c · #88 drop-reclaim+cancel B2 · #89 await-block-arg · #93 runProcess-throws Design A). ⚠ its CONSUMER — the parallel-harness dogfood (StreamingSubprocess + spec-runner fan-out, P1.5's acceptance test) — and multi-M (B1b) are separate later slices |
| S | `/specs` ratchet — an enabled test may never be re-disabled | ⏩ ongoing | continuous, every rung |

---

## 🧭 THE SLICE BOARD — the Phase 1 gate's remaining work, claimed by PUSHING THIS FILE

**Opened 2026-07-29.** Everything below is what stands between `main` and the 🚩 PHASE 1 GATE. It is a
**board, not a list**: an agent takes a row by editing it here, committing **this file only**, and
pushing — and **`git push` is the lock**. There is no other registry, no lockfile, and no coordinator
process. See `.claude/skills/rung/SKILL.md` §0a for the protocol an agent runs; this section is the
DATA it operates on, and the two must be read together.

> ### ⚠ WHY A BOARD AT ALL — a near-miss on the day it was opened
> While this section was being written, `main` advanced **5 commits** (`886f238c3`..`89a964f85`, one of
> them a PLAN.md edit) under an agent holding **107 uncommitted lines of PLAN.md**. It survived only
> because the two edits happened to touch different rows. **Nothing detected the overlap; nothing
> would have.** A claim that is not PUSHED is not a claim — it is a private intention that the next
> `git pull` silently arbitrates.

### The LANES — the real unit of exclusion, because most of this work lives in ONE file

**Slice independence here is NOT rung independence.** Six of the eight rows below are refusals raised
inside **`Compiler/Parser.maxon` (28,152 lines)**, so "independent" has to mean *"cannot collide"*,
which is a claim about FILES, not about mechanisms. A lane is a set of rows that would fight over the
same code. ⇒ **AT MOST ONE 🔶 CLAIM PER LANE.** That is the concurrency limit, and it is not advisory:
a second claim in a live lane is the failure this board exists to prevent.

| Lane | Owns | Rows |
|---|---|---|
| **L-harness** | `Testing/SpecWorkerPool.maxon` · `Main.maxon` — **no compiler source at all** | G1 |
| **L-stdlib** | `StdlibLoader.maxon` · `StdlibSource.maxon` · `Parser.maxon`'s builtin-call table | S1 → S2 |
| **L-parser-decl** | `Parser.maxon` declaration / type-body path (`:7922`) + its lowering | D1 |
| **L-parser-postfix** | `Parser.maxon` postfix + member-resolution walk (`:10877` · `:23893` · `:24523`) | D2 |
| **L-parser-stmt** | `Parser.maxon` statement dispatch (`:9317`) + `Lexer.maxon` directives | D5 |
| **L-types** | `TypeResolution.maxon` · `SignatureIndex.maxon` | D4 |
| **L-gt-runtime** | `Targets/X64/X64GtRuntime.maxon` · the `*Runtime.maxon` graph files — **no parser source at all** | 7a |
| **L-parser-closure** | `Parser.maxon` closure CAPTURE + env build/read (`buildClosureEnv` · `emitCaptureRead` · `captureSlotFor`) | W1b |

⚠ **Three lanes still share `Parser.maxon`** (decl / postfix / stmt). Their sites are thousands of lines
apart so they rebase, but **they are not free of each other**: whoever lands second replays onto a
`Parser.maxon` the first one changed and **must re-run its own gate battery on the merged tree** (skill
step 11 already demands this when `main` moved — this is that case, every time). ⇒ **Recommended
in-flight: ≤4**, matching the skill's own *"beyond ~4–5 agents integration dominates"*.

### ⭐ THE BATCHES — claim a BATCH, not a row

**Rewritten 2026-07-31, because the board was being worked one row at a time and that is the slow part.**
Measured over nine consecutive rungs: the per-rung *fixed* cost — claim, survey, plan, worktree, three
suite lanes, the ladder read, an independent review, rebase, merge, row, memory — dominated the actual
fix, and **integration friction (two landing races, three conflicted rebases, a stale-binary false red)
consumed more wall-clock than every review combined while finding nothing.** Twenty-two rows were filed
against thirteen closed the same day. A board of singletons *manufactures* that overhead.

**A BATCH is the unit of claiming, branching, reviewing and merging.** One worktree, one branch, one
review, one merge, one row-closure pass for the whole batch.

| Rule | |
|---|---|
| **Claim the BATCH row, not its members** | Same §0a protocol: edit this file only, push, and the push is the lock. Members inherit the batch's status. |
| **A batch claims EVERY lane it lists** | Stricter than one-row-one-lane, and it is what makes a batch safe. Two batches sharing a lane cannot be live at once. |
| **One gate, paid once** | B1 is eight bootstrap defects behind ONE C# suite run plus ONE codegen-neutrality A/B. Paying that per row is seven wasted runs. |
| **One thesis, so a review has one thing to attack** | Members share a mechanism or a file, so the reviewer forms a single argument instead of eight. |
| **A member may be dropped mid-batch** | If a member turns out not to share the thesis, release it back to `⬜ FREE` in the closure and say why. Do NOT stretch the batch to keep it. |
| ⛔ **Do not file a new singleton row for hygiene** | Fix it in the batch that found it, or add it to an existing batch. A row is a commitment someone must read and dispose of; today's board grew by nine net because that bar was too low. |

| Batch | Members | Lanes it claims | Why together | Status | Owner (branch) | Claimed (UTC) |
|---|---|---|---|:--:|---|---|
| **BATCH1** | ~~`A2d`~~ `A2e` `A2h` `D11d` `A1f-e9001` `A1k` `A1m` `A1n` `A2r` **`A1s-csgolden`** | **L-bootstrap** | **Eight `maxon-sharp` defects behind ONE gate.** Every one owes the full C# suite (3188+) *and* a codegen-neutrality proof against the pre-rung source — and `git status --short specs/` is *already* dirty from 139 pre-existing stale fragments, so that proof is an A/B, not a glance. Paying it once is most of this batch's value. ⚠ Two are serious on their own: `A1n` (`--target=x64-linux` is advertised and writes a **Windows PE**) and `A1k` (all 3188 spec compiles run with debug info OFF, so the debug path is untested). ⚠ `A2d` needs the **namespace ruling** first — if it blocks, drop it and land the other seven. ⚠ **`A2r` ADDED 2026-08-01** by BATCH2, which tripped over it at its own baseline: a gitignored `.scale-tmp/` left by `run_scale_test --perType` fails `flat-namespace check` and reddens `dotnet build` on a clean `main`. Filed into this batch rather than as a hygiene singleton, per this section's own rule. ⭐ **`A1s-csgolden` ADDED AT CLAIM TIME 2026-08-01, and it is THIS BATCH'S OWN GATE**: the row measures a full C# `spec-test` silently REWRITING 141 committed fragments while reporting all-passed, so *"the C# suite plus codegen neutrality"* — the one thing every member here is gated on — is today a gate that cannot fail. Same lane (`maxon-sharp/Testing/`). It lands FIRST; the other members are proven against a gate that checks. ⚠ **`A2d` DROPPED AT CLAIM TIME**, per this batch's own note and the section's drop rule: the user ruling turned it from *"emit a diagnostic"* into *"a NAMESPACE mechanism, plus spec tests for it"*, which is a language feature with its own contract and spec-port list — not a member sharing this batch's thesis. Released to `⬜ FREE`. ✅✅ **CLOSED 2026-08-01** (main `d4cd6aa20`; C# **3198 → 3229/0** with `Fragment goldens: 3229 verified, 0 written` and `git status --short specs/` EMPTY — the first C#-suite number in this repo's history that a golden gate actually stands behind; shv2 **3198/0**; `check-debug-goldens` **5 failures → 3**, all 3 pre-existing; 0 warnings; `error-codes: OK`; `flat-namespace: OK`; `stdlib-target-selftest: OK`; no exit 101; arm64 **SKIP — remote, UNVERIFIED**). ⭐⭐⭐ **THE BATCH'S OWN GATE WAS THE FIRST MEMBER, AND IT CHANGED WHAT EVERY OTHER MEMBER'S NUMBER MEANS**: a full C# `spec-test` REWROTE 141 committed goldens and still reported all-passed, so every fragment number the suite had ever produced was unfalsifiable. ⭐⭐ **AND THE 141 WERE NOT WHAT THE ROW SAID.** Filed as "register-allocation movement"; **measured** as the x64 half of a released-garbage-pointer fix that landed on arm64's goldens 3 days earlier and never on x64's — `x64.call mm_decref` **279 → 107** (−172 spurious releases), `x64.xor` 260 → 82, every other call target identical. **A real correctness improvement was invisible for three days because the gate that should have shown it was busy overwriting itself.** ⛔⛔ **EVERY ROW THAT GOT MEASURED WAS WRONG IN SOME WAY** — this is the batch's finding, not an aside: `A1f-e9001`'s premise was false (the `continue` is irrelevant; the trigger is **statement position**, and a `Box.show()` that only prints fails identically), it cited **two identifiers that do not exist in the repo** (`materializedParamSites`, and my own corrected guess `_refParamPtrVars`), and the coordinator's *replacement* hypothesis (by-ref/mutating receiver) was **also** wrong — three successive wrong causes for one defect, each refuted by the next measurement. `A2h` and `D11d` both cited stale line numbers (`:19163`→`:19252`, `:9374`→`:9608`). `A2e`'s residual is **latent, not live** (probed three ways, no reachable wrong answer ⇒ **no spec case written**, deliberately). `A1n` was filed as one spelling and was a **class**: `arm64-windows` silently wrote a **Mach-O named `.exe`**, and an unknown OS was an **unhandled `ArgumentException` with a stack trace**, not a diagnostic. ⭐⭐ **A1m WAS REPRODUCED FOR THE FIRST TIME IN ITS LIFE** — filed "confirmed by reading, explicitly NOT reproduced". Instrumenting the mint gave parse-order vs worker-order tables: `Array.resize` re-mints as `__stdlib_panic_msg_98`, which on the parse thread is `utf16.maxon:59` ⇒ **`panic at utf16.maxon:59: value outside typealias 'CodeUnit16'` … `in __Array_i64.resize`**, a panic reporting a UTF-16 range check the program never went near. ⭐⭐ **A1k MEASURED THE HOLE RATHER THAN ASSUMING IT**: with the OOB fixed, **3219/3219 pass with debug info ON and 3219 goldens verify, 0 written** — so the crash was the ONLY defect on a path the suite had executed **zero** times, and codegen neutrality is now measured over 3219 programs instead of asserted. ⭐⭐ **THE INDEPENDENT REVIEW FOUND THREE BLOCKERS THE SEVEN IMPLEMENTERS DID NOT**, one of them a wrong answer **no gate can see**: `StoreForwardingPass.cs:55` replaces a store with a NEW op object while the debug side-table is keyed by op REFERENCE ⇒ dropped spans in two files the 53/53 gate covers, green before and after. Also: `A1n`'s refusal had **four doors, not two** (`TestCommand.cs`, `Mcp/Tools/DebugBuild.cs` reached a writer with an unchecked host default), `A1m`'s claim was true but **unenforced** (`LowerPanic` silently dropped a second message under an existing label; now refuses, sabotage-proven), and **five rules were written more than once — two had already lost a clause**. ⚠⚠ **I OVERTURNED ONE REVIEW FINDING BY MEASURING IT**: it proposed deleting `specs/advent.md`'s four `RequiredIR:x64-linux` blocks as unselectable, but that file is **`status: selfhosted`** and **`maxon-selfhosted` HAS an ELF writer** — A1n withdrew x64-linux from the **BOOTSTRAP** only. Deleting them would have destroyed the only goldens that spec's own compiler can use. Only the census **comment** was false, and only that was fixed. ⭐ **A sabotage probe found those 4 `advent` fragments are ORPHANS** — appending junk to `day1.test` left the suite `3229 verified, 0 written`, exit 0: `status: selfhosted` means the bootstrap skips them and v1 has not built since 2026-07-13 ⇒ new row **`A3a`**. ⭐ **Fixed a gate RED on `main` since 2026-07-28, outside every row**: `check-debug-goldens.sh` — main's compiler rejected main's own debug sample (`Counter = int(0 to 1000)` vs the sample's own `label(4444)`; `7c05a6996` began enforcing bounds at a call). Kept to ONE line deliberately: the gate pins breakpoints by LINE NUMBER, so a multi-line comment drifts the golden while still reporting PASS **on the wrong statement**. ⚠⚠ **TWO RED GATES AT THE END WERE PROVEN PRE-EXISTING BY A CONTROL, NOT BY ARGUMENT** — `origin/main` with **none of BATCH1** was built and run in a detached worktree: wasm32-wasi fails there identically (**the H1 wedge — 13 dispatched, 1 collected, 198 queued, 300 s no progress; the harness's own panic says *"This is a HARNESS bug, not a test failure: RETRY"*** — B2's live lane), and `check-debug-goldens` fails there with **5** where BATCH1 has **3**. ⇒ **BATCH1 FIXES TWO AND INTRODUCES NONE.** The 3 remaining are `asyncbp`/`gtcontrol`/`threads`, offset-only drift (`0xf9b2`→`0xfbef`) from **today's** `d7a7c96c3` netpoll fix which never re-minted them — **`L-gt-runtime`, B2's lane, not mine to touch** ⇒ new row **`A3b`**. ⚠ A contended machine faked a red suite for **four** separate agents (`3098/116`, `3107/92`, `3195/4`, 16–23 timeouts) — every failure `process did not exit before its timeout`, a disjoint set each run, all green on re-run; **the authoritative battery was run last, alone, on an idle box** | ✅ DONE | `slice/BATCH1-bootstrap-defects` | 2026-08-01T14:29Z |
| **BATCH2** | `S2` `R4.5` `D6` | **L-stdlib** · **L-parser-decl** · **L-parser-postfix** *(added at slice-6 claim time: the `.managed`-as-a-value cure lives in `dispatchArrayMethod`'s `managedField` arm, which is the POSTFIX walk and not the decl path the row's own lane cell listed — claimed rather than worked in silently. The lane is free: BATCH7 ✅)* | ⭐⭐ **THE GATE'S LONG POLE, and the only batch that moves `stdlib cone N of 48`.** Everything else on this board makes the compiler more truthful; this is what makes Phase 1 real. It is also the **spec-portable** kind — the reference file is named in each row and `/specs` has the cases — so it deserves the *least* ceremony: skip the survey, batch several whitelist entries per pass, let the suite be the gate, and reserve the independent review for a mechanism change rather than a whitelist line. ⚠ `S2`'s blocker list has been measured wrong twice; **re-measure before trusting it**. ⚠ `D6` is ◑ and is what actually blocks `int.fromString`. ◑ **SLICE 1 LANDED 2026-08-01** (main `d7553412e`): `stdlib/URL.maxon` + its helper, the cone's self-contained ROOT, whitelisted; `specs/url.md` 42/42. ⚠⚠ **THE BATCH AS FILED IS NOT A RUNG AND THAT IS NOW MEASURED**: the members form a three-file chain **URL → FilePath → File** plus a LANGUAGE FEATURE (default parameter values, which shv2 refuses by name) — so it is a workstream, taken one link at a time. Slice 2 is `FilePath.maxon`; its measured remaining blockers are `String.hash()` and `String.init(<buffer>)`. `D6` remains ◑ and blocked on A1 ◑ **SLICE 2 LANDED 2026-08-01** (main `3cdf0bdd5`): the `s.hash()`/`s.equals(t)` direct-call doors (closing `D13`) plus a PRE-EXISTING SILENT WRONG ANSWER — `String`/`Character` were never reserved, so a user declaration REPLACED the compiler's conformance impls (`Set with String` stored a duplicate key; a `Hashable` witness ran the user's body). **FilePath was NOT reached**: its sole remaining blocker is `self` as a value at two sites ⇒ **S5**, which slice 3 must take first ✅ **S5 LANDED 2026-08-01** (`bf43f7d2f`). ◑ **SLICE 3 LANDED 2026-08-01** (main `9264ed9c3`): **`stdlib/FilePath.maxon` WHITELISTED and `specs/filepath.md` PORTED BYTE-IDENTICALLY — 40/40 green, 0 disabled** (hash equal, coordinator-verified); x64-windows 3102 → **3157/0** on the merged tree, x64-linux 3030/0, wasm 2981/0; **zero golden churn** (the row predicted `.rdata` churn — nothing reaches FilePath in a program that does not name it). ⭐⭐ **THE SLICE'S PREMISE WAS ONE MECHANISM SHORT, AND THE STANDALONE-COMPILE CHECK STRUCTURALLY COULD NOT SEE IT**: `build stdlib/FilePath.maxon` giving the clean `E3001` says nothing about the CALL syntax, and **31 of the 40 cases spell `FilePath from "…"`** — the `InitableFromStringLiteral` sugar, which shv2 did not implement. Built rather than disabling 31 cases, on this file's own routing of `directory`/`file-io`/`file-info` through R4.5 for that exact construct. ⇒ **a whitelist rung's real content is the CALL SITES the corpus uses, not the module's own parse.** ⛔⛔ **AND A GATE THAT WAS NOT GATING**: the runner pushed a case only `if haveExitCode`, so **a spec pinning only STDOUT was silently DROPPED** — it said *"no tests selected"*. Fixed and sabotage-verified. ⭐ **The independently re-derived sweep is the payoff: 2 dropped here, and 48 MORE across 13 upstream `specs/` files** (`match-dispatch` 17, `clock` 8, `match-jump-table` 7, …) **were latent drops waiting for their port — this unblocks all 48.** ⛔ The review also found **a wrong answer the slice opened**: widening `==` to a struct's own `equals` matched BY NAME, so `a != b` with an `equals` returning `String` compiled and ran `logicalNot` on a heap pointer stamped `boolean` — silently always true. The WITNESS form already refused exactly that and carried the measurement; the DIRECT form never asked. ⚠ **THE BATCH REMAINS ◑ — `R4.5` IS NOT DONE**: `stdlib/File.maxon` + `FileInfo` still need **DEFAULT PARAMETER VALUES**, which shv2 refuses by name — a LANGUAGE FEATURE with `specs/default-values.md` as ready-made acceptance and its own rung, deliberately not started here. ⇒ next slice is that feature, then File. ⇒ opened **`A2x`** (measured-linear debt: `resolveOverloadedCalls` walks via `IrModule.liveOpIndices`, which allocates per function — ≈225·2^r, ~0.045% at rung 5) and **`A2y`** (the `from` sugar's two composition gaps: a global `let` initializer the ORACLE compiles, and a false *"Undefined variable"* message at a non-literal). ◑ **SLICE 4 LANDED 2026-08-01** (main `61b168cdf`): **DEFAULT PARAMETER VALUES**, x64-windows 3157 → **3176/0**, x64-linux 3055/0, wasm 3006/0, zero golden `M`. ⭐⭐ **A PARAMETER DEFAULT IS A SYNTHESIZED NULLARY FUNCTION** — `name String = "World"` declares `__paramDefault#greet#0` returning `"World"`, and a call that omits the argument calls it. **v1's design taken and the bootstrap's REJECTED for an architectural reason**: the bootstrap re-parses the default's TOKENS at each call site, but a shv2 parse is a pure function of ONE file's `(tokens, filePath, namespace)` — the property that makes the per-file fan-out possible — so another file's tokens are unreachable. ⭐ **The fill happens in the PARSER, not lowering**, because shv2 decides drops in the parser and a value materialized at lowering has no scope to be dropped from; v1 can materialize late only because it has a refcount inserter shv2 deliberately lacks. ⇒ `FuncSignature` gains no column and `slotCallArgs`'s arity gate is untouched. ⛔⛔ **THE IMPLEMENTER FOUND A SEGFAULT IN ITS OWN CHANGE**: teaching `skipParamType` to stop at `=` silently TRUNCATED three other peek-only walks that step by the end of the TYPE — **a short parameter list is a well-formed one**, so it lost a per-parameter OWNERSHIP fact and `create(tag Integer = 1, item T)` gave **0xC0000005** where the undefaulted spelling gives exit 6. Cured by making the SPAN and the STEP two questions. ⛔⛔ **AND THE REVIEW FOUND A COMPILER CRASH**: a defaulted name declared TWICE panicked with no diagnostic and no position (the registry is last-wins while E3006 is only reported after every file parses, so the loser's drain read the winner's shape). Now the FOURTH positioned refusal. ⭐⭐ **AND NONE OF THE THREE DECLARED REFUSALS HAD A SPEC TEST — “a refusal nothing runs is a claim, not a door”**; all four now pinned, each seen RED first. ⚠ **9 of 14 cases enabled**, and the review RE-DERIVED each of the 5 disabled with the default REMOVED: every one is blocked by something else (an `[…]` int literal never unifies with `Array with Integer` ×2, `.rawValue` on an enum value, float interpolation, `ByteArray`/`Byte` not spellable). **The enum DEFAULT itself works** — `paint(color Color = Color.blue)` with a `match` body runs. ⚠⚠ **R4.5 IS STILL ◑ AND MY OWN BRIEF WAS OFF BY ONE BLOCKER**: `build stdlib/File.maxon` reports the SAME error before and after — `E2015 Array static method 'init'` at `:135`, which sits ABOVE the `FilePermission` defaults at `:139`/`:145`, so those lines are never reached. The default SHAPE is unblocked (probed: a static `writeText(…, mode FilePermission = FilePermission.normal)` called with `mode` omitted runs). **`Array.init` is what R4.5 now waits on.** ◑ **SLICE 5 LANDED 2026-08-01** (main `e02289aef`): **`Array.init(managed)`**, x64-windows 3176 → **3198/0**, x64-linux 3077/0, wasm PASS, zero golden `M` on any target, arm64 SKIP — remote, UNVERIFIED. ⭐⭐ **THE ADOPTION IS A RETAIN, AND THE FRESH `ValueId` THE RETAIN MINTS *IS* THE SURFACE FLIP.** `stdlib/Array.maxon:113`'s `init` body is `Self{managed: managed}`, which in shv2 is an IDENTITY on the record — `Array with T` and `__ManagedMemory with T` are ONE record, TWO surfaces — so there is nothing to construct. But a PURE identity would be a WRONG ANSWER: the ARRAY surface is the *absence* of the buffer mark (`bufferSurfaceValues` is insert-only, there is no unmark), so handing back the argument's own `ValueId` returns a value still dispatching on the BUFFER roster and `init(…).push(…)` is refused as an unknown `Array` method. `retainBorrowedAggregate` answers BOTH questions at once — a fresh name (⇒ array surface, free) and `__mm_retain` (⇒ a balanced second owner, since it raises the same header word `__arr_decref` lowers). **Sabotage-verified both ways by the review**: drop `trackOwnedTemp` ⇒ 3 red, all exit **101**; return the argument's own `ValueId` ⇒ 18 of 22 red with **0xC0000005**. ⭐ **THE ARGUMENT DOOR IS SAME-INSTANCE, AND THE OBVIOUS DOOR WAS WRONG IN THE EXPENSIVE DIRECTION**: `String.init`'s `requireManagedMemoryValue` is BYTE-ONLY, so it would have *accepted* `IntArray.init(<byte buffer>)` — an `Array with Integer` striding 8 through a byte-packed allocation. ⚠ **The review renamed the door** (`requireSameInstanceBuffer` → `requireSameArrayInstance`): the old noun claimed a test it does not make, and keying on the buffer MARK instead would have been wrong, not merely stricter — the mark does not survive a CLOSURE CAPTURE, so `File.maxon:135`'s own shape would be refused the day it moved inside a closure. ⛔ **A GAP THIS RUNG COULD NOT CLOSE AND PINNED INSTEAD**: `Array with int` — the WORD buffer instance — is **nameable by no program** (`Array with int` is E2061 and every ranged alias interns a different instance), so every adoption reachable today is a byte one. Keying the door on element WIDTH would reach it only by giving up soundness (a managed element and an `int` are both 8 bytes ⇒ `Array with String` would adopt raw integers as String pointers). Pinned as `error.a-word-buffer-has-no-nameable-array` so the day it becomes nameable is a RED case, not a silent widening; it intersects the `Array_int`/`Array_Integer` split `default-values.md` and `string-views.md` already pin. ⛔⛔ **AND THE RUNG FALSIFIED A REFUSAL IT DOES NOT OWN — HANDED TO BATCH7, NOT FIXED HERE**: adding a SECOND `Array` static makes `surfaceRosterRefusal`'s Array arm (`Parser.maxon:3234`) stale — it still says *"`create` is a STATIC factory"*, so `a.init(mm)` names `create` and not the static the author just reached for. That is *"widening a door falsifies the message before it"* (D10) again. It is **L-parser-postfix, BATCH7's LIVE lane**, and hand-patching the string would write a FOURTH copy of a fact BATCH7's derived-roster cure is about to delete. ⚖ Coordinator ruling: hand it over rather than patch it. ⚠⚠ **R4.5 STILL ◑, AND ITS REMAINING BLOCKER IS NOT MINE TO TAKE**: `stdlib/File.maxon` needs TWO mechanisms, and measurement after `Array.init` landed moved the error from `:135` to **`:148` `content.managed`** — `.managed` on a plain BINDING, producing an ESCAPING value. That is the exact INVERSE surface flip, and **`A2m` names it by name** (*"a bare `.managed` binding"*) inside BATCH7. ⇒ **File.maxon wants ONE rung holding BOTH directions, after BATCH7 lands.** | 🔶 **CLAIMED — SLICE 6: `.managed` AS A VALUE, then `stdlib/File.maxon` ⇒ R4.5** *(slices 1+2+3+4+5 LANDED; the block is CLEARED — BATCH7's `A2m` landed and **pinned its entrance (3) as R4.5's**, so the mechanism is mine, not its). **Re-measured on `main` before claiming, because this row's blocker list has been wrong twice:** `stdlib/File.maxon` no longer stops at `:148` but at **`:147:23` `content.managed`** with the **field-access** refusal, and it reproduces minimally in BOTH value positions — `let m = a.managed` and `sink(a.managed)` give the identical `E2015 field access on 'a'`, while the chained `a.managed.length()` already resolves past it* | `slice/BATCH2-file-cone` | 2026-08-01T23:57Z |
| **BATCH3** | `A2j` `A2k` `D11b`◑ | **L-types** · **L-parser-postfix** | **One thesis: a member roster must be DERIVED from the arms it describes, and must answer for the type it is shown about.** `D11b` proved the cure for the buffer surface and left half the ruling undone (`A2j`: a *declared* `__ManagedMemory` gets the `Array` surface, roster exactly inverted); `A2k` is the same defect one surface over, in a hand-written literal. Doing these apart means writing the same cure three times and shipping a true `Array` roster to something that is not an `Array`. ⚠ `A2j` needs a **declared-as-the-alias bit** carried per parameter/field/return — that is the mechanism, and it is the batch's real work. ✅✅ **DONE 2026-08-01** (main `4c61be486`, shv2 **2990 → 3012/0**, x64-linux 2891/0, wasm 2837/0; arm64 SKIP — remote, UNVERIFIED). All three members closed, and the batch earned its keep: **the derivation REINTRODUCED A2k's own defect one entry over** — it pulled `managed` onto a list the sentence calls *"methods"*, and `arr.managed()` gives `Unsupported: ( statement`, naming neither the member nor the mistake. The independent review found it by RUNNING the list, and found the CAUSE too: the refusal sentence was written TWICE (one template per surface), free to drift in the noun while each half stayed derived. ⇒ **deriving a list removes the drift between the list and its arms and does NOTHING about the drift between the list and the WORD that introduces it.** One sentence builder now. ⇒ opened **A2m** (three more entrances still inverted, each needing a mark shape A2j's cannot have). | ✅ DONE | `slice/BATCH3-derived-roster` | 2026-08-01T01:56Z |
| **BATCH4** | `A1v` `A2a` `A2b` ~~`A2f`~~ `A2l`(1,2) | **L-ir** · **L-testing** · **L-types** *(added at claim time: `A2l`(1)'s nine sites include `SemanticCheck.maxon` and `TypeResolution.maxon`, which the row's own lane cell did not list — claimed rather than worked in silently)* · *(docs)* — ⛔ **`A2f` DROPPED AT PLAN TIME AND RELEASED `⬜ FREE`: its files are `maxon-sharp/Compiler/{FlatNamespaceCheck,SourceCollector}.cs`, which is the L-bootstrap lane, and `B2` holds it with a LIVE remote branch.** The row filed it under L-ir; that is the misassignment. It belongs with **BATCH1** (the other eight `maxon-sharp` defects), whose one C# suite + codegen-neutrality A/B it would share — paying that gate twice is exactly what batching exists to stop | **The instrument-and-hygiene sweep — explicitly the batch that should NOT have been five rows.** Every member was filed as a singleton by a rung that tripped over it, which is the filing bar this rewrite exists to raise. `A2l`(1) is the one with teeth: **nine name-keyed maps plus the Target-tier twins**, all safe today by upstream refusals *none of them mention* — and fixing one would be a tenth spelling, so it is one guarantee or nothing. The rest are a corpus knob, a README index that should be derived rather than transcribed, two gate blind spots, and an unclosed trace span. ✅✅ **DONE 2026-08-01** (main `38397b45c`; shv2 **3012 → 3015/0**, x64-linux PASS, wasm32-wasi PASS, **arm64 SKIP — remote, UNVERIFIED**; fragments empty after a full run; 0 warnings; no exit 101). Four members closed, `A2f` dropped unstarted on a lane collision (above). ⭐⭐ **THE BATCH EARNED ITSELF ON ITS FIRST MEMBER: the row said `A2l`(1) was "not reachable today", and that was true of the nine sites it listed and FALSE OF THE CLASS** — the tenth, one tier down, was a **reachable silent miscompile** (exit 127, no diagnostic). A batch is what made a nine-site sweep worth doing at all; a singleton would have fixed one `upsert` and never looked at the Target tier. ⭐ Second lesson, from `A2b`: **the cure for a drifting index is the CHECK, not a generated table** — only the derivable half should be derived, and the prose half required to exist. ⇒ opened **A2n** (the corpus is compiled and never run, so no knob can catch a miscompile; plus two sibling instrument blind spots). | ✅ DONE | `slice/BATCH4-instrument-hygiene` | 2026-08-01T04:08Z |
| **BATCH5** | `R10c` `7b3` ~~`7b4`~~ | **L-types** · **L-parser-\*** · **L-gt-runtime** | **Witness/enum-ordinal work that shares the `EnumLayout` surface.** `R10c` needs `findWitnessDispatch` to carry a parent-table pointer (a missing mechanism, and it makes one message FALSE today); `7b3` moves `orderingTagOf` onto `EnumLayout` where the 7b review said it belongs; `7b4` is two more runtime families transcribing ordinals as literals, which `7b`'s mechanism structurally cannot reach. ⚠ `7b4` is **not live** and carries its own re-measure trigger — take it only as ballast, and drop it if the batch grows. ⛔ **DROPPED AT PLAN TIME 2026-08-01 AND RELEASED `⬜ FREE`, on the row's own advice**: its trigger (`throwsTypeName` being read for runtime functions) has NOT fired, its cure needs a mechanism this batch is not building (the enum is USER-declared, so the check must run after resolution, not at install time), and `R10c` turned out to be a real missing mechanism rather than a wording fix. Ballast is what you drop when the load is real. ✅✅ **DONE 2026-08-01** (main `ecb805485`; shv2 3079 → **3088/0**, x64-linux PASS, wasm32-wasi PASS, **arm64 SKIP — remote, UNVERIFIED**; fragments empty on both local targets; 0 warnings; no exit 101; the error-code registry was re-derived after a CONCURRENT mint by R10b and checks clean at 162 — only the GENERATED json conflicted, which is the registry working). `R10c` ✅ and `7b3` ✅; **`7b4` dropped unstarted** on its own row's advice. ⭐⭐ **THE BATCH EARNED ITSELF THE SAME WAY BATCH4 DID: the filed DIAGNOSIS was wrong and the filed OBSERVATIONS were right.** `R10c` was filed as *“`findWitnessDispatch` has no `parentTablePtr`”* — the cure needed no parent pointer at all, because the defect was three readers disagreeing about one fact. **A row's observations age well; its diagnosis does not** — third batch running to say so. ⇒ opened **`A2t`** (a witness dispatch does not filter `static` requirements, so an instance call binds a static slot and passes a spurious receiver) and **`A2u`** (`witnessSlotImplKey` carries no arity, so one interface still cannot declare two same-named requirements while an `extends` edge now can). | ✅ DONE | `slice/BATCH5-witness-ordinals` | 2026-08-01T08:51Z |
| **BATCH6** | `A1z` `A1f-arrayelem` `A1f-aliasclash` `A1f-dupguard` ~~`A2i`~~ | **L-types** | **A ranged type's premise is owed at a door and not enforced there — everything left after `A1f`.** ⚠ `A1f` is now **DONE**, so the "do not start while it is claimed" warning below is spent: `A1z` no longer folds into a live branch, it rebases onto a landed one. ⚖ **RESCOPED AT CLAIM TIME 2026-08-01: `A2i` DROPPED and released `⬜ FREE`; the three `A1f-*` residuals PULLED IN.** They are the same thesis and the same lane (all five rows read **L-types**), and the board's own rule is to add a finding to an existing batch rather than leave singletons: `A1z` is the CLOSURE-return door, `A1f-arrayelem` the ARRAY-ELEMENT door (the last unenforced one), `A1f-aliasclash` a cross-file ranged-alias resolution that is LAST-WINS, `A1f-dupguard` the param+return double guard `A1f` deliberately left. **`A2i` is out because it is the one member this rung cannot honestly finish**: it needs a USER RULING on whether arm64's silent wrap to `i64.min` is acceptable, and **arm64 cannot be verified on this host** — so taking it would mean guessing a ruling and shipping it unverified on the only lane where the behaviour differs. ✅✅ **DONE 2026-08-01** (main `b699c736d`; shv2 **3015 → 3026/0** standalone, **3079/0** on the merged tree after two rebases onto BATCH2's slices; x64-linux 2951/0, wasm32-wasi 2902/0; **arm64 SKIP — remote, UNVERIFIED, and 14 arm64-macos + 7 arm64-linux goldens are now STALE** and owed at the next Mac sync; fragments empty; no exit 101). ⭐⭐ **TWO OF THE THREE ROWS HAD THE WRONG ROOT CAUSE WRITTEN DOWN, AND BOTH ERRORS MADE THE WORK LOOK BIGGER THAN IT WAS** — `aliasclash`'s lookup already scoped by the site's file (the defect was its FALLBACK), and `arrayelem`'s *"a guard would land past the call"* is false for an element whose alias is known at parse time. **The implementer reported both instead of redesigning around them**, which is the whole reason a row's diagnosis is separated from its observations. ⭐ **The elision earned itself** — four clauses, sabotaged in both directions by two independent agents — and the seam where it met `aliasclash` (comparing aliases by NAME, one commit after resolution changed) is closed **by construction**: both sides read through the same file. ⇒ opened **`A2s`** (`Array.resize` exposes a zero slot into a range excluding zero — no door, so no store guard can close it). | ✅ DONE | `slice/BATCH6-ranged-doors` | 2026-08-01T06:52Z |
| **BATCH7** | `A2m` `A2q` `D11e` | **L-types** · **L-parser-postfix** | **ONE THESIS, THIRD TURN: a member ROSTER must be DERIVED from the arms it describes, and a buffer SURFACE must follow the VALUE — at the entrances and the surfaces BATCH3's cure structurally could not reach.** `A2m` is the same defect one canonicalisation deeper (a tuple element, a generic array element, and a bare `.managed` binding all get the roster INVERTED, and a declaration-site column cannot see a CANONICALISED type); `A2q` is `dispatchStringMethod`'s roster — a hand-written second copy of its own arm list that **has already lied twice**, needing the table-driven cure `A2k` proved on the `Array` roster; `D11e` is the same family's last open row and its **premise was measured FALSE** (2026-08-01), so it is a REPRODUCE-OR-CLOSE, not an implement. ⚠ Doing these apart means writing the derived-roster cure a fourth time. ⚠ **`Parser.maxon` is shared with BATCH2's L-parser-decl** — different region, but whoever lands second re-runs the full battery on the merged tree (skill step 11). ⛔ **INBOX FROM BATCH2 SLICE 5 (2026-08-01, main `e02289aef`) — A ROSTER SENTENCE THAT WENT STALE WHILE YOU HELD THE LANE.** Slice 5 gave `Array` a SECOND static (`init(managed)`) and updated `parseArrayStaticCall`'s own refusal, but `surfaceRosterRefusal`'s Array arm (`Parser.maxon:3234`) still ends *"`create` is a STATIC factory (`Array.create(…)`), not a member"* — so `a.init(mm)` on an array value is told about `create` and NOT about the static the author just reached for. **MEASURED on the landed tree, not predicted.** Slice 5 deliberately did **not** patch it: the lane is yours and live, and a hand-patched string would be a FOURTH copy of a fact your derived-roster cure is about to delete. ⇒ **Your cure must cover the STATICS clause, not only the member roster** — that clause is the same *"one fact written twice"* one surface over, and `error.array-roster-names-managed-and-not-create` already pins the tail. ⚠ The fix is one string PLUS **6 `maxoncstderr` blocks BY HAND** (4 in `specs-shv2/managed-memory-methods.md`, 2 in `void-call-result.md`) and 6 fragments across three target dirs — shv2's `--update-required` regenerates RequiredIR but **not** `maxoncstderr`. ⭐ Also handed over, same lane, NOT a defect: **three constants spell `create`** (`Parser.CreateMethod`, `SignatureIndex.ManagedMemoryCreateMethod`, `SignatureIndex.ManagedDirectoryCreateMethod`) while the file's own rule — *"a method NAME is a token spelling, not a container's property"* — is honoured for `init` and violated three ways for `create` right beside it; and `StringInitMethod` now serves the `Array` static too, so its `String` prefix understates it (a naming debt, **not** a second fact — the reuse is correct). Both need `SignatureIndex.maxon`, which is yours. ✅✅ **LANDED 2026-08-01 (main `b3450ea1b`), 3038→3078 suite, +40 cases, ZERO failures of its own.** ⭐ **THE BOARD'S TEXT WAS WRONG IN THREE PLACES AND EVERY CORRECTION WAS MEASURED, NOT ARGUED**: `A2m` named three entrances and there were FIVE, one of its three was FALSE, and `D11e`'s premise did not reproduce. ⭐ **The reviewer found a WRONG ANSWER the whole batch had shipped past** — with every door consulting the alias registry, the identical two-file program COMPILED when the alias file sorted first and was REFUSED when it sorted last; `slotElementsFromAliases` now makes a sweep-fed door structurally unable to ask a half-built table. It also caught its own first fix being wrong, via a pinned pair. ⭐ **The optimizer removed a PRE-EXISTING O(W²)** this batch had doubled — a field access re-finding by NAME the `StructLayout` it already held — leaving a wide-tuple probe **35% below the merge-base**. ⚠ **The ladder read Δ0 through all of it: the scale corpus emits NO TUPLE TYPES**, so that whole path is unexpressed — the `floatSpill`-at-4 shape, recorded in `docs/optimization-log.md`. ⇒ opened **`A3e`** (file-order dependence in `parseTypeReference`'s alias arms — PRE-EXISTING and wider than it looks), **`A3f`** (blocked on it), **`A3g`** (the sixth hand-written roster), **`A3h`** (a pre-existing use-after-free: an element borrowed out of a temporary array, shv2 **63** vs the oracle **5**).| ✅ DONE | `slice/BATCH7-roster-derivation` | 2026-08-01T15:24Z |
| **BATCH8** | `S4` `A1s-rdatapin` **`S4b`** **`G6`** | **L-stdlib** · **L-testing** | ⭐⭐ **ONE THESIS: THE GOLDEN RENDERER RECORDS WHAT THE PROGRAM *DECLARES*, AND THE COMPILER *EMITS* MORE THAN THAT — so a whole class of emitted artifact is invisible to every gate at once.** `S4` is emitted RUNTIME BODIES (`__str_bytes_view` / `__str_byte_at_or_panic` carry **no golden at all**, and the miss is not theoretical: the `separate` owed-base arm is reachable, load-bearing, and its failure is a **SILENT use-after-free** the full suite stayed **3050/0** over); `A1s-rdatapin` is `.rdata` PAYLOADS under a structural label (`registerRdataConstantLabeled` does not advance `nextStringId`, so it shifts offsets without moving any label a fragment prints). **Both are the same sentence one level up — *a MISSING golden never fails* — and here the golden cannot exist at all.** ⇒ ONE mechanism serves both: extend what the renderer RECORDS, on the `RequiredData`-for-`.data` precedent the tree already has, which `A1s-rdatapin`'s row says would *"pin all three at once"* (rdata payloads · E4016 · `reserved-double-underscore`'s positive case). ⚠ **A2n DELIBERATELY NOT A MEMBER** though it is the same *family*: it is the SCALE runner (a corpus that compiles and never runs) plus a board-id linter — a different instrument, and stretching this batch to hold it would buy the review two arguments instead of one. ⚠ **S4 cannot be closed by an ordinary spec** — the methods are stdlib-only and `stageSourceFiles` writes every test under `.spec-tmp/`, with `isSafeRelativeName` refusing the `..` that would escape it; **weakening that check to buy a test trades a real containment guarantee for one** and is not on the table.  ◑ **LANDED 2026-08-01** (main `01cc68dc6`): x64-windows 3198 → **3202/0**, x64-linux PASS, wasm PASS, arm64 SKIP — remote, UNVERIFIED; zero golden `M`; ladder **0/0 at all six rungs, same-path so `bytesComparable`** — a harness rung SHOULD read zero, making the row a negative control on the instrument. ⭐⭐ **THE GATE FIRES ON THE SABOTAGE THE OLD GATES MISSED — verified TWICE, with two DIFFERENT one-sided sabotages of `emitStringBytesAllocation`'s `separate` arm** (implementer's, then mine: the edge carrying `zero` instead of `owedBase`, so the view neither increfs nor decrefs the parent). Each read **one** failure — the new gate — while **3201 other tests stayed green over a real use-after-free**, including the 40 `filepath` and 30 `url` cases that *execute that exact code*, and nothing exited 101. ⭐ **A ROW'S STATED BLOCKER WAS FALSE AND MEASURING IT DELETED THE WORK**: `S4` said it *"cannot be closed by an ordinary spec"* — true of CALLING the stdlib-only methods, **false of the gate**, because a 3-line `let p = FilePath from "a/b.txt"` already emits BOTH runtime bodies (whitelisted `stdlib/FilePath.maxon` reaches `String.addressableBytes`). The gate never needed a call, it needed a RENDERING ⇒ **no stdlib-rooted harness, and `isSafeRelativeName` never had to be weakened.** ⛔⛔ **THE REVIEW FOUND FOUR VACUOUS GATES IN THE GATE-BUILDING RUNG ITSELF** — for this kind of rung a defect is not a failing test, it is **a test that passes when it should not**: an empty ` ```RequiredRuntime ` pinned nothing; an empty ` ```RequiredData `/` ```RequiredRdata ` passed *and disarmed `pinsAnyResult`* (**the empty prefix matches every image ever linked**); a section pin on a `maxoncstderr` case was silently discarded; `--emit-ir-runtime=,` was a no-op. ⭐ And a fifth: `requireRuntimeNamesRenderable` scanned per NAME while `namesFunction` decided per FUNCTION — **one question asked twice, inside the rung built to remove exactly that**, and its divergence is a WRONG ANSWER not a compile error. Cured by DERIVING the refusal from the render; all 3202 goldens byte-identical after. ⚖ **SLICE 2 CLAIMED 2026-08-01T22:25Z — THE TWO RESIDUALS ARE BEING FINISHED, NOT CARRIED.** ⛔ **Filing them was the error, and one of them was MINE**: slice 1 ADDED five refusals nothing runs, and *"a refusal nothing runs is a claim, not a door"* is this project's own rule — a rung does not get to introduce claims-not-doors and file the gap. ⛔⛔ **AND `S4b`'s PREMISE WAS FALSE, measured after filing**: I inherited the implementer's framing (*"hand-assembled chunks never enter `module.functions`"*), which is TRUE and IRRELEVANT — `FunctionCodeChunk` carries `funcName` **and** `code` (`CodeResult.maxon:329-334`), and `CodeResult` carries the whole `.text` plus `debugSymbols` mapping **name → `codeOffset`** (`:57-64`), so a chunk's bytes ARE addressable by name. ⇒ **it is ONE MORE PRODUCER on the existing ` ```RequiredRuntime `, not a new mechanism** — I filed a row on a diagnosis instead of checking for an adjacent representation, which is the exact failure [[a row from symptoms can name a cure that does not exist]] describes. ✅ **SLICE 2 LANDED 2026-08-01** (main `bc235a3f3`) — **BOTH RESIDUALS CLOSED, AND BOTH PREMISES WERE FALSE.** Suite **3202 → 3244/0** on the merged tree (40 of those are BATCH7's, landing between claim and merge), x64-linux 3082/0, wasm 3031/0, arm64 SKIP — remote, UNVERIFIED; zero golden `M`. ⭐ **`S4b`: a chunk IS pinnable.** `FunctionCodeChunk` carries `funcName` + `code`, so ` ```RequiredRuntime ` gained ONE MORE PRODUCER — a name matching no `module.functions` entry but matching an emitted chunk renders its BYTES (pre-link, so displacements read `e8 00 00 00 00` and nothing varies with program size — verified byte-identical across a 5,055-byte and a 17,485-byte program, all **11** renderable chunks). The unknown-name refusal stays DERIVED, and its order-dependence is itself pinned: moving the chunk pass ahead of the function pass turns exactly one fixture red. ⭐ **`G5`: the five refusals are now doors.** `Testing/HarnessSelfTest.maxon` + 8 fixtures spawn `spec-test` as a SUBPROCESS per fixture and assert non-zero exit + a message SUBSTRING (never `file:line`, which would false-red on any edit above it). Coordinator-verified: neutering one shared guard turns exactly its 2 dependent fixtures red, naming the removed refusal, and aborts the run. ⛔⛔ **THE REVIEW CAUGHT A RED BUILD THE RUNG SHIPPED — AND MY BATTERY COULD NOT HAVE SEEN IT**: an `export typealias FilePathArray` collided with `QueryDatabase.maxon`'s in the flat namespace, which **`dotnet build` refuses and `maxon build maxon-shv2` does not run at all**. I built shv2, ran 3204/0 and read fragments — all green, all irrelevant. ⇒ **`build csharp` belongs in the battery whenever shv2 gains or renames a TOP-LEVEL name, not only when `maxon-sharp/` is touched**: the trigger is a fact about shv2 sources; the command that checks it merely lives in the bootstrap's build. ⭐ The review also killed an **unbounded 8-ary process fan-out**: the recursion guard spelled the fixture directory a second time, so renaming the corpus would have made every sub-run re-run the whole self-test (measured 0.575 s vs 0.061 s). Cured by deriving the name from the path the listing actually reads. | ✅ **DONE** | `slice/BATCH8-finish-residuals` *(merged)* | 2026-08-01T22:25Z |

⚠ **`R10b` stays a singleton on purpose** — a hung compile holding the binary lock is operational, shares
no mechanism with anything above, and is the one row whose fix a stuck agent needs *immediately*.


### The BOARD

Status: **⬜ FREE** · **🔶 CLAIMED** *(branch + UTC)* · **✅ DONE** · **⛔ BLOCKED** *(never claimable —
take its blocker instead)*

| id | Slice | Lane | Status | Owner (branch) | Claimed (UTC) |
|---|---|---|:--:|---|---|
| **G1** | **Split `compilerExe` → compiler-under-test + WORKER exe.** §"⚠⚠ STEP 0" — unsplit, the gate passes while testing nothing. ✅ `9402d4620`..`20312151a`: two params + `--compiler=<path>` forwarded UNCONDITIONALLY (the worker used to re-derive it). Pinned by `scripts/worker-exe-split-gate.sh` — a **NEGATIVE control**, because a byte-identical copy distinguishes the two models not at all. Review found a second door to the same wrong answer (`--compiler=` empty read as "no flag") and closed it. **Moved NO committed goldens** — fragments empty after a full run | L-harness | ✅ DONE | `slice/G1-worker-exe-split` | 2026-07-29T18:22Z |
| **S1** | ~~`__Builtins.*` QUALIFIED-call recognition~~ **✅ THE ROW WAS DRAWN STALE — THIS LANDED 2026-07-24** as §"STDLIB WHITELIST" step (2) (`1c36c1ba8`), which this board's own text says three lines further down. **Re-verified by RUNNING it 2026-07-29**, not by reading: `__Builtins.currentTimeNanos()` in an ordinary user program compiles and returns a live reading, and `__Builtins.noSuchIntrinsic()` gives a clean **E3004** (no panic). The arm is `Parser.maxon:26681`, gated on `isBuiltinsIntrinsicCallee`, recognizing 4 members (`currentTimeNanos`, `currentTimeMs`, `currentUnixTimeSeconds`, `sleep`). **Nothing in the S2 cone is blocked on it** — see the corrected blocker table below | L-stdlib | ✅ DONE | *(landed 2026-07-24)* | — |
| **S2** | **Whitelist entries 4…n.** ⚠ **THE BLOCKER LIST BELOW WAS WRONG AND IS NOW MEASURED** (2026-07-29). **The dominant cost of the whole gate** . ⚠ **D5 IS NOW DONE AND ITS TWO FILES ADVANCED TO NEW, DIFFERENT BLOCKERS — both MEASURED 2026-07-29 after the fix, not predicted.** `stdlib/FilePath.maxon` clears all seven of its `#if` regions and now stops at **`E2010 :34:25 Expected 'identifier' but got 'from'`** — a function *named* `from`, which shv2 lexes as a keyword; that is a LEXER/parser question and belongs to no row yet. `stdlib/Process.maxon` clears its `#if` and now stops at **`E3004 :29:17 call to undefined function '__Builtins.executablePath'`** — a missing intrinsic, which IS S1-shaped work and is the first confirmed instance of it ✅ **SLICE 1 LANDED 2026-08-01** (main `d7553412e`) — **whitelist entries 5 and 6: `stdlib/URL.maxon` + `stdlib/helpers/url/urlHelpers.maxon`**, the cone's self-contained ROOT (it references neither `FilePath` nor `File`, so nothing had to land first). The single method that gated **ALL THREE** files — `String.addressableBytes` (URL:114, FilePath:133, File:60) — now exists as `__str_bytes_view`, a ZERO-COPY view: **owned 48-byte RECORD, borrowed BYTES under a refcount**. ⭐ The load-bearing finding: the String and Array 48-byte records agree at five slots and **disagree at `@40`** (`isAsciiFlag` vs `element_destroy`), so a retagged pointer would make `__arr_decref` `callIndirect` **address 1** on an all-ASCII slice — i.e. every corpus call, not a rare shape. `String.byteAtOrPanic` too; both gated stdlib-only by the file's **LOCATION**, not its visibility. `specs/url.md` ported byte-identical (`git hash-object` 4759fe3f, 42 cases, **42/42 green, 0 disabled**). x64-windows **3061/0**, x64-linux **2940/0**, wasm32-wasi **2898/0**, arm64 **SKIP — remote, UNVERIFIED**. ⚠ **This row's blocker list was re-measured before claiming and was NOT stale this time** — all three first blockers sat exactly where it said | L-stdlib | ▣ **BATCH2** *(blocked — see batch)* | on ~~D5~~ ✅ · ~~D1~~ ✅ · ~~D2~~ ✅ · D4 · S3 · ~~`from`-as-a-name~~ ✅ **D8** · **`String.byteAtOrPanic` (new, measured 2026-07-30 — FilePath's blocker AFTER D8, S3-family)** · **`__Builtins.executablePath`** — *not* on S1. See the measured table | — |
| **S3** | **`String.addressableBytes`** — `stdlib/File.maxon:60`'s first blocker, and **it was on no row until 2026-07-29**. Not a whitelist question and not a parser one: a String method shv2 does not provide (its E2015 lists what it does). Needs its own contract ✅ **CLOSED BY R4.1, 2026-07-30** (main `e1e26205e`) — and closed by delivering the NEED rather than the name. `addressableBytes` is `module`-visible (`stdlib/String.maxon:181`, one line: `return managed`), so **no spec could ever have tested it** — the oracle answers `E3008: not exported`. What the corpus actually writes is `"…".toByteArray().managed`, and that now works: `__ManagedMemory` resolves as a builtin generic alias to `Array with Byte`, and `.managed` is an identity read yielding it. The encapsulation door S3 was named for therefore exists, reached by the spelling user code uses. ⚠ The BINDING spelling (`let b = …; b.managed`) is still refused — deferred to R4.2 with a measurement, since the corpus reaches it only as `arr.managed.setLength(2)` and `setLength` is R4.2's | L-stdlib | ✅ DONE | *(closed by R4.1)* | — |
| **R4** | **THE FILE-IO RUNTIME — `__ManagedFile` + `__ManagedDirectory`.** Opened 2026-07-30 by S3, which measured its way into it. ⛔⛔ **CLAIMED 01:48Z AND RELEASED UNBUILT 02:0xZ — AND THE SURVEY BELOW LANDED 01:20–01:55Z, INDEPENDENTLY, FROM THE D4 SLICE, REACHING THE SAME VERDICT. TWO AGENTS SURVEYED THIS ROW IN PARALLEL AND BOTH STOPPED AT "NEEDS A RULING"; that agreement is the strongest evidence on the row.** *(Neither agent was wrong to race — the claim and the survey crossed on the wire. This is the board doing its job: the correction reaches you by rebase.)* ⛔⛔ **THE PREMISE IS FALSE, AND IT MAKES THIS A WORKSTREAM RATHER THAN A SLICE — SURVEYED 2026-07-30 across both references + shv2.** The row said *"shv2 synthesizes exactly one `__Managed*` type (`__ManagedMemory`) … need two more"*. **shv2 has NO `__Managed*` type AT ALL.** `isCompilerOwnedTypeName` (`TypeResolution.maxon:646-648`) enumerates exactly six names — `ExitCode`, `HashValue`, `Codepoint`, `Ordering`, `CharacterSet`, `CharacterMemberSetAlias` — and none is managed; shv2 never compiles `stdlib/Array.maxon` or `stdlib/String.maxon`, it **SYNTHESIZES** their layouts against `Runtime/MmRuntime.maxon`, and `specs-shv2/managed-memory-methods.md` disables every case that so much as names `__ManagedMemory` (same footnote 8+ times). ⇒ R4 is not *"add two more of a thing that exists"*; it is **"introduce the FIRST compiler-owned refcounted OS-handle type mechanism in shv2"** — plus **`ByteMemory`**, which is not a resolvable shv2 type either (`stdlib/String.maxon:68` declares `var managed as ByteMemory`) and is the REAL prerequisite hiding behind S3's one-line `addressableBytes`. **The contract, where both references agree:** 13 `__ManagedFile` methods + 7 `__ManagedDirectory` methods (bootstrap `2-Parser.cs:1414-1440`/`:1449-1463`; v1 `LowerMaxonToStd.maxon:1801-1842`/`:1883-1920`), each an 8-byte refcounted handle with a runtime-owned destructor that closes on drop. ⛔ **AND THE ORACLE CANNOT ANSWER FOR TWO OF THE THREE LANES shv2's OWN GATE RUNS: the bootstrap has NO Linux emitter and NO wasm backend whatsoever** — `3-MlirPipeline.cs:182` throws for any arch that is not x64/arm64, and x64 is hard-wired to Win32 while arm64 is hard-wired to Darwin. Only **v1** implements all four, including a full WASI Preview2 filesystem (`Targets/Wasm/MirToWasm.maxon:1723-1796`) — and v1 does not build, so it can only be read. ✅ **The wasm/arm64 question needs no new mechanism, though:** shv2 already owns the clean target-refusal path — **E3104** `targetUnsupportedConstruct` via `targetHasWin32Substrate` (`SemanticCheck.maxon:1338-1423`), which is exactly how the wall clock is x64-windows-only today. *(NOT E3074 — that is v1's subprocess-specific code, and `ErrorCodeRegistry.maxon:428` says so.)* ✅ **44 committed acceptance tests already exist to port**, byte-identical, across 7 `/specs` files (`managed-file.md` 9, `managed-directory.md` 10, `directory.md` 9, `file-info.md` 6, `file-io.md` 7, +3 scattered) — note `specs/filepath.md`'s 40 cases are pure string work and touch no filesystem. ⚠ Whitelisting `File.maxon`/`Directory.maxon` reserves `FileReadError`/`FileWriteError`/… **program-wide** (`StdlibLoader.maxon:90-114`), and File.maxon carries string literals ⇒ **it is the `.rdata` golden-churn trigger this board already warns about for S2.** ⇒ **THE SCOPE IS A RULING, NOT A MEASUREMENT** — host-only with E3104 elsewhere? `ByteMemory` as its own rung first? how many slices? **R4's owner should settle that before writing code, and should not expect one rung to hold it** ⭐⭐ **THE CLAIMANT'S INDEPENDENT SURVEY ADDS FOUR FACTS THE ABOVE DOES NOT, each run rather than read — see the full box in this section (“R4 IS A PHASE, NOT A ROW”):** **(a) v1 MEASURES THE PORT AT ~6,700–6,800 LINES ACROSS ~9 FILES**, of which **~3,900 is per-backend machine-code emission** (Windows IAT ~1,350 · wasm ~2,060 · arm64 ~460) for a ~20-method surface. **(b) shv2 has NO THROWING BUILTIN AT ALL, and it is STRUCTURAL:** `throwsMap` is built exclusively from DECLARED functions (`SemanticCheck.maxon:70-78`) and an intrinsic is never in `module.functions`, so it *cannot* appear there — yet every one of the ~20 members is `try X otherwise`-shaped in the stdlib. This would be shv2's **first** throwing synthesized builtin, with nothing to copy. **(c) shv2's WHOLE OS-PRIMITIVE SURFACE IS THREE SLOTS** — `OsImportSlot` (`Targets/Shared/OsImportSlot.maxon:25-29`) holds exactly `exit`, `write`, `mmap`; each new primitive costs an `OsImportSlot` case + a Mach-O symbol + a Windows `IatSlot` case + **per-arch** Linux syscall numbers + a WASI triple + a lowering arm in each of three backends, and there is **no `wasi:filesystem/*` import in shv2 at all**. **(d) WASI PREVIEW2, NOT PREVIEW1** — v1 uses `wasi:filesystem/types` resource handles and `MirToWasm.maxon:15-16` states *“Preview1 … is gone”*, so a port written against `path_open`/`fd_readdir` targets an API v1 abandoned. ⚠ **And the 8-byte handle struct both references register is a TYPE-CHECKING FICTION** — v1's real `__ManagedDirectory` runtime block is **40 bytes**, with no single source of truth tying the two. ⚠ **v1 NEVER FINISHED THIS ON arm64-macOS AND IT FAILS SILENTLY:** `getdents64` has *“no macOS import wired yet”* and returns `-1`, which `mrt_directory_next` reads as EOF ⇒ **`Directory.list()` returns an EMPTY ARRAY with no error** (`Arm64Backend.maxon:2396-2411`). ⇒ **RELEASED (status FREE) rather than held:** the ruling is the user's, and a claimed row would close L-stdlib to everyone while it waits. *(The glyph is spelled out here on purpose: a status marker loose in a row's prose is a second spelling of the row's state, and this board is read by scripts as well as people.)* ✅✅ **THE RULING LANDED 2026-07-30 — R4 IS A WORKSTREAM OF THREE SEQUENCED RUNGS, NOT A ROW. Take `R4.1`; this row is now a HEADER and is never claimed.** (1) **TARGET SCOPE: HOST-ONLY.** `x64-windows` is real; x64-linux, wasm and both arm64 lanes get the **existing** `E3104 targetUnsupportedConstruct` refusal via `targetHasWin32Substrate` — **no new mechanism, and the precedent is already load-bearing** (the wall clock is host-only by this exact route). Three measured reasons it is not a compromise: the **bootstrap oracle cannot answer for 2 of the 3 lanes shv2 gates on** (`3-MlirPipeline.cs` throws for any arch that is not x64/arm64 — re-verified 2026-07-30), the wasm slice needs WASI **Preview2** resource handles against a compiler with **no `wasi:filesystem/*` import at all**, and **v1's arm64 `Directory.list()` SILENTLY RETURNS AN EMPTY ARRAY** (`getdents64` never wired, `-1` read as EOF), so porting it there imports a wrong answer rather than a feature. Defers ~3,900 of v1's ~6,700 lines, all of it per-backend emission. (2) **THE PREREQUISITES FOLD INTO R4.1, THEY DO NOT GET THEIR OWN RUNG** — and that is S3's lesson applied rather than re-learned: **`__ManagedMemory`/`ByteMemory` IS NOT INDEPENDENTLY DELIVERABLE BECAUSE IT IS NOT USER-NAMABLE.** Measured 2026-07-30 over `specs-shv2/managed-memory-methods.md`: of its 29 cases, **all 11 GREEN ones reach managed memory purely through String/Array METHODS and not one names the type**, and **exactly ONE case names it in code at all** (`cstring-round-trip`, disabled). A standalone rung would therefore have ~1 acceptance case for an encapsulation type — the "green suite that tests nothing" this process exists to prevent. Built underneath `File.read()` it inherits real user-visible acceptance specs. (3) **SHAPE: three rungs, sequenced by a HARD DEPENDENCY** (the skill's slicing reason #1 — every later part codes against the handle contract R4.1 defines, so the parallelism a wide wave needs is genuinely unavailable). **S3 folds into R4.1 and closes with it.** | L-stdlib | 📋 **WORKSTREAM** *(header — take R4.1)* | *(ruling 2026-07-30)* | — |
| **R4.1** | **The handle mechanism + the first THROWING builtin + `__ManagedFile` open/read/close.** The contract rung: introduces shv2's FIRST compiler-owned refcounted OS-handle type (an 8-byte handle with a runtime-owned destructor that closes on drop) and its FIRST throwing synthesized builtin. ⚠ **The throwing half is STRUCTURAL, not a lookup fix, and it is verified at the code**: `buildThrowsMap` (`SemanticCheck.maxon:70-78`) iterates `module.functions` ONLY, and an intrinsic is never in `module.functions`, so an intrinsic **cannot** be in the throws map — yet every one of the ~20 stdlib members is `try X otherwise`-shaped. Nothing to copy from either reference. ⚠ **New OS primitives are expensive per unit**: `OsImportSlot` (`Compiler/Targets/Shared/OsImportSlot.maxon`) holds exactly **three** cases — `exit`, `write`, `mmap` — and its own comment notes there is not even a `munmap`. Host-only scope means each new primitive costs a Windows `IatSlot` case and a lowering arm, and an E3104 arm elsewhere. ⚠ **`ByteMemory` is the real prerequisite hiding behind S3's one-line `addressableBytes`** (`stdlib/String.maxon:50` — `typealias ByteMemory = __ManagedMemory with Byte`; `:68` — `var managed as ByteMemory`), and neither `__ManagedMemory` nor `ByteMemory` resolves in shv2 today. Acceptance: the reachable subset of `specs/managed-file.md` (9 cases), ported byte-identical. **Closes S3.** ⭐⭐ **SURVEYED 2026-07-30 AND THE ROW ABOVE IS WRONG IN THREE PLACES — ALL THREE OVERSTATE THE DIFFICULTY, AND EACH WAS MEASURED, NOT READ.** **(a) "shv2 has NO THROWING BUILTIN AT ALL, and it is STRUCTURAL" is FALSE.** `try a.get(9) otherwise 7` compiles and returns **7** today. The mechanism is named and already generalized: **`isThrowingRuntimeCallee` (`Parser.maxon:11553`) is "THE ONE home of 'which builtin throws'"**, composed from `isThrowingArrayRuntimeCallee` (`Runtime/ArrayRuntime.maxon:355`) and `isThrowingGtRuntimeCallee` (`Runtime/GtRuntime.maxon:617`) and read at the single `throwingAccessor` branch (`:11575`). ⇒ R4.1 adds a third composer, it does not invent a mechanism. ⭐ **And it must copy the DRIFT GUARD with it** (`ArrayRuntime.maxon:431-451`): `installArrayRuntime` panics if the predicate names a callee whose builder stamps no `throws` clause **or vice versa** — a bidirectional check, which is exactly the one-fact-two-places shape this project keeps paying for. **(b) "shv2's WHOLE OS-PRIMITIVE SURFACE IS THREE SLOTS" conflates two different tables.** `OsImportSlot`'s three (`exit`/`write`/`mmap`) are the **portable cross-target** set; **Windows has its own `IatSlot` with 31 entries** (`Targets/Windows/PeWriter.maxon:147`) **already including `CreateFileW`, `ReadFile`, `WriteFile`, `CloseHandle` and `GetLastError`** — four of the five primitives open/read/write/close needs are ALREADY IMPORTED. Host-only R4.1 adds roughly **three** slots (`GetFileSizeEx`, `DeleteFileW`, `GetFileAttributesW`), not an OS abstraction. **This is the single biggest cost the ruling's host-only scope avoids, and the row could not see it because it was reading the portable table.** **(c)** `managed-memory-methods.md` does not "disable every case naming `__ManagedMemory`" — only **one** case names it in code. ✅ **RED BASELINE, measured on a freshly built binary:** `__ManagedMemory` as a declared param type ⇒ **`E3011 Unknown type '__ManagedMemory'`**; `"x".toByteArray()` ⇒ **already works**; `.managed` ⇒ **`E2004 Function 'managed' does not return a value`**, a parse-time error that fires FIRST and hides the E3011 behind it (the `abortedParseArtifact` discard D4 filed). ⚠ **So the acceptance specs need `__ManagedMemory` USER-NAMABLE** — `specs/managed-file.md` writes `path __ManagedMemory` as a declared parameter and `export var file as __ManagedFile` as a field — which is a bigger front-end surface than "a handle type": **two** compiler-owned types plus a `.managed` accessor. The `CharacterSet` pattern is the template (`registerCharacterSetType`, `Parser.maxon:4724`, demand-driven on a producer being present; reserved layout name `__CharacterSet` vs user-facing bare name) — and note D4's residual, that the reserved/user-facing split is exactly what made `CharacterSet` misreport as undeclared ✅✅ **CLOSED 2026-07-30** (main `e1e26205e`; x64-windows 2561→**2565/0**, x64-linux PASS, wasm PASS, arm64 SKIP — remote, UNVERIFIED). **Fragment ADDITIONS only, ZERO modifications** — byte-identical codegen for every pre-existing program, which is what appending an op and an IAT slot past the last variant buys. **Delivers `openRead` + `close`, 4 of the 11 acceptance cases; the other 7 are disabled with named R4.2 blockers.** ⭐ **THE `__` PREFIX WAS NEVER THE BARRIER THE ROW ASSUMED, AND `CharacterSet`'s OWN COMMENT IS WHY IT LOOKED LIKE ONE.** That comment states a compiler-owned type *"cannot cross a function boundary, because a parameter or return type would have to NAME it"* — true of `__StringIndex`/`__CharacterSet`, and a consequence of their RESERVED-vs-BARE name split, not of the prefix. Measured: a field declared `as __StringIndex` reports **E3011 (unresolved)**, not E2051 (reserved), so a `__` name in a TYPE POSITION was always legal and only failed to resolve. `__ManagedFile` spells one name in source and registry, so registering the layout was the entire fix and it is namable as a parameter AND a field. ⭐ **`__ManagedMemory` IS `Array with Byte`, NOT A TYPE OF ITS OWN** — a builtin GENERIC ALIAS bound to the instance `toByteArray()` already yields, so a `path __ManagedMemory` parameter and its argument are ONE type by construction and `.managed` is an identity read costing nothing. That is right for the MEASURED slice boundary and is not assumed to survive R4.2: **9 of the 11 cases use a `__ManagedMemory` only as an opaque path buffer**; the two that construct one and read its capacity are R4.2's, and if the object surface cannot ride the alias the thing to change is that ONE binding. ⛔⛔ **THREE DECISIONS THAT COMPILED CLEAN AND WERE WRONG — the rung's whole risk lived here, not in the 27 mechanical classification sites.** (1) **`CreateFileW` because the slot was already imported.** Broken even for ASCII: a path is a UTF-8 BYTE buffer and the W entry reads it as UTF-16, so `"abc"` (`61 62 63`) becomes the single unit `0x6261`. v1 opens FILES with **`CreateFileA`** and reserves W for wide DEVICE names — which is exactly the split shv2 already had, both its W sites being pipe plumbing. Caught by asking the reference at the point of use. ⚠ Known limitation, written down rather than discovered later: `CreateFileA` reads its bytes in the system ANSI codepage, so a non-ASCII path is misread where that codepage is not UTF-8; every acceptance path is ASCII. (2) **`FileShareNone`, the nearby constant.** Right for a pipe end that must not be shared; it makes a second `openRead` of one path fail with a sharing violation, which `managed-file.exists` (open, close, re-open) would have tripped. (3) **Writing the NUL in place at `buffer[length]`.** A one-byte heap overflow exactly when capacity equals length — which is what `"…".toByteArray()` produces, since only string LITERALS are NUL-terminated (in rdata) while a materialized view is sized to its payload. The graph allocates `length+1`, copies via the now-exported `ArrayRuntime.emitForwardCopy`, terminates, calls, frees — and frees BEFORE the error branches, since two of the three would otherwise leak it. ⭐⭐ **THE FIRST ACCEPTANCE CASE PASSED FOR A REASON THAT PROVED NOTHING, AND THE CONTROL IS WHAT SETTLED IT.** `open-read-nonexistent` expects `notFound` — and a GARBAGE path returns `notFound` too, so it would have passed with all three of the above still broken. Opening a file that EXISTS is the control that actually demonstrates the path reaches the OS intact. (Its first run failed on a RELATIVE path; `run_program` executes from a temp directory, so the test was wrong, not the compiler — confirmed with an absolute path before concluding anything.) ⭐ **A BUILTIN'S ERROR TYPE IS A SEPARATE QUESTION FROM WHETHER IT THROWS, and only the first half of the row's concern was false.** `try a.get(9) otherwise 7` proves throwing builtins exist; but `throwsOf` is filled from DECLARED functions, so a runtime callee's `otherwise (e)` bound `e` with NO type and `match e` fell to the scalar path reporting *"enum-case patterns arrive in a later wave"* — a message about a missing FEATURE for a program whose feature is present. Measured against the control (the identical match over a USER enum's error compiles and runs). Fixed at that one door. ⭐ **`close()` IS IN THIS RUNG BECAUSE OF A LEAK THE GATE CANNOT SEE:** an open with no close leaks an OS HANDLE for the process lifetime, and a handle is not memory, so exit 101 never fires. `size`/`read`/`write` genuinely could wait; this could not. Idempotent (box zeroed at alloc, sentinel written back), double-close verified. ⭐ **THE DRIFT GUARD EARNED ITSELF IMMEDIATELY** — a careless edit named `__mf_close` as throwing; it does not throw and stamps no clause, which is precisely the named-not-stamped arm. Two callees now, one throwing and one not, so the predicate is a real discriminator rather than a tautology over one name. ⭐ **STEP-6b RAN (an IR op is the trigger) AND FOUND NO SUPERLINEARITY** — every ratio ≈ ×2.00; `splitting` ×2.14 / `liveness` ×2.08 are the pre-existing documented bends, untouched because nothing here enters regalloc. Allocations **+222…+1,487**, fitting ~181 flat + ~41 per doubling-unit to within 2% — the `__ManagedFileError` seed riding a doubling file count, **+0.01%** of rung 5. It DID find a constant-factor cost and fixed rather than filed it: the sweep took a THIRD complete token walk per file, now one `fileNamesProducerMask` asking all three probes (worst case one scan where it was three), which made `fileNamesAnyToken`/`probeMatchesHere`/`TokenProbe` dead and they are removed. ⚠ **The CPU column is deliberately NOT used to claim anything**: the prior row was a different binary on a different checkout path, and this log's own rule is that only the same-path A/B is attributable. **HOMED, NOT FIXED:** the `.managed` BINDING door still refuses (`let b = …; b.managed`) where the value door works — deferred to R4.2 with a measurement, because the corpus reaches the binding form ONLY as `arr.managed.setLength(2)` (`array-slots.md`, 4 cases) and `setLength` is R4.2's, so fixing it now would add a mechanism no committed case could exercise. Plus a **bootstrap oracle bug filed**: any standalone `__ManagedFile.*` crashes maxon-sharp with E9001, its own suite green only because the runner BATCHES — so this whole subsystem had **no ad-hoc oracle**, only the 11 committed cases. Re-verified still open after the unrelated bootstrap fix that landed mid-rung | L-stdlib | ✅ DONE | `slice/R4.1-file-handle-runtime` | 2026-07-30T03:15Z |
| **R4.2** | **The rest of `__ManagedFile` + `File` / `FileInfo`.** The remaining methods of the 13 the two references agree on (bootstrap `2-Parser.cs:1414-1440`; v1 `LowerMaxonToStd.maxon:1801-1842`), plus whitelisting `stdlib/File.maxon`. Acceptance: `specs/file-io.md` (7) + `specs/file-info.md` (6). ⚠ **Whitelisting `File.maxon` reserves `FileReadError`/`FileWriteError`/… PROGRAM-WIDE** (`StdlibLoader.maxon:90-114`) and the file carries string literals ⇒ **this is the `.rdata` golden-churn rung** the board already warns about for S2; expect fragment MODIFICATIONS and justify them rather than regenerating blind ✅ **UNBLOCKED 2026-07-30 — R4.1 landed.** Its acceptance set is now precise: the **7 disabled cases in `specs-shv2/managed-file.md`**, each carrying a named blocker, plus `file-io.md` (7) and `file-info.md` (6). **Three things R4.1 hands it, measured rather than guessed:** (a) **the `__ManagedMemory` OBJECT surface is the real work** — `create`/`setLength`/`setByte` + `String.init(buffer)`; R4.1 bound `__ManagedMemory` to `Array with Byte` as an opaque path buffer, which is exactly right for 9 of 11 cases and exactly insufficient for the other 2, so **change that ONE binding rather than adding a second type**; (b) **the `.managed` BINDING door** (`let b = …; b.managed`) is still refused where the value door works — deferred here on purpose, because the corpus reaches it only as `arr.managed.setLength(2)` and `setLength` is yours, so it becomes testable exactly when you need it; (c) **the RAII destructor** — `close()` already exists and is idempotent, so the destructor can close again harmlessly; what is missing is only the automatic half. ⚠ **Budget for the 27-site toll TWICE**: `read` and `write` are each a new StdOp, and the band-append invariant makes every match site fail to compile until it classifies them. That is mechanical; the real risk is the class of decision that bit R4.1 three times — a constant or entry point that compiles clean and is wrong at runtime (`CreateFileW` for `CreateFileA`, `FileShareNone` for `FileShareRead`, a NUL written in place). `read`/`write` carry more of exactly that (buffer lengths, byte counts, an out-param), which is why this stayed a separate rung ✅✅ **CLOSED 2026-07-30** (main `85a91cc02`; x64-windows **2632/0**, x64-linux PASS, wasm PASS, arm64 SKIP — remote, UNVERIFIED; tracked fragments empty-diff after a suite run). All 11 remaining methods + the RAII destructor + the byte-path `__ManagedMemory` members + `String.init(managed)`. **7 new StdOps, not 11** — `write` reuses the existing `osWriteFile`, `openWriteExecutable` ≡ `openWrite` under the host-only ruling, and `exists` reuses `stat`'s `FindFirstFileA`/`FindClose` rather than importing a second way to ask one question. ⛔⛔ **THIS ROW'S ACCEPTANCE CLAIM WAS MEASURED FALSE, WHICH IS WHY `File`/`FileInfo` IS NOT HERE**: every one of `file-io.md`'s 7 and `file-info.md`'s 6 cases is written `File.readText(FilePath from "…")`, `stdlib/File.maxon` stops at `E2015 String method 'addressableBytes'` (`:60`) — the method S3 deliberately did NOT add — and every public signature takes `FilePath`, which stops at `byteAtOrPanic`. ⇒ **R4.5**, and the `__ManagedMemory` OBJECT surface ⇒ **R4.4**, both below. Acceptance actually delivered: the **6** disabled `managed-file.md` cases + a new byte-identical port of `builtin-type-checking.md` (2 enabled) + an shv2-authored `rename` round trip. ⭐⭐ **THE REVIEW FOUND TWO BLOCKERS A GREEN SUITE WAS SILENT ON, AND THE FIRST IS THE LESSON: `statFree` DID NOT PARSE, so every SUCCESSFUL `stat` leaked and exited 101** — it is the only static that is both void and non-throwing, so a bare statement is its only position, and **not one committed case reached a successful `stat`** (the variant case throws before a buffer exists; `statField`/`statFree` had ZERO coverage). The implementer had MET this refusal, worked around it with `_ = Type.method(…)`, and FILED it as out-of-scope — the deferral habit this process exists to break. Second: `__ManagedMemoryError`'s 8-case order was built as a wire format *so a handler could discriminate it*, and `throwsOf` answered `none`, so `match e` reported a MISSING FEATURE for a program whose feature was present — a wire format with no reader. ⭐ **The RAII destructor closes a leak class the exit-101 gate STRUCTURALLY CANNOT SEE**: R4.1's drop answered `__mm_decref`, freeing 8 bytes and leaking the OS HANDLE for the process lifetime, because a handle is not an allocation. Proven by a negative control instead — a live unclosed write handle makes `openRead` fail with `ERROR_SHARING_VIOLATION`, so `auto-close` cannot pass vacuously. ⭐ **Step 6b found the rung's whole GROWING allocation term and it was not where the ladder reading said**: hoisting `internArrayByteInstance()` to the top of `dispatchArrayMethod` — right in shape, one probe for four arms — put a heap allocation on EVERY array-method call; memoized, −42,229 at rung 5, ×32.3 across a ×32 ladder, leaving the rung FLAT at +891 per compile | L-stdlib | ✅ DONE | `slice/R4.2-managed-file-rest` | 2026-07-30T07:48Z |
| **R4.3** | **`__ManagedDirectory` + `Directory`.** The 7 methods (bootstrap `2-Parser.cs:1449-1463`; v1 `LowerMaxonToStd.maxon:1883-1920`). Acceptance: `specs/managed-directory.md` (10) + `specs/directory.md` (9). ⚠ **Do NOT port v1's arm64 directory walk even when arm64 is eventually scoped in** — it is the unfinished lane that returns an empty array with no error (`Arm64Backend.maxon:2396-2411`). ⚠ **The 8-byte handle both references register is a TYPE-CHECKING FICTION**: v1's real `__ManagedDirectory` runtime block is **40 bytes**, with no single source of truth tying the two — pick one and make it the one ✅ **UNBLOCKED 2026-07-30 — R4.1 and R4.2 both landed**, and R4.2 hands it more than R4.1 did: the throwing-builtin path, the errno→variant mapping, the path→NUL scratch copy, the `Win32OutWordBytes` out-param shape, the RAII drop-callee door (`managedNameDropCallee`), and the 27-site toll's range-arm form (`osFileOpenRead to osFileFindClose` covers 20 of 27 — extend the range, do not add 27 arms). ⚠ **Budget for the toll ONCE more** and read R4.2's two blockers first: a **void, non-throwing** static needs the statement-position door (`voidCompilerStaticCallsAt`), and an error enum's case order is only a wire format if `throwsOf` names its callees — `__ManagedDirectory`'s `next`/`close` are exactly those two shapes again ✅✅ **CLOSED 2026-07-30** (main `47a3a4f91`; x64-windows **2685/0**, x64-linux PASS, wasm PASS — GREEN first run, no golden churn, because the mechanism is host-only by E3104 so its cases are x64-windows-only; arm64 SKIP — remote, UNVERIFIED). All 7 methods + `__ManagedDirectoryError` + the RAII destructor + 4 StdOps + 4 kernel32 IAT slots. ⚠ **SCOPE RE-CUT, THE THIRD ROW RUNNING WITH THIS SHAPE**: `specs/directory.md`'s 9 cases are ALL `Directory.*(FilePath from "…")` (19 `FilePath` uses) ⇒ **R4.5's**; `specs/managed-directory.md` has **zero** ⇒ this rung's 10. **Three decisions, each MEASURED from a reference disagreement rather than chosen:** (a) **the record follows the BOOTSTRAP** (8-byte box → separate 336-byte find block) — the reverse of R4.4's call, because here the bootstrap is self-consistent while **v1's** 8-byte declaration is the fiction (its runtime returns a bare 40-byte record, and on Windows adds a *third* shape at 616 bytes); (b) **`exists` returns `bool`** (v1's typing) because `specs/managed-directory.md:205` writes `if not __ManagedDirectory.exists(…)` and `not` on an `i64` is bitwise complement; (c) **`filename()` COPIES** (v1's shape) — the bootstrap returns a raw pointer *into* the find block that the next `next()` invalidates, and `String.init` reads `length@8`/`buffer@0`, which a cstring has not. ⭐ One deliberate divergence from BOTH: **`next()` after `close()` throws `closed`** — neither reference throws it *and neither guards*, so the unguarded sequence reaches Win32 with a NULL handle while the pending flag still claims an entry and **replays the first entry as found**; `__ManagedFile` already throws `closed` from the identical guard, so this applies shv2's family contract to a hole both references share. ⭐⭐ **THE CANONICAL CORPUS CANNOT SEE THIS MECHANISM'S TWO MAIN FAILURE MODES — verified, not inferred: all 10 upstream cases pass with `filename()` ENTIRELY UNIMPLEMENTED** (the only iterating case counts entries and never reads a name), **and `search-and-list` is blind to a broken advance-first pending flag** because a `dir/*` search's first two entries are `.`/`..`, discarded either way. 9 shv2-authored cases were added to close that, each sabotage-proven. Measured controls: pending flag cleared ⇒ a 2-file directory lists as **1**; `__md_close` removed from the destructor ⇒ **5,880 handles in five seconds** against a correct **50** at 400,000 searches — the negative control the exit-101 gate structurally cannot give, since a HANDLE is not an allocation (and the leak gate was separately checked for VACUITY: leaking the path scratch does exit 101). ⭐⭐ **The review's blocker is this project's signature bug at its purest**: `FindBlockPendingOffset = 328` / `FindBlockBytes = 336` were hard-coded literals that ARE `8 + FindDataBytes` — and `FindDataBytes` had been exported **by this rung's own refactor commit, expressly for this family, then never read**. Correct the record size where `stat`/`exists` allocate by, and the block silently under-allocates while the kernel writes the find record's tail over the pending flag on every `FindNextFileA`. Now derived. ⚠ **Measured debt, widening R4.4's** (see `docs/optimization-log.md` 2026-07-30): `managedDirectoryThrows()` is a **fifth** `String.from(<TypeName>)`-per-`throwsOf` speller — not a new debt, but when R4.4's recorded `E2045` trigger fires the fix covers five sites, not three. Ladder **flat +125 per compile** (a fourth builtin error enum); `phase:signatures` and `phase:regalloc` reproduce R4.4's numbers **digit for digit at all six rungs** | L-stdlib | ✅ DONE | `slice/R4.3-managed-directory` | 2026-07-30T14:38Z |
| **D12** | **`E3057` NAMES THE WRONG CONSTRUCT for two builtin families.** `__ManagedFile.openRead(…)` without a `try`, and `String.findFirst(…)` without a `try`, both report **"throwing array accessor requires try: … drops the out-of-bounds error"** — correct code, wrong noun, wrong explanation, about a construct the program does not contain. Cause: `validatePlainCall`'s family selection was a **binary** `GT ? … : array` expression whose own comment warned *"A THIRD family added to `isThrowingRuntimeCallee` must add its own arm here — otherwise it would silently inherit the array wording"*; **three families joined without one**. ✅ **R4.3 fixed the STRUCTURAL half** — recast as an explicit chain and added the directory arm, so a new family can no longer inherit a wording by accident — and deliberately left the two wrong wordings, because each needs its own callee → source-method-name map and its own spec case, and both belong to R4.1/P1.8-B2's surface. **Verified still wrong by RUNNING it, and no golden pins the bad wording**, so it is cheap; it is a row because it is two families' nouns, not one fix  ✅✅ **CLOSED 2026-07-30** (main `d83d76028`; **2816/0**, x64-linux PASS, wasm PASS, arm64 **SKIP — remote, UNVERIFIED**; fragment ADDITIONS only, **zero goldens modified**, ladder **delta 0 at every rung** against a same-checkout A/B). ⭐⭐ **THE ROW UNDERCOUNTED: it is 17 THROWING CALLEES ACROSS THREE UNARMED FAMILIES, not two families' nouns** — `__mf_` (8), `__ManagedMemory` (7, which hides INSIDE `isThrowingArrayRuntimeCallee` and so inherited its wording), `__strix_` (2). Each now names the method the author wrote, through a per-family map onto the PARSER'S OWN method constants (never a fresh literal), composed by `GtRuntime.runtimeCalleeSourceMethod` beside `isThrowingRuntimeCallee` (THAT it throws) and `runtimeThrowsClause` (WHAT it throws). ⭐ **AND THE SENTENCE HAD FIVE INLINE COPIES, NOT FOUR** — the fifth was the ordinary user-function arm nobody had counted; it is now spelled ONCE in `SemanticCheck.throwingCallRequiresTryMessage`. ⚠⚠ **R4.3's STRUCTURAL FIX HAD ASSERTED A GUARD THAT DID NOT EXIST**: its comment said a new family *"falls to the LAST arm, which names the callee rather than pretending to know what it is"* while the last arm WAS the array wording, which pretends exactly. The chain now ends by naming the callee, so a sixth family gets an honest message and the comment is true. ⭐⭐ **THE RUNG'S OWN DEFECT CLASS SURVIVED ONE LEVEL INSIDE ITS OWN FIX, and the review caught it**: `openWrite` and `openWriteExecutable` BOTH emit `__mf_open_write` (`Parser.maxon:24022`), so **the callee is a LOSSY KEY** and no map over it can be injective — shipping D12 as first written would have quoted `'openWrite'` at an author who wrote `openWriteExecutable`. Cured by a `sharedByTwoSpellings` union case whose sentence is TRUE of either spelling, rather than by minting a second runtime entry point to carry a diagnostic word. An **exhaustive** hunt for an N+1th found exactly one other two-spellings-one-callee arm (`count`/`length` → `__arr_count`) and it is in no throwing predicate, so it can never reach an E3057. ⭐⭐ **AND THE FAMILY'S THIRD LIST IS NOW HELD AT INSTALL.** Nothing tied each map to its own `isThrowingXRuntimeCallee` predicate — this rung took that pairing from ONE instance (R4.3) to FOUR — and the failure mode was the worst shape available: a future rung adding a throwing callee without a map arm would have turned **a USER'S missing `try`** into a compiler panic. Each installer now asks its own map for every throwing builder it installs, so **builders ≡ predicate ≡ map in ONE traversal with NO PARALLEL COLUMNS** — the shape `BuiltinErrorOrdinalCheck`'s header records 7b getting wrong first. **COORDINATOR-VERIFIED BY SABOTAGE**: delete the `findLast` arm, rebuild, and a program using `findLast` **correctly** (`try … otherwise`) dies at install naming the family, the missing callee and the fix; before the guard that same program compiled clean. ⚠ **The guard's scope was MEASURED, not assumed** — my first sabotage deleted an arm the probe program never installed and nothing fired: it is per-INSTALLED-builder, so drift is caught by any program that USES the callee. The forward arm is total for file/directory/string-search/buffer and deliberately NOT total for the nine genuine `Arr*` accessors, which are worded as a FAMILY on purpose and pinned by `try-postfix-target.error.two-throwing-calls-in-one-chain`. ⚠ 4 file-private `let`s moved `Parser.maxon` → `SignatureIndex.maxon` (visibility only; every reader re-verified) so the maps point at the parser's constants instead of second spellings — the only touch to a file another agent holds, in a declaration block thousands of lines from its claim. ⚠ **A LADDER'S ABSOLUTES ARE A PROPERTY OF THE CHECKOUT PATH AS WELL AS THE TREE**: this row's delta against R7's read +14 allocations flat purely because R7 was measured in a worktree; a same-checkout A/B against the parent reads 0. Extends 7b's rebase lesson | L-types | ✅ DONE | `slice/D12-throwing-call-family-noun` | 2026-07-30T22:35Z |
| **7b** | **A CROSS-FAMILY ORDINAL DRIFT GUARD for the builtin error enums.** Each `__Managed*Error` wire format exists in **three places with nothing tying them**: the case NAMES (`SignatureIndex`), the case ORDER (`Project.builtin*ErrorEnum`, where the ordinal is positional), and the ordinals as **literals** in the runtime's throw sites (e.g. `ManagedDirectoryRuntime.maxon:130-136`, `ManagedFileRuntime.maxon:67-75`). Drift ⇒ **every handler arm names the wrong case, silently**. ⚠ **Pre-existing across FOUR families** (`__ManagedFile`, `__ManagedMemory`, `__ManagedDirectory`, `ArrayError`) — which is exactly why the R4.3 review reported it instead of fixing one family and creating the asymmetry this project dislikes. Today's correspondence is well-checked but by coincidence of coverage: for the directory family ordinals 0, 5 and 7 are each pinned by a committed `match` arm, so a shift breaks 5 and 7 — **but nothing pins `nextFailed`/`accessDenied`/`openSearchFailed`/`currentPathFailed`**, which need a mid-iteration OS failure nobody could provoke. The guard is one mechanism for four families; `installManagedFileRuntime`'s existing bidirectional drift check is the shape to copy ⭐ **SURVEYED 2026-07-30 (read-only, lane untouched) — THE THREE PLACES ARE NAMED AND THE GUARD IS SPECIFIED.** (1) the case ORDER is positional `addBuiltinEnumCase` calls in `Project.builtinManagedFileErrorEnum` / `…DirectoryErrorEnum` / `…MemoryErrorEnum` / the `ArrayError` twin — that call ORDER *is* the wire format, as those functions' own headers say; (2) the case NAMES are `Managed*…CaseName` constants; (3) the ordinals are LITERAL `let Managed…Ordinal = N as ParsedInt` in the runtime throw sites (`ManagedDirectoryRuntime.maxon:140+`, `ManagedFileRuntime.maxon:67-75`). Nothing connects (1) to (3). ⭐ **THE MODEL IS EXACT**: `installManagedFileRuntime:998/1001` — a BIDIRECTIONAL pair of panics naming which side drifted. The analogue: at install time, per family, assert each runtime ordinal constant equals its case's POSITION in the layout, and that every declared case is claimed. ⚠ **ONE EXPLICIT EXCEPTION THE GUARD MUST ALLOW**: `__ManagedDirectoryError.iteratorInvalid` (ordinal 4) is DECLARED but deliberately never thrown (`filename()` is non-throwing in both references), so it has no runtime ordinal constant and a naive "every case is claimed" check false-positives on it. ⇒ a stated declared-but-unthrown allowance, not a hole. ⚠ The premise is corroborated from a third direction: `ManagedDirectoryRuntime`'s own header records the BOOTSTRAP's comment listing SEVEN cases where the enum has EIGHT (omitting `iteratorInvalid`, shifting every later ordinal), calling it *"the THIRD reference comment found stale about its own enum"* — the drift this row guards against has already happened in the reference ⚠ **LANE CORRECTED 2026-07-30 — this row is NOT lane-free, and I nearly claimed it as one.** It edits the case NAMES in `SignatureIndex.maxon`, which is **`L-types`** (`SignatureIndex.maxon` · `TypeResolution.maxon`) — live to T1 as I write this. A row whose lane reads `—` because its work spans several files is exactly how a claim collides while its author follows the board correctly. ⚠ **AND MY FIRST WORDING OF THIS FIX WAS TOO STRONG — corrected 2026-07-30.** I wrote *"if a row touches a file some lane names, it is IN that lane"*. **That is stricter than this board, and it would freeze the board solid**: the lane table's own text says three lanes share `Parser.maxon` and are expected to, because *"their sites are thousands of lines apart so they rebase"* — the second lander just re-runs its gate battery. **The real test is whether two rows would FIGHT OVER THE SAME CODE**, which is what a lane means (*"a lane is a set of rows that would fight over the same code"*). ⇒ **A row spanning several files names the lane whose CODE it actually edits, and lists the rest** — as this row now does. `—` still may not mean "I did not think about it" | **L-types** *(via `SignatureIndex.maxon`'s case names — also touches `Project.maxon` and the four `*Runtime.maxon` files, which no lane claims)* ⚠ **CLAIMED 2026-07-30T19:40Z, AFTER T1 RELEASED L-types.** R5 is live in `L-parser-decl` and its lane note says it READS L-types; this row WRITES it (the builtin error-enum case names in `SignatureIndex.maxon`), so the two are not a lane collision but they do share a file — **whoever lands second re-gates on the merged tree**, and the regions are disjoint (builtin `__Managed*Error` case-name constants here, associated-type substitution there). ✅✅ **CLOSED 2026-07-30.** x64-windows **2794/0** (2758/0 when first written — rebased onto D9, which added 36 cases), x64-linux **2665/0**, wasm32-wasi **2656/0**, arm64 **SKIP — remote, UNVERIFIED**; builds 0 warnings, no 101, fragment additions only, **zero goldens modified on any lane**. ⭐⭐ **THE RED WAS MEASURED, AND IT IS THE WHOLE ROW**: swap `accessDenied` and `openSearchFailed` in `builtinManagedDirectoryErrorEnum` and leave the runtime literals alone, and the suite is **2758/0 GREEN** — the two cases silently trade identities and every handler arm for them names the wrong error, with no crash and no wrong exit code. After the guard, that same swap **stops the compiler on the first program compiled**: *"'accessDenied' is ordinal 2 … but installManagedDirectoryRuntime throws it as 1 — the enum's case ORDER is the wire format and the two have DRIFTED"*. **One mechanism, `BuiltinErrorOrdinalCheck`, wired into all FOUR families** (`__ManagedFileError` 9 claims · `__ManagedDirectoryError` 7 · `__ManagedMemoryError` 5 · `ArrayError` 2), so no family gets the asymmetry the R4.3 review warned about. ⭐ **THE BETTER CURE WAS TRIED FIRST AND IS UNAVAILABLE, WHICH IS WORTH RECORDING**: `orderingTagOf` already reads a builtin enum's tag OFF the layout, and its header says the bootstrap *"spells 0/1/2 as literals at its consumer sites with nothing tying them back to the declaration; shv2 deliberately does not"* — these families are doing exactly what that comment disclaims. It cannot be copied here: the ordinals feed `…Flag = …Ordinal + ErrorFlagOrdinalBias` chains that are themselves global `let`s, and **a global initializer may hold no function call (E2045)** — the same constraint 7b2 hit hours earlier. Deriving would mean threading the layout to every throw site, which is a different rung. So the fact stays written twice and the guard makes disagreement LOUD. ⚠ **THE GUARD IS FORWARD-ONLY, AND THAT IS A DECISION RATHER THAN HALF THE JOB.** The `installManagedFileRuntime` check this copies is BIDIRECTIONAL because both its sides must be total; here they must not be. Three of the four families legitimately DECLARE cases their runtime never throws — `__ManagedDirectoryError.iteratorInvalid` (ordinal 4, *"filename() on an exhausted iterator"*, non-throwing in both references), and `__ManagedFileError`/`__ManagedMemoryError` each declare more than they throw — so a backward *"every case is claimed"* arm would fire on all of them. The forward arm is COMPLETE for the failure guarded: any insertion, deletion or reorder that moves a case somebody throws moves it off its claimed ordinal. ⭐ Also found while wiring: `ArrayError` and `__ManagedMemoryError` SHARE their case-name constants without sharing an enum (the first two cases coincide BY DESIGN), so each is asserted against its own layout rather than one being assumed a prefix of the other ⭐⭐ **THE REVIEW FOUND THE GUARD HAD REINTRODUCED, IN ITS OWN CALL SHAPE, THE EXACT FAILURE IT EXISTS TO CATCH** (`a099c95e8`). It first shipped as a free function taking two PARALLEL COLUMNS — a name array and an ordinal array pushed in lockstep at four call sites — plus a runtime `count() != count()` panic *apologizing for that shape*. **That panic catches only the ASYMMETRIC mistake.** It cannot see a name and an ordinal pushed in the wrong ORDER, nor a claim dropped from each column for DIFFERENT cases: both leave the counts equal and silently re-pair every claim below them — which is ordinal drift, in the drift guard. The decisive argument was already in the same file fifteen functions up, in `EnumLayout.addCase`'s own header (*"a caller that pushed a name and forgot a tag would shift every later case's tag onto the wrong case"*): **the rung violated its own file's established rule for parallel columns.** Cure: `BuiltinErrorOrdinalCheck.create(declaration, installer:)` + one `.claim(caseName, ordinal:)` per claim, checked on the spot. **There are no columns**, so the lockstep panic and both `try…otherwise panic` OOB arms are deleted as UNREPRESENTABLE rather than unlikely; each site drops from 2N+3 lines to N+1 and the four near-identical preambles are gone. Measured −20 allocations / −1,152 bytes, flat at all six rungs. ⭐ Also removed: the rung's `noSuchCase` arm was a verbatim third copy of `indexOfCase`'s "absent" convention (`index >= caseCount()`); it now asks `hasCase`, which already owns it ⭐ **THE PANICS ARE UNREACHABLE FROM USER SOURCE, AND THAT IS WHY `panic` IS RIGHT HERE RATHER THAN A DIAGNOSTIC** — all four layouts come from the `builtin…ErrorEnum()` CONSTRUCTORS, never an `enumTypes` lookup, and every claim is a compiler constant, so the verdict is a property of the compiler BINARY alone: it panics on the first program that reaches lowering, or on none. `requireUnreservedName` (E2051) additionally refuses all four `__`-prefixed names to user source ⭐ **COORDINATOR-VERIFIED RED, not taken on report**: swapping the two cases and rebuilding, a **three-line `main` that touches no directory at all** dies with *"'accessDenied' is ordinal 2 … but installManagedDirectoryRuntime throws it as 1"*. One nuance the prose had slightly wrong and this row now states correctly: a program with a SEMANTIC error (`returns int` ⇒ E3002) never reaches the install, so the guard fires on the first program that reaches **lowering**, not the first invocation ⚠⚠ **AND THE REVIEW CAUGHT THAT THE ROW'S OWN LADDER READING WAS STALE BY ONE REBASE** — 7b ran `scale-test` before rebasing onto D9 and did not re-run it, so its six ABSOLUTE columns were `+36/+60/+108/+204/+396/+780` low, *digit for digit* D9's own recorded `Self` cost, and the next rung's delta column would have read D9's work as its own. **A REBASE INVALIDATES A LADDER READING EXACTLY AS IT INVALIDATES A SUITE COUNT**: a row's ATTRIBUTION survives it (it is a difference against its own parent), its ABSOLUTES do not (they are a property of the whole tree). Re-measured post-rebase and post-review; a coordinator re-run then read **delta 0 at every rung**, confirming the logged row bit-for-bit. Allocation ratios ×1.78/×1.88/×1.93/×1.96/×1.98 per doubling — linear, no bend, so **no step-6b optimizer trigger fired** | ✅ DONE | `slice/7b-error-ordinal-drift-guard` | 2026-07-30T19:40Z |
| **7b3** | **`orderingTagOf` SHOULD BE AN `EnumLayout` METHOD, AND IS A FREE FUNCTION.** Opened by the 7b review (2026-07-30), which needed exactly this convention and deliberately did NOT add a third copy of it. `orderingTagOf` reads a builtin enum's tag off the layout and re-spells `indexOfCase`'s "absent" convention as `index >= caseCount()` — a convention `EnumLayout.hasCase` already owns. Moving it onto `EnumLayout` puts the tag-off-a-layout read beside `hasCase`/`tagOfIndex`/`indexOfCase` and deletes the free function; doing it in `Project.maxon` ALONE would leave a thin wrapper, which the code-quality rule forbids, so it is all-or-nothing. ⚠ **BLOCKED ONLY BY LANE, NOT BY DIFFICULTY** — the two callers are `Parser.maxon:20484-20485`, and 7b was told not to touch `Parser.maxon` while D9 held it. **~3 lines plus the two call sites.** Not a defect and nothing is silently wrong today: pick it up whenever the Parser lane holding those two lines is free. ✅✅ **CLOSED 2026-08-01** (BATCH5, main `ecb805485`) — `orderingTagOf` is now `EnumLayout.tagOfCase`, beside `hasCase`/`tagOfIndex`/`indexOfCase`, and the free function is deleted, so the “absent” convention is written once rather than re-spelled as `index >= caseCount()`. It rode a batch because it was never hard, only lane-blocked | **L-parser-\*** *(whichever lane holds `Parser.maxon:20484-20485`; also `Project.maxon`, which no lane claims)* | ✅ DONE | `slice/BATCH5-witness-ordinals` | 2026-08-01T08:51Z |
| **7b4** | **TWO MORE RUNTIME FAMILIES TRANSCRIBE ORDINALS AS LITERALS, AND 7b's MECHANISM STRUCTURALLY CANNOT REACH THEM.** `GtRuntime.maxon:353` (`RunProcessError`) and `StringIndexRuntime.maxon:69` (`StringError`) spell their ordinals as literals exactly as the four `__Managed*Error` families did — but their enums are **user/stdlib-DECLARED**, so there is no compiler-owned `EnumLayout` for `BuiltinErrorOrdinalCheck` to check a claim against. That is why 7b's scope is *"four families"* and not six: four is exactly the set where a compiler-owned declaration exists to be the authority. ⭐ **NOT LIVE TODAY, AND THAT WAS PROBED RATHER THAN ASSUMED** (7b review, 2026-07-30): shv2 currently REFUSES `otherwise (e)` + enum-case `match` for these (`E2015: a match pattern naming 'notFound' … enum-case patterns arrive in a later wave`), so no handler arm can name the wrong case because no handler arm can exist. ⚠ **IT BECOMES LIVE THE MOMENT `throwsTypeName` STARTS BEING READ FOR RUNTIME FUNCTIONS** — that is the re-measure trigger, and the cure will need a different mechanism (the declaration is the user's, so the check has to run after the enum is resolved, not at install time) | **L-gt-runtime** *(`GtRuntime.maxon` · `StringIndexRuntime.maxon`)* | ⬜ FREE *(dropped from BATCH5 at plan time — not live, and its cure needs a post-resolution check)* | — | — |
| **R4.4** | **`__ManagedMemory` AS A TYPE OF ITS OWN — a raw buffer with a RUNTIME element size.** Opened by R4.2, which measured its way into it rather than predicting it. Today `__ManagedMemory` is a builtin generic ALIAS to the `Array with Byte` instance (`SignatureIndex.maxon:4108`), and **two independent corpus facts say that binding cannot hold**: `specs/managed-memory-builtin.md` — **11 canonical cases, ALL enabled upstream, NEVER PORTED to shv2** — is written against `create(4, elementSize: 8)` and then reads `get`/`set` as 8-byte words; and `specs-shv2/array-slots.md`'s 4 disabled cases reach `.managed` on an **`Array with Slot`**, a struct element type, where R4.1 gated the door to the byte instance only (`Parser.maxon:24469-24472`). ⭐ **R4.2 left the seam in exactly one place on purpose: `create(count, elementSize)` takes the element size, validates it and STORES it into `element_size@24`** — it is honest rather than hardcoded — while the RESULT's static type stays `Array with Byte`. At the `1` every R4.2 path passes, the two agree; so this rung changes a **binding**, not a signature. Acceptance: port `specs/managed-memory-builtin.md` whole (11), flip `array-slots.md`'s 4, and flip the 2 `__ManagedMemory` cases R4.2 ported-and-disabled in `builtin-type-checking.md`. ⚠ Also yours: the **`.managed` BINDING door** (`let b = …; b.managed` — the value door works; the binding form falls to `structLayoutOfType`'s builtin E2015 at `Parser.maxon:26201-26246`), and `buildArrSetLength` **detaching a view BEFORE its negative-length check**, so `setLength(-1)` allocates and then throws — named by the R4.2 review, not changed there because reordering touches the "capacity read after detach" invariant its own comment calls load-bearing ✅✅ **CLOSED 2026-07-30** (main `491039768`; x64-windows **2666/0**, x64-linux PASS, wasm PASS, arm64 SKIP — remote, UNVERIFIED; fragments empty-diff after a suite run). **26 acceptance cases, all green, none left disabled**, incl. a new byte-identical port of `specs/managed-memory-builtin.md` (11, never previously ported). ⭐⭐ **THE DESIGN IS PROVENANCE, NOT A NEW TYPE — and the row's "change that ONE binding" was the wrong instinct.** `__ManagedMemory` and `Array` stay ONE record and ONE `GenericInstanceId` (`stdlib/Array.maxon:104-106` implements `Array` *on* `__ManagedMemory with Element`, so for a compiler that SYNTHESIZES that record they are one thing); the two SURFACES are told apart by a sparse per-function set marking values minted as a buffer, carried one dispatch hop. A distinct generic base would have minted two ids to reconcile at `String.init`, `.toByteArray()`, `valueIsByteArray` and every `__ManagedFile` parameter. ⭐ **Only 2 of the 12 members needed a runtime** (`grow`, `byteAt`); a 3rd (`append`) has one and MAY NOT USE IT — `__arr_append`'s internal grow ABORTS where a buffer must throw. ⭐⭐ **THE REVIEW FOUND TWO SILENT WRONG ANSWERS, both invisible to a green 2663-case suite**: (a) `ArrayError` was SEEDED but never RESERVED, so a user's own `enum ArrayError` compiled and the runtime's `indexOutOfBounds` routed into the user's first arm — **exit 11, no diagnostic**; (b) `create`'s elementSize→instance typing had **no `else`**, so `create(4, elementSize: 4)` took the machine-word instance, accepted `set(0, value: 5000000000)`, stored 4 bytes and read back **705032704**. ⭐⭐ **And the duplication it fixed had NO TEST: with the mark-detach sabotaged the whole suite stayed GREEN**, so it constructed the leak (ValueIds restart per function) and committed it — a program that writes a byte through a `String`'s buffer. ⚠ **R4.2 IS THE ROW THAT PAID FOR THIS ONE'S FINDINGS**: it shipped `create` publishing length = `count` instead of 0, and `setByte` bounded by LENGTH instead of CAPACITY — two halves of one coherent pair, both wrong, and `create-and-length` (a canonical case R4.2 never ported) is what caught them. ⚠ Cross-target went RED once on stale x64-linux goldens and is recorded in the landing commit: two of them were stale against `main`'s OWN spec source BEFORE this rung, invisible because **a `disabled-test:` case's golden is never checked** | L-stdlib | ✅ DONE | `slice/R4.4-managed-memory-object` | 2026-07-30T12:14Z |
| **R4.6** | **`__ManagedMemory.set` should bound by CAPACITY, not length** — the THIRD piece of the length/capacity pair whose other two halves R4.2 got wrong and R4.4 fixed. `specs/managed-memory-methods.md:21` documents `set(index, value)` as capacity-bounded and `get` as length-bounded, the same asymmetry as `setByte`/`byteAt`. shv2 folds the buffer's `set` onto `__arr_set`, which is length-bounded **and must stay so for `Array`** — so this needs `__arr_set` split, which is a slice and not a review edit. ⭐ **It is currently a REFUSAL, not a wrong answer** (`create(4, 8)` then `set(0, value: 7)` is refused), and **no committed case needs it** — every corpus case publishes with `setLength` first — which is exactly why it is a row and not a blocker. Found by the R4.4 review while verifying the other two halves ⚖ **USER RULING 2026-07-30 — CAPACITY, AND IT SETTLES A THREE-WAY CONTRADICTION. DO NOT RE-LITIGATE FROM THE CANONICAL LINE.** Measured before asking: **`specs/managed-memory-methods.md:21` says `setByte` panics if `index >= length × elementSize`**, and **`stdlib/File.maxon:117-127` BEHAVES that way** — it does `setLength(len+1)` before `setByte(len, 0)`, commenting *"at the length boundary, so temporarily extend length to allow setByte at that index"*, a dance that is pointless under capacity-bounding. Against that, **v1 `Internals.maxon:3527-3530`**: *"Byte writes are bounded by CAPACITY (the allocated region), NOT length — mirroring `__managed_mem_set`."* **The runnable oracle CANNOT ARBITRATE** — `E9001` lowering crash on the discriminating program (`create(4,1)`; `setLength(2)`; `setByte(3,…)`), the same bootstrap bug class as R4.1's standalone `__ManagedFile` crash. ⇒ **RULED: capacity. v1 followed, the canonical `setByte` line treated as STALE, R4.4's shipped behaviour stands.** ⭐ **What makes it coherent is `setLength`'s OWN canonical doc**: *"callers stage the new elements FIRST and use `setLength` to publish them"* — staging is only possible if writes may land in `[length, capacity)`, so capacity-bounding is what makes `setLength`'s documented contract implementable at all. **Nothing in either corpus tests that round trip**, which is this rung's best acceptance case. ⚠ **And the case that LOOKS like it already pins this does not**: `managed-memory-error-variants`' `setByte(4,…)` on a capacity-4 buffer is out of range under BOTH readings, so it discriminates nothing — a bounds case that does not fail under the rival reading is decoration | L-stdlib ✅✅ **CLOSED 2026-07-30** (main `06996db3a`; x64-windows **2734/0** on the merged tree, x64-linux PASS, wasm PASS, arm64 SKIP — remote, UNVERIFIED). `set` is capacity-bounded via a buffer-only `__arr_mem_set`, routed by the provenance answer R4.4 already computes — **no second discriminator was needed**, which is the design's own confirmation. `Array.set` keeps `__arr_set` and its length bound, byte-for-byte. ⭐⭐ **THE RUNG'S REAL OUTPUT WAS THREE DEFECTS, AND ONLY ONE WAS ITS OWN.** (1) **R4.4's write guard detached CONDITIONALLY** — a hand-written copy of `__arr_cow_detach`'s FIRST arm only, so `setByte` through the owner of a **viewed** buffer wrote straight through the sharing: **198 where the answer is 110**. (2) **`Array.set` LEAKED the managed element it was handed on the out-of-range path** — the parser MOVES the value in and the throw path owned nothing (exit **101**; the `Array with Int` control exited 0). Both pre-existing, both fixed rather than filed, because `__arr_mem_set` would otherwise have shipped the identical leak on day one. (3) **`.managed.setByte` writes a raw byte INTO A LIVE POINTER** (exit 101) **and `.managed.byteAt` reads one back AS DATA** (returns a nonzero address byte) — refused by **E3110**. ⭐⭐ **THE COVERAGE LESSON, and it is the sharpest of the session:** the fix to (1) was correct but pinned only ONE of the guard's two callers — the same program written against the `set` this rung ADDED returned the identical wrong **198** with nothing watching. **One guard, two callers, one case.** ⭐⭐ **And the reader is always the half that survives**: `setByte` corrupting a pointer exits 101 and announces itself; `byteAt` reading one is silent — no crash, no leak, no gate — so gating only the writer would have left it green forever. That is why E3110 covers both, and the sabotage that proves it is removing the `byteAt` gate alone. ⚠ **R4.6's own leak** — this rung made `[length, capacity)` user-writable, so staging a MANAGED element and never publishing it leaked in five lines of ordinary-looking code (`reserve(4)`; `set(0, "x")`; drop ⇒ 101). **RULED: REJECT, not fix** (E3109) — rejecting keeps the capacity-slot invariant TRUE, which five mechanisms depend on, costs nothing on `push`'s hot path, and gives up a capability with **no consumer** (`setLength`'s own doc names its stagers, and every one is compiler-internal; `__ManagedMemory.create` can only ever yield a TRIVIAL element). ⭐ **A false-reject hunt found the rule too NARROW rather than too wide** — a `struct` element leaks identically to a `String` because it is boxed — so the predicate is "managed", not "String", pinned as its own case. ⚠ Accepted divergence: buffer `get`/`slice` still throw `ArrayError` while the rest throw `__ManagedMemoryError`, so one `throws` clause cannot carry both — measured to give a clear `E3059` with a workaround, and a lateral move (`setLength`/`setByte` were already on the other family). Its own rung. ⚠ **Bootstrap oracle bug found and NOT this rung's:** writing `callee: ArrMemSetName if isManagedMemory else ArrSetName` — a shape `Parser.maxon` already uses elsewhere — fails the whole build with `E9001 … assign value %97103 (kind=MaxonInteger) not in valueMap` inside `MaxonToStandardConversion`. Reproduced by reverting the 4-line workaround; the obvious hypotheses were tested and REFUTED, so no mechanism is claimed. It needs the full C# suite as its gate ⇒ **bootstrap-oracle list, not a shv2 rung** | ✅ DONE | `slice/R4.6-managed-memory-set-capacity` | 2026-07-30T16:46Z |
| **B2** | ✅✅✅ **CLOSED 2026-08-01 (`a90dc2f10`). THE PARK WORD IS NOW THE ONLY CHANNEL A WAITER MAY LEARN READINESS THROUGH, AND THE COMPLETER'S OWNERSHIP IS VISIBLE IN IT.** Two defects, and **the second is the first one MOVED UP A LEVEL** — which is the thing this row kept predicting about itself. **(1) THE RELEASE RULE.** A completer published `status = ready` and THEN claimed the word, so a waiter that self-detected on `status` inside that gap RELEASED a word a completer was still in flight toward; it armed its NEXT I/O and the late claim landed on THAT park's `Wait`, so that park aborted against a completion which never happened. Closed by a fifth label **`__netpoll_woken`** — 3 arm64 sites (all inside `EmitGtParkForIoCompletion`) and 5 x86 `_mainthread_loop`s. `maxon_sleep` deliberately untouched: it has no park word, and giving it one is a separate row. **(2) THE RELEASE RULE ALONE LEFT OWNERSHIP INVISIBLE.** The word read `Parked` through an entire healthy publish — which *is* `__netpoll_recover`'s suspicion condition — so the net rescued merely-SLOW completers and their late claims landed on the next park. Closed by a fifth STATE **`Claiming`**: CLAIM → PUBLISH → RELEASE, so `Parked` + published-readiness is unreachable **by construction rather than by timing**. ⭐ It is a fifth STATE and not a reordering because a waiter claimed from `Wait` is **still running**: storing `Ready` first would let it leave the park before the results were written. ⭐⭐ **THE ACCEPTANCE DISCRIMINATES, AND THE CONTROL IS WHAT MAKES IT A MEASUREMENT.** `scripts/netpoll-park-driver/` under `MAXON_GT_CLAIM_DELAY_MS=5`: **0/5** (every run exit 101, leaking 54–67 allocations) → **0/10** (leaking 1–4) → **10/10**. `MAXON_MAX_PROCS=1` read **5/5 at every stage** — same injected sleep, one poller, green — so the cause is ownership ACROSS Ms, not the sleep. Park-delay arm and no-injection arm 10/10 throughout. ⛔ **THE INSTRUMENT WAS TWICE NEARLY WORTHLESS AND BOTH TIMES IT WAS CAUGHT.** `run.sh` scored on exit code + line count only, so a regression the recovery net rescued CLEANLY would have been reported `ok` — 4 of 5 red runs were caught solely because they ALSO leaked; a third verdict arm now fails any run that prints the net's warning. And the coordinator moved the completer knob to the claim's TAIL arguing the HEAD "cannot fail for the thing it is aimed at" — **the review falsified that with an experiment** (injected fourth-state regression, 10 iterations each: head **0/10**, tail **0/10**, correct protocol with both armed **10/10**). The head arm discriminates via the WAITER, not the net. Both ends now fire; neither subsumes the other. ⛔⛔ **AND THE REVIEW FOUND THE INJECTION KNOBS WERE DEAD ON x64 ENTIRELY** — `ReadEnvUnsigned` zeroed `dest`, which IS RAX, before testing `GetEnvironmentVariableA`'s return, so both variables read 0 on Windows. **B3 would have run an unarmable harness**; that is now fixed and B3 is viable for the first time. Also found and fixed: `maxon_net_tcp_connect` published `Parked` BEFORE clearing `ioYielded` (the 1 of 5 families that drifted — a completer spins on the stale 1 and enqueues a still-running GT onto a stale sp); `__gt_try_await_sched` never got `__netpoll_recover`; `__netpoll_recover` dereferenced its candidate OUTSIDE the lock its own comment invoked; and its 10 ms time gate was a load-compare-store on a global **every M** races, so two could pass in the same instant and the second would "confirm" a sighting microseconds old — now a CAS. ⚠ **THE x86 FIXES ARE UNEXECUTED — arm64-macOS host.** They were verified by reading. **That is B3's job.** ✅ **GATES:** `dotnet build` 0 warnings · C# **3197/0** · wasm32-wasi **2690/0** · no exit 101 · **codegen-neutral** (empty golden diff at the rung's own base, shv2 2696/0 there) · scale ladder linear throughout, **attributable delta EXACTLY ZERO** by same-path A/B against the true merge-base (`bc1625002` — and the first baseline, taken against the wrong tree, read a plausible +5 flat that would have been misattributed here). ⚠ **NO NEW SPEC CASE, A STATED EXCEPTION:** a suite run buys ~15 chances at this race; the driver takes ~80 protocol traversals in 2 s, which is why the suite stayed green over this bug for two revisions. The committed instrument is the driver + `run.sh` + both knobs. ⚠⚠ **shv2's arm64-macos suite reads RED at 110 on this host, and it is PRE-EXISTING — proven by ISOLATION, not asserted:** plain `origin/main` without B2 reads **111** on the same box, the same 7 C# arm64-macos fragments go `M` either way, and the "fails on B2 but not on main" set is **EMPTY**. Stale arm64-macos goldens from x64-developed shv2 work; filed as its own row for the sweep, deliberately not regenerated inside this merge. ⚠ arm64-macos/arm64-linux remote lanes and x64-linux (no WSL on a Mac) **SKIP — UNVERIFIED, not proven good**. ///// **THE TRAIL BELOW IS THE REASONING THAT GOT HERE, KEPT DELIBERATELY:** ⛔⛔ **THE GT I/O PARK IS A TWO-WORD HANDSHAKE, SO A WAKEUP CAN STILL BE LOST — AND THIS ONE IS *WORSE* THAN B1, NOT A LEFTOVER OF IT.** Opened 2026-07-30 by B1, which narrowed the race ~200× and **reported the remainder rather than claiming the wedge closed**. ⚠⚠ **RARITY IS THE SEVERITY, NOT A MITIGATION — THIS IS THE WHOLE ARGUMENT.** B1 was tractable *because* it was frequent: 2/75 meant reproduce in an afternoon, capture `sample` stacks, and build a SIGKILL probe that discriminated between two hypotheses. At the residual's ~0.005%/run (**~14,000 runs per occurrence**) **every one of those tools is unavailable** — no reproduction, no capture, no probe, no A/B. ⇒ **we fixed B1 with a method that cannot fix B2.** And it is **NOT milder**: identical lost wakeup, identical parked-forever GT, identical hang. Only the frequency moved. ⛔ **IT IS ALSO NOW THE ONLY SUSPECT, AND UNFALSIFIABLE**: after B1 any future wedge is either this window or an unknown third bug, and at that rate the two **cannot be told apart empirically** — B1 removed the frequent cause that was *masking* it, so the residual is simultaneously the prime suspect and untestable. ⭐ **THE WINDOW**: ~25 instructions from the pre-park `status` re-check to `ioYielded = 1` inside `__gt_context_switch` — *we load `status == waiting` → completer stores `ready`, loads `ioYielded == 0`, SKIPS the enqueue by its guard (c) → we set `ioYielded = 1` and park.* **Admissible under SEQUENTIAL CONSISTENCY**, so no fence, no reordering and no architecture-specific change touches it; B1's `DMB ISH` is irrelevant here. It cannot be closed by publishing `ioYielded = 1` early (that reintroduces the double-schedule the flag exists to prevent) nor by making the completer spin instead of skip (a self-detecting waiter never parks ⇒ livelock, which is *why* guard (c) exists). ⚠⚠ **AND IT IS CROSS-PLATFORM AND STRUCTURAL, NOT AN arm64 PORT ARTEFACT — coordinator-verified 2026-07-30**: `X86CodeEmitter.Runtime.cs` uses the **same two-word `status` + `ioYielded` handshake** (30 mentions) and carries its own **`OPEN #66`** scar tissue from the same arm→clear family. The race lives in the gap between two words, so it is in the PROTOCOL, and **the fix must land on BOTH emitters** (CLAUDE.md's cross-target rule). ⚠ The rate is **per PARK, not per run** — 0.005%/run is measured on today's workload and grows with self-host and wider fan-out. ⛔⛔⛔ **REVIEW VERDICT 2026-07-31: DO NOT MERGE — THE PROTOCOL STILL LOSES WAKEUPS, FROM THE SIDE NOTHING WAS MEASURING. Sixth rung in a row where the independent review found a reachable defect over a green suite.** ⭐ **THE PARTS PORTED ARE FAITHFUL** — `__netpoll_commit` is a real `CAS(Wait→Parked)` whose false return branches to a resume path on BOTH emitters; `__netpoll_unblock` matches `netpollunblock` arm for arm; and the memory-ordering claim HOLDS (arm64's `AtomicCAS` is `LDAXR/STLXR`, so the success path's store-release orders the completer's prior `status` store and the failure path still takes the acquire) — **retiring B1's hand-rolled `DMB ISH` was correct.** ⛔ **BUT THE PARKER'S FIRST MOVE IS STILL A PLAIN LOAD OF `status`, NOT AN ATOMIC.** Go's `CAS(pdReady→pdNil)` was deleted as unreachable *at arm time* — true, and a different claim. The consequence: **`__netpoll_park_done` accepts `Wait` and swaps it to `Nil`, releasing the word while a completer may still be in flight toward claiming it.** A waiter leaves the park on `status` alone, releases, returns, and arms the NEXT I/O; the late completer's CAS lands on that next park's `Wait`, marks it `Ready`, and **that park aborts a read nobody completed.** *"Nobody claimed it"* and *"nobody has claimed it YET"* are the same instant — **structurally the same mistake as guard (c), which this rung exists to delete, moved one level out.** ⭐ **Go is immune because `pd.rg` is PER-FD and OUTLIVES the park, and a goroutine's only exit from `netpollblock` IS the completer's claim. Moving the word into the GT kept Go's RELEASE RULE without Go's LIFETIME** — the partial-port failure this row was opened to prevent, committed again one level up. ⭐⭐ **MEASURED, with a knob the coordinator identified as missing but never built — stretching the COMPLETER side**: A no injection **5/5 clean** · B `MAXON_GT_PARK_DELAY_MS=5` (the SHIPPED acceptance) **5/5 clean** · C **`MAXON_GT_CLAIM_DELAY_MS=5`** ⇒ **0/5 — 2 WEDGED (60 s, no completion), 3 × exit 101 leaking 58–91 allocations, 3 recovery-net rescues** · D same as C **+ `MAXON_MAX_PROCS=1`** ⇒ **5/5 clean** (B1's own control: one P ⇒ one poller ⇒ the cross-M window unreachable — so the cause is OWNERSHIP ACROSS Ms, not the sleep). ⛔⛔ **AND ARM B'S GREENNESS IS STRUCTURAL, NOT LUCK**: `__netpoll_commit` is only reached when `__gt_dequeue` returns another runnable GT, and with every reader parked the queue is EMPTY, so the reader takes the **idle self-detect branch — which never commits and which the shipped knob never touches.** That branch is where the defect lives. ⇒ **the acceptance this rung shipped is green over its own bug BY CONSTRUCTION**, which is the exact standard `netpoll-inject-gate.sh` was deleted for failing. ⚠⚠ **AND THE COORDINATOR SHIPPED THE SAME CLASS OF BUG IN THE COMMIT THAT DELETED IT**: `9791942b7` committed a `spec-wedge-repro.sh` that `source`s `scripts/wedge-signature.sh` unconditionally at line 42 **without committing that file** — on a fresh checkout `wedgeProgressCheck` is undefined, the verdict is empty, the caller reads empty as *progressing*, and the deadline extends forever. **The wedge detector could never report a wedge.** Verified and fixed by the review (`a9f9cd254`), along with the driver's `total > 0` verdict (cannot tell 1600 from 3), a missing `run.sh` that existed only in a commit message, and a NON-ATOMIC `__netpoll_recovered` counter whose entire job is to be exactly 0. ✅ **THE SEAM IS SOUND**: `IEmitterBackend` gained only primitives, no ownership logic crossed it, the state machine is in one file and both emitters call the same four labels — **epoll will inherit the protocol**, and the reviewer's own fixes reached both targets for free. ✅ Gates re-run with the review's changes: `dotnet build` 0 warnings · C# **3187/0** · host **2696/0** · wasm **2690/0** · **zero modified goldens** · no exit 101 in the suites. ⇒ **NEXT: fix the release rule** — claim-before-publish, a per-arm epoch in the completion ctx, or `park_done` waiting on the claim; **a design call, not a reviewer's.** Then re-verify with **arm C**, which is now the real acceptance. ⚠ Also named for that work: *"a GT with no schedulable stack must never commit"* is spelled TWICE and differently (arm64 `stackBase == 0`, x86 `currentGt == &P->mainThread` at five sites) — assert it inside `__netpoll_commit`; arm64's `__io_complete_gt` is emitted, uncalled, and still carries a pre-B2 `ioYielded` spin that is now **unbounded** since a `Wait` parker can abort and never set the flag; and `__netpoll_suspect` holds a raw GT pointer across a 10 ms gap, safe only because a `Parked` GT cannot be freed. ///// ✅✅✅ **THE ACCEPTANCE NOW DISCRIMINATES, AND THE FIX IS VALIDATED (coordinator, 2026-07-31). `scripts/netpoll-park-driver/` IS THE INSTRUMENT; the suite-based gate never was.** **OLD protocol** (pre-B2 worktree + a 5 ms `usleep` at the exact race point — after the last `EmitBranchIfIoCompleted`, before `__gt_context_switch` sets `ioYielded = 1`): **HANGS EVERY RUN**, 3/3 `exit 124`, and with the RIGHT SIGNATURE — `STAT=S`, 1.5% CPU, **and NO CHILDREN**, i.e. the producers had exited so every byte was delivered and the reader sat parked on a wakeup that was lost. **NEW protocol, same driver, same workload: 15/15 clean, exit 0, ~2,760 injected parks, zero hangs.** ⇒ **the bug is reproducible ON DEMAND for the first time, and the fix demonstrably closes it.** ⭐⭐ **WHAT MADE IT WORK IS THE WORKLOAD, AND SPECIFICALLY THAT THE PRODUCER MUST BE SLOW.** A child dumping 2000 lines at once is drained almost entirely through the SELF-DETECT fast path — **8000 lines across 4 readers hit the park path ~3 times**. Pacing it at ~2 ms per line makes every read find an EMPTY pipe and PARK, and puts the completion a couple of ms later — comparable to the injected window, so the delay can straddle it. **~184 parks per 2.5 s driver run vs ~15 per 6 s suite run (~75×), and it scales with lines and readers.** ⚠ **Build the driver with `./bin/maxon`, NOT shv2**: shv2 cannot lower the P1.5 IOCP ops on arm64 (`StdToArm64Conversion.maxon:505` panics outright), so the async runtime under test is the BOOTSTRAP's — which is where this protocol lives. ✅ **FINAL GATES, all re-run by the coordinator on the final tree**: `dotnet build` **0 warnings**, C# suite **3187/0**, shv2 host **2696/0**, wasm32-wasi **2690/0**, **codegen neutrality: ZERO modified goldens**. ⛔⛔ **STILL OWED BEFORE MERGE — B2 DOES NOT LAND WITHOUT THESE:** **(1) THE MANDATORY INDEPENDENT REVIEW HAS NEVER RUN FOR B2.** Its implementer died three times (two 600 s stalls, one API error) and the coordinator finished the work, so **nobody independent has read this diff** — and on this phase the reviewer has caught a reachable defect on *every* rung, including a SIGSEGV and a silent wrong answer in code an implementer had called done. **(2) DECIDE THE FATE OF `scripts/netpoll-inject-gate.sh`** — it is now *measured* unable to fire, and by its own header *"an injection harness that never fails proves nothing at all… strictly worse than no harness"*. Either repoint it at the driver or delete it; leaving it is a green light wired to nothing. **(3) x64 REMAINS UNEXERCISED** — the port covers both emitters but only arm64 was driven here; the Windows/IOCP arm has no equivalent run. ///// ✅✅ **WHY THE GATE CANNOT FIRE IS NOW MEASURED, AND THE FIX FOR IT IS CONCRETE (coordinator, 2026-07-31). THE WORKLOAD IS WRONG, NOT THE INJECTION POINT.** Using B2's own shipped `MAXON_GT_PARK_DELAY_MS` as a probe — no rebuild needed, which is the payoff of the implementer's decision to SHIP the injection rather than hide it behind a build flag — the async park path was counted by how much a delay stretches a full suite run: **0 ms ⇒ 6.25 s · 200 ms ⇒ 8.63 s · 400 ms ⇒ 12.46 s · 800 ms ⇒ 19.44 s**, i.e. **+2.38 / +6.21 / +13.19 s ⇒ ~12, ~16, ~16 HITS PER RUN.** ⇒ **THE PARK PATH IS TAKEN ~12–16 TIMES PER SUITE RUN — ABOUT ONCE PER WORKER, NOT ONCE PER TEST.** Everything else completes synchronously with data already waiting, so the parker self-detects and never parks. ⇒ **A 6-second suite run buys ~15 chances at the race, and the 14-run vacuity arm bought ~210 in total** — orders of magnitude too few for a specific interleaving even with the window widened 200,000×. **That, not the injection site, is why the OLD protocol stayed green.** ⭐⭐ **THE INSTRUMENT'S FIX: STOP USING THE SPEC SUITE AS THE WORKLOAD.** Write a TARGETED driver that hammers the async park path — many concurrent green threads doing reads on pipes **that already hold data**, so a completer is guaranteed to be reaping while a parker sits in its window — giving **thousands of parks per second** instead of fifteen per six seconds. Then inject. **Bar unchanged**: the old protocol must wedge quickly, the new one must not. A targeted driver also runs in seconds rather than minutes, so the vacuity check and both arms become cheap enough to re-run on every future change to this handshake. ⚠ **Until that exists, NO number from `netpoll-inject-gate.sh` means anything** — including the new protocol's 20/0, which is green either because the fix works or because the gate cannot fire, and there is currently no way to tell. ///// ⛔⛔⛔ **THE ACCEPTANCE MECHANISM ITSELF MAY NOT WORK — COORDINATOR-MEASURED 2026-07-31, AND B2 CANNOT MERGE UNTIL THIS IS RESOLVED.** The rung's whole acceptance is *"the OLD protocol wedges under injection, the NEW one does not"*. **The old-protocol arm STAYS GREEN**, so the gate currently discriminates nothing. Measured: a throwaway worktree at pre-B2 `main` with a **5 ms `usleep` inserted at the exact race point** — immediately after `EmitBranchIfIoCompleted` (the last self-detect) and before `__gt_context_switch` sets `ioYielded = 1`, inside `EmitGtParkForIoCompletion`, i.e. the ~25-instruction window this row describes — then `netpoll-inject-gate.sh 20 5 60 --exe <that build>`: **14 runs, 0 wedges**, every run 2696/0. ⚠ The delay IS almost certainly executing: the vacuity build runs **6–7 s against a 5.5 s baseline**, and ~200 parks × 5 ms ≈ the +1 s observed. (`nm -u` is NOT a valid check here and was inconclusive — `kevent`, `nanosleep`, `write` and `mmap` are all absent from it too, so the emitted Mach-O does not expose imports that way.) ⭐⭐ **THE LIKELY REASON, AND IT IS STRUCTURAL, NOT A BUG IN THE INJECTION: WIDENING THE PARKER'S WINDOW ONLY CREATES THE RACE IF A COMPLETION LANDS INSIDE IT.** The interleaving needs *parent checks status → **the worker's answer arrives** → parent parks*. But a spec worker's answer arrives **SECONDS** after the parent parks, not microseconds — so delaying the parker by 5 ms does not make a late completion race with it, it merely delays the park. The race only bites when the pipe **already holds data at park time**, which is exactly the rare interleaving that cannot be summoned by slowing the parker. ⚠ **This is NOT specific to the coordinator's hand-rolled `usleep`**: `__netpoll_inject_delay` sits at the same instruction and faces the same problem, so **the new protocol's 20/0 injected and 120/0 regression runs are ALSO uninterpretable** — they may be green because the fix works or because the gate cannot fire. ⇒ **NEXT TASK IS THE INSTRUMENT, NOT THE FIX**: the injection must make the COMPLETION land in the window — delay or hold the COMPLETER (or arm a read on an fd that already has data and stall the completer's skip decision), not the parker alone. **Until an injected OLD build actually wedges, no number from this gate means anything**, and that is the rung's own stated bar: *an injection harness that never fails proves nothing at all — it would report a triumphant green against a compiler with the bug still in it, which is strictly worse than no harness.* ✅ What IS established: the protocol port builds clean, C# **3187/0**, shv2 host **2696/0**, wasm **2690/0**, and 120 serialised regression runs show **no regression** — the port does not break the pool. That is worth having and is not the acceptance. ///// ⭐⭐⭐ **AND THE ISSUE OF epoll RAISES THE BAR FROM "COPY GO'S PROTOCOL" TO "COPY GO'S SEPARATION" (user, 2026-07-30).** **MEASURED: there is NO `epoll` anywhere in this repository** — not in `maxon-sharp`, not in `maxon-shv2`, not in v1. And **shv2's GT runtime is `X64GtRuntime.maxon`, x64 ONLY**; `maxon-shv2/Compiler/Targets/Linux/` holds **`ElfWriter.maxon` and nothing else** — no GT, no async; while the bootstrap's runtime exists **TWICE, HAND-ROLLED SEPARATELY** (`ARM64CodeEmitter.Runtime.cs` macOS/kqueue, `X86CodeEmitter.Runtime.cs` Windows/IOCP). ⇒ **Linux has no async I/O yet, and when it lands it needs epoll — a THIRD readiness mechanism.** ⭐⭐ **GO'S REAL VALUE HERE IS THE SEPARATION, NOT ONLY THE STATE MACHINE**: `netpoll.go` holds the **PORTABLE** protocol (`netpollblock` / `netpollblockcommit` / `netpollunblock` / the sentinels) and `netpoll_epoll.go` · `netpoll_kqueue.go` · `netpoll_windows.go` hold **ONLY** readiness plumbing (`netpollopen`/`netpollclose`/`netpoll`). Adding a platform in Go is filling in two or three functions; **in our tree it currently means re-deriving the whole state machine by hand.** ⛔⛔ **AND THAT DUPLICATION IS PRECISELY WHY B1 EXISTED**: x64 already had the correct arm-ordering *and carried an `OPEN #66` comment about that exact hazard*, while arm64 did not — **one bug, fixed in one emitter and absent from the other, discovered long apart.** Port per-emitter again and **epoll becomes the THIRD independent hand-derivation of a state machine Go writes once**, with the next divergence already scheduled. ⇒ **B2 MUST EMIT THE PARK PROTOCOL FROM ONE PLACE**, parameterised by the per-target readiness bits: the two emitters may differ in *how readiness is discovered*, they must NOT differ in *who owns the wakeup*. **Structure it so epoll is ADDITIVE** — a future Linux implementation supplies discovery and inherits the protocol untouched. ⚠ If our emitter architecture genuinely blocks a single shared implementation, **that is a finding: say what blocks it rather than duplicating silently.** ⛔ **RETRACTED — SIZE IS NOT A REASON TO SPLIT (user, 2026-07-30), and CLAUDE.md says so outright: *"There are no time constraints. Complexity doesn't matter. If you are fixing an issue then fix it properly. No workarounds."*** The coordinator had written *"if this is too large to gate in one piece, split it"*; that is the workaround the doctrine forbids, and it is withdrawn. **Do the whole correct thing in one rung**: the faithful protocol port, emitted from ONE place across BOTH emitters, the poll word alongside `status`/`ioYielded`, Go's `throw`s, the observable recovery net, and the injection harness. ⭐ **THE SURVIVING STOP CONDITION IS UNVERIFIABILITY, NOT SIZE — and the distinction is the useful part.** *"This is large"* is not a reason to stop; *"I cannot establish that this is correct"* is. Concretely: **if the injection harness cannot redden the OLD code, the acceptance collapses** and the work cannot be trusted however good the diff looks; and **if a faithful port is impossible somewhere, name the exact spot rather than improvising** — silent divergence is the failure this rung exists to correct, and a partial Go port has already cost this project a lost panic and a `0xC0000005`. ⇒ **The fault-injection bar covers the protocol AND the factoring, so a bigger diff does not weaken the acceptance — it only takes longer to gate.** If the shared emission needs a primitive or a structural change in `ARM64CodeEmitter.Backend.cs` or the emitter architecture, **that is IN SCOPE**; report it rather than working around it to keep the diff small. ///// ⭐⭐⭐ **RULING 2026-07-30 (user): COPY GO, UNLESS THERE IS A REASON WE CANNOT. THE COORDINATOR CHECKED FOR SUCH A REASON AND THERE IS NONE.** **(a) Can a park be ABORTED after the context is saved?** — the structural question, and **YES**: `gopark`'s own contract is *"If unlockf returns false, the goroutine is resumed"*, and `netpollblockcommit` returns `bool` precisely to exercise it. **Maxon already has the hook** — `__gt_context_switch` sets `ioYielded = 1` **AFTER** the register save, which is exactly Go's commit point; the commit CAS goes there, and a failed CAS resumes instead of parking. ⚠ `park_m`'s body could not be retrieved from either mirror, so **the exact resume mechanism is the ONE thing the implementer must verify against real source before relying on it**. **(b) Atomics?** — LDAXR/STLXR exclusive loops are already emitted for cross-P refcounting (`ARM64CodeEmitter.Backend.cs:276-341`). **(c) GC pointer?** — Go stores a `*g`; we store a GT id we own outright, so no barrier concern. ⇒ **NO BLOCKER. PORT IT.** ⛔ **AND A COORDINATOR CORRECTION THAT MATTERS TO THE SHAPE**: I suggested `status`/`ioYielded` should be RETIRED in favour of the new word. **Wrong, and copying Go faithfully says the opposite — GO KEEPS `g.atomicstatus` SEPARATE FROM `pd.rg`.** They answer different questions (*what is this thread doing* vs *who owns this fd's wakeup*). ⇒ **add the poll word ALONGSIDE the existing pair; do not fold them.** The stopped agent's `park_state` word was right on both counts, and its own comment had already reached the reason: *"`status` says an I/O finished and `ioYielded` says a context has been saved; NEITHER says whether the completer or the waiter is responsible for resuming it."* ⇒ **PORT FAITHFULLY AND RECORD EVERY DEVIATION AS A DEVIATION** — this row exists because the last partial port (`_StackMin` and `928` taken, `_StackSystem` dropped) cost a lost panic and a `0xC0000005`. ///// ✅✅✅ **GO'S PROTOCOL IS NOW READ, NOT RECALLED — FETCHED VERBATIM 2026-07-30 from `raw.githubusercontent.com/golang/go/master/src/runtime/netpoll.go` and `netpoll_kqueue.go`. THIS SUPERSEDES EVERY "recalled, unverified" HEDGE BELOW.** ⭐ **AND THE USER'S PREMISE IS CORRECT AND DOCUMENTED: THIS RUNTIME IS ALREADY A GO PORT, AND A PARTIAL ONE HAS ALREADY COST A REAL BUG.** PLAN.md's own async-fault entry says shv2 *"carried over Go's `_StackMin` (2048) and the `928`, but NOT `_StackSystem` — the one term that exists for exactly this — the same omission the C# bootstrap made"*, and that omission is why a CPU fault inside `async` dies `0xC0000005` with the panic lost. ⇒ **the two-word `status`+`ioYielded` handshake and the spin gates are almost certainly UNJUSTIFIED DEVIATIONS from the model, not considered designs, and B2 should port the real protocol rather than patch the deviation.** ///// **THE VERIFIED PROTOCOL.** ONE atomic word per direction (`rg`/`wg`), holding a sentinel OR the parked G: `pdNil=0`, `pdReady=1`, `pdWait=2`, else a G pointer. Go's own state machine comment: *"pdReady — io readiness notification is pending; a goroutine consumes the notification by changing the state to pdNil. pdWait — a goroutine prepares to park, but not yet parked; **the goroutine commits to park by changing the state to G pointer**, or, alternatively, concurrent io notification changes the state to pdReady... G pointer — the goroutine is blocked; io notification or timeout/close changes the state to pdReady or pdNil and unparks it."* **PARKER (`netpollblock`)**: loop { `CAS(pdReady→pdNil)` ⇒ **return true** (readiness already pending); `CAS(pdNil→pdWait)` ⇒ break; else `throw("double wait")` }, then `gopark(netpollblockcommit, …)`; on wake `old := Swap(pdNil)`, `throw("corrupted polldesc")` if `old > pdWait`, **return `old == pdReady`**. **COMMIT (`netpollblockcommit`)**: `CAS(pdWait → g)` — **and if it FAILS the park is ABORTED and the G keeps running.** **COMPLETER (`netpollunblock`)**: loop { `old := Load()`; `pdReady` ⇒ return nil; `pdNil && !ioready` ⇒ return nil; `CAS(old → pdReady)` ⇒ **if `old == pdWait` return nil (nothing to wake — the parker will abort itself)**, else return the G }. ⭐⭐⭐ **THE DETAIL THAT DISSOLVES THIS ROW'S CENTRAL TRADEOFF, AND THE COORDINATOR HAD MISSED IT: GO'S SELF-DETECT IS A CAS.** The parker's FIRST move is `CAS(pdReady→pdNil) ⇒ return true` — a fast path for already-ready I/O, **expressed atomically**. **Our runtime does that same self-detect as a NON-ATOMIC LOAD of `status`**, which is exactly why the completer cannot distinguish *"will self-detect"* from *"will park"*, which is why guard (c) must SKIP, **which IS this window.** ⇒ **you do NOT have to choose between keeping the fast path and closing the race** — the earlier framing in this row (CAS vs always-park, trading the fast path away) was a false dilemma. ✅ **AND IT SATISFIES THE DRIVE-LOOP CONSTRAINT WE DERIVED INDEPENDENTLY**: `netpollunblock` is a pure CAS loop that NEVER waits on another thread's progress, so it is safe from a completion-draining loop — which is precisely where `__io_op_done`'s own comment says *"do NOT gate here… blocking it stalls every other pending completion and cascades into a livelock"*, and precisely why the spin gate is NOT available at this site. ✅ **REGISTRATION, VERIFIED**: `netpollopen` arms `_EV_ADD | _EV_CLEAR` — *"edge-triggered mode (EV_CLEAR) **for the whole fd lifetime**"*, auto-unregistered on close; **no `EV_ONESHOT`, no per-operation re-arm.** ⇒ the foreclosure argument against register-after-park HOLDS on verified ground. ⭐ **Go also THROWS on impossible states** (`"double wait"`, `"corrupted polldesc"`) — matching this project's own *no silent unhandled cases* rule; port those too. ⇒ **THE MAPPING FOR THE IMPLEMENTER**: our `status`+`ioYielded` pair collapses to ONE word; our self-detect becomes `CAS(pdReady→pdNil)`; guard (c)'s skip becomes `netpollunblock` seeing `pdWait`; our park becomes a commit CAS that may fail and abort. The stopped agent's instinct to add a `park_state` word was RIGHT — that word is Go's `rg`; the open question is whether `status`/`ioYielded` survive alongside it or are subsumed. ///// **SUPERSEDED BELOW (kept for the reasoning trail):** ⛔⛔ **CORRECTION 2026-07-30 — A PREMISE THIS ROW WAS BUILT ON IS FALSE, AND THERE IS A THIRD OPTION IN THE FILE ITSELF. Found by asking "did v1 use epoll?" (user).** ⚠ **v1 NEVER USED epoll** — kqueue on arm64-macOS, IOCP on x64-Windows, and **no epoll anywhere in its source** (checked). But reading v1's kqueue runtime surfaced the thing that matters. ⛔ **THE CLAIM "THE COMPLETER CANNOT SPIN INSTEAD OF SKIPPING, IT WOULD LIVELOCK" — inherited from B1's report and repeated by the coordinator into two briefs — IS FALSE AS A GENERAL STATEMENT. THE RUNTIME ALREADY SPINS.** **`__io_complete_gt_spin`** (`ARM64CodeEmitter.Runtime.cs:7007`) spin-waits on `ioYielded == 1` before enqueueing, `YIELD` per iteration, *"without this, the I/O worker thread could enqueue the GT while its SP is still being saved."* **`__gt_ppw_spin`** (`:6889`) is the same gate **with a written termination argument**: *"the awaiter cleared ioYielded=0 before publishing promise.waiter, and `__gt_context_switch` sets it to 1 after the save; the await idle path guarantees the awaiter always reaches a context switch, so this terminates."* **And v1 does the same** (`Arm64MacosGreenThread.maxon`, `emitArm64GtProcessPendingWaiter` ~`:2695`), whose own comment calls it *"the await/trampoline analogue of the C# `__io_complete_gt` spin gate"*. ⇒ **THE RUNTIME SPINS IN ONE COMPLETER AND SKIPS IN ANOTHER** — `__io_poll_kqueue`'s guard (c) skips, `__io_complete_gt` spins. That ASYMMETRY, not an impossibility, is the real state. ⭐⭐ **THIRD OPTION, AND IT SHOULD BE TRIED FIRST: MAKE `__io_poll_kqueue` SPIN LIKE `__io_complete_gt` ALREADY DOES.** Precedented twice in this file, once more in v1, on both sides of the C#/v1 port; cheaper than a CAS protocol; and — unlike register-after-park — **NOT registration-model-locked**, so it does not foreclose persistent registration either. ⚠ **THE CRUX IS THE TERMINATION CONDITION**: the spin terminates iff the parking GT is GUARANTEED to reach `__gt_context_switch` and set `ioYielded = 1`. The await path guarantees it; **whoever takes this row must establish whether the I/O park path does** — guard (c)'s stated reason for skipping is that a self-detecting waiter never parks, and B1's own *"resumed ≠ completed"* change (which loops back to `_try_dequeue`) may bear on it. **If it cannot be guaranteed, say exactly why — and only then is the CAS justified on its merits.** ⚠⚠ **COORDINATOR ERROR RECORDED**: I sent an implementer to an external reference **whose source is not on this machine**, twice, while the strongest candidate sat two functions away in the file it already had open. The doctrine says read the working in-tree implementation first; I inverted it, and then inverted it again by taking a livelock claim on trust instead of grepping for `_spin`. ///// **SUPERSEDED (the foreclosure argument against REGISTER-AFTER-PARK still stands — that one IS model-locked):** ⭐⭐ **RULING 2026-07-30 (user): PREFER GO'S CAS-COMMITTED PARK — and the reason is FORECLOSURE, not Google's engineering budget.** The user's instinct was *"copy Go if possible, they put a lot of effort in"*; that alone is **not admissible by this project's own doctrine**, which says a COPY needs a reason exactly as much as a divergence does (*"'it works in v1' is not a reason"*) — but a sufficient reason exists and it reverses the coordinator's earlier recommendation. **GO'S CAS PARK IS REGISTRATION-MODEL-AGNOSTIC; REGISTER-AFTER-PARK IS MODEL-LOCKED.** The CAS answers *"park safely against a readiness that may already have fired"*, which holds whether the fd was armed five seconds ago (persistent) or five instructions ago (per-op). Register-after-park answers it by ensuring **the registration does not exist until you are parked** — which works ONLY with per-operation registration and **structurally forecloses persistent registration**. ⇒ and we already have two measured reasons to want persistent registration later: it is **one `epoll_ctl` per FD instead of a `kevent` per READ**, and **`EV_ONESHOT` is precisely what made this wedge TERMINAL rather than transient** (B1's row). So the model-locked fix is a choice to redo this work when either bites. **Cheapness that forecloses the direction you are already leaning toward is not cheap.** ⚠ **SCOPE, and keep these apart or B2 becomes unverifiable**: the **PARK PROTOCOL is B2** — self-contained, LDAXR/STLXR already emitted for refcounting (`ARM64CodeEmitter.Backend.cs:276-341`), and directly testable by the fault-injection bar. **PERSISTENT REGISTRATION IS A SEPARATE, LARGER RUNG** — it changes fd lifecycle, not just parking. B2's job is to leave that door OPEN, which register-after-park would nail shut. ⚠⚠ **THE GO DETAILS IN THIS ROW ARE RECALLED, NOT READ — there is no Go toolchain or source on this host (checked 2026-07-30).** The once-per-fd registration, the edge-triggered mode, and the exact shape of `netpollblockcommit` are UNVERIFIED. **Whoever takes this row must obtain and read the real source before porting**, and if the recall is wrong the foreclosure argument must be re-derived rather than inherited. ///// **SUPERSEDED RECOMMENDATION, kept because the reasoning is still worth reading:** ⚠⚠ **AMENDED 2026-07-30 BY B1's REVIEW, BEFORE ANYONE STARTED THIS ROW — THE FIX BELOW IS PROBABLY NOT THE CHEAPEST ONE, AND THIS ROW ORIGINALLY NAMED THE WRONG PLAN.** The review agrees the window is real and agrees *"ordering cannot close it"*, but says **"it needs a CAS handoff" OVERSTATES THE COST** — because **`__io_submit_sync` IN THIS VERY FILE ALREADY SOLVES THE IDENTICAL PROBLEM WITH A DIFFERENT, CHEAPER SHAPE: REGISTER-AFTER-PARK.** Hand the request to `P.pendingSyncReq`, switch off, and let the SCHEDULER register it once you are **provably parked** (`__gt_process_pending_sync_req:6843`, `POffPendingSyncReq`). ⇒ **the window cannot exist because the registration does not exist until the GT is parked.** The `kevent` struct lives on our stack, which is intact-but-inactive after the switch — exactly like the sync request. Pair it with `__io_submit_sync`'s other rule (**the main OS thread never parks — it arms and spins**, safe because guard (a) means it always self-detects) and the window closes with **two shapes already implemented in this file**. ⚠⚠ **BUT "WHY DOESN'T GO USE REGISTER-AFTER-PARK?" IS THE RIGHT QUESTION, AND THE ANSWER IS NOT "BECAUSE IT IS WORSE" (user, 2026-07-30).** **GO HAS NOTHING TO DEFER.** Go registers an fd with epoll/kqueue **ONCE, AT FD CREATION**, and it stays registered for the fd's lifetime; the `pdWait`/`pdReady` CAS is not solving *"how do I arm safely"* but *"how do I park safely when readiness may arrive at ANY time, including long before any goroutine decides to wait"* — which is why it needs a `pdReady` SENTINEL to REMEMBER a readiness that arrived with nobody waiting. **Register-after-park requires PER-OPERATION registration.** Maxon re-arms `EV_ONESHOT` every read, so its arm is adjacent to its park and is deferrable; Go's is not. ⇒ **the two fixes each follow from a different REGISTRATION-LIFETIME choice, and Go's design is not evidence about which fix is better.** ⚠⚠ **THE REAL COST, AND IT MUST BE WEIGHED BEFORE CHOOSING**: Go's persistent registration is a genuine performance win — a socket doing a million reads costs **one** `epoll_ctl` where Maxon pays a `kevent` syscall **per read** — and B1's row already records that `EV_ONESHOT` is precisely what made the wedge **TERMINAL rather than transient** (a consumed-but-lost registration leaves nothing armed, so nothing can recover it). ⇒ **choosing register-after-park is also choosing to KEEP per-op `EV_ONESHOT`**, and it binds this runtime tighter to that model: if Maxon ever moves to persistent registration for the syscall win, it will then need something like Go's CAS anyway. **Say in the rung report which of the two models you are committing to and why** — that is the actual decision here, not the fix shape. ⚠ The Go claims above are from KNOWLEDGE, not from reading Go's source: **verify the once-per-fd registration and the edge-triggered mode before leaning on them.** ///// ⇒ **TAKE THIS ROW BY READING `__io_submit_sync` FIRST**, and treat Go below as the FALLBACK and as the vocabulary, not the plan. That ordering is this project's own doctrine — find the working in-tree implementation, then justify any divergence — and the coordinator inverted it by reaching for an external reference first. ///// **THE FALLBACK, if register-after-park does not fit: PORT GO'S CAS-COMMITTED PARK.** Go's runtime solves exactly this in `netpoll`, and the shape is the thesis: **ONE word per direction holds EITHER a state sentinel OR the parked G**, so *"am I parked?"* and *"has it fired?"* are decided by a **single atomic operation** instead of two stores with a gap. Parker: CAS `0 → pdWait`, then park through a **COMMIT FUNCTION** that CASes `pdWait → g` **after the context is saved** — *and if that CAS fails the park is ABORTED and the GT simply keeps running*. Completer: CAS to `pdReady`; seeing `pdWait` it knows there is nothing to wake (the parker will abort itself), seeing a G it schedules it. ⇒ **no spin and no livelock**, which is precisely the objection that made the window look unclosable. ✅ **THE PRIMITIVES ALREADY EXIST — this is not a new capability**: `ARM64CodeEmitter.Backend.cs:276-341` already emits **LDAXR/STLXR exclusive loops** for cross-P refcount atomics, and an acquire/release RMW also **subsumes the barrier question** B1 had to hand-roll. ⭐⭐ **ACCEPTANCE — AND IT MUST NOT DEPEND ON REPRODUCING A 14,000-RUN EVENT.** The instrument is **FAULT INJECTION, the dual of B1's own `MAXON_MAX_PROCS=1` control**: B1 proved causation by making the window *structurally unreachable*; B2 proves the fix by making it **structurally enormous** — insert a delay/spin at the exact point between the pre-park re-check and `ioYielded = 1` (build-flag gated, never shipped) so the race fires at near-100%. **Bar: the OLD protocol must wedge on almost every run under injection, and the NEW protocol on none.** That converts an unreproducible race into a deterministic test, and it is the only acceptance that actually discriminates. Supporting, necessary-but-not-sufficient: `scripts/spec-wedge-repro.sh` clean at ≥500 as a regression check, and the full C# + both shv2 lanes. ⭐ **SHIP THE RECOVERY NET WITH IT, NOT AFTER** — Go's **sysmon** re-polls when the netpoller has not run recently, so a lost wakeup becomes a hiccup rather than a dead run. ⚠ **The coordinator initially argued for doing this AFTER, because a recovery net would have masked B1 — that reasoning is BACKWARDS for an unreproducible race**: masking only matters while you still have a bug you can catch, and here recovery is the one thing that bounds the risk while the protocol change is unverified. ⚠ Also consider **registration lifetime**: `EV_ONESHOT` + re-arm-per-read means a consumed-but-lost registration leaves **nothing armed**, which is what made B1 terminal rather than transient; Go registers an fd once and keeps it. ⚠ **The Go details above are from KNOWLEDGE, not from reading its source in-tree** — sentinel values and the exact shape of `netpollblockcommit` must be checked against the real runtime before designing from them, and Go has a GC-managed `*g` where this needs a GT id | **L-bootstrap** *(`maxon-sharp/Compiler/MLIR/ARM64CodeEmitter.Runtime.cs` **and** `X86CodeEmitter.Runtime.cs` — both, by the cross-target rule. Gate is the C# suite plus both shv2 lanes)* | ✅ DONE | `slice/B2-gt-park-lost-wakeup` | 2026-08-01 |
| **B3** | ✅✅ **CLOSED 2026-08-01** (main `531ef7904`; C# **3229 → 3230/0** with `Fragment goldens: 3230 verified, 0 written` and `git status --short specs/` EMPTY; shv2 **3242/0**; x64-linux **PASS**, wasm32-wasi **PASS**, arm64 **SKIP — remote, UNVERIFIED**; 0 warnings; error-codes / flat-namespace / stdlib-target-selftest OK; no exit 101). ⛔⛔ **THE ROW COULD NOT START, AND THE REASON WAS TWO STACKED WINDOWS BLOCKERS NOBODY HAD MET.** (1) `scripts/netpoll-park-driver` hardcoded `/bin/sh`, which exists only inside Git Bash's virtual filesystem ⇒ `spawn failed`, 3 runs of 3. (2) The deeper one: **`StreamingSubprocess.spawn` FAILED FROM A GREEN THREAD.** One program, same function, both call paths: `direct_call_ok=1`, `green_thread_ok=0`. ⇒ the knob this row is *defined* by (`MAXON_GT_CLAIM_DELAY_MS`, *"took the park driver 5/5 clean to 0/5"*) had never been armed on the backend B2 shipped unexecuted, and could not have been. ⭐⭐⭐ **ROOT CAUSE — `EmitSystemStackEnter`'s TWO ARMS CLOBBER DIFFERENT REGISTERS.** The green-thread arm stages the new TIB bounds through **RAX**; the main-thread arm is only `SUB RSP`. So a value computed into RAX *before* the Enter survives on `main` and is silently replaced by `(systemStackTop - PSystemStackSize)` on a GT. Exactly two of ~51 sites did that, both in `__subp_create_overlapped_pipe` (`:13133`, `:13163`), both carrying a kernel argument ⇒ **`CreateNamedPipeW` received a STACK ADDRESS as its `openMode`**, failed `ERROR_INVALID_PARAMETER`, and surfaced as *"spawn failed: CreatePipe failed"*. ⚠⚠ **THE COORDINATOR'S HYPOTHESIS WAS REFUTED BY THE IMPLEMENTER, WITH MEASUREMENT.** I proposed *"CreateProcessW runs on the 8 KB GT stack"* citing `:12005`'s plain `EmitCallImport`. **False**: that call sits INSIDE an `EmitSystemStackEnter(0x50)`/`Leave` pair — a 5+-argument import must be a plain call because the macro switches RSP before the stack args are written — and `Subprocess.run`, same `spawn_core`, same `CreateProcessW`, returns **`green_thread_run_ok=1`**. 8 KB was never the problem and no stack was grown. ⭐⭐ **FIXED AT THE CLASS, NOT THE CALL SITES, AND PROVEN SO BY SABOTAGE.** TIB staging moved RAX → R11 in `X86CodeEmitter.cs`. The safety argument is what makes it cheap: `EmitGlobalLoadReg(R11, …)` is the **first emitted instruction** on every path into all three macros, so R11 is already dead everywhere ⇒ the change **strictly shrinks** the clobber set `{RAX,R11}` → `{R11}`, and **a change that only removes clobbers cannot break a caller** — a proof instead of a 51-site audit. **Control: with BOTH call-site fixes REVERTED, the hardened macro alone makes green-thread spawn work.** Call-site fixes kept anyway as defence in depth, with their comment retuned to say the MACRO is the guarantee so nobody 'simplifies' it back. ⭐⭐⭐ **THE INDEPENDENT REVIEW FOUND THE SAME CLASS ONE LEVEL UP, AND IT WAS A LIVE ACCESS VIOLATION.** The macro's doc comment dismissed **R10** as *"disturbed only BETWEEN Enter and Leave, where the Win64 call being set up destroys it anyway"* — but the safe window starts at the **CALL**, not at the Enter, and `SpawnWorker` (`X86CodeEmitter.Backend.cs:620`) stood in the gap: `MOV R10, pReg` … `EmitSystemStackEnter` … `MOV R9, R10` ⇒ `lpParameter` = a stack address. Reachable by the ordinary route (`__gt_enqueue` → `SpawnWorker`, i.e. **a green thread making another green thread runnable**). Measured: 8 `async` spawns from inside an `async` body ⇒ **`0xC0000005`, 12/12**; the identical 8 from `main` ⇒ exit 0, **5/5**. B3's own signature, twice, one level apart. **Fixed.** ⭐ **The review also collapsed 14 hand-encoded `gs:`-relative TIB accesses** (each with a hand-computed REX/ModRM — a wrong byte writes the WRONG REGISTER into the TIB and faults in some unrelated later Win32 call) into `EmitTibBoundStore`/`EmitTibBoundLoad`, **proved byte-identical by hashing a program compiled before and after** (`ca8db9dd…`). And it verified `__gt_context_switch` is genuinely NOT a fifth copy (unconditional, two different GT structs, RCX live) rather than assuming. ⚠⚠ **WHAT THIS ROW DID NOT ESTABLISH, STATED PLAINLY: THE WINDOWS PARK ARM IS STILL THINLY EXERCISED.** The driver now runs here for the first time (`lines=1600 expected=1600`, ~7 s) and **neither knob turns it red** — `CLAIM=5` **10/10 clean**, `PARK=5` **10/10 clean**, and `CLAIM=5` **5/5 clean under 2× CPU oversubscription**. ⭐ **That is EVIDENCE THE IOCP CLAIM ARM IS SOUND, NOT A FIX**, and the row's own stop-rule was honoured rather than waved through: at 5 ms the wall clock moved by **zero**, so the knob was raised until the slope showed — **`CLAIM=1000` → 14-15 s vs 6-7 s (+8 s), against arm64's recorded +8.5 s** ⇒ the claim arm is live and driven **comparably on both backends** (coordinator-verified independently: 7 s → 14 s). **But `PARK=1000` → +8 s resolves to ~8 parks per reader, where arm64's `PARK=5 → +2 s` resolves to 400 — ~50× less park coverage**, because most streaming pipe reads complete synchronously (`FILE_SKIP_COMPLETION_PORT_ON_SUCCESS`, `X86CodeEmitter.Runtime.cs:3604`). **Measured NOT to be a pacing problem**: park count is invariant across 2/10/25/50/200 ms pacing, an explicitly flushing producer and a jittered one ⇒ new row **`B9`**. ⭐ **Three false claims in the instrument itself, corrected with measurements**: `run.sh`'s *"PARK=5 → 400 parks per reader, exactly"*; `main.maxon`'s *"every read finds an empty pipe and PARKS"* — ⚠ **which the implementer fixed in ONE of THREE places** (this project's signature bug, inside the very commit correcting a false claim; the review caught it); and `run.sh` comparing Windows' **critical-path** traversals (4) against arm64's **total** (80) and calling it *"the same order"* — like for like it is 4 vs 4-5, a stronger result. ⭐ **Two more false comments fixed before the wave**: `SyncOpProcessWait` is declared, dispatched and **never called** — the subprocess wait calls `WaitForSingleObject` directly on the system stack and BLOCKS ITS OS THREAD — yet **two** comments claimed it yields, against one that said the truth. ⚠ **THE WEDGE IS NOT CLAIMED AND NOT FIXED.** Measured rate: **3 wedges in 3 runs under heavy load, 0 in 4 idle runs** ⇒ it needs preemption pressure, and a green suite says nothing about it. ⭐ Also fixed: `.gitignore`'s driver block listed only the POSIX artifact names, so Windows' `main.exe`/`main.exe.mxdbg` matched neither and 282 KB of binary leaked into a commit — the same nothing-here-was-ever-run-on-Windows shape, one layer down. ⚠ arm64 needs no twin and this was **verified, not omitted**: `ARM64CodeEmitter.Backend.cs:261` calls imports directly ("no TIB") and its `SpawnWorker` calls `pthread_create`, so there is no conditional stack switch for either defect to live in. ⭐⭐⭐ **CLAIMED 2026-08-01T21:05Z WITH A LIVE REPRODUCTION — THIS ROW IS NO LONGER "GO VALIDATE IT", IT IS "GO FIX THE WEDGE I CAN SHOW YOU."** Measured by BATCH1's coordinator during its cross-target gate, on **x64-windows**, and **AFTER B2's netpoll fix `d7a7c96c3` had landed (07:34Z the same day)**: the `wasm32-wasi` lane wedges the spec worker pool — `=== spec worker pool WEDGED: nothing dispatched or collected for 300.0 s ===`, **13 job(s) dispatched, 1 collected, 198 still queued**, all twelve slots reporting `drain complete: no` at 300–392 s, then `panic at SpecWorkerPool.maxon:963`. **Zero `FAIL` lines — no test failed; the pool stopped making progress.** ⭐ **Reproduced THREE times: twice on the BATCH1 tree and once on a detached `origin/main` worktree carrying none of BATCH1**, so it is not a BATCH1 artefact. ⚠ **The HOST lane is green (`3198/0`) while the WASM lane wedges** — the difference is that the wasm lane spawns a `wasmtime` subprocess per test, which drives the park/wake path far harder. That is a lead, not a diagnosis: the wedge is in the **HOST** runner's green threads either way, and the target is incidental to the mechanism. ⇒ **This row's premise — *"the Windows/IOCP arm has not had a single park driven through the new code"* — is exactly the gap the reproduction sits in.** Start where the row already says to: confirm `MAXON_GT_CLAIM_DELAY_MS=5` actually changes the driver's wall-clock now that `a90dc2f10` fixed the dead knobs, THEN drive this wedge. ⚠ **`scripts/spec-wedge-repro.sh` loops the suite and captures the parked stacks of the parent and its workers** — the panic message names it, and it has never been run against this. ⭐⭐ **UPDATED 2026-08-01 — B3 WAS NOT ACTUALLY RUNNABLE UNTIL NOW, AND NOTHING SAID SO.** B2's independent review found that **both injection knobs were DEAD on x64**: `ReadEnvUnsigned` zeroed `dest` — which IS RAX — before testing `GetEnvironmentVariableA`'s return, so `MAXON_GT_PARK_DELAY_MS` and `MAXON_GT_CLAIM_DELAY_MS` both read 0 on Windows and the harness was inert on the backend that carries IOCP, `__io_complete_gt` and four of the five submit families. Whoever took this row would have measured a triumphant green against an injection that never fired — **the exact failure `netpoll-inject-gate.sh` was deleted for**. Fixed in `a90dc2f10` (test RAX first, zero `dest` at a separate label, matching the working sibling `EmitReadMaxProcsEnvOverride`). ⚠ **ALSO UNEXECUTED AND OWED TO THIS ROW:** `maxon_net_tcp_connect` published `Parked` before clearing `ioYielded` (fixed by reading — the completer spins on the stale 1 and enqueues a still-running GT onto a stale sp), and `__gt_try_await_sched`'s missing `__netpoll_recover`. **Everything x86 in B2 was verified by READING; this row is where it gets RUN.** ⇒ start by confirming the knobs actually arm on Windows — if `MAXON_GT_CLAIM_DELAY_MS=5` does not change the driver's wall-clock, stop and fix that before believing any other number. ///// **VALIDATE THE NETPOLL PARK PROTOCOL ON x64-WINDOWS — it has never been run.** B2 ports the protocol to BOTH emitters from one shared file, but **every measurement was arm64/kqueue**; the Windows/IOCP arm has not had a single park driven through the new code. ⚠ **Needs a Windows host** — it cannot be validated on the macOS box B2 was developed on, which is why it is its own row rather than a B2 residual. **Instrument already exists**: `scripts/netpoll-park-driver/` plus `run.sh`, and the acceptance is **arm C** (`MAXON_GT_CLAIM_DELAY_MS`, the completer-side knob) — the arm that found B2's defect; the parker-side knob is green by construction and proves nothing. ⚠ **The review named two x64-specific things while reading, untested there**: the mainthread park loop is **FIVE VERBATIM COPIES** and this rung edited all five, inserting `__netpoll_recover` at **two different positions** among them (arm64 factored the same quartet at B1); and *"a GT with no schedulable stack must never commit"* is spelled as `currentGt == &P->mainThread` at those five sites against arm64's `stackBase == 0`, with nothing making them agree. ⇒ **do not start until B2's release-rule defect is fixed** — validating a protocol that is known to lose wakeups would measure the wrong thing | **L-bootstrap** *(`X86CodeEmitter.Runtime.cs` + the shared `Runtime/RuntimeEmitter.Netpoll.cs`)* | ✅ DONE | `slice/B3-netpoll-windows-park` | 2026-08-01T21:05Z |
| **B6b** | ⛔⛔ **NESTED `async` NEVER COMPLETES — A DETERMINISTIC HANG AT N=1, AND 3230 GREEN TESTS RUN OVER IT.** Opened 2026-08-01 by B3's independent review, **measured not predicted**, and coordinator-reproduced **3/3 (exit 124, 20 s timeout)**. One `async` inside one `async`, where the inner does I/O: `--async-trace` shows `spawn #1 / spawn #2 / io_yield #2` **and then silence** — a GT spawned by a GT yields for I/O and its completion is never pumped. ⭐ **PROVEN INDEPENDENT OF B3's FIX, IN BOTH DIRECTIONS**: pre-fix with `MAXON_MAX_PROCS=1` (which makes `SpawnWorker` unreachable) it **hangs 3/3**; pre-fix with default procs it is the **`0xC0000005` 12/12** B3 fixed; post-fix it **hangs 3/3**. So B3 fixed one of two stacked defects on this path and this is the other one. ⚠ **`specs/async-await.md` HAS NO NESTED-`async` CASE AT ALL** — that is how a deterministic hang stayed invisible under 3230 passing tests, and closing that hole is part of the row whatever the fix turns out to be. ⚠⚠ **DO NOT ASSUME THIS IS THE SPEC-RUNNER WEDGE.** It is tempting — same machinery, and the pool's parent parks 12 drain GTs on overlapped reads — but **the shapes disagree**: this hangs **deterministically at N=1**, while the wedge is **intermittent and load-dependent** (measured 3/3 under load, 0/4 idle). Establish identity by measurement or not at all. Repro: `function leaf(n) → _ = File.exists(…); return n` · `function outer() → let p1 = async leaf(1); return await p1` · `main() → let q = async outer(); await q` | **L-bootstrap** *(the GT scheduler / netpoll completion pump — `X86CodeEmitter.Runtime.cs` · `Runtime/RuntimeEmitter.Netpoll.cs`)* | 🔶 CLAIMED | `slice/B6b-nested-async-hang` | 2026-08-01T23:18Z |
| **B9** | **THE WINDOWS PARK DRIVER TAKES ~8 PARKS PER READER WHERE arm64 TAKES 400, SO B3 VALIDATED THE CLAIM ARM AND NOT THE PARK ARM.** Opened 2026-08-01 by B3, which made the driver run on Windows for the first time and then measured the honest limit of what that bought. `CLAIM=1000 → +8 s` matches arm64's recorded `+8.5 s` ⇒ **the claim arm is live and comparable**. But `PARK=1000 → +8 s` resolves to **~8 parks per reader**, against arm64's `PARK=5 → +2 s` ⇒ **400** — **~50× less park coverage**. ⭐ **The cause is structural and was MEASURED, not guessed**: most streaming pipe reads complete synchronously under `FILE_SKIP_COMPLETION_PORT_ON_SUCCESS` (`X86CodeEmitter.Runtime.cs:3604`), so the parker's commit is not on the path they take. **Ruled out as a pacing problem**: the park count is invariant across **2 / 10 / 25 / 50 / 200 ms** producer pacing, an explicitly `[Console]::Out.Flush()`-ing producer, and a 1-40 ms jittered one; the arrival cadence timed at the consuming end is a flat **64 lines/s with no bursting**. ⇒ **This row needs a DIFFERENT INSTRUMENT, not a tuned one** — most likely a socket-based driver (`WSARecv`, which cannot complete inline the way a ready pipe does) rather than the pipe-based one. Until it exists, **no honest claim can be made that the IOCP PARK path is validated**, and B3's row says so | **L-bootstrap** *(`scripts/netpoll-park-driver/**` + whatever new driver this needs)* | ⬜ FREE | — | — |
| **B4** | ✅✅ **SWEPT 2026-08-01 (`4f37c91dc`) — 427 FRAGMENTS MINTED, AND THE LANE IS NOW POPULATED RATHER THAN MERELY CURRENT.** shv2 **110 rewritten + 300 minted**; C# **7 rewritten + 10 minted**. After: shv2 **2994/0**, C# **3197/0**, wasm32-wasi PASS, no non-arm64 golden disturbed. ⭐⭐ **REVIEWED AGAINST THE x64 TWIN RATHER THAN RUBBER-STAMPED, WHICH IS THE WHOLE DIFFICULTY OF THIS ROW:** blessing 110 other slices' codegen wholesale IS the *"turn a gate green by narrowing what it tests"* failure, so every regenerated golden was checked against its `x64-windows` counterpart — the lane upstream actually reviewed. **shv2 rewritten 110/110 agree on `__rc_chk` guard count (0 disagree); shv2 minted 300/300 agree (0 disagree, 0 twinless); C# 17/17 agree on `mm_incref` count.** The diff's shape corroborates independently: **3,553 insertions vs 135 deletions**, dominated by **170 each of `__rc_chk`/`__rc_ok`/`__rc_panic`** — the range-check guards upstream added while arm64 was not looking — and on the C# side the fragments embed a test SOURCE that changed upstream (`default throws` → `default panic("unreachable: …")`), which no backend change could produce. ✅ **DETERMINISM VERIFIED: all 6,349 `.test` goldens byte-identical across two consecutive runs.** ⚠ An earlier "non-deterministic" alarm was the CHECK hashing `specs/fragments-arm64-macos/.spec-cache/`, which is gitignored (`.gitignore:20`) and churns every run — worth knowing before someone else re-derives that scare. ⭐ **THIS IS ROUTINE MAINTENANCE, NOT A DEFECT, AND NOTHING IS OWED (user ruling 2026-08-01).** arm64-macos goldens can only be minted on an arm64-macOS box; upstream develops on x64-windows and regenerates the lanes it can run. So the arm64 lane drifts between syncs and someone regenerates it here — **that is how the process works and it is fine.** An absent golden minting silently is part of that design, not a hole in it: do NOT "fix" it by making a missing golden fail. ⇒ **just re-run the sweep on this host whenever the lane has drifted.** ⚠ The one thing worth carrying forward is operational, not structural: **a red shv2 suite on a Mac after an upstream sync is very likely not yours.** Isolate against untouched `origin/main` before spending a session on it — B2 lost a full battery to reading this as its own regression. ///// **HOW IT WAS FIRST WRITTEN UP — as a defect, which it is NOT; kept only for the measurements:** **THE arm64-macos LANE HAD DRIFTED BY 110 shv2 + 7 C# FRAGMENTS.** Measured 2026-08-01 by ISOLATION during B2's merge: plain `origin/main` (`ca9a69fd8`), with no slice applied, runs **2834 passed / 111 failed** on an arm64-macOS host — every failure `codegen changed — golden fragment mismatch`, across `where-clauses`, `ranged-typealias`, `string-type`, `first-class-functions`, `self-type-member-expression` and a dozen more. The C# suite is 3197/0 but dirties **7** `specs/fragments-arm64-macos/` fragments on the same run. ⭐ **THE CAUSE IS STRUCTURAL, NOT NEGLECT:** shv2 work is developed on x64-windows, `--update-required` regenerates only the lanes the dev host can run, and **a MISSING arm64 golden is minted silently as an untracked file rather than failing** — so the rot is invisible until someone runs a suite on a Mac, and then it is invisible *again* because it looks like the slice under test broke something. B2 lost a full battery to exactly that. ⚠ **WORTH REVIEWING RATHER THAN BLIND-REGENERATING**: these are 110 tests' worth of OTHER slices' codegen changes, and blessing them wholesale is exactly the *"turn a gate green by narrowing what it tests"* failure. Each needs the owning change identified, or the sweep needs to be run by someone who can review the diffs. ⇒ **A phase-boundary stale-golden sweep, not a rung**, and it must also decide whether an ABSENT arm64 golden should keep minting silently — the property that lets this accumulate unseen. | **L-goldens** *(`specs-shv2/fragments/arm64-macos/`, `specs/fragments-arm64-macos/`)* | ✅ DONE | `slice/B4-arm64-golden-sweep` | 2026-08-01 |
| **B5** | **THE `IoCompletion` DONE-QUEUE IS DEAD ON BOTH BACKENDS — DEFINED TWICE, CALLED NOWHERE.** `__io_enqueue_completion` is emitted by `X86CodeEmitter.Runtime.cs` and `ARM64CodeEmitter.Runtime.cs` and has **no call site anywhere in the repository** (verified 2026-08-01, whole-tree grep). So `__io_dequeue_completion` and x86's `__io_check_completions` drain loop — **including one of the two claim/publish/release sites the netpoll protocol carries** — are unreachable in the current call graph. Dead code, **no behavioural consequence**, which is exactly why it survived: it costs nothing and reads as live. ⚠ It is not free to leave: a future reader modelling the completer set will find three sites and reason about three, and B2 already paid that tax once. B2 converted the dead site to the new protocol *for consistency* rather than delete it, because amputating 3–4 functions across two backends inside a concurrency fix is the wrong trade. ⇒ Delete it, or make its deadness impossible to miss. | **L-bootstrap** *(`X86CodeEmitter.Runtime.cs` · `ARM64CodeEmitter.Runtime.cs`)* | ⬜ FREE | — | — |
| **B6** | ⛔ **THE TIMER HEAP HAS NO OWNERSHIP WORD, SO A WORKER PARKING ON I/O CAN CUT A MAIN-THREAD `sleep()` SHORT (x86).** Found by B2's implementer 2026-08-01 while giving the I/O park its own word. `maxon_sleep`'s `__sleep_mainthread_loop` exits on `status != waiting` and has **no park word at all** — it is the one main-thread poll loop B2 deliberately did NOT convert, precisely because `__netpoll_woken` would be false forever there. Meanwhile three of x86's five submit families stamp `mainThread.status = running` on the way into `__gt_context_switch` while **two carry a CRITICAL comment saying they must not**. ⇒ the sleeping main thread's `status` is written by something that is not its completer, and it wakes early. ⭐ **THIS IS THE SAME DEFECT CLASS B2 JUST CLOSED, ON A DIFFERENT MECHANISM**: readiness carried on a word that does not say who owns it. The cure is the same shape — an ownership word for the timer heap — and `RuntimeEmitter.Netpoll.cs` is now the worked example. ⚠ **Windows-only and UNVERIFIABLE on the arm64-macOS host**; arm64's `maxon_sleep` uses the shared `__gt_timer_check`, which never touches `GtOffParkState` either, so check both before assuming it is x86's alone. | **L-bootstrap** *(`X86CodeEmitter.Runtime.cs` · `Runtime/RuntimeEmitter.Scheduler.cs`)* | ⬜ FREE | — | — |
| **B7** | **x86 CARRIES TEN HAND-WRITTEN COPIES OF A SEQUENCE arm64 CARRIES ONCE — AND B2 DRIFTED ON THE TENTH WHILE ADDING ONE LINE.** arm64 has a single `EmitDriveSchedulerAndIo` (7 callers) because **B1's review de-duplicated it**. x86 never was: 11 `__gt_process_pending_waiter` idle/park sites, each spelling the scheduler-drive sequence itself. B2 added `__netpoll_recover` to nine of them by hand and **missed `__gt_try_await_sched`** — caught by the independent review, not by any gate, because a park loop that cannot observe a lost wakeup fails silently by construction. The ORDER of the calls also differs between sites, and the pipe family's loop (`X86CodeEmitter.Runtime.cs:3647`) never calls `__io_check_completions` at all. ⇒ **The next scheduler line added will drift again**, and the failure mode is always "one loop silently does less". Build x86's `EmitDriveSchedulerAndIo`. ⚠ Windows-only, unverifiable on the macOS host — pair it with B3. | **L-bootstrap** *(`X86CodeEmitter.Runtime.cs`)* | ⬜ FREE | — | — |
| **B8** | **`mcp__maxon-dev__run_spec_test` WITH `compiler: "shv2"` AND NO FILTER TIMES OUT AT 600 s — THE SUITE ITSELF TAKES 5.6 s.** Reproduced 2026-08-01 against the **untouched main checkout**, so it is not any slice's doing; filtered runs are fine on both trees, and the same suite run by hand through a pipe is 3/3 clean in seconds. Looks like MCP-side handling of the runner's ~2,700 output lines. ⚠ **THE COST IS THAT NOBODY USES THE TOOL CLAUDE.md TELLS THEM TO PREFER**: every suite run in B2 went through the binary directly, which also means every one of them re-derived the redirect-to-a-file discipline by hand. A tool that hangs for ten minutes on its most common invocation is worse than one that is absent, because the first thing it costs is ten minutes. ⚠ **The MCP binary is gitignored and nothing rebuilds it** — see CLAUDE.md's box; a fix needs `maxon build maxon-dev-mcp/mcp` **and** a server restart. | **L-mcp** *(`maxon-dev-mcp/mcp/`)* | ⬜ FREE | — | — |
| **B1** | ⛔⛔ **THE arm64 GT RUNTIME ARMS A `kevent` BEFORE MARKING THE GREEN THREAD WAITING, AND LOSES THE WAKEUP — THIS IS H1's ROOT CAUSE AND THE ACTUAL FIX.** Opened 2026-07-30 by H1, which diagnosed it and **stopped at its file-list boundary rather than reaching into another lane**. ⚠ **`maxon-sharp/`, so it needs the FULL C# suite (2883+) as its gate plus codegen neutrality** — that is why it is its own rung and not H1's. ⭐ **THE BUG, in two instructions** (`Compiler/MLIR/ARM64CodeEmitter.Runtime.cs`, `EmitIoSubmitReadWrite` ~`:7262`): `EmitCallImport("kevent")` at `:7323` publishes the read to the **process-wide shared kqueue**, and only THEN does `:7325` store `status = waiting`. `EVFILT_READ` is **level-triggered**, so on a pipe already holding the worker's answer the event is ready *immediately*; any of the up-to-8 other Ms polling `__io_poll_kqueue` reaps it inside that window, stores `status = ready`, consumes the `EV_ONESHOT` registration, frees the ctx, and — by its own guard `ioYielded == 0` (*"it is still RUNNING and will self-detect"*) — **deliberately does not enqueue**. Our thread then executes the store and **overwrites `status` with `waiting`**; the pre-park re-check at `_woke_while_parking` reads `waiting`, so it parks with **no registration, no run-queue entry and no future event**. The drain green thread is lost forever. ⭐⭐ **THE x64 SIBLING IS THE COUNTER-EXAMPLE AND IT IS ALREADY CORRECT** — `X86CodeEmitter.Runtime.cs:7843` stores `status = waiting` **before** arming, carrying an explicit **`OPEN #66`** comment about exactly this arm→reap family. **Coordinator-verified: I read both orderings myself.** ⇒ arm64 is the outlier, and this is a **cross-target-consistency defect by CLAUDE.md's own rule**. **FIX: move the `status = waiting` store ABOVE the `kevent()` call.** One reorder per site. ⚠ **A SECOND SITE HAS THE SAME BUG PLUS ONE MORE**: `maxon_net_tcp_connect` (~`:5922`; `kevent` `:6020`, `status` `:6022`) is ordered identically **and** its `rt_ntc_has_next` path reaches `__gt_context_switch` with **no pre-park status re-check at all** — two windows, not one. Off the spec-runner path, same defect. ⚠ **Also while the file is open: `kevent()`'s RETURN VALUE is unchecked at both arm64 sites.** A failed registration parks the GT forever with the *identical* signature, so it would be indistinguishable from this race. ✅ **THE EVIDENCE IS UNUSUALLY COMPLETE, and none of it is a green suite** — at a 2.7% base rate a green run proves nothing, and H1 deliberately did not offer one: **(a)** captured `sample` stacks — parent in `__sleep_mainthread_park` with `kevent` returning nothing, all 12 workers 100% in a blocking `read(0)`; **(b)** ⭐ **a SIGKILL PROBE that FALSIFIED the coordinator's own hypothesis** — killing all twelve workers produced **no reaction at all**, so every drain's ONESHOT registration was already consumed ⇒ the parent LOST the wakeup, and it was *not* the parent→worker handshake I had suspected; **(c)** a 200-run control with `MAXON_MAX_PROCS=1` (one P ⇒ no second poller ⇒ the window is structurally unreachable): **0 wedges vs 2/75 at default**, P(0 in 200 | p=0.027) ≈ **0.004**. ✅ **VERIFY WITH `scripts/spec-wedge-repro.sh 250 40 host`** (~25 min; expect 0/250 against the measured 2/75). ⚠ **The remaining risk is that the reorder is NECESSARY but not SUFFICIENT** — a second window elsewhere in the same handshake — which is exactly what that harness is for | **L-bootstrap** *(NEW — `maxon-sharp/Compiler/MLIR/*CodeEmitter.Runtime.cs`. Touches no shv2 source, so it collides with every shv2 lane not at all; its gate is the C# suite, not specs-shv2)*  ✅✅ **CLOSED 2026-07-30** (main `ebc4f9e6c`; **C# suite 3187/0**, shv2 host **2696/0**, wasm **2690/0**, codegen neutrality **0 modified**). ⭐⭐ **THE HEADLINE IS EMPIRICAL, NOT AN ARGUMENT**: harness **RED 1/150** on the unchanged compiler, **GREEN 0/250 twice = 0/500** fixed, plus **150/0** re-run after the review's own edits. Pooling the red evidence (3/225 ≈ 1.33%), P(0 in 500) ≈ **0.0012**. ⚠ The implementer measured a LOWER base rate than the coordinator (0.67% vs 2.7%, load-dependent) and **ran 500 rather than the 250 asked for**, on the grounds that 250 could not carry the claim at the rate it observed. ⭐ **THE `DMB ISH` WAS NOT IN THE BRIEF AND IS NOT OPTIONAL** — arm64 is weakly ordered, so the correct instruction ORDER alone is insufficient: the status store can reach coherence after another M has already seen the knote. ⭐ **The park handshake was DE-DUPLICATED** into `EmitGtParkForIoCompletion`, so `maxon_net_tcp_connect` GAINS the pre-park re-check it never had — the class fixed, not the instance. ⛔⛔ **THE REVIEW FOUND A SILENT WRONG ANSWER THIS RUNG HAD WIDENED**: `__io_poll_kqueue` published `status = ready` **before the data it guards**, with no release fence — and B1 gave `maxon_net_tcp_connect` two new self-detect points that read `io_result_val` the instant status moves, where a stale `0` is accepted as a valid fd and wrapped in a `__ManagedSocket`. ⭐ **And the acquire half was missing on BOTH readers**: a control dependency orders a later STORE on arm64, never a later LOAD, so the release fence was protecting nothing. ⭐⭐ **PRIORITY-1 DUPLICATION: the scheduler/IO drive quartet was SEVEN VERBATIM COPIES**, held in agreement by *a comment on one of them*. Adding a fifth engine to one copy is not a compile error — it is a subset of GTs that never wake. Now `EmitDriveSchedulerAndIo`. ✅✅ **THE SIGN-EXTENSION QUESTION IS SETTLED BY MEASUREMENT, TWICE INDEPENDENTLY** — the implementer flagged that the file's own comment (*"Apple ARM64 zero-extends 32-bit return values"*) would make its new `CMP X0,#0 / B.GE` check **dead code**, and honestly recorded that it had dismissed this on a CONSISTENCY argument rather than a measurement. The reviewer measured; **the coordinator then measured independently** with a raw-`x0` probe: failing `open` ⇒ `0xffffffffffffffff` (`>= 0` NO), success ⇒ `0x0000000000000003`. **Darwin SIGN-extends; the checks work and the COMMENT was the false thing.** ⇒ this also clears the scarier corollary: `__io_poll_kqueue`'s long-standing `CMP X0,#0` has **not** been silently swallowing failed polls. ⚠ **14 arm64-macos goldens regenerated in an isolated commit, NONE of them this rung's** — `main` ALONE failed the same 11 (2685/11, identical names) and B1 produced the identical split, so it is A1's lag; regenerated from an **UNFILTERED** run deliberately, since filtered fragments are not authoritative (literal-pool indices depend on the batch), and exactly 11 files moved. ⇒ **B1 takes `main` from 11 red to 0 on arm64-macos.** ⚠⚠ **IT DOES NOT CLOSE THE WEDGE COMPLETELY — see B2**, and the implementer's own framing is the right one: *treat 0/500 as retiring the DIAGNOSED defect, not as proof the handshake is race-free* | ✅ DONE | `slice/B1-arm64-kevent-arm-before-mark` | 2026-07-30T14:45Z |
| **H1** | ⛔⛔ **THE SPEC RUNNER WEDGES NONDETERMINISTICALLY — AND IT IS THE ONE BUG THAT BREAKS UNATTENDED OPERATION.** Sequenced onto the board 2026-07-30 from "Future rungs", where the T1 coordinator filed it after hitting it **TWICE in one session** (host lane once, wasm lane once) on arm64-macos. Each time the identical binary and tree then ran the same suite **GREEN in 5.5 s and 8.1 s** after a `SIGTERM` and a retry. ⇒ **~2 wedges in ~15 suite runs (≈13%) — very reproducible for a loop harness.** ⭐ **THE SIGNATURE, CORRECTED — and the correction is the whole diagnosis.** The first filing said *"all workers AND the parent at 0% CPU"*. **Re-read of the captured `ps`: the PARENT was at 1.7–1.9%, only the 12 WORKERS were at 0.0%.** That rules out "everything parked" and matches the dispatch loop exactly: `runSpecPool`'s fallthrough is **a POLL, not a block** (`SpecWorkerPool.maxon:943` — *"Nobody idle, nobody answered. Park, so the scheduler can run the drain green threads"* ⇒ `sleep(PollYieldMs)`), so a healthy-but-forever-polling parent is precisely a ~2% trace. `findReadyDrain` is a non-blocking `gtIsComplete` peek; it simply never answered true for any slot. ⭐⭐ **AND THE WASM RUN NEVER SPAWNED A `wasmtime` PROCESS AT ALL** — so the workers were not stuck FINISHING a job, they were stuck WAITING for one. ⇒ **suspicion falls on the parent→worker direction (`sendAndDrain` + the `WorkerReadyMarker` handshake), NOT the result-drain direction the loop's own comment worries about.** ⚠ **That is a hypothesis, not a finding** — this project has burned agents on confident hypotheses that measured zero, so it is to be settled by capture, not by reading. **PLAN, and step 1 stands on its own merits whatever the cause turns out to be: (1) MAKE THE WEDGE LOUD** — the runner watchdogs itself and PANICS with every slot's state when no slot completes and no worker progresses for N seconds, instead of polling forever. That converts an unattended-loop killer into an ordinary red gate, and it doubles as the DETECTOR step 2 needs. **(2) REPRODUCE UNDER OBSERVATION** — loop the suite; on detection **capture instead of kill**: `sample` on the parent and on one worker (built into macOS) gives both park stacks free, plus `lsof` on the worker's stdin. **(3) ONE MEASUREMENT SETTLES IT** — did the parent WRITE the job, and did the worker READ it? Workstream O's `__DebugStream` monitor exists for exactly this; a write-side and a read-side marker answer it in a single wedge. Candidate causes then narrow to a missing flush, an unlooped partial write, a handshake race where the parent dispatches before the worker's reader is armed, or a GT lost wakeup on the drain thread. ⚠ Suspect the family of the GT scheduler race fixed 2026-07-20 (`8163f1056`) and the `--workers=16` orphaned-M wedge before it — a residual, or a second one. ✅ `pkill -TERM` cleared it with **zero** processes left behind, so the old *"killing maxon wedges the box"* hazard is genuinely gone and a retry loop is safe. ⇒ **Diagnostic that costs nothing and belongs in every operator's hands: `ps -o pid,etime,%cpu,stat -p $(pgrep -f 'maxon-shv2 spec-test')` — WORKERS at 0.0 for minutes means WEDGED; retry rather than wait.** ⚠ An empty log is NOT the tell (the runner buffers and prints only at the end); **the CPU column is** | **L-harness** *(`Testing/SpecWorkerPool.maxon` — no compiler source at all, and the lane has had no open row since G1 closed, so it collides with nobody)*  ✅✅ **CLOSED 2026-07-30** (main `f1b19b23b`; arm64-macos host **2672/0**, wasm32-wasi **2656/0**, fragments **byte-identical**, zero leftover processes). ⚠⚠ **IT MAKES THE WEDGE LOUD; IT DOES NOT FIX IT** — the fix is **B1**, above. ⭐ **THE WEDGE IS NOW REPRODUCED AND DIAGNOSED**: base rate **2 / 75 runs (2.7%)**, and `scripts/spec-wedge-repro.sh` is the instrument that will verify B1. ⭐⭐ **THE SIGKILL PROBE FALSIFIED THE COORDINATOR'S OWN HYPOTHESIS** — I had said parent→worker handshake; killing all twelve workers produced **no reaction**, so every ONESHOT registration was already consumed and the parent had LOST the wakeup. A job that never reached its worker would have left the registration armed and aborted loudly. **The discriminator was built because the observations could not tell the two apart** — which is the whole lesson. ⛔ **THE REVIEW FOUND A MERGE-BLOCKER, AND IT IS THIS PROJECT'S SIGNATURE BUG**: `WedgeWatchdogMs` (120 s) and `SpecSpawnTimeoutMs` (120 s) were **two independent literals of one number**, and they are **NESTED** deadlines — the pool's clock starts at DISPATCH while the worker's starts at whichever test hung, so at equal values **the outer pre-empts the inner STRUCTURALLY**. A hung compile that `SpecSpawnTimeoutMs` exists to turn into one named test failure would instead have killed the whole run with *"this is a HARNESS bug, RETRY"* — a wrong answer pointing at the wrong file, and hardest at the lane where the hang is *measured* (`runProcess` records 18m35s at ~1 exec in 700). Fixed by **DERIVATION**: `WedgeWatchdogMs = SpecSpawnTimeoutMs + WedgeWatchdogGraceMs` = **300 s**. ⭐ **The threshold was RE-MEASURED, not accepted** — slowest single spec **1.27 s** host / **1.70 s** wasm, all 210 solo = 50.1 s retired in 5.5 s ⇒ contention under 2×; so 120 s was defensible *on slowness* and wrong for an unrelated reason. The implementer's *"8.2 s is a ceiling no job can exceed"* was **circular** (a job's real ceiling is tests × spawns × `SpecSpawnTimeoutMs` = hours) and its suspend-tell *"every slot reads the same age"* **degenerates exactly where wedges land** — the tail of a run has ONE busy slot. The header now prints the MEASURED interval beside the limit. ⭐ **Two harness defects fixed, one of them the thing I asked be confirmed absent**: the probe's `SIGKILL` **reparented grandchildren to launchd** so the later tree-walk found the parent alone and leaked them (now kills the tree recorded *before* the probe, each pid checked against its command — verified 6 grandchildren recorded, **0 left behind**); and a watchdog-killed run was counted as an ordinary failure, so the harness would have silently stopped finding anything if the two thresholds were ever reordered. ⭐ **And an unbounded wait the watchdog cannot see**: `requestExitAndWait` used `child.wait()` = `waitWithTimeout(0)` = forever, PAST the dispatch loop's reach — every verdict computed and the gate simply never returns. Bounded at 30 s. ⚠ **The four-parallel-array shape was audited and the merge REJECTED WITH A REASON**: `drains` cannot live in a struct the loop mutates, so collapsing the other three leaves the guard still published separately from `drains[slot]` — a partial fix traded against a verified invariant. The duplication that WAS real: `gtIsComplete` spelled twice, in the scan that SERVES on it and the report that ACCUSES on it — now one `drainHasAnswered` | ✅ DONE | `slice/H1-spec-runner-wedge` | 2026-07-30T13:10Z |
| **D11** | **An unknown container method in VALUE position gets a diagnostic that ASSERTS IT EXISTS.** `let x = arr.frobnicate()` ⇒ `E2004: Function 'frobnicate' does not return a value` — a claim that the method is real and void, about a name the type does not have. Cause: `requireArrayVoidStatement` sits ahead of the unknown-method refusal in `dispatchArrayMethod`. **Pre-existing and untouched by R4.4** (verified against its diff). ⚠ **Two corrections to how this was first filed, both measured by the R4.4 review:** it is the **VALUE** position that misreports — *statement* position gives the correct roster `E2015` — and it is **NOT** cross-container as originally described: `Array` and `Set` only, `String` is clean. Same class as D4 (a refusal asserting something false about the program); the fix needs a void-mutator name list that would duplicate `arrayMethodMutatesReceiver`, so it is its own rung | L-parser-postfix ✅✅ **CLOSED 2026-07-31** (main `795d1c9c6`; x64-windows **2869/0** on the merged tree — 2843→2862 on its own base, +7 from R9 — x64-linux **PASS**, wasm32-wasi **PASS**, arm64 **SKIP — remote, UNVERIFIED**; builds 0 warnings, no exit 101, **ZERO goldens modified**, bootstrap untouched). ⭐⭐ **THE FIX IS THE PATTERN SHV2 ALREADY USED ONE CONTAINER OVER, and needed no new list.** `requireArrayVoidStatement` tested `resultUsed` and **NOTHING about the method name**, so its only scope was WHERE IT SAT — after the value-yielding arms, before the void ones; an unknown name matched nothing, fell past them all, and was told *"Function 'frobnicate' does not return a value"* — **false twice over: it asserted the method EXISTS and that it is VOID.** `String` was clean because its check sits INSIDE the arm that matched the name, so the refusal is now the first statement of each void arm, renamed `requireVoidMethodIsStatement`, with `parseStringAppend`'s hand-written copy calling the shared helper — **one definition for all three containers**. ⛔ **THREE CORRECTIONS TO THIS ROW, ALL MEASURED.** (1) **The row's stated reason for being its own rung was FALSE**: it said the fix *"needs a void-mutator name list that would duplicate `arrayMethodMutatesReceiver`"*. No list was needed, and one would have been **WRONG rather than merely duplicative** — that predicate also holds `set`/`pop`/`remove`, which mutate the receiver **and return values**, so reusing it would refuse the legal `let x = arr.pop()`. (2) **"`Array` and `Set` only" was wrong — the BUFFER SURFACE was a third affected container** (`let x = mm.frobnicate()` equally red; its roster had **no** spec pinning it at all before). (3) There are **THIRTEEN** void arms, not the five the row's framing implied. ⭐⭐ **THE ACCEPTANCE TURNS ON A CONTROL, NOT THE OBVIOUS CASES: `arr = arr.push(1)` must STILL give E2004**, because `push` genuinely is void — and MEASURED, neutering the shared helper leaves **all four roster cases GREEN** while reddening only the controls, i.e. **the deletion wearing a fix's clothes**. A missing guard is not a downstream type error: `arr = arr.push(1)` then **COMPILED AND LINKED**. Removing ONE arm's guard reddens EXACTLY one control (23/1) — what per-arm controls buy over per-funnel ones. ⛔⛔ **THE REVIEW FOUND A THIRTEENTH VOID ARM WITH NEITHER HALF OF THE REFUSAL — `set`, missed because it THROWS and therefore sits with the ACCESSORS, outside the block the rung enumerated.** `let x = try arr.set(0, value: 7) otherwise return 1` **compiled, linked and RAN, exit 0**, binding `x` to `emitElementStoreAndOkReturn`'s literal `0`; the oracle refuses the identical program. **D11's dual: not a false claim that a method exists, but a FABRICATED VALUE for a method that has none.** ⭐ **The cure needed BOTH halves, because the two spellings are seen by DIFFERENT checks and each is blind to the other's case:** in `let x = try arr.set(…) otherwise …` the arm's `resultUsed` is **false by design** (`parseTryCallTarget`), so only `parseTry`'s `voidInValue` can see it — and that reads **nothing but the result TAG**, which the arm had stamped `integer`. Fixing the tag ALONE then **PANICS the compiler** on the bare spelling (`maxonTypeOfTag: a void tag names no value`), because SemanticCheck's bare-throwing-call E3057 runs a pass too late. ⇒ tag ⇒ `void` (matching `setLength`/`setByte`/`grow`) **and** the arm guard. ⭐⭐ **ON THE DUPLICATION QUESTION THE PER-ARM SHAPE RAISES — 13 call sites of one helper — the verdict is that they are NOT 13 copies of one fact**: each is the fact stated inside the condition that defines its scope, and there is **no second list anywhere**. Both single-site formulations were tried and fail: a name predicate ahead of the arms **IS** the second copy of "which names are void" (a name with no arm resurrects D11 — exactly what `grow` on a plain `Array` was; an arm missing from the list is the silent accept or the panic), and a derived `resultUsed and tagOf(result) == void` at the dispatcher's exit cannot work because **8 of the 13 arms deliberately return the RECEIVER**, so there is nothing void to derive. ⚠ **The shape's real cost is precisely how `set` was missed: a void arm that PREDATES the pattern and sits elsewhere gets no line**, and the helper's *"a new void arm must call this"* directive speaks only to NEW ones. ⭐ **Two false prose claims corrected:** five comments in this mechanism said the refusal was **E3059** when it is **E2004** (E3059 is the `try` door — every surviving mention checked and left), and `Parser.maxon`'s claim that *"a void result is TERMINAL: the very next hop refuses it"* is **false for the 8 arms that hand back the receiver** (`arr.push(2).count()` compiles here; the oracle refuses it). Also corrected `closure-capture.md`'s comment, which recorded THIS BUG as the design (*"`map(…)` resolves to no function (E2004 …)"*). **Closes P1.7b NOTES (2)'s rename debt** — see that row, which was stale three ways. **Opens `D11b` and `D11c`.** ⭐ Ladder measured **POST-REVIEW deliberately** (A1 and A1r both logged rows describing a tree that never landed): **allocation delta 0 at every rung**; the bytes column is A1r's row at a different checkout path, proven by shape — deltas 9,285/10,440/12,750/17,370/26,610/45,090 have successive differences each exactly DOUBLE, i.e. `const + k·2^n`, a per-FILE term a parser-dispatch change cannot produce | ✅ DONE | `slice/D11-unknown-container-method` | 2026-07-31T02:02Z |
| **D11b** | **⛔ THE BUFFER SURFACE SILENTLY ACCEPTS `Array` METHODS ITS OWN ROSTER MESSAGE DENIES — D11's DUAL, and it needs a RULING on the legitimate member set.** Found by the D11 implementer, confirmed and widened by the D11 review, both by RUNNING it: `mm.push(7)` on a `__ManagedMemory` **compiled, linked and ran (exit 0)**, and so did `try mm.remove(0)`. The buffer roster explicitly states `remove`/`swap`/`shiftLeft`/`shiftRight` **"are not built"**, so the message is **false in both directions** — it denies members that compile and it is the only thing a user has to go on. ⭐ **This is D11's dual: D11 was a diagnostic claiming a method EXISTS when it does not; this is the compiler ACCEPTING a method the diagnostic says it does not have.** ⚠ **A DIFFERENT MECHANISM, verified — arm GATING, not check ordering.** `setLength`/`setByte`/`grow`/`byteAt` are gated on `isManagedMemory`; `push`/`reserve`/`resize`/`count`/`isEmpty`/`clone`/`first`/`last`/`pop`/`remove`/`insert`/`get`/`capacity` are **not**. Nothing in D11's diff moves it either way, which is why D11 did not take it. ⛔ **THE RULING NEEDED: which `Array` members does the buffer LEGITIMATELY have?** It is not a mechanical sweep — `clear` is un-gated and **is** in the roster, `count`/`length` are deliberately folded, and **neither reference gives `__ManagedMemory` a `push`**. Closing it means adding `isManagedMemory` gates to ~10 arms, and every gate is a decision about the buffer's surface. ⇒ an **R-workstream surface decision**, not a diagnostic-scoping one. ⚠ Do NOT settle it by seeing what compiles — that is what produced the current state. ⚖⚖ **USER RULING 2026-07-31: THE LEGITIMATE SURFACE IS EXACTLY WHAT THE ROSTER NAMES. Option (a) — refuse the rest, and make the diagnostic TRUE.** ⇒ the roster message becomes the specification of the surface rather than a description that guards nothing, which is the same cure D11/D12/R10/D11c all landed on: *a refusal is easier to get right than the noun in it*, so the noun becomes the authority. ⚠⚠ **THE RISK THE RULING ACCEPTS, AND IT MUST BE MEASURED FIRST: this is a NEW REFUSAL, and a new refusal hides its false rejects one nesting level below where you test it** (R6's lesson, in this file). **Before narrowing anything, enumerate every call the corpus and `stdlib/` actually make on a `__ManagedMemory` value** — if a legitimate site uses a member the roster omits, the ROSTER is what is wrong and it gains that member; the ruling is "roster ≡ surface", not "the roster is already correct". ⚠ Expect the sweep, not the narrowing, to be the rung. ✅✅ **NOW COMPLETE — the ◑ is CLEARED (BATCH3, main `4c61be486`)**: its one named residual was `A2j`, and `A2j` closed. ⭐ **The sweep was the rung, exactly as this row predicted, and it came back EMPTY** — of the whitelisted stdlib (four modules), only `Builtins.maxon` names `__ManagedMemory` and only in bodiless interface declarations that call nothing, and every declared-spelling member call anywhere in `stdlib/` that shv2 could reach is ALREADY on the roster. **No member joined the roster; the narrowing touched zero compiled code** | L-parser-postfix *(the `dispatchArrayMethod` arms)* | ✅ DONE | `slice/D11b-buffer-roster` → `slice/BATCH3-derived-roster` | 2026-07-31T19:19Z |
| **A2j** | **⛔⛔ THE BUFFER SURFACE FOLLOWS A VALUE'S PROVENANCE, SO A *DECLARED* `__ManagedMemory` GETS THE ARRAY SURFACE — AND THE ROSTER COMES OUT EXACTLY INVERTED.** This is the half of D11b's ruling that did not land, and it is why that row is ◑. **Three spellings dodge the buffer gate entirely — a PARAMETER, a RETURN TYPE, and a STRUCT FIELD declared `__ManagedMemory`** — because it is registered as a generic **alias of `Array with Byte`** (`SignatureIndex.maxon:4567`), whose own comment says it *"reaches the ordinary `Array` paths with nothing special-cased"*. **MEASURED on the reviewed tree, all compiling, linking and RUNNING:** through `function abuse(m __ManagedMemory)`, twelve off-roster members are accepted (`push`, `reserve`, `resize`, `insert`, `pop`, `first`, `last`, `remove`, `clone`, `isEmpty`, `count`) — while **`m.length()`, the roster's FIRST member, is REFUSED**, and the message shown is the **`Array`** roster, which does not list `length`. **Coordinator-verified independently.** ✅ **NOT a regression** (R4.4's mechanism, untouched by D11b) and **NOT a wrong answer** — the record *is* the same `Array` record, so `__arr_push` on it is well-defined: no crash, no exit 101. It is a **false surface**, not a miscompile. ⚠⚠ **WHY IT NEEDS A MECHANISM RATHER THAN A PATCH: the SPELLING IS GONE by the time `bindParameters` sees a `MaxonType`** (`Parser.maxon:10410`), so closing it means carrying a *declared-as-the-alias* bit per parameter, per field and per return through `SignatureIndex`. ⭐ **THE GENERALISABLE LESSON, and it is why D11b's sweep missed it: the enumeration came from the producers of a buffer VALUE, and a DECLARED TYPE is a fifth entrance that a value-producer sweep structurally cannot see.** ⚠ Whoever takes this must decide what the declared spelling *should* mean — is `__ManagedMemory` an alias that deliberately exposes `Array`, or a distinct surface? — which brushes A1s's whitelist question about who owns builtin type names. ✅✅ **CLOSED 2026-08-01** (BATCH3, main `4c61be486`). ⚖ **The spelling question needed NO new ruling: D11b's — "the buffer's legitimate surface is EXACTLY what its roster names" — already answers it**, because the alternative makes that message false at three spellings, which is the defect being removed. ⭐ **THE CURE IS A PRE-ERASURE COLUMN, NOT A NEW SURFACE**: a *declared-as-the-buffer-alias* bit carried from each declaration site to the site that BINDS the value, marking it through the EXISTING `markBufferSurface` — three storage columns (`paramSurfaces` threaded `parseFunctionParameters`→`bindParameters`, `FuncReturnTypeEntry.declaredSurface`→`ProgramSignatures.declaredReturnSurface` read at `emitCall`, `StructLayout`'s field surface read at `emitFieldLoad` via `ProgramSignatures.fieldSurfaceOf`) ⚠ **ALL FOUR RENAMED BY BATCH7 (2026-08-01)**, when the column widened from a bool to a `DeclaredSurface` tree so a tuple SLOT could carry it; the marking bridge is now `markDeclaredSurface`. The names above are this row's ORIGINAL spellings, corrected in place but ONE predicate and ONE marking bridge, on `paramAggregateNames`' precedent. ⚠ **THE PARSE-MEMO HASH HAD TO LEARN THE SPELLING**: `returns __ManagedMemory` and `returns ByteArray` fold to the identical `genericInstance`, so without new discriminators an edit between them would move no memo key while changing what every caller's calls mean. ⛔ **AND THE MECHANISM SHIPPED A DEFECT THE IMPLEMENTER FOUND AND FIXED**: a buffer-returning function could not be overloaded — the sweep publishes the return spelling per BARE name, the surface is chosen when the call is PARSED, and overloads resolve a whole pass later, so *whichever member was declared first won* (buffer-first ⇒ the Array member refused; Array-first ⇒ the buffer member refused). Now REFUSED cleanly (E2015, positioned on the buffer-returning declaration) rather than silently wrong | L-types *(`SignatureIndex.maxon` · `Parser.maxon`'s `bindParameters`)* | ✅ DONE | `slice/BATCH3-derived-roster` | 2026-08-01T01:56Z |
| **A2k** | **THE `Array` ROSTER MESSAGE IS WRONG IN BOTH DIRECTIONS, AND IT IS NOT DERIVED — D11b's defect, one surface over.** Confirmed by D11b's review: it **names `create`**, which `dispatchArrayMethod` never serves as a member (proven — the panic experiment shows it has no arm), and it **omits `managed`**, which that dispatch *does* serve. So it denies something real and advertises something absent. ⚠ Unlike the buffer roster it is a **hand-written literal**, so nothing keeps it honest as arms are added or removed. ⇒ apply D11b's cure: one list built from the constants the arms match on, rendered into the message, with the fall-through a panic naming the list. ⚠ **Budget for golden churn**: the message is a literal in `maxoncstderr` blocks and shv2's `--update-required` does **not** regenerate those, so every affected block moves by hand — D11b measured the neighbouring clause at **34 goldens**. ⚠ This message is also what a *declared* `__ManagedMemory` currently receives (`A2j`), so the two rows meet: fixing this one without A2j leaves a true `Array` roster shown to something that is not an `Array`. ✅✅ **CLOSED 2026-08-01** (BATCH3, main `4c61be486`). Derived from the constants the arms match on, sharing ONE renderer / ONE membership test / ONE refusal-sentence builder with the buffer roster, and **ONE gate rather than two symmetric ones** — the surface picks the roster and the roster picks the sentence, so the decision lives once; the fall-through is now unconditionally the drift PANIC, **seen RED by sabotage before it was trusted**. `create` left the member list for a clause that is TRUE of it (a static factory), `managed` joined. Golden churn came in at the measured 18 occurrences / 15 files (+14 more from the review's one-word fix), all message text — **no IR line moved anywhere**, and fragments were empty after a full suite run on the merged tree. ⛔⛔ **AND THE DERIVATION REINTRODUCED THIS ROW'S OWN DEFECT ONE ENTRY OVER** — see the BATCH3 row: `managed` is a FIELD on a list the sentence called *"methods"*, caught by the review by RUNNING it | L-parser-postfix *(the `dispatchArrayMethod` roster literal)* | ✅ DONE | `slice/BATCH3-derived-roster` | 2026-08-01T01:56Z |
| **A2m** | **⛔ THREE MORE ENTRANCES STILL GET THE ROSTER INVERTED, AND EACH NEEDS A MARK SHAPE `A2j`'s STRUCTURALLY CANNOT HAVE.** Opened 2026-08-01 by BATCH3's review, **measured on the merged tree, not predicted**: (1) a **TUPLE element** — `function pair() returns (__ManagedMemory, Int)` then `p.0.count()` COMPILES, LINKS AND RUNS (exit 8) while `p.0.length()` is refused with the `Array` roster; (2) a **generic ARRAY element** — `Array with __ManagedMemory` then `.get(0).count()`, same shape (exit 5); (3) a bare **`let m = arr.managed`** binding with no call following — `dispatchArrayMethod`'s `managedField` arm returns `arr` UNCHANGED, so the surface is lost at that one spelling. ⚠ **NOT a regression and NOT a wrong answer** — the record is the same `Array` record, so the accepted calls are well-defined: it is a false SURFACE, exactly `A2j`'s class, at spellings `A2j`'s row did not name. ⚠⚠ **WHY IT IS A RUNG AND NOT A MISSING LINE, and this is the part to read before claiming it:** a tuple's identity is keyed on its **CANONICALISED** element list (`SignatureIndex.canonicalTupleElement` → `declaredSlotType`), so `(__ManagedMemory, Int)` and `(ByteArray, Int)` are **ONE tuple type sharing ONE `StructLayout`** — a `fieldBufferSurface` column on it cannot answer differently for the two spellings, and forking the type would break the very assignability `A2j` deliberately preserved. Closing it needs a per-(value, field-index) mark carried from the tuple-typed BINDING's declaration. Case (3) is the same trap from the other side: marking the value the arm returns would mark **the Array itself**, everywhere it is used — the over-ACCEPTANCE direction, which no diagnostic reports (`error.buffer-mark-does-not-leak-across-functions` exists because that once shipped) — so it needs the `.managed` read to MINT a value rather than pass one through. ⭐ **The generalisable lesson, third turn of the same screw:** a value-producer sweep could not see a DECLARED type (`A2j`), and a declaration-site column cannot see a **CANONICALISED** one ✅✅ **CLOSED 2026-08-01 (main `b3450ea1b`). THE ROW NAMED THREE ENTRANCES; MEASURED, IT WAS FIVE — AND ONE OF THE THREE WAS FALSE.** Reproduced by the coordinator on `main` before the wave: the tuple RETURN and the `Array with __ManagedMemory` ELEMENT are real, **and so are a tuple DESTRUCTURING, a tuple PARAMETER and an element reached through a STRUCT FIELD — none of which this row named.** ⛔ **Entrance (3) — a bare `let m = arr.managed` — IS FALSE AS FILED**: that spelling is refused earlier by a different, known gap (`.managed` in value position, R4.5's list), the chained `arr.managed.length()` **already answered correctly**, and no mark leaked back onto `arr`. It is now PINNED by two cases rather than changed. ⭐ **THE CURE, and it is not the column this row predicted:** `DeclaredSurface` became a **flat pre-order TREE** (`Array with DeclaredSurfaceNode`, one inline word per named position), so a slot carries a WHOLE surface, not one bit — which is why the mechanism ended with **no arity limit and no depth limit** and the `MaxSurfaceMarkedTupleArity = 62` ceiling an earlier cut needed was DELETED rather than restated. A plain position returns the shared empty tree and allocates nothing; the shared tuple `StructLayout` was **not** widened and `A2j`'s assignability is intact (proved by four `ByteArray`-spelled over-acceptance controls, the strongest putting both spellings in ONE tuple in ONE statement). **+23 cases.** ⚠ Two defects found and FIXED mid-rung: `clone`/`slice` dropped the element surface, and an overload set differing only at a tuple SLOT was accepted with the surface decided by declaration order. | **L-types** *(`SignatureIndex.maxon`'s tuple canonicalisation)* · **L-parser-postfix** |  ✅ DONE  | `slice/BATCH7-roster-derivation` | 2026-08-01T15:24Z |
| **D11c** | **A `try` ON A CONTAINER VOID METHOD LEAKS THE EMITTED RUNTIME SYMBOL INTO THE USER'S DIAGNOSTIC, and shv2 is the one leaking.** `let x = try mm.setLength(1) otherwise return 2` gives `error E3059: type mismatch: ''__arr_set_length' does not return a value'` — the **runtime symbol**, not the `setLength` the author wrote, **with doubled quotes** (`Queries.maxon` wraps a detail string that already quotes a name). ⚠ **The oracle spells it `stdlib.Array.set`**, so this is an shv2 divergence rather than a shared limitation. ⭐ **A different door from D11's**: this is the `try` path, whose `tryTargetName(target)` is the REWRITTEN call's callee and which has no `methodTok` — the refusal itself is correct, only the noun is wrong. Cure: thread a display name through `TryTarget` (**the witness case already carries one**, so the shape exists). ⚠ **D11 WIDENED THIS to five methods** by routing `set` into the same message (`__arr_set`, `__arr_mem_set`) — the fix's correctness required it, and it makes this row more visible rather than worse. ⭐ Same class as **D12** (a diagnostic naming a construct the program does not contain) and as the D11 defect itself; the pattern is that a REFUSAL is easier to get right than the NOUN in it | L-parser-postfix *(the `try` door + `TryTarget`)* ✅✅ **CLOSED 2026-07-31** (main `a039cba68`; x64-windows **2901/0** on the merged tree — 2892→2901 on its own base — x64-linux **PASS**, wasm32-wasi **PASS**, arm64 **SKIP — remote, UNVERIFIED**; builds 0 warnings, no exit 101; **4 goldens modified — the defect itself, `''__arr_set'` → `''set'` — and 14 added**; arm64 fragments deliberately NOT minted for a lane this host cannot run). ⭐ **FIXED BY REUSING D12's MAP, NOT BY A NEW MECHANISM.** `tryTargetName`'s `named` arm asks `runtimeCalleeSourceMethod`, gated on `declaresCallee` — **the same door `requireThrowingNamedTryTarget` uses to ADMIT the `try`** — so the check that lets a target through and the message that blames it read ONE table and cannot disagree. `TryTarget.named` gains no display field: the name is DERIVABLE, and a stored field would be that derivation, wrong at whichever emission site forgot it. ⚠ The survey's proposal — a side-channel at every builtin emission site, mirroring `lastWitnessDispatchName` — was rejected as diffuse and partial-prone. ⭐ **THE ARRAY MAP WAS MADE TOTAL OVER ALL NINE ACCESSORS RATHER THAN "THE VOID ONES", AND THAT IS THE LOAD-BEARING CHOICE:** *"void members only"* is a criterion no predicate expresses, so a partial map could not be guarded; total, the install-time guard holds **builders ≡ predicate ≡ map**. D12 had recorded the array gap as *"a design fact"* — but the reason it gave was E3057's FAMILY WORDING, which is a **policy**, so the fact moved into the map and the policy moved to the consumer (the array arm now sits ABOVE the lookup). E3057 stayed byte-identical, sabotage-proven. ⭐ **A COMMENT DOING A TEST'S JOB WAS RETIRED:** two consumers were kept correct by *"the memory arm must stay above any array test"* (because `isThrowingArrayRuntimeCallee` composes the buffer members). Both now ask `isThrowingArrayAccessorCallee`, and all sixteen `Arr*`/`ArrMem*` literals are distinct byte strings compared by exact `.equals` ⇒ **disjoint by CONSTRUCTION, not by order** — verified by sabotage: adding a buffer name to the narrow predicate is refused LOUDLY at install (49 red), never silently. ⛔⛔ **TWO CORRECTIONS TO THIS ROW AS I WROTE IT, AND BOTH WERE MINE.** **(1) THE ROW'S PART (b) — "the doubled quotes are a second, wider defect (5 of 7 throw sites)" — IS FALSE. They are the ORACLE'S OWN SPELLING.** `maxon-sharp/Compiler/2-Parser.cs:9374` emits both layers in ONE literal (`$"type mismatch: ''{tryCallOp.Callee}' does not return a value'"`) and `specs/error-handling.md:634` pins it canonically; shv2's wrapper-plus-pre-quoted-detail split reproduces those bytes exactly. ⚠ **The fixture I cited as *"the proof (b) is wider than this row"* (`specs-shv2/error-handling.md:716`) is a byte-identical PORT of that canonical line — i.e. proof it is NOT a bug.** Had the plan been followed, shv2 would have diverged from the runnable oracle on a `maxoncstderr` block both suites compare exactly, and broken the port rule. **The implementer overruled the plan and was right; do not re-open (b).** I reasoned from how the text LOOKED and never asked what the bootstrap PRINTS. **(2) THE LEAK ROSTER IS NINE.** The plan said THREE, the implementer measured SIX, and the review **DERIVED** it: `isThrowingRuntimeCallee` admits 33 callees across six families, and intersecting with the emission sites that tag their result `void` gives **NINE** — the three missed being `__mf_delete`, `__mf_rename`, `__md_create`. ⭐⭐ **THE METHOD IS THE LESSON: PROBING CAN ONLY FIND FAMILIES YOU ALREADY SUSPECT.** Both earlier counts were probed and both wrong in the same direction; only deriving from the two predicates that DEFINE the set could reach a family nobody had thought of. ⚠⚠ **AND THE THREE MISSED WERE THE THREE NOTHING COULD OBSERVE:** `delete`/`rename`/`create` have no E3057 case, so once this rung made E3059 a second consumer of the file and directory maps, **E3059 became their SOLE reader and it was untested** — sabotaging those maps yields `''stat'` where the author wrote `delete` and `''next'` where they wrote `create`, i.e. **this rung's own defect, in the exact diagnostic the rung is about, with the suite fully green.** ⭐ **`git diff` on compiler sources after the review contains ZERO non-comment lines (coordinator-verified: 40 insertions, all comments)** — *the rung's CODE was right; its ENUMERATION and its COVERAGE were not*, a distinction worth keeping. ⭐ Also pinned by the review: the `declaresCallee` arm's user-METHOD half returns a qualified `Gate.bump`, which looks like the same defect and is not — `bin/maxon.exe` answers identically, so the qualified spelling is specified. ⭐ `notFound`/`sharedByTwoSpellings` panic, and are unreachable for a STATED REASON rather than a list (the only map-less families have no void member; `__mf_open_write` returns an owned handle; an undeclared typo gets `unresolved`, never `void`). Ladder: allocations **+17 FLAT at every rung** (the six constants and map entries, allocated once), ratios ×1.79→×1.98; the bytes column's apparent −9,821→−52,778 is the per-FILE checkout-path artefact (successive differences each roughly double), not a reduction. **Opens `D11d`.** | ✅ DONE | `slice/D11c-try-door-display-name` | 2026-07-31T05:12Z |
| **D11d** | ✅✅ **CLOSED 2026-08-01** by BATCH1 (main `d4cd6aa20`). ⚠ **THE ROW'S CITATION `2-Parser.cs:9374` WAS STALE** — the E3059 emission is at `:9608`. Measured as a clean two-compiler A/B: bootstrap `''__managed_mem_set_length' does not return a value'` vs shv2's `''setLength'`. ⭐ **The drift guard PANICS for an unmapped compiler-emitted symbol rather than falling back to the raw callee — the fallback IS the defect.** "Compiler-emitted" is **DERIVED, not listed**: a synthetic builtin is absent from the function registry, so a `__` name the stdlib genuinely declares still reports as written. ⭐ The map is an **explicit table, not a strip-the-prefix rule**: that rule is exact for 49 of 50 symbols and silently wrong for one (`__managed_socket_send` ← `sendFrom`), and a rule with a silent exception is the class this batch exists to close. ⚠ **The `specs/` re-baseline scope was EMPTY** — no committed golden pinned the leaked spelling, so the leak was unobserved by any bootstrap spec; positive coverage added. Review enumerated every `__`-literal callee against the table: no gap. **⛔ `maxon-sharp` STILL HAS THE SYMBOL LEAK D11c JUST FIXED, so shv2 now DELIBERATELY DIVERGES FROM THE ORACLE ON THIS NOUN — and the spec prose invokes the oracle as authority for the QUOTING in the same breath.** Measured by the D11c review: the bootstrap's E3059 quotes its own emitted symbol — `''__managed_mem_set_length' does not return a value'` for `mm.setLength(1)`, and `''__managed_file_delete' does not return a value'` for `__ManagedFile.delete(p)`. ⚠⚠ **THE HAZARD IS THE COMBINATION, AND I CREATED IT.** D11c correctly kept the oracle's DOUBLED QUOTES (`2-Parser.cs:9374` emits both layers in one literal; `specs/error-handling.md:634` pins it) and correctly diverged from the oracle's NOUN. So one paragraph now says *"the oracle is the authority for this text"* and the next says *"we deliberately differ from the oracle here"* — **a future agent can read the first as licence to revert the second.** Separate the two claims in the prose whichever way this row is settled. ⭐ **The fix direction is to bring the BOOTSTRAP up to shv2**, not the reverse: naming the author's method is the better diagnostic, shv2 has the map (`runtimeCalleeSourceMethod`) and the bootstrap does not, and `specs/`'s canonical lines for the leaking cases would move — which is a `/specs` re-baseline plus the full C# suite as its gate. ⚠ Needs the C# suite green (2883+) AND codegen neutrality, so it is a **bootstrap rung**, not an shv2 one. ⚠ Note the two `''__managed_*'` spellings differ from shv2's `''__arr_*'` — the two compilers name these runtime entry points differently, so the leaked strings are not even the same strings; do not assume a shared fix. ⭐ Related, cheap, and found by the same review: **`managed-file.md` / `managed-directory.md` carry `<!-- targets: x64-windows -->` on PURE COMPILE-ERROR tests** (e.g. `open-read-without-try`), which E3059's behaviour shows is over-narrow — those diagnostics are raised in `parseTry`/SemanticCheck ahead of any target gate, so the marker excludes lanes that would pass. A marker-widening pass in the style of the earlier `<!-- targets: -->` audit (147→136), not a defect | *(bootstrap — `maxon-sharp/`; plus a `specs-shv2/` marker pass)* | ▣ **BATCH1** | — | — |
| **R4.5** | **`File` / `FileInfo`, and whitelisting `stdlib/File.maxon`.** What R4.2's row CLAIMED and could not deliver — the blocker chain was measured, not guessed (2026-07-30): `maxon-shv2 build stdlib/File.maxon` stops at **`E2015 String method 'addressableBytes'` (`:60`)**, the `module`-visible one-liner S3 deliberately declined to add because no spec can reach it (the oracle answers `E3008: not exported`); and **every public signature takes `FilePath`**, so `stdlib/FilePath.maxon` must load too, and it stops at **`E2015 String method 'byteAtOrPanic'` (`:56`)** plus `__Builtins.executablePath` — S2's list. Acceptance: `specs/file-io.md` (7) + `specs/file-info.md` (6), all 13 of which spell `File.readText(FilePath from "…")`. ✅ **Two things R4.2 already cleared for it:** `__ManagedFile.statFree(buf)` in **statement** position now parses (it did not, and that was R4.2's first blocker — `stdlib/File.maxon:186` writes exactly that), and all 13 `__ManagedFile` methods it calls exist. ⚠ Still needs: default parameter values (`mode FilePermission = FilePermission.normal`, twice), `match … gives try … otherwise throw` as an expression, `ByteArray.init(managed)`, and `content.managed` on a **binding** ⇒ **R4.4 first**. ⚠ Whitelisting reserves `FileReadError`/`FileWriteError`/`FileDeleteError`/`FileRenameError`/`FileInfoError`/`FileInfo`/`FilePermission`/`File` **PROGRAM-WIDE** (`StdlibLoader.maxon:91-136`) and the file carries string literals ⇒ **this is still the `.rdata` golden-churn rung** ✅ **ONE BLOCKER CLEARED 2026-08-01** by BATCH2 slice 1 (main `d7553412e`): **`String.addressableBytes` now exists**, stdlib-only. ⚠⚠ **TWO OF THIS ROW'S FOUR "still needs" WERE MEASURED WRONG**: `match … gives try … otherwise throw` as an EXPRESSION **already works** (probed, compiles and runs, exit 2), while `.managed` on a plain binding is real and still refused. ⛔ **The remaining hard blocker is DEFAULT PARAMETER VALUES**, which shv2 refuses with its own explicit *"parameter defaults arrive at a later milestone"* — a LANGUAGE FEATURE with `specs/default-values.md` as ready-made acceptance, and its own rung, not a ride-along. ⭐ **The chain was re-measured and it is a THREE-FILE CONE, not a file**: `File.maxon` needs `FilePath.maxon` loaded (every public signature takes `FilePath`), and `FilePath.maxon` needs `URL.maxon` (`URL.parse` at :120, outside any `#if`) ⇒ **URL → FilePath → File**, of which URL is now done. FilePath's own next blockers, measured: `String.byteAtOrPanic` (✅ delivered), `String.hash()` (**missing**), and `String.init(<buffer>)` ✅ **SLICE 2, 2026-08-01** (main `3cdf0bdd5`): `String.hash()` delivered, so this row's chain is down to **ONE mechanism**. `maxon-shv2 build stdlib/FilePath.maxon` now stops at **`:422:10 'self' as a value`** ⇒ **S5**. ⭐ **MEASURED, NOT GUESSED, AND IT SHRINKS THE ROW**: with only the **TWO** `return self` sites (`normalize()` :422, `resolve()` :431) replaced, the module compiles to `E3001: No 'main'` — byte-for-byte the clean-library signal the already-whitelisted `stdlib/URL.maxon` gives. **`self`-as-a-value at 2 sites is therefore the SOLE remaining blocker for the entire module**; its struct-typed PARAMETERS and RETURNS (`other FilePath`, `returns FilePath`, 13 signatures) all already work, so the diagnostic's wording over-states what is missing ✅ **`Array.init` DELIVERED 2026-08-01** by BATCH2 slice 5 (main `e02289aef`) — see the batch row for the mechanism. ⚠⚠ **AND THE CHAIN IS NOW MEASURED DOWN TO ONE MECHANISM, WHICH THIS ROW HAS NEVER NAMED AS ITS LAST**: with `Array.init` landed, `maxon-shv2 build stdlib/File.maxon` moves from `:135` to **`:148:23` — `content.managed`**, i.e. `.managed` on a plain BINDING yielding an ESCAPING value. **That is the exact INVERSE of the flip slice 5 just built** (array→buffer, where `init` is buffer→array), and the two want to land together: one is `retainBorrowedAggregate` + *no* mark, the other is the same retain + `markBufferSurface`. ⛔ **It is NOT this row's to take — `A2m` names it explicitly** (*"a bare `.managed` binding"*) and sits in **BATCH7**, live in L-parser-postfix. ⇒ **R4.5 is now a ONE-MECHANISM wait**: when BATCH7 lands, File.maxon should go as a single rung — `.managed`-as-a-value, the whitelist entry, and `specs/file-io.md` (7) + `specs/file-info.md` (6) ported byte-identically. ⭐ Both spec files re-surveyed 2026-08-01 and **NEITHER carries a `<!-- targets: -->` marker or a `Stdout:<target>` block**, and neither exists in `specs-shv2/` yet, so the port is clean; all 13 cases spell `File.<op>(FilePath from "…")`, and the `from`-literal sugar those need **already landed** with slice 3 | L-stdlib | ▣ **BATCH2** *(blocked — see batch)* | on ~~R4.4~~ ✅ · ~~`String.addressableBytes`~~ ✅ · ~~`Array.init`~~ ✅ · **`.managed` as a VALUE ⇒ `A2m`, BATCH7** | — |
| **S5** | **`self` AS A VALUE — FilePath's SOLE remaining blocker, at exactly TWO sites.** `stdlib/FilePath.maxon:422` (`normalize()`) and `:431` (`resolve()`) both `return self`; shv2 refuses with *"'self' as a value … hands a struct across a call boundary, which needs the ownership ruling struct-typed parameters and returns are deferred to"*. ⭐ **THE DIAGNOSTIC OVER-STATES THE GAP, MEASURED**: struct-typed parameters and returns ALREADY WORK across 13 of this module's signatures (`other FilePath`, `base FilePath`, `returns FilePath`) — what is missing is only the RECEIVER itself as a value. Proven by substitution: replace just those two `return self` with `FilePath{path: path}` and the module compiles to `E3001: No 'main'`, the same clean-library signal `stdlib/URL.maxon` gives. ⇒ closing this unblocks **R4.5** (whitelist `FilePath.maxon` + port `specs/filepath.md`, 40 cases) and through it the rest of BATCH2's cone. ✅✅ **CLOSED 2026-08-01** (main `e0b0c35f0`; shv2 3088 → **3102/0**, x64-linux 2981/0, wasm32-wasi 2932/0; **arm64 SKIP — remote, UNVERIFIED**). ⭐ **COORDINATOR-VERIFIED HEADLINE: `stdlib/FilePath.maxon`, UNMODIFIED (`stdlib/` diff empty), now compiles to `E3001: No 'main'`** — the clean-library signal this row predicted. **`R4.5` and BATCH2's slice 3 are unblocked.** ⭐⭐ **THE OWNERSHIP ANSWER WAS ESTABLISHED FROM THREE INDEPENDENT SOURCES, NOT CHOSEN**: `return self` is REFERENCE semantics — the receiver's box is CO-OWNED by an `__mm_retain`, never cloned. (1) the bootstrap oracle **executed** (a write through the returned handle gives `p.x=5 q.x=5`; a clone would print 7); (2) v1's `InsertRefcounts.planReturnIncrefs` emits `__mm_incref(self)` before the `ret` **and names `FilePath.normalize`/`resolve` as its drivers**; (3) `specs/memory-safety.md:12-30` — struct assignment copies the heap POINTER, `.clone()` is opt-in. The `__clone_<T>` cascade exists and is deliberately NOT used: a clone would disagree with `var q = p`. ⭐⭐ **THE REFUSAL'S “arrives at P1.4b” POINTER WAS STALE TWICE OVER** — P1.4b shipped 2026-07-20, **and** the machinery it deferred to (`__mm_retain`) was already in the tree, one screen away in the same file — and the same false premise was then found in **FOUR** doors, all now routed through one `promoteBorrowedToOwned`. **A message naming a milestone that has shipped is a false claim.** ⛔ **THE INDEPENDENT REVIEW FOUND A WRONG ANSWER THIS RUNG OPENED**: the trivial-tuple COPY was the `returned` door's private rule, so opening the `merged` door onto the shared promotion increfed a borrowed tuple and returned a value wearing the owned bit — `return t if c else (7, 8)` gave `a.1=99` against the oracle's `2`. **Verified a clean E2015 on the PARENT binary, so the rung owned it**; fixed behind a MEASURED hand-off/binding split, all four spellings now oracle-checked. ⛔ **AND IT CAUGHT A RED CROSS-TARGET LANE THE RUNG LEFT BEHIND** — only x64-windows goldens were regenerated, so three ternary-ownership goldens left **x64-linux RED**, plus 14 MISSING x64-linux goldens (non-gates, because a missing golden never fails). Both closed. ⚠ **STILL REFUSED, and it is a different question**: a MOVE into durable storage (`self.next = self`, `bag.push(self)`, `Self{f: self}`, `Cell.create(self)`) — a hand-off co-owns, a sink needs one owner and a poisoned source, which needs the transitive-consume fixpoint. ⚠ **arm64 goldens for the three ternary tests are STALE and cannot be regenerated from a Windows host** (`SpecTestRunner` panics: a Mach-O can only be spawned on macOS) — true at the parent too; owed to the periodic Mac sync | L-parser-decl | ✅ DONE | `slice/S5-self-as-a-value` | 2026-08-01T11:00Z |
| **A2p** | ⛔ **`maxon-sharp` CRASHES ON A USER `type String` — and it dies inside its own STDLIB parse.** Compiling a program that declares `type String` gives **`E9001: Expected hash stub for FilePathError`** in `Parser.SynthesizeEnumHashAndEquals`: a user declaration of `String` breaks the bootstrap's parse of a stdlib enum that has nothing to do with the program. Found by BATCH2 slice 2 while building the shv2 controls for the same shape. ⚠ **shv2 now REFUSES that program (E2015), so no shv2 gate can ever cover this** — the oracle is on its own here. Bootstrap, so it needs the full C# suite (3198+) plus codegen neutrality as its gate | L-bootstrap | ⬜ FREE | — | — |
| **A3e** | ⛔⛔ **`parseTypeReference`'s ALIAS ARMS ARE FILE-ORDER DEPENDENT, AND THEIR OWN COMMENTS ASSERT THE OPPOSITE.** Opened 2026-08-01 by BATCH7, which tripped over it twice and fixed only its OWN half. The arms' comments say *"during the SWEEP the index is still building, so `isGenericAlias` answers no and the reference stays `named`"*. **That holds only by accident of FILE ORDER**: the sweeper is handed the very index it is filling, so an alias declared in an EARLIER-SWEPT file *is* registered, the reference resolves to `genericInstance`, and the alias NAME is gone. ⭐ **The cure is to gate those arms on `allFilesFolded` — exactly what the comments already claim happens**, so this is code disagreeing with its own stated invariant. ⚠⚠ **AND THE BLAST RADIUS IS WIDER THAN IT LOOKS, measured by BATCH7's review**: that same between-passes divergence is what makes the ROOT element bit's two halves complementary — `fieldSurfaceOf`/`declaredReturnSurface` derive from an alias name off a swept type that is still `named`, which is true exactly when the token test found nothing. **`var bufs as BufArray` is correct today only because two order-dependent mechanisms cancel.** Closing this MUST land with a replacement for whichever half it kills; `managed-memory-methods.md`'s `-either-order` sibling-file pair is the test that will say so. ⚠ Reproducing needs SIBLING FILES with aligned names — the harness stages real files and builds the directory, so order follows `Directory.list`, not fragment order | **L-types** *(`Parser.parseTypeReference`'s alias arms · `SignatureIndex` canonicalisation)* | 🔶 CLAIMED | `slice/A3e-alias-arm-file-order` | 2026-08-01T18:05Z |
| **A3f** | **A TUPLE ALIAS DOES NOT CARRY ITS SLOTS' SURFACE, and a tuple slot spelled with an array-of-buffers alias works at the PARAMETER door only.** The last open spelling of the D11b/A2j/A2m family. `typealias Pair = (__ManagedMemory, Int)` then `p.0.length()` is still refused with the `Array` roster; and measured at all three doors, `function slotted(t (BufArray, Int))` → `t.0.get(0).length()` **answers (exit 5)** while the RETURN and FIELD doors refuse. Everything else in the family now works (BATCH7 closed the tuple return, tuple parameter, destructuring, `p.i`, the generic array element, a slot holding an alias-of-buffers at the parameter door, and nested tuples at any depth). ⛔ **BLOCKED, not merely unclaimed.** The return and field doors are captured by the declaration SWEEP, and unlike the ROOT's element bit a SLOT's has no read-time repair — `internTupleType` canonicalises element types, so the alias name is gone before anything could ask. Recording the fact when the `typealias` is read would make the answer depend on whether it sits above or below the function: **the exact order dependence `A3e` is.** ⇒ **take `A3e` first** | **L-types** | ⛔ BLOCKED | on **A3a** | — |
| **A3g** | **A SIXTH HAND-WRITTEN ROSTER: `parseStringStaticCall`.** Found by BATCH7's A2q implementer, deliberately not fixed, then half-fixed by its reviewer. *"the reference exports two, `from(bytes)` and `init(managed)`"* was prose beside the arms that serve it. ⭐ **The `init` half is now DERIVED** (its arm matches `StringInitMethod` and passes `StringInitManagedParamName`, so the constants exist). ⛔ **The `from` half structurally cannot be**: it is matched on **`TokenKind.from`** — `from` is a KEYWORD, not a `b"…"` constant — so the only spelling available is the token kind's `rawValue`, a DISPLAY string that exists for *"Expected 'identifier' but got 'from'"*. Coupling a member roster to it would render the roster wrong, silently, the day that string gains punctuation. ⇒ closing this needs a decision about minting a constant for a keyword-matched member, which is a language-surface question rather than a cleanup | **L-parser-postfix** | ⬜ FREE | — | — |
| **A3h** | ⛔⛔ **A USE-AFTER-FREE: AN ELEMENT BORROWED OUT OF A TEMPORARY ARRAY OUTLIVES IT, AND THE ANSWER IS SILENTLY WRONG.** Found by BATCH7's review, **re-measured independently by the coordinator against untouched `main` and against the ORACLE**: `let n = try make().get(0) otherwise …` then `n.count()` returns **63** under shv2 and **5** under the bootstrap. Plain `Array with ByteArray`; **no buffer surface anywhere**, so it is neither BATCH7's nor the A2j/A2m family's — it reproduces on a tree with none of that work in it. The temporary array is dropped at end of statement while the borrow it handed out is still live. ⚠ **A WRONG ANSWER, not a crash and not a refusal** — nothing reports it, the suite is green over it, and the leak gate never fires because the free is real. ⭐ **It needs a RULING before a fix**: either the temporary's lifetime extends to cover borrows taken from it, or the borrow is REFUSED at compile time. That is a language-level ownership question, which is why it is a rung and not a patch ⚖⚖ **USER RULING 2026-08-01 — EXTEND THE TEMPORARY'S LIFETIME. The question is settled; what remains is the mechanism.** A temporary that yields a BORROW is promoted from statement-scoped to the BORROWER's scope, so `make().get(0)` answers **5** and reads as it looks. This matches the oracle, which accepts the program and answers 5 — chosen over refusing the borrow (a loud, cheap divergence that would reject natural code) and over retaining at the accessor (which would make `get` mean two things depending on the receiver's PROVENANCE — the exact shape the D11b/A2j/A2m family has already cost three rungs). ⭐ **THE SCOPE IS NARROW AND FULLY MEASURED (current `main`, 2026-08-01), so the acceptance list is already written:** the defect is a BORROW of a MANAGED element out of an UNBOUND container — `make().get(0)` and `make().first()` both read **63**. Everything adjacent is ALREADY CORRECT and must stay so, and each is a control this rung owes: `make().pop()` → **5** (an owned move-out; the element outlives the container by construction), a SCALAR element (`Array with Int`) `make().get(0)` → **41** (copied out, no pointer to dangle), and `let held = make()` then `held.get(0)` → **5** (a binding already has function scope). ⚠ **The mechanism is `pendingTempDrops` (`Parser.maxon:15654`), which must learn which temporaries have an outstanding borrow.** The risk direction inverts with the fix — dropping too LATE is a leak, which the exit-101 gate DOES catch, where today's drop-too-early is a silent wrong answer nothing catches. ⚠ **It is silent in every channel this project has**: not a crash, not a refusal, and no leak (the free is legitimate), so the suite is green over it and only a hand-probe finds it — which is why it needs its own committed cases in all four shapes above | **L-types** *(ownership / temporary lifetime)* | ⬜ FREE | — | — |
| **A2q** | **`dispatchStringMethod`'s ROSTER IS A HAND-WRITTEN SECOND COPY OF ITS OWN ARM LIST, and it has already lied twice.** Every String method name is written twice — once as the constant the arm tests (`b"startsWith"`), once as a literal inside the `ParseError.unsupported` message. BATCH2 slice 2 fixed the CONTENT (the message claimed `utf16` *"has no consumer yet"* while the arm above dispatches it, and omitted five working methods) but not the SHAPE. ⭐ The rung's own handling of the new pair is the right precedent — rendered from `builtinConformerMethodNames` rather than retyped. ⚠⚠ **Rendering the existing constants would fix only the RENAME direction, not the *arm added, roster not updated* direction that actually bit twice** ⇒ the real cure is a TABLE-DRIVEN dispatch where the roster IS the dispatch table. Too big to ride along; filed as measured debt with the evidence ✅✅ **CLOSED 2026-08-01 (main `b3450ea1b`) — AND THE ROSTER WAS LYING WHEN THE COORDINATOR MEASURED IT.** `addressableBytes` and `byteAtOrPanic` are real, dispatched arms named NOWHERE in the sentence that claims to list what shv2 provides. The cure is the one this row asked for and one step further: the roster is built from **the very constants the arms match on**, a **membership GATE** runs ahead of the arms and a **DRIFT PANIC** sits at the fall-through — together they close BOTH directions (an arm without a roster entry is unreachable; a roster entry without an arm panics naming the list), which is what makes this different from rendering the existing constants and fixing only renames. The drift panic was verified by SABOTAGE, not asserted. Same treatment given to `dispatchCharacterMethod` and `dispatchStringIndexMethod`; `MemberSurface` now reaches the REFUSALS too (the review found four gates and five sentences in four homes). ⚠ Two single-member dispatchers deliberately LEFT (`dispatchCharacterSetMethod`, `parseManagedMemoryStaticCall`) — a one-element roster reads worse than the `if` it replaces — and their names are now rendered from the arms' constants anyway. ⇒ opened **`A3g`** for the sixth copy, at `parseStringStaticCall`. | L-parser-postfix |  ✅ DONE  | `slice/BATCH7-roster-derivation` | 2026-08-01T15:24Z |
| **S4** | ⛔⛔ **A STDLIB-ONLY RUNTIME BODY HAS NO GOLDEN AND NO REACHABLE TEST — and BATCH2 slice 1 shipped the first one.** `__str_bytes_view` and `__str_byte_at_or_panic` are compiler-emitted runtime whose **whole bodies carry no golden at all**: the committed fragments record only Maxon-DECLARED functions. ⛔ **The consequence is MEASURED, not feared.** `emitStringBytesAllocation`'s owed-base (`separate`) arm — the one a DETACHED String's view takes — is **reachable and load-bearing**, and its failure mode is **SILENT**: the review's control read `sum=3240` clean against `sum=2520` with the incref neutered, a real **use-after-free**, while the full suite stayed **3050/0 and the exit-101 leak gate saw nothing** — because removing the incref removes the matching decref, so the counts balance. ⚠⚠ **THE COORDINATOR'S OWN SABOTAGE WAS SELF-CANCELLING AND READ AS "UNREACHABLE"; the independent reviewer refuted it with a proper control.** ⇒ **A sabotage that removes BOTH SIDES of a refcount is not a discriminator** — it proves only that the two edits cancel. ⛔ It cannot be closed by an ordinary spec: the methods are stdlib-only and `SpecTestRunner.stageSourceFiles` writes every test under `specs-shv2/.spec-tmp/`, with `isSafeRelativeName` refusing the `..` that would escape it — and weakening that check to buy a test would trade a real containment guarantee for one. ⇒ **needs its own gate mechanism**: a golden for emitted runtime bodies, or a stdlib-rooted harness. ⚠ **Re-measure trigger**: the first whitelisted stdlib module that `append`s and then takes bytes makes the uncovered arm live on the corpus ◑ **HALF CLOSED 2026-08-01** by BATCH8 (main `01cc68dc6`). ` ```RequiredRuntime ` names emitted runtime functions to render into a test's golden; `specs-shv2/emitted-runtime-body.md` pins `__str_bytes_view`, `__str_byte_at_or_panic`, `mrt_start` and `__arr_set`. **The silent UAF this row measured is now caught** — two independent one-sided sabotages each produced exactly one failure, this gate, against 3201 green. ⭐ **THIS ROW'S STATED BLOCKER WAS FALSE**: the methods being stdlib-only blocks CALLING them, not RENDERING them, and an ordinary `FilePath` program already emits both — so no stdlib-rooted harness was built and `isSafeRelativeName` was never weakened. ⛔ **THE RESIDUAL, MEASURED**: only runtimes that are `TargetModule` FUNCTIONS are pinnable. **Hand-assembled byte-level chunks are not** — `--emit-ir-runtime=__gt_morestack` is correctly refused (*"this program emits no function named…"*) because those chunks never enter `module.functions`. Pinning them needs a different mechanism (a byte-range gate over emitted `.text`) ✅ **AND THAT RESIDUAL IS NOW CLOSED TOO — slice 2, main `bc235a3f3`.** The byte-level chunks were pinnable all along (`FunctionCodeChunk.funcName` + `.code`); `__gt_morestack` and `mrt_panic` are pinned as byte dumps. **Every emitted runtime this compiler produces — Std-tier function OR hand-assembled chunk — can now be recorded in a golden**, which is the whole of what this row asked for | L-stdlib | ✅ **DONE** *(BATCH8 s1+s2)* | — | — |
| **D11e** | **`markBufferSurface` HAS A FOURTH PRODUCER, AND D11b'S INVERSION NOW REACHES IT.** BATCH2 slice 1 added `String.addressableBytes()` (`dispatchStringMethod`) as a buffer-surface producer, so the D11b/A2j finding — a value's buffer surface does not survive a DECLARED type or a `var` REASSIGNMENT — gains one more door: `var v = s.addressableBytes()` then `v = t.addressableBytes()` makes `v.length()` an unknown-`Array`-method **E2015**. Reported by the BATCH2 review and deliberately NOT fixed there: the lane was held by BATCH3 at the time. ⚠⚠ **THE CALLER-COUNT COMMENT AT `Parser.maxon` HAS NOW BEEN WRONG THREE TIMES** — it said TWO while there were three, THREE while there were four, and then **A2j's correction and BATCH2's each restated it WITHOUT the other's caller**, so a clean textual rebase would have landed a fourth wrong count. Resolved by grepping the merged file rather than merging the prose: **five call sites — four value-minting producers (`parseManagedMemoryStaticCall`, `parseArraySlice`, `emitOwnedManagedMemoryResult`, `dispatchStringMethod`) plus `markDeclaredBufferSurface`, which A2j reaches from three declared-type sites** — and the comment now says to COUNT them, not copy the sentence. ⭐ That is this project's signature bug caught live: one fact written twice, with two agents each fixing half ⚠⚠ **PREMISE MEASURED FALSE 2026-08-01 — IT DOES NOT REPRODUCE THROUGH ANY USER-REACHABLE PRODUCER.** Probed on `ca9a69fd8`, three routes: (1) `var v = x; v = y; v.length()` with both from `__ManagedMemory.create` ⇒ **compiles and runs, correct**; (2) the same with `setLength` so the length is non-zero ⇒ **correct**; (3) `var w = s; w = v` reassigning a `slice` result to a buffer ⇒ **correct** (combined probe returned 12 = 6+6). The buffer surface SURVIVES a `var` reassignment. ⚠ **And the row's own example cannot be written by a user at all**: `addressableBytes` is stdlib-only (BATCH2 slice 1), and the one whitelisted module that calls it binds with `let`, not `var` — so the shape is unreachable in every tree today. ⚠ `.managed` in VALUE position is still refused (a different, known gap — R4.5's list), which is what blocks the fourth producer from ever reaching a `var`. ⇒ **Either A2j closed this half along with the DECLARED half, or it was never reachable.** Left FREE rather than closed because I did not test a stdlib-internal route: if you can construct one, the row is real; otherwise close it. **Do not implement against the description without reproducing first** ✅✅ **CLOSED 2026-08-01 AS NOT REACHABLE — the disposition this row itself asked for, and the premise was re-measured INDEPENDENTLY before closing.** The row said *"Either A2j closed this half along with the DECLARED half, or it was never reachable"* and *"do not implement against the description without reproducing first"*. The coordinator reproduced both user-reachable producers on `main`: `create`→`var` reassignment answered **2**, and `slice`→`var` reassignment answered **3** — the buffer surface SURVIVES a `var` reassignment, in both routes. The row's own example is unwritable by a user (`addressableBytes` is stdlib-only and its one whitelisted caller binds with `let`). ⇒ closed, and **PINNED by two committed cases** (`buffer-surface-survives-a-var-reassignment`, `…-reassigned-from-a-slice`) so a future rung cannot silently reintroduce it — a closure by measurement plus a regression pin, not by assertion. | L-parser-postfix |  ✅ DONE  | `slice/BATCH7-roster-derivation` | 2026-08-01T15:24Z |
| **D13** | **shv2 HAS NO `String.equals`, AND THE ORACLE DOES.** `u.scheme().equals("https")` compiles and runs clean under `maxon-sharp` and is **E2015** under shv2 — found because the coordinator's own BATCH2 acceptance snippet used it. `==` works and is what every one of `specs/url.md`'s 42 cases spells, so nothing on the URL cone needs it; this is a missing method on the builtin roster, not a blocker. ⚠ Check the corpus for the false-reject set before adding it — the roster's refusal message is the authority on what `String` currently provides ✅✅ **CLOSED 2026-08-01 by BATCH2 slice 2** (main `3cdf0bdd5`), folded into the `hash` rung rather than run as its own loop — same file, same dispatch chain, same mechanism, so it was a WIDER WAVE and not a second slice. `s.equals(t)` and `s.hash()` now route through the existing `emitBuiltinConformanceCall`; shv2 prints `""`→5381, `"abc"`→193485963, `equals` true/false, **byte-identical to `maxon-sharp`** | L-parser-postfix | ✅ DONE | `slice/BATCH2-stdlib-file-cone` | 2026-08-01 |
| **A2r** | ✅✅ **CLOSED 2026-08-01** by BATCH1 (main `d4cd6aa20`). Reproduced first (`flat-namespace: … .scale-tmp/mmtrace-src/build.maxon`, exit 1 on a CLEAN checkout; OK the moment the dir is removed). ⭐ **Asks GIT instead of gaining a fifth hand-maintained exclusion set** — `git check-ignore --stdin -z`, one spawn. The tree had **FOUR walkers with FOUR different exclusion sets** and none consulted `.gitignore`; a fifth would have been the defect. ⭐ **Negative controls REQUIRED and run**: a genuine untabled project (not ignored) is still REFUSED by name, and a `build.maxon` without a `build()` export is still ignored — verified by the review as not passing vacuously. ⚠ **A suspected `ErrorCodeRegistry` twin DOES NOT EXIST and was checked rather than filed**: its walk is rooted at `root/maxon-sharp`, and worktrees are siblings of that, so it cannot descend into one. The two walkers are genuinely different in SCOPE ⇒ deliberately NOT forced together, and a comment says so. ⛔ **A GITIGNORED SCRATCH DIR MAKES `dotnet build` FAIL ON A CLEAN `main`.** `flat-namespace check` walks the whole tree and refuses any directory holding a `build.maxon` that exports `build()` which is not in `FlatNamespaceCheck.Units` — including **`.scale-tmp/mmtrace-src/`, which `run_scale_test --perType` creates itself**. Measured 2026-08-01 at the start of BATCH2: a clean checkout, nothing uncommitted, and `dotnet build` exit 1 naming `.scale-tmp/mmtrace-src/build.maxon`. ⭐ **The cure already exists one case over and was not generalised**: `IsInNestedCheckout` (`FlatNamespaceCheck.cs:368`) excludes worktrees for precisely this reason, and its own header records the identical symptom (*"three untabled projects reported, `dotnet build` exit 1, and nothing wrong with the tree at all"*). The instrument's own scratch output wants the same treatment. ⚠ It bites hardest UNATTENDED: any agent that runs `perType` and later builds the bootstrap gets a red baseline it did not cause | L-bootstrap | ▣ **BATCH1** | — | — |
| **D10** | **A USER type's VOID static may not appear in STATEMENT position.** `Helper.shout()` as a bare statement is **`E2015 Unsupported: identifier statement`** — measured 2026-07-30 on `main` *after* R4.2, so it is live, general and pre-existing. ⭐ **R4.2 fixed exactly the compiler-owned half** and the asymmetry is the row: its `statFree` had the same refusal for the same structural reason, and the fix (`voidCompilerStaticCallsAt`, `Parser.maxon:9864`/`:30180`) covers `__Managed*`/`__Builtins` statics only. **A value-returning static in statement position is a different question** (it needs `_ =` or a discard rule); this row is the VOID one, where there is nothing to discard and the call is the whole point. ⚠ It is the third time this exact shape has cost something — `__Builtins.sleep` first, `statFree` second — and the pattern is always *a void member of a family whose other members all return a value* ✅✅ **CLOSED 2026-07-30.** x64-windows 2708→**2717/0** · x64-linux **2588/0** · wasm32-wasi **2579/0** · arm64 SKIP — remote, UNVERIFIED. Fragment ADDITIONS ONLY, zero modifications; scale-test bit-identical at all six rungs. ⭐ **THE FIX IS A DERIVATION, NOT A THIRD TABLE.** `voidCompilerStaticCallsAt` recognized exactly two compiler-owned things — a hardcoded `__ManagedFile.statFree` and the `__Builtins` intrinsic table — so which statics could be statements was decided by *which callees happened to have a recognizer*, never by a rule. It now asks the WHOLE-PROGRAM INDEX for the callee's return type and accepts any declared **void** static, so it is renamed **`voidStaticCallsAt`**: leaving `Compiler` in the name would have been a false statement the moment a user type was accepted, which is the exact defect class this rung's own review kept finding. ⚠ **VOID IS THE WHOLE CONDITION, and the reason is the one the dispatch site already stated**: a value-returning static is refused here because a bare statement discards a box nothing would then free — a LEAK argument that does not reach a void static, which has no value to discard. `Point.create()` keeps its refusal, unchanged and pinned. ⭐⭐ **PROBING FOUND A SECOND DEFECT THAT THIS RUNG ITSELF CREATED, AND IT WAS FIXED RATHER THAN FILED.** With void statics legal, `Helper.nope()` still died as *"Unsupported: identifier statement"* — **a message that became FALSE the moment the rung landed**: the construct IS supported, the NAME is wrong, and E2015 says nothing about a name. Measured: the identical typo already got the positioned **E3004 `call to undefined function`** in EXPRESSION position AND from the compiler-owned half in THIS position (`__Builtins.nope()`), so shv2 held THREE answers for one question. The door now claims a qualified call whose base is a declared type even when the member resolves to nothing, routing it to `parseCallNamed` ⇒ one answer. **Exactly one case moves** — a member no file declares, from a refusal about SHAPE to one about the NAME; nothing else in the suite changed. Probes that came back clean, all now unnecessary to re-run because three became specs: a void static taking a MANAGED `String` argument (builds, exit 0, **no leak**), and a THROWING void static without `try` (correctly `E3057`, naming the right construct). **Ports `specs/static-methods.md` BYTE-IDENTICAL** — 3 cases, all green with no edits — and authors `specs-shv2/static-method-statement.md` (4 cases) because **every one of the corpus's static cases RETURNS A VALUE**, so the corpus had no case in this position at all. That is why the gap survived. ⭐ Both positives verified RED by SABOTAGE (remove the index arm ⇒ both fail `E2015`, while the negative case correctly stays GREEN because the sabotage restores the refusal it asserts, and the three ported cases are unaffected). ⛔⛔ **AND THE INDEPENDENT REVIEW FOUND THE RUNG COMMITTING THE DEFECT IT WAS FIXING, ONE LEVEL DOWN.** `voidStaticCallsAt`'s SHAPE CHECK asked a raw `TokenKind.identifier` at the member position — so **a void static DECLARED UNDER A KEYWORD** (`Helper.to()`, likewise `match`/`upto`, all legal since D8) was still `E2015`, while the ORACLE compiles and runs it and shv2 itself accepts the identical name in EXPRESSION position. **I widened the door and left a raw token scan standing in front of it**, re-imposing the exact asymmetry the rung exists to remove — and this is the D8 failure class the project has already paid for once (*"all FOUR reachable defects were TOKEN SCANS asking a raw `TokenKind`"*). Fixed with the shared `namesMemberAt` that `methodCallsAt`/`memberCallFollows`/`fieldAssignsAt` already spell the member half with; the BASE half deliberately stays `identifier`, because `type to` is refused at its declaration and `parseQualifiedCall` consumes the base with `consume(TokenKind.identifier)`. **Verified independently on both binaries before accepting it.** Review also killed a **FALSE SOUNDNESS ARGUMENT I had written**: my comment claimed the predicate is reachable from the tolerant declaration sweep and sound because it answers `undeclared` there — **it is not reachable from the sweep at all** (`foldDeclaredSignaturesInto` never reads a body; `queryProgramSignatures` sets `allFilesFolded` before the first file is parsed), so a true conclusion rested on a reason that CONTRADICTED an existing comment about the same fact. Plus two stale rename references — one still calling the shape *"a COMPILER-OWNED STATIC"*, the very false name the rename existed to remove — and a doubled `qualifiedCallBaseName` derivation. ⭐ **Everything else it attacked came back CLEAN BY MEASUREMENT rather than by reading**: no leak on the newly-legal path under a struct, an `Array`, a union payload, a consuming `String` static, or an interpolation temp in a loop (never 101); arm 2 newly accepts NOTHING — `Color.nope()`, `IntArray.nope()`, `String.nope()`, `CharacterSet.nope()`, `__ManagedMemory.nope()` all keep E2015 because `containsStruct` is false for each — and `__ManagedFile.nope()` IMPROVES; and `returnTypeOf`'s adopt-inside-a-predicate is unobservable, because E3062 reads a different set and a `false` answer always ends in a throw. | L-parser-stmt | ✅ DONE | `slice/D10-void-static-statement` | 2026-07-30T17:25Z |
| **7b2** | **"WHERE DOES OUTGOING ARG SLOT k LIVE" IS SPELLED THREE TIMES ON THE x64 PATH.** `win64OutgoingArgDisp(k) = ShadowSpaceBytes + k * SpillSlotBytes` is the rule the ISEL places every >4-argument import's stack arguments by — and it is file-PRIVATE to `StdToX64Conversion`, so the two places that must reproduce those exact addresses each respell it: the GT syscall shim's per-word copy (`emitGtShimmedIatCall`, whose stride 7a's review just corrected from the WRONG constant) and the hand-assembled completion loop's `IoLoopFifthArgDisp = 0x20`, a bare literal for `ShadowSpaceBytes` in the ONE site that actually passes `GetQueuedCompletionStatus`'s 5th argument. Three spellings of one addressing rule, on a path where disagreement is uninitialised kernel arguments and no test fails. Export the rule; make all three ask it ✅✅ **CLOSED 2026-07-30.** x64-windows **2734/0** · x64-linux **2605/0** · wasm32-wasi **PASS** · arm64 SKIP — remote, UNVERIFIED. **ZERO fragment churn**, which is the proof rather than a side note: every one of the three sites had to be provably the same number, and a single moved golden would have meant one of them was not. ⭐ The completion loop's site is also what makes 7a's `GetQueuedCompletionStatusStackArgWords` **LOAD-BEARING rather than merely stated** — that constant says the import has one stack argument, and this is the one place that writes it. ⚠ `win64OutgoingArgDisp(0)` is asked at the USE SITE, not bound to a `let`: a global initializer may hold no function call (`E2045`), which is a real constraint on how far a rule like this can be pulled out. ⭐⭐ **ONE SPELLING WAS DELIBERATELY LEFT ALONE, AND THAT IS THE INTERESTING PART.** The shim's FRAME SIZE (`ShadowSpaceBytes + stackArgWords * SpillSlotBytes`) arithmetics to exactly `win64OutgoingArgDisp(stackArgWords)` and I did NOT fold it in: a SIZE and a DISPLACEMENT are different kinds that coincide because the frame ends where the next slot would begin. Folding them would join two facts on the strength of a coincidence — **precisely the error 7a's review found one function away**, where `GtArgSlotBytes` and `SpillSlotBytes` were two facts sharing the number 8. The rung exists to remove that error, not to commit it in reverse; both still derive from the same two constants, which is the part that has to be true, and the reasoning is written at the site | L-gt-runtime | ✅ DONE | `slice/7b2-outgoing-arg-disp` | 2026-07-30T19:15Z |
| **7a** | **`X64GtRuntime.gtShimStackArgWords` is an int-keyed `if`-chain ending in a bare `return 0`** — a table that must be COMPLETE, whose incompleteness is silent. **Not a live defect** (R4.2 verified its five new imports genuinely need 0 words, and added the missing `CreateFileA` entry that R4.1 had left out). But R4.1's omission meant **a file open from a SPAWNED GREEN THREAD copied zero stack words instead of three**, so three `CreateFileA` arguments were garbage — a wrong answer reachable only off the main thread, which no committed case reaches. It also violates the project's own rule that an unhandled path throws rather than returning a default. The honest fix DERIVES the shim's copy count from the same place ISEL reserves it, which needs an op→slot mapping — a cross-cutting refactor, not a review edit (R4.2 review's call, and I agree) ✅✅ **CLOSED 2026-07-30 — AND THE FIX IS SMALLER THAN THE ROW PREDICTED, BECAUSE THE ENUM ALREADY CARRIES THE COMPLETENESS.** The row called for deriving the copy count from the op→slot mapping ISEL reserves with; that is not needed. The seven non-zero counts were ALREADY single-sourced (`CreateProcessStackArgWords` &c., shared with `stdOpOutgoingArgBytes` so copy and reservation cannot drift) — **what was silent was the SLOT→CONSTANT mapping**, an int-keyed `if`-chain whose bare `return 0` was indistinguishable from a correct answer. It is now a **`match` EXHAUSTIVE over `IatSlot`** (41 arms), reached through `IatSlot.allCases`, with **no `default` arm and a note that there must never be one** — a `default gives 0` would restore the defect wearing a match's clothes. ⭐ **PROVEN BY SABOTAGE, which is the whole deliverable**: add an `IatSlot` case and the compiler refuses — `E2026: match on enum 'IatSlot' is not exhaustive, missing: …`. **R4.1's omission is no longer expressible**: adding `CreateFileA` would not have compiled until its count was stated, and that omission is what left three `CreateFileA` arguments as garbage on a SPAWNED green thread. ⭐ **A `0` IS NOW A CLAIM, NOT A BLANK** — every register-only arm carries its argument count so the claim is checkable against MSDN without reading the emitter. ⚠ **ONE ARM WAS MEASURABLY WRONG IN THE OLD FALLTHROUGH**: `GetQueuedCompletionStatus` takes FIVE arguments, so one lands on the stack, and it was answering 0. Harmless today — the completion loop is hand-assembled and calls it DIRECTLY, never through the shim — and **that unreachability is now corroborated rather than asserted: its count changed 0→1 and the fragment diff is ZERO MODIFICATIONS**, which is only possible if nothing emits it as a shimmed `iatCall`. Stated at its true ABI count anyway, so the answer stays right if it is ever routed through the shim. ⚠ **NO arm64 COUNTERPART IS OWED** — `IatSlot` is the PE import table and arm64 references it nowhere (checked), so the shim is x64/Windows-only by construction, not by omission. **x64-windows 2734/0**, fragment additions only, **zero modifications** ⇒ byte-identical codegen for every existing program, which is the expected result for a refactor that changes no reachable answer ⭐⭐ **THE INDEPENDENT REVIEW CHECKED ALL 41 ABI VALUES ONE BY ONE AND FOUND NONE WRONG — and it did it against an INDEPENDENT WITNESS rather than my comments**: the bootstrap's own hand-assembled call sites (`maxon-sharp/…/X86CodeEmitter.Runtime.cs`), which set registers explicitly and therefore *state* each arity. It caught the traps I was most exposed to and confirmed I had avoided them — `VirtualAlloc` is 4 not `VirtualAllocEx`'s 5, `WaitForMultipleObjects` 4 not `…Ex`'s 5, `VirtualFree` 3 not `VirtualFreeEx`'s 4, `FindFirstFileA` 2 not `FindFirstFileExA`'s 6 — and separately verified the **dual invariant nothing enforces, reservation ≥ copy**, across all nine live op→slot pairs. ✅ It also confirmed `GetQueuedCompletionStatus` is **STRUCTURALLY** unreachable through the shim (its only emission is a bare `encodeIatCall` in the hand-assembled completion loop; no `TargetOp.iatCall` names it), so the zero-golden result is corroboration rather than the argument. ⭐ **AND IT TALKED ME OUT OF THE OBVIOUS SIMPLIFICATION**: replacing the `allCases` walk with `IatSlot.fromRawValue(slotIndex)` looks like an O(1) win, but the walk compares through `iatSlotIndex` — **the same function that MINTS the index** — so it is a derived inverse; `fromRawValue` inverts `.rawValue`, a second assumption that merely coincides today and would silently return the wrong slot the day `IatSlot` is given explicit raw values. **KEPT DELIBERATELY.** ⛔ **ONE REAL FIND, PRE-EXISTING, IN THE FUNCTION I REWROTE: the shim strided its copy by `GtArgSlotBytes` — the ASYNC ARGUMENT-BUFFER's stride — where the addresses it must reproduce are placed by `win64OutgoingArgDisp`, which strides by `SpillSlotBytes`.** Two names, two facts, one number (both 8), nothing making them agree. **The project has already swept exactly this confusion TWICE and written it down both times** (`GtRuntime.maxon:242`, `X64PrologueEpilogue.maxon:144` — *"it had quietly become the spelling for 'a stack word' as well, in four unrelated places"*) **and the shim was missed by both sweeps.** Failure scenario: widen `GtArgSlotBytes` 8→16 and every isel arg store still lands at `[rsp+0x20+k*8]` while the shim reads and writes `k*16` ⇒ a spawned green thread's `CreateProcessA`/`CreateFileA` gets uninitialised stack arguments, with no compile error and no failing test — **the R4.1 defect arriving through the other door of the same function.** Fixed, plus `ShimPushedWordBytes` sourced from `StackWordBytes` rather than a bare `8`. Byte-identical (8→8), zero fragment churn, re-verified 2734/0 by me | L-gt-runtime | ✅ DONE | `slice/7a-gt-shim-stack-words` | 2026-07-30T18:40Z |
| **D1** | **Methods on `union` / `enum`** — the DECLARATION SITE and the RECEIVER. ✅ `6b7c8a94b`..`f4c1c43e5`; **2372 → 2423/0**. `enclosingStructType`→`enclosingSelfType` (a struct's `Self` is `structRef`, an enum's `named`), `enterTypeScope` as ONE writer for name+kind, `enumMethodKeywordIndex` as one spelling of the modifier list. Statics **refused** (oracle misreads them; stdlib declares zero). Ports `specs/enum-full.md` byte-identical (36 of 54 enabled, 18 disabled with named blockers) + flips 2 in `enums-simple.md` + a shv2-authored receiver spec. ⭐⭐ **THE REVIEW FOUND 3 REACHABLE DEFECTS** — see the box. ⚠ **Moved NO committed goldens** (0 M); **added 51 x64-linux goldens the lane was silently missing**; arm64 SKIP — remote, UNVERIFIED, owes the same 51 | L-parser-decl | ✅ DONE | `slice/D1-union-enum-methods` | 2026-07-29T19:14Z |
| **D1c** | **`match` arm with a DISCARDED payload binding is silently ACCEPTED** — `value(_)` compiles and runs (exit 0) where the oracle gives **E3081**. Confirmed by the D1 review; pinned as `enum-full.md`'s `disabled-test: error.match-discarded-bindings`. A genuinely MISSING check, not a wording difference, and shv2 claims no E3081 ✅ **CLOSED 2026-07-30** (`e700a5133`..`f081f9392`). **E3081** now fires iff the binding list is non-empty AND every binding is `_` — the oracle's rule, MEASURED: `two(_, b)` partial discard stays LEGAL, a bare case name stays legal, `one(x)`-unused is a different rule entirely. Registry: added the missing `shv2` claim to the EXISTING E3081, no new number. Flips `enum-full.md`'s `error.match-discarded-bindings`. ⚠ Two PRE-EXISTING shv2-authored specs were INVALID programs the oracle rejects (`text(_)`) — rewritten to a PARTIAL discard, **not** a bare case name, because a bare name carries no binding list so `declarePayloadBindings`'s discard branch would never run and the tests would have passed while testing nothing (the P1.9 masked-subject trap). Their 4 goldens move for that reason only. ⭐ The first cut's comma-join dedup put a `StringArray.create()` in `checkEnumExhaustive`, which runs on EVERY enum match: **+20 allocs at rung 0 doubling to +640 at rung 5, ×2.00** — a CORRECT dedup that was still UNSCALABLE, caught by the optimizer pass and removed | L-parser-stmt | ✅ DONE | `slice/D1d-keyword-block-structure` | 2026-07-29T23:05Z |
| **D1d** | **⚠ REACHABLE COMPILER PANIC, PRE-EXISTING and D1-INDEPENDENT** — a keyword-named enum case used in an `if` condition (`if tagOf(Kw.end) != 3 'bad'`) ⇒ `panic at Parser.maxon:14467: parseIfStatement: the token scan predicted the closing 'end' at token 58 but the parser closed the last body at token 67`. Root: `closesBlockAt`/`opensBlockAt` read a keyword **after a `.`** as block structure. **The repro contains no enum method at all**, which is what proves it is not D1's. Same family as the two case-list misreads D1's review fixed, different walk ✅ **CLOSED 2026-07-30** (`e700a5133`..`f081f9392`). Cured at the ONE shared exclusion: `keywordIsMatchCaseName` → **`keywordIsAName`**, now a case-name LOOKAHEAD **or** a member-name LOOKBEHIND (`prev == dot`) **or** a payload-carrying case name. ⭐ **THREE MORE SCANS tested `TokenKind.end` themselves and needed folding in** — `blockDepthAfter`, `parseMatchCommon`'s end-of-arms test, and `elseFollowsBlockEnd`, whose 'is the previous token an `end`' was a THIRD panic (a ternary whose condition ends in a keyword member). `ensureSiblingReceivers` was refusing legal programs in BOTH directions. `assertScanAligned` is byte-unchanged — it produced every RED. ⭐⭐ **THE REVIEW FOUND THE ONE SHAPE THE FIX ITSELF MISSED, AND IT WAS RUNG-HALTING**: a keyword case name carrying a PAYLOAD (`end(m) then …`) — the lookahead is ONE token and the binding list sits between the name and its separator, so `end(m)` was falsely rejected (E2026 not-exhaustive) and `while(m)` **PANICKED**, both on programs the oracle runs to 42. Two subtleties it had to get right: the payload shape's separator set must be NARROWER (`then`/`gives` only — `if (a and b) or (c and d)` is legal Maxon and `stdlib/FilePath.maxon:137` is exactly that, verified RED by widening it), and the payload test may only answer YES and must fall through to the lookbehind (`Kw.end(20, b: 22)` has both) | L-parser-stmt | ✅ DONE | `slice/D1d-keyword-block-structure` | 2026-07-29T23:05Z |
| **D1e** | ~~**A method call on an RVALUE base is refused entirely**~~ ⭐ **THE ROW WAS DRAWN STALE — D2 CLOSED IT HOURS LATER, AND BOTH OF ITS OWN REPROS NOW RUN.** Measured 2026-07-30 on a freshly built binary, not read off the plan: `Point.create(21).doubled()` returns **42**, and `Kw.beta.twice() * 2` returns **42**. The row was filed by D1's review as an `E2015 Unsupported: . statement`; D2's `MethodReceiver` + `dispatchMethodOnReceiver` switch then made *both* spellings ride one dispatcher — its report says so directly (*"D1's enum arm gained a value receiver for free by riding the same switch"*), and a static-factory CALL RESULT was its own unnamed fifth symptom. ⇒ **Nothing to implement.** ⚠ **But it is closed by MEASUREMENT, not by COVERAGE**: `specs-shv2/postfix-member-walk.md` pins a method on a call result and on a literal, and **neither of D1e's two exact spellings has a committed case** — an enum CASE VALUE receiver (`Kw.beta.twice()`) is precisely the "for free" claim, and a free unlock with no test is a regression waiting for its rung. ⇒ **The residual is 2 spec cases, not a mechanism** — cheap, spec-only, collides with nobody (a spec file no other branch carries). Whoever takes it: pin both spellings, do not re-derive the feature ✅✅ **CLOSED 2026-07-30** (main `8cdc63b80`; x64-windows 2541→**2544/0**, x64-linux PASS, wasm PASS, arm64 SKIP — remote, UNVERIFIED). **Fragment ADDITIONS only on both live lanes, ZERO modifications**, and the compiler source diff is EMPTY — `Parser.maxon` verified byte-identical. ⛔⛔ **THE COVERAGE CLAIM ABOVE IS ITSELF MEASURED FALSE — THE ROW WAS DRAWN STALE A SECOND TIME, ON THE VERY SENTENCE CORRECTING THE FIRST STALENESS.** Both named spellings DO have committed cases, added by D2 hours before this row was written: `postfix.method-on-a-call-result` (`Inner.make(9).get()`) is `Point.create(21).doubled()`'s door, and **`postfix.enum-method-on-an-enum-literal` (`Direction.south.score()`) is exactly the enum CASE VALUE receiver the row calls "precisely the 'for free' claim"**. ⭐ **AND THAT WAS SETTLED BY SABOTAGE, NOT BY READING TWO PROGRAMS AND CALLING THEM THE SAME SHAPE**: deleting `parsePostfix`'s value-receiver arm (`:18614`) reddens **126 tests**, both of those among them. ⭐⭐ **THE REAL GAP IS ONE TYPE OVER, AND THE SAME SABOTAGE FOUND IT: `enum-union-method-receiver.md` DOES NOT APPEAR IN THE 126 AT ALL.** Not one case in D1's own receiver spec depends on the value door — every one binds its receiver to a name first (`let o = …; o.isPass()`). But D2's switch is gated on `containsEnum`, whose own comment reads *"a declared enum/**union**"*, so a **union** received its value spellings for free at the same instant an enum did, with **zero** committed coverage on either spelling. That is the row's own "a free unlock with no test is a regression waiting for its rung" — aimed at the wrong type. ⇒ **3 cases added to `postfix-member-walk.md`**, each verified RED under the sabotage and GREEN without it: a union method on a CALL RESULT; on a case CONSTRUCTOR result + a payload-less case literal (one program, two producers of one question); and on a **MANAGED-payload temporary over 200 rounds**, where no binding exists to hang the drop on so a per-iteration leak is exit 101. ⭐ **That leak gate was checked for VACUITY rather than assumed** — `__mm_leak_check` confirmed present in the emitted binary (shv2's leak check is unconditional for any program that pulls in the allocator; `ConditionalCompilation.maxon:432`), so the case can actually fail the way its description claims. ⚠ **The two spellings the row asked for were deliberately NOT added**: they are already pinned, and a second copy of a pinned fact is this project's signature bug, not extra safety | L-parser-postfix | ✅ DONE | `slice/D1e-rvalue-receiver-pins` | 2026-07-30T02:41Z |
| **D1b** | **Binding a MANAGED payload out of a BORROWED union** (`E2015` at `Parser.maxon`, whose text already says *"a parameter, **a receiver**"* and blames a P1.4 that has since closed). Two sub-cases the design must separate: **read-only borrow** (`unreadable(path) gives "…{path.toString()}"` — `PeReadError.displayReason`, never moves) vs **move/copy out** (`pass(s, _) gives s` — `WorkerRecord.spec`, yields the String). ⚠ **NEEDS A DESIGN RULING** — the bootstrap borrows-and-retains-on-store where the self-hosted tier consumes, so the oracle's answer does not transfer ⭐ **ITS BLOCKER CLOSED — the ⛔ was STALE.** D1 landed 2026-07-29 (`f4c1c43e5`), so a method CAN be declared and D1b is now reachable; the row is moved to ⬜ FREE. **SURVEYED 2026-07-30 on a freshly built binary** (arm64-macos host, baseline **2456/0**, exit 0) — every claim below is a RUN, not a read. **(a) BOTH SUB-CASES CONFIRMED REFUSED**, each at `Parser.maxon:17345` (`moveOutManagedPayload`, the single `if not scrutOwned` gate): the read-only receiver (`displayReason`) and the returning one (`spec`), verbatim in the shapes the row names. **(b) The CONTROL isolates the gate exactly** — the identical bind out of an OWNED LOCAL runs and returns **42**, so nothing else on the path is implicated. **(c) ⭐⭐ THE ORACLE ACCEPTS BOTH AND RETURNS 42** — measured, not read ⇒ **the row's "the oracle's answer does not transfer" is true of the MECHANISM and FALSE of the VERDICT.** Both programs are legal Maxon and the bootstrap runs them; the open question is only HOW shv2 makes them legal, never WHETHER. That narrows the ruling from a language question to an ownership-strategy one. **(d) ⭐ THE MECHANISM ALREADY EXISTS IN-TREE, TWICE, AND ONE OF THEM WAS ITSELF A USER RULING**: `retainBorrowedAggregate` (#40's retain-promotion, P1.2, *"user ruling: approach A"*) and **`retainThrownField` (`Parser.maxon:11408`)**, the throw-channel twin whose own comment states this row's whole argument — *retain rather than move, do NOT null the slot, the caller keeps its reference, the refcount balances at exactly one free*. ⇒ **D1b is the THIRD application of an established thesis, not a new one**, and `moveOutManagedPayload` (`:17343`) already has the symmetric shape a retain twin would take. 🚩 **ESCALATED to the user rather than guessed** — the choice prices a refcount op onto every managed match-bind through a receiver in a compiler that must self-host, which is not a coordinator's call to make silently. **Options as surveyed:** **(A) RETAIN on bind** — closes both sub-cases uniformly, no escape analysis, one `incref` per managed bind out of a borrowed union even when the binding never escapes; **(B) BORROW read-only + refuse escape** — free for the 4 read-only harness consumers but needs escape analysis shv2 does not have, and **still refuses `WorkerRecord.spec`, so it does not close the row**; **(C) borrow, promote to retain on escape** — best runtime cost, most machinery, needs (B)'s analysis anyway. **Surveyor's recommendation: (A)**, on precedent (d) and because (B) provably cannot close the row. ~~⚠ **NOT CLAIMED while the ruling is open**~~ ✅✅ **THE RULING LANDED 2026-07-30: APPROACH (A) — RETAIN ON BIND.** A managed payload bound out of a BORROWED union is `incref`'d and the binding owns its own reference, dropping at its own scope exit; **the box slot is NOT nulled**, so the container's owner keeps its reference and the refcount balances at exactly one free. Closes BOTH sub-cases with no escape analysis. ⇒ **the third application of retain-promotion**, after `retainBorrowedAggregate` (#40) and `retainThrownField` (`:11408`) — and (B) was ruled out on the measurement above rather than on taste: it cannot close `WorkerRecord.spec` at all ✅✅ **CLOSED 2026-07-30** (main `e6b4680f1`; arm64-macos host **2456 → 2500/0**, wasm32-wasi PASS 2497/0, x64-linux SKIP — no WSL on this host, arm64 remote lanes SKIP — UNVERIFIED). **44 fragment ADDITIONS, ZERO modifications** — byte-identical codegen for every pre-existing program, the expected result for a rung that only un-refuses a construct. `retainBorrowedPayload` is 2 lines beside `moveOutManagedPayload`, and `declarePayloadBindings` is the ONE place that picks between them; the shared half is `ownPayloadBinding`. ⭐ **THE `movedManaged` ASYMMETRY IS THE RUNG**: the retained binding IS enrolled in `ownedBindings` (it owns a real +1) but does NOT set `movedManaged` — nothing left the borrowed box, and marking it consumed would make the caller's later re-read a false E3102. ⚠ **THE LADDER CANNOT SEE THIS RUNG AND THE LOG ROW SAYS SO**: the corpus emits no `match`, so `declarePayloadBindings` is never reached at any rung — the same-path A/B (revert `Parser.maxon` to main, rebuild, ladder; restore, rebuild, ladder) is BYTE-IDENTICAL at all six rungs, and the cross-row delta column is a **cross-PLATFORM artefact** (previous row Windows, this host arm64-macOS) proven not-a-cost by the fact that the PRE-RUNG compiler prints the identical delta. No step-6b trigger (x1.79…x1.98). ⛔⛔ **TWO REACHABLE SIGSEGVs, BOTH FOUND AFTER A GREEN SUITE, AND THE SECOND ONE BY THE INDEPENDENT REVIEW — the phase's unbroken pattern held again.** (1) The implementer's probe hunt, over a green 2469: **`and fallthrough` reaches the NEXT arm's BODY, where that arm's payload bindings sit**, so they destructure a case the value provably does not hold — **exit 139** when the previous arm nulled the slot, a silent **type confusion** on the borrowed path (an `int` binding reading a String pointer), and a silently **WRONG NUMBER** for scalar payloads. ⭐ **A DELIBERATE DIVERGENCE, AND THE ORACLE IS THE ONE THAT IS WRONG**: neither reference refuses it and **the bootstrap returns 18** — verified by the coordinator directly — where `b`'s payload does not exist at all. Refused now; falling OUT of a binding arm stays legal and is pinned by its own positive case, plus blast-radius guards for a bare payload-carrying target and an or-pattern target. (2) The review, over a green 2489: **the scrutinee's binding was marked `partiallyMoved` only AFTER the whole arm loop**, so a re-read *inside the arm that did the moving* parsed against a LIVE binding and dereferenced the null it had just stored — **exit 139**. ⭐ **D1b's OWN WIDENING is what made it reachable without nesting** (`text(s) gives grab(m)` needs the borrowed bind that was E2015 until this rung), which is why it was fixed here and not filed. The mark is now per-path and lands at the arm that moved; **`beginFreshArm` collapses the two spellings of the per-arm rewind into ONE door** (they had drifted apart at the `default` arm and the pattern arm), and `matchMovedManaged` became `scrutConsumed` because a nested `match m` in an arm body consumes the same binding — strictly wider than "an arm of THIS match moved a payload out". The reviewer found that hole **in its own first fix, by probing it**. ⚠ Verified NOT over-broad by the committed control: a sibling arm stays live and the program runs (exit 0) — order-dependence is the point. **Acceptance: `specs/union-interface-payload.md` ported byte-identical** (`struct-payload-dispatch` enabled — a STRUCT payload bound out of a borrowed param; `error.dispatch-interface-payload` stays disabled on interface types, NOT on D1b) **+ 23 authored cases**, the corpus having no receiver spelling at all — only the two "never bound" workarounds this rung's twins now sit beside. ⚠ `specs/refcount-accessor-borrow-order.md` is the natural headline acceptance and is **NOT ported**: it needs nested payload-bearing union payloads, a different mechanism (see Future rungs) | ~~L-parser-postfix?~~ **resolved: the match-bind path (`Parser.maxon`'s `declarePayloadBindings`/`parseMatchCommon`), touching neither D1's decl walk nor D2's postfix walk** | ✅ DONE | `slice/D1b-borrowed-payload-retain` | 2026-07-30T05:58Z |
| **D2** | **The postfix / member walk**, all four symptoms as ONE slice: a method call on a struct FIELD · a field access on a non-struct base · a method call on a **literal** (`"a,b".split(",")`) · a static method on a primitive (`int.fromString`). ✅✅ **CLOSED 2026-07-29** (main `082797037`; x64-windows 2429→**2508/0**, x64-linux PASS, wasm PASS, arm64 SKIP — remote, UNVERIFIED; fragment ADDITIONS only on both live lanes). ⚠⚠ **THIS ROW WAS WRONG IN THREE WAYS AND ALL THREE WERE MEASURED BEFORE PLANNING.** (1) **Its three code citations — `:10877`, `:23893`, `:24523` — are ALL PROSE LINES in unrelated comments**; the real sites are `parsePostfix:18186`, `parsePrimary:19049`, `resolveFieldChainFrom:24198`, `dispatchMethodOnBinding:21121`. (2) **"a field access on a non-struct base" is NOT D2's** — `n.width` on a genuine `int` is a *correct* refusal with a fine message; Directory only produces it because `ByteArray` resolves to `int`, which is **D4**, exactly as D4's own row says. The board contradicted itself; the probe settled it. (3) **A FIFTH symptom went unnamed and is the commonest of all** — a method on a CALL RESULT (`Inner.make(9).get()`), which failed identically to the literal case. ⇒ the four "symptoms" were **two classes**: the base not being a legal postfix base at all (literal · call result · paren · primitive keyword), and the field chain never checking for a trailing `(`. ⭐⭐ **THE FIX WAS DE-DUPLICATION, NOT A FIFTH SPECIAL CASE**: `MethodReceiver` + `dispatchMethodOnReceiver`, ONE tag switch reached from both `parsePostfix` and `dispatchMethodOnBinding` — **nine dispatchers deleted, five of them binding/value PAIRS of one dispatcher**, which is precisely what `IrInterface.maxon:570-573` warns about; `resolveFieldChainFrom` now stops before a `.member(`, replacing two special-case arms. D1's enum arm, landed hours earlier, **gained a value receiver for free** by riding the same switch. ⛔ **THE REVIEW FOUND A REACHABLE USE-AFTER-FREE THE GREEN 2501 COULD NOT SEE**: a managed field read off a TEMPORARY borrows a box freed at the semicolon — `emitFieldLoad`'s invariant *"the read is a BORROW … the box drops at ITS scope exit"* holds only because a box is reached through a NAME, and this rung was the first door able to hand it one that isn't. Four managed field kinds, all refused on `main` and all compiling clean on the branch; three segfaulted and **the struct case silently returned the freed-memory fill byte `0x3F3F3F3F` as the program's answer** — a wrong answer no suite of exit codes can catch. Fixed by refusing (`requireFieldOutlivesItsRead`), with the by-NAME control committed beside it so the guard cannot "fix" the crash by deleting the feature. Also fixed in-rung: `declaredSlotType` re-tagged function/generic/float aliases but **not a declared `type`**, so a struct field's VALUE read as an integer (`return self.leaf` false-rejected as *"Cannot return 'int'"*); and an unknown member with no `(` reported the punctuation at one spelling and the name at the other. ⚠ **`int.fromString` is NOT delivered — scoped out UP FRONT and measured, see the new row below** | L-parser-postfix | ✅ DONE | `slice/D2-postfix-member-walk` | 2026-07-29T22:52Z |
| **D4** | ⚠ **THE ROW'S PREMISE IS MEASURED FALSE — REFRAMED, SAME LANE, SAME FIX SITE.** `ByteArray`/`Byte` are NOT broken as declared types: a `typealias Byte = int(0 to u8.max)` + `typealias ByteArray = Array with Byte` held in a struct field, pushed and read back through a method, **runs and returns 42** (measured 2026-07-30, unfiltered `run_program`). And "the 4 binary-image readers" names nothing — the phrase appears nowhere else in this document. **What is real is one layer up and worse:** `Directory.maxon:17`'s `a field access on 'path', which is declared 'int'` is `FilePath` being an **UNDECLARED** name (it is not one of the 3 whitelist entries), and shv2 **silently resolves a type name that denotes NOTHING to an integer**. `declaredSlotType` (`SignatureIndex.maxon:5560-5615`) re-tags a `named` for four registries that DO know it — function alias, generic alias, float alias, struct — then falls through to `adoptType`, and its own header states the consequence: *"`named` reads as an INTEGER (`tagIsIntegral`)"*. There is **no arm for a name no registry declares**, so E3011 — which the architecture already owns for exactly this fact — is preempted by a downstream parse-time E2015 that blames the wrong thing with a claim that is untrue (`h` is not declared `int`). This is the project's own **"no silent unhandled cases"** rule broken in the one door a slot's declared type comes out of. ⚠ The risk is FALSE REJECTS one level below the test: the fallthrough legitimately serves primitives, ranged int aliases, enums, unions, interfaces, type parameters and the six `isCompilerOwnedTypeName` names ✅✅ **CLOSED 2026-07-30** (main `67bef9cdc`; x64-windows 2537→**2541/0**, x64-linux PASS, wasm PASS, arm64 SKIP — remote, UNVERIFIED). **Fragment ADDITIONS only on both live lanes, ZERO modifications** — byte-identical codegen for every pre-existing program, which is the expected result for a diagnostics-only rung. ⭐⭐ **THE PRODUCER WAS NOT THE ONE THE PLAN NAMED, AND THE IMPLEMENTER STOPPED TO SAY SO RATHER THAN REDESIGNING AROUND IT.** The brief blamed `Parser.fieldStorageType`'s recovery `integer`; that function is never on this path. The real producer is **`TypeRules.typeTagName`, whose `named` arm deliberately prints `int`** — correct for the ranged INT alias it was written for, and a **false statement** the moment the name denotes nothing. Every rule reading `maxonTypeTag` on a `named` inherits it. ⭐⭐ **THREE DOORS, NOT TWO** — the brief named the two field-access refusals (`requireStructBase`, which also serves the WRITE path via the same arm, and `capturedStructBase`); probing found a third, **`structBaseOfReceiver:21676`**, asserting the same invented type in a third wording for a method call on a value. Fixing two would have left a third of the defect reachable. ⭐ **THE FIX IS THE VERDICT, NOT A BETTER SENTENCE.** The first cut kept each door's framing and merely swapped the invented type for the real name — leaving ONE FACT with a second wording under E2015, beside the E3011 the authority already gives it. Final shape: one predicate (`undeclaredBaseTypeNameOf`) asking the ONE authority (`TypeResolution.denotedNamedType`, the same cascade `denotedCastTargetTag` and `checkGenericArgType` ask), raising **E3011** — and the three original messages restored **byte-identical**, so the second commit is a SMALLER diff than the first. The `ParseError` arm formerly named `unknownCastTargetType` is renamed **`unknownTypeName`**: its payload and positioned rendering were never cast-specific, the cast was merely its only raiser. **No registry change and no new code** — E3011 already existed with a live shv2 claim. ⭐ **Why a parse-time door raises a code the authority also raises, and it is measured, not stylistic:** a thrown `ParseError` makes `abortedParseArtifact` discard the file's diagnostics, so `resolveTypes` never gets to speak — the identical bind `missingBaseLayout` already records (filed as its own rung). ⛔ **THE INDEPENDENT REVIEW FOUND THE RUNG COMMITTING ITS OWN SIN, AND IT WAS MERGE-BLOCKING**: `isCompilerOwnedTypeName` reserves **`CharacterSet`**, but `denotedNamedType` answers for only FIVE of the six — the layout is registered `__CharacterSet` while the user-facing name is `CharacterSet` — so the new verdict said *"Unknown type 'CharacterSet'"* about a name the compiler reserves and that `CharacterSet.letters()` uses successfully two lines away. **A worse falsehood than the one being removed.** Fixed by gating the predicate on `isCompilerOwnedTypeName` — the authority is untouched, so `CharacterSet` does NOT become a resolvable declaration type — and it cannot swallow a genuine typo *structurally* rather than luckily, because `requireTypeNameNotCompilerOwned` already refuses any user declaration binding one of the six, making "reserved" and "the program's own missing type" disjoint by construction. ⭐ **TWO GUARDS NOW, EACH WITH EXACTLY ONE WITNESS IN 2541, both verified by SABOTAGE rather than claimed**: delete the reserved-name gate ⇒ only the new pin reddens; widen the authority ask to every `named` ⇒ only `self-field-struct-typed/error.scalar-field-base-is-not-a-struct` reddens (a base declared with a ranged alias, whose tag IS `named` — the corpus's entire coverage of that branch). ⭐ Duplication collapsed to one `requireDeclaredBaseTypeName`, removing the empty-`ByteArray` sentinel — and the file's own MEASURED rule against `throw self.helperReturningAParseError(…)` (two leaked allocations) was **settled by measurement, not assumed away**: a helper that throws internally is a different shape, all seven programs reaching the guard exit 1 and never 101, and the implementer verified that gate was not VACUOUS (`mm_leak_check` sits on the single unconditional exit path, so two leaked allocations would have returned 101 instead of 1). **scale-test NEUTRAL for a structural reason** — the query is only ever reached inside a branch that has already decided to refuse, so it costs per REFUSED access and never per field read; same-path A/B byte-identical at all six rungs; no step-6b trigger. **THREE findings homed rather than fixed here, each with why it could not ride along** — see "Future rungs" (`typeTagName`'s float-alias falsehood · the `CharacterSet`-typed-PARAMETER false reject, whose closure should retire this rung's reserved-name gate · `abortedParseArtifact` dropping diagnostics). ⚠ Also corrected: the brief's claim that an `interface`-typed slot is legal — it is already E3011 today | L-types | ✅ DONE | `slice/D4-undeclared-type-slot` | 2026-07-30T01:20Z |
| **D5** | **`#if` conditional compilation.** ✅ The LEXER half already exists — `Lexer.maxon` emits `hashIf`/`hashElse`/`hashEndif` (`:257`, `:1364`) and `scanDirective` (`:1096`) reads them. ⚠ **The rest is not small**: condition EVALUATION (`os()`/`arch()`/`testing()` with `and`/`or`/`not`) plus `#else` plus **NESTING** — `Target.detectHostTarget()` is itself a nested `#if`/`#else`, so a flat implementation does not compile the one function this row exists to unblock. ⚠⚠ **THE LANE TABLE IS WRONG FOR THIS ROW — MEASURED 2026-07-29.** It assigned D5 to L-parser-stmt on the assumption that `#if` is a statement-dispatch concern. It is not: the directive must be wired into **FIVE** body loops, and one of them is `parseEnumDeclaration`'s member loop (`:7946-7966`) — **D1's exact site**. ⇒ **D5 and D1 OVERLAP in `Parser.maxon`**; whoever lands second must read the other's diff before re-gating, not merely rebase cleanly. (The other four: `parseModule`/`dispatchTopLevel` `:4263`/`:6567`, `parseStatements` `:9227`, `parseTypeDeclaration`'s member loop `:7698`, `parseInterfaceDeclaration` `:7743`. shv2 parses no `extension` at all, so v1's seventh site has no counterpart and is not a residual.) ⚠ Also measured: the **oracle REFUSES `#if` in enum/union bodies** (`E2010`) while the spec doc and v1 both allow it — shv2 follows the doc, and the bootstrap gap is filed. ✅✅ **CLOSED 2026-07-29** (main `07d7b5646`; x64-windows 2401→**2429/0**, x64-linux PASS, wasm PASS, arm64 SKIP — remote, UNVERIFIED). **28 fragment ADDITIONS, ZERO modifications on any lane** — byte-identical codegen for every pre-existing program. ⭐⭐ **THE DESIGN IS NOT THE ONE THIS ROW ASSUMED, AND THE LANE-OVERLAP WARNING ABOVE IS THEREFORE MOOT: `Parser.maxon` WAS NEVER TOUCHED.** The implementer stopped before writing a line and was right to — wiring directives into the five parse loops leaves shv2's **token-level declaration sweep** (`foldDeclaredSignaturesInto`, a raw token walk v1 and the bootstrap both lack) seeing BOTH branches. Measured consequence on the very file this row exists to unblock: `stdlib/FilePath.maxon:3-11` declares `pathSeparator` in both arms, the sweep records line 4 first-wins, the parse dispatches line 8 on non-Windows ⇒ **`E3006 duplicate definition` on 3 of 4 targets**, plus a silent wrong struct layout for a `#if`-gated field pair. ⇒ **USER RULING: conditional compilation lives in the TOKEN TIER** — a new `queryActiveTokens` layered over `queryTokens`, so the sweep and the parse read ONE filtered array and disagreement is *structurally impossible* rather than a thing to verify at N call sites. Both failure modes are now pinned by committed specs. **Three defects found after the design was settled, each by a different gate**: sabotage found an `#else` in a dead outer branch held dead by TWO mechanisms with no test for either (every nested case in the corpus has a LIVE outer branch); the reviewer found **trailing text on a `#if` line emitted as CODE** — `#if os(Windows) n = 5` assigned 5, and whether the file compiled depended on the TARGET, so the two compilers built *different programs* (now E2065); and the optimizer found the query engine probing its `FilePath` map TWICE per memo for one fact (~10 allocations a probe on Windows), a fix mostly PRE-DATING D5 that leaves rung 5 **388 allocations BELOW the pre-rung baseline**. Codes: E2063 unbalanced, E2064 unknown predicate, E2065 malformed condition | ~~L-parser-stmt ∩ L-parser-decl~~ **no lane — `Parser.maxon` untouched** | ✅ DONE | `slice/D5-conditional-compilation` | 2026-07-29T19:47Z |
| **D6** | **INTERFACE ASSOCIATED TYPES — and it is what actually blocks `int.fromString`.** Opened by D2, which stopped rather than build a parser arm dispatching to nothing. **Measured, not predicted:** `__int_fromString` / `__float_fromString` / `__bool_fromString` **DO exist** (`stdlib/Builtins.maxon:214/252/311`), but `maxon-shv2 build stdlib/Builtins.maxon` gives `E2010 :56:2 Expected 'function' but got 'typealias'` — an associated type inside an interface (`typealias ElementArray = Array with Element` in `interface InitableFromArrayLiteral uses Element`), which shv2 has no parser for. ⚠ **A second, INDEPENDENT blocker sits behind it**: listing the module would reserve `ParseError` program-wide and `E3006` the ported spec's own `enum ParseError` — it would break the very file it is meant to unblock. Both references route primitive statics through the stdlib free function, so there is no parser-only route. **Unblocks 14 disabled cases in `specs-shv2/parsable-interface.md`** (ported byte-identical by D2) plus D2's unfinished 4th symptom ✅ **PREMISE RE-VERIFIED 2026-07-30T18:25Z before claiming** — `maxon-shv2 build stdlib/Builtins.maxon` still gives `E2010 :56:2 Expected 'function' but got 'typealias'`, and the declaration at that site is exactly `typealias ElementArray = Array with Element` inside `interface InitableFromArrayLiteral uses Element`. Not stale, unlike three other rows measured this session ⛔ **BLOCKED ON A1 (fallible division) — user ruling 2026-07-30.** Two of its three blockers are cleared and landing as a PARTIAL; the third needs a rung of its own. ◑ **PARTIAL, LANDED 2026-07-30** (main `c290acdfb`; x64-windows **2737/0**, x64-linux PASS, wasm PASS, arm64 SKIP). **TWO of three blockers cleared; it flips NONE of its 14 target markers, so it is ◑ and not ✅.** (1) `typealias` inside an `interface` body now parses, with the interface's `uses` names installed as `enclosingTypeParams`. (2) `Builtins.maxon` may declare `__`-prefixed names, via a per-file `declaresCompilerInternalNames` bit — deliberately NOT `isStdlibSource`, which decides *visibility* and would have made `maxon-shv2 build stdlib/Builtins.maxon` — the command `StdlibLoader`'s own collision rule prescribes — refuse the very module the loader is about to list. ⛔ **(3) is A1** and the rung stops there. ⭐⭐ **THE ROW'S TITLE IS RIGHT ABOUT THE MECHANISM AND WRONG ABOUT THIS FILE.** `Builtins.maxon` needs no associated-type *substitution*: `interface Parsable` has none, and static interface methods skip signature verification in both references. **But `specs/associated-types.md` exists — 11 canonical cases, never ported — and it IS the titled mechanism's acceptance.** Ported here byte-identical: **2 green, 9 disabled**, and every one of the 9 was measured individually rather than labelled in bulk ⇒ **R5** below. ⭐⭐ **A COORDINATOR CLAIM WAS MEASURED FALSE AND MUST NOT BE RE-RECORDED**: I told the implementer that shv2 *cannot* skip the alias the way both references do, because the requirement would then name an undeclared type. **It can** — an interface requirement's declared types are never RESOLVED, only rendered, so `static function init(value Nonesuch) returns Self` naming a type no file declares **compiles and runs clean (exit 7)**. The real justification is the inverse and stronger: **a skip works only by leaning on that hole**, matching a conformer purely because a string is spelled the same in two files (`stdlib/List.maxon:13`, `Set.maxon:14`). Recording the alias buys it a meaning — `typealias A = Array with Nonesuch` in an interface is now **E3011** where a skip reports nothing — and that check is what forces the `uses` scope. ⚠ **New finding, pre-existing, narrowed here but not closed: an interface requirement may name a type nothing declares, SILENTLY.** R5 owns it, because substitution must resolve exactly those types. ⚠ Also: the rung introduced a per-file cost and caught it itself (`currentPath` + two `resolve`s + an interpolated `join` + a case-folding compare, **twice per file** — measured **+1,440 → +8,260**, ratios 1.15→1.74, linear in file count); a basename prefilter restored the ladder to identical-to-the-digit at all six rungs. **Second time this session a "spell it once" refactor was not free on a hot path** | L-parser-decl | ▣ **BATCH2** *(◑ residual)* | *(landed; resumes after A1)* | 2026-07-30T18:25Z |
| **R5** | **ASSOCIATED-TYPE SUBSTITUTION AT CONFORMANCE — the mechanism D6 is named for, with its canonical spec already in the tree.** `implements Container with Score` must bind `Element := Score` **before** `checkConformance` compares; today it does not, so the comparison prints the associated type's own name (*"expected `unwrap() returns Inner`"*). ⭐ **`associatedTypeNames` has no reader at all — that reader IS this rung.** Acceptance: the **9 `disabled-test:` cases in `specs-shv2/associated-types.md`**, ported byte-identical by D6, each carrying its OWN measured reason rather than a bulk label. ⚠ **One of the 9 is NOT this gap and its note says so in order**: `character-element-type` dies earlier on `Character.codepoints` (E2015) and needs substitution only afterwards. ⚠⚠ **And one carries a SECOND, INDEPENDENT divergence found only because each reason was verified individually** — `multiple-associated-types` is the only case whose `with` clause carries two arguments **unparenthesized**, and `skipOptionalWithClause`'s unparenthesized arm consumes exactly ONE type reference, so `parseTypeImplementsClause`'s comma loop reads `, Float` as a second **interface** and adds a spurious `E3015: implements unknown interface 'Float'`. **The oracle accepts that program (build 0, run 42 — measured.)** How many arguments that clause takes is the interface's `uses` **ARITY** — precisely the fact binding needs — so it is this rung's, not a separate parser fix. ⚠ Also inherits D6's finding that **an interface requirement may name a type nothing declares silently**; substitution must resolve those types, which closes it ⚠ **LANE, stated per the corrected rule above**: its CODE is the declaration-site clause parse (`skipOptionalWithClause`, `parseTypeImplementsClause`) plus conformance comparison, which lives in its own `ConformanceCheck.maxon` that no lane names. It also READS `associatedTypeNames` from `TypeResolution.maxon`/`SignatureIndex.maxon` — **`L-types`, live to T1 as this was claimed** — so if it turns out to need an EDIT there rather than a read, the second lander re-gates, exactly as the three `Parser.maxon` lanes already do | L-parser-decl *(+ reads L-types)* ✅✅ **CLOSED 2026-07-30** (main `6068efe97`; merged tree **2808/0**, x64-linux PASS, wasm PASS, arm64 SKIP — remote, UNVERIFIED; `associated-types` 16/16). **8 of 9 flipped**; `character-element-type` stays disabled on its unrelated `Character.codepoints` blocker, note untouched. ⭐⭐ **THE DESIGN IS "SUBSTITUTE ONCE", TAKEN FROM v1 OVER THE BOOTSTRAP, AND THE REASON IS A TEST NOTHING COULD HAVE WRITTEN.** The bootstrap substitutes at TWO sites off one dict — `ResolveInterfaceTypeName` for the comparison, `FormatResolved` for the message; v1 substitutes once and both consumers read the result. shv2 had the bootstrap's shape. Four of the eight cases pin the *expected* side of a diagnostic, so a substitution reaching the verdict and missing the sentence is **a correct answer with a wrong explanation, and no positive test can see it**. Done at `collectInterfaceMethods`, so verdict and sentence read one string. ⚠ **ONE DELIBERATE ASYMMETRY, and my own instruction would have broken it**: `Self` is substituted for the COMPARISON only, because `specs-shv2/comparable.md:411,422` pins `compare(other Self)` *unsubstituted* in E3016 text across 4 target lanes. The implementer grepped the pinned messages BEFORE writing code; *"substitute once"* read literally reddens two green tests. ⭐⭐ **THE REVIEW'S BLOCKER WAS A FALSE ACCEPT, AND ITS DUPLICATION FINDING *WAS* THE BUG.** `renderImplTypeName` was a fork of `renderDeclaredTypeName` — and both copies shared a blind spot: `maxonTypeName` names a KIND for two tags, this rung fixed `typeParameter` and left `genericInstance` → `struct`, so `Array with Integer` and `Array with String` render alike. shv2 **compiled and ran** `take(xs Array with String)` against an interface requiring `Array with Integer` (**exit 42**) where the bootstrap reports E3016. ⇒ **A SHARED BLIND SPOT ON A COMPARISON READS AS AN ACCEPTANCE, NOT A DISAGREEMENT** — two renderers agreeing in being wrong produce a false accept no positive test sees. One exported `renderDeclaredTypeName` now; both sides call it, and comparison routes through `canonicalInstanceName`, the compiler's own "same type?" that every other comparison site already asked. ⭐ **The rung also fixed a wrong REJECTION on how every generic container in `stdlib/` declares conformance** (`implements Holder with T` — `Array.maxon:490`, `List.maxon:158`, `Map.maxon:343`, `Set.maxon:356`). ⚠ **Both the rung and the review introduced a cost and caught it themselves** — the review's first shape bundled the renderer's four inputs into a record: **+32/+48/+80/+144/+272/+528**, a heap box per rendered type; four parameters instead, ladder byte-identical. **Third time this session a "spell it once" refactor was not free on a hot path.** Rung cost: `4·2^r + 4`, one 16-byte record per declared conformance, same-path A/B. ⚠ Surplus `with` argument is IGNORED (bootstrap) not rejected (v1) — and the review **corrected the justification**: the claimed "shape v1 supports" (`Sub extends` inheriting `uses`) is measured REJECTED by shv2 and the bootstrap alike, so the bullet is forward-compatibility, not live acceptance | ✅ DONE | `slice/R5-associated-type-substitution` | 2026-07-30T19:23Z |
| **R6** | **A SURPLUS ARGUMENT INSIDE PARENTHESES IS SILENTLY DROPPED — ⚖ RULED, and the asymmetry is the point.** `implements One with (Integer, Float)` against `interface One uses A` binds `A := Integer`, **drops `Float`, and compiles (exit 42)**. A typo nothing reports. ⚖ **COORDINATOR RULING 2026-07-30: REJECT a surplus inside PARENTHESES; keep IGNORING it unparenthesized.** The two spellings are not the same claim — **parentheses make the list explicit, so its LENGTH is something the author asserted and can be wrong about; without them the length is decided by the interface's `uses` ARITY and a trailing comma legitimately belongs to the outer `implements` list**, which is why both references consume exactly `arity` items there and why R5 committed a test for the unparenthesized shape. ⇒ rejecting the parenthesized surplus diverges from nothing R5 pinned. ⚠ **Deliberately NOT squeezed into R5**: it is a NEW REFUSAL, and a new rejection rule hides its false rejects one nesting level below where you test it — it needs its own sweep (`with (A)` ≡ `with A`, pinned at `specs-shv2/interfaces.md:552`; `stdlib/`'s real 2-arity uses at `Range.maxon:52`, `views.maxon:31/83/147`, `withIterator.maxon:5`), not a post-gate patch. ⚠ shv2's list reading of `with (…)` is SETTLED and is not reopened here — the bootstrap reads it as a TUPLE at 1-arity, and shv2's reading is the one `stdlib/` is written against | L-parser-decl ✅✅ **CLOSED 2026-07-31** (main; merged tree **2892/0** with R10, x64-linux PASS, wasm PASS, arm64 **SKIP — remote, UNVERIFIED**; fragment ADDITIONS only, zero goldens modified). The ruling is implemented as written: a surplus inside PARENTHESES is **E2066**, unparenthesized stays ignored, and `surplus-conformance-argument-is-ignored` is byte-identical. ⭐ **THE CHECK IS AT PARSE TIME BECAUSE THE ASYMMETRY DOES NOT SURVIVE THE PARSE** — `InterfaceConformance` carries `interfaceName` + a bare `ByteArrayArray`, so one line later the two spellings are ONE value with no token to anchor a diagnostic on. Recorded through `recordArtifactDiagnostic` (the door `checkGenericInstance` already uses) rather than thrown, so it does not discard the file's other diagnostics. ⭐ **ONE decider**: a new `union ConformanceUsesArity { declared(count) \| unresolvable }` read by BOTH the unparenthesized arity path and the refusal — a union rather than a count-with-a-default precisely because **the two readers need opposite answers to the miss**, which is the row's own warned-about false reject carried in the type instead of a constant. ⭐⭐ **THE FALSE-REJECT SWEEP IS THE RUNG** — the row warned that a new rejection rule hides its false rejects one nesting level below where you test it, so 9 shapes were measured GREEN and pinned: `with (A)` at arity 1, `with A` at arity 1, a generic conformer's own type parameter `implements Holder with (T)` on `type Box uses T`, a mixed list `implements One with (Integer), Two with Float`, a `where`-constrained conformer, the `stdlib/Map.maxon` shape (`uses Key, Value implements Marker, Pairing with (Key, Value) where Key is Stringable`), an interface declared BELOW its conformance, an interface in ANOTHER file, and — the one that matters most — **`with ((Integer, Float))`, the DOUBLE-PAREN tuple spelling, which compiles**. That last one is what makes E2066 a TYPO CHECK rather than a capability removal, and nothing in the suite would have noticed a future reader flattening the parenthesized arm or counting commas instead of items. ⭐ **The unresolvable carve-out fails OPEN**: a typo'd interface name gives E3015 alone with no arity complaint, and an interface the tolerant sweep could not read also answers `unresolvable` — so the refusal can only ever under-fire, never into a false reject. ⭐ **NEW CODE E2066 rather than widening E2056**, argued and correct: E2056 documents a generic INSTANTIATION whose arity rule is SYMMETRIC, whereas here too FEW is a different diagnostic entirely (E3016, named rather than counted, raised whole-program) and too many is refused in only ONE of two spellings — one code over two rules agreeing on neither symmetry nor spelling would document neither. Registry-generated, `error-codes check` OK at 156 codes. ⚠ **DELIBERATE DEVIATION, verified against the reference rather than asserted**: `conformanceUsesArity` counts the interface's OWN `uses` names and NOT `extends`-inherited ones, because `associatedTypeNames.count()` rides the signature-index hash that keys every parse memo while `extendsInterfaces` deliberately does not — walking the chain would make a parse depend on an unhashed fact, in another agent's lane. Consequence is message-only and bounded (`interface Sub extends Base` + `implements Sub with (Integer)` moves E3016 → E2066), and **the bootstrap refuses the same parenthesized shape too** (`E2003`, its tuple reading) and gives the unparenthesized control shv2's identical E3016 sentence character for character — so it is not a false reject. Pinned as a pair so the day inherited bindings land, it turns red. ⭐⭐ **AND THE RUNG FOUND MY BRIEF'S PREMISE WRONG**: I named five `stdlib/` sites as the real 2-arity surface to protect, but `stdlib/helpers/itertools/withIterator.maxon:5` is `implements Iterator with (Source, Element)` against `interface Iterator uses Element` — **arity ONE**. The file means the bootstrap's 1-arity TUPLE reading; under shv2's settled LIST reading it is a 2-vs-1 surplus that WOULD be E2066. It is invisible today for two independent reasons, both verified: shv2's loader whitelist is exactly three modules (`Clock`, `Sleep`, `helpers/string/utf16`) and **none contains the token `implements` at all**, and the file already dies earlier at `E2015` on a tuple-of-type-parameter. **R6 breaks nothing today and the escape hatch already exists** (`with ((Source, Element))`), but whoever widens the whitelist must rewrite that line or give `with (…)` a tuple spelling. ⚠ Two sabotages, both fired: inverting the comparison reddens the two refusal cases; making the UNPARENTHESIZED arm reject too reddens `surplus-conformance-argument-is-ignored` — so the ruling's asymmetry is pinned, not merely implemented. ⚠ Accepted, not fixed: redeclaring a synthesized protocol name with a `uses` clause (`interface Cloneable uses Element`) now gets E2066 saying the interface declares 0 associated types — TRUE (builtins win by the pre-existing no-shadow rule, so the user's declaration was already dead) but confusingly worded; its own rung is "redeclaring a synthesized protocol name is its own diagnostic", not a carve-out here. ⚠ `scale-test` is structurally BLIND to this rung — the corpus emits `implements ScaleProto0` with no `with` clause and zero `uses` clauses, so `parseConformanceWithArgs` runs ZERO times on the ladder; no row written because there is no signal to record | ✅ DONE | `slice/R6-parenthesized-surplus-with-arg` | 2026-07-31T03:28Z |
| **R7** | **DECLARATION-ORDER FALSE REJECT — an interface used ABOVE where it is declared, in the SAME file.** `interface Duo uses P, Q` written *below* `type Both implements Duo with Integer, Float` ⇒ shv2 `E3016` + `E3015`; **the bootstrap compiles and runs it (exit 42)**. Measured by the R5 review. Cause: `artifact.interfaces` fills as the linear parse reaches each declaration, so what looks like a cross-file limit is really *"not declared above"*. ✅ **Loud, never silently wrong**, and bounded to the **unparenthesized** spelling — `implements Duo with (Integer, Float)` needs no arity and compiles. ⭐ It is the SAME limit `requireInterfaceForParse` already states for a `where` constraint's interface, so the fix is one mechanism for both. ⚠ **Why R5 did not fix it**: it needs whole-program interface visibility at parse time, i.e. a field on `ProgramSignatures` in `SignatureIndex.maxon` — **`L-types`, live to another agent at the time.** A lane boundary, not a size one ✅✅ **CLOSED 2026-07-30** (main `d4aa0b866`; merged tree **2812/0**, x64-linux PASS, wasm PASS, arm64 **SKIP — remote, UNVERIFIED**; fragment ADDITIONS only, **zero goldens modified on any lane** — byte-identical codegen on all 2808 pre-existing tests, which is the shape a parse-time-only fix should have). ⭐⭐ **THE ROW'S PREMISE WAS TWICE TOO BROAD, AND MEASURING IT IS WHAT MADE THE FIX SMALL.** It is NOT "an interface used above where it is declared": a forward reference with ONE associated type already worked (exit 42), and the parenthesized `with (P, Q)` already worked. The bug is exactly **an unparenthesized `with` whose ARITY is unknown** — `interfaceUsesArity` returned the unresolvable default of **1**, so `with Score, Weight` consumed only `Score` and the leftover `, Weight` was read as a SECOND INTERFACE, which is why the second diagnostic literally says *"implements unknown interface 'Weight'"*. **Cross-file is the same bug and is the commoner shape** (measured: oracle 42, shv2 E3016+E3015). ⭐⭐ **AND "THE FIX IS ONE MECHANISM FOR BOTH" IS MEASURED FALSE AT THE CODE** — `interfaceUsesArity` needs a COUNT; `requireInterfaceForParse` needs full METHOD SIGNATURES (`iface.methods`, `paramTypeNames`, `throwsClause` — `findWitnessDispatch`, `Parser.maxon:25545-25549`). Different information, different size ⇒ split to **R8**. ⭐ **TAKE THE BOOTSTRAP, LEAVE v1 — AND v1 HAS THIS SAME BUG.** The bootstrap fixes it with a raw token pre-pass capturing name + `uses` arity before any parsing (`PreRegisterTypeNames`/`ParseUsesClauseTokens`, `0-Compiler.cs:991-999`); v1's `preRegisterInterfaceNames` (`Queries.maxon:214-289`) records the NAME ONLY, so `lookupInterfaceUsesParamCount` defaults to 1 exactly as shv2 did (static reading — v1 does not build). **shv2 already HAD the bootstrap's pre-pass**: the whole-program declaration sweep visits every `interface` in every file including stdlib, and `recordScannedInterface` deliberately recorded nothing. It now records name + arity into `ProgramSignatures.interfaceArities`, and `interfaceUsesArity`'s linear `artifact.interfaces` scan is **DELETED** rather than kept as a fallback — the two sources cannot disagree (`associatedTypeNames` is the literal `uses` clause; `extends` names go elsewhere and are not inherited), so keeping both would be one fact written twice. Reads the header through the SAME `parseInterfaceHeaderClauses` the real parse uses, so sweep and parse cannot drift. Rides the signature hash as `DeclShapeInterfaceArity = 51` — it must, because an arity in file A changes how file B parses. ⭐⭐ **IT DELETED A QUADRATIC RATHER THAN ADDING A TABLE.** The old scan was **O(interfaces × conformance-sites)**: on a hand-built doubling ladder (N interfaces `uses P, Q`, N unparenthesized arity-2 conformances, min of 5) pre-R7 `phase:parse` CPU bends **×2.13 ×2.29 ×2.33 ×2.84** while post-R7 holds **×1.99 ×2.00 ×2.02 ×2.03** dead flat across a 16× span — **50.8% cheaper parse CPU at N=4096**, gap widening with N. Cost added: `+77/+95/+139/+219/+371/+667` allocations (**+0.0045%** at rung 5), a constant plus one `InterfaceArityEntryArray` per source file, measured exactly `Δallocs = 1×files + 14`, left consistent with its twelve unconditional siblings. ⚠⚠ **THE LADDER IS BLIND TO THIS RUNG AND THE BLIND SPOT IS THE *CORPUS*, NOT THE COLUMN**: the generated ladder declares interfaces (8→132) but **zero `uses` clauses and zero `implements … with …`**, so the WRITE side scales there while the READ door never executes once — the quadratic above is invisible in every column, CPU included, until you build a corpus that expresses the feature. ⭐ **ACCEPTANCE HAD TO BE AUTHORED, AND THE SURVEY IS WHY THAT IS DEFENSIBLE**: `/specs` contains **no case exercising this** — its one declaration-order interface case (`specs/interface-dispatch.md:737`, `interface-param-declared-after-consumer`) is an arity-**0** interface-typed PARAMETER, blocked on existentials, a different code path. So the four new cases are the measured probes committed as specs. ⭐ **The over-fix guard is the load-bearing one and it is sabotage-proven**: with `arity + 1`, `forward-interface-comma-still-opens-a-sibling-conformance` goes RED (`E2010 Expected 'function' but got 'with'`) — a forward-declared arity-1 interface's comma must still open a SIBLING conformance, not be eaten. It is correctly GREEN on both sides of the fix, because that is what a guard is. ⚠ **The review found the rung had ENCODED A FALSE GUARD in prose** — the arity-0 case's text claimed to pin the `declared(0)` vs `undeclared` distinction, and it provably cannot: `parseConformanceWithArgs` consults arity only once `args.count()` is already 1, so 0 and 1 are behaviourally identical through the only reader that exists. Verified by collapsing the union and watching the suite stay **2812/0**. The union STAYS (the alternative is a sentinel count or pushing a Parser constant into `SignatureIndex`; it is the 4th instance of the house `structOf` idiom), but the spec now says what it pins and the measurement is recorded at the union. **A sentence claiming a guard that does not exist is worse than no sentence** — a later agent deletes the union with nothing going red | L-types | ✅ DONE | `slice/R7-interface-declaration-order` | 2026-07-30T20:55Z |
| **R8** | **WHOLE-PROGRAM INTERFACE METHOD SIGNATURES AT PARSE TIME — the half R7 measured itself OUT of, and THREE independent findings converged on the same function.** `Parser.requireInterfaceForParse` (`Parser.maxon:25599`) still resolves a `where` constraint's interface by a LINEAR SCAN over THIS FILE's `artifact.interfaces`, declared-above-only. **(1) CORRECTNESS** — `type Box uses T where T is Digest` with `interface Digest` declared BELOW is `E2015 … not declared before its constrained use` (measured 2026-07-30). ⚠ **AND THERE IS NO ORACLE EVIDENCE FOR THIS HALF, WHICH IS ITSELF THE FINDING**: the bootstrap fails the CONTROL — interface declared ABOVE — identically (`E4006 Primitive type 'int' has no method named 'digest'`), so it rejects the program in EVERY declaration order and cannot say what the language should do here. **A control that fails identically to its experiment is not a control**, so R8 opens with a design question, not a known answer. **(2) THE IDENTICAL QUADRATIC R7 JUST DELETED**, against the identical array: **O(interfaces × witness-dispatch-sites)**, and it ALLOCATES NOTHING, so only the CPU column can see it. Measured against a same-file control (constraint interface at a fixed position): experiment **×2.07 ×2.23 ×2.37** per doubling vs control **×2.00 ×2.01 ×1.97**; at N=1024 the scan alone is 466M ticks, **31.5% of `phase:parse`**. ⏭ **Measured debt (Workstream O), NOT a live defect**: across the real corpus the scan length K is bounded and small — the maximum interfaces in any one file is **10** (`stdlib/Interfaces.maxon`; next is 9, everything else ≤1), and the files carrying `where … is` constraints declare 0–1 interfaces because their constraint interfaces (`Comparable`, `Equatable`, …) hit the `builtinInterface` arm and never reach the scan at all. **Re-measure trigger:** one file combining many USER-declared interfaces with `where T is <user interface>` constraints — a machine-generated interface set, or an `Interfaces.maxon`-style hub growing past a few dozen while gaining same-file constrained generics. **(3) DUPLICATION** — *"which interface does this name denote"* is now written **three times at three scopes**, each with its own copy of the builtin-wins rule: `ConformanceCheck.resolveInterface:257` (whole-program), `requireInterfaceForParse` (same-file linear), `interfaceUsesArity` (whole-program map). R7 correctly moved the third and correctly left the second alone, but that **widened the gap**: two parse-phase readers of one name now answer at different scopes. Nothing disagrees today (all three put builtins first) and the miss is a clean positioned reject, so it is not a wrong answer — but it is one fact with three spellings. ⇒ **ONE MECHANISM CLOSES ALL THREE**: record interface METHOD SIGNATURES in the declaration sweep, as R7 did for arity, and the scan, its cost and its scope disagreement all disappear together. ⚠ Bigger than R7 by a lot — the sweep is TOLERANT and must not become a second builder of `IrInterface` (R7 avoided that by reusing `parseInterfaceHeaderClauses`; the same discipline is required here, and signatures are much more than a header). One of the three sites is in `ConformanceCheck.maxon`, so the lane is **L-types ∩ L-parser-\***. | **L-types** *(+ `Parser.maxon`'s witness path + `ConformanceCheck.maxon`)*  ✅✅ **CLOSED 2026-07-31** (main; merged tree **2839/0**, x64-linux PASS, wasm PASS, arm64 **SKIP — remote, UNVERIFIED**; fragment ADDITIONS only, **zero goldens modified**). Rebased twice mid-flight onto A1 and B1 and re-gated on the merged tree each time. **The `where`-constraint interface is now resolved WHOLE-PROGRAM**: the sweep records each interface's POSITION (`InterfaceDeclSite`) and `Queries.foldInterfaceDeclarations` reads the declarations AFTER the per-file fold through the parser's own `readInterfaceDeclaration` — extracted from `parseInterfaceDeclaration`, so there is **ONE builder of an `IrInterface`** with two recorders. `lookupInterfaceForParse` is the single parse-time door for BOTH readers; the `artifact.interfaces` scan is DELETED. ⭐ **It SUBSUMED R7's arity store** (`InterfaceArityEntry`/`InterfaceArityMap`/`DeclaredInterfaceArity` deleted) — the arity IS `associatedTypeNames.count()` on the declaration that door returns, so keeping a second map was R7's own precedent against itself. R7's 20 cases stay green. ⭐⭐ **NEITHER REFERENCE WAS THE DESIGN, AND THE SURVEY IS WHAT ESTABLISHED THAT**: the bootstrap records full signatures pre-parse but has **no parse-time witness dispatch at all** (it monomorphizes and resolves by qualified NAME); v1 defers dispatch to Phase 11.1/11.3 passes and its interface methods are ordinary whole-program FUNCTIONS WITH BODIES, so a missing signature cannot arise. Only the bootstrap's PRE-SCAN SHAPE was taken. ⭐⭐ **THE PLACEMENT IS THE THESIS AND IT IS SABOTAGE-PROVEN**: `isGenericAlias` answers "no" mid-sweep, so a mid-sweep read renders a generic-alias type as the RAW ALIAS NAME where the real parse renders the CANONICAL INSTANCE NAME. Three-way, each its own build: pre-R8 `E3011 Unknown type 'Array_Integer'`; R8 as shipped **identical**; R8 with the read moved INTO the sweep **compiles the same program (exit 42)** off the raw alias. ⚠⚠ **AND THAT CRUX WAS GUARDED BY NOTHING WHEN FIRST WRITTEN** — the review applied the sabotage and the suite stayed **2837/0 GREEN**, because the acceptance case used a generic-alias FORMAL, whose rendered string is only ever COMPARED, never RESOLVED; the one case that resolves it shipped DISABLED. Now an ENABLED case pinning the pre-existing `E3011` as a LIMITATION (with what unblocks it, and the instruction to flip it to `exitcode 42` rather than delete it), **verified to FAIL under the sabotage**. ⚠ **The review also caught a diagnostic defect THIS RUNG INTRODUCED**: subsuming R7's store means an interface whose BODY will not parse records nothing, so the reject said *"'Digest' is not declared anywhere in this program"* about a `Digest` eight lines below — and measured, it can be the program's ONLY diagnostic. Fixed and pinned. ⭐ **THE UPSTREAM SPEC HAD NEVER BEEN RUN**: all 8 cases of `specs/where-clauses.md` were ABSENT (shv2's 21 were entirely self-authored). Ported byte-identical — **2 pass** (`eq-requires-equatable`, `eq-with-equatable`), **6 disabled** with measured blockers, none of them R8's: 3 on `Map`/dictionary literals, 2 on reading a TYPE-PARAM-TYPED FIELD at an instantiation outside the generic body, and `and-violation` — which my own port list called green and **is not**: shv2 gives E3017 at the right position with a DIFFERENT SENTENCE (⇒ **R9**). ⭐⭐ **A SECOND QUADRATIC DELETED**: the old scan was O(interfaces × dispatch-sites); on a hand-built ladder `phase:parse` CPU went from ×2.60/×2.55/×3.75 per doubling to ×1.82/×1.86/×1.93 — **−82.7% at N=2048 and −48.5% of the WHOLE compile**. ⚠ A first isolating corpus measured NOTHING because it constrained against the FIRST-declared interface, so the scan hit on iteration 1: **the target's POSITION in the scanned array is the variable**. Cost: `phase:signatures` **92.8→93.5 allocations per interface, dead flat**, the delta's ratios tracking interface counts rather than interfaces×files. ⚠ **One placement claim is UNGUARDED though UNREFUTED**: the code also asserts the read must precede `deriveInstanceNames`; moving it after leaves the suite **2839/0 green** and I could not build a discriminating program (the obvious shape dies earlier on an unrelated array-literal typing gap). Conservative placement, recorded as untested rather than proven | ✅ DONE | `slice/R8-interface-signatures-at-parse` | 2026-07-31T00:01Z |
| **R9** | **A WITNESS TABLE RESERVES A SLOT FOR A `static` REQUIREMENT NOBODY INSTALLS ⇒ REACHABLE COMPILER PANIC.** An `interface` with a `static function` requirement, a conformer that legally does not supply it, and a witness table ⇒ `panic at CodeResult.maxon:155: bakeFuncAbs64Relocs: .rdata slot '__witness_Point.Wide'+32 references .text function 'Point.tag', which is in no debug symbol`. `checkConformance` SKIPS static requirements while the witness table still reserves the slot and relocs to a symbol nobody installed — two components disagreeing about whether a static requirement exists. **Measured byte-identical on the PRE-R8 compiler** by the R8 review, so it is not R8's; the fix is in `ConformanceCheck.maxon` / `LowerMaxonToStd.maxon`. ⚠ **A reachable PANIC on ordinary user source, so this is a correctness row, not a cleanup one.** Carries two adjacent findings: (a) **E3017's SENTENCE does not match the canonical corpus** — `specs/where-clauses.md`'s `and-violation` expects *"Type 'OnlyFoo' does not satisfy constraint 'Bar' required by type parameter 'T' of 'NeedsBoth'"*, shv2 says *"type 'OnlyFoo' does not implement 'Bar', which the `where` clause … requires"* — right code, right position, wrong words; the text is `ConformanceCheck.maxon:124` and shv2's own `where-clauses.error.instantiate-nonconforming` pins the current wording, so aligning it moves an enabled golden. It is ported and DISABLED with that measurement. (b) **`ConformanceCheck.resolveInterface` and `LowerMaxonToStd.resolveWitnessInterface:1159` are two functions stating one rule over the SAME store** (`project.interfaces`), differing only union-vs-panic — unlike the parse-time/check-time split R8 defends, these share a phase-adjacent store, so this one is genuine duplication | **L-types** *(+ `ConformanceCheck.maxon` · `LowerMaxonToStd.maxon`)*  ✅✅ **CLOSED 2026-07-31** (main; merged tree **2850/0** with A1r, x64-linux PASS, wasm PASS, arm64 **SKIP — remote, UNVERIFIED**). **THE PANIC IS NOW A CLEAN E3016**, and the row's own framing was right: `checkInterfaceMethods` SKIPPED every static requirement while `ensureWitnessTable` still reserved a slot AND a `funcAbs64InRdata` reloc for it. ⭐⭐ **THE SKIP'S OWN COMMENT CONTAINED THE FIX**: *"a conforming type need not supply it AS AN INSTANCE METHOD — the oracle skips it, and so does this check"*. The skip existed to avoid a FALSE REJECT (`checkOneMethod` looked up an instance member), not because the requirement was optional — so the cure is to look up the STATIC member, not to skip. **No language ruling was needed**, which I established by measurement before planning: `/specs` and `specs-shv2` contain **ZERO** interfaces declaring a static requirement (scripted scan of every `interface … end` block). ⭐ **AND THE TWO REFERENCES DISAGREE, WHICH I HAD ASSUMED THEY DID NOT**: the bootstrap `continue`s BEFORE its null test (`2-Parser.cs:3427-3431`) so a missing static is silently accepted — harmless there, since it monomorphizes and builds no witness table — but **v1 pushes the missing entry FIRST and only then skips** (`SemanticCheck.maxon:412-417`), i.e. **v1 already reports a missing static**. shv2 takes v1's answer, with a reason. v1 then skips a PRESENT static's SIGNATURE because *"no runtime witness is dispatched against them"* — the one sentence shv2's dictionary-passing falsifies, so shv2 **diverges from both** and compares it. ⭐ **The receiver-kind rule (`method.isStatic != implIsStatic ⇒ no match`) is shv2-ORIGINAL**, which is why it ships with two cases of its own; the review sabotaged it (deleted the test) and exactly those two go red. **Six over-rejection shapes were tried and all stay green**: `Self`-returning static, `Self`-parameter static, static inherited via `extends`, static on a generic conformer, a static typed by an associated type, and a non-`export`ed static with a witness table actually built. ⚠ **DELIBERATE, STATED DIVERGENCE**: this newly rejects the no-witness-table case too — a program that compiled before and that the bootstrap accepts — because whether a witness table happens to be built must not decide whether a type conforms. That was the whole defect. ⭐ **E3017 now speaks the CORPUS's sentence**, which enabled `where-clauses.and-violation` (the case R8's port list wrongly called green). It moved 8 goldens — two files × four targets — and I verified by `git hash-object` that all four target copies are **byte-identical**, so the two arm64 copies no gate can run are verified as strongly as a non-runnable file can be. ⭐ `ConformanceCheck.resolveInterface` DELETED in favour of one `IrInterface.lookupInterface(interfaces, name:)` taking the MAP not the `Project` — which is what lets `LowerMaxonToStd.resolveWitnessInterface` share it while keeping its own miss (panic vs E3015). Correctly NOT merged with `Parser.lookupInterfaceForParse` (R8's defended pre-merge/mid-parse split). ⚠⚠ **A `## ` HEADING IN A SPEC FILE SILENTLY DISABLES EVERY CASE BELOW IT** — the implementer's first draft put six new cases outside `SpecParser`'s active region and the run came back a clean `12 passed, 0 failed`: **a green that tested nothing**, caught only because the count did not rise. Use `###`. The review swept every `specs-shv2/*.md` for the same latent shape and found none on main | ✅ DONE | `slice/R9-static-requirement-witness-slot` | 2026-07-31T01:42Z |
| **R10** | **WHICH MEMBER SATISFIES A REQUIREMENT IS DECIDED BY A BARE `Type.method` KEY, SO CONFORMANCE DEPENDS ON MEMBER DECLARATION ORDER.** `ConformanceCheck.checkOneMethod` resolves a requirement by the bare key alone; under D7 that key belongs to the **first-declared** member and every later overload registers as `Type.method#<suffix>`. MEASURED by the R9 review, **with no `static` anywhere**, so it is neither R9's nor about statics: for `interface Named { function label(extra Code) returns Code }`, declaring `label()` first and `label(extra Code)` second gives a spurious **E3016** (*"label() returns Code (expected label(extra Code) returns Code)"*) on a type that DOES supply it — and swapping the two members compiles, exit 42. **Neither reference has this**: the bootstrap tries `MangleOverloadName` (`2-Parser.cs:3418-3425`), v1 walks the bucket via `findOverloadMatchingSignature` (`SemanticCheck.maxon:411-412`). ⚠ **It is a spurious REJECT, never a miscompile, and the reason matters**: `ensureWitnessTable` (`LowerMaxonToStd.maxon:1148`) stamps `qualifiedMethodName(typeName, method.name)` — the SAME bare name — so the two components agree by construction today. **Fixing only `ConformanceCheck` would accept a program whose witness slot then relocates to the OTHER overload**, which is R9's own defect class one level deeper traded for a worse failure mode. ⇒ The fix is **ONE decider for "which member satisfies this requirement", read by both**, and `MaxonToStdLowering` holds no `Project` from which to run `signatureMatches`. Carries a second, smaller finding: `IrInterface.interfaceMethodIndex` returns `methods.count()` as a NOT-FOUND SENTINEL (CLAUDE.md forbids sentinel returns), decoded by `if methodIndex < count()` at both call sites — one of which is in `Parser.maxon` | **L-types** *(+ `LowerMaxonToStd.maxon` · `Parser.maxon`'s witness path)* ✅✅ **CLOSED 2026-07-31** (main `3440706e0`; x64-windows **2882/0** on the merged tree — 2869→2880 on its own base, +2 from A1g — x64-linux **PASS**, wasm32-wasi **PASS**, arm64 **SKIP — remote, UNVERIFIED**; builds 0 warnings, no exit 101, **ZERO goldens modified** (22 additions), bootstrap untouched). ⭐⭐ **ROUTED THROUGH THE ENUMERATION THAT ALREADY EXISTED** — `project.overloadSets` + the now-exported `SemanticCheck.overloadCandidates` (the same staleness filter `resolveOverloadedCalls` applies) and the EXISTING `signatureMatches` per candidate; satisfied iff **exactly one** matches. No second enumeration, no second signature comparison. ⛔⛔ **THE TRAP THIS RUNG WAS PLANNED AROUND, AND IT IS THE REASON THE ROW WAS WORTH A FULL RUNG: FIXING CONFORMANCE ALONE WOULD HAVE TRADED A LOUD FALSE REJECT FOR A SILENT WRONG DISPATCH.** `ensureWitnessTable` independently minted the same un-suffixed `Type.method` join to record the witness slot's `.rdata` relocation — harmless ONLY because it was **coupled** to the bug (both sites wrong the same way, so they agreed, which is precisely why the defect presented as a loud E3016 rather than a misdispatch). ⇒ `ensureWitnessTable` now mints nothing: `checkOneMethod`'s accept path files the **SELECTED registration name** under `witnessSlotImplKey` in `project.witnessSlotImpls`, and `LowerMaxonToStd.witnessSlotImpl` reads it. **ONE value, ONE producer, ONE consumer** — coordinator-verified at the code, with a **panic** if two conformances ever claim one slot differently. ⭐ **RE-DERIVATION WAS NOT AVAILABLE, and that is why the value is RECORDED rather than RECOMPUTED**: `signatureMatches` compares **source spellings** and `checkConformance` runs **before `resolveTypes`**, which erases `Integer`→`int` — a lowering-time re-run would answer differently. Sharing the PROCEDURE was impossible; sharing the VALUE was the only correct shape. ⭐⭐ **THE DISPATCH CONTROL IS WHAT PROVES IT, AND IT WAS MANDATED BEFORE A LINE WAS WRITTEN**: reverting ONLY the recorded lookup gives **23 passed / 3 failed** — every conformance case GREEN while the two witness-dispatching programs return the **wrong overload's value with no diagnostic**. The silent wrong dispatch, reproduced on demand. **A rung whose obvious fix is a regression needs a control aimed at the regression, not at the bug.** ⛔⛔ **THE REVIEW FOUND THE RUNG SHIPPED A REACHABLE COMPILER PANIC — A REGRESSION FROM A CLEAN REJECTION, AND THE ARGUMENT FOR ITS IMPOSSIBILITY REFUTED ITSELF.** `recordWitnessSlotImpl`'s "cannot disagree" panic reasoned that *"the selection depends only on the conformer and the requirement's SUBSTITUTED signature"* — **and an associated-type binding is exactly what varies the substituted signature.** Two routes from ordinary source, each verified to be a clean **E3016 against the parent commit's own binary**: `implements Conv with Whole, Conv with Real`, and `implements Child with Whole, Parent with Real` where `interface Child extends Parent uses Item`. ⚠ **The second route names NO interface twice** — it reaches `Parent` once directly and once through `Child` — **so a duplicate-entry rule would have passed it straight to the panic; the cure had to be at the SINK.** Now **E3111** (154 → **155 codes**), positioned on the conforming type's name, naming both members' **signatures** rather than registration names (a key like `convert#Real` is compiler-minted, not something an author wrote). ⭐ **THE FALSE-REJECT SWEEP CAME BACK CLEAN, FOR A STRONGER REASON THAN EXPECTED — and this was the rung's biggest risk, since the fix introduces a NEW rejection of programs that compiled before.** Two ranged aliases over one primitive now both satisfy one requirement ⇒ E3016 ambiguity where the bare key silently picked one. But such a program was **UNCALLABLE ANYWAY**: `t.label(5)` already gives `E3007 Ambiguous overload … Candidates: (v int), (v int)`. **The new rejection says at the DECLARATION what the resolver already said at every CALL.** A full sweep of `stdlib/` + `specs-shv2/` + `maxon-shv2/` found exactly ONE same-primitive-alias overload pair on an `implements`-ing type — the rung's own deliberate negative test. ⭐ **A DEFECT THE RUNG ITSELF WOULD HAVE CREATED, FIXED NOT DEFERRED**: matching against the whole set makes an interface declaring TWO same-named requirements conform, but `interfaceMethodIndex` resolves dispatch by NAME, so the second requirement would own an unreachable slot and report a false arity error. Refused cleanly at the interface declaration — and verified **not** too broad: a type cannot declare a static and an instance method of one name (E3006), and same-named requirements on DIFFERENT interfaces are different slots and correctly still conform. ⭐ **STATICS — R9 MADE THIS BUG REACHABLE FOR THEM TWO RUNGS EARLIER** by removing the `continue` that skipped static requirements, and nothing covered it. The witness relocation **DCE-roots the member it names**, so the goldens are themselves the guard: `func @Point.tag#` (unlucky order) vs `func @Point.tag` (lucky). ⚠ The implementer first wrote in the spec that a static slot has NO guard; **its own sabotage falsified that prose and it corrected it.** ⭐ The review also deleted a heap `String` built **per guard call** to describe an unreachable panic (interpolation over a `let` String is not folded — confirmed by IR dump: `__mm_alloc` + three `__str_copy`s): **−149 bytes of code**, and it is why the ladder row is measured post-review — the implementer's own pre-review reading was ~14% higher. Ladder: allocations **×1.79→×1.98**, deltas `+213/+309/+509/+901/+1677/+3221` (successive differences roughly doubling ⇒ `const + k·2^n`), **0.021% of rung-5**; no step-6b trigger. **Opens `R10b`, `R10c`, `R10d`.** | ✅ DONE | `slice/R10-conformance-overload-key` | 2026-07-31T03:15Z |
| **R10b** | **⛔ A HUNG COMPILE HOLDS THE BINARY LOCK AND NOTHING REAPS IT — the two-suites-one-tree race's second, undocumented symptom, and it has now cost time TWICE IN ONE SESSION.** The known race is that two suites in one tree fight over `.spec-tmp` and produce a FALSE RED. **What is NOT written down is that it can leave a `maxon-shv2` process ALIVE indefinitely**, holding `maxon-shv2.exe`, so the *next* agent's build fails `E6002: could not remove the previous build artifact … it is locked or read-only` — an error that names neither the race nor the stale process. Measured twice: (1) during **A1**, a stray `maxon-shv2.exe` made a build fail and the suite then ran the **STALE binary**, producing a false red of exactly 4 tests — all four being D12's cases, which is what identified it as staleness rather than a defect; (2) during **R10**, the reviewer's first rebuild failed the same way on a process the *implementer* had left running ~30 minutes earlier (`build … --emit-ir`, started 21:00). ⚠ **A `;`-chained `build; suite` makes it WORSE, because the suite runs on the stale binary and can report either a false red OR A FALSE GREEN.** ⭐ Three candidate cures, in increasing order of value: (a) chain with `&&` and check the build's exit code — an operator discipline, and it is what caught (1); (b) make `E6002`'s message NAME the holding process and suggest the fix, since the compiler is the one that knows; (c) **reap it** — have the spec runner kill child compiles it spawned when it exits, so a timed-out run cannot outlive its parent. ⚠ **(c) is the only one that removes the failure mode rather than making it legible**; note the timed-out tool call does NOT kill the process, which is the root of it. This is a TOOLING gap, not operator error, and it is now the most frequent time-waster in the rung loop ✅✅ **CLOSED 2026-08-01** (main `9c8d77e67`; x64-windows **3079/0**, x64-linux **2958/0**, wasm32-wasi **2909/0**, arm64 SKIP — remote, UNVERIFIED; 0 warnings; no exit 101; fragments empty; ladder **0**, proven by a coordinator same-path A/B — the pre-rung compiler rebuilt in this checkout gives byte-identical absolutes, and the **+14 the delta column shows PREDATES this rung**). **The remainder was a MESSAGE**: `E6002` now names the likely cause, warns the previous binary survived and will answer anything you run next, and quotes a deadline interpolated from `WedgeWatchdogMs` so the number lives in one place. ⛔ **THE REVIEW FOUND IT STATED ONE DEADLINE OVER TWO HOLDERS** — a plain `maxon-shv2 build` is bounded by nothing, and THIS ROW'S OWN second incident (a stray build holding the binary ~30 minutes) is the counterexample, a hundredfold past the quoted 300 s. The rung's own measured case refuted its own sentence, erring toward *wait*. Two holders, two sentences now. ⛔ **AND THE POSIX ARM OF THE NEW GATE LOCKED THE DIRECTORY HOLDING ITS OWN LOGS**, so the redirect failed in the shell, the compiler never ran, and CHECK 2a passed FOR THE WRONG REASON (4/4 under WSL; 9/0 after). ⭐ **NEW EXECUTABLE COVERAGE FOR A BRANCH NO SPEC CAN REACH — `scripts/output-lock-gate.sh`, 9 checks, WIRED INTO `buildall.sh`** beside `check-debug-goldens.sh`, because an unrun gate rots. Sabotage-verified a real discriminator: revert only the message ⇒ 4 PASS / 3 FAIL, structural checks green. ⭐ A second `;`-chain script was argued DOWN: it would assert a HAZARD EXISTS and redden only if someone removed the hazard — an anti-gate. ⇒ residuals **A2t** (the bootstrap twin) and **G2** (the only cure that removes the false green) | L-harness *(`Testing/SpecWorkerPool.maxon` · `Testing/SpecTestRunner.maxon`; the `E6002` message is the compiler's)*  ⛔⛔ **THE ROW'S CENTRAL CLAIM — *"nothing reaps it"* — IS FALSE, MEASURED 2026-07-31, AND THE CURE IT NAMED IS UNAVAILABLE IN PRINCIPLE. I claimed this row, surveyed it, and RELEASED IT rather than implement against a false premise; correcting the row is the deliverable.** Three facts overturn it. **(1) THE REAPING ALREADY EXISTS.** `maxon-shv2.exe` is not self-hosted — `build.maxon` compiles it with the C# bootstrap — and the bootstrap's runtime already gives every subprocess handle an `hJob` created with `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` (`maxon-sharp/Compiler/MLIR/X86CodeEmitter.Runtime.cs:8874-8879`, job wiring `:11515`/`:11530-11535`/`:11850-11873`), and the streaming spawn uses the same `CreateProcessW` path (`:9691-9692`). **Children die when the owning process's handles close.** **(2) THE HANDLE WAS NEVER DISCARDED**, which is what I expected to be the blocker: `StreamingSubprocess` (`stdlib/Subprocess.maxon:646`) carries a real handle with `wait`/`waitWithTimeout`/`release` (`:733-761`). The only gap is that no Maxon-callable `kill` is exposed on it — and shv2's own `IatSlot` table has no `TerminateProcess` or JobObject slot (39 slots, `Targets/Windows/PeWriter.maxon:147-233`), though **v1 had all four** (`maxon-selfhosted/.../PeWriter.maxon:127-133`) and v1's own runner used a watchdog `subprocessKill` (`Testing/SpecTestRunner.maxon:3759`). **(3) ⭐ THE CURE I NAMED CANNOT WORK, AND THE REASON GENERALIZES: A RUNNER CANNOT REAP ITSELF ON AN EVENT IT NEVER OBSERVES.** The row's own text says the timed-out tool call does not kill the process — so the survivor is **the parent, still running**, not an orphan. There is nothing for parent-exit reaping to do, because the parent never exits. ✅ **AND IT DOES SELF-CLEAN, JUST SLOWER THAN THE TOOL BOUNDARY**: each compile/run subprocess is bounded by `SpecSpawnTimeoutMs = 120000` (`Testing/SpecTestRunner.maxon:274`, set as `config.timeoutMs` at `:279`), and a wedged dispatch loop trips `WedgeWatchdogMs` (= 120 s + grace, `Testing/SpecWorkerPool.maxon:173`) and **panics** (`abortOnWedgedPool:957`) — a panic exits, which closes handles, which fires the job object. **Both of my incidents were inside that window and I killed the process by hand before it self-cleaned**, which is exactly why it looked like a leak. ⚠ **WHAT REMAINS IS REAL BUT IT IS A MESSAGE, NOT A REAPER** — a different and smaller rung, which is why this row is released rather than silently re-scoped: for up to ~120–300 s an abandoned run holds the output binary, and `E6002` (`Compiler/Compiler.maxon:105-117`) names neither the cause nor the holder, so an agent inside that window cannot tell it from a real failure. It knows only `outputPath`; identifying a lock holder would need `RestartManager`/`NtQuerySystemInformation`, **neither imported anywhere in the repo**. ⭐ The cheap, correct fix needs NO OS surface: have `E6002` SAY the likely cause (*“a spec-test or build in this tree may still be running; it releases the binary within ~2 min, or kill it”*). ⚠ **The still-true half of this row, and the part worth keeping**: a `;`-chained `build; suite` runs the suite on the STALE binary and can report a false red — or a false GREEN. Measured during A1: a false red of exactly 4 tests, all four being D12's cases, which is what identified staleness rather than a defect in about a minute. **Chain with `&&`.** ⚠ One surveyor inference I do NOT accept: that those 4 tests correlate with `scripts/worker-exe-split-gate.sh`'s `EXPECTED_SELECTED=4`. Mine were managed-file/managed-memory/string-index cases, not `arithmetic/`; the shared number is a coincidence. ✅ And this class of property IS gateable, just not by the spec suite — `scripts/worker-exe-split-gate.sh` and `scripts/spec-wedge-repro.sh` are existing precedents for shell-level HARNESS gates | ✅ DONE | `slice/R10b-reap-child-compiles` | 2026-08-01T09:14Z |
| **A2t** | **THE BOOTSTRAP'S `E6002` TWIN HAS THE SAME GAP, AND THE TWO COMPILERS WERE ALREADY DIVERGENT.** `maxon-sharp/Compiler/0-Compiler.cs:300` says *"could not remove the previous build **artifact** at {path} — it is locked or read-only, so this build cannot replace it: {ex.Message}"* — it names no cause, does not warn that the stale binary survived, and calls it an **artifact** where shv2 says **output**; it does append the OS exception text, which shv2 does not. R10b fixed only the shv2 side and left this deliberately: **`L-bootstrap` was claimed by `B2` at the time**, and a bootstrap change needs the full C# suite (3198+) plus codegen neutrality as its gate. ⚠ The divergence PREDATES R10b — it is not damage this rung did, it is damage it declined to widen. Port the three clauses and settle on one noun | L-bootstrap | ⬜ FREE | — | — |
| **G2** | ⭐ **THE ONLY CURE THAT REMOVES THE STALE-BINARY FALSE GREEN RATHER THAN MAKING IT LEGIBLE: have `spec-test` REFUSE when its own binary is older than the sources it was built from.** R10b made `E6002` explain itself at the BUILD that failed; this kills the class at the point where the wrong answer is actually produced — a suite that runs a stale binary and reports **green**. Compare `Process.executablePath()`'s mtime against the newest `maxon-shv2/**/*.maxon` and refuse, or warn unmissably. ⚠ **Real design questions, which is why R10b reported it rather than inventing an answer mid-rung**: a RELEASED binary has no sources beside it; the rung workflow COPIES `bin/` into a worktree, so mtimes are not build times; and cross-machine clock skew. Each needs a decision, not a guess. ⭐ Precedent that it is gateable at all: `scripts/output-lock-gate.sh` (R10b) and `scripts/worker-exe-split-gate.sh` (G1) are shell-level HARNESS gates for exactly the properties the spec suite structurally cannot reach ✅✅ **CLOSED 2026-08-01** (main `72e888649`; x64-windows **3088/0** on the merged tree, x64-linux **2958/0**, wasm32-wasi **2909/0**, arm64 SKIP — remote, UNVERIFIED; 0 warnings; no exit 101; fragments empty; no ladder — `Testing/` only, no pass, no IR, no indexed structure). **`spec-test` AND `scale-test` now refuse a binary older than its sources, with exit 2** — distinct from 1, which is both a failing suite and `PanicExitCode`. ⭐ **The mechanism was DERIVED, not invented**: `cross-target-gate.sh:166` already did this for `--skip-build`. ⭐ **Ruling verified rather than trusted**: a spec run executes the parent harness, N `--worker-persistent` workers, `compilerExe`, the built program and any external runner — **`bin/maxon.exe` is never among them**, so the copied-`bin/` mtime concern does not reach the binary under test; G1's split is real, so BOTH binaries are checked. Scoped to project sources, **not `stdlib/`** (whitelisted stdlib is recompiled per fragment, so an edit is already live and refusing would be a FALSE refusal) — and no shv2 stdlib cache exists to change that. ⛔⛔ **THE RUNG SHIPPED ITS OWN DEFECT CLASS AND THE REVIEW CAUGHT IT: a `*.Test.maxon` refused EVERY run in the repo**, because the mirror matched the suffix byte-exactly while the bootstrap's `IsTestFile` folds case. **Measured both halves** — the bootstrap skips the file, and `spec-test` exited 2 naming it. That is the SECOND time in one rung that this hand-written mirror failed one clause at a time (the implementer caught the `.maxon/` output-dir clause the same way) ⇒ **G3**. ⭐ **New harness gate `scripts/stale-binary-gate.sh` (11 checks), wired into `buildall.sh`**, a 2×2 over *is-a-source-newer* × *do-sources-sit-beside-the-binary*, with SIX negative controls that must still RUN. **Coordinator-verified a real discriminator twice**: delete the check ⇒ exactly 2a/2b/2c/7 FAIL and all six controls PASS; revert only the review's fix ⇒ exactly CHECK 5c FAILS. ⚠ Boundary stated in the header, absent from the message: the check ships inside the binary it reports on, and mtime cannot see a **deleted** source ⇒ **G3**'s manifest route. ⚠ Three gate defects also fixed, one of them the same shape as R10b's: `age_forward` proved its work with `find -newer` (full precision) while the harness compares whole SECONDS, so a same-second tie let the false-refusal controls **pass without creating the condition they control for** | L-harness | ✅ DONE | `slice/G2-stale-binary-refusal` | 2026-08-01T10:35Z |
| **G3** | ⛔⛔ **"IS MY BINARY STALE" IS NOW WRITTEN THREE TIMES, IN THREE LANGUAGES, WITH THREE DIFFERENT EXCLUSION SETS AND NO SHARED AUTHORITY.** (1) `maxon-shv2/Testing/CompilerFreshness.maxon` (G2) — mirrors the bootstrap's three exclusions, compares whole seconds, **strictly newer**. (2) `scripts/cross-target-gate.sh:166` — `find … \( -name '*.maxon' -o -name '*.cs' \) -newer` with **NONE of the three exclusions**, so a newer `maxon-shv2/track0/*.maxon` or any `.maxon` under a `.maxon/` output dir makes `--skip-build` **refuse falsely**. ⚠ It must NOT simply defer to the binary's check: it covers `stdlib/` and the bootstrap's C#, and it has to work on a binary whose own check may itself be stale. (3) `maxon-dev-mcp/mcp/Staleness.maxon:87` — breaks the one-second tie the **opposite** way (`>=`), which the G2 review judged deliberate and correct: a different price on being wrong. ⭐⭐ **THE DIVERGENCE IS NOT HYPOTHETICAL — IT HAS ALREADY BITTEN TWICE IN ONE RUNG**, each time one clause at a time: the `.maxon/` output-dir clause (implementer, mid-rung) and the case-insensitive `*.test.maxon` suffix (review) — the latter shipped, and a `*.Test.maxon` wedged **every spec-test and scale-test in the repo** over a file the build never reads. ⚠ **AND THE MIRROR IS DECLARED AT ONE END ONLY**: `maxon-sharp/Compiler/SourceCollector.cs`'s `ExclusionFor` carries no comment saying a Maxon mirror exists, so a FOURTH clause added there diverges silently again. ⭐ **The real cure closes a blind spot too**: the bootstrap already writes `<project>/.maxon/cache/build-manifest.json` listing every source it compiled WITH its mtime, `stdlib/` included — reading it answers both *deleted source* and *stale stdlib compiled INTO the binary*, which no mtime sweep can. ⚠ But that is a `maxon-sharp`-owned format, so a second parser for it would be this project's signature bug wearing a fix's clothes — it needs a decision about who owns the format, not just a reader | L-harness *(+ `maxon-sharp/` for the comment, and the MCP is another project)* | ⬜ FREE | — | — |
| **G4** | **A BARE `maxon build` FROM THE REPO ROOT IS DESTRUCTIVE, AND NOTHING SAYS SO.** Measured by the G2 review as collateral: `./bin/maxon.exe build` with **no arguments** from the repo root **deleted the tracked `bench/sortbench.exe`** (delete-at-start of a previous output — the same `discardPreviousOutput` contract R10b documented) and swept `.scale-tmp/` as source. Recovered with `git checkout --`, tree clean. ⚠ It is the no-argument form that is dangerous: it takes the CWD as the project, so at the repo root it treats the whole checkout as one. **An agent that types it loses tracked build artifacts with no prompt and no summary of what it removed.** ⇒ either refuse a bare `build` whose implied project is a directory holding no `build.maxon`, or name what it is about to discard | L-harness | ⬜ FREE | — | — |
| **A2v** | **MCP `run_spec_test` REPORTS `success` FOR A REFUSAL, WITH 0 TESTS RUN.** After G2, a stale binary makes `spec-test` exit **2** without running anything; the MCP wrapper surfaces `exitCode: 2` but still shapes the result as `ToolOutcome.success`. Not a false green — the code IS in the payload — but an agent skimming *"0 failed"* can read a refusal as a pass, which is the exact misreading G2 exists to prevent, one layer up in the tooling. ⇒ special-case exit 2 in the wrapper and say REFUSED. ⚠ Touching `maxon-dev-mcp/mcp/` means rebuilding it AND restarting the MCP server, or every later `tools/call` refuses (by design) | *(maxon-dev-mcp — no lane)* | ⬜ FREE | — | — |
| **R10c** | **CALLING AN `extends`-INHERITED REQUIREMENT THROUGH A CONSTRAINT IS UNIMPLEMENTED, and R10 made one variant say something FALSE.** Any inherited requirement reached through a `where` constraint gives `E2015: no 'where' constraint … declares a method 'baseM'` — pre-existing and independent of R10. ⚠ **But where the child interface OVERLOADS the inherited name, R10 makes a worse message reachable**: `self.item.label()` reports `'Derived.label' expects 1 argument(s) but 0 were provided` — **false about an interface that inherits `label()`**, and the same class as D11/D12/R10's own original message (a refusal is easier to get right than the noun in it). ⭐ **Root cause is a MISSING MECHANISM, not a wording bug**: `findWitnessDispatch` has no `parentTablePtr`, so a parent interface's slots are unreachable from a child's table — widening the message without the dispatch would describe a call that still cannot be made. ⚠ Related and distinct: **`findWitnessDispatch` picks the FIRST constraint declaring a name**, so `where T is Stringable and FormattedStringable` can only ever dispatch `Stringable.toString()`. Pre-existing; now exercised by a green R10 case that deliberately dispatches nothing through it. ⇒ one rung for parent-table dispatch, which subsumes both. ✅✅ **CLOSED 2026-08-01** (BATCH5, main `ecb805485`; shv2 3079 → **3088/0**). ⭐⭐ **THE ROOT CAUSE WAS NOT A MISSING PARENT-TABLE POINTER — IT WAS THREE READERS HOLDING THREE DIFFERENT ANSWERS TO “WHICH SLOT IS REQUIREMENT M OF INTERFACE I”.** `ConformanceCheck` walked `extends` transitively; the witness-table BUILDER (`ensureWitnessTable`) and the dispatch RESOLVER (`findWitnessDispatch`) each read `iface.methods` alone. One function (`IrInterface.interfaceWitnessSlots`) now computes the transitive slot list — own requirements first, then each parent depth-first, `visited`-guarded so a **diamond is ONE slot** (verified in the golden: four `callMem` offsets, not five) — and the builder, the resolver and the conformance check all call it. Dispatch selects by name **AND arity**; ambiguity is **REFUSED** (E3114/E3115), never picked, because a witness dispatch compiles one slot offset and a silent pick is a wrong function pointer rather than a diagnostic. ⚠ **BOTH REFERENCES WERE WARNINGS, NOT TEMPLATES, AND THE SURVEY IS WHAT ESTABLISHED THAT**: v1 designed the OPPOSITE (a table per conformance plus a parent pointer) and left it **inert — zero on disk, no relocation**, because the rdata→rdata reloc kind was never written; the bootstrap **has no witness tables at all** (it monomorphizes) while sharing the identical dispatch blind spot. shv2's own layout comment claimed parent methods were *“projected INTO the child table”* — **nothing projected them**; that comment is now true. ⛔ **THE REVIEW FOUND A REACHABLE FALSE REJECT IN THE RUNG'S OWN NEW REFUSAL, one nesting level below where the rung tested it**: the diamond was closed inside ONE interface, but split across TWO CONSTRAINTS no `visited` set spanned them, so one requirement was reported as its own rival (`provided by both Root.base() and Root.base()`). Fixed by deduping ROUTES at collection on the same `(conformer, declaring interface, name)` triple the impl filing already uses. ⭐ **AND THE OPTIMIZER FOUND A QUADRATIC THE STANDING LADDER CANNOT SEE**: the slot list was built PER DISPATCH SITE (`1.02·M² + 19·M`, +19,072 allocations at M=128 on `genwitness.sh`, parse CPU +16.4%), now memoized per `Parser` → linear (+670); an `extends` chain had been strictly worse than a flat interface (85,584 vs 57,784) because the per-site `visited` set paid the depth on top of the slots. **`ScaleCorpus` generates no `where` constraints at all**, so none of it was visible to the trend instrument ⇒ see `A2n` | L-types *(`ConformanceCheck.maxon` · `findWitnessDispatch` · the witness table layout)* | ✅ DONE | `slice/BATCH5-witness-ordinals` | 2026-08-01T08:51Z |
| **R10d** | **TWO SPEC/DOC ROT ITEMS R10 EXPOSED, both cheap and both the "a description that guards nothing" shape.** (1) **`specs-shv2/string-interpolation.md:738` is a `disabled-test` carrying the `Stringable, FormattedStringable` overload shape** — so that shape is *in the corpus but never executed*, which is the same class as [[feedback_a_red_gate_pins_nothing]]: it reads as coverage in a listing and provides none. R10 authored a live case for the conformance half (`overloaded-tostring-satisfies-stringable-and-formatted`); this one is about the interpolation half and should either be enabled with a named blocker or state what it is waiting on. (2) **A `PLAN.md` row names `requireSeparatorFreeInterfaceName`, which R10 renamed to `requireSeparatorFreeLabelComponent`** (generalized because `witnessSlotImplKey` needs the same scan for two components). A row citing a function that no longer exists is a row a reader cannot check — the same rot the D11 rung found three ways over in P1.7b's NOTES (2). ⚠ **Also pre-existing and left deliberately**: `WitnessLabelSeparator.toByteArray()` allocates a `ByteArray` per guard call. Removing it cleanly needs a decision on how that ONE constant is spelled — a second `b"."` would be one fact written twice, which is exactly what that file exists to prevent, so it is a decision rather than a tidy-up | *(docs + `specs-shv2/` — no compiler lane)* | ✅✅ **CLOSED 2026-07-31** (main `4329816db`; x64-windows **2901 → 2902/0**, x64-linux **2783 → 2784/0**, wasm32-wasi **2757 → 2758/0** — the new case runs on all three; arm64 **SKIP — remote, UNVERIFIED**; no compiler source touched, so no scale-test row was written: the binary is byte-identical and a Δ0 row restating that would be noise). ⚠ **THE ROW'S CITATION `string-interpolation.md:738` DESCRIBES THE PRE-FIX FILE AND HAS MOVED.** ⭐ **ITEM (1) WAS SHARPER THAN FILED: the case already NAMED its blocker in detail — what nothing had re-checked was whether the blocker was still TRUE.** Measured: it named three things and **two were already closed** (the method-overload half by D7; zero-arg interp dispatch on a dual conformer). Only `{c:verbose}` fails, and it fails in the **PARSER** (`E2010: Expected 'interpolation end' but got ':'`), never reaching dispatch. So one case bundled a half that works with a half that cannot be read, and disabling the pair left the working half unexecuted ⇒ **split into a live case + a disabled one, because the halves have different blockers.** ⛔⛔ **THE REVIEW FOUND MY NEW LIVE CASE DID NOT ASSERT WHAT ITS NAME CLAIMED — the rung's own thesis, one level down.** I declared `toString()` FIRST, and **the first declaration of a name keeps the BARE key** (`Parser.maxon:6871`), which is exactly the key interpolation synthesizes (`:22178`). So `{c}` printed `42` whether the resolver SELECTED the zero-arg member **or never ran at all**; my `"two-arg:{value}"` discriminator only fired on a mis-SELECTION, and **NON-selection is the failure mode that actually exists** (`SemanticCheck.maxon:152-155` says so outright). **Cured by declaring the 2-arg member FIRST**, so the bare key sits on the member the call must not reach: the golden moved from `callDirect Counter.toString` to **`callDirect Counter.toString#`**, a symbol only an active retarget produces, and a measured non-selection is now `E3036` RED rather than lucky-green. ⛔ **MY REWRITTEN BLOCKER COMMENT WAS DIFFERENTLY WRONG TOO**: *"blocked on the format-spec syntax and on nothing else"* — but `materializeStructToStringPart` (`Parser.maxon:22192-22198`) builds a receiver-ONLY argument list and `formatspec` appears nowhere in the compiler, so landing `{x:spec}` parsing alone would parse a spec nothing consumes. **A parse error cannot license "and on nothing else" about what sits BEHIND it**, and the comment contradicted its own last clause. Now names both halves, marking (1) measured and (2) read-off-the-code-because-(1)-hides-it. ⛔ **AND MY COMMIT MESSAGE OVERSTATED IT**: R10's `interface-conformance.md:1234` **already** pinned interpolation dispatch on a dual conformer, in the ADVERSARIAL order with a total `P`/`F` discriminator — so *"the working half went untested"* was too strong; the true claim is that **this spec** carried no live case, and mine was the weaker of the two. Both kept (they exercise different sites — conformance checker vs interpolation materializer) and made to **cite each other in both directions**. ✅ The disabled case's expectations verified canonical against `stdlib/Interfaces.maxon:235-240` **and the bootstrap's own live case** (`specs/string-interpolation.md:796`), so it is not a trap for whoever enables it. ✅ Item (2) fixed: `PLAN.md:2548`'s `requireSeparatorFreeInterfaceName` → `requireSeparatorFreeLabelComponent` (`IR/Maxon/IrInterface.maxon:168`). ⚠ **`WitnessLabelSeparator.toByteArray()` deliberately LEFT, and the review sharpened why: the obvious cure is not obviously a win** — a second `b"."` is the one-fact-twice pattern that file exists to argue against, and inverting it merely moves the allocation from the scan onto the join, which runs on the same path. Both callers already allocate per call, so it is a **share, not a hot loop** ⇒ measured perf debt, not a tidy-up | `slice/R10d-doc-rot` | 2026-07-31T09:52Z |
| **A1** | **FALLIBLE DIVISION — `__checked_div` / `__checked_mod`.** ⚖ **Opened by a USER RULING 2026-07-30**, chosen over two cheaper alternatives that were measured and rejected. **It is D6's third blocker and the ONLY thing between shv2 and a loadable `stdlib/Builtins.maxon`**: `:285` writes `fracPart + (try (digit / fracDiv) otherwise panic("float parse: fracDiv is >= 10"))`, and shv2 has **no throwing division** — `specs-shv2/division.md` says so as a deliberate position (*"Divide-by-zero and `INT_MIN / -1` overflow raise a hardware `#DE` … so there is no compiler-inserted guard"*), so there is nothing for the `try` to catch and **E3055** rejects the form. ⭐ **MEASURED: it is the only blocker left** — a copy of the module with that one expression unwrapped compiles to `E3001 No 'main' function found` and nothing else, so the `ParseError` collision D6's survey centred on **never even arises**. **The shape to build** is the oracle's (`maxon-sharp/Compiler/2-Parser.cs:23604-23641`): a `__checked_div`/`__checked_mod` desugar over a synthesized `__DivisionByZeroError`, plus E3057/E3103. ⚠⚠ **AND IT IS NOT ADDITIVE — that is why it is its own rung and why the two cheap options were tempting.** It newly REFUSES divides in committed, currently-green shv2 specs (`specs-shv2/arithmetic.md:125,208-209`; `arithmetic-operators.md:164`) and it **contradicts `specs-shv2/division.md`'s documented design**, which must be rewritten rather than quietly left wrong. Expect golden movement and budget for it. ⚠ **Two rejected alternatives, recorded so they are not re-proposed**: (a) **three lines in `stdlib/Builtins.maxon`** — give `fracDiv` a ranged type excluding zero and drop the `try`, the oracle's own escape hatch, **tested identical on BOTH compilers (build 0 / run 30)** — rejected because it edits the shared stdlib; (b) an **opt-in guarded divide** accepting `try (a / b) otherwise H` without requiring `try` on a bare divide — rejected because `/` would then have two semantics chosen by syntax and `division.md` would still be wrong. ⚠ **LANE:** it touches `Parser.maxon`'s operator lowering, so by the rule 7b's row records (*a row touching a file some lane names is IN that lane*) it collides with any live `L-parser-*` claim — **check before claiming, and do not read the `—` as lane-free** | **L-parser-\*** *(via `Parser.maxon`'s operator lowering — collides with any live parser lane; also `IR/Std` and the arithmetic specs)*  ✅✅ **CLOSED 2026-07-30** (main `7690dfcfc`; x64-windows **2832/0**, x64-linux **PASS 2690/0**, wasm32-wasi **PASS**, C# **3187/1** — that 1 is PRE-EXISTING, reproduced on clean `main` before touching anything; arm64-macos + arm64-linux **SKIP — remote, UNVERIFIED**). Builds 0 warnings, no exit 101, fragments empty **including UNTRACKED** after an unfiltered run. ⭐ **THE GUARD IS BRANCHLESS AND THAT INVERTED THE PLAN'S CENTRAL WORRY.** `flag = (divisor == 0)` · `safe = divisor or flag` · `idiv dividend, safe` — a deliberate divergence from the bootstrap's three-block CFG, because `lowerMaxonToStd` maps Maxon→Std blocks **1:1 by construction** and shv2 carries values in BLOCK ARGS, so the reference's store/reload stack temp has no counterpart. The compare's own 0/1 IS the error flag (`divisionByZero` is ordinal 0, encoding `ordinal + ErrorFlagOrdinalBias`), **asserted** in the lowering, not assumed. ⇒ **a checked divide is STILL an `idiv` under the same RAX/RDX pinning**, so the plan's fear that a guarded call+branch would silently stop `register-fixed-registers` testing its subject was WRONG: `idivReg` is still present in all 10 goldens (2/1/2/1/1/1/2/3/6/6, coordinator-verified). ⛔⛔ **THE REVIEW FOUND A REACHABLE WRONG ANSWER THE IMPLEMENTER HAD ARGUED AWAY, and it is the whole case for the review running LAST and by a different agent.** A1 widened `try` to accept `try (…)`, claiming *"it is the EXISTING machinery — not a new gate — that keeps it shut"* because `rewriteLastCallToTryCall` converts the last op the target emitted. **That derivation holds only while the target is guaranteed to emit one, and `try (name)` is the first form that emits nothing.** Measured: `let a = arr.get(2)` (out of bounds, E3057 owed) then `let w = try (a) otherwise 99` **compiled clean and printed 99** — the handler was transplanted onto the array call two statements up, `w` took 99 instead of `a`, and the owed E3057 vanished with the plain `call` it would have been reported against. **Two lost diagnostics AND a wrong value, on a green suite.** Cured by a derivation (an op-index FLOOR recorded by `parseTry`; an op below it cannot be the target's) rather than a paren-shaped test, red first and red again under sabotage. ⭐ **`emitBinOp` NOW REFUSES a div/mod outright** — its "must not arrive here directly" comment had already failed once in this very rung (`emitMonotonicMs` had become a silent second division path), so DERIVE > CHECK > COMMENT was applied to a fact already broken once. ⭐ **A FALSE PROSE CLAIM IN THE RUNG'S OWN SPECS, FIXED:** both `safety.md` and `division.md` said `i64.min / -1` is classified to the `panic: integer divide by zero` diagnostic. It is not — it arrives as **STATUS_INTEGER_OVERFLOW 0xC0000095** and shv2's fault thunk converts 0xC0000094 and nothing else, so the process dies with **no panic line at all** (the bootstrap *does* classify it). ⚠ `INT_MIN / -1` stays deliberately unguarded (neither reference handles it) and the specs now say that accurately. ⭐⭐ **THE RUNG EXPOSED AND PAID FOR A QUADRATIC IN A PASS IT NEVER TOUCHED.** `InsertRangeChecks.splitBlock` moved every op past its cut into two fresh arrays on EVERY guard site (Θ(k·B) for k guards in a B-op block); A1's own corpus retyping — 1,025 `as ScaleDivisor` casts in one straight-line block — is what made it visible, at `phase:insertRangeChecks` **bytes ×2.83 ×2.82 ×3.73 ×4.11 ×4.32 per DOUBLING** and **13.0% of all bytes** at rung 5, with allocations FLAT (the signature: a fixed count of allocations each getting bigger). Cured by carving segments FORWARD out of one pre-split list: **−95.5% at rung 5, CPU 5.0× cheaper**, phase now 0.67% of bytes, ratios ×2.11 — and output proven unchanged three ways (every other phase identical to the digit at all six rungs, `codeBytes` identical at all six, fragments empty). **FIXED not filed**, because the file-it rule requires a debt measured LINEAR IN PRACTICE and this triggers on ordinary Maxon. ⚠ **Coordinator-added case: a RUNTIME `-0.0` divisor** — the suite tested `+0.0` at runtime and `-0.0` only at compile time, and `+0.0`'s bit pattern is 0 so it passes under either reading. Its first draft claimed a bit-compare would silently yield `-inf`; **sabotage falsified that** (the x64 register-file guard refuses an integer compare of an xmm operand) and the case's prose now records what it does and does NOT pin. ⚠ **`scale-test` was REFUSING TO RUN FOR EVERY AGENT ON MAIN** — D12's three log rows carried two different notes (one hand-extended) and the instrument checks they came from one run. Repaired without discarding either row's information. ⚠ **A CORPUS CHANGE INVALIDATES THE DELTA COLUMN exactly as a rebase invalidates absolutes** — a new `p_cdivide` knob means this row's delta is NOT this rung's attribution; the note carries the same-tree A/Bs instead. ⚠ **SHARES `SemanticCheck.maxon` WITH D12, which landed first** — its shared E3057 sentence + per-family source-method map is kept intact and division is a deliberately bespoke arm beside the array one (there is no method name to quote back: the author wrote an OPERATOR, and it is the only family with TWO ways out). Reconciled by hand and proven both ways by committed cases on each side. **Opens 4 rows below: `A1r`, `A1f`, `A1g`, `A1h`.** ⚠ **21 arm64 goldens now stale**, left stale rather than deleted so the lane goes loudly red at the periodic sync rather than silently losing the pin. ⛔⛔ **CORRECTED BY A1x 2026-07-31 — THIS ROW'S CENTRAL CLAIM WAS PARTIAL, AND THE PART IT MISSED WAS A PROCESS DEATH.** "A checked divide is fallible" was true of the ZERO divisor and of nothing else: `idiv` raises `#DE` for TWO reasons, and the second one — the unrepresentable quotient — was left to fault. For `/` that is defensible and documented; for **`mod` it was a WRONG ANSWER**, because `a mod -1` is `0` for every `a` and only x86's fusing of quotient and remainder into one instruction made it fatal. ⚠ **AND THE MISS WAS NOT CONFINED TO THE `try` PATH THIS ROW BUILT**: A1x measured **three** doors into a `mod` and **two of them never reach `lowerCheckedDivision` at all** — a literal `-1` divisor and a ranged divisor admitting `-1` are both `neverZero`, so A1's own proof sent them to the bare `idiv` it had just declared safe. **A divisor proven non-zero is not a divisor proven not `-1`**, and this row read one proof as if it were both. Cured in A1x by widening the verdict itself (`divisorZeroVerdict` → `divisorProof`, two facts from one walk) | ✅ DONE | `slice/A1-checked-division` | 2026-07-30T20:54Z |
| **A1r** | **⛔ A `__`-PREFIXED FUNCTION DECLARED IN `stdlib/Builtins.maxon` CANNOT BE CALLED — D6 opened the DECLARATION door and left the CALL door shut.** Measured 2026-07-30 while verifying A1's own premise, not read. `__int_fromString` is DECLARED at `stdlib/Builtins.maxon:214` and CALLED at `:322`, and compiling that real path (`maxon-shv2 build stdlib/Builtins.maxon`) gives **`E3004 :322:19 call to undefined function '__int_fromString': the '__' prefix names a compiler intrinsic, and no intrinsic of that name exists`** — a `__` callee is routed to the intrinsic table only and never consults a declaration. Confirmed general with a minimal probe: a `__probeReserved` declared AND called inside that same file gives the identical E3004. ⇒ this project's signature shape (ONE FACT, TWO DOORS): `requireUnreservedName`'s exemption (`Parser.maxon`, `declaresCompilerInternalNames`) lets that one file DECLARE reserved names, and nothing taught the call side. ⛔⛔ **IT ALSO FALSIFIES A1's HEADLINE CLAIM, which said A1 was *"the ONLY thing between shv2 and a loadable `stdlib/Builtins.maxon"*.** A1 was NECESSARY, not sufficient: with A1 landed, `try (digit / fracDiv)` at `:285` now parses and the file's next blocker is this. **Do not re-derive the claim — re-measure it**, which is one command. ⚠ Blocks the S2 whitelist cone through `Builtins.maxon` | L-stdlib *(also `Parser.maxon`'s call resolution — check the parser lanes before claiming)* ✅✅ **CLOSED 2026-07-31** (main `63ff12a72`; x64-windows **2843/0** on the merged tree — 2832→2836 on its own base, +7 from R8 — x64-linux **PASS**, wasm32-wasi **PASS**, arm64 **SKIP — remote, UNVERIFIED**; builds 0 warnings, no exit 101, **ZERO goldens modified**). Bootstrap untouched, so no C# lane owed. ⭐⭐ **THE FIX IS FIVE LINES AND THE ROOT CAUSE IS A BROKEN PROXY: `__`-prefixed had come to MEAN "the compiler owns this callee", and D6 falsified it** by letting one file declare such names. `requireCalleeIsNotReservedName` now admits a callee only when **BOTH** hold — this file owns the reserved space **AND** some file declares that name. ⭐ **The second clause is LOAD-BEARING, not belt-and-braces**: a flag-only door admits every `__` name NOBODY declares — `__mm_free`, `__gt_spawn`, every typo — and measured under the unconditional door, `__mm_free(0)` in the exempt file **COMPILED AND LINKED**. ⭐⭐ **AND THE BROKEN PROXY HAD ALREADY COST THREE SILENT DEFECTS on the declaring file**, all measured: a wrong ARGUMENT COUNT drew **no diagnostic at all** (now E3036); a throwing call with **no `try`** drew none and dropped the error flag (now E3057); and `try` on it was refused as *"this builtin call cannot fail"* (E3055). Five sites that read the NAME test to mean "has no declared signature" now ask the registry (`isSignaturelessCompilerCallee`). ⛔⛔ **THE REVIEW FOUND THE SWEEP WAS INCOMPLETE — THREE MORE READERS WERE LEFT ON THE PREFIX AND TWO WERE REACHABLY WRONG, both invisible to a green 2836-case suite. The rung's THESIS was right and its SWEEP was not, which is the generalize-fixes rule failing at the last step.** (1) `TargetPrinter.isRuntimeFunction` read the prefix on the written premise *"a user function can never collide"* — and one consumer is `X64Backend`'s **GREEN-THREAD STACK-GUARD EXEMPTION**, so a user function wearing the prefix was told never to call `__gt_morestack`: a `__`-named function recursing 4000 deep in an async worker **exits 139, SIGSEGV**; exit 42 with the prefix removed. ⚠ The first fix was wrong the OTHER way and **118 spec tests said so** — provenance alone is a strictly WIDER set that also holds the SYNTHESIZED BUILTIN bodies (`int.compare`, `String.hash`, the `Set`/`CharacterSet` families), which wear no prefix because a witness reloc needs a real `.text` symbol and which a green thread MUST grow frames for. Landed predicate is the **conjunction**, with the 118-test measurement recorded in its header. (2) `lowerFunctionRef`/`noteFnRefTarget` decided from the prefix whether a function VALUE gets its uniform-ABI env thunk; a declared `__` name is an ordinary user function, got the bare address, and callers invoked an `(a,b)->R` through `(a,b,env)->R`: **wasm32-wasi exit 3 `indirect call type mismatch`, while x64 RETURNED THE RIGHT ANSWER BOTH WAYS** (the spurious env word lands in a register the callee never reads — visible only as 1651 vs 1690 code bytes). **wasm is the local oracle for signature shape, again.** ⚠ **The implementer had judged (2) unreachable and its reason was TRUE of the four functions that exist and FALSE of the construct the rung enables** — all four `__*_fromString` throw and E3101 bars a throwing function from being a value, but a NON-throwing `__` declaration reaches it. **A reachability argument scoped to today's callers is not a reachability argument.** (3) That thunk decision was written out at BOTH sites, each comment calling it *"the same discriminator"* — a *these-must-agree* comment is a test nobody wrote, and a disagreement is a compile error at NEITHER end; now one predicate. ⚠ **AN HONEST LIMIT, sabotage-confirmed:** with the `declaresCallee` clause the basename-vs-path distinction is **unreachable from any spec** (a name needing no declaration fails the clause everywhere; a declared one can only be declared at the real stdlib path). Removing the path compare turns the DECLARATION-side case `user-file-named-builtins-is-not-exempt` RED while all three new call-side cases stay GREEN — so the flag is pinned exactly once, on the declaration side, and the new cases pin only that the call door consults it. Not a hole. ⚠ **Findings 1–2 are ALSO unspec-able and that is now a standing instruction in `reserved-double-underscore.md`** — a fragment compiles a throwaway project, so it can never contain the checkout's own `Builtins.maxon`. Both reproductions, both exit codes, and the rule *"sweep every reader of `isCompilerInternalCallee` and run the result on `--target=wasm32-wasi`"* are recorded there; the real pin is the derivation (one predicate, so a future edit cannot move one reader without moving all). **THE DELIVERABLE IS A MEASUREMENT, COORDINATOR-VERIFIED rather than reported:** `stdlib/Builtins.maxon` went from `E3004 :322` to **`E3001: No 'main' function found`** — no language gap left — and with a temporary `main` appended it compiles to a PE and `__byte_fromString("42")` **exits 42**, through the very `__int_fromString` call this rung fixed. **The whole surveyed blocker queue behind it is clear.** Also closed A1i's missing-golden half: **18 x64-linux fragments the repo simply LACKED** were emitted and committed (A1r's 4 + D6's associated-types 14). **Opens `A1s` and `A1t`.** | ✅ DONE | `slice/A1r-reserved-name-call-door` | 2026-07-31T00:13Z |
| **A1s** | **WHITELIST `stdlib/Builtins.maxon` — the last blocker on a LOADABLE reserved-space module, and it needs a RULING before a line is written.** A1r cleared every LANGUAGE gap in that file: it now compiles standalone (only `E3001: No 'main'`), and with a temporary `main` it runs. What remains is making its declarations exist in ORDINARY programs, i.e. one `listWhitelistedModule` line — **which is not one line, because the file declares FIVE `__Managed*Error` enums (`:144` list, `:152` memory, `:164` socket, `:175` file, `:190` directory) and THREE of those are ALSO SYNTHESIZED BY THE COMPILER**: `__ManagedMemoryError`, `__ManagedFileError`, `__ManagedDirectoryError` (`SignatureIndex.maxon` `ManagedMemoryErrorTypeName`/`ManagedFileErrorTypeName`/`ManagedDirectoryErrorTypeName`, registered by R4.1/R4.2/R4.3 through `builtin*ErrorEnum()` + `registry.upsert`). ⛔ **THE RULING NEEDED: which side OWNS those three?** Either the compiler stops synthesizing them and reads the stdlib declaration (which makes the builtin error families depend on a stdlib file being loaded — and `BuiltinErrorOrdinalCheck`'s authority is currently the synthesized `EnumLayout`), or the declarations are removed from `Builtins.maxon` (which breaks the bootstrap and v1, both of which read that file), or the whitelist path deliberately skips them (a third spelling of each enum). ⚠ **Do NOT settle it by seeing what compiles** — R4.4's review found `ArrayError` **SEEDED but never RESERVED**, so a user's own `enum ArrayError` compiled and the runtime's `indexOutOfBounds` routed into the user's first arm: **exit 11, no diagnostic**. A name owned by two authorities is a silent-wrong-answer shape, not an E3006 shape. ⚠ Also budget the ordinary whitelist toll `StdlibLoader.maxon`'s header states: listing a module RESERVES its type names program-wide, and this file declares `ParseError`, `ParsedInt`, `ParsedFloat`, `SourceLineNumber`, `Promise` and four `Initable*` interfaces. ⚠ And `Builtins.maxon` carries string literals ⇒ expect `.rdata` golden churn. **Unblocks the `int.fromString` rewrite** (shv2 has NONE — the only `fromString` hits in `Parser.maxon` are two comments) and behind that `parsable-interface.md`'s **14 disabled cases**. ⚠ The `float.fromString` cases in `ceil`/`floor`/`round`/`sqrt`/`trunc` need MORE than this: the spec harness has no `<!-- Args: -->` handler and `SpecTestRunner` spawns with an empty argv ⇒ L-harness, separate. ⚠⚠ **A1t (CLOSED) LEAVES THIS ROW AN INHERITED OBLIGATION, MEASURED AND NAMED: whitelisting this file is the exact change that DESTROYS ONE OF THE TWO BARS holding back a silent miscompile.** A1t established that the unchecked last-wins index at `StdlibSource.maxon:201` cannot suppress one side of a `__` name collision, for two independent reasons — (1) `unreachable` is built only from whitelisted modules and a `__` name in one of those is `E2051`; (2) this file is not whitelisted, so it can only ever enter as the USER ROOT. **Listing it here deletes bar 1.** Bar 2 alone still holds *for a user root*, but the whole point of A1s is to make these declarations live in ORDINARY programs, where bar 2 does not apply either. ⇒ **Before the `listWhitelistedModule` line goes in, re-derive whether a whitelisted `Builtins.maxon` can have one of its `__` names marked unreachable**; A1t's E4015 is a net at the BACKEND, strictly after that decision, so it does not cover this. Also note this file's own header already carries a **collision rule** every listed module must clear. ⚖⚖ **USER RULING 2026-07-31: THE STDLIB DECLARATION OWNS THEM; THE COMPILER READS IT. Option (a).** ⇒ the compiler stops synthesizing `__ManagedMemoryError` / `__ManagedFileError` / `__ManagedDirectoryError` and resolves them from `Builtins.maxon` instead — **one declaration, one place.** ⚠⚠ **THE HARD PART IS NOT THE DELETION, IT IS RE-ANCHORING THE AUTHORITY: `BuiltinErrorOrdinalCheck`'s authority currently IS the synthesized `EnumLayout`**, so once the layout comes from a parsed stdlib declaration, that check must verify against the parsed enum or it is checking a copy against itself. **Do not let the check end up reading the thing it is supposed to be checking.** ⚠ And it makes the builtin error families **depend on a stdlib file being LOADED** — which is the coupling this option buys, so the failure when the file is absent or unwhitelisted must be a clear diagnostic and not a panic or a silently-empty enum. ⚠ Keep R4.4's precedent in view: `ArrayError` was seeded but never RESERVED, so a user's own `enum ArrayError` compiled and the runtime's `indexOutOfBounds` routed into their first arm — **exit 11, no diagnostic.** Reading the declaration is not enough; the name must still be reserved against a user redeclaring it  ✅✅ **CLOSED 2026-07-31 IN TWO WAVES** (main `1fa251cad`, then `7f0062707`; x64-windows 2908 → **2968/0**, x64-linux PASS, wasm32-wasi PASS, arm64 SKIP — remote, UNVERIFIED; 32 goldens added, **not one pre-existing golden modified**). **WAVE 1** listed the module, moved the three `__Managed*Error` enums onto the stdlib declaration, and paid a debt that came due the moment it was listed. **WAVE 2** built the SCOPING mechanism and enabled `parsable-interface.md`'s **seven** non-`fromString` cases. ⛔⛔ **THE ROW'S PREMISE WAS FALSE TWICE, BOTH TIMES MEASURED RATHER THAN READ.** (1) *"Budget the ordinary whitelist toll"* — the toll does not COST, it BLOCKS: a user `enum ParseError` is E3006 against this module's, and every acceptance case declares one. (2) *"Unblocks `parsable-interface.md`'s 14 cases"* — the whitelist removed one of THREE blockers; the other two (this scoping, and E3016 `throws`-narrowing) were built here, and a THIRD (`<prim>.<method>` → `__<prim>_<method>`) still holds the seven `*fromstring*` cases ⇒ **A1s-prim**. ⭐⭐ **THE SCOPING DESIGN, AND WHY THE OBVIOUS ONE IS A WRONG ANSWER:** the oracle permits a user `ParseError` and this module's simultaneously (measured, exit 42) — that is SCOPING, which the bootstrap gets free from parse ORDER. shv2's whole-program sweep folds every file before any is parsed, so it has no *"already bound"* state; *"the user simply WINS"* was BUILT and raises `E3034` inside `stdlib/Builtins.maxon` itself, which names its own `ParseError` a dozen times. ⇒ the STDLIB declaration is renamed into the reserved space (`type Clock` → `__Clock`), applied to the stdlib file's IDENTIFIER TOKENS once at `Parser.create` — so `structRef`, `aggregateNameFor`, `__destruct_<name>`, `__layout_<name>`, both enum registries and the interface registry follow BY CONSTRUCTION rather than by a roster somebody must keep complete. **E3112** refuses the one shape a token rewrite cannot reach (a stdlib MEMBER spelled like a contested type name). ⚠⚠ **THE BYTE-NEUTRALITY DEBT WAS FILED AND CAME DUE HERE**: `InsertRangeChecks.maxon:167-179` had already measured that the invariant *"holds TODAY only because no listed module contains a literal, which is a property of the three files listed and not of the mechanism"* — `Builtins.maxon` is the first with literals, and the one-line entry moved **317 goldens** (`__fconst` 156 / `__str_blob` 132 / `__jumptable` 29). ONE gate on `project.stdlibFacts.unreachable` at `lowerMaxonToStd`'s walk; sabotage reads 2598/320 without it. The other two pools needed none — DFE runs at `BackendDispatch.maxon:99`, BEFORE target conversion. ⛔ **FOUR DEFECTS THE SUITE WAS GREEN OVER, every one found by PROBING**: the ordinal guard was checking a layout the program did not use (a case swap aborted at exit 134 with the guard SILENT); wave 1 shipped a SILENT ACCEPT — abstract-`throws` narrowed to ANY enum because an enum's `implements` clause was parsed and DISCARDED, so the conjunct was *unaskable*, not merely unasked; a legal program REFUSED (`throws ArrayError`, whose synthesized seed carried an EMPTY `conformsTo` while the declaration it stands in for says `implements Error`); and user code could CALL THE MINT (`__Clock.nowMs()` compiled and ran under a shadow). ⭐ **AND THE REVIEW CAUGHT THE RUNG COMMITTING ITS OWN SIN**: the coordinator's whitelist line spelled `"Builtins.maxon"` as a literal — a second copy of `Queries.maxon:551`'s constant, inside the rung whose thesis is one declaration one place. ⚠ Residuals filed as their own rows: **A1s-prim**, **A1s-throwsbox**, **A1s-rdatapin** | L-stdlib | ✅ DONE | `slice/A1s-builtins-whitelist` | 2026-07-31T17:00Z |
| **A1s-prim** | **THE `<prim>.<method>(args)` → `__<prim>_<method>` REWRITE — the LAST blocker on `parsable-interface.md`'s seven `*fromstring*` cases, and the only one of A1s's three still standing.** Both references perform it (v1 `Parser.maxon:15980-16020`; bootstrap `2-Parser.cs:24263-24276`). shv2 has NONE — measured, the only `fromString` hits in `Parser.maxon` are two comments. Today `try int.fromString(s)` gives ``E2015: `try` must be applied to a call … (got 'int')``, because `int` is a KEYWORD TokenKind with no `parsePrimary` arm, so this is a PARSE-SHAPE rung before it is a lookup one. ✅ **Its target now EXISTS**: A1s listed `stdlib/Builtins.maxon`, so `__int_fromString`/`__float_fromString`/`__bool_fromString`/`__byte_fromString` are declared, callable and present in ordinary programs — verified by the seven parsable cases A1s enabled, which reach `interface Parsable` from that same module. ⚠ The `float.fromString` cases in `ceil`/`floor`/`round`/`sqrt`/`trunc` need MORE than this: the spec harness has no `<!-- Args: -->` handler and `SpecTestRunner` spawns with an empty argv ⇒ **L-harness, a separate row** ✅✅ **CLOSED 2026-07-31** (main `550294de9`; x64-windows 2968 → **2983/0**, x64-linux PASS, wasm32-wasi PASS — `int.fromString` RUNS there, exit 41 — arm64 SKIP, remote, UNVERIFIED; **30 goldens added, not one pre-existing golden modified**; 0 warnings; no exit 101). **All seven `parsable-interface` `*fromstring*` cases are live**, plus `byte-fromstring`, two precedence pins and two diagnostic-noun pins — the file is 23/0. ⭐⭐ **THE ROW SAID ONE DOOR; THERE WERE TWO, AND THE SECOND WAS A WRONG ANSWER ON `main` RATHER THAN A MISSING FEATURE.** `int`/`float`/`bool` are KEYWORD TokenKinds with no `parsePrimary` arm; **`byte` is not a keyword in shv2 at all**, so `byte.fromString(…)` already reached `parseQualifiedCall` and mangled to a callee `"byte.fromString"` that nothing declares. **NEITHER REFERENCE HAS BOTH**: v1 tests three token kinds and its own comment records that `byte` fell out when it lost its keyword; the bootstrap covers four only because it still has `TokenType.Byte`. ⇒ shv2 keys on the primitive **TYPE NAME**, so one arm serves both doors, and `typeBaseTokenAt` resolves the keyword to an identifier-kinded token — the treatment `Self` already gets — so `parseDottedPrimary`, the enum split, the callee mangle and the bound expression serve it **without learning it exists**. ⚠⚠ **THE CALL DOOR IS GATED AND A PASSING TEST GUARDS THE GATE.** `requireCalleeIsNotReservedName` refuses `__`-prefixed callees from user files, pinned by `reserved-double-underscore.user-file-cannot-call-a-reserved-name`. A `CalleeMint` (`written`/`minted`) separates a compiler-minted callee from author-written bytes — **sabotage-verified: disable that clause and all eight `fromString` cases go RED while the user-written refusal stays 16/0 green.** The gate was opened, not weakened. ⛔ **BOTH REFERENCES' FAILURE PATHS ARE UNCOPYABLE, and shv2 diverges per D11c**: the bootstrap leaks the mangled symbol (`E2004: Undefined function '__int_frobnicate'`), and v1 emits **no diagnostic at all** and then PANICS in the backend (`StdOpHelpers.maxon:362`). shv2 refuses at the parser, naming what the author wrote. ⭐ **ONE CALLEE, TWO LEGITIMATE NOUNS, DECIDED BY SITE** — `int.fromString` for a minted call, `__int_fromString` inside `stdlib/Builtins.maxon` where the author really did write it. Both measured. `SemanticCheck.callDiagnosticNoun` is the ONE door for E3004/E3036/E3037/E3038/E3057 and E3104's subject; the decode splits the mangled name and **re-mints the halves through the forward minter, panicking unless they spell it back**, so its premise is a check rather than a comment. ⚠⚠ **THE WEAK SEAM, STATED RATHER THAN PAPERED OVER: the `written` direction CANNOT BE PINNED BY ANY FRAGMENT** — the exemption is an identity compare against the real `<stdlibDir>/Builtins.maxon`, and no spec file can BE that file. A future change keying the noun on the NAME passes every committed case and breaks only the untestable direction. The review hunted for a better gate and reports there is none; the guard is one mint site + the re-mint self-check + the comment. **This is the seam to re-derive before touching `callDiagnosticNoun`.** ⛔ **THE REVIEW FOUND THE RUNG'S OWN NEW COPY**: `<base> . <member> (` was written out at FOUR dispatch sites, none agreeing, including this rung's own — all now ask `memberCallFollows`. Plus a third copy of the field-chain hop walk whose header says a divergence there is a **USE-AFTER-FREE rather than a compile error**. ⭐ **AND A COVERAGE HOLE NOTHING ELSE SAW**: D8 admits a keyword-named PARAMETER, so `function f(float Box)` makes `float.fromString(…)` the identical token shape. The binding correctly wins — by an ordering inside one routine that **nothing pinned**, and the new case was the ONLY one of 2983 that noticed when it was inverted. ⭐ **A PRE-EXISTING BUG FIXED IN PASSING**: `panic()` inside a float-returning function did not compile — `a register-to-register move from rax to xmm0 crosses register files`. `emitDeadReturn` emitted an integer zero because the value is dead; its **register file** is not. Reproduced on `main`, needs no `try`, and the sibling rule was already written one door over in `emitZeroConstOfReturnType`. ⚖ **COORDINATOR RULING: the per-op `CalleeMint` field is ACCEPTED** over an index-keyed artifact map — allocations +1 FLAT at all six rungs, bytes linear-in-call-count (+0.18% at rung 5, ratios 1.93→2.00), **`phase:parse` alone moving, by the whole-program delta to the byte**. The map would trade a measured constant for an index-stability premise, and positional keys are this repo's recurring defect (D12's lossy key, 7b's parallel arrays, A1h's ordinal-for-an-identity). ⚠ Residual: **`A1s-prim-args`** — the 15 `ceil`/`floor`/`round`/`sqrt`/`trunc` `rt-*` cases stay disabled on `CommandLine.args()` + the harness's missing `<!-- Args: -->` directive (L-harness), which this rung re-measured and corrected their markers to name | L-parser-postfix *(`parsePrimary`'s keyword-type arm + the callee rewrite)* | ✅ DONE | `slice/A1s-prim-primitive-static-call` | 2026-07-31T21:17Z |
| **A1s-prim-args** | **THE SPEC HARNESS HAS NO `<!-- Args: -->` HANDLER, AND `SpecTestRunner` SPAWNS WITH AN EMPTY ARGV — so 15 canonical `rt-*` cases cannot run whatever the compiler does.** Re-measured by A1s-prim (2026-07-31) after it removed the *other* blocker those markers had named: `float.fromString` now works, and the markers were corrected to stop naming it. What remains is two things, both in the harness rather than the compiler: `Testing/SpecParser` does not parse the `Args:` directive at all, and `CommandLine.args()` is undeclared in shv2 (`E2015: a member access 'get' on a 'unknown' value`). ⭐ **The 15 are `specs-shv2/{ceil,floor,round,sqrt,trunc}.md`'s `rt-*` cases**, each an exact port of an ACTIVE case in the corresponding `specs/` file, so the coverage is real and merely unreachable. ⚠ **Two blockers, and they are independent** — teaching `SpecParser` the directive still leaves `CommandLine.args()` undeclared, and declaring it still leaves the runner spawning an empty argv. Budget both, or measure which one alone unlocks nothing | **L-harness** *(`Testing/SpecParser.maxon` · `Testing/SpecTestRunner.maxon` · the `CommandLine` surface)* | ⬜ FREE | — | — |
| **A1s-throwsbox** | **⛔⛔ `throws <interface>` ON A PLAIN FUNCTION SILENTLY LEAKS A BOXED ERROR — exit 101 IN BOTH COMPILERS.** Found by the A1s wave-1 implementer, **reproduced by the coordinator on `main`**: a `union BoxedError implements Error` with a `withMessage(msg String)` arm, thrown from `function f(x Code) returns Code throws Error`, caught by `try f(3) otherwise 55` ⇒ shv2 **exit 101**, and `maxon-sharp` **exit 101 with `MM leak: 1 allocation(s) remain`**. Cause: `Parser.emitThrowError` (`Parser.maxon:12419`) computes `errorIsBoxed` from the THROWN VALUE's type and writes a box pointer, while the catch site computes it from the DECLARED CLAUSE and decodes a scalar — so the handler subtracts the error-flag bias from a heap pointer and nobody ever releases the box. Nothing reconciles the two derivations. ⚠ **NOT enabled by A1s**: it needs no interface conformance and no witness dispatch, and A1s's throws-narrowing deliberately REFUSED the boxed case (`error.boxed-throws-under-abstract-requirement`) rather than opening a second route into it. ⚠ **A shared shv2 + bootstrap hole, so it needs the FULL C# suite (2883+) plus codegen neutrality as its gate** — which is exactly why it is a row and not a fix-now. ⚠ A sibling through the same door: **`throws <name that resolves to nothing>` is accepted on any plain function** (`throws Bogus` compiles and RUNS); A1s closed only the conformance door onto it (`error.throws-unresolvable-impl-type-under-abstract-requirement`) | **L-bootstrap** *(`Parser.emitThrowError` + the catch-site decode; `maxon-sharp` too)*  ✅✅ **CLOSED 2026-08-01** (main `ad0208aa0`; C# **3188 → 3198/0**, shv2 **2990 → 3004/0**, x64-linux PASS, wasm32-wasi PASS, arm64 SKIP — remote, UNVERIFIED; 0 warnings; no exit 101). **THE RULE: a function's own `throws` clause, and every `throw` under it, must agree with the enum the CALLER will decode against.** New code **E3113**; the throw-side mismatch reuses **E3059**. ⭐ **THE MECHANISM**: the THROW site stamps boxedness from the THROWN VALUE (`Parser.emitThrowError`), the CATCH site derives it from the DECLARED CLAUSE (`throwsTypeIsBoxedUnion` → `enumOf`), and an interface answers *not boxed* — so a box pointer is decoded as an ordinal, `enrollCaughtBox` never runs, and `emitScopeDrops`' decref never sees it. The oracle has the identical asymmetry (`2-Parser.cs:9910-9932` matches `errorType is IrEnumType` on the declared clause; `throws Error` resolves to an `IrInterfaceType`). ⚖ **RULING — REFUSE, NOT REPAIR, AND FIX BOTH COMPILERS. The corpus decided it, not taste**: `error-handling.md:14` already says *"Error types must be enums that conform to the `Error` interface"*; no prose sanctions an interface-typed clause on a plain function; and the false-reject sweep for that form was **EMPTY** — every `throws Error` in either corpus is an interface REQUIREMENT, policed on `ConformanceCheck`'s wholly separate path. Leaving the oracle accepting what shv2 refuses would have MANUFACTURED a divergence, when the oracle is the one violating the spec's own prose. ⛔⛔ **THE RUNG SHIPPED ITS OWN DEFECT CLASS ONE DOOR OVER, AND THE REVIEW FOUND IT BY PROBING.** Guarding a clause naming NO declared enum left a clause naming a DIFFERENT one open: a boxed union under `throws ScalarError` ⇒ **exit 101** again; `throw ErrB.bOne` under `throws ErrA` ⇒ **a SILENT WRONG ANSWER**, the handler running the `aOne` arm, in BOTH compilers; `throw` with no clause ⇒ the error DISCARDED, exit 0; `default throws <boxed union>` ⇒ exit 101. ⭐ **AND THE RULE ALREADY EXISTED AT THE SIBLING DOOR** — `checkPropagateType`'s E3059 has refused exactly this for `try` since P1.4b; the `throw` door was wired to none of it. Both doors now share ONE comparison (`requireErrorTypeFitsEnclosing`), the door choosing only the SENTENCE and never the verdict. ⛔ **THE BOOTSTRAP HELD THREE COPIES OF THE THROWN-ERROR EMISSION** (`ParseThrow`, the try-block handler's terminal `throws`, `EmitThrowsArmBody`); the fix landed in two and a fifth probe caught the third. Three → one `EmitThrownError`. ⚠ **HALF THE COORDINATOR'S RULING WAS DROPPED, CORRECTLY AND WITH MEASUREMENT.** I ruled the clause must name a declared enum **and conform to `Error`**; the conformance half has a real false-reject set (~30 committed spec programs across both corpora, plus thrown unions inside `maxon-shv2` itself carrying no `implements Error`) — **requiring it would stop shv2 building.** Filed as **A1s-throwsconform** rather than smuggled in. ⇒ *I asserted an empty sweep set for the interface form, which was measured, and extended it to conformance, which was not.* ⚠ **5 corpus programs were WRONG and are corrected, not exempted**: one `throws <a struct>`, and four `default throws` arms inside non-throwing `main`s that measurably leak. ⚖ Small ruling recorded: **`default panic(...)` is the correct spelling for an unreachable arm in a non-throwing function**, `default throws` is not. ## Numbers: **+22 allocations, FLAT at all six rungs**, in `phase:semanticCheck`; bytes WITHHELD by A2l/3's new path guard. ⚠ I first wrote +8 from the implementer's pre-review A/B and the instrument contradicted it — **quote the row's delta, not an A/B taken before the last commit.** ⚠ Residuals filed: **A1s-throwsconform**, **A1s-csgolden**, **A1s-trypropagate** | ✅ DONE | `slice/A1s-throwsbox-boxed-error-leak` | 2026-08-01T01:05Z |
| **A1s-csgolden** | ✅✅ **CLOSED 2026-08-01** by BATCH1 (main `d4cd6aa20`) — **added to the batch at claim time because it IS the batch's gate.** ⭐⭐⭐ **EVERY C#-SUITE FRAGMENT NUMBER IN THIS REPO'S HISTORY WAS UNFALSIFIABLE UNTIL NOW**: the runner rewrote each golden **unconditionally, on every run, pass or fail** (`TestRunner.cs:251`, `:438`) and **never compared** — `Fragment.GeneratedIR` was populated and **read nowhere**, as its own doc comment admitted. Now shv2's three-way logic: absent ⇒ write+pass; `--update-required` ⇒ write+pass; else compare, and a difference is a **test FAILURE** with a first-difference diff. **Acceptance: a plain unfiltered run leaves `git status --short specs/` EMPTY.** ⭐⭐ **THE 141 WERE NOT WHAT THE ROW SAID.** Filed as "register-allocation movement"; **measured** as the x64 half of a released-garbage-pointer fix that `4572c988b` (2026-07-29) applied to **455 arm64-macos goldens and ZERO x64-windows ones**: `x64.call mm_decref` **279 → 107** (−172 spurious releases, zero gained), `x64.xor` 260 → 82, **every other call target identical on both sides**. A real correctness improvement sat invisible for three days *because* the gate that should have shown it was overwriting itself. ⛔ **THE FIRST MINT WAS THROWN AWAY**: it ran while two sibling agents had the machine 4.5× oversubscribed, batched compiles missed their timeouts and fell back to per-fragment compiles, **every test still passed**, and one golden was minted with `mm_alloc` tag indices one lower than the batched compile's. ⇒ **a golden pins the COMPILE, not the run** (`FragmentSource`), and goldens are written only on an **unfiltered, batched** run — a filtered or `--no-batch` run neither compares nor writes, and SAYS so (before this, `--no-batch` would have rewritten ~2483 goldens). ⭐ **The brief's own exemption idea was measured and rejected**: skipping fallback-path fragments would have left **128 tests permanently ungated**. ⭐ **A bug fixed mid-rung**: `ProcessSpecBatch` re-derived batch membership from `Rewrite(...).Batchable` while `BuildBatchSource` ALSO drops a test with a null `RewrittenSource` — a test the two disagreed about would be pinned **without its `// CompiledIR` section** and pass forever. **⛔⛔ A GATE THAT IS NOT A GATE: a full C# `spec-test` REWRITES 141 files under `specs/fragments-x64-windows/` and still reports all-passed.** Reproduced independently on **THREE trees** (A1s-throwsbox's implementer, its reviewer, and the coordinator) — including on a **pristine HEAD worktree with a freshly built bootstrap**, so it is drift on `main`, not something a rung introduced. The churn is register-allocation movement unrelated to any recent rung. ⚠⚠ **A committed golden the suite silently regenerates cannot gate anything**, so every C#-suite FRAGMENT number is suspect until this is fixed — a codegen regression in the bootstrap would be absorbed rather than reported. ⭐ Two mitigating facts, both measured: a rung's OWN new fragments are not rewritten (so new-test numbers are meaningful), and the count is stable at 141 across all three reproductions. ⇒ the rung is to make the C# suite CHECK these fragments as the shv2 runner checks its own, and to mint the 141 once, deliberately, with the diff reviewed | **L-bootstrap** *(`maxon-sharp/Testing/` — the fragment comparison path)* | ▣ **BATCH1** *(added at claim time — it is the batch's own gate)* | — | — |
| **A1s-throwsconform** | **A `throws` CLAUSE MAY NAME A DECLARED ENUM THAT DOES NOT CONFORM TO `Error`.** A1s-throwsbox established that the clause must name a declared enum or union; it deliberately did NOT require `implements Error`, and the reason is measured rather than argued: **~30 committed spec programs across both corpora throw enums with no `implements Error`** (`enum MatchError`, `enum E`, `enum BoxError`, `enum AppError`, …), and `maxon-shv2` itself throws unions that do not conform (`ParserError`, `TupleElementRejection`) — **requiring conformance today would stop shv2 building.** ⚠ `specs-shv2/error-handling.md:14` states the rule the corpus does not keep: *"Error types must be enums that conform to the `Error` interface"* — so this is prose and practice disagreeing, and closing it is a **corpus-wide edit** before it is a compiler change. ⇒ decide whether the prose or the practice is wrong, then make one match the other; the compiler half is one extra clause in the check A1s-throwsbox already built | L-types *(the check in `SemanticCheck` + a corpus sweep)* | ⬜ FREE | — | — |
| **A1s-trypropagate** | **shv2 DOES NOT VERIFY THAT A BARE `try` PROPAGATE AGREES WITH THE RUNTIME CALLEE'S ERROR TYPE.** Measured: `try s.findFirst(" ")` inside `function f() throws SearchFailed` compiles and runs — the String runtime's `StringError.notFound` ordinal is decoded as `SearchFailed.notFound`, **correct here only because both are ordinal 0**. This is A1s-throwsbox's own defect class at the one door it did not reach: that rung guarded a `throw` against the enclosing clause, and this is a PROPAGATE from a RUNTIME callee, whose throws type comes from the runtime rather than from a user declaration. ⚠ **It interacts with a case A1s-throwsbox shipped**: `error.throws-a-stdlib-error-shv2-does-not-declare`'s control (`throws SearchFailed` while propagating a `StringError`) only compiles BECAUSE this check is missing — closing this rung must re-derive that case rather than assume it still holds | L-types *(`Parser.checkPropagateType` · the runtime-callee throws source)* | ⬜ FREE | — | — |
| **A1s-rdatapin** | **A `.rdata` PAYLOAD REGISTERED UNDER A STRUCTURAL LABEL IS INVISIBLE TO EVERY GOLDEN — there is a `RequiredData` gate for `.data` and NOTHING equivalent for `.rdata`.** Found by A1s's byte-neutrality agent while fixing the 317-golden shift, and it is the honest limit of that fix: `registerRdataConstantLabeled` (witness tables, `__layout_*`, UCD) does NOT advance `GlobalDataTable.nextStringId`, so it shifts `.rdata` OFFSETS without moving any label a fragment prints. A1s's gate covers it — **but no golden can SEE it**, so that coverage rests on the fix being right rather than on a check. ⭐ **This is the missing-golden shape one level up: a MISSING golden never fails, and here the golden cannot exist at all.** ⚠ Related and also unpinnable: **E4016 has no spec coverage and structurally cannot** (`lookup_error_code` reports `specFiles: []`) — a fragment cannot edit `stdlib/Builtins.maxon`, so only its FAILURE path is unreachable from the corpus; the check itself runs on all 2968 compiles. `specs-shv2/reserved-double-underscore.md` already documents the same unpinnability for its own positive case. ⇒ the rung is a `.rdata` equivalent of `RequiredData`, which would pin all three at once | L-testing *(`Testing/` + the fragment renderer)* ✅ **CLOSED 2026-08-01** by BATCH8 (main `01cc68dc6`): ` ```RequiredRdata ` is the `.rdata` twin of ` ```RequiredData `, **generalized rather than copied** — one `LinkedSectionKind{globals, readOnly}` axis, one fence table, one block parse, one prefix compare, one message producer; each format contributes only a name. **Section names read off the WRITERS, not guessed**: PE `.rdata` (`PeWriter.maxon:770`), ELF `.rodata` (`ElfWriter.maxon:444`), Mach-O `__DATA,__const` (`MachOWriter.maxon:817`) — and the ELF name is verified by a PASSING x64-linux lane, not by reading. `specs-shv2/rdata-section.md` pins a `__layout_*` descriptor, seen RED first by pinning eight words of `99` against the unchanged compiler, which **PASSED** — the section was invisible and a deliberately false claim was accepted. ⚠⚠ **THIS ROW'S "would pin all three at once" WAS OVER-CLAIMED — it pins ONE of three, and both agents reported that against the flattering row.** **E4016 is structurally out of reach**: it compares a `stdlib/Builtins.maxon` declaration (`project.enumTypes`) against compiler-side literals — **neither operand is a section payload** (coordinator-verified independently), so no ` ```RequiredRdata ` block can perturb either. And **`reserved-double-underscore`'s positive case was ALREADY closed by A1s**, so that third of the claim was simply stale. ⚠ **A prefix pins a prefix**: an APPEND past the last named byte is invisible to this gate — stated in the spec's own prose | ✅ **DONE** *(BATCH8)* | — | — |
| **A1t** | **A DECLARATION IN THE RESERVED-SPACE MODULE CAN SHADOW A COMPILER-EMITTED SYMBOL, and A1r's call door is what makes the collision reachable.** Found by the A1r review, measured POST-fix: `export function __print_string(value String)` in `stdlib/Builtins.maxon` **plus a call to it** — no `print` anywhere in the program — gives `panic at DeadFunctionElimination.maxon:110: indexFunctionsByName: two functions are named '__print_string'`, **with no file and no line**. Same for `__str_eq`. ⛔ **THIS ROW SAID "the DECLARATION alone is clean; it is the CALL that installs" AND THAT IS TRUE ONLY OF *USAGE-GATED* SYMBOLS.** `__module_init` / `__maxon_global_cleanup` install on `globals.count() != 0` alone — **measured**: an UNCALLED `export function __module_init()` plus one managed global collides with no call anywhere in the program. The door A1r opened is one entrance, not the entrance. ⭐⭐ **THE REVIEW DELIBERATELY DID NOT GATE `recordCallUsage`, AND THE REASON IS THE POINT: doing so removes the panic and replaces it with a SILENT MISCOMPILE** — `print("x")` emits `call __print_string`, which would then bind to the user's declaration. **A loud panic is not the defect; converting it to a silent wrong answer would be.** ⇒ the honest cure is a POSITIONED DIAGNOSTIC (*"the reserved-space module may not declare a name the compiler emits"*), which needs **a new error code plus a home for the emitted-name roster** — there is no such roster today, and inventing one is the actual work. ⛔ **THE ROSTER CLAUSE WAS THE ROW'S CENTRAL PREMISE AND IT WAS WRONG — no roster is POSSIBLE, and none is NEEDED.** Impossible: ~19 `install*Runtime` functions, **130+** `module.functions.push` sites, and the dynamic families (`__destruct_<T>`, cloners, builtin-conformance impls) synthesize names from **the program's own type names**, so they are not knowable from `Builtins.maxon`'s text at all. Unnecessary: **the collision never needed PREDICTING because it was already DETECTED** — at `indexFunctionsByName`'s count check, which merely reported it as an internal panic instead of a diagnostic. ⚠ **LATENT, not live:** the four `__*_fromString` functions and the five `__Managed*Error` enums collide with nothing, and the failure is loud and precisely worded when it does fire. ⭐ It is also the SHARPENED form of the coupling risk A1r's own header worried about: not the two registries DISAGREEING, but both AGREEING about a name **the compiler also owns** — a third state neither the door nor `isSignaturelessCompilerCallee` considers. ⚠ Interacts with **A1s**: whitelisting `Builtins.maxon` makes every name it declares live in ordinary programs, which is when a future collision stops being latent | L-stdlib *(also `DeadFunctionElimination.maxon`, `BackendDispatch.maxon`, `IrFunction.maxon`)* | ✅✅ **CLOSED 2026-07-31** (main `d44bf168e`; x64-windows **2901/0** — UNCHANGED, and that is correct: the collision is **NOT SPEC-TESTABLE**, because a spec compiles a throwaway project which can never contain the checkout's own `stdlib/Builtins.maxon`, exactly as `specs-shv2/reserved-double-underscore.md:223-232` already stated. x64-linux **2769/0**, wasm32-wasi **2757/0**, arm64 **SKIP — remote, UNVERIFIED**; builds 0 warnings; no exit 101; **ZERO goldens modified**; bootstrap untouched). ⭐⭐ **THE DISCRIMINATOR ALREADY EXISTED AND IT IS PROVENANCE.** The panic conflated THREE causes and named only two, both false in this case. Synthesized functions carry an EMPTY `sourceFilePath`, parsed ones a real one ⇒ one-real-one-blank is a USER error (**new E4015** — `IrDeclarationTakesCompilerOwnedName` *(it landed here as `IrDeclarationCollidesWithEmittedSymbol`; **A1w widened the rule to the NAME and renamed it**, so this row's original spelling is history, not the current code)*, positioned at the author's declaration, exit 1, **no binary written**), while both-blank (an installer ran twice) and both-parsed stay a **PANIC**. ⭐ **THE CONTROL WAS THE POINT, NOT THE FEATURE**: a fix turning every duplicate into a user diagnostic would hide real compiler bugs and would pass any test written only against the collision case — so both panic causes were sabotage-proven still panicking, independently, by implementer AND reviewer. ⭐⭐ **THE REVIEW FIXED A DEFECT THIS RUNG ITSELF DOUBLED THE BLAST RADIUS OF**: `IrFunction.create` HARDCODED an empty `sourceFilePath` and left all three callers to overwrite it two lines later — the provenance invariant was **CONVENTION, not construction**. A fourth caller that forgot would make a parsed function read as synthesized at **two** readers: this new check (E4015 silently degrades back to a panic) and `TargetPrinter.isRuntimeFunction`, where it is the green-thread stack-guard **SIGSEGV the A1r review measured**. Cured by DERIVE: `create` now takes a **required** `SourcePosition` (three fields as one argument, so a caller cannot supply a path and forget the line), and `SourcePosition.synthesized()` makes the compiler's own claim positive and greppable. ⚠ **A HOLE THE COORDINATOR RAISED DID NOT REPRODUCE, AND THE STRUCTURAL REASON IS NOW WRITTEN BESIDE THE CHECK**: could the UNCHECKED last-wins index at `StdlibSource.maxon:201` mark the runtime's own `__foo` unreachable, leaving ONE function of that name, so the count check never trips and the compile succeeds with every emitted call bound to the user's stub — the silent miscompile A1r ruled worse than the panic, reached by a different door? **Two independent bars, either sufficient:** (1) `unreachable` is built only from WHITELISTED stdlib modules, and a `__` name declared in one of those is `E2051` — measured; (2) `Builtins.maxon` is **not whitelisted**, so its only route into a compile is as the USER ROOT, which `registeredPathsWhenTheRootIsInsideStdlib` keeps out of `stdlibSourcePaths`. ⚠⚠ **BAR 1 DIES THE DAY `Builtins.maxon` IS WHITELISTED — i.e. the moment row A1s lands — leaving only bar 2.** ⚠ The reviewer's first probe produced **a clean build that printed normally**, which looked exactly like the hole; it was the whitelist, not a suppression. ⭐ Ladder: allocations **DOWN**, attributed by a same-path A/B to `phase:parse` (−151…−2,414, one saved allocation per parsed function — `create` no longer builds and discards a `FilePath from ""`) plus `runtimeAugment` **+1 flat**; a residual **+45 + 16·2ⁿ outside the phase brackets is UNATTRIBUTED and logged as unexplained, not as noise** | `slice/A1t-reserved-name-shadow` | 2026-07-31T05:56Z |
| **A1w** | **FOUR NAME→FUNCTION INDEXES OVER ONE MODULE, THREE TIE-BREAK POLICIES, ONE CHECKED — and A1t proved their SHARED PREMISE conditional while teaching only one of them.** All four build a `name → function` map over the same module and all four rest on *"no two functions here share a name"*: `DeadFunctionElimination.maxon:105` (last-wins, **now refuses with E4015 / panics**), `SemanticCheck.maxon:1062` (**FIRST**-wins), `StdlibSource.maxon:201` (last-wins, unchecked), `ConformanceCheck.maxon:188` (last-wins, unchecked). ⭐ **This is the project's signature shape at the level of an INVARIANT rather than a constant: one fact, four readers, and the reader that learned it is the only one that can say so.** ✅ **BOUNDED, NOT URGENT — none of the three unchecked ones is a wrong answer today**, and the bound is A1t's two bars plus one structural fact: `StdlibSource` cannot see a `__` collision at all (bars 1 and 2), `SemanticCheck` and `ConformanceCheck` sit strictly BEFORE the refusal and **emit no code**, and `ConformanceCheck`'s map is keyed by the REGISTRATION name (`Type.method`), which a bare `__foo` is never looked up under. ⇒ **hygiene with a live trap in it, not a defect.** ⛔ **The trap: `SemanticCheck.maxon:1057`'s comment asserts a duplicate there belongs to an "ALREADY-REFUSED" program, and that was WRONG BEFORE A1t AND IS STILL WRONG** — the refusal is in the backend, strictly after that pass, and `installManagedGlobalInit` appends into the same merged Maxon module before the summary is built. First-wins remains correct, but for a NARROWER reason that rests on no refusal at all (nothing between there and the backend emits code); the review rewrote it to say so. **A reader who trusts the old wording concludes "nothing upstream of the backend can see a duplicate" — which is exactly the premise A1t proved worth checking.** ⚠ Whoever takes this should decide the policy ONCE and derive the four sites from it, not add three more checks; and note that A1s changes the answer for `StdlibSource` — ⚠ **A1s HAS NOW LANDED (main), so that is no longer a future condition: re-measure `StdlibSource`'s reachability against the CURRENT whitelist before trusting anything this row says about it** | L-ir | ✅✅ **CLOSED 2026-07-31** (main `436322bf7`; x64-windows **2952/0**, x64-linux **2831/0**, wasm32-wasi **2796/0** — all at BASELINE, correctly, since the change is behaviour-neutral; **zero golden churn**; 0 warnings; `error-codes: OK — 158 codes`; `flat-namespace: OK — 599 type + 558 value`; no exit 101; **`maxon-sharp/` untouched (0 diff lines) ⇒ the C# suite is a SKIP, not a pass** — `ErrorCode.g.cs` holds only codes csharp claims, so an shv2-only rename could not touch it; arm64 SKIP, nothing owed — the one target file in the diff is comment-only). ⭐⭐ **THE POLICY, IN ONE PLACE: A NAME→FUNCTION INDEX HAS NO TIE-BREAK, BECAUSE IT REFUSES A TIE.** New `IR/FunctionNameIndex.maxon` — one builder, one classifier, four call sites; **the three surviving tie-breaks are DELETED, not made uniform** (three checks agreeing today would be this defect one level up), and `FuncIndexByName`/`ModuleFuncIndexByName`/`StdlibFuncIndexByName`/`FunctionByNameMap` collapse into one alias — verified to have been **the same type all along**, so the "distinction" was documentation and bundling the array *with* the map is the first real guard. ⭐ **AND THE FOUR SITES ARE NOT THE SAME QUESTION — established per site by measurement, not assumed**: `ConformanceCheck` can see **no** duplicate at all (two bars, the second verified *exhaustively* — the only writers of a Maxon module's function array are two `Parser` sites and `ModuleInit:276`, whose sole entry sits **below** `checkConformance`) yet uses the shared builder anyway, because a fourth spelling of the loop costs more than one integer compare; `StdlibSource` is the earliest that can meet a real pair; `SemanticCheck` sees only that pair; `DeadFunctionElimination` is the only one that can see A1t's. ⚠ **THE ROW'S "A1s CHANGES THE ANSWER" WAS STALE AND THE RE-MEASURE MATTERED**: A1s had landed, `Builtins.maxon` **is** now whitelisted, and that is exactly why `StdlibSource`'s index became the earliest that can meet a collision. ⛔ **A FIFTH SITE THE ROW DID NOT KNOW ABOUT, FOUND AND FIXED**: `seedRoots` rooted four names on the premise that whatever answers to them is the COMPILER's function, unchecked — `export function __mm_leak_check()` in `Builtins.maxon` plus a heapless program gave **a panic with no file and no line**, A1t's exact defect through a door with **no pair to count**. Now a positioned refusal via `markCompilerOwnedRoot`, and the rule is about the **NAME** rather than which runtime floor a program carries. **E4015 was RENAMED and WIDENED (`IrDeclarationCollidesWithEmittedSymbol` → `IrDeclarationTakesCompilerOwnedName`) rather than joined by a second code — one rule, one number.** ⚠⚠ **THE COORDINATOR'S OWN REVIEW PREMISE WAS FALSE AND THE REVIEW REFUTED IT WITH A TABLE**: I suspected the widening had left its noun behind, because the message I measured still said *"collides with a symbol the compiler EMITS into this program"*. **The two doors already carry two different sentences** — the root door says *"takes a name the compiler owns … installs no runtime under it"* — and my reproducer was not heapless: I had compiled `stdlib/Builtins.maxon` **as the root**, which brings the whole file's heap use with it. Each sentence is true at its door **structurally**: the index refuses duplicates at `:73`, *before* `seedRoots` at `:80`, so at the root door exactly one function of that name exists and a parsed one means the compiler installed none. ⚠ **One claim IS stronger than its code, and the review said so rather than letting it pass**: `refuseDuplicateFunctionName`'s both-parsed arm reads `projectHasErrors`, and replacing that arm with a panic showed **it never fires** — `PassPipeline.run` gates after every pass, so with E3006 reported the throw lands after `resolveTypes` and `semanticCheck` never runs. **It is defence-in-depth, not a live path**, and its header reads as necessity where it is insurance. ✅ Spec impossibility **confirmed, not accepted**: `declaresCompilerInternalNames` is a resolved-path identity compare, and the suite already pins it with fragment files *named* `Builtins.maxon` that still get E2051 ⇒ both E4015 doors are **sabotage measurements**, recorded as such beside A1r's and A1t's. ⚠ Neither E4015 path leaks (exit 1, never 101) — and no suite can see that, since both are un-spec-able. ⭐ Ladder **+3 allocations / +48 bytes FLAT at all six rungs**, fully attributed (one 16-byte record per index, three per compile) — and the raw delta against the logged row *looked like* a new per-function quadratic (+807…+25,617, doubling perfectly) until the parent was measured at the same path (+804…+25,614). **See `A2l` item 3** | `slice/A1w-one-name-index` | 2026-07-31T20:26Z |
| **A2l** | **THREE FINDINGS FROM A1w's REVIEW, ONE OF WHICH IS A CLASS AND NOT AN INSTANCE.** **(1) ⭐⭐ "EVERY NAME-KEYED MAP DERIVED FROM A MODULE'S FUNCTION ARRAY SHARES ONE STATED GUARANTEE" — the rung A1w's four sites were only the first quarter of.** `StdToWasm.buildCalleeFuncIdxMap` (`Targets/Wasm/StdToWasm.maxon:499-506`) is name → module-function **POSITION**, built with a silent last-wins `upsert` and no check, **deciding which function a wasm `call` binds to** — A1w's exact class. ✅ **Not reachable today**: `eliminateDeadFunctions` runs at `BackendDispatch.maxon:100`, before the target `match` at `:103`, and the wasm arm appends nothing to `stdModule.functions`. ⚠ **But its site says nothing about that bar** — which is the whole shape. **Eight siblings one class down** (name → a derived *property* rather than a position): `StdToWasm.maxon:510`, `:521`; `Targets/Shared/StdLoweringShared.maxon:179`, `:192`; `SemanticCheck.maxon:599`, `:625`; `LowerMaxonToStd.maxon:1034`; `TypeResolution.maxon:202`. **Plus the Target-tier twins** `X64Backend.maxon:1778` / `Arm64Backend.maxon:1364` (`labelOffsets.upsert(chunk.funcName, …)`) — **the key space A1j's per-lane chunk-list bug lived in.** All safe today by upstream refusals **none of them mention**. ⛔ **Fixing one would be a ninth spelling, not a cure** — take them as one rung with one stated guarantee, or not at all. *(Cheap interim if wanted: the same one-integer compare at `StdToWasm.maxon:499`.)* **(2)** `Runtime/BuiltinConformanceRuntime.maxon:253` `moduleDefinesFunction` is a **linear name scan over `StdModule.functions` driven once per candidate impl** ⇒ O(impls × functions), the product shape A1h found and fixed one pass over. Measured-linear today, **not** a wrong answer ⇒ Workstream O measured debt, with A1h's trigger: a program with many conformances **and** many functions. **(3) ⚠⚠ `docs/optimization-log.md`'s ABSOLUTES ARE NOT COMPARABLE ACROSS CHECKOUTS, and every agent rediscovers it.** Each row is minted at whatever path its author worked in, so **any** rung's raw delta carries an ~**800·2ⁿ** artifact that reads convincingly as a per-function regression — measured three separate times this session, once as a *sign reversal*. A same-path A/B recovers the truth but costs two extra builds **per rung, every rung**. ⇒ **worth a MECHANISM** (record the minting path in the row, or normalise), not a lesson relearned; the instrument that cannot be compared across the checkouts it is run from is under-specified, not merely awkward. ⚖ **COORDINATOR SCOPING 2026-07-31: THIS CLAIM COVERS ITEM (3) ONLY — the measurement mechanism. Items (1) and (2) stay OPEN on this row** and are not this rung's, because (1) is nine sites plus the Target twins and needs one stated guarantee across all of them, and (2) is measured-linear debt with a re-measure trigger. ⭐ **Item (3) is the pick because it is now confirmed INDEPENDENTLY by two agents in one session**: A1w measured it (+807…+25,617 vs +804…+25,614 for the parent at one path) and a parallel slice landed *"correct A1s wave 2's row — the delta column is CONTAMINATED, not flat"* from an unrelated rung. **Four sightings, two agents, one of them a sign reversal.** An instrument whose rows cannot be compared across the checkouts they were minted in is under-specified, and every agent currently pays two extra builds to recover the truth — or, worse, does not, and reads an ~800·2ⁿ artifact as a per-function regression ⚖ **ITEM 3 IS CLOSED 2026-07-31** (`slice/A2l-ladder-path`; x64-windows **2968/0**, x64-linux **2847/0**, wasm32-wasi **2812/0**, all at BASELINE; **zero golden churn**; 0 warnings; `error-codes: OK — 159 codes`; `flat-namespace: OK — 601 type + 561 value`; no exit 101; `maxon-sharp/` untouched ⇒ the C# suite is a SKIP). ⭐⭐ **THREE PREMISES CORRECTED BY MEASUREMENT — one of this row's, one of my own, and one the tree had written down wrongly for months.** **(i) THE ALLOCATION COLUMN IS PATH-INDEPENDENT, at every rung, in five independent A/Bs.** This row's *"+192, identical at all six rungs"* is **WRONG**, and **A1t's reading that the allocation column is path-independent was RIGHT** — it was 'corrected' by a later sighting that conflated the two columns. A longer path makes the same `String`s BIGGER, not more of them. *(Stated as EMPIRICAL, not structural: a path built through a doubling `StringBuilder` would cross a capacity boundary and add one.)* ⚠ **The memory note `feedback_ab_same_path_allocations.md` still asserts +192 to the digit for exactly the `maxon` vs `.claude/worktrees/<name>` pair; four measurements today read 0.** **(ii) THE BYTE COLUMN IS CONTAMINATED BY *TWO* PATHS, NOT ONE — and keying on either alone licenses the very fiction the rung exists to refuse.** The compiler's own executable path (every stdlib source path is derived from it): measured **+92,576 → +407,660 bytes / 0 allocations** across two real checkouts 44 characters apart. **AND the WORKING DIRECTORY**: measured **+34,515 → +203,310 bytes / 0 allocations** for ONE binary at ONE executable path run from two directories 44 characters apart. **The first implementation keyed on the executable alone, answered `comparable`, and printed +203,310 at rung 5 as a fact about the compiler** — found in review, by the one experiment the implementer had not run. **(iii) `CorpusRelativeRoot`'s comment claimed a relative rung path keeps the checkout out of the byte column, and it does NOT** — `Queries.isStdlibSourceFile` resolves each source path against `Directory.currentPath()` **per file, per compile**. The argv copy is relative; the resolved one is not. My own new prose repeated the false claim before it was measured. ⛔ **AND THE FIRST A/B I RAN MEASURED NOTHING: A JUNCTION IS NOT A DIFFERENT PATH.** Windows resolves the link before a process sees its own executable path, so two junctions of different lengths onto one tree read a perfect zero and looked like *proof of path-independence*. The control that caught it was renaming the exe IN PLACE (+45 filename characters ⇒ +45 bytes, 0 allocations). **Anyone re-measuring this needs real directories.** ✅ **THE MECHANISM: every row carries a `minted at` cell** — executable path **and** absolute corpus root, composed by ONE `mintedAtCell` so the string written and the string compared cannot be assembled differently, written identically into all three tables and cross-checked between them. `byteComparability` decides once per run; the byte-delta column prints `withheld` and names both cells; `--result-json` OMITS `bytesDelta` (the rule it already followed for a rung with no past) and carries `bytesComparable`/`bytesWithheld`. The ALLOCATION delta is always printed. **Nothing is normalised** — a per-character correction would be a number no run produced — and **no verdict is added**: exit stays **0** on a path mismatch, and what is refused is one SUBTRACTION. The 580 pre-existing rows are backfilled `*(unrecorded)*` across all three tables: absolutes and allocation deltas untouched, only the licence to subtract two byte cells minted who-knows-where withdrawn. ⭐ **The review also closed three defects the rung introduced or left**: *"did the memory move"* was decided **twice** in two files and the copies already disagreed (a run climbing past the logged ladder printed *"A number here MOVED"* over a table of zeroes); the **CPU table was written and never read**, so the claim that provenance is cross-checked across all three tables was true of **two**; and the leading-cell indices, the `LeadingColumns` count and the document's own header row were three facts nothing tied together across a 2→3 migration. Proven by **opposite verdicts on one pair of runs** in both experiments, plus **six** guards seen RED by sabotage. ⚠⚠ **TWO FINDINGS FOR THE COORDINATOR, NEITHER FIXED BY THIS AND NEITHER MINE TO DECIDE** — see the report: **(a) A LOGGED ROW IS NOT REPRODUCIBLE FROM THE COMMIT IT CLAIMS.** A1w's row, rebuilt from A1w's own tip in a clean worktree, with the copied bootstrap **and** with a freshly-built one, misses its own numbers by **+262…+295 ALLOCATIONS** — the column no path can touch — so the row is anchored to no tree state at all. A `--note` records whatever is in the working directory, dirty or not, and the log records neither the commit nor `sourceBytes`. **This is a SECOND incomparability, of the same family, that `minted at` does not cure.** **(b) THE BYTES TABLE HAS ONE ORPHAN ROW** the allocations table lacks (`2026-07-18`, *"P1.4b WAVE 2a … OPTIMIZATION PASS"*): 251 rows against 250, diverging at row 60, so from that date the k-th row of the two tables is a **different run** when a human reads them downwards — and the last-row check passes, because a single insertion re-aligns at the end. Repairing it means **deleting a measured row or fabricating one**, and a suffix-alignment check would make every `scale-test` a BROKEN RUN until it is reconciled. **(c) `.claude/CLAUDE.md` states *"Allocations and bytes are exact and bit-for-bit reproducible: ANY movement is real"*, now measurably false for bytes across checkouts — that file is not mine to edit.** ✅✅ **ITEMS 1 AND 2 CLOSED 2026-08-01** (BATCH4, main `38397b45c`) ⇒ **THE WHOLE ROW IS NOW DONE.** ⭐⭐ **THE GUARANTEE WAS NEVER MISSING — IT WAS WRITTEN DOWN ONCE AND TEN SITES WERE NOT STANDING BEHIND IT.** `IR/FunctionNameIndex.maxon` is the tree's ONLY duplicate-name check (`refuseDuplicateFunctionName`) and already had four callers; the ten filed sites each hand-rolled their own `Map with (ByteArray, …)` beside it. **The cure is the ARGUMENT TYPE**: those builders now take the checked index instead of the module, so one cannot be called without the check having run — and a derived sweep for "a name-keyed map still built from a raw module walk" now returns **nothing**. Four maps were **DELETED** outright (they stored what the index already resolves), three kept as genuine folds, one Maxon-tier index in `PassPipeline.run` and one Std-tier index **returned by `eliminateDeadFunctions`** (the prune is what makes an earlier one stale) threaded into all three backends. Item 2's 11 full rescans became one walk plus a ≤11-entry set. ⛔⛔ **AND THE CLASS TURNED OUT TO CONTAIN A REACHABLE SILENT MISCOMPILE — the row said "not reachable today", and that was TRUE of the sites it listed and FALSE of the class.** `function mrt_runtime_init()` in a plain user program **compiled, linked and exited 127**: the user body went into `.text`, the hand-assembled runtime chunk of the same name was laid after it, `labelOffsets.upsert` last-wins overwrote the label, and the program's call to its own function reached the runtime instead. `mrt_start` was merely LOUDER (a compiler panic). Both are now **E4015** through a shared `BackendDispatch.recordChunkLabel` (x64 and arm64, one implementation), and the review **re-verified it by removing the check and reproducing exit 127**. ⚖ **A DELIBERATE DIVERGENCE, RECORDED: the bootstrap compiles that same program correctly** (its runtime symbols do not share shv2's `.text` label namespace), so shv2 now refuses a program the oracle accepts. Chosen because it matches shv2's own established rule for compiler-emitted names (A1t/A1w, same code) and because a **silent wrong answer is the alternative**; the diagnostic is target-scoped (wasm has no such chunk) and a **negative control** (`mrt_not_a_chunk`, spelled like its neighbours, claimed by no chunk) keeps the rule from degenerating into "reserve the `mrt_` prefix". The other direction — namespacing shv2's `mrt_*` chunks so no collision can exist — is a larger rung touching hand-assembled asm and goldens, and is the alternative anyone revisiting this should weigh. ⚠ Structurally, `FunctionNameIndex` **cannot** cover it: the entry stub, the `mrt_*`/`__gt_*` chunks and `__symtable` are minted at the **Target** tier and are in no `functions` array ⇒ noticed at LAYOUT, which is A1t's own lesson one tier down. ⚠ `TypeResolution`'s `funcReturnTypes` was found **not to be in this class** and now says so, pointing at `ParseStaging.commitFuncSignatures` (E3006) as the owner of its duplicates | L-ir *(and `Targets/` · `docs/`)* | ✅ DONE | `slice/A2l-ladder-path` → `slice/BATCH4-instrument-hygiene` | 2026-07-31T22:12Z |
| **A1v** | **A THROWING BACKEND STAGE LEAVES AN UNCLOSED TRACE SPAN.** `PhaseProbe.begin` emits a trace-span BEGIN whose END is emitted only by `recordPhaseDelta`, so any stage that throws before completing leaves the span open in `--log` traces. **Pre-existing** (E5001's backend throw already had it); A1t added a second instance by making E4015 a second throwing exit from `buildBackend`. LOW — it degrades a debugging aid, never a compile. ⚠ Worth fixing as a `defer`-shaped close rather than by adding an END at each new throw site, which is the same one-fact-written-N-times shape that produced this row's neighbours. ✅✅ **CLOSED 2026-08-01** (BATCH4, main `38397b45c`). **SHOWN BEFORE IT WAS FIXED**, in a real trace: `log_phase_begin load` with no matching end, and the pair present after. Cured as the row asked — **a span stack drained once at the compile boundary**, not an END per throw site: `compile` splits into a thin boundary + `compileMeasured`, the error is RE-THROWN UNCHANGED (so `Main` keeps its three distinct file/IO messages), and the pop is CHECKED so a non-nesting bracket panics rather than silently mis-attributing events. There were **four** throwing stages inside brackets, and the fourth was added by this very batch — which is the argument for the structural cure in one sentence. ⚠ **ACCEPTED DEBT WITH AN EXPIRY, found by the review, NOT a residual:** `Queries.maxon:51` and `:522` return on a lex/parse failure before `recordPhase`, leaking a span. Harmless **today** — the leaked span sits at the BOTTOM of the stack, every later bracket pushes and pops above it, and the drain closes it; verified that **no `PhaseProbe` bracket transitively encloses a query call** (all 32 sites checked). ⛔ **THE EXPIRY: the moment any probe bracket comes to enclose a query, that leak turns the mismatch check into a COMPILER PANIC on a plain lex/parse error.** Whoever puts a query inside a bracket owns closing those two | L-ir | ✅ DONE | `slice/BATCH4-instrument-hygiene` | 2026-08-01T04:08Z |
| **A1x** | **⛔⛔ `i64.min / -1` ESCAPES A1's CHECKED DIVISION ENTIRELY — `try … otherwise` DOES NOT CATCH IT, AND THE PROGRAM DIES.** Surfaced by the A1j review, then **reproduced by the coordinator on `main`**: `function divide(a Integer, by Integer) returns Integer` whose body is `return try (a / by) otherwise 0`, called as `divide(i64.min as Integer, by: 0 - 1)`, prints `before` and then **dies with `panic: integer overflow`, exit 1 — the `otherwise 0` never runs.** ⭐ **THE SHAPE: `idiv` raises `#DE` for TWO different reasons and A1 guarded only one of them.** A1 made a zero divisor a catchable `DivisionByZero` throw with a branchless guard; the other `#DE` case — signed overflow, `i64.min / -1`, whose true quotient is not representable — reaches the bare `idiv` and becomes a HARDWARE FAULT, which no `try` can intercept because it is not a throw at all. **So a checked division is catchable for one input and fatal for another, with nothing in the type system or the diagnostic saying which.** ⚠ **This is a WRONG ANSWER, not a missing feature**: the author wrote a total function with a fallback and got a process death. ⚠⚠ **And A1's row claims the guard makes division fallible — that claim is now known to be PARTIAL.** Correct the A1 row when this lands, not just the code. ⭐ Cross-target: the fault message names a DIFFERENT CAUSE per lane — x64-windows says `panic: integer overflow`, x64-linux says `panic: integer divide by zero` — which is **fully documented and measured** (`X64Runtime.maxon:1133-1144`, `specs-shv2/safety.md:65-76`): x86 reports `FPE_INTDIV` for every `#DE`, so on Linux there is genuinely nothing to branch on. **That divergence is NOT the defect and must not be "fixed" by inventing a distinction the hardware does not report** — the defect is that the case is uncatchable at all. ⇒ the cure belongs in the division lowering (`lowerCheckedDivision`, Maxon→Std), extending the existing branchless guard to the overflow pair so it throws like its twin; it is **not** a panic-runtime change, which is why A1j filed it instead of fixing it. ⛔⛔ **CLAIMED AND RELEASED 2026-07-31T09:36Z WITHOUT CODE — the row as filed was HALF WRONG, and the measurements below replace it. IT NEEDS A LANGUAGE RULING BEFORE A LINE IS WRITTEN.** ⭐ **THE `/` HALF IS NOT A DEFECT — IT IS A DOCUMENTED, DELIBERATE DECISION, and I missed that when filing.** `specs-shv2/division.md:48-57` already states it in full: *"`INT_MIN / -1` IS STILL UNGUARDED … NEITHER reference compiler handles it … `DivisionByZero` is about the DIVISOR being zero and says nothing about the quotient being unrepresentable"*, and A1g deliberately made the fault DIAGNOSED (`STATUS_INTEGER_OVERFLOW` 0xC0000095 ⇒ `panic: integer overflow`, vs 0xC0000094 for the zero divisor). A quotient that does not exist has no value to return, so a fault is defensible. **Do not "fix" this half without a ruling.** ⭐⭐ **THE REAL DEFECT IS `mod`, IT IS UNDOCUMENTED, AND ITS ANSWER EXISTS: `i64.min mod -1` IS 0.** The spec's whole rationale is *"the quotient is unrepresentable"* — which is FALSE of the remainder. `mod` dies only because x86 computes quotient and remainder in ONE `idiv`, so it inherits a fault that has nothing to do with it. **MEASURED across every local target, same program:** x64-windows dies `panic: integer overflow` (exit 1); x64-linux dies `panic: integer divide by zero` (exit 1, wrong cause — its kernel reports `FPE_INTDIV` for every `#DE`, documented and not itself a defect); **wasm32-wasi returns `0` and exits 0.** ⇒ **a valid program's OBSERVABLE ANSWER differs by target** — not a diagnostic difference, a wrong answer on two lanes out of three. ⚠ **`try (a mod by) otherwise …` does NOT catch it** on either x64 lane: it is a hardware fault, not a throw, so the fallback never runs and the process dies. ⚠ **THE BOOTSTRAP ORACLE HAS THE IDENTICAL DEFECT** (measured: `panic: integer overflow`, exit 1, `otherwise` skipped) ⇒ **this is a LANGUAGE-LEVEL gap in both implementations, not an shv2 divergence**, and it is also a bootstrap-oracle bug. The oracle therefore CANNOT settle what the answer should be — it has the bug — and `/specs` is silent on `mod` at the boundary. ⛔ **THE RULING NEEDED: what is `i64.min mod -1`?** (a) **`0`** — mathematically correct, what wasm already returns, and what Go and Java mandate; costs a guard on the `mod` lowering only. (b) **a catchable throw** — but `DivisionByZero` would be a FALSE NOUN (the divisor is −1), so it needs a new builtin error, which touches the error-enum ordinal machinery. (c) **the documented fault, made deliberate and PINNED on every lane** — which means changing wasm to fault too, i.e. making the one correct lane wrong for consistency. ⚠ Rust chooses (b)-ish (it treats it as overflow); Go and Java choose (a). **The wider world genuinely disagrees, which is why this is not mine to pick.** ⇒ whoever takes this row must also decide whether `mod`'s answer implies anything for `/`, and must correct **`specs-shv2/division.md`**, whose rationale silently covers `mod` under a claim that is only true of `/`. ⚖⚖ **USER RULING 2026-07-31: `i64.min mod -1` IS `0`. Option (a).** ⇒ guard the `mod` lowering so the remainder is produced rather than faulted, on **every** target — which makes x64 agree with what wasm already returns, and with Go and Java. **The `/` half is UNCHANGED and stays a documented fault** (there is no representable quotient to return), so this row must not touch it. ⚠ **Two consequences to carry:** the fix is in the *shared* `idiv` lowering, so it must not re-break `/`; and **`try (a mod by) otherwise …` becomes dead weight for this case rather than newly working** — once `mod` returns 0 there is nothing to catch, so any spec case written as if the `otherwise` fires would be wrong. ⚠ **The bootstrap has the identical defect**, so it is also a **bootstrap-oracle bug** and the two compilers must end up agreeing; whoever takes this decides whether the oracle moves first or second and says so. **A spec case pinning `i64.min mod -1 == 0` on all four lanes is the deliverable** — it is currently pinned nowhere, and wasm passes it only by accident of not using `idiv`. ⭐ **A DESIGN INSIGHT THE IMPLEMENTER SHOULD START FROM, because it makes the guard cheaper than the `/` case: `a mod -1 == 0` FOR EVERY `a`** (truncated division: `a - (-1)·trunc(a/-1) = a - a = 0`). **So `mod` needs to test only the DIVISOR, never the dividend** — unlike `/`, where `a / -1` overflows solely at `a == i64.min` and therefore needs both. That should compose with A1's existing branchless zero-divisor guard rather than adding a second branch. ⛔⛔ **MY STATED SITE WAS WRONG, AND THE IMPLEMENTER STOPPED AND MEASURED BEFORE WRITING A LINE: `lowerCheckedDivision` IS ENTERED ONLY FOR A POSSIBLY-ZERO DIVISOR, AND "PROVEN NON-ZERO" IS NOT "PROVEN NOT −1".** There are **THREE doors** and that function is one of them; all three die `panic: integer overflow`, exit 1: (1) an opaque divisor ⇒ `__checked_mod` ⇒ `lowerCheckedDivision`; (2) **a LITERAL `-1` divisor** ⇒ `neverZero` ⇒ the **bare `StdOp.mod`** arm; (3) a ranged param `int(i64.min to -1)` ⇒ `neverZero` (the range excludes 0 and **includes −1**) ⇒ bare `StdOp.mod`. **Door 2 is the ORDINARY spelling of the expression the ruling is about**, so a fix confined to my stated site would have shipped a green rung over a live process death. ✅ Good news measured with it: **`lowerBinOp`'s `mod` arm is the single SINK for all three doors**, and nothing folds (`FoldConstOperands` *deliberately* defers `foldConstants` so fragments still show codegen), so there is no fold to lean on and none may be added without reversing that decision. ⚖ **COORDINATOR DECISION 2026-07-31 — widen the DIVISOR PROOF, not the sink; `Parser.maxon` IS IN SCOPE.** `Parser.divisorZeroVerdict` is already the one place that answers *"what can this divisor be?"* from `constIsKnown`/`constOf` + `declaredAliasOfValue`/`rangeExcludesZero`; it gains a second question, **asked only for `mod`**, and hands the bit to the sink. **This keeps A1's convention — VERDICT in the parser, EXPANSION in the lowering** — which is load-bearing, because it is what lets the `/` and `mod` paths diverge at all. ⛔ **REJECTED: an unconditional guard at the sink.** It would (a) **pessimize the two targets that are already correct** (arm64 `sdiv`+`msub` and wasm `i64.rem_s` yield 0 by construction) with a cost no test would ever show, (b) move **~55 fragments including 26 arm64 ones** on a host that cannot run those lanes — the unverified widening that once left 317 stale arm64 goldens on `main` — and (c) **make a spec's prose FALSE**: `ranged-divisor-is-a-bare-idiv` asserts "no `cmp`, no `or`, no branch" for an `int(1 to 1000)` divisor, a range that genuinely excludes −1. ⚠ Also rejected: emitting the mask ops from the parser (smaller, but it puts an ISA workaround in the parser and splits the convention). ⇒ **exactly ONE existing golden moves** (`safety/mod-by-zero`, 4 files incl. `arm64-macos`, regenerated without being run — unavoidable under any correct fix). ⚠ **The row's lane is corrected to reflect where the fix actually lives** | L-types *(`Parser.maxon`'s divisor verdict · `LowerMaxonToStd.maxon`'s `mod` sink)* | ✅✅ **CLOSED 2026-07-31** (main `ca3ad493a`; x64-windows **2918 → 2925/0**, x64-linux **2798 → 2805/0**, wasm32-wasi **2767 → 2770/0**, arm64 **SKIP — remote, UNVERIFIED, no arm64 golden minted**; 0 warnings; no exit 101; `specs/` and `maxon-sharp/` **untouched**; **zero pre-existing goldens modified by the review**, and exactly ONE by the rung — `safety/mod-by-zero`, whose `arm64-macos` twin is knowingly **STALE**, stated rather than silently regenerated). ⭐ **THE RULING IS IMPLEMENTED AS THE VERDICT, NOT THE EXPANSION**: `divisorZeroVerdict` → **`divisorProof`**, answering **both** of `idiv`'s hazards from one walk, with `emitDivOrMod` reading the half each operator needs — **so a bare `idiv` now requires BOTH hazards closed, where A1's rule demanded only the left half and waved `i64.min mod -1` through.** The new fact rides A1's own channel (the callee name): **`__guarded_mod`**, the family's third expansion and its first NON-THROWING member, making `divisionKindThrows` the hinge. Guard is **4 branchless ops** masking the **DIVIDEND** (since `a mod -1` is dividend-independent) and reusing the one `-1` const as comparand *and* decrement. **Coordinator-verified: `mod=0` and `lit=0`, exit 0, on x64-windows, x64-linux AND wasm** — both the `try`-wrapped opaque divisor and the bare literal. ⚠ **A STATED CONSEQUENCE OF THE RULING: `try (a mod -1) otherwise …` is now a COMPILE ERROR, not merely redundant** — the operation became total, and an unneeded `try` is already refused. That is a visible change to code that compiles today. ⭐ **`/` is untouched and its boundary is now PINNED rather than assumed** (`a-checked-divide-still-faults-at-int-min-over-minus-one`: a checked `/` with a fallback sitting right there, still dying deliberately); `needsOverflowGuard` requires `op == mod`, so the two paths never touched, and `ranged-divisor-is-a-bare-idiv`'s *"no `cmp`, no `or`, no branch"* prose stayed TRUE. ⛔ **THREE DEFECTS THE RUNG ITSELF CREATED OR EXPOSED, all fixed:** (1) **a LOST `E3103`** — `-1` became the only constant divisor leaving the bare emitter and the guarded route failed to record its fold, so `let z = 10 mod -1` then `try (100 / z) otherwise 7` **compiled and printed 7** where it had been refused at compile time; cured by one `recordBinOpFold` both routes call, **and** by `TypeRules.foldIntDivision`, which was **refusing `i64.min mod -1` for BOTH operators** — *the constant folder disagreeing with the language it folds for*, the same false generalization the specs carried, one tier down. (2) **a DIAGNOSTIC DOWNGRADE** — a needless `try` over the now-total `mod` landed on E3055's *"this builtin call cannot fail"*, about an operator the author typed; `tryOnTotalDivision` now names the operator and the divisor's range (**the sixth instance this session of a refusal being easier to get right than its NOUN**). (3) ⭐ **an ALLOCATION no suite could see**: `DivisorProof` as a two-field `type` **is a heap record** when returned by value, asked once per division and once per int→int ranged cast ⇒ **+6,146 allocations in `phase:parse` at rung 5**; re-spelled as a payload-free enum ⇒ **+0 exactly**, confirmed independently by the review's own same-path A/B. ⭐⭐⭐ **AND THE INDEPENDENT REVIEW FOUND A SOUNDNESS HOLE OLDER THAN THIS RUNG — see `A2g`** | `slice/A1x-mod-minus-one` | 2026-07-31T16:22Z |
| **A2g** | **⛔⛔ FIXED IN A1x'S REVIEW: A WHOLLY NEGATIVE RANGE LOST ITS BOUND CHECKS, so a runtime value outside it was ADMITTED — and `int(i64.min to -2)` was checked NOT AT ALL.** `TypeRules.rangeNeedsUpperCheck` asked `high >= 0 and high < i64.max` — i.e. **the bound's SIGN** — when the real rule is a single shape: a stored `-1` upper means the wrapped `u64.max` **only when `low >= 0`**. So every range with a negative upper lost its upper compare, and `int(i64.min to -2)` lost the lower one too (`low == i64.min` needs none), leaving **an empty check list**. **MEASURED, reachable through the `as` CAST and a ranged `return` with NO call argument anywhere:** `ident(0) as int(-100 to -1)` was **admitted and printed `a=0`**; `ident(5000) as int(-100 to -1)` printed `b=5000`; `100 / (ident(0) as int(-100 to -1))` died `panic: integer divide by zero`, **uncatchable**; and `i64.min mod (ident(-1) as int(i64.min to -2))` reproduced **the exact fault A1x exists to remove.** Coordinator-verified after the fix: the same cast now panics `Range check failed: value outside typealias 'NegativeOnly'`. ⚠⚠ **THE COMMENT THAT COVERED IT WAS A BUG WEARING THE ORACLE'S NAME**: `InsertRangeChecks.maxon:950` said the empty list was *"a signed range with a negative upper (matched to the bootstrap's both-elided path)"* — presenting a defect as deliberate parity. ⚠ **The LITERAL form was already refused** (`0 as int(-100 to -1)` ⇒ E3005) because `literalInRange` asked the one-shape question while the runtime check asked the sign — **one rule, two halves, disagreeing about one alias.** ⚠ **NOT the call-argument door (`A1f`), and that was proven not assumed**: the two cases pinning a FAULT through an unguarded argument still PASS, where closing that door would have flipped them to a range panic and gone red. **A1's zero-elision was NOT broadly unsound** — `int(1 to 1000)`'s upper is non-negative, so its cast door always worked; the defect was confined to the negative-upper family, and **A1f's pricing is unchanged** except that its text should now say it closes **two** hazards. ⚠ **No committed spec cast or returned into a wholly negative range**, which is why 2918/0 was green over it and why zero pre-existing goldens moved. **7 cases added, RED first.** ⚠ The renderer held the same one-shape fact wrongly too: `int(-1 to -1)` printed itself as `int(18446744073709551615 to …)`, self-contradictory inside one sentence; moved to `formatRangeDisplay`, the only party holding both bounds. ⇒ **WHAT REMAINS FOR THIS ROW: the ladder is BLIND to the fix's cost** (the corpus has no wholly negative range, so `phase:insertRangeChecks` read **+0**) — the guard is one `cmp`+`jcc` per site *when it fires*, and nothing measures it | L-types *(closed — kept for the finding)* | ✅ **DONE 2026-07-31** *(with A1x)* | — | — |
| **A2h** | ✅✅ **CLOSED 2026-08-01** by BATCH1 (main `d4cd6aa20`). ⚠ **THE ROW'S CITATION `2-Parser.cs:19163` WAS STALE** — that line is now inside `TryResolveEnumRawConstant`; the rule was at `:19252`. ⭐⭐ **A FOURTH DOOR NEITHER REFERENCE COULD HAVE TAUGHT US**: a ranged operand whose optimal type is 32 bits lowers to `StdRemI32Op`, and `i32.min mod -1` overflows in ITS OWN WIDTH — shv2 has no i32 narrowing, so A1x never met it. Reproduced (`panic: integer overflow` in `rem`), closed at the same site, pinned by `mod-at-int-min-by-a-32-bit-minus-one-is-zero`. ⭐ **The guard is emitted at ONE site in front of BOTH routes** (bare `EmitBinOp` and `MaxonCheckedDivTryCallOp`) because door 1 of the ported specs reaches the fault through the THROWING `__checked_mod` — guarding only the bare route would have been the defect wearing a fix's clothes. ⭐ The mask is spelled differently from shv2's `(divisor == -1) + (-1)` **because that cannot be written at the bootstrap's Maxon tier** (a comparison yields Bool/i1 and no op widens it); the sign-spread computes the identical mask. `/` deliberately NOT made total and its boundary is pinned. ⚠ **Two port-list cases were already GREEN** (controls, kept as such) and **one is NOT PORTABLE**: the bootstrap has no callee-entry range guard ⇒ new row `A3c`. **THE BOOTSTRAP HAS BOTH OF A1x's DEFECTS, AND ONE OF THEM IS THE IDENTICAL RULE.** (1) **`i64.min mod -1` dies there too, through the same THREE doors** — `2-Parser.cs` + `MaxonToStandardConversion` — and **measured: door 2 (a LITERAL `-1` divisor) fails as well**, not just the `try` spelling, so the entry I first filed was too narrow. (2) **`maxon-sharp/Compiler/2-Parser.cs:19163` carries the same `IntUpper >= 0 && …` rule** that `A2g` just fixed in shv2, so a wholly negative range loses its bound checks in the ORACLE as well. ⚠⚠ **UNTIL (2) IS FIXED THE ORACLE CANNOT ARBITRATE EITHER QUESTION** — and shv2 now deliberately diverges from it on both, which is exactly the hazard D11d records: a future reader can read the divergence as shv2 being wrong. **Gate is the full C# suite (3188+) plus codegen neutrality** — note `git status --short specs/` is *already* dirty from 139 pre-existing stale fragments, so any churn must be proven pre-existing by A/B against the parent, never asserted | **L-bootstrap** *(`maxon-sharp/` — needs the C# suite as its gate)* | ▣ **BATCH1** | — | — |
| **A2i** | **`i64.min / -1` NOW HAS THREE BEHAVIOURS ACROSS TARGETS, and with `mod` uniform it is the LAST place in the family where a valid-looking program's answer depends on the backend.** x64 **faults** (`panic: integer overflow` on Windows, `integer divide by zero` on Linux — the kernel reports `FPE_INTDIV` for every `#DE`, documented); **arm64 WRAPS to `i64.min`** (`sdiv` does not trap); **wasm TRAPS** (exit 3, no message). ⚠ **The specs are honestly SILENT here rather than wrong**, and the **user ruling deliberately scoped `/` out** — a quotient that does not exist has no value to return, so a fault is defensible. **This row is not a request to reopen that.** It exists because the divergence is now the family's only one and should be *recorded and pinned* rather than left as folklore: **decide whether arm64's silent wrap is acceptable** (it is the only lane that returns a WRONG VALUE rather than dying), and pin whatever is decided on every lane. ⚠ arm64 cannot be run on this host, so verifying any change needs the periodic Mac sync | L-types *(`/`'s overflow behaviour · needs a ruling on arm64's wrap)* | ⬜ FREE *(needs a USER RULING on arm64 silent wrap; arm64 unverifiable on this host — dropped from BATCH6 at claim time)* | — | — |
| **A1y** | **ONE VALUE, THREE SPELLINGS, ACROSS THREE BACKENDS — plus 6 orphan goldens nothing checks.** (1) **`RangePanicExitCode = 1` survives in `StdToArm64Conversion.maxon:79` (as `0x1`) and `StdToWasm.maxon:151` (as `1 as Imm32`), alongside x64's `PanicExitCode = 1`** — three declarations of one number that cross-reference each other *by comment*, which is this project's signature bug in miniature. A1j deleted the x64 duplicate but could not reach the other two (different backends, outside its file list). ✅ It fails LOUDLY rather than silently — every silent lane pins `exitcode 1` in the suite — and it is deliberately outside `RuntimeAbort`'s ">70" band per that file's own rule, so this is hygiene, not a defect. ⚠ The cure is a shared constant, not a fourth comment saying "keep these in sync" — see the cure ladder: DERIVE > CHECK > COMMENT. (2) **Orphan goldens: x64-windows carries 2904 goldens for 2901 tests and x64-linux 2786 for 2783 — exactly 3 per lane, the SAME three names on both**, left by target narrowings in review commits `936b22ab3` and `bb4c27a17`, and present before A1j. **A MISSING golden never fails and neither does a SURPLUS one** — nothing in any gate looks at goldens with no test, so they can only be found by going to look. Fits the phase-boundary stale-golden sweep, or delete the 6 files as a one-liner | L-ir | ✅✅ **CLOSED 2026-07-31** (main `a60543c9f`; x64-windows **2908/0**, x64-linux **2790/0**, wasm32-wasi **2759/0** — all UNCHANGED, correctly, since the rung adds no tests; arm64 **SKIP — remote, UNVERIFIED**; 0 warnings; no exit 101; **zero golden churn**). ✅ **Item 1:** one `export let PanicExitCode = 1` in a new `Targets/Shared/PanicExitCode.maxon`; the three declarations are gone. ⚠ **The row's own framing was slightly wrong and the rung says so**: x64's `PanicExitCode` was never range-specific (`mrt_panic` spends it on every terminal diagnostic, including the two `idiv` faults), so **`RangePanicExitCode` was the wrong name of the two to keep** — the surviving name is the one that had *not* survived in two of the three files. ✅ Deliberately NOT folded into `RuntimeAbort`: those cases are *reasons* picked above 70 to be distinguishable from a return, while this number is reason-free and **fixed at 1 by `specs-shv2/string-views.md`**, so filing it in a "next free number" registry would tell the next reader it can move. ⭐ **TWO spec cases pin the constant BY EXECUTION on a silent lane** (`whitelisted-stdlib-keeps-its-range-check` and `codepoint-return-is-range-checked`, both `targets: wasm32-wasi`) — verified RED by setting it to 2 (`expected 1, got 2`). The implementer claimed one; there are two. ✅ **Item 2: TEN orphan goldens, not six** — my count was scoped to the two lanes I had enumerated, and the `array-slots` pair is on both arm64 lanes too. Derived twice by two different methods (the ten files carry only **three** distinct test names, so the question needs no parser mirror — just grep the owning spec), and a full name-level audit of **all 9,051 committed goldens across four lanes** now reads **zero orphans**: 2908/2908, 2790/2790, 2696, 1657. Git says why: two removed by `11869f3c0`, one **RENAMED** by `5bf545e15` — goldens abandoned both times. ⭐⭐⭐ **AND THE REVIEW FOUND AN ACTIVE MISCOMPILE ONE ARGUMENT AWAY FROM A LINE THIS RUNG EDITED — see `A2c`.** ⚠ Prose that named the deleted constant was corrected in `ranged-typealias.md` and `string-views.md`, and **two stale claims were removed from `emitRangePanic`'s comment**: the *"the runtime-panic spec cases are restricted to x64-windows"* circular justification (**the third instance of that exact shape this session**, after A1j's original and arm64's twin) and a *"this is dead code"* claim the wasm cases above falsify | `slice/A1y-one-exit-code` | 2026-07-31T12:27Z |
| **A2c** | **⛔⛔⛔ FIXED IN A1y'S REVIEW, RECORDED HERE BECAUSE IT IS BIGGER THAN THE RUNG THAT FOUND IT: arm64-linux ISSUED x86-64's SYSCALL NUMBERS, and which numbers you got depended on WHICH MACHINE COMPILED THE COMPILER.** `Arm64Runtime.maxon` and `X64LinuxRuntime.maxon` each exported `LinuxSyscallWrite`/`LinuxSyscallMmap`/`LinuxSyscallExitGroup` over **different values**, and **Maxon's top-level namespace is flat: two exported globals of one name resolve SILENTLY BY FILE ORDER, with no diagnostic.** A file's own directory grants it nothing — probed against the bootstrap: a file in `Targets/Arm64/` gets `Targets/X64/`'s value, only a file's *own* declaration shadows an import, and the winner follows **position, not value**. **MEASURED by disassembling every `svc #0` in cross-compiled arm64-linux binaries:** a range panic emitted `[94, 231, 231]` where AArch64 needs `[94, 94, 94]`; `print` emitted `[94, 1]` for `[94, 64]`; `print`+interpolation `[94, 9, 1]` for `[94, 222, 64]`. ⚠⚠ **AND IT FAILED SILENTLY: on AArch64, 231/1/9 are `munlockall`, `io_destroy` and `lgetxattr` — none aborts**, so a violated range guard did not stop the program; it fell through to `mov x0, xzr; ret` and continued with a zero. **Every reached `panic()` took that path.** ⇒ Cured by arch-prefixing both tables (`Arm64LinuxSyscall*` / `X64LinuxSyscall*`), which is `PosixMmap.maxon`'s existing convention (`MmapAnonPrivateLinux` vs `…Macos`: the OS in the name when the fact differs). ⭐⭐ **THE TELL WAS IN THE GOLDENS, AND IT IS THE CASE AGAINST UNRUNNABLE LANES**: all **97** `arm64.syscall` sites in `fragments/arm64-linux/**` record **94** — synced from a real arm64 host *after* the collision existed — while this host's compiler emitted **231**, and **nothing compared them because that lane cannot run here.** The goldens held the right answer while the compiler produced the wrong one. ✅ Kept in its own commit (`a60543c9f`) so it is revertible in one step. ⚠ **What remains for THIS row: `Targets/Shared/` is a CONVENTION, not a MECHANISM** — the flat namespace enforces nothing, which is exactly why this collision could exist one level down, and A1y just made that directory the home for cross-backend facts. **A check that no two `export let`s share a name would be the DERIVE-level cure**; without it the next collision is equally silent. Also same-class and deliberately NOT renamed (no collision today, different table): `LinuxSigactionFlags`, `SigactionOff*`, `UContext*Offset` in `X64LinuxRuntime.maxon` are x86-64-specific facts under arch-neutral names. ⚖ **COORDINATOR SCOPING 2026-07-31: BUILD A PROJECT-LEVEL CHECK, NOT A LANGUAGE RULE — and that is what makes this row implementable WITHOUT `A2d`'s ruling.** A semantic check in the compiler would *decide* whether duplicate exported globals are a language error, which `/specs` has not settled (`A2d`). A check that refuses the **compiler's own sources** decides nothing about the language and still closes the class. **The precedent is exact and already wired**: `maxon error-codes check` fails the build from `maxon-sharp/MaxonSharp.csproj:112`, for the same reason (a fact spread across files that nothing reconciled) | L-ir *(`Targets/` + a check with `error-codes check`'s wiring)* | ✅✅ **CLOSED 2026-07-31** (main `c9f0c0b24`; C# suite **3188/0**, x64-windows **2908/0**, x64-linux **2790/0**, wasm32-wasi **2759/0**, arm64 **SKIP — remote, UNVERIFIED**; `dotnet build` 0 warnings; no exit 101; **`maxon-shv2.exe` BYTE-IDENTICAL** pre-rung vs rung vs review, `sha256 f466a5ff…`, on a build first verified deterministic). ✅ **`maxon flat-namespace <check|list>`**, a sibling command wired into `dotnet build` beside `error-codes check`; reports `OK — 595 type + 556 value cross-file top-level names across 142 files in 2 project(s)`. **Coordinator-verified RED**: reconstructing the historical `LinuxSyscallExitGroup` 94/231 pair **exits 1**, naming both files, lines and values with a remedy; clean tree exits 0. ⭐ **It re-implements nothing** — it asks through `Compiler.PreRegisterTypeNames` (new `onDeclaration` callback, because `TypeDefSourceFiles` is name-keyed and so has already discarded the loser a report must name, and **is never written for interfaces at all**) and a walk extracted from `Parser.PreScanTopLevelConstantDecls`, calling `IsFileInModuleScope` rather than restating the `module` rule. ⭐ **THE COLLIDING KINDS WERE DERIVED, by compiling a two-file project forwards and under `MAXON_SOURCE_ORDER=reverse`** — `export let/var/type/union/typealias/interface` and `module let` are all silent and order-dependent; **`export function` is already LOUD (E3006) and is excluded.** ⚠ **Two results NARROW the class**: a value and a function sharing a name is **not** a collision (two namespaces), and **`module` is DIRECTORY-scoped** so sibling subtrees may legitimately share a name — with a negative control proving the check declines that case. ⚠ `enum`-vs-`enum` is caught only **by accident** (the synthesized `hash` method trips E3006); a `union` pair stays silent, so "enum is safe" is not a property of the type name. ✅ **Fired on the existing codebase: two real collisions, both AGREEING** (`ByteCount`, which was a *third* copy of `stdlib/String.maxon:11`, and `TypeNameIdArray`) — fixed by **deletion, not renaming**, and the byte-identical binary confirms the types were the same. ⛔⛔ **AND RECONCILING THE CHECK'S OWN COUNT AGAINST `grep` FOUND A LIVE DEFECT (554 vs 556): `SkipToMatchingEnd` counts keywords, and a keyword is not always an opener** — shv2's `enum TokenKind` declares cases named `if`/`while`/`for`/`match` **and `end`**, while an interface's requirements have no `end` — so the skip walked past a declaration's own `end` and **silently swallowed every declaration after it.** Introduced by `134a8d089` (2026-07-16); three live instances beyond shv2's two, including `stdlib/Interfaces.maxon:53-55` after `interface Hashable`. Red written before the fix. ⛔ **A second blind spot in the rung's OWN gate, self-caught**: the value walk selected the **host's** `#if` arm, so a collision behind `#else` would be refused on one machine and passed on another — *the exact property this rung exists to remove.* ⭐⭐⭐ **AND THE REVIEW FOUND THE RUNG'S BOUNDARY WAS DRAWN WRONG — see `A2e`.** ⚠ Gate hygiene worth keeping: **`git status --short specs/` is NOT empty** — 139 `specs/fragments-x64-windows/**` files are rewritten by any C# suite run — and rather than arguing that away the implementer built the **pre-rung** source, ran the identical suite and `cmp`'d the two diffs: **byte-identical, 799,606 bytes each** ⇒ pre-existing staleness (**125 commits have touched `maxon-sharp/` since those goldens were regenerated**), not this change. ⚠⚠ **A GAP OF MINE THIS RUNG EXPOSED: `A1y` CLOSED WITHOUT A `docs/optimization-log.md` ROW**, so the ladder's current movement is A1y's and unattributed — measured here as bytes fitting **`44,201 + 7,623·2ⁿ`** (constant + strictly linear) with allocations flat at the −192 path artifact, and A2c's own delta is **exactly zero** (A/B against its parent at one path with equal-length binary names) | `slice/A2c-no-duplicate-exports` | 2026-07-31T13:47Z |
| **A2e** | ✅✅ **CLOSED 2026-08-01** by BATCH1 (main `d4cd6aa20`). ⚠ **THE RESIDUAL IS LATENT, NOT LIVE — measured, and that is why NO spec case was written.** The traced desync is real (`PreRegisterTopLevelTypeAliasNames`'s counter never returns to 0 after shv2's own `enum TokenKind`, whose cases are spelled `end`/`type`/`union`/`enum`/`interface`, so `TokenArray` is never pre-registered) — but probed THREE ways (the TokenKind shape, a conformance clause naming an alias declared below, an outer alias of the same name) and **all compile and run clean**, because the main walk still registers the alias later; only the PLACEHOLDER is missing. A vacuously-passing test would be worse than none. ⇒ **Cured as DUPLICATION instead**: new `WalkOneDeclarationLevel` routes copies #2, #3 and #4 through the canonical `SkipToMatchingEnd`; **#2 and #3 collapsed into ONE routine** (the starting level is the caller's position, not a rule), and #4 turned out to be a substitution, not the refactor the row feared. `SkipInterfaceDecl` (#5) correctly left alone — its own comment says why. **⛔⛔ FIXED IN A2c'S REVIEW: AN `interface` FOLLOWED BY AN `enum` IN ONE FILE MADE A CROSS-FILE ENUM REFERENCE FAIL — OR NOT — DEPENDING ON FILESYSTEM ORDER.** `PreScanTypeAliasesOnly` (`maxon-sharp/Compiler/2-Parser.cs:1214-1217`) skipped an `interface` with the **uncorrected** skimmer, so everything after an interface in that file was invisible to **the pass whose entire job is making cross-file enum/typealias references order-independent.** **MEASURED on the pristine merge candidate**: `export interface Shape` then `export enum Color` in one file, `export let Favorite = Color.green` in another ⇒ **`error E3034: unknown enum case: 'green'`, exit 1** in natural order and **exit 0 printing `green`** under `MAXON_SOURCE_ORDER=reverse`. Move the enum above the interface and it compiles both ways. **Coordinator-reproduced after the fix: both orders compile and print `green`, exit 0.** ⚠⚠ **THE ROW EXISTS FOR THE PROCESS LESSON, NOT THE FIX**: the implementer left this deliberately, reasoning *"changing it shifts the oracle, which is a different rung"* — **and that was wrong twice over.** It is the **same filesystem-order class the rung was scoped to close**, reached from the other side; and **the rung had already shifted the oracle for the identical fix** one function away. ⇒ **a boundary drawn by WHICH FILE a defect lives in, rather than by WHICH CLASS it belongs to, will cut through the middle of the class.** ✅ **FOUR more copies of "what opens a block" survive, each now with a stated reason rather than folklore** — and two deserve watching: `PreRegisterTopLevelTypeAliasNames` (`:1630`) is **provably desynchronized on committed source today** (after `enum TokenKind` its counter sits at depth 3, so `TokenArray` gets no placeholder) though probing found no wrong answer, and `ProcessExtensionBlock`'s clone (`:4482`) is **structurally unreachable only until a rung makes a keyword callable after `.`** — which **D8 already moved toward.** Unifying that clone with the shared skip is a real refactor (it collects positions at depth 1 while walking a copy index) ⇒ its own rung | **L-bootstrap** *(`2-Parser.cs` — needs the C# suite as its gate)* | ▣ **BATCH1** | — | — |
| **A2f** | **THE NEW FLAT-NAMESPACE GATE HAS TWO BLIND SPOTS, BOTH CHEAP TO CLOSE (the gate costs 0.56 s).** (1) **`*.test.maxon` files are EXCLUDED from the census** (`SourceCollector.FromDirectory` skips them) **yet `maxon test` compiles them WITH the project** — so a collision between `maxon-dev-mcp/test/*.test.maxon` and its own project is a real flat-namespace collision **the gate cannot see.** A gate that omits a compilation mode is a gate with a documented hole. (2) **The project-vs-`stdlib` overlap is measured and open**: `stdlib/` is compiled into every program and shares the flat namespace, with live overlaps today — `ByteCount`/`StringArray` (shv2 `Project.maxon` vs `stdlib/Json.maxon`), `BytePos` (`maxon-dev-mcp/mcp/Util.maxon` vs `stdlib/String.maxon`), and **`ParseError` — shv2's `union` against stdlib's `enum`.** A **typealias**-vs-stdlib pair *is* order-dependent (measured); a `union` shadowing an `enum` is order-INdependent (byte-identical binaries both ways) ⇒ shadowing, a different class. ⚠ `maxon-selfhosted` additionally has two within-tree pairs (`FilePathArray`, `VarSlotArray`). ⚠⚠ **DO NOT "FIX" (2) BY RENAMING: the A2d ruling is that this belongs to a NAMESPACE mechanism**, under which shv2's `ParseError` is simply not the same name as stdlib's and needs none of its 847 occurrences touched. This row is about **widening the CHECK's coverage**, and about deciding what it should say for a stdlib overlap while namespacing is pending. ⛔⛔ **A THIRD BLIND SPOT, FOUND AND FIXED DURING A1x's INTEGRATION 2026-07-31 (main — coordinator, not filed): THE GATE BROKE `dotnet build` WHENEVER AN AGENT WORKTREE EXISTED, WHICH IS THE RUNG WORKFLOW'S NORMAL STATE.** `.claude/worktrees/<slice>/` holds a **full checkout**, so its `maxon-shv2/build.maxon`, `maxon-selfhosted/build.maxon` and `maxon-dev-mcp/mcp/build.maxon` read as three projects `Units` had never heard of ⇒ *"these Maxon projects are not in FlatNamespaceCheck.Units"*, **`dotnet build` exit 1, with nothing wrong with the tree at all.** ⚠ **A2c's own implementer and reviewer could not have seen it: they worked INSIDE worktrees, where no nested worktree exists** — only the primary checkout, where the coordinator builds, is affected. That is a blind spot of the *review position*, not of either agent. ⭐ **CURED BY THE TEST THAT ACTUALLY DISQUALIFIES A DIRECTORY — it carries its own `.git`, so it belongs to a DIFFERENT checkout** — rather than by hardcoding `.claude/worktrees`, which would have worked today and missed a vendored clone tomorrow. A worktree's `.git` is a FILE and a clone's is a DIRECTORY; both are tested. ⚠ **Adding `Units` rows for worktrees would have been worse than useless: the rows would name paths that vanish when the slice merges.** Proven both directions — with a nested checkout present the check and `dotnet build` now exit **0**; before the fix both failed. ⚠⚠ **AND THE FAILURE CARRIED A SECOND TRAP WORTH RECORDING: the shv2 build and ALL THREE suites reported GREEN afterwards, on the STALE `bin/maxon.exe`** the failed C# build never replaced. **A red build makes every later green in the same run worthless**, and the numbers looked perfectly ordinary. ⛔ **(3) — ⚠ THIS ITEM IS NOW OWNED BY ROW `A2o`, WHICH ANOTHER AGENT FILED INDEPENDENTLY FOR THE SAME FINDING; DO NOT FIX IT TWICE.** Kept here because it is the third instance of this gate's one shape and belongs beside the other two. **A THIRD BLIND SPOT, MEASURED 2026-08-01 by the BATCH3 coordinator — the gate CENSUSES GENERATED SCRATCH, so a killed instrument run turns `dotnet build` RED with no source change.** A `scale-test --per-type` run that was killed mid-ladder left `.scale-tmp/mmtrace-src/build.maxon` behind, and the next `dotnet build` in that worktree failed with *"these Maxon projects are not in FlatNamespaceCheck.Units: .scale-tmp/mmtrace-src/build.maxon"*. `.scale-tmp` is GITIGNORED and machine-generated — it is the instrument's own corpus, not a project — so the census must exclude it (or every ignored path) rather than demand a `Units` row for a file that will not exist tomorrow. ⚠ The failure mode is the expensive one: **a red build that names a file the reader did not write**, on a tree whose sources are clean, which reads as *"the rung broke the build"*. `rm -rf .scale-tmp` clears it. ⚠⚠ **LANE CORRECTED 2026-08-01 (BATCH4 coordinator, at plan time): this row is NOT L-ir — `FlatNamespaceCheck.cs` and `SourceCollector.cs` are `maxon-sharp/Compiler/`, i.e. L-BOOTSTRAP**, which `B2` holds live. BATCH4 dropped it unstarted for that reason. ⇒ **take it with BATCH1**, whose one C# suite run and one codegen-neutrality A/B it shares; a `maxon-sharp` change owes that gate whether it is a build tool or a pass | **L-bootstrap** *(`FlatNamespaceCheck.cs` · `SourceCollector.cs`)* | ⬜ FREE | — | — |
| **A2d** | **⛔ THE BOOTSTRAP ACCEPTS TWO EXPORTED GLOBALS OF ONE NAME ACROSS FILES AND PICKS BY FILE ORDER — NO DIAGNOSTIC — and it is a SPEC question before a compiler one.** Found by A1y's review while diagnosing `A2c`, and it is the reason `A2c` could exist at all: nothing in either compiler objects. ⚠ **`specs/global-file-private-same-name.md` specifies the FILE-PRIVATE case as legal and says NOTHING about the exported one**, so the language has not decided whether this is an error, and a compiler change without that decision would be guessing. ⇒ settle the spec, then emit a diagnostic in both compilers. **Gated by the full C# suite** ⇒ its own rung. ⭐ Note the shape: this is the *enabling* defect for a whole class — any two backends, any two runtimes, any shared constant table. `A2c` fixed one instance by renaming; only this row can stop the next one. ⚖⚖⚖ **USER RULING 2026-07-31 — AND IT IS NONE OF THE THREE OPTIONS I OFFERED, WHICH IS THE POINT: "this should be handled with NAMESPACE. there should be spec tests for it."** I framed the question as *"should a duplicate exported global be an ERROR?"* and all three of my options were flavours of refusal. **The ruling rejects the framing: the cure is to make the two names DIFFERENT NAMES, not to forbid the pair.** ⇒ ⭐ **A namespace mechanism, plus spec tests for it, is the deliverable — NOT a diagnostic.** That dissolves the constraint I thought was binding: shv2's `union ParseError` vs stdlib's `enum ParseError` needs **no rename at all** (847 occurrences, 812 in `Parser.maxon`) because under namespacing they are simply not the same name. ⚠⚠ **A MEASURED LEAD THE IMPLEMENTER OF THIS ROW SHOULD START FROM, because the mechanism may already partly exist: `module` is DIRECTORY-scoped, not global** (`Lexer.ContextualKeywordHelp`), and A2c **measured** that two `module` declarations in *sibling* subtrees legitimately share a name while only *overlapping* subtrees collide — it ships a negative control proving its check declines the sibling case. **So `module let` in `Targets/Arm64/` and `Targets/X64/` would have prevented the original miscompile outright, and the bug may reduce to `export` having been used where `module` was correct.** ⇒ **first question for this row: is `module` already the namespace, needing only qualified references and specs — or is a new mechanism required?** Answer that before designing anything; the cheap outcome is that the language already has this and only `/specs` and the runtime files are missing. ⚠ **What A2c does NOT become:** its project-level check stays useful as a guard for *this repo* while the mechanism lands, but it is a **stopgap, not the cure** — say so on its row. ⚠ Still true and still needed regardless: `specs/global-file-private-same-name.md` specifies the file-private case and is **silent on the exported one**, and the bootstrap accepts the collision **with no diagnostic** \| whatever namespacing lands, that silence is what let a wrong answer ship, so the spec tests the ruling asks for must cover the *collision* case and not only the happy path. ⚠ **DROPPED FROM BATCH1 AT ITS CLAIM, 2026-08-01T14:29Z** — not because it is blocked, but because the ruling changed its SHAPE: *"handle it with NAMESPACE, with spec tests"* is a language feature needing its own contract, its own IR/scoping design and its own spec-port list, and it shares nothing with the eight `maxon-sharp` defects BATCH1 exists to put behind one C# suite run. Stretching the batch to keep it is exactly what this section's drop rule forbids. **Start from the row's own measured lead: is `module` already the namespace?** | **L-bootstrap** *(`maxon-sharp/` + `/specs` — needs the C# suite as its gate)* | ⬜ FREE | — | — |
| **A1z** | **⛔⛔ A RANGED `return` FROM A CLOSURE IS NEVER GUARDED — an out-of-range value comes back through a declared ranged type and the program EXITS 0.** Found by the A1h review, **reproduced by the coordinator**: `let f = function(n Wide) gives n` cast to `typealias Picker = function(Wide) returns Small` where `Small = int(0 to 100)`, then `g(2000)` **prints `high=2000` and exits 0**. No guard, no diagnostic, no panic. ⭐ **THIS IS A MISSING DOOR, NOT A BROKEN MECHANISM, and that is why A1h filed it rather than fixing it**: `parseReturnStatement` records a `RangeCheckReturnSite`, but a closure body's `gives` goes through `emitOwnedValueReturn` on a **different path that records nothing**, so `InsertRangeChecks` never sees a site and there is nothing in that pass that is wrong. ⇒ a **closures × ranged-typealias** slice with its own specs, not a line in a perf rung. ⚠⚠ **THE BOOTSTRAP CANNOT SERVE AS ORACLE HERE**: it rejects the program with `E3062: unused typealias 'Picker'`, which is spurious (the alias IS used, as the cast's target) and is very likely a **second, separate bootstrap bug** — file it on the oracle list when this row is taken. So this row must settle the intended semantics from `/specs` and the language rules, not by running the reference. ⛔⛔ **CORRECTED 2026-07-31, BEFORE ANY CODE — I FILED THIS ROW AND THREE MEASUREMENTS NARROWED IT TO A VARIANT OF `A1f`, NOT A DEFECT OF ITS OWN.** (1) **A closure CANNOT DECLARE a return type at all**: `function(n Wide) returns Small gives n` is `E2010: Expected 'gives' but got 'returns'`, so there is no closure-side declaration to attach a site to, and *"the closure-return path records nothing"* was the wrong diagnosis — there is nothing for it to record. The ranged return can only ever arrive from the function TYPE (`typealias Picker = function(Wide) returns Small`). (2) **The mechanism WORKS wherever a site exists**: `function apply(p Picker, v Wide) returns Small` whose body is `return p(v)` panics correctly — `panic at :6: Range check failed: value outside typealias 'Small'`, at `apply`'s own return line. (3) ⇒ **my original probe printed `2000` only because the result went straight into INTERPOLATION and never came to rest anywhere ranged.** ⭐ **AND THAT IS THE DOCUMENTED POLICY, NOT A HOLE THIS ROW FOUND**: `specs-shv2/range-check-panic.md:45-52` states it outright — the runtime half is emitted at a `return`, a struct-literal field, a field store, an array-literal element and an explicit `as`, and **NOT at a call boundary**, so *"an unfoldable argument is not guarded at the boundary it crosses — it is guarded wherever it comes to rest."* A call RESULT through a function-value type is the same case with the arrow reversed. ⇒ **THIS ROW IS A VARIANT OF `A1f`, WHICH ALREADY OWNS THE GENERAL CURE** (guard at the boundary, which A1f measures as a large blast radius and its own rung). **Do not implement it separately** — a second boundary-guard mechanism for function values would be one fact written twice. ⚠ **What genuinely remains, and it is narrower than the row's title**: a value whose STATIC type says `Small` can be *observed* out of range (printed) without ever being guarded, because interpolation asserts no range. Whether that is acceptable is the same question A1f asks, and it should be answered ONCE for both. ⚠ **Still true and still worth its own filing**: the bootstrap rejects the probe with `E3062: unused typealias 'Picker'` — spurious, since the alias IS used as the cast's target — so it is very likely a **second, separate bootstrap bug**, and the oracle cannot arbitrate this family until it is fixed. ✅✅ **CLOSED 2026-08-01 AS NOT A DEFECT — THE CORRECTED ROW WAS RIGHT, AND I RE-VERIFIED IT RATHER THAN INHERITING IT** (BATCH6, main `b699c736d`). Two measurements: `return p(v)` in a function declared `returns Small` **panics correctly** (`Range check failed: value outside typealias 'Small'`, in `g`), so the mechanism works wherever a site exists; and `print("high={p(2000)}")` **prints 2000 and exits 0**, because interpolation is not a resting place. That is `specs-shv2/range-check-panic.md:45-52`'s stated policy, which `A1f` shipped — *guarded wherever it comes to rest*, not at every observation. ⚠ **The one thing left is a QUESTION, not a defect**: whether a value whose static type says `Small` may be OBSERVED out of range. Changing that means guarding every observation, which is a language decision with a large blast radius, and nobody should take it as a rung without a ruling. ⚠ The bootstrap's spurious `E3062: unused typealias 'Picker'` on the same probe stands and belongs on the oracle list | L-types *(closed — policy, not a hole)* | ✅ DONE | `slice/BATCH6-ranged-doors` | 2026-08-01T06:52Z |
| **A2a** | **EXTEND THE SCALE-TEST CORPUS TO EMIT A *GUARDED* RANGED `return` — and know that it INVALIDATES THE DELTA COLUMN for one step.** A1h measured a real superlinearity (`findRetBlock`, ×2.92 per doubling) that the ladder read as **Δ0 in every column**, because no generated function returns a value needing a range guard. **A1h's numbers therefore live in a hand-run trigger** (`Testing/ladders/genretsites.sh`) rather than in the trend, so nothing re-checks that the cure holds — which is exactly what `genrangesites.sh` exists to prevent for the cast sites. ⇒ add a knob (the precedent is `floatSpill` 4 → 12, a corpus fix in the same spirit). ⛔ **THE COST IS REAL AND MUST BE PAID DELIBERATELY: changing the corpus changes what every rung is measuring, so the `delta` column of the next row compares two different questions and is NOT attributable.** Whoever takes this must say so in that row's note, exactly as a rebase invalidates a ladder's absolutes. ✅ Cheap and mechanical otherwise; the generator already has the shape (A1h's trigger is 20 lines) | L-testing *(`Testing/ladders/` + the corpus generator)* | ✅ DONE | `slice/BATCH4-instrument-hygiene` | 2026-08-01T04:08Z |
| **A2n** | **⛔ THE SCALE CORPUS IS COMPILED AND NEVER RUN — AND CANNOT BE RUN — SO NO KNOB CAN CATCH A MISCOMPILE.** Opened 2026-08-01 by BATCH4's A2a implementer, **measured on the baseline tree, not predicted**: `rung0.out.exe` exits **1** (its `main` returns 0 only when `acc > 0`) and **rung 1 and above do not terminate in 10 s** (the loop-nest knob's depth-4 × 10⁶ trips). Both are byte-for-byte unchanged by A2a. ⇒ **`scale-test` measures only that the ladder COMPILES**, while every hand-run ladder in `Testing/ladders/` pins an exit code precisely because the thing it is measuring could be miscompiled instead of merely slow (`genretsites.sh`'s exit-7 detector is the model). ⚠ **This is the THIRD blind spot of the same family** — the other two are the CORPUS not expressing a feature (`floatSpill` 4, `__ManagedMemory`, the ranged `return` A2a just closed) and the INSTALLERS running outside every `PhaseProbe` bracket (below) — and it is the one that lets a WRONG ANSWER through, not merely an unmeasured cost. **Fixing it is a rung**: trip counts must come down without flattening the cost curves they exist to grow, and each rung's `main` needs a pinned expected exit code the runner checks. ⚠ **ALSO IN SCOPE, same shape:** `install*Runtime` (all of them, incl. `installBuiltinConformanceRuntime`) run **outside every `PhaseProbe` bracket**, so their cost is structurally invisible to `scale-test` in ALL THREE columns — which is why A2l(2)'s fix carries a structural claim and no measured delta. ⚠ And the Maxon/Std-tier `FunctionNameIndex` E4015 has **no runnable spec case**: reaching it needs an edit to `stdlib/Builtins.maxon`, and the harness has no stdlib-overlay fixture — reachable by hand (BATCH4's review did it), untestable in the suite ⛔ **(2) THE SLICE BOARD'S ROW IDs ARE UNCHECKED, AND THE COLLISION HAS ALREADY HAPPENED TWICE IN ONE HOUR.** 2026-08-01: two agents filed different rows both called **`A2o`** (a gitignored scratch dir reddening `dotnet build`; and `Array.resize` putting a zero into a range excluding zero). Both noticed independently, **both renamed to `A2r`**, and the collision simply MOVED — a third rename settled it. Three pushes spent on a name. **This is the project's signature bug — ONE NAME, TWO DECLARATIONS, resolved by whoever pushed last — reproduced inside the ledger that tracks it**, and it is the same shape as the flat-namespace defect `A2c` found in emitted code. ⚠⚠ **MEASURE BEFORE YOU BUILD IT, because the obvious rule is WRONG and I paid for that:** a file-wide *"a bolded first table cell is a row id"* uniqueness check finds **19 duplicates that are all legitimate** — every ladder rung (`P1.0a`, `P1.2`, `P1.7a`, `P2.3`, …) appears once in the *Status at a glance* index and once in its detail row, by design. ⇒ the check must be **SCOPED to the board's own tables** (the BATCH table and *The BOARD*), which means section parsing, not a whole-file grep. ⚠ And a naive grep can *look* clean while being blind: mine did, purely because its charset excluded the `.` in `P1.7`. **The precedent to copy is `Testing/LadderIndex.maxon`** — same shape, same slot (`spec-test`'s parent), and its `namesRepeatedIn`/`nameListClause` are the two helpers this needs, so reuse them rather than writing a third copy. | **L-testing** *(`Testing/ScaleCorpus.maxon` · the runner)* | ⬜ FREE | — | — |
| **S4b** | **THE HAND-ASSEMBLED RUNTIME CHUNKS CANNOT BE PINNED, AND THAT IS THE HALF `S4` LEFT OPEN.** BATCH8 made every runtime that is a `TargetModule` FUNCTION renderable into a golden (` ```RequiredRuntime `). **A hand-assembled byte-level chunk is not a function** — `__gt_morestack` and its family never enter `module.functions`, so they have no IR to print and `--emit-ir-runtime=__gt_morestack` is **correctly refused** (measured, `exit=1`, *"this program emits no function named '__gt_morestack'"*). ⇒ their bytes are emitted into `.text` with **no golden, no `RequiredData`, and no run that distinguishes a correct chunk from a plausible one** — exactly the shape `S4` was filed for, one tier lower. **Needs a different mechanism**: a byte-range gate over emitted `.text` (the `RequiredData`/`RequiredRdata` pair is the precedent — read the LINKED image back, prefix-compare), not another renderer flag. ⚠ Do not reach for this by widening ` ```RequiredRuntime `: there is no IR body to widen it *to*  ✅ **CLOSED 2026-08-01** by BATCH8 slice 2 (main `bc235a3f3`). **THIS ROW SHOULD NEVER HAVE EXISTED**: it was filed on the diagnosis *"a hand-assembled chunk has no IR body"* — true, and irrelevant, because `FunctionCodeChunk` carries `funcName` **and** `code`. One more producer on ` ```RequiredRuntime `, not a new mechanism. `specs-shv2/emitted-runtime-body.md` now pins `mrt_panic` and `__gt_morestack` as byte dumps; **`__gt_morestack` is the exact piece the spec's own prose had called unpinnable at any spelling.** ⇒ *check for an adjacent representation before believing an unpinnability claim* | L-testing | ✅ **DONE** *(BATCH8 s2)* | on ~~`S4`~~ ✅ | — |
| **G6** | **THE HARNESS'S OWN REFUSALS ARE STRUCTURALLY UNPINNABLE — a bad spec ABORTS the suite rather than failing one case.** BATCH8 added three parser refusals (empty ` ```RequiredRuntime `, empty section pin, a pin on a `maxoncstderr` case) and the printer two more; **all five are `panic`s in `SpecParser`/`TargetPrinter`, so a committed case that triggers one takes the whole run down** — there is no `maxoncstderr` equivalent for the harness reading its own corpus. ⚠ And pinning the panic TEXT is not the cure: it embeds `SpecParser.maxon:<line>`, so it becomes a **false-red generator on every edit above that line** (measured — the panics print `TargetPrinter.maxon:185` and `:126`). ⇒ **the project's own rule (*"a refusal nothing runs is a claim, not a door"*) is currently satisfied only by HAND runs** — BATCH8's reviewer ran all five and the coordinator independently re-ran one, and that evidence expires the moment nobody repeats it. **The cure is a harness SELF-TEST FIXTURE**: a corpus of deliberately-malformed specs the runner parses in a sandbox and asserts the refusal of, without aborting the real run. ⭐ This is a *distinct feature* with its own contract, which is why BATCH8 filed it instead of riding it ✅ **CLOSED 2026-08-01** by BATCH8 slice 2 (main `bc235a3f3`): `Testing/HarnessSelfTest.maxon` + 8 fixtures under `Testing/harness-fixtures/`, each a deliberately-malformed spec parsed by a SUBPROCESS `spec-test` asserting non-zero exit and a message SUBSTRING. Wired into `runSpecPool` (the one function only a pool's PARENT reaches), so it runs on every invocation including a `--filter`ed one, and the whole change stayed inside `Testing/`. **All 8 verified to trip DISTINCT sites** — the two sharing a message cover different disjuncts of one `or`. **The fixture corpus is invisible to the ordinary walk** (3244 = 3242 + 2, not +8) and the negative control proves the check runs: hiding the corpus panics the suite. ⇒ **the rung that ADDED the refusals is the rung that gated them** ⚠ **RENAMED `G5`→`G6` 2026-08-01: it collided with the live `G5` below** (the ladder/ignored-directory row, still ⬜ FREE and senior to this one). The CLOSED row moved so the claimable name stays where it was first taken — the third instance of the unchecked-row-id defect `A2n` records, and the one case where the fix costs nothing because the row is history. | L-testing | ✅ **DONE** *(BATCH8 s2)* | — | — |
| **A2b** | **8 OF 31 LADDER SCRIPTS ARE ABSENT FROM `Testing/ladders/README.md`'s TABLE — a table that guards nothing.** Found by the A1h review, which added the two rows it could speak for (`genretsites.sh`, its own, and `genrangesites.sh`, the sibling that explains why there are two). **Still missing: `genborrow.sh`, `gennest.sh`, `genstring-grapheme.sh`, `genstringviews.sh`, `gentuples.sh`, `genwhitelist.sh`.** ⭐ The directory's own thesis is *"a headline nobody can re-run is not a result"* — a script absent from the index is a measurement nobody will find, which is the same failure one step earlier. ⚠ **The cure worth having is DERIVED, not transcribed**: a table hand-maintained beside 31 files will drift again (it already has, silently, for six of them). Prefer generating the table, or a check that fails when a script has no row — the same argument that retired PLAN.md's own hand-updated index in favour of per-rung detail rows. ✅✅ **CLOSED 2026-08-01** (BATCH4, main `38397b45c`) — **and the cure is the CHECK, not a generated table, for a reason worth keeping.** `Testing/LadderIndex.maxon`'s `requireLadderIndexComplete()` lists the directory, parses the README's rows, and fails in **three** directions (script with no row · row naming no script · two rows for one script). ⭐ **Only the NAME column is derivable**: SHAPE and *the question it answers* are the entire value of the index — `genwidelive.sh`'s row explains that its two modes are a shape and its control, which no header field could say — so generating them would put the same sentence in a second machine-readable place, which is the disease. **The derivable half is derived; the prose half is merely required to EXIST.** ⭐ Wired into **`spec-test`'s parent**, beside `requireRunnerAvailable` — the project's most-run gate, one `Directory.list` and one file read, **zero per-test cost** — because a check nobody runs is the very thing this row complained about. **Seen RED in all three directions before it was trusted**, by both the implementer and the independent review. The six missing rows were written from each script's own header, not its filename | *(docs — no compiler lane)* | ✅ DONE | `slice/BATCH4-instrument-hygiene` | 2026-08-01T04:08Z |
| **A1f** | **THE ONE PATH THAT REACHES A WRONGLY-ELIDED DIVIDE GUARD — a runtime 0 into a ranged parameter.** Found by the A1 review answering the coordinator's question *"what can reach `neverZero` wrongly?"*, and probed rather than reasoned: **four** of the five doors into a ranged binding emit a runtime guard (`as` cast, field store, ranged `return`, `var` reassign — all panic correctly), and the fifth, a **CALL ARGUMENT**, owes only the compile-time half by design (`InsertRangeChecks.processArgSite` / `RangeCheckGuard.compileTimeOnly`). So a runtime `0` passed into a `d NonZero` parameter — or `push`ed into an `Array with NonZero` — reaches an UNGUARDED `idiv` and dies `panic: integer divide by zero` instead of throwing. ✅ **NOT an shv2 divergence: the bootstrap emits the identical bare divide and traps identically**, so it is the language's unenforced premise rather than a port defect, which is why A1 recorded the measurement in `divisorZeroVerdict`'s header instead of smuggling a fix. ⚠ The cure belongs to the ARGUMENT door — a guard at every call site with a ranged parameter — which is a large blast radius and its own rung, not a division change | L-types *(`InsertRangeChecks.maxon` · the arg-site door)*  ✅✅ **CLOSED 2026-07-31** (main `0c902a761`; x64-windows 2983 → **2990/0**, x64-linux **2869/0**, wasm32-wasi **2829/0**, arm64 SKIP — remote, UNVERIFIED; 0 warnings; no exit 101). **A narrowed ranged PARAMETER is now enforced at runtime, at the CALLEE'S ENTRY.** ⚠ **THE ROW'S DOOR LIST WAS WRONG**: there is no `var reassign` door and there structurally cannot be — a local's type is inferred from its initializer (`Parser.maxon:11448`) and `resolveNamedType` erases a ranged alias to its primitive. The five doors are `as` cast, field store, field **default**, ranged `return`, call argument. ⭐⭐ **THE POINT IS A PROOF'S SOUNDNESS, NOT THE DEATH.** `/` and `mod` ARE throwing operations unless the divisor is *proven* non-zero — and a `NonZero` parameter was treated as proven (`Parser.maxon:21430-21441`), so A1's guard was ELIDED on a premise nothing enforced, turning a catchable throw into an uncatchable hardware fault (`safety.md:21-22` — no `recover()`). **Enforcing the premise is what makes the elision legitimate.** Coordinator-verified before and after: the same program went from `panic: integer divide by zero` to `panic at :12: Range check failed: value outside typealias 'NonZero'`. ⚖ **RULING — GUARD AT THE CALLEE'S ENTRY, NOT THE CALL SITE.** The call-site placement fights a STRUCTURAL obstacle (`Project.maxon:960-968`: a guard would land PAST the call, after the callee already ran) and scales with CALLS; the entry block splits cleanly, reuses `emitGuardAt`/`emitCascade`/`emitPanicBlock` unchanged, and costs one guard per narrowed parameter per function. **The review's `argument-through-a-function-value-is-guarded` case is what justifies it**: `main → apply → __fnref_divide → divide`, where no call site on the route knows the word `NonZero`. ⛔ **CLOSING THIS DOOR NEARLY DELETED THE FAULT RUNTIME'S COVERAGE** — afterwards NO program in the suite printed `panic: integer divide by zero`, so the `0xC0000094` classification arm would have lost its last test **inside a green suite** (the [[a1g-fault-classification-closed]] trap, rediscovered independently). Cured by pinning the surviving hole: the **ARRAY-ELEMENT door**, where the element travels as `__arr_push`'s argument into a shared `Array` body whose parameter is the OPAQUE element type — so there is no narrowed parameter to stand behind, and A1f's cure structurally cannot reach it ⇒ **A1f-arrayelem**. **The review proved the coverage by SABOTAGE, not by reading**: breaking `0xC0000094` fails exactly ONE test, breaking `0xC0000095` fails exactly TWO, and the sets are disjoint. ⛔ **THE REVIEW'S TOP FINDING — the rule "which doors owe the runtime half" was written in SIX places and this rung updated TWO.** The worst stale copy sat on `divisorProof` (`Parser.maxon:21435`) itself, still telling a future reader that `neverZero` off a `NonZero` parameter is unsound — **the very comment A1 wrote to record the hole was the one that had to change.** All five rewritten to state the new rule once, each pointing at the array-element survivor. ⭐ **FOUR COORDINATOR SUSPICIONS, ALL UNFOUNDED, EACH CHECKED AT THE MECHANISM**: the guard is not elidable (the `param` op has it as a use — pinned anyway); the anchor `stdOpPos = func.paramCount` is exactly past `emitParams`, probed across receiver-at-0, closures, `__env`, layout-descriptor slots, `throws`, `async` and a 7-param chkstk frame, and composes in SOURCE order; **parameter mutation cannot happen at all** (`E2013`, and there is no typed-local syntax); no second spelling survived. ⚠ **A LEAF FUNCTION WITH A NARROWED RANGED PARAMETER STOPS BEING A LEAF** — the panic block calls `mrt_panic`, so it now pays shadow space. A real per-function cost, and hot stdlib helpers (`Character`, `Json`, utf8/utf16) take it. ⚠⚠ **THE LADDER CANNOT SEE ANY OF IT**: `ScaleCorpus` declares no narrowed ranged parameter, so not one entry guard is emitted and the measured deltas are recording-side only — the instrument's blind spot, same class as `floatSpill 4 → 12`; the knob belongs to **A2a**. ✅ **THE CONTROL THAT KEEPS THE COST HONEST**: a FULL-range parameter is still a leaf — no `__rc_*`, no prologue — because `rangeIsFull` discards the alias before a cascade is built. **A parameter that promises nothing costs nothing.** ⚠ 283 goldens MODIFIED (+14 added, 3 renamed), every one gaining a `__rc_panic` block; the review re-verified mechanically that every removed line is the shared `nextStringId` renumber and nothing rode along. **arm64 goldens are STALE and owed to the periodic Mac sync: 110 `arm64-macos` + 34 `arm64-linux`.** ⚠ Residuals filed as their own rows: **A1f-dupguard**, **A1f-arrayelem**, **A1f-e9001**, **A1f-aliasclash** | ✅ DONE | `slice/A1f-ranged-arg-guard` | 2026-07-31T23:17Z |
| **A1f-dupguard** | **A PARAMETER GUARD AND A RETURN GUARD FIRE OVER THE SAME VALUE AGAINST THE SAME ALIAS.** `function check(x SmallInt) returns SmallInt / return x` now emits the entry cascade AND the identical return cascade — correct, but paid twice, and visible in `range-check-panic.in-range`'s fragment as two full cascades over `rcx` with two blobs. ⚖ **COORDINATOR RULING, A1f: NOT FIXED THERE, DELIBERATELY.** The proposed cure (skip a return guard whose value is a parameter already guarded against the same alias) is probably sound — in SSA the same `ValueId` is the same value — **but A1f exists precisely because a guard was ELIDED on a premise nothing enforced, and a rung with that thesis must not close by shipping a fresh elision resting on a fresh premise, argued in a paragraph, on its last day.** ⇒ it gets its own rung, its own soundness argument, and **sabotage proving the guard still fires when the premise does not hold**. The redundancy is documented at `InsertRangeChecks.guardRangedParam` and in `range-check-panic.md` so the next reader knows it is deliberate. ✅✅ **CLOSED 2026-08-01 — THE ELISION EARNED ITSELF** (BATCH6, main `b699c736d`). Four named clauses (same VALUE by SSA identity, since E2013 forbids parameter mutation · same ALIAS · DOMINANCE · an entry guard exists), and **the seam that could have made it unsound was closed by construction, not by luck**: both sides resolve the alias through the SAME `checks.filePath` reader, so equal names give a byte-identical declaration whatever `A1f-aliasclash` did to resolution one commit earlier — a name compare is safe HERE and would not be if either side used a different reader. **SABOTAGED IN BOTH DIRECTIONS BY BOTH AGENTS**: breaking the alias clause let `50` escape an `int(0 to 10)` (1 failure); breaking the value clause let `99` and `15` escape (3 failures). The review added the probes the implementer had not — an INSTANCE method (where `self` occupies slot 0, the likeliest off-by-one), mixed elided/non-elided returns in one function, and a 3-file contested alias crossed with the elision. **Payoff: 19 return cascades removed across 20 goldens per x64 target** | L-types *(`InsertRangeChecks.maxon`)* | ✅ DONE | `slice/BATCH6-ranged-doors` | 2026-08-01T06:52Z |
| **A1f-arrayelem** | **THE ARRAY-ELEMENT DOOR IS THE LAST UNENFORCED RANGED PREMISE — and it is structurally out of A1f's reach.** `xs.push(ident(0))` into an `Array with NonZero` stores an out-of-range element with no guard, and `100 / d` on the value read back is a bare `idiv` → `#DE`. **Why A1f's cure cannot reach it**: the element travels as `__arr_push`'s argument into a SHARED `Array` body whose parameter is the OPAQUE element type, so there is no narrowed parameter for an entry guard to stand behind (stated at `requireArrayElementType` and in `RangeCheckGuard`'s header). ⚠⚠ **THIS DOOR IS CURRENTLY THE SOLE TEST OF THE `0xC0000094` FAULT-CLASSIFICATION ARM** (`safety.divide-by-zero-fault-through-an-unchecked-array-element`, sabotage-verified: break that arm and exactly this one test fails). **So whoever closes it MUST first give the fault thunk another route to that arm, or a green suite will silently lose it** — the [[a1g-fault-classification-closed]] trap, which A1f hit and cured once already. ⇒ the cure is probably a guard at the STORE rather than at a callee entry, which is a different mechanism. ✅✅ **CLOSED 2026-08-01** (BATCH6, main `b699c736d`) — **and HALF THE FILED REASON WAS WRONG, which is what made the fix small.** The callee-entry cure genuinely cannot reach this door (opaque element type in a shared `Array` body), but the other half — *"a guard would land PAST the call"* — is false HERE: a general call argument has no alias at parse time, while **an element's alias is right there in the declared element type**, and `requireArrayElementType` runs BEFORE the store call is emitted, so the site's ordinal is the one `__arr_push` is about to take. The site simply asks for the runtime half. `RangeCheckSite.guard` deleted with the change that made it constant — every positioned site now owes both halves. Coordinator-verified: `panic: integer divide by zero` ⇒ `panic at :10: Range check failed: value outside typealias 'NonZero'`. ⭐ **THE TRAP WAS DISARMED FIRST, AS THE ROW DEMANDED, AND BOTH AGENTS SABOTAGED IT INDEPENDENTLY**: flipping `ExcCodeIntDivideByZero` 0xC0000094→0xC0000091 failed **exactly** the fault-classification cases and nothing else in 3021 — before the guard landed, and again after, on the new route. ⛔ **AND THE NEW ROUTE IS A LIVE DEFECT OF ITS OWN ⇒ `A2s`**: `Array.resize` exposes zero-initialised slots, so an `Array with NonZero` acquires a `0` with **no value crossing any door**, and no store guard can ever close it | L-types *(`InsertRangeChecks.maxon` · the array-store door)* | ✅ DONE | `slice/BATCH6-ranged-doors` | 2026-08-01T06:52Z |
| **A2w** | **⛔ A WITNESS DISPATCH DOES NOT FILTER `static` REQUIREMENTS, SO AN INSTANCE CALL BINDS A STATIC SLOT AND PASSES A SPURIOUS RECEIVER.** Found by BATCH5's review, **measured, and confirmed PRE-EXISTING by reading the base commit** (`5c1adf969` had no `isStatic` test either — the gap dates to R9 making static requirements possible; R10c only widened the reachable set to INHERITED statics). With `interface Base` declaring `static function origin() returns Code`, `self.item.origin()` inside a generic body **compiles and returns the right value** — it passes a receiver in the first argument register that the static impl ignores. ⚠ **It is right only by ABI luck, and only on register targets**; the DIRECT form of the same call is refused (`E3036: 'Widget.origin' expects 0 argument(s) but 1 were provided`), so **the witness path silently accepts a program the language rejects**. ⇒ needs a RULING before code: is `T.origin()` through a constraint a supported form at all? Neither existing message is true of the case, so it also needs a diagnostic ⚖ **RULING, MEASURED BY THE COORDINATOR BEFORE CLAIMING (2026-08-01) — REFUSE, and the false-reject sweep IS the rung.** The row asked whether `T.origin()` through a constraint is a supported form at all. **The oracle refuses BOTH spellings**: `T.origin()` gives a clean, deliberate **`E3003: 'T' is a type and cannot be used directly as a value`**, and `self.item.origin()` gives **`E4006: Primitive type 'int' has no method named 'origin'`** — refused, but by a message that reveals the bootstrap simply does not model the case, which is exactly this row's *"neither existing message is true"*. **And the corpus has no case doing it**: every `static function create` in `specs/interfaces.md` is called on the CONCRETE type (`Point.create(10, y: 20)`), never through a type parameter. ⇒ not ambiguous — unsupported, and shv2 silently accepts a program the oracle rejects. ✅✅ **CLOSED 2026-08-01** (main `70804dafb`; x64-windows **3108/0** on the merged tree, x64-linux **2973/0**, wasm32-wasi **2924/0**, arm64 SKIP — remote, UNVERIFIED; 0 warnings; no exit 101; fragments **additions only, zero modified**). **TWO DOORS, and the second was worse than this row described.** Door 1 (`Parser.requireWitnessDispatchCandidates`) filters statics BEFORE arity selects, so a same-named instance requirement still binds; new **E3116**. Door 2 (`IrInterface.witnessTargetIsProtocol`) compared result type, `throws` and formals but **not the receiver kind**, so a `static function equals(other Self) returns bool` passed the shape test for `Equatable`'s INSTANCE method — **compiled clean and returned `true` where the answer is `false`, a SILENT WRONG ANSWER with no ABI luck involved**; reuses E3005. ⚖ **The ruling was MEASURED before the claim, not chosen** (see above). ⭐ **The false-reject sweep WAS the rung and it is EMPTY**, re-run independently by the review with an awk state machine over interface BODIES — a loose grep opens **285 false interfaces** in `.md` prose — finding 36 static-bearing sites across 12 names, **not one named in a `where` clause in any tree**. Escape hatch pinned and proven a real control (invert the predicate and it reddens with 20 others). ⛔⛔ **THE REVIEW FOUND THE REFUSAL WAS CONTINGENT ON NOTHING ELSE IN THE FILE FAILING**: E3116 was RECORDED, not thrown, and the refused statics were handed back to arity selection — a wrong-arity static reported `E3036` alone, two same-named statics `E3114` alone, and **any unrelated later error replaced it entirely**, because `abortedParseArtifact` builds a fresh artifact and discards `artifact.diagnostics`. Now thrown. ⛔⛔ **And the reason it was recorded was a FALSE CLAIM IN THE SOURCE** ⇒ **A2x**. ⛔ **A third: `isStatic` became parse-visible and did not key the parse memo** — **1 re-parse where the property demands 8**, invisible to `verify-warm-rebuild`'s four probes (all move a NAME or a VALUE); a fifth probe moving only the keyword was needed. Now 8 of 8. ⚠ **No wasm regression guard is possible**: the new cases are struct/generic and the wasm suite is scalar-only, so the wasm trap is a BEFORE measurement nothing will re-check until wasm gains structs | **L-types** *(`findWitnessDispatch` · the requirement filter)* | ✅ DONE | `slice/A2w-static-witness-filter` | 2026-08-01T12:08Z |
| **A2x** | ⛔⛔ **A FALSE CONSTRAINT IS WRITTEN IN THE SOURCE, AND IT HAS ALREADY COST A DESIGN.** `Parser.maxon:8944` (`readGenericInstanceAlias`'s header) asserts that `ParseError` *"sits at exactly 64 String-carrying arms — a 65th overflows the C# runtime's u64 droppable-case bitmask"*. **Measured during A2w: it carries 96 String-carrying arms, and the 97th builds and runs clean** (`staticRequirementNotDispatchable`, suite 3108/0, no wild-free, no `mm_decref` panic). ⛔ **The cost was real, not theoretical**: A2w's implementer chose `recordArtifactDiagnostic` over a thrown `ParseError` specifically to avoid a 97th arm — and a recorded diagnostic turned out to be swallowed by three shapes, because `abortedParseArtifact` discards `artifact.diagnostics`. The review had to undo the whole delivery. ⚠ **The same false sentence is in the coordinator's memory note** (`maxon-sharp-64-union-arm-bug`), now corrected there. ⚠⚠ **DO NOT simply delete the comment** — the ORIGINAL claim (P1.6-A, 2026-07-21) was about the BOOTSTRAP tracking a union's *droppable/managed* cases in a u64 bitmask, and two readings survive today's measurement: the bootstrap bug was fixed, **or** *String-carrying* was never the same predicate as *managed/droppable*, which would make the arithmetic wrong and the mechanism real. ⇒ **re-measure the bootstrap's actual droppable-case tracking, then either replace the comment with the answer or delete it** — and note the rule this proves: a ceiling worth designing around is worth MEASURING, which took one build here | L-parser-decl *(+ `maxon-sharp/` to re-measure the real limit)* | ⬜ FREE | — | — |
| **A2y** | **TWO CONSECUTIVE LADDER ROWS CARRY AN UNATTRIBUTED `+14`, AND NOBODY HAS SPENT A BUILD ON IT.** R10b's row and A2w's row each read **+14 flat at all six rungs** in the delta column, while each rung's OWN cost measured **0** by a same-checkout A/B against its own parent. ⚠⚠ **The obvious explanation is REFUTED**: S5's row tested the checkout-path hypothesis directly — an 8-character path difference moved **ZERO** allocations — so the allocation column is path-independent, exactly as the instrument claims, and these constants are NOT a path artifact. ⭐ BATCH6's row saw the same shape first (+16 own vs +2 row) and named the rule that keeps both honest: **attribution belongs to a diff against a rung's OWN parent; the delta column belongs to whatever landed before it.** That rule explains why the two numbers differ; it does not explain why the difference is *14, twice*. ⇒ a cheap, bounded investigation: A/B the rows either side of each +14 at ONE path and find what actually moved. ⚠ Candidate worth checking first, from BATCH6's own note: geometric Map capacity quantization, where a slightly larger input crosses a growth boundary — it would produce exactly this shape (a flat constant, independent of rung) ✅✅ **CLOSED 2026-08-01 — FOUND, REPRODUCED AND ATTRIBUTED. It was ONE EMPTY, GITIGNORED DIRECTORY: `stdlib/.maxon`.** Not a commit, not a code change, and **not** path length. `collectMaxonSources` recurses into every subdirectory of `stdlib/`, dot-directories included, at a measured **14 allocations and 829 bytes per stray DIRECTORY** (10 for the entry, 4 for the recursive `Directory.list`), all in `phase:load` with matching frees — no leak. `stdlib/` is walked **once per compile**, which is exactly why it read FLAT at every rung. The directory was v1's orphaned stdlib cache, empty since 2026-07-13, matched by `.gitignore:27` — so **`git worktree add` never creates it** and no current build recreates it. **Coordinator-verified end to end**: ladder 683,366 → `rmdir` → 683,352, exactly S5's row, −14 and −829 flat at all six rungs. ⭐⭐ **AND IT RETRO-CORRECTS A CONCLUSION THIS LOG HAD RECORDED**: normalising the main-minted rows by −14 makes the delta column agree with **every** own-parent A/B in the run, so **BATCH6's `+16 own / +2 column` becomes `+16 / +16` and its "the two rungs do NOT compose additively" was itself the artifact.** R10b `0`, A2w `0`, BATCH5 `+5`, S5 `0` all reconcile. ⭐ **Path length refuted for the allocation column a SECOND time, directly**: a replica checkout at exactly 100 characters (main's own length) and at 144 both read 683,352. ✅ **Nothing was optimised and no compiler behaviour changed** — the deliverable was the explanation. ⇒ **A2z** (the walker that has no exclusions) and **G5** (the instrument cannot see ignored directory content) | L-testing *(`ScaleCorpus`/instrument-adjacent; the finding may land anywhere)* | ✅ DONE | `slice/A2y-unattributed-fourteen` | 2026-08-01T14:00Z |
| **A2z** | **`collectMaxonSources` WALKS EVERYTHING — no `.maxon/` output-dir, `.maxonignore` or `*.test.maxon` exclusion — WHILE THE AUTHORITY THAT BUILT THE BINARY EXCLUDES ALL THREE.** `Compiler.maxon:664-676` recurses into **every** subdirectory of a project root, dot-directories included; the bootstrap's `SourceCollector.ExclusionFor` (`maxon-sharp`) skips an output `.maxon/`, `.maxonignore`d subtrees and `*.test.maxon`. ⭐ **This is the direct cause of A2y's `+14`**: an empty, gitignored `stdlib/.maxon` was recursed into on every compile, at a measured 14 allocations and 829 bytes per stray directory. ⛔ **Today it costs a constant and risks nothing; it becomes a DEFECT the day anything writes a `.maxon` file under an output directory — that file would be silently COMPILED INTO the program.** Output dirs currently hold `build-manifest.json`, not sources, which is the only reason this is latent. ⚠ **Two other readers already re-apply the missing clauses ON TOP by hand** — `Testing/CompilerFreshness.maxon` (G2, which had to mirror them and lost a clause twice doing it) and the G2 review's note — so the rule is written in three places and derived in none ⇒ see **G3**. ⚠ Also case-sensitivity: `collectMaxonSources` compares `build.maxon` and the `.maxon` extension case-sensitively while the bootstrap folds, so a `Build.maxon` is excluded there and swept here. `Compiler/**` is BATCH2's lock today | L-parser-decl *(`Compiler/Compiler.maxon`)* | ⬜ FREE | — | — |
| **A3a** | ⛔ **FOUR COMMITTED GOLDENS THAT NOTHING RUNS, AND A SABOTAGE PROVED IT.** Opened 2026-08-01 by BATCH1's coordinator while checking a review finding, **measured not predicted**: appending a junk line to `specs/fragments-x64-windows/advent/day1.test` left a full C# suite at `3229 verified, 0 written`, **exit 0** — after `A1s-csgolden` had made every other golden a real gate. Cause: `specs/advent.md`'s front matter is **`status: selfhosted`**, so the bootstrap skips the file, and **`maxon-selfhosted` has not built since 2026-07-13** (`E3005` in `X64RegisterAlloc.maxon`, accepted and not being repaired). ⇒ its 4 tests run **nowhere**, while 4 fragments sit in the tree looking like coverage. ⭐ **The same query answers a bigger question**: how many OTHER spec files carry `status: selfhosted`, and how many goldens do they own? That census is the rung, not the one file. ⚠ **DO NOT just delete them**: the `RequiredIR:x64-linux` blocks inside are **valid for v1**, which does have an ELF writer — a review proposed deleting them as dead and that was **measured wrong**. The options are re-home to a compiler that runs (bootstrap or shv2), or mark the file's coverage as knowingly-suspended with the reason, not silently drop it | **L-testing** *(`specs/` census + `SpecParser`'s `status:` handling)* | ⬜ FREE | — | — |
| **A3b** | **THREE DEBUG-SAMPLE GOLDENS ARE STALE FROM `d7a7c96c3`, AND THEY ARE IN B2'S LANE, NOT MINE.** Opened 2026-08-01 by BATCH1, which **proved it with a control rather than an argument**: `asyncbp`, `gtcontrol` and `threads` fail `scripts/check-debug-goldens.sh` on a detached `origin/main` worktree carrying **none of BATCH1**, and identical BATCH1 content was **53/53 green before rebasing** onto that commit. The diff is **offset-only** — frame 1 moves `0xf9b2` → `0xfbef` (+573 bytes) and, with offsets normalized, **zero** lines differ in any of the three. Cause: `d7a7c96c3` *"fix(netpoll): the instrument was DEAD on x64…"* (2026-08-01 07:34) changed the emitted GT runtime's code size; the three goldens were last minted **2026-07-26** and it never re-minted them. ⭐ **Why BATCH1 did not fix it**: `Targets/*GtRuntime*` is **`L-gt-runtime`**, claimed by **B2**, and B2's own landing will move these offsets again — re-minting now buys a golden that is stale twice. ⇒ **B2 mints them at its close**, or this row does it after B2 lands. ⚠ Note the shape: a commit that changes emitted-runtime SIZE owes every golden that pins a runtime frame offset, and nothing enforces that today | **L-gt-runtime** *(`maxon-sharp/DebugSamples/*.expected.txt`)* | ⬜ FREE *(blocked on B2)* | — | — |
| **A3c** | **THE BOOTSTRAP HAS NO CALLEE-ENTRY RANGE GUARD, so a ranged parameter's premise is unenforced at the call — A1f's fix, one compiler over.** Opened 2026-08-01 by BATCH1's `A2h` implementer, which **measured its way into it**: `divide(10, d: z)` where `d` is a `NonZero` parameter and `z` is a runtime `0` still panics **`integer divide by zero`**, not the range panic — so shv2's ported case `mod-overflow-premise-enforced-at-the-callee-entry` (and its divide twin) **cannot be ported**, and the agent **correctly declined to write a vacuously-passing variant**. ⭐ This is exactly the `A1f` shape the shv2 side already closed (*"a PROOF resting on an unenforced premise"*), so the design question is answered — what is missing is the bootstrap's implementation and its spec port. **Gate is the full C# suite (3229+) plus codegen neutrality**, now a real proof rather than a glance since `A1s-csgolden` landed | **L-bootstrap** *(`maxon-sharp/Compiler/2-Parser.cs` — the call door)* | ⬜ FREE | — | — |
| **A3d** | **DEBUG INFO IS NOW GATED FOR ONE COMPILE AND MEASURABLE FOR ALL — the two gaps that leaves.** Opened 2026-08-01 by BATCH1's `A1k`, which closed the crash and **measured the rest of the path clean (3219/3219 with debug info ON, 3219 goldens verified)**. What remains is coverage, not correctness: (1) **`MAXON_SPEC_DEBUG_INFO=1` is an INSTRUMENT, not a gate** — nothing runs it, and promoting it to a real `spec-test --debug-info` flag needs `Program.cs`, which was outside that agent's file list; (2) **`FunctionCloner` copies no `_debugSpans`**, so **every monomorphized generic and every synthesized `clone()` has an EMPTY line table** — a debugger cannot step in a generic, and no test asserts otherwise; (3) ⛔ **nothing anywhere asserts `.mxdbg` line-table CONTENT** — `specs/debug-info-spans.md` pins the *crash*, and `check-debug-goldens` has teeth only on straight-line samples (**no sample uses an array `get`, a `try`, or a checked divide**), which is precisely why the review's `StoreForwardingPass` span-drop was invisible to a 53/53 green gate. ⇒ item (3) is the one that matters: a golden that pins spans through a block-splitting lowering | **L-testing** *(`Testing/` · `Program.cs` · `MLIR/Passes/FunctionCloner.cs`)* | ⬜ FREE | — | — |
| **G5** | **THE LADDER CANNOT SEE IGNORED DIRECTORY CONTENT, WHICH GIT GUARANTEES WILL DIFFER BETWEEN THE MAIN CHECKOUT AND EVERY WORKTREE.** `mintedAt` keys the byte column on the checkout PATH — correctly — but nothing records what was actually *walked*. A2y cost a full investigation to discover that one empty gitignored directory, present in main and in no worktree, had been adding a flat `+14` to every main-minted row for weeks, and had already caused **a wrong conclusion to be recorded** (BATCH6's *"the two rungs do NOT compose additively"*, which was the artifact). ⇒ **the honest cure is an OPERATOR-READABLE COUNT, not a gate**: print the number of stdlib source files found and directories walked beside each run, so the next discrepancy is one line of the output rather than a bisect. ⚠ **Deliberately NOT a verdict** — this instrument renders none, and a count that refused would be the gate apparatus this project already deleted once. It is a number a reader compares, nothing more | L-testing | ⬜ FREE | — | — |
| **A2u** | **ONE INTERFACE MAY NOT DECLARE TWO SAME-NAMED REQUIREMENTS, WHILE AN `extends` EDGE NOW MAY — AND THE ASYMMETRY IS A KEY THAT LACKS ARITY.** `Parser.requireUnclaimedInterfaceRequirementName` refuses `label()` beside `label(width)` in one interface; after BATCH5 the identical pair across a parent/child edge **works and is pinned green**. ⭐ **The true blocker is not the refusal but `witnessSlotImplKey`, which is `(conformer, interface, method NAME)` with no arity** — two such slots would contend for one filing, and the filing is what stamps every slot's address. ⚠ **BATCH5's review rewrote that refusal's justification, which had cited a rule this rung falsified and a function this rung deletes**, and pinned the true reason with a spec so the day the key widens it turns RED. ⇒ closing it means widening the impl key, which **moves R10's E3111 conflicting-conformance detection with it** — a rung, not a line | **L-types** *(`witnessSlotImplKey` · `ConformanceCheck`)* | ⬜ FREE | — | — |
| **A2s** | **⛔ `Array.resize` PUTS A ZERO INTO A RANGE THAT EXCLUDES ZERO, AND NO STORE GUARD CAN EVER CLOSE IT — because NO VALUE CROSSES A DOOR.** Opened 2026-08-01 by BATCH6's `A1f-arrayelem`, which found it while looking for a replacement route to the `0xC0000094` fault-classification arm — i.e. **it was found by the trap the row itself warned about, not in spite of it.** Growing an `Array with NonZero` EXPOSES zero-initialised slots, which `stdlib/Array.maxon` documents as elements for exactly the inline types a ranged alias resolves to, so the array acquires a `0` with nothing to stand in front of, and `100 / d` on it is a bare `idiv` → an uncatchable `#DE` where the language promises a catchable `DivisionByZero`. ⭐ **IT IS PINNED, NOT HIDDEN**: `specs-shv2/safety.md`'s `divide-by-zero-fault-through-a-resized-array-slot` is now **the suite's SOLE test of the 0xC0000094 arm** (sabotage-verified twice, by the implementer and again by the review) — so **do not delete that case casually**, and whoever closes this door owes the arm another route FIRST, exactly as BATCH6 did. ⚖ **THE CURE IS A LANGUAGE DECISION, NOT A GUARD**: refuse `resize` for an element range that excludes its zero, precisely as **E3106** already refuses it for a MANAGED element the compiler cannot invent (*"a grown slot holds NO element — Maxon has no default constructor"* — the same sentence, one type-class over). Needs its own diagnostic, a false-reject sweep over the corpus, and goldens | **L-types** *(the array-grow door · sibling of E3106)* | ⬜ FREE | — | — |
| **A1f-e9001** | ✅✅ **CLOSED 2026-08-01** by BATCH1 (main `d4cd6aa20`). ⛔⛔ **THE ROW'S PREMISE WAS FALSE, AND SO WAS THE COORDINATOR'S REPLACEMENT FOR IT — THREE WRONG CAUSES FOR ONE DEFECT.** (1) The row: *"a `continue` earlier in a loop body poisons the if-merge phi… delete the `continue` and it compiles"* — **measured false**: the minimal repro has **no loop and no `continue`**. (2) The coordinator: *"a by-ref/mutating receiver"* — **also false**: a `Box.show()` that only prints fails identically. (3) The truth: **STATEMENT POSITION**. `VarInfo.Value` is usable only where it dominates the read; expression-position receivers all asked `ResolveExprValue` that question, and **FOUR statement-position paths each kept their own copy — THREE omitted the check entirely** (`:8437` builtin, `:12656` instance method, `:12741` type-param method; `:8517` was already right). All four now route through one `ResolveReceiverValue`. ⛔⛔ **THE ROW CITED A WORKAROUND THAT DOES NOT EXIST** (`materializedParamSites`/`materializedParamAliasNames` — zero hits repo-wide), and the coordinator's own citation `_refParamPtrVars` does not exist either. ⭐⭐ **THE FIRST FIX SHIPPED A SILENT SAFETY REGRESSION AND THE IMPLEMENTER CAUGHT IT**: routing unconditionally through `ResolveExprValue` mints a fresh op even in the defining block, which put a `struct_var_ref` between a self-field alias and its `field_access` — the chain the MUTATION ANALYSIS walks — so `List.clear` stopped reading as mutating and three `value-survives-clear` cases went from correctly REJECTED (E3070) to **compiling clean**. ⚠ The implementer also **misattributed that regression once** (blamed the skimmer unification, reverted it, still red — the revert is what disproved them). ⭐ **A SECOND, REAL defect fixed alongside**: `ParseBreak`/`ParseContinue` left `_currentBlock` on a terminated block and neither was in `lastTerminatorKeyword`, so `break` followed by `total = total + 100` **compiled clean**, appending ops after a terminator. Both now under the existing E3071 — no new code. **⛔ A `continue` EARLIER IN A LOOP BODY POISONS THE `if`-MERGE PHI FOR A `var` REASSIGNED IN A LATER `if` — a valid program refused with a bogus `E9001`.** Found by the A1f implementer, whose first two drafts were rejected by it; **minimized to a standalone repro**: `error E9001: in 'build', op 'maxon.call @stdlib.Array.reserve' in block 'firstOne_2.merge' reads %13, which is defined in block 'firstOne_2' — and 'firstOne_2' does not dominate 'firstOne_2.merge'`. **Delete the `continue` and the identical program compiles.** ⭐ **AND IT CORRECTED A COMMENT THAT NAMED THE WRONG CAUSE**: the existing workaround (`materializedParamAliasNames`, now `materializedParamSites`) blamed the *padding loop* it happened to contain — the loop is incidental, the `continue` is the trigger. A comment that misnames a cause is worse than none. ✅ **Re-verified by the A1f review**, which inlined the helper and reproduced the E9001 exactly rather than believing the note. ⚠ **`maxon-sharp/`, so it needs the FULL C# suite (2883+) plus codegen neutrality as its gate** — which is why it is a row and not a fix-now | **L-bootstrap** *(`maxon-sharp/` SSA construction · the if-merge phi)* | ▣ **BATCH1** | — | — |
| **A1f-aliasclash** | **⛔ A USER `typealias` MAKES A STDLIB FUNCTION PANIC ON A LEGAL VALUE — cross-file ranged-alias resolution is LAST-WINS.** Measured by the A1f review: a user `typealias Codepoint = int(0 to 100)` makes `stdlib`'s `utf16LeadSurrogate(codepoint Codepoint)` — whose `Codepoint` is `stdlib/Character.maxon`'s `int(0 to 1114111)` — **panic on a perfectly legal `70000`**. Root: `RangedAliasRegistry.lookup` (`Project.maxon:648`) falls through, for a name the reader's file does not itself declare, to a bare `byName` door that is last-wins. ✅ **PROVEN PRE-EXISTING THREE WAYS, so it is NOT A1f's**: the control with the alias merely RENAMED passes; a collision on an alias the callee's own file DOES declare resolves correctly; and **the identical false panic reproduces on the PRE-A1f binary** through the `return` door (`utf16.maxon:67`). A1f adds one more door to one pre-existing gap and changes neither its existence nor its severity class. ⚠ `lookup`'s own header already names this as the open **E3063** cross-file ambiguity, and E3063 exists in `docs/error-codes.txt` claimed by csharp/selfhosted but **not shv2** ⇒ the correct resolution rule is a whole-program design decision with its own false-reject sweep and goldens, and fixing it piecemeal would pre-empt that rung. Reproducer recorded at `InsertRangeChecks.guardRangedParam` so it is greppable. ✅✅ **CLOSED 2026-08-01** (BATCH6, main `b699c736d`) — **and the filed root cause was wrong in a way that made the fix SAFE rather than sweeping.** `lookup` ALREADY scoped by the site's declaring file; the defect is its **FALLBACK**, taken when the reader's own file declares no such name, which was a bare last-wins door. Cure: consult the **`nameable`** index — declarations some file EXPORTED, or the compiler supplied — before the bare door, because **a declaration a third file MAY NOT NAME cannot be the one it meant**. It was never a tie between equals. ⭐ **THE E3063 BOUNDARY IS NOT CROSSED, AND THE REVIEW PROVED IT STRUCTURALLY, NOT BY EXAMPLE**: `nameable` and `byName` are upserted in the same `record` call, `nameable` first, so its key stream is a SUB-SEQUENCE of `byName`'s and their last-wins winners cannot diverge ⇒ two files that both EXPORT one name still answer exactly as before, on a strictly smaller candidate set. Coordinator-verified: the legal call that was refused now compiles and returns the right answer, and `small(150)` is still refused against the user's own alias. False-reject sweep clean (a user `export ElementCount`, a user `export Codepoint`, exported-narrow + private-wide + third file) | L-types *(`Project.maxon`'s `RangedAliasRegistry` · E3063)* | ✅ DONE | `slice/BATCH6-ranged-doors` | 2026-08-01T06:52Z |
| **A1g** | **shv2's FAULT THUNK CLASSIFIES 0xC0000094 AND NOTHING ELSE, so `i64.min / -1` dies with NO DIAGNOSTIC.** Found by the A1 review while checking a prose claim, and the prose was wrong: `INT_MIN / -1` raises `#DE` like a zero divisor but arrives as **`STATUS_INTEGER_OVERFLOW` 0xC0000095**, and `X64Runtime.buildFaultThunkChunk` converts only 0xC0000094 — so the process exits with no panic line and no backtrace, on raw 0xC0000095. **The bootstrap DOES classify it** (`ExceptionCodeIntOverflow` + `__gt_ftp_intovf`), so this is a real gap rather than a shared position. ⭐ Cheap — one `cmp` in the thunk — but it needs its own asserted panic line as a spec case, which is why the review fixed the FALSE PROSE (both `safety.md` and `division.md` now state the behaviour accurately) and left the mechanism. ⚠ A1 deliberately leaves `INT_MIN / -1` unguarded at the language level (neither reference guards it); this row is about the DIAGNOSTIC when the hardware trap fires, not about adding a guard | L-gt-runtime *(`Targets/X64/X64Runtime.maxon`)* ✅✅ **CLOSED 2026-07-31** (main; merged tree **2871/0** with D11, x64-linux PASS, wasm PASS, arm64 **SKIP — remote, UNVERIFIED**; fragment ADDITIONS only, zero goldens modified). `i64.min / -1` went from **exit 127 with completely EMPTY stderr** to `panic: integer overflow at rip=…` + a backtrace, exit 1. `X64Runtime` gained `ExcCodeIntOverflow`, an `IntegerFaultKind`, and `faultPanicLinePrefix` as the ONE fault→words map, both arms sharing one `emitFaultCodeArm`. Wording taken from the bootstrap (`RuntimeEmitter.FaultHandler.cs:41`) rather than invented. ⭐⭐ **THE BIGGEST THING THIS ROW FIXED WAS NOT THE MISSING ARM — IT WAS THAT THE WHOLE HARDWARE-FAULT RUNTIME HAD NO TEST AT ALL.** The brief asked for the existing divide-by-zero panic case as a control; **there was none**. Verified by `git grep` at both commits, not taken on report: before A1 (`558f89577^`) `safety.md` asserted the divide-by-zero panic at TWO enabled tests; at A1g's parent, **zero** — A1 deleted the last program that reached the thunk when `/` became a language-level throw. So the VEH thunk, the CONTEXT extraction, the frame walker, the symbolizer and the hex writer were **entirely uncovered** between A1 and A1g. Both halves are now pinned. ⭐⭐ **THE SIGN-EXTENSION TRAP WAS VERIFIED AT THE BYTE LEVEL, BY DISASSEMBLING THE EMITTED THUNK** — the file's own comment warns that a group-1 immediate `cmp` is SIGN-extended so `cmp rax, 0xC0000094` matches nothing, and 0xC0000095 has the same top bit. The review read the bytes out of a real binary: both arms go through `mov r32, imm32` (**B8+rd**, which ZERO-extends), the exception-code load is a `wide: false` `8B` (also zero-extending), and the two `lea`s are exactly `0x28` apart — the length of the first prefix plus its NUL. **Neither arm matches by luck, and arm 2 did not disturb arm 1's dodge.** Frame alignment re-checked too: `alignUp(32+40,16) = 80`, still a multiple of 16. ⭐⭐ **x64-linux CANNOT CLASSIFY IT, AND THAT IS THE KERNEL'S LIMIT — MEASURED TWICE, INDEPENDENTLY.** Both faults arrive as `SIGFPE` with **`si_code = 1` (FPE_INTDIV)**; the review confirmed it WITHOUT re-running the implementer's code, via a raw `SA_SIGINFO` `sigaction` harness executing a real 64-bit `idiv` from `mmap`'d PROT_EXEC memory (control: `10/2` faults not at all). x86's `exc_divide_error` names `FPE_INTDIV` unconditionally, so recovering the cause would mean decoding the faulting `idiv`'s ModRM at the ucontext RIP — a disassembler in hand-assembled bytes whose failure mode is a CONFIDENTLY WRONG diagnostic. Not built; the limitation is stated at `buildLinuxFaultHandlerChunk` and the overflow case is gated `x64-windows`. **arm64** needs nothing (`SDIV` does not trap; there is no fault handler to extend) — read, not run. **wasm** needs nothing (the trap is not deliverable to guest code; wasmtime prints its own, exit 3 — measured). ⭐ **THE REVIEW FOUND A LATENT BUG OF THE SAME CLASS THE ROW WAS CURING, ONE FUNCTION OVER**: `strlen` was **open-coded twice, instruction for instruction, ACROSS THE OS BOUNDARY** (`buildWriteStderrChunk` vs `buildLinuxWriteStderrChunk`). How long a C string is is not an OS question, and nothing held the two answers together — an off-by-one fixed in the Windows copy is a truncated diagnostic out of the Linux one at RUNTIME, with no compile error at either site. Now one `emitCstrLenIntoRax`. The backtrace header was likewise a bare literal in two producers that agreed by coincidence. **Byte-neutrality proved by rebuilding the PARENT and comparing binaries — the x64-linux ELF is byte-identical (`md5 e0d4965…` both ways).** ⭐ **THREE SABOTAGES, and the third is the one that matters**: breaking either compare constant reddens only its own case (each pins an exit code), but **SWAPPING THE TWO ARMS OF `faultPanicLinePrefix` reddens BOTH on stderr wording** — without it the words could have been wrong on a green exit code. ⚠ **Both new cases reach the bare `idiv` through the UNGUARDED CALL-ARGUMENT DOOR** (row **A1f**): a `NonZero` parameter legitimately holds a runtime `0` today. **If A1f is ever closed, both of these cases go RED** — which is the correct failure mode, and is written here so the next agent is not surprised by it | ✅ DONE | `slice/A1g-intovf-fault-classify` | 2026-07-31T02:35Z |
| **A1j** | **ON x64-linux A RANGE-CHECK PANIC PRINTS NOTHING AT ALL — the half of the diagnostic story A1g just completed for hardware faults.** Probed by the A1g review: the same out-of-range program prints `panic at …: Range check failed` plus a stack trace on x64-windows, and on x64-linux prints **nothing**, exit 1. ⚠ **This row said the silence was "DELIBERATE and DOCUMENTED", citing `StdToX64Conversion.lowerRangePanicLinux` (`:1861-1867`) and `appendX64LinuxRuntimeChunks`'s omission of `mrt_panic`. ⛔ IT WAS DOCUMENTED CIRCULARLY, AND THE DOCUMENTATION WAS THE DEFECT'S ALIBI:** the comment justified the gap by *"no x64-linux test emits `call mrt_panic`"* — and the reason none did is that **16 cases had been narrowed to `targets: x64-windows` PRECISELY BECAUSE the message never appeared.** The evidence for the silence being acceptable was the silence, and the green suite was measuring the narrowing. **Neither cited function still exists.** But it is the same user-facing hole A1g closed on the fault path: a program dies with no diagnostic on one target and a full backtrace on another, and **only the Windows behaviour is pinned**, so nothing would notice if the Linux side changed. Scope: decide whether x64-linux gets `mrt_panic` (plus the stderr/backtrace machinery A1g's review just consolidated behind `emitCstrLenIntoRax` / `emitFaultAddressesAndBacktrace`, which is what makes this cheaper than it was), or whether the silence is the settled position — in which case **pin it**, because an unpinned deliberate silence is indistinguishable from a regression | **L-gt-runtime** *(`Targets/X64/X64Runtime.maxon` · `StdToX64Conversion.maxon`)* | ✅✅ **CLOSED 2026-07-31** (main `bd554c7cf`; x64-windows **2901/0** UNCHANGED with `fragments/x64-windows/` **EMPTY** after a full run ⇒ byte-identical Windows codegen, x64-linux **2769 → 2783/0**, wasm32-wasi **2757/0** unchanged, arm64 **SKIP — remote, UNVERIFIED**; 0 warnings; no exit 101; 16 goldens added, **128 modified**, 2 renamed; `maxon-sharp/` untouched). **The silence was implemented, not decided** ⇒ x64-linux now prints stderr **byte-identical to x64-windows** (`diff` empty on a real WSL2 run): the same `panic at …: Range check failed …` line and the same symbolized backtrace. ⭐ **THE WHOLE FIX WAS ONE `push`** — every primitive was already on the Linux lane (the `write(2)` stderr writer, the frame walker *verbatim*, the frame printer, and `__symtable`, whose emitter has no `target.os` branch at all); `buildPanicChunk` is four steps and only step (4), the process exit, had an OS in it. ⭐⭐ **THE LOWERING'S OS SPLIT DISAPPEARED RATHER THAN GAINING AN ARM.** RCX there is not a Win64-ABI fact but that file's private *"cstr argument"* convention, which `emitPrintString` already spells that way for both lanes' writers — so with the chunk on both lanes the two arms were byte-identical, collapsed to one body, and x64's duplicate `RangePanicExitCode` was deleted (one spelling: `PanicExitCode`). ⭐⭐⭐ **THE REVIEW FOUND THE RUNG HAD FIXED THE INSTANCE AND LEFT THE STRUCTURE — AND THE STRUCTURE IS THE ROOT CAUSE: the two lane appenders each carried their OWN COPY of the shared chunk list, and nothing made the copies agree.** A chunk present on one copy and absent from the other was therefore not a compile error and read as ordinary at both sites, for months, under a green suite. Cured by `appendX64SharedRuntimeChunks` — the five shared chunks written ONCE, so a shared chunk *cannot* be on one lane and off the other. It reordered the chunks; proven inert by four probes byte-identical + three green suites (all runtime chunks lie inside `[mrt_start, __symtable)`, the only range `emitTextBoundsGuard` tests, and inter-chunk references are NAMES resolved by the intra-`.text` fixup, not distances). ⭐ **THE SPEC SWEEP WAS THE REAL DELIVERABLE AND THE SET WAS DERIVED, NOT PROBED** (a case that range-panics and asserts silence must exclude x64-windows; one pinning the message must exclude every silent lane) ⇒ 16 members, and the RED run failed **exactly those 16 and nothing else**; re-derived independently by the review (of 2936 tests, 17 mention *"Range check failed"*, 16 carry the marker, the 17th only in prose). ⚠ **The coordinator's starting list contained a FALSE member** — a `string-views.md` pair attributed to `safety.md`, i.e. one pair listed twice under two filenames — **and the derivation is what caught it.** ⚠ Five BYTE-IDENTICAL copies of the *"x64-windows ONLY"* paragraph in `ranged-typealias.md` collapsed to one canonical statement; rewriting the corrected paragraph five times would have recreated the defect inside the fix. ⚠ Four surviving FALSE claims fixed, three in the same self-refuting shape (`StdToArm64Conversion.maxon:1063` justified arm64's silence by the very markers this rung widened; `StdDialect.maxon:786` said arm64/wasm *"reject"* the construct when they discard the label and die silently; `X64Runtime.maxon:733` called the walker's `stack_high == 0` arm *"unreached"* when it prints EVERY range-check backtrace with no bounds validation; `RuntimeAbort.maxon:66`). ✅ **The walker's UNVALIDATED software arm is safe, measured four ways rather than argued**: panic in `main` (2 frames), through a closure (4, closure frame named), 40-deep recursion (exactly 32, clean truncation, no fault), and a green thread — **not constructible on x64-linux at all**, since async is x64-windows-gated by E3104, so probed on Windows both fresh and resumed-after-`await`: clean, because `__gt_trampoline` uses `encodePrologue` with no `push rbp` and inherits the zeroed RBP of a slab-allocated GT stack. ⚠ **arm64 and wasm32-wasi remain SILENT and remain PINNED** — they genuinely have no panic runtime, and this rung deliberately does not give them one | `slice/A1j-linux-range-panic` | 2026-07-31T08:17Z |
| **A1h** | **`findRetBlock` IS A LINEAR SCAN PER RETURN SITE — reported with NO measurement, and deliberately NOT filed as measured debt.** Found by the A1 optimizer while fixing the `splitBlock` quadratic beside it. `InsertRangeChecks.findRetBlock` scans `func.blockRefs` for every ranged-`return` site and `guardReturns` calls it once per site, so a function with R return sites and G emitted guards is **O(R × (blocks + 3G))** — every guard appends `__rc_ok`, `__rc_panic` and a `__rc_chk` to the very set the next scan walks, which is the same shape `buildBlockIdIndex` already cures for the CAST sites. ⚠ **It reads Δ0 in EVERY column and that is the corpus blind spot, not evidence**: no generated function returns a non-full ranged alias, so the ladder cannot express it — and the project rule is that a Δ0 from a ladder that cannot express the feature measures the instrument, not the cost. **That is exactly why it is a row and not a "Measured debt" entry: there is no measurement to file.** Trigger: a ranged return type with many `return` statements PLUS guards in the same function (a wide `match` returning a `CodeUnit16`). Cheap to make O(1) per site with a ret-value→block index | L-types *(`InsertRangeChecks.maxon`, and — necessarily — `Project.maxon` + `Parser.maxon`; see below)* | ✅✅ **CLOSED 2026-07-31** (main `8820e3d70`; x64-windows **2902 → 2908/0**, x64-linux **2784 → 2790/0**, wasm32-wasi **2758 → 2759/0**, arm64 **SKIP — remote, UNVERIFIED**; 0 warnings; no exit 101; 6 goldens added, **not one pre-existing golden modified**). ⭐ **THE PERF HALF, MEASURED ON A TRIGGER THE CORPUS CANNOT BUILD** (`Testing/ladders/genretsites.sh`): `phase:insertRangeChecks` went from a RISING ratio **×2.23 ×2.56 ×2.92** per doubling (N=50→400 guarded ranged-`return` sites in one function) to **FLAT ×1.93 ×1.89 ×2.03**, coordinator-verified on an independent generator; **N=400 is 2.70× cheaper** (41.1M → 15.3M ticks). A rising ratio is `linear + quadratic` summed — each guard appended three blocks to the very set the next site's scan walked. ⭐ **A `spread` CONTROL PROVES THE ATTRIBUTION rather than assuming it**: the same N sites one-per-function read dead linear (×1.97) on the OLD binary, so the bend was the PRODUCT term, and the flattening is `findRetBlock` and nothing else. ⛔⛔ **AND THE REVIEW FOUND THE RUNG HAD FAITHFULLY PRESERVED A WRONG ANSWER — this is the rung's real finding.** Old scan and new index both asked *"which is the k-th live `ret` of this VALUE?"* and gave it to the k-th SITE: an **ORDINAL** pairing where a site owns an **IDENTITY**. Block order is not site order — a `while` mints its exit block BEFORE its body, and a cast's guard moves a block's `ret` onto an `__rc_ok` block that sorts LAST in `blockRefs` however early its source line. **Measured against `maxon-sharp`, right in every case:** a `return` in a `while` body reported **line 14 instead of 8**; a `return` in a branch also holding an `as` cast, 11 instead of 8; three same-value returns, 15 instead of 12. **Coordinator-reproduced, and re-verified byte-for-byte against the oracle after the fix.** ⚠ **A/B against this rung's own parent was byte-identical on all seven probes ⇒ PRE-EXISTING, and the optimization was faithful.** ⇒ **OPTIMIZING A MECHANISM IS THE MOMENT TO CHECK WHAT IT COMPUTES, not only how fast.** ⭐ Cured by making the pairing an identity: the site records its `block`, the parser fills it, the pass reads THAT block's terminator — **a block carries exactly one terminator, so "the `ret` this site owns" has one answer no ordering can permute.** It is the same fact `RangeCheckSite.block` already recorded for CAST sites, so the fix removed a divergence rather than adding a mechanism. ⚠⚠ **LOAD-BEARING FOR ANY REBASE ONTO THIS: `parseReturnStatement` pushes the site AFTER `emitOwnedValueReturn`, not before** — a hand-off's scope drops can branch, so the `ret`'s block is `currentBlock` as that call leaves it. Reordering those two statements silently reintroduces the bug for every drop-heavy return, and **only a message LINE would show it.** ⚠ **The fix HAD to leave this row's lane, and the in-lane alternative was checked first**: ordering the chain by each `ret`'s original block fixes the cast case and **cannot** fix the loop case, because the Std module carries no source order at all. ⛔ **THE COORDINATOR'S FOUR SPEC CASES ALL PASSED PRE-FIX, which measures their blindness**: they pin the pairing only where block order happens to equal site order, and **no if-chain can vary that axis.** The two that catch it use a `while` body and a cast in the same branch — bug captures, failing pre-fix with the wrong lines (15 vs 9, 16 vs 13). ⭐ **DUPLICATION WENT THE RIGHT WAY: both new index columns were DELETED, not abstracted** — with the block in hand a return site asks what `guardSiteAt` already asks, reading the existing `guardChainTail`; **net −40 lines**, and it retired a near-duplicate that was about to be filed. ⚠ Ladder: allocations **0 at every rung** (the index is never built — the corpus never *guards* a ranged return) and bytes **+160 + 64·2ⁿ**, i.e. LINEAR from an existing record getting WIDER, not a new allocation: the site's new 8-byte `block` at 8 sites per rung-0 unit. ⚠ **That refines this row's premise — the corpus DOES record return sites (full-range aliases); it just never needs to guard them** | `slice/A1h-retblock-index` | 2026-07-31T10:34Z |
| **A1i** | **⛔ THE C# SUITE'S DESIGNATED REGRESSION PIN IS RED ON `main`, SO IT IS PINNING NOTHING.** `./bin/maxon.exe spec-test` reads **3187 passed / 1 failed**, and the 1 is `memory-safety/array-push-struct-incref` — which this PLAN's own entry calls *"THE REGRESSION PIN"* for the release-of-old-value memory bug and records as **PASSING at C# 3187/0** when that fix landed. ⚠ **Coordinator-verified NOT caused by A1**: reproduced on the **main checkout** with zero A1 changes before touching anything, and A1's entire `maxon-sharp/` diff is **one XML doc comment** (E3103's `doc` row widened to mention floats, regenerated from the registry), which cannot move emitted IR. ✅ **The cause looks benign and the shape says so — SSA RENUMBERING ONLY:** Expected reads `%75`/`%76` where Actual reads `%74`/`%75` in `__destruct_ItemArray`'s `raw_free_0`, with the op STRUCTURE identical, i.e. some other change removed an op earlier in the function and the golden was never regenerated. ⛔⛔ **BUT THAT IS WHY IT MATTERS RATHER THAN WHY IT IS FINE: A RED PIN CANNOT FIRE.** This test exists to catch the return of a bug whose *"bespoke minimal test is not possible"* — its manifestation depends on what an uninitialized slot happens to hold, so a small program passes on the BROKEN compiler, and it took the compiler compiling itself to surface it. While the pin is red for an unrelated reason, the real regression would land inside an already-failing test and nobody would look. **Regenerate the `RequiredIR` block, confirm the diff is renumbering ONLY and not a missing `mm_decref`, and get the suite back to 3187/0.** ⚠ Needs the full C# suite as its gate ⇒ a bootstrap rung, not an shv2 one. ⚠ **SEPARATE debt measured the same day:** a plain `./bin/maxon.exe spec-test` REWRITES **~140 `specs/fragments-x64-windows/*` goldens** (register-allocation / stack-slot drift, unrelated to any current rung — A1 reverted rather than bundling it into its own diff), and the x64-linux lane generates **14 absent `specs-shv2/fragments/x64-linux/associated-types/*`** goldens that no gate ever missed because **a MISSING golden never fails**. ✅ **THE MISSING-GOLDEN HALF IS DISCHARGED (A1r, 2026-07-31)** — the 14 absent `associated-types` x64-linux goldens were emitted by that rung's cross-target run and committed with it, along with 4 of its own. What REMAINS here is the red regression pin and the ~140 rewritable `specs/fragments-x64-windows/*` goldens |  *(bootstrap — `maxon-sharp/`)* ✅✅ **CLOSED 2026-07-31** (main; C# suite **3188 passed / 0 failed, exit 0**, verified twice by the coordinator on the final tree). The red gate is green. ⛔⛔ **BUT THE ROW'S PREMISE WAS MEASURED FALSE, AND THAT IS THE FINDING**: the row said the drift was *"SSA RENUMBERING ONLY"* and instructed *"confirm the diff is renumbering ONLY and not a missing `mm_decref`"*. Measured before touching anything: `mm_decref` **8 → 7** and stores to `__try_result_0` **3 → 2**. **It WAS a missing `mm_decref`** — the row's own precondition for regenerating was NOT met, and regenerating on the row's word would have buried the bug this pin exists to guard. The rung therefore became an investigation, and the golden was regenerated only after the benign reading was PROVEN three independent ways. ⭐⭐ **THE LOAD-BEARING PROOF IS A MECHANISM, NOT A PROPERTY OF THIS PROGRAM — and neither the implementer nor the coordinator had it until the review**: the vanished zero-init is not a second change, it is `DeadStoreEliminationPass`. `MaxonToStandardConversion` emits an entry zero-init for every managed scope-end var; the pipeline runs `StoreForwardingPass` + `DeadStoreEliminationPass` BEFORE the `=== standard` stage is captured; and DSE keeps that store **exactly when some path can LOAD the slot without storing it first**. The release-of-old-value `memref.load __try_result_0` WAS that load. Delete it and the zero-init is dead **by construction**. **Two changed ops, ONE cause.** Confirmed discriminating: in the same function `__call_tmp_0` — stored only on the error arm, loaded on the merge — KEEPS its zero-init. ⭐ **THE CAUSE**: commit `4572c988b` *"a match-arm payload binding released a value the slot never held"* (2026-07-29), one line in `MaxonToStandardConversion.cs:1440` (`varTypes.ContainsKey(dstName) && !assignOp.IsDeclaration`). The golden's own `=== maxon` section is the proof — BOTH `otherwise` arms carry `{decl = 1 : i1}` for `__try_result_0`, so the declaration guard is precisely what suppresses the load/decref pair. **The removed decref WAS the bug being fixed.** ⭐⭐ **WHY IT SAT RED FOR 15 DAYS, AND THIS IS THE REUSABLE LESSON**: `--update-required` regenerates **only the AUTHORING HOST's `RequiredIR` block**, silently. `4572c988b` ran on arm64-macOS, updated the arm64 block, left x64-windows stale — and its own gate note claims it had regenerated the pin. **The committed `arm64-macos` block of the SAME target-independent `=== standard` section already read 7 decrefs while x64 read 8**: two goldens for one section disagreeing is what settles which is stale, and it is a check nobody was running. ⭐ **The review could not break the benign verdict and says what it tried**: 11 hand-built adversarial probes (try-in-untaken-`if`, try-in-`while`, sibling mutually-exclusive binds, `otherwise` that returns, `otherwise panic`, `continue` skipping the declaration, `break` before it, nested try, try in `for-in`, two `otherwise (e)` handler orderings), each compiled AND run with exit codes hand-predicted — plus a **corpus-wide instrument**: all 3580 ```maxon blocks in `specs/` compiled with `--dump-stages` and a definite-assignment dataflow run over **41,559 functions ⇒ 0 violations**. A separate may-analysis flagged 9 double-decref candidates, all one documented shape (`ManagedList`'s link vs local binding), not a defect. ⚠ **CALIBRATION THE REVIEW INSISTED ON**: only **5** tests in the entire C# corpus carry a `=== standard` section under more than one target, so "no other test is silently divergent" is a **5-test** population, not a corpus sweep; the 41,559-function dataflow is the corpus-wide claim. ⭐ **THE REVIEW ALSO FIXED THE REAL DUPLICATION GAP**: the pin's MEANING existed only in `4572c988b`'s commit message — nowhere a future `--update-required` would look — so the next agent to see it red would inherit this rung's decision with none of its evidence. 23 lines of prose now sit above the block: what it pins (an ABSENCE), the exact diff to look for before committing a regeneration, and the DSE fact that stops the zero-init's departure reading as a second, independent loss. ⚠ **TWO IMPLEMENTER CLAIMS THE REVIEW CORRECTED**: `maxon-selfhosted` does not fail with *"two E9001 ahead of the E3005s"* — it emits **52 errors in one pass** (23×E9001, 14×E3005, 6×E3057, 5×E3014, 4×E3008), together, not ordered; and the inference that this leaves `specs/fragments-x64-windows/**` without a regenerator is a **NON-SEQUITUR** — the C# runner rewrites those 139 files unconditionally on **every run** (`TestRunner.cs:251`/`:438`, `File.WriteAllText`, never compared), which is why they clobber. The genuine unrelated debt is `specs/fragments-arm64-macos/**`, regenerable only on a Mac. ⚠ **STANDING HAZARD, now measured twice by the coordinator**: any C# suite run dirties exactly **139** tracked files under `specs/fragments-x64-windows/`; `git checkout -- specs/` after every run. | ✅ DONE | `slice/A1i-regression-pin-red` | 2026-07-31T04:22Z |
| **A1u** | **THE STDLIB IS PARSED ONCE WITH THE *HOST* TARGET AND CACHED, SO STDLIB `#if os(…)` IGNORES `--target`.** Found and MEASURED by the A1i review while calibrating a cross-target golden. `StdlibLoader.GetStdlibModule()` (`maxon-sharp/Compiler/0-Compiler.cs:1096`) calls `Compiler.CompileSources(module, sources, true)` with **no `target` argument** ⇒ `CompileTarget.Default` ⇒ the host — and then caches the module. User files DO get the target (`:196`, `:399`), so the two halves of one compile disagree about what OS they are building for. **Measured, not read:** a user-file `#if os(Windows)` correctly took its `#else` branch under `--target=arm64-macos` on a Windows host, while `stdlib/Process.maxon:7`'s `ExitCode` stayed `int(0 to u32.max)` — the `array-push-struct-incref` pin cross-compiled to arm64 from Windows emits `arm64.mov w6, #4294967295` where the Mac-generated golden has `arm64.mov x6, #255`. ⚠ **Blast radius is small but the failure is silent and semantic**: 1 site in `Process.maxon` and **7 in `FilePath.maxon`** (path separators), so **a `--target=arm64-macos` binary built on Windows has Windows path semantics baked into the stdlib**. It does not corrupt the committed arm64 goldens — those are minted on the Mac and are correct — which is exactly why nothing has caught it. ⇒ Fix is to thread the target into the stdlib compile and key the cache by it; ⚠ the cache key is the point, since a per-target stdlib module must not be shared across targets in one process (the spec runner compiles several targets in a run). **Gate is the full C# suite ⇒ a bootstrap rung.** | **L-bootstrap** *(`maxon-sharp/Compiler/0-Compiler.cs` · `StdlibLoader`)* ✅✅ **CLOSED 2026-07-31** (main; C# suite **3188 passed / 0 failed**, `dotnet build` 0 warnings, verified by the coordinator on the merged tree after rebasing onto A2c/A1x). **REPRODUCED FIRST, with a one-line discriminator**: `function main() returns ExitCode` compiled `--target=arm64-macos` and `--target=x64-linux` on a Windows host both emitted `func @main() -> u32` — the Windows-only `ExitCode = int(0 to u32.max)` — where both must be `u8`. The Mac-minted `RequiredIR:arm64-macos` golden of `array-push-struct-incref` proves the right answer (`arm64.mov x6, #255`). ⭐ **THE FIX CLOSES THE DOOR RATHER THAN PATCHING THE ONE CALL THAT WALKED THROUGH IT**: `CompileSources`' `CompileTarget? target = null` default WAS the mechanism, so the parameter is now REQUIRED and all six call sites state a target — and each choice was audited as *correct*, not merely present (the two LSP sites legitimately want the host and say so). The cache became `ConcurrentDictionary<CompileTarget, ParsedStdlib>`, with the two id watermarks moved INSIDE the record so a torn read across module-and-watermark is structurally impossible. ⚠ **THE CACHE KEY IS THE CRUX, AND IT IS SABOTAGE-PROVEN AT THE BUILD**: collapse the key to a constant and `dotnet build` FAILS (`MSB3073 … stdlib-target-selftest exited with code 1`). ⭐⭐ **THE BEHAVIOUR CANNOT BE PINNED BY A SPEC CASE, AND THE REASON IS STRUCTURAL — SO IT GOT A BUILD GUARD INSTEAD.** The suite builds ONE target per process and compares that target's `RequiredIR`, so on any single machine host == target and BOTH halves of the bug are invisible: a case would pass for exactly the reason it passed before the fix, and `SpecParser` will not even accept a test whose only expectation is another target's block. A vacuously-passing case would have been worse than none. `maxon stdlib-target-selftest` (following the repo's existing `mxdbg-selftest` idiom) compiles x64-windows / arm64-macos / **x64-windows / arm64-macos interleaved** — the THIRD entry is what tests the key — and is wired into `dotnet build` as the THIRD guard, chained `AfterTargets="CheckFlatNamespace"` so the three guards order deterministically instead of two sharing one anchor. **Cost measured: ~3.2 s (3.52/3.11/3.25) on a ~50 s build.** ⭐⭐ **CONCURRENCY ATTACKED AND HELD, WITH TWO METHOD ERRORS CAUGHT ON THE WAY** — the review built a throwaway probe and ran **30 compiles across 2 targets × 5 programs × 3 reps, all released from one gate: byte-identical to serial, 0 mismatches**, plus a cold-`[ThreadStatic]`-thread arrangement and a parse-on-worker arrangement (the spec-runner shape). ⚠ Its own CONTROL fired first and saved the result: two identical *serial* compiles to different output paths disagreed on `arm64-macos`, because the Mach-O writer stamps something path-derived — so arm64 was excluded from byte comparison and two PE targets used instead (they still differ in stdlib content, so the key stays under test). And a first attempt hashed `AllStagesIr`, which is printed with `f => !f.IsStdlib` — **stdlib bodies are not in it at all**, so it could never have answered the question. ⚠ **A LOAD-BEARING DECISION HAD A FALSE STATED REASON, AND THE REVIEW REPLACED IT.** The rung deliberately does NOT reset the stdlib name counters per target parse, claiming that continuing them "keeps names unique ACROSS the cached modules". Measured false — both counters are `[ThreadStatic]`, so a second target parsed on a second thread starts at zero and two cached modules can each hold `_$stdlib_closure_0`. Not resetting is still right, for a stronger reason now written at the site: the cache must remain a SUPERSET of the label→message mapping of every module the thread may later compile, because cloned panics re-mint from it. ⭐ **Duplication: the rung added a THIRD reader of the `=== <stage>` dump marker**, each with its own spelling (`LastIndexOf("\n=== ")` — blind to a marker on line 1; `line.Trim().StartsWith("=== ")`; and the new self-test's own pair). They agreed only because the emitter happens to emit a leading newline and no indent. All three now read through one `PipelineStages` beside the four writers. `LastSectionBody` stays a byte-preserving slice ON PURPOSE (its result is written verbatim into committed goldens, so re-joining with `\n` would rewrite every fragment on a Windows host and call it a diff). ⚠ **CONFIRMED REAL BUT NOT REPRODUCED — stated as reading-level, not measured**: cloned `MaxonPanicOp`s (`FunctionCloner.cs:402`, `MonomorphizationPass.cs:2397`) re-mint their label from a `[ThreadStatic]` cache while the module is process-global, so a worker with a cold cache can mint a label already bound to a different message ⇒ wrong panic text at runtime. **Pre-existing and NOT worsened by this rung** (it adds no threads and no sharing; each compile still touches one module) — the review tried and could not build a program where a non-generic stdlib panic and a monomorphized one meet in one module. Its own row. ⚠ Also left deliberately: `Compiler.Compile`'s own `target = null` default STAYS, argued rather than cargo-culted — the bug was a **disagreement between two halves of one compile**, not a default; `Compile` chooses the target and threads it to both halves consistently, so its default cannot produce one. But `TestRunner.CompileToExecutable`'s dead `= null` default WAS removed, because that is the one file where a forgotten target is a silent wrong answer (a cross-target run compiling for the host and reporting under the requested triple). | ✅ DONE | `slice/A1u-stdlib-target-cache` | 2026-07-31T05:18Z |
| **A1k** | ✅✅ **CLOSED 2026-08-01** by BATCH1 (main `d4cd6aa20`). ⭐⭐ **THE MEASUREMENT IS THE RESULT: with the OOB fixed, `MAXON_SPEC_DEBUG_INFO=1` gives 3219/3219 PASS and `3219 goldens VERIFIED, 0 written`** — so the crash was the ONLY defect on a path the suite had executed **zero** times, and codegen neutrality with debug info on is now MEASURED over 3219 programs rather than asserted. ⭐ **The invariant, not the symptom**: a debug-span mark is an index into ONE destination block, and `newBlock` is replaced mid-lowering by bounds-check / divide-guard / try-error merge blocks handed back through `ref`. The pass that changes blocks now owns the reset (`spanMarkBlock`); `AssignRange` **REJECTS** an out-of-block mark naming the block. **A clamp was rejected** — every misplaced mark is a wrong line number in the `.mxdbg`. **Sabotage-proven**: the guard fires and names both doors (`__setlen_merge_38`, `__div_merge_29`). ⭐ Correct, not merely non-crashing: `--dump-info` gives one monotonic row per executable statement, and code size is identical to `--no-debug-info` (98,309 bytes). ⭐ **Fixed a leak in its own mechanism**: six copies of delete-the-binary-but-not-the-`.mxdbg` dropped **1031 untracked files** into the committed `specs/` tree; one `CompiledArtifact.Delete` now removes both. ⚠ **Permanent coverage landed** (`<!-- DebugInfo -->`, threaded to the ONE compile whose binary is run, and excluded from batching); the env var is an INSTRUMENT, not a gate ⇒ promoting it to `spec-test --debug-info` needs `Program.cs` ⇒ folded into `A3d`. ⚠ **`FunctionCloner` copies no `_debugSpans`, so every monomorphized generic has an EMPTY line table** — a debugger cannot step in a generic ⇒ `A3d`. **⛔⛔ EVERY ONE OF THE 3188 SPEC COMPILES RUNS WITH DEBUG INFO **OFF**, SO THE DEBUG-INFO LOWERING PATH HAS NO GATE AT ALL — AND IT CRASHES ON A PROGRAM THE REPO ALREADY SHIPS.** Found by the A1u review while auditing `[ThreadStatic]` state. Take the source of the committed fragment `specs/fragments-x64-windows/array-slots/past-the-end-reports-index-out-of-bounds.test`, put it in a file, and `./bin/maxon.exe build <file>` gives **`error E9001: Lowering function 'main' failed: Index was out of range.`** at `MaxonToStandardConversion.Run`. With `--no-debug-info` the same program compiles clean (98,309 bytes). ⭐ **The cause is the COVERAGE HOLE, and it is the finding**: `Compiler.DebugInfo` is `[ThreadStatic]` and is set ONLY in `RunBuild` (`maxon-sharp/Program.cs:634`, `:731`); `maxon-sharp/Testing/**` never sets it — the grep is empty — so the suite exercises the debug-info path **zero times**, on any target, ever. `StandardToX86Conversion.cs:53` already documents the `[ThreadStatic]` hazard for the emitter's worker threads; nobody noticed the same fact means the whole suite runs with the flag off. ⚠ **So this is not one crash to fix — it is an untested lowering path that a user hits by DEFAULT** (debug info is on unless `--no-debug-info` is passed), while the 3188-test gate stays green. Scope: fix the OOB, then decide how the suite covers this at all — a per-test flag, a second pass over a subset, or a `RunBuild`-shaped harness — because fixing only the crash leaves the hole that hid it. **Gate is the full C# suite ⇒ a bootstrap rung.** | **L-bootstrap** *(`maxon-sharp/Compiler/MaxonToStandardConversion*.cs` · `maxon-sharp/Testing/**`)* | ▣ **BATCH1** | — | — |
| **A1m** | ✅✅ **CLOSED 2026-08-01** by BATCH1 (main `d4cd6aa20`). ⭐⭐⭐ **REPRODUCED FOR THE FIRST TIME IN ITS LIFE** — the row was filed *"confirmed by READING and explicitly NOT reproduced"*, and every earlier probe failed because all stdlib panics came from one side. Method: a temporary env-gated trace in `GetOrCreateLabel` (added, used, **removed**) dumping `threadId/label/message` at each mint, giving **parse-order vs worker-order tables**. `Array.resize` re-mints as `__stdlib_panic_msg_98`; parse `_98` is `utf16.maxon:59`. Result on tape: **`panic at utf16.maxon:59: Range check failed: value outside typealias 'CodeUnit16'` … `in __Array_i64.resize`** — a panic reporting a UTF-16 range check the program never went near. RED before, GREEN after, **codegen byte-identical across all 3200 programs**. ⭐ **Candidate (1) — PRESERVE, don't re-mint — was verified COMPLETE, not merely narrower**, so `0-Compiler.cs` was never needed: after `CloneKeepingLabel`, labels are minted only by the parser, the stdlib cache is touched only during a stdlib parse, and the two prefixes are disjoint. ⭐ **The spec is committed in DUPLICATE on purpose**: the bug is invisible to the thread that parsed the stdlib, so two work items force one thread to parse and the other not to — the first copy passes, the twin fails. ⭐⭐ **The review then found the claim TRUE BUT UNENFORCED**: `LowerPanic` silently DROPPED a second, different message under an existing label; it now compares bytes and refuses, sabotage-proven (naming `Interfaces.maxon:112` vs `Set.maxon:111`). ⚠ **The comment in `0-Compiler.cs` that had documented this bug verbatim for months was rewritten by the coordinator**, since the agent could not touch that file — it now describes the fix and keeps the trap. **A CLONED `MaxonPanicOp` RE-MINTS ITS LABEL FROM A `[ThreadStatic]` CACHE WHILE THE STDLIB MODULE IS PROCESS-GLOBAL ⇒ WRONG PANIC TEXT AT RUNTIME.** Confirmed by the A1u review, **by reading and explicitly NOT reproduced** — recorded at that confidence on purpose. The chain: `MaxonPanicOp._stdlibPanicLabelCache` is `[ThreadStatic]` (`MaxonDialect.cs:1873`) but the parsed stdlib module is shared across threads; `IrFunction.DeepClone` SHARES op references (`MlirFunction.cs:158-160`) so a non-generic stdlib panic keeps its parse-time label, while `FunctionCloner.cs:402` and `MonomorphizationPass.cs:2397` construct a **new** `MaxonPanicOp` that re-mints from the *compiling* thread's cache; and `LowerPanic` (`MaxonToStandardConversion.ManagedMemory.cs:1753`) writes `(label, messageBytes)` **only if the label is not already present** — so one label carrying two messages means the second panic prints the FIRST one's text. The spec runner compiles on worker threads (`TestRunner.cs:111`), so parse-thread ≠ compile-thread is the normal case. ⚠ **PRE-EXISTING and NOT worsened by A1u** — that rung adds modules, not threads or sharing, and each compile still touches exactly one module. ⚠ **What blocked reproduction, so the next agent does not repeat it**: every probe put all its stdlib panics on one side (an Array-heavy program's `__stdlib_panic_msg_0..5` are all minted by the monomorphization clone), so the two label spaces never met inside one emitted module. **It needs a program where a NON-GENERIC stdlib panic (`String.replace`, `Math`, `Json`) survives alongside a monomorphized one.** ⇒ The fix is to scope the label cache to the module rather than the thread — which means changing how `MaxonPanicOp` mints its `SymdataLabel` in its constructor, a real design change. A1u moved its id watermarks INSIDE `ParsedStdlib` for precisely this reason; this is the same fact still left outside. | **L-bootstrap** *(`maxon-sharp/Compiler/MLIR/MaxonDialect.cs` · `FunctionCloner.cs` · `MonomorphizationPass.cs`)* | ▣ **BATCH1** | — | — |
| **A1n** | ✅✅ **CLOSED 2026-08-01** by BATCH1 (main `d4cd6aa20`). ⚖ **USER RULING: WITHDRAW THE CLAIM** (an ELF writer was offered and declined). ⭐⭐ **THE ROW NAMED ONE SPELLING AND IT WAS A CLASS** — the dispatch keyed on `Arch` ALONE, so measured: `x64-linux`→PE, `x64-macos`→PE, `arm64-linux`→Mach-O, and **`arm64-windows`→a Mach-O named `p.exe`**; and an unknown OS (`x64-plan9`, `wasm32-wasi`, malformed `x64`) was an **unhandled `ArgumentException` + .NET stack trace, exit 127** — a crash, not a diagnostic, which the row did not carry. ⭐ **ONE roster, read by three consumers**: `SupportedTargets` backs the writer dispatch, the refusal (new **E5004**) and the help text, so what the compiler ADVERTISES can no longer outrun what it EMITS. Stage 5 keys on architecture, stage 6 on object format — the facts each actually depends on. ⚠ **Refused at FOUR doors, not the two the implementer built**: the review found `TestCommand.cs` and `Mcp/Tools/DebugBuild.cs` reaching a writer with an **unchecked host default**, reproducing the exact crash door 1 exists to prevent. ⭐ **NOT spec-testable and a stronger gate was built instead of a vacuous case**: the C# spec format has **no per-case `--target` directive at all**, so the gate went into `stdlib-target-selftest` — every supported target's executable must start with **its own format's magic bytes** (the only place the format is a FACT rather than an intention), plus all five refusals at both doors. Sabotage-verified: pointing the Mach-O arm at `PeWriter` FAILED the build with three named lines. ⭐ Linux **source** awareness deliberately KEPT (`#if os(Linux)` is a legal predicate, pinned by a new case) — only the EMISSION claim was withdrawn. Negative controls: `x64-windows` byte-identical (96421/27/7504/0/9127 + 91 imports). **`--target=x64-linux` IS ADVERTISED AND WRITES A WINDOWS PE.** Found by the A1u implementer while scoping the self-test. `Compiler.Compile` dispatches the object writer on `target.Arch` ALONE (`0-Compiler.cs:219`/`:231`), and the tree contains only `5-PeWriter.cs` and `6-MachOWriter.cs` — **there is no ELF writer**. So `maxon build --target=x64-linux` emits a PE that now (post-A1u) correctly contains POSIX stdlib semantics — a Windows executable full of Linux assumptions. `maxon`'s own usage text lists `x64-linux` among its examples, and `specs/fragments-x64-linux/` holds exactly ONE fragment, last touched 2026-06-13. ⚠ Note this is the BOOTSTRAP only — shv2 has a real x64-linux backend and its cross-target gate runs that lane green via WSL, which is exactly why nobody has tripped over the bootstrap's version. ⇒ **This is a question before it is a task: is bootstrap x64-linux a real target (⇒ write an ELF writer) or a claim to withdraw (⇒ remove it from the usage text and refuse the flag)?** A1u deliberately kept it out of the self-test for this reason while still reporting that its stdlib layer is now correct. | **L-bootstrap** *(`maxon-sharp/Compiler/0-Compiler.cs` · a writer, or the usage text)* | ▣ **BATCH1** | — | — |
| **D7** | **FUNCTION OVERLOADING.** shv2 has none: a second same-named `function` is `E3006 duplicate definition` **at the declaration**, before any call is resolved. Found by D2 when its own spec port list turned out to be wrong at the code — `specs/function-overloads.md` contains the two cases D2 was told to unlock, and neither is reachable without overloads. Ported byte-identical anyway; **23 of its 24 cases are disabled and become this row's acceptance criteria.** ⚠ One of them (`method-call-argument-chained`) needs a SECOND thing as well — `return self.leaf` hits P1.4b's borrowed-aggregate-return refusal, which the oracle does not have ⭐⭐ **SURVEYED 2026-07-30, AND THE ROW UNDERSTATES IT — MEASURED, NOT READ.** Both its stated claims hold (`E3006` at the second declaration, verified; the port is 1 enabled / 23 disabled and every line differing from `/specs` is a disable marker, verified). **What it omits is the prerequisite.** All 24 cases disambiguate by ARGUMENT TYPE — `basic-type-`, `bool-`, `float-`, `method-type-`, `second-param-disambiguation-result-type`, plus a nine-case `method-call-argument-*` family where the argument's type is another call's RETURN type. ⇒ **overload resolution must know an argument's type at the call site, and `SignatureIndex` STORES PARAMETER NAMES AND NO PARAMETER TYPES** (`ParamConsumeInfo.paramNames as ByteArrayArray`, `SignatureIndex.maxon:1212`; `formatFnTypeDisplay`'s `paramTypes` is a display argument, not the index). ⚠ **This is the same wall P1.8 Slice B hit and deliberately did not climb** — its own note: *"`SignatureIndex` stores parameter NAMES but no parameter TYPES, so at the moment `parseCallArgs` runs the expectation does not exist to consult... closing it puts a whole-program query under a per-file one and rehashes every parse memo. Coordinator's call, not taken."* ⇒ **D7 is NOT a parser rung; it is that architectural change plus a resolver on top, and it touches the content-hash parse memo.** 🚩 **ESCALATED to the user 2026-07-30 rather than guessed** — the cost is a compile-time regression risk across every program, which is not a coordinator's call to make silently ⏸ **IN PROGRESS 2026-07-30, branch PUSHED (`slice/D7-function-overloading`, feat `7b00a523b`) — NOT MERGEABLE YET, and the reasons are named so the next agent inherits them rather than rediscovering them.** The mechanism works: host **2580/0**, x64-linux **2482/0**, **zero committed goldens moved** (mangling is ON CONTEST ONLY, so the first declaration keeps its bare name). Two-phase and deliberately unlike BOTH references — their parsers select from a candidate list and shv2's parser has none, since it writes only a per-file artifact and learns of other files through the type-less sweep ⇒ **resolve POST-MERGE off the real IR** (`SemanticCheck.resolveOverloadedCalls`), bootstrap order (candidates → `slotCallArgs` for labels+arity as ONE question → type score 2/1/reject → tie is E3007). ⚠ **Acceptance is 16, not 23** — measured; 7 are blocked by other mechanisms and re-disabled with reasons each verified by extracting and building the case. ⭐⭐ **TWO REACHABLE WRONG ANSWERS**: a constant argument's RANGE CHECK was keyed by callee NAME, so with `f(x String)` first, `f(99)` **compiled and exited 99** where the control is `E3005`; and the review found **an AGGREGATE argument accepted at a SCALAR parameter** — `takesInt(sh)` on a boxed union compiled and did arithmetic on the payload word. ⚠ **That second one is PRE-EXISTING** (reproduces with no overload in the program) but D7's resolver leans on the check, so it is fixed here. Root: `namedAggregatesConflict` answers all four quadrants but **both callers gated the ask on the DECLARED side being an aggregate**, so aggregate-meets-scalar was never asked. 🚩 **STILL OWED, and none of it is optional**: (1) the review's fixes were UNCOMMITTED at handoff; (2) **A REGRESSION GUARD IS LOST** — converting `interface-conformance.md`'s `error.duplicate-method-conformance` from a negative test to a positive one left the panic it guarded unreachable, **provable by deleting `ConformanceCheck.maxon:63` and watching the new test stay GREEN**; repair is a negative test with two IDENTICAL-signature methods, which still collides on E3006; (3) `maxonOpCallee` (`MaxonDialect.maxon:32`) classifies via **RANGE ARMS**, so a new callee-carrying op inserted inside a band is silently `none` while its three sibling matches use `default panic` — this project's signature shape, in code D7 now depends on; (4) the same-path scale A/B against `db1d34937`; (5) `docs/optimization-log.md` has no row. ⚠ **COORDINATOR ERROR RECORDED:** I escalated this rung to the user as needing parameter types in `ProgramSignatures` with a parse-memo rehash. **Wrong** — see the correction above; I reached for a memory note about a PARSE-TIME question without checking which index the resolver needs 🚩✅ **THE RED GATE WAS REAL, AND ITS CAUSE WAS NEITHER OVERLOADING NOR CLOSURES — DIAGNOSED HERE, FIXED UPSTREAM BY W1 AND W1b.** `closure-capture.block-local-overload-arg` failed wasm-only (`expected 17, got 3`). A two-variable experiment settled it: keep BOTH overloads and move the thunk's return off `ExitCode` ⇒ **PASS**; DELETE the overload and keep `ExitCode` ⇒ **still traps**. The sole variable was the function typealias's `ExitCode` return — a WIDTH lost when a signature is rebuilt from a stored NAME, invisible on every register target because i64 and i32 share a GPR, and a hard trap on wasm alone. ⇒ **D7 did not break wasm, it EXPOSED a pre-existing gap**, and the twin marker naming that exact door was already sitting FOUR LINES BELOW the failing case. ⚠ **THE FIX IS NOT IN THIS ROW, AND THAT IS THE INTERESTING PART.** While D7 was held at its red gate, **W1** landed the same fix independently and better — one `builtinTypeNameTag` table asked by ALL THREE re-derivation doors, where D7's tail had covered two. **W1b** then closed what W1's own fix made reachable: an `exitCode`-tagged call RESULT captured into a closure panicked `accessWidthFor` on legal source, on `main`, with the suite silent. ⇒ **D7's ExitCode work was DROPPED as redundant and the rung was rebuilt from its three feature commits onto the advanced `main`** — the only conflict was the GENERATED `docs/error-codes.json`, resolved by regenerating it. Everything D7's tail found is preserved in W1b and in the Future-rungs rows; nothing was carried over unmeasured (the `ExitCode`-above-2^31 divergence D7's review filed was **re-measured and found already FIXED** by W1's review, so it was dropped rather than filed). ✅✅ **CLOSED 2026-07-30.** x64-windows **2688→2708/0** · x64-linux **PASS** · wasm32-wasi **PASS** (including the case that held the gate) · arm64 SKIP — remote, UNVERIFIED. Fragment ADDITIONS ONLY, zero modifications. **17 of 24 `function-overloads.md` cases enabled; the 7 that stay disabled are each blocked on a DIFFERENT, ALREADY-FILED mechanism and none on overloading** — `string-contains-char` on the synthesized String method set, `method-call-argument-chained` on P1.4b's borrowed-aggregate return, and FIVE on the enum-property gap now written into **#77** (an enum has no `.name`/`.ordinal`/`.rawValue` at all, measured). Plus the five cross-file collision cases the row named. | L-parser-decl | ✅ DONE | `slice/D7-function-overloading` | 2026-07-30T03:24Z |
| **D8** | **A KEYWORD MAY BE A DECLARED FUNCTION/METHOD NAME** — `stdlib/FilePath.maxon:34` declares `export static function from(path String)` and shv2 answers **`E2010 Expected 'identifier' but got 'from'`** (measured 2026-07-30, both as a static method and as a free function). ⭐ **The asymmetry is the whole row:** shv2 already accepts a keyword as a CALLED member name — its own source calls `String.from(...)` **218 times** — and as an ENUM CASE name (`keyword-as-case-name.md`). Only the DECLARATION position refuses one. `from` is a lexer keyword (`Lexer.maxon:182`/`:1360`, *"Used in range expressions (from X to Y)"*). ⚠ **ON THE CRITICAL PATH TO S2, the gate's dominant cost**: it is `stdlib/FilePath.maxon`'s first blocker, and FilePath gates `Process`, `File` and `Directory`. Same family as D1d one layer down — there a keyword that is a name was misread as BLOCK STRUCTURE; here it is refused as a NAME ✅ **CLOSED 2026-07-30** (`50bfbcbef`..`6797b4634`; 2537 → **2554/0** host, x64-linux 2456/0). `tokenNamesEnumCase` → **`tokenCanBeAName`**: ONE answer for all three positions that ask *"can this token be a NAME?"* — an enum case name, a DECLARED name, a MEMBER name after `.` — plus `consumeNameToken` as the one consume (9 sites, 13 widened, incl. `parseInterfaceMethod`, a gap the brief missed and which no conforming type could have spelled). **GENERAL, not a `from` allowlist** — the oracle accepts any keyword (`from`/`if`/`match` all compile, byte-identical size). ⭐ **ACCEPTANCE MET: `stdlib/FilePath.maxon` clears `:34` and now stops at `:56:18 String method 'byteAtOrPanic'`** — an S3-family blocker, so the critical path to S2 advanced by one file. ⚠⚠ **THE GRAMMAR WAS THE EASY HALF; ALL FOUR REACHABLE DEFECTS WERE TOKEN SCANS ASKING A RAW `TokenKind`.** Implementer found: a **SIGSEGV (139)** — ownership scans read a parameter *reference* by token KIND, so `Self{value: from}` recorded no consume and both ends dropped the String (*renaming the parameter was the whole difference*); and `E3034` because `function gives (…)` spells `function <name> (` character for character, so a match arm was recorded as a declaration. Review then found **TWO more reachable PANICS**: `opensBlockAt` spelled the keyword-is-a-name exclusion **once per arm and the `otherwise` arm had no copy**, so `while Ops.otherwise(i) 'loop'` panicked — and its enum-case half (`while k == Kw.otherwise 'loop'`) **had been live since `keyword-named-case-members.md` shipped, behind that spec's own prose claiming it was impossible**; and `recordScannedInterface` closed an interface body on a raw `end`, so `function end()` left the sweep's depth permanently one too deep and every later `depth == 0` gate stopped firing ⇒ `panic requireConstructible`. ⚠ **That one needs THREE requirements to show** — with one trailing requirement the stray `+1` cancels the interface's own `end` and the file compiles GREEN; the reviewer notes a two-case probe calls it clean. ⭐⭐ **THREE FALSE PROSE CLAIMS, EACH THE COVER FOR A DEFECT** — including the implementer's own *"`if`/`else`/`otherwise` are NOT refused"*, measured false (`while otherwise 'loop'` IS refused, correctly, BY DERIVATION — only the prose was wrong, which is the argument for deriving the set rather than writing it down). ⭐ **A DIVERGENCE, AND IT IS THE ORACLE THAT IS WRONG**: the bootstrap accepts `function from(n N)` unused (96,421 bytes) but answers **`E3003 Undefined variable 'from'`** when you call it — undefined for a function it just accepted. shv2 accepts the call; the bootstrap gap is filed. Ports `keyword-parameter-names.md` (4 cases, **all disabled behind `module typealias`** — zero stdlib demand, `module` is not even a lexer keyword) and `cross-file-union-keyword-case-range.md` (1 enabled free, 1 disabled on nested payload-bearing unions), plus an authored `keyword-as-a-declared-name.md`. ⚠ **Scale: +274→+5,879 allocs, ×1.68…×1.96 per doubling = LINEAR** (the reviewer corrected the implementer's "sub-linear"), flat 0.04% of total — a lazily built per-file SPARSE index of keyword tokens; a dense-column first cut had cost +5,487 allocs at rung 0 alone. Fragments: additions only on both local lanes, zero pre-existing goldens moved. ⚠ arm64 SKIP — remote, UNVERIFIED; `Parser.maxon` only, nothing target-specific | L-parser-decl | ✅ DONE | `slice/D8-keyword-as-declared-name` | 2026-07-30T00:52Z |
| **W1** | **A FUNCTION TYPEALIAS LOSES `ExitCode`'s WIDTH, so an `ExitCode`-returning function VALUE TRAPS ON WASM.** ⭐ **Not a new discovery — this row EXISTS to sequence a follow-up the corpus already documented and named.** `closure-capture.md`'s `targets:` exclusion on `closure-capture.block-local-method-receiver` diagnosed it in full on 2026-07-27 and says outright that *"the function-alias registry is a DIFFERENT door and stays its own follow-up rung"*; nothing had opened that rung. **Opened 2026-07-30 by the D1b coordinator**, on a loop tick with no claimable row, after D7 halted on it. ✅ **RE-VERIFIED BY RUNNING, with its control** (arm64-macos host, fresh binary): `typealias Thunk = function() returns ExitCode` + `nine() returns ExitCode` + `callThunk(t Thunk)` ⇒ native **9**, wasm **trap "indirect call type mismatch" (134)**; the identical program with a ranged `Integer` alias ⇒ **9 on BOTH**. So `ExitCode` is the sole offender, exactly as recorded, and **no closure is needed at all** — a bare function reference through a typealias does it. ⇒ the mechanism is a RETURN-TYPE WIDTH question, not a closure one. **ROOT CAUSE, confirmed at the code:** `Parser.interfaceReturnMaxonType` (`:24487`) special-cases `ExitCodeBuiltinName` ⇒ `MaxonType.exitCode` (u32/i32) — that is the twin already fixed — while `Parser.functionAliasReturnType` (`:29742`) rebuilds through `maxonTypeOfTag`, which yields `named` ⇒ **i64**. The call site then declares `(i64)->i64` against a `__fnref_` thunk typed `(i64)->i32`. ⚠⚠ **AND THE REBUILD IS SPELLED TWICE** — `TypeResolution.maxon:226` is the project-interner copy and **its own comment at `:217` calls itself "the twin"** — so this is ONE FACT IN THREE PLACES (both rebuilds plus `interfaceReturnMaxonType`) and the fix is to SINGLE-SOURCE "what MaxonType does this return-type NAME denote?", not to add a fourth copy of the `ExitCode` arm. ⭐ **ACCEPTANCE IS A RATCHET UNLOCK, which is the strongest kind:** REMOVE the `wasm32-wasi` omission from `closure-capture.block-local-method-receiver` (Workstream S — an enabled test may never be re-disabled, and this un-restricts one), plus an authored case pinning the bare `ExitCode`-through-a-typealias repro and its ranged-alias control. ⭐⭐ **IT ALSO UNBLOCKS D7**, whose rung is HALTED on a RED wasm lane: D7's `closure-capture.block-local-overload-arg` uses `IntThunk = function() returns ExitCode`, so it fails for THIS reason and not for the *"closure-captured struct arguments on the wasm backend"* its own commit blames — **D7's attribution is wrong, and the correction is already in the file its case lives in.** D7's agent was right to halt and right to refuse to re-disable the case ✅✅ **CLOSED 2026-07-30** (main `7c716fb83`; arm64-macos host **2500 → 2511/0**, **wasm32-wasi 2497 → 2509/0**, x64-linux SKIP — no WSL on this host, arm64 remote lanes SKIP — UNVERIFIED). **11 fragment ADDITIONS, ZERO modifications**, and **no `targets:` marker re-added anywhere** — the ratchet held. ⭐ **THE DELIVERABLE IS A RATCHET UNLOCK, WHICH IS THE RARE DIRECTION**: `closure-capture.block-local-method-receiver` lost its `wasm32-wasi` omission and its whole `OMITTED` prose block, and `closure-capture.md` now carries **no target restriction at all**. Verified by running it filtered and alone on wasm before the marker came off. **THE FIX IS THE DE-DUPLICATION, not the `ExitCode` arm**: two new authorities in `TypeResolution.maxon` — `builtinTypeNameTag` (`:116`, the ONE name→tag table) and `maxonTypeOfStoredTypeName` (`:141`, the ONE rebuild of an interner-free `(tag, name)` pair) — asked by four doors, with `interfaceReturnMaxonType`'s five hand-written arms DELETED and `ExitCodeBuiltinName` made file-private (its comment claiming it was *"EXPORTED because the parser must claim it too"* had become false). The interner difference is preserved rather than flattened: the helper takes the caller's already-interned `nameId`, so the parser supplies the FILE's interner and `TypeResolution` the project's. ⭐ **WHY ONLY THE RETURN HALF COULD EVER BREAK, verified at the code and structural rather than incidental**: on wasm an indirect call's PARAM valtypes come from `indirectCallParamValTypes` (`StdToWasm.maxon:589`), which has only `argCount` + `argFloatMask` and **physically cannot carry a width**, and the `__fnref_` thunk's params are a uniform i64/f64 — while the RESULT is carried explicitly off the parser's tag column (`indirectResultType`). A DIRECT `ExitCode` call was always fine because wasm reads the *callee's* declared return. ⛔⛔ **THE INDEPENDENT REVIEW FOUND A SILENT WRONG ANSWER — the worst class — AND IT WAS MERGE-BLOCKING.** `coerceOnStack` (`StdToWasm.maxon:737`) widened every narrow value with **`i64.extend_i32_s`**, but `ExitCode` is a **u32**, so its top bit is a VALUE bit: `returns ExitCode` of 4,000,000,000 printed **`4000000000` natively and `-294967296` under wasmtime** — through a direct call, a witness dispatch AND the function typealias. ⚠ **PRE-EXISTING and alias-independent — but W1 turned the alias path from a LOUD TRAP (exit 134) into this SILENT WRONG ANSWER, on the construct it ships**, which is why it was fixed here and not filed. Verified by the coordinator directly: `main` prints `-294967296`, the branch prints `4000000000`. Fixed unsigned; nothing in the tree constructs a signed sub-64 `StdType`, and `emitOsAllocPages`'s deliberate signed widen of `memory.grow`'s `-1` calls the encoder directly and is untouched. ⭐⭐ **THE REVIEW ALSO CLOSED THE DUPLICATION IN THE DANGEROUS DIRECTION**: `denotedNamedType` (`TypeResolution.maxon:762`) spelled `ExitCode`/`MaxonType.exitCode` itself, so the AUTHORITY for what a name denotes and the TABLE of which names are sub-64 were two independent lists — and adding the next such name to the authority (the natural place, since without an arm there it is E3011 everywhere) while forgetting the table **is W1's own bug re-armed.** Its arm now reads the table, admitting only the `exitCode` answer — a bound that is MEASURED, since `enum String` with `returns Self` compiles and arrives as `named("String")`, for which the table's `string` answer would be wrong. Also deleted `Parser.StringBuiltinName`/`CharacterBuiltinName`, two constants duplicating `IrInterface`'s canonical ones — exactly what `BoolConformerName`'s own header forbids. ⭐ **AND THE STALE PROSE THAT EXPLAINS WHY NOBODY LOOKED**: `StdToWasm.maxon:319` asserted the result half *"needs no such guard"* because the result type is the declared return *"(`ExitCode` included, since `Parser.interfaceReturnMaxonType` claims that name)"* — there were three more doors. ⚠ **`Character` is deliberately NOT in the table**, and the reachability argument was verified by probe rather than accepted: a function typealias stores `Character`/`String` by TAG with an empty name (`parseTypeReference` settles them syntactically), so the table is never consulted — now pinned by a spec instead of prose. ⚠ **Honest limit recorded in the header**: a missing table entry is **loud only on wasm**, silent on x64/arm64 | **L-fnalias** *(NEW — `Parser.maxon:29742` + `TypeResolution.maxon:226`, the function-alias return rebuild, plus the review's `StdToWasm.maxon` coercion. Disjoint from the three parser lanes' sites and from L-types' `SignatureIndex`; shared only the FILE `Parser.maxon` with D7, thousands of lines away — and `main` did not move while it was in flight, so nothing had to be re-gated)* | ✅ DONE | `slice/W1-fnalias-exitcode-width` | 2026-07-30T10:20Z |
| **W1b** | **W1 LEFT A REACHABLE COMPILER PANIC ON `main`, AND ITS OWN FIX IS WHAT MADE IT REACHABLE.** `let v = t(); let f = function() gives v` — where `t` is an `ExitCode`-returning function alias — is **`panic at StdLoweringShared.maxon:36: accessWidthFor: no memory-access form for u32`**. MEASURED on `origin/main` `cee679f8c` (built from source, host lane). ⭐ **W1 was RIGHT to make the alias return `exitCode`; that is precisely what makes the capture reachable.** `mintIndirectCallResult` types an indirect call's SSA RESULT from the same `functionAliasReturnType`, so the result VALUE is now `exitCode`-tagged, `exitCode` lowers to `StdType.u32`, and `buildClosureEnv` stores a capture at `fieldStorageType`'s width. W1 guarded the SIGNATURE column and the VALUE column came along with it. ⚠ **W1's suite cannot see it — `main` has NO case that captures an `ExitCode` call result** (checked). ⭐⭐ **TWO ENTRANCES, ONE SINK, and only one entrance is W1's**: `Parser.interfaceReturnMaxonType` has minted `exitCode` results since P1.7a, so an interface method `returns ExitCode` whose result is captured panicked BEFORE W1 too — confirmed on the pre-W1 binary. Fixing either door leaves the other live ⇒ fix the SINK. **Sinks enumerated by PROBE**: struct-field assign, array push, loop-carried var, plain return and direct call are all clean; the closure ENV STORE is the only path that uses a value's own tag as a memory-access width. Cure: `envSlotStorageType`, the ONE door asked by both the store (`buildClosureEnv`) and the read (`emitCaptureRead`) — an env slot is `EnvSlotBytes` wide by construction so a scalar capture owns a whole word, and zero-extension is unambiguous for an unsigned value, the identical reason `accessWidthFor` already admits `u8`. NOT folded into `fieldStorageType`: a struct field is packed against neighbours. Carries three pins, all sabotage-verified, one of them a WIDTH pin at `100000` because a `9` passes even through a 1-byte slot ✅✅ **CLOSED 2026-07-30.** x64-windows **2688/0** · x64-linux **2559/0** · wasm32-wasi **PASS** · arm64 SKIP — remote, UNVERIFIED. Fragment **ADDITIONS ONLY, zero modifications** — byte-identical codegen for every pre-existing program. ⭐ **BOTH SABOTAGES RUN, and they catch DIFFERENT things.** Revert the two call sites ⇒ all three pins panic. Force the slot to ONE BYTE ⇒ the wide-value pin fails on its VALUE (`expected 9, got 4294867465`), while the other two fail only as `codegen changed — golden fragment mismatch` — a real catch, but one that needs their fragments to already exist and says *"something moved"* rather than *"the answer is wrong"*. That difference is why the width pin was added and is recorded on the case. ⚠ **A STALE `bin/maxon.exe` nearly made this whole battery a lie, and the GATE caught it, not me** — the rebase brought newer `maxon-sharp/` sources and `cross-target-gate.sh --skip-build` REFUSED rather than reporting. Rebuilt the bootstrap, rebuilt shv2 on it (**8,451,767 → 8,395,855 bytes**, so the staleness was real), re-ran everything. Every number above is from after that. ⭐ **Two incidental finds from the D7 review were RE-MEASURED here rather than carried over, and one was already FIXED**: the `ExitCode`-above-2^31 sign divergence is gone on this `main` (W1's own review closed it — `3000000009` now prints identically on both targets), so the stale row was dropped instead of filed and the spec prose citing it was corrected. The nested-closure panic DOES still reproduce (`Integer`, nothing to do with `ExitCode`) and is filed under "Future rungs". | L-parser-closure | ✅ DONE | `slice/W1b-exitcode-capture-slot` | 2026-07-30T12:05Z |
| **D9** | **`Self.<member>` IN AN EXPRESSION IS NOT SUPPORTED — one mechanism, TWO symptoms, and one of them is a REACHABLE COMPILER PANIC.** Opened 2026-07-30 by the W1 coordinator on a loop tick with no claimable row. ⚠ **It SUPERSEDES the "Future rungs" entry W1's review filed**, which named only the enum symptom and only from a subagent's report; **both symptoms below were reproduced by the coordinator directly, and BOTH programs were verified LEGAL by RUNNING them on the oracle (each returns 42).** ⇒ shv2 is wrong in both, and the two differ only in how loudly. **(1) ⛔ PANIC** — `Self.<caseName>` inside an ENUM method body (`if self == Self.off … return Self.on`) ⇒ `panic at Parser.maxon:20956: requireConstructible: \`type Toggle\` is being parsed right now, but the declaration sweep never recorded it — \`recordScannedType\` and \`parseTypeDeclaration\` disagree about what opens a type declaration`. ⭐ **The message is wrong about its own subject TWICE**: it calls an `enum` a `type`, and it blames a `recordScannedType`/`parseTypeDeclaration` sweep disagreement that is **not** the cause — the D4/D8 shape of a diagnostic asserting an untruth about why it fired, which is what makes this one expensive to debug rather than merely broken. **(2) `E2010 Expected '{' but got '.'`** — `Self.make(self.n)` (a STATIC call) inside a `type` body. A clean refusal, but of a legal program. ⭐⭐ **THE TWO ARE ONE BUG, and symptom (2) is what names it: `Self` in an EXPRESSION position supports only `Self{…}`, the struct literal.** `Self.` falls off that single expectation — in a `type` body it surfaces as the "Expected `{`" parse error, and in an enum body it routes on into `requireConstructible` and panics. ⇒ **the fix is to make `Self.` resolve as the TYPE NAME IT DENOTES**, taking the same qualified static-call / enum-case-name path `Box.make(…)` and `Toggle.off` already take — not to add a second `Self`-specific arm. ✅ **CONTROL, measured**: `Self` as a RETURN TYPE already works (`returns Self` compiles and runs, exit 0), so this is purely the expression position. ⚠ Acceptance must pin BOTH spellings **and** that control, since a fix that only taught the enum path would leave (2) refusing a legal program. Search `/specs` for existing `Self.`-in-expression coverage before authoring ✅✅ **CLOSED 2026-07-30** (main `d3d223b10`; arm64-macos host **2658/0**, wasm32-wasi **2656/0**, x64-linux SKIP — no WSL here, arm64 remote lanes SKIP — UNVERIFIED). **36 fragment ADDITIONS, ZERO modifications.** ⭐ **BOTH SYMPTOMS FELL TO ONE TOKEN REWRITE, exactly as the row predicted.** `typeBaseTokenAt` returns the base token with `Self` already replaced by the enclosing declaration's own name — same line, column, span, `identifier`-kinded — and **five existing base readers were repointed at it** (`parseStructLiteral`, `parseQualifiedCall`, `parseEnumLiteral`, `parseTypeQualifiedBound`, `parseDottedPrimary`'s arm selector), so `Self.off` inside `enum Toggle` **IS** `Toggle.off` and **no downstream arm knows the keyword exists**. There is no `Self` arm anywhere — the anti-goal the brief set. Two things were DELETED: `readStructLiteralTypeName` (a second copy of the `Self` reader) and `enclosingSelfType`'s inline file-scope refusal, so the type position and the expression position now share one answer and cannot disagree. ⭐ **THE LOAD-BEARING ORDERING DECISION**: `parseDottedPrimary`'s VALUE-based arms deliberately keep reading the RAW token, so a local named after the enclosing type still shadows `Gate.` (the existing "a value outranks a type name" rule) without capturing `Self.`. ⛔ **A SECOND REACHABLE PANIC, through the same door**: `Self{}` — and `Toggle{}` — inside an `enum`/`union` body reached the identical lying `requireConstructible` panic; now a positioned E2015 naming the cure. **Coordinator-verified: `main` PANICS, the branch gives the clean error.** ⚠ **Both reference compilers CRASH here** (the bootstrap on an unhandled `IrEnumType`→`IrStructType` cast), so there was no oracle spelling to match. ⛔⛔ **THE INDEPENDENT REVIEW FOUND AN ORACLE DIVERGENCE THE IMPLEMENTER'S OWN SPEC PROSE ASSERTED WAS IMPOSSIBLE** — *"no binding named `Self` can exist"*. A **PARAMETER** can, because D8 lets a keyword spell a declared name, so `Self.make(41)` inside `twin(Self Num)` resolved to the parameter (`'int' has no method named 'make'`) **while `Self{n: 42}` three lines away still meant the TYPE** — one word, two meanings, in one body. The bootstrap resolves `Self` to the type and merely reports the parameter unread (**E3012**, coordinator-verified), so this was a divergence, not a judgement call. Fixed at the ONE place the value-outranks-type question is asked (`baseNamesAValueInScope`), shared by the statement and expression doors, which had spelled it out separately with a comment asking that they be kept in step BY HAND. The review then found the **store** door was a THIRD copy and closed it too. ⚠ **`/specs` HAS NOTHING — that IS the finding**: zero `Self.`-in-an-expression anywhere in 284 files; `specs/reserved-self.md` is entirely about the LOWERCASE receiver. Nothing to port, nothing to flip ⇒ **36 authored cases**, of which **22 were RED** against the pre-D9 compiler. ⚠ **REBASED ACROSS 60 COMMITS** (D7's overloading rewrote +479 lines of the same file, and T1 landed underneath); the code commits replayed cleanly and the full battery was re-run on the merged tree, where both repros and the control still return **42** | **L-parser-postfix** *(the postfix / member-resolution walk — where `Self.` belongs and where D1e, D2 and D1b all closed; the lane is FREE. Shares the FILE `Parser.maxon` with D7, which is live in L-parser-decl, so whoever lands second re-gates on the merged tree)* | ✅ DONE | `slice/D9-self-member-expression` | 2026-07-30T11:05Z |
| **T1** | ⛔ **A FIELD TYPED BY A TYPEALIAS DECLARED IN ANOTHER FILE CLASSIFIES AS *MANAGED*, so a SCALAR field read off a TEMPORARY is FALSELY REFUSED.** Opened 2026-07-30 by the D9 coordinator, whose gate battery caught it on the rebase onto R4.2. **`main` is GREEN at 2515/0 over this** — no committed case reads a scalar field off a temporary in a cross-file project, so the suite is silent on it, which is exactly the shape §13 warns about. ✅ **MEASURED, and the trigger needs BOTH halves** — each variant run, not reasoned: **(a)** `Gate.twice(21).n` where `n as Num`, `typealias Num = int(0 to 1000)`, `Gate` and `Num` in `gate.maxon` and the read in `main.maxon` ⇒ **`E2015` "reading the managed field 'n' out of a TEMPORARY `Gate`"** — a *false* refusal, the program is legal; **(b)** the SAME program in ONE file ⇒ **runs, 42**; **(c)** the same directory project with the field typed by the BUILTIN name `ExitCode` instead of an alias ⇒ **runs, 42**; **(d)** the same alias-typed field read off a BOUND name rather than a temporary ⇒ **runs, 42**. ⇒ **cross-file alias + temporary receiver, and nothing less.** ⭐ **The refusing guard is not the bug** — `requireFieldOutlivesItsRead` (D2's, which fixed a real UAF that returned the freed-fill byte `0x3F3F3F3F` as a program's answer) is correct and its own message even says *"A SCALAR field is copied rather than borrowed and needs none of this."* **The bug is the CLASSIFICATION it asks**: a `named` type whose typealias declaration lives in another file fails to resolve to its ranged-int nature and defaults to managed. ⭐ **Same family as D4's finding, inverted** — there a `named` that NO registry declares read as an INTEGER; here a `named` that a *sibling file's* registry declares reads as MANAGED. D4's row already names the door (`declaredSlotType`, `SignatureIndex.maxon:5560-5615`, which re-tags a `named` for the registries that know it and falls through otherwise) and its warning was about false rejects one level below the test — this is one. ⚠ **Acceptance must pin all four variants above**, since (b)/(c)/(d) are what prove the fix targeted the cross-file resolution and not the guard. **BLOCKS D9**, which is otherwise complete, reviewed and was green at 2547/0 — its `Self-static-across-a-file-boundary` case uses a directory project deliberately (to pin that the rewritten token's interner id does not leak across files) and is the first thing in the corpus to reach this ✅✅ **CLOSED 2026-07-30** (main `ea7f47b8a`; arm64-macos host **2622/0**, wasm32-wasi **2620/0**, x64-linux SKIP — no WSL here, arm64 remote lanes SKIP — UNVERIFIED). ⛔⛔ **THE ROW ABOVE UNDERSTATED IT: THIS FIXES THREE REACHABLE DEFECTS ON `main`, AND TWO OF THEM ARE WORSE THAN THE FILED ONE.** The misclassification ran BOTH ways and the corpus could see neither. **(1)** the filed false refusal; **(2)** ⛔ **A LIVE USE-AFTER-FREE** — a boxed-union field classified SCALAR, so `requireFieldOutlivesItsRead` **ADMITTED** the borrow out of a temporary: the program **compiles on `main` and HANGS on the freed box** (coordinator-verified, **exit 152 / SIGXCPU** under `ulimit -t 5`, where 5 was correct). Measured to flip at the **fourth unrelated `typealias`** in the reading file — refused at 0–3 padding aliases, admitted from 4 up; **(3)** ⛔ **A COMPILER PANIC**, found by the independent review — `panic at Arm64Backend.maxon:1502: bl to unknown function '__destruct_Payloaded'` on a valid two-file program, coordinator-verified through a **BOUND receiver**, i.e. reachable without T1's temporary door at all. ⭐ **ROOT CAUSE IS NOT THE DOOR THIS ROW NAMED.** `declaredSlotType` is correct and not on the path. The id is lost **in transit, one call later**: `fieldTypeOf(…, into: self.artifact.typeNames)` **adopts**, re-interning a `named` against THIS FILE's table, and the result is handed to `typeIsManaged`, which resolved that id against the **PROGRAM-WIDE** table. Same number, different table ⇒ **managedness followed INTERNING ORDER.** Proof it is order and not type: adding three unrelated `typealias`es to the reading file made the refused program compile and return 42. Single-file, both tables fold in the same order and every id coincides — which is exactly why 2515/0 was green over a use-after-free. ⭐⭐ **THE FIX IS TO STOP THE TYPE CROSSING AT ALL**, not to resolve it better: the guard now asks **`structFieldIsManaged(structName, index:)`** — literally the per-field predicate `__destruct_<T>`'s cascade loops over — so *"the box frees it at drop"* and *"the read borrows it"* are ONE answer and cannot come apart. The review then found the same shape **one door further in** and collapsed a THIRD spelling: `managedNameDropCallee`'s union arm resolved payload ids against the caller's interner while `installUnionDestructors` asked the correctly-paired project layout — the struct arm two lines above already asked by name, and `unionBoxDropCallee` already answered correctly over the index's own table (OPEN #52's fix), **so the same bug had been found and fixed twice in two doors and the third was never visited.** ⭐ **AND THE TRAP IS CLOSED, NOT JUST SPRUNG**: `typeIsManaged` now DEMANDS its interner (`from:`), the discipline `classifyUnionPayload` / `managedFieldDropCallee` / `adoptTypeFrom` / `foreignNameOf` already stated and this one query lacked. **Review verdict on the file-list deviation: the impossibility claim is CORRECT** — audited structurally, with all three escape routes (stop `fieldTypeOf` adopting; pre-resolve in `declaredSlotType`; share one interner) shown to fail — *"the code is right; the process was not."* All ten `from:` sites verified correct rather than merely compiling. ⚠ **INTEGRATION, and it is why the re-gate is owed and not optional**: 22 commits landed at the gate, **two of them new `typeIsManaged` callers**; the rebase was textually clean and the **build was not**. Each interner was derived from where its type comes from — `self.artifact.typeNames` for the parser-owned one, and a new index-side `instanceArgIsManaged` for the index-owned one (mirroring `instanceArgAggregateName`) so the private interner is never exported. ⚠ **14 arm64-macos goldens regenerated across two isolated commits, NONE of them this rung's** — measured, not asserted: `main` ALONE failed **14** on this host, a superset of the branch's 11, and **every delta across all 11 managed-memory files is the identical upstream rename `__arr_set` → `__arr_mem_set` (28 lines, nothing else)**, which x64 and wasm already carried. **T1 takes `main` from 14 red to 0 on arm64-macos.** ⭐ scale-test **A/B byte-identical at all six rungs** (measured against the branch's MERGE-BASE, not `main` — see the log row's recipe correction); the ladder emits ONE file per rung and this defect needs TWO, so the instrument is **structurally blind** to it in every column | **L-types** *(`SignatureIndex.maxon` · `TypeResolution.maxon` — the lane D4 closed, and it is FREE. Deliberately NOT L-stdlib, where R4.4 is live)* | ✅ DONE | `slice/T1-cross-file-alias-field-class` | 2026-07-30T12:40Z |
| **5d** | **RE-EXTRACT AND RE-MEASURE the runner.** A LOOP, not a slice | — | ⛔ BLOCKED | on ~~G1~~ ✅ · S2 · D1 · D2 · D4 · D5 | — |
| **6b** | **The differential oracle** · `-j1==-jN` · clean `mm-trace` | — | ⛔ BLOCKED | on **5d** | — |

> ### ⭐⭐ EVERY R4 ROW PAIRS A REACHABLE BUILTIN WITH AN UNREACHABLE WRAPPER — READ THIS BEFORE CLAIMING ONE
>
> **Measured three times, by three rungs, each rediscovering it from scratch. It is one fact; here it is once.**
> Every `R4.x` row as originally written bundles a `__Managed*` **builtin** (reachable now, spelled with
> `.toByteArray().managed`) with the `stdlib/` **wrapper type** that presents it (`File`, `Directory`) — and
> **every wrapper method's signature takes `FilePath`**, so the whole wrapper is gated behind
> `stdlib/FilePath.maxon`, which stops at `String.byteAtOrPanic` and needs `__Builtins.executablePath`.
>
> | rung | reachable half | blocked half | how it split |
> |---|---|---|---|
> | **R4.2** | `__ManagedFile` ×11 methods | `File`/`FileInfo`, `file-io.md` (7) + `file-info.md` (6) | → **R4.5** |
> | **R4.3** | `__ManagedDirectory` ×7 methods | `Directory`, `directory.md` (9) | → **R4.5** |
> | **R4.4** | the `__ManagedMemory` object surface | — (no stdlib wrapper) | did not split |
>
> **The tell is one grep, and it takes ten seconds:** `grep -c FilePath specs/<the-spec>.md`. Non-zero ⇒ that
> spec is R4.5's, whatever the row says. `managed-directory.md` reads **0**; `directory.md` reads **19**.
>
> ⇒ **A row naming both halves is a row that has not been measured yet.** Do the grep before you plan, not
> after — R4.2 planned against the row and had to re-cut mid-rung.

> ### ⚠ ABSENT HOST GOLDENS ON `main` RIGHT NOW — for the PHASE-BOUNDARY sweep, not for a rung
>
> **Found 2026-07-30 while landing R4.2, by looking rather than by any gate.** D1b and W1 landed ~55 new
> spec cases with their **`arm64-macos` goldens committed** and their **`x64-windows` and `x64-linux`
> goldens ABSENT** (`enum-union-method-receiver` 6 · `first-class-functions` 10 · `union-managed-payload`
> 37 · `union-interface-payload` 1 · `where-clauses` 1). A suite run on `main` regenerates all of them as
> untracked files.
>
> **Nothing is red, and that is the problem: A MISSING GOLDEN NEVER FAILS.** Absence is invisible to every
> gate in this process, so it can only be found by going to look — which is exactly why the stale-golden
> sweep is a phase-boundary job with its own entry. Deliberately **not** folded into R4.2's merge: they are
> two other rungs' goldens, and minting them inside an unrelated rung would put 55 unreviewed expected-value
> files under a commit message about file IO. The R4.2 merge carries **only** the four its own new cases
> needed, plus the two its own rename orphaned.
>
> ⚠ Note the direction, because it is the reverse of what this process usually leaks: the lane that got its
> goldens is the **remote, skipped** one, and the lanes that did not are the two that run locally on every
> rung. Whatever produced that asymmetry is worth finding at the sweep.
>
> **⛔ UPDATE 2026-07-30, and it is a CORRECTION TO THIS NOTE: the x64-windows half landed by ACCIDENT in
> R4.4, and the note above was untrue for that lane from the moment R4.4 merged.** R4.4's first commit
> (`f3a53a70e`, its RED acceptance set) staged `specs-shv2/` broadly and swept all 55 in. They are CORRECT
> — an unfiltered run on a green tree is exactly how a golden is minted — so they were kept rather than
> reverted; removing them would re-open the hole to buy tidiness. **x64-linux's 55 are still ABSENT** and
> remain the sweep's.
>
> ⭐⭐ **The coordinator's own check is what missed it, and the reason generalizes.** Step 8's file-list
> review was run as `git diff --name-status main...HEAD -- . ':!specs-shv2/fragments'` — fragments filtered
> out to keep the list readable, which filtered out precisely the directory another rung's work can ride in
> through. **A diff review that excludes the noisy directory cannot see what hid in it.** Review the
> fragment diff SEPARATELY and by GROUP (`… | sed 's|.*/fragments/[^/]*/||' | cut -d/ -f1 | sort | uniq -c`)
> — one line per spec directory is readable AND complete, where a filter is readable by being wrong.

> ### ⭐⭐ D1 IS A SMALLER UNLOCK THAN THE ROW CLAIMED — MEASURED 2026-07-29, SECOND STALE DEPENDENCY EDGE IN A ROW
>
> **The hard machinery already exists.** The identical body that is refused as a METHOD compiles and
> runs today as a **free function**: `union` payload construction + `match` with payload binding
> returns **16**, the `enum` equivalent returns **2**. ⇒ **D1 is a DECLARATION-SITE + RECEIVER rung**
> — not match, not layout, not payload. That is the good news and it should keep the rung small.
>
> **The bad news, measured on the same afternoon:** four of the five harness files D1 claims to unblock
> do not become compilable when the declaration is accepted. Their methods bind a **managed** payload
> out of the receiver, and that is refused by a SEPARATE rule:
>
> ```
> E2015: binding the managed payload 'n' out of a `union Exec` that is not an OWNED local —
>        a borrowed union (a parameter, A RECEIVER) cannot have its payload MOVED out
> ```
>
> Isolated to three cases, each run: **owned local + managed bind ⇒ WORKS (9)** · **borrowed param,
> managed payload not bound ⇒ WORKS (5)** · **borrowed param + managed bind ⇒ REFUSED.** The refusal's
> own text already anticipates "a receiver", so it fires the instant D1 lands. ⇒ **D1b**, above.
>
> | harness method | binds a managed payload out of `self`? |
> |---|---|
> | `TestOutcome.isPass` (`SpecTestRunner.maxon:209`) | **NO** — `pass gives true` / `fail gives false`. **D1 alone unblocks it.** |
> | `PeReadError.displayReason` (`PeSectionReader.maxon:61`) | YES, **read-only** (`path.toString()`) |
> | `WorkerRecord.spec`/`.test` (`SpecWorkerPool.maxon:464,471`) | YES, **moved out** (`pass(s, _) gives s`) |
> | `Elf`/`Macho`/`DataSection`/`TypedValue` `.displayReason` | YES, same convention as PeReadError |
>
> ### ✅ D1 CLOSED 2026-07-29 (`6b7c8a94b`..`f4c1c43e5`, 2372 → **2423/0**) — AND THE REVIEW EARNED THE RUNG
>
> The prediction above held: it is a declaration-site rung, and the acceptance list came out at **10**
> rather than 11 — `error.enum-method-non-exported-cross-file` turned out **not** to be an enum-method
> case at all. Its subject is FUNCTION VISIBILITY, which shv2 has never enforced: `func.visibility` is
> written at `Parser.maxon:8525` and **read nowhere**, no shv2 path emits **E3008**, and the registry
> proves it structurally (E3008 carries `csharp` + `selfhosted` claims and **no `shv2` claim**, and a
> code shv2 emitted could not be missing one). Its twin `export-keyword/error.non-exported-function-cross-file`
> was already disabled for exactly that reason. **The coordinator's triage was wrong and the
> implementer's disable was right, with better evidence than the triage had.**
>
> **⭐⭐ THE INDEPENDENT REVIEW FOUND THREE REACHABLE DEFECTS OVER A GREEN 2416/0 SUITE — every one in
> code both the implementer and the coordinator had called done.** The pattern is now unbroken across
> every rung of this phase.
>
> 1. **ONLY THE FIRST METHOD OF ANY ENUM WAS EVER SCANNED.** A method's closing `end` carries its block
>    LABEL; the sweep's new depth walk returned to 0 on the `end`, advanced, and landed the cursor **on
>    the label**, which the depth-0 fall-through handed to `readEnumCaseInto` — a charLiteral read as a
>    string-backed case, throwing into the caller's `otherwise break`. Three measured consequences: the
>    2nd+ method's return type never reached the whole-program index (⇒ `panic at LowerMaxonToStd:114:
>    valueTagToStdType: an 'unresolved' tag reached lowering`); **every case declared after a method was
>    SILENTLY DROPPED** from the layout; and the enum's own `end` was never consumed, so the outer sweep
>    resumed *inside* the body. ⭐ **Coordinator-verified against the pre-fix parser**: a case after a
>    method reads `E3034 unknown enum case: 'omega'`, and the same program returns 7 on the fix.
>    `recordScannedType` is immune only because its depth-0 fall-through advances a token instead of
>    reading a member. ⚠ **The rung's own 38 new acceptance cases were structurally blind to it** —
>    `enum-full.md` puts its methods after all cases and mostly declares one.
> 2. **`self.<name>` inside an enum method PANICKED** — `enclosingLayout` died resolving a `StructLayout`
>    no enum declares, with a message blaming *"recordScannedType and parseTypeDeclaration disagree"*, a
>    disagreement that never happened. Both spellings reached it (read and assignment). Now a positioned
>    E2015 at the one door both pass.
> 3. **Two walks read an enum's CASE LIST as block structure.** D1 newly points `ensureSiblingReceivers`
>    at an enum body, and a case may be spelled with a keyword: a case named `end` broke the walk early
>    (a legal sibling call became `E3004`), a case named `while` ran it PAST the enum's `end` so a
>    *later type's* method was adopted as a sibling. Cured by giving the walk the enum's own member level
>    and turning `atEnumBodyEnd` into the index-taking `enumBodyEndsAt` — **three walks ask that rule and
>    only two were cursor-driven**, the one-fact-written-twice shape again.
>
> **⭐ THE SAFETY ARGUMENT WAS RE-DERIVED INDEPENDENTLY AND HOLDS — but its written reason was wrong.**
> Eight escape routes for a boxed union's `self` were probed (return, rebind-then-return, `match self
> gives self`, `Array.push`, a struct field, a consuming argument, another union's payload, `async`, and
> two closure spellings); **all refused, none by an enum-specific check**. But `parseMethodCall`'s header
> still claimed *"`parseSelfPrimary` refuses a bare `self` as a value … that refusal is the whole
> load-bearing mechanism"* — true before D1, a **struct's only** after it. Corrected in place, naming the
> three rules that now carry it.
>
> **⭐⭐ AND A CROSS-TARGET LANE WAS SILENTLY TESTING NOTHING OF THE RUNG.** D1's three spec files carry no
> `<!-- targets: -->` marker, so every local target owes them goldens — and **only x64-windows had any**.
> x64-linux ran GREEN over none of the feature, because **a MISSING golden never fails**. 51 goldens
> generated from an unfiltered run and committed (`f4c1c43e5`). ⚠ `arm64-macos`/`arm64-linux` still owe
> the same 51 — remote, hand-synced, **named rather than fabricated**, and not a merge condition.
>
> **⭐ D1 × D5 COMPOSE, AND THAT WAS RE-GATED RATHER THAN ASSUMED.** `main` advanced by two rungs
> (P1.10 tuples + D5 `#if`) while D1 was live, and D5's own row had warned that it wires the directive
> into **`parseEnumDeclaration`'s member loop — D1's exact site**. The rebase was textually clean, which
> proves nothing about composition, so both directions are now pinned as cases: a `#if` selecting between
> two whole methods in the shared member loop, and a `#if` inside a method body — each routed through a
> `match` on a case declared AFTER the directive block, so a wrong post-directive tag fails them too.
>
> ⚠⚠ **AND THE FIRST VERSION OF THOSE TWO CASES DISCRIMINATED NOTHING** (caught by the user). They used
> D5's own target-invariance idiom, `#if os(Windows) or os(Linux) or os(Macos) or os(Wasi)` — **true on
> every target**, so the `#else` is dead everywhere and a compiler that ignored `#if` entirely and always
> took the first branch would pass both. A positive control both models pass, which is the trap the
> receiver spec's own header argues against. Cured by MEASURING the predicate instead: `testing()` is
> FALSE under a plain `build`, so **`#if testing(true)` must take the `#else`** — target-invariant AND
> discriminating (a directive-ignoring compiler returns 3 where the answer is 7). Verified RED.
> ⇒ **A convention borrowed from a neighbouring rung still owes you the question "what does this go red
> for?"**
>
> Gates on the MERGED tree: build 0 warnings · host **2482/0** · x64-linux **2384/0** ·
> **106 fragment ADDITIONS vs `main`, ZERO pre-existing goldens modified** ⇒ byte-identical codegen ·
> no run exits 101 · scale-test allocsDelta **0 at every rung**.
> ⚠ **A self-inflicted repeat, recorded because the shape is the lesson:** forcing a golden regeneration
> with `rm -rf specs-shv2/fragments/*/enum-union-method-receiver/` matched BOTH targets and deleted the
> x64-linux set — silently undoing `f4c1c43e5`, whose whole subject was that this lane had no goldens for
> the feature. **A missing golden never fails**, so nothing went red; it was caught by counting files per
> target. Restored, 51 per local target.
> Three findings outside the rung filed as **D1c** (a missing E3081 check), **D1d** (a pre-existing
> reachable panic, proven D1-independent by a repro with no enum method) and **D1e** (rvalue receivers).
>
> **⭐ THE ORACLE'S OWN SURFACE IS ASYMMETRIC, AND IT SETTLES THE STATIC QUESTION FOR FREE.** Measured
> against `bin/maxon.exe`: an `enum` **instance** method with `match self` works (2); a `union`
> instance method with payload binding works (16); an `enum` **static** method is a **parse error**
> (`E2010`); and a `union` **static** method is accepted-then-misread — `static` is silently ignored
> and the method becomes an instance method, so the call fails with **`E3036 missing argument for
> parameter 'self'`**. That last one is a bootstrap sharp edge worth its own filing. ⇒ **shv2 should
> implement INSTANCE methods and cleanly REFUSE statics**, and the cost of refusing is measured at
> **zero**: a sweep of every `union`/`enum` body in all of `stdlib/` finds **0 static methods**.
>
> ### ⭐⭐ S2's BLOCKERS, MEASURED 2026-07-29 — THE ROW HAD NAMED THE WRONG ONE, AND TWO OF ITS SIX NAMES ARE NOT FILES
>
> Taken by claiming S1 and then *reproducing its premise before planning on it* — which is how the row
> turned out to have no content left. **Every cell below is `maxon-shv2 build stdlib/<file>`, run on a
> freshly built binary**, not read off the plan:
>
> | file | FIRST error today | owned by |
> |---|---|---|
> | `stdlib/FilePath.maxon` | `E2015 ... :3:1: Unsupported: top-level #if` | **D5** |
> | `stdlib/File.maxon` | `E2015 ... :60:48: Unsupported: String method 'addressableBytes'` | **S3** *(new row — was on none)* |
> | `stdlib/Directory.maxon` | `E2015 ... :17:19: a field access on 'path', which is declared 'int'` | **D2** / **D4** |
> | `stdlib/Subprocess.maxon` | `E2015 ... :107:2: a method on \`union Executable\`` | **D1** |
>
> ⚠ **These are FIRST errors, so every row is a LOWER BOUND** — the driver gates at the first refusal
> per file and nothing behind it has been reached.
>
> **⇒ S1 blocks NOTHING here, and S2's real blockers are D5, D1, D2/D4 and S3.** The old spelling
> ("BLOCKED on S1") would have sent the next agent to implement a mechanism that has been live since
> 2026-07-24 — the failure mode this board exists to prevent, arriving through the plan instead of
> through a race.
>
> **⭐ TWO OF THE SIX NAMES IN S2's OWN LIST ARE NOT STDLIB FILES**, so a whitelist can never list them:
> `Executable` is a **`union` inside `stdlib/Subprocess.maxon:89`** (which is *why* Subprocess's first
> error is D1's), and `StringArray` is a **`typealias Array with String`** declared locally in five
> different files (`Json.maxon:110`, `String.maxon:604`, `CommandLine.maxon:4`, `Build.maxon:6,20`) —
> a generics question, not a loader one. The cone is **four files**, not six.
>
> ⚠ **And `FilePath`/`File`/`Directory` contain ZERO `__Builtins.*` calls** (grepped). Only
> `Subprocess.maxon` does — **21 distinct members**, none of them recognized today. That is real S1-shaped
> work, but it sits **behind D1**: the file cannot get past line 107 to reach any of them. ⇒ **Whoever
> takes D1 should re-probe Subprocess afterwards and open the follow-on row then**, with the second
> error measured rather than predicted.
>
> ### ⚠ S2 PROBABLY HAS A SECOND BLOCKER, AND IT IS NOT ON THIS BOARD YET
> §"Future rungs" records that the whitelist's byte-neutrality *"holds today ONLY because none of
> `Clock`/`Sleep`/`utf16` contains a string literal — that is a property of those three files, not of
> the mechanism"*, with the **next whitelist entry** named as the re-measure trigger. **S2 IS that
> trigger**, and `File`/`FilePath`/`Subprocess` are exactly the "substantial stdlib module with a
> literal" the note warns about. ⇒ **Whoever claims S2 must first MEASURE whether one entry moves the
> committed goldens.** If it does, **⬜ DEAD-`.rdata` ELIMINATION becomes S2's blocker** and belongs on
> this board above it. Measure before claiming; do not discover this halfway through.

> ### ⛔⛔ R4 (FILE IO) IS A PHASE, NOT A ROW — MEASURED 2026-07-30, RELEASED UNBUILT, AND IT NEEDS A RULING
>
> Claimed and released the same session. **Everything below was RUN, not read.** The row's own hedge —
> *"scope unknown until someone measures"* — is now discharged, and the answer is that no single rung can
> hold it.
>
> **WHAT THE CORPUS DEMANDS.** `specs/managed-file.md` (**9 cases**) and `specs/managed-directory.md`
> (**10 cases**) test `__ManagedFile`/`__ManagedDirectory` **directly from user code**, are **fully enabled
> upstream (0 disabled)**, and are **entirely absent from `specs-shv2/`**. So the builtin surface is owed
> *whatever* route shv2 takes for the stdlib façade — this is not a question that can be dodged by
> declining to compile `stdlib/File.maxon`. Behind them sit `specs/file-io.md` (7), `specs/directory.md`
> (9), `specs/file-info.md` (6) and `specs/filepath.md` (40), which need the façade and therefore D8.
> **No fixture mechanism exists in the corpus** — every case is self-contained write→read→delete — so
> byte-identical porting is viable and needs no harness work.
>
> **WHY IT CANNOT BE ONE RUNG.** The 19 builtin cases are blocked on a prerequisite the board never named,
> and it is not small. Each of the three routes the corpus uses to obtain a path buffer is **absent from
> shv2**, measured:
>
> | what the acceptance specs write | shv2 today |
> |---|---|
> | `__ManagedMemory.create(n, 1)` | **E3004** — no such intrinsic; shv2 synthesizes ZERO `__Managed*` types |
> | `"lit".toByteArray().managed` | **E2015** — `Array` is a synthesized 48-byte record and *"exposes no fields at all"* |
> | `String.cstr()` / `String.init(mm)` | **E2015** — the cstring intrinsics are not built |
>
> ⇒ a `__ManagedMemory` door + a String↔buffer bridge is **slice 1**, `__ManagedFile` **slice 2**,
> `__ManagedDirectory` **slice 3**, the stdlib façade **slice 4** (behind D8). v1 measures the whole thing
> at **~6,700 lines / ~9 files**, ~3,900 of it per-backend codegen.
>
> ### ⭐⭐ THE RULING IT NEEDS IS ABOUT **TARGETS**, AND THIS IS THE PART NO ROW HAS EVER STATED
>
> **shv2's OS-primitive surface is THREE slots.** `OsImportSlot` (`Compiler/Targets/Shared/OsImportSlot.maxon:25-29`)
> holds exactly `exit`, `write`, `mmap`. Every new primitive costs: an `OsImportSlot` case · a Mach-O
> symbol spelling · a Windows `IatSlot` case · **per-arch** Linux syscall numbers (x64 and arm64 differ) ·
> a WASI import triple · and a lowering arm in each of `StdToX64` / `StdToArm64` / `StdToWasm`.
> **There is no `wasi:filesystem/*` import in shv2 at all** — wholly new, not an extension.
>
> ⚠⚠ **AND THE PRECEDENT IS WORSE THAN IT LOOKS: even the CLOCK is x64-windows-only.** `osReadClock`,
> `osSleepMs` and `osReadWallClock` **panic** on arm64 and on wasm (`StdToArm64Conversion.maxon:484-548`,
> `StdToWasm.maxon:1244-1268`), fronted by a clean **E3104** for reachable calls. So following the
> established precedent — land file IO x64-windows-first behind E3104 — has a consequence this board must
> state plainly: **on an arm64-macOS host NOT ONE of the 19 acceptance cases could execute**, because
> x64-windows binaries do not run natively there and the feature would be E3104-refused on every lane such
> a host can reach. That is a rung whose suite is green because it tests nothing, which is the single
> failure this whole process exists to prevent.
>
> ⚠ **v1 never finished this on arm64-macOS either, and it fails SILENTLY:** `getdents64` has *"no macOS
> import wired yet"* and returns `-1`, which `mrt_directory_next` reads as EOF — so **`Directory.list()`
> returns an empty array with no error** (`Arm64Backend.maxon:2396-2411`). x64-macOS panics on every
> file op. The "debugged reference" is not debugged here.
>
> ⇒ **THE QUESTION FOR THE USER, and it is genuinely not the coordinator's:** does R4 (a) go
> x64-windows-first behind E3104, accepting that it is unverifiable from a Mac; (b) go
> host-target-first (arm64-macOS + wasm), inverting the precedent; or (c) go cross-platform from day one,
> ~6,700 lines and a new WASI filesystem surface? **Also unstated anywhere: WASI Preview2, not Preview1** —
> v1 uses `wasi:filesystem/types` resource handles (`descriptor.open-at`/`.read`/`.read-directory`), and
> `MirToWasm.maxon:15-16` says *"Preview1 … is gone"*, so a port written against `path_open`/`fd_readdir`
> targets an API v1 abandoned.
>
> ⚠ **One more trap for whoever builds it:** v1 registers both handles as **8-byte one-field structs**
> while the real `__ManagedDirectory` runtime block is **40 bytes** — a type-checking fiction with **no
> single source of truth** tying the two, kept in sync by hand.

> ### ⚠ TWO ROWS WILL MOVE EXISTING GOLDENS. THAT SERIALIZES THEM AGAINST EVERYTHING IN FLIGHT.
> A slice that only turns a REFUSAL into an ACCEPTANCE adds NEW specs and NEW fragments — which never
> conflict, because nobody else's branch has that file. A slice that perturbs **committed** goldens
> touches hundreds of files every other branch also carries. **S2** is the certain case (`.rdata`
> shifts); **D4** is the likely one (`ByteArray` stops being `int`, so anything that lowered it changes).
> ⇒ **If your gate battery rewrites goldens you did not add, say so on your claim row before merging** —
> the other agents need to rebase onto you, not the reverse.

---

## History of this document

- **ADOPTED 2026-07-11** — replaced the original M1–M18 plan (in git history). Core-first;
  load-bearing consequence: **generics before ownership** (`64b4dd60e`).
- **AMENDED 2026-07-13 (a)** — the `selfhost-distance` compass is **CUT**; `Set` reclassified
  into core.
- **AMENDED 2026-07-13 (b)** — merged the forked core-first draft. `String` is a **BUILTIN**
  gated on the runtime, not on generics; **closures and conditional conformance are IN core**;
  errors moved up.
- **RESTRUCTURED 2026-07-13 (c) — THIS REVISION.** Re-spined into the two phases above.
  Milestones renumbered `P1.x` / `P2.x`. **Three boundary corrections, each grep-verified
  against the harness — the previous revision's "Gate H" table was wrong on all three:**
  1. **Owned payloads in unions are PHASE 1**, not deferred — `SpecExpectation.compilerError(text String)`
     ([SpecParser.maxon:54](maxon-shv2/Testing/SpecParser.maxon#L54)) and
     `TestOutcome.fail(reason String)` ([SpecTestRunner.maxon:71](maxon-shv2/Testing/SpecTestRunner.maxon#L71))
     each carry a **managed `String` payload**.
  2. **Ranged typealiases are PHASE 1**, not last — `LineIndex = int(0 to u64.max)`,
     `ExitCodeValue = int(i64.min to i64.max)`
     ([SpecParser.maxon:41,46](maxon-shv2/Testing/SpecParser.maxon#L41)). Cheap here: the
     ranges are wide, so the checks are near-vacuous — but the *mechanism* must exist.
  3. **Extensions are NOT used by the harness** (the draft claimed 2; grep says **0**). They
     are forced only by the *stdlib* cone, at declaration level.
  And the finding that makes the split real: **§"The stdlib cone" — the EMIT/DECLARE split.**
- **AMENDED 2026-07-13 (d) — `async` / GREEN THREADS ARE IN CORE, IN PHASE 1.** Running the spec
  suite means running it **in parallel** — that is what `maxon-selfhosted` does
  ([SpecTestRunner.maxon:3401](maxon-selfhosted/Testing/SpecTestRunner.maxon#L3401)
  `runAllSpecTestsParallel`: a persistent worker-subprocess pool driven by `async`/`await` +
  Promises over the green-thread runtime), and it is part of the Phase-1 goal. **So `async` leaves
  "Beyond" and enters core, and Workstream R3 (the GT scheduler) is UN-DEFERRED into Phase 1.**
  shv2's current harness is serial *by deliberate omission*
  ([SpecTestRunner.maxon:11](maxon-shv2/Testing/SpecTestRunner.maxon#L11): *"Trimmed HARD from v1's
  SpecTestRunner: no fragment cache, no parallel worker pool"*) — Phase 1 grows that pool back.
  **The load-bearing consequence: async CO-LANDS with closures and escape at P1.5** — a value
  captured into a green thread **escapes**, exactly as a closure capturing into a heap env block
  does. Building escape analysis single-threaded and adding async later bolts a *second capture
  channel* onto `EscapeAnalysis` — v1's `sys.dropTypeParam` split-brain mistake, precisely.

**Stage ↔ old-M mapping** (so DEVLOG's M-numbered ledger stays interpretable): old M1–M5 =
**Stage 1 / scalar core, DONE**. Old M6–M15 = the mechanism ladder, now split across
**P1.1–P1.9** and **P2.1–P2.5**. Old M16–M17 = **Phase 2's fixpoint**. Old M18 = **Beyond /
budgets**. Old "Stage 0" (tooling) is **CLOSED**. Old "Stage 4" (broaden) is **Beyond**.

---

## Context

`maxon-shv2` is the ground-up rewrite of `maxon-selfhosted` (v1, 191,487 lines). shv2 is at
**44,671 lines** (measured 2026-07-16 — see "The honest sizing"), with a working `spec-test` runner
(**specs-shv2 371/0** as of 2026-07-16 — it was
126/0 across 19 files when this plan was written; Workstream S's on-demand ports are what grew it), a
warm-rebuild determinism gate, the full scalar core, and a **linear** SSA-chordal register
allocator. The bones are good — content-hash query spine, parse-staging, 3 IR tiers
(Maxon→Std→Target, no MIR), flat `StdOp`, x64/PE backend.

**~~The scalar core is DONE.~~ FALSE — measured 2026-07-13 against `/specs`: 48 of 2,746.** The 126
tests it passes were written by shv2, for shv2, and not one of them ever used a parenthesis. The core
was missing grouping, `true`/`false`, `not`/`and`/`or`, block scoping, void functions, top-level
`typealias`, floats, chars, and bitwise ops — and it turned `a / 0` into a hardware trap.

> ### ✅ **P1.0d IS CLOSED (2026-07-15). The scalar core is done — MEASURED this time: `specs-shv2` 355/355.**
> Every gap the 2026-07-13 sweep found is fixed, floats last (P1.0d.4: an XMM register class, the SSE
> backend, `trunc`, and the conversion band). **`main` is 355/0; the C# bootstrap is 3004/0.**
> ### ✅ **`P1.0r` IS CLOSED (2026-07-15) — shv2 HAS A HEAP. `specs-shv2` 355 → 357/0.**
> `osAllocPages` (the `system` band's FIRST producer) · a VirtualAlloc bump allocator, **always zeroed** ·
> `__mm_alloc`/`__mm_decref`/`__mm_free` as a **builder-built `StdModule` through the ordinary backend** —
> no IR text, no parser · `__mm_alloc_count` + **a leak gate that fires** · and the first heap value:
> `type` · layout · `Self{}` · `static create` · field access. **`Point.create(3, y: 4)` ⇒ 7.**
> ### ✅ **`P1.1a` WAVE 2 IS CLOSED (2026-07-16) — a struct field can be WRITTEN. `specs-shv2` 357 → 371/0.**
> Field WRITES (`p.x = 30`) · instance + field MUTABILITY (both **E2013**, no new code) · field DEFAULTS
> (`export var value = 0`, type inferred from the literal). **NO new IR op** — a field write is
> `storeIndirect`, the exact mirror of the read `emitFieldLoad` already emitted.
> ### ✅ **`P1.1a` WAVE 3 IS CLOSED (2026-07-16) — INSTANCE METHODS + `self`, and E3014. `specs-shv2` 371 → 399/0.**
> Instance methods (`p.add(10)`) · `self` · bare-field access (`count` **and** `self.count` — ONE path,
> both lower to `loadIndirect(__self, offset)`; v1's `selfFieldLoad`/`selfFieldStore` split NOT ported) ·
> field VISIBILITY **E3014** (`type B` reading `type A`'s private field, TYPE-scoped not file-scoped) ·
> and — folded in — **E3011 at a field declaration** (`var n as Nonexistent` was a silent i64; the
> `undeclared` recovery cited an error gate that never ran). **NO new IR op** — a method is an ordinary
> direct call, `__self` at param 0, BORROWED (the caller owns the +1; the consume sink is unreachable —
> a struct-typed field is uninstantiable here). Scaffold (`VarInfo.isSelfField`, `Scope.declareSelfField`)
> got its first consumer, exactly as `Parser.maxon:1996` promised. **⇒ GREP FOR YOUR RUNG'S NAME FIRST —
> it paid a fourth time.** Ports: `type-methods.md` (5 enabled, `method-call-on-call-result` disabled ←
> P1.1b `match` + a primitive-receiver method), `self-keyword.md` (6), `export-var-fields.md` (8).
>
> ### 🔴 THE FINDS (both silent WRONG ANSWERS, both the signature disease, both caught by an INDEPENDENT pass):
> - **The type-vs-file gate was enforced by NOTHING** (coordinator sabotage). `requireFieldAccessible`
>   asserted "the gate is the TYPE, not the FILE ... measured" — but every corpus case reaches the field
>   from `main()` (inside no type), so weakening the predicate to *"inside ANY type"* — a compiler where
>   every method reads every type's private state — left the suite at **392/0**. FOURTH instance of #27's
>   shape (#4c, #23, #27). Pinned now by `field-visibility-is-type-scoped.md` (2 sabotage-red + 1 positive).
> - **`next.a` compiled to `self.a`; `next.readA()` to `self.readA()` — exit 0, no diagnostic** (independent
>   review). A self-field alias has `boundValue: 0`, and **ValueId 0 IS the receiver**; three sites read
>   `binding.boundValue` themselves where `parseVariableReference` guarded it — one fact, four homes. Fixed
>   by DERIVE: `requireStructBase` returns the box WITH the layout (`StructBase`), one reader of `boundValue`.
>   Pinned by `self-field-struct-typed.md` (4). ⇒ **OPEN.md #29.**
> - **The receiver-borrow REASON was false as written** (review): it claimed "scalar fields ⇒ no sink", but
>   `next = other` DOES emit an un-increfed heap store. The CONCLUSION survives — bare `self`-as-value is
>   refused (E2015) and a struct-typed field is uninstantiable, so no live struct pointer ever reaches a
>   field — but for a different reason, now written where load-bearing. ⇒ **OPEN.md #29.**
> ### ⚠ AND THE SCALE CORPUS IS BLIND TO THE MECHANISM JUST LANDED (optimizer) — the instance-method path
> measures **zero** because the corpus emits no methods. Same shape as #4d (closed for structs/heap/globals/
> idiv). Methods are the knob nobody added. ⇒ **OPEN.md #30.**
> ### ✅ **`P1.1b` WAVE A IS CLOSED (2026-07-16) — `match` ON SCALARS. `specs-shv2` 399 → 462/0.**
> `match` STATEMENT (`then` arms) + EXPRESSION (`gives` arms; closed a separate parser gap — `match` was not
> a primary → E2004) · value / `or` / int RANGE (`to` inclusive, `upto` exclusive, `min`/`max` open, negative)
> / float-range patterns · a `default` arm (a non-enum match requires one, E2026; must be last, E2029) ·
> the scalar-match diagnostics E2025/26/27/28/29/42/43 + block-statement (shv2 claims added; the codes
> pre-existed for csharp/selfhosted). **NO new IR op, and NO result slot** — a match is a chain of two-way
> `condBranch` test-blocks, and the `gives` join is a **block-arg phi** (`mintPhi` + `recordBranchEdge`),
> exactly as `if`/`else` carries a var. **Both references join through a slot (v1 via Mem2Reg, the bootstrap
> via a mutable slot); shv2's on-the-fly SSA does not and must not — that is why it has no Mem2Reg pass.**
> This was **P1.1b sliced into two waves** (scalar match first, then enums). **⇒ NEXT = P1.1b WAVE B**
> (payload-free enum + union + enum match + the **OPEN #21 ordinal range arms**), then `P1.2` (`String`).
>
> ### ⚠ THREE THINGS worth carrying (the last two were caught AFTER the implementer, one after the review):
> - **The implementer AGENT DIED to an API error** after reaching green but before committing. Nothing was
>   lost: the coordinator verified the whole state on a fresh build, confirmed the block-arg join by reading
>   the crux, spot-checked a novel input, and resolved the one question the agent died on (repeated
>   `matcharm`/`matchnext` labels are the established convention — `ifcont`/`whilehdr` reuse labels the same
>   way; blocks are keyed by BlockId). **A mid-flight death corrupts nothing when nothing merges unverified.**
> - **The scalar exhaustiveness gate (E2026) was pinned by NOTHING** (review sabotage): the only
>   not-exhaustive corpus case is an *enum* one (disabled), so `if not sawDefault throw E2026` had zero
>   coverage and disabling it kept the suite green. Fixed by an authored `error.match-not-exhaustive`. **The
>   #27 disease, a fifth time** — the review now sabotages every new gate by reflex.
> - **A cross-register-class match EXPRESSION panicked the backend** (`1 gives 5  default gives 7.5` → a float
>   give and an int give into one phi → `X64Backend:603 crosses register files`). The oracle UNIFIES such
>   arms (promote int→float); shv2 HAS `promoteToFloat` + the lattice, but a promoted match's RESULT is a
>   float **value with no nameable type yet** (`typealias F = float(…)` is itself E2015) — the promotion has
>   no consumer, the `mintPhi` trap. **A crash must never ship**, so it is REFUSED now (positioned E2015,
>   pinned by `match-expr-divergent-class.md`), and the promotion is filed for the float rung. ⇒ **OPEN.md #31.**
>   *(Same-class int/bool arms are untouched and match the oracle exactly — E3005 on a bool→int return.)*
>
> ### ✅ **`P1.1b` WAVE B IS CLOSED (2026-07-16) — payload-free ENUM + UNION + enum match + OPEN #21 ranges. `specs-shv2` 462 → 508/0. ⇒ P1.1b IS FULLY CLOSED.**
> `enum`/`union` DECLARATION (auto-increment tags, `= N` resets the counter to N+1, float-backed as i64 bit
> patterns) · `Color.red` ⇒ its tag · **enum `match`** rides Wave A's `condBranch` chain (case-name→tag +
> exhaustiveness E2026) · **⭐ ORDINAL RANGE ARMS (OPEN #21)** · payload-free `union` == enum · diagnostics
> E3030/E3031/E3032/E3034/E3075/E2046. **NO new IR op.** The new artifact is a **parallel-column `EnumLayout`**
> (array index = declaration index, `caseTags` column = the tag) — so the bootstrap's `Ordinal` "one field two
> meanings" hazard that IS OPEN #21 is **unrepresentable**. **The #21 rule** — a range is the OR of covered
> cases *by declaration index*, kept as a two-compare tag range only where no uncovered case's tag falls
> between the covered extremes — is implemented from the bootstrap `645a690cc` (v1's range code is the unfixed
> bug), and **VERIFIED against the oracle**: `Code{ok=500 notFound=200 serverError=404}` `ok to notFound`
> excludes serverError (→42). The review sabotaged the #21 predicate → 2 tests RED (pinned at runtime AND
> codegen). **⇒ P1.1b FULLY CLOSED. Then P1.2 (the crux) began — see its box below.**
>
> ### ✅ **`P1.2` (String — THE CRUX) IS FULLY CLOSED (Waves A–D, 2026-07-16..17) — literals + `print` + `==` (A), primitive INTERPOLATION → the first OWNED heap String + the drop model (B), static single-owner MOVES + use-after-move (C), and `String.append` growth/detach + a String's own drop (D). `specs-shv2` 508 → 523 (A) → 564 (B) → 602 (C) → 611 (D).**
> P1.2 is sliced into 4 waves along the heap-alloc / leak-gate boundary. **Wave A (ownership-free):** string
> literals as **immortal `.rdata`** (a fused 48B record, `capacity=-2`, **NO `__mm_alloc`, no drop** — leak gate
> trivially green), `print` (→ `__print_string`, a `WriteFile` runtime fn — shv2's FIRST stdout, ever), `==`/`!=`
> (→ `__str_eq`). **Wave B (the leak gate's + static-ownership drop model's real debut):** `"pre {x} suf"` lowers
> to ONE `__mm_alloc`'d fused record with its UTF-8 bytes INLINE (`buffer=record+48`, `parent=-3`, `capacity=length`),
> concatenating each part; `__int_to_string` (branch-free → **i64.min-safe**, no negate), `__bool_to_string`,
> `__str_copy`; an UNBOUND `print("{x}")` temp is **STATEMENT-scoped** (drops per loop iteration), a BOUND
> `let s = "{x}"` transfers to the binding (scope-exit drop); ownership keys on a **PROVENANCE bit** — owned-interp
> vs borrowed-rdata literal share `ValueTypeTag.string`, so a per-value `valueOwnsHeap` column rides in lockstep
> with `valueTags`. int-backed enum interpolation rides the integral path for free. **⇒ NEXT = WAVE C** (moves +
> use-after-move), then D (growth/`append`/detach). ⚠ **`__destruct_String` NOT built in B** — every B interp
> result is INLINE, so `__mm_free` reclaims record+bytes together and the destructor is `TrivialDestructor` (0);
> it arrives with the external-buffer consumer in Wave D. **See OPEN.md — the value-class column fix (a backend
> panic on a valid program, coordinator-found) and the O(N²) provenance find (optimizer).**
>
> ### ✅ **THE OWNERSHIP / DROP MODEL WAS HARDENED post-Wave-B (2026-07-16, user: "leaks are not ok"), 564 → 578/0, merged (`deac21315`).** Three MANIFESTING leaks (exit 101), all fixed:
> - **Returning an OWNED String across a call** (OPEN #37): a `returns String` fn now hands back a uniformly OWNED String — the CALLER owns the result (`mintOwnedCallResult`→`trackOwnedTemp`), the callee MOVES an owned return out, a borrowed literal/param return is heap-PROMOTED to owned (`promoteToOwnedString`). String PARAMS are reachable and promote. (This is the caller-side of borrow-vs-consume, PULLED FORWARD from P1.4 because the user won't ship a leak; the full param borrow-vs-consume ruling still stays P1.4.)
> - **Owned bindings in a nested block** (OPEN #38): the Wave B `closeBlock` "fail-safe leak" is retired — owned bindings drop on every edge that leaves a block ALIVE, exactly once (fall-through, `break`/`continue` down to `LoopContext.ownedFloor`; `emitScopeDrops` gained a `floor` param). `while … let s = build(i) … end` and struct-in-block no longer leak.
> - **A returned loop-local binding** — the sibling non-returning iterations leaked (the return move-out permanently stripped `ownedBindings`, which the fall-through drop also reads): found + fixed by the INDEPENDENT review (`deac21315`), the signature "one fact, two readers" bug.
> - **OPEN #39 reassignment drop (`4a2ae34f1`) — FIXED.** Then **WAVE C ✅ CLOSED (`6c06e698c`..`75bd75fac`, 587→597): static single-owner MOVES + use-after-move (E3102, shv2's FIRST ownership program-rejection).** `let u=t`/`s=t` MOVE (source poisoned `VarInfo.movedFrom`, drop-skipped, read = E3102); reassign REVIVES; conditional moves poison conservatively past the merge. Fixes OPEN #41 (`s=t` double-free → move). Independent review caught a paren-`(t)` gate hole that reintroduced the double-free; optimizer removed an O(N²) poison scan.
> - **OPEN #42 (use-after-move at field/method/store sites) ✅ FIXED (`51f543fdd`, 597→602)** — one shared `requireBindingLive` guard at the two funnels; sabotage-proven by the review.
>
> ### ✅ **`P1.2` WAVE D IS CLOSED (2026-07-17, `42292af79`..`ea165a935`, 602 → 611/0) — `String.append` grows to an EXTERNAL buffer + a String's OWN drop. P1.2 IS FULLY CLOSED.**
> `s.append(t)` on an owned root that outgrows its inline capacity **DETACHES**: `__str_append` `__mm_alloc`s a fresh external byte buffer (`destructor 0` — raw bytes), copies the live bytes in, frees the old buffer only if it was itself a root (`parent == DetachedRootParent = -1`), publishes `buffer`/`capacity`/`parent=-1`, then blits `other`'s bytes at the old length. ⚠ **Growth WAS exact (`requiredLen`) and is now GEOMETRIC (`2 * requiredLen`) — corrected 2026-07-28 by P1.8 Slice C's optimizer.** Exact growth left the buffer full the instant it was published, so the NEXT append reallocated and re-copied every byte written so far: `var s = ""` plus a loop of appends was **O(n²)** — measured **×3.94 ×4.07 ×3.82 ×3.88** per doubling and **5.0 seconds to build a 288 KB string**, against **0.98 ms** after (**5,118×**; coordinator-verified end to end at 294,912 bytes in 15 ms of process time). The doubling is branch-free because `requiredLen = selfLen + otherLen >= selfLen`, so no `max(...)` and no compare is needed, and it is safe because `capacity@16` has exactly three readers and none treats it as the allocation's size. ⭐ **The comment above that line had called geometric growth "a later optimization with no consumer here" — the consumer was the commonest string idiom in the language, and the scale corpus never appends in a loop, so nothing had ever looked.** A String now carries its OWN drop: `emitDecref` is **tag-routed** — `ValueTypeTag.string` → `__str_decref` (the refcount RMW, then `__mm_free` of the external buffer when `parent == -1`), everything else → `__mm_decref`. `E3019` (existing code, shv2 claim added) rejects `.append` on a `let` binding (immutable). **`byteLength()` reads `length@8`.** ⚠ **DEVIATIONS, both ACCEPTED (thesis-aligned):** (1) **`__str_decref` is dispatched STATICALLY** by the tag-router, NOT via a dynamic `funcAddr`/`callIndirect` destructor stored in the record's MM header. The dynamic per-record `__destruct_String` cascade (`funcAddr`/`callIndirect`, deferred here per PLAN.md:291) is **forced only when a struct/array FIELD can hold a String** — a box's own `__mm_free` must then reach the field's destructor without the parser knowing the static tag — which is exactly **P1.4** (struct-with-String-field). Until then the tag is known statically at every drop site, so static dispatch is correct AND cheaper. (2) A **`var` String is EAGERLY PROMOTED** to an owned heap copy at binding (a `var` bound to a borrowed rdata literal is read-only `.rdata` — `append` would write through it), via `promoteToOwnedString`. The review deduped a real find: the refcount-RMW protocol was copied into both `__mm_decref` and `__str_decref` (`emitRefcountCheckToLastOwner`, the LOCK-prefix P1.5 note moved onto the shared helper). ⚠ **OPEN #43 (self-append `s.append(s)` UAF — the grow path frees the old buffer before the blit reads the aliased source; MASKED under bump alloc)** filed as its own follow-up.
>
> ### ⇒ **✅ P1.2 owned-value follow-up CLOSED — #40 fixed, P1.2 complete (2026-07-23, main `7057bf8ad`+`b9f13dcac`).** #40 was a REACHABLE UAF in the interaction of P1.2's reassignment depth-model with P1.4a's borrowed managed-aggregate PARAMS (`var q = <borrowed struct/union param>`; `q` conditionally reassigned to owned in a nested block → the owned value dropped at the inner block while `q` lived on). Both naive fixes were proven wrong before the ruling — "drop at declaration scope" ships an else-path double-free, copy-promotion (String's fix) diverges from the oracle by breaking struct aliasing — so the correct fix carried a thesis ruling (user chose **approach A, retain-promotion**): `var q = <borrowed aggregate param>` now retains (`__mm_retain`, incref) the param box so `q` co-owns it from declaration, owned on every path, aliasing preserved, refcount balancing — a BOUNDED refcount exception like COW #105. **See the "#40 → conditional ownership" entry in Future rungs for the full closed record.** Poison-on-free (#108) is the standing detector. **✅ OPEN #43 (`s.append(s)` self-append UAF) — CLOSED 2026-07-22** (main `0be8c7465`, with #105): `__str_append` released the old buffer BEFORE the blit read the aliased source; the release now rides a block arg past both copies. **Proven a REAL read-after-release** with a poisoning `__mm_free` (release-ahead-of-copy → `abcabc??????…`; fixed → `abcabc`), not merely inferred — see [[#108]].
>
> ### ✅ **`P1.3` SLICE 1 IS CLOSED (2026-07-17, `84db86992`..`e79e3d233`, 611 → 617/0) — SCALAR-payload unions/enums are HEAP-BOXED. P1.3 is SLICED: scalar first (this), managed next.**
> A payload-bearing union/enum is now a **heap box**: `8 + maxArity*8`, i64 tag@0, scalar payload slot `i`@`8+i*8`, unused slots zero-filled — structurally a struct-box (P1.1a) + a tag, so its drop reuses the **trivial destructor** (a scalar payload owns nothing). Construct (`emitEnumBox`), match (tag@0 + the P1.1b cmp/condBr ladder), scalar extract, discard `_`, multi-field scalar cases, payload-free-case-in-a-boxed-union. **No new IR op.** Plus **E3066** rejecting union `==`/`!=`/`<`/`>`/`<=`/`>=` (reference equality — they compared box ADDRESSES; the review caught `<` etc. silently miscompiling where the implementer had only guarded `==`/`!=`). The box is enrolled as an owned heap temp (`trackOwnedTemp`), so scope-drop / move-on-bind / `var`-reassign came free from the String machinery. A payload-FREE union stays the bare-i64-tag P1.1b form. Review also de-duped the `__mm_alloc` ABI (written **4×**) into `emitBoxAlloc`.
> ⚠ **THE PLANNED CORPUS UNLOCK WAS WRONG — Slice 1 unlocked only 1 positive + 3 error cases**, not the `union-cases.md`/`match-enum-or-pattern.md` the survey listed. The implementer PROBED and found those gated on **COMPANION features the corpus bundles with unions** — a synthesized `.unionCases` companion enum + `.name`/`.rawValue`/`.ordinal` accessors, `for-in`/`.allCases`, and **union-as-PARAMETER (E2015 → P1.4)** — NOT owned payloads. ⇒ **Owned-payload support in ISOLATION unlocks little corpus; the bulk needs a companion/accessor rung + P1.4.** ([[feedback-survey-verdicts-need-probes]]: an "unlockable" verdict from a READ is not one from a RUN.)
> ⚠ **A boxed-union RETURN type would have LEAKED (exit 101) — now REJECTED (E2015), the interim.** A boxed-union call-result is `named`-tagged (indistinguishable from a bare enum), so `valueIsOwned` misses it and the caller never drops the box. Rather than ship a reachable leak ("leaks are not ok"), a payload-bearing-union return type is refused at the return-type annotation, **symmetric with the union-PARAM E2015** already deferred. **⇒ P1.4 lifts param+return together with the full cross-call boxed-union ownership** (caller adopts the returned box via a `named`+`isBoxed` layout lookup) — **OPEN #44**.
> **⇒ NEXT for P1.3 = SLICE 2 (MANAGED payloads — String/struct):** construct = **move the String in**, match-bind = **move-out**, and a tag-conditional **STATIC-cascade** drop calling each managed field's own `__str_decref` (static because the union type is known at every drop site — the same ruling as Wave D). Targets: the harness's `compilerError(text String)`/`fail(reason String)` and OPEN.md's staged `rc-enum-struct-payload-freed`.
>
> ### ✅ **`P1.3` SLICE 2 IS CLOSED (2026-07-18, `0006306a2`..`31f773eaa`, 617 → 641/0) — MANAGED (String + struct) payloads: move-in / move-out / tag-conditional static-cascade drop. P1.3's OWNED-LOCAL payload story is DONE (cross-call stays P1.4).**
> A union case field may now be a **String or struct** (a heap pointer in the payload slot). **Construct = MOVE** (`moveInPayload`: no incref; source poisoned E3102; a borrowed literal promoted to owned first). **Match-bind = MOVE-OUT** (`moveOutManagedPayload`: binding owns, the box slot is NULLED; the scrutinee becomes `partiallyMoved` = E3102-on-read but STILL dropped). **Drop = tag-conditional STATIC cascade** (`__destruct_<U>`, synthesized in `MmRuntime.installUnionDestructors`: per live case drop each still-present managed field via its own destructor — String→`__str_decref`, struct→`__mm_decref`, null-guarded — then `__mm_free`). ONE classifier (`SignatureIndex.classifyUnionPayload`) drives construct/extract/drop/synthesis (moved-in set == dropped set). The box drops uniformly at scope exit via the null-guarded cascade (a design deviation from per-arm consume — identical observable semantics, simpler). Optimizer killed an O(unions×drops) destructor-install scan (a `Set`). **`rc-enum-struct-payload-freed` STAYS disabled** — it matches a BORROWED param (P1.4 borrow-consume), cleanly rejected (E2015); 14 authored owned-local specs (`union-managed-payload.md`) cover the mechanism instead.
> ### ⚠ **SLICE 2's review found TWO REACHABLE match-flow bugs, both PRE-EXISTING (Slice 1 / P1.2, shipped on main) — FIXED as a corrective rung (`fa685a7a1`), NOT deferred ("leaks are not ok", now codified):** (1) a **temporary union scrutinee** (`match U.case(x) {…}`) leaked its box (exit **101**) — the temp was never enrolled as a match-scoped owned binding; now enrolled + dropped on all four exits (return/break/continue/fall-through). (2) an owned **`gives` value** double-freed / UAF'd (**0xC0000005**) — it fed a non-owned result phi; now an owned give TRANSFERS to the phi (`transferGiveToPhi`/`promoteBorrowedGive`, phi marked owned via `trackOwnedTemp`). Independent review sabotage-proved every mechanism + probed 10 edge cases clean. **RESIDUALS → their own rungs:** give-type-consistency is unchecked (**OPEN #45** — a `String`+`int` give-mix segfaults, pre-existing + unreachable by well-typed code; `finalizeMatchMerge` checks register-class, not type identity); a bootstrap fixpoint non-convergence forced a documented 2-line `recordBranchEdge` bypass, byte-identical IR (**OPEN #46**).
> ### ⇒ **P1.3 is functionally COMPLETE for owned-local payload unions (scalar + managed). What remains is cross-call (union param + return = P1.4, OPEN #44) and the companion/accessor rung that unlocks the bulk of the payload-union corpus.**
>
> ### ✅ **`P1.4` STARTED — SLICED into P1.4a (cross-call OWNERSHIP) + P1.4b (ERRORS), which the survey confirmed separate CLEANLY (~0 entanglement). USER RULING 2026-07-18: cross-call params are BORROW-by-default, consume-by-use.**
> A param is a **BORROW** (caller keeps ownership, value usable after the call, callee never drops it); a **CONSUME** (move) only where the callee moves it into durable storage (a struct field); a return **ADOPTS** (owned move-out, caller drops once). Chosen because both reference compilers borrow-by-default, the harness overwhelmingly borrows (the same `test SpecTest` handed to four helpers in a row), and String already works this way; consume-on-pass would reject the pervasive `f(s); use(s)`.
> ### ✅ **`P1.4a` WAVE 1 IS CLOSED (2026-07-18, `354995f3f`..`f087e3152`, 641 → 647/0) — BORROW struct/union PARAMS + ADOPT struct/union RETURNS. OPEN #44 CLOSED.**
> Purely PARSE-TIME: `resolveNamedStruct` re-tags a `named` denoting a declared `type` → `structRef` at the param-base / return / call-result sites (the registry is complete in the real parse). ⭐ **Ownership unified onto the `valueOwnsHeap` provenance BIT** — `valueIsOwned(value,tag)` collapsed to `valueIsOwnedHeap(value)`, the `tagIsStructRef` proxy DELETED, because a borrowed struct param and a fresh owned struct box **both** carry the `structRef` tag; only the bit (set by `trackOwnedTemp` on every owned producer, absent on a borrow) distinguishes them. A borrow is never dropped by the callee; the caller drops its owned value once. `mintOwnedCallResult` now adopts a struct/boxed-union return (a boxed union via a `named`+`isBoxed` LAYOUT lookup, `valueIsBoxedUnion` — a bare enum owns no box); the Slice-1 `rejectBoxedUnionReturnType` is removed. Review sabotage-proved the bit BOTH directions (remove it from a producer → leak 101; add it to a param → double-free 101). **⭐ CLOSED A PRE-EXISTING LATENT UAF:** constructing a struct with a MANAGED field (`Self{name: name}` from a String param) stored a dangling borrow (bump-masked) — now a clean **E2015** (`rejectManagedStructFields`, whole-layout scan so every construction path is caught). **⇒ NEXT = P1.4a WAVE 2 (CONSUME-into-field):** struct-with-managed-field + its destructor cascade + the per-param ownership inference (`.create(inner Inner)` → `Self{inner: inner}`, moving a payload out of a borrowed union param). ⚠ **OPEN #47** filed: rejecting a String field DEFAULT leaks 4 compiler allocations (exit 101) on the error-unwind path — pre-existing, unrelated to borrow/adopt, its own follow-up.
>
> ### ✅ **`P1.4a` WAVE 2 IS CLOSED (2026-07-18, `d9fa9f62c`..`86e704a59`, 647 → 670/0) — CONSUME-into-field + a PATH-SENSITIVE move model. The pre-existing conditional-move leak is FIXED at the root.**
> **The consume half:** a struct may hold a MANAGED field (String/struct); a param moved into a field is CONSUMED (the caller's arg is moved-from, E3102). A struct destructor cascade `__destruct_<Struct>` (generalizing Slice 2's union cascade via one `managedFieldDropCallee` router — which also FIXED a latent leak the union cascade had: a nested managed struct was dropped via `__mm_decref` not its destructor) frees the managed fields, statically dispatched. The param-consume analysis rides the token SWEEP (`ProgramSignatures.funcParamConsumes`, keyed on the declaration hash for cross-file re-parse); scope = the DIRECT sink (`.create(f X)→Self{f:f}`), the transitive/recursive fixpoint cleanly REJECTED (E2015). A boxed-union struct FIELD stays E2015 (a later rung).
> **⭐ THE PATH-SENSITIVE MOVE MODEL (the review's blocker → USER RULING: "the real fix, no runtime flags").** Wave 2's review found a REACHABLE conditional-move leak (exit 101), **PRE-EXISTING in the core move model** — `VarInfo.movedFrom` was MONOTONIC, so a value moved on one `if`/`match` branch was drop-SKIPPED on EVERY path, leaking on the not-moved path (verified: it already leaked on main with union payloads + owned locals; Wave 2's struct-field consume was just one more trigger). **FIXED** via compile-time drop elaboration: `captureMoveMark`/`restoreMoveMark` snapshot the move bit around a branch, `reconcileMovesAtMerge` settles each owned binding at every JOIN — moved on some edges + live on others ⇒ **drop it on the LIVE edges** (NO runtime flags). Wired into `finalizeIfNoElse`/`finalizeIfElse`/`finalizeMatchMerge`. Conditional moves are now ACCEPTED and correct (dropped exactly once per path); reads past a maybe-moved join stay conservative E3102. Review sabotage-proved all three guards + probed every control-flow shape (triple-nested-if 8 combos, match arms, loops). **⚠ ONE CORRECTNESS BOUNDARY:** moving an OWNED binding declared OUTSIDE a loop from inside the loop body is REJECTED (E2015) — the back edge would re-move it into a double-free; accepting it needs a loop-exit reconcile (a follow-up). A loop-body-LOCAL move works.
> ⭐⭐ **This is the SECOND consecutive rung whose review found a PRE-EXISTING move-model leak** (Slice 2 was the first) — the move model's path-insensitivity kept surfacing as new constructs reached it; now closed at the root. Rebased over the parallel repo's **arm64-macOS backend + `/`/`mod` div-by-zero-throwing** landing (bootstrap now 133 codes). **⇒ REMAINING P1.4 = P1.4b (ERRORS).** Follow-ups: the loop-exit-reconcile (accept an outer-move that breaks a loop), **OPEN #48** (the `.spec-tmp` staging race).
>
> ### ✅ **`P1.4b` WAVE 1 IS CLOSED (2026-07-18, merged `9b9455698`, 670 → 719/0) — the SCALAR-error CORE. `throws`/`try`/`otherwise`/`throw`/`panic` end-to-end, on v1's dual-register `(value, errorFlag)` ABI VERBATIM.**
> A `throws` fn returns TWO registers: value in **R8**, an i64 error flag in **R10** (arm64 x0/x9) — `0`=no error, `ordinal+1`=a scalar enum case. New IR ops: `tryCall`/`throwError`/`propagateError` (Maxon), `tryCall`/`errorReturn` (Std, `role: OpRole.errorReturn` — the placeholder got its producer). The parser desugars `try` → `tryCall` + `compare` + `condBranch`; `(e)` binds `flag - 1` typed from the callee's static `throws` clause (so **NO `errorIsHeapPtr` runtime bit** — statically decidable). Error-edge drops ride P1.4a's path-sensitive reconciliation (trivial for scalars). **⭐ THE HIGHEST-RISK PIECE — the two-result capture** (`result←R8, flag←R10`): a naive `mov;mov` miscompiles when the allocator colors the pair cyclically (v1's array-sort bug), so it routes through `SsaDestruction`'s parallel-copy resolver (breaks a swap with `xchg`, no scratch — cleaner than v1). R10 chosen as a caller-saved non-arg register, mirroring R8's own "no return/arg conflict". Coordinator verified it under register pressure (both a success result and a thrown flag live across two `tryCall`s → correct). **Reject diagnostics: E3054/E3055/E3057/E3058/E3059** (shv2 claims added to the shared registry — numbers already existed; E2049 block-form-in-arm reused). **48 of 49 acceptance cases green** (the 49th, `try-pure-let-discard`/E3064, DEFERRED — shv2 has no purity classification; every E3064/E3065 discard case is disabled for the same reason). Ported `error-handling.md` (29 scalar) + `try-otherwise-value-flow.md` (5) byte-identical + flipped 13 staged markers.
> **Pre-existing bugs FIXED (blocked error enums):** enum `implements Error` was parsed as TWO enum cases (`implements`, `Error`) → `skipOptionalImplementsClause`; `as` casts were unimplemented (E2015/E2045) → inert numeric-cast pass-through added (range-check is OPEN #51).
> **⭐⭐ THE SIGNATURE BUG, FOUND BY REVIEW AGAIN:** the error-flag `+1` bias was written TWICE across a tier boundary (encode in `lowerThrowError`, decode in `parseOtherwiseBlock`) — a change to one would silently bind `match e` to the wrong case. DERIVED from ONE `ErrorFlagOrdinalBias` constant (byte-identical codegen). Optimizer confirmed nothing superlinear (linear +35…+159 allocs, a stale-baseline reading corrected).
> **⭐⭐ TWO reachable crashes on VALID constructs, found by adversarial PROBING (suite green over both):** (1) a **throwing fn returning a MANAGED type** (`returns String throws E`) accept-and-CRASHED (segfault) — its tryCall result is NULL on the error edge, genuinely Wave-2 — now a clean **E2015 reject** (`no accept-and-crash`, consistent with how the rung rejects every other unsupported error construct) rather than shipped; **OPEN #49** (the feature = Wave 2). (2) a throwing fn returning a **float** hits a PRE-EXISTING general float-return codegen gap (`StdToX64Conversion.maxon:1783` "float returns not supported until M3" — regular float-returns panic too) — filed **OPEN #50**, out of P1.4b's scope.
> ⚠ **Rebased over the parallel repo's float-`/`-language-level-throwing bootstrap landing** (`8c79208ec`, `stdlib/Builtins.maxon` now `try`s a division) — the STALE-BIN trap: rebuilt the bootstrap from the new main before shv2 would compile the stdlib. **⇒ REMAINING P1.4b = WAVE 2 (managed error VALUES):** `throw`ing a payload union (String/struct), the `(e)` binding OWNING + dropping the caught box, owned managed LOCALS dropped on the error edge — **the HARNESS slice** (`CompileError`/`SubprocessError` unions), and the removal of the OPEN #49 managed-return gate. [[project-p14b-errors-plan]]
>
> ### ✅ **`P1.4b` WAVE 2a IS CLOSED (2026-07-18, merged `8ca5225f2`, 719 → 726/0) — managed error VALUES (boxed-union throw/catch/drop). MOSTLY REUSE.**
> The flag register (R10) now carries the box **POINTER verbatim** for a boxed-union error (vs `ordinal+1` for a scalar enum) — distinguished STATICALLY via the derive-once helper `throwsTypeIsBoxedUnion` (Parser:4767, read at the throw-encode + both catch-decode sites; both it and `valueIsBoxedUnion` delegate to `EnumLayout.isBoxed()`, the ONE decider — cannot drift). The op carries a pre-classified **`errorIsBoxed bool`** (replaced a DEAD write-only `errorType` field on `throwError` — lowering lacks the enum registry, so classify once in the parser and ride the bit). `(e)` binds the box verbatim + is enrolled OWNED → drops via `__destruct_<U>` at handler `end`. **Almost entirely REUSE** — P1.3's box construct (`emitEnumBox`/`moveInPayload`) + `__destruct_<U>` cascade, Wave 1's ABI/capture, P1.4a's drops — plus 3 small branches. **NO new IR ops** (only the field change), NO MmRuntime edit.
> **⭐⭐ THE THROUGH-LINE, a THIRD consecutive time: independent review found a LEAK the implementer missed** — the `return` and `throw` hand-off exits agreed on the owned move-out (`moveOutForExit`) but **DIVERGED on the rest of the cleanup**: `throw` did NOT drain leftover borrowed temps the way `parseReturnStatement` does, so `throw E.problem(len("val={x}"))` leaked the interpolation temp (101) while the byte-identical `return` was clean. Fixed by factoring the WHOLE hand-off sequence (move-out + drain temps + scope drops + restore owned list) into ONE `emitHandoffExitDrops` both call — the signature-bug class (two exits agreeing on part, diverging on the rest) AGAIN. Optimizer confirmed nothing superlinear (true cost 0 — corpus has no error handling) and the change REDUCES duplication.
> **The implementer also self-caught a leak by probing** (the value/ignore/single-stmt `otherwise` forms weren't dropping the caught box → 101; fixed to enroll uniformly) and **gated a latent double-free** (`throw <borrowed union param>` → E2015, the throw twin of the borrowed-aggregate-return refusal) and **authored a String-payload coverage spec** (nested-managed-payload drop via `__destruct_<U>` — the portable corpus payloads are all SCALAR-in-box, so this is the only proof the payload itself drops). Every managed-error path probed leak-free (nested String drop, `throw e` re-throw transfer, all 5 `otherwise` forms, break/continue-in-loop). **OPEN #52** (cross-file union-payload interner PANIC, declared-after-use), **OPEN #53** (labeled union-construction args unparsed, blocks a byte-identical port). **⇒ REMAINING P1.4b = Wave 2b (managed RETURN — removes the OPEN #49 gate) + Wave 2c (union self-FIELDS + reload) + cross-file `throws`-prescan.** [[project-p14b-wave2a-closed]]
>
> ### ✅ **`P1.4b` WAVE 2b IS CLOSED (2026-07-18, merged `d16a45f0f`, 736/0 on the wasm-rebased main) — managed RETURN. The one UAF-risky piece of P1.4b; OPEN #49 gate REMOVED.**
> A `try f()` where `f` returns a MANAGED type: the tryCall `result` (R8) is owned on the ok edge, **NULL on the error edge** (a throw zeroes it); `mintOwnedCallResult` tracked it in the ENTRY block (flows to both), so an error-edge drain decref'd NULL → segfault (the exit-139 OPEN #49 gated). FIX = **re-scope the managed result to the `tryok` block ONLY** — `removeFromPendingTemps(result)` in `desugarTry` before the handler parses, then re-enrol owned in `tryok` per the three finish paths (`finishTerminatedTry` re-push; `finishValueTry` owned phi + promote a borrowed-literal fallback; `finishFellThroughTry` decref in okBlock). Deleted the E2015 gate + `returnTypeOwnsHeapBox`/`payloadClassOwnsHeapBox` + the reject spec. Strengthened `parseUnboundOtherwiseForm`: a managed result with a **tag-mismatched or borrowed** `otherwise` fallback is refused E2015 (else a wild free at the phi's single drop). NO new IR ops. I verified the segfault surface myself (throw-taken + managed result discarded → exit 7, no crash).
> **⭐⭐ THE SIGNATURE BUG, review found it a 4TH consecutive P1.4 time:** a **fell-through** `otherwise` block in a VALUE position leaked (managed, exit 101) — the "a valueless handler can't be assigned" rule was enforced at ONE of two spellings (`otherwise ignore` guarded, block-fall-through NOT). Consolidated into ONE E3059 check at `finishFellThroughTry` (the point both collapse to `fellThrough`), removing the duplication.
> **⭐⭐ A SYSTEMIC PRE-EXISTING type-soundness hole, CONFIRMED (review + me): shv2 does NOT name-check aggregate type IDENTITY.** Every struct shares the `structRef` tag / every union `named`, and the checks are tag-level only — so `return BoxB` from a `returns BoxA` fn (or a mismatched `otherwise` fallback) COMPILES and drops with the WRONG destructor = a wild free / **segfault (exit 139), reachable with NO error handling at all** (since P1.1 structs). NOT a Wave-2b regression — filed **OPEN #54** as a dedicated corrective rung. **USER (2026-07-18): v1 fixed it — PORT v1's type-identity check, don't re-derive.** ✅ **OPEN #54 Slice A LANDED 2026-07-18** (`16c729b07`..`406987b5d`, 736→741): ported v1's exact-interned-name check into ONE shared `aggregateNameOf`/`namedAggregatesConflict` pair, applied additively at return / reassign / `otherwise`-value; both confirmed 139s now compile-reject (E3005/E3059). Review: **the signature bug is ABSENT** (first clean P1.4-era review). ✅ **Slice B LANDED 2026-07-18** (`aa2ac2509`..`2a50ea55c`, 741→743): published `valueNameIds` on `IrFunction` + a `checkArgTypes` name branch → the struct consuming-call-arg wild-free (139) now rejects E3005 for ALL call shapes; `namedAggregatesConflict` moved to a shared `TypeRules.maxon` home; 2nd consecutive clean review. **Slice B2** (UNION call-arg — param name erased before SemanticCheck, needs a pre-erasure carrier, #52-adjacent) + **Slice C** (value-position `match … gives` merge = OPEN #45) remain.
> ⚠ **Rebased over the parallel repo's `wasm32-wasi` SCALAR backend landing** (+ a later MCP `--target` + wasm-docs landing) — resolved the `optimization-log.md` two-table conflict by keeping their re-baselined version (my pre-wasm rows were stale) + re-recorded a post-wasm Wave 2b scale row. **FIXED a pre-existing Windows-UAC bug their spec runner introduced** (`SpecTestRunner` named the temp exe after `test.name`, so a test `...its-update` tripped Windows installer-detection → "requires elevation" spawn fail; now a keyword-free `hostprog` stem). **⇒ REMAINING P1.4b = Wave 2c (union self-FIELDS + reload); + OPEN #54 Slice B2 (UNION call-arg).** ✅ **The cross-file `throws`-prescan (OPEN #52 backward-ordering slice) is VERIFIED CLOSED 2026-07-19** — the "declaration-order union-layout prescan" already exists (`queryProgramSignatures`→`foldFile` seeds every file's union layouts + `throws` clauses before any body parses), and every `classifyUnionPayload` site adopts the payload id into the interner it resolves against, so the `:582` panic is structurally unreachable; a spec-only rung (767→771) enabled `error.cross-file-throws-caught-later-file` + pinned the previously-uncovered backward managed-payload shapes. Slice C = OPEN #45 (closed). [[project-p14b-wave2b-closed]]
>
> ### ✅ **`P1.4b` WAVE 2c IS CLOSED (2026-07-20, merged `807180533`, 782 → 788/0) — boxed-union struct FIELDS. The LAST P1.4b mechanism; only OPEN #54 Slice B2 now remains for all of P1.4.**
> A `type` field may be a payload-bearing (boxed) union (`var pending as OuterErr`). Reclassified `PayloadClass.nestedUnion` unmanaged→**MANAGED but kept DISTINCT from `managed`**, carrying the field's own `named(<union>)` type — so the existing String/struct-field machinery (construct-MOVE, reassign-drops-old, `__destruct_<T>` cascade via `structFieldIsManaged`) handles it, while the union-PAYLOAD path (`requirePayloadLowerable`, store width from `payloadStorageOf` which can't carry `named`) still rejects a nested payload cleanly. The struct-FIELD path derives its width from `fieldStorageType` instead, so it lowers fine. `throw <boxed-union field>` is a MOVE (shv2 has no `__mm_incref`): null the field slot (recorded by `emitFieldLoad` in 4 scalar `lastFieldRead*` fields, matched by SSA value identity) so the container's cascade skips it. Deleted the blanket `rejectUnsupportedManagedStructField`. Enabled construct/reassign/scope-drop/owned-local-throw + a nested-String-payload cascade; the union-payload reject stays E2015 (P1.7).
> **⭐⭐ THE INDEPENDENT REVIEW FOUND A REACHABLE SILENT SEGFAULT (139) the x64 suite was green over** — `throw self.field` of a boxed-union field through a BORROWED receiver, caller CATCHES then re-reads the moved-out field: `moveOutThrownField` nulls the field through the borrow (the CALLER's box), sound if the caller tears the container down (propagation) but a use-after-move on re-read, and undetectable without cross-call move tracking. It was INCONSISTENT with the twin `moveOutManagedPayload`, which already REFUSES a borrowed scrutinee. **FIX (the disciplined clean-reject, matching P1.3 Slice 1's boxed-union-return reject): gate the field move-out on an OWNED-LOCAL container** (`valueIsOwnedHeap(lastFieldReadBox)` — an owned local dies with its frame, so no surviving caller can read the nulled slot). A borrowed `self`-field falls to a clean E2015, split from the borrowed-param reject so the param keeps its exact message (zero golden churn). The rung's real deliverable (owned-local construct/read/reassign/drop/throw-out) is untouched. Verified the exact repro now rejects (exit 1, was 139); owned-local still exit 20; suite 788/0 workers-invariant; scale delta 0; arm64 owned-local cross-compiles clean.
> **⏭ TWO P1.5 follow-ups FILED (#64, #65) — BOTH ✅ CLOSED 2026-07-23:** the BORROWED-container field throw (#64) was re-enabled not by escape analysis but by RETAIN (`eea72ec74`, applying #40's retain-promotion to the throw error channel — no re-reading-vs-tearing-down disambiguation needed); implicit-self method resolution (#65, bare `bump()`, `55cafda11`). `propagate-throw-through-local-struct` needed BOTH and is now ENABLED. [[project-p14b-wave2c-closed]]
>
> ### ✅✅ **`P1.4` IS COMPLETE (2026-07-20, `8b2bec781`, 792/0) — the whole moves+borrows+errors rung is DONE.** OPEN #54 Slice B2 (UNION call-arg type-identity) was the last piece.
> A `union`/`enum` PARAMETER is erased to bare `integer` by `resolveTypes` (`TypeResolution.resolveNamedType:192`, the OPPOSITE of a struct's surviving `structRef(id)`), so `SemanticCheck.checkArgTypes` could not name-check a mismatched union arg — dropped under the DECLARED param's `__destruct` = wild free (139 consumed / latent borrowed); the oracle rejects E3005. FIX = a pre-erasure per-param NAME carrier (`FuncSignature.paramAggregateNames`, filled at parse where the param is still `named`, threaded through `syncResolvedSignature`, read in `checkArgTypes` via the generalized `aggregateNameFor` + shared `namedAggregatesConflict` → E3005). NAME-based (ByteArray, no ids) ⇒ structurally immune to the #52 cross-file interner landmine — the independent review probed that 5 ways (id-shift, declared-after-use) all oracle-matched, found NO over-rejection, and judged the `aggregateNameFor`/`aggregateNameOf` twins a FORCED tier-boundary copy (comparison single-homed). Scope cut: `maxonOpIdxToString` (#51) is NOT in shv2 (a v1 printer). **⇒ NEXT MAJOR = P1.5 (closures + async + escape analysis)** — co-lands async with closures (see the async note); also re-enables OPEN #64 (borrowed-container field throw) + #65 (implicit-self). [[project-open54-slice-b2-closed]]
>
> ### ✅ **`P1.5` STARTED — sliced 5 ways** (A1 fn-values → A2 closures+escape → B1 GT-core → B2 await/Promise → C subprocess+harness). USER RULING 2026-07-20: start A1, take P1.5 through the parallel-harness gate; scheduler/stack-model rulings deferred to B1/B2. **⭐ P1.5 is the biggest rung: closures + async + escape analysis + the GT scheduler runtime (R3); shv2 had ZERO async/closure/GT code (only lexed `async`/`await`).** [[project_p15_decomposition]]
> ### ✅ **`P1.5-A2` SUB-SLICED → A2a then A2b** (the closure corpus has sub-deps: some cases need Array/P1.7, interfaces/P1.7a).
> ### ✅ **`P1.5-A2a` IS CLOSED (2026-07-20, merged, 813 → 821/0 x64 + 800 → 808/0 wasm) — NON-CAPTURING closure literals. FRONT-END ONLY, no new IR op.**
> `function(params) gives <expr>` parses as a primary; a closure that captures NOTHING lifts to a top-level `<enclosing>$closure_<k>` (per-outer-fn counter, source-deterministic) and yields a plain `functionRef` — reusing A1's whole machinery. Body parses into a swapped-in fn context (own scope/value-numbering/blocks/columns/drop-sets), restored after; return type = body type, filed in a file-local `closureReturns` map (a lifted closure never reaches the whole-program token sweep). **Capture DETECTION = the A2b seam**: an identifier resolving into `closureCaptureScopes` (a STACK of enclosing scopes, so a closure-in-closure detects any enclosing frame) → clean E2015 (A2b replaces the refusal with real env handling). ValueOrigin fix: functions pushed up-front + `currentFuncIndex` records each landing index (codegen-neutral for closure-free programs). ⭐ **Review found the recurring signature bug**: only 1 of 4 `lastFieldRead*` fields was saved/restored across the closure-body context swap (one logical record) → a reachable miscompile; fixed to save/restore all four. Enabled 6 `first-class-functions.md` + `closure-capture.md`'s no-capture-regression/string-literal-body (targets x64-windows+wasm32-wasi). ⏭ **`non-capturing-closure-through-ternary` STAYS disabled — OPEN #71: shv2 doesn't parse the inline conditional `a if c else b` (E2010); a prerequisite for ALL A2b ternary cases, its own front-end rung.** Plus OPEN #72 (bootstrap aliased-self-field-store double-free). **⇒ NEXT = A2b (capturing closures + env block + parse-time E3099 escape rejection).**
> ### ✅ **`P1.5-A2b` SUB-SLICED → A2b-1 (done) + A2b-2 (next).** Two implementer stop-and-report forks: (1) capture must be **BY-VALUE** — shv2 has no addressable stack slots (`stackAddr`/mem2reg absent), so v1/bootstrap's by-reference env is impossible; the env stores the captured VALUE (one load), correct for the corpus (all capture immutable `let`s), a documented divergence from the spec doc's "by reference"; (2) pass-down forces the uniform `(args,env)` ABI which churns A1's goldens → split off as A2b-2.
> ### ✅ **`P1.5-A2b-1` IS CLOSED (2026-07-20, merged, 821→833/0 x64 + 808→821/0 wasm) — CAPTURING closures (int, by-value) + env block + parse-time E3099 escape rejection. IN-FRAME calls; ZERO A1 golden churn.**
> Closure value = (`functionRef` fn ptr) + (env ptr on the per-fn `valueClosureEnv`/`valueIsCapturingClosure` columns, swapped like `valueTags` — **replaces v1's `fnEnvVarNames` sidetable**). **NO new IR op**: env = `emitBoxAlloc(n*8)` (trivial dtor, owns nothing) + `storeIndirect` by value; read = one `loadIndirect`; the call appends env via existing `indirectCall`. The lifted `<enclosing>$closure_<k>` gets a trailing `__env` param **only when it captures** (non-capturing stays A2a-identical → zero churn — the call site reads `valueClosureEnv` to pick env-carrying vs A2a's env-less callIndirect). Env dropped once at creation-scope exit via `ownedBindings.push` (scope-scoped, NOT `trackOwnedTemp` which frees before first call). E3099 parse-time, value-keyed on the stable `boundValue` (no name-bridge).
> ### ⭐⭐ **THE INDEPENDENT REVIEW FOUND FOUR REACHABLE MEMORY-SAFETY HOLES (all fixed `96daddca6`, each spec-pinned):** a capturing closure reaching a sink that doesn't carry its env — **struct construction** (panic), **call-arg pass-down** (SEGFAULT 139, env not threaded → E2015 until A2b-2), **match `gives` arm** (SEGFAULT → E3099), **`try…otherwise <closure>`** (SEGFAULT 139 → E3099). F3/F4 = reachable twins of the disabled ternary tests. Env-drop/UAF verified clean x64+wasm; over-rejection none; E3099 sabotage-proved load-bearing. **⭐ Design note → OPEN #73: the escape check is PER-SITE (enumerated) — a DERIVED invariant ("a capturing-closure value may appear only as an indirect-call callee or a frame-local binding") would be structurally robust (its own hardening rung).** Deferrals → OPEN #74 (payload-binding E2013 divergence), #75 (name-not-leaked needs struct-copy-return), #76 (bound-outside-loop x64 E5001 pressure, wasm-only). **⇒ NEXT = A2b-2 (uniform (args,env) ABI + pass-down + managed capture; the A1 golden churn lands here).**
> ### ✅ **`P1.5-A2b-2` IS CLOSED (2026-07-20, merged, 833→842/0 x64 + 821→829/0 wasm) — the uniform `(args, env)` ABI + closure PASS-DOWN + managed (struct/String) capture + the interprocedural closure-escape summary. ⇒ P1.5-A2 (closures + escape) IS DONE.**
> A function value is now **(fn ptr, env ptr)** uniformly, so a capturing closure passes DOWN to a generic `apply(f,x)`. **Plain function as a value** lowers through a `__fnref_<name>` THUNK (`Runtime/FnRefThunk.maxon`) with `(args, __env)` forwarding to the target, env=null — so wasm's functype-checked `call_indirect` matches and DIRECT calls stay unchurned. A function-typed PARAM carries a companion env **appended** after the user params (no source-slot remap). Env threaded once per call-shape (`finalizeCallArgs`/`appendCalleeEnvArgs` at lowering for direct/try; `emitClosureEnvArg` for indirect). **Managed capture = a BORROWED pointer by value** (env owns nothing, valid because the escape rule keeps the frame alive). **A1 golden CHURN (endorsed, contained): 24 function-value fragments moved** (`[name]→[__fnref_name]` + null-env companion arg), no other spec.
> ### ⭐⭐ **THE INDEPENDENT REVIEW FOUND A REACHABLE SEGFAULT (139) — removing A2b-1's blanket arg-guard reopened the INTERPROCEDURAL store/return escape** (a capturing closure passed to a callee that persists it → the companion env, valid only for the call, is lost → null-env nil-deref). **Fixed (`f6b8ec98e`) with the per-parameter ESCAPE SUMMARY** = OPEN #13's P1.5 promise: `SemanticCheck.buildEscapeSummary`, a function-typed param ESCAPES iff used anywhere except as an `indirectCall` callee (a WHITELIST — sound-by-construction, the #73 DERIVE cure); a capturing closure at an escaping param → E3099, a plain function passes. The implementer found a 3rd route (loop-phi/block-arg) by probing; the coordinator adversarially probed 7 routes (all E3099) + the accepts (green). ⚠ shv2 is now STRICTER than the bootstrap here (the bootstrap deferred #13). Diagnostic-only (0 codegen change), scale flat +11. **Deferrals/finds → OPEN #77** (string-backed error-enum raw values block try-call), **#78** (wasm non-i64-width function-value `call_indirect` trap), **#79** (closure-with-fn-param). **⇒ NEXT = the async half of P1.5: B1 (GT scheduler core), with the scheduler/stack-model rulings due to the user; and the small #71 (inline conditional) unlocks the deferred ternary closure cases.** [[project_p15_decomposition]]
> ### ✅ **`P1.5-B1a` IS CLOSED (2026-07-21, merged, → 930/0 host + 902/0 wasm) — the SINGLE-M COOPERATIVE GREEN-THREAD SCHEDULER CORE. `async f(args)` spawns a green thread and yields a Promise handle (an i64 GT pointer); `await p` drives a cooperative scheduler to completion and collects the scalar result. x64-windows.**
> **USER RULINGS (2026-07-21):** (1) **fixed-stack-FIRST → morestack B1a′** — B1a uses a FIXED 1 MiB `osAllocPages` stack (arm64's model, which runs the whole corpus incl. `stack-growth` by demand-paging), isolating relocating morestack (the highest-risk piece — a saved-rbp-chain-fixup bug is a silent stack-corruption UAF) into its own later slice B1a′; frame pointers were already paid for. (2) **defer E3073** (the "async never yields" gate) + **author BESPOKE scalar scheduler specs** — the corpus satisfies E3073 with `File.exists`, which shv2 has no file I/O for. **THE SPLIT:** HAND-ASSEMBLED raw bytes = `__gt_context_switch` + `__gt_trampoline` ONLY (`Targets/X64/X64GtRuntime.maxon` — rsp/rbp swap + ret-into-a-fresh-frame, unrepresentable in Std; neither touches `.data` — the `to` GT rides through in rdx); BUILDER-BUILT `StdModule` = `__gt_init/spawn/enqueue/dequeue/await` + a `.data` current-GT global + a global FIFO run queue (`Runtime/GtRuntime.maxon`, mirroring MmRuntime). Single-M (NO work-stealing/atomics/TLS — B1b), scalar (managed/float async arg or result → clean E2015 reject via the shared `tagIsAsyncScalar`, never leak). GT struct offsets byte-compatible with the authoritative GtLayout so B1a′/B1b graft cleanly. **The single-M crux:** `gt.waiter` is repurposed as "the GT to return to on completion," set by the await drive right before each switch-in; `await` is the sole scheduler driver (drains ready GTs until the awaited promise completes) — handles out-of-order, nested, and re-await. 12→13 bespoke `async-scheduler.md` cases (basic/parallel/sequence/spawn-arg/multiple-args/nested/spawn-immediately-awaited/spawn-not-awaited + 4 rejects + the leak guard). **⭐ THE INDEPENDENT REVIEW CONFIRMED the raw ABI byte-correct by DISASSEMBLY** (callee-saved set = `calleeSavedOrder` not rsi/rdi/rax which are shv2 arg-registers; trampoline parks gt in r12 before the arg loads; `call funcPtr` 16-aligned) and **fixed duplication itself** (`b8a595169`): the callee-saved set was written twice → now derives from `calleeSavedOrder`; the frame-size fact was comment-bound → now a compile-time `assertGtFrameConsistent` (sabotage-verified RED). **⭐ THE ONE MERGE BLOCKER — an unbounded GT stack leak** (every `__gt_spawn` committed a fresh 1 MiB `osAllocPages` stack with no free path; 15000 spawns → 14.27 GB, exhausts commit → crash; invisible to the `__mm` gate) — **fixed (`d29dee4a8`) with a STACK FREE-LIST**: `__gt_await` recycles a completed GT's 1 MiB stack from the WAITER side (after the switch off it lands back on `selfGt`), `__gt_spawn` pops-or-commits, re-laying the trampoline frame; the GT STRUCT stays RESIDENT so re-await still reads `p.result`. **15000 spawns 14.27 GB → 0.4 MB; 100k-loop 35 ms exit 0.** Residuals FILED not silent (review-sanctioned) → **OPEN #87** (the ~80 B struct bump-leaks per spawn — reclaiming it needs promise-liveness → B2 / a recycling allocator → B1c), **#88** (a NEVER-awaited GT never completes → its stack is never recycled; `while true { async f() }` still leaks → B2 linearity/exit-drain). Optimizer: **no superlinear defect** — B1a's own cost is +6,160 allocs/rung5, strictly LINEAR (the one new dense `valuePromiseMarks` per-value column). Rebased onto the parallel repo's x64-linux + P1.9 landings (3 conflicts resolved as COMBINATIONS: OS-gated panic-runtime + GT-chunks; `encodeCallReg` + `x64Syscall`; both log rows); OPEN issue-number COLLISION with the parallel repo's #82/#83 fixed by renumbering to #87/#88. **⇒ NEXT = B2 (await/Promise with the ERROR TYPE + timer/sleep + escape→`shared` + linear-await E3100), then C (async subprocess stdio + the parallel harness = the Phase-1 gate).** [[project_p15_b1_gt_scheduler]]
> ### ✅ **`P1.5-B2a` IS CLOSED (2026-07-21, merged `35349acfc`, → 946/0 host + 908/0 wasm) — LINEAR-AWAIT (E3100). `await` is LINEAR: a second await REACHABLE from a first, of the same promise, is a compile error. Closes OPEN #87's stated blocker (re-await is now unrepresentable). COMPILE-TIME only, single-M irrelevant.**
> ⭐ **THE SHV2-NATIVE DIVERGENCE (the rewrite's thesis in action): NO GreenThreadId column.** The bootstrap needs a GreenThreadId because ITS parser mints a fresh SSA value on every cross-block read (a re-tag op) and copies the promise on `let q = p`; shv2 does NEITHER — `parseVariableReference` returns `binding.boundValue` VERBATIM (SSA dominance, no re-tag) and `let q = p` binds q to p's ValueId. **So the promise's SSA ValueId ALREADY IS the stable green-thread identity** — aliasing shares it, cross-block reads share it, a re-arm (`p = async g()`) mints a fresh `asyncCall` ValueId. A GreenThreadId column would be a 1:1 relabelling = one fact written twice. NOT the E3102 move model either (both names stay live+awaitable, so `let q = p; await p` is ACCEPTED). The pass = a per-function CFG worklist reachability (`checkLinearAwait`/`reachableReawait`, `SemanticCheck.maxon`, gated behind the non-allocating `functionHasAwait`), keyed on the await's promise ValueId; **a path DIES at the `asyncCall` that RE-DEFINES the tracked ValueId** (the re-arm — since shv2 emits no assign op, re-passing the definition IS the kill), which separates `double-await-in-loop` (defn OUTSIDE the loop → refused) from a fresh-`let`-per-iteration loop (re-armed → accepted). Ported the ALGORITHM from the bootstrap's `CheckLinearAwait`/`FindReachableAwaitOf`; the identity mechanism is shv2's. 11 bespoke scalar `async-linearity.md` twins (6 double-await rejects → E3100, target-independent so they pass on x64 AND wasm; 5 linear accepts incl. exclusive-branches / rearm-after-await / ternary-arms, x64-gated). Optimizer: **own-cost EXACTLY ZERO** (A/B on/off bit-identical — the corpus emits no async so `functionHasAwait` skips the whole pass). **⭐ The independent review DIED mid-report** (hung compiling its probes — the same background-command hang the optimizer hit earlier); **the coordinator COMPLETED the verification independently** by running the dead reviewer's own adversarial probes + a batch: reachable double-awaits across conditional / nested-block / loop-back-edge / early-return CFGs → E3100; legal await-loops (fresh-per-iter), out-of-order, nested → accepted; the `await-aliased-loop-element` twin FAITHFUL (the corpus drains a `Promise`-Array needing P1.6 generics, so the twin re-spawns per iteration, testing the SAME alias+re-arm property). **NO over/under-rejection found.** OPEN #87 assessed NOT closed (the struct recycle is a delicate multi-part scheduler change — separate arg-buffer co-recycle, nested-await LIFO, GT0 safety — deferred to B1c per "only if clean"); **#88 stays deferred** (never-awaited is LEGAL — `spawn-not-awaited` pins it — so its fix is a drain-at-exit, NOT a reject; E3100 does not close it). New limitation FILED → **OPEN #89** (await of a LOOP-CARRIED/block-arg promise → E2015: the promise mark and the linearity both key on ONE `asyncCall` ValueId, and a reassigned-in-loop promise is a block-arg; pre-existing, SOUND, hits NO corpus case, → B2c). **⇒ NEXT = B2b (error-carrying `try await` — reuses the `throws` dual-value ABI, grows the GT struct + trampoline on x64+arm64), then B2c (Promise STORAGE — generics-coupled, P1.6), timer/`sleep` (own MID-BODY-YIELD rung), C (subprocess stdio + parallel harness = Phase-1 gate); B1b (multi-M) + B1a′ (morestack) + B1c (sharded alloc + managed async) remain.** [[project_p15_b1_gt_scheduler]]
> ### ✅ **`P1.5-B2b` IS CLOSED (2026-07-21, merged, → 957/0 host + 911/0 wasm) — ERROR-CARRYING `try await` (E3057/E3059). A throwing `async` fn is awaited with `try await p otherwise <h>`; the error rides shv2's EXISTING dual-register throws ABI (result R8 / flag R10). x64-windows.**
> ⭐ **Reuses the `throws` ABI end to end** — no new error machinery. `tryCall(result, errorFlag, __gt_try_await)` at the site; `__gt_try_await` is a THROWING-signature driver ending `errorReturn(gt.result, gt.threw)`. **The runtime change is MINIMAL:** the GT struct grows ONE field `threw@0x58` (0x50→0x60, byte-compat with the authoritative GtLayout); the hand-assembled trampoline gets ONE extra store `R10 → gt.threw` after the R8→result store (R10 was the arg-buffer scratch, provably dead after `call funcPtr`; captured unconditionally — garbage for a non-throwing thunk, but `gt.threw` is read ONLY by `__gt_try_await`, never by `__gt_await`). `__gt_await`/`__gt_try_await` refolded into ONE `buildGtAwaitDriver(name, throwing)` sharing the whole scheduler drive, differing only at the exit (`ret` vs `errorReturn`). Front-end: `parseTryCallTarget` accepts `await`; the promise carries the callee's ErrorType; **VOID async now awaits** (the handle is tagged `integer`, await-result derived from the callee — a `void` tag can't bind). The linearity pass EXTENDED to count `tryAwait` sites (double `try await` → E3100, locked in by a coordinator-added twin the review flagged as an untested gap). **⭐⭐ TWO LATENT SILENT-WILD-FREE HOLES FIXED (both caught by the corpus):** (1) plain `await` of a THROWING async COMPILED and dropped the error (the trampoline ignored R10) → now **E3057**; (2) ordinary `try f()` NEVER checked propagate error-type compatibility (`try throwsA()` in a `throws BError` fn compiled) → now **E3059** in the SHARED propagate path (fixes `try call()` AND `try await`). shv2's `throws` clause is a SINGLE type name (can't express `throws A|B`), so exact-name-match is correct + oracle-parity — union-membership over-rejection is structurally impossible. Optimizer: **own-cost ZERO** (corpus emits no `try`/`throws`/`async` — the whole front end is corpus-blind; baseline un-stale this time). Review: **MERGEABLE** — soundness verified with boxed/associated-value errors (error VALUE survives R8/R10, success sets R10=0 so no garbage-decref, boxed errors freed exactly once), trampoline R10 disassembly-verified in-bounds, duplication clean; fixed a whitespace mis-indent. 10 bespoke `async-try-await.md` twins (8 `try-await.*` + E3057 + E3059) + the double-try-await E3100 twin. Rebased onto the parallel repo's P1.9 follow-ups (clean). ⏭ Minor (reported, not filed): `asyncCallCalleeOf` runs 2× per await site (0 corpus cost); bare plain-`await` STATEMENT form unsupported (pre-existing). **⇒ NEXT = B2c (Promise STORAGE — generics-coupled, P1.6; also closes #89's block-arg await), timer/`sleep` (own MID-BODY-YIELD rung), C (subprocess stdio + parallel harness = Phase-1 gate); B1b (multi-M) + B1a′ (morestack) + B1c (sharded alloc + managed async) remain.** [[project_p15_b1_gt_scheduler]]
> ### ✅ **`P1.5 MID-BODY YIELD + NETPOLLER (sleep)` IS CLOSED (2026-07-21, merged, → 963/0 host + 911/0 wasm; rebased onto the parallel P1.6-A, 972/0 on the merged tree) — THE R3 SUBSTRATE. A GT can now SUSPEND MID-BODY: `sleep(ms)` parks the GT on a timer and yields; the scheduler NETPOLLS the earliest deadline when the run queue empties, re-enqueuing a parked GT when its deadline fires. This is the suspend/resume substrate the async subprocess harness rides. x64-windows.**
> ⭐ **THE YIELD REUSES `__gt_context_switch` — no new hand-assembled primitive** (`X64GtRuntime` bytes untouched). `__gt_sleep` parks (`status=waiting`, `io_yielded@0xA8=1`) and `context_switch(self, self.waiter)`; the driver KEEPS a `waiting` GT's stack (vs recycling a `completed` one) and switches back INTO it when its timer fires. **The `__gt_await` rewrite (the crux):** the drive loop is factored into a shared **`__gt_drive_until(targetGt, targetStatus)`** — `__gt_await`/`__gt_try_await` are now thin wrappers (B2a linearity + B2b error-carrying preserved, review-probed). **Completed-vs-yielded recycle gate** (the #1 stack-UAF risk): recycle only `status==completed`. **Netpoll:** on a dequeue miss, `timer_check(now)` wakes due GTs, else `Sleep(⌈(earliest−now)/1e6⌉ ms)` (real wait, never busy-spin). **GT0 fork:** a top-level `sleep` (`waiter==0`) has no driver, so it SELF-DRIVES `__gt_drive_until(self, ready)`; `timer_check` sets the current self-driver's status=ready WITHOUT enqueueing (else double-schedule). Deadline-ordered netpoll ⇒ deterministic interleave. **Timer store:** an UNSORTED O(n) fixed array (cap 256, 16-byte entries) — a deliberate divergence from the reference min-heap (a hand-Std sift-down is the subtle-corruption class this substrate must avoid; n=concurrent sleepers ≤2; layout min-heap-compatible for a later retrofit). **Clock:** `__gt_now_ns` = QPC via the OVERFLOW-SAFE SPLIT identity `(ticks/freq)*1e9 + ((ticks mod freq)*1e9)/freq` — NO 128-bit MUL/DIV, NO new Target op (kept in the Std tier, monotonic + overflow-safe for centuries). 2 new system-band StdOps `osReadClock`/`osSleepMs`; **3 always-present IatSlot imports** QPC/QPF/Sleep (in `PeWriter`, NOT `OsImportSlot` — that's the macOS enum, the implementer's correction of the brief). GT struct grew 0x60→0xB0 (`io_yielded@0xA8` + reserved 0x60–0xA0 for the future subprocess/IOCP fields, byte-compat with GtLayout). ⭐ **THE IMPLEMENTER STOP-AND-REPORTED TWO FORKS** (top-level-sleep-has-no-driver; the OS-import scope) — both resolved from the oracle, coordinator-approved; it also corrected two errors in the brief (OsImportSlot; the clock math). Optimizer: **no superlinear defect** — flat **+16 allocs/compile** (the 3 unconditional imports; the whole yield/timer path is `usesGt`-gated + corpus-blind). Review: **NO BLOCKERS** — 15 adversarial probes (stack-UAF-aliasing correct; the `waiting ⟺ live-timer-entry` invariant makes false deadlock-bails impossible; no double-schedule; B2a/B2b intact). 6 bespoke `async-sleep.md` twins. ⏭ Left (cosmetic, reviewer-judged non-defects): repeated timer-entry-address arithmetic (shared named constants, no silent-divergence path); the `TimerNoDeadline=0` domain sentinel. **⇒ NEXT (per the path-to-harness survey) = async SUBPROCESS spawn + yielding await (`cmd /c exit 3`→await→3, the harness SEED, rides THIS substrate), then subprocess stdio; the harness (C) also needs the ladder P1.6/P1.7/P1.8 the parallel repo is landing (P1.6-A generics now on main). B2c/B1c gated on P1.7 Array; B1b (multi-M) + B1a′ (morestack) OFF the critical path.** [[project_p15_b1_gt_scheduler]]
> ### ✅ **`P1.5 ASYNC SUBPROCESS SPAWN + YIELDING AWAIT` IS CLOSED (2026-07-21, merged, 976 → 984/0 host) — THE HARNESS SEED. A green thread spawns a Windows child (`cmd /c exit N`), does a YIELDING wait (parks the GT; the single-M scheduler netpolls parked children with `WaitForMultipleObjects`, resuming the GT on child exit), and returns the scalar exit code. Rides the mid-body-yield + timer-netpoll substrate. x64-windows.**
> New builtin `runProcess(cmd String) -> int` (name-recognized like `sleep`, borrows the String, returns int → usable in value position). 5 new system-band StdOps (`osProcessSpawn`=CreateProcessA / `osProcessExitCode`=GetExitCodeProcess / `osWaitHandle`=WaitForSingleObject / `osWaitHandles`=WaitForMultipleObjects / `osCloseHandle`) + 5 kernel32 IatSlots (append-only; Kernel32DirEntries stays 1). Runtime (`GtRuntime.maxon`): `GtOffIoHandle=0x80` (the reserved slot filled), a PROCESS STORE parallel to the timer store (two 64-slot parallel arrays `procHandles`/`procGts` fed straight to `WaitForMultipleObjects` — NO gather copy), `__gt_process_run` mirroring `__gt_sleep`'s park (slab scratch STARTUPINFOA+PROCESS_INFORMATION+mutable-cmdline-copy → CreateProcessA → park → reap via GetExitCodeProcess from the durable `io_handle`), and a netpoll extension in `__gt_drive_until` (pc==0 preserves the pre-subprocess timer path byte-for-byte; pc≥1 blocks on `WaitForMultipleObjects(handles, earliest-timer-delta or INFINITE)`, then re-checks via loopHdr — NOT netpoll — with a fresh-clock timer re-check).
> ⭐ **THE DESIGN DIVERGES FROM BOTH REFERENCES, and the divergence is the thesis:** v1 and the bootstrap BLOCK the M on `WaitForSingleObject(hProcess)` and get subprocess concurrency from MULTI-M work-stealing (their true-yield `SyncOpProcessWait` path is built-but-DEAD in both). shv2 is SINGLE-M, so it cannot block the M — the netpoll blocks on the handle set only when the run queue is empty, exactly as the timer netpoll already blocks on `osSleepMs`. The bootstrap validates the OUTPUT (exit code), not this mechanism; the yield is proven by `interleave-with-sleep` (21 not 12). **THE #1 RISK (TIB stack bounds on CreateProcessA from a GT stack) EVAPORATED** — shv2's FIXED 1 MiB GT stack (vs v1's 2 KB) runs CreateProcessA correctly with RSP outside the main-thread TIB bounds; NO TIB save/restore added to `__gt_context_switch` (the v1 TAKE was not needed). Probed first via a synchronous blocking wait before layering the netpoll yield.
>
> ### ✅ **`P1.5-B1c` IS CLOSED (2026-07-23, merged `b9bc8c98b`, rebased onto the parallel P1.7 → 1396/0 host) — GREEN-THREAD MEMORY RECLAMATION (#87 + #92).** The two `__slab`-invisible async leaks are closed. **#87 (GT struct + arg buffer):** an intrusive LIFO free-list (`__gt_free_list_head`, threaded through the idle `GtOffNext`) — the AWAITER (`__gt_await`/`__gt_try_await`) pushes the completed struct after reading `result`/`threw`; `__gt_spawn` pops-before-`__slab_alloc` and WHOLE-STRUCT memzeroes a recycled struct (v1's stale-field trap `Arm64MacosGreenThread.maxon:2376-2404`, NOT the bootstrap's weaker field-by-field). The arg buffer is INLINED into the struct (`GtOffArgInline`@0xB0 + a `gt.argBuf` self-pointer → the trampoline stays byte-identical) so it recycles with the struct — a deliberate divergence from both references (which keep it separate + `__slab_free`, a path shv2's pure-bump slab lacks). **#92 (subprocess scratch):** the three fixed-size buffers moved to one-time `__gt_init` allocs reused per call (like the timer heap), the cmdline copy a grow-on-demand global, three per-call re-zeros for reuse staleness. x64-windows.
> ⚠ **RECLAIM IS AT AWAIT, NOT COMPLETION — the review's blocker.** A first pass reclaimed at completion; a review found this aliased an out-of-order-awaited sibling (a GT completed as a side effect of driving a DIFFERENT await, its struct pushed while its promise was still live → next `async` pop-and-memzeroes it → SILENT WRONG ANSWER, repro→130 vs 100). Fixed by moving the push to the awaiter (v1's discipline). `async-scheduler.out-of-order-await` (=100) pins it; `struct-reuse` + `scratch-reuse-loop` pin the reuse paths. The proposed `__gt_live_count` leak-gate was REMOVED BY RULING: with reclaim-at-await it could only fire on a run-but-never-awaited GT (#88 family), i.e. it half-gated a deferred leak. **RESIDUAL: #88** — the un-awaited tail (never-run GT, and a completed-but-dropped-promise GT) still leaks its struct, now silently (no GT counter) → **B2**.
> ### 🔴 THE FINDS (coordinator verification + independent review — the process working):
> - **A REACHABLE HEAP OVERFLOW (coordinator):** `__gt_proc_add` wrote `procHandles[count]` with NO bound check; >64 concurrently-parked children (a loop of `async runProcess` then one await drives them all to their park) overran the 64-slot arrays → **segfault 139 (sabotage-verified)**. FIXED: a capacity guard `osExit(ProcStoreOverflowExitCode=70)` at 64 — a documented, SAFE hard bound (`WaitForMultipleObjects`' MAXIMUM_WAIT_OBJECTS; >64 = the IOCP C rung, no consumer yet). Pinned by `store-overflow-aborts`.
> - **A COVERAGE GAP (coordinator):** NO committed test parked ≥2 children concurrently (sequence/spawn-loop are one-at-a-time). Added `multi-concurrent` (3 parked → count=3 WaitForMultipleObjects + parallel-array swap-remove → 123).
> - **THE SIGNATURE BUG, review found it (`a9b3d00b4`):** the Win64 outgoing-arg ABI (5th+ stack-arg displacement + outgoing frame size) was written TWICE — `createProcessArgDisp`/`createProcessOutgoingArgBytes` byte-identical copies of the pre-existing `writeFileArgDisp`/`stringPrintOutgoingArgBytes`; a change to one miscompiles the other's calls. Collapsed into shared `win64OutgoingArgDisp`/`win64OutgoingArgBytes` (byte-identical codegen). Review assessed `buildGtProcCheck` vs `buildGtTimerCheck` as SHAPE-not-fact duplication and correctly declined to force-merge (the parallel-array store is load-bearing).
> Optimizer: nothing superlinear; own cost FLAT +28 allocs/+2,040 bytes = the 5 unconditional PE imports (`buildGtProcCheck`'s O(n²) is capped at 64 → linear-in-practice). 8 bespoke `async-subprocess.md` cases (exit-code / sequence / multi-concurrent / interleave-with-sleep / spawn-loop / spawn-failure-aborts / store-overflow-aborts / non-string-arg-rejected). **⇒ NEXT per the path-to-harness survey = async subprocess STDIO (pipes) — needs P1.6 generics / P1.7 Array / P1.8 String-methods (the harness cone); the async R3 substrate (yield + netpoll + timer + subprocess-exit) is now COMPLETE.** Residuals → OPEN #92 (per-call slab scratch bump-leak, B1c-reclaimed) + #93 (runProcess spawn-failure/overflow abort silently — a throwing `runProcess` is a follow-up). [[project_p15_b1_gt_scheduler]]
> ### ✅ **`P1.5-B1a′ slice 1` IS CLOSED (2026-07-22, merged, 989/0 unchanged host) — the GT→SYSTEM-STACK SYSCALL SHIM. A green thread's Win32 kernel calls (CreateProcessA, WriteFile, WaitFor*, Sleep, QPC, VirtualAlloc) now run on a single global 64 KB SYSTEM stack, not the GT's own stack, with the NT_TIB stack bounds (`gs:[0x08]`/`gs:[0x10]`) repointed at the system stack for the call. GT stacks STAY 1 MiB, so nothing faults today — TRANSPARENT infrastructure whose correctness proof is "every spec stays green through it"; it is what makes slice 2's 2 KB relocating GT stacks safe. x64-windows.**
> Ported v1's `emitX64GtIatCallOnSystemStack`/`SystemStackEnter` (`X64Backend.maxon` ~8290-8409), with TWO shv2-native divergences forced by the register-allocated Std runtime: **(1) COPY, not write-after-switch** — v1/bootstrap switch RSP then write the call's stack args on the system stack (sound only for HAND-assembled runtime); shv2's `__print_string`/`__gt_process_run` are builder-built Std addressing spills AND outgoing args RSP-relative, so an early switch would make a spill reload read the system stack = silent miscompile. The shim wraps ONLY the `iatCall` (RSP unchanged through arg-setup) and COPIES the N true stack-arg words (CreateProcessA 6, WriteFile 1) from the GT frame to the system stack after the switch; ≤4/>4-arg unify into one helper keyed on the import's stack-arg count. **(2) TIB in the CONTEXT SWITCH** — `__gt_context_switch` now SAVES the suspending GT's TIB bounds and LOADS the resumed GT's, so `gs:[0x08]`/`gs:[0x10]` always describe the executing stack (GT0's real bounds captured on the first switch away from it). ⚠ **This SUPERSEDES the subprocess-seed box's "NO TIB change needed"** — that held only because 1 MiB GT stacks + no shim never probed the TIB into a fault; the shim (and slice 2's 2 KB stacks) make TIB coherence load-bearing. Gated on `moduleUsesGt` (0 non-async fragments churn; non-GT codegen byte-identical) and self-gates at runtime (`currentGt==0 || stackBase==0 → direct call`, so GT0/main is unshimmed).
> 🔴 **REVIEW found THE SIGNATURE BUG in the rung's own new code:** `gtShimStackArgWords` restated "CreateProcessA = 6 / WriteFile = 1" as its OWN constants — a THIRD copy of a fact the isel owns (`CreateProcessOutgoingSlots`), with a comment CLAIMING a drift-safety property the code did not have. A future CreateProcessA arg-layout change would silently miscompile (kernel reads garbage stack args). FIXED: the count lives ONCE in `StdToX64Conversion.maxon`, exported, read by the shim — isel reservation + shim copy now derive from one fact (byte-identical). Review adversarially PROBED the hand-asm (register discipline vs the actual `iatCall` implicitDefs mask, copy offsets, TIB coherence across full switch cycles, guard, alignment, no-nesting) — all correct. Optimizer: nothing superlinear, all scale-test deltas 0 (shim never fires on the non-GT corpus). **⇒ NEXT = slice 2: relocating morestack + shrink GT stacks to 2 KB** (the prologue guard, the hand `__gt_morestack` chain-walk chunk, `osFreePages`, the alloc/free/free-old lifecycle) — the deep-recursion red baseline; kernel-safety already solved by this shim. [[project_p15_b1_gt_scheduler]]
> ### ✅ **`P1.5-B1a′ slice 2` IS CLOSED (2026-07-22, merged, 993/0 → rebased onto P1.6-B2 = **1002/0** on the merged tree) — RELOCATING MORESTACK + 2 KB GREEN-THREAD STACKS. ⇒ P1.5-B1a′ (relocating morestack) IS COMPLETE. GT stacks now START at 2 KB and GROW-AND-RELOCATE on a per-function prologue-guard fault: `__gt_morestack` copies the stack to a 2× `VirtualAlloc`, WALKS the saved-rbp chain fixing every interior frame pointer by the relocation delta, frees the old stack, and returns onto the new one. This is the model the frame-pointer-on-every-function decision (P1.0d.3) was MADE for. x64-windows.**
> Ported v1 `X64Backend.maxon:7102` (morestack) + `:7226` (chain walk) + `:7286` (guard) line-by-line. Prologue guard (byte-level in `emitFunctionChunk`, gated `usesGt && !isRuntimeFunction && frameBytes>0`; uses r10/r11, non-arg regs): `cmp rsp−frameBytes−margin, gt.stackGuard@0x50 ; jae skip ; else call __gt_morestack`. Reuses slice-1's scratch stack + TIB helpers. New `osFreePages` Std op (→ `VirtualFree`; rejects non-x64-windows with `panic` — the GT runtime is x64-windows-only, so the arms are unreachable). Free-list DELETED → alloc-on-spawn / free-old-on-grow / free-on-complete (shrinks OPEN #88's never-awaited leak from 1 MiB to a 2 KB seed). Runtime (`__`/`mrt_`) functions EXEMPT from the guard (they must not re-enter the grower; safe because their kernel calls are shimmed onto the scratch stack by slice 1, and their frames fit 2 KB — verified empirically). ⭐ SOUNDNESS: shv2 has no addressable stack slots, so the ONLY stack→stack pointers are saved-rbp links + `gt.sp`/`gt.fp`; the walk fixes the chain, and `gt.sp`/`gt.fp` are stale-but-overwritten-on-next-switch (a switch-IN requires a prior switch-OUT). x64 does NOT fix `gt.sp`/`gt.fp` (only arm64 does).
> 🔴 **RED-BEFORE-GREEN:** guard-off at 2 KB → deep recursion ACCESS-VIOLATES (0xC0000005 — a 2 KB stack cannot hold 200 frames); guard-on → green. New `async-stack-growth.md` (deep-recursion / multi-growth ≥4 / grow-across-yield / grow-then-complete-then-respawn). REVIEW adversarially probed `deepRecurse(1000/3000)` through 5–7 growths, main-thread deep recursion (GT0 never grows), yield-at-deepest-frame + resume, sibling-frees-while-parked-deep, print+String at depth 800 (margin coverage), CreateProcessA at depth 600 on a grown stack — all exact, no leak; and FIXED a cross-boundary duplication (the "repoint TIB at the system stack" idiom open-coded in BOTH the shim and morestack → shared `emitRepointTibToSystemStack`). Optimizer: growth policy DOUBLES (amortized O(1)/word), guard O(1)/fn, no superlinearity; the flat +216 B is the one new unconditional `VirtualFree` import (pre-existing import-table design — every x64-windows PE lists all IatSlots; a `usesGt`-conditional import table would be its own optimization rung). ⇒ **The full R3 async substrate AND the relocating morestack it was designed for are DONE.** Off the critical path: B1b (multi-M) + arm64/wasm morestack parity. [[project_p15_b1_gt_scheduler]]
> ### ✅ **`P1.6-A` IS CLOSED (2026-07-21, merged, 957 → 966/0) — GENERIC TYPES over TRIVIAL type arguments. FRONT-END ONLY, no layout descriptor, no new IR op.**
> `type X uses T` declarations + `typealias Y = X with C` instantiation. A type parameter is an **opaque 8-byte value** (`MaxonType.typeParameter` → i64), so a generic type's method bodies compile **ONCE** against `T` — no substitution, no monomorphization, no descriptor. **⭐ THE CENTRAL INVARIANT: a trivial generic instantiation lowers to BYTE-IDENTICAL Target IR as the equivalent monomorphic struct** (whole program, verified). `Self{}` in a shared body allocs `fieldCount*8` with destructor pointer 0 (trivial); a struct type-argument is a **BORROW** (the store into a `T` field is not a move — the caller retains + drops it), which keeps the shared trivial destructor sound. **Managed type args are REJECTED** (E2057, deferred to P1.6-B) so no leak ships (4 coordinator + 10 reviewer adversarial leak/UAF probes all clean). New arms `typeParameter(TypeParamId)`/`genericInstance(GenericInstanceId)` band-appended past `function`; args held OUT OF LINE in an **O(1) composite-key `GenericInstanceRegistry`** (NOT v1's O(n²) structural scan). New codes: **E2055** GenericBaseNotGeneric (oracle-parity "has no associated types"), **E2056** GenericArityMismatch, **E2057** GenericManagedArg. Generic FREE functions are unsupported (oracle-parity E2010 — generics are type-level only). 9 bespoke `generic-types.md` tests; `sizeof.type-parameter`×2 ported disabled → P1.6-B. **⭐ Independent review killed a latent silent-wrong-answer**: `paramNamesIn`/`paramTypeParamFlagsIn` were two index-aligned parallel walks (a misaligned flag → wrong-param borrow → struct leak) — merged into ONE walk (structural alignment). Optimizer: **no superlinear defect** (own cost linear, corpus-blind to `with`). ⚠ **maxon-sharp bug found (NOT triggered, cleanly worked around)**: the bootstrap tracks droppable union cases in a u64 bitmask, so a Maxon union's **65th** managed arm corrupts its synthesized destructor; shv2's `ParseError` is at exactly 64, so the 3 generic diagnostics route through a new `FileParseArtifact.diagnostics` channel (single-sink, review-verified sound) — **the bootstrap fix is a separate follow-up**. **⇒ NEXT = P1.6-B (the layout descriptor goes LIVE: managed-T + dictionary-passing + drop-through-`destroyFunc@40` + `sizeof(T)`), then P1.6-C (per-instance ranged typealiases).**
> ### ✅ **`P1.6-B1` IS CLOSED (2026-07-21, merged `96790fa01`, rebased onto async-subprocess main → 989/0 host) — `sizeof(T)` via a LIVE LAYOUT DESCRIPTOR + the DICTIONARY-PASSING substrate. The descriptor-READ half of P1.6-B; managed-T + drop is B2. x64-windows.**
> ⭐ **DECISION (settled by the committed architecture, not a user ruling): DICTIONARY-PASSING, not monomorphization.** The two references disagree — the bootstrap MONOMORPHIZES (`MonomorphizationPass`, `sizeof` folds post-substitution), v1 threads a runtime descriptor. shv2 follows v1 because P1.6-A committed to shared bodies ("compiled ONCE against opaque T") + PLAN's "dictionary-passing + 64-byte descriptors" + the `funcAbs64InRdata` scaffolding. The bootstrap is the BEHAVIOR oracle (managed T allowed, `sizeof(T)`=`sizeof(concrete)`: bool 1, 2-int-struct 16, String 48 = the full envelope, drop exactly-once). NEW `Compiler/IR/LayoutDescriptor.maxon` — a 64-byte descriptor byte-ABI-identical to v1's (`size@0`/`alignment@8`/`elementSize@16`/`flags@24`/`copyFunc@32`/`destroyFunc@40`/`fieldOffsetsPtr@48`/`elementLogicalSize@56`); at B1 only the DATA fields are non-zero (funcptr slots 0 ⇒ **NO `funcAbs64InRdata` reloc yet** — that's B2), the gid→label registry is an **O(1) Map** (NOT v1's linear scan). `sizeof` is NET-NEW (no `sizeof` existed for any type): lexer keyword + parser + `MaxonOp.sizeofType`; a CONCRETE operand folds to a literal in the PARSER (`mergeArtifact` doesn't remap op operands, so a `structRef` id can't ride the op cross-file), a `typeParameter` operand emits `sizeofType(typeParameter)` lowering to a runtime read of the threaded descriptor's `elementLogicalSize@56`. **Dictionary-passing substrate:** a generic method that (transitively) reads `sizeof(T)` reserves ONE hidden trailing i64 descriptor param (slot in `[0,paramCount)` for the `ParamCrossForbid` clobber guard); a concrete call site materializes `rdataAddr(__layout_…)`, a self-receiver call FORWARDS the enclosing descriptor. Also closed a P1.6-A gap: a generic-alias constructor result is retyped `structRef(base)`→`genericInstance(gid)` so the receiver carries the instance identity, and `genericInstance` joins the managed-aggregate boundary (owned box dropped once; the type-arg store is a BORROW) — a genuine leak-fix.
> ### 🔴 THE FINDS (coordinator adversarial probing + independent review — the process working): **FOUR reachable compiler PANICS on valid programs, ALL the committed suite was green over, ALL fixed (never filed):** (1) transitive `self.helper()` where the helper reads `sizeof(T)` (non-transitive pre-scan + `self` typed `structRef`) → FIXED with a transitive fixpoint + self-receiver forwarding; (2) multi-parameter `sizeof(K)` — the implementer REPORTED a clean rejection that did NOT fire → FIXED to a genuine E2015; (3) `sizeof(T)` in a STATIC method and (4) in a CLOSURE (both receiverless) — found by the independent review → FIXED to a clean E2015 (no `__self` in scope). Optimizer found + fixed the ONE superlinearity: `solveDescriptorNeedFixpoint` was **O(N²+N·C)** (Gauss-Seidel iterate-to-stable, quadratic in a generic type's method count on a deep self-call chain, measured 288→30 ms parse at N=800) → **O(N+C)** reverse-reachability BFS over a CSR reversed self-call graph, byte-identical codegen. Review deduped 3 (the hand-rolled CSR → shared `CsrGraph.buildCsr`; `Parser` size-compute clone → the one `ProgramSignatures` authority; `registerRdataConstant`/`…Labeled` bookkeeping → `appendRdataPayload`). ⏭ **BOUNDED LIMITATION (documented, blocks no spec):** forwarding a descriptor from a non-`self` same-base value (`let c = self.makeCopy(); c.directSize()`, or `combine(other Self)`) → clean E2015, deferred to a later slice. Acceptance: flipped `sizeof.type-parameter` (→1) + `sizeof.type-parameter-struct` (→16); added bespoke `sizeof.concrete` (25), `sizeof.self-forward` (1), `sizeof.transitive-two-hop` (16). ⚠ maxon-sharp `fmt` mis-parses union/enum keyword-named cases (`await(`→`await (`) — reverted by hand, a separate bootstrap follow-up. **⇒ NEXT = P1.6-B2 (managed type args + drop-through-`destroyFunc@40`: the `funcAbs64InRdata` reloc across the 6 targets + WASM funcref-table, destroy-thunk synthesis, revise the trivial-scalar classification, remove E2057), then P1.6-C (per-instance ranged typealiases).**
> ### ✅ **`P1.6-B2` IS CLOSED (2026-07-21, merged `94269ee23`, rebased onto P1.5-B1a′ main → 998/0 host + 938/0 wasm) — MANAGED generic type arguments, OWNED + dropped EXACTLY ONCE. `Box with String` works end to end. x64-windows (+ wasm scalar; a managed arg is Beyond wasm's scalar core).**
> ⭐⭐ **THE B1-BANNER "NEXT" WAS WRONG, and a survey caught it BEFORE the wave: shv2 drop is NOT a header-destructor pointer.** `emitBoxAlloc` always bakes `TrivialDestructor`; the drop callee is picked STATICALLY at the parser decref site (`decrefCalleeFor`). A concrete `Box with String` is dropped at a CONCRETE site where the value carries `genericInstance(gid)` (via `retypeGenericAliasConstructorResult`), so the parser knows the gid ⇒ STATIC per-instance destructor synthesis. **⇒ B2 needs NO `funcAbs64InRdata` and NO descriptor `destroyFunc@40` (they stay 0/panicking).** That reloc path is needed ONLY to drop an OPAQUE `T` at RUNTIME inside a shared body (an overwrite `self.value = newT`, or `Array` element teardown) — DEFERRED (folds into **P1.7 `Array`**); the full reloc implementation map (mirror `StringBufferFixup`, `GlobalDataTable.pendingRdataRelocs`, debug-symbol VA resolve, wasm funcref-index) is captured for that day.
> ⭐ **THE DESIGN RULING — MODEL A (owning containers), settled by the plan's charter (NOT a user ruling).** The implementer STOP-and-REPORTED that the naive "synthesize a destructor that drops the field" DOUBLE-FREES: shv2 generic instances BORROWED their `T` (the P1.6-A invariant, `Parser.maxon:2509` — the box aliases, the caller keeps the +1), and with NO `__mm_incref` a box destructor + the caller both drop → underflow. Coordinator ratified OWNING containers: a MANAGED type argument is CONSUMED into the box at the concrete construction site (reversing the P1.6-A type-param-borrow invariant FOR MANAGED INSTANCES ONLY — trivial args still borrow, goldens byte-identical; authorized because the borrow was an explicit deferral and the invariant's own comment says it exists only "because the trivial destructor never drops it"), and the box drops it via `synthesizeGenericInstanceDestructor` (a clone of `synthesizeStructDestructor` sharing `emitFieldCascadeDestructor`). The consume: `scanConsumeBits` records a per-constructor-param TYPE-PARAM-POSITION feed; the concrete call site consumes the arg iff `genericInstanceArgIsOwned(gid, pos)`. The own/borrow boundary is ONE classifier read by BOTH the consume gate and the drop gate ⇒ **consumed set == dropped set by construction.** E2057 RETIRED to `reserved`; nested `with` parses + interns via a flat pre-order `GenericArgNode`; a `T`-field read on a concrete receiver retypes to the substituted arg (a borrow).
> ### 🔴 THE FINDS (the process working): the implementer's stop-and-report (→ Model A) + the independent review found a **reachable DOUBLE-FREE the green suite hid** — a single MANAGED value stored into TWO owning fields (`Self{a: v, b: v}`), consumed once but dropped twice (exit 101). PRE-EXISTING in the monomorphic struct path; B2 makes it reachable via a generic managed arg. FIXED AT THE ROOT (shared struct-literal layer): the 2nd store of a moved managed value is a **use-after-move (E3102)**, closing BOTH forms (shv2 is move-only, no `__mm_incref`). Coordinator adversarial probes confirmed the owning thesis (double-hop escape reads valid content; var-reassign double-free-free). Optimizer: NO superlinear — the +87..+2,195 linear delta is a per-call `ConstructorInstance` union box (0.02%, mirrors `ReceiverArg`); a cheap shared-global fix hit a UAF (the bootstrap MOVES the union) and was correctly REVERTED. Review deduped `resolveArgList`, dropped 2 orphan fragments. Acceptance: converted `error.managed-string-arg`/`error.managed-struct-field-arg` → success, enabled `generic-nested-trivial`, authored leak-gated `managed-string-arg-loop` / `managed-instance-escape-owns-content` / `nested-managed-cascade` / `managed-string-arg-moved-not-double-freed` / `generic-string-member-via-generic` + `error.generic-double-store-managed` + monomorphic `error.managed-double-store` + `scalar-double-store`. ⏭ **BOUNDED (documented, sound):** a TRIVIAL generic instance's double-store (`DPair with Integer`, `Self{a:v,b:v}`) is ALSO rejected E3102 (shared body compiled once against opaque `T`, move-only has no clone) — stricter than monomorphic `IntPair`, niche. **OPEN #94** filed (a union payload typed as a generic-instance alias constructs/drops but can't be match-bound — clean E2015, beyond-scope). **⇒ NEXT = P1.6-C (per-instance ranged typealiases); the deferred descriptor-`@40`/`funcAbs64InRdata` opaque-T runtime drop folds into P1.7 `Array`.**
> ### ✅ **`P1.6-C` IS CLOSED (2026-07-22, merged `e65a164c9`, rebased onto P1.5-B1a′ slice-2 main → 1011/0 host + 947/0 wasm) — PER-INSTANCE RANGED TYPEALIASES. ⭐⭐ THIS COMPLETES P1.6 (A + B1 + B2 + C). Front-end / type-system only, no codegen. x64-windows.**
> A ranged `typealias Idx = int(…)` declared inside a generic type is NOMINALLY DISTINCT per instantiation: `WrapperA.Idx ≠ WrapperB.Idx` even when both are `Wrapper with Integer` — distinctness keyed on the INSTANCE-ALIAS NAME, not the type args. Referenced as `Instance.Idx`; cross-instance misuse → E3005; `as` converts between compatible per-instance aliases; a per-instance Idx DECAYS to plain int where a plain numeric is expected. The Idx is a scalar → erases to i64 before lowering, so NO codegen, no new IR op (P1.6-A's "front-end only" envelope). **DESIGN = Option B** — ride `MaxonType.named` with a qualified REGISTERED name, reusing the struct/boxed-union nominal-identity machinery (`namedAggregatesConflict`/`aggregateNameOf`), settled vs a new arm. The bootstrap is the behavior oracle (v1 mirrors it).
> ⭐ **THE MODEL MISMATCH the implementer STOP-and-reported: `WrapperA` and `WrapperB` intern to ONE `GenericInstanceId` (keyed `(baseId,args)`) and method dispatch produces ONE shared `Wrapper.setTag` signature — so the alias NAME is NOT recoverable from a value's gid; it is a PARSE-TIME source fact.** Coordinator greenlit a parser-side `valueInstanceAlias` map (value→originating-alias-name, propagated through `Self`-returning methods, delivered per-function via `FunctionRangeChecks`) + a parse-time method-arg check; free-function syntactically-qualified params (`takeStrTag(t StrWrapper.Idx)`) keep the SemanticCheck route. Rejected: distinct gids (breaks descriptor/destructor dedup), a per-value IrFunction column (can't produce the EXPECTED name for a shared method), monomorphized signatures (breaks the shared-body thesis).
> ### 🔴 THE FINDS (coordinator oracle-probing + review — the process working): **TWO reachable front-end wrong-answers the green suite hid, both FIXED:** (1) the `as`-cast RETAGGED ITS SOURCE in place → `let b = aTag as WB.Idx` corrupted `aTag`'s nominal type → a later valid use of `aTag` false-E3005'd; FIXED to emit a DISTINCT `value + 0` result (source preserved). (2) a per-instance value OVER-REJECTED on RETURN/reassignment/coercion instead of DECAYING to plain int (`return w.getTag()` from an `int` fn → false E3005 where the oracle exits 42); FIXED with ONE `aggregatesConflict` authority — the per-instance conflict fires ONLY when the TARGET is itself a per-instance alias (a per-instance source into a plain numeric decays; struct/union unaffected, caught by the tag check first). Optimizer REFUTED the implementer's "allocates nothing" claim (a REAL +2,505..+56,197 alloc regression: `Parser.create` minted 5 collections/construction + a per-call union box) → FIXED allocation-neutral (module-global shared-empty COW + a `hasInnerAliasParams()` gate), ~99.4% removed. Review traced the module-global COW invariant sound (all writes behind an is-identity guard) + consolidated the aggregate-conflict rule. Acceptance: the 5 canonical `per-instance-typealias.md` cases + bespoke `cast-preserves-source` / `error.cast-does-not-launder-source` / `return-decays-to-plain` / `reassign-decays-to-plain`. ⏭ **BOUNDED (matches the oracle, NOT a defect):** factory/constructor inner-alias args aren't nominally checked (`WB.create(tag: aWAIdx)` decays — the bootstrap does too) — a future strictness slice. **⇒ P1.6 IS COMPLETE. NEXT = P1.7 `Array` (= P1.6 ∘ P1.2), which also picks up the DEFERRED descriptor-`@40`/`funcAbs64InRdata` opaque-`T` runtime drop.**
> ### ✅✅ **`P1.0d.4` IS COMPLETE (2026-07-22) — THE FLOATS PHASE IS DONE. suite 767 → 1313/0.** All three ledger residuals closed: #19 (float range checks), `floor`/`ceil`/`round`/`sqrt`, and float const-folding. Six slices: A (exact literals) · C (math intrinsics) · D1 (bootstrap inverse bitcast) · B (range checks) · A′ (literal diagnostics + `-N` fold) · D2 (const-fold). **This is the first phase the residual-clearing loop closed end to end.**
>
> **SLICE D2 — shv2's const evaluator now FOLDS float arithmetic** (`let X = 1.0 + 2.0` → 3.0, was `E2015`). ⭐ **The plan was WRONG at the code, and the correction is the lesson**: the guard's own comment said folding floats "needs software IEEE-754 arithmetic inside the compiler." It does NOT. The real blocker was a missing `i64↔f64` **reinterpret** — so **D1** added `__Builtins.bitsToFloat` to the bootstrap (mirror of the existing `floatToBits`, its arm64 half already dead-coded and awaiting a caller), and **D2** folds via `bitsToFloat`→**host f64**→`floatToBits` (`TypeRules.foldFloatBinOp`/`foldFloatCompare`), borrowing the host's IEEE arithmetic. Writing software IEEE-754 would have been a SECOND copy of what `parseFloatBits` already encodes — the project's signature "one fact written twice." ⚠ **The comparison is the subtle half**: two positive doubles order identically as their integer bit patterns, so a float `>` fold "works" by accident on positives — `foldFloatCompare` compares as real f64, verified against the oracle on the negative-operand and NaN cases an integer compare gets BACKWARDS. Deliberate divergences, all oracle-consistent: `1.0/0.0` is REJECTED at compile time (a const can't represent the `+inf` the runtime `divsd` gives; rides the host trap, not an integer `rhs==0` check that would misread `-0.0`); float `mod` / word-ops rejected exactly as shv2's runtime rejects them. Byte-identical `.data` proof: `var pi = 3.0 + 0.14` lays down the EXACT bits of the literal `3.14` — the fold produced a number, not a summed bit pattern. Math intrinsics in a constant (`sqrt(2.0)`) are cleanly rejected E2045 (a call, caught before any fold). The D2 review found the bootstrap itself mis-folds NaN self-comparison (#117 — shv2 is right) and confirmed a pre-existing `__Builtins`-comparison lowering panic (#118).

> ### ✅ **`P1.0d.4` SLICES A + C ARE CLOSED (2026-07-22, merged `b57395995` + `a307ae4ac`, 1175 → 1249/0 host) — EXACT float LITERALS and the `floor`/`ceil`/`round`/`sqrt` intrinsics. ⚠ P1.0d.4 STAYS ◑ — slices B, D and A′ remain (see the glance table).**
>
> **SLICE A — the literal decoder was returning WRONG ANSWERS AND HANGING THE COMPILER, and no test could see it.**
> `parseFloatBits` applied the decimal exponent by repeated `*10` in an **i64**. `10^k` contains `2^k`, so:
> `1.0e20` → wrapped garbage · `1.0e64` → **silently `0.0`** (`k ≥ 64` is exactly 0 mod 2^64) · **`1.0e63` → the
> compiler SPINS FOREVER** (`10^63 ≡ 2^63 ≡ i64.min`, negative, so the `shiftUp` loop doubles a negative without
> bound). Invisible because **`specs-shv2/` contained no float exponent literal at all** — the largest exponent
> anywhere in `/specs` is `e3`. ⭐ **NOT A PORT, and this is the lesson:** v1's `NumberParsing.maxon:13-226` is
> **byte-identical** to shv2's *including the bug* — shv2 inherited a defect by copying — and the bootstrap
> sidesteps it entirely via .NET `double.Parse`. Neither reference had an algorithm to take. Replaced with
> bounded-width base-2^32 limb magnitudes, a 768-significant-digit budget + a **sticky** bit feeding
> round-to-nearest-even, `MaxBigLimbs = 144` enforced by a panicking `requireLimbBudget`; **subnormals and
> underflow-to-zero added (neither existed)**; overflow decided by the CONVERTED VALUE, not a decimal estimate.
> ⚠ **The independent review found a BLOCKER the suite was green over**: `sciExp` was clamped at a FIXED 100000,
> but it competes with `decExp`, which moves **one per digit** — so a long enough digit run out-pulls the clamp
> and flips the verdict BOTH ways. `0.<200001 zeros>1e999999999` (magnitude 1e999799997, astronomically past
> `f64.max`) was **accepted and compiled to `0.0`**; the mirror case got a spurious E2011. Both confirmed against
> the oracle; regression tests **proven to have teeth** by reintroducing the clamp. The review also deduped a
> carry flush written THREE times (one spelling pushed UNMASKED) and **corrected the 768 bound's own
> justification** — the claimed 752 is the width of `5^1075` ALONE, ignoring the odd significand up to `2^54-1`;
> the true widest rounding boundary `(2^54-1)·2^-1075` needs **exactly 768**, so the budget has ZERO slack while
> its stated reason would have justified shaving 16 digits off it.
>
> **SLICE C — `floor`/`ceil`/`round`/`sqrt` across 3 tiers and 5 targets.** x64 `roundsd 66 0F 3A 0B /r imm8`
> (floor `01`, ceil `02`, round `00`) through a NEW `encodeRoundsdRegReg` — a three-byte opcode plus a trailing
> imm8 that `encodeSseRegReg` structurally cannot express; `sqrtsd` on the existing path; arm64
> `frintm`/`frintp`/`frintn`/`fsqrt`; wasm `f64.floor`/`ceil`/`nearest`/`sqrt`. **`round` is ties-to-EVEN** on all
> three targets — both references agree and `specs/round.md`'s `round.halfway` pins `round(2.5) == 2`, so the bare
> hardware instruction is already correct and **no correction layer was added**. Took the bootstrap's plain
> immediates, **left** v1's `0x08/0x09/0x0A` (the extra bit is the Precision Mask: changes no value, and shv2
> reads no MXCSR flag). ⚠ **shv2's x64 baseline moves SSE2 → SSE4.1 HERE AND NOWHERE ELSE.** The review found the
> `StdUnaryOpcode → FloatRoundMode` mapping **FORKED** across the two isels — with the arm64 comment *asserting*
> the property the code lacked — where respelling one arm makes x64 silently compute `floor` for `ceil`, a wrong
> answer and not a build break, and asymmetrically so because **arm64's goldens can be regenerated on no host we
> have**; un-forked into `floatRoundModeFor`. It also byte-verified the encoder in the register range the suite
> **structurally cannot reach** (committed tests only touch xmm0-xmm2, so REX on `roundsd` was untested).
>
> ⇒ **REMAINING for P1.0d.4: slice B** (float RANGE CHECKS — #19 proper; the oracle already settles the rule by
> running: compile-time E3005 for a literal, a runtime panic guard for a non-literal, exactly the int rule P1.9
> ruled — shv2 simply never built the float arm, and the parser filters float aliases out at ONE place,
> `Parser.maxon:5052-5059`; **v1 HAS a float arm worth porting**, including that `0 to N` unsigned folding is
> ineligible for floats because IEEE ordering ≠ unsigned ordering — but **do NOT port v1's `f32.min`/`f32.max`,
> which are garbage ≈7.97e74 where shv2's are correct**). **Slice D** (float const-fold; the guard's stated
> reason — "needs software IEEE-754" — is wrong: the real blocker is that shv2 has no `i64↔f64` **reinterpret**,
> which is ~1 instruction on each of 5 targets and also unblocks `stdlib/PrimitiveExtensions.maxon:32`).
> **Slice A′** (`Parser.maxon`: `i64-min-literal` — `parseIntLiteral` passes `negated: false` unconditionally at
> `:16233`, wrongly rejecting `-9223372036854775808`; and thread the literal TEXT into `integerOverflow` /
> `floatLiteralOverflow`, which carry only `(line, column)` so `Queries.maxon` cannot render the oracle's
> message — 5 corpus cases blocked on it, and the tempting shortcut of re-deriving the text from `(line,column)`
> at render time was correctly REFUSED as re-deriving a fact the throw site held).

> ### ✅ **`P1.7` Slice 3b (ii–v) — MANAGED opaque-`T` arrays, move-IN COMPLETE — CLOSED (2026-07-23, merged `e8b45b613`, rebased over upstream #40 + wasm-array-support → native 1374/0, wasm 1294/0). P1.7 stays ◑ — element MOVE-OUT (3b-vi) remains.**
> `Container with String` (and struct/union/nested-array elements) now constructs, pushes, forwards, and drops LEAK-FREE. Five sub-slices, each implemented → optimized → independently reviewed: **3b-ii** the `funcAbs64InRdata` reloc (a DATA→TEXT fixup on `GlobalDataTable.pendingRdataRelocs` mirroring `StringBufferFixup`; deletes the vestigial `RdataRelocKind.funcAbs64InRdata`/`FuncAbs64Slot` code-reloc scaffold — a NET alloc reduction since `RdataRelocKind` collapsed boxed-union→enum; PE/ELF bake `ImageBase+textRva+funcOffset`, arm64/macOS/wasm loudly `requireNoFuncAbs64Relocs` refuse). **3b-iii** concrete opaque-value ownership (element-destroy walk via the reloc'd `destroyFunc@40`; DCE-root the destructor in `noteDestructorUsage` since the rdata slot is its only reference; call-site consume via receiver-instance threading; move-out/read cleanly E2015-rejected as a future rung). **3b-iv** TRANSITIVE feed fixpoint — a param forwarded to a consuming self-sibling (`add`→`self.store(item)`→push) is a transitive feed, consumed at the concrete call site (closes a review-found forward double-free; mirrors `solveDescriptorNeedFixpoint`, shares `reverseReachabilityMark`). **3b-v** MOVE-TRACKING — a consumed opaque type-param feed param is ENROLLED OWNED in the shared body (scoped to instance methods), so the existing move machinery gives double-move→**E3102** (use-after-move; a type param is move-only, no `Copy` bound — so E3102 for ALL instantiations incl. trivial, by design) and conditional-move→a RUNTIME descriptor-gated drop `__drop_type_param` (`if destroyFunc@40 != 0: callIndirect`, inert for trivial). ⭐ **The review earned its keep twice** — the first pass found the forward double-free (→3b-iv), and 3b-iv's implementer found the double/conditional-move class (→3b-v); the final pass ran 14 adversarial ownership probes (loops→E2015, feed-to-two-siblings→E3102, struct/union-payload elements, the #40 retain-promotion interaction, conditional-pure-forwarder ordering) all clean. Rebase adopted main's wasm-correct `StdReturnType.void` in the shared `emitElementDestructorCall` (used by both the array walk and `__drop_type_param`). **Measured debt (Workstream O):** `replaceOrAppendParamConsume` is O(M²) in methods-per-generic-type — bounded, small-N, rare forwarder shape, NOT whole-program; re-measure if a generic type grows many transitive-forwarders. **⇒ NEXT = 3b-vi element MOVE-OUT (pop/remove return-value ownership; get borrow; slice/clone/append deep-clone — the return-value-ownership mechanism 3b-iii named).**
> ### ✅ **`P1.7` Slice 3b-i — opaque-`T` TRIVIAL-element arrays (the generics-depth substrate; NO reloc) — CLOSED (2026-07-23, merged `2dda47a3c`, rebased over the parallel match-dispatch + P1.1b/P1.0d.4 cluster → 1335/0, +1). P1.7 stays ◑ — 3b-ii (the reloc) remains.**
> User APPROVED building the deferred Slice 3b (2026-07-23); split into 3b-i (substrate) + 3b-ii (reloc). Three new mechanisms, all gated behind generic types: **(1) nested generic-instance typealias** — `readNestedRangedAlias`→`readNestedTypeAlias` returns a `NestedAliasResult` union (ranged | genericInstance); the generic form folds into `genericAliases` under its base-qualified name `Container.ElementArray`, and `parseTypeReference` resolves the bare inner alias to `genericInstance(gid)` in the real parse. **(2) opaque-`T` `Array`-typed field + drop cascade** — the field is `Array with typeParameter(0)`; the existing managed-field drop routes it to `__arr_decref` (trivial element → `element_destroy@40 == 0` → just frees the buffer). **(3) static/receiverless descriptor threading** — `FuncSignature.takesReceiver()` discriminates an instance-method receiver from a static constructor's RESULT gid; the shared body stamps the array's `element_destroy@40` from a RUN-TIME `loadIndirect(descriptor, +40)` (`LAYOUT_OFFSET_DESTROY_FUNC=40`, present as 0), so ONE compiled create serves every concrete instance (dictionary-passing, not monomorphization). A MANAGED opaque element is cleanly REJECTED (E2015 at construction, `genericInstanceHasManagedOpaqueArrayField`) until 3b-ii fills the slot. element_size for an opaque `T` is the literal 8 (a machine word). This 3-mechanism substrate is what **P1.7b `Set` / P2 `Map`/`Vector`/`List`** also need. **Optimizer: clean** — allocation-neutral (generic-gated, invisible to the generic-free scale corpus); every new helper confirmed linear. **Review found+FIXED 2 reachable compiler panics by PROBING** (the process working): (1) the descriptor-need seed's mirrored body-walk broke at the first `end` without skipping method bodies, so an opaque-array typealias declared textually AFTER a constructing method wasn't seeded → `X.create()` lowering-panicked; fixed by deleting the walk and reading opacity straight off `self.signatures.arrayElementIsOpaque` (the SAME folded index the lowering branches on — token-distance-independent, also resolves the duplication); (2) a receiverless static that constructs an opaque array but returns a non-`Self` type had no descriptor source → panic; now a clean E2015 (`returnsEnclosingType` guard, mirroring the receiverless-`sizeof` rejection). Review also single-sourced `signatureTakesSelf`/`signatureTakesReceiver` onto `FuncSignature.takesReceiver()`. **⇒ SPEC PORT: `generic-type-nested-array-typealias/basic-nested-array` flipped to `test:`; `string-element` (managed) stays disabled, cites 3b-ii.** **RESIDUALS (named, both PRE-EXISTING — not 3b-i defects):** (a) **nested-typealias FORWARD-REFERENCE** — a member referencing a nested typealias declared textually AFTER it fails with a clean E3011/E3005; PRE-EXISTING P1.6-C (a ranged inner alias fails identically), inherited, idiomatic ordering (typealias first) works; (b) **closure inside a generic type's method** → E3004 (a clean error), a PRE-EXISTING P1.5-closures × P1.6-generics interaction (reproduces on a zero-opaque-array `type W uses T` + a trivial closure) → a candidate for its own rung. **⇒ NEXT = P1.7 Slice 3b-ii (the `funcAbs64InRdata` reloc → `string-element` + managed opaque arrays).**
> ### ◑ **`P1.7` Slice 4 — Array `.slice`/`.append`/`.clone` (trivial elements) — CLOSED (2026-07-22, merged `3c65ccde2`, rebased over parallel maxon-sharp #90 → 1140 → 1162/0). ⭐ THIS IS THE LAST BUILDABLE P1.7 SLICE — P1.7 stays ◑ (the opaque-`T` reloc is deferred to a generics-depth rung; the borrow-on-get UAF is a user thesis decision).**
> `.slice(start, endIndex:)` (throwing, `end>length OR start>end`), `.clone()` (= `slice(0,length)`), `.append(other)` (reserve-once + block-copy, `other` preserved) — **EAGER INDEPENDENT COPIES — but this is an INTERIM. ⚠⚠ CORRECTION (user ruling 2026-07-22): COW IS WANTED — "slices should be nearly free". The earlier "NO COW is the shv2 departure" framing was WRONG and is retracted.** Eager-copy is behavior-CORRECT (the `slice-cow-modify-*` specs test only INDEPENDENCE, which disjoint buffers give) but O(n)/slice; **COW is an IMMEDIATE P1.7 RESIDUAL — build NEXT, not a future defer (user ruling 2026-07-22: "worked in as early as possible; a residual if it can be implemented immediately"). It CAN: mechanism A (refcount the COW-shared record) has NO deferred prerequisite** — shv2's MM header already carries the refcount slot + `__mm_decref` frees only the last owner; the only missing piece is `__mm_incref`, which `MmRuntime.maxon` explicitly anticipates re-adding "when there's a caller" — COW IS that caller (the sanctioned "refcount only where escape/sharing requires it"). Eager-copy stays as the correct fallback until COW lands, so P1.7 is ◑ (Slice 4 has this residual). The bootstrap's `capacity=-1/-2` view sentinels + `maxon_cow_check` (detach-on-write) are the reference to PORT this time. See the COW design + the A-vs-B fork in the residual entry. Shared `emitArrayRangeCopy` (slice+clone) + the one `emitForwardCopy` block-copy builder (also used by grow/remove). Optimizer: allocation-neutral (inert on the array-free corpus). **⭐ MANAGED slice/append/clone — ✅ CONCRETE now DONE (3b-vi-b-α, `3487ec063`→rebased):** the eager per-element deep-clone `__clone_<T>` family (the DUAL of `__destruct_<T>`: `__str_clone` reuses `promoteToOwnedString`'s fused-record path, `synthesizeStruct/UnionCloner` per concrete type recurse through nested managed fields) — the 5 managed `*-source-freed` cases are enabled and PASS (+5 extras). `requireTrivialArrayElementForCopy` → `requireArrayElementCopyable`. `append-preserves-originals` holds (deep-clone into a fresh owner, `src` untouched). OPAQUE-`T` managed copy inside a generic body remains **3b-vi-b-β** (descriptor `copyFunc@32` + reloc). See the P1.7 ladder row for the full record + the Workstream-O `noteClonerUsage` measured-debt note.
> ### 🔴 THE FINDS (independent review + a coordinator bounds-hardening pass): **(1) `__arr_append` CROSS-STRIDE byte over-read — the element_size 8-vs-1 residual biting through `.append`**: a word-array (`element_size=8`) `.append(b"…")` (byte-packed `element_size=1`) — same static type — blitted at the wrong stride (read "AB" as one word + past the rdata blob). FIXED: abort exit 72 on `dest.element_size != src.element_size` (a safe stopgap; the proper fix is the element_size unification / a widening append). **(2) SYSTEMIC negative-index OOB read/write** — every accessor bounds-checked only `index >= length` (signed), so a NEGATIVE index slipped through → `[10,20,30].get(-1)` returned 0 (garbage), `.set(-1)`/`.insert(-1)` wrote before the buffer. Pre-existing since Slice 1 (shv2's `StdCmpPred` has no unsigned compare). **The coordinator FIXED it before merge** (a live OOB read/write is a wrong-answer we own): single-homed `emitIsNegative`/`emitIndexOutOfRange` (`index<0 OR index>=length`) across get/set/remove/slice → throw; `insert` CLAMPS a negative index to 0 (matching `stdlib/Array.maxon`'s documented "clamps to [0,count]" — making it throw would fire E3057 on 20+ existing insert specs); 6 pinned `arrays.md` negative-index tests (1156→1162/0). **⇒ SPEC PORT (Slice 4):** `arrays.md` 14 trivial slice/append/COW + 6 negative-index; `array-clone-element-size.md` 2. **RESIDUALS (named):** managed slice/append/clone (deep-clone primitive → P1.8); byte-string↔word `.append` widening (the abort-72 makes the incoherent case safe; the real fix is element_size unification); ⚠ **`let`-receiver mutator asymmetry** — array mutators (`.push`/`.set`/`.append`) silently mutate a `let` array whereas `String.append` on a `let` is E3019 (pre-existing, uniform — a coordinator/user ruling). **⇒ P1.7's BUILDABLE work is COMPLETE (all four slices' trivial + concrete-managed cores landed → 1011→1162/0, +151). P1.7 stays ◑ for the two deferrals below.**
> ### ◑ **`P1.7` Slice 3a — CONCRETE managed-element arrays (THE CRUX, first half) — CLOSED (2026-07-22, merged `15ace5d01`, rebased over parallel P1.5 #68/#78 → 1100 → 1140/0). P1.7 still ◑ — Slice 3b (the reloc) + Slice 4 + residuals remain.**
> `Array with String`/struct/enum where the element type is CONCRETE (statically known). The element-DESTROY on array drop: `element_destroy@40` is stamped at the concrete construction site with **`funcAddr(__destruct_<Element>)`** (a runtime store into the heap record via the P1.5-A1 code→register path — **NO `funcAbs64InRdata` reloc**; that's for opaque-`T`, = Slice 3b), and `__arr_decref` runs a null-guarded per-slot walk calling that fn. ⭐ **THE SHV2 DEPARTURE FROM v1 IS REALIZED: the walk calls the element's DESTROY FUNCTION once per live slot, NOT v1's `&__mm_decref` refcount-net** (shv2 is move-only). Ownership: push/set/insert MOVE-in (consume, E3102 on reuse); get/first/last = element BORROW; pop/remove = OWNED move-out. Element_size = 8 (pointer) for managed. Optimizer found + fixed a REAL **O(files×types)→O(types)** superlinearity (`internArrayLiteralAggregateInstances` re-interned every declared type per aggregate-literal file — invisible to the array-free scale corpus, caught by reading).
> ### 🔴 THE FINDS (independent review — the process working, THREE this slice): **(1) a NEW reachable LEAK the green suite hid — the commit CLAIMED `set`/`clear`/`resize`-shrink destroy the vacated managed elements, but the destroy walk was wired ONLY into `__arr_decref`** (claim ≠ code = one-fact-written-twice); `a.set(0,s3)`/`a.clear()`/`a.resize(1)` all LEAKED (exit 101). FIXED: one shared `emitDestroyRangeIfManaged` gate across all 4 vacating ops (verified: no 101). **(2) the mandated fix — `f(x, x)` cross-arg DOUBLE-CONSUME silently double-freed (exit 101)** where the struct-literal guard rejects `Self{a:v,b:v}` with E3102 (the check existed for literals, not call args). FIXED: `transferConsumedArgs` shares `isRepeatedOwningMove` with `rejectDoubleOwningStore` (single-homed), precisely scoped to both-positions-CONSUME (two-borrow is fine); bespoke `moves.md` tests pin both. **(3) verified the 2 implementer pre-existing fixes SOUND** (borrowed-long-literal→owned promotion is a real heap copy; `self.arr.push`/`try self.arr.get` field-chains parse). **⇒ SPEC PORT (Slice 3a, ~29 B-cases):** stdlib-array(8 string-elem), array-managed-elements(3), array-enum-element-size, challenge-array-of-structs, struct-array-get-refcount(2), array-slots(6 struct), struct-enum-array-grow(managed), array-of-bytearray(1), + more. **RESIDUALS (named):** (a) ⭐ **borrow-on-get + `try arr.get(i) otherwise <owned>` = a BUMP-MASKED latent UAF** on the taken error path (the owned fallback drops on the error edge; a read then hits freed-but-bump-intact memory) — same class as the accepted P1.2 #40/#43 bump-masked UAFs. **The honest cure is a THESIS DECISION for the user: incref-on-get (contradicts static-ownership) OR copy-on-get OR E3070 borrow-liveness — deliberately absent in move-only.** Revisit at P1.8/when the allocator recycles. (b) **nested union payload** (union-in-union) / **enum-field `.rawValue` via field chain** / **enum-case match pattern** / **>6-param M5 ABI** — pre-existing mechanism gaps (union/match/ABI), each disabled with reason; (c) **array-push transitive-consume** (consume-by-use fixpoint doesn't treat array-push as consuming) + **E2013 param-field mutation** — the Slice-1 borrow divergence; (d) **E3070 borrow-across-mutation** (`array-realloc-dangling-ref`, 3) — no borrow-liveness infra, deferred. **⇒ NEXT = P1.7 Slice 3b (opaque-`T` managed elements — build the `funcAbs64InRdata` reloc + descriptor `destroyFunc@40`), then Slice 4 (slice/append/clone).**
> ### ◑ **`P1.7` Slice 2 — byte-string `b"…"` literals — CLOSED (2026-07-22, merged `82e8f35a9`, rebased over the parallel P1.5 value-merge cluster → 1082 → 1100/0). P1.7 still ◑ — Slices 3, 4 + residuals remain.**
> A `b"…"` literal lowers to an rdata-backed **`Array with Byte`**: 48-byte record, `buffer@0` → an immortal `.rdata` blob (Latin-1, `element_size@24 = 1` byte-packed), `capacity@16 = -2` (the immortal-rdata sentinel), `element_destroy@40 = 0`. It rides Slice 1's Array-with-Byte (gid, descriptor, accessors already existed). Codepoint > 0xFF ⇒ **E1004** at parse time (the **self-hosted oracle** behavior — the C# bootstrap silently Latin-1-transliterates and is NOT the oracle here; `byte-string-literal-codepoint-range.md` is `status: selfhosted`). E1004 (`LexerInvalidEscape`) was REUSED (a shv2 claim added to the existing csharp+selfhosted code — matching self-hosted's overload), so **maxon-sharp/selfhosted stayed untouched**. The escape decoder shares String's `escapeByte` table (one authority; only the loop wrapper + `\xNN`/`\uNNNN` + Latin-1 collapse fork — a justified, documented fork). Optimizer: **inert on the array-free scale corpus** + linear-by-reading audit (decode O(literal-len); blob registration reuses the amortized-O(1) `registerRdataConstant` cursor).
> ### 🔴 THE FINDS (independent review — the process working again): **TWO reachable bugs the green suite hid, BOTH FIXED before merge (both the "one fact written twice" shape):** (1) `try b"…".get(…)` was REJECTED — the try-able-literal-primary set had an array-literal arm but no byte-string arm (drifted); FIXED to mirror the array arm (verified). (2) **growing a byte-string LEAKED (exit 101) + freed `.rdata`** — `__arr_grow`'s old-buffer free lacked the `capacity<0` sentinel guard that `__arr_decref` got (`var a = b"hi"; a.push(88)` → 101 + `__mm_free` of a never-alloc'd rdata address); FIXED by gating the grow free-site on `capacity>=0` (verified: exit 0, no 101). **⇒ SPEC PORT (Slice 2):** `byte-string-literal.md` (12 C-cases), `byte-string-literal-codepoint-range.md` (3, E1004), `byte-type.md` (4 prereq). **RESIDUALS (named):** (a) **top-level `b"…"`** (`top-level-let`/`-var`/`dead-global`) — shv2 has NO top-level MANAGED constants (`let G="hi"` at module scope is E2004); this GENERALIZES the "module-level arrays" residual to "top-level managed const" ⇒ a named P1.7/P1.8 follow-up; (b) **bare `ByteArray`/`Array with Byte` type annotations** in a signature/struct-field don't parse (need a `typealias`) — pre-existing shv2 generic-annotation limit (E3011), not a byte-string defect; (c) **element_size 8-vs-1**: Slice-1 `ByteArray.create()` gives element_size **8** (word-slot) but a byte-string gives **1** (byte-packed) — INERT today (element_size is a runtime record field, each array internally consistent, unmixable until Slice 4 append; review confirmed correct reads on both) — DEFER unifying storage-width sizing to when it's observable (P1.8 `bytearray-element-size`). **⇒ NEXT = P1.7 Slice 3 (managed elements — THE CRUX: build the `funcAbs64InRdata` reloc path + descriptor `destroyFunc@40` + the per-slot element-DESTROY walk), then Slice 4 (slice/append/clone).**
> ### ◑ **`P1.7` IS IN PROGRESS — SLICED A/B/C/D. Slice 1 (Array SCALAR/TRIVIAL-ELEMENT CORE) CLOSED (2026-07-22, merged `94b4f667c`, 1011 → 1079/0). NOT COMPLETE — 3 slices + residuals remain.**
> ⭐ **shv2 SYNTHESIZES `Array` NATIVELY — it does NOT compile `stdlib/Array.maxon`** (shv2 never reads stdlib; the whole runtime is hand-built in `Runtime/*.maxon`, like String). `Array` = a builtin one-param generic (SignatureIndex) riding P1.6's `GenericInstanceRegistry`; its value = a pointer to a fused **48-byte** `__mm_alloc`'d backing record (`buffer@0/length@8/capacity@16/element_size@24/parent@32/element_destroy@40`, a DISTINCT record from String's — slot@40 is `element_destroy` not `isAscii`) pointing at a **separate** `__mm_alloc`'d element span (two allocs, like a detached String). **`element_size@24` is the FIRST runtime-polymorphic element size in shv2** — every helper reads it and moves exactly that many bytes via 5 shared byte-granular builders, so ONE helper set serves every element type. New `Compiler/Runtime/ArrayRuntime.maxon` (~975L): create/push/get/set/count/capacity/is_empty/reserve/resize/first/last/pop/clear/insert/remove/decref/grow + `__arr_grown_cap` (Go nextslicecap, ported VERBATIM from `stdlib/Array.maxon:65-95` WITH its scale-test-bend warning). Parser: `[…]` literal desugar, 16 array methods, field-chain receivers (`b.ops.push(1)`), `_ =` discard. `get/set/first/last/pop/remove` THROW (dual-register `tryCall`, `ArrayError.indexOutOfBounds`). **element_destroy@40 = 0 at this slice (TRIVIAL elements only — no element-walk, no reloc); the drop path is single-sited (`decrefCalleeFor`→`genericInstanceBoxDropCallee`→`__arr_decref`) with the "if element_destroy@40 != 0 walk-and-destroy else free" seam in place for Slice 3.** Optimizer: **allocation-neutral** (+0 allocs / +8 bytes flat = one `usesArray` bool; the implementer's flagged +29.. delta PROVEN a stale-baseline artifact — the parent binary reproduces it). Capacity-slot invariant (`[length,capacity)` reads zero) VERIFIED to hold — so Slice 3 is safe.
> ### 🔴 THE FINDS (independent review — the process working): **TWO reachable wrong-answers the green suite hid, BOTH FIXED before merge:** (1) a **throwing accessor without `try` silently swallowed the OOB error** (`let x = a.get(0)` returned dummy 0 where the oracle rejects) — the compiler-internal callee skip bypassed E3057; FIXED with an `isThrowingArrayRuntimeCallee` authority + E3057 (verified: `let x=a.get(0)`→E3057). (2) **integer overflow in `__arr_grow` → heap overflow** (`reserve(2^61)`+push wrapped `capacity*element_size` to 0, under-alloc'd, wrote OOB and RETURNED a value) — FIXED with a round-trip-division overflow guard → abort exit 71 (verified). Review also deduped the `lowerTryCall` runtime-call fork → shared `requireRuntimeCallPositional`. **⇒ SPEC PORT (Slice 1, category-A ~68 enabled):** `arrays`(25), `stdlib-array`(27), `collection`(5), `initablefromarrayliteral`(4), `array-slots`(1), `struct-enum-array-grow`(2), `byte-enum-comparison`(5). **RESIDUALS (named, not hidden):** (a) **module-level arrays** (`array-literal-with-dependency`, `get-empty-module-level-array`) — need const→rdata OR a `__module_init` shv2 lacks ⇒ a named P1.7 follow-up; (b) **value-position `try` on a field-chain array accessor** (`try bag.items.get(0) otherwise 0`) → clean E2015 (the non-throwing twin works; asymmetric) — parseTryCallTarget doesn't run the postfix loop after field access, fix touches ownership-under-`try` ⇒ P1.7 follow-up; (c) `basic-nested-array` (Array-over-a-type-PARAM in a generic body) ⇒ **Slice 3** (dictionary-passing element integration); (d) `field-assign-*` (mutating a PARAM's struct field) → pre-existing shv2 **E2013** borrow rule (a divergence from the bootstrap, NOT an Array bug); (e) `enum-coerces-as-return` (enum→byte RETURN coercion) — a separate coercion mechanism. **BOOTSTRAP-ORACLE BUG (worked around, for a separate follow-up):** maxon-sharp `StandardToX86Conversion` crashes **E9001** on an shv2 fn whose body is `return self.method(...)` (tail-return of a call result). **⇒ NEXT = P1.7 Slice 2 (byte-string `b"…"`), then Slice 3 (managed elements — THE CRUX: `funcAbs64InRdata` reloc + descriptor `destroyFunc@40` + element-destroy walk), then Slice 4 (slice/append/clone).**
> ### ✅ **`P1.5-A1` IS CLOSED (2026-07-20, merged, 792 → 813/0 on x64 + wasm) — NON-CAPTURING FUNCTION VALUES, front-to-back.**
> `let f = double`, function-typed `typealias`/params/fields, indirect calls, forward references, and the function-vs-int (E3005) / throwing-function-as-value (E3101) type discipline. **NO closure literals** (`function(…) gives` = A2). NEW IR ops: Maxon `functionRef`/`indirectCall`; Std `funcAddr`/`callIndirect`; x64 Target `leaRegFunc` (`.text` twin of `leaRegRdata`, via the existing `CallFixup` channel — no PeWriter change) + `callReg` (`FF /2`, clobber `0xFFFF0FC7` held equal to `callerSavedMask()` by `assertCallClobberConsistent`). **⭐ The scaffold named this rung throughout** (`leaFuncAddr`, `arm64BranchLinkReg`, the `closureProducer`/`awaiting` op-category bands, StdOpMeta's `indirectCall` note, E3101's registry doc) — "grep for your rung's name" paid again. Function type is NAME-based (`MaxonType.function(id)` on the `(tag,nameId)` column — no interned FunctionTypeRegistry); forward refs resolve at PARSE time via the complete whole-program sweep (no TypeResolution rewrite — honors "extend the ONE sweep"). E3005 single-homed in `checkDeclaredType`→`functionIntoNonFunction` (`TypeRules.maxon:222`). Ported `first-class-functions.md` (21 of 49 enabled; the rest = closure literals→A2, `field-void-statement`→Array/P1.7, `cross-file-extension-typealias-param`→generics/P1.6). ⚠ Two `##` prose sub-headings in the Tests region were demoted to `###` (shv2's `SpecParser` bounds the region at the next `##` — the `## Deferred` convention; NOT a workaround).
> ### ⭐ wasm IMPLEMENTED (user directive 2026-07-20, not deferred): a funcref TABLE (section 4) + active ELEMENT segment (section 9) + `call_indirect` — `funcAddr`→`i64.const <funcIdx>` (table index == funcIdx), `callIndirect`→`i32.wrap` + `call_indirect (type $sig) (table 0)`. The `call_indirect` return-type is derived wasm-locally (every A1 fn value is `(i64ⁿ)→i64`, so the sig is an arg-count-keyed pre-interned functype — NO cross-tier op change). Full wasm suite 779→**800/0** under wasmtime. **⚠ arm64 DEFERRED** — `arm64EmitLeaFuncAddr` panics (the `.text`-function ADRP+ADD relocation channel was OMITTED per `CodeResult.maxon`, "grow those fields back at the arm64 milestone"); cases restricted `x64-windows, wasm32-wasi`; **OPEN #67** wires it (shared with A2). Optimizer: LINEAR (per-file signature registry + forward-ref resolution both x2.0 on the doubling ladder, +2,494 allocs/rung). Independent review: **MERGE, no harmful duplication, no latent bug in the enabled slice** (probed 6-arg indirect call / callee-reg liveness, E2004-vs-functionRef, E3101 at all routes, E3005 at all coercion sites, wasm i64 narrowing); fixed a "fully wired" arm64 comment lie (`cc8b154cc`). ⏭ Follow-ups filed: **OPEN #68** (statement-position indirect calls / void fn types uncallable), **#69** (2 unpinned E3005 routing sites → 815/0), **#70** (bootstrap match-arm managed-Array-tail-field miscompile). **⇒ NEXT = P1.5-A2 (capturing closures + the Own-tier EscapeAnalysis).** [[project_p15a1_contract]]
>
> ### ⚠ WAVE A — worth carrying (a big slice; the process caught the finds):
> - **The plan was WRONG at the code, TWICE, both forced+implemented:** (1) `buffer@0` is a data→data pointer
>   the fixed-base image has NO base relocation for, so a new **`PeWriter.patchStringBufferFixups`** bakes
>   `ImageBase + rdataRva + blobOffset` after RVAs are known; (2) the spec runner had **no `stdout` fence**
>   (OPEN #2's live warning) — `print` could not be gated without adding one (faking a golden is forbidden).
> - **⭐ The optimizer found a REAL O(N²)** the memory-only instrument is blind to (compile-TIME, allocates
>   nothing): `registerRdataConstant` re-summed all prior rdata payloads per registration; each literal
>   registers TWO constants and literals-per-program are **unbounded** → quadratic at self-host scale. Fixed to
>   O(N) with a running total, offsets bit-identical (verified: 9 interleaved literals print byte-correct).
> - **⭐ The independent review found a SILENT MISCOMPILE this rung introduced** (the self-review missed it,
>   again): the `__` predicate was widened from `__mm_`-only to **any `__` prefix** (needed — `__print_string`/
>   `__str_eq` have no signature), but shv2 never emitted **E2051**, so a USER `function __add(a,b)` called as
>   `__add(7)` now bypasses arity validation and runs with an uninitialised arg (exit 65543) where the
>   bootstrap rejects it. **Reserved-namespace only — NO in-language program is affected, no corpus case** — so
>   deferred (a surgical narrow hits a circular `MmRuntime`↔`StringRuntime` dep; the clean fix is E2051, a
>   6-site slice). Comment corrected to be honest. ⇒ **OPEN.md #35 — the IMMEDIATE P1.2 follow-up.**
> - The backend breadth (50 files, the register allocator etc.) is justified: band-append of the new
>   `leaRegRdata` `TargetOp` + `WriteFile`'s stack-arg/out-param. `print` is the first call needing a stack arg.
>
> ### ⚠ WAVE B — four things worth carrying (the process caught them; nothing shipped wrong):
> - **A trailing enum-arm `and fallthrough` panicked the backend** (unterminated block) — found in the
>   implementer's SELF-review, fixed, regression spec added. Same class as Wave A's crash: *a backend panic on
>   a valid program*. **Every wave this rung has surfaced one; the pattern is real.**
> - **E3034 said "unknown ENUM case" for a UNION** — the noun was hardcoded where E2026/E2046 derive it from
>   `EnumLayout.kindWord()`. ONE FACT WRITTEN THREE WAYS, one disagreeing (the signature disease). Found by the
>   INDEPENDENT review (not the self-review), threaded through `kindWord`, pinned by `error.unknown-union-case`.
>   **462→508 became 507→508.** The self-review is the pass that MISSES things; the independent one is why.
> - **The implementer DEFERRED `enum-method` against the brief** (I listed it enable; it needs instance methods
>   on an ENUM receiver — `match self` over an enum — a distinct mechanism). shv2 rejects it with a clean
>   positioned E2015. **Correct call: the plan was wrong at the code, and it reported rather than shipping it
>   broken.** ⇒ instance-methods-on-enums wants a rung; see OPEN.md #26's overloading gap for the shape.
> - **A flagged "wrong-accept" was a FALSE ALARM** — the implementer thought `Color.red == Color.green` should
>   be E3066, but the ORACLE accepts it too (E3066 is for *payload* unions). Measured before recording: shv2
>   matches the oracle. **Probe the oracle before believing a divergence.** ⇒ two real follow-ups: **OPEN.md #33**
>   (negative-FLOAT enum raw values reject with a misleading E2010 where the bootstrap accepts — a bounded clean
>   reject) and **#34** (union E2026/E2046 wording — shv2 says "union" consistently; the bootstrap hardcodes
>   "enum"; no golden pins either).
>
> **The rung was three deliberately-DROPPED facts acquiring their consumers**, and `readStructFieldInto`'s
> own comment said so before it was written: *"`let` AND `var` ARE READ IDENTICALLY AND THE DISTINCTION IS
> DROPPED… that is P1.1a wave 2… Recording the bit now would be a `StructLayout` column nothing reads."*
> **E3086's user-visible message already promised the mechanism too** — *"and it has no default value"*.
> ⇒ **The rule keeps paying: GREP FOR YOUR RUNG'S NAME FIRST.** Wave 1 left three notes and all three were right.
>
> **What it DECLINED to copy, and why the decline is the point.** v1 desugars each default into a synthetic
> `__field_init_*` nullary function and splices `call + fieldStore(isInit: true)` in a post-TypeResolution
> pass. **Its own comment gives the reason, and the reason does not hold here:** *"the parser is pure-record
> and can't see the type's field list."* **shv2's parser CAN** — `queryProgramSignatures` is a whole-program
> pass that runs before any body parses, which is why `requireAllFieldsInitialized` already reads it. So the
> `method-before-field` ordering hazard that forces v1's whole architecture is **structurally absent**, and
> the copy had no reason. Defaults fold at the declaration (type from the literal's TOKEN KIND — v1's
> `inferShorthandFieldType` shape, no const-evaluator) and materialize in the `Self{}` loop that already
> walks the layout. `isInit` was left behind for the same test: shv2's construction store is a different
> code path from the assignment statement, so the flag would have had **no consumer**.
>
> ### 🔴 THE FIND: **`StdOpMeta.isPure` HAS ZERO READERS, and SIX places called it a live correctness constraint**
> **The THIRD instance of "a gate reported PASS while structurally blind" (#4c, #23), and a SABOTAGE found it
> again.** `loadIndirect` flipped to `isPure: true` — the exact edit six sites call a silent wrong answer —
> and the suite reads **371/0**, including P1.0d.5b's own `global-load-not-hoisted`. **Nothing hoists**
> (there is no LICM/CSE/inliner), so the property holds for a reason nobody had written down, while the
> reason everybody HAD written down enforces nothing. **P1.0d.5b's row below said "pinned by a spec" — it was
> not; that claim is now corrected in place.** `isPure` is KEPT (unlike #7's `setsFlags`, which was a *second*
> home for a live fact — this is the SOLE declaration and its reader is scheduled), and **six of `StdOpMeta`'s
> nine fields have no reader at all.** ⇒ **OPEN.md #27.**
>
> ### ⚠ AND THE RUNG WROTE ITS OWN SUBJECT TWICE — caught by the independent review, not the author
> `if not binding.mutable → throw assignToImmutable` landed at **two** sites with nothing making them agree.
> A rung *about* one-fact-written-twice committed it, in the fact it was adding. Now one
> `requireMutableBinding`; `binding.mutable` is read at **exactly one site** — against the bootstrap's 3
> readers / 4 message copies and v1's 6. **This is why the review is independent and why it runs LAST.**
>
> ### ⚠ KNOWN GAPS, named not hidden
> - **`struct-field-default` stays `disabled-test:` and its reason is CORRECTED — it needs FUNCTION
>   OVERLOADING** (two `Counter.create` arities ⇒ **E3006**), not defaults. **Overloading is on NO rung of
>   this ladder** though the stdlib has overload sets (§487's "env-map overload"). ⇒ **OPEN.md #26**; it needs
>   a number, and that is a PLAN decision. Field defaults are covered instead by `field-initialization.md`'s
>   `all-defaults` / `literal-overrides-default` / `mixed-default-and-literal` — real corpus cases, all green.
> - **`as Type = <non-literal>` is deferred and rejected LOUDLY (E2015 naming the gap).** It needs token
>   capture + replay at each `Self{}` site and has **ZERO reachable consumers** — every corpus case wanting it
>   also wants Array (P1.7), String (P1.2) or a struct-typed field. Building it now is P1.0r's error #2 again.
>   *(⚠ The contract claimed the oracle rejects `= 2 + 3` outright. **FALSE, and measured by the implementer:**
>   `as Integer = 2 + 3` ⇒ 5 and `as Integer = pick()` ⇒ 4. The grammar is ASYMMETRIC — un-annotated takes one
>   literal, annotated takes any expression. The deferral survived; its stated reason did not.)*
> - **The corpus cannot see this rung's cost dimension.** `structTypeDecl` emits exactly **two** fields per
>   generated type while the ladder doubles the NUMBER of types, so `indexOfField`'s O(F²) lives in a
>   dimension `scale-test` structurally cannot measure — F had to be measured against real source instead
>   (**max 22, mean 4.14 over 166 declarations** ⇒ linear in program size; DEBT, not a bug). ⇒ **OPEN.md #28.**
>
> ⚠⚠ **READ THIS BEFORE THE NEXT RUNG — the rung was RE-SLICED TWICE and the CONTRACT was wrong FOUR times,
> every one the SAME error: SOMETHING SHIPPED WITHOUT A CONSUMER.**
> - The plan said structs before the heap. **A struct IS a heap value** (measured: aliasing ⇒ 95 not 6;
>   `sizeof(Outer{p Point, n Integer})` ⇒ **16** not 24 ⇒ a struct field is a POINTER). The dependency was
>   inverted, and *"String is the FIRST heap value"* was false.
> - Then `P1.0r.1 = the vocabulary` — **all four items had NO consumer**, so it was `mintPhi` one step
>   earlier. Slice by **CONSUMER, never by layer.**
> - Then the contract's golden emitted `funcAddr __destruct_Point` while its **own footnote** said a
>   trivial struct passes `const 0` — **the footnote deleted the golden's only consumer.** The
>   coordinator committed the exact error it had warned the implementer about **one paragraph earlier**.
>   ⇒ `funcAddr`/`callIndirect` **deferred to P1.2**, where `__destruct_String` is a real consumer.
> - Then the contract mandated **`__mm_incref`, which the review found had ZERO call sites** — emitted into
>   every binary, never executed. **Deleted.** Its call site is decided by the borrow-vs-consume ruling that
>   does not exist yet: **it had no correct call site to be missing.**
>
> ⭐ **What made the absence CORRECT rather than merely unused:** the refcount word is written by **NOTHING**
> — *the slab's zeroing contract is its only initializer* — so with no incref anywhere, `rc` is **provably 0
> at every drop**. The zeroing contract is load-bearing for the refcount model itself.
>
> ### 🔴 THE FIND: `iatCall` SAID "full call-barrier semantics" AND CLOBBERED NOTHING
> `TargetDialect.iatCall` carried **`implicitDefs: 0`** under a comment reading *"Full call-barrier
> semantics."* **ONE FACT WRITTEN TWICE, DISAGREEING** — in the op metadata. Inert while the only `iatCall`
> was `mrt_start`'s hand-built `ExitProcess` (nothing live across it); **P1.0r put one inside a
> register-allocated function and it went live the same day.** `__slab_alloc` published `base+base` instead
> of `base+size` ⇒ `next > end` forever ⇒ the bump path never runs again ⇒ **one 64 KiB VirtualAlloc per
> object. 133.74 MB for 2,000 Points → 412 KB fixed.**
> ⚠⚠ **It never produced a wrong USER-VISIBLE answer — which is why `specs-shv2` 357/0, the worker-invariance
> gate, AND `scale-test` were ALL GREEN OVER IT. Only reading the emitted machine code found it.**
> ### ✅ **AND THE FIX MADE IT UNREPRESENTABLE — the part to COPY, not the mask** (verified 2026-07-16 by
> trying to put the bug back: `implicitDefs: 0` now compiles **NOTHING**). `assertCallClobberConsistent`
> compares the op metadata against `RegisterAllocator.callerSavedMask` on **every `allocateRegisters`** and
> panics naming the drift. ⇒ **For ONE FACT WRITTEN TWICE, make the two copies CHECK EACH OTHER. That beats
> every gate, because a gate can be pointed at the wrong program and a check cannot.**
> *(The corpus blind spot the entry blamed is ALSO closed now — 2026-07-16, five knobs; structs, the heap,
> globals and the idiv path are all generated. But it was **never what would have caught this**: `scale-test`
> COMPILES each rung and never RUNS it, so shv2's **emitted** runtime is measured by nothing. See OPEN.md
> #23 / #4d.)*
> *(Also fixed: a void function falling off the end never dropped ⇒ **101 on a valid program** — the drop hung
> only off `parseReturnStatement`; and a diagnostic hardcoded `float` under a comment calling it "the only tag
> that can trip this rule" — **a theorem P1.0r falsified in the same commit, without touching the line it
> falsified.**)*
>
> ### ⚠ KNOWN GAPS, named not hidden
> - **A struct declared in a NESTED BLOCK and not returned LEAKS** (verified: a valid program returning 7
>   gives **101**). `closeBlock` emits no drop **deliberately**: a drop there cannot be correct without
>   deciding whether `q = p` retains, and that IS the P1.4 ruling. Wrong here is a **use-after-free**; the gap
>   is a **loud 101**. Fail-safe over untested — reviewed and agreed.
> - **`struct-param` · `struct-return` · `struct-literal-as-arg` stay `disabled-test:` — P1.4, USER RULING
>   2026-07-15.** A struct crossing a call boundary as a PARAMETER forces the **borrow-vs-consume** choice,
>   and **the two references genuinely disagree**: the bootstrap **BORROWS + retains-on-store** (*"on a throw
>   nothing was ever retained, so nothing is owed"*); v1 **CONSUMES** — which is documented as what MADE its
>   member-alias leak. `ARCHITECTURE.md:453`'s `OwnershipKind` lattice expresses both and is *"present but
>   inert."* ⭐ **The leak gate is what made this observable a rung EARLY** — that is the gate working.
> - **`__mm_decref`'s `rc-1` arm is unreachable** (nothing can make `rc` nonzero). **Kept, deliberately**:
>   deleting it collapses decref into a thin wrapper over `__mm_free` and unwinds the 24-byte header. The
>   distinction from `__mm_incref`: incref was never *asked* anything; decref's *"am I the last owner?"* is
>   asked and answered on every drop — the "no" arm is unreachable by the **program set**, not speculative.
> - **Cross-file struct id-remap**: written, sabotage-probed by the reviewer (fires at 2 files, correct — 57),
>   but **no spec case covers it** because the corpus port's enabled set is under user ruling.
>
> ⚠ **Read P1.0d.4's ledger rows before starting the next rung** — the contract was wrong TEN times, nine of
> them ONE FACT WRITTEN DOWN TWICE, and the rung shipped two of its OWN features (float negation,
> float phis) with zero coverage until the corpus and the instrument were made to look. **Step 0 is
> the SPEC PORT LIST, and it is the coordinator's.**

> ## ⭐⭐ RESTRUCTURED 2026-07-15 — **P1.1 COULD NOT BE BUILT AS WRITTEN. A STRUCT IS A HEAP VALUE.**
>
> **The old ladder said `P1.1` structs ("concrete, trivial-ownership only") came BEFORE `P1.2`'s heap, and
> that `String` is "the FIRST heap value". Both are FALSE, and they were falsified by MEASURING the
> bootstrap** — the oracle — not by reading it:
>
> | Probe | Result | What it proves |
> |---|---|---|
> | `mutate(p)` writes `q.x = 90`, caller reads `p.x` | **95** (=90+5), not 6 | a struct **ALIASES** — reference semantics, not value |
> | `sizeof(Outer)`, `Outer{p Point, n Integer}` | **16**, not 24 | a struct-typed field is an **8-byte POINTER**, never inlined |
> | `var q = p` where `p` is `let` | **E3078** *"…or use `clone()`"* | the language **POLICES** struct aliasing — it knows |
> | both reference compilers | `IsHeapAllocated => true` **unconditionally**; v1 `__mm_alloc` + `freshRc0` | they AGREE, with no exception for scalar fields |
>
> ⇒ **A struct needs `__mm_alloc` ⇒ it needs Workstream R1 ⇒ which the plan scheduled AFTER it.** The
> dependency was inverted. **And `String` is NOT the first heap value — a scalar struct is, and it is a
> far SIMPLER one**: no rdata `capacity = -2` sentinel, no interning, no 40-byte `__ManagedMemory`
> record, no interpolation memcpy chain, no `BuiltinStringLiteral` marker-interface discovery. A struct
> is `__mm_alloc(size, null)` + `loadIndirect`/`storeIndirect` at an offset. **That is the whole thing.**
>
> ⚠ **The stack/value-struct escape hatch is CLOSED, on evidence rather than taste**: it returns **6
> instead of 95** and **24 instead of 16** — wrong answers on cases already in the port list — and
> nested structs force the heap regardless (an 8-byte field slot cannot inline a 16-byte `Point`). It is
> also the retrofit the PRINCIPLE forbids. **v1 independently confirms the direction**: *"Plain structs
> used to leak… conflating drop-tracked vs heap-managed is what leaked every plain heap struct (it was
> allocated untracked via `mrt_alloc`)"* — **v1 TRIED the cheap non-refcounted struct path, and it leaked.**
>
> **⇒ THE ORDER IS NOW: heap → structs → enums+`match` → `String`** (user ruling, 2026-07-15).
>
> ### ⚠ WHY THE NUMBERS DID NOT CHANGE — and this is load-bearing
> **The rungs are NOT renumbered, deliberately.** `specs-shv2/` carries **153 `disabled-test:` reason
> markers that cite a rung BY NUMBER** (41 × `P1.2`, 33 × `P1.9`, 24 × `P1.1`, 21 × `P1.7`, 12 × `P1.4`,
> 9 × `P1.8`) — and §"the disabled-test reasons ARE the ranked roadmap" is what makes them the roadmap.
> **Renumbering would silently re-point 77+ of them at the wrong rung** — the project's own signature bug
> (ONE FACT WRITTEN DOWN TWICE) at the ladder level, and the markers would go on looking right.
> ⇒ The allocator takes a **NEW** number in the established pre-P1.1 slice convention (`P1.0a`…`P1.0d`):
> **`P1.0r`** — `r` for Workstream **R**. `P1.1` splits into **`P1.1a` structs** / **`P1.1b` enums+`match`**,
> both of which its 24 markers already read correctly. **`P1.2` KEEPS ITS NUMBER** and keeps meaning
> `String`; it merely stops being *first*. Every existing marker still resolves.

> **Note the two senses of "shv2 runs its spec tests."** shv2 *already* has a `spec-test`
> command (371/0) — but that harness is compiled by **`maxon.exe`**, the C# bootstrap. Phase 1
> is the other sense, the one `maxon-selfhosted` has and shv2 does not: **shv2 compiles the
> harness itself.** That is the whole difference, and it is the entire mechanism ladder.

**The goal:** implement every hard mechanism of Maxon *minimally*, so the compiler can be built
and validated with fast iterations.

### The honest sizing

Self-host is **~5–20k lines of new Maxon** away (from **44,671** today — measured 2026-07-16; the
`21,038` this line used to cite was 2× stale, see "The honest sizing"), and "Maxon-core" is
**~75–80% of the language**, not 25% — it excludes only async, general `Iterable`/associated
types, and higher-order/sort. **That is exactly why Phase 1 exists.**

**Two items on the path are unbudgeted in the original plan documents:**

1. **The runtime shv2 must *emit*** (~5–7k lines of hand-assembled machine code). A
   shv2-compiled binary has to *run* — so shv2's backend must emit the slab allocator,
   refcounting, `__destruct` cascades, and `__ManagedMemory`. The original plan *scheduled*
   this but budgeted zero lines for it. Here it is **Workstream R**, sliced into Phase 1
   (**R1 @ P1.2** — the first milestone that cannot even be *gated* without it).
2. **The runtime-binding decision.** v1 binds its runtime by *compiling* `stdlib/Internals.maxon`
   (3,862) through a file-path-gated `__Internals` intrinsic + `StdlibLoader.maxon` (2,572). The
   C# bootstrap instead *excludes* `Internals.maxon`
   ([0-Compiler.cs:879](maxon-sharp/Compiler/0-Compiler.cs#L879)) and emits its runtime natively.
   **Decision: shv2 follows the C# bootstrap** — exclude `Internals.maxon`, emit natively (that
   *is* Workstream R), register the `__Managed*` builtin surface. v1's 6,434 combined lines are
   the road not taken, not a budget line.

**Explicitly deferred: the stdlib cache.** v1's is ~6k lines (`.mxc`, format v89) and its
version history — **v81→v89, every bump a soundness bug** in id-space stability or hash coverage
— is a warning label. Revisited *after* the compiler works, approach chosen fresh. One cheap
hedge meanwhile: keep every id-producing table (`TypeNameInterner.foldInto`/`TypeNameRemap`)
serializable-in-order, so a future cache has a stable id space to build on rather than a retrofit.

---

## Locked decisions

| | Choice |
|---|---|
| **Generics** | **Dictionary-passing + 64-byte layout descriptors + witness tables** — v1's design. (Rejected: monomorphization. It contaminates 3 files instead of 20, but trades code size and compile time — exactly the resources the ≤30 s / ≤1.7 GB budget exists to conserve.) |
| **String** | **A BUILTIN, at P1.2** — gated on the RUNTIME, *not* on generics. It is the FIRST heap value. v1 shipped String in Phase 7; generics in Phase 11. |
| **Closures** | **IN core** — co-landed with escape analysis at **P1.5**, because closure capture *is* the canonical escape. The `LazyMessage` sites are kept as dogfood. |
| **`async` / green threads** | **IN core, at P1.5** *(reversed 2026-07-13 — was "Beyond")*. Parallel spec execution is part of the Phase-1 goal, and a green-thread capture **IS** an escape — so it co-lands with closures + escape, never after. Brings **Workstream R3 (the GT scheduler) into Phase 1.** Its dogfood + acceptance test is the parallel harness; it also un-blocks **P2.6** per-function fan-out, which was always going to need it. **⭐ AND: give `Promise` the ERROR TYPE — see below. Do not port the bootstrap's shape.** |
| **Frontend / pre-scan** | **The parser stays a PURE PER-FILE function of its own tokens.** Whole-program facts come from the ONE `queryProgramSignatures` sweep — extended by **arms**, never by a second pre-pass or an inline `self.signatures.*` shadow-resolver that keeps a second copy of `TypeResolution`'s logic. Sweep + parse both **fan out at P2.6** (parallel extract → path-ordered serial merge → parallel parse, reusing the sweep's cached tokens). *This is the antidote to v1's disease — v1 kept ADDING pre-passes for missing info and slowed every time; shv2 has ONE, and keeping it one is a decision, not an accident.* See **⭐ Frontend parallelism** below. |
| **Conditional conformance** | **IN core** — *declared* in Phase 1 (the stdlib forces it), *emitted* at **P2.2**. It is a hard mechanism, and this plan implements hard mechanisms rather than dodging them. See the PRINCIPLE. |
| **`Map` / `Set`** | multi-param generics + one `Hashable` constraint. No `Iterable`-on-Map, no tuple `Entry`. **P2.3.** `Set` rides Map's exact mechanism. |
| **Iteration** | **Hardcode `for-in`** over Array/Range/String. No general `Iterable`/associated types. |
| **Errors** | v1's design **verbatim**: dual-register `(value, errorFlag)`. No unwind tables. Already minimal. |
| **Stdlib lowering** | **Reachability-seeded — lower only the stdlib bodies the program transitively reaches.** *Not an optimization: it is what makes Phase 1 a phase.* See §"The stdlib cone." |
| **Stdlib fork** | `stdlib-shv2/stdlib/` — ❌ **NOT NEEDED. Re-deferred 2026-07-13 on measurement (P1.0c).** It was un-deferred as the backstop for `Map`, *"if reachability-seeded lowering proves insufficient."* **It proved sufficient** — `Map` is laid out and never codegen'd. The one edge it *could* have cut (`String.trim()` → `CharacterSet` → `Set`) we chose NOT to cut: do the hard things early ⇒ `Set` is in Phase 1 (P1.7b). **So the fork now gates nothing, and its original ~1 s justification is still dead.** Do not resurrect it without a NEW reason. |
| **Targets** | ~~x64-windows only through Phase 2.~~ **SUPERSEDED — arm64-macOS pulled forward (user ruling 2026-07-17), parallel to Phase 1.** The arm64-macOS **scalar-core backend LANDED 2026-07-18** (`85237f70d`): shv2 emits running, ad-hoc-signed Mach-O, and `specs-shv2` runs GREEN natively on arm64 (**497/0**) via new per-target `<!-- targets: -->` gating; ~150 M2+ (heap/String/struct/union/print) tests are gated to x64-windows until the arm64 **runtime floor** (the next arm64 rung). Neutral register model — one allocator, both ISAs; x64 codegen byte-identical. **wasm32-wasi SCALAR-CORE backend LANDED 2026-07-18 (`9a6c6b45d`), reversing the "wasm Beyond" lock — user-authorized.** A **WASI Preview2 COMPONENT** backend, sibling of x64/arm64: it branches off `buildBackend` and consumes the **Std module directly** (no register allocator, no Target dialect — the `wasm32` panics in RegisterAllocator/RegBits/SsaDestruction/TargetPrinter stay dead-but-exhaustive), maps every SSA ValueId to a wasm local, and reconstructs control flow with a **`loop`+`br_table` state-machine dispatcher** (no relooper). Emits a core module (`WasmBinary`/`StdToWasm`), wrapped into a component via the vendored `wasm-tools` (`embed`+`new`, `WasmComponent`), run under the vendored `wasmtime` with `-S cli-exit-with-code=y`. The spec runner gained a **cross-compile-and-run path** (`--target=wasm32-wasi` builds + runs under wasmtime) the per-target `<!-- targets: -->` gating (originally an additive per-target opt-in; **REPLACED 2026-07-19 (`588d82f3a`) by the UNIFORM rule — every test runs on EVERY target unless it declares a restriction, used only for a technical reason** — see the sweep note below). **93/0 exit-code-checked** across the ~16 Tier-1 scalar files; then **FLOATS (`f64`) landed 2026-07-18 (`ab462f0dd`)** — native `f64`, NO runtime / NO data section (f64 ALU/compare + native `f64.const` + `siToFp`/`fpToSi` conversions), un-gating float-type + float-compare-branch: **106/0**. Then the **MEMORY BAND landed 2026-07-18 (`4c8a8f297`)** — top-level `var` globals via a linear-memory active **data segment** (`globalAddr`→`i32.const <base+slot>`, `loadIndirect`/`storeIndirect`→`iN.load`/`store` with a memarg; **addresses are i32**, data base 1024 as a null guard; `GlobalDataTable` threaded through `buildWasmBackend`), un-gating global-load-not-hoisted + top-level-let + the 6 NaN-via-globals float tests: **128/0**. Then **HEAP / String / `print` LANDED 2026-07-18 (`674e104b6`) in two phases** — Phase A (`91e108c45`): the shared pointer-valtype fix (coerce every load/store address to **i32 at the access site** — this RESOLVED the prerequisite that used to sit here), `rdataAddr` + a second `.rdata` data segment with String-record buffer-pointer **relocation**, and `osWriteStdout` → WASI Preview2 stdout (`get-stdout` + `blocking-write-and-flush`); Phase B (`149a9ff7f`): `osAllocPages` → `memory.grow`, the **byte-load band** (`i32.load8_u`/`store8`), and the **leak gate** (`$run` calls `__mm_leak_check`, ported from the native entry stub, verified firing on an induced leak). **The allocator, refcount, and String builders all emit for free via the Std-IR function path** — so structs-with-String-fields + unions-with-managed-payloads came with it, drops and all. Un-gated heap Strings / interpolation / append / equality / drops / structs / unions: **228/0**. Mostly PORTED from v1's `MirToWasm.maxon` (user direction — the wasm emitter is a mechanical translator hitting the identical WASI surface, the one place a near-verbatim v1 port is right). ⚠ Width coercion (i64↔i32) at every value boundary — ret, call-arg, phi-edge, binOp/cmp/div-mod/unary operands — recovers the widths the register backends leave implicit in 64-bit registers (four such gaps found by integration + review, all reproduced + fixed + regression-tested). Then **ERROR HANDLING landed 2026-07-19 (`634f957fb`)** — `throws`/`try`/`otherwise`/`throw`/`panic` via wasm **MULTI-VALUE returns**: a throwing function's signature becomes `(params) → (resultType, i64)` (the native dual-register R8/R10 ABI mapped to a 2-result wasm function — verified surviving the component pipeline), `tryCall` captures both results (flag on top), `errorReturn` pushes both, `osExit` → `exit-with-code`; managed/boxed-union error payloads verified, leak-clean. ⭐ **COVERAGE SWEEP + GATING FLIP (`934154310`, `588d82f3a`, user-directed 2026-07-19):** the per-slice marking had only marked each implementer's own files, leaving ~450 already-passing tests unmarked — so the gating rule was flipped from per-target opt-in to **UNIFORM (all targets run every test unless RESTRICTED for a technical reason)**, all `wasm32-wasi` markers deleted, and only the genuine exceptions restricted (ISA-specific register-allocation tests; `RequiredData` tests that read a native `.data` section a wasm module has no equivalent of). **wasm now runs 732/0 by default** — the host's full runnable set minus those technical restrictions. Then **`Array` (P1.7) LANDED ON WASM 2026-07-23** (this parity session), reversing the "Arrays Beyond wasm" lock — two fixes of ONE class (a type inconsistency invisible on native but caught by wasm's strict `call_indirect` type check): (1) the throwing array accessors (`get/set/first/last/pop/remove/slice`) emitted a 1-result wasm signature against a 2-result (value + error-flag) throwing body/call-site → **139 invalid modules** — fixed by stamping their `throwsTypeName` (`funcSignatureResultValTypes` then adds the flag result); (2) the per-element destructor `callIndirect` in `emitElementDestroyRange` declared an i64 result against a genuinely VOID destructor (`__str_decref`/`__destruct_<T>`/`__mm_decref`/`__arr_decref`) → **28 `call_indirect` type-mismatch traps** (exit 134) — fixed by widening `StdOp.callIndirect`'s `resultType` from `StdType` to `StdReturnType` (`void|value`), the void arm interning ZERO results. **wasm 1132/144 → 1291/0**, native byte-neutral (`fragments/` untouched — the value path is unchanged and the void path lives only in golden-excluded `__arr_` functions), drift guard ties the throwing-accessor set to `isThrowingArrayRuntimeCallee`. Concrete + concrete-managed (struct/string/enum/bytearray) element arrays now run on wasm exactly as on native; only the opaque-`T` managed array (P1.7 3b-ii's reloc) stays Beyond, and it is Beyond on EVERY target, not a wasm gap. **STILL BEYOND (later wasm slices): `async` → asyncify (green threads), the narrow-integer cast widths (`i8/i16/i32/u16/u32/u64` load/store — gated language-wide on P1.9's `as`-cast, so the wasm width door panics loudly for now, mirroring native `accessWidthFor`), and a real relooper for codegen quality (the `br_table` dispatcher is correct but verbose). The HEAP-PREREQUISITE pointer-valtype divergence once flagged here is RESOLVED — every load/store address is now i32-coerced at the access site, so a loop-carried heap pointer wraps losslessly instead of failing validation.** ⭐ **arm64-linux AUTHORIZED 2026-07-19 (user ruling) — TWO RUNGS. ✅ RUNG 1 (compiler) LANDED 2026-07-19 (`c8ec215ef`..`1aa3b4323`): shv2 emits a running STATIC aarch64 ELF for `--target=arm64-linux`** — `min`⇒7, `hello`⇒prints+42, and a heap/String probe (mmap + append + refcounts + literal buffer bake + print) byte-identical to the arm64-macOS control, all under `orb run`; specs-shv2 **772/0** with an EMPTY fragment tree (macOS/x64 codegen byte-identical). An independent review then ran the **whole corpus** through the new target — **590 of 793 spec tests compiled and run on arm64-linux, 0 leaks, 0 faults** — and verified the leak gate actually FIRES there (a NOP'd `__str_decref` ⇒ exit 101; a gate that cannot fire is worthless). Scale-test: **+1 alloc / +48 bytes FLAT at every rung**, a once-per-compile constant (the `ElfIdent` byte literal), isolated by an A/B against the pre-rung parent — the raw delta column's +255k was **accumulated unlogged upstream work, not this rung**, which is why the A/B was necessary. ⚠ **RUNG 1 WAS BLOCKED BY A PRE-EXISTING BOOTSTRAP BUG, now fixed (`ea9a4d94a`):** `maxon-sharp`'s Mach-O writer built the GOT as ONE chained-fixup chain while telling dyld about ONE start page — but **a chain cannot cross a page boundary**, which is what `page_start[]` exists for. Once shv2's image grew ~20 KB the GOT straddled a 16 KB boundary and 16 of 56 imports were never bound, still holding their raw BIND words, so calling one jumped to `0x8000000000000000 | ordinal`; the unbound set was `getdirentries64`/`posix_spawn`/`waitpid`, i.e. `spec-test` itself. A pure size lottery — identical source built green until the image crossed a page. The comment directly above it records fixing the **segment**-level version of the same mistake. shv2's own `buildGot` has the same latent shape (safe only because its GOT is 3 slots and page-aligned) and now **asserts** that invariant rather than trusting it. **The port is unusually cheap, and the reason is that Linux PERMITS what macOS FORBIDS:** shv2 reaches the OS through exactly three GOT import slots (`_exit`/`_write`/`_mmap`) *only* because [macOS forbids raw syscalls](maxon-shv2/Compiler/Targets/Arm64/Arm64Runtime.maxon#L6). On Linux those become raw `svc #0`, so the whole dynamic-linking apparatus — PT_INTERP, GOT/PLT, chained fixups, LC_UUID, ad-hoc code signing — **does not exist**: v1's [ElfWriter.maxon](maxon-selfhosted/Compiler/Targets/Linux/ElfWriter.maxon) is **421 lines against Mach-O's 948**, and it **already patches arm64 relocations** (`patchElfRdataRelocationsArm64`), because v1 shipped x64-linux *and* arm64-linux. shv2's `Targets/` tree already mirrors v1's, so `Targets/Linux/` is a literally empty slot. **AUDITED — the ISA layer is OS-neutral:** `Arm64Backend.maxon` (the encoder) and `Arm64PrologueEpilogue.maxon` need **ZERO changes**, and the two divergences that normally make macOS→Linux arm64 painful **do not apply at all** — shv2 emits **no varargs anywhere**, and works exclusively in 64-bit values, so Apple's caller-extends-small-ints rule is satisfied vacuously. x18 is already out of the allocatable pool. **The one numerically-wrong constant is [`MmapAnonPrivate = 0x1002`](maxon-shv2/Compiler/Targets/Arm64/StdToArm64Conversion.maxon#L73)** (Darwin `MAP_ANON`); Linux `MAP_ANONYMOUS|MAP_PRIVATE` is **`0x22`**. **DESIGN PROVEN BEFORE ANY COMPILER CODE** (2026-07-19): a hand-built static aarch64 ELF — no libc, no PT_INTERP, raw `svc #0` for `write`+`exit` — runs, prints byte-exact stdout, returns exit 42. **TEST SUBSTRATE: OrbStack**, machine `maxon-linux` — native aarch64 (virtualization, **not** emulation), and **the Mac filesystem is mirrored at IDENTICAL paths**, so the runner passes the same `exePath` the compiler wrote with **no path translation**; **~21 ms/exec** against `docker run`'s 200–500 ms, which would have dominated a ~770-test suite. Exit codes + stdout/stderr separation verified. ⇒ **RUNG 1 (compiler) — DELIVERED, as planned except where marked:** port the ELF writer · add an `Arm64Op.syscall` (**shv2 has none** — v1's `Arm64Op.syscall(resultSlot, number:, argSlots:)` is the model) · Linux arms for the three lowering sites ([`:752`/`:776`/`:789`](maxon-shv2/Compiler/Targets/Arm64/StdToArm64Conversion.maxon#L752)) · a Linux entry stub (`_start` takes argc at `[sp]` with x30 undefined — **not** dyld's ABI) · hoist `MachoImportSlot` out of `Targets/Macos/` into a neutral `Targets/Shared/` slot (**a real design leak: two `Arm64/` files import a Mach-O enum today**) · the [`Main.maxon:70`](maxon-shv2/Main.maxon#L70) accept-list + [`BackendDispatch.maxon:249`](maxon-shv2/Compiler/Targets/BackendDispatch.maxon#L249). Acceptance: hello-world exits 42 under `orb run`. ⚠ **TWO ITEMS IN THIS PLAN WERE WRONG AT THE CODE, and both were caught by READING rather than by a gate.** (1) It said to port v1's `.covdata` third PT_LOAD and "mirror MachOWriter's `hasCovdata` branch" — **MachOWriter has no such branch, it PANICS on non-empty covdata**; the plan had been written off a grep hit read without its context. shv2 hard-codes the coverage image empty, so the segment could never execute or be tested, and it was removed for a guard matching Mach-O's. (2) It said to take v1's 64 KiB `.data` BSS tail; **the implementer refused, correctly** — that arena existed because *v1's bump allocator lived in `.data`*, and shv2 mmaps its arenas, so `p_memsz == p_filesz`. Also recorded: **v1's `stashEntryArgv` is a latent bug on Linux** (it stashes argc/argv from x0/x1, the dyld convention; Linux passes argc at `[sp]` with x0/x1 unspecified). shv2 needs no argv at the scalar core so this rung dodges it entirely, and the entry stub carries a landmine comment for whoever wires argv later. ⚠ **HOST TRAP, cost ~20 min:** a freshly rebuilt binary that exits **137 (SIGKILL) with no output** is macOS's code-signature cache poisoning that INODE after repeated rebuilds at one path — NOT a malformed image. `codesign -v` passes while the kernel still kills it; `rm` before rebuilding (new inode) clears it, and copying the same bytes to a fresh path proves it. ⇒ **RUNG 2 (harness) — ✅ LANDED 2026-07-20 (`5f5cd8fca`..`ef6d823c7`): `spec-test --target=arm64-linux` runs the suite through OrbStack at 780/0 — the FULL host-runnable set, the same count as the host.** ⭐ **THE DRIFT NUMBER IS THE RESULT:** across all 780 goldens, `fragments/arm64-linux` and `fragments/arm64-macos` differ in **FOUR files**, and every difference is the one substitution `arm64.importCall 0` ↔ `arm64.syscall 94` — same isel, same register allocator, same encoder, byte for byte. That MEASURES the claim rung 1 was built on, and makes the pair a permanent drift detector: a future arm64 change that diverges between the two OSes appears as a fifth differing file. The three `register-pressure` arm64 markers were WIDENED to name both arm64 targets (they restrict on register-pool grounds, which arm64-linux shares; their goldens came out byte-identical to the macOS twins) — leaving them narrow would have been a per-target opt-OUT with no technical reason, the shape the uniform rule bans. ⚠ **Three bugs fixed here had nothing to do with arm64-linux and would otherwise still be live:** (a) the mod-2⁶⁴ overflow in ALL THREE binary readers' truncation guard (`offset + size > count` wraps, so a header claiming `sh_offset=0xC000…`/`sh_size=0x4000…` sums to ZERO, passes, and panics inside the slice blaming a caller that did check) — now one shared subtraction; (b) `.data`'s `sh_size` was FLOORED along with the segment, so a 1-byte `bool` global advertised 8 bytes and `RequiredData`'s PREFIX comparison could not tell padding from globals — **the gate was WEAKER on arm64-linux than on its twins, on the very rung whose value is drift detection**; `p_filesz`/`p_memsz` are what the loader must MAP, `sh_size` is what was LAID DOWN, and they are different claims; (c) every spec spawn was unbounded (`timeoutMs = 0` = wait forever) — a cross run through `orb` was observed blocking **18m35s AFTER the guest had exited and been reaped**, with ZERO output because the runner's stdout is block-buffered when redirected. Now 120 s, ~1000× the ~0.12 s per-test average: the point is converting an unbounded SILENT hang into a named failure, not policing slow tests. ✅ **BOTH ITEMS ONCE LISTED OPEN HERE ARE NOW FIXED (2026-07-20, `8163f1056`/`847906427`), and the `--workers` one was NOT what this file said it was.** The ORPHAN golden was a stale P1.4b-Wave-2b artifact (`037c29f65` implemented managed returns and deleted the declaring test, but was authored on the x64/wasm host, so only the arm64-macOS golden was left behind) — deleted; the trees now match. ⚠⚠ **THE `--workers` ENTRY WAS WRONG IN EVERY PARTICULAR, AND IT HAD BEEN WRITTEN OFF AS "the host".** It said "≥4, pre-existing, under load". Measured on an IDLE host: **4 passes, 5 fails** — probabilistic, scaling with worker count, and **not environmental at all.** It is a REAL PUBLISH-BEFORE-PARK RACE in the emitted GT scheduler: `__io_submit_read` registers its kevent on the **SHARED** kqueue and only THEN parks (dequeue a successor, then switch — driving the scheduler INLINE on its own stack when nothing is runnable), so any other M polling that kqueue reaps the event in that window and enqueues a **still-running** GT; a third M switches in on its stale `gt.sp`, and because GT stacks start at 2 KB and RELOCATE when they grow, that pointer is into `munmap`ped memory. Caught under lldb with `P0->currentGt == P4->currentGt`. `MAXON_MAX_PROCS=1` at `--workers=8` was 4/4 green, which isolates it to multi-M rather than GT count. **Fixed by gating every wakeup on `ioYielded`** — the runtime's OWN existing "parked, off-stack" signal, which three other paths already stand on and which `__io_op_done`'s comment already claimed to mirror — plus the `DMB ISH` StoreStore/Dekker barriers the arm64 protocol needs. Now **786/0 exit 0 at `--workers=8`, six consecutive, ONE hash across all seven runs** (baseline: 6/6 crashed). ⚠ **THE SCARIEST PART WAS NOT THE CRASH:** at `--workers=6` it produced **33 SILENTLY WRONG results** — "codegen changed" failures whose actual IR was missing or started at the wrong function — so the suite REPORTED FALSE FAILURES rather than dying. A gate that lies under load is worse than one that stops. ⚠ **v1 has the IDENTICAL hole** (`Arm64MacosGreenThread.maxon:3359-3367`), so this is a divergence from BOTH references, not a port; and the **x86 twin is documented-but-unpatched on purpose** (`X86CodeEmitter.Runtime.cs`): an IOCP completion is CONSUMED when dequeued, so the arm64 trick of declining the wakeup would LOSE it and hang — a correct fix needs a hold-and-re-drive design, and x86 publishes "parked" LATER than arm64 (its `__gt_context_switch` does not set `ioYielded` itself), so a naive gate could read 0 for a genuinely parked GT. What rung 2 delivered, as planned: ⚠ **`isCrossTarget` CONFLATES THREE FACTS — this file's own signature disease, and arm64-linux is the first target to split them.** It simultaneously means *needs an external runner*, *emits no Target IR*, and *isn't the host*; those coincide for wasm alone. Two real bugs follow: [`SpecTestRunner.maxon:462-469`](maxon-shv2/Testing/SpecTestRunner.maxon#L462) makes cross ⇒ skip `--emit-ir` ⇒ **no golden**, but arm64-linux DOES emit Target IR and must be pinned; and [`:226`](maxon-shv2/Testing/SpecTestRunner.maxon#L226) keys the fragment dir to `detectHostTarget()` instead of `effectiveTarget`, so a cross-native run from this host would **write arm64-linux goldens into `fragments/arm64-macos/`, CLOBBERING them**. Fix by splitting the axes, **not** by adding a third special case. Plus an `ElfSectionReader` for the `RequiredData` gate, and `arm64-linux` appended to `static-variables.md`'s 8 markers. **arm64-linux shares ALL lowering with arm64-macos, so their `.test` fragments should be near-identical — which makes it a standing DRIFT DETECTOR on the arm64 path.** ⭐ **x64-linux LANDED 2026-07-21 — the x64 twin of arm64-linux, and the FIRST cross target runnable from THIS Windows host.** shv2 emits a running STATIC x86-64 `ET_EXEC` ELF for `--target=x64-linux`, cross-run under **WSL2** (`wsl --cd <dir> -e ./<exe>`; exit codes propagate; **NO install** — a static raw-syscall ELF runs under the WSL2 kernel with nothing in the distro). ONE new IR op **`x64Syscall(number)`** (encoder `mov eax,num; 0F 05`; args pre-moved to rdi/rsi/rdx/**r10**/r8/r9, result rax; `iatCall`'s clobber mask, guarded by `assertX64CallClobbers`), an **`x64OsCallKind`** door OS-branching `osExit`/`osAllocPages`/`osWriteStdout` to **`exit_group`(231) / `mmap`(9) / `write`(1)** — the deliberate 231-not-60 divergence from v1, mirroring arm64's 94-not-93 — a **FRAMELESS** Linux entry stub (no VEH panic runtime: the scalar core needs none, exactly as arm64-linux appends only its stub; System-V alignment via NO `push rbp` — the kernel enters `_start` at rsp≡0 so `call main` reaches 16-alignment without one), and the ELF writer relaxed to `EM_X86_64` + an x64 RIP-relative reloc patcher (`patchX64SectionRelocations`, the PeWriter twin over absolute VAs). The spec harness gained a **HOST-AWARE** runner: `externalRunnerFor`'s linux arm defers to `linuxRunnerForHost` (WSL from Windows, OrbStack from macOS) and rejects a foreign-CPU linux target upfront (WSL runs only the host CPU — no in-box cross-exec). **x64-linux runs the host's FULL x64-windows set** — originally minus the 2 divide/mod-by-zero tests (safety.md), which are now un-gated too: a **SIGFPE fault runtime landed 2026-07-21 (OPEN #82)** — the Linux twin of the Windows VEH handler, reusing its OS-neutral backtrace chunks over a `write` syscall, registered via rt_sigaction (⚠ x86-64 needs `SA_RESTORER` + an rt_sigreturn trampoline, or the kernel SIGSEGVs building the frame — found by strace). Goldens byte-identical `--workers=1` vs 12. 67 x64-generic markers (first-class-functions / closures / RequiredData / register-pressure / ternary) widened to name x64-linux. **`fragments/x64-linux` is a DRIFT DETECTOR vs `fragments/x64-windows`** — the two differ ONLY at the OS-call sites (syscall vs IAT), the x64 twin of the arm64-linux/arm64-macOS pair. ✅ BOTH FOLLOW-UPS DONE 2026-07-21: the SIGFPE handler (#82, above) and the shared `Targets/Shared/PosixMmap.maxon` home for the duplicated POSIX mmap constants (#83). |

### ⭐ Frontend parallelism — the pre-scan FANS OUT; the barrier is cheap and STAYS

The frontend is a whole-program `queryProgramSignatures` sweep (lex every file, extract its
DECLARATIONS off tokens, fold into one index) sitting UNDER the per-file `queryParseOps` parse. That
makes the sweep a **barrier**: no file's real parse can start until every file's declarations are
known. **This is intrinsic, not incidental.** *"Is `Foo` a type declared ANYWHERE?"* is a whole-program
predicate, and the parse needs the answer to shape control flow — a bool `and`/`or` is short-circuit
blocks + a phi, an int one is a bitwise op, and which it is must be decided BEFORE the right operand is
parsed. **The deferral was never the defect; not looking was** (see ARCHITECTURE.md → Query spine, and
`SignatureIndex.maxon`).

⇒ **Do not try to DELETE the barrier. Parallelize ACROSS it (BSP):**

```
parallel[ per file: lex → extract declarations into local arrays ]   ← the map
serial [ fold contributions into the index, in SOURCE-PATH order ]    ← the reduce (cheap)
parallel[ per file: parse, reusing the tokens the sweep already cached ]  ← the map
```

- **Token reuse is free** — `queryTokens` is memoized; the sweep populating it and the parse reading it
  back is today's behavior, just serial. No double-lex.
- **The extract is already the map half** — `foldDeclaredSignaturesInto` builds per-file LOCAL arrays and
  reads no shared state while walking (a sweep types no call, so it never reads the index it fills). Only
  the fold touches the shared index.
- **The serial reduce is small** — O(declarations) fold + the O(constants) `evaluateInitializers` DFS (the
  one genuinely-sequential piece: forward + cross-file constant refs, cycle-detected → `E2012`).
- ⚠ **TWO GUARDS, or a gate breaks.** (1) The fold must stay **LINEAR** — this exact whole-program-under-
  per-file spot has gone **O(files²) twice** (`clearDepsFor`, `compositeSourceHash`); before adding work
  to it, multiply by the file count. (2) Fold in **PATH ORDER, not completion order**, or the byte-identity
  bootstrap gate dies on a nondeterministic index.

This lands at **P2.6**, folded into the fan-out rung — it is the one upstream phase that rung otherwise
leaves serial.

**BATCH ≠ INCREMENTAL — two independent, composable wins.** Parallelizing the sweep is the **batch** win
(self-host throughput, the >90% CPU number). It does NOT shrink the **incremental** cost: the sweep memo is
keyed on the whole-program composite hash, so a one-file edit still re-sweeps every file (faster now, not
smaller). The orthogonal knob is **per-file sweep memoization** — cache each file's declaration contribution
on its own content hash, re-extract only the edited file, re-fold. Do both and a cold build saturates cores
while a warm rebuild does O(changed file) + O(declarations).

**Removing the barrier ENTIRELY is a MEASUREMENT-GATED alternative, not committed work.** Two designs would
dissolve the pre-scan: **(a) post-parse fixup** — parse with no cross-file info, record ops of unknown type
on a worklist, resolve after all files parse (needs *detached-block* deferral for `and`/`or`, the one
CFG-shape case); **(b) blocking demand-lookup** — each parse is a green-thread task that SUSPENDS on an
unknown type until a peer registers it (handles `and`/`or` inline, but adds a shared mutable registry,
wait-graph cycle/quiescence detection, and a determinism proof over interleavings — against the byte-identity
gate). **Neither is required** — the barrier does not block P2.6's fan-out or the ≤30 s goal. Promote one
only if the `signatures`-phase scale-test bends or a real incremental-latency target appears; prefer **(a)**
for shv2 (deterministic by construction). Until then this note is the record, so the analysis is not
re-derived.

### ⭐ P1.5: `Promise` MUST CARRY ITS ERROR TYPE — `Promise with (T, E)`, not `Promise with T`

> ## ✅ REVERSED, SAME DAY — **PORT THE BOOTSTRAP'S SHAPE. IT IS NOW THE RIGHT ONE** (`34b77758e`)
>
> This section used to say **"Do NOT port its shape"**, because the bootstrap had the broken one. **The
> bootstrap has been FIXED**, along exactly the lines this section prescribed, so the instruction inverts:
> **shv2 should PORT `Promise with (T, E)` from the bootstrap rather than invent it.** The design below is
> no longer a plan — it is a working, gated implementation (C# suite 2915/0), and it is the cheapest kind
> of prior art: *the same design, already debugged.*
>
> **What the fix proved, and it is stronger than the argument that predicted it:**
> - **BOTH bits are now dead and REMOVED** — not merely unused. The proof is in the codegen: the promise
>   box drops **24 → 8 bytes** and loses **two stores per boxed promise**, and the `otherwise` emitter
>   loses its runtime-branched decref for the straight-line one a *static* type affords. `ErrorType` is
>   the whole story; `Throws` is *derived* (`ErrorType != null`), so there is **no second copy to drift**.
> - **`throws_` turned out to be WRITE-ONLY for its entire life.** Nothing ever read it — while a comment
>   claimed *"the runtime branches on it."* The comment described a compiler nobody wrote. (The same
>   disease as `TargetOpMeta.setsFlags` and the `xor reg,reg` idiom that never existed. **Three instances
>   in one day.**)
> - ⚠ **It was leaking in shv2's OWN worker pool, live.** `DrainPromise` was `Promise with StringArray`
>   while `drainResultsThunk` throws the **union** `SubprocessError`; the box said "not a heap pointer",
>   the conditional decref never fired, and **every dead worker leaked its error** — invisible to a green
>   suite, because that path only runs when a worker dies. It is now `Promise with (StringArray,
>   SubprocessError)` and binds `(e)`, which both releases the payload *and* reports which failure killed
>   the worker.
>
> **`await` is LINEAR** (user decision, 2026-07-14; `E3100`): a promise is awaited **exactly once**, and a
> second await *reachable* from the first is a compile error — flow-sensitive, keyed on the **binding**, so
> the central `for p in promises 'each' … await p … end` idiom stays legal (the loop re-arms `p` each
> iteration) and two awaits in **mutually exclusive branches** are fine. The double-free is now
> **unrepresentable**, not fixed.
> ⚠ **Known boundary, carry it into P1.5:** linearity is enforced on **bindings, not through containers** —
> awaiting the same *array slot* twice is not statically caught. That needs ownership tracking through the
> container, which is **shv2's ownership milestone**, and shv2 is the compiler that will have it.

**Learned from a real bug in the bootstrap (2026-07-14).** *(The history below is kept because it is the
argument for the design, and it is the reason to trust it.)*

The bootstrap's `MaxonPromise` stored **two BITS distilled from the callee's `throws` clause** (`Throws`,
`ErrorIsHeapPtr`) and **threw the type away**. Everything downstream then had to reconstruct what it
could from a bit, and it went wrong in four separate places:

- `try await p otherwise (e)` **bound `e` as `int`** — the *value* was right all along (the error
  ordinal), only the **type** was lost, so the binding fell back to `Integer`.
- An **associated-value** union error emitted **no decref** ⇒ the payload **LEAKED**.
- `async` and `await` in **different blocks** re-tagged the promise and **re-erased** the type.
- **Awaiting a thunk that throws `A` inside a function that throws `B`** skipped the type check
  entirely and **reinterpreted A's ordinals as B's tags — a silent miscompile.**

⚠ And it bit **shv2's own worker pool**: `drainResultsThunk` throws the union `SubprocessError`, the box
said "not a heap pointer", the conditional decref never fired, and **every dead worker leaked its
error.** Invisible to a green suite, because that path only runs when a worker dies.

**The erasure is not a threading bug — it is the TYPE.** `Promise with T` has one parameter, the
*result*; boxing therefore has nowhere to keep `E`, and no amount of plumbing recovers it (an
`Array with (Promise with Integer)` may legally hold promises from two functions with different
`throws`). The bootstrap's `errorIsHeapPtr` runtime bit exists **only** to paper over that erasure.

⇒ **shv2 builds `Promise` with the error type in it from the start. The runtime bit then becomes
unnecessary**, and all four bugs above are unrepresentable rather than fixed.

### P1.5 residuals — closures, escape, and value-merge follow-ups

> ## ✅ ALL P1.5 FRONT-END RESIDUALS ARE CLOSED (2026-07-26). The list below is kept as history.
>
> **#67 ✅ · #75 ✅ · #79 ✅ (resolved by measurement, not deferred) · #74 = a DECISION, not a residual.**
> See the P1.5 ladder row. Two lessons worth carrying, because both cost real time here:
>
> - **A COMMENT THAT LIED OUTRANKED THE CODE FOR SIX DAYS.** `StdToArm64Conversion.maxon` said
>   `arm64EmitLeaFuncAddr` was "a DELIBERATE PANIC STUB"; the function files a relocation and emits
>   ADRP+ADD, and a three-line program proved it (`apply(double, 10)` + a capturing closure = 42,
>   oracle-matched). PLAN.md carried the same false claim and *contradicted itself about it in two
>   places*. It fooled an independent auditor into classifying live cases as technically restricted.
>   **This is the project's signature bug — ONE FACT WRITTEN DOWN TWICE — and the cure is the same as
>   always: pin it with output.** 104 committed goldens now contain `arm64.leaFuncAddr`.
> - **A STALE BLOCKER IS AS EXPENSIVE AS A LIVE ONE.** #75 was recorded as waiting on struct-copy-return
>   (E2015). That landed with P1.7 and nobody re-checked, so the residual outlived its cause; the ACTUAL
>   blocker was a different defect entirely. **Re-measure a blocker before you cite it.**
>
> ⚠ **THREE DEFECTS FOUND WHILE FINISHING P1.5 THAT ARE *NOT* P1.5's, each a silent WRONG ANSWER, each
> filed rather than fixed because each is a distinct mechanism needing its own contract:**
>
> - **🔴 #125 — `var` REASSIGNMENT DISCARDS THE BINDING'S DECLARED TYPE.** No cast, no closure:
>   `var h = w` (w a `Word`, unsigned) then `h = p` (a signed `Num`), then `h shr 62` — **oracle 42,
>   shv2 38**, because the rebind adopts the assigned value's tag and the shift sign-fills. A MERGE is
>   correct (`mintPhiForBinding` stamps the BINDING's type, measured: an `if` gives 42); a straight-line
>   reassignment is not. `Parser.parseAssignment`/`reassignBindingValue`. It is rung-sized because the
>   real question — which of the two records of a value's type wins, for ALL FOUR things the tag decides
>   (signedness, drop routing, conformance dispatch, float class) — is a language decision, and the fix
>   sits in ownership machinery (promise re-arm, the `ownedBindings` stack, COW promotion).
> - **🔴 #126 — shv2 does not range-check `as ExitCode` AT ALL.** `let e = <out-of-range> as ExitCode`
>   compiles and runs; the oracle refuses it E3005. `recordCastSite` records nothing for a non-`named`
>   target and `RangeCheckCastSite` carries an alias NAME, which `ExitCode` has not. Needs
>   `Project.maxon` + `InsertRangeChecks.maxon`.
> - **🔴 #127 — shv2 accepts `true as Byte`** (failing later at the arithmetic, E2004) where the oracle
>   rejects the cast itself, E3009. `castHasNoLegalConversion` admits bool↔int because both are scalars.
>
> **🔴 BOOTSTRAP ORACLE BUGS found here** (their home is the "Bootstrap oracle bugs" list; the C# suite
> is their gate): **(a)** the bootstrap MISCOMPILES an inline cast-then-shift — `let w = x as Word;
> w shr 63` gives **1** but `(x as Word) shr 63` gives **−1** in the same program; shv2 answers 1 in all
> three forms and is the self-consistent one. It cannot bite the tree the bootstrap compiles (no inline
> `(… as Alias) shr` exists in `stdlib/` or `maxon-shv2/`). **✅ (b) FIXED 2026-07-26** — `maxon fmt`
> MIS-LEXED A BARE ENUM MEMBER NAMED `function` as opening a function body and indented everything
> after it one tab deeper to EOF, including the top-level `end` — 2,189 lines across
> `TypeRules.maxon` and `MaxonDialect.maxon`. **Its output no longer needs refusing for those two
> files, and a whole-tree `maxon fmt` is idempotent again** (345 files, 0 changed on the second
> pass). Cause: the data-block lookahead in `MaxonFormatter.FormatCore` SKIPPED NEWLINES before
> testing whether an Identifier followed, so a bare case borrowed the NEXT case's name and read as a
> real declaration — the loop defeated the exact rule its own comment stated ("bare cases are
> followed by a Newline"). A declared name is on the SAME line as its keyword, so the lookahead no
> longer skips.
>
> **A SECOND fmt bug fell out of the same audit, and it had ALREADY committed its damage:** every
> keyword can also be an ordinary name in Maxon (the parser's `IsIdentifierLikeToken`), but the
> formatter's unary-minus test recognized only literals and identifiers as operands — so `to`, used
> as the PARAMETER it is, looked like the range operator and `to - from` was rewritten to `to -from`.
> **8 sites across 4 files were already mangled in-tree** (`IrBlock.maxon`, `Parser.maxon` ×5,
> `SplitLiveRanges.maxon`, `stdlib/Internals.maxon`'s `end - start`), repaired by this change. The
> cure is contextual rather than a second keyword list: an infix keyword operator always has its left
> operand before it, so a keyword with no operand behind it is a NAME. The keyword set is DERIVED
> from `Lexer.KeywordMap` rather than restated — a second copy would stop matching the day a keyword
> is added.
>
> **✅ arm64-linux IS DONE, NOT DEBT — 1589/0** (`1de8d480a`, 119 new + 38 refreshed goldens). This was
> filed as "needs its own hardware"; that was WRONG, and the correction is worth keeping because the
> capability had been sitting in the runner the whole time: `SpecTestRunner.externalRunnerFor` routes a
> Linux target from a macOS host to **OrbStack** (`orb run -m maxon-linux <abs-path>`), and the VM
> mirrors the Mac's filesystem at identical REAL paths, so a binary the build just wrote runs with no
> copying. **Check what the harness already does before filing a target as unreachable.**
> - ⚠ **`maxon build maxon-shv2 --target=arm64-linux` yields a SILENTLY CORRUPTED COMPILER.** The
>   backend ignores `--target` for a directory build (still Mach-O) but the frontend's `#if os()` does
>   NOT, so `detectHostTarget()` answers `arm64-linux` on a macOS host, `needsExternalRunner()` answers
>   false, and the runner spawns ELFs directly → **every test fails 126**, looking exactly like a broken
>   harness. Clean rebuild cures it. Same root as the `--target`-vs-frontend-`os()` split that writes
>   wrong golden VALUES, but worse: this one makes a compiler misidentify its own HOST.
> - ⚠ **Run arm64-linux at `--workers=4`.** Each test is an `orb run` into one VM; at 12 workers three
>   tests hit a 120 s spawn timeout. All pass individually — contention, not a defect.
>
> **⏭ CROSS-TARGET DEBT that remains** (genuinely not runnable here, alongside #86/#91/#20):
> **x64-linux — 37 cases** newly live. Plus **4 x64-windows fragments** owed by the cast fix
> (`cast-to-loaded-stdlib-internal-typealias`, `wide-file-cast-still-accepted`, and the two
> `ranged-typealias/unsigned-max-upper-*`). **x64-windows gains nothing else** — the one marker that
> excludes it was preserved on the merits (E5001, 18 live values against a 14-register pool).
>
> **⏭ Also blocking, not this rung's:** there is **no non-Windows panic runtime** (`mrt_panic` is a
> hand-assembled Windows `.text` chunk; everywhere else `osRangePanic` lowers to a bare exit 1), which
> is what keeps those 8 panic-MESSAGE cases gated on four targets; and wasm's `call_indirect` result
> type is arg-count-derived to `(i64ⁿ)→i64`, so an `ExitCode`-returning function VALUE traps.

The closure + async halves of P1.5 landed their CORE (A1 · A2 · B1a · B2a · B2b) — **this list is the
front-end follow-on history, now fully closed** (the runtime/async ones are under Workstream R; #65
implicit-self is noted with the P1.4b closed-box / the future-rungs list):

- **✅ #64 — borrowed-container boxed-union field throw RETAINS (was E2015) — CLOSED 2026-07-23** (main
  `eea72ec74` feat + `2c77fd674` perf + `6eedafce7` review, 1451→1452/0). `throw self.field` (or `throw
  borrowedParam.field`) of a boxed-union FIELD whose container is BORROWED was refused E2015; its premise
  ("shv2 has no `__mm_incref`") was STALE since #40 reintroduced the retain primitive. **RESOLUTION (user
  ruling, extending #40's approach-A retain-promotion to the throw error channel):** `retainThrownField`
  emits `__mm_incref(box)` + `markValueOwnsHeap` — the RETAIN twin of the owned-local `moveOutThrownField`
  (which nulls the slot). The slot is LEFT INTACT so a caller that catches AND re-reads the field sees a live
  box; the caller's container keeps its own reference, the catch consumes the thrown one, refcount balances
  at one free — NO cross-call move tracking needed, the mechanism the old reject was "waiting for." The WHOLE
  borrowed-union-PARAM throw (`borrowedNonField`) still rejects (message reworded). Enables
  `throw-transfers-ownership/propagate-throw-through-local-struct`; the old E2015 pinning test became the
  positive `boxed-union-field-throw-borrowed-self` (re-reads the field after catching → exit 40, which a
  nulling move-out could not survive under poison-free #108). **scale-test #64-isolated footprint = 0**
  (A/B vs parent, byte-identical corpus — the branch never fires on the arithmetic corpus). Independent
  review = MERGE (13 adversarial leak/double-free/UAF probes clean; found + fixed D2: DRY'd the void-incref
  idiom into `emitIncref`, single-sourcing 3 sites). ⏭ **Two follow-ups noted, neither a blocker:** (a) the
  arm64-macos/arm64-linux/x64-linux positive-test goldens are DEFERRED (this host runs x64-windows only; code
  cross-compiles clean on all three) — regenerate on a Mac/WSL host via `--update-required`; (b) **AWARENESS
  for P1.7+**: because the retain shares the SAME physical box between the thrown `e` and the still-live
  field, a future borrowed-union payload MOVE-OUT of the caught `e` would null the shared field's payload
  (memory-safe today — such a move-out is itself E2015, so the payload can only be `_`-discarded, never read
  back — but revisit when borrowed-payload move-out lands).
- **#31 + #80 + #81 — the value-merge cluster — ✅ CLOSED 2026-07-22** (main `11a4bbce5`, 1079→1082/0, rebased
  onto P1.7). All three fired through the ONE `finalizeMatchMerge`. **#31**: a mixed int/float match/ternary now
  PROMOTES each integer arm to float (`promoteToFloat` in the arm's own exit block; a float-meets-non-numeric arm
  stays a give-type mismatch), matching the runnable bootstrap oracle. **#80**: the diagnostics are construct-aware
  via a `MergeConstruct` enum + ONE shared `valueMergeMismatchError` mapping — a ternary reports E2028 "ternary
  expression …", a match keeps E3005. **#81**: function-value arms must agree in signature (NOMINAL fn-type display
  equality, matching the oracle, which rejects `fn(Integer)` vs `fn(Whole)`); moved WHOLE-PROGRAM (recorded at parse,
  drained in `compileToCodeResult` before `resolveTypes`) so a same-file FORWARD-REF and a cross-file mismatch are
  both caught — a coordinator-probed silent miscompile the first parse-time-file-local pass had missed; an
  unverifiable branch (a function value from a param/field/call-result) is cleanly REJECTED, never silent-accepted.
- **✅ #73 — the capturing-closure escape rule is now DERIVED — CLOSED 2026-07-24** (1552→1561/0 x64,
  1367→1374/0 wasm). It was NOT "complete for today's grammar": the enumeration listed three MERGES (a
  ternary arm, a match `gives`, an `otherwise` fallback) and an `if`-continuation phi and a loop-header phi
  are merges too — `var f = <capturing closure>` reassigned inside an `if` or a `while` compiled clean and
  **SEGFAULTED**. `SemanticCheck.checkCapturingClosureUses` now states the rule ONCE over the finished IR
  (a capturing closure may appear only where its environment travels with it), through the SAME exhaustive
  band-append `match` the interprocedural parameter summary already walked — generalised from
  `paramEscapesInBody`/`opEscapesParam` to `valueEscapeSite`/`opEscapesValue` over an `EscapeScanSubject`,
  so ops, terminators AND branch edges are covered for both questions and a newly appended `MaxonOp` fails
  to COMPILE rather than becoming an unguarded sink.
  ⚠ **The parser's per-route refusals STAY, and that is not the enumeration surviving** — it is the only
  place that still holds the words (`storeIndirect` carries no field/struct/case name and a branch edge
  carries no source span at all, so "cannot store a closure that captures in field 'op' of 'Handler'", at
  the field token, is unreconstructible later). SABOTAGE-PROVED non-load-bearing: with
  `rejectEscapingClosure` disabled, every one of the 14 pinned escape programs is still REFUSED (6 by this
  rule as E3099 in generic words, the rest by pre-existing type rules) and every accept stays green — so
  soundness is this rule's and only the wording is the parser's. **Reviewer's sabotage went further and the
  answer is stronger than "a type rule caught it":** the three that stopped at a type rule
  (`ternary-used-in-frame`, `match-arm`, `ternary-to-global`) do so only because those particular corpus
  programs also mismatch `Integer` against the closure's inferred `int` — an EARLIER pass, so the derived
  rule never ran. Rewritten so the arms' types agree, all three reach E3099 from
  `checkCapturingClosureUses` as `mergedIntoPhi` with the parser gate still off. **No live escape route
  rests on the parser or on a type rule.**
  It also closed two holes the delegation had left open, both by the same principle (*delegate a slot only
  where the delegate actually RUNS*): a capturing closure at a **witness dispatch** (no callee name keys an
  escape summary — it reached lowering and PANICKED) and one handed to a **compiler-internal `__` callee**
  (`a.push(closure)` — `validateCall` returns before the escape check, so a heap Array took the bare code
  pointer). Memory: exactly 0 allocs / 0 bytes delta across the whole scale ladder.
- **✅ #78 — indirect calls carry the callee's float result/param types — CLOSED 2026-07-22** (main
  `a1515dfb7`, 1100→1106/0, cross-target). Was WORSE than framed: not a wasm-only trap but a COMPILER CRASH on
  x64 too (a float-returning/float-param function value called indirectly panicked "crosses register files").
  The `callIndirect` StdOp now carries `resultType` + a scalar `argFloatMask`; x64/arm64/wasm route the result
  + each arg by register class through ONE shared `emitArgMovesByFloatMask` (the reviewer collapsed a
  direct/indirect arg-move fork); the `__fnref_` thunk forwards float params in the FP file. Result AND param
  float cases closed, oracle-matched; the mask's width is now the STATED argument ceiling `MaxAbiArgs=64`
  (arguments past the register files travel in stack slots — see the stack-arguments rung).
- **#75 — `capturing-closure-name-not-leaked` stays disabled on a borrowed-struct-copy-return gap**
  (`var hh = h; hh.op = op; return hh` → E2015, no struct clone) — NOT a closure bug (its actual property is
  independently verified). Enable when struct-copy-return lands (P1.7-adjacent).
- **#79 — a closure that itself takes a function-typed param gets no companion env** (`parseClosureExpression`
  never calls `bindClosureEnvParams`); and the escape summary's transitive rule may over-reject an exotic
  pass-through-that-only-calls chain (sound). Both exotic, no corpus. Low priority.
- **#74 — payload-binding IMMUTABILITY is a deliberate divergence to CARRY** (shv2 declares union payload
  bindings immutable → E2013, where the bootstrap routes E3099 through a heap alias). Safer — shv2 never
  writes back through a payload binding. Author a spec pinning E2013 only if desired.

### PRINCIPLE — when may we rewrite shv2's own source?

> **Rewriting shv2's source to dodge a mechanism is a SCOPE CUT, not a simplification.**
> Use it *only* for mechanisms deliberately deferred as out-of-scope (async; and wasm's
> POST-SCALAR slices — the wasm **scalar core landed 2026-07-18**, see the Targets row).
> **NEVER** for a mechanism that is merely *hard* (closures, conditional conformance) —
> that is the whole point of this plan.

shv2's own source is **the best test corpus available**: every construct it uses is a free,
real, integration-level test of a mechanism, written by someone who wasn't trying to test it.
Deleting a construct to avoid implementing it costs twice — it defers the mechanism *and*
throws away the test.

This is why `LazyMessage` stays (closure dogfood) and `GlobalDedupMap` stays as
`Map with (ByteArray, String)` (the conditional-conformance acceptance test). **Do not re-key
it on a `ContentHash`.**

**The same trap, freshly walked into and rejected (2026-07-13) — the "cheap parallel runner."**
`stdlib/Subprocess.maxon` exposes a split spawn/wait API (`spawn()` → a live `StreamingSubprocess`
handle, `wait()`, `waitWithTimeout()`). It is therefore *possible* to parallelize the spec suite
with **no green threads at all**: spawn N children over disjoint test shards, have each write its
results to a file (so no pipe ever fills and deadlocks), then blocking-`wait()` each in turn — the
children run concurrently, so wall time is `max`, not `sum`. **~80 lines, zero new mechanisms.**
**REJECTED.** It buys a fast test run and *defers the single most retrofit-hostile mechanism left
in the language*. Green-thread capture is an **escape**; discovering that after `EscapeAnalysis`
ships single-threaded means retrofitting it with a second capture channel. **Do the hard thing
early: implement `async`.** The parallel harness is not a task to be completed by the cheapest
route — it is `async`'s **dogfood and acceptance test**, exactly as `LazyMessage` is for closures.

The principle applies to **shv2's source, not the stdlib.** Pruning the stdlib *fork* is
explicitly sanctioned — see below.

`Set` is the counter-example that proves the boundary, and it is the signal the cut compass told
us to watch for: rewriting it was cheaper than implementing it right up until it wasn't
(`StdValueUseSet` is now load-bearing in four Std passes). It is in core at P2.3. **If a second
such case appears, re-open the compass decision.**

---

## ⭐ STRING IS A BUILTIN — gated on the RUNTIME, not on generics

**The single biggest ordering correction in this plan.** An earlier revision put `String` after
generics and `Array`, on the premise that `String` **is** `__ManagedMemory with Byte`. **That
premise is false**, and the whole Phase-1 ladder depends on it being false.

**v1 shipped `String` in Phase 7. Generics + layout descriptors landed in Phase 11** — four
phases later. The Phase-7 bootstrap declaration (commit `5fc3b569a`, `StdlibLoader.maxon:74`)
was, in full:

```maxon
export interface BuiltinStringLiteral
end 'BuiltinStringLiteral'                  // EMPTY marker — no `init` requirement

export type String implements BuiltinStringLiteral
	export var managed __ManagedMemory      // BARE — not `__ManagedMemory with Byte`
	export var isAsciiFlag bool
end 'String'                                // ZERO methods
```

…and string literals, interpolation, and `print` all worked. `stdlib/String.maxon` was not
compiled at all.

**Why it works — carry this over as deliberate design.** `__ManagedMemory` is a
compiler-registered, **hardcoded 40-byte struct** — verified at
[LowerMaxonToStd.maxon:1523](maxon-selfhosted/Compiler/IR/Maxon/LowerMaxonToStd.maxon#L1523)
(`registerManagedMemoryStruct`): `buffer@0, length@8, capacity@16, element_size@24,
parent_ptr@32`, every field `offsetResolved: true`, stamped with the synthetic source path
`stdlib-bootstrap.maxon`. **`element_size` is a RUNTIME FIELD.** Element-size polymorphism is a
data field, not a compile-time dictionary — *that single decision is why String needs no layout
descriptor.*

`let s = "hello"` lowers to: an rdata blob (`__istr_<id>`, NUL-terminated) → a
`__ManagedMemory` with **`capacity = -2`** (the rdata sentinel: the destructor must not free
`.rdata`) → a 16-byte `String` envelope `{mm@0, isAscii@8}` via
`__mm_alloc(16, &__destruct_String)`, classified `freshRc0`. **Zero stdlib calls.**
`__destruct_String` is *compiler-synthesized* from the registered field list, not a stdlib body.
Interpolation is a **compiler-emitted memcpy chain** (`mrt_alloc` + `memcpy` per part), not a
stdlib `concat`.

**Minimal builtin surface:** `MaxonType.string` primitive arm · `MaxonOp.stringConst` +
`stringInterp` · rdata interning (shv2 **already has this** — `GlobalDataTable`) · hardcoded
`__ManagedMemory` + `String` structs · marker-interface discovery · synthesized
`__destruct_String` · the MM runtime (**Workstream R1**).

**Two things to carry over, one to fix:**
- **Carry:** the dual representation — `MaxonType.string` (pre-discovery tag) ↔
  `MaxonType.named(String)` (post-discovery), with `builtinStringLiteralType()` falling back to
  `MaxonType.string`. This is what lets the parser synthesize string-typed IR *before* the stdlib
  decl has been seen.
- **Carry:** `element_size` inside the `__ManagedMemory` record (above).
- **FIX:** v1 **panics** when no type implements `BuiltinStringLiteral`
  ([TypeResolution.maxon:417](maxon-selfhosted/Compiler/TypeResolution.maxon#L417)). The C#
  bootstrap raises a proper `CompileError`
  ([2-Parser.cs:15615](maxon-sharp/Compiler/2-Parser.cs#L15615)). **Emit a real diagnostic.**

**Generics-before-ownership is REFINED, not reversed.** That decision's real intent was *"never
retrofit the descriptor drop path."* That intent binds **`Array`** (managed elements), not
**`String`** (trivial elements). We keep it by **declaring `own.drop` with BOTH arms at P1.2** —
the concrete arm (`__destruct_T`) live immediately, the descriptor-mediated arm
(`destroyFunc@40`) declared but unreachable until generics at P1.6. `LayoutDescriptor` is 218
lines in v1; defining it early costs almost nothing and buys the whole design-in.

**Deferred to generics — exactly two string-adjacent things:**
- **`b"…"` byte-string literals** — `lowerByteStringLiteral` interns an `Array with Byte` gid and
  *requires* a layout descriptor. The **only** string op genuinely gated on generics ⇒ **P1.7**.
- **`"{userStruct}"`** interpolation — the `stringable` arm dispatches through a witness table
  ⇒ **P2.1**. *(The harness interpolates only primitives and `String` — grep-verified — so
  Phase 1 needs only the builtin arms.)*

---

## Phase 1 — the measured boundary

**MEASURED 2026-07-13 (P1.0c) against the UPGRADED, PARALLEL harness — NOT GREPPED, COMPILED.**
`maxon-shv2/Testing/` (`SpecParser` 364 + `SpecTestRunner` 467 + `SpecWorkerPool` 1091 = **1,922
lines**) plus the `Main` a standalone runner needs. A standalone `spec-runner` was extracted (the
harness verbatim + `Compiler/Target.maxon` verbatim + a 2-symbol shim + `Main` minus the driver
commands), built with `maxon.exe --emit-ir`, and its EMIT set read off **both** the emitted X86 module
**and** the PE COFF symbol table (`llvm-nm`) — **146 stdlib functions, the two lists identical.** The
extracted runner **runs**: it drives a real 2-worker green-thread pool.

> ### ⚠⚠ THIS MEASUREMENT IS STALE, AND IT UNDER-SCOPES THE GATE. RE-MEASURED 2026-07-29.
>
> **The subject grew 73% while the row that describes it did not.** Nothing re-checks a measured
> boundary, so it reads exactly as authoritative on the day it stops being true — the same failure
> mode as a `disabled-test:` marker that outlived its cause (P1.7 finish, slice 1), one level up.
> Re-extracted 2026-07-29 by the same recipe; the artifact that changed:
>
> | | P1.0c (2026-07-13) | Re-measured (2026-07-29) |
> |---|---|---|
> | harness lines | `SpecParser` 364 + `SpecTestRunner` 467 + `SpecWorkerPool` 1091 = **1,922** | 739 + 1475 + 1114 = **3,328** (**+73%**) |
> | other `Testing/` files | none | **5** — `PeSectionReader` · `BinaryImageReader` · `ElfSectionReader` · `MachoSectionReader` · `LinkedDataSection` |
> | the shim | **2 symbols**, one compiler file (`Diagnostics`) | **5 symbols across 5 compiler files** — `CompileError`+`reportCompileError` (`Diagnostics`) · `BoolArray` (`Project`) · `findFileUpward`+`hostWasmToolCandidates` (`Targets/Wasm/WasmComponent`) · `CharOffset` (`Lexer`) |
> | copied whole | — | `Compiler/Targets/Shared/BinaryHelpers.maxon` · `Compiler/NumberParsing.maxon` |
>
> **The two new edges are the informative ones, because neither is the harness "getting bigger".**
> `SpecTestRunner` now locates the vendored `wasmtime`/`orb`/`wsl` runners with the COMPILER's own
> upward file search (`findFileUpward`), and `PeSectionReader` parses a golden's FLOAT values with the
> COMPILER's own `parseFloatBits`. Each is a genuine harness→compiler dependency that did not exist in
> July, and each one the gate must either shim or sever. ⇒ **A gate scoped off the 2026-07-13 row is
> scoped off a program that no longer exists.** Re-extract before sequencing, never plan from the row.
>
> ✅ **What did NOT change: the EMIT/DECLARE conclusion — RE-MEASURED, not assumed.** Read off the
> 2026-07-29 runner's own `--emit-ir` module (94,649 lines), by P1.0c's method:
>
> - **`Map` is still DECLARE-ONLY.** 45 textual `Map` hits, and **all 45 are `FragmentPathMapping*`** —
>   a harness struct whose NAME contains "Map". Zero instantiations, zero bodies. ⚠ The grep that
>   "finds `Map`" is the wrong instrument here; the hit list has to be read, not counted.
> - **`Set` is still EMITTED, still via `String.trim()`.** `CharSet.insert` 56 · `.grow` 47 ·
>   `.contains` 32 · `.init` 12, with its element constraints as `Character.hash` 15 /
>   `Character.equals` 17. (39 `trim` hits, the edge that drags it in.)
> - **CLOSURES ARE STILL 0.** `__closure` 0, `envAlloc` 0 — the `async`-on-a-direct-free-function
>   choice in `SpecWorkerPool` has held across the +73%.
> - **The bootstrap still emits 0 `callIndirect`/`witnessCall`** — it monomorphizes, exactly as P1.0c
>   recorded, which is why it cannot settle shv2's witness-slot question. Unchanged, still inferred.
>
> ⇒ The boundary's *shape* survived; only its *size* rotted.

| The harness **EMITs** (⇒ Phase 1 must CODEGEN) | The harness **DECLAREs only** (⇒ Phase 2) |
|---|---|
| structs · enums · **unions** · `match` | ✅ **`Map`** — **0** instantiation, **0** bodies. Its `EnvMap` arms in `Subprocess.Environment` compile to a bare `mm_decref`: **laid out, never codegen'd.** ⇒ **Reachability-seeded lowering WORKS, and the pruned fork is NOT needed for `Map`** |
| **`String`** + interpolation of primitives, `String`, **and one STRUCT** (`FilePath`, via `Stringable`) — `Main.maxon:233-236` | **conditional conformance** — `Array implements Hashable, Equatable where Element is …` (`Array.maxon:415`) is **NOT reached** |
| heap · ownership · drops | **`extension`** — **0**. Not one of the 4 `Array` / 3 `Interfaces` / 3 `PrimitiveExtensions` blocks emits a method |
| **owned `String` payloads in union cases** | **existentials / interface-typed storage** — 0 |
| `throws` / `try` / `otherwise` — **80 `try`, 16 `throws`** | the entire compiler cone — no IR, no allocator, no backend |
| **generics** — `Array with {SpecTest, SpecTestResult, FilePath, SpecJob, SpecPlan, …}` ⇒ managed elements | `Json` · `List` · `Math` · `Range` · `Sha256` · `Vector` · `Log` · `Clock` · `HttpClient` · `TcpClient` · `Ascii` · `Unicode` · `helpers/sort/*` |
| **`Array`** · `for-in` — **18** sites | |
| **ranged typealiases** — **8** | |
| ⭐ **`Set` — EMITTED, and not by anything the harness wrote.** `CharSet = Set with Character` (`CharacterSet.maxon:19`) arrives via **`String.trim()` (13 sites)** → `CharacterSet.whitespacesAndNewlines()`. Emits `CharSet.{init,insert,contains,grow}` + `HashSlotArray`/`StateArray` | |
| ⭐ **`Hashable` + `Equatable` — EMITTED**, as `Set`'s element constraints: `stdlib.Character.hash` / `.equals` (type-body conformances) | |
| **`async` / `await` / `Promise`** — from the pool. **`Promise` has ZERO stdlib bodies** (a compiler-synthesised facade); the real surface is **runtime** — `__gt_spawn` · `__gt_try_await` · `__gt_is_complete` ⇒ **Workstream R3** | |
| **async subprocess stdio** — `StreamingSubprocess` ×11 + `Stdin` ×7 (+23 fns — the pool's ENTIRE stdlib delta over the serial harness) | |
| ⚠ **CLOSURES — 0.** *(The old table claimed the pool added them. It did not.)* `SpecWorkerPool.maxon:1000` is `async drainResultsThunk(handle.child)` — `async` on a **direct free-function call**, chosen deliberately to avoid capturing a struct. **The escape channel Phase 1 exercises is the async call's ARGUMENTS, not a closure env — two distinct channels, and only one is tested.** P1.5 must not mistake one for the other | |

### ⇒ `Set` + `Hashable` + WITNESS TABLES are PHASE 1 (decided 2026-07-13)

*"`Set` rides `Map`'s exact mechanism"* is now **false as sequencing**: **`Set` is reached and `Map` is
not.** It cannot ride a mechanism that arrives after it.

**`String.trim()` is about the most innocuous call in the stdlib, and it pulls in `Set` → `Hashable` →
`Equatable`.** This is exactly the class of thing §"the stdlib cone" was written to catch — and it
caught `Map` while missing `Set`.

The option was to cut the `trim()` → `CharacterSet` → `Set` edge in the `stdlib-shv2/` fork (sanctioned:
the PRINCIPLE binds shv2's own source, not the stdlib). **REJECTED, on this plan's governing principle:
DO THE HARD THINGS EARLY.** So **`P2.1` interfaces+witness and `P2.3` `Set`** are **promoted into Phase 1**
(as **P1.7a** / **P1.7b**), forced by a real program rather than a bespoke test. Phase 1 ≈ Phase 2 minus
`Map`, `extension`, and conditional conformance.

⚠ **One consequence is INFERRED, not measured, and it is the thing to watch.** The bootstrap
**monomorphizes**, so it discharges `Element is Hashable` as a *static direct call* and emits **zero**
witness tables (measured: zero indirect calls in the whole module). **shv2's locked design is the
opposite** — dictionary-passing + witness tables — under which `element.hash()` on a *type parameter* has
no route except a witness slot. The only architecture-matched compiler that could settle it is v1, and
**v1 NO LONGER BUILDS** (see below).

### ⚠ Two methodological corrections, both of which cost this plan a wrong answer

1. **Measure the PROGRAM, not the DIRECTORY.** The old table was *"grep-verified against
   `maxon-shv2/Testing/`"* — and `Testing/` really is clean (0 bare struct interpolations; all 13 of its
   `.toString()` calls are explicit). But **the Phase-1 artifact is `Testing/` + a `Main`**, and
   `Main.maxon:233-236`'s error reporter interpolates a bare `FilePath` — a **struct**, through
   `Stringable`. Four sites. A grep of the wrong scope reported zero.
2. **Compile it, don't grep it.** Every one of the four corrections here (`Set` used, `Map` unused,
   closures 0, struct interpolation ≠ 0) was invisible to grep and obvious to the linker.

### ⚠ The stdlib cone — the EMIT/DECLARE split, and why it decides everything

**Phase 1's boundary is NOT "what the harness uses."** shv2 parses **all 48 stdlib files**, and
the stdlib *declares* mechanisms the harness never dispatches through:

- [stdlib/Subprocess.maxon:120](stdlib/Subprocess.maxon#L120) — `typealias EnvMap = Map with String, String`.
  The harness **must** have `Subprocess` (it spawns the compiler under test) and calls
  `Subprocess.run(exe, arguments:)` — **never the env-map overload**.
- [stdlib/Array.maxon:406](stdlib/Array.maxon#L406) — `Array implements Hashable, Equatable where
  Element is Hashable and Equatable`. The harness has `Array with SpecTest` but never `==` or
  `.contains` on an Array.
- `stdlib/{Array,Interfaces,PrimitiveExtensions}.maxon` declare **`extension`** blocks the harness
  never calls into.

⇒ **The rule:**

> **Phase 1 = EMIT(what the harness uses) ∪ DECLARE(what the stdlib cone declares).**
> Declaration-level support — *parse it, resolve it, emit nothing* — is far cheaper than
> emission-level, and it is all `Map`, conditional conformance, interfaces, and `extension`
> need in Phase 1. **Phase 2 promotes each from DECLARE to EMIT.**

⇒ **The architectural obligation that makes the rule hold:**

> **shv2 must lower only the stdlib bodies a program transitively reaches** (reachability-seeded
> lowering, as both v1 and the C# bootstrap do). **This is not an optimization — it is what makes
> Phase 1 a phase.** If shv2 lowers whole files instead, `EnvMap` drags in `Map` → `Map` drags in
> the `Hashable` constraint → that drags in **interfaces + witness tables**, and Phase 2's entire
> generics tail collapses back into Phase 1.

⇒ ~~**The backstop, and why the pruned fork is UN-DEFERRED**~~ ❌ **RESOLVED BY MEASUREMENT — the fork
is NOT needed.** It was un-deferred as the lever that bounds Phase 1's surface, *"if reachability-seeded
lowering proves insufficient"*, naming `EnvMap`/`Map` as the case.

**P1.0c settled it with the machine code: reachability-seeded lowering IS sufficient for `Map`.** Zero
`Map` instantiation, zero `Map` bodies, in a 401-function emitted module. `__destruct_Environment` *is*
emitted — and its `EnvMap` arms compile to a bare **`x64.call mm_decref`**, a header-driven refcount drop
that needs only the *resolve-time* fact that the payload is managed. **It never names `Map` and never
needs a `Map` body.** That is the EMIT/DECLARE split, visible in the emitted code.

The one edge the fork could still have cut — `String.trim()` → `CharacterSet` → `Set` — we chose **not**
to cut (do the hard things early; `Set` is now P1.7b). **So the fork gates nothing. Do not resurrect it
without a NEW reason.**
- **TRAP (kept for the day someone does):** the stdlib dir's **LEAF NAME *is* the namespace**
  ([2-Parser.cs:804](maxon-sharp/Compiler/2-Parser.cs#L804)), and `"stdlib."` is hardcoded in
  [MonomorphizationPass.cs:754,522,619](maxon-sharp/Compiler/MLIR/Passes/MonomorphizationPass.cs#L754).
  A dir named `stdlib-shv2` ⇒ namespace `stdlib-shv2` ⇒ monomorphization **silently** misses every
  call site. **The path must be `stdlib-shv2/stdlib/`**, and `FindStdlibPath()` must hard-error if
  the leaf isn't `stdlib`.

**✅ DONE (P1.0c, 2026-07-13) — see §"Phase 1 — the measured boundary" above for the result.** ~~FIRST
TASK OF PHASE 1 (before P1.1): measure the cone.~~ Compile the harness with `maxon.exe`
and list which stdlib functions actually get codegen'd. That list *is* Phase 1's true stdlib
surface, and it settles the `Map` question with data instead of argument.

---

## Phase 1 — the ladder

Each rung is individually shippable, with one spec. **Core-first is a *re-ordering*, not a
replacement** — if that isn't explicit, Phase 1 becomes a six-month integration hole with no
green build.

> **A rung is COMPLETE only when it has NO open residuals.** A documented residual — a bug or gap the rung
> generated and did not close — means the rung is **NOT done**, however green its suite; its status says so
> (**◑**, not ✅). *"Core landed"* is not *"complete."* Residuals live with the rung (or its workstream) and
> are what keep it on this ladder until they close. Accepted debt measured linear-in-practice and a
> deliberate divergence are DECISIONS, not residuals — those do not hold a rung open.

| # | Mechanism | Note |
|---|---|---|
| **P1.0o** | **the compiler traces ITSELF — Workstream O1** ⭐ | **FIRST, because it is the instrument the rest of the ladder is debugged with.** shv2's stderr `Logger` dies the moment P1.0a interleaves N workers into one stream. A `__DebugStream` builtin in **`maxon.exe`** + 4 new event codes + a sink behind `Logger`'s existing API ⇒ binary events into the shared-memory ring, demuxed per-worker by `maxon monitor`. **Depends on NOTHING in Phase 1** — the bootstrap already carries the ring, the reserve, and the monitor *(see Workstream O)* |
| **P1.0a** | **grow the harness's parallel worker pool back** | **The acceptance target must exist before it can be a target.** Port `maxon-selfhosted`'s [`runAllSpecTestsParallel`](maxon-selfhosted/Testing/SpecTestRunner.maxon#L3401) worker pool into `maxon-shv2/Testing/`. Written in Maxon, compiled by **`maxon.exe`**, green under today's gates — so it lands *now*, and every later rung is measured against the real Phase-1 target instead of the serial stub. **Workstream S is what makes it pay:** the corpus takes the suite from 126 tests to thousands |
| **P1.0b** | **Workstream S — the `disabled-test:` marker, and ON-DEMAND porting** ⭐ | *(see Workstream S.)* The marker is SHIPPED (`362b07b72`). **The bulk port is NOT, and will not be** (user directive): spec files are copied from `/specs` **on demand, by the rung that needs them**, not as a corpus dump. A trial bulk sweep was run once, as a MEASUREMENT, and then discarded — see P1.0d, which is what it found |
| **P1.0d** | **complete the SCALAR CORE** ⭐⭐ | **NEW, and it exists because the sweep proved this plan's central claim false.** See "The scalar core is NOT done" below. ✅ **ALL SLICES DONE; P1.0d CLOSED 2026-07-15 — suite 126 → 355/0.** Slice 1 (parens · `true`/`false` · block scoping · void fns · top-level `typealias`) 126 → 159; then `.2` word/bitwise/chars, `.3` divide-by-zero, `.5a` top-level `let`, `.5b` globals, `.4` floats. |
| **P1.0d.2** | `not` / `and` / `or` · bitwise · character literals | ✅ **DONE.** Short-circuit `and`/`or` is control flow, so it landed as blocks + phis on the parser's on-the-fly SSA. Bitwise + chars are new `StdOp`s in the existing integer register class — **APPENDED at the END of a band** (a `match` range arm silently swallows anything inserted mid-band) |
| **P1.0d.3** | **`a / 0` ⇒ a clean panic** | ✅ **DONE 2026-07-15** — suite **279 → 281**. It escaped as a raw `0xC0000094` with **empty stderr**; it is now exit **1** + `panic: integer divide by zero` + a symbolized backtrace, and `specs/safety.md`'s `divide-by-zero` + `mod-by-zero` are ported and ENABLED. **Workstream R's first slice.** Three things this rung settled, each bigger than the divide: (1) **the fault is caught by a VEH thunk, not a divisor check** — shv2 is x64-only and the CPU raises `#DE` for free, so a `cmp`/`branch` before every `idiv` would be the scope cut the PRINCIPLE names; **NO gt redirect** (the reference's fault path rides green threads, which arrive at P1.5) — the thunk prints and exits in place, so the context travels as ordinary arguments and needs no fault globals at all. (2) ⭐ **FRAME POINTERS, on every function, leaves included** — see below. (3) the harness grew a ` ```stderr ` fence: `maxoncstderr` is the COMPILER's stderr, and a program's **RUNTIME** stderr was a thing no spec could pin |
| **P1.0d.5a** | **top-level `let`** ⚠ **the plan never listed this one either** | ✅ **DONE 2026-07-15** — suite **281 → 295**. **Found by probing, not by the ladder** — and it has **its own stable spec nobody had found: `specs/top-level-let.md`, 17 cases**, which the bootstrap passes. *(Missed twice because a grep for `global|static|module|init` does not match the filename — **look for a feature's own spec BY NAME.**)* **A module-scope constant is a NUMBER: it inlines as an ordinary `literal` at each use ⇒ no IR op, no `.data`, no relocation, NO BACKEND AT ALL.** Zero pre-existing goldens moved, which is the proof. Design: an arena of `(name, visibility, file, token range)` + a memoized DFS with cycle detection, driven inside `queryProgramSignatures` — that is what makes **forward references** and cross-file `export let` work. The evaluator **shares `TypeRules`' folds** (one opinion on constant arithmetic; only the climb loop forks, because `and`/`or` are *control flow* in a body and a *value* at file scope). ⚠ **Still `disabled-test:`: `basic-float-constant` (P1.0d.4), `file-private-same-name-cross-file` (P1.9 `as`), `from-literal-initializer` (P1.2 `String`)** |
| **P1.0d.5b** | **top-level `var` (GLOBALS)** ⚠ **another one the plan never listed** | ✅ **DONE 2026-07-15** — suite **295 → 317**. A module-scope global with a **real `.data` slot**. ⭐ **This rung created the Std MEMORY band** (`globalAddr` + `loadIndirect` + `storeIndirect`, v1's shape — NOT a fused `globalLoad`, because P1.1/P1.2 need the general pair) — **which begins retiring R1 @ P1.2's precondition** (see the R1 box: the Std-IR runtime route was blocked on the Std tier producing only `arith`/`call`). Plus `DataSectionEntry` in `GlobalDataTable`, a `.data` section in `PeWriter`, and the `dataSectionRipRelDisp32` arm. **NO `__module_init`** — initializers are constant-only, so they const-evaluate into `.data` **bytes**. ⚠ **`loadIndirect` MUST be `isPure: false`** or a global's read **would hoist out of a loop that writes it** — a silent wrong answer. ⚠ **CORRECTED 2026-07-15: this row said "pinned by a spec". IT IS NOT, AND NO SPEC CAN PIN IT — `StdOpMeta.isPure` has ZERO readers** (per-field sweep + a sabotage: flipped to `isPure: true`, the suite passes 371/0). The property holds because **nothing in the pipeline hoists**; the flag is a correct declaration awaiting DCE/CSE/LICM/the inliner. The spec pins the ANSWER, not the flag. **See OPEN.md #27.** ⭐ **shv2 does NOT inherit the bootstrap's aliasing bug (OPEN.md #17):** identity is per-FILE **by mechanism** (`fileScopedDeclKey(name, readerFilePath)`), label = bare name + `$1` **only on collision** (path-free, so goldens stay stable; `$` is structurally unwritable — `isAlphaNum` is `[A-Za-z0-9_]`). **The corpus had no `var` twin of `file-private-same-name-cross-file`, so we wrote one**: shv2 returns **118**, the bootstrap **212**. ✅ **`short-circuit-elision.md` RETIRED** — its 5 divide-by-zero workaround cases are superseded by the real corpus cases; its 9 genuine *lowering* tests moved to `short-circuit-lowering.md` (goldens are **R100 renames** — proof the codegen did not move). ✅ **`RequiredData` blocks now actually compare** (they were **silently ignored**) |
| **P1.0d.4** | **floats (f64)** | ✅ **DONE 2026-07-15 — suite 317/19 → 355/0, and P1.0d IS CLOSED.** Wave 1 (an all-caller-saved XMM pool; a float across a call force-spills). ⭐ **ONE `X64Register` enum, xmm0-15 at rawValue 16-31 — the low nibble IS the encoding number, so `and 0xF` encodes and bit 4 classifies.** The design that paid for itself: **CLASS-AGNOSTIC OPS, CLASS-DISPATCHED ENCODERS** — a move/spill/reload/swap is a class-agnostic *concept*, so there is no float spill op, no float move, no float `xchg`; the encoder picks `mov` vs `movsd` from `regClassOf`. ⇒ `SsaDestruction` needs **no float case**, the splitter no float spill op, coloring is class-blind, and a spill slot is 8 bytes = an f64. *(v1 mints a SECOND enum and pays a parallel op per emit, threading a bare `regClass` int through each — that tax is absent here.)* **The class travels FORWARD** (`ValueClassColumns`, per FUNCTION — ValueIds are function-local — produced by `StdToX64Conversion`, consumed by the allocator; the Target tier carries no types, so it *cannot* derive one). ⚠ **`operandType` is the SOURCE and the OPCODE names the result** — true only for `siToFp`/`fpToSi`, the dialect's only cross-class ops. `cmp` is ALWAYS `i1` (a float compare's answer is a GPR bool). Float `/` is `StdBinOpcode.div` on `binOp`, **not** `StdOp.div`: `idiv` faults and `divsd` does not, and purity is the band's membership rule. ⭐ **`<`/`<=` SWAP OPERANDS** into the already-NaN-correct `ja`/`jae` family (`ucomisd` sets CF on unordered) — one jump, four of six predicates shape-identical to an integer compare; only `==`/`!=` need parity, **as a second BLOCK**, so `IrBlock.CondBranch`'s one-branch-per-block invariant is never bent and v1's phi-copy miscompile is **unrepresentable, not fixed** (`float-compare-branch.md`, 11/11). **`E3009`, not a new code**, for narrowing — see the ⚠ row below |
| | ⚠ **What P1.0d.4 cost, and it is the ledger** | **The contract was WRONG TEN TIMES**, and every one shares a shape: **TYPE surfaces specified without OPERATIONS** — `operandType` with no ops consuming it, a register enum with no instructions, a float type with no conversions (**`trunc` had 0 hits in shv2**). **Nine were ONE FACT WRITTEN DOWN TWICE**, the file's own through-line: `classPoolSize` returned the FILE's width (16) where the POOL's (14) was meant *with the correct number in the comment directly above it*; `globalStdType` kept a second tag→StdType table that had diverged (no `float` arm); `notOperatorFor`'s range arm swallowed `float` **while its header boasted the `match` had FIXED that exact fall-through**; `floatCondCodeForPred` + `floatCmpSwapsOperands` had to agree with nothing making them; `valueConfinedTo` and `analyzePressure` gave opposite answers about a ∅-allowed value; the bootstrap **disagreed with itself** (`CoerceValueToExpectedKind` rejected float→int, `ConvertArgToParamType` truncated it) — **and the fix for THAT re-spelled the integer family 19 lines from its home**, which the review caught. ⭐⭐ **`Parser.mintPhi` hardcoded `argType: i64`, and its comment NAMED THIS RUNG**: *"the scaffold the float (XMM) register class will need at P1.0d.4, and that is when it acquires a consumer and a reason to be right."* **The rung gave it the consumer and not the reason** ⇒ `var f = 0.0` in a loop — the commonest float idiom there is — **crashed the emitter**, and the authored spec could not see it (its phis carry INTs). **Only extending the instrument found it.** ⇒ **READ THE COMMENT THAT NAMES YOUR RUNG** |
| | ⚠ **Deferred out of P1.0d.4, named not hidden** | ✅ **Float NAMEABLE (F1) LANDED 2026-07-19** (`fda8f73bb`, 756→761): `typealias F = float(low to high)` now parses + resolves to `MaxonType.float`, enabling the 5 `ranged-typealias.md` float-LOCAL tests (float-range, f32-range/arithmetic/comparison/to-int — float stays local via the no-op `as`-cast, `trunc`→int, no ABI). ✅✅ **THE FLOAT FEATURE IS COMPLETE 2026-07-19 ("do BOTH": F1 nameable → F2a return → F2b args).** F2a (float RETURN #50, `f8c516e33`): float-alias return types resolve to float + XMM0/d0 return codegen → `float-return-from-function` GREEN. F2b (float ARGS #57, `c428191ac`, 762→763): float param/arg in XMM/d-reg by a SEPARATE int/FP counter (v1's convention) + `buildCalleeParamClasses` + `emitCallArgMoves` → `f32-function-param-return` GREEN. ⭐ F2b review caught a real ARM64 compiler crash (shared liveness decoded float-arg regs through an x64-only door — the signature bug) — fixed x64-byte-identical. ✅ **The float NUMBER-TYPE story landed 2026-07-19**: F1 nameable → F2a return (#50) → F2b args (#57) → F3a int→float widening (`cc698eaf9`) → F3b throwing-float return (#60, `0b113576a`), suite 767/0 — nameable typealias, return, args, int→float coercion, and the throwing-return throw edge, on x64 + arm64 (cross-host golden-synced). ◑ **But floats are NOT COMPLETE — three follow-ups remain (below): `f64→f32` #19, `floor`/`ceil`/`round`, and float const-folding.** — **Wave 2 = the float ABI** (a float PARAM/RETURN needs the xmm arg slots; **measured** — `takeFloat(42.0)`, a literal with no coercion, panics in the callee's ABI reception). Win64 preserves all **128 bits** of xmm6-15 and `movsd` ZEROES 64-127, so callee-saved XMMs drag in 16-byte slots + leaf misalignment together — that is why Wave 1 is all-caller-saved. Also: **`floor`/`ceil`/`round`** (`roundsd` is SSE4.1, a three-byte escape) ⇒ their own rung; **float const-FOLDING** is rejected (E2015) — it needs software IEEE-754 in the compiler, and v1 does not do it either; **`0.0 - x` ≠ true negation on `-(+0.0)`** — unobservable until `print` (P1.2), and v1 has shipped it for its whole life; **`f64→f32` still coerces implicitly (#19)** — the same lossy-narrowing bug wearing different types, outside the float→int ruling, **unresolved** (its own lossy-narrowing rung — spec + both compilers); **`trunc` is reserved** wherever a call is parsed, silently shadowing a user function (v1 has the identical property for its whole intrinsic set) ⇒ ✅✅ **ALL THREE FOLLOW-UPS CLOSED 2026-07-22 (P1.0d.4 COMPLETE, 767→1313):** #19 was NOT "unresolved outside the float→int ruling" — the oracle settles it by RUNNING (compile-time E3005 for a literal, a runtime panic guard for a non-literal, the SAME rule P1.9 gave ints; shv2 just never built the float arm) → **slice B**. `floor`/`ceil`/`round`/`sqrt` → **slice C** (SSE4.1 `roundsd`, ties-to-even). And **float const-folding did NOT "need software IEEE-754"** — that claim was wrong at the code: the real blocker was that shv2 had no `i64↔f64` reinterpret; **slice D1** added `__Builtins.bitsToFloat` to the bootstrap (the mirror of the existing `floatToBits`), and **slice D2** folds via `bitsToFloat`→host-f64→`floatToBits`, borrowing the host's IEEE arithmetic rather than re-implementing it (a 2nd copy of what `parseFloatBits` already encodes). `-0.0` is now sign-correct (A′'s `parseNegatedNumericLiteral`). |
| **P1.0c** | **measure the stdlib cone** | against the **upgraded** harness. Cheap, and it sets the boundary — see above |

### ⚠⚠ "The scalar core is DONE" was FALSE — measured 2026-07-13

The 126 spec tests shv2 passes were all written **by shv2, for shv2**. Run against `/specs` — the real,
accumulated definition of the language, written by people who were not trying to make shv2 look good —
the scalar core scored **48 passing out of 2,746**. *Not one of the 126 self-authored tests had ever
used a parenthesis.* **This is exactly the bias Workstream S exists to break, and it broke it on the
first run.** Every item below was reproduced against the compiler, not inferred:

| Gap | Evidence |
|---|---|
| **parenthesized expressions** | `(a + b) * c` ⇒ `E2004: Expected expression but got '('`. The Pratt parser has **no grouping primary** |
| **`true` / `false` literals** | ⇒ `E2004: Expected expression but got 'true'`. The `bool` TYPE exists (comparisons make it, `if` eats it); only the literals are missing |
| **`not` / `and` / `or`** | not parseable |
| **top-level `typealias`** | ⇒ `E2015: Unsupported: top-level typealias (M1 parses only function declarations)`. **1080 cases** — the single biggest blocker in the corpus, and the plan had it scheduled LAST, at P1.9. Most `/specs` files simply OPEN with `typealias Integer = int(i64.min to i64.max)` |
| **block scoping** | `Parser.maxon` calls **neither** `Scope.pushScope` nor `popScope` — both are correct, and both are **DEAD CODE** (`Scope.maxon:267,276`). Every `if`/`while` body declares into the FUNCTION frame. One twin silently accepts an invalid program; the other feeds malformed SSA to the allocator and panics (`RegisterAllocator.maxon:645` — a **symptom**, not the cause) |
| **void functions** | **ANY** void function panics the compiler, *even if never called* (`IrBlock.maxon:238`, no terminator). A second, distinct panic fires on an explicit bare `return` (`LowerMaxonToStd.maxon:352`), whose own message wrongly assumes only `main` can be void |
| **`a / 0`** | escapes as a raw `0xC0000094` hardware trap. `specs/safety.md` requires a clean `panic` + exit 1 |
| **floats · chars · bitwise** | had **no rung anywhere on the ladder**. Folded into P1.0d (user decision): they are scalar primitives, not mechanisms. ✅ **ALL THREE DONE** — bitwise + chars at P1.0d.2, floats at P1.0d.4 |

**✅ EVERY ROW ABOVE IS NOW CLOSED. P1.0d IS DONE, and the SCALAR CORE IS — this time measured, not asserted: 355/355, from 48-of-2,746 against `/specs` when the claim was first made.** The table above is kept because the lesson outlived it: *"the scalar core is DONE"* was **false**, and it was false because the 126 tests it rested on were **written by shv2, for shv2** — not one had ever used a parenthesis. **The corpus is the only judge that was not trying to make shv2 look good.**

⚠ **AND THAT IS STILL TRUE OF THE THING YOU ARE ABOUT TO SHIP.** P1.0d.4 shipped **float negation** and **float phis** with **ZERO test coverage** — the first was caught only by porting `unary-negation.md`, the second only by teaching `scale-test` to emit a float. Both were features the rung *itself wrote*. ⇒ **Step 0 of a rung is the SPEC PORT LIST, and it is the coordinator's** (now in the rung skill, `03b4a47dc`). Inheriting a ported corpus is not doing it.

### ⭐ FRAME POINTERS, on every function — decided at P1.0d.3, but the forcing reason is **P1.5**

**shv2 now emits `push rbp` / `mov rbp, rsp` in EVERY prologue, leaves included** (user decision,
2026-07-15). It previously used frame-pointer-omitted (rsp-only) frames **while still reserving rbp** —
a **strictly dominated** state: it paid the register cost and took none of the benefit. **rbp stays
NON-allocatable; the pool is unchanged at 14.** Addressing stays **rsp-relative** — the divergence from
v1 is deliberate and narrow: **rbp is for the CHAIN, not for addressing.**

**The stack trace only EXPOSED this. The reason it is not a P1.0d.3 detail is
[`__gt_morestack`](maxon-selfhosted/Compiler/Targets/X64/X64Backend.maxon#L7094):** v1 grows a green-thread
stack by allocating 2×, copying, and then **FIXING THE SAVED-RBP CHAIN by the relocation offset** (step 6b
walks the chain on the new stack, adjusting every saved rbp in the old range). GT stacks are 2 KB initial
and **RELOCATE** on growth, Go-`runtime.morestack`-style. ⇒ **A relocating stack must find and fix every
interior frame pointer, and the chain is how it finds them.** **R3 @ P1.5 commits shv2 to exactly that
model**, so shv2 needs frame pointers there *regardless of panics*. Landing them now moved all 279 goldens
**once**; retrofitting at P1.5 moves the same goldens **then**, onto a shipped runtime. *Do the hard things
early.*

⚠ **A frameless leaf could not be spared:** you cannot unwind OUT of one without knowing its frame layout,
and the faulting `main` **is** one — so framing only "framed" functions does not help. (And because no shv2
function ever wrote rbp, rbp was *invariant program-wide*: walking `[rbp]` gave garbage, not a short trace.)
**`.pdata`/`.xdata` + `RtlVirtualUnwind` is the FPO-correct alternative and was rejected: NO compiler in
this tree emits unwind info**, so it has no port source and is its own rung — and it does **not** solve
morestack's fixup, which needs the chain anyway.

⇒ **P1.0d lands before P1.1**, and it pulls the **declaration half of P1.9 forward** (parse + resolve a
top-level ranged `typealias`; the corpus's ranges are wide, so the *checks* — `ExpandCastRangeChecks` /
`InsertRangeChecks` — stay at P1.9 where they belong). Floats need a whole new register bank (there is
**no XMM class** in the allocator today), so they are the last slice of the rung, not the first.
| **P1.0r** | **R1-core — the ALLOCATOR + refcounting runtime** ⭐⭐ | ✅ **DONE 2026-07-15** — suite **355 → 357/0**; see the closed box at the top of this file. *(Was "R1 @ P1.2". Promoted ahead of structs 2026-07-15 — see the RESTRUCTURED box: a struct is a heap value, so the heap cannot come second.)* The slab allocator · `__mm_alloc` · `__mm_incref`/`__mm_decref` · the `__destruct_*` cascade. **The allocator ALWAYS RETURNS ZEROED MEMORY, from commit 1** — a property of the allocator, not of each caller (it cost v1 three separately root-caused bugs; see ARCHITECTURE.md → "Allocator: the zeroing contract"). ⚠ **THIS RUNG MUST DECIDE THE RUNTIME'S *FORM*** — hand-assembled machine code vs. v1's `runtime.std` (6,049 lines of Std-IR text through the ordinary backend). **The excuse for deferring it has now EXPIRED: its precondition was the Std memory band, and P1.0d.5b shipped it.** See the R1 box in Workstream R — **the named risk is inertia**, 6 hand-assembled functions becoming 5,000 by default. **Write the reason down.** ⚠ Its acceptance test is **P1.1a**, not a bespoke spec: an allocator with no heap value to allocate is untestable, so the two are adjacent on purpose |
| **P1.1a** | **structs** — R1's DOGFOOD ⭐ | **✅ CLOSED 2026-07-16.** **WAVE 1** (with P1.0r: `type` · layout · `Self{}` · `static create` · field READS — 355→357). **WAVE 2** (field WRITES · mutability · defaults — 357→**371**). **WAVE 3** (INSTANCE METHODS · `self` · field VISIBILITY E3014 · E3011-at-fields — 371→**399**; see the closed box at the top). The receiver question resolved: it does **NOT** force P1.4's borrow-vs-consume ruling — both references AGREE the receiver borrows (the deferral's "the references disagree" reason does not hold for it), the corpus requires it, and the consume sink is unreachable at this rung (a struct-typed field is uninstantiable). *(was the struct half of `P1.1`.)* **trivial-ownership only** — scalar/float fields, destructor NULL, no field write increfs. Heap-boxed via `__mm_alloc`, **uniform 8-byte field slots** (`sizeof(Point)`=16, `sizeof(Outer{p Point, n Integer})`=**16** — a struct field is a POINTER). Field access = `loadIndirect`/`storeIndirect`. Methods = ordinary direct call, `__self` at param 0. `Self{…}` construction restricted to the type's own methods (**E3076**) |
| **P1.1b** | **enums + `match`** | **✅ COMPLETE 2026-07-22 (→1316/0). All residuals closed:** #33 (negative float-backed enum — `readEnumCaseInto` now folds a leading `-` into the sign bit via `negatedFloatBits`, the positive path byte-identical), #34 (union wording — shv2 already emits "match on union" via `EnumLayout.kindWord()`; pinned by `error.union-not-exhaustive`; the bootstrap's "enum" is a maxon-sharp finding), #58 (mixed-type `match … gives` → E3005, closed with the value-merge cluster). #31 cross-class match-expr promotion CLOSED 2026-07-22 (value-merge cluster). ⚠ Bootstrap-side divergences filed, NOT shv2's phase: #34 wording, and the `-0.0`/`+0.0` enum-raw-value fold (bootstrap rejects the pair as duplicate `'0'` folding `-0.0`→`+0.0`; shv2 keeps them bit-distinct — ONE equality governs both its dispatch and its duplicate check, pinned by `float-backed-signed-zero`). **(core landed 2026-07-16, sliced into two waves.)** **WAVE A** — `match` on SCALARS (stmt + expr, value/or/range/default, block-arg `gives` join; 399→462). **WAVE B** — payload-free `enum` + `union` + enum match + the OPEN #21 ordinal range arms (462→**508**); see the closed boxes at top. **NO new IR op** in either — a match is a `condBranch` chain with a block-arg `gives` join (NOT a result slot, the shv2 thesis); an enum is a parallel-column `EnumLayout` + tag constants, `Color.red` ⇒ its tag. **The OPEN #21 range rule** (OR of covered cases by declaration index, two-compare range only where no uncovered tag intrudes) is from the bootstrap `645a690cc` — v1's range code was the unfixed bug — and verified against the oracle. `union` = enum. *(was the enum/match half of `P1.1`.)* |
| **P1.2** | **`String` + ownership + drops** ⭐ | **THE CRUX — SLICED into 4 waves 2026-07-16..17** (survey: too big for one; seam = the heap-alloc / leak-gate boundary). **✅ COMPLETE (Waves A–D), `specs-shv2` 508 → 611/0. ✅ #40 (nested borrowed→owned UAF) CLOSED 2026-07-23 by retain-promotion; ✅ #43 (self-append) CLOSED 2026-07-22 with #105.** A: string LITERALS (immortal rdata, `capacity=-2`, NO alloc) + `print` (→`__print_string`, a `WriteFile` runtime fn via `iatCall`) + `==`/`!=` (→`__str_eq`); **508→523**. B: primitive INTERPOLATION → the first OWNED heap String (ONE `__mm_alloc`'d fused record, bytes INLINE at `record+48`, `parent=-3`) + int/bool/expr converters + STATEMENT-scoped temp-drop (per loop iteration) + a per-value `valueOwnsHeap` provenance column; **523→564**. Then the DROP MODEL was hardened (return-ownership + nested-block scope-exit drops + drop-on-reassign, `564→587`, user: "leaks are not ok"). C: static single-owner **MOVES + use-after-move** (`let u=t`/`s=t` MOVE, source poisoned, read = **E3102**, reassign REVIVES, conditional poison conservative), **587→597** — shv2's FIRST ownership program-rejection; fixes `s=t` double-free; then extended to field/method/store sites (OPEN #42, **597→602**). D: **`String.append` grows to an EXTERNAL buffer** (`__str_append` detach: `__mm_alloc` raw bytes, free old root, blit) + a String's OWN drop (`emitDecref` tag-routed → `__str_decref` frees the external buffer) + `byteLength()` + `E3019` on `.append` of a `let`; **602→611**. ⚠ **Wave D deviations, both ACCEPTED:** `__str_decref` STATIC dispatch (the dynamic `funcAddr`/`callIndirect` `__destruct_String` cascade is forced only by a struct/array FIELD holding a String ⇒ **P1.4**), and eager `var`-String heap promotion (a `var` on borrowed rdata is read-only). **⇒ ✅ #40 CLOSED 2026-07-23 (main `7057bf8ad`+`b9f13dcac`) — the REACHABLE UAF (P1.2 depth-model × P1.4a borrowed-aggregate PARAMS) fixed by RETAIN-PROMOTION (user ruling: approach A); see the #40 entry in Future rungs. ✅ OPEN #43 (`s.append(s)` self-append) CLOSED 2026-07-22 with #105.** P1.2's own String constructs are all correct (the buggy `borrowedToOwned` branch is unreachable by any String program — String vars are promoted owned at declaration; #40's trigger was a P1.4a borrowed-aggregate param re-bound to a `var`, now fixed); **P1.2 is ✅ COMPLETE.** **A `String` IS its `__ManagedMemory`** — ONE fused 48B record (`buffer`@0, `length`@8, `capacity`@16, `element_size`@24, `parent_ptr`@32, `isAsciiFlag`@40), NOT a 16B envelope (⚠ **v1 NEVER fused it — v1 is the envelope form, TWO allocs; the BOOTSTRAP is the layout reference, v1 only for the `parent_ptr` sentinels**). Owned bytes INSIDE at `record+48`, `parent_ptr=-3`; growth DETACHES; `capacity=-2` rdata sentinel; synthesized `__destruct_String`. **Rides P1.0r's `__mm_alloc`.** mm-trace gates from Wave B. **⭐ OWNERSHIP MODEL = STATIC single-owner (drop at scope exit, moves transfer, no runtime refcount) — shv2's thesis, NOT the refcounting BOTH references use.** The full borrow-vs-consume ruling (String as a user-fn PARAM) stays **P1.4**, exactly as P1.1a deferred `struct-param`. `toByteArray()` is a MOVE (needs Array — P1.7); its COW-view specs become use-after-move errors, not ports. ⚠ v1's two-result-capture miscompile (`sequentializeCallResultCapture`) is a **P1.4** precondition (it bites `try`/`otherwise`), not P1.2 |
| **P1.3** | **owned payloads in enums/unions** | *moved into Phase 1* — `compilerError(text String)`, `fail(reason String)`. **SLICED: scalar first, managed next.** **✅ SLICE 1 CLOSED 2026-07-17 (611→617):** SCALAR-payload unions are heap-boxed (`8+maxArity*8`, tag@0, slot`i`@`8+i*8`, `emitEnumBox`, trivial drop) + E3066 (union `==`/`!=`/`<`… = box-address compare, refused) + boxed-union RETURN rejected (E2015, interim; leak else — OPEN #44). ⚠ **The corpus unlock was over-counted — owned payloads alone unlock ~1 case; the payload-union corpus is gated on COMPANION features** (`.unionCases` + `.name`/`.rawValue` accessors, `for-in`, union-PARAM P1.4), a separate rung. **✅ SLICE 2 CLOSED 2026-07-18 (617→641):** MANAGED (String+struct) payloads — move-in (E3102 on source reuse) / move-out (box slot nulled, scrutinee `partiallyMoved`) / tag-conditional static-cascade drop (`__destruct_<U>` → `__str_decref`/`__mm_decref`, null-guarded). A corrective rung (`fa685a7a1`) also fixed TWO pre-existing reachable match-flow bugs the review found: a temp-scrutinee leak (101) and an owned-`gives` double-free (0xC0000005). ⚠ Residual OPEN #46 (bootstrap fixpoint bypass); **#45 (give-type-consistency) ✅ CLOSED 2026-07-22** — the value-merge cluster's `checkGiveTypes` catches a String-vs-int give mismatch (E3005 match / E2028 ternary), pinned by existing match-expr-divergent-class + ternary-expression tests (recon-verified, no segfault). **⇒ P1.3 owned-local payloads DONE (scalar+managed); cross-call param/return = P1.4 (#44); the companion/accessor rung unlocks the bulk of the corpus.** Errors (P1.4) want it too: the harness calls `e.displayReason()` |
| **P1.4** | moves + borrows (NLL) · **errors** | first program-rejection point. **SLICED: P1.4a cross-call OWNERSHIP + P1.4b ERRORS (separate cleanly).** **RULING 2026-07-18 (user):** params BORROW-by-default, consume-by-use; returns ADOPT. **✅ P1.4a WAVE 1 CLOSED 2026-07-18 (641→647):** BORROW struct/union params + ADOPT struct/union returns (parse-time `named`→`structRef` re-tag; ownership unified onto the `valueOwnsHeap` BIT not the tag; `mintOwnedCallResult` adopts struct/boxed-union; **OPEN #44 closed**; closed a pre-existing managed-struct-field UAF via E2015). **✅ P1.4a WAVE 2 CLOSED 2026-07-18 (647→670):** CONSUME-into-field (struct-with-managed-field + `__destruct_<Struct>` cascade + a direct-sink param-consume analysis; transitive fixpoint E2015-deferred) **+ a PATH-SENSITIVE move model** — the review found a PRE-EXISTING conditional-move leak (`movedFrom` was monotonic → a value moved on one branch leaked on the not-moved path); user ruled the real fix, so drops are now reconciled at every join (drop on the LIVE edges, no runtime flags). Conditional moves accepted; outer-move-in-a-loop rejected (E2015, the back-edge double-free boundary). **⇒ P1.4a DONE; REMAINING P1.4 = P1.4b = errors**: `throws`/`try`/`otherwise` (v1's dual-register `(value, errorFlag)`, verbatim; needs `sequentializeCallResultCapture` for the 2-result capture) + drops on the error edge. **~201 corpus + 36 harness sites** (errors dominate). ⚠ OPEN #48 (`.spec-tmp` race), loop-exit-reconcile follow-up; **#47 (field-default reject leak) ✅ CLOSED 2026-07-22** — the P1.4b/P1.5 drop-on-error-edge work already freed those allocs (no live leak; the always-on `__mm_leak_check` proves the E2015-reject path exits 1 not 101), now pinned by a committed field-default-reject leak-guard test (`035eefdaa`). **#96 (out-of-range ranged field default compiles silently) → RE-SCOPED to P1.9** — a field-default range-check gap the BOOTSTRAP SHARES (it runtime-range-checks ARGS — `takeSmall(500)` panics "Range check failed" — but silently accepts a field default `= 500`); range checks are P1.9 (`InsertRangeChecks`/`ExpandCastRangeChecks`), so the field-default constant check lands there, NOT as a fix-now (recon-confirmed 2026-07-22). |
| **P1.5** | **closures + `async` + escape → `shared`** ⭐⭐ | **SLICED 5 ways (A1→A2→B1→B2→C, see the closed boxes at top) — P1.5 is IN PROGRESS and NOT COMPLETE (async residuals #88/#89/#93 → Workstream R, ✅ #87+#92 GT/scratch reclamation CLOSED 2026-07-23 B1c `b9bc8c98b`; front-end residuals #73/#74/#75/#79 → "P1.5 residuals" — the #31/#80/#81 value-merge cluster + #78 (float indirect calls) ✅ CLOSED 2026-07-22 (main `a1515dfb7`); #64 (borrowed-container field throw RETAINS) + #65 (implicit-self) ✅ CLOSED 2026-07-23, #68 statement-position calls ✅ CLOSED). ◑ A1 (non-capturing function VALUES) core landed 2026-07-20, 792→813/0 on x64+wasm** (NOT complete: arm64 un-restriction #67 remains; statement-position calls #68 + wasm/x64 non-i64 return #78 CLOSED). **THE THREE ARE ONE MECHANISM, AND THEY CO-LAND — this is the plan's "do the hard things early" in its purest form.** Capture-into-heap **IS** escape: a closure captures into an env block; a green thread captures into a task frame. Escape analysis is needed for heap correctness regardless — so build all three together and `EscapeAnalysis` gets **both** capture channels *from birth*. Land escape single-threaded and add `async` later and you bolt a **second capture channel** onto it: v1's `sys.dropTypeParam` split-brain mistake, exactly. Minimal closure = int capture, 0-arg, heap env, uniform `(args, env)` ABI (v1 lifts at parse time). Minimal `async` = `async`/`await` + Promise + the worker pool's needs. Escape is the **only** place refcounts appear. **Track `% values promoted to shared`** — if it's 40%, static ownership bought nothing. **Runtime slice R3 lands here** (the GT scheduler + async subprocess stdio) |
| **P1.6** ✅ **COMPLETE** | **generics + layout descriptors** ⭐ | declarations + instantiation. **SLICED A/B1/B2/C — ALL CLOSED.** **✅ A** (957→966/0) TYPES over trivial args, front-end. **✅ B1** (→989/0) `sizeof(T)` via a live descriptor READ + dictionary-passing. **✅ B2** (→998/0) MANAGED type args OWNED + dropped-once via STATIC per-instance destructors + a managed-only call-site consume (drop is STATIC-DISPATCH `decrefCalleeFor`, NOT a header ptr, so **NO `funcAbs64InRdata`/`destroyFunc@40`** — that reloc path is DEFERRED to opaque-`T` runtime drop → **P1.7 `Array`**). **✅ C** 2026-07-22 (→1011/0 host, 947/0 wasm) PER-INSTANCE ranged typealiases (nominal per instantiation, front-end only). ⏭ #94 (generic-instance union payload can't be match-bound — beyond-scope, P1.7-adjacent) is in "Future rungs". ⚠⚠ **REOPENED ✅→◑ 2026-07-28.** ✅ **BARE / FLOAT TYPE ARGUMENTS REFUSED — DONE, MERGED** (impl `65e3ae265`+`e3e0d6187` · review `bc7803c8e`; x64-windows **1942→1951/0**, x64-linux **PASS**, wasm **1840/0**, builds 0 warnings, no 101, fragments additions-only, scale **Δ0 every rung**; arm64 SKIP — remote, UNVERIFIED). **THE RED:** `typealias FloatBox = Box with float` **PANICKED THE COMPILER** (`X64Backend.maxon:715`, a cross-register-file move) while `Box with int` was **silently ACCEPTED** — the oracle refuses both (`RejectBarePrimitiveTypeArgs`, `2-Parser.cs:7577-7584`; predicate `IrType.IsBarePrimitive`, `MlirType.cs:74-76` = `byte|int|float`, **`bool` deliberately excluded** as "already a constrained type"). ⭐ **ONE MISSING RULE, TWO SYMPTOMS** — and PLAN's filed description was wrong in both directions: it said *"`Box with float` under ANY `where T is Equatable/Comparable` constraint"*, but the **minimal reproducer is `FloatBox.create(1.5)`** with no constraint, no comparison and no method call. **THE DOOR** is `Parser.checkGenericArgType`, whose `alreadyConcrete` early-out (`Parser.maxon:6031`) WAS the hiding place: `parseTypeReference` mints a bare keyword as a CONCRETE tag (`7671-7682`), never `named`, so it never entered the name cascade — ⚠ **extending `denotedNamedType` would not have fired at all**, which the survey caught before the wave. **TWO CODES, ONE MESSAGE TEXT** (`floatTypeArgMessage`): **E2061 `GenericBarePrimitiveArg`** (bare `int`; shv2 has no `byte` keyword) keeps the oracle's *"use a ranged typealias instead"*, **verified to actually work**; **E2062** refuses a float type argument **HOWEVER SPELLED** — coordinator ruling, because a ranged float alias hits the **SAME panic**, so E2061's advice would have routed users into a crash. Two codes because they have different LIFETIMES (E2061 permanent; E2062 retires when the float register class lands); one message because one rule must not drift into two wordings. ⛔ **NOT E2003** — that is `ParserExpectedType` in shv2's registry, and PLAN had already recorded a prior rung correctly refusing that number; ⛔ not E3011, which means *"this name denotes nothing"*, a different fact. **Column is a deliberate divergence**: shv2 reports at the OFFENDING ARGUMENT, the oracle at the alias name on the left of `=`; shv2's sibling E3011 in the same function already points at the argument. ⭐ **THE IMPLEMENTER REFUSED THE COORDINATOR'S OWN HINT WITH A MEASUREMENT** — resolving via `resolveFloatAliasType` cost **+16/+30/+58 allocations across three rungs** (a heap box per named type argument via `signatures.aliasOf`'s payload union, linear in program size); the float answer is instead taken off `denotedNamedType`'s result, **already computed one line later and discarded into `_`**, which erases through the non-boxing `aliasUnderlyingInAnyFile` ⇒ Δ0 restored. The review then found that reuse is not merely cheaper but **MORE CORRECT**: `namedIsFloatAlias`/`rangedAliasTag` are FILE-SCOPED and would have missed a cross-file float alias, which it probed. ⭐⭐ **THE REVIEW FOUND THE DOCUMENTED ORDERING WAS NOT LOAD-BEARING** (`bc7803c8e`): four places — both code headers, both registry entries, the spec prose — asserted E2062-before-E2061 resolved an OVERLAP (*"a bare `float` satisfies BOTH rules, and E2061's advice is a TRAP for it"*), but the code tested **two DISJOINT tags** (`tag == integer` vs a bare float tagged `float`), freely swappable with zero effect. **Prose and code disagreeing, with nothing forcing agreement — this file's signature bug in its most deniable form, because the two copies were of DIFFERENT KINDS and no reviewer of either alone sees it.** Cured by stating it once (`TypeRules.tagIsBareNumericKeyword`, the shv2 twin of the oracle's `IsBarePrimitive`) and **PROVED LOAD-BEARING BY SABOTAGE**: disabling the float arm now prints the trap message verbatim and turns two specs RED, where before the same experiment produced **no diagnostic at all** and the panic returned silently. **PORTS NOTHING AND SAYS WHY** — coordinator-verified: `specs/ranged-typealias.md`'s `error.bare-shorthand` is a **DIFFERENT RULE** (`typealias Integer = i64`, the RHS sized-shorthand) with a different trigger, message and column, and **neither corpus contains a single case writing `with int`/`with float` as a type argument** ⇒ blast radius measured at **ZERO**, and the 24 `with bool` / 304 `with String` / 600+ user-type sites are the regression set. Deliverable is 9 authored cases in `specs-shv2/generic-types.md`. **⇒ THREE RESIDUALS, all measured, all needing their own rung:** · ⬜ **(1) A FLOAT VALUE AT A `T`-TYPED FORMAL STILL PANICS — with a LEGAL type argument.** `typealias IntBox = Box with Integer` … `IntBox.create(1.5)` ⇒ `X64Backend.maxon:715`, **coordinator-reproduced**; `Box with String` identical. No float anywhere in the type arguments, so E2062 cannot see it, and the non-generic control `takeInt(1.5)` is a clean E3009. **A DESIGNED-IN P1.6-A ADMISSION, not introduced here** — `TypeRules.tagIsIntegral` answers `false` for `typeParameter` *deliberately*, "so that a float into a `T` does NOT read as a lossy conversion". ⚠ **Needed a RULING because the two candidate fixes give DIFFERENT RIGHT ANSWERS**: either a generic call site SUBSTITUTES its instance's type arguments (making `IntBox.create(1.5)` the ordinary E3009 the non-generic path already gives), or a float actual at an opaque formal is REFUSED outright (which would be the wrong answer for `Box with Integer`). This rung strictly SHRANK the panic's reach; it did not widen it. ✅ **USER RULING 2026-07-28 — SUBSTITUTE THE TYPE ARGUMENTS.** A generic call site resolves `T` to the instance's argument BEFORE the actual-vs-formal check, so `IntBox.create(1.5)` and `StrBox.create(1.5)` both land on the SAME E3009 the non-generic `takeInt(1.5)` already gives — **one rule, both paths**, rather than a second rule that decides from the formal's SHAPE and would answer wrongly the moment a float type argument becomes legal (E2062 is explicitly temporary — it retires when the float register class lands). ⚠ The obligation this creates: the substitution must reach **every** call-site argument check, not the one the reproducer walks — `TypeRules.tagIsIntegral` answering `false` for `typeParameter` stays deliberate and is NOT the thing to edit. · ⬜ **(2) THE int→float PROMOTION FAMILY — and it contains a SILENT WRONG ANSWER LIVE ON `main`.** `Self{v: 3}` into a **bare `float`** field **compiles and runs, storing the raw int bit pattern** — the field is neither `3.0` nor `0.0` but ≈**1.5e-323**, and `trunc(p.v)` yields **0**. The language's rule is settled by MEASUREMENT: an int literal in a float context **coerces** (`half(6)` with `x float` ⇒ 3, and the same through a ranged float alias parameter). The doors that do it are `LowerMaxonToStd.widenIntArgsToFloatParams:1502` and `Parser.floatWidenedWitnessArgs:19379`; **the struct-literal field store has no twin, and neither does `return`** (`return 3` from a `returns float` function **panics** `X64Backend.maxon:715`). ⚠ **NO ORACLE FOR THE DECISIVE CASE — the bootstrap CRASHES** on `Self{v: 3}` (`E9001 … float value %0 has no FP register and no stack home`), so this needed a design ruling, not a lookup. ✅ **USER RULING 2026-07-28 — AN INT LITERAL IN A FLOAT CONTEXT COERCES, EVERYWHERE, FROM ONE SHARED DOOR.** `Self{v: 3}` stores `3.0` and `return 3` from a `returns float` function returns `3.0`, because the language's rule is **already settled by measurement on the parameter path** (`half(6)` with `x float` ⇒ 3, and the same through a ranged float alias parameter) and a second answer for the field store would be ONE FACT WITH TWO ANSWERS. ⚠⚠ **THE SHAPE OF THE FIX IS THE POINT: the two existing doors (`LowerMaxonToStd.widenIntArgsToFloatParams:1502`, `Parser.floatWidenedWitnessArgs:19379`) and the two missing ones (struct-literal field store, `return`) must be SINGLE-SOURCED into one derivation — four copies of a widening rule is this file's signature bug, and adding the missing two as copies would ship it rather than cure it.** ⛔ **WIDENING ONLY** — implicit float→int NARROWING stays an ERROR (prior user ruling; `/specs` contradicts itself there and the ruling is the tiebreak). ⇒ **the bootstrap's `E9001` crash on `Self{v: 3}` is a BOOTSTRAP ORACLE BUG and belongs on that list** — the oracle cannot be consulted here until it is fixed, and the ruling above is what shv2 is measured against instead. · ⬜ **(3) A RANGED FLOAT ALIAS ON THE FIELD PATH RESOLVES TO `int`** (E3009 on legal code), because `SignatureIndex.maxon:4775` re-tags **function** aliases and **generic** aliases but not float ones — the missing third sibling in a block that exists for exactly this reason. ⚠ **The coordinator first ruled this fixable at `Parser.maxon:5427/6888` and the implementer DISPROVED IT BY MEASUREMENT** — the edit had byte-identical, zero effect, because field types are read through `ProgramSignatures.fieldTypeOf`, populated by the declaration SWEEP, not from where the parser reads them. ⚠⚠ **(2) AND (3) MUST LAND TOGETHER**: fixing the field's TYPE alone converts alias-field + int-literal from a clean E3009 into (2)'s silent wrong answer — i.e. it would INTRODUCE a miscompile. ⭐ Note a **bare `float` field works END TO END today** (store, `* 2.0`, compare — coordinator-verified), so (3) is a missing door, not the register-class limitation. **ALSO FILED (out of rung):** `typealias BoxBox = Box with (Box with String)` is a **PASSING shv2 test** (`generic-types.md` `nested-managed-cascade`) that **the oracle CANNOT PARSE AT ALL** (`E2003: Expected type name`), in a spec file that exists only in `specs-shv2/` — coordinator-probed; and shv2 permits bare `int`/`float` at **parameter/return/field** positions where the oracle refuses (`E3005`), a wider ranged-type-discipline gap with **no recorded rationale anywhere** and a corpus-wide blast radius, which is why this rung deliberately refused at the TYPE-ARGUMENT position only — the one position where the omission produced a compiler panic rather than mere permissiveness. |
| **P1.7** ✅ **COMPLETE** | **`Array`** | = P1.6 ∘ P1.2 — the first real integration proof (managed elements → element-destroy through the descriptor). ⇒ unlocks **`b"…"` byte-string literals**. **SLICED A/B/C/D — ◑ ALL BUILDABLE SLICES CLOSED (1011→1162/0, +151).** ✅ **Slice 1** (scalar core, →1079/0, `94b4f667c`) + ✅ **Slice 2** (byte-string, →1100/0, `82e8f35a9`) + ✅ **Slice 3a** (CONCRETE managed elements — element-destroy via `funcAddr`; →1140/0, `15ace5d01`) + ✅ **Slice 4** (slice/append/clone trivial + a systemic negative-index OOB fix; →1162/0, `3c65ccde2`) CLOSED — see the closed boxes at top. ⏳ **Slice 3b (opaque-`T` managed arrays) — user APPROVED building it (2026-07-23); split into 3b-i (substrate, ✅ LANDED `2dda47a3c`) + 3b-ii (the reloc, remaining).** ✅ **3b-i** built the 3-mechanism generics-depth substrate the reloc needs — nested generic-instance typealias in a generic body, opaque-`T` `Array`-typed field + drop cascade, and static/receiverless layout-descriptor threading (`FuncSignature.takesReceiver()`; the shared body reads the element's destroy fn from the descriptor `destroyFunc@40` at run time — 0 for a trivial element) — unlocking `basic-nested-array`; a MANAGED opaque element is cleanly REJECTED (E2015) until 3b-ii. This substrate is what **P1.7b `Set` / P2 `Map`/`Vector`/`List`** also need (see the 3b-i closed box + the "Future rungs" reloc design map). ⏭ **3b-ii — the ONE remaining P1.7 build item — the `funcAbs64InRdata` reloc** that fills a MANAGED instance's `destroyFunc@40`. ✅ **borrow-on-get `try…otherwise <owned>` bump-masked UAF — RESOLVED on main** (incref-on-get, `936b22ab3`: `__mm_incref` the ok-edge borrow so the merged phi is owned and dropped once after the read; pinned + green under #108 poison) — no longer an open thesis call. Other residuals: **✅⭐ Array slice COW — BOTH SLICES CLOSED 2026-07-22, `.slice` is now O(1).** Slice A (detach-on-write, `12705a7d9`+`66bd13c24`, →1171): fixed FIVE reachable segfaults (`set`/`clear`/`remove`/`pop` on a `b"…"` literal wrote read-only `.rdata`; `resize(-2)` wrote BELOW the buffer — now abort 73), with the check in the emitted RUNTIME FN not at the call sites (both references' call-site lists had DRIFTED into real holes). Slice B (zero-copy views, `14a0aaa75`+`cd77c6805`, →**1199/0**): **469 ms → 15 ms on 500 slices of a 200k array, asymptotically not by a constant** — a NEW DESIGN, not a port, since NEITHER reference zero-copies an owned-array slice; `__mm_incref` returned, refcounting the BUFFER (a separate allocation) rather than the record, which is the unambiguous "someone views me" signal the bootstrap said it could not get. See the Future-rungs entry for the full design + the sabotage results.
⚠⚠ **THIS ROW WAS THE STALER OF P1.7's TWO, AND THE DRIFT RAN OPPOSITE TO THIS FILE'S OWN TIE-BREAK RULE.** The "Status at a glance" header says *"when a status here and the detail below disagree, the detail wins"* — but this "detail" row still called **3b-ii** *"the ONE remaining P1.7 build item"* long after 3b-ii, 3b-vi, 3b-vii, both COW slices, the borrow-on-get UAF and reserved-space disjointness had all closed and been recorded in the ladder row. **Two rows describing one rung is the file's own signature bug — ONE FACT WRITTEN TWICE — and it cost this rung a full survey to find out which entries were still true.**
**✅ P1.7 CLOSED 2026-07-27 — the residual list above is resolved, entry by entry, and every survivor is re-homed to the mechanism that actually owns it:**
- **`>6-param`** — ✅ CLOSED by the STACK ARGUMENTS rung (`MaxAbiArgs = 64`).
- **module-level / top-level managed constants** — ✅ CLOSED (slices 1 / 2a / 2b / 3, `12dcd9a36` · `31732e606` · `b08622781` · `7cc15f25f`).
- **`element_size` 8-vs-1** — ✅ CLOSED by finish-Slice 2 (`679e023a9`); it was never a P1.8 item, it was a live oracle divergence (shv2 aborted 72 where the oracle returns 4).
- **nested-union payload (union-in-union)** · **`.rawValue` via a field chain** · **enum-case match pattern** — ⬜ **RE-HOMED, not closed.** Each is a *union / enum-field* mechanism that an array merely happens to reach; none is Array's. They keep their disabled cases and their measured blockers, now stated as their own mechanisms rather than as P1.7 residuals.
- **E3070 borrow-liveness** — ✅ **CLOSED 2026-07-28 as its own rung, as it always was** (the COW design fork deliberately took (A) buffer-refcounting *over* (B) borrow-liveness; the true mutate-while-borrowed pass was never P1.7's to build). See the Future-rungs entry for the outcome — and note that P1.7 Slice 3a's residual (a), which named *"a THESIS DECISION for the user: incref-on-get OR copy-on-get OR E3070 borrow-liveness"*, is decided by it: **E3070, and the borrow model keeps its zero refcount traffic.**
⭐ **THE RULE THIS RUNG PROVES, worth keeping:** a rung's residual list is written at the moment the rung closes and **nothing re-checks it afterwards**, so entries outlive their causes in *both* directions — three of these were already fixed elsewhere, one was a genuine live divergence mislabelled as deferred-to-P1.8, and three were other mechanisms' work all along. **Re-measure a residual list before believing it.** |
| **P1.7a** | **interfaces + witness tables** ⭐ **(promoted from P2.1, 2026-07-13)** | Static conformance (`Hashable`, `Equatable`, `Stringable`). **No existentials** — shv2 stores nothing at interface type. **Forced into Phase 1 by MEASUREMENT (P1.0c):** `Set`'s element constraint needs `Character.hash`/`.equals` dispatched, and `Main.maxon:233-236` interpolates a bare `FilePath` **struct** through `Stringable`. Under dictionary-passing there is no route to `element.hash()` on a type parameter *except* a witness slot. ⇒ promotes the stdlib's interface decls DECLARE→EMIT, and unlocks `"{userStruct}"` |
| **P1.7b** | **`Set` + `Hashable`/`Equatable`** ⭐ **(promoted from P2.3, 2026-07-13)** | **Forced into Phase 1 by MEASUREMENT (P1.0c), and by nothing the harness wrote:** `String.trim()` (13 sites) → `CharacterSet.whitespacesAndNewlines()` → `typealias CharSet = Set with Character` ([CharacterSet.maxon:19](stdlib/CharacterSet.maxon#L19)). *"`Set` rides `Map`'s exact mechanism"* is **false as sequencing** — **`Set` is reached and `Map` is NOT.** The `stdlib-shv2/` fork could have cut the `trim()`→`CharacterSet` edge; **REJECTED — do the hard things early.** `Map` stays in Phase 2 (multi-param generics; genuinely unreached) |
| **P1.8** ◑ **SLICE A DONE** | `String` methods · `for-in` | real `String.equals` body (struct-`cmp` → `methodCall`); hardcoded `for-in` over Array/Range/String. ⚠ **`trim()` lands here and it is the thing that dragged `Set` in** — so P1.7a/P1.7b must precede it. ✅ **Slice A (`for` over ranges + Arrays) COMPLETE 2026-07-28 →1906/0** — see the ladder row. ✅ **C — the SEVEN byte/ASCII String methods — COMPLETE 2026-07-28** (`startsWith` `endsWith` `contains` `toLower` `toUpper` `replace` `split`; 2003→2036/0 on its base, **2056/0** after rebasing onto upstream; x64-linux PASS, wasm32-wasi PASS, build 0 warnings, no 101; ⚠ **arm64 SKIPPED — UNVERIFIED**). ⭐ **THE SLICE'S LINE IS THE INDEX TYPE, NOT THE ALGORITHM, AND THE PLAN HAD IT WRONG**: this row said TEN methods "which need no UCD", and the survey measured that false for three — `findFirst`/`findLast` return a `StringIndex` whose `charIdx` is a GRAPHEME index and `slice` takes two of them, so all three are grapheme-indexed BY SIGNATURE however byte-level their search is. They move to **B**, and their 11 cases were re-marked with that measured blocker (one had been blaming "P1.2 wave D" since before a String method dispatcher existed). ⭐ **Only ONE reference exists here** — v1 has no String-specific dispatch and the bootstrap inlines only four byte primitives, so both execute the same `stdlib/String.maxon`; no divergence to resolve. Two of its DECISIONS were deliberately not ported: no `StringBuilder` (`replace` uses one as a fixed buffer — the two-pass COUNT is the part carrying the property), and no `pos <= selfLen - oldLen` guard (`__str_find` already answers "cannot fit", and that subtraction is where the stdlib's shape underflows). `split` is the first builtin CONSTRUCTING an `Array with String` at runtime — the array is created by the parser through `emitArrayCreateOp`, the one array-creation door, so it cannot disagree with a literal about element size or the `element_destroy@40` stamp. ⭐⭐ **THE OPTIMIZER'S LADDER FOUND A QUADRATIC IN `__str_append` DATING TO P1.2 — see the ladder row above and `docs/optimization-log.md`.** Review found `__str_split`'s two segment producers were one rule written twice (and that rule IS "a segment is a copy, not a view", which is what keeps `__arr_decref` from double-freeing), and that `replace`'s two-pass sizing had a guard ONE CHARACTER wide — sabotaging the count stride wrote 2 bytes into a 1-byte allocation and exactly one test noticed, by one character; the new case uses replacements LONGER than their needles so a divergence disagrees about SIZE. Ports `specs/string-split.md` byte-identical; adds shv2-authored `string-methods-ascii.md`. ⬜ **B/D REMAIN: B** (`Character` + graphemes + `for c in s` + `findFirst`/`findLast`/`slice`), **D** (R2/UCD + `CharacterSet` + `trim()` + `Map`). ✅ **B — `Character` + UAX#29 + `count()` + `for c in s` — COMPLETE 2026-07-28** (main `857bca80b`..`370e46838`; 2056→2114/0 on its base, **2129/0** rebased; x64-windows / x64-linux / wasm32-wasi all PASS; ⚠ **arm64 SKIPPED — UNVERIFIED**). A real **`ValueTypeTag.character`** (user ruling) rather than a zero-arg generic instance, because `Character` is not generic and `Set with Character` would have pushed Slice D into nested-instance territory. ⭐⭐ **THE RUNG'S REAL CONTENT WAS A PREDICATE, NOT A TYPE: `tagIsText` was ONE QUESTION ANSWERED FOR 29 CALLERS**, and a second byte-record type is what separated them — 13 sites ask about the **TYPE** (`print`, `panic`, Set key, match scrutinee; a Character is correctly refused at every one) and 16 about the **REPRESENTATION** (dropped/cloned/promoted/moved), now `tagIsByteRecord`. Sabotage: 19 red / 3 red / 1 red. ⭐ **EVERY STAGE FOUND WHAT THE ONE BEFORE HAD PASSED**: the implementer's own sabotage turned up a COVERAGE HOLE, and probing it found a leak (the minted element carried no owned-heap provenance — two allocs, one free per trip, exit 101) and a wrong type (`var found = 'é'` typed the binding String); the coordinator found **`c == 'a'` REFUSED** — the commonest character operation there is, on this rung's own headline feature — which the implementer had filed as a documented divergence on the grounds that *no enabled case distinguished the models* (that was the hole, not the licence); the optimizer found the SLAB (below); and the review found a **REACHABLE SEGFAULT** — `for c in s` skipped `lockIterationSource`, reasoning that a minted Character cannot dangle, which answers the wrong question: the lock also stops a REBIND, and the loop re-reads `length@8`/`buffer@0` off the record it captured, so `s = "z"` mid-loop was **0xC0000005** where the oracle says E2013. ⚠ **The ownership answer is NOT Slice A's**: an Array *keeps* its element so there is an owner to borrow from; a String's clusters are stored nowhere, so each trip MINTS one and it is **owned**, paid by a per-trip frame so all four exits are correct by construction (5,200 minted Characters, exit 0). **Ruling 2 (coordinator, user-confirmed): shv2 is SELF-CONSISTENT on CRLF** — `"

"` is one cluster in BOTH `count()` and `for`, diverging from the reference's iteration path because **the oracle contradicted ITSELF** there (count 1 / iterate 2) and there was no single answer to match; the bootstrap has since been FIXED and now agrees. The Extend-block gap is reproduced and commented rather than widened, because there the oracle is self-consistent and merely incomplete. ⭐ Ports `specs/grapheme-clusters.md` byte-identical (21 cases) and flips 12 `character-type` + 5 `string-type-2`. ⚠ **RESIDUAL, NOT COMPLETE: `StringIndex` + `findFirst`/`findLast`/`slice` are Slice B2** — sanctioned up front, they depend on the grapheme-INDEX machinery this rung built and `slice` carries its own open question (the reference makes it a zero-copy VIEW with parent refcounting; `specs/slice-memory.md` is unported). ⭐ **A character literal as a CALL ARGUMENT is still refused**, and the blocker is measured: `SignatureIndex` stores parameter NAMES but no parameter TYPES, so at the moment `parseCallArgs` runs the expectation does not exist to consult — closing it puts a whole-program query under a per-file one and rehashes every parse memo. **Coordinator's call, not taken.** ⚠⚠ **THE B/D ORDER IS SETTLED, AND THE "CONTRADICTION" THIS ROW USED TO CLAIM WAS FALSE — MEASURED 2026-07-28.** It said *"B needs UAX#29 segmentation, which is DEFINED by the UCD tables assigned to D"*. True of UAX#29 in the abstract; **false of this codebase.** `stdlib/helpers/string/grapheme.maxon` consults **ZERO BYTES** of the UCD blob — its two required properties (`Grapheme_Cluster_Break`, 14-valued; `Extended_Pictographic`, boolean) are ~120 lines of **hand-coded range checks** in Maxon source, and the "table" is 0 bytes. The real blobs (`ucd_bmp.bin` 65,536 B + `ucd_supp.bin` 6,448 B, read via the `ucdByteAt`/`ucdI64At` intrinsics into a PE `.ucd` section) have **exactly ONE consumer in the whole stdlib**: `CharacterSet.contains` (`stdlib/CharacterSet.maxon:129`) — which calls `c.codepoint()` and therefore needs the `Character` type **B** introduces. ⇒ **D DEPENDS ON B; B NEEDS NOTHING FROM D.** The dependency runs the opposite way to the chain at the head of this row, and **B is next** (user ruling 2026-07-28, taken on the measurement after the first ruling had been taken on the false premise). ⭐ **The lesson is the one this project keeps relearning: a dependency asserted from a spec's reputation is not a dependency measured from the code.** ✅ **E3070 borrow-liveness — the rung Slice A's BORROW ruling owed — LANDED 2026-07-28 (→2003/0)**, sequenced ahead of B/C/D by user ruling because every later slice adds borrow surface onto an unsound base (see Future rungs for the full entry). **B/C/D now stand on a base where mutate-while-borrowed is a compile error.** ✅ **B2 — `StringIndex` + `startIndex`/`endIndex`/`findFirst`/`findLast`/`slice` — COMPLETE 2026-07-28, CLOSING SLICE B's RESIDUAL** (`7a0de1a24`..`a0fc6a5c0`; 2142 → **2178/0**, build 0 warnings, fragments EMPTY after a full run ⇒ byte-identical codegen, no 101; ⚠ **arm64 SKIPPED — UNVERIFIED**). **`StringIndex` is a SYNTHESIZED STRUCT, not a new `ValueTypeTag`** — Slice B minted `ValueTypeTag.character` because a Character is a BYTE RECORD raising drop/clone/promote/move questions at 16 sites (what `tagIsByteRecord` exists to answer); a `StringIndex` raises none of them (two integers, no heap, no destructor, not `Cloneable`), and a tag would have forced every `match tag` site to answer a question with one boring answer — a cost this project has measured, since Slice B's own rebase was broken by a **25th** such site. ⭐⭐ **THE PLAN'S OWN PREMISE WAS FALSE, AND READING THE CODE SETTLED IT WITHOUT A RULING.** This row used to say *"the reference makes it a zero-copy VIEW with parent refcounting"*. It does not: `__managed_mem_slice` (`stdlib/Internals.maxon:3814-3818`) allocates a fresh buffer, copies, stores `MM_PARENT_ROOT` and says outright *"we don't hold a reference to the source"*, and `String.maxon:293-298` records the stdlib **deliberately moving away** from view slicing because a view *"PINNED the entire source string alive behind one grapheme"*. `specs/slice-memory.md`'s **PROSE** still describes the abandoned design, but its five **TESTS** assert only stdout/exitcode and **the canonical oracle passes all five (5/5) on the copy** — the prose is stale documentation, the tests are the canonical part. ⇒ shv2 COPIES, which is already its own rule (`emitBuildOwnedInlineString`: *"none of them aliases the receiver's bytes"*), so there was no divergence to design and no new ownership model. (`Array.slice` stays COW — a different layer.) ⭐ **The two `slice` overloads are distinguished by ARGUMENT LABEL** (`endIndex:` vs `length:`), not by parameter type — the oracle's own mangled name says so (`String.slice$endIndex`) — so no overload-by-type resolution was needed, which matters because `SignatureIndex` stores parameter names and no types. ⭐⭐ **THE REVIEW FOUND A REACHABLE COMPILER CRASH THE GREEN SUITE WAS STRUCTURALLY BLIND TO**: `registerStringIndexType`'s only caller sat in `foldDeclaredSignaturesInto`, a **DECLARATION walk, not a token walk** — its arms hand a whole `type` to a `recordScanned*` routine and resume past its `end`, so tokens inside method bodies never reach it. A `StringIndex` producer **inside a `type` method** — the first place anyone puts a String helper — left the compiler-owned `StructLayout` unregistered, and died two different ways (a field read panicked at `stringIndexLayoutOrPanic`, a scope-exit drop at `managedNameDropCallee`). All 2,177 tests spelled the feature in a free-standing function, **the only shape that loop can see.** Two comments claimed the opposite, including the loop's own *"walks EVERY token in the file, function bodies included"*; cured by DERIVE (`fileNamesStringIndexProducer` gets its own complete token walk) and pinned by `string-index/an-index-inside-a-type-method`, verified RED against the parent. Review also found **ONE FACT IN FOUR SPELLINGS** — the "no error" `errorReturn` flag re-declared privately as `ArrayRuntime.NoErrorFlag`, `GtRuntime.RunProcessNoError` and `StringIndexRuntime.StrIndexNoErrorFlag` when `MaxonDialect.ErrorFlagOrdinalBias` already owned the encoding — collapsed to one exported `NoErrorFlag`, zero codegen change (the empty fragment diff proves it). **Sabotage: end-exclusive→inclusive 9 red · byte→grapheme→byte position 2 red · not-found throw→sentinel 2 red · `StringIndex` box drop removed 19 red, ALL exit 101.** Ports `specs/slice-memory.md` byte-identical and adds shv2-authored `specs-shv2/string-index.md`. `slice-iteration`'s marker had claimed a second blocker (*"String iteration (`for c in sub`)"*) and it was **STALE** — Slice B shipped that, and the case passes untouched. ⚠ **TWO CASES STAY DISABLED AND NEITHER BLOCKER IS THIS RUNG'S — each verified by the coordinator on a program that REMOVES the feature under suspicion**: `slice-copy-increfs-parent` (shv2 is MOVE-ONLY for an owned managed value, so `let sub2 = sub1` moves and the later read is E3102 — a **slice-free** program with `let b = a.toUpper()` + `let c = b` gives the identical E3102) and `find-last-not-found` (`if try <throwing call>` as a success test — a **String-free** program with a plain user function gives the identical E3059). ⚠ **DEBT THIS RUNG LEAVES NAMED:** the reserved name `__StringIndex` leaks into four user-facing diagnostic families (E3005/E3059/E2015/E3016) and two goldens, telling users to write a spelling `requireUnreservedName` forbids — confusing, not wrong at runtime; the one-place cure is a **display name for compiler-owned reserved types** in the type-name rendering funnel (`giveTypeName` + the interpolation site), a general mechanism serving future compiler-owned types ⇒ its own rung, not a review commit. ⬜ **SLICE D — R2/UCD + `CharacterSet` + `trim()` — COMPLETE 2026-07-28** (`a070aedf4`..`bd3b0aee7`; 2178 → **2241/0** rebased onto upstream's `float`-Hashable rung; x64-windows / x64-linux / wasm32-wasi all PASS; build 0 warnings; fragments additions-only ⇒ no pre-existing golden moved; no run exits 101; ⚠ **arm64-macos + arm64-linux SKIP — remote, UNVERIFIED**, and this rung added runtime graphs ⇒ **the arm64 goldens are owed a mint at the next periodic sync**). Ports `specs/unicode-escape.md` (6), `specs/string-trim.md` (22), `specs/unicode-category.md` (8) BYTE-IDENTICAL — **every case enabled, none disabled** — plus a shv2-authored `character-set.md` (18). ⭐⭐ **THE LOAD-BEARING FACT, AND IT IS WHY R2 COULD NOT BE SKIPPED: plain space U+0020 IS NOT IN `CharacterSet.whitespacesAndNewlines()`'s EXPLICIT CHAR LIST** (`stdlib/CharacterSet.maxon:36-37` seeds only `\t \n \r \r\n \x0B \x0C `) — it is whitespace ONLY via the `Zs` bit of `categoryMask`, so even `"  hello  ".trim()` goes through the UCD table. ⛔ A **dead** `stdlib/Unicode.maxon:3-52` `isWhitespace` implements `White_Space` by hand with **zero callers**; wiring trim to it would have made R2 look unnecessary and silently diverged from the oracle. ⭐ **`__str_trim` scans FORWARD ONLY, and the refusal to port the reference's backward scan is a CORRECTNESS decision, not a shortcut**: `findGraphemeStart` (`stdlib/helpers/string/grapheme.maxon:144-260`) has **no GB12/GB13 rule at all**, so it splits a regional-indicator pair the forward `findGraphemeEnd` joins — porting it would make shv2 answer "where does this cluster begin?" differently by direction. Cost is O(1)→O(n) on `trimEnd`, and the optimizer **measured it O(n) and not O(n²)** (×2.00 per doubling to 524 KB, time AND RSS, every seed). ⭐ **THE OPTIMIZER FOUND `trimStart` WALKING THE WHOLE STRING TO SETTLE A BOUND IT ALREADY HAD** — the tell was that a clean `trimStart` cost the same ABSOLUTE nanoseconds as a full `trim`: **56.2 ms / 45.0 MB → 0.46 ms / 4.8 MB (121× / 9.3×)**, `trim`/`trimEnd` unchanged to the KB, and it is ONE scan rule with an early-settle inside it (sabotage: firing it for all three ⇒ 19 red). ⭐⭐ **SIX GUARDS HAD ZERO CORPUS COVERAGE and every one is now pinned** — four found by the implementer (the sharpest **compiles clean and SEGFAULTS**: a bare-local member set not poisoned when `from` consumes it, a use-after-free none of 2,214 tests noticed), two by the review (a `__ucd_cat` misclassifying EVERY unassigned supplementary codepoint kept the suite at a clean 2230/0). ⭐ **THE REVIEW REFUTED 4 OF 6 DUPLICATION SUSPECTS AT THE CODE and confirmed the 5th load-bearing by sabotage** (`managedNameDropCallee`'s `__CharacterSet` arm: removing it ⇒ 40 red, all `resolveCallFixups: call to unknown function`). ⭐⭐ **THE COLLISION FIX FOUND THE RULE ALREADY EXISTED AND THIS SLICE HAD NOT JOINED IT**: `isCompilerOwnedTypeName` (`TypeResolution.maxon:533`) already governed `ExitCode`/`HashValue`/`Ordering` — the same shape as an unconditional `genericAliases.upsert` — so two names added to ONE predicate replaced a proposed second collision check (the coordinator directed E3006; the reviewer overruled it with a reason that holds: E3006's shape needs `already declared as <keyword> <name>` and **a synthesized type has no keyword and no source site to name**). It closed a hole neither party had named: a bare `type CharacterSet` was being **silently accepted**. ⚠ **COORDINATOR ERROR, RECORDED BECAUSE THE METHOD IS THE LESSON**: the brief's RED item 3 claimed `'А'` was unsupported (`E2007 Unexpected end of input`). It was **already implemented**; every probe was missing `end 'main'`, so ALL of them failed identically — **including the plain-`'A'` control, which had already disproved the claim and was not heeded**. A control that fails the same way as the experiment is not a control. The real gap was only the malformed-escape diagnostic (E1004, an EXISTING code with an existing `shv2` claim — no registry drift). ⬜ **✅ **SLICE E — the String view/method leftovers — COMPLETE 2026-07-28, CLOSING P1.8** (`c132b1d8d`..`e65e314bc`; 2241 → 2297/0 on its base, **2318/0** rebased onto upstream's P1.6 closure; x64-windows / x64-linux / wasm32-wasi all PASS; build 0 warnings; fragments **112 A / 0 M**; no 101; ⚠ **arm64 SKIP — remote, UNVERIFIED**). Ships `isEmpty`/`clone`/`replaceFirst`/`codepoints`/`toByteArray`, a `String.from(ByteArray)` static door, and `.utf16()`. ⭐⭐ **THE NINE `utf16*` FREE FUNCTIONS RODE THE STDLIB WHITELIST, NOT A NEW DISPATCH DOOR** — shv2 has no bare-name builtin dispatch at all, and inventing one was the obvious-but-wrong move. `StdlibLoader.whitelistedStdlibModules` already existed for this and its own header says *"WIDENING IS ONE LINE"*; the route was MEASURED before it was taken (`build stdlib/helpers/string/utf16.maxon` fails with exactly five `E3011 Unknown type 'Codepoint'` and nothing else, while whitelisting `Character.maxon` instead is a compiler-owned-name collision). Third entry after `Clock`/`Sleep`. ⭐⭐ **THE STALE-MARKER SWEEP PAID FOR ITSELF BEFORE A LINE WAS WRITTEN: 13 of 60 markers were FREE FLIPS** — `.bytes()` already materialized a real `Array with Byte` and `requireIterableArray` already accepted it, so byte-view iteration had quietly arrived rungs earlier. ⛔ **And there was no "implicit append optimization" to build**: `specs/string-type-2.md:850-852`'s prose describes a `s = "{s}…"` rewrite that exists in NEITHER reference — the behaviour is emergent from general fused-length interpolation lowering. Prose stale, tests canonical, second time this phase (cf. B2's view-vs-copy). ⭐ **THE OPTIMIZER ANSWERED THE QUESTION THE SHARED INSTRUMENT CANNOT ASK**: is a whitelisted module's cost O(corpus × modules)? Holding the corpus fixed and moving the module count on a purpose-built ladder (`Testing/ladders/genwhitelist.sh`), an unused listed module costs **+1,914 parse allocations REGARDLESS of corpus size** — identical from 4,890 to 140,917 lines. **A SUM, not a product**, so the loader's intended 49-file end state carries no hidden superlinearity. The growing remainder was isolated to **+3 allocations per trivial array-element read** (×2.00 exactly) — the price of the type-identity fix, a slope not a curve. ⚠⚠ **THE CROSS-TARGET GATE CAUGHT WHAT A GREEN HOST SUITE COULD NOT, AND THE COORDINATOR'S HYPOTHESIS WAS REFUTED BY MEASUREMENT.** Two new range-check cases failed on x64-linux AND wasm with **EMPTY stderr** — i.e. the silent wrong answer still live on two of three targets. It was *not* the `stdlibFacts.unreachable` skip being target-dependent (the coordinator's guess): the isolating case is a program with no whitelist and no `Codepoint` in it, which behaves identically — **wasm and x64-linux have no panic RUNTIME** (`emitRangePanic` is `i32.const 1; call $exit`, documented in place), which is why every pre-existing panic-text case carries `targets: x64-windows`. ⭐ **Fixed by a PARTITION, not a gate**: each guard is pinned twice — the silent lanes assert **exit 1** with the program rewritten so a fired guard and the unguarded answer differ by exit code (1 vs 7), the host lane asserts the full message. ⇒ **the first cases in this suite to assert that a range guard fires on wasm or linux at all**, verified by sabotage ON the wasm lane. ⚠ **A `stderr` block cannot merely be omitted** — the runner's `unpinned` arm asserts the program printed NOTHING, so partitioning is what lets both facts be stated without either lying. ⭐ **TWO DEFECTS THE COORDINATOR REFUSED TO MERGE OVER, both this rung's own constructs**: `utf16DecodeSurrogates(0, low: 0)` returned **−56613888**, a negative number wearing a type whose range starts at 0, where the oracle panics (cured narrowly — `Codepoint` became an ordinary registry alias seeded through `RangedAliasRegistry.create()`, the ONE constructor all three registries are built through; `HashValue` untouched); and `String.from` + truncated UTF-8 **overread the buffer by 1–3 bytes** (cured in `emitUtf8LeadLadder`, already the one home of "how wide is this sequence", so the two answers cannot diverge — a lead promising more than the buffer holds routes to the one-byte arm). ⭐ **THE REVIEW'S BEST FIND WAS A FOLD THAT STOPPED AT A FILE BOUNDARY** — five method-name spellings written twice across `Parser.maxon` and `SetRuntime.maxon`, whose stated exemption (*"homed beside `setMethodMutatesReceiver`"*) does not survive reading the five names, since that rule mentions only two of them — plus three view builders sharing a **byte-identical twelve-line skeleton** where nothing made them agree the bound is `>=`. ⚠ **DEBT THIS SLICE LEAVES NAMED (all filed below, none holding the rung open): a SECOND `nextStringId` producer** — `lowerStringLiteral` registers `.rdata` for any body, pre-elimination, so the whitelist's byte-neutrality holds today only because none of the three listed modules contains a string literal — **a property of those files, not of the mechanism**, and the next entry with a literal will renumber goldens.** ⛔ **`charAt`/`indexAfter`/`indexBefore` were EXCLUDED from B2 by coordinator decision with a measured reason** — `indexBefore` needs a **backward UAX#29 segmenter** shv2 does not have (a genuinely distinct mechanism with its own contract) and **no ported case uses any of the three** (grepped: zero hits in `specs-shv2/`); they and the canonical `crlf-index-and-search-arithmetic` case are a future rung. |
| **P1.9** | **ranged typealiases** | ✅ | 887→**2352** | **✅ CLOSED 2026-07-29 (`19ebc7759` · `1c1933535` · `9286cc763`). The core landed 2026-07-21 (`cb7107e8c`); what held it open afterwards was recorded WRONG, and what actually held it open was recorded NOWHERE.** ⭐ **#86 MEASURED FALSE IN BOTH HALVES.** It claimed *arm64-macos/arm64-linux + x64-linux goldens owed*, citing a 2026-07-25 measurement of *x64-linux is `1225 passed, 288 failed`*. Measured on a freshly built compiler: **x64-linux PASSES 2216/0** via the WSL lane. PLAN.md had already contradicted itself — the *Cross-target goldens still owed* section read *“#86 … (the x64-linux half is done)”* while this row and the index still said it was owed: **three spellings of one fact**. The arm64 half was never a residual: by step 12's rule an unverified remote lane is a reported **SKIP**. **THIRD CONSECUTIVE RUNG whose filed residual measured false** (P1.6, P1.7a). ⭐⭐ **THE RESIDUAL THAT WAS REAL APPEARED IN NO ROW — it was found by sweeping the `disabled-test:` markers that still NAMED the rung.** P1.9 had shipped the **USE-site** half (`as` casts, runtime guards, ranged returns) and never built the **DECLARATION-site** half. Flipping all 12 markers: **1 passed immediately** (`struct-field`, whose marker blamed P1.1 + P1.2, both landed rungs earlier), 11 genuinely red; every expected block byte-identical to `/specs` and re-verified message-and-column against the RUNNING bootstrap. **⇒ sweep the markers naming your rung; do not read only its row.** **WHAT LANDED — five mechanisms, one wave.** **(1) E3062 unused-typealias**, which had ZERO occurrences in shv2 and no `shv2` claim in the registry (its generated enum jumped E3011→E3013). Per-file: a use is the NAME in a type position in its OWN declaring file; exported/module-visible aliases are EXEMPT (one file cannot see another's uses); **being implicitly inferable from a bare `[…]` literal is NOT a use** — the oracle reports `IntArray` unused even though the literal could have been inferred as it, while `Integer` (named inside `IntArray`'s own RHS) is not reported. **(2) E3005 bound validation AT THE DECLARATION**, which needed provenance shv2 had thrown away: `sizedTypeBound` collapsed every bound to a bare `ParsedInt` the moment it returned, so nothing downstream could tell `i64.max` from `u64.max` from a literal. Both references keep a `(value, qualifier)` pair — a convergence that settled the design. Order is load-bearing: the qualifier-mismatch check runs BEFORE the negative/`u64.max` one, so `int(i64.min to u64.max)` reports *Mismatched type bounds*. **(3) E2003 bare sized-type shorthand** — **the two references DISAGREED (bootstrap 10 names, v1 only the 8 integer ones) and the ORACLE decided**: `typealias Real = f64` ⇒ E2003, so `f32`/`f64` are in. **(4) ALIAS VISIBILITY + the stdlib carve-out, which are ONE mechanism, not two** — the corpus demands a non-exported USER alias be invisible cross-file (E2003) *and* a non-exported STDLIB alias be nameable anywhere; shv2 had satisfied the second by making EVERY alias visible, a placeholder `RangedAliasRegistry`'s own header flagged as becoming load-bearing *“with cross-file name resolution”*. It did. `dispatchTopLevel` had been parsing `export` and DROPPING it. **(5) E3005 on an out-of-range `otherwise` value** — found by PROBING, not by the suite: shv2 compiled `try f() otherwise -1` against a `int(0 to 100)` return and RAN, binding an out-of-range value to a ranged type. The one corpus case covering it is blocked on `Character.asciiValue()`, so the suite was green over it. ⚠ **DIAGNOSTIC ORDER CONSTRAINED THE PLACEMENT**: `export-keyword/error.non-exported-typealias-cross-file` pins **E3062 then E2003**, and `PassPipeline` throws between passes on `projectHasErrors`, so both had to land in ONE pass — and further, in a deferred queue, because the file walk is alphabetical and per-file reporting could not produce that order. ⭐ **THE CORPUS ITSELF WAS WRONG IN 29 PLACES.** E3062 turned 29 pre-existing tests red on landing. **None exists in `/specs`** — all shv2-authored, so none had ever been put to the oracle; put to it, the bootstrap rejects them identically. They were invalid programs that compiled only because shv2 had no check, so the PROGRAMS were repaired, not the rule: 24 vestigial `typealias Integer`/`UnaryOp` deleted, 5 gained the E3062 line (the alias IS the collision the test proves, and the canonical corpus already pins two errors this way). ⚠ **One repair went the OTHER way and is the instructive one**: `typealias-file-scope/narrow-file-cast-still-rejected`'s dead `Limit` was real, but E3062 is raised in `resolveTypes` and the range check it exists to prove runs FOUR PASSES LATER — so the new diagnostic did not fail that test, it **MASKED** it, and the test silently stopped testing anything. `checkB` now returns `Limit`. ⭐ **THE INDEPENDENT REVIEW FOUND TWO REACHABLE FALSE DIAGNOSTICS, each refusing a LEGAL program** (`1c1933535`): a conformance argument in PARENTHESES was not a use — `implements I with (Slot)` went through a brace-skipper while the bare `with Slot` beside it went through the type-reference reader that records the use, and **`stdlib/helpers/string/views.maxon` writes the parenthesized form three times**; and the hidden-cast E2003 judged only the LAST-recorded declaration, so the verdict was decided by the alphabetical file walk — **the same program gave two answers under two filenames, and it refused the legal one**. Both are now pinned as cases (the filenames in the second are load-bearing). Plus a visibility bit folded into `SignatureIndex`'s content hash, which P1.9 had just made decide what another file's cast means. ⭐ **A THIRD path defect, fixed by the coordinator**: `isStdlibSourceFile` compared path SPELLINGS, so `build stdlib/p19chk` and `build C:/…/stdlib/p19chk` classified the SAME file differently — the absolute spelling silently promoted a user's private alias to `Visibility.global` and dropped its E3062. Both sides are now resolved against the cwd, exactly as `StdlibLoader.registeredPathsWhenTheRootIsInsideStdlib` already did, and that function's ruling (a project ROOTED under `stdlib/` keeps its own files as the USER's) is restated where this door can act on it. Verified by probe both spellings; **not spec-able — the harness cannot root a test project inside `stdlib/`**. **GATES:** build exit 0 / 0 warnings; suite **2318→2333 on its own base, 2337→**2352** after rebasing onto the break-in-match rung, always /0**, exit 0, no 101; `specs-shv2/fragments/` EMPTY after a full unfiltered run ⇒ byte-identical codegen; fragment churn exactly the 29 repaired programs + 4 new cases, nothing else. **x64-linux PASS**, **wasm PASS**, **arm64-macos / arm64-linux SKIP — remote, UNVERIFIED**. `maxon-sharp` touched in generated doc-comments only (enum members and values unchanged). **scale-test: LINEAR and fully attributed** — deltas +1,990→+11,897 over six rungs fit **1,670 flat + ~320·n** to within 0.5% at every rung (fixed per-compile seeding + a per-file used-alias set); **+0.08%** of rung 5. No optimizer trigger. ⚠ **arm64 goldens are OWED to the periodic sync** — this rung moved codegen for 33 tests and only the lane that emits them can mint them. **OUT OF RUNG, homed rather than lost:** (a) ⭐ **shv2 and the bootstrap genuinely DISAGREE on `a + b if c else d`** — `return 1 + 20 if false else 40` answers **40** here, **41** there; the one corpus case that exercises it (`ternary-expression.complex-expressions`, cond=true) evaluates to **5 under BOTH readings**, so it never distinguished them, and `Parser.maxon`'s comment asserting the two agree was measured FALSE and is corrected. **NEEDS A RULING; P2 self-hosting hazard** (a self-hosted parse that disagrees with the bootstrap's silently changes the compiler's own meaning) → Future rungs. (b) two **bootstrap message defects** (`int(i8.max to i32.max)` reports `'i8.min'`; the legal `int(-1 to u64.min)` is refused quoting a `u64.max` the source never wrote) → Bootstrap oracle bugs. (c) **no merge phi for a managed `var` reassigned inside a labelled `if` in a free function** (E9001 `does not dominate`) — loud, pre-existing, unrelated → Future rungs. (d) **E3063 ambiguous-typealias** is registered and specced but unimplemented in shv2; this rung's visibility work is its prerequisite → Future rungs. |
| 🚩 | **PHASE 1 GATE** | below |

> ### ⭐ P1.2 — two decisions taken elsewhere that land HERE (2026-07-15)
>
> **1. `String.toByteArray()` is a MOVE, and the source is not mutable afterwards** (user decision).
> This is an OWNERSHIP statement, which is why it belongs to this rung and not to the bootstrap's
> refcounting. It **dissolves** a problem rather than managing it: with one owner there is no second
> observer to keep consistent, so there is no aliasing question, **no copy-on-write**, and no
> "independent value" contract to enforce.
>
> The bootstrap cannot do this — it has no ownership — so it takes the fallback: `toByteArray()` returns
> an **independent** ByteArray, implemented as a COW view (`185351d1f`). **That is a stopgap for a
> compiler without moves, not a design to port.** Two of its specs
> (`specs/string-type-2` / `tobytearray-is-independent-of-an-owned-source`,
> `tobytearray-survives-the-source-growing`) **use the string AFTER `toByteArray()` and must become
> use-after-move ERRORS here.** Delete them at this rung with that reason, do not make them pass.
>
> The bootstrap's COW is also **one-directional** — a write through the source's raw `managed` shows
> through a view that has not detached yet (measured). Moves make that unrepresentable. **Do not port
> the COW view, and do not port the hole.**
>
> **2. ⚠ v1's two-result-capture MISCOMPILE is waiting for THIS allocator.** shv2's register allocator is
> a deliberately different **linear SSA-chordal** design — i.e. v1's shape, where virtuals are colored
> independently and call-result **capture movs are real**. v1 paid for this bug already
> ([CopyResolution.maxon:377-395](maxon-selfhosted/Compiler/Targets/Shared/CopyResolution.maxon#L377-L395)):
> the colorer can legitimately assign two result dests into a **SWAP** or **CHAIN**, which naive
> sequential movs cannot realize — `mov x1, x0; mov x0, x1` collapsed value+flag into one register, so a
> bounds check read the element value instead of the OOB flag and **panicked on in-bounds data** (the
> array-sort `try get(j) otherwise` miscompile). The fix is `sequentializeCallResultCapture`: a
> **parallel-copy sequencer with per-class cycle-break scratch**, never sequential movs.
>
> The **bootstrap is immune and its code is therefore NOT the model** — its `Assign(reg, value)` merely
> pins a value into a register map and emits no capture mov at all, so it has nothing to sequence.
> **Port v1's lesson here, not the bootstrap's shortcut.**

### 🚩 PHASE 1 GATE — the differential oracle

**Acceptance — and step 1 is the whole point:**
0. ✅ **DONE 2026-07-29 (slice G1).** Split `compilerExe` into the COMPILER-UNDER-TEST and the WORKER
   EXECUTABLE. ⚠⚠ See the box below. Without this, steps 1–4 all pass while testing nothing.
   **Gate: `scripts/worker-exe-split-gate.sh` — run it whenever the pool's dispatch changes.**
1. Compile the **parallel** harness with **`maxon.exe`** → `spec-runner-ref.exe`.
2. Compile the **parallel** harness with **`maxon-shv2.exe`** → `spec-runner-shv2.exe`.
3. Run **both** against `specs-shv2/`, both driving `maxon-shv2.exe` as the compiler-under-test,
   **both on an N-worker pool**.
4. **The two must produce identical results.**

If shv2 miscompiles the harness, the shv2-built harness's own verdicts are untrustworthy — so the
`maxon.exe`-built harness is the oracle that catches it. **Without step 1 this gate could pass
while silently broken.**

> ### ⚠⚠ STEP 0 — ONE `FilePath` IS DOING TWO JOBS, AND THE GATE IS WHERE THAT STOPS BEING FREE
> *(found 2026-07-29, while re-extracting the runner)*
>
> **`PersistentWorkerHandle.spawnNoWait(compilerExe, …)` spawns `compilerExe` — as the WORKER.**
> (declared [SpecWorkerPool.maxon:663](maxon-shv2/Testing/SpecWorkerPool.maxon#L663), spawned
> [:693](maxon-shv2/Testing/SpecWorkerPool.maxon#L693)). That one parameter is
> simultaneously *"the compiler this suite is testing"* and *"the executable a worker process is"*.
>
> **In-tree the two are the same file and always have been** — the harness IS the compiler binary, so
> `runSpecTest` passes `Process.executablePath()` and both readings land on it. The conflation has
> therefore never been wrong, never been tested, and is invisible to every green suite.
>
> **The gate is precisely the configuration that separates them**, and unsplit it does not fail
> loudly — **it passes**. `spec-runner-shv2.exe` would spawn `maxon-shv2.exe` as its workers; so would
> `spec-runner-ref.exe`; both parents would delegate every actual verdict to the SAME in-tree
> harness, the two result streams would agree perfectly, and the gate would certify that shv2
> compiles the harness correctly **without a single test having been run by shv2-compiled code.**
>
> ⇒ **`spawnNoWait`, `runSpecPool` and `runPersistentWorker` take TWO paths, not one.** The project's
> signature bug — ONE FACT WRITTEN DOWN TWICE — inverted: here it is *two facts spelled once*, and it
> is wearing this gate's own intention as a mask. Fix it BEFORE the gate can mean anything, and pin
> it with a run whose worker exe is deliberately NOT the compiler under test.
>
> ### ✅ CLOSED 2026-07-29 — slice **G1** (`9402d4620`..`20312151a`)
>
> `workerExe` (always `Process.executablePath()`, **not settable** — a flag spawning some *other*
> harness as a worker has no caller and could only lie) and `compilerExe` (**`--compiler=<path>`**,
> else the running exe) are now two parameters. The parent forwards `--compiler=` to every worker
> **unconditionally**, which is the part that actually kills the bug: the worker used to *re-derive*
> the compiler from its own `Process.executablePath()`, and `spawnNoWait` had already written the
> cure for the sibling case — *"a worker that decided again could decide differently"* — without
> anyone applying it here.
>
> **⭐ NEITHER REFERENCE COMPILER COULD ANSWER THIS, AND THAT IS A FINDING RATHER THAN A GAP.** v1's
> workers compile **in-process** (`compileFragmentInMemory`,
> [SpecTestRunner.maxon:1134](maxon-selfhosted/Testing/SpecTestRunner.maxon#L1134)) and the bootstrap
> uses in-process **threads** ([TestRunner.cs:109-176](maxon-sharp/Testing/TestRunner.cs#L109)) —
> **neither ever spawns a compiler binary from a worker**, so the two facts never had to separate and
> neither tree contains a `--compiler`-like flag. shv2's spawn-the-compiler harness is its own
> deliberate design; this split was **designed here, not ported**, and no reference could validate it.
>
> **⭐⭐ THE PIN IS A NEGATIVE CONTROL, BECAUSE THE OBVIOUS CHECK DISTINGUISHES NOTHING.**
> `scripts/worker-exe-split-gate.sh`. Running with `--compiler=<a byte-identical copy>` and seeing it
> pass is **not evidence** — a harness that honours the flag and one that ignores it and uses itself
> BOTH pass, which is *"no failing test is about coverage, never agreement"* in its purest form. The
> experiment where the two models give **opposite** verdicts is pointing `--compiler=` at a real
> executable that is not a compiler: split honoured ⇒ **every test fails**; split broken ⇒ **every
> test passes**. Verified by sabotage — passing `workerExe` where `compilerExe` belongs turns that
> check from `0 passed, 4 failed` into `4 passed, 0 failed` **while the positive control stays green
> in both**, which is the whole argument written out as a measurement.
>
> **⭐ THE REVIEW FOUND A SECOND DOOR INTO THE SAME WRONG ANSWER, AND IT WAS THE RUNG'S OWN.**
> `--compiler=` with an **empty value** exited 0 with `4 passed, 0 failed`:
> `CommandLine.optionValue("--compiler=")` returns `""` **without throwing**, and `""` is exactly how
> the field spells *"the caller named no compiler"* — so an empty VALUE and an ABSENT FLAG arrived
> indistinguishable and were both answered with the running executable. The rung's own comment said
> that outcome was impossible; the code reached it through the argument parser instead of the
> filesystem. Reachable, not theoretical: `set -u` catches an *unset* variable and never an *empty*
> one, so any wrapper computing `--compiler=$CUT` — **including this gate script itself** — got the
> green. Refused at the parser through the door `--workers=`/`--rungs=` already use, and pinned as
> gate CHECK 5, seen RED first.
>
> ⚠ **`Testing/ScaleTestRunner.maxon:364` has the same `Process.executablePath()` spelling and is NOT
> the same bug** — checked at the code rather than assumed: its `compilerExe` has exactly one
> consumer (`compileRung` → `runProcess`), no pool, no second process, nothing re-deriving it. One
> fact, one consumer. (`Compiler/StdlibSource.maxon:58` locates the *running image's own* stdlib, so
> the subject is by definition this process — and it is what makes the gate's positive control work.)
>
> Gates: shv2 **2372/0** unchanged · build 0 warnings · `specs-shv2/fragments/` **EMPTY** after a full
> run ⇒ byte-identical codegen · no run exits 101 · split gate **5/5** · scale-test **unmoved, and
> A/B-confirmed at the same checkout path against the pre-change binary** (the log's standing +192 is
> upstream's, not this rung's — the plausible story was wrong).

### 📏 THE GATE'S MEASURED DISTANCE (2026-07-29) — 13 files, 13 first-errors, and a LOWER BOUND

The runner re-extracted per the corrected boundary above **builds clean with `maxon.exe`** (528,225
bytes of code + 13,095 `.rdata`; 94,649 lines of `--emit-ir`). Handed the identical 13-file program, **`maxon-shv2.exe` reports 13 × `E2015
Unsupported`** — one per file, because the driver gates at the first error per file. ⚠ **That is a
LOWER bound on the gap, not the gap**: every file stops at its first refusal, and nothing behind it
has been reached. Ranked by how many files each blocks:

| Missing mechanism | Files | Evidence |
|---|:--:|---|
| ⭐⭐ **the STDLIB CONE** — `FilePath` · `File` · `Directory` · `Subprocess` · `Executable` · `StringArray` | (all) | `E2004 Undefined variable 'FilePath'`, `E3004 call to undefined function 'Directory.currentPath'` / `'Subprocess.run'` / `'Executable.name'` / `'StringArray.create'`. **3 of 48 stdlib files whitelisted** — see §"THE DOGFOOD'S REAL BLOCKER" |
| **methods on `union` / `enum`** | 5 | `TestOutcome` · `PeReadError` · `ElfReadError` · `MachoReadError` · `DataSectionReadError`. Already self-described as *"arrive with a later rung"* |
| **a METHOD CALL on a struct FIELD** | 2+ | `args.badOption.isEmpty()` ⇒ *"a field access through 'RunnerArgs.badOption', which is declared 'String' and not a struct"*. Pervasive in the harness |
| **`ByteArray` / `Byte` as a declared type** | 3 | resolves to `int`, so `buf.push` / `image.get` / `bytes.count` all report *"a field access on … declared 'int'"* |
| **`#if` conditional compilation** | 1 | `Target.maxon:33` — blocks `detectHostTarget()`, which the harness cannot run without |
| **static methods on primitives** (`int.fromString`) | 1 | `try` sees a field access, not a call |

Each row was confirmed by a **standalone minimal probe**, not read off the diagnostic — the messages
name a symptom (*"declared 'int'"*) that is downstream of the real cause (*the type name resolves to
nothing*). ⚠ And the whole read is valid only against a **freshly built** shv2: the first attempt used
the on-disk binary and mis-read the SUITE as `2319/53`.

**Additional gates the parallel pool brings — and they are a feature, not a tax:**
- **Worker-count invariance:** `-j1` and `-jN` must produce **identical results**. This is the
  same shape as the 1-core-vs-N-core byte-identity gate the plan already demands of P2.6's
  fan-out, and it is the sharpest test of `async` correctness that exists — a dropped or
  double-freed capture shows up as a flake, and a flake is a bug.
- **`mm-trace` must stay clean under the pool.** A green-thread capture that leaks or
  double-decrefs is exactly what P1.5 exists to get right, and this is where it is proven.

**Then the circle closes:** the shv2-compiled, shv2-parallel harness runs the spec suite that
tests shv2 — which is what `maxon-selfhosted` does, and is the Phase-1 goal.

### Future rungs — corpus-surfaced features that each need their own number

Distinct front-end / IR features the corpus surfaced that do NOT ride an existing rung; each gets a ladder
number when it is sequenced (each also has a `disabled-test:` or oracle-divergence pinning it):

- **⬜ u32 MEMORY ACCESS — `ExitCode` IS A SIGNATURE/ABI TYPE AND NOT YET A STORAGE TYPE** (D7's tail +
  W1b, 2026-07-30, MEASURED not predicted). `parseTypeReference` claims `String` and `Character`
  syntactically and deliberately does NOT claim `ExitCode`, because that door answers for the VALUE column
  too: promoting it makes `ExitCode` a STORAGE type, `var n as ExitCode` becomes a u32 field, and
  `Targets/Shared/StdLoweringShared.maxon`'s `accessWidthFor` panics — *"no memory-access form for u32"*.
  **Measured: claiming it there was 2578/3**, all three the same panic. ⚠ **Why it is its own rung:** the
  panic is DELIBERATE and its cure is a backend slice — a narrow unsigned load/store needs the width AND
  the sign-extension form chosen per ISA, so it needs a contract across x64, arm64 and wasm, and
  `accessWidthFor` is the shared door all three come through. Same `as`-cast family P1.9 deferred (`i8 to
  i32` panics one line above for the signed twin). ⚠⚠ **AND THE SINK LIST ITSELF CHANGES THE DAY IT LANDS.**
  Today the env slot is the ONE place a narrow type reaches memory — which is why W1b's `envSlotStorageType`
  carve-out is sound and does NOT generalize; every other store takes its width from a DECLARED slot type
  (`fieldStorageType` at three sites, `payloadStorageOf` at three more) or from a fixed i64 (the GT/async arg
  spill stores every argument at i64 regardless of type). Claim `ExitCode` in `parseTypeReference` and all
  six see a narrow type for the first time — struct fields, union payloads, globals — and the env-slot
  answer must be RE-DECIDED against real 4-byte packed fields rather than copied.
- **⬜ AN OVERLOAD SET PUBLISHES ONE RETURN TYPE FOR THE WHOLE SET — `returnTypeOf` IS KEYED BY THE BARE
  NAME** (found 2026-07-30 by D10's review, measured on both compilers; a D7 × D10 interaction, and
  neither rung's own defect). D7 mangles a REGISTRATION by parameter types but `ProgramSignatures.
  returnTypeOf` still answers for the bare name, keeping the FIRST declaration's return type for the set.
  ⭐ **The leak direction is already blocked, and not by D10**: a MIXED set — some members void, some
  value-returning — is refused by D7's own guard (*"the overloads of 'Helper.f' do not agree on their
  return type"*), probed with `struct`, `String` and `int` returns alike, so no discarded box exists.
  An ALL-VOID set works end to end (both members callable as statements, measured). ⚠ **What is wrong is
  a FALSE REJECT**: in a mixed set the genuinely void member is refused in statement position with
  `E2015`, while **the oracle compiles and runs it** and shv2 itself accepts the value-returning sibling
  in expression position in the same program. ⚠ **Why it could not ride D10:** the only change available
  inside `voidStaticCallsAt` is to let a non-void `declared` fall through to the unknown-member arm — and
  that would claim `Point.create()`, discard its box, and create exactly the leak D10's negative spec
  pins. The real fix is **D7's index becoming per-MEMBER rather than per-NAME**, which is the boundary
  D7's own diagnostic text describes, and it is a distinct rung rather than a ride-along.
- **⬜ A NESTED CLOSURE THAT CALLS AN ENCLOSING CLOSURE PANICS THE COMPILER, FOR ANY TYPE** (found
  2026-07-30 by W1b's review, PRE-EXISTING and independent of both W1 and W1b). `let v = n; let f =
  function() gives v; let g = function() gives f(); return g()` ⇒ **`panic at TypeResolution.maxon:672:
  resolveTypes: 'unresolved' reached type resolution — the parser emits no unresolved placeholders at
  M1`**. Reproduced with a plain `Integer`, re-measured on `main` at W1b time, so it is nothing to do with
  `ExitCode`, widths or the env-slot door W1b fixed — a closure VALUE captured into another closure never
  gets its return type resolved. Legal source, hard crash. ⚠ **Why it could not ride W1b:** W1b's footprint
  is the env slot's WIDTH; this is `closureReturns` threading through a NESTED env, which is P1.5-A2's
  mechanism and wants its own survey of how a lifted closure's file-local return type reaches an inner
  closure's call site.
- **⬜ `readStdoutLine` HAS NO DEADLINE ANYWHERE IN THE STDLIB, so `awaitReady` can hang the pool BEFORE
  the dispatch loop exists.** **Filed by H1's review (2026-07-30).** A worker that never prints
  `WORKER_READY` hangs `spawnPool` (`SpecWorkerPool.maxon:775`) forever — the **same class** as the
  unbounded `child.wait()` that review DID fix, and squarely the failure H1 exists to abolish, but it lands
  one phase earlier where H1's watchdog does not yet exist. ⚠ **Deliberately NOT hacked around**: there is
  no bounded-read API to reach for, so fixing it means ADDING one to `stdlib/Subprocess.maxon` — a stdlib
  change with the full C# suite behind it, i.e. its own rung with its own gate.
- **⬜ `./bin/maxon fmt` WANTS TO REWRITE THREE FILES NO RUNG TOUCHED** — `ParseStaging.maxon`,
  `StdToX64Conversion.maxon`, `X64GtRuntime.maxon` (62 lines, zero semantic content), and in the last it
  **destroys a hand-aligned comment column**. Pre-existing drift, consistent with the filed *"fmt is not
  idempotent"* note, and it makes the review step's *"format the whole tree"* instruction produce a diff
  that is not the review's. H1's review reverted them so its own commit read clean. ⇒ normalizing the tree
  is a chore of its own, and it should probably be done at a phase boundary rather than inside a rung.
- **⬜ `unionHasManagedField` STILL TAKES `(layout, typeNames)` — the THIRD instance of the pattern that has
  now cost a use-after-free and a panic.** **Filed by T1's review (2026-07-30), deliberately not ridden along.**
  Both remaining callers (`installUnionDestructors` and the `usesStrDecref` gate) legitimately hold a
  *project* layout and pair correctly **today** — so this is not a wrong answer in the reviewed files, which is
  precisely why it was filed rather than fixed. ⚠ **But it is correct by CONVENTION, and convention is what
  failed twice already** (T1's own guard, then `managedNameDropCallee` one door in). Making the project side
  by-name too requires auditing `project.enumTypes` against `signatures.enumTypes` for set equality — a real
  question about registry duality that touches runtime synthesis broadly and needs the full suite as its gate.
  ⇒ belongs beside **OPEN #52 / #56** as the third instance of one pattern.
- ~~**⬜ THE SPEC RUNNER WEDGES NONDETERMINISTICALLY**~~ ⭐ **SEQUENCED as board row `H1` (2026-07-30) — take it there, not here.** ⚠ And the signature recorded below is CORRECTED on the row: the PARENT was at 1.7–1.9% CPU, not 0% — only the workers were idle — which is what identifies the dispatch loop's poll fallthrough as the thing spinning and re-points the suspicion at the parent→worker handshake. Original filing follows:
- **⬜ (superseded by H1) ALL WORKERS IDLE AT 0% CPU, INDEFINITELY.** Hit **TWICE
  in one session** by the T1 coordinator (2026-07-30), on the arm64-macos host: once on the host lane, once on
  the wasm lane, in a tree whose identical binary then ran the same suite **green in 5.5 s and 8.1 s** after a
  `SIGTERM` and a retry. Signature both times: the parent plus all **12** workers in `STAT=S` at **0.0% CPU**
  for **10+ minutes**, zero output written (the runner buffers, so an empty log is expected and is NOT the
  tell — the CPU column is), and on the wasm lane **no `wasmtime` process ever spawned**. ⚠ **Not a target
  issue and not T1's** — T1 touches no runner code, and the wedge struck both lanes. `pkill -TERM` cleared it
  with **zero** processes left behind, so the old *"kill wedges the box"* hazard is genuinely gone. ⚠⚠ **This
  is the single biggest threat to running rungs UNATTENDED**: a wedged gate is indistinguishable from a slow
  one until you look at `%CPU`, and a `/loop` that simply waits will sit on it forever. Suspect the same
  family as the GT scheduler race fixed 2026-07-20 (`8163f1056`) — a residual, or a second one.
  ⇒ **Diagnostic that costs nothing: `ps -o pid,etime,%cpu,stat -p $(pgrep -f 'maxon-shv2 spec-test')` — all
  workers at 0.0 for minutes means WEDGED, retry rather than wait.**
- **⬜ A NESTED FUNCTION TYPEALIAS EMITS INVALID WASM** — `typealias Outer = function(Inner) returns Num`,
  where `Inner` is itself a function typealias, called indirectly ⇒ `wasm-tools`: *"type mismatch: expected
  i64 but nothing on stack"*. **Filed by W1's review (2026-07-30).** ⚠ **NOT W1's**, and that was measured
  rather than argued: **no `ExitCode` is involved** (it reproduces with a plain `int(0 to 1000)` alias) and it
  is **identical on the pre-W1 binary at the same byte offset (`0x3de`)**. A **loud compile abort**, never a
  wrong answer. Almost certainly the companion-env arity — the call passes `(fnptr, its env, callee env)` = 3
  while `synthesizeFnRefThunk` builds `sig.paramCount() + 1` = 2. ⇒ an ABI/arity change across
  `LowerMaxonToStd`, `FnRefThunk` and the wasm functype interning — three files W1 does not touch — needing
  its own acceptance cases for higher-order function values. The existing guard's own comment already names
  this family.
- ~~**⬜ `Self.<member>` IN AN EXPRESSION INSIDE AN ENUM BODY PANICS THE COMPILER**~~ ⭐ **SEQUENCED as board
  row `D9` (2026-07-30) — take it there, not here.** Reproduced by the coordinator rather than inherited from
  the review's report, which grew it: there is a **SECOND symptom** (a static `Self.make(…)` in a `type` body
  is `E2010 Expected '{' but got '.'`), **both programs are LEGAL — the oracle runs each to 42**, and the two
  are ONE bug (`Self` in an expression supports only `Self{…}`).
- **⬜ A FOURTH PRIVATE RE-DERIVATION OF NAME → MACHINE WIDTH, LEFT IN PLACE ON PURPOSE** —
  `IrInterface.builtinProtocolResultStdType` is exactly `valueTagToStdType(builtinTypeNameTag(name))` for all
  three names it accepts. **Filed by W1's review (2026-07-30), and it is the SAME CLASS as the bug W1 fixed** —
  its own header records that this family already cost a wasm trap across 18 spec cases. ⚠ **Deferred because
  the fix is a PLACEMENT DECISION, not a repair**: routing it through the shared table would give
  `IrInterface.maxon` a dependency on `TypeResolution.maxon`/`LowerMaxonToStd.maxon` that its own header
  disclaims, so the clean move is to RELOCATE `builtinTypeNameTag` + `ExitCodeBuiltinName` into
  `IrInterface.maxon` beside the four sibling constants — which undoes W1's deliberate file-private choice and
  is an architecture call, not a bug fix. ✅ **Mitigating, and why it is safe to hold**: it **panics** on any
  name outside its three, so realistic drift is LOUD today rather than a silent wrong answer.
- **⬜ A NESTED PAYLOAD-BEARING UNION AS A UNION PAYLOAD** — `union Expr { direct(ty Ty) }` where `Ty` is
  itself a payload-bearing union. Refused today with a named E2015 (*"a nested payload-bearing union
  payload needs its own destructor cascade"*), and pinned by `union-managed-payload.md`'s
  `error.nested-union-payload`. **Filed by D1b (2026-07-30), which measured it while choosing acceptance
  specs.** ⭐ **It is what blocks `specs/refcount-accessor-borrow-order.md`, which is the natural headline
  acceptance for D1b's own mechanism and is therefore still UNPORTED** — that spec's `exprType(e Expr)`
  binds a managed payload out of a borrowed param and returns it, exactly D1b's shape, and its
  Documentation section is the canonical write-up of the retain-before-release hazard (release the base
  first and its destructor drives the field to zero, so the following incref resurrects freed memory).
  ⇒ whoever takes this rung **ports that spec as its acceptance**; D1b satisfied the hazard by emitting
  the incref at the bind point, but nothing in the corpus exercises it through a nested union yet.
- **⬜ `partiallyMoved` IS NOT CARRIED IN `MoveMark`** — so a consume still leaks across `if`/`else` and
  `try`/`otherwise` arms. **Filed by D1b's review (2026-07-30), which fixed only the MATCH-ARM axis**, where
  the bit *under*-rejected and cost two reachable SIGSEGVs. ⚠ **This one is the opposite sign and is
  therefore NOT a bug fix**: `MoveMark`'s own header (`Parser.maxon:3089`) argues the leak is sound because
  it *over*-rejects, and making it fully path-sensitive would **ACCEPT programs currently rejected**. ⇒ a
  loosening, and a design call for whoever owns the borrow-checker rung — which is why it was filed rather
  than ridden along.

- **⬜ `typeTagName`'s `named` ARM PRINTS `int`, WHICH IS A FALSE STATEMENT FOR A RANGED *FLOAT* ALIAS** (D4's
  review, 2026-07-30, measured both sides of that rung's branch point and IDENTICAL on each — pre-existing, and
  D4 preserved it deliberately). `typealias Ratio = float(0.0 to 1.0)` is legal and round-trips (`half(0.5)`
  ⇒ exit 0), but `r.width` on a `Ratio` parameter refuses with *"'r', which is declared **'int'**"*. It is the
  SAME invented-type class D4 removed, on the other branch: D4's rule is *"the name denotes something ⇒ the
  existing refusal stands VERBATIM"*, so every `named` that DOES denote something still routes through this arm.
  ⚠ **Why it could not ride D4:** the fix is in `TypeRules.typeTagName` itself, whose blast radius is **every
  diagnostic that names a type tag**, not the three member-access doors. ⚠ And the honest scope is wider than
  the float case — the arm cannot distinguish an int alias from a float one from a `named` that denotes an
  `enum`/`union`, so *"what should a tag PRINT as"* is the actual question. Its own header, corrected by D4 to
  say "ranged **INT** alias", is the pin.

- **⬜ A `CharacterSet`-TYPED PARAMETER CAN NEVER BE A MEMBER BASE, THOUGH A LOCAL CAN** (D4's review,
  2026-07-30; measured identical on both sides of that branch point, so pre-existing). `resolveNamedStruct`
  (`Parser.maxon:9461-9473`) re-tags `named`→`structRef` only for names in `containsStruct`, and the layout is
  registered under **`__CharacterSet`** (`SignatureIndex.maxon:1620`) while the user-facing name is
  **`CharacterSet`** (`:1624`) — deliberately distinct. So `function f(s CharacterSet) … s.contains(c)` is
  refused while `let s = CharacterSet.letters()` … `s.contains('a')` compiles and runs. **A real false reject of
  legal Maxon.** ⚠ **This split identity is also why D4 had to add a guard**: without it, D4's new E3011 verdict
  fired on `CharacterSet` and asserted that a name the compiler RESERVES denotes no type — so the two are the
  same root, and closing this one should retire D4's `isCompilerOwnedTypeName` gate as part of the same rung
  (`error.compiler-reserved-base-type-is-not-undeclared` is the case that will go red when it is, by design).

- **⬜ `abortedParseArtifact` DISCARDS A FILE'S ARTIFACT DIAGNOSTICS, AND TWO SITES NOW CARRY A CAUSE BECAUSE
  OF IT** (D4's implementer, 2026-07-30). A thrown `ParseError` drops the file's collected `Diagnostic`s, so a
  parse-time door cannot defer to the authority that owns the fact — `Parser.maxon:25604` (`missingBaseLayout`)
  says so outright, *"a diagnostic the user will never see"*, and D4's undeclared-base arm is the second site to
  reason the same way. That is **why** both raise E3011 themselves rather than letting `resolveTypes` speak. The
  header's own soundness argument — *"each entry is a FINISHED record … no partial state to salvage"* — applies
  verbatim to a `Diagnostic`, so carrying them across the abort would let both sites defer instead of duplicate.
  ⚠ **Why it could not ride D4:** it CHANGES OUTPUT FOR OTHER PROGRAMS — the `checkGenericInstance` E2055 case
  would then print two diagnostics where it prints one — so it needs its own red baseline and golden review.
- **⬜ BOOTSTRAP ORACLE BUG — a keyword-named FREE function cannot be CALLED** (found by D8, 2026-07-30).
  `bin/maxon.exe` accepts `function from(n N) returns N` while it is unused (96,421 bytes of code) and then
  answers **`E3003 Undefined variable 'from'`** at the call — *undefined* for a function it just accepted.
  Declaring is fine, calling is not, so it is a resolution gap and not a designed refusal. **shv2 diverges
  DELIBERATELY and is the correct one** (D8's `parsePrimary` keyword arm, gated on resolution so no
  diagnostic moves): a declaration you cannot use is worse than one you can. ⚠ Needs the full C# suite as
  its gate, which is why it is filed here rather than fixed in a shv2 rung. Note the shape shv2 does NOT
  support either, and both references agree: a keyword call in STATEMENT position (`from(3)` on its own
  line) is genuinely ambiguous with a parenthesized condition after `while`/`if`.

- **⬜⭐⭐ DEAD-`.rdata` ELIMINATION / UNREACHABLE-STDLIB LOWERING — THE ONE THAT WILL BITE THE NEXT WHITELIST ENTRY**
  (P1.8 Slice E's review, 2026-07-28, with a MEASURED repro). `DeadFunctionElimination` prunes FUNCTIONS, never
  `.rdata` — its own header already says so — and `GlobalDataTable.nextStringId` is **ONE counter across four
  prefixes**. Slice E hit this once and cured that instance (an unreachable whitelisted function's range-check panic
  string shifted **193 committed goldens**), but the review then found a **SECOND producer**: `lowerStringLiteral` /
  `lowerByteStringLiteral` run a pass EARLIER and register `.rdata` for any body, equally pre-elimination.
  **Repro:** append one unreachable `export function … returns String` holding a single literal to
  `stdlib/helpers/string/utf16.maxon`; a two-float program that mentions no string goes md5 `765a309c…` →
  `57d2a6cd…`, the blob taking the front of `.rdata` and displacing both `__fconst_` payloads.
  ⚠ **The whitelist's byte-neutrality — the property that lets the list grow one entry at a time — holds today ONLY
  because none of `Clock`/`Sleep`/`utf16` contains a string literal. That is a property of those three files, not of
  the mechanism.** Almost every substantial stdlib module has a literal. **Re-measure trigger: the next whitelist
  entry.** Cannot ride a widening rung: the fix is either the rung DFE's header already names, or a new field on
  `MaxonToStdLowering` (tried as a one-liner; does not compile).

- **⬜ RANGES FOR COMPILER-SYNTHESIZED INT ALIASES — `HashValue` is the one left**
  (P1.8 Slice E, 2026-07-28). A synthesized alias erases to `integer` with no registry entry, so `insertRangeChecks`
  — which guards RETURNS and can only find a range a registry holds — cannot fire for it. Measured consequence while
  `Codepoint` was still synthesized: `utf16DecodeSurrogates(0, low: 0)` returned **−56613888** silently while its
  sibling `utf16LeadSurrogate` panicked correctly, the difference being only that `CodeUnit16` is a registry alias.
  **Slice E fixed `Codepoint` NARROWLY** (seeded as an ordinary ranged registry alias through
  `RangedAliasRegistry.create()`, the one constructor all three registries are built through — seeding either alone
  would have made the planner and the emitter disagree about whether the alias has a range at all) and deliberately
  left `HashValue` alone, so this rung is exactly one name wide. ⚠ **The argument side is a different question and is
  NOT covered**: shv2 does not range-check an argument against its parameter's alias at all.

- **⬜ THE WHITELIST'S NAME-COLLISION DIAGNOSTIC — a real error pointing into a file the author never opened**
  (surfaced by P1.8 Slice E; **pre-existing since `Clock`/`Sleep`**, Slice E merely added 9 more names to it). A user
  `function utf16Width(…)` gets `E3006` positioned inside the whitelisted stdlib file; identical for `function
  sleep(…)` against `Sleep.maxon:5`. No silent shadow and no wrong answer — the verdict is right, the POSITION is
  unhelpable. ⚠ **Not goldenable as-is**: the message embeds an ABSOLUTE path. Needs a shadowing/namespace policy
  decision, which is why it is a rung and not a fix. (Type names are already answered — `isCompilerOwnedTypeName`
  gives `type Codepoint` a clean E2015 at the USER's line. It is FUNCTION names that lack the equivalent.)

- **⬜ CHARACTER RANGES — `'a' upto 'f'` iterates INTEGERS**
  (re-attributed out of P1.8 when it closed, 2026-07-28; measured: `97 98 99 100 101`). 2 corpus cases. ⛔ **One of
  them, `ranges.character-range`, is a KNOWN FALSE PASS and must STAY DISABLED** — it only COUNTS iterations, so 26
  codepoints count like 26 characters; its twin `character-range-print`, which looks at the value, does not pass.

- **⬜ E3005 RENDERS A GENERIC INSTANCE BY ITS COMPILED NAME** (`Array_Wide`) rather than the user's own typealias
  (re-attributed out of P1.8, 2026-07-28). 2 corpus cases in `ranged-element-invariance.md`. A diagnostic-fidelity
  rung: the verdict is correct, the name in the message is the compiler's spelling rather than the author's.

- **✅⭐⭐ TUPLES (first-class, HEAP-ALLOCATED) — CLOSED 2026-07-29** (main `f25737501` · `867126aca` · `e6ccf3d5e` · `25f9aefd3` · `2921a6468`; x64-windows **2372 → 2401/0**, x64-linux PASS, wasm PASS, build 0 warnings, **arm64 SKIP — remote, UNVERIFIED**). `Map` and `for (k, v)` remain OPEN and are the follow-on rung (below).
  ⭐⭐ **A TUPLE IS A SYNTHESIZED STRUCT, NOT A TYPE-SYSTEM ARM.** `(10, 32)` mints a `StructLayout` under a mangled name (`__Tuple2.int.int`) and the value is typed with the existing `MaxonType.structRef(TypeNameId)`. **Measured cost of the alternative: ~25 exhaustive `match` sites** across `TypeRules`/`MaxonDialect`/`LowerMaxonToStd`/`LayoutDescriptor`/`Parser`/`SignatureIndex`/`SemanticCheck`/`TypeResolution`/`TypeNameInterner` would each have needed a new arm. Taking the synthesized-struct route, `TypeResolution.maxon`, `LowerMaxonToStd.maxon` and `MmRuntime.maxon` needed **no edit at all**, and destructors for a tuple containing a `String` came free from the existing `installStructDestructors` walk over `project.structTypes`.
  ⭐ **THE SLICE BOUNDARY WAS MEASURED, NOT JUDGED.** Only **2 of the 18** canonical cases assert allocation (an `mm-trace` block): `small-tuple-return-allocates-nothing` and `for-in-over-map-allocates-no-tuple`. **Every other case asserts an exit code, which heap-allocated tuples satisfy** — which is exactly how v1 ships them. So the two-register ABI is worth **one test**, against a new calling convention on x64 + arm64 + wasm; it is its own rung. 15 of 18 unlocked here.
  ⛔⛔ **THE TWO REFERENCES SPLIT, AND THAT DECIDED THE DESIGN.** On STRUCTURE they agree almost line for line (comma disambiguator after the first inner expression; positional `_N` fields; `.N` → `_N`; destructure via a hidden temp; `for (k,v)` special-cased). On the **ABI they do not**: the bootstrap implements the register pair (verified in its own IR — `RAX`+`R10`, no `mm_alloc`, `R10` chosen because a throwing call already uses `RDX`), while **v1 has no such thing at all** — every tuple heap-allocated, `ret` takes one value, no escape analysis. Corroborated: `stdlib/Map.maxon:359`'s `current() returns Entry` would necessarily allocate per iterator step under v1, which is the exact allocation the spec's own MmTrace case says does not happen. ⇒ `specs/tuples.md`'s ABI section targets the BOOTSTRAP, not v1.
  ⭐ **A DELIBERATE DIVERGENCE FROM BOTH REFERENCES, RULED BY THE COORDINATOR:** `t.0.1` (chained tuple access) lexed as `identifier`, `dot`, `floatLiteral(0.1)` because `scanNumber` greedily eats a following `.` as a fraction. **Both references REJECT it** (oracle: `E2010 Expected identifier but got '0.1'`; v1 makes it a parse error). shv2 now **accepts** it. Justification: the corpus is **silent** — no case, no doc mention — so nothing in the definition is contradicted; both references fail it *incidentally* via a greedy float scan rather than by design; and accepting more never invalidates a valid program. The rule is exact (a digit can follow `.` for exactly one reason — there is no `.5` form). Pinned by an shv2-only case that says so.
  ⭐ **THE MANGLED-NAME JOIN IS `.`, NOT THE BOOTSTRAP's `_`, FOR INJECTIVITY.** `_` is inside the identifier alphabet, so `(A_B, C)` and `(A, B_C)` both spell `__Tuple_A_B_C` — one layout, two element lists, and the second silently *becomes* the first. This is the project's own lossy-key-used-as-identity shape. Sabotage-verified.
  ⭐ **TUPLES ARE COPIED WHERE A STRUCT IS MOVED**, at two sites gated by one predicate, restricted to trivial-element tuples (a managed element keeps the struct rules — stricter, never wrong). **Oracle-verified**: `xs.push(p)` then reading `p` back returns 7 there too.
  🔴 **THE REVIEW FOUND TWO BLOCKERS, BOTH FROM ONE ROOT CAUSE** — a tuple type had **two `MaxonType` spellings** reaching the generic-instance registry, which keys on `(tag, id)` while the mangler reads only the name: (1) a spurious **`E3006`** rejecting a valid program, blaming a `typealias` written once; (2) a **compiler PANIC**, `resolveCallFixups: call to unknown function '__destruct___Tuple2.Num.String'`. Both attributed to the tuple commit by rebuild, both fixed, both sabotage-verified and spec-cased.
  ⚠ **THE LADDER COULD NOT SEE THIS RUNG — NINTH CONSECUTIVE, and this time trivially so:** the feature did not exist until the commit, so the corpus could not contain it. Measured rather than assumed — `--emit-corpus` wrote 465 files / 279,453 lines and every tuple-syntax grep returned **zero**. `Testing/ladders/gentuples.sh` is the committed instrument (7 modes in 4 CONTROL PAIRS, which is what lets a product term separate). ⭐ **What it found was NOT tuples**: `internArrayLiteralAggregateInstances` ran once per FILE over every type registered so far — **O(files × types)**, `phase:signatures` ×2.16→×3.03 per doubling, fixed to ×1.92→×1.99 (CPU 786M → 188M at n=1024). **The struct-only control bends identically**, which is how it was attributed as pre-existing; tuples merely added a second producer of registered types. Rung's own footprint: **+12 allocations flat**, 29 fragment additions, **zero modifications**.

- **⬜⭐⭐ `Map` + `for (k, v)` DESTRUCTURING — the follow-on to tuples, now unblocked.** The tuple half closed 2026-07-29 (above), which was the stated blocker. `Map` is compiled stdlib in BOTH references (`stdlib/Map.maxon`, open-addressed, 4 parallel `Array` fields, 75% load, tombstones — structurally shv2's `Set` plus a `values` buffer and a second drop slot). Corpus: `specs/map.md` 34 · `specs/map-struct-bytearray.md` 3 · `specs/map-try-otherwise-block.md` 4 · `specs/with-iterator.md` 6, all unported, plus `specs-shv2/tuples.md`'s 2 held cases (`for-destructuring-map`, `for-in-over-map-allocates-no-tuple`) and the 2 live `block-scoping.md` markers. ⚠ **`for (k, v)` belongs HERE, not with tuples** — measured: **zero** tuple cases use it, and shv2 has no map-like iterable to pair it with, so it had no source to iterate. ⚠ The first-class `Range` VALUE + iterator protocol (5-6 `ranges.md` cases) still rides this rung — `withIterator()` yields `(Iterator, Element)` tuples.

- **⬜⭐ THE TWO-REGISTER VALUE-TUPLE ABI — the other follow-on to tuples.** Worth exactly ONE canonical case (`small-tuple-return-allocates-nothing`, the only non-Map case in `specs/tuples.md` asserting allocation), and it is a genuinely large piece: **shv2 has no multi-register or hidden-pointer return path on any target** — one GPR (`R8`/x0), every aggregate a heap pointer, one register per argument. So this is a new calling convention on **x64 + arm64 + wasm**, plus escape analysis and a throws exclusion. ⭐ **The bootstrap is the sole model and its gate is the interesting part**: an explicit `IsGprScalar` **whitelist** (integer/bool/cstring only) means a tuple containing a `String` never enters the register path at all, so there is no "register-pair return with a dangling destructor" case to solve — the gate prevents it by construction. Throwing and address-taken functions are excluded module-wide (`R10` vs `RDX` would collide with the error flag). v1 implements none of this.

- **⬜⭐ A LONE CHARACTER LITERAL ADOPTS `Character` ONLY AT AN ENUMERATED LIST OF PARSER SITES**
  (surfaced by P1.8 Slice D's independent review, 2026-07-28; **pre-existing from Slices B/C, NOT Slice
  D** — D extended the door list correctly and completely for its own three positions [set key,
  `cs.contains`, `CharSet from […]`], and the review hunted an N+1th of D's own and found none). Both
  references run all three of these; shv2 refuses all three:
  `f('x')` at `function f(c Character)` ⇒ **E3005** expected 'Character', got 'int' ·
  `members.insert(c)` after `let c = 'y'` ⇒ **E2015** requires a Character key — got a 'int' ·
  `c.byteLength()` ⇒ **E2015** 'int' has no method named 'byteLength'.
  ⚠ **These are FALSE REJECTS, not wrong answers** — which is the only reason they were not fixed in-rung.
  The shape is the `int→float` SEVEN DOORS pattern (one rule, N hand-listed doors); the doors still closed
  are a `let` binding, a user-call argument, a return, a field initializer and a comparison. **The measured
  blocker for the call-argument door specifically:** `SignatureIndex` stores parameter NAMES but no
  parameter TYPES, so at the moment `parseCallArgs` runs the expectation does not exist to consult —
  closing it puts a whole-program query under a per-file one. That is the design question this rung owes.

- **⬜ P1.8 SLICE E — the String view/method leftovers** (split out of Slice D, 2026-07-28; **45 of the 56
  live P1.8-blamed corpus cases**, the largest single unlock left in P1.8). `.utf16()` + the nine `utf16*`
  free functions (13) · byte/codepoint view ITERATION (`for b in s.bytes()`, `.codepoints()`) +
  `.toByteArray()` + `String.from(ByteArray)` (16) · `.isEmpty()`/`.clone()`/`.replaceFirst()`/the
  implicit-append rewrite (10) · String-source cursor iteration (6). **Why it could not ride Slice D:** it
  shares `parseStringMethodCall` (`Parser.maxon:19984`) and `StringRuntime.maxon` with `trim`, so under
  one-file-one-owner it cannot be a concurrent agent — an exclusive-file-list collision, not a size
  judgement. It carries its own spec-port list.

- **✅⭐⭐ A RANGED ALIAS BINDS EVERY POSITION A VALUE MEETS IT — CLOSED 2026-07-29** (main `349002a56` · `57f931f60` · `0f5048d1d` · `07309764a` · `a7f3263ad` · `2ba120280` + integration; x64-windows **2337 → 2372/0**, x64-linux **2274/0**, wasm PASS, build 0 warnings, **arm64 SKIP — remote, UNVERIFIED**).
  **THE FILING WAS RIGHT AND I ALMOST CONCLUDED IT WAS WRONG, BECAUSE MY ORACLE WAS STALE.** `bin/maxon.exe` had not been rebuilt against `maxon-sharp/`; against it shv2 and the bootstrap looked BYTE-IDENTICAL on every probe — same output, same panic, same stack trace. A 54-second rebuild reversed every reading. ⇒ **STEP 1's "ALWAYS BUILD" INCLUDES THE ORACLE. It is a binary too.**
  **MEASURED DIVERGENCE:** shv2 missed the compile-time `E3005` at FIVE positions the bootstrap rejects — call argument, array `push`, struct-literal field, field default, field store — and the RUNTIME guard at a field store. `return` and `as` were already right.
  ⭐ **THE CORPUS SETTLED THE SCOPE, SO NO RULING WAS NEEDED**: `specs/range-check-panic.md` states normatively that the runtime half is emitted at a return, struct-literal field, field store, array-literal element and `as`, and **explicitly NOT at a call argument** (a documented, filed bootstrap limitation). So shv2 matching the oracle there is CORRECT, and only the compile-time half was owed — which is also why shv2 never met the arg-pinning blocker that stopped the bootstrap: no branch, no block split, no pinning pass to break.
  ⛔⛔ **NEITHER REFERENCE WAS THE DESIGN — BOTH HAVE THIS BUG.** The pre-fix bootstrap drove checks off a `MaxonValueKind`; **v1 drives them off `CastCategory`, a five-value enum with NO range field**, and checks NEITHER half at a call argument, struct-literal field, field store or local assignment. **shv2's architecture was already the right one** — a site recorded at PARSE time off the still-`named` `MaxonType`, consumed by a Std-tier pass, immune to the erasure both references died on. The rung pointed it at four more doors.
  ⭐ **THE ERASURE IS ASYMMETRIC AND THAT DECIDED THE WORK**: `resolveTypes` overwrites function PARAMETER types with bare primitives, but **struct field types are never rewritten**, so the field doors already held the alias name. Only the call argument was hard, and the RETURN had already solved it — parameter alias names are now captured in `parseFunction` exactly as `rangeCheckReturnAlias` always was.
  🔴 **THE RUNG FOUND AND FIXED FOUR DEFECTS BEYOND ITS BRIEF, EACH A WRONG ANSWER:**
  1. **Guards chained BACKWARDS.** A guard splits its block, so resolving every site to the parser-recorded block id put guard k+1's cascade *before* guard k's branch. Guards ran in REVERSE SOURCE ORDER (the compiler blamed the second of two bad casts), and `where-clauses.two-constraints` proves it was a genuine MISCOMPILE — two cascades comparing `rbx` against 0 and `r12` against 1000, mixing two fields across two checks. It also broke compare fusion, leaving N−1 `setcc` values in registers until the file overflowed at **N=16** and the RULE 3 backstop panicked. Fixed; both doors now clean at **N=512**.
  2. **A range-check site written inside a CLOSURE was filed against the ENCLOSING function** — a closure has its own SSA numbering, so ids crossed a boundary. A CORRECT program was killed at run time, and the dual let an out-of-range closure cast through unchecked. **Reachable through the `as` door before this rung.**
  3. **The guard fired at END OF BLOCK**, so the rest of the block ran first: an out-of-range value was PRINTED and read back out of a `Percent` slot before its panic, and `100 / d` panicked *"integer divide by zero"* where the oracle panics *"Range check failed"* — the language's own check pre-empted. **USER RULING: fix in-rung.** Fixed with a two-tier position (`maxonOpPos` parser-stamped, `stdOpPos` translated by one `RangeSiteWalk`), composed with the chain via `guardChainBase`.
  4. **A FALSE `E5001`** exposed by (3): `peakOutranks` resolved a full-pool tie to the highest-indexed block, so the driver relieved a chain back-to-front and refused a program that fits in two registers. Fixed by letting a value already in a slot be re-relieved at a COLD peak; loop `E5001` untouched by construction.
  ⚠ **THE LADDER COULD NOT SEE THIS RUNG — EIGHTH CONSECUTIVE, AND A NEW MECHANISM.** The manifest check PASSES (the corpus really does declare narrow aliases) but they are only ever GENERIC TYPE ARGUMENTS, erased to opaque `T` before any door sees them: **128 narrow aliases, ZERO doors**. Measured: the rung-0 and rung-5 binaries each contain exactly ONE range-check panic blob. ⇒ **A NARROW ALIAS IN THE CORPUS IS NOT A NARROW ALIAS AT A DOOR**; a manifest check must count DOORS. `Testing/ladders/genrangesites.sh` is the committed instrument that does, and it found a real `O(sites × blocks)` scan (−27.4% CPU) that the allocation column was structurally blind to.
  ⚠ **AND THE MERGE ITSELF FOUND A THIRD THING**: `origin/main` advanced mid-rung with P1.9's declaration-site half, and on the rebased tree upstream's new **E3062 pre-empted this rung's E3005** in a cross-file case whose caller-side alias was, correctly, unused. The case predated the rule and was rewritten to use it — and now proves strictly more. **Step 11's "re-run the suites if main advanced" is what caught it.**

- **⬜⭐ A UNION CASE PAYLOAD DECLARED WITH A RANGED ALIAS IS UNCHECKED — IN BOTH COMPILERS.** Found
  2026-07-29 by the ranged-bounds rung. `Reading.pct(500)` where the case is declared `pct(v Percent)`
  exits 244 under the bootstrap and under shv2 alike. **Not a divergence — shv2 matches the oracle** —
  and the position is not in `specs/range-check-panic.md`'s normative list, which is why it was out of
  scope. But the rule as the corpus states it (*"a ranged typealias binds every value that meets it"*)
  does not obviously exempt a union payload, so this is a question for the SPEC before it is a question
  for the compiler.

- **⬜ IS STRAIGHT-LINE `E5001` REACHABLE AT ALL, AND IF NOT, DOES ITS MESSAGE BODY BELONG?** Found
  2026-07-29 when the ranged-bounds rung's false-`E5001` fix (`a7f3263ad`) made
  `register-pressure/straight-line-overflow-names-no-loop` COMPILE — 21 parameters live across two
  calls, every one already in a slot by then, so relieving that peak costs nothing unpaid. The case was
  kept and renamed `straight-line-wide-signature-compiles` with the reasoning recorded, **but the
  straight-line `E5001` message body now has NO coverage**, and no program could be constructed that
  still reaches it (with no calls, tier 2 handles straight-line overflow). ⚠ A diagnostic nothing can
  reach is either dead code or a gap in the search — decide which.

- **⬜ A RANGED FLOAT ALIAS AT A FIELD OR ARRAY ELEMENT CANNOT BE RANGE-CHECKED AT ALL.**
  `SignatureIndex.declaredSlotType:5106` re-tags a float alias to bare `float` before `fieldTypeOf`
  answers, and its own comment says so: *"a float alias has no range check today"*. Confirmed by the
  ranged-bounds rung's review to produce no half-applied verdict — a clean absence, not a wrong
  answer. A float PARAMETER is not erased, which is why the call-argument door could be fixed and these
  cannot. Needs what the int side just got: carry the name through a channel `declaredSlotType` does
  not erase.

- **⬜⭐ THE ARGUMENT-MISMATCH SENTENCE HAS TWO VOICES, AND `specs/type-checking.md` IS UNPORTED BECAUSE
  OF IT — NEEDS A RULING.** Found 2026-07-28 by the P1.6 Array-element rung. shv2's parser-side doors
  render `assignTypeMismatch` as *"cannot assign 'int' to variable 'push' of type 'String'"* — the noun
  is **"variable"** for every place, so it now calls a METHOD a variable, and it already called a struct
  field one (*"variable 'Cell.ch'"*). The oracle's corpus pins the **argument-voiced** spelling instead
  (*"argument type mismatch for 'value': expected 'String', got 'int'"*), which shv2 **structurally
  cannot produce from the parser**: that sentence exists INLINE FIVE TIMES inside `SemanticCheck.maxon`
  with no shared helper, so reaching it would mean composing a sixth copy — the exact duplication the
  rung's own review had just consolidated away one file over. ⇒ **The ruling is: adopt the oracle's
  wording behind ONE shared helper (and move the five inline copies onto it), or port
  `specs/type-checking.md` with its wording cases disabled.** ⛔ Do NOT hand-patch the noun at one door:
  it is rendered in a single place (`Queries.maxon:459`) for every caller, so a local fix would give one
  site a private spelling — one rule, two voices, which is what this entry exists to end.

- **⬜ A ONE-BYTE CHARACTER LITERAL IS REFUSED AT A STRUCT-LITERAL FIELD AND AT A CALL ARGUMENT, WHERE THE
  ORACLE ACCEPTS.** Found 2026-07-28 by the P1.6 Array-element rung, which hit the same defect at the
  ARRAY element and fixed it there by asking `characterizedOperand` before the comparison. Measured:
  `Self{ch: 'e'}` gives `E3005 … cannot assign 'int' to variable 'Cell.ch' of type 'Character'` and
  `Cell.create('e')` gives `E3005 argument type mismatch for 'ch': expected 'Character', got 'int'`,
  while the bootstrap compiles and runs both. ⭐ **The cause is a ROSTER, which is the shape to fix
  rather than the sites**: `characterizedOperand`'s adoption positions cover comparison / assignment /
  `return` / CharSet member / (now) array element, and a struct-literal field and a call argument are
  simply missing from it. The call-argument half lives in `SemanticCheck`.

- **⬜ `ProgramSignatures.typeIsManaged`'S HEADER CLAIMS `classifyUnionPayload` "DELEGATES HERE". IT DOES
  NOT.** Found 2026-07-28 by the P1.6 Array-element rung. They are separate implementations and they
  DISAGREE for a generic-alias `named` type (`typeIsManaged` answers managed, `classifyUnionPayload`
  answers `undeclaredName`). No current caller is bitten — every one resolves the alias first — so this
  is filed rather than fixed, but it is this file's signature shape with the documentation asserting the
  single-sourcing that does not exist, which is strictly worse than not claiming it.

- **⬜⭐ A METHOD ON A NON-BINDING RECEIVER IS REFUSED — `"abc".toUpper()` AND `s.toUpper().byteLength()`**
  (surfaced by P1.8 Slice C, 2026-07-28; **pre-existing since P1.2 wave D** — `"abc".byteLength()` has
  always failed the same way). shv2 answers `E2015: Unsupported: . statement` for a literal receiver and
  `E2010: Expected 'interpolation end' but got '.'` for a chain inside interpolation; **the oracle compiles
  and runs both.** `parseStringMethodCall` takes a `VarInfo`, so the receiver must be a bound name. These
  are TWO independent mechanisms — the method dispatcher and the interpolation expression parser — and the
  cure for the first is a `parseStringMethodOnValue` alongside the existing `parseArrayMethodOnValue` /
  `parseBuiltinConformanceMethodOnValue`. ⚠ **Slice C made this far more visible without causing it**:
  `toUpper`/`replace`/`split` all RETURN a `String`, so chaining is the natural way to write them, and
  `string-type.md`'s own **Documentation section** shows `"a,b,c".split(",")` — i.e. shv2 does not yet
  implement its own documented surface. No TEST case needs it (verified by grep over the ported files),
  which is exactly why no gate caught it. It is a clean REFUSAL everywhere reachable, never a wrong answer.

- **⬜ AN UNUSED `for` LOOP VARIABLE IS ACCEPTED WHERE THE ORACLE RAISES E3012** (coordinator-measured
  2026-07-28, while probing something else). `for i in 0 upto 3 'each' … end` with `i` unread compiles
  clean in shv2 and prints the right answer; the bootstrap refuses it with
  `E3012: unused variable: 'i'`. Filed against **P1.8 Slice A**'s `for` machinery, not Slice C. A missing
  LINT, so the divergence is "shv2 accepts more", never a wrong answer — but it is exactly the kind of gap
  that lets a typo'd loop variable through.

- **✅⭐⭐ E3070 BORROW-LIVENESS — COMPLETE 2026-07-28** (main `bb390d834` · `aef7a3480` · `2dd8dbf2b` · `03bf57153` · `d79a0ac71` · `f49f3c088`; x64-windows **1922→2003/0**, x64-linux PASS, wasm32-wasi PASS, build 0 warnings, fragments additions-only and byte-identical after a full run, no 101. ⚠ **arm64-macos/arm64-linux SKIPPED — UNVERIFIED**, owed to the hand sync). **A DIRECT PORT WAS IMPOSSIBLE, AND THAT IS THE RUNG'S THESIS:** both references key a borrow on a VAR SLOT recovered from `varDecl`/`varLoad`/`varStore`, and shv2 has none — `let` binds a name to an SSA `ValueId` in `Scope` at parse time and mints no op, so an IR pass cannot see the bindings *because the bindings are not in the IR*. So the **PARSER RECORDS** onto `IrFunction.borrowFacts` and **ONE RESOLVER DECIDES** in `semanticCheck` (`Compiler/BorrowCheck.maxon`), the first point holding the whole-program `ParamMutationSummary`. It is P1.8 Slice A's write doors asking a SECOND question of the same binding — the lock asks *"is this being iterated?"*, the borrow set asks *"is an element of it still referenced?"* — both answered from ONE derivation (`iterationSubjectNameAt`, shared via `borrowSubjectNameAt`). Three things came out BETTER than the references: liveness is **exact** (a use is a `Scope` resolution) rather than a highest-op-index stand-in; `pop`/`remove` are excluded **structurally** by `emitArrayElementAccessor`'s `owned` flag, so v1's `mutatingMethodNames` list is not ported; and nothing is recorded until a function actually borrows, where the bootstrap allocates a key list on EVERY op. **USER RULING (2026-07-28): the `for`-in element ENROLS**, so `for it in b.items 'scan' wipe(b.items) … end` is E3070 — a deliberate divergence from the oracle, which accepts it because it RETAINS. Its liveness is **LEXICAL over the loop body**, not the loop variable's last use: ordinary NLL would expire the borrow at the last use of `it` and wave through a mutation the *iteration itself* still dies on. ⭐ **EVERY STAGE OF THE PROCESS FOUND A REACHABLE DEFECT THE ONE BEFORE IT HAD CALLED DONE** — the implementer's own probing found 4 (try/otherwise, ternary and `match … gives` merges each DROPPING the borrow, and a blanket attach that falsely rejected `let n = sizeOf(try arr.get(0)…)`), cured by keying `PendingBorrow` on the accessor's **ValueId**; the OPTIMIZER found two quadratics (see the trend log); the coordinator's probe found **`b.wipe()`** — a method clearing its own field — reading freed memory (`4557430888798830399` where the oracle prints 44), which the USER ruled must be FIXED not filed, and whose cure was **ONE MASK ANSWERING TWO QUESTIONS**: `writtenParamMask` ("does passing an immutable binding here make it an error?", which shv2 answers NO for a self-field write, pinned by `self-keyword.md:self-with-params` returning 42) split from `storageWrittenParamMask` ("does this body write the storage this parameter points at?"). **NEITHER MASK CONTAINS THE OTHER — they diverge in four places in BOTH directions**, enumerated exhaustively on `IrFunction` because "a superset" is the plausible and FALSE summary. And the INDEPENDENT REVIEW found the biggest one: **E3070 could not see MODULE STORAGE at all** (the subject was gated on `self.scope.contains`, and a top-level `var` has no `VarInfo` there) — four spellings, four use-after-frees, one of which the oracle refuses with this very diagnostic, so shv2 was giving a WRONG ANSWER against its own oracle. ⭐ **Ports `specs/borrow-checker.md` and flips all 3 of `array-realloc-dangling-ref.md`'s cases — that file is now BYTE-IDENTICAL to `/specs`.** `borrow-not-live-in-otherwise` stays disabled naming **`Map` (Phase 2)**, not E3070. **DECISIONS, not residuals (none holds the rung open):** an opaque-`T` element mints no borrow (a shared generic body cannot know whether the instantiation's element owns heap; refusing would reject what the monomorphizing oracle accepts — and the reviewer probed it as *not demonstrably reachable*); a sibling field shares its base's subject (forced by soundness — `b = other` drops what `b.items` points at — and pinned by `sibling-field-shares-the-subject`); a WITNESS DISPATCH cannot be asked at all, because the conflict is keyed by callee and dictionary-passing has none, exactly as E3019 already documents for the same door; and `borrowSubjectIdOf`'s O(K²) is measured debt in Workstream O. **The superseded filing follows, kept for the reasoning:** Filed by P1.8 Slice A, whose BORROW ruling puts every `for` loop on this path. **shv2 reads a freed element and returns `0x3F3F3F3F` where the oracle either REFUSES the program or gets it RIGHT** — measured both ways by the coordinator: (a) `let it = try arr.get(0) otherwise …` then `arr.clear()` then `it.value` ⇒ shv2 **0x3F3F3F3F**, oracle **rejects, `E3070: cannot mutate 'arr' via 'clear' while it is borrowed by 'it'`**; (b) `for it in b.items 'scan' wipe(b.items) … end` ⇒ shv2 **0x3F3F3F3F**, oracle **accepts and returns the correct 5**, because it RETAINS. ⭐ **NEITHER PROGRAM NEEDS A LOOP TO REACH THE HOLE** — (a) has no `for` in it at all — so this is **pre-existing and P1.8 Slice A neither created nor widened it**; Slice A's lock closes the direct in-loop mutation route (E3019) and the two indirect routes (a field chain handed to a mutating callee; a method clearing its own field) are structurally beyond what any lock keyed on a binding NAME can see. ⚠ **The two references disagree and that disagreement IS the design question**: the bootstrap is safe here *because* it retains, v1 borrows, and **a borrow model is only sound with a liveness pass**. The cure is one of incref-on-get / copy-on-get / a real `E3070` NLL borrow-liveness pass; the code `E3070` is already RESERVED for it and the 3 `disabled-test: E3070` cases in `array-realloc-dangling-ref.md` already pin it. **Sequenced AHEAD of P1.8 slices B/C/D by user ruling, because every later slice adds borrow surface onto an unsound base.** ⚠ Do not confuse this with the ✅ retain-escaping rung, which fixed a borrow OUTLIVING its referent; this is the MUTATE-WHILE-BORROWED half, which that rung explicitly did not build.

- **✅⭐⭐ `break` INSIDE A `match` ARM TARGETS THE MATCH — CLOSED 2026-07-29** (main `46cdeae35` · `77872eee3` · `b1a733167` · `980613d66` · `015c7b939`; x64-windows **2318 → 2337/0**, x64-linux **2245/0**, wasm32-wasi PASS, build 0 warnings, scale linear, **arm64 SKIP — remote, UNVERIFIED**). Filed by P1.8 Slice A under a user ruling that it get its own rung. **ONE ROOT CAUSE, FIVE SYMPTOMS** — shv2's break-target stack held only loops, so a `match` was not a break target at all: `break` in an arm with no loop ⇒ E2047 (oracle 0) · `break 'm'` naming the match ⇒ E2047 (oracle 0) · `break 'loop'` naming the enclosing loop ACROSS a match ⇒ **E2048 "targets its own loop"**, a false reject of the only legal spelling (oracle 3) · `break 'check'` naming the match's own label inside a loop ⇒ E2047 (oracle 23) · and the filed one, an unlabelled `break` in a match in a `while` ⇒ **11 where the oracle returns 23, a SILENT WRONG ANSWER**. ⚠ **The filing named only the last two of the five**; the other three were found by coordinator probing before the wave, which is the whole reason step 2 reproduces before it plans.
  **THE RULE, and it needed no design ruling because all three authorities already agreed** — `specs/match-statements.md`'s own `## Break` documentation section states it verbatim, `docs/WRITING_MAXON_CODE.md:529-532` agrees, and both reference compilers implement it identically: **break targets = matches ∪ loops; continue targets = loops ONLY.** Unlabelled → innermost break target. `break L` naming a MATCH → always legal, any depth, **never E2048**. `break L` naming a LOOP → E2048 iff that loop is the innermost loop AND no match is open. Match EXPRESSIONS are not break targets. ⭐ The asymmetry is real and load-bearing: `continue 'loop'` written across a match is STILL E2048-redundant while `break 'loop'` across the same match is REQUIRED — measured on both compilers, and both spellings are now committed controls.
  **TAKEN from both references: TWO STACKS** (`self.matches` beside `self.loops`), not one kind-tagged stack — and the reason is shv2-specific rather than "it works there": `bindingIndexEscapesLoop` reads top-of-`self.loops` and MEANS "innermost loop", so a merged stack would silently corrupt it, whereas a second stack leaves every existing reader correct by construction.
  ⛔ **NEITHER REFERENCE'S LOWERING TRANSFERS, and that is the rung's one real design finding.** The bootstrap emits `br <label-string>` for a merge block that does not exist yet — it can, because it branches by NAME. v1 instead creates the merge block EAGERLY in `parseMatchHeader`. **shv2 can do neither**: it branches by `BlockId` (the rewrite's thesis), and `finalizeMatchMerge` mints `matchcont` **LAST, deliberately**, so it lays out after every arm body — copying v1 would move block layout and churn goldens wholesale. ⇒ **The shv2-native answer is that a match-targeted break ENROLS ITS BLOCK AS A REACHING ARM EXIT**, draining into the join path `finalizeMatchMerge` already runs. No new block, no early block, no layout change, and ONE join path instead of two.
  ⭐ **This also cured, by construction, a bug v1 had to fix twice.** v1's all-arms-break match wrongly sealed its live merge dead (`maxon-selfhosted/Compiler/Parser.maxon:9713-9729`) and needed a dedicated `mergeReached` flag, because its break blocks were detached-and-dead and invisible to the fall-through count. shv2 needs no flag: the enrolled block is left UNTERMINATED and the dead block is a fresh one that is never enrolled. Verified by the review, and locked by `match-break.every-arm-breaks`.
  **DELIVERABLE: 7 markers flipped + 12 new cases (2318 → 2337).** ⭐ **TWO of the seven were STALE MARKERS blaming the wrong mechanism** — `match-simple/match-arm-field-assignment` blamed "enum scrutinee (Wave B)" and `match-statements.break-only-arms-with-panic-arm` blamed "payload union + panic (Wave B)"; both failed purely on break-in-match, found by flipping every case that USES `break` in an arm and reading the split. ⛔ **And one of those two could NOT be unlocked** — see the parameter-field-store rung below; it is re-disabled with a measured reason, not a guess.
  ⭐⭐ **THE STRONGEST EVIDENCE IS THE GOLDENS, not the suite count: the rung moves ZERO pre-existing fragments on x64-windows AND on x64-linux.** 19 additions per target plus exactly ONE modification, and that one's diff is **the source line alone** (`break` → `break 'loop'`) with **byte-identical IR on both targets**. A change that re-targets a control-flow keyword and moves no existing byte of codegen is the strongest available statement that the new target is reached only by programs that ask for it.
  ⚠ **A COMMITTED SPEC HAD ENCODED THE BUG.** `specs-shv2/match-owned-value-flow.md: temp-scrutinee-break-arm` (an `specs-shv2`-only case, no canonical counterpart) expected exit 7, reachable ONLY if the unlabelled break exits the loop; under correct semantics nothing in its body can change `result` and the program is an **infinite loop** (confirmed: exit 124 under timeout). Repaired to `break 'loop'`, which restores exactly the loop-floor-drop property its own prose states. ⚠ **The bootstrap is not an oracle for it either way** — it refuses the program outright with `E3012 unused variable: 'n'`, a lint shv2 lacks.
  ⭐ **FOUR DECISIONS THE IMPLEMENTATION MAKES WERE UNTESTABLE FROM THE SUITE**, found by the independent review and now committed, two of them **proved load-bearing by SABOTAGE**: reversing the matches-before-loops search order failed exactly ONE test of 229 (nothing else distinguished the two loops), and dropping the `isBreak` gate on `matchDepth` turned an unlabelled `continue` in a `for` into a **compiler panic** — the existing control caught only the labelled route.
  **THE E2049 COUPLING IS NOW RECORDED IN THE CODE.** "Prefer the match unconditionally" computes "innermost break target" only because a match arm body is a single statement that may not open a loop; both references depend on this and **neither writes it down**. A push-depth counter would remove the dependency and was deliberately NOT built — speculative generality for a nesting the language forbids — so the comment names what must be re-read if E2049 is ever relaxed. The review tried every route to a loop inside an open match (`if`, `try` block forms, closures, nesting through the scrutinee) and found none.

- **⬜⭐ A FIELD STORE THROUGH A STRUCT **PARAMETER** IS E2013, WHERE THE ORACLE COMPILES AND RUNS IT.**
  Found 2026-07-29 by the break-in-match rung, as the SECOND and independent blocker on
  `match-simple/match-arm-field-assignment` (that case is re-disabled naming this, not break-in-match,
  which it now clears). Measured on both compilers, **all four cells**, because the obvious control is
  the wrong one: `function scan(state State)` then `state.flag = true` is **E2013 "cannot assign to
  immutable variable"** in shv2 and **compiles and runs** in the bootstrap — while `let s = State.make()`
  then `s.flag = true` is **E2013 in BOTH**. ⇒ **shv2 is RIGHT about `let` and wrong about parameters**,
  so the fix is emphatically NOT "permit a field store through any immutable binding". ⭐ It is the
  P1.4 ruling not carried through: **params BORROW by default** (user, 2026-07-18), and a borrow's
  POINTEE fields are writable even though the binding itself is not rebindable — shv2 conflates
  "cannot rebind the reference" with "cannot write through it". Reproduced with no `match` anywhere,
  so it is entirely independent of the match machinery. ⚠ **A FALSE REJECT, never a wrong answer** —
  which is the only reason it did not have to be fixed in-rung.

- **⬜ shv2 HAS NO E3071 AT ALL — UNREACHABLE CODE AFTER AN ALL-TERMINATING MATCH IS ACCEPTED SILENTLY.**
  Found 2026-07-29 by the break-in-match rung's independent review, and **coordinator-confirmed on the
  merged tree with no `break` anywhere**: a match whose every arm `return`s, followed by a further
  statement, is `E3071 unreachable code after exhaustive match or branching where all paths
  return/throw` in the bootstrap and compiles clean in shv2 (returning the right answer; the dead
  statement is simply kept). It surfaced here only because "every arm breaks to the outer loop" is a
  NEW way to write an all-terminating match, but the defect is general to the construct and predates
  this rung. A missing LINT, so the divergence is "shv2 accepts more" and never a miscompile — same
  class as the unused-`for`-variable E3012 gap filed against P1.8 Slice A. ⚠ **Not spec-portable as a
  passing case**: the oracle REFUSES the program, so the case can only be a `maxoncstderr` one, which
  is exactly what the rung that adds the lint should write.

- **⬜⭐ A GENERIC INSTANCE'S IDENTITY SHOULD BE ITS ELEMENT'S RESOLVED WIDTH, NOT THE ELEMENT'S NAME — USER RULING 2026-07-27: "match the references".** Filed by the P1.7-finish rung. **shv2 is the only one of the three compilers that can NAME a type it cannot resolve.** `SignatureIndex.ByteElementTypeName = b"Byte"` is the name a `b"…"` literal interns its `Array with Byte` under, and shv2 reads no stdlib, so nothing defines `Byte` — which is why `Bytes.create()` + `.append(b"CD")` only agrees on a stride once the *user* declares `typealias Byte = int(0 to u8.max)`.
  ⭐ **THE REFERENCE EVIDENCE, gathered before the ruling and the reason for it:** **neither reference treats `Byte` as a builtin.** The bootstrap registers it as an ordinary `IrRangedPrimitiveType` and `ResolveTypeParamName` (`2-Parser.cs:18347-18356`) resolves it to base `i8`, with `:18320-18322` **explicitly unifying `u8`/`i8` element names** so the auto-generated `__Array_i8` a literal mints and stdlib's `ByteArray` are **the same instance**. v1 does the same through `computeOptimalIntType` (`TypeResolution.maxon:4434`). **Identity is by resolved width; neither needs `Byte` defined, because neither identifies an array instance by its element's NAME.**
  ⇒ **Registering `Byte` as compiler-owned would be a DIVERGENCE from both references, not an alignment** — which is why the coordinator's first framing of the choice was wrong and the user asked for the reference answer before ruling. ⚠ This is a change to **generic-instance identity**, which P1.6-C made deliberately nominal — so it is a rung with its own contract, not a slice. It also subsumes the two divergences the finish rung's own review left open: `[1, 2 as Index]` **and** `[2 as Index, 1]` are *both* accepted by the oracle (→2) and both E2015 in shv2 (the divergence is symmetric — the implementer reported only half), and `takes([10, 20])` against an `Array with Index` parameter is E3005. ⚠ **Collapsing the literal's inference to `integer` is NOT the fix** — measured: it regresses `takes([x, y])`, which works today. The real shape is a **literal-vs-typed-value distinction** in `requireArrayLiteralElementMatches` (a bare int literal coerces into the fixed element type if in range; a typed value keeps its type), which is the same machinery **P1.8's map literals** will want.
- **⬜ A USER `type Array` / `type Set` DECLARATION IS SILENTLY INERT — USER RULING 2026-07-27: leave it, decide when real code forces it.** Filed by the P1.7-finish rung's review. After that rung nothing miscompiles (every door either routes to the builtin or reports E2015 — this was the silent-miscompile fix), but the declaration itself is accepted and does nothing, with no diagnostic saying so. ⚠ **A "builtin base name is reserved" error is NOT obviously right**, because it would fight the stdlib-whitelist roadmap: `stdlib/Array.maxon`'s own declaration is literally `type Array uses Element`, so the compiler would refuse the stdlib it intends to grow into. ⇒ **decide when the whitelist actually reaches `Array.maxon` and real code forces the answer**, rather than guessing now.
- **⬜ THE PARSE CAN BE THE FIRST WRITER OF A GENERIC INSTANCE, AND TWO WRITTEN-DOWN INVARIANTS SAY IT MUST NOT BE.** Filed by the P1.7-finish Slice-3 implementer, which **stopped and reported rather than take a policy decision inside a rung about three front-end divergences** — the right call. `arrayLiteralInstanceFor`'s `named` arm reaches `arrayInstanceForNamedElement`, which **interns**; the declaration sweep over-interns `Array with <declared struct/union>` only — **not ranged typealiases, not per-instance aliases** — so `[x]` with an `Index`-typed `x` interns `Array with Index` *during parse*, mutating the memoized `ProgramSignatures`. That contradicts `internBuiltinIntegerInstance`'s *"the sweep's call is the WRITE and every parser call is a pure READ"* and `deriveInstanceNames`' *"it warms the instances the SWEEP interned, which is exactly the set a parse can name"* — the latter existing precisely to stop a per-file fan-out writing one shared memo. **Pre-existing** (`let x = 10 as Index` then `[x]` does it today) and **not observable through the CLI** (each `build` gets a fresh `Project`, and `verify-warm-rebuild` passes) — it bites the **LSP / long-lived project** path and the future **parallel frontend**, as a cached parse artifact holding a gid a fresh index need not reproduce. Closing it means widening both the sweep's over-intern **trigger** and its **set** (structs + unions + ranged aliases + inner/per-instance aliases — a cross product), across `Parser.maxon`, `SignatureIndex.maxon` and `Project.maxon`, with its own cost curve and gid-ordering risk. **A policy decision about how much the sweep should over-intern.**
- **⬜ SUB-BYTE BIT-PACKING (`bool` + narrow ranged ints) — the boundary P1.7-finish Slice 2 deliberately did NOT cross.** Slice 2 stopped at **byte-granular** 1/2/4/8, exactly where `ComputeOptimalIntType` stops. v1 goes further, packing any zero-based `upper <= 15` into **1/2/4 BITS** (`BuildLayoutDescriptors.maxon:406-462 packedElementBits`), and both references bit-pack `bool`. That needs a **negative `element_size` encoding** and has its own unported corpus: `specs/bool-bit-packing.md` (20 cases) and `specs/ranged-int-bit-packing.md` (11), neither in `specs-shv2`. ⚠ Also still 8: an **enum**-element array, which hits the machine-word fallback for a *different* reason (an enum is not a ranged int) and was left alone deliberately.
- **⬜ SIGNED NARROW ARRAY STORAGE.** P1.7-finish Slice 2 narrows on the **unsigned** ladder only — a negative lower bound keeps 8 — because `ArrayRuntime.emitLoadElem` is one shared byte loop that **zero-extends**, with no signedness anywhere in the record. **Measured**: narrowed naively, an `int(-5 to 5)` storing `-5` reads back **251**. Both references narrow signed because they carry a static signed element type into the load (the bootstrap monomorphizes; v1 stamps `StoreWidth` + signedness). Needs a runtime/ABI channel for signedness — a rung, not a widening.
- **⬜ `sizeof(<ranged alias>)` DIVERGES FROM THE ORACLE.** shv2 answers **8**; the bootstrap answers **1** for `Byte`, **4** for `int(0 to 100000)`, in both a generic body and at a concrete site. shv2 is *internally consistent* (verified by the Slice-2 review), so this is a divergence rather than a wrong answer. Closing it means teaching `Parser.concreteSizeofBytes` to see an alias's bounds — it sizes an already-**resolved** type today — and narrowing `typeLogicalByteSize` in lockstep. ⚠ **That is exactly the desync the Slice-2 implementer measured and correctly declined to half-do** (`Sizer with Byte` read `sizeof(T)`=1 off the descriptor while `sizeof(Byte)` folded to 8 in the same program).
- **⬜ THE `__ManagedMemory` BUILTIN — the `arr.managed` door.** `stdlib/Array.maxon:106` really does declare `export var managed as ElementMemory`, so `.managed` is a genuine language feature; shv2 SYNTHESIZES `Array`'s 48-byte record instead of compiling `stdlib/Array.maxon`, so it exposes no fields at all. After P1.7-finish Slice 1 this is a clean E2015 rather than a **panic**, but the feature is absent: **12 disabled `managed-memory-methods.md` cases** plus the whole unported `specs/managed-memory-builtin.md` (11 cases).
- **⬜ A WIDENING BYTE-STRING BLOB.** Would turn Slice 2's review-added E2015 (a `b"…"` literal in a program whose `Byte` is wider than one byte) into working code, by emitting the blob element-wise at the declared stride. **Same mechanism a widening `__arr_append` across differing strides needs** — the two should land together.
- **⬜ `specs/lexer-parser-robustness.md` IS 0% PORTED**, and it contains `truncated-after-open-bracket`, directly on P1.7-finish Slice 3's mechanism. shv2 answers `E2004 3:1 "Expected expression but got 'end of file'"` where the oracle answers `E2007 3:11 "Unexpected end of input, expected expression"` — different code *and* column, and **shv2 does claim E2007** (`parserUnexpectedEof`), it just does not use it here.
- **◑ THE SPLITTER'S POSITIONAL STRUCTURE — LARGELY PAID 2026-07-27 (merged `e40405890`), AND STILL OPEN.**
  **Allocations went QUADRATIC → LINEAR (×3.96 → ×1.98 per doubling); CPU went N^1.83 → N^1.25 (×3.57 → ×2.48).**
  At `genwidelive` N=400: allocations 2,019,538 → **25,905** (−98.7%), CPU 6,243M → **1,670M** (−72.7%).
  ⭐ **On the many-function scale corpus `regalloc:splitting` is now LINEAR IN BOTH COLUMNS** (allocations ×1.96,
  CPU ×2.12) — the residual N^1.25 is specific to ONE function carrying Θ(N) simultaneously-live values.
  Gates: suite 1794/0; 243 goldens byte-identical; `valuesSplit` 96/196/396 unchanged; x64-windows / x64-linux /
  wasm32-wasi / arm64-macos all PASS; **arm64-linux SKIP** (see the cross-target gate defects below).

  **⚠ THREE THINGS THIS ENTRY USED TO SAY WERE WRONG, and each cost design time:**
  1. **"a lazy range-add / range-max over op positions"** — the per-INTERVAL form is the obvious shape and the wrong
     one. A split cuts one value's range over a stretch Θ(function) wide, so it needs interval endpoints in every block
     of that range **including the pass-through blocks**, re-derived from live sets that are mid-edit. What shipped is
     a **per-op MAX-SUFFIX-SUM**: the backward sweep is a running population `L(p) = L(p+1) + e(p)`, so a block's peak
     is a max-suffix-sum of ONE PER-OP number, and `e(p)` separates per value —
     `contrib(w,p) = (w is a USE operand of p) − (w is live AFTER p)` — so a split's repair is
     **O(uses of one value), never O(blocks in its range)**.
  2. **"the pool is static per op, including the REDUCED pool at fixed-register points"** — there is NO reduced pool at
     a fixed-register point. `classPoolSizeOf(pool, class, file)` is a pure function of three FUNCTION-level constants.
     What a call imposes is `forbidden[v]`, a PER-VALUE mask feeding Hall's condition (`allowedRegistersOf`). So the
     full-pool test is a range-max against ONE constant per class and needs no per-op offset.
  3. **"it needs order maintenance (an implicit treap or equivalent)"** — overstated. **Gap-labelled SLOTS** suffice
     (`SlotGap = 4`), which is what shv2 already does BETWEEN blocks via `SeqStride`. (`SplitEdits.resequence`'s binary
     search is NOT the alternative: it repairs coordinates of records that already exist and says nothing about where a
     NEW op goes, which is the whole question a splice asks.)

  **⚠ AND THE PRESCRIPTION WAS INCOMPLETE — the tree ALONE does not move the exponent.** Measured: 2a alone read
  ×2.81/×3.14, barely moved; 2a+2b read ×2.25/×2.45. Only ONE of the four walks was a range problem. The other three
  were **"walk the whole block to service an O(1) value set"** — `sweepBlockPressure` rebuilt `forbidden` for just the
  victim + its fresh ids, and `reindexSplitValues` re-indexed the same handful. Those are **EDIT-PROPORTIONAL**
  problems (walk the values' USE LISTS), not range-structural ones.

  ⭐ **`chooseVictim` — this entry's own prediction was RIGHT, and the coordinator wrongly overruled it.** Profiled
  BEFORE the fix it was **below 2%** and was struck off as disproved; with the terms burying it removed it is **7.0%**.
  A term measured small behind a dominant one is not a term disproved.

  **⛔⛔ THE CONFINED HALF IS STILL QUADRATIC, AND THIS RUNG DID NOT FIX IT — MEASURED 2026-07-27, and it
  CORRECTS THIS ENTRY'S OWN FIRST CLAIM.** The entry originally said `confinedPeakAt`/`hallVerdictAt` was
  "49.5% of the splitter and LINEAR (×2.03/×2.07), so debt not defect". **That reading was corpus blindness of
  the known kind, on BOTH standing ladders at once**, and it is the reason to state a shape before a verdict:
  - `genwidelive` is one big block but has Θ(N) values against a 14-register pool, so it sits in **FULL-POOL
    mode for ~386 of its 396 splits** — Hall barely engages, and reads linear.
  - the scale corpus IS in confined mode from its first analysis, but its **blocks are small**, so the
    O(block) confined walk is tiny and also reads linear.
  **The product of the two axes — a LARGE block in CONFINED mode — is what neither expresses**, and
  `gen12i.sh <N> 8` reaches it exactly: at `intsPer ≤ 10` **100% of splits are confined**
  (`valuesSplit` == `forced at fixed-reg points`, measured 159/159, 319/319, 639/639 at N=32/64/128).
  On that shape, at head: allocations **69,537 → 269,494 → 1,062,605 = ×3.88 then ×3.94**, CPU ×4.11 then ×4.42.
  **That is QUADRATIC in the exact column.** A/B against `d28583442` proves it **PRE-EXISTING, not introduced**:
  base reads ×3.98 / ×4.14 on the same inputs. **What the rung bought here is the CONSTANT — allocations
  −85.7% / −86.0%, CPU ≈ −14% — and NOT the exponent.**
  **⇒ MECHANISM, and it is one line of code:** `resweepBlock` calls
  `analyzeBlockPressure(wantFullPool: false, wantConfined: wantConfined)`. 2a moved the FULL-POOL peak onto the
  positional index (O(log ops)); **the CONFINED peak still requires the per-op backward walk**, so once a
  function enters confined mode the pass reverts to its pre-rung Θ(splits × block) shape. Profiled at N=64:
  `analyzeBlockPressure` **81.4%**, `confinedPeakAt` 59.0%, `hallVerdictAt` 56.2%, `augmentValue` 38.3% —
  while `refreshAfterSplit` is only 11.1%, i.e. **2b's edit-proportional work is holding; this is the confined
  re-sweep alone.**
  **⇒ WHY THE 2a TRICK DOES NOT TRANSFER DIRECTLY, and where it still might.** Hall feasibility is a property
  of a SET, not a number: there is no per-op aggregate with an associative combine (knowing [a,b] and [b,c] are
  feasible says nothing about [a,c], and adjacent ops have different live sets), and even ONE point cannot be
  maintained cheaply because adding a value triggers an augmenting path that may rewire arbitrarily many prior
  assignments. **A matching is a flow problem, not an aggregation problem.**

  **⛔ TWO DESIGNS FOR THIS ARE NOW REFUTED BY MEASUREMENT (2026-07-27). Do not re-derive either.**

  **(D1) "Index the census SCREEN and run the matching only at candidates" — CANNOT RANK, so it buys nothing.**
  `RegPoolCensus.screenViolates` is a count and IS range-additive, so it can find CANDIDATES. But a confined
  rank's `pressure` is `tight`, and `tight = countConfinedTo(allowed, witness)` where
  `witness = tightRegisterSet(...)` is alternating-path reachability over the FINISHED maximum matching
  (`HallCondition.maxon:567-568`), with a consistency panic at `:575-578` that makes the dependency
  STRUCTURAL. `screenViolates` returns a bare `bool` and stops at the first violating `p` (`:379-384`).
  Since `peakOutranks` orders confined ranks PRIMARILY by `pressure`, an index of the screen can identify
  candidates but never ORDER them — and the screen fires at ~44% of ops, so you still run the matching
  Θ(block) times per split.

  **(D2) "MEMOIZE the per-op verdict and invalidate proportionally to the edit" — DEAD TWICE OVER, both
  measured on `gen12i.sh <N> 8`, N=32/64/128.**
  - **The obvious invalidation rule is Θ(block).** *"Ops whose live set changed, plus ops where a value whose
    `forbidden` mask changed is live"* sums **×3.98 then ×4.00**; mean per split 233 → 463 → 923 (×1.99), max
    per split 2,224 → 4,528 → 9,136. **Cause: an unbroken RELOAD CASCADE.** `let s = scaleSeed()` is live
    across the whole function; one reload serves all its after-peak uses, so the fresh id is live across every
    remaining call and is itself the next victim — N/2 times, each over a Θ(N−i) range. **19% of splits carry
    89.7% of the width.** `markRunStarts` exists to break exactly this, but its rule is *"the op leaves this
    file NO register at all"* and **on x64 no op clobbers all 14 GPRs** (`SplitLiveRanges.maxon:4475-4495`), so
    the INT cascade is unbroken by design.
  - ⭐ **A LINEAR invalidation set DOES exist — and it still does not save the design.** Invalidating where the
    MULTISET `{allowedRegistersOf(v) : v live at p}` changed (which is all `hallVerdictAt` reads) sums
    **×2.01, ×2.01**, mean 29.5/29.5/29.6, **max per split 46/46/46 — FLAT across a doubling ladder**, because
    a victim and its reload have IDENTICAL allowed sets so almost every op cancels.
  - ⛔ **The killer is REACHING the invalidated ops, not finding them.** Recomputing a verdict needs the live
    set AT that op, and shv2's only route is `fillLiveBeforeOp` (`SplitLiveRanges.maxon:3348-3377`), a backward
    walk seeded from the block's live-OUT. That reach cost is **×3.94 then ×3.97** — mean per split
    651 → 1,278 → 2,533 — **quadratic INDEPENDENTLY of the cascade**, and it is ~45% of today's whole sweep.
    ⇒ ***The memo's MISSES are not the cost. REACHING them is.*** Any design that must materialize a live set
    at an arbitrary op inherits this, and that is the sentence to re-read before proposing the next one.

  ⭐ **(R2) THE ROUTE THAT REMAINS, and it is a COUNT after all — but of the right quantity.** Hall is
  infeasible at `p` iff `∃U : #{v live at p : A(v) ⊆ U} > |U|`. A minimal violating `U` is always a **UNION OF
  THE LIVE VALUES' OWN ALLOWED MASKS** — if `U` violates then so does `U' = ⋃{A(v) : A(v) ⊆ U}`, which is no
  larger — so enumerating the union-closure of the distinct masks is **EXACT, not a screen**. For each candidate
  witness `U`, `tight_U(p)` is a per-value-INTERVAL count: precisely the range-additive shape
  `PressureIndex.leafDelta` already carries per class, repairable in O(uses of one value) by the same
  `applyValueToPressure` machinery. **The confined peak becomes an argmax over `(slot, U)` read off the tree —
  no live set, no matching, no memo, so it escapes the `reach` trap.**
  **The number that decides it is the witness-family size, MEASURED:** distinct `allowedRegistersOf` masks are
  **2** on `gen12i` at N=32/64/128 (**flat in N**) and **8** on `genwidelive 100 sum`.
  ⚠ Two real risks, both needing red-before-green: (a) whether the union-CLOSURE stays small on the committed
  corpus, not just on ladders; (b) **the tie-break is a GOLDENS question** — the counting formulation picks the
  MAXIMAL violating `U` while today's code reports the Dulmage–Mendelsohn canonical one, and `peakOutranks`
  compares `(pressure, atFixedRegisterOp)` derived from it.
  ⚠ **OPEN, and do not assume it:** the memo argument above needs `tightRegisterSet`'s witness to be independent
  of WHICH maximum matching `augmentValue` happens to build (D-M canonicity). **The panic at
  `HallCondition.maxon:575-578` does NOT prove it** — that identity holds for any maximum matching. Probe it
  before building on it.
  📌 **The measuring scaffold was deliberately NOT committed** (a compile-time `let MeasureInvalidationWidth`
  plus interval/symmetric-difference width helpers, ~110 lines in `SplitLiveRanges.maxon`): it is O(function)
  per split and must never ship enabled. Its shape is recorded here so it can be rebuilt.

  ### ⛔ DO NOT BUILD R2 YET — THE EXPOSURE WAS MEASURED, AND NOTHING REAL COMES NEAR THE SHAPE (2026-07-27)

  Before designing the fix I asked the cheaper question: **does any real workload put the allocator into
  confined mode with a LARGE block?** The quadratic needs BOTH factors large *in the same function*
  (Θ(N) confined splits over a Θ(N) block). **They never co-occur outside a ladder built to make them.**
  Predictor = `confinedSplits × maxBlockOps`:

  | corpus | largest product | that row's block | its confined splits |
  |---|---|---|---|
  | `gen12i 64 8` (synthetic, known-bad) | **920,315** | 2,885 | 319 |
  | spec suite, ORDINARY specs | **5,193** | 577 | 9 |
  | spec suite incl. deliberate regalloc stress specs | 44,880 | 5,610 | **8** |
  | scale corpus rung5 | 87,040 | **85** | 1,024 |
  | `genwidelive 200 sum` (known full-pool) | 11,280 | 1,410 | 8 |

  - **Ordinary real code is 177× below the synthetic shape.** In aggregate the confined half walks
    **330,146 ops across ALL 1,840 spec programs put together**; ONE `gen12i 64 8` compile walks
    **823,072** — a single synthetic file is **2.5× the entire real corpus**.
  - **802 of 803 confined-splitting functions take FEWER THAN 16 confined splits** (max 19, in a 217-op block).
  - **The scale corpus's 87,040 is not a near-miss**: its block is CONSTANT at 85 ops across all six rungs
    while the split count doubles — total confined-walk ops 8,564 → … → 255,634, ratios 1.93 … **2.00**.
    Only ONE factor grows, so it is exactly linear and always was.
  - **`x64-large-frame-arg7` has the block and not the mode**: 5,610 ops, 796 splits, **8 confined**. The
    size is there; the confined half is not. That is `genwidelive`'s shape pinned as a spec.
  - **Mechanism, and it looks structural:** confined mode is entered by almost everything that splits
    (803 of 826) — what is rare is a large block, and what does not occur is a large block that ALSO splits
    many times while confined. Reaching it needs N call sites in one straight-line block *each* carrying its
    own cross-call live set. Real large blocks (interpolation chains, byte-literal detach, `__gt_subp_spawn`)
    are large because of SEQUENTIAL work, so their live sets stay small and their split counts stay single-digit.
  - Secondary, for whenever R2 is revisited: distinct `allowedRegistersOf` masks in real code are **2–6, mode 2**.
    `gen12i` = 2 and `genwidelive` = 8, so **the ladders BRACKET the real range rather than sitting inside it.**

  ⇒ **VERDICT: the fix is speculative and has NO TRIGGER TODAY.** ⭐ **The trigger, now nameable instead of the
  vague "pressure growing" that let this go unmeasured twice: a GENERATED or MACRO-EXPANDED straight-line
  block with many call sites, or SELF-HOST (P2) — the compiler's own long straight-line dispatch functions are
  the first plausible candidate, and cannot be measured until shv2 compiles itself.**
  ⚠ **Two corpus facts this probe corrected, both of which had been assumed:**
  **(1) shv2 does NOT compile `stdlib/*.maxon` through its own register allocator at all** — every
  stdlib-shaped name reaching the splitter is a SYNTHESIZED runtime function (`__arr_*`, `__destruct_*`,
  `__gt_*`, `__mm_*`, `__set_*`, `__str_*`), so stdlib exposure is fully contained in the spec run.
  **(2) THERE IS NO THIRD REAL-CODE CORPUS — none of the checked-in examples compile under shv2**
  (verified: `examples/fannkuch-redux.maxon` E2015 `for` statement; `examples/nbody.maxon` E3009 float→int
  narrowing; `bench/sortbench.maxon` E2015 `String.toByteArray`). **Self-host is not the only gap**, and any
  future claim about "real Maxon code" has exactly the spec suite behind it and nothing else.
  📌 The exposure probe was also NOT committed (a compile-time `let ProbeConfinedExposure` + one walk counter
  + a per-function `CONFPROBE` line, plus a `ProbeKeepStagedSources` flag in `SpecTestRunner.maxon` because the
  spec runner swallows its compiles' stderr). It allocates per function, so **no `scale-test` memory reading
  from such a tree is valid.**
  **Ladder + knob are the deliverable here: `Testing/ladders/gen12i.sh <N> 8 <out>`, doubling N.**
  ⭐ **The old "re-measure trigger: a single block's PRESSURE growing" was too vague and is why this went
  unmeasured — the trigger is specifically a LARGE BLOCK ENTERING CONFINED MODE EARLY.**

  **⛔ OTHER RESIDUALS** (measured linear-per-split): `fillLiveBeforeOp` 15.8% — the peak's live-before SET is
  Θ(pressure) data no count-tree can carry; `SplitEdits.resequence`/`commit` 8.6%; `chooseVictim` 7.0%. Also
  `refreshSplitTransients` is O(uses(victim)²) worst case, measured ×1.98 per doubling.
  Ladders: `Testing/ladders/genwidelive.sh <N> sum|dead <out>` and `genrespace.sh <N> <out>`; oracle:
  `VerifyIncrementalSplit` (`SplitLiveRanges.maxon`, compile-time `let`, ships `false`) — it caught two real bugs this
  rung and no committed test enables it.
- **✅ THE x64-linux GOLDEN ROT — CLOSED, AND THIS ENTRY WAS ALREADY STALE WHEN IT WAS READ (2026-07-27).**
  It described **1395 passed / 288 failed**, 288 stale + ~489 absent fragments. **Commit `339c2cf07` had already fixed
  it** — regenerating all 777 and verifying 1687/0 — *three commits before the entry was next read.* Confirmed by
  MEASUREMENT, not archaeology: a full `spec-test --target=x64-linux` under WSL2 on `main@d28583442` reports
  **1687 passed / 0 failed**, zero FAIL markers, zero fragment content churn; and
  `git diff --stat 339c2cf07..HEAD -- 'maxon-shv2/Compiler/**' 'specs-shv2/**'` is EMPTY.
  ⚠ **The lesson is the FILING, not the bug** — the same one PLAN.md:2731 states in its own words: *"a ⛔ that is
  already fixed costs the next reader the same hour every time. Re-run a filed repro before planning against it."*
  This is the **second** entry in this file to do it. The splitter rung budgeted its cross-target gate against a lane
  it expected to be dirty; the lane was clean, which is what made "any x64-linux failure is MINE" a usable statement.
  ✅ Still true and worth keeping: **a PLAIN `spec-test` run rewrites fragment MTIMES with zero content change**, so
  use `git status --short` / `git diff --stat` to decide what moved, never timestamps. And **a MISSING golden NEVER
  FAILS** — the runner writes one for a brand-new test and reports PASS, which looks identical to real coverage.

- **⬜ THE CROSS-TARGET GATE HANGS INSTEAD OF SKIPPING, AND THEN MISATTRIBUTES WHY — two defects, found 2026-07-27
  by the splitter positional-structure rung, which they cost ~95 minutes.** `scripts/cross-target-gate.sh` /
  `scripts/remote-mac.sh`. The gate's own contract is *"Unreachable ⇒ SKIP, and the gate still passes… but a SKIP is
  reported, never folded into the green."* Both defects break it.
  **(1) `orb run` HAS NO TIMEOUT.** The arm64-linux preflight is `orb run -m maxon-linux true` — a trivial `true`
  inside the OrbStack guest. With a WEDGED guest it blocks **forever**: measured **82 minutes at 0.0% CPU**, while
  `orb list` cheerfully reported `maxon-linux  running`. A capped re-probe never returned inside 60 s either, so the
  block is uninterruptible. **A gate that hangs is strictly worse than one that fails** — it produces no verdict at all.
  **(2) ITS FALLBACK MESSAGE THEN BLAMES THE WRONG THING.** After the hung run was killed, the script reported
  *"INCOMPLETE — the Mac went away mid-run (ssh exit 255)… Most likely it went to sleep."* **The Mac never went away** —
  it answered `ssh` throughout, and the 255 came from the kill. A wrong diagnosis in a gate is worse than none: the
  next reader retries at a different time of day instead of looking at the VM.
  **(3) A PREFLIGHT FOR ONE LANE TAKES DOWN A DIFFERENT LANE.** `arm64-macos` is NATIVE and needs no OrbStack at all,
  yet it was lost to the linux lane's probe — the matrix reported BOTH arm64 rows as SKIP.
  ✅ **Workaround, rehearsed and used for this rung's step 10:**
  `bash scripts/remote-mac.sh --host=<user@mac> --shv2` (NO `--linux`) runs the arm64-macos lane natively and bypasses
  the wedged preflight entirely — RESULT=PASS, 1603/0. `remote-mac.sh` does **not** auto-apply the golden patch; it
  prints `git apply --stat` and the command, so it is safe to run against a clean tree.
  ⇒ Fix wants: a timeout on every remote probe, per-lane preflight isolation, and a fallback message that reports what
  was actually observed rather than guessing.
  ⚠ **THIS NO LONGER COSTS A RUNG — but it is NOT fixed** (user ruling, 2026-07-27). **The two remote arm64 lanes are
  OUT of the per-rung cross-target gate**: `cross-target-gate.sh` now skips them unless `--mac` is passed, and the
  targets are **synced periodically BY HAND** (`scripts/cross-target-gate.sh --mac --require-mac`). The reason is this
  entry — the expense and the hang are both properties of the REMOTE hop, not of arm64 — so a rung no longer waits on
  another machine. **The three defects above still hold on the manual sync run**, which is now the only thing that
  trips them, and the arm64 lanes are **UNVERIFIED between syncs**, not proven good: golden rot on an unrun lane is a
  measured, recurring fact here (the 317 stale C# fragments; the x64-linux 288-stale/~489-absent entry above).
- **✅ THE WITNESS LABEL JOIN IS INJECTIVE — CLOSED 2026-07-27 (main `55053c207`; x64 1793/0, wasm 1597/0).**
  Filed 2026-07-26 by the P1.7 disjointness rung's independent review, which went looking for whether the newly
  reserved instance names could collide with a witness label and found **the same defect one namespace over, needing
  no generic-instance name to reach it.** `__witness_<conformer>_<interface>` joined with a bare `_`, and `_` is a
  legal type-name character, so `(conformer A_B, interface C)` and `(conformer A, interface B_C)` both labelled
  `__witness_A_B_C`. **Coordinator-reproduced:** two interfaces, two conformers, two `where T is …` holders, right
  answer **43** — shv2 built **exit 0, no diagnostic, and returned 33**, and the same program with the underscores
  renamed away returned 43. ⭐ **The RENAME-CONTROL is what proved it**, because the bootstrap is not an oracle here:
  measured, it refuses the colliding program **and the underscore-free control identically** (`E4006`), so its
  refusal is a general constrained-type-param gap, orthogonal to the collision.
  ⭐⭐ **THE CAUSE WAS NOT THE JOIN ALONE — IT WAS A LOSSY KEY USED AS AN IDENTITY.** `ensureWitnessTable` is
  *"idempotent by label"*, so the second pair never contested the first: **it silently BECAME it**, taking the first
  pair's table and never recording its own relocations. Which pair won is **dispatch order, not declaration order**
  (isolated by the review: same declarations, reversed dispatch ⇒ 33 vs 44).
  **The fix — the single mint site now joins with `.`** (`IrInterface.witnessTableLabel`), injective **by character
  class**: the lexer admits only `[A-Za-z0-9_]` inside an identifier, so the join has exactly one split and
  injectivity is a property of the LEXER, not of a decoder. (Escape-by-doubling would NOT have worked — with a
  one-byte separator a run of five underscores admits three valid splits.) With the label injective its memo key IS
  the pair, so no second pair-keyed memo was added. ⭐ **The argument is shv2's own:** `ensureWitnessTable` already
  called `qualifiedMethodName` — which joins with `.` — to name each witness slot's reloc target, so a table's
  *contents* were injectively named while the table's own *label* was not, **in the same function. One namespace,
  two separators, one of them lossy.**
  ⚠ **NEITHER REFERENCE IS A SOURCE HERE, AND BOTH DEMONSTRATE THE PRINCIPLE ELSEWHERE.** The bootstrap has **no
  witness tables at all** (it rewrites interface call sites statically), yet picks `.` for its one dispatch-critical
  join *because `.` is illegal in an identifier*; v1 has the **identical bug at four duplicated sites**
  (`BuildWitnessTables.maxon:86,189,245` + a hand-rolled recompute in `LowerMaxonToStd.maxon:14627`) and documents
  the collision class for its sibling `__layout_*` label, yet applies the illegal-separator rule to
  `"{qualifiedName}~static"` with the reason spelled out. **We took the principle, not the code.**
  🔴 **THE REVIEW'S FIND — THE INJECTIVITY PROOF NAMED THE HALF THAT CANNOT PROMISE IT.** The comment claimed *"a `.`
  cannot occur in EITHER half"*. False: `mangleTypeArg`'s `named` arm returns interned type names, which include the
  qualified per-instance spellings `joinQualifiedName` builds (`IntWrapper.Idx`) — so what keeps a dotted conformer
  out today is a **front-end gate, not the lexer the comment credited**. The fix survives because injectivity needs
  only **one** half separator-free, and the interface half is a single `identifier` token, so the split at the LAST
  separator is exact whatever the conformer holds. The guard now sits on the half that carries the promise
  (`requireSeparatorFreeLabelComponent`, `IR/Maxon/IrInterface.maxon:168` — reading the byte **out of** the separator
  constant so it cannot drift; R10 generalized it from `requireSeparatorFreeInterfaceName` because
  `witnessSlotImplKey` needs the same scan for a second component), proved
  live by sabotage. *Same defect class the previous rung's review caught twice: a safety argument naming the wrong
  mechanism.*
  ⭐ **DUPLICATION ADJUDICATED BY MEASUREMENT, NOT ARGUMENT.** `WitnessLabelSeparator` and `Parser.MethodNameSeparator`
  hold the same byte and are used in the same function — the project's signature shape. Verdict: **two facts**, and
  the evidence is that their admissible sets differ (the method separator is pinned to exactly `.` as surface syntax
  both references emit; the label separator admits any of ~30 bytes outside `[A-Za-z0-9_]`). Moving the witness
  separator alone to `#` produced **60 golden mismatches and ZERO wrong answers** — one fact written twice would have
  produced a wrong answer.
  **The backstop:** `registerRdataConstantLabeled` was silent first-write-wins on a duplicate label while its readers
  `rdataOffsetOf`/`dataSectionOffsetOf` **panic by name** on a missing one — one map, two opposite opinions about how
  far it could be trusted. A re-registration whose **bytes differ** now panics. ⚠ **It would NOT have caught this
  bug and the header says so**: two equal-method-count witness tables carry byte-identical zero-filled blobs, so only
  the label could separate them. Kept because it is a **panic, not a user diagnostic**, and it catches the residue the
  separator does not — the `__layout_` shape. Hit count **measured at zero** by sabotage (full suite + 256 tables +
  4096 sites + the ladder).
  **Optimizer: no compiler change, and the reason is measured.** The scale ladder's Δ0 is a **corpus blind spot** —
  `ScaleCorpus.maxon:554` generates no `where` clause, so `ensureWitnessTable` is never called — so the rung was
  measured on a purpose-built witness ladder instead: **+0 allocations, +0 bytes** across 256→4096 dispatch sites and
  16→256 pairs, linear on both axes (the label is minted per *site*, before the memo check). It also ran down the
  log's `+612`, reproduced it on the *base* binary, and traced it to a filename-path-length term in the byte column
  plus a bootstrap refresh — **no number was "fixed"**; the allocation column is immune and reads 0.
  **Goldens:** 56 `x64-windows` fragments moved, every one of the 190 changed lines exactly `__witness_X_Y` →
  `__witness_X.Y`, verified mechanically; `x64-linux`/`arm64-linux`/`arm64-macos` **empty** (witness tables need the
  x64-only `funcAbs64InRdata` reloc). 5 new cases in `specs-shv2/type-name-collision.md` — the P1.7 disjointness
  rung's own file, whose witness-vs-*instance* disjointness argument was correct but silent on witness-vs-witness.

- **⬜ CONFORMANCE IS RECORDED UNDER ONE NAME AND ASKED UNDER ANOTHER — TWO FACES, PROBABLY ONE CAUSE.** Filed
  2026-07-27 by the witness-join rung (coordinator + review), which needed a generic-instance conformer for a spec
  case and found it unreachable. **Both are LOUD REFUSALS, not wrong answers** — which is why they are a rung and
  not a fix-now.
  **(a) A generic type's `implements` is validated but its instances never satisfy a `where` clause.**
  Coordinator-measured: `type Box uses T implements Q` compiles and a *direct* `b.q()` dispatches correctly, but
  `type Holder uses U where U is Q` instantiated with `Box with Num` is **`E3017: type 'IntBox' does not implement
  'Q'`** — `checkConformance` accepts and records under the bare `Box` while `checkWhereConstraints` asks
  `typeConformsTo("Box_Num", "Q")`. So the compiler validates a conformance it will never honour.
  **(b) A per-instance ranged alias over `int` does not conform to `Hashable` while a plain ranged alias does** —
  review-measured, in `canonicalizeRangedAlias`; as a `Set` key it surfaces as **E2015**.
  ⭐ **Why this matters to the rung above, and why it must not be "fixed" by loosening:** face (a) is *currently the
  only thing keeping a dotted conformer out of the witness label*, since interned type names include qualified
  per-instance spellings (`IntWrapper.Idx`). That is exactly why the injectivity guard was placed on the **interface**
  half, which is a single `identifier` token — so closing this rung cannot silently reopen the witness collision.
  Verify that property still holds when it lands.

- **🔴 BOOTSTRAP ORACLE BUG — a `b"…"` BLOB IS EMITTED BYTE-PACKED BUT THE ARRAY IS TYPED FROM THE ALIAS, AND THE TWO ARE NEVER COMPARED.** Found 2026-07-27 by the P1.7-finish Slice-2 rung, reproduced independently by its review. With `typealias Byte = int(0 to 1000)` (a 2-byte range), the bootstrap emits the `b"CD"` blob **byte-packed** while typing the array **u16**, so `lit.get(0)` returns **17475** (`0x4443` — two source bytes read as one element) and the `.append` variant reads `17475` mid-array. **shv2 is CORRECT here** (`count=2 e0=67 e1=68`), and refuses the program outright after Slice 2's review added the gate. The sharp form: the bootstrap decides `b"…"`'s element type from whatever alias the program happens to name `Byte`, then emits the blob **without consulting it** — the same "one stamp, two producers, never compared" shape shv2 had, but landing as a silent wrong answer instead of an abort.
- **🔴 BOOTSTRAP ORACLE BUG — a PAYLOAD-LESS union constant as a global SILENTLY MISCOMPILES.** Found 2026-07-27 by the
  splitting rung's implementer (which hit it trying to hoist a `ScannedValues.all` to module scope) and **reproduced by
  the coordinator before filing** — the "Bootstrap oracle bugs" list is its home; it needs the full C# suite as its gate.
  `let TheShape = Shape.circle` on a `union Shape { circle, square(side Side) }` **passes the constant-expression gate,
  builds exit 0 with no diagnostic, and leaves the global NULL** — the first read panics `nil pointer or invalid memory
  access at addr=0x0` inside the reading function. ⭐ **The payload-BEARING form is correctly refused** (`Shape.square(5)`
  ⇒ `E2045: Global initializer … is not a constant expression`), so the gate exists and the payload-less arm slips
  through it while the global-data emitter never materializes the object. **A silent wrong answer, not a crash at the
  right place** — the class this project treats as worst. Repro is five lines; run it through `run_program compiler=csharp`.
  Workaround in shv2 sources until fixed: do not give a union constant module scope (a comment sits at the site in
  `TargetLiveness.maxon`'s `computeLivenessOn` warning the next person off it).
- **🔴 BOOTSTRAP ORACLE BUGS — witness/interface dispatch and its own symbol joins** (found by the witness-join rung
  while surveying for an oracle; the **"Bootstrap oracle bugs"** list is their home — each needs the full C# suite as
  its gate, which is why none rode that rung).
  **(1) `where T is <user interface>` with a struct type argument is refused outright: `E4006: Primitive type 'int'
  has no method named 'idc'`.** Measured on the colliding program **and on the underscore-free control**, which the
  bootstrap refuses identically — so this is a general constrained-type-param gap, **orthogonal to any collision, and
  it means the bootstrap cannot oracle this feature area at all.** shv2 compiles both and returns the correct 43.
  **(2) The bootstrap carries the SAME non-injective `_`-join defect the shv2 rung just fixed, in at least five live
  sites** — `__Tuple_` (`MLIR/Core/MlirType.cs:291-295`), `__ErrorUnion_` (`MlirType.cs:594`), the generic auto-alias
  name (`TypeSubstitution.cs:413-414`, duplicated inline at `2-Parser.cs:21387` and `:21600`), and the inner
  param-name join of `MangleOverloadName`/`MangleOverloadNameWithTypes` (`2-Parser.cs:341,353`). Each `string.Join("_",
  …)`s two or more user-controlled names, so `("A_B","C")` and `("A","B_C")` collide. ⚠ **Nothing downstream would
  catch it:** the rdata/symdata/global/label emitters do a plain dictionary overwrite with no duplicate check
  (`X86CodeEmitter.cs:79-132`, `ARM64CodeEmitter.cs:93-118`) and the PE writer deliberately omits the COFF symbol
  table (`5-PeWriter.cs:272-275`), so there is no linker check either. ⭐ The bootstrap already **has** the cure and
  applies it where dispatch correctness depends on it — `$"{TypeName}.{method}"` and the `$` overload separator, both
  chosen because those bytes are illegal in an identifier (`1-Lexer.cs:429,509`).
- **✅ PARAMETER-MUTATION ANALYSIS — the call-site half of E3019 — CLOSED 2026-07-26 (main `b7e438342`; x64 1774/0, wasm 1572/0).** `grow(a)` where `grow(dest)` pushes and `a` is a `let` was **accepted and the push landed**; the oracle refuses it. Now a **whole-program per-callee effect summary**: `IrFunction.writtenParamMask` (one i64 bit per parameter) **seeded by the parser at `noteReceiverWrite`** — the single site that already owns *which methods write their receiver* — with a worklist least-fixpoint over the reverse call graph (the `solveDescriptorNeedFixpoint` idiom, on the shared `CsrGraph`). ⭐ **Refusing a write and recording it are one decision on one receiver at one instant**; a summary re-reading the *lowered* callees (`__arr_push`, `__str_append`) would have been a second list three tiers from the first. The call-site check bridges source→declaration order through `argSlotPosition` **factored out of `slotCallArgs`**, so a call cannot be summarised against one slotting and checked against another. E3019 now has **one** wire format; it had two and this would have been a third. ⚠ **`argImmutableNames` is SYNTACTIC on purpose** — `let a = p` carries `p`'s own ValueId and the oracle refuses `f(a)` while accepting `f(p)`, so a per-value mark would refuse `f(p)` in any function that also wrote `let a = p`. **This is the deliberate OPPOSITE of the array-alias rung's ruling** (where the mark had to ride the value to avoid a fourth encoding of the resolution order) — same-looking choice, opposite answer, because there the hazard was aliasing and here the oracle's own semantics are syntactic.
  - ⭐⭐ **THE RULING: a method writing its OWN receiver's data is deliberately NOT a parameter mutation — because THE ORACLE DISAGREES WITH ITSELF.** Measured on one program with a `let` receiver: `self.total = self.total + value` is **accepted** while `total = total + value` — the same write, the other spelling — is **E3019**. shv2 has one self-field store for both spellings so it must answer once; it answers the way the corpus pins (`self-keyword.md`'s `self-with-params`), and refusing would have broken two ported, green, `status: stable` cases. 📌 **Second time this session that matching the oracle would have meant matching a bug** — oracle agreement is the default standard, not the ceiling. An **indirect call** (`let f = grow; f(a)`) is likewise deliberately accepted: *both* references accept it, so a conservative refusal breaks agreement rather than closing a hole. Pinned as an accepting case.
  - ⭐ **THREE reachable defects, each made reachable by the fix before it — a chain, not a coincidence.** **(1)** A self-field alias carries no SSA value (`createSelfField` leaves `boundValue` **0**, and 0 is `self`'s own id), so container dispatch addressed the enclosing struct's box as the field: `items.count()` answered **0** for a one-element array **with no diagnostic**, and `items.push(v)` was wrongly E3019. **Lifting the false rejection is what made the wrong answer reachable, so both had to land together.** **(2)** `requireSetKeyMatchesType` compared `keyTag != integer`, so a **ranged-alias key (every parameter)** was refused with a self-contradictory message — pre-existing, and it **survived only because it sat behind the false E3019 this rung removed**. **(3)** ⭐ **The `boundValue`-is-0 SIBLING the review was asked to hunt: `emitCaptureRead` was the THIRD such reader.** A closure naming a field by its **bare** name stored the enclosing receiver's *box* in its env slot and returned **25 where the answer is 4**, clean compile, no diagnostic — and through a ranged alias the same capture **panicked in lowering**: one defect, two faces. `self.n` was already refused and `parseSelfPrimary`'s own header predicts it word for word. The gate moved **inside `requireReceiverValue`** — the one door a bare read, a bare write, `self.x` and a bare `self` all pass — *because the code asserted "the two spellings cannot disagree" and there were three.* Now cleanly **E2015**.
  - **Duplication:** "may this self field be written through?" was derived **twice** from the same column, once **inverted** — *"one question, one field, two askers that must agree; diverge and a `var` field becomes one a method may push onto but no callee may be handed, which reads as a bug at neither site."* Now `Parser.selfFieldIsWritable`, both askers pinned against one field.
  - **Optimizer:** `argSlotPosition` → `indexOfParam` was a **linear scan of parameter names**, and Maxon spells calls with labels *in declaration order*, so slotting a P-argument call cost 1+2+…+P comparisons — **and this rung TRIPLED the exposure** (asked once per call at base, three times at head). O(P²)→O(P) by testing `paramNames[argIndex]` first; **exactly equivalent, because a repeated parameter name is E2014 at the declaration**. At P=64: base 39.6 ms → head 69.8 → **20.5**, growth ×3.05 → **×1.89** against a program doubling at ×2.00 — *below the pre-rung base*. ⚠ Worth **~1.5% on the real corpus**, kept because the trend is toward wider signatures (**the stack-arguments rung made them possible**, and this rung already widened two functions to 12 parameters). The fixpoint itself is **linear, verified by separating the axes** (depth at constant program size: allocations flat 17,602→16,589 across a 1024× depth change) — the `queued` flag earns it, re-enqueueing only on a real mask change. Two further optimizations **declined on measurement**: one worth 0.08% needing a mark/truncate discipline that must survive a `throws ParseError` unwind (*"a leaked frame would mis-blame every subsequent call — a correctness hazard priced at 0.08%"*), and one at 0.68% that *"would only look like an optimization."*
  - ⏭ **Filed, not fixed:** **`specs/pass-by-reference.md` is a rung of its own** — all 28 cases rest on parameter reassignment write-back (`x = 99` in the callee), which shv2 refuses with **E2013**. **A top-level array global cannot be passed to any array parameter** (`let NUMS = [1,2,3]` infers `Array_int`, parameters want `Array_Integer` → E3005) — a gap the array-globals slice created, and it blocks the oracle's nameless-form E3019 entirely. **`self.seen.insert(v)` on a container-typed field is refused** while the bare spelling now works — `requireStructBase`'s documented struct-typed-field gap, its own slice. shv2 anchors every method-call diagnostic **2 columns left of the oracle** (pre-existing, general). **Measured debt (Workstream O):** the fixpoint is O(V + E × min(K, 64)), K = callee parameter count, bounded by `MaxAbiArgs = 64` — **it can only bend if that ceiling is raised.**
  - 📌 **Process:** the ladder generator is committed at `maxon-shv2/Testing/ladders/genmutchain.sh` with its three knobs documented — *the previous rung threw its ladder away, which is the exact failure that directory's README exists to prevent.*

- **◑ TOP-LEVEL MANAGED CONSTANTS — SLICE 1 (`let`) CLOSED 2026-07-25 (main `12dcd9a36`; x64 1689/0, wasm 1494/0).** `let G = "hi"` at module scope was a **parse** error (*"Expected expression but got 'string literal'"*) — shv2's own spec comments named it: *"the const evaluator is scalar-only"*, *"global-var storage is scalar-only"*. Now **a top-level managed `let` is a compile-time constant whose value is BYTES**: `TopLevelConstant` gained a `managedValue(bytes, kind)` arm and the scalar/managed split is made **once**, at the evaluator's entry. ⭐ **ONE mechanism — v1's FOUR-way dispatch on RHS syntax is deliberately NOT ported** (`maxon-selfhosted/Compiler/Parser.maxon:3251-3347`: const-folded string rematerialized per read · eager `__module_init_<n>` array · eager `T.from` · lazy guarded struct). ⭐ **A managed initializer may not name another global — E2004 in BOTH declaration orders, matching the measured oracle** (`let A = "x"` / `let B = A` fails either way: a bare literal goes to the managed bucket, a bare identifier to the scalar-fold bucket, and the scalar folder never sees managed decls). **That is what deletes the dependency-ordering and cycle problems entirely**, and it satisfies *"the compiler must not depend on file order to work correctly"* **by construction** — v1 orders eager globals by **file-path lexicographic sort**, a sort its own source says exists for *build-cache reproducibility*. **THREE reachable defects a green 1680/0 suite never reached**, all oracle-measured: a **SILENT WRONG ANSWER** where a constant's name collides with a type's (`let Widget = "abcdefghij"` + `type Widget.byteLength()` returned **10**, oracle **42** — a managed constant is the first receiver that can be *both* a value and a type, and it was asked in the FIRST arm of all three dispatchers; now a **fallback**, so the type wins when it declares the member and the constant wins when it does not — 42/10, verified); a **false rejection** of `function grow(s String) … s.append(…)`; and **`let s = IntSet.create(); s.insert(1)` silently ACCEPTED** where the oracle raises E3019. One rule had **three premises, two wrong** — now one `requireMutableReceiver`, with only *which names write the receiver* stated per container. ⚠ `VarInfo.isParameter` is **not** `boundValue < paramCount` wearing a flag (`let a = p` carries the parameter's ValueId while being a `let`); recorded at both sites so a future "simplification" cannot delete it. Optimizer removed a **cost cliff the instrument structurally cannot see**: `anyManagedConstant as bool` went permanently true at the first managed constant, after which **every static dotted call in the program** paid a ~4-alloc string-key probe — O(call sites) → O(1) (+390/+710/+1,350/+2,630/+5,190 → flat; coordinator re-measured **+115 at N=128 and at N=512**). *It would have gone live the moment the stdlib whitelist grew one constant.*
  - **✅ SLICE 2a — top-level managed `var` (String + byte-string) CLOSED 2026-07-25 (main `31732e606`; x64 1692/0, wasm 1497/0).** An 8-byte **pointer slot in writable `.data`**, filled by a synthesized `__module_init` before `main` and released by `__maxon_global_cleanup` after it, on all four backends. ⭐ **NOT immortal — the difference from Slice 1, and the spec leak-gates it** (`specs/static-variables.md:450-451`): a `let`'s String is the one shared immortal `.rdata` record, a `var` gets `__str_clone` of it. Verified: `let SHARED = "hi"` + `var one` + `var two`, mutating `one`, prints **`hi hi! hi`**. ⭐⭐ **BOTH runtime branches the references carry are ABSENT BY CONSTRUCTION, not by choice** — the oracle suppresses its init-store decref by matching `func.Name == "__module_init"` and both references null-guard the slot; shv2 emits init stores from *different code* than user stores (so nothing asks at runtime) and `__module_init` runs unconditionally over the same list (so no slot is ever null). Both premises were verified by the review on every path (disjoint emitters · both functions DFE roots · `managedGlobals ⊆ declaredGlobals` via an **exhaustive** match so a new arm is a compile error in both · all four stubs gate on one `usesManagedGlobals` bit · straight-line init). **`Parser.requireStorableGlobal` was DELETED, not extended** — its surviving half already lived in `globalStdType` and the two had drifted (one had no `float` arm); after this rung every reachable outcome is storable, so keeping it would have left an unreachable **user diagnostic**, worse than an unreachable panic. ⭐ **THE REVIEW CAUGHT A SILENT WRONG ANSWER**: `parseMethodCall` resolved a receiver name in a **different ORDER** than a bare read — `parseVariableReference` asks *capture* before the top-level fall-through, `parseMethodCall` had them inverted, so in one closure body `{msg}` took the capture while `msg.byteLength()` took the **global** (printed `13 ab`; neither reading is a compile error, so nothing failed). Inherited from Slice 1's `let` half — **2a is what made it dangerous, because a `var` receiver is WRITABLE**: a mutating method would have written the global while the program named a local. Rule now stated at all three sites that encode it. Also enforced `indexFunctionsByName`'s documented-but-unchecked "installed at exactly one site" (`ModuleInit` is the first installer to append into the *memoized* module), and single-sourced the slot-address emission (divergence = init fills one slot while cleanup drops another, at run time, on an accepted program). Optimizer: `parseMethodCall` resolved the receiver name **twice** (−12.000 allocations per managed-`var` site, −6.000 per `let` site) — ⚠ **and its FIRST fix was worse**, costing +441…+3,465 on a corpus declaring **no managed binding at all**, because a payload-free `none` is still a heap object, so a union-returning gate allocated once per *dotted call*: **the narrow ANSWER was not the cheap thing, the narrow QUESTION was.** Marginal cost pinned at **321.0 allocations per global** from 512→16,384 (a superlinearity makes the marginal grow); `regalloc:splitting` flat 1,203→1,242 at 16,384 globals against a control ladder reproducing its known quadratic at ×3.88→×3.99.
  - **✅ SLICE 2b — array `let`/`var` CLOSED 2026-07-26 (main `b08622781`; x64 1713/0, wasm 1518/0).** Slice 2a's slot / `__module_init` / `__maxon_global_cleanup` / store path reused **verbatim** — this slice built the **value**: `initialRecordOf` gained a third arm (`__arr_create` + one `__arr_push` per element), a String element is `__str_clone`d (the array becomes sole owner), and elements fold through the **same scalar constant evaluator** (`let BASE = 40` / `[BASE, BASE + 2]` → **42**, oracle-exact). ⭐ A premise changed: **`mutable` no longer decides which door a use goes through — `TopLevelDecl.hasStorage` does** (`DeclKind` = `inlined | stored`), written where the outcome is written. ⚠ **DELIBERATE DIVERGENCE: an array global is HEAP for both `let` and `var`**, where the oracle makes a never-mutated `let` array an immortal shared static record — shv2 cannot until the two prerequisites in the immortality residual below are built. ⭐ **A REACHABLE MEMORY-SAFETY DEFECT, found and fixed by the wave**: `let A = [1,2]` · `var b = A` · `b.push(9)` **grew A** (3 where 2 is correct) with E2013 *and* E3019 both intact — a borrowed aggregate bound to a `var` is promoted by an **incref of the same box**, so an immutable binding acquired a **writable alias**. Refused with **E2015**, keyed on the SOURCE's immutability. ⚠ The incref was deliberately **not** widened into a copy: a borrowed array bound to a `var` genuinely *does* share in both references when the source is **mutable** (verified — a `var` global, a parameter and a local all still share). ⭐⭐ **AND THE MARK RIDES THE VALUE, NOT THE INITIALIZER'S TOKENS** — deliberately, because a token test would have been **a FOURTH encoding of the local→capture→top-level resolution order**, an order that had already produced a silent wrong answer in *both* preceding slices. ⭐ **THE REVIEW THEN FOUND THE ONE HOLE IN IT: the mark did not survive a MERGE.** A value merge mints a new value, so the mark stopped at every join and the whole refusal was **bypassable by writing the same thing one keyword longer** — `var pick = A if c else M`, `A if c else A`, a reassignment from a merge, and a `match`-expression all compiled clean and returned 3. Closed at the phi mint in `finalizeMatchMerge`, which the ternary and the match-expression **both** route through, so one call fixes both by construction; the mark's WRITE is now single-homed, because a second insert site skipping the copy-on-write identity test would have inserted into the **module-level shared empty set** and every function in every file would have seen a mark naming an unrelated value in its own SSA space. Optimizer: that same mark was a **linear-scanned array** at the promotion gate — Θ(reads × promotions) per function, **39% of `phase:parse` at K=16,000** (669.85 → 410.87 ms), now a `Set`. ⚠ **It allocated +21 across the whole ladder, so `scale-test` is STRUCTURALLY BLIND to it** — found by reading and confirmed with `--metrics`. **The second pure-TIME superlinearity this session that the memory instrument cannot register by design.** The optimizer also **declined** to port v1's `ConstantArrayLiteralRdata` with reasons (~63 allocations/~30 µs per constant element is real but **linear**; it is the *language's* array-literal cost, not the top level's — an in-body control pays the same; and its `capacity = -2` encoding needs a `__arr_push` detach the runtime lacks — "a rung, not an optimizer pass").
    - ⚠ **REPORTED, NOT FIXED — the guard is INTRA-FUNCTION and stops at a call.** `g(A)` whose callee does `var b = xs; b.push(9)` still grows a `let` array (measured 3, integer and String elements alike). Whether a parameter may hold an immutable global's record is a property of every **call site**, not the callee, so it needs the whole-program call-graph fixpoint the transitive-consume case already waits on — i.e. **exactly the mutation-enforcement prerequisite the immortality residual names**. ✅ **The oracle does not refuse it either — it shares and then LEAKS (exit 101)** — so shv2 is no looser, only not yet stricter. Two narrow closures were considered and rejected: refusing `var b = <borrowed param>` breaks the oracle agreement, and refusing the global as a call argument kills `sum(NUMS)`. ⭐ **The spec claimed "cannot be laundered into a mutable ALIAS" full stop; that was FALSE and now states the exact bound** — an overclaiming spec is worse than a silent one.
    - 📌 **Epistemic note worth keeping:** for the direct `let` case the **oracle also returns 3, and leaks**. So this refusal is shv2 refusing a program the oracle **miscompiles**, not matching it — *"2 is correct" is a semantics judgement, not an oracle reading.* Oracle agreement is the default standard, not the ceiling.
  - **✅ SLICE 3 — DEAD-GLOBAL ELIMINATION CLOSED 2026-07-26 (main `7cc15f25f`; x64 1720/0, wasm 1521/0).** New `Compiler/IR/Maxon/DeadGlobalElimination.maxon`: `liveGlobalLabels` walks the merged Maxon module once, collecting the `.data` labels that `globalAddr` ops name; `declaredGlobals` and `managedGlobals` both filter through one `contributesStorage`. ⭐ **The slot, the init ops and the cleanup decref are NEVER BUILT, not removed** — all three are produced from the same filtered arena, so there is no splice, no re-layout, and no way to drop one and keep another. **Measured, coordinator-verified: a dead array global and a dead String global each emit 1,512 bytes — IDENTICAL to the same program with no global at all (was 3,676) — while a write-only global stays 3,710 unshrunk, and one dead + one live array still returns 20.** ⭐⭐ **BIDIRECTIONAL LIVENESS CAME OUT FREE, and the reason is the rewrite's thesis:** shv2's three-op split means a read and a write **both open with `globalAddr`**, so there is only one question to ask — where the references must name *both* `globalLoad` and `globalStore`. **A rule naming two forms can be half-written; this one structurally cannot be.** ⚠ **v1's granularity model was deliberately NOT ported**: it prunes all-or-nothing per `__module_init_<n>` with one init *per file*, and shv2 has ONE `__module_init` for the whole program, so all-or-nothing would mean never dropping anything — shv2 prunes **per-global**. ⚠⚠ **Nor was the oracle's heuristic**, which is a **measured bug**: `IsPureAllocatorCallee` decides side-effect removal by NAME (`callee.EndsWith(".create") || ".from"`), so `Counter.build()` keeps its side effect while the identical body renamed `Counter.create()` **silently loses it**, both compiling clean. shv2 is conservative, as `specs/dead-top-level-var-elim.md:31` actually requires. ⭐⭐⭐ **THE REVIEW FOUND THE SAFETY PROPERTY THE DESIGN DID NOT KNOW IT HAD.** A liveness predicate is *"a predicate placed ahead of what it guards"* — the shape that produced a **silent wrong answer in the three preceding slices**. Here it cannot: all three backends resolve a `globalAddr` label through one lookup (`GlobalDataTable.dataSectionOffsetOf`) which **panics BY NAME** on a label the layout does not hold. **Proved by SABOTAGE** — forcing `liveGlobalLabels` to answer "nothing is live" made x64, arm64 and wasm each die with `panic … no .data slot is labelled '__data_G'`. ⇒ **an under-walk here is a COMPILE-TIME ABORT, never a miscompile**, which is what makes the pass safe to extend; now recorded in the file header and `ARCHITECTURE.md`. 28 probe programs (ternary · match · loop · try/otherwise · call argument · closure body · **uncalled** function, kept · deep call chain · indirect call · async/await · interface method · generic instance · interpolation · compound assign · `as` cast · cross-file · the label-collision hazard **both ways**) all correct; a dead String global's binary is **byte-identical** to the same program with no global. The review also fixed a **real desync hazard** the optimizer had scored as a harmless re-fetch: `contributesStorage` took an arena *index* **and** a *decl* with nothing forcing them to agree — pairing `declAt(j)` with index `i` yields `i`'s label under `j`'s storage bit, **a wrong answer rather than a compile error**. Now the decl alone; seven redundant `declAt` calls gone. ⚠ **The 3 canonical spec cases were ALREADY GREEN ON BASE** — they assert only exit codes, so the canonical corpus **structurally cannot see this pass**; 4 shv2-authored `RequiredData` cases were added (each verified to fail from *both* the too-eager and too-timid side) and are the only instrument that can catch a regression here. Optimizer: **no code change, and the reason is measured** — it went past the implementer's evidence by building the two shapes the ladder cannot reach (many ops/one global: flat at 707 allocations with the walk live; many globals/few ops: flat at 689) and the real **O(ops × globals) diagonal at ×2.05**; `Set.reserve` priced (0.004% at rung 5, **exactly zero** on the real corpus) and declined.
    - ⏭ **Coordinator's call, filed not blocked:** `liveGlobalLabels`, `scanRuntimeUsage` and `markMaxonCallees` each spell the same `functions → blockRefs → opRefs` traversal (2 of the 3 predate this slice). Deliberately not deduped — each has a different per-op classifier, a shared visitor would need closures over unrelated context, and the backstop above turns the only under-walk that could have been silent into a loud abort. Worth a consolidation rung eventually; forcing it now would build an abstraction for its own sake.
    - 🔴 **MEASURED DEBT (Workstream O) — `stdlib/Array.maxon:571` `hash()` is djb2 masked to 32 bits and `Set`/`Map` index on the LOW bits**, so identifiers sharing a prefix and differing only by a numeric suffix cluster hard under linear probing: **77.5 probes/insert at K=16,384 vs 0.53** for well-distributed keys (`×1.99 → ×3.66` against a flat `×2.05`). ⚠ **Another shape `scale-test` cannot see** — same binary, same K, *identical* allocation and byte counts. **Linear in practice today** (the corpus knob tops out at 1,024, ~7,800 probes total) and *erratic* (hash/capacity resonance) rather than superlinear. **Re-measure trigger:** machine-generated code minting ≥8k prefix+numeric identifiers in one namespace. Not fixed because the fix reorders every `Set`/`Map` iteration in every program.
  - ⏭ **RESIDUAL — byte-string record immortality.** A **String** constant is one immortal `.rdata` record shared by every read; a **byte-string**'s *payload* is immortal deduped rdata but its **48-byte record is per-read and OWNED**. Two named prerequisites, and **one of them is now DONE**: enforcement that such a value is never mutated is COMPLETE (the E3019 receiver rule plus the call-site rule the parameter-mutation rung landed — a `let` handed to a parameter the callee writes, transitively, is refused), and a real **copy** promotion for `var b = <borrowed array>` (today `__mm_incref`s the box, and an rdata box has no refcount word) REMAINS.
  - ⏭ `byte-string-literal.top-level-let` stays disabled — needs `ByteArray`, which is **no compiler's builtin**: it is `stdlib/File.maxon`'s `export typealias ByteArray = Array with Byte`, and the whitelist loads only `Clock.maxon`. **Belongs to the stdlib-whitelist chain.** Supplying the typealias makes that exact program build and return 0.

- **✅ E3019's OTHER HALF — PARAMETER-MUTATION ANALYSIS — CLOSED 2026-07-26 (x64 1769/0, wasm 1569/0).** `let a = IntArray.create(); grow(a)` where `grow(dest IntArray)` calls `dest.push(9)` was **BUILD=0 RUN=9** in shv2 and `E3019 … mutates parameter 'dest'` in the oracle. Now a **whole-program per-callee effect summary** — `IrFunction.writtenParamMask` (one i64 bit per parameter) closed under the call graph by a worklist least-fixpoint (`SemanticCheck.buildParamMutationSummary`) and read at every call site. **Direct · transitive · recursive · mutually recursive** all refuse with the oracle's message at the oracle's line and column, byte-for-byte; `var`, a parameter, a temporary and a `let` at a NON-written position all still compile. ⭐ **THE SEED IS THE PARSER'S, NOT A RE-DERIVATION.** The base fact is recorded at `noteReceiverWrite` — the one site that already decides whether a receiver-writing method may be called at all, which is where `arrayMethodMutatesReceiver` / `setMethodMutatesReceiver` / String `append` already answer. Refusing the write and recording it are ONE decision on ONE receiver at ONE instant. A summary that re-read the LOWERED callees (`__arr_push`, `__str_append`, `__set_insert`) would be a second list of receiver-writing operations three tiers from the first, free to disagree about a method added to either. ⭐ **THE BLAME NAME RIDES THE CALL OP** (`MaxonOp.call.argImmutableNames`, a fourth column through `pushCallArg`) because immutability is a fact about the SYNTAX: `let a = p` binds `a` to the parameter `p`'s own ValueId, so a per-VALUE mark would refuse `f(p)` in any function that also wrote `let a = p` — while the oracle refuses `f(a)` and accepts `f(p)` (measured). ⭐⭐ **TWO REACHABLE DEFECTS FIXED IN THE SELF-FIELD RECEIVER, both prerequisites and one of them SILENT.** A self-field alias carries no SSA value (`createSelfField` leaves `boundValue` 0 — and **0 is `self`'s own id**), so the container dispatch addressed the enclosing struct's BOX as if it were the field: `items.count()` inside a method answered **0** for an array holding one element (oracle: 1, no diagnostic anywhere), and `items.push(v)` was **E3019** against an ordinary struct-with-a-container method (oracle: accepted). `methodReceiverBinding` now materializes it through the same `parseSelfFieldRead` a bare read uses and takes its writability from `layout.fieldIsMutable` — the SAME column `emitCheckedSelfFieldStore` asks of `n = 1`. `builtinConformanceReceiverValue` was that rule's third copy and is gone. ⚠ **This was the trap the brief names: lifting the false rejection is what makes the wrong-answer path reachable**, so both had to land together. ⭐ **THE `let` STRING / BYTE-STRING WRITE IS NOW UNREACHABLE, which is the residual below.** `let t = "ab"` handed to `function grow(s String) … s.append(…)` returned **139** — it appends into the ONE immortal `.rdata` record every read of that literal shares. Same for a top-level `let G = "hi"`. Both are E3019 now. ⚠ **A METHOD WRITING ITS OWN RECEIVER'S DATA IS DELIBERATELY NOT A PARAMETER MUTATION, and the ORACLE DISAGREES WITH ITSELF there:** on one program with a `let` receiver, `self.total = self.total + value` is ACCEPTED (42) while `total = total + value` — the same write, the other spelling — is E3019, because its analysis matches op TYPES and only the bare spelling emits the operation its self-field check inspects. shv2 has exactly ONE self-field store for both spellings, so it must answer once; it answers the way the corpus pins (`self-keyword.md`'s `self-with-params` = 42, and `parseSelfFieldAssignment`'s own stated rule that a `let` struct binding *"does not reach inside the type's own methods"*). ⚠ **An INDIRECT call and a WITNESS dispatch are NOT checked**: neither names a callee to summarise, and **both reference compilers accept** `let f = grow` then `f(a)` (measured), so a conservative refusal would break agreement on a legal program rather than close a hole — pinned as an accepting case so the gap is visible. ⭐ **THE ADJACENCY IS A `CsrGraph`, and the first draft's `Map with (ByteArray, OpIndexArray)` was exactly what `CsrGraph.maxon`'s header forbids.** MEASURED on a purpose-built ladder that doubles functions, call sites AND depth together (a maximal chain where every function ends up in the summary): the map index cost `phase:semanticCheck` **+984 → +13,104** allocations from N=400 to N=6,400 — linear in the call graph, one array per callee — where the CSR costs **+142 → +238**, and the drain loop hashes nothing. Time on the same ladder is a CONSTANT ×2.1–2.3 over base with the same growth exponent, i.e. the fixpoint is linear. The rung's whole remaining cost is `phase:parse`, **+0.88 allocations per source call site** (the new column), attributed by a same-worktree base A/B; the flat-stack elimination of even that is measured (0.08% of a compile) and left for the optimizer.
  - **Spec port survey.** `/specs` has exactly two files mentioning E3019: `immutable-method-call.md` (already ported; extended here with the four self-field receiver cases) and `pass-by-reference.md` — **REJECTED, and it is a rung of its own**: all 28 of its cases rest on parameter REASSIGNMENT write-back (`x = 99` inside the callee), which shv2 refuses at the CALLEE with **E2013**, so even its one E3019 case (`let-to-mutating-param-error`) is refused earlier and for a different reason and would not go RED→GREEN. `borrow-checker.md` (E3070) and `var-should-be-let.md` are different rules. The 28 new `parameter-mutation.md` cases are authored to the brief's list, every expectation measured against the runnable oracle one program at a time.

- **✅ STACK ARGUMENTS — >6 PARAMETERS — CLOSED.** The six-parameter cap is GONE, not raised: it was never a limit of the machine, only of a milestone that had not been built, and it refused **34 of shv2's own functions**. Arguments past a register file now travel in stack slots on x64 and arm64, and wasm never had a limit to remove (its parameters are locals). What remains is a **STATED** ceiling with its own diagnostic — `MaxAbiArgs = 64`, the width of the per-argument float mask both ends of a call route through — and a **lower, separate** one for `async` spawns (`MaxAsyncArgs = 6`, the green thread's inline argument region, which the hand-assembled trampoline reads back into registers), also diagnosed where it used to be a compiler panic. ⭐ The rung's thesis: **ONE placement rule, consulted by both ends.** The callee's recomputing `computeParamSlotIndex` and each caller's running-counter twin of it were two spellings of one rule held together by a comment — survivable while every argument fit a register, and not survivable once "which slot" means either a register index within one FILE or an index into the single merged STACK area. Both now call `abiFileSlotIndex` / `abiArgIsOnStack` / `abiStackSlotsBefore`. ⚠ The trap the corpus caught: **the stack stores must be emitted BEFORE the register moves** — an argument register an earlier move loaded is not a *value*, so nothing marks it live to the call, and materializing a later stack argument's constant took it back (`x64-stack-arg-disp32` returned 400 for 200). Both reference compilers order it this way and say why.
  - ⭐⭐ **THE LESSON, and this rung hit all three: REMOVING A LIMIT DOES THREE DIFFERENT KINDS OF DAMAGE.** **(1) It ACTIVATES code that documented its own unreachability** — the `async` 7-arg panic above, *and* `E5001`'s line-0 fallback carrying *"which cannot happen for a real loop overflow"*, so a **loop-free** program was refused with *"the loop at &lt;file&gt;:0 needs 52 more register(s)"* and every candidate ranked "used 0 times in the loop" (fixed without touching the register allocator — the refusal was right, only the rendering wrong; loop body byte-for-byte unchanged on four targets). **(2) It DELETES a check that was doing real work** — a hand-assembled runtime chunk reads its arguments only out of registers, so the loud `argRegisterAt` failure became **a stack store nothing loads: a silent wrong answer**. The ceiling is now stated at the one door a call site learns its callee is a chunk. **(3) It FALSIFIES documentation** — `x64-stack-arg-disp32.md`'s prose described **v1's ABI verbatim** (seven register parameters, first stack arg `[rbp+0x10]`, boundary at the 22nd) where shv2 has **six**, `[rbp+0x30]`, boundary at **a17**; verified against emitted code. The tests still pin what they claim (each sums every parameter, straddling the boundary either way) — only the narration was false, in the spec named after the convention this rung defines.
  - ⚠ **Two arm64 frame immediates silently TRUNCATE**, reachable arithmetic the moment `recordOffset` stopped being a literal `0`: `arm64EmitAddFpFromSp` masks to **imm12** (an over-range offset points x29 *inside* the outgoing region, corrupting every spill and every incoming argument), and `arm64EmitFrameRecordPair`'s imm7 is **SIGNED** (a scaled offset of 64 puts the frame record *below* sp, so the epilogue reloads x30 from the outgoing area). In range today only as a conclusion about three numbers in three files; both now assert. **arm64 has NEVER EXECUTED** — verified by disassembling a caller/callee round trip with 10 parameters on both sides so both regions are live at once. Real bytes, **not a run**.
  - **A `try` inside the closure parser's context-save window** made **every closure program** die `mm_decref: refcount underflow` — **43 suite failures, on six-parameter programs, from a check about sixty-five** — because ~25 `savedX` locals alias the fields they were read from and a throw edge releases what the field still holds. ⚠ **The window is still there; only the check moved out. And its guard comment named the WRONG window** ("nothing may throw between the saves and the restore" — three things there must throw); the real invariant is **first save → last reassignment**, now verified field by field and stated precisely. *A comment that misidentifies the window cannot protect it.*
  - **Measured and ACCEPTED, not fixed:** the placement rule is **Θ(N²) per call site** in arity — but N ≤ 64 by diagnostic, so program-wide it is **linear**, and at the worst legal arity the whole quadratic term is **+0.24 ms inside a 3,273 ms compile** (isel flat 1.55→1.79 ms at constant total arguments while `regalloc` ran 9→3,251 ms — the budgeted `SplitLiveRanges` debt a naive many-args ladder measures instead). ⚠ **The scale corpus structurally cannot exercise this rung: maximum arity 2, zero functions over the old cap.** `calleeArgFloatMask` rebuilt bit-by-bit the mask that IS the stored map value, twice per direct call op — now one lookup; ⚠ deliberately **not** truncated to `argCount` bits, because `(1 shl argCount) - 1` **overflows the word at the legal argCount of 64**.
  - ⏭ **Cross-target follow-up:** the three new specs are gated per case to the targets that have goldens, and each names its own regeneration command so the follow-up is discoverable from the file. **`x64-linux` shares the x86 encoding and belongs in `x64-stack-arg-disp32` ON THE MERITS** — gated out only for the missing golden, and the spec says so. ⚠ **wasm was deliberately KEPT on all three**: `specs-shv2/fragments/` has no wasm directory at all (fragments are a native-target mechanism), so nothing there "lacks a golden", and all 16 cases pass on wasm today. The gating was **proven to parse, not assumed** — excluding one case gave 1537→1536, reverted →1537, because a malformed directive silently falls back to "all targets" and looks identical.
  - 📌 **Coordinator errors worth recording:** the port list **omitted `specs/x64-stack-arg-disp32.md`** — *the* stack-argument spec, and the one that caught the 400-instead-of-200 miscompile; the agent found it by surveying past the list it was given. And the gating ruling was **internally inconsistent** (it demanded wasm ≥1537 while implying wasm be stripped from two specs); the reviewer read it against its own gate and resolved it correctly.

- **🔴 BOOTSTRAP ORACLE BUG — a BOGUS `otherwise` range message** (coordinator-measured 2026-07-28 while evaluating the withdrawn `try … otherwise` rung above). A float-returning `try` with an INT fallback (`try risky(-1.0) otherwise 3`, result type `typealias Real = float(f64.min to f64.max)`) is rejected **E3005: `otherwise value 3 is outside the range of 'Real' (float(-1.7976931348623157E+308 to 1.7976931348623157E+308))`** — and **3 is plainly INSIDE that range**. The oracle is applying a RANGE check where the real objection is a KIND mismatch (an int literal is not a float), so the diagnostic names a condition that is false. The *rejection* matches shv2 (which refuses the same program) and may well be intended; only the stated reason is wrong. ⭐ **shv2's own message is correct** — `otherwise type 'int' does not match expected type 'float'` — so this is a divergence in shv2's favour and shv2 owes no change. Needs the full C# suite as its gate.
- **🔴 BOOTSTRAP ORACLE BUGS — top-level constants** (found while surveying for the rung above; the **"Bootstrap oracle bugs"** list is their home). **(1) An immortal constant is MUTATED IN PLACE, and it leaks.** `let name = "Ada"` ; `var s = name` ; `s.append("!")` ; `print(name)` prints **`Ada!`** and exits **101** (`MM raw leak: 1 allocation(s) remain`). **shv2 gets this right** (`promoteBorrowedToOwned` copies). **(2) Dead-global elimination decides side-effect removal by a NAMING HEURISTIC** — see Slice 3 above; renaming a method changes whether its side effects run, silently, with no warning. **(3) A top-level constant's member access is broken outside interpolation:** `print(X.toString())` where `let X = 5` is `E2010 Expected ')' but got '.'` while `print("{X.toString()}")` works — the top-level-constant branch of `ParsePrimary` (`2-Parser.cs:17129-17131`) is the only one that skips `ParseFieldAccessChain`. **(4)** For a String-backed enum, a top-level `let CT = ContentType.json` then `"{CT.name}"` prints the **raw value**, not the case name; the same access on a *local* correctly prints the case name.

- **🔴 BOOTSTRAP ORACLE BUG — a closure created AFTER a loop reads a mutated `var` from INSIDE the loop body**
  (found 2026-07-27 by the confined-half investigation's measuring scaffold, hit twice; the "Bootstrap oracle
  bugs" list is its home). A closure constructed after a `for` (or `while`) loop, capturing a `var` the loop
  mutated, binds the value from the loop BODY rather than from the loop-exit phi. **shv2's own verifier catches
  it**, which is how it surfaced:
  `error E9001: … op 'maxon.closure_create @_$closure_30' … reads %179270, which is defined in block
  'eachFresh_13' — and 'eachFresh_13' does not dominate 'eachFresh_13.exit'`.
  Reproduced with both a `while`-carried and a `for`-carried var, conditional and unconditional assignment
  alike. **Workaround: a `let` snapshot before the closure.** ⭐ Note the shape of the evidence — the BOOTSTRAP
  miscompiles and **shv2's verifier is the thing that noticed**, which is the oracle relationship running
  backwards and worth keeping. ✅ **MINIMIZED and coordinator-verified 2026-07-27** — 16 lines, `maxon build`:
  ```maxon
  typealias Thunk = function() returns String
  typealias Small = int(0 to 100)
  function emit(f Thunk)
  	print("{f()}\n")
  end 'emit'
  function probe(n Small)
  	var biggest = 0
  	var i = 0
  	while i < n 'loop'
  		biggest = biggest + i
  		i = i + 1
  	end 'loop'
  	emit(function() gives "biggest={biggest}")
  end 'probe'
  function main() returns ExitCode
  	probe(4)
  	return 0
  end 'main'
  ```
  ⇒ `error E9001: … in 'probe', op 'maxon.closure_create @_$closure_0' in block 'loop_0.exit' reads %11, which
  is defined in block 'loop_0' — and 'loop_0' does not dominate 'loop_0.exit'.` The bootstrap's own SSA
  construction fails to thread the loop-exit block arg to the `closure_create`.
- **🔴 BOOTSTRAP ORACLE BUGS — two LOWERING failures, both found by the splitter positional-structure rung
  (2026-07-27, merged `e40405890`); the "Bootstrap oracle bugs" list is their home, each needs the full C# suite as its
  gate.** Both are `E9001` out of `MaxonToStandardConversion`, both were hit while writing ordinary Maxon, and both were
  worked around by reshaping the source rather than chased — the rung is not a bootstrap rung.
  **(1) `try (a / b) otherwise panic(...)` inside a METHOD fails to lower** — `E9001 … Index was out of range`. The
  IDENTICAL expression as a **free function** compiles, which is how `spreadStride` ended up a free function.
  **(2) A `match` arm whose `then` body is a method call carrying a NESTED call, in a function holding a mutated
  `var`** — `E9001: Lowering function 'applyValueToPressure' failed: assign value %177052 (kind=MaxonInteger) not in
  valueMap; assigning to '__arg_pin_10'`. Worked around by hoisting the nested call into locals.
  ⚠ **Neither is MINIMIZED** — both are "the shape that failed", not a reduced repro, so budget minimization time
  before planning against them.
- **✅ GENERIC TYPE-ARGUMENT IDENTITY — CLOSED 2026-07-25 (main `f372bbdef`; x64 1674/0, wasm 1480/0, C# 3103/0).** A generic type-argument mismatch **SEGFAULTED**; it is now a clean **E3005**. A **CYCLIC** alias (`typealias A = Box with A`, and the mutual form) also segfaulted on base and is now **E3091** — *claimed*, not minted, on the existing `SemanticTypeResolutionCycle` entry, and it fires on the **declare-only** cycle too, which base accepted silently. Seven commits, three of them agent-found defects a green suite never reached: a **CUBIC** cycle check (12.5 s on a 3,200-alias program that **compiles clean** — one Tarjan SCC now, 197 ms), a **630× superlinear** namer that consulted its memo only at the outermost ask, and `try … otherwise return` three lines above two `panic`s on the **same** invariant, which silently deleted the whole type-parameter check for that call. ⭐ The rung's own thesis: **ONE canonical comparison door.** Four extractors derived type identity from the compiled name and **three of them did not canonicalize** — the review found three, the implementer found a **fourth** that agreed at depth 1 and diverged at depth 2. Two producers are deliberately left non-canonical **with reasons**: `paramAggregateNamesOf` (gated `tag == named`), and the per-instance-alias join, which is keyed on the alias **spelling on purpose** — `WrapperA`/`WrapperB` share one gid, so canonicalizing there would have collapsed both to one string and **deleted the P1.6-C check green**. Also pinned: the **bare** generic constructor (`Box.create(…)`, `T` unbound) is rejected — it **exits 101 on base**, and the bootstrap corroborates by resolving the field as bare `int` (E4006).

- **⭐ THE SWEEP STORES *DERIVATIVES* OF A TYPE REFERENCE, NOT JUST THE REFERENCE — and FOUR designs each died on a different one (filed 2026-07-25).** ONE type still compiles to TWO names: `typealias N1 = Box with N0` mints `Box_N0` where the inline spelling mints `Box_Box_S0`. **Confirmed in BASE IR — pre-existing, not introduced.** Every attempt to unify at intern time was killed by a stored derivative the design did not know about: **(1)** fix at `resolveArgNode` ⇒ **declaration-order-dependent identity** (measured: swapping two adjacent `typealias` lines flips compile↔reject); **(2)** plain defer-intern ⇒ the swept **tag** feeds the managed-drop cascade (`baseLayoutOf(id).fieldTypes` → `substituteInstanceFieldType`/`genericInstanceFieldIsManaged`), so a field could flip *managed → not-managed* = **a silent missed drop**; **(3)** canonicalize after the sweep ⇒ **gid holders DO exist** in swept layouts (`structTypes[*].fieldTypes`), so re-interning strands them; **(4)** defer + rewrite swept types ⇒ `readFunctionTypeAlias` stores `returnTag` (a bare tag **enum**) and `returnTypeName` (a **`ByteArray`**), which **no `MaxonType` rewrite can reach**, and both are mixed into `ProgramSignatures.hash`. ⇒ **The rung must first ENUMERATE every derivative the sweep stores from `parseTypeReference` — types, tags, names, hash contributions — and design against that complete list**, and must move `isFunctionAlias` (`Parser.maxon:7107`) at the same time, since it carries the identical shape in the adjacent arm and half-correcting the mechanism is worse than not touching it. ✅ **Deferring is SAFE, and this is why:** the sweep is **structurally blind** to previously-swept declarations' member types — nothing in the window reads `layout.fieldTypes`, `casePayloads` or swept parameter types (`skipParamType` is a paren-balanced `kindAt` walk), and **no layout geometry derives from a field's tag** (`offsetOfField` is `index * FieldSlotBytes`). ⚠ **Correction to an earlier coordinator claim: the file-order byte-difference was FUNCTION EMISSION ORDER, not the alias mechanism** — holding the code file fixed and moving only the `typealias` line between a first- and last-sorting file is **byte-identical**. Verified; do not carry the stronger claim forward.
  - ⏭ **Two under-checks to fold into that rung** (both **missing rejections, not wrong answers** — probed, neither reaches a wild free): `namedTypeArgIdentity` lacks the per-instance-alias arm both value-side extractors have, so a *wrong* per-instance alias as a **type argument** decays rather than being caught (it matches the parser's own decay, and P1.6-C still catches the case it owns); and a **static** generic method whose result is not the instance is unchecked — `LeafBox.absorb(Other.make(7))` compiles, while the form that actually **uses** `T` is correctly rejected (E2015).
  - ⚠ **A latent hazard worth fixing when that code is next open:** `paramAggregateNamesOf`'s doc block enumerates its deliberate skips (`structRef`, scalar, ranged alias) and **does not mention `genericInstance`**. It is sound today only because `TypeResolution.maxon:204` leaves `genericInstance` unerased — if that ever changes, **every generic-instance parameter silently stops being identity-checked**, with nothing linking the two files.

- **⭐⭐ USER RULING 2026-07-25 — ADOPT GO'S STRUCTURE FOR BOTH GT-STACK ENTRIES BELOW ("we should just copy what Go does").** Applies to *this* entry (per-M system stack) and the fault-headroom entry after it. **COPY THE STRUCTURE:** (1) **a per-M system stack**, and specifically **g0 = the M's OWN OS THREAD STACK**, not a synthesized region — Go allocates nothing for it. That is **cheaper than what EITHER Maxon compiler does today** (the bootstrap `VirtualAlloc`s a separate 64 KB per P, `X86CodeEmitter.Runtime.cs:4355-4365`; shv2 commits one global 64 KB): per-M by construction, zero extra allocation, ~1 MB demand-paged rather than a fixed 64 KB, and — the real prize — **NO TIB `StackBase`/`StackLimit` REPOINTING**, because the thread's own bounds are already correct for its own stack. shv2 has already had to de-duplicate that repoint idiom once (`emitRepointTibToSystemStack`, shared between shim and morestack); this DELETES it instead of sharing it. (2) **POSIX: `sigaltstack` + `SA_ONSTACK`** (the bootstrap already does this for its FAULT handler and merely omits it on the debug TRAP handler). (3) **Windows fault headroom reserved INSIDE the stack**, counted in BOTH the allocation and the guard, à la `_StackSystem`. ⚠ **The coordinator's earlier "red zone mapped BELOW the stack base" proposal is WITHDRAWN and Go's in-stack placement is better FOR SHV2 SPECIFICALLY**: shv2's morestack **copies and relocates** stacks, so a below-base red zone would have to be re-established on every relocation — a fresh invariant to maintain, i.e. exactly the class of thing that rots; reserving inside means `morestack` maintains it for free. **⚠ DO NOT COPY THE NUMBERS.** `2048` / `928` / `512*PtrSize` are Go's frame sizes and Go's platform assumptions; **Maxon has now twice copied Go's constants without Go's structure** (this entry and the next), and taking the constants again without measuring would be the same error mirrored. **Copy the structure, MEASURE the sizes against Maxon's own frames.** The rationale is not authority-by-Google: Go's structure makes the invariant *unrepresentable* rather than *documented*, which is the precise property both entries below are missing.
- **🔴 B1b (MULTI-M) PREREQUISITE, TODAY UNRECORDED: THE SYSTEM STACK IS ONE GLOBAL REGION, NOT PER-M — AND ITS SAFETY INVARIANT IS PROSE ONLY.** `GtSystemStackSize = 0x10000` is allocated **once** in `__gt_init` with its top published to a single `.data` global `__gt_system_stack_top` (`GtRuntime.maxon:203-208,787-798`), and the syscall shim sets `rsp` to that **fixed** top on **every** entry (`X64GtRuntime.maxon:288,321`). The invariant that makes this safe is written down in exactly one place, as a comment (`X64GtRuntime.maxon:214-216`): *"⚠ NO NESTING … Safe only because a kernel call is a LEAF that never re-enters guarded Maxon code, **and single-M runs one thing at a time** — a shimmed call must never reach another shimmed call on the same thread."* **Leg 2 of that invariant is falsified by B1b BY DEFINITION.** Two Ms inside shimmed calls concurrently both load the same top and write the same 64 KB region ⇒ **silent interleaved corruption, not a clean fault** — the worst failure shape. **Nothing enforces it:** no assertion, no gate, and **none of this file's six B1b mentions names the system stack**, while B1b is repeatedly filed *"off the critical path"* — so the fact and the change that will break it do not reference each other. On the DERIVE > CHECK > COMMENT ladder this sits at the weakest rung, guarding a silent-corruption failure. **⭐ BOTH REFERENCES SOLVE IT STRUCTURALLY, SO THE INVARIANT CANNOT BE VIOLATED RATHER THAN MERELY BEING FORBIDDEN:** Go gives **each M its own g0 system stack** — `systemstack()` switches to the *current* M's g0, and on Windows `asmstdcall` runs Win32 calls there — so no global exists to contend over; and **the C# bootstrap does the same**, where `PSystemStackSize` (`GtLayout.cs:178`) is a **per-P** constant sitting beside `POffSystemStackSP = 0x70` (*"saved OS-thread RSP for syscalls/morestack"*). **shv2 copied the SIZE from that per-P constant and made the REGION global** — the number came across, the structure did not, exactly the shape of the `_StackSystem` omission in the entry below. **⇒ FIX WHEN B1b IS SEQUENCED: a per-P system stack (the bootstrap's shape, Go's shape), which DELETES the invariant instead of documenting it.** ⚠ Re-check **leg 1** at the same time: *"a kernel call is a LEAF that never re-enters guarded Maxon code"* also breaks if any Win32 call re-enters Maxon — an IOCP completion routine, APC, or callback — and subprocess/stdio (workstream C) is precisely where that arises. Not a live defect today (shv2 is single-M and the shim's callers are leaves); filed so the rung that falsifies it finds it.
- **🔴 A CPU FAULT INSIDE AN `async` FUNCTION LOSES ITS PANIC ENTIRELY AND DIES `0xC0000005` (x64-windows). MEASURED by the coordinator 2026-07-25, not inferred.** Identical program, identical fault, only the STACK differs: on the main thread `let zero = 0; 100 / zero` gives `panic: integer divide by zero` + a symbolized trace (`in boom / in main / in mrt_start`), **exit 1** — exactly what `specs-shv2/safety.md`'s `divide-by-zero` test asserts. Wrap the same `boom()` in `let p = async boom(); await p` and stderr is **EMPTY** and the exit is **3221225477 = `0xC0000005` STATUS_ACCESS_VIOLATION**. ⇒ **A SPECIFIED, TESTED behaviour silently breaks when the same code runs on a green thread, and no test in the suite covers that combination.** **MECHANISM:** Windows delivers an exception on the *current thread's* stack — `KiUserExceptionDispatcher` writes `CONTEXT` (1232 B) + `EXCEPTION_RECORD` (152 B) + machine frame, **~1.5 KB below RSP, BEFORE any VEH runs**. A GT stack is `GtStackSize` = **0x800 (2 KB)** with `GtStackGuardMargin` = **0x3A0 (928 B)** of slack (`GtRuntime.maxon:191,200`), so the kernel's write runs off the end into unmapped memory and the process is gone before `X64Runtime`'s fault thunk — the thing that *prints* the panic — can execute. **⚠ THE 64 KB SYSTEM STACK DOES NOT AND CANNOT COVER THIS**, and that is the crux: `__gt_system_stack_top` solves **VOLUNTARY** kernel entry (the syscall shim picks the moment and switches RSP before a Win32 call); a fault is **INVOLUNTARY** — the CPU traps at an arbitrary instruction and there is no hook to switch stacks in first. shv2 has the system stack *and still dies*. **GO HIT THIS AND SOLVES IT TWICE:** on POSIX a per-M `sigaltstack` + `SA_ONSTACK` (the kernel switches stacks, so the small goroutine stack is never used); on Windows, where VEH has no altstack, it RESERVES the space via `_StackSystem` (`512*PtrSize` = **4 KB** on amd64) counted **TWICE** — in the allocation (`_FixedStack0 = _StackMin + _StackSystem`) *and* in the guard (`_StackGuard = 928*mult + _StackSystem`) so morestack fires while the reserve is intact. **shv2 carried over Go's `_StackMin` (2048) and the `928`, but NOT `_StackSystem`** — the one term that exists for exactly this — the same omission the C# bootstrap made (`GtLayout.cs`; see `docs/DEBUGGER_DESIGN.md` **P4d-GT-STACK**, where it was found as *"a breakpoint on a green-thread stack kills the debuggee"*). **CANDIDATE FIX, CHEAPER THAN GO'S — a PROPOSAL for the rung to validate, NOT a measured design:** map a small red zone below each GT stack sized for the kernel's pre-handler write (~1.5 KB ⇒ one page) so that write lands in MAPPED memory, and have the fault thunk switch RSP to `__gt_system_stack_top` as its FIRST action and run the handler body there. Go must fold a full 4 KB into every goroutine stack because it has no pre-existing system stack to hand off to; shv2 does, so it may pay one page per GT instead of inflating every stack — the system stack earning its keep twice. Whatever is chosen, the footprint is a **global per-GT memory tradeoff and must be MEASURED, not asserted**. ⚠ **shv2's exposure is STRICTLY LARGER than the bootstrap's**: the bootstrap forbids unguarded division statically (`E3103` when foldable, else `E3057` demanding `try/otherwise` or a non-zero ranged type), so safe bootstrap code cannot even reach a `#DE`; shv2 has no such check yet and `safety.md` deliberately specifies a *runtime* panic — so ordinary safe shv2 code reaches it today. **Acceptance when sequenced:** the `async` twin of `safety.md`'s divide-by-zero panics with a correct symbolized trace and exit 1; plus the arm64 question answered (POSIX `sigaltstack` is the right shape there, and the bootstrap already does it for its fault handler but NOT its debug trap handler).

- **✅ THE CROSS-REGISTER-FILE-MOVE PANIC IS FIXED AT THE ROOT — WAVE 2 (2026-07-25).** ROOT CAUSE, and it was NOT the missing callee-saved XMMs: `LivenessResult.growValueSpace` recorded a split's fresh reload ids by GROW-FILLING the value-class column with the victim's class, which is a **silent no-op when the column is already that long** — and it routinely is. The allocator's `valueCount` is `scanFunctionValueCount`, which counts values a Target OPERAND names; the lowering's column records a class for every value an op DEFINES, and a fused compare's boolean lives only in EFLAGS — defined, never an operand. `InsertRangeChecks` mints its cascade LAST, so its compare carries the function's HIGHEST id: **that is exactly why the repro needed a ranged typealias**, and why a bare `int` parameter compiled. The f64 reload then kept the column's `gpr` fill, was coloured into a GPR, and surfaced as either `emitRegRegMove` (`mov xmm1, rax`) or `requireClass` (`cvttsd2si` from `rcx`) — **one defect, the two manifestation points the filing noted.** `growValueSpace` now WRITES the class over the minted range. ⚠ **Wave 2 alone would only have made it RARER**, not fixed: it removes the spill for ≤10 cross-call floats, and the higher-pressure probe (12 floats + a ranged typealias) reproduced the panic on the base with Wave 2's trigger removed. Pinned by `register-spill.a-reload-inherits-its-victims-register-file` (12 floats, RED on base at `X64Backend.maxon:1367`) and `register-spill.two-floats-across-a-call-with-a-ranged-typealias` (the original repro, RED at `:703`). Probed clean at 20 and 31 cross-call floats; 20 floats across calls INSIDE a loop is a clean **E5001**, which is the designed answer.
- **✅ THE FLOAT-ACROSS-CALLS QUADRATIC IS FIXED — MERGED `abb5e7807`** (impl `20201e254` · specs `5fd335b60` · optimize `31686bdf9` · review `abb5e7807`; merged-tree x64 **1637/0**, wasm **1446/0**, `-j1`==`-j12` byte-identical, fragments stable, builds 0 warnings, no 101). **TWO quadratics, not one.** ⭐ **The first: the split minted its own next victim.** A reload's live range reaches its run's last use (`useIsInRun` groups by block + side-of-peak), so a call inside that span strands it — allowed = ∅, uncolourable — and the driver must split it *again*. The remat path never had this because it re-emits at every use; the spill path grouped into runs, and **that asymmetry WAS the cascade**. Cure: a run **breaks at an op that leaves the value's file no register at all** (`opStrandsClass`, sharing `opClobberMask` with the liveness sweep so the run-break test and the sweep's forbidding are ONE fact), plus slot residency derived from `loadRegSlot` in the IR. `valuesSplit` becomes **2, constant in N**. **N^1.97 → N^1.19; 128.0× fewer bytes and ~161× less time at N=512; emitted code 33–39% SMALLER; int control byte-identical.** ⚠⚠ **THE COORDINATOR'S PLAN WAS WRONG AND THE IMPLEMENTER PROVED IT BY BUILDING IT** — the brief said "prefer `SplitScope.everyUse` so each reload lives only reload→use"; because reloads are minted per RUN, `everyUse` merely adds a second reload for the before-peak run and leaves the after-peak one spanning every later call. Implemented literally, measured **×3.84 → ×3.84 with +25% more code**, then reverted. *A plan that survives contact only because nobody tested it is worth nothing.* ⭐ **The second quadratic, which the first exposed:** inserting an op was O(block) **three times over** (`positionInBlock`, `insertOpRef`→`Array.insert`, `UseIndex.shiftBlockSeqs`) ⇒ Θ(insertions × block) in TIME — fitted `16.0n + 6.59n²`, **68% of the whole compile at N=1024** — and **allocation-free, so `scale-test` WAS structurally blind to it** (⟲ no longer: the O4 CPU-TIME column, 2026-07-25, sees exactly this shape — Workstream O). Cured by ONE change of shape rather than three patches: `SplitEdits` becomes the whole edit log (`spliceOp` RECORDS; `commit` materializes one backward pass per block via `IrBlock.spliceOpRefs`), so **nothing moves until the split ends and every position stays in ONE coordinate system** — which lets a reload's anchor be read out of the record that asked for it. `positionInBlock` O(block)→**O(1)**; `Array.insert`→one O(block+K) pass; `shiftBlockSeqs` **deleted**. **550 → 139 ms at N=1024, N^1.53 → N^1.03, fitted quadratic coefficient ≈ 0.** ⭐⭐ **THE REVIEW ATTACKED THE COORDINATE-SYSTEM INVARIANT EXHAUSTIVELY RATHER THAN BY ARGUMENT** — it transcribed `spliceOpRefs`/`splicesAtOrBefore`/`resequence` into an independent model and differentially tested every block of 0–6 ops × every ascending multiset of 0–4 splice positions (position 0, the append arm, repeats, adjacency, store-plus-reloads, removals): **791 cases, 0 disagreements**, checking the final array, the position mapping for every pre-batch position including the terminator, and the removal index. It then enumerated **every** `opRefs` mutation in the tree to prove nothing moves mid-split, and ran a **3-oracle differential fuzz** (40 generated programs whose expected output comes from an independent model, against HEAD / a rebuilt base / the bootstrap): **HEAD 22 compile, 22 exact, ZERO wrong answers**, while BASE hit **2 `noVictimAtPeak` panics** — an independent red-anchor of the rung's central claim. ⭐ **It also found the signature bug INSIDE the fix**: `markRunStarts` derived a use's position by SEARCH while `bodyPositionOfUse`, added by the same commit, read it from the record — one fact, two derivations, in one file, silent on disagreement. Single-sourced; and the search was also computing a prefix answer it **discarded** (Θ(block) per split, allocation-free ⇒ invisible to the instrument), worth **−2,840 allocations at rung 5**. It added **two always-on O(1) cross-checks** over the pair nothing derives from each other (`spliceOpRefs` performs the shift, `splicesAtOrBefore` predicts it, in different files) and **saw both RED by sabotage**. ⚠⚠ **THE SAFETY NET IS NARROWER THAN ITS NAME, and this is the durable lesson:** the implementer's first attempt used the LOOSE run-break rule ("clobbers something usable" rather than "leaves NO register") and **nine `streaming-subprocess` tests silently returned WRONG ANSWERS with `VerifyIncrementalSplit` ON** — every derived structure agreed with a full rebuild, because the verifier validates the **invalidation set**, not the emitted **shape**; the goldens missed it too, since the miscompiled function is in the emitted GT runtime whose IR nothing pins. ⇒ **For this pass: THE RUN > THE GOLDENS > THE VERIFIER — but know which claim you are testing**, because for an invalidation-set claim the verifier *is* the right instrument (`verifyUseIndex` compares `useSeqAt` AND `useOpAt` per use per value against a full rebuild). **PINNED BY SPEC** (`specs-shv2/register-spill.md`, red-anchored on a rebuilt base): a **pair** of tests neither of which can state the property alone — the same program with the call sites doubled, both goldens carrying **2 stores / 2 slots / frame 48** while only reloads follow the reads (4 → 8), so *doubling the calls leaves the bracket count alone*; base showed 4/4/4/64 → **8/8/8/96**, the cascade in the specs' own units. Plus `two-floats-multiplied-between-calls`, which **panicked `noVictimAtPeak` on base** and now returns 9 — that panic's leg-4 justification (*"any witness a clobber produces has at least the five callee-saved registers in it"*) is **false for an EMPTY witness**, and now has its own arm. 20 goldens moved, **all strictly better** (−23 stores, reloads unchanged, 9 frames smaller). **MEASURED DEBT (Workstream O), and NOT a regression:** the residual per-split cost is O(block) via the store's def-anchor search, `beforeBranchPosition` and `refreshAfterSplit`'s dirty re-sweep ⇒ O(splits-touching-a-block × block). The review **could** bend it (~N² on M distinct f64 locals spilled out of one block, ×3.6–3.9/doubling) — **but the BASE bends identically** (×3.24–3.81), so the rung neither introduced nor removed it; HEAD carries a **+29% constant-factor allocation** on that shape at the same exponent. **RE-MEASURE TRIGGER:** a single block taking Θ(block) splits. ⟲ **PULLABLE AS OF O4 (2026-07-25):** `scale-test`'s per-phase **CPU-TIME** column reads the O(block) residual directly, where the memory columns show only the +29% constant factor — so the trigger is now a run and a read rather than a bespoke ladder. **The measurement above is unchanged.** ⚠ The +29% is a constant-factor claim and belongs to the ALLOCATION columns, which are exact; the CPU column's few-percent noise band cannot settle it. ⚠ Also surfaced: **`specs-shv2/fragments/x64-linux/` is 288 goldens STALE**, pre-existing and provable — a golden with **zero** spill ops still mismatches — so it is not this rung's churn; and **a reload can be placed inside a physical argument pre-move sequence and take an already-loaded argument register** (pre-existing, latent, the exact class the loose-rule miscompile hit; a multi-float-argument call is the same shape).
- **✅ THE `regalloc:splitting` FLOAT QUADRATIC IS FIXED — WAVE 2 MADE xmm6–15 CALLEE-SAVED (2026-07-25).** The source already named the cause: every XMM was caller-saved, so a float live across a call was forbidden its ENTIRE file, had no register left, and force-spilled a store/reload bracket **at every call**. xmm6–15 are now callee-saved (`calleeSavedMask`; `callDirect`/`iatCall`/`callReg`/`callMem`/`x64Syscall` clobber `0x003F0FC7`, six XMMs not sixteen), preserved in ordinary 8-byte frame SLOTS by `storeSlotReg`/`loadRegSlot` — **converging on the mechanism arm64 already used for d8–d15**, so no new `TargetOp`, no 16-byte encoder and no leaf-frame alignment work. **MEASURED** on the motivating ladder (2 floats read after each of N calls, `regalloc:splitting` bytes): base 404,409 → 788,153 → 2,218,086 at N=128/256/512 (×1.95, ×**2.81** — bending upward) versus head 43,717 → 86,829 → 165,645 (×1.99, ×1.91 — **linear**, and now within 1.43× of the int control instead of 19×). **13.4× less at N=512**, and the gap widens with N; whole-compile memory −24% on the same program. Same exit code before and after. ⚠ **8 bytes per XMM is a LIVE PRECONDITION, stated in `X64PrologueEpilogue`'s header**: Win64 owes 128 bits to EXTERNAL callers and shv2 has none (no DLL export, no callback, only outbound `iatCall`), and nothing reads bits 64–127 — **if shv2 ever gains a >64-bit vector type or exposes a function externally, this widens to 16.** ⚠ **`__gt_context_switch` saves the XMM half by hand** (it is hand-assembled and switches stacks into another green thread), and `assertGtFrameConsistent` is re-derived from BOTH callee-saved lists so it cannot go silent; proven by RUNNING — `async-scheduler.callee-saved-xmms-survive-a-context-switch` returns **5524** (the other thread's floats) with those saves removed. The `floatSpill` scale knob went 4 → 12 in the same commit: four cross-call floats now FIT, so the knob had stopped measuring the path it exists for.
  - **⭐ THE MEASUREMENT ABOVE IS THE IMPLEMENTER'S AND IT DOES NOT REPRODUCE — the optimizer's does, and it is STRONGER.** The rung **recorded no ladder**, so its `404,409 → 2,218,086` table cannot be re-run by anyone, and its base column is **~60× off**: it was measuring a different program. Rebuilt and recorded (generators kept, base binary kept): `regalloc:splitting` bytes at N=128/256/512/1024 go **24,466,558 / 96,360,042 / 382,486,175 / 1,524,199,159** (×3.94/3.97/3.98 — a clean **N^1.99 quadratic**) → **77,359 / 142,119 / 314,175 / 669,695** (**N^1.04**). Splitter **allocations** go 1,275,640 → 80,849,418 against **427 / 429 / 432 / 435 — CONSTANT**: one float across one call now takes a callee-saved XMM and *no split happens at all*. At N=1024, **126.3 s of splitting → 13 ms** and **1.52 GB → 0.67 MB**, emitted code −35%. **The int control is BIT-IDENTICAL base vs head at every N**, which is the evidence that Wave 2 provably did not touch integer allocation. ⇒ **A headline nobody can re-run is not a result. Record the ladder.**
  - **⭐⭐ 75% OF THIS ROW'S OWN SAVING IS NOT THE SPLITTER, AND THE ROW ABOVE MISATTRIBUTES IT.** A/B on an identical corpus: `regalloc:coloring` −10,369…**−294,884** against `regalloc:splitting` −2,518…−93,023. The splitter is **24%**. The rest is that **`callerSavedMask()` STOPPED ALLOCATING** — Wave 1 built it as `classRegisterMask(RegClass.xmm, file: x64RegisterFile())`, and **`x64RegisterFile()` constructs a HEAP `RegisterFile`**, on the colorer's **per-value** path (`preferredVolatilityMask`, up to 3× per `chooseRegister`). Spelling xmm0–5 out register-by-register deleted it. **PROVEN**: a probe build of the *base* compiler changing only that expression to a bit-identical literal reproduces **99.9%** of the saving with `codeBytes` identical at all six rungs. ⚠ **The independent review had filed the register-by-register spelling as a COST** ("thirty operations on a per-value path") and recommended undoing or caching it — **acting on that would have given back most of the rung.** The reviewer counted operations by reading; the optimizer ran it and found the operation count was never the cost. **A measurement beat a reading, and the finding is comment-only as a result.**
  - **⭐⭐ A SECOND SILENT MISCOMPILE WAS FIXED IN THIS RUNG, AND WAVE 2 IS WHAT MADE IT REACHABLE: `__gt_morestack` DESTROYED EVERY FLOAT ARGUMENT.** The stack guard is emitted at offset 0 of the entry block — **before `push rbp` and before the `param` captures** — so an incoming float argument is still in xmm0–5 when the guard `call`s `__gt_morestack`, which calls VirtualAlloc/VirtualFree, both **Win64-volatile in exactly those six**. `morestackSavedGprOrder` saved **thirteen GPRs and zero XMMs**, under a comment claiming it *"PRESERVES EVERY register the guarded function's incoming args occupy"* and then enumerating only the GPR half. **A wrong ANSWER with no crash**, on a green thread only: `scale(20.0, depth: n)` returns 20/60/140/300 synchronously and **20/60/60/60** through `await async` — once `x` reads 0, `0 * 2.0` is 0 for ever. **Wave 2 made it MORE reachable** (`computeFrameBytes` now returns non-zero for a leaf that saves callee-saved XMMs, so `emitGuard` fires on functions that previously had none), which is why it was pulled INTO this rung rather than filed. **The decisive experiment**: ten floats filling the callee-saved half *and* a float parameter — the ten survive at every level, the parameter reads 0 at every level; controls `fat`(float)=0 vs `fatint`(int)=300 differ by **one type**, and the corruption point **moves with frame size**, which nothing but the stack guard depends on. Fixed by saving `argFpRegisters` (the ABI list READ, not respelled) **above** the shadow space — a callee may clobber its caller's 32 bytes, so a float parked inside would be destroyed by the very call it is saved across. Pinned by two RUNNING specs (`a-float-argument-survives-a-stack-grow`, `every-fp-argument-register-survives-a-stack-grow`), both seen **RED against the unmodified compiler** (`expected 0, got 3` — the `d2` mismatch, to the digit).
  - **⚠⚠ AND THE ASSERT THAT PROMISED TO CATCH THE WIDENING COULD NEVER HAVE FIRED.** `assertGtFrameConsistent`'s comment said *"if that widens to 16 bytes, THIS widens with it, and [this assert] is what makes forgetting the second half a compile-time panic."* It derived the XMM half from **`GtArgSlotBytes`** — the *async argument-buffer* stride, a different fact that merely happens to be 8 — and never read the prologue's stride at all. **Proven vacuous by sabotage**: with the prologue widened to 16 and the assert left as written, the same async program **compiled clean, exit 0**. Cured by `XmmSaveSlotBytes`, one constant read by the prologue, `gtXmmSaveAreaBytes` and the assert; `StackWordBytes` split out for pushes and ferry displacements; `GtArgSlotBytes` returned to meaning only the async buffer. **Three meanings had been wearing one name.** Sabotaged again after the fix: it now panics `208 bytes vs 128`.
  - **The review found sixteen items; three were comments POSITIVELY ASSERTING A CHECK THAT DID NOT EXIST** (the assert above; `callMem` carrying the caller-saved literal while absent from `assertX64CallClobbers` whose comment named it — one line, sabotage-proven; and the 8-byte argument's stated invalidating condition *"hands no callback to any Win32 API"*, **already false** — shv2 hands the OS three inbound entry points, each now enumerated with why it is nonetheless safe). ⭐ **The pattern worth keeping: the commit built GENUINE derivation checks for the two pairs it was thinking about and left comments claiming checks for the three it wasn't.** A whole-tree audit of **2554 functions across all 1641 x64-windows goldens** (frame vs top slot byte, 16-byte residue against push parity, spill/XMM slot collision, contiguity, every save restored from its own slot before **every** `ret`) found **zero violations** — and also that only **13 functions in the entire suite** save a callee-saved XMM, so the new mechanism's golden coverage is thin.
  - **The knob's stated rationale was wrong twice**: the knee is at **TEN**, not eleven (`total` is an eleventh cross-call float), and *"twelve exceeds ten by the same slack the GPR knob keeps over its five"* is **FALSE** — `PressuredValuesPerLoop` is 8 over 5, a slack of three. Swept and written down; and the ten is **no longer written down at all** — `floatsLivePerSpillLoop()` derives it from `calleeSavedMask()`, verified RED by flipping the class term to `gpr` (the corpus regenerates at seven).
  - **MERGED gates, re-run by the coordinator on the rebased tree rather than inherited:** build 0 warnings · x64 **1651/0** at `--workers=12` **and** `--workers=1` with **zero golden churn after each** · wasm32-wasi **1457/0** · total working-tree churn 0 · no run exited 101 · `fragments/x64-linux/` and `fragments/arm64-macos/` untouched. Independently re-verified: the original repro is **RED on the base binary** (`X64Backend.maxon:703`, *"rcx to xmm0 crosses register files"*) and **GREEN on the merged one**, printing 349/350 — arithmetically correct, not merely compiling.
- **🔴 THE ENTIRE EMITTED RUNTIME IS INVISIBLE TO EVERY GATE EXCEPT THE RUN — and that is WHY the `__gt_morestack` miscompile survived.** Filed 2026-07-25 by the Wave 2 review. `writeTargetIr(…, includeRuntime: false)` (`Compiler.maxon:93`) excludes the runtime from **both** `--emit-ir` and the `.test` goldens, and the hand-assembled chunks (`__gt_context_switch`, `__gt_morestack`, `__gt_trampoline`, `mrt_*`, the syscall shim) are emitted at the **byte level**, so they appear in no IR at all. ⇒ **`specs-shv2/fragments/` was byte-identical across a silent-miscompile fix**, and the previous rung's nine silently-wrong `streaming-subprocess` tests were the same blind spot from the other side. **This is a structural hole in the gate battery, not a missing test**: the only instrument that sees these chunks is a program that RUNS and checks its answer. The code says a runtime-debugging view arrives with **Workstream R** — the rung owes a check that it covers the hand-assembled chunks, because they are precisely the part no golden can reach. ⚠ Note also that the two new morestack specs **depend on `GtStackSize = 0x800` to reach the guard at all**: raise it and they keep passing while testing nothing, and nothing enforces the dependency (the prose names it).
- **⚠ `preferredVolatilityMask` APPLIES THE **x64** CALLER-SAVED MASK ON EVERY TARGET.** Filed 2026-07-25 by the Wave 2 fixer. `RegisterAllocator.maxon:1240` calls the x64 `callerSavedMask()` unconditionally, so on **arm64** a GPR value's volatility preference becomes the accidental subset `{x0,x1,x2,x6..x11}` of the true `x0..x15`, and an arm64 **vector** value gets `volatilePart == 0` — **no volatility preference at all**, so d8–d15 are taken as readily as d0–d7 and the prologue then saves them for nothing. **Legality is untouched** (a preference never widens `blocked`), so this is codegen **quality**, not a miscompile — which is exactly why nothing caught it. Fixing it **moves arm64 goldens**, which this host can neither run nor validate, so it wants the same host-awareness the scale-corpus residuals want.
- **⚠ TWO PLACES WAVE 2 LEFT ONE FACT WRITTEN TWICE, both filed rather than fixed because each is a refactor rather than a repair.** (a) **The frame's four INPUTS are derived twice** — `insertFrameForFunction` (`X64PrologueEpilogue.maxon:135-142`) and `computeGuardFrameBytes` (`:166-170`) each independently compute `colouredPhysicalRegs`, both `usedRegsFrom` calls and `functionMakesCall`; only the final formula is shared, and Wave 2 took this from two derivations to **four**. `computeGuardFrameBytes`'s own header calls itself *"the SINGLE source of that number"* and `insertFrameForFunction` never calls it. Worse for pass order: `insertPrologueEpilogue` runs, **then** `emitFunctionChunk` recomputes on the **mutated** module — they agree today only by an unstated property (every op the prologue inserts names no register outside the set it was derived from), which the review verified against every arm of `opPhysicalRegs` and **nothing checks**. Give the prologue a scratch GPR for an oversized `sub rsp` and the guard projects a different frame than the function reserves, with no compile error. Wants one `frameLayoutFor(module, func)` returning `(usedCallee, usedXmm, xmmBaseSlot, frameBytes)`. (b) **The callee-saved-in-frame-slots mechanism is now two implementations** — x64's and arm64's first-free-slot index, byte folding, save loop, restore loop and used-callee scan are five parallel pairs with nothing holding them together, despite the commit header claiming the slot-op reuse *"makes the two targets ONE mechanism rather than two."* Wants at minimum a shared `calleeSavedSlotBase(func)` in `Targets/Shared/`; ideally the emit loops too, since both already speak the class-agnostic `storeSlotReg`/`loadRegSlot`.
- **⚠ A PRE-EXISTING `regalloc:splitting` QUADRATIC ON A SINGLE-BASIC-BLOCK FUNCTION — measured 2026-07-25, NOT this rung's and NOT fixed.** 8 ints live across each of N calls in **one** basic block: 124,175 / 489,272 / 1,943,676 / 7,749,362 allocations at N=16/32/64/128 (**×3.94/3.97/3.99**). **Base and head are BIT-IDENTICAL on that ladder at every column and every N**, so it is file-agnostic and predates Wave 2 (the float twin is quadratic on both, head merely 6.4× cheaper). **Cause: the incremental repair's DIRTY UNIT is a BLOCK**, so in a one-block function every split re-commits and re-sweeps the whole function. **Measured LINEAR in practice**, which is why it is reported rather than fixed: the corpus's own shape (N loops each holding floats live across a call) reads **×2.02** out to 256 loops, and the six-rung ladder reads ×2.04 in bytes with allocation ratios converging on 2.00. **RE-MEASURE TRIGGER: a function whose ONE basic block grows with program size while forcing spills at that density** — machine-generated straight-line numeric code, or an **inliner** that flattens loops away (shv2 has none today).
- **✅✅ RETIRED 2026-07-30 (`4572c988b`) — ~~A BOOTSTRAP DEFECT THAT MAKES GROWING `TargetOp` A LANDMINE~~.** ⭐⭐ **IT WAS THE `match`-ARM PAYLOAD-BINDING DEFECT** (see the arm64-macOS refcount-underflow entry in "Bootstrap oracle bugs"), hit through `TargetOp` rather than `ParseError`, and the diagnosis below is now fully explained: **the destructor really WAS correct** (the bug is at the BINDING site, not the destructor), **a synthetic 112-case union really could NOT reproduce it** (case count was never the ingredient — it takes TWO MUTUALLY EXCLUSIVE ARMS BINDING THE SAME **MANAGED** NAME), and it looked like "something about *this* union's shape" because what the uninitialized slot holds depends on FRAME LAYOUT: on x64 it read 0 and the null guard hid it. ⇒ **growing a union is no longer a landmine.** *(Original filing follows.)* **~~every compile dies in teardown once the union passes 111 payload-carrying cases~~.** Found 2026-07-25 by the Wave 2 implementer, whose first design added two `TargetOp` variants. Bisected to a tree containing **only** the two new union cases plus the ~20 match arms the band-append invariant forces, with **zero behaviour change** — nothing constructed them, the masks were Wave 1's, the prologue was untouched. **Every** compile, down to `function main() returns ExitCode return 0 end`, wrote a correct PE and then died in teardown with **`mm_decref: refcount underflow (already zero)` inside `__destruct_TargetOp`**. The generated destructor was dumped at baseline and at that tree and **diffed: it is correct** (the two new cases carry only an int and an enum, get no arm, and the only change is one `check_` block's `jne` retargeting). 31 generated functions changed, all plausible match-arm and `rawValue`-chain extensions. **NOT a raw case-count limit** — a synthetic 112-case union does not reproduce it; something about *this* union's shape. ⇒ **Anyone adding a `TargetOp` variant will hit it and will have no reason to suspect the bootstrap.** Wave 2 sidesteps it entirely (the shipped design adds no union case, per the coordinator's correction to copy arm64's slot mechanism), so it is filed unblocked and unowned.
- **⚠ A `bool` CLOSURE PARAMETER TRAPS ON WASM — `wasm trap: indirect call type mismatch` (exit 3).** Found 2026-07-25 by the 2b-iii review while probing bool routes; **PRE-EXISTING** (reproduces on the pre-rung compiler) and **not this rung's**. Minimal repro: `let pass = function(x bool) gives x` then `pass(true)` — x64 exit **0**, wasm exit **3**. An `int` closure parameter is fine; a **named** function value with a `bool` param is fine. **ROOT CAUSE, both halves read:** `Targets/Wasm/StdToWasm.maxon:472` (`indirectCallParamValTypes`) declares every non-float indirect-call param **i64**, and `Runtime/FnRefThunk.maxon:200` (`thunkUserParamStdType`) normalizes every non-float param to `StdType.i64` — which is exactly why a **thunked** function value matches. A **LIFTED CLOSURE is not thunked**: it declares its source param types verbatim, so a `bool` param is `StdType.i1` → `wasmValTypeOf` → **i32**. The funcref's real type is `(i32, i64) → i64` while the `call_indirect` immediate says `(i64, i64) → i64`. ⭐ **The stated defect: the param model is TWO-valued (float / not-float) where the truth is THREE-valued (f64 / i32 / i64).** The suite is green over it only because no wasm-enabled closure spec uses a `bool` parameter. **Belongs to the P1.5-A1/A2 function-value workstream**, not to conformance.
- **⚠ FLOAT AS A GENERIC TYPE ARGUMENT PANICS THE BACKEND — `X64Backend.maxon:703`, xmm0 → rcx crosses register files.** Filed 2026-07-25; **PRE-EXISTING and coordinator-reproduced on the base**: `type Box uses T` + `typealias FBox = Box with float` + `FBox.create(2.5)`, **with no `where` clause at all**, panics. A float in an opaque `T` slot has no float-aware marshalling. ⚠ **2b-iii WIDENS THE DOOR, and the filed scope must say so:** admitting `float` to `Comparable`/`Equatable` means `Pair with Decimal where T is Comparable` — which was a clean **E3017** on base — now reaches that same panic, and **the constrained spelling is the one a user is most likely to write**. Neither form miscompiles (both fail the build) and the panic names the real gap, which is why it did not hold the rung. **The cure IS this rung's deliverable**: float-aware marshalling for an opaque `T`, plus a positioned diagnostic for the shapes that remain unsupported — which would land on `Box with float` too. Adjacent to the Std-tier bitcast that `float.hash` needs.
- **⚠ A NUMERIC-LITERAL METHOD RECEIVER MIS-LEXES — `7.hash()` and `1.5.equals(1.5)`.** Filed 2026-07-25; **PRE-EXISTING, and BOTH compilers reject it** (shv2: *"'if' requires a bool condition, got 'float'"*; the bootstrap: `E2010 Expected characterliteral`). `Lexer.maxon:972-1030`'s number scanner treats a `.` after digits as a decimal point unconditionally and then swallows a SECOND dot into the literal. Equally broken for `int` since the direct-dispatch rung, **no canonical corpus case exercises it**, and it is a clean compile error rather than a miscompile — so it is a lexer rung, not a conformance one. Bindings are unaffected (`let a = 1.5; a.equals(1.5)` compiles and runs).
- **⚠ A USER `type String` SHADOWS THE SYNTHESIZED `String.hash`/`String.equals` CONFORMER, AND IT COMPILES — LAST-WINS, NOT A DIAGNOSTIC.** Filed 2026-07-25 by the coordinator; surfaced (and MEASURED) by the direct-primitive-dispatch rung, but **pre-existing on the WITNESS path since Slice C**. `TypeResolution.isCompilerOwnedTypeName` (`:189`) refuses a user declaration that mints a nominal identity under `ExitCode`/`HashValue`/`Ordering` (2b-ii, E2015) — **but NOT under `String`.** So a user `type String` with its own `hash()` legally mints the symbol `String.hash`, which is the exact name `BuiltinConformanceRuntime` installs its synthesized djb2 body under (`qualifiedMethodName(StringConformerName, …)`). A program with a user `type String` AND a `Box with String where T is Hashable` therefore puts **two functions named `String.hash` in `module.functions`**, and `indexFunctionsByName` **upserts last-wins** — no duplicate check, no diagnostic. ⭐ **MEASURED, not inferred:** in the dispatch rung's intermediate state a user `type String` whose `hash()` returns 7 returned **5381 — djb2's SEED constant** — because the synthesized body had been installed over the user's function. That rung cured its own *install* side (candidates narrowed to symbols the module does not already define), so the collision is currently masked rather than fixed. ⭐⭐ **AND IT IS A REACHABLE `0xC0000005`, NOT MERELY A WRONG ANSWER — measured by the dispatch rung's independent review, and ATTRIBUTED to the base by building `874e2f7bd` itself**: a user `type String` **plus** `Box with String where T is Hashable` FAULTS at runtime, **with or without a user `hash()` body**, identically on base. Root cause sits UPSTREAM of the install: `isIntrinsicBuiltinConformance` keys on the NAME `"String"`, so a user struct of that name is believed to conform intrinsically and is then handled as the builtin managed String RECORD — a struct pointer read through a fused-record layout it does not have. ⭐ **The proof that the synthesized body was silently replacing the user's**: at base both variants emit **2771 bytes, byte-identical whether or not the user wrote a `hash()`**; after the dispatch rung they differ (2695 vs 2771), i.e. the user's body is now the one that survives. ⇒ The `undefinedImplNames` narrowing is sound and this rung IMPROVES the case; **the name collision underneath remains, and it is a live segfault on `main` today.** ⚠ **`int` is structurally immune and that is the whole asymmetry**: `int` is a KEYWORD, so no user symbol can ever wear the `int.` qualifier — which is exactly why the dispatch rung scoped its widened `isCompilerInternalCallee` to `int.` only and deliberately left `String.*` out. **What the rung owes — this is a DESIGN CHOICE, not a bug fix, and the three options diverge:** (a) add `String` to `isCompilerOwnedTypeName`, refusing a user `type String` — symmetric with `Ordering`, but a sharper divergence from the bootstrap, which permits it because its stdlib is NAMESPACED (`stdlib.String.hash`) and shv2 has no namespacing; (b) give the synthesized conformer a distinguishable symbol — but it must stay nameable by a `funcAbs64` witness reloc, which is why it is not `__`-prefixed today; (c) keep both symbols legal and make the collision a DIAGNOSTIC rather than a silent last-wins. ⇒ Whichever is chosen, **the silent last-wins must not survive**: it is a wrong answer that no committed test reaches. **RELATED ASYMMETRY, accepted for now and recorded so a reader does not trip over it:** an `ExitCode`-tagged receiver does not dispatch — `ExitCode` is `int(0 to u32.max)` but carries its OWN value tag, and `canonicalizeRangedAlias`/`rangedAliasTag` reduce only `named` types, so `let a as ExitCode = 3` then `a.compare(b)` falls to the pre-existing clean E2015 while `Integer`/`Byte`/bare `int` all dispatch. (`10 as ExitCode` is unaffected — an int→int cast keeps the value's `integer` tag.)
- **⚠ HARNESS ROBUSTNESS — A SPEC-TEST CHILD OUTLIVED ITS SUITE BY ~1h45m, AND TWO KNOWN FLAKES POINT AT THE SAME MECHANISM.** Filed 2026-07-25 (user: *"file it"*). ⚠⚠ **UPGRADED THE SAME DAY BY TWO MORE INCIDENTS — IT IS NOT ONE STRAY BINARY, IT IS A WHOLE RUN'S CHILD SET, AND IT PRODUCES A MISATTRIBUTABLE RED GATE.** (2) During the primitive-dispatch rung, **FOUR leaked `maxon-shv2` workers held `maxon-shv2.exe` open and made the next BUILD FAIL** — `error E9001: The process cannot access the file … because it is being used by another process` — **immediately after a rebase over ten upstream commits that touched the same two compiler files.** That is the dangerous part: the exit-1 looks exactly like a semantic rebase conflict, and the coordinator was one inference away from hunting a conflict that did not exist. Killing the four PIDs made the identical build succeed, exit 0, zero warnings. (3) Removing that worktree then failed on a leaked **`wasm-tools.exe`** from the same suite run. ⭐ **All five leaked processes carried the SAME start timestamp (01:06:40)** — four spec workers *and* the wasm toolchain helper — so a single green suite run leaked its entire child set, not a stray. ⇒ **A leaked child is not untidiness: it FAILS THE NEXT BUILD, MISATTRIBUTES the failure, and BLOCKS teardown for every concurrent rung.** The leak is also **intermittent** (a later identical battery leaked zero), which is the same profile as the sidecar flake below and further supports their sharing one mechanism. **THE HARNESS, NOT THE COMPILER** — so its gate is the suite plus a deliberate leak/handle check, not `/specs` coverage. **HARD EVIDENCE, measured:** PID **15964 `enum-array-push-count`**, started **2026-07-24 20:36:41**, was **still running at ~22:20** — holding an open handle on `specs-shv2/.spec-tmp/array-enum-element-size/enum-array-push-count.exe` (8,022 B). It **blocked `git worktree remove`** (`error: failed to delete … Invalid argument`) and `Remove-Item` (`Access to the path … is denied`); killing that one PID made the directory delete instantly. ⚠⚠ **The suite that spawned it reported 1581/0 — GREEN.** So either the runner collected the result and LEAKED the child, or the program did its work and hung on exit; **a test executable outliving its harness by hours is a defect either way, and the suite is structurally unable to notice.** **THREE SYMPTOMS, ONE PLAUSIBLE MECHANISM — the rung should test that hypothesis first rather than fixing them separately:** (1) this leaked child holding a handle **under `.spec-tmp`**; (2) the **fragment-SIDECAR flake** already noted in the P1.7a row — *"compile produced no sidecar"* on a **RANDOM unrelated** test, firing **even under `-j1`** (so not parallelism), where the "actual" is a **MISSING `.ir`** rather than different codegen, and every hit passes in isolation — which is exactly what a live handle or a not-yet-flushed file under `.spec-tmp` would produce; (3) **OPEN #48**, the pre-existing `.spec-tmp` race. **⇒ The unifying suspicion is child-process/handle lifetime around `.spec-tmp`, not filesystem timing** — and the P1.7a row's "harness note" and #48 fold into THIS rung rather than staying scattered. **What the rung owes:** a runner that **waits for and reaps every spawned child** (and fails loudly if one is still alive at suite end — a leaked child should be a RED suite, not an invisible orphan); a `.spec-tmp` lifetime rule stated once; and a check that a completed suite leaves **no live descendants and no open handles** under it. ⚠ Note the coordinator could not simply kill the blocker unattended, because a *different* agent's live worktree shares the repo — so a leaked child is not merely untidy, it **blocks worktree teardown for every concurrent rung**. **SEPARATE TOOLING BUG, same session, do not conflate:** the `maxon-dev` MCP `build` **spuriously refused** a valid worktree once with *"repoRoot … is not a Maxon checkout — it has no stdlib/ and maxon-sharp/"* and succeeded on immediate retry. It failed **CLOSED** (the correct direction, and by design), but a flaky refusal reads as a real misconfiguration to the next agent; it lives in `maxon-dev-mcp/mcp/`, whose binary is gitignored and needs an explicit rebuild + server restart.
- **◑ SCALE-CORPUS CATCH-UP — SLICE 1 DONE 2026-07-25, MERGED `29c06e7a9`** (implement `497c03341` · optimize `f4815babf` · review `29c06e7a9`; merged-tree x64 **1586/0**, wasm **1390/0**, `-j1`==`-j12` byte-identical, `specs-shv2/fragments` **EMPTY** — this rung is codegen-neutral by construction *except* for the optimizer's fix, which is codegen-neutral by measurement — `scale-test` exit 0, wall **14.7 s**, code growth x1.85–x1.99 vs the x1.50 floor; **rebased TWICE** over upstream, the second time after `merge --ff-only` correctly REFUSED a diverged base). **RED, measured by the coordinator before the wave: ZERO `match`/`enum`/`String`/`Array with`/`type … uses`/`interface`/`implements`/`print(`/`or`/`xor`/struct-field-WRITE in 109,697 lines of rung-5 source.** Ten knobs now generate them, each paired against the knob a future change must move independently of it (enum TYPES vs match SITES · String op sites vs managed-struct TYPES · trivial vs MANAGED Array groups · generic BASES vs INSTANTIATIONS · interface CONFORMANCES vs concrete DISPATCH SITES) plus a fixed-shape coverage file on `shiftCoverageSource`'s precedent. Counts verified by the coordinator at rung0/rung5: `match` 21/641, enums 6/130, `as String` 9/257, `.append` 16/512, `.push` 44/1408, generic `uses` 5/129, `interface` 8/132, field WRITE 41/1281; fixed coverage flat by design. Corpus rung0 19→30 files, rung5 143→185 files / 109,697→139,381 lines. **⭐⭐ IT PAID FOR ITSELF ON ITS FIRST RUN — the new heap knobs exposed a REAL SUPERLINEARITY the old ladder structurally could not see, and it was FIXED, not filed** (`f4815babf`): `regalloc:splitting` bytes ×192.1 over a ×128 ladder (**N^1.084**), per-rung ratio CLIMBING 1.77→2.61 while allocations stayed linear at ×2.03 — linear allocs + superlinear bytes = one O(N)-sized allocation made O(N) times. **TWO** scratch buffers in `SplitLiveRanges.maxon`, both sized by the FUNCTION and allocated per SPLIT: `materializePeak` took a fresh `wordsPerRow` live-before row on every peak (and `refreshPeak` materializes after EVERY split) ⇒ O(splits×values) bytes/function, now `SplitScratch.peakLiveBefore` grown with `growFilled` and zeroed in place; and `markBlocksReachableFromPeak` allocated its BFS worklist fresh per un-memoized (peak block, def block) pair, pushing a block per IN-EDGE ⇒ O(splits×edges), now `ReachCache.worklist`. Each saving QUADRUPLES per rung (1,568→48,474,016 and 832→104,249,328 bytes) — the signature of a splits×values term on a doubling ladder. **RESULT N^1.084 → N^0.972**, per-rung flat at 2.04, −41.9% of the phase at rung 7, −3.2% of the whole compile, every other phase and regalloc sub-phase bit-identical at all 8 rungs. ⚠ **A stale liveness row is a MISCOMPILE, not a crash**, so the pass's own `VerifyIncrementalSplit` was run (1581/0) and then SABOTAGED to prove it fires (11/15 `register-spill` red, `the peak's live-before row differs from a full sweep's at word 0`; 3 fail even with verify OFF); flag restored to `false`. ⭐⭐ **The independent review found a FALSE MANIFEST CLAIM — this file's signature bug, in the very file whose rule is *"a heading is a claim; an omission is a claim too"***: the new text claimed `Array with <T>` instances are "FIXED at four (one trivial, one String, one struct-element, one nested)" when there are **THREE and no nested array exists at any rung** (the coordinator's own count of 5 had been fooled by two occurrences inside a GENERATED COMMENT); the count is now derived from the generator's constants so the enumeration IS the count. It also caught `structTypesSource` still claiming "THERE IS NO `__destruct_*` CASCADE TO MEASURE YET" while citing a file that lists that lowering as performed — it shipped in P1.4a wave 2 and this rung's own manifest says so, i.e. the file contradicted itself. And it single-sourced a magic `2` written at both of `genericBasesSource`'s loops (`GenericBaseArityShapeCount`) plus two 2-way `if/else` shape selectors that would absorb a raised count in SILENCE (each `else` now panics) — emitted corpus byte-identical, structure only. **The host-panic exclusion is proved BY TEST, not by argument: rung0 compiles for `arm64-macos`, exit 0, Mach-O written** (coordinator-reproduced), and zero `where`/`Set`/opaque-`T` elements exist at any rung. The three `match` dispatch strategies were confirmed distinct in dumped IR (`x64.jumpTable`, a compare chain, a `cmp 200 / jcc less` binary-search pivot), so `EnumCasesPerType = 4` being `Parser.MinIntervalsForDispatch` is load-bearing. **`phase:insertRangeChecks` went INERT → ACTIVE** — the old corpus emitted no cast at all and only FULL-range aliases, which `rangeIsFull` refuses to guard, so the pass did *nothing* for the ladder's entire history; its x2.41 byte reading is a reallocation LUMP (ratio humps then FALLS BACK 2.41→2.26→2.07, which a quadratic cannot do) not a curve. **◑ SANCTIONED RESIDUALS (slice 2+, named up front, so this rung is NOT complete):** witness dispatch (`where T is I`), `Set`, and opaque-`T` managed array elements — all need **host-awareness in the runner** first (a decision: `compileRung` spawns `build` with no `--target`, so on arm64/wasm a witness call is a `panic()` that takes the whole run down as a BROKEN RUN); **closures** (arm64 genuinely unverified — this file contradicts itself: `#67` says `arm64EmitLeaFuncAddr` panics, a later line says the codegen landed but has no runner); **async**; **`for`-in**. **DESIGN CALL FOR THE USER (reviewer-surfaced, stdlib is out of an agent's bounds):** `stdlib/Array.maxon` has no geometric "refill that grows", so every caller wanting *reuse the storage, zero the whole row, extend* hand-rolls it — `zeroRow` is now that helper, locally; a `refillGrown` would be the real single source. **TOOLING BUG (not the compiler):** the `maxon-dev` MCP `build` spuriously refused once with *"repoRoot … is not a Maxon checkout — it has no stdlib/ and maxon-sharp/"* on a worktree that has both, succeeding on immediate retry. It failed CLOSED (the correct direction) but a flaky refusal reads as a real misconfiguration to the next agent. **Original rationale kept below, because it is why this was sequenced ahead of 2b-iii/2b-iv.**
- **⭐⭐ SCALE-CORPUS CATCH-UP — the RATIONALE (USER RULING 2026-07-24, sequenced ahead of P1.7a 2b-iii/2b-iv).** **The corpus measures the compiler roughly as it stood at P1.1a.** `Testing/ScaleCorpus.maxon:1601`'s own manifest declares under NOT GENERATED: *"`break` …; `or` / `xor` / `not`; print, arrays, strings, `for`, `match`; closures, async, generics; `as` casts"*, plus float ARGUMENTS/RETURNS, struct field WRITES and structs across a CALL. So every mechanism from P1.1b onward is **structurally invisible**: `match` (P1.1b), `String` (P1.2), closures + async (P1.5), generics (P1.6), `Array` (P1.7), interfaces/witnesses (P1.7a, under "generics"), `Set` (P1.7b). ⇒ **the recurring `scale allocsΔ0 … the corpus is generics-free / Set-free / conformance-free` lines in this very file are not evidence of anything** — a Δ0 for a corpus that cannot express the feature says only that the instrument is blind to it — and each optimizer instead rebuilds a THROWAWAY ladder to get a real answer (P1.7 3b-vi-b-α built a managed corpus because "scale-test's is managed-free"; P1.7a 2b-ii built a 64→2,048-operator `where T is Comparable` ladder, measured it linear, and discarded it). Nothing accumulates in the trend table, which is the whole deliverable. ⚠ **The gap is DECLARED, not hidden** — the *"an undeclared gap reads as coverage"* rule from the earlier corpus rung is working — so this rung EXTENDS coverage and is the opposite of touching the instrument to flatter a number. **Its plan must settle three things up front:** (1) **knob order** — generics/interfaces first (most compiler surface, most active rungs), then `match`, then `String`/`Array`; each knob carrying the per-knob rationale `ScaleCorpus.maxon` demands, and **every construct under exactly ONE manifest heading with a partly-generated one naming WHICH part**; (2) **trend continuity** — new knobs move every absolute number, so prior `docs/optimization-log.md` rows stop being comparable unless an explicit EPOCH boundary is recorded, or a second corpus profile keeps the scalar ladder's rows intact (the profile option doubles run time — decide, don't drift); (3) **scope honesty** — `scale-test` **COMPILES each rung and never RUNS it**, so these knobs trend the COMPILER's cost of compiling those constructs and **not** the emitted program's runtime, which remains measured by nothing and is its own instrument on its own rung. ⚠ Also remember the corpus's own warning that it **measured MEMORY only**, so a TIME-only superlinearity stayed invisible here by design (2b-ii's optimizer hit exactly that with `requireInterfaceForParse` — see Workstream O's measured debt). ⟲ **SUPERSEDED AS A LIMITATION BY O4 (2026-07-25)** — `scale-test` gained a per-phase **CPU-TIME** column, so a TIME-only superlinearity is now readable. ⚠ **This does NOT dissolve the point above:** the CPU column cures the memory-only blindness, **not** the corpus blindness. A corpus that cannot express `match`/`String`/generics still reports Δ0 in *every* column, CPU included — which is why this rung exists and why the two must not be credited with each other's fix.
- **A COMPILER PANIC ON A VALID PROGRAM — an enum-typed LOCAL captured in a CLOSURE.** `let o = Color.green` then `let f = function(bump Small) gives bump + match o 'c' … end 'c'` ⇒ `panic at LowerMaxonToStd.maxon:49: maxonTypeToStdType: a 'named' type must be resolved to a primitive before lowering (TypeResolution did not run)`, in `MaxonToStdLowering.lowerOp`. **Pre-existing and NOT `Ordering`-specific: coordinator-reproduced on the pre-2b-ii base with a plain user `enum Color`**, and closure-capture code is untouched by every 2b-ii diff — so the capture path never gets the enum's `named` type resolved. An enum closure **parameter** works (runs 3), which localizes it to CAPTURE. Adjacent, same-shaped, also pre-existing and also confirmed user-enum-identical: `Array with <enum>` + `otherwise <Case>` ⇒ E3059, and a global initialized from a qualified enum case is unsupported. Found by 2b-ii's review while probing `Ordering` in unusual positions.
- **✅✅ #40 → CONDITIONAL OWNERSHIP OF A BORROWED MANAGED-AGGREGATE PARAM RE-BOUND TO A `var` — CLOSED 2026-07-23 (main `7057bf8ad` fix + `b9f13dcac` review, on `134600b69`; suite 1366/0, `--workers=1`==`12` byte-identical, no 101). RESOLUTION: user ruled approach (A) RETAIN-PROMOTION. `var q = <borrowed aggregate param>` now emits `__mm_retain(p)` (NEW `MmRuntime` fn returning the ptr — `__mm_incref` returns void, so a returning twin was needed) so `q` co-owns the box from declaration: owned on every path (then/else/break/continue/loop-carried), aliasing preserved (`q.x=100` still shows through, matching the oracle), refcount balances. A BOUNDED refcount exception like COW #105. The parser promotes via a unified `promoteBorrowedToOwned` (review-consolidated declaration + reassignment dispatch), mirroring `promoteMutableString` but incref-not-copy. The `borrowedToOwned` branch that `ownedBindings.push`ed at the wrong depth is thereby dead on this path. 13 oracle-derived tests in `specs-shv2/conditional-ownership-param.md`. scale-test: #40's ISOLATED footprint is +0 allocs / +8 bytes (the one `usesRetain` bool) — proven by a post-merge A/B vs the parent `134600b69` (bit-identical absolute allocs), the printed row delta being the upstream perf commit `ffc521235`. Kept below as the closed technical record.** **What #40 actually was** (established 2026-07-23 by a reachability probe + an implementer stop-and-report, after the coordinator first *wrongly* concluded it unreachable): a borrowed struct/union **PARAMETER** (a P1.4a pass-by-reference value — its owned-heap bit unset) re-bound to a `var` (`var q = p`) is a borrowed managed var that SKIPS String promotion, so a later `q = <owned>` hits `reassignBindingValue`'s `borrowedToOwned` branch (`Parser.maxon:~6153`), which `ownedBindings.push`es `q` at the CURRENT block depth. When the reassignment is in a NESTED block, `q`'s owned value drops at the inner block's exit while `q` (declared outer) lives on → **use-after-free**. CONFIRMED RED under poison-on-free (#108): struct nested → **`0x3F3F3F3F`** (1061109567), union nested → **`0xC0000005`** fault, and the **`break` / `continue` / loop-carried / used-after-loop** variants all UAF too; oracle gives the correct value (9 / 7). **Same-depth reassign is BENIGN** (push depth == decl depth → exit 9). ⚠ **THE NAIVE FIX (drop `q` at its declaration scope) IS WRONG — it ships a NEW else-path double-free** (implementer-proven from emitted IR): after the merge `q` is a phi `{borrowed p (else), owned Point(9) (then)}`; an unconditional drop at function exit frees the borrowed `p` on the else-path, and the CALLER also decrefs that param → double-free. No committed spec exercises the else-path-taken case, so the suite stays green over it. ⚠ **COPY-PROMOTION (String's fix) IS ALSO WRONG** — a struct `var q = p` ALIASES the param box (`q.x = 100` shows through to the caller's `p`; verified both compilers), so copying diverges from the oracle. (`var q = <owned LOCAL>` is a MOVE on shv2 / rejected E3078 on the bootstrap — so this is PARAM-specific.) ⇒ **THE DESIGN RULING THE RUNG MUST CARRY (every correct fix breaks a thesis invariant — decide at sequencing):** **(A) retain-promotion** — `incref` the borrowed aggregate param at `var q = p` so `q` co-owns the box from declaration (owned on every path, aliasing preserved, refcount balances); a BOUNDED refcount exception like COW #105; needs `__mm_retain(ptr) returns ptr` in `MmRuntime.maxon` (today's `__mm_incref` returns void) + parser promotion mirroring String; **expands the "escape is the only place refcounts appear" thesis.** **(B) parser-side drop-flags** — a runtime "owns?" bool through every merge + conditional decref; no refcount but ADDS runtime flags, which the model deliberately avoids ("reconcile at joins, no flags"); larger, higher double-free/leak risk. **(C) reject** — diverges from the oracle (scope cut). Coordinator recommends (A). **Poison-on-free (#108) is the standing detector — the RED anchors + the `mutate(Point)`/`pick(Num)` probes reproduce it today.** Reachable via P1.4a's borrowed aggregate params, so P1.4a-adjacent.
- **⭐ Array slice COW — SLICE A ✅ CLOSED 2026-07-22 (`12705a7d9` + review `66bd13c24`, 1162→1171, 1184/0 after rebase); SLICE B (zero-copy slices) IS THE REMAINING P1.7 RESIDUAL.** (Number TBD — the parallel repo files #NN concurrently, so assign at sequencing to avoid a collision.) *(User ruling 2026-07-22: "COW makes slices nearly free… worked in as early as possible, a residual if it can be implemented immediately".)* **SLICED A/B by the coordinator after the survey, because A fixes live crashes and is a strict prerequisite for B.**
  - **✅ SLICE A — DETACH-ON-WRITE.** `__arr_cow_detach(record)` gives a record a private writable buffer before any in-place write; the only non-owned buffer at this slice is the `b"…"` literal's immortal `.rdata` blob (`capacity<0`). **It fixed FIVE reachable segfaults on valid programs**, none of which any spec covered: `set`/`clear`/`remove`/`pop` on a literal wrote into read-only `.rdata` (`0xC0000005`, empty stderr), and **`resize(-2)` wrote BELOW the buffer** (`-2 >= -2` satisfied grow's check so nothing detached) — on an owned array that silently erased the `__mm_alloc` header and published `count() == -1`, so it is now a clean abort (exit **73**), grounded in `stdlib/Array.maxon:11`'s `resize(newLength ElementCount)` where `ElementCount = int(0 to u64.max)`: the contract was always non-negative, shv2 just was not asserting it. Expected values all taken from the RUNNABLE ORACLE. ⚠ **`/specs` has NO twin** — no corpus case mutates a byte-string literal, and `codegen-internals.md`'s `rdata-cow-*` family covers `let arr = [1,2,3]` constant arrays in `.rdata`, a route shv2 does not have — so the 9 cases are authored (precedent: the shv2-only `push-grows-off-rdata`), oracle-valued.
  - **⭐ THE CHECK LIVES IN THE EMITTED RUNTIME FUNCTION, NOT AT THE CALL SITES — a deliberate divergence from BOTH references, and the slice's thesis.** Both refs emit a COW check per mutation site and **both have holes because that list drifted**: the bootstrap's `clear` and `setLength` mutate a shared buffer with *no check at all* (`MaxonToStandardConversion.ManagedMemory.cs:1654`, `:1588`), and v1's `shift_left`/`shift_right` are saved only by an arithmetic accident. Putting the detach inside `__arr_*` makes "every mutation site is gated" a property of the runtime instead of a checklist. **Gating rule, stated once: a mutator either calls `__arr_grow` (whose FIRST op is an unconditional detach, ahead of its own `capacity >= required` early-out) or calls the detach itself.** ⇒ 5 call sites (`grow`/`set`/`clear`/`remove`/`pop`) gate all 9 mutators; a 6th in `insert` would be an unreachable branch. Verified adversarially via the INTERNED-TWIN test (mutate one `b"hey"`, read the other — rdata intact).
  - **✅ SLICE B — ZERO-COPY SLICES — CLOSED 2026-07-22** (`14a0aaa75` + review `cd77c6805`, 1184→**1199/0**). `.slice`/`.clone` return a VIEW: own 48-byte record, `buffer@0 = base + start·element_size` (**INTERIOR**), `capacity@16 = -1` (view sentinel, distinct from `-2` rdata), `parent@32` = the **BASE pointer `__mm_alloc` returned** (the base, not the parent RECORD — `__mm_decref` needs the header at `base-24`, and an interior pointer would read the parent's DATA). **`__mm_incref` returned**, installed by `installArrayRuntime` since the view is its only caller — the consumer `MmRuntime.maxon:362-380` had been waiting for. `__arr_cow_detach` gained **ARM 2** (an owner whose buffer's refcount is non-zero detaches too) exactly as slice A promised: another arm of the SAME test, **no call site touched**. `__arr_decref`/`__arr_grow` now `__mm_decref` the shared base instead of `__mm_free`. **Nested views FLATTEN** — a view's `parent@32` is always the ORIGINAL base, never another record, so there is no chain and nothing walks one. Managed elements stay E2015-rejected (see the trap below).
    **⭐ MEASURED, by the coordinator, not claimed:** 500 slices of a 200k array **469 ms → 15 ms**, and *doubling the slice size doubles the OLD time while the new stays flat* — O(n)→O(1), asymptotic not constant-factor. Optimizer confirmed depth-independence (2,000,000 slices at nesting depth 1/10/100/1000 → 121/97/107/98 ms; a chain walk would make depth 1000 ~1000× slower) and length-independence (~50 ns/slice, flat across 100×).
    **⭐ THE SABOTAGE THAT MATTERED:** deleting the incref turned **19 tests red** including `slice-outlives-its-parent` (exit **101**) — and exactly the three base-0 cases stayed green (`slice-of-empty-array-then-push`, `slice-of-byte-string-literal`, `clone-of-byte-string-literal`), which is correct: they owe no incref. The gate was seen RED before it was believed.
    ⚠⚠ **A SECOND SABOTAGE DID *NOT* GO RED, AND THAT IS THE MORE IMPORTANT FINDING** — see #108. Moving the buffer release *ahead* of the copy in `__arr_cow_detach` still passed 1197/0, because `__mm_free` reclaims nothing under the bump allocator, so the "freed" bytes read back intact. The spec prose claiming *"a release before the copy would read freed bytes"* was **FALSE AS WRITTEN** and was corrected: the ordering is a rule the function KEEPS, not one a gate PROTECTS. It becomes load-bearing the day the allocator reuses memory.
    ⚠ **`scale-test` MOVED and is attributed**: +78 allocs / +3,566 bytes / +24 `.text` bytes **FLAT at every rung** — proven by A/B (restoring the old threaded `emitRefcountCheckToLastOwner` shape returns every rung to the previous numbers EXACTLY) to be the 5 Std ops `emitAdjustRefcount` adds to `__mm_decref`'s `lower` block, at the one reachable call site per module. Bought to put `owners - 1` and the refcount RMW in ONE function each now that `__arr_cow_detach` reads that convention across a file boundary. Row written to `docs/optimization-log.md`.
  - ⚠⚠ **THE "REFERENCE TO PORT" PREMISE THIS ENTRY USED TO STATE IS MEASURED FALSE (survey 2026-07-22).** It said to port bootstrap `LowerManagedMemSlice`+`EmitCowCheck` and v1 `__managed_mem_slice`/`__managed_mem_cow_check`. **NEITHER reference has ever zero-copied a slice of an OWNED array.** v1's `__managed_mem_slice` (`stdlib/Internals.maxon:3818`) is an EAGER COPY (`parent = ROOT`, `capacity = sliceLen`) — its `capacity == -1` sentinel is *tested everywhere and produced nowhere*. The bootstrap views only IMMUTABLE sources (rdata `-2`, or another view `-1`) and **refuses** an owned one, in its own comment (`Strings.cs:1480`): *"heap-allocated source cannot be zero-copy because struct-level COW can't distinguish slice refs from normal refs."* Its parent-side detacher `maxon_cow_struct_detach` (`RuntimeEmitter.MemoryManager.cs:1402`) tests `refcount != 1`, is emitted into every binary, and is **CALLED FROM NOWHERE** — that dead function IS the "one-directional hole", and `docs/MEMORY_MANAGEMENT.md:474` documents it as if it were live. ⇒ **Slice B is a NEW DESIGN, not a port.**
  ⭐ **THE ONE DESIGN FORK a move-only compiler forces (settled for B by the survey):** a shared view must keep the parent's buffer ALIVE while it references it. **(A) REFCOUNT the shared buffer** — a bounded, deliberate exception to "no runtime refcount", ONLY for COW-shared array buffers. **(B) BORROW-LIVENESS (E3070)** — a compile-time-bounded borrow that provably can't outlive the parent; purer vs the thesis, but forbids slice-outlives-parent. ⇒ **pairs with the E3070 residual + the borrow-on-get thesis — one design thread.** **⇒ TAKE (A), ANCHORED ON THE BUFFER, NOT THE RECORD — and that is precisely why shv2 can do what neither reference could.** Both refs refcount the RECORD, where a count of 2 cannot distinguish *"two owners"* from *"someone views my buffer"* — exactly why the bootstrap gave up. **shv2's array buffer is a SEPARATE `__mm_alloc` allocation whose own refcount slot at `buffer-8` is currently always 0 and read by nothing** (`MmRuntime.maxon:48`, `ArrayRuntime.maxon:744-746`), so a non-zero count there means one thing only: someone is viewing these bytes. shv2 gets that unambiguous signal **because** it is move-only. `__mm_incref` returns here, which is the caller `MmRuntime.maxon:362-380` explicitly anticipates. ⚠ Keep the E2015 rejection of MANAGED slice/clone/append: v1's `cow_check` does **no element retain** and is sound only because its two reachable triggers stamp `element_destroy = 0` — a managed-element view detaching shallowly is a double-free (v1 `Internals.maxon:3843-3849` names the UAF it already paid for).
- **#103 — builtin array methods do not enforce their RANGED parameter types, so every bounds guard is hand-written.**
  `Parser.parseArrayOneArg` types a builtin array method's argument as plain `integer`, so `stdlib/Array.maxon`'s
  `ElementIndex` / `ElementCount = int(0 to u64.max)` are not applied at the call site. That is *why* `get`/`set`/
  `remove`/`insert`/`slice` each carry a runtime `emitIsNegative` guard and why `resize` needed a new one (#102,
  abort 73) — shv2's `StdCmpPred` has no unsigned compare, so a negative slips every signed `>=` test. **A
  compile-time rejection at the call site would SUBSUME all of them** and turn a class of runtime aborts into
  E3005s. Front-end (parser + P1.9's range machinery), orthogonal to the array runtime. *(Surfaced by the COW
  slice-A rung; it is the generalization of the five segfaults that rung fixed one at a time.)*
- **✅ #104 — runtime ABORT exit codes now have ONE home, and the duplicate check IS THE LANGUAGE — CLOSED 2026-07-22** (main `0be8c7465`). The whole space became a single `RuntimeAbort` `enum`, so two cases sharing a raw value is `E3030`/`E3031` **from the bootstrap that compiles shv2** — no hand-written checker to rot, verified RED before relying on it. The 70/70 collision (`GtRuntime.ProcStoreOverflowExitCode` vs `ArrayRuntime.ElementSizeZeroExitCode`) was resolved by moving the array's unreachable `elementSizeZero` 70→74 (GT's 70 is spec-pinned); `101` (leak gate) and `134` (`userPanic`) folded in as reserved cases. `emitVoidAbort`'s `osExit`+dead-terminator fact single-homed as `emitRuntimeAbort`, which **derives the terminator from `func.returnType`** (so a caller cannot pick the wrong one), folding in `GtRuntime`'s two open-codings AND `buildArrCreate`'s value-returning twin (a prior review's "keep it separate" finding overturned on its own terms — it was conditioned on being the file's only value-returning abort, and `__gt_process_run` is a second). ⚠ Residual: `SpecWorkerPool.LeakCheckExitCode = 101` stays spelled locally BY DESIGN — a worker is the shv2 binary compiled by the C# bootstrap, so its 101 is `maxon-sharp`'s leak gate, not this enum's; the review corrected the note that framed it as an unresolved copy.
- **✅ #105 — ONE buffer-ownership encoding for String and Array, made STRUCTURAL — CLOSED 2026-07-22** (main `0be8c7465`). New `Compiler/Runtime/BufferOwnership.maxon`: **`capacity@16 < 0` ⇒ "not mine to write or release"** (the negative records only provenance — `ViewBufferCapacity -1`, `RdataBufferCapacity -2`, `InlineBufferCapacity -3`); **`parent@32` ⇒ "which separate allocation do I owe a `__mm_decref`"** or `NoOwedAllocation` (0). Three exported predicates read by BOTH runtimes; String's `DetachedRootParent`/`InlineParentSentinel` and both rdata sentinels DELETED (a detached String is simply an owner; an inline String a negative-capacity non-owner, so `emitStringInterp` was changed and `InlineParentSentinel` — written by the parser, read by nothing — is gone, 150 interpolation goldens −1 op each). ⭐ **Made STRUCTURAL not commented:** the subsumption `__str_append` relies on (`capacity < requiredLen` subsumes the sign) is enforced by `typealias NotOwnedBufferCapacity = int(i64.min to -1)` — a fourth provenance written `= 0` **does not compile** (verified E3005). ⭐ **This also fixed [[#43]]** — see the P1.2 rung: `__str_append` released the old buffer BEFORE the blit read it (`s.append(s)` read-after-release), proven a real UAF with a poisoning `__mm_free`.
- **✅ #107 — `try` binds the OUTERMOST call of a postfix chain — CLOSED 2026-07-22** (main `83d072d35`, →1308/0). Two bugs in one mechanism: (a) `parseTryCallTarget` stopped at the primary (`make()`), leaving `.slice(…)` unconsumed, so the tail-rewrite converted the wrong call — fixed by running the full postfix loop (`await` stays terminal), matching the bootstrap's `ParsePrimary`+`RewriteTailCallAsTryCall`; (b) E3055's gate asked `calleeIsDeclared`, **false** for compiler-internal callees, so `try arr.count()` COMPILED and desugared to branch on an error flag the callee never writes — fixed to read `isThrowingArrayRuntimeCallee` (the same predicate `SemanticCheck` reads for the mirror E3057). ⭐ **The fix cascaded FOUR more reachable defects the suite was green over, all found by probing and all FIXED, not filed:** (1) **the `pendingTempDrops` NESTING bug** — a flat list with no discipline meant a branching construct's condition/scrutinee/arm temporaries were drained by the wrong block: on too few paths ⇒ **leak** (`if`/`else if`/`while`/`match`-scrutinee, exit 101), on a path that never built the value ⇒ **register-allocator PANIC** (`match`-expr arm / ternary arm / short-circuit RHS — a *compiler crash on a valid program*). Cured generally: each forking region marks its pending height and drops what it built while control is single-threaded. (2) **array-slots UAF** — `try arr.get(i) otherwise <owned box>` merged a borrow (ok) with an owned fallback (error), dropping the fallback before `result.value` read it; fixed by increfing the ok-edge borrow so the phi is uniformly owned. (3) **aliased-binding DOUBLE-FREE** — `try … otherwise <live binding>` aliased the binding on the error edge with no incref, so scope-exit dropped one box from two owners (exit 101, reached the owned-result arm and non-array programs too); fixed by `transferFallbackToPhi` — MOVE an unconsumed temp, INCREF a live binding, the discriminator being `pendingTempsContain`. **⚠ (3) had been deferred by a review on the false premise "rejected by the type checker anyway"; the coordinator tested the deferred shape, found it a masked double-free, and sent it back — the deferral, not just the fix, needed verifying.** `__mm_incref`'s install moved `usesArray`→`usesIncref` (the `try` phi is now a non-array caller). *(Found by the COW slice-B rung.)*
- **✅ #108 — `__mm_free` POISONS the freed payload, ALWAYS ON — CLOSED 2026-07-23** (main `145a42fa8`). Not a flag (user directive: "always be on"): `__mm_free` unconditionally overwrites `[ptr, ptr+size)` with `PoisonByte = 0x3F` the instant a box dies, so the bump allocator's never-reused-slot no longer hides a read-after-free — it now returns 63, or faults on the non-canonical poisoned pointer `0x3F3F3F3F3F3F3F3F`, instead of the stale-but-correct bytes. The size comes from the header (`HeaderSizeOffset`), so the range is exactly the payload. **The whole class this was filed for is now a STANDING GATE**: the committed suite runs under poison on every spec run. **PROVEN TO HAVE TEETH, not merely green** (a gate is not evidence until seen RED): moving `__str_append`'s release ahead of its blit (the #43 bug) made `var s="abc"; s.append(s); s.append(s)` print `abcabc??????` under poison, restored → `abcabcabcabc`; the review independently re-proved RED with a different `__str_decref` sabotage (`0xC0000005` fault). **Suite 1308/0 under poison** — no committed test masks a UAF the suite reaches. GOLDEN-NEUTRAL (`__mm_free` is `__`-prefixed, excluded from fragments; ZERO churn). scale-test row recorded: +352 allocs / +15,442 bytes FLAT per rung — the COMPILER compiling the now-larger `__mm_free` once per module, a per-module constant, not the emitted program's poison cost (which scale-test structurally cannot see). Review single-sourced the byte-fill loop the poison introduced (a near-copy of `ArrayRuntime.emitZeroRange`) into `MmRuntime.emitFillRange(dest, byteCount, fillValue)`, homed in the base layer, with the four array-zeroing sites now sharing it (`emitZeroRange` deleted, net −14 lines). ⚠ **Pairs with the zeroing contract — this is why it landed NOW, before a recycling free list:** once `__mm_free` returns a slot to a list, the reused slot must be re-zeroed on the way out of `__mm_alloc` (poison then zero) or the `[length, capacity)`-reads-zero invariant breaks. ⇒ **The residuals it now detects the day they become suite-reachable: P1.2 #40 (nested borrowed→owned depth-model) and the P1.7 borrow-on-get residual.** *(This began as a throwaway ~20–30-line instrument during the #43/#105/#107 batch — it confirmed #43 was a real read-after-release, found the array-slots UAF #107 fixed, and distinguished a masked double-free from a leak, which is a distinction only poison can draw. Now permanent.)*
- **#26 — function overloading.** `commitFuncSignatures` keys duplicate-function detection by NAME alone
  → E3006, so two same-name arities (`Counter.create` arity 0 + 2) collide; no `overload` resolution exists.
  Blocks `structs.md` `struct-field-default`, and the stdlib's overload sets (§487/§817) lean on it. Function
  resolution, orthogonal to structs.
- **✅ #65 — implicit-self method resolution — CLOSED 2026-07-23** (main `55cafda11`, 1335→1347/0). A bare
  call `foo(args)` inside a method body now resolves to the sibling method `foo` and routes through the SAME
  `parseCallNamed`/`ReceiverArg` path as explicit `self.foo()` (byte-identical IR, zero fragment M). ⭐ **RULE
  (oracle-verified): prepend `self` IFF the resolved sibling TAKES A RECEIVER** — an instance sibling gets
  self, a STATIC sibling gets none; a bare call in a static method has no self to prepend; a method WINS over a
  same-named free function; static→instance falls out as a clean E3036 (arity). `ensureSiblingReceivers` is a
  lazy per-type token walk (the canonical `funcSignatures` isn't assembled at real-parse time, and the
  file-local artifact holds only backward-declared methods — a forward-referenced sibling can't be queried
  otherwise). The implementer caught + fixed a crash IT introduced (a generic `sizeof(T)`-reading sibling via a
  bare call panicked in `forwardCallerLayout` — the descriptor-need scan only saw literal `self.method()`).
  Review found + fixed TWO duplication defects: (1a) the bare receiver-resolution had a silent receiver-less
  fallback where the explicit path refuses self-capture, so a bare sibling call inside a self-capturing closure
  gave E3036 where `self.foo()` gives the P1.5 E2015 — un-forked via a shared `requireNotInSelfCapture`; (1b)
  the "is this `static`?" token test was written a THIRD time — shared `functionDeclIsStatic`. `throw-transfers-
  ownership.md`'s `propagate-throw-through-local-struct` after #65 blocked ONLY on #64 (borrowed-self throw),
  which then ✅ CLOSED 2026-07-23 by retain (`eea72ec74`), so the test is now ENABLED. ⏭ Two shv2-vs-oracle divergences left as pre-existing, non-#65 (both shv2 internally
  consistent, no miscompile): a local `let f = fn` SHADOWS a same-named sibling method (shv2 calls the local;
  oracle gives the method precedence + flags the unused local E3012); a method named after a builtin (`sqrt`)
  wins over the builtin for BOTH bare and explicit `self.` calls.
- **#77 — string-backed enum raw values** (`enum … implements Error { case = "text" }` → E2015). Needs
  String's `.rawValue`/`.name` accessors (P1.2); may unblock other corpus.
- **✅ #33 — negative float-backed enum raw values — CLOSED 2026-07-22** (main `8718c8d7c`, 1313→1315/0).
  `readEnumCaseInto` routed a `= -2.2` to the INT reader (which consumed the `minus` then failed on the float
  token → a misleading E2010). The float raw-value branch now also fires on `at(minus) and peekKind() ==
  floatLiteral`, folding the `-` into the sign bit via `negatedFloatBits` (the same spelling
  `parseNegatedNumericLiteral` uses). Positive path BYTE-IDENTICAL (zero fragment M); oracle-matched on dispatch
  (exit 2), mixed backing (E3032), duplicate (E3031), forward-ref/tolerant-sweep. ⚠ Exposed a bootstrap
  divergence, filed not fixed: `-0.0`/`+0.0` as sibling enum cases — shv2 keeps them bit-DISTINCT (ONE i64
  equality governs both dispatch and the duplicate check), the bootstrap REJECTS the pair as duplicate `'0'`
  (folding `-0.0`→`+0.0` for the duplicate check while dispatching by bits — TWO equalities). shv2 is the
  consistent one; pinned by `float-backed-signed-zero`.
- **✅ #68 — statement-position function-value calls — CLOSED 2026-07-22** (main `191e4175c`, 1106→1109/0).
  `callStmt` now shares `parsePrimary`'s `calleeIsFunctionTypedLocal` diversion (ONE predicate, both
  positions), so `let cb = doIt; cb()` and a discarded `f(21)` route to the indirect path (oracle-matched);
  an undefined name still E3004. The void case earned its keep: it exposed + FIXED two latent void panics
  (`appendCallIndirect`/`stdTypeOf` on a void result; `synthesizeFnRefThunk` on a void target — its comment
  cited "OPEN #68"), and the review caught a void-function-value-in-VALUE-position compiler panic (now a
  clean E2004 at the call, `resultUsed` threaded). **✅ #97 — postfix callee at statement position — CLOSED
  2026-07-22** (main `d717fb5f0`, 1162→1170/0): `h.op()` (function-typed FIELD), `o.inner.op()` (chain), and
  `getFn()()` (call-result) at statement position now route through the SAME `parsePostfix`/indirect-call path
  expression position uses (0 goldens moved — existing method/direct calls byte-identical). ⚠ DELIBERATE
  SUPERSET: shv2's uniform-postfix design ACCEPTS a call-result callee `getFn()()` that the bootstrap oracle
  REJECTS (an oracle parser limitation) — it runs correctly (verified), never miscompiles.
- **#4b — the constant top-level-decl DFS is UNBOUNDED** (native-stack recursion → SIGSEGV at ~700 deep, no
  diagnostic, defeats E2012; inherited — the bootstrap overflows earlier). Fix = an explicit WORKLIST in BOTH
  compilers (NOT the dependency-graph pre-scan — that would be a third evaluator). *(Its other two sub-bullets
  are closed: duplicate top-level decl → E3006, `A=5` on a top-level `let` → E2013.)*
- **#27 — five `StdOpMeta` fields have ZERO readers** (`role`, `isMemory`, `isStore`, `isCmp`,
  `isUnsupportedInInlineBody`; `isPure` is kept as sole home, reader scheduled). They are held by REVIEW alone
  — a wrong one becomes a silent wrong answer the day a hoisting/scheduling pass lands. A survey with a
  decision per field (`isMemory`/`isStore` = scheduler barriers, `isCmp` = compare/branch pairing).
- **#94 — a union payload typed as a GENERIC-INSTANCE alias cannot be MATCH-BOUND** (P1.6-B2 found).
  `union Maybe some(b StrBox)` where `StrBox = Box with String` constructs and drops leak-free, but
  `some(b) then b.value` types `b` as its i64 storage → E2015 "field access on 'b', which is declared
  'int'". A CLEAN compile error, not a soundness hole, and beyond B2's surface (managed type ARGUMENTS, not
  unions carrying instances). Fix = a match-binding RETYPE of a generic-instance-aliased payload to its
  substituted concrete type — the same retype B2 added for field reads, applied at the match-bind site.
  Combines with P1.7 union-of-`Array` patterns.

---

## Workstream S — `/specs` DRIVES development, from now on ⭐

**Every rung is driven by porting the real spec files from [`/specs`](specs/) into
[`/specs-shv2`](specs-shv2/) — not by authoring a bespoke spec for the occasion.** Start as early
as possible; the first slice runs *before* P1.1.

**Why the corpus, and not hand-written specs.** `/specs` is **276 files / ~2,584 `exitcode` cases**
— the accumulated definition of the language, written against real semantics by someone who was not
trying to make shv2 look good. A spec authored fresh for a rung tests what its author *remembered*
to test. A ported one tests what the language *actually promises*, including every edge case a past
bug already paid for. shv2's five allocator stress specs make the point: written as a corpus shv2
had not seen, they turned up **two real bugs** in code that was green.

**The formats are IDENTICAL — porting is `cp`.** shv2's `SpecParser` was modeled on the main suite
and shares its every convention: **275 of the 276** `/specs` files already use the same
`<!-- test: <name> -->` markers under the same `## Tests` heading.

> ⚠ **CORRECTED 2026-07-15 — this paragraph asserted a capability that was never built, which is this
> plan committing the project's own signature bug.** It claimed *"the **four** fences shv2 accepts
> (` ```maxon ` / ` ```exitcode ` / ` ```stdout ` / ` ```maxoncstderr `) cover ~6,980 of 7,875 fences"*
> and dismissed ` ```stderr ` as *"triage, not a porting layer."* **Measured against the source:**
> - **There is NO ` ```stdout ` fence. There never was.** `SpecParser.maxon` defines exactly
>   `SourceFence` / `ExitCodeFence` / `CompilerErrorFence` — **three**. `/specs` has **711** ` ```stdout `
>   blocks. It is moot only because a shv2 program **cannot yet produce stdout** (no `print`, no `String`
>   until **P1.2**) — so it is P1.2's obligation, not a gap today. **Do not let the count be re-asserted
>   from this document.**
> - ` ```stderr ` was **32 blocks, and it was NOT triage** — it is a program's **RUNTIME** stderr, wholly
>   distinct from ` ```maxoncstderr ` (the COMPILER's). It is what `specs/safety.md` pins a panic with, so
>   **P1.0d.3 had to build it** (`bdff8491f`). ✅ **Now shipped**, with `stripFaultRipSuffix` mirroring the
>   reference's `TestRunner.cs:1427`.
>
> ⇒ **Fences shv2 accepts today: FOUR** — ` ```maxon ` · ` ```exitcode ` · ` ```maxoncstderr ` ·
> ` ```stderr `. **Missing: ` ```stdout ` (711 blocks, gated on P1.2)**, ` ```text ` (243, mostly prose),
> ` ```mm-trace ` (1). **There is still no translation step to build.**

### ⚠ The hard part: port at TEST granularity, not FILE granularity

**Most spec files depend on far more of the language than the feature they name.** Measured across
all **3,259** ` ```maxon ` blocks in `/specs`: **36% use a string literal, 32% declare a
type/union, 26% use `try`/`throws`, 24% call `print`.** `specs/arithmetic.md` is about `+` and
`mod`, but a sibling case in the same file will happily `print` an interpolated string.

⇒ **A spec FILE is not a portable unit. A test CASE is.** File-level `status: draft` — which is all
[SpecTestRunner.maxon:137](maxon-shv2/Testing/SpecTestRunner.maxon#L137) supports today — is
therefore *the wrong granularity*: it would strand a file's in-core cases behind its out-of-core
ones.

**Use the marker the project already has: `<!-- disabled-test: <name> -->`.** It is the established
convention, honored by **both** existing runners — v1 skips it at
[SpecTestRunner.maxon:2233](maxon-selfhosted/Testing/SpecTestRunner.maxon#L2233), and the C#
runner's marker regex is `<!--\s*(?:disabled-)?test:\s*\S+\s*-->`
([TestRunner.cs:1760](maxon-sharp/Testing/TestRunner.cs#L1760)) — and the in-tree usage already
carries a **reason on the following comment line**:

```
<!-- disabled-test: http-client.async-trace-interleave -->
<!-- AsyncTrace -->
```

> **So the ONE piece of machinery Workstream S must build is small: teach shv2's `SpecParser` to
> recognize `disabled-test:` and skip it** — parsed as a test boundary, never compiled, never run,
> and **no `.test` golden generated**. Goldens then accrete only as tests are enabled, and the
> fragment diff stays reviewable instead of becoming a ten-thousand-file dump.
>
> **Port convention:** flip `test:` → `disabled-test:` for any case shv2 cannot yet pass, and put
> **the rung that unlocks it** in the reason comment (`<!-- P1.2 String -->`). Enabling a test is
> then a one-word diff, and it is the deliverable of the rung that earns it.

**Keep the copied file otherwise byte-identical to its `/specs` original**, so a future `diff`
against upstream shows real drift rather than porting noise. The marker flip is the *only*
sanctioned edit.

### ⇒ The disabled-test reasons ARE the ranked roadmap — the thing the compass promised, for free

Because every disabled case names the rung that unlocks it, one `grep` **groups the entire
remaining language surface by milestone**:

```
$ grep -A1 -h 'disabled-test:' specs-shv2/*.md | grep -o 'P1\.[0-9]*' | sort | uniq -c | sort -rn
    412 P1.2      ← String + heap unlocks 412 cases
    288 P1.6      ← generics
    201 P1.4      ← errors
    ...
```

That is **exactly** the `TOP UNSUPPORTED` table `selfhost-distance` was going to produce — the one
thing genuinely lost when the compass was cut. Here it costs **nothing**: no parser recovery mode,
no 626 recoverable panics, no 485-line reporter. And it is strictly better, because it ranks by
**cases that must actually pass**, not by syntax-node frequency.

### ⇒ The draft count is the ratchet — and it is what the cut compass was reaching for

`selfhost-distance` was meant to answer *"how much of the language does shv2 have, and what should I
build next?"* It was cut because its true price was a parser recovery mode plus making `panic()`
recoverable at 626 sites — **it bought a number, not a feature.**

**The un-drafted spec count answers the same question for free**, and answers it *better*:
- **Zero new infrastructure.** The runner, the frontmatter key, and the skip are already shipped.
- It measures **BEHAVIOUR** (the spec runs and produces the right answer), where the compass measured
  only **ACCEPTANCE** (the frontend didn't choke). SHD=0 would have meant "shv2 parses its source,"
  not "shv2 compiles it correctly." A green spec means it works.
- The ranked list of *what is still draft* is a roadmap, in the same way the compass's
  `TOP UNSUPPORTED` table was meant to be — but grounded in cases that must actually pass.

**The gate: an ENABLED test may never be re-disabled.** That is the per-unit non-regression ratchet
the compass promised, enforced on behaviour and at no cost. *(This does not reopen the compass — it
retires the last argument for it.)*

### The slices

- **S1 — ❌ THE BULK PORT IS CANCELLED (user directive, 2026-07-13). Port spec files ON DEMAND, by
  the rung that needs them.** The projection here was **≥650 of 3,259 cases portable today**. The
  sweep was run once, as a *measurement*, and the real number was **48 of 2,746** — because most
  `/specs` files open with a top-level `typealias` (1080 cases) and because the core cannot parse a
  parenthesis. **A bulk port would therefore have landed ~2,700 `disabled-test:` markers: 98% of the
  corpus shelved. That is not a roadmap, it is noise** — the ranked-by-rung grep only says anything
  once the scalar core is complete.
  ⇒ The sweep **already did its job**: it named P1.0d. It is discarded, and from here **each rung
  copies in exactly the `/specs` files it needs** (P1.0d takes `parentheses.md`, `bool-type.md`,
  `block-scoping.md`, `empty-block.md`, `discarded-results.md`, `ranged-typealias.md`, …). The
  `disabled-test:` marker still governs *within* a ported file, because a ported file still mixes
  in-core cases with out-of-core ones — that is unchanged, and it is why the marker exists.
  **The lesson stands, and it is the whole point of Workstream S: a corpus shv2 did not author found,
  on its FIRST run, eight gaps and four outright bugs in code that was green.**
- **S2 — every rung, P1.1 onward: a rung is DONE when the cases it unlocks are ENABLED and green.**
  Author a bespoke spec only where no real one exists — i.e. for shv2-specific surface (allocator
  stress, IR goldens, `E5001` pressure), never for a language feature `/specs` already covers.
- **S3 — triage the remainder:** the 19 `status: selfhosted` files (where v1 and the C# bootstrap
  genuinely conflict), the ` ```text `/` ```stderr `/` ```mm-trace ` blocks, and any case whose
  expectation is bootstrap-specific rather than language-level.

### ⚠ This is what makes P1.0a (the parallel pool) pay for itself

At today's measured **~24 ms/test**, the full corpus is **~2,584 tests ≈ 60 s serially** — the
iteration loop stops being instant exactly when the corpus lands. On a 12-worker pool it is a few
seconds. **So the pool should land before or with the bulk port, and the two justify each other:**
`async` is in Phase 1 because it is a hard mechanism that must not be retrofitted (§P1.5), and the
corpus is what makes it *also* the thing keeping the loop fast.

---

## Phase 2 — self-host

**= Phase 1, plus exactly what shv2's own 44,671-line source adds.** Bounded by measurement
against that source:

| # | Mechanism | Forced by |
|---|---|---|
| ~~**P2.1**~~ | ~~interfaces + witness tables~~ | ⬆ **PROMOTED TO PHASE 1 (P1.7a)** — P1.0c measured `Stringable` and `Hashable` dispatch inside the harness's own cone |
| **P2.2** | **conditional conformance** | per-gid witness tables + synthesized thunks — one witness blob per `GenericInstanceId` (`Array<Byte>: Equatable`, …), each method slot pointing at a thunk with the interface-declared signature whose body forwards to the shared generic impl, materializing that instance's implicit layout/witness args (v1: `synthesizeWitnessThunks`). **The last big generics piece.** ⇒ **[GlobalDataTable.maxon:23](maxon-shv2/Compiler/Targets/Shared/GlobalDataTable.maxon#L23)** `Map with (ByteArray, String)` compiles — **its acceptance test, kept deliberately** |
| **P2.3** | **`Map`** *(`Set` ⬆ PROMOTED to P1.7b)* | multi-param generics. **`Map` is genuinely unreached by the harness — P1.0c proved it with the machine code** (its `EnvMap` arms compile to a bare `mm_decref`), so reachability-seeded lowering holds and it stays here. 12 `Map` typealiases in shv2's own source. ⚠ **`Set` does NOT wait for it** — it was reached in Phase 1 by `String.trim()`, which is why *"`Set` rides `Map`'s exact mechanism"* was false as sequencing |
| **P2.4** | **`extension`** | promotes the stdlib's extension blocks DECLARE→EMIT |
| **P2.5** | **closure dogfood** | shv2's `LazyMessage` sites ([Logger.maxon:35](maxon-shv2/Compiler/Logger.maxon#L35)) compile — the acceptance test for P1.5 |
| **P2.6** | **per-function fan-out + parallel frontend** | the one carry-over from the scalar core (M5's original scope, never built). Both seams exist (`PassPipeline.classifyPass`; the parser is already a pure function of its file) — and **the runtime under it now exists, because P1.5 brought R3 forward**. ⭐ **This rung ALSO parallelizes the frontend — the one upstream phase the fan-out otherwise leaves SERIAL.** The `queryProgramSignatures` sweep (lex + declaration extract) is today a serial `for path in sourcePaths` loop under the per-file parse; make it BSP — `parallel[lex + extract into local arrays]` → `serial[fold in SOURCE-PATH order]` → `parallel[parse, reusing cached tokens]`. The extract already builds per-file local arrays and reads no shared state, so only the fold is serial (O(declarations) + the cheap O(constants) `evaluateInitializers` DFS). **Guards:** the fold stays LINEAR (this spot has gone O(files²) twice) and folds in PATH order, not completion order, or byte-identity dies. **See ⭐ Frontend parallelism (Locked decisions).** Batch win only — the incremental re-sweep-all cost is a separate knob (per-file sweep memoization). Gate: **1-core-vs-N-core byte identity** |
| 🚩 | **PHASE 2 GATE — self-host** | **3-stage bootstrap fixpoint: stage-2 == stage-3, byte-identical**, and stage-2 shv2 passes the whole `specs-shv2/` suite |

**Byte-identity is a cliff, not a ramp** — it demands determinism in rdata ordering, hash
iteration order, name mangling, and float formatting. It is **already gated from Stage 1 on the
toy corpus**: `specs-shv2/fragments/` goldens are compared, so a clean `git status` after a spec
run *proves* byte-identical codegen. Keep it that way and the fixpoint is a ramp.

**Budget a core-drift pass in Phase 2.** With the compass cut, nothing enforces "shv2's source
stays in core," and it demonstrably drifted during the scalar core alone (`panic()` 148→626, a
second `Set`). shv2 pointing at its own source names every violation for free. Every *known* one
is now **in core** (closures, conditional conformance, `Set`), so this should be small. **Watch
for a violation that is NOT a mechanical rewrite** — that, not the drift itself, is the signal to
re-open the compass decision.

---

## Workstream R — the runtime shv2 must EMIT ⚠

**~5–7k lines, on the critical path — a workstream, NOT a stage.** shv2-compiled binaries carry
the runtime *shv2's backend hand-assembles*; a shv2-compiled harness cannot **run** without it.
Sequencing it after the ladder would contradict P1.2's own mm-trace gate, so it lands in slices,
each WITH the milestone that first needs it:

> ### ✅ DECIDED 2026-07-15 @ P1.0r — **BUILDER-BUILT `StdModule` THROUGH THE ORDINARY BACKEND.** Hand-assembly is reserved for raw-ABI escapes.
>
> **The box below framed this as TWO choices. There are THREE, and the one v1 actually uses for THIS
> code is the one the box never names:**
>
> | | route | v1's use | size, allocator+refcount |
> |---|---|---|---|
> | **(a)** | **hand-assembled bytes** | the VEH thunk + ~93 x64 fns (GT/IOCP/subprocess) | **~3,900 lines of C#** (bootstrap) |
> | **(b)** | **Std-IR TEXT** → `StdParser` → ordinary backend | `runtime.std`, 6,049 lines — panic/backtrace/glue **only** | — |
> | **(c)** | **plain Maxon source** over a bounded intrinsic surface | ⭐ **`stdlib/Internals.maxon` — where the slab + refcount ACTUALLY live** | **~2,730 lines of Maxon** |
>
> ⭐⭐ **THE DECISIVE FACT: v1 MIGRATED the allocator and `__mm_incref`/`__mm_decref` OUT of (b) INTO (c),
> and left its reasons in the source.** `runtime.std`'s `mm_incref` is now a **3-op delegator** (`call
> __mm_incref`) whose comment reads *"single source of truth… lives in stdlib/Internals.maxon"*.
> - [Internals.maxon:19](stdlib/Internals.maxon#L19) — *"Migrated from runtime.std's mm_incref **so the
>   inliner can see this 2-op body at every call site**."*
> - [Internals.maxon:1-7](stdlib/Internals.maxon#L1) — they live in stdlib because they *"participate in
>   the full optimization pipeline (canonicalize, cse, licm, dce, Stage A const hoist, inliner) — **that's
>   why they live in stdlib rather than runtime.std (which only gets mem2reg)**."*
> - [runtime.std:4102](maxon-selfhosted/Compiler/Runtime/runtime.std#L4102) — *"an UNCONDITIONAL call
>   because **runtime.std has no conditional compilation**"* ⇒ `--rc-sanitize`/`--leak-report` pay a call
>   on the DISABLED path forever.
>
> ⇒ **(b) is a route v1 TRIED for exactly this code and ABANDONED.** Adopting it would re-walk a path whose
> exit is documented. **And (c) is already LOCKED OUT** by §Context's runtime-binding decision (*"exclude
> `Internals.maxon`, emit natively"*). So the choice is **(a) vs (b)** — with (b)'s one fatal flaw, no
> inlining, **applying EQUALLY to (a): hand-assembled bytes can never be inlined either.** The thing that
> killed `runtime.std` for v1 **cannot decide between the two candidates shv2 actually has.**
>
> **⇒ Decide on what DOES differ — and shv2 gets a fourth option v1 never had:**
>
> **(b′) BUILD THE `StdModule` DIRECTLY, in Maxon, with builder calls — no text, no parser.** v1 wrote IR
> *text* because it wanted to edit it as text; that cost it a **992-line `StdParser`**, a file-path walk
> that **panics if `runtime.std` is missing**, and a stdlib-checksum dependency. shv2 needs none of it:
> - **The pipeline is ALREADY REACHABLE, and `mrt_start` PROVES it** — it is hand-built from raw
>   `TargetOp`s and flows through the same `emitFunctionChunk` as every user function
>   ([BackendDispatch.maxon:149-175](maxon-shv2/Compiler/Targets/BackendDispatch.maxon#L149)).
>   `IrFunction.createFromStd` ([IrFunction.maxon:175](maxon-shv2/Compiler/IR/IrFunction.maxon#L175))
>   needs **no `FileParseArtifact`, no source position, no `MaxonType`**. ⇒ **The blocker was never the
>   architecture. It is the VOCABULARY.**
> - It is **type-checked by the compiler** rather than parsed at runtime, and it is **one spelling for
>   x64 and arm64** — where (a) duplicates the subsystem per target *by hand* (v1 did: `Arm64MacosGreenThread.maxon`
>   re-implements 3,853 lines of the same thing in AArch64).
> - **(a) buys NO CAPABILITY here.** Everything genuinely impossible in ordinary IR — the VEH thunk, GT
>   context switch, `__gt_morestack`, the OS worker-thread entry — belongs to the **fault handler (already
>   hand-assembled at P1.0d.3, correctly)** and the **GT scheduler (R3 @ P1.5)**. The allocator + refcount
>   core needs **only** atomics, OS-import calls, and ordinary internal calls.
> - ⚠ **(a) STRUCTURALLY INVITES THIS PROJECT'S SIGNATURE BUG, and it already did — in shv2, at P1.0d.3.**
>   The review (`651b4ea80`) caught the hand-assembled walker having **independently re-spelled the symbol
>   table's layout that the emitter writes** — *"widen the count and the walker still adds 4, striding the
>   table at the wrong pitch and naming the wrong function for every frame."* The fix had to be a **runtime
>   assertion** (`assertSymtabStride`) because **hand-assembly has no compiler-checked contract with the
>   tier that produced the data.** ONE FACT WRITTEN DOWN TWICE, structurally, forever.
>
> ### ⇒ P1.0r's REAL WORK IS THE VOCABULARY — and **"the precondition is satisfied" was FALSE**
> P1.0d.5b shipped the `memory` band, and the box below treats that as the whole gate. **It is not.** Four
> things are missing or wrong, and the first two are **the language's own semantics, not a tax the form
> chose** — `__destruct_*` dispatch reads a destructor pointer *out of the object header* and calls it, in
> **every** form:
> 1. ⛔ **NO REGISTER-INDIRECT CALL.** `TargetOp` has only `callDirect` + `iatCall`; `FF /2` with `mod=11`
>    (`call rax`) **has no encoder path at all**. **This is the hard blocker** — it is how a destructor is
>    reached.
> 2. ⛔ **NO `funcAddr`** — needed to *store* that pointer. Already named missing in-tree
>    ([X64Runtime.maxon:838](maxon-shv2/Compiler/Targets/X64/X64Runtime.maxon#L838)).
> 3. ⛔ **NO OS-import call from Std** — `VirtualAlloc` is not among `PeWriter`'s four kernel32 imports, and
>    `iatCall` is Target-tier only, unreachable from a Std op.
> 4. ⚠ **`StdTypeInfo` RECORDS NO SIGNEDNESS.** It carries `isFloat`/`castCategory`/`storageBytes`, so
>    `condCodeForPred` **cannot ask**, and every `u64` compare silently takes the SIGNED family
>    ([StdToX64Conversion.maxon:167](maxon-shv2/Compiler/Targets/X64/StdToX64Conversion.maxon#L167):
>    *"every Std integer is a signed i64"* — false; `StdType` has `u8`/`u16`/`u32`/`u64`). **Latent** (nothing
>    emits a `u64` cmp yet) and it does **not** bite Win64 pointers (user-mode addresses never set the high
>    bit, so both families agree) — **but it bites P1.2's `capacity = -2` rdata sentinel**, which is unsigned
>    and enormous. The fix is the file's own idiom: **put `isSigned` in the BACKING**, so *"a new case cannot
>    be added without stating"* it — exactly the argument `storageBytes` already makes at
>    [StdDialect.maxon:55-62](maxon-shv2/Compiler/IR/Std/StdDialect.maxon#L55).
>
> ### ⛔ THAT SLICE WAS WRONG, AND IT WAS WRONG THE `mintPhi` WAY — corrected within the hour
>
> ~~SLICE: `P1.0r.1` = the vocabulary (indirect call · `funcAddr` · OS-import call · signedness), each
> red-pinnable on its own; `P1.0r.2` = the allocator + refcount, built on it.~~
>
> **"Each red-pinnable on its own" was ASSERTED, not measured. It is false — measured, all four:**
>
> | item | its only consumer | red-pinnable alone? |
> |---|---|---|
> | `indirectCall` · `funcAddr` | `__destruct_*` dispatch | ❌ the allocator |
> | OS-import call | `VirtualAlloc` | ❌ the allocator |
> | **signedness** | `capacity = -2` | ❌ **P1.2's String — not this rung** |
> | **the allocator itself** | a heap value to allocate | ❌ ⇒ **structs** |
>
> ⭐ **The signedness row was measured and it is the sharpest**: a `u64` above `i64.max` **cannot be
> constructed at all** — `typealias Big = int(0 to u64.max)` + the literal `9223372036854775808` is
> **E2011** in BOTH compilers (*"outside the range of int (-9223372036854775808 to 9223372036854775807)"*),
> because the literal is range-checked against **i64**, never against the declared target range that
> contains it. So no program can reach the signed/unsigned divergence, and a "red spec" for it is
> unwritable. *(That the literal check ignores the target's declared range is arguably its own bug — but
> both compilers agree, so it is the language's de facto rule and it is not this rung's.)*
>
> ⇒ **A vocabulary-only rung is EXACTLY `mintPhi`, one step earlier.** That scaffold got *a consumer and
> not a reason to be right*, and `var f = 0.0` in a loop crashed the emitter. **Four ops with NO consumer
> is the same disease with nothing to catch it at all** — nothing would prove `indirectCall` calls the
> right address until the allocator dispatched a destructor through it, one rung later, on shipped code.
>
> ⇒ **CORRECTED SLICE — by CONSUMER, never by layer. `P1.0r` and `P1.1a` CO-LAND as ONE rung:**
> ### **"the heap and its first value"** — the allocator + refcount + **exactly the ops it consumes** +
> the minimal struct that is **the reason each of them has to be right**.
> **Acceptance is a REAL corpus case, not a bespoke one**: [`specs/structs.md`](specs/structs.md)'s
> `simple-type` — `let p = Point.create(3, y: 4)  return p.x + p.y` ⇒ **7**. It exercises the whole chain
> in one line: parse · layout · `__slab_alloc` · field store · field load · the struct-return ABI · and
> the **decref at scope end** that keeps the leak gate (exit **101**) green. **Then** `struct-field-access`
> (50), `sizeof.struct` (16), and the rest of the ~72 portable struct cases.
> ⚠ **This is what §"the acceptance test is P1.1a, not a bespoke spec" already said.** The layered slice
> contradicted it the same day it was written — *the plan disagreeing with itself, in the file whose own
> through-line is ONE FACT WRITTEN DOWN TWICE.*
> ⚠ **Atomics are NOT in P1.0r.** The refcount is atomic in the bootstrap (`LOCK INC`), but shv2 is
> single-threaded until R3 @ P1.5 brings green threads. **State the decision when P1.0r.2 lands** — do not
> let it be made by inertia, which is precisely what this box exists to prevent.
>
> ---
> *(The original box is kept below: it is the argument that forced the survey, and its instruction —
> "choose then, with the ops in hand, and write the reason down here" — is what the section above is.)*
>
> ### ⭐ R1 @ P1.2 MUST DECIDE THE RUNTIME'S **FORM**. P1.0d.3 DEFERRED IT — DELIBERATELY, NOT BY INERTIA.
>
> **The panic runtime (P1.0d.3) is HAND-ASSEMBLED machine code**, per the budget line above and the C#
> bootstrap's approach. That was a **user decision (2026-07-15), taken with the alternative on the table**,
> and it was scoped to *that rung* — ~6 functions, needing **zero** new Std ops. **It is NOT a decision
> about the other ~5–7k lines, and R1 must make that one on purpose.**
>
> **The alternative, and it is real: v1 does NOT hand-assemble its runtime.**
> [`maxon-selfhosted/Compiler/Runtime/runtime.std`](maxon-selfhosted/Compiler/Runtime/runtime.std) is
> **6,049 lines of Std-level IR text**, parsed by
> [`StdParser.maxon`](maxon-selfhosted/Compiler/Runtime/StdParser.maxon) (992 lines) into `IrFunction`s
> and pushed through the ordinary backend — so **the runtime gets register allocation and
> target-independence for free**, and every later slice is IR text rather than bytes. v1 hand-emits
> **only** the VEH thunk (`X64Backend.maxon:6948`), because the OS calls it with a raw ABI. **That hybrid
> is the shape to weigh.**
>
> **Why it was NOT adopted at P1.0d.3:** shv2's Std tier declares `memory`/`system` bands but **only
> produces `arith` and `call`** — no `globalAddr`, `loadIndirect`, `funcAddr`, `osWrite`, `osExit`. The IR
> route needs those, and they are **P1.2's own work**. ⇒ **At R1 the precondition is satisfied and the
> excuse expires. Choose then, with the ops in hand, and write the reason down here.**
> ⚠ **The named risk is inertia: 6 hand-assembled functions becoming 5,000 by default.** This box exists
> so that cannot happen quietly.

- **R1 @ P1.2** — slab allocator · `__mm_incref`/`__mm_decref` · the `__destruct_*` cascade ·
  `__ManagedMemory` · the **string runtime** (`mrt_alloc_with_dtor`, `memcpy`,
  `mrt_i64_to_string` / `mrt_f64_to_string` / `mrt_bool_to_string` — interpolation is a
  compiler-emitted memcpy chain, so these are the only helpers it needs — and `mrt_write_stdout`)
  · the DebugStream producer (schema-compatible port of `RuntimeEmitter.DebugStream.cs`), which is
  what lets mm-trace gate P1.2 onward. v1's force-seeded bootstrap roots (`__slab_init`,
  `__mm_alloc`, `__managed_mem_create`, …) become a real obligation here.
  **⇒ The port must also carry the `__DebugStream` builtin (Workstream O3)** — the `Log` events
  (`0x60–0x63`) are emitted by *shv2's own source*, so if shv2's backend does not emit the builtin,
  **the compiler's self-trace dies exactly at the self-host boundary**, which is the point at which
  it is most needed.
  **The allocator R1 emits must ALWAYS RETURN ZEROED MEMORY, from commit 1** — it is a property of
  the allocator, not a thing each caller remembers. Non-zeroing alloc cost v1 at least three
  separately root-caused bugs (the `__gt_spawn` `cancel_flag` deadlock, the socket
  `OVERLAPPED.hEvent` IOCP hang, and `mrt_alloc`'s Map/Set hash tables decref'ing garbage), each
  "fixed" by bolting a zeroing loop onto the caller. Retrofitting the guarantee later means
  re-auditing every raw-buffer call site. See **ARCHITECTURE.md → "Allocator: the zeroing
  contract"** for the full design (Go's `needzero` model, the bump cursor, `__slab_alloc_raw` + its
  audit rule, and the memzero size ladder R1's backend must emit).
- **R2 @ P1.8** — UCD table access, for the real `String` methods.
- **R3 @ P1.5 — UN-DEFERRED into Phase 1 (2026-07-13).** The **GT scheduler** (`emitX64Gt*` port;
  allocator mirrored from the C# **sharded** design, **not** v1's single-shared-mcache) **+ async
  subprocess stdio** (v1's pool does `try await drainPromise` on worker stdout, so the pipes must
  be non-blocking — IOCP on Windows). *Previously scheduled at "Beyond," on the claim that "a
  single-threaded shv2 is acceptable through both phases — shv2's own source uses no `async`."*
  **That claim is dead:** the Phase-1 goal is a **parallel** spec harness, so the harness *is* an
  async user, and P2.6's per-function fan-out was always going to be a second one. Deferring R3
  would mean shipping `EscapeAnalysis` single-threaded and retrofitting green-thread capture into
  it later — the exact retrofit this plan exists to prevent. **Doing it at P1.5 pays twice.**
  ⚠ Note the zeroing contract (R1) is *load-bearing here*: two of the three bugs it was written
  for were green-thread bugs (`__gt_spawn`'s `cancel_flag` deadlock; the socket
  `OVERLAPPED.hEvent` IOCP hang).

### R3 residuals — P1.5's async slice is NOT complete until these close

The single-M R3 substrate (yield · netpoll · timer · subprocess-exit) shipped its CORE, but **P1.5's async
slice is not complete** — these reclamation and linearity residuals (the B-series holds them) remain. All
are `__slab`-invisible to the `__mm` leak gate:

- **✅ #87 — GT struct + arg buffer reclaimed — CLOSED 2026-07-23 (B1c, main `b9bc8c98b`).** An intrusive
  LIFO free-list (`__gt_free_list_head`, threaded through the idle `GtOffNext`): the **awaiter**
  (`__gt_await`/`__gt_try_await`) pushes the completed struct after reading `result`/`threw`; `__gt_spawn`
  pops-before-`__slab_alloc` and WHOLE-STRUCT memzeroes the recycled struct (v1's stale-field trap, not the
  bootstrap's weaker field-by-field). The arg buffer is INLINED into the struct (`GtOffArgInline`@0xB0, a
  `gt.argBuf` self-pointer — trampoline byte-identical) so it recycles with the struct; shv2's slab has no
  free path, so inlining is the correct divergence from the references (which keep it separate + `__slab_free`).
  ⚠ Reclaim is at AWAIT, not completion — a review caught that reclaiming at completion aliased an out-of-order
  awaited sibling (silent wrong answer, repro→130 vs 100); `async-scheduler.out-of-order-await` pins it. The
  proposed `__gt_live_count` leak-gate was REMOVED by ruling: with reclaim-at-await it could only fire on a
  run-but-never-awaited GT (#88 family), so it half-gated a deferred leak.
- **✅ #92 — `__gt_process_run` scratch reclaimed — CLOSED 2026-07-23 (B1c, main `b9bc8c98b`).** The three
  fixed-size scratch buffers (STARTUPINFOA/PROCESS_INFORMATION/exit-code) are now allocated ONCE in `__gt_init`
  and reused (like the timer heap), the cmdline copy is a grow-on-demand global — no per-call `__slab_alloc`.
  Safe because scratch is consumed in non-yielding single-M windows; three per-call re-zeros (`procInfo.hProcess`
  spawn-failure sentinel, cmdline NUL, exit-code high-dword) cover reuse staleness.
- **✅ #88 — un-awaited Promise leak — CLOSED 2026-07-23 (B2, main `eb3c834b0`).** A Promise is now an OWNED
  value: on any path to scope exit / a re-arm that does NOT `await` it, the front-end emits `__gt_promise_drop`
  (the ownership DUAL of the E3100 linear-await check), which reclaims AND cancels the green thread — branching on
  status: `completed` → reclaim struct only (stack already freed at completion); `ready` (never-run) →
  predecessor-walk unlink from the run queue + free seed stack; `waiting` (parked) → swap-remove from the timer
  AND process stores (`CloseHandle` to abandon a parked child's wait, NOT kill it) + free the grown stack.
  Deregister-before-free throughout (no store ever holds a freed GT). **Neither reference does in-place parked
  cancel** — both are flag-only + exit-drain; shv2's simpler unsorted-array / parallel-array / polling stores make
  it cheap, and that is the thesis divergence. The `__gt_live_count` leak gate is BACK (spawn++, decrement at
  await- AND drop-reclaim, asserted 0 at program exit → `RuntimeAbort.greenThreadLeak = 75`, distinct from the
  heap gate's 101) — the prior ruling removed it *because #88 was deferred*; fixing #88 flips that predicate.
  Acceptance = shv2-authored `specs-shv2/async-promise-drop.md` (8 cases). Independent review caught TWO
  memory-safety blockers (phi-blind re-arm across a control-flow merge; alias + re-arm UAF) — both fixed before
  merge, with regression specs. ⚠ **DELIBERATE ORACLE DIVERGENCE (USER RULING 2026-07-23):** `/specs/async-await.md`
  documents the canonical model as fire-and-forget drained at PROGRAM exit + an explicit `promise.cancel()` method;
  shv2 diverges to drop-at-SCOPE-exit + implicit-drop cancellation (deterministic ownership vs GC-like drain). NOT
  observable in the portable substrate — the out-of-order sibling still runs (scheduled during the sibling await →
  dropped-`completed` = reclaim, matching the oracle); only the never-SCHEDULED case differs, and async-await.md's
  un-awaited / `.cancel` cases need filesystem (`File.exists`) so are not portable. `.cancel()` as an explicit
  method is a later feature mapping to the same drop.
- **✅ #89 (B2c) — `await` of a loop-carried / block-arg promise (was over-rejected E2015) — CLOSED 2026-07-23,
  by the SAME #88 phi work.** #88 had to propagate the promise mark onto binding phis (`mintPhiForBinding`) and
  trace a phi's incoming `asyncCall`s for await-result resolution (`resolveAsyncCallee`) to fix its own re-arm
  blockers — which is exactly "flow promise identity across block args," so the E2015 over-rejection is gone:
  awaiting a promise carried through a loop-exit phi or an if-continuation phi now works (verified loop + branch
  shapes → correct results). The dual was checked too — a double-`await` across a phi is STILL caught by E3100
  (no under-detection introduced). Pinned by `async-promise-drop.{await-rearmed-var, await-rearmed-loop-var}`.
- **✅ #93 — `runProcess` osExit-aborts (spawn-failure 1 / store-overflow 70) → made THROWING — CLOSED
  2026-07-23 (Design A, main `7f3721b00`).** USER RULING chose the BOUNDED **Design A** over the plan's
  message-carrying Design B (which would have needed the `.rdata` interner threaded into `installGtRuntime` +
  a managed-error async path). Instead `runProcess` throws a SCALAR error (`spawnFailed`/`storeOverflow`
  ordinals) riding the EXISTING B2b dual-register async throws ABI, exactly as a throwing array accessor throws
  `ArrayError` — the two abort sites become `errorReturn`; `try [await] runProcess(cmd) otherwise <h>` catches
  them and the program RECOVERS. Bare `runProcess` → E3057 (single `isThrowingRuntimeCallee` predicate, parser
  accept ↔ SemanticCheck reject, no drift). The two dead `RuntimeAbort` cases were removed. The ~15 spec call
  sites adopted `try`; the 2 abort specs flipped to caught-error specs. ⚠ **Review caught a Priority-1 latent
  wild-free trap:** the seed scalar error was named `SubprocessError`, colliding with the compiled boxed
  `stdlib/Subprocess.maxon union SubprocessError` (different cases/ordinals) — renamed to `RunProcessError` (a
  name naming NO union, so a future `otherwise (e)` decode can't box-bind and wild-free), fixed before merge.
  **The message-carrying model + reconciling `runProcess` onto the real stdlib `SubprocessError` union is the
  deferred full-Subprocess-API / managed-error rung (Design B).** → the streaming subprocess + parallel-harness
  dogfood remains the P1.5 acceptance test.

### ⭐ DOGFOOD STARTED (2026-07-24, user-directed "continue with the dogfood") — sliced ~6 ways

The parallel-harness acceptance test (compile `maxon-shv2/Testing/`'s ~1,922-line spec-runner with shv2, run it
alongside the `maxon.exe`-built oracle, results must agree) is a MULTI-RUNG milestone. Sliced: **1 async
subprocess stdio** (StreamingSubprocess streaming builtins + async pipe I/O — its own two sub-slices, below) ·
**2 Set + Hashable + witness tables** (via `String.trim()` → `CharacterSet`; ⭐ **the parallel repo is already
building this — P1.7a slice 2a "witness-table dispatch" landed upstream `a507e6b24`**, so slice 2 is partly done)
· **3 String methods** (`trim`/`slice`/`split`/`toByteArray`, Array-backed) + the `__Builtins.*` surface · **4
`Stringable`/interface completeness** (FilePath interp) · **5 compile the harness** · **6 the differential-oracle
gate**.

> ### 📌 SLICES 5–6 SEQUENCED AGAINST A RE-MEASUREMENT (2026-07-29)
>
> Slices 5 and 6 were one line each because nothing had yet tried them. The runner has now been
> re-extracted and handed to both compilers (§"THE GATE'S MEASURED DISTANCE"), so they can be
> ordered by what actually blocks them rather than by what they sound like. **Read the two
> corrections first** — §"⚠⚠ THIS MEASUREMENT IS STALE" and step 0's box — because each one changes a
> slice's size, and one of them changes whether slice 6 means anything at all.
>
> | | Work | Why here |
> |---|---|---|
> | **5a** | **`__Builtins.*` recognition + whitelist entries 4…n** — the sequence §"STDLIB WHITELIST" already fixed: DFE ✅ → `__Builtins.*` → the whitelist. The harness needs `FilePath` · `File` · `Directory` · `Subprocess` · `Executable` · `StringArray`; **3 of 48 are listed today** | **Dominant cost, and it is not a ladder rung.** Nothing above it can be reached: the harness is an IO program and shv2 currently cannot name a path |
> | **5b** | **methods on `union`/`enum`** | Blocks 5 of the 13 files by itself, including `TestOutcome` — which `reportResults` matches on, i.e. the gate's entire observable surface |
> | **5c** | **a method call on a struct FIELD** · **`ByteArray`/`Byte`** · **`#if`** · **`int.fromString`** | Individually small, jointly the rest of the measured 13. `#if` is the sharp one: it blocks `detectHostTarget()`, so nothing target-aware runs until it lands |
> | **5d** | **RE-EXTRACT AND RE-MEASURE** | The list above is a **LOWER bound** — one error per file, nothing behind the first reached. Expect a new list, not an empty one. Budget for the loop; do not read 5a–5c completing as "the harness compiles" |
> | **6a** | ✅ **DONE 2026-07-29 (slice G1).** SPLIT `compilerExe` → compiler-under-test + worker exe (step 0's box) | **MUST precede 6b.** Unsplit, the gate passes while testing nothing — it is not a cleanup, it is the precondition for 6b having content. **6b can now be written against `--compiler=<path>`**, and `scripts/worker-exe-split-gate.sh` is what keeps that flag honest |
> | **6b** | the differential oracle · `-j1==-jN` · clean `mm-trace` | The gate as written |
>
> ⚠ **5d is the row most likely to be skipped, and it is the one the measurement argues for.** A
> first-error-per-file gate reports a CONSTANT 13 whether the true gap is 13 or 300 — so "13 errors"
> must never be read as "13 fixes". It is the count of FILES, wearing the shape of a count of defects.

- **✅ SLICE 1a — the IOCP overlapped-read SUBSTRATE — CLOSED 2026-07-24** (main `18feb228d`; 5 commits `150ad27c2`
  foundation + `a8731fa13` core + `692b5b5b4` perf + `0ceebc7fe` UAF-fix + `18feb228d` docs; 1451→1462/0 after
  rebase over upstream P1.7a-2a/debugger-P2b). A temporary `spawnReadLine(cmd)` PROBE builtin (the seed for the
  real API): spawn a Windows child with stdout on an **overlapped named pipe**, issue a **yielding** overlapped
  `ReadFile`, park the GT, and be resumed by a **dedicated IOCP completion OS thread** that re-enqueues the parked
  GT under a `CRITICAL_SECTION` (`__io_lock`) — **shv2's FIRST cross-thread run-queue mutation.** **DESIGN = Option A
  (IOCP + completion thread), a USER RULING** over the coordinator-recommended single-M overlapped-event-wait; it
  pulls a bounded piece of the multi-M concern (a run-queue lock) forward, deliberately. ⭐ Adaptations from the v1/
  bootstrap references (which are multi-M): the OVERLAPPED is EMBEDDED in the GT's reserved `0x60–0x7F` window (shv2's
  bump slab has NO free, so v1's per-read `__slab_alloc`+free of an IoAsyncCtx would leak); a **publish-after-park
  handshake** (park sets `ioParked` only after committing `waiting`; the completion thread spin-waits it before
  `status=ready`) closes the lost-wakeup race the completion thread otherwise causes — the coordinator OVERRULED the
  implementer's plan to drop it ("single-M ⇒ no gate" covers only the stale-SP race, not the second-thread status
  race). Built as **1a-i** (15 IAT slots + 11 StdOps + lowerings, no consumer) then **1a-ii** (the core), landed
  together (vocabulary-with-consumer). Optimizer: the `osReadOverlapped` 6-field case widens the flat StdOp union
  **+8 B/op for every program** (proven arithmetically) — **accepted LINEAR debt** (narrowing breaks the tiering
  thesis); no superlinearity (io path `usesIocp`-gated + corpus-blind). ⭐⭐ **The independent review CAUGHT A
  DETERMINISTIC REACHABLE cross-thread UAF (0xC0000005) a green suite + 1,400 stress cycles MISSED:** dropping an
  in-flight reader promise un-awaited freed the GT while the completion thread still held `ovlPtr = gt+0x60` (the
  `__gt_promise_drop` waiting arm's "no in-flight OVERLAPPED to abort" invariant, made false by this slice). FIXED
  with a 4-state `ioParked` machine + an `emitGtIoCancelArm` that `CancelIoEx`+drains (abandon/DrainDone handshake)
  before freeing; re-reviewed race-free; pinned by `spawn-read-line.drop-in-flight`. 4 bespoke `spawn-read-line.md`
  tests, x64-windows-gated. ⏭ **Measured debt for slice 1b** (Workstream O): `__gt_io_read` `__slab_alloc`s ~4.2 KB/
  call never freed (bump slab; fine for a probe, invisible to `__mm`) — the real API wants freed/stack-local scratch
  (the #92 reuse pattern, adapted for the read's yield). **⇒ NEXT = slice 1b** (the full StreamingSubprocess surface:
  `readStdoutLine`/`readStderrLine` line-buffering + `writeStdinAll`/`closeStdin`/`waitExit`/`release` + the 7 native
  builtins + making shv2 compile the stdlib `StreamingSubprocess` API + specs).
- **✅ SLICE 1b — the StreamingSubprocess handle-table API (NATIVE surface) — CLOSED 2026-07-24** (main `2a382664d`;
  5 commits `8869ca445` feat + `afeaac53e` drop-fix + `37b400970` perf + `33cc4090e` review + `2a382664d` P1b-fix;
  1462→1471/0 on the branch, **1496/0** on the merged tree after rebasing over upstream P1.7a-2b-i + retain-escaping +
  debugger P3). A `__subp_table` of 64 fixed slots (handle = slot index, main-thread-only ⇒ **no lock**: the completion
  thread reaches a parked GT only through its OVERLAPPED, never the table), `__gt_subp_spawn` (3 pipes: sync stdin +
  2 overlapped IOCP stdout/stderr), buffered `__gt_subp_read_line` (stdout/stderr share ONE body-builder), write/
  close/wait/release, and **7 bare-name builtins** (`subpSpawn`/`subpReadLine`/`subpReadErrLine`/`subpWriteLine`/
  `subpCloseStdin`/`subpWait`/`subpRelease`), `usesStreaming`-gated. 1a's yielding read was **factored into ONE copy**
  (`__gt_subp_yield_read`) shared by the probe and the streaming reader. 1a's ~4.2 KB/call slab debt is FIXED (slot
  buffers REUSED across respawn — reuse, not free, is the fix for a no-free bump slab). 9 bespoke
  `streaming-subprocess.md` tests. **⭐ THREE reachable defects were found and FIXED before merge, none deferred:**
  (1) **drop a streaming reader mid-read** broke the handle — 1a's cancel arm `CloseHandle`d the TABLE-owned pipe, so
  a later read got EOF forever (silent wrong answer, control=4 vs drop=0). Fixed by an ownership tag; the optimizer
  then moved it onto **`io_handle@0x80`'s spare LOW BIT** (kernel handles are 4-aligned; the completion thread
  provably never touches `io_handle`), which REVERTED a +8 GT-struct growth and its 17-golden churn. (2) the committed
  **`write-echo` test was RED** and it was the TEST, not a race: it hard-coded an LF-only exit 23 while `sort`'s
  terminator is inherited from the launching environment (CRLF→34) — PROVEN not a race (60/60 in-process, content
  always correctly sorted; 30/30 standalone LF) and rewritten to assert terminator-INVARIANT round-trip + ordering
  facts. (3) **FINDING P1b — release-while-parked + slot reuse** corrupted a DIFFERENT handle (a stale reader resumed
  and wrote EOF into a reused slot; repro 0 vs expected 80). ⚠ **COORDINATOR RULING: FIXED, not accepted as a
  documented constraint** (the review recommended documenting it as out-of-contract misuse of a temporary probe
  surface; the project's "fix it properly, no workarounds" + "never defer a reachable wrong answer" rules govern) —
  a per-slot **GENERATION** (`SubpSlotGeneration@128`, stride 136) bumped when spawn claims a slot, captured
  immediately before the single park point, checked on resume with a `staleExit` that writes NO slot state; the other
  entries (write/wait/close/release) are synchronous and cannot park, so one guard covers the set. Repro 0→80, 8/8.
  Review also fixed a **cross-file fused-String duplication** (`buildStrFromBytes` hand-rolled `buildStrClone` → one
  shared `emitBuildOwnedInlineString`, byte-identical codegen). scale: **+8 bytes/compile FLAT** (the `usesStreaming`
  bool), A/B-proven — the larger raw delta vs the 1a row was BUILD PROVENANCE, not 1b. No runtime superlinearity
  (handle table O(1) indexed; the line read re-scans only NEW bytes; buffers DOUBLE; tail compaction ≤1 chunk).

### 🔴 THE DOGFOOD'S REAL BLOCKER, discovered by slice 1b — the missing STDLIB CONE

**shv2 cannot compile `stdlib/Subprocess.maxon` AT ALL** — it panics immediately (`SignatureIndex.maxon:3073: base
struct 'Map' is not declared`). The harness calls the *stdlib* `StreamingSubprocess` API, so **the harness cannot
compile until a whole stdlib cone lands that shv2 lacks: `Map`, `FilePath`, `__ManagedMemory`, `.cstr()`,
`.addressableBytes()`, `String.init(__ManagedMemory)`, and `__Builtins.*` QUALIFIED-CALL recognition.** ⚠ Correcting
a wrong assumption in the slice-1b brief: shv2's own source uses `__Builtins.floatToBits`/`mmAllocTotal`, but those
are **BOOTSTRAP** intrinsics (maxon-sharp compiles shv2) — **shv2 has NO `__Builtins.*` recognizer for the programs
IT compiles** (a qualified callee can never match the bare-name builtin table, `Parser.maxon:18830`), which is why
1b correctly used bare-name builtins. When the cone lands, the stdlib method bodies wire to the `__gt_subp_*` runtime
with minimal churn (the read builtin already returns the natural owned String; names/shapes mirror the stdlib ops).
**⇒ This cone — not more subprocess runtime — is now the dogfood's dominant remaining cost, and `Map` (a generic hash
map) is its largest piece.** Slice 2 (Set/Hashable/witness) is meanwhile being landed UPSTREAM by the parallel repo
(P1.7a-2a witness dispatch `a507e6b24`, 2b-i integer Hashable+Equatable `f1d6690ca`).

### ⭐ STDLIB WHITELIST (user-directed 2026-07-24: *"use a whitelist for loading stdlib so we don't have to implement all of it at once"*) — RECON DONE, PREMISE CORRECTED, SEQUENCED

The idea is right and it is the plan of record: shv2 should PREPEND a **listed subset** of the real `stdlib/*.maxon`
source (the bootstrap prepends ALL of it — `StdlibLoader.LoadStdlibModules()`/`PrependStdlib`,
`maxon-sharp/BuildCache.cs:30-32`), so stdlib support grows incrementally instead of every type being reimplemented
as a builtin. **shv2 loads NO stdlib today** (`loadSourceFiles`, `Compiler.maxon:328`, registers only the project's
rootPath; `String`/`Array` are compiler BUILTINS — PLAN's "the road not taken"). A recon rung STOPPED before landing
it, correctly, because three measured facts invalidate a naive whitelist:

1. **ZERO of the 48 files under `stdlib/` compile with shv2 today** (all 34 top-level + 14 `helpers/` probed
   individually). There is no candidate set for a non-empty whitelist yet.
2. ⭐ **shv2 has NO DEAD-FUNCTION ELIMINATION — measured.** An uncalled `unusedHelper` is still emitted,
   register-allocated, encoded and linked. So whitelisting a module drags EVERY function in it into EVERY program:
   (a) all 221+ committed fragments gain them (`writeTargetIr` passes `includeRuntime: false`, which excludes only
   `__`-prefixed runtime — stdlib functions are not `__`-prefixed) = a suite-wide `M` with no reviewable content;
   (b) **the whole wasm + arm64 suites break**, because the GT/clock/IOCP substrate is x64-windows-only and nothing
   prunes it (confirmed: a `sleep(1)` program dies on wasm at `StdToWasm.maxon:1500: emitFuncAddr: no wasm function
   index for '__gt_trampoline'`); (c) every compile pays. **⇒ DFE is the PREREQUISITE, and its own rung.**
3. **`__Builtins.*` is the universal floor** — every leaf module bottoms out there (`Print`→`writeStdout`,
   `Sleep`→`sleep`, `Clock`→3 clock intrinsics) and **shv2 has no `__Builtins.*` recognizer for programs it
   compiles** (the ones in shv2's OWN source are BOOTSTRAP intrinsics; a qualified callee can never match the
   bare-name builtin table, `Parser.maxon:18830`).

**⇒ SEQUENCE: (1) dead-function elimination → (2) `__Builtins.*` recognition → (3) the whitelist mechanism with
`stdlib/Clock.maxon` as entry #1.**

- **✅ STEP (1) DEAD-FUNCTION ELIMINATION — CLOSED 2026-07-24** (main `5984d5090`; 4 commits `1bcb8a201` feat +
  `33c6e15a8` perf + `79a37d1b4` review + `5984d5090` final scale row; 1496→**1513/0** on the merged tree, wasm
  **1360/0**). `Compiler/IR/Std/DeadFunctionElimination.maxon` — roots → mark → sweep over the finished Std module,
  run ONCE at the target-neutral backend entry (`buildBackend`, after `assertNoSugarOps`) so x64/arm64/wasm all
  inherit it. **ROOTS:** `main`; the two Std functions a hand-assembled Target CHUNK calls by name (`__mm_leak_check`,
  `__gt_enqueue`); and every `GlobalDataTable.pendingRdataRelocs[*].funcName` (descriptor `destroyFunc`/`copyFunc` +
  witness-table method slots), rooted UNCONDITIONALLY because the pass never prunes `.rdata` — keeping the data and
  pruning only code is what lets the root set be three lines (v1 pruned data too and paid ~400 of its 772 lines for
  it; dead-DATA elimination is a separate rung, `.rdata` offsets move so `RequiredData` moves with them). **EDGES:**
  `call`, `tryCall`, and **`funcAddr` — a static edge even though the consuming call is indirect** (v1 SHIPPED the bug
  of assuming otherwise and pruned a destructor; shv2 has the correct arm from commit one because v1 paid for it).
  `callIndirect`/`witnessCall` name no callee by design. Took v1's KNOWLEDGE, left its CODE: v1 pushed name STRINGS on
  the worklist (3 byte-hashes per edge) — shv2 resolves to a `FuncIndex` once (one hash), refusing to port a cost
  curve into the pass whose job is to make the back end cheaper. **⭐ IT EXPOSED A REAL DUPLICATED-FACT BUG:** three
  Target gates RE-DERIVED "is the GT/IOCP runtime installed?" by probing the module for a representative function
  (`moduleHasFunction(module, GtSpawnName)`) — a proxy valid only while nothing REMOVES an installed function. A
  `sleep(1)` program installs the whole scheduler and never calls `__gt_spawn`; DFE correctly pruned it, the gate went
  false, and 13 async specs failed loud at link. Collapsed to ONE decider (`RuntimeUsage` owns INSTALLATION, DFE owns
  REACHABILITY within it and can only remove); **`moduleHasFunction` and `IrModule.dropFunctionsByName` DELETED**.
  ⭐⭐ **The independent review then found TWO more reachable defects a green suite hid:** (a) **DFE renumbers
  `module.functions`, but `ValueOriginTable`/`ParamOriginTable` key every row by a function's POSITION in that array**
  — after the first pruned function every later one resolved its values against a DIFFERENT function's rows; loud half
  a `RegisterPressureDiagnostic` COMPILER-BUG panic, **silent half worse: E5001 pointing at the WRONG SOURCE LINE**
  (fixed with `remapFunctionIndices` + a `dropped == 0` early-out so the no-prune corpus pays nothing); (b) the gate
  collapse had been applied to x64 and arm64 but **NOT wasm**, whose `appendLeakGate` still asked the module and
  **SILENTLY skipped the leak gate** (measured by deleting the root: x64/arm64 panic at link, wasm exits 0 with a
  4,332-byte module vs 4,421). ⇒ all three backends now emit the gate from `usage.usesHeap`, so **pruning that root is
  a link failure on EVERY target — a STRUCTURAL guard**, which is why no leak-firing spec is needed (note: no
  committed spec makes the leak gate fire, since nothing legitimately leaks). Optimizer: corrected the implementer's
  own attribution (the flat instrumentation term is +2 allocs/+112 B, not +1/+80 — part hid INSIDE the new column),
  proved linearity three ways (allocation first-differences double; the body/`sugarGate` ratio DECREASES monotonically
  1.195→1.024; by construction O(F + ops + relocs + callee-name bytes) with no nested scan and no unbounded re-push),
  and fixed a cost bug (an EAGER `String.from(func.name)` beside a LAZY `logTrace` — a cost rising with how well the
  prune works, on every non-trace build). **BENEFIT, measured on a heap program the corpus cannot express: 15 of 31
  functions dropped, whole compile −41.4% allocations / −40.3% bytes, emitted code −44%; DFE spends 131 allocations to
  save ~17,300 — ~130×.** Corpus caveat recorded: the arithmetic corpus has NO dead code (`dropped 0 of 60`), so the
  instrument shows only cost and structurally cannot show the win. ⏭ **Measured debt, deliberately not taken:** no
  `compactOps`/`compactBlocks` after the sweep (orphans verified inert — every Std consumer goes through
  `liveOpIndices`/`func.blockRefs`); no CHUNK-level pruning (a sleep-only program still carries the `__gt_trampoline`
  chunk as dead bytes) because the chunks are a FIXED set, O(1) in program size, and pruning them needs a Target-tier
  reachability graph = a SECOND decider beside `RuntimeUsage`, the precise defect this rung removed — revisit only if
  chunk count ever becomes a function of program size. ⏭ Also flagged, unmeasured: DFE runs AFTER the mid-tier
  pipeline, so `pruneDeadBlockArgs`/`elimTrivialBlockArgs`/`foldConstOperands` (~9.6% of rung-5 allocs) still run over
  functions DFE will delete; moving it earlier is blocked because the runtime installers append functions after the
  pipeline and an early run would need `fnRefThunkTargets` as a second root set.
- ✅ **`verify-warm-rebuild`'s INVALIDATION property — FIXED at step (3) 2026-07-24** (was DEAD, found by the DFE
  rung, pre-existing): its synthetic probe edits used `__`-prefixed identifiers (`__warmRebuildProbe*`,
  `VerifyWarmRebuild.maxon:264-302`) and the parser rejects those with **E2051**, so the gate could never report `PASS
  invalidation` — it reported *"a synthetic probe edit made &lt;file&gt; stop parsing"*. Its DETERMINISM and CACHE
  properties always passed. **A gate that cannot fire is worthless** — the probe now uses legal identifiers (no `__`
  prefix), E2051 no longer fires, and the gate reports `PASS invalidation`.
- **✅ STEP (2) `__Builtins.*` RECOGNITION — CLOSED 2026-07-24** (main `1c36c1ba8`; feat `233c01045`; 1513→**1529/0**
  x64 (1525 on the branch + upstream Set-B), wasm **1366/0**). A `__Builtins.<name>` recognizer as a SEPARATE arm of
  the bare-name builtin table in `Parser.maxon`'s `parseCallNamed`, gated behind one `bytesStartWith(callee,
  BuiltinsQualifierPrefix)` (a qualified callee arrives already mangled `__Builtins.currentTimeMs`, so the bare table
  can never match it — hence a separate arm, not more rows). Three Clock intrinsics: `currentTimeNanos()` →
  `__gt_now_ns` verbatim (0 new codegen); `currentTimeMs()` → `__gt_now_ns()/1_000_000` inline (⚠ **COORDINATOR
  RULING:** the stdlib doc says a COARSE `GetTickCount64` backing — a QPC-derived ms is strictly better, finer +
  monotonic; `stdlib/` untouched, pinned by `ms-resolves-sub-tick` which a `GetTickCount64` backing would fail at
  15/16); `currentUnixTimeSeconds()` → a new `__clock_now_unix_s` (`ClockRuntime.maxon`, `osReadWallClock`,
  `IatSlot.GetSystemTimeAsFileTime`; epoch const `FileTimeTicksFrom1601To1970 = 116444736000000000`, review-verified
  decoding to the exact host `date +%s`). x64-windows only; a clock read on wasm/arm64/x64-linux refuses at its span
  with the NEW **E3104** (`SemanticTargetUnsupportedConstruct`, registry process followed) via a positive-capability
  `isClockSubstrateCallee` test (RENAMED `calleeNeedsWin32Substrate` and moved into `SemanticCheck.maxon` beside
  `targetHasWin32Substrate` on 2026-07-25, when `__gt_sleep` joined it — it is no longer clock-only) — NARROWED
  after the wasm suite showed a whole-`__gt_`-band gate also fired on
  `__gt_promise_drop` (a COMPILER-inserted scope-exit call), which would have blamed the drop instead of the `async`.
  ⭐ **FIXED THE REACHABLE PANIC (three panics, one root cause)** the recon flagged: `__Builtins.anything()` /
  `__whatever()` in plain user code died at `resolveCallFixups`/`lowerAsyncCall`/`valueTagToStdType` with NO
  diagnostic. Now a clean **E3004** (reused, not a new code — `__whatever()` and `whatever()` are ONE mistake) at the
  callee's span. ⚠ **DEVIATION FROM BRIEF, correct:** the brief said put the check in SemanticCheck; the implementer
  put it in the PARSER (`requireCalleeIsNotReservedName` at `emitCall` + `emitAsyncCall`, the complete source-call-door
  set) because SemanticCheck STRUCTURALLY CANNOT distinguish a parser-emitted `call __mm_decref` from a user-written
  `call __whatever` — by then both are the same `MaxonOp.call` and nothing records which door the name came through.
  This RESTORES `validateCall`'s early-return premise instead of papering over it. The independent review ran ~40
  constructed programs through every call door (method, type-qualified, `try`/`async`, function-value → E2004, closure
  body, interpolation, arity, builtin-name collision) — all clean-diagnostic, no panic escaped — and confirmed
  compiler-inserted calls unaffected (double-await on wasm still E3100 not E3104). ⭐ **DFE COMPOSITION MEASURED
  (review):** `currentTimeNanos` sets `usesGt` (one counter shared by scheduler deadlines + user measurements), so a
  clock-only program installs the scheduler — then **DFE prunes 19 of 23 functions**, residual ~1.35 KB code + 176 B
  `.data` (the wall clock has its own `usesWallClock` bit and installs a leaf `.data` slot only). scale: FLAT +5 allocs
  / +232 bytes per compile = the ONE new import name (proven by adding a temp 2nd import → exactly +10/+448); the
  recognizer itself allocates nothing. **⭐⭐ HEADLINE (independently re-verified): `stdlib/Clock.maxon` compiles with
  shv2 UNMODIFIED** — byte-identical copy + a driver over all 5 API surfaces builds + runs, printing a correct current
  date. **⇒ STEP (3) — the whitelist loader with Clock as entry #1 — IS READY.**
- **✅ STEP (3) STDLIB WHITELIST LOADER — CLOSED 2026-07-24, COMPLETING THE CHAIN** (feat `95bb62c4b` + perf
  `4fc57f111` + review `17c44abd0`; merged-tree x64 **1552/0**, wasm **1367/0**, no leak, `--workers=1==12`
  byte-identical). `Compiler/StdlibLoader.maxon`: `whitelistedStdlibRelativePaths()` returns
  `["stdlib/Clock.maxon"]`; `locateStdlibDir()` walks up from `Process.executablePath()`'s dir (the bootstrap's
  exe-dir walk, NOT v1's cwd walk, which fails for a program compiled in a temp dir); `loadStdlibSources(project)` (renamed, with its file, 2026-07-25)
  prepends each listed path as an ORDINARY source via `loadOneSourceFile`, so Clock rides the normal
  lex→parse→lower→DFE path with no special-casing. **BYTE-NEUTRAL for every program that does not use a whitelisted
  module:** Clock is merged but unreachable-from-`main`, so DFE prunes all 7 of its functions before the backend and
  the emitted PE/wasm is byte-identical (ZERO fragment M across the whole suite) — which is the property that lets the
  whitelist grow one path at a time without disturbing anything already green. ⚠ **BRIEF PREMISE INCOMPLETE (fixed):**
  "DFE prunes unused Clock ⇒ byte-identical" was wrong on its own — `scanRuntimeUsage`/`checkCalls` run BEFORE DFE and
  dragged Clock's scheduler cone into every program; fixed with `whitelistSkipSet` (whitelisted-AND-unreachable-from-
  `main`), which those two passes skip, restoring byte-neutrality. **⭐ OPTIMIZER caught a false scale claim:** the
  implementer's "flat ~+0.5 MB" was actually DOUBLING every rung (622 K→20.9 M) — Clock drops from the MIDDLE of a
  runtime-installing compile, shifting a survivor and forcing `valueOrigins.remapFunctionIndices`'s O(program) column
  rebuild on EVERY such compile; fixed by compacting `valueOrigins` IN PLACE (−20.5 MB rung5). **⭐⭐ REVIEW caught the
  mechanism's last growing term was DEAD CODE:** `moduleReferencesWhitelisted`'s short-circuit NEVER fired — it scanned
  the whitelisted functions' OWN bodies, whose internal `Clock.elapsedMs`→`Clock.nowMs` edge always matched and
  returned true, so the O(functions) reachability MAP was rebuilt on every compile; fixed by skipping whitelisted
  bodies (the only edge that counts is the first crossing INTO the whitelist from a non-whitelisted caller), which
  removes −16 K…−246 K x2.0/rung and leaves the non-Clock corpus with NO growing whitelist term at all — a FLAT
  per-compile constant (Clock's DFE-pruned lex+parse+lower). Headline: `stdlib/Clock.maxon`, UNMODIFIED, now compiles
  as part of every build and a driver reads a correct current date (exit=today). ⇒ **the whitelist now widens by
  appending ONE path** (`Sleep.maxon` measured as the next-closest: one `__Builtins.sleep`, and `__gt_sleep` already
  exists). **This CLOSES the user-directed stdlib-whitelist chain (steps 1→2→3).**
- **◑ WIDENING #2 (`stdlib/Sleep.maxon`) — ATTEMPTED 2026-07-25, ENTRY *HELD*, THREE FIXES MERGED.** The widening
  was tried and **withdrawn by coordinator ruling**; the branch keeps the try-then-withdraw history because the
  withdrawn entry's measurement is the evidence the next rung needs. **What it exposed — which is exactly why the
  cheap module goes before `Map`:**
  1. **`__Builtins.<member>` only ever worked in EXPRESSION position.** Step 2's recognizer served Clock's three
     intrinsics and **all three RETURN a value**, so the void case was never exercised; `__Builtins.sleep(ms)` —
     the entire body of `stdlib/Sleep.maxon` — died `E2015: Unsupported: identifier statement`. FIXED: a
     `parseStatement` arm gated on `builtinsIntrinsicCallsAt`, widening a *shape* (`ident . ident (`), not a name
     list; an unknown member still gets the same positioned E3004 expression position gives.
  2. **A REACHABLE COMPILER PANIC on ordinary code** — `sleep(1)` built for wasm died `panic at
     StdToWasm.maxon:1108` (arm64 twin `StdToArm64Conversion.maxon:406`), exit 1, no file/line. It survived
     because every sleep spec is `<!-- targets: x64-windows -->`, so nothing reached it. FIXED: clean **E3104** at
     the call span on both targets, both spellings. ⚠ SCOPE RULING (coordinator): gate the **sleep callee** only;
     `async`/`await`, `runProcess` and the streaming-subprocess callees keep their documented panic — this rung did
     not make them reachable. Review confirmed the scope held and that the previous rung's `__gt_promise_drop`
     narrowing survives.
  3. **🔴 THE BLOCKER — a bare-name builtin SILENTLY SHADOWS a declared function of the same name.** Verified on
     unmodified `main`: `export function sleep(ms)` whose body is `panic(…)` is NEVER called (`sleep(1)` → exit 7),
     no diagnostic. So a whitelisted `stdlib/Sleep.maxon` is **dead at every ordinary call site** — only
     `let f = sleep` (a `functionRef`, not a call) reaches it. ⇒ **ENTRY HELD.** It would have cost **+677 allocs /
     ~30 KB on EVERY compile** (the whitelist's measured per-module price) for zero reachable capability, and put a
     false claim in this file. **RULE, now written into `StdlibLoader.maxon` + `specs-shv2/stdlib-whitelist.md`:
     do not whitelist a module whose function name a bare builtin already claims — retire the builtin first.**
  Merged as three fixes only (x64 1581→**1586/0**, wasm 1387→**1390/0**, scale **0 allocs / 0 bytes**, fragments
  additions-only). Review caught a real duplication (the statement-door predicate derived the callee name
  *differently* from the parse it predicts — two derivations of one name), an **INCOMPLETE WITHDRAWAL** (three
  surviving "whitelist entry #2" claims, one directly contradicting the withdrawal doc), and **corrected three
  false sentences by measuring them** — including one from the coordinator's own brief.
- **⬜ FUTURE RUNG — RETIRE THE BARE-NAME BUILTINS, and give a whitelisted stdlib body a USER-FACING SPAN.** The
  blocker above, plus the general gap it uncovered. Two halves, and the second is the real one:
  - **Retire the bare-name builtin** so the whitelisted stdlib declaration is authoritative. Scoping measured by
    the review (not estimated): **15 goldens across 7 specs** move, and `async-sleep.float-arg-rejected`'s pinned
    `maxoncstderr` changes. The oracle agrees with the destination — the bootstrap has **no** bare `sleep`, only
    `__Builtins.sleep`, and `stdlib/Sleep.maxon` *is* its user-facing API.
  - ⭐ **DIAGNOSTIC ATTRIBUTION FOR ERRORS RAISED INSIDE WHITELISTED STDLIB SOURCE — GENERAL, NOT SLEEP-SPECIFIC.**
    A rejection raised in a whitelisted body is positioned at the **stdlib path**, not the user's call. It fires
    wherever a whitelisted body bottoms out in something target-gated — **which is what a stdlib leaf is FOR**;
    every module queued behind Clock ends in a `__Builtins.*` intrinsic. Clock is spared only because no test
    compiles a Clock program for a non-x64 target. ⚠ The reachable repro is **`let f = sleep; f(5)`** →
    `E3104 at stdlib/Sleep.maxon:6:2` — *not* a bare `sleep(1)`, which the builtin still anchors at the user's
    span (a coordinator-brief claim the review measured and corrected). **This gap gates every future widening,
    `Map` included.**
  ⚠ Load-bearing asymmetry to decide in that rung, pinned by `builtins-sleep.rejected-on-wasm-when-unreached`:
  `checkCalls` **skips an unreached WHITELISTED body** but not an unreached ordinary call, so a dead `sleep(1)` is
  refused on wasm today while a dead `Clock.nowMs()` compiles. **The day `sleep` moves into a whitelisted module
  that program silently starts compiling again** — that flip must be a decision, not a side effect.

### ⭐⭐ THE WHITELIST IS TEMPORARY SCAFFOLDING, NOT A FEATURE (USER RULING, 2026-07-25) — and the loader is now REAL
**Three user directives, all landed** (`stdlib-diagnostic-attribution`, x64 **1586/0**, wasm **1394/0**, fragments
zero `M`/zero `A`):
1. **"The whitelist is temporary — it only limits the blast radius of adding stdlib files one at a time."** The end
   state is shv2 loading ALL of `stdlib/`, as the bootstrap does, and the whitelist being **DELETED**. Do not grow
   it as though it were a feature.
2. **"Any mention of the whitelist belongs in ONE place — where it decides which stdlib files to parse."**
3. **"The compiler must not depend on file order to work correctly, so it must not SORT — nondeterminism is to be
   HANDLED, not hidden."**

**⇒ THE LOADER IS NOW THE REAL LOADER.** `Compiler/StdlibLoader.maxon` walks `stdlib/` recursively and loads every
`.maxon` it finds; the whitelist is a single `if not isWhitelistedStdlibModule(…) continue` inside that loop.
**Deleting those lines loads all of stdlib** — verified by deleting them (the loader then attempts all 49 files and
dies on `__ManagedMemory`, which is the scaffolding doing its job). Previously the hardcoded LIST *was* the loader,
so removing it left no loader at all: the end state was a rewrite, and it is now a **deletion**. `StdlibSource.maxon`
holds the durable facts (`isStdlibFunction`, reachability, the substrate fixpoint) and never names the scaffolding.
⚠ **The file was renamed `StdlibWhitelist.maxon` → `StdlibLoader.maxon` (coordinator, at merge):** a permanent
loader must not be named for the temporary thing inside it, and the old name forced every cross-file reference to
re-introduce a "whitelist" hit.

⭐ **ATTRIBUTION — a target refusal raised inside STDLIB source now blames the USER's call.** Was:
`error E3104: C:\…\stdlib\Clock.maxon:19:10: …` — an ABSOLUTE path into the checkout, at a line the user never
wrote and cannot change, for a program whose own line 2 is `Clock.nowMs()`. Now: `main.maxon:2:10: … 'Clock.nowMs'
lowers to the runtime entry '__gt_now_ns', which has no wasm32-wasi implementation`. Reported at **the first
crossing from non-stdlib into stdlib** (`main → readIt → Clock.nowMs` blames the line inside `readIt` — user code
they can change), transitive, and it propagates the **runtime ENTRY NAME, not a bool** (Clock reaches two entries,
so a bool would name whichever it found first — a wrong answer, not a cosmetic one). **This is GENERAL and gates
every future widening including `Map`**: every stdlib leaf bottoms out in a `__Builtins.*` intrinsic, so this is
what a stdlib leaf *is*. Keyed on **stdlib-vs-user**, NOT on whitelist membership — it must survive the deletion.

⭐ **THE SORT WAS DELETED AND IT WAS A MEASURED O(n²)** — ×3.57/×3.76/×3.87/×3.94 per doubling at 12→192 synthetic
modules, converging on ×4.00: insertion sort, 4 heap allocations per comparison (`pathSortsBefore` did
`toString().toByteArray()` on BOTH sides). It priced at exactly **zero** at today's one module, which is why it had
to be measured at the size it was heading for. **Order-independence is now MEASURED, not assumed:** reversed
walks leave semantics identical and the emitted IR a PURE PERMUTATION. ⚠ **What DOES follow load order is EMISSION
order**, including which of two same-named file-private globals gets the `$1` suffix — verified both ways, exit 118
either way. **A reproducibility property, NOT a miscompile** (coordinator ruling) ⇒ its own rung, below.

⭐ **THE REVIEW CAUGHT THAT THE ONE-FILE TEST HAD FAILED WHILE THE GREP PASSED.** The whitelist owned a
`CompileError` arm (`Diagnostics.maxon`) and its printer (`Main.maxon`), thrown from nowhere else; the grep was
green only because the WORD had been scrubbed from those two comments — **the concept had been renamed out of the
search, not moved.** Deleting the filter would have stranded a dead union arm and a dead printer in two other files.
Fixed (the missing-module case is now ordinary `CompileError.fileNotFound`). **A grep is not the test; "does
deleting it touch exactly one file?" is.** The review also collapsed **two independent recursive `.maxon` directory
walks** (`loadSourceDirectory` vs the loader's) which used two constants both spelling `".maxon"` and **already
disagreed about `build.maxon`** — the next clause added to one and not the other would silently compile a file on
one side and skip it on the other.

⭐ **It also found and fixed a reachable defect: `maxon-shv2 build stdlib/Clock.maxon`** — the exact command you
would run to check whether a module is ready to be listed — reported **E3006 duplicate definition** for every
function in the file. The file was parsed TWICE and collided with itself, because `FilePath.equals` compares path
TEXT and the relative spelling and the loader's absolute one are two identities to the query database. Pre-existing
by one rung; fixed with a guard. ⚠ **It implemented the deeper repair (resolve the project root so every registered
path is absolute), MEASURED it at +824…+5,009 allocations DOUBLING with the corpus — 2.6× the per-file cost the
optimizer had rejected one commit earlier — and REVERTED it.** Taking it in a review commit would have silently
overturned a measured decision.

### ⬜ FILED BY THIS RUNG — four items, none of them its to fix
- **⭐ "One spelling of a path is not one identity in the query database."** Root cause of the E3006 self-collision
  above, and it governs the USER directory walk equally. The proper repair (resolve every registered path to
  absolute) is MEASURED superlinear-in-corpus above and moves every relative-invocation diagnostic to absolute —
  which moves the spec runner's `<fragment>` mapping. **Its own rung.**
- **⭐ E3006 for a user/stdlib function-name collision is positioned INSIDE `stdlib/Clock.maxon`** — the same
  "blames a file the user never opened" defect this rung fixed for E3104, in a different diagnostic.
  `ParseStaging.commitFuncSignatures:533` reports at the LATER declaration and stdlib always merges last, so stdlib
  is always blamed. Needs the FIRST declaration's span carried in `funcSignatures` (a core registry change) + a
  message change that moves every pinned E3006. Distinct in kind from E3104: E3104 fires on VALID user code over a
  target choice; E3006 only on genuinely broken code, and its text still names the culprit. **Its own rung.**
- **⬜ EMISSION-ORDER CANONICALIZATION.** The emitted byte image is a function of filesystem order (see above).
  Fix at the EMISSION point from a stable declaration key — **not** by sorting the loader's input, which is
  explicitly ruled out. Covers `stdlib/` and user directories together. **Its own rung.**
- **⚠ TWO REACHABILITY WALKS WITH DIFFERENT ROOT SETS.** `StdlibSource.reachableMaxonFunctionNames` roots only at
  `main`; `DeadFunctionElimination.seedRoots` also roots `__mm_leak_check`, `__gt_enqueue` and every `.rdata` reloc
  target. If a stdlib function ever lands in an `.rdata` slot (witness table / layout descriptor) the Maxon-tier
  walk calls it unreachable — so `scanRuntimeUsage` skips its floor and `checkCalls` skips its gate — while DFE
  KEEPS it. Not reachable today (`stdlib/Clock.maxon` declares no `implements`; descriptor slots name synthesized
  `__destruct_*`). **The premise is currently guarded by a COMMENT, not a mechanism** — wants a root-set assertion
  the moment stdlib grows a conforming type. *(This is the project's signature bug shape — one fact derived twice —
  caught before it could fire.)*
- ⏭ Also: `spec-test --target=arm64-macos` panics at `SpecTestRunner.maxon:928` before running anything, so even a
  COMPILE-ONLY test (one that needs no executor) cannot run on a non-arm64 host. The arm64 attribution golden was
  verified BY HAND and is byte-for-byte what it pins. A runner change would let compile-only tests run anywhere.

### ✅ FIRST STAND-IN RETIRED — `stdlib/Sleep.maxon` IS the real `sleep` (2026-07-25, x64 1586→**1587/0**, wasm 1394→**1396/0**)
**Bare-name builtins are the OTHER temporary scaffolding** (stand-ins for stdlib functions that did not exist yet),
so retiring one and deleting the whitelist point the same way. `SleepBuiltinName` + its recognizer arm DELETED;
`"Sleep.maxon"` listed; `stdlib/` untouched — **the module works unmodified, which is the test.**
- **It fixed a case where shv2 REJECTED A PROGRAM THE ORACLE ACCEPTS.** `let nap = sleep; nap(5)` was `E2004`
  (a call-site-only builtin name has no address; a declaration does) — now runs, exit 7, matching the bootstrap.
  The float-arg diagnostic also stopped being bespoke `E3005 'sleep' requires a integer` and became the oracle's
  exact **E3009** conversion message. **Retiring a stand-in is not relocation — the function starts participating
  in the language.**
- **NOT byte-neutral, and that is expected here:** 15 goldens across 7 specs moved. Whole aggregated diff =
  −15 `callDirect __gt_sleep`, +15 `callDirect sleep`, +15 `func @sleep` frames + standard prologue/epilogue.
  "A call frame appeared" accounts for ALL of it.
- **DELIBERATE behaviour change, both halves pinned:** an UNREACHED `sleep(1)` now compiles for wasm — the entry
  moved out of reachability-BLIND user code into reachability-AWARE stdlib.
- ⭐ **PRICED FOR THE NEXT SIXTEEN** (`print`, `trunc`, `runProcess`, 7×`subp*`, 4 math): the forwarding frame is
  **86 bytes ONCE per retired builtin, not per call site** (identical on a 233-byte and a 1.4 MB program). Only
  ~34 is the frame the goldens show; **~52 is a GT stack guard emitted at the BYTE level, invisible to Target-IR
  goldens** — nobody reading the diff would have found it. ⚠ The reviewer measured the one real non-GT forwarder
  (`WallClock.nowUnixSeconds`) at **+53, not ~34** — treat "~34 for a non-GT forwarder" as a PREDICTION, not a
  constant. Retiring all sixteen ⇒ a one-off ~0.5–1.4 KB.
- ⚠ **shv2 HAS NO INLINER AND NONE IS SCHEDULED** (`PLAN.md:493`, `ARCHITECTURE.md:572`), so every retired builtin
  costs that frame permanently. Measured worth of one: a 200M-iteration `trunc` loop is **282 ms direct vs 536 ms
  through a forwarder** — noise for `sleep`/`runProcess` (a park/syscall dwarfs it), but **the entire cost** for
  `trunc` and the math intrinsics. Cheaper option named, not built: a body that is ONE forwarding call can be a
  **tail-call**, turning the whole frame into a `jmp`.
- ⭐ **THE FLOOR, which `scale-test` STRUCTURALLY CANNOT FLAG:** loading is linear in module count (×1.967/doubling,
  per-entry term exactly 16 allocations), and the right unit is the FUNCTION, not the byte. Real `stdlib/` is 49
  files / 695 function declarations ⇒ **the fully-loaded stdlib will cost ~350–500 K allocations and ~25 MB on
  EVERY compile as a FLAT FLOOR** — ~60–80% of a whole rung-0 compile. Every term is measured linear, so this is
  neither debt nor defect; it is the design consequence of parsing all of stdlib every time, removable only by a
  **lazy/cached stdlib artifact — its own rung, and the one that pays for the end state.** It is a floor, not a
  slope, so no curve will ever bend to reveal it.
- ⚠ Retiring `print` will flip essentially every program onto the stdlib path, at which point
  `StdlibSource.maxon:149`'s `userCodeCallsStdlib` short-circuit — written to keep stdlib cost a per-compile
  constant — becomes dead in practice. Linear either way; note it before it is mistaken for a regression.
- ⭐ **THE REVIEW FOUND THE SAFETY NET ITSELF WAS WRONG:** the collision rule (*"do not list a module whose function
  name a bare builtin claims"*) was stated in TWO files beside a prose roster naming **4 of the 15** claiming names
  — omitting `spawnReadLine` and all seven `subp*`. A maintainer checking a module against the roster instead of
  against `parseCallNamed`'s `if` chain would be told a CLAIMED name was free, and get back exactly the silent
  shadow the rule exists to prevent. Both rosters deleted; the chain is named as the one authority. *(One fact
  written down twice — in the documentation of the rule against writing one fact down twice.)*

### ✅ TYPEALIAS RESOLUTION IS FILE-SCOPED — CLOSED 2026-07-25 (x64 1596→**1604/0**, wasm 1405→**1413/0**)
**ONE defect, four symptoms.** `project.typeAliases` was a single whole-program map keyed by the BARE name and
upserted LAST-WINS, with `RangedTypeAlias` carrying no file, no position, no export bit ⇒ same-file duplicate
silently last-wins · **stdlib clobbering the user** · cross-file user-vs-user **silent wrong answer** · the live
`Utf16UnitCount` landmine (`int(1 to 2)` in `helpers/string/utf16.maxon` vs `int(0 to u64.max)` in `views.maxon`,
one slot, winner decided by filesystem order). ⇒ same-file duplicate = **E3061** (claimed on the EXISTING entry);
cross-file = **resolved per declaring file**. `Utf16UnitCount` defused by the mechanism, `stdlib/` untouched.

⚠⚠ **THE COORDINATOR'S DESIGN RULING WAS WRONG AND THE IMPLEMENTER REFUSED IT — correctly.** I ruled "any duplicate
typealias is an error, including user-vs-stdlib", reasoning by consistency with E3006. **Function names are
whole-program in Maxon; a NON-EXPORTED TYPEALIAS IS FILE-SCOPED BY THE LANGUAGE** — that is the whole difference,
and I generalised from the wrong neighbour. Three authorities contradict it: `specs/duplicate-typealias.md`
(**status: stable**, cross-file non-exported duplicates legal, both cases passing), `specs/typealias-collision.md:116`
(`project-export-shadows-stdlib-export` — the user-shadows-stdlib the ruling forbade by name), and **stdlib itself**
(`Byte = int(0 to u8.max)` in SEVEN files — file-private re-declaration IS the stdlib idiom, so the ruling would have
made the load-all-stdlib end state unreachable). The brief also said "take the next free number"; **E3061 already
existed** for csharp+selfhosted, and minting a second would have been the duplicated-fact bug the registry prevents.

⭐ **THE REVIEW CAUGHT A REACHABLE DEFECT THE RUNG ITSELF INTRODUCED** — the exact class this gate exists for. The
rung scoped the PARSER and left four readers BARE: two deciders, nothing making them agree. With
`a.maxon: typealias Measure = int(…)` and `b.maxon: typealias Measure = float(…)`, the parser answered `int`
(scoped) and `resolveNamedType` answered `float` (bare, last-wins) ⇒ **x64 ICE** (`X64Backend.maxon:1367`, xmm in
the gpr file, no diagnostic), a **wasm component its own validator rejects**, and a struct field **silently the
wrong width**. ⚠ Coordinator-verified: **base main COMPILES that program clean (exit 0)** — a latent silent
miscompile that scoping would have promoted to an ICE. Fixed with **E3105** (137→138 codes): `record` refuses a
cross-file pair whose underlying PRIMITIVES differ, blamed on the second file's own token; **two files over two
RANGES stays legal**. That makes the bare door's contract **structural rather than a bet about the corpus**: every
declaration of a name shares one primitive in any program that compiles, so the bare answer IS the scoped answer.

⭐ **OPTIMIZER: THE CORPUS PRICED THE REAL COST AT ZERO.** Contested-name lookup was **24 allocations / 2,224 bytes
per USE** (`46 + 24N`, exact) — invisible because all 169 rung-5 corpus declarations have DISTINCT names, while
stdlib has NINE contested ones. Now flat to the byte over an 8× span. It also corrected its predecessor's
per-declaration constant (wrong denominator — 20.2, not 5.9) and found `lookupInAnyFile` WAS on the hot path
(`resolveNamedType`), hiding a term that scaled with the PROGRAM because `DeclaredAlias` is an associated-value
union ⇒ heap-boxed per call — **the same defect the P1.7a pass fixed one rung earlier**. The door now returns a
`MaxonType` and structurally cannot return the alias. Net `42 − 14·2ⁿ` allocations = **−406 at rung 5: the rung
leaves the compiler CHEAPER at scale than before it**, because the boxing fix repaid a pre-existing cost in
`classifyUnionPayload`. Path-spelling hazard probed across 7 spellings — **negative**, and structurally so: one
`FilePath` value per file, writer and reader taking it from the same object.

### ✅ A CAST TO AN UNDECLARED TYPE IS E3011 — CLOSED 2026-07-25 (x64 1604→**1610/0**, wasm 1413→**1419/0**)
`5 as CompletelyMadeUpNameXyz` **compiled clean and returned 5** — the cast to a type that exists nowhere silently
evaporated. ⇒ **E3011** (an EXISTING code, CLAIMED with an `shv2` line, not minted: using the oracle's `E2003` would
have made shv2 report two codes for one fact depending on where it was written, since `f(x Bogus)` already says
E3011). A **second** silent no-op in the same lookup also fixed: `5 as Point`, a declared `type` named from outside
its body ⇒ **E3009**; `applyCast`'s header had *claimed* to reject that spelling and was false for the one users
actually write.

⭐ **WHY IT MATTERED MORE THAN IT LOOKED: it made a committed test GREEN FOR THE WRONG REASON.**
`ranged-typealias.md`'s `cast-to-stdlib-internal-typealias` asserts a real property — a stdlib-INTERNAL alias must
be nameable as a cast target — but verified it against `ElementCount` from the **un-whitelisted** `stdlib/Array.maxon`.
The name resolved to nothing, the cast vanished, and the exit code came out right anyway. **A test that passes
because a lookup FAILS.** It was the ONLY regression in 1,611 tests once the fix landed — which is itself the
confirmation of the diagnosis.

⚠ **THE COORDINATOR'S RULING WAS REFUTED AT THE CODE — AGAIN, AND ON TWO POINTS.** I ruled "rewrite that case
in place against a loaded alias, do not disable it", and specified a negative-value discriminator.
1. **`Milliseconds` is `int(0 to u64.max)` and a negative value is NOT out of that range** — the corpus's own
   `unsigned-domain-negative-sentinel-cast` pins that `(-1) as Slot` WRAPS and is in range. The implementer went
   further and MEASURED that `x as Milliseconds` and `x as NoSuchName` emit **byte-identical Target IR** for a
   runtime operand, and that all six whitelisted stdlib aliases share that full range ⇒ **no exit code and no
   golden can discriminate; the diagnostic this rung installs is the ONLY possible discriminator.**
2. **The file is a BYTE-IDENTICAL PORT** of `/specs/ranged-typealias.md` (coordinator-verified: the diff is only
   the marker line). Rewriting a ported case's body makes shv2's copy silently stop being a port, and a future
   re-port reconciles to OUR invention — the one-fact-twice bug, in the spec corpus.
⇒ **RULE, now standing: PORTS STAY PORTS; NEW COVERAGE GOES IN A NEW FILE.** The port is `disabled-test:` with a
reason naming its blocker (`Array.maxon`, behind the `Map`/`__ManagedMemory` cone); the property is re-tested in
**`specs-shv2/cast-target-type-resolution.md`** against `Sleep.maxon`'s internal+whitelisted `Milliseconds`, with a
**one-character discriminating pair** (`Milliseconds` → exit 5 · `Millisecond` → E3011). ⭐ **The review audited that
chain and found the replacement STRICTLY STRONGER than the port**: the port could not discriminate resolved from
vanished — that IS the bug — whereas the replacement now FAILS if `Sleep.maxon` ever leaves the whitelist.

⭐ **THE REVIEW FOUND THE RUNG'S OWN FAILURE MODE ONE LEVEL UP.** The two cast sites (`castTargetTag`/`applyCast`
and `requireConstCastTargetKind`) **shared the FACT but each kept its own copy of the DECISION** — name→tag, and
`castHasNoLegalConversion`→`invalidCast` — in a `named`-`MaxonType` arm and a `ConstValue`/`Token` arm, with nothing
forcing agreement. They agreed *today*; a clause added to one (an enum ruling, per-instance wording) becomes a
**wrong answer at the other** — `let X = 5 as T` accepted where `x as T` is refused, and a compile error at neither.
Fixed by extracting `denotedCastTargetTag` + `requireCastKindsConvert`, following **this file's own existing shape**
(`requireConstOperands`) rather than an invented one. It also killed a second already-diverged pair: `evalConstPostfix`'s
header said the target RANGE is not checked while `applyConstCast` 15 lines below said it is — **measured false**
(`256 as Octet` IS E3005).

⭐⭐ **A STANDING FACT ABOUT THE INSTRUMENT, measured and then independently reproduced: THE BYTES COLUMN CARRIES
THE CHECKOUT PATH.** `ScaleTestRunner`'s `CorpusRelativeRoot = ".scale-tmp"` is relative to the working directory,
so a longer worktree path costs bytes. Measured **204 bytes per path character** (the rung's own "+408 flat"
headline was refuted: base and HEAD are identical in EVERY column at all six rungs when built at the same path —
408 ÷ 204 = exactly 2 characters), then reproduced by the review from a second worktree 13 characters shorter:
**−2,652 = 13 × 204, flat at all six rungs, allocations delta 0.** ⇒ **BYTES ARE NOT COMPARABLE ACROSS WORKTREES;
ALLOCATIONS ARE.** This retroactively explains phantom flat constants this log has chased more than once.
Measured wins: per-reference cost of the new cascade is **0 allocations** (the payload-free-union choice holds at
runtime), and a qualified-alias program is now **−125 ns/reference, −3.4% of the phase**.

### ✅ A TYPE NAME IS DECLARED EXACTLY ONCE — CLOSED 2026-07-25 (x64 1620→**1633/0**, wasm 1429→**1442/0**)
`type Box` + `typealias Box` **compiled clean and silently discarded the alias** (exit 7); the reverse order gave
`E3009 Cannot cast from int to struct` — a consequence, not a diagnosis. **Which declaration won was decided by
SOURCE ORDER**, the same class of order-dependence being removed elsewhere. ⇒ the whole type-name namespace is now
one-declaration-per-name: **E3006** (kind-neutral) and **E3061** (both typealiases). **No number minted** — E3006's
doc was already kind-neutral, and shv2's member `duplicateFunction` **was already a lie at 3 of its 4 sites**
(renamed `duplicateDefinition`). ⚠ **The oracle could not arbitrate: `maxon-sharp` ICEs (`E9001`) on one ordering
and silently last-wins on the other** — so the rule rests on CORPUS evidence, checked twice.

⭐ **THE CARVE-OUT IS PER ALIAS *FORM*, NOT PER KEYWORD**, and `stdlib/` forces both halves: 7 files declare `Byte`
(ranged) and 5 declare `ByteArray = Array with Byte` (generic-instance) — a ranged-only carve-out makes the
load-all-stdlib end state UNREACHABLE. Derived by applying the alias rung's PRINCIPLE (*reject exactly when nothing
downstream can pick a winner*) rather than copying its answer: ranged aliases are file-scoped ⇒ a per-file winner
exists ⇒ cross-file legal; every other type-name door is bare and whole-program ⇒ no winner ⇒ rejected.
⚠ **Honest weak point, recorded in the spec (`type-name-collision.md:61-64`) rather than papered over:** the
*generic-instance* half is NOT justified by "a winner exists" (that registry is bare too) but by "the corpus needs
it, and disagreeing instances are last-wins" — E3105's unbuilt sibling.

⭐⭐ **THE REVIEW CAUGHT A REGRESSION AGAINST MAIN THAT THE IMPLEMENTER AND OPTIMIZER BOTH MISSED.**
`TypeNameRegistry.record` kept the FIRST declaration and never updated it when a legal repeated alias was accepted,
so `isLegalRepeatedAlias` measured every newcomer against the first declaring FILE. **A file declaring one typealias
TWICE was then accepted IN SILENCE whenever any other file also declared that name** — each compared against the far
file, found legal-cross-file, let through:
```
a.maxon: typealias L = int(0 to 10)      main:            E3061 b.maxon:2:11  ✅
b.maxon: typealias L = int(0 to 20)      branch (before): exit 0, NO DIAGNOSTIC  ← REGRESSION
         typealias L = int(0 to 30)      branch (after):  E3061 b.maxon:2:11  ✅
```
⚠ **`stdlib/` is precisely that shape (7 files declare `Byte`), so the hole was open for every name the rule
protects.** Fixed with one `upsert` — an accepted declaration becomes the incumbent the next is measured against —
which also closed the same hole for the **generic-instance and function** alias forms, which nothing had ever
checked. Coordinator-verified: branch now matches main's code AND position exactly.

⭐ **THE REVIEW VALIDATED THE RULE WITH ITS OWN EXTRACTOR, NOT THE ONE IT WAS HANDED.** Over `stdlib/`,
`maxon-shv2/Compiler/`, **and `maxon-selfhosted/` (191 k lines)**, plus every ```` ```maxon ```` block in BOTH spec
suites (3,568 + 2,269): **ZERO names claimed by two kinds or forms, zero same-file duplicates, zero non-alias names
in >1 file.** The ONLY pre-existing program declaring a top-level name twice is `error.duplicate-typealias-same-file`,
already an error test ⇒ **no previously-legal program in any corpus is newly rejected.** It also found a THIRD
motivating defect nobody had reported: a user `type Clock` **silently last-wins against the whitelisted
`stdlib/Clock.maxon`** on main. All 49 cross-file and 49 same-file kind pairs measured.

⭐ **OPTIMIZER: 41% OF THE RUNG'S MEMORY COST WAS SPENT ON NOTHING.** `FileParseArtifact.create` built the new
`TypeNameDeclArray` EAGERLY, but only the real parse ever pushes to it — the tolerant sweep and
`walkConstInitializer` structurally never could. Fixed with the shared-empty COW this file already documents for
`interfaces`/`conformances`; attribution exact to the unit (`phase:signatures` delta now **0 at all six rungs**,
was precisely *files + top-level constants*). It also built an experiment for a suspected throwing-`get` cost,
measured it IDENTICAL, and reverted it — "it bought zero and cost a `panic` arm."

### ⬜ FILED BY THIS RUNG
- **Doubled E3005 when a WHITELISTED stdlib file fails to parse** — `StdlibSource.stdlibFunctionNames` re-queries
  `queryParseOps` under a comment asserting "a memo HIT", FALSE for a failed parse (never cached), so the file
  re-parses and `reportParseError` fires twice. Pre-existing, reproduced on main. **Belongs to the whitelist rung.**
- **`abortedParseArtifact` carries type-name decls but NOT `artifact.diagnostics`** — a file that records an E3011
  and then dies on a `ParseError` loses it. Pre-existing, identical on main; a distinct diagnostic-completeness
  change needing its own spec cases.
- **`maxon-sharp` ICE** — `type Box` then `typealias Box` crashes the bootstrap (`E9001`, `ParseStructLiteral`);
  reverse order silently last-wins. ⇒ bootstrap-oracle list.
- ⏭ Measured and REJECTED, number recorded so nobody re-tries it blind: sourcing the five type-minting keywords from
  `TokenKind.<k>.rawValue` compiles but costs **+26,503 code bytes** — a string-backed enum's `rawValue` is a real
  accessor, not a folded constant. Named goldens hold the two spellings in step instead.

### ✅ A GENERIC INSTANCE'S *COMPILED* NAME IS IN THE TYPE NAMESPACE — CLOSED 2026-07-25 (x64 1634→**1642/0**, wasm 1443→**1451/0**)
**TWO silent memory-safety defects, both coordinator-verified on base:**
1. `type Box_String` + `Box with String` ⇒ both emit `__destruct_Box_String`, one body wins ⇒ **build exit 0, ZERO
   diagnostics, exit 101 (LEAK)**. ⚠ With matching layouts it silently "WORKS" instead — the worse half, because the
   surviving cascade releases through a real refcount header, so the wrong destructor is *plausible*.
2. ⭐ **THE MANGLING JOIN IS NOT INJECTIVE** — `_` is a legal name character, so `Pair with (Box_Int, Str)` and
   `Pair with (Box, Int_Str)` both compile to `Pair_Box_Int_Str` ⇒ **build exit 0, no diagnostic, SIGSEGV**, with
   **no declaration involved at all**. ⚠ **NOT fixable by changing the separator**: `Pair with (Box with A, B)` and
   `Pair with (Box, A with B)` collide under ANY separator — only a length-prefixed (Itanium-style) mangling makes
   the class unrepresentable, and that moves every golden. A future rung; the diagnostic makes it detectable today.
⇒ **E3006 (CLAIMED, not minted — its doc already owned "one name, two declarations"), reported at the USER's
declaration span** by `ParseStaging.checkTypeSymbolNamespace`, one pass, two O(1) probes per instance.
⚠ **Load-bearing carve-out: a `typealias` is NOT a claimant** — an alias mints no symbol (an instance's methods are
emitted under its BASE's name) — so `typealias Box_Integer = Box with Integer` stays legal. Review probed it
adversarially, not just with the spec's self-referential case.
✅ Corpus safety checked BEFORE choosing the rule, then re-checked by the review with its own greps: every
pre-existing `_`-bearing nominal name in all five corpora is leading-underscore, and **no generic base name contains
`_` or prefixes any of them** ⇒ nothing existing is newly rejected.

⭐⭐ **THE "ROUND-TRIP" THE OPTIMIZER WAS SENT TO PRICE WAS A QUADRATIC.** `mangleGenericInstance` was rebuilt at 8
call sites, and **the rebuild RECURSES** (`mangleTypeArg`'s nested arm re-enters it), so an instance nested *d* deep
re-derived all *d* enclosing names on EVERY ask: **×4.02 / ×4.01 per doubling and 87% of every allocation the
compile made** (N=1600: 31,134,966 allocations / 55.7 GB / 5,922 ms). Derived ONCE into gid-keyed
`ProgramSignatures.compiledInstanceNames` ⇒ **407,079 allocations / 93 MB / 915 ms — 76× / 600× / 6.5×**, and it
pays more on the realistic ladder than the new check ever cost, leaving the rung **allocation-NEGATIVE vs base**
(−79 … −14,445 across 8 rungs). ⭐ The check itself is LINEAR, settling at **×1.984 over 8 rungs** — and the
decisive reading is not the ratio but the **SHARE, which DECREASES monotonically across a 128× span** (a superlinear
term's share GROWS). *"The implementer's conclusion was right; his evidence wasn't sufficient for it."*

⭐⭐ **THE REVIEW FOUND THE RUNG'S OWN DIAGNOSTIC WAS UNREACHABLE ON THE SHAPE THAT MATTERS.**
`checkTypeSymbolNamespace` RECORDS E3006 at `Compiler.maxon:186`, but the gate that RAISES a recorded diagnostic sits
inside the pipeline, after `noteDestructorUsage`. On a colliding program whose declared type owns managed state the
compiler **died first** — coordinator-verified: base = `panic at SignatureIndex.maxon:3276`, stack trace, **no
diagnostic**; branch = clean **E3006**. **Cause: ONE FACT WRITTEN THREE TIMES WITH THE GUARD ON ONE COPY** — *"an
`Array`/`Set` instance has no base struct, so never read its fields"* lived in `genericInstanceHasManagedField`
(whose comment even NAMES the passes that must respect it), while its twin `genericInstanceHasStringField` and
`addNestedInstanceDestructors` never got the early-out. They agreed until something asked the other two alone.
⚠ The review also **backed out a broader first attempt** that turned a clean E2056 into a second panic.

### ⬜ FILED BY THIS RUNG — one judgment call worth reading
- **✅ CLOSED 2026-07-26 (main `22195fba7`) — BUT NOT THE WAY THIS ENTRY PRESCRIBED, AND THE PRESCRIPTION WAS
  MEASURED WRONG.** This entry said the real fix was *"demand-driven array-literal instance interning"*. Measured at
  the reported repro, that does not fix it: `[Foo.create(1)]` genuinely DEMANDS `Array with Foo`, so a narrower
  interner still interns it and `type Array_Foo` is still refused. Demand-driven interning only ever cured the
  weaker variant where the literal is of some *other* type. **The defect was never the speculation — it was that the
  compiled name was claimed in the USER's namespace at all.** ⭐ **The bootstrap oracle accepts every one of these
  programs, and the reason is a design, not an accident: it names its instances `__Array_Foo`, inside the `__` space
  `CheckReservedDeclName` already bars user code from — its two namespaces are DISJOINT BY CONSTRUCTION and it needs
  no collision check.** shv2 already shipped the enforcing half (**E2051**, message byte-identical to the oracle's);
  only the instance names were outside it. **USER RULING 2026-07-26: adopt that disjointness.** A contested compiled
  name is re-minted behind `__`, at the SOLE producer `mangleGenericInstance` — **not** at `checkTypeSymbolNamespace`
  as the coordinator's plan said, because `Parser.decrefCalleeFor` bakes `__destruct_<mangled>` into a drop call at
  PARSE time (proven by IR dump), so a name revised after the parse leaves `main` calling a symbol nothing emits.
  Applied **ON CONTEST ONLY**, so no name that ever compiled moves: `specs-shv2/fragments` came out **0-modified**
  across all four targets — which is what made the rung payable on a host that cannot regenerate arm64/x64-linux.
  **The E3006 pair check STAYS** (below). Suite 1778 → **1789/0**, wasm 1597/0, `-j1`==`-j12` byte-identical, no 101.
  ⭐ **The reviewer found the stated safety argument was the WRONG mechanism**: reserved-ness is *not* what makes
  minting safe — the re-probe loop buys disjointness from declared names for any prefix. What `__` uniquely buys is
  disjointness from every OTHER INSTANCE's compiled name — the half nothing probes — because a compiled name always
  begins with its base and no base may start with `__`. That invariant existed nowhere in the code; it is now stated
  beside the probe that depends on it and pinned by a case. ⭐ **Coverage was prose, and the prose disagreed with
  itself**: the spec said only `type`/`enum`/`union` can contest in one section and "the four NOMINAL keywords" in
  another (stale — `interface` left the roster this rung). Both halves are now cases, and the reviewer FALSIFIED
  them: deleting the `containsEnum` arm makes a `union` contest die with `panic: two functions are named
  '__destruct_Box_String'` while every other case stays green. ⚠ **One claim in the rung's own commit was corrected
  before merge**: the `Array with T` / declared `Array_T` case does **not** reach `baseLayoutOf`'s panic — that cure
  is the builtin `Array`/`Set` early-out from `5356807c8`, which predates the branch. Also fixed, same defect one
  keyword over: an `interface` named like a mangling was wrongly E3006'd too (an interface mints no per-type symbol).
  **Optimizer: nothing superlinear** — the re-probe is **at most 2 probes on anything that compiles** (a 3rd needs a
  declaration starting with `__`, which E2051 refuses), both probes are `Map.contains`; allocations went **DOWN by
  exactly 1 per interned instance** (the deleted `CompiledNameClaimant` box).
  **⇒ RESIDUAL, sanctioned up front and now COST-ONLY:** the interning is still indiscriminate — any `[` followed by
  an `identifier` interns `Array with T` for EVERY declared struct and enum in the program (`SignatureIndex.maxon`
  `internArrayLiteralAggregateInstances`). Under disjointness a spurious instance contests nothing, so this can no
  longer produce a wrong answer; it is bounded registry entries. Narrowing it is a token-level soundness problem in
  its own right (the sweep cannot see a local's type, which is WHY it over-approximates) and therefore its own rung.
- **⛔ SPECULATIVE OVER-INTERNING lets an `Array`/`Set` instance claim a compiled name the author never wrote.**
  `type Array_Foo` + any matching array literal ⇒ E3006 naming "the generic instantiation `Array with Foo`", which
  is nowhere in the source. ⚠ **The review DECLINED to narrow the check, and the reasoning is the point:** narrowing
  would be safe *today* only because of the guard it had just added and because `ArrayRuntime.maxon:2148` records a
  **slice-local** omission (a generic instance has `__destruct_<mangled>` but no `__clone_<mangled>` *at this
  slice*). **Making a memory-safety rule depend on a slice-local omission is how this class of defect comes back.
  Over-reporting a name clash is recoverable by renaming; under-reporting is a wild free.** ⇒ the real fix is to stop
  the speculation — **demand-driven array-literal instance interning** — which is `SignatureIndex`/`Parser` design
  work and its own rung.
- **⛔ A GENERIC TYPE-ARGUMENT MISMATCH IS SILENTLY ACCEPTED AND SIGSEGVs** — `Box with (Pair with (Box with Leaf,
  Box with (Box with Leaf)))` constructed from a `Pair with (Box with Leaf, Leaf)` value compiles clean and
  segfaults. Base and branch emit **byte-identical** executables ⇒ pre-existing P1.7 generics type-checking,
  untouched here. Repro in the rung's scratchpad (`probe/sd.maxon`).
- **⛔ `maxon-sharp` HAS THE SAME COLLISION DEFECT AND WORSE** — `mm_decref: refcount underflow (already zero)` then
  `panic: nil pointer`. Undiagnosed there. ⭐ **The two references agreeing is what made this a LANGUAGE rule rather
  than an shv2 artifact.** ⇒ bootstrap-oracle list.

### ⛔ FILED BY THE P1.7 CASCADE RUNG (2026-07-25) — a SILENT LEAK, and a ×5.0 in regalloc
- **⛔ A DECLARED TYPE WHOSE NAME EQUALS A GENERIC INSTANCE'S MANGLING COLLIDES ON THE SYNTHESIZED DESTRUCTOR —
  SILENT LEAK.** `typealias SBox = Box with String` mangles to `Box_String`; a user `type Box_String` then makes
  **both** `installStructDestructors` and `installGenericInstanceDestructors` emit `__destruct_Box_String`. One body
  wins. **Coordinator-verified on base `15321679e`: BUILD exit 0, ZERO diagnostics, program exits 101 — a memory
  leak.** With matching layouts it silently "works" instead. ⭐ **This is the DIRECT SIBLING of the type-name
  collision rung (2026-07-25, above), one namespace over: that one made a name declared twice in SOURCE a
  diagnostic; this is a name claimed twice in the EMITTED SYMBOL space.** The fix wants the same treatment — one
  decider, a diagnostic, specs — which is why it is its own rung and not a perf rung's side-quest. Pre-existing and
  unchanged by the cascade rung (its index is a bucket, so every claimant still expands; review confirmed the shape
  is not worsened).
  ⟲ **✅ CLOSED TWICE OVER, AND THIS ENTRY WAS ALREADY STALE WHEN IT WAS READ (2026-07-26).** The *diagnostic* half
  landed as `788f4423d` ("a generic instantiation's COMPILED name is in the type namespace — E3006") — before this
  entry was ever actioned, so the repro above had stopped exiting 101 and started printing a clean E3006. Then the
  P1.7 disjointness rung (`22195fba7`, above) removed the need for the diagnostic entirely: the instance's name is
  minted behind `__`, so the two claimants no longer share a symbol and the program **compiles and runs**, which is
  what both the oracle and this entry's own author wanted. ⚠ **The lesson is the filing, not the bug:** two separate
  agents read this entry as live work, and one of them reproduced it before noticing the E3006. A `⛔` that is
  already fixed costs the next reader the same hour every time. **Re-run a filed repro before planning against it.**
- **⛔ `regalloc:splitting` IS SUPERLINEAR — measured ×3.9 then ×5.0 per doubling, and it is ~98% of the compile.**
  On an N-generic-base ladder: **797 ms → 3,112 ms → 15,572 ms** at N=128/256/512, **identical base vs head** so it
  is pre-existing and nothing to do with the cascade work. ⚠ **TIME-only ⇒ `scale-test` structurally COULD NOT see it**
  — the same blind spot that hid both cascade quadratics. Caveat recorded honestly: the repro concentrates growth in
  ONE very large `main`, which is the known worst shape for splitting, so the exponent may be corpus-specific —
  **re-measure on a many-function corpus before designing a fix.** Repro:
  `scratchpad/pair1/n{128,256,512}.maxon`. ⇒ **the strongest Workstream-O candidate on the board.**
  ⟲ **RE-MEASURABLE (O4, 2026-07-25) — AND THIS ENTRY GOT ITS INSTRUMENT.** *"The strongest Workstream-O candidate"*
  was answered: `scale-test` now carries a per-phase **CPU-TIME** column, so **`regalloc:splitting` is readable on
  the standing ladder**, in the units this entry was forced to measure by hand. **The corpus-specificity caveat is
  the one this unblocks** — the many-function question no longer needs a bespoke ladder, it needs a run and a read.
  **The 797 / 3,112 / 15,572 ms measurement is unchanged and still stands.** ⚠ Its ×3.9→×5.0 clears the CPU column's
  few-percent noise band by an enormous margin, so this is squarely the class the column is honest for.
  ◑ **LARGELY PAID 2026-07-27 — 8.8× on the committed spec, but the pass is STILL SUPERLINEAR and this entry stays
  OPEN.** The dominant term was **not** the pressure sweep everyone assumed: it was the **exact Hall/confined matching
  computed and then DISCARDED**. `analyzeBlockPressure` ran `confinedPeakAt`/`hallVerdictAt` at **~44% of ops** at
  ~**80,000 CPU ticks a call** against ~2,900 for the whole rest of an op's step — because a pressure profile RISES
  and FALLS, so ops at both ends fit the full pool and take the `not anyFullPoolOverflow` branch. *(The coordinator's
  brief asserted the opposite — "every op overflows, so it never runs" — and the implementer measured it false. The
  instrument outranked the reasoning, which is the whole point of owning one.)* Cure: the confined half is **deferred
  until it can win**, a one-way `SplitScratch.confinedMode` flip, sound because a full-pool overflow outranks every
  confined one *and confined mode still sweeps BOTH halves* — the flip's correctness does not rest on "a split never
  raises pressure", and neither does its bound. Plus `applyForbidden` scoped to the split's own ids (its total had
  been growing **×5.07 then ×5.70 — past quadratic**), `opAtBlockPos` deleted (it restated `collectBlockOps`, and
  `fillLiveBeforeOp` already ends on that op), `PeakRank` de-allocated (a heap object per winning op and twice per
  tree combine), and the fresh-id walk made a **word-range** walk — that last one a **REAL unbounded defect the
  review found**, Θ(clobber-ops × M) for a value read in M blocks, which the committed suite already reached at 6
  (`streaming-subprocess/*`) while a probe set at 8 stayed silent. ⭐ **A bound established by probing is not a bound.**
  MEASURED: `x64-large-frame-arg7` **53.9 s → ~6.1 s**; suite **56.3 s → ~18 s**; ladder N=400 `regalloc:splitting`
  **11.49 s → 1.56 s**; whole-compile allocations **−3,598 … −177,023** across the six rungs. Decisions UNCHANGED —
  243 goldens byte-identical on every target that ran, `valuesSplit` 96/196/396, verified with `VerifyIncrementalSplit`
  on. ⚠ **RESIDUAL, and it is why this stays ⛔-adjacent rather than ✅:** per-split cost went ×2.05 → **×1.50–1.71**
  per doubling, NOT flat. Four O(block)-per-split walks remain (`analyzeBlockPressure` 45%, `sweepBlockPressure` 32%,
  `reindexSplitValues` 14%, `fillLiveBeforeOp` 5%) and on ONE basic block the dirty region IS the function. ⇒ **its own
  rung: "the splitter's positional structure"** in Future rungs. ⚠ **And the corpus STILL cannot see this**: the ladder's
  pressured functions are LOOPS, which are in confined mode from their first analysis, so the deferral reads a true Δ0
  on the standing instrument — the ladder that CAN see it is committed at `Testing/ladders/genwidelive.sh`.

### ⬜ FILED BY THE UNDECLARED-CAST RUNG — four
- **`5 as Color` (declared enum) is accepted.** shv2 erases an enum to `integer`, so it is `5 as int` and coherent;
  the bootstrap refuses it, but via its `as` parser accepting a fixed list of type FORMS, not a rule about what an
  int may become. **Whether Maxon permits `int` ↔ `enum` casts is a LANGUAGE question** — documented in the new
  spec's prose rather than silently left.
- **`p as OtherStruct` (struct → different struct) is accepted** — pre-existing, both sides already reached the
  gate; distinct from "the target denotes nothing".
- **A declared `interface` reports `Unknown type 'Shape'`** — pre-existing (`f(s Shape)` already said it); this rung
  made the cast position CONSISTENT with it rather than introducing it. The message is wrong for a declared
  interface and belongs with interface-typed values.
- **⏭ MEASURED DEBT — the QUALIFIED arm costs 18 allocations / 700 bytes PER REFERENCE** (`splitQualifiedName` +
  `joinQualifiedName` rebuild the `Base.Member` key each time, and `DeclaredAlias` boxes on both hit and miss).
  Measured LINEAR (×2.00 at three sizes) ⇒ debt, not defect. **Re-measure trigger: generic-heavy code once P1.7
  Array lands.** ⚠ This is the FOURTH rung in which "a Maxon union value is a HEAP OBJECT" has been the cost —
  check it first when a per-call term appears.
  ⭐ **SHARPENED 2026-07-27 by the splitter positional-structure rung, and the sharper form is the counter-intuitive
  one: A PAYLOAD-FREE CASE OF A PAYLOAD-CARRYING UNION STILL HEAP-ALLOCATES.** Constructing `.none` of a
  `union { none, slot(x) }` emits `x64.call mm_alloc` (16 bytes) + `mm_incref` — verified by dumping the bootstrap's IR
  for a minimal case. You would expect `.none` to be free. It is not, and it is **invisible to reading**.
  Two instances in ONE rung, both pre-existing, together the larger half of a −98.7% allocation win:
  `ValueSpillSlot.none` — asked TWICE PER OP by `indexBlockFacts` (via `spillStoreSlotOf` + `slotResidencyOf`) on every
  dirty block of every split, i.e. **two heap objects per op per split just to say "this op touches no slot"**, and
  **94.7% of all allocations in `regalloc:splitting`**; and `ValueDefSite`, asked of every value crossing the peak at
  every split — Θ(N²) heap allocations to read a THREE-WAY TAG. **Cure both times: DELETE the union, use an `enum`**
  (shv2 erases an enum to `integer`), which is why `OpSlotRole` replaced two classifiers whose answers were always
  three cases between them. **Attribution was exact: bytes-removed ÷ allocations-removed = 16.0 at all three ladder
  rungs, to the digit.** ⇒ **When a per-op or per-call allocation term appears, grep the hot path for `.none` / any
  payload-free case construction BEFORE looking anywhere else.**

### ⬜ FILED BY THE TYPEALIAS RUNG — three, none of them its to fix
- **⭐ `5 as MadeUpName` COMPILES SILENTLY** and returns 5 (oracle: `E2003`). Identical on base and branch. **It is
  why `cast-to-stdlib-internal-typealias` is GREEN — for the wrong reason**: `ElementCount` lives in the
  un-whitelisted `stdlib/Array.maxon`, so nothing resolves and the cast vanishes. **A test that passes because a
  lookup fails.** Fixing it means deciding that test's fate, entangled with the `Map`/`__ManagedMemory` cone. **Its
  own rung — and it is why any future alias-scoping mistake would be SILENT rather than loud.**
- **`commitStructTypes` is LAST-WINS for duplicate `type` names**, and a `typealias` + `type` of one name compiles
  silently (probed same-file and cross-file, no panic). The same defect this rung just fixed for aliases, one
  declaration-kind over. Wants per-file TYPE scoping.
- **E3063 / `export` as a resolution key** — deliberately out of scope; needs cross-file name resolution.
- ⏭ `recordAlias`'s content hash omits `declFilePath`. Traced NOT reachable (the fold runs in `sourcePaths` order so
  position encodes file identity; E3061/E3105 are raised at the merge, which is not per-file memoized). Sound, but
  **the argument is unwritten — write it down before someone reorders the fold.**

### ⛔ SUPERSEDED — duplicate `typealias` is a SILENT WRONG ANSWER (CLOSED above)
**shv2 has no duplicate-`typealias` diagnostic and resolution is LAST-REGISTERED-WINS. stdlib registers AFTER user
source, so a listed module's typealias SILENTLY OVERRIDES the user's.** Coordinator-verified against the oracle:
```maxon
typealias Milliseconds = int(0 to 100)      // the user's narrow range
let m = 500 as Milliseconds                  // shv2: COMPILES, m == 500 (exit 9)
                                             // bootstrap: E3005 Value 500 is outside the range of 'Milliseconds'
```
A range check that must reject silently passes. **Pre-existing and GENERAL** — it reproduces with two typealiases
in ONE user file and zero stdlib involvement, and Clock already shipped five such names (`DurationMs`, `InstantMs`,
`InstantNanos`, `DurationNanos`, `UnixSeconds`); this rung added one (`Milliseconds`). ⇒ merged, not blocked.
**But it gates the programme: every one of the sixteen remaining retirements claims MORE type names**, and the
loader's collision rule covers function-vs-function (loud) and type-vs-builtin (maintainer's job) — **not this
case, which is the one that silently returns a wrong answer.** Fix = a whole-program duplicate-typealias check
(new error code + specs + goldens). **Do this BEFORE retiring `print`.**
- Clock is the CLOSEST module by a wide margin — measured: it parses, resolves,
lowers, register-allocates and reaches LINK; stubbing ONLY its three intrinsics makes it compile clean (rc=0), so the
intrinsics are its sole blocker. Two are nearly free — **`__gt_now_ns()` already exists** (`GtRuntime.maxon:1081`,
QPC/QPF monotonic nanos): `currentTimeNanos()` ≡ `__gt_now_ns()`; `currentTimeMs()` = `__gt_now_ns()/1e6` (⚠ the
stdlib documents a COARSE `GetTickCount64` backing — QPC-derived ms is strictly better but changes the documented
backing, a small ruling); only `currentUnixTimeSeconds()` needs a wall clock (`GetSystemTimeAsFileTime`, one IAT slot).
For the whitelist mechanism itself, take the **bootstrap's exe-dir walk** (`0-Compiler.cs:929`) to locate `stdlib/`,
NOT v1's `Directory.currentPath()` walk (which fails for a program compiled in a temp dir).

**First blocker per module** (the roadmap for widening the whitelist): undeclared base struct
(`__ManagedMemory`/`Map`/`Set`/`__ManagedList`) → PANIC at `SignatureIndex.maxon:3129` — Array, Character, String,
Vector, Map, CharacterSet, List, Subprocess, HttpClient, Interfaces · `__Builtins.*` unknown → link PANIC — Clock,
Print, PrintError, Sleep · missing String methods (`addressableBytes`/`byteAt`/`trim`) — File, TcpClient, URL, utf8,
grapheme, urlHelpers · `for` statement — CommandLine, string/hash · top-level `extension` — PrimitiveExtensions, all 6
`helpers/sort/*` · top-level `#if os(...)` — FilePath, Process · non-literal field default — Json, Set · `try
(parenthesized-expr)` + `for` — Math (the only intrinsic-free near-miss) · misc — Builtins, Unicode/utf16, Sha256,
unicodeCategory, withIterator, Ascii.

⚠ **A REACHABLE PRE-EXISTING DEFECT found by the recon, SEQUENCED onto rung (2), not dropped:** `__Builtins.anything()`
in PLAIN user code panics at link with no file/line/diagnostic — `panic at X64Backend.maxon:1793: resolveCallFixups:
call to unknown function '__Builtins.currentTimeMs'`. ROOT CAUSE: `isCompilerInternalCallee` (`MmRuntime.maxon:137`)
returns true for ANY `__`-prefixed callee and `SemanticCheck.validateCall` (`SemanticCheck.maxon:642`) returns EARLY
on it, skipping E3004 — its stated premise ("no user name can be DECLARED with this prefix, so a `__` CALLEE is
provably compiler-owned") is FALSE: E2051 governs DECLARATIONS, not a callee declared nowhere
(`reserved-double-underscore.md:17` even says user code may reference internal names). `undeclaredThing()` and
`Undeclared.thing()` both correctly give E3004; only `__`-prefixed callees slip through. The fix is an explicit
"unknown intrinsic" arm on the recognizer rung (2) builds — rejecting all source-written `__` callees would be wrong
at the code, since shv2 must eventually ACCEPT a recognized set. (`resolveCallFixups` cannot host the diagnostic: it
has only `targetName`/`codeOffset`, no span, no `project`.)
⏭ Two diagnostic-quality bugs also found: `Log.maxon:27` and `Range.maxon:23` report `Expected ''min' or 'max''` for
an unknown qualified name — the top-level-initializer parser emits a PRIMITIVE-LIMIT message for any unknown
qualified name. Misleading, not wrong.

### Bootstrap / front-end robustness findings filed by the dogfood slices (not their rung to fix)
- **The register-pressure diagnostic PANICS** (`RegisterPressureDiagnostic.maxon:361`) whenever ANY hand-built runtime
  function raises E5001: runtime-function values are span-less, so the message backstop has no user code to blame and
  panics instead of degrading. Reachable by any sufficiently register-hungry runtime function (slice 1b hit it and
  restructured to dodge it). An allocator-robustness gap.
- **An undeclared generic base panics** (`SignatureIndex.maxon:3073`, `Map with …`) instead of emitting a clean
  diagnostic — a general front-end robustness gap, not subprocess-specific.
- Probe-surface deviations deliberately left on the temporary bare builtins: `subpSpawn` returns a `-1` sentinel
  (CLAUDE.md "no sentinel returns") and the handle ops do not guard an invalid handle; the real error-union API
  resolves both.

**Filed by D1b (2026-07-30) — three, none of them its to fix; each needs the full C# suite as its gate.**
- **⛔ `and fallthrough` INTO a payload-binding match arm is a SILENT WRONG ANSWER in the bootstrap.** The arm
  fallen into destructures a case the value provably does not hold. **Measured: exit 18** where the second
  case's payload does not exist at all (`a(n) then t = n and fallthrough` / `b(m) then t = t + m`, entered as
  `U.a(9)`, so `m` re-reads `a`'s 9) — **verified directly by the coordinator, not read off a report.** v1
  agrees, which is not evidence it is right; it is evidence neither reference asks the question. ⭐ **shv2 now
  REFUSES this construct** (D1b, positioned at the arm being fallen into), so this is a live
  shv2-vs-oracle DIVERGENCE and the oracle is the wrong one. In shv2 the same shape was worse than wrong: a
  **SIGSEGV** when the previous arm had nulled the slot, and a silent **type confusion** on the borrowed path.
- **⛔ `maxon-sharp` LEAKS (exit 101, `MM raw leak: 1 allocation(s) remain`) on an escaped union payload that is
  then `append`ed.** Narrowed by D1b's implementer: the bare escape is clean, and `append` on a non-union String
  is clean — it needs BOTH. shv2 runs the identical program at exit 0, and it is committed as
  `union-managed-payload.md`'s `retained-payload-that-escapes-shares-the-owner-s-record` (the one case where the
  incref-vs-copy choice is observable, since an escaped payload is a second owner of the same record).
- **⬜ `PurityAnalysisPass` classifies a function that mutates THROUGH a reference as PURE.** A write via a
  `VarInfo` obtained from `.get(i)` never enters `MutatedParams`, so a `returns bool` helper doing exactly that
  is un-discardable (E3064) — which is why D1b's review had to ship a reader/writer pair rather than a swap.
  ⚠ **Bounded, and over-strictness rather than miscompilation**: `IsPure` has exactly ONE consumer
  (`SemanticCheckPass.cs:139`, the discard diagnostic).
- **`Map with (Integer, <function type>)` PANICS** — `SignatureIndex.maxon:3202: ProgramSignatures.baseLayoutOf:
  generic instance 0's base struct 'Map' is not declared`. A generic-instance INTERNING gap, nothing to do with
  escapes; **reproduced identically on `13a3fcefe`**, so it predates the #73 rung that found it. Distinct from the
  undeclared-generic-base row above (that one is an undeclared base; this one declares `Map` and still fails to
  intern the instance). Its own rung. Found by the #73 reviewer 2026-07-24.
- **BOOTSTRAP (`maxon-sharp`) codegen bug — `E9001: assign value %N (kind=MaxonInteger) not in valueMap`** when a
  `try … otherwise panic` nests **two** levels deep inside a labelled argument (`f(reason: g(try h() otherwise
  panic("…")))`). ONE level deep is fine and is used throughout this codebase, which is why nothing hit it before.
  Filed here rather than fixed because a bootstrap fix is gated on the full C# suite (2883+) — not the #73 rung's
  gate. Worked around at the one site that hit it (`reportClosureEscapeAtOp`, which records why). Found 2026-07-24.

**⚠ A NAMED LIMIT OF #73's GUARD, accepted as a DECISION (coordinator, 2026-07-24) — not a residual.** The
band-append invariant makes a **newly appended** `MaxonOp` fail to compile until someone classifies it. It does
**not** catch an existing op being **MOVED** into a `break` band: the derived rule would loosen while the parser's
per-route refusals keep the corpus green, so a green suite would hide it. This is bounded (it needs a deliberate
edit to a band, in the one file whose comments say what the bands are for) and no live escape route rests on the
parser — the reviewer's sabotage proved that, including for the three programs that only *appeared* to be caught by
a type rule. The alternative — parser records route+wording into a side table keyed by `(funcIndex, valueId)` and
SemanticCheck alone decides and reports — is NOT obviously cheaper: a closure can escape by several routes, the
parser refuses at the first while the derived rule stops at the earliest op in scan order, so the mapping is not
1:1. Revisit only if a band is ever moved in anger.

Per the runtime-binding decision (Context): shv2 **excludes `Internals.maxon` and emits natively**
— builtin registration for the `__Managed*` surface replaces v1's `__Internals` mechanism +
`StdlibLoader` (6,434 lines, the road not taken).

---

## Register allocator — the DEAD-DEF pressure hole ✅ CLOSED 2026-07-28

✅ **A DEAD DEF HOLDS A REGISTER, AND NO LIVE SET COULD EVER SEE IT — DONE, MERGED** (impl `eccbf31f4` · review `bf2324a6d`; x64-windows **2071→2084/0**, x64-linux **1978/0**, wasm **1971/0**, builds 0 warnings, no 101, `verify-warm-rebuild` PASS, and the whole suite **2084/0 under a `VerifyIncrementalSplit = true` build**; arm64 SKIP — remote, UNVERIFIED, but byte-compared, see below).

⚠⚠ **THE FILED PREMISE WAS A SYMPTOM, NOT THE BUG — the fourth filing this session to be wrong about its own defect.** It read *"an INDIRECT call of arity ≥ 14 panics the register allocator"*, and the coordinator's brief carried an `N+1` arithmetic (N arguments + the callee address) that **gets the right number from the wrong mechanism**. The implementer disproved it by instrumenting, and the coordinator verified the correction on unmodified `main`: **15 parameters with the last one unused PANICS; 14 compiles — with no function value anywhere.**

**THE REAL DEFECT:** a virtual def that is **live at no program point** is absent from every live-before/live-after set, so it is **invisible to every popcount over a live set** — but **its register is real** (`mov rax, [rbp+96]` clobbers rax whatever becomes of the value, and `allocateDef` runs for every def, dead or not). So `analyzePressure` under-counts by one, `peak.found` goes false with the point still over pool, and the **COLORER** panics (`RegisterAllocator.maxon:1173`) *after the splitter believed it was finished* — violating the allocator's stated contract (`RegisterAllocator.maxon:47-56`: after splitting "coloring CANNOT fail"). ⭐ **`maxPressure`, the supposedly INDEPENDENT post-condition cross-check, has the identical hole**, so it did not fire either — though it agrees *by construction* (one implementation), which is why the rung did not need to fix it twice. Indirect calls merely sat one arity lower because **`__fnref_` thunks add a `__env` parameter the author never wrote and the thunk never reads.**

**THE FIX:** `addReuseTransientPressure` → `addOpTransientPressure`, answering both halves of one question — *"does this op's def need a register this op does not free?"* — i.e. the existing reuse-copy transient **plus +1 when the op's plain virtual def is absent from live-after and no virtual use dies there**. A def that IS live after is charged nothing, being already billed as live-before at the next op, which is why every previously-compiling program emits identical instructions. ⭐ **It is the op-def twin of the dead-PHI reservation — only the phi half existed.** A known pattern with one half missing is this file's most reliable bug signature.

⭐⭐ **THE INDEPENDENT REVIEW FOUND THE FIX INCOMPLETE, AND THE PANIC STILL REACHABLE** (`bf2324a6d`). The new charge was **CLASS-BLIND**: `anyVirtualUseDiesAt` scored a dying operand of the *other register file* as relief for a dead def that cannot inherit it — `blocked = (working ∖ dyingRegs) ∪ forbidden ∪ (fullRegisterMask ∖ classPool)` names every dying register whatever its file, and `classPool` then puts the foreign ones straight back. **So a dying XMM counted as relief for a dead GPR def.** Measured three ways (`main` / rung HEAD / after fix) with real exit codes: a **99-program sweep (9 op shapes × arities 10–20) fails 7 on the rung's HEAD** — `f2i` at *every* arity from 14 to 20, the whole cliff, not an edge. Coordinator-verified independently: a 17-float dead-tail panics on `main` and compiles on the branch. **The rung's own committed cases were all same-file, which is exactly why a green suite stood over it.**

⭐⭐ **AND THE DUPLICATION WAS THE LOAD-BEARING KIND, DEMONSTRATED IN REAL TIME.** `refreshTransientAt` (the incremental repair) was a **SECOND IMPLEMENTATION of the same decision in a second file** — *"is the def dead? does the op free a register?"* asked against the use index instead of a bitset — with nothing forcing the copies to agree and `VerifyIncrementalSplit`, **compiled out by default**, the only thing that would ever catch a divergence. **The proof it mattered is that the review's one-conjunct class fix had to be applied to BOTH copies.** It now supplies only the liveness oracle and calls the single decision procedure. **De-duplicating cost exactly ZERO** — all six scale rungs identical in allocations, bytes *and* `codeBytes` against the pre-review binary.

**COSTS:** allocations went **DOWN** (−43 / −191 / −487 / −1,079 / −2,263 at rungs 1–5) — the `ScannedValues.split` box `refreshSplitTransients` no longer builds; `regalloc:splitting` ratios 1.98–2.18, linear. ⚠ Rung 0's +31/+2,733 is the **worktree path length**, not a change. **CODEGEN IS BYTE-IDENTICAL on both targets**, byte-compared with a **same-binary CONTROL column** (x64-windows 9 programs, arm64-macos 4 programs, 0 differences and 0 control differences).

⚠ **THE 4 MODIFIED GOLDENS ARE PURE SPILL-SLOT RENUMBERING, VERIFIED NOT ASSERTED** — an op now reporting its true demand outranks one it used to tie with, so victims receive slots in a different order. Normalising ONLY the slot number (registers and mnemonics intact), the removed and added line multisets are **IDENTICAL**, slot counts unchanged and the distinct slot-ID *sets* identical (13/24/16/11). The same four tests' **x64-linux** goldens owed the identical permutation and were regenerated unfiltered (70 lines, zero unmatched).

⚠ **THE SCALE CORPUS IS STRUCTURALLY BLIND TO THE CLASS FIX** — it is int-only, so it contains **zero cross-class dead defs**. The identical-allocation result proves the *de-duplication* costs nothing and says **nothing** about the class fix; what covers that is the four review-added spec cases and the 99-program sweep. **The reviewer stated this itself rather than letting the Δ0 imply coverage it does not have.**

**CORPUS GAP THAT LET IT SHIP:** the maximum arity anywhere in either corpus is **22 (DIRECT)**; the maximum **INDIRECT** arity was **2**. The corpus had never come within an order of magnitude of the cliff. 13 authored cases now pin it — the mechanism (dead defs, **no function value anywhere**) in `register-pressure.md`, the symptom (indirect arity 13/14/20/26, plus arity 20 with **computed** arguments, since literal arguments are relieved free by remat) in `first-class-functions.md`, and the **arm64 boundary at 27 params** ungated so it runs on both lanes — verified to genuinely exercise the fix (it panics on `main` under `--target=arm64-macos`).

**⇒ THREE RESIDUALS FILED:** · ⬜ **`__fnref_` thunks materialize a `__env` parameter they never read** (`Compiler/Runtime/FnRefThunk.maxon:153`) — a **compiler-introduced surplus value invisible to the author**, which is what pushed the arity boundary down by one for every function value. Post-fix it is no longer a panic but costs a **store + reload in every thunk of user arity ≥ 13**. ⭐ Reviewer's shape note, adopted: the rung is **NOT** *"remove `__env`"* (closures need the slot) but *"a materialized parameter with no uses should not be materialized"* — a DFE/lowering question, in `Compiler/Runtime/`, not `Targets/`. · ⬜ **`phase:parse` is SUPERLINEAR IN CPU while LINEAR IN ALLOCATIONS** — coordinator-measured at `--repeat=3` (min-of-3, so not a noise sample): ratios **1.80 → 2.23 → 2.48 → 3.00 → 3.14** across the doubling ladder, against parse allocations of 1.85 → 1.99. At rung 5 parse is **13.0e9 ticks against regalloc's 14.0e9** — nearly the whole compile. This rung runs *after* parse and cannot have caused it. **This is exactly the cost class the CPU column was added to make visible**, and it is invisible to the memory columns by construction. **RE-MEASURE TRIGGER: any parser change; confirm at `--repeat=3` before acting.** · ⬜ **`verify-warm-rebuild maxon-shv2/track0` is BROKEN on `main`** — `track0/alloc-torture.maxon:47` calls `__Builtins.parallelBoundary`, which no longer exists (**E3004**). Reproduced on `main`; track0 cannot serve as that gate's target until repaired.

⚠⚠ **STANDING TRAP, NOW THIRD-TIME-CONFIRMED IN ONE SESSION: A COMPARISON WITHOUT A CONTROL IS NOT A MEASUREMENT.** The reviewer's first byte-compare read *"16 of 16 DIFFER"* — because it wrote the two artifacts to different output paths and **the image embeds its own output path**. It self-corrected by adding a **same-binary control column**, which read 0 and proved the harness sound. The same path-length family has now caught **an optimizer, a reviewer, and the coordinator** in this session alone. ⇒ **Every A/B carries a same-binary control, and the control is run BEFORE any difference is reported.**

## `phase:parse` — the Θ(vars × constructs) bend ✅ CLOSED 2026-07-28

✅ **THE PARSE BEND WAS Θ(VARS IN SCOPE × MERGING CONSTRUCTS), AND IT ALLOCATED NOTHING — DONE, MERGED** (optimize `88ca97b33`; x64-windows **2142/0**, x64-linux PASS, wasm **2029/0**, builds 0 warnings, no 101, `verify-warm-rebuild` PASS, **`specs-shv2/fragments/` EMPTY after a full run**; arm64 SKIP — remote, UNVERIFIED).

**THE READING THAT FOUND IT — off the doubling ladder, at `--repeat=3`.** `phase:parse` was the **ONLY** superlinear phase: CPU **×2.02 → 2.29 → 2.57 → 2.86 → 3.24** per doubling while its **allocations stayed linear** (1.85 → 1.99), and every other phase was linear in CPU (lex 1.84–2.01, signatures 1.84–2.04, isel 1.85–1.90, regalloc 1.93–2.19). Parse's share of compile CPU was climbing **14.4% → 37.5%** while regalloc fell 51.6% → 40.5% — **on that trajectory parse overtakes the register allocator at the next rung.** ⭐ **A cost that bends in CPU while flat in allocations ALLOCATES NOTHING**, which is exactly the class the CPU column was added to expose, and why the memory columns read flat over this for the project's whole history.

**THE BEND, NAMED:** `Parser.assignedBindingsIn` (`Parser.maxon:13402`). Every `if`/`match`/`while`/`for` asks which mutable bindings its token extent assigns; the old answer walked the **entire** `mutableVars` list and probed each binding's name against a per-construct `Set` — one `ByteArray` hash per binding in scope, per construct. `Parser.mintLoopHeaderPhis` had the same shape for the loop half. **Instrumented on the corpus BEFORE any change: 39,400,068 probes at rung 5 across 18,571 constructs, ×3.99 per doubling — and `maxA = 2`. It was scanning 4,102 bindings to select two.**

⭐ **THE METHOD IS THE LESSON.** The corpus doubles *both* axes at once and structurally cannot separate them, so a new ladder did: `maxon-shv2/Testing/ladders/genscope.sh` (constructs / locals / both / a `straight` control), committed and **indexed in that directory's README**. Constructs alone ×1.72–1.89, locals alone ×1.46–1.83, **both ×2.21–2.83**, straight control ×1.80–1.89 — only the product bends. Then the diagnosis was **confirmed before anything was designed**, by capping the walk at 8 entries: bend gone, allocations unchanged to the digit.

**THE FIX:** an inverse index on the parser — `mutableVarInnermost` (name → innermost position) and `mutableVarShadowed` (per position, the position it shadows) — pushed/popped in lockstep under the existing `openBlock` mark, copy-on-written off shared empties. Θ(vars × constructs) ⇒ **Θ(tokens in the extent + assigned)**. `namesAssignedIn` and its per-construct `Set` are **deleted**, and `mintLoopHeaderPhis` takes the same carried list — **the two orderings that had to agree are now one.**

⚠⚠ **THE RISK HERE IS CORRECTNESS, NOT SPEED: THE CARRIED SET'S ORDER DECIDES PHI ORDER.** The claim the whole change rests on is that *positions are collected ASCENDING, ascending position IS `mutableVars` declaration order, so there is no sort and no reordering — and the shadow chain is what keeps it exact.* A mistake there **miscompiles**; it does not merely run slower.

**RESULTS (path-clean A/B, parent and fix built in the same worktree at the same path, `--repeat=3`):** rung-5 parse CPU **13,160,151,600 → 3,354,949,049 (−74.5%, 3.92×)**; whole-compile CPU **35.87G → 24.78G (−30.9%)**. **Coordinator-verified independently on the merged branch: parse ratios now ×1.71 1.95 1.94 2.03 2.00 — LINEAR — and parse's share reads 14.0% → 13.6% ACROSS the ladder instead of climbing to 37.5%.** Same-binary control: allocations and bytes bit-identical, CPU spread −5.5%…+0.5%, so the noise band is ~6% and a 3.9× is nowhere near it.

**MEMORY — exactly two phases moved and the arithmetic closes:** `phase:parse` −4,060 … −143,487 allocations and −9,493,072 bytes at rung 5 (the per-construct `Set`, ~7.7 allocations each); `phase:signatures` **+0 allocations**, +35,760 bytes — the `Parser` record grew 16 bytes for two pointer fields, and 1,536/16 = 96 = the 30 files plus 66 top-level constants that phase builds a `Parser` for. **−9,493,072 + 35,760 = −9,457,312, the total.** (An unconditional `create()` pair first measured **+13,410** allocations there; the shared-empty COW is why it is now zero.)

⚠⚠ **THE INDEPENDENT REVIEW DID NOT COMPLETE — it died on a hard weekly API limit mid-investigation, and its report is lost.** Its *measurements* survive in the worktree and are strong; the coordinator then closed the four checks it had not reached. **Recorded as a process deviation, not papered over.**
- ⭐ **What the review DID establish, and it is the load-bearing evidence: a 250-case DIFFERENTIAL FUZZ against a control worktree pinned at the exact parent commit (`3f2e3203f`)**, generating precisely the shapes that stress the index — many mutable bindings (wide carried sets), **the same name re-declared at several nesting depths (shadow chains)**, merging constructs assigning random subsets **in deliberately inverted order**, and shadows popped with the outer binding carried by a later construct. It compared the build exit code, **the emitted binary BYTE-FOR-BYTE**, and **runtime stdout + exit code**: **130 compiled clean on both, 0 mismatches.** It also ran `verify-warm-rebuild` on a shadow-heavy program: PASS.
- **Coordinator-closed afterwards:** `namesAssignedIn` is **fully deleted** (zero occurrences ⇒ one door); the **shared-empty COW is safe** (`if self.mutableVarShadowed is sharedEmptyMutableVarShadowed` swaps in fresh arrays *before* any push, so the shared empty is never written through); **lockstep violations PANIC loudly** rather than corrupting silently (*"the shadow column ran out before `mutableVars` did — the two grew apart"*); and the **closure-lifting path saves/restores both index fields AS A UNIT with `mutableVars`**, so the index travels with the thing it indexes.
- **Corpus-wide codegen neutrality is the other pillar:** `specs-shv2/fragments/` **empty after a full suite run** on x64, with x64-linux and wasm both PASS — for a change that decides phi order, byte-identical goldens across ~2,142 programs is the strongest available evidence.

**⇒ THREE ITEMS FILED, each with its numbers:** · ⬜ **The token scan still walks token-by-token**, so a construct nested D deep has its tokens re-read by all D enclosing scans (`LoopNestBase`, isolated by `gennest.sh`). ⭐ The optimizer also **corrected a stale claim** in `blockStatementEndIndex` that inverting the scan costs ×3.5 — that figure had been measured when its constant was multiplied by the Θ(V) walk now removed, **so it no longer stands in either direction and the question is open.** · ⬜ **`desugarTry` snapshots the whole `mutableVars` per `try`** (`Parser.maxon:10039`, `10284`) — the **same O(V×C) shape, in time AND bytes**. ⚠ **The scale corpus generates no `try`**, so it is unmeasured and explicitly unclaimed; it is the obvious next thing to point `genscope.sh` at. · **MEASURED DEBT (Workstream O):** the ordered insert is O(A²) in 4-byte shifts where the old walk was O(V) in hash probes — worst case measured by hand (A = V = 250/500/1000/2000, and 8 wide `if`s at V = 125…1000): **both stay linear, ×1.95–×2.03, costing +1%…+4%**. **RE-MEASURE TRIGGER: a rung that makes wide carried sets ordinary.**

## Workstream O — the compiler traces ITSELF ⭐

**Small, and it lands FIRST — it is a debugging instrument, and an instrument that arrives after
the bug is worthless.** shv2's `Logger` today formats a `String` and prints it to **stderr**. That
survives exactly as long as the compiler is single-threaded. **P1.0a interleaves N workers into one
stderr**, and P1.5 puts green threads under them; at that point a text log is not degraded, it is
*useless* — you cannot tell which worker, which compilation unit, or which phase a line came from,
and the lines themselves are torn.

**The mechanism already exists — the bootstrap has all of it.** `maxon.exe` compiles binaries that
carry a shared-memory ring (128-byte header, 8-byte packed entry headers, ticket-spinlock reserve),
and `maxon monitor` creates the segment, spawns the child with `MAXON_DEBUGSTREAM` set, drains the
ring, and decodes it — parsing real names out of a `MXDS_TAGS` blob in the PE. **shv2 is a Maxon
program compiled by `maxon.exe`, so it already runs under that monitor.** The one missing link:
the producer is wired only into *runtime internals* (mm / sched / dbg events). **Nothing lets user
Maxon source put an event into the ring.** That is the whole gap, and it is a builtin.

⇒ **Workstream O does NOT depend on Workstream R.** It is not blocked on the shv2 runtime, on
ownership, or on `String`. It can land against the compiler as it stands *today*.

**Two tiers, because the hot tier must not allocate.** A formatted-text log inside the register
allocator would (a) allocate into the very `mm` stream you are trying to read and (b) cost more
than the work it is measuring. `Logger`'s `LazyMessage` thunk does not save you: the closure env is
built at the call site whether or not the level check passes.

- **Tier 1 — structured, ZERO-ALLOC.** Event code + fixed numeric args, exactly like the existing
  Dbg events. Names are **interned at compile time** into a `MXDS_STRS` PE blob (the `MXDS_TAGS`
  trick, reused), so an event carries a `u16` id and the monitor prints the real name. No `String`,
  no thunk, no allocation. **This is the tier the passes use.**
- **Tier 2 — text.** A length-prefixed UTF-8 tail for the rare human message. Allocating, and it
  says so. `entry_size` is a `u16`, so an entry caps at 64 KiB — truncate, never tear.

**Every event carries `gt` + `p_id`.** Obtained the way `EmitDbgCallCore` gets them (`LoadCurrentP`,
NULL-guarded). **This is the entire point of the workstream** — it is what lets the monitor demux N
workers back into per-worker, per-unit timelines instead of a shuffled pile. An event also carries
a `unit_id` (which fragment / which function), so a parallel harness run reads as one timeline per
unit rather than one per process.

**The schema is FROZEN — so these are NEW codes, in a free range** (`0x5F–0xFD` is unused):

| code | event | payload after the 8-byte entry header |
|---|---|---|
| `0x60` | `LOG_PHASE_BEGIN` | `gt`(8) · `p_id`(8) · `phase_id`(2) `rsvd`(2) `unit_id`(4) — 32B entry |
| `0x61` | `LOG_PHASE_END` | *(same)* |
| `0x62` | `LOG_EVENT` | `gt`(8) · `p_id`(8) · `cat`(1) `lvl`(1) `event_id`(2) `unit_id`(4) · `arg0`(8) · `arg1`(8) — 48B entry, the Dbg shape |
| `0x63` | `LOG_TEXT` | `gt`(8) · `p_id`(8) · `cat`(1) `lvl`(1) `len`(2) `unit_id`(4) · UTF-8 tail, zero-padded to 8 |

**Keep the two-tier gating, and inline the guard** — the rule ARCHITECTURE.md already states for
shv2's producer applies here from commit 1: compile-time (`--debugstream` off ⇒ **zero
instructions**), runtime (`__ds_base == 0` ⇒ inline bail, *before* any CALL). The C# producer's MM
events pay two real CALLs before the runtime-off check; do not reproduce that wart.

### The slices

- **O1 — NOW, before P1.0a.** A `__DebugStream` builtin in **`maxon-sharp`** (callable from Maxon
  source), the four event codes, the `MXDS_STRS` intern blob, a DebugStream **sink behind
  `Logger`'s existing category/level API** (the call sites do not change), and `maxon monitor
  --filter=log` decode. **No dependency on anything in Phase 1.**
- **O2 — with P1.0a.** `gt`/`p_id`/`unit_id` demux in the monitor: per-worker and per-unit
  timelines. The pool's first debugging tool exists the day the pool does — not a month after it.
- **O3 — inside R1 @ P1.2.** shv2's own backend emits the same builtin, so the trace **survives
  self-host**. This is not new work: it is an *obligation on* the DebugStream producer port R1
  already carries. Name it there or it will be forgotten.
- **O4 — ✅ LANDED 2026-07-25. The per-phase CPU-TIME column.** A `__Builtins.threadCpuTicks()`
  intrinsic in **`maxon-sharp`**, bracketed by shv2's `PhaseProbe`, surfaced in `--metrics`,
  `--log=compiler:debug` and `scale-test`. **This is the instrument the measured-debt list below
  asks for BY NAME** (*"an argument for the runtime/time instrument that does not exist yet"*,
  *"the strongest Workstream-O candidate on the board"*). Its own section follows.

### ⚠ It is also the replacement for a timing instrument P1.0a is about to BREAK

`CompileTimings`/`CompileMemory` accumulate into **global** per-phase timers. Under a worker pool
two workers are in different phases at once, and a global accumulator cannot express that — the
numbers do not just get noisy, they stop *meaning* anything. `LOG_PHASE_BEGIN`/`END` give properly
nested, per-worker, per-unit spans, and the disjoint-and-sums-to-total check gets computed **from
the trace** instead of from an accumulator that is structurally unable to be right. *(This is the
"a dominant cost hid in the wrong timing bucket" failure, four times over in v1, and the parallel
pool is about to make the bucket itself invalid.)*

**And the trace can be a GOLDEN.** Normalize it the way `mm-trace` already normalizes — drop the
timestamp, dense-renumber ids in first-appearance order — and the **worker-count-invariance gate**
at the Phase-1 GATE sharpens from "`-j1` and `-jN` agree on the *verdicts*" to "they agree on the
**per-unit event sequence**." That is a far tighter net, and it costs nothing extra to hold.

### ✅ O4 — THE PER-PHASE **CPU-TIME** COLUMN (2026-07-25). The instrument this list has been asking for by name.

**It spans both compilers, because neither half is enough alone:** `maxon-sharp` mints the intrinsic,
shv2 brackets it. **What it closes is a sentence this file writes about itself over and over** —
*"TIME-only and allocation-free, so `scale-test` STRUCTURALLY CANNOT SEE IT"*, *"no rung of this
ladder can ever show it"*, *"the strongest Workstream-O candidate on the board"*. Those were true,
and they are now out of date **as a limitation**; every measurement filed under them stands
untouched.

- **`__Builtins.threadCpuTicks()`** — bootstrap intrinsic, `QueryThreadCycleTime` on Windows (⇒ TSC
  ticks) and `clock_gettime(CLOCK_THREAD_CPUTIME_ID)` on macOS (⇒ nanoseconds). It reads CPU time
  consumed by the **CALLING THREAD ONLY**. **It cannot see preemption and it cannot see any other
  process**, which is precisely why it can be trended where wall time cannot. *(Verified
  empirically, not argued: across a 300 ms sleep it advanced **837,520 ticks** while wall time
  advanced **301,000,000 ns**.)*
- **`PhaseProbe` brackets it alongside the existing wall clock and memory counters**, so every
  `CompilePhase` and every `RegAllocPhase` reports a CPU cost. `--metrics=<path>`'s TSV gains a
  **7th, APPENDED** column `cputicks` (appended rather than slotted in beside `nanos`, so every
  existing field keeps its index). `scale-test` prints a **third** per-phase table beside
  allocations and bytes, and records a third table in `docs/optimization-log.md` under **`## CPU`**.
- **`--log=compiler:debug`'s timing table gains a `cpu%` beside the wall `%`** — and the pair is
  worth more than either column: **a phase where the two disagree spent its wall time NOT RUNNING.**
  Measured: `load` is **51.2% of wall but 25.4% of CPU** because it waits on file IO, while
  `regalloc` is **22.2% of wall and 36.1% of CPU**.

⚠ **THE HONEST LIMITS. State them, because a column read as if it were the allocation column would
be worse than no column at all.**

- **CPU ticks are NOT bit-reproducible the way allocations are.** They move with turbo, thermal
  throttling and cache pressure from other cores — **a few percent** — so **a movement inside that
  band is not a datapoint**. Against the only question a doubling ladder asks (**×2 linear vs ×4
  quadratic**) that band has a **100% margin**, which is exactly why it is good enough here and
  worth nothing against a claimed 3% constant factor. **The memory columns keep their stronger
  property: any movement at all is real.**
- **The unit is platform-defined and the platforms do not agree** (TSC ticks vs nanoseconds), and
  there is **no honest conversion** — `QueryPerformanceFrequency` is the *performance counter's*
  rate, not the TSC's. ⇒ **Compare RATIOS between rungs, which are unit-free, or absolutes only
  within one platform.**
- **Wall-clock nanos are still emitted in the TSV and still deliberately IGNORED by the scale
  runner.** The July-vs-August argument is untouched: wall time counts every *other* process, and
  one run read `phase:parse` at **×5.03 then ×1.78 across a DOUBLING ladder** — preemption, not a
  curve of any shape.
- ⚠ **It is still an INSTRUMENT, not a gate**, exactly as the memory columns are. No verdict, no
  exponent fit, no residual, no NOISY: `scale-test` exits **0** whatever the numbers say, and a
  non-zero exit still means the **RUN broke**. A third column is a third thing to READ, not a thing
  to pass.
- ⚠ **It does NOT cure the OTHER blindness this file records — a corpus that cannot express the
  feature.** A Δ0 from a managed-free or generics-free corpus is still *"the instrument's blind
  spot, not the cost"*, and the cure for that is the scale-corpus catch-up, not this column. **Two
  different blind spots; do not credit one with the other's fix.**

⇒ **WHY IT HAD TO EXIST — the receipts are all in this file.** Every one of these is a measured
quadratic that **allocates nothing**, so the memory-only instrument read it as **Δ0**:

- **the op-insertion quadratic inside the float-across-calls rung** — `positionInBlock` +
  `Array.insert` + `shiftBlockSeqs`, fitted **`16.0n + 6.59n²`**, **68% of the whole compile at
  N=1024**, and stated there in its own words as *"allocation-free, so `scale-test` is structurally
  blind to it"*;
- **`regalloc:splitting` at ×3.9 then ×5.0 per doubling, ~98% of the compile** (the cascade rung's
  filing) — *"the strongest Workstream-O candidate on the board"*;
- **`requireInterfaceForParse`**, whose two arms allocate **identically to the digit, 1,417,523 both
  ways**, against a **+24.15 ms** parse delta;
- **`getBlockByIdIn`**'s per-guard-site linear scan — *"O(sites × blocks) in TIME, allocating
  NOTHING"*;
- **the per-instantiation generic gates** (`allocsΔ0`, measured **×2.36 → ×3.59 converging on ×4**),
  one of the two cascade duals whose closing commit (`4c4524b45` / `e306c562a`) says it outright —
  ***"'0 corpus hits' measured the instrument's blind spot, not the cost."***

⚠ **NOT on that list, and the exclusion is the point:** the float-across-calls quadratic's own
headline was hidden by the **CORPUS**, not by the memory-only column — the `floatSpill` knob was
**4**, few enough that every float took a register and no split happened, and it went **4 → 12** in
the fixing commit. Its allocations were never flat once the path was exercised (base
**1,275,640 → 80,849,418**). **That is the corpus blind spot wearing the same clothes, and O4 does
not touch it.**

⇒ **WHAT IT CHANGES FOR THE LIST BELOW.** Entries filed as unmeasurable-in-principle are marked
**⟲ RE-MEASURABLE (O4)** in place. **Their findings, numbers and attributions are unchanged** — a
measurement does not stop being true because a second way to take it appeared. What changed is that
each named RE-MEASURE TRIGGER can now be **pulled on the ladder** instead of waited for.

### Measured debt the trend log carries — revisit ONLY if the named trigger fires

`scale-test` and the per-type/per-scope tables have flagged these as **linear-in-practice**: real terms
that only bend under a specific future change. Each is filed with its measurement and its re-measure
trigger, not chased (a superlinearity you can *trigger* today is fixed, not filed):

- **⬜ ONE `MethodReceiver` PER MEMBER-ACCESS SITE — 0.034%, AND THE NAMED FIX WAS DECLINED ON
  CLARITY GROUNDS** (D2 rung, 2026-07-29). A/B with both binaries rebuilt in one worktree:
  **+146 / +306 / +626 / +1,266 / +2,546 / +5,106** allocations, ratios **×2.10 ×2.05 ×2.02 ×2.01
  ×2.01** against a doubling ladder ⇒ **strictly linear**, one bundle per site. Attribution is exact
  rather than argued: the whole-module delta **equals `phase:parse`'s delta to the unit** at every
  rung with every other phase at 0, and bytes/objects at rung 5 is 67.7 — a header plus seven 8-byte
  fields. **0.034% of rung 5.** The implementer named the fix and it removes 100% of it: flatten
  `MethodReceiver` into seven positional parameters. **Declined by the coordinator, and the reviewer
  concurred** — the struct has no sentinel members and no field whose meaning changes by door, and
  seven positional parameters would make every call site worse to read in code that had just collapsed
  NINE dispatchers into one. Paying 0.034% for that is the right trade. **Re-measure trigger:** the
  per-site cost stops being linear, or `phase:parse` allocations move for a reason nothing else
  explains.

- **⬜ `tokensHoldDirective` RE-SCANS EVERY TOKEN OF EVERY FILE TO ANSWER A QUESTION THE LEXER KNOWS
  FOR FREE** (`Compiler/ConditionalCompilation.maxon:228`; D5 rung, 2026-07-29). The filter asks "does
  this file contain a directive at all?" by walking the whole token array, so a program with **zero**
  directives still pays a full extra token pass per file. Exactly the class the memory columns cannot
  see — **allocations flat per file, CPU dead linear**: `3,390,864 / 6,436,030 / 12,617,014 /
  25,826,151 / 50,942,407 / 100,523,097` ticks = **×1.90 ×1.96 ×2.05 ×1.97 ×1.97**, and **0.41% of
  total compile CPU at rung 5 on a corpus containing no directives at all.** Attributed rather than
  guessed: that CPU tracks **source bytes (×29.7)**, not **file count (×6.2)**, so it is the token walk
  and not the per-file memo probe. Not fixed because removing it means threading a flag out of
  `tokenize` through `TokenMemo` — putting directive knowledge in the lexer's signature for four
  tenths of a percent. **Re-measure trigger:** `Testing/ladders/gendirectives.sh plain` (whose
  directives phase is 100% this scan), or `phase:directives` CPU passing ~1% of a rung.
  ⭐ **BOTH ESCAPES WERE RE-EXAMINED 2026-07-29 AND BOTH ARE CLOSED — do not re-derive this.**
  (1) *"Resolve directives in the LEXER so the seam stops existing"* is **blocked by `fmt`**, which
  shv2 will have: a formatter needs the RAW stream — directives present, BOTH branches intact — and
  a lexer that resolves them would silently delete the dead branch from any file it formatted. Giving
  `tokenize` a `resolveDirectives` mode re-creates one-fact-two-spellings (and a memo keyed on the
  mode) to save a rescan. **The shipped tier split is the right one: `fmt` reads `queryTokens`, the
  compiler reads `queryActiveTokens`.** (2) *"Fuse the pre-scan into the filter and copy lazily"*
  **saves nothing**: the gate exists so a directive-free file needs no array copy, and that file walks
  its tokens exactly ONCE either way. ⇒ the flag is the only route, and it is declined above. Note the
  failure mode if a flag were ever wrong is LOUD, not silent — a leaked directive token hits the parse
  loops' generic unsupported arm, since D5 deliberately left no directive arms there.

- **⬜ THE ARRAY ELEMENT-TYPE CHECK COSTS ~2.5 ALLOCATIONS PER MUTATOR SITE** (P1.6 Array-element rung,
  2026-07-28). Measured by a same-path A/B against the parent, built by the byte-identical bootstrap:
  **+112 / +224 / +448 / +896 / +1,792 / +3,584 allocations** and +2,560 … +81,920 bytes — **exactly ×2
  per rung against a ×2 corpus, so strictly LINEAR in sites** — entirely in `phase:parse`, over 44
  array-mutator sites at rung 0. Growth ratios are byte-identical to the parent at every rung
  (x1.79 x1.88 x1.93 x1.96 x1.98), so **no curve moved anywhere**; this is a constant factor bought
  knowingly to close a wild free. ⚠ **The eager `String.from(methodTok.value)` site name in the MANAGED
  arm is most of it** (~2 allocations per managed-element site) — the same eager cost `requireFieldType`'s
  `fieldSiteName` already pays, and curing it means changing `requireManagedValueMatches`'s signature,
  which also moves the struct-field and union-payload doors. **RE-MEASURE TRIGGER:** any rung that makes
  the site name lazy at one of those three doors — do all three, or the rule acquires a second spelling.
  ⭐ **One optimization was implemented, measured and REVERTED, deliberately**: gating the trivial arm on
  `arrayElementIsManaged` instead of the classifier saves exactly one `PayloadClass` box per trivial site
  (**0.007%**), at the price of a SECOND implementation of "does this own heap?" — and the two already
  disagree on a generic-alias `named` type. A rung whose whole subject is two things disagreeing about a
  type does not buy 0.007% with a second opinion whose failure mode is the wild free it exists to stop.

- **⬜ THE THREE MATERIALIZED STRING VIEWS CREATE THEIR ARRAY WITH NO RESERVE** (P1.8 Slice E optimizer,
  2026-07-28). `.codepoints()` / `.utf16()` / `.toByteArray()` walk 4→8→16→… capacities and the bump allocator keeps
  **every intermediate buffer**. Measured never-reclaimed slab per call on a 10-codepoint subject: `clone()` **72 B**
  (cheapest measured), a control **141 B**, `toByteArray()` **161 B**, `codepoints()` **376 B**, `utf16()` **378 B** —
  ~2.6× the control. The runtime already loads `len` at entry, so **one `__arr_reserve` collapses it**. Also measured:
  `toByteArray()` costs **~10× `clone()` per byte** (16.4 vs 1.66 ns), a per-byte `__arr_push` against a block copy.
  ⚠ **Deliberately NOT shipped by the optimizer, on two grounds that both hold**: it is a constant factor on a curve
  measured LINEAR (×2.00 per doubling out to 524,288 bytes in every seed), and **it would move committed goldens**.
  **Re-measure trigger:** a fourth materialized view, or any consumer that calls one in a hot loop.

- **⬜ THE TWO UTF-8 DECODERS SHARE THREE LINES IN DIFFERENT ORDERS** (P1.8 Slice E review). `buffer + cursor`,
  `__utf8_cp_at`, `__utf8_len_at` — the genuinely coupled pair, left unfolded because folding renumbers values inside
  `body` and **moves committed goldens**. Three lines and a golden re-mint; the skeleton around them WAS folded
  (`emitStringWalkSkeleton`, byte-identical, sabotage reds 27).

- **⬜ A NO-ARGUMENT `s.trim()` COSTS THE COMPILER ~6.5× WHAT `s.trim(cs)` COSTS, PER SITE** (P1.8 Slice D
  optimizer, 2026-07-28). Measured on a private ladder at 1,024 call sites: **~3,266 allocations per site
  vs ~500** for the explicit form (4.24M vs the control's 0.90M), because the parser **inlines the whole
  7-member default preset at every site** rather than calling a shared entry point. **Strictly LINEAR in
  sites** — ×1.83…×1.97 per doubling — so it is a constant factor and NOT the fix mandate. **Named cure:**
  a runtime `__cs_default()` entry point, which collapses the per-site expansion to a call.
  **Re-measure trigger:** any rung that adds another no-argument `CharacterSet`-defaulted method (the
  obvious next ones are `trim`'s siblings gaining defaults, or `split`/`replace` taking a set), or any
  corpus that starts emitting `trim()` in bulk — at which point the constant is multiplied by a new
  surface and is worth the entry point.

- **⚠ RUNTIME COST, NOT COMPILER COST — a predefined `CharacterSet` is REBUILT ON EVERY CALL**
  (~1,068 ns and ~1,155 bytes of never-reclaimed slab per call, ≈ HALF the cost of a whole `trim()` of a
  9-byte string; P1.8 Slice D optimizer A/B against a hoisted-set control). It is a **constant** per call
  — the set has a fixed 7 members — and the blocker is that **shv2 has no module-level `static let` of a
  heap value**, so there is nowhere to cache it. **This is the THIRD and FOURTH firing of the slab
  trigger below** (a trim mints an owned result per call; the preset is rebuilt per call), after
  `for c in s` and `slice`-in-a-loop. Cured by the same recycling `__mm_free`; do not fix separately.

- **⭐⭐ `for c in s` ALLOCATES ONCE PER TRIP AND THE SLAB NEVER GIVES IT BACK — MEMORY IS LINEAR IN
  ITERATIONS, NOT LIVE DATA** (filed by P1.8 Slice B's optimizer, 2026-07-28). `MmRuntime` is a bump
  allocator with **no free list**, so a loop that mints a value per trip grows the working set without
  bound: **65,536 clusters → 11.6 MB · 262,144 → 97.1 MB · 1,048,576 → 398.7 MB**, at ~82 bytes per
  minted `Character`. ⚠ **It is NOT a leak** — every allocation is freed and no run exits 101 — the slab
  simply never reuses a slot. **TWO CONTROLS make it shv2's divergence rather than the algorithm's**:
  `count()` runs the SAME `__gr_end` scan minting nothing and stays **FLAT at 3.7 MB across a 4× data
  range**, and the C# bootstrap runs the same source in the same time (155.7 vs 156.4 ms) at **7.3 MB**.
  Not fixed here because the cure is a **recycling `__mm_free`** — which `Compiler/Runtime/MmRuntime.maxon:738`
  already anticipates in its own comment ("the day `__mm_free` returns a slot to a free list, the reused
  slot must be re-zeroed on alloc") — and that is a RUNTIME rung, not an optimizer's edit.
  **TRIGGER TO RE-MEASURE: any construct that allocates once per loop trip.** `for c in s` is the FIRST
  one the language has, which is exactly why this debt only became visible now.

- **⭐ THE SCALE CORPUS CONTAINS ZERO `for … in`, AND FOUR CONSECUTIVE RUNGS HAVE PAID FOR IT SEPARATELY**
  (measured by inventory, 2026-07-28). `--emit-corpus` at rung 5: `for … in` in any form **0**; `.count()`
  **1,024 sites, every one an Array/Set receiver**, not one String; `.bytes()` **0**; `'…'` tokens 53,766
  **of which 0 are in a value position** (all block labels). ⚠ P1.8 Slice A committed `Testing/ladders/genfor.sh`
  for precisely this hole and **the main corpus was never taught the construct**, so every later rung
  re-hit it. The tree now carries FOUR private ladders — `genfor.sh` (Slice A), `genborrow.sh` (E3070),
  `genstring.sh` (Slice C), `genstring-grapheme.sh` (Slice B) — plus upstream's function-value ladder, each
  built because the shared instrument could not express what its rung added. **That is an instrument
  problem, not five coincidences**, and it is why a Δ0 from `ScaleCorpus` is worth nothing until you have
  read what the corpus actually contains. ⚠ Note the trap runs BOTH ways: the corpus is *not* blind to
  `String` (756 `==` sites), so a neighbour of the shape reads non-zero and can be mistaken for coverage.

- **`__str_find` IS A NAIVE SEARCH WITH A GENUINE O(haystack × needle) TERM** (filed by P1.8 Slice C's
  optimizer, 2026-07-28). `Testing/ladders/genstring.sh <n> data-findquad` reads **×3.92 ×4.15 ×3.95 ×4.14**.
  ⚠ **It needs BOTH conditions at once**: a needle whose LENGTH SCALES WITH the haystack, *and* data
  self-similar enough to defeat the first-byte fast reject. A fixed needle is a constant factor —
  `data-find` reads **×2.04** — and it is measured LINEAR on every realistic shape including `split`
  (×2.04 over 16× segments) and `replace` (×1.96 over 16× matches), its two biggest consumers.
  **TRIGGER TO RE-MEASURE: a consumer that searches with a needle whose length grows with its input.**
  The fix is two-way/KMP. ⚠ **`stdlib/String.maxon`'s `findIn` — which BOTH reference compilers run —
  has the same shape**, so this is a shared property of the language's search, not an shv2 divergence.

- **`Parser.borrowSubjectIdOf` IS O(K²) IN DISTINCT BORROWED-AND-WRITTEN STORAGES IN ONE BODY** (filed by the
  E3070 rung's optimizer, 2026-07-28). A linear scan of the distinct-subject list, entered once per borrow
  mint and once per recorded write. Measured on `Testing/ladders/genborrow.sh`'s `subjects` mode:
  **×3.99 ×2.23 ×2.66 ×3.08** per doubling. Fitting `aK + bK²` over the top two rungs gives **a = 15,697,
  b = 36.2 ticks**, so the quadratic term does not equal the linear one below **K ≈ 434 distinct subjects in
  a single function body**. The scale corpus reaches 256 there (~2.4M ticks at rung 5, **~0.02% of the
  compile**); hand-written code reaches single digits. **TRIGGER TO RE-MEASURE: machine-generated code
  declaring hundreds of distinct arrays in one body.** Not fixed because both closures — 8 bytes on every
  `VarInfo`, or a per-function `Map` — tax every program to buy a term that is invisible on all of them.
  ⚠ Note what made it findable at all: the scale corpus was **NOT BLIND to this rung, it was NARROW**, which
  reads as coverage and is worse — it holds one borrow per subject and binds every accessor to a name, so
  every quadratic the rung could contain has degree 1 there and cannot appear. The purpose-built ladder is
  the instrument; the corpus's healthy non-zero was not evidence.

- **#124 — shv2's ARRAY ROSTER IS SHORT OF THE BOOTSTRAP'S, AND ITS EMPTY-SLOT SURFACE IS ONE FUNCTION WIDE.**
  Two residuals from the `Array.resize` fix (`98834dd2e`), both disclosed rather than papered over.
  **(a) THE REMEDY LIST IS ASYMMETRIC.** E3106 refuses `resize`-to-grow on a managed element type in BOTH
  compilers, but shv2 synthesizes neither `growFilled` nor `truncate`, so its diagnostic can only offer `push`
  where the bootstrap offers all three. A user told to shrink a managed array in shv2 has **no supported call** —
  `resize` is now refused and `truncate` does not exist. **5 cases in `specs-shv2/array-slots.md` are
  `disabled-test`** with the missing mechanism named in each.
  **(b) `emptySlot` COLLAPSES INTO `indexOutOfBounds`.** The bootstrap now distinguishes them — that was the second
  defect the fix uncovered, and the reason `CompileTimings` died reporting *"phase index OOB for 'load'"* about an
  index that was perfectly in range. shv2's `emitLoadElemAndOkReturn` still reports a NULL slot through the same
  `emitOobReturn` a real range violation uses, so 4 of those 5 disabled cases are this. **A wrong error code sent a
  reader hunting an index bug for a null-slot bug once already; it will again.**
  **(c) AND THE CHECK LIVES ONLY IN `get`, IN BOTH.** Measured, not inferred: `for x in arr` over a sparse array
  panics *nil pointer or invalid memory access*, and `arr.pop()` gives *mm_incref called with NULL pointer* in
  `SlotArray.pop`. E3106 closes the only ORDINARY route to a sparse array, so these are now reachable only through
  raw `managed.setLength` or opaque-generic code — which is why this is filed and not chased.
  **Re-measure trigger:** shv2 gaining `growFilled`/`truncate` (which would let the 5 specs re-enable and is the
  natural moment to widen the read surface), or any new route that can open a slot without going through `resize`.

- **#122 — `phase:parse` IS Θ(MUTABLE BINDINGS × CONSTRUCTS) ON THE REAL CORPUS, AND IT IS A RUNG RATHER THAN AN EDIT.**
  ⭐ **This is the corpus's parse superlinearity — NOT nesting depth**, and the distinction was measured, not reasoned.
  `parseWhileStatement` (`Parser.maxon:10925`) and `assignedBindingsIn` (`:11463`) each loop
  `for k in 0 upto self.mutableVars.count()` per construct, asking one membership question per binding.
  The corpus's `d_blocks.maxon` is ONE function with **518 mutable vars in scope**, so that is **414,400 tests at
  rung 2 and 26.5 MILLION at rung 5**. **Measured:** halving the O(depth × tokens) term (`4110c7d00`) left the corpus
  parse ratios UNCHANGED — ×1.91 ×2.14 ×2.39 ×2.75 ×3.03 against a baseline ×1.94 ×2.16 ×2.43 ×2.74 ×2.99 — which is
  what proves the depth term is not what this corpus spends its parse time on. ⚠ **AN ATTEMPTED FIX WAS A ×3.45
  REGRESSION AND WAS BACKED OUT:** a per-file `name → assignment-sites` index with binary search went flat on the
  depth ladder, passed every gate, and then measured ×3.45 at rung 5 — because it replaced a small per-construct set
  (one hash, one probe per test) with a structure whose per-test cost MULTIPLIED this same Θ(V×C). **A ladder-flat,
  corpus-worse change is a regression, and only running BOTH caught it.** The cure for this and for the remaining
  depth term is the same one thing: **carried sets computed BOTTOM-UP over the construct tree** — each construct's set
  is its own direct sites ∪ its children's memoized sets — making both the scan and the per-binding loop O(Σ|set|).
  **It is a RUNG because it changes the ORDER a phi set is built in, and the goldens pin that order** (`LoopPhiVar`'s
  header calls the ordering load-bearing). `matchingEndOf` from `4110c7d00` is the piece it builds on.
  **Re-measure trigger:** any corpus knob that raises mutable-vars-in-scope, or an inliner that fuses functions.

- **✅ #123 — THE `Map` PROBE TRAP — CLOSED 2026-07-26. Instance, class AND every named site are done; nothing is
  left as debt, and the five maps' verdicts were MEASURED rather than assumed.** The trap: `stdlib` `Map` is
  open-addressed with linear probing and `int.hash()` is the **identity**
  (`stdlib/PrimitiveExtensions.maxon:3-5`), so a key's home slot was `id and (capacity-1)`. Insert two contiguous id
  runs *interleaved* and the high run wraps onto the low run's slots, fusing them into one cluster every insert
  walks. **Measured** on the encoder's per-function block-offset map: probes per insert were **exactly
  `capacity/4 + 2/3`** (3.33 at cap 16 … 512.67 at cap 2048), collapsing to **1.00** the instant capacity first
  exceeded the id range — 171.44 probes/insert against 1.00 for the same keys sorted. Three things closed it:
  - **The INSTANCE** — `a78d17c35` made the encoder's table two pre-sized arrays (the key WAS the index).
  - **The CLASS** — ⚠ **the mixer this entry used to say was "DELIBERATELY NOT DONE" HAS BEEN DONE**, in
    `4def53ced`: `spreadHash` (MurmurHash3 `fmix32`, `stdlib/Interfaces.maxon:105`) now avalanches a hash at all six
    slot derivations in `Map`/`Set`, so **no key ORDER can cluster and the trap is unreachable for any key shape**.
    The iteration-order risk this entry declined it on was *measured*, not reasoned about, and is zero:
    `specs-shv2/fragments/` empty on two targets, the bootstrap's fragment diff byte-identical, all 16 `Map`/`Set`
    iteration sites swept, `verify-warm-rebuild` PASS.
  - **The SITES** — the two remaining `BlockId`-keyed maps became dense tables (2026-07-26).
    `SemanticCheck.buildBlockByIdMap` and `StdToWasm.BlockDispatchMap` are gone; both now read one
    `BlockIndexById` from `IrModule.blockIndexById` (the inverse of `func.blockRefs`), an instantiation of the new
    generic `BlockKeyedTable` (`Compiler/IR/`) that `BlockOffsetTable` also instantiates — **one array-pair-with-
    presence-column, not three copies**. −1,536 `semanticCheck` allocations (−68%) and −92% of its bytes on
    `Testing/ladders/genawait.sh 24 400`; −6,864 `encode` allocations on the wasm rung3 corpus. `scale-test` sees
    NEITHER (no `async` in the corpus, and `compileRung` always builds for the host) — its own Δ0 for them is the
    corpus blind spot, and the ladder is committed so the numbers can be re-run.
  ⭐ **AND THE MIXER DOES NOT MAKE A DENSE `Map` FINE — IT MAKES IT ~3× WORSE, WHICH IS WHY THE PER-SITE "TIDY
  O(n)" WAS STILL WORTH TAKING.** Replaying the real key sequences of a 2,808-function corpus through `Map`'s exact
  algorithm, both maps' keys arrive **perfectly ascending in 2,808/2,808 and 24/24 instances** — the interleaving
  run comes from the register allocator's critical-edge split, which neither site sees — and that is the case an
  identity hash gets perfect: **1.000 probes/insert, against 3.088 / 3.062 under `spreadHash`**.
  **The three `ValueId`-keyed maps were MEASURED AND LEFT, and each is a closure rather than a residual** — the
  trap needs an interleaved union of contiguous runs, not merely int keys, and replaying their real key sequences
  says none of them has one. In every case the arrival order costs **exactly what the SORTED order costs**, which
  is the definitive test, because the trap is order-induced:
  - `StdToWasm.DependentLhsMap` — 1,065 instances, 39,137 keys, largest 16,902. **Ascending in 1,065/1,065.**
    1.948 probes/insert under the identity hash, 1.948 sorted (3.212 under `spreadHash`, sparse keys).
  - `StdToX64Conversion.CmpPredMap` — 1,101 instances, 20,222 keys. Ascending in 1,098/1,101; the three
    exceptions cost nothing (2.583 arrival vs **2.583** sorted in aggregate; worst single instance 4.957, from
    SPARSITY, not clustering). It is also a genuinely sparse subset of the value space that supports `remove`, so
    a dense column would allocate per-function for a handful of entries.
  - `Parser.ValueInstanceAliasMap` — ascending in 2/2 instances, 2.203 arrival vs 2.203 sorted. The parser mints
    `ValueId`s monotonically in one forward pass and records the alias where the value is created, so ascending is
    structural. ⚠ **It is also ITERATED** (`Parser.maxon:6925`, feeding `recordFunctionRangeChecks`), so converting
    it would change an order that reaches a recorded artifact — a real hazard for no measured gain.
  **Nothing to re-measure**: the mixer makes the class unreachable regardless of key order, so an inliner giving
  Maxon-tier ids a second run can no longer trigger it, and the two sites that would have felt it most are arrays.

- **#106 — the COW detach is an unconditional call on the ARRAY HOT PATH (a constant factor, deliberately taken).**
  Every `push`/`reserve`/`resize`/`insert`/`append` now pays a `__arr_cow_detach` call that no-ops for an ordinary
  owned array, and `set`/`clear`/`remove`/`pop` pay it directly; a `push` onto a literal allocates and copies TWICE
  (detach, then grow). **Measured** (optimizer, 2026-07-22, 6-padding sweep): a candidate hoisting the guard to the
  call sites won **−18% on `reserve`** (spreads disjoint, faster 6/6) and −7.5% on `set`. **REJECTED, and the reason
  is the point:** a hoisted site guard tests `capacity < 0`, but a Slice-B shared HEAP buffer has `capacity >= 0`, so
  the site would skip the call and **Slice B's detach would never fire** — a slice-A optimization silently defeating
  the next slice. The unconditional placement is also what makes the gate structural rather than a checklist.
  **Re-measure trigger: after Slice B lands** (when the detach test has its second arm and the two questions are
  genuinely one), or if array-heavy code shows up in a profile. ⭐ **The better lever is SHRINK-WRAPPING** —
  12 of the 18 instructions on the detach's fast path are frame, and 6 of those (`push`/`pop` of `rbx`/`r12`/`r13`)
  serve only the cold arm; sinking callee-saved saves into the blocks that clobber them pays off across every emitted
  runtime function with a hot early-out (`__arr_get`/`__arr_set`, the `__str_*` and `__mm_*` families). ⚠ Not free:
  `X64GtRuntime.maxon:85-95` hard-couples `calleeSavedOrder` to `__gt_spawn`'s initial frame behind a live
  `panic("GT frame drift: …")`, so a variable frame shape must be reconciled with the green-thread context switch;
  frame pointers stay mandatory (`__gt_morestack`).
  ⚠⚠ **METHODOLOGY, and it invalidated this rung's first answer: emitted-code benchmarks here swing ±30% ON CODE
  LAYOUT ALONE.** The first A/B read "−16% push, −22% clone" and **both were noise** — proven by a `clone` CONTROL the
  change could not possibly affect, which still moved −22%. `emitForwardCopy` is a byte-at-a-time loop with 3 branches
  per byte, so it is acutely BTB/alignment-sensitive. **Require a padding sweep with disjoint spreads before believing
  any emitted-code delta, and never A/B on a benchmark dominated by that copy loop.**
- **#24(a) — `SplitLiveRanges` is O(blocks × K²)**, K = simultaneously-live owned values per block; measured
  K max 8, mean 0.16 over 2,445 fns ⇒ linear. (A separate quadratic — full liveness re-sweep per split —
  was already retired via `ReachCache`.) Re-measure trigger = an **INLINER** (shv2 has none).
- **scale-corpus catch-up — `RangeCheckEmitter` locates each guard site's block with `getBlockByIdIn` (`IrModule.maxon:361`), a LINEAR SCAN of `func.blockRefs`, called once per guard site (`InsertRangeChecks.maxon:329`)** ⇒ **O(sites × blocks) in TIME, allocating NOTHING**, so **no rung of this ladder could ever show it** — the manifest's own MEASURES-MEMORY-ONLY blind spot, stated by the instrument about itself. ⟲ **RE-MEASURABLE (O4, 2026-07-25): that sentence is no longer true** — see the tail of this entry. Surfaced because this rung took `phase:insertRangeChecks` from INERT to ACTIVE (the old corpus contained no narrow-range cast, only FULL-range aliases that `rangeIsFull` refuses to guard, so the pass did literally nothing — 72 allocs / 2,395 bytes flat at every rung, for the ladder's entire history). Independently re-measured by the review, which agreed. The phase is 0.066% of rung-7 bytes and its guard-SITE count is FIXED at one per rung, so the growing term is the per-function scan over `module.functions` (`InsertRangeChecks.maxon:83-86`), whose `project.rangeChecks.contains(func.name)` probe now hashes against a non-empty map. **RE-MEASURE TRIGGER: many narrow-range casts inside LARGE functions** — a machine-generated program, or a real one once ranged types are used densely. **CURE:** an id→block index on `IrFunction`, single-writer. ⚠ Note the shape it shares with the `requireInterfaceForParse` entry below: both are per-site linear scans over a per-function list, both allocation-free, and **both were therefore invisible to the only trend artifact this project keeps** — which was an argument for the runtime/time instrument that did not exist yet, not for a knob. ⟲ **RE-MEASURABLE (O4, 2026-07-25) — THAT INSTRUMENT NOW EXISTS.** `scale-test` carries a per-phase **CPU-TIME** column, so this entry's trigger can be **pulled on the ladder** rather than waited for: build the many-narrow-casts-in-large-functions corpus and read `phase:insertRangeChecks` in CPU ticks, where an O(sites × blocks) term shows as ×4 per doubling against the memory columns' ×2. **The measurement above is unchanged and still stands.** ⚠ Read it against the CPU column's noise band (a few percent): the ×2-vs-×4 question clears it by 100%, a constant-factor claim would not.
- **P1.7a 2b-ii — `Parser.requireInterfaceForParse` resolves a `where`-constraint interface by LINEAR SCAN over the current file's `artifact.interfaces` after missing the builtin table**, making witness dispatch **O(dispatch sites × interfaces declared in the same file)** — unchanged by 2b-ii, which merely measured it. ⚠ **TIME-ONLY and allocation-free, so `scale-test` STRUCTURALLY COULD NOT SEE IT** — the class the corpus manifest warns about in its own words (*"a cost that re-walks tokens or chases pointers but allocates little will read FLAT in these columns even when it is quadratic in time"*). Isolated with the hit's INDEX as the only variable (`Digest` declared last vs first — same program, same N interfaces, same N sites): **allocations identical at 1,417,523 both ways**, parse ratios ×2.52 vs ×2.19, and **+24.15 ms of a +27.05 ms whole-compile delta at N=1024**, everything else inside noise. **MEASURED LINEAR-IN-PRACTICE: max 17 interfaces in any single file** across `maxon-shv2`, `stdlib`, `maxon-selfhosted` and `specs-shv2`, and the scan only walks the CURRENT file's artifact ⇒ K ≤ 17, the `SplitLiveRanges` K² situation. **RE-MEASURE TRIGGER: a machine-generated source file, or any rung that lands many protocol interfaces in ONE artifact.** ⟲ **RE-MEASURABLE (O4, 2026-07-25) — AND THIS ENTRY IS THE CASE THAT ARGUED FOR THE COLUMN.** *"Allocations identical at 1,417,523 both ways"* against *"+24.15 ms"* is the exact shape a memory-only instrument reports as Δ0; `scale-test` now carries a per-phase **CPU-TIME** column, so the trigger is **pullable on the ladder** — put many interfaces in one artifact and read `phase:parse` in CPU ticks. **Every number above is unchanged and still stands**; K ≤ 17 across the whole tree remains the reason this is filed rather than fixed. **CURE:** an index beside `artifact.interfaces` with a single writer — which would also remove `builtinInterface`'s construct-on-every-lookup (it builds a fresh `IrInterface` per call, the same constructor-as-lookup shape as `builtinOrderingEnum`), folding in this file's existing "memoize `builtinInterface`" note. ⟲ **SECOND AXIS, MEASURED BY 2b-v's OPTIMIZER (2026-07-28) — same family, different dimension, same cure.** The entry above measures INTERFACES-PER-FILE (K ≤ 17). `IrInterface.interfaceMethodIndex` (`IrInterface.maxon:302`) is the METHODS-PER-INTERFACE twin: also a linear scan, also run once per witness-dispatch site, so witness dispatch is **O(sites × methods-on-that-interface)** as well. Isolated with the target method's POSITION as the only variable (same program, moved first→last): `phase:parse` **+13.7% at M=D=256, +39.3% at 512, +36.2% at 1024** (min of 5). ⚠ **THE OPTIMIZER EXPLICITLY DECLINED TO CLAIM THE EXPONENT** and was right to: the isolated delta grew ×5.52 then ×1.90 where a clean D×M term predicts ×4.00, so the measurement shows the term EXISTS and cannot show its SHAPE. **FILED, NOT FIXED, on a coordinator-verified trigger: the widest interface anywhere in the tree is FOUR methods** (`stdlib/Interfaces.maxon`'s `Hashable` has one; the widest in `specs-shv2` is 4), and the effect needs a few hundred — ~64× beyond anything that exists, so it is not triggerable on realistic input. **RE-MEASURE TRIGGER: an interface wider than a few dozen methods** (a machine-generated one, or a protocol that grows). **CURE: index `IrInterface.methods` by name** — the same single-writer index the axis above wants, which is why both belong to ONE entry rather than two.
- **P1.7 deep-clone — `noteClonerUsage` needs-fixpoint is O(passes × total-types)**, passes = depth of the
  deepest cloner-dependency chain (`ArrayRuntime.maxon`, 3b-vi-b-α). Measured LINEAR on realistic input
  (optimizer, 2026-07-23, hand-built managed Corpus A — scale-test's corpus is managed-free so it CANNOT see
  this: ~5015 allocs per added managed type at nesting depth D≈1); bends to superlinear only when deep-clone
  NESTING depth scales with program size (Corpus B, a 256-deep managed struct chain: `unattributed` allocs
  ×6.44 per doubling). A structural DUAL of the pre-existing destructor-needs fixpoint `closeDestructorNeeds`
  (`MmRuntime.maxon`), which carries the IDENTICAL quadratic (measured 175,415 unattributed at D=256 with the
  clone removed — so this is not a regression the rung introduced, it inherited the destructor's shape).
  Re-measure trigger = a machine-generated deeply-nested managed type that gets deep-cloned. Fix, if ever
  warranted = ONE shared worklist helper for BOTH the destructor and cloner needs-closures (O(types+edges)),
  which single-sources the dual too — NOT a clone-only fork.
  ⚠ **NOT an O4 case, and the distinction matters: this one is ALLOCATION-shaped, and its blind spot is the
  CORPUS** (managed-free), not the memory-only column. The CPU-time column does not cure it; corpus coverage
  does. ✅ **And it is CLOSED** — re-measured at the trigger it names (cloner **×2.75 → ×3.82**, destructor
  **×2.94 → ×3.83** at D=64→512, so *"bends only on machine-generated 256-deep chains"* was wrong), and the
  ONE shared worklist this entry prescribed shipped as `Compiler/Runtime/CascadeNeeds.maxon` (`4c4524b45`,
  closing note `e306c562a`; after: ×1.93 / ×1.96). Detail lives in the P1.7 status row; kept here for the
  measurement and the lesson.
- **P1.7 opaque-copy — the front-end per-instantiation GATES scan ALL generic instances per call-site.**
  `genericTypeFieldHasUncopyableInstantiation` (copy-reject gate, `SignatureIndex.maxon:3098`, 3b-vi-b-β) and
  its shipped twin `genericTypeFieldHasManagedInstantiation` (opaque-field-reassign drop gate, `:3073`, Finding
  A) each `for id in 0 upto genericInstances.count()` base-name `.equals` per call-site → **O(sites × total-
  instances)**. Measured TIME-only (optimizer, 2026-07-23, managed-opaque corpus: copied-vs-nocopy `parse` grows
  ×2.5/doubling, quadratic ≈40–75 ns/K²) — but `nameOf` borrows and `.equals` doesn't allocate, so it was
  **INVISIBLE to scale-test** (allocsΔ0). ⟲ **RE-MEASURABLE (O4, 2026-07-25):** the per-phase CPU-TIME column
  sees exactly this shape, so the trigger below is pullable on the ladder. ⚠ **And this entry's own verdict was
  re-measured and OVERTURNED** — see the P1.7 status row: *"linear-in-practice (0 corpus hits)"* was the
  instrument's blind spot, the gates measured **×2.36 → ×3.59 converging on ×4**, and the shared by-base-id index
  this entry prescribed **SHIPPED** (`4c4524b45`, closing note `e306c562a`). Kept here for the measurement and
  the lesson. Linear-in-practice: **0 occurrences** in the scale corpus or shv2's own
  source; sub-ms at realistic K (tens of generic copy/reassign-container types); bends visibly only at K in the
  hundreds (~32 ms at K=800, machine-generated). Re-measure trigger = a program with hundreds of generic types
  that copy/reassign opaque type-parameter fields. Fix = a shared **by-base-name index** on the generic-instance
  registry benefiting BOTH gates (do NOT fork one gate — the twin is a shipped path).
- **#28 — the corpus hardcodes struct F=2**, hiding `StructLayout.indexOfField`'s O(F²) scan; measured F
  max 22 mean 4.14 over 166 real type decls ⇒ linear (programs add TYPES, not width). Re-measure needs a
  field-count corpus knob (a machine-generated wide type).
- **#32 — the E2027 duplicate-pattern check is O(P²)** in one match's hand-written arm count P; whole-program
  linear for bounded arms, quadratic only in a degenerate single-giant-match. Kept deliberately (a `Set` = a
  heap object per match to save a bounded scan).
- **#30 — the corpus is BLIND to instance methods AND `match`** (no knob emits either), so the optimizer
  measured ZERO allocation there; both paths are O(1)/O(fields) by inspection. Fold a method knob + a match
  knob in together.
- **#24(b) — post-pipeline runtime installs are billed to NO phase** (`installMmRuntime`/`installGtRuntime`/
  `installStringRuntime`/the destructor cascades run after `pipeline.run()`, outside every `PhaseProbe`), so
  phases don't sum to total; a constant ~316-alloc delta shows as `unattributed`. Constant, not superlinear.
- **#76 — the 14-register x64 pool refuses a 16-live-value interpolation-in-a-loop body (E5001)**, which
  restricts `capturing-closure-bound-outside-loop-called-inside` to wasm. Proven closure-INDEPENDENT (the
  same body with a plain `let r = i + 5` E5001s identically) — the "refuse the search" thesis firing, not a
  bug; x64 env-drop timing is covered by the `-rebound-in-loop`/`-called-from-nested-block` siblings.

---

## Beyond the two phases

**Broaden:** general `Iterable` + associated types · `List`/Json/… · ~~arm64~~ (macOS **scalar core DONE 2026-07-18** — see the Targets row; the arm64 **runtime floor** is its next rung) + wasm (**scalar+floats+globals+heap/String/print/structs/unions+ERROR-HANDLING DONE 2026-07-19** — WASI Preview2 component, **732/0** = the host's full runnable set minus technical restrictions (register/RequiredData); only Arrays/generics + async + a codegen relooper remain) · coverage ·
inliner. *(Two things LEFT this list on 2026-07-13. **`async`/green threads** are core, at P1.5.
**Porting the spec suite** is no longer an endgame chore — it is **Workstream S**, the driver of
every rung, starting at P1.0b.)*

**Cross-target goldens still owed** — all blocked on a CAPABLE HOST (this Windows box can *emit* for these
targets but cannot *run* their suites): **#91** the `does not throw'` stray-quote message typo
(`Queries.maxon`, pinned by all four fragment trees — fix + `--update-required` on a capable host); **#86**
the P1.9 arm64-macos/arm64-linux goldens — ⚠ **the x64-linux half is DONE and this entry was the ONLY
place that said so** while P1.9's own row and the index still called x64-linux owed, which is how the
rung sat at ◑ for eight days on a residual that had already closed; **the arm64 half GREW on 2026-07-29**,
since P1.9's declaration-site rung moved codegen for 33 tests and only the lane that emits those goldens
can mint them; **#67** un-restrict the A1
function-value + A2a closure spec cases to arm64 (the ADRP+ADD codegen has landed and is disasm-verified —
this needs an arm64 RUNNER); **#20** the arm64/wasm compile-error fragments a Windows host structurally
cannot regenerate (it cross-compiles then tries to EXECUTE).

> ### ⭐ THE arm64-macos GOLDEN DEBT IS NOW **QUANTIFIED**, MEASURED ON AN arm64-macOS HOST, 2026-07-30
>
> This section's premise — *"blocked on a CAPABLE HOST"* — is written from the Windows box. **Run ON a
> Mac, the arm64-macos lane is the HOST lane and the debt is directly measurable.** On clean `main`
> (`913f99e12`), with both compilers freshly built (0 warnings): **`specs-shv2` reads `2370 passed,
> 62 failed`, exit 1** — and **all 62 are `specs-shv2/fragments/arm64-macos/` golden mismatches**, i.e.
> exactly the accumulated `#86`-family debt, not a regression. By spec: `type-name-collision` 12 ·
> `where-clauses` 11 · `union-managed-payload` 8 · `witness-throws` 6 · `generic-types` 5 ·
> `ranged-typealias` 4 · `retain-escaping-borrow` 3 · then 11 specs with 1–2 each.
>
> ⚠⚠ **THE CONSEQUENCE FOR THIS PROCESS, AND IT IS NOT SMALL: `/rung` STEP 1 CANNOT GET A GREEN BASELINE
> ON A MAC.** The skill requires one and forbids *"turning a gate green by narrowing what it tests"*, so
> **every rung attempted from an arm64-macOS host halts at step 1** until these 62 are minted. The
> batching rule that produced this ("arm64 is remote, synced periodically") is correct *from Windows* and
> **inverts on a Mac**, where arm64-macos is native and x64-windows is the lane that cannot execute.
> ⇒ **Minting these is a deliberate stale-golden sweep (skill step 13), owed BEFORE the next Mac-hosted
> rung — not something to fold into an unrelated slice.**
>
> ### ✅ THE SWEEP RAN — 2026-07-30, `01ca36bd0` (user-directed). **62 red → 2**, and the 2 are NOT goldens.
>
> Minted from **ONE FULL UNFILTERED run** (a filtered run's fragments are not authoritative — literal-pool
> indices depend on which tests share the batch, and 20 fragments the oracle probes had touched were
> reverted for exactly that reason). It touches `specs-shv2/fragments/arm64-macos/` **and nothing else**:
> no other target's goldens, no source. **60 stale re-minted + 152 absent added**, and the arithmetic
> closes — 62 failures = **60 golden staleness + 2 not-goldens**.
>
> **The 60 diffs were justified, not rubber-stamped** (skill step 9: an `M` on a pre-existing fragment is a
> codegen change to justify or fix). All 60 lag behind work already reviewed on the x64 lanes: 32 move real
> arm64 instructions from **P1.9's runtime range checks** (`__rc_chk`/`__rc_panic`/`__rc_ok` blocks, plus
> `fsub`→`fcmp` and `add`→`cmp` on the float/unsigned bound checks), 8 are **P1.9's E3062** unused-typealias
> diagnostic text, and `union-managed-payload`'s **source itself** changed per **D1c** (`text(_)` rewritten
> to a partial discard) — which D1c's own row had already recorded as moving 4 goldens. Nothing arm64-novel.
>
> ⛔→✅ **THE RESIDUAL 2 WERE A REACHABLE MEMORY-CORRUPTION BUG, NOT A GOLDEN — AND IT IS NOW FIXED**
> (`4572c988b`, same session). A pre-existing arm64-macOS `mm_decref: refcount underflow` in shv2's
> `__maxon_global_cleanup`, on the one-line program `typealias Bad = int(i8.min to i32.max)`.
> `--update-required` could not touch it: expected and actual diagnostics are byte-identical, so the test
> failed on the crash text appended AFTER them. Root cause was a `match`-arm payload binding emitting a
> re-assignment's release-of-old against a slot never written on that path — see the ✅ entry on the
> "Bootstrap oracle bugs" list, which also records why it had been filed as an unowned `TargetOp` shape
> problem. ⇒ **a Mac-hosted `/rung` step 1 now reads `2456 passed, 0 failed`** — the lane is GREEN, and
> this host no longer needs a documented-exception baseline.
>
> ⚠⚠ **AND THE STALE 62 ARE THE SMALLER HALF. THE SAME RUN MINTED `130` GOLDENS THAT WERE SIMPLY
> ABSENT, plus `22` whole spec directories, across `42` DISTINCT SPECS** — among them
> `enum-union-method-receiver` (D1's feature), `compiler-directives` (D5's), `enum-full`, `arrays`,
> `character-set` and `function-overloads`. **A MISSING golden never fails**, so the arm64-macos lane has
> been reporting PASS over ~42 specs' worth of codegen it never compared. That is the exact shape D1's own
> row recorded for x64-linux (*"x64-linux ran GREEN over none of the feature"*, 51 goldens, `f4c1c43e5`)
> — **it was never fixed for arm64-macos, and it is now measured at 130 + 22.** ⇒ the sweep is
> **62 stale + 130 absent**, and the absent half is invisible to every gate that exists.



**Budgets:** **≤30 s / ≤1.7 GB / >90% CPU** on self-compile. Runtime multi-core is already proven
(Track 0). **Caching is revisited here, on a working compiler, with the approach chosen fresh** —
not ported from v1's `.mxc`.

---

## Closed — do not reopen

- **`selfhost-distance` (the compass)** — ❌ **CUT 2026-07-13.** It was sold as "the ranked
  TOP-UNSUPPORTED table IS the roadmap," but the roadmap is ordered by *reasons* a frequency table
  cannot discover (dictionary-passing forces the descriptor drop path before `Array`; `String` is
  a runtime-gated builtin). **We know what we need to implement.** Its real price, re-measured: a
  parser recovery mode + making `panic()` recoverable at **626** sites (the plan guessed 148, and
  projected ~600 only by *mid*-ladder; we hit it at the *end of the scalar core*). It buys a
  number, not a feature. `SelfhostDistance.maxon` was never written — the cut is docs-only.
  **The Phase 1 gate is the mitigation:** it answers "does shv2 compile real Maxon?" on 704 lines
  instead of 21k, which is most of what the compass promised for none of its cost.
- **The `panic()` audit** and **parser recovery mode** — VOID with the compass. shv2 needs neither.
- **The `spec-test` runner** — ✅ **SHIPPED** (`72ffe87a9`). `Compiler.maxon` grew `compileSource()`
  + `CompileOutcome` (returns `project.diagnostics` instead of printing them — `Diagnostic.render()`
  already emits the exact `error EXXXX:` wire format `maxoncstderr` compares against). **Do NOT
  port v1's 7,699-line harness.**
- **Rewriting shv2's core-violating sites** — VOID. Closures and conditional conformance are *in
  core*; the sites are the tests.

### Traps that survive (each is a silent miscompile, not an error)

| # | Trap | Consequence |
|---|---|---|
| 1 | **The stdlib dir's LEAF NAME *is* the namespace** ([2-Parser.cs:804](maxon-sharp/Compiler/2-Parser.cs#L804)); `"stdlib."` hardcoded in [MonomorphizationPass.cs:754](maxon-sharp/Compiler/MLIR/Passes/MonomorphizationPass.cs#L754) | `stdlib-shv2` as a leaf ⇒ monomorphization silently misses every call site. **Path must be `stdlib-shv2/stdlib/`** |
| 2 | UCD `.bin` files resolve from `<stdlib>/helpers/string/` ([MaxonToStandardConversion.cs:2629](maxon-sharp/Compiler/MLIR/Conversion/MaxonToStandardConversion.cs#L2629)) | A pruned fork without `ucd_bmp.bin`/`ucd_supp.bin` ⇒ hard throw mid-lowering |
| 3 | `_cachedSources`/`_cachedStdlibModule` are **process-static** ([0-Compiler.cs:803](maxon-sharp/Compiler/0-Compiler.cs#L803)) | Use an **env var, not a per-invocation flag** — the C# TestRunner batches many compiles per process and would serve whichever stdlib loaded first |
| 4 | `__Managed*Error` case ordinals must match `Builtins.maxon` **exactly** ([2-Parser.cs:1116](maxon-sharp/Compiler/2-Parser.cs#L1116)) | Reordering a case re-points a builtin's `throwsType` at the wrong ordinal |
| 5 | `ExitCode` lives in **`Process.maxon`** | Drop it from the fork and `main() returns ExitCode` won't resolve |
| 6 | Spec fragments need a **trailing newline** | Every spec test dies with a lexer EOF error |
| 7 | *(inverse)* `BuildCache` is **already** stdlib-path-keyed ([BuildCache.cs:52](maxon-sharp/BuildCache.cs#L52)) | Don't "fix" it. But never set `MAXON_STDLIB` globally — v1's own `findStdlibPath()` ignores it and the two compilers would diverge |
| 8 | **A compiler-SYNTHESIZED type that gains a managed field must be named in `managedNameDropCallee`, and the arm is a NAME EQUALITY** (shv2; filed by P1.8 Slice D's review, 2026-07-28). `installStructDestructors` walks `project.structTypes`, so a synthesized type is in no file's declarations and the generic route mints `__destruct___<Name>` — a symbol nothing builds. Today exactly ONE hand-written arm exists (`__CharacterSet`), and `__StringIndex` needs none because it is scalar-only, so this is **not yet duplication** and the abstraction was deliberately not built on speculation | The next synthesized type to acquire a managed field **dies in the BACKEND** — `panic at X64Backend.maxon:1820: resolveCallFixups: call to unknown function '__destruct___<Name>'` — a panic, not a diagnostic. Sabotage-confirmed: removing the existing arm gives **40 red, all that panic**; replacing it with the generic `__mm_decref` gives **40 red, all exit 101** (a leak). ⇒ **When a SECOND such type appears, replace both arms with ONE predicate** ("a reserved `__`-prefixed layout with a managed field must name its compiler-owned drop"), which turns a backend panic into a compile-time assertion |

### Bootstrap (`maxon-sharp`) oracle bugs — silent wrong answers to fix when the subsystem is next touched

- **🔴 `__ManagedFile` IN A STANDALONE MODULE CRASHES THE BOOTSTRAP — `E9001 … Index was out of range`
  in `MaxonToStandardConversion.Run`.** Found 2026-07-30 while surveying **R4.1**, whose acceptance
  specs these are. **The `/specs` suite is GREEN over it (11/11) because the runner BATCHES** — its own
  log reads *"Batching: 8 test(s) in 1 batch(es), 3 test(s) per-fragment"*, and the 8 batched cases are
  the ones that use `__ManagedFile`. Compile any one of them **alone** and the bootstrap dies.
  **2-line repro:**
  ```maxon
  function main() returns ExitCode
  	let e = __ManagedFile.exists("nope.txt".toByteArray().managed)
  	return e
  end 'main'
  ```
  ⚠ **The narrowing matters, because it locates the bug away from the obvious suspect:**
  `"abc".toByteArray().managed` **on its own compiles fine** (it reaches `E3012 unused variable`), so
  `.managed` and `__ManagedMemory` are NOT implicated — it is specifically a `__ManagedFile.*` call
  reaching the Maxon→Standard lowering in a module that contains nothing else.
  ⛔⛔ **THAT LAST NARROWING IS WRONG, AND IT WAS THE DIAGNOSIS RATHER THAN THE OBSERVATION THAT AGED**
  (re-measured 2026-08-01, BATCH2 slice 5). `__ManagedMemory` **IS** implicated, with no `__ManagedFile`
  anywhere in the program: a **MUTATING** buffer method in a standalone module reaches the identical
  crash site. Both of these die with the same `E9001 … Index was out of range` in
  `MaxonToStandardConversion.Run`:
  ```maxon
  function main() returns ExitCode
  	let mm = try __ManagedMemory.create(3, elementSize: 1) otherwise return 1
  	try mm.setLength(2) otherwise return 2        // ← crashes; `setByte(0, value: 65)` crashes too
  	return mm.length() as ExitCode
  end 'main'
  ```
  ⭐ **The line the observation supports is CREATE-vs-MUTATE, not `__ManagedFile`-vs-everything-else:**
  `__ManagedMemory.create(…)` **alone** compiles and runs clean (measured — `create` + `ByteArray.init(mm)`
  + `count()` builds and exits 3), and adding EITHER `setLength` or `setByte` crashes it. The original
  probe missed this only because its exonerating example (`"abc".toByteArray().managed`) never mutates.
  ⇒ **shv2 has no oracle for any standalone program that WRITES a buffer** — which is why slice 5's
  `Array.init` semantics rest on the reference SOURCE (`stdlib/Array.maxon:113`) and on `File.maxon`'s
  own use, not on a differential run. ⚠ Found while attributing a *different* suspected bootstrap bug to
  `ByteArray.init(mm)`; that attribution was **measured FALSE** — `init` builds fine under the bootstrap —
  so the crash was never about the static this rung added. *Separate the observation from the diagnosis.*
  ⚠⚠ **CONSEQUENCE FOR R4.1, and it is a process cost rather than a defect in shv2: THE ORACLE CANNOT
  BE PROBED AD HOC FOR THIS ENTIRE SUBSYSTEM.** Every other rung has used `run_program compiler=csharp`
  to ask *"what should this actually DO?"*; for `__ManagedFile` that route answers with a crash, so the
  only oracle available is `spec_test_outcome` over the committed cases — which can confirm the 11
  cases the corpus already has and **cannot answer a question the corpus does not already ask.**
  Same E9001 family as the `return self.method(...)` tail-return crash filed at P1.7 Slice 1.

- **✅✅ FIXED 2026-07-30 (`4572c988b`) — arm64-macOS REFCOUNT UNDERFLOW IN shv2's GLOBAL TEARDOWN.**
  ⭐⭐ **ROOT CAUSE: A `match`-ARM PAYLOAD BINDING RELEASED A VALUE THE SLOT NEVER HELD.** The binding
  emitted the ordinary RE-ASSIGNMENT sequence — load the destination, release its old value, store,
  incref — where a binding is a **DEFINING** store. The guard meant to prevent that asked
  `varNameToStructType`, a flat map populated in **BLOCK-WALK ORDER with no model of control flow**, so
  the first arm to bind a name registered it and every **LATER** arm binding that same name failed the
  skip test — though the arms are **MUTUALLY EXCLUSIVE**, so at runtime that store is still the first
  into an uninitialized slot. `EmitDecrefValueIfNonnull` guards NULL, not garbage. One condition in
  `MaxonToStandardConversion.cs`: a declaration never releases a previous value, and that fact may not
  be decided by walk order. Each arm already releases its own bindings at arm end.
  **Corpus effect: 187 spurious `mm_decref` CALLS removed from 139 programs**, the now-dead zero-init
  from 296 more, **ZERO programs gain one**; 296 frames shrank a slot and shv2's binary lost 37 KB.
  **GATES (merged tree): C# 3187/0 · shv2 2456/0 no leaks · `specs/` churn re-runs BYTE-IDENTICAL ·
  `specs-shv2/fragments` ZERO modified.**
  ⚠ **THE REGRESSION PIN IS `memory-safety/array-push-struct-incref`'s `RequiredIR` BLOCK** — it was the
  single C# failure before the fix and passes after, and it pins the ABSENCE of the release-of-old.
  ⚠⚠ **A BESPOKE MINIMAL TEST IS NOT POSSIBLE, and that is worth knowing before anyone tries:** the
  manifestation depends on what the uninitialized slot happens to hold, so a small program reads **0**
  there and passes **on the BROKEN compiler** (verified by reverting the fix and re-running). Only a deep
  call stack leaves a live pointer at that offset, which is why it took the compiler compiling itself.
  ⚠ **TWO WRONG TURNS, recorded because each would cost the next reader the same time.** (1) The full
  `specs-shv2` suite takes **~7 MINUTES on an arm64-macOS host**, not the **17 s** this repo's CLAUDE.md
  quotes from the Windows box — and the runner BUFFERS every result to print in fixed order, so a healthy
  slow run has an EMPTY log and stacks showing the parent blocked in `kevent` with all 12 workers in
  `nanosleep`. That is **indistinguishable from a deadlock** and three healthy runs were killed as hung.
  **The check that settles it in 8 s: are the child PIDs churning?** (2) Disabling
  `RefcountOptimizationPass` changed nothing, from which the extra decref was wrongly localized to the
  target-AWARE lowering; reading the Standard IR showed the throw site AND the destructor both correct,
  and only the **debug stream** (`--debugstream` + `maxon monitor`, which unlike `--mm-trace` does NOT
  perturb it) named the victims — `decref String #166`/`#168`, two live `KeywordInfo.helpText` Strings,
  each immediately before a payload incref.
  *(Original filing follows, kept for the evidence trail.)*
- **~~🔴🔴 arm64-macOS: A REACHABLE REFCOUNT UNDERFLOW IN shv2's GLOBAL TEARDOWN~~ — 2 SUITE FAILURES.** Found 2026-07-30 by the
  arm64-macos golden sweep (`01ca36bd0`), which took the lane 62 red → **2**; these are the 2, and
  `--update-required` **cannot** touch them because the expected and actual diagnostics are
  **byte-identical** — the test fails on the crash text appended after them.
  **4-line repro, and the whole file is it:**
  ```maxon
  typealias Bad = int(i8.min to i32.max)
  ```
  ⇒ the correct `error E3005: … Mismatched type bounds: 'i8.min' and 'i32.max' …`, then:
  ```
  mm_decref: refcount underflow (already zero)
    in mm_decref / __destruct_KeywordInfo / mm_decref / mm_decref_managed_elements
    in __destruct_Array_KeywordInfo / mm_decref / __destruct___Map_ByteArray_KeywordInfo
    in mm_decref / __maxon_global_cleanup / in mrt_start
  ```
  **PRE-EXISTING** — identical 2 occurrences in a baseline run of clean `main` before the sweep — and
  **arm64-macOS-specific**: the x64 lanes are green over `ranged-typealias` upstream (P1.9 closed at
  2352/0), and a double-decref would fire on every target, so the divergence is in the **bootstrap's
  arm64 emission of shv2's own source**. ⇒ **it is a `maxon-sharp` rung, gated by the full C# suite.**
  **⭐⭐ IT IS THE SAME DEFECT AS THE FILED-AND-UNOWNED `TargetOp` LANDMINE ABOVE** — identical signature
  (`refcount underflow` inside a generated union destructor, in TEARDOWN, after a correct binary is
  written), reached through `ParseError` instead. That filing said *"a synthetic 112-case union does not
  reproduce it; something about THIS union's shape"* and was left unowned with no cheap repro. **This entry
  supplies the repro: one line, deterministic, 11 s to rebuild and test.** `ParseError` has **101**
  payload-carrying cases and the failing one is **#98** — so the "111" figure was `TargetOp`-specific and
  is NOT the threshold.

  **⭐ THE ONE POSITIVE DISCRIMINATOR, and it is the lead: THE THROWN CASE MUST CARRY A MANAGED PAYLOAD.**
  Reducing `mismatchedRangeBoundTypes` to `(line, column)` — no `String` at all — makes the underflow
  **GO AWAY** (verified on a build that succeeded; an earlier attempt at this experiment reported a false
  "fixed" from a build that had failed, so check the binary exists). Its sibling
  `unrepresentableIntegerRange(low ParsedInt, …)` — thrown from the SAME function on
  `int(-1 to u64.max)` — carries a **scalar** and is clean, which is the same fact from the other side.

  **NINE hypotheses RULED OUT by experiment, each a rebuild-and-run (do not re-derive these):**
  1. **NOT `spelling()` or the argument computation.** Stubbing `spelling()` to a literal still underflows;
     so does passing two bare string literals to the throw.
  2. **NOT the payloads' USE.** Rewriting the report arm so it never interpolates `{low}`/`{high}` still
     underflows — the payloads are never read and it still fires.
  3. **NOT the payload COUNT.** Cutting the case from two `String`s to one still underflows. And three
     other two-`String` cases (`unexpectedToken`, `returnTypeMismatch`, `comparisonTypeMismatch`) are clean.
  4. **NOT the ternary** `MaxBoundField if self.isMax else MinBoundField` — rewriting it as `if`/`else`
     changes nothing.
  5. **NOT two owned temporaries in one argument list.** Hoisting both `spelling()` calls into `let`s first
     still underflows.
  6. **NOT the case's POSITION in the union.** Moving it from #98 to #1 changes nothing.
  7. **NOT the `qualifier` borrow.** `ParsedRangeBound` stores `qualifier: typeTok.value` — a direct
     reference to the token's `ByteArray` — but `.clone()`ing it at both construction sites changes nothing.
  8. **NOT frame depth / the unwind through a nested call.** Moving the check and throw up into the caller
     (`readTypeAliasDeclaration`, the same frame the CLEAN `bareSizedTypeAlias` throws from) still underflows.
  9. **NOT `RefcountOptimizationPass`.** Disabling it outright in `3-MlirPipeline.cs` still underflows ⇒
     the extra decref is emitted by **`MaxonToStandardConversion.Run(module, target)`**, the one stage that
     is target-AWARE; every pass after it (`StoreForwarding`/`DSE`/`ParameterRetention`/refcount) takes only
     `stdModule`. **That is where to look.**

  ⚠ **AND IT IS A HEISENBUG: `--mm-trace` HIDES IT COMPLETELY** (169,960 trace lines, zero underflows,
  clean teardown), while `--mm-debug` reproduces it with **no magic/canary/poison violation** — the box
  header is intact and the refcount is honestly 0. So the object is over-decref'd rather than
  used-after-free, and any instrumentation that wraps decrefs perturbs it away. **Do not expect a trace to
  find this; bisect the emitted code.**
  ⚠ **`helpText` is READ NOWHERE** (`Lexer.maxon:1310` declares, `:1313` assigns, no third mention), so the
  global `keywordMap` teardown is only where it SURFACES — the corrupted object is reached by an unrelated
  over-decref, not by anything that touches keyword help.
  ⚠ A minimal hand-written program reproducing the *shape* (global `var` map of structs-with-`String`,
  global `b"…"` `let`s, ternary over them, `String.from`, interpolation, a 2-`String` boxed-union payload
  thrown/caught/matched) **runs clean under BOTH compilers** — this could not be minimized from the outside.

  **Anyone touching the bootstrap's arm64 emitter or
  shv2's `ParsedRangeBound`/`spelling()` should start here — the repro is one line.**

- **⬜ `E9001` ON LEGAL MAXON: ANY INSTANCE METHOD ON A `__ManagedMemory` OBTAINED FROM THE STATIC
  `__ManagedMemory.create` FACTORY.** Found 2026-07-30 by the R4 survey, probing whether the corpus's
  file-IO specs are portable. **4-line repro, and the whole file is it:**
  ```maxon
  function main() returns ExitCode
  	var m = try __ManagedMemory.create(8, 1) otherwise panic("alloc")
  	try m.setLength(4) otherwise panic("len")
  	return 0
  end 'main'
  ```
  ⇒ `error E9001: Lowering function 'main' failed: Index was out of range. Must be non-negative and less
  than the size of the collection. (Parameter 'index')` at
  `MaxonSharp.Compiler.Ir.Conversion.MaxonToStandardConversion.Run`. **It is the RECEIVER's provenance,
  not the method**: `setLength`, `setByte` and `length` all ICE identically, while the static `create`
  call ALONE compiles and runs, and a `__ManagedMemory` handed straight to `__ManagedFile.openWrite` /
  `.write` compiles fine. Independent of the `otherwise` form (inline `panic` and the labelled-block form
  both ICE) and of whether the index/capacity is a literal or a runtime value — so it is **not** the
  argument-pinning family the entries below describe, despite also surfacing as E9001.
  ⭐⭐ **AND THE SUITE IS GREEN OVER IT, STRUCTURALLY: `specs/managed-memory-methods.md` passes 29/29 and
  `specs/managed-file.md` 11/11, because every one of those 40 cases reaches its buffer through
  `arr.managed` — an `Array`'s field — and NOT ONE of them calls the static `__ManagedMemory.create`
  factory that the documentation block at `specs/managed-memory-methods.md:33-36` nonetheless
  advertises.** A documented public factory whose result is unusable, behind a green 40-case suite: this
  is *"a green suite is proof no COMMITTED TEST fails, never proof the feature works"* in its purest
  form. ⚠ `stdlib/File.maxon:71,78` writes exactly this shape (`create` then `setLength`) and the stdlib
  compiles — so the trigger boundary is narrower still and is the one thing this filing does NOT know.
  **Whoever fixes it: add a case that goes through the FACTORY, or the next survey re-finds this.**

- **⬜ `E9001` ON LEGAL MAXON: a `try … otherwise …` INSIDE AN ARGUMENT LIST.** Found 2026-07-29 by the
  ranged-bounds rung, which had to write around it. `assign value %N (kind=MaxonInteger) not in
  valueMap; assigning to '__arg_pin_12'`. This is the SAME argument-pinning pass whose block-splitting
  fragility `specs/range-check-panic.md` already documents normatively as the reason the bootstrap does
  not emit a runtime range check at a call argument — so the two are one defect seen from two sides, and
  fixing the pinning pass would retire the spec's documented gap as well.

- **⬜ `E9001` ON LEGAL MAXON: a TERNARY AS A LABELLED ARGUMENT, in a call with enough arguments to
  pin.** Found 2026-07-29 by the D2 rung, which hit it writing shv2 spec code and worked around it with
  a `let`. `Lowering function 'main' failed: assign value %55 (kind=MaxonInteger) not in valueMap;
  assigning to '__arg_pin_2'`. **Isolated repro: an 8-argument static factory reproduces it; a
  2-argument one does not**, and hoisting the ternary to a `let` compiles and runs. ⚠ This is the
  **same argument-pinning pass** as the two `E9001` entries below — a third face of one defect, and the
  argument count is the discriminator, which is the most specific handle anyone has had on it yet.

- **⬜ `#if` IS REFUSED INSIDE AN `enum` OR `union` BODY, WHICH `specs/compiler-directives.md:31-34`
  EXPLICITLY PERMITS.** Found 2026-07-29 by the D5 rung, oracle-probed. `E2010 Expected identifier but
  got '#if'` at the directive. The spec doc lists `type`, `enum`, `union`, `interface` and `extension`
  bodies as legal positions; the bootstrap accepts `type` and `interface` and refuses the other two.
  **v1 wires all of them** (`Parser.maxon:5768`, `:5975`), so the doc and v1 agree against the
  bootstrap. **shv2 follows the doc** — in shv2 `enum` and `union` share one member loop, so accepting
  is a single arm and *refusing* would have cost more code than accepting. No spec asserts the
  refusal, so nothing depends on it.

- **⬜ PARENTHESES ARE REFUSED IN A `#if` CONDITION, WHICH `specs/compiler-directives.md:27`
  EXPLICITLY DOCUMENTS.** Found 2026-07-29 by the D5 rung. Even `#if (testing(false))` — a single
  parenthesised atom — is `E2010 Expected identifier but got '('`. v1 implements grouping
  (`Parser.maxon:17889-17893`) and the doc names it under "Boolean operators", so this is the same
  shape as the entry above: **doc + v1 against the bootstrap**, and shv2 follows the doc. ⚠ Two
  independent directive gaps in the bootstrap found by one rung, both in positions/grammar the doc
  states normatively — **the bootstrap's directive support is narrower than the spec it is canonical
  for**, which is worth knowing before trusting it as the oracle on this feature again.

- **⬜ `E9001` ON LEGAL MAXON: a `while` INSIDE AN `if` BRANCH THAT ALSO REASSIGNS AN ENCLOSING `var`.**
  Found 2026-07-29 by the ranged-bounds rung. *"reads %N, which is defined in block 'firstAlias_7' — and
  'firstAlias_7' does not dominate 'firstAlias_7.merge'"*. Workaround used: hoist the loop into a free
  function. shv2 compiles the same shape, so this is bootstrap-only.

The bootstrap is the runnable `/specs` oracle, so a silent wrong answer in it erodes what shv2 is checked
against (user has ruled before: *fix the bootstrap so it stays a solid reference* — cf. the throw-ownership
work). **shv2 does NOT inherit these** (it diverges deliberately, usually stricter); each needs the full C#
suite as its gate:

- **◑⭐⭐ A RANGED TYPE'S BOUNDS WERE NEVER ENFORCED AT A CALL OR A FIELD — COMPILE-TIME HALF FIXED
  2026-07-28, RUNTIME HALF AT CALL ARGUMENTS STILL OPEN (see the entry below it).** Every
  statically-known violation is now an **E3005** at a call argument, a struct-literal field, a field
  default and a field store, and the runtime check is emitted at the three field positions. What
  remains is the runtime check at a **call argument**, blocked by the arg-pinning bug filed
  immediately below. ⚠ **Local and global assignment are NOT covered and cannot be, yet**:
  `ResolvedVar` records a `Kind` and a struct name, never an `IrType`, so a local does not know its
  own range — the declaration loses it, not the store. That is a separate, deeper rung.
  Original finding, kept because the measurements are the evidence:
  found 2026-07-28 by the review of the struct-literal fix below, while auditing whether that fix's
  two widening tables should be collapsed. **The A/B is one conversion in two contexts**, with
  `typealias Byte = int(0 to u8.max)` and `let n = 300`: at a `return` it is
  `E3005: Value 300 is outside the range of 'Byte' (int(0 to 255))`; as an ARGUMENT it compiles clean
  and the callee receives **300**. ⭐ **The cause is NOT the widening table, and that matters because
  the obvious fix is wrong.** `ConvertArgToParamType`'s table and `IsWideningCast` disagree on four
  pairs (`int→byte`, `int→short`, `short→byte`, `float→f32`), but deleting the argument table's arms
  would also reject the IN-RANGE `takeByte(65)`, with an "argument type mismatch" naming the wrong
  problem. The real gap is that **`ValidateAndEmitRangeCheck` has exactly four callers — `return`, an
  explicit `as`, an array-literal element, and its own definition — and the CALL PATH IS NOT ONE OF
  THEM.** ⚠⚠ **NOR IS THE STRUCT-LITERAL FIELD: `Self{b: 300}` into that same `Byte` field also
  stores 300** (probed on the fixed binary — the kind coercion below and the range check are
  DIFFERENT MECHANISMS, so fixing the first neither caused nor cured the second). Two positions
  measured, both silent; the missing-caller list is the defect, not any one caller. ⚠ Its fix
  carries **codegen blast radius** (a runtime range check emitted at call sites
  that have none today), so it is its own change and not a review edit. ⇒ **Related to but wider than
  `#103`** ("builtin array methods do not enforce their RANGED parameter types"), which is this same
  hole seen from one caller: the enforcement is missing for *every* function, not only the builtins.

- **🔴⭐⭐ SPLITTING A BLOCK PART-WAY THROUGH AN ARGUMENT LIST BREAKS ARG-PINNING — `E9001 … not in
  valueMap`.** Found 2026-07-28 by the range-check rung, which is the FIRST caller that ever emitted
  a branch there. `ParseArgList` pins any argument evaluated in a block other than the final one:
  it inserts a store into the block the argument was recorded against and reloads in the final block.
  That assumes an argument's value lives in the block its evaluation was recorded against — and a
  block SPLIT during a later argument's evaluation breaks the assumption. Emitting the runtime range
  check at a call argument dies building shv2 with
  `E9001: Lowering 'Compiler.CompileMemory.deltaOf' failed: assign value %3748 (kind=MaxonInteger)
  not in valueMap; assigning to '__arg_pin_4'`, on
  `PhaseDelta.create(0, cpuTicks: 0, allocs: allocs, frees: frees, bytes: self.columns.bytesAt(phase.ordinal))`
  — five ranged params whose LAST argument is itself a call needing a runtime check, so the split
  lands mid-list. ⭐ **Established by BISECTION, not by reading**: the identical tree builds clean with
  `emitRuntimeCheck: false` and dies with it true. ⚠ **The minimal repro is not yet reduced** — three
  hand-built shapes (nested call in a named arg; `let`-bound args ahead of a splitting one; a
  differing ranged type to defeat the skip) all compile fine, so the trigger is narrower than any of
  them and the real one is still only reproducible through shv2's own source. ⇒ **Closing this
  un-blocks the runtime half of the entry above**, which is why the two are filed together.

- **🔴⭐ AN INT LITERAL IN A `float` STRUCT-LITERAL FIELD CRASHES THE ORACLE — `E9001: float value %0 has
  no FP register and no stack home`.** Filed 2026-07-28 by the bare-type-argument rung; **it is the reason
  P1.6 residual (2) needed a user ruling instead of a lookup** — the one case that would have settled
  int→float coercion for a field store is the one case the oracle cannot compile. `type P { var v as
  float }` then `P{v: 3}`. ⚠ **This is a CRASH, not a silent wrong answer, so it erodes the oracle
  differently: it does not corrupt an answer, it withholds one** — and the withheld answer was
  load-bearing. The parameter path (`half(6)` with `x float`) compiles and answers 3 in the same
  compiler, so the bootstrap already HAS the coercion; the struct-literal field store reaches codegen
  without it. ⇒ shv2's behaviour here is fixed by **USER RULING 2026-07-28 (coerce, one shared door —
  see P1.6 residual (2))**, and this entry is what has to land before the oracle can confirm it
  independently.

- **✅ THREE STRING BUGS IN THE ORACLE — ALL FIXED 2026-07-28** (main `95756de83` + `95551bffb`, C#
  suite **3175 → 3181/0**, codegen neutrality proven by experiment). User-directed: *"we should fix the
  bootstrap too."* All three are the SAME failure — **a fact stated in two places, drifted, with the
  test that would catch it written on a shape too small to fail.**
  1. **`"ASCII ⇒ one byte per grapheme"` was FALSE for CR+LF, and ten methods believed it.**
     `isAsciiFlag` promised *"every byte < 0x80"*; `charAt`/`indexAfter`/`indexBefore`/`findFirst`/
     `findLast`/`slice`-by-length/`trim`/`trimStart`/`trimEnd`/`advance` all read it as *"one byte per
     character"*. `"\r\n"` is ONE cluster under GB3, so all ten split it — measured `count=3 trips=4`.
     ⭐ **Cured at the FIELD, not with ten guards**: `singleByteGraphemesFlag` = ASCII **and CR-free**.
     **CR-free rather than CRLF-free is load-bearing** — "no CR" is a PER-BYTE property, so it survives
     slicing *and* concatenation, where the other spelling is defeated by `"a\r"` + `"\nb"`.
     ⚠ `trim` was **right by coincidence** (CR and LF are each independently whitespace) — the worst
     kind of site, because it stays green until a `CharacterSet` holds one and not the other.
  2. **A repeated self-append read FREED MEMORY.** `LowerManagedMemAppend` captured `other.buffer`
     *before* `maxon_string_ensure_cap`; when `other` IS `self`, that is the block ensure_cap frees.
     ⚠⚠ **THE COORDINATOR'S ORIGINAL FILING OF THIS BUG WAS WRONG AND IS CORRECTED HERE.** It said the
     program *"prints `B=abcabc` and STOPS — no `byteLength`, no later statement, exit 0"*, i.e. a silent
     truncation. **It does no such thing.** Re-measured to a FILE and hexdumped, the real bytes are
     `B=abcabc\0\0\0\0\0\0|12\nC=done\n`, exit 0 — `byteLength()` is correctly 12, execution continues,
     and nothing is truncated. **Six of the twelve bytes are NUL, read out of the freed buffer.**
     ⭐ **The "truncation" was the INSTRUMENT: the MCP surfaces stdout as a string and a NUL ends it.**
     ⇒ **A NUL in program output truncates the CAPTURE, not the PROGRAM.** Redirect to a file and
     `od -c` it before believing output stopped.
  3. **`String.clone()` returned a VIEW, so the clone read freed memory the moment the source grew**
     (found by the review, coordinator-reproduced). `String{managed: X, …}` builds a `capacity=-1` view
     of X's buffer *whatever X is*; `managed.slice` **copies** when the source owns its buffer, and that
     copy is the entire invariant. **`Array.clone()` always went through `managed.slice`; `String.clone()`
     was "optimised" off it** with a comment claiming *"the same thing in one allocation"*. It is not.
     `toByteArray()` had it too — and ⚠ **`specs/string-type-2.md` PINNED THE EXACT PROMISE and passed
     anyway**, because its string was five bytes, short enough that the freed block still held them.
     Not a spec encoding a bug: **a correct rule pinned on too small a shape.**
     ⚠ Coordinator note: the obvious repro also MISSES it — a string *literal* lives in rdata, so the
     view stays valid. The bug needs an **owned heap** buffer (`var a = ""` then `append`).
  ⇒ **The oracle is now trustworthy for CRLF iteration, self-append and clone**, and its `"a\r\nb"`
  answer (`count=3 trips=3 widest=2`) now MATCHES shv2's — P1.8 Slice B's licensed divergence has
  become plain agreement.

- **⛔ `String.from(bytes)` / `String.init(managed)` HAND BACK A VIEW OF A BUFFER THE CALLER KEEPS**
  (filed by the oracle rung's review, 2026-07-28 — **not** fixed there, because it is a CONTRACT question,
  not a bug). `var raw = <ByteArray>` · `let s = String.from(raw)` · `raw.push(…)` ⇒ `s` reads freed
  memory. ⚠ **This is a DIFFERENT family from the `clone()`/`toByteArray()` bugs fixed above**: those are
  *self-derived accessors*, which must copy; these are *hand-over constructors*, **documented** as
  zero-copy, and `Array.init` / `Vector.init` / `Character.init` share the shape. Closing it means buffer
  refcounting (**which is what shv2 chose** — see P1.7's COW slices) or copy-on-view with a measured cost
  on Json/Subprocess/Console. **Needs a ruling before a rung.**

- **⬜ `maxon_cow_struct_detach` IS EMITTED INTO EVERY BINARY (x64 AND arm64) AND CALLED BY NOTHING**
  (filed by the oracle rung's review). Dead runtime code in every program the bootstrap produces — and
  pointedly, **it is exactly the machinery that would have DETECTED the `refcount > 1` sharing that the
  `String.clone()` bug rode on.** Either wire it up or delete it; carrying an unreachable guard is the
  shape that lets the thing it guards against ship.

- **⬜⭐ `int(0 to u64.max)` COMPARES **SIGNED**, so `u64.max` ORDERS AS −1** (filed by the oracle rung,
  2026-07-28; present at HEAD before and after it). Measured: `"abcd".slice(startIndex(), length: u64.max)`
  returns `""` rather than `"abcd"`, a silent wrong answer on both the fast and slow paths. ⚠ **Not a
  String bug — it is bootstrap-wide and affects every `int(0 to u64.max)`**, which is the most common
  ranged typealias in the stdlib. The oracle rung's new fast paths deliberately decline those inputs
  (`count > 0`) so they cannot turn it into a panic, but nothing fixes it.

- **🔴🔴 AN UN-AWAITED GREEN THREAD HANGS THE PROCESS AT EXIT — FOREVER, WITH NO DEBUGGER INVOLVED.** Surfaced by P4d-2b's review; **reproduced and SHARPENED by the coordinator**. `__gt_cleanup`'s drain waits `WaitForSingleObject(__io_done_event, INFINITE)` when threads are alive and nothing is runnable (`X86CodeEmitter.Runtime.cs:~5871-5883`, plus its arm64 twin). ⚠ **The review framed this as "a program whose un-awaited thread is still SLEEPING when `main` returns"; the measurement says the sleep is NOT the ingredient.** Three variants of one program, `MAXON_MAX_PROCS=1`, no debugger: an un-awaited thread sleeping **3000 ms** → **HUNG 3/3** (killed at 12 s); the same thread sleeping only **10 ms**, so it FINISHES well before `main` returns → **HUNG 2/2** (killed at 8 s); the **identical program that AWAITS it** → **exit 1 in 126 ms**. ⇒ **The defect is being un-awaited, not being un-finished.** Nothing ever signals `__io_done_event` for a thread nobody awaits, so the drain blocks on a wait that cannot be satisfied. ⚠ **The language makes this HARD to write but not impossible, which is why it has survived**: the bootstrap rejects the two obvious shapes — a bare `async f()` statement is **E2001** and a never-used `let p = async f()` is **E3012** — so the reproduction needs a *conditional* await (`if n > 100 … await p`, false at runtime), which is an ordinary pattern, not a contrivance. **Fix direction (review's, and it is right):** a bounded wait so the drain loop re-reaches `__gt_timer_check` and its own liveness re-check, rather than an unsatisfiable INFINITE. **Both backends.** ⚠ Note the interaction with the held-thread wedge P4d-2b fixed: that one had the SAME shape (a thread off every queue ⇒ drain never finds it ⇒ INFINITE), and was cured on the agent side by not holding once `__sched_shutdown_flag` is set. **This one has no debugger to teach — the runtime must not offer an unsatisfiable wait in the first place.**

- **🔴 A MISSED WAKEUP COSTS A FULL 100 ms, AND THE RUNTIME ALREADY KNOWS — the timeout IS the workaround.** Surfaced by P4d-2b's optimizer while benchmarking; **reproduced and root-caused by the coordinator**. `__sched_wloop_really_park` parks a worker with `WaitForSingleObject(P->wakeEvent, 100)` under a comment that says it outright: *"use timeout to avoid missed-wakeup hangs"* (`X86CodeEmitter.Runtime.cs:~5672`). So a wakeup that should have been signalled is instead waited out at **100 ms granularity**, and the 100 ms poll is a safety net standing in for the fix. **Measured:** a yield-heavy program (2 green threads × 20,000 `sleep(0)` round trips, `MAXON_MAX_PROCS=2`) has a tight mode of **71–78 ms** and one run in ten at **176 ms** — an excess of **exactly one timeout period**. The optimizer independently saw *"occasional runs ~140 ms above the mode"* and, worse, caught one process at **2 minutes wall for 0.19 s CPU**, which is the same defect compounding across many consecutive missed wakeups. ⚠ **Pre-existing and NOT P4d-2b** — it reproduces on the pre-rung binary and in null controls (identical binaries, 9 vs 7 stalls), and it made the P=2 yield benchmark unusable as an instrument. **What the rung owes:** find why the `__gt_enqueue` wake path misses (the `SetEvent` racing the park's shutdown re-check at `:5666-5668` is the obvious suspect — the code re-checks shutdown *before* parking specifically to "avoid missed wakeup", so the same window is already known to exist), and fix the SIGNAL rather than shortening the timeout. ⚠ **Shortening the 100 ms would hide it harder** — it converts a latency defect into a busy-poll and makes the underlying race less observable, which is the opposite of what the measurement above is for.

- **🔴🔴 NESTED `async` SEGFAULTS DETERMINISTICALLY AT >=3 PROCESSORS — I.E. IN THE DEFAULT CONFIGURATION.** Surfaced by P4d-2b, **reproduced by the coordinator three independent ways**. A green thread that spawns and awaits another green thread — 17 lines, no I/O, no debugger: `inner()` sleeps 10 ms; `outer()` does `let p = async inner(); return await p`; `main()` does the same over `outer()`. **Measured: `MAXON_MAX_PROCS` 1 → 6/6 OK, 2 → 6/6 OK, 3 → 6/6 SEGFAULT, 4 → 6/6 SEGFAULT.** ⚠ **NOT the debug agent** (3/3 segfault built `--no-debug-agent`, so none of the agent's code is in the binary). ⚠ **NOT P4d-2b** (3/3 segfault on the pre-rung compiler). ⚠⚠ **AND IT IS THE DEFAULT**: with no `MAXON_MAX_PROCS` set at all, 3/3 segfault, because `__sched_max_procs` is seeded from `dwNumberOfProcessors` (16 on this host). **So nested `async` — an ordinary, unremarkable pattern — is broken out of the box on any machine with 3+ cores.** This is a far SHARPER form of the intermittent multi-processor crash filed alongside it (that one needed 50,000 spawns for ~10%; this is 100% with three). **Whoever takes that one should start HERE** — a deterministic 17-line repro at a known threshold is a categorically better handle than a rare corruption, and the threshold itself (fails at 3, not 2) is a strong hint: two processors are main's P0 plus one worker, three is the first configuration where a *nested* spawn can be stolen by a processor that is neither the parent's nor main's. Related but distinct: `sleep(N)` on the MAIN thread returns EARLY whenever another green thread completes (`__gt_yield_completed` sets `P->mainThread.status = running` before switching to it, and `maxon_sleep`'s main-thread loop reads *its own status* as "my timer fired"; on P0 the mainThread IS `main` — `__gt_await` is immune because it gates on the promise's status). **Both are runtime/scheduler defects with the C# suite as their gate; neither is inherited by shv2 automatically, and shv2's own scheduler should be checked against both shapes when this is fixed.**

- **🔴 AN INTERMITTENT MULTI-PROCESSOR CRASH IN THE bootstrap's SPAWN/AWAIT PATH — corrupted return address, ~10% of runs, and it currently has NO HOME.** Surfaced by P4d-GT-STACK's optimizer while benchmarking spawn throughput. **Symptoms:** a corrupted return address — `rip=0x0`, `rip=0xb`, `rbp=0x0`. **Measured (its method, its numbers):** a 50,000-spawn+await benchmark crashed **3/30 runs on the pre-rung binary and 2/30 after** — *statistically identical, so P4d-GT-STACK neither causes nor fixes it* — and **0/20 in BOTH arms under `MAXON_MAX_PROCS=1`**, which localizes it to multi-processor scheduling. ⚠ **VOLUME MATTERS FOR THE REPRO:** the coordinator's lighter probe (120 spawns of `sleep(1)`, 15 runs, default processors, pre-rung compiler) got **0/15** — consistent with a ~10% rate (P≈21%) but NOT a reproduction. Whoever takes this needs the heavy workload, not a toy one. ⚠⚠ **DO NOT INHERIT THE ORIGINAL ATTRIBUTION.** It was reported as belonging to "the documented OPEN #66 family (a second M resuming a GT onto a relocated/freed stack)" — but **#66 is a LANDED FIX** ("x86 GT double-schedule fix", referenced only in a historical `docs/optimization-log.md` row alongside `OPEN #54 Slice B2` and the x64-linux backend), **not an open family**, and no open entry for a second-M/relocated-stack race exists anywhere in this file. A live ~10% failure quietly re-labelled as an already-known issue is how it stops being anyone's. It is filed here, on its own, unattributed. **Plausible direction (unverified):** the fix `8163f1056` *"gate every GT wakeup on ioYielded — the `--workers>=5` double-schedule"* is in the same area; whether this is a residual of that or a distinct race is exactly what the rung must establish first.

- **🔴 MORE THAN 256 CONCURRENT SLEEPING GREEN THREADS = A SILENT SEGFAULT, NOT A DIAGNOSTIC.** Found by the debugger's P4d-GT-STACK rung (a runtime rung, not a debugger one) and **reproduced by the coordinator on the PRE-rung bootstrap**, so it is neither caused nor fixed by that rung: a program spawning N `async` thunks that each `sleep(200)` and awaiting them all runs fine at **N=250 (exit 0)** and **SEGFAULTS at N=300 — exit 139, and stderr is EMPTY**, so there is not even a panic to read. Cause: `TimerHeapCapacity = 256` (`RuntimeEmitter.Scheduler.cs:15`) is a **fixed 256-entry array** and `__gt_timer_add` writes past it rather than refusing; the implementer separately observed the write landing at `addr=0xffffffffffffffff`. ⚠ **300 concurrent sleeps is not an exotic program** — any fan-out that parks its workers hits it, and the failure gives the author nothing to go on. **The fix is a design choice, not a patch:** (a) bounds-check and panic with an honest message naming the cap (cheapest, and strictly better than today); (b) grow the heap — its own comment already calls the layout *"min-heap-compatible for a later retrofit"*, so growth was anticipated; or (c) both. ⚠ The timer heap is the SLEEPING-thread structure, so the cap bounds concurrent *sleepers*, not live green threads. shv2 has its own `TimerHeapCapacity` with the same shape and should be checked when this is fixed.

- **#17 — two file-private `var`s of the same name alias into ONE `.data` slot** (`getA()+getB()` returns
  200 not 107): the name resolver is file-scoped but the global label is bare-name (`_globalLabels[name]`).
  Fix = file-qualify the global label in both emitters + the registry.
- **✅ #90 — duplicate top-level `let`/`var` → E3006 — CLOSED 2026-07-22** (main `8dab48de0`, C# suite 3048→3051/0,
  shv2 1140/0 unchanged). Fixed in `2-Parser.cs` `CollectAndEvaluateTopLevelDecls` (a per-file
  `HashSet<declaredValueNames>` gating the first-wins `byName` maps — kind-independent: a `let` and a `var`
  share one storage key; per-file: cross-file homonyms untouched). Message byte-identical to shv2 ("duplicate
  definition of 'X'"). The review also corrected the `error-codes.txt` E3006 doc the rung had falsified.
  ⚠ **The value-vs-FUNCTION/TYPE cross-kind homonym (`let A` + `function A`) still coexists silently — in BOTH
  compilers, consistently** — pre-existing, out of scope (a distinct rule if ever wanted).
- **#98 — the C# bootstrap cannot `spec-test --target=wasm32-wasi`** (any test): `CompileTarget.parserOs`
  throws `E9001 Unknown OS 'wasi'` before parsing. Bootstrap-wide, pre-existing; low-priority (the wasm oracle
  is shv2 over `specs-shv2`, not the bootstrap). Found 2026-07-22 during #90.
- **✅ #102 — FOUR bootstrap `__ManagedMemory` wrong answers — CLOSED 2026-07-22** (main `115fdcfc3`, C# 3068→3072/0, shv2 unaffected). Found by the P1.7 COW slice-A rung using the bootstrap as its oracle; shv2 was RIGHT in all four. ⚠ **The single-root-cause hypothesis was WRONG, corrected by probing — there were TWO causes:**
  - **CAUSE A (element-size, NOT the sentinel):** `LowerManagedMemShift` erased a vacated slot with a fixed 8-byte `StdStoreIndirectOp(…, IrType.I64)`; at `element_size = 1` that destroyed the slot **and the seven live elements after it** — so `b"hey".insert(1, value: 88)` printed `104 88 0 0` (tail zeroed, `count()==4`, a silent wrong answer with a green exit). Reproduced on a fully HEAP-owned buffer too, which is what disproved the sentinel hypothesis. Fixed by routing through `mm_zero_element_range` (scales by the record's own `element_size`). The single-homed `EmitZeroElementSlot` folded three hand-rolled copies (insert-shift, `shiftLeft`, `remove`).
  - **CAUSE B (sentinel-into-arithmetic):** a negative `capacity` (`-2` rdata) reached guards written for non-negative capacities — `resize(-2)` silently published `count()==-2`, `resize(0)` panicked `realloc size must be > 0`, and a FOURTH found in-rung (`[10,20,30].reserve(1)` → `count()==3 cap()==1` + a 16-byte OOB zero). Fixed by clamping `capacity()`'s Maxon-facing read non-negative behind a `MaxonFieldAccessOp.ClampNegativeSentinel` flag (the review caught that this flag was `init`-only and **copied in TWO cloners** — `FunctionCloner` + `MonomorphizationPass` — one omission silently restoring the whole leak; single-homed as `CloneWith`). `LowerManagedMemClear`'s non-owned guard was made a **local invariant** (proven by widening, not argued). `Array.resize`'s now-reachable panic message was corrected (it blamed `reserve` for the caller's out-of-range argument).
- **#46 — the whole-program mutating-parameter fixpoint does not converge** as the call graph grows,
  alternately demanding `let` (E3019) then `var` (E3077) on the SAME binding. Worked around in shv2 by a
  direct `branchEdges.push`. Fix = make `ParameterMutationAnalysisPass.SequentialFixpoint` converge.
- **#70 — a managed `Array` payload bound as a variant's TAIL field in a LARGE `match` arm corrupts the
  scrutinee's decref** (`mm_decref` on a sentinel; crashed every `try`/`throw` lowering when `indirectCall`
  joined `lowerOp`). Worked around in shv2 by a dedicated `lowerIndirectCall`.
- **#72 — a helper storing into ~11 aliased `self` array-fields double-frees at shv2 self-compile time**
  (`__destruct_FuncSignature`). Forces shv2 to keep the per-body column reset inline. Same class as #70.
- **✅ #58 — mixed-type `match … gives` crash → clean E3005 — CLOSED 2026-07-22** (main `9d0d88f3f`, C# suite
  3051→3060/0, shv2 unaffected). The bootstrap's `ParseMatchExpression` reconciled arm kinds by widening but
  left an INCOMPATIBLE pair unreconciled — the ill-typed value flowed downstream and crashed E9001 (an
  unhandled cast, or the null-ref). Added the give-type-consistency check the ternary path already had (class
  + aggregate disagreement → E3005, byte-identical to shv2); the review single-sourced the aggregate-conflict
  + type-display decisions the match and ternary paths had duplicated.
- **#95 — two function-VALUE arms of DIFFERENT signatures in a match/ternary EXPRESSION crash** (`E9001:
  Cannot determine function type from MaxonVarRefOp`, at the `let` reading back the merged fn type). ⇒ **#58
  PARTIALLY covered this**: function-vs-NON-function arms (`'int' vs 'function'`) are now clean E3005; the
  two-function-value remainder still crashes (both `Function` kind, no aggregate, so the give-type check
  passes — the crash is reading the merged fn signature, the SAME failure the bootstrap's ternary `let h = f
  if c else g` has). shv2 rejects both cleanly (whole-program `pendingFnMergeChecks`). Fix = mirror shv2's
  fn-signature merge in the bootstrap's match AND ternary read-back. Its own rung.
- **✅ #99 — invalid `as` casts reject cleanly (E3009), not a crash/no-op — CLOSED 2026-07-22** (user-directed;
  main `9adc5f6de`, C# 3060→3064/0, shv2 1170→1175/0). A cast between fundamentally-incompatible kinds (a
  managed aggregate — String/struct/union/function value — crossing to/from a scalar number, OR to a DIFFERENT
  aggregate) CRASHED the bootstrap (`E9001` in `IsWideningCast`) and was silently NO-OP'd by shv2. Both now
  reject at the cast site with byte-identical **E3009 "Cannot cast from {src} to {tgt}"**. Bootstrap:
  `IsWideningCast` `_ => throw` → `_ => false` → E3009 (also turned a sibling arithmetic-path crash into a clean
  E2004). shv2: `castHasNoLegalConversion` (TypeRules). ⚠ The review CAUGHT A REACHABLE DOUBLE-FREE the first
  pass missed: `p as String` (struct→String) no-op'd → a String drop ran on the struct box → exit 101; the
  generalized predicate fixes it (opaque-`T`/scalar↔scalar untouched, provably minimal).
- **#100 — shv2 mis-resolves a struct/aggregate TYPE used as a cast TARGET to `integer`** (`int as Point`
  compiles as a no-op exit 0; `String as Point` errors E3009 with a WRONG "…to int" message — the target's
  `maxonTypeTag` resolves a struct to integer). Distinct from #99 (the aggregate SOURCE); a cast-TARGET
  type-resolution bug, not memory-unsafe. Found 2026-07-22 by the #99 review.
- **#101 — cross-compiler grammar asymmetry on aggregate cast TARGETS + the float→int explicit-`as` divergence
  (RULING).** Bootstrap rejects `x as String`/`x as Point` at PARSE (`E2003`), shv2 accepts an aggregate target
  (why #99's struct→String test is shv2-only). And a **float→int explicit `as` DIVERGES — needs a RULING (same
  class as #19)**: shv2 TRUNCATES `5.9 as Whole` (exit 5), the bootstrap REJECTS it (`E3009`, "use trunc").
  (`structRef` coarseness — `Point as Rect`, two structs share `structRef` so the tag can't split them — is a
  documented deferred gap.) Found 2026-07-22 by the #99 review.
- **#4i — E3097 (enum-accessor comparison) is fully defeated by any NARROWING `as` cast** (`c.rawValue as
  Ordinal` interposes a range-check value that hides the accessor; a WIDE cast does not). Key the rule on the
  QUESTION, not the producing-op identity. (shv2 does not emit E3097 yet — a future shv2 rule too.)
- **#3 (residual only) — a double-await through STORAGE stays silent** (double-free): a boxed promise read
  from storage gets a FRESH `GreenThreadId`. shv2's side is closed (E3100 linear-await; stored/param promises
  are E2015), so shv2 does NOT hit this; maxon-sharp does.
- **◑ #34 — union `E2026`/`E2046` say "enum"** (a hardcode oversight — the bootstrap's own E3034 IS
  `IsUnion`-conditional) where shv2 says "union" via `EnumLayout.kindWord()`. ✅ **shv2 SIDE CLOSED 2026-07-22**
  (main `8718c8d7c`): `error.union-not-exhaustive` pins shv2's correct "match on union 'Shape'". ⚠ The BOOTSTRAP
  wording is still wrong (a maxon-sharp fix, C# suite as its gate) — a bootstrap-oracle finding, not an shv2
  residual.
- **#120 — bootstrap enum raw-value duplicate check FOLDS `-0.0`→`+0.0`** (rejects `enum X { a=-0.0 b=0.0 }` as
  duplicate `'0'`) where shv2 keeps them bit-distinct. shv2 is self-consistent (one i64 equality for both
  dispatch and dedup); the bootstrap mixes float-`==` dedup with bit-compare dispatch. A `maxon-sharp` fix
  (C# suite gate); shv2's behavior is pinned by `float-backed-signed-zero`. Found 2026-07-22 by the #33 review.
- **#121 — enum negated-non-numeric raw value: shv2 E2010 vs bootstrap E2004** (`enum X { a = -y }` etc.). Both
  REJECT with a correct positioned error (not a wrong answer, no float-path misfire); only the code/message
  diverge. Pre-existing in `readEnumIntRawValue`, un-touched by #33. Aligning likely needs an error-code
  decision — its own small rung if desired. Found 2026-07-22 by the #33 review.
- **#109 — `sqrt(true)` CRASHES the bootstrap with an internal `E9001` + a C# stack trace**
  (`LowerUnaryFloat: unexpected input type StdBool`) where a positioned type diagnostic belongs. shv2 answers
  `E3005: 'sqrt' requires a number, but its argument is bool`. **shv2 is right; the oracle leaks an internal
  failure.** Found 2026-07-22 by the P1.0d.4-C review.
- **#110 — the bootstrap CRASHES with `E9001: all FP registers occupied`** on a 20-simultaneously-live-float
  probe that shv2 compiles correctly (returns 255, matching wasm). A register-allocator limit surfacing as an
  internal error rather than a spill. Found 2026-07-22 by the P1.0d.4-C review.
- **#111 — the bootstrap rejects `int(-9223372036854775808 to 0)` as a range bound** where shv2 accepts it.
  Noticed incidentally by the P1.0d.4-A review; needs the full C# suite as its gate.
- **#112 — malformed-exponent wording**: `1.5e1_0` gives shv2 `E2007 Unexpected end of input` vs the
  bootstrap's `E2001 unexpected token: '_0'`. Both REJECT, so behaviour agrees; only the message differs.
- **⭐ #113 — the bootstrap resolves stdlib `#if os(...)` against the HOST, not `--target`, so cross-target
  goldens are only regenerable on their native host.** ROOT CAUSE (verified twice, by two independent agents):
  `maxon-sharp/Compiler/0-Compiler.cs:869` parses the stdlib once with `CompileTarget.Default` and caches it,
  so every host-conditional typealias evaluates against the host OS regardless of `--target`. `ExitCode` is the
  visible one — cross-compiling to `arm64-macos` from Windows emits `func @main() -> u32` where the target wants
  `-> u8` (`stdlib/Process.maxon:7-16`). Structural proof: committed goldens are **arm64 `u32`=0/`u8`=2711 · wasm
  `u32`=0/`u8`=2534 · x64-windows `u32`=2733/`u8`=7** — each host stamped its own width. This is WHY
  `shv2-cross-target-golden-hosts` needs a Mac/WSL for the other-host goldens, and it needs its own rung (fix:
  thread `--target` into the cached stdlib parse). Found 2026-07-22 by the P1.0d.4-D1 review.
- **#114 — part of `/specs`' wasm corner is UNREGENERABLE by any building compiler.** The committed
  `specs/fragments-wasm32-wasi/builtins-type/builtins-type.float-to-bits.test` golden is **v1 MIR** dialect
  (`mir.bitcast.f64.i64`). `maxon-sharp` has no MIR and no wasm backend (`build --target=wasm32-wasi` throws
  `Unknown OS 'wasi'`); those goldens came from `maxon-selfhosted`, which no longer builds. So new
  `__Builtins.*` cases cannot get a wasm fragment from the bootstrap — D1 correctly shipped x64-only fragments
  and did not fabricate a wasm one. (Related minor: `build --target=wasm32-wasi` on the bootstrap throws an
  unhandled `ArgumentException` in `GetOutputExtension` rather than a clean "unsupported target".) Found
  2026-07-22 by the P1.0d.4-D1 review.
- **⭐ #115 — shv2 does NOT range-check a `main() returns ExitCode` return, so an out-of-`[0,255]` exit code is
  a SILENT WRONG ANSWER.** `main` returning `id(0) - 50` exits **4294967246** (−50 as u32) on shv2 where the
  bootstrap PANICS `Range check failed: value outside typealias 'ExitCode'` exit 1 (verified both, pure INT — no
  float). ⚠ **This is an shv2 gap, not a bootstrap bug** — and it is NARROW: the P1.0d.4-B review confirmed a
  USER int/float ranged return DOES panic cleanly, so only the `main`/`ExitCode` return path bypasses the guard
  (likely the `mrt_start` exit path, which no rung has range-checked). PRE-EXISTING (B+A′ left the ExitCode
  cascade byte-identical; reproduces with zero float), so it did not hold P1.0d.4 (the floats phase) — but it is
  a real silent miscompile and a **strong candidate for the next rung**, being range-check-adjacent to the
  P1.9 / `InsertRangeChecks` machinery. Found 2026-07-22 by the P1.0d.4-B review.
- **#116 — the bootstrap ICEs on a negated radix literal `-0x10` / `-0b101`** (`E9001` "not in a correct
  format" — its negated-literal path feeds a hex/binary token to a decimal parser) where **shv2 is RIGHT**
  (`-0x10` → 16, `-0b101` → 5). A `maxon-sharp` bug; needs the full C# suite as its gate. Found 2026-07-22 by
  the P1.0d.4-B review.
- **#117 — the bootstrap CONST-FOLDER mis-folds a NaN self-comparison**: for `let NANV = INF - INF` (a NaN),
  it folds `NANV == NANV` → **true** and `NANV != NANV` → **false** (IEEE says the reverse — NaN is unordered,
  unequal to everything including itself). **shv2 is RIGHT** — D2's `foldFloatCompare` uses the host's real f64
  `==`, so it matches shv2's own runtime SSE. A `maxon-sharp` const-folder bug (needs the full C# suite as its
  gate); affects no committed shv2 spec. Found 2026-07-22 by the P1.0d.4-D2 review.
- **#118 — a USER program comparing two `__Builtins.*` results PANICS in lowering, not a clean diagnostic**:
  `__Builtins.floatToBits(a) == __Builtins.floatToBits(b)` in a function body reaches
  `LowerMaxonToStd.maxon:112` (`valueTagToStdType: an 'unresolved' tag reached lowering`, via `lowerCompare`).
  shv2 has NO `__Builtins` recognition, so the call result stays `unresolved` and a COMPARISON slips past
  SemanticCheck's E3004 into lowering. A compiler must never panic on user input — SemanticCheck should reject
  an unresolved-tag comparison cleanly. Pre-existing (D2's own `__Builtins` calls are COMPILER source the
  bootstrap compiles — a different thing from user code); low-priority (the trigger is already-unsupported
  `__Builtins` syntax). Found 2026-07-22 by the P1.0d.4-D2 review.

### ⚠ Measured debt — recorded with its trigger, NOT fixed (2026-07-22, P1.0d.4 A+C)

- **A payload-free union case STILL HEAP-ALLOCATES, in BOTH codegens — so the cost SURVIVES SELF-HOSTING.**
  `parseCallNamed` constructs **two** such boxes per parsed call — `ReceiverArg.none` **and**
  `ConstructorInstance.none` on the same line ([Parser.maxon:15507](maxon-shv2/Compiler/Parser.maxon#L15507);
  `ReceiverArg.none` also at `:12786`) — costing 174/310/582/1,126/2,214/**4,390** allocations across rungs 0-5,
  **LINEAR** (increments 68/136/272/544/1088 double exactly), 0.042 % of rung-5. Bootstrap side:
  `MaxonToStandardConversion.cs:533-541` calls `EmitAlloc(8 + maxPayload*8)` **unconditionally** — the zero-arg
  case only empties `argsList`. shv2's own lowering does the identical thing (verified from emitted IR).
  Correctness-neutral and linear ⇒ debt, not a fix-now. **Trigger to revisit: any rung that makes parse
  allocation a bottleneck, or the self-host gate.**
- **The three emitted GT-store scans** (`emitGtRunQueueRemove` / `emitGtTimerStoreRemove` / `emitGtProcStoreRemove`
  inside `__gt_promise_drop`, P1.5-B2 #88) are O(pending/parked GTs) at GENERATED-CODE runtime. **Measured
  linear-in-practice:** simultaneously-live un-awaited promises are bounded by the fixed count of source-level
  promise bindings — a loop reuses ONE binding, so the dropped GT is the sole run-queue head → O(1) removal
  (`spawn-drop-loop-bounded` over 1000 iterations stays linear). It is generated-code runtime, not compiler cost,
  so `scale-test` NEVER sees it. **Trigger to revisit: a construct that holds O(N) un-awaited, PARKED promises
  simultaneously and batch-drops them** — e.g. a promise-holding container (an array of promises) whose element
  drop routes through `__gt_promise_drop` — which does not exist at P1.5 but arrives with a promise container.
- **`bigMulPow` + the digit accumulation are Θ(strides × limbs)** — quadratic in exponent and digit count,
  **linear in practice** only because the real corpus tops out at 21 significant digits.
  **Trigger: a WIDER FLOAT TYPE.** binary128 pushes the decimal exponent 308→4932, the digit budget 768→~11,500
  and `MaxBigLimbs` 144→~1,700, growing both quadratic terms ~15× per dimension (~225×).
- **⭐ A TREE-WIDE TENSION BETWEEN TWO PROJECT RULES, measured and left for a user decision.** The bootstrap
  forms a jump table only for **≥4 dense, individually-named** match arms
  (`JumpTableFormationPass.cs:12,55,59` — `Threshold = 4`, ordinals dense from 0), so **any `to` range arm
  demotes a whole match to a linear compare chain** (probe: 9 named arms → a 9-case `x64.jump_table`; the same
  enum with two range arms → a linear `cmp` chain). CLAUDE.md's *"Consolidate redundant match arms"* rule
  therefore systematically demotes jump tables across this compiler. Not acted on: **whether that rule wants a
  carve-out for hot dispatch matches is the user's call, not a rung's.**

---

## Execution — how this parallelizes across agents

**The project has two axes, and only one of them parallelizes.**

- **The mechanism ladder is SERIAL.** heap/drops → String → owned payloads → errors → escape →
  generics → `Array`. These cannot be split: `own.drop` routes through the layout descriptor,
  `Array` *is* generics ∘ ownership. An agent "doing ownership in parallel with generics" would be
  writing against an IR that doesn't exist yet.
- **The pipeline (within any one rung) is PARALLEL.** Every rung touches Parser →
  TypeResolution/SemanticCheck → LowerMaxonToStd → Std passes → Backend → specs, and those are
  *different files*. Fix the dialect ops first and four agents write against them simultaneously.

⇒ **The coordinator owns the contract; agents own layers.** Two rules make it work:

> **RULE 1 — one file, one owner, per wave.** Never let two agents hold the same file.
> **RULE 2 — the coordinator writes the dialect ops BEFORE the wave launches**
> (`MaxonDialect.maxon`, `StdDialect.maxon`, `TargetDialect.maxon` + the `*OpMeta` backing), and
> hands agents a concrete golden-IR example for a sample program. The op definitions ARE the
> contract; agents coding against a contract that is still moving is the failure mode that makes
> parallel agents net-negative.

### The per-rung layer split

| Agent | Owns |
|---|---|
| **P** | `Compiler/Parser.maxon` (+ `ParseStaging.maxon`) |
| **T** | `Compiler/TypeResolution.maxon`, `SemanticCheck.maxon` |
| **L** | `Compiler/IR/Maxon/LowerMaxonToStd.maxon` (+ Std passes) |
| **B** | `Targets/X64/**`, `Targets/Shared/**` |
| **S** | `specs-shv2/<feature>.md` — authors the spec *before* the others finish (it is the acceptance test, so writing it first is a feature, not a nicety) |

**Coordinator-owned, in NO brief:** `Main.maxon` (agents want to add commands — wire them after
they land).

### Standing agent brief requirements (learned the hard way in v1)

Every brief MUST: (a) name the **specific v1 file to port**, with line numbers, and the shv2
**divergences to adapt to** (no MIR tier; `project.diagnostics` is first-class; `FileParseArtifact`
staging); (b) state the **exclusive file list** and the coordinator-owned files it must not touch;
(c) name the **traps** for that area; (d) require the report to **justify every deviation** and to
never claim a step passed unless it was run.

### Isolation & integration

Worktree-isolated agents; the coordinator merges and then **builds + runs `spec-test`** at each
integration point. That build is not distrust of the agents — it is the only place the coupling
between layers is actually exercised. Integration is inherently serial and is the real limit on
wave size: **beyond ~4–5 agents per wave, integration dominates and adding agents makes it
slower.**

### The workstreams that run ALONGSIDE the waves

Big, self-contained, stable input contract — the best parallel value in the project because they
never block on the ladder:

- **Workstream R — the emitted runtime** (~5–7k lines; R1 @ P1.2 · R2 @ P1.8 · R3 @ Beyond). Its
  contract is the MM ABI, fixed at P1.2.
- ~~**Workstream A — the register allocator**~~ ✅ **DONE** (M5.1→M5.12, and *linear*). The plan's
  biggest single risk, retired.
- **Workstream S — the spec corpus IS the development driver.** See its own section below. *(It was
  scoped here as "pure analysis — which spec ports at which rung," with the porting itself deferred
  to Beyond. That was backwards: the corpus is the driver, not the aftermath.)*

---

## Where we are

**The scalar core is DONE — and this time MEASURED, not asserted: `specs-shv2` 371/0** (P1.0d closed
2026-07-15). `let`/`var` · full-Pratt arithmetic/comparison/unary · parens · `true`/`false` ·
`not`/`and`/`or` · bitwise · chars · **floats (f64, XMM class)** · real block scoping · void functions ·
top-level `typealias`/`let`/`var` · `if`/`else` · `while`/`break`/`continue` (on-the-fly SSA +
`EliminatePhis`) · functions with params + calls · integer `/` and `mod` with **`a / 0` a clean panic**.

**shv2 HAS A HEAP** (P1.0r, 2026-07-15): a VirtualAlloc bump allocator that always returns zeroed memory,
`__mm_alloc`/`__mm_decref`/`__mm_free` as a builder-built `StdModule`, and a leak gate that fires.
**And STRUCTS are real** (P1.1a waves 1–2): `type` · layout · `Self{}` · `static create` · field reads,
writes, mutability rules and defaults.

⚠ **The paragraph that used to live here said the scalar core was INCOMPLETE, and before that, that it was
DONE. Both were written with equal confidence, and the second was wrong by 48-of-2,746.** The difference
was never the prose — it was whether anyone had run the corpus. **Every claim above is a suite number that
`spec-test` will reproduce, or it is not in this section.**

**The register allocator shipped and beat its own brief.** Register allocation was ~74% of v1's
self-compile wall time (~418 s of 561 s) against shv2's ≤30 s *whole-compile* budget; shv2's is
**linear** (M5.10 killed the quadratics for 8.8×; M5.11 replaced the fixpoint with SSA
path-exploration liveness + sparse live sets), with a cold-spill live-range splitter, `E5001` +
`ValueOrigin` for genuine pressure, and a custom ABI. **This was the plan's single biggest risk and
it is retired.**

| | v1 | shv2 (est.) |
|---|---:|---:|
| Parser | 21,862 | 6–8k |
| TypeResolution | 12,142 | 4–5k |
| LowerMaxonToStd | 16,135 | 6–8k |
| Memory model (`Own/*` vs `InsertRefcounts`) | 7,755 | 3–4k |
| Std passes | ~10k | 5–6k |
| x64 backend + emitters | 16,030 | 8–10k |
| Register allocator | 8,520 | ✅ **DONE** (linear) |
| **Workstream R (emitted runtime)** | *(inside X64Backend)* | **5–7k** |
| Testing | 7,699 | ✅ ~~704~~ → **6,982** (measured 2026-07-16) |
| **Total** | **191,487** → **192,971** (measured) | **~50–65k** |

**Current: ~~21,038~~ → 44,671** (measured 2026-07-16; `find maxon-shv2 -name '*.maxon' -exec cat {} + | wc -l`
— `Compiler/` is 37,096 of it, `Testing/` 6,982). Self-compile is **~5–20k lines away** on the estimate
above — and the hardest *single* piece of it (the allocator) is already behind us.

> ⚠ **THIS TABLE WAS STALE BY 2×, AND ITS `Testing` CELL BY 10× — and the drift is the file's own disease,
> aimed at its own schedule.** `21,038` and `704` were true when the plan was written and **nothing
> re-derives them**, so they aged into a claim ("~30–45k away") that was never re-measured. **The method is
> confirmed identical, not guessed:** the same command over v1 reads **192,971** against its recorded
> **191,487** — a 0.8% drift from ordinary edits, which is what a *maintained* number looks like next to an
> abandoned one. **The command is now written down beside the number**, because a figure whose derivation is
> unstated cannot be checked and therefore will not be. ⚠ **The `~50–65k` ESTIMATE is itself unvalidated** —
> it is a 2026-07-13 guess, and `Testing` alone overran its whole line by 6,278. Treat "5–20k away" as
> arithmetic on an untested premise, not as a schedule.

---

## Verification

- **Per rung:** `maxon-shv2 spec-test` stays green (**371/0** as of 2026-07-16, and growing with
  every Workstream-S port); ownership rungs (P1.2+) also assert an `mm-trace` block via
  `maxon monitor`.
- **The ratchet (Workstream S):** **an ENABLED spec case may never be re-disabled.** Behavioural,
  per-unit, and free — this is what the cut compass was for. A rung's deliverable is the set of
  `disabled-test:` markers it flips to `test:`.
- **Per commit:** `specs-shv2/fragments/` **clean** under `git status` after a spec run — the
  goldens are compared, so an empty diff *proves* byte-identical codegen. Plus
  `verify-warm-rebuild` PASS. These, not a distance metric, are the continuous gates.
- **From P1.5:** track **`% values promoted to shared`** — if it's 40%, static ownership bought
  nothing.
- **🚩 PHASE 1 GATE:** the differential oracle — `maxon.exe`-built and `maxon-shv2.exe`-built
  **parallel** harnesses must produce **identical** results over `specs-shv2/`. Plus
  **worker-count invariance** (`-j1` == `-jN`) and a clean `mm-trace` under the pool. Then the
  shv2-built harness becomes the runner.
- **🚩 PHASE 2 GATE:** stage-2 == stage-3 **byte-identical**; stage-2 shv2 passes the suite.
- **Beyond:** ≤30 s / ≤1.7 GB / >90% CPU on self-compile.

## Critical files

- **New:** `maxon-shv2/Compiler/IR/LayoutDescriptor.maxon` (declared at P1.2, live at P1.6);
  `maxon-shv2/Compiler/IR/Own/{OwnDialect,OwnershipInfer,OwnershipCheck,EscapeAnalysis,InsertDrops}.maxon`;
  `stdlib-shv2/stdlib/` (leaf name **must** be `stdlib`).
  ✅ done: `maxon-shv2/Testing/{SpecParser,SpecTestRunner}.maxon`.
  ~~`maxon-shv2/Compiler/SelfhostDistance.maxon`~~ (**cut — never written**).
- **Kept as tests, NOT rewritten** (per the PRINCIPLE):
  [GlobalDataTable.maxon:23](maxon-shv2/Compiler/Targets/Shared/GlobalDataTable.maxon#L23)
  (`GlobalDedupMap` — the conditional-conformance acceptance test, P2.2) ·
  [Logger.maxon:35](maxon-shv2/Compiler/Logger.maxon#L35) (`LazyMessage` — closure dogfood, P2.5) ·
  [StdDialect.maxon:333](maxon-shv2/Compiler/IR/Std/StdDialect.maxon#L333) (`StdValueUseSet` — why
  `Set` is core, P2.3).
- **Modified:** [Parser.maxon](maxon-shv2/Compiler/Parser.maxon) (grows every rung; **no recovery
  mode** — that was the compass's requirement);
  [Compiler.maxon](maxon-shv2/Compiler/Compiler.maxon) (`compileSource` ✅);
  [Main.maxon](maxon-shv2/Main.maxon) (`spec-test` ✅; the standalone `spec-runner` extraction at the
  Phase 1 gate); [0-Compiler.cs:846](maxon-sharp/Compiler/0-Compiler.cs#L846) (`MAXON_STDLIB`, for
  the fork).
- **Reference (read, don't copy):** `maxon-selfhosted/Compiler/IR/LayoutDescriptor.maxon`,
  `Compiler/Passes/BuildWitnessTables.maxon`, `Compiler/IR/Std/InsertRefcounts.maxon`, and
  [LowerMaxonToStd.maxon:1523](maxon-selfhosted/Compiler/IR/Maxon/LowerMaxonToStd.maxon#L1523)
  (`registerManagedMemoryStruct` — the hardcoded 40-byte record String rides on).
