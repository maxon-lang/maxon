// maxon-shv2 compiler entry point (walking skeleton — M1).
// Mirrors maxon-selfhosted/Main.maxon's command structure; grows into the real
// driver (parse args -> compile a file/directory) as the pipeline lands.

function main() returns ExitCode
	print("maxon-shv2 (walking skeleton)\n")
	return 0
end 'main'
