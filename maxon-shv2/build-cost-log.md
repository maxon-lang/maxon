<!-- ⚠ WRITTEN BY `scripts/spec-port-finish.sh`, AND BY NOTHING ELSE — DO NOT HAND-EDIT.

     Every cell is measured by the run that appends it. A row added by hand is a number nobody
     measured, and it is indistinguishable from one that was. (User ruling 2026-08-03: this log is
     `/spec-port`'s. The same ruling cut the prose column — the reasoning goes in the commit message,
     which is what `parent` points a reader at.)

     `parent` is the commit the tick was built ON, not the tick itself: a commit cannot contain its
     own hash. `main` is linear, so the change a row measures is that parent's one child:

         git log --ancestry-path --reverse <parent>..main | head -1

     It is a TREND, not a gate — nothing here passes or fails. `exe bytes` is exact and
     bit-reproducible; `build s` and `suite s` are wall clock with a measured ~5% noise band, and a
     movement inside it is not a datapoint. `compile cpu` is TSC ticks summed over the suite's test
     compiles at 12 workers — comparable only within one host and one worker count.
-->

| date | parent | build s | exe bytes | suite s | compile cpu | tests |
| ---- | ------ | ------- | --------- | ------- | ----------- | ----- |
| 2026-08-01 | `21aabd580` | 19.7 | 10,076,672 | 48.0 / 48.9 / 50.4 | — | 3262 |
| 2026-08-01 | `b8f914bfc` | 19.0 | 10,080,768 | 49.6 | — | 3266 |
| 2026-08-01 | `8cc9d4d72` | 18.9 | 10,212,352 | 43.4 | — | 3272 |
| 2026-08-01 | `017cf56a6` | 19.1 | 10,222,592 | 43.5 | — | 3285 |
| 2026-08-02 | `1b6f33a91` | 19.1 | 10,294,784 | 42.5 | 673.9e9 | 3318 |
| 2026-08-02 | `ac9ccbbfd` | 19.7 | 10,359,808 | 43.1 | 669.3e9 | 3379 |
| 2026-08-02 | `74f59ba47` | 19.4 | 10,363,392 | 43.2 | 677.6e9 | 3386 |
| 2026-08-02 | `4c69d2ee9` | 19.5 | 10,341,888 | 43.3 | 675.4e9 | 3388 |
| 2026-08-02 | `5f6db70b1` | 19.5 | 10,414,080 | 46.9 | 769.3e9 | 3422 |
| 2026-08-02 | `4d109bf33` | 19.4 | 10,571,264 | 48.7 | 790.6e9 | 3462 |
| 2026-08-02 | `9c8387a5f` | 20.0 | 10,585,088 | 55.2 | 799.4e9 | 3483 |
| 2026-08-02 | `ff9c825fa` | 20.0 | 10,639,360 | 56.1 | 811.3e9 | 3500 |
| 2026-08-02 | `c0802e309` | 21.1 | 10,646,016 | 59.0 | 825.9e9 | 3514 |
| 2026-08-02 | `1ce6e03c3` | 22.1 | 10,731,520 | 73.7 | 1325.0e9 | 3566 |
| 2026-08-02 | `c76afda44` | 20.8 | 10,731,520 | 74.8 | 1356.0e9 | 3581 |
| 2026-08-02 | `723c8eff7` | 19.8 | 10,787,328 | 77.5 | 1468.5e9 | 3615 |
| 2026-08-02 | `b90fe9d7d` | 19.9 | 10,797,056 | 77.5 | 1446.1e9 | 3620 |
| 2026-08-02 | `70861315c` | 19.6 | 10,797,056 | 78.4 | 1478.2e9 | 3690 |
| 2026-08-02 | `8d19fe576` | 20.0 | 10,841,088 | 78.3 | 1464.3e9 | 3721 |
| 2026-08-02 | `80e241318` | 19.9 | 10,844,160 | 79.0 | 1482.7e9 | 3734 |
| 2026-08-03 | `59b56fda1` | 20.0 | 10,906,112 | 79.6 | 1478.6e9 | 3750 |
| 2026-08-03 | `9d078816a` | 19.5 | 10,924,544 | 79.3 | 1494.1e9 | 3786 |
| 2026-08-03 | `b063f5fa1` | 19.9 | 10,961,920 | 80.9 | 1522.9e9 | 3832 |
| 2026-08-03 | `3b04b2954` | 20.3 | 10,962,432 | 81.7 | 1541.4e9 | 3837 |
| 2026-08-03 | `a8ccd6296` | 20.0 | 11,013,120 | 83.7 | 1573.8e9 | 3884 |
| 2026-08-02 | `37edca184` | 17.2 | 10,733,568 | 48.9 | 785.8e9 | 3557 |
| 2026-08-02 | `ac50430d5` | 18.2 | 10,798,080 | 68.0 | 1186.6e9 | 3591 |
| 2026-08-02 | `6a1ee9940` | 12.4 | 11,714,560 | 40.0 | 183.9e9 | 3400 |
| 2026-08-02 | `ec48af447` | 13.0 | 11,714,560 | 39.6 | 185.8e9 | 3405 |
| 2026-08-02 | `395e98dbc` | 12.5 | 11,714,560 | 39.6 | 186.3e9 | 3405 |
| 2026-08-02 | `b93522385` | 13.1 | 11,714,560 | 40.3 | 188.1e9 | 3475 |
| 2026-08-02 | `7175bfb84` | 12.7 | 11,714,560 | 40.3 | 188.3e9 | 3475 |
| 2026-08-02 | `dbcee82d3` | 16.6 | 10,788,352 | 60.5 | 1291.5e9 | 3618 |
| 2026-08-02 | `e165d464a` | 17.2 | 10,785,792 | 60.3 | 1287.0e9 | 3619 |
| 2026-08-02 | `60d512007` | 16.4 | 10,784,768 | 60.5 | 1276.9e9 | 3619 |
| 2026-08-02 | `14c4ea44b` | 12.6 | 11,714,560 | 40.0 | 187.3e9 | 3484 |
| 2026-08-02 | `2e7b0d813` | 12.6 | 11,714,560 | 40.0 | 186.3e9 | 3484 |
| 2026-08-02 | `c5f12d41f` | 12.3 | 11,763,712 | 40.1 | 188.0e9 | 3526 |
| 2026-08-02 | `d695128e6` | 13.0 | 11,763,712 | 40.2 | 188.8e9 | 3526 |
| 2026-08-03 | `7dfb87891` | 12.4 | 11,763,712 | 40.8 | 188.7e9 | 3540 |
| 2026-08-03 | `167fb2a8a` | 13.0 | 11,763,712 | 40.5 | 189.1e9 | 3540 |
| 2026-08-02 | `90cad446d` | 16.8 | 10,786,304 | 61.5 | 1292.8e9 | 3703 |
| 2026-08-03 | `804767c9f` | 16.3 | 10,786,304 | 60.8 | 1260.9e9 | 3704 |
| 2026-08-03 | `3999c6265` | 11.9 | 11,829,248 | 40.8 | 190.0e9 | 3556 |
| 2026-08-03 | `4fdb8dab2` | 12.9 | 11,829,248 | 41.8 | 187.6e9 | 3557 |
| 2026-08-03 | `4bcafece1` | 16.5 | 10,907,648 | 67 | 1323.2e9 | 3793 |
| 2026-08-03 | `4e988c2ba` | 14.3 | 10,906,112 | 63.9 | — | 3793 |
| 2026-08-03 | `e9d6cc41e` | 16.4 | 10,923,520 | 63.5 | 1340.7e9 | 3812 |
| 2026-08-03 | `103bf2cd8` | 14.0 | 10,924,032 | 63.3 | 1335.6e9 | 3814 |
| 2026-08-03 | `563b4ad21` | 15.8 | 10,961,920 | 68.9 | 1379.2e9 | 3876 |
| 2026-08-03 | `3b79bca9f` | 14.8 | 10,961,920 | 65.4 | 1376.3e9 | 3878 |
| 2026-08-03 | `562a3afd6` | 12.4 | 11,829,248 | 40.9 | 192.0e9 | 3572 |
| 2026-08-03 | `29efe7558` | 16.4 | 11,011,584 | 66.9 | 1375.8e9 | 3895 |
| 2026-08-03 | `c16571530` | 16.6 | 11,012,096 | 66.9 | 1364.0e9 | 3895 |
| 2026-08-03 | `492f2e71b` | 12.7 | 11,927,552 | 44.1 | 206.1e9 | 3688 |
| 2026-08-03 | `469fde0e6` | 11.4 | 11,927,552 | 44.1 | 209.4e9 | 3693 |
| 2026-08-03 | `7285d8bfc` | 13.1 | 11,927,552 | 45.3 | 215.8e9 | 3693 |
| 2026-08-03 | `22ffd6764` | 12.6 | 11,927,552 | 45.1 | 216.0e9 | 3693 |
| 2026-08-03 | `ac6ba330d` | 12.7 | 11,960,320 | 45.6 | 216.2e9 | 3720 |
| 2026-08-03 | `08bb42056` | 12.8 | 11,960,320 | 45.7 | 219.3e9 | 3720 |
| 2026-08-03 | `ad646997b` | 12.1 | 11,960,320 | 49.7 | 229,116,144,174 | 3757 |
| 2026-08-03 | `08c86c387` | 11.8 / 12.9 | 11,960,320 | 50 | — | 3750 |
| 2026-08-03 | `905f80656` | 11.8 / 12.9 | 11,960,320 | 48 | 225.6e9 | 3750 |
**The wall-clock noise band is MEASURED, not assumed.** Three consecutive full-suite runs of the
**identical binary over the identical suite** (2026-08-01, 3262 tests, an otherwise-idle box) read
**48.0 s / 48.9 s / 50.4 s** — a spread of **~5%**. So:

> **⇒ A time movement under ~5% is NOT a datapoint.** Do not write a sentence explaining it. A build
> that goes 19.7 s → 20.4 s has told you nothing. One that goes 19.7 s → 31 s has.

The size columns have no such band. A 4-byte move in `exe bytes` is four real bytes.

*(This is the same argument `.claude/CLAUDE.md` makes about `scale-test`, which is why that instrument
deliberately collects **no** wall time at all. These columns are kept anyway because they answer a
different question — not "does this pass scale" but "is the compiler still cheap to build and test?" —
and because a 10× regression in either is worth catching whatever the noise floor is. Do not mistake
them for the scaling instrument: for per-phase memory and CPU-time trends, `run_scale_test` and
`docs/optimization-log.md` remain the only honest answer.)*

## Where the numbers come from — no stopwatch, no extra runs

All three fall out of work the tick is doing anyway:

```
build     target=shv2   ->  durationMs, and the rawTail's "Wrote <N> bytes code, ..." line
run_spec_test compiler=shv2  ->  durationMs, passed, total
```
plus the on-disk size:
```bash
stat -c '%s' maxon-shv2/.maxon/maxon-shv2.exe
```

⚠ **Record the build of the tree you are COMMITTING**, not an earlier one. A stale binary's numbers
belong to a different commit, and the `.mxdbg` debug sidecar is a separate file — `exe bytes` is the
executable alone.

## The table

`code bytes` is the emitted machine code alone (from the build's own report); `exe bytes` is the whole
PE/ELF on disk, which also carries rdata, data, ucddata, symdata and headers.

⚠ **A row cannot carry the hash of the commit that creates it** — the row is *in* that commit. So the
first column is the commit's SUBJECT, not its hash: `git log --oneline --grep=` finds it, and nothing
has to be filled in afterwards by a second commit that would then need its own row. (The first row is
the exception: it measures a tree that already existed.)

| date | commit | change | build s | exe bytes | code bytes | suite s | compile cpu | tests |
| ------ | -------- | -------- | --------- | ----------- | ------------ | --------- | ------------ | ------- |
| 2026-08-01 | `f3a208b5b` | baseline — the tree `/spec-port` was built against (no compiler change of its own) | 19.7 | 10,076,672 | 9,002,998 | 48.0 / 48.9 / 50.4 | — | 3262 |
| 2026-08-01 | `spec-port: tick 2 — panic` | `panic("…")` routed to the `mrt_panic` runtime (`StdOp.osPanic`), message baked at parse time. Exe +4,096 B (one page); emitted panic BLOCKS shrank 4 instructions → 2 everywhere, but the compiler gained the parse-time baking, so `code bytes` is +3,093. Both times inside the ~5% band ⇒ no reading. ⚠ **ITS TWO SIZE NUMBERS DO NOT DESCRIBE ITS OWN TREE — see the tick-4 row and do not diff against them.** | 19.0 | 10,080,768 | 9,006,091 | 49.6 | — | 3266 |
| 2026-08-01 | `spec-port: tick 4 — function-declaration` | A function SIGNATURE now has to end at its line (`requireSignatureLineEnd`), so `function foo() int` — the `returns` keyword missing — is E2001 at the `int` instead of falling into the body as `E2015 Unsupported: int statement`. Deduplicating the "token's text, or its kind's spelling" fact into one `tokenDisplayText` (3 open-coded readers → 1) paid for the new check and more: **exe −4,608 B, code −4,827 B.** ⚠ **Those deltas are against a MEASURED CONTROL, not against the row above** — see the note under the table. Build and suite times are not comparable to earlier rows either (different bootstrap); both are unremarkable on their own. | 18.9 | 10,212,352 | 9,118,910 | 43.4 | — | 3272 |
| 2026-08-01 | `spec-port: tick 5 — method-calls` | The struct-typed self-field receiver guard narrowed from "is a self-field alias" to **constructibility** (a new whole-program `structTypeIsConstructible` DFS over the struct-typed-field graph), so `inner.get()` compiles where `self.inner.get()` always did. Review then deduplicated the "can this raw slot name an aggregate, and what name?" two-step, which was written **five** tag-guard ways and four name-resolution ways across this one type, down to two helpers. **exe +10,240 B, code +9,550 B** — exact, and directly comparable to the row above (same bootstrap, same machine; this tick re-measured that row’s tree at 9,118,910 code bytes before touching anything). ⚠ Most of that is the new DFS and its `Set with ByteArray`, NOT the dedup, which is call-for-call equivalent: **no committed fragment moved**, so nothing the compiler EMITS changed. Build 18.9 → 19.1 s and suite 43.4 → 43.5 s are inside the measured ~5% band ⇒ **not datapoints**; the suite also grew 13 tests, which a wall-clock comparison cannot separate anyway. | 19.1 | 10,222,592 | 9,128,460 | 43.5 | — | 3285 |
| 2026-08-02 | `spec-port: tick 6 — type-casting` | Two cast rules. **E3009**: `castHasNoLegalConversion` deemed any two different SCALAR classes convertible, so `5.9 as Integer` silently truncated to 5 and `true as Integer` was a complete no-op — the value kept its bool type through to `print`. Only `int → float` legitimately crosses; `float → int`, `bool ↔ int` and `↔ bool` are E3009. **E3010** (`unneeded cast`) did not exist in shv2 at all — newly claimed in `docs/error-codes.txt` and implemented in the parser via a sparse `ValueDeclaredAliasMap` (the ranged typealias a value's declared type names, recorded where the type columns cannot say so) plus a non-throwing recoverable report, which is what the 3-in-one-function and 2-across-functions cases pin. **exe +11,776 B, code +11,602 B**, against a MEASURED CONTROL on this tree's own bootstrap and its own base (stash + rebuild read 10,283,008 / 9,178,709) — not against the row above. ⭐ The same control was run BEFORE the rebase too and read the identical **+11,602 code bytes**, so the figure is the change's own and not the base's. ⚠ **The bootstrap was rebuilt this tick** (regenerating the registry moved `ErrorCode.g.cs`), which the note under the table had asked the next `maxon-sharp` change to do; the pre-rebase control then reproduced the row above's exe **byte-for-byte** at 10,266,624, so the size columns do NOT restart here. **No committed fragment moved by the compiler change** — the six that did move are spec PROGRAMS this tick repaired (`x as int` → `trunc(x)`), and both spellings lower through the same `MaxonUnaryOp.trunc`, so their emitted IR is byte-identical and only the embedded source line differs. Tests 3288 → **3318** (this spec's 30 cases, none shelved). Build 19.0 → 19.1 s and suite 44.4 → 42.5 s are inside the measured ~5% band ⇒ **not datapoints**, and the suite also grew 30 tests and absorbed upstream's W3, which a wall comparison cannot separate anyway. compile cpu 673.9e9 from **2,477** of 3,318 compiles at 12 workers (was 2,467 of 3,288): +10 reporting compiles, +20 non-reporting — 18 of this spec's 30 cases are `compilerError`, which pass by failing to compile and write no metrics. | 19.1 | 10,294,784 | 9,190,311 | 42.5 | 673.9e9 | 3318 |
| 2026-08-02 | `spec-port: lexer-edge-cases` | spec-port tick 7 (lexer-edge-cases): two parser throw-sites re-coded off E2015, two new `ParseError` variants and two `Queries` render arms. Diagnostics only — both sites are throw-only paths, so nothing that compiles today changed shape. | 19.7 | 10,359,808 | 9,243,200 | 43.1 | 669.3e9 | 3379 |
| 2026-08-02 | `spec-port: unknown-keyword-error` | spec-port(unknown-keyword-error): `parseStatement`'s fall-through split in two — a member-access arm keeping E2015 verbatim, and an E2001 `strayToken` arm for a token that begins no statement. A refusal path only; no codegen moved. | 19.4 | 10,363,392 | 9,246,291 | 43.2 | 677.6e9 | 3386 |
| 2026-08-02 | `spec-port: expected-expression-error` | spec-port(expected-expression-error): new `expressionOperandDisplayText` renderer for the E2004 expression path, five `expectedExpression` raisers routed through it. Diagnostic path only; no codegen moved. | 19.5 | 10,341,888 | 9,224,804 | 43.3 | 675.4e9 | 3388 |
| 2026-08-02 | `spec-port: stdlib-basic` | spec-port(stdlib-basic): `stdlib/Math.maxon` added to `StdlibLoader`'s whitelist — one `listWhitelistedModule` line. Loader-only; no pass, IR or backend change, and zero committed goldens moved. | 19.5 | 10,414,080 | 9,289,863 | 46.9 | 769.3e9 | 3422 |
| 2026-08-02 | `spec-port: abs` | spec-port(abs): four mechanisms — the `abs` float intrinsic across both IR tiers and all three targets (new `MaxonUnaryOp`/`StdUnaryOpcode`/`TargetOp` members), the spec harness's `Args:` directive, `CommandLine.args()` (synthesized `StringError`, `String.indexAfter`, new `Runtime/CommandLineRuntime.maxon`, one appended kernel32 IAT slot, `stdlib/CommandLine.maxon` whitelisted), and a reserved-element fix for synthesized `__ManagedMemory` views. Emitted code moves only for programs that call `abs` or `CommandLine`; everything new is gated behind a usage bit and the new IAT slot is appended so no existing slot index shifts. | 19.4 | 10,571,264 | 9,435,826 | 48.7 | 790.6e9 | 3462 |
| 2026-08-02 | `spec-port: unused-parameters` | spec-port(unused-parameters): three compiler changes — the E3012 unused-PARAMETER check (candidate column, `referenced` bit, per-method interface waiver), `_` exempted from the duplicate-parameter guard (E2014), and the defaulted-argument slot channel that replaced a name-keyed lookup (E3038 — one new `argDefaultSlots` column on `call`/`tryCall`/`asyncCall`, threaded through lowering, validation and the two stdlib/runtime scanners). Emitted code is unchanged for every program that already compiled: the check only ADDS a diagnostic, and the slot channel is empty for any call that omitted nothing. | 20.0 | 10,585,088 | 9,449,179 | 55.2 | 799.4e9 | 3483 |
| 2026-08-02 | `spec-port: min` | spec-port(min): shv2's first bare arity-2 builtin — `MaxonBinOp.min` / `StdBinOpcode.min` across both IR tiers and all three backends (x64 `minsd`, arm64 `fcmp`+`fcsel`, wasm `f64.lt`+`select`), plus the register-allocator / liveness / SSA-destruction / live-range-splitting arms for the two new `TargetOp`s; a three-case `ArgLabelRule` replacing a boolean so a builtin call can be all-positional; and new E2067 `ParserBuiltinArgNamed` for a label on a builtin argument, which had been silently dropped. ⚠ **Size FELL 4,608 bytes exe / 5,538 code bytes against the previous row, and size is exact and reproducible, so it is real**: a review fix routed the seven math-intrinsic emit arms through the `calleeTakesPositionalArgsOnly` roster, collapsing seven `equals` tests strung through the dispatch chain into one predicate call and a single consolidated dispatch. Emitted code for existing programs is unchanged — the new ops have one producer each (`min(a,b)`), and the new diagnostic only refuses what was previously mis-accepted. | 20.0 | 10,639,360 | 9,501,178 | 56.1 | 811.3e9 | 3500 |
| 2026-08-02 | `spec-port: max` | spec-port(max): `min`'s mirror — `MaxonBinOp.max` / `StdBinOpcode.max` across both IR tiers and all three backends, added by CONSOLIDATION rather than duplication. `TargetOp.minsdRegReg` became `minMaxSdRegReg(…, kind FloatMinMaxKind)` — one variant with a discriminating field rather than a parallel twin, so no pass gained an arm; new `FloatMinMaxKind` plus `floatMinMaxKindFor` / `floatMinMaxSelectPred` state the min-vs-max decision once for all three isels; arm64 reused `arm64Fcsel` (already carried `cond`) and wasm reused `f64Gt`, so neither gained a `TargetOp` or an emitter. Size rose ~+6.7 KB exe / +5.9 KB code against `min`'s row — the new enum, the two shared mappers, `minMaxSdOpcode`, one roster test and one emit arm. Emitted code for existing programs is unchanged: the new opcodes have exactly one producer each (`max(a,b)`), and the full suite showed zero churn in any pre-existing golden. | 21.1 | 10,646,016 | 9,507,100 | 59.0 | 825.9e9 | 3514 |
| 2026-08-02 | `spec-port: sin` | spec-port(sin): float interpolation end to end, and the subsystem it needed. ~940 lines of float↔text moved OUT of `maxon-shv2/Compiler/NumberParsing.maxon` and INTO `stdlib/Builtins.maxon` (+~1200) as ONE shortest-round-trip algorithm serving four callers — the compiler's literal reader, its diagnostic renderer, the emitted program's `"{x}"`, and `float.fromString`, which converged onto it and deleted its own naive parser. `stdlib/` is the only tree both the compiling compiler and the compiled program reach, which is why the shared home is there rather than in the compiler. Plus three backend defect fixes (compare/branch fusion re-keyed by `BlockRef`; a confined-transient rule for reuse defs; `emitScopeDrops` given an explicit destination), a new `bitcastI64ToF64` opcode across all three backends, and two new registered diagnostics (E2067 was the previous tick's; E2068 `ParserMalformedNumericLiteral` here, claimed by both compilers). ⚠ **Size rose ~+45 KB exe against the previous row and it is REAL, not noise** — size is exact and reproducible. It is the float subsystem becoming reachable from emitted programs for the first time: a program that interpolates a float or calls `float.fromString` now links the exact reader, the bignum core and the shortest-digit search. A program that does neither is unchanged (measured md5-identical for hello-world under both compilers). | 22.1 | 10,731,520 | 9,587,773 | 73.7 | 1325.0e9 | 3566 |
| 2026-08-02 | `spec-port: atan2` | spec-port(atan2): `stdlib/Math.maxon` (accuracy + duplication + IEEE signed zeros) and the C# bootstrap's float constant pool — **no `maxon-shv2/Compiler/` source changed**. `Math.atan` gained a three-fold range reduction so nothing reaches its Taylor series above `tan(π/8)` (it was wrong by ~1e-2 near |z| = 1); seven duplicated math constants collapsed to one definition each, including **two disagreeing spellings of ln 2**; `lnBySeries` and `reduceToPiRange` replace series/loop bodies that were duplicated between `log`/`log2` and `sin`/`cos`; `atan`/`sin`/`tan`/`atan2` now carry IEEE signed zeros. On the bootstrap side, the float constant pool is re-keyed on the IEEE bit pattern (it was keyed by value, and `double.Equals(+0.0, -0.0)` is true, so `-0.0` literals were miscompiled). ⚠ **Every field is byte-identical to the previous row — build ~22 s, exe 10,731,520, code 9,587,773 — and that is the EXPECTED confirmation, not a stale measurement**: the shv2 binary does not link `stdlib/Math.maxon`, and the bootstrap is a different product. Size is exact and reproducible, so identical fields here are a positive result: a stdlib-only change that moved shv2's own binary would have meant something was linked that should not be. What did move is the emitted code of programs that CALL these functions — 26 spec goldens, all read, no printed value changed except the one `pow` expectation the ln-2 correction improves. | 20.8 | 10,731,520 | 9,587,773 | 74.8 | 1356.0e9 | 3581 |
| 2026-08-02 | `spec-port: log` | spec-port(log+log2+log10): IEEE 754 semantics for the logarithms and for `Math.pow`, plus two non-termination guards the ruling itself made reachable, in `stdlib/Math.maxon`; and non-finite handling in the C# bootstrap's `maxon_f64_to_string` on **both** targets (hand-written assembly, one independent copy per target — it printed `--9223372036854775808.775808` for `-inf` and a plausible-looking negative for NaN). **No `maxon-shv2/Compiler/` source changed.** ⚠ **The shv2 binary's numbers are therefore expected to be unmoved, and that is a positive result, not a stale measurement** — shv2 does not link `stdlib/Math.maxon`, and the bootstrap is a different product. Size is exact and reproducible, so a stdlib-only change that DID move shv2's own binary would mean something was linked that should not be. What moved is the emitted code of programs that call these functions: 41 spec goldens, each diff read. One of those diffs was verified rather than assumed — the `atan2` fragments moved only because a helper's relocation changed its slot in the IR dump, confirmed by sorting the file before and after and comparing the multiset (identical, 6,484 lines), with the sole delta being one function's position. | 19.8 | 10,787,328 | 9,637,906 | 77.5 | 1468.5e9 | 3615 |
| 2026-08-02 | `spec-port: exp` | spec-port(exp): **comment-only changes to compiler source** — `StdlibLoader.maxon`'s Math-whitelist note (it declared a wrong answer that has been fixed since `c5e5ac179`) and `StdToX64Conversion.maxon`'s `CmpPredMap` header (it still said "Keyed by ValueId" directly above a `Map with (BlockRef, …)`, the two copies contradicting in the very code whose lossy key was the defect). **No executable change whatsoever**, so every number here should equal the previous row exactly — and that is the point of recording it rather than skipping it: size is exact and bit-reproducible, so a comment-only commit that MOVED the binary would mean something in the build depends on comment text. `exp.md` itself is byte-identical to `/specs` and needed no compiler work. | 19.9 | 10,797,056 | 9,646,432 | 77.5 | 1446.1e9 | 3620 |
| 2026-08-02 | `spec-port: lexer-parser-robustness` | spec-port(lexer-parser-robustness): `\xNN` wired into regular string and character literals, sharing one marker-keyed `DigitEscape` with `\u`, plus three pre-existing duplications collapsed in the escape decoder (two hand-unrolled hex folds into one `digitEscapeValue`, and `appendByteStringEscape`'s two near-identical arms into one). A real front-end change, but a small one confined to literal decoding — no lowering, runtime or op change, so the code-size movement should be modest and any suite-time movement inside the measured ~5% band is not a datapoint. | 19.6 | 10,797,056 | 9,646,361 | 78.4 | 1478.2e9 | 3690 |
| 2026-08-02 | `spec-port: if-try` | spec-port(if-try): the largest compiler change in this loop's recent history — three mechanisms. The `if try` construct (new `IfCondition`/`IfBinder` unions and a catching condition path in `Parser.maxon`), `Character.asciiValue()` (a new `__char_ascii` runtime entry point), and **`Map` as a compiler-owned builtin generic** with a new shared `HashTableRuntime.maxon` (+942) that `SetRuntime.maxon` was cut 839→~200 lines to sit on top of. ⚠ **Expect the code-size column to MOVE and to mean something** — a whole hash-table runtime and a new construct entered the compiler, so unlike a normal tick this row's size delta is a real signal rather than noise. The two times still carry the measured ~5% band; a movement inside it is not a datapoint. | 20.0 | 10,841,088 | 9,686,059 | 78.3 | 1464.3e9 | 3721 |
| 2026-08-02 | `spec-port: closure-self` | spec-port(closure-self): `self` became an ordinary closure capture — the capture path re-keyed from `tok.value` onto `baseBindingName`, so the receiver (bound under `__self`, which no token spells) is reachable at all three doors; plus a diagnostic re-code (E2015→E2001 for `self` outside an instance method) and two review fixes. Two files, ~130 net lines, no new runtime and no lowering change — expect the size column to sit inside the noise it usually does, unlike the previous tick's row. | 19.9 | 10,844,160 | 9,688,336 | 79.0 | 1482.7e9 | 3734 |
| 2026-08-03 | `spec-port: list-custom-types` | spec-port(list-custom-types): `List with <T>` added as a fourth compiler-owned generic container — a new `Runtime/ListRuntime.maxon` (+582: 32-byte chain header, 24-byte nodes, 13 `StdOp` graphs) plus its wiring, the `for … in` cursor form and `List from […]`, and a batch of Array→container renames that consolidated rather than forked. ⚠ **Expect the code-size column to move and to mean something** — a whole new container runtime entered the compiler. `RuntimeUsage` also gained one `bool`, the same +8-bytes-per-compile shape `usesSet` is already logged for. The two times still carry the measured ~5% band. | 20.0 | 10,906,112 | 9,743,971 | 79.6 | 1478.6e9 | 3750 |
| 2026-08-03 | `spec-port: unused-export` — **WITHDRAWN on a user ruling; enum `.ordinal` only** | ⚠ **This row records a tick whose spec did NOT land, and the numbers are for what survived it.** `specs/unused-export.md` was implemented in full — E3092/E3093/E3094, a 919-line whole-program pass, three gates, a consulted-counts-as-referenced rule — and then **removed entirely**, because the user ruled its premise wrong: *an `export` is a public API surface, and a shared module that this app happens not to call has not thereby got a dead symbol.* "No caller in this compilation" is not "dead". The lint, its wiring across four files, and the `shv2` claims on all three codes are gone; the registry hash is back to its pre-tick value and `docs/` is byte-identical. **What remains is enum `.ordinal`** — 145 lines across `Parser.maxon` and `Project.maxon`, a genuine language gap one withdrawn case had needed. It reads the enum off the value's `named(X)` tag through the existing `enumMatchFor` (the same door `match` uses, so the two cannot disagree), emits `<enum> + 0` so the result type comes from `TypeRules.arithResultTag` rather than an assertion, and **refuses a renumbered enum rather than answering it wrong**. ⭐ **It lands PINNED**: `function-overloads/enum-ordinal-argument` was `disabled-test:` blocked solely on `.ordinal` and is now live, which is why an otherwise-unpinned feature could land at all. ⚠ **The size columns are NOT comparable to the row above** — that was an arm64-macOS host, this is x64-windows, and 17 other commits landed in between; read this row against the next x64-windows one, not against its neighbour. Both times sit inside this file's measured ~5% band. **tests 3751 → 3786** is those 17 commits plus this tick's one enabled case. | 19.5 | 10,924,544 | 9,760,701 | 79.3 | 1494.1e9 | 3786 |
| 2026-08-03 | `spec-port: vector` | spec-port(vector): `Vector with <N> <T>` as a fifth compiler-owned container — the first carrying a VALUE in its type identity (a `fixedSizes` column on the instance registry, part of the intern key), plus float and bool container elements made to work across three layers that previously refused or panicked, and E2069. No new runtime file: a Vector IS an `Array` record, so the size cost is front-end only. ⚠ **`rewriteLastCallToTryCall` gained a one-step-back test every `try` in the corpus now goes through** — bounded to a `bitsToFloat` over the immediately preceding op and never below `targetOpFloor`, but it is the widest-reach edit here, so a compile-CPU movement outside the ~5% band would be the thing to look at first. Size movement is real front-end growth; both times sit inside the band. | 19.9 | 10,961,920 | 9,793,664 | 80.9 | 1522.9e9 | 3832 |
| 2026-08-03 | `spec-port: primitive-stringable` | spec-port(primitive-stringable): `.toString()` exposed on primitives — 61 insertions across `Parser.maxon` and `IrInterface.maxon`, no new conversion and no new runtime. The dispatch arm is reachable only through a method name that previously failed to compile, so no existing program's IR can change; expect the size column to move by roughly the cost of one emit helper and a roster append, and both times to sit inside the measured ~5% band. | 20.3 | 10,962,432 | 9,794,539 | 81.7 | 1541.4e9 | 3837 |
| 2026-08-03 | `spec-port: ranged-int-bit-packing` | spec-port(ranged-int-bit-packing): **SUB-BYTE BIT-PACKING** — `PLAN.md:2549`'s ⬜ rung, closed by doing the work (the ladder itself is untouched; it is not this loop's to edit). ~1,435 insertions across 17 files: a signed `element_size@24`, one packed address derivation serving every accessor, a new constant-literal `.rdata` path, and the `applyCast` predicate that keeps a packing cast a conversion rather than a re-tag. ⚠ **The size movement is real front-end + runtime growth and worth reading**; the two times sit inside the measured ~5% band. ⚠ Also expect a **memory** movement in `scale-test` if anyone runs it: the new re-tag predicate mints one value per packing-cast site, measured at **+150 / +178 / +234 / +346 allocations across four rungs — linear, attributable, rung-over-rung ratios unchanged (1.64 / 1.78 / 1.88)**, i.e. a new constant, not a new exponent. | 20.0 | 11,013,120 | 9,840,713 | 83.7 | 1573.8e9 | 3884 |

## ⛔ 2026-08-01 — the first TWO rows' SIZE columns are not comparable to later ones

**Measured, not inferred.** Tick 4 read `exe 10,212,352 / code 9,118,910` where the tick-2 row above
records `10,080,768 / 9,006,091` — a step of **+131,584 / +112,819** that no change could account for,
so it was treated as a reading to explain before anything was written down.

**The control settles it.** `git stash` → build the immediate predecessor tree (`ddefd957e`) → measure:

```
CONTROL (ddefd957e, none of tick 4's work)   exe 10,216,960   code 9,123,737
tick 4  (the tree being committed)           exe 10,212,352   code 9,118,910
                                                  -4,608          -4,827
```

**No commit between the tick-2 row and that control touched `stdlib/` or `maxon-shv2/Compiler/`** (all
five are skill/doc commits), and the bootstrap binary that compiles shv2 was untouched across them. So
the control tree and tick 2's tree must produce the *same* shv2 binary — and the control says that
binary is `9,123,737`, not the `9,006,091` the row claims. ⇒ **The tick-2 row's size columns describe
some other build.** The baseline row above it is low by the same ~117 KB, so both predate whatever
changed.

**Most likely cause, stated as the hypothesis it is:** `bin/maxon.exe` — the bootstrap that *emits*
shv2 — has an mtime of **16:08**, while the rows were written in the evening. A bootstrap refresh
changes shv2's emitted size without a line of shv2 changing, which is the trap already recorded for
`docs/optimization-log.md`. Unverified, because reproducing it means rebuilding the old bootstrap; the
*consequence* needs no verification and is what matters here.

⇒ **Two rules this buys, both cheap:**
1. **A size step you cannot account for gets a CONTROL before it gets a sentence** — stash, build the
   predecessor, measure. It costs one build (~19 s) and it is the only thing that separates "my change
   did this" from "this row is measuring a different compiler."
2. **`exe bytes` / `code bytes` are only comparable across rows built by the SAME bootstrap.** When the
   bootstrap moves, the size columns restart; say so in the row rather than reading the step as a
   regression.

⚠ **Separately, and worth someone's attention:** that bootstrap binary is **older than its own source**
— `bin/maxon.exe` is 16:08, while the last commit touching `maxon-sharp/` (`e4c654236`) is 19:55. Every
shv2 build in this log since then was emitted by a stale bootstrap. That is not this tick's to fix, and
it does not threaten any correctness gate (the suite is green and the goldens are the compiler's own
output either way), but the next `maxon-sharp` change should rebuild it before recording a row.

| 2026-08-03 | `review(shv2 N3): one predicate had two spellings, and the ladder's "zero" was the corpus's` | ⚠ **x64-WINDOWS HOST**, `maxon-n1` clone — compares against the `feat(shv2 N3)` row directly above it (same host, same tree, same bootstrap binary; this commit touches no `maxon-sharp/`). **exe 10,733,568 → 10,733,568, UNCHANGED TO THE BYTE**; code 9,586,364 → 9,586,318, **−46 bytes**, which is the whole of the executable change: the review folded the composite contest predicate `not anyRangeContest() and contestedGenericAliases.count() == 0` — spelled at BOTH `fileScopedInstance` and `rekeyScopedGenericAliases`, and already the cause of one measured N2 regression inside this rung — into a single `anythingContested()`, so one inlined disjunction becomes one call. Everything else in the commit is comment (a bidirectional pointer between the two spellings of the three-tier scoped lookup) and prose (the `docs/optimization-log.md` correction below). ⚠ **BOTH TIMES ARE INSIDE THE MEASURED ~5% BAND** and neither is a datapoint: build 17.2 → 17.2 s, suite 54.0 → 48.9 s over an identical 3557 tests — the suite figure is a differently-loaded box, not a speedup, and it is recorded only because a suite time means nothing without its test count. ⭐ The substantive review output is NOT in this table: `docs/optimization-log.md`'s N3 row claimed the contest cost **exactly zero**, which was the reading of a ladder whose corpus declares no generic-alias name twice. An exact base-vs-HEAD A/B from one path (`--log=compiler:debug`, `signatures` allocations) prices the case the ladder cannot express: **+6** for distinct names against **+29** for two repeated-but-agreeing names, i.e. **~+23 per program that merely repeats a generic-alias name**. The row now says so. | 17.2 | 10,733,568 | 9,586,318 | 48.9 | 785.8e9 | 3557 |
| 2026-08-03 | `feat(shv2 X4): the goldens stop being a gate — a mismatch REPORTS and the exit code never hears about it` | ⚠ **x64-WINDOWS HOST**, `maxon-n1` clone. ⚠⚠ **IT DOES NOT COMPARE AGAINST THE ROW ABOVE IT, AND SAYING SO IS THE POINT:** three commits have landed on `main` since that row (`X1`'s sweep and the `sin`/`cos`/`tan`/`atan2` spec-ports), and `2f8f7b014` among them **changed `maxon-sharp/`** — a bootstrap move restarts the size columns by this file's own rule, and the test count moved 3557 → 3591. So the control was BUILT rather than borrowed: the parent commit's tree and this one, built into the SAME path minutes apart, same host, same bootstrap ⇒ **exe 10,788,864 → 10,798,080 (+9,216 B), code 9,639,058 → 9,647,584 (+8,526 B).** That is the whole executable cost of the change: a second axis on every result (`GoldenDrift`, two arms), a third wire record kind with its encoder/decoder, `FragmentCheck` replacing a `TestOutcome` return from `checkTestFragment`, and the drift report itself. ⭐ **`docs/optimization-log.md` carries the same A/B on the ladder: Δ0, bit-identical allocations and bytes at all six rungs** — this rung adds no code to any compile phase, and the instrument's own `+153,811 …` delta column is measured against the **N3 row of 2026-08-03** and belongs to `main`. ⚠ **THE SUITE FIGURES ARE NOT COMPARABLE TO THE ROW ABOVE AND ARE NOT A REGRESSION:** 48.9 s / 785.8e9 ticks over 3557 tests becomes 68.0 s / 1186.6e9 over 3591 (three runs of this binary read 72.7 / 77.3 / 68.0 s — a ~7% spread, wider than the band this file records and a reason to trust the CPU column over the clock here). The two instruments still agree on the direction and roughly the size (+39% wall, +51% CPU at the same 12 workers), which is what says it is WORK rather than a loaded box — and the work is `main`'s: the four float spec-ports add 34 cases, several of which compile the whole `Math` surface (`tan.multiple-values`'s golden alone is 6,277 lines). Host suite **3591 / 0**, exit 0, no leak. ⛔ **Cross-target RED, and correctly so — that is this rung's end-to-end proof, not its regression:** x64-linux **3420 / 0 with 21 goldens REPORTED as drifted** (it was filed as red-with-9-real-failures), wasm32-wasi **3353 / 15**, every one an `exit code mismatch: expected 0, got 1` in the float family and hand-reproduced outside the harness as a `__shortestRoundTrip` panic. `git status --short specs-shv2/fragments/` shows **no `M` and no addition** — drift never writes. | 18.2 | 10,798,080 | 9,647,584 | 68.0 | 1186.6e9 | 3591 |
| 2026-08-02 | `fix(A3o)` + `review(A3o)` | ⚠ **arm64-macOS HOST**, and the row the A3o fix commit (`6a1ee9940`) owed and did not record — added here by the review, with the fix and the review measured together because the review's own change is spec + docs only. **The size columns RESTART**: this rung's base (`f1baa5786` after rebase; the pair was re-measured at `dc2a39f54`, and first measured at `ddd208843`) is a different bootstrap from the A3p row above, so nothing here is comparable upwards. The comparison that matters is against a **same-tick, same-host control worktree of this rung's own base commit**, built and run beside it. ⭐ **The bootstrap change is CODEGEN-NEUTRAL for shv2, proven by hash rather than argued: both trees emit a BYTE-IDENTICAL `maxon-shv2` — `shasum -a 256` reads `b6c3813e2702dbffbc2dc8d9e9befc73292e6004d712fc4ab59c33fd3ee87ddb` on each — **RE-MEASURED after the rebase onto `dc2a39f54`**, which moved both emitters under both trees and so moved the shared hash away from the `24174cfee0…` this row first recorded; byte-identity is a property of the PAIR, not of the value — so exe 11,714,560 → 11,714,560 and code 10,340,144 → 10,340,144 are identical by construction, not by luck.** That is the expected result: A3o changes how the bootstrap lowers a boxed-union GLOBAL, and shv2's own sources declare none. ⭐ **The C# suite is where this rung's cost actually lands: 3234 → 3242 passed, `3242 verified, 0 written`, exit 0** — the seven fragments the fix added plus the one the review added (`module-union-in-monomorphized-method`), and **zero committed fragments moved**, so the change is additions-only in codegen terms. ⚠ **The shv2 suite reads 3393 passed / 7 failed on this host, and NOT ONE of the 7 can be this rung's: the two trees emit a byte-identical binary and A3o adds nothing to `specs-shv2`, so the result is identical by construction rather than by observation.** Three are the standing file-order blame anchors (`namespace-qualified-resolution`, `global-file-private-same-name`, `typealias-file-scope`); the other **four are REAL and NEW on main — `parsable-interface/parsable.float-fromstring*` exiting 11, i.e. SIGSEGV**, arriving with the same float spec-ports that moved the C# arm64 goldens. They belong to **X5**, not here, and are recorded in this row only because a suite figure means nothing without saying what is red inside it. The run also reports arm64-macOS goldens the suite MINTS but nothing tracks — since X4 that report no longer gates, and it is the standing arm64-lane regen debt, left uncommitted here deliberately because it belongs to that lane and not to a bootstrap fix. Build 11.9 → 12.4 s (control → this tree) is inside the measured ~5% band ⇒ no reading. Suite compile CPU is **183.9e9 ticks from 2472 of 3400 test compiles at 12 workers** — recorded as this row's absolute, NOT as a delta: the pre-rebase pair read 113e9 over a 3288-test suite, and comparing that to this is comparing two different corpora. | 12.4 | 11,714,560 | 10,340,144 | 40.0 | 183.9e9 | 3400 |
| 2026-08-03 | `feat(csharp G11): the cross-OS golden mint is refused, and the row's stated mechanism was already fixed` | ⚠ **arm64-macOS HOST**, and **BOOTSTRAP-ONLY**: the change is four files under `maxon-sharp/` (`Testing/GoldenMintHost.cs`, `Testing/GoldenMintHostSelfTest.cs`, the `--update-required` door in `Program.RunSpecTests`, the golden-write door in `TestRunner.CheckFragmentGolden`, plus the fourth build guard in the csproj). ⭐ **CODEGEN-NEUTRAL FOR shv2 BY MEASUREMENT, NOT BY ARGUMENT: exe 11,714,560 → 11,714,560 and code 10,340,144 → 10,340,144, identical to the byte against the `fix(A3o)` row directly above** (same host, same shv2 sources, stdlib cache deleted before the build so nothing was reused). That is the expected result — nothing here touches a lowering, an emitter or the stdlib; the whole change lives in the spec RUNNER and the CLI. ⚠ **Build 12.4 → 13.0 s is inside the measured ~5% band and is not a datapoint.** The suite figure is **39.6 s over 3405 tests** against the row above's 40.0 s over 3400: the test count moved because `main` ported `exp.md` between the two rows, not because of anything here, and the two times are one clock tick apart on a band of ~2 s. Compile CPU **185.8e9 ticks from 2477 of 3405 test compiles at 12 workers** — an absolute, not a delta (the row above reads 183.9e9 over a 3400-test corpus, a different corpus). shv2 suite **3398 passed / 7 failed**, the identical 7 `main` carries (3 file-order blame anchors + 4 `parsable-interface/parsable.float-fromstring*` exiting 11, row X5); C# suite **3242 passed / 3242, `3242 verified, 0 written`, exit 0**, unchanged from this base's baseline. | 13.0 | 11,714,560 | 10,340,144 | 39.6 | 185.8e9 | 3405 |
| 2026-08-03 | `review(G11): the refusal had a THIRD door, in shv2, and it needed no flag` | ⚠ **arm64-macOS HOST**, and it compares against the `feat(csharp G11)` row directly above it — same host, same tree, same bootstrap. **This is the first G11 row that touches `maxon-shv2/` sources, so the size columns MOVE and the movement is the whole point: code 10,340,144 → 10,341,644, +1,500 bytes**, which is `requireRunnerAvailable` gaining a `probeInstalled bool` parameter, an early return, and one extra call site in `Main.runSpecTest`'s worker branch. **exe 11,714,560 → 11,714,560, unchanged** — the delta is inside the section alignment, so the file size is not a second reading of the same fact. ⭐ **The change closes a MEASURED cross-OS mint that the row above declared impossible:** `spec-test --worker-persistent --target=x64-windows`, driven by hand with one `JOB:singles:` line on this Mac, re-minted a deleted `specs-shv2/fragments/x64-windows/…/let-rhs-missing.test` **with no flag, no tree lock and no runner** — the worker branch returns before the parent's preflight, and a `maxoncstderr` case reaches the golden writer without ever launching a binary, so nothing downstream refused it. After: the same command panics naming both targets and writes nothing. ⚠ **Both times are inside the measured ~5% band and neither is a datapoint:** build 13.0 → 12.5 s, suite 39.6 → 39.6 s over an identical 3405 tests. Compile CPU **186.3e9 ticks from 2477 of 3405 compiles at 12 workers** against the row above's 185.8e9 on the identical corpus — 0.3%, inside the band. shv2 suite **3398 passed / 7 failed**, the identical 7 (3 file-order blame anchors + 4 `parsable-interface/parsable.float-fromstring*`, row X5), exit 1 not 101. C# suite **3242 passed / 3242, `3242 verified, 0 written`, exit 0**, `dotnet build` exit 0 / 0 warnings / all four guards OK. | 12.5 | 11,714,560 | 10,341,644 | 39.6 | 186.3e9 | 3405 |
| 2026-08-03 | `feat(csharp G12): a cross-OS spec run REPORTS instead of aborting` | ⚠ **arm64-macOS HOST**, and **BOOTSTRAP-ONLY**: every changed line is under `maxon-sharp/Testing/` or in `Program.cs`'s spec-test reporting and command dispatch — **`maxon-sharp/Compiler/` and `stdlib/` are untouched** (`git diff --stat b93522385 -- maxon-sharp/Compiler stdlib` is empty). ⚠⚠ **THE SIZE COLUMNS DO NOT COMPARE AGAINST THE G11 ROWS ABOVE**, and saying so is the point: `main` moved between them and this base (`b93522385`) — the suite is **3405 → 3475 tests** and `code` reads 10,341,644 → 10,338,088, a **−3,556 B that belongs to `main`, not to this row**, since nothing here can reach a lowering. ⭐ **CODEGEN NEUTRALITY IS MEASURED, over 3,242 programs rather than one hash: the C# suite reads `3242 verified, 0 written` with `git status --short specs/ specs-shv2/` EMPTY**, so every fragment this compiler emits is byte-identical to what the tree already committed. ⭐ **THE RUNG'S OWN RESULT IS THE CROSS-OS RUN, which had no numbers before because it did not finish:** `spec-test --target=x64-windows` on this Mac died **exit 134** with an unhandled `Win32Exception (13) Permission denied` out of `ProcessSpecBatch` at the first test binary it tried to launch. It now completes — **526 passed, 2719 not run here (total 3245), and `3237 verified, 0 written`** — so a host that cannot execute one x64 binary still gates every committed x64-windows golden byte for byte. The 8 refusals it reports are `module-level-union-var`'s goldens, which the x64-windows lane **does not have** (row A3o landed them on arm64 only); G11's mint rule correctly refuses to invent them here. ⚠ **Both times are inside the measured ~5% band and neither is a datapoint:** build 12.5 → 13.1 s, suite 39.6 → 40.3 s — over 3405 vs **3475** tests, which is a different corpus and so not a comparison at all. Compile CPU **188.1e9 ticks from 2484 of 3475 test compiles at 12 workers**, an absolute and not a delta. shv2 suite **3468 passed / 7 failed**, the identical 7 `main` carries (3 file-order blame anchors + 4 `parsable-interface/parsable.float-fromstring*` exiting 11, row X5), exit 1 not 101. `dotnet build` exit 0 / 0 warnings / **all five guards OK** — the fifth, `spec-run-selftest`, sabotage-proven in both directions. | 13.1 | 11,714,560 | 10,338,088 | 40.3 | 188.1e9 | 3475 |
| 2026-08-03 | `review(G12): the fifth guard restated the rule it guards` | ⚠ **arm64-macOS HOST**, and it compares against the `feat(csharp G12)` row directly above it — same host, same tree, same base (`b93522385`). ⭐ **THE CHANGE IS ONE FILE AND ZERO PRODUCTION LINES: `maxon-sharp/Testing/SpecRunSelfTest.cs` only, so `exe 11,714,560` and `code 10,338,088` are unchanged to the byte, as they must be** — nothing here can reach a lowering, an emitter or the stdlib, and the C# suite's `3242 verified, 0 written` says the same thing over 3,242 programs. ⭐ **WHAT IT FIXES: the fifth build guard derived its own expectation from `host.Os == target.Os`, which is a SECOND COPY of `TargetRunHost.WhyCannotRun`'s predicate** — the exact shape this project keeps getting bitten by, and one both the guard's docstring (*"repeating it here would be the same fact written down twice"*) and the csproj comment (*"deliberately does NOT re-check its rows"*) already claimed it did not have. **MEASURED, not argued**: teaching `WhyCannotRun` a windows runner — the one future change its own docstring promises *"every consequence below follows without being told"* — left the RUNNER following correctly and the GUARD failing, with the sentence *"This host CANNOT run that target"* that the rule no longer said. It now asks the rule. **All three sabotages the guard exists for still fire, re-proven after the change**: `Uncomparable` collapsed to always-`NotRunHere` fails naming `arm64-macos`; collapsed to always-`Fail` fails naming `x64-windows`; and the `HostCannotRun` wiring cut fails naming `x64-windows`. A rule change now propagates instead, leaving `golden-mint-selftest` — the table's one oracle — as the single place a rule change has to be reviewed. ⚠ **Both times are inside the measured ~5% band and neither is a datapoint:** build 13.1 → 12.7 s, suite 40.3 → 40.3 s over an identical 3475 tests. Compile CPU **188.3e9 ticks from 2484 of 3475 compiles at 12 workers** against the row above's 188.1e9 on the identical corpus — 0.1%, inside the band. **GATES:** `dotnet build` exit 0 / 0 warnings / all five guards OK · C# **3242 passed / 3242, `3242 verified, 0 written`, exit 0** · cross-OS `--target=x64-windows` **526 passed, 2719 not run here (total 3245), `3237 verified, 0 written`, exit 1** · shv2 **3468 passed / 7 failed**, main's identical 7 (row X5), exit 1 not 101 · `git status --short` clean but for the one changed file. | 12.7 | 11,714,560 | 10,338,088 | 40.3 | 188.3e9 | 3475 |
| 2026-08-03 | `feat(shv2 X5): an operand's storage width is not the operation's width — the wasm float printer was miscompiled, not wrong` | ⚠ **x64-WINDOWS HOST**, `maxon-n1` clone; this commit touches no `maxon-sharp/`, so it shares the bootstrap with the `X4` row above and the size columns ARE comparable. ⭐ **SIZE IS DOWN, AND IT WAS MEASURED WITH A CONTROL RATHER THAN READ OFF THE ROW ABOVE:** the parent commit's tree and this one, built into the SAME path minutes apart ⇒ **code 9,647,725 → 9,638,291 (−9,434 B)**, exe 10,798,080 → 10,788,352 (−9,728 B). The change is a NET DELETION: `binOpImm`/`cmpImm` gain one `StdType` field and every backend arm is updated, but the wasm backend LOSES its whole dependent-width mechanism (`DependentLhsMap`, `recordDependentValtype`, `resolveDependents`, `widthOfValue`, the two queue columns and the second walk) plus two now-unemittable opcodes (`i32.div_s`/`i32.rem_s`). ⚠ The logged `X4` row reads `9,647,584` where the control tree measures `9,647,725` — a 141 B gap between the row and the tree it names, which is why the control was built rather than borrowed (this file's rule 1). ⭐ **`docs/optimization-log.md` carries the same A/B on the ladder: Δ0, byte-identical allocations at all six rungs** — no compile phase gained or lost work, and the `+7,474`-per-rung the instrument prints is CONSTANT across a doubling ladder, so it cannot be a term in anything the ladder scales; it belongs to the stale row it is measured against. ⚠ **THE SUITE TIME IS NOT A DATAPOINT AND THE TEST COUNT MOVED ANYWAY:** 68.0 s / 3591 becomes 60.5 s / **3618** (+27 cases from `main`'s spec-ports, +3 authored by this rung). Host **3618 / 0**, exit 0, no leak. **Cross-target GREEN on every LOCAL lane: x64-linux 3447 / 0, wasm32-wasi 3395 / 0** — the lane that motivated the rung went **3366 / 26 → 3395 / 0** — with the two arm64 lanes SKIPPED (remote, not requested). ⭐ **`git status --short specs-shv2/fragments/` shows additions only and NO `M`, and no run on any of the three lanes reported a single drifted golden** — the emitted x64 code is byte-identical, which is the whole proof that a shared IR change fixed wasm without touching the native targets. | 16.6 | 10,788,352 | 9,638,291 | 60.5 | 1291.5e9 | 3618 |
| 2026-08-03 | `review(shv2 X5): a narrow operand type is a STORAGE class, and wasm was reading it as an arithmetic one` | ⚠ **x64-WINDOWS HOST**, `maxon-n1` clone; touches no `maxon-sharp/`, so the size columns are comparable to the `X5` row directly above and the comparison is against that row's own tree, rebuilt into the SAME path minutes earlier as the RED control ⇒ **code 9,638,291 → 9,636,082 (−2,209 B)**, exe 10,788,352 → 10,785,792 (−2,560 B). A NET DELETION again, and of the same kind: the wasm backend stops SELECTING a width for integer ops (`opComputeValType` answers the machine word for every one), so `emitBinOpcode`'s and `emitCmpPred`'s i32 arms go, and with them twelve now-unemittable `WasmBinary` encoders (`i32.mul`, the bitwise pair, the three shifts, the six compares). ⚠ **THE SUITE TIME IS NOT A DATAPOINT:** 60.5 → 60.3 s, inside this file's measured ~5% band, over 3619 tests (+1 authored here). Host **3619 / 0**, exit 0, no leak. **Cross-target: wasm32-wasi 3396 / 0, x64-linux 3448 / 0**, arm64 lanes SKIPPED (remote). ⛔ **AND ONE CORRECTION TO THE ROW ABOVE, MEASURED:** it claims "no run on any of the three lanes reported a single drifted golden". The x64-linux lane reports **~21 drifted goldens** (`pow`/`sin`/`tan`/`log*`/`stdlib-*`: `x64.prologue 32` vs `48`, `Math.sin` vs `Math.reduceToPiRange`, `__str_blob_186` vs `185`) — and it is **PRE-EXISTING**, not this review's: the parent commit's own source was stashed, shv2 rebuilt from it, and `spec-test --target=x64-linux --filter=pow/` reproduced the identical list. It is the same drift `X4`'s row recorded as 21, still unresolved; drift does not fail a run, which is how a rung closed while asserting it was absent. | 17.2 | 10,785,792 | 9,636,082 | 60.3 | 1287.0e9 | 3619 |
| 2026-08-03 | `review(shv2 X5) #2: the one coercion door, spelled four times — and three copies were the weaker one` | ⚠ **x64-WINDOWS HOST**, `maxon-n1` clone; touches no `maxon-sharp/`, so the size columns are comparable to the two `X5` rows above. ⭐ **A NET DELETION AGAIN, and a small one because the change is almost entirely SUBTRACTION: code 9,636,082 → 9,635,757 (−325 B)**, exe 10,785,792 → 10,784,768 (−1,024 B). Three hand-rolled `localGet` + `i32WrapI64` copies in the wasm backend (`appendRunEntry`, `emitDispatchCondBranch`, `emitSwitchIndex`) go through the ONE door (`coerceOnStack`/`pushOperandAt`); `emitSwitchIndex` itself disappears (it became a thin wrapper) along with the hoisted `subjectIsWide` flag, and the `i64Eqz` encoder goes with them — the 13th unemittable encoder, left behind by the previous commit's twelve-encoder sweep under a comment naming two callers that do not exist. The only ADDITION is one threaded field: `lowerCmpImm` on x64 and arm64 now READ the `operandType` X5 gave the op instead of restating it as `StdType.i64` / `isFloat: false`. ⚠ **THE TIMES ARE NOT DATAPOINTS:** build 17.2 → 16.4 s and suite 60.3 → 60.5 s over an identical 3619 tests, both inside this file's measured ~5% band. Host **3619 / 0**, exit 0, no leak. **Cross-target: wasm32-wasi 3396 / 0, x64-linux 3448 / 0**, arm64 lanes SKIPPED (remote; the arm64 arm was reviewed by READING the encodings — `Arm64OpAddImm` 0x91…, `Arm64OpSubImm` 0xD1…, `Arm64OpCmpImm` 0xF1…, `Arm64OpMul` 0x9B…, `Arm64OpSdiv` 0x9AC…, every one `sf`-set, i.e. the 64-bit X form the dropped-width comment claims). ⭐ `git status --short specs-shv2/fragments/` is EMPTY — no `M`, no addition — and both wasm coercion sites were seen RED BY SABOTAGE (pushing at i64 instead of i32 takes `match-dispatch` to 10/12 and `short-circuit` to 0/23), so the emitted code is byte-identical AND the paths are demonstrably live. ⛔ The substantive finding is prose, not bytes: `coerceOnStack`'s load-bearing invariant is a claim about the TYPE and is false of the VALUE — `return -1` from a `returns ExitCode` function compiles everywhere and reads -1 on the host against 4294967295 on wasm, through a DIRECT call. Same root as X5's filed residual; the header now records the measurement. | 16.4 | 10,784,768 | 9,635,757 | 60.5 | 1276.9e9 | 3619 |
| 2026-08-03 | `fix(shv2 A3r)` — a byte-string literal byte the element's declared range cannot hold was a SILENT WRONG ANSWER | ⚠ **arm64-macOS HOST**, base `dbcee82d3`, and it compares against the `review(G12)` row directly above it — same host, same `maxon-shv2/` sources at the base, and **the bootstrap was rebuilt in this worktree from the base's own `maxon-sharp/`** (`dotnet build` exit 0, 0 warnings, all five guards OK) rather than borrowed, so the code column is a like-for-like read. **code 10,338,088 → 10,346,200, +8,112 B**, which is the whole executable cost of the change: a ninth `ParseError` arm and its `Queries` renderer, a third out-of-range sentence in `TypeRules`, the `ProgramSignatures.arrayElementAliasOf` door, and the per-byte walk. **exe 11,714,560 → 11,714,560, UNCHANGED TO THE BYTE** — the delta fits inside the section alignment, so the file size is not a second reading of the same fact. ⚠ **Build 12.7 → 12.6 s and suite 40.3 → 40.0 s are both inside the measured ~5% band and neither is a datapoint**; the suite figure is over **3475 → 3484** tests, which is this rung's own nine cases and not a speedup. Compile CPU **187.3e9 ticks from 2488 of 3484 test compiles at 12 workers** against the row above's 188.3e9 over 3475 — a different corpus, so an absolute and not a delta. ⭐ **`git status --short specs-shv2/fragments/` shows NINE ADDITIONS AND NO `M`**, so every pre-existing golden is byte-identical: the refusal is reached only by a program that was already wrong, and nothing the corpus compiles changed shape. ⚠⚠ **`scale-test` IS STRUCTURALLY BLIND TO THIS RUNG AND ITS Δ0 MUST NOT BE READ AS A COST** — measured, not assumed: `--emit-corpus` contains **zero** `b"…"` literals and `stdlib/` contains zero real ones (its five `grep` hits are `b"…"` inside prose), so no rung of the ladder executes one line of the changed path in any of the three columns. The added work is per LITERAL and per BYTE OF THAT LITERAL — one `arrayElementAliasOf` (one instance-arg read, one interner read, at most three map probes, one boxed `DeclaredAlias`) plus one pass over the blob — and it removes one `internArrayByteInstance` call per literal by folding the stride and the bounds onto ONE instance read. Linear in source text; no index gains a column and no data structure the compiler indexes by is touched. **GATES:** `build maxon-shv2` exit 0, error-codes OK · shv2 **3477 passed / 7 failed**, the identical 7 `main` carries at this base (3 file-order blame anchors + 4 `parsable-interface/parsable.float-fromstring*` exiting 11, row X5), exit 1 not 101 · C# suite NOT re-run and not owed: `git status` shows no file under `maxon-sharp/`. | 12.6 | 11,714,560 | 10,346,200 | 40.0 | 187.3e9 | 3484 |
| 2026-08-03 | `review(A3r)` — three headers named functions the rung had just deleted, and one claimed an open hole that had closed | ⚠ **arm64-macOS HOST**, independent review of `58330256f`, and it compares against the `fix(shv2 A3r)` row directly above it — same host, same tree, same base (`dbcee82d3`). ⭐ **THE CHANGE IS COMMENTS ONLY, IN TWO FILES, AND THE SIZE COLUMNS PROVE IT: `code 10,346,200 → 10,346,200` and `exe 11,714,560 → 11,714,560`, IDENTICAL TO THE BYTE**, which is the whole claim a comment-only commit has to make; `git status --short specs-shv2/fragments/` is EMPTY, so not one golden moved either. ⭐ **WHAT IT FIXES — the project's own signature bug, in prose:** the rung collapsed `requireByteStringBlobStridesLikeItsType` + `requireConstByteStringBlobStridesLikeItsType` + `byteElementDeclaredStride` into one rule function and **three headers elsewhere went on naming the deleted three** (`Parser.maxon:4390`, `SignatureIndex.maxon:7389`, `:7556`) — a grep for the rule now lands on nothing. Worse, `byteBufferBoundaryAdmits`'s **"WHAT IS STILL *NOT* CLOSED"** section still filed the `b"…"` narrow twin as an OPEN entrance *"filed with its reproduction as its own rung"* — the very rung the commit above it IS. A header that says a hole is open when it is shut is how the next editor re-opens it. ⚠⚠ **AND THAT SECTION IS NOW TRUE AGAIN BECAUSE A DIFFERENT ENTRANCE IS STILL OPEN, MEASURED BY THIS REVIEW AND LEFT FOR THE COORDINATOR:** `String.toByteArray()` / `.bytes()` (`Parser.parseByteView`) types its record with `internArrayByteInstance(<reading file>)` — the USER's `Byte` — and fills it with the receiver's raw UTF-8, so it never mints `__ManagedByte` and R4.7's boundary door never sees it. **Under `typealias Byte = int(0 to 100)`, `takes("ß".toByteArray())` into `takes(b Array with Byte)` returns 195 — exit 195, no diagnostic, on `origin/main` AND on this branch.** Not a `b"…"` question and unreachable by A3r's per-byte compile-time check (the bytes are runtime data); its cure changes user-visible typing corpus-wide, so it is written into the door's header rather than ridden along. ⚠ **Build 12.6 → 12.6 s and suite 40.0 → 40.0 s over an identical 3484 tests are inside the measured ~5% band and are not datapoints.** Compile CPU **186.3e9 ticks from 2488 of 3484 test compiles at 12 workers** against the row above's 187.3e9 on the identical corpus — 0.5%, inside the band. **GATES:** `dotnet build maxon-sharp` exit 0 / 0 warnings / **all five guards OK** · `build maxon-shv2` exit 0, error-codes OK · shv2 **3477 passed / 7 failed**, the identical 7 `main` carries at this base (3 file-order blame anchors + 4 `parsable-interface/parsable.float-fromstring*` exiting 11, row X5), **exit 1 not 101**, and **no `note:` golden-drift block** · `git status --short specs-shv2/fragments/` EMPTY · `maxon-sharp/` untouched, so the C# suite is not owed. | 12.6 | 11,714,560 | 10,346,200 | 40.0 | 186.3e9 | 3484 |
| 2026-08-03 | `fix(shv2 A4a)` — a RAW-BYTE FILL of an element too narrow to hold a byte was a SILENT WRONG ANSWER at three producers | ⚠ **arm64-macOS HOST**, base `80e241318`, and the code column is a like-for-like read against **this worktree's own base build**, measured before a line was edited (same host, same path, bootstrap rebuilt here from the base's own `maxon-sharp/`, `dotnet build` exit 0 / 0 warnings / all five guards OK): **code 10,387,976 → 10,394,120, +6,144 B.** That is the whole executable cost: a tenth `ParseError` arm and its `Queries` renderer, a fourth out-of-range sentence in `TypeRules`, one two-function rule in `Parser`, and three call sites. The exe column is **11,763,712** and has **no comparable base reading** — the base exe was never `stat`ed, and the row above it was minted at a different base — so it is an absolute here, not a delta. ⚠ **Build 12.6 → 12.3 s and suite 40.1 s over 3515 → 3526 tests are inside this file's measured ~5% band and neither is a datapoint**; the test count is this rung's own ELEVEN cases. Compile CPU **188.0e9 ticks from 2521 of 3526 test compiles at 12 workers** — a different corpus from any earlier row, so an absolute. ⭐ **`git status --short specs-shv2/fragments/` shows ELEVEN ADDITIONS AND NO `M`**: every pre-existing golden is byte-identical, which is what a pure REFUSAL should look like. The suite's `note:` block lists **3 drifted goldens and all three are PRE-EXISTING**, proven by provenance rather than asserted — `generic-types`'s two were last minted `5bafc9bc0` (2026-07-27) while `specs-shv2/generic-types.md` was edited `14c4ea44b` (2026-08-02), and the `builtin-member-rosters` one was minted `383cab13e` (2026-08-01) before the same commit added the `asciiValue` arm its text is missing. ⚠⚠ **`scale-test` CAN see ONE of this rung's three sites and is BLIND to the other two — measured, not assumed.** `--emit-corpus` contains `y_bufsurf.maxon`, which declares `typealias Byte = int(0 to u8.max)` and calls `toByteArray`, so the byte-view gate DOES execute in every rung; `setByte` and `__ManagedFile` appear in **zero** corpus files, so those two clauses execute nowhere in the ladder and a Δ0 through them would be the instrument's blind spot. The added work is one `arrayElementAliasOf` per gated site (one instance-arg read, one interner read, at most three map probes, one boxed `DeclaredAlias`) — linear in source text, no index gains a column, no data structure the compiler indexes by is touched; every phase ratio read ×1.9–×2.1 and `regalloc:liveness` ×2.18, its documented two-call-site sum. **No `--note` row was written to `docs/optimization-log.md`**: the only comparison row available is a Windows tree at a 106-character path against this 140-character one, and the instrument itself WITHHELD the byte delta for that reason — a row measured against an incomparable baseline would pollute the trend rather than extend it. **GATES:** `dotnet build maxon-sharp` exit 0 / 0 warnings / all five guards OK · `build maxon-shv2` exit 0, error-codes OK 167 codes · shv2 **3519 passed / 7 failed**, the identical 7 `main` carries at this base (3 file-order blame anchors + 4 `parsable-interface/parsable.float-fromstring*` exiting 11, row X5), **exit 1 not 101** · `wasm32-wasi --filter=bytearray-element-size` **37 / 0**, which is what proves the `__ManagedFile.read` case needs no `targets:` marker · C# suite **3242 / 3242, `3242 verified, 0 written`, exit 0** and `git status --short specs/` EMPTY. | 12.3 | 11,763,712 | 10,394,120 | 40.1 | 188.0e9 | 3526 |
| 2026-08-03 | `review(A4a)` — the `setByte` stride precondition was described as a proof and is a boundary; three measured wrong answers behind it | ⚠ **arm64-macOS HOST**, independent review of `ca5eb4d60`, compared against the `fix(shv2 A4a)` row directly above it — same host, same worktree, same base (`80e241318`). ⭐ **COMMENTS AND ONE REGENERATED REGISTRY DOC ONLY, AND THE SIZE COLUMNS PROVE IT: `code 10,394,120 → 10,394,120` and `exe 11,763,712 → 11,763,712`, IDENTICAL TO THE BYTE** (`symdata 640,168 → 640,400` and the `.mxdbg` moved, which is line numbers shifting under added comment lines and is the only thing that may move); `git status --short specs-shv2/fragments/` shows the rung's eleven additions and **no `M`**. ⭐⭐ **THE LOAD-BEARING CLAIM WAS REPRODUCED ON A CONTROL BUILT AT `80e241318` AND IT HOLDS, so the row's own suggested cure was the wrong one**: under `typealias Byte = int(0 to 1000)` the byte view is genuinely STRIDE 2 and `__str_to_bytes` fills it element-wise — measured through `.managed.byteAt` as `raw0=97 raw1=0 raw2=98`, which is stronger than the `b.get()` reading the rung recorded — while a stride-1 `__ManagedByte` buffer at a declared `Array with Byte` position is refused `E3005 … expected 'Array_Byte$0_1000', got 'Array___ManagedByte'`. Retyping the view onto `__ManagedByte` would therefore have turned a correct program into a compile error. The same control reproduces the defect (**exit 195, silent**) and the drift note (**3 fragments, byte-identical blocks, 3508/7**), so the rung's *"all three drifts are pre-existing"* is now a MEASURED CONTROL rather than a provenance argument. ⛔⛔ **WHAT THIS COMMIT FIXES IS A FALSE INVARIANT IN PROSE, WHICH IS THE ONE THING THIS PROJECT LETS NOBODY LEAVE**: the `setByte` arm and the E3117 registry doc both said the stride precondition skips *"the byte-level staging the buffer surface exists for"*. Staging is honest only where every bit pattern of the SLOT is a value the element admits — true of `Array with Int`, and FALSE of three shapes the skip lets through, each MEASURED silent on this tree: a ranged element wider than a byte (`int(0 to 1000)`, `setByte(1, 223)` reads back **57089**), a 4-byte `ExitCode` element (`setByte(3, 223)` → **3741319169**), and a `bool` element (`setByte(0, 223)` makes `v` and `not v` both true). They ask *"does the element admit every bit pattern of its slot?"*, which is E3117's question only at a one-byte slot, and they are FILED rather than fixed because the prerequisite is a different door: `__ManagedMemory.create(n, 1)` written in a file whose `Byte` is two bytes is TYPED stride 2 over a record stamped stride 1 (`set(0, value: 300)` reads back **44**) while the same file's `b"…"` literal is already refused for that identical incoherence — one producer refused, its twin accepted, and settling it changes which programs compile. ⚠ **Build 12.3 → 13.0 s and suite 40.1 → 40.2 s over an identical 3526 tests are inside this file's measured ~5% band and are not datapoints.** Compile CPU **188.8e9 ticks from 2521 of 3526 test compiles at 12 workers** against the row above's 188.0e9 on the identical corpus — 0.4%, inside the band. **GATES:** `dotnet build maxon-sharp` exit 0 / **0 warnings** / all five guards OK · `error-codes generate` rewrote `ErrorCodeRegistry.maxon` + `error-codes.json`, `build maxon-shv2` exit 0 with **error-codes OK 167 codes, 4 generated files up to date** · shv2 **3519 passed / 7 failed**, `main`'s identical 7 (3 file-order anchors + 4 `parsable-interface/parsable.float-fromstring*`, row X5), **exit 1 not 101** · `wasm32-wasi --filter=bytearray-element-size` **37 / 0**, re-run rather than trusted, which is what licenses the missing `targets:` marker on the `__ManagedFile.read` case · C# suite **3242 / 3242, `3242 verified, 0 written`, exit 0**, `git status --short specs/` EMPTY. | 13.0 | 11,763,712 | 10,394,120 | 40.2 | 188.8e9 | 3526 |
| 2026-08-03 | `fix(shv2 A4b)` — `append`'s argument is the ninth coercion site; a SHAPE test where its eight neighbours ask identity | ⚠ **arm64-macOS HOST**, measured on the tree being committed, against the `review(A4a)` row directly above it (same host, same worktree, same base `59b56fda1`). ⭐ **`code` − 3,332 B (10,394,120 → 10,390,788), `exe` UNCHANGED at 11,763,712** — the compiler got SMALLER while gaining a rule, because the rule it gained replaced more than it added: `requireArrayAppendArg`'s four hand-written interpolated sentences and `requireManagedMemoryAppendArg`'s open-coded two-part test collapse into one shared predicate (`ProgramSignatures.arrayAppendArgAdmits`) and two `builtinArgumentType` throws, and three interpolated message strings stop being emitted at all. `exe` is page-granular and did not move. **Both times are inside the measured ~5% band and are NOT datapoints** (build 13.0 → 12.4, suite 40.2 → 40.8); the CPU column likewise (188.8e9 → 188.7e9). **tests 3526 → 3540**: this rung adds ELEVEN cases, ten of which run on this host (the eleventh is `targets: x64-windows`, verified here by cross-compiling to PE by hand rather than by running). ⚠ `scale-test` was run and READ and no row was written to `docs/optimization-log.md`: the last row there is a Windows run at a different path, so no absolute comparison is honest — what it says is that every phase including `phase:parse` (the only one this touches) is linear at x1.93 allocations / x2.02 bytes / x1.98 CPU. The new rule allocates NOTHING on the corpus's appends, which are same-instance and settle on the `destGiid == sourceGiid` compare before any bounds are read. | 12.4 | 11,763,712 | 10,390,788 | 40.8 | 188.7e9 | 3540 |
| 2026-08-03 | `review(A4b)` — the domain rule was one-way, and a `currentPath()` buffer taking a byte array stopped compiling | ⚠ **arm64-macOS HOST**, independent review of `bf319037a`, compared against the `fix(shv2 A4b)` row directly above it — same host, same worktree, same base (`59b56fda1`), and the bootstrap was NOT rebuilt between the two rows (`git diff --stat 59b56fda1..HEAD -- maxon-sharp/` is EMPTY, so the C# suite is not owed and the code column is like-for-like). ⭐ **`code` 10,390,788 → 10,390,936, +148 B; `exe` UNCHANGED at 11,763,712.** That is the whole executable cost: one `ElementIntegerRange` arm, two named bounds, minus one `byteBufferBoundaryAdmits` call. ⛔⛔ **WHAT IT FIXES IS AN OVER-REFUSAL THE RUNG INTRODUCED, MEASURED AGAINST A CONTROL BUILT AT `59b56fda1` IN A SECOND WORKTREE** — the exact MIRROR of the acceptance case the rung itself added. `var cwd = try __ManagedDirectory.currentPath() …; try cwd.append(<an Array with Byte>)` under `typealias Byte = int(0 to u8.max)` **compiled at the base and stopped compiling** (`E3005 … expected '__ManagedMemory', got 'Array_Byte'`), and so did the `int(0 to 100)` and `b"…"`-literal forms — three correct programs, since a buffer the compiler minted is a run of RAW bytes and every value of those elements is a byte. **CAUSE: `elementIntegerValueRange` answered `notIntegerValued` for `__ManagedByte`**, so the domain test could only answer the direction where the receiver was DECLARED, and the rung filled the other direction with `byteBufferBoundaryAdmits` — a NOMINAL whole-container door that is one-way *by design* (only `__ManagedByte` may be ADOPTED where a byte-holding `Byte` is declared). `append` adopts nothing, so the asymmetry was inherited rather than intended. **CURE: state the compiler-owned element's value set** — `Project.ByteValueSetLow`/`High`, the SAME pair `rangeHoldsEveryByte` already measured every declared byte element against, now named once and read by both — after which the domain test answers both directions on its own. ⭐ **AND THE NOMINAL DOOR IS THEN DEAD, WHICH IS MEASURED AND NOT ARGUED**: with the fix in and the call stubbed to `false`, the FULL SUITE reads an identical **3533 / 7** and all four hand-built probes still compile — including the one written specifically to reach `rosterElementHoldsEveryByte`'s registry-MISS arm (a `b"…"` literal array in a program declaring no `Byte`), which the arm's own header names as the shape where a roster name has no declaration. So the call was REMOVED: two answers to one byte question that nothing keeps in agreement is the shape this rung exists to end, and the one that would diverge silently is the nominal door, because it ADMITS rather than refuses. ✅ **THE RUNG'S OWN NEAR-MISS WAS CONFIRMED BY SABOTAGE FIRST** — before the fix, stubbing the same call made `<Array with Byte(0..u8.max)>.append(<a cwd buffer>)` refuse, so the implementer's flag was real and the call WAS load-bearing under the design as shipped. ⭐ **THE TRAP THE ROW EXISTS FOR HOLDS**: `[1,2,3]` is `Array_int` and `IntArray.create()` is `Array_Integer` — the compiler prints both spellings in one diagnostic, so the giids differ and an identity test would refuse `arrays.md`'s two joining cases. ⭐ **NO THIRD SHAPE SITE**: `arrayElementSize` has six call sites in the whole compiler and only ONE compares two arrays (`sameTrivialArrayShape`); the rest read a single array's stride. ⚠ **Build 12.4 → 13.0 s, suite 40.8 → 40.5 s over an identical 3540 tests, CPU 188.7e9 → 189.1e9 (+0.2%) — all three inside this file's measured ~5% band and NONE of them a datapoint.** **GATES:** `dotnet build maxon-sharp` exit 0 / **0 warnings** / all five guards OK / **167 codes** · `build maxon-shv2` exit 0, error-codes OK, registry hash unchanged (`docs/` diff EMPTY — no new code, E3005 reused) · shv2 **3533 passed / 7 failed**, `main`'s identical 7 (3 file-order anchors + 4 `parsable-interface/parsable.float-fromstring*`, row X5), **exit 1 not 101** · `git status --short specs-shv2/fragments/` EMPTY, and the rung's 10 additions carry no `M` · the 3 `note:` golden drifts are the pre-existing ones. ⚠ **THE TWO CASES THIS COMMIT ADDS ARE `targets: x64-windows` AND ARE NOT RUN ON THIS HOST** — the only producers of `__ManagedByte` are the x64-windows OS-buffer family and the stdlib-only `String.addressableBytes()`. Both were verified by CROSS-COMPILING the identical source to PE (exit 0 after, refused before), and the `cwd.length() == before + 1` assertion they turn on was grounded by a HOST-runnable measurement of the same operation (`a.managed.append(b)`: `before=2 after=3`). | 13.0 | 11,763,712 | 10,390,936 | 40.5 | 189.1e9 | 3540 |
| 2026-08-03 | `feat(shv2 X6): a narrow declared type with no range — ExitCode was checked nowhere, and the two targets disagreed about the wrong answer` | ⚠ **x64-WINDOWS HOST**, `maxon-n1` clone; touches no `maxon-sharp/`, so the size columns are comparable to the three `X5` rows above. ⭐ **THE SMALLEST COMPILER DELTA IN THIS FILE FOR THE LARGEST BEHAVIOUR CHANGE: code 9,635,757 → 9,636,360 (+603 B)**, exe 10,784,768 → 10,786,304 (+1,536 B). That is the whole of it, and the ratio is the point — the fix is ONE builtin-first arm in `RangedAliasRegistry.lookup` plus the constants it returns, because every door the range now has to be enforced at was **already built** (`InsertRangeChecks`, P1.9 + A1f) and was answering `undeclared` for one name. The other +bytes are `emitDeadReturn` learning the throwing ABI. ⚠ **THE SUITE TIME IS NOT A DATAPOINT** — 60.5 → 61.5 s, inside this file's measured ~5% band — and the corpus moved anyway: 3619 → **3703** (+9 authored here in `specs-shv2/exit-code-range.md`, the rest from `main`). Compile CPU **1292.8e9 ticks from 2700 of 3703 test compiles at 12 workers** against the row above's 1276.9e9 over 3619: +1.2% for +2.3% more tests, i.e. nothing. Host **3703 / 0**, exit 0, no leak. **Cross-target GREEN on every LOCAL lane: x64-linux 3532 / 0, wasm32-wasi 3479 / 0**, both arm64 lanes SKIPPED (remote, not requested) and therefore UNVERIFIED. ⛔ **THE COST THAT MATTERS IS NOT IN THE COMPILER, IT IS IN WHAT THE COMPILER EMITS: 3,146 committed goldens moved** (1,627 x64-windows + 1,519 x64-linux), and every one is the same shape — a `main` that returns a value no constant fold can see now carries a two-bound cascade, a panic block and a real `prologue`/`epilogue` frame it did not have. It is exactly what any other ranged return costs, and `docs/optimization-log.md`'s X6 row prices it on the ladder: **+90 / +109 / +143 / +213 / +351 / +625 allocations** across a DOUBLING ladder — a linear constant, no new growth shape, `phase:insertRangeChecks` still ×1.92…×1.99 per rung. | 16.8 | 10,786,304 | 9,636,360 | 61.5 | 1292.8e9 | 3703 |
| 2026-08-03 | `review(shv2 X6): builtin-FIRST was a second authority over one name, and the width door never got the memo` | ⚠ **x64-WINDOWS HOST**, `maxon-n1` clone; touches no `maxon-sharp/`, so the size columns are comparable to the X6 row directly above ⇒ **code 9,636,360 → 9,636,451 (+91 B)**, exe unchanged at 10,786,304 (page granularity). ⛔ **THE FINDING IS A MISCOMPILE X6 INTRODUCED, FOUND BY PROBING AND VERIFIED AGAINST BOTH REFERENCES.** X6 put its builtin `ExitCode` arm FIRST in `RangedAliasRegistry.lookup`, so a program declaring `typealias ExitCode = int(0 to 125)` had its GUARD answered by the builtin (`0 to u32.max`) while `storageBytesInEveryFile` — fed by the same `record`, and not short-circuited — still answered the user's ONE-BYTE slot. MEASURED: `Array with ExitCode` + `a.push(300)` compiled clean and printed **44**; with `int(0 to 300)` a `70000` printed **4464**. The parent binary refuses both (`E3005 … outside the range of 'ExitCode' (int(0 to 125))`) and so does the bootstrap ORACLE. ⇒ the arm is now a FALLBACK, LAST, and every door reads one declaration again; it is also off the hot path, where it was paying `builtinTypeNameTag`'s compares on every named type reference. ⭐ Two duplications closed beside it: `emitZeroConstOfReturnType` and `emitDeadReturn` had TWO rules for one fact (register-file-coarse `f64`/`i64` vs the declared type) and now share `stdDefaultPrimaryReturnType`; `builtinStdlibAlias` and `builtinExitCodeAlias` were one `RangedTypeAlias.create` shape written twice and now share `compilerSuppliedIntAlias`. And E3010 quoted a RANGE-CONTESTED alias under the compiler's mint where E3005 has normalized since N2 — **`unneeded cast: 'Byte$0_200' already fits in 'Wide'`**, seen RED by sabotage against the new `type-casting/error.unneeded.contested-alias-quoted-as-source-spells-it`. ⚠ **THE SUITE TIME IS NOT A DATAPOINT** — 61.5 → 60.8 s, inside this file's measured ~5% band — over 3703 → **3704** (+1 authored here). Host **3704 / 0**, exit 0, no leak. **Cross-target GREEN on every LOCAL lane: wasm32-wasi 3480 / 0, x64-linux 3533 / 0**, both arm64 lanes SKIPPED (remote) and therefore UNVERIFIED. ⭐ `git status --short specs-shv2/fragments/` shows TWO ADDITIONS (the new case's x64-windows and x64-linux goldens) and no `M`: the emitted code is byte-identical, which is the proof that reversing the lookup order changed no program the corpus contains. `docs/optimization-log.md` carries the ladder read: **Δ0 in both exact columns at all six rungs**, at `--repeat=2` so the cross-repeat allocation-equality check ran. | 16.3 | 10,786,304 | 9,636,451 | 60.8 | 1260.9e9 | 3704 |
| 2026-08-03 | `fix(shv2 A4c)` — the byte-PACKED producer that was NOT refused, and the raw-byte writer that answered to a byte instead of to its SLOT | ⚠ **arm64-macOS HOST**, base `cfdd70449`, and the size columns are a like-for-like read against **this worktree's own base build**, made before a line was edited (same host, same path, bootstrap rebuilt here from the base's own `maxon-sharp/`: `dotnet build` exit 0 / 0 warnings / all five guards OK). **code 10,438,728 → 10,452,452, +13,724 B; exe 11,812,864 → 11,829,248, +16,384 B (one page).** That is the whole executable cost: an eleventh `ParseError` arm and its `Queries` renderer, a fifth out-of-range sentence in `TypeRules` plus one element renderer, `slotUnsignedMax` + `SlotUnsignedMin` in `LayoutDescriptor`, `rangeHoldsEverySlotValue` + `elementAdmitsEverySlotValue` + `elementIsAByteNoFileDeclares` in `SignatureIndex`, `RangedAliasRegistry.anyFileDeclares`, one shared stride rule in `Parser` with a two-case `BytePackedProducer` enum, and one deleted stride precondition. ⚠ **Build 11.4 → 11.9 s and suite 40.8 s are inside this file's measured ~5% band and are NOT datapoints**; **tests 3545 → 3556** is this rung's ELEVEN cases. Compile CPU **190.0e9 ticks from 2538 of 3556 test compiles at 12 workers** — a different corpus from every earlier row, so an absolute. ⭐⭐ **`scale-test` WAS RUN, READ, AND IT CAUGHT A REGRESSION THIS RUNG HAD JUST INTRODUCED — the A3g cost, one door over.** The first cut passed the refusal's producer name to the shared rule AS A RENDERED STRING, and `stdlib/File.maxon` plus the ladder's own `y_bufsurf.maxon` call `__ManagedMemory.create` **nine times at rung 0**, so ~6 `String` allocations per ACCEPTED call were built and thrown away: **`phase:parse` +55 allocations at rung 0 rising to +1,543 at rung 5**, perfectly linear and entirely for a sentence no compiling program reads. Cured by making the producer an ENUM matched on the THROW path (which also single-homes the producer/subject pairing the message needs). **Re-measured after the fix: +8 allocations and +353 bytes, FLAT and IDENTICAL at all six rungs** (`phase:load` +7, `phase:parse` +1; ~48 B each, the size of a `String` record, consistent with the new top-level constants materialized once at module init), **and every ratio column byte-identical to the control** — `phase:parse` ×1.93 allocations / ×2.02 bytes, `phase:load` ×1.73. ⚠ The control was run TWICE and the two runs' allocation and byte tables were **byte-identical** (only the CPU table moved), which is what licenses reading a +62 as real rather than as noise. **No `--note` row was written to `docs/optimization-log.md`**, for the third rung running and the same reason A4a and A4b gave: the only comparison rows there are Windows trees at a different path, and a row measured against an incomparable baseline pollutes the trend rather than extending it. ⭐ **`git status --short specs-shv2/fragments/` shows ELEVEN ADDITIONS AND FOUR `M`, and every `M` is deliberate** — three are A4a cases whose refusal moved E3117 → E3118 (same programs, same positions, one line each), and the fourth is `a-wide-byte-still-writes-raw-bytes-through-the-buffer-surface`, whose PROGRAM this rung refuses twice over and which was rewritten onto `int(0 to u16.max)` to keep pinning the same thing (a range wider than a byte is not by itself a reason to refuse a raw write). The suite's `note:` block lists **3 drifted goldens, all pre-existing** — the same three A4a measured on a control and named. **GATES:** `dotnet build maxon-sharp` exit 0 / **0 warnings** / all five guards OK · `error-codes generate` → **168 codes**, `build maxon-shv2` exit 0 with error-codes OK and 4 generated files up to date · shv2 **3549 passed / 7 failed**, `main`'s identical 7 (3 file-order anchors + 4 `parsable-interface/parsable.float-fromstring*`, row X5), **exit 1 not 101** · `wasm32-wasi --filter=bytearray-element-size` **49 / 0** · C# suite **3242 / 3242, `3242 verified, 0 written`, exit 0** and `git status --short specs/` EMPTY. | 11.9 | 11,829,248 | 10,452,452 | 40.8 | 190.0e9 | 3556 |
| 2026-08-03 | `review(A4c)` — the slot's own zero read from a BYTE's constant, and one alias-plus-range pair spelled five times | ⚠ **arm64-macOS HOST**, independent review of `63faae106`, compared against the `fix(shv2 A4c)` row directly above — same host, same worktree, same path. **code 10,452,452 → 10,450,612, −1,840 B; exe 11,829,248 → 11,829,248, IDENTICAL** (one page's slack absorbed it). The shrink is four hand-composed message halves collapsing onto one renderer. **Build 12.9 s and suite 41.8 s are inside this file's measured ~5% band and are NOT datapoints**; **tests 3556 → 3557** is this review's one added case. ⭐⭐ **THE RUNG'S SCALE CLAIM WAS RE-MEASURED THE ONLY WAY THAT CAN BE HONEST — BOTH BINARIES RUN FROM ONE PATH**, the base binary copied INTO this worktree's own `.maxon/` and swapped back out, so the instrument's path-length term (which it warns about at length, and which a second worktree injects as a per-rung constant of exactly the magnitude under test) is identically zero on both sides. Result: **+1 allocation and +24 bytes, FLAT at all six rungs, entirely in `phase:parse`** — flatter than the `+8 / +353` the row above records, and confirming the substantive claim, that the LINEAR `+55 → +1,543` regression the rung caught in itself is gone. ⚠ Do not read the `+8` and the `+1` as a disagreement about the compiler: they are two different controls, and only the swapped-in-place one has no path term. **This review's own delta against the rung is Δ0 in BOTH exact columns at all six rungs** — its four renderer substitutions and one predicate constant are on THROW paths and at module scope, and the ladder says so exactly. ⭐ **NO `M` FRAGMENT, which is the proof the four substitutions are byte-identical**: E3005's `'Alias' (int(lo to hi))` pair is printed by goldens throughout the suite, and the only fragment status is the ONE addition this review mints. **WHAT WAS FIXED:** (1) `rangeHoldsEverySlotValue` compared an element's low bound against `Project.ByteValueSetLow` — a BYTE's declared low — while `rawByteWriteSlotDomainMessage` renders the same slot's low from `SlotUnsignedMin`, whose own header says the two are the same zero for DIFFERENT reasons and must not be conflated; predicate and sentence were two facts that happened to agree. (2) `rangedElementValueSetDisplay` was minted for E3118 alone while FOUR pre-existing sentences in the same file still composed `'{quotedAliasName}' ({formatRangeDisplay})` by hand — each under a header arguing that no site may print an alias or a range two ways; renamed `rangedAliasValueSetDisplay` and adopted at all five. (3) E3117's message spelled a byte's value set as a literal `0 to {u8.max}` beside `rangeHoldsEveryByte`, the predicate it reports; it now reads `ByteValueSetLow`/`High`. (4) `slotUnsignedMax`'s header sat above `SlotUnsignedMin` with that constant's own doc concatenated onto its end. (5) Two spec paragraphs claimed `int(0 to u16.max)` is REFUSED by E3117's every-byte question and that its case is "the acceptance case that rules out a widened E3117" — MEASURED FALSE both ways (`rangeHoldsEveryByte(0, 65535)` is true, and that same alias compiles a `"abc".toByteArray()` through E3117's live site reading `97 98 99`); the real control is the machine-word case, and the prose now says so. (6) The rewrite of `a-wide-byte-still-writes-raw-bytes-through-the-buffer-surface` left NOTHING composing the two rungs — `__ManagedMemory.create(n, 1)` under the canonical `Byte` followed by `setByte` — so that case is added. ⭐⭐ **STAGE 1's LOAD-BEARING CLAIM WAS PROVED BY EXPERIMENT, NOT INFERRED**: `stdlib/File.maxon:45`'s `Byte` was temporarily widened to `int(0 to 1000)` and a bare `return 7` program then FAILED with the new E2015 pointed at `stdlib/File.maxon:71:43` — so that line IS parsed for every program, the rule DOES ask about it, and the only thing holding it shut is its own file's `Byte`, exactly as claimed (the `wide-byte-program-still-round-trips-a-file` case that was cited is `targets: x64-windows` and does not run on this host). Reverted immediately. ⭐ `ExitCode`'s slot re-measured INDEPENDENTLY of the compiler's own claim, by byte layout: element 1 of an `Array with ExitCode` starts at `byteAt(8)` ⇒ **EIGHT bytes**, confirming the rung's correction of the PLAN row's "4-byte" filing. **GATES:** `dotnet build maxon-sharp` exit 0 / **0 warnings** / all five guards OK / **168 codes** · `build maxon-shv2` exit 0 · shv2 **3550 passed / 7 failed**, `main`'s identical 7, **exit 1 not 101** · `bytearray-element-size` filter **50 / 0** · C# suite **3242 / 3242, `3242 verified, 0 written`, exit 0**, `git status --short specs/` EMPTY · the same **3 pre-existing** drifted goldens and no new one. | 12.9 | 11,829,248 | 10,450,612 | 41.8 | 187.6e9 | 3557 |
| 2026-08-03 | `fix(shv2 A3h): a temporary that yields a BORROW is held to the borrower's scope — the use-after-free nothing reported` | ⚠ **x64-WINDOWS HOST**, `maxon-n1` clone; touches no `maxon-sharp/`. ⛔⛔ **DO NOT DIFFERENCE THE SIZE COLUMNS AGAINST THE `review(shv2 X6)` ROW ABOVE — IT DOES NOT REPRODUCE ON THIS TREE.** That row records `code 9,636,451`, and the merge base this branch sits on (`3999c6265`, X6's review plus a docs-only claim commit) builds **9,744,902 / exe 10,907,648** here, with the same `bin/maxon.exe` that built this row. So the comparison below is against a `git worktree` OF THAT BASE, built and measured on this machine — the like-for-like read the trend needs: **code 9,744,902 → 9,745,227 (+325 B)**, **exe 10,907,648 → 10,907,648, UNCHANGED TO THE BYTE** (page granularity). +325 B is the whole of a rung that DELETED a refusal: the ~700-byte `E2015` message a managed field read out of a temporary used to earn is gone from `rdata` (393,303 → 392,903, −400 B), and three small functions took its place. ⭐ **THE MECHANISM IS ONE DOOR THIS PROJECT ALREADY OWNED, WRITTEN DOWN ONCE**: `giveTemporaryScopeLifetime` — remove from `pendingTempDrops`, enrol as a nameless `ownedBindings` entry — was already spelled out three times (`for … in` source, temporary `match` scrutinee, escaping instance-method feed) and this rung needed the fourth and fifth (an element BORROW's container, a managed FIELD read's box), so all five now share it. ⚠ **THE SUITE TIME IS NOT A DATAPOINT** — 60.8 → 67 s is outside this file's measured ~5% band but the corpus moved with it (3704 → **3793**, +20 authored here plus main's) and the base tree measured 3773 tests on the same box in the same session. **Compile CPU is the column that can carry it, and it is FLAT: 1323.2e9 ticks over 3793 tests at 12 workers against the BASE BUILD's 1326.8e9 over 3773 — −0.3% for +0.5% more tests**, both measured here, minutes apart. Host **3793 / 0**, exit 0, no leak. **Cross-target GREEN on every LOCAL lane: x64-linux 3619 / 0, wasm32-wasi 3566 / 0**; both arm64 lanes SKIPPED (remote, not requested) and therefore UNVERIFIED. ⭐ `git status --short specs-shv2/fragments/` shows **ZERO `M`** — additions and deliberate deletions only — so the emitted code is byte-identical for every program the corpus already pinned, which is the proof that the promotion reaches only programs that take a borrow out of a temporary. The 27 deletions are the goldens of nine cases RENAMED off their `error.` prefix when their refusal was lifted; the 57 additions are those nine, this rung's 20, and 38 x64-linux goldens the cross-target gate minted for tests that predate this rung. `docs/optimization-log.md` carries the ladder read: **Δ0 in both exact columns at all six rungs against that same base build**. | 16.5 | 10,907,648 | 9,745,227 | 67 | 1323.2e9 | 3793 |
| 2026-08-03 | `review(shv2 A3h): the byte-identical-codegen proof was measured with `git status`, which cannot see a golden that still exists` | ⚠ **x64-WINDOWS HOST**, `maxon-n1` clone, independent review of `6672122db`; touches no `maxon-sharp/`. ⛔⛔ **THE ROW ABOVE IS WRONG IN TWO PLACES, AND BOTH ARE MEASUREMENT-INSTRUMENT ERRORS RATHER THAN CODE DEFECTS.** (1) It claims `git status --short specs-shv2/fragments/` shows **ZERO `M`**, and reads that as proof the emitted code is byte-identical for every program the corpus already pinned. `git status` cannot answer that question: a golden only becomes `M` once somebody REGENERATES it, so zero `M` measures whether `--update-required` was run, not whether the goldens still match. The instrument that answers it is the runner's own trailer, and it read **34 golden fragment(s) no longer match** at `6672122db` against **33** at the merge base built and run here — so the rung moved exactly ONE: `union-managed-payload/retained-give-merged-with-literal-gives`. It is refreshed in this commit. The movement is real, benign and explained: `settleArmGive` launders that arm's borrowed literal give at the ARM'S EXIT, where `resolveGivesOwnership` used to launder it at the MERGE, so the string copy is now built while the arm's payload binding `b` is still live — one more callee-saved register (`r15`) and 8 more bytes of frame in `pick`, with the `__str_decref` of `b` moved after the copy instead of before it. Correctness is unchanged; the arm's own drop still runs on its own edge. (2) The SIZE columns do not reproduce. Measured here with three FULL clean rebuilds (`rm -rf maxon-shv2/.maxon` before each, same `bin/maxon.exe`, byte-stable across repeats): merge base `3999c6265` = **code 9,744,902 / exe 10,907,648** — which is exactly what that row records for the base — but `6672122db` = **code 9,743,934 / exe 10,906,112**, against the **9,745,227 / exe unchanged** it logs. So the rung SHRANK the compiler by **968 B** where the row reports +325 B; the row's HEAD figure belongs to a tree that is not the one committed, which is the trap this file's own header names. ⚠ The size column is also cache-sensitive on this host — a WARM-cache build of the same tree reads `exe 10,907,648` where a cold one reads `10,906,112` — so only full-clean numbers are comparable, and the base/HEAD pair above is one. ⭐ **WHAT THE REVIEW VERIFIED RATHER THAN CHANGED**, all of it re-run on the committed tree: **33 hand-written probes across loops, `break`/`continue`, early `return`, `throw`, 200-deep recursion, a closure body, an `otherwise` block, nested and chained ternaries, `default` arms, `match` arms that return or break, short-circuit right-hand sides and reassignment of an outer `var` from a borrow — every one agrees with the C# oracle and none exits 101.** The leak gate itself was proved live by SABOTAGE: deleting the `while`-condition `dropArmScopedOwned` in a scratch worktree and rebuilding turns `while i < (try make().get(0) otherwise …).count() * 40` into exit **101**, and the committed tree answers 5. The **nine `maxoncstderr`→`exitcode` rewrites all match the bootstrap oracle exactly** (5/3/5/1/7/`hello`/5/3/5). `run_scale_test --repeat=1` reads **Δ0 in both exact columns at all six rungs** against the rung's own logged row, with this review's changes in. **WHAT THIS COMMIT CHANGES IN THE COMPILER IS COMMENTS AND TWO NAMES**: the rung deleted `finishArmValues` but left its header comment in place and inserted `armGiveUnion` between the new header and `finishArmExit`, so the three-step ordering argument — the one that prevents a use-after-free AND a leak — documented the wrong function, under a dead header for a function that no longer exists, above a stranded `transferGiveToPhi` header sitting 120 lines from its own function. Each header now sits on its function. And `giveTemporaryScopeLifetime`'s promise that *“each caller states its own name, so a dump of `ownedBindings` says WHY each nameless entry is there”* was false for two of its five callers, which both passed `EmptySourceText`; they now pass `ScrutineeSourceBindingName` and `EscapingFeedSourceBindingName`. **code 9,743,934 → 9,744,293 (+359 B, the two byte strings), exe UNCHANGED at 10,906,112.** Host **3793 / 0**, exit 0, no leak; mismatched fragments back to the base's **33**, none new. | 14.3 | 10,906,112 | 9,744,293 | 63.9 | | 3793 |
| 2026-08-03 | `fix(shv2 A3k): the CONSUME boundary was blind to a generic alias while its DROP twin was not` | ⭐ **x64-Windows host, and both size numbers are against a MEASURED SAME-TREE CONTROL, not against the row above**: `maxon-shv2/Compiler/SignatureIndex.maxon` stashed, `rm -rf maxon-shv2/.maxon`, full-clean rebuild, `stat` — the control reads **exe 10,923,520 / code 9,758,799**, this tree reads **exe 10,923,520 / code 9,759,118**. So **code +319 B and exe UNCHANGED TO THE BYTE** (the 319 fits inside the PE section alignment, so the exe column is not a second reading of the same fact). 319 B is the whole executable cost: one `Map.contains` predicate (`genericAliasSpellingIsSuperseded`), one arm in `typeArgIsOwned` and one in `typeSupportsDeepClone`. ⚠ **Every size row this session was taken from a FULL-CLEAN build for a reason — the incremental build of this identical tree reported `code 9,760,411`, 1,293 B away**, so an incremental number is not comparable to a clean one and this row would have been wrong by more than its own delta. ⚠ Build 16.4 s and suite 63.5 s are wall clock and are NOT comparable to the arm64-macOS rows above (a Mach-O and a PE are different binaries of the same sources); against this host's own control build (16.5 s) the build time is inside the measured ~5% band ⇒ not a datapoint. Suite is over **3806 → 3812** tests — this rung's six new cases — so the +time is corpus, not speed. Compile CPU **1340.7e9 ticks from 2786 of 3812 compiles at 12 workers**, against the base tree's 1327.3e9 over 2784 of 3806: a different corpus, so an absolute and not a delta. ⭐ **Golden drift measured with the RUNNER, not with `git status`: 16 mismatching before, 22 after the fix, 16 again once this rung's six were regenerated on both local lanes** — so the net drift this rung leaves is **zero**, and the six that moved are exactly the six the PLAN row predicted (`alias-named-type-argument-{agrees,forward-declared-agrees,cross-file-types-first,cross-file-use-first}`, `both-spellings-agree-at-{the-comparison,a-nested-type-argument}`), each losing one `__mm_incref` and one caller-side `__destruct_Box_S0` in favour of a move. | 16.4 | 10,923,520 | 9,759,118 | 63.5 | 1340.7e9 | 3812 |
| 2026-08-03 | `review(shv2 A3k): the superseded guard was written at TWO of THREE argument doors, and the third re-manufactured the kind the rung abolishes` | ⚠ **x64-WINDOWS HOST**, `maxon-n1` clone, independent review of `d6798f979`; touches no `maxon-sharp/`. **Both size numbers are a FULL-CLEAN A/B taken in this same tree, minutes apart** (`rm -rf maxon-shv2/.maxon` before each, same `bin/maxon.exe`): the control — the rung's own HEAD source — reads **exe 10,923,520 / code 9,759,118**, reproducing the row above to the byte, and this tree reads **exe 10,924,032 / code 9,759,385** ⇒ **code +267 B, exe +512 B** (one file-alignment step; the control sat just under it). 267 B is the whole cost: one early-out in `typeArgIsCoOwnedTrivial` and one private `typeArgSpellingIsSuperseded`. ⛔⛔ **THE FINDING IS A WRONG ANSWER THE RUNG'S OWN CURE INTRODUCED, FOUND BY PROBING.** `typeArgIsCoOwnedTrivial` is `typeIsManaged and not typeArgIsOwned`, and A3k guarded only the OWNED half — so a pre-settle orphan voted MANAGED-AND-NOT-OWNED, i.e. **CO-OWNED TRIVIAL**, the manufactured kind the rung exists to abolish, re-made one spelling over. The other two doors state their own identity (`false`; `true` under `uncopyableElement`'s `not`), and every kind is folded by OR in `anyInstanceTypeArgHasKind`, so `false` is what *contributes nothing* means for all three — this one inverts it. **MEASURED**: two files declaring `typealias N0 = Box with S0` / `= Box with S1` over String-owning structs plus a `typealias N1 = Box with N0` in each were refused **E2015** *'a trivial-struct instantiation co-owns the field'* — said of a box that owns a String — off the orphan's vote alone, while the byte-identical program whose second file spells its aliases `M0`/`M1` compiles and runs to **exit 0**, and so does the INLINE `Box with (Box with S0)` spelling of the contested one. Seen RED by reverting `SignatureIndex.maxon`, rebuilding and running the new filter, with the inline half PASSING both times as the control. ⭐ **THE CURE KEEPS THE FACT AT ONE HOME**: the new `typeArgSpellingIsSuperseded` is the TYPE-arity form of `genericAliasSpellingIsSuperseded` and bottoms out in it, so all three doors read one predicate over `contestedGenericAliases`. ⭐ **THE TWO NEW GOLDENS ARE THE PROOF THE SPELLINGS NOW AGREE**: the contested-alias case and its inline twin differ ONLY in source text and in two symbol names (`Box_N0$Box_S1` vs `Box_Box_S1`) — same instructions, no `__mm_incref`, one `__destruct_`. `git status --short specs-shv2/fragments/` shows **FOUR ADDITIONS AND NO `M`**, and the runner's own trailer agrees: **16 → 16** mismatching on x64-windows and **8 → 8** on x64-linux, the IDENTICAL sets (diffed), measured against this tree built from the rung's unmodified source. So this fix moves no emitted code for any program the corpus already pins. ⚠ **THE SUITE TIME IS NOT A DATAPOINT** — 63.5 → 63.3 s, inside this file's measured ~5% band — over 3812 → **3814** (+2 authored here). Compile CPU **1335.6e9 ticks from 2788 of 3814 compiles at 12 workers**, against the rung's 1340.7e9 over 2786 of 3812: a different corpus and inside the band either way. **GATES:** `build maxon-shv2` exit 0, error-codes OK / 168 codes / 4 generated files up to date · shv2 host **3814 / 0**, exit 0, **no leak** · **cross-target GREEN on every LOCAL lane: wasm32-wasi 3587 / 0, x64-linux 3640 / 0**, both arm64 lanes SKIPPED (remote, opt-in) and therefore UNVERIFIED · `docs/optimization-log.md` carries the ladder read: **Δ0 in both exact columns at all six rungs** at `--repeat=2`, so the cross-repeat allocation-equality check ran and passed. ⚠ **The rung's scale row claimed the corpus uses no generic alias as a type ARGUMENT; that is FALSE** (`Array with __ManagedMemory`, every rung) and is corrected in place there. | 14.0 | 10,924,032 | 9,759,385 | 63.3 | 1335.6e9 | 3814 |
| 2026-08-03 | fix(shv2 A4i): an instance's type ARGUMENT is a slot, and reading one as a bare `named` typed a POINTER as `int` | ⭐ **x64-WINDOWS HOST, `maxon-n1` clone, and BOTH SIZE NUMBERS ARE AGAINST A MEASURED SAME-TREE CONTROL, not against the row above.** Every build in this row is FULL-CLEAN (`rm -rf maxon-shv2/.maxon`), because an incremental one on this host over-reported a delta by ~4x the day before; the control is this very tree with `Parser.maxon` and `SignatureIndex.maxon` reverted and rebuilt the same way. **code 9,793,832 → 9,793,852 = +20 BYTES; exe 10,961,920 → 10,961,920, UNCHANGED TO THE BYTE.** That is the whole executable cost of the rung: `Parser.instanceSubstitutedType` swaps a 2-argument `adoptType` for a 3-argument `declaredSlotType`, and everything else in the commit is comment and spec. ⛔ **Do NOT difference these against the `review(shv2 A3k)` row directly above** — it reads 9,759,385, i.e. **+34,467**, and that 34 KB is the OTHER row's base, not this change; the same-tree control is why this row can say +20 instead of guessing. ⭐ **No EMITTED byte moved either**, and that is measured with the runner's own instrument rather than `git status`: the golden-drift trailer reads **18 on x64-windows and 10 on x64-linux both BEFORE and AFTER**, the x64-linux base taken on this same binary with only `specs-shv2/generic-types.md` reverted — the compiler held fixed, only the spec data varying. Host suite **3876 / 0**, exit 0, no leak; 3872 → 3876 is this rung's four new cases, every one seen RED against the unchanged compiler first. **Cross-target GREEN: x64-linux 3702 / 0, wasm32-wasi 3649 / 0**; both arm64 lanes SKIP — remote, not requested, UNVERIFIED. ⚠ **The board's recorded x64-linux baseline of 3640 was STALE by exactly 58**, measured here at **3698** on this tree, and the 58 is not a guess: five earlier spec-port commits (`bool-bit-packing` 20, `vector` 26, `primitive-stringable` 5, `challenge-nested-structs` 4, `challenge-struct-lifetime` 3) left exactly 58 untracked `fragments/x64-linux/` goldens behind, having never run that lane. `scale-test` recorded in `docs/optimization-log.md`: both exact columns MOVED — **+320 … +4,695 allocations, with a per-rung delta ratio of x1.44 x1.61 x1.76 x1.86 x1.93** against the ladder's own x2, so LINEAR, +0.022% of allocations at rung 5, attributed to `declaredSlotType`'s `named` cascade being paid once per opaque-`T` field read (the identical cost `fieldTypeOf` already pays at every `named` struct field). Both times inside this file's measured ~5% band ⇒ no reading. | 15.8 | 10,961,920 | 9,793,852 | 68.9 | 1379.2e9 | 3876 |
| 2026-08-03 | `review(shv2 A4i): the READ door was fixed and its declared WRITE twin kept a copy of the broken spelling` | ⚠ **x64-WINDOWS HOST**, `maxon-n1` clone, independent review of `3a5e65c1e`+`3b79bca9f`; touches no `maxon-sharp/`. **Both size numbers are FULL-CLEAN** (`rm -rf maxon-shv2/.maxon` before each, same `bin/maxon.exe`, and the number reproduced to the byte across two clean builds minutes apart): the rung reads **code 9,793,852 / exe 10,961,920** and this tree reads **code 9,793,543 / exe 10,961,920** ⇒ **code −309 B, exe UNCHANGED TO THE BYTE**. It SHRANK because the fix DELETES a spelling: an inline `adoptType(substitutedInstanceArg(…), into: …)` becomes one call to the helper that already existed. ⚠ The incremental build of this identical tree reports **9,794,836**, exactly **+1,293 B** away — the same incremental-vs-clean offset this file's `A3k` row measured — so only the clean pair above is comparable. ⛔⛔ **THE FINDING IS A READ-AFTER-FREE THE RUNG MADE REACHABLE, FOUND BY PROBING, AND IT IS FIXED HERE RATHER THAN FILED.** `Parser.emitFieldWrite`'s concrete-instance arm (`Parser.maxon:35003`) carried its own copy of `instanceSubstitutedType`'s PRE-A4i body — the very `adoptType(substitutedInstanceArg(…))` the rung replaced — under a comment declaring itself the read's twin (*"RETYPE the field to that substituted concrete type (as `emitFieldLoad` retypes a READ)"*). So the fix reached the READ and not the WRITE, and what the copy handed on was a bare `named`. Its consumer is `classifyUnionPayload`, which resolves a `named` through its OWN cascade — one that has a struct arm (so a `Box with Point` write kept working, which is why the split hid) and **NO generic-alias arm**. A `Holder with IntBox` write therefore fell through to `undeclaredName`, took the SCALAR store, and neither dropped the old box nor moved the new one in. **MEASURED**: `var h = BoxHolder.hold(IntBox.create(7)); h.item = IntBox.create(8); h.item.value` printed **4557430888798830399** = `0x3F3F3F3F3F3F3F3F`, the free poison, where the C# oracle prints **8** — the temporary was freed at the statement and the field kept pointing at it. ⭐ **A4i is what made it reachable**: at the merge base the same program is refused `E3011 Unknown type 'IntBox'` (verified by building `563b4ad21` in a worktree and running it), so a clean compile error became a silent wrong answer — which is the one thing this rung may not merge with. The cure is the door itself: `emitFieldWrite` now CALLS `instanceSubstitutedType`, so a re-tag added to `declaredSlotType` reaches the write the same day it reaches the read. **Two cases pin it**, the generic-alias one above and its managed-struct twin (the arm that already worked, so the two cannot part again); the minted golden shows the `__mm_decref` of the old box that was previously absent. ⚠ **CLAIM A OF THE RUNG'S REPORT IS WRONG ON ONE STEP** — the composition argument said the only `declaredSlotType` arm reachable through `retypeOpaqueReturnValue`'s managed guard is the float alias, refused at instantiation. The **FUNCTION-ALIAS** arm is reachable too: `typeIsManaged` reads a function alias FALSE, and the upstream refusal is at CONSTRUCTION, which does not stop a function BODY typed `Box with UnaryOp` from being compiled. `function use(b OpBox) … let q = b.get(); return q(3)` is `E3004` at the base and COMPILES here, through both doors. The conclusion survives (the new answer — an indirect call through a function value — is correct, and no such instance can be constructed) but by a weaker argument than the one filed. Float args are refused at the ALIAS DECLARATION and by **E2062**, not E2061 as the report states. **GATES:** `build maxon-shv2` exit 0, error-codes OK / 169 codes / 4 generated up to date · shv2 host **3878 / 0**, exit 0, **no leak** (3876 → 3878 is this review's two cases) · **x64-linux 3704 / 0**, exit 0 (3702 → 3704, same two) · both arm64 lanes SKIPPED (remote, opt-in) and therefore UNVERIFIED · golden drift measured with the RUNNER: **18 on x64-windows and 10 on x64-linux, the IDENTICAL SETS as the merge base built and run here** (diffed, not counted), so neither the rung nor this fix moves an emitted byte for any program the corpus already pins · `docs/optimization-log.md` carries the ladder: **Δ0 in BOTH exact columns at all six rungs** against the rung's own row — expected, because the corpus instantiates generics and reads `T` back through `get()` but never WRITES an opaque `T` field on a concrete instance, a corpus blind spot rather than a measurement of zero. ⚠ Suite 68.9 → 65.4 s and build 15.8 → 14.8 s are wall clock over a corpus that grew, and are NOT datapoints. Compile CPU **1376.3e9 ticks from 2851 of 3878 compiles at 12 workers** against the rung's 1379.2e9 over 3876. | 14.8 | 10,961,920 | 9,793,543 | 65.4 | 1376.3e9 | 3878 |
| 2026-08-03 | `fix(shv2 A4f): the dead return guard — the emitter now reads what E3010 proves` | ⚠ **arm64-macOS HOST — the same-host A/B is against a worktree of this rung's base commit (`9d078816a`), built here**: exe **11,829,248 → 11,829,248, UNCHANGED TO THE BYTE**; code **10,451,208 → 10,453,536 (+2,328)**. ⭐⭐ **AND THE `code bytes` COLUMN CANNOT SHOW THIS RUNG'S POINT, WHICH IS WORTH SAYING RATHER THAN LETTING A READER MISREAD +2,588 AS A COST.** `maxon-shv2` is compiled by the BOOTSTRAP, so its own binary is the size of the compiler's SOURCE — and this rung ADDS source (one containment predicate, its two unwraps, a parameter on the shared door). The elision it buys is in what **shv2 EMITS**, which this table never measures. That number is in the goldens: across `specs-shv2/fragments/arm64-macos`, **`__rc_panic:` blocks 3,069 → 2,461 (−608, −19.8%)**, `__rc_chk:` −607, `__rc_ok:` −608, `bl mrt_panic` 5,521 → 4,915 — **616 guard cascades removed and 10 added by this rung's own new negative controls**, over **431 modified goldens with ZERO removed `ExitCode:`/`CompilerError:`/`Stdout:`/`Stderr:` lines**. Golden text 20,903,911 → 20,652,644 B (−251,267). On one 5-line program (`return pick()`, `pick` returning a `Byte`) emitted code goes **783 → 719 B, −8.2%**. ⚠ Neither TIME is a datapoint here: build 11.2 → 12.4 s, but the base build was run from a DIFFERENT directory (a scratch worktree), which the ~5% band was never measured across; the suite's 40.9 s covers **3,572** tests where the base covered 3,565, and a wall clock cannot separate 7 more tests from a differently-loaded box. ⭐ The per-phase reading is in `docs/optimization-log.md`'s A4f row: a same-path base-vs-HEAD A/B, whole-compile allocations **−0.55% rising to −0.86%** with the delta DOUBLING per rung (a LINEAR saving), `phase:insertRangeChecks` −13.8% allocations, against `phase:parse` **+0.097%** for the source-range probe a ranged destination now makes. | 12.4 | 11,829,248 | 10,453,536 | 40.9 | 192.0e9 | 3572 |
| 2026-08-03 | `fix(shv2 A4k): the two cascades that answered "what does this bare `named` denote" are one door` | ⭐ **x64-WINDOWS HOST**, `maxon-n1` clone; touches no `maxon-sharp/`. **BOTH SIZE NUMBERS ARE A SAME-TREE FULL-CLEAN CONTROL, not a difference against the row above** — the row above predates `5572d1e89` (the ranged-int-bit-packing spec-port, ~1.5k lines of compiler change), so differencing it would bill that rung's bytes to this one. `rm -rf maxon-shv2/.maxon` before each, same `bin/maxon.exe`: the merge base (`562a3afd6`) reads **code 9,839,131 / exe 11,011,584** and this tree reads **code 9,839,530 / exe 11,011,584** ⇒ **code +399 B, exe UNCHANGED TO THE BYTE**. The clean build reproduced to the digit across THREE separate clean builds of this tree (and a comment-only edit between two of them moved nothing, which is the check that the numbers are the code's). ⚠ The incremental build of the same tree reports **9,840,823**, **+1,293 B** away — the same incremental-vs-clean offset the `A3k` and `A4i` rows measured, so only the clean pair is comparable. The +399 B buys a new shared cascade (`ProgramSignatures.denotedSlotType`, the ONE home of "what does this bare `named` denote"), a new `payloadTypeOf` slot door, and a `slotScopeFilePath` argument at every `classifyUnionPayload` call site. ⭐ **THE LADDER IS THE INTERESTING NUMBER AND IT IS NEGATIVE**: a same-path base-vs-HEAD A/B (both full-clean) moves exactly ONE per-phase row — `phase:parse`, by **−3 allocations at EVERY rung** (243,586→243,583 at rung 0, 3,904,222→3,904,219 at rung 5), a CONSTANT invariant across a 32× input with the growth exponent unchanged at ×1.93 — because the struct arm dropped `structOf` (which boxes a `DeclaredStruct`) for the allocation-free `structTypes.contains` the shared door already used. Every other phase byte-identical. ⚠ **BOTH TIMES ARE INSIDE THE MEASURED ~5% BAND and neither is a datapoint**: clean build 17.0 / 16.5 / 16.4 s across three builds of this tree, suite 66.9 s over **3895** tests (the base tree's own run of the same 3895 was 3891/4 — the four reds are this rung's specs). Compile CPU **1375.8e9 ticks from 2868 of 3895 compiles at 12 workers**, against the base tree's **1384.3e9 over 2864** on the identical corpus. | 16.4 | 11,011,584 | 9,839,530 | 66.9 | 1375.8e9 | 3895 |
| 2026-08-03 | `review(shv2 A4k): the one-door cascade holds, and its TERMINATION is a property of the OTHER function's arms` | ⚠ **x64-WINDOWS HOST**, `maxon-n1` clone, independent review of `90118a558`; touches no `maxon-sharp/`. **Both size numbers are FULL-CLEAN and each reproduced to the byte across two clean builds** (`rm -rf maxon-shv2/.maxon` before each, same `bin/maxon.exe`): the rung's own HEAD reads **exe 11,011,584 / code 9,839,530** and this tree reads **exe 11,012,096 / code 9,839,878** ⇒ **code +348 B, exe +512 B** (one file-alignment step). The +348 is one panic and its message (rdata 395,975 → 396,207 = +232, the string) minus the deleted branch; a comment-only edit between two of the clean builds moved NOTHING, which is the check that the number is the code's. ⭐ **NO FINDING BLOCKS THE MERGE, AND THAT IS THE RESULT OF PROBING RATHER THAN OF READING.** Every question the rung flagged was attacked and each came back CORRECT: **(a)** `denotedSlotType` cannot answer `named` — all five arms answer `function`/`genericInstance`/`structRef`/`float`, each a RETURNING arm of the classifier's tag match, and *no name can denote another name at all* (`typealias B = A` is refused *'a typealias over identifier'*), so no chain, contest or self-reference exists to recurse on; **(b)** a CONTESTED generic-alias payload BOUND on both sides of a two-file program reads its OWN file's element (`nums=8` from `Array with Num`, `words=4` from `Array with String`, exit 0) — the mint `recordGenericAliasContest` registers per declaration is what makes the name-keyed door right; **(c)** the ownership fix is one-owner in BOTH directions — the golden shows the local's `__arr_decref` DISAPPEARING as the cascade's `__destruct_NumBox` appears, use-after-move is E3102, and Set/Map/Array/managed-instance payloads bound, unbound, nested in an array and deep-CLONED all exit 0 with no 101 and no fault. ⚠ **THE RUNG'S OWN JUSTIFICATION FOR KEEPING A4i's LINE IS WRONG ON ITS FACE, THOUGH THE DECISION IS RIGHT.** It says the line survives *"because it does the SUBSTITUTION, which no name cascade can do"* — but the substitution is the INNER `instanceSubstitutedIndexType`, which survives any revert; what the revert removes is the outer `declaredSlotType`. Re-measured here: sabotaging `emitFieldWrite` and rebuilding leaves the A4i poison program printing **8** AND the contested cross-file probe answering **11**, so the line is redundant in EFFECT today. Keep it anyway — the two spellings would then resolve one slot against two different files (`self.filePath` vs `layout.declFilePath`) and only agree by an accident that a generic alias is always managed. ⛔ **TWO FIXES MADE, both Δ0 on the ladder:** a FOURTH copy of the generic-alias arm in `Parser.containerSlotValueType` was PROVABLY DEAD and is deleted (`declaredSlotType` one line above already asks it and RETURNS a `genericInstance` the arm above catches); and the shared cascade's one-deep recursion is now ASSERTED rather than asserted-in-prose, because the invariant lives in a different function whose own header describes the int-alias shape that would break it — an unbounded recursion is a HANG with nothing naming the edit that caused it. ⭐ **AND ONE HEADER CLAIM IS UPGRADED FROM PROSE TO A MEASUREMENT**: `typeIsManaged` says its agreement with the payload classifier is *"by INSPECTION rather than by construction"*. Half of it IS by construction — the two cascades ask their registries in DIFFERENT ORDERS, and an order can only matter for a name in TWO registries, which **E3006** and **E3061** refuse (measured in-file AND cross-file; a `function`-alias/`generic`-alias collision was caught, and the E3005 beside it shows the classifier really had read the other file's answer). ⇒ the ORDERS cannot disagree, only the COVERAGE can, and its failure shape is definite: a FORM taught to one and not the other LEAKS. **GATES:** `build maxon-shv2` exit 0, **zero warnings**, error-codes OK / 169 codes / 4 generated files up to date · shv2 host **3895 / 0**, exit 0, **no leak**, 0 `^FAIL` lines · **cross-target GREEN on every LOCAL lane: x64-linux 3721 / 0, wasm32-wasi 3665 / 0**, both exit 0 and no leak; both arm64 lanes SKIPPED (remote, opt-in) and therefore UNVERIFIED · golden drift measured with the RUNNER's own trailer: **128 before my edits and 128 after**, so neither moves an emitted byte · `docs/optimization-log.md` carries the ladder: **Δ0 in BOTH exact columns at all six rungs** against the rung's row, at `--repeat=2` so the cross-repeat allocation-equality check ran and passed, and `phase:parse` reads 243,583 / 3,904,219 — the rung's own post-fix numbers, reproduced. ⚠ Build 16.4 → 16.6 s and suite 66.9 → 66.9 s over an unchanged 3895 are inside this file's measured ~5% band and are NOT datapoints. Compile CPU **1364.0e9 ticks from 2868 of 3895 compiles at 12 workers** against the rung's 1375.8e9 over the identical corpus. | 16.6 | 11,012,096 | 9,839,878 | 66.9 | 1364.0e9 | 3895 |
| 2026-08-03 | `fix(shv2 A4d): the float element accessor — a panic already cured, an over-refusal that was not, and a shortest-round-trip search that died at f64.max` | ⭐ **arm64-macOS HOST, and BOTH SIZE NUMBERS ARE A FULL-CLEAN A/B against this rung's base commit `b069cee8f` built the same way** (a fresh worktree with no `maxon-shv2/.maxon`, the same `bin/maxon`): base **code 10,540,968 / exe 11,927,552**, this tree **code 10,541,120 / exe 11,927,552** ⇒ **code +152 B, exe UNCHANGED TO THE BYTE.** 152 B is the whole executable cost: one `or self.signatures.isArrayBaseName(name)` disjunct in `Parser.baseStoresArgumentsInline`, plus `stdlib/Builtins.maxon`'s `__shortestRoundTrip` gaining an `overflow` arm of its own. ⚠ **DO NOT compare against a WARM build of this same tree — it reads code 10,542,552 / symdata 650,816 where the clean build reads 10,541,120 / 650,584, a 1,432 B gap that is cache and not code**, which is this file's own header's warning measured once more on this host. ⭐ **NO EMITTED BYTE MOVED**, measured with the RUNNER rather than `git status`: golden drift reads **124 at the base and 124 here**, and the sets are IDENTICAL — the only fragment this rung touches is the one belonging to the spec case it rewrote (`generic-types/error.float-type-arg-builtin-generic`), regenerated under `--filter`. ⚠ The first `--update-required` run ALSO rewrote seven neighbouring goldens that were already drifting on main; all seven were reverted. Host suite **3681 / 0 new** — 3681 passed / 7 failed, main's identical 7 (3 file-order blame anchors + 4 `parsable-interface/parsable.float-fromstring*`, row X5, which SEGFAULT: measured exit 139 by hand, and independent of this rung — that is the READER `__float_bitsFromText`, this rung fixed the WRITER's digit search). Base measured here at **3670 / 7**, so +11 is exactly this rung's 11 new cases (`git`-counted test markers 3914 -> 3925). ⚠ **The board's recorded baseline of 3658 was STALE by 12.** wasm32-wasi green on every touched family (arrays 118/0, generic-types 76/0, float-literal-magnitude 20/0), including the 311-character `f64.max` print. C# **3242 / 3242, `3242 verified, 0 written`, exit 0, `specs/` diff EMPTY** — `stdlib/` changed, so that was a real gate and not a formality. ⚠ Build 12.7 s and suite 44.1 s are wall clock and are NOT datapoints. Compile CPU **206.1e9 ticks from 2666 of 3688 compiles at 12 workers**. `docs/optimization-log.md` carries the ladder: **Δ0 in both exact columns at all six rungs** on a same-path A/B — and that zero is the CORPUS's blind spot, since `ScaleCorpus.maxon`'s manifest records `Array with` measured 0 and the ladder emits no `Base with Args` at all. | 12.7 | 11,927,552 | 10,541,120 | 44.1 | 206.1e9 | 3688 |
| 2026-08-03 | `fix(A4l): the bootstrap's float-to-decimal — the oracle was wrong and the rewrite was right` | ⭐ **A `maxon-sharp` RUNG, so the shv2 numbers here measure a SIDE EFFECT**: shv2's own sources are untouched and only `stdlib/Builtins.maxon` changed under it. **arm64-macOS HOST. Both size numbers are FULL-CLEAN** (`rm -rf maxon-shv2/.maxon` before each) against the merge base `cff966f58` built the same way with ITS own `bin/maxon`: base **code 10,542,772 / exe 11,927,552**, this tree **code 10,545,232 / exe 11,927,552** ⇒ **code +2,460 B, exe UNCHANGED TO THE BYTE.** ⚠ **The base was built in the MAIN checkout and this tree in a worktree — different directory paths — so a small constant offset cannot be ruled out** (this file's `A4c` row measured exactly that hazard for the scale ladder); the +2,460 B is nonetheless the right ORDER for what was added to stdlib: `__float_exactDigits` split out of `__float_shortestDigits`, `__floatSignText`/`__floatMagnitudeText`/`__floatIsNan`/`__floatIsNotFinite`, `__leftPadded`, `__repeatedText` (which REPLACES `__zeros`), and the two new field renderers `__float_toStringPadded` / `__float_toStringFixed`, which shv2 never calls and its DFE therefore drops — only the restructured `__float_toString` path is really in shv2's binary. ⭐ **Golden drift measured with the RUNNER, not `git status`: 125 at the base and 125 here after regenerating the ONE fragment this rung moves** (`float-literal-magnitude/f64-max-prints-its-shortest-round-trip-digits`, whose `arm64.prologue` goes 64 → 32 because the sign/magnitude split moved locals into helpers — the test PASSES either way). The drift SETS were diffed, not counted: before regeneration the only difference was that one entry. shv2 host suite **3686 passed / 7 failed**, main's identical 7 (3 file-order blame anchors + the four `parsable-interface/parsable.float-fromstring*`, row X5 — those are the float READER segfaulting, and this rung touched only the WRITER). Exit 1, **not 101**. ⚠ Build 11.5 → 11.4 s and suite 44.1 s over an unchanged 3693 are inside this file's measured ~5% band and are NOT datapoints. Compile CPU **209.4e9 ticks from 2671 of 3693 compiles at 12 workers** against the base's **202.9e9 over the identical corpus** — +3.2%, which is at the edge of the band and is at least directionally real: every `"{float}"` in shv2's own suite now runs one more call layer. **The rung's real gate is the C# suite: 3244 passed / 3244, `3244 verified, 0 written`, exit 0** (3242 → 3244 is this rung's two new `specs/string-interpolation.md` cases), with **110 arm64-macOS fragment goldens regenerated and audited** and **18 `/specs` stdout values moved** from the fixed-six-decimal printer's output to the shortest round-trip — each of which `specs-shv2` already pinned to the identical digits. | 11.4 | 11,927,552 | 10,545,232 | 44.1 | 209.4e9 | 3693 |
| 2026-08-03 | `fix(A4n): the bootstrap's formatted integer — an unbounded width writing into a 72-byte scratch` | ⭐ **A `maxon-sharp` RUNG, so the shv2 numbers measure a SIDE EFFECT**: shv2's own sources are untouched, it uses NO formatted integer interpolation at all (grepped: 0 files), and only `stdlib/Builtins.maxon` and the emitted RUNTIME BLOB changed under it. **arm64-macOS HOST. Both size numbers are FULL-CLEAN** (`rm -rf maxon-shv2/.maxon` before each) against this rung's base `4f8069d45` built the same way, in a worktree of the same kind with its own `bin/maxon`: base **code 10,545,232 / exe 11,927,552**, this tree **code 10,544,696 / exe 11,927,552** ⇒ **code −536 B, exe UNCHANGED TO THE BYTE.** ⭐ **THE −536 IS NOT shv2's — IT IS EVERY MAXON BINARY'S, AND IT REPRODUCES EXACTLY**: a program that is nothing but `return 0`, with no interpolation anywhere, reads **48,356 → 47,820 bytes code, the same −536**. It is the runtime blob losing the two hand-emitted `maxon_i64_to_string_fmt` / `maxon_u64_to_string_fmt` routines (≈35.7 KB of C# emitter source deleted across the two targets) net of arm64 gaining a REAL `maxon_u64_to_string`, which had been a bare `B maxon_i64_to_string` — so an `int(0 to u64.max)` with bit 63 set printed `18446744073709551615` on x64 and `-1` here, one program with two answers decided by the target. ⭐ **Golden drift measured with the RUNNER, not `git status`: 125 at the base and 125 here**, the identical count A4l's row records, so no emitted byte moves for any program `specs-shv2` pins. shv2 host suite **3686 passed / 7 failed**, main's identical 7 (3 file-order blame anchors + the four `parsable-interface/parsable.float-fromstring*`, row X5). Exit 1, **not 101**. ⚠ Build 11.4 → 13.1 s and suite 44.1 → 45.3 s over an unchanged 3693 are wall clock inside this file's measured ~5% band and are **NOT datapoints**. Compile CPU **215.8e9 ticks from 2671 of 3693 compiles at 12 workers** against A4l's 209.4e9 over the identical corpus — +3.1%, at the edge of the band; shv2 calls none of the new code, so if it is real it is the stdlib parse, not the emitted program. **The rung's real gate is the C# suite: 3247 passed / 3247, `3247 verified, 0 written`, exit 0** (3244 → 3247 is this rung's three new `specs/string-interpolation.md` cases), with **36 arm64-macOS fragment goldens regenerated and audited** — 8 are the tests whose code genuinely changed (7 `arm64.prologue` stack sizes on the int-format cases, plus `panic-interpolation.format-spec` losing its `__fmt_spec_*` rdata literal) and 28 are `__interp_lit_*` / `__static_lit_*` / union tag renumbering, PROVED to be batch composition by compiling three of them standalone against the base: **byte-identical IR**. No x64-windows or wasm golden touched. | 13.1 | 11,927,552 | 10,544,696 | 45.3 | 215.8e9 | 3693 |
| 2026-08-03 | `review(A4n): the formatted integer holds — and the pair that drifted on arm64 was still two copies on x64` | ⚠ **arm64-macOS HOST, independent review of `a67c72e0a`; a `maxon-sharp` review, so the shv2 numbers again measure a SIDE EFFECT.** **Both size numbers are FULL-CLEAN** (`rm -rf maxon-shv2/.maxon` before the build): **code 10,544,696 / exe 11,927,552 — BOTH BYTE-IDENTICAL TO THE RUNG'S OWN ROW ABOVE**, which is the check that this review's three fixes are emitted-code-neutral, and it is corroborated directly: a probe program compiled to **x64-windows AND arm64-macos is BYTE-IDENTICAL to the pre-review binary on both targets** (`cmp` on the whole file, not a size comparison). ⭐ **THE ROW'S DEFECT REPRODUCES AND IS GONE.** On a control built at the base `4f8069d45`, `"A<{a:200}>B<{b:X}>"` prints `A<` + 96 spaces + `DEADBEEF` and **STOPS — exit 0, and the NEXT `print` never runs at all**; a width of 100,000 hangs past 20 s. On this tree the same program prints all 214 characters and then `END`, and widths 5,000 / 100,000 complete. ⭐ **THE CLEVEREST CLAIM SURVIVED AN EXHAUSTIVE ATTACK.** "One division serves every base" was tested against **Python's `format()` as an independent oracle**, not against the branch's own output: **58 values × 34 format spellings × 2 signedness flavours = 3,944 lines, BYTE-IDENTICAL to the oracle**, covering `0`, `1`, `i64.max`, `i64.min`, `2^63−1`, `2^63`, `2^63+1`, `u64.max−1`, `u64.max` and 30 seeded random 64-bit patterns, in `d`/`x`/`X`/`o`/`b` at widths 0/1/8/25/70 with and without zero fill, plus the degenerate `{n:0}` `{n:}` `{n:5q}` `{n:0x}` `{n:007}`. **The halving argument has no edge at 2^63 and none at the last digit.** The same battery run against the CONTROL differs on **exactly 391 lines = 23 bit-63 values × 17 decimal spellings, ALL in the unsigned flavour** — every signed row and every `x`/`X`/`o`/`b` row byte-identical, so the over-correction check is clean and the A/B is not one line wider than the defect. ⛔ **THREE FIXES, all Priority-1 duplication, all emitted-code-neutral:** **(a)** `X86CodeEmitter` still carried `maxon_i64_to_string` and `maxon_u64_to_string` as TWO hand-written instruction streams differing only in the sign block — **the very pair whose arm64 copies had already drifted into a wrong answer, and which this rung consolidated on arm64 only**; one `EmitMaxonIntegerToString(name, labelPrefix, signed)` now emits both, label names preserved so the output is byte-identical; **(b)** the parser's float and integer rewrites each carried a verbatim copy of the 7-statement resolve/mint/invalidate/**root** protocol — a step added to one and not the other is a leak or a UAF at RUNTIME rather than a compile error, so it is now `EmitStdlibTextCall`; **(c)** `I64ToStringMaxBytes` and `U64ToStringMaxBytes` were two constants holding one fact (both 21, both filled backwards from `buffer + 20` by both emitters on both targets) and are now `DecimalToStringMaxBytes`. ⭐ **The 36 regenerated goldens were re-audited independently and the rung's 28/8 split is CONFIRMED** — but by normalising the counters, not by the standalone compile the commit cites: a standalone `--no-batch --update-required` at base and tip does NOT produce byte-identical IR (label counters shift), so that particular proof does not reproduce as stated. Normalised, **21 are pure `__interp_lit_*`/`__static_lit_*` label renumbering, 7 are `mm_alloc` type-ID renumbering** (stdlib gained `__UnsignedDigitStep`), and **8 genuinely changed**. ⭐ **ZERO `ExitCode:` / `Stdout:` / `Stderr:` / `CompilerError:` lines moved in any of the 36**, and `specs/string-interpolation.md` has **96 insertions and 0 deletions** — no existing expected output moved at all. **GATES, all run first-hand after the fixes:** `dotnet build` exit 0, **0 warnings**, all five guards OK, 169 codes · C# suite **3247 passed / 3247, `3247 verified, 0 written`, exit 0** · shv2 **3686 passed / 7 failed**, exit 1 and **not 101**, the failure SET diffed against the base and IDENTICAL (3 file-order anchors + 4 `parsable.float-fromstring*`) · **golden drift 125 here against 125 at the base, and the SETS were diffed rather than counted — identical** · the −536 B every-binary shrink re-measured independently at **48,356 → 47,820** on a `return 0` program. ⚠ Build 13.1 → 12.6 s and suite 45.3 → 45.1 s over an unchanged 3693 are wall clock inside this file's measured ~5% band and are **NOT datapoints**. Compile CPU **216.0e9 ticks from 2671 of 3693 compiles at 12 workers** against the rung's 215.8e9 over the identical corpus — 0.1%, well inside the band. ⚠ **This branch still carries the PRE-review `__repeatedText`** (one append per repeat, quadratic in the width); `main` already has the halve-and-square rewrite from A4l's review and the branch does not touch the function, so the rebase takes main's version cleanly — the wide-width timings above are therefore an UPPER bound. | 12.6 | 11,927,552 | 10,544,696 | 45.1 | 216.0e9 | 3693 |
| 2026-08-03 | `fix(A4q): an interpolation expression must consume its own braces` | ⭐ **ONE RULE AT FOUR DOORS, IN BOTH COMPILERS: an expression parsed out of a DELIMITED TOKEN REGION must consume the region.** The row was filed about interpolation and the same silent drop was live at parameter and field defaults — `b Real = 1e100` defaulted to **1.0** on BOTH compilers. Three hunks in `maxon-sharp/Compiler/2-Parser.cs` (one shared `RequireInjectedStreamConsumed`, called at the interpolation body and at the captured default; plus a brace-depth term in the format specifier's colon scan) and one in `maxon-shv2/Compiler/Parser.maxon` (`PendingParamDefault.exprEnd`, checked at the drain). **arm64-macOS HOST. Every size number is FULL-CLEAN** (`rm -rf maxon-shv2/.maxon/cache` before each) against the merge base `ac6ba330d` built the same way with ITS own `bin/maxon`: base **code 10,555,944 / exe 11,943,936**, this tree **code 10,561,764 / exe 11,960,320** ⇒ **code +5,820 B, exe +16,384 B (four pages)**, which is one new `TokenIndex` field on `PendingParamDefault` (widening its array's stride and every construction of it) plus the drain's guard and its message. ⭐ **THE BOOTSTRAP HALF IS PROVED EMITTED-CODE-NEUTRAL BY A DIRECT A/B**: the BASE shv2 sources compiled by the NEW bootstrap are **`cmp`-IDENTICAL to the control**, byte for byte across 10.5 MB and 157 files — so no interpolation and no default anywhere in `stdlib/` or `maxon-shv2/` was reading its boundary the old way. ⚠ **FULL-CLEAN IS LOAD-BEARING AND IT COST A FALSE ALARM**: an INCREMENTAL rebuild (one source `touch`ed, warm `.mxc` cache) emits **+1,432 code bytes** over a full rebuild of the identical sources, and the merge base with its OWN unmodified bootstrap reproduces the same pair — a pre-existing property of `maxon build <project>`, reported to the coordinator, not fixed here. **Compare a full-clean number only against another full-clean number.** shv2 suite **3713 passed / 7 failed** — the base's identical 7, and 3708 → 3713 is this rung's five new cases; golden drift **130 at the tip against 130 at the base, the two note blocks diffed BYTE-IDENTICAL**, so not one fragment moved. C# suite **3253/3253, `3250 verified, 3 written`, exit 0** — the three written are newly minted fragments of this rung's own cases and **no pre-existing golden shows `M`**. | 12.7 | 11,960,320 | 10,561,764 | 45.6 | 216.2e9 | 3720 |
| 2026-08-03 | `review(A4q): the rule was right and a third injected stream was never asked` | ⚠ **arm64-macOS HOST, independent review of `08bb42056`.** **Size numbers are FULL-CLEAN** (`rm -rf maxon-shv2/.maxon/cache` before the build): **code 10,561,764 / exe 11,960,320 — BOTH BYTE-IDENTICAL TO THE RUNG'S OWN ROW ABOVE**, and better than a size comparison: the rebuilt `maxon-shv2` binary is **`cmp`-IDENTICAL to the pre-review one**, so this review's bootstrap change moves no emitted byte anywhere in 157 files. ⭐ **THE RUNG'S OWN LOAD-BEARING CLAIMS ALL REPRODUCE FIRST-HAND.** The `cmp` proof: base shv2 sources compiled by the NEW bootstrap are byte-identical to a control built at `ac6ba330d` by its own bootstrap (sha256 `7057c616…` both ways). Golden drift **130 at tip against 130 at base, the two `note:` blocks diffed BYTE-IDENTICAL over 654 lines** after normalising the worktree path. The (b)-rejection evidence: `LANGUAGE_REFERENCE.md:238` does say floats must contain a decimal point, `specs/literals.md`'s four exponent examples all carry one, `lexer-edge-cases/float-exponent-eof` pins `1e10` at top level, and `1e10` is refused in **every** other position measured — `let x =`, a call argument, an array literal, a binary operand and `return`. Both cured defects reproduce on controls: `b Real = 1e100` defaults to **1.0** on BOTH compilers at the base and is E2010 on both at the tip; the field-default drop was **bootstrap-only** as claimed (shv2 already refused), and a FLOAT field default (`var v as Real = 1e100` → `1.0`) proves the single `EmitDefaultFromTokens` call really does serve parameter and field defaults both. Over-refusal probed on both compilers across bare names, format specs, named arguments, operators, casts, ternaries, nested strings, escaped braces, `try/otherwise`, array literals and defaults carrying commas inside brackets and parens — **every result identical to the control except the two the rung intends** (`"{Pt{x: 7}}"` and `"{Pt{x: 7}:8}"`, E2007 before and working now). ⛔ **THE REVIEW'S FINDING IS THE RUNG'S OWN THESIS ONE DOOR FURTHER ON: `EmitSingleDeferredGlobalInit` IS A THIRD INJECTED STREAM AND IT ASKED NOTHING.** `var g = Box.create() zzz` compiled, ran and printed 3 on the branch; so did `static var b = Box.create() zzz` inside a type, through the lazy-static door. It could not ask, because it parsed IN PLACE — so it now copies its region into a private EOF-terminated list exactly as `EmitDefaultFromTokens` does and calls the SAME `RequireInjectedStreamConsumed`, which is what makes one rule serve three doors instead of two plus a gap. **It also had no `try/finally`**: `ParseExpression` throws on an unresolvable initializer and the token swap was undone on the success path only — the precise hazard `EmitDefaultFromTokens`' own header is written about. `ExpectConstInitializerFullyConsumed` is documented in place as NOT a second copy (it fires when the const EVALUATOR stopped, not when the parser did) so a later reader does not merge two different causes. **GATES, all run first-hand after the fix:** `dotnet build` exit 0, **0 warnings**, five guards OK, 169 codes · C# suite **3255 passed / 3255, `3253 verified, 2 written`, exit 0** — the two written are this review's own new fragments, **no pre-existing golden shows `M`** · shv2 **3713 passed / 7 failed**, the base's identical 7, exit 1 and **not 101** · drift 130 vs 130. ⚠ Build 12.7 → 12.8 s and suite 45.6 → 45.7 s over an unchanged 3720 are wall clock inside this file's measured ~5% band and are **NOT datapoints**. Compile CPU **219.3e9 ticks from 2687 of 3720 compiles at 12 workers** against the rung's 216.2e9 — +1.4%, inside the band, and shv2's binary is byte-identical so there is nothing it could be. ⚠ **Reported, deliberately NOT fixed (its own row):** `enum W  light = 1e10` declares **TWO** cases — `light`=1 and `e10`=2 — identically on both compilers. That is not this rung's rule at a fourth door: nothing is dropped, and `enum W  a = 1  b = 2` on ONE line is legal today, so refusing it is a GRAMMAR decision about whether enum cases are newline-separated. | 12.8 | 11,960,320 | 10,561,764 | 45.7 | 219.3e9 | 3720 |
| 2026-08-03 | `A4r` — the compile no longer inherits the previous compile's conclusions | ⚠ **arm64-macOS HOST. THIS ROW REPAIRS THE PREMISE OF THE `code bytes` COLUMN, so read every earlier row knowing what it did not know.** Until this commit a build's output depended on **whether another program had been compiled first in the same process**: the process-wide parsed-stdlib cache handed every compile a `Clone()` that copied the function bodies but SHARED the type graph, the op objects and the SSA values, all of which a compile WRITES to. So `maxon build <project>` — which compiles the build-runner first — emitted different bytes from a warm build that skipped the runner. **Measured on the merge base, same tree, minutes apart: cold `10,564,420` code / `651,856` symdata (sha `c19f04aa…`), warm `10,565,852` / `652,096` (sha `db8fc008…`).** ⇒ **an earlier row's `code bytes` records whichever state its measurer happened to be in, and nothing on the row says which.** ⭐ **After the fix BOTH read `10,565,852` / `652,096`, sha `db8fc008…` — the SAME hash the old compiler produced when it compiled the project ALONE.** The fix invents no third answer; it makes the hermetic one universal. The +1,432 code / +240 symdata against the old COLD number is six functions (`__Array_DecimalDigit{,.get,.set,.slice,.accessFailure}`, `__destruct___Array_DecimalDigit`) that a contaminated compile silently folded onto `DigitString`; ⚠ do NOT "fix" it back — the smaller number was the contaminated one. Suite and drift are unmoved against a like-for-like base build in a second worktree: **3750/7 both, 130 drifting fragments both** (this Mac's inherited arm64 lane staleness — zero of the 130 mention `DigitString`/`DecimalDigit`). Build time +0 outside the ~5% band. | 12.1 | 11,960,320 | 10,565,852 | 49.7 | 229,116,144,174 | 3757 |
| 2026-08-03 | `review(A4r): an op's reference INTO the type graph is a reference into the type graph` | ⚠ **arm64-macOS HOST, independent review of `c67db4fe6`.** **Size numbers are FULL-CLEAN** (`rm -rf maxon-shv2/.maxon/cache` before the build): **code 10,565,852 / exe 11,960,320**, and the stronger statement — the rebuilt binary is **`shasum`-identical to the rung's own (`db8fc008618fc82d`)**, so this review's bootstrap change moves no emitted byte across all 157 files. shv2 build 11,516 ms cold / 12,823 ms warm (wall, inside the ~5% band; not a datapoint). Suite **3750 passed / 7 failed in 50 s**, main's identical 7, golden drift **130 — SET-IDENTICAL to a like-for-like `origin/main` build** in a second worktree, not merely equal in count. ⭐ **THE RUNG'S CLAIMS ALL REPRODUCE FIRST-HAND, INCLUDING THE DEFECT.** Built the merge base in a control worktree and measured the cold/warm cycle both sides: base cold **10,564,420 / 651,856 / `c19f04aa…`** vs base warm **10,565,852 / 652,096 / `db8fc008…`**, tip cold *and* warm **`db8fc008…`** — the fix invents no third answer, it makes every compile emit what the old compiler emitted when the program was compiled ALONE. Reproduced independently on a **second, unrelated project** (a 4-line float program): base cold 112,996 / 18,056 vs base warm 114,428 / 18,288 — **+1,432 code / +232 symdata, the same six-function delta as shv2** — against tip cold == tip warm == 114,428 / 18,288. ⭐ **THE REVIEW'S OWN FINDING — the fifth shared thing.** `Clone` copied Functions, the type graph, the ops and the SSA values, and then let every op's and value's own reference INTO the type graph keep pointing at the TEMPLATE: `MaxonTryCallOp.ThrowsType` and `MaxonPromise.ErrorType` reach a **mutable `IrEnumType`**, and thirteen more op fields reach the graph through an `IrFunctionType` — which `TypeGraphCopier` copies precisely because "a reference into the ORIGINAL graph reached through one of these is exactly as contaminating as a direct one". `OpGraphCopier` now takes the module's ONE `TypeGraphCopier` and rebinds types beside values, which also restores the reference identity `RefreshTypeAliasTypeParams`'s `currentType != paramType` test reads. Emitted-byte-neutral, measured. ⚠ **The exhaustiveness THROW was not exhaustive, and that is measured in BOTH directions**: its reachability walk never crossed an ARRAY, so a `MaxonValue[]` field was neither rebound nor refused. Probe field added to `MaxonBinOp` — **without the array traversal the compile SUCCEEDS at 114,428 bytes and says nothing**; with it, `error E9001: MaxonBinOp.<GuardProbe>k__BackingField reaches an SSA value or a type through 'MaxonValue[]'`. ✅ **The x86 counter claim survives falsification**: `_labelCounter`/`_nonnullSkipCounter`/`_floatBranchCounter` seeded at 800000/900000/700000 and the compiler rebuilt — an x64-windows program with a dense enum match (jump table, `__jt_<func>_<n>`) and floats is **`shasum`-identical to the unseeded build**, `74d4a33b…` both ways. Those numbers really do not reach the emitted bytes. | 11.8 / 12.9 | 11,960,320 | 10,565,852 | 50 | — | 3750 |
| 2026-08-03 | `fix(A4s): one generic instance, one name — the instance's identity is settled before it is looked up` | ⚠ **arm64-macOS HOST.** Size numbers are FULL-CLEAN (`rm -rf maxon-shv2/.maxon` before the build) and **cold == warm == `46aab122b62941c3`**, so A4r's hermeticity holds through an interning change. ⭐ **code 10,565,852 → 10,562,912 (−2,940 B), symdata 652,096 → 651,640 (−456 B), exe unchanged at 11,960,320 (page granularity).** The delta is EXACTLY ten emitted functions removed and one added, and nothing else: `__Array_Byte.{count,accessFailure,get,slice}`, `__Array_DecimalDigit.{accessFailure,get,set,slice}`, `__destruct___Array_Byte`, `__destruct___Array_DecimalDigit` go, `DigitString.set` arrives — the real `.set` moving under the name that owns the instance. ⭐ **THE CAUSE WAS AN ORDERING, NOT A MISSING RULE.** `ResolveStructReturnTypeThroughSelf` searched the alias table for an existing name for the instance BEFORE dropping the conformance-bound params, and minted a mangled name AFTER: `Array.slice` resolves `{Element=DecimalDigit, Iter=ArrayIter}`, so the search compared a 2-param key against `DigitString`'s 1-param key, matched nothing it could ever match, then dropped `Iter` and minted `__Array_DecimalDigit` beside the `DigitString` it had just failed to recognise. The filter now runs before BOTH, so lookup and mint ask about the same instance. ⚠ **`FindArrayAliasByElementName`'s stdlib/local exclusion was NOT the cause and is untouched** — it guards array-literal INFERENCE (a bare `[1,2,3]` must not adopt an unrelated declared alias and mask its unused-typealias error), a different question from instance identity. **7 C# goldens moved and every one is a pure symbol rename** (`__Array_Narrow.get` → `NarrowCol.get`, `__Array_Int.count` → `IntArray.count`, `__Array_i1.get` → `_b_push_and_get_BoolArray.get`) plus the deletion of the duplicate `__destruct___Array_DecimalDigit` — not one instruction, register or offset changed, which is itself the proof the two families were the same code twice. shv2 golden drift **130, unchanged from A4r's like-for-like base** — expected, since a bootstrap change cannot move what shv2 itself emits. ⚠ **THE SUITE TIMES ARE NOT DATAPOINTS** (48 s over 3757, inside the ~5% band). Also repaired the row above, which lost its six numeric columns; every number in it is quoted from its own prose and `compile cpu` is `—` because it was never recorded. | 11.8 / 12.9 | 11,960,320 | 10,562,912 | 48 | 225.6e9 @12w | 3750 |
| 2026-08-03 | `fix(bootstrap A4t): the alias table is complete before any specialization expands a field` | ⚠ **arm64-macOS HOST. THE SIZE COLUMNS ARE A REAL A/B, MEASURED IN THIS WORKTREE ON BOTH SIDES**: base (`f94846c83`, its own bootstrap built here) exe **12,124,160**, code **10,708,328**; tip exe **12,091,392**, code **10,686,688** — **−32,768 exe / −21,640 code / −3,288 symdata**, and the shrink is entirely **70 fewer emitted functions** (9,308 → 9,238, a name-by-name diff: **105 removed, 35 added**, every addition the same body under the DECLARED name it was already emitted under). Thirteen generic instances were being emitted TWICE — `Array_ValueId` beside `ValueIdArray`, `Array_i1` beside `BoolArray` — because `RegisterConcreteTypeAlias`'s field-alias mint asked "does the project already name this instance?" against the aliases registered SO FAR, i.e. against the order the filesystem handed over the sources. A whole-project declaration pass now settles that index before any file specializes anything. ⭐ **THE ACCEPTANCE TEST IS A PROPERTY, AND IT IS NOW EXACT**: built forwards and with `MAXON_SOURCE_ORDER=reverse`, the emitted FUNCTION SET is **identical (9,238 both ways, empty diff both directions)** where the base differed by **75 in / 42 out**; every one of those 9,238 functions has the identical byte length in both orders **except `__module_init` (−52 B) and 28 `_$closure_N` renumberings whose size multiset is a permutation** — the two remaining, unrelated file-order effects (global-init sequence, closure counter), neither of which is an alias. ⚠ **BYTE-IDENTITY UNDER REVERSAL WAS NEVER ACHIEVABLE AND IS NOT THIS DEFECT**: a two-file program with no typealias at all already builds to a different binary both ways (`0395d170…` vs `8974011790…`), because function EMISSION ORDER follows file order. ⚠ Neither TIME is a datapoint: build 11.9 → 11.7 s and the suite 47 s both sit inside the ~5% band, and the whole-project pass this rung adds is a seventh token walk over every file. A4r's cold-vs-warm property re-verified on the tip: both `0cf4ad91b9f5f25c…`. | 11.7 | 12,091,392 | 10,686,688 | 47 | 230.4e9 @12w | 3816 |
| 2026-08-03 | `fix(A4u): a generic instance's CONST arguments are part of its identity` | ⚠ **arm64-macOS HOST. THIS IS A BOOTSTRAP-ONLY CHANGE AND IT IS EMITTED-CODE-NEUTRAL, MEASURED TWO WAYS.** The shv2 binary built by the NEW bootstrap is `shasum`-verified base-vs-tip BY THE REVIEW rather than against A4t's number — A4t recorded `0cf4ad91b9f5f25c…` for a DIFFERENT subject (A4r's hermeticity property), never an shv2 shasum, so the attribution in the first draft of this row was wrong even though the neutrality itself is true. And the C# suite run against the merge base's OWN spec files reads **3255 passed / 3255, `3255 verified, 0 written`, exit 0**: every pre-existing golden verifies unmoved. ⭐ **THE DEFECT: `__capacity` was dropped from every written form of a generic instance's identity, and three doors gave three different wrong answers.** (1) a `Vector with 4 Element` field of a generic type ADOPTED a declared `Vec3`; (2) with nothing declared, two capacities of one element type minted the SAME structural name and both read `count() == 0`; (3) `IsStructTypeCompatible` accepted a `Vec3` where a `Vec4` was declared, with no diagnostic — the callee then read 3. ⭐ **THE CURE IS ONE DECIDER, NOT SIX THAT AGREE**: `IrStructType.InstanceKey` / `InstanceNameSuffix` / `ConstArgSegments` now live on the type that OWNS `ConstParams`, and the parser's five sites plus monomorphization's `TypeSubstitution.FindConcreteAlias` all ask them — three of those six were the same count-plus-per-parameter-name loop written out again, and `FindConcreteAlias` was the one that re-decided the parser's correct answer wrongly (the field type was already `Vector_4_Int`; every CALL on it went to the 3-element family). `TypeAliasInfo` had to gain `ConstParams` because that record is monomorphization's only description of the instance. ⚠ **OVER-SEPARATION WAS PROBED AND ONE DIRECTION HAD TO STAY LOOSE**: `Array from [1, 2, 3]` reaches assignability as a capacity-3 instance while `IntArray = Array with Int` declares none, so `HaveMatchingTypeParams` treats an ABSENT size as making no claim — deliberately looser than `InstanceKey`, exactly as the ranged rule beside it is. Without that, an array literal stopped being assignable to its own declared alias. ⚠ Neither TIME is a datapoint (build 12.0 s, suite 51 s, inside the ~5% band). shv2 **3809 passed / 7 failed**, main's identical 7, exit 1 and not 101; golden drift **139, unchanged from A4t's**. A4t's reversal property re-verified on the tip: **9,238 functions both ways, set diff EMPTY**, with ⚠ **THE REVERSAL RESIDUE LINE WAS COPIED FORWARD, NOT FRESHLY MEASURED** — the review measured **ZERO** residue here (9,238 functions both ways, empty set diff, identical sizes AND order) but could not confirm its seam invocation perturbed anything, since every control came out identical including one A4t reported as differing. The property holds; the mechanism of the check is unproven. | 12.0 | 12,091,392 | 10,686,688 | 51 | 234.2e9 @12w | 3816 |
| 2026-08-04 | `241c0d79c` | 22.6 | 11,462,144 | 117.9 | 1971.7e9 | 4129 |
