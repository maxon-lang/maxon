function main() int
	var x = 5
	if x = 5 'check'
		return 1
		else 'check'
			return 0
		end 'check'

		let arr = [1, 2, 3, 4, 5]
		for i in arr 'loop'
			print()
		end 'loop'

		return 0

	end 'main'
