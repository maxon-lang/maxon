namespace math 'math'
    function add(a int, b int) int
        return a + b
    end 'add'
end 'math'

function main() int
    // Hover over math.add to see the warning
    // Use Quick Fix (Ctrl+. or Cmd+.) to simplify
    return math.add(5, 3)
end 'main'
