// Code generator for the Maxon self-hosted compiler
// Minimal version - just enough to generate MIR for basic.maxon

export struct CodeGen
    var output string
    var tempCounter int
    var localVars map from string to string

    export function create() CodeGen
        return CodeGen{output: "", tempCounter: 0, localVars: map from string to string}
    end 'create'

    export function newTemp() string
        var temp = "%t" + tempCounter.toString()
        tempCounter = tempCounter + 1
        return temp
    end 'newTemp'

    export function emit(code string) void
        output = output + code + "\n"
    end 'emit'

    //=========================================================================
    // Code generation
    //=========================================================================

    export function generate(program Program) void
        emit("; Maxon IR generated by self-hosted compiler")
        emit("; Source: " + program.fileName)
        emit("")

        var i = 0
        while i < program.declarations.count() 'decls'
            genDecl(program.declarations[i])
            i = i + 1
        end 'decls'
    end 'generate'

    export function genDecl(decl Decl) void
        match decl 'gen'
            case function_(name, params, returnType, body, isExport, isExtern, loc) then
                genFunction(name, returnType, body)
        end 'gen'
    end 'genDecl'

    export function genFunction(name string, returnType TypeRef, body array of Stmt) void
        // Reset state for each function
        localVars = map from string to string
        tempCounter = 0

        var returnTypeStr = "i32"
        if returnType.kind == TypeKind.void 'void'
            returnTypeStr = "void"
        end 'void'

        emit("define " + returnTypeStr + " @" + name + "() {")
        emit("entry:")

        var i = 0
        while i < body.count() 'body'
            genStmt(body[i])
            i = i + 1
        end 'body'

        emit("}")
        emit("")
    end 'genFunction'

    export function genStmt(stmt Stmt) void
        match stmt 'gen'
            case return_(value, loc) then
                if let v = value 'hasValue'
                    var temp = genExpr(v)
                    emit("    ret i32 " + temp)
                else 'hasValue'
                    emit("    ret void")
                end 'hasValue'
            case let_(name, typeAnnotation, value, loc) then
                var valueTemp = genExpr(value)
                var varTemp = newTemp()
                emit("    " + varTemp + " = copy i32 " + valueTemp)
                localVars[name] = varTemp
        end 'gen'
    end 'genStmt'

    export function genExpr(expr Expr) string
        match expr 'gen'
            case intLiteral(value, loc) then
                return value.toString()
            case varRef(name, loc) then
                return localVars[name]
        end 'gen'
    end 'genExpr'
end 'CodeGen'
