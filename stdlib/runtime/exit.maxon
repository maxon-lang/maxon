// stdlib/runtime/exit.maxon
// Process exit functionality

// Windows API declaration (at module level so it's accessible)
extern function ExitProcess(exitCode int)

namespace runtime 'runtime'
    function exit(code int) int
        ExitProcess(code)
        return 0
    end 'exit'
end 'runtime'
