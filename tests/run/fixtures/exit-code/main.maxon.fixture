// A program whose whole product is its exit code: `maxon run` must forward it rather than reporting its
// own success at having run something.

function main() returns ExitCode
	return 7
end 'main'
