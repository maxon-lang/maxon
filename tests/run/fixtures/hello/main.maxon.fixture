// The smallest thing `maxon run` can be asked to do. The greeting is spelled in the harness too, where
// every case that stages this fixture reads it back.

function main() returns ExitCode
	print("hello from maxon run\n")
	return 0
end 'main'
