tests/test-fixtures/implied-try/ledger.test.maxon:
  ✓ a bare assertion passes
  ✗ a foreign error mid-statement fails the test
  ✗ a throwing interface method called bare fails the test
  ✗ a bare await of a throwing promise fails the test
  ✗ a bound String from a throwing call fails the test
  ✗ a discarded String from a throwing call fails the test
  ✓ an empty parenthesized traversal runs no trips
  ✓ an empty rebranded traversal runs no trips

FAIL  tests/test-fixtures/implied-try/ledger.test.maxon > a foreign error mid-statement fails the test
  threw FetchError.missing
  at ledger.test.maxon:27

FAIL  tests/test-fixtures/implied-try/ledger.test.maxon > a throwing interface method called bare fails the test
  threw FetchError.missing
  at ledger.test.maxon:53

FAIL  tests/test-fixtures/implied-try/ledger.test.maxon > a bare await of a throwing promise fails the test
  threw FetchError.missing
  at ledger.test.maxon:64

FAIL  tests/test-fixtures/implied-try/ledger.test.maxon > a bound String from a throwing call fails the test
  threw FetchError.missing
  at ledger.test.maxon:77

FAIL  tests/test-fixtures/implied-try/ledger.test.maxon > a discarded String from a throwing call fails the test
  threw FetchError.missing
  at ledger.test.maxon:89

 3 pass
 5 fail

 8 tests across 1 file.
