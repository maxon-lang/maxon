tests/test-fixtures/implied-try/ledger.maxtest:
  ✓ a bare assertion passes
  ✗ a foreign error mid-statement fails the test
  ✗ a throwing interface method called bare fails the test
  ✗ a bare await of a throwing promise fails the test
  ✗ a bound String from a throwing call fails the test
  ✗ a discarded String from a throwing call fails the test
  ✓ an empty parenthesized traversal runs no trips
  ✓ an empty rebranded traversal runs no trips

FAIL  tests/test-fixtures/implied-try/ledger.maxtest > a foreign error mid-statement fails the test
  threw FetchError.missing
  at ledger.maxtest:27

FAIL  tests/test-fixtures/implied-try/ledger.maxtest > a throwing interface method called bare fails the test
  threw FetchError.missing
  at ledger.maxtest:53

FAIL  tests/test-fixtures/implied-try/ledger.maxtest > a bare await of a throwing promise fails the test
  threw FetchError.missing
  at ledger.maxtest:64

FAIL  tests/test-fixtures/implied-try/ledger.maxtest > a bound String from a throwing call fails the test
  threw FetchError.missing
  at ledger.maxtest:77

FAIL  tests/test-fixtures/implied-try/ledger.maxtest > a discarded String from a throwing call fails the test
  threw FetchError.missing
  at ledger.maxtest:89

 3 pass
 5 fail

 8 tests across 1 file.
