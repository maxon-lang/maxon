// The string literal below is left unterminated on purpose: this file must not tokenize.
test 'the tokenizer never gets past this file'
	let unterminated = "this string literal has no closing quote
	try Expect.equal(unterminated.byteLength() as AssertedInt, expected: 0)
end 'the tokenizer never gets past this file'
