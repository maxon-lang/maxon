test 'a directive quoted in a prose block outside any case is skipped and the case after it runs'
	requireMarkerShapeRan(testLabel("quoted-directive", testName: "quoted-directive.runs-after-a-prose-block"))
end 'a directive quoted in a prose block outside any case is skipped and the case after it runs'
