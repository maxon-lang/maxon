test 'a test marker opened with two spaces after the comment opener is read'
	requireMarkerShapeRan(testLabel("marker-spaced", testName: "marker-spaced.opened-with-two-spaces"))
end 'a test marker opened with two spaces after the comment opener is read'
