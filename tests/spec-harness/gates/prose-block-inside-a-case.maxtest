test 'a prose block inside a case holding a heading and a quoted marker leaves the case whole'
	requireMarkerShapeRan(testLabel("prose-inside", testName: "prose-inside.runs-past-its-prose-block"))
end 'a prose block inside a case holding a heading and a quoted marker leaves the case whole'
