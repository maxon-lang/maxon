// A literal's interior is NOT grouped: its lines are elements, and forcing them apart would invent a
// structure the author did not write.
module function corpus() returns StringArray
	let rows = [
		"alpha",
		"beta",
		"gamma",
	]
	return rows
end 'corpus'
