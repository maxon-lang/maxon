typealias Word = bits(64)
typealias Flags = bits(32)
typealias Nibble = bits(4)

function main() returns ExitCode
	let w = 1 as Word
	let f = 2 as Flags
	let n = 3 as Nibble
	print("{w} {f} {n}\n")
	return 0
end 'main'
