function main() returns ExitCode
	for i in 1 to 4000 'backlog'
		print("backlog {i}\n")
	end 'backlog'

	printError("err after the backlog\n")

	for i in 1 to 40 'tail'
		sleep(5)
		print("tail {i}\n")
	end 'tail'

	return 0
end 'main'
