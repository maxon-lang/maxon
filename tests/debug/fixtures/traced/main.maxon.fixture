typealias Answer = int(0 to 255)

function later(base Answer) returns Answer
	sleep(1)

	return base + 1
end 'later'

function main() returns ExitCode
	let pending = async later(41)
	let answer = await pending

	print("answer {answer}\n") // anchor: after-await

	return 0
end 'main'
