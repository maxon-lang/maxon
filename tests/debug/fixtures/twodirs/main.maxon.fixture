export typealias Visit = int(0 to 1000)

function main() returns ExitCode
	var total = 0

	for round in 0 upto 2 'rounds'
		total = total + first(round) + second(round)
	end 'rounds'

	print("total {total}\n")

	return 0
end 'main'
