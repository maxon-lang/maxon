// list-only.maxtest — `--list` prints the roster and compiles nothing.
//
// One spawning `test` per file — `fixtures.maxtest`'s header owns why.

test 'list-only prints the roster and compiles nothing'
	checkFixture("list-only")
end 'list-only prints the roster and compiles nothing'
