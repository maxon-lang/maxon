// uncaught-throw.maxtest — A test body that lets a FOREIGN error out: the substituted handler names the error's
// type, its case and the throwing call's own line.
//
// One spawning `test` per file — `fixtures.maxtest`'s header owns why.

test 'uncaught-throw names the error type, its case and the throwing line'
	checkFixture("uncaught-throw")
end 'uncaught-throw names the error type, its case and the throwing line'
