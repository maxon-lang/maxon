// assertion-failure.maxtest — A red assertion: the `✗` marker, the FAIL block, and the `expected:`/`received:` detail.
//
// One spawning `test` per file — `fixtures.maxtest`'s header owns why.

test 'assertion-failure prints the expected/received block and exits 1'
	checkFixture("assertion-failure")
end 'assertion-failure prints the expected/received block and exits 1'
