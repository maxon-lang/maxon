// json-face.test.maxon — `--json` reports the run the text face reports, carrying BOTH outcome spellings in one
// document.
//
// One spawning `test` per file — `fixtures.test.maxon`'s header owns why.

test 'json-face spells both outcomes in one document'
	try checkFixture("json-face")
end 'json-face spells both outcomes in one document'
