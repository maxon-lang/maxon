// list-only.test.maxon — `--list` prints the roster and compiles nothing.
//
// One spawning `test` per file — `fixtures.test.maxon`'s header owns why.

test 'list-only prints the roster and compiles nothing'
	try checkFixture("list-only")
end 'list-only prints the roster and compiles nothing'
