// implied-try.test.maxon — A test body with no `try`: a bare foreign throw fails its test, reported
// at the throwing operation's own line, and a bare assertion passes.
//
// One spawning `test` per file — `fixtures.test.maxon`'s header owns why.

test 'implied-try fails a test on a bare foreign throw and passes bare assertions'
	checkFixture("implied-try")
end 'implied-try fails a test on a bare foreign throw and passes bare assertions'
