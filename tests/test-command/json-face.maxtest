// json-face.maxtest — `--json` reports the run the text face reports, carrying BOTH outcome spellings in one
// document.
//
// One spawning `test` per file — `fixtures.maxtest`'s header owns why.

test 'json-face spells both outcomes in one document'
	checkFixture("json-face")
end 'json-face spells both outcomes in one document'
