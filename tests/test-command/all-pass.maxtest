// all-pass.maxtest — The whole GREEN report: two tests, one file, both passing, and the summary under them.
//
// One spawning `test` per file — `fixtures.maxtest`'s header owns why.

test 'all-pass reports two green tests and exits 0'
	checkFixture("all-pass")
end 'all-pass reports two green tests and exits 0'
