// no-tests-found.test.maxon — A project that compiles and declares no `test` exits 1 ON PURPOSE: a suite that has
// stopped containing tests reads exactly like one where everything passed.
//
// One spawning `test` per file — `fixtures.test.maxon`'s header owns why.

test 'no-tests-found is a FAILURE, not an empty green run'
	try checkFixture("no-tests-found")
end 'no-tests-found is a FAILURE, not an empty green run'
