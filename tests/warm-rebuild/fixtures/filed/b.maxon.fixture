// The last file of the filed-type fixture, and the one the gate edits. Its argument-inferred `create` is the only
// instance of `Box` in the program, so an edit here must leave the first file's parse reused.
function main() returns ExitCode
	let b = Box.create(5)
	return b.pack() as ExitCode
end 'main'
