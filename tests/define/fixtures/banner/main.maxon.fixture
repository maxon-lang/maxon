// One top-level String constant with a written-out default, and a program whose whole output is that
// constant — so what the binary prints IS the value the build gave it.
typealias ExitCode = int(0 to 255)

let Banner = "default"

function main() returns ExitCode
	print("{Banner}\n")
	return 0
end 'main'
