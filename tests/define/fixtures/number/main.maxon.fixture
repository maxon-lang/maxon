// A top-level constant that is NOT a string literal. `--define` must refuse it rather than retype the
// binding to whatever the command line spelled.
typealias ExitCode = int(0 to 255)

let Number = 7

function main() returns ExitCode
	print("{Number}\n")
	return 0
end 'main'
