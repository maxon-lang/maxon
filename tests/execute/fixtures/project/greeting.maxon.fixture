// The half of the project `main.maxon` cannot supply for itself: a run that compiled only the named file
// would fail to resolve this.

export function greeting() returns String
	return "two files, one program"
end 'greeting'
