// A project of two files: `maxon run <directory>` must compile both, exactly as `maxon build` does with
// a directory.

function main() returns ExitCode
	print("{greeting()}\n")
	return 0
end 'main'
