// A program that does not compile. The call names a function nothing declares, which is a SEMANTIC
// refusal: it reaches the pipeline's own diagnostic sink rather than the lexer's.

function main() returns ExitCode
	return thereIsNoSuchFunction()
end 'main'
