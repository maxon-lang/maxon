# This directory holds a FIXTURE that `lsp-selftest` loads BY PATH (`DeclFixtureRelativePath`), never
# through a directory walk. `decl.maxon` declares its own `main`, so any project walk reaching this
# subtree would compile it and collide with the real one (E3006); this marker keeps it out of one.
# The same reason `track0/` carries one.
