// A version with more components than a Windows version resource holds.
// `program` is `BuildManifestHarness.maxon`'s `BuiltStem`.
typealias ExitCode = int(0 to 255)

export function build() returns ExitCode
	Build.build(".", output: "program", version: "1.2.3.4.5")
	return 0
end 'build'
