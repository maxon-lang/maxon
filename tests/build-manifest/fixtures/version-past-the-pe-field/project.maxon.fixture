// A version one of whose components the target's version field cannot hold.
// `program` is `BuildManifestHarness.maxon`'s `BuiltStem`.
typealias ExitCode = int(0 to 255)

export function build() returns ExitCode
	Build.build(".", output: "program", version: "1.70000.0")
	return 0
end 'build'
