test 'library tier project: a stdlib document that was only opened is the one project rooted at the stdlib directory, with the tier counted'
	requireTheOnlyOpenedLibraryTierProject(libraryTierFilePathDocument())
end 'library tier project: a stdlib document that was only opened is the one project rooted at the stdlib directory, with the tier counted'
