test 'library tier project: a runtime document that was only opened is the one project rooted at the runtime directory, with the tier counted'
	requireTheOnlyOpenedLibraryTierProject(libraryTierClockDocument())
end 'library tier project: a runtime document that was only opened is the one project rooted at the runtime directory, with the tier counted'
