// Test: stdlib-namespaces.doc-example-1.1
namespace fs 'fs'
    function stdout() ptr
        // Returns stdout handle
    end 'stdout'
    
    function write(handle ptr, buffer ptr, length int) int
        // Write bytes to file handle
    end 'write'
end 'fs'

function main() int
    let out = stdout()
    var msg = "Hello\n"
    write(out, msg, 6)
    return 0
end 'main'
---
N/A
---
N/A
---
