namespace fs 'fs'
    function stdout() ptr
        // Returns stdout handle
    end 'stdout'
    
    function write(handle ptr, buffer ptr, length int) int
        // Write bytes to file handle
    end 'write'
end 'fs'

function main() int
    let out = stdout()
    var msg = "Hello\n"
    write(out, msg, 6)
    return 0
end 'main'

---
N/A
---
N/A
---
MaxoncStderr: ```
=== Compilation Failed ===
Found 5 errors:

Semantic Error: line 2, column 5
Function 'stdout' must return a value of type 'ptr'
  Note: All execution paths through the function must end with a return statement

  2 |     function stdout() ptr
    |     ^

Semantic Error: line 6, column 5
Function 'write' must return a value of type 'int'
  Note: All execution paths through the function must end with a return statement

  6 |     function write(handle ptr, buffer ptr, length int) int
    |     ^

Semantic Error: line 6, column 32
The parameter 'buffer' is declared but its value is never used

  6 |     function write(handle ptr, buffer ptr, length int) int
    |                                ^

Semantic Error: line 6, column 20
The parameter 'handle' is declared but its value is never used

  6 |     function write(handle ptr, buffer ptr, length int) int
    |                    ^

Semantic Error: line 6, column 44
The parameter 'length' is declared but its value is never used

  6 |     function write(handle ptr, buffer ptr, length int) int
    |                                            ^

```
