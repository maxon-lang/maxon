<Project>

  <!-- Default the solution build to Release when no -c is passed, so the
       shipped compiler binary is optimized. `dotnet build -c Debug` overrides. -->
  <PropertyGroup>
    <Configuration Condition="'$(Configuration)' == ''">Release</Configuration>
  </PropertyGroup>

</Project>
