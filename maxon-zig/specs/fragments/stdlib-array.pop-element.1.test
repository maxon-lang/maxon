// Test: stdlib-array.pop-element.1
function main() returns int
    var arr = Array of int{}
    arr.push(10)
    arr.push(20)
    arr.push(30)
    
    if let val = arr.pop() 'pop'
        if val != 30 'check'
            return 1
        end 'check'
    end 'pop' else 'nil'
        return 2
    end 'nil'
    
    if arr.count() != 2 'cnt'
        return 3
    end 'cnt'
    
    return 0
end 'main'
---
ExitCode: 0
---
// IR generation failed: error.IrError
---
