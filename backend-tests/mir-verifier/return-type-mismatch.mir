; EXPECT: fail
; Test: return type mismatch

define i32 @test_return_type_mismatch() {
entry:
    %x = sitofp i32 42 to f64
    ; ERROR: returning f64 from function declared to return i32
    ret f64 %x
}
