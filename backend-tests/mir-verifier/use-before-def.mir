; EXPECT: fail
; Test: use before definition in same block

define i32 @test_use_before_def() {
entry:
    ; ERROR: %val is used before it's defined
    %result = add i32 %val, 5
    %val = add i32 10, 20
    ret i32 %result
}
