// ExitCode: 15
// Phase 8: Array sum using loop
// 1 + 2 + 3 + 4 + 5 = 15
function main() int
    var arr = [1, 2, 3, 4, 5]
    var sum = 0
    var i = 0
    while i < arr.length 'loop'
        sum = sum + arr[i]
        i = i + 1
    end 'loop'
    return sum
end 'main'
