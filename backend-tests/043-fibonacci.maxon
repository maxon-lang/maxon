// ExitCode: 55
// Phase 6: Recursion - fibonacci
// fib(10) = 55
function fib(n int) int
    if n <= 1 'base'
        return n
    end 'base'
    return fib(n - 1) + fib(n - 2)
end 'fib'

function main() int
    return fib(10)
end 'main'
