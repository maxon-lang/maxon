// ExitCode: 4
// Test: Integer to float promotion with sqrt
// Regression test for optimizer incorrectly treating SIToFP as a constant copy
function main() int
	var x = 16
	var result = sqrt(x)
	return trunc(result)
end 'main'
