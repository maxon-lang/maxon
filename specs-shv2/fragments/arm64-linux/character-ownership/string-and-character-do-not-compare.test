// Test: string-and-character-do-not-compare
function main() returns ExitCode
	let s = "é"
	let c = 'é'
	if s == c 'same'
		return 1
	end 'same'
	return 0
end 'main'
---
CompilerError:
error E3005: <fragment>:5:7: type mismatch: 'cannot compare String with Character'
