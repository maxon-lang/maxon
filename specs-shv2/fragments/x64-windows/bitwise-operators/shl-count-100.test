// Test: shl-count-100
function main() returns ExitCode
	let a = 1
	return a shl 100
end 'main'
---
CompilerError:
error E2054: <fragment>:4:15: Shift count 100 is outside the range 0 to 63: an int is 64 bits, so any other count is silently masked into that range
