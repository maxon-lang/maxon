// Test: shr-count-negative
function main() returns ExitCode
	let a = 128
	return a shr -1
end 'main'
---
CompilerError:
error E2054: <fragment>:4:15: Shift count -1 is outside the range 0 to 63: an int is 64 bits, so any other count is silently masked into that range
