// Test: while-loops.zero-iterations
function main() returns ExitCode
	var x = 10
	while x < 5 'loop'
		x = x + 1
	end 'loop'
	return x
end 'main'
---
ExitCode: 10
---
func @main {
  entry:
    x64.movRegImm32 rax, 10
    x64.movRegReg r8, rax
    x64.jmp whilehdr
  whilehdr:
    x64.movRegImm32 rax, 5
    x64.cmpRegReg r8, rax
    x64.jcc less, loop
    x64.jmp whileexit
  loop:
    x64.movRegImm32 rax, 1
    x64.addRegReg r8, rax
    x64.jmp whilehdr
  whileexit:
    x64.ret
}
