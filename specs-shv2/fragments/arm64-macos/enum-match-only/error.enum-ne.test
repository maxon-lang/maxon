// Test: error.enum-ne
typealias Integer = int(i64.min to i64.max)

union Container
	empty
	value(n Integer)
end 'Container'

function main() returns ExitCode
	let c = Container.empty
	if c != Container.empty 'check'
		return 1
	end 'check'
	return 0
end 'main'
---
CompilerError:
error E3066: <fragment>:11:7: cannot compare union values using '!=', use 'match' instead
