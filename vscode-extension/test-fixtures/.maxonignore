# Grammar test fixtures for the VS Code TextMate extension. Their content is
# pinned by snapshot tests (`*.maxon.snap`) and must remain byte-stable, so
# the formatter (which would rewrite indentation, normalize trailing
# comments, etc.) must skip this directory.
