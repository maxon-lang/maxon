// Test runner project for the maxon-dev MCP server. Built into
// `.maxon/maxon-dev-mcp-test.exe`; invoked by the `test_mcp` target in the
// repo's root `build.maxon`.
export function build() returns ExitCode
	Build.build("maxon-dev-mcp-test")
	return 0
end 'build'
