# Every hunk withdraws a visibility MODIFIER and nothing else: the tier the signature-visibility rule
# (E3167) requires of a type an exported signature names. The previous release audits exports without
# that rule, so it reads each of these as E3092/E3093/E3094 and refuses the tree; at the tier this
# patch writes, its audit agrees. What the shimmed first build loses is the wider tier and no code.
diff --git a/maxon-bin/Compiler/CompileMemory.maxon b/maxon-bin/Compiler/CompileMemory.maxon
--- a/maxon-bin/Compiler/CompileMemory.maxon
+++ b/maxon-bin/Compiler/CompileMemory.maxon
@@ -131,7 +131,7 @@ end 'deltaSum'
 
 // Index into a dense per-phase column. Bounded by the enums it indexes, both of which are
 // a couple of dozen arms.
-export typealias PhaseIndex = int(0 to 1024)
+typealias PhaseIndex = int(0 to 1024)
 
 typealias ColumnCell = int(0 to u64.max)
 typealias PhaseColumnArray = Array with ColumnCell
@@ -278,7 +278,7 @@ let TagTableBytesColumnTitle = "bytes"
 let TagLabelWidth = 34 as ColumnWidth
 let TagCensusRuleWidth = (TagLabelWidth + MemAllocsWidth + MemBytesWidth) as ColumnWidth
 
-export typealias TagBucketIndex = int(0 to 2047)
+typealias TagBucketIndex = int(0 to 2047)
 export typealias TagCensusMode = int(0 to 1)
 typealias TagRowCount = int(0 to 5)
 typealias ColumnSlot = int(0 to 2099199)
diff --git a/maxon-bin/Compiler/Compiler.maxon b/maxon-bin/Compiler/Compiler.maxon
--- a/maxon-bin/Compiler/Compiler.maxon
+++ b/maxon-bin/Compiler/Compiler.maxon
@@ -1938,7 +1938,7 @@ export function collectMaxonSources(dir FilePath, into MaxonSourcePathArray, sel
 	return try collectListedSources(entries, into: into, selection: selection, nestedProjects: nestedProjects)
 end 'collectMaxonSources'
 
-export enum NestedProjectRule
+module enum NestedProjectRule
 	refused
 	pruned
 end 'NestedProjectRule'
@@ -2432,7 +2432,7 @@ end 'mintedNamesInclude'
 // A call that returns at all lists at least the directory it was handed, so the answer is 1 or more; a
 // root that cannot be listed throws instead. The floor is 0 because the type has no narrower honest
 // bound to state, not because a walk reports it.
-export typealias DirectoryWalkCount = int(0 to u64.max)
+typealias DirectoryWalkCount = int(0 to u64.max)
 
 // The same paths where the question is MEMBERSHIP rather than order, so it is answered by one hash
 // instead of a scan — and each pair a scan would compare is about four String allocations, because
diff --git a/maxon-bin/Compiler/Formatter.maxon b/maxon-bin/Compiler/Formatter.maxon
--- a/maxon-bin/Compiler/Formatter.maxon
+++ b/maxon-bin/Compiler/Formatter.maxon
@@ -178,7 +178,7 @@ typealias FormatBlockKindArray = Array with FormatBlockKind
 /// A block comment with code BOTH before and after it on one line (`var y = /* note */ 5`) cannot be
 /// cleanly relocated by a formatter that does not parse; it comes out as a trailing comment. The
 /// comment is PRESERVED — only its position within the line is lost.
-export type SourceComment
+type SourceComment
 	export let text as String
 	export let wholeLine as bool
 
@@ -1696,7 +1696,7 @@ end 'blockKindOpenedBy'
 ///
 /// The two constructors are the only way in, so no caller can hand the emitter an indent unit that is
 /// not whitespace.
-export type FormatOptions
+module type FormatOptions
 	module let indentUnit as String
 
 	/// One tab per level — the language's own rule, and what `maxon fmt` always wants.
diff --git a/maxon-bin/Compiler/IR/IrFunction.maxon b/maxon-bin/Compiler/IR/IrFunction.maxon
--- a/maxon-bin/Compiler/IR/IrFunction.maxon
+++ b/maxon-bin/Compiler/IR/IrFunction.maxon
@@ -513,7 +513,7 @@ export type LabelledArgumentValues
 end 'LabelledArgumentValues'
 
 // The row a labelled call argument's value was recorded at, by its label's position.
-export typealias LabelledValueRow = int(0 to u64.max)
+typealias LabelledValueRow = int(0 to u64.max)
 export typealias LabelledValueRowByPosition = Map with (PositionKey, LabelledValueRow)
 
 // ⭐ **A NAME BOUND OR ASSIGNED WHOLE TO A MUTABLE NAME, AND THE FIRST READ OF IT AFTER.** Filed where a `let` — or
diff --git a/maxon-bin/Compiler/IR/IrModule.maxon b/maxon-bin/Compiler/IR/IrModule.maxon
--- a/maxon-bin/Compiler/IR/IrModule.maxon
+++ b/maxon-bin/Compiler/IR/IrModule.maxon
@@ -10,7 +10,7 @@
 
 export typealias OpCount = int(0 to u64.max)
 export typealias BlockCount = int(0 to u64.max)
-export typealias FuncCount = int(0 to u64.max)
+module typealias FuncCount = int(0 to u64.max)
 export typealias FuncIndex = int(0 to u64.max)
 
 export typealias BlockRefIndex = int(0 to u64.max)
diff --git a/maxon-bin/Compiler/IR/Maxon/ModuleInit.maxon b/maxon-bin/Compiler/IR/Maxon/ModuleInit.maxon
--- a/maxon-bin/Compiler/IR/Maxon/ModuleInit.maxon
+++ b/maxon-bin/Compiler/IR/Maxon/ModuleInit.maxon
@@ -86,7 +86,7 @@ export function installManagedGlobalInit(module MaxonModule, project Project, gl
 end 'installManagedGlobalInit'
 
 // A `let` `__module_init` marks shared, and the walk that marks it.
-export type SharedLet
+type SharedLet
 	export let global as ManagedGlobal
 	export let walk as ByteArray
 
diff --git a/maxon-bin/Compiler/IR/PassPipeline.maxon b/maxon-bin/Compiler/IR/PassPipeline.maxon
--- a/maxon-bin/Compiler/IR/PassPipeline.maxon
+++ b/maxon-bin/Compiler/IR/PassPipeline.maxon
@@ -20,7 +20,7 @@
 
 // One arm per scheduled pass. Payload-free today; kept a `union` (not an enum)
 // to leave room for passes that carry configuration payloads.
-export union PassKind
+union PassKind
 	// Phase 2: Maxon-level.
 	reportDeferredNames
 	checkSignatureVisibility
diff --git a/maxon-bin/Compiler/IR/Std/StdPassPool.maxon b/maxon-bin/Compiler/IR/Std/StdPassPool.maxon
--- a/maxon-bin/Compiler/IR/Std/StdPassPool.maxon
+++ b/maxon-bin/Compiler/IR/Std/StdPassPool.maxon
@@ -266,7 +266,7 @@ end 'refineVersionedRangesInFunc'
 
 // EVERYTHING ONE WORKER HANDS BACK AT THE END: its per-step costs and tallies, as records nothing else
 // holds (a reply moves).
-module type StdPassWorkerReport
+type StdPassWorkerReport
 	export var costs as PoolPhaseCosts
 	export var tallies as StdPassTallyArray
 
diff --git a/maxon-bin/Compiler/IR/Target/TargetDialect.maxon b/maxon-bin/Compiler/IR/Target/TargetDialect.maxon
--- a/maxon-bin/Compiler/IR/Target/TargetDialect.maxon
+++ b/maxon-bin/Compiler/IR/Target/TargetDialect.maxon
@@ -1889,7 +1889,7 @@ end 'X64AccessWidth'
 // only when the scale IS this size (`indexedFormCarries`), and a future narrow-width access grows a
 // case here rather than a third table. Written as named constants because a bare `1` and `8` beside an
 // enum case say nothing about which fact they are.
-export typealias AccessWidthBytes = int(1 to 8)
+typealias AccessWidthBytes = int(1 to 8)
 
 let SingleByteAccessBytes = 1 as AccessWidthBytes
 let Word64AccessBytes = 8 as AccessWidthBytes
@@ -1924,10 +1924,10 @@ end 'MemoryIndexScale'
 
 // The SHIFT the scale denotes — x64's 2-bit SIB scale field verbatim, and AArch64's `LSL #amount`
 // verbatim. Two bits wide because that is the whole domain (see the enum's header).
-export typealias MemoryIndexScaleShift = int(0 to 3)
+typealias MemoryIndexScaleShift = int(0 to 3)
 
 // The MULTIPLIER the scale denotes — what a Std `binOpImm(mul, index, imm)` carried before the fold.
-export typealias MemoryIndexScaleFactor = int(1 to 8)
+typealias MemoryIndexScaleFactor = int(1 to 8)
 
 // A dense per-value column of scales — what `ScaledIndexFolds` records against each address it matched.
 export typealias MemoryIndexScaleArray = Array with MemoryIndexScale
diff --git a/maxon-bin/Compiler/Mcp/McpTools.maxon b/maxon-bin/Compiler/Mcp/McpTools.maxon
--- a/maxon-bin/Compiler/Mcp/McpTools.maxon
+++ b/maxon-bin/Compiler/Mcp/McpTools.maxon
@@ -25,7 +25,7 @@ var snippetsStaged = 0 as SnippetSerial
 // `repoRootLabel` is empty exactly when the tool acts in the host's working directory: there is no
 // tree to name, and every answer leaves the key out rather than reporting an empty one
 // (`addRepoRootIfKnown`).
-module type ToolContext
+type ToolContext
 	export var compiler as FilePath
 	export var workingDir as FilePath
 	export var repoRootLabel as String
@@ -490,7 +490,7 @@ end 'executeInfoTool'
 // Contributor tools
 // =============================================================================
 
-module union TreeContext
+union TreeContext
 	usable(ctx ToolContext)
 	answered(reply String)
 end 'TreeContext'
diff --git a/maxon-bin/Compiler/PhaseProbe.maxon b/maxon-bin/Compiler/PhaseProbe.maxon
--- a/maxon-bin/Compiler/PhaseProbe.maxon
+++ b/maxon-bin/Compiler/PhaseProbe.maxon
@@ -421,7 +421,7 @@ end 'foldPoolPhaseDelta'
 
 // A position in a `PoolPhaseCosts` — an ordinal of whichever phase enum, or a step of whichever schedule,
 // the holder was sized for.
-export typealias PoolPhaseIndex = int(0 to u64.max)
+typealias PoolPhaseIndex = int(0 to u64.max)
 
 // ⭐⭐ **ONE UNIT OF POOL WORK'S PER-PHASE COSTS, MEASURED WITHOUT A `Project`.**
 //
diff --git a/maxon-bin/Compiler/Project.maxon b/maxon-bin/Compiler/Project.maxon
--- a/maxon-bin/Compiler/Project.maxon
+++ b/maxon-bin/Compiler/Project.maxon
@@ -395,7 +395,7 @@ export function tierOrdinal(visibility Visibility) returns VisibilityTier
 	end 'tier'
 end 'tierOrdinal'
 
-export typealias VisibilityTier = int(0 to 3)
+typealias VisibilityTier = int(0 to 3)
 
 // ⚠ **THE ONE READER THAT RANKS `public` ABOVE `global`** — E3167 asks a CLAIM question, not a REACH one,
 // so the equivalence `isProgramWideTier` states does not hold here: a `public` signature naming an
diff --git a/maxon-bin/Compiler/RawIntrinsics.maxon b/maxon-bin/Compiler/RawIntrinsics.maxon
--- a/maxon-bin/Compiler/RawIntrinsics.maxon
+++ b/maxon-bin/Compiler/RawIntrinsics.maxon
@@ -247,7 +247,7 @@ end 'RawIntrinsic'
 // position is a compile-time integer the emitted instruction CARRIES in place of an operand: a scratch
 // record's size fixes a frame slot, a field offset is encoded in the addressing mode, and a fill byte is
 // broadcast into an immediate, so none of the three can be a value computed at run time.
-export enum RawArgShape
+enum RawArgShape
 	nothing
 	oneWord
 	twoWords
diff --git a/maxon-bin/Compiler/Runtime/ManagedSocketRuntime.maxon b/maxon-bin/Compiler/Runtime/ManagedSocketRuntime.maxon
--- a/maxon-bin/Compiler/Runtime/ManagedSocketRuntime.maxon
+++ b/maxon-bin/Compiler/Runtime/ManagedSocketRuntime.maxon
@@ -598,7 +598,7 @@ end 'emitSocketPollerRelease'
 // be that written TWICE, once per Linux arch, with two chances to get a label-length check wrong. Written
 // here it is one body compiled by the isel that already lowers every op it uses, which is the same
 // arrangement that lets `mrt_host_proc_reap` serve both POSIX lanes from one place.
-export enum SocketResolveShape
+enum SocketResolveShape
 	// Windows and macOS: `getaddrinfo` fills a linked list of `struct addrinfo` and the graph reads the
 	// first entry's `ai_addr`. ⚠ **THE `ai_addr` OFFSET IS THE SAME 32 ON BOTH, AND THAT IS MEASURED RATHER
 	// THAN ASSUMED** — Darwin orders `ai_canonname` before `ai_addr` exactly as Win64 does (`offsetof` on
diff --git a/maxon-bin/Compiler/Runtime/SlabClassTable.maxon b/maxon-bin/Compiler/Runtime/SlabClassTable.maxon
--- a/maxon-bin/Compiler/Runtime/SlabClassTable.maxon
+++ b/maxon-bin/Compiler/Runtime/SlabClassTable.maxon
@@ -154,7 +154,7 @@ end 'slabClassCount'
 
 // The ladder holds the sentinel plus the real classes, so it is never empty; the upper bound is a byte's
 // worth, which is what one reverse-table entry can name.
-export typealias SlabClassCount = int(1 to 256)
+typealias SlabClassCount = int(1 to 256)
 
 // --- The two reverse lookups ---
 //
diff --git a/maxon-bin/Compiler/Runtime/SlabRuntime.maxon b/maxon-bin/Compiler/Runtime/SlabRuntime.maxon
--- a/maxon-bin/Compiler/Runtime/SlabRuntime.maxon
+++ b/maxon-bin/Compiler/Runtime/SlabRuntime.maxon
@@ -1090,7 +1090,7 @@ end 'slabShardProc'
 // into it carry `SlabRawShardRow` — that is what "serialised" means here, a thread serialising exactly when
 // the row it takes is the shared one — so both of that row's addresses are emitted inside the arm and no
 // edge carries either.
-export type SlabShardArms
+type SlabShardArms
 	export let schedulerWord as ValueId
 	export let sole as SlabShardSite
 	export let serial as SlabShardSite
diff --git a/maxon-bin/Compiler/SignatureIndex.maxon b/maxon-bin/Compiler/SignatureIndex.maxon
--- a/maxon-bin/Compiler/SignatureIndex.maxon
+++ b/maxon-bin/Compiler/SignatureIndex.maxon
@@ -5585,7 +5585,7 @@ export union ManagedCascadeStrategy
 end 'ManagedCascadeStrategy'
 
 // What a graph handed to another green thread is, as far as walking it goes.
-export union TransferSubject
+union TransferSubject
 	// A String or a Character: its bytes can be viewed, and a view is an owner no compile-time fact sees.
 	byteRecord
 	instance(id GenericInstanceId)
@@ -8371,7 +8371,7 @@ typealias CanonicalInstanceNameIndex = Map with (ByteArray, GenericInstanceId)
 // no id: it IS its name (`ProgramSignatures.tupleCanonicalNames`).
 export typealias TupleCanonicalNameMap = Map with (ByteArray, ByteArray)
 
-module union ScopedGenericAliasDecl
+union ScopedGenericAliasDecl
 	declared(decl GenericAliasEntry)
 	undeclared
 end 'ScopedGenericAliasDecl'
diff --git a/maxon-bin/Compiler/Targets/Arm64/Arm64Backend.maxon b/maxon-bin/Compiler/Targets/Arm64/Arm64Backend.maxon
--- a/maxon-bin/Compiler/Targets/Arm64/Arm64Backend.maxon
+++ b/maxon-bin/Compiler/Targets/Arm64/Arm64Backend.maxon
@@ -1444,7 +1444,7 @@ end 'arm64NonNegDisp'
 // it targets. `wide` selects the field width — an unconditional `b` carries a 26-bit imm (±128 MiB),
 // a `b.cond`/`cbz`/`cbnz` a 19-bit imm (±1 MiB). A function's blocks are contiguous within its one
 // chunk, so these resolve HERE (like x64's `resolveChunkRel32Fixups`), independent of concat.
-export type Arm64BlockBranchFixup
+type Arm64BlockBranchFixup
 	export var codeOffset as CodeOffset
 	export var targetBlock as BlockId
 	export var wide as bool
diff --git a/maxon-bin/Compiler/Targets/Shared/BackendPool.maxon b/maxon-bin/Compiler/Targets/Shared/BackendPool.maxon
--- a/maxon-bin/Compiler/Targets/Shared/BackendPool.maxon
+++ b/maxon-bin/Compiler/Targets/Shared/BackendPool.maxon
@@ -75,7 +75,7 @@ export typealias PoolJobSize = int(0 to u64.max)
 export typealias PoolJobSizeArray = Array with PoolJobSize
 
 // A position in a pool's landing array — where one job's answer was pushed as it arrived.
-export typealias LandingSlot = int(0 to u64.max)
+typealias LandingSlot = int(0 to u64.max)
 export typealias LandingSlotArray = Array with LandingSlot
 
 // ⭐ **WHERE EACH JOB'S ANSWER LANDED — the column that turns arrival order back into job order.** A drain
@@ -331,7 +331,7 @@ typealias LongestFirstKey = int(0 to u64.max)
 typealias LongestFirstKeyArray = Array with LongestFirstKey
 
 // The isel of one ISA, as a worker holds it for the whole compile.
-module union UnitLowering
+union UnitLowering
 	x64(lowering StdToX64Lowering)
 	arm64(lowering StdToArm64Lowering)
 end 'UnitLowering'
diff --git a/maxon-bin/Compiler/Targets/Shared/BacktraceFormat.maxon b/maxon-bin/Compiler/Targets/Shared/BacktraceFormat.maxon
--- a/maxon-bin/Compiler/Targets/Shared/BacktraceFormat.maxon
+++ b/maxon-bin/Compiler/Targets/Shared/BacktraceFormat.maxon
@@ -109,7 +109,7 @@ let SymtabNotScaffolding = 0
 // The widest unsafe prefix, in granules, the byte's seven safe-point bits can carry. A guard past
 // it is refused rather than truncated into a safe point that is not one.
 let SymtabMaxUnsafeGranules = SymtabSafeFromMask - SymtabSafeFromBias
-export typealias SafePointGranule = int(1 to 4)
+typealias SafePointGranule = int(1 to 4)
 
 let X64SafePointGranule = 1 as SafePointGranule
 
diff --git a/maxon-bin/Compiler/Targets/Shared/CodeResult.maxon b/maxon-bin/Compiler/Targets/Shared/CodeResult.maxon
--- a/maxon-bin/Compiler/Targets/Shared/CodeResult.maxon
+++ b/maxon-bin/Compiler/Targets/Shared/CodeResult.maxon
@@ -230,7 +230,7 @@ export let JumpTableSlotBytes = 4 as BytePos
 // is slot `i`'s target block's `.text` offset — CHUNK-LOCAL when the chunk emits it, rebased to a whole-`.text`
 // offset by the concat pass (`+ funcStart`, exactly as `Arm64FuncAdrpReloc.textOffset` is resolved). One
 // record per switch (not per slot), so the whole N-slot table is patched from a single walk.
-export type JumpTableReloc
+type JumpTableReloc
 	export var tableLabel as String
 	export var targetTextOffsets as CodeOffsetArray
 
diff --git a/maxon-bin/Compiler/Targets/Shared/RegBits.maxon b/maxon-bin/Compiler/Targets/Shared/RegBits.maxon
--- a/maxon-bin/Compiler/Targets/Shared/RegBits.maxon
+++ b/maxon-bin/Compiler/Targets/Shared/RegBits.maxon
@@ -24,7 +24,7 @@ export typealias BitWordCount = int(0 to u64.max)
 
 // A bit's position within ONE 64-bit word. HARD BOUND: there are 64 of them, so a value outside
 // 0..63 is a bug in the word arithmetic, not a legal position.
-export typealias WordBitIndex = int(0 to 63)
+typealias WordBitIndex = int(0 to 63)
 
 // How many bits are SET in one 64-bit word. HARD BOUND at 64 — a full word — which is one MORE
 // than `WordBitIndex` admits, so the two are not interchangeable.
diff --git a/maxon-bin/Compiler/Targets/Shared/SsaDestruction.maxon b/maxon-bin/Compiler/Targets/Shared/SsaDestruction.maxon
--- a/maxon-bin/Compiler/Targets/Shared/SsaDestruction.maxon
+++ b/maxon-bin/Compiler/Targets/Shared/SsaDestruction.maxon
@@ -44,7 +44,7 @@ end 'EdgePlacement'
 // encoder, from `regClassOf` of the register (`emitRegRegMove` / `emitRegRegSwap` in X64Backend).
 // That is why floats needed no case here: a `mov` becomes a `movsd`, and a `swap` — which x64
 // has no `xchg` form of for XMMs — becomes a triple-`xorpd`, without this file knowing either.
-export type EdgeMovePlan
+type EdgeMovePlan
 	export var predLocal as CfgIndex
 	export var succLocal as CfgIndex
 	export var predBlockId as BlockId
diff --git a/maxon-bin/Compiler/Targets/X64/X64Runtime.maxon b/maxon-bin/Compiler/Targets/X64/X64Runtime.maxon
--- a/maxon-bin/Compiler/Targets/X64/X64Runtime.maxon
+++ b/maxon-bin/Compiler/Targets/X64/X64Runtime.maxon
@@ -80,7 +80,7 @@ let VehContinueSearch = 0
 
 // A Windows NTSTATUS exception code. A DWORD — which is also the width `EXCEPTION_RECORD` stores it
 // at and the width the thunk loads it with, so the comparand and the loaded value are one type.
-export typealias ExceptionCode = int(0 to u32.max)
+typealias ExceptionCode = int(0 to u32.max)
 
 // The two NTSTATUS codes Windows delivers for the `#DE` that `idiv` raises. `idiv` raises the SAME trap
 // for a zero divisor and for a quotient that does not fit (`i64.min / -1`), and the CPU distinguishes them
diff --git a/maxon-bin/Debug/DebugCommand.maxon b/maxon-bin/Debug/DebugCommand.maxon
--- a/maxon-bin/Debug/DebugCommand.maxon
+++ b/maxon-bin/Debug/DebugCommand.maxon
@@ -83,7 +83,7 @@ let MaxCodeOffset = u32.max as OffsetAccumulator
 typealias DigitValue = int(0 to 15)
 
 // How many leading characters of a token are its base prefix rather than digits.
-module typealias PrefixLength = int(0 to u32.max)
+typealias PrefixLength = int(0 to u32.max)
 
 // Whether a command-line token named an offset at all. A union rather than a `-1`, because "not a
 // number" is a refusal the caller reports by name and 0 is a perfectly good offset.
diff --git a/maxon-bin/Main.maxon b/maxon-bin/Main.maxon
--- a/maxon-bin/Main.maxon
+++ b/maxon-bin/Main.maxon
@@ -398,7 +398,7 @@ let ProductPathSeparator = "/"
 let DescribedBuildsOpen = "["
 let DescribedBuildsClose = "]"
 
-module typealias DelegationDepth = int(0 to 64)
+typealias DelegationDepth = int(0 to 64)
 
 module union ManifestBase
 	workingDirectory
@@ -1383,7 +1383,7 @@ module function refusedPositionalBeyond(args MaxonArgs, allowed PositionalCount,
 	return true
 end 'refusedPositionalBeyond'
 
-module typealias PositionalCount = int(0 to 1)
+typealias PositionalCount = int(0 to 1)
 
 // A command's streams belong to its subject: at the default INFO level every compile announces the
 // bytes it wrote, which is noise on `test`'s fixture-compared stdout, on `run`'s program streams,
@@ -2211,7 +2211,7 @@ export typealias SourcePathSpellingArray = Array with String
 export typealias FilterPatternArray = Array with String
 
 // `module` because `Testing/ScaleTestRunner` takes the parsed args.
-export type MaxonArgs
+module type MaxonArgs
 	export var command as MaxonCommand
 
 	// The order is load-bearing: `build` compiles every positional, and `Compiler.loadSourceFiles`
