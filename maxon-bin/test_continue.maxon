// Test while loop with continue
function main() int
    var x = 0
    var count = 0
    while count < 10 'loop'
        count = count + 1
        if count == 5 'skip'
            continue
        end 'skip'
        x = x + count
    end 'loop'
    return x
end 'main'
