// Second test project - isolated from first project
// This project has its own helper function with different behavior

function main() returns int
	return calculate(10)
end 'main'

function calculate(x int) returns int
	return x + 100
end 'calculate'
