// Test: challenge-sized-arrays.sized-array-all-elements-writable.1
function main() returns int
    var arr = Array of 5 int
    arr[0] = 1
    arr[1] = 2
    arr[2] = 3
    arr[3] = 4
    arr[4] = 5
    var sum = 0
    if let a = arr[0] 'g0'
        sum = sum + a
    end 'g0'
    if let b = arr[1] 'g1'
        sum = sum + b
    end 'g1'
    if let c = arr[2] 'g2'
        sum = sum + c
    end 'g2'
    if let d = arr[3] 'g3'
        sum = sum + d
    end 'g3'
    if let e = arr[4] 'g4'
        sum = sum + e
    end 'g4'
    return sum
end 'main'
---
ExitCode: 15
---
// IR generation failed: error.IrError
---
