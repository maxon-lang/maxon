// Test: error-handling.error.enum-string-backed-error.1
// String-backed enum error type (type inferred from values)
enum MyError is Error
    invalidInput = "Invalid input"
    notFound = "Not found"
end 'MyError'

function main() returns int
    return 42
end 'main'
---
ExitCode: 42
---
function main() -> i64 {
entry:
    %tmp_const = const.i64 i64 42
    ret %tmp_const
}

---
