// Test: alloc-tracking.array-borrow-multiple-times.1
function getElement(arr Array of int, idx int) returns int
    if let val = arr[idx] 'get'
        return val
    end 'get' else 'nil'
        return 0
    end 'nil'
end 'getElement'

function main() returns int
    let size = 5
    var arr = Array of size int
    arr[0] = 10
    arr[1] = 20
    arr[2] = 30
    let a = getElement(arr, 0)
    let b = getElement(arr, 1)
    let c = getElement(arr, 2)
    return a + b + c
end 'main'
---
ExitCode: 60
TrackAllocs: true
Stdout: ```
ALLOC #1: 40 bytes (dynamic array)
FREE #1: 40 bytes (dynamic array)

=== ALLOC STATS ===
Allocated: 40 bytes
Freed:     40 bytes
Leaked:    0 bytes
```
---
// IR generation failed: error.ParserError
---
