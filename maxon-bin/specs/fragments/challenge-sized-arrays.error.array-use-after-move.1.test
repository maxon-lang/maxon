// Test: challenge-sized-arrays.error.array-use-after-move.1
function mutate(arr Array of 3 int) returns int
    arr.set(0, value: 100)
    return try arr.get(0) otherwise 0
end 'mutate'

function main() returns int
    var arr = Array of 3 int
    arr.set(0, value: 42)
    var result = mutate(arr)
    var val = try arr.get(0) otherwise 0
    return val + result
end 'main'
---
MaxoncStderr: ```
error E008: specs/fragments/challenge-sized-arrays.error.array-use-after-move.1.test:11:5: use after move: 'arr'
```
---
// IR: N/A (compiler error test)
---
