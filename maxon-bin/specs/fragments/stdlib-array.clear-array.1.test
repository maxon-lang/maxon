// Test: stdlib-array.clear-array.1
function main() returns int
    var arr = Array of int{}
    arr.push(10)
    arr.push(20)
    arr.push(30)
    
    arr.clear()
    
    if arr.count() != 0 'cnt'
        return 1
    end 'cnt'
    
    if arr.isEmpty() == false 'empty'
        return 2
    end 'empty'
    
    return 0
end 'main'
---
ExitCode: 0
---
// IR generation failed: error.IrError
---
