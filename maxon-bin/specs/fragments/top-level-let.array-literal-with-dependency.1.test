// Test: top-level-let.array-literal-with-dependency.1
let FIRST = 10
let SECOND = 20
let values = [FIRST, SECOND, 30]

function main() returns int
    return (values[0] else 0) + (values[1] else 0) + (values[2] else 0)
end 'main'
---
ExitCode: 60
---
