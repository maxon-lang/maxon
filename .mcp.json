{
  "mcpServers": {
    "maxon-dev": {
      "type": "stdio",
      "command": "./maxon-bin/.maxon/maxon",
      "args": ["mcp-server", "--dev"],
      "description": "Maxon compiler development tools: build, spec-test, scale-test, error-code lookup, IR dump."
    }
  }
}