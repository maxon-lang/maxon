
4. Iterator Invalidation
This is a specific type of logic error common in languages like C++ or Java.

The Problem: You are iterating through a list, and inside the loop, you add or remove an element. This often causes the internal pointer of the iterator to point to garbage or skip elements.

Rust's Solution: While you have an active iterator (which "borrows" the list), the borrow checker prevents you from modifying the list. The code simply won't compile if you try to push an item into a collection you are currently looping over.




3. Data Races
A data race occurs in multi-threaded programming when two or more threads access the same memory location concurrently, and at least one of them is writing.

The Problem: Because the timing of threads is unpredictable, the final value of the memory becomes non-deterministic, leading to "Heisenbugs" that are nearly impossible to debug.

Rust's Solution: The borrow checker enforces the Aliasing XOR Mutation rule: You can have many "readers" (immutable references) OR exactly one "writer" (mutable reference), but never both at the same time.


- lexer: source -> tokens
- parser: tokens -> AST
- semantic: check AST for errors
- optimizer: AST -> better AST
- codegen: AST -> IR
- x86gen: IR -> byte code
- pe_writer: byte code -> exe file


With Semantic IR, your architecture looks like this:
Lexer/Parser: Text $\rightarrow$ AST.
Semantic Analysis: AST $\rightarrow$ Semantic IR. (Type checking, constant folding, identifying unrollable loops).
Lowering: Semantic IR $\rightarrow$ Smart Blocks (LIR). (Selecting the optimized backend blocks).
Code Gen: Smart Blocks $\rightarrow$ Machine Code. (Blindly writing bytes).

Everything declared is an instance
Every instance has a owner
Every owner has a scope
If the instance goes out of scope it is freed

Instance passed to a function by reference
If the function does not modify it then its pure
If the function does modify it then it is automatically borrowed
Code trying to modify borrowed instance is an error

b4:35:22:ff:fe:c1:97:b4

- types
- enums
- strings
- arrays
- generics
- ownership
- managed



FFI boundaries (exit(), Windows APIs) - intentionally Int32
Safe FFI code - uses Int32 for FFI protocol
Refcount functions - match runtime headers (i32)
Runtime memory headers - out of scope for this fix
Backend x86_codegen.cpp Int32 branches - handle legitimate Int32 values



var s = "hello"
// set capacity
s.reserveCapacity(20)

s = "{s} world" // optimize this to an append
